#include "lib.h"
#include <iostream>
int func_16000(int& val) {
    func_in_16000(val);
    const int x = 16000;
    std::cout << 16000;
    for (int i = 0; i < 16000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16000(int& val) {
    const int x = 16000;
    std::cout << 16000;
    for (int i = 0; i < 16000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16001(int& val) {
    func_in_16001(val);
    const int x = 16001;
    std::cout << 16001;
    for (int i = 0; i < 16001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16001(int& val) {
    const int x = 16001;
    std::cout << 16001;
    for (int i = 0; i < 16001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16002(int& val) {
    func_in_16002(val);
    const int x = 16002;
    std::cout << 16002;
    for (int i = 0; i < 16002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16002(int& val) {
    const int x = 16002;
    std::cout << 16002;
    for (int i = 0; i < 16002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16003(int& val) {
    func_in_16003(val);
    const int x = 16003;
    std::cout << 16003;
    for (int i = 0; i < 16003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16003(int& val) {
    const int x = 16003;
    std::cout << 16003;
    for (int i = 0; i < 16003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16004(int& val) {
    func_in_16004(val);
    const int x = 16004;
    std::cout << 16004;
    for (int i = 0; i < 16004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16004(int& val) {
    const int x = 16004;
    std::cout << 16004;
    for (int i = 0; i < 16004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16005(int& val) {
    func_in_16005(val);
    const int x = 16005;
    std::cout << 16005;
    for (int i = 0; i < 16005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16005(int& val) {
    const int x = 16005;
    std::cout << 16005;
    for (int i = 0; i < 16005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16006(int& val) {
    func_in_16006(val);
    const int x = 16006;
    std::cout << 16006;
    for (int i = 0; i < 16006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16006(int& val) {
    const int x = 16006;
    std::cout << 16006;
    for (int i = 0; i < 16006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16007(int& val) {
    func_in_16007(val);
    const int x = 16007;
    std::cout << 16007;
    for (int i = 0; i < 16007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16007(int& val) {
    const int x = 16007;
    std::cout << 16007;
    for (int i = 0; i < 16007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16008(int& val) {
    func_in_16008(val);
    const int x = 16008;
    std::cout << 16008;
    for (int i = 0; i < 16008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16008(int& val) {
    const int x = 16008;
    std::cout << 16008;
    for (int i = 0; i < 16008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16009(int& val) {
    func_in_16009(val);
    const int x = 16009;
    std::cout << 16009;
    for (int i = 0; i < 16009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16009(int& val) {
    const int x = 16009;
    std::cout << 16009;
    for (int i = 0; i < 16009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16010(int& val) {
    func_in_16010(val);
    const int x = 16010;
    std::cout << 16010;
    for (int i = 0; i < 16010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16010(int& val) {
    const int x = 16010;
    std::cout << 16010;
    for (int i = 0; i < 16010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16011(int& val) {
    func_in_16011(val);
    const int x = 16011;
    std::cout << 16011;
    for (int i = 0; i < 16011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16011(int& val) {
    const int x = 16011;
    std::cout << 16011;
    for (int i = 0; i < 16011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16012(int& val) {
    func_in_16012(val);
    const int x = 16012;
    std::cout << 16012;
    for (int i = 0; i < 16012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16012(int& val) {
    const int x = 16012;
    std::cout << 16012;
    for (int i = 0; i < 16012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16013(int& val) {
    func_in_16013(val);
    const int x = 16013;
    std::cout << 16013;
    for (int i = 0; i < 16013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16013(int& val) {
    const int x = 16013;
    std::cout << 16013;
    for (int i = 0; i < 16013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16014(int& val) {
    func_in_16014(val);
    const int x = 16014;
    std::cout << 16014;
    for (int i = 0; i < 16014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16014(int& val) {
    const int x = 16014;
    std::cout << 16014;
    for (int i = 0; i < 16014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16015(int& val) {
    func_in_16015(val);
    const int x = 16015;
    std::cout << 16015;
    for (int i = 0; i < 16015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16015(int& val) {
    const int x = 16015;
    std::cout << 16015;
    for (int i = 0; i < 16015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16016(int& val) {
    func_in_16016(val);
    const int x = 16016;
    std::cout << 16016;
    for (int i = 0; i < 16016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16016(int& val) {
    const int x = 16016;
    std::cout << 16016;
    for (int i = 0; i < 16016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16017(int& val) {
    func_in_16017(val);
    const int x = 16017;
    std::cout << 16017;
    for (int i = 0; i < 16017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16017(int& val) {
    const int x = 16017;
    std::cout << 16017;
    for (int i = 0; i < 16017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16018(int& val) {
    func_in_16018(val);
    const int x = 16018;
    std::cout << 16018;
    for (int i = 0; i < 16018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16018(int& val) {
    const int x = 16018;
    std::cout << 16018;
    for (int i = 0; i < 16018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16019(int& val) {
    func_in_16019(val);
    const int x = 16019;
    std::cout << 16019;
    for (int i = 0; i < 16019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16019(int& val) {
    const int x = 16019;
    std::cout << 16019;
    for (int i = 0; i < 16019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16020(int& val) {
    func_in_16020(val);
    const int x = 16020;
    std::cout << 16020;
    for (int i = 0; i < 16020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16020(int& val) {
    const int x = 16020;
    std::cout << 16020;
    for (int i = 0; i < 16020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16021(int& val) {
    func_in_16021(val);
    const int x = 16021;
    std::cout << 16021;
    for (int i = 0; i < 16021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16021(int& val) {
    const int x = 16021;
    std::cout << 16021;
    for (int i = 0; i < 16021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16022(int& val) {
    func_in_16022(val);
    const int x = 16022;
    std::cout << 16022;
    for (int i = 0; i < 16022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16022(int& val) {
    const int x = 16022;
    std::cout << 16022;
    for (int i = 0; i < 16022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16023(int& val) {
    func_in_16023(val);
    const int x = 16023;
    std::cout << 16023;
    for (int i = 0; i < 16023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16023(int& val) {
    const int x = 16023;
    std::cout << 16023;
    for (int i = 0; i < 16023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16024(int& val) {
    func_in_16024(val);
    const int x = 16024;
    std::cout << 16024;
    for (int i = 0; i < 16024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16024(int& val) {
    const int x = 16024;
    std::cout << 16024;
    for (int i = 0; i < 16024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16025(int& val) {
    func_in_16025(val);
    const int x = 16025;
    std::cout << 16025;
    for (int i = 0; i < 16025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16025(int& val) {
    const int x = 16025;
    std::cout << 16025;
    for (int i = 0; i < 16025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16026(int& val) {
    func_in_16026(val);
    const int x = 16026;
    std::cout << 16026;
    for (int i = 0; i < 16026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16026(int& val) {
    const int x = 16026;
    std::cout << 16026;
    for (int i = 0; i < 16026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16027(int& val) {
    func_in_16027(val);
    const int x = 16027;
    std::cout << 16027;
    for (int i = 0; i < 16027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16027(int& val) {
    const int x = 16027;
    std::cout << 16027;
    for (int i = 0; i < 16027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16028(int& val) {
    func_in_16028(val);
    const int x = 16028;
    std::cout << 16028;
    for (int i = 0; i < 16028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16028(int& val) {
    const int x = 16028;
    std::cout << 16028;
    for (int i = 0; i < 16028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16029(int& val) {
    func_in_16029(val);
    const int x = 16029;
    std::cout << 16029;
    for (int i = 0; i < 16029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16029(int& val) {
    const int x = 16029;
    std::cout << 16029;
    for (int i = 0; i < 16029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16030(int& val) {
    func_in_16030(val);
    const int x = 16030;
    std::cout << 16030;
    for (int i = 0; i < 16030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16030(int& val) {
    const int x = 16030;
    std::cout << 16030;
    for (int i = 0; i < 16030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16031(int& val) {
    func_in_16031(val);
    const int x = 16031;
    std::cout << 16031;
    for (int i = 0; i < 16031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16031(int& val) {
    const int x = 16031;
    std::cout << 16031;
    for (int i = 0; i < 16031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16032(int& val) {
    func_in_16032(val);
    const int x = 16032;
    std::cout << 16032;
    for (int i = 0; i < 16032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16032(int& val) {
    const int x = 16032;
    std::cout << 16032;
    for (int i = 0; i < 16032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16033(int& val) {
    func_in_16033(val);
    const int x = 16033;
    std::cout << 16033;
    for (int i = 0; i < 16033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16033(int& val) {
    const int x = 16033;
    std::cout << 16033;
    for (int i = 0; i < 16033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16034(int& val) {
    func_in_16034(val);
    const int x = 16034;
    std::cout << 16034;
    for (int i = 0; i < 16034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16034(int& val) {
    const int x = 16034;
    std::cout << 16034;
    for (int i = 0; i < 16034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16035(int& val) {
    func_in_16035(val);
    const int x = 16035;
    std::cout << 16035;
    for (int i = 0; i < 16035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16035(int& val) {
    const int x = 16035;
    std::cout << 16035;
    for (int i = 0; i < 16035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16036(int& val) {
    func_in_16036(val);
    const int x = 16036;
    std::cout << 16036;
    for (int i = 0; i < 16036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16036(int& val) {
    const int x = 16036;
    std::cout << 16036;
    for (int i = 0; i < 16036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16037(int& val) {
    func_in_16037(val);
    const int x = 16037;
    std::cout << 16037;
    for (int i = 0; i < 16037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16037(int& val) {
    const int x = 16037;
    std::cout << 16037;
    for (int i = 0; i < 16037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16038(int& val) {
    func_in_16038(val);
    const int x = 16038;
    std::cout << 16038;
    for (int i = 0; i < 16038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16038(int& val) {
    const int x = 16038;
    std::cout << 16038;
    for (int i = 0; i < 16038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16039(int& val) {
    func_in_16039(val);
    const int x = 16039;
    std::cout << 16039;
    for (int i = 0; i < 16039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16039(int& val) {
    const int x = 16039;
    std::cout << 16039;
    for (int i = 0; i < 16039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16040(int& val) {
    func_in_16040(val);
    const int x = 16040;
    std::cout << 16040;
    for (int i = 0; i < 16040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16040(int& val) {
    const int x = 16040;
    std::cout << 16040;
    for (int i = 0; i < 16040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16041(int& val) {
    func_in_16041(val);
    const int x = 16041;
    std::cout << 16041;
    for (int i = 0; i < 16041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16041(int& val) {
    const int x = 16041;
    std::cout << 16041;
    for (int i = 0; i < 16041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16042(int& val) {
    func_in_16042(val);
    const int x = 16042;
    std::cout << 16042;
    for (int i = 0; i < 16042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16042(int& val) {
    const int x = 16042;
    std::cout << 16042;
    for (int i = 0; i < 16042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16043(int& val) {
    func_in_16043(val);
    const int x = 16043;
    std::cout << 16043;
    for (int i = 0; i < 16043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16043(int& val) {
    const int x = 16043;
    std::cout << 16043;
    for (int i = 0; i < 16043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16044(int& val) {
    func_in_16044(val);
    const int x = 16044;
    std::cout << 16044;
    for (int i = 0; i < 16044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16044(int& val) {
    const int x = 16044;
    std::cout << 16044;
    for (int i = 0; i < 16044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16045(int& val) {
    func_in_16045(val);
    const int x = 16045;
    std::cout << 16045;
    for (int i = 0; i < 16045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16045(int& val) {
    const int x = 16045;
    std::cout << 16045;
    for (int i = 0; i < 16045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16046(int& val) {
    func_in_16046(val);
    const int x = 16046;
    std::cout << 16046;
    for (int i = 0; i < 16046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16046(int& val) {
    const int x = 16046;
    std::cout << 16046;
    for (int i = 0; i < 16046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16047(int& val) {
    func_in_16047(val);
    const int x = 16047;
    std::cout << 16047;
    for (int i = 0; i < 16047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16047(int& val) {
    const int x = 16047;
    std::cout << 16047;
    for (int i = 0; i < 16047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16048(int& val) {
    func_in_16048(val);
    const int x = 16048;
    std::cout << 16048;
    for (int i = 0; i < 16048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16048(int& val) {
    const int x = 16048;
    std::cout << 16048;
    for (int i = 0; i < 16048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16049(int& val) {
    func_in_16049(val);
    const int x = 16049;
    std::cout << 16049;
    for (int i = 0; i < 16049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16049(int& val) {
    const int x = 16049;
    std::cout << 16049;
    for (int i = 0; i < 16049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16050(int& val) {
    func_in_16050(val);
    const int x = 16050;
    std::cout << 16050;
    for (int i = 0; i < 16050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16050(int& val) {
    const int x = 16050;
    std::cout << 16050;
    for (int i = 0; i < 16050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16051(int& val) {
    func_in_16051(val);
    const int x = 16051;
    std::cout << 16051;
    for (int i = 0; i < 16051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16051(int& val) {
    const int x = 16051;
    std::cout << 16051;
    for (int i = 0; i < 16051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16052(int& val) {
    func_in_16052(val);
    const int x = 16052;
    std::cout << 16052;
    for (int i = 0; i < 16052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16052(int& val) {
    const int x = 16052;
    std::cout << 16052;
    for (int i = 0; i < 16052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16053(int& val) {
    func_in_16053(val);
    const int x = 16053;
    std::cout << 16053;
    for (int i = 0; i < 16053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16053(int& val) {
    const int x = 16053;
    std::cout << 16053;
    for (int i = 0; i < 16053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16054(int& val) {
    func_in_16054(val);
    const int x = 16054;
    std::cout << 16054;
    for (int i = 0; i < 16054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16054(int& val) {
    const int x = 16054;
    std::cout << 16054;
    for (int i = 0; i < 16054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16055(int& val) {
    func_in_16055(val);
    const int x = 16055;
    std::cout << 16055;
    for (int i = 0; i < 16055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16055(int& val) {
    const int x = 16055;
    std::cout << 16055;
    for (int i = 0; i < 16055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16056(int& val) {
    func_in_16056(val);
    const int x = 16056;
    std::cout << 16056;
    for (int i = 0; i < 16056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16056(int& val) {
    const int x = 16056;
    std::cout << 16056;
    for (int i = 0; i < 16056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16057(int& val) {
    func_in_16057(val);
    const int x = 16057;
    std::cout << 16057;
    for (int i = 0; i < 16057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16057(int& val) {
    const int x = 16057;
    std::cout << 16057;
    for (int i = 0; i < 16057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16058(int& val) {
    func_in_16058(val);
    const int x = 16058;
    std::cout << 16058;
    for (int i = 0; i < 16058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16058(int& val) {
    const int x = 16058;
    std::cout << 16058;
    for (int i = 0; i < 16058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16059(int& val) {
    func_in_16059(val);
    const int x = 16059;
    std::cout << 16059;
    for (int i = 0; i < 16059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16059(int& val) {
    const int x = 16059;
    std::cout << 16059;
    for (int i = 0; i < 16059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16060(int& val) {
    func_in_16060(val);
    const int x = 16060;
    std::cout << 16060;
    for (int i = 0; i < 16060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16060(int& val) {
    const int x = 16060;
    std::cout << 16060;
    for (int i = 0; i < 16060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16061(int& val) {
    func_in_16061(val);
    const int x = 16061;
    std::cout << 16061;
    for (int i = 0; i < 16061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16061(int& val) {
    const int x = 16061;
    std::cout << 16061;
    for (int i = 0; i < 16061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16062(int& val) {
    func_in_16062(val);
    const int x = 16062;
    std::cout << 16062;
    for (int i = 0; i < 16062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16062(int& val) {
    const int x = 16062;
    std::cout << 16062;
    for (int i = 0; i < 16062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16063(int& val) {
    func_in_16063(val);
    const int x = 16063;
    std::cout << 16063;
    for (int i = 0; i < 16063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16063(int& val) {
    const int x = 16063;
    std::cout << 16063;
    for (int i = 0; i < 16063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16064(int& val) {
    func_in_16064(val);
    const int x = 16064;
    std::cout << 16064;
    for (int i = 0; i < 16064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16064(int& val) {
    const int x = 16064;
    std::cout << 16064;
    for (int i = 0; i < 16064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16065(int& val) {
    func_in_16065(val);
    const int x = 16065;
    std::cout << 16065;
    for (int i = 0; i < 16065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16065(int& val) {
    const int x = 16065;
    std::cout << 16065;
    for (int i = 0; i < 16065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16066(int& val) {
    func_in_16066(val);
    const int x = 16066;
    std::cout << 16066;
    for (int i = 0; i < 16066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16066(int& val) {
    const int x = 16066;
    std::cout << 16066;
    for (int i = 0; i < 16066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16067(int& val) {
    func_in_16067(val);
    const int x = 16067;
    std::cout << 16067;
    for (int i = 0; i < 16067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16067(int& val) {
    const int x = 16067;
    std::cout << 16067;
    for (int i = 0; i < 16067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16068(int& val) {
    func_in_16068(val);
    const int x = 16068;
    std::cout << 16068;
    for (int i = 0; i < 16068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16068(int& val) {
    const int x = 16068;
    std::cout << 16068;
    for (int i = 0; i < 16068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16069(int& val) {
    func_in_16069(val);
    const int x = 16069;
    std::cout << 16069;
    for (int i = 0; i < 16069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16069(int& val) {
    const int x = 16069;
    std::cout << 16069;
    for (int i = 0; i < 16069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16070(int& val) {
    func_in_16070(val);
    const int x = 16070;
    std::cout << 16070;
    for (int i = 0; i < 16070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16070(int& val) {
    const int x = 16070;
    std::cout << 16070;
    for (int i = 0; i < 16070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16071(int& val) {
    func_in_16071(val);
    const int x = 16071;
    std::cout << 16071;
    for (int i = 0; i < 16071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16071(int& val) {
    const int x = 16071;
    std::cout << 16071;
    for (int i = 0; i < 16071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16072(int& val) {
    func_in_16072(val);
    const int x = 16072;
    std::cout << 16072;
    for (int i = 0; i < 16072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16072(int& val) {
    const int x = 16072;
    std::cout << 16072;
    for (int i = 0; i < 16072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16073(int& val) {
    func_in_16073(val);
    const int x = 16073;
    std::cout << 16073;
    for (int i = 0; i < 16073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16073(int& val) {
    const int x = 16073;
    std::cout << 16073;
    for (int i = 0; i < 16073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16074(int& val) {
    func_in_16074(val);
    const int x = 16074;
    std::cout << 16074;
    for (int i = 0; i < 16074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16074(int& val) {
    const int x = 16074;
    std::cout << 16074;
    for (int i = 0; i < 16074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16075(int& val) {
    func_in_16075(val);
    const int x = 16075;
    std::cout << 16075;
    for (int i = 0; i < 16075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16075(int& val) {
    const int x = 16075;
    std::cout << 16075;
    for (int i = 0; i < 16075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16076(int& val) {
    func_in_16076(val);
    const int x = 16076;
    std::cout << 16076;
    for (int i = 0; i < 16076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16076(int& val) {
    const int x = 16076;
    std::cout << 16076;
    for (int i = 0; i < 16076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16077(int& val) {
    func_in_16077(val);
    const int x = 16077;
    std::cout << 16077;
    for (int i = 0; i < 16077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16077(int& val) {
    const int x = 16077;
    std::cout << 16077;
    for (int i = 0; i < 16077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16078(int& val) {
    func_in_16078(val);
    const int x = 16078;
    std::cout << 16078;
    for (int i = 0; i < 16078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16078(int& val) {
    const int x = 16078;
    std::cout << 16078;
    for (int i = 0; i < 16078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16079(int& val) {
    func_in_16079(val);
    const int x = 16079;
    std::cout << 16079;
    for (int i = 0; i < 16079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16079(int& val) {
    const int x = 16079;
    std::cout << 16079;
    for (int i = 0; i < 16079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16080(int& val) {
    func_in_16080(val);
    const int x = 16080;
    std::cout << 16080;
    for (int i = 0; i < 16080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16080(int& val) {
    const int x = 16080;
    std::cout << 16080;
    for (int i = 0; i < 16080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16081(int& val) {
    func_in_16081(val);
    const int x = 16081;
    std::cout << 16081;
    for (int i = 0; i < 16081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16081(int& val) {
    const int x = 16081;
    std::cout << 16081;
    for (int i = 0; i < 16081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16082(int& val) {
    func_in_16082(val);
    const int x = 16082;
    std::cout << 16082;
    for (int i = 0; i < 16082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16082(int& val) {
    const int x = 16082;
    std::cout << 16082;
    for (int i = 0; i < 16082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16083(int& val) {
    func_in_16083(val);
    const int x = 16083;
    std::cout << 16083;
    for (int i = 0; i < 16083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16083(int& val) {
    const int x = 16083;
    std::cout << 16083;
    for (int i = 0; i < 16083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16084(int& val) {
    func_in_16084(val);
    const int x = 16084;
    std::cout << 16084;
    for (int i = 0; i < 16084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16084(int& val) {
    const int x = 16084;
    std::cout << 16084;
    for (int i = 0; i < 16084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16085(int& val) {
    func_in_16085(val);
    const int x = 16085;
    std::cout << 16085;
    for (int i = 0; i < 16085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16085(int& val) {
    const int x = 16085;
    std::cout << 16085;
    for (int i = 0; i < 16085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16086(int& val) {
    func_in_16086(val);
    const int x = 16086;
    std::cout << 16086;
    for (int i = 0; i < 16086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16086(int& val) {
    const int x = 16086;
    std::cout << 16086;
    for (int i = 0; i < 16086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16087(int& val) {
    func_in_16087(val);
    const int x = 16087;
    std::cout << 16087;
    for (int i = 0; i < 16087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16087(int& val) {
    const int x = 16087;
    std::cout << 16087;
    for (int i = 0; i < 16087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16088(int& val) {
    func_in_16088(val);
    const int x = 16088;
    std::cout << 16088;
    for (int i = 0; i < 16088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16088(int& val) {
    const int x = 16088;
    std::cout << 16088;
    for (int i = 0; i < 16088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16089(int& val) {
    func_in_16089(val);
    const int x = 16089;
    std::cout << 16089;
    for (int i = 0; i < 16089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16089(int& val) {
    const int x = 16089;
    std::cout << 16089;
    for (int i = 0; i < 16089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16090(int& val) {
    func_in_16090(val);
    const int x = 16090;
    std::cout << 16090;
    for (int i = 0; i < 16090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16090(int& val) {
    const int x = 16090;
    std::cout << 16090;
    for (int i = 0; i < 16090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16091(int& val) {
    func_in_16091(val);
    const int x = 16091;
    std::cout << 16091;
    for (int i = 0; i < 16091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16091(int& val) {
    const int x = 16091;
    std::cout << 16091;
    for (int i = 0; i < 16091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16092(int& val) {
    func_in_16092(val);
    const int x = 16092;
    std::cout << 16092;
    for (int i = 0; i < 16092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16092(int& val) {
    const int x = 16092;
    std::cout << 16092;
    for (int i = 0; i < 16092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16093(int& val) {
    func_in_16093(val);
    const int x = 16093;
    std::cout << 16093;
    for (int i = 0; i < 16093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16093(int& val) {
    const int x = 16093;
    std::cout << 16093;
    for (int i = 0; i < 16093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16094(int& val) {
    func_in_16094(val);
    const int x = 16094;
    std::cout << 16094;
    for (int i = 0; i < 16094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16094(int& val) {
    const int x = 16094;
    std::cout << 16094;
    for (int i = 0; i < 16094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16095(int& val) {
    func_in_16095(val);
    const int x = 16095;
    std::cout << 16095;
    for (int i = 0; i < 16095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16095(int& val) {
    const int x = 16095;
    std::cout << 16095;
    for (int i = 0; i < 16095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16096(int& val) {
    func_in_16096(val);
    const int x = 16096;
    std::cout << 16096;
    for (int i = 0; i < 16096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16096(int& val) {
    const int x = 16096;
    std::cout << 16096;
    for (int i = 0; i < 16096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16097(int& val) {
    func_in_16097(val);
    const int x = 16097;
    std::cout << 16097;
    for (int i = 0; i < 16097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16097(int& val) {
    const int x = 16097;
    std::cout << 16097;
    for (int i = 0; i < 16097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16098(int& val) {
    func_in_16098(val);
    const int x = 16098;
    std::cout << 16098;
    for (int i = 0; i < 16098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16098(int& val) {
    const int x = 16098;
    std::cout << 16098;
    for (int i = 0; i < 16098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16099(int& val) {
    func_in_16099(val);
    const int x = 16099;
    std::cout << 16099;
    for (int i = 0; i < 16099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16099(int& val) {
    const int x = 16099;
    std::cout << 16099;
    for (int i = 0; i < 16099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16100(int& val) {
    func_in_16100(val);
    const int x = 16100;
    std::cout << 16100;
    for (int i = 0; i < 16100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16100(int& val) {
    const int x = 16100;
    std::cout << 16100;
    for (int i = 0; i < 16100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16101(int& val) {
    func_in_16101(val);
    const int x = 16101;
    std::cout << 16101;
    for (int i = 0; i < 16101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16101(int& val) {
    const int x = 16101;
    std::cout << 16101;
    for (int i = 0; i < 16101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16102(int& val) {
    func_in_16102(val);
    const int x = 16102;
    std::cout << 16102;
    for (int i = 0; i < 16102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16102(int& val) {
    const int x = 16102;
    std::cout << 16102;
    for (int i = 0; i < 16102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16103(int& val) {
    func_in_16103(val);
    const int x = 16103;
    std::cout << 16103;
    for (int i = 0; i < 16103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16103(int& val) {
    const int x = 16103;
    std::cout << 16103;
    for (int i = 0; i < 16103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16104(int& val) {
    func_in_16104(val);
    const int x = 16104;
    std::cout << 16104;
    for (int i = 0; i < 16104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16104(int& val) {
    const int x = 16104;
    std::cout << 16104;
    for (int i = 0; i < 16104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16105(int& val) {
    func_in_16105(val);
    const int x = 16105;
    std::cout << 16105;
    for (int i = 0; i < 16105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16105(int& val) {
    const int x = 16105;
    std::cout << 16105;
    for (int i = 0; i < 16105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16106(int& val) {
    func_in_16106(val);
    const int x = 16106;
    std::cout << 16106;
    for (int i = 0; i < 16106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16106(int& val) {
    const int x = 16106;
    std::cout << 16106;
    for (int i = 0; i < 16106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16107(int& val) {
    func_in_16107(val);
    const int x = 16107;
    std::cout << 16107;
    for (int i = 0; i < 16107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16107(int& val) {
    const int x = 16107;
    std::cout << 16107;
    for (int i = 0; i < 16107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16108(int& val) {
    func_in_16108(val);
    const int x = 16108;
    std::cout << 16108;
    for (int i = 0; i < 16108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16108(int& val) {
    const int x = 16108;
    std::cout << 16108;
    for (int i = 0; i < 16108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16109(int& val) {
    func_in_16109(val);
    const int x = 16109;
    std::cout << 16109;
    for (int i = 0; i < 16109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16109(int& val) {
    const int x = 16109;
    std::cout << 16109;
    for (int i = 0; i < 16109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16110(int& val) {
    func_in_16110(val);
    const int x = 16110;
    std::cout << 16110;
    for (int i = 0; i < 16110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16110(int& val) {
    const int x = 16110;
    std::cout << 16110;
    for (int i = 0; i < 16110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16111(int& val) {
    func_in_16111(val);
    const int x = 16111;
    std::cout << 16111;
    for (int i = 0; i < 16111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16111(int& val) {
    const int x = 16111;
    std::cout << 16111;
    for (int i = 0; i < 16111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16112(int& val) {
    func_in_16112(val);
    const int x = 16112;
    std::cout << 16112;
    for (int i = 0; i < 16112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16112(int& val) {
    const int x = 16112;
    std::cout << 16112;
    for (int i = 0; i < 16112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16113(int& val) {
    func_in_16113(val);
    const int x = 16113;
    std::cout << 16113;
    for (int i = 0; i < 16113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16113(int& val) {
    const int x = 16113;
    std::cout << 16113;
    for (int i = 0; i < 16113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16114(int& val) {
    func_in_16114(val);
    const int x = 16114;
    std::cout << 16114;
    for (int i = 0; i < 16114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16114(int& val) {
    const int x = 16114;
    std::cout << 16114;
    for (int i = 0; i < 16114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16115(int& val) {
    func_in_16115(val);
    const int x = 16115;
    std::cout << 16115;
    for (int i = 0; i < 16115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16115(int& val) {
    const int x = 16115;
    std::cout << 16115;
    for (int i = 0; i < 16115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16116(int& val) {
    func_in_16116(val);
    const int x = 16116;
    std::cout << 16116;
    for (int i = 0; i < 16116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16116(int& val) {
    const int x = 16116;
    std::cout << 16116;
    for (int i = 0; i < 16116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16117(int& val) {
    func_in_16117(val);
    const int x = 16117;
    std::cout << 16117;
    for (int i = 0; i < 16117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16117(int& val) {
    const int x = 16117;
    std::cout << 16117;
    for (int i = 0; i < 16117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16118(int& val) {
    func_in_16118(val);
    const int x = 16118;
    std::cout << 16118;
    for (int i = 0; i < 16118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16118(int& val) {
    const int x = 16118;
    std::cout << 16118;
    for (int i = 0; i < 16118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16119(int& val) {
    func_in_16119(val);
    const int x = 16119;
    std::cout << 16119;
    for (int i = 0; i < 16119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16119(int& val) {
    const int x = 16119;
    std::cout << 16119;
    for (int i = 0; i < 16119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16120(int& val) {
    func_in_16120(val);
    const int x = 16120;
    std::cout << 16120;
    for (int i = 0; i < 16120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16120(int& val) {
    const int x = 16120;
    std::cout << 16120;
    for (int i = 0; i < 16120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16121(int& val) {
    func_in_16121(val);
    const int x = 16121;
    std::cout << 16121;
    for (int i = 0; i < 16121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16121(int& val) {
    const int x = 16121;
    std::cout << 16121;
    for (int i = 0; i < 16121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16122(int& val) {
    func_in_16122(val);
    const int x = 16122;
    std::cout << 16122;
    for (int i = 0; i < 16122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16122(int& val) {
    const int x = 16122;
    std::cout << 16122;
    for (int i = 0; i < 16122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16123(int& val) {
    func_in_16123(val);
    const int x = 16123;
    std::cout << 16123;
    for (int i = 0; i < 16123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16123(int& val) {
    const int x = 16123;
    std::cout << 16123;
    for (int i = 0; i < 16123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16124(int& val) {
    func_in_16124(val);
    const int x = 16124;
    std::cout << 16124;
    for (int i = 0; i < 16124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16124(int& val) {
    const int x = 16124;
    std::cout << 16124;
    for (int i = 0; i < 16124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16125(int& val) {
    func_in_16125(val);
    const int x = 16125;
    std::cout << 16125;
    for (int i = 0; i < 16125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16125(int& val) {
    const int x = 16125;
    std::cout << 16125;
    for (int i = 0; i < 16125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16126(int& val) {
    func_in_16126(val);
    const int x = 16126;
    std::cout << 16126;
    for (int i = 0; i < 16126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16126(int& val) {
    const int x = 16126;
    std::cout << 16126;
    for (int i = 0; i < 16126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16127(int& val) {
    func_in_16127(val);
    const int x = 16127;
    std::cout << 16127;
    for (int i = 0; i < 16127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16127(int& val) {
    const int x = 16127;
    std::cout << 16127;
    for (int i = 0; i < 16127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16128(int& val) {
    func_in_16128(val);
    const int x = 16128;
    std::cout << 16128;
    for (int i = 0; i < 16128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16128(int& val) {
    const int x = 16128;
    std::cout << 16128;
    for (int i = 0; i < 16128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16129(int& val) {
    func_in_16129(val);
    const int x = 16129;
    std::cout << 16129;
    for (int i = 0; i < 16129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16129(int& val) {
    const int x = 16129;
    std::cout << 16129;
    for (int i = 0; i < 16129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16130(int& val) {
    func_in_16130(val);
    const int x = 16130;
    std::cout << 16130;
    for (int i = 0; i < 16130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16130(int& val) {
    const int x = 16130;
    std::cout << 16130;
    for (int i = 0; i < 16130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16131(int& val) {
    func_in_16131(val);
    const int x = 16131;
    std::cout << 16131;
    for (int i = 0; i < 16131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16131(int& val) {
    const int x = 16131;
    std::cout << 16131;
    for (int i = 0; i < 16131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16132(int& val) {
    func_in_16132(val);
    const int x = 16132;
    std::cout << 16132;
    for (int i = 0; i < 16132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16132(int& val) {
    const int x = 16132;
    std::cout << 16132;
    for (int i = 0; i < 16132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16133(int& val) {
    func_in_16133(val);
    const int x = 16133;
    std::cout << 16133;
    for (int i = 0; i < 16133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16133(int& val) {
    const int x = 16133;
    std::cout << 16133;
    for (int i = 0; i < 16133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16134(int& val) {
    func_in_16134(val);
    const int x = 16134;
    std::cout << 16134;
    for (int i = 0; i < 16134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16134(int& val) {
    const int x = 16134;
    std::cout << 16134;
    for (int i = 0; i < 16134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16135(int& val) {
    func_in_16135(val);
    const int x = 16135;
    std::cout << 16135;
    for (int i = 0; i < 16135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16135(int& val) {
    const int x = 16135;
    std::cout << 16135;
    for (int i = 0; i < 16135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16136(int& val) {
    func_in_16136(val);
    const int x = 16136;
    std::cout << 16136;
    for (int i = 0; i < 16136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16136(int& val) {
    const int x = 16136;
    std::cout << 16136;
    for (int i = 0; i < 16136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16137(int& val) {
    func_in_16137(val);
    const int x = 16137;
    std::cout << 16137;
    for (int i = 0; i < 16137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16137(int& val) {
    const int x = 16137;
    std::cout << 16137;
    for (int i = 0; i < 16137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16138(int& val) {
    func_in_16138(val);
    const int x = 16138;
    std::cout << 16138;
    for (int i = 0; i < 16138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16138(int& val) {
    const int x = 16138;
    std::cout << 16138;
    for (int i = 0; i < 16138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16139(int& val) {
    func_in_16139(val);
    const int x = 16139;
    std::cout << 16139;
    for (int i = 0; i < 16139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16139(int& val) {
    const int x = 16139;
    std::cout << 16139;
    for (int i = 0; i < 16139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16140(int& val) {
    func_in_16140(val);
    const int x = 16140;
    std::cout << 16140;
    for (int i = 0; i < 16140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16140(int& val) {
    const int x = 16140;
    std::cout << 16140;
    for (int i = 0; i < 16140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16141(int& val) {
    func_in_16141(val);
    const int x = 16141;
    std::cout << 16141;
    for (int i = 0; i < 16141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16141(int& val) {
    const int x = 16141;
    std::cout << 16141;
    for (int i = 0; i < 16141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16142(int& val) {
    func_in_16142(val);
    const int x = 16142;
    std::cout << 16142;
    for (int i = 0; i < 16142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16142(int& val) {
    const int x = 16142;
    std::cout << 16142;
    for (int i = 0; i < 16142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16143(int& val) {
    func_in_16143(val);
    const int x = 16143;
    std::cout << 16143;
    for (int i = 0; i < 16143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16143(int& val) {
    const int x = 16143;
    std::cout << 16143;
    for (int i = 0; i < 16143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16144(int& val) {
    func_in_16144(val);
    const int x = 16144;
    std::cout << 16144;
    for (int i = 0; i < 16144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16144(int& val) {
    const int x = 16144;
    std::cout << 16144;
    for (int i = 0; i < 16144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16145(int& val) {
    func_in_16145(val);
    const int x = 16145;
    std::cout << 16145;
    for (int i = 0; i < 16145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16145(int& val) {
    const int x = 16145;
    std::cout << 16145;
    for (int i = 0; i < 16145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16146(int& val) {
    func_in_16146(val);
    const int x = 16146;
    std::cout << 16146;
    for (int i = 0; i < 16146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16146(int& val) {
    const int x = 16146;
    std::cout << 16146;
    for (int i = 0; i < 16146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16147(int& val) {
    func_in_16147(val);
    const int x = 16147;
    std::cout << 16147;
    for (int i = 0; i < 16147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16147(int& val) {
    const int x = 16147;
    std::cout << 16147;
    for (int i = 0; i < 16147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16148(int& val) {
    func_in_16148(val);
    const int x = 16148;
    std::cout << 16148;
    for (int i = 0; i < 16148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16148(int& val) {
    const int x = 16148;
    std::cout << 16148;
    for (int i = 0; i < 16148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16149(int& val) {
    func_in_16149(val);
    const int x = 16149;
    std::cout << 16149;
    for (int i = 0; i < 16149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16149(int& val) {
    const int x = 16149;
    std::cout << 16149;
    for (int i = 0; i < 16149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16150(int& val) {
    func_in_16150(val);
    const int x = 16150;
    std::cout << 16150;
    for (int i = 0; i < 16150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16150(int& val) {
    const int x = 16150;
    std::cout << 16150;
    for (int i = 0; i < 16150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16151(int& val) {
    func_in_16151(val);
    const int x = 16151;
    std::cout << 16151;
    for (int i = 0; i < 16151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16151(int& val) {
    const int x = 16151;
    std::cout << 16151;
    for (int i = 0; i < 16151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16152(int& val) {
    func_in_16152(val);
    const int x = 16152;
    std::cout << 16152;
    for (int i = 0; i < 16152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16152(int& val) {
    const int x = 16152;
    std::cout << 16152;
    for (int i = 0; i < 16152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16153(int& val) {
    func_in_16153(val);
    const int x = 16153;
    std::cout << 16153;
    for (int i = 0; i < 16153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16153(int& val) {
    const int x = 16153;
    std::cout << 16153;
    for (int i = 0; i < 16153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16154(int& val) {
    func_in_16154(val);
    const int x = 16154;
    std::cout << 16154;
    for (int i = 0; i < 16154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16154(int& val) {
    const int x = 16154;
    std::cout << 16154;
    for (int i = 0; i < 16154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16155(int& val) {
    func_in_16155(val);
    const int x = 16155;
    std::cout << 16155;
    for (int i = 0; i < 16155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16155(int& val) {
    const int x = 16155;
    std::cout << 16155;
    for (int i = 0; i < 16155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16156(int& val) {
    func_in_16156(val);
    const int x = 16156;
    std::cout << 16156;
    for (int i = 0; i < 16156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16156(int& val) {
    const int x = 16156;
    std::cout << 16156;
    for (int i = 0; i < 16156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16157(int& val) {
    func_in_16157(val);
    const int x = 16157;
    std::cout << 16157;
    for (int i = 0; i < 16157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16157(int& val) {
    const int x = 16157;
    std::cout << 16157;
    for (int i = 0; i < 16157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16158(int& val) {
    func_in_16158(val);
    const int x = 16158;
    std::cout << 16158;
    for (int i = 0; i < 16158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16158(int& val) {
    const int x = 16158;
    std::cout << 16158;
    for (int i = 0; i < 16158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16159(int& val) {
    func_in_16159(val);
    const int x = 16159;
    std::cout << 16159;
    for (int i = 0; i < 16159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16159(int& val) {
    const int x = 16159;
    std::cout << 16159;
    for (int i = 0; i < 16159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16160(int& val) {
    func_in_16160(val);
    const int x = 16160;
    std::cout << 16160;
    for (int i = 0; i < 16160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16160(int& val) {
    const int x = 16160;
    std::cout << 16160;
    for (int i = 0; i < 16160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16161(int& val) {
    func_in_16161(val);
    const int x = 16161;
    std::cout << 16161;
    for (int i = 0; i < 16161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16161(int& val) {
    const int x = 16161;
    std::cout << 16161;
    for (int i = 0; i < 16161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16162(int& val) {
    func_in_16162(val);
    const int x = 16162;
    std::cout << 16162;
    for (int i = 0; i < 16162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16162(int& val) {
    const int x = 16162;
    std::cout << 16162;
    for (int i = 0; i < 16162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16163(int& val) {
    func_in_16163(val);
    const int x = 16163;
    std::cout << 16163;
    for (int i = 0; i < 16163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16163(int& val) {
    const int x = 16163;
    std::cout << 16163;
    for (int i = 0; i < 16163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16164(int& val) {
    func_in_16164(val);
    const int x = 16164;
    std::cout << 16164;
    for (int i = 0; i < 16164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16164(int& val) {
    const int x = 16164;
    std::cout << 16164;
    for (int i = 0; i < 16164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16165(int& val) {
    func_in_16165(val);
    const int x = 16165;
    std::cout << 16165;
    for (int i = 0; i < 16165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16165(int& val) {
    const int x = 16165;
    std::cout << 16165;
    for (int i = 0; i < 16165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16166(int& val) {
    func_in_16166(val);
    const int x = 16166;
    std::cout << 16166;
    for (int i = 0; i < 16166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16166(int& val) {
    const int x = 16166;
    std::cout << 16166;
    for (int i = 0; i < 16166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16167(int& val) {
    func_in_16167(val);
    const int x = 16167;
    std::cout << 16167;
    for (int i = 0; i < 16167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16167(int& val) {
    const int x = 16167;
    std::cout << 16167;
    for (int i = 0; i < 16167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16168(int& val) {
    func_in_16168(val);
    const int x = 16168;
    std::cout << 16168;
    for (int i = 0; i < 16168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16168(int& val) {
    const int x = 16168;
    std::cout << 16168;
    for (int i = 0; i < 16168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16169(int& val) {
    func_in_16169(val);
    const int x = 16169;
    std::cout << 16169;
    for (int i = 0; i < 16169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16169(int& val) {
    const int x = 16169;
    std::cout << 16169;
    for (int i = 0; i < 16169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16170(int& val) {
    func_in_16170(val);
    const int x = 16170;
    std::cout << 16170;
    for (int i = 0; i < 16170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16170(int& val) {
    const int x = 16170;
    std::cout << 16170;
    for (int i = 0; i < 16170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16171(int& val) {
    func_in_16171(val);
    const int x = 16171;
    std::cout << 16171;
    for (int i = 0; i < 16171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16171(int& val) {
    const int x = 16171;
    std::cout << 16171;
    for (int i = 0; i < 16171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16172(int& val) {
    func_in_16172(val);
    const int x = 16172;
    std::cout << 16172;
    for (int i = 0; i < 16172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16172(int& val) {
    const int x = 16172;
    std::cout << 16172;
    for (int i = 0; i < 16172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16173(int& val) {
    func_in_16173(val);
    const int x = 16173;
    std::cout << 16173;
    for (int i = 0; i < 16173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16173(int& val) {
    const int x = 16173;
    std::cout << 16173;
    for (int i = 0; i < 16173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16174(int& val) {
    func_in_16174(val);
    const int x = 16174;
    std::cout << 16174;
    for (int i = 0; i < 16174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16174(int& val) {
    const int x = 16174;
    std::cout << 16174;
    for (int i = 0; i < 16174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16175(int& val) {
    func_in_16175(val);
    const int x = 16175;
    std::cout << 16175;
    for (int i = 0; i < 16175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16175(int& val) {
    const int x = 16175;
    std::cout << 16175;
    for (int i = 0; i < 16175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16176(int& val) {
    func_in_16176(val);
    const int x = 16176;
    std::cout << 16176;
    for (int i = 0; i < 16176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16176(int& val) {
    const int x = 16176;
    std::cout << 16176;
    for (int i = 0; i < 16176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16177(int& val) {
    func_in_16177(val);
    const int x = 16177;
    std::cout << 16177;
    for (int i = 0; i < 16177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16177(int& val) {
    const int x = 16177;
    std::cout << 16177;
    for (int i = 0; i < 16177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16178(int& val) {
    func_in_16178(val);
    const int x = 16178;
    std::cout << 16178;
    for (int i = 0; i < 16178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16178(int& val) {
    const int x = 16178;
    std::cout << 16178;
    for (int i = 0; i < 16178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16179(int& val) {
    func_in_16179(val);
    const int x = 16179;
    std::cout << 16179;
    for (int i = 0; i < 16179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16179(int& val) {
    const int x = 16179;
    std::cout << 16179;
    for (int i = 0; i < 16179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16180(int& val) {
    func_in_16180(val);
    const int x = 16180;
    std::cout << 16180;
    for (int i = 0; i < 16180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16180(int& val) {
    const int x = 16180;
    std::cout << 16180;
    for (int i = 0; i < 16180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16181(int& val) {
    func_in_16181(val);
    const int x = 16181;
    std::cout << 16181;
    for (int i = 0; i < 16181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16181(int& val) {
    const int x = 16181;
    std::cout << 16181;
    for (int i = 0; i < 16181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16182(int& val) {
    func_in_16182(val);
    const int x = 16182;
    std::cout << 16182;
    for (int i = 0; i < 16182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16182(int& val) {
    const int x = 16182;
    std::cout << 16182;
    for (int i = 0; i < 16182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16183(int& val) {
    func_in_16183(val);
    const int x = 16183;
    std::cout << 16183;
    for (int i = 0; i < 16183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16183(int& val) {
    const int x = 16183;
    std::cout << 16183;
    for (int i = 0; i < 16183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16184(int& val) {
    func_in_16184(val);
    const int x = 16184;
    std::cout << 16184;
    for (int i = 0; i < 16184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16184(int& val) {
    const int x = 16184;
    std::cout << 16184;
    for (int i = 0; i < 16184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16185(int& val) {
    func_in_16185(val);
    const int x = 16185;
    std::cout << 16185;
    for (int i = 0; i < 16185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16185(int& val) {
    const int x = 16185;
    std::cout << 16185;
    for (int i = 0; i < 16185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16186(int& val) {
    func_in_16186(val);
    const int x = 16186;
    std::cout << 16186;
    for (int i = 0; i < 16186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16186(int& val) {
    const int x = 16186;
    std::cout << 16186;
    for (int i = 0; i < 16186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16187(int& val) {
    func_in_16187(val);
    const int x = 16187;
    std::cout << 16187;
    for (int i = 0; i < 16187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16187(int& val) {
    const int x = 16187;
    std::cout << 16187;
    for (int i = 0; i < 16187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16188(int& val) {
    func_in_16188(val);
    const int x = 16188;
    std::cout << 16188;
    for (int i = 0; i < 16188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16188(int& val) {
    const int x = 16188;
    std::cout << 16188;
    for (int i = 0; i < 16188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16189(int& val) {
    func_in_16189(val);
    const int x = 16189;
    std::cout << 16189;
    for (int i = 0; i < 16189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16189(int& val) {
    const int x = 16189;
    std::cout << 16189;
    for (int i = 0; i < 16189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16190(int& val) {
    func_in_16190(val);
    const int x = 16190;
    std::cout << 16190;
    for (int i = 0; i < 16190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16190(int& val) {
    const int x = 16190;
    std::cout << 16190;
    for (int i = 0; i < 16190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16191(int& val) {
    func_in_16191(val);
    const int x = 16191;
    std::cout << 16191;
    for (int i = 0; i < 16191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16191(int& val) {
    const int x = 16191;
    std::cout << 16191;
    for (int i = 0; i < 16191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16192(int& val) {
    func_in_16192(val);
    const int x = 16192;
    std::cout << 16192;
    for (int i = 0; i < 16192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16192(int& val) {
    const int x = 16192;
    std::cout << 16192;
    for (int i = 0; i < 16192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16193(int& val) {
    func_in_16193(val);
    const int x = 16193;
    std::cout << 16193;
    for (int i = 0; i < 16193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16193(int& val) {
    const int x = 16193;
    std::cout << 16193;
    for (int i = 0; i < 16193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16194(int& val) {
    func_in_16194(val);
    const int x = 16194;
    std::cout << 16194;
    for (int i = 0; i < 16194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16194(int& val) {
    const int x = 16194;
    std::cout << 16194;
    for (int i = 0; i < 16194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16195(int& val) {
    func_in_16195(val);
    const int x = 16195;
    std::cout << 16195;
    for (int i = 0; i < 16195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16195(int& val) {
    const int x = 16195;
    std::cout << 16195;
    for (int i = 0; i < 16195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16196(int& val) {
    func_in_16196(val);
    const int x = 16196;
    std::cout << 16196;
    for (int i = 0; i < 16196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16196(int& val) {
    const int x = 16196;
    std::cout << 16196;
    for (int i = 0; i < 16196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16197(int& val) {
    func_in_16197(val);
    const int x = 16197;
    std::cout << 16197;
    for (int i = 0; i < 16197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16197(int& val) {
    const int x = 16197;
    std::cout << 16197;
    for (int i = 0; i < 16197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16198(int& val) {
    func_in_16198(val);
    const int x = 16198;
    std::cout << 16198;
    for (int i = 0; i < 16198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16198(int& val) {
    const int x = 16198;
    std::cout << 16198;
    for (int i = 0; i < 16198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16199(int& val) {
    func_in_16199(val);
    const int x = 16199;
    std::cout << 16199;
    for (int i = 0; i < 16199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16199(int& val) {
    const int x = 16199;
    std::cout << 16199;
    for (int i = 0; i < 16199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16200(int& val) {
    func_in_16200(val);
    const int x = 16200;
    std::cout << 16200;
    for (int i = 0; i < 16200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16200(int& val) {
    const int x = 16200;
    std::cout << 16200;
    for (int i = 0; i < 16200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16201(int& val) {
    func_in_16201(val);
    const int x = 16201;
    std::cout << 16201;
    for (int i = 0; i < 16201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16201(int& val) {
    const int x = 16201;
    std::cout << 16201;
    for (int i = 0; i < 16201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16202(int& val) {
    func_in_16202(val);
    const int x = 16202;
    std::cout << 16202;
    for (int i = 0; i < 16202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16202(int& val) {
    const int x = 16202;
    std::cout << 16202;
    for (int i = 0; i < 16202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16203(int& val) {
    func_in_16203(val);
    const int x = 16203;
    std::cout << 16203;
    for (int i = 0; i < 16203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16203(int& val) {
    const int x = 16203;
    std::cout << 16203;
    for (int i = 0; i < 16203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16204(int& val) {
    func_in_16204(val);
    const int x = 16204;
    std::cout << 16204;
    for (int i = 0; i < 16204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16204(int& val) {
    const int x = 16204;
    std::cout << 16204;
    for (int i = 0; i < 16204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16205(int& val) {
    func_in_16205(val);
    const int x = 16205;
    std::cout << 16205;
    for (int i = 0; i < 16205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16205(int& val) {
    const int x = 16205;
    std::cout << 16205;
    for (int i = 0; i < 16205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16206(int& val) {
    func_in_16206(val);
    const int x = 16206;
    std::cout << 16206;
    for (int i = 0; i < 16206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16206(int& val) {
    const int x = 16206;
    std::cout << 16206;
    for (int i = 0; i < 16206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16207(int& val) {
    func_in_16207(val);
    const int x = 16207;
    std::cout << 16207;
    for (int i = 0; i < 16207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16207(int& val) {
    const int x = 16207;
    std::cout << 16207;
    for (int i = 0; i < 16207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16208(int& val) {
    func_in_16208(val);
    const int x = 16208;
    std::cout << 16208;
    for (int i = 0; i < 16208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16208(int& val) {
    const int x = 16208;
    std::cout << 16208;
    for (int i = 0; i < 16208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16209(int& val) {
    func_in_16209(val);
    const int x = 16209;
    std::cout << 16209;
    for (int i = 0; i < 16209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16209(int& val) {
    const int x = 16209;
    std::cout << 16209;
    for (int i = 0; i < 16209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16210(int& val) {
    func_in_16210(val);
    const int x = 16210;
    std::cout << 16210;
    for (int i = 0; i < 16210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16210(int& val) {
    const int x = 16210;
    std::cout << 16210;
    for (int i = 0; i < 16210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16211(int& val) {
    func_in_16211(val);
    const int x = 16211;
    std::cout << 16211;
    for (int i = 0; i < 16211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16211(int& val) {
    const int x = 16211;
    std::cout << 16211;
    for (int i = 0; i < 16211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16212(int& val) {
    func_in_16212(val);
    const int x = 16212;
    std::cout << 16212;
    for (int i = 0; i < 16212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16212(int& val) {
    const int x = 16212;
    std::cout << 16212;
    for (int i = 0; i < 16212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16213(int& val) {
    func_in_16213(val);
    const int x = 16213;
    std::cout << 16213;
    for (int i = 0; i < 16213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16213(int& val) {
    const int x = 16213;
    std::cout << 16213;
    for (int i = 0; i < 16213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16214(int& val) {
    func_in_16214(val);
    const int x = 16214;
    std::cout << 16214;
    for (int i = 0; i < 16214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16214(int& val) {
    const int x = 16214;
    std::cout << 16214;
    for (int i = 0; i < 16214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16215(int& val) {
    func_in_16215(val);
    const int x = 16215;
    std::cout << 16215;
    for (int i = 0; i < 16215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16215(int& val) {
    const int x = 16215;
    std::cout << 16215;
    for (int i = 0; i < 16215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16216(int& val) {
    func_in_16216(val);
    const int x = 16216;
    std::cout << 16216;
    for (int i = 0; i < 16216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16216(int& val) {
    const int x = 16216;
    std::cout << 16216;
    for (int i = 0; i < 16216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16217(int& val) {
    func_in_16217(val);
    const int x = 16217;
    std::cout << 16217;
    for (int i = 0; i < 16217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16217(int& val) {
    const int x = 16217;
    std::cout << 16217;
    for (int i = 0; i < 16217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16218(int& val) {
    func_in_16218(val);
    const int x = 16218;
    std::cout << 16218;
    for (int i = 0; i < 16218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16218(int& val) {
    const int x = 16218;
    std::cout << 16218;
    for (int i = 0; i < 16218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16219(int& val) {
    func_in_16219(val);
    const int x = 16219;
    std::cout << 16219;
    for (int i = 0; i < 16219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16219(int& val) {
    const int x = 16219;
    std::cout << 16219;
    for (int i = 0; i < 16219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16220(int& val) {
    func_in_16220(val);
    const int x = 16220;
    std::cout << 16220;
    for (int i = 0; i < 16220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16220(int& val) {
    const int x = 16220;
    std::cout << 16220;
    for (int i = 0; i < 16220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16221(int& val) {
    func_in_16221(val);
    const int x = 16221;
    std::cout << 16221;
    for (int i = 0; i < 16221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16221(int& val) {
    const int x = 16221;
    std::cout << 16221;
    for (int i = 0; i < 16221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16222(int& val) {
    func_in_16222(val);
    const int x = 16222;
    std::cout << 16222;
    for (int i = 0; i < 16222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16222(int& val) {
    const int x = 16222;
    std::cout << 16222;
    for (int i = 0; i < 16222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16223(int& val) {
    func_in_16223(val);
    const int x = 16223;
    std::cout << 16223;
    for (int i = 0; i < 16223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16223(int& val) {
    const int x = 16223;
    std::cout << 16223;
    for (int i = 0; i < 16223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16224(int& val) {
    func_in_16224(val);
    const int x = 16224;
    std::cout << 16224;
    for (int i = 0; i < 16224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16224(int& val) {
    const int x = 16224;
    std::cout << 16224;
    for (int i = 0; i < 16224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16225(int& val) {
    func_in_16225(val);
    const int x = 16225;
    std::cout << 16225;
    for (int i = 0; i < 16225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16225(int& val) {
    const int x = 16225;
    std::cout << 16225;
    for (int i = 0; i < 16225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16226(int& val) {
    func_in_16226(val);
    const int x = 16226;
    std::cout << 16226;
    for (int i = 0; i < 16226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16226(int& val) {
    const int x = 16226;
    std::cout << 16226;
    for (int i = 0; i < 16226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16227(int& val) {
    func_in_16227(val);
    const int x = 16227;
    std::cout << 16227;
    for (int i = 0; i < 16227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16227(int& val) {
    const int x = 16227;
    std::cout << 16227;
    for (int i = 0; i < 16227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16228(int& val) {
    func_in_16228(val);
    const int x = 16228;
    std::cout << 16228;
    for (int i = 0; i < 16228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16228(int& val) {
    const int x = 16228;
    std::cout << 16228;
    for (int i = 0; i < 16228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16229(int& val) {
    func_in_16229(val);
    const int x = 16229;
    std::cout << 16229;
    for (int i = 0; i < 16229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16229(int& val) {
    const int x = 16229;
    std::cout << 16229;
    for (int i = 0; i < 16229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16230(int& val) {
    func_in_16230(val);
    const int x = 16230;
    std::cout << 16230;
    for (int i = 0; i < 16230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16230(int& val) {
    const int x = 16230;
    std::cout << 16230;
    for (int i = 0; i < 16230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16231(int& val) {
    func_in_16231(val);
    const int x = 16231;
    std::cout << 16231;
    for (int i = 0; i < 16231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16231(int& val) {
    const int x = 16231;
    std::cout << 16231;
    for (int i = 0; i < 16231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16232(int& val) {
    func_in_16232(val);
    const int x = 16232;
    std::cout << 16232;
    for (int i = 0; i < 16232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16232(int& val) {
    const int x = 16232;
    std::cout << 16232;
    for (int i = 0; i < 16232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16233(int& val) {
    func_in_16233(val);
    const int x = 16233;
    std::cout << 16233;
    for (int i = 0; i < 16233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16233(int& val) {
    const int x = 16233;
    std::cout << 16233;
    for (int i = 0; i < 16233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16234(int& val) {
    func_in_16234(val);
    const int x = 16234;
    std::cout << 16234;
    for (int i = 0; i < 16234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16234(int& val) {
    const int x = 16234;
    std::cout << 16234;
    for (int i = 0; i < 16234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16235(int& val) {
    func_in_16235(val);
    const int x = 16235;
    std::cout << 16235;
    for (int i = 0; i < 16235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16235(int& val) {
    const int x = 16235;
    std::cout << 16235;
    for (int i = 0; i < 16235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16236(int& val) {
    func_in_16236(val);
    const int x = 16236;
    std::cout << 16236;
    for (int i = 0; i < 16236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16236(int& val) {
    const int x = 16236;
    std::cout << 16236;
    for (int i = 0; i < 16236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16237(int& val) {
    func_in_16237(val);
    const int x = 16237;
    std::cout << 16237;
    for (int i = 0; i < 16237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16237(int& val) {
    const int x = 16237;
    std::cout << 16237;
    for (int i = 0; i < 16237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16238(int& val) {
    func_in_16238(val);
    const int x = 16238;
    std::cout << 16238;
    for (int i = 0; i < 16238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16238(int& val) {
    const int x = 16238;
    std::cout << 16238;
    for (int i = 0; i < 16238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16239(int& val) {
    func_in_16239(val);
    const int x = 16239;
    std::cout << 16239;
    for (int i = 0; i < 16239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16239(int& val) {
    const int x = 16239;
    std::cout << 16239;
    for (int i = 0; i < 16239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16240(int& val) {
    func_in_16240(val);
    const int x = 16240;
    std::cout << 16240;
    for (int i = 0; i < 16240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16240(int& val) {
    const int x = 16240;
    std::cout << 16240;
    for (int i = 0; i < 16240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16241(int& val) {
    func_in_16241(val);
    const int x = 16241;
    std::cout << 16241;
    for (int i = 0; i < 16241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16241(int& val) {
    const int x = 16241;
    std::cout << 16241;
    for (int i = 0; i < 16241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16242(int& val) {
    func_in_16242(val);
    const int x = 16242;
    std::cout << 16242;
    for (int i = 0; i < 16242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16242(int& val) {
    const int x = 16242;
    std::cout << 16242;
    for (int i = 0; i < 16242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16243(int& val) {
    func_in_16243(val);
    const int x = 16243;
    std::cout << 16243;
    for (int i = 0; i < 16243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16243(int& val) {
    const int x = 16243;
    std::cout << 16243;
    for (int i = 0; i < 16243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16244(int& val) {
    func_in_16244(val);
    const int x = 16244;
    std::cout << 16244;
    for (int i = 0; i < 16244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16244(int& val) {
    const int x = 16244;
    std::cout << 16244;
    for (int i = 0; i < 16244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16245(int& val) {
    func_in_16245(val);
    const int x = 16245;
    std::cout << 16245;
    for (int i = 0; i < 16245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16245(int& val) {
    const int x = 16245;
    std::cout << 16245;
    for (int i = 0; i < 16245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16246(int& val) {
    func_in_16246(val);
    const int x = 16246;
    std::cout << 16246;
    for (int i = 0; i < 16246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16246(int& val) {
    const int x = 16246;
    std::cout << 16246;
    for (int i = 0; i < 16246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16247(int& val) {
    func_in_16247(val);
    const int x = 16247;
    std::cout << 16247;
    for (int i = 0; i < 16247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16247(int& val) {
    const int x = 16247;
    std::cout << 16247;
    for (int i = 0; i < 16247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16248(int& val) {
    func_in_16248(val);
    const int x = 16248;
    std::cout << 16248;
    for (int i = 0; i < 16248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16248(int& val) {
    const int x = 16248;
    std::cout << 16248;
    for (int i = 0; i < 16248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16249(int& val) {
    func_in_16249(val);
    const int x = 16249;
    std::cout << 16249;
    for (int i = 0; i < 16249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16249(int& val) {
    const int x = 16249;
    std::cout << 16249;
    for (int i = 0; i < 16249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16250(int& val) {
    func_in_16250(val);
    const int x = 16250;
    std::cout << 16250;
    for (int i = 0; i < 16250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16250(int& val) {
    const int x = 16250;
    std::cout << 16250;
    for (int i = 0; i < 16250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16251(int& val) {
    func_in_16251(val);
    const int x = 16251;
    std::cout << 16251;
    for (int i = 0; i < 16251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16251(int& val) {
    const int x = 16251;
    std::cout << 16251;
    for (int i = 0; i < 16251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16252(int& val) {
    func_in_16252(val);
    const int x = 16252;
    std::cout << 16252;
    for (int i = 0; i < 16252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16252(int& val) {
    const int x = 16252;
    std::cout << 16252;
    for (int i = 0; i < 16252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16253(int& val) {
    func_in_16253(val);
    const int x = 16253;
    std::cout << 16253;
    for (int i = 0; i < 16253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16253(int& val) {
    const int x = 16253;
    std::cout << 16253;
    for (int i = 0; i < 16253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16254(int& val) {
    func_in_16254(val);
    const int x = 16254;
    std::cout << 16254;
    for (int i = 0; i < 16254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16254(int& val) {
    const int x = 16254;
    std::cout << 16254;
    for (int i = 0; i < 16254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16255(int& val) {
    func_in_16255(val);
    const int x = 16255;
    std::cout << 16255;
    for (int i = 0; i < 16255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16255(int& val) {
    const int x = 16255;
    std::cout << 16255;
    for (int i = 0; i < 16255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16256(int& val) {
    func_in_16256(val);
    const int x = 16256;
    std::cout << 16256;
    for (int i = 0; i < 16256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16256(int& val) {
    const int x = 16256;
    std::cout << 16256;
    for (int i = 0; i < 16256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16257(int& val) {
    func_in_16257(val);
    const int x = 16257;
    std::cout << 16257;
    for (int i = 0; i < 16257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16257(int& val) {
    const int x = 16257;
    std::cout << 16257;
    for (int i = 0; i < 16257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16258(int& val) {
    func_in_16258(val);
    const int x = 16258;
    std::cout << 16258;
    for (int i = 0; i < 16258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16258(int& val) {
    const int x = 16258;
    std::cout << 16258;
    for (int i = 0; i < 16258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16259(int& val) {
    func_in_16259(val);
    const int x = 16259;
    std::cout << 16259;
    for (int i = 0; i < 16259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16259(int& val) {
    const int x = 16259;
    std::cout << 16259;
    for (int i = 0; i < 16259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16260(int& val) {
    func_in_16260(val);
    const int x = 16260;
    std::cout << 16260;
    for (int i = 0; i < 16260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16260(int& val) {
    const int x = 16260;
    std::cout << 16260;
    for (int i = 0; i < 16260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16261(int& val) {
    func_in_16261(val);
    const int x = 16261;
    std::cout << 16261;
    for (int i = 0; i < 16261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16261(int& val) {
    const int x = 16261;
    std::cout << 16261;
    for (int i = 0; i < 16261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16262(int& val) {
    func_in_16262(val);
    const int x = 16262;
    std::cout << 16262;
    for (int i = 0; i < 16262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16262(int& val) {
    const int x = 16262;
    std::cout << 16262;
    for (int i = 0; i < 16262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16263(int& val) {
    func_in_16263(val);
    const int x = 16263;
    std::cout << 16263;
    for (int i = 0; i < 16263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16263(int& val) {
    const int x = 16263;
    std::cout << 16263;
    for (int i = 0; i < 16263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16264(int& val) {
    func_in_16264(val);
    const int x = 16264;
    std::cout << 16264;
    for (int i = 0; i < 16264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16264(int& val) {
    const int x = 16264;
    std::cout << 16264;
    for (int i = 0; i < 16264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16265(int& val) {
    func_in_16265(val);
    const int x = 16265;
    std::cout << 16265;
    for (int i = 0; i < 16265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16265(int& val) {
    const int x = 16265;
    std::cout << 16265;
    for (int i = 0; i < 16265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16266(int& val) {
    func_in_16266(val);
    const int x = 16266;
    std::cout << 16266;
    for (int i = 0; i < 16266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16266(int& val) {
    const int x = 16266;
    std::cout << 16266;
    for (int i = 0; i < 16266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16267(int& val) {
    func_in_16267(val);
    const int x = 16267;
    std::cout << 16267;
    for (int i = 0; i < 16267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16267(int& val) {
    const int x = 16267;
    std::cout << 16267;
    for (int i = 0; i < 16267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16268(int& val) {
    func_in_16268(val);
    const int x = 16268;
    std::cout << 16268;
    for (int i = 0; i < 16268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16268(int& val) {
    const int x = 16268;
    std::cout << 16268;
    for (int i = 0; i < 16268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16269(int& val) {
    func_in_16269(val);
    const int x = 16269;
    std::cout << 16269;
    for (int i = 0; i < 16269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16269(int& val) {
    const int x = 16269;
    std::cout << 16269;
    for (int i = 0; i < 16269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16270(int& val) {
    func_in_16270(val);
    const int x = 16270;
    std::cout << 16270;
    for (int i = 0; i < 16270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16270(int& val) {
    const int x = 16270;
    std::cout << 16270;
    for (int i = 0; i < 16270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16271(int& val) {
    func_in_16271(val);
    const int x = 16271;
    std::cout << 16271;
    for (int i = 0; i < 16271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16271(int& val) {
    const int x = 16271;
    std::cout << 16271;
    for (int i = 0; i < 16271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16272(int& val) {
    func_in_16272(val);
    const int x = 16272;
    std::cout << 16272;
    for (int i = 0; i < 16272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16272(int& val) {
    const int x = 16272;
    std::cout << 16272;
    for (int i = 0; i < 16272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16273(int& val) {
    func_in_16273(val);
    const int x = 16273;
    std::cout << 16273;
    for (int i = 0; i < 16273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16273(int& val) {
    const int x = 16273;
    std::cout << 16273;
    for (int i = 0; i < 16273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16274(int& val) {
    func_in_16274(val);
    const int x = 16274;
    std::cout << 16274;
    for (int i = 0; i < 16274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16274(int& val) {
    const int x = 16274;
    std::cout << 16274;
    for (int i = 0; i < 16274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16275(int& val) {
    func_in_16275(val);
    const int x = 16275;
    std::cout << 16275;
    for (int i = 0; i < 16275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16275(int& val) {
    const int x = 16275;
    std::cout << 16275;
    for (int i = 0; i < 16275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16276(int& val) {
    func_in_16276(val);
    const int x = 16276;
    std::cout << 16276;
    for (int i = 0; i < 16276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16276(int& val) {
    const int x = 16276;
    std::cout << 16276;
    for (int i = 0; i < 16276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16277(int& val) {
    func_in_16277(val);
    const int x = 16277;
    std::cout << 16277;
    for (int i = 0; i < 16277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16277(int& val) {
    const int x = 16277;
    std::cout << 16277;
    for (int i = 0; i < 16277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16278(int& val) {
    func_in_16278(val);
    const int x = 16278;
    std::cout << 16278;
    for (int i = 0; i < 16278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16278(int& val) {
    const int x = 16278;
    std::cout << 16278;
    for (int i = 0; i < 16278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16279(int& val) {
    func_in_16279(val);
    const int x = 16279;
    std::cout << 16279;
    for (int i = 0; i < 16279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16279(int& val) {
    const int x = 16279;
    std::cout << 16279;
    for (int i = 0; i < 16279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16280(int& val) {
    func_in_16280(val);
    const int x = 16280;
    std::cout << 16280;
    for (int i = 0; i < 16280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16280(int& val) {
    const int x = 16280;
    std::cout << 16280;
    for (int i = 0; i < 16280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16281(int& val) {
    func_in_16281(val);
    const int x = 16281;
    std::cout << 16281;
    for (int i = 0; i < 16281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16281(int& val) {
    const int x = 16281;
    std::cout << 16281;
    for (int i = 0; i < 16281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16282(int& val) {
    func_in_16282(val);
    const int x = 16282;
    std::cout << 16282;
    for (int i = 0; i < 16282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16282(int& val) {
    const int x = 16282;
    std::cout << 16282;
    for (int i = 0; i < 16282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16283(int& val) {
    func_in_16283(val);
    const int x = 16283;
    std::cout << 16283;
    for (int i = 0; i < 16283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16283(int& val) {
    const int x = 16283;
    std::cout << 16283;
    for (int i = 0; i < 16283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16284(int& val) {
    func_in_16284(val);
    const int x = 16284;
    std::cout << 16284;
    for (int i = 0; i < 16284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16284(int& val) {
    const int x = 16284;
    std::cout << 16284;
    for (int i = 0; i < 16284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16285(int& val) {
    func_in_16285(val);
    const int x = 16285;
    std::cout << 16285;
    for (int i = 0; i < 16285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16285(int& val) {
    const int x = 16285;
    std::cout << 16285;
    for (int i = 0; i < 16285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16286(int& val) {
    func_in_16286(val);
    const int x = 16286;
    std::cout << 16286;
    for (int i = 0; i < 16286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16286(int& val) {
    const int x = 16286;
    std::cout << 16286;
    for (int i = 0; i < 16286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16287(int& val) {
    func_in_16287(val);
    const int x = 16287;
    std::cout << 16287;
    for (int i = 0; i < 16287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16287(int& val) {
    const int x = 16287;
    std::cout << 16287;
    for (int i = 0; i < 16287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16288(int& val) {
    func_in_16288(val);
    const int x = 16288;
    std::cout << 16288;
    for (int i = 0; i < 16288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16288(int& val) {
    const int x = 16288;
    std::cout << 16288;
    for (int i = 0; i < 16288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16289(int& val) {
    func_in_16289(val);
    const int x = 16289;
    std::cout << 16289;
    for (int i = 0; i < 16289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16289(int& val) {
    const int x = 16289;
    std::cout << 16289;
    for (int i = 0; i < 16289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16290(int& val) {
    func_in_16290(val);
    const int x = 16290;
    std::cout << 16290;
    for (int i = 0; i < 16290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16290(int& val) {
    const int x = 16290;
    std::cout << 16290;
    for (int i = 0; i < 16290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16291(int& val) {
    func_in_16291(val);
    const int x = 16291;
    std::cout << 16291;
    for (int i = 0; i < 16291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16291(int& val) {
    const int x = 16291;
    std::cout << 16291;
    for (int i = 0; i < 16291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16292(int& val) {
    func_in_16292(val);
    const int x = 16292;
    std::cout << 16292;
    for (int i = 0; i < 16292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16292(int& val) {
    const int x = 16292;
    std::cout << 16292;
    for (int i = 0; i < 16292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16293(int& val) {
    func_in_16293(val);
    const int x = 16293;
    std::cout << 16293;
    for (int i = 0; i < 16293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16293(int& val) {
    const int x = 16293;
    std::cout << 16293;
    for (int i = 0; i < 16293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16294(int& val) {
    func_in_16294(val);
    const int x = 16294;
    std::cout << 16294;
    for (int i = 0; i < 16294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16294(int& val) {
    const int x = 16294;
    std::cout << 16294;
    for (int i = 0; i < 16294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16295(int& val) {
    func_in_16295(val);
    const int x = 16295;
    std::cout << 16295;
    for (int i = 0; i < 16295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16295(int& val) {
    const int x = 16295;
    std::cout << 16295;
    for (int i = 0; i < 16295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16296(int& val) {
    func_in_16296(val);
    const int x = 16296;
    std::cout << 16296;
    for (int i = 0; i < 16296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16296(int& val) {
    const int x = 16296;
    std::cout << 16296;
    for (int i = 0; i < 16296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16297(int& val) {
    func_in_16297(val);
    const int x = 16297;
    std::cout << 16297;
    for (int i = 0; i < 16297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16297(int& val) {
    const int x = 16297;
    std::cout << 16297;
    for (int i = 0; i < 16297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16298(int& val) {
    func_in_16298(val);
    const int x = 16298;
    std::cout << 16298;
    for (int i = 0; i < 16298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16298(int& val) {
    const int x = 16298;
    std::cout << 16298;
    for (int i = 0; i < 16298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16299(int& val) {
    func_in_16299(val);
    const int x = 16299;
    std::cout << 16299;
    for (int i = 0; i < 16299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16299(int& val) {
    const int x = 16299;
    std::cout << 16299;
    for (int i = 0; i < 16299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16300(int& val) {
    func_in_16300(val);
    const int x = 16300;
    std::cout << 16300;
    for (int i = 0; i < 16300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16300(int& val) {
    const int x = 16300;
    std::cout << 16300;
    for (int i = 0; i < 16300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16301(int& val) {
    func_in_16301(val);
    const int x = 16301;
    std::cout << 16301;
    for (int i = 0; i < 16301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16301(int& val) {
    const int x = 16301;
    std::cout << 16301;
    for (int i = 0; i < 16301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16302(int& val) {
    func_in_16302(val);
    const int x = 16302;
    std::cout << 16302;
    for (int i = 0; i < 16302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16302(int& val) {
    const int x = 16302;
    std::cout << 16302;
    for (int i = 0; i < 16302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16303(int& val) {
    func_in_16303(val);
    const int x = 16303;
    std::cout << 16303;
    for (int i = 0; i < 16303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16303(int& val) {
    const int x = 16303;
    std::cout << 16303;
    for (int i = 0; i < 16303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16304(int& val) {
    func_in_16304(val);
    const int x = 16304;
    std::cout << 16304;
    for (int i = 0; i < 16304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16304(int& val) {
    const int x = 16304;
    std::cout << 16304;
    for (int i = 0; i < 16304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16305(int& val) {
    func_in_16305(val);
    const int x = 16305;
    std::cout << 16305;
    for (int i = 0; i < 16305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16305(int& val) {
    const int x = 16305;
    std::cout << 16305;
    for (int i = 0; i < 16305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16306(int& val) {
    func_in_16306(val);
    const int x = 16306;
    std::cout << 16306;
    for (int i = 0; i < 16306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16306(int& val) {
    const int x = 16306;
    std::cout << 16306;
    for (int i = 0; i < 16306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16307(int& val) {
    func_in_16307(val);
    const int x = 16307;
    std::cout << 16307;
    for (int i = 0; i < 16307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16307(int& val) {
    const int x = 16307;
    std::cout << 16307;
    for (int i = 0; i < 16307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16308(int& val) {
    func_in_16308(val);
    const int x = 16308;
    std::cout << 16308;
    for (int i = 0; i < 16308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16308(int& val) {
    const int x = 16308;
    std::cout << 16308;
    for (int i = 0; i < 16308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16309(int& val) {
    func_in_16309(val);
    const int x = 16309;
    std::cout << 16309;
    for (int i = 0; i < 16309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16309(int& val) {
    const int x = 16309;
    std::cout << 16309;
    for (int i = 0; i < 16309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16310(int& val) {
    func_in_16310(val);
    const int x = 16310;
    std::cout << 16310;
    for (int i = 0; i < 16310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16310(int& val) {
    const int x = 16310;
    std::cout << 16310;
    for (int i = 0; i < 16310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16311(int& val) {
    func_in_16311(val);
    const int x = 16311;
    std::cout << 16311;
    for (int i = 0; i < 16311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16311(int& val) {
    const int x = 16311;
    std::cout << 16311;
    for (int i = 0; i < 16311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16312(int& val) {
    func_in_16312(val);
    const int x = 16312;
    std::cout << 16312;
    for (int i = 0; i < 16312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16312(int& val) {
    const int x = 16312;
    std::cout << 16312;
    for (int i = 0; i < 16312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16313(int& val) {
    func_in_16313(val);
    const int x = 16313;
    std::cout << 16313;
    for (int i = 0; i < 16313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16313(int& val) {
    const int x = 16313;
    std::cout << 16313;
    for (int i = 0; i < 16313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16314(int& val) {
    func_in_16314(val);
    const int x = 16314;
    std::cout << 16314;
    for (int i = 0; i < 16314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16314(int& val) {
    const int x = 16314;
    std::cout << 16314;
    for (int i = 0; i < 16314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16315(int& val) {
    func_in_16315(val);
    const int x = 16315;
    std::cout << 16315;
    for (int i = 0; i < 16315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16315(int& val) {
    const int x = 16315;
    std::cout << 16315;
    for (int i = 0; i < 16315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16316(int& val) {
    func_in_16316(val);
    const int x = 16316;
    std::cout << 16316;
    for (int i = 0; i < 16316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16316(int& val) {
    const int x = 16316;
    std::cout << 16316;
    for (int i = 0; i < 16316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16317(int& val) {
    func_in_16317(val);
    const int x = 16317;
    std::cout << 16317;
    for (int i = 0; i < 16317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16317(int& val) {
    const int x = 16317;
    std::cout << 16317;
    for (int i = 0; i < 16317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16318(int& val) {
    func_in_16318(val);
    const int x = 16318;
    std::cout << 16318;
    for (int i = 0; i < 16318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16318(int& val) {
    const int x = 16318;
    std::cout << 16318;
    for (int i = 0; i < 16318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16319(int& val) {
    func_in_16319(val);
    const int x = 16319;
    std::cout << 16319;
    for (int i = 0; i < 16319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16319(int& val) {
    const int x = 16319;
    std::cout << 16319;
    for (int i = 0; i < 16319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16320(int& val) {
    func_in_16320(val);
    const int x = 16320;
    std::cout << 16320;
    for (int i = 0; i < 16320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16320(int& val) {
    const int x = 16320;
    std::cout << 16320;
    for (int i = 0; i < 16320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16321(int& val) {
    func_in_16321(val);
    const int x = 16321;
    std::cout << 16321;
    for (int i = 0; i < 16321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16321(int& val) {
    const int x = 16321;
    std::cout << 16321;
    for (int i = 0; i < 16321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16322(int& val) {
    func_in_16322(val);
    const int x = 16322;
    std::cout << 16322;
    for (int i = 0; i < 16322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16322(int& val) {
    const int x = 16322;
    std::cout << 16322;
    for (int i = 0; i < 16322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16323(int& val) {
    func_in_16323(val);
    const int x = 16323;
    std::cout << 16323;
    for (int i = 0; i < 16323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16323(int& val) {
    const int x = 16323;
    std::cout << 16323;
    for (int i = 0; i < 16323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16324(int& val) {
    func_in_16324(val);
    const int x = 16324;
    std::cout << 16324;
    for (int i = 0; i < 16324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16324(int& val) {
    const int x = 16324;
    std::cout << 16324;
    for (int i = 0; i < 16324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16325(int& val) {
    func_in_16325(val);
    const int x = 16325;
    std::cout << 16325;
    for (int i = 0; i < 16325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16325(int& val) {
    const int x = 16325;
    std::cout << 16325;
    for (int i = 0; i < 16325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16326(int& val) {
    func_in_16326(val);
    const int x = 16326;
    std::cout << 16326;
    for (int i = 0; i < 16326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16326(int& val) {
    const int x = 16326;
    std::cout << 16326;
    for (int i = 0; i < 16326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16327(int& val) {
    func_in_16327(val);
    const int x = 16327;
    std::cout << 16327;
    for (int i = 0; i < 16327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16327(int& val) {
    const int x = 16327;
    std::cout << 16327;
    for (int i = 0; i < 16327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16328(int& val) {
    func_in_16328(val);
    const int x = 16328;
    std::cout << 16328;
    for (int i = 0; i < 16328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16328(int& val) {
    const int x = 16328;
    std::cout << 16328;
    for (int i = 0; i < 16328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16329(int& val) {
    func_in_16329(val);
    const int x = 16329;
    std::cout << 16329;
    for (int i = 0; i < 16329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16329(int& val) {
    const int x = 16329;
    std::cout << 16329;
    for (int i = 0; i < 16329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16330(int& val) {
    func_in_16330(val);
    const int x = 16330;
    std::cout << 16330;
    for (int i = 0; i < 16330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16330(int& val) {
    const int x = 16330;
    std::cout << 16330;
    for (int i = 0; i < 16330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16331(int& val) {
    func_in_16331(val);
    const int x = 16331;
    std::cout << 16331;
    for (int i = 0; i < 16331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16331(int& val) {
    const int x = 16331;
    std::cout << 16331;
    for (int i = 0; i < 16331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16332(int& val) {
    func_in_16332(val);
    const int x = 16332;
    std::cout << 16332;
    for (int i = 0; i < 16332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16332(int& val) {
    const int x = 16332;
    std::cout << 16332;
    for (int i = 0; i < 16332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16333(int& val) {
    func_in_16333(val);
    const int x = 16333;
    std::cout << 16333;
    for (int i = 0; i < 16333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16333(int& val) {
    const int x = 16333;
    std::cout << 16333;
    for (int i = 0; i < 16333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16334(int& val) {
    func_in_16334(val);
    const int x = 16334;
    std::cout << 16334;
    for (int i = 0; i < 16334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16334(int& val) {
    const int x = 16334;
    std::cout << 16334;
    for (int i = 0; i < 16334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16335(int& val) {
    func_in_16335(val);
    const int x = 16335;
    std::cout << 16335;
    for (int i = 0; i < 16335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16335(int& val) {
    const int x = 16335;
    std::cout << 16335;
    for (int i = 0; i < 16335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16336(int& val) {
    func_in_16336(val);
    const int x = 16336;
    std::cout << 16336;
    for (int i = 0; i < 16336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16336(int& val) {
    const int x = 16336;
    std::cout << 16336;
    for (int i = 0; i < 16336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16337(int& val) {
    func_in_16337(val);
    const int x = 16337;
    std::cout << 16337;
    for (int i = 0; i < 16337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16337(int& val) {
    const int x = 16337;
    std::cout << 16337;
    for (int i = 0; i < 16337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16338(int& val) {
    func_in_16338(val);
    const int x = 16338;
    std::cout << 16338;
    for (int i = 0; i < 16338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16338(int& val) {
    const int x = 16338;
    std::cout << 16338;
    for (int i = 0; i < 16338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16339(int& val) {
    func_in_16339(val);
    const int x = 16339;
    std::cout << 16339;
    for (int i = 0; i < 16339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16339(int& val) {
    const int x = 16339;
    std::cout << 16339;
    for (int i = 0; i < 16339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16340(int& val) {
    func_in_16340(val);
    const int x = 16340;
    std::cout << 16340;
    for (int i = 0; i < 16340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16340(int& val) {
    const int x = 16340;
    std::cout << 16340;
    for (int i = 0; i < 16340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16341(int& val) {
    func_in_16341(val);
    const int x = 16341;
    std::cout << 16341;
    for (int i = 0; i < 16341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16341(int& val) {
    const int x = 16341;
    std::cout << 16341;
    for (int i = 0; i < 16341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16342(int& val) {
    func_in_16342(val);
    const int x = 16342;
    std::cout << 16342;
    for (int i = 0; i < 16342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16342(int& val) {
    const int x = 16342;
    std::cout << 16342;
    for (int i = 0; i < 16342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16343(int& val) {
    func_in_16343(val);
    const int x = 16343;
    std::cout << 16343;
    for (int i = 0; i < 16343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16343(int& val) {
    const int x = 16343;
    std::cout << 16343;
    for (int i = 0; i < 16343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16344(int& val) {
    func_in_16344(val);
    const int x = 16344;
    std::cout << 16344;
    for (int i = 0; i < 16344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16344(int& val) {
    const int x = 16344;
    std::cout << 16344;
    for (int i = 0; i < 16344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16345(int& val) {
    func_in_16345(val);
    const int x = 16345;
    std::cout << 16345;
    for (int i = 0; i < 16345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16345(int& val) {
    const int x = 16345;
    std::cout << 16345;
    for (int i = 0; i < 16345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16346(int& val) {
    func_in_16346(val);
    const int x = 16346;
    std::cout << 16346;
    for (int i = 0; i < 16346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16346(int& val) {
    const int x = 16346;
    std::cout << 16346;
    for (int i = 0; i < 16346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16347(int& val) {
    func_in_16347(val);
    const int x = 16347;
    std::cout << 16347;
    for (int i = 0; i < 16347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16347(int& val) {
    const int x = 16347;
    std::cout << 16347;
    for (int i = 0; i < 16347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16348(int& val) {
    func_in_16348(val);
    const int x = 16348;
    std::cout << 16348;
    for (int i = 0; i < 16348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16348(int& val) {
    const int x = 16348;
    std::cout << 16348;
    for (int i = 0; i < 16348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16349(int& val) {
    func_in_16349(val);
    const int x = 16349;
    std::cout << 16349;
    for (int i = 0; i < 16349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16349(int& val) {
    const int x = 16349;
    std::cout << 16349;
    for (int i = 0; i < 16349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16350(int& val) {
    func_in_16350(val);
    const int x = 16350;
    std::cout << 16350;
    for (int i = 0; i < 16350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16350(int& val) {
    const int x = 16350;
    std::cout << 16350;
    for (int i = 0; i < 16350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16351(int& val) {
    func_in_16351(val);
    const int x = 16351;
    std::cout << 16351;
    for (int i = 0; i < 16351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16351(int& val) {
    const int x = 16351;
    std::cout << 16351;
    for (int i = 0; i < 16351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16352(int& val) {
    func_in_16352(val);
    const int x = 16352;
    std::cout << 16352;
    for (int i = 0; i < 16352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16352(int& val) {
    const int x = 16352;
    std::cout << 16352;
    for (int i = 0; i < 16352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16353(int& val) {
    func_in_16353(val);
    const int x = 16353;
    std::cout << 16353;
    for (int i = 0; i < 16353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16353(int& val) {
    const int x = 16353;
    std::cout << 16353;
    for (int i = 0; i < 16353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16354(int& val) {
    func_in_16354(val);
    const int x = 16354;
    std::cout << 16354;
    for (int i = 0; i < 16354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16354(int& val) {
    const int x = 16354;
    std::cout << 16354;
    for (int i = 0; i < 16354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16355(int& val) {
    func_in_16355(val);
    const int x = 16355;
    std::cout << 16355;
    for (int i = 0; i < 16355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16355(int& val) {
    const int x = 16355;
    std::cout << 16355;
    for (int i = 0; i < 16355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16356(int& val) {
    func_in_16356(val);
    const int x = 16356;
    std::cout << 16356;
    for (int i = 0; i < 16356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16356(int& val) {
    const int x = 16356;
    std::cout << 16356;
    for (int i = 0; i < 16356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16357(int& val) {
    func_in_16357(val);
    const int x = 16357;
    std::cout << 16357;
    for (int i = 0; i < 16357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16357(int& val) {
    const int x = 16357;
    std::cout << 16357;
    for (int i = 0; i < 16357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16358(int& val) {
    func_in_16358(val);
    const int x = 16358;
    std::cout << 16358;
    for (int i = 0; i < 16358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16358(int& val) {
    const int x = 16358;
    std::cout << 16358;
    for (int i = 0; i < 16358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16359(int& val) {
    func_in_16359(val);
    const int x = 16359;
    std::cout << 16359;
    for (int i = 0; i < 16359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16359(int& val) {
    const int x = 16359;
    std::cout << 16359;
    for (int i = 0; i < 16359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16360(int& val) {
    func_in_16360(val);
    const int x = 16360;
    std::cout << 16360;
    for (int i = 0; i < 16360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16360(int& val) {
    const int x = 16360;
    std::cout << 16360;
    for (int i = 0; i < 16360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16361(int& val) {
    func_in_16361(val);
    const int x = 16361;
    std::cout << 16361;
    for (int i = 0; i < 16361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16361(int& val) {
    const int x = 16361;
    std::cout << 16361;
    for (int i = 0; i < 16361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16362(int& val) {
    func_in_16362(val);
    const int x = 16362;
    std::cout << 16362;
    for (int i = 0; i < 16362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16362(int& val) {
    const int x = 16362;
    std::cout << 16362;
    for (int i = 0; i < 16362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16363(int& val) {
    func_in_16363(val);
    const int x = 16363;
    std::cout << 16363;
    for (int i = 0; i < 16363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16363(int& val) {
    const int x = 16363;
    std::cout << 16363;
    for (int i = 0; i < 16363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16364(int& val) {
    func_in_16364(val);
    const int x = 16364;
    std::cout << 16364;
    for (int i = 0; i < 16364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16364(int& val) {
    const int x = 16364;
    std::cout << 16364;
    for (int i = 0; i < 16364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16365(int& val) {
    func_in_16365(val);
    const int x = 16365;
    std::cout << 16365;
    for (int i = 0; i < 16365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16365(int& val) {
    const int x = 16365;
    std::cout << 16365;
    for (int i = 0; i < 16365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16366(int& val) {
    func_in_16366(val);
    const int x = 16366;
    std::cout << 16366;
    for (int i = 0; i < 16366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16366(int& val) {
    const int x = 16366;
    std::cout << 16366;
    for (int i = 0; i < 16366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16367(int& val) {
    func_in_16367(val);
    const int x = 16367;
    std::cout << 16367;
    for (int i = 0; i < 16367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16367(int& val) {
    const int x = 16367;
    std::cout << 16367;
    for (int i = 0; i < 16367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16368(int& val) {
    func_in_16368(val);
    const int x = 16368;
    std::cout << 16368;
    for (int i = 0; i < 16368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16368(int& val) {
    const int x = 16368;
    std::cout << 16368;
    for (int i = 0; i < 16368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16369(int& val) {
    func_in_16369(val);
    const int x = 16369;
    std::cout << 16369;
    for (int i = 0; i < 16369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16369(int& val) {
    const int x = 16369;
    std::cout << 16369;
    for (int i = 0; i < 16369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16370(int& val) {
    func_in_16370(val);
    const int x = 16370;
    std::cout << 16370;
    for (int i = 0; i < 16370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16370(int& val) {
    const int x = 16370;
    std::cout << 16370;
    for (int i = 0; i < 16370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16371(int& val) {
    func_in_16371(val);
    const int x = 16371;
    std::cout << 16371;
    for (int i = 0; i < 16371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16371(int& val) {
    const int x = 16371;
    std::cout << 16371;
    for (int i = 0; i < 16371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16372(int& val) {
    func_in_16372(val);
    const int x = 16372;
    std::cout << 16372;
    for (int i = 0; i < 16372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16372(int& val) {
    const int x = 16372;
    std::cout << 16372;
    for (int i = 0; i < 16372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16373(int& val) {
    func_in_16373(val);
    const int x = 16373;
    std::cout << 16373;
    for (int i = 0; i < 16373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16373(int& val) {
    const int x = 16373;
    std::cout << 16373;
    for (int i = 0; i < 16373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16374(int& val) {
    func_in_16374(val);
    const int x = 16374;
    std::cout << 16374;
    for (int i = 0; i < 16374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16374(int& val) {
    const int x = 16374;
    std::cout << 16374;
    for (int i = 0; i < 16374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16375(int& val) {
    func_in_16375(val);
    const int x = 16375;
    std::cout << 16375;
    for (int i = 0; i < 16375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16375(int& val) {
    const int x = 16375;
    std::cout << 16375;
    for (int i = 0; i < 16375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16376(int& val) {
    func_in_16376(val);
    const int x = 16376;
    std::cout << 16376;
    for (int i = 0; i < 16376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16376(int& val) {
    const int x = 16376;
    std::cout << 16376;
    for (int i = 0; i < 16376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16377(int& val) {
    func_in_16377(val);
    const int x = 16377;
    std::cout << 16377;
    for (int i = 0; i < 16377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16377(int& val) {
    const int x = 16377;
    std::cout << 16377;
    for (int i = 0; i < 16377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16378(int& val) {
    func_in_16378(val);
    const int x = 16378;
    std::cout << 16378;
    for (int i = 0; i < 16378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16378(int& val) {
    const int x = 16378;
    std::cout << 16378;
    for (int i = 0; i < 16378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16379(int& val) {
    func_in_16379(val);
    const int x = 16379;
    std::cout << 16379;
    for (int i = 0; i < 16379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16379(int& val) {
    const int x = 16379;
    std::cout << 16379;
    for (int i = 0; i < 16379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16380(int& val) {
    func_in_16380(val);
    const int x = 16380;
    std::cout << 16380;
    for (int i = 0; i < 16380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16380(int& val) {
    const int x = 16380;
    std::cout << 16380;
    for (int i = 0; i < 16380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16381(int& val) {
    func_in_16381(val);
    const int x = 16381;
    std::cout << 16381;
    for (int i = 0; i < 16381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16381(int& val) {
    const int x = 16381;
    std::cout << 16381;
    for (int i = 0; i < 16381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16382(int& val) {
    func_in_16382(val);
    const int x = 16382;
    std::cout << 16382;
    for (int i = 0; i < 16382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16382(int& val) {
    const int x = 16382;
    std::cout << 16382;
    for (int i = 0; i < 16382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16383(int& val) {
    func_in_16383(val);
    const int x = 16383;
    std::cout << 16383;
    for (int i = 0; i < 16383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16383(int& val) {
    const int x = 16383;
    std::cout << 16383;
    for (int i = 0; i < 16383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16384(int& val) {
    func_in_16384(val);
    const int x = 16384;
    std::cout << 16384;
    for (int i = 0; i < 16384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16384(int& val) {
    const int x = 16384;
    std::cout << 16384;
    for (int i = 0; i < 16384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16385(int& val) {
    func_in_16385(val);
    const int x = 16385;
    std::cout << 16385;
    for (int i = 0; i < 16385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16385(int& val) {
    const int x = 16385;
    std::cout << 16385;
    for (int i = 0; i < 16385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16386(int& val) {
    func_in_16386(val);
    const int x = 16386;
    std::cout << 16386;
    for (int i = 0; i < 16386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16386(int& val) {
    const int x = 16386;
    std::cout << 16386;
    for (int i = 0; i < 16386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16387(int& val) {
    func_in_16387(val);
    const int x = 16387;
    std::cout << 16387;
    for (int i = 0; i < 16387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16387(int& val) {
    const int x = 16387;
    std::cout << 16387;
    for (int i = 0; i < 16387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16388(int& val) {
    func_in_16388(val);
    const int x = 16388;
    std::cout << 16388;
    for (int i = 0; i < 16388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16388(int& val) {
    const int x = 16388;
    std::cout << 16388;
    for (int i = 0; i < 16388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16389(int& val) {
    func_in_16389(val);
    const int x = 16389;
    std::cout << 16389;
    for (int i = 0; i < 16389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16389(int& val) {
    const int x = 16389;
    std::cout << 16389;
    for (int i = 0; i < 16389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16390(int& val) {
    func_in_16390(val);
    const int x = 16390;
    std::cout << 16390;
    for (int i = 0; i < 16390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16390(int& val) {
    const int x = 16390;
    std::cout << 16390;
    for (int i = 0; i < 16390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16391(int& val) {
    func_in_16391(val);
    const int x = 16391;
    std::cout << 16391;
    for (int i = 0; i < 16391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16391(int& val) {
    const int x = 16391;
    std::cout << 16391;
    for (int i = 0; i < 16391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16392(int& val) {
    func_in_16392(val);
    const int x = 16392;
    std::cout << 16392;
    for (int i = 0; i < 16392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16392(int& val) {
    const int x = 16392;
    std::cout << 16392;
    for (int i = 0; i < 16392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16393(int& val) {
    func_in_16393(val);
    const int x = 16393;
    std::cout << 16393;
    for (int i = 0; i < 16393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16393(int& val) {
    const int x = 16393;
    std::cout << 16393;
    for (int i = 0; i < 16393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16394(int& val) {
    func_in_16394(val);
    const int x = 16394;
    std::cout << 16394;
    for (int i = 0; i < 16394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16394(int& val) {
    const int x = 16394;
    std::cout << 16394;
    for (int i = 0; i < 16394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16395(int& val) {
    func_in_16395(val);
    const int x = 16395;
    std::cout << 16395;
    for (int i = 0; i < 16395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16395(int& val) {
    const int x = 16395;
    std::cout << 16395;
    for (int i = 0; i < 16395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16396(int& val) {
    func_in_16396(val);
    const int x = 16396;
    std::cout << 16396;
    for (int i = 0; i < 16396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16396(int& val) {
    const int x = 16396;
    std::cout << 16396;
    for (int i = 0; i < 16396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16397(int& val) {
    func_in_16397(val);
    const int x = 16397;
    std::cout << 16397;
    for (int i = 0; i < 16397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16397(int& val) {
    const int x = 16397;
    std::cout << 16397;
    for (int i = 0; i < 16397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16398(int& val) {
    func_in_16398(val);
    const int x = 16398;
    std::cout << 16398;
    for (int i = 0; i < 16398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16398(int& val) {
    const int x = 16398;
    std::cout << 16398;
    for (int i = 0; i < 16398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16399(int& val) {
    func_in_16399(val);
    const int x = 16399;
    std::cout << 16399;
    for (int i = 0; i < 16399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16399(int& val) {
    const int x = 16399;
    std::cout << 16399;
    for (int i = 0; i < 16399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16400(int& val) {
    func_in_16400(val);
    const int x = 16400;
    std::cout << 16400;
    for (int i = 0; i < 16400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16400(int& val) {
    const int x = 16400;
    std::cout << 16400;
    for (int i = 0; i < 16400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16401(int& val) {
    func_in_16401(val);
    const int x = 16401;
    std::cout << 16401;
    for (int i = 0; i < 16401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16401(int& val) {
    const int x = 16401;
    std::cout << 16401;
    for (int i = 0; i < 16401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16402(int& val) {
    func_in_16402(val);
    const int x = 16402;
    std::cout << 16402;
    for (int i = 0; i < 16402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16402(int& val) {
    const int x = 16402;
    std::cout << 16402;
    for (int i = 0; i < 16402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16403(int& val) {
    func_in_16403(val);
    const int x = 16403;
    std::cout << 16403;
    for (int i = 0; i < 16403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16403(int& val) {
    const int x = 16403;
    std::cout << 16403;
    for (int i = 0; i < 16403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16404(int& val) {
    func_in_16404(val);
    const int x = 16404;
    std::cout << 16404;
    for (int i = 0; i < 16404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16404(int& val) {
    const int x = 16404;
    std::cout << 16404;
    for (int i = 0; i < 16404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16405(int& val) {
    func_in_16405(val);
    const int x = 16405;
    std::cout << 16405;
    for (int i = 0; i < 16405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16405(int& val) {
    const int x = 16405;
    std::cout << 16405;
    for (int i = 0; i < 16405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16406(int& val) {
    func_in_16406(val);
    const int x = 16406;
    std::cout << 16406;
    for (int i = 0; i < 16406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16406(int& val) {
    const int x = 16406;
    std::cout << 16406;
    for (int i = 0; i < 16406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16407(int& val) {
    func_in_16407(val);
    const int x = 16407;
    std::cout << 16407;
    for (int i = 0; i < 16407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16407(int& val) {
    const int x = 16407;
    std::cout << 16407;
    for (int i = 0; i < 16407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16408(int& val) {
    func_in_16408(val);
    const int x = 16408;
    std::cout << 16408;
    for (int i = 0; i < 16408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16408(int& val) {
    const int x = 16408;
    std::cout << 16408;
    for (int i = 0; i < 16408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16409(int& val) {
    func_in_16409(val);
    const int x = 16409;
    std::cout << 16409;
    for (int i = 0; i < 16409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16409(int& val) {
    const int x = 16409;
    std::cout << 16409;
    for (int i = 0; i < 16409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16410(int& val) {
    func_in_16410(val);
    const int x = 16410;
    std::cout << 16410;
    for (int i = 0; i < 16410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16410(int& val) {
    const int x = 16410;
    std::cout << 16410;
    for (int i = 0; i < 16410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16411(int& val) {
    func_in_16411(val);
    const int x = 16411;
    std::cout << 16411;
    for (int i = 0; i < 16411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16411(int& val) {
    const int x = 16411;
    std::cout << 16411;
    for (int i = 0; i < 16411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16412(int& val) {
    func_in_16412(val);
    const int x = 16412;
    std::cout << 16412;
    for (int i = 0; i < 16412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16412(int& val) {
    const int x = 16412;
    std::cout << 16412;
    for (int i = 0; i < 16412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16413(int& val) {
    func_in_16413(val);
    const int x = 16413;
    std::cout << 16413;
    for (int i = 0; i < 16413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16413(int& val) {
    const int x = 16413;
    std::cout << 16413;
    for (int i = 0; i < 16413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16414(int& val) {
    func_in_16414(val);
    const int x = 16414;
    std::cout << 16414;
    for (int i = 0; i < 16414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16414(int& val) {
    const int x = 16414;
    std::cout << 16414;
    for (int i = 0; i < 16414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16415(int& val) {
    func_in_16415(val);
    const int x = 16415;
    std::cout << 16415;
    for (int i = 0; i < 16415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16415(int& val) {
    const int x = 16415;
    std::cout << 16415;
    for (int i = 0; i < 16415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16416(int& val) {
    func_in_16416(val);
    const int x = 16416;
    std::cout << 16416;
    for (int i = 0; i < 16416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16416(int& val) {
    const int x = 16416;
    std::cout << 16416;
    for (int i = 0; i < 16416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16417(int& val) {
    func_in_16417(val);
    const int x = 16417;
    std::cout << 16417;
    for (int i = 0; i < 16417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16417(int& val) {
    const int x = 16417;
    std::cout << 16417;
    for (int i = 0; i < 16417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16418(int& val) {
    func_in_16418(val);
    const int x = 16418;
    std::cout << 16418;
    for (int i = 0; i < 16418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16418(int& val) {
    const int x = 16418;
    std::cout << 16418;
    for (int i = 0; i < 16418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16419(int& val) {
    func_in_16419(val);
    const int x = 16419;
    std::cout << 16419;
    for (int i = 0; i < 16419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16419(int& val) {
    const int x = 16419;
    std::cout << 16419;
    for (int i = 0; i < 16419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16420(int& val) {
    func_in_16420(val);
    const int x = 16420;
    std::cout << 16420;
    for (int i = 0; i < 16420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16420(int& val) {
    const int x = 16420;
    std::cout << 16420;
    for (int i = 0; i < 16420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16421(int& val) {
    func_in_16421(val);
    const int x = 16421;
    std::cout << 16421;
    for (int i = 0; i < 16421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16421(int& val) {
    const int x = 16421;
    std::cout << 16421;
    for (int i = 0; i < 16421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16422(int& val) {
    func_in_16422(val);
    const int x = 16422;
    std::cout << 16422;
    for (int i = 0; i < 16422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16422(int& val) {
    const int x = 16422;
    std::cout << 16422;
    for (int i = 0; i < 16422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16423(int& val) {
    func_in_16423(val);
    const int x = 16423;
    std::cout << 16423;
    for (int i = 0; i < 16423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16423(int& val) {
    const int x = 16423;
    std::cout << 16423;
    for (int i = 0; i < 16423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16424(int& val) {
    func_in_16424(val);
    const int x = 16424;
    std::cout << 16424;
    for (int i = 0; i < 16424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16424(int& val) {
    const int x = 16424;
    std::cout << 16424;
    for (int i = 0; i < 16424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16425(int& val) {
    func_in_16425(val);
    const int x = 16425;
    std::cout << 16425;
    for (int i = 0; i < 16425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16425(int& val) {
    const int x = 16425;
    std::cout << 16425;
    for (int i = 0; i < 16425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16426(int& val) {
    func_in_16426(val);
    const int x = 16426;
    std::cout << 16426;
    for (int i = 0; i < 16426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16426(int& val) {
    const int x = 16426;
    std::cout << 16426;
    for (int i = 0; i < 16426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16427(int& val) {
    func_in_16427(val);
    const int x = 16427;
    std::cout << 16427;
    for (int i = 0; i < 16427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16427(int& val) {
    const int x = 16427;
    std::cout << 16427;
    for (int i = 0; i < 16427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16428(int& val) {
    func_in_16428(val);
    const int x = 16428;
    std::cout << 16428;
    for (int i = 0; i < 16428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16428(int& val) {
    const int x = 16428;
    std::cout << 16428;
    for (int i = 0; i < 16428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16429(int& val) {
    func_in_16429(val);
    const int x = 16429;
    std::cout << 16429;
    for (int i = 0; i < 16429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16429(int& val) {
    const int x = 16429;
    std::cout << 16429;
    for (int i = 0; i < 16429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16430(int& val) {
    func_in_16430(val);
    const int x = 16430;
    std::cout << 16430;
    for (int i = 0; i < 16430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16430(int& val) {
    const int x = 16430;
    std::cout << 16430;
    for (int i = 0; i < 16430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16431(int& val) {
    func_in_16431(val);
    const int x = 16431;
    std::cout << 16431;
    for (int i = 0; i < 16431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16431(int& val) {
    const int x = 16431;
    std::cout << 16431;
    for (int i = 0; i < 16431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16432(int& val) {
    func_in_16432(val);
    const int x = 16432;
    std::cout << 16432;
    for (int i = 0; i < 16432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16432(int& val) {
    const int x = 16432;
    std::cout << 16432;
    for (int i = 0; i < 16432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16433(int& val) {
    func_in_16433(val);
    const int x = 16433;
    std::cout << 16433;
    for (int i = 0; i < 16433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16433(int& val) {
    const int x = 16433;
    std::cout << 16433;
    for (int i = 0; i < 16433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16434(int& val) {
    func_in_16434(val);
    const int x = 16434;
    std::cout << 16434;
    for (int i = 0; i < 16434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16434(int& val) {
    const int x = 16434;
    std::cout << 16434;
    for (int i = 0; i < 16434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16435(int& val) {
    func_in_16435(val);
    const int x = 16435;
    std::cout << 16435;
    for (int i = 0; i < 16435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16435(int& val) {
    const int x = 16435;
    std::cout << 16435;
    for (int i = 0; i < 16435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16436(int& val) {
    func_in_16436(val);
    const int x = 16436;
    std::cout << 16436;
    for (int i = 0; i < 16436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16436(int& val) {
    const int x = 16436;
    std::cout << 16436;
    for (int i = 0; i < 16436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16437(int& val) {
    func_in_16437(val);
    const int x = 16437;
    std::cout << 16437;
    for (int i = 0; i < 16437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16437(int& val) {
    const int x = 16437;
    std::cout << 16437;
    for (int i = 0; i < 16437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16438(int& val) {
    func_in_16438(val);
    const int x = 16438;
    std::cout << 16438;
    for (int i = 0; i < 16438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16438(int& val) {
    const int x = 16438;
    std::cout << 16438;
    for (int i = 0; i < 16438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16439(int& val) {
    func_in_16439(val);
    const int x = 16439;
    std::cout << 16439;
    for (int i = 0; i < 16439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16439(int& val) {
    const int x = 16439;
    std::cout << 16439;
    for (int i = 0; i < 16439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16440(int& val) {
    func_in_16440(val);
    const int x = 16440;
    std::cout << 16440;
    for (int i = 0; i < 16440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16440(int& val) {
    const int x = 16440;
    std::cout << 16440;
    for (int i = 0; i < 16440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16441(int& val) {
    func_in_16441(val);
    const int x = 16441;
    std::cout << 16441;
    for (int i = 0; i < 16441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16441(int& val) {
    const int x = 16441;
    std::cout << 16441;
    for (int i = 0; i < 16441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16442(int& val) {
    func_in_16442(val);
    const int x = 16442;
    std::cout << 16442;
    for (int i = 0; i < 16442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16442(int& val) {
    const int x = 16442;
    std::cout << 16442;
    for (int i = 0; i < 16442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16443(int& val) {
    func_in_16443(val);
    const int x = 16443;
    std::cout << 16443;
    for (int i = 0; i < 16443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16443(int& val) {
    const int x = 16443;
    std::cout << 16443;
    for (int i = 0; i < 16443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16444(int& val) {
    func_in_16444(val);
    const int x = 16444;
    std::cout << 16444;
    for (int i = 0; i < 16444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16444(int& val) {
    const int x = 16444;
    std::cout << 16444;
    for (int i = 0; i < 16444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16445(int& val) {
    func_in_16445(val);
    const int x = 16445;
    std::cout << 16445;
    for (int i = 0; i < 16445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16445(int& val) {
    const int x = 16445;
    std::cout << 16445;
    for (int i = 0; i < 16445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16446(int& val) {
    func_in_16446(val);
    const int x = 16446;
    std::cout << 16446;
    for (int i = 0; i < 16446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16446(int& val) {
    const int x = 16446;
    std::cout << 16446;
    for (int i = 0; i < 16446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16447(int& val) {
    func_in_16447(val);
    const int x = 16447;
    std::cout << 16447;
    for (int i = 0; i < 16447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16447(int& val) {
    const int x = 16447;
    std::cout << 16447;
    for (int i = 0; i < 16447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16448(int& val) {
    func_in_16448(val);
    const int x = 16448;
    std::cout << 16448;
    for (int i = 0; i < 16448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16448(int& val) {
    const int x = 16448;
    std::cout << 16448;
    for (int i = 0; i < 16448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16449(int& val) {
    func_in_16449(val);
    const int x = 16449;
    std::cout << 16449;
    for (int i = 0; i < 16449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16449(int& val) {
    const int x = 16449;
    std::cout << 16449;
    for (int i = 0; i < 16449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16450(int& val) {
    func_in_16450(val);
    const int x = 16450;
    std::cout << 16450;
    for (int i = 0; i < 16450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16450(int& val) {
    const int x = 16450;
    std::cout << 16450;
    for (int i = 0; i < 16450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16451(int& val) {
    func_in_16451(val);
    const int x = 16451;
    std::cout << 16451;
    for (int i = 0; i < 16451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16451(int& val) {
    const int x = 16451;
    std::cout << 16451;
    for (int i = 0; i < 16451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16452(int& val) {
    func_in_16452(val);
    const int x = 16452;
    std::cout << 16452;
    for (int i = 0; i < 16452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16452(int& val) {
    const int x = 16452;
    std::cout << 16452;
    for (int i = 0; i < 16452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16453(int& val) {
    func_in_16453(val);
    const int x = 16453;
    std::cout << 16453;
    for (int i = 0; i < 16453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16453(int& val) {
    const int x = 16453;
    std::cout << 16453;
    for (int i = 0; i < 16453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16454(int& val) {
    func_in_16454(val);
    const int x = 16454;
    std::cout << 16454;
    for (int i = 0; i < 16454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16454(int& val) {
    const int x = 16454;
    std::cout << 16454;
    for (int i = 0; i < 16454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16455(int& val) {
    func_in_16455(val);
    const int x = 16455;
    std::cout << 16455;
    for (int i = 0; i < 16455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16455(int& val) {
    const int x = 16455;
    std::cout << 16455;
    for (int i = 0; i < 16455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16456(int& val) {
    func_in_16456(val);
    const int x = 16456;
    std::cout << 16456;
    for (int i = 0; i < 16456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16456(int& val) {
    const int x = 16456;
    std::cout << 16456;
    for (int i = 0; i < 16456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16457(int& val) {
    func_in_16457(val);
    const int x = 16457;
    std::cout << 16457;
    for (int i = 0; i < 16457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16457(int& val) {
    const int x = 16457;
    std::cout << 16457;
    for (int i = 0; i < 16457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16458(int& val) {
    func_in_16458(val);
    const int x = 16458;
    std::cout << 16458;
    for (int i = 0; i < 16458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16458(int& val) {
    const int x = 16458;
    std::cout << 16458;
    for (int i = 0; i < 16458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16459(int& val) {
    func_in_16459(val);
    const int x = 16459;
    std::cout << 16459;
    for (int i = 0; i < 16459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16459(int& val) {
    const int x = 16459;
    std::cout << 16459;
    for (int i = 0; i < 16459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16460(int& val) {
    func_in_16460(val);
    const int x = 16460;
    std::cout << 16460;
    for (int i = 0; i < 16460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16460(int& val) {
    const int x = 16460;
    std::cout << 16460;
    for (int i = 0; i < 16460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16461(int& val) {
    func_in_16461(val);
    const int x = 16461;
    std::cout << 16461;
    for (int i = 0; i < 16461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16461(int& val) {
    const int x = 16461;
    std::cout << 16461;
    for (int i = 0; i < 16461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16462(int& val) {
    func_in_16462(val);
    const int x = 16462;
    std::cout << 16462;
    for (int i = 0; i < 16462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16462(int& val) {
    const int x = 16462;
    std::cout << 16462;
    for (int i = 0; i < 16462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16463(int& val) {
    func_in_16463(val);
    const int x = 16463;
    std::cout << 16463;
    for (int i = 0; i < 16463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16463(int& val) {
    const int x = 16463;
    std::cout << 16463;
    for (int i = 0; i < 16463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16464(int& val) {
    func_in_16464(val);
    const int x = 16464;
    std::cout << 16464;
    for (int i = 0; i < 16464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16464(int& val) {
    const int x = 16464;
    std::cout << 16464;
    for (int i = 0; i < 16464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16465(int& val) {
    func_in_16465(val);
    const int x = 16465;
    std::cout << 16465;
    for (int i = 0; i < 16465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16465(int& val) {
    const int x = 16465;
    std::cout << 16465;
    for (int i = 0; i < 16465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16466(int& val) {
    func_in_16466(val);
    const int x = 16466;
    std::cout << 16466;
    for (int i = 0; i < 16466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16466(int& val) {
    const int x = 16466;
    std::cout << 16466;
    for (int i = 0; i < 16466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16467(int& val) {
    func_in_16467(val);
    const int x = 16467;
    std::cout << 16467;
    for (int i = 0; i < 16467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16467(int& val) {
    const int x = 16467;
    std::cout << 16467;
    for (int i = 0; i < 16467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16468(int& val) {
    func_in_16468(val);
    const int x = 16468;
    std::cout << 16468;
    for (int i = 0; i < 16468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16468(int& val) {
    const int x = 16468;
    std::cout << 16468;
    for (int i = 0; i < 16468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16469(int& val) {
    func_in_16469(val);
    const int x = 16469;
    std::cout << 16469;
    for (int i = 0; i < 16469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16469(int& val) {
    const int x = 16469;
    std::cout << 16469;
    for (int i = 0; i < 16469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16470(int& val) {
    func_in_16470(val);
    const int x = 16470;
    std::cout << 16470;
    for (int i = 0; i < 16470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16470(int& val) {
    const int x = 16470;
    std::cout << 16470;
    for (int i = 0; i < 16470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16471(int& val) {
    func_in_16471(val);
    const int x = 16471;
    std::cout << 16471;
    for (int i = 0; i < 16471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16471(int& val) {
    const int x = 16471;
    std::cout << 16471;
    for (int i = 0; i < 16471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16472(int& val) {
    func_in_16472(val);
    const int x = 16472;
    std::cout << 16472;
    for (int i = 0; i < 16472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16472(int& val) {
    const int x = 16472;
    std::cout << 16472;
    for (int i = 0; i < 16472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16473(int& val) {
    func_in_16473(val);
    const int x = 16473;
    std::cout << 16473;
    for (int i = 0; i < 16473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16473(int& val) {
    const int x = 16473;
    std::cout << 16473;
    for (int i = 0; i < 16473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16474(int& val) {
    func_in_16474(val);
    const int x = 16474;
    std::cout << 16474;
    for (int i = 0; i < 16474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16474(int& val) {
    const int x = 16474;
    std::cout << 16474;
    for (int i = 0; i < 16474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16475(int& val) {
    func_in_16475(val);
    const int x = 16475;
    std::cout << 16475;
    for (int i = 0; i < 16475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16475(int& val) {
    const int x = 16475;
    std::cout << 16475;
    for (int i = 0; i < 16475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16476(int& val) {
    func_in_16476(val);
    const int x = 16476;
    std::cout << 16476;
    for (int i = 0; i < 16476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16476(int& val) {
    const int x = 16476;
    std::cout << 16476;
    for (int i = 0; i < 16476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16477(int& val) {
    func_in_16477(val);
    const int x = 16477;
    std::cout << 16477;
    for (int i = 0; i < 16477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16477(int& val) {
    const int x = 16477;
    std::cout << 16477;
    for (int i = 0; i < 16477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16478(int& val) {
    func_in_16478(val);
    const int x = 16478;
    std::cout << 16478;
    for (int i = 0; i < 16478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16478(int& val) {
    const int x = 16478;
    std::cout << 16478;
    for (int i = 0; i < 16478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16479(int& val) {
    func_in_16479(val);
    const int x = 16479;
    std::cout << 16479;
    for (int i = 0; i < 16479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16479(int& val) {
    const int x = 16479;
    std::cout << 16479;
    for (int i = 0; i < 16479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16480(int& val) {
    func_in_16480(val);
    const int x = 16480;
    std::cout << 16480;
    for (int i = 0; i < 16480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16480(int& val) {
    const int x = 16480;
    std::cout << 16480;
    for (int i = 0; i < 16480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16481(int& val) {
    func_in_16481(val);
    const int x = 16481;
    std::cout << 16481;
    for (int i = 0; i < 16481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16481(int& val) {
    const int x = 16481;
    std::cout << 16481;
    for (int i = 0; i < 16481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16482(int& val) {
    func_in_16482(val);
    const int x = 16482;
    std::cout << 16482;
    for (int i = 0; i < 16482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16482(int& val) {
    const int x = 16482;
    std::cout << 16482;
    for (int i = 0; i < 16482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16483(int& val) {
    func_in_16483(val);
    const int x = 16483;
    std::cout << 16483;
    for (int i = 0; i < 16483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16483(int& val) {
    const int x = 16483;
    std::cout << 16483;
    for (int i = 0; i < 16483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16484(int& val) {
    func_in_16484(val);
    const int x = 16484;
    std::cout << 16484;
    for (int i = 0; i < 16484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16484(int& val) {
    const int x = 16484;
    std::cout << 16484;
    for (int i = 0; i < 16484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16485(int& val) {
    func_in_16485(val);
    const int x = 16485;
    std::cout << 16485;
    for (int i = 0; i < 16485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16485(int& val) {
    const int x = 16485;
    std::cout << 16485;
    for (int i = 0; i < 16485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16486(int& val) {
    func_in_16486(val);
    const int x = 16486;
    std::cout << 16486;
    for (int i = 0; i < 16486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16486(int& val) {
    const int x = 16486;
    std::cout << 16486;
    for (int i = 0; i < 16486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16487(int& val) {
    func_in_16487(val);
    const int x = 16487;
    std::cout << 16487;
    for (int i = 0; i < 16487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16487(int& val) {
    const int x = 16487;
    std::cout << 16487;
    for (int i = 0; i < 16487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16488(int& val) {
    func_in_16488(val);
    const int x = 16488;
    std::cout << 16488;
    for (int i = 0; i < 16488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16488(int& val) {
    const int x = 16488;
    std::cout << 16488;
    for (int i = 0; i < 16488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16489(int& val) {
    func_in_16489(val);
    const int x = 16489;
    std::cout << 16489;
    for (int i = 0; i < 16489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16489(int& val) {
    const int x = 16489;
    std::cout << 16489;
    for (int i = 0; i < 16489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16490(int& val) {
    func_in_16490(val);
    const int x = 16490;
    std::cout << 16490;
    for (int i = 0; i < 16490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16490(int& val) {
    const int x = 16490;
    std::cout << 16490;
    for (int i = 0; i < 16490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16491(int& val) {
    func_in_16491(val);
    const int x = 16491;
    std::cout << 16491;
    for (int i = 0; i < 16491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16491(int& val) {
    const int x = 16491;
    std::cout << 16491;
    for (int i = 0; i < 16491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16492(int& val) {
    func_in_16492(val);
    const int x = 16492;
    std::cout << 16492;
    for (int i = 0; i < 16492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16492(int& val) {
    const int x = 16492;
    std::cout << 16492;
    for (int i = 0; i < 16492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16493(int& val) {
    func_in_16493(val);
    const int x = 16493;
    std::cout << 16493;
    for (int i = 0; i < 16493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16493(int& val) {
    const int x = 16493;
    std::cout << 16493;
    for (int i = 0; i < 16493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16494(int& val) {
    func_in_16494(val);
    const int x = 16494;
    std::cout << 16494;
    for (int i = 0; i < 16494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16494(int& val) {
    const int x = 16494;
    std::cout << 16494;
    for (int i = 0; i < 16494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16495(int& val) {
    func_in_16495(val);
    const int x = 16495;
    std::cout << 16495;
    for (int i = 0; i < 16495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16495(int& val) {
    const int x = 16495;
    std::cout << 16495;
    for (int i = 0; i < 16495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16496(int& val) {
    func_in_16496(val);
    const int x = 16496;
    std::cout << 16496;
    for (int i = 0; i < 16496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16496(int& val) {
    const int x = 16496;
    std::cout << 16496;
    for (int i = 0; i < 16496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16497(int& val) {
    func_in_16497(val);
    const int x = 16497;
    std::cout << 16497;
    for (int i = 0; i < 16497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16497(int& val) {
    const int x = 16497;
    std::cout << 16497;
    for (int i = 0; i < 16497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16498(int& val) {
    func_in_16498(val);
    const int x = 16498;
    std::cout << 16498;
    for (int i = 0; i < 16498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16498(int& val) {
    const int x = 16498;
    std::cout << 16498;
    for (int i = 0; i < 16498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16499(int& val) {
    func_in_16499(val);
    const int x = 16499;
    std::cout << 16499;
    for (int i = 0; i < 16499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16499(int& val) {
    const int x = 16499;
    std::cout << 16499;
    for (int i = 0; i < 16499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16500(int& val) {
    func_in_16500(val);
    const int x = 16500;
    std::cout << 16500;
    for (int i = 0; i < 16500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16500(int& val) {
    const int x = 16500;
    std::cout << 16500;
    for (int i = 0; i < 16500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16501(int& val) {
    func_in_16501(val);
    const int x = 16501;
    std::cout << 16501;
    for (int i = 0; i < 16501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16501(int& val) {
    const int x = 16501;
    std::cout << 16501;
    for (int i = 0; i < 16501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16502(int& val) {
    func_in_16502(val);
    const int x = 16502;
    std::cout << 16502;
    for (int i = 0; i < 16502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16502(int& val) {
    const int x = 16502;
    std::cout << 16502;
    for (int i = 0; i < 16502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16503(int& val) {
    func_in_16503(val);
    const int x = 16503;
    std::cout << 16503;
    for (int i = 0; i < 16503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16503(int& val) {
    const int x = 16503;
    std::cout << 16503;
    for (int i = 0; i < 16503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16504(int& val) {
    func_in_16504(val);
    const int x = 16504;
    std::cout << 16504;
    for (int i = 0; i < 16504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16504(int& val) {
    const int x = 16504;
    std::cout << 16504;
    for (int i = 0; i < 16504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16505(int& val) {
    func_in_16505(val);
    const int x = 16505;
    std::cout << 16505;
    for (int i = 0; i < 16505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16505(int& val) {
    const int x = 16505;
    std::cout << 16505;
    for (int i = 0; i < 16505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16506(int& val) {
    func_in_16506(val);
    const int x = 16506;
    std::cout << 16506;
    for (int i = 0; i < 16506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16506(int& val) {
    const int x = 16506;
    std::cout << 16506;
    for (int i = 0; i < 16506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16507(int& val) {
    func_in_16507(val);
    const int x = 16507;
    std::cout << 16507;
    for (int i = 0; i < 16507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16507(int& val) {
    const int x = 16507;
    std::cout << 16507;
    for (int i = 0; i < 16507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16508(int& val) {
    func_in_16508(val);
    const int x = 16508;
    std::cout << 16508;
    for (int i = 0; i < 16508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16508(int& val) {
    const int x = 16508;
    std::cout << 16508;
    for (int i = 0; i < 16508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16509(int& val) {
    func_in_16509(val);
    const int x = 16509;
    std::cout << 16509;
    for (int i = 0; i < 16509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16509(int& val) {
    const int x = 16509;
    std::cout << 16509;
    for (int i = 0; i < 16509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16510(int& val) {
    func_in_16510(val);
    const int x = 16510;
    std::cout << 16510;
    for (int i = 0; i < 16510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16510(int& val) {
    const int x = 16510;
    std::cout << 16510;
    for (int i = 0; i < 16510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16511(int& val) {
    func_in_16511(val);
    const int x = 16511;
    std::cout << 16511;
    for (int i = 0; i < 16511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16511(int& val) {
    const int x = 16511;
    std::cout << 16511;
    for (int i = 0; i < 16511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16512(int& val) {
    func_in_16512(val);
    const int x = 16512;
    std::cout << 16512;
    for (int i = 0; i < 16512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16512(int& val) {
    const int x = 16512;
    std::cout << 16512;
    for (int i = 0; i < 16512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16513(int& val) {
    func_in_16513(val);
    const int x = 16513;
    std::cout << 16513;
    for (int i = 0; i < 16513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16513(int& val) {
    const int x = 16513;
    std::cout << 16513;
    for (int i = 0; i < 16513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16514(int& val) {
    func_in_16514(val);
    const int x = 16514;
    std::cout << 16514;
    for (int i = 0; i < 16514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16514(int& val) {
    const int x = 16514;
    std::cout << 16514;
    for (int i = 0; i < 16514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16515(int& val) {
    func_in_16515(val);
    const int x = 16515;
    std::cout << 16515;
    for (int i = 0; i < 16515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16515(int& val) {
    const int x = 16515;
    std::cout << 16515;
    for (int i = 0; i < 16515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16516(int& val) {
    func_in_16516(val);
    const int x = 16516;
    std::cout << 16516;
    for (int i = 0; i < 16516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16516(int& val) {
    const int x = 16516;
    std::cout << 16516;
    for (int i = 0; i < 16516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16517(int& val) {
    func_in_16517(val);
    const int x = 16517;
    std::cout << 16517;
    for (int i = 0; i < 16517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16517(int& val) {
    const int x = 16517;
    std::cout << 16517;
    for (int i = 0; i < 16517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16518(int& val) {
    func_in_16518(val);
    const int x = 16518;
    std::cout << 16518;
    for (int i = 0; i < 16518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16518(int& val) {
    const int x = 16518;
    std::cout << 16518;
    for (int i = 0; i < 16518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16519(int& val) {
    func_in_16519(val);
    const int x = 16519;
    std::cout << 16519;
    for (int i = 0; i < 16519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16519(int& val) {
    const int x = 16519;
    std::cout << 16519;
    for (int i = 0; i < 16519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16520(int& val) {
    func_in_16520(val);
    const int x = 16520;
    std::cout << 16520;
    for (int i = 0; i < 16520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16520(int& val) {
    const int x = 16520;
    std::cout << 16520;
    for (int i = 0; i < 16520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16521(int& val) {
    func_in_16521(val);
    const int x = 16521;
    std::cout << 16521;
    for (int i = 0; i < 16521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16521(int& val) {
    const int x = 16521;
    std::cout << 16521;
    for (int i = 0; i < 16521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16522(int& val) {
    func_in_16522(val);
    const int x = 16522;
    std::cout << 16522;
    for (int i = 0; i < 16522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16522(int& val) {
    const int x = 16522;
    std::cout << 16522;
    for (int i = 0; i < 16522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16523(int& val) {
    func_in_16523(val);
    const int x = 16523;
    std::cout << 16523;
    for (int i = 0; i < 16523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16523(int& val) {
    const int x = 16523;
    std::cout << 16523;
    for (int i = 0; i < 16523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16524(int& val) {
    func_in_16524(val);
    const int x = 16524;
    std::cout << 16524;
    for (int i = 0; i < 16524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16524(int& val) {
    const int x = 16524;
    std::cout << 16524;
    for (int i = 0; i < 16524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16525(int& val) {
    func_in_16525(val);
    const int x = 16525;
    std::cout << 16525;
    for (int i = 0; i < 16525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16525(int& val) {
    const int x = 16525;
    std::cout << 16525;
    for (int i = 0; i < 16525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16526(int& val) {
    func_in_16526(val);
    const int x = 16526;
    std::cout << 16526;
    for (int i = 0; i < 16526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16526(int& val) {
    const int x = 16526;
    std::cout << 16526;
    for (int i = 0; i < 16526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16527(int& val) {
    func_in_16527(val);
    const int x = 16527;
    std::cout << 16527;
    for (int i = 0; i < 16527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16527(int& val) {
    const int x = 16527;
    std::cout << 16527;
    for (int i = 0; i < 16527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16528(int& val) {
    func_in_16528(val);
    const int x = 16528;
    std::cout << 16528;
    for (int i = 0; i < 16528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16528(int& val) {
    const int x = 16528;
    std::cout << 16528;
    for (int i = 0; i < 16528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16529(int& val) {
    func_in_16529(val);
    const int x = 16529;
    std::cout << 16529;
    for (int i = 0; i < 16529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16529(int& val) {
    const int x = 16529;
    std::cout << 16529;
    for (int i = 0; i < 16529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16530(int& val) {
    func_in_16530(val);
    const int x = 16530;
    std::cout << 16530;
    for (int i = 0; i < 16530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16530(int& val) {
    const int x = 16530;
    std::cout << 16530;
    for (int i = 0; i < 16530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16531(int& val) {
    func_in_16531(val);
    const int x = 16531;
    std::cout << 16531;
    for (int i = 0; i < 16531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16531(int& val) {
    const int x = 16531;
    std::cout << 16531;
    for (int i = 0; i < 16531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16532(int& val) {
    func_in_16532(val);
    const int x = 16532;
    std::cout << 16532;
    for (int i = 0; i < 16532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16532(int& val) {
    const int x = 16532;
    std::cout << 16532;
    for (int i = 0; i < 16532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16533(int& val) {
    func_in_16533(val);
    const int x = 16533;
    std::cout << 16533;
    for (int i = 0; i < 16533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16533(int& val) {
    const int x = 16533;
    std::cout << 16533;
    for (int i = 0; i < 16533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16534(int& val) {
    func_in_16534(val);
    const int x = 16534;
    std::cout << 16534;
    for (int i = 0; i < 16534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16534(int& val) {
    const int x = 16534;
    std::cout << 16534;
    for (int i = 0; i < 16534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16535(int& val) {
    func_in_16535(val);
    const int x = 16535;
    std::cout << 16535;
    for (int i = 0; i < 16535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16535(int& val) {
    const int x = 16535;
    std::cout << 16535;
    for (int i = 0; i < 16535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16536(int& val) {
    func_in_16536(val);
    const int x = 16536;
    std::cout << 16536;
    for (int i = 0; i < 16536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16536(int& val) {
    const int x = 16536;
    std::cout << 16536;
    for (int i = 0; i < 16536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16537(int& val) {
    func_in_16537(val);
    const int x = 16537;
    std::cout << 16537;
    for (int i = 0; i < 16537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16537(int& val) {
    const int x = 16537;
    std::cout << 16537;
    for (int i = 0; i < 16537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16538(int& val) {
    func_in_16538(val);
    const int x = 16538;
    std::cout << 16538;
    for (int i = 0; i < 16538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16538(int& val) {
    const int x = 16538;
    std::cout << 16538;
    for (int i = 0; i < 16538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16539(int& val) {
    func_in_16539(val);
    const int x = 16539;
    std::cout << 16539;
    for (int i = 0; i < 16539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16539(int& val) {
    const int x = 16539;
    std::cout << 16539;
    for (int i = 0; i < 16539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16540(int& val) {
    func_in_16540(val);
    const int x = 16540;
    std::cout << 16540;
    for (int i = 0; i < 16540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16540(int& val) {
    const int x = 16540;
    std::cout << 16540;
    for (int i = 0; i < 16540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16541(int& val) {
    func_in_16541(val);
    const int x = 16541;
    std::cout << 16541;
    for (int i = 0; i < 16541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16541(int& val) {
    const int x = 16541;
    std::cout << 16541;
    for (int i = 0; i < 16541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16542(int& val) {
    func_in_16542(val);
    const int x = 16542;
    std::cout << 16542;
    for (int i = 0; i < 16542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16542(int& val) {
    const int x = 16542;
    std::cout << 16542;
    for (int i = 0; i < 16542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16543(int& val) {
    func_in_16543(val);
    const int x = 16543;
    std::cout << 16543;
    for (int i = 0; i < 16543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16543(int& val) {
    const int x = 16543;
    std::cout << 16543;
    for (int i = 0; i < 16543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16544(int& val) {
    func_in_16544(val);
    const int x = 16544;
    std::cout << 16544;
    for (int i = 0; i < 16544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16544(int& val) {
    const int x = 16544;
    std::cout << 16544;
    for (int i = 0; i < 16544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16545(int& val) {
    func_in_16545(val);
    const int x = 16545;
    std::cout << 16545;
    for (int i = 0; i < 16545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16545(int& val) {
    const int x = 16545;
    std::cout << 16545;
    for (int i = 0; i < 16545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16546(int& val) {
    func_in_16546(val);
    const int x = 16546;
    std::cout << 16546;
    for (int i = 0; i < 16546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16546(int& val) {
    const int x = 16546;
    std::cout << 16546;
    for (int i = 0; i < 16546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16547(int& val) {
    func_in_16547(val);
    const int x = 16547;
    std::cout << 16547;
    for (int i = 0; i < 16547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16547(int& val) {
    const int x = 16547;
    std::cout << 16547;
    for (int i = 0; i < 16547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16548(int& val) {
    func_in_16548(val);
    const int x = 16548;
    std::cout << 16548;
    for (int i = 0; i < 16548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16548(int& val) {
    const int x = 16548;
    std::cout << 16548;
    for (int i = 0; i < 16548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16549(int& val) {
    func_in_16549(val);
    const int x = 16549;
    std::cout << 16549;
    for (int i = 0; i < 16549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16549(int& val) {
    const int x = 16549;
    std::cout << 16549;
    for (int i = 0; i < 16549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16550(int& val) {
    func_in_16550(val);
    const int x = 16550;
    std::cout << 16550;
    for (int i = 0; i < 16550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16550(int& val) {
    const int x = 16550;
    std::cout << 16550;
    for (int i = 0; i < 16550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16551(int& val) {
    func_in_16551(val);
    const int x = 16551;
    std::cout << 16551;
    for (int i = 0; i < 16551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16551(int& val) {
    const int x = 16551;
    std::cout << 16551;
    for (int i = 0; i < 16551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16552(int& val) {
    func_in_16552(val);
    const int x = 16552;
    std::cout << 16552;
    for (int i = 0; i < 16552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16552(int& val) {
    const int x = 16552;
    std::cout << 16552;
    for (int i = 0; i < 16552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16553(int& val) {
    func_in_16553(val);
    const int x = 16553;
    std::cout << 16553;
    for (int i = 0; i < 16553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16553(int& val) {
    const int x = 16553;
    std::cout << 16553;
    for (int i = 0; i < 16553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16554(int& val) {
    func_in_16554(val);
    const int x = 16554;
    std::cout << 16554;
    for (int i = 0; i < 16554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16554(int& val) {
    const int x = 16554;
    std::cout << 16554;
    for (int i = 0; i < 16554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16555(int& val) {
    func_in_16555(val);
    const int x = 16555;
    std::cout << 16555;
    for (int i = 0; i < 16555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16555(int& val) {
    const int x = 16555;
    std::cout << 16555;
    for (int i = 0; i < 16555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16556(int& val) {
    func_in_16556(val);
    const int x = 16556;
    std::cout << 16556;
    for (int i = 0; i < 16556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16556(int& val) {
    const int x = 16556;
    std::cout << 16556;
    for (int i = 0; i < 16556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16557(int& val) {
    func_in_16557(val);
    const int x = 16557;
    std::cout << 16557;
    for (int i = 0; i < 16557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16557(int& val) {
    const int x = 16557;
    std::cout << 16557;
    for (int i = 0; i < 16557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16558(int& val) {
    func_in_16558(val);
    const int x = 16558;
    std::cout << 16558;
    for (int i = 0; i < 16558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16558(int& val) {
    const int x = 16558;
    std::cout << 16558;
    for (int i = 0; i < 16558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16559(int& val) {
    func_in_16559(val);
    const int x = 16559;
    std::cout << 16559;
    for (int i = 0; i < 16559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16559(int& val) {
    const int x = 16559;
    std::cout << 16559;
    for (int i = 0; i < 16559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16560(int& val) {
    func_in_16560(val);
    const int x = 16560;
    std::cout << 16560;
    for (int i = 0; i < 16560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16560(int& val) {
    const int x = 16560;
    std::cout << 16560;
    for (int i = 0; i < 16560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16561(int& val) {
    func_in_16561(val);
    const int x = 16561;
    std::cout << 16561;
    for (int i = 0; i < 16561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16561(int& val) {
    const int x = 16561;
    std::cout << 16561;
    for (int i = 0; i < 16561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16562(int& val) {
    func_in_16562(val);
    const int x = 16562;
    std::cout << 16562;
    for (int i = 0; i < 16562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16562(int& val) {
    const int x = 16562;
    std::cout << 16562;
    for (int i = 0; i < 16562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16563(int& val) {
    func_in_16563(val);
    const int x = 16563;
    std::cout << 16563;
    for (int i = 0; i < 16563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16563(int& val) {
    const int x = 16563;
    std::cout << 16563;
    for (int i = 0; i < 16563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16564(int& val) {
    func_in_16564(val);
    const int x = 16564;
    std::cout << 16564;
    for (int i = 0; i < 16564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16564(int& val) {
    const int x = 16564;
    std::cout << 16564;
    for (int i = 0; i < 16564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16565(int& val) {
    func_in_16565(val);
    const int x = 16565;
    std::cout << 16565;
    for (int i = 0; i < 16565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16565(int& val) {
    const int x = 16565;
    std::cout << 16565;
    for (int i = 0; i < 16565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16566(int& val) {
    func_in_16566(val);
    const int x = 16566;
    std::cout << 16566;
    for (int i = 0; i < 16566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16566(int& val) {
    const int x = 16566;
    std::cout << 16566;
    for (int i = 0; i < 16566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16567(int& val) {
    func_in_16567(val);
    const int x = 16567;
    std::cout << 16567;
    for (int i = 0; i < 16567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16567(int& val) {
    const int x = 16567;
    std::cout << 16567;
    for (int i = 0; i < 16567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16568(int& val) {
    func_in_16568(val);
    const int x = 16568;
    std::cout << 16568;
    for (int i = 0; i < 16568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16568(int& val) {
    const int x = 16568;
    std::cout << 16568;
    for (int i = 0; i < 16568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16569(int& val) {
    func_in_16569(val);
    const int x = 16569;
    std::cout << 16569;
    for (int i = 0; i < 16569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16569(int& val) {
    const int x = 16569;
    std::cout << 16569;
    for (int i = 0; i < 16569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16570(int& val) {
    func_in_16570(val);
    const int x = 16570;
    std::cout << 16570;
    for (int i = 0; i < 16570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16570(int& val) {
    const int x = 16570;
    std::cout << 16570;
    for (int i = 0; i < 16570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16571(int& val) {
    func_in_16571(val);
    const int x = 16571;
    std::cout << 16571;
    for (int i = 0; i < 16571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16571(int& val) {
    const int x = 16571;
    std::cout << 16571;
    for (int i = 0; i < 16571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16572(int& val) {
    func_in_16572(val);
    const int x = 16572;
    std::cout << 16572;
    for (int i = 0; i < 16572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16572(int& val) {
    const int x = 16572;
    std::cout << 16572;
    for (int i = 0; i < 16572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16573(int& val) {
    func_in_16573(val);
    const int x = 16573;
    std::cout << 16573;
    for (int i = 0; i < 16573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16573(int& val) {
    const int x = 16573;
    std::cout << 16573;
    for (int i = 0; i < 16573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16574(int& val) {
    func_in_16574(val);
    const int x = 16574;
    std::cout << 16574;
    for (int i = 0; i < 16574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16574(int& val) {
    const int x = 16574;
    std::cout << 16574;
    for (int i = 0; i < 16574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16575(int& val) {
    func_in_16575(val);
    const int x = 16575;
    std::cout << 16575;
    for (int i = 0; i < 16575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16575(int& val) {
    const int x = 16575;
    std::cout << 16575;
    for (int i = 0; i < 16575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16576(int& val) {
    func_in_16576(val);
    const int x = 16576;
    std::cout << 16576;
    for (int i = 0; i < 16576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16576(int& val) {
    const int x = 16576;
    std::cout << 16576;
    for (int i = 0; i < 16576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16577(int& val) {
    func_in_16577(val);
    const int x = 16577;
    std::cout << 16577;
    for (int i = 0; i < 16577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16577(int& val) {
    const int x = 16577;
    std::cout << 16577;
    for (int i = 0; i < 16577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16578(int& val) {
    func_in_16578(val);
    const int x = 16578;
    std::cout << 16578;
    for (int i = 0; i < 16578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16578(int& val) {
    const int x = 16578;
    std::cout << 16578;
    for (int i = 0; i < 16578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16579(int& val) {
    func_in_16579(val);
    const int x = 16579;
    std::cout << 16579;
    for (int i = 0; i < 16579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16579(int& val) {
    const int x = 16579;
    std::cout << 16579;
    for (int i = 0; i < 16579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16580(int& val) {
    func_in_16580(val);
    const int x = 16580;
    std::cout << 16580;
    for (int i = 0; i < 16580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16580(int& val) {
    const int x = 16580;
    std::cout << 16580;
    for (int i = 0; i < 16580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16581(int& val) {
    func_in_16581(val);
    const int x = 16581;
    std::cout << 16581;
    for (int i = 0; i < 16581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16581(int& val) {
    const int x = 16581;
    std::cout << 16581;
    for (int i = 0; i < 16581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16582(int& val) {
    func_in_16582(val);
    const int x = 16582;
    std::cout << 16582;
    for (int i = 0; i < 16582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16582(int& val) {
    const int x = 16582;
    std::cout << 16582;
    for (int i = 0; i < 16582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16583(int& val) {
    func_in_16583(val);
    const int x = 16583;
    std::cout << 16583;
    for (int i = 0; i < 16583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16583(int& val) {
    const int x = 16583;
    std::cout << 16583;
    for (int i = 0; i < 16583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16584(int& val) {
    func_in_16584(val);
    const int x = 16584;
    std::cout << 16584;
    for (int i = 0; i < 16584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16584(int& val) {
    const int x = 16584;
    std::cout << 16584;
    for (int i = 0; i < 16584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16585(int& val) {
    func_in_16585(val);
    const int x = 16585;
    std::cout << 16585;
    for (int i = 0; i < 16585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16585(int& val) {
    const int x = 16585;
    std::cout << 16585;
    for (int i = 0; i < 16585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16586(int& val) {
    func_in_16586(val);
    const int x = 16586;
    std::cout << 16586;
    for (int i = 0; i < 16586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16586(int& val) {
    const int x = 16586;
    std::cout << 16586;
    for (int i = 0; i < 16586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16587(int& val) {
    func_in_16587(val);
    const int x = 16587;
    std::cout << 16587;
    for (int i = 0; i < 16587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16587(int& val) {
    const int x = 16587;
    std::cout << 16587;
    for (int i = 0; i < 16587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16588(int& val) {
    func_in_16588(val);
    const int x = 16588;
    std::cout << 16588;
    for (int i = 0; i < 16588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16588(int& val) {
    const int x = 16588;
    std::cout << 16588;
    for (int i = 0; i < 16588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16589(int& val) {
    func_in_16589(val);
    const int x = 16589;
    std::cout << 16589;
    for (int i = 0; i < 16589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16589(int& val) {
    const int x = 16589;
    std::cout << 16589;
    for (int i = 0; i < 16589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16590(int& val) {
    func_in_16590(val);
    const int x = 16590;
    std::cout << 16590;
    for (int i = 0; i < 16590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16590(int& val) {
    const int x = 16590;
    std::cout << 16590;
    for (int i = 0; i < 16590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16591(int& val) {
    func_in_16591(val);
    const int x = 16591;
    std::cout << 16591;
    for (int i = 0; i < 16591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16591(int& val) {
    const int x = 16591;
    std::cout << 16591;
    for (int i = 0; i < 16591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16592(int& val) {
    func_in_16592(val);
    const int x = 16592;
    std::cout << 16592;
    for (int i = 0; i < 16592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16592(int& val) {
    const int x = 16592;
    std::cout << 16592;
    for (int i = 0; i < 16592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16593(int& val) {
    func_in_16593(val);
    const int x = 16593;
    std::cout << 16593;
    for (int i = 0; i < 16593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16593(int& val) {
    const int x = 16593;
    std::cout << 16593;
    for (int i = 0; i < 16593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16594(int& val) {
    func_in_16594(val);
    const int x = 16594;
    std::cout << 16594;
    for (int i = 0; i < 16594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16594(int& val) {
    const int x = 16594;
    std::cout << 16594;
    for (int i = 0; i < 16594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16595(int& val) {
    func_in_16595(val);
    const int x = 16595;
    std::cout << 16595;
    for (int i = 0; i < 16595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16595(int& val) {
    const int x = 16595;
    std::cout << 16595;
    for (int i = 0; i < 16595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16596(int& val) {
    func_in_16596(val);
    const int x = 16596;
    std::cout << 16596;
    for (int i = 0; i < 16596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16596(int& val) {
    const int x = 16596;
    std::cout << 16596;
    for (int i = 0; i < 16596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16597(int& val) {
    func_in_16597(val);
    const int x = 16597;
    std::cout << 16597;
    for (int i = 0; i < 16597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16597(int& val) {
    const int x = 16597;
    std::cout << 16597;
    for (int i = 0; i < 16597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16598(int& val) {
    func_in_16598(val);
    const int x = 16598;
    std::cout << 16598;
    for (int i = 0; i < 16598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16598(int& val) {
    const int x = 16598;
    std::cout << 16598;
    for (int i = 0; i < 16598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16599(int& val) {
    func_in_16599(val);
    const int x = 16599;
    std::cout << 16599;
    for (int i = 0; i < 16599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16599(int& val) {
    const int x = 16599;
    std::cout << 16599;
    for (int i = 0; i < 16599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16600(int& val) {
    func_in_16600(val);
    const int x = 16600;
    std::cout << 16600;
    for (int i = 0; i < 16600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16600(int& val) {
    const int x = 16600;
    std::cout << 16600;
    for (int i = 0; i < 16600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16601(int& val) {
    func_in_16601(val);
    const int x = 16601;
    std::cout << 16601;
    for (int i = 0; i < 16601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16601(int& val) {
    const int x = 16601;
    std::cout << 16601;
    for (int i = 0; i < 16601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16602(int& val) {
    func_in_16602(val);
    const int x = 16602;
    std::cout << 16602;
    for (int i = 0; i < 16602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16602(int& val) {
    const int x = 16602;
    std::cout << 16602;
    for (int i = 0; i < 16602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16603(int& val) {
    func_in_16603(val);
    const int x = 16603;
    std::cout << 16603;
    for (int i = 0; i < 16603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16603(int& val) {
    const int x = 16603;
    std::cout << 16603;
    for (int i = 0; i < 16603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16604(int& val) {
    func_in_16604(val);
    const int x = 16604;
    std::cout << 16604;
    for (int i = 0; i < 16604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16604(int& val) {
    const int x = 16604;
    std::cout << 16604;
    for (int i = 0; i < 16604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16605(int& val) {
    func_in_16605(val);
    const int x = 16605;
    std::cout << 16605;
    for (int i = 0; i < 16605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16605(int& val) {
    const int x = 16605;
    std::cout << 16605;
    for (int i = 0; i < 16605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16606(int& val) {
    func_in_16606(val);
    const int x = 16606;
    std::cout << 16606;
    for (int i = 0; i < 16606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16606(int& val) {
    const int x = 16606;
    std::cout << 16606;
    for (int i = 0; i < 16606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16607(int& val) {
    func_in_16607(val);
    const int x = 16607;
    std::cout << 16607;
    for (int i = 0; i < 16607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16607(int& val) {
    const int x = 16607;
    std::cout << 16607;
    for (int i = 0; i < 16607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16608(int& val) {
    func_in_16608(val);
    const int x = 16608;
    std::cout << 16608;
    for (int i = 0; i < 16608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16608(int& val) {
    const int x = 16608;
    std::cout << 16608;
    for (int i = 0; i < 16608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16609(int& val) {
    func_in_16609(val);
    const int x = 16609;
    std::cout << 16609;
    for (int i = 0; i < 16609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16609(int& val) {
    const int x = 16609;
    std::cout << 16609;
    for (int i = 0; i < 16609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16610(int& val) {
    func_in_16610(val);
    const int x = 16610;
    std::cout << 16610;
    for (int i = 0; i < 16610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16610(int& val) {
    const int x = 16610;
    std::cout << 16610;
    for (int i = 0; i < 16610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16611(int& val) {
    func_in_16611(val);
    const int x = 16611;
    std::cout << 16611;
    for (int i = 0; i < 16611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16611(int& val) {
    const int x = 16611;
    std::cout << 16611;
    for (int i = 0; i < 16611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16612(int& val) {
    func_in_16612(val);
    const int x = 16612;
    std::cout << 16612;
    for (int i = 0; i < 16612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16612(int& val) {
    const int x = 16612;
    std::cout << 16612;
    for (int i = 0; i < 16612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16613(int& val) {
    func_in_16613(val);
    const int x = 16613;
    std::cout << 16613;
    for (int i = 0; i < 16613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16613(int& val) {
    const int x = 16613;
    std::cout << 16613;
    for (int i = 0; i < 16613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16614(int& val) {
    func_in_16614(val);
    const int x = 16614;
    std::cout << 16614;
    for (int i = 0; i < 16614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16614(int& val) {
    const int x = 16614;
    std::cout << 16614;
    for (int i = 0; i < 16614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16615(int& val) {
    func_in_16615(val);
    const int x = 16615;
    std::cout << 16615;
    for (int i = 0; i < 16615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16615(int& val) {
    const int x = 16615;
    std::cout << 16615;
    for (int i = 0; i < 16615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16616(int& val) {
    func_in_16616(val);
    const int x = 16616;
    std::cout << 16616;
    for (int i = 0; i < 16616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16616(int& val) {
    const int x = 16616;
    std::cout << 16616;
    for (int i = 0; i < 16616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16617(int& val) {
    func_in_16617(val);
    const int x = 16617;
    std::cout << 16617;
    for (int i = 0; i < 16617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16617(int& val) {
    const int x = 16617;
    std::cout << 16617;
    for (int i = 0; i < 16617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16618(int& val) {
    func_in_16618(val);
    const int x = 16618;
    std::cout << 16618;
    for (int i = 0; i < 16618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16618(int& val) {
    const int x = 16618;
    std::cout << 16618;
    for (int i = 0; i < 16618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16619(int& val) {
    func_in_16619(val);
    const int x = 16619;
    std::cout << 16619;
    for (int i = 0; i < 16619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16619(int& val) {
    const int x = 16619;
    std::cout << 16619;
    for (int i = 0; i < 16619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16620(int& val) {
    func_in_16620(val);
    const int x = 16620;
    std::cout << 16620;
    for (int i = 0; i < 16620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16620(int& val) {
    const int x = 16620;
    std::cout << 16620;
    for (int i = 0; i < 16620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16621(int& val) {
    func_in_16621(val);
    const int x = 16621;
    std::cout << 16621;
    for (int i = 0; i < 16621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16621(int& val) {
    const int x = 16621;
    std::cout << 16621;
    for (int i = 0; i < 16621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16622(int& val) {
    func_in_16622(val);
    const int x = 16622;
    std::cout << 16622;
    for (int i = 0; i < 16622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16622(int& val) {
    const int x = 16622;
    std::cout << 16622;
    for (int i = 0; i < 16622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16623(int& val) {
    func_in_16623(val);
    const int x = 16623;
    std::cout << 16623;
    for (int i = 0; i < 16623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16623(int& val) {
    const int x = 16623;
    std::cout << 16623;
    for (int i = 0; i < 16623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16624(int& val) {
    func_in_16624(val);
    const int x = 16624;
    std::cout << 16624;
    for (int i = 0; i < 16624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16624(int& val) {
    const int x = 16624;
    std::cout << 16624;
    for (int i = 0; i < 16624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16625(int& val) {
    func_in_16625(val);
    const int x = 16625;
    std::cout << 16625;
    for (int i = 0; i < 16625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16625(int& val) {
    const int x = 16625;
    std::cout << 16625;
    for (int i = 0; i < 16625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16626(int& val) {
    func_in_16626(val);
    const int x = 16626;
    std::cout << 16626;
    for (int i = 0; i < 16626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16626(int& val) {
    const int x = 16626;
    std::cout << 16626;
    for (int i = 0; i < 16626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16627(int& val) {
    func_in_16627(val);
    const int x = 16627;
    std::cout << 16627;
    for (int i = 0; i < 16627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16627(int& val) {
    const int x = 16627;
    std::cout << 16627;
    for (int i = 0; i < 16627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16628(int& val) {
    func_in_16628(val);
    const int x = 16628;
    std::cout << 16628;
    for (int i = 0; i < 16628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16628(int& val) {
    const int x = 16628;
    std::cout << 16628;
    for (int i = 0; i < 16628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16629(int& val) {
    func_in_16629(val);
    const int x = 16629;
    std::cout << 16629;
    for (int i = 0; i < 16629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16629(int& val) {
    const int x = 16629;
    std::cout << 16629;
    for (int i = 0; i < 16629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16630(int& val) {
    func_in_16630(val);
    const int x = 16630;
    std::cout << 16630;
    for (int i = 0; i < 16630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16630(int& val) {
    const int x = 16630;
    std::cout << 16630;
    for (int i = 0; i < 16630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16631(int& val) {
    func_in_16631(val);
    const int x = 16631;
    std::cout << 16631;
    for (int i = 0; i < 16631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16631(int& val) {
    const int x = 16631;
    std::cout << 16631;
    for (int i = 0; i < 16631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16632(int& val) {
    func_in_16632(val);
    const int x = 16632;
    std::cout << 16632;
    for (int i = 0; i < 16632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16632(int& val) {
    const int x = 16632;
    std::cout << 16632;
    for (int i = 0; i < 16632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16633(int& val) {
    func_in_16633(val);
    const int x = 16633;
    std::cout << 16633;
    for (int i = 0; i < 16633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16633(int& val) {
    const int x = 16633;
    std::cout << 16633;
    for (int i = 0; i < 16633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16634(int& val) {
    func_in_16634(val);
    const int x = 16634;
    std::cout << 16634;
    for (int i = 0; i < 16634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16634(int& val) {
    const int x = 16634;
    std::cout << 16634;
    for (int i = 0; i < 16634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16635(int& val) {
    func_in_16635(val);
    const int x = 16635;
    std::cout << 16635;
    for (int i = 0; i < 16635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16635(int& val) {
    const int x = 16635;
    std::cout << 16635;
    for (int i = 0; i < 16635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16636(int& val) {
    func_in_16636(val);
    const int x = 16636;
    std::cout << 16636;
    for (int i = 0; i < 16636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16636(int& val) {
    const int x = 16636;
    std::cout << 16636;
    for (int i = 0; i < 16636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16637(int& val) {
    func_in_16637(val);
    const int x = 16637;
    std::cout << 16637;
    for (int i = 0; i < 16637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16637(int& val) {
    const int x = 16637;
    std::cout << 16637;
    for (int i = 0; i < 16637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16638(int& val) {
    func_in_16638(val);
    const int x = 16638;
    std::cout << 16638;
    for (int i = 0; i < 16638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16638(int& val) {
    const int x = 16638;
    std::cout << 16638;
    for (int i = 0; i < 16638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16639(int& val) {
    func_in_16639(val);
    const int x = 16639;
    std::cout << 16639;
    for (int i = 0; i < 16639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16639(int& val) {
    const int x = 16639;
    std::cout << 16639;
    for (int i = 0; i < 16639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16640(int& val) {
    func_in_16640(val);
    const int x = 16640;
    std::cout << 16640;
    for (int i = 0; i < 16640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16640(int& val) {
    const int x = 16640;
    std::cout << 16640;
    for (int i = 0; i < 16640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16641(int& val) {
    func_in_16641(val);
    const int x = 16641;
    std::cout << 16641;
    for (int i = 0; i < 16641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16641(int& val) {
    const int x = 16641;
    std::cout << 16641;
    for (int i = 0; i < 16641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16642(int& val) {
    func_in_16642(val);
    const int x = 16642;
    std::cout << 16642;
    for (int i = 0; i < 16642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16642(int& val) {
    const int x = 16642;
    std::cout << 16642;
    for (int i = 0; i < 16642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16643(int& val) {
    func_in_16643(val);
    const int x = 16643;
    std::cout << 16643;
    for (int i = 0; i < 16643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16643(int& val) {
    const int x = 16643;
    std::cout << 16643;
    for (int i = 0; i < 16643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16644(int& val) {
    func_in_16644(val);
    const int x = 16644;
    std::cout << 16644;
    for (int i = 0; i < 16644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16644(int& val) {
    const int x = 16644;
    std::cout << 16644;
    for (int i = 0; i < 16644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16645(int& val) {
    func_in_16645(val);
    const int x = 16645;
    std::cout << 16645;
    for (int i = 0; i < 16645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16645(int& val) {
    const int x = 16645;
    std::cout << 16645;
    for (int i = 0; i < 16645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16646(int& val) {
    func_in_16646(val);
    const int x = 16646;
    std::cout << 16646;
    for (int i = 0; i < 16646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16646(int& val) {
    const int x = 16646;
    std::cout << 16646;
    for (int i = 0; i < 16646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16647(int& val) {
    func_in_16647(val);
    const int x = 16647;
    std::cout << 16647;
    for (int i = 0; i < 16647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16647(int& val) {
    const int x = 16647;
    std::cout << 16647;
    for (int i = 0; i < 16647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16648(int& val) {
    func_in_16648(val);
    const int x = 16648;
    std::cout << 16648;
    for (int i = 0; i < 16648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16648(int& val) {
    const int x = 16648;
    std::cout << 16648;
    for (int i = 0; i < 16648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16649(int& val) {
    func_in_16649(val);
    const int x = 16649;
    std::cout << 16649;
    for (int i = 0; i < 16649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16649(int& val) {
    const int x = 16649;
    std::cout << 16649;
    for (int i = 0; i < 16649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16650(int& val) {
    func_in_16650(val);
    const int x = 16650;
    std::cout << 16650;
    for (int i = 0; i < 16650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16650(int& val) {
    const int x = 16650;
    std::cout << 16650;
    for (int i = 0; i < 16650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16651(int& val) {
    func_in_16651(val);
    const int x = 16651;
    std::cout << 16651;
    for (int i = 0; i < 16651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16651(int& val) {
    const int x = 16651;
    std::cout << 16651;
    for (int i = 0; i < 16651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16652(int& val) {
    func_in_16652(val);
    const int x = 16652;
    std::cout << 16652;
    for (int i = 0; i < 16652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16652(int& val) {
    const int x = 16652;
    std::cout << 16652;
    for (int i = 0; i < 16652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16653(int& val) {
    func_in_16653(val);
    const int x = 16653;
    std::cout << 16653;
    for (int i = 0; i < 16653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16653(int& val) {
    const int x = 16653;
    std::cout << 16653;
    for (int i = 0; i < 16653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16654(int& val) {
    func_in_16654(val);
    const int x = 16654;
    std::cout << 16654;
    for (int i = 0; i < 16654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16654(int& val) {
    const int x = 16654;
    std::cout << 16654;
    for (int i = 0; i < 16654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16655(int& val) {
    func_in_16655(val);
    const int x = 16655;
    std::cout << 16655;
    for (int i = 0; i < 16655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16655(int& val) {
    const int x = 16655;
    std::cout << 16655;
    for (int i = 0; i < 16655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16656(int& val) {
    func_in_16656(val);
    const int x = 16656;
    std::cout << 16656;
    for (int i = 0; i < 16656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16656(int& val) {
    const int x = 16656;
    std::cout << 16656;
    for (int i = 0; i < 16656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16657(int& val) {
    func_in_16657(val);
    const int x = 16657;
    std::cout << 16657;
    for (int i = 0; i < 16657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16657(int& val) {
    const int x = 16657;
    std::cout << 16657;
    for (int i = 0; i < 16657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16658(int& val) {
    func_in_16658(val);
    const int x = 16658;
    std::cout << 16658;
    for (int i = 0; i < 16658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16658(int& val) {
    const int x = 16658;
    std::cout << 16658;
    for (int i = 0; i < 16658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16659(int& val) {
    func_in_16659(val);
    const int x = 16659;
    std::cout << 16659;
    for (int i = 0; i < 16659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16659(int& val) {
    const int x = 16659;
    std::cout << 16659;
    for (int i = 0; i < 16659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16660(int& val) {
    func_in_16660(val);
    const int x = 16660;
    std::cout << 16660;
    for (int i = 0; i < 16660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16660(int& val) {
    const int x = 16660;
    std::cout << 16660;
    for (int i = 0; i < 16660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16661(int& val) {
    func_in_16661(val);
    const int x = 16661;
    std::cout << 16661;
    for (int i = 0; i < 16661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16661(int& val) {
    const int x = 16661;
    std::cout << 16661;
    for (int i = 0; i < 16661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16662(int& val) {
    func_in_16662(val);
    const int x = 16662;
    std::cout << 16662;
    for (int i = 0; i < 16662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16662(int& val) {
    const int x = 16662;
    std::cout << 16662;
    for (int i = 0; i < 16662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16663(int& val) {
    func_in_16663(val);
    const int x = 16663;
    std::cout << 16663;
    for (int i = 0; i < 16663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16663(int& val) {
    const int x = 16663;
    std::cout << 16663;
    for (int i = 0; i < 16663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16664(int& val) {
    func_in_16664(val);
    const int x = 16664;
    std::cout << 16664;
    for (int i = 0; i < 16664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16664(int& val) {
    const int x = 16664;
    std::cout << 16664;
    for (int i = 0; i < 16664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16665(int& val) {
    func_in_16665(val);
    const int x = 16665;
    std::cout << 16665;
    for (int i = 0; i < 16665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16665(int& val) {
    const int x = 16665;
    std::cout << 16665;
    for (int i = 0; i < 16665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16666(int& val) {
    func_in_16666(val);
    const int x = 16666;
    std::cout << 16666;
    for (int i = 0; i < 16666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16666(int& val) {
    const int x = 16666;
    std::cout << 16666;
    for (int i = 0; i < 16666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16667(int& val) {
    func_in_16667(val);
    const int x = 16667;
    std::cout << 16667;
    for (int i = 0; i < 16667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16667(int& val) {
    const int x = 16667;
    std::cout << 16667;
    for (int i = 0; i < 16667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16668(int& val) {
    func_in_16668(val);
    const int x = 16668;
    std::cout << 16668;
    for (int i = 0; i < 16668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16668(int& val) {
    const int x = 16668;
    std::cout << 16668;
    for (int i = 0; i < 16668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16669(int& val) {
    func_in_16669(val);
    const int x = 16669;
    std::cout << 16669;
    for (int i = 0; i < 16669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16669(int& val) {
    const int x = 16669;
    std::cout << 16669;
    for (int i = 0; i < 16669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16670(int& val) {
    func_in_16670(val);
    const int x = 16670;
    std::cout << 16670;
    for (int i = 0; i < 16670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16670(int& val) {
    const int x = 16670;
    std::cout << 16670;
    for (int i = 0; i < 16670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16671(int& val) {
    func_in_16671(val);
    const int x = 16671;
    std::cout << 16671;
    for (int i = 0; i < 16671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16671(int& val) {
    const int x = 16671;
    std::cout << 16671;
    for (int i = 0; i < 16671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16672(int& val) {
    func_in_16672(val);
    const int x = 16672;
    std::cout << 16672;
    for (int i = 0; i < 16672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16672(int& val) {
    const int x = 16672;
    std::cout << 16672;
    for (int i = 0; i < 16672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16673(int& val) {
    func_in_16673(val);
    const int x = 16673;
    std::cout << 16673;
    for (int i = 0; i < 16673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16673(int& val) {
    const int x = 16673;
    std::cout << 16673;
    for (int i = 0; i < 16673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16674(int& val) {
    func_in_16674(val);
    const int x = 16674;
    std::cout << 16674;
    for (int i = 0; i < 16674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16674(int& val) {
    const int x = 16674;
    std::cout << 16674;
    for (int i = 0; i < 16674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16675(int& val) {
    func_in_16675(val);
    const int x = 16675;
    std::cout << 16675;
    for (int i = 0; i < 16675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16675(int& val) {
    const int x = 16675;
    std::cout << 16675;
    for (int i = 0; i < 16675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16676(int& val) {
    func_in_16676(val);
    const int x = 16676;
    std::cout << 16676;
    for (int i = 0; i < 16676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16676(int& val) {
    const int x = 16676;
    std::cout << 16676;
    for (int i = 0; i < 16676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16677(int& val) {
    func_in_16677(val);
    const int x = 16677;
    std::cout << 16677;
    for (int i = 0; i < 16677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16677(int& val) {
    const int x = 16677;
    std::cout << 16677;
    for (int i = 0; i < 16677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16678(int& val) {
    func_in_16678(val);
    const int x = 16678;
    std::cout << 16678;
    for (int i = 0; i < 16678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16678(int& val) {
    const int x = 16678;
    std::cout << 16678;
    for (int i = 0; i < 16678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16679(int& val) {
    func_in_16679(val);
    const int x = 16679;
    std::cout << 16679;
    for (int i = 0; i < 16679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16679(int& val) {
    const int x = 16679;
    std::cout << 16679;
    for (int i = 0; i < 16679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16680(int& val) {
    func_in_16680(val);
    const int x = 16680;
    std::cout << 16680;
    for (int i = 0; i < 16680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16680(int& val) {
    const int x = 16680;
    std::cout << 16680;
    for (int i = 0; i < 16680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16681(int& val) {
    func_in_16681(val);
    const int x = 16681;
    std::cout << 16681;
    for (int i = 0; i < 16681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16681(int& val) {
    const int x = 16681;
    std::cout << 16681;
    for (int i = 0; i < 16681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16682(int& val) {
    func_in_16682(val);
    const int x = 16682;
    std::cout << 16682;
    for (int i = 0; i < 16682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16682(int& val) {
    const int x = 16682;
    std::cout << 16682;
    for (int i = 0; i < 16682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16683(int& val) {
    func_in_16683(val);
    const int x = 16683;
    std::cout << 16683;
    for (int i = 0; i < 16683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16683(int& val) {
    const int x = 16683;
    std::cout << 16683;
    for (int i = 0; i < 16683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16684(int& val) {
    func_in_16684(val);
    const int x = 16684;
    std::cout << 16684;
    for (int i = 0; i < 16684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16684(int& val) {
    const int x = 16684;
    std::cout << 16684;
    for (int i = 0; i < 16684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16685(int& val) {
    func_in_16685(val);
    const int x = 16685;
    std::cout << 16685;
    for (int i = 0; i < 16685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16685(int& val) {
    const int x = 16685;
    std::cout << 16685;
    for (int i = 0; i < 16685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16686(int& val) {
    func_in_16686(val);
    const int x = 16686;
    std::cout << 16686;
    for (int i = 0; i < 16686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16686(int& val) {
    const int x = 16686;
    std::cout << 16686;
    for (int i = 0; i < 16686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16687(int& val) {
    func_in_16687(val);
    const int x = 16687;
    std::cout << 16687;
    for (int i = 0; i < 16687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16687(int& val) {
    const int x = 16687;
    std::cout << 16687;
    for (int i = 0; i < 16687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16688(int& val) {
    func_in_16688(val);
    const int x = 16688;
    std::cout << 16688;
    for (int i = 0; i < 16688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16688(int& val) {
    const int x = 16688;
    std::cout << 16688;
    for (int i = 0; i < 16688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16689(int& val) {
    func_in_16689(val);
    const int x = 16689;
    std::cout << 16689;
    for (int i = 0; i < 16689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16689(int& val) {
    const int x = 16689;
    std::cout << 16689;
    for (int i = 0; i < 16689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16690(int& val) {
    func_in_16690(val);
    const int x = 16690;
    std::cout << 16690;
    for (int i = 0; i < 16690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16690(int& val) {
    const int x = 16690;
    std::cout << 16690;
    for (int i = 0; i < 16690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16691(int& val) {
    func_in_16691(val);
    const int x = 16691;
    std::cout << 16691;
    for (int i = 0; i < 16691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16691(int& val) {
    const int x = 16691;
    std::cout << 16691;
    for (int i = 0; i < 16691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16692(int& val) {
    func_in_16692(val);
    const int x = 16692;
    std::cout << 16692;
    for (int i = 0; i < 16692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16692(int& val) {
    const int x = 16692;
    std::cout << 16692;
    for (int i = 0; i < 16692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16693(int& val) {
    func_in_16693(val);
    const int x = 16693;
    std::cout << 16693;
    for (int i = 0; i < 16693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16693(int& val) {
    const int x = 16693;
    std::cout << 16693;
    for (int i = 0; i < 16693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16694(int& val) {
    func_in_16694(val);
    const int x = 16694;
    std::cout << 16694;
    for (int i = 0; i < 16694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16694(int& val) {
    const int x = 16694;
    std::cout << 16694;
    for (int i = 0; i < 16694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16695(int& val) {
    func_in_16695(val);
    const int x = 16695;
    std::cout << 16695;
    for (int i = 0; i < 16695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16695(int& val) {
    const int x = 16695;
    std::cout << 16695;
    for (int i = 0; i < 16695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16696(int& val) {
    func_in_16696(val);
    const int x = 16696;
    std::cout << 16696;
    for (int i = 0; i < 16696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16696(int& val) {
    const int x = 16696;
    std::cout << 16696;
    for (int i = 0; i < 16696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16697(int& val) {
    func_in_16697(val);
    const int x = 16697;
    std::cout << 16697;
    for (int i = 0; i < 16697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16697(int& val) {
    const int x = 16697;
    std::cout << 16697;
    for (int i = 0; i < 16697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16698(int& val) {
    func_in_16698(val);
    const int x = 16698;
    std::cout << 16698;
    for (int i = 0; i < 16698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16698(int& val) {
    const int x = 16698;
    std::cout << 16698;
    for (int i = 0; i < 16698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16699(int& val) {
    func_in_16699(val);
    const int x = 16699;
    std::cout << 16699;
    for (int i = 0; i < 16699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16699(int& val) {
    const int x = 16699;
    std::cout << 16699;
    for (int i = 0; i < 16699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16700(int& val) {
    func_in_16700(val);
    const int x = 16700;
    std::cout << 16700;
    for (int i = 0; i < 16700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16700(int& val) {
    const int x = 16700;
    std::cout << 16700;
    for (int i = 0; i < 16700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16701(int& val) {
    func_in_16701(val);
    const int x = 16701;
    std::cout << 16701;
    for (int i = 0; i < 16701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16701(int& val) {
    const int x = 16701;
    std::cout << 16701;
    for (int i = 0; i < 16701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16702(int& val) {
    func_in_16702(val);
    const int x = 16702;
    std::cout << 16702;
    for (int i = 0; i < 16702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16702(int& val) {
    const int x = 16702;
    std::cout << 16702;
    for (int i = 0; i < 16702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16703(int& val) {
    func_in_16703(val);
    const int x = 16703;
    std::cout << 16703;
    for (int i = 0; i < 16703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16703(int& val) {
    const int x = 16703;
    std::cout << 16703;
    for (int i = 0; i < 16703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16704(int& val) {
    func_in_16704(val);
    const int x = 16704;
    std::cout << 16704;
    for (int i = 0; i < 16704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16704(int& val) {
    const int x = 16704;
    std::cout << 16704;
    for (int i = 0; i < 16704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16705(int& val) {
    func_in_16705(val);
    const int x = 16705;
    std::cout << 16705;
    for (int i = 0; i < 16705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16705(int& val) {
    const int x = 16705;
    std::cout << 16705;
    for (int i = 0; i < 16705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16706(int& val) {
    func_in_16706(val);
    const int x = 16706;
    std::cout << 16706;
    for (int i = 0; i < 16706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16706(int& val) {
    const int x = 16706;
    std::cout << 16706;
    for (int i = 0; i < 16706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16707(int& val) {
    func_in_16707(val);
    const int x = 16707;
    std::cout << 16707;
    for (int i = 0; i < 16707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16707(int& val) {
    const int x = 16707;
    std::cout << 16707;
    for (int i = 0; i < 16707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16708(int& val) {
    func_in_16708(val);
    const int x = 16708;
    std::cout << 16708;
    for (int i = 0; i < 16708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16708(int& val) {
    const int x = 16708;
    std::cout << 16708;
    for (int i = 0; i < 16708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16709(int& val) {
    func_in_16709(val);
    const int x = 16709;
    std::cout << 16709;
    for (int i = 0; i < 16709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16709(int& val) {
    const int x = 16709;
    std::cout << 16709;
    for (int i = 0; i < 16709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16710(int& val) {
    func_in_16710(val);
    const int x = 16710;
    std::cout << 16710;
    for (int i = 0; i < 16710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16710(int& val) {
    const int x = 16710;
    std::cout << 16710;
    for (int i = 0; i < 16710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16711(int& val) {
    func_in_16711(val);
    const int x = 16711;
    std::cout << 16711;
    for (int i = 0; i < 16711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16711(int& val) {
    const int x = 16711;
    std::cout << 16711;
    for (int i = 0; i < 16711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16712(int& val) {
    func_in_16712(val);
    const int x = 16712;
    std::cout << 16712;
    for (int i = 0; i < 16712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16712(int& val) {
    const int x = 16712;
    std::cout << 16712;
    for (int i = 0; i < 16712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16713(int& val) {
    func_in_16713(val);
    const int x = 16713;
    std::cout << 16713;
    for (int i = 0; i < 16713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16713(int& val) {
    const int x = 16713;
    std::cout << 16713;
    for (int i = 0; i < 16713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16714(int& val) {
    func_in_16714(val);
    const int x = 16714;
    std::cout << 16714;
    for (int i = 0; i < 16714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16714(int& val) {
    const int x = 16714;
    std::cout << 16714;
    for (int i = 0; i < 16714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16715(int& val) {
    func_in_16715(val);
    const int x = 16715;
    std::cout << 16715;
    for (int i = 0; i < 16715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16715(int& val) {
    const int x = 16715;
    std::cout << 16715;
    for (int i = 0; i < 16715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16716(int& val) {
    func_in_16716(val);
    const int x = 16716;
    std::cout << 16716;
    for (int i = 0; i < 16716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16716(int& val) {
    const int x = 16716;
    std::cout << 16716;
    for (int i = 0; i < 16716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16717(int& val) {
    func_in_16717(val);
    const int x = 16717;
    std::cout << 16717;
    for (int i = 0; i < 16717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16717(int& val) {
    const int x = 16717;
    std::cout << 16717;
    for (int i = 0; i < 16717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16718(int& val) {
    func_in_16718(val);
    const int x = 16718;
    std::cout << 16718;
    for (int i = 0; i < 16718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16718(int& val) {
    const int x = 16718;
    std::cout << 16718;
    for (int i = 0; i < 16718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16719(int& val) {
    func_in_16719(val);
    const int x = 16719;
    std::cout << 16719;
    for (int i = 0; i < 16719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16719(int& val) {
    const int x = 16719;
    std::cout << 16719;
    for (int i = 0; i < 16719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16720(int& val) {
    func_in_16720(val);
    const int x = 16720;
    std::cout << 16720;
    for (int i = 0; i < 16720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16720(int& val) {
    const int x = 16720;
    std::cout << 16720;
    for (int i = 0; i < 16720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16721(int& val) {
    func_in_16721(val);
    const int x = 16721;
    std::cout << 16721;
    for (int i = 0; i < 16721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16721(int& val) {
    const int x = 16721;
    std::cout << 16721;
    for (int i = 0; i < 16721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16722(int& val) {
    func_in_16722(val);
    const int x = 16722;
    std::cout << 16722;
    for (int i = 0; i < 16722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16722(int& val) {
    const int x = 16722;
    std::cout << 16722;
    for (int i = 0; i < 16722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16723(int& val) {
    func_in_16723(val);
    const int x = 16723;
    std::cout << 16723;
    for (int i = 0; i < 16723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16723(int& val) {
    const int x = 16723;
    std::cout << 16723;
    for (int i = 0; i < 16723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16724(int& val) {
    func_in_16724(val);
    const int x = 16724;
    std::cout << 16724;
    for (int i = 0; i < 16724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16724(int& val) {
    const int x = 16724;
    std::cout << 16724;
    for (int i = 0; i < 16724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16725(int& val) {
    func_in_16725(val);
    const int x = 16725;
    std::cout << 16725;
    for (int i = 0; i < 16725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16725(int& val) {
    const int x = 16725;
    std::cout << 16725;
    for (int i = 0; i < 16725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16726(int& val) {
    func_in_16726(val);
    const int x = 16726;
    std::cout << 16726;
    for (int i = 0; i < 16726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16726(int& val) {
    const int x = 16726;
    std::cout << 16726;
    for (int i = 0; i < 16726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16727(int& val) {
    func_in_16727(val);
    const int x = 16727;
    std::cout << 16727;
    for (int i = 0; i < 16727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16727(int& val) {
    const int x = 16727;
    std::cout << 16727;
    for (int i = 0; i < 16727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16728(int& val) {
    func_in_16728(val);
    const int x = 16728;
    std::cout << 16728;
    for (int i = 0; i < 16728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16728(int& val) {
    const int x = 16728;
    std::cout << 16728;
    for (int i = 0; i < 16728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16729(int& val) {
    func_in_16729(val);
    const int x = 16729;
    std::cout << 16729;
    for (int i = 0; i < 16729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16729(int& val) {
    const int x = 16729;
    std::cout << 16729;
    for (int i = 0; i < 16729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16730(int& val) {
    func_in_16730(val);
    const int x = 16730;
    std::cout << 16730;
    for (int i = 0; i < 16730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16730(int& val) {
    const int x = 16730;
    std::cout << 16730;
    for (int i = 0; i < 16730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16731(int& val) {
    func_in_16731(val);
    const int x = 16731;
    std::cout << 16731;
    for (int i = 0; i < 16731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16731(int& val) {
    const int x = 16731;
    std::cout << 16731;
    for (int i = 0; i < 16731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16732(int& val) {
    func_in_16732(val);
    const int x = 16732;
    std::cout << 16732;
    for (int i = 0; i < 16732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16732(int& val) {
    const int x = 16732;
    std::cout << 16732;
    for (int i = 0; i < 16732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16733(int& val) {
    func_in_16733(val);
    const int x = 16733;
    std::cout << 16733;
    for (int i = 0; i < 16733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16733(int& val) {
    const int x = 16733;
    std::cout << 16733;
    for (int i = 0; i < 16733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16734(int& val) {
    func_in_16734(val);
    const int x = 16734;
    std::cout << 16734;
    for (int i = 0; i < 16734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16734(int& val) {
    const int x = 16734;
    std::cout << 16734;
    for (int i = 0; i < 16734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16735(int& val) {
    func_in_16735(val);
    const int x = 16735;
    std::cout << 16735;
    for (int i = 0; i < 16735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16735(int& val) {
    const int x = 16735;
    std::cout << 16735;
    for (int i = 0; i < 16735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16736(int& val) {
    func_in_16736(val);
    const int x = 16736;
    std::cout << 16736;
    for (int i = 0; i < 16736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16736(int& val) {
    const int x = 16736;
    std::cout << 16736;
    for (int i = 0; i < 16736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16737(int& val) {
    func_in_16737(val);
    const int x = 16737;
    std::cout << 16737;
    for (int i = 0; i < 16737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16737(int& val) {
    const int x = 16737;
    std::cout << 16737;
    for (int i = 0; i < 16737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16738(int& val) {
    func_in_16738(val);
    const int x = 16738;
    std::cout << 16738;
    for (int i = 0; i < 16738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16738(int& val) {
    const int x = 16738;
    std::cout << 16738;
    for (int i = 0; i < 16738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16739(int& val) {
    func_in_16739(val);
    const int x = 16739;
    std::cout << 16739;
    for (int i = 0; i < 16739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16739(int& val) {
    const int x = 16739;
    std::cout << 16739;
    for (int i = 0; i < 16739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16740(int& val) {
    func_in_16740(val);
    const int x = 16740;
    std::cout << 16740;
    for (int i = 0; i < 16740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16740(int& val) {
    const int x = 16740;
    std::cout << 16740;
    for (int i = 0; i < 16740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16741(int& val) {
    func_in_16741(val);
    const int x = 16741;
    std::cout << 16741;
    for (int i = 0; i < 16741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16741(int& val) {
    const int x = 16741;
    std::cout << 16741;
    for (int i = 0; i < 16741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16742(int& val) {
    func_in_16742(val);
    const int x = 16742;
    std::cout << 16742;
    for (int i = 0; i < 16742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16742(int& val) {
    const int x = 16742;
    std::cout << 16742;
    for (int i = 0; i < 16742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16743(int& val) {
    func_in_16743(val);
    const int x = 16743;
    std::cout << 16743;
    for (int i = 0; i < 16743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16743(int& val) {
    const int x = 16743;
    std::cout << 16743;
    for (int i = 0; i < 16743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16744(int& val) {
    func_in_16744(val);
    const int x = 16744;
    std::cout << 16744;
    for (int i = 0; i < 16744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16744(int& val) {
    const int x = 16744;
    std::cout << 16744;
    for (int i = 0; i < 16744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16745(int& val) {
    func_in_16745(val);
    const int x = 16745;
    std::cout << 16745;
    for (int i = 0; i < 16745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16745(int& val) {
    const int x = 16745;
    std::cout << 16745;
    for (int i = 0; i < 16745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16746(int& val) {
    func_in_16746(val);
    const int x = 16746;
    std::cout << 16746;
    for (int i = 0; i < 16746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16746(int& val) {
    const int x = 16746;
    std::cout << 16746;
    for (int i = 0; i < 16746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16747(int& val) {
    func_in_16747(val);
    const int x = 16747;
    std::cout << 16747;
    for (int i = 0; i < 16747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16747(int& val) {
    const int x = 16747;
    std::cout << 16747;
    for (int i = 0; i < 16747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16748(int& val) {
    func_in_16748(val);
    const int x = 16748;
    std::cout << 16748;
    for (int i = 0; i < 16748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16748(int& val) {
    const int x = 16748;
    std::cout << 16748;
    for (int i = 0; i < 16748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16749(int& val) {
    func_in_16749(val);
    const int x = 16749;
    std::cout << 16749;
    for (int i = 0; i < 16749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16749(int& val) {
    const int x = 16749;
    std::cout << 16749;
    for (int i = 0; i < 16749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16750(int& val) {
    func_in_16750(val);
    const int x = 16750;
    std::cout << 16750;
    for (int i = 0; i < 16750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16750(int& val) {
    const int x = 16750;
    std::cout << 16750;
    for (int i = 0; i < 16750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16751(int& val) {
    func_in_16751(val);
    const int x = 16751;
    std::cout << 16751;
    for (int i = 0; i < 16751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16751(int& val) {
    const int x = 16751;
    std::cout << 16751;
    for (int i = 0; i < 16751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16752(int& val) {
    func_in_16752(val);
    const int x = 16752;
    std::cout << 16752;
    for (int i = 0; i < 16752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16752(int& val) {
    const int x = 16752;
    std::cout << 16752;
    for (int i = 0; i < 16752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16753(int& val) {
    func_in_16753(val);
    const int x = 16753;
    std::cout << 16753;
    for (int i = 0; i < 16753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16753(int& val) {
    const int x = 16753;
    std::cout << 16753;
    for (int i = 0; i < 16753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16754(int& val) {
    func_in_16754(val);
    const int x = 16754;
    std::cout << 16754;
    for (int i = 0; i < 16754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16754(int& val) {
    const int x = 16754;
    std::cout << 16754;
    for (int i = 0; i < 16754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16755(int& val) {
    func_in_16755(val);
    const int x = 16755;
    std::cout << 16755;
    for (int i = 0; i < 16755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16755(int& val) {
    const int x = 16755;
    std::cout << 16755;
    for (int i = 0; i < 16755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16756(int& val) {
    func_in_16756(val);
    const int x = 16756;
    std::cout << 16756;
    for (int i = 0; i < 16756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16756(int& val) {
    const int x = 16756;
    std::cout << 16756;
    for (int i = 0; i < 16756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16757(int& val) {
    func_in_16757(val);
    const int x = 16757;
    std::cout << 16757;
    for (int i = 0; i < 16757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16757(int& val) {
    const int x = 16757;
    std::cout << 16757;
    for (int i = 0; i < 16757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16758(int& val) {
    func_in_16758(val);
    const int x = 16758;
    std::cout << 16758;
    for (int i = 0; i < 16758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16758(int& val) {
    const int x = 16758;
    std::cout << 16758;
    for (int i = 0; i < 16758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16759(int& val) {
    func_in_16759(val);
    const int x = 16759;
    std::cout << 16759;
    for (int i = 0; i < 16759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16759(int& val) {
    const int x = 16759;
    std::cout << 16759;
    for (int i = 0; i < 16759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16760(int& val) {
    func_in_16760(val);
    const int x = 16760;
    std::cout << 16760;
    for (int i = 0; i < 16760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16760(int& val) {
    const int x = 16760;
    std::cout << 16760;
    for (int i = 0; i < 16760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16761(int& val) {
    func_in_16761(val);
    const int x = 16761;
    std::cout << 16761;
    for (int i = 0; i < 16761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16761(int& val) {
    const int x = 16761;
    std::cout << 16761;
    for (int i = 0; i < 16761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16762(int& val) {
    func_in_16762(val);
    const int x = 16762;
    std::cout << 16762;
    for (int i = 0; i < 16762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16762(int& val) {
    const int x = 16762;
    std::cout << 16762;
    for (int i = 0; i < 16762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16763(int& val) {
    func_in_16763(val);
    const int x = 16763;
    std::cout << 16763;
    for (int i = 0; i < 16763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16763(int& val) {
    const int x = 16763;
    std::cout << 16763;
    for (int i = 0; i < 16763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16764(int& val) {
    func_in_16764(val);
    const int x = 16764;
    std::cout << 16764;
    for (int i = 0; i < 16764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16764(int& val) {
    const int x = 16764;
    std::cout << 16764;
    for (int i = 0; i < 16764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16765(int& val) {
    func_in_16765(val);
    const int x = 16765;
    std::cout << 16765;
    for (int i = 0; i < 16765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16765(int& val) {
    const int x = 16765;
    std::cout << 16765;
    for (int i = 0; i < 16765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16766(int& val) {
    func_in_16766(val);
    const int x = 16766;
    std::cout << 16766;
    for (int i = 0; i < 16766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16766(int& val) {
    const int x = 16766;
    std::cout << 16766;
    for (int i = 0; i < 16766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16767(int& val) {
    func_in_16767(val);
    const int x = 16767;
    std::cout << 16767;
    for (int i = 0; i < 16767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16767(int& val) {
    const int x = 16767;
    std::cout << 16767;
    for (int i = 0; i < 16767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16768(int& val) {
    func_in_16768(val);
    const int x = 16768;
    std::cout << 16768;
    for (int i = 0; i < 16768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16768(int& val) {
    const int x = 16768;
    std::cout << 16768;
    for (int i = 0; i < 16768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16769(int& val) {
    func_in_16769(val);
    const int x = 16769;
    std::cout << 16769;
    for (int i = 0; i < 16769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16769(int& val) {
    const int x = 16769;
    std::cout << 16769;
    for (int i = 0; i < 16769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16770(int& val) {
    func_in_16770(val);
    const int x = 16770;
    std::cout << 16770;
    for (int i = 0; i < 16770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16770(int& val) {
    const int x = 16770;
    std::cout << 16770;
    for (int i = 0; i < 16770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16771(int& val) {
    func_in_16771(val);
    const int x = 16771;
    std::cout << 16771;
    for (int i = 0; i < 16771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16771(int& val) {
    const int x = 16771;
    std::cout << 16771;
    for (int i = 0; i < 16771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16772(int& val) {
    func_in_16772(val);
    const int x = 16772;
    std::cout << 16772;
    for (int i = 0; i < 16772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16772(int& val) {
    const int x = 16772;
    std::cout << 16772;
    for (int i = 0; i < 16772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16773(int& val) {
    func_in_16773(val);
    const int x = 16773;
    std::cout << 16773;
    for (int i = 0; i < 16773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16773(int& val) {
    const int x = 16773;
    std::cout << 16773;
    for (int i = 0; i < 16773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16774(int& val) {
    func_in_16774(val);
    const int x = 16774;
    std::cout << 16774;
    for (int i = 0; i < 16774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16774(int& val) {
    const int x = 16774;
    std::cout << 16774;
    for (int i = 0; i < 16774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16775(int& val) {
    func_in_16775(val);
    const int x = 16775;
    std::cout << 16775;
    for (int i = 0; i < 16775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16775(int& val) {
    const int x = 16775;
    std::cout << 16775;
    for (int i = 0; i < 16775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16776(int& val) {
    func_in_16776(val);
    const int x = 16776;
    std::cout << 16776;
    for (int i = 0; i < 16776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16776(int& val) {
    const int x = 16776;
    std::cout << 16776;
    for (int i = 0; i < 16776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16777(int& val) {
    func_in_16777(val);
    const int x = 16777;
    std::cout << 16777;
    for (int i = 0; i < 16777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16777(int& val) {
    const int x = 16777;
    std::cout << 16777;
    for (int i = 0; i < 16777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16778(int& val) {
    func_in_16778(val);
    const int x = 16778;
    std::cout << 16778;
    for (int i = 0; i < 16778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16778(int& val) {
    const int x = 16778;
    std::cout << 16778;
    for (int i = 0; i < 16778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16779(int& val) {
    func_in_16779(val);
    const int x = 16779;
    std::cout << 16779;
    for (int i = 0; i < 16779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16779(int& val) {
    const int x = 16779;
    std::cout << 16779;
    for (int i = 0; i < 16779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16780(int& val) {
    func_in_16780(val);
    const int x = 16780;
    std::cout << 16780;
    for (int i = 0; i < 16780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16780(int& val) {
    const int x = 16780;
    std::cout << 16780;
    for (int i = 0; i < 16780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16781(int& val) {
    func_in_16781(val);
    const int x = 16781;
    std::cout << 16781;
    for (int i = 0; i < 16781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16781(int& val) {
    const int x = 16781;
    std::cout << 16781;
    for (int i = 0; i < 16781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16782(int& val) {
    func_in_16782(val);
    const int x = 16782;
    std::cout << 16782;
    for (int i = 0; i < 16782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16782(int& val) {
    const int x = 16782;
    std::cout << 16782;
    for (int i = 0; i < 16782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16783(int& val) {
    func_in_16783(val);
    const int x = 16783;
    std::cout << 16783;
    for (int i = 0; i < 16783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16783(int& val) {
    const int x = 16783;
    std::cout << 16783;
    for (int i = 0; i < 16783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16784(int& val) {
    func_in_16784(val);
    const int x = 16784;
    std::cout << 16784;
    for (int i = 0; i < 16784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16784(int& val) {
    const int x = 16784;
    std::cout << 16784;
    for (int i = 0; i < 16784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16785(int& val) {
    func_in_16785(val);
    const int x = 16785;
    std::cout << 16785;
    for (int i = 0; i < 16785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16785(int& val) {
    const int x = 16785;
    std::cout << 16785;
    for (int i = 0; i < 16785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16786(int& val) {
    func_in_16786(val);
    const int x = 16786;
    std::cout << 16786;
    for (int i = 0; i < 16786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16786(int& val) {
    const int x = 16786;
    std::cout << 16786;
    for (int i = 0; i < 16786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16787(int& val) {
    func_in_16787(val);
    const int x = 16787;
    std::cout << 16787;
    for (int i = 0; i < 16787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16787(int& val) {
    const int x = 16787;
    std::cout << 16787;
    for (int i = 0; i < 16787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16788(int& val) {
    func_in_16788(val);
    const int x = 16788;
    std::cout << 16788;
    for (int i = 0; i < 16788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16788(int& val) {
    const int x = 16788;
    std::cout << 16788;
    for (int i = 0; i < 16788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16789(int& val) {
    func_in_16789(val);
    const int x = 16789;
    std::cout << 16789;
    for (int i = 0; i < 16789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16789(int& val) {
    const int x = 16789;
    std::cout << 16789;
    for (int i = 0; i < 16789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16790(int& val) {
    func_in_16790(val);
    const int x = 16790;
    std::cout << 16790;
    for (int i = 0; i < 16790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16790(int& val) {
    const int x = 16790;
    std::cout << 16790;
    for (int i = 0; i < 16790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16791(int& val) {
    func_in_16791(val);
    const int x = 16791;
    std::cout << 16791;
    for (int i = 0; i < 16791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16791(int& val) {
    const int x = 16791;
    std::cout << 16791;
    for (int i = 0; i < 16791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16792(int& val) {
    func_in_16792(val);
    const int x = 16792;
    std::cout << 16792;
    for (int i = 0; i < 16792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16792(int& val) {
    const int x = 16792;
    std::cout << 16792;
    for (int i = 0; i < 16792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16793(int& val) {
    func_in_16793(val);
    const int x = 16793;
    std::cout << 16793;
    for (int i = 0; i < 16793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16793(int& val) {
    const int x = 16793;
    std::cout << 16793;
    for (int i = 0; i < 16793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16794(int& val) {
    func_in_16794(val);
    const int x = 16794;
    std::cout << 16794;
    for (int i = 0; i < 16794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16794(int& val) {
    const int x = 16794;
    std::cout << 16794;
    for (int i = 0; i < 16794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16795(int& val) {
    func_in_16795(val);
    const int x = 16795;
    std::cout << 16795;
    for (int i = 0; i < 16795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16795(int& val) {
    const int x = 16795;
    std::cout << 16795;
    for (int i = 0; i < 16795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16796(int& val) {
    func_in_16796(val);
    const int x = 16796;
    std::cout << 16796;
    for (int i = 0; i < 16796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16796(int& val) {
    const int x = 16796;
    std::cout << 16796;
    for (int i = 0; i < 16796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16797(int& val) {
    func_in_16797(val);
    const int x = 16797;
    std::cout << 16797;
    for (int i = 0; i < 16797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16797(int& val) {
    const int x = 16797;
    std::cout << 16797;
    for (int i = 0; i < 16797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16798(int& val) {
    func_in_16798(val);
    const int x = 16798;
    std::cout << 16798;
    for (int i = 0; i < 16798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16798(int& val) {
    const int x = 16798;
    std::cout << 16798;
    for (int i = 0; i < 16798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16799(int& val) {
    func_in_16799(val);
    const int x = 16799;
    std::cout << 16799;
    for (int i = 0; i < 16799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16799(int& val) {
    const int x = 16799;
    std::cout << 16799;
    for (int i = 0; i < 16799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16800(int& val) {
    func_in_16800(val);
    const int x = 16800;
    std::cout << 16800;
    for (int i = 0; i < 16800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16800(int& val) {
    const int x = 16800;
    std::cout << 16800;
    for (int i = 0; i < 16800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16801(int& val) {
    func_in_16801(val);
    const int x = 16801;
    std::cout << 16801;
    for (int i = 0; i < 16801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16801(int& val) {
    const int x = 16801;
    std::cout << 16801;
    for (int i = 0; i < 16801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16802(int& val) {
    func_in_16802(val);
    const int x = 16802;
    std::cout << 16802;
    for (int i = 0; i < 16802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16802(int& val) {
    const int x = 16802;
    std::cout << 16802;
    for (int i = 0; i < 16802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16803(int& val) {
    func_in_16803(val);
    const int x = 16803;
    std::cout << 16803;
    for (int i = 0; i < 16803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16803(int& val) {
    const int x = 16803;
    std::cout << 16803;
    for (int i = 0; i < 16803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16804(int& val) {
    func_in_16804(val);
    const int x = 16804;
    std::cout << 16804;
    for (int i = 0; i < 16804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16804(int& val) {
    const int x = 16804;
    std::cout << 16804;
    for (int i = 0; i < 16804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16805(int& val) {
    func_in_16805(val);
    const int x = 16805;
    std::cout << 16805;
    for (int i = 0; i < 16805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16805(int& val) {
    const int x = 16805;
    std::cout << 16805;
    for (int i = 0; i < 16805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16806(int& val) {
    func_in_16806(val);
    const int x = 16806;
    std::cout << 16806;
    for (int i = 0; i < 16806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16806(int& val) {
    const int x = 16806;
    std::cout << 16806;
    for (int i = 0; i < 16806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16807(int& val) {
    func_in_16807(val);
    const int x = 16807;
    std::cout << 16807;
    for (int i = 0; i < 16807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16807(int& val) {
    const int x = 16807;
    std::cout << 16807;
    for (int i = 0; i < 16807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16808(int& val) {
    func_in_16808(val);
    const int x = 16808;
    std::cout << 16808;
    for (int i = 0; i < 16808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16808(int& val) {
    const int x = 16808;
    std::cout << 16808;
    for (int i = 0; i < 16808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16809(int& val) {
    func_in_16809(val);
    const int x = 16809;
    std::cout << 16809;
    for (int i = 0; i < 16809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16809(int& val) {
    const int x = 16809;
    std::cout << 16809;
    for (int i = 0; i < 16809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16810(int& val) {
    func_in_16810(val);
    const int x = 16810;
    std::cout << 16810;
    for (int i = 0; i < 16810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16810(int& val) {
    const int x = 16810;
    std::cout << 16810;
    for (int i = 0; i < 16810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16811(int& val) {
    func_in_16811(val);
    const int x = 16811;
    std::cout << 16811;
    for (int i = 0; i < 16811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16811(int& val) {
    const int x = 16811;
    std::cout << 16811;
    for (int i = 0; i < 16811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16812(int& val) {
    func_in_16812(val);
    const int x = 16812;
    std::cout << 16812;
    for (int i = 0; i < 16812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16812(int& val) {
    const int x = 16812;
    std::cout << 16812;
    for (int i = 0; i < 16812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16813(int& val) {
    func_in_16813(val);
    const int x = 16813;
    std::cout << 16813;
    for (int i = 0; i < 16813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16813(int& val) {
    const int x = 16813;
    std::cout << 16813;
    for (int i = 0; i < 16813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16814(int& val) {
    func_in_16814(val);
    const int x = 16814;
    std::cout << 16814;
    for (int i = 0; i < 16814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16814(int& val) {
    const int x = 16814;
    std::cout << 16814;
    for (int i = 0; i < 16814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16815(int& val) {
    func_in_16815(val);
    const int x = 16815;
    std::cout << 16815;
    for (int i = 0; i < 16815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16815(int& val) {
    const int x = 16815;
    std::cout << 16815;
    for (int i = 0; i < 16815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16816(int& val) {
    func_in_16816(val);
    const int x = 16816;
    std::cout << 16816;
    for (int i = 0; i < 16816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16816(int& val) {
    const int x = 16816;
    std::cout << 16816;
    for (int i = 0; i < 16816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16817(int& val) {
    func_in_16817(val);
    const int x = 16817;
    std::cout << 16817;
    for (int i = 0; i < 16817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16817(int& val) {
    const int x = 16817;
    std::cout << 16817;
    for (int i = 0; i < 16817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16818(int& val) {
    func_in_16818(val);
    const int x = 16818;
    std::cout << 16818;
    for (int i = 0; i < 16818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16818(int& val) {
    const int x = 16818;
    std::cout << 16818;
    for (int i = 0; i < 16818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16819(int& val) {
    func_in_16819(val);
    const int x = 16819;
    std::cout << 16819;
    for (int i = 0; i < 16819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16819(int& val) {
    const int x = 16819;
    std::cout << 16819;
    for (int i = 0; i < 16819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16820(int& val) {
    func_in_16820(val);
    const int x = 16820;
    std::cout << 16820;
    for (int i = 0; i < 16820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16820(int& val) {
    const int x = 16820;
    std::cout << 16820;
    for (int i = 0; i < 16820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16821(int& val) {
    func_in_16821(val);
    const int x = 16821;
    std::cout << 16821;
    for (int i = 0; i < 16821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16821(int& val) {
    const int x = 16821;
    std::cout << 16821;
    for (int i = 0; i < 16821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16822(int& val) {
    func_in_16822(val);
    const int x = 16822;
    std::cout << 16822;
    for (int i = 0; i < 16822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16822(int& val) {
    const int x = 16822;
    std::cout << 16822;
    for (int i = 0; i < 16822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16823(int& val) {
    func_in_16823(val);
    const int x = 16823;
    std::cout << 16823;
    for (int i = 0; i < 16823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16823(int& val) {
    const int x = 16823;
    std::cout << 16823;
    for (int i = 0; i < 16823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16824(int& val) {
    func_in_16824(val);
    const int x = 16824;
    std::cout << 16824;
    for (int i = 0; i < 16824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16824(int& val) {
    const int x = 16824;
    std::cout << 16824;
    for (int i = 0; i < 16824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16825(int& val) {
    func_in_16825(val);
    const int x = 16825;
    std::cout << 16825;
    for (int i = 0; i < 16825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16825(int& val) {
    const int x = 16825;
    std::cout << 16825;
    for (int i = 0; i < 16825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16826(int& val) {
    func_in_16826(val);
    const int x = 16826;
    std::cout << 16826;
    for (int i = 0; i < 16826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16826(int& val) {
    const int x = 16826;
    std::cout << 16826;
    for (int i = 0; i < 16826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16827(int& val) {
    func_in_16827(val);
    const int x = 16827;
    std::cout << 16827;
    for (int i = 0; i < 16827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16827(int& val) {
    const int x = 16827;
    std::cout << 16827;
    for (int i = 0; i < 16827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16828(int& val) {
    func_in_16828(val);
    const int x = 16828;
    std::cout << 16828;
    for (int i = 0; i < 16828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16828(int& val) {
    const int x = 16828;
    std::cout << 16828;
    for (int i = 0; i < 16828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16829(int& val) {
    func_in_16829(val);
    const int x = 16829;
    std::cout << 16829;
    for (int i = 0; i < 16829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16829(int& val) {
    const int x = 16829;
    std::cout << 16829;
    for (int i = 0; i < 16829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16830(int& val) {
    func_in_16830(val);
    const int x = 16830;
    std::cout << 16830;
    for (int i = 0; i < 16830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16830(int& val) {
    const int x = 16830;
    std::cout << 16830;
    for (int i = 0; i < 16830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16831(int& val) {
    func_in_16831(val);
    const int x = 16831;
    std::cout << 16831;
    for (int i = 0; i < 16831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16831(int& val) {
    const int x = 16831;
    std::cout << 16831;
    for (int i = 0; i < 16831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16832(int& val) {
    func_in_16832(val);
    const int x = 16832;
    std::cout << 16832;
    for (int i = 0; i < 16832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16832(int& val) {
    const int x = 16832;
    std::cout << 16832;
    for (int i = 0; i < 16832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16833(int& val) {
    func_in_16833(val);
    const int x = 16833;
    std::cout << 16833;
    for (int i = 0; i < 16833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16833(int& val) {
    const int x = 16833;
    std::cout << 16833;
    for (int i = 0; i < 16833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16834(int& val) {
    func_in_16834(val);
    const int x = 16834;
    std::cout << 16834;
    for (int i = 0; i < 16834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16834(int& val) {
    const int x = 16834;
    std::cout << 16834;
    for (int i = 0; i < 16834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16835(int& val) {
    func_in_16835(val);
    const int x = 16835;
    std::cout << 16835;
    for (int i = 0; i < 16835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16835(int& val) {
    const int x = 16835;
    std::cout << 16835;
    for (int i = 0; i < 16835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16836(int& val) {
    func_in_16836(val);
    const int x = 16836;
    std::cout << 16836;
    for (int i = 0; i < 16836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16836(int& val) {
    const int x = 16836;
    std::cout << 16836;
    for (int i = 0; i < 16836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16837(int& val) {
    func_in_16837(val);
    const int x = 16837;
    std::cout << 16837;
    for (int i = 0; i < 16837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16837(int& val) {
    const int x = 16837;
    std::cout << 16837;
    for (int i = 0; i < 16837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16838(int& val) {
    func_in_16838(val);
    const int x = 16838;
    std::cout << 16838;
    for (int i = 0; i < 16838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16838(int& val) {
    const int x = 16838;
    std::cout << 16838;
    for (int i = 0; i < 16838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16839(int& val) {
    func_in_16839(val);
    const int x = 16839;
    std::cout << 16839;
    for (int i = 0; i < 16839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16839(int& val) {
    const int x = 16839;
    std::cout << 16839;
    for (int i = 0; i < 16839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16840(int& val) {
    func_in_16840(val);
    const int x = 16840;
    std::cout << 16840;
    for (int i = 0; i < 16840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16840(int& val) {
    const int x = 16840;
    std::cout << 16840;
    for (int i = 0; i < 16840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16841(int& val) {
    func_in_16841(val);
    const int x = 16841;
    std::cout << 16841;
    for (int i = 0; i < 16841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16841(int& val) {
    const int x = 16841;
    std::cout << 16841;
    for (int i = 0; i < 16841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16842(int& val) {
    func_in_16842(val);
    const int x = 16842;
    std::cout << 16842;
    for (int i = 0; i < 16842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16842(int& val) {
    const int x = 16842;
    std::cout << 16842;
    for (int i = 0; i < 16842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16843(int& val) {
    func_in_16843(val);
    const int x = 16843;
    std::cout << 16843;
    for (int i = 0; i < 16843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16843(int& val) {
    const int x = 16843;
    std::cout << 16843;
    for (int i = 0; i < 16843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16844(int& val) {
    func_in_16844(val);
    const int x = 16844;
    std::cout << 16844;
    for (int i = 0; i < 16844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16844(int& val) {
    const int x = 16844;
    std::cout << 16844;
    for (int i = 0; i < 16844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16845(int& val) {
    func_in_16845(val);
    const int x = 16845;
    std::cout << 16845;
    for (int i = 0; i < 16845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16845(int& val) {
    const int x = 16845;
    std::cout << 16845;
    for (int i = 0; i < 16845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16846(int& val) {
    func_in_16846(val);
    const int x = 16846;
    std::cout << 16846;
    for (int i = 0; i < 16846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16846(int& val) {
    const int x = 16846;
    std::cout << 16846;
    for (int i = 0; i < 16846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16847(int& val) {
    func_in_16847(val);
    const int x = 16847;
    std::cout << 16847;
    for (int i = 0; i < 16847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16847(int& val) {
    const int x = 16847;
    std::cout << 16847;
    for (int i = 0; i < 16847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16848(int& val) {
    func_in_16848(val);
    const int x = 16848;
    std::cout << 16848;
    for (int i = 0; i < 16848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16848(int& val) {
    const int x = 16848;
    std::cout << 16848;
    for (int i = 0; i < 16848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16849(int& val) {
    func_in_16849(val);
    const int x = 16849;
    std::cout << 16849;
    for (int i = 0; i < 16849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16849(int& val) {
    const int x = 16849;
    std::cout << 16849;
    for (int i = 0; i < 16849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16850(int& val) {
    func_in_16850(val);
    const int x = 16850;
    std::cout << 16850;
    for (int i = 0; i < 16850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16850(int& val) {
    const int x = 16850;
    std::cout << 16850;
    for (int i = 0; i < 16850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16851(int& val) {
    func_in_16851(val);
    const int x = 16851;
    std::cout << 16851;
    for (int i = 0; i < 16851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16851(int& val) {
    const int x = 16851;
    std::cout << 16851;
    for (int i = 0; i < 16851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16852(int& val) {
    func_in_16852(val);
    const int x = 16852;
    std::cout << 16852;
    for (int i = 0; i < 16852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16852(int& val) {
    const int x = 16852;
    std::cout << 16852;
    for (int i = 0; i < 16852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16853(int& val) {
    func_in_16853(val);
    const int x = 16853;
    std::cout << 16853;
    for (int i = 0; i < 16853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16853(int& val) {
    const int x = 16853;
    std::cout << 16853;
    for (int i = 0; i < 16853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16854(int& val) {
    func_in_16854(val);
    const int x = 16854;
    std::cout << 16854;
    for (int i = 0; i < 16854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16854(int& val) {
    const int x = 16854;
    std::cout << 16854;
    for (int i = 0; i < 16854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16855(int& val) {
    func_in_16855(val);
    const int x = 16855;
    std::cout << 16855;
    for (int i = 0; i < 16855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16855(int& val) {
    const int x = 16855;
    std::cout << 16855;
    for (int i = 0; i < 16855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16856(int& val) {
    func_in_16856(val);
    const int x = 16856;
    std::cout << 16856;
    for (int i = 0; i < 16856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16856(int& val) {
    const int x = 16856;
    std::cout << 16856;
    for (int i = 0; i < 16856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16857(int& val) {
    func_in_16857(val);
    const int x = 16857;
    std::cout << 16857;
    for (int i = 0; i < 16857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16857(int& val) {
    const int x = 16857;
    std::cout << 16857;
    for (int i = 0; i < 16857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16858(int& val) {
    func_in_16858(val);
    const int x = 16858;
    std::cout << 16858;
    for (int i = 0; i < 16858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16858(int& val) {
    const int x = 16858;
    std::cout << 16858;
    for (int i = 0; i < 16858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16859(int& val) {
    func_in_16859(val);
    const int x = 16859;
    std::cout << 16859;
    for (int i = 0; i < 16859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16859(int& val) {
    const int x = 16859;
    std::cout << 16859;
    for (int i = 0; i < 16859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16860(int& val) {
    func_in_16860(val);
    const int x = 16860;
    std::cout << 16860;
    for (int i = 0; i < 16860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16860(int& val) {
    const int x = 16860;
    std::cout << 16860;
    for (int i = 0; i < 16860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16861(int& val) {
    func_in_16861(val);
    const int x = 16861;
    std::cout << 16861;
    for (int i = 0; i < 16861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16861(int& val) {
    const int x = 16861;
    std::cout << 16861;
    for (int i = 0; i < 16861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16862(int& val) {
    func_in_16862(val);
    const int x = 16862;
    std::cout << 16862;
    for (int i = 0; i < 16862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16862(int& val) {
    const int x = 16862;
    std::cout << 16862;
    for (int i = 0; i < 16862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16863(int& val) {
    func_in_16863(val);
    const int x = 16863;
    std::cout << 16863;
    for (int i = 0; i < 16863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16863(int& val) {
    const int x = 16863;
    std::cout << 16863;
    for (int i = 0; i < 16863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16864(int& val) {
    func_in_16864(val);
    const int x = 16864;
    std::cout << 16864;
    for (int i = 0; i < 16864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16864(int& val) {
    const int x = 16864;
    std::cout << 16864;
    for (int i = 0; i < 16864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16865(int& val) {
    func_in_16865(val);
    const int x = 16865;
    std::cout << 16865;
    for (int i = 0; i < 16865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16865(int& val) {
    const int x = 16865;
    std::cout << 16865;
    for (int i = 0; i < 16865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16866(int& val) {
    func_in_16866(val);
    const int x = 16866;
    std::cout << 16866;
    for (int i = 0; i < 16866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16866(int& val) {
    const int x = 16866;
    std::cout << 16866;
    for (int i = 0; i < 16866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16867(int& val) {
    func_in_16867(val);
    const int x = 16867;
    std::cout << 16867;
    for (int i = 0; i < 16867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16867(int& val) {
    const int x = 16867;
    std::cout << 16867;
    for (int i = 0; i < 16867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16868(int& val) {
    func_in_16868(val);
    const int x = 16868;
    std::cout << 16868;
    for (int i = 0; i < 16868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16868(int& val) {
    const int x = 16868;
    std::cout << 16868;
    for (int i = 0; i < 16868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16869(int& val) {
    func_in_16869(val);
    const int x = 16869;
    std::cout << 16869;
    for (int i = 0; i < 16869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16869(int& val) {
    const int x = 16869;
    std::cout << 16869;
    for (int i = 0; i < 16869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16870(int& val) {
    func_in_16870(val);
    const int x = 16870;
    std::cout << 16870;
    for (int i = 0; i < 16870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16870(int& val) {
    const int x = 16870;
    std::cout << 16870;
    for (int i = 0; i < 16870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16871(int& val) {
    func_in_16871(val);
    const int x = 16871;
    std::cout << 16871;
    for (int i = 0; i < 16871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16871(int& val) {
    const int x = 16871;
    std::cout << 16871;
    for (int i = 0; i < 16871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16872(int& val) {
    func_in_16872(val);
    const int x = 16872;
    std::cout << 16872;
    for (int i = 0; i < 16872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16872(int& val) {
    const int x = 16872;
    std::cout << 16872;
    for (int i = 0; i < 16872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16873(int& val) {
    func_in_16873(val);
    const int x = 16873;
    std::cout << 16873;
    for (int i = 0; i < 16873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16873(int& val) {
    const int x = 16873;
    std::cout << 16873;
    for (int i = 0; i < 16873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16874(int& val) {
    func_in_16874(val);
    const int x = 16874;
    std::cout << 16874;
    for (int i = 0; i < 16874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16874(int& val) {
    const int x = 16874;
    std::cout << 16874;
    for (int i = 0; i < 16874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16875(int& val) {
    func_in_16875(val);
    const int x = 16875;
    std::cout << 16875;
    for (int i = 0; i < 16875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16875(int& val) {
    const int x = 16875;
    std::cout << 16875;
    for (int i = 0; i < 16875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16876(int& val) {
    func_in_16876(val);
    const int x = 16876;
    std::cout << 16876;
    for (int i = 0; i < 16876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16876(int& val) {
    const int x = 16876;
    std::cout << 16876;
    for (int i = 0; i < 16876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16877(int& val) {
    func_in_16877(val);
    const int x = 16877;
    std::cout << 16877;
    for (int i = 0; i < 16877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16877(int& val) {
    const int x = 16877;
    std::cout << 16877;
    for (int i = 0; i < 16877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16878(int& val) {
    func_in_16878(val);
    const int x = 16878;
    std::cout << 16878;
    for (int i = 0; i < 16878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16878(int& val) {
    const int x = 16878;
    std::cout << 16878;
    for (int i = 0; i < 16878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16879(int& val) {
    func_in_16879(val);
    const int x = 16879;
    std::cout << 16879;
    for (int i = 0; i < 16879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16879(int& val) {
    const int x = 16879;
    std::cout << 16879;
    for (int i = 0; i < 16879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16880(int& val) {
    func_in_16880(val);
    const int x = 16880;
    std::cout << 16880;
    for (int i = 0; i < 16880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16880(int& val) {
    const int x = 16880;
    std::cout << 16880;
    for (int i = 0; i < 16880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16881(int& val) {
    func_in_16881(val);
    const int x = 16881;
    std::cout << 16881;
    for (int i = 0; i < 16881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16881(int& val) {
    const int x = 16881;
    std::cout << 16881;
    for (int i = 0; i < 16881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16882(int& val) {
    func_in_16882(val);
    const int x = 16882;
    std::cout << 16882;
    for (int i = 0; i < 16882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16882(int& val) {
    const int x = 16882;
    std::cout << 16882;
    for (int i = 0; i < 16882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16883(int& val) {
    func_in_16883(val);
    const int x = 16883;
    std::cout << 16883;
    for (int i = 0; i < 16883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16883(int& val) {
    const int x = 16883;
    std::cout << 16883;
    for (int i = 0; i < 16883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16884(int& val) {
    func_in_16884(val);
    const int x = 16884;
    std::cout << 16884;
    for (int i = 0; i < 16884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16884(int& val) {
    const int x = 16884;
    std::cout << 16884;
    for (int i = 0; i < 16884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16885(int& val) {
    func_in_16885(val);
    const int x = 16885;
    std::cout << 16885;
    for (int i = 0; i < 16885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16885(int& val) {
    const int x = 16885;
    std::cout << 16885;
    for (int i = 0; i < 16885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16886(int& val) {
    func_in_16886(val);
    const int x = 16886;
    std::cout << 16886;
    for (int i = 0; i < 16886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16886(int& val) {
    const int x = 16886;
    std::cout << 16886;
    for (int i = 0; i < 16886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16887(int& val) {
    func_in_16887(val);
    const int x = 16887;
    std::cout << 16887;
    for (int i = 0; i < 16887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16887(int& val) {
    const int x = 16887;
    std::cout << 16887;
    for (int i = 0; i < 16887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16888(int& val) {
    func_in_16888(val);
    const int x = 16888;
    std::cout << 16888;
    for (int i = 0; i < 16888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16888(int& val) {
    const int x = 16888;
    std::cout << 16888;
    for (int i = 0; i < 16888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16889(int& val) {
    func_in_16889(val);
    const int x = 16889;
    std::cout << 16889;
    for (int i = 0; i < 16889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16889(int& val) {
    const int x = 16889;
    std::cout << 16889;
    for (int i = 0; i < 16889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16890(int& val) {
    func_in_16890(val);
    const int x = 16890;
    std::cout << 16890;
    for (int i = 0; i < 16890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16890(int& val) {
    const int x = 16890;
    std::cout << 16890;
    for (int i = 0; i < 16890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16891(int& val) {
    func_in_16891(val);
    const int x = 16891;
    std::cout << 16891;
    for (int i = 0; i < 16891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16891(int& val) {
    const int x = 16891;
    std::cout << 16891;
    for (int i = 0; i < 16891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16892(int& val) {
    func_in_16892(val);
    const int x = 16892;
    std::cout << 16892;
    for (int i = 0; i < 16892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16892(int& val) {
    const int x = 16892;
    std::cout << 16892;
    for (int i = 0; i < 16892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16893(int& val) {
    func_in_16893(val);
    const int x = 16893;
    std::cout << 16893;
    for (int i = 0; i < 16893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16893(int& val) {
    const int x = 16893;
    std::cout << 16893;
    for (int i = 0; i < 16893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16894(int& val) {
    func_in_16894(val);
    const int x = 16894;
    std::cout << 16894;
    for (int i = 0; i < 16894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16894(int& val) {
    const int x = 16894;
    std::cout << 16894;
    for (int i = 0; i < 16894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16895(int& val) {
    func_in_16895(val);
    const int x = 16895;
    std::cout << 16895;
    for (int i = 0; i < 16895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16895(int& val) {
    const int x = 16895;
    std::cout << 16895;
    for (int i = 0; i < 16895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16896(int& val) {
    func_in_16896(val);
    const int x = 16896;
    std::cout << 16896;
    for (int i = 0; i < 16896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16896(int& val) {
    const int x = 16896;
    std::cout << 16896;
    for (int i = 0; i < 16896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16897(int& val) {
    func_in_16897(val);
    const int x = 16897;
    std::cout << 16897;
    for (int i = 0; i < 16897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16897(int& val) {
    const int x = 16897;
    std::cout << 16897;
    for (int i = 0; i < 16897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16898(int& val) {
    func_in_16898(val);
    const int x = 16898;
    std::cout << 16898;
    for (int i = 0; i < 16898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16898(int& val) {
    const int x = 16898;
    std::cout << 16898;
    for (int i = 0; i < 16898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16899(int& val) {
    func_in_16899(val);
    const int x = 16899;
    std::cout << 16899;
    for (int i = 0; i < 16899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16899(int& val) {
    const int x = 16899;
    std::cout << 16899;
    for (int i = 0; i < 16899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16900(int& val) {
    func_in_16900(val);
    const int x = 16900;
    std::cout << 16900;
    for (int i = 0; i < 16900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16900(int& val) {
    const int x = 16900;
    std::cout << 16900;
    for (int i = 0; i < 16900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16901(int& val) {
    func_in_16901(val);
    const int x = 16901;
    std::cout << 16901;
    for (int i = 0; i < 16901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16901(int& val) {
    const int x = 16901;
    std::cout << 16901;
    for (int i = 0; i < 16901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16902(int& val) {
    func_in_16902(val);
    const int x = 16902;
    std::cout << 16902;
    for (int i = 0; i < 16902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16902(int& val) {
    const int x = 16902;
    std::cout << 16902;
    for (int i = 0; i < 16902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16903(int& val) {
    func_in_16903(val);
    const int x = 16903;
    std::cout << 16903;
    for (int i = 0; i < 16903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16903(int& val) {
    const int x = 16903;
    std::cout << 16903;
    for (int i = 0; i < 16903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16904(int& val) {
    func_in_16904(val);
    const int x = 16904;
    std::cout << 16904;
    for (int i = 0; i < 16904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16904(int& val) {
    const int x = 16904;
    std::cout << 16904;
    for (int i = 0; i < 16904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16905(int& val) {
    func_in_16905(val);
    const int x = 16905;
    std::cout << 16905;
    for (int i = 0; i < 16905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16905(int& val) {
    const int x = 16905;
    std::cout << 16905;
    for (int i = 0; i < 16905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16906(int& val) {
    func_in_16906(val);
    const int x = 16906;
    std::cout << 16906;
    for (int i = 0; i < 16906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16906(int& val) {
    const int x = 16906;
    std::cout << 16906;
    for (int i = 0; i < 16906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16907(int& val) {
    func_in_16907(val);
    const int x = 16907;
    std::cout << 16907;
    for (int i = 0; i < 16907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16907(int& val) {
    const int x = 16907;
    std::cout << 16907;
    for (int i = 0; i < 16907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16908(int& val) {
    func_in_16908(val);
    const int x = 16908;
    std::cout << 16908;
    for (int i = 0; i < 16908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16908(int& val) {
    const int x = 16908;
    std::cout << 16908;
    for (int i = 0; i < 16908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16909(int& val) {
    func_in_16909(val);
    const int x = 16909;
    std::cout << 16909;
    for (int i = 0; i < 16909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16909(int& val) {
    const int x = 16909;
    std::cout << 16909;
    for (int i = 0; i < 16909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16910(int& val) {
    func_in_16910(val);
    const int x = 16910;
    std::cout << 16910;
    for (int i = 0; i < 16910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16910(int& val) {
    const int x = 16910;
    std::cout << 16910;
    for (int i = 0; i < 16910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16911(int& val) {
    func_in_16911(val);
    const int x = 16911;
    std::cout << 16911;
    for (int i = 0; i < 16911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16911(int& val) {
    const int x = 16911;
    std::cout << 16911;
    for (int i = 0; i < 16911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16912(int& val) {
    func_in_16912(val);
    const int x = 16912;
    std::cout << 16912;
    for (int i = 0; i < 16912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16912(int& val) {
    const int x = 16912;
    std::cout << 16912;
    for (int i = 0; i < 16912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16913(int& val) {
    func_in_16913(val);
    const int x = 16913;
    std::cout << 16913;
    for (int i = 0; i < 16913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16913(int& val) {
    const int x = 16913;
    std::cout << 16913;
    for (int i = 0; i < 16913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16914(int& val) {
    func_in_16914(val);
    const int x = 16914;
    std::cout << 16914;
    for (int i = 0; i < 16914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16914(int& val) {
    const int x = 16914;
    std::cout << 16914;
    for (int i = 0; i < 16914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16915(int& val) {
    func_in_16915(val);
    const int x = 16915;
    std::cout << 16915;
    for (int i = 0; i < 16915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16915(int& val) {
    const int x = 16915;
    std::cout << 16915;
    for (int i = 0; i < 16915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16916(int& val) {
    func_in_16916(val);
    const int x = 16916;
    std::cout << 16916;
    for (int i = 0; i < 16916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16916(int& val) {
    const int x = 16916;
    std::cout << 16916;
    for (int i = 0; i < 16916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16917(int& val) {
    func_in_16917(val);
    const int x = 16917;
    std::cout << 16917;
    for (int i = 0; i < 16917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16917(int& val) {
    const int x = 16917;
    std::cout << 16917;
    for (int i = 0; i < 16917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16918(int& val) {
    func_in_16918(val);
    const int x = 16918;
    std::cout << 16918;
    for (int i = 0; i < 16918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16918(int& val) {
    const int x = 16918;
    std::cout << 16918;
    for (int i = 0; i < 16918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16919(int& val) {
    func_in_16919(val);
    const int x = 16919;
    std::cout << 16919;
    for (int i = 0; i < 16919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16919(int& val) {
    const int x = 16919;
    std::cout << 16919;
    for (int i = 0; i < 16919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16920(int& val) {
    func_in_16920(val);
    const int x = 16920;
    std::cout << 16920;
    for (int i = 0; i < 16920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16920(int& val) {
    const int x = 16920;
    std::cout << 16920;
    for (int i = 0; i < 16920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16921(int& val) {
    func_in_16921(val);
    const int x = 16921;
    std::cout << 16921;
    for (int i = 0; i < 16921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16921(int& val) {
    const int x = 16921;
    std::cout << 16921;
    for (int i = 0; i < 16921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16922(int& val) {
    func_in_16922(val);
    const int x = 16922;
    std::cout << 16922;
    for (int i = 0; i < 16922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16922(int& val) {
    const int x = 16922;
    std::cout << 16922;
    for (int i = 0; i < 16922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16923(int& val) {
    func_in_16923(val);
    const int x = 16923;
    std::cout << 16923;
    for (int i = 0; i < 16923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16923(int& val) {
    const int x = 16923;
    std::cout << 16923;
    for (int i = 0; i < 16923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16924(int& val) {
    func_in_16924(val);
    const int x = 16924;
    std::cout << 16924;
    for (int i = 0; i < 16924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16924(int& val) {
    const int x = 16924;
    std::cout << 16924;
    for (int i = 0; i < 16924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16925(int& val) {
    func_in_16925(val);
    const int x = 16925;
    std::cout << 16925;
    for (int i = 0; i < 16925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16925(int& val) {
    const int x = 16925;
    std::cout << 16925;
    for (int i = 0; i < 16925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16926(int& val) {
    func_in_16926(val);
    const int x = 16926;
    std::cout << 16926;
    for (int i = 0; i < 16926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16926(int& val) {
    const int x = 16926;
    std::cout << 16926;
    for (int i = 0; i < 16926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16927(int& val) {
    func_in_16927(val);
    const int x = 16927;
    std::cout << 16927;
    for (int i = 0; i < 16927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16927(int& val) {
    const int x = 16927;
    std::cout << 16927;
    for (int i = 0; i < 16927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16928(int& val) {
    func_in_16928(val);
    const int x = 16928;
    std::cout << 16928;
    for (int i = 0; i < 16928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16928(int& val) {
    const int x = 16928;
    std::cout << 16928;
    for (int i = 0; i < 16928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16929(int& val) {
    func_in_16929(val);
    const int x = 16929;
    std::cout << 16929;
    for (int i = 0; i < 16929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16929(int& val) {
    const int x = 16929;
    std::cout << 16929;
    for (int i = 0; i < 16929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16930(int& val) {
    func_in_16930(val);
    const int x = 16930;
    std::cout << 16930;
    for (int i = 0; i < 16930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16930(int& val) {
    const int x = 16930;
    std::cout << 16930;
    for (int i = 0; i < 16930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16931(int& val) {
    func_in_16931(val);
    const int x = 16931;
    std::cout << 16931;
    for (int i = 0; i < 16931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16931(int& val) {
    const int x = 16931;
    std::cout << 16931;
    for (int i = 0; i < 16931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16932(int& val) {
    func_in_16932(val);
    const int x = 16932;
    std::cout << 16932;
    for (int i = 0; i < 16932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16932(int& val) {
    const int x = 16932;
    std::cout << 16932;
    for (int i = 0; i < 16932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16933(int& val) {
    func_in_16933(val);
    const int x = 16933;
    std::cout << 16933;
    for (int i = 0; i < 16933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16933(int& val) {
    const int x = 16933;
    std::cout << 16933;
    for (int i = 0; i < 16933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16934(int& val) {
    func_in_16934(val);
    const int x = 16934;
    std::cout << 16934;
    for (int i = 0; i < 16934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16934(int& val) {
    const int x = 16934;
    std::cout << 16934;
    for (int i = 0; i < 16934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16935(int& val) {
    func_in_16935(val);
    const int x = 16935;
    std::cout << 16935;
    for (int i = 0; i < 16935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16935(int& val) {
    const int x = 16935;
    std::cout << 16935;
    for (int i = 0; i < 16935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16936(int& val) {
    func_in_16936(val);
    const int x = 16936;
    std::cout << 16936;
    for (int i = 0; i < 16936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16936(int& val) {
    const int x = 16936;
    std::cout << 16936;
    for (int i = 0; i < 16936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16937(int& val) {
    func_in_16937(val);
    const int x = 16937;
    std::cout << 16937;
    for (int i = 0; i < 16937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16937(int& val) {
    const int x = 16937;
    std::cout << 16937;
    for (int i = 0; i < 16937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16938(int& val) {
    func_in_16938(val);
    const int x = 16938;
    std::cout << 16938;
    for (int i = 0; i < 16938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16938(int& val) {
    const int x = 16938;
    std::cout << 16938;
    for (int i = 0; i < 16938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16939(int& val) {
    func_in_16939(val);
    const int x = 16939;
    std::cout << 16939;
    for (int i = 0; i < 16939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16939(int& val) {
    const int x = 16939;
    std::cout << 16939;
    for (int i = 0; i < 16939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16940(int& val) {
    func_in_16940(val);
    const int x = 16940;
    std::cout << 16940;
    for (int i = 0; i < 16940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16940(int& val) {
    const int x = 16940;
    std::cout << 16940;
    for (int i = 0; i < 16940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16941(int& val) {
    func_in_16941(val);
    const int x = 16941;
    std::cout << 16941;
    for (int i = 0; i < 16941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16941(int& val) {
    const int x = 16941;
    std::cout << 16941;
    for (int i = 0; i < 16941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16942(int& val) {
    func_in_16942(val);
    const int x = 16942;
    std::cout << 16942;
    for (int i = 0; i < 16942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16942(int& val) {
    const int x = 16942;
    std::cout << 16942;
    for (int i = 0; i < 16942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16943(int& val) {
    func_in_16943(val);
    const int x = 16943;
    std::cout << 16943;
    for (int i = 0; i < 16943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16943(int& val) {
    const int x = 16943;
    std::cout << 16943;
    for (int i = 0; i < 16943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16944(int& val) {
    func_in_16944(val);
    const int x = 16944;
    std::cout << 16944;
    for (int i = 0; i < 16944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16944(int& val) {
    const int x = 16944;
    std::cout << 16944;
    for (int i = 0; i < 16944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16945(int& val) {
    func_in_16945(val);
    const int x = 16945;
    std::cout << 16945;
    for (int i = 0; i < 16945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16945(int& val) {
    const int x = 16945;
    std::cout << 16945;
    for (int i = 0; i < 16945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16946(int& val) {
    func_in_16946(val);
    const int x = 16946;
    std::cout << 16946;
    for (int i = 0; i < 16946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16946(int& val) {
    const int x = 16946;
    std::cout << 16946;
    for (int i = 0; i < 16946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16947(int& val) {
    func_in_16947(val);
    const int x = 16947;
    std::cout << 16947;
    for (int i = 0; i < 16947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16947(int& val) {
    const int x = 16947;
    std::cout << 16947;
    for (int i = 0; i < 16947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16948(int& val) {
    func_in_16948(val);
    const int x = 16948;
    std::cout << 16948;
    for (int i = 0; i < 16948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16948(int& val) {
    const int x = 16948;
    std::cout << 16948;
    for (int i = 0; i < 16948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16949(int& val) {
    func_in_16949(val);
    const int x = 16949;
    std::cout << 16949;
    for (int i = 0; i < 16949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16949(int& val) {
    const int x = 16949;
    std::cout << 16949;
    for (int i = 0; i < 16949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16950(int& val) {
    func_in_16950(val);
    const int x = 16950;
    std::cout << 16950;
    for (int i = 0; i < 16950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16950(int& val) {
    const int x = 16950;
    std::cout << 16950;
    for (int i = 0; i < 16950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16951(int& val) {
    func_in_16951(val);
    const int x = 16951;
    std::cout << 16951;
    for (int i = 0; i < 16951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16951(int& val) {
    const int x = 16951;
    std::cout << 16951;
    for (int i = 0; i < 16951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16952(int& val) {
    func_in_16952(val);
    const int x = 16952;
    std::cout << 16952;
    for (int i = 0; i < 16952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16952(int& val) {
    const int x = 16952;
    std::cout << 16952;
    for (int i = 0; i < 16952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16953(int& val) {
    func_in_16953(val);
    const int x = 16953;
    std::cout << 16953;
    for (int i = 0; i < 16953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16953(int& val) {
    const int x = 16953;
    std::cout << 16953;
    for (int i = 0; i < 16953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16954(int& val) {
    func_in_16954(val);
    const int x = 16954;
    std::cout << 16954;
    for (int i = 0; i < 16954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16954(int& val) {
    const int x = 16954;
    std::cout << 16954;
    for (int i = 0; i < 16954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16955(int& val) {
    func_in_16955(val);
    const int x = 16955;
    std::cout << 16955;
    for (int i = 0; i < 16955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16955(int& val) {
    const int x = 16955;
    std::cout << 16955;
    for (int i = 0; i < 16955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16956(int& val) {
    func_in_16956(val);
    const int x = 16956;
    std::cout << 16956;
    for (int i = 0; i < 16956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16956(int& val) {
    const int x = 16956;
    std::cout << 16956;
    for (int i = 0; i < 16956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16957(int& val) {
    func_in_16957(val);
    const int x = 16957;
    std::cout << 16957;
    for (int i = 0; i < 16957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16957(int& val) {
    const int x = 16957;
    std::cout << 16957;
    for (int i = 0; i < 16957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16958(int& val) {
    func_in_16958(val);
    const int x = 16958;
    std::cout << 16958;
    for (int i = 0; i < 16958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16958(int& val) {
    const int x = 16958;
    std::cout << 16958;
    for (int i = 0; i < 16958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16959(int& val) {
    func_in_16959(val);
    const int x = 16959;
    std::cout << 16959;
    for (int i = 0; i < 16959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16959(int& val) {
    const int x = 16959;
    std::cout << 16959;
    for (int i = 0; i < 16959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16960(int& val) {
    func_in_16960(val);
    const int x = 16960;
    std::cout << 16960;
    for (int i = 0; i < 16960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16960(int& val) {
    const int x = 16960;
    std::cout << 16960;
    for (int i = 0; i < 16960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16961(int& val) {
    func_in_16961(val);
    const int x = 16961;
    std::cout << 16961;
    for (int i = 0; i < 16961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16961(int& val) {
    const int x = 16961;
    std::cout << 16961;
    for (int i = 0; i < 16961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16962(int& val) {
    func_in_16962(val);
    const int x = 16962;
    std::cout << 16962;
    for (int i = 0; i < 16962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16962(int& val) {
    const int x = 16962;
    std::cout << 16962;
    for (int i = 0; i < 16962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16963(int& val) {
    func_in_16963(val);
    const int x = 16963;
    std::cout << 16963;
    for (int i = 0; i < 16963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16963(int& val) {
    const int x = 16963;
    std::cout << 16963;
    for (int i = 0; i < 16963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16964(int& val) {
    func_in_16964(val);
    const int x = 16964;
    std::cout << 16964;
    for (int i = 0; i < 16964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16964(int& val) {
    const int x = 16964;
    std::cout << 16964;
    for (int i = 0; i < 16964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16965(int& val) {
    func_in_16965(val);
    const int x = 16965;
    std::cout << 16965;
    for (int i = 0; i < 16965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16965(int& val) {
    const int x = 16965;
    std::cout << 16965;
    for (int i = 0; i < 16965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16966(int& val) {
    func_in_16966(val);
    const int x = 16966;
    std::cout << 16966;
    for (int i = 0; i < 16966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16966(int& val) {
    const int x = 16966;
    std::cout << 16966;
    for (int i = 0; i < 16966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16967(int& val) {
    func_in_16967(val);
    const int x = 16967;
    std::cout << 16967;
    for (int i = 0; i < 16967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16967(int& val) {
    const int x = 16967;
    std::cout << 16967;
    for (int i = 0; i < 16967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16968(int& val) {
    func_in_16968(val);
    const int x = 16968;
    std::cout << 16968;
    for (int i = 0; i < 16968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16968(int& val) {
    const int x = 16968;
    std::cout << 16968;
    for (int i = 0; i < 16968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16969(int& val) {
    func_in_16969(val);
    const int x = 16969;
    std::cout << 16969;
    for (int i = 0; i < 16969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16969(int& val) {
    const int x = 16969;
    std::cout << 16969;
    for (int i = 0; i < 16969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16970(int& val) {
    func_in_16970(val);
    const int x = 16970;
    std::cout << 16970;
    for (int i = 0; i < 16970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16970(int& val) {
    const int x = 16970;
    std::cout << 16970;
    for (int i = 0; i < 16970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16971(int& val) {
    func_in_16971(val);
    const int x = 16971;
    std::cout << 16971;
    for (int i = 0; i < 16971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16971(int& val) {
    const int x = 16971;
    std::cout << 16971;
    for (int i = 0; i < 16971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16972(int& val) {
    func_in_16972(val);
    const int x = 16972;
    std::cout << 16972;
    for (int i = 0; i < 16972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16972(int& val) {
    const int x = 16972;
    std::cout << 16972;
    for (int i = 0; i < 16972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16973(int& val) {
    func_in_16973(val);
    const int x = 16973;
    std::cout << 16973;
    for (int i = 0; i < 16973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16973(int& val) {
    const int x = 16973;
    std::cout << 16973;
    for (int i = 0; i < 16973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16974(int& val) {
    func_in_16974(val);
    const int x = 16974;
    std::cout << 16974;
    for (int i = 0; i < 16974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16974(int& val) {
    const int x = 16974;
    std::cout << 16974;
    for (int i = 0; i < 16974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16975(int& val) {
    func_in_16975(val);
    const int x = 16975;
    std::cout << 16975;
    for (int i = 0; i < 16975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16975(int& val) {
    const int x = 16975;
    std::cout << 16975;
    for (int i = 0; i < 16975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16976(int& val) {
    func_in_16976(val);
    const int x = 16976;
    std::cout << 16976;
    for (int i = 0; i < 16976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16976(int& val) {
    const int x = 16976;
    std::cout << 16976;
    for (int i = 0; i < 16976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16977(int& val) {
    func_in_16977(val);
    const int x = 16977;
    std::cout << 16977;
    for (int i = 0; i < 16977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16977(int& val) {
    const int x = 16977;
    std::cout << 16977;
    for (int i = 0; i < 16977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16978(int& val) {
    func_in_16978(val);
    const int x = 16978;
    std::cout << 16978;
    for (int i = 0; i < 16978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16978(int& val) {
    const int x = 16978;
    std::cout << 16978;
    for (int i = 0; i < 16978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16979(int& val) {
    func_in_16979(val);
    const int x = 16979;
    std::cout << 16979;
    for (int i = 0; i < 16979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16979(int& val) {
    const int x = 16979;
    std::cout << 16979;
    for (int i = 0; i < 16979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16980(int& val) {
    func_in_16980(val);
    const int x = 16980;
    std::cout << 16980;
    for (int i = 0; i < 16980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16980(int& val) {
    const int x = 16980;
    std::cout << 16980;
    for (int i = 0; i < 16980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16981(int& val) {
    func_in_16981(val);
    const int x = 16981;
    std::cout << 16981;
    for (int i = 0; i < 16981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16981(int& val) {
    const int x = 16981;
    std::cout << 16981;
    for (int i = 0; i < 16981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16982(int& val) {
    func_in_16982(val);
    const int x = 16982;
    std::cout << 16982;
    for (int i = 0; i < 16982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16982(int& val) {
    const int x = 16982;
    std::cout << 16982;
    for (int i = 0; i < 16982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16983(int& val) {
    func_in_16983(val);
    const int x = 16983;
    std::cout << 16983;
    for (int i = 0; i < 16983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16983(int& val) {
    const int x = 16983;
    std::cout << 16983;
    for (int i = 0; i < 16983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16984(int& val) {
    func_in_16984(val);
    const int x = 16984;
    std::cout << 16984;
    for (int i = 0; i < 16984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16984(int& val) {
    const int x = 16984;
    std::cout << 16984;
    for (int i = 0; i < 16984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16985(int& val) {
    func_in_16985(val);
    const int x = 16985;
    std::cout << 16985;
    for (int i = 0; i < 16985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16985(int& val) {
    const int x = 16985;
    std::cout << 16985;
    for (int i = 0; i < 16985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16986(int& val) {
    func_in_16986(val);
    const int x = 16986;
    std::cout << 16986;
    for (int i = 0; i < 16986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16986(int& val) {
    const int x = 16986;
    std::cout << 16986;
    for (int i = 0; i < 16986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16987(int& val) {
    func_in_16987(val);
    const int x = 16987;
    std::cout << 16987;
    for (int i = 0; i < 16987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16987(int& val) {
    const int x = 16987;
    std::cout << 16987;
    for (int i = 0; i < 16987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16988(int& val) {
    func_in_16988(val);
    const int x = 16988;
    std::cout << 16988;
    for (int i = 0; i < 16988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16988(int& val) {
    const int x = 16988;
    std::cout << 16988;
    for (int i = 0; i < 16988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16989(int& val) {
    func_in_16989(val);
    const int x = 16989;
    std::cout << 16989;
    for (int i = 0; i < 16989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16989(int& val) {
    const int x = 16989;
    std::cout << 16989;
    for (int i = 0; i < 16989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16990(int& val) {
    func_in_16990(val);
    const int x = 16990;
    std::cout << 16990;
    for (int i = 0; i < 16990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16990(int& val) {
    const int x = 16990;
    std::cout << 16990;
    for (int i = 0; i < 16990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16991(int& val) {
    func_in_16991(val);
    const int x = 16991;
    std::cout << 16991;
    for (int i = 0; i < 16991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16991(int& val) {
    const int x = 16991;
    std::cout << 16991;
    for (int i = 0; i < 16991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16992(int& val) {
    func_in_16992(val);
    const int x = 16992;
    std::cout << 16992;
    for (int i = 0; i < 16992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16992(int& val) {
    const int x = 16992;
    std::cout << 16992;
    for (int i = 0; i < 16992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16993(int& val) {
    func_in_16993(val);
    const int x = 16993;
    std::cout << 16993;
    for (int i = 0; i < 16993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16993(int& val) {
    const int x = 16993;
    std::cout << 16993;
    for (int i = 0; i < 16993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16994(int& val) {
    func_in_16994(val);
    const int x = 16994;
    std::cout << 16994;
    for (int i = 0; i < 16994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16994(int& val) {
    const int x = 16994;
    std::cout << 16994;
    for (int i = 0; i < 16994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16995(int& val) {
    func_in_16995(val);
    const int x = 16995;
    std::cout << 16995;
    for (int i = 0; i < 16995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16995(int& val) {
    const int x = 16995;
    std::cout << 16995;
    for (int i = 0; i < 16995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16996(int& val) {
    func_in_16996(val);
    const int x = 16996;
    std::cout << 16996;
    for (int i = 0; i < 16996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16996(int& val) {
    const int x = 16996;
    std::cout << 16996;
    for (int i = 0; i < 16996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16997(int& val) {
    func_in_16997(val);
    const int x = 16997;
    std::cout << 16997;
    for (int i = 0; i < 16997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16997(int& val) {
    const int x = 16997;
    std::cout << 16997;
    for (int i = 0; i < 16997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16998(int& val) {
    func_in_16998(val);
    const int x = 16998;
    std::cout << 16998;
    for (int i = 0; i < 16998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16998(int& val) {
    const int x = 16998;
    std::cout << 16998;
    for (int i = 0; i < 16998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16999(int& val) {
    func_in_16999(val);
    const int x = 16999;
    std::cout << 16999;
    for (int i = 0; i < 16999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16999(int& val) {
    const int x = 16999;
    std::cout << 16999;
    for (int i = 0; i < 16999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17000(int& val) {
    func_in_17000(val);
    const int x = 17000;
    std::cout << 17000;
    for (int i = 0; i < 17000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17000(int& val) {
    const int x = 17000;
    std::cout << 17000;
    for (int i = 0; i < 17000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17001(int& val) {
    func_in_17001(val);
    const int x = 17001;
    std::cout << 17001;
    for (int i = 0; i < 17001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17001(int& val) {
    const int x = 17001;
    std::cout << 17001;
    for (int i = 0; i < 17001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17002(int& val) {
    func_in_17002(val);
    const int x = 17002;
    std::cout << 17002;
    for (int i = 0; i < 17002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17002(int& val) {
    const int x = 17002;
    std::cout << 17002;
    for (int i = 0; i < 17002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17003(int& val) {
    func_in_17003(val);
    const int x = 17003;
    std::cout << 17003;
    for (int i = 0; i < 17003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17003(int& val) {
    const int x = 17003;
    std::cout << 17003;
    for (int i = 0; i < 17003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17004(int& val) {
    func_in_17004(val);
    const int x = 17004;
    std::cout << 17004;
    for (int i = 0; i < 17004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17004(int& val) {
    const int x = 17004;
    std::cout << 17004;
    for (int i = 0; i < 17004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17005(int& val) {
    func_in_17005(val);
    const int x = 17005;
    std::cout << 17005;
    for (int i = 0; i < 17005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17005(int& val) {
    const int x = 17005;
    std::cout << 17005;
    for (int i = 0; i < 17005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17006(int& val) {
    func_in_17006(val);
    const int x = 17006;
    std::cout << 17006;
    for (int i = 0; i < 17006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17006(int& val) {
    const int x = 17006;
    std::cout << 17006;
    for (int i = 0; i < 17006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17007(int& val) {
    func_in_17007(val);
    const int x = 17007;
    std::cout << 17007;
    for (int i = 0; i < 17007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17007(int& val) {
    const int x = 17007;
    std::cout << 17007;
    for (int i = 0; i < 17007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17008(int& val) {
    func_in_17008(val);
    const int x = 17008;
    std::cout << 17008;
    for (int i = 0; i < 17008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17008(int& val) {
    const int x = 17008;
    std::cout << 17008;
    for (int i = 0; i < 17008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17009(int& val) {
    func_in_17009(val);
    const int x = 17009;
    std::cout << 17009;
    for (int i = 0; i < 17009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17009(int& val) {
    const int x = 17009;
    std::cout << 17009;
    for (int i = 0; i < 17009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17010(int& val) {
    func_in_17010(val);
    const int x = 17010;
    std::cout << 17010;
    for (int i = 0; i < 17010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17010(int& val) {
    const int x = 17010;
    std::cout << 17010;
    for (int i = 0; i < 17010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17011(int& val) {
    func_in_17011(val);
    const int x = 17011;
    std::cout << 17011;
    for (int i = 0; i < 17011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17011(int& val) {
    const int x = 17011;
    std::cout << 17011;
    for (int i = 0; i < 17011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17012(int& val) {
    func_in_17012(val);
    const int x = 17012;
    std::cout << 17012;
    for (int i = 0; i < 17012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17012(int& val) {
    const int x = 17012;
    std::cout << 17012;
    for (int i = 0; i < 17012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17013(int& val) {
    func_in_17013(val);
    const int x = 17013;
    std::cout << 17013;
    for (int i = 0; i < 17013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17013(int& val) {
    const int x = 17013;
    std::cout << 17013;
    for (int i = 0; i < 17013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17014(int& val) {
    func_in_17014(val);
    const int x = 17014;
    std::cout << 17014;
    for (int i = 0; i < 17014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17014(int& val) {
    const int x = 17014;
    std::cout << 17014;
    for (int i = 0; i < 17014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17015(int& val) {
    func_in_17015(val);
    const int x = 17015;
    std::cout << 17015;
    for (int i = 0; i < 17015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17015(int& val) {
    const int x = 17015;
    std::cout << 17015;
    for (int i = 0; i < 17015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17016(int& val) {
    func_in_17016(val);
    const int x = 17016;
    std::cout << 17016;
    for (int i = 0; i < 17016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17016(int& val) {
    const int x = 17016;
    std::cout << 17016;
    for (int i = 0; i < 17016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17017(int& val) {
    func_in_17017(val);
    const int x = 17017;
    std::cout << 17017;
    for (int i = 0; i < 17017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17017(int& val) {
    const int x = 17017;
    std::cout << 17017;
    for (int i = 0; i < 17017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17018(int& val) {
    func_in_17018(val);
    const int x = 17018;
    std::cout << 17018;
    for (int i = 0; i < 17018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17018(int& val) {
    const int x = 17018;
    std::cout << 17018;
    for (int i = 0; i < 17018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17019(int& val) {
    func_in_17019(val);
    const int x = 17019;
    std::cout << 17019;
    for (int i = 0; i < 17019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17019(int& val) {
    const int x = 17019;
    std::cout << 17019;
    for (int i = 0; i < 17019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17020(int& val) {
    func_in_17020(val);
    const int x = 17020;
    std::cout << 17020;
    for (int i = 0; i < 17020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17020(int& val) {
    const int x = 17020;
    std::cout << 17020;
    for (int i = 0; i < 17020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17021(int& val) {
    func_in_17021(val);
    const int x = 17021;
    std::cout << 17021;
    for (int i = 0; i < 17021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17021(int& val) {
    const int x = 17021;
    std::cout << 17021;
    for (int i = 0; i < 17021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17022(int& val) {
    func_in_17022(val);
    const int x = 17022;
    std::cout << 17022;
    for (int i = 0; i < 17022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17022(int& val) {
    const int x = 17022;
    std::cout << 17022;
    for (int i = 0; i < 17022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17023(int& val) {
    func_in_17023(val);
    const int x = 17023;
    std::cout << 17023;
    for (int i = 0; i < 17023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17023(int& val) {
    const int x = 17023;
    std::cout << 17023;
    for (int i = 0; i < 17023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17024(int& val) {
    func_in_17024(val);
    const int x = 17024;
    std::cout << 17024;
    for (int i = 0; i < 17024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17024(int& val) {
    const int x = 17024;
    std::cout << 17024;
    for (int i = 0; i < 17024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17025(int& val) {
    func_in_17025(val);
    const int x = 17025;
    std::cout << 17025;
    for (int i = 0; i < 17025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17025(int& val) {
    const int x = 17025;
    std::cout << 17025;
    for (int i = 0; i < 17025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17026(int& val) {
    func_in_17026(val);
    const int x = 17026;
    std::cout << 17026;
    for (int i = 0; i < 17026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17026(int& val) {
    const int x = 17026;
    std::cout << 17026;
    for (int i = 0; i < 17026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17027(int& val) {
    func_in_17027(val);
    const int x = 17027;
    std::cout << 17027;
    for (int i = 0; i < 17027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17027(int& val) {
    const int x = 17027;
    std::cout << 17027;
    for (int i = 0; i < 17027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17028(int& val) {
    func_in_17028(val);
    const int x = 17028;
    std::cout << 17028;
    for (int i = 0; i < 17028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17028(int& val) {
    const int x = 17028;
    std::cout << 17028;
    for (int i = 0; i < 17028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17029(int& val) {
    func_in_17029(val);
    const int x = 17029;
    std::cout << 17029;
    for (int i = 0; i < 17029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17029(int& val) {
    const int x = 17029;
    std::cout << 17029;
    for (int i = 0; i < 17029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17030(int& val) {
    func_in_17030(val);
    const int x = 17030;
    std::cout << 17030;
    for (int i = 0; i < 17030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17030(int& val) {
    const int x = 17030;
    std::cout << 17030;
    for (int i = 0; i < 17030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17031(int& val) {
    func_in_17031(val);
    const int x = 17031;
    std::cout << 17031;
    for (int i = 0; i < 17031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17031(int& val) {
    const int x = 17031;
    std::cout << 17031;
    for (int i = 0; i < 17031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17032(int& val) {
    func_in_17032(val);
    const int x = 17032;
    std::cout << 17032;
    for (int i = 0; i < 17032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17032(int& val) {
    const int x = 17032;
    std::cout << 17032;
    for (int i = 0; i < 17032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17033(int& val) {
    func_in_17033(val);
    const int x = 17033;
    std::cout << 17033;
    for (int i = 0; i < 17033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17033(int& val) {
    const int x = 17033;
    std::cout << 17033;
    for (int i = 0; i < 17033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17034(int& val) {
    func_in_17034(val);
    const int x = 17034;
    std::cout << 17034;
    for (int i = 0; i < 17034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17034(int& val) {
    const int x = 17034;
    std::cout << 17034;
    for (int i = 0; i < 17034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17035(int& val) {
    func_in_17035(val);
    const int x = 17035;
    std::cout << 17035;
    for (int i = 0; i < 17035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17035(int& val) {
    const int x = 17035;
    std::cout << 17035;
    for (int i = 0; i < 17035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17036(int& val) {
    func_in_17036(val);
    const int x = 17036;
    std::cout << 17036;
    for (int i = 0; i < 17036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17036(int& val) {
    const int x = 17036;
    std::cout << 17036;
    for (int i = 0; i < 17036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17037(int& val) {
    func_in_17037(val);
    const int x = 17037;
    std::cout << 17037;
    for (int i = 0; i < 17037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17037(int& val) {
    const int x = 17037;
    std::cout << 17037;
    for (int i = 0; i < 17037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17038(int& val) {
    func_in_17038(val);
    const int x = 17038;
    std::cout << 17038;
    for (int i = 0; i < 17038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17038(int& val) {
    const int x = 17038;
    std::cout << 17038;
    for (int i = 0; i < 17038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17039(int& val) {
    func_in_17039(val);
    const int x = 17039;
    std::cout << 17039;
    for (int i = 0; i < 17039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17039(int& val) {
    const int x = 17039;
    std::cout << 17039;
    for (int i = 0; i < 17039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17040(int& val) {
    func_in_17040(val);
    const int x = 17040;
    std::cout << 17040;
    for (int i = 0; i < 17040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17040(int& val) {
    const int x = 17040;
    std::cout << 17040;
    for (int i = 0; i < 17040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17041(int& val) {
    func_in_17041(val);
    const int x = 17041;
    std::cout << 17041;
    for (int i = 0; i < 17041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17041(int& val) {
    const int x = 17041;
    std::cout << 17041;
    for (int i = 0; i < 17041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17042(int& val) {
    func_in_17042(val);
    const int x = 17042;
    std::cout << 17042;
    for (int i = 0; i < 17042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17042(int& val) {
    const int x = 17042;
    std::cout << 17042;
    for (int i = 0; i < 17042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17043(int& val) {
    func_in_17043(val);
    const int x = 17043;
    std::cout << 17043;
    for (int i = 0; i < 17043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17043(int& val) {
    const int x = 17043;
    std::cout << 17043;
    for (int i = 0; i < 17043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17044(int& val) {
    func_in_17044(val);
    const int x = 17044;
    std::cout << 17044;
    for (int i = 0; i < 17044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17044(int& val) {
    const int x = 17044;
    std::cout << 17044;
    for (int i = 0; i < 17044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17045(int& val) {
    func_in_17045(val);
    const int x = 17045;
    std::cout << 17045;
    for (int i = 0; i < 17045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17045(int& val) {
    const int x = 17045;
    std::cout << 17045;
    for (int i = 0; i < 17045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17046(int& val) {
    func_in_17046(val);
    const int x = 17046;
    std::cout << 17046;
    for (int i = 0; i < 17046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17046(int& val) {
    const int x = 17046;
    std::cout << 17046;
    for (int i = 0; i < 17046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17047(int& val) {
    func_in_17047(val);
    const int x = 17047;
    std::cout << 17047;
    for (int i = 0; i < 17047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17047(int& val) {
    const int x = 17047;
    std::cout << 17047;
    for (int i = 0; i < 17047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17048(int& val) {
    func_in_17048(val);
    const int x = 17048;
    std::cout << 17048;
    for (int i = 0; i < 17048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17048(int& val) {
    const int x = 17048;
    std::cout << 17048;
    for (int i = 0; i < 17048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17049(int& val) {
    func_in_17049(val);
    const int x = 17049;
    std::cout << 17049;
    for (int i = 0; i < 17049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17049(int& val) {
    const int x = 17049;
    std::cout << 17049;
    for (int i = 0; i < 17049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17050(int& val) {
    func_in_17050(val);
    const int x = 17050;
    std::cout << 17050;
    for (int i = 0; i < 17050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17050(int& val) {
    const int x = 17050;
    std::cout << 17050;
    for (int i = 0; i < 17050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17051(int& val) {
    func_in_17051(val);
    const int x = 17051;
    std::cout << 17051;
    for (int i = 0; i < 17051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17051(int& val) {
    const int x = 17051;
    std::cout << 17051;
    for (int i = 0; i < 17051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17052(int& val) {
    func_in_17052(val);
    const int x = 17052;
    std::cout << 17052;
    for (int i = 0; i < 17052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17052(int& val) {
    const int x = 17052;
    std::cout << 17052;
    for (int i = 0; i < 17052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17053(int& val) {
    func_in_17053(val);
    const int x = 17053;
    std::cout << 17053;
    for (int i = 0; i < 17053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17053(int& val) {
    const int x = 17053;
    std::cout << 17053;
    for (int i = 0; i < 17053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17054(int& val) {
    func_in_17054(val);
    const int x = 17054;
    std::cout << 17054;
    for (int i = 0; i < 17054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17054(int& val) {
    const int x = 17054;
    std::cout << 17054;
    for (int i = 0; i < 17054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17055(int& val) {
    func_in_17055(val);
    const int x = 17055;
    std::cout << 17055;
    for (int i = 0; i < 17055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17055(int& val) {
    const int x = 17055;
    std::cout << 17055;
    for (int i = 0; i < 17055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17056(int& val) {
    func_in_17056(val);
    const int x = 17056;
    std::cout << 17056;
    for (int i = 0; i < 17056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17056(int& val) {
    const int x = 17056;
    std::cout << 17056;
    for (int i = 0; i < 17056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17057(int& val) {
    func_in_17057(val);
    const int x = 17057;
    std::cout << 17057;
    for (int i = 0; i < 17057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17057(int& val) {
    const int x = 17057;
    std::cout << 17057;
    for (int i = 0; i < 17057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17058(int& val) {
    func_in_17058(val);
    const int x = 17058;
    std::cout << 17058;
    for (int i = 0; i < 17058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17058(int& val) {
    const int x = 17058;
    std::cout << 17058;
    for (int i = 0; i < 17058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17059(int& val) {
    func_in_17059(val);
    const int x = 17059;
    std::cout << 17059;
    for (int i = 0; i < 17059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17059(int& val) {
    const int x = 17059;
    std::cout << 17059;
    for (int i = 0; i < 17059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17060(int& val) {
    func_in_17060(val);
    const int x = 17060;
    std::cout << 17060;
    for (int i = 0; i < 17060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17060(int& val) {
    const int x = 17060;
    std::cout << 17060;
    for (int i = 0; i < 17060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17061(int& val) {
    func_in_17061(val);
    const int x = 17061;
    std::cout << 17061;
    for (int i = 0; i < 17061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17061(int& val) {
    const int x = 17061;
    std::cout << 17061;
    for (int i = 0; i < 17061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17062(int& val) {
    func_in_17062(val);
    const int x = 17062;
    std::cout << 17062;
    for (int i = 0; i < 17062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17062(int& val) {
    const int x = 17062;
    std::cout << 17062;
    for (int i = 0; i < 17062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17063(int& val) {
    func_in_17063(val);
    const int x = 17063;
    std::cout << 17063;
    for (int i = 0; i < 17063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17063(int& val) {
    const int x = 17063;
    std::cout << 17063;
    for (int i = 0; i < 17063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17064(int& val) {
    func_in_17064(val);
    const int x = 17064;
    std::cout << 17064;
    for (int i = 0; i < 17064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17064(int& val) {
    const int x = 17064;
    std::cout << 17064;
    for (int i = 0; i < 17064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17065(int& val) {
    func_in_17065(val);
    const int x = 17065;
    std::cout << 17065;
    for (int i = 0; i < 17065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17065(int& val) {
    const int x = 17065;
    std::cout << 17065;
    for (int i = 0; i < 17065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17066(int& val) {
    func_in_17066(val);
    const int x = 17066;
    std::cout << 17066;
    for (int i = 0; i < 17066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17066(int& val) {
    const int x = 17066;
    std::cout << 17066;
    for (int i = 0; i < 17066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17067(int& val) {
    func_in_17067(val);
    const int x = 17067;
    std::cout << 17067;
    for (int i = 0; i < 17067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17067(int& val) {
    const int x = 17067;
    std::cout << 17067;
    for (int i = 0; i < 17067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17068(int& val) {
    func_in_17068(val);
    const int x = 17068;
    std::cout << 17068;
    for (int i = 0; i < 17068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17068(int& val) {
    const int x = 17068;
    std::cout << 17068;
    for (int i = 0; i < 17068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17069(int& val) {
    func_in_17069(val);
    const int x = 17069;
    std::cout << 17069;
    for (int i = 0; i < 17069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17069(int& val) {
    const int x = 17069;
    std::cout << 17069;
    for (int i = 0; i < 17069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17070(int& val) {
    func_in_17070(val);
    const int x = 17070;
    std::cout << 17070;
    for (int i = 0; i < 17070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17070(int& val) {
    const int x = 17070;
    std::cout << 17070;
    for (int i = 0; i < 17070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17071(int& val) {
    func_in_17071(val);
    const int x = 17071;
    std::cout << 17071;
    for (int i = 0; i < 17071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17071(int& val) {
    const int x = 17071;
    std::cout << 17071;
    for (int i = 0; i < 17071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17072(int& val) {
    func_in_17072(val);
    const int x = 17072;
    std::cout << 17072;
    for (int i = 0; i < 17072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17072(int& val) {
    const int x = 17072;
    std::cout << 17072;
    for (int i = 0; i < 17072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17073(int& val) {
    func_in_17073(val);
    const int x = 17073;
    std::cout << 17073;
    for (int i = 0; i < 17073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17073(int& val) {
    const int x = 17073;
    std::cout << 17073;
    for (int i = 0; i < 17073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17074(int& val) {
    func_in_17074(val);
    const int x = 17074;
    std::cout << 17074;
    for (int i = 0; i < 17074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17074(int& val) {
    const int x = 17074;
    std::cout << 17074;
    for (int i = 0; i < 17074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17075(int& val) {
    func_in_17075(val);
    const int x = 17075;
    std::cout << 17075;
    for (int i = 0; i < 17075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17075(int& val) {
    const int x = 17075;
    std::cout << 17075;
    for (int i = 0; i < 17075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17076(int& val) {
    func_in_17076(val);
    const int x = 17076;
    std::cout << 17076;
    for (int i = 0; i < 17076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17076(int& val) {
    const int x = 17076;
    std::cout << 17076;
    for (int i = 0; i < 17076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17077(int& val) {
    func_in_17077(val);
    const int x = 17077;
    std::cout << 17077;
    for (int i = 0; i < 17077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17077(int& val) {
    const int x = 17077;
    std::cout << 17077;
    for (int i = 0; i < 17077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17078(int& val) {
    func_in_17078(val);
    const int x = 17078;
    std::cout << 17078;
    for (int i = 0; i < 17078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17078(int& val) {
    const int x = 17078;
    std::cout << 17078;
    for (int i = 0; i < 17078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17079(int& val) {
    func_in_17079(val);
    const int x = 17079;
    std::cout << 17079;
    for (int i = 0; i < 17079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17079(int& val) {
    const int x = 17079;
    std::cout << 17079;
    for (int i = 0; i < 17079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17080(int& val) {
    func_in_17080(val);
    const int x = 17080;
    std::cout << 17080;
    for (int i = 0; i < 17080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17080(int& val) {
    const int x = 17080;
    std::cout << 17080;
    for (int i = 0; i < 17080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17081(int& val) {
    func_in_17081(val);
    const int x = 17081;
    std::cout << 17081;
    for (int i = 0; i < 17081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17081(int& val) {
    const int x = 17081;
    std::cout << 17081;
    for (int i = 0; i < 17081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17082(int& val) {
    func_in_17082(val);
    const int x = 17082;
    std::cout << 17082;
    for (int i = 0; i < 17082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17082(int& val) {
    const int x = 17082;
    std::cout << 17082;
    for (int i = 0; i < 17082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17083(int& val) {
    func_in_17083(val);
    const int x = 17083;
    std::cout << 17083;
    for (int i = 0; i < 17083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17083(int& val) {
    const int x = 17083;
    std::cout << 17083;
    for (int i = 0; i < 17083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17084(int& val) {
    func_in_17084(val);
    const int x = 17084;
    std::cout << 17084;
    for (int i = 0; i < 17084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17084(int& val) {
    const int x = 17084;
    std::cout << 17084;
    for (int i = 0; i < 17084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17085(int& val) {
    func_in_17085(val);
    const int x = 17085;
    std::cout << 17085;
    for (int i = 0; i < 17085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17085(int& val) {
    const int x = 17085;
    std::cout << 17085;
    for (int i = 0; i < 17085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17086(int& val) {
    func_in_17086(val);
    const int x = 17086;
    std::cout << 17086;
    for (int i = 0; i < 17086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17086(int& val) {
    const int x = 17086;
    std::cout << 17086;
    for (int i = 0; i < 17086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17087(int& val) {
    func_in_17087(val);
    const int x = 17087;
    std::cout << 17087;
    for (int i = 0; i < 17087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17087(int& val) {
    const int x = 17087;
    std::cout << 17087;
    for (int i = 0; i < 17087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17088(int& val) {
    func_in_17088(val);
    const int x = 17088;
    std::cout << 17088;
    for (int i = 0; i < 17088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17088(int& val) {
    const int x = 17088;
    std::cout << 17088;
    for (int i = 0; i < 17088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17089(int& val) {
    func_in_17089(val);
    const int x = 17089;
    std::cout << 17089;
    for (int i = 0; i < 17089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17089(int& val) {
    const int x = 17089;
    std::cout << 17089;
    for (int i = 0; i < 17089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17090(int& val) {
    func_in_17090(val);
    const int x = 17090;
    std::cout << 17090;
    for (int i = 0; i < 17090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17090(int& val) {
    const int x = 17090;
    std::cout << 17090;
    for (int i = 0; i < 17090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17091(int& val) {
    func_in_17091(val);
    const int x = 17091;
    std::cout << 17091;
    for (int i = 0; i < 17091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17091(int& val) {
    const int x = 17091;
    std::cout << 17091;
    for (int i = 0; i < 17091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17092(int& val) {
    func_in_17092(val);
    const int x = 17092;
    std::cout << 17092;
    for (int i = 0; i < 17092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17092(int& val) {
    const int x = 17092;
    std::cout << 17092;
    for (int i = 0; i < 17092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17093(int& val) {
    func_in_17093(val);
    const int x = 17093;
    std::cout << 17093;
    for (int i = 0; i < 17093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17093(int& val) {
    const int x = 17093;
    std::cout << 17093;
    for (int i = 0; i < 17093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17094(int& val) {
    func_in_17094(val);
    const int x = 17094;
    std::cout << 17094;
    for (int i = 0; i < 17094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17094(int& val) {
    const int x = 17094;
    std::cout << 17094;
    for (int i = 0; i < 17094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17095(int& val) {
    func_in_17095(val);
    const int x = 17095;
    std::cout << 17095;
    for (int i = 0; i < 17095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17095(int& val) {
    const int x = 17095;
    std::cout << 17095;
    for (int i = 0; i < 17095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17096(int& val) {
    func_in_17096(val);
    const int x = 17096;
    std::cout << 17096;
    for (int i = 0; i < 17096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17096(int& val) {
    const int x = 17096;
    std::cout << 17096;
    for (int i = 0; i < 17096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17097(int& val) {
    func_in_17097(val);
    const int x = 17097;
    std::cout << 17097;
    for (int i = 0; i < 17097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17097(int& val) {
    const int x = 17097;
    std::cout << 17097;
    for (int i = 0; i < 17097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17098(int& val) {
    func_in_17098(val);
    const int x = 17098;
    std::cout << 17098;
    for (int i = 0; i < 17098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17098(int& val) {
    const int x = 17098;
    std::cout << 17098;
    for (int i = 0; i < 17098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17099(int& val) {
    func_in_17099(val);
    const int x = 17099;
    std::cout << 17099;
    for (int i = 0; i < 17099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17099(int& val) {
    const int x = 17099;
    std::cout << 17099;
    for (int i = 0; i < 17099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17100(int& val) {
    func_in_17100(val);
    const int x = 17100;
    std::cout << 17100;
    for (int i = 0; i < 17100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17100(int& val) {
    const int x = 17100;
    std::cout << 17100;
    for (int i = 0; i < 17100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17101(int& val) {
    func_in_17101(val);
    const int x = 17101;
    std::cout << 17101;
    for (int i = 0; i < 17101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17101(int& val) {
    const int x = 17101;
    std::cout << 17101;
    for (int i = 0; i < 17101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17102(int& val) {
    func_in_17102(val);
    const int x = 17102;
    std::cout << 17102;
    for (int i = 0; i < 17102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17102(int& val) {
    const int x = 17102;
    std::cout << 17102;
    for (int i = 0; i < 17102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17103(int& val) {
    func_in_17103(val);
    const int x = 17103;
    std::cout << 17103;
    for (int i = 0; i < 17103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17103(int& val) {
    const int x = 17103;
    std::cout << 17103;
    for (int i = 0; i < 17103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17104(int& val) {
    func_in_17104(val);
    const int x = 17104;
    std::cout << 17104;
    for (int i = 0; i < 17104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17104(int& val) {
    const int x = 17104;
    std::cout << 17104;
    for (int i = 0; i < 17104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17105(int& val) {
    func_in_17105(val);
    const int x = 17105;
    std::cout << 17105;
    for (int i = 0; i < 17105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17105(int& val) {
    const int x = 17105;
    std::cout << 17105;
    for (int i = 0; i < 17105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17106(int& val) {
    func_in_17106(val);
    const int x = 17106;
    std::cout << 17106;
    for (int i = 0; i < 17106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17106(int& val) {
    const int x = 17106;
    std::cout << 17106;
    for (int i = 0; i < 17106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17107(int& val) {
    func_in_17107(val);
    const int x = 17107;
    std::cout << 17107;
    for (int i = 0; i < 17107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17107(int& val) {
    const int x = 17107;
    std::cout << 17107;
    for (int i = 0; i < 17107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17108(int& val) {
    func_in_17108(val);
    const int x = 17108;
    std::cout << 17108;
    for (int i = 0; i < 17108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17108(int& val) {
    const int x = 17108;
    std::cout << 17108;
    for (int i = 0; i < 17108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17109(int& val) {
    func_in_17109(val);
    const int x = 17109;
    std::cout << 17109;
    for (int i = 0; i < 17109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17109(int& val) {
    const int x = 17109;
    std::cout << 17109;
    for (int i = 0; i < 17109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17110(int& val) {
    func_in_17110(val);
    const int x = 17110;
    std::cout << 17110;
    for (int i = 0; i < 17110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17110(int& val) {
    const int x = 17110;
    std::cout << 17110;
    for (int i = 0; i < 17110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17111(int& val) {
    func_in_17111(val);
    const int x = 17111;
    std::cout << 17111;
    for (int i = 0; i < 17111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17111(int& val) {
    const int x = 17111;
    std::cout << 17111;
    for (int i = 0; i < 17111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17112(int& val) {
    func_in_17112(val);
    const int x = 17112;
    std::cout << 17112;
    for (int i = 0; i < 17112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17112(int& val) {
    const int x = 17112;
    std::cout << 17112;
    for (int i = 0; i < 17112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17113(int& val) {
    func_in_17113(val);
    const int x = 17113;
    std::cout << 17113;
    for (int i = 0; i < 17113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17113(int& val) {
    const int x = 17113;
    std::cout << 17113;
    for (int i = 0; i < 17113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17114(int& val) {
    func_in_17114(val);
    const int x = 17114;
    std::cout << 17114;
    for (int i = 0; i < 17114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17114(int& val) {
    const int x = 17114;
    std::cout << 17114;
    for (int i = 0; i < 17114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17115(int& val) {
    func_in_17115(val);
    const int x = 17115;
    std::cout << 17115;
    for (int i = 0; i < 17115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17115(int& val) {
    const int x = 17115;
    std::cout << 17115;
    for (int i = 0; i < 17115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17116(int& val) {
    func_in_17116(val);
    const int x = 17116;
    std::cout << 17116;
    for (int i = 0; i < 17116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17116(int& val) {
    const int x = 17116;
    std::cout << 17116;
    for (int i = 0; i < 17116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17117(int& val) {
    func_in_17117(val);
    const int x = 17117;
    std::cout << 17117;
    for (int i = 0; i < 17117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17117(int& val) {
    const int x = 17117;
    std::cout << 17117;
    for (int i = 0; i < 17117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17118(int& val) {
    func_in_17118(val);
    const int x = 17118;
    std::cout << 17118;
    for (int i = 0; i < 17118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17118(int& val) {
    const int x = 17118;
    std::cout << 17118;
    for (int i = 0; i < 17118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17119(int& val) {
    func_in_17119(val);
    const int x = 17119;
    std::cout << 17119;
    for (int i = 0; i < 17119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17119(int& val) {
    const int x = 17119;
    std::cout << 17119;
    for (int i = 0; i < 17119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17120(int& val) {
    func_in_17120(val);
    const int x = 17120;
    std::cout << 17120;
    for (int i = 0; i < 17120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17120(int& val) {
    const int x = 17120;
    std::cout << 17120;
    for (int i = 0; i < 17120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17121(int& val) {
    func_in_17121(val);
    const int x = 17121;
    std::cout << 17121;
    for (int i = 0; i < 17121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17121(int& val) {
    const int x = 17121;
    std::cout << 17121;
    for (int i = 0; i < 17121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17122(int& val) {
    func_in_17122(val);
    const int x = 17122;
    std::cout << 17122;
    for (int i = 0; i < 17122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17122(int& val) {
    const int x = 17122;
    std::cout << 17122;
    for (int i = 0; i < 17122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17123(int& val) {
    func_in_17123(val);
    const int x = 17123;
    std::cout << 17123;
    for (int i = 0; i < 17123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17123(int& val) {
    const int x = 17123;
    std::cout << 17123;
    for (int i = 0; i < 17123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17124(int& val) {
    func_in_17124(val);
    const int x = 17124;
    std::cout << 17124;
    for (int i = 0; i < 17124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17124(int& val) {
    const int x = 17124;
    std::cout << 17124;
    for (int i = 0; i < 17124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17125(int& val) {
    func_in_17125(val);
    const int x = 17125;
    std::cout << 17125;
    for (int i = 0; i < 17125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17125(int& val) {
    const int x = 17125;
    std::cout << 17125;
    for (int i = 0; i < 17125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17126(int& val) {
    func_in_17126(val);
    const int x = 17126;
    std::cout << 17126;
    for (int i = 0; i < 17126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17126(int& val) {
    const int x = 17126;
    std::cout << 17126;
    for (int i = 0; i < 17126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17127(int& val) {
    func_in_17127(val);
    const int x = 17127;
    std::cout << 17127;
    for (int i = 0; i < 17127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17127(int& val) {
    const int x = 17127;
    std::cout << 17127;
    for (int i = 0; i < 17127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17128(int& val) {
    func_in_17128(val);
    const int x = 17128;
    std::cout << 17128;
    for (int i = 0; i < 17128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17128(int& val) {
    const int x = 17128;
    std::cout << 17128;
    for (int i = 0; i < 17128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17129(int& val) {
    func_in_17129(val);
    const int x = 17129;
    std::cout << 17129;
    for (int i = 0; i < 17129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17129(int& val) {
    const int x = 17129;
    std::cout << 17129;
    for (int i = 0; i < 17129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17130(int& val) {
    func_in_17130(val);
    const int x = 17130;
    std::cout << 17130;
    for (int i = 0; i < 17130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17130(int& val) {
    const int x = 17130;
    std::cout << 17130;
    for (int i = 0; i < 17130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17131(int& val) {
    func_in_17131(val);
    const int x = 17131;
    std::cout << 17131;
    for (int i = 0; i < 17131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17131(int& val) {
    const int x = 17131;
    std::cout << 17131;
    for (int i = 0; i < 17131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17132(int& val) {
    func_in_17132(val);
    const int x = 17132;
    std::cout << 17132;
    for (int i = 0; i < 17132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17132(int& val) {
    const int x = 17132;
    std::cout << 17132;
    for (int i = 0; i < 17132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17133(int& val) {
    func_in_17133(val);
    const int x = 17133;
    std::cout << 17133;
    for (int i = 0; i < 17133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17133(int& val) {
    const int x = 17133;
    std::cout << 17133;
    for (int i = 0; i < 17133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17134(int& val) {
    func_in_17134(val);
    const int x = 17134;
    std::cout << 17134;
    for (int i = 0; i < 17134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17134(int& val) {
    const int x = 17134;
    std::cout << 17134;
    for (int i = 0; i < 17134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17135(int& val) {
    func_in_17135(val);
    const int x = 17135;
    std::cout << 17135;
    for (int i = 0; i < 17135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17135(int& val) {
    const int x = 17135;
    std::cout << 17135;
    for (int i = 0; i < 17135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17136(int& val) {
    func_in_17136(val);
    const int x = 17136;
    std::cout << 17136;
    for (int i = 0; i < 17136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17136(int& val) {
    const int x = 17136;
    std::cout << 17136;
    for (int i = 0; i < 17136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17137(int& val) {
    func_in_17137(val);
    const int x = 17137;
    std::cout << 17137;
    for (int i = 0; i < 17137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17137(int& val) {
    const int x = 17137;
    std::cout << 17137;
    for (int i = 0; i < 17137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17138(int& val) {
    func_in_17138(val);
    const int x = 17138;
    std::cout << 17138;
    for (int i = 0; i < 17138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17138(int& val) {
    const int x = 17138;
    std::cout << 17138;
    for (int i = 0; i < 17138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17139(int& val) {
    func_in_17139(val);
    const int x = 17139;
    std::cout << 17139;
    for (int i = 0; i < 17139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17139(int& val) {
    const int x = 17139;
    std::cout << 17139;
    for (int i = 0; i < 17139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17140(int& val) {
    func_in_17140(val);
    const int x = 17140;
    std::cout << 17140;
    for (int i = 0; i < 17140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17140(int& val) {
    const int x = 17140;
    std::cout << 17140;
    for (int i = 0; i < 17140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17141(int& val) {
    func_in_17141(val);
    const int x = 17141;
    std::cout << 17141;
    for (int i = 0; i < 17141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17141(int& val) {
    const int x = 17141;
    std::cout << 17141;
    for (int i = 0; i < 17141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17142(int& val) {
    func_in_17142(val);
    const int x = 17142;
    std::cout << 17142;
    for (int i = 0; i < 17142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17142(int& val) {
    const int x = 17142;
    std::cout << 17142;
    for (int i = 0; i < 17142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17143(int& val) {
    func_in_17143(val);
    const int x = 17143;
    std::cout << 17143;
    for (int i = 0; i < 17143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17143(int& val) {
    const int x = 17143;
    std::cout << 17143;
    for (int i = 0; i < 17143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17144(int& val) {
    func_in_17144(val);
    const int x = 17144;
    std::cout << 17144;
    for (int i = 0; i < 17144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17144(int& val) {
    const int x = 17144;
    std::cout << 17144;
    for (int i = 0; i < 17144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17145(int& val) {
    func_in_17145(val);
    const int x = 17145;
    std::cout << 17145;
    for (int i = 0; i < 17145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17145(int& val) {
    const int x = 17145;
    std::cout << 17145;
    for (int i = 0; i < 17145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17146(int& val) {
    func_in_17146(val);
    const int x = 17146;
    std::cout << 17146;
    for (int i = 0; i < 17146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17146(int& val) {
    const int x = 17146;
    std::cout << 17146;
    for (int i = 0; i < 17146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17147(int& val) {
    func_in_17147(val);
    const int x = 17147;
    std::cout << 17147;
    for (int i = 0; i < 17147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17147(int& val) {
    const int x = 17147;
    std::cout << 17147;
    for (int i = 0; i < 17147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17148(int& val) {
    func_in_17148(val);
    const int x = 17148;
    std::cout << 17148;
    for (int i = 0; i < 17148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17148(int& val) {
    const int x = 17148;
    std::cout << 17148;
    for (int i = 0; i < 17148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17149(int& val) {
    func_in_17149(val);
    const int x = 17149;
    std::cout << 17149;
    for (int i = 0; i < 17149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17149(int& val) {
    const int x = 17149;
    std::cout << 17149;
    for (int i = 0; i < 17149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17150(int& val) {
    func_in_17150(val);
    const int x = 17150;
    std::cout << 17150;
    for (int i = 0; i < 17150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17150(int& val) {
    const int x = 17150;
    std::cout << 17150;
    for (int i = 0; i < 17150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17151(int& val) {
    func_in_17151(val);
    const int x = 17151;
    std::cout << 17151;
    for (int i = 0; i < 17151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17151(int& val) {
    const int x = 17151;
    std::cout << 17151;
    for (int i = 0; i < 17151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17152(int& val) {
    func_in_17152(val);
    const int x = 17152;
    std::cout << 17152;
    for (int i = 0; i < 17152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17152(int& val) {
    const int x = 17152;
    std::cout << 17152;
    for (int i = 0; i < 17152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17153(int& val) {
    func_in_17153(val);
    const int x = 17153;
    std::cout << 17153;
    for (int i = 0; i < 17153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17153(int& val) {
    const int x = 17153;
    std::cout << 17153;
    for (int i = 0; i < 17153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17154(int& val) {
    func_in_17154(val);
    const int x = 17154;
    std::cout << 17154;
    for (int i = 0; i < 17154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17154(int& val) {
    const int x = 17154;
    std::cout << 17154;
    for (int i = 0; i < 17154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17155(int& val) {
    func_in_17155(val);
    const int x = 17155;
    std::cout << 17155;
    for (int i = 0; i < 17155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17155(int& val) {
    const int x = 17155;
    std::cout << 17155;
    for (int i = 0; i < 17155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17156(int& val) {
    func_in_17156(val);
    const int x = 17156;
    std::cout << 17156;
    for (int i = 0; i < 17156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17156(int& val) {
    const int x = 17156;
    std::cout << 17156;
    for (int i = 0; i < 17156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17157(int& val) {
    func_in_17157(val);
    const int x = 17157;
    std::cout << 17157;
    for (int i = 0; i < 17157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17157(int& val) {
    const int x = 17157;
    std::cout << 17157;
    for (int i = 0; i < 17157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17158(int& val) {
    func_in_17158(val);
    const int x = 17158;
    std::cout << 17158;
    for (int i = 0; i < 17158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17158(int& val) {
    const int x = 17158;
    std::cout << 17158;
    for (int i = 0; i < 17158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17159(int& val) {
    func_in_17159(val);
    const int x = 17159;
    std::cout << 17159;
    for (int i = 0; i < 17159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17159(int& val) {
    const int x = 17159;
    std::cout << 17159;
    for (int i = 0; i < 17159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17160(int& val) {
    func_in_17160(val);
    const int x = 17160;
    std::cout << 17160;
    for (int i = 0; i < 17160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17160(int& val) {
    const int x = 17160;
    std::cout << 17160;
    for (int i = 0; i < 17160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17161(int& val) {
    func_in_17161(val);
    const int x = 17161;
    std::cout << 17161;
    for (int i = 0; i < 17161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17161(int& val) {
    const int x = 17161;
    std::cout << 17161;
    for (int i = 0; i < 17161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17162(int& val) {
    func_in_17162(val);
    const int x = 17162;
    std::cout << 17162;
    for (int i = 0; i < 17162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17162(int& val) {
    const int x = 17162;
    std::cout << 17162;
    for (int i = 0; i < 17162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17163(int& val) {
    func_in_17163(val);
    const int x = 17163;
    std::cout << 17163;
    for (int i = 0; i < 17163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17163(int& val) {
    const int x = 17163;
    std::cout << 17163;
    for (int i = 0; i < 17163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17164(int& val) {
    func_in_17164(val);
    const int x = 17164;
    std::cout << 17164;
    for (int i = 0; i < 17164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17164(int& val) {
    const int x = 17164;
    std::cout << 17164;
    for (int i = 0; i < 17164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17165(int& val) {
    func_in_17165(val);
    const int x = 17165;
    std::cout << 17165;
    for (int i = 0; i < 17165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17165(int& val) {
    const int x = 17165;
    std::cout << 17165;
    for (int i = 0; i < 17165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17166(int& val) {
    func_in_17166(val);
    const int x = 17166;
    std::cout << 17166;
    for (int i = 0; i < 17166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17166(int& val) {
    const int x = 17166;
    std::cout << 17166;
    for (int i = 0; i < 17166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17167(int& val) {
    func_in_17167(val);
    const int x = 17167;
    std::cout << 17167;
    for (int i = 0; i < 17167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17167(int& val) {
    const int x = 17167;
    std::cout << 17167;
    for (int i = 0; i < 17167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17168(int& val) {
    func_in_17168(val);
    const int x = 17168;
    std::cout << 17168;
    for (int i = 0; i < 17168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17168(int& val) {
    const int x = 17168;
    std::cout << 17168;
    for (int i = 0; i < 17168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17169(int& val) {
    func_in_17169(val);
    const int x = 17169;
    std::cout << 17169;
    for (int i = 0; i < 17169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17169(int& val) {
    const int x = 17169;
    std::cout << 17169;
    for (int i = 0; i < 17169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17170(int& val) {
    func_in_17170(val);
    const int x = 17170;
    std::cout << 17170;
    for (int i = 0; i < 17170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17170(int& val) {
    const int x = 17170;
    std::cout << 17170;
    for (int i = 0; i < 17170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17171(int& val) {
    func_in_17171(val);
    const int x = 17171;
    std::cout << 17171;
    for (int i = 0; i < 17171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17171(int& val) {
    const int x = 17171;
    std::cout << 17171;
    for (int i = 0; i < 17171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17172(int& val) {
    func_in_17172(val);
    const int x = 17172;
    std::cout << 17172;
    for (int i = 0; i < 17172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17172(int& val) {
    const int x = 17172;
    std::cout << 17172;
    for (int i = 0; i < 17172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17173(int& val) {
    func_in_17173(val);
    const int x = 17173;
    std::cout << 17173;
    for (int i = 0; i < 17173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17173(int& val) {
    const int x = 17173;
    std::cout << 17173;
    for (int i = 0; i < 17173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17174(int& val) {
    func_in_17174(val);
    const int x = 17174;
    std::cout << 17174;
    for (int i = 0; i < 17174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17174(int& val) {
    const int x = 17174;
    std::cout << 17174;
    for (int i = 0; i < 17174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17175(int& val) {
    func_in_17175(val);
    const int x = 17175;
    std::cout << 17175;
    for (int i = 0; i < 17175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17175(int& val) {
    const int x = 17175;
    std::cout << 17175;
    for (int i = 0; i < 17175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17176(int& val) {
    func_in_17176(val);
    const int x = 17176;
    std::cout << 17176;
    for (int i = 0; i < 17176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17176(int& val) {
    const int x = 17176;
    std::cout << 17176;
    for (int i = 0; i < 17176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17177(int& val) {
    func_in_17177(val);
    const int x = 17177;
    std::cout << 17177;
    for (int i = 0; i < 17177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17177(int& val) {
    const int x = 17177;
    std::cout << 17177;
    for (int i = 0; i < 17177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17178(int& val) {
    func_in_17178(val);
    const int x = 17178;
    std::cout << 17178;
    for (int i = 0; i < 17178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17178(int& val) {
    const int x = 17178;
    std::cout << 17178;
    for (int i = 0; i < 17178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17179(int& val) {
    func_in_17179(val);
    const int x = 17179;
    std::cout << 17179;
    for (int i = 0; i < 17179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17179(int& val) {
    const int x = 17179;
    std::cout << 17179;
    for (int i = 0; i < 17179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17180(int& val) {
    func_in_17180(val);
    const int x = 17180;
    std::cout << 17180;
    for (int i = 0; i < 17180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17180(int& val) {
    const int x = 17180;
    std::cout << 17180;
    for (int i = 0; i < 17180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17181(int& val) {
    func_in_17181(val);
    const int x = 17181;
    std::cout << 17181;
    for (int i = 0; i < 17181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17181(int& val) {
    const int x = 17181;
    std::cout << 17181;
    for (int i = 0; i < 17181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17182(int& val) {
    func_in_17182(val);
    const int x = 17182;
    std::cout << 17182;
    for (int i = 0; i < 17182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17182(int& val) {
    const int x = 17182;
    std::cout << 17182;
    for (int i = 0; i < 17182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17183(int& val) {
    func_in_17183(val);
    const int x = 17183;
    std::cout << 17183;
    for (int i = 0; i < 17183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17183(int& val) {
    const int x = 17183;
    std::cout << 17183;
    for (int i = 0; i < 17183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17184(int& val) {
    func_in_17184(val);
    const int x = 17184;
    std::cout << 17184;
    for (int i = 0; i < 17184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17184(int& val) {
    const int x = 17184;
    std::cout << 17184;
    for (int i = 0; i < 17184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17185(int& val) {
    func_in_17185(val);
    const int x = 17185;
    std::cout << 17185;
    for (int i = 0; i < 17185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17185(int& val) {
    const int x = 17185;
    std::cout << 17185;
    for (int i = 0; i < 17185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17186(int& val) {
    func_in_17186(val);
    const int x = 17186;
    std::cout << 17186;
    for (int i = 0; i < 17186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17186(int& val) {
    const int x = 17186;
    std::cout << 17186;
    for (int i = 0; i < 17186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17187(int& val) {
    func_in_17187(val);
    const int x = 17187;
    std::cout << 17187;
    for (int i = 0; i < 17187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17187(int& val) {
    const int x = 17187;
    std::cout << 17187;
    for (int i = 0; i < 17187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17188(int& val) {
    func_in_17188(val);
    const int x = 17188;
    std::cout << 17188;
    for (int i = 0; i < 17188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17188(int& val) {
    const int x = 17188;
    std::cout << 17188;
    for (int i = 0; i < 17188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17189(int& val) {
    func_in_17189(val);
    const int x = 17189;
    std::cout << 17189;
    for (int i = 0; i < 17189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17189(int& val) {
    const int x = 17189;
    std::cout << 17189;
    for (int i = 0; i < 17189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17190(int& val) {
    func_in_17190(val);
    const int x = 17190;
    std::cout << 17190;
    for (int i = 0; i < 17190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17190(int& val) {
    const int x = 17190;
    std::cout << 17190;
    for (int i = 0; i < 17190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17191(int& val) {
    func_in_17191(val);
    const int x = 17191;
    std::cout << 17191;
    for (int i = 0; i < 17191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17191(int& val) {
    const int x = 17191;
    std::cout << 17191;
    for (int i = 0; i < 17191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17192(int& val) {
    func_in_17192(val);
    const int x = 17192;
    std::cout << 17192;
    for (int i = 0; i < 17192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17192(int& val) {
    const int x = 17192;
    std::cout << 17192;
    for (int i = 0; i < 17192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17193(int& val) {
    func_in_17193(val);
    const int x = 17193;
    std::cout << 17193;
    for (int i = 0; i < 17193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17193(int& val) {
    const int x = 17193;
    std::cout << 17193;
    for (int i = 0; i < 17193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17194(int& val) {
    func_in_17194(val);
    const int x = 17194;
    std::cout << 17194;
    for (int i = 0; i < 17194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17194(int& val) {
    const int x = 17194;
    std::cout << 17194;
    for (int i = 0; i < 17194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17195(int& val) {
    func_in_17195(val);
    const int x = 17195;
    std::cout << 17195;
    for (int i = 0; i < 17195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17195(int& val) {
    const int x = 17195;
    std::cout << 17195;
    for (int i = 0; i < 17195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17196(int& val) {
    func_in_17196(val);
    const int x = 17196;
    std::cout << 17196;
    for (int i = 0; i < 17196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17196(int& val) {
    const int x = 17196;
    std::cout << 17196;
    for (int i = 0; i < 17196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17197(int& val) {
    func_in_17197(val);
    const int x = 17197;
    std::cout << 17197;
    for (int i = 0; i < 17197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17197(int& val) {
    const int x = 17197;
    std::cout << 17197;
    for (int i = 0; i < 17197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17198(int& val) {
    func_in_17198(val);
    const int x = 17198;
    std::cout << 17198;
    for (int i = 0; i < 17198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17198(int& val) {
    const int x = 17198;
    std::cout << 17198;
    for (int i = 0; i < 17198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17199(int& val) {
    func_in_17199(val);
    const int x = 17199;
    std::cout << 17199;
    for (int i = 0; i < 17199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17199(int& val) {
    const int x = 17199;
    std::cout << 17199;
    for (int i = 0; i < 17199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17200(int& val) {
    func_in_17200(val);
    const int x = 17200;
    std::cout << 17200;
    for (int i = 0; i < 17200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17200(int& val) {
    const int x = 17200;
    std::cout << 17200;
    for (int i = 0; i < 17200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17201(int& val) {
    func_in_17201(val);
    const int x = 17201;
    std::cout << 17201;
    for (int i = 0; i < 17201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17201(int& val) {
    const int x = 17201;
    std::cout << 17201;
    for (int i = 0; i < 17201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17202(int& val) {
    func_in_17202(val);
    const int x = 17202;
    std::cout << 17202;
    for (int i = 0; i < 17202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17202(int& val) {
    const int x = 17202;
    std::cout << 17202;
    for (int i = 0; i < 17202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17203(int& val) {
    func_in_17203(val);
    const int x = 17203;
    std::cout << 17203;
    for (int i = 0; i < 17203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17203(int& val) {
    const int x = 17203;
    std::cout << 17203;
    for (int i = 0; i < 17203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17204(int& val) {
    func_in_17204(val);
    const int x = 17204;
    std::cout << 17204;
    for (int i = 0; i < 17204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17204(int& val) {
    const int x = 17204;
    std::cout << 17204;
    for (int i = 0; i < 17204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17205(int& val) {
    func_in_17205(val);
    const int x = 17205;
    std::cout << 17205;
    for (int i = 0; i < 17205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17205(int& val) {
    const int x = 17205;
    std::cout << 17205;
    for (int i = 0; i < 17205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17206(int& val) {
    func_in_17206(val);
    const int x = 17206;
    std::cout << 17206;
    for (int i = 0; i < 17206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17206(int& val) {
    const int x = 17206;
    std::cout << 17206;
    for (int i = 0; i < 17206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17207(int& val) {
    func_in_17207(val);
    const int x = 17207;
    std::cout << 17207;
    for (int i = 0; i < 17207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17207(int& val) {
    const int x = 17207;
    std::cout << 17207;
    for (int i = 0; i < 17207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17208(int& val) {
    func_in_17208(val);
    const int x = 17208;
    std::cout << 17208;
    for (int i = 0; i < 17208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17208(int& val) {
    const int x = 17208;
    std::cout << 17208;
    for (int i = 0; i < 17208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17209(int& val) {
    func_in_17209(val);
    const int x = 17209;
    std::cout << 17209;
    for (int i = 0; i < 17209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17209(int& val) {
    const int x = 17209;
    std::cout << 17209;
    for (int i = 0; i < 17209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17210(int& val) {
    func_in_17210(val);
    const int x = 17210;
    std::cout << 17210;
    for (int i = 0; i < 17210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17210(int& val) {
    const int x = 17210;
    std::cout << 17210;
    for (int i = 0; i < 17210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17211(int& val) {
    func_in_17211(val);
    const int x = 17211;
    std::cout << 17211;
    for (int i = 0; i < 17211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17211(int& val) {
    const int x = 17211;
    std::cout << 17211;
    for (int i = 0; i < 17211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17212(int& val) {
    func_in_17212(val);
    const int x = 17212;
    std::cout << 17212;
    for (int i = 0; i < 17212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17212(int& val) {
    const int x = 17212;
    std::cout << 17212;
    for (int i = 0; i < 17212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17213(int& val) {
    func_in_17213(val);
    const int x = 17213;
    std::cout << 17213;
    for (int i = 0; i < 17213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17213(int& val) {
    const int x = 17213;
    std::cout << 17213;
    for (int i = 0; i < 17213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17214(int& val) {
    func_in_17214(val);
    const int x = 17214;
    std::cout << 17214;
    for (int i = 0; i < 17214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17214(int& val) {
    const int x = 17214;
    std::cout << 17214;
    for (int i = 0; i < 17214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17215(int& val) {
    func_in_17215(val);
    const int x = 17215;
    std::cout << 17215;
    for (int i = 0; i < 17215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17215(int& val) {
    const int x = 17215;
    std::cout << 17215;
    for (int i = 0; i < 17215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17216(int& val) {
    func_in_17216(val);
    const int x = 17216;
    std::cout << 17216;
    for (int i = 0; i < 17216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17216(int& val) {
    const int x = 17216;
    std::cout << 17216;
    for (int i = 0; i < 17216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17217(int& val) {
    func_in_17217(val);
    const int x = 17217;
    std::cout << 17217;
    for (int i = 0; i < 17217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17217(int& val) {
    const int x = 17217;
    std::cout << 17217;
    for (int i = 0; i < 17217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17218(int& val) {
    func_in_17218(val);
    const int x = 17218;
    std::cout << 17218;
    for (int i = 0; i < 17218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17218(int& val) {
    const int x = 17218;
    std::cout << 17218;
    for (int i = 0; i < 17218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17219(int& val) {
    func_in_17219(val);
    const int x = 17219;
    std::cout << 17219;
    for (int i = 0; i < 17219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17219(int& val) {
    const int x = 17219;
    std::cout << 17219;
    for (int i = 0; i < 17219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17220(int& val) {
    func_in_17220(val);
    const int x = 17220;
    std::cout << 17220;
    for (int i = 0; i < 17220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17220(int& val) {
    const int x = 17220;
    std::cout << 17220;
    for (int i = 0; i < 17220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17221(int& val) {
    func_in_17221(val);
    const int x = 17221;
    std::cout << 17221;
    for (int i = 0; i < 17221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17221(int& val) {
    const int x = 17221;
    std::cout << 17221;
    for (int i = 0; i < 17221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17222(int& val) {
    func_in_17222(val);
    const int x = 17222;
    std::cout << 17222;
    for (int i = 0; i < 17222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17222(int& val) {
    const int x = 17222;
    std::cout << 17222;
    for (int i = 0; i < 17222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17223(int& val) {
    func_in_17223(val);
    const int x = 17223;
    std::cout << 17223;
    for (int i = 0; i < 17223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17223(int& val) {
    const int x = 17223;
    std::cout << 17223;
    for (int i = 0; i < 17223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17224(int& val) {
    func_in_17224(val);
    const int x = 17224;
    std::cout << 17224;
    for (int i = 0; i < 17224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17224(int& val) {
    const int x = 17224;
    std::cout << 17224;
    for (int i = 0; i < 17224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17225(int& val) {
    func_in_17225(val);
    const int x = 17225;
    std::cout << 17225;
    for (int i = 0; i < 17225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17225(int& val) {
    const int x = 17225;
    std::cout << 17225;
    for (int i = 0; i < 17225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17226(int& val) {
    func_in_17226(val);
    const int x = 17226;
    std::cout << 17226;
    for (int i = 0; i < 17226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17226(int& val) {
    const int x = 17226;
    std::cout << 17226;
    for (int i = 0; i < 17226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17227(int& val) {
    func_in_17227(val);
    const int x = 17227;
    std::cout << 17227;
    for (int i = 0; i < 17227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17227(int& val) {
    const int x = 17227;
    std::cout << 17227;
    for (int i = 0; i < 17227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17228(int& val) {
    func_in_17228(val);
    const int x = 17228;
    std::cout << 17228;
    for (int i = 0; i < 17228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17228(int& val) {
    const int x = 17228;
    std::cout << 17228;
    for (int i = 0; i < 17228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17229(int& val) {
    func_in_17229(val);
    const int x = 17229;
    std::cout << 17229;
    for (int i = 0; i < 17229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17229(int& val) {
    const int x = 17229;
    std::cout << 17229;
    for (int i = 0; i < 17229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17230(int& val) {
    func_in_17230(val);
    const int x = 17230;
    std::cout << 17230;
    for (int i = 0; i < 17230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17230(int& val) {
    const int x = 17230;
    std::cout << 17230;
    for (int i = 0; i < 17230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17231(int& val) {
    func_in_17231(val);
    const int x = 17231;
    std::cout << 17231;
    for (int i = 0; i < 17231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17231(int& val) {
    const int x = 17231;
    std::cout << 17231;
    for (int i = 0; i < 17231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17232(int& val) {
    func_in_17232(val);
    const int x = 17232;
    std::cout << 17232;
    for (int i = 0; i < 17232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17232(int& val) {
    const int x = 17232;
    std::cout << 17232;
    for (int i = 0; i < 17232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17233(int& val) {
    func_in_17233(val);
    const int x = 17233;
    std::cout << 17233;
    for (int i = 0; i < 17233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17233(int& val) {
    const int x = 17233;
    std::cout << 17233;
    for (int i = 0; i < 17233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17234(int& val) {
    func_in_17234(val);
    const int x = 17234;
    std::cout << 17234;
    for (int i = 0; i < 17234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17234(int& val) {
    const int x = 17234;
    std::cout << 17234;
    for (int i = 0; i < 17234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17235(int& val) {
    func_in_17235(val);
    const int x = 17235;
    std::cout << 17235;
    for (int i = 0; i < 17235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17235(int& val) {
    const int x = 17235;
    std::cout << 17235;
    for (int i = 0; i < 17235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17236(int& val) {
    func_in_17236(val);
    const int x = 17236;
    std::cout << 17236;
    for (int i = 0; i < 17236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17236(int& val) {
    const int x = 17236;
    std::cout << 17236;
    for (int i = 0; i < 17236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17237(int& val) {
    func_in_17237(val);
    const int x = 17237;
    std::cout << 17237;
    for (int i = 0; i < 17237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17237(int& val) {
    const int x = 17237;
    std::cout << 17237;
    for (int i = 0; i < 17237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17238(int& val) {
    func_in_17238(val);
    const int x = 17238;
    std::cout << 17238;
    for (int i = 0; i < 17238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17238(int& val) {
    const int x = 17238;
    std::cout << 17238;
    for (int i = 0; i < 17238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17239(int& val) {
    func_in_17239(val);
    const int x = 17239;
    std::cout << 17239;
    for (int i = 0; i < 17239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17239(int& val) {
    const int x = 17239;
    std::cout << 17239;
    for (int i = 0; i < 17239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17240(int& val) {
    func_in_17240(val);
    const int x = 17240;
    std::cout << 17240;
    for (int i = 0; i < 17240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17240(int& val) {
    const int x = 17240;
    std::cout << 17240;
    for (int i = 0; i < 17240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17241(int& val) {
    func_in_17241(val);
    const int x = 17241;
    std::cout << 17241;
    for (int i = 0; i < 17241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17241(int& val) {
    const int x = 17241;
    std::cout << 17241;
    for (int i = 0; i < 17241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17242(int& val) {
    func_in_17242(val);
    const int x = 17242;
    std::cout << 17242;
    for (int i = 0; i < 17242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17242(int& val) {
    const int x = 17242;
    std::cout << 17242;
    for (int i = 0; i < 17242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17243(int& val) {
    func_in_17243(val);
    const int x = 17243;
    std::cout << 17243;
    for (int i = 0; i < 17243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17243(int& val) {
    const int x = 17243;
    std::cout << 17243;
    for (int i = 0; i < 17243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17244(int& val) {
    func_in_17244(val);
    const int x = 17244;
    std::cout << 17244;
    for (int i = 0; i < 17244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17244(int& val) {
    const int x = 17244;
    std::cout << 17244;
    for (int i = 0; i < 17244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17245(int& val) {
    func_in_17245(val);
    const int x = 17245;
    std::cout << 17245;
    for (int i = 0; i < 17245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17245(int& val) {
    const int x = 17245;
    std::cout << 17245;
    for (int i = 0; i < 17245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17246(int& val) {
    func_in_17246(val);
    const int x = 17246;
    std::cout << 17246;
    for (int i = 0; i < 17246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17246(int& val) {
    const int x = 17246;
    std::cout << 17246;
    for (int i = 0; i < 17246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17247(int& val) {
    func_in_17247(val);
    const int x = 17247;
    std::cout << 17247;
    for (int i = 0; i < 17247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17247(int& val) {
    const int x = 17247;
    std::cout << 17247;
    for (int i = 0; i < 17247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17248(int& val) {
    func_in_17248(val);
    const int x = 17248;
    std::cout << 17248;
    for (int i = 0; i < 17248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17248(int& val) {
    const int x = 17248;
    std::cout << 17248;
    for (int i = 0; i < 17248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17249(int& val) {
    func_in_17249(val);
    const int x = 17249;
    std::cout << 17249;
    for (int i = 0; i < 17249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17249(int& val) {
    const int x = 17249;
    std::cout << 17249;
    for (int i = 0; i < 17249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17250(int& val) {
    func_in_17250(val);
    const int x = 17250;
    std::cout << 17250;
    for (int i = 0; i < 17250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17250(int& val) {
    const int x = 17250;
    std::cout << 17250;
    for (int i = 0; i < 17250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17251(int& val) {
    func_in_17251(val);
    const int x = 17251;
    std::cout << 17251;
    for (int i = 0; i < 17251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17251(int& val) {
    const int x = 17251;
    std::cout << 17251;
    for (int i = 0; i < 17251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17252(int& val) {
    func_in_17252(val);
    const int x = 17252;
    std::cout << 17252;
    for (int i = 0; i < 17252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17252(int& val) {
    const int x = 17252;
    std::cout << 17252;
    for (int i = 0; i < 17252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17253(int& val) {
    func_in_17253(val);
    const int x = 17253;
    std::cout << 17253;
    for (int i = 0; i < 17253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17253(int& val) {
    const int x = 17253;
    std::cout << 17253;
    for (int i = 0; i < 17253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17254(int& val) {
    func_in_17254(val);
    const int x = 17254;
    std::cout << 17254;
    for (int i = 0; i < 17254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17254(int& val) {
    const int x = 17254;
    std::cout << 17254;
    for (int i = 0; i < 17254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17255(int& val) {
    func_in_17255(val);
    const int x = 17255;
    std::cout << 17255;
    for (int i = 0; i < 17255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17255(int& val) {
    const int x = 17255;
    std::cout << 17255;
    for (int i = 0; i < 17255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17256(int& val) {
    func_in_17256(val);
    const int x = 17256;
    std::cout << 17256;
    for (int i = 0; i < 17256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17256(int& val) {
    const int x = 17256;
    std::cout << 17256;
    for (int i = 0; i < 17256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17257(int& val) {
    func_in_17257(val);
    const int x = 17257;
    std::cout << 17257;
    for (int i = 0; i < 17257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17257(int& val) {
    const int x = 17257;
    std::cout << 17257;
    for (int i = 0; i < 17257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17258(int& val) {
    func_in_17258(val);
    const int x = 17258;
    std::cout << 17258;
    for (int i = 0; i < 17258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17258(int& val) {
    const int x = 17258;
    std::cout << 17258;
    for (int i = 0; i < 17258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17259(int& val) {
    func_in_17259(val);
    const int x = 17259;
    std::cout << 17259;
    for (int i = 0; i < 17259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17259(int& val) {
    const int x = 17259;
    std::cout << 17259;
    for (int i = 0; i < 17259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17260(int& val) {
    func_in_17260(val);
    const int x = 17260;
    std::cout << 17260;
    for (int i = 0; i < 17260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17260(int& val) {
    const int x = 17260;
    std::cout << 17260;
    for (int i = 0; i < 17260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17261(int& val) {
    func_in_17261(val);
    const int x = 17261;
    std::cout << 17261;
    for (int i = 0; i < 17261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17261(int& val) {
    const int x = 17261;
    std::cout << 17261;
    for (int i = 0; i < 17261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17262(int& val) {
    func_in_17262(val);
    const int x = 17262;
    std::cout << 17262;
    for (int i = 0; i < 17262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17262(int& val) {
    const int x = 17262;
    std::cout << 17262;
    for (int i = 0; i < 17262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17263(int& val) {
    func_in_17263(val);
    const int x = 17263;
    std::cout << 17263;
    for (int i = 0; i < 17263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17263(int& val) {
    const int x = 17263;
    std::cout << 17263;
    for (int i = 0; i < 17263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17264(int& val) {
    func_in_17264(val);
    const int x = 17264;
    std::cout << 17264;
    for (int i = 0; i < 17264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17264(int& val) {
    const int x = 17264;
    std::cout << 17264;
    for (int i = 0; i < 17264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17265(int& val) {
    func_in_17265(val);
    const int x = 17265;
    std::cout << 17265;
    for (int i = 0; i < 17265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17265(int& val) {
    const int x = 17265;
    std::cout << 17265;
    for (int i = 0; i < 17265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17266(int& val) {
    func_in_17266(val);
    const int x = 17266;
    std::cout << 17266;
    for (int i = 0; i < 17266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17266(int& val) {
    const int x = 17266;
    std::cout << 17266;
    for (int i = 0; i < 17266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17267(int& val) {
    func_in_17267(val);
    const int x = 17267;
    std::cout << 17267;
    for (int i = 0; i < 17267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17267(int& val) {
    const int x = 17267;
    std::cout << 17267;
    for (int i = 0; i < 17267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17268(int& val) {
    func_in_17268(val);
    const int x = 17268;
    std::cout << 17268;
    for (int i = 0; i < 17268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17268(int& val) {
    const int x = 17268;
    std::cout << 17268;
    for (int i = 0; i < 17268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17269(int& val) {
    func_in_17269(val);
    const int x = 17269;
    std::cout << 17269;
    for (int i = 0; i < 17269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17269(int& val) {
    const int x = 17269;
    std::cout << 17269;
    for (int i = 0; i < 17269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17270(int& val) {
    func_in_17270(val);
    const int x = 17270;
    std::cout << 17270;
    for (int i = 0; i < 17270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17270(int& val) {
    const int x = 17270;
    std::cout << 17270;
    for (int i = 0; i < 17270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17271(int& val) {
    func_in_17271(val);
    const int x = 17271;
    std::cout << 17271;
    for (int i = 0; i < 17271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17271(int& val) {
    const int x = 17271;
    std::cout << 17271;
    for (int i = 0; i < 17271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17272(int& val) {
    func_in_17272(val);
    const int x = 17272;
    std::cout << 17272;
    for (int i = 0; i < 17272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17272(int& val) {
    const int x = 17272;
    std::cout << 17272;
    for (int i = 0; i < 17272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17273(int& val) {
    func_in_17273(val);
    const int x = 17273;
    std::cout << 17273;
    for (int i = 0; i < 17273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17273(int& val) {
    const int x = 17273;
    std::cout << 17273;
    for (int i = 0; i < 17273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17274(int& val) {
    func_in_17274(val);
    const int x = 17274;
    std::cout << 17274;
    for (int i = 0; i < 17274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17274(int& val) {
    const int x = 17274;
    std::cout << 17274;
    for (int i = 0; i < 17274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17275(int& val) {
    func_in_17275(val);
    const int x = 17275;
    std::cout << 17275;
    for (int i = 0; i < 17275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17275(int& val) {
    const int x = 17275;
    std::cout << 17275;
    for (int i = 0; i < 17275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17276(int& val) {
    func_in_17276(val);
    const int x = 17276;
    std::cout << 17276;
    for (int i = 0; i < 17276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17276(int& val) {
    const int x = 17276;
    std::cout << 17276;
    for (int i = 0; i < 17276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17277(int& val) {
    func_in_17277(val);
    const int x = 17277;
    std::cout << 17277;
    for (int i = 0; i < 17277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17277(int& val) {
    const int x = 17277;
    std::cout << 17277;
    for (int i = 0; i < 17277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17278(int& val) {
    func_in_17278(val);
    const int x = 17278;
    std::cout << 17278;
    for (int i = 0; i < 17278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17278(int& val) {
    const int x = 17278;
    std::cout << 17278;
    for (int i = 0; i < 17278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17279(int& val) {
    func_in_17279(val);
    const int x = 17279;
    std::cout << 17279;
    for (int i = 0; i < 17279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17279(int& val) {
    const int x = 17279;
    std::cout << 17279;
    for (int i = 0; i < 17279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17280(int& val) {
    func_in_17280(val);
    const int x = 17280;
    std::cout << 17280;
    for (int i = 0; i < 17280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17280(int& val) {
    const int x = 17280;
    std::cout << 17280;
    for (int i = 0; i < 17280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17281(int& val) {
    func_in_17281(val);
    const int x = 17281;
    std::cout << 17281;
    for (int i = 0; i < 17281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17281(int& val) {
    const int x = 17281;
    std::cout << 17281;
    for (int i = 0; i < 17281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17282(int& val) {
    func_in_17282(val);
    const int x = 17282;
    std::cout << 17282;
    for (int i = 0; i < 17282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17282(int& val) {
    const int x = 17282;
    std::cout << 17282;
    for (int i = 0; i < 17282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17283(int& val) {
    func_in_17283(val);
    const int x = 17283;
    std::cout << 17283;
    for (int i = 0; i < 17283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17283(int& val) {
    const int x = 17283;
    std::cout << 17283;
    for (int i = 0; i < 17283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17284(int& val) {
    func_in_17284(val);
    const int x = 17284;
    std::cout << 17284;
    for (int i = 0; i < 17284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17284(int& val) {
    const int x = 17284;
    std::cout << 17284;
    for (int i = 0; i < 17284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17285(int& val) {
    func_in_17285(val);
    const int x = 17285;
    std::cout << 17285;
    for (int i = 0; i < 17285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17285(int& val) {
    const int x = 17285;
    std::cout << 17285;
    for (int i = 0; i < 17285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17286(int& val) {
    func_in_17286(val);
    const int x = 17286;
    std::cout << 17286;
    for (int i = 0; i < 17286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17286(int& val) {
    const int x = 17286;
    std::cout << 17286;
    for (int i = 0; i < 17286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17287(int& val) {
    func_in_17287(val);
    const int x = 17287;
    std::cout << 17287;
    for (int i = 0; i < 17287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17287(int& val) {
    const int x = 17287;
    std::cout << 17287;
    for (int i = 0; i < 17287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17288(int& val) {
    func_in_17288(val);
    const int x = 17288;
    std::cout << 17288;
    for (int i = 0; i < 17288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17288(int& val) {
    const int x = 17288;
    std::cout << 17288;
    for (int i = 0; i < 17288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17289(int& val) {
    func_in_17289(val);
    const int x = 17289;
    std::cout << 17289;
    for (int i = 0; i < 17289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17289(int& val) {
    const int x = 17289;
    std::cout << 17289;
    for (int i = 0; i < 17289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17290(int& val) {
    func_in_17290(val);
    const int x = 17290;
    std::cout << 17290;
    for (int i = 0; i < 17290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17290(int& val) {
    const int x = 17290;
    std::cout << 17290;
    for (int i = 0; i < 17290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17291(int& val) {
    func_in_17291(val);
    const int x = 17291;
    std::cout << 17291;
    for (int i = 0; i < 17291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17291(int& val) {
    const int x = 17291;
    std::cout << 17291;
    for (int i = 0; i < 17291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17292(int& val) {
    func_in_17292(val);
    const int x = 17292;
    std::cout << 17292;
    for (int i = 0; i < 17292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17292(int& val) {
    const int x = 17292;
    std::cout << 17292;
    for (int i = 0; i < 17292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17293(int& val) {
    func_in_17293(val);
    const int x = 17293;
    std::cout << 17293;
    for (int i = 0; i < 17293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17293(int& val) {
    const int x = 17293;
    std::cout << 17293;
    for (int i = 0; i < 17293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17294(int& val) {
    func_in_17294(val);
    const int x = 17294;
    std::cout << 17294;
    for (int i = 0; i < 17294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17294(int& val) {
    const int x = 17294;
    std::cout << 17294;
    for (int i = 0; i < 17294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17295(int& val) {
    func_in_17295(val);
    const int x = 17295;
    std::cout << 17295;
    for (int i = 0; i < 17295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17295(int& val) {
    const int x = 17295;
    std::cout << 17295;
    for (int i = 0; i < 17295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17296(int& val) {
    func_in_17296(val);
    const int x = 17296;
    std::cout << 17296;
    for (int i = 0; i < 17296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17296(int& val) {
    const int x = 17296;
    std::cout << 17296;
    for (int i = 0; i < 17296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17297(int& val) {
    func_in_17297(val);
    const int x = 17297;
    std::cout << 17297;
    for (int i = 0; i < 17297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17297(int& val) {
    const int x = 17297;
    std::cout << 17297;
    for (int i = 0; i < 17297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17298(int& val) {
    func_in_17298(val);
    const int x = 17298;
    std::cout << 17298;
    for (int i = 0; i < 17298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17298(int& val) {
    const int x = 17298;
    std::cout << 17298;
    for (int i = 0; i < 17298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17299(int& val) {
    func_in_17299(val);
    const int x = 17299;
    std::cout << 17299;
    for (int i = 0; i < 17299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17299(int& val) {
    const int x = 17299;
    std::cout << 17299;
    for (int i = 0; i < 17299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17300(int& val) {
    func_in_17300(val);
    const int x = 17300;
    std::cout << 17300;
    for (int i = 0; i < 17300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17300(int& val) {
    const int x = 17300;
    std::cout << 17300;
    for (int i = 0; i < 17300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17301(int& val) {
    func_in_17301(val);
    const int x = 17301;
    std::cout << 17301;
    for (int i = 0; i < 17301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17301(int& val) {
    const int x = 17301;
    std::cout << 17301;
    for (int i = 0; i < 17301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17302(int& val) {
    func_in_17302(val);
    const int x = 17302;
    std::cout << 17302;
    for (int i = 0; i < 17302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17302(int& val) {
    const int x = 17302;
    std::cout << 17302;
    for (int i = 0; i < 17302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17303(int& val) {
    func_in_17303(val);
    const int x = 17303;
    std::cout << 17303;
    for (int i = 0; i < 17303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17303(int& val) {
    const int x = 17303;
    std::cout << 17303;
    for (int i = 0; i < 17303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17304(int& val) {
    func_in_17304(val);
    const int x = 17304;
    std::cout << 17304;
    for (int i = 0; i < 17304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17304(int& val) {
    const int x = 17304;
    std::cout << 17304;
    for (int i = 0; i < 17304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17305(int& val) {
    func_in_17305(val);
    const int x = 17305;
    std::cout << 17305;
    for (int i = 0; i < 17305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17305(int& val) {
    const int x = 17305;
    std::cout << 17305;
    for (int i = 0; i < 17305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17306(int& val) {
    func_in_17306(val);
    const int x = 17306;
    std::cout << 17306;
    for (int i = 0; i < 17306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17306(int& val) {
    const int x = 17306;
    std::cout << 17306;
    for (int i = 0; i < 17306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17307(int& val) {
    func_in_17307(val);
    const int x = 17307;
    std::cout << 17307;
    for (int i = 0; i < 17307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17307(int& val) {
    const int x = 17307;
    std::cout << 17307;
    for (int i = 0; i < 17307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17308(int& val) {
    func_in_17308(val);
    const int x = 17308;
    std::cout << 17308;
    for (int i = 0; i < 17308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17308(int& val) {
    const int x = 17308;
    std::cout << 17308;
    for (int i = 0; i < 17308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17309(int& val) {
    func_in_17309(val);
    const int x = 17309;
    std::cout << 17309;
    for (int i = 0; i < 17309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17309(int& val) {
    const int x = 17309;
    std::cout << 17309;
    for (int i = 0; i < 17309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17310(int& val) {
    func_in_17310(val);
    const int x = 17310;
    std::cout << 17310;
    for (int i = 0; i < 17310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17310(int& val) {
    const int x = 17310;
    std::cout << 17310;
    for (int i = 0; i < 17310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17311(int& val) {
    func_in_17311(val);
    const int x = 17311;
    std::cout << 17311;
    for (int i = 0; i < 17311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17311(int& val) {
    const int x = 17311;
    std::cout << 17311;
    for (int i = 0; i < 17311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17312(int& val) {
    func_in_17312(val);
    const int x = 17312;
    std::cout << 17312;
    for (int i = 0; i < 17312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17312(int& val) {
    const int x = 17312;
    std::cout << 17312;
    for (int i = 0; i < 17312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17313(int& val) {
    func_in_17313(val);
    const int x = 17313;
    std::cout << 17313;
    for (int i = 0; i < 17313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17313(int& val) {
    const int x = 17313;
    std::cout << 17313;
    for (int i = 0; i < 17313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17314(int& val) {
    func_in_17314(val);
    const int x = 17314;
    std::cout << 17314;
    for (int i = 0; i < 17314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17314(int& val) {
    const int x = 17314;
    std::cout << 17314;
    for (int i = 0; i < 17314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17315(int& val) {
    func_in_17315(val);
    const int x = 17315;
    std::cout << 17315;
    for (int i = 0; i < 17315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17315(int& val) {
    const int x = 17315;
    std::cout << 17315;
    for (int i = 0; i < 17315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17316(int& val) {
    func_in_17316(val);
    const int x = 17316;
    std::cout << 17316;
    for (int i = 0; i < 17316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17316(int& val) {
    const int x = 17316;
    std::cout << 17316;
    for (int i = 0; i < 17316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17317(int& val) {
    func_in_17317(val);
    const int x = 17317;
    std::cout << 17317;
    for (int i = 0; i < 17317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17317(int& val) {
    const int x = 17317;
    std::cout << 17317;
    for (int i = 0; i < 17317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17318(int& val) {
    func_in_17318(val);
    const int x = 17318;
    std::cout << 17318;
    for (int i = 0; i < 17318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17318(int& val) {
    const int x = 17318;
    std::cout << 17318;
    for (int i = 0; i < 17318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17319(int& val) {
    func_in_17319(val);
    const int x = 17319;
    std::cout << 17319;
    for (int i = 0; i < 17319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17319(int& val) {
    const int x = 17319;
    std::cout << 17319;
    for (int i = 0; i < 17319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17320(int& val) {
    func_in_17320(val);
    const int x = 17320;
    std::cout << 17320;
    for (int i = 0; i < 17320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17320(int& val) {
    const int x = 17320;
    std::cout << 17320;
    for (int i = 0; i < 17320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17321(int& val) {
    func_in_17321(val);
    const int x = 17321;
    std::cout << 17321;
    for (int i = 0; i < 17321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17321(int& val) {
    const int x = 17321;
    std::cout << 17321;
    for (int i = 0; i < 17321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17322(int& val) {
    func_in_17322(val);
    const int x = 17322;
    std::cout << 17322;
    for (int i = 0; i < 17322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17322(int& val) {
    const int x = 17322;
    std::cout << 17322;
    for (int i = 0; i < 17322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17323(int& val) {
    func_in_17323(val);
    const int x = 17323;
    std::cout << 17323;
    for (int i = 0; i < 17323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17323(int& val) {
    const int x = 17323;
    std::cout << 17323;
    for (int i = 0; i < 17323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17324(int& val) {
    func_in_17324(val);
    const int x = 17324;
    std::cout << 17324;
    for (int i = 0; i < 17324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17324(int& val) {
    const int x = 17324;
    std::cout << 17324;
    for (int i = 0; i < 17324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17325(int& val) {
    func_in_17325(val);
    const int x = 17325;
    std::cout << 17325;
    for (int i = 0; i < 17325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17325(int& val) {
    const int x = 17325;
    std::cout << 17325;
    for (int i = 0; i < 17325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17326(int& val) {
    func_in_17326(val);
    const int x = 17326;
    std::cout << 17326;
    for (int i = 0; i < 17326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17326(int& val) {
    const int x = 17326;
    std::cout << 17326;
    for (int i = 0; i < 17326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17327(int& val) {
    func_in_17327(val);
    const int x = 17327;
    std::cout << 17327;
    for (int i = 0; i < 17327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17327(int& val) {
    const int x = 17327;
    std::cout << 17327;
    for (int i = 0; i < 17327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17328(int& val) {
    func_in_17328(val);
    const int x = 17328;
    std::cout << 17328;
    for (int i = 0; i < 17328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17328(int& val) {
    const int x = 17328;
    std::cout << 17328;
    for (int i = 0; i < 17328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17329(int& val) {
    func_in_17329(val);
    const int x = 17329;
    std::cout << 17329;
    for (int i = 0; i < 17329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17329(int& val) {
    const int x = 17329;
    std::cout << 17329;
    for (int i = 0; i < 17329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17330(int& val) {
    func_in_17330(val);
    const int x = 17330;
    std::cout << 17330;
    for (int i = 0; i < 17330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17330(int& val) {
    const int x = 17330;
    std::cout << 17330;
    for (int i = 0; i < 17330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17331(int& val) {
    func_in_17331(val);
    const int x = 17331;
    std::cout << 17331;
    for (int i = 0; i < 17331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17331(int& val) {
    const int x = 17331;
    std::cout << 17331;
    for (int i = 0; i < 17331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17332(int& val) {
    func_in_17332(val);
    const int x = 17332;
    std::cout << 17332;
    for (int i = 0; i < 17332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17332(int& val) {
    const int x = 17332;
    std::cout << 17332;
    for (int i = 0; i < 17332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17333(int& val) {
    func_in_17333(val);
    const int x = 17333;
    std::cout << 17333;
    for (int i = 0; i < 17333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17333(int& val) {
    const int x = 17333;
    std::cout << 17333;
    for (int i = 0; i < 17333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17334(int& val) {
    func_in_17334(val);
    const int x = 17334;
    std::cout << 17334;
    for (int i = 0; i < 17334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17334(int& val) {
    const int x = 17334;
    std::cout << 17334;
    for (int i = 0; i < 17334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17335(int& val) {
    func_in_17335(val);
    const int x = 17335;
    std::cout << 17335;
    for (int i = 0; i < 17335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17335(int& val) {
    const int x = 17335;
    std::cout << 17335;
    for (int i = 0; i < 17335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17336(int& val) {
    func_in_17336(val);
    const int x = 17336;
    std::cout << 17336;
    for (int i = 0; i < 17336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17336(int& val) {
    const int x = 17336;
    std::cout << 17336;
    for (int i = 0; i < 17336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17337(int& val) {
    func_in_17337(val);
    const int x = 17337;
    std::cout << 17337;
    for (int i = 0; i < 17337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17337(int& val) {
    const int x = 17337;
    std::cout << 17337;
    for (int i = 0; i < 17337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17338(int& val) {
    func_in_17338(val);
    const int x = 17338;
    std::cout << 17338;
    for (int i = 0; i < 17338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17338(int& val) {
    const int x = 17338;
    std::cout << 17338;
    for (int i = 0; i < 17338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17339(int& val) {
    func_in_17339(val);
    const int x = 17339;
    std::cout << 17339;
    for (int i = 0; i < 17339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17339(int& val) {
    const int x = 17339;
    std::cout << 17339;
    for (int i = 0; i < 17339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17340(int& val) {
    func_in_17340(val);
    const int x = 17340;
    std::cout << 17340;
    for (int i = 0; i < 17340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17340(int& val) {
    const int x = 17340;
    std::cout << 17340;
    for (int i = 0; i < 17340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17341(int& val) {
    func_in_17341(val);
    const int x = 17341;
    std::cout << 17341;
    for (int i = 0; i < 17341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17341(int& val) {
    const int x = 17341;
    std::cout << 17341;
    for (int i = 0; i < 17341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17342(int& val) {
    func_in_17342(val);
    const int x = 17342;
    std::cout << 17342;
    for (int i = 0; i < 17342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17342(int& val) {
    const int x = 17342;
    std::cout << 17342;
    for (int i = 0; i < 17342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17343(int& val) {
    func_in_17343(val);
    const int x = 17343;
    std::cout << 17343;
    for (int i = 0; i < 17343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17343(int& val) {
    const int x = 17343;
    std::cout << 17343;
    for (int i = 0; i < 17343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17344(int& val) {
    func_in_17344(val);
    const int x = 17344;
    std::cout << 17344;
    for (int i = 0; i < 17344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17344(int& val) {
    const int x = 17344;
    std::cout << 17344;
    for (int i = 0; i < 17344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17345(int& val) {
    func_in_17345(val);
    const int x = 17345;
    std::cout << 17345;
    for (int i = 0; i < 17345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17345(int& val) {
    const int x = 17345;
    std::cout << 17345;
    for (int i = 0; i < 17345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17346(int& val) {
    func_in_17346(val);
    const int x = 17346;
    std::cout << 17346;
    for (int i = 0; i < 17346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17346(int& val) {
    const int x = 17346;
    std::cout << 17346;
    for (int i = 0; i < 17346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17347(int& val) {
    func_in_17347(val);
    const int x = 17347;
    std::cout << 17347;
    for (int i = 0; i < 17347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17347(int& val) {
    const int x = 17347;
    std::cout << 17347;
    for (int i = 0; i < 17347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17348(int& val) {
    func_in_17348(val);
    const int x = 17348;
    std::cout << 17348;
    for (int i = 0; i < 17348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17348(int& val) {
    const int x = 17348;
    std::cout << 17348;
    for (int i = 0; i < 17348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17349(int& val) {
    func_in_17349(val);
    const int x = 17349;
    std::cout << 17349;
    for (int i = 0; i < 17349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17349(int& val) {
    const int x = 17349;
    std::cout << 17349;
    for (int i = 0; i < 17349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17350(int& val) {
    func_in_17350(val);
    const int x = 17350;
    std::cout << 17350;
    for (int i = 0; i < 17350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17350(int& val) {
    const int x = 17350;
    std::cout << 17350;
    for (int i = 0; i < 17350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17351(int& val) {
    func_in_17351(val);
    const int x = 17351;
    std::cout << 17351;
    for (int i = 0; i < 17351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17351(int& val) {
    const int x = 17351;
    std::cout << 17351;
    for (int i = 0; i < 17351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17352(int& val) {
    func_in_17352(val);
    const int x = 17352;
    std::cout << 17352;
    for (int i = 0; i < 17352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17352(int& val) {
    const int x = 17352;
    std::cout << 17352;
    for (int i = 0; i < 17352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17353(int& val) {
    func_in_17353(val);
    const int x = 17353;
    std::cout << 17353;
    for (int i = 0; i < 17353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17353(int& val) {
    const int x = 17353;
    std::cout << 17353;
    for (int i = 0; i < 17353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17354(int& val) {
    func_in_17354(val);
    const int x = 17354;
    std::cout << 17354;
    for (int i = 0; i < 17354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17354(int& val) {
    const int x = 17354;
    std::cout << 17354;
    for (int i = 0; i < 17354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17355(int& val) {
    func_in_17355(val);
    const int x = 17355;
    std::cout << 17355;
    for (int i = 0; i < 17355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17355(int& val) {
    const int x = 17355;
    std::cout << 17355;
    for (int i = 0; i < 17355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17356(int& val) {
    func_in_17356(val);
    const int x = 17356;
    std::cout << 17356;
    for (int i = 0; i < 17356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17356(int& val) {
    const int x = 17356;
    std::cout << 17356;
    for (int i = 0; i < 17356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17357(int& val) {
    func_in_17357(val);
    const int x = 17357;
    std::cout << 17357;
    for (int i = 0; i < 17357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17357(int& val) {
    const int x = 17357;
    std::cout << 17357;
    for (int i = 0; i < 17357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17358(int& val) {
    func_in_17358(val);
    const int x = 17358;
    std::cout << 17358;
    for (int i = 0; i < 17358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17358(int& val) {
    const int x = 17358;
    std::cout << 17358;
    for (int i = 0; i < 17358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17359(int& val) {
    func_in_17359(val);
    const int x = 17359;
    std::cout << 17359;
    for (int i = 0; i < 17359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17359(int& val) {
    const int x = 17359;
    std::cout << 17359;
    for (int i = 0; i < 17359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17360(int& val) {
    func_in_17360(val);
    const int x = 17360;
    std::cout << 17360;
    for (int i = 0; i < 17360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17360(int& val) {
    const int x = 17360;
    std::cout << 17360;
    for (int i = 0; i < 17360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17361(int& val) {
    func_in_17361(val);
    const int x = 17361;
    std::cout << 17361;
    for (int i = 0; i < 17361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17361(int& val) {
    const int x = 17361;
    std::cout << 17361;
    for (int i = 0; i < 17361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17362(int& val) {
    func_in_17362(val);
    const int x = 17362;
    std::cout << 17362;
    for (int i = 0; i < 17362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17362(int& val) {
    const int x = 17362;
    std::cout << 17362;
    for (int i = 0; i < 17362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17363(int& val) {
    func_in_17363(val);
    const int x = 17363;
    std::cout << 17363;
    for (int i = 0; i < 17363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17363(int& val) {
    const int x = 17363;
    std::cout << 17363;
    for (int i = 0; i < 17363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17364(int& val) {
    func_in_17364(val);
    const int x = 17364;
    std::cout << 17364;
    for (int i = 0; i < 17364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17364(int& val) {
    const int x = 17364;
    std::cout << 17364;
    for (int i = 0; i < 17364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17365(int& val) {
    func_in_17365(val);
    const int x = 17365;
    std::cout << 17365;
    for (int i = 0; i < 17365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17365(int& val) {
    const int x = 17365;
    std::cout << 17365;
    for (int i = 0; i < 17365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17366(int& val) {
    func_in_17366(val);
    const int x = 17366;
    std::cout << 17366;
    for (int i = 0; i < 17366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17366(int& val) {
    const int x = 17366;
    std::cout << 17366;
    for (int i = 0; i < 17366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17367(int& val) {
    func_in_17367(val);
    const int x = 17367;
    std::cout << 17367;
    for (int i = 0; i < 17367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17367(int& val) {
    const int x = 17367;
    std::cout << 17367;
    for (int i = 0; i < 17367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17368(int& val) {
    func_in_17368(val);
    const int x = 17368;
    std::cout << 17368;
    for (int i = 0; i < 17368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17368(int& val) {
    const int x = 17368;
    std::cout << 17368;
    for (int i = 0; i < 17368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17369(int& val) {
    func_in_17369(val);
    const int x = 17369;
    std::cout << 17369;
    for (int i = 0; i < 17369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17369(int& val) {
    const int x = 17369;
    std::cout << 17369;
    for (int i = 0; i < 17369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17370(int& val) {
    func_in_17370(val);
    const int x = 17370;
    std::cout << 17370;
    for (int i = 0; i < 17370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17370(int& val) {
    const int x = 17370;
    std::cout << 17370;
    for (int i = 0; i < 17370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17371(int& val) {
    func_in_17371(val);
    const int x = 17371;
    std::cout << 17371;
    for (int i = 0; i < 17371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17371(int& val) {
    const int x = 17371;
    std::cout << 17371;
    for (int i = 0; i < 17371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17372(int& val) {
    func_in_17372(val);
    const int x = 17372;
    std::cout << 17372;
    for (int i = 0; i < 17372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17372(int& val) {
    const int x = 17372;
    std::cout << 17372;
    for (int i = 0; i < 17372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17373(int& val) {
    func_in_17373(val);
    const int x = 17373;
    std::cout << 17373;
    for (int i = 0; i < 17373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17373(int& val) {
    const int x = 17373;
    std::cout << 17373;
    for (int i = 0; i < 17373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17374(int& val) {
    func_in_17374(val);
    const int x = 17374;
    std::cout << 17374;
    for (int i = 0; i < 17374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17374(int& val) {
    const int x = 17374;
    std::cout << 17374;
    for (int i = 0; i < 17374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17375(int& val) {
    func_in_17375(val);
    const int x = 17375;
    std::cout << 17375;
    for (int i = 0; i < 17375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17375(int& val) {
    const int x = 17375;
    std::cout << 17375;
    for (int i = 0; i < 17375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17376(int& val) {
    func_in_17376(val);
    const int x = 17376;
    std::cout << 17376;
    for (int i = 0; i < 17376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17376(int& val) {
    const int x = 17376;
    std::cout << 17376;
    for (int i = 0; i < 17376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17377(int& val) {
    func_in_17377(val);
    const int x = 17377;
    std::cout << 17377;
    for (int i = 0; i < 17377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17377(int& val) {
    const int x = 17377;
    std::cout << 17377;
    for (int i = 0; i < 17377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17378(int& val) {
    func_in_17378(val);
    const int x = 17378;
    std::cout << 17378;
    for (int i = 0; i < 17378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17378(int& val) {
    const int x = 17378;
    std::cout << 17378;
    for (int i = 0; i < 17378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17379(int& val) {
    func_in_17379(val);
    const int x = 17379;
    std::cout << 17379;
    for (int i = 0; i < 17379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17379(int& val) {
    const int x = 17379;
    std::cout << 17379;
    for (int i = 0; i < 17379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17380(int& val) {
    func_in_17380(val);
    const int x = 17380;
    std::cout << 17380;
    for (int i = 0; i < 17380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17380(int& val) {
    const int x = 17380;
    std::cout << 17380;
    for (int i = 0; i < 17380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17381(int& val) {
    func_in_17381(val);
    const int x = 17381;
    std::cout << 17381;
    for (int i = 0; i < 17381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17381(int& val) {
    const int x = 17381;
    std::cout << 17381;
    for (int i = 0; i < 17381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17382(int& val) {
    func_in_17382(val);
    const int x = 17382;
    std::cout << 17382;
    for (int i = 0; i < 17382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17382(int& val) {
    const int x = 17382;
    std::cout << 17382;
    for (int i = 0; i < 17382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17383(int& val) {
    func_in_17383(val);
    const int x = 17383;
    std::cout << 17383;
    for (int i = 0; i < 17383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17383(int& val) {
    const int x = 17383;
    std::cout << 17383;
    for (int i = 0; i < 17383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17384(int& val) {
    func_in_17384(val);
    const int x = 17384;
    std::cout << 17384;
    for (int i = 0; i < 17384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17384(int& val) {
    const int x = 17384;
    std::cout << 17384;
    for (int i = 0; i < 17384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17385(int& val) {
    func_in_17385(val);
    const int x = 17385;
    std::cout << 17385;
    for (int i = 0; i < 17385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17385(int& val) {
    const int x = 17385;
    std::cout << 17385;
    for (int i = 0; i < 17385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17386(int& val) {
    func_in_17386(val);
    const int x = 17386;
    std::cout << 17386;
    for (int i = 0; i < 17386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17386(int& val) {
    const int x = 17386;
    std::cout << 17386;
    for (int i = 0; i < 17386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17387(int& val) {
    func_in_17387(val);
    const int x = 17387;
    std::cout << 17387;
    for (int i = 0; i < 17387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17387(int& val) {
    const int x = 17387;
    std::cout << 17387;
    for (int i = 0; i < 17387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17388(int& val) {
    func_in_17388(val);
    const int x = 17388;
    std::cout << 17388;
    for (int i = 0; i < 17388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17388(int& val) {
    const int x = 17388;
    std::cout << 17388;
    for (int i = 0; i < 17388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17389(int& val) {
    func_in_17389(val);
    const int x = 17389;
    std::cout << 17389;
    for (int i = 0; i < 17389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17389(int& val) {
    const int x = 17389;
    std::cout << 17389;
    for (int i = 0; i < 17389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17390(int& val) {
    func_in_17390(val);
    const int x = 17390;
    std::cout << 17390;
    for (int i = 0; i < 17390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17390(int& val) {
    const int x = 17390;
    std::cout << 17390;
    for (int i = 0; i < 17390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17391(int& val) {
    func_in_17391(val);
    const int x = 17391;
    std::cout << 17391;
    for (int i = 0; i < 17391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17391(int& val) {
    const int x = 17391;
    std::cout << 17391;
    for (int i = 0; i < 17391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17392(int& val) {
    func_in_17392(val);
    const int x = 17392;
    std::cout << 17392;
    for (int i = 0; i < 17392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17392(int& val) {
    const int x = 17392;
    std::cout << 17392;
    for (int i = 0; i < 17392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17393(int& val) {
    func_in_17393(val);
    const int x = 17393;
    std::cout << 17393;
    for (int i = 0; i < 17393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17393(int& val) {
    const int x = 17393;
    std::cout << 17393;
    for (int i = 0; i < 17393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17394(int& val) {
    func_in_17394(val);
    const int x = 17394;
    std::cout << 17394;
    for (int i = 0; i < 17394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17394(int& val) {
    const int x = 17394;
    std::cout << 17394;
    for (int i = 0; i < 17394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17395(int& val) {
    func_in_17395(val);
    const int x = 17395;
    std::cout << 17395;
    for (int i = 0; i < 17395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17395(int& val) {
    const int x = 17395;
    std::cout << 17395;
    for (int i = 0; i < 17395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17396(int& val) {
    func_in_17396(val);
    const int x = 17396;
    std::cout << 17396;
    for (int i = 0; i < 17396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17396(int& val) {
    const int x = 17396;
    std::cout << 17396;
    for (int i = 0; i < 17396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17397(int& val) {
    func_in_17397(val);
    const int x = 17397;
    std::cout << 17397;
    for (int i = 0; i < 17397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17397(int& val) {
    const int x = 17397;
    std::cout << 17397;
    for (int i = 0; i < 17397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17398(int& val) {
    func_in_17398(val);
    const int x = 17398;
    std::cout << 17398;
    for (int i = 0; i < 17398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17398(int& val) {
    const int x = 17398;
    std::cout << 17398;
    for (int i = 0; i < 17398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17399(int& val) {
    func_in_17399(val);
    const int x = 17399;
    std::cout << 17399;
    for (int i = 0; i < 17399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17399(int& val) {
    const int x = 17399;
    std::cout << 17399;
    for (int i = 0; i < 17399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17400(int& val) {
    func_in_17400(val);
    const int x = 17400;
    std::cout << 17400;
    for (int i = 0; i < 17400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17400(int& val) {
    const int x = 17400;
    std::cout << 17400;
    for (int i = 0; i < 17400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17401(int& val) {
    func_in_17401(val);
    const int x = 17401;
    std::cout << 17401;
    for (int i = 0; i < 17401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17401(int& val) {
    const int x = 17401;
    std::cout << 17401;
    for (int i = 0; i < 17401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17402(int& val) {
    func_in_17402(val);
    const int x = 17402;
    std::cout << 17402;
    for (int i = 0; i < 17402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17402(int& val) {
    const int x = 17402;
    std::cout << 17402;
    for (int i = 0; i < 17402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17403(int& val) {
    func_in_17403(val);
    const int x = 17403;
    std::cout << 17403;
    for (int i = 0; i < 17403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17403(int& val) {
    const int x = 17403;
    std::cout << 17403;
    for (int i = 0; i < 17403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17404(int& val) {
    func_in_17404(val);
    const int x = 17404;
    std::cout << 17404;
    for (int i = 0; i < 17404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17404(int& val) {
    const int x = 17404;
    std::cout << 17404;
    for (int i = 0; i < 17404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17405(int& val) {
    func_in_17405(val);
    const int x = 17405;
    std::cout << 17405;
    for (int i = 0; i < 17405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17405(int& val) {
    const int x = 17405;
    std::cout << 17405;
    for (int i = 0; i < 17405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17406(int& val) {
    func_in_17406(val);
    const int x = 17406;
    std::cout << 17406;
    for (int i = 0; i < 17406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17406(int& val) {
    const int x = 17406;
    std::cout << 17406;
    for (int i = 0; i < 17406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17407(int& val) {
    func_in_17407(val);
    const int x = 17407;
    std::cout << 17407;
    for (int i = 0; i < 17407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17407(int& val) {
    const int x = 17407;
    std::cout << 17407;
    for (int i = 0; i < 17407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17408(int& val) {
    func_in_17408(val);
    const int x = 17408;
    std::cout << 17408;
    for (int i = 0; i < 17408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17408(int& val) {
    const int x = 17408;
    std::cout << 17408;
    for (int i = 0; i < 17408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17409(int& val) {
    func_in_17409(val);
    const int x = 17409;
    std::cout << 17409;
    for (int i = 0; i < 17409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17409(int& val) {
    const int x = 17409;
    std::cout << 17409;
    for (int i = 0; i < 17409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17410(int& val) {
    func_in_17410(val);
    const int x = 17410;
    std::cout << 17410;
    for (int i = 0; i < 17410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17410(int& val) {
    const int x = 17410;
    std::cout << 17410;
    for (int i = 0; i < 17410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17411(int& val) {
    func_in_17411(val);
    const int x = 17411;
    std::cout << 17411;
    for (int i = 0; i < 17411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17411(int& val) {
    const int x = 17411;
    std::cout << 17411;
    for (int i = 0; i < 17411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17412(int& val) {
    func_in_17412(val);
    const int x = 17412;
    std::cout << 17412;
    for (int i = 0; i < 17412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17412(int& val) {
    const int x = 17412;
    std::cout << 17412;
    for (int i = 0; i < 17412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17413(int& val) {
    func_in_17413(val);
    const int x = 17413;
    std::cout << 17413;
    for (int i = 0; i < 17413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17413(int& val) {
    const int x = 17413;
    std::cout << 17413;
    for (int i = 0; i < 17413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17414(int& val) {
    func_in_17414(val);
    const int x = 17414;
    std::cout << 17414;
    for (int i = 0; i < 17414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17414(int& val) {
    const int x = 17414;
    std::cout << 17414;
    for (int i = 0; i < 17414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17415(int& val) {
    func_in_17415(val);
    const int x = 17415;
    std::cout << 17415;
    for (int i = 0; i < 17415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17415(int& val) {
    const int x = 17415;
    std::cout << 17415;
    for (int i = 0; i < 17415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17416(int& val) {
    func_in_17416(val);
    const int x = 17416;
    std::cout << 17416;
    for (int i = 0; i < 17416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17416(int& val) {
    const int x = 17416;
    std::cout << 17416;
    for (int i = 0; i < 17416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17417(int& val) {
    func_in_17417(val);
    const int x = 17417;
    std::cout << 17417;
    for (int i = 0; i < 17417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17417(int& val) {
    const int x = 17417;
    std::cout << 17417;
    for (int i = 0; i < 17417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17418(int& val) {
    func_in_17418(val);
    const int x = 17418;
    std::cout << 17418;
    for (int i = 0; i < 17418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17418(int& val) {
    const int x = 17418;
    std::cout << 17418;
    for (int i = 0; i < 17418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17419(int& val) {
    func_in_17419(val);
    const int x = 17419;
    std::cout << 17419;
    for (int i = 0; i < 17419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17419(int& val) {
    const int x = 17419;
    std::cout << 17419;
    for (int i = 0; i < 17419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17420(int& val) {
    func_in_17420(val);
    const int x = 17420;
    std::cout << 17420;
    for (int i = 0; i < 17420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17420(int& val) {
    const int x = 17420;
    std::cout << 17420;
    for (int i = 0; i < 17420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17421(int& val) {
    func_in_17421(val);
    const int x = 17421;
    std::cout << 17421;
    for (int i = 0; i < 17421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17421(int& val) {
    const int x = 17421;
    std::cout << 17421;
    for (int i = 0; i < 17421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17422(int& val) {
    func_in_17422(val);
    const int x = 17422;
    std::cout << 17422;
    for (int i = 0; i < 17422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17422(int& val) {
    const int x = 17422;
    std::cout << 17422;
    for (int i = 0; i < 17422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17423(int& val) {
    func_in_17423(val);
    const int x = 17423;
    std::cout << 17423;
    for (int i = 0; i < 17423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17423(int& val) {
    const int x = 17423;
    std::cout << 17423;
    for (int i = 0; i < 17423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17424(int& val) {
    func_in_17424(val);
    const int x = 17424;
    std::cout << 17424;
    for (int i = 0; i < 17424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17424(int& val) {
    const int x = 17424;
    std::cout << 17424;
    for (int i = 0; i < 17424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17425(int& val) {
    func_in_17425(val);
    const int x = 17425;
    std::cout << 17425;
    for (int i = 0; i < 17425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17425(int& val) {
    const int x = 17425;
    std::cout << 17425;
    for (int i = 0; i < 17425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17426(int& val) {
    func_in_17426(val);
    const int x = 17426;
    std::cout << 17426;
    for (int i = 0; i < 17426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17426(int& val) {
    const int x = 17426;
    std::cout << 17426;
    for (int i = 0; i < 17426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17427(int& val) {
    func_in_17427(val);
    const int x = 17427;
    std::cout << 17427;
    for (int i = 0; i < 17427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17427(int& val) {
    const int x = 17427;
    std::cout << 17427;
    for (int i = 0; i < 17427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17428(int& val) {
    func_in_17428(val);
    const int x = 17428;
    std::cout << 17428;
    for (int i = 0; i < 17428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17428(int& val) {
    const int x = 17428;
    std::cout << 17428;
    for (int i = 0; i < 17428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17429(int& val) {
    func_in_17429(val);
    const int x = 17429;
    std::cout << 17429;
    for (int i = 0; i < 17429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17429(int& val) {
    const int x = 17429;
    std::cout << 17429;
    for (int i = 0; i < 17429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17430(int& val) {
    func_in_17430(val);
    const int x = 17430;
    std::cout << 17430;
    for (int i = 0; i < 17430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17430(int& val) {
    const int x = 17430;
    std::cout << 17430;
    for (int i = 0; i < 17430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17431(int& val) {
    func_in_17431(val);
    const int x = 17431;
    std::cout << 17431;
    for (int i = 0; i < 17431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17431(int& val) {
    const int x = 17431;
    std::cout << 17431;
    for (int i = 0; i < 17431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17432(int& val) {
    func_in_17432(val);
    const int x = 17432;
    std::cout << 17432;
    for (int i = 0; i < 17432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17432(int& val) {
    const int x = 17432;
    std::cout << 17432;
    for (int i = 0; i < 17432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17433(int& val) {
    func_in_17433(val);
    const int x = 17433;
    std::cout << 17433;
    for (int i = 0; i < 17433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17433(int& val) {
    const int x = 17433;
    std::cout << 17433;
    for (int i = 0; i < 17433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17434(int& val) {
    func_in_17434(val);
    const int x = 17434;
    std::cout << 17434;
    for (int i = 0; i < 17434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17434(int& val) {
    const int x = 17434;
    std::cout << 17434;
    for (int i = 0; i < 17434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17435(int& val) {
    func_in_17435(val);
    const int x = 17435;
    std::cout << 17435;
    for (int i = 0; i < 17435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17435(int& val) {
    const int x = 17435;
    std::cout << 17435;
    for (int i = 0; i < 17435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17436(int& val) {
    func_in_17436(val);
    const int x = 17436;
    std::cout << 17436;
    for (int i = 0; i < 17436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17436(int& val) {
    const int x = 17436;
    std::cout << 17436;
    for (int i = 0; i < 17436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17437(int& val) {
    func_in_17437(val);
    const int x = 17437;
    std::cout << 17437;
    for (int i = 0; i < 17437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17437(int& val) {
    const int x = 17437;
    std::cout << 17437;
    for (int i = 0; i < 17437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17438(int& val) {
    func_in_17438(val);
    const int x = 17438;
    std::cout << 17438;
    for (int i = 0; i < 17438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17438(int& val) {
    const int x = 17438;
    std::cout << 17438;
    for (int i = 0; i < 17438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17439(int& val) {
    func_in_17439(val);
    const int x = 17439;
    std::cout << 17439;
    for (int i = 0; i < 17439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17439(int& val) {
    const int x = 17439;
    std::cout << 17439;
    for (int i = 0; i < 17439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17440(int& val) {
    func_in_17440(val);
    const int x = 17440;
    std::cout << 17440;
    for (int i = 0; i < 17440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17440(int& val) {
    const int x = 17440;
    std::cout << 17440;
    for (int i = 0; i < 17440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17441(int& val) {
    func_in_17441(val);
    const int x = 17441;
    std::cout << 17441;
    for (int i = 0; i < 17441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17441(int& val) {
    const int x = 17441;
    std::cout << 17441;
    for (int i = 0; i < 17441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17442(int& val) {
    func_in_17442(val);
    const int x = 17442;
    std::cout << 17442;
    for (int i = 0; i < 17442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17442(int& val) {
    const int x = 17442;
    std::cout << 17442;
    for (int i = 0; i < 17442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17443(int& val) {
    func_in_17443(val);
    const int x = 17443;
    std::cout << 17443;
    for (int i = 0; i < 17443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17443(int& val) {
    const int x = 17443;
    std::cout << 17443;
    for (int i = 0; i < 17443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17444(int& val) {
    func_in_17444(val);
    const int x = 17444;
    std::cout << 17444;
    for (int i = 0; i < 17444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17444(int& val) {
    const int x = 17444;
    std::cout << 17444;
    for (int i = 0; i < 17444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17445(int& val) {
    func_in_17445(val);
    const int x = 17445;
    std::cout << 17445;
    for (int i = 0; i < 17445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17445(int& val) {
    const int x = 17445;
    std::cout << 17445;
    for (int i = 0; i < 17445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17446(int& val) {
    func_in_17446(val);
    const int x = 17446;
    std::cout << 17446;
    for (int i = 0; i < 17446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17446(int& val) {
    const int x = 17446;
    std::cout << 17446;
    for (int i = 0; i < 17446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17447(int& val) {
    func_in_17447(val);
    const int x = 17447;
    std::cout << 17447;
    for (int i = 0; i < 17447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17447(int& val) {
    const int x = 17447;
    std::cout << 17447;
    for (int i = 0; i < 17447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17448(int& val) {
    func_in_17448(val);
    const int x = 17448;
    std::cout << 17448;
    for (int i = 0; i < 17448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17448(int& val) {
    const int x = 17448;
    std::cout << 17448;
    for (int i = 0; i < 17448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17449(int& val) {
    func_in_17449(val);
    const int x = 17449;
    std::cout << 17449;
    for (int i = 0; i < 17449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17449(int& val) {
    const int x = 17449;
    std::cout << 17449;
    for (int i = 0; i < 17449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17450(int& val) {
    func_in_17450(val);
    const int x = 17450;
    std::cout << 17450;
    for (int i = 0; i < 17450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17450(int& val) {
    const int x = 17450;
    std::cout << 17450;
    for (int i = 0; i < 17450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17451(int& val) {
    func_in_17451(val);
    const int x = 17451;
    std::cout << 17451;
    for (int i = 0; i < 17451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17451(int& val) {
    const int x = 17451;
    std::cout << 17451;
    for (int i = 0; i < 17451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17452(int& val) {
    func_in_17452(val);
    const int x = 17452;
    std::cout << 17452;
    for (int i = 0; i < 17452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17452(int& val) {
    const int x = 17452;
    std::cout << 17452;
    for (int i = 0; i < 17452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17453(int& val) {
    func_in_17453(val);
    const int x = 17453;
    std::cout << 17453;
    for (int i = 0; i < 17453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17453(int& val) {
    const int x = 17453;
    std::cout << 17453;
    for (int i = 0; i < 17453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17454(int& val) {
    func_in_17454(val);
    const int x = 17454;
    std::cout << 17454;
    for (int i = 0; i < 17454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17454(int& val) {
    const int x = 17454;
    std::cout << 17454;
    for (int i = 0; i < 17454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17455(int& val) {
    func_in_17455(val);
    const int x = 17455;
    std::cout << 17455;
    for (int i = 0; i < 17455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17455(int& val) {
    const int x = 17455;
    std::cout << 17455;
    for (int i = 0; i < 17455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17456(int& val) {
    func_in_17456(val);
    const int x = 17456;
    std::cout << 17456;
    for (int i = 0; i < 17456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17456(int& val) {
    const int x = 17456;
    std::cout << 17456;
    for (int i = 0; i < 17456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17457(int& val) {
    func_in_17457(val);
    const int x = 17457;
    std::cout << 17457;
    for (int i = 0; i < 17457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17457(int& val) {
    const int x = 17457;
    std::cout << 17457;
    for (int i = 0; i < 17457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17458(int& val) {
    func_in_17458(val);
    const int x = 17458;
    std::cout << 17458;
    for (int i = 0; i < 17458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17458(int& val) {
    const int x = 17458;
    std::cout << 17458;
    for (int i = 0; i < 17458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17459(int& val) {
    func_in_17459(val);
    const int x = 17459;
    std::cout << 17459;
    for (int i = 0; i < 17459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17459(int& val) {
    const int x = 17459;
    std::cout << 17459;
    for (int i = 0; i < 17459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17460(int& val) {
    func_in_17460(val);
    const int x = 17460;
    std::cout << 17460;
    for (int i = 0; i < 17460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17460(int& val) {
    const int x = 17460;
    std::cout << 17460;
    for (int i = 0; i < 17460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17461(int& val) {
    func_in_17461(val);
    const int x = 17461;
    std::cout << 17461;
    for (int i = 0; i < 17461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17461(int& val) {
    const int x = 17461;
    std::cout << 17461;
    for (int i = 0; i < 17461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17462(int& val) {
    func_in_17462(val);
    const int x = 17462;
    std::cout << 17462;
    for (int i = 0; i < 17462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17462(int& val) {
    const int x = 17462;
    std::cout << 17462;
    for (int i = 0; i < 17462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17463(int& val) {
    func_in_17463(val);
    const int x = 17463;
    std::cout << 17463;
    for (int i = 0; i < 17463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17463(int& val) {
    const int x = 17463;
    std::cout << 17463;
    for (int i = 0; i < 17463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17464(int& val) {
    func_in_17464(val);
    const int x = 17464;
    std::cout << 17464;
    for (int i = 0; i < 17464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17464(int& val) {
    const int x = 17464;
    std::cout << 17464;
    for (int i = 0; i < 17464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17465(int& val) {
    func_in_17465(val);
    const int x = 17465;
    std::cout << 17465;
    for (int i = 0; i < 17465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17465(int& val) {
    const int x = 17465;
    std::cout << 17465;
    for (int i = 0; i < 17465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17466(int& val) {
    func_in_17466(val);
    const int x = 17466;
    std::cout << 17466;
    for (int i = 0; i < 17466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17466(int& val) {
    const int x = 17466;
    std::cout << 17466;
    for (int i = 0; i < 17466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17467(int& val) {
    func_in_17467(val);
    const int x = 17467;
    std::cout << 17467;
    for (int i = 0; i < 17467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17467(int& val) {
    const int x = 17467;
    std::cout << 17467;
    for (int i = 0; i < 17467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17468(int& val) {
    func_in_17468(val);
    const int x = 17468;
    std::cout << 17468;
    for (int i = 0; i < 17468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17468(int& val) {
    const int x = 17468;
    std::cout << 17468;
    for (int i = 0; i < 17468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17469(int& val) {
    func_in_17469(val);
    const int x = 17469;
    std::cout << 17469;
    for (int i = 0; i < 17469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17469(int& val) {
    const int x = 17469;
    std::cout << 17469;
    for (int i = 0; i < 17469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17470(int& val) {
    func_in_17470(val);
    const int x = 17470;
    std::cout << 17470;
    for (int i = 0; i < 17470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17470(int& val) {
    const int x = 17470;
    std::cout << 17470;
    for (int i = 0; i < 17470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17471(int& val) {
    func_in_17471(val);
    const int x = 17471;
    std::cout << 17471;
    for (int i = 0; i < 17471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17471(int& val) {
    const int x = 17471;
    std::cout << 17471;
    for (int i = 0; i < 17471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17472(int& val) {
    func_in_17472(val);
    const int x = 17472;
    std::cout << 17472;
    for (int i = 0; i < 17472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17472(int& val) {
    const int x = 17472;
    std::cout << 17472;
    for (int i = 0; i < 17472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17473(int& val) {
    func_in_17473(val);
    const int x = 17473;
    std::cout << 17473;
    for (int i = 0; i < 17473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17473(int& val) {
    const int x = 17473;
    std::cout << 17473;
    for (int i = 0; i < 17473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17474(int& val) {
    func_in_17474(val);
    const int x = 17474;
    std::cout << 17474;
    for (int i = 0; i < 17474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17474(int& val) {
    const int x = 17474;
    std::cout << 17474;
    for (int i = 0; i < 17474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17475(int& val) {
    func_in_17475(val);
    const int x = 17475;
    std::cout << 17475;
    for (int i = 0; i < 17475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17475(int& val) {
    const int x = 17475;
    std::cout << 17475;
    for (int i = 0; i < 17475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17476(int& val) {
    func_in_17476(val);
    const int x = 17476;
    std::cout << 17476;
    for (int i = 0; i < 17476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17476(int& val) {
    const int x = 17476;
    std::cout << 17476;
    for (int i = 0; i < 17476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17477(int& val) {
    func_in_17477(val);
    const int x = 17477;
    std::cout << 17477;
    for (int i = 0; i < 17477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17477(int& val) {
    const int x = 17477;
    std::cout << 17477;
    for (int i = 0; i < 17477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17478(int& val) {
    func_in_17478(val);
    const int x = 17478;
    std::cout << 17478;
    for (int i = 0; i < 17478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17478(int& val) {
    const int x = 17478;
    std::cout << 17478;
    for (int i = 0; i < 17478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17479(int& val) {
    func_in_17479(val);
    const int x = 17479;
    std::cout << 17479;
    for (int i = 0; i < 17479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17479(int& val) {
    const int x = 17479;
    std::cout << 17479;
    for (int i = 0; i < 17479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17480(int& val) {
    func_in_17480(val);
    const int x = 17480;
    std::cout << 17480;
    for (int i = 0; i < 17480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17480(int& val) {
    const int x = 17480;
    std::cout << 17480;
    for (int i = 0; i < 17480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17481(int& val) {
    func_in_17481(val);
    const int x = 17481;
    std::cout << 17481;
    for (int i = 0; i < 17481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17481(int& val) {
    const int x = 17481;
    std::cout << 17481;
    for (int i = 0; i < 17481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17482(int& val) {
    func_in_17482(val);
    const int x = 17482;
    std::cout << 17482;
    for (int i = 0; i < 17482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17482(int& val) {
    const int x = 17482;
    std::cout << 17482;
    for (int i = 0; i < 17482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17483(int& val) {
    func_in_17483(val);
    const int x = 17483;
    std::cout << 17483;
    for (int i = 0; i < 17483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17483(int& val) {
    const int x = 17483;
    std::cout << 17483;
    for (int i = 0; i < 17483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17484(int& val) {
    func_in_17484(val);
    const int x = 17484;
    std::cout << 17484;
    for (int i = 0; i < 17484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17484(int& val) {
    const int x = 17484;
    std::cout << 17484;
    for (int i = 0; i < 17484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17485(int& val) {
    func_in_17485(val);
    const int x = 17485;
    std::cout << 17485;
    for (int i = 0; i < 17485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17485(int& val) {
    const int x = 17485;
    std::cout << 17485;
    for (int i = 0; i < 17485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17486(int& val) {
    func_in_17486(val);
    const int x = 17486;
    std::cout << 17486;
    for (int i = 0; i < 17486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17486(int& val) {
    const int x = 17486;
    std::cout << 17486;
    for (int i = 0; i < 17486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17487(int& val) {
    func_in_17487(val);
    const int x = 17487;
    std::cout << 17487;
    for (int i = 0; i < 17487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17487(int& val) {
    const int x = 17487;
    std::cout << 17487;
    for (int i = 0; i < 17487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17488(int& val) {
    func_in_17488(val);
    const int x = 17488;
    std::cout << 17488;
    for (int i = 0; i < 17488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17488(int& val) {
    const int x = 17488;
    std::cout << 17488;
    for (int i = 0; i < 17488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17489(int& val) {
    func_in_17489(val);
    const int x = 17489;
    std::cout << 17489;
    for (int i = 0; i < 17489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17489(int& val) {
    const int x = 17489;
    std::cout << 17489;
    for (int i = 0; i < 17489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17490(int& val) {
    func_in_17490(val);
    const int x = 17490;
    std::cout << 17490;
    for (int i = 0; i < 17490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17490(int& val) {
    const int x = 17490;
    std::cout << 17490;
    for (int i = 0; i < 17490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17491(int& val) {
    func_in_17491(val);
    const int x = 17491;
    std::cout << 17491;
    for (int i = 0; i < 17491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17491(int& val) {
    const int x = 17491;
    std::cout << 17491;
    for (int i = 0; i < 17491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17492(int& val) {
    func_in_17492(val);
    const int x = 17492;
    std::cout << 17492;
    for (int i = 0; i < 17492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17492(int& val) {
    const int x = 17492;
    std::cout << 17492;
    for (int i = 0; i < 17492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17493(int& val) {
    func_in_17493(val);
    const int x = 17493;
    std::cout << 17493;
    for (int i = 0; i < 17493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17493(int& val) {
    const int x = 17493;
    std::cout << 17493;
    for (int i = 0; i < 17493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17494(int& val) {
    func_in_17494(val);
    const int x = 17494;
    std::cout << 17494;
    for (int i = 0; i < 17494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17494(int& val) {
    const int x = 17494;
    std::cout << 17494;
    for (int i = 0; i < 17494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17495(int& val) {
    func_in_17495(val);
    const int x = 17495;
    std::cout << 17495;
    for (int i = 0; i < 17495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17495(int& val) {
    const int x = 17495;
    std::cout << 17495;
    for (int i = 0; i < 17495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17496(int& val) {
    func_in_17496(val);
    const int x = 17496;
    std::cout << 17496;
    for (int i = 0; i < 17496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17496(int& val) {
    const int x = 17496;
    std::cout << 17496;
    for (int i = 0; i < 17496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17497(int& val) {
    func_in_17497(val);
    const int x = 17497;
    std::cout << 17497;
    for (int i = 0; i < 17497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17497(int& val) {
    const int x = 17497;
    std::cout << 17497;
    for (int i = 0; i < 17497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17498(int& val) {
    func_in_17498(val);
    const int x = 17498;
    std::cout << 17498;
    for (int i = 0; i < 17498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17498(int& val) {
    const int x = 17498;
    std::cout << 17498;
    for (int i = 0; i < 17498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17499(int& val) {
    func_in_17499(val);
    const int x = 17499;
    std::cout << 17499;
    for (int i = 0; i < 17499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17499(int& val) {
    const int x = 17499;
    std::cout << 17499;
    for (int i = 0; i < 17499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17500(int& val) {
    func_in_17500(val);
    const int x = 17500;
    std::cout << 17500;
    for (int i = 0; i < 17500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17500(int& val) {
    const int x = 17500;
    std::cout << 17500;
    for (int i = 0; i < 17500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17501(int& val) {
    func_in_17501(val);
    const int x = 17501;
    std::cout << 17501;
    for (int i = 0; i < 17501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17501(int& val) {
    const int x = 17501;
    std::cout << 17501;
    for (int i = 0; i < 17501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17502(int& val) {
    func_in_17502(val);
    const int x = 17502;
    std::cout << 17502;
    for (int i = 0; i < 17502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17502(int& val) {
    const int x = 17502;
    std::cout << 17502;
    for (int i = 0; i < 17502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17503(int& val) {
    func_in_17503(val);
    const int x = 17503;
    std::cout << 17503;
    for (int i = 0; i < 17503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17503(int& val) {
    const int x = 17503;
    std::cout << 17503;
    for (int i = 0; i < 17503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17504(int& val) {
    func_in_17504(val);
    const int x = 17504;
    std::cout << 17504;
    for (int i = 0; i < 17504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17504(int& val) {
    const int x = 17504;
    std::cout << 17504;
    for (int i = 0; i < 17504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17505(int& val) {
    func_in_17505(val);
    const int x = 17505;
    std::cout << 17505;
    for (int i = 0; i < 17505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17505(int& val) {
    const int x = 17505;
    std::cout << 17505;
    for (int i = 0; i < 17505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17506(int& val) {
    func_in_17506(val);
    const int x = 17506;
    std::cout << 17506;
    for (int i = 0; i < 17506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17506(int& val) {
    const int x = 17506;
    std::cout << 17506;
    for (int i = 0; i < 17506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17507(int& val) {
    func_in_17507(val);
    const int x = 17507;
    std::cout << 17507;
    for (int i = 0; i < 17507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17507(int& val) {
    const int x = 17507;
    std::cout << 17507;
    for (int i = 0; i < 17507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17508(int& val) {
    func_in_17508(val);
    const int x = 17508;
    std::cout << 17508;
    for (int i = 0; i < 17508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17508(int& val) {
    const int x = 17508;
    std::cout << 17508;
    for (int i = 0; i < 17508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17509(int& val) {
    func_in_17509(val);
    const int x = 17509;
    std::cout << 17509;
    for (int i = 0; i < 17509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17509(int& val) {
    const int x = 17509;
    std::cout << 17509;
    for (int i = 0; i < 17509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17510(int& val) {
    func_in_17510(val);
    const int x = 17510;
    std::cout << 17510;
    for (int i = 0; i < 17510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17510(int& val) {
    const int x = 17510;
    std::cout << 17510;
    for (int i = 0; i < 17510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17511(int& val) {
    func_in_17511(val);
    const int x = 17511;
    std::cout << 17511;
    for (int i = 0; i < 17511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17511(int& val) {
    const int x = 17511;
    std::cout << 17511;
    for (int i = 0; i < 17511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17512(int& val) {
    func_in_17512(val);
    const int x = 17512;
    std::cout << 17512;
    for (int i = 0; i < 17512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17512(int& val) {
    const int x = 17512;
    std::cout << 17512;
    for (int i = 0; i < 17512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17513(int& val) {
    func_in_17513(val);
    const int x = 17513;
    std::cout << 17513;
    for (int i = 0; i < 17513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17513(int& val) {
    const int x = 17513;
    std::cout << 17513;
    for (int i = 0; i < 17513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17514(int& val) {
    func_in_17514(val);
    const int x = 17514;
    std::cout << 17514;
    for (int i = 0; i < 17514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17514(int& val) {
    const int x = 17514;
    std::cout << 17514;
    for (int i = 0; i < 17514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17515(int& val) {
    func_in_17515(val);
    const int x = 17515;
    std::cout << 17515;
    for (int i = 0; i < 17515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17515(int& val) {
    const int x = 17515;
    std::cout << 17515;
    for (int i = 0; i < 17515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17516(int& val) {
    func_in_17516(val);
    const int x = 17516;
    std::cout << 17516;
    for (int i = 0; i < 17516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17516(int& val) {
    const int x = 17516;
    std::cout << 17516;
    for (int i = 0; i < 17516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17517(int& val) {
    func_in_17517(val);
    const int x = 17517;
    std::cout << 17517;
    for (int i = 0; i < 17517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17517(int& val) {
    const int x = 17517;
    std::cout << 17517;
    for (int i = 0; i < 17517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17518(int& val) {
    func_in_17518(val);
    const int x = 17518;
    std::cout << 17518;
    for (int i = 0; i < 17518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17518(int& val) {
    const int x = 17518;
    std::cout << 17518;
    for (int i = 0; i < 17518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17519(int& val) {
    func_in_17519(val);
    const int x = 17519;
    std::cout << 17519;
    for (int i = 0; i < 17519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17519(int& val) {
    const int x = 17519;
    std::cout << 17519;
    for (int i = 0; i < 17519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17520(int& val) {
    func_in_17520(val);
    const int x = 17520;
    std::cout << 17520;
    for (int i = 0; i < 17520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17520(int& val) {
    const int x = 17520;
    std::cout << 17520;
    for (int i = 0; i < 17520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17521(int& val) {
    func_in_17521(val);
    const int x = 17521;
    std::cout << 17521;
    for (int i = 0; i < 17521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17521(int& val) {
    const int x = 17521;
    std::cout << 17521;
    for (int i = 0; i < 17521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17522(int& val) {
    func_in_17522(val);
    const int x = 17522;
    std::cout << 17522;
    for (int i = 0; i < 17522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17522(int& val) {
    const int x = 17522;
    std::cout << 17522;
    for (int i = 0; i < 17522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17523(int& val) {
    func_in_17523(val);
    const int x = 17523;
    std::cout << 17523;
    for (int i = 0; i < 17523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17523(int& val) {
    const int x = 17523;
    std::cout << 17523;
    for (int i = 0; i < 17523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17524(int& val) {
    func_in_17524(val);
    const int x = 17524;
    std::cout << 17524;
    for (int i = 0; i < 17524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17524(int& val) {
    const int x = 17524;
    std::cout << 17524;
    for (int i = 0; i < 17524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17525(int& val) {
    func_in_17525(val);
    const int x = 17525;
    std::cout << 17525;
    for (int i = 0; i < 17525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17525(int& val) {
    const int x = 17525;
    std::cout << 17525;
    for (int i = 0; i < 17525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17526(int& val) {
    func_in_17526(val);
    const int x = 17526;
    std::cout << 17526;
    for (int i = 0; i < 17526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17526(int& val) {
    const int x = 17526;
    std::cout << 17526;
    for (int i = 0; i < 17526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17527(int& val) {
    func_in_17527(val);
    const int x = 17527;
    std::cout << 17527;
    for (int i = 0; i < 17527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17527(int& val) {
    const int x = 17527;
    std::cout << 17527;
    for (int i = 0; i < 17527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17528(int& val) {
    func_in_17528(val);
    const int x = 17528;
    std::cout << 17528;
    for (int i = 0; i < 17528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17528(int& val) {
    const int x = 17528;
    std::cout << 17528;
    for (int i = 0; i < 17528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17529(int& val) {
    func_in_17529(val);
    const int x = 17529;
    std::cout << 17529;
    for (int i = 0; i < 17529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17529(int& val) {
    const int x = 17529;
    std::cout << 17529;
    for (int i = 0; i < 17529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17530(int& val) {
    func_in_17530(val);
    const int x = 17530;
    std::cout << 17530;
    for (int i = 0; i < 17530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17530(int& val) {
    const int x = 17530;
    std::cout << 17530;
    for (int i = 0; i < 17530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17531(int& val) {
    func_in_17531(val);
    const int x = 17531;
    std::cout << 17531;
    for (int i = 0; i < 17531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17531(int& val) {
    const int x = 17531;
    std::cout << 17531;
    for (int i = 0; i < 17531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17532(int& val) {
    func_in_17532(val);
    const int x = 17532;
    std::cout << 17532;
    for (int i = 0; i < 17532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17532(int& val) {
    const int x = 17532;
    std::cout << 17532;
    for (int i = 0; i < 17532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17533(int& val) {
    func_in_17533(val);
    const int x = 17533;
    std::cout << 17533;
    for (int i = 0; i < 17533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17533(int& val) {
    const int x = 17533;
    std::cout << 17533;
    for (int i = 0; i < 17533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17534(int& val) {
    func_in_17534(val);
    const int x = 17534;
    std::cout << 17534;
    for (int i = 0; i < 17534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17534(int& val) {
    const int x = 17534;
    std::cout << 17534;
    for (int i = 0; i < 17534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17535(int& val) {
    func_in_17535(val);
    const int x = 17535;
    std::cout << 17535;
    for (int i = 0; i < 17535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17535(int& val) {
    const int x = 17535;
    std::cout << 17535;
    for (int i = 0; i < 17535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17536(int& val) {
    func_in_17536(val);
    const int x = 17536;
    std::cout << 17536;
    for (int i = 0; i < 17536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17536(int& val) {
    const int x = 17536;
    std::cout << 17536;
    for (int i = 0; i < 17536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17537(int& val) {
    func_in_17537(val);
    const int x = 17537;
    std::cout << 17537;
    for (int i = 0; i < 17537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17537(int& val) {
    const int x = 17537;
    std::cout << 17537;
    for (int i = 0; i < 17537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17538(int& val) {
    func_in_17538(val);
    const int x = 17538;
    std::cout << 17538;
    for (int i = 0; i < 17538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17538(int& val) {
    const int x = 17538;
    std::cout << 17538;
    for (int i = 0; i < 17538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17539(int& val) {
    func_in_17539(val);
    const int x = 17539;
    std::cout << 17539;
    for (int i = 0; i < 17539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17539(int& val) {
    const int x = 17539;
    std::cout << 17539;
    for (int i = 0; i < 17539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17540(int& val) {
    func_in_17540(val);
    const int x = 17540;
    std::cout << 17540;
    for (int i = 0; i < 17540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17540(int& val) {
    const int x = 17540;
    std::cout << 17540;
    for (int i = 0; i < 17540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17541(int& val) {
    func_in_17541(val);
    const int x = 17541;
    std::cout << 17541;
    for (int i = 0; i < 17541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17541(int& val) {
    const int x = 17541;
    std::cout << 17541;
    for (int i = 0; i < 17541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17542(int& val) {
    func_in_17542(val);
    const int x = 17542;
    std::cout << 17542;
    for (int i = 0; i < 17542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17542(int& val) {
    const int x = 17542;
    std::cout << 17542;
    for (int i = 0; i < 17542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17543(int& val) {
    func_in_17543(val);
    const int x = 17543;
    std::cout << 17543;
    for (int i = 0; i < 17543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17543(int& val) {
    const int x = 17543;
    std::cout << 17543;
    for (int i = 0; i < 17543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17544(int& val) {
    func_in_17544(val);
    const int x = 17544;
    std::cout << 17544;
    for (int i = 0; i < 17544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17544(int& val) {
    const int x = 17544;
    std::cout << 17544;
    for (int i = 0; i < 17544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17545(int& val) {
    func_in_17545(val);
    const int x = 17545;
    std::cout << 17545;
    for (int i = 0; i < 17545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17545(int& val) {
    const int x = 17545;
    std::cout << 17545;
    for (int i = 0; i < 17545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17546(int& val) {
    func_in_17546(val);
    const int x = 17546;
    std::cout << 17546;
    for (int i = 0; i < 17546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17546(int& val) {
    const int x = 17546;
    std::cout << 17546;
    for (int i = 0; i < 17546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17547(int& val) {
    func_in_17547(val);
    const int x = 17547;
    std::cout << 17547;
    for (int i = 0; i < 17547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17547(int& val) {
    const int x = 17547;
    std::cout << 17547;
    for (int i = 0; i < 17547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17548(int& val) {
    func_in_17548(val);
    const int x = 17548;
    std::cout << 17548;
    for (int i = 0; i < 17548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17548(int& val) {
    const int x = 17548;
    std::cout << 17548;
    for (int i = 0; i < 17548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17549(int& val) {
    func_in_17549(val);
    const int x = 17549;
    std::cout << 17549;
    for (int i = 0; i < 17549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17549(int& val) {
    const int x = 17549;
    std::cout << 17549;
    for (int i = 0; i < 17549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17550(int& val) {
    func_in_17550(val);
    const int x = 17550;
    std::cout << 17550;
    for (int i = 0; i < 17550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17550(int& val) {
    const int x = 17550;
    std::cout << 17550;
    for (int i = 0; i < 17550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17551(int& val) {
    func_in_17551(val);
    const int x = 17551;
    std::cout << 17551;
    for (int i = 0; i < 17551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17551(int& val) {
    const int x = 17551;
    std::cout << 17551;
    for (int i = 0; i < 17551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17552(int& val) {
    func_in_17552(val);
    const int x = 17552;
    std::cout << 17552;
    for (int i = 0; i < 17552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17552(int& val) {
    const int x = 17552;
    std::cout << 17552;
    for (int i = 0; i < 17552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17553(int& val) {
    func_in_17553(val);
    const int x = 17553;
    std::cout << 17553;
    for (int i = 0; i < 17553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17553(int& val) {
    const int x = 17553;
    std::cout << 17553;
    for (int i = 0; i < 17553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17554(int& val) {
    func_in_17554(val);
    const int x = 17554;
    std::cout << 17554;
    for (int i = 0; i < 17554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17554(int& val) {
    const int x = 17554;
    std::cout << 17554;
    for (int i = 0; i < 17554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17555(int& val) {
    func_in_17555(val);
    const int x = 17555;
    std::cout << 17555;
    for (int i = 0; i < 17555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17555(int& val) {
    const int x = 17555;
    std::cout << 17555;
    for (int i = 0; i < 17555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17556(int& val) {
    func_in_17556(val);
    const int x = 17556;
    std::cout << 17556;
    for (int i = 0; i < 17556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17556(int& val) {
    const int x = 17556;
    std::cout << 17556;
    for (int i = 0; i < 17556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17557(int& val) {
    func_in_17557(val);
    const int x = 17557;
    std::cout << 17557;
    for (int i = 0; i < 17557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17557(int& val) {
    const int x = 17557;
    std::cout << 17557;
    for (int i = 0; i < 17557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17558(int& val) {
    func_in_17558(val);
    const int x = 17558;
    std::cout << 17558;
    for (int i = 0; i < 17558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17558(int& val) {
    const int x = 17558;
    std::cout << 17558;
    for (int i = 0; i < 17558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17559(int& val) {
    func_in_17559(val);
    const int x = 17559;
    std::cout << 17559;
    for (int i = 0; i < 17559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17559(int& val) {
    const int x = 17559;
    std::cout << 17559;
    for (int i = 0; i < 17559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17560(int& val) {
    func_in_17560(val);
    const int x = 17560;
    std::cout << 17560;
    for (int i = 0; i < 17560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17560(int& val) {
    const int x = 17560;
    std::cout << 17560;
    for (int i = 0; i < 17560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17561(int& val) {
    func_in_17561(val);
    const int x = 17561;
    std::cout << 17561;
    for (int i = 0; i < 17561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17561(int& val) {
    const int x = 17561;
    std::cout << 17561;
    for (int i = 0; i < 17561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17562(int& val) {
    func_in_17562(val);
    const int x = 17562;
    std::cout << 17562;
    for (int i = 0; i < 17562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17562(int& val) {
    const int x = 17562;
    std::cout << 17562;
    for (int i = 0; i < 17562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17563(int& val) {
    func_in_17563(val);
    const int x = 17563;
    std::cout << 17563;
    for (int i = 0; i < 17563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17563(int& val) {
    const int x = 17563;
    std::cout << 17563;
    for (int i = 0; i < 17563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17564(int& val) {
    func_in_17564(val);
    const int x = 17564;
    std::cout << 17564;
    for (int i = 0; i < 17564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17564(int& val) {
    const int x = 17564;
    std::cout << 17564;
    for (int i = 0; i < 17564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17565(int& val) {
    func_in_17565(val);
    const int x = 17565;
    std::cout << 17565;
    for (int i = 0; i < 17565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17565(int& val) {
    const int x = 17565;
    std::cout << 17565;
    for (int i = 0; i < 17565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17566(int& val) {
    func_in_17566(val);
    const int x = 17566;
    std::cout << 17566;
    for (int i = 0; i < 17566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17566(int& val) {
    const int x = 17566;
    std::cout << 17566;
    for (int i = 0; i < 17566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17567(int& val) {
    func_in_17567(val);
    const int x = 17567;
    std::cout << 17567;
    for (int i = 0; i < 17567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17567(int& val) {
    const int x = 17567;
    std::cout << 17567;
    for (int i = 0; i < 17567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17568(int& val) {
    func_in_17568(val);
    const int x = 17568;
    std::cout << 17568;
    for (int i = 0; i < 17568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17568(int& val) {
    const int x = 17568;
    std::cout << 17568;
    for (int i = 0; i < 17568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17569(int& val) {
    func_in_17569(val);
    const int x = 17569;
    std::cout << 17569;
    for (int i = 0; i < 17569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17569(int& val) {
    const int x = 17569;
    std::cout << 17569;
    for (int i = 0; i < 17569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17570(int& val) {
    func_in_17570(val);
    const int x = 17570;
    std::cout << 17570;
    for (int i = 0; i < 17570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17570(int& val) {
    const int x = 17570;
    std::cout << 17570;
    for (int i = 0; i < 17570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17571(int& val) {
    func_in_17571(val);
    const int x = 17571;
    std::cout << 17571;
    for (int i = 0; i < 17571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17571(int& val) {
    const int x = 17571;
    std::cout << 17571;
    for (int i = 0; i < 17571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17572(int& val) {
    func_in_17572(val);
    const int x = 17572;
    std::cout << 17572;
    for (int i = 0; i < 17572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17572(int& val) {
    const int x = 17572;
    std::cout << 17572;
    for (int i = 0; i < 17572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17573(int& val) {
    func_in_17573(val);
    const int x = 17573;
    std::cout << 17573;
    for (int i = 0; i < 17573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17573(int& val) {
    const int x = 17573;
    std::cout << 17573;
    for (int i = 0; i < 17573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17574(int& val) {
    func_in_17574(val);
    const int x = 17574;
    std::cout << 17574;
    for (int i = 0; i < 17574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17574(int& val) {
    const int x = 17574;
    std::cout << 17574;
    for (int i = 0; i < 17574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17575(int& val) {
    func_in_17575(val);
    const int x = 17575;
    std::cout << 17575;
    for (int i = 0; i < 17575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17575(int& val) {
    const int x = 17575;
    std::cout << 17575;
    for (int i = 0; i < 17575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17576(int& val) {
    func_in_17576(val);
    const int x = 17576;
    std::cout << 17576;
    for (int i = 0; i < 17576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17576(int& val) {
    const int x = 17576;
    std::cout << 17576;
    for (int i = 0; i < 17576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17577(int& val) {
    func_in_17577(val);
    const int x = 17577;
    std::cout << 17577;
    for (int i = 0; i < 17577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17577(int& val) {
    const int x = 17577;
    std::cout << 17577;
    for (int i = 0; i < 17577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17578(int& val) {
    func_in_17578(val);
    const int x = 17578;
    std::cout << 17578;
    for (int i = 0; i < 17578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17578(int& val) {
    const int x = 17578;
    std::cout << 17578;
    for (int i = 0; i < 17578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17579(int& val) {
    func_in_17579(val);
    const int x = 17579;
    std::cout << 17579;
    for (int i = 0; i < 17579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17579(int& val) {
    const int x = 17579;
    std::cout << 17579;
    for (int i = 0; i < 17579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17580(int& val) {
    func_in_17580(val);
    const int x = 17580;
    std::cout << 17580;
    for (int i = 0; i < 17580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17580(int& val) {
    const int x = 17580;
    std::cout << 17580;
    for (int i = 0; i < 17580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17581(int& val) {
    func_in_17581(val);
    const int x = 17581;
    std::cout << 17581;
    for (int i = 0; i < 17581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17581(int& val) {
    const int x = 17581;
    std::cout << 17581;
    for (int i = 0; i < 17581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17582(int& val) {
    func_in_17582(val);
    const int x = 17582;
    std::cout << 17582;
    for (int i = 0; i < 17582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17582(int& val) {
    const int x = 17582;
    std::cout << 17582;
    for (int i = 0; i < 17582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17583(int& val) {
    func_in_17583(val);
    const int x = 17583;
    std::cout << 17583;
    for (int i = 0; i < 17583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17583(int& val) {
    const int x = 17583;
    std::cout << 17583;
    for (int i = 0; i < 17583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17584(int& val) {
    func_in_17584(val);
    const int x = 17584;
    std::cout << 17584;
    for (int i = 0; i < 17584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17584(int& val) {
    const int x = 17584;
    std::cout << 17584;
    for (int i = 0; i < 17584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17585(int& val) {
    func_in_17585(val);
    const int x = 17585;
    std::cout << 17585;
    for (int i = 0; i < 17585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17585(int& val) {
    const int x = 17585;
    std::cout << 17585;
    for (int i = 0; i < 17585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17586(int& val) {
    func_in_17586(val);
    const int x = 17586;
    std::cout << 17586;
    for (int i = 0; i < 17586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17586(int& val) {
    const int x = 17586;
    std::cout << 17586;
    for (int i = 0; i < 17586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17587(int& val) {
    func_in_17587(val);
    const int x = 17587;
    std::cout << 17587;
    for (int i = 0; i < 17587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17587(int& val) {
    const int x = 17587;
    std::cout << 17587;
    for (int i = 0; i < 17587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17588(int& val) {
    func_in_17588(val);
    const int x = 17588;
    std::cout << 17588;
    for (int i = 0; i < 17588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17588(int& val) {
    const int x = 17588;
    std::cout << 17588;
    for (int i = 0; i < 17588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17589(int& val) {
    func_in_17589(val);
    const int x = 17589;
    std::cout << 17589;
    for (int i = 0; i < 17589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17589(int& val) {
    const int x = 17589;
    std::cout << 17589;
    for (int i = 0; i < 17589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17590(int& val) {
    func_in_17590(val);
    const int x = 17590;
    std::cout << 17590;
    for (int i = 0; i < 17590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17590(int& val) {
    const int x = 17590;
    std::cout << 17590;
    for (int i = 0; i < 17590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17591(int& val) {
    func_in_17591(val);
    const int x = 17591;
    std::cout << 17591;
    for (int i = 0; i < 17591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17591(int& val) {
    const int x = 17591;
    std::cout << 17591;
    for (int i = 0; i < 17591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17592(int& val) {
    func_in_17592(val);
    const int x = 17592;
    std::cout << 17592;
    for (int i = 0; i < 17592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17592(int& val) {
    const int x = 17592;
    std::cout << 17592;
    for (int i = 0; i < 17592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17593(int& val) {
    func_in_17593(val);
    const int x = 17593;
    std::cout << 17593;
    for (int i = 0; i < 17593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17593(int& val) {
    const int x = 17593;
    std::cout << 17593;
    for (int i = 0; i < 17593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17594(int& val) {
    func_in_17594(val);
    const int x = 17594;
    std::cout << 17594;
    for (int i = 0; i < 17594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17594(int& val) {
    const int x = 17594;
    std::cout << 17594;
    for (int i = 0; i < 17594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17595(int& val) {
    func_in_17595(val);
    const int x = 17595;
    std::cout << 17595;
    for (int i = 0; i < 17595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17595(int& val) {
    const int x = 17595;
    std::cout << 17595;
    for (int i = 0; i < 17595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17596(int& val) {
    func_in_17596(val);
    const int x = 17596;
    std::cout << 17596;
    for (int i = 0; i < 17596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17596(int& val) {
    const int x = 17596;
    std::cout << 17596;
    for (int i = 0; i < 17596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17597(int& val) {
    func_in_17597(val);
    const int x = 17597;
    std::cout << 17597;
    for (int i = 0; i < 17597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17597(int& val) {
    const int x = 17597;
    std::cout << 17597;
    for (int i = 0; i < 17597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17598(int& val) {
    func_in_17598(val);
    const int x = 17598;
    std::cout << 17598;
    for (int i = 0; i < 17598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17598(int& val) {
    const int x = 17598;
    std::cout << 17598;
    for (int i = 0; i < 17598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17599(int& val) {
    func_in_17599(val);
    const int x = 17599;
    std::cout << 17599;
    for (int i = 0; i < 17599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17599(int& val) {
    const int x = 17599;
    std::cout << 17599;
    for (int i = 0; i < 17599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17600(int& val) {
    func_in_17600(val);
    const int x = 17600;
    std::cout << 17600;
    for (int i = 0; i < 17600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17600(int& val) {
    const int x = 17600;
    std::cout << 17600;
    for (int i = 0; i < 17600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17601(int& val) {
    func_in_17601(val);
    const int x = 17601;
    std::cout << 17601;
    for (int i = 0; i < 17601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17601(int& val) {
    const int x = 17601;
    std::cout << 17601;
    for (int i = 0; i < 17601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17602(int& val) {
    func_in_17602(val);
    const int x = 17602;
    std::cout << 17602;
    for (int i = 0; i < 17602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17602(int& val) {
    const int x = 17602;
    std::cout << 17602;
    for (int i = 0; i < 17602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17603(int& val) {
    func_in_17603(val);
    const int x = 17603;
    std::cout << 17603;
    for (int i = 0; i < 17603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17603(int& val) {
    const int x = 17603;
    std::cout << 17603;
    for (int i = 0; i < 17603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17604(int& val) {
    func_in_17604(val);
    const int x = 17604;
    std::cout << 17604;
    for (int i = 0; i < 17604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17604(int& val) {
    const int x = 17604;
    std::cout << 17604;
    for (int i = 0; i < 17604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17605(int& val) {
    func_in_17605(val);
    const int x = 17605;
    std::cout << 17605;
    for (int i = 0; i < 17605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17605(int& val) {
    const int x = 17605;
    std::cout << 17605;
    for (int i = 0; i < 17605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17606(int& val) {
    func_in_17606(val);
    const int x = 17606;
    std::cout << 17606;
    for (int i = 0; i < 17606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17606(int& val) {
    const int x = 17606;
    std::cout << 17606;
    for (int i = 0; i < 17606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17607(int& val) {
    func_in_17607(val);
    const int x = 17607;
    std::cout << 17607;
    for (int i = 0; i < 17607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17607(int& val) {
    const int x = 17607;
    std::cout << 17607;
    for (int i = 0; i < 17607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17608(int& val) {
    func_in_17608(val);
    const int x = 17608;
    std::cout << 17608;
    for (int i = 0; i < 17608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17608(int& val) {
    const int x = 17608;
    std::cout << 17608;
    for (int i = 0; i < 17608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17609(int& val) {
    func_in_17609(val);
    const int x = 17609;
    std::cout << 17609;
    for (int i = 0; i < 17609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17609(int& val) {
    const int x = 17609;
    std::cout << 17609;
    for (int i = 0; i < 17609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17610(int& val) {
    func_in_17610(val);
    const int x = 17610;
    std::cout << 17610;
    for (int i = 0; i < 17610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17610(int& val) {
    const int x = 17610;
    std::cout << 17610;
    for (int i = 0; i < 17610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17611(int& val) {
    func_in_17611(val);
    const int x = 17611;
    std::cout << 17611;
    for (int i = 0; i < 17611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17611(int& val) {
    const int x = 17611;
    std::cout << 17611;
    for (int i = 0; i < 17611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17612(int& val) {
    func_in_17612(val);
    const int x = 17612;
    std::cout << 17612;
    for (int i = 0; i < 17612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17612(int& val) {
    const int x = 17612;
    std::cout << 17612;
    for (int i = 0; i < 17612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17613(int& val) {
    func_in_17613(val);
    const int x = 17613;
    std::cout << 17613;
    for (int i = 0; i < 17613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17613(int& val) {
    const int x = 17613;
    std::cout << 17613;
    for (int i = 0; i < 17613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17614(int& val) {
    func_in_17614(val);
    const int x = 17614;
    std::cout << 17614;
    for (int i = 0; i < 17614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17614(int& val) {
    const int x = 17614;
    std::cout << 17614;
    for (int i = 0; i < 17614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17615(int& val) {
    func_in_17615(val);
    const int x = 17615;
    std::cout << 17615;
    for (int i = 0; i < 17615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17615(int& val) {
    const int x = 17615;
    std::cout << 17615;
    for (int i = 0; i < 17615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17616(int& val) {
    func_in_17616(val);
    const int x = 17616;
    std::cout << 17616;
    for (int i = 0; i < 17616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17616(int& val) {
    const int x = 17616;
    std::cout << 17616;
    for (int i = 0; i < 17616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17617(int& val) {
    func_in_17617(val);
    const int x = 17617;
    std::cout << 17617;
    for (int i = 0; i < 17617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17617(int& val) {
    const int x = 17617;
    std::cout << 17617;
    for (int i = 0; i < 17617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17618(int& val) {
    func_in_17618(val);
    const int x = 17618;
    std::cout << 17618;
    for (int i = 0; i < 17618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17618(int& val) {
    const int x = 17618;
    std::cout << 17618;
    for (int i = 0; i < 17618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17619(int& val) {
    func_in_17619(val);
    const int x = 17619;
    std::cout << 17619;
    for (int i = 0; i < 17619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17619(int& val) {
    const int x = 17619;
    std::cout << 17619;
    for (int i = 0; i < 17619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17620(int& val) {
    func_in_17620(val);
    const int x = 17620;
    std::cout << 17620;
    for (int i = 0; i < 17620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17620(int& val) {
    const int x = 17620;
    std::cout << 17620;
    for (int i = 0; i < 17620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17621(int& val) {
    func_in_17621(val);
    const int x = 17621;
    std::cout << 17621;
    for (int i = 0; i < 17621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17621(int& val) {
    const int x = 17621;
    std::cout << 17621;
    for (int i = 0; i < 17621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17622(int& val) {
    func_in_17622(val);
    const int x = 17622;
    std::cout << 17622;
    for (int i = 0; i < 17622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17622(int& val) {
    const int x = 17622;
    std::cout << 17622;
    for (int i = 0; i < 17622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17623(int& val) {
    func_in_17623(val);
    const int x = 17623;
    std::cout << 17623;
    for (int i = 0; i < 17623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17623(int& val) {
    const int x = 17623;
    std::cout << 17623;
    for (int i = 0; i < 17623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17624(int& val) {
    func_in_17624(val);
    const int x = 17624;
    std::cout << 17624;
    for (int i = 0; i < 17624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17624(int& val) {
    const int x = 17624;
    std::cout << 17624;
    for (int i = 0; i < 17624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17625(int& val) {
    func_in_17625(val);
    const int x = 17625;
    std::cout << 17625;
    for (int i = 0; i < 17625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17625(int& val) {
    const int x = 17625;
    std::cout << 17625;
    for (int i = 0; i < 17625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17626(int& val) {
    func_in_17626(val);
    const int x = 17626;
    std::cout << 17626;
    for (int i = 0; i < 17626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17626(int& val) {
    const int x = 17626;
    std::cout << 17626;
    for (int i = 0; i < 17626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17627(int& val) {
    func_in_17627(val);
    const int x = 17627;
    std::cout << 17627;
    for (int i = 0; i < 17627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17627(int& val) {
    const int x = 17627;
    std::cout << 17627;
    for (int i = 0; i < 17627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17628(int& val) {
    func_in_17628(val);
    const int x = 17628;
    std::cout << 17628;
    for (int i = 0; i < 17628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17628(int& val) {
    const int x = 17628;
    std::cout << 17628;
    for (int i = 0; i < 17628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17629(int& val) {
    func_in_17629(val);
    const int x = 17629;
    std::cout << 17629;
    for (int i = 0; i < 17629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17629(int& val) {
    const int x = 17629;
    std::cout << 17629;
    for (int i = 0; i < 17629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17630(int& val) {
    func_in_17630(val);
    const int x = 17630;
    std::cout << 17630;
    for (int i = 0; i < 17630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17630(int& val) {
    const int x = 17630;
    std::cout << 17630;
    for (int i = 0; i < 17630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17631(int& val) {
    func_in_17631(val);
    const int x = 17631;
    std::cout << 17631;
    for (int i = 0; i < 17631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17631(int& val) {
    const int x = 17631;
    std::cout << 17631;
    for (int i = 0; i < 17631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17632(int& val) {
    func_in_17632(val);
    const int x = 17632;
    std::cout << 17632;
    for (int i = 0; i < 17632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17632(int& val) {
    const int x = 17632;
    std::cout << 17632;
    for (int i = 0; i < 17632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17633(int& val) {
    func_in_17633(val);
    const int x = 17633;
    std::cout << 17633;
    for (int i = 0; i < 17633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17633(int& val) {
    const int x = 17633;
    std::cout << 17633;
    for (int i = 0; i < 17633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17634(int& val) {
    func_in_17634(val);
    const int x = 17634;
    std::cout << 17634;
    for (int i = 0; i < 17634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17634(int& val) {
    const int x = 17634;
    std::cout << 17634;
    for (int i = 0; i < 17634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17635(int& val) {
    func_in_17635(val);
    const int x = 17635;
    std::cout << 17635;
    for (int i = 0; i < 17635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17635(int& val) {
    const int x = 17635;
    std::cout << 17635;
    for (int i = 0; i < 17635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17636(int& val) {
    func_in_17636(val);
    const int x = 17636;
    std::cout << 17636;
    for (int i = 0; i < 17636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17636(int& val) {
    const int x = 17636;
    std::cout << 17636;
    for (int i = 0; i < 17636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17637(int& val) {
    func_in_17637(val);
    const int x = 17637;
    std::cout << 17637;
    for (int i = 0; i < 17637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17637(int& val) {
    const int x = 17637;
    std::cout << 17637;
    for (int i = 0; i < 17637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17638(int& val) {
    func_in_17638(val);
    const int x = 17638;
    std::cout << 17638;
    for (int i = 0; i < 17638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17638(int& val) {
    const int x = 17638;
    std::cout << 17638;
    for (int i = 0; i < 17638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17639(int& val) {
    func_in_17639(val);
    const int x = 17639;
    std::cout << 17639;
    for (int i = 0; i < 17639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17639(int& val) {
    const int x = 17639;
    std::cout << 17639;
    for (int i = 0; i < 17639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17640(int& val) {
    func_in_17640(val);
    const int x = 17640;
    std::cout << 17640;
    for (int i = 0; i < 17640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17640(int& val) {
    const int x = 17640;
    std::cout << 17640;
    for (int i = 0; i < 17640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17641(int& val) {
    func_in_17641(val);
    const int x = 17641;
    std::cout << 17641;
    for (int i = 0; i < 17641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17641(int& val) {
    const int x = 17641;
    std::cout << 17641;
    for (int i = 0; i < 17641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17642(int& val) {
    func_in_17642(val);
    const int x = 17642;
    std::cout << 17642;
    for (int i = 0; i < 17642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17642(int& val) {
    const int x = 17642;
    std::cout << 17642;
    for (int i = 0; i < 17642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17643(int& val) {
    func_in_17643(val);
    const int x = 17643;
    std::cout << 17643;
    for (int i = 0; i < 17643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17643(int& val) {
    const int x = 17643;
    std::cout << 17643;
    for (int i = 0; i < 17643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17644(int& val) {
    func_in_17644(val);
    const int x = 17644;
    std::cout << 17644;
    for (int i = 0; i < 17644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17644(int& val) {
    const int x = 17644;
    std::cout << 17644;
    for (int i = 0; i < 17644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17645(int& val) {
    func_in_17645(val);
    const int x = 17645;
    std::cout << 17645;
    for (int i = 0; i < 17645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17645(int& val) {
    const int x = 17645;
    std::cout << 17645;
    for (int i = 0; i < 17645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17646(int& val) {
    func_in_17646(val);
    const int x = 17646;
    std::cout << 17646;
    for (int i = 0; i < 17646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17646(int& val) {
    const int x = 17646;
    std::cout << 17646;
    for (int i = 0; i < 17646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17647(int& val) {
    func_in_17647(val);
    const int x = 17647;
    std::cout << 17647;
    for (int i = 0; i < 17647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17647(int& val) {
    const int x = 17647;
    std::cout << 17647;
    for (int i = 0; i < 17647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17648(int& val) {
    func_in_17648(val);
    const int x = 17648;
    std::cout << 17648;
    for (int i = 0; i < 17648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17648(int& val) {
    const int x = 17648;
    std::cout << 17648;
    for (int i = 0; i < 17648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17649(int& val) {
    func_in_17649(val);
    const int x = 17649;
    std::cout << 17649;
    for (int i = 0; i < 17649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17649(int& val) {
    const int x = 17649;
    std::cout << 17649;
    for (int i = 0; i < 17649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17650(int& val) {
    func_in_17650(val);
    const int x = 17650;
    std::cout << 17650;
    for (int i = 0; i < 17650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17650(int& val) {
    const int x = 17650;
    std::cout << 17650;
    for (int i = 0; i < 17650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17651(int& val) {
    func_in_17651(val);
    const int x = 17651;
    std::cout << 17651;
    for (int i = 0; i < 17651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17651(int& val) {
    const int x = 17651;
    std::cout << 17651;
    for (int i = 0; i < 17651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17652(int& val) {
    func_in_17652(val);
    const int x = 17652;
    std::cout << 17652;
    for (int i = 0; i < 17652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17652(int& val) {
    const int x = 17652;
    std::cout << 17652;
    for (int i = 0; i < 17652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17653(int& val) {
    func_in_17653(val);
    const int x = 17653;
    std::cout << 17653;
    for (int i = 0; i < 17653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17653(int& val) {
    const int x = 17653;
    std::cout << 17653;
    for (int i = 0; i < 17653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17654(int& val) {
    func_in_17654(val);
    const int x = 17654;
    std::cout << 17654;
    for (int i = 0; i < 17654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17654(int& val) {
    const int x = 17654;
    std::cout << 17654;
    for (int i = 0; i < 17654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17655(int& val) {
    func_in_17655(val);
    const int x = 17655;
    std::cout << 17655;
    for (int i = 0; i < 17655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17655(int& val) {
    const int x = 17655;
    std::cout << 17655;
    for (int i = 0; i < 17655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17656(int& val) {
    func_in_17656(val);
    const int x = 17656;
    std::cout << 17656;
    for (int i = 0; i < 17656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17656(int& val) {
    const int x = 17656;
    std::cout << 17656;
    for (int i = 0; i < 17656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17657(int& val) {
    func_in_17657(val);
    const int x = 17657;
    std::cout << 17657;
    for (int i = 0; i < 17657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17657(int& val) {
    const int x = 17657;
    std::cout << 17657;
    for (int i = 0; i < 17657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17658(int& val) {
    func_in_17658(val);
    const int x = 17658;
    std::cout << 17658;
    for (int i = 0; i < 17658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17658(int& val) {
    const int x = 17658;
    std::cout << 17658;
    for (int i = 0; i < 17658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17659(int& val) {
    func_in_17659(val);
    const int x = 17659;
    std::cout << 17659;
    for (int i = 0; i < 17659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17659(int& val) {
    const int x = 17659;
    std::cout << 17659;
    for (int i = 0; i < 17659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17660(int& val) {
    func_in_17660(val);
    const int x = 17660;
    std::cout << 17660;
    for (int i = 0; i < 17660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17660(int& val) {
    const int x = 17660;
    std::cout << 17660;
    for (int i = 0; i < 17660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17661(int& val) {
    func_in_17661(val);
    const int x = 17661;
    std::cout << 17661;
    for (int i = 0; i < 17661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17661(int& val) {
    const int x = 17661;
    std::cout << 17661;
    for (int i = 0; i < 17661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17662(int& val) {
    func_in_17662(val);
    const int x = 17662;
    std::cout << 17662;
    for (int i = 0; i < 17662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17662(int& val) {
    const int x = 17662;
    std::cout << 17662;
    for (int i = 0; i < 17662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17663(int& val) {
    func_in_17663(val);
    const int x = 17663;
    std::cout << 17663;
    for (int i = 0; i < 17663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17663(int& val) {
    const int x = 17663;
    std::cout << 17663;
    for (int i = 0; i < 17663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17664(int& val) {
    func_in_17664(val);
    const int x = 17664;
    std::cout << 17664;
    for (int i = 0; i < 17664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17664(int& val) {
    const int x = 17664;
    std::cout << 17664;
    for (int i = 0; i < 17664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17665(int& val) {
    func_in_17665(val);
    const int x = 17665;
    std::cout << 17665;
    for (int i = 0; i < 17665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17665(int& val) {
    const int x = 17665;
    std::cout << 17665;
    for (int i = 0; i < 17665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17666(int& val) {
    func_in_17666(val);
    const int x = 17666;
    std::cout << 17666;
    for (int i = 0; i < 17666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17666(int& val) {
    const int x = 17666;
    std::cout << 17666;
    for (int i = 0; i < 17666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17667(int& val) {
    func_in_17667(val);
    const int x = 17667;
    std::cout << 17667;
    for (int i = 0; i < 17667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17667(int& val) {
    const int x = 17667;
    std::cout << 17667;
    for (int i = 0; i < 17667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17668(int& val) {
    func_in_17668(val);
    const int x = 17668;
    std::cout << 17668;
    for (int i = 0; i < 17668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17668(int& val) {
    const int x = 17668;
    std::cout << 17668;
    for (int i = 0; i < 17668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17669(int& val) {
    func_in_17669(val);
    const int x = 17669;
    std::cout << 17669;
    for (int i = 0; i < 17669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17669(int& val) {
    const int x = 17669;
    std::cout << 17669;
    for (int i = 0; i < 17669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17670(int& val) {
    func_in_17670(val);
    const int x = 17670;
    std::cout << 17670;
    for (int i = 0; i < 17670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17670(int& val) {
    const int x = 17670;
    std::cout << 17670;
    for (int i = 0; i < 17670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17671(int& val) {
    func_in_17671(val);
    const int x = 17671;
    std::cout << 17671;
    for (int i = 0; i < 17671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17671(int& val) {
    const int x = 17671;
    std::cout << 17671;
    for (int i = 0; i < 17671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17672(int& val) {
    func_in_17672(val);
    const int x = 17672;
    std::cout << 17672;
    for (int i = 0; i < 17672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17672(int& val) {
    const int x = 17672;
    std::cout << 17672;
    for (int i = 0; i < 17672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17673(int& val) {
    func_in_17673(val);
    const int x = 17673;
    std::cout << 17673;
    for (int i = 0; i < 17673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17673(int& val) {
    const int x = 17673;
    std::cout << 17673;
    for (int i = 0; i < 17673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17674(int& val) {
    func_in_17674(val);
    const int x = 17674;
    std::cout << 17674;
    for (int i = 0; i < 17674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17674(int& val) {
    const int x = 17674;
    std::cout << 17674;
    for (int i = 0; i < 17674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17675(int& val) {
    func_in_17675(val);
    const int x = 17675;
    std::cout << 17675;
    for (int i = 0; i < 17675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17675(int& val) {
    const int x = 17675;
    std::cout << 17675;
    for (int i = 0; i < 17675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17676(int& val) {
    func_in_17676(val);
    const int x = 17676;
    std::cout << 17676;
    for (int i = 0; i < 17676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17676(int& val) {
    const int x = 17676;
    std::cout << 17676;
    for (int i = 0; i < 17676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17677(int& val) {
    func_in_17677(val);
    const int x = 17677;
    std::cout << 17677;
    for (int i = 0; i < 17677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17677(int& val) {
    const int x = 17677;
    std::cout << 17677;
    for (int i = 0; i < 17677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17678(int& val) {
    func_in_17678(val);
    const int x = 17678;
    std::cout << 17678;
    for (int i = 0; i < 17678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17678(int& val) {
    const int x = 17678;
    std::cout << 17678;
    for (int i = 0; i < 17678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17679(int& val) {
    func_in_17679(val);
    const int x = 17679;
    std::cout << 17679;
    for (int i = 0; i < 17679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17679(int& val) {
    const int x = 17679;
    std::cout << 17679;
    for (int i = 0; i < 17679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17680(int& val) {
    func_in_17680(val);
    const int x = 17680;
    std::cout << 17680;
    for (int i = 0; i < 17680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17680(int& val) {
    const int x = 17680;
    std::cout << 17680;
    for (int i = 0; i < 17680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17681(int& val) {
    func_in_17681(val);
    const int x = 17681;
    std::cout << 17681;
    for (int i = 0; i < 17681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17681(int& val) {
    const int x = 17681;
    std::cout << 17681;
    for (int i = 0; i < 17681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17682(int& val) {
    func_in_17682(val);
    const int x = 17682;
    std::cout << 17682;
    for (int i = 0; i < 17682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17682(int& val) {
    const int x = 17682;
    std::cout << 17682;
    for (int i = 0; i < 17682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17683(int& val) {
    func_in_17683(val);
    const int x = 17683;
    std::cout << 17683;
    for (int i = 0; i < 17683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17683(int& val) {
    const int x = 17683;
    std::cout << 17683;
    for (int i = 0; i < 17683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17684(int& val) {
    func_in_17684(val);
    const int x = 17684;
    std::cout << 17684;
    for (int i = 0; i < 17684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17684(int& val) {
    const int x = 17684;
    std::cout << 17684;
    for (int i = 0; i < 17684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17685(int& val) {
    func_in_17685(val);
    const int x = 17685;
    std::cout << 17685;
    for (int i = 0; i < 17685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17685(int& val) {
    const int x = 17685;
    std::cout << 17685;
    for (int i = 0; i < 17685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17686(int& val) {
    func_in_17686(val);
    const int x = 17686;
    std::cout << 17686;
    for (int i = 0; i < 17686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17686(int& val) {
    const int x = 17686;
    std::cout << 17686;
    for (int i = 0; i < 17686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17687(int& val) {
    func_in_17687(val);
    const int x = 17687;
    std::cout << 17687;
    for (int i = 0; i < 17687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17687(int& val) {
    const int x = 17687;
    std::cout << 17687;
    for (int i = 0; i < 17687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17688(int& val) {
    func_in_17688(val);
    const int x = 17688;
    std::cout << 17688;
    for (int i = 0; i < 17688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17688(int& val) {
    const int x = 17688;
    std::cout << 17688;
    for (int i = 0; i < 17688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17689(int& val) {
    func_in_17689(val);
    const int x = 17689;
    std::cout << 17689;
    for (int i = 0; i < 17689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17689(int& val) {
    const int x = 17689;
    std::cout << 17689;
    for (int i = 0; i < 17689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17690(int& val) {
    func_in_17690(val);
    const int x = 17690;
    std::cout << 17690;
    for (int i = 0; i < 17690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17690(int& val) {
    const int x = 17690;
    std::cout << 17690;
    for (int i = 0; i < 17690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17691(int& val) {
    func_in_17691(val);
    const int x = 17691;
    std::cout << 17691;
    for (int i = 0; i < 17691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17691(int& val) {
    const int x = 17691;
    std::cout << 17691;
    for (int i = 0; i < 17691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17692(int& val) {
    func_in_17692(val);
    const int x = 17692;
    std::cout << 17692;
    for (int i = 0; i < 17692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17692(int& val) {
    const int x = 17692;
    std::cout << 17692;
    for (int i = 0; i < 17692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17693(int& val) {
    func_in_17693(val);
    const int x = 17693;
    std::cout << 17693;
    for (int i = 0; i < 17693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17693(int& val) {
    const int x = 17693;
    std::cout << 17693;
    for (int i = 0; i < 17693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17694(int& val) {
    func_in_17694(val);
    const int x = 17694;
    std::cout << 17694;
    for (int i = 0; i < 17694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17694(int& val) {
    const int x = 17694;
    std::cout << 17694;
    for (int i = 0; i < 17694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17695(int& val) {
    func_in_17695(val);
    const int x = 17695;
    std::cout << 17695;
    for (int i = 0; i < 17695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17695(int& val) {
    const int x = 17695;
    std::cout << 17695;
    for (int i = 0; i < 17695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17696(int& val) {
    func_in_17696(val);
    const int x = 17696;
    std::cout << 17696;
    for (int i = 0; i < 17696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17696(int& val) {
    const int x = 17696;
    std::cout << 17696;
    for (int i = 0; i < 17696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17697(int& val) {
    func_in_17697(val);
    const int x = 17697;
    std::cout << 17697;
    for (int i = 0; i < 17697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17697(int& val) {
    const int x = 17697;
    std::cout << 17697;
    for (int i = 0; i < 17697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17698(int& val) {
    func_in_17698(val);
    const int x = 17698;
    std::cout << 17698;
    for (int i = 0; i < 17698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17698(int& val) {
    const int x = 17698;
    std::cout << 17698;
    for (int i = 0; i < 17698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17699(int& val) {
    func_in_17699(val);
    const int x = 17699;
    std::cout << 17699;
    for (int i = 0; i < 17699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17699(int& val) {
    const int x = 17699;
    std::cout << 17699;
    for (int i = 0; i < 17699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17700(int& val) {
    func_in_17700(val);
    const int x = 17700;
    std::cout << 17700;
    for (int i = 0; i < 17700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17700(int& val) {
    const int x = 17700;
    std::cout << 17700;
    for (int i = 0; i < 17700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17701(int& val) {
    func_in_17701(val);
    const int x = 17701;
    std::cout << 17701;
    for (int i = 0; i < 17701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17701(int& val) {
    const int x = 17701;
    std::cout << 17701;
    for (int i = 0; i < 17701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17702(int& val) {
    func_in_17702(val);
    const int x = 17702;
    std::cout << 17702;
    for (int i = 0; i < 17702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17702(int& val) {
    const int x = 17702;
    std::cout << 17702;
    for (int i = 0; i < 17702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17703(int& val) {
    func_in_17703(val);
    const int x = 17703;
    std::cout << 17703;
    for (int i = 0; i < 17703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17703(int& val) {
    const int x = 17703;
    std::cout << 17703;
    for (int i = 0; i < 17703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17704(int& val) {
    func_in_17704(val);
    const int x = 17704;
    std::cout << 17704;
    for (int i = 0; i < 17704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17704(int& val) {
    const int x = 17704;
    std::cout << 17704;
    for (int i = 0; i < 17704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17705(int& val) {
    func_in_17705(val);
    const int x = 17705;
    std::cout << 17705;
    for (int i = 0; i < 17705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17705(int& val) {
    const int x = 17705;
    std::cout << 17705;
    for (int i = 0; i < 17705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17706(int& val) {
    func_in_17706(val);
    const int x = 17706;
    std::cout << 17706;
    for (int i = 0; i < 17706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17706(int& val) {
    const int x = 17706;
    std::cout << 17706;
    for (int i = 0; i < 17706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17707(int& val) {
    func_in_17707(val);
    const int x = 17707;
    std::cout << 17707;
    for (int i = 0; i < 17707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17707(int& val) {
    const int x = 17707;
    std::cout << 17707;
    for (int i = 0; i < 17707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17708(int& val) {
    func_in_17708(val);
    const int x = 17708;
    std::cout << 17708;
    for (int i = 0; i < 17708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17708(int& val) {
    const int x = 17708;
    std::cout << 17708;
    for (int i = 0; i < 17708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17709(int& val) {
    func_in_17709(val);
    const int x = 17709;
    std::cout << 17709;
    for (int i = 0; i < 17709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17709(int& val) {
    const int x = 17709;
    std::cout << 17709;
    for (int i = 0; i < 17709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17710(int& val) {
    func_in_17710(val);
    const int x = 17710;
    std::cout << 17710;
    for (int i = 0; i < 17710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17710(int& val) {
    const int x = 17710;
    std::cout << 17710;
    for (int i = 0; i < 17710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17711(int& val) {
    func_in_17711(val);
    const int x = 17711;
    std::cout << 17711;
    for (int i = 0; i < 17711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17711(int& val) {
    const int x = 17711;
    std::cout << 17711;
    for (int i = 0; i < 17711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17712(int& val) {
    func_in_17712(val);
    const int x = 17712;
    std::cout << 17712;
    for (int i = 0; i < 17712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17712(int& val) {
    const int x = 17712;
    std::cout << 17712;
    for (int i = 0; i < 17712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17713(int& val) {
    func_in_17713(val);
    const int x = 17713;
    std::cout << 17713;
    for (int i = 0; i < 17713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17713(int& val) {
    const int x = 17713;
    std::cout << 17713;
    for (int i = 0; i < 17713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17714(int& val) {
    func_in_17714(val);
    const int x = 17714;
    std::cout << 17714;
    for (int i = 0; i < 17714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17714(int& val) {
    const int x = 17714;
    std::cout << 17714;
    for (int i = 0; i < 17714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17715(int& val) {
    func_in_17715(val);
    const int x = 17715;
    std::cout << 17715;
    for (int i = 0; i < 17715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17715(int& val) {
    const int x = 17715;
    std::cout << 17715;
    for (int i = 0; i < 17715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17716(int& val) {
    func_in_17716(val);
    const int x = 17716;
    std::cout << 17716;
    for (int i = 0; i < 17716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17716(int& val) {
    const int x = 17716;
    std::cout << 17716;
    for (int i = 0; i < 17716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17717(int& val) {
    func_in_17717(val);
    const int x = 17717;
    std::cout << 17717;
    for (int i = 0; i < 17717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17717(int& val) {
    const int x = 17717;
    std::cout << 17717;
    for (int i = 0; i < 17717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17718(int& val) {
    func_in_17718(val);
    const int x = 17718;
    std::cout << 17718;
    for (int i = 0; i < 17718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17718(int& val) {
    const int x = 17718;
    std::cout << 17718;
    for (int i = 0; i < 17718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17719(int& val) {
    func_in_17719(val);
    const int x = 17719;
    std::cout << 17719;
    for (int i = 0; i < 17719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17719(int& val) {
    const int x = 17719;
    std::cout << 17719;
    for (int i = 0; i < 17719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17720(int& val) {
    func_in_17720(val);
    const int x = 17720;
    std::cout << 17720;
    for (int i = 0; i < 17720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17720(int& val) {
    const int x = 17720;
    std::cout << 17720;
    for (int i = 0; i < 17720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17721(int& val) {
    func_in_17721(val);
    const int x = 17721;
    std::cout << 17721;
    for (int i = 0; i < 17721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17721(int& val) {
    const int x = 17721;
    std::cout << 17721;
    for (int i = 0; i < 17721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17722(int& val) {
    func_in_17722(val);
    const int x = 17722;
    std::cout << 17722;
    for (int i = 0; i < 17722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17722(int& val) {
    const int x = 17722;
    std::cout << 17722;
    for (int i = 0; i < 17722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17723(int& val) {
    func_in_17723(val);
    const int x = 17723;
    std::cout << 17723;
    for (int i = 0; i < 17723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17723(int& val) {
    const int x = 17723;
    std::cout << 17723;
    for (int i = 0; i < 17723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17724(int& val) {
    func_in_17724(val);
    const int x = 17724;
    std::cout << 17724;
    for (int i = 0; i < 17724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17724(int& val) {
    const int x = 17724;
    std::cout << 17724;
    for (int i = 0; i < 17724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17725(int& val) {
    func_in_17725(val);
    const int x = 17725;
    std::cout << 17725;
    for (int i = 0; i < 17725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17725(int& val) {
    const int x = 17725;
    std::cout << 17725;
    for (int i = 0; i < 17725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17726(int& val) {
    func_in_17726(val);
    const int x = 17726;
    std::cout << 17726;
    for (int i = 0; i < 17726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17726(int& val) {
    const int x = 17726;
    std::cout << 17726;
    for (int i = 0; i < 17726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17727(int& val) {
    func_in_17727(val);
    const int x = 17727;
    std::cout << 17727;
    for (int i = 0; i < 17727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17727(int& val) {
    const int x = 17727;
    std::cout << 17727;
    for (int i = 0; i < 17727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17728(int& val) {
    func_in_17728(val);
    const int x = 17728;
    std::cout << 17728;
    for (int i = 0; i < 17728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17728(int& val) {
    const int x = 17728;
    std::cout << 17728;
    for (int i = 0; i < 17728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17729(int& val) {
    func_in_17729(val);
    const int x = 17729;
    std::cout << 17729;
    for (int i = 0; i < 17729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17729(int& val) {
    const int x = 17729;
    std::cout << 17729;
    for (int i = 0; i < 17729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17730(int& val) {
    func_in_17730(val);
    const int x = 17730;
    std::cout << 17730;
    for (int i = 0; i < 17730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17730(int& val) {
    const int x = 17730;
    std::cout << 17730;
    for (int i = 0; i < 17730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17731(int& val) {
    func_in_17731(val);
    const int x = 17731;
    std::cout << 17731;
    for (int i = 0; i < 17731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17731(int& val) {
    const int x = 17731;
    std::cout << 17731;
    for (int i = 0; i < 17731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17732(int& val) {
    func_in_17732(val);
    const int x = 17732;
    std::cout << 17732;
    for (int i = 0; i < 17732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17732(int& val) {
    const int x = 17732;
    std::cout << 17732;
    for (int i = 0; i < 17732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17733(int& val) {
    func_in_17733(val);
    const int x = 17733;
    std::cout << 17733;
    for (int i = 0; i < 17733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17733(int& val) {
    const int x = 17733;
    std::cout << 17733;
    for (int i = 0; i < 17733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17734(int& val) {
    func_in_17734(val);
    const int x = 17734;
    std::cout << 17734;
    for (int i = 0; i < 17734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17734(int& val) {
    const int x = 17734;
    std::cout << 17734;
    for (int i = 0; i < 17734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17735(int& val) {
    func_in_17735(val);
    const int x = 17735;
    std::cout << 17735;
    for (int i = 0; i < 17735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17735(int& val) {
    const int x = 17735;
    std::cout << 17735;
    for (int i = 0; i < 17735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17736(int& val) {
    func_in_17736(val);
    const int x = 17736;
    std::cout << 17736;
    for (int i = 0; i < 17736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17736(int& val) {
    const int x = 17736;
    std::cout << 17736;
    for (int i = 0; i < 17736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17737(int& val) {
    func_in_17737(val);
    const int x = 17737;
    std::cout << 17737;
    for (int i = 0; i < 17737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17737(int& val) {
    const int x = 17737;
    std::cout << 17737;
    for (int i = 0; i < 17737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17738(int& val) {
    func_in_17738(val);
    const int x = 17738;
    std::cout << 17738;
    for (int i = 0; i < 17738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17738(int& val) {
    const int x = 17738;
    std::cout << 17738;
    for (int i = 0; i < 17738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17739(int& val) {
    func_in_17739(val);
    const int x = 17739;
    std::cout << 17739;
    for (int i = 0; i < 17739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17739(int& val) {
    const int x = 17739;
    std::cout << 17739;
    for (int i = 0; i < 17739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17740(int& val) {
    func_in_17740(val);
    const int x = 17740;
    std::cout << 17740;
    for (int i = 0; i < 17740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17740(int& val) {
    const int x = 17740;
    std::cout << 17740;
    for (int i = 0; i < 17740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17741(int& val) {
    func_in_17741(val);
    const int x = 17741;
    std::cout << 17741;
    for (int i = 0; i < 17741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17741(int& val) {
    const int x = 17741;
    std::cout << 17741;
    for (int i = 0; i < 17741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17742(int& val) {
    func_in_17742(val);
    const int x = 17742;
    std::cout << 17742;
    for (int i = 0; i < 17742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17742(int& val) {
    const int x = 17742;
    std::cout << 17742;
    for (int i = 0; i < 17742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17743(int& val) {
    func_in_17743(val);
    const int x = 17743;
    std::cout << 17743;
    for (int i = 0; i < 17743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17743(int& val) {
    const int x = 17743;
    std::cout << 17743;
    for (int i = 0; i < 17743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17744(int& val) {
    func_in_17744(val);
    const int x = 17744;
    std::cout << 17744;
    for (int i = 0; i < 17744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17744(int& val) {
    const int x = 17744;
    std::cout << 17744;
    for (int i = 0; i < 17744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17745(int& val) {
    func_in_17745(val);
    const int x = 17745;
    std::cout << 17745;
    for (int i = 0; i < 17745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17745(int& val) {
    const int x = 17745;
    std::cout << 17745;
    for (int i = 0; i < 17745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17746(int& val) {
    func_in_17746(val);
    const int x = 17746;
    std::cout << 17746;
    for (int i = 0; i < 17746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17746(int& val) {
    const int x = 17746;
    std::cout << 17746;
    for (int i = 0; i < 17746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17747(int& val) {
    func_in_17747(val);
    const int x = 17747;
    std::cout << 17747;
    for (int i = 0; i < 17747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17747(int& val) {
    const int x = 17747;
    std::cout << 17747;
    for (int i = 0; i < 17747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17748(int& val) {
    func_in_17748(val);
    const int x = 17748;
    std::cout << 17748;
    for (int i = 0; i < 17748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17748(int& val) {
    const int x = 17748;
    std::cout << 17748;
    for (int i = 0; i < 17748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17749(int& val) {
    func_in_17749(val);
    const int x = 17749;
    std::cout << 17749;
    for (int i = 0; i < 17749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17749(int& val) {
    const int x = 17749;
    std::cout << 17749;
    for (int i = 0; i < 17749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17750(int& val) {
    func_in_17750(val);
    const int x = 17750;
    std::cout << 17750;
    for (int i = 0; i < 17750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17750(int& val) {
    const int x = 17750;
    std::cout << 17750;
    for (int i = 0; i < 17750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17751(int& val) {
    func_in_17751(val);
    const int x = 17751;
    std::cout << 17751;
    for (int i = 0; i < 17751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17751(int& val) {
    const int x = 17751;
    std::cout << 17751;
    for (int i = 0; i < 17751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17752(int& val) {
    func_in_17752(val);
    const int x = 17752;
    std::cout << 17752;
    for (int i = 0; i < 17752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17752(int& val) {
    const int x = 17752;
    std::cout << 17752;
    for (int i = 0; i < 17752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17753(int& val) {
    func_in_17753(val);
    const int x = 17753;
    std::cout << 17753;
    for (int i = 0; i < 17753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17753(int& val) {
    const int x = 17753;
    std::cout << 17753;
    for (int i = 0; i < 17753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17754(int& val) {
    func_in_17754(val);
    const int x = 17754;
    std::cout << 17754;
    for (int i = 0; i < 17754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17754(int& val) {
    const int x = 17754;
    std::cout << 17754;
    for (int i = 0; i < 17754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17755(int& val) {
    func_in_17755(val);
    const int x = 17755;
    std::cout << 17755;
    for (int i = 0; i < 17755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17755(int& val) {
    const int x = 17755;
    std::cout << 17755;
    for (int i = 0; i < 17755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17756(int& val) {
    func_in_17756(val);
    const int x = 17756;
    std::cout << 17756;
    for (int i = 0; i < 17756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17756(int& val) {
    const int x = 17756;
    std::cout << 17756;
    for (int i = 0; i < 17756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17757(int& val) {
    func_in_17757(val);
    const int x = 17757;
    std::cout << 17757;
    for (int i = 0; i < 17757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17757(int& val) {
    const int x = 17757;
    std::cout << 17757;
    for (int i = 0; i < 17757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17758(int& val) {
    func_in_17758(val);
    const int x = 17758;
    std::cout << 17758;
    for (int i = 0; i < 17758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17758(int& val) {
    const int x = 17758;
    std::cout << 17758;
    for (int i = 0; i < 17758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17759(int& val) {
    func_in_17759(val);
    const int x = 17759;
    std::cout << 17759;
    for (int i = 0; i < 17759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17759(int& val) {
    const int x = 17759;
    std::cout << 17759;
    for (int i = 0; i < 17759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17760(int& val) {
    func_in_17760(val);
    const int x = 17760;
    std::cout << 17760;
    for (int i = 0; i < 17760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17760(int& val) {
    const int x = 17760;
    std::cout << 17760;
    for (int i = 0; i < 17760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17761(int& val) {
    func_in_17761(val);
    const int x = 17761;
    std::cout << 17761;
    for (int i = 0; i < 17761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17761(int& val) {
    const int x = 17761;
    std::cout << 17761;
    for (int i = 0; i < 17761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17762(int& val) {
    func_in_17762(val);
    const int x = 17762;
    std::cout << 17762;
    for (int i = 0; i < 17762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17762(int& val) {
    const int x = 17762;
    std::cout << 17762;
    for (int i = 0; i < 17762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17763(int& val) {
    func_in_17763(val);
    const int x = 17763;
    std::cout << 17763;
    for (int i = 0; i < 17763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17763(int& val) {
    const int x = 17763;
    std::cout << 17763;
    for (int i = 0; i < 17763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17764(int& val) {
    func_in_17764(val);
    const int x = 17764;
    std::cout << 17764;
    for (int i = 0; i < 17764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17764(int& val) {
    const int x = 17764;
    std::cout << 17764;
    for (int i = 0; i < 17764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17765(int& val) {
    func_in_17765(val);
    const int x = 17765;
    std::cout << 17765;
    for (int i = 0; i < 17765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17765(int& val) {
    const int x = 17765;
    std::cout << 17765;
    for (int i = 0; i < 17765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17766(int& val) {
    func_in_17766(val);
    const int x = 17766;
    std::cout << 17766;
    for (int i = 0; i < 17766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17766(int& val) {
    const int x = 17766;
    std::cout << 17766;
    for (int i = 0; i < 17766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17767(int& val) {
    func_in_17767(val);
    const int x = 17767;
    std::cout << 17767;
    for (int i = 0; i < 17767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17767(int& val) {
    const int x = 17767;
    std::cout << 17767;
    for (int i = 0; i < 17767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17768(int& val) {
    func_in_17768(val);
    const int x = 17768;
    std::cout << 17768;
    for (int i = 0; i < 17768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17768(int& val) {
    const int x = 17768;
    std::cout << 17768;
    for (int i = 0; i < 17768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17769(int& val) {
    func_in_17769(val);
    const int x = 17769;
    std::cout << 17769;
    for (int i = 0; i < 17769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17769(int& val) {
    const int x = 17769;
    std::cout << 17769;
    for (int i = 0; i < 17769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17770(int& val) {
    func_in_17770(val);
    const int x = 17770;
    std::cout << 17770;
    for (int i = 0; i < 17770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17770(int& val) {
    const int x = 17770;
    std::cout << 17770;
    for (int i = 0; i < 17770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17771(int& val) {
    func_in_17771(val);
    const int x = 17771;
    std::cout << 17771;
    for (int i = 0; i < 17771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17771(int& val) {
    const int x = 17771;
    std::cout << 17771;
    for (int i = 0; i < 17771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17772(int& val) {
    func_in_17772(val);
    const int x = 17772;
    std::cout << 17772;
    for (int i = 0; i < 17772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17772(int& val) {
    const int x = 17772;
    std::cout << 17772;
    for (int i = 0; i < 17772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17773(int& val) {
    func_in_17773(val);
    const int x = 17773;
    std::cout << 17773;
    for (int i = 0; i < 17773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17773(int& val) {
    const int x = 17773;
    std::cout << 17773;
    for (int i = 0; i < 17773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17774(int& val) {
    func_in_17774(val);
    const int x = 17774;
    std::cout << 17774;
    for (int i = 0; i < 17774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17774(int& val) {
    const int x = 17774;
    std::cout << 17774;
    for (int i = 0; i < 17774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17775(int& val) {
    func_in_17775(val);
    const int x = 17775;
    std::cout << 17775;
    for (int i = 0; i < 17775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17775(int& val) {
    const int x = 17775;
    std::cout << 17775;
    for (int i = 0; i < 17775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17776(int& val) {
    func_in_17776(val);
    const int x = 17776;
    std::cout << 17776;
    for (int i = 0; i < 17776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17776(int& val) {
    const int x = 17776;
    std::cout << 17776;
    for (int i = 0; i < 17776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17777(int& val) {
    func_in_17777(val);
    const int x = 17777;
    std::cout << 17777;
    for (int i = 0; i < 17777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17777(int& val) {
    const int x = 17777;
    std::cout << 17777;
    for (int i = 0; i < 17777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17778(int& val) {
    func_in_17778(val);
    const int x = 17778;
    std::cout << 17778;
    for (int i = 0; i < 17778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17778(int& val) {
    const int x = 17778;
    std::cout << 17778;
    for (int i = 0; i < 17778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17779(int& val) {
    func_in_17779(val);
    const int x = 17779;
    std::cout << 17779;
    for (int i = 0; i < 17779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17779(int& val) {
    const int x = 17779;
    std::cout << 17779;
    for (int i = 0; i < 17779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17780(int& val) {
    func_in_17780(val);
    const int x = 17780;
    std::cout << 17780;
    for (int i = 0; i < 17780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17780(int& val) {
    const int x = 17780;
    std::cout << 17780;
    for (int i = 0; i < 17780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17781(int& val) {
    func_in_17781(val);
    const int x = 17781;
    std::cout << 17781;
    for (int i = 0; i < 17781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17781(int& val) {
    const int x = 17781;
    std::cout << 17781;
    for (int i = 0; i < 17781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17782(int& val) {
    func_in_17782(val);
    const int x = 17782;
    std::cout << 17782;
    for (int i = 0; i < 17782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17782(int& val) {
    const int x = 17782;
    std::cout << 17782;
    for (int i = 0; i < 17782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17783(int& val) {
    func_in_17783(val);
    const int x = 17783;
    std::cout << 17783;
    for (int i = 0; i < 17783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17783(int& val) {
    const int x = 17783;
    std::cout << 17783;
    for (int i = 0; i < 17783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17784(int& val) {
    func_in_17784(val);
    const int x = 17784;
    std::cout << 17784;
    for (int i = 0; i < 17784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17784(int& val) {
    const int x = 17784;
    std::cout << 17784;
    for (int i = 0; i < 17784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17785(int& val) {
    func_in_17785(val);
    const int x = 17785;
    std::cout << 17785;
    for (int i = 0; i < 17785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17785(int& val) {
    const int x = 17785;
    std::cout << 17785;
    for (int i = 0; i < 17785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17786(int& val) {
    func_in_17786(val);
    const int x = 17786;
    std::cout << 17786;
    for (int i = 0; i < 17786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17786(int& val) {
    const int x = 17786;
    std::cout << 17786;
    for (int i = 0; i < 17786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17787(int& val) {
    func_in_17787(val);
    const int x = 17787;
    std::cout << 17787;
    for (int i = 0; i < 17787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17787(int& val) {
    const int x = 17787;
    std::cout << 17787;
    for (int i = 0; i < 17787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17788(int& val) {
    func_in_17788(val);
    const int x = 17788;
    std::cout << 17788;
    for (int i = 0; i < 17788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17788(int& val) {
    const int x = 17788;
    std::cout << 17788;
    for (int i = 0; i < 17788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17789(int& val) {
    func_in_17789(val);
    const int x = 17789;
    std::cout << 17789;
    for (int i = 0; i < 17789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17789(int& val) {
    const int x = 17789;
    std::cout << 17789;
    for (int i = 0; i < 17789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17790(int& val) {
    func_in_17790(val);
    const int x = 17790;
    std::cout << 17790;
    for (int i = 0; i < 17790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17790(int& val) {
    const int x = 17790;
    std::cout << 17790;
    for (int i = 0; i < 17790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17791(int& val) {
    func_in_17791(val);
    const int x = 17791;
    std::cout << 17791;
    for (int i = 0; i < 17791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17791(int& val) {
    const int x = 17791;
    std::cout << 17791;
    for (int i = 0; i < 17791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17792(int& val) {
    func_in_17792(val);
    const int x = 17792;
    std::cout << 17792;
    for (int i = 0; i < 17792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17792(int& val) {
    const int x = 17792;
    std::cout << 17792;
    for (int i = 0; i < 17792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17793(int& val) {
    func_in_17793(val);
    const int x = 17793;
    std::cout << 17793;
    for (int i = 0; i < 17793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17793(int& val) {
    const int x = 17793;
    std::cout << 17793;
    for (int i = 0; i < 17793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17794(int& val) {
    func_in_17794(val);
    const int x = 17794;
    std::cout << 17794;
    for (int i = 0; i < 17794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17794(int& val) {
    const int x = 17794;
    std::cout << 17794;
    for (int i = 0; i < 17794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17795(int& val) {
    func_in_17795(val);
    const int x = 17795;
    std::cout << 17795;
    for (int i = 0; i < 17795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17795(int& val) {
    const int x = 17795;
    std::cout << 17795;
    for (int i = 0; i < 17795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17796(int& val) {
    func_in_17796(val);
    const int x = 17796;
    std::cout << 17796;
    for (int i = 0; i < 17796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17796(int& val) {
    const int x = 17796;
    std::cout << 17796;
    for (int i = 0; i < 17796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17797(int& val) {
    func_in_17797(val);
    const int x = 17797;
    std::cout << 17797;
    for (int i = 0; i < 17797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17797(int& val) {
    const int x = 17797;
    std::cout << 17797;
    for (int i = 0; i < 17797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17798(int& val) {
    func_in_17798(val);
    const int x = 17798;
    std::cout << 17798;
    for (int i = 0; i < 17798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17798(int& val) {
    const int x = 17798;
    std::cout << 17798;
    for (int i = 0; i < 17798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17799(int& val) {
    func_in_17799(val);
    const int x = 17799;
    std::cout << 17799;
    for (int i = 0; i < 17799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17799(int& val) {
    const int x = 17799;
    std::cout << 17799;
    for (int i = 0; i < 17799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17800(int& val) {
    func_in_17800(val);
    const int x = 17800;
    std::cout << 17800;
    for (int i = 0; i < 17800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17800(int& val) {
    const int x = 17800;
    std::cout << 17800;
    for (int i = 0; i < 17800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17801(int& val) {
    func_in_17801(val);
    const int x = 17801;
    std::cout << 17801;
    for (int i = 0; i < 17801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17801(int& val) {
    const int x = 17801;
    std::cout << 17801;
    for (int i = 0; i < 17801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17802(int& val) {
    func_in_17802(val);
    const int x = 17802;
    std::cout << 17802;
    for (int i = 0; i < 17802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17802(int& val) {
    const int x = 17802;
    std::cout << 17802;
    for (int i = 0; i < 17802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17803(int& val) {
    func_in_17803(val);
    const int x = 17803;
    std::cout << 17803;
    for (int i = 0; i < 17803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17803(int& val) {
    const int x = 17803;
    std::cout << 17803;
    for (int i = 0; i < 17803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17804(int& val) {
    func_in_17804(val);
    const int x = 17804;
    std::cout << 17804;
    for (int i = 0; i < 17804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17804(int& val) {
    const int x = 17804;
    std::cout << 17804;
    for (int i = 0; i < 17804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17805(int& val) {
    func_in_17805(val);
    const int x = 17805;
    std::cout << 17805;
    for (int i = 0; i < 17805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17805(int& val) {
    const int x = 17805;
    std::cout << 17805;
    for (int i = 0; i < 17805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17806(int& val) {
    func_in_17806(val);
    const int x = 17806;
    std::cout << 17806;
    for (int i = 0; i < 17806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17806(int& val) {
    const int x = 17806;
    std::cout << 17806;
    for (int i = 0; i < 17806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17807(int& val) {
    func_in_17807(val);
    const int x = 17807;
    std::cout << 17807;
    for (int i = 0; i < 17807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17807(int& val) {
    const int x = 17807;
    std::cout << 17807;
    for (int i = 0; i < 17807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17808(int& val) {
    func_in_17808(val);
    const int x = 17808;
    std::cout << 17808;
    for (int i = 0; i < 17808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17808(int& val) {
    const int x = 17808;
    std::cout << 17808;
    for (int i = 0; i < 17808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17809(int& val) {
    func_in_17809(val);
    const int x = 17809;
    std::cout << 17809;
    for (int i = 0; i < 17809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17809(int& val) {
    const int x = 17809;
    std::cout << 17809;
    for (int i = 0; i < 17809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17810(int& val) {
    func_in_17810(val);
    const int x = 17810;
    std::cout << 17810;
    for (int i = 0; i < 17810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17810(int& val) {
    const int x = 17810;
    std::cout << 17810;
    for (int i = 0; i < 17810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17811(int& val) {
    func_in_17811(val);
    const int x = 17811;
    std::cout << 17811;
    for (int i = 0; i < 17811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17811(int& val) {
    const int x = 17811;
    std::cout << 17811;
    for (int i = 0; i < 17811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17812(int& val) {
    func_in_17812(val);
    const int x = 17812;
    std::cout << 17812;
    for (int i = 0; i < 17812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17812(int& val) {
    const int x = 17812;
    std::cout << 17812;
    for (int i = 0; i < 17812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17813(int& val) {
    func_in_17813(val);
    const int x = 17813;
    std::cout << 17813;
    for (int i = 0; i < 17813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17813(int& val) {
    const int x = 17813;
    std::cout << 17813;
    for (int i = 0; i < 17813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17814(int& val) {
    func_in_17814(val);
    const int x = 17814;
    std::cout << 17814;
    for (int i = 0; i < 17814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17814(int& val) {
    const int x = 17814;
    std::cout << 17814;
    for (int i = 0; i < 17814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17815(int& val) {
    func_in_17815(val);
    const int x = 17815;
    std::cout << 17815;
    for (int i = 0; i < 17815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17815(int& val) {
    const int x = 17815;
    std::cout << 17815;
    for (int i = 0; i < 17815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17816(int& val) {
    func_in_17816(val);
    const int x = 17816;
    std::cout << 17816;
    for (int i = 0; i < 17816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17816(int& val) {
    const int x = 17816;
    std::cout << 17816;
    for (int i = 0; i < 17816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17817(int& val) {
    func_in_17817(val);
    const int x = 17817;
    std::cout << 17817;
    for (int i = 0; i < 17817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17817(int& val) {
    const int x = 17817;
    std::cout << 17817;
    for (int i = 0; i < 17817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17818(int& val) {
    func_in_17818(val);
    const int x = 17818;
    std::cout << 17818;
    for (int i = 0; i < 17818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17818(int& val) {
    const int x = 17818;
    std::cout << 17818;
    for (int i = 0; i < 17818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17819(int& val) {
    func_in_17819(val);
    const int x = 17819;
    std::cout << 17819;
    for (int i = 0; i < 17819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17819(int& val) {
    const int x = 17819;
    std::cout << 17819;
    for (int i = 0; i < 17819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17820(int& val) {
    func_in_17820(val);
    const int x = 17820;
    std::cout << 17820;
    for (int i = 0; i < 17820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17820(int& val) {
    const int x = 17820;
    std::cout << 17820;
    for (int i = 0; i < 17820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17821(int& val) {
    func_in_17821(val);
    const int x = 17821;
    std::cout << 17821;
    for (int i = 0; i < 17821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17821(int& val) {
    const int x = 17821;
    std::cout << 17821;
    for (int i = 0; i < 17821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17822(int& val) {
    func_in_17822(val);
    const int x = 17822;
    std::cout << 17822;
    for (int i = 0; i < 17822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17822(int& val) {
    const int x = 17822;
    std::cout << 17822;
    for (int i = 0; i < 17822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17823(int& val) {
    func_in_17823(val);
    const int x = 17823;
    std::cout << 17823;
    for (int i = 0; i < 17823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17823(int& val) {
    const int x = 17823;
    std::cout << 17823;
    for (int i = 0; i < 17823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17824(int& val) {
    func_in_17824(val);
    const int x = 17824;
    std::cout << 17824;
    for (int i = 0; i < 17824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17824(int& val) {
    const int x = 17824;
    std::cout << 17824;
    for (int i = 0; i < 17824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17825(int& val) {
    func_in_17825(val);
    const int x = 17825;
    std::cout << 17825;
    for (int i = 0; i < 17825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17825(int& val) {
    const int x = 17825;
    std::cout << 17825;
    for (int i = 0; i < 17825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17826(int& val) {
    func_in_17826(val);
    const int x = 17826;
    std::cout << 17826;
    for (int i = 0; i < 17826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17826(int& val) {
    const int x = 17826;
    std::cout << 17826;
    for (int i = 0; i < 17826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17827(int& val) {
    func_in_17827(val);
    const int x = 17827;
    std::cout << 17827;
    for (int i = 0; i < 17827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17827(int& val) {
    const int x = 17827;
    std::cout << 17827;
    for (int i = 0; i < 17827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17828(int& val) {
    func_in_17828(val);
    const int x = 17828;
    std::cout << 17828;
    for (int i = 0; i < 17828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17828(int& val) {
    const int x = 17828;
    std::cout << 17828;
    for (int i = 0; i < 17828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17829(int& val) {
    func_in_17829(val);
    const int x = 17829;
    std::cout << 17829;
    for (int i = 0; i < 17829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17829(int& val) {
    const int x = 17829;
    std::cout << 17829;
    for (int i = 0; i < 17829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17830(int& val) {
    func_in_17830(val);
    const int x = 17830;
    std::cout << 17830;
    for (int i = 0; i < 17830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17830(int& val) {
    const int x = 17830;
    std::cout << 17830;
    for (int i = 0; i < 17830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17831(int& val) {
    func_in_17831(val);
    const int x = 17831;
    std::cout << 17831;
    for (int i = 0; i < 17831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17831(int& val) {
    const int x = 17831;
    std::cout << 17831;
    for (int i = 0; i < 17831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17832(int& val) {
    func_in_17832(val);
    const int x = 17832;
    std::cout << 17832;
    for (int i = 0; i < 17832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17832(int& val) {
    const int x = 17832;
    std::cout << 17832;
    for (int i = 0; i < 17832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17833(int& val) {
    func_in_17833(val);
    const int x = 17833;
    std::cout << 17833;
    for (int i = 0; i < 17833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17833(int& val) {
    const int x = 17833;
    std::cout << 17833;
    for (int i = 0; i < 17833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17834(int& val) {
    func_in_17834(val);
    const int x = 17834;
    std::cout << 17834;
    for (int i = 0; i < 17834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17834(int& val) {
    const int x = 17834;
    std::cout << 17834;
    for (int i = 0; i < 17834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17835(int& val) {
    func_in_17835(val);
    const int x = 17835;
    std::cout << 17835;
    for (int i = 0; i < 17835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17835(int& val) {
    const int x = 17835;
    std::cout << 17835;
    for (int i = 0; i < 17835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17836(int& val) {
    func_in_17836(val);
    const int x = 17836;
    std::cout << 17836;
    for (int i = 0; i < 17836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17836(int& val) {
    const int x = 17836;
    std::cout << 17836;
    for (int i = 0; i < 17836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17837(int& val) {
    func_in_17837(val);
    const int x = 17837;
    std::cout << 17837;
    for (int i = 0; i < 17837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17837(int& val) {
    const int x = 17837;
    std::cout << 17837;
    for (int i = 0; i < 17837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17838(int& val) {
    func_in_17838(val);
    const int x = 17838;
    std::cout << 17838;
    for (int i = 0; i < 17838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17838(int& val) {
    const int x = 17838;
    std::cout << 17838;
    for (int i = 0; i < 17838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17839(int& val) {
    func_in_17839(val);
    const int x = 17839;
    std::cout << 17839;
    for (int i = 0; i < 17839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17839(int& val) {
    const int x = 17839;
    std::cout << 17839;
    for (int i = 0; i < 17839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17840(int& val) {
    func_in_17840(val);
    const int x = 17840;
    std::cout << 17840;
    for (int i = 0; i < 17840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17840(int& val) {
    const int x = 17840;
    std::cout << 17840;
    for (int i = 0; i < 17840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17841(int& val) {
    func_in_17841(val);
    const int x = 17841;
    std::cout << 17841;
    for (int i = 0; i < 17841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17841(int& val) {
    const int x = 17841;
    std::cout << 17841;
    for (int i = 0; i < 17841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17842(int& val) {
    func_in_17842(val);
    const int x = 17842;
    std::cout << 17842;
    for (int i = 0; i < 17842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17842(int& val) {
    const int x = 17842;
    std::cout << 17842;
    for (int i = 0; i < 17842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17843(int& val) {
    func_in_17843(val);
    const int x = 17843;
    std::cout << 17843;
    for (int i = 0; i < 17843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17843(int& val) {
    const int x = 17843;
    std::cout << 17843;
    for (int i = 0; i < 17843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17844(int& val) {
    func_in_17844(val);
    const int x = 17844;
    std::cout << 17844;
    for (int i = 0; i < 17844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17844(int& val) {
    const int x = 17844;
    std::cout << 17844;
    for (int i = 0; i < 17844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17845(int& val) {
    func_in_17845(val);
    const int x = 17845;
    std::cout << 17845;
    for (int i = 0; i < 17845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17845(int& val) {
    const int x = 17845;
    std::cout << 17845;
    for (int i = 0; i < 17845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17846(int& val) {
    func_in_17846(val);
    const int x = 17846;
    std::cout << 17846;
    for (int i = 0; i < 17846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17846(int& val) {
    const int x = 17846;
    std::cout << 17846;
    for (int i = 0; i < 17846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17847(int& val) {
    func_in_17847(val);
    const int x = 17847;
    std::cout << 17847;
    for (int i = 0; i < 17847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17847(int& val) {
    const int x = 17847;
    std::cout << 17847;
    for (int i = 0; i < 17847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17848(int& val) {
    func_in_17848(val);
    const int x = 17848;
    std::cout << 17848;
    for (int i = 0; i < 17848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17848(int& val) {
    const int x = 17848;
    std::cout << 17848;
    for (int i = 0; i < 17848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17849(int& val) {
    func_in_17849(val);
    const int x = 17849;
    std::cout << 17849;
    for (int i = 0; i < 17849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17849(int& val) {
    const int x = 17849;
    std::cout << 17849;
    for (int i = 0; i < 17849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17850(int& val) {
    func_in_17850(val);
    const int x = 17850;
    std::cout << 17850;
    for (int i = 0; i < 17850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17850(int& val) {
    const int x = 17850;
    std::cout << 17850;
    for (int i = 0; i < 17850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17851(int& val) {
    func_in_17851(val);
    const int x = 17851;
    std::cout << 17851;
    for (int i = 0; i < 17851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17851(int& val) {
    const int x = 17851;
    std::cout << 17851;
    for (int i = 0; i < 17851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17852(int& val) {
    func_in_17852(val);
    const int x = 17852;
    std::cout << 17852;
    for (int i = 0; i < 17852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17852(int& val) {
    const int x = 17852;
    std::cout << 17852;
    for (int i = 0; i < 17852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17853(int& val) {
    func_in_17853(val);
    const int x = 17853;
    std::cout << 17853;
    for (int i = 0; i < 17853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17853(int& val) {
    const int x = 17853;
    std::cout << 17853;
    for (int i = 0; i < 17853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17854(int& val) {
    func_in_17854(val);
    const int x = 17854;
    std::cout << 17854;
    for (int i = 0; i < 17854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17854(int& val) {
    const int x = 17854;
    std::cout << 17854;
    for (int i = 0; i < 17854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17855(int& val) {
    func_in_17855(val);
    const int x = 17855;
    std::cout << 17855;
    for (int i = 0; i < 17855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17855(int& val) {
    const int x = 17855;
    std::cout << 17855;
    for (int i = 0; i < 17855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17856(int& val) {
    func_in_17856(val);
    const int x = 17856;
    std::cout << 17856;
    for (int i = 0; i < 17856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17856(int& val) {
    const int x = 17856;
    std::cout << 17856;
    for (int i = 0; i < 17856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17857(int& val) {
    func_in_17857(val);
    const int x = 17857;
    std::cout << 17857;
    for (int i = 0; i < 17857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17857(int& val) {
    const int x = 17857;
    std::cout << 17857;
    for (int i = 0; i < 17857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17858(int& val) {
    func_in_17858(val);
    const int x = 17858;
    std::cout << 17858;
    for (int i = 0; i < 17858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17858(int& val) {
    const int x = 17858;
    std::cout << 17858;
    for (int i = 0; i < 17858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17859(int& val) {
    func_in_17859(val);
    const int x = 17859;
    std::cout << 17859;
    for (int i = 0; i < 17859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17859(int& val) {
    const int x = 17859;
    std::cout << 17859;
    for (int i = 0; i < 17859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17860(int& val) {
    func_in_17860(val);
    const int x = 17860;
    std::cout << 17860;
    for (int i = 0; i < 17860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17860(int& val) {
    const int x = 17860;
    std::cout << 17860;
    for (int i = 0; i < 17860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17861(int& val) {
    func_in_17861(val);
    const int x = 17861;
    std::cout << 17861;
    for (int i = 0; i < 17861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17861(int& val) {
    const int x = 17861;
    std::cout << 17861;
    for (int i = 0; i < 17861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17862(int& val) {
    func_in_17862(val);
    const int x = 17862;
    std::cout << 17862;
    for (int i = 0; i < 17862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17862(int& val) {
    const int x = 17862;
    std::cout << 17862;
    for (int i = 0; i < 17862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17863(int& val) {
    func_in_17863(val);
    const int x = 17863;
    std::cout << 17863;
    for (int i = 0; i < 17863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17863(int& val) {
    const int x = 17863;
    std::cout << 17863;
    for (int i = 0; i < 17863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17864(int& val) {
    func_in_17864(val);
    const int x = 17864;
    std::cout << 17864;
    for (int i = 0; i < 17864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17864(int& val) {
    const int x = 17864;
    std::cout << 17864;
    for (int i = 0; i < 17864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17865(int& val) {
    func_in_17865(val);
    const int x = 17865;
    std::cout << 17865;
    for (int i = 0; i < 17865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17865(int& val) {
    const int x = 17865;
    std::cout << 17865;
    for (int i = 0; i < 17865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17866(int& val) {
    func_in_17866(val);
    const int x = 17866;
    std::cout << 17866;
    for (int i = 0; i < 17866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17866(int& val) {
    const int x = 17866;
    std::cout << 17866;
    for (int i = 0; i < 17866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17867(int& val) {
    func_in_17867(val);
    const int x = 17867;
    std::cout << 17867;
    for (int i = 0; i < 17867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17867(int& val) {
    const int x = 17867;
    std::cout << 17867;
    for (int i = 0; i < 17867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17868(int& val) {
    func_in_17868(val);
    const int x = 17868;
    std::cout << 17868;
    for (int i = 0; i < 17868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17868(int& val) {
    const int x = 17868;
    std::cout << 17868;
    for (int i = 0; i < 17868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17869(int& val) {
    func_in_17869(val);
    const int x = 17869;
    std::cout << 17869;
    for (int i = 0; i < 17869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17869(int& val) {
    const int x = 17869;
    std::cout << 17869;
    for (int i = 0; i < 17869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17870(int& val) {
    func_in_17870(val);
    const int x = 17870;
    std::cout << 17870;
    for (int i = 0; i < 17870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17870(int& val) {
    const int x = 17870;
    std::cout << 17870;
    for (int i = 0; i < 17870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17871(int& val) {
    func_in_17871(val);
    const int x = 17871;
    std::cout << 17871;
    for (int i = 0; i < 17871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17871(int& val) {
    const int x = 17871;
    std::cout << 17871;
    for (int i = 0; i < 17871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17872(int& val) {
    func_in_17872(val);
    const int x = 17872;
    std::cout << 17872;
    for (int i = 0; i < 17872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17872(int& val) {
    const int x = 17872;
    std::cout << 17872;
    for (int i = 0; i < 17872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17873(int& val) {
    func_in_17873(val);
    const int x = 17873;
    std::cout << 17873;
    for (int i = 0; i < 17873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17873(int& val) {
    const int x = 17873;
    std::cout << 17873;
    for (int i = 0; i < 17873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17874(int& val) {
    func_in_17874(val);
    const int x = 17874;
    std::cout << 17874;
    for (int i = 0; i < 17874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17874(int& val) {
    const int x = 17874;
    std::cout << 17874;
    for (int i = 0; i < 17874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17875(int& val) {
    func_in_17875(val);
    const int x = 17875;
    std::cout << 17875;
    for (int i = 0; i < 17875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17875(int& val) {
    const int x = 17875;
    std::cout << 17875;
    for (int i = 0; i < 17875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17876(int& val) {
    func_in_17876(val);
    const int x = 17876;
    std::cout << 17876;
    for (int i = 0; i < 17876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17876(int& val) {
    const int x = 17876;
    std::cout << 17876;
    for (int i = 0; i < 17876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17877(int& val) {
    func_in_17877(val);
    const int x = 17877;
    std::cout << 17877;
    for (int i = 0; i < 17877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17877(int& val) {
    const int x = 17877;
    std::cout << 17877;
    for (int i = 0; i < 17877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17878(int& val) {
    func_in_17878(val);
    const int x = 17878;
    std::cout << 17878;
    for (int i = 0; i < 17878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17878(int& val) {
    const int x = 17878;
    std::cout << 17878;
    for (int i = 0; i < 17878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17879(int& val) {
    func_in_17879(val);
    const int x = 17879;
    std::cout << 17879;
    for (int i = 0; i < 17879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17879(int& val) {
    const int x = 17879;
    std::cout << 17879;
    for (int i = 0; i < 17879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17880(int& val) {
    func_in_17880(val);
    const int x = 17880;
    std::cout << 17880;
    for (int i = 0; i < 17880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17880(int& val) {
    const int x = 17880;
    std::cout << 17880;
    for (int i = 0; i < 17880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17881(int& val) {
    func_in_17881(val);
    const int x = 17881;
    std::cout << 17881;
    for (int i = 0; i < 17881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17881(int& val) {
    const int x = 17881;
    std::cout << 17881;
    for (int i = 0; i < 17881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17882(int& val) {
    func_in_17882(val);
    const int x = 17882;
    std::cout << 17882;
    for (int i = 0; i < 17882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17882(int& val) {
    const int x = 17882;
    std::cout << 17882;
    for (int i = 0; i < 17882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17883(int& val) {
    func_in_17883(val);
    const int x = 17883;
    std::cout << 17883;
    for (int i = 0; i < 17883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17883(int& val) {
    const int x = 17883;
    std::cout << 17883;
    for (int i = 0; i < 17883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17884(int& val) {
    func_in_17884(val);
    const int x = 17884;
    std::cout << 17884;
    for (int i = 0; i < 17884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17884(int& val) {
    const int x = 17884;
    std::cout << 17884;
    for (int i = 0; i < 17884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17885(int& val) {
    func_in_17885(val);
    const int x = 17885;
    std::cout << 17885;
    for (int i = 0; i < 17885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17885(int& val) {
    const int x = 17885;
    std::cout << 17885;
    for (int i = 0; i < 17885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17886(int& val) {
    func_in_17886(val);
    const int x = 17886;
    std::cout << 17886;
    for (int i = 0; i < 17886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17886(int& val) {
    const int x = 17886;
    std::cout << 17886;
    for (int i = 0; i < 17886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17887(int& val) {
    func_in_17887(val);
    const int x = 17887;
    std::cout << 17887;
    for (int i = 0; i < 17887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17887(int& val) {
    const int x = 17887;
    std::cout << 17887;
    for (int i = 0; i < 17887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17888(int& val) {
    func_in_17888(val);
    const int x = 17888;
    std::cout << 17888;
    for (int i = 0; i < 17888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17888(int& val) {
    const int x = 17888;
    std::cout << 17888;
    for (int i = 0; i < 17888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17889(int& val) {
    func_in_17889(val);
    const int x = 17889;
    std::cout << 17889;
    for (int i = 0; i < 17889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17889(int& val) {
    const int x = 17889;
    std::cout << 17889;
    for (int i = 0; i < 17889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17890(int& val) {
    func_in_17890(val);
    const int x = 17890;
    std::cout << 17890;
    for (int i = 0; i < 17890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17890(int& val) {
    const int x = 17890;
    std::cout << 17890;
    for (int i = 0; i < 17890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17891(int& val) {
    func_in_17891(val);
    const int x = 17891;
    std::cout << 17891;
    for (int i = 0; i < 17891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17891(int& val) {
    const int x = 17891;
    std::cout << 17891;
    for (int i = 0; i < 17891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17892(int& val) {
    func_in_17892(val);
    const int x = 17892;
    std::cout << 17892;
    for (int i = 0; i < 17892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17892(int& val) {
    const int x = 17892;
    std::cout << 17892;
    for (int i = 0; i < 17892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17893(int& val) {
    func_in_17893(val);
    const int x = 17893;
    std::cout << 17893;
    for (int i = 0; i < 17893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17893(int& val) {
    const int x = 17893;
    std::cout << 17893;
    for (int i = 0; i < 17893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17894(int& val) {
    func_in_17894(val);
    const int x = 17894;
    std::cout << 17894;
    for (int i = 0; i < 17894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17894(int& val) {
    const int x = 17894;
    std::cout << 17894;
    for (int i = 0; i < 17894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17895(int& val) {
    func_in_17895(val);
    const int x = 17895;
    std::cout << 17895;
    for (int i = 0; i < 17895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17895(int& val) {
    const int x = 17895;
    std::cout << 17895;
    for (int i = 0; i < 17895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17896(int& val) {
    func_in_17896(val);
    const int x = 17896;
    std::cout << 17896;
    for (int i = 0; i < 17896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17896(int& val) {
    const int x = 17896;
    std::cout << 17896;
    for (int i = 0; i < 17896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17897(int& val) {
    func_in_17897(val);
    const int x = 17897;
    std::cout << 17897;
    for (int i = 0; i < 17897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17897(int& val) {
    const int x = 17897;
    std::cout << 17897;
    for (int i = 0; i < 17897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17898(int& val) {
    func_in_17898(val);
    const int x = 17898;
    std::cout << 17898;
    for (int i = 0; i < 17898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17898(int& val) {
    const int x = 17898;
    std::cout << 17898;
    for (int i = 0; i < 17898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17899(int& val) {
    func_in_17899(val);
    const int x = 17899;
    std::cout << 17899;
    for (int i = 0; i < 17899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17899(int& val) {
    const int x = 17899;
    std::cout << 17899;
    for (int i = 0; i < 17899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17900(int& val) {
    func_in_17900(val);
    const int x = 17900;
    std::cout << 17900;
    for (int i = 0; i < 17900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17900(int& val) {
    const int x = 17900;
    std::cout << 17900;
    for (int i = 0; i < 17900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17901(int& val) {
    func_in_17901(val);
    const int x = 17901;
    std::cout << 17901;
    for (int i = 0; i < 17901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17901(int& val) {
    const int x = 17901;
    std::cout << 17901;
    for (int i = 0; i < 17901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17902(int& val) {
    func_in_17902(val);
    const int x = 17902;
    std::cout << 17902;
    for (int i = 0; i < 17902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17902(int& val) {
    const int x = 17902;
    std::cout << 17902;
    for (int i = 0; i < 17902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17903(int& val) {
    func_in_17903(val);
    const int x = 17903;
    std::cout << 17903;
    for (int i = 0; i < 17903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17903(int& val) {
    const int x = 17903;
    std::cout << 17903;
    for (int i = 0; i < 17903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17904(int& val) {
    func_in_17904(val);
    const int x = 17904;
    std::cout << 17904;
    for (int i = 0; i < 17904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17904(int& val) {
    const int x = 17904;
    std::cout << 17904;
    for (int i = 0; i < 17904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17905(int& val) {
    func_in_17905(val);
    const int x = 17905;
    std::cout << 17905;
    for (int i = 0; i < 17905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17905(int& val) {
    const int x = 17905;
    std::cout << 17905;
    for (int i = 0; i < 17905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17906(int& val) {
    func_in_17906(val);
    const int x = 17906;
    std::cout << 17906;
    for (int i = 0; i < 17906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17906(int& val) {
    const int x = 17906;
    std::cout << 17906;
    for (int i = 0; i < 17906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17907(int& val) {
    func_in_17907(val);
    const int x = 17907;
    std::cout << 17907;
    for (int i = 0; i < 17907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17907(int& val) {
    const int x = 17907;
    std::cout << 17907;
    for (int i = 0; i < 17907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17908(int& val) {
    func_in_17908(val);
    const int x = 17908;
    std::cout << 17908;
    for (int i = 0; i < 17908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17908(int& val) {
    const int x = 17908;
    std::cout << 17908;
    for (int i = 0; i < 17908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17909(int& val) {
    func_in_17909(val);
    const int x = 17909;
    std::cout << 17909;
    for (int i = 0; i < 17909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17909(int& val) {
    const int x = 17909;
    std::cout << 17909;
    for (int i = 0; i < 17909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17910(int& val) {
    func_in_17910(val);
    const int x = 17910;
    std::cout << 17910;
    for (int i = 0; i < 17910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17910(int& val) {
    const int x = 17910;
    std::cout << 17910;
    for (int i = 0; i < 17910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17911(int& val) {
    func_in_17911(val);
    const int x = 17911;
    std::cout << 17911;
    for (int i = 0; i < 17911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17911(int& val) {
    const int x = 17911;
    std::cout << 17911;
    for (int i = 0; i < 17911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17912(int& val) {
    func_in_17912(val);
    const int x = 17912;
    std::cout << 17912;
    for (int i = 0; i < 17912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17912(int& val) {
    const int x = 17912;
    std::cout << 17912;
    for (int i = 0; i < 17912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17913(int& val) {
    func_in_17913(val);
    const int x = 17913;
    std::cout << 17913;
    for (int i = 0; i < 17913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17913(int& val) {
    const int x = 17913;
    std::cout << 17913;
    for (int i = 0; i < 17913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17914(int& val) {
    func_in_17914(val);
    const int x = 17914;
    std::cout << 17914;
    for (int i = 0; i < 17914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17914(int& val) {
    const int x = 17914;
    std::cout << 17914;
    for (int i = 0; i < 17914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17915(int& val) {
    func_in_17915(val);
    const int x = 17915;
    std::cout << 17915;
    for (int i = 0; i < 17915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17915(int& val) {
    const int x = 17915;
    std::cout << 17915;
    for (int i = 0; i < 17915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17916(int& val) {
    func_in_17916(val);
    const int x = 17916;
    std::cout << 17916;
    for (int i = 0; i < 17916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17916(int& val) {
    const int x = 17916;
    std::cout << 17916;
    for (int i = 0; i < 17916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17917(int& val) {
    func_in_17917(val);
    const int x = 17917;
    std::cout << 17917;
    for (int i = 0; i < 17917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17917(int& val) {
    const int x = 17917;
    std::cout << 17917;
    for (int i = 0; i < 17917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17918(int& val) {
    func_in_17918(val);
    const int x = 17918;
    std::cout << 17918;
    for (int i = 0; i < 17918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17918(int& val) {
    const int x = 17918;
    std::cout << 17918;
    for (int i = 0; i < 17918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17919(int& val) {
    func_in_17919(val);
    const int x = 17919;
    std::cout << 17919;
    for (int i = 0; i < 17919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17919(int& val) {
    const int x = 17919;
    std::cout << 17919;
    for (int i = 0; i < 17919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17920(int& val) {
    func_in_17920(val);
    const int x = 17920;
    std::cout << 17920;
    for (int i = 0; i < 17920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17920(int& val) {
    const int x = 17920;
    std::cout << 17920;
    for (int i = 0; i < 17920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17921(int& val) {
    func_in_17921(val);
    const int x = 17921;
    std::cout << 17921;
    for (int i = 0; i < 17921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17921(int& val) {
    const int x = 17921;
    std::cout << 17921;
    for (int i = 0; i < 17921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17922(int& val) {
    func_in_17922(val);
    const int x = 17922;
    std::cout << 17922;
    for (int i = 0; i < 17922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17922(int& val) {
    const int x = 17922;
    std::cout << 17922;
    for (int i = 0; i < 17922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17923(int& val) {
    func_in_17923(val);
    const int x = 17923;
    std::cout << 17923;
    for (int i = 0; i < 17923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17923(int& val) {
    const int x = 17923;
    std::cout << 17923;
    for (int i = 0; i < 17923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17924(int& val) {
    func_in_17924(val);
    const int x = 17924;
    std::cout << 17924;
    for (int i = 0; i < 17924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17924(int& val) {
    const int x = 17924;
    std::cout << 17924;
    for (int i = 0; i < 17924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17925(int& val) {
    func_in_17925(val);
    const int x = 17925;
    std::cout << 17925;
    for (int i = 0; i < 17925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17925(int& val) {
    const int x = 17925;
    std::cout << 17925;
    for (int i = 0; i < 17925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17926(int& val) {
    func_in_17926(val);
    const int x = 17926;
    std::cout << 17926;
    for (int i = 0; i < 17926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17926(int& val) {
    const int x = 17926;
    std::cout << 17926;
    for (int i = 0; i < 17926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17927(int& val) {
    func_in_17927(val);
    const int x = 17927;
    std::cout << 17927;
    for (int i = 0; i < 17927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17927(int& val) {
    const int x = 17927;
    std::cout << 17927;
    for (int i = 0; i < 17927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17928(int& val) {
    func_in_17928(val);
    const int x = 17928;
    std::cout << 17928;
    for (int i = 0; i < 17928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17928(int& val) {
    const int x = 17928;
    std::cout << 17928;
    for (int i = 0; i < 17928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17929(int& val) {
    func_in_17929(val);
    const int x = 17929;
    std::cout << 17929;
    for (int i = 0; i < 17929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17929(int& val) {
    const int x = 17929;
    std::cout << 17929;
    for (int i = 0; i < 17929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17930(int& val) {
    func_in_17930(val);
    const int x = 17930;
    std::cout << 17930;
    for (int i = 0; i < 17930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17930(int& val) {
    const int x = 17930;
    std::cout << 17930;
    for (int i = 0; i < 17930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17931(int& val) {
    func_in_17931(val);
    const int x = 17931;
    std::cout << 17931;
    for (int i = 0; i < 17931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17931(int& val) {
    const int x = 17931;
    std::cout << 17931;
    for (int i = 0; i < 17931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17932(int& val) {
    func_in_17932(val);
    const int x = 17932;
    std::cout << 17932;
    for (int i = 0; i < 17932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17932(int& val) {
    const int x = 17932;
    std::cout << 17932;
    for (int i = 0; i < 17932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17933(int& val) {
    func_in_17933(val);
    const int x = 17933;
    std::cout << 17933;
    for (int i = 0; i < 17933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17933(int& val) {
    const int x = 17933;
    std::cout << 17933;
    for (int i = 0; i < 17933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17934(int& val) {
    func_in_17934(val);
    const int x = 17934;
    std::cout << 17934;
    for (int i = 0; i < 17934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17934(int& val) {
    const int x = 17934;
    std::cout << 17934;
    for (int i = 0; i < 17934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17935(int& val) {
    func_in_17935(val);
    const int x = 17935;
    std::cout << 17935;
    for (int i = 0; i < 17935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17935(int& val) {
    const int x = 17935;
    std::cout << 17935;
    for (int i = 0; i < 17935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17936(int& val) {
    func_in_17936(val);
    const int x = 17936;
    std::cout << 17936;
    for (int i = 0; i < 17936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17936(int& val) {
    const int x = 17936;
    std::cout << 17936;
    for (int i = 0; i < 17936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17937(int& val) {
    func_in_17937(val);
    const int x = 17937;
    std::cout << 17937;
    for (int i = 0; i < 17937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17937(int& val) {
    const int x = 17937;
    std::cout << 17937;
    for (int i = 0; i < 17937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17938(int& val) {
    func_in_17938(val);
    const int x = 17938;
    std::cout << 17938;
    for (int i = 0; i < 17938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17938(int& val) {
    const int x = 17938;
    std::cout << 17938;
    for (int i = 0; i < 17938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17939(int& val) {
    func_in_17939(val);
    const int x = 17939;
    std::cout << 17939;
    for (int i = 0; i < 17939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17939(int& val) {
    const int x = 17939;
    std::cout << 17939;
    for (int i = 0; i < 17939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17940(int& val) {
    func_in_17940(val);
    const int x = 17940;
    std::cout << 17940;
    for (int i = 0; i < 17940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17940(int& val) {
    const int x = 17940;
    std::cout << 17940;
    for (int i = 0; i < 17940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17941(int& val) {
    func_in_17941(val);
    const int x = 17941;
    std::cout << 17941;
    for (int i = 0; i < 17941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17941(int& val) {
    const int x = 17941;
    std::cout << 17941;
    for (int i = 0; i < 17941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17942(int& val) {
    func_in_17942(val);
    const int x = 17942;
    std::cout << 17942;
    for (int i = 0; i < 17942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17942(int& val) {
    const int x = 17942;
    std::cout << 17942;
    for (int i = 0; i < 17942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17943(int& val) {
    func_in_17943(val);
    const int x = 17943;
    std::cout << 17943;
    for (int i = 0; i < 17943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17943(int& val) {
    const int x = 17943;
    std::cout << 17943;
    for (int i = 0; i < 17943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17944(int& val) {
    func_in_17944(val);
    const int x = 17944;
    std::cout << 17944;
    for (int i = 0; i < 17944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17944(int& val) {
    const int x = 17944;
    std::cout << 17944;
    for (int i = 0; i < 17944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17945(int& val) {
    func_in_17945(val);
    const int x = 17945;
    std::cout << 17945;
    for (int i = 0; i < 17945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17945(int& val) {
    const int x = 17945;
    std::cout << 17945;
    for (int i = 0; i < 17945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17946(int& val) {
    func_in_17946(val);
    const int x = 17946;
    std::cout << 17946;
    for (int i = 0; i < 17946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17946(int& val) {
    const int x = 17946;
    std::cout << 17946;
    for (int i = 0; i < 17946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17947(int& val) {
    func_in_17947(val);
    const int x = 17947;
    std::cout << 17947;
    for (int i = 0; i < 17947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17947(int& val) {
    const int x = 17947;
    std::cout << 17947;
    for (int i = 0; i < 17947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17948(int& val) {
    func_in_17948(val);
    const int x = 17948;
    std::cout << 17948;
    for (int i = 0; i < 17948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17948(int& val) {
    const int x = 17948;
    std::cout << 17948;
    for (int i = 0; i < 17948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17949(int& val) {
    func_in_17949(val);
    const int x = 17949;
    std::cout << 17949;
    for (int i = 0; i < 17949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17949(int& val) {
    const int x = 17949;
    std::cout << 17949;
    for (int i = 0; i < 17949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17950(int& val) {
    func_in_17950(val);
    const int x = 17950;
    std::cout << 17950;
    for (int i = 0; i < 17950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17950(int& val) {
    const int x = 17950;
    std::cout << 17950;
    for (int i = 0; i < 17950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17951(int& val) {
    func_in_17951(val);
    const int x = 17951;
    std::cout << 17951;
    for (int i = 0; i < 17951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17951(int& val) {
    const int x = 17951;
    std::cout << 17951;
    for (int i = 0; i < 17951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17952(int& val) {
    func_in_17952(val);
    const int x = 17952;
    std::cout << 17952;
    for (int i = 0; i < 17952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17952(int& val) {
    const int x = 17952;
    std::cout << 17952;
    for (int i = 0; i < 17952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17953(int& val) {
    func_in_17953(val);
    const int x = 17953;
    std::cout << 17953;
    for (int i = 0; i < 17953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17953(int& val) {
    const int x = 17953;
    std::cout << 17953;
    for (int i = 0; i < 17953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17954(int& val) {
    func_in_17954(val);
    const int x = 17954;
    std::cout << 17954;
    for (int i = 0; i < 17954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17954(int& val) {
    const int x = 17954;
    std::cout << 17954;
    for (int i = 0; i < 17954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17955(int& val) {
    func_in_17955(val);
    const int x = 17955;
    std::cout << 17955;
    for (int i = 0; i < 17955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17955(int& val) {
    const int x = 17955;
    std::cout << 17955;
    for (int i = 0; i < 17955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17956(int& val) {
    func_in_17956(val);
    const int x = 17956;
    std::cout << 17956;
    for (int i = 0; i < 17956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17956(int& val) {
    const int x = 17956;
    std::cout << 17956;
    for (int i = 0; i < 17956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17957(int& val) {
    func_in_17957(val);
    const int x = 17957;
    std::cout << 17957;
    for (int i = 0; i < 17957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17957(int& val) {
    const int x = 17957;
    std::cout << 17957;
    for (int i = 0; i < 17957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17958(int& val) {
    func_in_17958(val);
    const int x = 17958;
    std::cout << 17958;
    for (int i = 0; i < 17958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17958(int& val) {
    const int x = 17958;
    std::cout << 17958;
    for (int i = 0; i < 17958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17959(int& val) {
    func_in_17959(val);
    const int x = 17959;
    std::cout << 17959;
    for (int i = 0; i < 17959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17959(int& val) {
    const int x = 17959;
    std::cout << 17959;
    for (int i = 0; i < 17959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17960(int& val) {
    func_in_17960(val);
    const int x = 17960;
    std::cout << 17960;
    for (int i = 0; i < 17960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17960(int& val) {
    const int x = 17960;
    std::cout << 17960;
    for (int i = 0; i < 17960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17961(int& val) {
    func_in_17961(val);
    const int x = 17961;
    std::cout << 17961;
    for (int i = 0; i < 17961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17961(int& val) {
    const int x = 17961;
    std::cout << 17961;
    for (int i = 0; i < 17961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17962(int& val) {
    func_in_17962(val);
    const int x = 17962;
    std::cout << 17962;
    for (int i = 0; i < 17962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17962(int& val) {
    const int x = 17962;
    std::cout << 17962;
    for (int i = 0; i < 17962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17963(int& val) {
    func_in_17963(val);
    const int x = 17963;
    std::cout << 17963;
    for (int i = 0; i < 17963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17963(int& val) {
    const int x = 17963;
    std::cout << 17963;
    for (int i = 0; i < 17963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17964(int& val) {
    func_in_17964(val);
    const int x = 17964;
    std::cout << 17964;
    for (int i = 0; i < 17964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17964(int& val) {
    const int x = 17964;
    std::cout << 17964;
    for (int i = 0; i < 17964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17965(int& val) {
    func_in_17965(val);
    const int x = 17965;
    std::cout << 17965;
    for (int i = 0; i < 17965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17965(int& val) {
    const int x = 17965;
    std::cout << 17965;
    for (int i = 0; i < 17965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17966(int& val) {
    func_in_17966(val);
    const int x = 17966;
    std::cout << 17966;
    for (int i = 0; i < 17966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17966(int& val) {
    const int x = 17966;
    std::cout << 17966;
    for (int i = 0; i < 17966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17967(int& val) {
    func_in_17967(val);
    const int x = 17967;
    std::cout << 17967;
    for (int i = 0; i < 17967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17967(int& val) {
    const int x = 17967;
    std::cout << 17967;
    for (int i = 0; i < 17967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17968(int& val) {
    func_in_17968(val);
    const int x = 17968;
    std::cout << 17968;
    for (int i = 0; i < 17968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17968(int& val) {
    const int x = 17968;
    std::cout << 17968;
    for (int i = 0; i < 17968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17969(int& val) {
    func_in_17969(val);
    const int x = 17969;
    std::cout << 17969;
    for (int i = 0; i < 17969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17969(int& val) {
    const int x = 17969;
    std::cout << 17969;
    for (int i = 0; i < 17969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17970(int& val) {
    func_in_17970(val);
    const int x = 17970;
    std::cout << 17970;
    for (int i = 0; i < 17970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17970(int& val) {
    const int x = 17970;
    std::cout << 17970;
    for (int i = 0; i < 17970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17971(int& val) {
    func_in_17971(val);
    const int x = 17971;
    std::cout << 17971;
    for (int i = 0; i < 17971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17971(int& val) {
    const int x = 17971;
    std::cout << 17971;
    for (int i = 0; i < 17971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17972(int& val) {
    func_in_17972(val);
    const int x = 17972;
    std::cout << 17972;
    for (int i = 0; i < 17972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17972(int& val) {
    const int x = 17972;
    std::cout << 17972;
    for (int i = 0; i < 17972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17973(int& val) {
    func_in_17973(val);
    const int x = 17973;
    std::cout << 17973;
    for (int i = 0; i < 17973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17973(int& val) {
    const int x = 17973;
    std::cout << 17973;
    for (int i = 0; i < 17973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17974(int& val) {
    func_in_17974(val);
    const int x = 17974;
    std::cout << 17974;
    for (int i = 0; i < 17974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17974(int& val) {
    const int x = 17974;
    std::cout << 17974;
    for (int i = 0; i < 17974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17975(int& val) {
    func_in_17975(val);
    const int x = 17975;
    std::cout << 17975;
    for (int i = 0; i < 17975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17975(int& val) {
    const int x = 17975;
    std::cout << 17975;
    for (int i = 0; i < 17975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17976(int& val) {
    func_in_17976(val);
    const int x = 17976;
    std::cout << 17976;
    for (int i = 0; i < 17976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17976(int& val) {
    const int x = 17976;
    std::cout << 17976;
    for (int i = 0; i < 17976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17977(int& val) {
    func_in_17977(val);
    const int x = 17977;
    std::cout << 17977;
    for (int i = 0; i < 17977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17977(int& val) {
    const int x = 17977;
    std::cout << 17977;
    for (int i = 0; i < 17977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17978(int& val) {
    func_in_17978(val);
    const int x = 17978;
    std::cout << 17978;
    for (int i = 0; i < 17978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17978(int& val) {
    const int x = 17978;
    std::cout << 17978;
    for (int i = 0; i < 17978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17979(int& val) {
    func_in_17979(val);
    const int x = 17979;
    std::cout << 17979;
    for (int i = 0; i < 17979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17979(int& val) {
    const int x = 17979;
    std::cout << 17979;
    for (int i = 0; i < 17979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17980(int& val) {
    func_in_17980(val);
    const int x = 17980;
    std::cout << 17980;
    for (int i = 0; i < 17980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17980(int& val) {
    const int x = 17980;
    std::cout << 17980;
    for (int i = 0; i < 17980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17981(int& val) {
    func_in_17981(val);
    const int x = 17981;
    std::cout << 17981;
    for (int i = 0; i < 17981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17981(int& val) {
    const int x = 17981;
    std::cout << 17981;
    for (int i = 0; i < 17981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17982(int& val) {
    func_in_17982(val);
    const int x = 17982;
    std::cout << 17982;
    for (int i = 0; i < 17982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17982(int& val) {
    const int x = 17982;
    std::cout << 17982;
    for (int i = 0; i < 17982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17983(int& val) {
    func_in_17983(val);
    const int x = 17983;
    std::cout << 17983;
    for (int i = 0; i < 17983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17983(int& val) {
    const int x = 17983;
    std::cout << 17983;
    for (int i = 0; i < 17983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17984(int& val) {
    func_in_17984(val);
    const int x = 17984;
    std::cout << 17984;
    for (int i = 0; i < 17984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17984(int& val) {
    const int x = 17984;
    std::cout << 17984;
    for (int i = 0; i < 17984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17985(int& val) {
    func_in_17985(val);
    const int x = 17985;
    std::cout << 17985;
    for (int i = 0; i < 17985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17985(int& val) {
    const int x = 17985;
    std::cout << 17985;
    for (int i = 0; i < 17985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17986(int& val) {
    func_in_17986(val);
    const int x = 17986;
    std::cout << 17986;
    for (int i = 0; i < 17986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17986(int& val) {
    const int x = 17986;
    std::cout << 17986;
    for (int i = 0; i < 17986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17987(int& val) {
    func_in_17987(val);
    const int x = 17987;
    std::cout << 17987;
    for (int i = 0; i < 17987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17987(int& val) {
    const int x = 17987;
    std::cout << 17987;
    for (int i = 0; i < 17987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17988(int& val) {
    func_in_17988(val);
    const int x = 17988;
    std::cout << 17988;
    for (int i = 0; i < 17988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17988(int& val) {
    const int x = 17988;
    std::cout << 17988;
    for (int i = 0; i < 17988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17989(int& val) {
    func_in_17989(val);
    const int x = 17989;
    std::cout << 17989;
    for (int i = 0; i < 17989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17989(int& val) {
    const int x = 17989;
    std::cout << 17989;
    for (int i = 0; i < 17989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17990(int& val) {
    func_in_17990(val);
    const int x = 17990;
    std::cout << 17990;
    for (int i = 0; i < 17990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17990(int& val) {
    const int x = 17990;
    std::cout << 17990;
    for (int i = 0; i < 17990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17991(int& val) {
    func_in_17991(val);
    const int x = 17991;
    std::cout << 17991;
    for (int i = 0; i < 17991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17991(int& val) {
    const int x = 17991;
    std::cout << 17991;
    for (int i = 0; i < 17991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17992(int& val) {
    func_in_17992(val);
    const int x = 17992;
    std::cout << 17992;
    for (int i = 0; i < 17992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17992(int& val) {
    const int x = 17992;
    std::cout << 17992;
    for (int i = 0; i < 17992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17993(int& val) {
    func_in_17993(val);
    const int x = 17993;
    std::cout << 17993;
    for (int i = 0; i < 17993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17993(int& val) {
    const int x = 17993;
    std::cout << 17993;
    for (int i = 0; i < 17993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17994(int& val) {
    func_in_17994(val);
    const int x = 17994;
    std::cout << 17994;
    for (int i = 0; i < 17994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17994(int& val) {
    const int x = 17994;
    std::cout << 17994;
    for (int i = 0; i < 17994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17995(int& val) {
    func_in_17995(val);
    const int x = 17995;
    std::cout << 17995;
    for (int i = 0; i < 17995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17995(int& val) {
    const int x = 17995;
    std::cout << 17995;
    for (int i = 0; i < 17995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17996(int& val) {
    func_in_17996(val);
    const int x = 17996;
    std::cout << 17996;
    for (int i = 0; i < 17996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17996(int& val) {
    const int x = 17996;
    std::cout << 17996;
    for (int i = 0; i < 17996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17997(int& val) {
    func_in_17997(val);
    const int x = 17997;
    std::cout << 17997;
    for (int i = 0; i < 17997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17997(int& val) {
    const int x = 17997;
    std::cout << 17997;
    for (int i = 0; i < 17997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17998(int& val) {
    func_in_17998(val);
    const int x = 17998;
    std::cout << 17998;
    for (int i = 0; i < 17998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17998(int& val) {
    const int x = 17998;
    std::cout << 17998;
    for (int i = 0; i < 17998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17999(int& val) {
    func_in_17999(val);
    const int x = 17999;
    std::cout << 17999;
    for (int i = 0; i < 17999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17999(int& val) {
    const int x = 17999;
    std::cout << 17999;
    for (int i = 0; i < 17999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18000(int& val) {
    func_in_18000(val);
    const int x = 18000;
    std::cout << 18000;
    for (int i = 0; i < 18000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18000(int& val) {
    const int x = 18000;
    std::cout << 18000;
    for (int i = 0; i < 18000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18001(int& val) {
    func_in_18001(val);
    const int x = 18001;
    std::cout << 18001;
    for (int i = 0; i < 18001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18001(int& val) {
    const int x = 18001;
    std::cout << 18001;
    for (int i = 0; i < 18001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18002(int& val) {
    func_in_18002(val);
    const int x = 18002;
    std::cout << 18002;
    for (int i = 0; i < 18002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18002(int& val) {
    const int x = 18002;
    std::cout << 18002;
    for (int i = 0; i < 18002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18003(int& val) {
    func_in_18003(val);
    const int x = 18003;
    std::cout << 18003;
    for (int i = 0; i < 18003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18003(int& val) {
    const int x = 18003;
    std::cout << 18003;
    for (int i = 0; i < 18003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18004(int& val) {
    func_in_18004(val);
    const int x = 18004;
    std::cout << 18004;
    for (int i = 0; i < 18004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18004(int& val) {
    const int x = 18004;
    std::cout << 18004;
    for (int i = 0; i < 18004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18005(int& val) {
    func_in_18005(val);
    const int x = 18005;
    std::cout << 18005;
    for (int i = 0; i < 18005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18005(int& val) {
    const int x = 18005;
    std::cout << 18005;
    for (int i = 0; i < 18005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18006(int& val) {
    func_in_18006(val);
    const int x = 18006;
    std::cout << 18006;
    for (int i = 0; i < 18006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18006(int& val) {
    const int x = 18006;
    std::cout << 18006;
    for (int i = 0; i < 18006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18007(int& val) {
    func_in_18007(val);
    const int x = 18007;
    std::cout << 18007;
    for (int i = 0; i < 18007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18007(int& val) {
    const int x = 18007;
    std::cout << 18007;
    for (int i = 0; i < 18007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18008(int& val) {
    func_in_18008(val);
    const int x = 18008;
    std::cout << 18008;
    for (int i = 0; i < 18008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18008(int& val) {
    const int x = 18008;
    std::cout << 18008;
    for (int i = 0; i < 18008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18009(int& val) {
    func_in_18009(val);
    const int x = 18009;
    std::cout << 18009;
    for (int i = 0; i < 18009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18009(int& val) {
    const int x = 18009;
    std::cout << 18009;
    for (int i = 0; i < 18009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18010(int& val) {
    func_in_18010(val);
    const int x = 18010;
    std::cout << 18010;
    for (int i = 0; i < 18010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18010(int& val) {
    const int x = 18010;
    std::cout << 18010;
    for (int i = 0; i < 18010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18011(int& val) {
    func_in_18011(val);
    const int x = 18011;
    std::cout << 18011;
    for (int i = 0; i < 18011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18011(int& val) {
    const int x = 18011;
    std::cout << 18011;
    for (int i = 0; i < 18011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18012(int& val) {
    func_in_18012(val);
    const int x = 18012;
    std::cout << 18012;
    for (int i = 0; i < 18012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18012(int& val) {
    const int x = 18012;
    std::cout << 18012;
    for (int i = 0; i < 18012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18013(int& val) {
    func_in_18013(val);
    const int x = 18013;
    std::cout << 18013;
    for (int i = 0; i < 18013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18013(int& val) {
    const int x = 18013;
    std::cout << 18013;
    for (int i = 0; i < 18013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18014(int& val) {
    func_in_18014(val);
    const int x = 18014;
    std::cout << 18014;
    for (int i = 0; i < 18014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18014(int& val) {
    const int x = 18014;
    std::cout << 18014;
    for (int i = 0; i < 18014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18015(int& val) {
    func_in_18015(val);
    const int x = 18015;
    std::cout << 18015;
    for (int i = 0; i < 18015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18015(int& val) {
    const int x = 18015;
    std::cout << 18015;
    for (int i = 0; i < 18015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18016(int& val) {
    func_in_18016(val);
    const int x = 18016;
    std::cout << 18016;
    for (int i = 0; i < 18016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18016(int& val) {
    const int x = 18016;
    std::cout << 18016;
    for (int i = 0; i < 18016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18017(int& val) {
    func_in_18017(val);
    const int x = 18017;
    std::cout << 18017;
    for (int i = 0; i < 18017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18017(int& val) {
    const int x = 18017;
    std::cout << 18017;
    for (int i = 0; i < 18017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18018(int& val) {
    func_in_18018(val);
    const int x = 18018;
    std::cout << 18018;
    for (int i = 0; i < 18018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18018(int& val) {
    const int x = 18018;
    std::cout << 18018;
    for (int i = 0; i < 18018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18019(int& val) {
    func_in_18019(val);
    const int x = 18019;
    std::cout << 18019;
    for (int i = 0; i < 18019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18019(int& val) {
    const int x = 18019;
    std::cout << 18019;
    for (int i = 0; i < 18019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18020(int& val) {
    func_in_18020(val);
    const int x = 18020;
    std::cout << 18020;
    for (int i = 0; i < 18020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18020(int& val) {
    const int x = 18020;
    std::cout << 18020;
    for (int i = 0; i < 18020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18021(int& val) {
    func_in_18021(val);
    const int x = 18021;
    std::cout << 18021;
    for (int i = 0; i < 18021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18021(int& val) {
    const int x = 18021;
    std::cout << 18021;
    for (int i = 0; i < 18021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18022(int& val) {
    func_in_18022(val);
    const int x = 18022;
    std::cout << 18022;
    for (int i = 0; i < 18022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18022(int& val) {
    const int x = 18022;
    std::cout << 18022;
    for (int i = 0; i < 18022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18023(int& val) {
    func_in_18023(val);
    const int x = 18023;
    std::cout << 18023;
    for (int i = 0; i < 18023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18023(int& val) {
    const int x = 18023;
    std::cout << 18023;
    for (int i = 0; i < 18023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18024(int& val) {
    func_in_18024(val);
    const int x = 18024;
    std::cout << 18024;
    for (int i = 0; i < 18024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18024(int& val) {
    const int x = 18024;
    std::cout << 18024;
    for (int i = 0; i < 18024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18025(int& val) {
    func_in_18025(val);
    const int x = 18025;
    std::cout << 18025;
    for (int i = 0; i < 18025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18025(int& val) {
    const int x = 18025;
    std::cout << 18025;
    for (int i = 0; i < 18025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18026(int& val) {
    func_in_18026(val);
    const int x = 18026;
    std::cout << 18026;
    for (int i = 0; i < 18026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18026(int& val) {
    const int x = 18026;
    std::cout << 18026;
    for (int i = 0; i < 18026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18027(int& val) {
    func_in_18027(val);
    const int x = 18027;
    std::cout << 18027;
    for (int i = 0; i < 18027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18027(int& val) {
    const int x = 18027;
    std::cout << 18027;
    for (int i = 0; i < 18027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18028(int& val) {
    func_in_18028(val);
    const int x = 18028;
    std::cout << 18028;
    for (int i = 0; i < 18028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18028(int& val) {
    const int x = 18028;
    std::cout << 18028;
    for (int i = 0; i < 18028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18029(int& val) {
    func_in_18029(val);
    const int x = 18029;
    std::cout << 18029;
    for (int i = 0; i < 18029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18029(int& val) {
    const int x = 18029;
    std::cout << 18029;
    for (int i = 0; i < 18029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18030(int& val) {
    func_in_18030(val);
    const int x = 18030;
    std::cout << 18030;
    for (int i = 0; i < 18030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18030(int& val) {
    const int x = 18030;
    std::cout << 18030;
    for (int i = 0; i < 18030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18031(int& val) {
    func_in_18031(val);
    const int x = 18031;
    std::cout << 18031;
    for (int i = 0; i < 18031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18031(int& val) {
    const int x = 18031;
    std::cout << 18031;
    for (int i = 0; i < 18031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18032(int& val) {
    func_in_18032(val);
    const int x = 18032;
    std::cout << 18032;
    for (int i = 0; i < 18032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18032(int& val) {
    const int x = 18032;
    std::cout << 18032;
    for (int i = 0; i < 18032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18033(int& val) {
    func_in_18033(val);
    const int x = 18033;
    std::cout << 18033;
    for (int i = 0; i < 18033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18033(int& val) {
    const int x = 18033;
    std::cout << 18033;
    for (int i = 0; i < 18033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18034(int& val) {
    func_in_18034(val);
    const int x = 18034;
    std::cout << 18034;
    for (int i = 0; i < 18034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18034(int& val) {
    const int x = 18034;
    std::cout << 18034;
    for (int i = 0; i < 18034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18035(int& val) {
    func_in_18035(val);
    const int x = 18035;
    std::cout << 18035;
    for (int i = 0; i < 18035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18035(int& val) {
    const int x = 18035;
    std::cout << 18035;
    for (int i = 0; i < 18035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18036(int& val) {
    func_in_18036(val);
    const int x = 18036;
    std::cout << 18036;
    for (int i = 0; i < 18036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18036(int& val) {
    const int x = 18036;
    std::cout << 18036;
    for (int i = 0; i < 18036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18037(int& val) {
    func_in_18037(val);
    const int x = 18037;
    std::cout << 18037;
    for (int i = 0; i < 18037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18037(int& val) {
    const int x = 18037;
    std::cout << 18037;
    for (int i = 0; i < 18037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18038(int& val) {
    func_in_18038(val);
    const int x = 18038;
    std::cout << 18038;
    for (int i = 0; i < 18038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18038(int& val) {
    const int x = 18038;
    std::cout << 18038;
    for (int i = 0; i < 18038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18039(int& val) {
    func_in_18039(val);
    const int x = 18039;
    std::cout << 18039;
    for (int i = 0; i < 18039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18039(int& val) {
    const int x = 18039;
    std::cout << 18039;
    for (int i = 0; i < 18039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18040(int& val) {
    func_in_18040(val);
    const int x = 18040;
    std::cout << 18040;
    for (int i = 0; i < 18040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18040(int& val) {
    const int x = 18040;
    std::cout << 18040;
    for (int i = 0; i < 18040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18041(int& val) {
    func_in_18041(val);
    const int x = 18041;
    std::cout << 18041;
    for (int i = 0; i < 18041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18041(int& val) {
    const int x = 18041;
    std::cout << 18041;
    for (int i = 0; i < 18041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18042(int& val) {
    func_in_18042(val);
    const int x = 18042;
    std::cout << 18042;
    for (int i = 0; i < 18042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18042(int& val) {
    const int x = 18042;
    std::cout << 18042;
    for (int i = 0; i < 18042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18043(int& val) {
    func_in_18043(val);
    const int x = 18043;
    std::cout << 18043;
    for (int i = 0; i < 18043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18043(int& val) {
    const int x = 18043;
    std::cout << 18043;
    for (int i = 0; i < 18043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18044(int& val) {
    func_in_18044(val);
    const int x = 18044;
    std::cout << 18044;
    for (int i = 0; i < 18044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18044(int& val) {
    const int x = 18044;
    std::cout << 18044;
    for (int i = 0; i < 18044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18045(int& val) {
    func_in_18045(val);
    const int x = 18045;
    std::cout << 18045;
    for (int i = 0; i < 18045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18045(int& val) {
    const int x = 18045;
    std::cout << 18045;
    for (int i = 0; i < 18045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18046(int& val) {
    func_in_18046(val);
    const int x = 18046;
    std::cout << 18046;
    for (int i = 0; i < 18046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18046(int& val) {
    const int x = 18046;
    std::cout << 18046;
    for (int i = 0; i < 18046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18047(int& val) {
    func_in_18047(val);
    const int x = 18047;
    std::cout << 18047;
    for (int i = 0; i < 18047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18047(int& val) {
    const int x = 18047;
    std::cout << 18047;
    for (int i = 0; i < 18047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18048(int& val) {
    func_in_18048(val);
    const int x = 18048;
    std::cout << 18048;
    for (int i = 0; i < 18048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18048(int& val) {
    const int x = 18048;
    std::cout << 18048;
    for (int i = 0; i < 18048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18049(int& val) {
    func_in_18049(val);
    const int x = 18049;
    std::cout << 18049;
    for (int i = 0; i < 18049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18049(int& val) {
    const int x = 18049;
    std::cout << 18049;
    for (int i = 0; i < 18049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18050(int& val) {
    func_in_18050(val);
    const int x = 18050;
    std::cout << 18050;
    for (int i = 0; i < 18050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18050(int& val) {
    const int x = 18050;
    std::cout << 18050;
    for (int i = 0; i < 18050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18051(int& val) {
    func_in_18051(val);
    const int x = 18051;
    std::cout << 18051;
    for (int i = 0; i < 18051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18051(int& val) {
    const int x = 18051;
    std::cout << 18051;
    for (int i = 0; i < 18051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18052(int& val) {
    func_in_18052(val);
    const int x = 18052;
    std::cout << 18052;
    for (int i = 0; i < 18052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18052(int& val) {
    const int x = 18052;
    std::cout << 18052;
    for (int i = 0; i < 18052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18053(int& val) {
    func_in_18053(val);
    const int x = 18053;
    std::cout << 18053;
    for (int i = 0; i < 18053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18053(int& val) {
    const int x = 18053;
    std::cout << 18053;
    for (int i = 0; i < 18053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18054(int& val) {
    func_in_18054(val);
    const int x = 18054;
    std::cout << 18054;
    for (int i = 0; i < 18054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18054(int& val) {
    const int x = 18054;
    std::cout << 18054;
    for (int i = 0; i < 18054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18055(int& val) {
    func_in_18055(val);
    const int x = 18055;
    std::cout << 18055;
    for (int i = 0; i < 18055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18055(int& val) {
    const int x = 18055;
    std::cout << 18055;
    for (int i = 0; i < 18055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18056(int& val) {
    func_in_18056(val);
    const int x = 18056;
    std::cout << 18056;
    for (int i = 0; i < 18056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18056(int& val) {
    const int x = 18056;
    std::cout << 18056;
    for (int i = 0; i < 18056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18057(int& val) {
    func_in_18057(val);
    const int x = 18057;
    std::cout << 18057;
    for (int i = 0; i < 18057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18057(int& val) {
    const int x = 18057;
    std::cout << 18057;
    for (int i = 0; i < 18057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18058(int& val) {
    func_in_18058(val);
    const int x = 18058;
    std::cout << 18058;
    for (int i = 0; i < 18058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18058(int& val) {
    const int x = 18058;
    std::cout << 18058;
    for (int i = 0; i < 18058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18059(int& val) {
    func_in_18059(val);
    const int x = 18059;
    std::cout << 18059;
    for (int i = 0; i < 18059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18059(int& val) {
    const int x = 18059;
    std::cout << 18059;
    for (int i = 0; i < 18059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18060(int& val) {
    func_in_18060(val);
    const int x = 18060;
    std::cout << 18060;
    for (int i = 0; i < 18060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18060(int& val) {
    const int x = 18060;
    std::cout << 18060;
    for (int i = 0; i < 18060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18061(int& val) {
    func_in_18061(val);
    const int x = 18061;
    std::cout << 18061;
    for (int i = 0; i < 18061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18061(int& val) {
    const int x = 18061;
    std::cout << 18061;
    for (int i = 0; i < 18061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18062(int& val) {
    func_in_18062(val);
    const int x = 18062;
    std::cout << 18062;
    for (int i = 0; i < 18062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18062(int& val) {
    const int x = 18062;
    std::cout << 18062;
    for (int i = 0; i < 18062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18063(int& val) {
    func_in_18063(val);
    const int x = 18063;
    std::cout << 18063;
    for (int i = 0; i < 18063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18063(int& val) {
    const int x = 18063;
    std::cout << 18063;
    for (int i = 0; i < 18063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18064(int& val) {
    func_in_18064(val);
    const int x = 18064;
    std::cout << 18064;
    for (int i = 0; i < 18064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18064(int& val) {
    const int x = 18064;
    std::cout << 18064;
    for (int i = 0; i < 18064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18065(int& val) {
    func_in_18065(val);
    const int x = 18065;
    std::cout << 18065;
    for (int i = 0; i < 18065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18065(int& val) {
    const int x = 18065;
    std::cout << 18065;
    for (int i = 0; i < 18065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18066(int& val) {
    func_in_18066(val);
    const int x = 18066;
    std::cout << 18066;
    for (int i = 0; i < 18066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18066(int& val) {
    const int x = 18066;
    std::cout << 18066;
    for (int i = 0; i < 18066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18067(int& val) {
    func_in_18067(val);
    const int x = 18067;
    std::cout << 18067;
    for (int i = 0; i < 18067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18067(int& val) {
    const int x = 18067;
    std::cout << 18067;
    for (int i = 0; i < 18067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18068(int& val) {
    func_in_18068(val);
    const int x = 18068;
    std::cout << 18068;
    for (int i = 0; i < 18068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18068(int& val) {
    const int x = 18068;
    std::cout << 18068;
    for (int i = 0; i < 18068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18069(int& val) {
    func_in_18069(val);
    const int x = 18069;
    std::cout << 18069;
    for (int i = 0; i < 18069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18069(int& val) {
    const int x = 18069;
    std::cout << 18069;
    for (int i = 0; i < 18069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18070(int& val) {
    func_in_18070(val);
    const int x = 18070;
    std::cout << 18070;
    for (int i = 0; i < 18070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18070(int& val) {
    const int x = 18070;
    std::cout << 18070;
    for (int i = 0; i < 18070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18071(int& val) {
    func_in_18071(val);
    const int x = 18071;
    std::cout << 18071;
    for (int i = 0; i < 18071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18071(int& val) {
    const int x = 18071;
    std::cout << 18071;
    for (int i = 0; i < 18071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18072(int& val) {
    func_in_18072(val);
    const int x = 18072;
    std::cout << 18072;
    for (int i = 0; i < 18072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18072(int& val) {
    const int x = 18072;
    std::cout << 18072;
    for (int i = 0; i < 18072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18073(int& val) {
    func_in_18073(val);
    const int x = 18073;
    std::cout << 18073;
    for (int i = 0; i < 18073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18073(int& val) {
    const int x = 18073;
    std::cout << 18073;
    for (int i = 0; i < 18073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18074(int& val) {
    func_in_18074(val);
    const int x = 18074;
    std::cout << 18074;
    for (int i = 0; i < 18074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18074(int& val) {
    const int x = 18074;
    std::cout << 18074;
    for (int i = 0; i < 18074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18075(int& val) {
    func_in_18075(val);
    const int x = 18075;
    std::cout << 18075;
    for (int i = 0; i < 18075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18075(int& val) {
    const int x = 18075;
    std::cout << 18075;
    for (int i = 0; i < 18075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18076(int& val) {
    func_in_18076(val);
    const int x = 18076;
    std::cout << 18076;
    for (int i = 0; i < 18076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18076(int& val) {
    const int x = 18076;
    std::cout << 18076;
    for (int i = 0; i < 18076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18077(int& val) {
    func_in_18077(val);
    const int x = 18077;
    std::cout << 18077;
    for (int i = 0; i < 18077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18077(int& val) {
    const int x = 18077;
    std::cout << 18077;
    for (int i = 0; i < 18077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18078(int& val) {
    func_in_18078(val);
    const int x = 18078;
    std::cout << 18078;
    for (int i = 0; i < 18078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18078(int& val) {
    const int x = 18078;
    std::cout << 18078;
    for (int i = 0; i < 18078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18079(int& val) {
    func_in_18079(val);
    const int x = 18079;
    std::cout << 18079;
    for (int i = 0; i < 18079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18079(int& val) {
    const int x = 18079;
    std::cout << 18079;
    for (int i = 0; i < 18079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18080(int& val) {
    func_in_18080(val);
    const int x = 18080;
    std::cout << 18080;
    for (int i = 0; i < 18080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18080(int& val) {
    const int x = 18080;
    std::cout << 18080;
    for (int i = 0; i < 18080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18081(int& val) {
    func_in_18081(val);
    const int x = 18081;
    std::cout << 18081;
    for (int i = 0; i < 18081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18081(int& val) {
    const int x = 18081;
    std::cout << 18081;
    for (int i = 0; i < 18081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18082(int& val) {
    func_in_18082(val);
    const int x = 18082;
    std::cout << 18082;
    for (int i = 0; i < 18082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18082(int& val) {
    const int x = 18082;
    std::cout << 18082;
    for (int i = 0; i < 18082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18083(int& val) {
    func_in_18083(val);
    const int x = 18083;
    std::cout << 18083;
    for (int i = 0; i < 18083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18083(int& val) {
    const int x = 18083;
    std::cout << 18083;
    for (int i = 0; i < 18083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18084(int& val) {
    func_in_18084(val);
    const int x = 18084;
    std::cout << 18084;
    for (int i = 0; i < 18084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18084(int& val) {
    const int x = 18084;
    std::cout << 18084;
    for (int i = 0; i < 18084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18085(int& val) {
    func_in_18085(val);
    const int x = 18085;
    std::cout << 18085;
    for (int i = 0; i < 18085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18085(int& val) {
    const int x = 18085;
    std::cout << 18085;
    for (int i = 0; i < 18085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18086(int& val) {
    func_in_18086(val);
    const int x = 18086;
    std::cout << 18086;
    for (int i = 0; i < 18086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18086(int& val) {
    const int x = 18086;
    std::cout << 18086;
    for (int i = 0; i < 18086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18087(int& val) {
    func_in_18087(val);
    const int x = 18087;
    std::cout << 18087;
    for (int i = 0; i < 18087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18087(int& val) {
    const int x = 18087;
    std::cout << 18087;
    for (int i = 0; i < 18087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18088(int& val) {
    func_in_18088(val);
    const int x = 18088;
    std::cout << 18088;
    for (int i = 0; i < 18088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18088(int& val) {
    const int x = 18088;
    std::cout << 18088;
    for (int i = 0; i < 18088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18089(int& val) {
    func_in_18089(val);
    const int x = 18089;
    std::cout << 18089;
    for (int i = 0; i < 18089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18089(int& val) {
    const int x = 18089;
    std::cout << 18089;
    for (int i = 0; i < 18089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18090(int& val) {
    func_in_18090(val);
    const int x = 18090;
    std::cout << 18090;
    for (int i = 0; i < 18090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18090(int& val) {
    const int x = 18090;
    std::cout << 18090;
    for (int i = 0; i < 18090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18091(int& val) {
    func_in_18091(val);
    const int x = 18091;
    std::cout << 18091;
    for (int i = 0; i < 18091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18091(int& val) {
    const int x = 18091;
    std::cout << 18091;
    for (int i = 0; i < 18091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18092(int& val) {
    func_in_18092(val);
    const int x = 18092;
    std::cout << 18092;
    for (int i = 0; i < 18092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18092(int& val) {
    const int x = 18092;
    std::cout << 18092;
    for (int i = 0; i < 18092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18093(int& val) {
    func_in_18093(val);
    const int x = 18093;
    std::cout << 18093;
    for (int i = 0; i < 18093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18093(int& val) {
    const int x = 18093;
    std::cout << 18093;
    for (int i = 0; i < 18093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18094(int& val) {
    func_in_18094(val);
    const int x = 18094;
    std::cout << 18094;
    for (int i = 0; i < 18094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18094(int& val) {
    const int x = 18094;
    std::cout << 18094;
    for (int i = 0; i < 18094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18095(int& val) {
    func_in_18095(val);
    const int x = 18095;
    std::cout << 18095;
    for (int i = 0; i < 18095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18095(int& val) {
    const int x = 18095;
    std::cout << 18095;
    for (int i = 0; i < 18095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18096(int& val) {
    func_in_18096(val);
    const int x = 18096;
    std::cout << 18096;
    for (int i = 0; i < 18096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18096(int& val) {
    const int x = 18096;
    std::cout << 18096;
    for (int i = 0; i < 18096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18097(int& val) {
    func_in_18097(val);
    const int x = 18097;
    std::cout << 18097;
    for (int i = 0; i < 18097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18097(int& val) {
    const int x = 18097;
    std::cout << 18097;
    for (int i = 0; i < 18097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18098(int& val) {
    func_in_18098(val);
    const int x = 18098;
    std::cout << 18098;
    for (int i = 0; i < 18098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18098(int& val) {
    const int x = 18098;
    std::cout << 18098;
    for (int i = 0; i < 18098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18099(int& val) {
    func_in_18099(val);
    const int x = 18099;
    std::cout << 18099;
    for (int i = 0; i < 18099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18099(int& val) {
    const int x = 18099;
    std::cout << 18099;
    for (int i = 0; i < 18099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18100(int& val) {
    func_in_18100(val);
    const int x = 18100;
    std::cout << 18100;
    for (int i = 0; i < 18100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18100(int& val) {
    const int x = 18100;
    std::cout << 18100;
    for (int i = 0; i < 18100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18101(int& val) {
    func_in_18101(val);
    const int x = 18101;
    std::cout << 18101;
    for (int i = 0; i < 18101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18101(int& val) {
    const int x = 18101;
    std::cout << 18101;
    for (int i = 0; i < 18101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18102(int& val) {
    func_in_18102(val);
    const int x = 18102;
    std::cout << 18102;
    for (int i = 0; i < 18102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18102(int& val) {
    const int x = 18102;
    std::cout << 18102;
    for (int i = 0; i < 18102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18103(int& val) {
    func_in_18103(val);
    const int x = 18103;
    std::cout << 18103;
    for (int i = 0; i < 18103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18103(int& val) {
    const int x = 18103;
    std::cout << 18103;
    for (int i = 0; i < 18103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18104(int& val) {
    func_in_18104(val);
    const int x = 18104;
    std::cout << 18104;
    for (int i = 0; i < 18104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18104(int& val) {
    const int x = 18104;
    std::cout << 18104;
    for (int i = 0; i < 18104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18105(int& val) {
    func_in_18105(val);
    const int x = 18105;
    std::cout << 18105;
    for (int i = 0; i < 18105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18105(int& val) {
    const int x = 18105;
    std::cout << 18105;
    for (int i = 0; i < 18105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18106(int& val) {
    func_in_18106(val);
    const int x = 18106;
    std::cout << 18106;
    for (int i = 0; i < 18106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18106(int& val) {
    const int x = 18106;
    std::cout << 18106;
    for (int i = 0; i < 18106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18107(int& val) {
    func_in_18107(val);
    const int x = 18107;
    std::cout << 18107;
    for (int i = 0; i < 18107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18107(int& val) {
    const int x = 18107;
    std::cout << 18107;
    for (int i = 0; i < 18107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18108(int& val) {
    func_in_18108(val);
    const int x = 18108;
    std::cout << 18108;
    for (int i = 0; i < 18108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18108(int& val) {
    const int x = 18108;
    std::cout << 18108;
    for (int i = 0; i < 18108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18109(int& val) {
    func_in_18109(val);
    const int x = 18109;
    std::cout << 18109;
    for (int i = 0; i < 18109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18109(int& val) {
    const int x = 18109;
    std::cout << 18109;
    for (int i = 0; i < 18109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18110(int& val) {
    func_in_18110(val);
    const int x = 18110;
    std::cout << 18110;
    for (int i = 0; i < 18110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18110(int& val) {
    const int x = 18110;
    std::cout << 18110;
    for (int i = 0; i < 18110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18111(int& val) {
    func_in_18111(val);
    const int x = 18111;
    std::cout << 18111;
    for (int i = 0; i < 18111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18111(int& val) {
    const int x = 18111;
    std::cout << 18111;
    for (int i = 0; i < 18111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18112(int& val) {
    func_in_18112(val);
    const int x = 18112;
    std::cout << 18112;
    for (int i = 0; i < 18112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18112(int& val) {
    const int x = 18112;
    std::cout << 18112;
    for (int i = 0; i < 18112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18113(int& val) {
    func_in_18113(val);
    const int x = 18113;
    std::cout << 18113;
    for (int i = 0; i < 18113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18113(int& val) {
    const int x = 18113;
    std::cout << 18113;
    for (int i = 0; i < 18113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18114(int& val) {
    func_in_18114(val);
    const int x = 18114;
    std::cout << 18114;
    for (int i = 0; i < 18114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18114(int& val) {
    const int x = 18114;
    std::cout << 18114;
    for (int i = 0; i < 18114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18115(int& val) {
    func_in_18115(val);
    const int x = 18115;
    std::cout << 18115;
    for (int i = 0; i < 18115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18115(int& val) {
    const int x = 18115;
    std::cout << 18115;
    for (int i = 0; i < 18115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18116(int& val) {
    func_in_18116(val);
    const int x = 18116;
    std::cout << 18116;
    for (int i = 0; i < 18116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18116(int& val) {
    const int x = 18116;
    std::cout << 18116;
    for (int i = 0; i < 18116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18117(int& val) {
    func_in_18117(val);
    const int x = 18117;
    std::cout << 18117;
    for (int i = 0; i < 18117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18117(int& val) {
    const int x = 18117;
    std::cout << 18117;
    for (int i = 0; i < 18117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18118(int& val) {
    func_in_18118(val);
    const int x = 18118;
    std::cout << 18118;
    for (int i = 0; i < 18118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18118(int& val) {
    const int x = 18118;
    std::cout << 18118;
    for (int i = 0; i < 18118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18119(int& val) {
    func_in_18119(val);
    const int x = 18119;
    std::cout << 18119;
    for (int i = 0; i < 18119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18119(int& val) {
    const int x = 18119;
    std::cout << 18119;
    for (int i = 0; i < 18119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18120(int& val) {
    func_in_18120(val);
    const int x = 18120;
    std::cout << 18120;
    for (int i = 0; i < 18120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18120(int& val) {
    const int x = 18120;
    std::cout << 18120;
    for (int i = 0; i < 18120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18121(int& val) {
    func_in_18121(val);
    const int x = 18121;
    std::cout << 18121;
    for (int i = 0; i < 18121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18121(int& val) {
    const int x = 18121;
    std::cout << 18121;
    for (int i = 0; i < 18121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18122(int& val) {
    func_in_18122(val);
    const int x = 18122;
    std::cout << 18122;
    for (int i = 0; i < 18122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18122(int& val) {
    const int x = 18122;
    std::cout << 18122;
    for (int i = 0; i < 18122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18123(int& val) {
    func_in_18123(val);
    const int x = 18123;
    std::cout << 18123;
    for (int i = 0; i < 18123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18123(int& val) {
    const int x = 18123;
    std::cout << 18123;
    for (int i = 0; i < 18123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18124(int& val) {
    func_in_18124(val);
    const int x = 18124;
    std::cout << 18124;
    for (int i = 0; i < 18124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18124(int& val) {
    const int x = 18124;
    std::cout << 18124;
    for (int i = 0; i < 18124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18125(int& val) {
    func_in_18125(val);
    const int x = 18125;
    std::cout << 18125;
    for (int i = 0; i < 18125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18125(int& val) {
    const int x = 18125;
    std::cout << 18125;
    for (int i = 0; i < 18125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18126(int& val) {
    func_in_18126(val);
    const int x = 18126;
    std::cout << 18126;
    for (int i = 0; i < 18126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18126(int& val) {
    const int x = 18126;
    std::cout << 18126;
    for (int i = 0; i < 18126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18127(int& val) {
    func_in_18127(val);
    const int x = 18127;
    std::cout << 18127;
    for (int i = 0; i < 18127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18127(int& val) {
    const int x = 18127;
    std::cout << 18127;
    for (int i = 0; i < 18127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18128(int& val) {
    func_in_18128(val);
    const int x = 18128;
    std::cout << 18128;
    for (int i = 0; i < 18128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18128(int& val) {
    const int x = 18128;
    std::cout << 18128;
    for (int i = 0; i < 18128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18129(int& val) {
    func_in_18129(val);
    const int x = 18129;
    std::cout << 18129;
    for (int i = 0; i < 18129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18129(int& val) {
    const int x = 18129;
    std::cout << 18129;
    for (int i = 0; i < 18129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18130(int& val) {
    func_in_18130(val);
    const int x = 18130;
    std::cout << 18130;
    for (int i = 0; i < 18130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18130(int& val) {
    const int x = 18130;
    std::cout << 18130;
    for (int i = 0; i < 18130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18131(int& val) {
    func_in_18131(val);
    const int x = 18131;
    std::cout << 18131;
    for (int i = 0; i < 18131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18131(int& val) {
    const int x = 18131;
    std::cout << 18131;
    for (int i = 0; i < 18131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18132(int& val) {
    func_in_18132(val);
    const int x = 18132;
    std::cout << 18132;
    for (int i = 0; i < 18132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18132(int& val) {
    const int x = 18132;
    std::cout << 18132;
    for (int i = 0; i < 18132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18133(int& val) {
    func_in_18133(val);
    const int x = 18133;
    std::cout << 18133;
    for (int i = 0; i < 18133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18133(int& val) {
    const int x = 18133;
    std::cout << 18133;
    for (int i = 0; i < 18133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18134(int& val) {
    func_in_18134(val);
    const int x = 18134;
    std::cout << 18134;
    for (int i = 0; i < 18134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18134(int& val) {
    const int x = 18134;
    std::cout << 18134;
    for (int i = 0; i < 18134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18135(int& val) {
    func_in_18135(val);
    const int x = 18135;
    std::cout << 18135;
    for (int i = 0; i < 18135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18135(int& val) {
    const int x = 18135;
    std::cout << 18135;
    for (int i = 0; i < 18135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18136(int& val) {
    func_in_18136(val);
    const int x = 18136;
    std::cout << 18136;
    for (int i = 0; i < 18136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18136(int& val) {
    const int x = 18136;
    std::cout << 18136;
    for (int i = 0; i < 18136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18137(int& val) {
    func_in_18137(val);
    const int x = 18137;
    std::cout << 18137;
    for (int i = 0; i < 18137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18137(int& val) {
    const int x = 18137;
    std::cout << 18137;
    for (int i = 0; i < 18137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18138(int& val) {
    func_in_18138(val);
    const int x = 18138;
    std::cout << 18138;
    for (int i = 0; i < 18138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18138(int& val) {
    const int x = 18138;
    std::cout << 18138;
    for (int i = 0; i < 18138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18139(int& val) {
    func_in_18139(val);
    const int x = 18139;
    std::cout << 18139;
    for (int i = 0; i < 18139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18139(int& val) {
    const int x = 18139;
    std::cout << 18139;
    for (int i = 0; i < 18139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18140(int& val) {
    func_in_18140(val);
    const int x = 18140;
    std::cout << 18140;
    for (int i = 0; i < 18140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18140(int& val) {
    const int x = 18140;
    std::cout << 18140;
    for (int i = 0; i < 18140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18141(int& val) {
    func_in_18141(val);
    const int x = 18141;
    std::cout << 18141;
    for (int i = 0; i < 18141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18141(int& val) {
    const int x = 18141;
    std::cout << 18141;
    for (int i = 0; i < 18141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18142(int& val) {
    func_in_18142(val);
    const int x = 18142;
    std::cout << 18142;
    for (int i = 0; i < 18142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18142(int& val) {
    const int x = 18142;
    std::cout << 18142;
    for (int i = 0; i < 18142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18143(int& val) {
    func_in_18143(val);
    const int x = 18143;
    std::cout << 18143;
    for (int i = 0; i < 18143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18143(int& val) {
    const int x = 18143;
    std::cout << 18143;
    for (int i = 0; i < 18143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18144(int& val) {
    func_in_18144(val);
    const int x = 18144;
    std::cout << 18144;
    for (int i = 0; i < 18144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18144(int& val) {
    const int x = 18144;
    std::cout << 18144;
    for (int i = 0; i < 18144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18145(int& val) {
    func_in_18145(val);
    const int x = 18145;
    std::cout << 18145;
    for (int i = 0; i < 18145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18145(int& val) {
    const int x = 18145;
    std::cout << 18145;
    for (int i = 0; i < 18145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18146(int& val) {
    func_in_18146(val);
    const int x = 18146;
    std::cout << 18146;
    for (int i = 0; i < 18146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18146(int& val) {
    const int x = 18146;
    std::cout << 18146;
    for (int i = 0; i < 18146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18147(int& val) {
    func_in_18147(val);
    const int x = 18147;
    std::cout << 18147;
    for (int i = 0; i < 18147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18147(int& val) {
    const int x = 18147;
    std::cout << 18147;
    for (int i = 0; i < 18147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18148(int& val) {
    func_in_18148(val);
    const int x = 18148;
    std::cout << 18148;
    for (int i = 0; i < 18148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18148(int& val) {
    const int x = 18148;
    std::cout << 18148;
    for (int i = 0; i < 18148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18149(int& val) {
    func_in_18149(val);
    const int x = 18149;
    std::cout << 18149;
    for (int i = 0; i < 18149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18149(int& val) {
    const int x = 18149;
    std::cout << 18149;
    for (int i = 0; i < 18149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18150(int& val) {
    func_in_18150(val);
    const int x = 18150;
    std::cout << 18150;
    for (int i = 0; i < 18150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18150(int& val) {
    const int x = 18150;
    std::cout << 18150;
    for (int i = 0; i < 18150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18151(int& val) {
    func_in_18151(val);
    const int x = 18151;
    std::cout << 18151;
    for (int i = 0; i < 18151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18151(int& val) {
    const int x = 18151;
    std::cout << 18151;
    for (int i = 0; i < 18151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18152(int& val) {
    func_in_18152(val);
    const int x = 18152;
    std::cout << 18152;
    for (int i = 0; i < 18152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18152(int& val) {
    const int x = 18152;
    std::cout << 18152;
    for (int i = 0; i < 18152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18153(int& val) {
    func_in_18153(val);
    const int x = 18153;
    std::cout << 18153;
    for (int i = 0; i < 18153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18153(int& val) {
    const int x = 18153;
    std::cout << 18153;
    for (int i = 0; i < 18153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18154(int& val) {
    func_in_18154(val);
    const int x = 18154;
    std::cout << 18154;
    for (int i = 0; i < 18154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18154(int& val) {
    const int x = 18154;
    std::cout << 18154;
    for (int i = 0; i < 18154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18155(int& val) {
    func_in_18155(val);
    const int x = 18155;
    std::cout << 18155;
    for (int i = 0; i < 18155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18155(int& val) {
    const int x = 18155;
    std::cout << 18155;
    for (int i = 0; i < 18155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18156(int& val) {
    func_in_18156(val);
    const int x = 18156;
    std::cout << 18156;
    for (int i = 0; i < 18156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18156(int& val) {
    const int x = 18156;
    std::cout << 18156;
    for (int i = 0; i < 18156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18157(int& val) {
    func_in_18157(val);
    const int x = 18157;
    std::cout << 18157;
    for (int i = 0; i < 18157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18157(int& val) {
    const int x = 18157;
    std::cout << 18157;
    for (int i = 0; i < 18157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18158(int& val) {
    func_in_18158(val);
    const int x = 18158;
    std::cout << 18158;
    for (int i = 0; i < 18158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18158(int& val) {
    const int x = 18158;
    std::cout << 18158;
    for (int i = 0; i < 18158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18159(int& val) {
    func_in_18159(val);
    const int x = 18159;
    std::cout << 18159;
    for (int i = 0; i < 18159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18159(int& val) {
    const int x = 18159;
    std::cout << 18159;
    for (int i = 0; i < 18159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18160(int& val) {
    func_in_18160(val);
    const int x = 18160;
    std::cout << 18160;
    for (int i = 0; i < 18160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18160(int& val) {
    const int x = 18160;
    std::cout << 18160;
    for (int i = 0; i < 18160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18161(int& val) {
    func_in_18161(val);
    const int x = 18161;
    std::cout << 18161;
    for (int i = 0; i < 18161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18161(int& val) {
    const int x = 18161;
    std::cout << 18161;
    for (int i = 0; i < 18161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18162(int& val) {
    func_in_18162(val);
    const int x = 18162;
    std::cout << 18162;
    for (int i = 0; i < 18162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18162(int& val) {
    const int x = 18162;
    std::cout << 18162;
    for (int i = 0; i < 18162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18163(int& val) {
    func_in_18163(val);
    const int x = 18163;
    std::cout << 18163;
    for (int i = 0; i < 18163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18163(int& val) {
    const int x = 18163;
    std::cout << 18163;
    for (int i = 0; i < 18163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18164(int& val) {
    func_in_18164(val);
    const int x = 18164;
    std::cout << 18164;
    for (int i = 0; i < 18164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18164(int& val) {
    const int x = 18164;
    std::cout << 18164;
    for (int i = 0; i < 18164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18165(int& val) {
    func_in_18165(val);
    const int x = 18165;
    std::cout << 18165;
    for (int i = 0; i < 18165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18165(int& val) {
    const int x = 18165;
    std::cout << 18165;
    for (int i = 0; i < 18165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18166(int& val) {
    func_in_18166(val);
    const int x = 18166;
    std::cout << 18166;
    for (int i = 0; i < 18166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18166(int& val) {
    const int x = 18166;
    std::cout << 18166;
    for (int i = 0; i < 18166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18167(int& val) {
    func_in_18167(val);
    const int x = 18167;
    std::cout << 18167;
    for (int i = 0; i < 18167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18167(int& val) {
    const int x = 18167;
    std::cout << 18167;
    for (int i = 0; i < 18167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18168(int& val) {
    func_in_18168(val);
    const int x = 18168;
    std::cout << 18168;
    for (int i = 0; i < 18168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18168(int& val) {
    const int x = 18168;
    std::cout << 18168;
    for (int i = 0; i < 18168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18169(int& val) {
    func_in_18169(val);
    const int x = 18169;
    std::cout << 18169;
    for (int i = 0; i < 18169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18169(int& val) {
    const int x = 18169;
    std::cout << 18169;
    for (int i = 0; i < 18169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18170(int& val) {
    func_in_18170(val);
    const int x = 18170;
    std::cout << 18170;
    for (int i = 0; i < 18170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18170(int& val) {
    const int x = 18170;
    std::cout << 18170;
    for (int i = 0; i < 18170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18171(int& val) {
    func_in_18171(val);
    const int x = 18171;
    std::cout << 18171;
    for (int i = 0; i < 18171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18171(int& val) {
    const int x = 18171;
    std::cout << 18171;
    for (int i = 0; i < 18171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18172(int& val) {
    func_in_18172(val);
    const int x = 18172;
    std::cout << 18172;
    for (int i = 0; i < 18172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18172(int& val) {
    const int x = 18172;
    std::cout << 18172;
    for (int i = 0; i < 18172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18173(int& val) {
    func_in_18173(val);
    const int x = 18173;
    std::cout << 18173;
    for (int i = 0; i < 18173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18173(int& val) {
    const int x = 18173;
    std::cout << 18173;
    for (int i = 0; i < 18173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18174(int& val) {
    func_in_18174(val);
    const int x = 18174;
    std::cout << 18174;
    for (int i = 0; i < 18174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18174(int& val) {
    const int x = 18174;
    std::cout << 18174;
    for (int i = 0; i < 18174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18175(int& val) {
    func_in_18175(val);
    const int x = 18175;
    std::cout << 18175;
    for (int i = 0; i < 18175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18175(int& val) {
    const int x = 18175;
    std::cout << 18175;
    for (int i = 0; i < 18175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18176(int& val) {
    func_in_18176(val);
    const int x = 18176;
    std::cout << 18176;
    for (int i = 0; i < 18176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18176(int& val) {
    const int x = 18176;
    std::cout << 18176;
    for (int i = 0; i < 18176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18177(int& val) {
    func_in_18177(val);
    const int x = 18177;
    std::cout << 18177;
    for (int i = 0; i < 18177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18177(int& val) {
    const int x = 18177;
    std::cout << 18177;
    for (int i = 0; i < 18177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18178(int& val) {
    func_in_18178(val);
    const int x = 18178;
    std::cout << 18178;
    for (int i = 0; i < 18178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18178(int& val) {
    const int x = 18178;
    std::cout << 18178;
    for (int i = 0; i < 18178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18179(int& val) {
    func_in_18179(val);
    const int x = 18179;
    std::cout << 18179;
    for (int i = 0; i < 18179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18179(int& val) {
    const int x = 18179;
    std::cout << 18179;
    for (int i = 0; i < 18179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18180(int& val) {
    func_in_18180(val);
    const int x = 18180;
    std::cout << 18180;
    for (int i = 0; i < 18180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18180(int& val) {
    const int x = 18180;
    std::cout << 18180;
    for (int i = 0; i < 18180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18181(int& val) {
    func_in_18181(val);
    const int x = 18181;
    std::cout << 18181;
    for (int i = 0; i < 18181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18181(int& val) {
    const int x = 18181;
    std::cout << 18181;
    for (int i = 0; i < 18181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18182(int& val) {
    func_in_18182(val);
    const int x = 18182;
    std::cout << 18182;
    for (int i = 0; i < 18182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18182(int& val) {
    const int x = 18182;
    std::cout << 18182;
    for (int i = 0; i < 18182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18183(int& val) {
    func_in_18183(val);
    const int x = 18183;
    std::cout << 18183;
    for (int i = 0; i < 18183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18183(int& val) {
    const int x = 18183;
    std::cout << 18183;
    for (int i = 0; i < 18183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18184(int& val) {
    func_in_18184(val);
    const int x = 18184;
    std::cout << 18184;
    for (int i = 0; i < 18184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18184(int& val) {
    const int x = 18184;
    std::cout << 18184;
    for (int i = 0; i < 18184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18185(int& val) {
    func_in_18185(val);
    const int x = 18185;
    std::cout << 18185;
    for (int i = 0; i < 18185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18185(int& val) {
    const int x = 18185;
    std::cout << 18185;
    for (int i = 0; i < 18185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18186(int& val) {
    func_in_18186(val);
    const int x = 18186;
    std::cout << 18186;
    for (int i = 0; i < 18186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18186(int& val) {
    const int x = 18186;
    std::cout << 18186;
    for (int i = 0; i < 18186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18187(int& val) {
    func_in_18187(val);
    const int x = 18187;
    std::cout << 18187;
    for (int i = 0; i < 18187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18187(int& val) {
    const int x = 18187;
    std::cout << 18187;
    for (int i = 0; i < 18187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18188(int& val) {
    func_in_18188(val);
    const int x = 18188;
    std::cout << 18188;
    for (int i = 0; i < 18188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18188(int& val) {
    const int x = 18188;
    std::cout << 18188;
    for (int i = 0; i < 18188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18189(int& val) {
    func_in_18189(val);
    const int x = 18189;
    std::cout << 18189;
    for (int i = 0; i < 18189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18189(int& val) {
    const int x = 18189;
    std::cout << 18189;
    for (int i = 0; i < 18189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18190(int& val) {
    func_in_18190(val);
    const int x = 18190;
    std::cout << 18190;
    for (int i = 0; i < 18190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18190(int& val) {
    const int x = 18190;
    std::cout << 18190;
    for (int i = 0; i < 18190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18191(int& val) {
    func_in_18191(val);
    const int x = 18191;
    std::cout << 18191;
    for (int i = 0; i < 18191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18191(int& val) {
    const int x = 18191;
    std::cout << 18191;
    for (int i = 0; i < 18191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18192(int& val) {
    func_in_18192(val);
    const int x = 18192;
    std::cout << 18192;
    for (int i = 0; i < 18192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18192(int& val) {
    const int x = 18192;
    std::cout << 18192;
    for (int i = 0; i < 18192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18193(int& val) {
    func_in_18193(val);
    const int x = 18193;
    std::cout << 18193;
    for (int i = 0; i < 18193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18193(int& val) {
    const int x = 18193;
    std::cout << 18193;
    for (int i = 0; i < 18193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18194(int& val) {
    func_in_18194(val);
    const int x = 18194;
    std::cout << 18194;
    for (int i = 0; i < 18194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18194(int& val) {
    const int x = 18194;
    std::cout << 18194;
    for (int i = 0; i < 18194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18195(int& val) {
    func_in_18195(val);
    const int x = 18195;
    std::cout << 18195;
    for (int i = 0; i < 18195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18195(int& val) {
    const int x = 18195;
    std::cout << 18195;
    for (int i = 0; i < 18195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18196(int& val) {
    func_in_18196(val);
    const int x = 18196;
    std::cout << 18196;
    for (int i = 0; i < 18196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18196(int& val) {
    const int x = 18196;
    std::cout << 18196;
    for (int i = 0; i < 18196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18197(int& val) {
    func_in_18197(val);
    const int x = 18197;
    std::cout << 18197;
    for (int i = 0; i < 18197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18197(int& val) {
    const int x = 18197;
    std::cout << 18197;
    for (int i = 0; i < 18197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18198(int& val) {
    func_in_18198(val);
    const int x = 18198;
    std::cout << 18198;
    for (int i = 0; i < 18198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18198(int& val) {
    const int x = 18198;
    std::cout << 18198;
    for (int i = 0; i < 18198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18199(int& val) {
    func_in_18199(val);
    const int x = 18199;
    std::cout << 18199;
    for (int i = 0; i < 18199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18199(int& val) {
    const int x = 18199;
    std::cout << 18199;
    for (int i = 0; i < 18199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18200(int& val) {
    func_in_18200(val);
    const int x = 18200;
    std::cout << 18200;
    for (int i = 0; i < 18200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18200(int& val) {
    const int x = 18200;
    std::cout << 18200;
    for (int i = 0; i < 18200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18201(int& val) {
    func_in_18201(val);
    const int x = 18201;
    std::cout << 18201;
    for (int i = 0; i < 18201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18201(int& val) {
    const int x = 18201;
    std::cout << 18201;
    for (int i = 0; i < 18201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18202(int& val) {
    func_in_18202(val);
    const int x = 18202;
    std::cout << 18202;
    for (int i = 0; i < 18202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18202(int& val) {
    const int x = 18202;
    std::cout << 18202;
    for (int i = 0; i < 18202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18203(int& val) {
    func_in_18203(val);
    const int x = 18203;
    std::cout << 18203;
    for (int i = 0; i < 18203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18203(int& val) {
    const int x = 18203;
    std::cout << 18203;
    for (int i = 0; i < 18203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18204(int& val) {
    func_in_18204(val);
    const int x = 18204;
    std::cout << 18204;
    for (int i = 0; i < 18204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18204(int& val) {
    const int x = 18204;
    std::cout << 18204;
    for (int i = 0; i < 18204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18205(int& val) {
    func_in_18205(val);
    const int x = 18205;
    std::cout << 18205;
    for (int i = 0; i < 18205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18205(int& val) {
    const int x = 18205;
    std::cout << 18205;
    for (int i = 0; i < 18205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18206(int& val) {
    func_in_18206(val);
    const int x = 18206;
    std::cout << 18206;
    for (int i = 0; i < 18206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18206(int& val) {
    const int x = 18206;
    std::cout << 18206;
    for (int i = 0; i < 18206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18207(int& val) {
    func_in_18207(val);
    const int x = 18207;
    std::cout << 18207;
    for (int i = 0; i < 18207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18207(int& val) {
    const int x = 18207;
    std::cout << 18207;
    for (int i = 0; i < 18207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18208(int& val) {
    func_in_18208(val);
    const int x = 18208;
    std::cout << 18208;
    for (int i = 0; i < 18208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18208(int& val) {
    const int x = 18208;
    std::cout << 18208;
    for (int i = 0; i < 18208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18209(int& val) {
    func_in_18209(val);
    const int x = 18209;
    std::cout << 18209;
    for (int i = 0; i < 18209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18209(int& val) {
    const int x = 18209;
    std::cout << 18209;
    for (int i = 0; i < 18209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18210(int& val) {
    func_in_18210(val);
    const int x = 18210;
    std::cout << 18210;
    for (int i = 0; i < 18210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18210(int& val) {
    const int x = 18210;
    std::cout << 18210;
    for (int i = 0; i < 18210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18211(int& val) {
    func_in_18211(val);
    const int x = 18211;
    std::cout << 18211;
    for (int i = 0; i < 18211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18211(int& val) {
    const int x = 18211;
    std::cout << 18211;
    for (int i = 0; i < 18211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18212(int& val) {
    func_in_18212(val);
    const int x = 18212;
    std::cout << 18212;
    for (int i = 0; i < 18212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18212(int& val) {
    const int x = 18212;
    std::cout << 18212;
    for (int i = 0; i < 18212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18213(int& val) {
    func_in_18213(val);
    const int x = 18213;
    std::cout << 18213;
    for (int i = 0; i < 18213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18213(int& val) {
    const int x = 18213;
    std::cout << 18213;
    for (int i = 0; i < 18213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18214(int& val) {
    func_in_18214(val);
    const int x = 18214;
    std::cout << 18214;
    for (int i = 0; i < 18214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18214(int& val) {
    const int x = 18214;
    std::cout << 18214;
    for (int i = 0; i < 18214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18215(int& val) {
    func_in_18215(val);
    const int x = 18215;
    std::cout << 18215;
    for (int i = 0; i < 18215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18215(int& val) {
    const int x = 18215;
    std::cout << 18215;
    for (int i = 0; i < 18215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18216(int& val) {
    func_in_18216(val);
    const int x = 18216;
    std::cout << 18216;
    for (int i = 0; i < 18216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18216(int& val) {
    const int x = 18216;
    std::cout << 18216;
    for (int i = 0; i < 18216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18217(int& val) {
    func_in_18217(val);
    const int x = 18217;
    std::cout << 18217;
    for (int i = 0; i < 18217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18217(int& val) {
    const int x = 18217;
    std::cout << 18217;
    for (int i = 0; i < 18217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18218(int& val) {
    func_in_18218(val);
    const int x = 18218;
    std::cout << 18218;
    for (int i = 0; i < 18218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18218(int& val) {
    const int x = 18218;
    std::cout << 18218;
    for (int i = 0; i < 18218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18219(int& val) {
    func_in_18219(val);
    const int x = 18219;
    std::cout << 18219;
    for (int i = 0; i < 18219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18219(int& val) {
    const int x = 18219;
    std::cout << 18219;
    for (int i = 0; i < 18219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18220(int& val) {
    func_in_18220(val);
    const int x = 18220;
    std::cout << 18220;
    for (int i = 0; i < 18220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18220(int& val) {
    const int x = 18220;
    std::cout << 18220;
    for (int i = 0; i < 18220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18221(int& val) {
    func_in_18221(val);
    const int x = 18221;
    std::cout << 18221;
    for (int i = 0; i < 18221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18221(int& val) {
    const int x = 18221;
    std::cout << 18221;
    for (int i = 0; i < 18221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18222(int& val) {
    func_in_18222(val);
    const int x = 18222;
    std::cout << 18222;
    for (int i = 0; i < 18222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18222(int& val) {
    const int x = 18222;
    std::cout << 18222;
    for (int i = 0; i < 18222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18223(int& val) {
    func_in_18223(val);
    const int x = 18223;
    std::cout << 18223;
    for (int i = 0; i < 18223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18223(int& val) {
    const int x = 18223;
    std::cout << 18223;
    for (int i = 0; i < 18223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18224(int& val) {
    func_in_18224(val);
    const int x = 18224;
    std::cout << 18224;
    for (int i = 0; i < 18224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18224(int& val) {
    const int x = 18224;
    std::cout << 18224;
    for (int i = 0; i < 18224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18225(int& val) {
    func_in_18225(val);
    const int x = 18225;
    std::cout << 18225;
    for (int i = 0; i < 18225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18225(int& val) {
    const int x = 18225;
    std::cout << 18225;
    for (int i = 0; i < 18225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18226(int& val) {
    func_in_18226(val);
    const int x = 18226;
    std::cout << 18226;
    for (int i = 0; i < 18226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18226(int& val) {
    const int x = 18226;
    std::cout << 18226;
    for (int i = 0; i < 18226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18227(int& val) {
    func_in_18227(val);
    const int x = 18227;
    std::cout << 18227;
    for (int i = 0; i < 18227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18227(int& val) {
    const int x = 18227;
    std::cout << 18227;
    for (int i = 0; i < 18227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18228(int& val) {
    func_in_18228(val);
    const int x = 18228;
    std::cout << 18228;
    for (int i = 0; i < 18228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18228(int& val) {
    const int x = 18228;
    std::cout << 18228;
    for (int i = 0; i < 18228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18229(int& val) {
    func_in_18229(val);
    const int x = 18229;
    std::cout << 18229;
    for (int i = 0; i < 18229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18229(int& val) {
    const int x = 18229;
    std::cout << 18229;
    for (int i = 0; i < 18229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18230(int& val) {
    func_in_18230(val);
    const int x = 18230;
    std::cout << 18230;
    for (int i = 0; i < 18230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18230(int& val) {
    const int x = 18230;
    std::cout << 18230;
    for (int i = 0; i < 18230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18231(int& val) {
    func_in_18231(val);
    const int x = 18231;
    std::cout << 18231;
    for (int i = 0; i < 18231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18231(int& val) {
    const int x = 18231;
    std::cout << 18231;
    for (int i = 0; i < 18231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18232(int& val) {
    func_in_18232(val);
    const int x = 18232;
    std::cout << 18232;
    for (int i = 0; i < 18232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18232(int& val) {
    const int x = 18232;
    std::cout << 18232;
    for (int i = 0; i < 18232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18233(int& val) {
    func_in_18233(val);
    const int x = 18233;
    std::cout << 18233;
    for (int i = 0; i < 18233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18233(int& val) {
    const int x = 18233;
    std::cout << 18233;
    for (int i = 0; i < 18233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18234(int& val) {
    func_in_18234(val);
    const int x = 18234;
    std::cout << 18234;
    for (int i = 0; i < 18234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18234(int& val) {
    const int x = 18234;
    std::cout << 18234;
    for (int i = 0; i < 18234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18235(int& val) {
    func_in_18235(val);
    const int x = 18235;
    std::cout << 18235;
    for (int i = 0; i < 18235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18235(int& val) {
    const int x = 18235;
    std::cout << 18235;
    for (int i = 0; i < 18235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18236(int& val) {
    func_in_18236(val);
    const int x = 18236;
    std::cout << 18236;
    for (int i = 0; i < 18236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18236(int& val) {
    const int x = 18236;
    std::cout << 18236;
    for (int i = 0; i < 18236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18237(int& val) {
    func_in_18237(val);
    const int x = 18237;
    std::cout << 18237;
    for (int i = 0; i < 18237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18237(int& val) {
    const int x = 18237;
    std::cout << 18237;
    for (int i = 0; i < 18237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18238(int& val) {
    func_in_18238(val);
    const int x = 18238;
    std::cout << 18238;
    for (int i = 0; i < 18238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18238(int& val) {
    const int x = 18238;
    std::cout << 18238;
    for (int i = 0; i < 18238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18239(int& val) {
    func_in_18239(val);
    const int x = 18239;
    std::cout << 18239;
    for (int i = 0; i < 18239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18239(int& val) {
    const int x = 18239;
    std::cout << 18239;
    for (int i = 0; i < 18239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18240(int& val) {
    func_in_18240(val);
    const int x = 18240;
    std::cout << 18240;
    for (int i = 0; i < 18240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18240(int& val) {
    const int x = 18240;
    std::cout << 18240;
    for (int i = 0; i < 18240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18241(int& val) {
    func_in_18241(val);
    const int x = 18241;
    std::cout << 18241;
    for (int i = 0; i < 18241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18241(int& val) {
    const int x = 18241;
    std::cout << 18241;
    for (int i = 0; i < 18241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18242(int& val) {
    func_in_18242(val);
    const int x = 18242;
    std::cout << 18242;
    for (int i = 0; i < 18242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18242(int& val) {
    const int x = 18242;
    std::cout << 18242;
    for (int i = 0; i < 18242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18243(int& val) {
    func_in_18243(val);
    const int x = 18243;
    std::cout << 18243;
    for (int i = 0; i < 18243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18243(int& val) {
    const int x = 18243;
    std::cout << 18243;
    for (int i = 0; i < 18243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18244(int& val) {
    func_in_18244(val);
    const int x = 18244;
    std::cout << 18244;
    for (int i = 0; i < 18244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18244(int& val) {
    const int x = 18244;
    std::cout << 18244;
    for (int i = 0; i < 18244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18245(int& val) {
    func_in_18245(val);
    const int x = 18245;
    std::cout << 18245;
    for (int i = 0; i < 18245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18245(int& val) {
    const int x = 18245;
    std::cout << 18245;
    for (int i = 0; i < 18245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18246(int& val) {
    func_in_18246(val);
    const int x = 18246;
    std::cout << 18246;
    for (int i = 0; i < 18246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18246(int& val) {
    const int x = 18246;
    std::cout << 18246;
    for (int i = 0; i < 18246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18247(int& val) {
    func_in_18247(val);
    const int x = 18247;
    std::cout << 18247;
    for (int i = 0; i < 18247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18247(int& val) {
    const int x = 18247;
    std::cout << 18247;
    for (int i = 0; i < 18247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18248(int& val) {
    func_in_18248(val);
    const int x = 18248;
    std::cout << 18248;
    for (int i = 0; i < 18248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18248(int& val) {
    const int x = 18248;
    std::cout << 18248;
    for (int i = 0; i < 18248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18249(int& val) {
    func_in_18249(val);
    const int x = 18249;
    std::cout << 18249;
    for (int i = 0; i < 18249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18249(int& val) {
    const int x = 18249;
    std::cout << 18249;
    for (int i = 0; i < 18249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18250(int& val) {
    func_in_18250(val);
    const int x = 18250;
    std::cout << 18250;
    for (int i = 0; i < 18250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18250(int& val) {
    const int x = 18250;
    std::cout << 18250;
    for (int i = 0; i < 18250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18251(int& val) {
    func_in_18251(val);
    const int x = 18251;
    std::cout << 18251;
    for (int i = 0; i < 18251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18251(int& val) {
    const int x = 18251;
    std::cout << 18251;
    for (int i = 0; i < 18251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18252(int& val) {
    func_in_18252(val);
    const int x = 18252;
    std::cout << 18252;
    for (int i = 0; i < 18252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18252(int& val) {
    const int x = 18252;
    std::cout << 18252;
    for (int i = 0; i < 18252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18253(int& val) {
    func_in_18253(val);
    const int x = 18253;
    std::cout << 18253;
    for (int i = 0; i < 18253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18253(int& val) {
    const int x = 18253;
    std::cout << 18253;
    for (int i = 0; i < 18253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18254(int& val) {
    func_in_18254(val);
    const int x = 18254;
    std::cout << 18254;
    for (int i = 0; i < 18254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18254(int& val) {
    const int x = 18254;
    std::cout << 18254;
    for (int i = 0; i < 18254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18255(int& val) {
    func_in_18255(val);
    const int x = 18255;
    std::cout << 18255;
    for (int i = 0; i < 18255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18255(int& val) {
    const int x = 18255;
    std::cout << 18255;
    for (int i = 0; i < 18255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18256(int& val) {
    func_in_18256(val);
    const int x = 18256;
    std::cout << 18256;
    for (int i = 0; i < 18256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18256(int& val) {
    const int x = 18256;
    std::cout << 18256;
    for (int i = 0; i < 18256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18257(int& val) {
    func_in_18257(val);
    const int x = 18257;
    std::cout << 18257;
    for (int i = 0; i < 18257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18257(int& val) {
    const int x = 18257;
    std::cout << 18257;
    for (int i = 0; i < 18257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18258(int& val) {
    func_in_18258(val);
    const int x = 18258;
    std::cout << 18258;
    for (int i = 0; i < 18258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18258(int& val) {
    const int x = 18258;
    std::cout << 18258;
    for (int i = 0; i < 18258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18259(int& val) {
    func_in_18259(val);
    const int x = 18259;
    std::cout << 18259;
    for (int i = 0; i < 18259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18259(int& val) {
    const int x = 18259;
    std::cout << 18259;
    for (int i = 0; i < 18259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18260(int& val) {
    func_in_18260(val);
    const int x = 18260;
    std::cout << 18260;
    for (int i = 0; i < 18260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18260(int& val) {
    const int x = 18260;
    std::cout << 18260;
    for (int i = 0; i < 18260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18261(int& val) {
    func_in_18261(val);
    const int x = 18261;
    std::cout << 18261;
    for (int i = 0; i < 18261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18261(int& val) {
    const int x = 18261;
    std::cout << 18261;
    for (int i = 0; i < 18261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18262(int& val) {
    func_in_18262(val);
    const int x = 18262;
    std::cout << 18262;
    for (int i = 0; i < 18262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18262(int& val) {
    const int x = 18262;
    std::cout << 18262;
    for (int i = 0; i < 18262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18263(int& val) {
    func_in_18263(val);
    const int x = 18263;
    std::cout << 18263;
    for (int i = 0; i < 18263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18263(int& val) {
    const int x = 18263;
    std::cout << 18263;
    for (int i = 0; i < 18263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18264(int& val) {
    func_in_18264(val);
    const int x = 18264;
    std::cout << 18264;
    for (int i = 0; i < 18264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18264(int& val) {
    const int x = 18264;
    std::cout << 18264;
    for (int i = 0; i < 18264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18265(int& val) {
    func_in_18265(val);
    const int x = 18265;
    std::cout << 18265;
    for (int i = 0; i < 18265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18265(int& val) {
    const int x = 18265;
    std::cout << 18265;
    for (int i = 0; i < 18265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18266(int& val) {
    func_in_18266(val);
    const int x = 18266;
    std::cout << 18266;
    for (int i = 0; i < 18266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18266(int& val) {
    const int x = 18266;
    std::cout << 18266;
    for (int i = 0; i < 18266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18267(int& val) {
    func_in_18267(val);
    const int x = 18267;
    std::cout << 18267;
    for (int i = 0; i < 18267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18267(int& val) {
    const int x = 18267;
    std::cout << 18267;
    for (int i = 0; i < 18267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18268(int& val) {
    func_in_18268(val);
    const int x = 18268;
    std::cout << 18268;
    for (int i = 0; i < 18268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18268(int& val) {
    const int x = 18268;
    std::cout << 18268;
    for (int i = 0; i < 18268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18269(int& val) {
    func_in_18269(val);
    const int x = 18269;
    std::cout << 18269;
    for (int i = 0; i < 18269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18269(int& val) {
    const int x = 18269;
    std::cout << 18269;
    for (int i = 0; i < 18269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18270(int& val) {
    func_in_18270(val);
    const int x = 18270;
    std::cout << 18270;
    for (int i = 0; i < 18270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18270(int& val) {
    const int x = 18270;
    std::cout << 18270;
    for (int i = 0; i < 18270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18271(int& val) {
    func_in_18271(val);
    const int x = 18271;
    std::cout << 18271;
    for (int i = 0; i < 18271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18271(int& val) {
    const int x = 18271;
    std::cout << 18271;
    for (int i = 0; i < 18271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18272(int& val) {
    func_in_18272(val);
    const int x = 18272;
    std::cout << 18272;
    for (int i = 0; i < 18272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18272(int& val) {
    const int x = 18272;
    std::cout << 18272;
    for (int i = 0; i < 18272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18273(int& val) {
    func_in_18273(val);
    const int x = 18273;
    std::cout << 18273;
    for (int i = 0; i < 18273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18273(int& val) {
    const int x = 18273;
    std::cout << 18273;
    for (int i = 0; i < 18273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18274(int& val) {
    func_in_18274(val);
    const int x = 18274;
    std::cout << 18274;
    for (int i = 0; i < 18274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18274(int& val) {
    const int x = 18274;
    std::cout << 18274;
    for (int i = 0; i < 18274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18275(int& val) {
    func_in_18275(val);
    const int x = 18275;
    std::cout << 18275;
    for (int i = 0; i < 18275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18275(int& val) {
    const int x = 18275;
    std::cout << 18275;
    for (int i = 0; i < 18275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18276(int& val) {
    func_in_18276(val);
    const int x = 18276;
    std::cout << 18276;
    for (int i = 0; i < 18276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18276(int& val) {
    const int x = 18276;
    std::cout << 18276;
    for (int i = 0; i < 18276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18277(int& val) {
    func_in_18277(val);
    const int x = 18277;
    std::cout << 18277;
    for (int i = 0; i < 18277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18277(int& val) {
    const int x = 18277;
    std::cout << 18277;
    for (int i = 0; i < 18277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18278(int& val) {
    func_in_18278(val);
    const int x = 18278;
    std::cout << 18278;
    for (int i = 0; i < 18278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18278(int& val) {
    const int x = 18278;
    std::cout << 18278;
    for (int i = 0; i < 18278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18279(int& val) {
    func_in_18279(val);
    const int x = 18279;
    std::cout << 18279;
    for (int i = 0; i < 18279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18279(int& val) {
    const int x = 18279;
    std::cout << 18279;
    for (int i = 0; i < 18279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18280(int& val) {
    func_in_18280(val);
    const int x = 18280;
    std::cout << 18280;
    for (int i = 0; i < 18280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18280(int& val) {
    const int x = 18280;
    std::cout << 18280;
    for (int i = 0; i < 18280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18281(int& val) {
    func_in_18281(val);
    const int x = 18281;
    std::cout << 18281;
    for (int i = 0; i < 18281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18281(int& val) {
    const int x = 18281;
    std::cout << 18281;
    for (int i = 0; i < 18281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18282(int& val) {
    func_in_18282(val);
    const int x = 18282;
    std::cout << 18282;
    for (int i = 0; i < 18282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18282(int& val) {
    const int x = 18282;
    std::cout << 18282;
    for (int i = 0; i < 18282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18283(int& val) {
    func_in_18283(val);
    const int x = 18283;
    std::cout << 18283;
    for (int i = 0; i < 18283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18283(int& val) {
    const int x = 18283;
    std::cout << 18283;
    for (int i = 0; i < 18283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18284(int& val) {
    func_in_18284(val);
    const int x = 18284;
    std::cout << 18284;
    for (int i = 0; i < 18284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18284(int& val) {
    const int x = 18284;
    std::cout << 18284;
    for (int i = 0; i < 18284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18285(int& val) {
    func_in_18285(val);
    const int x = 18285;
    std::cout << 18285;
    for (int i = 0; i < 18285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18285(int& val) {
    const int x = 18285;
    std::cout << 18285;
    for (int i = 0; i < 18285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18286(int& val) {
    func_in_18286(val);
    const int x = 18286;
    std::cout << 18286;
    for (int i = 0; i < 18286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18286(int& val) {
    const int x = 18286;
    std::cout << 18286;
    for (int i = 0; i < 18286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18287(int& val) {
    func_in_18287(val);
    const int x = 18287;
    std::cout << 18287;
    for (int i = 0; i < 18287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18287(int& val) {
    const int x = 18287;
    std::cout << 18287;
    for (int i = 0; i < 18287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18288(int& val) {
    func_in_18288(val);
    const int x = 18288;
    std::cout << 18288;
    for (int i = 0; i < 18288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18288(int& val) {
    const int x = 18288;
    std::cout << 18288;
    for (int i = 0; i < 18288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18289(int& val) {
    func_in_18289(val);
    const int x = 18289;
    std::cout << 18289;
    for (int i = 0; i < 18289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18289(int& val) {
    const int x = 18289;
    std::cout << 18289;
    for (int i = 0; i < 18289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18290(int& val) {
    func_in_18290(val);
    const int x = 18290;
    std::cout << 18290;
    for (int i = 0; i < 18290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18290(int& val) {
    const int x = 18290;
    std::cout << 18290;
    for (int i = 0; i < 18290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18291(int& val) {
    func_in_18291(val);
    const int x = 18291;
    std::cout << 18291;
    for (int i = 0; i < 18291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18291(int& val) {
    const int x = 18291;
    std::cout << 18291;
    for (int i = 0; i < 18291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18292(int& val) {
    func_in_18292(val);
    const int x = 18292;
    std::cout << 18292;
    for (int i = 0; i < 18292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18292(int& val) {
    const int x = 18292;
    std::cout << 18292;
    for (int i = 0; i < 18292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18293(int& val) {
    func_in_18293(val);
    const int x = 18293;
    std::cout << 18293;
    for (int i = 0; i < 18293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18293(int& val) {
    const int x = 18293;
    std::cout << 18293;
    for (int i = 0; i < 18293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18294(int& val) {
    func_in_18294(val);
    const int x = 18294;
    std::cout << 18294;
    for (int i = 0; i < 18294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18294(int& val) {
    const int x = 18294;
    std::cout << 18294;
    for (int i = 0; i < 18294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18295(int& val) {
    func_in_18295(val);
    const int x = 18295;
    std::cout << 18295;
    for (int i = 0; i < 18295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18295(int& val) {
    const int x = 18295;
    std::cout << 18295;
    for (int i = 0; i < 18295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18296(int& val) {
    func_in_18296(val);
    const int x = 18296;
    std::cout << 18296;
    for (int i = 0; i < 18296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18296(int& val) {
    const int x = 18296;
    std::cout << 18296;
    for (int i = 0; i < 18296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18297(int& val) {
    func_in_18297(val);
    const int x = 18297;
    std::cout << 18297;
    for (int i = 0; i < 18297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18297(int& val) {
    const int x = 18297;
    std::cout << 18297;
    for (int i = 0; i < 18297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18298(int& val) {
    func_in_18298(val);
    const int x = 18298;
    std::cout << 18298;
    for (int i = 0; i < 18298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18298(int& val) {
    const int x = 18298;
    std::cout << 18298;
    for (int i = 0; i < 18298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18299(int& val) {
    func_in_18299(val);
    const int x = 18299;
    std::cout << 18299;
    for (int i = 0; i < 18299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18299(int& val) {
    const int x = 18299;
    std::cout << 18299;
    for (int i = 0; i < 18299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18300(int& val) {
    func_in_18300(val);
    const int x = 18300;
    std::cout << 18300;
    for (int i = 0; i < 18300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18300(int& val) {
    const int x = 18300;
    std::cout << 18300;
    for (int i = 0; i < 18300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18301(int& val) {
    func_in_18301(val);
    const int x = 18301;
    std::cout << 18301;
    for (int i = 0; i < 18301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18301(int& val) {
    const int x = 18301;
    std::cout << 18301;
    for (int i = 0; i < 18301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18302(int& val) {
    func_in_18302(val);
    const int x = 18302;
    std::cout << 18302;
    for (int i = 0; i < 18302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18302(int& val) {
    const int x = 18302;
    std::cout << 18302;
    for (int i = 0; i < 18302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18303(int& val) {
    func_in_18303(val);
    const int x = 18303;
    std::cout << 18303;
    for (int i = 0; i < 18303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18303(int& val) {
    const int x = 18303;
    std::cout << 18303;
    for (int i = 0; i < 18303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18304(int& val) {
    func_in_18304(val);
    const int x = 18304;
    std::cout << 18304;
    for (int i = 0; i < 18304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18304(int& val) {
    const int x = 18304;
    std::cout << 18304;
    for (int i = 0; i < 18304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18305(int& val) {
    func_in_18305(val);
    const int x = 18305;
    std::cout << 18305;
    for (int i = 0; i < 18305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18305(int& val) {
    const int x = 18305;
    std::cout << 18305;
    for (int i = 0; i < 18305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18306(int& val) {
    func_in_18306(val);
    const int x = 18306;
    std::cout << 18306;
    for (int i = 0; i < 18306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18306(int& val) {
    const int x = 18306;
    std::cout << 18306;
    for (int i = 0; i < 18306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18307(int& val) {
    func_in_18307(val);
    const int x = 18307;
    std::cout << 18307;
    for (int i = 0; i < 18307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18307(int& val) {
    const int x = 18307;
    std::cout << 18307;
    for (int i = 0; i < 18307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18308(int& val) {
    func_in_18308(val);
    const int x = 18308;
    std::cout << 18308;
    for (int i = 0; i < 18308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18308(int& val) {
    const int x = 18308;
    std::cout << 18308;
    for (int i = 0; i < 18308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18309(int& val) {
    func_in_18309(val);
    const int x = 18309;
    std::cout << 18309;
    for (int i = 0; i < 18309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18309(int& val) {
    const int x = 18309;
    std::cout << 18309;
    for (int i = 0; i < 18309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18310(int& val) {
    func_in_18310(val);
    const int x = 18310;
    std::cout << 18310;
    for (int i = 0; i < 18310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18310(int& val) {
    const int x = 18310;
    std::cout << 18310;
    for (int i = 0; i < 18310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18311(int& val) {
    func_in_18311(val);
    const int x = 18311;
    std::cout << 18311;
    for (int i = 0; i < 18311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18311(int& val) {
    const int x = 18311;
    std::cout << 18311;
    for (int i = 0; i < 18311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18312(int& val) {
    func_in_18312(val);
    const int x = 18312;
    std::cout << 18312;
    for (int i = 0; i < 18312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18312(int& val) {
    const int x = 18312;
    std::cout << 18312;
    for (int i = 0; i < 18312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18313(int& val) {
    func_in_18313(val);
    const int x = 18313;
    std::cout << 18313;
    for (int i = 0; i < 18313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18313(int& val) {
    const int x = 18313;
    std::cout << 18313;
    for (int i = 0; i < 18313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18314(int& val) {
    func_in_18314(val);
    const int x = 18314;
    std::cout << 18314;
    for (int i = 0; i < 18314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18314(int& val) {
    const int x = 18314;
    std::cout << 18314;
    for (int i = 0; i < 18314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18315(int& val) {
    func_in_18315(val);
    const int x = 18315;
    std::cout << 18315;
    for (int i = 0; i < 18315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18315(int& val) {
    const int x = 18315;
    std::cout << 18315;
    for (int i = 0; i < 18315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18316(int& val) {
    func_in_18316(val);
    const int x = 18316;
    std::cout << 18316;
    for (int i = 0; i < 18316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18316(int& val) {
    const int x = 18316;
    std::cout << 18316;
    for (int i = 0; i < 18316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18317(int& val) {
    func_in_18317(val);
    const int x = 18317;
    std::cout << 18317;
    for (int i = 0; i < 18317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18317(int& val) {
    const int x = 18317;
    std::cout << 18317;
    for (int i = 0; i < 18317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18318(int& val) {
    func_in_18318(val);
    const int x = 18318;
    std::cout << 18318;
    for (int i = 0; i < 18318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18318(int& val) {
    const int x = 18318;
    std::cout << 18318;
    for (int i = 0; i < 18318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18319(int& val) {
    func_in_18319(val);
    const int x = 18319;
    std::cout << 18319;
    for (int i = 0; i < 18319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18319(int& val) {
    const int x = 18319;
    std::cout << 18319;
    for (int i = 0; i < 18319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18320(int& val) {
    func_in_18320(val);
    const int x = 18320;
    std::cout << 18320;
    for (int i = 0; i < 18320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18320(int& val) {
    const int x = 18320;
    std::cout << 18320;
    for (int i = 0; i < 18320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18321(int& val) {
    func_in_18321(val);
    const int x = 18321;
    std::cout << 18321;
    for (int i = 0; i < 18321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18321(int& val) {
    const int x = 18321;
    std::cout << 18321;
    for (int i = 0; i < 18321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18322(int& val) {
    func_in_18322(val);
    const int x = 18322;
    std::cout << 18322;
    for (int i = 0; i < 18322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18322(int& val) {
    const int x = 18322;
    std::cout << 18322;
    for (int i = 0; i < 18322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18323(int& val) {
    func_in_18323(val);
    const int x = 18323;
    std::cout << 18323;
    for (int i = 0; i < 18323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18323(int& val) {
    const int x = 18323;
    std::cout << 18323;
    for (int i = 0; i < 18323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18324(int& val) {
    func_in_18324(val);
    const int x = 18324;
    std::cout << 18324;
    for (int i = 0; i < 18324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18324(int& val) {
    const int x = 18324;
    std::cout << 18324;
    for (int i = 0; i < 18324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18325(int& val) {
    func_in_18325(val);
    const int x = 18325;
    std::cout << 18325;
    for (int i = 0; i < 18325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18325(int& val) {
    const int x = 18325;
    std::cout << 18325;
    for (int i = 0; i < 18325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18326(int& val) {
    func_in_18326(val);
    const int x = 18326;
    std::cout << 18326;
    for (int i = 0; i < 18326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18326(int& val) {
    const int x = 18326;
    std::cout << 18326;
    for (int i = 0; i < 18326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18327(int& val) {
    func_in_18327(val);
    const int x = 18327;
    std::cout << 18327;
    for (int i = 0; i < 18327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18327(int& val) {
    const int x = 18327;
    std::cout << 18327;
    for (int i = 0; i < 18327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18328(int& val) {
    func_in_18328(val);
    const int x = 18328;
    std::cout << 18328;
    for (int i = 0; i < 18328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18328(int& val) {
    const int x = 18328;
    std::cout << 18328;
    for (int i = 0; i < 18328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18329(int& val) {
    func_in_18329(val);
    const int x = 18329;
    std::cout << 18329;
    for (int i = 0; i < 18329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18329(int& val) {
    const int x = 18329;
    std::cout << 18329;
    for (int i = 0; i < 18329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18330(int& val) {
    func_in_18330(val);
    const int x = 18330;
    std::cout << 18330;
    for (int i = 0; i < 18330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18330(int& val) {
    const int x = 18330;
    std::cout << 18330;
    for (int i = 0; i < 18330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18331(int& val) {
    func_in_18331(val);
    const int x = 18331;
    std::cout << 18331;
    for (int i = 0; i < 18331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18331(int& val) {
    const int x = 18331;
    std::cout << 18331;
    for (int i = 0; i < 18331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18332(int& val) {
    func_in_18332(val);
    const int x = 18332;
    std::cout << 18332;
    for (int i = 0; i < 18332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18332(int& val) {
    const int x = 18332;
    std::cout << 18332;
    for (int i = 0; i < 18332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18333(int& val) {
    func_in_18333(val);
    const int x = 18333;
    std::cout << 18333;
    for (int i = 0; i < 18333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18333(int& val) {
    const int x = 18333;
    std::cout << 18333;
    for (int i = 0; i < 18333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18334(int& val) {
    func_in_18334(val);
    const int x = 18334;
    std::cout << 18334;
    for (int i = 0; i < 18334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18334(int& val) {
    const int x = 18334;
    std::cout << 18334;
    for (int i = 0; i < 18334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18335(int& val) {
    func_in_18335(val);
    const int x = 18335;
    std::cout << 18335;
    for (int i = 0; i < 18335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18335(int& val) {
    const int x = 18335;
    std::cout << 18335;
    for (int i = 0; i < 18335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18336(int& val) {
    func_in_18336(val);
    const int x = 18336;
    std::cout << 18336;
    for (int i = 0; i < 18336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18336(int& val) {
    const int x = 18336;
    std::cout << 18336;
    for (int i = 0; i < 18336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18337(int& val) {
    func_in_18337(val);
    const int x = 18337;
    std::cout << 18337;
    for (int i = 0; i < 18337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18337(int& val) {
    const int x = 18337;
    std::cout << 18337;
    for (int i = 0; i < 18337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18338(int& val) {
    func_in_18338(val);
    const int x = 18338;
    std::cout << 18338;
    for (int i = 0; i < 18338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18338(int& val) {
    const int x = 18338;
    std::cout << 18338;
    for (int i = 0; i < 18338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18339(int& val) {
    func_in_18339(val);
    const int x = 18339;
    std::cout << 18339;
    for (int i = 0; i < 18339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18339(int& val) {
    const int x = 18339;
    std::cout << 18339;
    for (int i = 0; i < 18339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18340(int& val) {
    func_in_18340(val);
    const int x = 18340;
    std::cout << 18340;
    for (int i = 0; i < 18340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18340(int& val) {
    const int x = 18340;
    std::cout << 18340;
    for (int i = 0; i < 18340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18341(int& val) {
    func_in_18341(val);
    const int x = 18341;
    std::cout << 18341;
    for (int i = 0; i < 18341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18341(int& val) {
    const int x = 18341;
    std::cout << 18341;
    for (int i = 0; i < 18341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18342(int& val) {
    func_in_18342(val);
    const int x = 18342;
    std::cout << 18342;
    for (int i = 0; i < 18342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18342(int& val) {
    const int x = 18342;
    std::cout << 18342;
    for (int i = 0; i < 18342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18343(int& val) {
    func_in_18343(val);
    const int x = 18343;
    std::cout << 18343;
    for (int i = 0; i < 18343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18343(int& val) {
    const int x = 18343;
    std::cout << 18343;
    for (int i = 0; i < 18343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18344(int& val) {
    func_in_18344(val);
    const int x = 18344;
    std::cout << 18344;
    for (int i = 0; i < 18344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18344(int& val) {
    const int x = 18344;
    std::cout << 18344;
    for (int i = 0; i < 18344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18345(int& val) {
    func_in_18345(val);
    const int x = 18345;
    std::cout << 18345;
    for (int i = 0; i < 18345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18345(int& val) {
    const int x = 18345;
    std::cout << 18345;
    for (int i = 0; i < 18345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18346(int& val) {
    func_in_18346(val);
    const int x = 18346;
    std::cout << 18346;
    for (int i = 0; i < 18346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18346(int& val) {
    const int x = 18346;
    std::cout << 18346;
    for (int i = 0; i < 18346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18347(int& val) {
    func_in_18347(val);
    const int x = 18347;
    std::cout << 18347;
    for (int i = 0; i < 18347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18347(int& val) {
    const int x = 18347;
    std::cout << 18347;
    for (int i = 0; i < 18347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18348(int& val) {
    func_in_18348(val);
    const int x = 18348;
    std::cout << 18348;
    for (int i = 0; i < 18348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18348(int& val) {
    const int x = 18348;
    std::cout << 18348;
    for (int i = 0; i < 18348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18349(int& val) {
    func_in_18349(val);
    const int x = 18349;
    std::cout << 18349;
    for (int i = 0; i < 18349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18349(int& val) {
    const int x = 18349;
    std::cout << 18349;
    for (int i = 0; i < 18349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18350(int& val) {
    func_in_18350(val);
    const int x = 18350;
    std::cout << 18350;
    for (int i = 0; i < 18350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18350(int& val) {
    const int x = 18350;
    std::cout << 18350;
    for (int i = 0; i < 18350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18351(int& val) {
    func_in_18351(val);
    const int x = 18351;
    std::cout << 18351;
    for (int i = 0; i < 18351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18351(int& val) {
    const int x = 18351;
    std::cout << 18351;
    for (int i = 0; i < 18351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18352(int& val) {
    func_in_18352(val);
    const int x = 18352;
    std::cout << 18352;
    for (int i = 0; i < 18352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18352(int& val) {
    const int x = 18352;
    std::cout << 18352;
    for (int i = 0; i < 18352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18353(int& val) {
    func_in_18353(val);
    const int x = 18353;
    std::cout << 18353;
    for (int i = 0; i < 18353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18353(int& val) {
    const int x = 18353;
    std::cout << 18353;
    for (int i = 0; i < 18353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18354(int& val) {
    func_in_18354(val);
    const int x = 18354;
    std::cout << 18354;
    for (int i = 0; i < 18354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18354(int& val) {
    const int x = 18354;
    std::cout << 18354;
    for (int i = 0; i < 18354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18355(int& val) {
    func_in_18355(val);
    const int x = 18355;
    std::cout << 18355;
    for (int i = 0; i < 18355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18355(int& val) {
    const int x = 18355;
    std::cout << 18355;
    for (int i = 0; i < 18355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18356(int& val) {
    func_in_18356(val);
    const int x = 18356;
    std::cout << 18356;
    for (int i = 0; i < 18356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18356(int& val) {
    const int x = 18356;
    std::cout << 18356;
    for (int i = 0; i < 18356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18357(int& val) {
    func_in_18357(val);
    const int x = 18357;
    std::cout << 18357;
    for (int i = 0; i < 18357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18357(int& val) {
    const int x = 18357;
    std::cout << 18357;
    for (int i = 0; i < 18357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18358(int& val) {
    func_in_18358(val);
    const int x = 18358;
    std::cout << 18358;
    for (int i = 0; i < 18358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18358(int& val) {
    const int x = 18358;
    std::cout << 18358;
    for (int i = 0; i < 18358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18359(int& val) {
    func_in_18359(val);
    const int x = 18359;
    std::cout << 18359;
    for (int i = 0; i < 18359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18359(int& val) {
    const int x = 18359;
    std::cout << 18359;
    for (int i = 0; i < 18359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18360(int& val) {
    func_in_18360(val);
    const int x = 18360;
    std::cout << 18360;
    for (int i = 0; i < 18360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18360(int& val) {
    const int x = 18360;
    std::cout << 18360;
    for (int i = 0; i < 18360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18361(int& val) {
    func_in_18361(val);
    const int x = 18361;
    std::cout << 18361;
    for (int i = 0; i < 18361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18361(int& val) {
    const int x = 18361;
    std::cout << 18361;
    for (int i = 0; i < 18361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18362(int& val) {
    func_in_18362(val);
    const int x = 18362;
    std::cout << 18362;
    for (int i = 0; i < 18362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18362(int& val) {
    const int x = 18362;
    std::cout << 18362;
    for (int i = 0; i < 18362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18363(int& val) {
    func_in_18363(val);
    const int x = 18363;
    std::cout << 18363;
    for (int i = 0; i < 18363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18363(int& val) {
    const int x = 18363;
    std::cout << 18363;
    for (int i = 0; i < 18363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18364(int& val) {
    func_in_18364(val);
    const int x = 18364;
    std::cout << 18364;
    for (int i = 0; i < 18364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18364(int& val) {
    const int x = 18364;
    std::cout << 18364;
    for (int i = 0; i < 18364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18365(int& val) {
    func_in_18365(val);
    const int x = 18365;
    std::cout << 18365;
    for (int i = 0; i < 18365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18365(int& val) {
    const int x = 18365;
    std::cout << 18365;
    for (int i = 0; i < 18365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18366(int& val) {
    func_in_18366(val);
    const int x = 18366;
    std::cout << 18366;
    for (int i = 0; i < 18366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18366(int& val) {
    const int x = 18366;
    std::cout << 18366;
    for (int i = 0; i < 18366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18367(int& val) {
    func_in_18367(val);
    const int x = 18367;
    std::cout << 18367;
    for (int i = 0; i < 18367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18367(int& val) {
    const int x = 18367;
    std::cout << 18367;
    for (int i = 0; i < 18367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18368(int& val) {
    func_in_18368(val);
    const int x = 18368;
    std::cout << 18368;
    for (int i = 0; i < 18368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18368(int& val) {
    const int x = 18368;
    std::cout << 18368;
    for (int i = 0; i < 18368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18369(int& val) {
    func_in_18369(val);
    const int x = 18369;
    std::cout << 18369;
    for (int i = 0; i < 18369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18369(int& val) {
    const int x = 18369;
    std::cout << 18369;
    for (int i = 0; i < 18369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18370(int& val) {
    func_in_18370(val);
    const int x = 18370;
    std::cout << 18370;
    for (int i = 0; i < 18370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18370(int& val) {
    const int x = 18370;
    std::cout << 18370;
    for (int i = 0; i < 18370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18371(int& val) {
    func_in_18371(val);
    const int x = 18371;
    std::cout << 18371;
    for (int i = 0; i < 18371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18371(int& val) {
    const int x = 18371;
    std::cout << 18371;
    for (int i = 0; i < 18371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18372(int& val) {
    func_in_18372(val);
    const int x = 18372;
    std::cout << 18372;
    for (int i = 0; i < 18372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18372(int& val) {
    const int x = 18372;
    std::cout << 18372;
    for (int i = 0; i < 18372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18373(int& val) {
    func_in_18373(val);
    const int x = 18373;
    std::cout << 18373;
    for (int i = 0; i < 18373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18373(int& val) {
    const int x = 18373;
    std::cout << 18373;
    for (int i = 0; i < 18373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18374(int& val) {
    func_in_18374(val);
    const int x = 18374;
    std::cout << 18374;
    for (int i = 0; i < 18374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18374(int& val) {
    const int x = 18374;
    std::cout << 18374;
    for (int i = 0; i < 18374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18375(int& val) {
    func_in_18375(val);
    const int x = 18375;
    std::cout << 18375;
    for (int i = 0; i < 18375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18375(int& val) {
    const int x = 18375;
    std::cout << 18375;
    for (int i = 0; i < 18375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18376(int& val) {
    func_in_18376(val);
    const int x = 18376;
    std::cout << 18376;
    for (int i = 0; i < 18376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18376(int& val) {
    const int x = 18376;
    std::cout << 18376;
    for (int i = 0; i < 18376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18377(int& val) {
    func_in_18377(val);
    const int x = 18377;
    std::cout << 18377;
    for (int i = 0; i < 18377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18377(int& val) {
    const int x = 18377;
    std::cout << 18377;
    for (int i = 0; i < 18377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18378(int& val) {
    func_in_18378(val);
    const int x = 18378;
    std::cout << 18378;
    for (int i = 0; i < 18378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18378(int& val) {
    const int x = 18378;
    std::cout << 18378;
    for (int i = 0; i < 18378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18379(int& val) {
    func_in_18379(val);
    const int x = 18379;
    std::cout << 18379;
    for (int i = 0; i < 18379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18379(int& val) {
    const int x = 18379;
    std::cout << 18379;
    for (int i = 0; i < 18379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18380(int& val) {
    func_in_18380(val);
    const int x = 18380;
    std::cout << 18380;
    for (int i = 0; i < 18380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18380(int& val) {
    const int x = 18380;
    std::cout << 18380;
    for (int i = 0; i < 18380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18381(int& val) {
    func_in_18381(val);
    const int x = 18381;
    std::cout << 18381;
    for (int i = 0; i < 18381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18381(int& val) {
    const int x = 18381;
    std::cout << 18381;
    for (int i = 0; i < 18381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18382(int& val) {
    func_in_18382(val);
    const int x = 18382;
    std::cout << 18382;
    for (int i = 0; i < 18382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18382(int& val) {
    const int x = 18382;
    std::cout << 18382;
    for (int i = 0; i < 18382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18383(int& val) {
    func_in_18383(val);
    const int x = 18383;
    std::cout << 18383;
    for (int i = 0; i < 18383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18383(int& val) {
    const int x = 18383;
    std::cout << 18383;
    for (int i = 0; i < 18383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18384(int& val) {
    func_in_18384(val);
    const int x = 18384;
    std::cout << 18384;
    for (int i = 0; i < 18384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18384(int& val) {
    const int x = 18384;
    std::cout << 18384;
    for (int i = 0; i < 18384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18385(int& val) {
    func_in_18385(val);
    const int x = 18385;
    std::cout << 18385;
    for (int i = 0; i < 18385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18385(int& val) {
    const int x = 18385;
    std::cout << 18385;
    for (int i = 0; i < 18385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18386(int& val) {
    func_in_18386(val);
    const int x = 18386;
    std::cout << 18386;
    for (int i = 0; i < 18386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18386(int& val) {
    const int x = 18386;
    std::cout << 18386;
    for (int i = 0; i < 18386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18387(int& val) {
    func_in_18387(val);
    const int x = 18387;
    std::cout << 18387;
    for (int i = 0; i < 18387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18387(int& val) {
    const int x = 18387;
    std::cout << 18387;
    for (int i = 0; i < 18387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18388(int& val) {
    func_in_18388(val);
    const int x = 18388;
    std::cout << 18388;
    for (int i = 0; i < 18388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18388(int& val) {
    const int x = 18388;
    std::cout << 18388;
    for (int i = 0; i < 18388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18389(int& val) {
    func_in_18389(val);
    const int x = 18389;
    std::cout << 18389;
    for (int i = 0; i < 18389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18389(int& val) {
    const int x = 18389;
    std::cout << 18389;
    for (int i = 0; i < 18389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18390(int& val) {
    func_in_18390(val);
    const int x = 18390;
    std::cout << 18390;
    for (int i = 0; i < 18390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18390(int& val) {
    const int x = 18390;
    std::cout << 18390;
    for (int i = 0; i < 18390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18391(int& val) {
    func_in_18391(val);
    const int x = 18391;
    std::cout << 18391;
    for (int i = 0; i < 18391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18391(int& val) {
    const int x = 18391;
    std::cout << 18391;
    for (int i = 0; i < 18391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18392(int& val) {
    func_in_18392(val);
    const int x = 18392;
    std::cout << 18392;
    for (int i = 0; i < 18392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18392(int& val) {
    const int x = 18392;
    std::cout << 18392;
    for (int i = 0; i < 18392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18393(int& val) {
    func_in_18393(val);
    const int x = 18393;
    std::cout << 18393;
    for (int i = 0; i < 18393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18393(int& val) {
    const int x = 18393;
    std::cout << 18393;
    for (int i = 0; i < 18393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18394(int& val) {
    func_in_18394(val);
    const int x = 18394;
    std::cout << 18394;
    for (int i = 0; i < 18394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18394(int& val) {
    const int x = 18394;
    std::cout << 18394;
    for (int i = 0; i < 18394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18395(int& val) {
    func_in_18395(val);
    const int x = 18395;
    std::cout << 18395;
    for (int i = 0; i < 18395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18395(int& val) {
    const int x = 18395;
    std::cout << 18395;
    for (int i = 0; i < 18395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18396(int& val) {
    func_in_18396(val);
    const int x = 18396;
    std::cout << 18396;
    for (int i = 0; i < 18396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18396(int& val) {
    const int x = 18396;
    std::cout << 18396;
    for (int i = 0; i < 18396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18397(int& val) {
    func_in_18397(val);
    const int x = 18397;
    std::cout << 18397;
    for (int i = 0; i < 18397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18397(int& val) {
    const int x = 18397;
    std::cout << 18397;
    for (int i = 0; i < 18397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18398(int& val) {
    func_in_18398(val);
    const int x = 18398;
    std::cout << 18398;
    for (int i = 0; i < 18398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18398(int& val) {
    const int x = 18398;
    std::cout << 18398;
    for (int i = 0; i < 18398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18399(int& val) {
    func_in_18399(val);
    const int x = 18399;
    std::cout << 18399;
    for (int i = 0; i < 18399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18399(int& val) {
    const int x = 18399;
    std::cout << 18399;
    for (int i = 0; i < 18399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18400(int& val) {
    func_in_18400(val);
    const int x = 18400;
    std::cout << 18400;
    for (int i = 0; i < 18400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18400(int& val) {
    const int x = 18400;
    std::cout << 18400;
    for (int i = 0; i < 18400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18401(int& val) {
    func_in_18401(val);
    const int x = 18401;
    std::cout << 18401;
    for (int i = 0; i < 18401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18401(int& val) {
    const int x = 18401;
    std::cout << 18401;
    for (int i = 0; i < 18401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18402(int& val) {
    func_in_18402(val);
    const int x = 18402;
    std::cout << 18402;
    for (int i = 0; i < 18402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18402(int& val) {
    const int x = 18402;
    std::cout << 18402;
    for (int i = 0; i < 18402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18403(int& val) {
    func_in_18403(val);
    const int x = 18403;
    std::cout << 18403;
    for (int i = 0; i < 18403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18403(int& val) {
    const int x = 18403;
    std::cout << 18403;
    for (int i = 0; i < 18403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18404(int& val) {
    func_in_18404(val);
    const int x = 18404;
    std::cout << 18404;
    for (int i = 0; i < 18404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18404(int& val) {
    const int x = 18404;
    std::cout << 18404;
    for (int i = 0; i < 18404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18405(int& val) {
    func_in_18405(val);
    const int x = 18405;
    std::cout << 18405;
    for (int i = 0; i < 18405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18405(int& val) {
    const int x = 18405;
    std::cout << 18405;
    for (int i = 0; i < 18405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18406(int& val) {
    func_in_18406(val);
    const int x = 18406;
    std::cout << 18406;
    for (int i = 0; i < 18406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18406(int& val) {
    const int x = 18406;
    std::cout << 18406;
    for (int i = 0; i < 18406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18407(int& val) {
    func_in_18407(val);
    const int x = 18407;
    std::cout << 18407;
    for (int i = 0; i < 18407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18407(int& val) {
    const int x = 18407;
    std::cout << 18407;
    for (int i = 0; i < 18407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18408(int& val) {
    func_in_18408(val);
    const int x = 18408;
    std::cout << 18408;
    for (int i = 0; i < 18408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18408(int& val) {
    const int x = 18408;
    std::cout << 18408;
    for (int i = 0; i < 18408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18409(int& val) {
    func_in_18409(val);
    const int x = 18409;
    std::cout << 18409;
    for (int i = 0; i < 18409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18409(int& val) {
    const int x = 18409;
    std::cout << 18409;
    for (int i = 0; i < 18409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18410(int& val) {
    func_in_18410(val);
    const int x = 18410;
    std::cout << 18410;
    for (int i = 0; i < 18410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18410(int& val) {
    const int x = 18410;
    std::cout << 18410;
    for (int i = 0; i < 18410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18411(int& val) {
    func_in_18411(val);
    const int x = 18411;
    std::cout << 18411;
    for (int i = 0; i < 18411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18411(int& val) {
    const int x = 18411;
    std::cout << 18411;
    for (int i = 0; i < 18411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18412(int& val) {
    func_in_18412(val);
    const int x = 18412;
    std::cout << 18412;
    for (int i = 0; i < 18412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18412(int& val) {
    const int x = 18412;
    std::cout << 18412;
    for (int i = 0; i < 18412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18413(int& val) {
    func_in_18413(val);
    const int x = 18413;
    std::cout << 18413;
    for (int i = 0; i < 18413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18413(int& val) {
    const int x = 18413;
    std::cout << 18413;
    for (int i = 0; i < 18413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18414(int& val) {
    func_in_18414(val);
    const int x = 18414;
    std::cout << 18414;
    for (int i = 0; i < 18414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18414(int& val) {
    const int x = 18414;
    std::cout << 18414;
    for (int i = 0; i < 18414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18415(int& val) {
    func_in_18415(val);
    const int x = 18415;
    std::cout << 18415;
    for (int i = 0; i < 18415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18415(int& val) {
    const int x = 18415;
    std::cout << 18415;
    for (int i = 0; i < 18415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18416(int& val) {
    func_in_18416(val);
    const int x = 18416;
    std::cout << 18416;
    for (int i = 0; i < 18416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18416(int& val) {
    const int x = 18416;
    std::cout << 18416;
    for (int i = 0; i < 18416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18417(int& val) {
    func_in_18417(val);
    const int x = 18417;
    std::cout << 18417;
    for (int i = 0; i < 18417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18417(int& val) {
    const int x = 18417;
    std::cout << 18417;
    for (int i = 0; i < 18417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18418(int& val) {
    func_in_18418(val);
    const int x = 18418;
    std::cout << 18418;
    for (int i = 0; i < 18418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18418(int& val) {
    const int x = 18418;
    std::cout << 18418;
    for (int i = 0; i < 18418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18419(int& val) {
    func_in_18419(val);
    const int x = 18419;
    std::cout << 18419;
    for (int i = 0; i < 18419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18419(int& val) {
    const int x = 18419;
    std::cout << 18419;
    for (int i = 0; i < 18419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18420(int& val) {
    func_in_18420(val);
    const int x = 18420;
    std::cout << 18420;
    for (int i = 0; i < 18420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18420(int& val) {
    const int x = 18420;
    std::cout << 18420;
    for (int i = 0; i < 18420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18421(int& val) {
    func_in_18421(val);
    const int x = 18421;
    std::cout << 18421;
    for (int i = 0; i < 18421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18421(int& val) {
    const int x = 18421;
    std::cout << 18421;
    for (int i = 0; i < 18421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18422(int& val) {
    func_in_18422(val);
    const int x = 18422;
    std::cout << 18422;
    for (int i = 0; i < 18422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18422(int& val) {
    const int x = 18422;
    std::cout << 18422;
    for (int i = 0; i < 18422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18423(int& val) {
    func_in_18423(val);
    const int x = 18423;
    std::cout << 18423;
    for (int i = 0; i < 18423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18423(int& val) {
    const int x = 18423;
    std::cout << 18423;
    for (int i = 0; i < 18423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18424(int& val) {
    func_in_18424(val);
    const int x = 18424;
    std::cout << 18424;
    for (int i = 0; i < 18424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18424(int& val) {
    const int x = 18424;
    std::cout << 18424;
    for (int i = 0; i < 18424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18425(int& val) {
    func_in_18425(val);
    const int x = 18425;
    std::cout << 18425;
    for (int i = 0; i < 18425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18425(int& val) {
    const int x = 18425;
    std::cout << 18425;
    for (int i = 0; i < 18425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18426(int& val) {
    func_in_18426(val);
    const int x = 18426;
    std::cout << 18426;
    for (int i = 0; i < 18426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18426(int& val) {
    const int x = 18426;
    std::cout << 18426;
    for (int i = 0; i < 18426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18427(int& val) {
    func_in_18427(val);
    const int x = 18427;
    std::cout << 18427;
    for (int i = 0; i < 18427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18427(int& val) {
    const int x = 18427;
    std::cout << 18427;
    for (int i = 0; i < 18427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18428(int& val) {
    func_in_18428(val);
    const int x = 18428;
    std::cout << 18428;
    for (int i = 0; i < 18428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18428(int& val) {
    const int x = 18428;
    std::cout << 18428;
    for (int i = 0; i < 18428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18429(int& val) {
    func_in_18429(val);
    const int x = 18429;
    std::cout << 18429;
    for (int i = 0; i < 18429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18429(int& val) {
    const int x = 18429;
    std::cout << 18429;
    for (int i = 0; i < 18429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18430(int& val) {
    func_in_18430(val);
    const int x = 18430;
    std::cout << 18430;
    for (int i = 0; i < 18430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18430(int& val) {
    const int x = 18430;
    std::cout << 18430;
    for (int i = 0; i < 18430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18431(int& val) {
    func_in_18431(val);
    const int x = 18431;
    std::cout << 18431;
    for (int i = 0; i < 18431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18431(int& val) {
    const int x = 18431;
    std::cout << 18431;
    for (int i = 0; i < 18431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18432(int& val) {
    func_in_18432(val);
    const int x = 18432;
    std::cout << 18432;
    for (int i = 0; i < 18432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18432(int& val) {
    const int x = 18432;
    std::cout << 18432;
    for (int i = 0; i < 18432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18433(int& val) {
    func_in_18433(val);
    const int x = 18433;
    std::cout << 18433;
    for (int i = 0; i < 18433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18433(int& val) {
    const int x = 18433;
    std::cout << 18433;
    for (int i = 0; i < 18433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18434(int& val) {
    func_in_18434(val);
    const int x = 18434;
    std::cout << 18434;
    for (int i = 0; i < 18434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18434(int& val) {
    const int x = 18434;
    std::cout << 18434;
    for (int i = 0; i < 18434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18435(int& val) {
    func_in_18435(val);
    const int x = 18435;
    std::cout << 18435;
    for (int i = 0; i < 18435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18435(int& val) {
    const int x = 18435;
    std::cout << 18435;
    for (int i = 0; i < 18435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18436(int& val) {
    func_in_18436(val);
    const int x = 18436;
    std::cout << 18436;
    for (int i = 0; i < 18436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18436(int& val) {
    const int x = 18436;
    std::cout << 18436;
    for (int i = 0; i < 18436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18437(int& val) {
    func_in_18437(val);
    const int x = 18437;
    std::cout << 18437;
    for (int i = 0; i < 18437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18437(int& val) {
    const int x = 18437;
    std::cout << 18437;
    for (int i = 0; i < 18437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18438(int& val) {
    func_in_18438(val);
    const int x = 18438;
    std::cout << 18438;
    for (int i = 0; i < 18438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18438(int& val) {
    const int x = 18438;
    std::cout << 18438;
    for (int i = 0; i < 18438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18439(int& val) {
    func_in_18439(val);
    const int x = 18439;
    std::cout << 18439;
    for (int i = 0; i < 18439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18439(int& val) {
    const int x = 18439;
    std::cout << 18439;
    for (int i = 0; i < 18439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18440(int& val) {
    func_in_18440(val);
    const int x = 18440;
    std::cout << 18440;
    for (int i = 0; i < 18440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18440(int& val) {
    const int x = 18440;
    std::cout << 18440;
    for (int i = 0; i < 18440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18441(int& val) {
    func_in_18441(val);
    const int x = 18441;
    std::cout << 18441;
    for (int i = 0; i < 18441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18441(int& val) {
    const int x = 18441;
    std::cout << 18441;
    for (int i = 0; i < 18441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18442(int& val) {
    func_in_18442(val);
    const int x = 18442;
    std::cout << 18442;
    for (int i = 0; i < 18442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18442(int& val) {
    const int x = 18442;
    std::cout << 18442;
    for (int i = 0; i < 18442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18443(int& val) {
    func_in_18443(val);
    const int x = 18443;
    std::cout << 18443;
    for (int i = 0; i < 18443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18443(int& val) {
    const int x = 18443;
    std::cout << 18443;
    for (int i = 0; i < 18443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18444(int& val) {
    func_in_18444(val);
    const int x = 18444;
    std::cout << 18444;
    for (int i = 0; i < 18444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18444(int& val) {
    const int x = 18444;
    std::cout << 18444;
    for (int i = 0; i < 18444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18445(int& val) {
    func_in_18445(val);
    const int x = 18445;
    std::cout << 18445;
    for (int i = 0; i < 18445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18445(int& val) {
    const int x = 18445;
    std::cout << 18445;
    for (int i = 0; i < 18445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18446(int& val) {
    func_in_18446(val);
    const int x = 18446;
    std::cout << 18446;
    for (int i = 0; i < 18446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18446(int& val) {
    const int x = 18446;
    std::cout << 18446;
    for (int i = 0; i < 18446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18447(int& val) {
    func_in_18447(val);
    const int x = 18447;
    std::cout << 18447;
    for (int i = 0; i < 18447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18447(int& val) {
    const int x = 18447;
    std::cout << 18447;
    for (int i = 0; i < 18447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18448(int& val) {
    func_in_18448(val);
    const int x = 18448;
    std::cout << 18448;
    for (int i = 0; i < 18448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18448(int& val) {
    const int x = 18448;
    std::cout << 18448;
    for (int i = 0; i < 18448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18449(int& val) {
    func_in_18449(val);
    const int x = 18449;
    std::cout << 18449;
    for (int i = 0; i < 18449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18449(int& val) {
    const int x = 18449;
    std::cout << 18449;
    for (int i = 0; i < 18449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18450(int& val) {
    func_in_18450(val);
    const int x = 18450;
    std::cout << 18450;
    for (int i = 0; i < 18450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18450(int& val) {
    const int x = 18450;
    std::cout << 18450;
    for (int i = 0; i < 18450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18451(int& val) {
    func_in_18451(val);
    const int x = 18451;
    std::cout << 18451;
    for (int i = 0; i < 18451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18451(int& val) {
    const int x = 18451;
    std::cout << 18451;
    for (int i = 0; i < 18451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18452(int& val) {
    func_in_18452(val);
    const int x = 18452;
    std::cout << 18452;
    for (int i = 0; i < 18452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18452(int& val) {
    const int x = 18452;
    std::cout << 18452;
    for (int i = 0; i < 18452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18453(int& val) {
    func_in_18453(val);
    const int x = 18453;
    std::cout << 18453;
    for (int i = 0; i < 18453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18453(int& val) {
    const int x = 18453;
    std::cout << 18453;
    for (int i = 0; i < 18453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18454(int& val) {
    func_in_18454(val);
    const int x = 18454;
    std::cout << 18454;
    for (int i = 0; i < 18454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18454(int& val) {
    const int x = 18454;
    std::cout << 18454;
    for (int i = 0; i < 18454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18455(int& val) {
    func_in_18455(val);
    const int x = 18455;
    std::cout << 18455;
    for (int i = 0; i < 18455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18455(int& val) {
    const int x = 18455;
    std::cout << 18455;
    for (int i = 0; i < 18455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18456(int& val) {
    func_in_18456(val);
    const int x = 18456;
    std::cout << 18456;
    for (int i = 0; i < 18456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18456(int& val) {
    const int x = 18456;
    std::cout << 18456;
    for (int i = 0; i < 18456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18457(int& val) {
    func_in_18457(val);
    const int x = 18457;
    std::cout << 18457;
    for (int i = 0; i < 18457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18457(int& val) {
    const int x = 18457;
    std::cout << 18457;
    for (int i = 0; i < 18457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18458(int& val) {
    func_in_18458(val);
    const int x = 18458;
    std::cout << 18458;
    for (int i = 0; i < 18458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18458(int& val) {
    const int x = 18458;
    std::cout << 18458;
    for (int i = 0; i < 18458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18459(int& val) {
    func_in_18459(val);
    const int x = 18459;
    std::cout << 18459;
    for (int i = 0; i < 18459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18459(int& val) {
    const int x = 18459;
    std::cout << 18459;
    for (int i = 0; i < 18459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18460(int& val) {
    func_in_18460(val);
    const int x = 18460;
    std::cout << 18460;
    for (int i = 0; i < 18460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18460(int& val) {
    const int x = 18460;
    std::cout << 18460;
    for (int i = 0; i < 18460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18461(int& val) {
    func_in_18461(val);
    const int x = 18461;
    std::cout << 18461;
    for (int i = 0; i < 18461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18461(int& val) {
    const int x = 18461;
    std::cout << 18461;
    for (int i = 0; i < 18461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18462(int& val) {
    func_in_18462(val);
    const int x = 18462;
    std::cout << 18462;
    for (int i = 0; i < 18462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18462(int& val) {
    const int x = 18462;
    std::cout << 18462;
    for (int i = 0; i < 18462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18463(int& val) {
    func_in_18463(val);
    const int x = 18463;
    std::cout << 18463;
    for (int i = 0; i < 18463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18463(int& val) {
    const int x = 18463;
    std::cout << 18463;
    for (int i = 0; i < 18463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18464(int& val) {
    func_in_18464(val);
    const int x = 18464;
    std::cout << 18464;
    for (int i = 0; i < 18464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18464(int& val) {
    const int x = 18464;
    std::cout << 18464;
    for (int i = 0; i < 18464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18465(int& val) {
    func_in_18465(val);
    const int x = 18465;
    std::cout << 18465;
    for (int i = 0; i < 18465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18465(int& val) {
    const int x = 18465;
    std::cout << 18465;
    for (int i = 0; i < 18465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18466(int& val) {
    func_in_18466(val);
    const int x = 18466;
    std::cout << 18466;
    for (int i = 0; i < 18466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18466(int& val) {
    const int x = 18466;
    std::cout << 18466;
    for (int i = 0; i < 18466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18467(int& val) {
    func_in_18467(val);
    const int x = 18467;
    std::cout << 18467;
    for (int i = 0; i < 18467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18467(int& val) {
    const int x = 18467;
    std::cout << 18467;
    for (int i = 0; i < 18467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18468(int& val) {
    func_in_18468(val);
    const int x = 18468;
    std::cout << 18468;
    for (int i = 0; i < 18468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18468(int& val) {
    const int x = 18468;
    std::cout << 18468;
    for (int i = 0; i < 18468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18469(int& val) {
    func_in_18469(val);
    const int x = 18469;
    std::cout << 18469;
    for (int i = 0; i < 18469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18469(int& val) {
    const int x = 18469;
    std::cout << 18469;
    for (int i = 0; i < 18469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18470(int& val) {
    func_in_18470(val);
    const int x = 18470;
    std::cout << 18470;
    for (int i = 0; i < 18470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18470(int& val) {
    const int x = 18470;
    std::cout << 18470;
    for (int i = 0; i < 18470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18471(int& val) {
    func_in_18471(val);
    const int x = 18471;
    std::cout << 18471;
    for (int i = 0; i < 18471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18471(int& val) {
    const int x = 18471;
    std::cout << 18471;
    for (int i = 0; i < 18471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18472(int& val) {
    func_in_18472(val);
    const int x = 18472;
    std::cout << 18472;
    for (int i = 0; i < 18472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18472(int& val) {
    const int x = 18472;
    std::cout << 18472;
    for (int i = 0; i < 18472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18473(int& val) {
    func_in_18473(val);
    const int x = 18473;
    std::cout << 18473;
    for (int i = 0; i < 18473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18473(int& val) {
    const int x = 18473;
    std::cout << 18473;
    for (int i = 0; i < 18473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18474(int& val) {
    func_in_18474(val);
    const int x = 18474;
    std::cout << 18474;
    for (int i = 0; i < 18474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18474(int& val) {
    const int x = 18474;
    std::cout << 18474;
    for (int i = 0; i < 18474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18475(int& val) {
    func_in_18475(val);
    const int x = 18475;
    std::cout << 18475;
    for (int i = 0; i < 18475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18475(int& val) {
    const int x = 18475;
    std::cout << 18475;
    for (int i = 0; i < 18475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18476(int& val) {
    func_in_18476(val);
    const int x = 18476;
    std::cout << 18476;
    for (int i = 0; i < 18476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18476(int& val) {
    const int x = 18476;
    std::cout << 18476;
    for (int i = 0; i < 18476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18477(int& val) {
    func_in_18477(val);
    const int x = 18477;
    std::cout << 18477;
    for (int i = 0; i < 18477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18477(int& val) {
    const int x = 18477;
    std::cout << 18477;
    for (int i = 0; i < 18477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18478(int& val) {
    func_in_18478(val);
    const int x = 18478;
    std::cout << 18478;
    for (int i = 0; i < 18478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18478(int& val) {
    const int x = 18478;
    std::cout << 18478;
    for (int i = 0; i < 18478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18479(int& val) {
    func_in_18479(val);
    const int x = 18479;
    std::cout << 18479;
    for (int i = 0; i < 18479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18479(int& val) {
    const int x = 18479;
    std::cout << 18479;
    for (int i = 0; i < 18479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18480(int& val) {
    func_in_18480(val);
    const int x = 18480;
    std::cout << 18480;
    for (int i = 0; i < 18480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18480(int& val) {
    const int x = 18480;
    std::cout << 18480;
    for (int i = 0; i < 18480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18481(int& val) {
    func_in_18481(val);
    const int x = 18481;
    std::cout << 18481;
    for (int i = 0; i < 18481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18481(int& val) {
    const int x = 18481;
    std::cout << 18481;
    for (int i = 0; i < 18481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18482(int& val) {
    func_in_18482(val);
    const int x = 18482;
    std::cout << 18482;
    for (int i = 0; i < 18482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18482(int& val) {
    const int x = 18482;
    std::cout << 18482;
    for (int i = 0; i < 18482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18483(int& val) {
    func_in_18483(val);
    const int x = 18483;
    std::cout << 18483;
    for (int i = 0; i < 18483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18483(int& val) {
    const int x = 18483;
    std::cout << 18483;
    for (int i = 0; i < 18483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18484(int& val) {
    func_in_18484(val);
    const int x = 18484;
    std::cout << 18484;
    for (int i = 0; i < 18484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18484(int& val) {
    const int x = 18484;
    std::cout << 18484;
    for (int i = 0; i < 18484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18485(int& val) {
    func_in_18485(val);
    const int x = 18485;
    std::cout << 18485;
    for (int i = 0; i < 18485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18485(int& val) {
    const int x = 18485;
    std::cout << 18485;
    for (int i = 0; i < 18485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18486(int& val) {
    func_in_18486(val);
    const int x = 18486;
    std::cout << 18486;
    for (int i = 0; i < 18486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18486(int& val) {
    const int x = 18486;
    std::cout << 18486;
    for (int i = 0; i < 18486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18487(int& val) {
    func_in_18487(val);
    const int x = 18487;
    std::cout << 18487;
    for (int i = 0; i < 18487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18487(int& val) {
    const int x = 18487;
    std::cout << 18487;
    for (int i = 0; i < 18487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18488(int& val) {
    func_in_18488(val);
    const int x = 18488;
    std::cout << 18488;
    for (int i = 0; i < 18488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18488(int& val) {
    const int x = 18488;
    std::cout << 18488;
    for (int i = 0; i < 18488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18489(int& val) {
    func_in_18489(val);
    const int x = 18489;
    std::cout << 18489;
    for (int i = 0; i < 18489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18489(int& val) {
    const int x = 18489;
    std::cout << 18489;
    for (int i = 0; i < 18489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18490(int& val) {
    func_in_18490(val);
    const int x = 18490;
    std::cout << 18490;
    for (int i = 0; i < 18490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18490(int& val) {
    const int x = 18490;
    std::cout << 18490;
    for (int i = 0; i < 18490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18491(int& val) {
    func_in_18491(val);
    const int x = 18491;
    std::cout << 18491;
    for (int i = 0; i < 18491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18491(int& val) {
    const int x = 18491;
    std::cout << 18491;
    for (int i = 0; i < 18491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18492(int& val) {
    func_in_18492(val);
    const int x = 18492;
    std::cout << 18492;
    for (int i = 0; i < 18492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18492(int& val) {
    const int x = 18492;
    std::cout << 18492;
    for (int i = 0; i < 18492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18493(int& val) {
    func_in_18493(val);
    const int x = 18493;
    std::cout << 18493;
    for (int i = 0; i < 18493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18493(int& val) {
    const int x = 18493;
    std::cout << 18493;
    for (int i = 0; i < 18493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18494(int& val) {
    func_in_18494(val);
    const int x = 18494;
    std::cout << 18494;
    for (int i = 0; i < 18494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18494(int& val) {
    const int x = 18494;
    std::cout << 18494;
    for (int i = 0; i < 18494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18495(int& val) {
    func_in_18495(val);
    const int x = 18495;
    std::cout << 18495;
    for (int i = 0; i < 18495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18495(int& val) {
    const int x = 18495;
    std::cout << 18495;
    for (int i = 0; i < 18495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18496(int& val) {
    func_in_18496(val);
    const int x = 18496;
    std::cout << 18496;
    for (int i = 0; i < 18496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18496(int& val) {
    const int x = 18496;
    std::cout << 18496;
    for (int i = 0; i < 18496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18497(int& val) {
    func_in_18497(val);
    const int x = 18497;
    std::cout << 18497;
    for (int i = 0; i < 18497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18497(int& val) {
    const int x = 18497;
    std::cout << 18497;
    for (int i = 0; i < 18497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18498(int& val) {
    func_in_18498(val);
    const int x = 18498;
    std::cout << 18498;
    for (int i = 0; i < 18498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18498(int& val) {
    const int x = 18498;
    std::cout << 18498;
    for (int i = 0; i < 18498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18499(int& val) {
    func_in_18499(val);
    const int x = 18499;
    std::cout << 18499;
    for (int i = 0; i < 18499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18499(int& val) {
    const int x = 18499;
    std::cout << 18499;
    for (int i = 0; i < 18499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18500(int& val) {
    func_in_18500(val);
    const int x = 18500;
    std::cout << 18500;
    for (int i = 0; i < 18500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18500(int& val) {
    const int x = 18500;
    std::cout << 18500;
    for (int i = 0; i < 18500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18501(int& val) {
    func_in_18501(val);
    const int x = 18501;
    std::cout << 18501;
    for (int i = 0; i < 18501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18501(int& val) {
    const int x = 18501;
    std::cout << 18501;
    for (int i = 0; i < 18501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18502(int& val) {
    func_in_18502(val);
    const int x = 18502;
    std::cout << 18502;
    for (int i = 0; i < 18502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18502(int& val) {
    const int x = 18502;
    std::cout << 18502;
    for (int i = 0; i < 18502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18503(int& val) {
    func_in_18503(val);
    const int x = 18503;
    std::cout << 18503;
    for (int i = 0; i < 18503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18503(int& val) {
    const int x = 18503;
    std::cout << 18503;
    for (int i = 0; i < 18503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18504(int& val) {
    func_in_18504(val);
    const int x = 18504;
    std::cout << 18504;
    for (int i = 0; i < 18504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18504(int& val) {
    const int x = 18504;
    std::cout << 18504;
    for (int i = 0; i < 18504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18505(int& val) {
    func_in_18505(val);
    const int x = 18505;
    std::cout << 18505;
    for (int i = 0; i < 18505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18505(int& val) {
    const int x = 18505;
    std::cout << 18505;
    for (int i = 0; i < 18505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18506(int& val) {
    func_in_18506(val);
    const int x = 18506;
    std::cout << 18506;
    for (int i = 0; i < 18506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18506(int& val) {
    const int x = 18506;
    std::cout << 18506;
    for (int i = 0; i < 18506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18507(int& val) {
    func_in_18507(val);
    const int x = 18507;
    std::cout << 18507;
    for (int i = 0; i < 18507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18507(int& val) {
    const int x = 18507;
    std::cout << 18507;
    for (int i = 0; i < 18507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18508(int& val) {
    func_in_18508(val);
    const int x = 18508;
    std::cout << 18508;
    for (int i = 0; i < 18508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18508(int& val) {
    const int x = 18508;
    std::cout << 18508;
    for (int i = 0; i < 18508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18509(int& val) {
    func_in_18509(val);
    const int x = 18509;
    std::cout << 18509;
    for (int i = 0; i < 18509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18509(int& val) {
    const int x = 18509;
    std::cout << 18509;
    for (int i = 0; i < 18509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18510(int& val) {
    func_in_18510(val);
    const int x = 18510;
    std::cout << 18510;
    for (int i = 0; i < 18510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18510(int& val) {
    const int x = 18510;
    std::cout << 18510;
    for (int i = 0; i < 18510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18511(int& val) {
    func_in_18511(val);
    const int x = 18511;
    std::cout << 18511;
    for (int i = 0; i < 18511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18511(int& val) {
    const int x = 18511;
    std::cout << 18511;
    for (int i = 0; i < 18511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18512(int& val) {
    func_in_18512(val);
    const int x = 18512;
    std::cout << 18512;
    for (int i = 0; i < 18512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18512(int& val) {
    const int x = 18512;
    std::cout << 18512;
    for (int i = 0; i < 18512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18513(int& val) {
    func_in_18513(val);
    const int x = 18513;
    std::cout << 18513;
    for (int i = 0; i < 18513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18513(int& val) {
    const int x = 18513;
    std::cout << 18513;
    for (int i = 0; i < 18513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18514(int& val) {
    func_in_18514(val);
    const int x = 18514;
    std::cout << 18514;
    for (int i = 0; i < 18514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18514(int& val) {
    const int x = 18514;
    std::cout << 18514;
    for (int i = 0; i < 18514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18515(int& val) {
    func_in_18515(val);
    const int x = 18515;
    std::cout << 18515;
    for (int i = 0; i < 18515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18515(int& val) {
    const int x = 18515;
    std::cout << 18515;
    for (int i = 0; i < 18515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18516(int& val) {
    func_in_18516(val);
    const int x = 18516;
    std::cout << 18516;
    for (int i = 0; i < 18516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18516(int& val) {
    const int x = 18516;
    std::cout << 18516;
    for (int i = 0; i < 18516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18517(int& val) {
    func_in_18517(val);
    const int x = 18517;
    std::cout << 18517;
    for (int i = 0; i < 18517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18517(int& val) {
    const int x = 18517;
    std::cout << 18517;
    for (int i = 0; i < 18517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18518(int& val) {
    func_in_18518(val);
    const int x = 18518;
    std::cout << 18518;
    for (int i = 0; i < 18518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18518(int& val) {
    const int x = 18518;
    std::cout << 18518;
    for (int i = 0; i < 18518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18519(int& val) {
    func_in_18519(val);
    const int x = 18519;
    std::cout << 18519;
    for (int i = 0; i < 18519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18519(int& val) {
    const int x = 18519;
    std::cout << 18519;
    for (int i = 0; i < 18519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18520(int& val) {
    func_in_18520(val);
    const int x = 18520;
    std::cout << 18520;
    for (int i = 0; i < 18520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18520(int& val) {
    const int x = 18520;
    std::cout << 18520;
    for (int i = 0; i < 18520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18521(int& val) {
    func_in_18521(val);
    const int x = 18521;
    std::cout << 18521;
    for (int i = 0; i < 18521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18521(int& val) {
    const int x = 18521;
    std::cout << 18521;
    for (int i = 0; i < 18521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18522(int& val) {
    func_in_18522(val);
    const int x = 18522;
    std::cout << 18522;
    for (int i = 0; i < 18522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18522(int& val) {
    const int x = 18522;
    std::cout << 18522;
    for (int i = 0; i < 18522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18523(int& val) {
    func_in_18523(val);
    const int x = 18523;
    std::cout << 18523;
    for (int i = 0; i < 18523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18523(int& val) {
    const int x = 18523;
    std::cout << 18523;
    for (int i = 0; i < 18523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18524(int& val) {
    func_in_18524(val);
    const int x = 18524;
    std::cout << 18524;
    for (int i = 0; i < 18524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18524(int& val) {
    const int x = 18524;
    std::cout << 18524;
    for (int i = 0; i < 18524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18525(int& val) {
    func_in_18525(val);
    const int x = 18525;
    std::cout << 18525;
    for (int i = 0; i < 18525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18525(int& val) {
    const int x = 18525;
    std::cout << 18525;
    for (int i = 0; i < 18525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18526(int& val) {
    func_in_18526(val);
    const int x = 18526;
    std::cout << 18526;
    for (int i = 0; i < 18526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18526(int& val) {
    const int x = 18526;
    std::cout << 18526;
    for (int i = 0; i < 18526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18527(int& val) {
    func_in_18527(val);
    const int x = 18527;
    std::cout << 18527;
    for (int i = 0; i < 18527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18527(int& val) {
    const int x = 18527;
    std::cout << 18527;
    for (int i = 0; i < 18527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18528(int& val) {
    func_in_18528(val);
    const int x = 18528;
    std::cout << 18528;
    for (int i = 0; i < 18528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18528(int& val) {
    const int x = 18528;
    std::cout << 18528;
    for (int i = 0; i < 18528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18529(int& val) {
    func_in_18529(val);
    const int x = 18529;
    std::cout << 18529;
    for (int i = 0; i < 18529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18529(int& val) {
    const int x = 18529;
    std::cout << 18529;
    for (int i = 0; i < 18529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18530(int& val) {
    func_in_18530(val);
    const int x = 18530;
    std::cout << 18530;
    for (int i = 0; i < 18530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18530(int& val) {
    const int x = 18530;
    std::cout << 18530;
    for (int i = 0; i < 18530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18531(int& val) {
    func_in_18531(val);
    const int x = 18531;
    std::cout << 18531;
    for (int i = 0; i < 18531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18531(int& val) {
    const int x = 18531;
    std::cout << 18531;
    for (int i = 0; i < 18531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18532(int& val) {
    func_in_18532(val);
    const int x = 18532;
    std::cout << 18532;
    for (int i = 0; i < 18532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18532(int& val) {
    const int x = 18532;
    std::cout << 18532;
    for (int i = 0; i < 18532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18533(int& val) {
    func_in_18533(val);
    const int x = 18533;
    std::cout << 18533;
    for (int i = 0; i < 18533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18533(int& val) {
    const int x = 18533;
    std::cout << 18533;
    for (int i = 0; i < 18533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18534(int& val) {
    func_in_18534(val);
    const int x = 18534;
    std::cout << 18534;
    for (int i = 0; i < 18534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18534(int& val) {
    const int x = 18534;
    std::cout << 18534;
    for (int i = 0; i < 18534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18535(int& val) {
    func_in_18535(val);
    const int x = 18535;
    std::cout << 18535;
    for (int i = 0; i < 18535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18535(int& val) {
    const int x = 18535;
    std::cout << 18535;
    for (int i = 0; i < 18535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18536(int& val) {
    func_in_18536(val);
    const int x = 18536;
    std::cout << 18536;
    for (int i = 0; i < 18536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18536(int& val) {
    const int x = 18536;
    std::cout << 18536;
    for (int i = 0; i < 18536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18537(int& val) {
    func_in_18537(val);
    const int x = 18537;
    std::cout << 18537;
    for (int i = 0; i < 18537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18537(int& val) {
    const int x = 18537;
    std::cout << 18537;
    for (int i = 0; i < 18537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18538(int& val) {
    func_in_18538(val);
    const int x = 18538;
    std::cout << 18538;
    for (int i = 0; i < 18538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18538(int& val) {
    const int x = 18538;
    std::cout << 18538;
    for (int i = 0; i < 18538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18539(int& val) {
    func_in_18539(val);
    const int x = 18539;
    std::cout << 18539;
    for (int i = 0; i < 18539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18539(int& val) {
    const int x = 18539;
    std::cout << 18539;
    for (int i = 0; i < 18539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18540(int& val) {
    func_in_18540(val);
    const int x = 18540;
    std::cout << 18540;
    for (int i = 0; i < 18540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18540(int& val) {
    const int x = 18540;
    std::cout << 18540;
    for (int i = 0; i < 18540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18541(int& val) {
    func_in_18541(val);
    const int x = 18541;
    std::cout << 18541;
    for (int i = 0; i < 18541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18541(int& val) {
    const int x = 18541;
    std::cout << 18541;
    for (int i = 0; i < 18541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18542(int& val) {
    func_in_18542(val);
    const int x = 18542;
    std::cout << 18542;
    for (int i = 0; i < 18542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18542(int& val) {
    const int x = 18542;
    std::cout << 18542;
    for (int i = 0; i < 18542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18543(int& val) {
    func_in_18543(val);
    const int x = 18543;
    std::cout << 18543;
    for (int i = 0; i < 18543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18543(int& val) {
    const int x = 18543;
    std::cout << 18543;
    for (int i = 0; i < 18543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18544(int& val) {
    func_in_18544(val);
    const int x = 18544;
    std::cout << 18544;
    for (int i = 0; i < 18544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18544(int& val) {
    const int x = 18544;
    std::cout << 18544;
    for (int i = 0; i < 18544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18545(int& val) {
    func_in_18545(val);
    const int x = 18545;
    std::cout << 18545;
    for (int i = 0; i < 18545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18545(int& val) {
    const int x = 18545;
    std::cout << 18545;
    for (int i = 0; i < 18545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18546(int& val) {
    func_in_18546(val);
    const int x = 18546;
    std::cout << 18546;
    for (int i = 0; i < 18546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18546(int& val) {
    const int x = 18546;
    std::cout << 18546;
    for (int i = 0; i < 18546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18547(int& val) {
    func_in_18547(val);
    const int x = 18547;
    std::cout << 18547;
    for (int i = 0; i < 18547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18547(int& val) {
    const int x = 18547;
    std::cout << 18547;
    for (int i = 0; i < 18547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18548(int& val) {
    func_in_18548(val);
    const int x = 18548;
    std::cout << 18548;
    for (int i = 0; i < 18548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18548(int& val) {
    const int x = 18548;
    std::cout << 18548;
    for (int i = 0; i < 18548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18549(int& val) {
    func_in_18549(val);
    const int x = 18549;
    std::cout << 18549;
    for (int i = 0; i < 18549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18549(int& val) {
    const int x = 18549;
    std::cout << 18549;
    for (int i = 0; i < 18549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18550(int& val) {
    func_in_18550(val);
    const int x = 18550;
    std::cout << 18550;
    for (int i = 0; i < 18550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18550(int& val) {
    const int x = 18550;
    std::cout << 18550;
    for (int i = 0; i < 18550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18551(int& val) {
    func_in_18551(val);
    const int x = 18551;
    std::cout << 18551;
    for (int i = 0; i < 18551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18551(int& val) {
    const int x = 18551;
    std::cout << 18551;
    for (int i = 0; i < 18551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18552(int& val) {
    func_in_18552(val);
    const int x = 18552;
    std::cout << 18552;
    for (int i = 0; i < 18552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18552(int& val) {
    const int x = 18552;
    std::cout << 18552;
    for (int i = 0; i < 18552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18553(int& val) {
    func_in_18553(val);
    const int x = 18553;
    std::cout << 18553;
    for (int i = 0; i < 18553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18553(int& val) {
    const int x = 18553;
    std::cout << 18553;
    for (int i = 0; i < 18553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18554(int& val) {
    func_in_18554(val);
    const int x = 18554;
    std::cout << 18554;
    for (int i = 0; i < 18554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18554(int& val) {
    const int x = 18554;
    std::cout << 18554;
    for (int i = 0; i < 18554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18555(int& val) {
    func_in_18555(val);
    const int x = 18555;
    std::cout << 18555;
    for (int i = 0; i < 18555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18555(int& val) {
    const int x = 18555;
    std::cout << 18555;
    for (int i = 0; i < 18555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18556(int& val) {
    func_in_18556(val);
    const int x = 18556;
    std::cout << 18556;
    for (int i = 0; i < 18556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18556(int& val) {
    const int x = 18556;
    std::cout << 18556;
    for (int i = 0; i < 18556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18557(int& val) {
    func_in_18557(val);
    const int x = 18557;
    std::cout << 18557;
    for (int i = 0; i < 18557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18557(int& val) {
    const int x = 18557;
    std::cout << 18557;
    for (int i = 0; i < 18557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18558(int& val) {
    func_in_18558(val);
    const int x = 18558;
    std::cout << 18558;
    for (int i = 0; i < 18558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18558(int& val) {
    const int x = 18558;
    std::cout << 18558;
    for (int i = 0; i < 18558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18559(int& val) {
    func_in_18559(val);
    const int x = 18559;
    std::cout << 18559;
    for (int i = 0; i < 18559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18559(int& val) {
    const int x = 18559;
    std::cout << 18559;
    for (int i = 0; i < 18559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18560(int& val) {
    func_in_18560(val);
    const int x = 18560;
    std::cout << 18560;
    for (int i = 0; i < 18560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18560(int& val) {
    const int x = 18560;
    std::cout << 18560;
    for (int i = 0; i < 18560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18561(int& val) {
    func_in_18561(val);
    const int x = 18561;
    std::cout << 18561;
    for (int i = 0; i < 18561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18561(int& val) {
    const int x = 18561;
    std::cout << 18561;
    for (int i = 0; i < 18561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18562(int& val) {
    func_in_18562(val);
    const int x = 18562;
    std::cout << 18562;
    for (int i = 0; i < 18562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18562(int& val) {
    const int x = 18562;
    std::cout << 18562;
    for (int i = 0; i < 18562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18563(int& val) {
    func_in_18563(val);
    const int x = 18563;
    std::cout << 18563;
    for (int i = 0; i < 18563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18563(int& val) {
    const int x = 18563;
    std::cout << 18563;
    for (int i = 0; i < 18563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18564(int& val) {
    func_in_18564(val);
    const int x = 18564;
    std::cout << 18564;
    for (int i = 0; i < 18564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18564(int& val) {
    const int x = 18564;
    std::cout << 18564;
    for (int i = 0; i < 18564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18565(int& val) {
    func_in_18565(val);
    const int x = 18565;
    std::cout << 18565;
    for (int i = 0; i < 18565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18565(int& val) {
    const int x = 18565;
    std::cout << 18565;
    for (int i = 0; i < 18565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18566(int& val) {
    func_in_18566(val);
    const int x = 18566;
    std::cout << 18566;
    for (int i = 0; i < 18566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18566(int& val) {
    const int x = 18566;
    std::cout << 18566;
    for (int i = 0; i < 18566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18567(int& val) {
    func_in_18567(val);
    const int x = 18567;
    std::cout << 18567;
    for (int i = 0; i < 18567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18567(int& val) {
    const int x = 18567;
    std::cout << 18567;
    for (int i = 0; i < 18567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18568(int& val) {
    func_in_18568(val);
    const int x = 18568;
    std::cout << 18568;
    for (int i = 0; i < 18568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18568(int& val) {
    const int x = 18568;
    std::cout << 18568;
    for (int i = 0; i < 18568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18569(int& val) {
    func_in_18569(val);
    const int x = 18569;
    std::cout << 18569;
    for (int i = 0; i < 18569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18569(int& val) {
    const int x = 18569;
    std::cout << 18569;
    for (int i = 0; i < 18569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18570(int& val) {
    func_in_18570(val);
    const int x = 18570;
    std::cout << 18570;
    for (int i = 0; i < 18570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18570(int& val) {
    const int x = 18570;
    std::cout << 18570;
    for (int i = 0; i < 18570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18571(int& val) {
    func_in_18571(val);
    const int x = 18571;
    std::cout << 18571;
    for (int i = 0; i < 18571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18571(int& val) {
    const int x = 18571;
    std::cout << 18571;
    for (int i = 0; i < 18571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18572(int& val) {
    func_in_18572(val);
    const int x = 18572;
    std::cout << 18572;
    for (int i = 0; i < 18572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18572(int& val) {
    const int x = 18572;
    std::cout << 18572;
    for (int i = 0; i < 18572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18573(int& val) {
    func_in_18573(val);
    const int x = 18573;
    std::cout << 18573;
    for (int i = 0; i < 18573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18573(int& val) {
    const int x = 18573;
    std::cout << 18573;
    for (int i = 0; i < 18573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18574(int& val) {
    func_in_18574(val);
    const int x = 18574;
    std::cout << 18574;
    for (int i = 0; i < 18574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18574(int& val) {
    const int x = 18574;
    std::cout << 18574;
    for (int i = 0; i < 18574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18575(int& val) {
    func_in_18575(val);
    const int x = 18575;
    std::cout << 18575;
    for (int i = 0; i < 18575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18575(int& val) {
    const int x = 18575;
    std::cout << 18575;
    for (int i = 0; i < 18575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18576(int& val) {
    func_in_18576(val);
    const int x = 18576;
    std::cout << 18576;
    for (int i = 0; i < 18576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18576(int& val) {
    const int x = 18576;
    std::cout << 18576;
    for (int i = 0; i < 18576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18577(int& val) {
    func_in_18577(val);
    const int x = 18577;
    std::cout << 18577;
    for (int i = 0; i < 18577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18577(int& val) {
    const int x = 18577;
    std::cout << 18577;
    for (int i = 0; i < 18577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18578(int& val) {
    func_in_18578(val);
    const int x = 18578;
    std::cout << 18578;
    for (int i = 0; i < 18578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18578(int& val) {
    const int x = 18578;
    std::cout << 18578;
    for (int i = 0; i < 18578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18579(int& val) {
    func_in_18579(val);
    const int x = 18579;
    std::cout << 18579;
    for (int i = 0; i < 18579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18579(int& val) {
    const int x = 18579;
    std::cout << 18579;
    for (int i = 0; i < 18579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18580(int& val) {
    func_in_18580(val);
    const int x = 18580;
    std::cout << 18580;
    for (int i = 0; i < 18580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18580(int& val) {
    const int x = 18580;
    std::cout << 18580;
    for (int i = 0; i < 18580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18581(int& val) {
    func_in_18581(val);
    const int x = 18581;
    std::cout << 18581;
    for (int i = 0; i < 18581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18581(int& val) {
    const int x = 18581;
    std::cout << 18581;
    for (int i = 0; i < 18581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18582(int& val) {
    func_in_18582(val);
    const int x = 18582;
    std::cout << 18582;
    for (int i = 0; i < 18582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18582(int& val) {
    const int x = 18582;
    std::cout << 18582;
    for (int i = 0; i < 18582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18583(int& val) {
    func_in_18583(val);
    const int x = 18583;
    std::cout << 18583;
    for (int i = 0; i < 18583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18583(int& val) {
    const int x = 18583;
    std::cout << 18583;
    for (int i = 0; i < 18583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18584(int& val) {
    func_in_18584(val);
    const int x = 18584;
    std::cout << 18584;
    for (int i = 0; i < 18584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18584(int& val) {
    const int x = 18584;
    std::cout << 18584;
    for (int i = 0; i < 18584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18585(int& val) {
    func_in_18585(val);
    const int x = 18585;
    std::cout << 18585;
    for (int i = 0; i < 18585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18585(int& val) {
    const int x = 18585;
    std::cout << 18585;
    for (int i = 0; i < 18585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18586(int& val) {
    func_in_18586(val);
    const int x = 18586;
    std::cout << 18586;
    for (int i = 0; i < 18586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18586(int& val) {
    const int x = 18586;
    std::cout << 18586;
    for (int i = 0; i < 18586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18587(int& val) {
    func_in_18587(val);
    const int x = 18587;
    std::cout << 18587;
    for (int i = 0; i < 18587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18587(int& val) {
    const int x = 18587;
    std::cout << 18587;
    for (int i = 0; i < 18587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18588(int& val) {
    func_in_18588(val);
    const int x = 18588;
    std::cout << 18588;
    for (int i = 0; i < 18588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18588(int& val) {
    const int x = 18588;
    std::cout << 18588;
    for (int i = 0; i < 18588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18589(int& val) {
    func_in_18589(val);
    const int x = 18589;
    std::cout << 18589;
    for (int i = 0; i < 18589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18589(int& val) {
    const int x = 18589;
    std::cout << 18589;
    for (int i = 0; i < 18589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18590(int& val) {
    func_in_18590(val);
    const int x = 18590;
    std::cout << 18590;
    for (int i = 0; i < 18590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18590(int& val) {
    const int x = 18590;
    std::cout << 18590;
    for (int i = 0; i < 18590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18591(int& val) {
    func_in_18591(val);
    const int x = 18591;
    std::cout << 18591;
    for (int i = 0; i < 18591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18591(int& val) {
    const int x = 18591;
    std::cout << 18591;
    for (int i = 0; i < 18591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18592(int& val) {
    func_in_18592(val);
    const int x = 18592;
    std::cout << 18592;
    for (int i = 0; i < 18592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18592(int& val) {
    const int x = 18592;
    std::cout << 18592;
    for (int i = 0; i < 18592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18593(int& val) {
    func_in_18593(val);
    const int x = 18593;
    std::cout << 18593;
    for (int i = 0; i < 18593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18593(int& val) {
    const int x = 18593;
    std::cout << 18593;
    for (int i = 0; i < 18593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18594(int& val) {
    func_in_18594(val);
    const int x = 18594;
    std::cout << 18594;
    for (int i = 0; i < 18594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18594(int& val) {
    const int x = 18594;
    std::cout << 18594;
    for (int i = 0; i < 18594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18595(int& val) {
    func_in_18595(val);
    const int x = 18595;
    std::cout << 18595;
    for (int i = 0; i < 18595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18595(int& val) {
    const int x = 18595;
    std::cout << 18595;
    for (int i = 0; i < 18595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18596(int& val) {
    func_in_18596(val);
    const int x = 18596;
    std::cout << 18596;
    for (int i = 0; i < 18596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18596(int& val) {
    const int x = 18596;
    std::cout << 18596;
    for (int i = 0; i < 18596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18597(int& val) {
    func_in_18597(val);
    const int x = 18597;
    std::cout << 18597;
    for (int i = 0; i < 18597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18597(int& val) {
    const int x = 18597;
    std::cout << 18597;
    for (int i = 0; i < 18597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18598(int& val) {
    func_in_18598(val);
    const int x = 18598;
    std::cout << 18598;
    for (int i = 0; i < 18598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18598(int& val) {
    const int x = 18598;
    std::cout << 18598;
    for (int i = 0; i < 18598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18599(int& val) {
    func_in_18599(val);
    const int x = 18599;
    std::cout << 18599;
    for (int i = 0; i < 18599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18599(int& val) {
    const int x = 18599;
    std::cout << 18599;
    for (int i = 0; i < 18599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18600(int& val) {
    func_in_18600(val);
    const int x = 18600;
    std::cout << 18600;
    for (int i = 0; i < 18600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18600(int& val) {
    const int x = 18600;
    std::cout << 18600;
    for (int i = 0; i < 18600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18601(int& val) {
    func_in_18601(val);
    const int x = 18601;
    std::cout << 18601;
    for (int i = 0; i < 18601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18601(int& val) {
    const int x = 18601;
    std::cout << 18601;
    for (int i = 0; i < 18601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18602(int& val) {
    func_in_18602(val);
    const int x = 18602;
    std::cout << 18602;
    for (int i = 0; i < 18602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18602(int& val) {
    const int x = 18602;
    std::cout << 18602;
    for (int i = 0; i < 18602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18603(int& val) {
    func_in_18603(val);
    const int x = 18603;
    std::cout << 18603;
    for (int i = 0; i < 18603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18603(int& val) {
    const int x = 18603;
    std::cout << 18603;
    for (int i = 0; i < 18603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18604(int& val) {
    func_in_18604(val);
    const int x = 18604;
    std::cout << 18604;
    for (int i = 0; i < 18604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18604(int& val) {
    const int x = 18604;
    std::cout << 18604;
    for (int i = 0; i < 18604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18605(int& val) {
    func_in_18605(val);
    const int x = 18605;
    std::cout << 18605;
    for (int i = 0; i < 18605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18605(int& val) {
    const int x = 18605;
    std::cout << 18605;
    for (int i = 0; i < 18605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18606(int& val) {
    func_in_18606(val);
    const int x = 18606;
    std::cout << 18606;
    for (int i = 0; i < 18606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18606(int& val) {
    const int x = 18606;
    std::cout << 18606;
    for (int i = 0; i < 18606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18607(int& val) {
    func_in_18607(val);
    const int x = 18607;
    std::cout << 18607;
    for (int i = 0; i < 18607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18607(int& val) {
    const int x = 18607;
    std::cout << 18607;
    for (int i = 0; i < 18607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18608(int& val) {
    func_in_18608(val);
    const int x = 18608;
    std::cout << 18608;
    for (int i = 0; i < 18608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18608(int& val) {
    const int x = 18608;
    std::cout << 18608;
    for (int i = 0; i < 18608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18609(int& val) {
    func_in_18609(val);
    const int x = 18609;
    std::cout << 18609;
    for (int i = 0; i < 18609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18609(int& val) {
    const int x = 18609;
    std::cout << 18609;
    for (int i = 0; i < 18609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18610(int& val) {
    func_in_18610(val);
    const int x = 18610;
    std::cout << 18610;
    for (int i = 0; i < 18610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18610(int& val) {
    const int x = 18610;
    std::cout << 18610;
    for (int i = 0; i < 18610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18611(int& val) {
    func_in_18611(val);
    const int x = 18611;
    std::cout << 18611;
    for (int i = 0; i < 18611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18611(int& val) {
    const int x = 18611;
    std::cout << 18611;
    for (int i = 0; i < 18611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18612(int& val) {
    func_in_18612(val);
    const int x = 18612;
    std::cout << 18612;
    for (int i = 0; i < 18612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18612(int& val) {
    const int x = 18612;
    std::cout << 18612;
    for (int i = 0; i < 18612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18613(int& val) {
    func_in_18613(val);
    const int x = 18613;
    std::cout << 18613;
    for (int i = 0; i < 18613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18613(int& val) {
    const int x = 18613;
    std::cout << 18613;
    for (int i = 0; i < 18613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18614(int& val) {
    func_in_18614(val);
    const int x = 18614;
    std::cout << 18614;
    for (int i = 0; i < 18614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18614(int& val) {
    const int x = 18614;
    std::cout << 18614;
    for (int i = 0; i < 18614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18615(int& val) {
    func_in_18615(val);
    const int x = 18615;
    std::cout << 18615;
    for (int i = 0; i < 18615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18615(int& val) {
    const int x = 18615;
    std::cout << 18615;
    for (int i = 0; i < 18615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18616(int& val) {
    func_in_18616(val);
    const int x = 18616;
    std::cout << 18616;
    for (int i = 0; i < 18616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18616(int& val) {
    const int x = 18616;
    std::cout << 18616;
    for (int i = 0; i < 18616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18617(int& val) {
    func_in_18617(val);
    const int x = 18617;
    std::cout << 18617;
    for (int i = 0; i < 18617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18617(int& val) {
    const int x = 18617;
    std::cout << 18617;
    for (int i = 0; i < 18617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18618(int& val) {
    func_in_18618(val);
    const int x = 18618;
    std::cout << 18618;
    for (int i = 0; i < 18618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18618(int& val) {
    const int x = 18618;
    std::cout << 18618;
    for (int i = 0; i < 18618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18619(int& val) {
    func_in_18619(val);
    const int x = 18619;
    std::cout << 18619;
    for (int i = 0; i < 18619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18619(int& val) {
    const int x = 18619;
    std::cout << 18619;
    for (int i = 0; i < 18619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18620(int& val) {
    func_in_18620(val);
    const int x = 18620;
    std::cout << 18620;
    for (int i = 0; i < 18620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18620(int& val) {
    const int x = 18620;
    std::cout << 18620;
    for (int i = 0; i < 18620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18621(int& val) {
    func_in_18621(val);
    const int x = 18621;
    std::cout << 18621;
    for (int i = 0; i < 18621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18621(int& val) {
    const int x = 18621;
    std::cout << 18621;
    for (int i = 0; i < 18621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18622(int& val) {
    func_in_18622(val);
    const int x = 18622;
    std::cout << 18622;
    for (int i = 0; i < 18622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18622(int& val) {
    const int x = 18622;
    std::cout << 18622;
    for (int i = 0; i < 18622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18623(int& val) {
    func_in_18623(val);
    const int x = 18623;
    std::cout << 18623;
    for (int i = 0; i < 18623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18623(int& val) {
    const int x = 18623;
    std::cout << 18623;
    for (int i = 0; i < 18623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18624(int& val) {
    func_in_18624(val);
    const int x = 18624;
    std::cout << 18624;
    for (int i = 0; i < 18624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18624(int& val) {
    const int x = 18624;
    std::cout << 18624;
    for (int i = 0; i < 18624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18625(int& val) {
    func_in_18625(val);
    const int x = 18625;
    std::cout << 18625;
    for (int i = 0; i < 18625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18625(int& val) {
    const int x = 18625;
    std::cout << 18625;
    for (int i = 0; i < 18625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18626(int& val) {
    func_in_18626(val);
    const int x = 18626;
    std::cout << 18626;
    for (int i = 0; i < 18626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18626(int& val) {
    const int x = 18626;
    std::cout << 18626;
    for (int i = 0; i < 18626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18627(int& val) {
    func_in_18627(val);
    const int x = 18627;
    std::cout << 18627;
    for (int i = 0; i < 18627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18627(int& val) {
    const int x = 18627;
    std::cout << 18627;
    for (int i = 0; i < 18627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18628(int& val) {
    func_in_18628(val);
    const int x = 18628;
    std::cout << 18628;
    for (int i = 0; i < 18628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18628(int& val) {
    const int x = 18628;
    std::cout << 18628;
    for (int i = 0; i < 18628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18629(int& val) {
    func_in_18629(val);
    const int x = 18629;
    std::cout << 18629;
    for (int i = 0; i < 18629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18629(int& val) {
    const int x = 18629;
    std::cout << 18629;
    for (int i = 0; i < 18629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18630(int& val) {
    func_in_18630(val);
    const int x = 18630;
    std::cout << 18630;
    for (int i = 0; i < 18630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18630(int& val) {
    const int x = 18630;
    std::cout << 18630;
    for (int i = 0; i < 18630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18631(int& val) {
    func_in_18631(val);
    const int x = 18631;
    std::cout << 18631;
    for (int i = 0; i < 18631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18631(int& val) {
    const int x = 18631;
    std::cout << 18631;
    for (int i = 0; i < 18631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18632(int& val) {
    func_in_18632(val);
    const int x = 18632;
    std::cout << 18632;
    for (int i = 0; i < 18632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18632(int& val) {
    const int x = 18632;
    std::cout << 18632;
    for (int i = 0; i < 18632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18633(int& val) {
    func_in_18633(val);
    const int x = 18633;
    std::cout << 18633;
    for (int i = 0; i < 18633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18633(int& val) {
    const int x = 18633;
    std::cout << 18633;
    for (int i = 0; i < 18633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18634(int& val) {
    func_in_18634(val);
    const int x = 18634;
    std::cout << 18634;
    for (int i = 0; i < 18634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18634(int& val) {
    const int x = 18634;
    std::cout << 18634;
    for (int i = 0; i < 18634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18635(int& val) {
    func_in_18635(val);
    const int x = 18635;
    std::cout << 18635;
    for (int i = 0; i < 18635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18635(int& val) {
    const int x = 18635;
    std::cout << 18635;
    for (int i = 0; i < 18635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18636(int& val) {
    func_in_18636(val);
    const int x = 18636;
    std::cout << 18636;
    for (int i = 0; i < 18636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18636(int& val) {
    const int x = 18636;
    std::cout << 18636;
    for (int i = 0; i < 18636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18637(int& val) {
    func_in_18637(val);
    const int x = 18637;
    std::cout << 18637;
    for (int i = 0; i < 18637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18637(int& val) {
    const int x = 18637;
    std::cout << 18637;
    for (int i = 0; i < 18637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18638(int& val) {
    func_in_18638(val);
    const int x = 18638;
    std::cout << 18638;
    for (int i = 0; i < 18638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18638(int& val) {
    const int x = 18638;
    std::cout << 18638;
    for (int i = 0; i < 18638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18639(int& val) {
    func_in_18639(val);
    const int x = 18639;
    std::cout << 18639;
    for (int i = 0; i < 18639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18639(int& val) {
    const int x = 18639;
    std::cout << 18639;
    for (int i = 0; i < 18639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18640(int& val) {
    func_in_18640(val);
    const int x = 18640;
    std::cout << 18640;
    for (int i = 0; i < 18640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18640(int& val) {
    const int x = 18640;
    std::cout << 18640;
    for (int i = 0; i < 18640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18641(int& val) {
    func_in_18641(val);
    const int x = 18641;
    std::cout << 18641;
    for (int i = 0; i < 18641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18641(int& val) {
    const int x = 18641;
    std::cout << 18641;
    for (int i = 0; i < 18641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18642(int& val) {
    func_in_18642(val);
    const int x = 18642;
    std::cout << 18642;
    for (int i = 0; i < 18642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18642(int& val) {
    const int x = 18642;
    std::cout << 18642;
    for (int i = 0; i < 18642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18643(int& val) {
    func_in_18643(val);
    const int x = 18643;
    std::cout << 18643;
    for (int i = 0; i < 18643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18643(int& val) {
    const int x = 18643;
    std::cout << 18643;
    for (int i = 0; i < 18643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18644(int& val) {
    func_in_18644(val);
    const int x = 18644;
    std::cout << 18644;
    for (int i = 0; i < 18644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18644(int& val) {
    const int x = 18644;
    std::cout << 18644;
    for (int i = 0; i < 18644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18645(int& val) {
    func_in_18645(val);
    const int x = 18645;
    std::cout << 18645;
    for (int i = 0; i < 18645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18645(int& val) {
    const int x = 18645;
    std::cout << 18645;
    for (int i = 0; i < 18645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18646(int& val) {
    func_in_18646(val);
    const int x = 18646;
    std::cout << 18646;
    for (int i = 0; i < 18646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18646(int& val) {
    const int x = 18646;
    std::cout << 18646;
    for (int i = 0; i < 18646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18647(int& val) {
    func_in_18647(val);
    const int x = 18647;
    std::cout << 18647;
    for (int i = 0; i < 18647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18647(int& val) {
    const int x = 18647;
    std::cout << 18647;
    for (int i = 0; i < 18647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18648(int& val) {
    func_in_18648(val);
    const int x = 18648;
    std::cout << 18648;
    for (int i = 0; i < 18648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18648(int& val) {
    const int x = 18648;
    std::cout << 18648;
    for (int i = 0; i < 18648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18649(int& val) {
    func_in_18649(val);
    const int x = 18649;
    std::cout << 18649;
    for (int i = 0; i < 18649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18649(int& val) {
    const int x = 18649;
    std::cout << 18649;
    for (int i = 0; i < 18649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18650(int& val) {
    func_in_18650(val);
    const int x = 18650;
    std::cout << 18650;
    for (int i = 0; i < 18650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18650(int& val) {
    const int x = 18650;
    std::cout << 18650;
    for (int i = 0; i < 18650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18651(int& val) {
    func_in_18651(val);
    const int x = 18651;
    std::cout << 18651;
    for (int i = 0; i < 18651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18651(int& val) {
    const int x = 18651;
    std::cout << 18651;
    for (int i = 0; i < 18651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18652(int& val) {
    func_in_18652(val);
    const int x = 18652;
    std::cout << 18652;
    for (int i = 0; i < 18652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18652(int& val) {
    const int x = 18652;
    std::cout << 18652;
    for (int i = 0; i < 18652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18653(int& val) {
    func_in_18653(val);
    const int x = 18653;
    std::cout << 18653;
    for (int i = 0; i < 18653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18653(int& val) {
    const int x = 18653;
    std::cout << 18653;
    for (int i = 0; i < 18653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18654(int& val) {
    func_in_18654(val);
    const int x = 18654;
    std::cout << 18654;
    for (int i = 0; i < 18654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18654(int& val) {
    const int x = 18654;
    std::cout << 18654;
    for (int i = 0; i < 18654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18655(int& val) {
    func_in_18655(val);
    const int x = 18655;
    std::cout << 18655;
    for (int i = 0; i < 18655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18655(int& val) {
    const int x = 18655;
    std::cout << 18655;
    for (int i = 0; i < 18655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18656(int& val) {
    func_in_18656(val);
    const int x = 18656;
    std::cout << 18656;
    for (int i = 0; i < 18656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18656(int& val) {
    const int x = 18656;
    std::cout << 18656;
    for (int i = 0; i < 18656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18657(int& val) {
    func_in_18657(val);
    const int x = 18657;
    std::cout << 18657;
    for (int i = 0; i < 18657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18657(int& val) {
    const int x = 18657;
    std::cout << 18657;
    for (int i = 0; i < 18657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18658(int& val) {
    func_in_18658(val);
    const int x = 18658;
    std::cout << 18658;
    for (int i = 0; i < 18658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18658(int& val) {
    const int x = 18658;
    std::cout << 18658;
    for (int i = 0; i < 18658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18659(int& val) {
    func_in_18659(val);
    const int x = 18659;
    std::cout << 18659;
    for (int i = 0; i < 18659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18659(int& val) {
    const int x = 18659;
    std::cout << 18659;
    for (int i = 0; i < 18659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18660(int& val) {
    func_in_18660(val);
    const int x = 18660;
    std::cout << 18660;
    for (int i = 0; i < 18660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18660(int& val) {
    const int x = 18660;
    std::cout << 18660;
    for (int i = 0; i < 18660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18661(int& val) {
    func_in_18661(val);
    const int x = 18661;
    std::cout << 18661;
    for (int i = 0; i < 18661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18661(int& val) {
    const int x = 18661;
    std::cout << 18661;
    for (int i = 0; i < 18661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18662(int& val) {
    func_in_18662(val);
    const int x = 18662;
    std::cout << 18662;
    for (int i = 0; i < 18662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18662(int& val) {
    const int x = 18662;
    std::cout << 18662;
    for (int i = 0; i < 18662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18663(int& val) {
    func_in_18663(val);
    const int x = 18663;
    std::cout << 18663;
    for (int i = 0; i < 18663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18663(int& val) {
    const int x = 18663;
    std::cout << 18663;
    for (int i = 0; i < 18663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18664(int& val) {
    func_in_18664(val);
    const int x = 18664;
    std::cout << 18664;
    for (int i = 0; i < 18664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18664(int& val) {
    const int x = 18664;
    std::cout << 18664;
    for (int i = 0; i < 18664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18665(int& val) {
    func_in_18665(val);
    const int x = 18665;
    std::cout << 18665;
    for (int i = 0; i < 18665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18665(int& val) {
    const int x = 18665;
    std::cout << 18665;
    for (int i = 0; i < 18665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18666(int& val) {
    func_in_18666(val);
    const int x = 18666;
    std::cout << 18666;
    for (int i = 0; i < 18666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18666(int& val) {
    const int x = 18666;
    std::cout << 18666;
    for (int i = 0; i < 18666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18667(int& val) {
    func_in_18667(val);
    const int x = 18667;
    std::cout << 18667;
    for (int i = 0; i < 18667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18667(int& val) {
    const int x = 18667;
    std::cout << 18667;
    for (int i = 0; i < 18667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18668(int& val) {
    func_in_18668(val);
    const int x = 18668;
    std::cout << 18668;
    for (int i = 0; i < 18668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18668(int& val) {
    const int x = 18668;
    std::cout << 18668;
    for (int i = 0; i < 18668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18669(int& val) {
    func_in_18669(val);
    const int x = 18669;
    std::cout << 18669;
    for (int i = 0; i < 18669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18669(int& val) {
    const int x = 18669;
    std::cout << 18669;
    for (int i = 0; i < 18669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18670(int& val) {
    func_in_18670(val);
    const int x = 18670;
    std::cout << 18670;
    for (int i = 0; i < 18670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18670(int& val) {
    const int x = 18670;
    std::cout << 18670;
    for (int i = 0; i < 18670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18671(int& val) {
    func_in_18671(val);
    const int x = 18671;
    std::cout << 18671;
    for (int i = 0; i < 18671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18671(int& val) {
    const int x = 18671;
    std::cout << 18671;
    for (int i = 0; i < 18671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18672(int& val) {
    func_in_18672(val);
    const int x = 18672;
    std::cout << 18672;
    for (int i = 0; i < 18672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18672(int& val) {
    const int x = 18672;
    std::cout << 18672;
    for (int i = 0; i < 18672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18673(int& val) {
    func_in_18673(val);
    const int x = 18673;
    std::cout << 18673;
    for (int i = 0; i < 18673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18673(int& val) {
    const int x = 18673;
    std::cout << 18673;
    for (int i = 0; i < 18673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18674(int& val) {
    func_in_18674(val);
    const int x = 18674;
    std::cout << 18674;
    for (int i = 0; i < 18674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18674(int& val) {
    const int x = 18674;
    std::cout << 18674;
    for (int i = 0; i < 18674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18675(int& val) {
    func_in_18675(val);
    const int x = 18675;
    std::cout << 18675;
    for (int i = 0; i < 18675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18675(int& val) {
    const int x = 18675;
    std::cout << 18675;
    for (int i = 0; i < 18675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18676(int& val) {
    func_in_18676(val);
    const int x = 18676;
    std::cout << 18676;
    for (int i = 0; i < 18676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18676(int& val) {
    const int x = 18676;
    std::cout << 18676;
    for (int i = 0; i < 18676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18677(int& val) {
    func_in_18677(val);
    const int x = 18677;
    std::cout << 18677;
    for (int i = 0; i < 18677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18677(int& val) {
    const int x = 18677;
    std::cout << 18677;
    for (int i = 0; i < 18677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18678(int& val) {
    func_in_18678(val);
    const int x = 18678;
    std::cout << 18678;
    for (int i = 0; i < 18678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18678(int& val) {
    const int x = 18678;
    std::cout << 18678;
    for (int i = 0; i < 18678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18679(int& val) {
    func_in_18679(val);
    const int x = 18679;
    std::cout << 18679;
    for (int i = 0; i < 18679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18679(int& val) {
    const int x = 18679;
    std::cout << 18679;
    for (int i = 0; i < 18679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18680(int& val) {
    func_in_18680(val);
    const int x = 18680;
    std::cout << 18680;
    for (int i = 0; i < 18680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18680(int& val) {
    const int x = 18680;
    std::cout << 18680;
    for (int i = 0; i < 18680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18681(int& val) {
    func_in_18681(val);
    const int x = 18681;
    std::cout << 18681;
    for (int i = 0; i < 18681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18681(int& val) {
    const int x = 18681;
    std::cout << 18681;
    for (int i = 0; i < 18681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18682(int& val) {
    func_in_18682(val);
    const int x = 18682;
    std::cout << 18682;
    for (int i = 0; i < 18682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18682(int& val) {
    const int x = 18682;
    std::cout << 18682;
    for (int i = 0; i < 18682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18683(int& val) {
    func_in_18683(val);
    const int x = 18683;
    std::cout << 18683;
    for (int i = 0; i < 18683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18683(int& val) {
    const int x = 18683;
    std::cout << 18683;
    for (int i = 0; i < 18683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18684(int& val) {
    func_in_18684(val);
    const int x = 18684;
    std::cout << 18684;
    for (int i = 0; i < 18684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18684(int& val) {
    const int x = 18684;
    std::cout << 18684;
    for (int i = 0; i < 18684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18685(int& val) {
    func_in_18685(val);
    const int x = 18685;
    std::cout << 18685;
    for (int i = 0; i < 18685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18685(int& val) {
    const int x = 18685;
    std::cout << 18685;
    for (int i = 0; i < 18685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18686(int& val) {
    func_in_18686(val);
    const int x = 18686;
    std::cout << 18686;
    for (int i = 0; i < 18686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18686(int& val) {
    const int x = 18686;
    std::cout << 18686;
    for (int i = 0; i < 18686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18687(int& val) {
    func_in_18687(val);
    const int x = 18687;
    std::cout << 18687;
    for (int i = 0; i < 18687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18687(int& val) {
    const int x = 18687;
    std::cout << 18687;
    for (int i = 0; i < 18687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18688(int& val) {
    func_in_18688(val);
    const int x = 18688;
    std::cout << 18688;
    for (int i = 0; i < 18688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18688(int& val) {
    const int x = 18688;
    std::cout << 18688;
    for (int i = 0; i < 18688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18689(int& val) {
    func_in_18689(val);
    const int x = 18689;
    std::cout << 18689;
    for (int i = 0; i < 18689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18689(int& val) {
    const int x = 18689;
    std::cout << 18689;
    for (int i = 0; i < 18689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18690(int& val) {
    func_in_18690(val);
    const int x = 18690;
    std::cout << 18690;
    for (int i = 0; i < 18690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18690(int& val) {
    const int x = 18690;
    std::cout << 18690;
    for (int i = 0; i < 18690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18691(int& val) {
    func_in_18691(val);
    const int x = 18691;
    std::cout << 18691;
    for (int i = 0; i < 18691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18691(int& val) {
    const int x = 18691;
    std::cout << 18691;
    for (int i = 0; i < 18691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18692(int& val) {
    func_in_18692(val);
    const int x = 18692;
    std::cout << 18692;
    for (int i = 0; i < 18692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18692(int& val) {
    const int x = 18692;
    std::cout << 18692;
    for (int i = 0; i < 18692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18693(int& val) {
    func_in_18693(val);
    const int x = 18693;
    std::cout << 18693;
    for (int i = 0; i < 18693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18693(int& val) {
    const int x = 18693;
    std::cout << 18693;
    for (int i = 0; i < 18693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18694(int& val) {
    func_in_18694(val);
    const int x = 18694;
    std::cout << 18694;
    for (int i = 0; i < 18694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18694(int& val) {
    const int x = 18694;
    std::cout << 18694;
    for (int i = 0; i < 18694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18695(int& val) {
    func_in_18695(val);
    const int x = 18695;
    std::cout << 18695;
    for (int i = 0; i < 18695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18695(int& val) {
    const int x = 18695;
    std::cout << 18695;
    for (int i = 0; i < 18695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18696(int& val) {
    func_in_18696(val);
    const int x = 18696;
    std::cout << 18696;
    for (int i = 0; i < 18696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18696(int& val) {
    const int x = 18696;
    std::cout << 18696;
    for (int i = 0; i < 18696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18697(int& val) {
    func_in_18697(val);
    const int x = 18697;
    std::cout << 18697;
    for (int i = 0; i < 18697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18697(int& val) {
    const int x = 18697;
    std::cout << 18697;
    for (int i = 0; i < 18697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18698(int& val) {
    func_in_18698(val);
    const int x = 18698;
    std::cout << 18698;
    for (int i = 0; i < 18698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18698(int& val) {
    const int x = 18698;
    std::cout << 18698;
    for (int i = 0; i < 18698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18699(int& val) {
    func_in_18699(val);
    const int x = 18699;
    std::cout << 18699;
    for (int i = 0; i < 18699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18699(int& val) {
    const int x = 18699;
    std::cout << 18699;
    for (int i = 0; i < 18699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18700(int& val) {
    func_in_18700(val);
    const int x = 18700;
    std::cout << 18700;
    for (int i = 0; i < 18700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18700(int& val) {
    const int x = 18700;
    std::cout << 18700;
    for (int i = 0; i < 18700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18701(int& val) {
    func_in_18701(val);
    const int x = 18701;
    std::cout << 18701;
    for (int i = 0; i < 18701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18701(int& val) {
    const int x = 18701;
    std::cout << 18701;
    for (int i = 0; i < 18701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18702(int& val) {
    func_in_18702(val);
    const int x = 18702;
    std::cout << 18702;
    for (int i = 0; i < 18702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18702(int& val) {
    const int x = 18702;
    std::cout << 18702;
    for (int i = 0; i < 18702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18703(int& val) {
    func_in_18703(val);
    const int x = 18703;
    std::cout << 18703;
    for (int i = 0; i < 18703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18703(int& val) {
    const int x = 18703;
    std::cout << 18703;
    for (int i = 0; i < 18703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18704(int& val) {
    func_in_18704(val);
    const int x = 18704;
    std::cout << 18704;
    for (int i = 0; i < 18704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18704(int& val) {
    const int x = 18704;
    std::cout << 18704;
    for (int i = 0; i < 18704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18705(int& val) {
    func_in_18705(val);
    const int x = 18705;
    std::cout << 18705;
    for (int i = 0; i < 18705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18705(int& val) {
    const int x = 18705;
    std::cout << 18705;
    for (int i = 0; i < 18705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18706(int& val) {
    func_in_18706(val);
    const int x = 18706;
    std::cout << 18706;
    for (int i = 0; i < 18706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18706(int& val) {
    const int x = 18706;
    std::cout << 18706;
    for (int i = 0; i < 18706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18707(int& val) {
    func_in_18707(val);
    const int x = 18707;
    std::cout << 18707;
    for (int i = 0; i < 18707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18707(int& val) {
    const int x = 18707;
    std::cout << 18707;
    for (int i = 0; i < 18707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18708(int& val) {
    func_in_18708(val);
    const int x = 18708;
    std::cout << 18708;
    for (int i = 0; i < 18708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18708(int& val) {
    const int x = 18708;
    std::cout << 18708;
    for (int i = 0; i < 18708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18709(int& val) {
    func_in_18709(val);
    const int x = 18709;
    std::cout << 18709;
    for (int i = 0; i < 18709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18709(int& val) {
    const int x = 18709;
    std::cout << 18709;
    for (int i = 0; i < 18709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18710(int& val) {
    func_in_18710(val);
    const int x = 18710;
    std::cout << 18710;
    for (int i = 0; i < 18710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18710(int& val) {
    const int x = 18710;
    std::cout << 18710;
    for (int i = 0; i < 18710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18711(int& val) {
    func_in_18711(val);
    const int x = 18711;
    std::cout << 18711;
    for (int i = 0; i < 18711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18711(int& val) {
    const int x = 18711;
    std::cout << 18711;
    for (int i = 0; i < 18711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18712(int& val) {
    func_in_18712(val);
    const int x = 18712;
    std::cout << 18712;
    for (int i = 0; i < 18712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18712(int& val) {
    const int x = 18712;
    std::cout << 18712;
    for (int i = 0; i < 18712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18713(int& val) {
    func_in_18713(val);
    const int x = 18713;
    std::cout << 18713;
    for (int i = 0; i < 18713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18713(int& val) {
    const int x = 18713;
    std::cout << 18713;
    for (int i = 0; i < 18713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18714(int& val) {
    func_in_18714(val);
    const int x = 18714;
    std::cout << 18714;
    for (int i = 0; i < 18714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18714(int& val) {
    const int x = 18714;
    std::cout << 18714;
    for (int i = 0; i < 18714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18715(int& val) {
    func_in_18715(val);
    const int x = 18715;
    std::cout << 18715;
    for (int i = 0; i < 18715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18715(int& val) {
    const int x = 18715;
    std::cout << 18715;
    for (int i = 0; i < 18715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18716(int& val) {
    func_in_18716(val);
    const int x = 18716;
    std::cout << 18716;
    for (int i = 0; i < 18716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18716(int& val) {
    const int x = 18716;
    std::cout << 18716;
    for (int i = 0; i < 18716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18717(int& val) {
    func_in_18717(val);
    const int x = 18717;
    std::cout << 18717;
    for (int i = 0; i < 18717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18717(int& val) {
    const int x = 18717;
    std::cout << 18717;
    for (int i = 0; i < 18717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18718(int& val) {
    func_in_18718(val);
    const int x = 18718;
    std::cout << 18718;
    for (int i = 0; i < 18718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18718(int& val) {
    const int x = 18718;
    std::cout << 18718;
    for (int i = 0; i < 18718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18719(int& val) {
    func_in_18719(val);
    const int x = 18719;
    std::cout << 18719;
    for (int i = 0; i < 18719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18719(int& val) {
    const int x = 18719;
    std::cout << 18719;
    for (int i = 0; i < 18719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18720(int& val) {
    func_in_18720(val);
    const int x = 18720;
    std::cout << 18720;
    for (int i = 0; i < 18720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18720(int& val) {
    const int x = 18720;
    std::cout << 18720;
    for (int i = 0; i < 18720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18721(int& val) {
    func_in_18721(val);
    const int x = 18721;
    std::cout << 18721;
    for (int i = 0; i < 18721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18721(int& val) {
    const int x = 18721;
    std::cout << 18721;
    for (int i = 0; i < 18721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18722(int& val) {
    func_in_18722(val);
    const int x = 18722;
    std::cout << 18722;
    for (int i = 0; i < 18722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18722(int& val) {
    const int x = 18722;
    std::cout << 18722;
    for (int i = 0; i < 18722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18723(int& val) {
    func_in_18723(val);
    const int x = 18723;
    std::cout << 18723;
    for (int i = 0; i < 18723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18723(int& val) {
    const int x = 18723;
    std::cout << 18723;
    for (int i = 0; i < 18723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18724(int& val) {
    func_in_18724(val);
    const int x = 18724;
    std::cout << 18724;
    for (int i = 0; i < 18724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18724(int& val) {
    const int x = 18724;
    std::cout << 18724;
    for (int i = 0; i < 18724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18725(int& val) {
    func_in_18725(val);
    const int x = 18725;
    std::cout << 18725;
    for (int i = 0; i < 18725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18725(int& val) {
    const int x = 18725;
    std::cout << 18725;
    for (int i = 0; i < 18725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18726(int& val) {
    func_in_18726(val);
    const int x = 18726;
    std::cout << 18726;
    for (int i = 0; i < 18726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18726(int& val) {
    const int x = 18726;
    std::cout << 18726;
    for (int i = 0; i < 18726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18727(int& val) {
    func_in_18727(val);
    const int x = 18727;
    std::cout << 18727;
    for (int i = 0; i < 18727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18727(int& val) {
    const int x = 18727;
    std::cout << 18727;
    for (int i = 0; i < 18727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18728(int& val) {
    func_in_18728(val);
    const int x = 18728;
    std::cout << 18728;
    for (int i = 0; i < 18728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18728(int& val) {
    const int x = 18728;
    std::cout << 18728;
    for (int i = 0; i < 18728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18729(int& val) {
    func_in_18729(val);
    const int x = 18729;
    std::cout << 18729;
    for (int i = 0; i < 18729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18729(int& val) {
    const int x = 18729;
    std::cout << 18729;
    for (int i = 0; i < 18729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18730(int& val) {
    func_in_18730(val);
    const int x = 18730;
    std::cout << 18730;
    for (int i = 0; i < 18730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18730(int& val) {
    const int x = 18730;
    std::cout << 18730;
    for (int i = 0; i < 18730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18731(int& val) {
    func_in_18731(val);
    const int x = 18731;
    std::cout << 18731;
    for (int i = 0; i < 18731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18731(int& val) {
    const int x = 18731;
    std::cout << 18731;
    for (int i = 0; i < 18731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18732(int& val) {
    func_in_18732(val);
    const int x = 18732;
    std::cout << 18732;
    for (int i = 0; i < 18732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18732(int& val) {
    const int x = 18732;
    std::cout << 18732;
    for (int i = 0; i < 18732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18733(int& val) {
    func_in_18733(val);
    const int x = 18733;
    std::cout << 18733;
    for (int i = 0; i < 18733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18733(int& val) {
    const int x = 18733;
    std::cout << 18733;
    for (int i = 0; i < 18733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18734(int& val) {
    func_in_18734(val);
    const int x = 18734;
    std::cout << 18734;
    for (int i = 0; i < 18734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18734(int& val) {
    const int x = 18734;
    std::cout << 18734;
    for (int i = 0; i < 18734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18735(int& val) {
    func_in_18735(val);
    const int x = 18735;
    std::cout << 18735;
    for (int i = 0; i < 18735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18735(int& val) {
    const int x = 18735;
    std::cout << 18735;
    for (int i = 0; i < 18735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18736(int& val) {
    func_in_18736(val);
    const int x = 18736;
    std::cout << 18736;
    for (int i = 0; i < 18736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18736(int& val) {
    const int x = 18736;
    std::cout << 18736;
    for (int i = 0; i < 18736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18737(int& val) {
    func_in_18737(val);
    const int x = 18737;
    std::cout << 18737;
    for (int i = 0; i < 18737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18737(int& val) {
    const int x = 18737;
    std::cout << 18737;
    for (int i = 0; i < 18737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18738(int& val) {
    func_in_18738(val);
    const int x = 18738;
    std::cout << 18738;
    for (int i = 0; i < 18738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18738(int& val) {
    const int x = 18738;
    std::cout << 18738;
    for (int i = 0; i < 18738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18739(int& val) {
    func_in_18739(val);
    const int x = 18739;
    std::cout << 18739;
    for (int i = 0; i < 18739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18739(int& val) {
    const int x = 18739;
    std::cout << 18739;
    for (int i = 0; i < 18739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18740(int& val) {
    func_in_18740(val);
    const int x = 18740;
    std::cout << 18740;
    for (int i = 0; i < 18740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18740(int& val) {
    const int x = 18740;
    std::cout << 18740;
    for (int i = 0; i < 18740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18741(int& val) {
    func_in_18741(val);
    const int x = 18741;
    std::cout << 18741;
    for (int i = 0; i < 18741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18741(int& val) {
    const int x = 18741;
    std::cout << 18741;
    for (int i = 0; i < 18741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18742(int& val) {
    func_in_18742(val);
    const int x = 18742;
    std::cout << 18742;
    for (int i = 0; i < 18742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18742(int& val) {
    const int x = 18742;
    std::cout << 18742;
    for (int i = 0; i < 18742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18743(int& val) {
    func_in_18743(val);
    const int x = 18743;
    std::cout << 18743;
    for (int i = 0; i < 18743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18743(int& val) {
    const int x = 18743;
    std::cout << 18743;
    for (int i = 0; i < 18743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18744(int& val) {
    func_in_18744(val);
    const int x = 18744;
    std::cout << 18744;
    for (int i = 0; i < 18744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18744(int& val) {
    const int x = 18744;
    std::cout << 18744;
    for (int i = 0; i < 18744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18745(int& val) {
    func_in_18745(val);
    const int x = 18745;
    std::cout << 18745;
    for (int i = 0; i < 18745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18745(int& val) {
    const int x = 18745;
    std::cout << 18745;
    for (int i = 0; i < 18745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18746(int& val) {
    func_in_18746(val);
    const int x = 18746;
    std::cout << 18746;
    for (int i = 0; i < 18746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18746(int& val) {
    const int x = 18746;
    std::cout << 18746;
    for (int i = 0; i < 18746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18747(int& val) {
    func_in_18747(val);
    const int x = 18747;
    std::cout << 18747;
    for (int i = 0; i < 18747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18747(int& val) {
    const int x = 18747;
    std::cout << 18747;
    for (int i = 0; i < 18747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18748(int& val) {
    func_in_18748(val);
    const int x = 18748;
    std::cout << 18748;
    for (int i = 0; i < 18748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18748(int& val) {
    const int x = 18748;
    std::cout << 18748;
    for (int i = 0; i < 18748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18749(int& val) {
    func_in_18749(val);
    const int x = 18749;
    std::cout << 18749;
    for (int i = 0; i < 18749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18749(int& val) {
    const int x = 18749;
    std::cout << 18749;
    for (int i = 0; i < 18749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18750(int& val) {
    func_in_18750(val);
    const int x = 18750;
    std::cout << 18750;
    for (int i = 0; i < 18750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18750(int& val) {
    const int x = 18750;
    std::cout << 18750;
    for (int i = 0; i < 18750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18751(int& val) {
    func_in_18751(val);
    const int x = 18751;
    std::cout << 18751;
    for (int i = 0; i < 18751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18751(int& val) {
    const int x = 18751;
    std::cout << 18751;
    for (int i = 0; i < 18751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18752(int& val) {
    func_in_18752(val);
    const int x = 18752;
    std::cout << 18752;
    for (int i = 0; i < 18752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18752(int& val) {
    const int x = 18752;
    std::cout << 18752;
    for (int i = 0; i < 18752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18753(int& val) {
    func_in_18753(val);
    const int x = 18753;
    std::cout << 18753;
    for (int i = 0; i < 18753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18753(int& val) {
    const int x = 18753;
    std::cout << 18753;
    for (int i = 0; i < 18753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18754(int& val) {
    func_in_18754(val);
    const int x = 18754;
    std::cout << 18754;
    for (int i = 0; i < 18754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18754(int& val) {
    const int x = 18754;
    std::cout << 18754;
    for (int i = 0; i < 18754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18755(int& val) {
    func_in_18755(val);
    const int x = 18755;
    std::cout << 18755;
    for (int i = 0; i < 18755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18755(int& val) {
    const int x = 18755;
    std::cout << 18755;
    for (int i = 0; i < 18755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18756(int& val) {
    func_in_18756(val);
    const int x = 18756;
    std::cout << 18756;
    for (int i = 0; i < 18756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18756(int& val) {
    const int x = 18756;
    std::cout << 18756;
    for (int i = 0; i < 18756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18757(int& val) {
    func_in_18757(val);
    const int x = 18757;
    std::cout << 18757;
    for (int i = 0; i < 18757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18757(int& val) {
    const int x = 18757;
    std::cout << 18757;
    for (int i = 0; i < 18757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18758(int& val) {
    func_in_18758(val);
    const int x = 18758;
    std::cout << 18758;
    for (int i = 0; i < 18758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18758(int& val) {
    const int x = 18758;
    std::cout << 18758;
    for (int i = 0; i < 18758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18759(int& val) {
    func_in_18759(val);
    const int x = 18759;
    std::cout << 18759;
    for (int i = 0; i < 18759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18759(int& val) {
    const int x = 18759;
    std::cout << 18759;
    for (int i = 0; i < 18759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18760(int& val) {
    func_in_18760(val);
    const int x = 18760;
    std::cout << 18760;
    for (int i = 0; i < 18760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18760(int& val) {
    const int x = 18760;
    std::cout << 18760;
    for (int i = 0; i < 18760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18761(int& val) {
    func_in_18761(val);
    const int x = 18761;
    std::cout << 18761;
    for (int i = 0; i < 18761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18761(int& val) {
    const int x = 18761;
    std::cout << 18761;
    for (int i = 0; i < 18761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18762(int& val) {
    func_in_18762(val);
    const int x = 18762;
    std::cout << 18762;
    for (int i = 0; i < 18762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18762(int& val) {
    const int x = 18762;
    std::cout << 18762;
    for (int i = 0; i < 18762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18763(int& val) {
    func_in_18763(val);
    const int x = 18763;
    std::cout << 18763;
    for (int i = 0; i < 18763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18763(int& val) {
    const int x = 18763;
    std::cout << 18763;
    for (int i = 0; i < 18763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18764(int& val) {
    func_in_18764(val);
    const int x = 18764;
    std::cout << 18764;
    for (int i = 0; i < 18764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18764(int& val) {
    const int x = 18764;
    std::cout << 18764;
    for (int i = 0; i < 18764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18765(int& val) {
    func_in_18765(val);
    const int x = 18765;
    std::cout << 18765;
    for (int i = 0; i < 18765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18765(int& val) {
    const int x = 18765;
    std::cout << 18765;
    for (int i = 0; i < 18765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18766(int& val) {
    func_in_18766(val);
    const int x = 18766;
    std::cout << 18766;
    for (int i = 0; i < 18766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18766(int& val) {
    const int x = 18766;
    std::cout << 18766;
    for (int i = 0; i < 18766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18767(int& val) {
    func_in_18767(val);
    const int x = 18767;
    std::cout << 18767;
    for (int i = 0; i < 18767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18767(int& val) {
    const int x = 18767;
    std::cout << 18767;
    for (int i = 0; i < 18767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18768(int& val) {
    func_in_18768(val);
    const int x = 18768;
    std::cout << 18768;
    for (int i = 0; i < 18768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18768(int& val) {
    const int x = 18768;
    std::cout << 18768;
    for (int i = 0; i < 18768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18769(int& val) {
    func_in_18769(val);
    const int x = 18769;
    std::cout << 18769;
    for (int i = 0; i < 18769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18769(int& val) {
    const int x = 18769;
    std::cout << 18769;
    for (int i = 0; i < 18769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18770(int& val) {
    func_in_18770(val);
    const int x = 18770;
    std::cout << 18770;
    for (int i = 0; i < 18770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18770(int& val) {
    const int x = 18770;
    std::cout << 18770;
    for (int i = 0; i < 18770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18771(int& val) {
    func_in_18771(val);
    const int x = 18771;
    std::cout << 18771;
    for (int i = 0; i < 18771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18771(int& val) {
    const int x = 18771;
    std::cout << 18771;
    for (int i = 0; i < 18771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18772(int& val) {
    func_in_18772(val);
    const int x = 18772;
    std::cout << 18772;
    for (int i = 0; i < 18772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18772(int& val) {
    const int x = 18772;
    std::cout << 18772;
    for (int i = 0; i < 18772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18773(int& val) {
    func_in_18773(val);
    const int x = 18773;
    std::cout << 18773;
    for (int i = 0; i < 18773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18773(int& val) {
    const int x = 18773;
    std::cout << 18773;
    for (int i = 0; i < 18773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18774(int& val) {
    func_in_18774(val);
    const int x = 18774;
    std::cout << 18774;
    for (int i = 0; i < 18774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18774(int& val) {
    const int x = 18774;
    std::cout << 18774;
    for (int i = 0; i < 18774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18775(int& val) {
    func_in_18775(val);
    const int x = 18775;
    std::cout << 18775;
    for (int i = 0; i < 18775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18775(int& val) {
    const int x = 18775;
    std::cout << 18775;
    for (int i = 0; i < 18775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18776(int& val) {
    func_in_18776(val);
    const int x = 18776;
    std::cout << 18776;
    for (int i = 0; i < 18776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18776(int& val) {
    const int x = 18776;
    std::cout << 18776;
    for (int i = 0; i < 18776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18777(int& val) {
    func_in_18777(val);
    const int x = 18777;
    std::cout << 18777;
    for (int i = 0; i < 18777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18777(int& val) {
    const int x = 18777;
    std::cout << 18777;
    for (int i = 0; i < 18777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18778(int& val) {
    func_in_18778(val);
    const int x = 18778;
    std::cout << 18778;
    for (int i = 0; i < 18778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18778(int& val) {
    const int x = 18778;
    std::cout << 18778;
    for (int i = 0; i < 18778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18779(int& val) {
    func_in_18779(val);
    const int x = 18779;
    std::cout << 18779;
    for (int i = 0; i < 18779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18779(int& val) {
    const int x = 18779;
    std::cout << 18779;
    for (int i = 0; i < 18779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18780(int& val) {
    func_in_18780(val);
    const int x = 18780;
    std::cout << 18780;
    for (int i = 0; i < 18780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18780(int& val) {
    const int x = 18780;
    std::cout << 18780;
    for (int i = 0; i < 18780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18781(int& val) {
    func_in_18781(val);
    const int x = 18781;
    std::cout << 18781;
    for (int i = 0; i < 18781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18781(int& val) {
    const int x = 18781;
    std::cout << 18781;
    for (int i = 0; i < 18781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18782(int& val) {
    func_in_18782(val);
    const int x = 18782;
    std::cout << 18782;
    for (int i = 0; i < 18782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18782(int& val) {
    const int x = 18782;
    std::cout << 18782;
    for (int i = 0; i < 18782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18783(int& val) {
    func_in_18783(val);
    const int x = 18783;
    std::cout << 18783;
    for (int i = 0; i < 18783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18783(int& val) {
    const int x = 18783;
    std::cout << 18783;
    for (int i = 0; i < 18783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18784(int& val) {
    func_in_18784(val);
    const int x = 18784;
    std::cout << 18784;
    for (int i = 0; i < 18784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18784(int& val) {
    const int x = 18784;
    std::cout << 18784;
    for (int i = 0; i < 18784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18785(int& val) {
    func_in_18785(val);
    const int x = 18785;
    std::cout << 18785;
    for (int i = 0; i < 18785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18785(int& val) {
    const int x = 18785;
    std::cout << 18785;
    for (int i = 0; i < 18785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18786(int& val) {
    func_in_18786(val);
    const int x = 18786;
    std::cout << 18786;
    for (int i = 0; i < 18786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18786(int& val) {
    const int x = 18786;
    std::cout << 18786;
    for (int i = 0; i < 18786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18787(int& val) {
    func_in_18787(val);
    const int x = 18787;
    std::cout << 18787;
    for (int i = 0; i < 18787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18787(int& val) {
    const int x = 18787;
    std::cout << 18787;
    for (int i = 0; i < 18787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18788(int& val) {
    func_in_18788(val);
    const int x = 18788;
    std::cout << 18788;
    for (int i = 0; i < 18788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18788(int& val) {
    const int x = 18788;
    std::cout << 18788;
    for (int i = 0; i < 18788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18789(int& val) {
    func_in_18789(val);
    const int x = 18789;
    std::cout << 18789;
    for (int i = 0; i < 18789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18789(int& val) {
    const int x = 18789;
    std::cout << 18789;
    for (int i = 0; i < 18789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18790(int& val) {
    func_in_18790(val);
    const int x = 18790;
    std::cout << 18790;
    for (int i = 0; i < 18790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18790(int& val) {
    const int x = 18790;
    std::cout << 18790;
    for (int i = 0; i < 18790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18791(int& val) {
    func_in_18791(val);
    const int x = 18791;
    std::cout << 18791;
    for (int i = 0; i < 18791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18791(int& val) {
    const int x = 18791;
    std::cout << 18791;
    for (int i = 0; i < 18791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18792(int& val) {
    func_in_18792(val);
    const int x = 18792;
    std::cout << 18792;
    for (int i = 0; i < 18792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18792(int& val) {
    const int x = 18792;
    std::cout << 18792;
    for (int i = 0; i < 18792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18793(int& val) {
    func_in_18793(val);
    const int x = 18793;
    std::cout << 18793;
    for (int i = 0; i < 18793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18793(int& val) {
    const int x = 18793;
    std::cout << 18793;
    for (int i = 0; i < 18793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18794(int& val) {
    func_in_18794(val);
    const int x = 18794;
    std::cout << 18794;
    for (int i = 0; i < 18794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18794(int& val) {
    const int x = 18794;
    std::cout << 18794;
    for (int i = 0; i < 18794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18795(int& val) {
    func_in_18795(val);
    const int x = 18795;
    std::cout << 18795;
    for (int i = 0; i < 18795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18795(int& val) {
    const int x = 18795;
    std::cout << 18795;
    for (int i = 0; i < 18795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18796(int& val) {
    func_in_18796(val);
    const int x = 18796;
    std::cout << 18796;
    for (int i = 0; i < 18796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18796(int& val) {
    const int x = 18796;
    std::cout << 18796;
    for (int i = 0; i < 18796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18797(int& val) {
    func_in_18797(val);
    const int x = 18797;
    std::cout << 18797;
    for (int i = 0; i < 18797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18797(int& val) {
    const int x = 18797;
    std::cout << 18797;
    for (int i = 0; i < 18797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18798(int& val) {
    func_in_18798(val);
    const int x = 18798;
    std::cout << 18798;
    for (int i = 0; i < 18798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18798(int& val) {
    const int x = 18798;
    std::cout << 18798;
    for (int i = 0; i < 18798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18799(int& val) {
    func_in_18799(val);
    const int x = 18799;
    std::cout << 18799;
    for (int i = 0; i < 18799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18799(int& val) {
    const int x = 18799;
    std::cout << 18799;
    for (int i = 0; i < 18799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18800(int& val) {
    func_in_18800(val);
    const int x = 18800;
    std::cout << 18800;
    for (int i = 0; i < 18800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18800(int& val) {
    const int x = 18800;
    std::cout << 18800;
    for (int i = 0; i < 18800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18801(int& val) {
    func_in_18801(val);
    const int x = 18801;
    std::cout << 18801;
    for (int i = 0; i < 18801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18801(int& val) {
    const int x = 18801;
    std::cout << 18801;
    for (int i = 0; i < 18801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18802(int& val) {
    func_in_18802(val);
    const int x = 18802;
    std::cout << 18802;
    for (int i = 0; i < 18802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18802(int& val) {
    const int x = 18802;
    std::cout << 18802;
    for (int i = 0; i < 18802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18803(int& val) {
    func_in_18803(val);
    const int x = 18803;
    std::cout << 18803;
    for (int i = 0; i < 18803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18803(int& val) {
    const int x = 18803;
    std::cout << 18803;
    for (int i = 0; i < 18803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18804(int& val) {
    func_in_18804(val);
    const int x = 18804;
    std::cout << 18804;
    for (int i = 0; i < 18804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18804(int& val) {
    const int x = 18804;
    std::cout << 18804;
    for (int i = 0; i < 18804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18805(int& val) {
    func_in_18805(val);
    const int x = 18805;
    std::cout << 18805;
    for (int i = 0; i < 18805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18805(int& val) {
    const int x = 18805;
    std::cout << 18805;
    for (int i = 0; i < 18805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18806(int& val) {
    func_in_18806(val);
    const int x = 18806;
    std::cout << 18806;
    for (int i = 0; i < 18806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18806(int& val) {
    const int x = 18806;
    std::cout << 18806;
    for (int i = 0; i < 18806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18807(int& val) {
    func_in_18807(val);
    const int x = 18807;
    std::cout << 18807;
    for (int i = 0; i < 18807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18807(int& val) {
    const int x = 18807;
    std::cout << 18807;
    for (int i = 0; i < 18807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18808(int& val) {
    func_in_18808(val);
    const int x = 18808;
    std::cout << 18808;
    for (int i = 0; i < 18808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18808(int& val) {
    const int x = 18808;
    std::cout << 18808;
    for (int i = 0; i < 18808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18809(int& val) {
    func_in_18809(val);
    const int x = 18809;
    std::cout << 18809;
    for (int i = 0; i < 18809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18809(int& val) {
    const int x = 18809;
    std::cout << 18809;
    for (int i = 0; i < 18809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18810(int& val) {
    func_in_18810(val);
    const int x = 18810;
    std::cout << 18810;
    for (int i = 0; i < 18810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18810(int& val) {
    const int x = 18810;
    std::cout << 18810;
    for (int i = 0; i < 18810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18811(int& val) {
    func_in_18811(val);
    const int x = 18811;
    std::cout << 18811;
    for (int i = 0; i < 18811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18811(int& val) {
    const int x = 18811;
    std::cout << 18811;
    for (int i = 0; i < 18811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18812(int& val) {
    func_in_18812(val);
    const int x = 18812;
    std::cout << 18812;
    for (int i = 0; i < 18812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18812(int& val) {
    const int x = 18812;
    std::cout << 18812;
    for (int i = 0; i < 18812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18813(int& val) {
    func_in_18813(val);
    const int x = 18813;
    std::cout << 18813;
    for (int i = 0; i < 18813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18813(int& val) {
    const int x = 18813;
    std::cout << 18813;
    for (int i = 0; i < 18813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18814(int& val) {
    func_in_18814(val);
    const int x = 18814;
    std::cout << 18814;
    for (int i = 0; i < 18814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18814(int& val) {
    const int x = 18814;
    std::cout << 18814;
    for (int i = 0; i < 18814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18815(int& val) {
    func_in_18815(val);
    const int x = 18815;
    std::cout << 18815;
    for (int i = 0; i < 18815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18815(int& val) {
    const int x = 18815;
    std::cout << 18815;
    for (int i = 0; i < 18815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18816(int& val) {
    func_in_18816(val);
    const int x = 18816;
    std::cout << 18816;
    for (int i = 0; i < 18816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18816(int& val) {
    const int x = 18816;
    std::cout << 18816;
    for (int i = 0; i < 18816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18817(int& val) {
    func_in_18817(val);
    const int x = 18817;
    std::cout << 18817;
    for (int i = 0; i < 18817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18817(int& val) {
    const int x = 18817;
    std::cout << 18817;
    for (int i = 0; i < 18817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18818(int& val) {
    func_in_18818(val);
    const int x = 18818;
    std::cout << 18818;
    for (int i = 0; i < 18818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18818(int& val) {
    const int x = 18818;
    std::cout << 18818;
    for (int i = 0; i < 18818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18819(int& val) {
    func_in_18819(val);
    const int x = 18819;
    std::cout << 18819;
    for (int i = 0; i < 18819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18819(int& val) {
    const int x = 18819;
    std::cout << 18819;
    for (int i = 0; i < 18819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18820(int& val) {
    func_in_18820(val);
    const int x = 18820;
    std::cout << 18820;
    for (int i = 0; i < 18820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18820(int& val) {
    const int x = 18820;
    std::cout << 18820;
    for (int i = 0; i < 18820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18821(int& val) {
    func_in_18821(val);
    const int x = 18821;
    std::cout << 18821;
    for (int i = 0; i < 18821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18821(int& val) {
    const int x = 18821;
    std::cout << 18821;
    for (int i = 0; i < 18821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18822(int& val) {
    func_in_18822(val);
    const int x = 18822;
    std::cout << 18822;
    for (int i = 0; i < 18822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18822(int& val) {
    const int x = 18822;
    std::cout << 18822;
    for (int i = 0; i < 18822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18823(int& val) {
    func_in_18823(val);
    const int x = 18823;
    std::cout << 18823;
    for (int i = 0; i < 18823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18823(int& val) {
    const int x = 18823;
    std::cout << 18823;
    for (int i = 0; i < 18823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18824(int& val) {
    func_in_18824(val);
    const int x = 18824;
    std::cout << 18824;
    for (int i = 0; i < 18824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18824(int& val) {
    const int x = 18824;
    std::cout << 18824;
    for (int i = 0; i < 18824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18825(int& val) {
    func_in_18825(val);
    const int x = 18825;
    std::cout << 18825;
    for (int i = 0; i < 18825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18825(int& val) {
    const int x = 18825;
    std::cout << 18825;
    for (int i = 0; i < 18825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18826(int& val) {
    func_in_18826(val);
    const int x = 18826;
    std::cout << 18826;
    for (int i = 0; i < 18826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18826(int& val) {
    const int x = 18826;
    std::cout << 18826;
    for (int i = 0; i < 18826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18827(int& val) {
    func_in_18827(val);
    const int x = 18827;
    std::cout << 18827;
    for (int i = 0; i < 18827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18827(int& val) {
    const int x = 18827;
    std::cout << 18827;
    for (int i = 0; i < 18827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18828(int& val) {
    func_in_18828(val);
    const int x = 18828;
    std::cout << 18828;
    for (int i = 0; i < 18828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18828(int& val) {
    const int x = 18828;
    std::cout << 18828;
    for (int i = 0; i < 18828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18829(int& val) {
    func_in_18829(val);
    const int x = 18829;
    std::cout << 18829;
    for (int i = 0; i < 18829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18829(int& val) {
    const int x = 18829;
    std::cout << 18829;
    for (int i = 0; i < 18829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18830(int& val) {
    func_in_18830(val);
    const int x = 18830;
    std::cout << 18830;
    for (int i = 0; i < 18830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18830(int& val) {
    const int x = 18830;
    std::cout << 18830;
    for (int i = 0; i < 18830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18831(int& val) {
    func_in_18831(val);
    const int x = 18831;
    std::cout << 18831;
    for (int i = 0; i < 18831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18831(int& val) {
    const int x = 18831;
    std::cout << 18831;
    for (int i = 0; i < 18831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18832(int& val) {
    func_in_18832(val);
    const int x = 18832;
    std::cout << 18832;
    for (int i = 0; i < 18832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18832(int& val) {
    const int x = 18832;
    std::cout << 18832;
    for (int i = 0; i < 18832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18833(int& val) {
    func_in_18833(val);
    const int x = 18833;
    std::cout << 18833;
    for (int i = 0; i < 18833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18833(int& val) {
    const int x = 18833;
    std::cout << 18833;
    for (int i = 0; i < 18833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18834(int& val) {
    func_in_18834(val);
    const int x = 18834;
    std::cout << 18834;
    for (int i = 0; i < 18834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18834(int& val) {
    const int x = 18834;
    std::cout << 18834;
    for (int i = 0; i < 18834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18835(int& val) {
    func_in_18835(val);
    const int x = 18835;
    std::cout << 18835;
    for (int i = 0; i < 18835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18835(int& val) {
    const int x = 18835;
    std::cout << 18835;
    for (int i = 0; i < 18835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18836(int& val) {
    func_in_18836(val);
    const int x = 18836;
    std::cout << 18836;
    for (int i = 0; i < 18836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18836(int& val) {
    const int x = 18836;
    std::cout << 18836;
    for (int i = 0; i < 18836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18837(int& val) {
    func_in_18837(val);
    const int x = 18837;
    std::cout << 18837;
    for (int i = 0; i < 18837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18837(int& val) {
    const int x = 18837;
    std::cout << 18837;
    for (int i = 0; i < 18837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18838(int& val) {
    func_in_18838(val);
    const int x = 18838;
    std::cout << 18838;
    for (int i = 0; i < 18838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18838(int& val) {
    const int x = 18838;
    std::cout << 18838;
    for (int i = 0; i < 18838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18839(int& val) {
    func_in_18839(val);
    const int x = 18839;
    std::cout << 18839;
    for (int i = 0; i < 18839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18839(int& val) {
    const int x = 18839;
    std::cout << 18839;
    for (int i = 0; i < 18839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18840(int& val) {
    func_in_18840(val);
    const int x = 18840;
    std::cout << 18840;
    for (int i = 0; i < 18840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18840(int& val) {
    const int x = 18840;
    std::cout << 18840;
    for (int i = 0; i < 18840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18841(int& val) {
    func_in_18841(val);
    const int x = 18841;
    std::cout << 18841;
    for (int i = 0; i < 18841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18841(int& val) {
    const int x = 18841;
    std::cout << 18841;
    for (int i = 0; i < 18841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18842(int& val) {
    func_in_18842(val);
    const int x = 18842;
    std::cout << 18842;
    for (int i = 0; i < 18842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18842(int& val) {
    const int x = 18842;
    std::cout << 18842;
    for (int i = 0; i < 18842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18843(int& val) {
    func_in_18843(val);
    const int x = 18843;
    std::cout << 18843;
    for (int i = 0; i < 18843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18843(int& val) {
    const int x = 18843;
    std::cout << 18843;
    for (int i = 0; i < 18843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18844(int& val) {
    func_in_18844(val);
    const int x = 18844;
    std::cout << 18844;
    for (int i = 0; i < 18844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18844(int& val) {
    const int x = 18844;
    std::cout << 18844;
    for (int i = 0; i < 18844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18845(int& val) {
    func_in_18845(val);
    const int x = 18845;
    std::cout << 18845;
    for (int i = 0; i < 18845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18845(int& val) {
    const int x = 18845;
    std::cout << 18845;
    for (int i = 0; i < 18845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18846(int& val) {
    func_in_18846(val);
    const int x = 18846;
    std::cout << 18846;
    for (int i = 0; i < 18846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18846(int& val) {
    const int x = 18846;
    std::cout << 18846;
    for (int i = 0; i < 18846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18847(int& val) {
    func_in_18847(val);
    const int x = 18847;
    std::cout << 18847;
    for (int i = 0; i < 18847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18847(int& val) {
    const int x = 18847;
    std::cout << 18847;
    for (int i = 0; i < 18847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18848(int& val) {
    func_in_18848(val);
    const int x = 18848;
    std::cout << 18848;
    for (int i = 0; i < 18848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18848(int& val) {
    const int x = 18848;
    std::cout << 18848;
    for (int i = 0; i < 18848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18849(int& val) {
    func_in_18849(val);
    const int x = 18849;
    std::cout << 18849;
    for (int i = 0; i < 18849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18849(int& val) {
    const int x = 18849;
    std::cout << 18849;
    for (int i = 0; i < 18849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18850(int& val) {
    func_in_18850(val);
    const int x = 18850;
    std::cout << 18850;
    for (int i = 0; i < 18850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18850(int& val) {
    const int x = 18850;
    std::cout << 18850;
    for (int i = 0; i < 18850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18851(int& val) {
    func_in_18851(val);
    const int x = 18851;
    std::cout << 18851;
    for (int i = 0; i < 18851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18851(int& val) {
    const int x = 18851;
    std::cout << 18851;
    for (int i = 0; i < 18851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18852(int& val) {
    func_in_18852(val);
    const int x = 18852;
    std::cout << 18852;
    for (int i = 0; i < 18852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18852(int& val) {
    const int x = 18852;
    std::cout << 18852;
    for (int i = 0; i < 18852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18853(int& val) {
    func_in_18853(val);
    const int x = 18853;
    std::cout << 18853;
    for (int i = 0; i < 18853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18853(int& val) {
    const int x = 18853;
    std::cout << 18853;
    for (int i = 0; i < 18853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18854(int& val) {
    func_in_18854(val);
    const int x = 18854;
    std::cout << 18854;
    for (int i = 0; i < 18854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18854(int& val) {
    const int x = 18854;
    std::cout << 18854;
    for (int i = 0; i < 18854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18855(int& val) {
    func_in_18855(val);
    const int x = 18855;
    std::cout << 18855;
    for (int i = 0; i < 18855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18855(int& val) {
    const int x = 18855;
    std::cout << 18855;
    for (int i = 0; i < 18855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18856(int& val) {
    func_in_18856(val);
    const int x = 18856;
    std::cout << 18856;
    for (int i = 0; i < 18856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18856(int& val) {
    const int x = 18856;
    std::cout << 18856;
    for (int i = 0; i < 18856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18857(int& val) {
    func_in_18857(val);
    const int x = 18857;
    std::cout << 18857;
    for (int i = 0; i < 18857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18857(int& val) {
    const int x = 18857;
    std::cout << 18857;
    for (int i = 0; i < 18857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18858(int& val) {
    func_in_18858(val);
    const int x = 18858;
    std::cout << 18858;
    for (int i = 0; i < 18858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18858(int& val) {
    const int x = 18858;
    std::cout << 18858;
    for (int i = 0; i < 18858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18859(int& val) {
    func_in_18859(val);
    const int x = 18859;
    std::cout << 18859;
    for (int i = 0; i < 18859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18859(int& val) {
    const int x = 18859;
    std::cout << 18859;
    for (int i = 0; i < 18859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18860(int& val) {
    func_in_18860(val);
    const int x = 18860;
    std::cout << 18860;
    for (int i = 0; i < 18860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18860(int& val) {
    const int x = 18860;
    std::cout << 18860;
    for (int i = 0; i < 18860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18861(int& val) {
    func_in_18861(val);
    const int x = 18861;
    std::cout << 18861;
    for (int i = 0; i < 18861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18861(int& val) {
    const int x = 18861;
    std::cout << 18861;
    for (int i = 0; i < 18861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18862(int& val) {
    func_in_18862(val);
    const int x = 18862;
    std::cout << 18862;
    for (int i = 0; i < 18862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18862(int& val) {
    const int x = 18862;
    std::cout << 18862;
    for (int i = 0; i < 18862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18863(int& val) {
    func_in_18863(val);
    const int x = 18863;
    std::cout << 18863;
    for (int i = 0; i < 18863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18863(int& val) {
    const int x = 18863;
    std::cout << 18863;
    for (int i = 0; i < 18863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18864(int& val) {
    func_in_18864(val);
    const int x = 18864;
    std::cout << 18864;
    for (int i = 0; i < 18864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18864(int& val) {
    const int x = 18864;
    std::cout << 18864;
    for (int i = 0; i < 18864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18865(int& val) {
    func_in_18865(val);
    const int x = 18865;
    std::cout << 18865;
    for (int i = 0; i < 18865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18865(int& val) {
    const int x = 18865;
    std::cout << 18865;
    for (int i = 0; i < 18865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18866(int& val) {
    func_in_18866(val);
    const int x = 18866;
    std::cout << 18866;
    for (int i = 0; i < 18866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18866(int& val) {
    const int x = 18866;
    std::cout << 18866;
    for (int i = 0; i < 18866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18867(int& val) {
    func_in_18867(val);
    const int x = 18867;
    std::cout << 18867;
    for (int i = 0; i < 18867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18867(int& val) {
    const int x = 18867;
    std::cout << 18867;
    for (int i = 0; i < 18867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18868(int& val) {
    func_in_18868(val);
    const int x = 18868;
    std::cout << 18868;
    for (int i = 0; i < 18868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18868(int& val) {
    const int x = 18868;
    std::cout << 18868;
    for (int i = 0; i < 18868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18869(int& val) {
    func_in_18869(val);
    const int x = 18869;
    std::cout << 18869;
    for (int i = 0; i < 18869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18869(int& val) {
    const int x = 18869;
    std::cout << 18869;
    for (int i = 0; i < 18869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18870(int& val) {
    func_in_18870(val);
    const int x = 18870;
    std::cout << 18870;
    for (int i = 0; i < 18870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18870(int& val) {
    const int x = 18870;
    std::cout << 18870;
    for (int i = 0; i < 18870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18871(int& val) {
    func_in_18871(val);
    const int x = 18871;
    std::cout << 18871;
    for (int i = 0; i < 18871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18871(int& val) {
    const int x = 18871;
    std::cout << 18871;
    for (int i = 0; i < 18871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18872(int& val) {
    func_in_18872(val);
    const int x = 18872;
    std::cout << 18872;
    for (int i = 0; i < 18872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18872(int& val) {
    const int x = 18872;
    std::cout << 18872;
    for (int i = 0; i < 18872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18873(int& val) {
    func_in_18873(val);
    const int x = 18873;
    std::cout << 18873;
    for (int i = 0; i < 18873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18873(int& val) {
    const int x = 18873;
    std::cout << 18873;
    for (int i = 0; i < 18873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18874(int& val) {
    func_in_18874(val);
    const int x = 18874;
    std::cout << 18874;
    for (int i = 0; i < 18874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18874(int& val) {
    const int x = 18874;
    std::cout << 18874;
    for (int i = 0; i < 18874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18875(int& val) {
    func_in_18875(val);
    const int x = 18875;
    std::cout << 18875;
    for (int i = 0; i < 18875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18875(int& val) {
    const int x = 18875;
    std::cout << 18875;
    for (int i = 0; i < 18875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18876(int& val) {
    func_in_18876(val);
    const int x = 18876;
    std::cout << 18876;
    for (int i = 0; i < 18876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18876(int& val) {
    const int x = 18876;
    std::cout << 18876;
    for (int i = 0; i < 18876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18877(int& val) {
    func_in_18877(val);
    const int x = 18877;
    std::cout << 18877;
    for (int i = 0; i < 18877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18877(int& val) {
    const int x = 18877;
    std::cout << 18877;
    for (int i = 0; i < 18877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18878(int& val) {
    func_in_18878(val);
    const int x = 18878;
    std::cout << 18878;
    for (int i = 0; i < 18878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18878(int& val) {
    const int x = 18878;
    std::cout << 18878;
    for (int i = 0; i < 18878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18879(int& val) {
    func_in_18879(val);
    const int x = 18879;
    std::cout << 18879;
    for (int i = 0; i < 18879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18879(int& val) {
    const int x = 18879;
    std::cout << 18879;
    for (int i = 0; i < 18879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18880(int& val) {
    func_in_18880(val);
    const int x = 18880;
    std::cout << 18880;
    for (int i = 0; i < 18880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18880(int& val) {
    const int x = 18880;
    std::cout << 18880;
    for (int i = 0; i < 18880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18881(int& val) {
    func_in_18881(val);
    const int x = 18881;
    std::cout << 18881;
    for (int i = 0; i < 18881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18881(int& val) {
    const int x = 18881;
    std::cout << 18881;
    for (int i = 0; i < 18881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18882(int& val) {
    func_in_18882(val);
    const int x = 18882;
    std::cout << 18882;
    for (int i = 0; i < 18882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18882(int& val) {
    const int x = 18882;
    std::cout << 18882;
    for (int i = 0; i < 18882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18883(int& val) {
    func_in_18883(val);
    const int x = 18883;
    std::cout << 18883;
    for (int i = 0; i < 18883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18883(int& val) {
    const int x = 18883;
    std::cout << 18883;
    for (int i = 0; i < 18883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18884(int& val) {
    func_in_18884(val);
    const int x = 18884;
    std::cout << 18884;
    for (int i = 0; i < 18884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18884(int& val) {
    const int x = 18884;
    std::cout << 18884;
    for (int i = 0; i < 18884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18885(int& val) {
    func_in_18885(val);
    const int x = 18885;
    std::cout << 18885;
    for (int i = 0; i < 18885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18885(int& val) {
    const int x = 18885;
    std::cout << 18885;
    for (int i = 0; i < 18885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18886(int& val) {
    func_in_18886(val);
    const int x = 18886;
    std::cout << 18886;
    for (int i = 0; i < 18886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18886(int& val) {
    const int x = 18886;
    std::cout << 18886;
    for (int i = 0; i < 18886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18887(int& val) {
    func_in_18887(val);
    const int x = 18887;
    std::cout << 18887;
    for (int i = 0; i < 18887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18887(int& val) {
    const int x = 18887;
    std::cout << 18887;
    for (int i = 0; i < 18887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18888(int& val) {
    func_in_18888(val);
    const int x = 18888;
    std::cout << 18888;
    for (int i = 0; i < 18888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18888(int& val) {
    const int x = 18888;
    std::cout << 18888;
    for (int i = 0; i < 18888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18889(int& val) {
    func_in_18889(val);
    const int x = 18889;
    std::cout << 18889;
    for (int i = 0; i < 18889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18889(int& val) {
    const int x = 18889;
    std::cout << 18889;
    for (int i = 0; i < 18889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18890(int& val) {
    func_in_18890(val);
    const int x = 18890;
    std::cout << 18890;
    for (int i = 0; i < 18890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18890(int& val) {
    const int x = 18890;
    std::cout << 18890;
    for (int i = 0; i < 18890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18891(int& val) {
    func_in_18891(val);
    const int x = 18891;
    std::cout << 18891;
    for (int i = 0; i < 18891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18891(int& val) {
    const int x = 18891;
    std::cout << 18891;
    for (int i = 0; i < 18891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18892(int& val) {
    func_in_18892(val);
    const int x = 18892;
    std::cout << 18892;
    for (int i = 0; i < 18892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18892(int& val) {
    const int x = 18892;
    std::cout << 18892;
    for (int i = 0; i < 18892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18893(int& val) {
    func_in_18893(val);
    const int x = 18893;
    std::cout << 18893;
    for (int i = 0; i < 18893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18893(int& val) {
    const int x = 18893;
    std::cout << 18893;
    for (int i = 0; i < 18893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18894(int& val) {
    func_in_18894(val);
    const int x = 18894;
    std::cout << 18894;
    for (int i = 0; i < 18894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18894(int& val) {
    const int x = 18894;
    std::cout << 18894;
    for (int i = 0; i < 18894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18895(int& val) {
    func_in_18895(val);
    const int x = 18895;
    std::cout << 18895;
    for (int i = 0; i < 18895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18895(int& val) {
    const int x = 18895;
    std::cout << 18895;
    for (int i = 0; i < 18895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18896(int& val) {
    func_in_18896(val);
    const int x = 18896;
    std::cout << 18896;
    for (int i = 0; i < 18896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18896(int& val) {
    const int x = 18896;
    std::cout << 18896;
    for (int i = 0; i < 18896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18897(int& val) {
    func_in_18897(val);
    const int x = 18897;
    std::cout << 18897;
    for (int i = 0; i < 18897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18897(int& val) {
    const int x = 18897;
    std::cout << 18897;
    for (int i = 0; i < 18897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18898(int& val) {
    func_in_18898(val);
    const int x = 18898;
    std::cout << 18898;
    for (int i = 0; i < 18898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18898(int& val) {
    const int x = 18898;
    std::cout << 18898;
    for (int i = 0; i < 18898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18899(int& val) {
    func_in_18899(val);
    const int x = 18899;
    std::cout << 18899;
    for (int i = 0; i < 18899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18899(int& val) {
    const int x = 18899;
    std::cout << 18899;
    for (int i = 0; i < 18899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18900(int& val) {
    func_in_18900(val);
    const int x = 18900;
    std::cout << 18900;
    for (int i = 0; i < 18900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18900(int& val) {
    const int x = 18900;
    std::cout << 18900;
    for (int i = 0; i < 18900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18901(int& val) {
    func_in_18901(val);
    const int x = 18901;
    std::cout << 18901;
    for (int i = 0; i < 18901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18901(int& val) {
    const int x = 18901;
    std::cout << 18901;
    for (int i = 0; i < 18901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18902(int& val) {
    func_in_18902(val);
    const int x = 18902;
    std::cout << 18902;
    for (int i = 0; i < 18902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18902(int& val) {
    const int x = 18902;
    std::cout << 18902;
    for (int i = 0; i < 18902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18903(int& val) {
    func_in_18903(val);
    const int x = 18903;
    std::cout << 18903;
    for (int i = 0; i < 18903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18903(int& val) {
    const int x = 18903;
    std::cout << 18903;
    for (int i = 0; i < 18903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18904(int& val) {
    func_in_18904(val);
    const int x = 18904;
    std::cout << 18904;
    for (int i = 0; i < 18904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18904(int& val) {
    const int x = 18904;
    std::cout << 18904;
    for (int i = 0; i < 18904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18905(int& val) {
    func_in_18905(val);
    const int x = 18905;
    std::cout << 18905;
    for (int i = 0; i < 18905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18905(int& val) {
    const int x = 18905;
    std::cout << 18905;
    for (int i = 0; i < 18905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18906(int& val) {
    func_in_18906(val);
    const int x = 18906;
    std::cout << 18906;
    for (int i = 0; i < 18906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18906(int& val) {
    const int x = 18906;
    std::cout << 18906;
    for (int i = 0; i < 18906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18907(int& val) {
    func_in_18907(val);
    const int x = 18907;
    std::cout << 18907;
    for (int i = 0; i < 18907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18907(int& val) {
    const int x = 18907;
    std::cout << 18907;
    for (int i = 0; i < 18907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18908(int& val) {
    func_in_18908(val);
    const int x = 18908;
    std::cout << 18908;
    for (int i = 0; i < 18908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18908(int& val) {
    const int x = 18908;
    std::cout << 18908;
    for (int i = 0; i < 18908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18909(int& val) {
    func_in_18909(val);
    const int x = 18909;
    std::cout << 18909;
    for (int i = 0; i < 18909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18909(int& val) {
    const int x = 18909;
    std::cout << 18909;
    for (int i = 0; i < 18909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18910(int& val) {
    func_in_18910(val);
    const int x = 18910;
    std::cout << 18910;
    for (int i = 0; i < 18910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18910(int& val) {
    const int x = 18910;
    std::cout << 18910;
    for (int i = 0; i < 18910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18911(int& val) {
    func_in_18911(val);
    const int x = 18911;
    std::cout << 18911;
    for (int i = 0; i < 18911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18911(int& val) {
    const int x = 18911;
    std::cout << 18911;
    for (int i = 0; i < 18911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18912(int& val) {
    func_in_18912(val);
    const int x = 18912;
    std::cout << 18912;
    for (int i = 0; i < 18912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18912(int& val) {
    const int x = 18912;
    std::cout << 18912;
    for (int i = 0; i < 18912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18913(int& val) {
    func_in_18913(val);
    const int x = 18913;
    std::cout << 18913;
    for (int i = 0; i < 18913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18913(int& val) {
    const int x = 18913;
    std::cout << 18913;
    for (int i = 0; i < 18913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18914(int& val) {
    func_in_18914(val);
    const int x = 18914;
    std::cout << 18914;
    for (int i = 0; i < 18914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18914(int& val) {
    const int x = 18914;
    std::cout << 18914;
    for (int i = 0; i < 18914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18915(int& val) {
    func_in_18915(val);
    const int x = 18915;
    std::cout << 18915;
    for (int i = 0; i < 18915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18915(int& val) {
    const int x = 18915;
    std::cout << 18915;
    for (int i = 0; i < 18915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18916(int& val) {
    func_in_18916(val);
    const int x = 18916;
    std::cout << 18916;
    for (int i = 0; i < 18916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18916(int& val) {
    const int x = 18916;
    std::cout << 18916;
    for (int i = 0; i < 18916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18917(int& val) {
    func_in_18917(val);
    const int x = 18917;
    std::cout << 18917;
    for (int i = 0; i < 18917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18917(int& val) {
    const int x = 18917;
    std::cout << 18917;
    for (int i = 0; i < 18917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18918(int& val) {
    func_in_18918(val);
    const int x = 18918;
    std::cout << 18918;
    for (int i = 0; i < 18918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18918(int& val) {
    const int x = 18918;
    std::cout << 18918;
    for (int i = 0; i < 18918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18919(int& val) {
    func_in_18919(val);
    const int x = 18919;
    std::cout << 18919;
    for (int i = 0; i < 18919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18919(int& val) {
    const int x = 18919;
    std::cout << 18919;
    for (int i = 0; i < 18919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18920(int& val) {
    func_in_18920(val);
    const int x = 18920;
    std::cout << 18920;
    for (int i = 0; i < 18920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18920(int& val) {
    const int x = 18920;
    std::cout << 18920;
    for (int i = 0; i < 18920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18921(int& val) {
    func_in_18921(val);
    const int x = 18921;
    std::cout << 18921;
    for (int i = 0; i < 18921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18921(int& val) {
    const int x = 18921;
    std::cout << 18921;
    for (int i = 0; i < 18921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18922(int& val) {
    func_in_18922(val);
    const int x = 18922;
    std::cout << 18922;
    for (int i = 0; i < 18922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18922(int& val) {
    const int x = 18922;
    std::cout << 18922;
    for (int i = 0; i < 18922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18923(int& val) {
    func_in_18923(val);
    const int x = 18923;
    std::cout << 18923;
    for (int i = 0; i < 18923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18923(int& val) {
    const int x = 18923;
    std::cout << 18923;
    for (int i = 0; i < 18923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18924(int& val) {
    func_in_18924(val);
    const int x = 18924;
    std::cout << 18924;
    for (int i = 0; i < 18924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18924(int& val) {
    const int x = 18924;
    std::cout << 18924;
    for (int i = 0; i < 18924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18925(int& val) {
    func_in_18925(val);
    const int x = 18925;
    std::cout << 18925;
    for (int i = 0; i < 18925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18925(int& val) {
    const int x = 18925;
    std::cout << 18925;
    for (int i = 0; i < 18925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18926(int& val) {
    func_in_18926(val);
    const int x = 18926;
    std::cout << 18926;
    for (int i = 0; i < 18926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18926(int& val) {
    const int x = 18926;
    std::cout << 18926;
    for (int i = 0; i < 18926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18927(int& val) {
    func_in_18927(val);
    const int x = 18927;
    std::cout << 18927;
    for (int i = 0; i < 18927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18927(int& val) {
    const int x = 18927;
    std::cout << 18927;
    for (int i = 0; i < 18927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18928(int& val) {
    func_in_18928(val);
    const int x = 18928;
    std::cout << 18928;
    for (int i = 0; i < 18928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18928(int& val) {
    const int x = 18928;
    std::cout << 18928;
    for (int i = 0; i < 18928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18929(int& val) {
    func_in_18929(val);
    const int x = 18929;
    std::cout << 18929;
    for (int i = 0; i < 18929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18929(int& val) {
    const int x = 18929;
    std::cout << 18929;
    for (int i = 0; i < 18929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18930(int& val) {
    func_in_18930(val);
    const int x = 18930;
    std::cout << 18930;
    for (int i = 0; i < 18930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18930(int& val) {
    const int x = 18930;
    std::cout << 18930;
    for (int i = 0; i < 18930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18931(int& val) {
    func_in_18931(val);
    const int x = 18931;
    std::cout << 18931;
    for (int i = 0; i < 18931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18931(int& val) {
    const int x = 18931;
    std::cout << 18931;
    for (int i = 0; i < 18931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18932(int& val) {
    func_in_18932(val);
    const int x = 18932;
    std::cout << 18932;
    for (int i = 0; i < 18932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18932(int& val) {
    const int x = 18932;
    std::cout << 18932;
    for (int i = 0; i < 18932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18933(int& val) {
    func_in_18933(val);
    const int x = 18933;
    std::cout << 18933;
    for (int i = 0; i < 18933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18933(int& val) {
    const int x = 18933;
    std::cout << 18933;
    for (int i = 0; i < 18933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18934(int& val) {
    func_in_18934(val);
    const int x = 18934;
    std::cout << 18934;
    for (int i = 0; i < 18934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18934(int& val) {
    const int x = 18934;
    std::cout << 18934;
    for (int i = 0; i < 18934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18935(int& val) {
    func_in_18935(val);
    const int x = 18935;
    std::cout << 18935;
    for (int i = 0; i < 18935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18935(int& val) {
    const int x = 18935;
    std::cout << 18935;
    for (int i = 0; i < 18935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18936(int& val) {
    func_in_18936(val);
    const int x = 18936;
    std::cout << 18936;
    for (int i = 0; i < 18936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18936(int& val) {
    const int x = 18936;
    std::cout << 18936;
    for (int i = 0; i < 18936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18937(int& val) {
    func_in_18937(val);
    const int x = 18937;
    std::cout << 18937;
    for (int i = 0; i < 18937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18937(int& val) {
    const int x = 18937;
    std::cout << 18937;
    for (int i = 0; i < 18937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18938(int& val) {
    func_in_18938(val);
    const int x = 18938;
    std::cout << 18938;
    for (int i = 0; i < 18938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18938(int& val) {
    const int x = 18938;
    std::cout << 18938;
    for (int i = 0; i < 18938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18939(int& val) {
    func_in_18939(val);
    const int x = 18939;
    std::cout << 18939;
    for (int i = 0; i < 18939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18939(int& val) {
    const int x = 18939;
    std::cout << 18939;
    for (int i = 0; i < 18939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18940(int& val) {
    func_in_18940(val);
    const int x = 18940;
    std::cout << 18940;
    for (int i = 0; i < 18940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18940(int& val) {
    const int x = 18940;
    std::cout << 18940;
    for (int i = 0; i < 18940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18941(int& val) {
    func_in_18941(val);
    const int x = 18941;
    std::cout << 18941;
    for (int i = 0; i < 18941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18941(int& val) {
    const int x = 18941;
    std::cout << 18941;
    for (int i = 0; i < 18941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18942(int& val) {
    func_in_18942(val);
    const int x = 18942;
    std::cout << 18942;
    for (int i = 0; i < 18942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18942(int& val) {
    const int x = 18942;
    std::cout << 18942;
    for (int i = 0; i < 18942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18943(int& val) {
    func_in_18943(val);
    const int x = 18943;
    std::cout << 18943;
    for (int i = 0; i < 18943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18943(int& val) {
    const int x = 18943;
    std::cout << 18943;
    for (int i = 0; i < 18943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18944(int& val) {
    func_in_18944(val);
    const int x = 18944;
    std::cout << 18944;
    for (int i = 0; i < 18944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18944(int& val) {
    const int x = 18944;
    std::cout << 18944;
    for (int i = 0; i < 18944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18945(int& val) {
    func_in_18945(val);
    const int x = 18945;
    std::cout << 18945;
    for (int i = 0; i < 18945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18945(int& val) {
    const int x = 18945;
    std::cout << 18945;
    for (int i = 0; i < 18945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18946(int& val) {
    func_in_18946(val);
    const int x = 18946;
    std::cout << 18946;
    for (int i = 0; i < 18946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18946(int& val) {
    const int x = 18946;
    std::cout << 18946;
    for (int i = 0; i < 18946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18947(int& val) {
    func_in_18947(val);
    const int x = 18947;
    std::cout << 18947;
    for (int i = 0; i < 18947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18947(int& val) {
    const int x = 18947;
    std::cout << 18947;
    for (int i = 0; i < 18947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18948(int& val) {
    func_in_18948(val);
    const int x = 18948;
    std::cout << 18948;
    for (int i = 0; i < 18948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18948(int& val) {
    const int x = 18948;
    std::cout << 18948;
    for (int i = 0; i < 18948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18949(int& val) {
    func_in_18949(val);
    const int x = 18949;
    std::cout << 18949;
    for (int i = 0; i < 18949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18949(int& val) {
    const int x = 18949;
    std::cout << 18949;
    for (int i = 0; i < 18949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18950(int& val) {
    func_in_18950(val);
    const int x = 18950;
    std::cout << 18950;
    for (int i = 0; i < 18950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18950(int& val) {
    const int x = 18950;
    std::cout << 18950;
    for (int i = 0; i < 18950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18951(int& val) {
    func_in_18951(val);
    const int x = 18951;
    std::cout << 18951;
    for (int i = 0; i < 18951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18951(int& val) {
    const int x = 18951;
    std::cout << 18951;
    for (int i = 0; i < 18951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18952(int& val) {
    func_in_18952(val);
    const int x = 18952;
    std::cout << 18952;
    for (int i = 0; i < 18952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18952(int& val) {
    const int x = 18952;
    std::cout << 18952;
    for (int i = 0; i < 18952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18953(int& val) {
    func_in_18953(val);
    const int x = 18953;
    std::cout << 18953;
    for (int i = 0; i < 18953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18953(int& val) {
    const int x = 18953;
    std::cout << 18953;
    for (int i = 0; i < 18953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18954(int& val) {
    func_in_18954(val);
    const int x = 18954;
    std::cout << 18954;
    for (int i = 0; i < 18954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18954(int& val) {
    const int x = 18954;
    std::cout << 18954;
    for (int i = 0; i < 18954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18955(int& val) {
    func_in_18955(val);
    const int x = 18955;
    std::cout << 18955;
    for (int i = 0; i < 18955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18955(int& val) {
    const int x = 18955;
    std::cout << 18955;
    for (int i = 0; i < 18955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18956(int& val) {
    func_in_18956(val);
    const int x = 18956;
    std::cout << 18956;
    for (int i = 0; i < 18956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18956(int& val) {
    const int x = 18956;
    std::cout << 18956;
    for (int i = 0; i < 18956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18957(int& val) {
    func_in_18957(val);
    const int x = 18957;
    std::cout << 18957;
    for (int i = 0; i < 18957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18957(int& val) {
    const int x = 18957;
    std::cout << 18957;
    for (int i = 0; i < 18957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18958(int& val) {
    func_in_18958(val);
    const int x = 18958;
    std::cout << 18958;
    for (int i = 0; i < 18958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18958(int& val) {
    const int x = 18958;
    std::cout << 18958;
    for (int i = 0; i < 18958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18959(int& val) {
    func_in_18959(val);
    const int x = 18959;
    std::cout << 18959;
    for (int i = 0; i < 18959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18959(int& val) {
    const int x = 18959;
    std::cout << 18959;
    for (int i = 0; i < 18959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18960(int& val) {
    func_in_18960(val);
    const int x = 18960;
    std::cout << 18960;
    for (int i = 0; i < 18960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18960(int& val) {
    const int x = 18960;
    std::cout << 18960;
    for (int i = 0; i < 18960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18961(int& val) {
    func_in_18961(val);
    const int x = 18961;
    std::cout << 18961;
    for (int i = 0; i < 18961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18961(int& val) {
    const int x = 18961;
    std::cout << 18961;
    for (int i = 0; i < 18961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18962(int& val) {
    func_in_18962(val);
    const int x = 18962;
    std::cout << 18962;
    for (int i = 0; i < 18962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18962(int& val) {
    const int x = 18962;
    std::cout << 18962;
    for (int i = 0; i < 18962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18963(int& val) {
    func_in_18963(val);
    const int x = 18963;
    std::cout << 18963;
    for (int i = 0; i < 18963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18963(int& val) {
    const int x = 18963;
    std::cout << 18963;
    for (int i = 0; i < 18963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18964(int& val) {
    func_in_18964(val);
    const int x = 18964;
    std::cout << 18964;
    for (int i = 0; i < 18964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18964(int& val) {
    const int x = 18964;
    std::cout << 18964;
    for (int i = 0; i < 18964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18965(int& val) {
    func_in_18965(val);
    const int x = 18965;
    std::cout << 18965;
    for (int i = 0; i < 18965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18965(int& val) {
    const int x = 18965;
    std::cout << 18965;
    for (int i = 0; i < 18965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18966(int& val) {
    func_in_18966(val);
    const int x = 18966;
    std::cout << 18966;
    for (int i = 0; i < 18966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18966(int& val) {
    const int x = 18966;
    std::cout << 18966;
    for (int i = 0; i < 18966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18967(int& val) {
    func_in_18967(val);
    const int x = 18967;
    std::cout << 18967;
    for (int i = 0; i < 18967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18967(int& val) {
    const int x = 18967;
    std::cout << 18967;
    for (int i = 0; i < 18967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18968(int& val) {
    func_in_18968(val);
    const int x = 18968;
    std::cout << 18968;
    for (int i = 0; i < 18968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18968(int& val) {
    const int x = 18968;
    std::cout << 18968;
    for (int i = 0; i < 18968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18969(int& val) {
    func_in_18969(val);
    const int x = 18969;
    std::cout << 18969;
    for (int i = 0; i < 18969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18969(int& val) {
    const int x = 18969;
    std::cout << 18969;
    for (int i = 0; i < 18969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18970(int& val) {
    func_in_18970(val);
    const int x = 18970;
    std::cout << 18970;
    for (int i = 0; i < 18970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18970(int& val) {
    const int x = 18970;
    std::cout << 18970;
    for (int i = 0; i < 18970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18971(int& val) {
    func_in_18971(val);
    const int x = 18971;
    std::cout << 18971;
    for (int i = 0; i < 18971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18971(int& val) {
    const int x = 18971;
    std::cout << 18971;
    for (int i = 0; i < 18971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18972(int& val) {
    func_in_18972(val);
    const int x = 18972;
    std::cout << 18972;
    for (int i = 0; i < 18972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18972(int& val) {
    const int x = 18972;
    std::cout << 18972;
    for (int i = 0; i < 18972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18973(int& val) {
    func_in_18973(val);
    const int x = 18973;
    std::cout << 18973;
    for (int i = 0; i < 18973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18973(int& val) {
    const int x = 18973;
    std::cout << 18973;
    for (int i = 0; i < 18973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18974(int& val) {
    func_in_18974(val);
    const int x = 18974;
    std::cout << 18974;
    for (int i = 0; i < 18974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18974(int& val) {
    const int x = 18974;
    std::cout << 18974;
    for (int i = 0; i < 18974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18975(int& val) {
    func_in_18975(val);
    const int x = 18975;
    std::cout << 18975;
    for (int i = 0; i < 18975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18975(int& val) {
    const int x = 18975;
    std::cout << 18975;
    for (int i = 0; i < 18975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18976(int& val) {
    func_in_18976(val);
    const int x = 18976;
    std::cout << 18976;
    for (int i = 0; i < 18976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18976(int& val) {
    const int x = 18976;
    std::cout << 18976;
    for (int i = 0; i < 18976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18977(int& val) {
    func_in_18977(val);
    const int x = 18977;
    std::cout << 18977;
    for (int i = 0; i < 18977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18977(int& val) {
    const int x = 18977;
    std::cout << 18977;
    for (int i = 0; i < 18977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18978(int& val) {
    func_in_18978(val);
    const int x = 18978;
    std::cout << 18978;
    for (int i = 0; i < 18978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18978(int& val) {
    const int x = 18978;
    std::cout << 18978;
    for (int i = 0; i < 18978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18979(int& val) {
    func_in_18979(val);
    const int x = 18979;
    std::cout << 18979;
    for (int i = 0; i < 18979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18979(int& val) {
    const int x = 18979;
    std::cout << 18979;
    for (int i = 0; i < 18979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18980(int& val) {
    func_in_18980(val);
    const int x = 18980;
    std::cout << 18980;
    for (int i = 0; i < 18980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18980(int& val) {
    const int x = 18980;
    std::cout << 18980;
    for (int i = 0; i < 18980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18981(int& val) {
    func_in_18981(val);
    const int x = 18981;
    std::cout << 18981;
    for (int i = 0; i < 18981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18981(int& val) {
    const int x = 18981;
    std::cout << 18981;
    for (int i = 0; i < 18981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18982(int& val) {
    func_in_18982(val);
    const int x = 18982;
    std::cout << 18982;
    for (int i = 0; i < 18982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18982(int& val) {
    const int x = 18982;
    std::cout << 18982;
    for (int i = 0; i < 18982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18983(int& val) {
    func_in_18983(val);
    const int x = 18983;
    std::cout << 18983;
    for (int i = 0; i < 18983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18983(int& val) {
    const int x = 18983;
    std::cout << 18983;
    for (int i = 0; i < 18983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18984(int& val) {
    func_in_18984(val);
    const int x = 18984;
    std::cout << 18984;
    for (int i = 0; i < 18984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18984(int& val) {
    const int x = 18984;
    std::cout << 18984;
    for (int i = 0; i < 18984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18985(int& val) {
    func_in_18985(val);
    const int x = 18985;
    std::cout << 18985;
    for (int i = 0; i < 18985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18985(int& val) {
    const int x = 18985;
    std::cout << 18985;
    for (int i = 0; i < 18985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18986(int& val) {
    func_in_18986(val);
    const int x = 18986;
    std::cout << 18986;
    for (int i = 0; i < 18986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18986(int& val) {
    const int x = 18986;
    std::cout << 18986;
    for (int i = 0; i < 18986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18987(int& val) {
    func_in_18987(val);
    const int x = 18987;
    std::cout << 18987;
    for (int i = 0; i < 18987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18987(int& val) {
    const int x = 18987;
    std::cout << 18987;
    for (int i = 0; i < 18987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18988(int& val) {
    func_in_18988(val);
    const int x = 18988;
    std::cout << 18988;
    for (int i = 0; i < 18988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18988(int& val) {
    const int x = 18988;
    std::cout << 18988;
    for (int i = 0; i < 18988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18989(int& val) {
    func_in_18989(val);
    const int x = 18989;
    std::cout << 18989;
    for (int i = 0; i < 18989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18989(int& val) {
    const int x = 18989;
    std::cout << 18989;
    for (int i = 0; i < 18989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18990(int& val) {
    func_in_18990(val);
    const int x = 18990;
    std::cout << 18990;
    for (int i = 0; i < 18990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18990(int& val) {
    const int x = 18990;
    std::cout << 18990;
    for (int i = 0; i < 18990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18991(int& val) {
    func_in_18991(val);
    const int x = 18991;
    std::cout << 18991;
    for (int i = 0; i < 18991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18991(int& val) {
    const int x = 18991;
    std::cout << 18991;
    for (int i = 0; i < 18991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18992(int& val) {
    func_in_18992(val);
    const int x = 18992;
    std::cout << 18992;
    for (int i = 0; i < 18992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18992(int& val) {
    const int x = 18992;
    std::cout << 18992;
    for (int i = 0; i < 18992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18993(int& val) {
    func_in_18993(val);
    const int x = 18993;
    std::cout << 18993;
    for (int i = 0; i < 18993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18993(int& val) {
    const int x = 18993;
    std::cout << 18993;
    for (int i = 0; i < 18993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18994(int& val) {
    func_in_18994(val);
    const int x = 18994;
    std::cout << 18994;
    for (int i = 0; i < 18994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18994(int& val) {
    const int x = 18994;
    std::cout << 18994;
    for (int i = 0; i < 18994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18995(int& val) {
    func_in_18995(val);
    const int x = 18995;
    std::cout << 18995;
    for (int i = 0; i < 18995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18995(int& val) {
    const int x = 18995;
    std::cout << 18995;
    for (int i = 0; i < 18995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18996(int& val) {
    func_in_18996(val);
    const int x = 18996;
    std::cout << 18996;
    for (int i = 0; i < 18996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18996(int& val) {
    const int x = 18996;
    std::cout << 18996;
    for (int i = 0; i < 18996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18997(int& val) {
    func_in_18997(val);
    const int x = 18997;
    std::cout << 18997;
    for (int i = 0; i < 18997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18997(int& val) {
    const int x = 18997;
    std::cout << 18997;
    for (int i = 0; i < 18997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18998(int& val) {
    func_in_18998(val);
    const int x = 18998;
    std::cout << 18998;
    for (int i = 0; i < 18998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18998(int& val) {
    const int x = 18998;
    std::cout << 18998;
    for (int i = 0; i < 18998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18999(int& val) {
    func_in_18999(val);
    const int x = 18999;
    std::cout << 18999;
    for (int i = 0; i < 18999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18999(int& val) {
    const int x = 18999;
    std::cout << 18999;
    for (int i = 0; i < 18999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19000(int& val) {
    func_in_19000(val);
    const int x = 19000;
    std::cout << 19000;
    for (int i = 0; i < 19000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19000(int& val) {
    const int x = 19000;
    std::cout << 19000;
    for (int i = 0; i < 19000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19001(int& val) {
    func_in_19001(val);
    const int x = 19001;
    std::cout << 19001;
    for (int i = 0; i < 19001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19001(int& val) {
    const int x = 19001;
    std::cout << 19001;
    for (int i = 0; i < 19001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19002(int& val) {
    func_in_19002(val);
    const int x = 19002;
    std::cout << 19002;
    for (int i = 0; i < 19002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19002(int& val) {
    const int x = 19002;
    std::cout << 19002;
    for (int i = 0; i < 19002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19003(int& val) {
    func_in_19003(val);
    const int x = 19003;
    std::cout << 19003;
    for (int i = 0; i < 19003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19003(int& val) {
    const int x = 19003;
    std::cout << 19003;
    for (int i = 0; i < 19003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19004(int& val) {
    func_in_19004(val);
    const int x = 19004;
    std::cout << 19004;
    for (int i = 0; i < 19004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19004(int& val) {
    const int x = 19004;
    std::cout << 19004;
    for (int i = 0; i < 19004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19005(int& val) {
    func_in_19005(val);
    const int x = 19005;
    std::cout << 19005;
    for (int i = 0; i < 19005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19005(int& val) {
    const int x = 19005;
    std::cout << 19005;
    for (int i = 0; i < 19005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19006(int& val) {
    func_in_19006(val);
    const int x = 19006;
    std::cout << 19006;
    for (int i = 0; i < 19006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19006(int& val) {
    const int x = 19006;
    std::cout << 19006;
    for (int i = 0; i < 19006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19007(int& val) {
    func_in_19007(val);
    const int x = 19007;
    std::cout << 19007;
    for (int i = 0; i < 19007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19007(int& val) {
    const int x = 19007;
    std::cout << 19007;
    for (int i = 0; i < 19007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19008(int& val) {
    func_in_19008(val);
    const int x = 19008;
    std::cout << 19008;
    for (int i = 0; i < 19008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19008(int& val) {
    const int x = 19008;
    std::cout << 19008;
    for (int i = 0; i < 19008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19009(int& val) {
    func_in_19009(val);
    const int x = 19009;
    std::cout << 19009;
    for (int i = 0; i < 19009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19009(int& val) {
    const int x = 19009;
    std::cout << 19009;
    for (int i = 0; i < 19009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19010(int& val) {
    func_in_19010(val);
    const int x = 19010;
    std::cout << 19010;
    for (int i = 0; i < 19010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19010(int& val) {
    const int x = 19010;
    std::cout << 19010;
    for (int i = 0; i < 19010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19011(int& val) {
    func_in_19011(val);
    const int x = 19011;
    std::cout << 19011;
    for (int i = 0; i < 19011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19011(int& val) {
    const int x = 19011;
    std::cout << 19011;
    for (int i = 0; i < 19011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19012(int& val) {
    func_in_19012(val);
    const int x = 19012;
    std::cout << 19012;
    for (int i = 0; i < 19012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19012(int& val) {
    const int x = 19012;
    std::cout << 19012;
    for (int i = 0; i < 19012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19013(int& val) {
    func_in_19013(val);
    const int x = 19013;
    std::cout << 19013;
    for (int i = 0; i < 19013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19013(int& val) {
    const int x = 19013;
    std::cout << 19013;
    for (int i = 0; i < 19013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19014(int& val) {
    func_in_19014(val);
    const int x = 19014;
    std::cout << 19014;
    for (int i = 0; i < 19014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19014(int& val) {
    const int x = 19014;
    std::cout << 19014;
    for (int i = 0; i < 19014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19015(int& val) {
    func_in_19015(val);
    const int x = 19015;
    std::cout << 19015;
    for (int i = 0; i < 19015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19015(int& val) {
    const int x = 19015;
    std::cout << 19015;
    for (int i = 0; i < 19015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19016(int& val) {
    func_in_19016(val);
    const int x = 19016;
    std::cout << 19016;
    for (int i = 0; i < 19016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19016(int& val) {
    const int x = 19016;
    std::cout << 19016;
    for (int i = 0; i < 19016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19017(int& val) {
    func_in_19017(val);
    const int x = 19017;
    std::cout << 19017;
    for (int i = 0; i < 19017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19017(int& val) {
    const int x = 19017;
    std::cout << 19017;
    for (int i = 0; i < 19017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19018(int& val) {
    func_in_19018(val);
    const int x = 19018;
    std::cout << 19018;
    for (int i = 0; i < 19018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19018(int& val) {
    const int x = 19018;
    std::cout << 19018;
    for (int i = 0; i < 19018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19019(int& val) {
    func_in_19019(val);
    const int x = 19019;
    std::cout << 19019;
    for (int i = 0; i < 19019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19019(int& val) {
    const int x = 19019;
    std::cout << 19019;
    for (int i = 0; i < 19019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19020(int& val) {
    func_in_19020(val);
    const int x = 19020;
    std::cout << 19020;
    for (int i = 0; i < 19020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19020(int& val) {
    const int x = 19020;
    std::cout << 19020;
    for (int i = 0; i < 19020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19021(int& val) {
    func_in_19021(val);
    const int x = 19021;
    std::cout << 19021;
    for (int i = 0; i < 19021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19021(int& val) {
    const int x = 19021;
    std::cout << 19021;
    for (int i = 0; i < 19021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19022(int& val) {
    func_in_19022(val);
    const int x = 19022;
    std::cout << 19022;
    for (int i = 0; i < 19022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19022(int& val) {
    const int x = 19022;
    std::cout << 19022;
    for (int i = 0; i < 19022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19023(int& val) {
    func_in_19023(val);
    const int x = 19023;
    std::cout << 19023;
    for (int i = 0; i < 19023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19023(int& val) {
    const int x = 19023;
    std::cout << 19023;
    for (int i = 0; i < 19023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19024(int& val) {
    func_in_19024(val);
    const int x = 19024;
    std::cout << 19024;
    for (int i = 0; i < 19024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19024(int& val) {
    const int x = 19024;
    std::cout << 19024;
    for (int i = 0; i < 19024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19025(int& val) {
    func_in_19025(val);
    const int x = 19025;
    std::cout << 19025;
    for (int i = 0; i < 19025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19025(int& val) {
    const int x = 19025;
    std::cout << 19025;
    for (int i = 0; i < 19025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19026(int& val) {
    func_in_19026(val);
    const int x = 19026;
    std::cout << 19026;
    for (int i = 0; i < 19026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19026(int& val) {
    const int x = 19026;
    std::cout << 19026;
    for (int i = 0; i < 19026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19027(int& val) {
    func_in_19027(val);
    const int x = 19027;
    std::cout << 19027;
    for (int i = 0; i < 19027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19027(int& val) {
    const int x = 19027;
    std::cout << 19027;
    for (int i = 0; i < 19027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19028(int& val) {
    func_in_19028(val);
    const int x = 19028;
    std::cout << 19028;
    for (int i = 0; i < 19028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19028(int& val) {
    const int x = 19028;
    std::cout << 19028;
    for (int i = 0; i < 19028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19029(int& val) {
    func_in_19029(val);
    const int x = 19029;
    std::cout << 19029;
    for (int i = 0; i < 19029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19029(int& val) {
    const int x = 19029;
    std::cout << 19029;
    for (int i = 0; i < 19029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19030(int& val) {
    func_in_19030(val);
    const int x = 19030;
    std::cout << 19030;
    for (int i = 0; i < 19030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19030(int& val) {
    const int x = 19030;
    std::cout << 19030;
    for (int i = 0; i < 19030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19031(int& val) {
    func_in_19031(val);
    const int x = 19031;
    std::cout << 19031;
    for (int i = 0; i < 19031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19031(int& val) {
    const int x = 19031;
    std::cout << 19031;
    for (int i = 0; i < 19031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19032(int& val) {
    func_in_19032(val);
    const int x = 19032;
    std::cout << 19032;
    for (int i = 0; i < 19032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19032(int& val) {
    const int x = 19032;
    std::cout << 19032;
    for (int i = 0; i < 19032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19033(int& val) {
    func_in_19033(val);
    const int x = 19033;
    std::cout << 19033;
    for (int i = 0; i < 19033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19033(int& val) {
    const int x = 19033;
    std::cout << 19033;
    for (int i = 0; i < 19033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19034(int& val) {
    func_in_19034(val);
    const int x = 19034;
    std::cout << 19034;
    for (int i = 0; i < 19034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19034(int& val) {
    const int x = 19034;
    std::cout << 19034;
    for (int i = 0; i < 19034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19035(int& val) {
    func_in_19035(val);
    const int x = 19035;
    std::cout << 19035;
    for (int i = 0; i < 19035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19035(int& val) {
    const int x = 19035;
    std::cout << 19035;
    for (int i = 0; i < 19035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19036(int& val) {
    func_in_19036(val);
    const int x = 19036;
    std::cout << 19036;
    for (int i = 0; i < 19036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19036(int& val) {
    const int x = 19036;
    std::cout << 19036;
    for (int i = 0; i < 19036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19037(int& val) {
    func_in_19037(val);
    const int x = 19037;
    std::cout << 19037;
    for (int i = 0; i < 19037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19037(int& val) {
    const int x = 19037;
    std::cout << 19037;
    for (int i = 0; i < 19037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19038(int& val) {
    func_in_19038(val);
    const int x = 19038;
    std::cout << 19038;
    for (int i = 0; i < 19038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19038(int& val) {
    const int x = 19038;
    std::cout << 19038;
    for (int i = 0; i < 19038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19039(int& val) {
    func_in_19039(val);
    const int x = 19039;
    std::cout << 19039;
    for (int i = 0; i < 19039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19039(int& val) {
    const int x = 19039;
    std::cout << 19039;
    for (int i = 0; i < 19039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19040(int& val) {
    func_in_19040(val);
    const int x = 19040;
    std::cout << 19040;
    for (int i = 0; i < 19040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19040(int& val) {
    const int x = 19040;
    std::cout << 19040;
    for (int i = 0; i < 19040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19041(int& val) {
    func_in_19041(val);
    const int x = 19041;
    std::cout << 19041;
    for (int i = 0; i < 19041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19041(int& val) {
    const int x = 19041;
    std::cout << 19041;
    for (int i = 0; i < 19041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19042(int& val) {
    func_in_19042(val);
    const int x = 19042;
    std::cout << 19042;
    for (int i = 0; i < 19042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19042(int& val) {
    const int x = 19042;
    std::cout << 19042;
    for (int i = 0; i < 19042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19043(int& val) {
    func_in_19043(val);
    const int x = 19043;
    std::cout << 19043;
    for (int i = 0; i < 19043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19043(int& val) {
    const int x = 19043;
    std::cout << 19043;
    for (int i = 0; i < 19043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19044(int& val) {
    func_in_19044(val);
    const int x = 19044;
    std::cout << 19044;
    for (int i = 0; i < 19044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19044(int& val) {
    const int x = 19044;
    std::cout << 19044;
    for (int i = 0; i < 19044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19045(int& val) {
    func_in_19045(val);
    const int x = 19045;
    std::cout << 19045;
    for (int i = 0; i < 19045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19045(int& val) {
    const int x = 19045;
    std::cout << 19045;
    for (int i = 0; i < 19045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19046(int& val) {
    func_in_19046(val);
    const int x = 19046;
    std::cout << 19046;
    for (int i = 0; i < 19046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19046(int& val) {
    const int x = 19046;
    std::cout << 19046;
    for (int i = 0; i < 19046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19047(int& val) {
    func_in_19047(val);
    const int x = 19047;
    std::cout << 19047;
    for (int i = 0; i < 19047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19047(int& val) {
    const int x = 19047;
    std::cout << 19047;
    for (int i = 0; i < 19047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19048(int& val) {
    func_in_19048(val);
    const int x = 19048;
    std::cout << 19048;
    for (int i = 0; i < 19048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19048(int& val) {
    const int x = 19048;
    std::cout << 19048;
    for (int i = 0; i < 19048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19049(int& val) {
    func_in_19049(val);
    const int x = 19049;
    std::cout << 19049;
    for (int i = 0; i < 19049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19049(int& val) {
    const int x = 19049;
    std::cout << 19049;
    for (int i = 0; i < 19049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19050(int& val) {
    func_in_19050(val);
    const int x = 19050;
    std::cout << 19050;
    for (int i = 0; i < 19050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19050(int& val) {
    const int x = 19050;
    std::cout << 19050;
    for (int i = 0; i < 19050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19051(int& val) {
    func_in_19051(val);
    const int x = 19051;
    std::cout << 19051;
    for (int i = 0; i < 19051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19051(int& val) {
    const int x = 19051;
    std::cout << 19051;
    for (int i = 0; i < 19051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19052(int& val) {
    func_in_19052(val);
    const int x = 19052;
    std::cout << 19052;
    for (int i = 0; i < 19052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19052(int& val) {
    const int x = 19052;
    std::cout << 19052;
    for (int i = 0; i < 19052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19053(int& val) {
    func_in_19053(val);
    const int x = 19053;
    std::cout << 19053;
    for (int i = 0; i < 19053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19053(int& val) {
    const int x = 19053;
    std::cout << 19053;
    for (int i = 0; i < 19053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19054(int& val) {
    func_in_19054(val);
    const int x = 19054;
    std::cout << 19054;
    for (int i = 0; i < 19054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19054(int& val) {
    const int x = 19054;
    std::cout << 19054;
    for (int i = 0; i < 19054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19055(int& val) {
    func_in_19055(val);
    const int x = 19055;
    std::cout << 19055;
    for (int i = 0; i < 19055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19055(int& val) {
    const int x = 19055;
    std::cout << 19055;
    for (int i = 0; i < 19055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19056(int& val) {
    func_in_19056(val);
    const int x = 19056;
    std::cout << 19056;
    for (int i = 0; i < 19056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19056(int& val) {
    const int x = 19056;
    std::cout << 19056;
    for (int i = 0; i < 19056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19057(int& val) {
    func_in_19057(val);
    const int x = 19057;
    std::cout << 19057;
    for (int i = 0; i < 19057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19057(int& val) {
    const int x = 19057;
    std::cout << 19057;
    for (int i = 0; i < 19057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19058(int& val) {
    func_in_19058(val);
    const int x = 19058;
    std::cout << 19058;
    for (int i = 0; i < 19058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19058(int& val) {
    const int x = 19058;
    std::cout << 19058;
    for (int i = 0; i < 19058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19059(int& val) {
    func_in_19059(val);
    const int x = 19059;
    std::cout << 19059;
    for (int i = 0; i < 19059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19059(int& val) {
    const int x = 19059;
    std::cout << 19059;
    for (int i = 0; i < 19059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19060(int& val) {
    func_in_19060(val);
    const int x = 19060;
    std::cout << 19060;
    for (int i = 0; i < 19060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19060(int& val) {
    const int x = 19060;
    std::cout << 19060;
    for (int i = 0; i < 19060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19061(int& val) {
    func_in_19061(val);
    const int x = 19061;
    std::cout << 19061;
    for (int i = 0; i < 19061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19061(int& val) {
    const int x = 19061;
    std::cout << 19061;
    for (int i = 0; i < 19061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19062(int& val) {
    func_in_19062(val);
    const int x = 19062;
    std::cout << 19062;
    for (int i = 0; i < 19062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19062(int& val) {
    const int x = 19062;
    std::cout << 19062;
    for (int i = 0; i < 19062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19063(int& val) {
    func_in_19063(val);
    const int x = 19063;
    std::cout << 19063;
    for (int i = 0; i < 19063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19063(int& val) {
    const int x = 19063;
    std::cout << 19063;
    for (int i = 0; i < 19063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19064(int& val) {
    func_in_19064(val);
    const int x = 19064;
    std::cout << 19064;
    for (int i = 0; i < 19064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19064(int& val) {
    const int x = 19064;
    std::cout << 19064;
    for (int i = 0; i < 19064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19065(int& val) {
    func_in_19065(val);
    const int x = 19065;
    std::cout << 19065;
    for (int i = 0; i < 19065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19065(int& val) {
    const int x = 19065;
    std::cout << 19065;
    for (int i = 0; i < 19065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19066(int& val) {
    func_in_19066(val);
    const int x = 19066;
    std::cout << 19066;
    for (int i = 0; i < 19066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19066(int& val) {
    const int x = 19066;
    std::cout << 19066;
    for (int i = 0; i < 19066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19067(int& val) {
    func_in_19067(val);
    const int x = 19067;
    std::cout << 19067;
    for (int i = 0; i < 19067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19067(int& val) {
    const int x = 19067;
    std::cout << 19067;
    for (int i = 0; i < 19067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19068(int& val) {
    func_in_19068(val);
    const int x = 19068;
    std::cout << 19068;
    for (int i = 0; i < 19068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19068(int& val) {
    const int x = 19068;
    std::cout << 19068;
    for (int i = 0; i < 19068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19069(int& val) {
    func_in_19069(val);
    const int x = 19069;
    std::cout << 19069;
    for (int i = 0; i < 19069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19069(int& val) {
    const int x = 19069;
    std::cout << 19069;
    for (int i = 0; i < 19069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19070(int& val) {
    func_in_19070(val);
    const int x = 19070;
    std::cout << 19070;
    for (int i = 0; i < 19070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19070(int& val) {
    const int x = 19070;
    std::cout << 19070;
    for (int i = 0; i < 19070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19071(int& val) {
    func_in_19071(val);
    const int x = 19071;
    std::cout << 19071;
    for (int i = 0; i < 19071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19071(int& val) {
    const int x = 19071;
    std::cout << 19071;
    for (int i = 0; i < 19071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19072(int& val) {
    func_in_19072(val);
    const int x = 19072;
    std::cout << 19072;
    for (int i = 0; i < 19072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19072(int& val) {
    const int x = 19072;
    std::cout << 19072;
    for (int i = 0; i < 19072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19073(int& val) {
    func_in_19073(val);
    const int x = 19073;
    std::cout << 19073;
    for (int i = 0; i < 19073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19073(int& val) {
    const int x = 19073;
    std::cout << 19073;
    for (int i = 0; i < 19073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19074(int& val) {
    func_in_19074(val);
    const int x = 19074;
    std::cout << 19074;
    for (int i = 0; i < 19074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19074(int& val) {
    const int x = 19074;
    std::cout << 19074;
    for (int i = 0; i < 19074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19075(int& val) {
    func_in_19075(val);
    const int x = 19075;
    std::cout << 19075;
    for (int i = 0; i < 19075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19075(int& val) {
    const int x = 19075;
    std::cout << 19075;
    for (int i = 0; i < 19075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19076(int& val) {
    func_in_19076(val);
    const int x = 19076;
    std::cout << 19076;
    for (int i = 0; i < 19076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19076(int& val) {
    const int x = 19076;
    std::cout << 19076;
    for (int i = 0; i < 19076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19077(int& val) {
    func_in_19077(val);
    const int x = 19077;
    std::cout << 19077;
    for (int i = 0; i < 19077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19077(int& val) {
    const int x = 19077;
    std::cout << 19077;
    for (int i = 0; i < 19077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19078(int& val) {
    func_in_19078(val);
    const int x = 19078;
    std::cout << 19078;
    for (int i = 0; i < 19078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19078(int& val) {
    const int x = 19078;
    std::cout << 19078;
    for (int i = 0; i < 19078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19079(int& val) {
    func_in_19079(val);
    const int x = 19079;
    std::cout << 19079;
    for (int i = 0; i < 19079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19079(int& val) {
    const int x = 19079;
    std::cout << 19079;
    for (int i = 0; i < 19079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19080(int& val) {
    func_in_19080(val);
    const int x = 19080;
    std::cout << 19080;
    for (int i = 0; i < 19080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19080(int& val) {
    const int x = 19080;
    std::cout << 19080;
    for (int i = 0; i < 19080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19081(int& val) {
    func_in_19081(val);
    const int x = 19081;
    std::cout << 19081;
    for (int i = 0; i < 19081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19081(int& val) {
    const int x = 19081;
    std::cout << 19081;
    for (int i = 0; i < 19081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19082(int& val) {
    func_in_19082(val);
    const int x = 19082;
    std::cout << 19082;
    for (int i = 0; i < 19082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19082(int& val) {
    const int x = 19082;
    std::cout << 19082;
    for (int i = 0; i < 19082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19083(int& val) {
    func_in_19083(val);
    const int x = 19083;
    std::cout << 19083;
    for (int i = 0; i < 19083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19083(int& val) {
    const int x = 19083;
    std::cout << 19083;
    for (int i = 0; i < 19083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19084(int& val) {
    func_in_19084(val);
    const int x = 19084;
    std::cout << 19084;
    for (int i = 0; i < 19084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19084(int& val) {
    const int x = 19084;
    std::cout << 19084;
    for (int i = 0; i < 19084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19085(int& val) {
    func_in_19085(val);
    const int x = 19085;
    std::cout << 19085;
    for (int i = 0; i < 19085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19085(int& val) {
    const int x = 19085;
    std::cout << 19085;
    for (int i = 0; i < 19085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19086(int& val) {
    func_in_19086(val);
    const int x = 19086;
    std::cout << 19086;
    for (int i = 0; i < 19086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19086(int& val) {
    const int x = 19086;
    std::cout << 19086;
    for (int i = 0; i < 19086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19087(int& val) {
    func_in_19087(val);
    const int x = 19087;
    std::cout << 19087;
    for (int i = 0; i < 19087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19087(int& val) {
    const int x = 19087;
    std::cout << 19087;
    for (int i = 0; i < 19087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19088(int& val) {
    func_in_19088(val);
    const int x = 19088;
    std::cout << 19088;
    for (int i = 0; i < 19088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19088(int& val) {
    const int x = 19088;
    std::cout << 19088;
    for (int i = 0; i < 19088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19089(int& val) {
    func_in_19089(val);
    const int x = 19089;
    std::cout << 19089;
    for (int i = 0; i < 19089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19089(int& val) {
    const int x = 19089;
    std::cout << 19089;
    for (int i = 0; i < 19089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19090(int& val) {
    func_in_19090(val);
    const int x = 19090;
    std::cout << 19090;
    for (int i = 0; i < 19090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19090(int& val) {
    const int x = 19090;
    std::cout << 19090;
    for (int i = 0; i < 19090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19091(int& val) {
    func_in_19091(val);
    const int x = 19091;
    std::cout << 19091;
    for (int i = 0; i < 19091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19091(int& val) {
    const int x = 19091;
    std::cout << 19091;
    for (int i = 0; i < 19091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19092(int& val) {
    func_in_19092(val);
    const int x = 19092;
    std::cout << 19092;
    for (int i = 0; i < 19092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19092(int& val) {
    const int x = 19092;
    std::cout << 19092;
    for (int i = 0; i < 19092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19093(int& val) {
    func_in_19093(val);
    const int x = 19093;
    std::cout << 19093;
    for (int i = 0; i < 19093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19093(int& val) {
    const int x = 19093;
    std::cout << 19093;
    for (int i = 0; i < 19093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19094(int& val) {
    func_in_19094(val);
    const int x = 19094;
    std::cout << 19094;
    for (int i = 0; i < 19094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19094(int& val) {
    const int x = 19094;
    std::cout << 19094;
    for (int i = 0; i < 19094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19095(int& val) {
    func_in_19095(val);
    const int x = 19095;
    std::cout << 19095;
    for (int i = 0; i < 19095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19095(int& val) {
    const int x = 19095;
    std::cout << 19095;
    for (int i = 0; i < 19095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19096(int& val) {
    func_in_19096(val);
    const int x = 19096;
    std::cout << 19096;
    for (int i = 0; i < 19096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19096(int& val) {
    const int x = 19096;
    std::cout << 19096;
    for (int i = 0; i < 19096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19097(int& val) {
    func_in_19097(val);
    const int x = 19097;
    std::cout << 19097;
    for (int i = 0; i < 19097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19097(int& val) {
    const int x = 19097;
    std::cout << 19097;
    for (int i = 0; i < 19097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19098(int& val) {
    func_in_19098(val);
    const int x = 19098;
    std::cout << 19098;
    for (int i = 0; i < 19098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19098(int& val) {
    const int x = 19098;
    std::cout << 19098;
    for (int i = 0; i < 19098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19099(int& val) {
    func_in_19099(val);
    const int x = 19099;
    std::cout << 19099;
    for (int i = 0; i < 19099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19099(int& val) {
    const int x = 19099;
    std::cout << 19099;
    for (int i = 0; i < 19099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19100(int& val) {
    func_in_19100(val);
    const int x = 19100;
    std::cout << 19100;
    for (int i = 0; i < 19100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19100(int& val) {
    const int x = 19100;
    std::cout << 19100;
    for (int i = 0; i < 19100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19101(int& val) {
    func_in_19101(val);
    const int x = 19101;
    std::cout << 19101;
    for (int i = 0; i < 19101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19101(int& val) {
    const int x = 19101;
    std::cout << 19101;
    for (int i = 0; i < 19101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19102(int& val) {
    func_in_19102(val);
    const int x = 19102;
    std::cout << 19102;
    for (int i = 0; i < 19102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19102(int& val) {
    const int x = 19102;
    std::cout << 19102;
    for (int i = 0; i < 19102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19103(int& val) {
    func_in_19103(val);
    const int x = 19103;
    std::cout << 19103;
    for (int i = 0; i < 19103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19103(int& val) {
    const int x = 19103;
    std::cout << 19103;
    for (int i = 0; i < 19103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19104(int& val) {
    func_in_19104(val);
    const int x = 19104;
    std::cout << 19104;
    for (int i = 0; i < 19104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19104(int& val) {
    const int x = 19104;
    std::cout << 19104;
    for (int i = 0; i < 19104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19105(int& val) {
    func_in_19105(val);
    const int x = 19105;
    std::cout << 19105;
    for (int i = 0; i < 19105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19105(int& val) {
    const int x = 19105;
    std::cout << 19105;
    for (int i = 0; i < 19105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19106(int& val) {
    func_in_19106(val);
    const int x = 19106;
    std::cout << 19106;
    for (int i = 0; i < 19106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19106(int& val) {
    const int x = 19106;
    std::cout << 19106;
    for (int i = 0; i < 19106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19107(int& val) {
    func_in_19107(val);
    const int x = 19107;
    std::cout << 19107;
    for (int i = 0; i < 19107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19107(int& val) {
    const int x = 19107;
    std::cout << 19107;
    for (int i = 0; i < 19107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19108(int& val) {
    func_in_19108(val);
    const int x = 19108;
    std::cout << 19108;
    for (int i = 0; i < 19108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19108(int& val) {
    const int x = 19108;
    std::cout << 19108;
    for (int i = 0; i < 19108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19109(int& val) {
    func_in_19109(val);
    const int x = 19109;
    std::cout << 19109;
    for (int i = 0; i < 19109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19109(int& val) {
    const int x = 19109;
    std::cout << 19109;
    for (int i = 0; i < 19109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19110(int& val) {
    func_in_19110(val);
    const int x = 19110;
    std::cout << 19110;
    for (int i = 0; i < 19110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19110(int& val) {
    const int x = 19110;
    std::cout << 19110;
    for (int i = 0; i < 19110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19111(int& val) {
    func_in_19111(val);
    const int x = 19111;
    std::cout << 19111;
    for (int i = 0; i < 19111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19111(int& val) {
    const int x = 19111;
    std::cout << 19111;
    for (int i = 0; i < 19111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19112(int& val) {
    func_in_19112(val);
    const int x = 19112;
    std::cout << 19112;
    for (int i = 0; i < 19112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19112(int& val) {
    const int x = 19112;
    std::cout << 19112;
    for (int i = 0; i < 19112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19113(int& val) {
    func_in_19113(val);
    const int x = 19113;
    std::cout << 19113;
    for (int i = 0; i < 19113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19113(int& val) {
    const int x = 19113;
    std::cout << 19113;
    for (int i = 0; i < 19113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19114(int& val) {
    func_in_19114(val);
    const int x = 19114;
    std::cout << 19114;
    for (int i = 0; i < 19114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19114(int& val) {
    const int x = 19114;
    std::cout << 19114;
    for (int i = 0; i < 19114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19115(int& val) {
    func_in_19115(val);
    const int x = 19115;
    std::cout << 19115;
    for (int i = 0; i < 19115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19115(int& val) {
    const int x = 19115;
    std::cout << 19115;
    for (int i = 0; i < 19115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19116(int& val) {
    func_in_19116(val);
    const int x = 19116;
    std::cout << 19116;
    for (int i = 0; i < 19116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19116(int& val) {
    const int x = 19116;
    std::cout << 19116;
    for (int i = 0; i < 19116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19117(int& val) {
    func_in_19117(val);
    const int x = 19117;
    std::cout << 19117;
    for (int i = 0; i < 19117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19117(int& val) {
    const int x = 19117;
    std::cout << 19117;
    for (int i = 0; i < 19117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19118(int& val) {
    func_in_19118(val);
    const int x = 19118;
    std::cout << 19118;
    for (int i = 0; i < 19118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19118(int& val) {
    const int x = 19118;
    std::cout << 19118;
    for (int i = 0; i < 19118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19119(int& val) {
    func_in_19119(val);
    const int x = 19119;
    std::cout << 19119;
    for (int i = 0; i < 19119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19119(int& val) {
    const int x = 19119;
    std::cout << 19119;
    for (int i = 0; i < 19119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19120(int& val) {
    func_in_19120(val);
    const int x = 19120;
    std::cout << 19120;
    for (int i = 0; i < 19120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19120(int& val) {
    const int x = 19120;
    std::cout << 19120;
    for (int i = 0; i < 19120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19121(int& val) {
    func_in_19121(val);
    const int x = 19121;
    std::cout << 19121;
    for (int i = 0; i < 19121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19121(int& val) {
    const int x = 19121;
    std::cout << 19121;
    for (int i = 0; i < 19121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19122(int& val) {
    func_in_19122(val);
    const int x = 19122;
    std::cout << 19122;
    for (int i = 0; i < 19122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19122(int& val) {
    const int x = 19122;
    std::cout << 19122;
    for (int i = 0; i < 19122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19123(int& val) {
    func_in_19123(val);
    const int x = 19123;
    std::cout << 19123;
    for (int i = 0; i < 19123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19123(int& val) {
    const int x = 19123;
    std::cout << 19123;
    for (int i = 0; i < 19123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19124(int& val) {
    func_in_19124(val);
    const int x = 19124;
    std::cout << 19124;
    for (int i = 0; i < 19124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19124(int& val) {
    const int x = 19124;
    std::cout << 19124;
    for (int i = 0; i < 19124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19125(int& val) {
    func_in_19125(val);
    const int x = 19125;
    std::cout << 19125;
    for (int i = 0; i < 19125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19125(int& val) {
    const int x = 19125;
    std::cout << 19125;
    for (int i = 0; i < 19125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19126(int& val) {
    func_in_19126(val);
    const int x = 19126;
    std::cout << 19126;
    for (int i = 0; i < 19126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19126(int& val) {
    const int x = 19126;
    std::cout << 19126;
    for (int i = 0; i < 19126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19127(int& val) {
    func_in_19127(val);
    const int x = 19127;
    std::cout << 19127;
    for (int i = 0; i < 19127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19127(int& val) {
    const int x = 19127;
    std::cout << 19127;
    for (int i = 0; i < 19127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19128(int& val) {
    func_in_19128(val);
    const int x = 19128;
    std::cout << 19128;
    for (int i = 0; i < 19128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19128(int& val) {
    const int x = 19128;
    std::cout << 19128;
    for (int i = 0; i < 19128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19129(int& val) {
    func_in_19129(val);
    const int x = 19129;
    std::cout << 19129;
    for (int i = 0; i < 19129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19129(int& val) {
    const int x = 19129;
    std::cout << 19129;
    for (int i = 0; i < 19129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19130(int& val) {
    func_in_19130(val);
    const int x = 19130;
    std::cout << 19130;
    for (int i = 0; i < 19130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19130(int& val) {
    const int x = 19130;
    std::cout << 19130;
    for (int i = 0; i < 19130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19131(int& val) {
    func_in_19131(val);
    const int x = 19131;
    std::cout << 19131;
    for (int i = 0; i < 19131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19131(int& val) {
    const int x = 19131;
    std::cout << 19131;
    for (int i = 0; i < 19131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19132(int& val) {
    func_in_19132(val);
    const int x = 19132;
    std::cout << 19132;
    for (int i = 0; i < 19132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19132(int& val) {
    const int x = 19132;
    std::cout << 19132;
    for (int i = 0; i < 19132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19133(int& val) {
    func_in_19133(val);
    const int x = 19133;
    std::cout << 19133;
    for (int i = 0; i < 19133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19133(int& val) {
    const int x = 19133;
    std::cout << 19133;
    for (int i = 0; i < 19133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19134(int& val) {
    func_in_19134(val);
    const int x = 19134;
    std::cout << 19134;
    for (int i = 0; i < 19134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19134(int& val) {
    const int x = 19134;
    std::cout << 19134;
    for (int i = 0; i < 19134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19135(int& val) {
    func_in_19135(val);
    const int x = 19135;
    std::cout << 19135;
    for (int i = 0; i < 19135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19135(int& val) {
    const int x = 19135;
    std::cout << 19135;
    for (int i = 0; i < 19135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19136(int& val) {
    func_in_19136(val);
    const int x = 19136;
    std::cout << 19136;
    for (int i = 0; i < 19136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19136(int& val) {
    const int x = 19136;
    std::cout << 19136;
    for (int i = 0; i < 19136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19137(int& val) {
    func_in_19137(val);
    const int x = 19137;
    std::cout << 19137;
    for (int i = 0; i < 19137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19137(int& val) {
    const int x = 19137;
    std::cout << 19137;
    for (int i = 0; i < 19137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19138(int& val) {
    func_in_19138(val);
    const int x = 19138;
    std::cout << 19138;
    for (int i = 0; i < 19138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19138(int& val) {
    const int x = 19138;
    std::cout << 19138;
    for (int i = 0; i < 19138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19139(int& val) {
    func_in_19139(val);
    const int x = 19139;
    std::cout << 19139;
    for (int i = 0; i < 19139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19139(int& val) {
    const int x = 19139;
    std::cout << 19139;
    for (int i = 0; i < 19139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19140(int& val) {
    func_in_19140(val);
    const int x = 19140;
    std::cout << 19140;
    for (int i = 0; i < 19140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19140(int& val) {
    const int x = 19140;
    std::cout << 19140;
    for (int i = 0; i < 19140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19141(int& val) {
    func_in_19141(val);
    const int x = 19141;
    std::cout << 19141;
    for (int i = 0; i < 19141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19141(int& val) {
    const int x = 19141;
    std::cout << 19141;
    for (int i = 0; i < 19141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19142(int& val) {
    func_in_19142(val);
    const int x = 19142;
    std::cout << 19142;
    for (int i = 0; i < 19142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19142(int& val) {
    const int x = 19142;
    std::cout << 19142;
    for (int i = 0; i < 19142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19143(int& val) {
    func_in_19143(val);
    const int x = 19143;
    std::cout << 19143;
    for (int i = 0; i < 19143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19143(int& val) {
    const int x = 19143;
    std::cout << 19143;
    for (int i = 0; i < 19143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19144(int& val) {
    func_in_19144(val);
    const int x = 19144;
    std::cout << 19144;
    for (int i = 0; i < 19144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19144(int& val) {
    const int x = 19144;
    std::cout << 19144;
    for (int i = 0; i < 19144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19145(int& val) {
    func_in_19145(val);
    const int x = 19145;
    std::cout << 19145;
    for (int i = 0; i < 19145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19145(int& val) {
    const int x = 19145;
    std::cout << 19145;
    for (int i = 0; i < 19145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19146(int& val) {
    func_in_19146(val);
    const int x = 19146;
    std::cout << 19146;
    for (int i = 0; i < 19146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19146(int& val) {
    const int x = 19146;
    std::cout << 19146;
    for (int i = 0; i < 19146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19147(int& val) {
    func_in_19147(val);
    const int x = 19147;
    std::cout << 19147;
    for (int i = 0; i < 19147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19147(int& val) {
    const int x = 19147;
    std::cout << 19147;
    for (int i = 0; i < 19147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19148(int& val) {
    func_in_19148(val);
    const int x = 19148;
    std::cout << 19148;
    for (int i = 0; i < 19148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19148(int& val) {
    const int x = 19148;
    std::cout << 19148;
    for (int i = 0; i < 19148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19149(int& val) {
    func_in_19149(val);
    const int x = 19149;
    std::cout << 19149;
    for (int i = 0; i < 19149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19149(int& val) {
    const int x = 19149;
    std::cout << 19149;
    for (int i = 0; i < 19149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19150(int& val) {
    func_in_19150(val);
    const int x = 19150;
    std::cout << 19150;
    for (int i = 0; i < 19150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19150(int& val) {
    const int x = 19150;
    std::cout << 19150;
    for (int i = 0; i < 19150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19151(int& val) {
    func_in_19151(val);
    const int x = 19151;
    std::cout << 19151;
    for (int i = 0; i < 19151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19151(int& val) {
    const int x = 19151;
    std::cout << 19151;
    for (int i = 0; i < 19151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19152(int& val) {
    func_in_19152(val);
    const int x = 19152;
    std::cout << 19152;
    for (int i = 0; i < 19152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19152(int& val) {
    const int x = 19152;
    std::cout << 19152;
    for (int i = 0; i < 19152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19153(int& val) {
    func_in_19153(val);
    const int x = 19153;
    std::cout << 19153;
    for (int i = 0; i < 19153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19153(int& val) {
    const int x = 19153;
    std::cout << 19153;
    for (int i = 0; i < 19153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19154(int& val) {
    func_in_19154(val);
    const int x = 19154;
    std::cout << 19154;
    for (int i = 0; i < 19154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19154(int& val) {
    const int x = 19154;
    std::cout << 19154;
    for (int i = 0; i < 19154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19155(int& val) {
    func_in_19155(val);
    const int x = 19155;
    std::cout << 19155;
    for (int i = 0; i < 19155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19155(int& val) {
    const int x = 19155;
    std::cout << 19155;
    for (int i = 0; i < 19155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19156(int& val) {
    func_in_19156(val);
    const int x = 19156;
    std::cout << 19156;
    for (int i = 0; i < 19156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19156(int& val) {
    const int x = 19156;
    std::cout << 19156;
    for (int i = 0; i < 19156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19157(int& val) {
    func_in_19157(val);
    const int x = 19157;
    std::cout << 19157;
    for (int i = 0; i < 19157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19157(int& val) {
    const int x = 19157;
    std::cout << 19157;
    for (int i = 0; i < 19157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19158(int& val) {
    func_in_19158(val);
    const int x = 19158;
    std::cout << 19158;
    for (int i = 0; i < 19158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19158(int& val) {
    const int x = 19158;
    std::cout << 19158;
    for (int i = 0; i < 19158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19159(int& val) {
    func_in_19159(val);
    const int x = 19159;
    std::cout << 19159;
    for (int i = 0; i < 19159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19159(int& val) {
    const int x = 19159;
    std::cout << 19159;
    for (int i = 0; i < 19159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19160(int& val) {
    func_in_19160(val);
    const int x = 19160;
    std::cout << 19160;
    for (int i = 0; i < 19160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19160(int& val) {
    const int x = 19160;
    std::cout << 19160;
    for (int i = 0; i < 19160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19161(int& val) {
    func_in_19161(val);
    const int x = 19161;
    std::cout << 19161;
    for (int i = 0; i < 19161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19161(int& val) {
    const int x = 19161;
    std::cout << 19161;
    for (int i = 0; i < 19161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19162(int& val) {
    func_in_19162(val);
    const int x = 19162;
    std::cout << 19162;
    for (int i = 0; i < 19162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19162(int& val) {
    const int x = 19162;
    std::cout << 19162;
    for (int i = 0; i < 19162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19163(int& val) {
    func_in_19163(val);
    const int x = 19163;
    std::cout << 19163;
    for (int i = 0; i < 19163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19163(int& val) {
    const int x = 19163;
    std::cout << 19163;
    for (int i = 0; i < 19163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19164(int& val) {
    func_in_19164(val);
    const int x = 19164;
    std::cout << 19164;
    for (int i = 0; i < 19164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19164(int& val) {
    const int x = 19164;
    std::cout << 19164;
    for (int i = 0; i < 19164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19165(int& val) {
    func_in_19165(val);
    const int x = 19165;
    std::cout << 19165;
    for (int i = 0; i < 19165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19165(int& val) {
    const int x = 19165;
    std::cout << 19165;
    for (int i = 0; i < 19165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19166(int& val) {
    func_in_19166(val);
    const int x = 19166;
    std::cout << 19166;
    for (int i = 0; i < 19166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19166(int& val) {
    const int x = 19166;
    std::cout << 19166;
    for (int i = 0; i < 19166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19167(int& val) {
    func_in_19167(val);
    const int x = 19167;
    std::cout << 19167;
    for (int i = 0; i < 19167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19167(int& val) {
    const int x = 19167;
    std::cout << 19167;
    for (int i = 0; i < 19167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19168(int& val) {
    func_in_19168(val);
    const int x = 19168;
    std::cout << 19168;
    for (int i = 0; i < 19168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19168(int& val) {
    const int x = 19168;
    std::cout << 19168;
    for (int i = 0; i < 19168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19169(int& val) {
    func_in_19169(val);
    const int x = 19169;
    std::cout << 19169;
    for (int i = 0; i < 19169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19169(int& val) {
    const int x = 19169;
    std::cout << 19169;
    for (int i = 0; i < 19169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19170(int& val) {
    func_in_19170(val);
    const int x = 19170;
    std::cout << 19170;
    for (int i = 0; i < 19170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19170(int& val) {
    const int x = 19170;
    std::cout << 19170;
    for (int i = 0; i < 19170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19171(int& val) {
    func_in_19171(val);
    const int x = 19171;
    std::cout << 19171;
    for (int i = 0; i < 19171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19171(int& val) {
    const int x = 19171;
    std::cout << 19171;
    for (int i = 0; i < 19171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19172(int& val) {
    func_in_19172(val);
    const int x = 19172;
    std::cout << 19172;
    for (int i = 0; i < 19172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19172(int& val) {
    const int x = 19172;
    std::cout << 19172;
    for (int i = 0; i < 19172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19173(int& val) {
    func_in_19173(val);
    const int x = 19173;
    std::cout << 19173;
    for (int i = 0; i < 19173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19173(int& val) {
    const int x = 19173;
    std::cout << 19173;
    for (int i = 0; i < 19173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19174(int& val) {
    func_in_19174(val);
    const int x = 19174;
    std::cout << 19174;
    for (int i = 0; i < 19174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19174(int& val) {
    const int x = 19174;
    std::cout << 19174;
    for (int i = 0; i < 19174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19175(int& val) {
    func_in_19175(val);
    const int x = 19175;
    std::cout << 19175;
    for (int i = 0; i < 19175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19175(int& val) {
    const int x = 19175;
    std::cout << 19175;
    for (int i = 0; i < 19175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19176(int& val) {
    func_in_19176(val);
    const int x = 19176;
    std::cout << 19176;
    for (int i = 0; i < 19176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19176(int& val) {
    const int x = 19176;
    std::cout << 19176;
    for (int i = 0; i < 19176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19177(int& val) {
    func_in_19177(val);
    const int x = 19177;
    std::cout << 19177;
    for (int i = 0; i < 19177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19177(int& val) {
    const int x = 19177;
    std::cout << 19177;
    for (int i = 0; i < 19177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19178(int& val) {
    func_in_19178(val);
    const int x = 19178;
    std::cout << 19178;
    for (int i = 0; i < 19178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19178(int& val) {
    const int x = 19178;
    std::cout << 19178;
    for (int i = 0; i < 19178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19179(int& val) {
    func_in_19179(val);
    const int x = 19179;
    std::cout << 19179;
    for (int i = 0; i < 19179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19179(int& val) {
    const int x = 19179;
    std::cout << 19179;
    for (int i = 0; i < 19179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19180(int& val) {
    func_in_19180(val);
    const int x = 19180;
    std::cout << 19180;
    for (int i = 0; i < 19180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19180(int& val) {
    const int x = 19180;
    std::cout << 19180;
    for (int i = 0; i < 19180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19181(int& val) {
    func_in_19181(val);
    const int x = 19181;
    std::cout << 19181;
    for (int i = 0; i < 19181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19181(int& val) {
    const int x = 19181;
    std::cout << 19181;
    for (int i = 0; i < 19181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19182(int& val) {
    func_in_19182(val);
    const int x = 19182;
    std::cout << 19182;
    for (int i = 0; i < 19182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19182(int& val) {
    const int x = 19182;
    std::cout << 19182;
    for (int i = 0; i < 19182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19183(int& val) {
    func_in_19183(val);
    const int x = 19183;
    std::cout << 19183;
    for (int i = 0; i < 19183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19183(int& val) {
    const int x = 19183;
    std::cout << 19183;
    for (int i = 0; i < 19183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19184(int& val) {
    func_in_19184(val);
    const int x = 19184;
    std::cout << 19184;
    for (int i = 0; i < 19184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19184(int& val) {
    const int x = 19184;
    std::cout << 19184;
    for (int i = 0; i < 19184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19185(int& val) {
    func_in_19185(val);
    const int x = 19185;
    std::cout << 19185;
    for (int i = 0; i < 19185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19185(int& val) {
    const int x = 19185;
    std::cout << 19185;
    for (int i = 0; i < 19185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19186(int& val) {
    func_in_19186(val);
    const int x = 19186;
    std::cout << 19186;
    for (int i = 0; i < 19186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19186(int& val) {
    const int x = 19186;
    std::cout << 19186;
    for (int i = 0; i < 19186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19187(int& val) {
    func_in_19187(val);
    const int x = 19187;
    std::cout << 19187;
    for (int i = 0; i < 19187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19187(int& val) {
    const int x = 19187;
    std::cout << 19187;
    for (int i = 0; i < 19187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19188(int& val) {
    func_in_19188(val);
    const int x = 19188;
    std::cout << 19188;
    for (int i = 0; i < 19188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19188(int& val) {
    const int x = 19188;
    std::cout << 19188;
    for (int i = 0; i < 19188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19189(int& val) {
    func_in_19189(val);
    const int x = 19189;
    std::cout << 19189;
    for (int i = 0; i < 19189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19189(int& val) {
    const int x = 19189;
    std::cout << 19189;
    for (int i = 0; i < 19189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19190(int& val) {
    func_in_19190(val);
    const int x = 19190;
    std::cout << 19190;
    for (int i = 0; i < 19190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19190(int& val) {
    const int x = 19190;
    std::cout << 19190;
    for (int i = 0; i < 19190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19191(int& val) {
    func_in_19191(val);
    const int x = 19191;
    std::cout << 19191;
    for (int i = 0; i < 19191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19191(int& val) {
    const int x = 19191;
    std::cout << 19191;
    for (int i = 0; i < 19191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19192(int& val) {
    func_in_19192(val);
    const int x = 19192;
    std::cout << 19192;
    for (int i = 0; i < 19192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19192(int& val) {
    const int x = 19192;
    std::cout << 19192;
    for (int i = 0; i < 19192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19193(int& val) {
    func_in_19193(val);
    const int x = 19193;
    std::cout << 19193;
    for (int i = 0; i < 19193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19193(int& val) {
    const int x = 19193;
    std::cout << 19193;
    for (int i = 0; i < 19193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19194(int& val) {
    func_in_19194(val);
    const int x = 19194;
    std::cout << 19194;
    for (int i = 0; i < 19194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19194(int& val) {
    const int x = 19194;
    std::cout << 19194;
    for (int i = 0; i < 19194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19195(int& val) {
    func_in_19195(val);
    const int x = 19195;
    std::cout << 19195;
    for (int i = 0; i < 19195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19195(int& val) {
    const int x = 19195;
    std::cout << 19195;
    for (int i = 0; i < 19195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19196(int& val) {
    func_in_19196(val);
    const int x = 19196;
    std::cout << 19196;
    for (int i = 0; i < 19196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19196(int& val) {
    const int x = 19196;
    std::cout << 19196;
    for (int i = 0; i < 19196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19197(int& val) {
    func_in_19197(val);
    const int x = 19197;
    std::cout << 19197;
    for (int i = 0; i < 19197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19197(int& val) {
    const int x = 19197;
    std::cout << 19197;
    for (int i = 0; i < 19197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19198(int& val) {
    func_in_19198(val);
    const int x = 19198;
    std::cout << 19198;
    for (int i = 0; i < 19198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19198(int& val) {
    const int x = 19198;
    std::cout << 19198;
    for (int i = 0; i < 19198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19199(int& val) {
    func_in_19199(val);
    const int x = 19199;
    std::cout << 19199;
    for (int i = 0; i < 19199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19199(int& val) {
    const int x = 19199;
    std::cout << 19199;
    for (int i = 0; i < 19199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19200(int& val) {
    func_in_19200(val);
    const int x = 19200;
    std::cout << 19200;
    for (int i = 0; i < 19200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19200(int& val) {
    const int x = 19200;
    std::cout << 19200;
    for (int i = 0; i < 19200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19201(int& val) {
    func_in_19201(val);
    const int x = 19201;
    std::cout << 19201;
    for (int i = 0; i < 19201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19201(int& val) {
    const int x = 19201;
    std::cout << 19201;
    for (int i = 0; i < 19201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19202(int& val) {
    func_in_19202(val);
    const int x = 19202;
    std::cout << 19202;
    for (int i = 0; i < 19202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19202(int& val) {
    const int x = 19202;
    std::cout << 19202;
    for (int i = 0; i < 19202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19203(int& val) {
    func_in_19203(val);
    const int x = 19203;
    std::cout << 19203;
    for (int i = 0; i < 19203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19203(int& val) {
    const int x = 19203;
    std::cout << 19203;
    for (int i = 0; i < 19203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19204(int& val) {
    func_in_19204(val);
    const int x = 19204;
    std::cout << 19204;
    for (int i = 0; i < 19204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19204(int& val) {
    const int x = 19204;
    std::cout << 19204;
    for (int i = 0; i < 19204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19205(int& val) {
    func_in_19205(val);
    const int x = 19205;
    std::cout << 19205;
    for (int i = 0; i < 19205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19205(int& val) {
    const int x = 19205;
    std::cout << 19205;
    for (int i = 0; i < 19205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19206(int& val) {
    func_in_19206(val);
    const int x = 19206;
    std::cout << 19206;
    for (int i = 0; i < 19206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19206(int& val) {
    const int x = 19206;
    std::cout << 19206;
    for (int i = 0; i < 19206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19207(int& val) {
    func_in_19207(val);
    const int x = 19207;
    std::cout << 19207;
    for (int i = 0; i < 19207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19207(int& val) {
    const int x = 19207;
    std::cout << 19207;
    for (int i = 0; i < 19207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19208(int& val) {
    func_in_19208(val);
    const int x = 19208;
    std::cout << 19208;
    for (int i = 0; i < 19208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19208(int& val) {
    const int x = 19208;
    std::cout << 19208;
    for (int i = 0; i < 19208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19209(int& val) {
    func_in_19209(val);
    const int x = 19209;
    std::cout << 19209;
    for (int i = 0; i < 19209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19209(int& val) {
    const int x = 19209;
    std::cout << 19209;
    for (int i = 0; i < 19209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19210(int& val) {
    func_in_19210(val);
    const int x = 19210;
    std::cout << 19210;
    for (int i = 0; i < 19210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19210(int& val) {
    const int x = 19210;
    std::cout << 19210;
    for (int i = 0; i < 19210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19211(int& val) {
    func_in_19211(val);
    const int x = 19211;
    std::cout << 19211;
    for (int i = 0; i < 19211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19211(int& val) {
    const int x = 19211;
    std::cout << 19211;
    for (int i = 0; i < 19211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19212(int& val) {
    func_in_19212(val);
    const int x = 19212;
    std::cout << 19212;
    for (int i = 0; i < 19212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19212(int& val) {
    const int x = 19212;
    std::cout << 19212;
    for (int i = 0; i < 19212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19213(int& val) {
    func_in_19213(val);
    const int x = 19213;
    std::cout << 19213;
    for (int i = 0; i < 19213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19213(int& val) {
    const int x = 19213;
    std::cout << 19213;
    for (int i = 0; i < 19213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19214(int& val) {
    func_in_19214(val);
    const int x = 19214;
    std::cout << 19214;
    for (int i = 0; i < 19214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19214(int& val) {
    const int x = 19214;
    std::cout << 19214;
    for (int i = 0; i < 19214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19215(int& val) {
    func_in_19215(val);
    const int x = 19215;
    std::cout << 19215;
    for (int i = 0; i < 19215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19215(int& val) {
    const int x = 19215;
    std::cout << 19215;
    for (int i = 0; i < 19215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19216(int& val) {
    func_in_19216(val);
    const int x = 19216;
    std::cout << 19216;
    for (int i = 0; i < 19216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19216(int& val) {
    const int x = 19216;
    std::cout << 19216;
    for (int i = 0; i < 19216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19217(int& val) {
    func_in_19217(val);
    const int x = 19217;
    std::cout << 19217;
    for (int i = 0; i < 19217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19217(int& val) {
    const int x = 19217;
    std::cout << 19217;
    for (int i = 0; i < 19217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19218(int& val) {
    func_in_19218(val);
    const int x = 19218;
    std::cout << 19218;
    for (int i = 0; i < 19218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19218(int& val) {
    const int x = 19218;
    std::cout << 19218;
    for (int i = 0; i < 19218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19219(int& val) {
    func_in_19219(val);
    const int x = 19219;
    std::cout << 19219;
    for (int i = 0; i < 19219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19219(int& val) {
    const int x = 19219;
    std::cout << 19219;
    for (int i = 0; i < 19219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19220(int& val) {
    func_in_19220(val);
    const int x = 19220;
    std::cout << 19220;
    for (int i = 0; i < 19220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19220(int& val) {
    const int x = 19220;
    std::cout << 19220;
    for (int i = 0; i < 19220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19221(int& val) {
    func_in_19221(val);
    const int x = 19221;
    std::cout << 19221;
    for (int i = 0; i < 19221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19221(int& val) {
    const int x = 19221;
    std::cout << 19221;
    for (int i = 0; i < 19221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19222(int& val) {
    func_in_19222(val);
    const int x = 19222;
    std::cout << 19222;
    for (int i = 0; i < 19222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19222(int& val) {
    const int x = 19222;
    std::cout << 19222;
    for (int i = 0; i < 19222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19223(int& val) {
    func_in_19223(val);
    const int x = 19223;
    std::cout << 19223;
    for (int i = 0; i < 19223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19223(int& val) {
    const int x = 19223;
    std::cout << 19223;
    for (int i = 0; i < 19223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19224(int& val) {
    func_in_19224(val);
    const int x = 19224;
    std::cout << 19224;
    for (int i = 0; i < 19224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19224(int& val) {
    const int x = 19224;
    std::cout << 19224;
    for (int i = 0; i < 19224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19225(int& val) {
    func_in_19225(val);
    const int x = 19225;
    std::cout << 19225;
    for (int i = 0; i < 19225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19225(int& val) {
    const int x = 19225;
    std::cout << 19225;
    for (int i = 0; i < 19225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19226(int& val) {
    func_in_19226(val);
    const int x = 19226;
    std::cout << 19226;
    for (int i = 0; i < 19226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19226(int& val) {
    const int x = 19226;
    std::cout << 19226;
    for (int i = 0; i < 19226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19227(int& val) {
    func_in_19227(val);
    const int x = 19227;
    std::cout << 19227;
    for (int i = 0; i < 19227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19227(int& val) {
    const int x = 19227;
    std::cout << 19227;
    for (int i = 0; i < 19227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19228(int& val) {
    func_in_19228(val);
    const int x = 19228;
    std::cout << 19228;
    for (int i = 0; i < 19228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19228(int& val) {
    const int x = 19228;
    std::cout << 19228;
    for (int i = 0; i < 19228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19229(int& val) {
    func_in_19229(val);
    const int x = 19229;
    std::cout << 19229;
    for (int i = 0; i < 19229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19229(int& val) {
    const int x = 19229;
    std::cout << 19229;
    for (int i = 0; i < 19229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19230(int& val) {
    func_in_19230(val);
    const int x = 19230;
    std::cout << 19230;
    for (int i = 0; i < 19230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19230(int& val) {
    const int x = 19230;
    std::cout << 19230;
    for (int i = 0; i < 19230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19231(int& val) {
    func_in_19231(val);
    const int x = 19231;
    std::cout << 19231;
    for (int i = 0; i < 19231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19231(int& val) {
    const int x = 19231;
    std::cout << 19231;
    for (int i = 0; i < 19231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19232(int& val) {
    func_in_19232(val);
    const int x = 19232;
    std::cout << 19232;
    for (int i = 0; i < 19232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19232(int& val) {
    const int x = 19232;
    std::cout << 19232;
    for (int i = 0; i < 19232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19233(int& val) {
    func_in_19233(val);
    const int x = 19233;
    std::cout << 19233;
    for (int i = 0; i < 19233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19233(int& val) {
    const int x = 19233;
    std::cout << 19233;
    for (int i = 0; i < 19233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19234(int& val) {
    func_in_19234(val);
    const int x = 19234;
    std::cout << 19234;
    for (int i = 0; i < 19234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19234(int& val) {
    const int x = 19234;
    std::cout << 19234;
    for (int i = 0; i < 19234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19235(int& val) {
    func_in_19235(val);
    const int x = 19235;
    std::cout << 19235;
    for (int i = 0; i < 19235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19235(int& val) {
    const int x = 19235;
    std::cout << 19235;
    for (int i = 0; i < 19235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19236(int& val) {
    func_in_19236(val);
    const int x = 19236;
    std::cout << 19236;
    for (int i = 0; i < 19236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19236(int& val) {
    const int x = 19236;
    std::cout << 19236;
    for (int i = 0; i < 19236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19237(int& val) {
    func_in_19237(val);
    const int x = 19237;
    std::cout << 19237;
    for (int i = 0; i < 19237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19237(int& val) {
    const int x = 19237;
    std::cout << 19237;
    for (int i = 0; i < 19237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19238(int& val) {
    func_in_19238(val);
    const int x = 19238;
    std::cout << 19238;
    for (int i = 0; i < 19238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19238(int& val) {
    const int x = 19238;
    std::cout << 19238;
    for (int i = 0; i < 19238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19239(int& val) {
    func_in_19239(val);
    const int x = 19239;
    std::cout << 19239;
    for (int i = 0; i < 19239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19239(int& val) {
    const int x = 19239;
    std::cout << 19239;
    for (int i = 0; i < 19239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19240(int& val) {
    func_in_19240(val);
    const int x = 19240;
    std::cout << 19240;
    for (int i = 0; i < 19240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19240(int& val) {
    const int x = 19240;
    std::cout << 19240;
    for (int i = 0; i < 19240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19241(int& val) {
    func_in_19241(val);
    const int x = 19241;
    std::cout << 19241;
    for (int i = 0; i < 19241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19241(int& val) {
    const int x = 19241;
    std::cout << 19241;
    for (int i = 0; i < 19241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19242(int& val) {
    func_in_19242(val);
    const int x = 19242;
    std::cout << 19242;
    for (int i = 0; i < 19242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19242(int& val) {
    const int x = 19242;
    std::cout << 19242;
    for (int i = 0; i < 19242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19243(int& val) {
    func_in_19243(val);
    const int x = 19243;
    std::cout << 19243;
    for (int i = 0; i < 19243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19243(int& val) {
    const int x = 19243;
    std::cout << 19243;
    for (int i = 0; i < 19243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19244(int& val) {
    func_in_19244(val);
    const int x = 19244;
    std::cout << 19244;
    for (int i = 0; i < 19244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19244(int& val) {
    const int x = 19244;
    std::cout << 19244;
    for (int i = 0; i < 19244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19245(int& val) {
    func_in_19245(val);
    const int x = 19245;
    std::cout << 19245;
    for (int i = 0; i < 19245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19245(int& val) {
    const int x = 19245;
    std::cout << 19245;
    for (int i = 0; i < 19245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19246(int& val) {
    func_in_19246(val);
    const int x = 19246;
    std::cout << 19246;
    for (int i = 0; i < 19246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19246(int& val) {
    const int x = 19246;
    std::cout << 19246;
    for (int i = 0; i < 19246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19247(int& val) {
    func_in_19247(val);
    const int x = 19247;
    std::cout << 19247;
    for (int i = 0; i < 19247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19247(int& val) {
    const int x = 19247;
    std::cout << 19247;
    for (int i = 0; i < 19247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19248(int& val) {
    func_in_19248(val);
    const int x = 19248;
    std::cout << 19248;
    for (int i = 0; i < 19248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19248(int& val) {
    const int x = 19248;
    std::cout << 19248;
    for (int i = 0; i < 19248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19249(int& val) {
    func_in_19249(val);
    const int x = 19249;
    std::cout << 19249;
    for (int i = 0; i < 19249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19249(int& val) {
    const int x = 19249;
    std::cout << 19249;
    for (int i = 0; i < 19249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19250(int& val) {
    func_in_19250(val);
    const int x = 19250;
    std::cout << 19250;
    for (int i = 0; i < 19250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19250(int& val) {
    const int x = 19250;
    std::cout << 19250;
    for (int i = 0; i < 19250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19251(int& val) {
    func_in_19251(val);
    const int x = 19251;
    std::cout << 19251;
    for (int i = 0; i < 19251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19251(int& val) {
    const int x = 19251;
    std::cout << 19251;
    for (int i = 0; i < 19251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19252(int& val) {
    func_in_19252(val);
    const int x = 19252;
    std::cout << 19252;
    for (int i = 0; i < 19252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19252(int& val) {
    const int x = 19252;
    std::cout << 19252;
    for (int i = 0; i < 19252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19253(int& val) {
    func_in_19253(val);
    const int x = 19253;
    std::cout << 19253;
    for (int i = 0; i < 19253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19253(int& val) {
    const int x = 19253;
    std::cout << 19253;
    for (int i = 0; i < 19253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19254(int& val) {
    func_in_19254(val);
    const int x = 19254;
    std::cout << 19254;
    for (int i = 0; i < 19254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19254(int& val) {
    const int x = 19254;
    std::cout << 19254;
    for (int i = 0; i < 19254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19255(int& val) {
    func_in_19255(val);
    const int x = 19255;
    std::cout << 19255;
    for (int i = 0; i < 19255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19255(int& val) {
    const int x = 19255;
    std::cout << 19255;
    for (int i = 0; i < 19255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19256(int& val) {
    func_in_19256(val);
    const int x = 19256;
    std::cout << 19256;
    for (int i = 0; i < 19256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19256(int& val) {
    const int x = 19256;
    std::cout << 19256;
    for (int i = 0; i < 19256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19257(int& val) {
    func_in_19257(val);
    const int x = 19257;
    std::cout << 19257;
    for (int i = 0; i < 19257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19257(int& val) {
    const int x = 19257;
    std::cout << 19257;
    for (int i = 0; i < 19257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19258(int& val) {
    func_in_19258(val);
    const int x = 19258;
    std::cout << 19258;
    for (int i = 0; i < 19258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19258(int& val) {
    const int x = 19258;
    std::cout << 19258;
    for (int i = 0; i < 19258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19259(int& val) {
    func_in_19259(val);
    const int x = 19259;
    std::cout << 19259;
    for (int i = 0; i < 19259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19259(int& val) {
    const int x = 19259;
    std::cout << 19259;
    for (int i = 0; i < 19259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19260(int& val) {
    func_in_19260(val);
    const int x = 19260;
    std::cout << 19260;
    for (int i = 0; i < 19260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19260(int& val) {
    const int x = 19260;
    std::cout << 19260;
    for (int i = 0; i < 19260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19261(int& val) {
    func_in_19261(val);
    const int x = 19261;
    std::cout << 19261;
    for (int i = 0; i < 19261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19261(int& val) {
    const int x = 19261;
    std::cout << 19261;
    for (int i = 0; i < 19261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19262(int& val) {
    func_in_19262(val);
    const int x = 19262;
    std::cout << 19262;
    for (int i = 0; i < 19262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19262(int& val) {
    const int x = 19262;
    std::cout << 19262;
    for (int i = 0; i < 19262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19263(int& val) {
    func_in_19263(val);
    const int x = 19263;
    std::cout << 19263;
    for (int i = 0; i < 19263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19263(int& val) {
    const int x = 19263;
    std::cout << 19263;
    for (int i = 0; i < 19263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19264(int& val) {
    func_in_19264(val);
    const int x = 19264;
    std::cout << 19264;
    for (int i = 0; i < 19264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19264(int& val) {
    const int x = 19264;
    std::cout << 19264;
    for (int i = 0; i < 19264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19265(int& val) {
    func_in_19265(val);
    const int x = 19265;
    std::cout << 19265;
    for (int i = 0; i < 19265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19265(int& val) {
    const int x = 19265;
    std::cout << 19265;
    for (int i = 0; i < 19265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19266(int& val) {
    func_in_19266(val);
    const int x = 19266;
    std::cout << 19266;
    for (int i = 0; i < 19266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19266(int& val) {
    const int x = 19266;
    std::cout << 19266;
    for (int i = 0; i < 19266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19267(int& val) {
    func_in_19267(val);
    const int x = 19267;
    std::cout << 19267;
    for (int i = 0; i < 19267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19267(int& val) {
    const int x = 19267;
    std::cout << 19267;
    for (int i = 0; i < 19267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19268(int& val) {
    func_in_19268(val);
    const int x = 19268;
    std::cout << 19268;
    for (int i = 0; i < 19268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19268(int& val) {
    const int x = 19268;
    std::cout << 19268;
    for (int i = 0; i < 19268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19269(int& val) {
    func_in_19269(val);
    const int x = 19269;
    std::cout << 19269;
    for (int i = 0; i < 19269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19269(int& val) {
    const int x = 19269;
    std::cout << 19269;
    for (int i = 0; i < 19269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19270(int& val) {
    func_in_19270(val);
    const int x = 19270;
    std::cout << 19270;
    for (int i = 0; i < 19270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19270(int& val) {
    const int x = 19270;
    std::cout << 19270;
    for (int i = 0; i < 19270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19271(int& val) {
    func_in_19271(val);
    const int x = 19271;
    std::cout << 19271;
    for (int i = 0; i < 19271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19271(int& val) {
    const int x = 19271;
    std::cout << 19271;
    for (int i = 0; i < 19271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19272(int& val) {
    func_in_19272(val);
    const int x = 19272;
    std::cout << 19272;
    for (int i = 0; i < 19272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19272(int& val) {
    const int x = 19272;
    std::cout << 19272;
    for (int i = 0; i < 19272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19273(int& val) {
    func_in_19273(val);
    const int x = 19273;
    std::cout << 19273;
    for (int i = 0; i < 19273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19273(int& val) {
    const int x = 19273;
    std::cout << 19273;
    for (int i = 0; i < 19273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19274(int& val) {
    func_in_19274(val);
    const int x = 19274;
    std::cout << 19274;
    for (int i = 0; i < 19274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19274(int& val) {
    const int x = 19274;
    std::cout << 19274;
    for (int i = 0; i < 19274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19275(int& val) {
    func_in_19275(val);
    const int x = 19275;
    std::cout << 19275;
    for (int i = 0; i < 19275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19275(int& val) {
    const int x = 19275;
    std::cout << 19275;
    for (int i = 0; i < 19275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19276(int& val) {
    func_in_19276(val);
    const int x = 19276;
    std::cout << 19276;
    for (int i = 0; i < 19276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19276(int& val) {
    const int x = 19276;
    std::cout << 19276;
    for (int i = 0; i < 19276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19277(int& val) {
    func_in_19277(val);
    const int x = 19277;
    std::cout << 19277;
    for (int i = 0; i < 19277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19277(int& val) {
    const int x = 19277;
    std::cout << 19277;
    for (int i = 0; i < 19277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19278(int& val) {
    func_in_19278(val);
    const int x = 19278;
    std::cout << 19278;
    for (int i = 0; i < 19278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19278(int& val) {
    const int x = 19278;
    std::cout << 19278;
    for (int i = 0; i < 19278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19279(int& val) {
    func_in_19279(val);
    const int x = 19279;
    std::cout << 19279;
    for (int i = 0; i < 19279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19279(int& val) {
    const int x = 19279;
    std::cout << 19279;
    for (int i = 0; i < 19279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19280(int& val) {
    func_in_19280(val);
    const int x = 19280;
    std::cout << 19280;
    for (int i = 0; i < 19280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19280(int& val) {
    const int x = 19280;
    std::cout << 19280;
    for (int i = 0; i < 19280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19281(int& val) {
    func_in_19281(val);
    const int x = 19281;
    std::cout << 19281;
    for (int i = 0; i < 19281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19281(int& val) {
    const int x = 19281;
    std::cout << 19281;
    for (int i = 0; i < 19281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19282(int& val) {
    func_in_19282(val);
    const int x = 19282;
    std::cout << 19282;
    for (int i = 0; i < 19282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19282(int& val) {
    const int x = 19282;
    std::cout << 19282;
    for (int i = 0; i < 19282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19283(int& val) {
    func_in_19283(val);
    const int x = 19283;
    std::cout << 19283;
    for (int i = 0; i < 19283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19283(int& val) {
    const int x = 19283;
    std::cout << 19283;
    for (int i = 0; i < 19283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19284(int& val) {
    func_in_19284(val);
    const int x = 19284;
    std::cout << 19284;
    for (int i = 0; i < 19284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19284(int& val) {
    const int x = 19284;
    std::cout << 19284;
    for (int i = 0; i < 19284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19285(int& val) {
    func_in_19285(val);
    const int x = 19285;
    std::cout << 19285;
    for (int i = 0; i < 19285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19285(int& val) {
    const int x = 19285;
    std::cout << 19285;
    for (int i = 0; i < 19285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19286(int& val) {
    func_in_19286(val);
    const int x = 19286;
    std::cout << 19286;
    for (int i = 0; i < 19286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19286(int& val) {
    const int x = 19286;
    std::cout << 19286;
    for (int i = 0; i < 19286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19287(int& val) {
    func_in_19287(val);
    const int x = 19287;
    std::cout << 19287;
    for (int i = 0; i < 19287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19287(int& val) {
    const int x = 19287;
    std::cout << 19287;
    for (int i = 0; i < 19287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19288(int& val) {
    func_in_19288(val);
    const int x = 19288;
    std::cout << 19288;
    for (int i = 0; i < 19288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19288(int& val) {
    const int x = 19288;
    std::cout << 19288;
    for (int i = 0; i < 19288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19289(int& val) {
    func_in_19289(val);
    const int x = 19289;
    std::cout << 19289;
    for (int i = 0; i < 19289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19289(int& val) {
    const int x = 19289;
    std::cout << 19289;
    for (int i = 0; i < 19289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19290(int& val) {
    func_in_19290(val);
    const int x = 19290;
    std::cout << 19290;
    for (int i = 0; i < 19290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19290(int& val) {
    const int x = 19290;
    std::cout << 19290;
    for (int i = 0; i < 19290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19291(int& val) {
    func_in_19291(val);
    const int x = 19291;
    std::cout << 19291;
    for (int i = 0; i < 19291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19291(int& val) {
    const int x = 19291;
    std::cout << 19291;
    for (int i = 0; i < 19291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19292(int& val) {
    func_in_19292(val);
    const int x = 19292;
    std::cout << 19292;
    for (int i = 0; i < 19292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19292(int& val) {
    const int x = 19292;
    std::cout << 19292;
    for (int i = 0; i < 19292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19293(int& val) {
    func_in_19293(val);
    const int x = 19293;
    std::cout << 19293;
    for (int i = 0; i < 19293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19293(int& val) {
    const int x = 19293;
    std::cout << 19293;
    for (int i = 0; i < 19293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19294(int& val) {
    func_in_19294(val);
    const int x = 19294;
    std::cout << 19294;
    for (int i = 0; i < 19294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19294(int& val) {
    const int x = 19294;
    std::cout << 19294;
    for (int i = 0; i < 19294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19295(int& val) {
    func_in_19295(val);
    const int x = 19295;
    std::cout << 19295;
    for (int i = 0; i < 19295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19295(int& val) {
    const int x = 19295;
    std::cout << 19295;
    for (int i = 0; i < 19295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19296(int& val) {
    func_in_19296(val);
    const int x = 19296;
    std::cout << 19296;
    for (int i = 0; i < 19296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19296(int& val) {
    const int x = 19296;
    std::cout << 19296;
    for (int i = 0; i < 19296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19297(int& val) {
    func_in_19297(val);
    const int x = 19297;
    std::cout << 19297;
    for (int i = 0; i < 19297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19297(int& val) {
    const int x = 19297;
    std::cout << 19297;
    for (int i = 0; i < 19297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19298(int& val) {
    func_in_19298(val);
    const int x = 19298;
    std::cout << 19298;
    for (int i = 0; i < 19298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19298(int& val) {
    const int x = 19298;
    std::cout << 19298;
    for (int i = 0; i < 19298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19299(int& val) {
    func_in_19299(val);
    const int x = 19299;
    std::cout << 19299;
    for (int i = 0; i < 19299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19299(int& val) {
    const int x = 19299;
    std::cout << 19299;
    for (int i = 0; i < 19299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19300(int& val) {
    func_in_19300(val);
    const int x = 19300;
    std::cout << 19300;
    for (int i = 0; i < 19300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19300(int& val) {
    const int x = 19300;
    std::cout << 19300;
    for (int i = 0; i < 19300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19301(int& val) {
    func_in_19301(val);
    const int x = 19301;
    std::cout << 19301;
    for (int i = 0; i < 19301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19301(int& val) {
    const int x = 19301;
    std::cout << 19301;
    for (int i = 0; i < 19301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19302(int& val) {
    func_in_19302(val);
    const int x = 19302;
    std::cout << 19302;
    for (int i = 0; i < 19302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19302(int& val) {
    const int x = 19302;
    std::cout << 19302;
    for (int i = 0; i < 19302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19303(int& val) {
    func_in_19303(val);
    const int x = 19303;
    std::cout << 19303;
    for (int i = 0; i < 19303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19303(int& val) {
    const int x = 19303;
    std::cout << 19303;
    for (int i = 0; i < 19303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19304(int& val) {
    func_in_19304(val);
    const int x = 19304;
    std::cout << 19304;
    for (int i = 0; i < 19304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19304(int& val) {
    const int x = 19304;
    std::cout << 19304;
    for (int i = 0; i < 19304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19305(int& val) {
    func_in_19305(val);
    const int x = 19305;
    std::cout << 19305;
    for (int i = 0; i < 19305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19305(int& val) {
    const int x = 19305;
    std::cout << 19305;
    for (int i = 0; i < 19305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19306(int& val) {
    func_in_19306(val);
    const int x = 19306;
    std::cout << 19306;
    for (int i = 0; i < 19306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19306(int& val) {
    const int x = 19306;
    std::cout << 19306;
    for (int i = 0; i < 19306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19307(int& val) {
    func_in_19307(val);
    const int x = 19307;
    std::cout << 19307;
    for (int i = 0; i < 19307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19307(int& val) {
    const int x = 19307;
    std::cout << 19307;
    for (int i = 0; i < 19307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19308(int& val) {
    func_in_19308(val);
    const int x = 19308;
    std::cout << 19308;
    for (int i = 0; i < 19308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19308(int& val) {
    const int x = 19308;
    std::cout << 19308;
    for (int i = 0; i < 19308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19309(int& val) {
    func_in_19309(val);
    const int x = 19309;
    std::cout << 19309;
    for (int i = 0; i < 19309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19309(int& val) {
    const int x = 19309;
    std::cout << 19309;
    for (int i = 0; i < 19309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19310(int& val) {
    func_in_19310(val);
    const int x = 19310;
    std::cout << 19310;
    for (int i = 0; i < 19310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19310(int& val) {
    const int x = 19310;
    std::cout << 19310;
    for (int i = 0; i < 19310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19311(int& val) {
    func_in_19311(val);
    const int x = 19311;
    std::cout << 19311;
    for (int i = 0; i < 19311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19311(int& val) {
    const int x = 19311;
    std::cout << 19311;
    for (int i = 0; i < 19311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19312(int& val) {
    func_in_19312(val);
    const int x = 19312;
    std::cout << 19312;
    for (int i = 0; i < 19312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19312(int& val) {
    const int x = 19312;
    std::cout << 19312;
    for (int i = 0; i < 19312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19313(int& val) {
    func_in_19313(val);
    const int x = 19313;
    std::cout << 19313;
    for (int i = 0; i < 19313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19313(int& val) {
    const int x = 19313;
    std::cout << 19313;
    for (int i = 0; i < 19313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19314(int& val) {
    func_in_19314(val);
    const int x = 19314;
    std::cout << 19314;
    for (int i = 0; i < 19314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19314(int& val) {
    const int x = 19314;
    std::cout << 19314;
    for (int i = 0; i < 19314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19315(int& val) {
    func_in_19315(val);
    const int x = 19315;
    std::cout << 19315;
    for (int i = 0; i < 19315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19315(int& val) {
    const int x = 19315;
    std::cout << 19315;
    for (int i = 0; i < 19315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19316(int& val) {
    func_in_19316(val);
    const int x = 19316;
    std::cout << 19316;
    for (int i = 0; i < 19316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19316(int& val) {
    const int x = 19316;
    std::cout << 19316;
    for (int i = 0; i < 19316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19317(int& val) {
    func_in_19317(val);
    const int x = 19317;
    std::cout << 19317;
    for (int i = 0; i < 19317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19317(int& val) {
    const int x = 19317;
    std::cout << 19317;
    for (int i = 0; i < 19317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19318(int& val) {
    func_in_19318(val);
    const int x = 19318;
    std::cout << 19318;
    for (int i = 0; i < 19318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19318(int& val) {
    const int x = 19318;
    std::cout << 19318;
    for (int i = 0; i < 19318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19319(int& val) {
    func_in_19319(val);
    const int x = 19319;
    std::cout << 19319;
    for (int i = 0; i < 19319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19319(int& val) {
    const int x = 19319;
    std::cout << 19319;
    for (int i = 0; i < 19319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19320(int& val) {
    func_in_19320(val);
    const int x = 19320;
    std::cout << 19320;
    for (int i = 0; i < 19320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19320(int& val) {
    const int x = 19320;
    std::cout << 19320;
    for (int i = 0; i < 19320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19321(int& val) {
    func_in_19321(val);
    const int x = 19321;
    std::cout << 19321;
    for (int i = 0; i < 19321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19321(int& val) {
    const int x = 19321;
    std::cout << 19321;
    for (int i = 0; i < 19321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19322(int& val) {
    func_in_19322(val);
    const int x = 19322;
    std::cout << 19322;
    for (int i = 0; i < 19322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19322(int& val) {
    const int x = 19322;
    std::cout << 19322;
    for (int i = 0; i < 19322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19323(int& val) {
    func_in_19323(val);
    const int x = 19323;
    std::cout << 19323;
    for (int i = 0; i < 19323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19323(int& val) {
    const int x = 19323;
    std::cout << 19323;
    for (int i = 0; i < 19323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19324(int& val) {
    func_in_19324(val);
    const int x = 19324;
    std::cout << 19324;
    for (int i = 0; i < 19324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19324(int& val) {
    const int x = 19324;
    std::cout << 19324;
    for (int i = 0; i < 19324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19325(int& val) {
    func_in_19325(val);
    const int x = 19325;
    std::cout << 19325;
    for (int i = 0; i < 19325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19325(int& val) {
    const int x = 19325;
    std::cout << 19325;
    for (int i = 0; i < 19325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19326(int& val) {
    func_in_19326(val);
    const int x = 19326;
    std::cout << 19326;
    for (int i = 0; i < 19326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19326(int& val) {
    const int x = 19326;
    std::cout << 19326;
    for (int i = 0; i < 19326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19327(int& val) {
    func_in_19327(val);
    const int x = 19327;
    std::cout << 19327;
    for (int i = 0; i < 19327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19327(int& val) {
    const int x = 19327;
    std::cout << 19327;
    for (int i = 0; i < 19327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19328(int& val) {
    func_in_19328(val);
    const int x = 19328;
    std::cout << 19328;
    for (int i = 0; i < 19328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19328(int& val) {
    const int x = 19328;
    std::cout << 19328;
    for (int i = 0; i < 19328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19329(int& val) {
    func_in_19329(val);
    const int x = 19329;
    std::cout << 19329;
    for (int i = 0; i < 19329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19329(int& val) {
    const int x = 19329;
    std::cout << 19329;
    for (int i = 0; i < 19329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19330(int& val) {
    func_in_19330(val);
    const int x = 19330;
    std::cout << 19330;
    for (int i = 0; i < 19330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19330(int& val) {
    const int x = 19330;
    std::cout << 19330;
    for (int i = 0; i < 19330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19331(int& val) {
    func_in_19331(val);
    const int x = 19331;
    std::cout << 19331;
    for (int i = 0; i < 19331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19331(int& val) {
    const int x = 19331;
    std::cout << 19331;
    for (int i = 0; i < 19331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19332(int& val) {
    func_in_19332(val);
    const int x = 19332;
    std::cout << 19332;
    for (int i = 0; i < 19332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19332(int& val) {
    const int x = 19332;
    std::cout << 19332;
    for (int i = 0; i < 19332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19333(int& val) {
    func_in_19333(val);
    const int x = 19333;
    std::cout << 19333;
    for (int i = 0; i < 19333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19333(int& val) {
    const int x = 19333;
    std::cout << 19333;
    for (int i = 0; i < 19333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19334(int& val) {
    func_in_19334(val);
    const int x = 19334;
    std::cout << 19334;
    for (int i = 0; i < 19334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19334(int& val) {
    const int x = 19334;
    std::cout << 19334;
    for (int i = 0; i < 19334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19335(int& val) {
    func_in_19335(val);
    const int x = 19335;
    std::cout << 19335;
    for (int i = 0; i < 19335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19335(int& val) {
    const int x = 19335;
    std::cout << 19335;
    for (int i = 0; i < 19335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19336(int& val) {
    func_in_19336(val);
    const int x = 19336;
    std::cout << 19336;
    for (int i = 0; i < 19336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19336(int& val) {
    const int x = 19336;
    std::cout << 19336;
    for (int i = 0; i < 19336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19337(int& val) {
    func_in_19337(val);
    const int x = 19337;
    std::cout << 19337;
    for (int i = 0; i < 19337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19337(int& val) {
    const int x = 19337;
    std::cout << 19337;
    for (int i = 0; i < 19337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19338(int& val) {
    func_in_19338(val);
    const int x = 19338;
    std::cout << 19338;
    for (int i = 0; i < 19338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19338(int& val) {
    const int x = 19338;
    std::cout << 19338;
    for (int i = 0; i < 19338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19339(int& val) {
    func_in_19339(val);
    const int x = 19339;
    std::cout << 19339;
    for (int i = 0; i < 19339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19339(int& val) {
    const int x = 19339;
    std::cout << 19339;
    for (int i = 0; i < 19339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19340(int& val) {
    func_in_19340(val);
    const int x = 19340;
    std::cout << 19340;
    for (int i = 0; i < 19340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19340(int& val) {
    const int x = 19340;
    std::cout << 19340;
    for (int i = 0; i < 19340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19341(int& val) {
    func_in_19341(val);
    const int x = 19341;
    std::cout << 19341;
    for (int i = 0; i < 19341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19341(int& val) {
    const int x = 19341;
    std::cout << 19341;
    for (int i = 0; i < 19341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19342(int& val) {
    func_in_19342(val);
    const int x = 19342;
    std::cout << 19342;
    for (int i = 0; i < 19342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19342(int& val) {
    const int x = 19342;
    std::cout << 19342;
    for (int i = 0; i < 19342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19343(int& val) {
    func_in_19343(val);
    const int x = 19343;
    std::cout << 19343;
    for (int i = 0; i < 19343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19343(int& val) {
    const int x = 19343;
    std::cout << 19343;
    for (int i = 0; i < 19343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19344(int& val) {
    func_in_19344(val);
    const int x = 19344;
    std::cout << 19344;
    for (int i = 0; i < 19344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19344(int& val) {
    const int x = 19344;
    std::cout << 19344;
    for (int i = 0; i < 19344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19345(int& val) {
    func_in_19345(val);
    const int x = 19345;
    std::cout << 19345;
    for (int i = 0; i < 19345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19345(int& val) {
    const int x = 19345;
    std::cout << 19345;
    for (int i = 0; i < 19345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19346(int& val) {
    func_in_19346(val);
    const int x = 19346;
    std::cout << 19346;
    for (int i = 0; i < 19346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19346(int& val) {
    const int x = 19346;
    std::cout << 19346;
    for (int i = 0; i < 19346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19347(int& val) {
    func_in_19347(val);
    const int x = 19347;
    std::cout << 19347;
    for (int i = 0; i < 19347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19347(int& val) {
    const int x = 19347;
    std::cout << 19347;
    for (int i = 0; i < 19347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19348(int& val) {
    func_in_19348(val);
    const int x = 19348;
    std::cout << 19348;
    for (int i = 0; i < 19348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19348(int& val) {
    const int x = 19348;
    std::cout << 19348;
    for (int i = 0; i < 19348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19349(int& val) {
    func_in_19349(val);
    const int x = 19349;
    std::cout << 19349;
    for (int i = 0; i < 19349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19349(int& val) {
    const int x = 19349;
    std::cout << 19349;
    for (int i = 0; i < 19349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19350(int& val) {
    func_in_19350(val);
    const int x = 19350;
    std::cout << 19350;
    for (int i = 0; i < 19350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19350(int& val) {
    const int x = 19350;
    std::cout << 19350;
    for (int i = 0; i < 19350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19351(int& val) {
    func_in_19351(val);
    const int x = 19351;
    std::cout << 19351;
    for (int i = 0; i < 19351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19351(int& val) {
    const int x = 19351;
    std::cout << 19351;
    for (int i = 0; i < 19351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19352(int& val) {
    func_in_19352(val);
    const int x = 19352;
    std::cout << 19352;
    for (int i = 0; i < 19352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19352(int& val) {
    const int x = 19352;
    std::cout << 19352;
    for (int i = 0; i < 19352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19353(int& val) {
    func_in_19353(val);
    const int x = 19353;
    std::cout << 19353;
    for (int i = 0; i < 19353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19353(int& val) {
    const int x = 19353;
    std::cout << 19353;
    for (int i = 0; i < 19353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19354(int& val) {
    func_in_19354(val);
    const int x = 19354;
    std::cout << 19354;
    for (int i = 0; i < 19354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19354(int& val) {
    const int x = 19354;
    std::cout << 19354;
    for (int i = 0; i < 19354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19355(int& val) {
    func_in_19355(val);
    const int x = 19355;
    std::cout << 19355;
    for (int i = 0; i < 19355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19355(int& val) {
    const int x = 19355;
    std::cout << 19355;
    for (int i = 0; i < 19355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19356(int& val) {
    func_in_19356(val);
    const int x = 19356;
    std::cout << 19356;
    for (int i = 0; i < 19356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19356(int& val) {
    const int x = 19356;
    std::cout << 19356;
    for (int i = 0; i < 19356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19357(int& val) {
    func_in_19357(val);
    const int x = 19357;
    std::cout << 19357;
    for (int i = 0; i < 19357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19357(int& val) {
    const int x = 19357;
    std::cout << 19357;
    for (int i = 0; i < 19357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19358(int& val) {
    func_in_19358(val);
    const int x = 19358;
    std::cout << 19358;
    for (int i = 0; i < 19358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19358(int& val) {
    const int x = 19358;
    std::cout << 19358;
    for (int i = 0; i < 19358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19359(int& val) {
    func_in_19359(val);
    const int x = 19359;
    std::cout << 19359;
    for (int i = 0; i < 19359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19359(int& val) {
    const int x = 19359;
    std::cout << 19359;
    for (int i = 0; i < 19359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19360(int& val) {
    func_in_19360(val);
    const int x = 19360;
    std::cout << 19360;
    for (int i = 0; i < 19360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19360(int& val) {
    const int x = 19360;
    std::cout << 19360;
    for (int i = 0; i < 19360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19361(int& val) {
    func_in_19361(val);
    const int x = 19361;
    std::cout << 19361;
    for (int i = 0; i < 19361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19361(int& val) {
    const int x = 19361;
    std::cout << 19361;
    for (int i = 0; i < 19361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19362(int& val) {
    func_in_19362(val);
    const int x = 19362;
    std::cout << 19362;
    for (int i = 0; i < 19362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19362(int& val) {
    const int x = 19362;
    std::cout << 19362;
    for (int i = 0; i < 19362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19363(int& val) {
    func_in_19363(val);
    const int x = 19363;
    std::cout << 19363;
    for (int i = 0; i < 19363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19363(int& val) {
    const int x = 19363;
    std::cout << 19363;
    for (int i = 0; i < 19363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19364(int& val) {
    func_in_19364(val);
    const int x = 19364;
    std::cout << 19364;
    for (int i = 0; i < 19364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19364(int& val) {
    const int x = 19364;
    std::cout << 19364;
    for (int i = 0; i < 19364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19365(int& val) {
    func_in_19365(val);
    const int x = 19365;
    std::cout << 19365;
    for (int i = 0; i < 19365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19365(int& val) {
    const int x = 19365;
    std::cout << 19365;
    for (int i = 0; i < 19365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19366(int& val) {
    func_in_19366(val);
    const int x = 19366;
    std::cout << 19366;
    for (int i = 0; i < 19366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19366(int& val) {
    const int x = 19366;
    std::cout << 19366;
    for (int i = 0; i < 19366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19367(int& val) {
    func_in_19367(val);
    const int x = 19367;
    std::cout << 19367;
    for (int i = 0; i < 19367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19367(int& val) {
    const int x = 19367;
    std::cout << 19367;
    for (int i = 0; i < 19367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19368(int& val) {
    func_in_19368(val);
    const int x = 19368;
    std::cout << 19368;
    for (int i = 0; i < 19368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19368(int& val) {
    const int x = 19368;
    std::cout << 19368;
    for (int i = 0; i < 19368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19369(int& val) {
    func_in_19369(val);
    const int x = 19369;
    std::cout << 19369;
    for (int i = 0; i < 19369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19369(int& val) {
    const int x = 19369;
    std::cout << 19369;
    for (int i = 0; i < 19369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19370(int& val) {
    func_in_19370(val);
    const int x = 19370;
    std::cout << 19370;
    for (int i = 0; i < 19370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19370(int& val) {
    const int x = 19370;
    std::cout << 19370;
    for (int i = 0; i < 19370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19371(int& val) {
    func_in_19371(val);
    const int x = 19371;
    std::cout << 19371;
    for (int i = 0; i < 19371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19371(int& val) {
    const int x = 19371;
    std::cout << 19371;
    for (int i = 0; i < 19371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19372(int& val) {
    func_in_19372(val);
    const int x = 19372;
    std::cout << 19372;
    for (int i = 0; i < 19372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19372(int& val) {
    const int x = 19372;
    std::cout << 19372;
    for (int i = 0; i < 19372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19373(int& val) {
    func_in_19373(val);
    const int x = 19373;
    std::cout << 19373;
    for (int i = 0; i < 19373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19373(int& val) {
    const int x = 19373;
    std::cout << 19373;
    for (int i = 0; i < 19373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19374(int& val) {
    func_in_19374(val);
    const int x = 19374;
    std::cout << 19374;
    for (int i = 0; i < 19374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19374(int& val) {
    const int x = 19374;
    std::cout << 19374;
    for (int i = 0; i < 19374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19375(int& val) {
    func_in_19375(val);
    const int x = 19375;
    std::cout << 19375;
    for (int i = 0; i < 19375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19375(int& val) {
    const int x = 19375;
    std::cout << 19375;
    for (int i = 0; i < 19375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19376(int& val) {
    func_in_19376(val);
    const int x = 19376;
    std::cout << 19376;
    for (int i = 0; i < 19376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19376(int& val) {
    const int x = 19376;
    std::cout << 19376;
    for (int i = 0; i < 19376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19377(int& val) {
    func_in_19377(val);
    const int x = 19377;
    std::cout << 19377;
    for (int i = 0; i < 19377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19377(int& val) {
    const int x = 19377;
    std::cout << 19377;
    for (int i = 0; i < 19377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19378(int& val) {
    func_in_19378(val);
    const int x = 19378;
    std::cout << 19378;
    for (int i = 0; i < 19378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19378(int& val) {
    const int x = 19378;
    std::cout << 19378;
    for (int i = 0; i < 19378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19379(int& val) {
    func_in_19379(val);
    const int x = 19379;
    std::cout << 19379;
    for (int i = 0; i < 19379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19379(int& val) {
    const int x = 19379;
    std::cout << 19379;
    for (int i = 0; i < 19379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19380(int& val) {
    func_in_19380(val);
    const int x = 19380;
    std::cout << 19380;
    for (int i = 0; i < 19380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19380(int& val) {
    const int x = 19380;
    std::cout << 19380;
    for (int i = 0; i < 19380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19381(int& val) {
    func_in_19381(val);
    const int x = 19381;
    std::cout << 19381;
    for (int i = 0; i < 19381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19381(int& val) {
    const int x = 19381;
    std::cout << 19381;
    for (int i = 0; i < 19381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19382(int& val) {
    func_in_19382(val);
    const int x = 19382;
    std::cout << 19382;
    for (int i = 0; i < 19382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19382(int& val) {
    const int x = 19382;
    std::cout << 19382;
    for (int i = 0; i < 19382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19383(int& val) {
    func_in_19383(val);
    const int x = 19383;
    std::cout << 19383;
    for (int i = 0; i < 19383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19383(int& val) {
    const int x = 19383;
    std::cout << 19383;
    for (int i = 0; i < 19383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19384(int& val) {
    func_in_19384(val);
    const int x = 19384;
    std::cout << 19384;
    for (int i = 0; i < 19384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19384(int& val) {
    const int x = 19384;
    std::cout << 19384;
    for (int i = 0; i < 19384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19385(int& val) {
    func_in_19385(val);
    const int x = 19385;
    std::cout << 19385;
    for (int i = 0; i < 19385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19385(int& val) {
    const int x = 19385;
    std::cout << 19385;
    for (int i = 0; i < 19385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19386(int& val) {
    func_in_19386(val);
    const int x = 19386;
    std::cout << 19386;
    for (int i = 0; i < 19386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19386(int& val) {
    const int x = 19386;
    std::cout << 19386;
    for (int i = 0; i < 19386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19387(int& val) {
    func_in_19387(val);
    const int x = 19387;
    std::cout << 19387;
    for (int i = 0; i < 19387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19387(int& val) {
    const int x = 19387;
    std::cout << 19387;
    for (int i = 0; i < 19387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19388(int& val) {
    func_in_19388(val);
    const int x = 19388;
    std::cout << 19388;
    for (int i = 0; i < 19388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19388(int& val) {
    const int x = 19388;
    std::cout << 19388;
    for (int i = 0; i < 19388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19389(int& val) {
    func_in_19389(val);
    const int x = 19389;
    std::cout << 19389;
    for (int i = 0; i < 19389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19389(int& val) {
    const int x = 19389;
    std::cout << 19389;
    for (int i = 0; i < 19389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19390(int& val) {
    func_in_19390(val);
    const int x = 19390;
    std::cout << 19390;
    for (int i = 0; i < 19390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19390(int& val) {
    const int x = 19390;
    std::cout << 19390;
    for (int i = 0; i < 19390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19391(int& val) {
    func_in_19391(val);
    const int x = 19391;
    std::cout << 19391;
    for (int i = 0; i < 19391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19391(int& val) {
    const int x = 19391;
    std::cout << 19391;
    for (int i = 0; i < 19391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19392(int& val) {
    func_in_19392(val);
    const int x = 19392;
    std::cout << 19392;
    for (int i = 0; i < 19392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19392(int& val) {
    const int x = 19392;
    std::cout << 19392;
    for (int i = 0; i < 19392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19393(int& val) {
    func_in_19393(val);
    const int x = 19393;
    std::cout << 19393;
    for (int i = 0; i < 19393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19393(int& val) {
    const int x = 19393;
    std::cout << 19393;
    for (int i = 0; i < 19393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19394(int& val) {
    func_in_19394(val);
    const int x = 19394;
    std::cout << 19394;
    for (int i = 0; i < 19394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19394(int& val) {
    const int x = 19394;
    std::cout << 19394;
    for (int i = 0; i < 19394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19395(int& val) {
    func_in_19395(val);
    const int x = 19395;
    std::cout << 19395;
    for (int i = 0; i < 19395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19395(int& val) {
    const int x = 19395;
    std::cout << 19395;
    for (int i = 0; i < 19395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19396(int& val) {
    func_in_19396(val);
    const int x = 19396;
    std::cout << 19396;
    for (int i = 0; i < 19396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19396(int& val) {
    const int x = 19396;
    std::cout << 19396;
    for (int i = 0; i < 19396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19397(int& val) {
    func_in_19397(val);
    const int x = 19397;
    std::cout << 19397;
    for (int i = 0; i < 19397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19397(int& val) {
    const int x = 19397;
    std::cout << 19397;
    for (int i = 0; i < 19397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19398(int& val) {
    func_in_19398(val);
    const int x = 19398;
    std::cout << 19398;
    for (int i = 0; i < 19398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19398(int& val) {
    const int x = 19398;
    std::cout << 19398;
    for (int i = 0; i < 19398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19399(int& val) {
    func_in_19399(val);
    const int x = 19399;
    std::cout << 19399;
    for (int i = 0; i < 19399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19399(int& val) {
    const int x = 19399;
    std::cout << 19399;
    for (int i = 0; i < 19399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19400(int& val) {
    func_in_19400(val);
    const int x = 19400;
    std::cout << 19400;
    for (int i = 0; i < 19400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19400(int& val) {
    const int x = 19400;
    std::cout << 19400;
    for (int i = 0; i < 19400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19401(int& val) {
    func_in_19401(val);
    const int x = 19401;
    std::cout << 19401;
    for (int i = 0; i < 19401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19401(int& val) {
    const int x = 19401;
    std::cout << 19401;
    for (int i = 0; i < 19401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19402(int& val) {
    func_in_19402(val);
    const int x = 19402;
    std::cout << 19402;
    for (int i = 0; i < 19402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19402(int& val) {
    const int x = 19402;
    std::cout << 19402;
    for (int i = 0; i < 19402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19403(int& val) {
    func_in_19403(val);
    const int x = 19403;
    std::cout << 19403;
    for (int i = 0; i < 19403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19403(int& val) {
    const int x = 19403;
    std::cout << 19403;
    for (int i = 0; i < 19403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19404(int& val) {
    func_in_19404(val);
    const int x = 19404;
    std::cout << 19404;
    for (int i = 0; i < 19404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19404(int& val) {
    const int x = 19404;
    std::cout << 19404;
    for (int i = 0; i < 19404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19405(int& val) {
    func_in_19405(val);
    const int x = 19405;
    std::cout << 19405;
    for (int i = 0; i < 19405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19405(int& val) {
    const int x = 19405;
    std::cout << 19405;
    for (int i = 0; i < 19405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19406(int& val) {
    func_in_19406(val);
    const int x = 19406;
    std::cout << 19406;
    for (int i = 0; i < 19406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19406(int& val) {
    const int x = 19406;
    std::cout << 19406;
    for (int i = 0; i < 19406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19407(int& val) {
    func_in_19407(val);
    const int x = 19407;
    std::cout << 19407;
    for (int i = 0; i < 19407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19407(int& val) {
    const int x = 19407;
    std::cout << 19407;
    for (int i = 0; i < 19407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19408(int& val) {
    func_in_19408(val);
    const int x = 19408;
    std::cout << 19408;
    for (int i = 0; i < 19408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19408(int& val) {
    const int x = 19408;
    std::cout << 19408;
    for (int i = 0; i < 19408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19409(int& val) {
    func_in_19409(val);
    const int x = 19409;
    std::cout << 19409;
    for (int i = 0; i < 19409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19409(int& val) {
    const int x = 19409;
    std::cout << 19409;
    for (int i = 0; i < 19409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19410(int& val) {
    func_in_19410(val);
    const int x = 19410;
    std::cout << 19410;
    for (int i = 0; i < 19410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19410(int& val) {
    const int x = 19410;
    std::cout << 19410;
    for (int i = 0; i < 19410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19411(int& val) {
    func_in_19411(val);
    const int x = 19411;
    std::cout << 19411;
    for (int i = 0; i < 19411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19411(int& val) {
    const int x = 19411;
    std::cout << 19411;
    for (int i = 0; i < 19411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19412(int& val) {
    func_in_19412(val);
    const int x = 19412;
    std::cout << 19412;
    for (int i = 0; i < 19412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19412(int& val) {
    const int x = 19412;
    std::cout << 19412;
    for (int i = 0; i < 19412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19413(int& val) {
    func_in_19413(val);
    const int x = 19413;
    std::cout << 19413;
    for (int i = 0; i < 19413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19413(int& val) {
    const int x = 19413;
    std::cout << 19413;
    for (int i = 0; i < 19413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19414(int& val) {
    func_in_19414(val);
    const int x = 19414;
    std::cout << 19414;
    for (int i = 0; i < 19414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19414(int& val) {
    const int x = 19414;
    std::cout << 19414;
    for (int i = 0; i < 19414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19415(int& val) {
    func_in_19415(val);
    const int x = 19415;
    std::cout << 19415;
    for (int i = 0; i < 19415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19415(int& val) {
    const int x = 19415;
    std::cout << 19415;
    for (int i = 0; i < 19415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19416(int& val) {
    func_in_19416(val);
    const int x = 19416;
    std::cout << 19416;
    for (int i = 0; i < 19416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19416(int& val) {
    const int x = 19416;
    std::cout << 19416;
    for (int i = 0; i < 19416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19417(int& val) {
    func_in_19417(val);
    const int x = 19417;
    std::cout << 19417;
    for (int i = 0; i < 19417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19417(int& val) {
    const int x = 19417;
    std::cout << 19417;
    for (int i = 0; i < 19417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19418(int& val) {
    func_in_19418(val);
    const int x = 19418;
    std::cout << 19418;
    for (int i = 0; i < 19418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19418(int& val) {
    const int x = 19418;
    std::cout << 19418;
    for (int i = 0; i < 19418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19419(int& val) {
    func_in_19419(val);
    const int x = 19419;
    std::cout << 19419;
    for (int i = 0; i < 19419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19419(int& val) {
    const int x = 19419;
    std::cout << 19419;
    for (int i = 0; i < 19419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19420(int& val) {
    func_in_19420(val);
    const int x = 19420;
    std::cout << 19420;
    for (int i = 0; i < 19420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19420(int& val) {
    const int x = 19420;
    std::cout << 19420;
    for (int i = 0; i < 19420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19421(int& val) {
    func_in_19421(val);
    const int x = 19421;
    std::cout << 19421;
    for (int i = 0; i < 19421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19421(int& val) {
    const int x = 19421;
    std::cout << 19421;
    for (int i = 0; i < 19421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19422(int& val) {
    func_in_19422(val);
    const int x = 19422;
    std::cout << 19422;
    for (int i = 0; i < 19422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19422(int& val) {
    const int x = 19422;
    std::cout << 19422;
    for (int i = 0; i < 19422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19423(int& val) {
    func_in_19423(val);
    const int x = 19423;
    std::cout << 19423;
    for (int i = 0; i < 19423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19423(int& val) {
    const int x = 19423;
    std::cout << 19423;
    for (int i = 0; i < 19423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19424(int& val) {
    func_in_19424(val);
    const int x = 19424;
    std::cout << 19424;
    for (int i = 0; i < 19424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19424(int& val) {
    const int x = 19424;
    std::cout << 19424;
    for (int i = 0; i < 19424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19425(int& val) {
    func_in_19425(val);
    const int x = 19425;
    std::cout << 19425;
    for (int i = 0; i < 19425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19425(int& val) {
    const int x = 19425;
    std::cout << 19425;
    for (int i = 0; i < 19425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19426(int& val) {
    func_in_19426(val);
    const int x = 19426;
    std::cout << 19426;
    for (int i = 0; i < 19426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19426(int& val) {
    const int x = 19426;
    std::cout << 19426;
    for (int i = 0; i < 19426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19427(int& val) {
    func_in_19427(val);
    const int x = 19427;
    std::cout << 19427;
    for (int i = 0; i < 19427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19427(int& val) {
    const int x = 19427;
    std::cout << 19427;
    for (int i = 0; i < 19427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19428(int& val) {
    func_in_19428(val);
    const int x = 19428;
    std::cout << 19428;
    for (int i = 0; i < 19428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19428(int& val) {
    const int x = 19428;
    std::cout << 19428;
    for (int i = 0; i < 19428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19429(int& val) {
    func_in_19429(val);
    const int x = 19429;
    std::cout << 19429;
    for (int i = 0; i < 19429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19429(int& val) {
    const int x = 19429;
    std::cout << 19429;
    for (int i = 0; i < 19429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19430(int& val) {
    func_in_19430(val);
    const int x = 19430;
    std::cout << 19430;
    for (int i = 0; i < 19430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19430(int& val) {
    const int x = 19430;
    std::cout << 19430;
    for (int i = 0; i < 19430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19431(int& val) {
    func_in_19431(val);
    const int x = 19431;
    std::cout << 19431;
    for (int i = 0; i < 19431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19431(int& val) {
    const int x = 19431;
    std::cout << 19431;
    for (int i = 0; i < 19431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19432(int& val) {
    func_in_19432(val);
    const int x = 19432;
    std::cout << 19432;
    for (int i = 0; i < 19432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19432(int& val) {
    const int x = 19432;
    std::cout << 19432;
    for (int i = 0; i < 19432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19433(int& val) {
    func_in_19433(val);
    const int x = 19433;
    std::cout << 19433;
    for (int i = 0; i < 19433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19433(int& val) {
    const int x = 19433;
    std::cout << 19433;
    for (int i = 0; i < 19433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19434(int& val) {
    func_in_19434(val);
    const int x = 19434;
    std::cout << 19434;
    for (int i = 0; i < 19434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19434(int& val) {
    const int x = 19434;
    std::cout << 19434;
    for (int i = 0; i < 19434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19435(int& val) {
    func_in_19435(val);
    const int x = 19435;
    std::cout << 19435;
    for (int i = 0; i < 19435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19435(int& val) {
    const int x = 19435;
    std::cout << 19435;
    for (int i = 0; i < 19435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19436(int& val) {
    func_in_19436(val);
    const int x = 19436;
    std::cout << 19436;
    for (int i = 0; i < 19436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19436(int& val) {
    const int x = 19436;
    std::cout << 19436;
    for (int i = 0; i < 19436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19437(int& val) {
    func_in_19437(val);
    const int x = 19437;
    std::cout << 19437;
    for (int i = 0; i < 19437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19437(int& val) {
    const int x = 19437;
    std::cout << 19437;
    for (int i = 0; i < 19437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19438(int& val) {
    func_in_19438(val);
    const int x = 19438;
    std::cout << 19438;
    for (int i = 0; i < 19438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19438(int& val) {
    const int x = 19438;
    std::cout << 19438;
    for (int i = 0; i < 19438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19439(int& val) {
    func_in_19439(val);
    const int x = 19439;
    std::cout << 19439;
    for (int i = 0; i < 19439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19439(int& val) {
    const int x = 19439;
    std::cout << 19439;
    for (int i = 0; i < 19439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19440(int& val) {
    func_in_19440(val);
    const int x = 19440;
    std::cout << 19440;
    for (int i = 0; i < 19440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19440(int& val) {
    const int x = 19440;
    std::cout << 19440;
    for (int i = 0; i < 19440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19441(int& val) {
    func_in_19441(val);
    const int x = 19441;
    std::cout << 19441;
    for (int i = 0; i < 19441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19441(int& val) {
    const int x = 19441;
    std::cout << 19441;
    for (int i = 0; i < 19441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19442(int& val) {
    func_in_19442(val);
    const int x = 19442;
    std::cout << 19442;
    for (int i = 0; i < 19442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19442(int& val) {
    const int x = 19442;
    std::cout << 19442;
    for (int i = 0; i < 19442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19443(int& val) {
    func_in_19443(val);
    const int x = 19443;
    std::cout << 19443;
    for (int i = 0; i < 19443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19443(int& val) {
    const int x = 19443;
    std::cout << 19443;
    for (int i = 0; i < 19443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19444(int& val) {
    func_in_19444(val);
    const int x = 19444;
    std::cout << 19444;
    for (int i = 0; i < 19444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19444(int& val) {
    const int x = 19444;
    std::cout << 19444;
    for (int i = 0; i < 19444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19445(int& val) {
    func_in_19445(val);
    const int x = 19445;
    std::cout << 19445;
    for (int i = 0; i < 19445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19445(int& val) {
    const int x = 19445;
    std::cout << 19445;
    for (int i = 0; i < 19445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19446(int& val) {
    func_in_19446(val);
    const int x = 19446;
    std::cout << 19446;
    for (int i = 0; i < 19446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19446(int& val) {
    const int x = 19446;
    std::cout << 19446;
    for (int i = 0; i < 19446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19447(int& val) {
    func_in_19447(val);
    const int x = 19447;
    std::cout << 19447;
    for (int i = 0; i < 19447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19447(int& val) {
    const int x = 19447;
    std::cout << 19447;
    for (int i = 0; i < 19447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19448(int& val) {
    func_in_19448(val);
    const int x = 19448;
    std::cout << 19448;
    for (int i = 0; i < 19448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19448(int& val) {
    const int x = 19448;
    std::cout << 19448;
    for (int i = 0; i < 19448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19449(int& val) {
    func_in_19449(val);
    const int x = 19449;
    std::cout << 19449;
    for (int i = 0; i < 19449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19449(int& val) {
    const int x = 19449;
    std::cout << 19449;
    for (int i = 0; i < 19449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19450(int& val) {
    func_in_19450(val);
    const int x = 19450;
    std::cout << 19450;
    for (int i = 0; i < 19450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19450(int& val) {
    const int x = 19450;
    std::cout << 19450;
    for (int i = 0; i < 19450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19451(int& val) {
    func_in_19451(val);
    const int x = 19451;
    std::cout << 19451;
    for (int i = 0; i < 19451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19451(int& val) {
    const int x = 19451;
    std::cout << 19451;
    for (int i = 0; i < 19451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19452(int& val) {
    func_in_19452(val);
    const int x = 19452;
    std::cout << 19452;
    for (int i = 0; i < 19452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19452(int& val) {
    const int x = 19452;
    std::cout << 19452;
    for (int i = 0; i < 19452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19453(int& val) {
    func_in_19453(val);
    const int x = 19453;
    std::cout << 19453;
    for (int i = 0; i < 19453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19453(int& val) {
    const int x = 19453;
    std::cout << 19453;
    for (int i = 0; i < 19453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19454(int& val) {
    func_in_19454(val);
    const int x = 19454;
    std::cout << 19454;
    for (int i = 0; i < 19454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19454(int& val) {
    const int x = 19454;
    std::cout << 19454;
    for (int i = 0; i < 19454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19455(int& val) {
    func_in_19455(val);
    const int x = 19455;
    std::cout << 19455;
    for (int i = 0; i < 19455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19455(int& val) {
    const int x = 19455;
    std::cout << 19455;
    for (int i = 0; i < 19455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19456(int& val) {
    func_in_19456(val);
    const int x = 19456;
    std::cout << 19456;
    for (int i = 0; i < 19456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19456(int& val) {
    const int x = 19456;
    std::cout << 19456;
    for (int i = 0; i < 19456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19457(int& val) {
    func_in_19457(val);
    const int x = 19457;
    std::cout << 19457;
    for (int i = 0; i < 19457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19457(int& val) {
    const int x = 19457;
    std::cout << 19457;
    for (int i = 0; i < 19457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19458(int& val) {
    func_in_19458(val);
    const int x = 19458;
    std::cout << 19458;
    for (int i = 0; i < 19458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19458(int& val) {
    const int x = 19458;
    std::cout << 19458;
    for (int i = 0; i < 19458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19459(int& val) {
    func_in_19459(val);
    const int x = 19459;
    std::cout << 19459;
    for (int i = 0; i < 19459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19459(int& val) {
    const int x = 19459;
    std::cout << 19459;
    for (int i = 0; i < 19459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19460(int& val) {
    func_in_19460(val);
    const int x = 19460;
    std::cout << 19460;
    for (int i = 0; i < 19460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19460(int& val) {
    const int x = 19460;
    std::cout << 19460;
    for (int i = 0; i < 19460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19461(int& val) {
    func_in_19461(val);
    const int x = 19461;
    std::cout << 19461;
    for (int i = 0; i < 19461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19461(int& val) {
    const int x = 19461;
    std::cout << 19461;
    for (int i = 0; i < 19461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19462(int& val) {
    func_in_19462(val);
    const int x = 19462;
    std::cout << 19462;
    for (int i = 0; i < 19462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19462(int& val) {
    const int x = 19462;
    std::cout << 19462;
    for (int i = 0; i < 19462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19463(int& val) {
    func_in_19463(val);
    const int x = 19463;
    std::cout << 19463;
    for (int i = 0; i < 19463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19463(int& val) {
    const int x = 19463;
    std::cout << 19463;
    for (int i = 0; i < 19463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19464(int& val) {
    func_in_19464(val);
    const int x = 19464;
    std::cout << 19464;
    for (int i = 0; i < 19464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19464(int& val) {
    const int x = 19464;
    std::cout << 19464;
    for (int i = 0; i < 19464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19465(int& val) {
    func_in_19465(val);
    const int x = 19465;
    std::cout << 19465;
    for (int i = 0; i < 19465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19465(int& val) {
    const int x = 19465;
    std::cout << 19465;
    for (int i = 0; i < 19465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19466(int& val) {
    func_in_19466(val);
    const int x = 19466;
    std::cout << 19466;
    for (int i = 0; i < 19466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19466(int& val) {
    const int x = 19466;
    std::cout << 19466;
    for (int i = 0; i < 19466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19467(int& val) {
    func_in_19467(val);
    const int x = 19467;
    std::cout << 19467;
    for (int i = 0; i < 19467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19467(int& val) {
    const int x = 19467;
    std::cout << 19467;
    for (int i = 0; i < 19467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19468(int& val) {
    func_in_19468(val);
    const int x = 19468;
    std::cout << 19468;
    for (int i = 0; i < 19468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19468(int& val) {
    const int x = 19468;
    std::cout << 19468;
    for (int i = 0; i < 19468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19469(int& val) {
    func_in_19469(val);
    const int x = 19469;
    std::cout << 19469;
    for (int i = 0; i < 19469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19469(int& val) {
    const int x = 19469;
    std::cout << 19469;
    for (int i = 0; i < 19469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19470(int& val) {
    func_in_19470(val);
    const int x = 19470;
    std::cout << 19470;
    for (int i = 0; i < 19470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19470(int& val) {
    const int x = 19470;
    std::cout << 19470;
    for (int i = 0; i < 19470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19471(int& val) {
    func_in_19471(val);
    const int x = 19471;
    std::cout << 19471;
    for (int i = 0; i < 19471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19471(int& val) {
    const int x = 19471;
    std::cout << 19471;
    for (int i = 0; i < 19471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19472(int& val) {
    func_in_19472(val);
    const int x = 19472;
    std::cout << 19472;
    for (int i = 0; i < 19472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19472(int& val) {
    const int x = 19472;
    std::cout << 19472;
    for (int i = 0; i < 19472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19473(int& val) {
    func_in_19473(val);
    const int x = 19473;
    std::cout << 19473;
    for (int i = 0; i < 19473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19473(int& val) {
    const int x = 19473;
    std::cout << 19473;
    for (int i = 0; i < 19473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19474(int& val) {
    func_in_19474(val);
    const int x = 19474;
    std::cout << 19474;
    for (int i = 0; i < 19474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19474(int& val) {
    const int x = 19474;
    std::cout << 19474;
    for (int i = 0; i < 19474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19475(int& val) {
    func_in_19475(val);
    const int x = 19475;
    std::cout << 19475;
    for (int i = 0; i < 19475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19475(int& val) {
    const int x = 19475;
    std::cout << 19475;
    for (int i = 0; i < 19475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19476(int& val) {
    func_in_19476(val);
    const int x = 19476;
    std::cout << 19476;
    for (int i = 0; i < 19476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19476(int& val) {
    const int x = 19476;
    std::cout << 19476;
    for (int i = 0; i < 19476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19477(int& val) {
    func_in_19477(val);
    const int x = 19477;
    std::cout << 19477;
    for (int i = 0; i < 19477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19477(int& val) {
    const int x = 19477;
    std::cout << 19477;
    for (int i = 0; i < 19477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19478(int& val) {
    func_in_19478(val);
    const int x = 19478;
    std::cout << 19478;
    for (int i = 0; i < 19478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19478(int& val) {
    const int x = 19478;
    std::cout << 19478;
    for (int i = 0; i < 19478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19479(int& val) {
    func_in_19479(val);
    const int x = 19479;
    std::cout << 19479;
    for (int i = 0; i < 19479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19479(int& val) {
    const int x = 19479;
    std::cout << 19479;
    for (int i = 0; i < 19479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19480(int& val) {
    func_in_19480(val);
    const int x = 19480;
    std::cout << 19480;
    for (int i = 0; i < 19480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19480(int& val) {
    const int x = 19480;
    std::cout << 19480;
    for (int i = 0; i < 19480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19481(int& val) {
    func_in_19481(val);
    const int x = 19481;
    std::cout << 19481;
    for (int i = 0; i < 19481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19481(int& val) {
    const int x = 19481;
    std::cout << 19481;
    for (int i = 0; i < 19481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19482(int& val) {
    func_in_19482(val);
    const int x = 19482;
    std::cout << 19482;
    for (int i = 0; i < 19482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19482(int& val) {
    const int x = 19482;
    std::cout << 19482;
    for (int i = 0; i < 19482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19483(int& val) {
    func_in_19483(val);
    const int x = 19483;
    std::cout << 19483;
    for (int i = 0; i < 19483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19483(int& val) {
    const int x = 19483;
    std::cout << 19483;
    for (int i = 0; i < 19483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19484(int& val) {
    func_in_19484(val);
    const int x = 19484;
    std::cout << 19484;
    for (int i = 0; i < 19484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19484(int& val) {
    const int x = 19484;
    std::cout << 19484;
    for (int i = 0; i < 19484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19485(int& val) {
    func_in_19485(val);
    const int x = 19485;
    std::cout << 19485;
    for (int i = 0; i < 19485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19485(int& val) {
    const int x = 19485;
    std::cout << 19485;
    for (int i = 0; i < 19485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19486(int& val) {
    func_in_19486(val);
    const int x = 19486;
    std::cout << 19486;
    for (int i = 0; i < 19486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19486(int& val) {
    const int x = 19486;
    std::cout << 19486;
    for (int i = 0; i < 19486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19487(int& val) {
    func_in_19487(val);
    const int x = 19487;
    std::cout << 19487;
    for (int i = 0; i < 19487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19487(int& val) {
    const int x = 19487;
    std::cout << 19487;
    for (int i = 0; i < 19487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19488(int& val) {
    func_in_19488(val);
    const int x = 19488;
    std::cout << 19488;
    for (int i = 0; i < 19488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19488(int& val) {
    const int x = 19488;
    std::cout << 19488;
    for (int i = 0; i < 19488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19489(int& val) {
    func_in_19489(val);
    const int x = 19489;
    std::cout << 19489;
    for (int i = 0; i < 19489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19489(int& val) {
    const int x = 19489;
    std::cout << 19489;
    for (int i = 0; i < 19489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19490(int& val) {
    func_in_19490(val);
    const int x = 19490;
    std::cout << 19490;
    for (int i = 0; i < 19490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19490(int& val) {
    const int x = 19490;
    std::cout << 19490;
    for (int i = 0; i < 19490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19491(int& val) {
    func_in_19491(val);
    const int x = 19491;
    std::cout << 19491;
    for (int i = 0; i < 19491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19491(int& val) {
    const int x = 19491;
    std::cout << 19491;
    for (int i = 0; i < 19491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19492(int& val) {
    func_in_19492(val);
    const int x = 19492;
    std::cout << 19492;
    for (int i = 0; i < 19492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19492(int& val) {
    const int x = 19492;
    std::cout << 19492;
    for (int i = 0; i < 19492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19493(int& val) {
    func_in_19493(val);
    const int x = 19493;
    std::cout << 19493;
    for (int i = 0; i < 19493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19493(int& val) {
    const int x = 19493;
    std::cout << 19493;
    for (int i = 0; i < 19493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19494(int& val) {
    func_in_19494(val);
    const int x = 19494;
    std::cout << 19494;
    for (int i = 0; i < 19494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19494(int& val) {
    const int x = 19494;
    std::cout << 19494;
    for (int i = 0; i < 19494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19495(int& val) {
    func_in_19495(val);
    const int x = 19495;
    std::cout << 19495;
    for (int i = 0; i < 19495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19495(int& val) {
    const int x = 19495;
    std::cout << 19495;
    for (int i = 0; i < 19495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19496(int& val) {
    func_in_19496(val);
    const int x = 19496;
    std::cout << 19496;
    for (int i = 0; i < 19496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19496(int& val) {
    const int x = 19496;
    std::cout << 19496;
    for (int i = 0; i < 19496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19497(int& val) {
    func_in_19497(val);
    const int x = 19497;
    std::cout << 19497;
    for (int i = 0; i < 19497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19497(int& val) {
    const int x = 19497;
    std::cout << 19497;
    for (int i = 0; i < 19497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19498(int& val) {
    func_in_19498(val);
    const int x = 19498;
    std::cout << 19498;
    for (int i = 0; i < 19498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19498(int& val) {
    const int x = 19498;
    std::cout << 19498;
    for (int i = 0; i < 19498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19499(int& val) {
    func_in_19499(val);
    const int x = 19499;
    std::cout << 19499;
    for (int i = 0; i < 19499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19499(int& val) {
    const int x = 19499;
    std::cout << 19499;
    for (int i = 0; i < 19499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19500(int& val) {
    func_in_19500(val);
    const int x = 19500;
    std::cout << 19500;
    for (int i = 0; i < 19500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19500(int& val) {
    const int x = 19500;
    std::cout << 19500;
    for (int i = 0; i < 19500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19501(int& val) {
    func_in_19501(val);
    const int x = 19501;
    std::cout << 19501;
    for (int i = 0; i < 19501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19501(int& val) {
    const int x = 19501;
    std::cout << 19501;
    for (int i = 0; i < 19501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19502(int& val) {
    func_in_19502(val);
    const int x = 19502;
    std::cout << 19502;
    for (int i = 0; i < 19502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19502(int& val) {
    const int x = 19502;
    std::cout << 19502;
    for (int i = 0; i < 19502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19503(int& val) {
    func_in_19503(val);
    const int x = 19503;
    std::cout << 19503;
    for (int i = 0; i < 19503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19503(int& val) {
    const int x = 19503;
    std::cout << 19503;
    for (int i = 0; i < 19503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19504(int& val) {
    func_in_19504(val);
    const int x = 19504;
    std::cout << 19504;
    for (int i = 0; i < 19504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19504(int& val) {
    const int x = 19504;
    std::cout << 19504;
    for (int i = 0; i < 19504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19505(int& val) {
    func_in_19505(val);
    const int x = 19505;
    std::cout << 19505;
    for (int i = 0; i < 19505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19505(int& val) {
    const int x = 19505;
    std::cout << 19505;
    for (int i = 0; i < 19505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19506(int& val) {
    func_in_19506(val);
    const int x = 19506;
    std::cout << 19506;
    for (int i = 0; i < 19506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19506(int& val) {
    const int x = 19506;
    std::cout << 19506;
    for (int i = 0; i < 19506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19507(int& val) {
    func_in_19507(val);
    const int x = 19507;
    std::cout << 19507;
    for (int i = 0; i < 19507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19507(int& val) {
    const int x = 19507;
    std::cout << 19507;
    for (int i = 0; i < 19507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19508(int& val) {
    func_in_19508(val);
    const int x = 19508;
    std::cout << 19508;
    for (int i = 0; i < 19508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19508(int& val) {
    const int x = 19508;
    std::cout << 19508;
    for (int i = 0; i < 19508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19509(int& val) {
    func_in_19509(val);
    const int x = 19509;
    std::cout << 19509;
    for (int i = 0; i < 19509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19509(int& val) {
    const int x = 19509;
    std::cout << 19509;
    for (int i = 0; i < 19509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19510(int& val) {
    func_in_19510(val);
    const int x = 19510;
    std::cout << 19510;
    for (int i = 0; i < 19510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19510(int& val) {
    const int x = 19510;
    std::cout << 19510;
    for (int i = 0; i < 19510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19511(int& val) {
    func_in_19511(val);
    const int x = 19511;
    std::cout << 19511;
    for (int i = 0; i < 19511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19511(int& val) {
    const int x = 19511;
    std::cout << 19511;
    for (int i = 0; i < 19511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19512(int& val) {
    func_in_19512(val);
    const int x = 19512;
    std::cout << 19512;
    for (int i = 0; i < 19512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19512(int& val) {
    const int x = 19512;
    std::cout << 19512;
    for (int i = 0; i < 19512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19513(int& val) {
    func_in_19513(val);
    const int x = 19513;
    std::cout << 19513;
    for (int i = 0; i < 19513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19513(int& val) {
    const int x = 19513;
    std::cout << 19513;
    for (int i = 0; i < 19513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19514(int& val) {
    func_in_19514(val);
    const int x = 19514;
    std::cout << 19514;
    for (int i = 0; i < 19514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19514(int& val) {
    const int x = 19514;
    std::cout << 19514;
    for (int i = 0; i < 19514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19515(int& val) {
    func_in_19515(val);
    const int x = 19515;
    std::cout << 19515;
    for (int i = 0; i < 19515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19515(int& val) {
    const int x = 19515;
    std::cout << 19515;
    for (int i = 0; i < 19515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19516(int& val) {
    func_in_19516(val);
    const int x = 19516;
    std::cout << 19516;
    for (int i = 0; i < 19516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19516(int& val) {
    const int x = 19516;
    std::cout << 19516;
    for (int i = 0; i < 19516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19517(int& val) {
    func_in_19517(val);
    const int x = 19517;
    std::cout << 19517;
    for (int i = 0; i < 19517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19517(int& val) {
    const int x = 19517;
    std::cout << 19517;
    for (int i = 0; i < 19517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19518(int& val) {
    func_in_19518(val);
    const int x = 19518;
    std::cout << 19518;
    for (int i = 0; i < 19518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19518(int& val) {
    const int x = 19518;
    std::cout << 19518;
    for (int i = 0; i < 19518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19519(int& val) {
    func_in_19519(val);
    const int x = 19519;
    std::cout << 19519;
    for (int i = 0; i < 19519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19519(int& val) {
    const int x = 19519;
    std::cout << 19519;
    for (int i = 0; i < 19519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19520(int& val) {
    func_in_19520(val);
    const int x = 19520;
    std::cout << 19520;
    for (int i = 0; i < 19520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19520(int& val) {
    const int x = 19520;
    std::cout << 19520;
    for (int i = 0; i < 19520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19521(int& val) {
    func_in_19521(val);
    const int x = 19521;
    std::cout << 19521;
    for (int i = 0; i < 19521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19521(int& val) {
    const int x = 19521;
    std::cout << 19521;
    for (int i = 0; i < 19521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19522(int& val) {
    func_in_19522(val);
    const int x = 19522;
    std::cout << 19522;
    for (int i = 0; i < 19522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19522(int& val) {
    const int x = 19522;
    std::cout << 19522;
    for (int i = 0; i < 19522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19523(int& val) {
    func_in_19523(val);
    const int x = 19523;
    std::cout << 19523;
    for (int i = 0; i < 19523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19523(int& val) {
    const int x = 19523;
    std::cout << 19523;
    for (int i = 0; i < 19523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19524(int& val) {
    func_in_19524(val);
    const int x = 19524;
    std::cout << 19524;
    for (int i = 0; i < 19524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19524(int& val) {
    const int x = 19524;
    std::cout << 19524;
    for (int i = 0; i < 19524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19525(int& val) {
    func_in_19525(val);
    const int x = 19525;
    std::cout << 19525;
    for (int i = 0; i < 19525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19525(int& val) {
    const int x = 19525;
    std::cout << 19525;
    for (int i = 0; i < 19525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19526(int& val) {
    func_in_19526(val);
    const int x = 19526;
    std::cout << 19526;
    for (int i = 0; i < 19526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19526(int& val) {
    const int x = 19526;
    std::cout << 19526;
    for (int i = 0; i < 19526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19527(int& val) {
    func_in_19527(val);
    const int x = 19527;
    std::cout << 19527;
    for (int i = 0; i < 19527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19527(int& val) {
    const int x = 19527;
    std::cout << 19527;
    for (int i = 0; i < 19527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19528(int& val) {
    func_in_19528(val);
    const int x = 19528;
    std::cout << 19528;
    for (int i = 0; i < 19528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19528(int& val) {
    const int x = 19528;
    std::cout << 19528;
    for (int i = 0; i < 19528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19529(int& val) {
    func_in_19529(val);
    const int x = 19529;
    std::cout << 19529;
    for (int i = 0; i < 19529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19529(int& val) {
    const int x = 19529;
    std::cout << 19529;
    for (int i = 0; i < 19529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19530(int& val) {
    func_in_19530(val);
    const int x = 19530;
    std::cout << 19530;
    for (int i = 0; i < 19530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19530(int& val) {
    const int x = 19530;
    std::cout << 19530;
    for (int i = 0; i < 19530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19531(int& val) {
    func_in_19531(val);
    const int x = 19531;
    std::cout << 19531;
    for (int i = 0; i < 19531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19531(int& val) {
    const int x = 19531;
    std::cout << 19531;
    for (int i = 0; i < 19531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19532(int& val) {
    func_in_19532(val);
    const int x = 19532;
    std::cout << 19532;
    for (int i = 0; i < 19532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19532(int& val) {
    const int x = 19532;
    std::cout << 19532;
    for (int i = 0; i < 19532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19533(int& val) {
    func_in_19533(val);
    const int x = 19533;
    std::cout << 19533;
    for (int i = 0; i < 19533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19533(int& val) {
    const int x = 19533;
    std::cout << 19533;
    for (int i = 0; i < 19533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19534(int& val) {
    func_in_19534(val);
    const int x = 19534;
    std::cout << 19534;
    for (int i = 0; i < 19534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19534(int& val) {
    const int x = 19534;
    std::cout << 19534;
    for (int i = 0; i < 19534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19535(int& val) {
    func_in_19535(val);
    const int x = 19535;
    std::cout << 19535;
    for (int i = 0; i < 19535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19535(int& val) {
    const int x = 19535;
    std::cout << 19535;
    for (int i = 0; i < 19535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19536(int& val) {
    func_in_19536(val);
    const int x = 19536;
    std::cout << 19536;
    for (int i = 0; i < 19536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19536(int& val) {
    const int x = 19536;
    std::cout << 19536;
    for (int i = 0; i < 19536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19537(int& val) {
    func_in_19537(val);
    const int x = 19537;
    std::cout << 19537;
    for (int i = 0; i < 19537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19537(int& val) {
    const int x = 19537;
    std::cout << 19537;
    for (int i = 0; i < 19537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19538(int& val) {
    func_in_19538(val);
    const int x = 19538;
    std::cout << 19538;
    for (int i = 0; i < 19538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19538(int& val) {
    const int x = 19538;
    std::cout << 19538;
    for (int i = 0; i < 19538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19539(int& val) {
    func_in_19539(val);
    const int x = 19539;
    std::cout << 19539;
    for (int i = 0; i < 19539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19539(int& val) {
    const int x = 19539;
    std::cout << 19539;
    for (int i = 0; i < 19539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19540(int& val) {
    func_in_19540(val);
    const int x = 19540;
    std::cout << 19540;
    for (int i = 0; i < 19540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19540(int& val) {
    const int x = 19540;
    std::cout << 19540;
    for (int i = 0; i < 19540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19541(int& val) {
    func_in_19541(val);
    const int x = 19541;
    std::cout << 19541;
    for (int i = 0; i < 19541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19541(int& val) {
    const int x = 19541;
    std::cout << 19541;
    for (int i = 0; i < 19541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19542(int& val) {
    func_in_19542(val);
    const int x = 19542;
    std::cout << 19542;
    for (int i = 0; i < 19542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19542(int& val) {
    const int x = 19542;
    std::cout << 19542;
    for (int i = 0; i < 19542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19543(int& val) {
    func_in_19543(val);
    const int x = 19543;
    std::cout << 19543;
    for (int i = 0; i < 19543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19543(int& val) {
    const int x = 19543;
    std::cout << 19543;
    for (int i = 0; i < 19543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19544(int& val) {
    func_in_19544(val);
    const int x = 19544;
    std::cout << 19544;
    for (int i = 0; i < 19544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19544(int& val) {
    const int x = 19544;
    std::cout << 19544;
    for (int i = 0; i < 19544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19545(int& val) {
    func_in_19545(val);
    const int x = 19545;
    std::cout << 19545;
    for (int i = 0; i < 19545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19545(int& val) {
    const int x = 19545;
    std::cout << 19545;
    for (int i = 0; i < 19545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19546(int& val) {
    func_in_19546(val);
    const int x = 19546;
    std::cout << 19546;
    for (int i = 0; i < 19546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19546(int& val) {
    const int x = 19546;
    std::cout << 19546;
    for (int i = 0; i < 19546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19547(int& val) {
    func_in_19547(val);
    const int x = 19547;
    std::cout << 19547;
    for (int i = 0; i < 19547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19547(int& val) {
    const int x = 19547;
    std::cout << 19547;
    for (int i = 0; i < 19547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19548(int& val) {
    func_in_19548(val);
    const int x = 19548;
    std::cout << 19548;
    for (int i = 0; i < 19548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19548(int& val) {
    const int x = 19548;
    std::cout << 19548;
    for (int i = 0; i < 19548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19549(int& val) {
    func_in_19549(val);
    const int x = 19549;
    std::cout << 19549;
    for (int i = 0; i < 19549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19549(int& val) {
    const int x = 19549;
    std::cout << 19549;
    for (int i = 0; i < 19549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19550(int& val) {
    func_in_19550(val);
    const int x = 19550;
    std::cout << 19550;
    for (int i = 0; i < 19550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19550(int& val) {
    const int x = 19550;
    std::cout << 19550;
    for (int i = 0; i < 19550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19551(int& val) {
    func_in_19551(val);
    const int x = 19551;
    std::cout << 19551;
    for (int i = 0; i < 19551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19551(int& val) {
    const int x = 19551;
    std::cout << 19551;
    for (int i = 0; i < 19551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19552(int& val) {
    func_in_19552(val);
    const int x = 19552;
    std::cout << 19552;
    for (int i = 0; i < 19552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19552(int& val) {
    const int x = 19552;
    std::cout << 19552;
    for (int i = 0; i < 19552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19553(int& val) {
    func_in_19553(val);
    const int x = 19553;
    std::cout << 19553;
    for (int i = 0; i < 19553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19553(int& val) {
    const int x = 19553;
    std::cout << 19553;
    for (int i = 0; i < 19553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19554(int& val) {
    func_in_19554(val);
    const int x = 19554;
    std::cout << 19554;
    for (int i = 0; i < 19554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19554(int& val) {
    const int x = 19554;
    std::cout << 19554;
    for (int i = 0; i < 19554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19555(int& val) {
    func_in_19555(val);
    const int x = 19555;
    std::cout << 19555;
    for (int i = 0; i < 19555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19555(int& val) {
    const int x = 19555;
    std::cout << 19555;
    for (int i = 0; i < 19555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19556(int& val) {
    func_in_19556(val);
    const int x = 19556;
    std::cout << 19556;
    for (int i = 0; i < 19556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19556(int& val) {
    const int x = 19556;
    std::cout << 19556;
    for (int i = 0; i < 19556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19557(int& val) {
    func_in_19557(val);
    const int x = 19557;
    std::cout << 19557;
    for (int i = 0; i < 19557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19557(int& val) {
    const int x = 19557;
    std::cout << 19557;
    for (int i = 0; i < 19557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19558(int& val) {
    func_in_19558(val);
    const int x = 19558;
    std::cout << 19558;
    for (int i = 0; i < 19558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19558(int& val) {
    const int x = 19558;
    std::cout << 19558;
    for (int i = 0; i < 19558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19559(int& val) {
    func_in_19559(val);
    const int x = 19559;
    std::cout << 19559;
    for (int i = 0; i < 19559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19559(int& val) {
    const int x = 19559;
    std::cout << 19559;
    for (int i = 0; i < 19559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19560(int& val) {
    func_in_19560(val);
    const int x = 19560;
    std::cout << 19560;
    for (int i = 0; i < 19560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19560(int& val) {
    const int x = 19560;
    std::cout << 19560;
    for (int i = 0; i < 19560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19561(int& val) {
    func_in_19561(val);
    const int x = 19561;
    std::cout << 19561;
    for (int i = 0; i < 19561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19561(int& val) {
    const int x = 19561;
    std::cout << 19561;
    for (int i = 0; i < 19561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19562(int& val) {
    func_in_19562(val);
    const int x = 19562;
    std::cout << 19562;
    for (int i = 0; i < 19562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19562(int& val) {
    const int x = 19562;
    std::cout << 19562;
    for (int i = 0; i < 19562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19563(int& val) {
    func_in_19563(val);
    const int x = 19563;
    std::cout << 19563;
    for (int i = 0; i < 19563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19563(int& val) {
    const int x = 19563;
    std::cout << 19563;
    for (int i = 0; i < 19563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19564(int& val) {
    func_in_19564(val);
    const int x = 19564;
    std::cout << 19564;
    for (int i = 0; i < 19564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19564(int& val) {
    const int x = 19564;
    std::cout << 19564;
    for (int i = 0; i < 19564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19565(int& val) {
    func_in_19565(val);
    const int x = 19565;
    std::cout << 19565;
    for (int i = 0; i < 19565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19565(int& val) {
    const int x = 19565;
    std::cout << 19565;
    for (int i = 0; i < 19565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19566(int& val) {
    func_in_19566(val);
    const int x = 19566;
    std::cout << 19566;
    for (int i = 0; i < 19566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19566(int& val) {
    const int x = 19566;
    std::cout << 19566;
    for (int i = 0; i < 19566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19567(int& val) {
    func_in_19567(val);
    const int x = 19567;
    std::cout << 19567;
    for (int i = 0; i < 19567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19567(int& val) {
    const int x = 19567;
    std::cout << 19567;
    for (int i = 0; i < 19567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19568(int& val) {
    func_in_19568(val);
    const int x = 19568;
    std::cout << 19568;
    for (int i = 0; i < 19568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19568(int& val) {
    const int x = 19568;
    std::cout << 19568;
    for (int i = 0; i < 19568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19569(int& val) {
    func_in_19569(val);
    const int x = 19569;
    std::cout << 19569;
    for (int i = 0; i < 19569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19569(int& val) {
    const int x = 19569;
    std::cout << 19569;
    for (int i = 0; i < 19569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19570(int& val) {
    func_in_19570(val);
    const int x = 19570;
    std::cout << 19570;
    for (int i = 0; i < 19570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19570(int& val) {
    const int x = 19570;
    std::cout << 19570;
    for (int i = 0; i < 19570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19571(int& val) {
    func_in_19571(val);
    const int x = 19571;
    std::cout << 19571;
    for (int i = 0; i < 19571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19571(int& val) {
    const int x = 19571;
    std::cout << 19571;
    for (int i = 0; i < 19571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19572(int& val) {
    func_in_19572(val);
    const int x = 19572;
    std::cout << 19572;
    for (int i = 0; i < 19572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19572(int& val) {
    const int x = 19572;
    std::cout << 19572;
    for (int i = 0; i < 19572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19573(int& val) {
    func_in_19573(val);
    const int x = 19573;
    std::cout << 19573;
    for (int i = 0; i < 19573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19573(int& val) {
    const int x = 19573;
    std::cout << 19573;
    for (int i = 0; i < 19573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19574(int& val) {
    func_in_19574(val);
    const int x = 19574;
    std::cout << 19574;
    for (int i = 0; i < 19574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19574(int& val) {
    const int x = 19574;
    std::cout << 19574;
    for (int i = 0; i < 19574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19575(int& val) {
    func_in_19575(val);
    const int x = 19575;
    std::cout << 19575;
    for (int i = 0; i < 19575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19575(int& val) {
    const int x = 19575;
    std::cout << 19575;
    for (int i = 0; i < 19575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19576(int& val) {
    func_in_19576(val);
    const int x = 19576;
    std::cout << 19576;
    for (int i = 0; i < 19576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19576(int& val) {
    const int x = 19576;
    std::cout << 19576;
    for (int i = 0; i < 19576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19577(int& val) {
    func_in_19577(val);
    const int x = 19577;
    std::cout << 19577;
    for (int i = 0; i < 19577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19577(int& val) {
    const int x = 19577;
    std::cout << 19577;
    for (int i = 0; i < 19577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19578(int& val) {
    func_in_19578(val);
    const int x = 19578;
    std::cout << 19578;
    for (int i = 0; i < 19578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19578(int& val) {
    const int x = 19578;
    std::cout << 19578;
    for (int i = 0; i < 19578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19579(int& val) {
    func_in_19579(val);
    const int x = 19579;
    std::cout << 19579;
    for (int i = 0; i < 19579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19579(int& val) {
    const int x = 19579;
    std::cout << 19579;
    for (int i = 0; i < 19579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19580(int& val) {
    func_in_19580(val);
    const int x = 19580;
    std::cout << 19580;
    for (int i = 0; i < 19580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19580(int& val) {
    const int x = 19580;
    std::cout << 19580;
    for (int i = 0; i < 19580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19581(int& val) {
    func_in_19581(val);
    const int x = 19581;
    std::cout << 19581;
    for (int i = 0; i < 19581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19581(int& val) {
    const int x = 19581;
    std::cout << 19581;
    for (int i = 0; i < 19581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19582(int& val) {
    func_in_19582(val);
    const int x = 19582;
    std::cout << 19582;
    for (int i = 0; i < 19582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19582(int& val) {
    const int x = 19582;
    std::cout << 19582;
    for (int i = 0; i < 19582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19583(int& val) {
    func_in_19583(val);
    const int x = 19583;
    std::cout << 19583;
    for (int i = 0; i < 19583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19583(int& val) {
    const int x = 19583;
    std::cout << 19583;
    for (int i = 0; i < 19583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19584(int& val) {
    func_in_19584(val);
    const int x = 19584;
    std::cout << 19584;
    for (int i = 0; i < 19584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19584(int& val) {
    const int x = 19584;
    std::cout << 19584;
    for (int i = 0; i < 19584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19585(int& val) {
    func_in_19585(val);
    const int x = 19585;
    std::cout << 19585;
    for (int i = 0; i < 19585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19585(int& val) {
    const int x = 19585;
    std::cout << 19585;
    for (int i = 0; i < 19585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19586(int& val) {
    func_in_19586(val);
    const int x = 19586;
    std::cout << 19586;
    for (int i = 0; i < 19586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19586(int& val) {
    const int x = 19586;
    std::cout << 19586;
    for (int i = 0; i < 19586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19587(int& val) {
    func_in_19587(val);
    const int x = 19587;
    std::cout << 19587;
    for (int i = 0; i < 19587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19587(int& val) {
    const int x = 19587;
    std::cout << 19587;
    for (int i = 0; i < 19587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19588(int& val) {
    func_in_19588(val);
    const int x = 19588;
    std::cout << 19588;
    for (int i = 0; i < 19588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19588(int& val) {
    const int x = 19588;
    std::cout << 19588;
    for (int i = 0; i < 19588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19589(int& val) {
    func_in_19589(val);
    const int x = 19589;
    std::cout << 19589;
    for (int i = 0; i < 19589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19589(int& val) {
    const int x = 19589;
    std::cout << 19589;
    for (int i = 0; i < 19589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19590(int& val) {
    func_in_19590(val);
    const int x = 19590;
    std::cout << 19590;
    for (int i = 0; i < 19590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19590(int& val) {
    const int x = 19590;
    std::cout << 19590;
    for (int i = 0; i < 19590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19591(int& val) {
    func_in_19591(val);
    const int x = 19591;
    std::cout << 19591;
    for (int i = 0; i < 19591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19591(int& val) {
    const int x = 19591;
    std::cout << 19591;
    for (int i = 0; i < 19591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19592(int& val) {
    func_in_19592(val);
    const int x = 19592;
    std::cout << 19592;
    for (int i = 0; i < 19592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19592(int& val) {
    const int x = 19592;
    std::cout << 19592;
    for (int i = 0; i < 19592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19593(int& val) {
    func_in_19593(val);
    const int x = 19593;
    std::cout << 19593;
    for (int i = 0; i < 19593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19593(int& val) {
    const int x = 19593;
    std::cout << 19593;
    for (int i = 0; i < 19593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19594(int& val) {
    func_in_19594(val);
    const int x = 19594;
    std::cout << 19594;
    for (int i = 0; i < 19594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19594(int& val) {
    const int x = 19594;
    std::cout << 19594;
    for (int i = 0; i < 19594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19595(int& val) {
    func_in_19595(val);
    const int x = 19595;
    std::cout << 19595;
    for (int i = 0; i < 19595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19595(int& val) {
    const int x = 19595;
    std::cout << 19595;
    for (int i = 0; i < 19595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19596(int& val) {
    func_in_19596(val);
    const int x = 19596;
    std::cout << 19596;
    for (int i = 0; i < 19596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19596(int& val) {
    const int x = 19596;
    std::cout << 19596;
    for (int i = 0; i < 19596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19597(int& val) {
    func_in_19597(val);
    const int x = 19597;
    std::cout << 19597;
    for (int i = 0; i < 19597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19597(int& val) {
    const int x = 19597;
    std::cout << 19597;
    for (int i = 0; i < 19597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19598(int& val) {
    func_in_19598(val);
    const int x = 19598;
    std::cout << 19598;
    for (int i = 0; i < 19598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19598(int& val) {
    const int x = 19598;
    std::cout << 19598;
    for (int i = 0; i < 19598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19599(int& val) {
    func_in_19599(val);
    const int x = 19599;
    std::cout << 19599;
    for (int i = 0; i < 19599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19599(int& val) {
    const int x = 19599;
    std::cout << 19599;
    for (int i = 0; i < 19599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19600(int& val) {
    func_in_19600(val);
    const int x = 19600;
    std::cout << 19600;
    for (int i = 0; i < 19600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19600(int& val) {
    const int x = 19600;
    std::cout << 19600;
    for (int i = 0; i < 19600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19601(int& val) {
    func_in_19601(val);
    const int x = 19601;
    std::cout << 19601;
    for (int i = 0; i < 19601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19601(int& val) {
    const int x = 19601;
    std::cout << 19601;
    for (int i = 0; i < 19601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19602(int& val) {
    func_in_19602(val);
    const int x = 19602;
    std::cout << 19602;
    for (int i = 0; i < 19602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19602(int& val) {
    const int x = 19602;
    std::cout << 19602;
    for (int i = 0; i < 19602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19603(int& val) {
    func_in_19603(val);
    const int x = 19603;
    std::cout << 19603;
    for (int i = 0; i < 19603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19603(int& val) {
    const int x = 19603;
    std::cout << 19603;
    for (int i = 0; i < 19603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19604(int& val) {
    func_in_19604(val);
    const int x = 19604;
    std::cout << 19604;
    for (int i = 0; i < 19604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19604(int& val) {
    const int x = 19604;
    std::cout << 19604;
    for (int i = 0; i < 19604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19605(int& val) {
    func_in_19605(val);
    const int x = 19605;
    std::cout << 19605;
    for (int i = 0; i < 19605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19605(int& val) {
    const int x = 19605;
    std::cout << 19605;
    for (int i = 0; i < 19605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19606(int& val) {
    func_in_19606(val);
    const int x = 19606;
    std::cout << 19606;
    for (int i = 0; i < 19606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19606(int& val) {
    const int x = 19606;
    std::cout << 19606;
    for (int i = 0; i < 19606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19607(int& val) {
    func_in_19607(val);
    const int x = 19607;
    std::cout << 19607;
    for (int i = 0; i < 19607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19607(int& val) {
    const int x = 19607;
    std::cout << 19607;
    for (int i = 0; i < 19607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19608(int& val) {
    func_in_19608(val);
    const int x = 19608;
    std::cout << 19608;
    for (int i = 0; i < 19608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19608(int& val) {
    const int x = 19608;
    std::cout << 19608;
    for (int i = 0; i < 19608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19609(int& val) {
    func_in_19609(val);
    const int x = 19609;
    std::cout << 19609;
    for (int i = 0; i < 19609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19609(int& val) {
    const int x = 19609;
    std::cout << 19609;
    for (int i = 0; i < 19609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19610(int& val) {
    func_in_19610(val);
    const int x = 19610;
    std::cout << 19610;
    for (int i = 0; i < 19610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19610(int& val) {
    const int x = 19610;
    std::cout << 19610;
    for (int i = 0; i < 19610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19611(int& val) {
    func_in_19611(val);
    const int x = 19611;
    std::cout << 19611;
    for (int i = 0; i < 19611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19611(int& val) {
    const int x = 19611;
    std::cout << 19611;
    for (int i = 0; i < 19611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19612(int& val) {
    func_in_19612(val);
    const int x = 19612;
    std::cout << 19612;
    for (int i = 0; i < 19612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19612(int& val) {
    const int x = 19612;
    std::cout << 19612;
    for (int i = 0; i < 19612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19613(int& val) {
    func_in_19613(val);
    const int x = 19613;
    std::cout << 19613;
    for (int i = 0; i < 19613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19613(int& val) {
    const int x = 19613;
    std::cout << 19613;
    for (int i = 0; i < 19613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19614(int& val) {
    func_in_19614(val);
    const int x = 19614;
    std::cout << 19614;
    for (int i = 0; i < 19614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19614(int& val) {
    const int x = 19614;
    std::cout << 19614;
    for (int i = 0; i < 19614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19615(int& val) {
    func_in_19615(val);
    const int x = 19615;
    std::cout << 19615;
    for (int i = 0; i < 19615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19615(int& val) {
    const int x = 19615;
    std::cout << 19615;
    for (int i = 0; i < 19615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19616(int& val) {
    func_in_19616(val);
    const int x = 19616;
    std::cout << 19616;
    for (int i = 0; i < 19616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19616(int& val) {
    const int x = 19616;
    std::cout << 19616;
    for (int i = 0; i < 19616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19617(int& val) {
    func_in_19617(val);
    const int x = 19617;
    std::cout << 19617;
    for (int i = 0; i < 19617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19617(int& val) {
    const int x = 19617;
    std::cout << 19617;
    for (int i = 0; i < 19617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19618(int& val) {
    func_in_19618(val);
    const int x = 19618;
    std::cout << 19618;
    for (int i = 0; i < 19618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19618(int& val) {
    const int x = 19618;
    std::cout << 19618;
    for (int i = 0; i < 19618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19619(int& val) {
    func_in_19619(val);
    const int x = 19619;
    std::cout << 19619;
    for (int i = 0; i < 19619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19619(int& val) {
    const int x = 19619;
    std::cout << 19619;
    for (int i = 0; i < 19619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19620(int& val) {
    func_in_19620(val);
    const int x = 19620;
    std::cout << 19620;
    for (int i = 0; i < 19620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19620(int& val) {
    const int x = 19620;
    std::cout << 19620;
    for (int i = 0; i < 19620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19621(int& val) {
    func_in_19621(val);
    const int x = 19621;
    std::cout << 19621;
    for (int i = 0; i < 19621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19621(int& val) {
    const int x = 19621;
    std::cout << 19621;
    for (int i = 0; i < 19621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19622(int& val) {
    func_in_19622(val);
    const int x = 19622;
    std::cout << 19622;
    for (int i = 0; i < 19622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19622(int& val) {
    const int x = 19622;
    std::cout << 19622;
    for (int i = 0; i < 19622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19623(int& val) {
    func_in_19623(val);
    const int x = 19623;
    std::cout << 19623;
    for (int i = 0; i < 19623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19623(int& val) {
    const int x = 19623;
    std::cout << 19623;
    for (int i = 0; i < 19623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19624(int& val) {
    func_in_19624(val);
    const int x = 19624;
    std::cout << 19624;
    for (int i = 0; i < 19624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19624(int& val) {
    const int x = 19624;
    std::cout << 19624;
    for (int i = 0; i < 19624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19625(int& val) {
    func_in_19625(val);
    const int x = 19625;
    std::cout << 19625;
    for (int i = 0; i < 19625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19625(int& val) {
    const int x = 19625;
    std::cout << 19625;
    for (int i = 0; i < 19625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19626(int& val) {
    func_in_19626(val);
    const int x = 19626;
    std::cout << 19626;
    for (int i = 0; i < 19626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19626(int& val) {
    const int x = 19626;
    std::cout << 19626;
    for (int i = 0; i < 19626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19627(int& val) {
    func_in_19627(val);
    const int x = 19627;
    std::cout << 19627;
    for (int i = 0; i < 19627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19627(int& val) {
    const int x = 19627;
    std::cout << 19627;
    for (int i = 0; i < 19627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19628(int& val) {
    func_in_19628(val);
    const int x = 19628;
    std::cout << 19628;
    for (int i = 0; i < 19628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19628(int& val) {
    const int x = 19628;
    std::cout << 19628;
    for (int i = 0; i < 19628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19629(int& val) {
    func_in_19629(val);
    const int x = 19629;
    std::cout << 19629;
    for (int i = 0; i < 19629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19629(int& val) {
    const int x = 19629;
    std::cout << 19629;
    for (int i = 0; i < 19629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19630(int& val) {
    func_in_19630(val);
    const int x = 19630;
    std::cout << 19630;
    for (int i = 0; i < 19630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19630(int& val) {
    const int x = 19630;
    std::cout << 19630;
    for (int i = 0; i < 19630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19631(int& val) {
    func_in_19631(val);
    const int x = 19631;
    std::cout << 19631;
    for (int i = 0; i < 19631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19631(int& val) {
    const int x = 19631;
    std::cout << 19631;
    for (int i = 0; i < 19631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19632(int& val) {
    func_in_19632(val);
    const int x = 19632;
    std::cout << 19632;
    for (int i = 0; i < 19632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19632(int& val) {
    const int x = 19632;
    std::cout << 19632;
    for (int i = 0; i < 19632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19633(int& val) {
    func_in_19633(val);
    const int x = 19633;
    std::cout << 19633;
    for (int i = 0; i < 19633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19633(int& val) {
    const int x = 19633;
    std::cout << 19633;
    for (int i = 0; i < 19633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19634(int& val) {
    func_in_19634(val);
    const int x = 19634;
    std::cout << 19634;
    for (int i = 0; i < 19634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19634(int& val) {
    const int x = 19634;
    std::cout << 19634;
    for (int i = 0; i < 19634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19635(int& val) {
    func_in_19635(val);
    const int x = 19635;
    std::cout << 19635;
    for (int i = 0; i < 19635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19635(int& val) {
    const int x = 19635;
    std::cout << 19635;
    for (int i = 0; i < 19635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19636(int& val) {
    func_in_19636(val);
    const int x = 19636;
    std::cout << 19636;
    for (int i = 0; i < 19636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19636(int& val) {
    const int x = 19636;
    std::cout << 19636;
    for (int i = 0; i < 19636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19637(int& val) {
    func_in_19637(val);
    const int x = 19637;
    std::cout << 19637;
    for (int i = 0; i < 19637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19637(int& val) {
    const int x = 19637;
    std::cout << 19637;
    for (int i = 0; i < 19637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19638(int& val) {
    func_in_19638(val);
    const int x = 19638;
    std::cout << 19638;
    for (int i = 0; i < 19638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19638(int& val) {
    const int x = 19638;
    std::cout << 19638;
    for (int i = 0; i < 19638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19639(int& val) {
    func_in_19639(val);
    const int x = 19639;
    std::cout << 19639;
    for (int i = 0; i < 19639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19639(int& val) {
    const int x = 19639;
    std::cout << 19639;
    for (int i = 0; i < 19639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19640(int& val) {
    func_in_19640(val);
    const int x = 19640;
    std::cout << 19640;
    for (int i = 0; i < 19640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19640(int& val) {
    const int x = 19640;
    std::cout << 19640;
    for (int i = 0; i < 19640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19641(int& val) {
    func_in_19641(val);
    const int x = 19641;
    std::cout << 19641;
    for (int i = 0; i < 19641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19641(int& val) {
    const int x = 19641;
    std::cout << 19641;
    for (int i = 0; i < 19641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19642(int& val) {
    func_in_19642(val);
    const int x = 19642;
    std::cout << 19642;
    for (int i = 0; i < 19642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19642(int& val) {
    const int x = 19642;
    std::cout << 19642;
    for (int i = 0; i < 19642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19643(int& val) {
    func_in_19643(val);
    const int x = 19643;
    std::cout << 19643;
    for (int i = 0; i < 19643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19643(int& val) {
    const int x = 19643;
    std::cout << 19643;
    for (int i = 0; i < 19643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19644(int& val) {
    func_in_19644(val);
    const int x = 19644;
    std::cout << 19644;
    for (int i = 0; i < 19644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19644(int& val) {
    const int x = 19644;
    std::cout << 19644;
    for (int i = 0; i < 19644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19645(int& val) {
    func_in_19645(val);
    const int x = 19645;
    std::cout << 19645;
    for (int i = 0; i < 19645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19645(int& val) {
    const int x = 19645;
    std::cout << 19645;
    for (int i = 0; i < 19645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19646(int& val) {
    func_in_19646(val);
    const int x = 19646;
    std::cout << 19646;
    for (int i = 0; i < 19646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19646(int& val) {
    const int x = 19646;
    std::cout << 19646;
    for (int i = 0; i < 19646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19647(int& val) {
    func_in_19647(val);
    const int x = 19647;
    std::cout << 19647;
    for (int i = 0; i < 19647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19647(int& val) {
    const int x = 19647;
    std::cout << 19647;
    for (int i = 0; i < 19647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19648(int& val) {
    func_in_19648(val);
    const int x = 19648;
    std::cout << 19648;
    for (int i = 0; i < 19648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19648(int& val) {
    const int x = 19648;
    std::cout << 19648;
    for (int i = 0; i < 19648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19649(int& val) {
    func_in_19649(val);
    const int x = 19649;
    std::cout << 19649;
    for (int i = 0; i < 19649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19649(int& val) {
    const int x = 19649;
    std::cout << 19649;
    for (int i = 0; i < 19649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19650(int& val) {
    func_in_19650(val);
    const int x = 19650;
    std::cout << 19650;
    for (int i = 0; i < 19650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19650(int& val) {
    const int x = 19650;
    std::cout << 19650;
    for (int i = 0; i < 19650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19651(int& val) {
    func_in_19651(val);
    const int x = 19651;
    std::cout << 19651;
    for (int i = 0; i < 19651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19651(int& val) {
    const int x = 19651;
    std::cout << 19651;
    for (int i = 0; i < 19651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19652(int& val) {
    func_in_19652(val);
    const int x = 19652;
    std::cout << 19652;
    for (int i = 0; i < 19652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19652(int& val) {
    const int x = 19652;
    std::cout << 19652;
    for (int i = 0; i < 19652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19653(int& val) {
    func_in_19653(val);
    const int x = 19653;
    std::cout << 19653;
    for (int i = 0; i < 19653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19653(int& val) {
    const int x = 19653;
    std::cout << 19653;
    for (int i = 0; i < 19653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19654(int& val) {
    func_in_19654(val);
    const int x = 19654;
    std::cout << 19654;
    for (int i = 0; i < 19654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19654(int& val) {
    const int x = 19654;
    std::cout << 19654;
    for (int i = 0; i < 19654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19655(int& val) {
    func_in_19655(val);
    const int x = 19655;
    std::cout << 19655;
    for (int i = 0; i < 19655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19655(int& val) {
    const int x = 19655;
    std::cout << 19655;
    for (int i = 0; i < 19655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19656(int& val) {
    func_in_19656(val);
    const int x = 19656;
    std::cout << 19656;
    for (int i = 0; i < 19656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19656(int& val) {
    const int x = 19656;
    std::cout << 19656;
    for (int i = 0; i < 19656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19657(int& val) {
    func_in_19657(val);
    const int x = 19657;
    std::cout << 19657;
    for (int i = 0; i < 19657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19657(int& val) {
    const int x = 19657;
    std::cout << 19657;
    for (int i = 0; i < 19657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19658(int& val) {
    func_in_19658(val);
    const int x = 19658;
    std::cout << 19658;
    for (int i = 0; i < 19658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19658(int& val) {
    const int x = 19658;
    std::cout << 19658;
    for (int i = 0; i < 19658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19659(int& val) {
    func_in_19659(val);
    const int x = 19659;
    std::cout << 19659;
    for (int i = 0; i < 19659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19659(int& val) {
    const int x = 19659;
    std::cout << 19659;
    for (int i = 0; i < 19659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19660(int& val) {
    func_in_19660(val);
    const int x = 19660;
    std::cout << 19660;
    for (int i = 0; i < 19660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19660(int& val) {
    const int x = 19660;
    std::cout << 19660;
    for (int i = 0; i < 19660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19661(int& val) {
    func_in_19661(val);
    const int x = 19661;
    std::cout << 19661;
    for (int i = 0; i < 19661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19661(int& val) {
    const int x = 19661;
    std::cout << 19661;
    for (int i = 0; i < 19661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19662(int& val) {
    func_in_19662(val);
    const int x = 19662;
    std::cout << 19662;
    for (int i = 0; i < 19662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19662(int& val) {
    const int x = 19662;
    std::cout << 19662;
    for (int i = 0; i < 19662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19663(int& val) {
    func_in_19663(val);
    const int x = 19663;
    std::cout << 19663;
    for (int i = 0; i < 19663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19663(int& val) {
    const int x = 19663;
    std::cout << 19663;
    for (int i = 0; i < 19663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19664(int& val) {
    func_in_19664(val);
    const int x = 19664;
    std::cout << 19664;
    for (int i = 0; i < 19664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19664(int& val) {
    const int x = 19664;
    std::cout << 19664;
    for (int i = 0; i < 19664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19665(int& val) {
    func_in_19665(val);
    const int x = 19665;
    std::cout << 19665;
    for (int i = 0; i < 19665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19665(int& val) {
    const int x = 19665;
    std::cout << 19665;
    for (int i = 0; i < 19665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19666(int& val) {
    func_in_19666(val);
    const int x = 19666;
    std::cout << 19666;
    for (int i = 0; i < 19666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19666(int& val) {
    const int x = 19666;
    std::cout << 19666;
    for (int i = 0; i < 19666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19667(int& val) {
    func_in_19667(val);
    const int x = 19667;
    std::cout << 19667;
    for (int i = 0; i < 19667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19667(int& val) {
    const int x = 19667;
    std::cout << 19667;
    for (int i = 0; i < 19667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19668(int& val) {
    func_in_19668(val);
    const int x = 19668;
    std::cout << 19668;
    for (int i = 0; i < 19668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19668(int& val) {
    const int x = 19668;
    std::cout << 19668;
    for (int i = 0; i < 19668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19669(int& val) {
    func_in_19669(val);
    const int x = 19669;
    std::cout << 19669;
    for (int i = 0; i < 19669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19669(int& val) {
    const int x = 19669;
    std::cout << 19669;
    for (int i = 0; i < 19669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19670(int& val) {
    func_in_19670(val);
    const int x = 19670;
    std::cout << 19670;
    for (int i = 0; i < 19670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19670(int& val) {
    const int x = 19670;
    std::cout << 19670;
    for (int i = 0; i < 19670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19671(int& val) {
    func_in_19671(val);
    const int x = 19671;
    std::cout << 19671;
    for (int i = 0; i < 19671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19671(int& val) {
    const int x = 19671;
    std::cout << 19671;
    for (int i = 0; i < 19671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19672(int& val) {
    func_in_19672(val);
    const int x = 19672;
    std::cout << 19672;
    for (int i = 0; i < 19672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19672(int& val) {
    const int x = 19672;
    std::cout << 19672;
    for (int i = 0; i < 19672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19673(int& val) {
    func_in_19673(val);
    const int x = 19673;
    std::cout << 19673;
    for (int i = 0; i < 19673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19673(int& val) {
    const int x = 19673;
    std::cout << 19673;
    for (int i = 0; i < 19673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19674(int& val) {
    func_in_19674(val);
    const int x = 19674;
    std::cout << 19674;
    for (int i = 0; i < 19674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19674(int& val) {
    const int x = 19674;
    std::cout << 19674;
    for (int i = 0; i < 19674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19675(int& val) {
    func_in_19675(val);
    const int x = 19675;
    std::cout << 19675;
    for (int i = 0; i < 19675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19675(int& val) {
    const int x = 19675;
    std::cout << 19675;
    for (int i = 0; i < 19675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19676(int& val) {
    func_in_19676(val);
    const int x = 19676;
    std::cout << 19676;
    for (int i = 0; i < 19676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19676(int& val) {
    const int x = 19676;
    std::cout << 19676;
    for (int i = 0; i < 19676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19677(int& val) {
    func_in_19677(val);
    const int x = 19677;
    std::cout << 19677;
    for (int i = 0; i < 19677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19677(int& val) {
    const int x = 19677;
    std::cout << 19677;
    for (int i = 0; i < 19677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19678(int& val) {
    func_in_19678(val);
    const int x = 19678;
    std::cout << 19678;
    for (int i = 0; i < 19678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19678(int& val) {
    const int x = 19678;
    std::cout << 19678;
    for (int i = 0; i < 19678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19679(int& val) {
    func_in_19679(val);
    const int x = 19679;
    std::cout << 19679;
    for (int i = 0; i < 19679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19679(int& val) {
    const int x = 19679;
    std::cout << 19679;
    for (int i = 0; i < 19679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19680(int& val) {
    func_in_19680(val);
    const int x = 19680;
    std::cout << 19680;
    for (int i = 0; i < 19680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19680(int& val) {
    const int x = 19680;
    std::cout << 19680;
    for (int i = 0; i < 19680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19681(int& val) {
    func_in_19681(val);
    const int x = 19681;
    std::cout << 19681;
    for (int i = 0; i < 19681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19681(int& val) {
    const int x = 19681;
    std::cout << 19681;
    for (int i = 0; i < 19681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19682(int& val) {
    func_in_19682(val);
    const int x = 19682;
    std::cout << 19682;
    for (int i = 0; i < 19682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19682(int& val) {
    const int x = 19682;
    std::cout << 19682;
    for (int i = 0; i < 19682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19683(int& val) {
    func_in_19683(val);
    const int x = 19683;
    std::cout << 19683;
    for (int i = 0; i < 19683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19683(int& val) {
    const int x = 19683;
    std::cout << 19683;
    for (int i = 0; i < 19683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19684(int& val) {
    func_in_19684(val);
    const int x = 19684;
    std::cout << 19684;
    for (int i = 0; i < 19684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19684(int& val) {
    const int x = 19684;
    std::cout << 19684;
    for (int i = 0; i < 19684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19685(int& val) {
    func_in_19685(val);
    const int x = 19685;
    std::cout << 19685;
    for (int i = 0; i < 19685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19685(int& val) {
    const int x = 19685;
    std::cout << 19685;
    for (int i = 0; i < 19685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19686(int& val) {
    func_in_19686(val);
    const int x = 19686;
    std::cout << 19686;
    for (int i = 0; i < 19686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19686(int& val) {
    const int x = 19686;
    std::cout << 19686;
    for (int i = 0; i < 19686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19687(int& val) {
    func_in_19687(val);
    const int x = 19687;
    std::cout << 19687;
    for (int i = 0; i < 19687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19687(int& val) {
    const int x = 19687;
    std::cout << 19687;
    for (int i = 0; i < 19687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19688(int& val) {
    func_in_19688(val);
    const int x = 19688;
    std::cout << 19688;
    for (int i = 0; i < 19688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19688(int& val) {
    const int x = 19688;
    std::cout << 19688;
    for (int i = 0; i < 19688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19689(int& val) {
    func_in_19689(val);
    const int x = 19689;
    std::cout << 19689;
    for (int i = 0; i < 19689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19689(int& val) {
    const int x = 19689;
    std::cout << 19689;
    for (int i = 0; i < 19689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19690(int& val) {
    func_in_19690(val);
    const int x = 19690;
    std::cout << 19690;
    for (int i = 0; i < 19690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19690(int& val) {
    const int x = 19690;
    std::cout << 19690;
    for (int i = 0; i < 19690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19691(int& val) {
    func_in_19691(val);
    const int x = 19691;
    std::cout << 19691;
    for (int i = 0; i < 19691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19691(int& val) {
    const int x = 19691;
    std::cout << 19691;
    for (int i = 0; i < 19691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19692(int& val) {
    func_in_19692(val);
    const int x = 19692;
    std::cout << 19692;
    for (int i = 0; i < 19692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19692(int& val) {
    const int x = 19692;
    std::cout << 19692;
    for (int i = 0; i < 19692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19693(int& val) {
    func_in_19693(val);
    const int x = 19693;
    std::cout << 19693;
    for (int i = 0; i < 19693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19693(int& val) {
    const int x = 19693;
    std::cout << 19693;
    for (int i = 0; i < 19693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19694(int& val) {
    func_in_19694(val);
    const int x = 19694;
    std::cout << 19694;
    for (int i = 0; i < 19694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19694(int& val) {
    const int x = 19694;
    std::cout << 19694;
    for (int i = 0; i < 19694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19695(int& val) {
    func_in_19695(val);
    const int x = 19695;
    std::cout << 19695;
    for (int i = 0; i < 19695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19695(int& val) {
    const int x = 19695;
    std::cout << 19695;
    for (int i = 0; i < 19695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19696(int& val) {
    func_in_19696(val);
    const int x = 19696;
    std::cout << 19696;
    for (int i = 0; i < 19696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19696(int& val) {
    const int x = 19696;
    std::cout << 19696;
    for (int i = 0; i < 19696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19697(int& val) {
    func_in_19697(val);
    const int x = 19697;
    std::cout << 19697;
    for (int i = 0; i < 19697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19697(int& val) {
    const int x = 19697;
    std::cout << 19697;
    for (int i = 0; i < 19697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19698(int& val) {
    func_in_19698(val);
    const int x = 19698;
    std::cout << 19698;
    for (int i = 0; i < 19698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19698(int& val) {
    const int x = 19698;
    std::cout << 19698;
    for (int i = 0; i < 19698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19699(int& val) {
    func_in_19699(val);
    const int x = 19699;
    std::cout << 19699;
    for (int i = 0; i < 19699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19699(int& val) {
    const int x = 19699;
    std::cout << 19699;
    for (int i = 0; i < 19699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19700(int& val) {
    func_in_19700(val);
    const int x = 19700;
    std::cout << 19700;
    for (int i = 0; i < 19700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19700(int& val) {
    const int x = 19700;
    std::cout << 19700;
    for (int i = 0; i < 19700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19701(int& val) {
    func_in_19701(val);
    const int x = 19701;
    std::cout << 19701;
    for (int i = 0; i < 19701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19701(int& val) {
    const int x = 19701;
    std::cout << 19701;
    for (int i = 0; i < 19701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19702(int& val) {
    func_in_19702(val);
    const int x = 19702;
    std::cout << 19702;
    for (int i = 0; i < 19702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19702(int& val) {
    const int x = 19702;
    std::cout << 19702;
    for (int i = 0; i < 19702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19703(int& val) {
    func_in_19703(val);
    const int x = 19703;
    std::cout << 19703;
    for (int i = 0; i < 19703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19703(int& val) {
    const int x = 19703;
    std::cout << 19703;
    for (int i = 0; i < 19703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19704(int& val) {
    func_in_19704(val);
    const int x = 19704;
    std::cout << 19704;
    for (int i = 0; i < 19704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19704(int& val) {
    const int x = 19704;
    std::cout << 19704;
    for (int i = 0; i < 19704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19705(int& val) {
    func_in_19705(val);
    const int x = 19705;
    std::cout << 19705;
    for (int i = 0; i < 19705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19705(int& val) {
    const int x = 19705;
    std::cout << 19705;
    for (int i = 0; i < 19705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19706(int& val) {
    func_in_19706(val);
    const int x = 19706;
    std::cout << 19706;
    for (int i = 0; i < 19706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19706(int& val) {
    const int x = 19706;
    std::cout << 19706;
    for (int i = 0; i < 19706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19707(int& val) {
    func_in_19707(val);
    const int x = 19707;
    std::cout << 19707;
    for (int i = 0; i < 19707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19707(int& val) {
    const int x = 19707;
    std::cout << 19707;
    for (int i = 0; i < 19707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19708(int& val) {
    func_in_19708(val);
    const int x = 19708;
    std::cout << 19708;
    for (int i = 0; i < 19708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19708(int& val) {
    const int x = 19708;
    std::cout << 19708;
    for (int i = 0; i < 19708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19709(int& val) {
    func_in_19709(val);
    const int x = 19709;
    std::cout << 19709;
    for (int i = 0; i < 19709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19709(int& val) {
    const int x = 19709;
    std::cout << 19709;
    for (int i = 0; i < 19709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19710(int& val) {
    func_in_19710(val);
    const int x = 19710;
    std::cout << 19710;
    for (int i = 0; i < 19710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19710(int& val) {
    const int x = 19710;
    std::cout << 19710;
    for (int i = 0; i < 19710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19711(int& val) {
    func_in_19711(val);
    const int x = 19711;
    std::cout << 19711;
    for (int i = 0; i < 19711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19711(int& val) {
    const int x = 19711;
    std::cout << 19711;
    for (int i = 0; i < 19711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19712(int& val) {
    func_in_19712(val);
    const int x = 19712;
    std::cout << 19712;
    for (int i = 0; i < 19712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19712(int& val) {
    const int x = 19712;
    std::cout << 19712;
    for (int i = 0; i < 19712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19713(int& val) {
    func_in_19713(val);
    const int x = 19713;
    std::cout << 19713;
    for (int i = 0; i < 19713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19713(int& val) {
    const int x = 19713;
    std::cout << 19713;
    for (int i = 0; i < 19713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19714(int& val) {
    func_in_19714(val);
    const int x = 19714;
    std::cout << 19714;
    for (int i = 0; i < 19714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19714(int& val) {
    const int x = 19714;
    std::cout << 19714;
    for (int i = 0; i < 19714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19715(int& val) {
    func_in_19715(val);
    const int x = 19715;
    std::cout << 19715;
    for (int i = 0; i < 19715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19715(int& val) {
    const int x = 19715;
    std::cout << 19715;
    for (int i = 0; i < 19715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19716(int& val) {
    func_in_19716(val);
    const int x = 19716;
    std::cout << 19716;
    for (int i = 0; i < 19716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19716(int& val) {
    const int x = 19716;
    std::cout << 19716;
    for (int i = 0; i < 19716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19717(int& val) {
    func_in_19717(val);
    const int x = 19717;
    std::cout << 19717;
    for (int i = 0; i < 19717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19717(int& val) {
    const int x = 19717;
    std::cout << 19717;
    for (int i = 0; i < 19717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19718(int& val) {
    func_in_19718(val);
    const int x = 19718;
    std::cout << 19718;
    for (int i = 0; i < 19718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19718(int& val) {
    const int x = 19718;
    std::cout << 19718;
    for (int i = 0; i < 19718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19719(int& val) {
    func_in_19719(val);
    const int x = 19719;
    std::cout << 19719;
    for (int i = 0; i < 19719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19719(int& val) {
    const int x = 19719;
    std::cout << 19719;
    for (int i = 0; i < 19719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19720(int& val) {
    func_in_19720(val);
    const int x = 19720;
    std::cout << 19720;
    for (int i = 0; i < 19720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19720(int& val) {
    const int x = 19720;
    std::cout << 19720;
    for (int i = 0; i < 19720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19721(int& val) {
    func_in_19721(val);
    const int x = 19721;
    std::cout << 19721;
    for (int i = 0; i < 19721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19721(int& val) {
    const int x = 19721;
    std::cout << 19721;
    for (int i = 0; i < 19721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19722(int& val) {
    func_in_19722(val);
    const int x = 19722;
    std::cout << 19722;
    for (int i = 0; i < 19722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19722(int& val) {
    const int x = 19722;
    std::cout << 19722;
    for (int i = 0; i < 19722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19723(int& val) {
    func_in_19723(val);
    const int x = 19723;
    std::cout << 19723;
    for (int i = 0; i < 19723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19723(int& val) {
    const int x = 19723;
    std::cout << 19723;
    for (int i = 0; i < 19723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19724(int& val) {
    func_in_19724(val);
    const int x = 19724;
    std::cout << 19724;
    for (int i = 0; i < 19724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19724(int& val) {
    const int x = 19724;
    std::cout << 19724;
    for (int i = 0; i < 19724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19725(int& val) {
    func_in_19725(val);
    const int x = 19725;
    std::cout << 19725;
    for (int i = 0; i < 19725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19725(int& val) {
    const int x = 19725;
    std::cout << 19725;
    for (int i = 0; i < 19725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19726(int& val) {
    func_in_19726(val);
    const int x = 19726;
    std::cout << 19726;
    for (int i = 0; i < 19726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19726(int& val) {
    const int x = 19726;
    std::cout << 19726;
    for (int i = 0; i < 19726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19727(int& val) {
    func_in_19727(val);
    const int x = 19727;
    std::cout << 19727;
    for (int i = 0; i < 19727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19727(int& val) {
    const int x = 19727;
    std::cout << 19727;
    for (int i = 0; i < 19727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19728(int& val) {
    func_in_19728(val);
    const int x = 19728;
    std::cout << 19728;
    for (int i = 0; i < 19728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19728(int& val) {
    const int x = 19728;
    std::cout << 19728;
    for (int i = 0; i < 19728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19729(int& val) {
    func_in_19729(val);
    const int x = 19729;
    std::cout << 19729;
    for (int i = 0; i < 19729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19729(int& val) {
    const int x = 19729;
    std::cout << 19729;
    for (int i = 0; i < 19729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19730(int& val) {
    func_in_19730(val);
    const int x = 19730;
    std::cout << 19730;
    for (int i = 0; i < 19730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19730(int& val) {
    const int x = 19730;
    std::cout << 19730;
    for (int i = 0; i < 19730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19731(int& val) {
    func_in_19731(val);
    const int x = 19731;
    std::cout << 19731;
    for (int i = 0; i < 19731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19731(int& val) {
    const int x = 19731;
    std::cout << 19731;
    for (int i = 0; i < 19731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19732(int& val) {
    func_in_19732(val);
    const int x = 19732;
    std::cout << 19732;
    for (int i = 0; i < 19732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19732(int& val) {
    const int x = 19732;
    std::cout << 19732;
    for (int i = 0; i < 19732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19733(int& val) {
    func_in_19733(val);
    const int x = 19733;
    std::cout << 19733;
    for (int i = 0; i < 19733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19733(int& val) {
    const int x = 19733;
    std::cout << 19733;
    for (int i = 0; i < 19733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19734(int& val) {
    func_in_19734(val);
    const int x = 19734;
    std::cout << 19734;
    for (int i = 0; i < 19734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19734(int& val) {
    const int x = 19734;
    std::cout << 19734;
    for (int i = 0; i < 19734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19735(int& val) {
    func_in_19735(val);
    const int x = 19735;
    std::cout << 19735;
    for (int i = 0; i < 19735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19735(int& val) {
    const int x = 19735;
    std::cout << 19735;
    for (int i = 0; i < 19735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19736(int& val) {
    func_in_19736(val);
    const int x = 19736;
    std::cout << 19736;
    for (int i = 0; i < 19736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19736(int& val) {
    const int x = 19736;
    std::cout << 19736;
    for (int i = 0; i < 19736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19737(int& val) {
    func_in_19737(val);
    const int x = 19737;
    std::cout << 19737;
    for (int i = 0; i < 19737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19737(int& val) {
    const int x = 19737;
    std::cout << 19737;
    for (int i = 0; i < 19737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19738(int& val) {
    func_in_19738(val);
    const int x = 19738;
    std::cout << 19738;
    for (int i = 0; i < 19738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19738(int& val) {
    const int x = 19738;
    std::cout << 19738;
    for (int i = 0; i < 19738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19739(int& val) {
    func_in_19739(val);
    const int x = 19739;
    std::cout << 19739;
    for (int i = 0; i < 19739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19739(int& val) {
    const int x = 19739;
    std::cout << 19739;
    for (int i = 0; i < 19739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19740(int& val) {
    func_in_19740(val);
    const int x = 19740;
    std::cout << 19740;
    for (int i = 0; i < 19740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19740(int& val) {
    const int x = 19740;
    std::cout << 19740;
    for (int i = 0; i < 19740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19741(int& val) {
    func_in_19741(val);
    const int x = 19741;
    std::cout << 19741;
    for (int i = 0; i < 19741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19741(int& val) {
    const int x = 19741;
    std::cout << 19741;
    for (int i = 0; i < 19741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19742(int& val) {
    func_in_19742(val);
    const int x = 19742;
    std::cout << 19742;
    for (int i = 0; i < 19742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19742(int& val) {
    const int x = 19742;
    std::cout << 19742;
    for (int i = 0; i < 19742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19743(int& val) {
    func_in_19743(val);
    const int x = 19743;
    std::cout << 19743;
    for (int i = 0; i < 19743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19743(int& val) {
    const int x = 19743;
    std::cout << 19743;
    for (int i = 0; i < 19743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19744(int& val) {
    func_in_19744(val);
    const int x = 19744;
    std::cout << 19744;
    for (int i = 0; i < 19744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19744(int& val) {
    const int x = 19744;
    std::cout << 19744;
    for (int i = 0; i < 19744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19745(int& val) {
    func_in_19745(val);
    const int x = 19745;
    std::cout << 19745;
    for (int i = 0; i < 19745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19745(int& val) {
    const int x = 19745;
    std::cout << 19745;
    for (int i = 0; i < 19745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19746(int& val) {
    func_in_19746(val);
    const int x = 19746;
    std::cout << 19746;
    for (int i = 0; i < 19746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19746(int& val) {
    const int x = 19746;
    std::cout << 19746;
    for (int i = 0; i < 19746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19747(int& val) {
    func_in_19747(val);
    const int x = 19747;
    std::cout << 19747;
    for (int i = 0; i < 19747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19747(int& val) {
    const int x = 19747;
    std::cout << 19747;
    for (int i = 0; i < 19747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19748(int& val) {
    func_in_19748(val);
    const int x = 19748;
    std::cout << 19748;
    for (int i = 0; i < 19748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19748(int& val) {
    const int x = 19748;
    std::cout << 19748;
    for (int i = 0; i < 19748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19749(int& val) {
    func_in_19749(val);
    const int x = 19749;
    std::cout << 19749;
    for (int i = 0; i < 19749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19749(int& val) {
    const int x = 19749;
    std::cout << 19749;
    for (int i = 0; i < 19749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19750(int& val) {
    func_in_19750(val);
    const int x = 19750;
    std::cout << 19750;
    for (int i = 0; i < 19750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19750(int& val) {
    const int x = 19750;
    std::cout << 19750;
    for (int i = 0; i < 19750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19751(int& val) {
    func_in_19751(val);
    const int x = 19751;
    std::cout << 19751;
    for (int i = 0; i < 19751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19751(int& val) {
    const int x = 19751;
    std::cout << 19751;
    for (int i = 0; i < 19751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19752(int& val) {
    func_in_19752(val);
    const int x = 19752;
    std::cout << 19752;
    for (int i = 0; i < 19752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19752(int& val) {
    const int x = 19752;
    std::cout << 19752;
    for (int i = 0; i < 19752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19753(int& val) {
    func_in_19753(val);
    const int x = 19753;
    std::cout << 19753;
    for (int i = 0; i < 19753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19753(int& val) {
    const int x = 19753;
    std::cout << 19753;
    for (int i = 0; i < 19753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19754(int& val) {
    func_in_19754(val);
    const int x = 19754;
    std::cout << 19754;
    for (int i = 0; i < 19754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19754(int& val) {
    const int x = 19754;
    std::cout << 19754;
    for (int i = 0; i < 19754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19755(int& val) {
    func_in_19755(val);
    const int x = 19755;
    std::cout << 19755;
    for (int i = 0; i < 19755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19755(int& val) {
    const int x = 19755;
    std::cout << 19755;
    for (int i = 0; i < 19755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19756(int& val) {
    func_in_19756(val);
    const int x = 19756;
    std::cout << 19756;
    for (int i = 0; i < 19756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19756(int& val) {
    const int x = 19756;
    std::cout << 19756;
    for (int i = 0; i < 19756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19757(int& val) {
    func_in_19757(val);
    const int x = 19757;
    std::cout << 19757;
    for (int i = 0; i < 19757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19757(int& val) {
    const int x = 19757;
    std::cout << 19757;
    for (int i = 0; i < 19757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19758(int& val) {
    func_in_19758(val);
    const int x = 19758;
    std::cout << 19758;
    for (int i = 0; i < 19758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19758(int& val) {
    const int x = 19758;
    std::cout << 19758;
    for (int i = 0; i < 19758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19759(int& val) {
    func_in_19759(val);
    const int x = 19759;
    std::cout << 19759;
    for (int i = 0; i < 19759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19759(int& val) {
    const int x = 19759;
    std::cout << 19759;
    for (int i = 0; i < 19759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19760(int& val) {
    func_in_19760(val);
    const int x = 19760;
    std::cout << 19760;
    for (int i = 0; i < 19760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19760(int& val) {
    const int x = 19760;
    std::cout << 19760;
    for (int i = 0; i < 19760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19761(int& val) {
    func_in_19761(val);
    const int x = 19761;
    std::cout << 19761;
    for (int i = 0; i < 19761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19761(int& val) {
    const int x = 19761;
    std::cout << 19761;
    for (int i = 0; i < 19761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19762(int& val) {
    func_in_19762(val);
    const int x = 19762;
    std::cout << 19762;
    for (int i = 0; i < 19762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19762(int& val) {
    const int x = 19762;
    std::cout << 19762;
    for (int i = 0; i < 19762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19763(int& val) {
    func_in_19763(val);
    const int x = 19763;
    std::cout << 19763;
    for (int i = 0; i < 19763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19763(int& val) {
    const int x = 19763;
    std::cout << 19763;
    for (int i = 0; i < 19763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19764(int& val) {
    func_in_19764(val);
    const int x = 19764;
    std::cout << 19764;
    for (int i = 0; i < 19764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19764(int& val) {
    const int x = 19764;
    std::cout << 19764;
    for (int i = 0; i < 19764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19765(int& val) {
    func_in_19765(val);
    const int x = 19765;
    std::cout << 19765;
    for (int i = 0; i < 19765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19765(int& val) {
    const int x = 19765;
    std::cout << 19765;
    for (int i = 0; i < 19765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19766(int& val) {
    func_in_19766(val);
    const int x = 19766;
    std::cout << 19766;
    for (int i = 0; i < 19766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19766(int& val) {
    const int x = 19766;
    std::cout << 19766;
    for (int i = 0; i < 19766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19767(int& val) {
    func_in_19767(val);
    const int x = 19767;
    std::cout << 19767;
    for (int i = 0; i < 19767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19767(int& val) {
    const int x = 19767;
    std::cout << 19767;
    for (int i = 0; i < 19767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19768(int& val) {
    func_in_19768(val);
    const int x = 19768;
    std::cout << 19768;
    for (int i = 0; i < 19768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19768(int& val) {
    const int x = 19768;
    std::cout << 19768;
    for (int i = 0; i < 19768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19769(int& val) {
    func_in_19769(val);
    const int x = 19769;
    std::cout << 19769;
    for (int i = 0; i < 19769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19769(int& val) {
    const int x = 19769;
    std::cout << 19769;
    for (int i = 0; i < 19769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19770(int& val) {
    func_in_19770(val);
    const int x = 19770;
    std::cout << 19770;
    for (int i = 0; i < 19770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19770(int& val) {
    const int x = 19770;
    std::cout << 19770;
    for (int i = 0; i < 19770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19771(int& val) {
    func_in_19771(val);
    const int x = 19771;
    std::cout << 19771;
    for (int i = 0; i < 19771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19771(int& val) {
    const int x = 19771;
    std::cout << 19771;
    for (int i = 0; i < 19771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19772(int& val) {
    func_in_19772(val);
    const int x = 19772;
    std::cout << 19772;
    for (int i = 0; i < 19772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19772(int& val) {
    const int x = 19772;
    std::cout << 19772;
    for (int i = 0; i < 19772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19773(int& val) {
    func_in_19773(val);
    const int x = 19773;
    std::cout << 19773;
    for (int i = 0; i < 19773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19773(int& val) {
    const int x = 19773;
    std::cout << 19773;
    for (int i = 0; i < 19773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19774(int& val) {
    func_in_19774(val);
    const int x = 19774;
    std::cout << 19774;
    for (int i = 0; i < 19774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19774(int& val) {
    const int x = 19774;
    std::cout << 19774;
    for (int i = 0; i < 19774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19775(int& val) {
    func_in_19775(val);
    const int x = 19775;
    std::cout << 19775;
    for (int i = 0; i < 19775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19775(int& val) {
    const int x = 19775;
    std::cout << 19775;
    for (int i = 0; i < 19775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19776(int& val) {
    func_in_19776(val);
    const int x = 19776;
    std::cout << 19776;
    for (int i = 0; i < 19776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19776(int& val) {
    const int x = 19776;
    std::cout << 19776;
    for (int i = 0; i < 19776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19777(int& val) {
    func_in_19777(val);
    const int x = 19777;
    std::cout << 19777;
    for (int i = 0; i < 19777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19777(int& val) {
    const int x = 19777;
    std::cout << 19777;
    for (int i = 0; i < 19777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19778(int& val) {
    func_in_19778(val);
    const int x = 19778;
    std::cout << 19778;
    for (int i = 0; i < 19778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19778(int& val) {
    const int x = 19778;
    std::cout << 19778;
    for (int i = 0; i < 19778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19779(int& val) {
    func_in_19779(val);
    const int x = 19779;
    std::cout << 19779;
    for (int i = 0; i < 19779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19779(int& val) {
    const int x = 19779;
    std::cout << 19779;
    for (int i = 0; i < 19779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19780(int& val) {
    func_in_19780(val);
    const int x = 19780;
    std::cout << 19780;
    for (int i = 0; i < 19780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19780(int& val) {
    const int x = 19780;
    std::cout << 19780;
    for (int i = 0; i < 19780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19781(int& val) {
    func_in_19781(val);
    const int x = 19781;
    std::cout << 19781;
    for (int i = 0; i < 19781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19781(int& val) {
    const int x = 19781;
    std::cout << 19781;
    for (int i = 0; i < 19781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19782(int& val) {
    func_in_19782(val);
    const int x = 19782;
    std::cout << 19782;
    for (int i = 0; i < 19782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19782(int& val) {
    const int x = 19782;
    std::cout << 19782;
    for (int i = 0; i < 19782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19783(int& val) {
    func_in_19783(val);
    const int x = 19783;
    std::cout << 19783;
    for (int i = 0; i < 19783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19783(int& val) {
    const int x = 19783;
    std::cout << 19783;
    for (int i = 0; i < 19783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19784(int& val) {
    func_in_19784(val);
    const int x = 19784;
    std::cout << 19784;
    for (int i = 0; i < 19784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19784(int& val) {
    const int x = 19784;
    std::cout << 19784;
    for (int i = 0; i < 19784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19785(int& val) {
    func_in_19785(val);
    const int x = 19785;
    std::cout << 19785;
    for (int i = 0; i < 19785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19785(int& val) {
    const int x = 19785;
    std::cout << 19785;
    for (int i = 0; i < 19785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19786(int& val) {
    func_in_19786(val);
    const int x = 19786;
    std::cout << 19786;
    for (int i = 0; i < 19786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19786(int& val) {
    const int x = 19786;
    std::cout << 19786;
    for (int i = 0; i < 19786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19787(int& val) {
    func_in_19787(val);
    const int x = 19787;
    std::cout << 19787;
    for (int i = 0; i < 19787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19787(int& val) {
    const int x = 19787;
    std::cout << 19787;
    for (int i = 0; i < 19787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19788(int& val) {
    func_in_19788(val);
    const int x = 19788;
    std::cout << 19788;
    for (int i = 0; i < 19788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19788(int& val) {
    const int x = 19788;
    std::cout << 19788;
    for (int i = 0; i < 19788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19789(int& val) {
    func_in_19789(val);
    const int x = 19789;
    std::cout << 19789;
    for (int i = 0; i < 19789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19789(int& val) {
    const int x = 19789;
    std::cout << 19789;
    for (int i = 0; i < 19789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19790(int& val) {
    func_in_19790(val);
    const int x = 19790;
    std::cout << 19790;
    for (int i = 0; i < 19790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19790(int& val) {
    const int x = 19790;
    std::cout << 19790;
    for (int i = 0; i < 19790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19791(int& val) {
    func_in_19791(val);
    const int x = 19791;
    std::cout << 19791;
    for (int i = 0; i < 19791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19791(int& val) {
    const int x = 19791;
    std::cout << 19791;
    for (int i = 0; i < 19791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19792(int& val) {
    func_in_19792(val);
    const int x = 19792;
    std::cout << 19792;
    for (int i = 0; i < 19792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19792(int& val) {
    const int x = 19792;
    std::cout << 19792;
    for (int i = 0; i < 19792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19793(int& val) {
    func_in_19793(val);
    const int x = 19793;
    std::cout << 19793;
    for (int i = 0; i < 19793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19793(int& val) {
    const int x = 19793;
    std::cout << 19793;
    for (int i = 0; i < 19793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19794(int& val) {
    func_in_19794(val);
    const int x = 19794;
    std::cout << 19794;
    for (int i = 0; i < 19794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19794(int& val) {
    const int x = 19794;
    std::cout << 19794;
    for (int i = 0; i < 19794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19795(int& val) {
    func_in_19795(val);
    const int x = 19795;
    std::cout << 19795;
    for (int i = 0; i < 19795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19795(int& val) {
    const int x = 19795;
    std::cout << 19795;
    for (int i = 0; i < 19795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19796(int& val) {
    func_in_19796(val);
    const int x = 19796;
    std::cout << 19796;
    for (int i = 0; i < 19796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19796(int& val) {
    const int x = 19796;
    std::cout << 19796;
    for (int i = 0; i < 19796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19797(int& val) {
    func_in_19797(val);
    const int x = 19797;
    std::cout << 19797;
    for (int i = 0; i < 19797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19797(int& val) {
    const int x = 19797;
    std::cout << 19797;
    for (int i = 0; i < 19797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19798(int& val) {
    func_in_19798(val);
    const int x = 19798;
    std::cout << 19798;
    for (int i = 0; i < 19798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19798(int& val) {
    const int x = 19798;
    std::cout << 19798;
    for (int i = 0; i < 19798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19799(int& val) {
    func_in_19799(val);
    const int x = 19799;
    std::cout << 19799;
    for (int i = 0; i < 19799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19799(int& val) {
    const int x = 19799;
    std::cout << 19799;
    for (int i = 0; i < 19799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19800(int& val) {
    func_in_19800(val);
    const int x = 19800;
    std::cout << 19800;
    for (int i = 0; i < 19800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19800(int& val) {
    const int x = 19800;
    std::cout << 19800;
    for (int i = 0; i < 19800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19801(int& val) {
    func_in_19801(val);
    const int x = 19801;
    std::cout << 19801;
    for (int i = 0; i < 19801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19801(int& val) {
    const int x = 19801;
    std::cout << 19801;
    for (int i = 0; i < 19801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19802(int& val) {
    func_in_19802(val);
    const int x = 19802;
    std::cout << 19802;
    for (int i = 0; i < 19802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19802(int& val) {
    const int x = 19802;
    std::cout << 19802;
    for (int i = 0; i < 19802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19803(int& val) {
    func_in_19803(val);
    const int x = 19803;
    std::cout << 19803;
    for (int i = 0; i < 19803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19803(int& val) {
    const int x = 19803;
    std::cout << 19803;
    for (int i = 0; i < 19803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19804(int& val) {
    func_in_19804(val);
    const int x = 19804;
    std::cout << 19804;
    for (int i = 0; i < 19804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19804(int& val) {
    const int x = 19804;
    std::cout << 19804;
    for (int i = 0; i < 19804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19805(int& val) {
    func_in_19805(val);
    const int x = 19805;
    std::cout << 19805;
    for (int i = 0; i < 19805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19805(int& val) {
    const int x = 19805;
    std::cout << 19805;
    for (int i = 0; i < 19805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19806(int& val) {
    func_in_19806(val);
    const int x = 19806;
    std::cout << 19806;
    for (int i = 0; i < 19806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19806(int& val) {
    const int x = 19806;
    std::cout << 19806;
    for (int i = 0; i < 19806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19807(int& val) {
    func_in_19807(val);
    const int x = 19807;
    std::cout << 19807;
    for (int i = 0; i < 19807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19807(int& val) {
    const int x = 19807;
    std::cout << 19807;
    for (int i = 0; i < 19807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19808(int& val) {
    func_in_19808(val);
    const int x = 19808;
    std::cout << 19808;
    for (int i = 0; i < 19808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19808(int& val) {
    const int x = 19808;
    std::cout << 19808;
    for (int i = 0; i < 19808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19809(int& val) {
    func_in_19809(val);
    const int x = 19809;
    std::cout << 19809;
    for (int i = 0; i < 19809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19809(int& val) {
    const int x = 19809;
    std::cout << 19809;
    for (int i = 0; i < 19809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19810(int& val) {
    func_in_19810(val);
    const int x = 19810;
    std::cout << 19810;
    for (int i = 0; i < 19810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19810(int& val) {
    const int x = 19810;
    std::cout << 19810;
    for (int i = 0; i < 19810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19811(int& val) {
    func_in_19811(val);
    const int x = 19811;
    std::cout << 19811;
    for (int i = 0; i < 19811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19811(int& val) {
    const int x = 19811;
    std::cout << 19811;
    for (int i = 0; i < 19811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19812(int& val) {
    func_in_19812(val);
    const int x = 19812;
    std::cout << 19812;
    for (int i = 0; i < 19812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19812(int& val) {
    const int x = 19812;
    std::cout << 19812;
    for (int i = 0; i < 19812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19813(int& val) {
    func_in_19813(val);
    const int x = 19813;
    std::cout << 19813;
    for (int i = 0; i < 19813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19813(int& val) {
    const int x = 19813;
    std::cout << 19813;
    for (int i = 0; i < 19813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19814(int& val) {
    func_in_19814(val);
    const int x = 19814;
    std::cout << 19814;
    for (int i = 0; i < 19814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19814(int& val) {
    const int x = 19814;
    std::cout << 19814;
    for (int i = 0; i < 19814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19815(int& val) {
    func_in_19815(val);
    const int x = 19815;
    std::cout << 19815;
    for (int i = 0; i < 19815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19815(int& val) {
    const int x = 19815;
    std::cout << 19815;
    for (int i = 0; i < 19815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19816(int& val) {
    func_in_19816(val);
    const int x = 19816;
    std::cout << 19816;
    for (int i = 0; i < 19816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19816(int& val) {
    const int x = 19816;
    std::cout << 19816;
    for (int i = 0; i < 19816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19817(int& val) {
    func_in_19817(val);
    const int x = 19817;
    std::cout << 19817;
    for (int i = 0; i < 19817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19817(int& val) {
    const int x = 19817;
    std::cout << 19817;
    for (int i = 0; i < 19817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19818(int& val) {
    func_in_19818(val);
    const int x = 19818;
    std::cout << 19818;
    for (int i = 0; i < 19818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19818(int& val) {
    const int x = 19818;
    std::cout << 19818;
    for (int i = 0; i < 19818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19819(int& val) {
    func_in_19819(val);
    const int x = 19819;
    std::cout << 19819;
    for (int i = 0; i < 19819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19819(int& val) {
    const int x = 19819;
    std::cout << 19819;
    for (int i = 0; i < 19819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19820(int& val) {
    func_in_19820(val);
    const int x = 19820;
    std::cout << 19820;
    for (int i = 0; i < 19820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19820(int& val) {
    const int x = 19820;
    std::cout << 19820;
    for (int i = 0; i < 19820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19821(int& val) {
    func_in_19821(val);
    const int x = 19821;
    std::cout << 19821;
    for (int i = 0; i < 19821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19821(int& val) {
    const int x = 19821;
    std::cout << 19821;
    for (int i = 0; i < 19821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19822(int& val) {
    func_in_19822(val);
    const int x = 19822;
    std::cout << 19822;
    for (int i = 0; i < 19822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19822(int& val) {
    const int x = 19822;
    std::cout << 19822;
    for (int i = 0; i < 19822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19823(int& val) {
    func_in_19823(val);
    const int x = 19823;
    std::cout << 19823;
    for (int i = 0; i < 19823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19823(int& val) {
    const int x = 19823;
    std::cout << 19823;
    for (int i = 0; i < 19823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19824(int& val) {
    func_in_19824(val);
    const int x = 19824;
    std::cout << 19824;
    for (int i = 0; i < 19824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19824(int& val) {
    const int x = 19824;
    std::cout << 19824;
    for (int i = 0; i < 19824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19825(int& val) {
    func_in_19825(val);
    const int x = 19825;
    std::cout << 19825;
    for (int i = 0; i < 19825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19825(int& val) {
    const int x = 19825;
    std::cout << 19825;
    for (int i = 0; i < 19825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19826(int& val) {
    func_in_19826(val);
    const int x = 19826;
    std::cout << 19826;
    for (int i = 0; i < 19826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19826(int& val) {
    const int x = 19826;
    std::cout << 19826;
    for (int i = 0; i < 19826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19827(int& val) {
    func_in_19827(val);
    const int x = 19827;
    std::cout << 19827;
    for (int i = 0; i < 19827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19827(int& val) {
    const int x = 19827;
    std::cout << 19827;
    for (int i = 0; i < 19827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19828(int& val) {
    func_in_19828(val);
    const int x = 19828;
    std::cout << 19828;
    for (int i = 0; i < 19828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19828(int& val) {
    const int x = 19828;
    std::cout << 19828;
    for (int i = 0; i < 19828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19829(int& val) {
    func_in_19829(val);
    const int x = 19829;
    std::cout << 19829;
    for (int i = 0; i < 19829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19829(int& val) {
    const int x = 19829;
    std::cout << 19829;
    for (int i = 0; i < 19829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19830(int& val) {
    func_in_19830(val);
    const int x = 19830;
    std::cout << 19830;
    for (int i = 0; i < 19830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19830(int& val) {
    const int x = 19830;
    std::cout << 19830;
    for (int i = 0; i < 19830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19831(int& val) {
    func_in_19831(val);
    const int x = 19831;
    std::cout << 19831;
    for (int i = 0; i < 19831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19831(int& val) {
    const int x = 19831;
    std::cout << 19831;
    for (int i = 0; i < 19831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19832(int& val) {
    func_in_19832(val);
    const int x = 19832;
    std::cout << 19832;
    for (int i = 0; i < 19832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19832(int& val) {
    const int x = 19832;
    std::cout << 19832;
    for (int i = 0; i < 19832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19833(int& val) {
    func_in_19833(val);
    const int x = 19833;
    std::cout << 19833;
    for (int i = 0; i < 19833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19833(int& val) {
    const int x = 19833;
    std::cout << 19833;
    for (int i = 0; i < 19833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19834(int& val) {
    func_in_19834(val);
    const int x = 19834;
    std::cout << 19834;
    for (int i = 0; i < 19834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19834(int& val) {
    const int x = 19834;
    std::cout << 19834;
    for (int i = 0; i < 19834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19835(int& val) {
    func_in_19835(val);
    const int x = 19835;
    std::cout << 19835;
    for (int i = 0; i < 19835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19835(int& val) {
    const int x = 19835;
    std::cout << 19835;
    for (int i = 0; i < 19835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19836(int& val) {
    func_in_19836(val);
    const int x = 19836;
    std::cout << 19836;
    for (int i = 0; i < 19836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19836(int& val) {
    const int x = 19836;
    std::cout << 19836;
    for (int i = 0; i < 19836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19837(int& val) {
    func_in_19837(val);
    const int x = 19837;
    std::cout << 19837;
    for (int i = 0; i < 19837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19837(int& val) {
    const int x = 19837;
    std::cout << 19837;
    for (int i = 0; i < 19837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19838(int& val) {
    func_in_19838(val);
    const int x = 19838;
    std::cout << 19838;
    for (int i = 0; i < 19838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19838(int& val) {
    const int x = 19838;
    std::cout << 19838;
    for (int i = 0; i < 19838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19839(int& val) {
    func_in_19839(val);
    const int x = 19839;
    std::cout << 19839;
    for (int i = 0; i < 19839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19839(int& val) {
    const int x = 19839;
    std::cout << 19839;
    for (int i = 0; i < 19839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19840(int& val) {
    func_in_19840(val);
    const int x = 19840;
    std::cout << 19840;
    for (int i = 0; i < 19840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19840(int& val) {
    const int x = 19840;
    std::cout << 19840;
    for (int i = 0; i < 19840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19841(int& val) {
    func_in_19841(val);
    const int x = 19841;
    std::cout << 19841;
    for (int i = 0; i < 19841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19841(int& val) {
    const int x = 19841;
    std::cout << 19841;
    for (int i = 0; i < 19841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19842(int& val) {
    func_in_19842(val);
    const int x = 19842;
    std::cout << 19842;
    for (int i = 0; i < 19842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19842(int& val) {
    const int x = 19842;
    std::cout << 19842;
    for (int i = 0; i < 19842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19843(int& val) {
    func_in_19843(val);
    const int x = 19843;
    std::cout << 19843;
    for (int i = 0; i < 19843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19843(int& val) {
    const int x = 19843;
    std::cout << 19843;
    for (int i = 0; i < 19843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19844(int& val) {
    func_in_19844(val);
    const int x = 19844;
    std::cout << 19844;
    for (int i = 0; i < 19844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19844(int& val) {
    const int x = 19844;
    std::cout << 19844;
    for (int i = 0; i < 19844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19845(int& val) {
    func_in_19845(val);
    const int x = 19845;
    std::cout << 19845;
    for (int i = 0; i < 19845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19845(int& val) {
    const int x = 19845;
    std::cout << 19845;
    for (int i = 0; i < 19845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19846(int& val) {
    func_in_19846(val);
    const int x = 19846;
    std::cout << 19846;
    for (int i = 0; i < 19846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19846(int& val) {
    const int x = 19846;
    std::cout << 19846;
    for (int i = 0; i < 19846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19847(int& val) {
    func_in_19847(val);
    const int x = 19847;
    std::cout << 19847;
    for (int i = 0; i < 19847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19847(int& val) {
    const int x = 19847;
    std::cout << 19847;
    for (int i = 0; i < 19847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19848(int& val) {
    func_in_19848(val);
    const int x = 19848;
    std::cout << 19848;
    for (int i = 0; i < 19848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19848(int& val) {
    const int x = 19848;
    std::cout << 19848;
    for (int i = 0; i < 19848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19849(int& val) {
    func_in_19849(val);
    const int x = 19849;
    std::cout << 19849;
    for (int i = 0; i < 19849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19849(int& val) {
    const int x = 19849;
    std::cout << 19849;
    for (int i = 0; i < 19849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19850(int& val) {
    func_in_19850(val);
    const int x = 19850;
    std::cout << 19850;
    for (int i = 0; i < 19850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19850(int& val) {
    const int x = 19850;
    std::cout << 19850;
    for (int i = 0; i < 19850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19851(int& val) {
    func_in_19851(val);
    const int x = 19851;
    std::cout << 19851;
    for (int i = 0; i < 19851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19851(int& val) {
    const int x = 19851;
    std::cout << 19851;
    for (int i = 0; i < 19851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19852(int& val) {
    func_in_19852(val);
    const int x = 19852;
    std::cout << 19852;
    for (int i = 0; i < 19852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19852(int& val) {
    const int x = 19852;
    std::cout << 19852;
    for (int i = 0; i < 19852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19853(int& val) {
    func_in_19853(val);
    const int x = 19853;
    std::cout << 19853;
    for (int i = 0; i < 19853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19853(int& val) {
    const int x = 19853;
    std::cout << 19853;
    for (int i = 0; i < 19853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19854(int& val) {
    func_in_19854(val);
    const int x = 19854;
    std::cout << 19854;
    for (int i = 0; i < 19854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19854(int& val) {
    const int x = 19854;
    std::cout << 19854;
    for (int i = 0; i < 19854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19855(int& val) {
    func_in_19855(val);
    const int x = 19855;
    std::cout << 19855;
    for (int i = 0; i < 19855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19855(int& val) {
    const int x = 19855;
    std::cout << 19855;
    for (int i = 0; i < 19855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19856(int& val) {
    func_in_19856(val);
    const int x = 19856;
    std::cout << 19856;
    for (int i = 0; i < 19856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19856(int& val) {
    const int x = 19856;
    std::cout << 19856;
    for (int i = 0; i < 19856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19857(int& val) {
    func_in_19857(val);
    const int x = 19857;
    std::cout << 19857;
    for (int i = 0; i < 19857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19857(int& val) {
    const int x = 19857;
    std::cout << 19857;
    for (int i = 0; i < 19857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19858(int& val) {
    func_in_19858(val);
    const int x = 19858;
    std::cout << 19858;
    for (int i = 0; i < 19858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19858(int& val) {
    const int x = 19858;
    std::cout << 19858;
    for (int i = 0; i < 19858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19859(int& val) {
    func_in_19859(val);
    const int x = 19859;
    std::cout << 19859;
    for (int i = 0; i < 19859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19859(int& val) {
    const int x = 19859;
    std::cout << 19859;
    for (int i = 0; i < 19859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19860(int& val) {
    func_in_19860(val);
    const int x = 19860;
    std::cout << 19860;
    for (int i = 0; i < 19860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19860(int& val) {
    const int x = 19860;
    std::cout << 19860;
    for (int i = 0; i < 19860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19861(int& val) {
    func_in_19861(val);
    const int x = 19861;
    std::cout << 19861;
    for (int i = 0; i < 19861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19861(int& val) {
    const int x = 19861;
    std::cout << 19861;
    for (int i = 0; i < 19861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19862(int& val) {
    func_in_19862(val);
    const int x = 19862;
    std::cout << 19862;
    for (int i = 0; i < 19862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19862(int& val) {
    const int x = 19862;
    std::cout << 19862;
    for (int i = 0; i < 19862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19863(int& val) {
    func_in_19863(val);
    const int x = 19863;
    std::cout << 19863;
    for (int i = 0; i < 19863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19863(int& val) {
    const int x = 19863;
    std::cout << 19863;
    for (int i = 0; i < 19863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19864(int& val) {
    func_in_19864(val);
    const int x = 19864;
    std::cout << 19864;
    for (int i = 0; i < 19864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19864(int& val) {
    const int x = 19864;
    std::cout << 19864;
    for (int i = 0; i < 19864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19865(int& val) {
    func_in_19865(val);
    const int x = 19865;
    std::cout << 19865;
    for (int i = 0; i < 19865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19865(int& val) {
    const int x = 19865;
    std::cout << 19865;
    for (int i = 0; i < 19865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19866(int& val) {
    func_in_19866(val);
    const int x = 19866;
    std::cout << 19866;
    for (int i = 0; i < 19866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19866(int& val) {
    const int x = 19866;
    std::cout << 19866;
    for (int i = 0; i < 19866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19867(int& val) {
    func_in_19867(val);
    const int x = 19867;
    std::cout << 19867;
    for (int i = 0; i < 19867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19867(int& val) {
    const int x = 19867;
    std::cout << 19867;
    for (int i = 0; i < 19867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19868(int& val) {
    func_in_19868(val);
    const int x = 19868;
    std::cout << 19868;
    for (int i = 0; i < 19868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19868(int& val) {
    const int x = 19868;
    std::cout << 19868;
    for (int i = 0; i < 19868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19869(int& val) {
    func_in_19869(val);
    const int x = 19869;
    std::cout << 19869;
    for (int i = 0; i < 19869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19869(int& val) {
    const int x = 19869;
    std::cout << 19869;
    for (int i = 0; i < 19869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19870(int& val) {
    func_in_19870(val);
    const int x = 19870;
    std::cout << 19870;
    for (int i = 0; i < 19870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19870(int& val) {
    const int x = 19870;
    std::cout << 19870;
    for (int i = 0; i < 19870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19871(int& val) {
    func_in_19871(val);
    const int x = 19871;
    std::cout << 19871;
    for (int i = 0; i < 19871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19871(int& val) {
    const int x = 19871;
    std::cout << 19871;
    for (int i = 0; i < 19871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19872(int& val) {
    func_in_19872(val);
    const int x = 19872;
    std::cout << 19872;
    for (int i = 0; i < 19872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19872(int& val) {
    const int x = 19872;
    std::cout << 19872;
    for (int i = 0; i < 19872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19873(int& val) {
    func_in_19873(val);
    const int x = 19873;
    std::cout << 19873;
    for (int i = 0; i < 19873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19873(int& val) {
    const int x = 19873;
    std::cout << 19873;
    for (int i = 0; i < 19873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19874(int& val) {
    func_in_19874(val);
    const int x = 19874;
    std::cout << 19874;
    for (int i = 0; i < 19874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19874(int& val) {
    const int x = 19874;
    std::cout << 19874;
    for (int i = 0; i < 19874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19875(int& val) {
    func_in_19875(val);
    const int x = 19875;
    std::cout << 19875;
    for (int i = 0; i < 19875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19875(int& val) {
    const int x = 19875;
    std::cout << 19875;
    for (int i = 0; i < 19875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19876(int& val) {
    func_in_19876(val);
    const int x = 19876;
    std::cout << 19876;
    for (int i = 0; i < 19876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19876(int& val) {
    const int x = 19876;
    std::cout << 19876;
    for (int i = 0; i < 19876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19877(int& val) {
    func_in_19877(val);
    const int x = 19877;
    std::cout << 19877;
    for (int i = 0; i < 19877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19877(int& val) {
    const int x = 19877;
    std::cout << 19877;
    for (int i = 0; i < 19877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19878(int& val) {
    func_in_19878(val);
    const int x = 19878;
    std::cout << 19878;
    for (int i = 0; i < 19878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19878(int& val) {
    const int x = 19878;
    std::cout << 19878;
    for (int i = 0; i < 19878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19879(int& val) {
    func_in_19879(val);
    const int x = 19879;
    std::cout << 19879;
    for (int i = 0; i < 19879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19879(int& val) {
    const int x = 19879;
    std::cout << 19879;
    for (int i = 0; i < 19879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19880(int& val) {
    func_in_19880(val);
    const int x = 19880;
    std::cout << 19880;
    for (int i = 0; i < 19880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19880(int& val) {
    const int x = 19880;
    std::cout << 19880;
    for (int i = 0; i < 19880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19881(int& val) {
    func_in_19881(val);
    const int x = 19881;
    std::cout << 19881;
    for (int i = 0; i < 19881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19881(int& val) {
    const int x = 19881;
    std::cout << 19881;
    for (int i = 0; i < 19881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19882(int& val) {
    func_in_19882(val);
    const int x = 19882;
    std::cout << 19882;
    for (int i = 0; i < 19882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19882(int& val) {
    const int x = 19882;
    std::cout << 19882;
    for (int i = 0; i < 19882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19883(int& val) {
    func_in_19883(val);
    const int x = 19883;
    std::cout << 19883;
    for (int i = 0; i < 19883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19883(int& val) {
    const int x = 19883;
    std::cout << 19883;
    for (int i = 0; i < 19883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19884(int& val) {
    func_in_19884(val);
    const int x = 19884;
    std::cout << 19884;
    for (int i = 0; i < 19884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19884(int& val) {
    const int x = 19884;
    std::cout << 19884;
    for (int i = 0; i < 19884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19885(int& val) {
    func_in_19885(val);
    const int x = 19885;
    std::cout << 19885;
    for (int i = 0; i < 19885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19885(int& val) {
    const int x = 19885;
    std::cout << 19885;
    for (int i = 0; i < 19885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19886(int& val) {
    func_in_19886(val);
    const int x = 19886;
    std::cout << 19886;
    for (int i = 0; i < 19886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19886(int& val) {
    const int x = 19886;
    std::cout << 19886;
    for (int i = 0; i < 19886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19887(int& val) {
    func_in_19887(val);
    const int x = 19887;
    std::cout << 19887;
    for (int i = 0; i < 19887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19887(int& val) {
    const int x = 19887;
    std::cout << 19887;
    for (int i = 0; i < 19887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19888(int& val) {
    func_in_19888(val);
    const int x = 19888;
    std::cout << 19888;
    for (int i = 0; i < 19888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19888(int& val) {
    const int x = 19888;
    std::cout << 19888;
    for (int i = 0; i < 19888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19889(int& val) {
    func_in_19889(val);
    const int x = 19889;
    std::cout << 19889;
    for (int i = 0; i < 19889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19889(int& val) {
    const int x = 19889;
    std::cout << 19889;
    for (int i = 0; i < 19889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19890(int& val) {
    func_in_19890(val);
    const int x = 19890;
    std::cout << 19890;
    for (int i = 0; i < 19890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19890(int& val) {
    const int x = 19890;
    std::cout << 19890;
    for (int i = 0; i < 19890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19891(int& val) {
    func_in_19891(val);
    const int x = 19891;
    std::cout << 19891;
    for (int i = 0; i < 19891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19891(int& val) {
    const int x = 19891;
    std::cout << 19891;
    for (int i = 0; i < 19891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19892(int& val) {
    func_in_19892(val);
    const int x = 19892;
    std::cout << 19892;
    for (int i = 0; i < 19892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19892(int& val) {
    const int x = 19892;
    std::cout << 19892;
    for (int i = 0; i < 19892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19893(int& val) {
    func_in_19893(val);
    const int x = 19893;
    std::cout << 19893;
    for (int i = 0; i < 19893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19893(int& val) {
    const int x = 19893;
    std::cout << 19893;
    for (int i = 0; i < 19893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19894(int& val) {
    func_in_19894(val);
    const int x = 19894;
    std::cout << 19894;
    for (int i = 0; i < 19894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19894(int& val) {
    const int x = 19894;
    std::cout << 19894;
    for (int i = 0; i < 19894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19895(int& val) {
    func_in_19895(val);
    const int x = 19895;
    std::cout << 19895;
    for (int i = 0; i < 19895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19895(int& val) {
    const int x = 19895;
    std::cout << 19895;
    for (int i = 0; i < 19895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19896(int& val) {
    func_in_19896(val);
    const int x = 19896;
    std::cout << 19896;
    for (int i = 0; i < 19896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19896(int& val) {
    const int x = 19896;
    std::cout << 19896;
    for (int i = 0; i < 19896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19897(int& val) {
    func_in_19897(val);
    const int x = 19897;
    std::cout << 19897;
    for (int i = 0; i < 19897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19897(int& val) {
    const int x = 19897;
    std::cout << 19897;
    for (int i = 0; i < 19897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19898(int& val) {
    func_in_19898(val);
    const int x = 19898;
    std::cout << 19898;
    for (int i = 0; i < 19898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19898(int& val) {
    const int x = 19898;
    std::cout << 19898;
    for (int i = 0; i < 19898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19899(int& val) {
    func_in_19899(val);
    const int x = 19899;
    std::cout << 19899;
    for (int i = 0; i < 19899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19899(int& val) {
    const int x = 19899;
    std::cout << 19899;
    for (int i = 0; i < 19899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19900(int& val) {
    func_in_19900(val);
    const int x = 19900;
    std::cout << 19900;
    for (int i = 0; i < 19900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19900(int& val) {
    const int x = 19900;
    std::cout << 19900;
    for (int i = 0; i < 19900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19901(int& val) {
    func_in_19901(val);
    const int x = 19901;
    std::cout << 19901;
    for (int i = 0; i < 19901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19901(int& val) {
    const int x = 19901;
    std::cout << 19901;
    for (int i = 0; i < 19901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19902(int& val) {
    func_in_19902(val);
    const int x = 19902;
    std::cout << 19902;
    for (int i = 0; i < 19902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19902(int& val) {
    const int x = 19902;
    std::cout << 19902;
    for (int i = 0; i < 19902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19903(int& val) {
    func_in_19903(val);
    const int x = 19903;
    std::cout << 19903;
    for (int i = 0; i < 19903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19903(int& val) {
    const int x = 19903;
    std::cout << 19903;
    for (int i = 0; i < 19903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19904(int& val) {
    func_in_19904(val);
    const int x = 19904;
    std::cout << 19904;
    for (int i = 0; i < 19904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19904(int& val) {
    const int x = 19904;
    std::cout << 19904;
    for (int i = 0; i < 19904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19905(int& val) {
    func_in_19905(val);
    const int x = 19905;
    std::cout << 19905;
    for (int i = 0; i < 19905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19905(int& val) {
    const int x = 19905;
    std::cout << 19905;
    for (int i = 0; i < 19905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19906(int& val) {
    func_in_19906(val);
    const int x = 19906;
    std::cout << 19906;
    for (int i = 0; i < 19906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19906(int& val) {
    const int x = 19906;
    std::cout << 19906;
    for (int i = 0; i < 19906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19907(int& val) {
    func_in_19907(val);
    const int x = 19907;
    std::cout << 19907;
    for (int i = 0; i < 19907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19907(int& val) {
    const int x = 19907;
    std::cout << 19907;
    for (int i = 0; i < 19907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19908(int& val) {
    func_in_19908(val);
    const int x = 19908;
    std::cout << 19908;
    for (int i = 0; i < 19908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19908(int& val) {
    const int x = 19908;
    std::cout << 19908;
    for (int i = 0; i < 19908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19909(int& val) {
    func_in_19909(val);
    const int x = 19909;
    std::cout << 19909;
    for (int i = 0; i < 19909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19909(int& val) {
    const int x = 19909;
    std::cout << 19909;
    for (int i = 0; i < 19909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19910(int& val) {
    func_in_19910(val);
    const int x = 19910;
    std::cout << 19910;
    for (int i = 0; i < 19910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19910(int& val) {
    const int x = 19910;
    std::cout << 19910;
    for (int i = 0; i < 19910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19911(int& val) {
    func_in_19911(val);
    const int x = 19911;
    std::cout << 19911;
    for (int i = 0; i < 19911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19911(int& val) {
    const int x = 19911;
    std::cout << 19911;
    for (int i = 0; i < 19911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19912(int& val) {
    func_in_19912(val);
    const int x = 19912;
    std::cout << 19912;
    for (int i = 0; i < 19912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19912(int& val) {
    const int x = 19912;
    std::cout << 19912;
    for (int i = 0; i < 19912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19913(int& val) {
    func_in_19913(val);
    const int x = 19913;
    std::cout << 19913;
    for (int i = 0; i < 19913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19913(int& val) {
    const int x = 19913;
    std::cout << 19913;
    for (int i = 0; i < 19913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19914(int& val) {
    func_in_19914(val);
    const int x = 19914;
    std::cout << 19914;
    for (int i = 0; i < 19914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19914(int& val) {
    const int x = 19914;
    std::cout << 19914;
    for (int i = 0; i < 19914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19915(int& val) {
    func_in_19915(val);
    const int x = 19915;
    std::cout << 19915;
    for (int i = 0; i < 19915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19915(int& val) {
    const int x = 19915;
    std::cout << 19915;
    for (int i = 0; i < 19915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19916(int& val) {
    func_in_19916(val);
    const int x = 19916;
    std::cout << 19916;
    for (int i = 0; i < 19916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19916(int& val) {
    const int x = 19916;
    std::cout << 19916;
    for (int i = 0; i < 19916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19917(int& val) {
    func_in_19917(val);
    const int x = 19917;
    std::cout << 19917;
    for (int i = 0; i < 19917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19917(int& val) {
    const int x = 19917;
    std::cout << 19917;
    for (int i = 0; i < 19917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19918(int& val) {
    func_in_19918(val);
    const int x = 19918;
    std::cout << 19918;
    for (int i = 0; i < 19918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19918(int& val) {
    const int x = 19918;
    std::cout << 19918;
    for (int i = 0; i < 19918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19919(int& val) {
    func_in_19919(val);
    const int x = 19919;
    std::cout << 19919;
    for (int i = 0; i < 19919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19919(int& val) {
    const int x = 19919;
    std::cout << 19919;
    for (int i = 0; i < 19919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19920(int& val) {
    func_in_19920(val);
    const int x = 19920;
    std::cout << 19920;
    for (int i = 0; i < 19920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19920(int& val) {
    const int x = 19920;
    std::cout << 19920;
    for (int i = 0; i < 19920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19921(int& val) {
    func_in_19921(val);
    const int x = 19921;
    std::cout << 19921;
    for (int i = 0; i < 19921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19921(int& val) {
    const int x = 19921;
    std::cout << 19921;
    for (int i = 0; i < 19921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19922(int& val) {
    func_in_19922(val);
    const int x = 19922;
    std::cout << 19922;
    for (int i = 0; i < 19922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19922(int& val) {
    const int x = 19922;
    std::cout << 19922;
    for (int i = 0; i < 19922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19923(int& val) {
    func_in_19923(val);
    const int x = 19923;
    std::cout << 19923;
    for (int i = 0; i < 19923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19923(int& val) {
    const int x = 19923;
    std::cout << 19923;
    for (int i = 0; i < 19923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19924(int& val) {
    func_in_19924(val);
    const int x = 19924;
    std::cout << 19924;
    for (int i = 0; i < 19924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19924(int& val) {
    const int x = 19924;
    std::cout << 19924;
    for (int i = 0; i < 19924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19925(int& val) {
    func_in_19925(val);
    const int x = 19925;
    std::cout << 19925;
    for (int i = 0; i < 19925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19925(int& val) {
    const int x = 19925;
    std::cout << 19925;
    for (int i = 0; i < 19925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19926(int& val) {
    func_in_19926(val);
    const int x = 19926;
    std::cout << 19926;
    for (int i = 0; i < 19926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19926(int& val) {
    const int x = 19926;
    std::cout << 19926;
    for (int i = 0; i < 19926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19927(int& val) {
    func_in_19927(val);
    const int x = 19927;
    std::cout << 19927;
    for (int i = 0; i < 19927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19927(int& val) {
    const int x = 19927;
    std::cout << 19927;
    for (int i = 0; i < 19927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19928(int& val) {
    func_in_19928(val);
    const int x = 19928;
    std::cout << 19928;
    for (int i = 0; i < 19928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19928(int& val) {
    const int x = 19928;
    std::cout << 19928;
    for (int i = 0; i < 19928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19929(int& val) {
    func_in_19929(val);
    const int x = 19929;
    std::cout << 19929;
    for (int i = 0; i < 19929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19929(int& val) {
    const int x = 19929;
    std::cout << 19929;
    for (int i = 0; i < 19929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19930(int& val) {
    func_in_19930(val);
    const int x = 19930;
    std::cout << 19930;
    for (int i = 0; i < 19930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19930(int& val) {
    const int x = 19930;
    std::cout << 19930;
    for (int i = 0; i < 19930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19931(int& val) {
    func_in_19931(val);
    const int x = 19931;
    std::cout << 19931;
    for (int i = 0; i < 19931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19931(int& val) {
    const int x = 19931;
    std::cout << 19931;
    for (int i = 0; i < 19931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19932(int& val) {
    func_in_19932(val);
    const int x = 19932;
    std::cout << 19932;
    for (int i = 0; i < 19932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19932(int& val) {
    const int x = 19932;
    std::cout << 19932;
    for (int i = 0; i < 19932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19933(int& val) {
    func_in_19933(val);
    const int x = 19933;
    std::cout << 19933;
    for (int i = 0; i < 19933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19933(int& val) {
    const int x = 19933;
    std::cout << 19933;
    for (int i = 0; i < 19933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19934(int& val) {
    func_in_19934(val);
    const int x = 19934;
    std::cout << 19934;
    for (int i = 0; i < 19934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19934(int& val) {
    const int x = 19934;
    std::cout << 19934;
    for (int i = 0; i < 19934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19935(int& val) {
    func_in_19935(val);
    const int x = 19935;
    std::cout << 19935;
    for (int i = 0; i < 19935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19935(int& val) {
    const int x = 19935;
    std::cout << 19935;
    for (int i = 0; i < 19935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19936(int& val) {
    func_in_19936(val);
    const int x = 19936;
    std::cout << 19936;
    for (int i = 0; i < 19936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19936(int& val) {
    const int x = 19936;
    std::cout << 19936;
    for (int i = 0; i < 19936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19937(int& val) {
    func_in_19937(val);
    const int x = 19937;
    std::cout << 19937;
    for (int i = 0; i < 19937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19937(int& val) {
    const int x = 19937;
    std::cout << 19937;
    for (int i = 0; i < 19937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19938(int& val) {
    func_in_19938(val);
    const int x = 19938;
    std::cout << 19938;
    for (int i = 0; i < 19938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19938(int& val) {
    const int x = 19938;
    std::cout << 19938;
    for (int i = 0; i < 19938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19939(int& val) {
    func_in_19939(val);
    const int x = 19939;
    std::cout << 19939;
    for (int i = 0; i < 19939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19939(int& val) {
    const int x = 19939;
    std::cout << 19939;
    for (int i = 0; i < 19939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19940(int& val) {
    func_in_19940(val);
    const int x = 19940;
    std::cout << 19940;
    for (int i = 0; i < 19940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19940(int& val) {
    const int x = 19940;
    std::cout << 19940;
    for (int i = 0; i < 19940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19941(int& val) {
    func_in_19941(val);
    const int x = 19941;
    std::cout << 19941;
    for (int i = 0; i < 19941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19941(int& val) {
    const int x = 19941;
    std::cout << 19941;
    for (int i = 0; i < 19941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19942(int& val) {
    func_in_19942(val);
    const int x = 19942;
    std::cout << 19942;
    for (int i = 0; i < 19942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19942(int& val) {
    const int x = 19942;
    std::cout << 19942;
    for (int i = 0; i < 19942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19943(int& val) {
    func_in_19943(val);
    const int x = 19943;
    std::cout << 19943;
    for (int i = 0; i < 19943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19943(int& val) {
    const int x = 19943;
    std::cout << 19943;
    for (int i = 0; i < 19943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19944(int& val) {
    func_in_19944(val);
    const int x = 19944;
    std::cout << 19944;
    for (int i = 0; i < 19944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19944(int& val) {
    const int x = 19944;
    std::cout << 19944;
    for (int i = 0; i < 19944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19945(int& val) {
    func_in_19945(val);
    const int x = 19945;
    std::cout << 19945;
    for (int i = 0; i < 19945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19945(int& val) {
    const int x = 19945;
    std::cout << 19945;
    for (int i = 0; i < 19945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19946(int& val) {
    func_in_19946(val);
    const int x = 19946;
    std::cout << 19946;
    for (int i = 0; i < 19946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19946(int& val) {
    const int x = 19946;
    std::cout << 19946;
    for (int i = 0; i < 19946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19947(int& val) {
    func_in_19947(val);
    const int x = 19947;
    std::cout << 19947;
    for (int i = 0; i < 19947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19947(int& val) {
    const int x = 19947;
    std::cout << 19947;
    for (int i = 0; i < 19947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19948(int& val) {
    func_in_19948(val);
    const int x = 19948;
    std::cout << 19948;
    for (int i = 0; i < 19948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19948(int& val) {
    const int x = 19948;
    std::cout << 19948;
    for (int i = 0; i < 19948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19949(int& val) {
    func_in_19949(val);
    const int x = 19949;
    std::cout << 19949;
    for (int i = 0; i < 19949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19949(int& val) {
    const int x = 19949;
    std::cout << 19949;
    for (int i = 0; i < 19949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19950(int& val) {
    func_in_19950(val);
    const int x = 19950;
    std::cout << 19950;
    for (int i = 0; i < 19950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19950(int& val) {
    const int x = 19950;
    std::cout << 19950;
    for (int i = 0; i < 19950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19951(int& val) {
    func_in_19951(val);
    const int x = 19951;
    std::cout << 19951;
    for (int i = 0; i < 19951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19951(int& val) {
    const int x = 19951;
    std::cout << 19951;
    for (int i = 0; i < 19951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19952(int& val) {
    func_in_19952(val);
    const int x = 19952;
    std::cout << 19952;
    for (int i = 0; i < 19952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19952(int& val) {
    const int x = 19952;
    std::cout << 19952;
    for (int i = 0; i < 19952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19953(int& val) {
    func_in_19953(val);
    const int x = 19953;
    std::cout << 19953;
    for (int i = 0; i < 19953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19953(int& val) {
    const int x = 19953;
    std::cout << 19953;
    for (int i = 0; i < 19953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19954(int& val) {
    func_in_19954(val);
    const int x = 19954;
    std::cout << 19954;
    for (int i = 0; i < 19954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19954(int& val) {
    const int x = 19954;
    std::cout << 19954;
    for (int i = 0; i < 19954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19955(int& val) {
    func_in_19955(val);
    const int x = 19955;
    std::cout << 19955;
    for (int i = 0; i < 19955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19955(int& val) {
    const int x = 19955;
    std::cout << 19955;
    for (int i = 0; i < 19955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19956(int& val) {
    func_in_19956(val);
    const int x = 19956;
    std::cout << 19956;
    for (int i = 0; i < 19956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19956(int& val) {
    const int x = 19956;
    std::cout << 19956;
    for (int i = 0; i < 19956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19957(int& val) {
    func_in_19957(val);
    const int x = 19957;
    std::cout << 19957;
    for (int i = 0; i < 19957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19957(int& val) {
    const int x = 19957;
    std::cout << 19957;
    for (int i = 0; i < 19957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19958(int& val) {
    func_in_19958(val);
    const int x = 19958;
    std::cout << 19958;
    for (int i = 0; i < 19958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19958(int& val) {
    const int x = 19958;
    std::cout << 19958;
    for (int i = 0; i < 19958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19959(int& val) {
    func_in_19959(val);
    const int x = 19959;
    std::cout << 19959;
    for (int i = 0; i < 19959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19959(int& val) {
    const int x = 19959;
    std::cout << 19959;
    for (int i = 0; i < 19959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19960(int& val) {
    func_in_19960(val);
    const int x = 19960;
    std::cout << 19960;
    for (int i = 0; i < 19960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19960(int& val) {
    const int x = 19960;
    std::cout << 19960;
    for (int i = 0; i < 19960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19961(int& val) {
    func_in_19961(val);
    const int x = 19961;
    std::cout << 19961;
    for (int i = 0; i < 19961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19961(int& val) {
    const int x = 19961;
    std::cout << 19961;
    for (int i = 0; i < 19961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19962(int& val) {
    func_in_19962(val);
    const int x = 19962;
    std::cout << 19962;
    for (int i = 0; i < 19962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19962(int& val) {
    const int x = 19962;
    std::cout << 19962;
    for (int i = 0; i < 19962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19963(int& val) {
    func_in_19963(val);
    const int x = 19963;
    std::cout << 19963;
    for (int i = 0; i < 19963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19963(int& val) {
    const int x = 19963;
    std::cout << 19963;
    for (int i = 0; i < 19963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19964(int& val) {
    func_in_19964(val);
    const int x = 19964;
    std::cout << 19964;
    for (int i = 0; i < 19964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19964(int& val) {
    const int x = 19964;
    std::cout << 19964;
    for (int i = 0; i < 19964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19965(int& val) {
    func_in_19965(val);
    const int x = 19965;
    std::cout << 19965;
    for (int i = 0; i < 19965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19965(int& val) {
    const int x = 19965;
    std::cout << 19965;
    for (int i = 0; i < 19965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19966(int& val) {
    func_in_19966(val);
    const int x = 19966;
    std::cout << 19966;
    for (int i = 0; i < 19966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19966(int& val) {
    const int x = 19966;
    std::cout << 19966;
    for (int i = 0; i < 19966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19967(int& val) {
    func_in_19967(val);
    const int x = 19967;
    std::cout << 19967;
    for (int i = 0; i < 19967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19967(int& val) {
    const int x = 19967;
    std::cout << 19967;
    for (int i = 0; i < 19967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19968(int& val) {
    func_in_19968(val);
    const int x = 19968;
    std::cout << 19968;
    for (int i = 0; i < 19968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19968(int& val) {
    const int x = 19968;
    std::cout << 19968;
    for (int i = 0; i < 19968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19969(int& val) {
    func_in_19969(val);
    const int x = 19969;
    std::cout << 19969;
    for (int i = 0; i < 19969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19969(int& val) {
    const int x = 19969;
    std::cout << 19969;
    for (int i = 0; i < 19969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19970(int& val) {
    func_in_19970(val);
    const int x = 19970;
    std::cout << 19970;
    for (int i = 0; i < 19970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19970(int& val) {
    const int x = 19970;
    std::cout << 19970;
    for (int i = 0; i < 19970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19971(int& val) {
    func_in_19971(val);
    const int x = 19971;
    std::cout << 19971;
    for (int i = 0; i < 19971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19971(int& val) {
    const int x = 19971;
    std::cout << 19971;
    for (int i = 0; i < 19971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19972(int& val) {
    func_in_19972(val);
    const int x = 19972;
    std::cout << 19972;
    for (int i = 0; i < 19972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19972(int& val) {
    const int x = 19972;
    std::cout << 19972;
    for (int i = 0; i < 19972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19973(int& val) {
    func_in_19973(val);
    const int x = 19973;
    std::cout << 19973;
    for (int i = 0; i < 19973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19973(int& val) {
    const int x = 19973;
    std::cout << 19973;
    for (int i = 0; i < 19973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19974(int& val) {
    func_in_19974(val);
    const int x = 19974;
    std::cout << 19974;
    for (int i = 0; i < 19974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19974(int& val) {
    const int x = 19974;
    std::cout << 19974;
    for (int i = 0; i < 19974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19975(int& val) {
    func_in_19975(val);
    const int x = 19975;
    std::cout << 19975;
    for (int i = 0; i < 19975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19975(int& val) {
    const int x = 19975;
    std::cout << 19975;
    for (int i = 0; i < 19975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19976(int& val) {
    func_in_19976(val);
    const int x = 19976;
    std::cout << 19976;
    for (int i = 0; i < 19976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19976(int& val) {
    const int x = 19976;
    std::cout << 19976;
    for (int i = 0; i < 19976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19977(int& val) {
    func_in_19977(val);
    const int x = 19977;
    std::cout << 19977;
    for (int i = 0; i < 19977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19977(int& val) {
    const int x = 19977;
    std::cout << 19977;
    for (int i = 0; i < 19977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19978(int& val) {
    func_in_19978(val);
    const int x = 19978;
    std::cout << 19978;
    for (int i = 0; i < 19978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19978(int& val) {
    const int x = 19978;
    std::cout << 19978;
    for (int i = 0; i < 19978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19979(int& val) {
    func_in_19979(val);
    const int x = 19979;
    std::cout << 19979;
    for (int i = 0; i < 19979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19979(int& val) {
    const int x = 19979;
    std::cout << 19979;
    for (int i = 0; i < 19979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19980(int& val) {
    func_in_19980(val);
    const int x = 19980;
    std::cout << 19980;
    for (int i = 0; i < 19980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19980(int& val) {
    const int x = 19980;
    std::cout << 19980;
    for (int i = 0; i < 19980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19981(int& val) {
    func_in_19981(val);
    const int x = 19981;
    std::cout << 19981;
    for (int i = 0; i < 19981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19981(int& val) {
    const int x = 19981;
    std::cout << 19981;
    for (int i = 0; i < 19981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19982(int& val) {
    func_in_19982(val);
    const int x = 19982;
    std::cout << 19982;
    for (int i = 0; i < 19982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19982(int& val) {
    const int x = 19982;
    std::cout << 19982;
    for (int i = 0; i < 19982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19983(int& val) {
    func_in_19983(val);
    const int x = 19983;
    std::cout << 19983;
    for (int i = 0; i < 19983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19983(int& val) {
    const int x = 19983;
    std::cout << 19983;
    for (int i = 0; i < 19983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19984(int& val) {
    func_in_19984(val);
    const int x = 19984;
    std::cout << 19984;
    for (int i = 0; i < 19984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19984(int& val) {
    const int x = 19984;
    std::cout << 19984;
    for (int i = 0; i < 19984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19985(int& val) {
    func_in_19985(val);
    const int x = 19985;
    std::cout << 19985;
    for (int i = 0; i < 19985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19985(int& val) {
    const int x = 19985;
    std::cout << 19985;
    for (int i = 0; i < 19985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19986(int& val) {
    func_in_19986(val);
    const int x = 19986;
    std::cout << 19986;
    for (int i = 0; i < 19986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19986(int& val) {
    const int x = 19986;
    std::cout << 19986;
    for (int i = 0; i < 19986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19987(int& val) {
    func_in_19987(val);
    const int x = 19987;
    std::cout << 19987;
    for (int i = 0; i < 19987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19987(int& val) {
    const int x = 19987;
    std::cout << 19987;
    for (int i = 0; i < 19987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19988(int& val) {
    func_in_19988(val);
    const int x = 19988;
    std::cout << 19988;
    for (int i = 0; i < 19988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19988(int& val) {
    const int x = 19988;
    std::cout << 19988;
    for (int i = 0; i < 19988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19989(int& val) {
    func_in_19989(val);
    const int x = 19989;
    std::cout << 19989;
    for (int i = 0; i < 19989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19989(int& val) {
    const int x = 19989;
    std::cout << 19989;
    for (int i = 0; i < 19989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19990(int& val) {
    func_in_19990(val);
    const int x = 19990;
    std::cout << 19990;
    for (int i = 0; i < 19990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19990(int& val) {
    const int x = 19990;
    std::cout << 19990;
    for (int i = 0; i < 19990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19991(int& val) {
    func_in_19991(val);
    const int x = 19991;
    std::cout << 19991;
    for (int i = 0; i < 19991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19991(int& val) {
    const int x = 19991;
    std::cout << 19991;
    for (int i = 0; i < 19991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19992(int& val) {
    func_in_19992(val);
    const int x = 19992;
    std::cout << 19992;
    for (int i = 0; i < 19992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19992(int& val) {
    const int x = 19992;
    std::cout << 19992;
    for (int i = 0; i < 19992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19993(int& val) {
    func_in_19993(val);
    const int x = 19993;
    std::cout << 19993;
    for (int i = 0; i < 19993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19993(int& val) {
    const int x = 19993;
    std::cout << 19993;
    for (int i = 0; i < 19993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19994(int& val) {
    func_in_19994(val);
    const int x = 19994;
    std::cout << 19994;
    for (int i = 0; i < 19994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19994(int& val) {
    const int x = 19994;
    std::cout << 19994;
    for (int i = 0; i < 19994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19995(int& val) {
    func_in_19995(val);
    const int x = 19995;
    std::cout << 19995;
    for (int i = 0; i < 19995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19995(int& val) {
    const int x = 19995;
    std::cout << 19995;
    for (int i = 0; i < 19995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19996(int& val) {
    func_in_19996(val);
    const int x = 19996;
    std::cout << 19996;
    for (int i = 0; i < 19996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19996(int& val) {
    const int x = 19996;
    std::cout << 19996;
    for (int i = 0; i < 19996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19997(int& val) {
    func_in_19997(val);
    const int x = 19997;
    std::cout << 19997;
    for (int i = 0; i < 19997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19997(int& val) {
    const int x = 19997;
    std::cout << 19997;
    for (int i = 0; i < 19997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19998(int& val) {
    func_in_19998(val);
    const int x = 19998;
    std::cout << 19998;
    for (int i = 0; i < 19998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19998(int& val) {
    const int x = 19998;
    std::cout << 19998;
    for (int i = 0; i < 19998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19999(int& val) {
    func_in_19999(val);
    const int x = 19999;
    std::cout << 19999;
    for (int i = 0; i < 19999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19999(int& val) {
    const int x = 19999;
    std::cout << 19999;
    for (int i = 0; i < 19999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

#include "lib.h"
#include <iostream>
int func_0(int& val) {
    const int x = 0;
    std::cout << 0;
    for (int i = 0; i < 0; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1(int& val) {
    const int x = 1;
    std::cout << 1;
    for (int i = 0; i < 1; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2(int& val) {
    const int x = 2;
    std::cout << 2;
    for (int i = 0; i < 2; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3(int& val) {
    const int x = 3;
    std::cout << 3;
    for (int i = 0; i < 3; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4(int& val) {
    const int x = 4;
    std::cout << 4;
    for (int i = 0; i < 4; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5(int& val) {
    const int x = 5;
    std::cout << 5;
    for (int i = 0; i < 5; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6(int& val) {
    const int x = 6;
    std::cout << 6;
    for (int i = 0; i < 6; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7(int& val) {
    const int x = 7;
    std::cout << 7;
    for (int i = 0; i < 7; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8(int& val) {
    const int x = 8;
    std::cout << 8;
    for (int i = 0; i < 8; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9(int& val) {
    const int x = 9;
    std::cout << 9;
    for (int i = 0; i < 9; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10(int& val) {
    const int x = 10;
    std::cout << 10;
    for (int i = 0; i < 10; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11(int& val) {
    const int x = 11;
    std::cout << 11;
    for (int i = 0; i < 11; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12(int& val) {
    const int x = 12;
    std::cout << 12;
    for (int i = 0; i < 12; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13(int& val) {
    const int x = 13;
    std::cout << 13;
    for (int i = 0; i < 13; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14(int& val) {
    const int x = 14;
    std::cout << 14;
    for (int i = 0; i < 14; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15(int& val) {
    const int x = 15;
    std::cout << 15;
    for (int i = 0; i < 15; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16(int& val) {
    const int x = 16;
    std::cout << 16;
    for (int i = 0; i < 16; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17(int& val) {
    const int x = 17;
    std::cout << 17;
    for (int i = 0; i < 17; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18(int& val) {
    const int x = 18;
    std::cout << 18;
    for (int i = 0; i < 18; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19(int& val) {
    const int x = 19;
    std::cout << 19;
    for (int i = 0; i < 19; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20(int& val) {
    const int x = 20;
    std::cout << 20;
    for (int i = 0; i < 20; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21(int& val) {
    const int x = 21;
    std::cout << 21;
    for (int i = 0; i < 21; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22(int& val) {
    const int x = 22;
    std::cout << 22;
    for (int i = 0; i < 22; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23(int& val) {
    const int x = 23;
    std::cout << 23;
    for (int i = 0; i < 23; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24(int& val) {
    const int x = 24;
    std::cout << 24;
    for (int i = 0; i < 24; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25(int& val) {
    const int x = 25;
    std::cout << 25;
    for (int i = 0; i < 25; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26(int& val) {
    const int x = 26;
    std::cout << 26;
    for (int i = 0; i < 26; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27(int& val) {
    const int x = 27;
    std::cout << 27;
    for (int i = 0; i < 27; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28(int& val) {
    const int x = 28;
    std::cout << 28;
    for (int i = 0; i < 28; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29(int& val) {
    const int x = 29;
    std::cout << 29;
    for (int i = 0; i < 29; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30(int& val) {
    const int x = 30;
    std::cout << 30;
    for (int i = 0; i < 30; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31(int& val) {
    const int x = 31;
    std::cout << 31;
    for (int i = 0; i < 31; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32(int& val) {
    const int x = 32;
    std::cout << 32;
    for (int i = 0; i < 32; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33(int& val) {
    const int x = 33;
    std::cout << 33;
    for (int i = 0; i < 33; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34(int& val) {
    const int x = 34;
    std::cout << 34;
    for (int i = 0; i < 34; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35(int& val) {
    const int x = 35;
    std::cout << 35;
    for (int i = 0; i < 35; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36(int& val) {
    const int x = 36;
    std::cout << 36;
    for (int i = 0; i < 36; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37(int& val) {
    const int x = 37;
    std::cout << 37;
    for (int i = 0; i < 37; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38(int& val) {
    const int x = 38;
    std::cout << 38;
    for (int i = 0; i < 38; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39(int& val) {
    const int x = 39;
    std::cout << 39;
    for (int i = 0; i < 39; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_40(int& val) {
    const int x = 40;
    std::cout << 40;
    for (int i = 0; i < 40; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_41(int& val) {
    const int x = 41;
    std::cout << 41;
    for (int i = 0; i < 41; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_42(int& val) {
    const int x = 42;
    std::cout << 42;
    for (int i = 0; i < 42; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_43(int& val) {
    const int x = 43;
    std::cout << 43;
    for (int i = 0; i < 43; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_44(int& val) {
    const int x = 44;
    std::cout << 44;
    for (int i = 0; i < 44; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_45(int& val) {
    const int x = 45;
    std::cout << 45;
    for (int i = 0; i < 45; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_46(int& val) {
    const int x = 46;
    std::cout << 46;
    for (int i = 0; i < 46; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_47(int& val) {
    const int x = 47;
    std::cout << 47;
    for (int i = 0; i < 47; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_48(int& val) {
    const int x = 48;
    std::cout << 48;
    for (int i = 0; i < 48; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_49(int& val) {
    const int x = 49;
    std::cout << 49;
    for (int i = 0; i < 49; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_50(int& val) {
    const int x = 50;
    std::cout << 50;
    for (int i = 0; i < 50; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_51(int& val) {
    const int x = 51;
    std::cout << 51;
    for (int i = 0; i < 51; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_52(int& val) {
    const int x = 52;
    std::cout << 52;
    for (int i = 0; i < 52; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_53(int& val) {
    const int x = 53;
    std::cout << 53;
    for (int i = 0; i < 53; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_54(int& val) {
    const int x = 54;
    std::cout << 54;
    for (int i = 0; i < 54; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_55(int& val) {
    const int x = 55;
    std::cout << 55;
    for (int i = 0; i < 55; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_56(int& val) {
    const int x = 56;
    std::cout << 56;
    for (int i = 0; i < 56; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_57(int& val) {
    const int x = 57;
    std::cout << 57;
    for (int i = 0; i < 57; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_58(int& val) {
    const int x = 58;
    std::cout << 58;
    for (int i = 0; i < 58; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_59(int& val) {
    const int x = 59;
    std::cout << 59;
    for (int i = 0; i < 59; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_60(int& val) {
    const int x = 60;
    std::cout << 60;
    for (int i = 0; i < 60; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_61(int& val) {
    const int x = 61;
    std::cout << 61;
    for (int i = 0; i < 61; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_62(int& val) {
    const int x = 62;
    std::cout << 62;
    for (int i = 0; i < 62; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_63(int& val) {
    const int x = 63;
    std::cout << 63;
    for (int i = 0; i < 63; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_64(int& val) {
    const int x = 64;
    std::cout << 64;
    for (int i = 0; i < 64; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_65(int& val) {
    const int x = 65;
    std::cout << 65;
    for (int i = 0; i < 65; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_66(int& val) {
    const int x = 66;
    std::cout << 66;
    for (int i = 0; i < 66; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_67(int& val) {
    const int x = 67;
    std::cout << 67;
    for (int i = 0; i < 67; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_68(int& val) {
    const int x = 68;
    std::cout << 68;
    for (int i = 0; i < 68; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_69(int& val) {
    const int x = 69;
    std::cout << 69;
    for (int i = 0; i < 69; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_70(int& val) {
    const int x = 70;
    std::cout << 70;
    for (int i = 0; i < 70; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_71(int& val) {
    const int x = 71;
    std::cout << 71;
    for (int i = 0; i < 71; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_72(int& val) {
    const int x = 72;
    std::cout << 72;
    for (int i = 0; i < 72; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_73(int& val) {
    const int x = 73;
    std::cout << 73;
    for (int i = 0; i < 73; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_74(int& val) {
    const int x = 74;
    std::cout << 74;
    for (int i = 0; i < 74; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_75(int& val) {
    const int x = 75;
    std::cout << 75;
    for (int i = 0; i < 75; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_76(int& val) {
    const int x = 76;
    std::cout << 76;
    for (int i = 0; i < 76; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_77(int& val) {
    const int x = 77;
    std::cout << 77;
    for (int i = 0; i < 77; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_78(int& val) {
    const int x = 78;
    std::cout << 78;
    for (int i = 0; i < 78; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_79(int& val) {
    const int x = 79;
    std::cout << 79;
    for (int i = 0; i < 79; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_80(int& val) {
    const int x = 80;
    std::cout << 80;
    for (int i = 0; i < 80; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_81(int& val) {
    const int x = 81;
    std::cout << 81;
    for (int i = 0; i < 81; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_82(int& val) {
    const int x = 82;
    std::cout << 82;
    for (int i = 0; i < 82; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_83(int& val) {
    const int x = 83;
    std::cout << 83;
    for (int i = 0; i < 83; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_84(int& val) {
    const int x = 84;
    std::cout << 84;
    for (int i = 0; i < 84; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_85(int& val) {
    const int x = 85;
    std::cout << 85;
    for (int i = 0; i < 85; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_86(int& val) {
    const int x = 86;
    std::cout << 86;
    for (int i = 0; i < 86; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_87(int& val) {
    const int x = 87;
    std::cout << 87;
    for (int i = 0; i < 87; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_88(int& val) {
    const int x = 88;
    std::cout << 88;
    for (int i = 0; i < 88; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_89(int& val) {
    const int x = 89;
    std::cout << 89;
    for (int i = 0; i < 89; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_90(int& val) {
    const int x = 90;
    std::cout << 90;
    for (int i = 0; i < 90; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_91(int& val) {
    const int x = 91;
    std::cout << 91;
    for (int i = 0; i < 91; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_92(int& val) {
    const int x = 92;
    std::cout << 92;
    for (int i = 0; i < 92; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_93(int& val) {
    const int x = 93;
    std::cout << 93;
    for (int i = 0; i < 93; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_94(int& val) {
    const int x = 94;
    std::cout << 94;
    for (int i = 0; i < 94; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_95(int& val) {
    const int x = 95;
    std::cout << 95;
    for (int i = 0; i < 95; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_96(int& val) {
    const int x = 96;
    std::cout << 96;
    for (int i = 0; i < 96; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_97(int& val) {
    const int x = 97;
    std::cout << 97;
    for (int i = 0; i < 97; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_98(int& val) {
    const int x = 98;
    std::cout << 98;
    for (int i = 0; i < 98; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_99(int& val) {
    const int x = 99;
    std::cout << 99;
    for (int i = 0; i < 99; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_100(int& val) {
    const int x = 100;
    std::cout << 100;
    for (int i = 0; i < 100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_101(int& val) {
    const int x = 101;
    std::cout << 101;
    for (int i = 0; i < 101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_102(int& val) {
    const int x = 102;
    std::cout << 102;
    for (int i = 0; i < 102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_103(int& val) {
    const int x = 103;
    std::cout << 103;
    for (int i = 0; i < 103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_104(int& val) {
    const int x = 104;
    std::cout << 104;
    for (int i = 0; i < 104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_105(int& val) {
    const int x = 105;
    std::cout << 105;
    for (int i = 0; i < 105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_106(int& val) {
    const int x = 106;
    std::cout << 106;
    for (int i = 0; i < 106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_107(int& val) {
    const int x = 107;
    std::cout << 107;
    for (int i = 0; i < 107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_108(int& val) {
    const int x = 108;
    std::cout << 108;
    for (int i = 0; i < 108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_109(int& val) {
    const int x = 109;
    std::cout << 109;
    for (int i = 0; i < 109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_110(int& val) {
    const int x = 110;
    std::cout << 110;
    for (int i = 0; i < 110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_111(int& val) {
    const int x = 111;
    std::cout << 111;
    for (int i = 0; i < 111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_112(int& val) {
    const int x = 112;
    std::cout << 112;
    for (int i = 0; i < 112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_113(int& val) {
    const int x = 113;
    std::cout << 113;
    for (int i = 0; i < 113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_114(int& val) {
    const int x = 114;
    std::cout << 114;
    for (int i = 0; i < 114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_115(int& val) {
    const int x = 115;
    std::cout << 115;
    for (int i = 0; i < 115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_116(int& val) {
    const int x = 116;
    std::cout << 116;
    for (int i = 0; i < 116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_117(int& val) {
    const int x = 117;
    std::cout << 117;
    for (int i = 0; i < 117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_118(int& val) {
    const int x = 118;
    std::cout << 118;
    for (int i = 0; i < 118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_119(int& val) {
    const int x = 119;
    std::cout << 119;
    for (int i = 0; i < 119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_120(int& val) {
    const int x = 120;
    std::cout << 120;
    for (int i = 0; i < 120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_121(int& val) {
    const int x = 121;
    std::cout << 121;
    for (int i = 0; i < 121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_122(int& val) {
    const int x = 122;
    std::cout << 122;
    for (int i = 0; i < 122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_123(int& val) {
    const int x = 123;
    std::cout << 123;
    for (int i = 0; i < 123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_124(int& val) {
    const int x = 124;
    std::cout << 124;
    for (int i = 0; i < 124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_125(int& val) {
    const int x = 125;
    std::cout << 125;
    for (int i = 0; i < 125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_126(int& val) {
    const int x = 126;
    std::cout << 126;
    for (int i = 0; i < 126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_127(int& val) {
    const int x = 127;
    std::cout << 127;
    for (int i = 0; i < 127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_128(int& val) {
    const int x = 128;
    std::cout << 128;
    for (int i = 0; i < 128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_129(int& val) {
    const int x = 129;
    std::cout << 129;
    for (int i = 0; i < 129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_130(int& val) {
    const int x = 130;
    std::cout << 130;
    for (int i = 0; i < 130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_131(int& val) {
    const int x = 131;
    std::cout << 131;
    for (int i = 0; i < 131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_132(int& val) {
    const int x = 132;
    std::cout << 132;
    for (int i = 0; i < 132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_133(int& val) {
    const int x = 133;
    std::cout << 133;
    for (int i = 0; i < 133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_134(int& val) {
    const int x = 134;
    std::cout << 134;
    for (int i = 0; i < 134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_135(int& val) {
    const int x = 135;
    std::cout << 135;
    for (int i = 0; i < 135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_136(int& val) {
    const int x = 136;
    std::cout << 136;
    for (int i = 0; i < 136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_137(int& val) {
    const int x = 137;
    std::cout << 137;
    for (int i = 0; i < 137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_138(int& val) {
    const int x = 138;
    std::cout << 138;
    for (int i = 0; i < 138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_139(int& val) {
    const int x = 139;
    std::cout << 139;
    for (int i = 0; i < 139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_140(int& val) {
    const int x = 140;
    std::cout << 140;
    for (int i = 0; i < 140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_141(int& val) {
    const int x = 141;
    std::cout << 141;
    for (int i = 0; i < 141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_142(int& val) {
    const int x = 142;
    std::cout << 142;
    for (int i = 0; i < 142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_143(int& val) {
    const int x = 143;
    std::cout << 143;
    for (int i = 0; i < 143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_144(int& val) {
    const int x = 144;
    std::cout << 144;
    for (int i = 0; i < 144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_145(int& val) {
    const int x = 145;
    std::cout << 145;
    for (int i = 0; i < 145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_146(int& val) {
    const int x = 146;
    std::cout << 146;
    for (int i = 0; i < 146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_147(int& val) {
    const int x = 147;
    std::cout << 147;
    for (int i = 0; i < 147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_148(int& val) {
    const int x = 148;
    std::cout << 148;
    for (int i = 0; i < 148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_149(int& val) {
    const int x = 149;
    std::cout << 149;
    for (int i = 0; i < 149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_150(int& val) {
    const int x = 150;
    std::cout << 150;
    for (int i = 0; i < 150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_151(int& val) {
    const int x = 151;
    std::cout << 151;
    for (int i = 0; i < 151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_152(int& val) {
    const int x = 152;
    std::cout << 152;
    for (int i = 0; i < 152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_153(int& val) {
    const int x = 153;
    std::cout << 153;
    for (int i = 0; i < 153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_154(int& val) {
    const int x = 154;
    std::cout << 154;
    for (int i = 0; i < 154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_155(int& val) {
    const int x = 155;
    std::cout << 155;
    for (int i = 0; i < 155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_156(int& val) {
    const int x = 156;
    std::cout << 156;
    for (int i = 0; i < 156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_157(int& val) {
    const int x = 157;
    std::cout << 157;
    for (int i = 0; i < 157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_158(int& val) {
    const int x = 158;
    std::cout << 158;
    for (int i = 0; i < 158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_159(int& val) {
    const int x = 159;
    std::cout << 159;
    for (int i = 0; i < 159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_160(int& val) {
    const int x = 160;
    std::cout << 160;
    for (int i = 0; i < 160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_161(int& val) {
    const int x = 161;
    std::cout << 161;
    for (int i = 0; i < 161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_162(int& val) {
    const int x = 162;
    std::cout << 162;
    for (int i = 0; i < 162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_163(int& val) {
    const int x = 163;
    std::cout << 163;
    for (int i = 0; i < 163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_164(int& val) {
    const int x = 164;
    std::cout << 164;
    for (int i = 0; i < 164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_165(int& val) {
    const int x = 165;
    std::cout << 165;
    for (int i = 0; i < 165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_166(int& val) {
    const int x = 166;
    std::cout << 166;
    for (int i = 0; i < 166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_167(int& val) {
    const int x = 167;
    std::cout << 167;
    for (int i = 0; i < 167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_168(int& val) {
    const int x = 168;
    std::cout << 168;
    for (int i = 0; i < 168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_169(int& val) {
    const int x = 169;
    std::cout << 169;
    for (int i = 0; i < 169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_170(int& val) {
    const int x = 170;
    std::cout << 170;
    for (int i = 0; i < 170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_171(int& val) {
    const int x = 171;
    std::cout << 171;
    for (int i = 0; i < 171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_172(int& val) {
    const int x = 172;
    std::cout << 172;
    for (int i = 0; i < 172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_173(int& val) {
    const int x = 173;
    std::cout << 173;
    for (int i = 0; i < 173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_174(int& val) {
    const int x = 174;
    std::cout << 174;
    for (int i = 0; i < 174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_175(int& val) {
    const int x = 175;
    std::cout << 175;
    for (int i = 0; i < 175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_176(int& val) {
    const int x = 176;
    std::cout << 176;
    for (int i = 0; i < 176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_177(int& val) {
    const int x = 177;
    std::cout << 177;
    for (int i = 0; i < 177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_178(int& val) {
    const int x = 178;
    std::cout << 178;
    for (int i = 0; i < 178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_179(int& val) {
    const int x = 179;
    std::cout << 179;
    for (int i = 0; i < 179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_180(int& val) {
    const int x = 180;
    std::cout << 180;
    for (int i = 0; i < 180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_181(int& val) {
    const int x = 181;
    std::cout << 181;
    for (int i = 0; i < 181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_182(int& val) {
    const int x = 182;
    std::cout << 182;
    for (int i = 0; i < 182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_183(int& val) {
    const int x = 183;
    std::cout << 183;
    for (int i = 0; i < 183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_184(int& val) {
    const int x = 184;
    std::cout << 184;
    for (int i = 0; i < 184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_185(int& val) {
    const int x = 185;
    std::cout << 185;
    for (int i = 0; i < 185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_186(int& val) {
    const int x = 186;
    std::cout << 186;
    for (int i = 0; i < 186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_187(int& val) {
    const int x = 187;
    std::cout << 187;
    for (int i = 0; i < 187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_188(int& val) {
    const int x = 188;
    std::cout << 188;
    for (int i = 0; i < 188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_189(int& val) {
    const int x = 189;
    std::cout << 189;
    for (int i = 0; i < 189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_190(int& val) {
    const int x = 190;
    std::cout << 190;
    for (int i = 0; i < 190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_191(int& val) {
    const int x = 191;
    std::cout << 191;
    for (int i = 0; i < 191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_192(int& val) {
    const int x = 192;
    std::cout << 192;
    for (int i = 0; i < 192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_193(int& val) {
    const int x = 193;
    std::cout << 193;
    for (int i = 0; i < 193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_194(int& val) {
    const int x = 194;
    std::cout << 194;
    for (int i = 0; i < 194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_195(int& val) {
    const int x = 195;
    std::cout << 195;
    for (int i = 0; i < 195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_196(int& val) {
    const int x = 196;
    std::cout << 196;
    for (int i = 0; i < 196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_197(int& val) {
    const int x = 197;
    std::cout << 197;
    for (int i = 0; i < 197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_198(int& val) {
    const int x = 198;
    std::cout << 198;
    for (int i = 0; i < 198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_199(int& val) {
    const int x = 199;
    std::cout << 199;
    for (int i = 0; i < 199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_200(int& val) {
    const int x = 200;
    std::cout << 200;
    for (int i = 0; i < 200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_201(int& val) {
    const int x = 201;
    std::cout << 201;
    for (int i = 0; i < 201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_202(int& val) {
    const int x = 202;
    std::cout << 202;
    for (int i = 0; i < 202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_203(int& val) {
    const int x = 203;
    std::cout << 203;
    for (int i = 0; i < 203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_204(int& val) {
    const int x = 204;
    std::cout << 204;
    for (int i = 0; i < 204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_205(int& val) {
    const int x = 205;
    std::cout << 205;
    for (int i = 0; i < 205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_206(int& val) {
    const int x = 206;
    std::cout << 206;
    for (int i = 0; i < 206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_207(int& val) {
    const int x = 207;
    std::cout << 207;
    for (int i = 0; i < 207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_208(int& val) {
    const int x = 208;
    std::cout << 208;
    for (int i = 0; i < 208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_209(int& val) {
    const int x = 209;
    std::cout << 209;
    for (int i = 0; i < 209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_210(int& val) {
    const int x = 210;
    std::cout << 210;
    for (int i = 0; i < 210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_211(int& val) {
    const int x = 211;
    std::cout << 211;
    for (int i = 0; i < 211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_212(int& val) {
    const int x = 212;
    std::cout << 212;
    for (int i = 0; i < 212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_213(int& val) {
    const int x = 213;
    std::cout << 213;
    for (int i = 0; i < 213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_214(int& val) {
    const int x = 214;
    std::cout << 214;
    for (int i = 0; i < 214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_215(int& val) {
    const int x = 215;
    std::cout << 215;
    for (int i = 0; i < 215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_216(int& val) {
    const int x = 216;
    std::cout << 216;
    for (int i = 0; i < 216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_217(int& val) {
    const int x = 217;
    std::cout << 217;
    for (int i = 0; i < 217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_218(int& val) {
    const int x = 218;
    std::cout << 218;
    for (int i = 0; i < 218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_219(int& val) {
    const int x = 219;
    std::cout << 219;
    for (int i = 0; i < 219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_220(int& val) {
    const int x = 220;
    std::cout << 220;
    for (int i = 0; i < 220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_221(int& val) {
    const int x = 221;
    std::cout << 221;
    for (int i = 0; i < 221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_222(int& val) {
    const int x = 222;
    std::cout << 222;
    for (int i = 0; i < 222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_223(int& val) {
    const int x = 223;
    std::cout << 223;
    for (int i = 0; i < 223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_224(int& val) {
    const int x = 224;
    std::cout << 224;
    for (int i = 0; i < 224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_225(int& val) {
    const int x = 225;
    std::cout << 225;
    for (int i = 0; i < 225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_226(int& val) {
    const int x = 226;
    std::cout << 226;
    for (int i = 0; i < 226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_227(int& val) {
    const int x = 227;
    std::cout << 227;
    for (int i = 0; i < 227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_228(int& val) {
    const int x = 228;
    std::cout << 228;
    for (int i = 0; i < 228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_229(int& val) {
    const int x = 229;
    std::cout << 229;
    for (int i = 0; i < 229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_230(int& val) {
    const int x = 230;
    std::cout << 230;
    for (int i = 0; i < 230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_231(int& val) {
    const int x = 231;
    std::cout << 231;
    for (int i = 0; i < 231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_232(int& val) {
    const int x = 232;
    std::cout << 232;
    for (int i = 0; i < 232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_233(int& val) {
    const int x = 233;
    std::cout << 233;
    for (int i = 0; i < 233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_234(int& val) {
    const int x = 234;
    std::cout << 234;
    for (int i = 0; i < 234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_235(int& val) {
    const int x = 235;
    std::cout << 235;
    for (int i = 0; i < 235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_236(int& val) {
    const int x = 236;
    std::cout << 236;
    for (int i = 0; i < 236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_237(int& val) {
    const int x = 237;
    std::cout << 237;
    for (int i = 0; i < 237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_238(int& val) {
    const int x = 238;
    std::cout << 238;
    for (int i = 0; i < 238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_239(int& val) {
    const int x = 239;
    std::cout << 239;
    for (int i = 0; i < 239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_240(int& val) {
    const int x = 240;
    std::cout << 240;
    for (int i = 0; i < 240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_241(int& val) {
    const int x = 241;
    std::cout << 241;
    for (int i = 0; i < 241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_242(int& val) {
    const int x = 242;
    std::cout << 242;
    for (int i = 0; i < 242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_243(int& val) {
    const int x = 243;
    std::cout << 243;
    for (int i = 0; i < 243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_244(int& val) {
    const int x = 244;
    std::cout << 244;
    for (int i = 0; i < 244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_245(int& val) {
    const int x = 245;
    std::cout << 245;
    for (int i = 0; i < 245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_246(int& val) {
    const int x = 246;
    std::cout << 246;
    for (int i = 0; i < 246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_247(int& val) {
    const int x = 247;
    std::cout << 247;
    for (int i = 0; i < 247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_248(int& val) {
    const int x = 248;
    std::cout << 248;
    for (int i = 0; i < 248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_249(int& val) {
    const int x = 249;
    std::cout << 249;
    for (int i = 0; i < 249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_250(int& val) {
    const int x = 250;
    std::cout << 250;
    for (int i = 0; i < 250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_251(int& val) {
    const int x = 251;
    std::cout << 251;
    for (int i = 0; i < 251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_252(int& val) {
    const int x = 252;
    std::cout << 252;
    for (int i = 0; i < 252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_253(int& val) {
    const int x = 253;
    std::cout << 253;
    for (int i = 0; i < 253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_254(int& val) {
    const int x = 254;
    std::cout << 254;
    for (int i = 0; i < 254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_255(int& val) {
    const int x = 255;
    std::cout << 255;
    for (int i = 0; i < 255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_256(int& val) {
    const int x = 256;
    std::cout << 256;
    for (int i = 0; i < 256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_257(int& val) {
    const int x = 257;
    std::cout << 257;
    for (int i = 0; i < 257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_258(int& val) {
    const int x = 258;
    std::cout << 258;
    for (int i = 0; i < 258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_259(int& val) {
    const int x = 259;
    std::cout << 259;
    for (int i = 0; i < 259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_260(int& val) {
    const int x = 260;
    std::cout << 260;
    for (int i = 0; i < 260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_261(int& val) {
    const int x = 261;
    std::cout << 261;
    for (int i = 0; i < 261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_262(int& val) {
    const int x = 262;
    std::cout << 262;
    for (int i = 0; i < 262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_263(int& val) {
    const int x = 263;
    std::cout << 263;
    for (int i = 0; i < 263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_264(int& val) {
    const int x = 264;
    std::cout << 264;
    for (int i = 0; i < 264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_265(int& val) {
    const int x = 265;
    std::cout << 265;
    for (int i = 0; i < 265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_266(int& val) {
    const int x = 266;
    std::cout << 266;
    for (int i = 0; i < 266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_267(int& val) {
    const int x = 267;
    std::cout << 267;
    for (int i = 0; i < 267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_268(int& val) {
    const int x = 268;
    std::cout << 268;
    for (int i = 0; i < 268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_269(int& val) {
    const int x = 269;
    std::cout << 269;
    for (int i = 0; i < 269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_270(int& val) {
    const int x = 270;
    std::cout << 270;
    for (int i = 0; i < 270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_271(int& val) {
    const int x = 271;
    std::cout << 271;
    for (int i = 0; i < 271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_272(int& val) {
    const int x = 272;
    std::cout << 272;
    for (int i = 0; i < 272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_273(int& val) {
    const int x = 273;
    std::cout << 273;
    for (int i = 0; i < 273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_274(int& val) {
    const int x = 274;
    std::cout << 274;
    for (int i = 0; i < 274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_275(int& val) {
    const int x = 275;
    std::cout << 275;
    for (int i = 0; i < 275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_276(int& val) {
    const int x = 276;
    std::cout << 276;
    for (int i = 0; i < 276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_277(int& val) {
    const int x = 277;
    std::cout << 277;
    for (int i = 0; i < 277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_278(int& val) {
    const int x = 278;
    std::cout << 278;
    for (int i = 0; i < 278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_279(int& val) {
    const int x = 279;
    std::cout << 279;
    for (int i = 0; i < 279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_280(int& val) {
    const int x = 280;
    std::cout << 280;
    for (int i = 0; i < 280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_281(int& val) {
    const int x = 281;
    std::cout << 281;
    for (int i = 0; i < 281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_282(int& val) {
    const int x = 282;
    std::cout << 282;
    for (int i = 0; i < 282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_283(int& val) {
    const int x = 283;
    std::cout << 283;
    for (int i = 0; i < 283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_284(int& val) {
    const int x = 284;
    std::cout << 284;
    for (int i = 0; i < 284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_285(int& val) {
    const int x = 285;
    std::cout << 285;
    for (int i = 0; i < 285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_286(int& val) {
    const int x = 286;
    std::cout << 286;
    for (int i = 0; i < 286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_287(int& val) {
    const int x = 287;
    std::cout << 287;
    for (int i = 0; i < 287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_288(int& val) {
    const int x = 288;
    std::cout << 288;
    for (int i = 0; i < 288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_289(int& val) {
    const int x = 289;
    std::cout << 289;
    for (int i = 0; i < 289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_290(int& val) {
    const int x = 290;
    std::cout << 290;
    for (int i = 0; i < 290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_291(int& val) {
    const int x = 291;
    std::cout << 291;
    for (int i = 0; i < 291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_292(int& val) {
    const int x = 292;
    std::cout << 292;
    for (int i = 0; i < 292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_293(int& val) {
    const int x = 293;
    std::cout << 293;
    for (int i = 0; i < 293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_294(int& val) {
    const int x = 294;
    std::cout << 294;
    for (int i = 0; i < 294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_295(int& val) {
    const int x = 295;
    std::cout << 295;
    for (int i = 0; i < 295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_296(int& val) {
    const int x = 296;
    std::cout << 296;
    for (int i = 0; i < 296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_297(int& val) {
    const int x = 297;
    std::cout << 297;
    for (int i = 0; i < 297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_298(int& val) {
    const int x = 298;
    std::cout << 298;
    for (int i = 0; i < 298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_299(int& val) {
    const int x = 299;
    std::cout << 299;
    for (int i = 0; i < 299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_300(int& val) {
    const int x = 300;
    std::cout << 300;
    for (int i = 0; i < 300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_301(int& val) {
    const int x = 301;
    std::cout << 301;
    for (int i = 0; i < 301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_302(int& val) {
    const int x = 302;
    std::cout << 302;
    for (int i = 0; i < 302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_303(int& val) {
    const int x = 303;
    std::cout << 303;
    for (int i = 0; i < 303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_304(int& val) {
    const int x = 304;
    std::cout << 304;
    for (int i = 0; i < 304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_305(int& val) {
    const int x = 305;
    std::cout << 305;
    for (int i = 0; i < 305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_306(int& val) {
    const int x = 306;
    std::cout << 306;
    for (int i = 0; i < 306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_307(int& val) {
    const int x = 307;
    std::cout << 307;
    for (int i = 0; i < 307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_308(int& val) {
    const int x = 308;
    std::cout << 308;
    for (int i = 0; i < 308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_309(int& val) {
    const int x = 309;
    std::cout << 309;
    for (int i = 0; i < 309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_310(int& val) {
    const int x = 310;
    std::cout << 310;
    for (int i = 0; i < 310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_311(int& val) {
    const int x = 311;
    std::cout << 311;
    for (int i = 0; i < 311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_312(int& val) {
    const int x = 312;
    std::cout << 312;
    for (int i = 0; i < 312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_313(int& val) {
    const int x = 313;
    std::cout << 313;
    for (int i = 0; i < 313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_314(int& val) {
    const int x = 314;
    std::cout << 314;
    for (int i = 0; i < 314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_315(int& val) {
    const int x = 315;
    std::cout << 315;
    for (int i = 0; i < 315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_316(int& val) {
    const int x = 316;
    std::cout << 316;
    for (int i = 0; i < 316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_317(int& val) {
    const int x = 317;
    std::cout << 317;
    for (int i = 0; i < 317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_318(int& val) {
    const int x = 318;
    std::cout << 318;
    for (int i = 0; i < 318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_319(int& val) {
    const int x = 319;
    std::cout << 319;
    for (int i = 0; i < 319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_320(int& val) {
    const int x = 320;
    std::cout << 320;
    for (int i = 0; i < 320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_321(int& val) {
    const int x = 321;
    std::cout << 321;
    for (int i = 0; i < 321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_322(int& val) {
    const int x = 322;
    std::cout << 322;
    for (int i = 0; i < 322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_323(int& val) {
    const int x = 323;
    std::cout << 323;
    for (int i = 0; i < 323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_324(int& val) {
    const int x = 324;
    std::cout << 324;
    for (int i = 0; i < 324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_325(int& val) {
    const int x = 325;
    std::cout << 325;
    for (int i = 0; i < 325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_326(int& val) {
    const int x = 326;
    std::cout << 326;
    for (int i = 0; i < 326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_327(int& val) {
    const int x = 327;
    std::cout << 327;
    for (int i = 0; i < 327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_328(int& val) {
    const int x = 328;
    std::cout << 328;
    for (int i = 0; i < 328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_329(int& val) {
    const int x = 329;
    std::cout << 329;
    for (int i = 0; i < 329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_330(int& val) {
    const int x = 330;
    std::cout << 330;
    for (int i = 0; i < 330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_331(int& val) {
    const int x = 331;
    std::cout << 331;
    for (int i = 0; i < 331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_332(int& val) {
    const int x = 332;
    std::cout << 332;
    for (int i = 0; i < 332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_333(int& val) {
    const int x = 333;
    std::cout << 333;
    for (int i = 0; i < 333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_334(int& val) {
    const int x = 334;
    std::cout << 334;
    for (int i = 0; i < 334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_335(int& val) {
    const int x = 335;
    std::cout << 335;
    for (int i = 0; i < 335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_336(int& val) {
    const int x = 336;
    std::cout << 336;
    for (int i = 0; i < 336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_337(int& val) {
    const int x = 337;
    std::cout << 337;
    for (int i = 0; i < 337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_338(int& val) {
    const int x = 338;
    std::cout << 338;
    for (int i = 0; i < 338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_339(int& val) {
    const int x = 339;
    std::cout << 339;
    for (int i = 0; i < 339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_340(int& val) {
    const int x = 340;
    std::cout << 340;
    for (int i = 0; i < 340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_341(int& val) {
    const int x = 341;
    std::cout << 341;
    for (int i = 0; i < 341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_342(int& val) {
    const int x = 342;
    std::cout << 342;
    for (int i = 0; i < 342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_343(int& val) {
    const int x = 343;
    std::cout << 343;
    for (int i = 0; i < 343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_344(int& val) {
    const int x = 344;
    std::cout << 344;
    for (int i = 0; i < 344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_345(int& val) {
    const int x = 345;
    std::cout << 345;
    for (int i = 0; i < 345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_346(int& val) {
    const int x = 346;
    std::cout << 346;
    for (int i = 0; i < 346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_347(int& val) {
    const int x = 347;
    std::cout << 347;
    for (int i = 0; i < 347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_348(int& val) {
    const int x = 348;
    std::cout << 348;
    for (int i = 0; i < 348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_349(int& val) {
    const int x = 349;
    std::cout << 349;
    for (int i = 0; i < 349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_350(int& val) {
    const int x = 350;
    std::cout << 350;
    for (int i = 0; i < 350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_351(int& val) {
    const int x = 351;
    std::cout << 351;
    for (int i = 0; i < 351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_352(int& val) {
    const int x = 352;
    std::cout << 352;
    for (int i = 0; i < 352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_353(int& val) {
    const int x = 353;
    std::cout << 353;
    for (int i = 0; i < 353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_354(int& val) {
    const int x = 354;
    std::cout << 354;
    for (int i = 0; i < 354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_355(int& val) {
    const int x = 355;
    std::cout << 355;
    for (int i = 0; i < 355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_356(int& val) {
    const int x = 356;
    std::cout << 356;
    for (int i = 0; i < 356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_357(int& val) {
    const int x = 357;
    std::cout << 357;
    for (int i = 0; i < 357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_358(int& val) {
    const int x = 358;
    std::cout << 358;
    for (int i = 0; i < 358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_359(int& val) {
    const int x = 359;
    std::cout << 359;
    for (int i = 0; i < 359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_360(int& val) {
    const int x = 360;
    std::cout << 360;
    for (int i = 0; i < 360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_361(int& val) {
    const int x = 361;
    std::cout << 361;
    for (int i = 0; i < 361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_362(int& val) {
    const int x = 362;
    std::cout << 362;
    for (int i = 0; i < 362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_363(int& val) {
    const int x = 363;
    std::cout << 363;
    for (int i = 0; i < 363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_364(int& val) {
    const int x = 364;
    std::cout << 364;
    for (int i = 0; i < 364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_365(int& val) {
    const int x = 365;
    std::cout << 365;
    for (int i = 0; i < 365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_366(int& val) {
    const int x = 366;
    std::cout << 366;
    for (int i = 0; i < 366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_367(int& val) {
    const int x = 367;
    std::cout << 367;
    for (int i = 0; i < 367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_368(int& val) {
    const int x = 368;
    std::cout << 368;
    for (int i = 0; i < 368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_369(int& val) {
    const int x = 369;
    std::cout << 369;
    for (int i = 0; i < 369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_370(int& val) {
    const int x = 370;
    std::cout << 370;
    for (int i = 0; i < 370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_371(int& val) {
    const int x = 371;
    std::cout << 371;
    for (int i = 0; i < 371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_372(int& val) {
    const int x = 372;
    std::cout << 372;
    for (int i = 0; i < 372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_373(int& val) {
    const int x = 373;
    std::cout << 373;
    for (int i = 0; i < 373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_374(int& val) {
    const int x = 374;
    std::cout << 374;
    for (int i = 0; i < 374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_375(int& val) {
    const int x = 375;
    std::cout << 375;
    for (int i = 0; i < 375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_376(int& val) {
    const int x = 376;
    std::cout << 376;
    for (int i = 0; i < 376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_377(int& val) {
    const int x = 377;
    std::cout << 377;
    for (int i = 0; i < 377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_378(int& val) {
    const int x = 378;
    std::cout << 378;
    for (int i = 0; i < 378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_379(int& val) {
    const int x = 379;
    std::cout << 379;
    for (int i = 0; i < 379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_380(int& val) {
    const int x = 380;
    std::cout << 380;
    for (int i = 0; i < 380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_381(int& val) {
    const int x = 381;
    std::cout << 381;
    for (int i = 0; i < 381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_382(int& val) {
    const int x = 382;
    std::cout << 382;
    for (int i = 0; i < 382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_383(int& val) {
    const int x = 383;
    std::cout << 383;
    for (int i = 0; i < 383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_384(int& val) {
    const int x = 384;
    std::cout << 384;
    for (int i = 0; i < 384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_385(int& val) {
    const int x = 385;
    std::cout << 385;
    for (int i = 0; i < 385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_386(int& val) {
    const int x = 386;
    std::cout << 386;
    for (int i = 0; i < 386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_387(int& val) {
    const int x = 387;
    std::cout << 387;
    for (int i = 0; i < 387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_388(int& val) {
    const int x = 388;
    std::cout << 388;
    for (int i = 0; i < 388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_389(int& val) {
    const int x = 389;
    std::cout << 389;
    for (int i = 0; i < 389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_390(int& val) {
    const int x = 390;
    std::cout << 390;
    for (int i = 0; i < 390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_391(int& val) {
    const int x = 391;
    std::cout << 391;
    for (int i = 0; i < 391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_392(int& val) {
    const int x = 392;
    std::cout << 392;
    for (int i = 0; i < 392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_393(int& val) {
    const int x = 393;
    std::cout << 393;
    for (int i = 0; i < 393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_394(int& val) {
    const int x = 394;
    std::cout << 394;
    for (int i = 0; i < 394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_395(int& val) {
    const int x = 395;
    std::cout << 395;
    for (int i = 0; i < 395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_396(int& val) {
    const int x = 396;
    std::cout << 396;
    for (int i = 0; i < 396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_397(int& val) {
    const int x = 397;
    std::cout << 397;
    for (int i = 0; i < 397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_398(int& val) {
    const int x = 398;
    std::cout << 398;
    for (int i = 0; i < 398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_399(int& val) {
    const int x = 399;
    std::cout << 399;
    for (int i = 0; i < 399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_400(int& val) {
    const int x = 400;
    std::cout << 400;
    for (int i = 0; i < 400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_401(int& val) {
    const int x = 401;
    std::cout << 401;
    for (int i = 0; i < 401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_402(int& val) {
    const int x = 402;
    std::cout << 402;
    for (int i = 0; i < 402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_403(int& val) {
    const int x = 403;
    std::cout << 403;
    for (int i = 0; i < 403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_404(int& val) {
    const int x = 404;
    std::cout << 404;
    for (int i = 0; i < 404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_405(int& val) {
    const int x = 405;
    std::cout << 405;
    for (int i = 0; i < 405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_406(int& val) {
    const int x = 406;
    std::cout << 406;
    for (int i = 0; i < 406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_407(int& val) {
    const int x = 407;
    std::cout << 407;
    for (int i = 0; i < 407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_408(int& val) {
    const int x = 408;
    std::cout << 408;
    for (int i = 0; i < 408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_409(int& val) {
    const int x = 409;
    std::cout << 409;
    for (int i = 0; i < 409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_410(int& val) {
    const int x = 410;
    std::cout << 410;
    for (int i = 0; i < 410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_411(int& val) {
    const int x = 411;
    std::cout << 411;
    for (int i = 0; i < 411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_412(int& val) {
    const int x = 412;
    std::cout << 412;
    for (int i = 0; i < 412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_413(int& val) {
    const int x = 413;
    std::cout << 413;
    for (int i = 0; i < 413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_414(int& val) {
    const int x = 414;
    std::cout << 414;
    for (int i = 0; i < 414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_415(int& val) {
    const int x = 415;
    std::cout << 415;
    for (int i = 0; i < 415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_416(int& val) {
    const int x = 416;
    std::cout << 416;
    for (int i = 0; i < 416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_417(int& val) {
    const int x = 417;
    std::cout << 417;
    for (int i = 0; i < 417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_418(int& val) {
    const int x = 418;
    std::cout << 418;
    for (int i = 0; i < 418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_419(int& val) {
    const int x = 419;
    std::cout << 419;
    for (int i = 0; i < 419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_420(int& val) {
    const int x = 420;
    std::cout << 420;
    for (int i = 0; i < 420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_421(int& val) {
    const int x = 421;
    std::cout << 421;
    for (int i = 0; i < 421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_422(int& val) {
    const int x = 422;
    std::cout << 422;
    for (int i = 0; i < 422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_423(int& val) {
    const int x = 423;
    std::cout << 423;
    for (int i = 0; i < 423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_424(int& val) {
    const int x = 424;
    std::cout << 424;
    for (int i = 0; i < 424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_425(int& val) {
    const int x = 425;
    std::cout << 425;
    for (int i = 0; i < 425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_426(int& val) {
    const int x = 426;
    std::cout << 426;
    for (int i = 0; i < 426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_427(int& val) {
    const int x = 427;
    std::cout << 427;
    for (int i = 0; i < 427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_428(int& val) {
    const int x = 428;
    std::cout << 428;
    for (int i = 0; i < 428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_429(int& val) {
    const int x = 429;
    std::cout << 429;
    for (int i = 0; i < 429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_430(int& val) {
    const int x = 430;
    std::cout << 430;
    for (int i = 0; i < 430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_431(int& val) {
    const int x = 431;
    std::cout << 431;
    for (int i = 0; i < 431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_432(int& val) {
    const int x = 432;
    std::cout << 432;
    for (int i = 0; i < 432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_433(int& val) {
    const int x = 433;
    std::cout << 433;
    for (int i = 0; i < 433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_434(int& val) {
    const int x = 434;
    std::cout << 434;
    for (int i = 0; i < 434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_435(int& val) {
    const int x = 435;
    std::cout << 435;
    for (int i = 0; i < 435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_436(int& val) {
    const int x = 436;
    std::cout << 436;
    for (int i = 0; i < 436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_437(int& val) {
    const int x = 437;
    std::cout << 437;
    for (int i = 0; i < 437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_438(int& val) {
    const int x = 438;
    std::cout << 438;
    for (int i = 0; i < 438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_439(int& val) {
    const int x = 439;
    std::cout << 439;
    for (int i = 0; i < 439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_440(int& val) {
    const int x = 440;
    std::cout << 440;
    for (int i = 0; i < 440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_441(int& val) {
    const int x = 441;
    std::cout << 441;
    for (int i = 0; i < 441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_442(int& val) {
    const int x = 442;
    std::cout << 442;
    for (int i = 0; i < 442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_443(int& val) {
    const int x = 443;
    std::cout << 443;
    for (int i = 0; i < 443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_444(int& val) {
    const int x = 444;
    std::cout << 444;
    for (int i = 0; i < 444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_445(int& val) {
    const int x = 445;
    std::cout << 445;
    for (int i = 0; i < 445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_446(int& val) {
    const int x = 446;
    std::cout << 446;
    for (int i = 0; i < 446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_447(int& val) {
    const int x = 447;
    std::cout << 447;
    for (int i = 0; i < 447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_448(int& val) {
    const int x = 448;
    std::cout << 448;
    for (int i = 0; i < 448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_449(int& val) {
    const int x = 449;
    std::cout << 449;
    for (int i = 0; i < 449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_450(int& val) {
    const int x = 450;
    std::cout << 450;
    for (int i = 0; i < 450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_451(int& val) {
    const int x = 451;
    std::cout << 451;
    for (int i = 0; i < 451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_452(int& val) {
    const int x = 452;
    std::cout << 452;
    for (int i = 0; i < 452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_453(int& val) {
    const int x = 453;
    std::cout << 453;
    for (int i = 0; i < 453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_454(int& val) {
    const int x = 454;
    std::cout << 454;
    for (int i = 0; i < 454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_455(int& val) {
    const int x = 455;
    std::cout << 455;
    for (int i = 0; i < 455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_456(int& val) {
    const int x = 456;
    std::cout << 456;
    for (int i = 0; i < 456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_457(int& val) {
    const int x = 457;
    std::cout << 457;
    for (int i = 0; i < 457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_458(int& val) {
    const int x = 458;
    std::cout << 458;
    for (int i = 0; i < 458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_459(int& val) {
    const int x = 459;
    std::cout << 459;
    for (int i = 0; i < 459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_460(int& val) {
    const int x = 460;
    std::cout << 460;
    for (int i = 0; i < 460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_461(int& val) {
    const int x = 461;
    std::cout << 461;
    for (int i = 0; i < 461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_462(int& val) {
    const int x = 462;
    std::cout << 462;
    for (int i = 0; i < 462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_463(int& val) {
    const int x = 463;
    std::cout << 463;
    for (int i = 0; i < 463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_464(int& val) {
    const int x = 464;
    std::cout << 464;
    for (int i = 0; i < 464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_465(int& val) {
    const int x = 465;
    std::cout << 465;
    for (int i = 0; i < 465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_466(int& val) {
    const int x = 466;
    std::cout << 466;
    for (int i = 0; i < 466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_467(int& val) {
    const int x = 467;
    std::cout << 467;
    for (int i = 0; i < 467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_468(int& val) {
    const int x = 468;
    std::cout << 468;
    for (int i = 0; i < 468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_469(int& val) {
    const int x = 469;
    std::cout << 469;
    for (int i = 0; i < 469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_470(int& val) {
    const int x = 470;
    std::cout << 470;
    for (int i = 0; i < 470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_471(int& val) {
    const int x = 471;
    std::cout << 471;
    for (int i = 0; i < 471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_472(int& val) {
    const int x = 472;
    std::cout << 472;
    for (int i = 0; i < 472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_473(int& val) {
    const int x = 473;
    std::cout << 473;
    for (int i = 0; i < 473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_474(int& val) {
    const int x = 474;
    std::cout << 474;
    for (int i = 0; i < 474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_475(int& val) {
    const int x = 475;
    std::cout << 475;
    for (int i = 0; i < 475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_476(int& val) {
    const int x = 476;
    std::cout << 476;
    for (int i = 0; i < 476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_477(int& val) {
    const int x = 477;
    std::cout << 477;
    for (int i = 0; i < 477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_478(int& val) {
    const int x = 478;
    std::cout << 478;
    for (int i = 0; i < 478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_479(int& val) {
    const int x = 479;
    std::cout << 479;
    for (int i = 0; i < 479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_480(int& val) {
    const int x = 480;
    std::cout << 480;
    for (int i = 0; i < 480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_481(int& val) {
    const int x = 481;
    std::cout << 481;
    for (int i = 0; i < 481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_482(int& val) {
    const int x = 482;
    std::cout << 482;
    for (int i = 0; i < 482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_483(int& val) {
    const int x = 483;
    std::cout << 483;
    for (int i = 0; i < 483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_484(int& val) {
    const int x = 484;
    std::cout << 484;
    for (int i = 0; i < 484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_485(int& val) {
    const int x = 485;
    std::cout << 485;
    for (int i = 0; i < 485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_486(int& val) {
    const int x = 486;
    std::cout << 486;
    for (int i = 0; i < 486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_487(int& val) {
    const int x = 487;
    std::cout << 487;
    for (int i = 0; i < 487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_488(int& val) {
    const int x = 488;
    std::cout << 488;
    for (int i = 0; i < 488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_489(int& val) {
    const int x = 489;
    std::cout << 489;
    for (int i = 0; i < 489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_490(int& val) {
    const int x = 490;
    std::cout << 490;
    for (int i = 0; i < 490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_491(int& val) {
    const int x = 491;
    std::cout << 491;
    for (int i = 0; i < 491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_492(int& val) {
    const int x = 492;
    std::cout << 492;
    for (int i = 0; i < 492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_493(int& val) {
    const int x = 493;
    std::cout << 493;
    for (int i = 0; i < 493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_494(int& val) {
    const int x = 494;
    std::cout << 494;
    for (int i = 0; i < 494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_495(int& val) {
    const int x = 495;
    std::cout << 495;
    for (int i = 0; i < 495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_496(int& val) {
    const int x = 496;
    std::cout << 496;
    for (int i = 0; i < 496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_497(int& val) {
    const int x = 497;
    std::cout << 497;
    for (int i = 0; i < 497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_498(int& val) {
    const int x = 498;
    std::cout << 498;
    for (int i = 0; i < 498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_499(int& val) {
    const int x = 499;
    std::cout << 499;
    for (int i = 0; i < 499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_500(int& val) {
    const int x = 500;
    std::cout << 500;
    for (int i = 0; i < 500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_501(int& val) {
    const int x = 501;
    std::cout << 501;
    for (int i = 0; i < 501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_502(int& val) {
    const int x = 502;
    std::cout << 502;
    for (int i = 0; i < 502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_503(int& val) {
    const int x = 503;
    std::cout << 503;
    for (int i = 0; i < 503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_504(int& val) {
    const int x = 504;
    std::cout << 504;
    for (int i = 0; i < 504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_505(int& val) {
    const int x = 505;
    std::cout << 505;
    for (int i = 0; i < 505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_506(int& val) {
    const int x = 506;
    std::cout << 506;
    for (int i = 0; i < 506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_507(int& val) {
    const int x = 507;
    std::cout << 507;
    for (int i = 0; i < 507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_508(int& val) {
    const int x = 508;
    std::cout << 508;
    for (int i = 0; i < 508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_509(int& val) {
    const int x = 509;
    std::cout << 509;
    for (int i = 0; i < 509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_510(int& val) {
    const int x = 510;
    std::cout << 510;
    for (int i = 0; i < 510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_511(int& val) {
    const int x = 511;
    std::cout << 511;
    for (int i = 0; i < 511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_512(int& val) {
    const int x = 512;
    std::cout << 512;
    for (int i = 0; i < 512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_513(int& val) {
    const int x = 513;
    std::cout << 513;
    for (int i = 0; i < 513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_514(int& val) {
    const int x = 514;
    std::cout << 514;
    for (int i = 0; i < 514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_515(int& val) {
    const int x = 515;
    std::cout << 515;
    for (int i = 0; i < 515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_516(int& val) {
    const int x = 516;
    std::cout << 516;
    for (int i = 0; i < 516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_517(int& val) {
    const int x = 517;
    std::cout << 517;
    for (int i = 0; i < 517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_518(int& val) {
    const int x = 518;
    std::cout << 518;
    for (int i = 0; i < 518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_519(int& val) {
    const int x = 519;
    std::cout << 519;
    for (int i = 0; i < 519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_520(int& val) {
    const int x = 520;
    std::cout << 520;
    for (int i = 0; i < 520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_521(int& val) {
    const int x = 521;
    std::cout << 521;
    for (int i = 0; i < 521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_522(int& val) {
    const int x = 522;
    std::cout << 522;
    for (int i = 0; i < 522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_523(int& val) {
    const int x = 523;
    std::cout << 523;
    for (int i = 0; i < 523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_524(int& val) {
    const int x = 524;
    std::cout << 524;
    for (int i = 0; i < 524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_525(int& val) {
    const int x = 525;
    std::cout << 525;
    for (int i = 0; i < 525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_526(int& val) {
    const int x = 526;
    std::cout << 526;
    for (int i = 0; i < 526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_527(int& val) {
    const int x = 527;
    std::cout << 527;
    for (int i = 0; i < 527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_528(int& val) {
    const int x = 528;
    std::cout << 528;
    for (int i = 0; i < 528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_529(int& val) {
    const int x = 529;
    std::cout << 529;
    for (int i = 0; i < 529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_530(int& val) {
    const int x = 530;
    std::cout << 530;
    for (int i = 0; i < 530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_531(int& val) {
    const int x = 531;
    std::cout << 531;
    for (int i = 0; i < 531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_532(int& val) {
    const int x = 532;
    std::cout << 532;
    for (int i = 0; i < 532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_533(int& val) {
    const int x = 533;
    std::cout << 533;
    for (int i = 0; i < 533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_534(int& val) {
    const int x = 534;
    std::cout << 534;
    for (int i = 0; i < 534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_535(int& val) {
    const int x = 535;
    std::cout << 535;
    for (int i = 0; i < 535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_536(int& val) {
    const int x = 536;
    std::cout << 536;
    for (int i = 0; i < 536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_537(int& val) {
    const int x = 537;
    std::cout << 537;
    for (int i = 0; i < 537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_538(int& val) {
    const int x = 538;
    std::cout << 538;
    for (int i = 0; i < 538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_539(int& val) {
    const int x = 539;
    std::cout << 539;
    for (int i = 0; i < 539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_540(int& val) {
    const int x = 540;
    std::cout << 540;
    for (int i = 0; i < 540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_541(int& val) {
    const int x = 541;
    std::cout << 541;
    for (int i = 0; i < 541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_542(int& val) {
    const int x = 542;
    std::cout << 542;
    for (int i = 0; i < 542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_543(int& val) {
    const int x = 543;
    std::cout << 543;
    for (int i = 0; i < 543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_544(int& val) {
    const int x = 544;
    std::cout << 544;
    for (int i = 0; i < 544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_545(int& val) {
    const int x = 545;
    std::cout << 545;
    for (int i = 0; i < 545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_546(int& val) {
    const int x = 546;
    std::cout << 546;
    for (int i = 0; i < 546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_547(int& val) {
    const int x = 547;
    std::cout << 547;
    for (int i = 0; i < 547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_548(int& val) {
    const int x = 548;
    std::cout << 548;
    for (int i = 0; i < 548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_549(int& val) {
    const int x = 549;
    std::cout << 549;
    for (int i = 0; i < 549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_550(int& val) {
    const int x = 550;
    std::cout << 550;
    for (int i = 0; i < 550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_551(int& val) {
    const int x = 551;
    std::cout << 551;
    for (int i = 0; i < 551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_552(int& val) {
    const int x = 552;
    std::cout << 552;
    for (int i = 0; i < 552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_553(int& val) {
    const int x = 553;
    std::cout << 553;
    for (int i = 0; i < 553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_554(int& val) {
    const int x = 554;
    std::cout << 554;
    for (int i = 0; i < 554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_555(int& val) {
    const int x = 555;
    std::cout << 555;
    for (int i = 0; i < 555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_556(int& val) {
    const int x = 556;
    std::cout << 556;
    for (int i = 0; i < 556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_557(int& val) {
    const int x = 557;
    std::cout << 557;
    for (int i = 0; i < 557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_558(int& val) {
    const int x = 558;
    std::cout << 558;
    for (int i = 0; i < 558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_559(int& val) {
    const int x = 559;
    std::cout << 559;
    for (int i = 0; i < 559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_560(int& val) {
    const int x = 560;
    std::cout << 560;
    for (int i = 0; i < 560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_561(int& val) {
    const int x = 561;
    std::cout << 561;
    for (int i = 0; i < 561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_562(int& val) {
    const int x = 562;
    std::cout << 562;
    for (int i = 0; i < 562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_563(int& val) {
    const int x = 563;
    std::cout << 563;
    for (int i = 0; i < 563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_564(int& val) {
    const int x = 564;
    std::cout << 564;
    for (int i = 0; i < 564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_565(int& val) {
    const int x = 565;
    std::cout << 565;
    for (int i = 0; i < 565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_566(int& val) {
    const int x = 566;
    std::cout << 566;
    for (int i = 0; i < 566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_567(int& val) {
    const int x = 567;
    std::cout << 567;
    for (int i = 0; i < 567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_568(int& val) {
    const int x = 568;
    std::cout << 568;
    for (int i = 0; i < 568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_569(int& val) {
    const int x = 569;
    std::cout << 569;
    for (int i = 0; i < 569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_570(int& val) {
    const int x = 570;
    std::cout << 570;
    for (int i = 0; i < 570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_571(int& val) {
    const int x = 571;
    std::cout << 571;
    for (int i = 0; i < 571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_572(int& val) {
    const int x = 572;
    std::cout << 572;
    for (int i = 0; i < 572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_573(int& val) {
    const int x = 573;
    std::cout << 573;
    for (int i = 0; i < 573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_574(int& val) {
    const int x = 574;
    std::cout << 574;
    for (int i = 0; i < 574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_575(int& val) {
    const int x = 575;
    std::cout << 575;
    for (int i = 0; i < 575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_576(int& val) {
    const int x = 576;
    std::cout << 576;
    for (int i = 0; i < 576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_577(int& val) {
    const int x = 577;
    std::cout << 577;
    for (int i = 0; i < 577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_578(int& val) {
    const int x = 578;
    std::cout << 578;
    for (int i = 0; i < 578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_579(int& val) {
    const int x = 579;
    std::cout << 579;
    for (int i = 0; i < 579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_580(int& val) {
    const int x = 580;
    std::cout << 580;
    for (int i = 0; i < 580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_581(int& val) {
    const int x = 581;
    std::cout << 581;
    for (int i = 0; i < 581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_582(int& val) {
    const int x = 582;
    std::cout << 582;
    for (int i = 0; i < 582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_583(int& val) {
    const int x = 583;
    std::cout << 583;
    for (int i = 0; i < 583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_584(int& val) {
    const int x = 584;
    std::cout << 584;
    for (int i = 0; i < 584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_585(int& val) {
    const int x = 585;
    std::cout << 585;
    for (int i = 0; i < 585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_586(int& val) {
    const int x = 586;
    std::cout << 586;
    for (int i = 0; i < 586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_587(int& val) {
    const int x = 587;
    std::cout << 587;
    for (int i = 0; i < 587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_588(int& val) {
    const int x = 588;
    std::cout << 588;
    for (int i = 0; i < 588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_589(int& val) {
    const int x = 589;
    std::cout << 589;
    for (int i = 0; i < 589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_590(int& val) {
    const int x = 590;
    std::cout << 590;
    for (int i = 0; i < 590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_591(int& val) {
    const int x = 591;
    std::cout << 591;
    for (int i = 0; i < 591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_592(int& val) {
    const int x = 592;
    std::cout << 592;
    for (int i = 0; i < 592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_593(int& val) {
    const int x = 593;
    std::cout << 593;
    for (int i = 0; i < 593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_594(int& val) {
    const int x = 594;
    std::cout << 594;
    for (int i = 0; i < 594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_595(int& val) {
    const int x = 595;
    std::cout << 595;
    for (int i = 0; i < 595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_596(int& val) {
    const int x = 596;
    std::cout << 596;
    for (int i = 0; i < 596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_597(int& val) {
    const int x = 597;
    std::cout << 597;
    for (int i = 0; i < 597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_598(int& val) {
    const int x = 598;
    std::cout << 598;
    for (int i = 0; i < 598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_599(int& val) {
    const int x = 599;
    std::cout << 599;
    for (int i = 0; i < 599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_600(int& val) {
    const int x = 600;
    std::cout << 600;
    for (int i = 0; i < 600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_601(int& val) {
    const int x = 601;
    std::cout << 601;
    for (int i = 0; i < 601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_602(int& val) {
    const int x = 602;
    std::cout << 602;
    for (int i = 0; i < 602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_603(int& val) {
    const int x = 603;
    std::cout << 603;
    for (int i = 0; i < 603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_604(int& val) {
    const int x = 604;
    std::cout << 604;
    for (int i = 0; i < 604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_605(int& val) {
    const int x = 605;
    std::cout << 605;
    for (int i = 0; i < 605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_606(int& val) {
    const int x = 606;
    std::cout << 606;
    for (int i = 0; i < 606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_607(int& val) {
    const int x = 607;
    std::cout << 607;
    for (int i = 0; i < 607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_608(int& val) {
    const int x = 608;
    std::cout << 608;
    for (int i = 0; i < 608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_609(int& val) {
    const int x = 609;
    std::cout << 609;
    for (int i = 0; i < 609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_610(int& val) {
    const int x = 610;
    std::cout << 610;
    for (int i = 0; i < 610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_611(int& val) {
    const int x = 611;
    std::cout << 611;
    for (int i = 0; i < 611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_612(int& val) {
    const int x = 612;
    std::cout << 612;
    for (int i = 0; i < 612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_613(int& val) {
    const int x = 613;
    std::cout << 613;
    for (int i = 0; i < 613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_614(int& val) {
    const int x = 614;
    std::cout << 614;
    for (int i = 0; i < 614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_615(int& val) {
    const int x = 615;
    std::cout << 615;
    for (int i = 0; i < 615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_616(int& val) {
    const int x = 616;
    std::cout << 616;
    for (int i = 0; i < 616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_617(int& val) {
    const int x = 617;
    std::cout << 617;
    for (int i = 0; i < 617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_618(int& val) {
    const int x = 618;
    std::cout << 618;
    for (int i = 0; i < 618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_619(int& val) {
    const int x = 619;
    std::cout << 619;
    for (int i = 0; i < 619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_620(int& val) {
    const int x = 620;
    std::cout << 620;
    for (int i = 0; i < 620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_621(int& val) {
    const int x = 621;
    std::cout << 621;
    for (int i = 0; i < 621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_622(int& val) {
    const int x = 622;
    std::cout << 622;
    for (int i = 0; i < 622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_623(int& val) {
    const int x = 623;
    std::cout << 623;
    for (int i = 0; i < 623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_624(int& val) {
    const int x = 624;
    std::cout << 624;
    for (int i = 0; i < 624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_625(int& val) {
    const int x = 625;
    std::cout << 625;
    for (int i = 0; i < 625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_626(int& val) {
    const int x = 626;
    std::cout << 626;
    for (int i = 0; i < 626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_627(int& val) {
    const int x = 627;
    std::cout << 627;
    for (int i = 0; i < 627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_628(int& val) {
    const int x = 628;
    std::cout << 628;
    for (int i = 0; i < 628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_629(int& val) {
    const int x = 629;
    std::cout << 629;
    for (int i = 0; i < 629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_630(int& val) {
    const int x = 630;
    std::cout << 630;
    for (int i = 0; i < 630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_631(int& val) {
    const int x = 631;
    std::cout << 631;
    for (int i = 0; i < 631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_632(int& val) {
    const int x = 632;
    std::cout << 632;
    for (int i = 0; i < 632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_633(int& val) {
    const int x = 633;
    std::cout << 633;
    for (int i = 0; i < 633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_634(int& val) {
    const int x = 634;
    std::cout << 634;
    for (int i = 0; i < 634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_635(int& val) {
    const int x = 635;
    std::cout << 635;
    for (int i = 0; i < 635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_636(int& val) {
    const int x = 636;
    std::cout << 636;
    for (int i = 0; i < 636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_637(int& val) {
    const int x = 637;
    std::cout << 637;
    for (int i = 0; i < 637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_638(int& val) {
    const int x = 638;
    std::cout << 638;
    for (int i = 0; i < 638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_639(int& val) {
    const int x = 639;
    std::cout << 639;
    for (int i = 0; i < 639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_640(int& val) {
    const int x = 640;
    std::cout << 640;
    for (int i = 0; i < 640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_641(int& val) {
    const int x = 641;
    std::cout << 641;
    for (int i = 0; i < 641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_642(int& val) {
    const int x = 642;
    std::cout << 642;
    for (int i = 0; i < 642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_643(int& val) {
    const int x = 643;
    std::cout << 643;
    for (int i = 0; i < 643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_644(int& val) {
    const int x = 644;
    std::cout << 644;
    for (int i = 0; i < 644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_645(int& val) {
    const int x = 645;
    std::cout << 645;
    for (int i = 0; i < 645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_646(int& val) {
    const int x = 646;
    std::cout << 646;
    for (int i = 0; i < 646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_647(int& val) {
    const int x = 647;
    std::cout << 647;
    for (int i = 0; i < 647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_648(int& val) {
    const int x = 648;
    std::cout << 648;
    for (int i = 0; i < 648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_649(int& val) {
    const int x = 649;
    std::cout << 649;
    for (int i = 0; i < 649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_650(int& val) {
    const int x = 650;
    std::cout << 650;
    for (int i = 0; i < 650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_651(int& val) {
    const int x = 651;
    std::cout << 651;
    for (int i = 0; i < 651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_652(int& val) {
    const int x = 652;
    std::cout << 652;
    for (int i = 0; i < 652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_653(int& val) {
    const int x = 653;
    std::cout << 653;
    for (int i = 0; i < 653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_654(int& val) {
    const int x = 654;
    std::cout << 654;
    for (int i = 0; i < 654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_655(int& val) {
    const int x = 655;
    std::cout << 655;
    for (int i = 0; i < 655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_656(int& val) {
    const int x = 656;
    std::cout << 656;
    for (int i = 0; i < 656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_657(int& val) {
    const int x = 657;
    std::cout << 657;
    for (int i = 0; i < 657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_658(int& val) {
    const int x = 658;
    std::cout << 658;
    for (int i = 0; i < 658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_659(int& val) {
    const int x = 659;
    std::cout << 659;
    for (int i = 0; i < 659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_660(int& val) {
    const int x = 660;
    std::cout << 660;
    for (int i = 0; i < 660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_661(int& val) {
    const int x = 661;
    std::cout << 661;
    for (int i = 0; i < 661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_662(int& val) {
    const int x = 662;
    std::cout << 662;
    for (int i = 0; i < 662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_663(int& val) {
    const int x = 663;
    std::cout << 663;
    for (int i = 0; i < 663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_664(int& val) {
    const int x = 664;
    std::cout << 664;
    for (int i = 0; i < 664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_665(int& val) {
    const int x = 665;
    std::cout << 665;
    for (int i = 0; i < 665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_666(int& val) {
    const int x = 666;
    std::cout << 666;
    for (int i = 0; i < 666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_667(int& val) {
    const int x = 667;
    std::cout << 667;
    for (int i = 0; i < 667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_668(int& val) {
    const int x = 668;
    std::cout << 668;
    for (int i = 0; i < 668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_669(int& val) {
    const int x = 669;
    std::cout << 669;
    for (int i = 0; i < 669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_670(int& val) {
    const int x = 670;
    std::cout << 670;
    for (int i = 0; i < 670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_671(int& val) {
    const int x = 671;
    std::cout << 671;
    for (int i = 0; i < 671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_672(int& val) {
    const int x = 672;
    std::cout << 672;
    for (int i = 0; i < 672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_673(int& val) {
    const int x = 673;
    std::cout << 673;
    for (int i = 0; i < 673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_674(int& val) {
    const int x = 674;
    std::cout << 674;
    for (int i = 0; i < 674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_675(int& val) {
    const int x = 675;
    std::cout << 675;
    for (int i = 0; i < 675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_676(int& val) {
    const int x = 676;
    std::cout << 676;
    for (int i = 0; i < 676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_677(int& val) {
    const int x = 677;
    std::cout << 677;
    for (int i = 0; i < 677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_678(int& val) {
    const int x = 678;
    std::cout << 678;
    for (int i = 0; i < 678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_679(int& val) {
    const int x = 679;
    std::cout << 679;
    for (int i = 0; i < 679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_680(int& val) {
    const int x = 680;
    std::cout << 680;
    for (int i = 0; i < 680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_681(int& val) {
    const int x = 681;
    std::cout << 681;
    for (int i = 0; i < 681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_682(int& val) {
    const int x = 682;
    std::cout << 682;
    for (int i = 0; i < 682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_683(int& val) {
    const int x = 683;
    std::cout << 683;
    for (int i = 0; i < 683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_684(int& val) {
    const int x = 684;
    std::cout << 684;
    for (int i = 0; i < 684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_685(int& val) {
    const int x = 685;
    std::cout << 685;
    for (int i = 0; i < 685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_686(int& val) {
    const int x = 686;
    std::cout << 686;
    for (int i = 0; i < 686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_687(int& val) {
    const int x = 687;
    std::cout << 687;
    for (int i = 0; i < 687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_688(int& val) {
    const int x = 688;
    std::cout << 688;
    for (int i = 0; i < 688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_689(int& val) {
    const int x = 689;
    std::cout << 689;
    for (int i = 0; i < 689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_690(int& val) {
    const int x = 690;
    std::cout << 690;
    for (int i = 0; i < 690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_691(int& val) {
    const int x = 691;
    std::cout << 691;
    for (int i = 0; i < 691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_692(int& val) {
    const int x = 692;
    std::cout << 692;
    for (int i = 0; i < 692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_693(int& val) {
    const int x = 693;
    std::cout << 693;
    for (int i = 0; i < 693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_694(int& val) {
    const int x = 694;
    std::cout << 694;
    for (int i = 0; i < 694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_695(int& val) {
    const int x = 695;
    std::cout << 695;
    for (int i = 0; i < 695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_696(int& val) {
    const int x = 696;
    std::cout << 696;
    for (int i = 0; i < 696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_697(int& val) {
    const int x = 697;
    std::cout << 697;
    for (int i = 0; i < 697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_698(int& val) {
    const int x = 698;
    std::cout << 698;
    for (int i = 0; i < 698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_699(int& val) {
    const int x = 699;
    std::cout << 699;
    for (int i = 0; i < 699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_700(int& val) {
    const int x = 700;
    std::cout << 700;
    for (int i = 0; i < 700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_701(int& val) {
    const int x = 701;
    std::cout << 701;
    for (int i = 0; i < 701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_702(int& val) {
    const int x = 702;
    std::cout << 702;
    for (int i = 0; i < 702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_703(int& val) {
    const int x = 703;
    std::cout << 703;
    for (int i = 0; i < 703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_704(int& val) {
    const int x = 704;
    std::cout << 704;
    for (int i = 0; i < 704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_705(int& val) {
    const int x = 705;
    std::cout << 705;
    for (int i = 0; i < 705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_706(int& val) {
    const int x = 706;
    std::cout << 706;
    for (int i = 0; i < 706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_707(int& val) {
    const int x = 707;
    std::cout << 707;
    for (int i = 0; i < 707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_708(int& val) {
    const int x = 708;
    std::cout << 708;
    for (int i = 0; i < 708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_709(int& val) {
    const int x = 709;
    std::cout << 709;
    for (int i = 0; i < 709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_710(int& val) {
    const int x = 710;
    std::cout << 710;
    for (int i = 0; i < 710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_711(int& val) {
    const int x = 711;
    std::cout << 711;
    for (int i = 0; i < 711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_712(int& val) {
    const int x = 712;
    std::cout << 712;
    for (int i = 0; i < 712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_713(int& val) {
    const int x = 713;
    std::cout << 713;
    for (int i = 0; i < 713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_714(int& val) {
    const int x = 714;
    std::cout << 714;
    for (int i = 0; i < 714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_715(int& val) {
    const int x = 715;
    std::cout << 715;
    for (int i = 0; i < 715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_716(int& val) {
    const int x = 716;
    std::cout << 716;
    for (int i = 0; i < 716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_717(int& val) {
    const int x = 717;
    std::cout << 717;
    for (int i = 0; i < 717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_718(int& val) {
    const int x = 718;
    std::cout << 718;
    for (int i = 0; i < 718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_719(int& val) {
    const int x = 719;
    std::cout << 719;
    for (int i = 0; i < 719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_720(int& val) {
    const int x = 720;
    std::cout << 720;
    for (int i = 0; i < 720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_721(int& val) {
    const int x = 721;
    std::cout << 721;
    for (int i = 0; i < 721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_722(int& val) {
    const int x = 722;
    std::cout << 722;
    for (int i = 0; i < 722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_723(int& val) {
    const int x = 723;
    std::cout << 723;
    for (int i = 0; i < 723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_724(int& val) {
    const int x = 724;
    std::cout << 724;
    for (int i = 0; i < 724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_725(int& val) {
    const int x = 725;
    std::cout << 725;
    for (int i = 0; i < 725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_726(int& val) {
    const int x = 726;
    std::cout << 726;
    for (int i = 0; i < 726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_727(int& val) {
    const int x = 727;
    std::cout << 727;
    for (int i = 0; i < 727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_728(int& val) {
    const int x = 728;
    std::cout << 728;
    for (int i = 0; i < 728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_729(int& val) {
    const int x = 729;
    std::cout << 729;
    for (int i = 0; i < 729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_730(int& val) {
    const int x = 730;
    std::cout << 730;
    for (int i = 0; i < 730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_731(int& val) {
    const int x = 731;
    std::cout << 731;
    for (int i = 0; i < 731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_732(int& val) {
    const int x = 732;
    std::cout << 732;
    for (int i = 0; i < 732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_733(int& val) {
    const int x = 733;
    std::cout << 733;
    for (int i = 0; i < 733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_734(int& val) {
    const int x = 734;
    std::cout << 734;
    for (int i = 0; i < 734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_735(int& val) {
    const int x = 735;
    std::cout << 735;
    for (int i = 0; i < 735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_736(int& val) {
    const int x = 736;
    std::cout << 736;
    for (int i = 0; i < 736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_737(int& val) {
    const int x = 737;
    std::cout << 737;
    for (int i = 0; i < 737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_738(int& val) {
    const int x = 738;
    std::cout << 738;
    for (int i = 0; i < 738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_739(int& val) {
    const int x = 739;
    std::cout << 739;
    for (int i = 0; i < 739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_740(int& val) {
    const int x = 740;
    std::cout << 740;
    for (int i = 0; i < 740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_741(int& val) {
    const int x = 741;
    std::cout << 741;
    for (int i = 0; i < 741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_742(int& val) {
    const int x = 742;
    std::cout << 742;
    for (int i = 0; i < 742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_743(int& val) {
    const int x = 743;
    std::cout << 743;
    for (int i = 0; i < 743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_744(int& val) {
    const int x = 744;
    std::cout << 744;
    for (int i = 0; i < 744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_745(int& val) {
    const int x = 745;
    std::cout << 745;
    for (int i = 0; i < 745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_746(int& val) {
    const int x = 746;
    std::cout << 746;
    for (int i = 0; i < 746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_747(int& val) {
    const int x = 747;
    std::cout << 747;
    for (int i = 0; i < 747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_748(int& val) {
    const int x = 748;
    std::cout << 748;
    for (int i = 0; i < 748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_749(int& val) {
    const int x = 749;
    std::cout << 749;
    for (int i = 0; i < 749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_750(int& val) {
    const int x = 750;
    std::cout << 750;
    for (int i = 0; i < 750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_751(int& val) {
    const int x = 751;
    std::cout << 751;
    for (int i = 0; i < 751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_752(int& val) {
    const int x = 752;
    std::cout << 752;
    for (int i = 0; i < 752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_753(int& val) {
    const int x = 753;
    std::cout << 753;
    for (int i = 0; i < 753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_754(int& val) {
    const int x = 754;
    std::cout << 754;
    for (int i = 0; i < 754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_755(int& val) {
    const int x = 755;
    std::cout << 755;
    for (int i = 0; i < 755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_756(int& val) {
    const int x = 756;
    std::cout << 756;
    for (int i = 0; i < 756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_757(int& val) {
    const int x = 757;
    std::cout << 757;
    for (int i = 0; i < 757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_758(int& val) {
    const int x = 758;
    std::cout << 758;
    for (int i = 0; i < 758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_759(int& val) {
    const int x = 759;
    std::cout << 759;
    for (int i = 0; i < 759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_760(int& val) {
    const int x = 760;
    std::cout << 760;
    for (int i = 0; i < 760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_761(int& val) {
    const int x = 761;
    std::cout << 761;
    for (int i = 0; i < 761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_762(int& val) {
    const int x = 762;
    std::cout << 762;
    for (int i = 0; i < 762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_763(int& val) {
    const int x = 763;
    std::cout << 763;
    for (int i = 0; i < 763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_764(int& val) {
    const int x = 764;
    std::cout << 764;
    for (int i = 0; i < 764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_765(int& val) {
    const int x = 765;
    std::cout << 765;
    for (int i = 0; i < 765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_766(int& val) {
    const int x = 766;
    std::cout << 766;
    for (int i = 0; i < 766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_767(int& val) {
    const int x = 767;
    std::cout << 767;
    for (int i = 0; i < 767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_768(int& val) {
    const int x = 768;
    std::cout << 768;
    for (int i = 0; i < 768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_769(int& val) {
    const int x = 769;
    std::cout << 769;
    for (int i = 0; i < 769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_770(int& val) {
    const int x = 770;
    std::cout << 770;
    for (int i = 0; i < 770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_771(int& val) {
    const int x = 771;
    std::cout << 771;
    for (int i = 0; i < 771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_772(int& val) {
    const int x = 772;
    std::cout << 772;
    for (int i = 0; i < 772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_773(int& val) {
    const int x = 773;
    std::cout << 773;
    for (int i = 0; i < 773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_774(int& val) {
    const int x = 774;
    std::cout << 774;
    for (int i = 0; i < 774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_775(int& val) {
    const int x = 775;
    std::cout << 775;
    for (int i = 0; i < 775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_776(int& val) {
    const int x = 776;
    std::cout << 776;
    for (int i = 0; i < 776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_777(int& val) {
    const int x = 777;
    std::cout << 777;
    for (int i = 0; i < 777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_778(int& val) {
    const int x = 778;
    std::cout << 778;
    for (int i = 0; i < 778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_779(int& val) {
    const int x = 779;
    std::cout << 779;
    for (int i = 0; i < 779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_780(int& val) {
    const int x = 780;
    std::cout << 780;
    for (int i = 0; i < 780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_781(int& val) {
    const int x = 781;
    std::cout << 781;
    for (int i = 0; i < 781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_782(int& val) {
    const int x = 782;
    std::cout << 782;
    for (int i = 0; i < 782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_783(int& val) {
    const int x = 783;
    std::cout << 783;
    for (int i = 0; i < 783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_784(int& val) {
    const int x = 784;
    std::cout << 784;
    for (int i = 0; i < 784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_785(int& val) {
    const int x = 785;
    std::cout << 785;
    for (int i = 0; i < 785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_786(int& val) {
    const int x = 786;
    std::cout << 786;
    for (int i = 0; i < 786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_787(int& val) {
    const int x = 787;
    std::cout << 787;
    for (int i = 0; i < 787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_788(int& val) {
    const int x = 788;
    std::cout << 788;
    for (int i = 0; i < 788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_789(int& val) {
    const int x = 789;
    std::cout << 789;
    for (int i = 0; i < 789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_790(int& val) {
    const int x = 790;
    std::cout << 790;
    for (int i = 0; i < 790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_791(int& val) {
    const int x = 791;
    std::cout << 791;
    for (int i = 0; i < 791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_792(int& val) {
    const int x = 792;
    std::cout << 792;
    for (int i = 0; i < 792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_793(int& val) {
    const int x = 793;
    std::cout << 793;
    for (int i = 0; i < 793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_794(int& val) {
    const int x = 794;
    std::cout << 794;
    for (int i = 0; i < 794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_795(int& val) {
    const int x = 795;
    std::cout << 795;
    for (int i = 0; i < 795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_796(int& val) {
    const int x = 796;
    std::cout << 796;
    for (int i = 0; i < 796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_797(int& val) {
    const int x = 797;
    std::cout << 797;
    for (int i = 0; i < 797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_798(int& val) {
    const int x = 798;
    std::cout << 798;
    for (int i = 0; i < 798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_799(int& val) {
    const int x = 799;
    std::cout << 799;
    for (int i = 0; i < 799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_800(int& val) {
    const int x = 800;
    std::cout << 800;
    for (int i = 0; i < 800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_801(int& val) {
    const int x = 801;
    std::cout << 801;
    for (int i = 0; i < 801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_802(int& val) {
    const int x = 802;
    std::cout << 802;
    for (int i = 0; i < 802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_803(int& val) {
    const int x = 803;
    std::cout << 803;
    for (int i = 0; i < 803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_804(int& val) {
    const int x = 804;
    std::cout << 804;
    for (int i = 0; i < 804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_805(int& val) {
    const int x = 805;
    std::cout << 805;
    for (int i = 0; i < 805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_806(int& val) {
    const int x = 806;
    std::cout << 806;
    for (int i = 0; i < 806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_807(int& val) {
    const int x = 807;
    std::cout << 807;
    for (int i = 0; i < 807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_808(int& val) {
    const int x = 808;
    std::cout << 808;
    for (int i = 0; i < 808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_809(int& val) {
    const int x = 809;
    std::cout << 809;
    for (int i = 0; i < 809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_810(int& val) {
    const int x = 810;
    std::cout << 810;
    for (int i = 0; i < 810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_811(int& val) {
    const int x = 811;
    std::cout << 811;
    for (int i = 0; i < 811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_812(int& val) {
    const int x = 812;
    std::cout << 812;
    for (int i = 0; i < 812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_813(int& val) {
    const int x = 813;
    std::cout << 813;
    for (int i = 0; i < 813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_814(int& val) {
    const int x = 814;
    std::cout << 814;
    for (int i = 0; i < 814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_815(int& val) {
    const int x = 815;
    std::cout << 815;
    for (int i = 0; i < 815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_816(int& val) {
    const int x = 816;
    std::cout << 816;
    for (int i = 0; i < 816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_817(int& val) {
    const int x = 817;
    std::cout << 817;
    for (int i = 0; i < 817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_818(int& val) {
    const int x = 818;
    std::cout << 818;
    for (int i = 0; i < 818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_819(int& val) {
    const int x = 819;
    std::cout << 819;
    for (int i = 0; i < 819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_820(int& val) {
    const int x = 820;
    std::cout << 820;
    for (int i = 0; i < 820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_821(int& val) {
    const int x = 821;
    std::cout << 821;
    for (int i = 0; i < 821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_822(int& val) {
    const int x = 822;
    std::cout << 822;
    for (int i = 0; i < 822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_823(int& val) {
    const int x = 823;
    std::cout << 823;
    for (int i = 0; i < 823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_824(int& val) {
    const int x = 824;
    std::cout << 824;
    for (int i = 0; i < 824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_825(int& val) {
    const int x = 825;
    std::cout << 825;
    for (int i = 0; i < 825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_826(int& val) {
    const int x = 826;
    std::cout << 826;
    for (int i = 0; i < 826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_827(int& val) {
    const int x = 827;
    std::cout << 827;
    for (int i = 0; i < 827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_828(int& val) {
    const int x = 828;
    std::cout << 828;
    for (int i = 0; i < 828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_829(int& val) {
    const int x = 829;
    std::cout << 829;
    for (int i = 0; i < 829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_830(int& val) {
    const int x = 830;
    std::cout << 830;
    for (int i = 0; i < 830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_831(int& val) {
    const int x = 831;
    std::cout << 831;
    for (int i = 0; i < 831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_832(int& val) {
    const int x = 832;
    std::cout << 832;
    for (int i = 0; i < 832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_833(int& val) {
    const int x = 833;
    std::cout << 833;
    for (int i = 0; i < 833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_834(int& val) {
    const int x = 834;
    std::cout << 834;
    for (int i = 0; i < 834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_835(int& val) {
    const int x = 835;
    std::cout << 835;
    for (int i = 0; i < 835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_836(int& val) {
    const int x = 836;
    std::cout << 836;
    for (int i = 0; i < 836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_837(int& val) {
    const int x = 837;
    std::cout << 837;
    for (int i = 0; i < 837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_838(int& val) {
    const int x = 838;
    std::cout << 838;
    for (int i = 0; i < 838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_839(int& val) {
    const int x = 839;
    std::cout << 839;
    for (int i = 0; i < 839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_840(int& val) {
    const int x = 840;
    std::cout << 840;
    for (int i = 0; i < 840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_841(int& val) {
    const int x = 841;
    std::cout << 841;
    for (int i = 0; i < 841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_842(int& val) {
    const int x = 842;
    std::cout << 842;
    for (int i = 0; i < 842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_843(int& val) {
    const int x = 843;
    std::cout << 843;
    for (int i = 0; i < 843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_844(int& val) {
    const int x = 844;
    std::cout << 844;
    for (int i = 0; i < 844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_845(int& val) {
    const int x = 845;
    std::cout << 845;
    for (int i = 0; i < 845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_846(int& val) {
    const int x = 846;
    std::cout << 846;
    for (int i = 0; i < 846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_847(int& val) {
    const int x = 847;
    std::cout << 847;
    for (int i = 0; i < 847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_848(int& val) {
    const int x = 848;
    std::cout << 848;
    for (int i = 0; i < 848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_849(int& val) {
    const int x = 849;
    std::cout << 849;
    for (int i = 0; i < 849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_850(int& val) {
    const int x = 850;
    std::cout << 850;
    for (int i = 0; i < 850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_851(int& val) {
    const int x = 851;
    std::cout << 851;
    for (int i = 0; i < 851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_852(int& val) {
    const int x = 852;
    std::cout << 852;
    for (int i = 0; i < 852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_853(int& val) {
    const int x = 853;
    std::cout << 853;
    for (int i = 0; i < 853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_854(int& val) {
    const int x = 854;
    std::cout << 854;
    for (int i = 0; i < 854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_855(int& val) {
    const int x = 855;
    std::cout << 855;
    for (int i = 0; i < 855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_856(int& val) {
    const int x = 856;
    std::cout << 856;
    for (int i = 0; i < 856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_857(int& val) {
    const int x = 857;
    std::cout << 857;
    for (int i = 0; i < 857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_858(int& val) {
    const int x = 858;
    std::cout << 858;
    for (int i = 0; i < 858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_859(int& val) {
    const int x = 859;
    std::cout << 859;
    for (int i = 0; i < 859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_860(int& val) {
    const int x = 860;
    std::cout << 860;
    for (int i = 0; i < 860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_861(int& val) {
    const int x = 861;
    std::cout << 861;
    for (int i = 0; i < 861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_862(int& val) {
    const int x = 862;
    std::cout << 862;
    for (int i = 0; i < 862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_863(int& val) {
    const int x = 863;
    std::cout << 863;
    for (int i = 0; i < 863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_864(int& val) {
    const int x = 864;
    std::cout << 864;
    for (int i = 0; i < 864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_865(int& val) {
    const int x = 865;
    std::cout << 865;
    for (int i = 0; i < 865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_866(int& val) {
    const int x = 866;
    std::cout << 866;
    for (int i = 0; i < 866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_867(int& val) {
    const int x = 867;
    std::cout << 867;
    for (int i = 0; i < 867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_868(int& val) {
    const int x = 868;
    std::cout << 868;
    for (int i = 0; i < 868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_869(int& val) {
    const int x = 869;
    std::cout << 869;
    for (int i = 0; i < 869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_870(int& val) {
    const int x = 870;
    std::cout << 870;
    for (int i = 0; i < 870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_871(int& val) {
    const int x = 871;
    std::cout << 871;
    for (int i = 0; i < 871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_872(int& val) {
    const int x = 872;
    std::cout << 872;
    for (int i = 0; i < 872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_873(int& val) {
    const int x = 873;
    std::cout << 873;
    for (int i = 0; i < 873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_874(int& val) {
    const int x = 874;
    std::cout << 874;
    for (int i = 0; i < 874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_875(int& val) {
    const int x = 875;
    std::cout << 875;
    for (int i = 0; i < 875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_876(int& val) {
    const int x = 876;
    std::cout << 876;
    for (int i = 0; i < 876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_877(int& val) {
    const int x = 877;
    std::cout << 877;
    for (int i = 0; i < 877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_878(int& val) {
    const int x = 878;
    std::cout << 878;
    for (int i = 0; i < 878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_879(int& val) {
    const int x = 879;
    std::cout << 879;
    for (int i = 0; i < 879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_880(int& val) {
    const int x = 880;
    std::cout << 880;
    for (int i = 0; i < 880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_881(int& val) {
    const int x = 881;
    std::cout << 881;
    for (int i = 0; i < 881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_882(int& val) {
    const int x = 882;
    std::cout << 882;
    for (int i = 0; i < 882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_883(int& val) {
    const int x = 883;
    std::cout << 883;
    for (int i = 0; i < 883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_884(int& val) {
    const int x = 884;
    std::cout << 884;
    for (int i = 0; i < 884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_885(int& val) {
    const int x = 885;
    std::cout << 885;
    for (int i = 0; i < 885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_886(int& val) {
    const int x = 886;
    std::cout << 886;
    for (int i = 0; i < 886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_887(int& val) {
    const int x = 887;
    std::cout << 887;
    for (int i = 0; i < 887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_888(int& val) {
    const int x = 888;
    std::cout << 888;
    for (int i = 0; i < 888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_889(int& val) {
    const int x = 889;
    std::cout << 889;
    for (int i = 0; i < 889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_890(int& val) {
    const int x = 890;
    std::cout << 890;
    for (int i = 0; i < 890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_891(int& val) {
    const int x = 891;
    std::cout << 891;
    for (int i = 0; i < 891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_892(int& val) {
    const int x = 892;
    std::cout << 892;
    for (int i = 0; i < 892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_893(int& val) {
    const int x = 893;
    std::cout << 893;
    for (int i = 0; i < 893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_894(int& val) {
    const int x = 894;
    std::cout << 894;
    for (int i = 0; i < 894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_895(int& val) {
    const int x = 895;
    std::cout << 895;
    for (int i = 0; i < 895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_896(int& val) {
    const int x = 896;
    std::cout << 896;
    for (int i = 0; i < 896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_897(int& val) {
    const int x = 897;
    std::cout << 897;
    for (int i = 0; i < 897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_898(int& val) {
    const int x = 898;
    std::cout << 898;
    for (int i = 0; i < 898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_899(int& val) {
    const int x = 899;
    std::cout << 899;
    for (int i = 0; i < 899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_900(int& val) {
    const int x = 900;
    std::cout << 900;
    for (int i = 0; i < 900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_901(int& val) {
    const int x = 901;
    std::cout << 901;
    for (int i = 0; i < 901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_902(int& val) {
    const int x = 902;
    std::cout << 902;
    for (int i = 0; i < 902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_903(int& val) {
    const int x = 903;
    std::cout << 903;
    for (int i = 0; i < 903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_904(int& val) {
    const int x = 904;
    std::cout << 904;
    for (int i = 0; i < 904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_905(int& val) {
    const int x = 905;
    std::cout << 905;
    for (int i = 0; i < 905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_906(int& val) {
    const int x = 906;
    std::cout << 906;
    for (int i = 0; i < 906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_907(int& val) {
    const int x = 907;
    std::cout << 907;
    for (int i = 0; i < 907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_908(int& val) {
    const int x = 908;
    std::cout << 908;
    for (int i = 0; i < 908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_909(int& val) {
    const int x = 909;
    std::cout << 909;
    for (int i = 0; i < 909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_910(int& val) {
    const int x = 910;
    std::cout << 910;
    for (int i = 0; i < 910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_911(int& val) {
    const int x = 911;
    std::cout << 911;
    for (int i = 0; i < 911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_912(int& val) {
    const int x = 912;
    std::cout << 912;
    for (int i = 0; i < 912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_913(int& val) {
    const int x = 913;
    std::cout << 913;
    for (int i = 0; i < 913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_914(int& val) {
    const int x = 914;
    std::cout << 914;
    for (int i = 0; i < 914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_915(int& val) {
    const int x = 915;
    std::cout << 915;
    for (int i = 0; i < 915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_916(int& val) {
    const int x = 916;
    std::cout << 916;
    for (int i = 0; i < 916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_917(int& val) {
    const int x = 917;
    std::cout << 917;
    for (int i = 0; i < 917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_918(int& val) {
    const int x = 918;
    std::cout << 918;
    for (int i = 0; i < 918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_919(int& val) {
    const int x = 919;
    std::cout << 919;
    for (int i = 0; i < 919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_920(int& val) {
    const int x = 920;
    std::cout << 920;
    for (int i = 0; i < 920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_921(int& val) {
    const int x = 921;
    std::cout << 921;
    for (int i = 0; i < 921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_922(int& val) {
    const int x = 922;
    std::cout << 922;
    for (int i = 0; i < 922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_923(int& val) {
    const int x = 923;
    std::cout << 923;
    for (int i = 0; i < 923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_924(int& val) {
    const int x = 924;
    std::cout << 924;
    for (int i = 0; i < 924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_925(int& val) {
    const int x = 925;
    std::cout << 925;
    for (int i = 0; i < 925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_926(int& val) {
    const int x = 926;
    std::cout << 926;
    for (int i = 0; i < 926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_927(int& val) {
    const int x = 927;
    std::cout << 927;
    for (int i = 0; i < 927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_928(int& val) {
    const int x = 928;
    std::cout << 928;
    for (int i = 0; i < 928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_929(int& val) {
    const int x = 929;
    std::cout << 929;
    for (int i = 0; i < 929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_930(int& val) {
    const int x = 930;
    std::cout << 930;
    for (int i = 0; i < 930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_931(int& val) {
    const int x = 931;
    std::cout << 931;
    for (int i = 0; i < 931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_932(int& val) {
    const int x = 932;
    std::cout << 932;
    for (int i = 0; i < 932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_933(int& val) {
    const int x = 933;
    std::cout << 933;
    for (int i = 0; i < 933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_934(int& val) {
    const int x = 934;
    std::cout << 934;
    for (int i = 0; i < 934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_935(int& val) {
    const int x = 935;
    std::cout << 935;
    for (int i = 0; i < 935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_936(int& val) {
    const int x = 936;
    std::cout << 936;
    for (int i = 0; i < 936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_937(int& val) {
    const int x = 937;
    std::cout << 937;
    for (int i = 0; i < 937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_938(int& val) {
    const int x = 938;
    std::cout << 938;
    for (int i = 0; i < 938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_939(int& val) {
    const int x = 939;
    std::cout << 939;
    for (int i = 0; i < 939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_940(int& val) {
    const int x = 940;
    std::cout << 940;
    for (int i = 0; i < 940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_941(int& val) {
    const int x = 941;
    std::cout << 941;
    for (int i = 0; i < 941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_942(int& val) {
    const int x = 942;
    std::cout << 942;
    for (int i = 0; i < 942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_943(int& val) {
    const int x = 943;
    std::cout << 943;
    for (int i = 0; i < 943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_944(int& val) {
    const int x = 944;
    std::cout << 944;
    for (int i = 0; i < 944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_945(int& val) {
    const int x = 945;
    std::cout << 945;
    for (int i = 0; i < 945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_946(int& val) {
    const int x = 946;
    std::cout << 946;
    for (int i = 0; i < 946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_947(int& val) {
    const int x = 947;
    std::cout << 947;
    for (int i = 0; i < 947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_948(int& val) {
    const int x = 948;
    std::cout << 948;
    for (int i = 0; i < 948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_949(int& val) {
    const int x = 949;
    std::cout << 949;
    for (int i = 0; i < 949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_950(int& val) {
    const int x = 950;
    std::cout << 950;
    for (int i = 0; i < 950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_951(int& val) {
    const int x = 951;
    std::cout << 951;
    for (int i = 0; i < 951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_952(int& val) {
    const int x = 952;
    std::cout << 952;
    for (int i = 0; i < 952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_953(int& val) {
    const int x = 953;
    std::cout << 953;
    for (int i = 0; i < 953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_954(int& val) {
    const int x = 954;
    std::cout << 954;
    for (int i = 0; i < 954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_955(int& val) {
    const int x = 955;
    std::cout << 955;
    for (int i = 0; i < 955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_956(int& val) {
    const int x = 956;
    std::cout << 956;
    for (int i = 0; i < 956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_957(int& val) {
    const int x = 957;
    std::cout << 957;
    for (int i = 0; i < 957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_958(int& val) {
    const int x = 958;
    std::cout << 958;
    for (int i = 0; i < 958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_959(int& val) {
    const int x = 959;
    std::cout << 959;
    for (int i = 0; i < 959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_960(int& val) {
    const int x = 960;
    std::cout << 960;
    for (int i = 0; i < 960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_961(int& val) {
    const int x = 961;
    std::cout << 961;
    for (int i = 0; i < 961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_962(int& val) {
    const int x = 962;
    std::cout << 962;
    for (int i = 0; i < 962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_963(int& val) {
    const int x = 963;
    std::cout << 963;
    for (int i = 0; i < 963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_964(int& val) {
    const int x = 964;
    std::cout << 964;
    for (int i = 0; i < 964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_965(int& val) {
    const int x = 965;
    std::cout << 965;
    for (int i = 0; i < 965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_966(int& val) {
    const int x = 966;
    std::cout << 966;
    for (int i = 0; i < 966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_967(int& val) {
    const int x = 967;
    std::cout << 967;
    for (int i = 0; i < 967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_968(int& val) {
    const int x = 968;
    std::cout << 968;
    for (int i = 0; i < 968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_969(int& val) {
    const int x = 969;
    std::cout << 969;
    for (int i = 0; i < 969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_970(int& val) {
    const int x = 970;
    std::cout << 970;
    for (int i = 0; i < 970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_971(int& val) {
    const int x = 971;
    std::cout << 971;
    for (int i = 0; i < 971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_972(int& val) {
    const int x = 972;
    std::cout << 972;
    for (int i = 0; i < 972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_973(int& val) {
    const int x = 973;
    std::cout << 973;
    for (int i = 0; i < 973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_974(int& val) {
    const int x = 974;
    std::cout << 974;
    for (int i = 0; i < 974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_975(int& val) {
    const int x = 975;
    std::cout << 975;
    for (int i = 0; i < 975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_976(int& val) {
    const int x = 976;
    std::cout << 976;
    for (int i = 0; i < 976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_977(int& val) {
    const int x = 977;
    std::cout << 977;
    for (int i = 0; i < 977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_978(int& val) {
    const int x = 978;
    std::cout << 978;
    for (int i = 0; i < 978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_979(int& val) {
    const int x = 979;
    std::cout << 979;
    for (int i = 0; i < 979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_980(int& val) {
    const int x = 980;
    std::cout << 980;
    for (int i = 0; i < 980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_981(int& val) {
    const int x = 981;
    std::cout << 981;
    for (int i = 0; i < 981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_982(int& val) {
    const int x = 982;
    std::cout << 982;
    for (int i = 0; i < 982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_983(int& val) {
    const int x = 983;
    std::cout << 983;
    for (int i = 0; i < 983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_984(int& val) {
    const int x = 984;
    std::cout << 984;
    for (int i = 0; i < 984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_985(int& val) {
    const int x = 985;
    std::cout << 985;
    for (int i = 0; i < 985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_986(int& val) {
    const int x = 986;
    std::cout << 986;
    for (int i = 0; i < 986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_987(int& val) {
    const int x = 987;
    std::cout << 987;
    for (int i = 0; i < 987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_988(int& val) {
    const int x = 988;
    std::cout << 988;
    for (int i = 0; i < 988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_989(int& val) {
    const int x = 989;
    std::cout << 989;
    for (int i = 0; i < 989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_990(int& val) {
    const int x = 990;
    std::cout << 990;
    for (int i = 0; i < 990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_991(int& val) {
    const int x = 991;
    std::cout << 991;
    for (int i = 0; i < 991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_992(int& val) {
    const int x = 992;
    std::cout << 992;
    for (int i = 0; i < 992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_993(int& val) {
    const int x = 993;
    std::cout << 993;
    for (int i = 0; i < 993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_994(int& val) {
    const int x = 994;
    std::cout << 994;
    for (int i = 0; i < 994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_995(int& val) {
    const int x = 995;
    std::cout << 995;
    for (int i = 0; i < 995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_996(int& val) {
    const int x = 996;
    std::cout << 996;
    for (int i = 0; i < 996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_997(int& val) {
    const int x = 997;
    std::cout << 997;
    for (int i = 0; i < 997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_998(int& val) {
    const int x = 998;
    std::cout << 998;
    for (int i = 0; i < 998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_999(int& val) {
    const int x = 999;
    std::cout << 999;
    for (int i = 0; i < 999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1000(int& val) {
    const int x = 1000;
    std::cout << 1000;
    for (int i = 0; i < 1000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1001(int& val) {
    const int x = 1001;
    std::cout << 1001;
    for (int i = 0; i < 1001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1002(int& val) {
    const int x = 1002;
    std::cout << 1002;
    for (int i = 0; i < 1002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1003(int& val) {
    const int x = 1003;
    std::cout << 1003;
    for (int i = 0; i < 1003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1004(int& val) {
    const int x = 1004;
    std::cout << 1004;
    for (int i = 0; i < 1004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1005(int& val) {
    const int x = 1005;
    std::cout << 1005;
    for (int i = 0; i < 1005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1006(int& val) {
    const int x = 1006;
    std::cout << 1006;
    for (int i = 0; i < 1006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1007(int& val) {
    const int x = 1007;
    std::cout << 1007;
    for (int i = 0; i < 1007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1008(int& val) {
    const int x = 1008;
    std::cout << 1008;
    for (int i = 0; i < 1008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1009(int& val) {
    const int x = 1009;
    std::cout << 1009;
    for (int i = 0; i < 1009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1010(int& val) {
    const int x = 1010;
    std::cout << 1010;
    for (int i = 0; i < 1010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1011(int& val) {
    const int x = 1011;
    std::cout << 1011;
    for (int i = 0; i < 1011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1012(int& val) {
    const int x = 1012;
    std::cout << 1012;
    for (int i = 0; i < 1012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1013(int& val) {
    const int x = 1013;
    std::cout << 1013;
    for (int i = 0; i < 1013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1014(int& val) {
    const int x = 1014;
    std::cout << 1014;
    for (int i = 0; i < 1014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1015(int& val) {
    const int x = 1015;
    std::cout << 1015;
    for (int i = 0; i < 1015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1016(int& val) {
    const int x = 1016;
    std::cout << 1016;
    for (int i = 0; i < 1016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1017(int& val) {
    const int x = 1017;
    std::cout << 1017;
    for (int i = 0; i < 1017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1018(int& val) {
    const int x = 1018;
    std::cout << 1018;
    for (int i = 0; i < 1018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1019(int& val) {
    const int x = 1019;
    std::cout << 1019;
    for (int i = 0; i < 1019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1020(int& val) {
    const int x = 1020;
    std::cout << 1020;
    for (int i = 0; i < 1020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1021(int& val) {
    const int x = 1021;
    std::cout << 1021;
    for (int i = 0; i < 1021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1022(int& val) {
    const int x = 1022;
    std::cout << 1022;
    for (int i = 0; i < 1022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1023(int& val) {
    const int x = 1023;
    std::cout << 1023;
    for (int i = 0; i < 1023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1024(int& val) {
    const int x = 1024;
    std::cout << 1024;
    for (int i = 0; i < 1024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1025(int& val) {
    const int x = 1025;
    std::cout << 1025;
    for (int i = 0; i < 1025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1026(int& val) {
    const int x = 1026;
    std::cout << 1026;
    for (int i = 0; i < 1026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1027(int& val) {
    const int x = 1027;
    std::cout << 1027;
    for (int i = 0; i < 1027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1028(int& val) {
    const int x = 1028;
    std::cout << 1028;
    for (int i = 0; i < 1028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1029(int& val) {
    const int x = 1029;
    std::cout << 1029;
    for (int i = 0; i < 1029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1030(int& val) {
    const int x = 1030;
    std::cout << 1030;
    for (int i = 0; i < 1030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1031(int& val) {
    const int x = 1031;
    std::cout << 1031;
    for (int i = 0; i < 1031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1032(int& val) {
    const int x = 1032;
    std::cout << 1032;
    for (int i = 0; i < 1032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1033(int& val) {
    const int x = 1033;
    std::cout << 1033;
    for (int i = 0; i < 1033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1034(int& val) {
    const int x = 1034;
    std::cout << 1034;
    for (int i = 0; i < 1034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1035(int& val) {
    const int x = 1035;
    std::cout << 1035;
    for (int i = 0; i < 1035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1036(int& val) {
    const int x = 1036;
    std::cout << 1036;
    for (int i = 0; i < 1036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1037(int& val) {
    const int x = 1037;
    std::cout << 1037;
    for (int i = 0; i < 1037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1038(int& val) {
    const int x = 1038;
    std::cout << 1038;
    for (int i = 0; i < 1038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1039(int& val) {
    const int x = 1039;
    std::cout << 1039;
    for (int i = 0; i < 1039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1040(int& val) {
    const int x = 1040;
    std::cout << 1040;
    for (int i = 0; i < 1040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1041(int& val) {
    const int x = 1041;
    std::cout << 1041;
    for (int i = 0; i < 1041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1042(int& val) {
    const int x = 1042;
    std::cout << 1042;
    for (int i = 0; i < 1042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1043(int& val) {
    const int x = 1043;
    std::cout << 1043;
    for (int i = 0; i < 1043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1044(int& val) {
    const int x = 1044;
    std::cout << 1044;
    for (int i = 0; i < 1044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1045(int& val) {
    const int x = 1045;
    std::cout << 1045;
    for (int i = 0; i < 1045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1046(int& val) {
    const int x = 1046;
    std::cout << 1046;
    for (int i = 0; i < 1046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1047(int& val) {
    const int x = 1047;
    std::cout << 1047;
    for (int i = 0; i < 1047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1048(int& val) {
    const int x = 1048;
    std::cout << 1048;
    for (int i = 0; i < 1048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1049(int& val) {
    const int x = 1049;
    std::cout << 1049;
    for (int i = 0; i < 1049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1050(int& val) {
    const int x = 1050;
    std::cout << 1050;
    for (int i = 0; i < 1050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1051(int& val) {
    const int x = 1051;
    std::cout << 1051;
    for (int i = 0; i < 1051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1052(int& val) {
    const int x = 1052;
    std::cout << 1052;
    for (int i = 0; i < 1052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1053(int& val) {
    const int x = 1053;
    std::cout << 1053;
    for (int i = 0; i < 1053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1054(int& val) {
    const int x = 1054;
    std::cout << 1054;
    for (int i = 0; i < 1054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1055(int& val) {
    const int x = 1055;
    std::cout << 1055;
    for (int i = 0; i < 1055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1056(int& val) {
    const int x = 1056;
    std::cout << 1056;
    for (int i = 0; i < 1056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1057(int& val) {
    const int x = 1057;
    std::cout << 1057;
    for (int i = 0; i < 1057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1058(int& val) {
    const int x = 1058;
    std::cout << 1058;
    for (int i = 0; i < 1058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1059(int& val) {
    const int x = 1059;
    std::cout << 1059;
    for (int i = 0; i < 1059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1060(int& val) {
    const int x = 1060;
    std::cout << 1060;
    for (int i = 0; i < 1060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1061(int& val) {
    const int x = 1061;
    std::cout << 1061;
    for (int i = 0; i < 1061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1062(int& val) {
    const int x = 1062;
    std::cout << 1062;
    for (int i = 0; i < 1062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1063(int& val) {
    const int x = 1063;
    std::cout << 1063;
    for (int i = 0; i < 1063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1064(int& val) {
    const int x = 1064;
    std::cout << 1064;
    for (int i = 0; i < 1064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1065(int& val) {
    const int x = 1065;
    std::cout << 1065;
    for (int i = 0; i < 1065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1066(int& val) {
    const int x = 1066;
    std::cout << 1066;
    for (int i = 0; i < 1066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1067(int& val) {
    const int x = 1067;
    std::cout << 1067;
    for (int i = 0; i < 1067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1068(int& val) {
    const int x = 1068;
    std::cout << 1068;
    for (int i = 0; i < 1068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1069(int& val) {
    const int x = 1069;
    std::cout << 1069;
    for (int i = 0; i < 1069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1070(int& val) {
    const int x = 1070;
    std::cout << 1070;
    for (int i = 0; i < 1070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1071(int& val) {
    const int x = 1071;
    std::cout << 1071;
    for (int i = 0; i < 1071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1072(int& val) {
    const int x = 1072;
    std::cout << 1072;
    for (int i = 0; i < 1072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1073(int& val) {
    const int x = 1073;
    std::cout << 1073;
    for (int i = 0; i < 1073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1074(int& val) {
    const int x = 1074;
    std::cout << 1074;
    for (int i = 0; i < 1074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1075(int& val) {
    const int x = 1075;
    std::cout << 1075;
    for (int i = 0; i < 1075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1076(int& val) {
    const int x = 1076;
    std::cout << 1076;
    for (int i = 0; i < 1076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1077(int& val) {
    const int x = 1077;
    std::cout << 1077;
    for (int i = 0; i < 1077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1078(int& val) {
    const int x = 1078;
    std::cout << 1078;
    for (int i = 0; i < 1078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1079(int& val) {
    const int x = 1079;
    std::cout << 1079;
    for (int i = 0; i < 1079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1080(int& val) {
    const int x = 1080;
    std::cout << 1080;
    for (int i = 0; i < 1080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1081(int& val) {
    const int x = 1081;
    std::cout << 1081;
    for (int i = 0; i < 1081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1082(int& val) {
    const int x = 1082;
    std::cout << 1082;
    for (int i = 0; i < 1082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1083(int& val) {
    const int x = 1083;
    std::cout << 1083;
    for (int i = 0; i < 1083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1084(int& val) {
    const int x = 1084;
    std::cout << 1084;
    for (int i = 0; i < 1084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1085(int& val) {
    const int x = 1085;
    std::cout << 1085;
    for (int i = 0; i < 1085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1086(int& val) {
    const int x = 1086;
    std::cout << 1086;
    for (int i = 0; i < 1086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1087(int& val) {
    const int x = 1087;
    std::cout << 1087;
    for (int i = 0; i < 1087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1088(int& val) {
    const int x = 1088;
    std::cout << 1088;
    for (int i = 0; i < 1088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1089(int& val) {
    const int x = 1089;
    std::cout << 1089;
    for (int i = 0; i < 1089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1090(int& val) {
    const int x = 1090;
    std::cout << 1090;
    for (int i = 0; i < 1090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1091(int& val) {
    const int x = 1091;
    std::cout << 1091;
    for (int i = 0; i < 1091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1092(int& val) {
    const int x = 1092;
    std::cout << 1092;
    for (int i = 0; i < 1092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1093(int& val) {
    const int x = 1093;
    std::cout << 1093;
    for (int i = 0; i < 1093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1094(int& val) {
    const int x = 1094;
    std::cout << 1094;
    for (int i = 0; i < 1094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1095(int& val) {
    const int x = 1095;
    std::cout << 1095;
    for (int i = 0; i < 1095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1096(int& val) {
    const int x = 1096;
    std::cout << 1096;
    for (int i = 0; i < 1096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1097(int& val) {
    const int x = 1097;
    std::cout << 1097;
    for (int i = 0; i < 1097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1098(int& val) {
    const int x = 1098;
    std::cout << 1098;
    for (int i = 0; i < 1098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1099(int& val) {
    const int x = 1099;
    std::cout << 1099;
    for (int i = 0; i < 1099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1100(int& val) {
    const int x = 1100;
    std::cout << 1100;
    for (int i = 0; i < 1100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1101(int& val) {
    const int x = 1101;
    std::cout << 1101;
    for (int i = 0; i < 1101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1102(int& val) {
    const int x = 1102;
    std::cout << 1102;
    for (int i = 0; i < 1102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1103(int& val) {
    const int x = 1103;
    std::cout << 1103;
    for (int i = 0; i < 1103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1104(int& val) {
    const int x = 1104;
    std::cout << 1104;
    for (int i = 0; i < 1104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1105(int& val) {
    const int x = 1105;
    std::cout << 1105;
    for (int i = 0; i < 1105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1106(int& val) {
    const int x = 1106;
    std::cout << 1106;
    for (int i = 0; i < 1106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1107(int& val) {
    const int x = 1107;
    std::cout << 1107;
    for (int i = 0; i < 1107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1108(int& val) {
    const int x = 1108;
    std::cout << 1108;
    for (int i = 0; i < 1108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1109(int& val) {
    const int x = 1109;
    std::cout << 1109;
    for (int i = 0; i < 1109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1110(int& val) {
    const int x = 1110;
    std::cout << 1110;
    for (int i = 0; i < 1110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1111(int& val) {
    const int x = 1111;
    std::cout << 1111;
    for (int i = 0; i < 1111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1112(int& val) {
    const int x = 1112;
    std::cout << 1112;
    for (int i = 0; i < 1112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1113(int& val) {
    const int x = 1113;
    std::cout << 1113;
    for (int i = 0; i < 1113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1114(int& val) {
    const int x = 1114;
    std::cout << 1114;
    for (int i = 0; i < 1114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1115(int& val) {
    const int x = 1115;
    std::cout << 1115;
    for (int i = 0; i < 1115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1116(int& val) {
    const int x = 1116;
    std::cout << 1116;
    for (int i = 0; i < 1116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1117(int& val) {
    const int x = 1117;
    std::cout << 1117;
    for (int i = 0; i < 1117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1118(int& val) {
    const int x = 1118;
    std::cout << 1118;
    for (int i = 0; i < 1118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1119(int& val) {
    const int x = 1119;
    std::cout << 1119;
    for (int i = 0; i < 1119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1120(int& val) {
    const int x = 1120;
    std::cout << 1120;
    for (int i = 0; i < 1120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1121(int& val) {
    const int x = 1121;
    std::cout << 1121;
    for (int i = 0; i < 1121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1122(int& val) {
    const int x = 1122;
    std::cout << 1122;
    for (int i = 0; i < 1122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1123(int& val) {
    const int x = 1123;
    std::cout << 1123;
    for (int i = 0; i < 1123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1124(int& val) {
    const int x = 1124;
    std::cout << 1124;
    for (int i = 0; i < 1124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1125(int& val) {
    const int x = 1125;
    std::cout << 1125;
    for (int i = 0; i < 1125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1126(int& val) {
    const int x = 1126;
    std::cout << 1126;
    for (int i = 0; i < 1126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1127(int& val) {
    const int x = 1127;
    std::cout << 1127;
    for (int i = 0; i < 1127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1128(int& val) {
    const int x = 1128;
    std::cout << 1128;
    for (int i = 0; i < 1128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1129(int& val) {
    const int x = 1129;
    std::cout << 1129;
    for (int i = 0; i < 1129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1130(int& val) {
    const int x = 1130;
    std::cout << 1130;
    for (int i = 0; i < 1130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1131(int& val) {
    const int x = 1131;
    std::cout << 1131;
    for (int i = 0; i < 1131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1132(int& val) {
    const int x = 1132;
    std::cout << 1132;
    for (int i = 0; i < 1132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1133(int& val) {
    const int x = 1133;
    std::cout << 1133;
    for (int i = 0; i < 1133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1134(int& val) {
    const int x = 1134;
    std::cout << 1134;
    for (int i = 0; i < 1134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1135(int& val) {
    const int x = 1135;
    std::cout << 1135;
    for (int i = 0; i < 1135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1136(int& val) {
    const int x = 1136;
    std::cout << 1136;
    for (int i = 0; i < 1136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1137(int& val) {
    const int x = 1137;
    std::cout << 1137;
    for (int i = 0; i < 1137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1138(int& val) {
    const int x = 1138;
    std::cout << 1138;
    for (int i = 0; i < 1138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1139(int& val) {
    const int x = 1139;
    std::cout << 1139;
    for (int i = 0; i < 1139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1140(int& val) {
    const int x = 1140;
    std::cout << 1140;
    for (int i = 0; i < 1140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1141(int& val) {
    const int x = 1141;
    std::cout << 1141;
    for (int i = 0; i < 1141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1142(int& val) {
    const int x = 1142;
    std::cout << 1142;
    for (int i = 0; i < 1142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1143(int& val) {
    const int x = 1143;
    std::cout << 1143;
    for (int i = 0; i < 1143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1144(int& val) {
    const int x = 1144;
    std::cout << 1144;
    for (int i = 0; i < 1144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1145(int& val) {
    const int x = 1145;
    std::cout << 1145;
    for (int i = 0; i < 1145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1146(int& val) {
    const int x = 1146;
    std::cout << 1146;
    for (int i = 0; i < 1146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1147(int& val) {
    const int x = 1147;
    std::cout << 1147;
    for (int i = 0; i < 1147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1148(int& val) {
    const int x = 1148;
    std::cout << 1148;
    for (int i = 0; i < 1148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1149(int& val) {
    const int x = 1149;
    std::cout << 1149;
    for (int i = 0; i < 1149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1150(int& val) {
    const int x = 1150;
    std::cout << 1150;
    for (int i = 0; i < 1150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1151(int& val) {
    const int x = 1151;
    std::cout << 1151;
    for (int i = 0; i < 1151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1152(int& val) {
    const int x = 1152;
    std::cout << 1152;
    for (int i = 0; i < 1152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1153(int& val) {
    const int x = 1153;
    std::cout << 1153;
    for (int i = 0; i < 1153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1154(int& val) {
    const int x = 1154;
    std::cout << 1154;
    for (int i = 0; i < 1154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1155(int& val) {
    const int x = 1155;
    std::cout << 1155;
    for (int i = 0; i < 1155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1156(int& val) {
    const int x = 1156;
    std::cout << 1156;
    for (int i = 0; i < 1156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1157(int& val) {
    const int x = 1157;
    std::cout << 1157;
    for (int i = 0; i < 1157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1158(int& val) {
    const int x = 1158;
    std::cout << 1158;
    for (int i = 0; i < 1158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1159(int& val) {
    const int x = 1159;
    std::cout << 1159;
    for (int i = 0; i < 1159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1160(int& val) {
    const int x = 1160;
    std::cout << 1160;
    for (int i = 0; i < 1160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1161(int& val) {
    const int x = 1161;
    std::cout << 1161;
    for (int i = 0; i < 1161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1162(int& val) {
    const int x = 1162;
    std::cout << 1162;
    for (int i = 0; i < 1162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1163(int& val) {
    const int x = 1163;
    std::cout << 1163;
    for (int i = 0; i < 1163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1164(int& val) {
    const int x = 1164;
    std::cout << 1164;
    for (int i = 0; i < 1164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1165(int& val) {
    const int x = 1165;
    std::cout << 1165;
    for (int i = 0; i < 1165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1166(int& val) {
    const int x = 1166;
    std::cout << 1166;
    for (int i = 0; i < 1166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1167(int& val) {
    const int x = 1167;
    std::cout << 1167;
    for (int i = 0; i < 1167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1168(int& val) {
    const int x = 1168;
    std::cout << 1168;
    for (int i = 0; i < 1168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1169(int& val) {
    const int x = 1169;
    std::cout << 1169;
    for (int i = 0; i < 1169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1170(int& val) {
    const int x = 1170;
    std::cout << 1170;
    for (int i = 0; i < 1170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1171(int& val) {
    const int x = 1171;
    std::cout << 1171;
    for (int i = 0; i < 1171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1172(int& val) {
    const int x = 1172;
    std::cout << 1172;
    for (int i = 0; i < 1172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1173(int& val) {
    const int x = 1173;
    std::cout << 1173;
    for (int i = 0; i < 1173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1174(int& val) {
    const int x = 1174;
    std::cout << 1174;
    for (int i = 0; i < 1174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1175(int& val) {
    const int x = 1175;
    std::cout << 1175;
    for (int i = 0; i < 1175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1176(int& val) {
    const int x = 1176;
    std::cout << 1176;
    for (int i = 0; i < 1176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1177(int& val) {
    const int x = 1177;
    std::cout << 1177;
    for (int i = 0; i < 1177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1178(int& val) {
    const int x = 1178;
    std::cout << 1178;
    for (int i = 0; i < 1178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1179(int& val) {
    const int x = 1179;
    std::cout << 1179;
    for (int i = 0; i < 1179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1180(int& val) {
    const int x = 1180;
    std::cout << 1180;
    for (int i = 0; i < 1180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1181(int& val) {
    const int x = 1181;
    std::cout << 1181;
    for (int i = 0; i < 1181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1182(int& val) {
    const int x = 1182;
    std::cout << 1182;
    for (int i = 0; i < 1182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1183(int& val) {
    const int x = 1183;
    std::cout << 1183;
    for (int i = 0; i < 1183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1184(int& val) {
    const int x = 1184;
    std::cout << 1184;
    for (int i = 0; i < 1184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1185(int& val) {
    const int x = 1185;
    std::cout << 1185;
    for (int i = 0; i < 1185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1186(int& val) {
    const int x = 1186;
    std::cout << 1186;
    for (int i = 0; i < 1186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1187(int& val) {
    const int x = 1187;
    std::cout << 1187;
    for (int i = 0; i < 1187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1188(int& val) {
    const int x = 1188;
    std::cout << 1188;
    for (int i = 0; i < 1188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1189(int& val) {
    const int x = 1189;
    std::cout << 1189;
    for (int i = 0; i < 1189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1190(int& val) {
    const int x = 1190;
    std::cout << 1190;
    for (int i = 0; i < 1190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1191(int& val) {
    const int x = 1191;
    std::cout << 1191;
    for (int i = 0; i < 1191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1192(int& val) {
    const int x = 1192;
    std::cout << 1192;
    for (int i = 0; i < 1192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1193(int& val) {
    const int x = 1193;
    std::cout << 1193;
    for (int i = 0; i < 1193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1194(int& val) {
    const int x = 1194;
    std::cout << 1194;
    for (int i = 0; i < 1194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1195(int& val) {
    const int x = 1195;
    std::cout << 1195;
    for (int i = 0; i < 1195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1196(int& val) {
    const int x = 1196;
    std::cout << 1196;
    for (int i = 0; i < 1196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1197(int& val) {
    const int x = 1197;
    std::cout << 1197;
    for (int i = 0; i < 1197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1198(int& val) {
    const int x = 1198;
    std::cout << 1198;
    for (int i = 0; i < 1198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1199(int& val) {
    const int x = 1199;
    std::cout << 1199;
    for (int i = 0; i < 1199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1200(int& val) {
    const int x = 1200;
    std::cout << 1200;
    for (int i = 0; i < 1200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1201(int& val) {
    const int x = 1201;
    std::cout << 1201;
    for (int i = 0; i < 1201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1202(int& val) {
    const int x = 1202;
    std::cout << 1202;
    for (int i = 0; i < 1202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1203(int& val) {
    const int x = 1203;
    std::cout << 1203;
    for (int i = 0; i < 1203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1204(int& val) {
    const int x = 1204;
    std::cout << 1204;
    for (int i = 0; i < 1204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1205(int& val) {
    const int x = 1205;
    std::cout << 1205;
    for (int i = 0; i < 1205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1206(int& val) {
    const int x = 1206;
    std::cout << 1206;
    for (int i = 0; i < 1206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1207(int& val) {
    const int x = 1207;
    std::cout << 1207;
    for (int i = 0; i < 1207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1208(int& val) {
    const int x = 1208;
    std::cout << 1208;
    for (int i = 0; i < 1208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1209(int& val) {
    const int x = 1209;
    std::cout << 1209;
    for (int i = 0; i < 1209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1210(int& val) {
    const int x = 1210;
    std::cout << 1210;
    for (int i = 0; i < 1210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1211(int& val) {
    const int x = 1211;
    std::cout << 1211;
    for (int i = 0; i < 1211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1212(int& val) {
    const int x = 1212;
    std::cout << 1212;
    for (int i = 0; i < 1212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1213(int& val) {
    const int x = 1213;
    std::cout << 1213;
    for (int i = 0; i < 1213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1214(int& val) {
    const int x = 1214;
    std::cout << 1214;
    for (int i = 0; i < 1214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1215(int& val) {
    const int x = 1215;
    std::cout << 1215;
    for (int i = 0; i < 1215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1216(int& val) {
    const int x = 1216;
    std::cout << 1216;
    for (int i = 0; i < 1216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1217(int& val) {
    const int x = 1217;
    std::cout << 1217;
    for (int i = 0; i < 1217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1218(int& val) {
    const int x = 1218;
    std::cout << 1218;
    for (int i = 0; i < 1218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1219(int& val) {
    const int x = 1219;
    std::cout << 1219;
    for (int i = 0; i < 1219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1220(int& val) {
    const int x = 1220;
    std::cout << 1220;
    for (int i = 0; i < 1220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1221(int& val) {
    const int x = 1221;
    std::cout << 1221;
    for (int i = 0; i < 1221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1222(int& val) {
    const int x = 1222;
    std::cout << 1222;
    for (int i = 0; i < 1222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1223(int& val) {
    const int x = 1223;
    std::cout << 1223;
    for (int i = 0; i < 1223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1224(int& val) {
    const int x = 1224;
    std::cout << 1224;
    for (int i = 0; i < 1224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1225(int& val) {
    const int x = 1225;
    std::cout << 1225;
    for (int i = 0; i < 1225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1226(int& val) {
    const int x = 1226;
    std::cout << 1226;
    for (int i = 0; i < 1226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1227(int& val) {
    const int x = 1227;
    std::cout << 1227;
    for (int i = 0; i < 1227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1228(int& val) {
    const int x = 1228;
    std::cout << 1228;
    for (int i = 0; i < 1228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1229(int& val) {
    const int x = 1229;
    std::cout << 1229;
    for (int i = 0; i < 1229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1230(int& val) {
    const int x = 1230;
    std::cout << 1230;
    for (int i = 0; i < 1230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1231(int& val) {
    const int x = 1231;
    std::cout << 1231;
    for (int i = 0; i < 1231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1232(int& val) {
    const int x = 1232;
    std::cout << 1232;
    for (int i = 0; i < 1232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1233(int& val) {
    const int x = 1233;
    std::cout << 1233;
    for (int i = 0; i < 1233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1234(int& val) {
    const int x = 1234;
    std::cout << 1234;
    for (int i = 0; i < 1234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1235(int& val) {
    const int x = 1235;
    std::cout << 1235;
    for (int i = 0; i < 1235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1236(int& val) {
    const int x = 1236;
    std::cout << 1236;
    for (int i = 0; i < 1236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1237(int& val) {
    const int x = 1237;
    std::cout << 1237;
    for (int i = 0; i < 1237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1238(int& val) {
    const int x = 1238;
    std::cout << 1238;
    for (int i = 0; i < 1238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1239(int& val) {
    const int x = 1239;
    std::cout << 1239;
    for (int i = 0; i < 1239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1240(int& val) {
    const int x = 1240;
    std::cout << 1240;
    for (int i = 0; i < 1240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1241(int& val) {
    const int x = 1241;
    std::cout << 1241;
    for (int i = 0; i < 1241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1242(int& val) {
    const int x = 1242;
    std::cout << 1242;
    for (int i = 0; i < 1242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1243(int& val) {
    const int x = 1243;
    std::cout << 1243;
    for (int i = 0; i < 1243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1244(int& val) {
    const int x = 1244;
    std::cout << 1244;
    for (int i = 0; i < 1244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1245(int& val) {
    const int x = 1245;
    std::cout << 1245;
    for (int i = 0; i < 1245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1246(int& val) {
    const int x = 1246;
    std::cout << 1246;
    for (int i = 0; i < 1246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1247(int& val) {
    const int x = 1247;
    std::cout << 1247;
    for (int i = 0; i < 1247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1248(int& val) {
    const int x = 1248;
    std::cout << 1248;
    for (int i = 0; i < 1248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1249(int& val) {
    const int x = 1249;
    std::cout << 1249;
    for (int i = 0; i < 1249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1250(int& val) {
    const int x = 1250;
    std::cout << 1250;
    for (int i = 0; i < 1250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1251(int& val) {
    const int x = 1251;
    std::cout << 1251;
    for (int i = 0; i < 1251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1252(int& val) {
    const int x = 1252;
    std::cout << 1252;
    for (int i = 0; i < 1252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1253(int& val) {
    const int x = 1253;
    std::cout << 1253;
    for (int i = 0; i < 1253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1254(int& val) {
    const int x = 1254;
    std::cout << 1254;
    for (int i = 0; i < 1254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1255(int& val) {
    const int x = 1255;
    std::cout << 1255;
    for (int i = 0; i < 1255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1256(int& val) {
    const int x = 1256;
    std::cout << 1256;
    for (int i = 0; i < 1256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1257(int& val) {
    const int x = 1257;
    std::cout << 1257;
    for (int i = 0; i < 1257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1258(int& val) {
    const int x = 1258;
    std::cout << 1258;
    for (int i = 0; i < 1258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1259(int& val) {
    const int x = 1259;
    std::cout << 1259;
    for (int i = 0; i < 1259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1260(int& val) {
    const int x = 1260;
    std::cout << 1260;
    for (int i = 0; i < 1260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1261(int& val) {
    const int x = 1261;
    std::cout << 1261;
    for (int i = 0; i < 1261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1262(int& val) {
    const int x = 1262;
    std::cout << 1262;
    for (int i = 0; i < 1262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1263(int& val) {
    const int x = 1263;
    std::cout << 1263;
    for (int i = 0; i < 1263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1264(int& val) {
    const int x = 1264;
    std::cout << 1264;
    for (int i = 0; i < 1264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1265(int& val) {
    const int x = 1265;
    std::cout << 1265;
    for (int i = 0; i < 1265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1266(int& val) {
    const int x = 1266;
    std::cout << 1266;
    for (int i = 0; i < 1266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1267(int& val) {
    const int x = 1267;
    std::cout << 1267;
    for (int i = 0; i < 1267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1268(int& val) {
    const int x = 1268;
    std::cout << 1268;
    for (int i = 0; i < 1268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1269(int& val) {
    const int x = 1269;
    std::cout << 1269;
    for (int i = 0; i < 1269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1270(int& val) {
    const int x = 1270;
    std::cout << 1270;
    for (int i = 0; i < 1270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1271(int& val) {
    const int x = 1271;
    std::cout << 1271;
    for (int i = 0; i < 1271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1272(int& val) {
    const int x = 1272;
    std::cout << 1272;
    for (int i = 0; i < 1272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1273(int& val) {
    const int x = 1273;
    std::cout << 1273;
    for (int i = 0; i < 1273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1274(int& val) {
    const int x = 1274;
    std::cout << 1274;
    for (int i = 0; i < 1274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1275(int& val) {
    const int x = 1275;
    std::cout << 1275;
    for (int i = 0; i < 1275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1276(int& val) {
    const int x = 1276;
    std::cout << 1276;
    for (int i = 0; i < 1276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1277(int& val) {
    const int x = 1277;
    std::cout << 1277;
    for (int i = 0; i < 1277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1278(int& val) {
    const int x = 1278;
    std::cout << 1278;
    for (int i = 0; i < 1278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1279(int& val) {
    const int x = 1279;
    std::cout << 1279;
    for (int i = 0; i < 1279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1280(int& val) {
    const int x = 1280;
    std::cout << 1280;
    for (int i = 0; i < 1280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1281(int& val) {
    const int x = 1281;
    std::cout << 1281;
    for (int i = 0; i < 1281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1282(int& val) {
    const int x = 1282;
    std::cout << 1282;
    for (int i = 0; i < 1282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1283(int& val) {
    const int x = 1283;
    std::cout << 1283;
    for (int i = 0; i < 1283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1284(int& val) {
    const int x = 1284;
    std::cout << 1284;
    for (int i = 0; i < 1284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1285(int& val) {
    const int x = 1285;
    std::cout << 1285;
    for (int i = 0; i < 1285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1286(int& val) {
    const int x = 1286;
    std::cout << 1286;
    for (int i = 0; i < 1286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1287(int& val) {
    const int x = 1287;
    std::cout << 1287;
    for (int i = 0; i < 1287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1288(int& val) {
    const int x = 1288;
    std::cout << 1288;
    for (int i = 0; i < 1288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1289(int& val) {
    const int x = 1289;
    std::cout << 1289;
    for (int i = 0; i < 1289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1290(int& val) {
    const int x = 1290;
    std::cout << 1290;
    for (int i = 0; i < 1290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1291(int& val) {
    const int x = 1291;
    std::cout << 1291;
    for (int i = 0; i < 1291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1292(int& val) {
    const int x = 1292;
    std::cout << 1292;
    for (int i = 0; i < 1292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1293(int& val) {
    const int x = 1293;
    std::cout << 1293;
    for (int i = 0; i < 1293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1294(int& val) {
    const int x = 1294;
    std::cout << 1294;
    for (int i = 0; i < 1294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1295(int& val) {
    const int x = 1295;
    std::cout << 1295;
    for (int i = 0; i < 1295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1296(int& val) {
    const int x = 1296;
    std::cout << 1296;
    for (int i = 0; i < 1296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1297(int& val) {
    const int x = 1297;
    std::cout << 1297;
    for (int i = 0; i < 1297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1298(int& val) {
    const int x = 1298;
    std::cout << 1298;
    for (int i = 0; i < 1298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1299(int& val) {
    const int x = 1299;
    std::cout << 1299;
    for (int i = 0; i < 1299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1300(int& val) {
    const int x = 1300;
    std::cout << 1300;
    for (int i = 0; i < 1300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1301(int& val) {
    const int x = 1301;
    std::cout << 1301;
    for (int i = 0; i < 1301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1302(int& val) {
    const int x = 1302;
    std::cout << 1302;
    for (int i = 0; i < 1302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1303(int& val) {
    const int x = 1303;
    std::cout << 1303;
    for (int i = 0; i < 1303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1304(int& val) {
    const int x = 1304;
    std::cout << 1304;
    for (int i = 0; i < 1304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1305(int& val) {
    const int x = 1305;
    std::cout << 1305;
    for (int i = 0; i < 1305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1306(int& val) {
    const int x = 1306;
    std::cout << 1306;
    for (int i = 0; i < 1306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1307(int& val) {
    const int x = 1307;
    std::cout << 1307;
    for (int i = 0; i < 1307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1308(int& val) {
    const int x = 1308;
    std::cout << 1308;
    for (int i = 0; i < 1308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1309(int& val) {
    const int x = 1309;
    std::cout << 1309;
    for (int i = 0; i < 1309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1310(int& val) {
    const int x = 1310;
    std::cout << 1310;
    for (int i = 0; i < 1310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1311(int& val) {
    const int x = 1311;
    std::cout << 1311;
    for (int i = 0; i < 1311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1312(int& val) {
    const int x = 1312;
    std::cout << 1312;
    for (int i = 0; i < 1312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1313(int& val) {
    const int x = 1313;
    std::cout << 1313;
    for (int i = 0; i < 1313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1314(int& val) {
    const int x = 1314;
    std::cout << 1314;
    for (int i = 0; i < 1314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1315(int& val) {
    const int x = 1315;
    std::cout << 1315;
    for (int i = 0; i < 1315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1316(int& val) {
    const int x = 1316;
    std::cout << 1316;
    for (int i = 0; i < 1316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1317(int& val) {
    const int x = 1317;
    std::cout << 1317;
    for (int i = 0; i < 1317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1318(int& val) {
    const int x = 1318;
    std::cout << 1318;
    for (int i = 0; i < 1318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1319(int& val) {
    const int x = 1319;
    std::cout << 1319;
    for (int i = 0; i < 1319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1320(int& val) {
    const int x = 1320;
    std::cout << 1320;
    for (int i = 0; i < 1320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1321(int& val) {
    const int x = 1321;
    std::cout << 1321;
    for (int i = 0; i < 1321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1322(int& val) {
    const int x = 1322;
    std::cout << 1322;
    for (int i = 0; i < 1322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1323(int& val) {
    const int x = 1323;
    std::cout << 1323;
    for (int i = 0; i < 1323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1324(int& val) {
    const int x = 1324;
    std::cout << 1324;
    for (int i = 0; i < 1324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1325(int& val) {
    const int x = 1325;
    std::cout << 1325;
    for (int i = 0; i < 1325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1326(int& val) {
    const int x = 1326;
    std::cout << 1326;
    for (int i = 0; i < 1326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1327(int& val) {
    const int x = 1327;
    std::cout << 1327;
    for (int i = 0; i < 1327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1328(int& val) {
    const int x = 1328;
    std::cout << 1328;
    for (int i = 0; i < 1328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1329(int& val) {
    const int x = 1329;
    std::cout << 1329;
    for (int i = 0; i < 1329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1330(int& val) {
    const int x = 1330;
    std::cout << 1330;
    for (int i = 0; i < 1330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1331(int& val) {
    const int x = 1331;
    std::cout << 1331;
    for (int i = 0; i < 1331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1332(int& val) {
    const int x = 1332;
    std::cout << 1332;
    for (int i = 0; i < 1332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1333(int& val) {
    const int x = 1333;
    std::cout << 1333;
    for (int i = 0; i < 1333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1334(int& val) {
    const int x = 1334;
    std::cout << 1334;
    for (int i = 0; i < 1334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1335(int& val) {
    const int x = 1335;
    std::cout << 1335;
    for (int i = 0; i < 1335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1336(int& val) {
    const int x = 1336;
    std::cout << 1336;
    for (int i = 0; i < 1336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1337(int& val) {
    const int x = 1337;
    std::cout << 1337;
    for (int i = 0; i < 1337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1338(int& val) {
    const int x = 1338;
    std::cout << 1338;
    for (int i = 0; i < 1338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1339(int& val) {
    const int x = 1339;
    std::cout << 1339;
    for (int i = 0; i < 1339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1340(int& val) {
    const int x = 1340;
    std::cout << 1340;
    for (int i = 0; i < 1340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1341(int& val) {
    const int x = 1341;
    std::cout << 1341;
    for (int i = 0; i < 1341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1342(int& val) {
    const int x = 1342;
    std::cout << 1342;
    for (int i = 0; i < 1342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1343(int& val) {
    const int x = 1343;
    std::cout << 1343;
    for (int i = 0; i < 1343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1344(int& val) {
    const int x = 1344;
    std::cout << 1344;
    for (int i = 0; i < 1344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1345(int& val) {
    const int x = 1345;
    std::cout << 1345;
    for (int i = 0; i < 1345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1346(int& val) {
    const int x = 1346;
    std::cout << 1346;
    for (int i = 0; i < 1346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1347(int& val) {
    const int x = 1347;
    std::cout << 1347;
    for (int i = 0; i < 1347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1348(int& val) {
    const int x = 1348;
    std::cout << 1348;
    for (int i = 0; i < 1348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1349(int& val) {
    const int x = 1349;
    std::cout << 1349;
    for (int i = 0; i < 1349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1350(int& val) {
    const int x = 1350;
    std::cout << 1350;
    for (int i = 0; i < 1350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1351(int& val) {
    const int x = 1351;
    std::cout << 1351;
    for (int i = 0; i < 1351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1352(int& val) {
    const int x = 1352;
    std::cout << 1352;
    for (int i = 0; i < 1352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1353(int& val) {
    const int x = 1353;
    std::cout << 1353;
    for (int i = 0; i < 1353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1354(int& val) {
    const int x = 1354;
    std::cout << 1354;
    for (int i = 0; i < 1354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1355(int& val) {
    const int x = 1355;
    std::cout << 1355;
    for (int i = 0; i < 1355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1356(int& val) {
    const int x = 1356;
    std::cout << 1356;
    for (int i = 0; i < 1356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1357(int& val) {
    const int x = 1357;
    std::cout << 1357;
    for (int i = 0; i < 1357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1358(int& val) {
    const int x = 1358;
    std::cout << 1358;
    for (int i = 0; i < 1358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1359(int& val) {
    const int x = 1359;
    std::cout << 1359;
    for (int i = 0; i < 1359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1360(int& val) {
    const int x = 1360;
    std::cout << 1360;
    for (int i = 0; i < 1360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1361(int& val) {
    const int x = 1361;
    std::cout << 1361;
    for (int i = 0; i < 1361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1362(int& val) {
    const int x = 1362;
    std::cout << 1362;
    for (int i = 0; i < 1362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1363(int& val) {
    const int x = 1363;
    std::cout << 1363;
    for (int i = 0; i < 1363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1364(int& val) {
    const int x = 1364;
    std::cout << 1364;
    for (int i = 0; i < 1364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1365(int& val) {
    const int x = 1365;
    std::cout << 1365;
    for (int i = 0; i < 1365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1366(int& val) {
    const int x = 1366;
    std::cout << 1366;
    for (int i = 0; i < 1366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1367(int& val) {
    const int x = 1367;
    std::cout << 1367;
    for (int i = 0; i < 1367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1368(int& val) {
    const int x = 1368;
    std::cout << 1368;
    for (int i = 0; i < 1368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1369(int& val) {
    const int x = 1369;
    std::cout << 1369;
    for (int i = 0; i < 1369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1370(int& val) {
    const int x = 1370;
    std::cout << 1370;
    for (int i = 0; i < 1370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1371(int& val) {
    const int x = 1371;
    std::cout << 1371;
    for (int i = 0; i < 1371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1372(int& val) {
    const int x = 1372;
    std::cout << 1372;
    for (int i = 0; i < 1372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1373(int& val) {
    const int x = 1373;
    std::cout << 1373;
    for (int i = 0; i < 1373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1374(int& val) {
    const int x = 1374;
    std::cout << 1374;
    for (int i = 0; i < 1374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1375(int& val) {
    const int x = 1375;
    std::cout << 1375;
    for (int i = 0; i < 1375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1376(int& val) {
    const int x = 1376;
    std::cout << 1376;
    for (int i = 0; i < 1376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1377(int& val) {
    const int x = 1377;
    std::cout << 1377;
    for (int i = 0; i < 1377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1378(int& val) {
    const int x = 1378;
    std::cout << 1378;
    for (int i = 0; i < 1378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1379(int& val) {
    const int x = 1379;
    std::cout << 1379;
    for (int i = 0; i < 1379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1380(int& val) {
    const int x = 1380;
    std::cout << 1380;
    for (int i = 0; i < 1380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1381(int& val) {
    const int x = 1381;
    std::cout << 1381;
    for (int i = 0; i < 1381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1382(int& val) {
    const int x = 1382;
    std::cout << 1382;
    for (int i = 0; i < 1382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1383(int& val) {
    const int x = 1383;
    std::cout << 1383;
    for (int i = 0; i < 1383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1384(int& val) {
    const int x = 1384;
    std::cout << 1384;
    for (int i = 0; i < 1384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1385(int& val) {
    const int x = 1385;
    std::cout << 1385;
    for (int i = 0; i < 1385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1386(int& val) {
    const int x = 1386;
    std::cout << 1386;
    for (int i = 0; i < 1386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1387(int& val) {
    const int x = 1387;
    std::cout << 1387;
    for (int i = 0; i < 1387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1388(int& val) {
    const int x = 1388;
    std::cout << 1388;
    for (int i = 0; i < 1388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1389(int& val) {
    const int x = 1389;
    std::cout << 1389;
    for (int i = 0; i < 1389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1390(int& val) {
    const int x = 1390;
    std::cout << 1390;
    for (int i = 0; i < 1390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1391(int& val) {
    const int x = 1391;
    std::cout << 1391;
    for (int i = 0; i < 1391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1392(int& val) {
    const int x = 1392;
    std::cout << 1392;
    for (int i = 0; i < 1392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1393(int& val) {
    const int x = 1393;
    std::cout << 1393;
    for (int i = 0; i < 1393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1394(int& val) {
    const int x = 1394;
    std::cout << 1394;
    for (int i = 0; i < 1394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1395(int& val) {
    const int x = 1395;
    std::cout << 1395;
    for (int i = 0; i < 1395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1396(int& val) {
    const int x = 1396;
    std::cout << 1396;
    for (int i = 0; i < 1396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1397(int& val) {
    const int x = 1397;
    std::cout << 1397;
    for (int i = 0; i < 1397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1398(int& val) {
    const int x = 1398;
    std::cout << 1398;
    for (int i = 0; i < 1398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1399(int& val) {
    const int x = 1399;
    std::cout << 1399;
    for (int i = 0; i < 1399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1400(int& val) {
    const int x = 1400;
    std::cout << 1400;
    for (int i = 0; i < 1400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1401(int& val) {
    const int x = 1401;
    std::cout << 1401;
    for (int i = 0; i < 1401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1402(int& val) {
    const int x = 1402;
    std::cout << 1402;
    for (int i = 0; i < 1402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1403(int& val) {
    const int x = 1403;
    std::cout << 1403;
    for (int i = 0; i < 1403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1404(int& val) {
    const int x = 1404;
    std::cout << 1404;
    for (int i = 0; i < 1404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1405(int& val) {
    const int x = 1405;
    std::cout << 1405;
    for (int i = 0; i < 1405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1406(int& val) {
    const int x = 1406;
    std::cout << 1406;
    for (int i = 0; i < 1406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1407(int& val) {
    const int x = 1407;
    std::cout << 1407;
    for (int i = 0; i < 1407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1408(int& val) {
    const int x = 1408;
    std::cout << 1408;
    for (int i = 0; i < 1408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1409(int& val) {
    const int x = 1409;
    std::cout << 1409;
    for (int i = 0; i < 1409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1410(int& val) {
    const int x = 1410;
    std::cout << 1410;
    for (int i = 0; i < 1410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1411(int& val) {
    const int x = 1411;
    std::cout << 1411;
    for (int i = 0; i < 1411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1412(int& val) {
    const int x = 1412;
    std::cout << 1412;
    for (int i = 0; i < 1412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1413(int& val) {
    const int x = 1413;
    std::cout << 1413;
    for (int i = 0; i < 1413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1414(int& val) {
    const int x = 1414;
    std::cout << 1414;
    for (int i = 0; i < 1414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1415(int& val) {
    const int x = 1415;
    std::cout << 1415;
    for (int i = 0; i < 1415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1416(int& val) {
    const int x = 1416;
    std::cout << 1416;
    for (int i = 0; i < 1416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1417(int& val) {
    const int x = 1417;
    std::cout << 1417;
    for (int i = 0; i < 1417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1418(int& val) {
    const int x = 1418;
    std::cout << 1418;
    for (int i = 0; i < 1418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1419(int& val) {
    const int x = 1419;
    std::cout << 1419;
    for (int i = 0; i < 1419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1420(int& val) {
    const int x = 1420;
    std::cout << 1420;
    for (int i = 0; i < 1420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1421(int& val) {
    const int x = 1421;
    std::cout << 1421;
    for (int i = 0; i < 1421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1422(int& val) {
    const int x = 1422;
    std::cout << 1422;
    for (int i = 0; i < 1422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1423(int& val) {
    const int x = 1423;
    std::cout << 1423;
    for (int i = 0; i < 1423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1424(int& val) {
    const int x = 1424;
    std::cout << 1424;
    for (int i = 0; i < 1424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1425(int& val) {
    const int x = 1425;
    std::cout << 1425;
    for (int i = 0; i < 1425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1426(int& val) {
    const int x = 1426;
    std::cout << 1426;
    for (int i = 0; i < 1426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1427(int& val) {
    const int x = 1427;
    std::cout << 1427;
    for (int i = 0; i < 1427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1428(int& val) {
    const int x = 1428;
    std::cout << 1428;
    for (int i = 0; i < 1428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1429(int& val) {
    const int x = 1429;
    std::cout << 1429;
    for (int i = 0; i < 1429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1430(int& val) {
    const int x = 1430;
    std::cout << 1430;
    for (int i = 0; i < 1430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1431(int& val) {
    const int x = 1431;
    std::cout << 1431;
    for (int i = 0; i < 1431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1432(int& val) {
    const int x = 1432;
    std::cout << 1432;
    for (int i = 0; i < 1432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1433(int& val) {
    const int x = 1433;
    std::cout << 1433;
    for (int i = 0; i < 1433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1434(int& val) {
    const int x = 1434;
    std::cout << 1434;
    for (int i = 0; i < 1434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1435(int& val) {
    const int x = 1435;
    std::cout << 1435;
    for (int i = 0; i < 1435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1436(int& val) {
    const int x = 1436;
    std::cout << 1436;
    for (int i = 0; i < 1436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1437(int& val) {
    const int x = 1437;
    std::cout << 1437;
    for (int i = 0; i < 1437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1438(int& val) {
    const int x = 1438;
    std::cout << 1438;
    for (int i = 0; i < 1438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1439(int& val) {
    const int x = 1439;
    std::cout << 1439;
    for (int i = 0; i < 1439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1440(int& val) {
    const int x = 1440;
    std::cout << 1440;
    for (int i = 0; i < 1440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1441(int& val) {
    const int x = 1441;
    std::cout << 1441;
    for (int i = 0; i < 1441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1442(int& val) {
    const int x = 1442;
    std::cout << 1442;
    for (int i = 0; i < 1442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1443(int& val) {
    const int x = 1443;
    std::cout << 1443;
    for (int i = 0; i < 1443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1444(int& val) {
    const int x = 1444;
    std::cout << 1444;
    for (int i = 0; i < 1444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1445(int& val) {
    const int x = 1445;
    std::cout << 1445;
    for (int i = 0; i < 1445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1446(int& val) {
    const int x = 1446;
    std::cout << 1446;
    for (int i = 0; i < 1446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1447(int& val) {
    const int x = 1447;
    std::cout << 1447;
    for (int i = 0; i < 1447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1448(int& val) {
    const int x = 1448;
    std::cout << 1448;
    for (int i = 0; i < 1448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1449(int& val) {
    const int x = 1449;
    std::cout << 1449;
    for (int i = 0; i < 1449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1450(int& val) {
    const int x = 1450;
    std::cout << 1450;
    for (int i = 0; i < 1450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1451(int& val) {
    const int x = 1451;
    std::cout << 1451;
    for (int i = 0; i < 1451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1452(int& val) {
    const int x = 1452;
    std::cout << 1452;
    for (int i = 0; i < 1452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1453(int& val) {
    const int x = 1453;
    std::cout << 1453;
    for (int i = 0; i < 1453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1454(int& val) {
    const int x = 1454;
    std::cout << 1454;
    for (int i = 0; i < 1454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1455(int& val) {
    const int x = 1455;
    std::cout << 1455;
    for (int i = 0; i < 1455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1456(int& val) {
    const int x = 1456;
    std::cout << 1456;
    for (int i = 0; i < 1456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1457(int& val) {
    const int x = 1457;
    std::cout << 1457;
    for (int i = 0; i < 1457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1458(int& val) {
    const int x = 1458;
    std::cout << 1458;
    for (int i = 0; i < 1458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1459(int& val) {
    const int x = 1459;
    std::cout << 1459;
    for (int i = 0; i < 1459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1460(int& val) {
    const int x = 1460;
    std::cout << 1460;
    for (int i = 0; i < 1460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1461(int& val) {
    const int x = 1461;
    std::cout << 1461;
    for (int i = 0; i < 1461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1462(int& val) {
    const int x = 1462;
    std::cout << 1462;
    for (int i = 0; i < 1462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1463(int& val) {
    const int x = 1463;
    std::cout << 1463;
    for (int i = 0; i < 1463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1464(int& val) {
    const int x = 1464;
    std::cout << 1464;
    for (int i = 0; i < 1464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1465(int& val) {
    const int x = 1465;
    std::cout << 1465;
    for (int i = 0; i < 1465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1466(int& val) {
    const int x = 1466;
    std::cout << 1466;
    for (int i = 0; i < 1466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1467(int& val) {
    const int x = 1467;
    std::cout << 1467;
    for (int i = 0; i < 1467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1468(int& val) {
    const int x = 1468;
    std::cout << 1468;
    for (int i = 0; i < 1468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1469(int& val) {
    const int x = 1469;
    std::cout << 1469;
    for (int i = 0; i < 1469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1470(int& val) {
    const int x = 1470;
    std::cout << 1470;
    for (int i = 0; i < 1470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1471(int& val) {
    const int x = 1471;
    std::cout << 1471;
    for (int i = 0; i < 1471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1472(int& val) {
    const int x = 1472;
    std::cout << 1472;
    for (int i = 0; i < 1472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1473(int& val) {
    const int x = 1473;
    std::cout << 1473;
    for (int i = 0; i < 1473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1474(int& val) {
    const int x = 1474;
    std::cout << 1474;
    for (int i = 0; i < 1474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1475(int& val) {
    const int x = 1475;
    std::cout << 1475;
    for (int i = 0; i < 1475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1476(int& val) {
    const int x = 1476;
    std::cout << 1476;
    for (int i = 0; i < 1476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1477(int& val) {
    const int x = 1477;
    std::cout << 1477;
    for (int i = 0; i < 1477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1478(int& val) {
    const int x = 1478;
    std::cout << 1478;
    for (int i = 0; i < 1478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1479(int& val) {
    const int x = 1479;
    std::cout << 1479;
    for (int i = 0; i < 1479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1480(int& val) {
    const int x = 1480;
    std::cout << 1480;
    for (int i = 0; i < 1480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1481(int& val) {
    const int x = 1481;
    std::cout << 1481;
    for (int i = 0; i < 1481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1482(int& val) {
    const int x = 1482;
    std::cout << 1482;
    for (int i = 0; i < 1482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1483(int& val) {
    const int x = 1483;
    std::cout << 1483;
    for (int i = 0; i < 1483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1484(int& val) {
    const int x = 1484;
    std::cout << 1484;
    for (int i = 0; i < 1484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1485(int& val) {
    const int x = 1485;
    std::cout << 1485;
    for (int i = 0; i < 1485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1486(int& val) {
    const int x = 1486;
    std::cout << 1486;
    for (int i = 0; i < 1486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1487(int& val) {
    const int x = 1487;
    std::cout << 1487;
    for (int i = 0; i < 1487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1488(int& val) {
    const int x = 1488;
    std::cout << 1488;
    for (int i = 0; i < 1488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1489(int& val) {
    const int x = 1489;
    std::cout << 1489;
    for (int i = 0; i < 1489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1490(int& val) {
    const int x = 1490;
    std::cout << 1490;
    for (int i = 0; i < 1490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1491(int& val) {
    const int x = 1491;
    std::cout << 1491;
    for (int i = 0; i < 1491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1492(int& val) {
    const int x = 1492;
    std::cout << 1492;
    for (int i = 0; i < 1492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1493(int& val) {
    const int x = 1493;
    std::cout << 1493;
    for (int i = 0; i < 1493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1494(int& val) {
    const int x = 1494;
    std::cout << 1494;
    for (int i = 0; i < 1494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1495(int& val) {
    const int x = 1495;
    std::cout << 1495;
    for (int i = 0; i < 1495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1496(int& val) {
    const int x = 1496;
    std::cout << 1496;
    for (int i = 0; i < 1496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1497(int& val) {
    const int x = 1497;
    std::cout << 1497;
    for (int i = 0; i < 1497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1498(int& val) {
    const int x = 1498;
    std::cout << 1498;
    for (int i = 0; i < 1498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1499(int& val) {
    const int x = 1499;
    std::cout << 1499;
    for (int i = 0; i < 1499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1500(int& val) {
    const int x = 1500;
    std::cout << 1500;
    for (int i = 0; i < 1500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1501(int& val) {
    const int x = 1501;
    std::cout << 1501;
    for (int i = 0; i < 1501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1502(int& val) {
    const int x = 1502;
    std::cout << 1502;
    for (int i = 0; i < 1502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1503(int& val) {
    const int x = 1503;
    std::cout << 1503;
    for (int i = 0; i < 1503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1504(int& val) {
    const int x = 1504;
    std::cout << 1504;
    for (int i = 0; i < 1504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1505(int& val) {
    const int x = 1505;
    std::cout << 1505;
    for (int i = 0; i < 1505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1506(int& val) {
    const int x = 1506;
    std::cout << 1506;
    for (int i = 0; i < 1506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1507(int& val) {
    const int x = 1507;
    std::cout << 1507;
    for (int i = 0; i < 1507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1508(int& val) {
    const int x = 1508;
    std::cout << 1508;
    for (int i = 0; i < 1508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1509(int& val) {
    const int x = 1509;
    std::cout << 1509;
    for (int i = 0; i < 1509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1510(int& val) {
    const int x = 1510;
    std::cout << 1510;
    for (int i = 0; i < 1510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1511(int& val) {
    const int x = 1511;
    std::cout << 1511;
    for (int i = 0; i < 1511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1512(int& val) {
    const int x = 1512;
    std::cout << 1512;
    for (int i = 0; i < 1512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1513(int& val) {
    const int x = 1513;
    std::cout << 1513;
    for (int i = 0; i < 1513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1514(int& val) {
    const int x = 1514;
    std::cout << 1514;
    for (int i = 0; i < 1514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1515(int& val) {
    const int x = 1515;
    std::cout << 1515;
    for (int i = 0; i < 1515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1516(int& val) {
    const int x = 1516;
    std::cout << 1516;
    for (int i = 0; i < 1516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1517(int& val) {
    const int x = 1517;
    std::cout << 1517;
    for (int i = 0; i < 1517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1518(int& val) {
    const int x = 1518;
    std::cout << 1518;
    for (int i = 0; i < 1518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1519(int& val) {
    const int x = 1519;
    std::cout << 1519;
    for (int i = 0; i < 1519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1520(int& val) {
    const int x = 1520;
    std::cout << 1520;
    for (int i = 0; i < 1520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1521(int& val) {
    const int x = 1521;
    std::cout << 1521;
    for (int i = 0; i < 1521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1522(int& val) {
    const int x = 1522;
    std::cout << 1522;
    for (int i = 0; i < 1522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1523(int& val) {
    const int x = 1523;
    std::cout << 1523;
    for (int i = 0; i < 1523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1524(int& val) {
    const int x = 1524;
    std::cout << 1524;
    for (int i = 0; i < 1524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1525(int& val) {
    const int x = 1525;
    std::cout << 1525;
    for (int i = 0; i < 1525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1526(int& val) {
    const int x = 1526;
    std::cout << 1526;
    for (int i = 0; i < 1526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1527(int& val) {
    const int x = 1527;
    std::cout << 1527;
    for (int i = 0; i < 1527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1528(int& val) {
    const int x = 1528;
    std::cout << 1528;
    for (int i = 0; i < 1528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1529(int& val) {
    const int x = 1529;
    std::cout << 1529;
    for (int i = 0; i < 1529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1530(int& val) {
    const int x = 1530;
    std::cout << 1530;
    for (int i = 0; i < 1530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1531(int& val) {
    const int x = 1531;
    std::cout << 1531;
    for (int i = 0; i < 1531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1532(int& val) {
    const int x = 1532;
    std::cout << 1532;
    for (int i = 0; i < 1532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1533(int& val) {
    const int x = 1533;
    std::cout << 1533;
    for (int i = 0; i < 1533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1534(int& val) {
    const int x = 1534;
    std::cout << 1534;
    for (int i = 0; i < 1534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1535(int& val) {
    const int x = 1535;
    std::cout << 1535;
    for (int i = 0; i < 1535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1536(int& val) {
    const int x = 1536;
    std::cout << 1536;
    for (int i = 0; i < 1536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1537(int& val) {
    const int x = 1537;
    std::cout << 1537;
    for (int i = 0; i < 1537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1538(int& val) {
    const int x = 1538;
    std::cout << 1538;
    for (int i = 0; i < 1538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1539(int& val) {
    const int x = 1539;
    std::cout << 1539;
    for (int i = 0; i < 1539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1540(int& val) {
    const int x = 1540;
    std::cout << 1540;
    for (int i = 0; i < 1540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1541(int& val) {
    const int x = 1541;
    std::cout << 1541;
    for (int i = 0; i < 1541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1542(int& val) {
    const int x = 1542;
    std::cout << 1542;
    for (int i = 0; i < 1542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1543(int& val) {
    const int x = 1543;
    std::cout << 1543;
    for (int i = 0; i < 1543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1544(int& val) {
    const int x = 1544;
    std::cout << 1544;
    for (int i = 0; i < 1544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1545(int& val) {
    const int x = 1545;
    std::cout << 1545;
    for (int i = 0; i < 1545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1546(int& val) {
    const int x = 1546;
    std::cout << 1546;
    for (int i = 0; i < 1546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1547(int& val) {
    const int x = 1547;
    std::cout << 1547;
    for (int i = 0; i < 1547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1548(int& val) {
    const int x = 1548;
    std::cout << 1548;
    for (int i = 0; i < 1548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1549(int& val) {
    const int x = 1549;
    std::cout << 1549;
    for (int i = 0; i < 1549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1550(int& val) {
    const int x = 1550;
    std::cout << 1550;
    for (int i = 0; i < 1550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1551(int& val) {
    const int x = 1551;
    std::cout << 1551;
    for (int i = 0; i < 1551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1552(int& val) {
    const int x = 1552;
    std::cout << 1552;
    for (int i = 0; i < 1552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1553(int& val) {
    const int x = 1553;
    std::cout << 1553;
    for (int i = 0; i < 1553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1554(int& val) {
    const int x = 1554;
    std::cout << 1554;
    for (int i = 0; i < 1554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1555(int& val) {
    const int x = 1555;
    std::cout << 1555;
    for (int i = 0; i < 1555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1556(int& val) {
    const int x = 1556;
    std::cout << 1556;
    for (int i = 0; i < 1556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1557(int& val) {
    const int x = 1557;
    std::cout << 1557;
    for (int i = 0; i < 1557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1558(int& val) {
    const int x = 1558;
    std::cout << 1558;
    for (int i = 0; i < 1558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1559(int& val) {
    const int x = 1559;
    std::cout << 1559;
    for (int i = 0; i < 1559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1560(int& val) {
    const int x = 1560;
    std::cout << 1560;
    for (int i = 0; i < 1560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1561(int& val) {
    const int x = 1561;
    std::cout << 1561;
    for (int i = 0; i < 1561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1562(int& val) {
    const int x = 1562;
    std::cout << 1562;
    for (int i = 0; i < 1562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1563(int& val) {
    const int x = 1563;
    std::cout << 1563;
    for (int i = 0; i < 1563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1564(int& val) {
    const int x = 1564;
    std::cout << 1564;
    for (int i = 0; i < 1564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1565(int& val) {
    const int x = 1565;
    std::cout << 1565;
    for (int i = 0; i < 1565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1566(int& val) {
    const int x = 1566;
    std::cout << 1566;
    for (int i = 0; i < 1566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1567(int& val) {
    const int x = 1567;
    std::cout << 1567;
    for (int i = 0; i < 1567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1568(int& val) {
    const int x = 1568;
    std::cout << 1568;
    for (int i = 0; i < 1568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1569(int& val) {
    const int x = 1569;
    std::cout << 1569;
    for (int i = 0; i < 1569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1570(int& val) {
    const int x = 1570;
    std::cout << 1570;
    for (int i = 0; i < 1570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1571(int& val) {
    const int x = 1571;
    std::cout << 1571;
    for (int i = 0; i < 1571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1572(int& val) {
    const int x = 1572;
    std::cout << 1572;
    for (int i = 0; i < 1572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1573(int& val) {
    const int x = 1573;
    std::cout << 1573;
    for (int i = 0; i < 1573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1574(int& val) {
    const int x = 1574;
    std::cout << 1574;
    for (int i = 0; i < 1574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1575(int& val) {
    const int x = 1575;
    std::cout << 1575;
    for (int i = 0; i < 1575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1576(int& val) {
    const int x = 1576;
    std::cout << 1576;
    for (int i = 0; i < 1576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1577(int& val) {
    const int x = 1577;
    std::cout << 1577;
    for (int i = 0; i < 1577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1578(int& val) {
    const int x = 1578;
    std::cout << 1578;
    for (int i = 0; i < 1578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1579(int& val) {
    const int x = 1579;
    std::cout << 1579;
    for (int i = 0; i < 1579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1580(int& val) {
    const int x = 1580;
    std::cout << 1580;
    for (int i = 0; i < 1580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1581(int& val) {
    const int x = 1581;
    std::cout << 1581;
    for (int i = 0; i < 1581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1582(int& val) {
    const int x = 1582;
    std::cout << 1582;
    for (int i = 0; i < 1582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1583(int& val) {
    const int x = 1583;
    std::cout << 1583;
    for (int i = 0; i < 1583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1584(int& val) {
    const int x = 1584;
    std::cout << 1584;
    for (int i = 0; i < 1584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1585(int& val) {
    const int x = 1585;
    std::cout << 1585;
    for (int i = 0; i < 1585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1586(int& val) {
    const int x = 1586;
    std::cout << 1586;
    for (int i = 0; i < 1586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1587(int& val) {
    const int x = 1587;
    std::cout << 1587;
    for (int i = 0; i < 1587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1588(int& val) {
    const int x = 1588;
    std::cout << 1588;
    for (int i = 0; i < 1588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1589(int& val) {
    const int x = 1589;
    std::cout << 1589;
    for (int i = 0; i < 1589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1590(int& val) {
    const int x = 1590;
    std::cout << 1590;
    for (int i = 0; i < 1590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1591(int& val) {
    const int x = 1591;
    std::cout << 1591;
    for (int i = 0; i < 1591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1592(int& val) {
    const int x = 1592;
    std::cout << 1592;
    for (int i = 0; i < 1592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1593(int& val) {
    const int x = 1593;
    std::cout << 1593;
    for (int i = 0; i < 1593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1594(int& val) {
    const int x = 1594;
    std::cout << 1594;
    for (int i = 0; i < 1594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1595(int& val) {
    const int x = 1595;
    std::cout << 1595;
    for (int i = 0; i < 1595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1596(int& val) {
    const int x = 1596;
    std::cout << 1596;
    for (int i = 0; i < 1596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1597(int& val) {
    const int x = 1597;
    std::cout << 1597;
    for (int i = 0; i < 1597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1598(int& val) {
    const int x = 1598;
    std::cout << 1598;
    for (int i = 0; i < 1598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1599(int& val) {
    const int x = 1599;
    std::cout << 1599;
    for (int i = 0; i < 1599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1600(int& val) {
    const int x = 1600;
    std::cout << 1600;
    for (int i = 0; i < 1600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1601(int& val) {
    const int x = 1601;
    std::cout << 1601;
    for (int i = 0; i < 1601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1602(int& val) {
    const int x = 1602;
    std::cout << 1602;
    for (int i = 0; i < 1602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1603(int& val) {
    const int x = 1603;
    std::cout << 1603;
    for (int i = 0; i < 1603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1604(int& val) {
    const int x = 1604;
    std::cout << 1604;
    for (int i = 0; i < 1604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1605(int& val) {
    const int x = 1605;
    std::cout << 1605;
    for (int i = 0; i < 1605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1606(int& val) {
    const int x = 1606;
    std::cout << 1606;
    for (int i = 0; i < 1606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1607(int& val) {
    const int x = 1607;
    std::cout << 1607;
    for (int i = 0; i < 1607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1608(int& val) {
    const int x = 1608;
    std::cout << 1608;
    for (int i = 0; i < 1608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1609(int& val) {
    const int x = 1609;
    std::cout << 1609;
    for (int i = 0; i < 1609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1610(int& val) {
    const int x = 1610;
    std::cout << 1610;
    for (int i = 0; i < 1610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1611(int& val) {
    const int x = 1611;
    std::cout << 1611;
    for (int i = 0; i < 1611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1612(int& val) {
    const int x = 1612;
    std::cout << 1612;
    for (int i = 0; i < 1612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1613(int& val) {
    const int x = 1613;
    std::cout << 1613;
    for (int i = 0; i < 1613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1614(int& val) {
    const int x = 1614;
    std::cout << 1614;
    for (int i = 0; i < 1614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1615(int& val) {
    const int x = 1615;
    std::cout << 1615;
    for (int i = 0; i < 1615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1616(int& val) {
    const int x = 1616;
    std::cout << 1616;
    for (int i = 0; i < 1616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1617(int& val) {
    const int x = 1617;
    std::cout << 1617;
    for (int i = 0; i < 1617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1618(int& val) {
    const int x = 1618;
    std::cout << 1618;
    for (int i = 0; i < 1618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1619(int& val) {
    const int x = 1619;
    std::cout << 1619;
    for (int i = 0; i < 1619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1620(int& val) {
    const int x = 1620;
    std::cout << 1620;
    for (int i = 0; i < 1620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1621(int& val) {
    const int x = 1621;
    std::cout << 1621;
    for (int i = 0; i < 1621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1622(int& val) {
    const int x = 1622;
    std::cout << 1622;
    for (int i = 0; i < 1622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1623(int& val) {
    const int x = 1623;
    std::cout << 1623;
    for (int i = 0; i < 1623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1624(int& val) {
    const int x = 1624;
    std::cout << 1624;
    for (int i = 0; i < 1624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1625(int& val) {
    const int x = 1625;
    std::cout << 1625;
    for (int i = 0; i < 1625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1626(int& val) {
    const int x = 1626;
    std::cout << 1626;
    for (int i = 0; i < 1626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1627(int& val) {
    const int x = 1627;
    std::cout << 1627;
    for (int i = 0; i < 1627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1628(int& val) {
    const int x = 1628;
    std::cout << 1628;
    for (int i = 0; i < 1628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1629(int& val) {
    const int x = 1629;
    std::cout << 1629;
    for (int i = 0; i < 1629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1630(int& val) {
    const int x = 1630;
    std::cout << 1630;
    for (int i = 0; i < 1630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1631(int& val) {
    const int x = 1631;
    std::cout << 1631;
    for (int i = 0; i < 1631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1632(int& val) {
    const int x = 1632;
    std::cout << 1632;
    for (int i = 0; i < 1632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1633(int& val) {
    const int x = 1633;
    std::cout << 1633;
    for (int i = 0; i < 1633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1634(int& val) {
    const int x = 1634;
    std::cout << 1634;
    for (int i = 0; i < 1634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1635(int& val) {
    const int x = 1635;
    std::cout << 1635;
    for (int i = 0; i < 1635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1636(int& val) {
    const int x = 1636;
    std::cout << 1636;
    for (int i = 0; i < 1636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1637(int& val) {
    const int x = 1637;
    std::cout << 1637;
    for (int i = 0; i < 1637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1638(int& val) {
    const int x = 1638;
    std::cout << 1638;
    for (int i = 0; i < 1638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1639(int& val) {
    const int x = 1639;
    std::cout << 1639;
    for (int i = 0; i < 1639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1640(int& val) {
    const int x = 1640;
    std::cout << 1640;
    for (int i = 0; i < 1640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1641(int& val) {
    const int x = 1641;
    std::cout << 1641;
    for (int i = 0; i < 1641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1642(int& val) {
    const int x = 1642;
    std::cout << 1642;
    for (int i = 0; i < 1642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1643(int& val) {
    const int x = 1643;
    std::cout << 1643;
    for (int i = 0; i < 1643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1644(int& val) {
    const int x = 1644;
    std::cout << 1644;
    for (int i = 0; i < 1644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1645(int& val) {
    const int x = 1645;
    std::cout << 1645;
    for (int i = 0; i < 1645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1646(int& val) {
    const int x = 1646;
    std::cout << 1646;
    for (int i = 0; i < 1646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1647(int& val) {
    const int x = 1647;
    std::cout << 1647;
    for (int i = 0; i < 1647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1648(int& val) {
    const int x = 1648;
    std::cout << 1648;
    for (int i = 0; i < 1648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1649(int& val) {
    const int x = 1649;
    std::cout << 1649;
    for (int i = 0; i < 1649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1650(int& val) {
    const int x = 1650;
    std::cout << 1650;
    for (int i = 0; i < 1650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1651(int& val) {
    const int x = 1651;
    std::cout << 1651;
    for (int i = 0; i < 1651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1652(int& val) {
    const int x = 1652;
    std::cout << 1652;
    for (int i = 0; i < 1652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1653(int& val) {
    const int x = 1653;
    std::cout << 1653;
    for (int i = 0; i < 1653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1654(int& val) {
    const int x = 1654;
    std::cout << 1654;
    for (int i = 0; i < 1654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1655(int& val) {
    const int x = 1655;
    std::cout << 1655;
    for (int i = 0; i < 1655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1656(int& val) {
    const int x = 1656;
    std::cout << 1656;
    for (int i = 0; i < 1656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1657(int& val) {
    const int x = 1657;
    std::cout << 1657;
    for (int i = 0; i < 1657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1658(int& val) {
    const int x = 1658;
    std::cout << 1658;
    for (int i = 0; i < 1658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1659(int& val) {
    const int x = 1659;
    std::cout << 1659;
    for (int i = 0; i < 1659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1660(int& val) {
    const int x = 1660;
    std::cout << 1660;
    for (int i = 0; i < 1660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1661(int& val) {
    const int x = 1661;
    std::cout << 1661;
    for (int i = 0; i < 1661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1662(int& val) {
    const int x = 1662;
    std::cout << 1662;
    for (int i = 0; i < 1662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1663(int& val) {
    const int x = 1663;
    std::cout << 1663;
    for (int i = 0; i < 1663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1664(int& val) {
    const int x = 1664;
    std::cout << 1664;
    for (int i = 0; i < 1664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1665(int& val) {
    const int x = 1665;
    std::cout << 1665;
    for (int i = 0; i < 1665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1666(int& val) {
    const int x = 1666;
    std::cout << 1666;
    for (int i = 0; i < 1666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1667(int& val) {
    const int x = 1667;
    std::cout << 1667;
    for (int i = 0; i < 1667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1668(int& val) {
    const int x = 1668;
    std::cout << 1668;
    for (int i = 0; i < 1668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1669(int& val) {
    const int x = 1669;
    std::cout << 1669;
    for (int i = 0; i < 1669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1670(int& val) {
    const int x = 1670;
    std::cout << 1670;
    for (int i = 0; i < 1670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1671(int& val) {
    const int x = 1671;
    std::cout << 1671;
    for (int i = 0; i < 1671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1672(int& val) {
    const int x = 1672;
    std::cout << 1672;
    for (int i = 0; i < 1672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1673(int& val) {
    const int x = 1673;
    std::cout << 1673;
    for (int i = 0; i < 1673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1674(int& val) {
    const int x = 1674;
    std::cout << 1674;
    for (int i = 0; i < 1674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1675(int& val) {
    const int x = 1675;
    std::cout << 1675;
    for (int i = 0; i < 1675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1676(int& val) {
    const int x = 1676;
    std::cout << 1676;
    for (int i = 0; i < 1676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1677(int& val) {
    const int x = 1677;
    std::cout << 1677;
    for (int i = 0; i < 1677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1678(int& val) {
    const int x = 1678;
    std::cout << 1678;
    for (int i = 0; i < 1678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1679(int& val) {
    const int x = 1679;
    std::cout << 1679;
    for (int i = 0; i < 1679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1680(int& val) {
    const int x = 1680;
    std::cout << 1680;
    for (int i = 0; i < 1680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1681(int& val) {
    const int x = 1681;
    std::cout << 1681;
    for (int i = 0; i < 1681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1682(int& val) {
    const int x = 1682;
    std::cout << 1682;
    for (int i = 0; i < 1682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1683(int& val) {
    const int x = 1683;
    std::cout << 1683;
    for (int i = 0; i < 1683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1684(int& val) {
    const int x = 1684;
    std::cout << 1684;
    for (int i = 0; i < 1684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1685(int& val) {
    const int x = 1685;
    std::cout << 1685;
    for (int i = 0; i < 1685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1686(int& val) {
    const int x = 1686;
    std::cout << 1686;
    for (int i = 0; i < 1686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1687(int& val) {
    const int x = 1687;
    std::cout << 1687;
    for (int i = 0; i < 1687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1688(int& val) {
    const int x = 1688;
    std::cout << 1688;
    for (int i = 0; i < 1688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1689(int& val) {
    const int x = 1689;
    std::cout << 1689;
    for (int i = 0; i < 1689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1690(int& val) {
    const int x = 1690;
    std::cout << 1690;
    for (int i = 0; i < 1690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1691(int& val) {
    const int x = 1691;
    std::cout << 1691;
    for (int i = 0; i < 1691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1692(int& val) {
    const int x = 1692;
    std::cout << 1692;
    for (int i = 0; i < 1692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1693(int& val) {
    const int x = 1693;
    std::cout << 1693;
    for (int i = 0; i < 1693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1694(int& val) {
    const int x = 1694;
    std::cout << 1694;
    for (int i = 0; i < 1694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1695(int& val) {
    const int x = 1695;
    std::cout << 1695;
    for (int i = 0; i < 1695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1696(int& val) {
    const int x = 1696;
    std::cout << 1696;
    for (int i = 0; i < 1696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1697(int& val) {
    const int x = 1697;
    std::cout << 1697;
    for (int i = 0; i < 1697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1698(int& val) {
    const int x = 1698;
    std::cout << 1698;
    for (int i = 0; i < 1698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1699(int& val) {
    const int x = 1699;
    std::cout << 1699;
    for (int i = 0; i < 1699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1700(int& val) {
    const int x = 1700;
    std::cout << 1700;
    for (int i = 0; i < 1700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1701(int& val) {
    const int x = 1701;
    std::cout << 1701;
    for (int i = 0; i < 1701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1702(int& val) {
    const int x = 1702;
    std::cout << 1702;
    for (int i = 0; i < 1702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1703(int& val) {
    const int x = 1703;
    std::cout << 1703;
    for (int i = 0; i < 1703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1704(int& val) {
    const int x = 1704;
    std::cout << 1704;
    for (int i = 0; i < 1704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1705(int& val) {
    const int x = 1705;
    std::cout << 1705;
    for (int i = 0; i < 1705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1706(int& val) {
    const int x = 1706;
    std::cout << 1706;
    for (int i = 0; i < 1706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1707(int& val) {
    const int x = 1707;
    std::cout << 1707;
    for (int i = 0; i < 1707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1708(int& val) {
    const int x = 1708;
    std::cout << 1708;
    for (int i = 0; i < 1708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1709(int& val) {
    const int x = 1709;
    std::cout << 1709;
    for (int i = 0; i < 1709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1710(int& val) {
    const int x = 1710;
    std::cout << 1710;
    for (int i = 0; i < 1710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1711(int& val) {
    const int x = 1711;
    std::cout << 1711;
    for (int i = 0; i < 1711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1712(int& val) {
    const int x = 1712;
    std::cout << 1712;
    for (int i = 0; i < 1712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1713(int& val) {
    const int x = 1713;
    std::cout << 1713;
    for (int i = 0; i < 1713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1714(int& val) {
    const int x = 1714;
    std::cout << 1714;
    for (int i = 0; i < 1714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1715(int& val) {
    const int x = 1715;
    std::cout << 1715;
    for (int i = 0; i < 1715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1716(int& val) {
    const int x = 1716;
    std::cout << 1716;
    for (int i = 0; i < 1716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1717(int& val) {
    const int x = 1717;
    std::cout << 1717;
    for (int i = 0; i < 1717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1718(int& val) {
    const int x = 1718;
    std::cout << 1718;
    for (int i = 0; i < 1718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1719(int& val) {
    const int x = 1719;
    std::cout << 1719;
    for (int i = 0; i < 1719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1720(int& val) {
    const int x = 1720;
    std::cout << 1720;
    for (int i = 0; i < 1720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1721(int& val) {
    const int x = 1721;
    std::cout << 1721;
    for (int i = 0; i < 1721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1722(int& val) {
    const int x = 1722;
    std::cout << 1722;
    for (int i = 0; i < 1722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1723(int& val) {
    const int x = 1723;
    std::cout << 1723;
    for (int i = 0; i < 1723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1724(int& val) {
    const int x = 1724;
    std::cout << 1724;
    for (int i = 0; i < 1724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1725(int& val) {
    const int x = 1725;
    std::cout << 1725;
    for (int i = 0; i < 1725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1726(int& val) {
    const int x = 1726;
    std::cout << 1726;
    for (int i = 0; i < 1726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1727(int& val) {
    const int x = 1727;
    std::cout << 1727;
    for (int i = 0; i < 1727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1728(int& val) {
    const int x = 1728;
    std::cout << 1728;
    for (int i = 0; i < 1728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1729(int& val) {
    const int x = 1729;
    std::cout << 1729;
    for (int i = 0; i < 1729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1730(int& val) {
    const int x = 1730;
    std::cout << 1730;
    for (int i = 0; i < 1730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1731(int& val) {
    const int x = 1731;
    std::cout << 1731;
    for (int i = 0; i < 1731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1732(int& val) {
    const int x = 1732;
    std::cout << 1732;
    for (int i = 0; i < 1732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1733(int& val) {
    const int x = 1733;
    std::cout << 1733;
    for (int i = 0; i < 1733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1734(int& val) {
    const int x = 1734;
    std::cout << 1734;
    for (int i = 0; i < 1734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1735(int& val) {
    const int x = 1735;
    std::cout << 1735;
    for (int i = 0; i < 1735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1736(int& val) {
    const int x = 1736;
    std::cout << 1736;
    for (int i = 0; i < 1736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1737(int& val) {
    const int x = 1737;
    std::cout << 1737;
    for (int i = 0; i < 1737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1738(int& val) {
    const int x = 1738;
    std::cout << 1738;
    for (int i = 0; i < 1738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1739(int& val) {
    const int x = 1739;
    std::cout << 1739;
    for (int i = 0; i < 1739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1740(int& val) {
    const int x = 1740;
    std::cout << 1740;
    for (int i = 0; i < 1740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1741(int& val) {
    const int x = 1741;
    std::cout << 1741;
    for (int i = 0; i < 1741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1742(int& val) {
    const int x = 1742;
    std::cout << 1742;
    for (int i = 0; i < 1742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1743(int& val) {
    const int x = 1743;
    std::cout << 1743;
    for (int i = 0; i < 1743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1744(int& val) {
    const int x = 1744;
    std::cout << 1744;
    for (int i = 0; i < 1744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1745(int& val) {
    const int x = 1745;
    std::cout << 1745;
    for (int i = 0; i < 1745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1746(int& val) {
    const int x = 1746;
    std::cout << 1746;
    for (int i = 0; i < 1746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1747(int& val) {
    const int x = 1747;
    std::cout << 1747;
    for (int i = 0; i < 1747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1748(int& val) {
    const int x = 1748;
    std::cout << 1748;
    for (int i = 0; i < 1748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1749(int& val) {
    const int x = 1749;
    std::cout << 1749;
    for (int i = 0; i < 1749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1750(int& val) {
    const int x = 1750;
    std::cout << 1750;
    for (int i = 0; i < 1750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1751(int& val) {
    const int x = 1751;
    std::cout << 1751;
    for (int i = 0; i < 1751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1752(int& val) {
    const int x = 1752;
    std::cout << 1752;
    for (int i = 0; i < 1752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1753(int& val) {
    const int x = 1753;
    std::cout << 1753;
    for (int i = 0; i < 1753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1754(int& val) {
    const int x = 1754;
    std::cout << 1754;
    for (int i = 0; i < 1754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1755(int& val) {
    const int x = 1755;
    std::cout << 1755;
    for (int i = 0; i < 1755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1756(int& val) {
    const int x = 1756;
    std::cout << 1756;
    for (int i = 0; i < 1756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1757(int& val) {
    const int x = 1757;
    std::cout << 1757;
    for (int i = 0; i < 1757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1758(int& val) {
    const int x = 1758;
    std::cout << 1758;
    for (int i = 0; i < 1758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1759(int& val) {
    const int x = 1759;
    std::cout << 1759;
    for (int i = 0; i < 1759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1760(int& val) {
    const int x = 1760;
    std::cout << 1760;
    for (int i = 0; i < 1760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1761(int& val) {
    const int x = 1761;
    std::cout << 1761;
    for (int i = 0; i < 1761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1762(int& val) {
    const int x = 1762;
    std::cout << 1762;
    for (int i = 0; i < 1762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1763(int& val) {
    const int x = 1763;
    std::cout << 1763;
    for (int i = 0; i < 1763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1764(int& val) {
    const int x = 1764;
    std::cout << 1764;
    for (int i = 0; i < 1764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1765(int& val) {
    const int x = 1765;
    std::cout << 1765;
    for (int i = 0; i < 1765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1766(int& val) {
    const int x = 1766;
    std::cout << 1766;
    for (int i = 0; i < 1766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1767(int& val) {
    const int x = 1767;
    std::cout << 1767;
    for (int i = 0; i < 1767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1768(int& val) {
    const int x = 1768;
    std::cout << 1768;
    for (int i = 0; i < 1768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1769(int& val) {
    const int x = 1769;
    std::cout << 1769;
    for (int i = 0; i < 1769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1770(int& val) {
    const int x = 1770;
    std::cout << 1770;
    for (int i = 0; i < 1770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1771(int& val) {
    const int x = 1771;
    std::cout << 1771;
    for (int i = 0; i < 1771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1772(int& val) {
    const int x = 1772;
    std::cout << 1772;
    for (int i = 0; i < 1772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1773(int& val) {
    const int x = 1773;
    std::cout << 1773;
    for (int i = 0; i < 1773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1774(int& val) {
    const int x = 1774;
    std::cout << 1774;
    for (int i = 0; i < 1774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1775(int& val) {
    const int x = 1775;
    std::cout << 1775;
    for (int i = 0; i < 1775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1776(int& val) {
    const int x = 1776;
    std::cout << 1776;
    for (int i = 0; i < 1776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1777(int& val) {
    const int x = 1777;
    std::cout << 1777;
    for (int i = 0; i < 1777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1778(int& val) {
    const int x = 1778;
    std::cout << 1778;
    for (int i = 0; i < 1778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1779(int& val) {
    const int x = 1779;
    std::cout << 1779;
    for (int i = 0; i < 1779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1780(int& val) {
    const int x = 1780;
    std::cout << 1780;
    for (int i = 0; i < 1780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1781(int& val) {
    const int x = 1781;
    std::cout << 1781;
    for (int i = 0; i < 1781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1782(int& val) {
    const int x = 1782;
    std::cout << 1782;
    for (int i = 0; i < 1782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1783(int& val) {
    const int x = 1783;
    std::cout << 1783;
    for (int i = 0; i < 1783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1784(int& val) {
    const int x = 1784;
    std::cout << 1784;
    for (int i = 0; i < 1784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1785(int& val) {
    const int x = 1785;
    std::cout << 1785;
    for (int i = 0; i < 1785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1786(int& val) {
    const int x = 1786;
    std::cout << 1786;
    for (int i = 0; i < 1786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1787(int& val) {
    const int x = 1787;
    std::cout << 1787;
    for (int i = 0; i < 1787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1788(int& val) {
    const int x = 1788;
    std::cout << 1788;
    for (int i = 0; i < 1788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1789(int& val) {
    const int x = 1789;
    std::cout << 1789;
    for (int i = 0; i < 1789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1790(int& val) {
    const int x = 1790;
    std::cout << 1790;
    for (int i = 0; i < 1790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1791(int& val) {
    const int x = 1791;
    std::cout << 1791;
    for (int i = 0; i < 1791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1792(int& val) {
    const int x = 1792;
    std::cout << 1792;
    for (int i = 0; i < 1792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1793(int& val) {
    const int x = 1793;
    std::cout << 1793;
    for (int i = 0; i < 1793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1794(int& val) {
    const int x = 1794;
    std::cout << 1794;
    for (int i = 0; i < 1794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1795(int& val) {
    const int x = 1795;
    std::cout << 1795;
    for (int i = 0; i < 1795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1796(int& val) {
    const int x = 1796;
    std::cout << 1796;
    for (int i = 0; i < 1796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1797(int& val) {
    const int x = 1797;
    std::cout << 1797;
    for (int i = 0; i < 1797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1798(int& val) {
    const int x = 1798;
    std::cout << 1798;
    for (int i = 0; i < 1798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1799(int& val) {
    const int x = 1799;
    std::cout << 1799;
    for (int i = 0; i < 1799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1800(int& val) {
    const int x = 1800;
    std::cout << 1800;
    for (int i = 0; i < 1800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1801(int& val) {
    const int x = 1801;
    std::cout << 1801;
    for (int i = 0; i < 1801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1802(int& val) {
    const int x = 1802;
    std::cout << 1802;
    for (int i = 0; i < 1802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1803(int& val) {
    const int x = 1803;
    std::cout << 1803;
    for (int i = 0; i < 1803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1804(int& val) {
    const int x = 1804;
    std::cout << 1804;
    for (int i = 0; i < 1804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1805(int& val) {
    const int x = 1805;
    std::cout << 1805;
    for (int i = 0; i < 1805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1806(int& val) {
    const int x = 1806;
    std::cout << 1806;
    for (int i = 0; i < 1806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1807(int& val) {
    const int x = 1807;
    std::cout << 1807;
    for (int i = 0; i < 1807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1808(int& val) {
    const int x = 1808;
    std::cout << 1808;
    for (int i = 0; i < 1808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1809(int& val) {
    const int x = 1809;
    std::cout << 1809;
    for (int i = 0; i < 1809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1810(int& val) {
    const int x = 1810;
    std::cout << 1810;
    for (int i = 0; i < 1810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1811(int& val) {
    const int x = 1811;
    std::cout << 1811;
    for (int i = 0; i < 1811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1812(int& val) {
    const int x = 1812;
    std::cout << 1812;
    for (int i = 0; i < 1812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1813(int& val) {
    const int x = 1813;
    std::cout << 1813;
    for (int i = 0; i < 1813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1814(int& val) {
    const int x = 1814;
    std::cout << 1814;
    for (int i = 0; i < 1814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1815(int& val) {
    const int x = 1815;
    std::cout << 1815;
    for (int i = 0; i < 1815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1816(int& val) {
    const int x = 1816;
    std::cout << 1816;
    for (int i = 0; i < 1816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1817(int& val) {
    const int x = 1817;
    std::cout << 1817;
    for (int i = 0; i < 1817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1818(int& val) {
    const int x = 1818;
    std::cout << 1818;
    for (int i = 0; i < 1818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1819(int& val) {
    const int x = 1819;
    std::cout << 1819;
    for (int i = 0; i < 1819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1820(int& val) {
    const int x = 1820;
    std::cout << 1820;
    for (int i = 0; i < 1820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1821(int& val) {
    const int x = 1821;
    std::cout << 1821;
    for (int i = 0; i < 1821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1822(int& val) {
    const int x = 1822;
    std::cout << 1822;
    for (int i = 0; i < 1822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1823(int& val) {
    const int x = 1823;
    std::cout << 1823;
    for (int i = 0; i < 1823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1824(int& val) {
    const int x = 1824;
    std::cout << 1824;
    for (int i = 0; i < 1824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1825(int& val) {
    const int x = 1825;
    std::cout << 1825;
    for (int i = 0; i < 1825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1826(int& val) {
    const int x = 1826;
    std::cout << 1826;
    for (int i = 0; i < 1826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1827(int& val) {
    const int x = 1827;
    std::cout << 1827;
    for (int i = 0; i < 1827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1828(int& val) {
    const int x = 1828;
    std::cout << 1828;
    for (int i = 0; i < 1828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1829(int& val) {
    const int x = 1829;
    std::cout << 1829;
    for (int i = 0; i < 1829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1830(int& val) {
    const int x = 1830;
    std::cout << 1830;
    for (int i = 0; i < 1830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1831(int& val) {
    const int x = 1831;
    std::cout << 1831;
    for (int i = 0; i < 1831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1832(int& val) {
    const int x = 1832;
    std::cout << 1832;
    for (int i = 0; i < 1832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1833(int& val) {
    const int x = 1833;
    std::cout << 1833;
    for (int i = 0; i < 1833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1834(int& val) {
    const int x = 1834;
    std::cout << 1834;
    for (int i = 0; i < 1834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1835(int& val) {
    const int x = 1835;
    std::cout << 1835;
    for (int i = 0; i < 1835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1836(int& val) {
    const int x = 1836;
    std::cout << 1836;
    for (int i = 0; i < 1836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1837(int& val) {
    const int x = 1837;
    std::cout << 1837;
    for (int i = 0; i < 1837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1838(int& val) {
    const int x = 1838;
    std::cout << 1838;
    for (int i = 0; i < 1838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1839(int& val) {
    const int x = 1839;
    std::cout << 1839;
    for (int i = 0; i < 1839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1840(int& val) {
    const int x = 1840;
    std::cout << 1840;
    for (int i = 0; i < 1840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1841(int& val) {
    const int x = 1841;
    std::cout << 1841;
    for (int i = 0; i < 1841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1842(int& val) {
    const int x = 1842;
    std::cout << 1842;
    for (int i = 0; i < 1842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1843(int& val) {
    const int x = 1843;
    std::cout << 1843;
    for (int i = 0; i < 1843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1844(int& val) {
    const int x = 1844;
    std::cout << 1844;
    for (int i = 0; i < 1844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1845(int& val) {
    const int x = 1845;
    std::cout << 1845;
    for (int i = 0; i < 1845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1846(int& val) {
    const int x = 1846;
    std::cout << 1846;
    for (int i = 0; i < 1846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1847(int& val) {
    const int x = 1847;
    std::cout << 1847;
    for (int i = 0; i < 1847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1848(int& val) {
    const int x = 1848;
    std::cout << 1848;
    for (int i = 0; i < 1848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1849(int& val) {
    const int x = 1849;
    std::cout << 1849;
    for (int i = 0; i < 1849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1850(int& val) {
    const int x = 1850;
    std::cout << 1850;
    for (int i = 0; i < 1850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1851(int& val) {
    const int x = 1851;
    std::cout << 1851;
    for (int i = 0; i < 1851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1852(int& val) {
    const int x = 1852;
    std::cout << 1852;
    for (int i = 0; i < 1852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1853(int& val) {
    const int x = 1853;
    std::cout << 1853;
    for (int i = 0; i < 1853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1854(int& val) {
    const int x = 1854;
    std::cout << 1854;
    for (int i = 0; i < 1854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1855(int& val) {
    const int x = 1855;
    std::cout << 1855;
    for (int i = 0; i < 1855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1856(int& val) {
    const int x = 1856;
    std::cout << 1856;
    for (int i = 0; i < 1856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1857(int& val) {
    const int x = 1857;
    std::cout << 1857;
    for (int i = 0; i < 1857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1858(int& val) {
    const int x = 1858;
    std::cout << 1858;
    for (int i = 0; i < 1858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1859(int& val) {
    const int x = 1859;
    std::cout << 1859;
    for (int i = 0; i < 1859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1860(int& val) {
    const int x = 1860;
    std::cout << 1860;
    for (int i = 0; i < 1860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1861(int& val) {
    const int x = 1861;
    std::cout << 1861;
    for (int i = 0; i < 1861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1862(int& val) {
    const int x = 1862;
    std::cout << 1862;
    for (int i = 0; i < 1862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1863(int& val) {
    const int x = 1863;
    std::cout << 1863;
    for (int i = 0; i < 1863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1864(int& val) {
    const int x = 1864;
    std::cout << 1864;
    for (int i = 0; i < 1864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1865(int& val) {
    const int x = 1865;
    std::cout << 1865;
    for (int i = 0; i < 1865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1866(int& val) {
    const int x = 1866;
    std::cout << 1866;
    for (int i = 0; i < 1866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1867(int& val) {
    const int x = 1867;
    std::cout << 1867;
    for (int i = 0; i < 1867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1868(int& val) {
    const int x = 1868;
    std::cout << 1868;
    for (int i = 0; i < 1868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1869(int& val) {
    const int x = 1869;
    std::cout << 1869;
    for (int i = 0; i < 1869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1870(int& val) {
    const int x = 1870;
    std::cout << 1870;
    for (int i = 0; i < 1870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1871(int& val) {
    const int x = 1871;
    std::cout << 1871;
    for (int i = 0; i < 1871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1872(int& val) {
    const int x = 1872;
    std::cout << 1872;
    for (int i = 0; i < 1872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1873(int& val) {
    const int x = 1873;
    std::cout << 1873;
    for (int i = 0; i < 1873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1874(int& val) {
    const int x = 1874;
    std::cout << 1874;
    for (int i = 0; i < 1874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1875(int& val) {
    const int x = 1875;
    std::cout << 1875;
    for (int i = 0; i < 1875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1876(int& val) {
    const int x = 1876;
    std::cout << 1876;
    for (int i = 0; i < 1876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1877(int& val) {
    const int x = 1877;
    std::cout << 1877;
    for (int i = 0; i < 1877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1878(int& val) {
    const int x = 1878;
    std::cout << 1878;
    for (int i = 0; i < 1878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1879(int& val) {
    const int x = 1879;
    std::cout << 1879;
    for (int i = 0; i < 1879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1880(int& val) {
    const int x = 1880;
    std::cout << 1880;
    for (int i = 0; i < 1880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1881(int& val) {
    const int x = 1881;
    std::cout << 1881;
    for (int i = 0; i < 1881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1882(int& val) {
    const int x = 1882;
    std::cout << 1882;
    for (int i = 0; i < 1882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1883(int& val) {
    const int x = 1883;
    std::cout << 1883;
    for (int i = 0; i < 1883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1884(int& val) {
    const int x = 1884;
    std::cout << 1884;
    for (int i = 0; i < 1884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1885(int& val) {
    const int x = 1885;
    std::cout << 1885;
    for (int i = 0; i < 1885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1886(int& val) {
    const int x = 1886;
    std::cout << 1886;
    for (int i = 0; i < 1886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1887(int& val) {
    const int x = 1887;
    std::cout << 1887;
    for (int i = 0; i < 1887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1888(int& val) {
    const int x = 1888;
    std::cout << 1888;
    for (int i = 0; i < 1888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1889(int& val) {
    const int x = 1889;
    std::cout << 1889;
    for (int i = 0; i < 1889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1890(int& val) {
    const int x = 1890;
    std::cout << 1890;
    for (int i = 0; i < 1890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1891(int& val) {
    const int x = 1891;
    std::cout << 1891;
    for (int i = 0; i < 1891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1892(int& val) {
    const int x = 1892;
    std::cout << 1892;
    for (int i = 0; i < 1892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1893(int& val) {
    const int x = 1893;
    std::cout << 1893;
    for (int i = 0; i < 1893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1894(int& val) {
    const int x = 1894;
    std::cout << 1894;
    for (int i = 0; i < 1894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1895(int& val) {
    const int x = 1895;
    std::cout << 1895;
    for (int i = 0; i < 1895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1896(int& val) {
    const int x = 1896;
    std::cout << 1896;
    for (int i = 0; i < 1896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1897(int& val) {
    const int x = 1897;
    std::cout << 1897;
    for (int i = 0; i < 1897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1898(int& val) {
    const int x = 1898;
    std::cout << 1898;
    for (int i = 0; i < 1898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1899(int& val) {
    const int x = 1899;
    std::cout << 1899;
    for (int i = 0; i < 1899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1900(int& val) {
    const int x = 1900;
    std::cout << 1900;
    for (int i = 0; i < 1900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1901(int& val) {
    const int x = 1901;
    std::cout << 1901;
    for (int i = 0; i < 1901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1902(int& val) {
    const int x = 1902;
    std::cout << 1902;
    for (int i = 0; i < 1902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1903(int& val) {
    const int x = 1903;
    std::cout << 1903;
    for (int i = 0; i < 1903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1904(int& val) {
    const int x = 1904;
    std::cout << 1904;
    for (int i = 0; i < 1904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1905(int& val) {
    const int x = 1905;
    std::cout << 1905;
    for (int i = 0; i < 1905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1906(int& val) {
    const int x = 1906;
    std::cout << 1906;
    for (int i = 0; i < 1906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1907(int& val) {
    const int x = 1907;
    std::cout << 1907;
    for (int i = 0; i < 1907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1908(int& val) {
    const int x = 1908;
    std::cout << 1908;
    for (int i = 0; i < 1908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1909(int& val) {
    const int x = 1909;
    std::cout << 1909;
    for (int i = 0; i < 1909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1910(int& val) {
    const int x = 1910;
    std::cout << 1910;
    for (int i = 0; i < 1910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1911(int& val) {
    const int x = 1911;
    std::cout << 1911;
    for (int i = 0; i < 1911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1912(int& val) {
    const int x = 1912;
    std::cout << 1912;
    for (int i = 0; i < 1912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1913(int& val) {
    const int x = 1913;
    std::cout << 1913;
    for (int i = 0; i < 1913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1914(int& val) {
    const int x = 1914;
    std::cout << 1914;
    for (int i = 0; i < 1914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1915(int& val) {
    const int x = 1915;
    std::cout << 1915;
    for (int i = 0; i < 1915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1916(int& val) {
    const int x = 1916;
    std::cout << 1916;
    for (int i = 0; i < 1916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1917(int& val) {
    const int x = 1917;
    std::cout << 1917;
    for (int i = 0; i < 1917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1918(int& val) {
    const int x = 1918;
    std::cout << 1918;
    for (int i = 0; i < 1918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1919(int& val) {
    const int x = 1919;
    std::cout << 1919;
    for (int i = 0; i < 1919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1920(int& val) {
    const int x = 1920;
    std::cout << 1920;
    for (int i = 0; i < 1920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1921(int& val) {
    const int x = 1921;
    std::cout << 1921;
    for (int i = 0; i < 1921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1922(int& val) {
    const int x = 1922;
    std::cout << 1922;
    for (int i = 0; i < 1922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1923(int& val) {
    const int x = 1923;
    std::cout << 1923;
    for (int i = 0; i < 1923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1924(int& val) {
    const int x = 1924;
    std::cout << 1924;
    for (int i = 0; i < 1924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1925(int& val) {
    const int x = 1925;
    std::cout << 1925;
    for (int i = 0; i < 1925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1926(int& val) {
    const int x = 1926;
    std::cout << 1926;
    for (int i = 0; i < 1926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1927(int& val) {
    const int x = 1927;
    std::cout << 1927;
    for (int i = 0; i < 1927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1928(int& val) {
    const int x = 1928;
    std::cout << 1928;
    for (int i = 0; i < 1928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1929(int& val) {
    const int x = 1929;
    std::cout << 1929;
    for (int i = 0; i < 1929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1930(int& val) {
    const int x = 1930;
    std::cout << 1930;
    for (int i = 0; i < 1930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1931(int& val) {
    const int x = 1931;
    std::cout << 1931;
    for (int i = 0; i < 1931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1932(int& val) {
    const int x = 1932;
    std::cout << 1932;
    for (int i = 0; i < 1932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1933(int& val) {
    const int x = 1933;
    std::cout << 1933;
    for (int i = 0; i < 1933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1934(int& val) {
    const int x = 1934;
    std::cout << 1934;
    for (int i = 0; i < 1934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1935(int& val) {
    const int x = 1935;
    std::cout << 1935;
    for (int i = 0; i < 1935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1936(int& val) {
    const int x = 1936;
    std::cout << 1936;
    for (int i = 0; i < 1936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1937(int& val) {
    const int x = 1937;
    std::cout << 1937;
    for (int i = 0; i < 1937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1938(int& val) {
    const int x = 1938;
    std::cout << 1938;
    for (int i = 0; i < 1938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1939(int& val) {
    const int x = 1939;
    std::cout << 1939;
    for (int i = 0; i < 1939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1940(int& val) {
    const int x = 1940;
    std::cout << 1940;
    for (int i = 0; i < 1940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1941(int& val) {
    const int x = 1941;
    std::cout << 1941;
    for (int i = 0; i < 1941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1942(int& val) {
    const int x = 1942;
    std::cout << 1942;
    for (int i = 0; i < 1942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1943(int& val) {
    const int x = 1943;
    std::cout << 1943;
    for (int i = 0; i < 1943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1944(int& val) {
    const int x = 1944;
    std::cout << 1944;
    for (int i = 0; i < 1944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1945(int& val) {
    const int x = 1945;
    std::cout << 1945;
    for (int i = 0; i < 1945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1946(int& val) {
    const int x = 1946;
    std::cout << 1946;
    for (int i = 0; i < 1946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1947(int& val) {
    const int x = 1947;
    std::cout << 1947;
    for (int i = 0; i < 1947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1948(int& val) {
    const int x = 1948;
    std::cout << 1948;
    for (int i = 0; i < 1948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1949(int& val) {
    const int x = 1949;
    std::cout << 1949;
    for (int i = 0; i < 1949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1950(int& val) {
    const int x = 1950;
    std::cout << 1950;
    for (int i = 0; i < 1950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1951(int& val) {
    const int x = 1951;
    std::cout << 1951;
    for (int i = 0; i < 1951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1952(int& val) {
    const int x = 1952;
    std::cout << 1952;
    for (int i = 0; i < 1952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1953(int& val) {
    const int x = 1953;
    std::cout << 1953;
    for (int i = 0; i < 1953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1954(int& val) {
    const int x = 1954;
    std::cout << 1954;
    for (int i = 0; i < 1954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1955(int& val) {
    const int x = 1955;
    std::cout << 1955;
    for (int i = 0; i < 1955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1956(int& val) {
    const int x = 1956;
    std::cout << 1956;
    for (int i = 0; i < 1956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1957(int& val) {
    const int x = 1957;
    std::cout << 1957;
    for (int i = 0; i < 1957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1958(int& val) {
    const int x = 1958;
    std::cout << 1958;
    for (int i = 0; i < 1958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1959(int& val) {
    const int x = 1959;
    std::cout << 1959;
    for (int i = 0; i < 1959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1960(int& val) {
    const int x = 1960;
    std::cout << 1960;
    for (int i = 0; i < 1960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1961(int& val) {
    const int x = 1961;
    std::cout << 1961;
    for (int i = 0; i < 1961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1962(int& val) {
    const int x = 1962;
    std::cout << 1962;
    for (int i = 0; i < 1962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1963(int& val) {
    const int x = 1963;
    std::cout << 1963;
    for (int i = 0; i < 1963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1964(int& val) {
    const int x = 1964;
    std::cout << 1964;
    for (int i = 0; i < 1964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1965(int& val) {
    const int x = 1965;
    std::cout << 1965;
    for (int i = 0; i < 1965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1966(int& val) {
    const int x = 1966;
    std::cout << 1966;
    for (int i = 0; i < 1966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1967(int& val) {
    const int x = 1967;
    std::cout << 1967;
    for (int i = 0; i < 1967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1968(int& val) {
    const int x = 1968;
    std::cout << 1968;
    for (int i = 0; i < 1968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1969(int& val) {
    const int x = 1969;
    std::cout << 1969;
    for (int i = 0; i < 1969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1970(int& val) {
    const int x = 1970;
    std::cout << 1970;
    for (int i = 0; i < 1970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1971(int& val) {
    const int x = 1971;
    std::cout << 1971;
    for (int i = 0; i < 1971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1972(int& val) {
    const int x = 1972;
    std::cout << 1972;
    for (int i = 0; i < 1972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1973(int& val) {
    const int x = 1973;
    std::cout << 1973;
    for (int i = 0; i < 1973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1974(int& val) {
    const int x = 1974;
    std::cout << 1974;
    for (int i = 0; i < 1974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1975(int& val) {
    const int x = 1975;
    std::cout << 1975;
    for (int i = 0; i < 1975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1976(int& val) {
    const int x = 1976;
    std::cout << 1976;
    for (int i = 0; i < 1976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1977(int& val) {
    const int x = 1977;
    std::cout << 1977;
    for (int i = 0; i < 1977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1978(int& val) {
    const int x = 1978;
    std::cout << 1978;
    for (int i = 0; i < 1978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1979(int& val) {
    const int x = 1979;
    std::cout << 1979;
    for (int i = 0; i < 1979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1980(int& val) {
    const int x = 1980;
    std::cout << 1980;
    for (int i = 0; i < 1980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1981(int& val) {
    const int x = 1981;
    std::cout << 1981;
    for (int i = 0; i < 1981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1982(int& val) {
    const int x = 1982;
    std::cout << 1982;
    for (int i = 0; i < 1982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1983(int& val) {
    const int x = 1983;
    std::cout << 1983;
    for (int i = 0; i < 1983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1984(int& val) {
    const int x = 1984;
    std::cout << 1984;
    for (int i = 0; i < 1984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1985(int& val) {
    const int x = 1985;
    std::cout << 1985;
    for (int i = 0; i < 1985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1986(int& val) {
    const int x = 1986;
    std::cout << 1986;
    for (int i = 0; i < 1986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1987(int& val) {
    const int x = 1987;
    std::cout << 1987;
    for (int i = 0; i < 1987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1988(int& val) {
    const int x = 1988;
    std::cout << 1988;
    for (int i = 0; i < 1988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1989(int& val) {
    const int x = 1989;
    std::cout << 1989;
    for (int i = 0; i < 1989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1990(int& val) {
    const int x = 1990;
    std::cout << 1990;
    for (int i = 0; i < 1990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1991(int& val) {
    const int x = 1991;
    std::cout << 1991;
    for (int i = 0; i < 1991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1992(int& val) {
    const int x = 1992;
    std::cout << 1992;
    for (int i = 0; i < 1992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1993(int& val) {
    const int x = 1993;
    std::cout << 1993;
    for (int i = 0; i < 1993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1994(int& val) {
    const int x = 1994;
    std::cout << 1994;
    for (int i = 0; i < 1994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1995(int& val) {
    const int x = 1995;
    std::cout << 1995;
    for (int i = 0; i < 1995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1996(int& val) {
    const int x = 1996;
    std::cout << 1996;
    for (int i = 0; i < 1996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1997(int& val) {
    const int x = 1997;
    std::cout << 1997;
    for (int i = 0; i < 1997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1998(int& val) {
    const int x = 1998;
    std::cout << 1998;
    for (int i = 0; i < 1998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1999(int& val) {
    const int x = 1999;
    std::cout << 1999;
    for (int i = 0; i < 1999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2000(int& val) {
    const int x = 2000;
    std::cout << 2000;
    for (int i = 0; i < 2000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2001(int& val) {
    const int x = 2001;
    std::cout << 2001;
    for (int i = 0; i < 2001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2002(int& val) {
    const int x = 2002;
    std::cout << 2002;
    for (int i = 0; i < 2002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2003(int& val) {
    const int x = 2003;
    std::cout << 2003;
    for (int i = 0; i < 2003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2004(int& val) {
    const int x = 2004;
    std::cout << 2004;
    for (int i = 0; i < 2004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2005(int& val) {
    const int x = 2005;
    std::cout << 2005;
    for (int i = 0; i < 2005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2006(int& val) {
    const int x = 2006;
    std::cout << 2006;
    for (int i = 0; i < 2006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2007(int& val) {
    const int x = 2007;
    std::cout << 2007;
    for (int i = 0; i < 2007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2008(int& val) {
    const int x = 2008;
    std::cout << 2008;
    for (int i = 0; i < 2008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2009(int& val) {
    const int x = 2009;
    std::cout << 2009;
    for (int i = 0; i < 2009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2010(int& val) {
    const int x = 2010;
    std::cout << 2010;
    for (int i = 0; i < 2010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2011(int& val) {
    const int x = 2011;
    std::cout << 2011;
    for (int i = 0; i < 2011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2012(int& val) {
    const int x = 2012;
    std::cout << 2012;
    for (int i = 0; i < 2012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2013(int& val) {
    const int x = 2013;
    std::cout << 2013;
    for (int i = 0; i < 2013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2014(int& val) {
    const int x = 2014;
    std::cout << 2014;
    for (int i = 0; i < 2014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2015(int& val) {
    const int x = 2015;
    std::cout << 2015;
    for (int i = 0; i < 2015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2016(int& val) {
    const int x = 2016;
    std::cout << 2016;
    for (int i = 0; i < 2016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2017(int& val) {
    const int x = 2017;
    std::cout << 2017;
    for (int i = 0; i < 2017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2018(int& val) {
    const int x = 2018;
    std::cout << 2018;
    for (int i = 0; i < 2018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2019(int& val) {
    const int x = 2019;
    std::cout << 2019;
    for (int i = 0; i < 2019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2020(int& val) {
    const int x = 2020;
    std::cout << 2020;
    for (int i = 0; i < 2020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2021(int& val) {
    const int x = 2021;
    std::cout << 2021;
    for (int i = 0; i < 2021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2022(int& val) {
    const int x = 2022;
    std::cout << 2022;
    for (int i = 0; i < 2022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2023(int& val) {
    const int x = 2023;
    std::cout << 2023;
    for (int i = 0; i < 2023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2024(int& val) {
    const int x = 2024;
    std::cout << 2024;
    for (int i = 0; i < 2024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2025(int& val) {
    const int x = 2025;
    std::cout << 2025;
    for (int i = 0; i < 2025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2026(int& val) {
    const int x = 2026;
    std::cout << 2026;
    for (int i = 0; i < 2026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2027(int& val) {
    const int x = 2027;
    std::cout << 2027;
    for (int i = 0; i < 2027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2028(int& val) {
    const int x = 2028;
    std::cout << 2028;
    for (int i = 0; i < 2028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2029(int& val) {
    const int x = 2029;
    std::cout << 2029;
    for (int i = 0; i < 2029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2030(int& val) {
    const int x = 2030;
    std::cout << 2030;
    for (int i = 0; i < 2030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2031(int& val) {
    const int x = 2031;
    std::cout << 2031;
    for (int i = 0; i < 2031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2032(int& val) {
    const int x = 2032;
    std::cout << 2032;
    for (int i = 0; i < 2032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2033(int& val) {
    const int x = 2033;
    std::cout << 2033;
    for (int i = 0; i < 2033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2034(int& val) {
    const int x = 2034;
    std::cout << 2034;
    for (int i = 0; i < 2034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2035(int& val) {
    const int x = 2035;
    std::cout << 2035;
    for (int i = 0; i < 2035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2036(int& val) {
    const int x = 2036;
    std::cout << 2036;
    for (int i = 0; i < 2036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2037(int& val) {
    const int x = 2037;
    std::cout << 2037;
    for (int i = 0; i < 2037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2038(int& val) {
    const int x = 2038;
    std::cout << 2038;
    for (int i = 0; i < 2038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2039(int& val) {
    const int x = 2039;
    std::cout << 2039;
    for (int i = 0; i < 2039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2040(int& val) {
    const int x = 2040;
    std::cout << 2040;
    for (int i = 0; i < 2040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2041(int& val) {
    const int x = 2041;
    std::cout << 2041;
    for (int i = 0; i < 2041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2042(int& val) {
    const int x = 2042;
    std::cout << 2042;
    for (int i = 0; i < 2042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2043(int& val) {
    const int x = 2043;
    std::cout << 2043;
    for (int i = 0; i < 2043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2044(int& val) {
    const int x = 2044;
    std::cout << 2044;
    for (int i = 0; i < 2044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2045(int& val) {
    const int x = 2045;
    std::cout << 2045;
    for (int i = 0; i < 2045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2046(int& val) {
    const int x = 2046;
    std::cout << 2046;
    for (int i = 0; i < 2046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2047(int& val) {
    const int x = 2047;
    std::cout << 2047;
    for (int i = 0; i < 2047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2048(int& val) {
    const int x = 2048;
    std::cout << 2048;
    for (int i = 0; i < 2048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2049(int& val) {
    const int x = 2049;
    std::cout << 2049;
    for (int i = 0; i < 2049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2050(int& val) {
    const int x = 2050;
    std::cout << 2050;
    for (int i = 0; i < 2050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2051(int& val) {
    const int x = 2051;
    std::cout << 2051;
    for (int i = 0; i < 2051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2052(int& val) {
    const int x = 2052;
    std::cout << 2052;
    for (int i = 0; i < 2052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2053(int& val) {
    const int x = 2053;
    std::cout << 2053;
    for (int i = 0; i < 2053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2054(int& val) {
    const int x = 2054;
    std::cout << 2054;
    for (int i = 0; i < 2054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2055(int& val) {
    const int x = 2055;
    std::cout << 2055;
    for (int i = 0; i < 2055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2056(int& val) {
    const int x = 2056;
    std::cout << 2056;
    for (int i = 0; i < 2056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2057(int& val) {
    const int x = 2057;
    std::cout << 2057;
    for (int i = 0; i < 2057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2058(int& val) {
    const int x = 2058;
    std::cout << 2058;
    for (int i = 0; i < 2058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2059(int& val) {
    const int x = 2059;
    std::cout << 2059;
    for (int i = 0; i < 2059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2060(int& val) {
    const int x = 2060;
    std::cout << 2060;
    for (int i = 0; i < 2060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2061(int& val) {
    const int x = 2061;
    std::cout << 2061;
    for (int i = 0; i < 2061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2062(int& val) {
    const int x = 2062;
    std::cout << 2062;
    for (int i = 0; i < 2062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2063(int& val) {
    const int x = 2063;
    std::cout << 2063;
    for (int i = 0; i < 2063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2064(int& val) {
    const int x = 2064;
    std::cout << 2064;
    for (int i = 0; i < 2064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2065(int& val) {
    const int x = 2065;
    std::cout << 2065;
    for (int i = 0; i < 2065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2066(int& val) {
    const int x = 2066;
    std::cout << 2066;
    for (int i = 0; i < 2066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2067(int& val) {
    const int x = 2067;
    std::cout << 2067;
    for (int i = 0; i < 2067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2068(int& val) {
    const int x = 2068;
    std::cout << 2068;
    for (int i = 0; i < 2068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2069(int& val) {
    const int x = 2069;
    std::cout << 2069;
    for (int i = 0; i < 2069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2070(int& val) {
    const int x = 2070;
    std::cout << 2070;
    for (int i = 0; i < 2070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2071(int& val) {
    const int x = 2071;
    std::cout << 2071;
    for (int i = 0; i < 2071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2072(int& val) {
    const int x = 2072;
    std::cout << 2072;
    for (int i = 0; i < 2072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2073(int& val) {
    const int x = 2073;
    std::cout << 2073;
    for (int i = 0; i < 2073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2074(int& val) {
    const int x = 2074;
    std::cout << 2074;
    for (int i = 0; i < 2074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2075(int& val) {
    const int x = 2075;
    std::cout << 2075;
    for (int i = 0; i < 2075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2076(int& val) {
    const int x = 2076;
    std::cout << 2076;
    for (int i = 0; i < 2076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2077(int& val) {
    const int x = 2077;
    std::cout << 2077;
    for (int i = 0; i < 2077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2078(int& val) {
    const int x = 2078;
    std::cout << 2078;
    for (int i = 0; i < 2078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2079(int& val) {
    const int x = 2079;
    std::cout << 2079;
    for (int i = 0; i < 2079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2080(int& val) {
    const int x = 2080;
    std::cout << 2080;
    for (int i = 0; i < 2080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2081(int& val) {
    const int x = 2081;
    std::cout << 2081;
    for (int i = 0; i < 2081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2082(int& val) {
    const int x = 2082;
    std::cout << 2082;
    for (int i = 0; i < 2082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2083(int& val) {
    const int x = 2083;
    std::cout << 2083;
    for (int i = 0; i < 2083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2084(int& val) {
    const int x = 2084;
    std::cout << 2084;
    for (int i = 0; i < 2084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2085(int& val) {
    const int x = 2085;
    std::cout << 2085;
    for (int i = 0; i < 2085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2086(int& val) {
    const int x = 2086;
    std::cout << 2086;
    for (int i = 0; i < 2086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2087(int& val) {
    const int x = 2087;
    std::cout << 2087;
    for (int i = 0; i < 2087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2088(int& val) {
    const int x = 2088;
    std::cout << 2088;
    for (int i = 0; i < 2088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2089(int& val) {
    const int x = 2089;
    std::cout << 2089;
    for (int i = 0; i < 2089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2090(int& val) {
    const int x = 2090;
    std::cout << 2090;
    for (int i = 0; i < 2090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2091(int& val) {
    const int x = 2091;
    std::cout << 2091;
    for (int i = 0; i < 2091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2092(int& val) {
    const int x = 2092;
    std::cout << 2092;
    for (int i = 0; i < 2092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2093(int& val) {
    const int x = 2093;
    std::cout << 2093;
    for (int i = 0; i < 2093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2094(int& val) {
    const int x = 2094;
    std::cout << 2094;
    for (int i = 0; i < 2094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2095(int& val) {
    const int x = 2095;
    std::cout << 2095;
    for (int i = 0; i < 2095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2096(int& val) {
    const int x = 2096;
    std::cout << 2096;
    for (int i = 0; i < 2096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2097(int& val) {
    const int x = 2097;
    std::cout << 2097;
    for (int i = 0; i < 2097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2098(int& val) {
    const int x = 2098;
    std::cout << 2098;
    for (int i = 0; i < 2098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2099(int& val) {
    const int x = 2099;
    std::cout << 2099;
    for (int i = 0; i < 2099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2100(int& val) {
    const int x = 2100;
    std::cout << 2100;
    for (int i = 0; i < 2100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2101(int& val) {
    const int x = 2101;
    std::cout << 2101;
    for (int i = 0; i < 2101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2102(int& val) {
    const int x = 2102;
    std::cout << 2102;
    for (int i = 0; i < 2102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2103(int& val) {
    const int x = 2103;
    std::cout << 2103;
    for (int i = 0; i < 2103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2104(int& val) {
    const int x = 2104;
    std::cout << 2104;
    for (int i = 0; i < 2104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2105(int& val) {
    const int x = 2105;
    std::cout << 2105;
    for (int i = 0; i < 2105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2106(int& val) {
    const int x = 2106;
    std::cout << 2106;
    for (int i = 0; i < 2106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2107(int& val) {
    const int x = 2107;
    std::cout << 2107;
    for (int i = 0; i < 2107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2108(int& val) {
    const int x = 2108;
    std::cout << 2108;
    for (int i = 0; i < 2108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2109(int& val) {
    const int x = 2109;
    std::cout << 2109;
    for (int i = 0; i < 2109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2110(int& val) {
    const int x = 2110;
    std::cout << 2110;
    for (int i = 0; i < 2110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2111(int& val) {
    const int x = 2111;
    std::cout << 2111;
    for (int i = 0; i < 2111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2112(int& val) {
    const int x = 2112;
    std::cout << 2112;
    for (int i = 0; i < 2112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2113(int& val) {
    const int x = 2113;
    std::cout << 2113;
    for (int i = 0; i < 2113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2114(int& val) {
    const int x = 2114;
    std::cout << 2114;
    for (int i = 0; i < 2114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2115(int& val) {
    const int x = 2115;
    std::cout << 2115;
    for (int i = 0; i < 2115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2116(int& val) {
    const int x = 2116;
    std::cout << 2116;
    for (int i = 0; i < 2116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2117(int& val) {
    const int x = 2117;
    std::cout << 2117;
    for (int i = 0; i < 2117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2118(int& val) {
    const int x = 2118;
    std::cout << 2118;
    for (int i = 0; i < 2118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2119(int& val) {
    const int x = 2119;
    std::cout << 2119;
    for (int i = 0; i < 2119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2120(int& val) {
    const int x = 2120;
    std::cout << 2120;
    for (int i = 0; i < 2120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2121(int& val) {
    const int x = 2121;
    std::cout << 2121;
    for (int i = 0; i < 2121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2122(int& val) {
    const int x = 2122;
    std::cout << 2122;
    for (int i = 0; i < 2122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2123(int& val) {
    const int x = 2123;
    std::cout << 2123;
    for (int i = 0; i < 2123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2124(int& val) {
    const int x = 2124;
    std::cout << 2124;
    for (int i = 0; i < 2124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2125(int& val) {
    const int x = 2125;
    std::cout << 2125;
    for (int i = 0; i < 2125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2126(int& val) {
    const int x = 2126;
    std::cout << 2126;
    for (int i = 0; i < 2126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2127(int& val) {
    const int x = 2127;
    std::cout << 2127;
    for (int i = 0; i < 2127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2128(int& val) {
    const int x = 2128;
    std::cout << 2128;
    for (int i = 0; i < 2128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2129(int& val) {
    const int x = 2129;
    std::cout << 2129;
    for (int i = 0; i < 2129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2130(int& val) {
    const int x = 2130;
    std::cout << 2130;
    for (int i = 0; i < 2130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2131(int& val) {
    const int x = 2131;
    std::cout << 2131;
    for (int i = 0; i < 2131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2132(int& val) {
    const int x = 2132;
    std::cout << 2132;
    for (int i = 0; i < 2132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2133(int& val) {
    const int x = 2133;
    std::cout << 2133;
    for (int i = 0; i < 2133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2134(int& val) {
    const int x = 2134;
    std::cout << 2134;
    for (int i = 0; i < 2134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2135(int& val) {
    const int x = 2135;
    std::cout << 2135;
    for (int i = 0; i < 2135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2136(int& val) {
    const int x = 2136;
    std::cout << 2136;
    for (int i = 0; i < 2136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2137(int& val) {
    const int x = 2137;
    std::cout << 2137;
    for (int i = 0; i < 2137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2138(int& val) {
    const int x = 2138;
    std::cout << 2138;
    for (int i = 0; i < 2138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2139(int& val) {
    const int x = 2139;
    std::cout << 2139;
    for (int i = 0; i < 2139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2140(int& val) {
    const int x = 2140;
    std::cout << 2140;
    for (int i = 0; i < 2140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2141(int& val) {
    const int x = 2141;
    std::cout << 2141;
    for (int i = 0; i < 2141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2142(int& val) {
    const int x = 2142;
    std::cout << 2142;
    for (int i = 0; i < 2142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2143(int& val) {
    const int x = 2143;
    std::cout << 2143;
    for (int i = 0; i < 2143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2144(int& val) {
    const int x = 2144;
    std::cout << 2144;
    for (int i = 0; i < 2144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2145(int& val) {
    const int x = 2145;
    std::cout << 2145;
    for (int i = 0; i < 2145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2146(int& val) {
    const int x = 2146;
    std::cout << 2146;
    for (int i = 0; i < 2146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2147(int& val) {
    const int x = 2147;
    std::cout << 2147;
    for (int i = 0; i < 2147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2148(int& val) {
    const int x = 2148;
    std::cout << 2148;
    for (int i = 0; i < 2148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2149(int& val) {
    const int x = 2149;
    std::cout << 2149;
    for (int i = 0; i < 2149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2150(int& val) {
    const int x = 2150;
    std::cout << 2150;
    for (int i = 0; i < 2150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2151(int& val) {
    const int x = 2151;
    std::cout << 2151;
    for (int i = 0; i < 2151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2152(int& val) {
    const int x = 2152;
    std::cout << 2152;
    for (int i = 0; i < 2152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2153(int& val) {
    const int x = 2153;
    std::cout << 2153;
    for (int i = 0; i < 2153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2154(int& val) {
    const int x = 2154;
    std::cout << 2154;
    for (int i = 0; i < 2154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2155(int& val) {
    const int x = 2155;
    std::cout << 2155;
    for (int i = 0; i < 2155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2156(int& val) {
    const int x = 2156;
    std::cout << 2156;
    for (int i = 0; i < 2156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2157(int& val) {
    const int x = 2157;
    std::cout << 2157;
    for (int i = 0; i < 2157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2158(int& val) {
    const int x = 2158;
    std::cout << 2158;
    for (int i = 0; i < 2158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2159(int& val) {
    const int x = 2159;
    std::cout << 2159;
    for (int i = 0; i < 2159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2160(int& val) {
    const int x = 2160;
    std::cout << 2160;
    for (int i = 0; i < 2160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2161(int& val) {
    const int x = 2161;
    std::cout << 2161;
    for (int i = 0; i < 2161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2162(int& val) {
    const int x = 2162;
    std::cout << 2162;
    for (int i = 0; i < 2162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2163(int& val) {
    const int x = 2163;
    std::cout << 2163;
    for (int i = 0; i < 2163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2164(int& val) {
    const int x = 2164;
    std::cout << 2164;
    for (int i = 0; i < 2164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2165(int& val) {
    const int x = 2165;
    std::cout << 2165;
    for (int i = 0; i < 2165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2166(int& val) {
    const int x = 2166;
    std::cout << 2166;
    for (int i = 0; i < 2166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2167(int& val) {
    const int x = 2167;
    std::cout << 2167;
    for (int i = 0; i < 2167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2168(int& val) {
    const int x = 2168;
    std::cout << 2168;
    for (int i = 0; i < 2168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2169(int& val) {
    const int x = 2169;
    std::cout << 2169;
    for (int i = 0; i < 2169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2170(int& val) {
    const int x = 2170;
    std::cout << 2170;
    for (int i = 0; i < 2170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2171(int& val) {
    const int x = 2171;
    std::cout << 2171;
    for (int i = 0; i < 2171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2172(int& val) {
    const int x = 2172;
    std::cout << 2172;
    for (int i = 0; i < 2172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2173(int& val) {
    const int x = 2173;
    std::cout << 2173;
    for (int i = 0; i < 2173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2174(int& val) {
    const int x = 2174;
    std::cout << 2174;
    for (int i = 0; i < 2174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2175(int& val) {
    const int x = 2175;
    std::cout << 2175;
    for (int i = 0; i < 2175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2176(int& val) {
    const int x = 2176;
    std::cout << 2176;
    for (int i = 0; i < 2176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2177(int& val) {
    const int x = 2177;
    std::cout << 2177;
    for (int i = 0; i < 2177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2178(int& val) {
    const int x = 2178;
    std::cout << 2178;
    for (int i = 0; i < 2178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2179(int& val) {
    const int x = 2179;
    std::cout << 2179;
    for (int i = 0; i < 2179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2180(int& val) {
    const int x = 2180;
    std::cout << 2180;
    for (int i = 0; i < 2180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2181(int& val) {
    const int x = 2181;
    std::cout << 2181;
    for (int i = 0; i < 2181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2182(int& val) {
    const int x = 2182;
    std::cout << 2182;
    for (int i = 0; i < 2182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2183(int& val) {
    const int x = 2183;
    std::cout << 2183;
    for (int i = 0; i < 2183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2184(int& val) {
    const int x = 2184;
    std::cout << 2184;
    for (int i = 0; i < 2184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2185(int& val) {
    const int x = 2185;
    std::cout << 2185;
    for (int i = 0; i < 2185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2186(int& val) {
    const int x = 2186;
    std::cout << 2186;
    for (int i = 0; i < 2186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2187(int& val) {
    const int x = 2187;
    std::cout << 2187;
    for (int i = 0; i < 2187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2188(int& val) {
    const int x = 2188;
    std::cout << 2188;
    for (int i = 0; i < 2188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2189(int& val) {
    const int x = 2189;
    std::cout << 2189;
    for (int i = 0; i < 2189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2190(int& val) {
    const int x = 2190;
    std::cout << 2190;
    for (int i = 0; i < 2190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2191(int& val) {
    const int x = 2191;
    std::cout << 2191;
    for (int i = 0; i < 2191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2192(int& val) {
    const int x = 2192;
    std::cout << 2192;
    for (int i = 0; i < 2192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2193(int& val) {
    const int x = 2193;
    std::cout << 2193;
    for (int i = 0; i < 2193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2194(int& val) {
    const int x = 2194;
    std::cout << 2194;
    for (int i = 0; i < 2194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2195(int& val) {
    const int x = 2195;
    std::cout << 2195;
    for (int i = 0; i < 2195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2196(int& val) {
    const int x = 2196;
    std::cout << 2196;
    for (int i = 0; i < 2196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2197(int& val) {
    const int x = 2197;
    std::cout << 2197;
    for (int i = 0; i < 2197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2198(int& val) {
    const int x = 2198;
    std::cout << 2198;
    for (int i = 0; i < 2198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2199(int& val) {
    const int x = 2199;
    std::cout << 2199;
    for (int i = 0; i < 2199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2200(int& val) {
    const int x = 2200;
    std::cout << 2200;
    for (int i = 0; i < 2200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2201(int& val) {
    const int x = 2201;
    std::cout << 2201;
    for (int i = 0; i < 2201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2202(int& val) {
    const int x = 2202;
    std::cout << 2202;
    for (int i = 0; i < 2202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2203(int& val) {
    const int x = 2203;
    std::cout << 2203;
    for (int i = 0; i < 2203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2204(int& val) {
    const int x = 2204;
    std::cout << 2204;
    for (int i = 0; i < 2204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2205(int& val) {
    const int x = 2205;
    std::cout << 2205;
    for (int i = 0; i < 2205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2206(int& val) {
    const int x = 2206;
    std::cout << 2206;
    for (int i = 0; i < 2206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2207(int& val) {
    const int x = 2207;
    std::cout << 2207;
    for (int i = 0; i < 2207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2208(int& val) {
    const int x = 2208;
    std::cout << 2208;
    for (int i = 0; i < 2208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2209(int& val) {
    const int x = 2209;
    std::cout << 2209;
    for (int i = 0; i < 2209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2210(int& val) {
    const int x = 2210;
    std::cout << 2210;
    for (int i = 0; i < 2210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2211(int& val) {
    const int x = 2211;
    std::cout << 2211;
    for (int i = 0; i < 2211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2212(int& val) {
    const int x = 2212;
    std::cout << 2212;
    for (int i = 0; i < 2212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2213(int& val) {
    const int x = 2213;
    std::cout << 2213;
    for (int i = 0; i < 2213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2214(int& val) {
    const int x = 2214;
    std::cout << 2214;
    for (int i = 0; i < 2214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2215(int& val) {
    const int x = 2215;
    std::cout << 2215;
    for (int i = 0; i < 2215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2216(int& val) {
    const int x = 2216;
    std::cout << 2216;
    for (int i = 0; i < 2216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2217(int& val) {
    const int x = 2217;
    std::cout << 2217;
    for (int i = 0; i < 2217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2218(int& val) {
    const int x = 2218;
    std::cout << 2218;
    for (int i = 0; i < 2218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2219(int& val) {
    const int x = 2219;
    std::cout << 2219;
    for (int i = 0; i < 2219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2220(int& val) {
    const int x = 2220;
    std::cout << 2220;
    for (int i = 0; i < 2220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2221(int& val) {
    const int x = 2221;
    std::cout << 2221;
    for (int i = 0; i < 2221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2222(int& val) {
    const int x = 2222;
    std::cout << 2222;
    for (int i = 0; i < 2222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2223(int& val) {
    const int x = 2223;
    std::cout << 2223;
    for (int i = 0; i < 2223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2224(int& val) {
    const int x = 2224;
    std::cout << 2224;
    for (int i = 0; i < 2224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2225(int& val) {
    const int x = 2225;
    std::cout << 2225;
    for (int i = 0; i < 2225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2226(int& val) {
    const int x = 2226;
    std::cout << 2226;
    for (int i = 0; i < 2226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2227(int& val) {
    const int x = 2227;
    std::cout << 2227;
    for (int i = 0; i < 2227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2228(int& val) {
    const int x = 2228;
    std::cout << 2228;
    for (int i = 0; i < 2228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2229(int& val) {
    const int x = 2229;
    std::cout << 2229;
    for (int i = 0; i < 2229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2230(int& val) {
    const int x = 2230;
    std::cout << 2230;
    for (int i = 0; i < 2230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2231(int& val) {
    const int x = 2231;
    std::cout << 2231;
    for (int i = 0; i < 2231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2232(int& val) {
    const int x = 2232;
    std::cout << 2232;
    for (int i = 0; i < 2232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2233(int& val) {
    const int x = 2233;
    std::cout << 2233;
    for (int i = 0; i < 2233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2234(int& val) {
    const int x = 2234;
    std::cout << 2234;
    for (int i = 0; i < 2234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2235(int& val) {
    const int x = 2235;
    std::cout << 2235;
    for (int i = 0; i < 2235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2236(int& val) {
    const int x = 2236;
    std::cout << 2236;
    for (int i = 0; i < 2236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2237(int& val) {
    const int x = 2237;
    std::cout << 2237;
    for (int i = 0; i < 2237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2238(int& val) {
    const int x = 2238;
    std::cout << 2238;
    for (int i = 0; i < 2238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2239(int& val) {
    const int x = 2239;
    std::cout << 2239;
    for (int i = 0; i < 2239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2240(int& val) {
    const int x = 2240;
    std::cout << 2240;
    for (int i = 0; i < 2240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2241(int& val) {
    const int x = 2241;
    std::cout << 2241;
    for (int i = 0; i < 2241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2242(int& val) {
    const int x = 2242;
    std::cout << 2242;
    for (int i = 0; i < 2242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2243(int& val) {
    const int x = 2243;
    std::cout << 2243;
    for (int i = 0; i < 2243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2244(int& val) {
    const int x = 2244;
    std::cout << 2244;
    for (int i = 0; i < 2244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2245(int& val) {
    const int x = 2245;
    std::cout << 2245;
    for (int i = 0; i < 2245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2246(int& val) {
    const int x = 2246;
    std::cout << 2246;
    for (int i = 0; i < 2246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2247(int& val) {
    const int x = 2247;
    std::cout << 2247;
    for (int i = 0; i < 2247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2248(int& val) {
    const int x = 2248;
    std::cout << 2248;
    for (int i = 0; i < 2248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2249(int& val) {
    const int x = 2249;
    std::cout << 2249;
    for (int i = 0; i < 2249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2250(int& val) {
    const int x = 2250;
    std::cout << 2250;
    for (int i = 0; i < 2250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2251(int& val) {
    const int x = 2251;
    std::cout << 2251;
    for (int i = 0; i < 2251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2252(int& val) {
    const int x = 2252;
    std::cout << 2252;
    for (int i = 0; i < 2252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2253(int& val) {
    const int x = 2253;
    std::cout << 2253;
    for (int i = 0; i < 2253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2254(int& val) {
    const int x = 2254;
    std::cout << 2254;
    for (int i = 0; i < 2254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2255(int& val) {
    const int x = 2255;
    std::cout << 2255;
    for (int i = 0; i < 2255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2256(int& val) {
    const int x = 2256;
    std::cout << 2256;
    for (int i = 0; i < 2256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2257(int& val) {
    const int x = 2257;
    std::cout << 2257;
    for (int i = 0; i < 2257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2258(int& val) {
    const int x = 2258;
    std::cout << 2258;
    for (int i = 0; i < 2258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2259(int& val) {
    const int x = 2259;
    std::cout << 2259;
    for (int i = 0; i < 2259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2260(int& val) {
    const int x = 2260;
    std::cout << 2260;
    for (int i = 0; i < 2260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2261(int& val) {
    const int x = 2261;
    std::cout << 2261;
    for (int i = 0; i < 2261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2262(int& val) {
    const int x = 2262;
    std::cout << 2262;
    for (int i = 0; i < 2262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2263(int& val) {
    const int x = 2263;
    std::cout << 2263;
    for (int i = 0; i < 2263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2264(int& val) {
    const int x = 2264;
    std::cout << 2264;
    for (int i = 0; i < 2264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2265(int& val) {
    const int x = 2265;
    std::cout << 2265;
    for (int i = 0; i < 2265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2266(int& val) {
    const int x = 2266;
    std::cout << 2266;
    for (int i = 0; i < 2266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2267(int& val) {
    const int x = 2267;
    std::cout << 2267;
    for (int i = 0; i < 2267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2268(int& val) {
    const int x = 2268;
    std::cout << 2268;
    for (int i = 0; i < 2268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2269(int& val) {
    const int x = 2269;
    std::cout << 2269;
    for (int i = 0; i < 2269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2270(int& val) {
    const int x = 2270;
    std::cout << 2270;
    for (int i = 0; i < 2270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2271(int& val) {
    const int x = 2271;
    std::cout << 2271;
    for (int i = 0; i < 2271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2272(int& val) {
    const int x = 2272;
    std::cout << 2272;
    for (int i = 0; i < 2272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2273(int& val) {
    const int x = 2273;
    std::cout << 2273;
    for (int i = 0; i < 2273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2274(int& val) {
    const int x = 2274;
    std::cout << 2274;
    for (int i = 0; i < 2274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2275(int& val) {
    const int x = 2275;
    std::cout << 2275;
    for (int i = 0; i < 2275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2276(int& val) {
    const int x = 2276;
    std::cout << 2276;
    for (int i = 0; i < 2276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2277(int& val) {
    const int x = 2277;
    std::cout << 2277;
    for (int i = 0; i < 2277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2278(int& val) {
    const int x = 2278;
    std::cout << 2278;
    for (int i = 0; i < 2278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2279(int& val) {
    const int x = 2279;
    std::cout << 2279;
    for (int i = 0; i < 2279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2280(int& val) {
    const int x = 2280;
    std::cout << 2280;
    for (int i = 0; i < 2280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2281(int& val) {
    const int x = 2281;
    std::cout << 2281;
    for (int i = 0; i < 2281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2282(int& val) {
    const int x = 2282;
    std::cout << 2282;
    for (int i = 0; i < 2282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2283(int& val) {
    const int x = 2283;
    std::cout << 2283;
    for (int i = 0; i < 2283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2284(int& val) {
    const int x = 2284;
    std::cout << 2284;
    for (int i = 0; i < 2284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2285(int& val) {
    const int x = 2285;
    std::cout << 2285;
    for (int i = 0; i < 2285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2286(int& val) {
    const int x = 2286;
    std::cout << 2286;
    for (int i = 0; i < 2286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2287(int& val) {
    const int x = 2287;
    std::cout << 2287;
    for (int i = 0; i < 2287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2288(int& val) {
    const int x = 2288;
    std::cout << 2288;
    for (int i = 0; i < 2288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2289(int& val) {
    const int x = 2289;
    std::cout << 2289;
    for (int i = 0; i < 2289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2290(int& val) {
    const int x = 2290;
    std::cout << 2290;
    for (int i = 0; i < 2290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2291(int& val) {
    const int x = 2291;
    std::cout << 2291;
    for (int i = 0; i < 2291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2292(int& val) {
    const int x = 2292;
    std::cout << 2292;
    for (int i = 0; i < 2292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2293(int& val) {
    const int x = 2293;
    std::cout << 2293;
    for (int i = 0; i < 2293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2294(int& val) {
    const int x = 2294;
    std::cout << 2294;
    for (int i = 0; i < 2294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2295(int& val) {
    const int x = 2295;
    std::cout << 2295;
    for (int i = 0; i < 2295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2296(int& val) {
    const int x = 2296;
    std::cout << 2296;
    for (int i = 0; i < 2296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2297(int& val) {
    const int x = 2297;
    std::cout << 2297;
    for (int i = 0; i < 2297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2298(int& val) {
    const int x = 2298;
    std::cout << 2298;
    for (int i = 0; i < 2298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2299(int& val) {
    const int x = 2299;
    std::cout << 2299;
    for (int i = 0; i < 2299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2300(int& val) {
    const int x = 2300;
    std::cout << 2300;
    for (int i = 0; i < 2300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2301(int& val) {
    const int x = 2301;
    std::cout << 2301;
    for (int i = 0; i < 2301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2302(int& val) {
    const int x = 2302;
    std::cout << 2302;
    for (int i = 0; i < 2302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2303(int& val) {
    const int x = 2303;
    std::cout << 2303;
    for (int i = 0; i < 2303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2304(int& val) {
    const int x = 2304;
    std::cout << 2304;
    for (int i = 0; i < 2304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2305(int& val) {
    const int x = 2305;
    std::cout << 2305;
    for (int i = 0; i < 2305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2306(int& val) {
    const int x = 2306;
    std::cout << 2306;
    for (int i = 0; i < 2306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2307(int& val) {
    const int x = 2307;
    std::cout << 2307;
    for (int i = 0; i < 2307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2308(int& val) {
    const int x = 2308;
    std::cout << 2308;
    for (int i = 0; i < 2308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2309(int& val) {
    const int x = 2309;
    std::cout << 2309;
    for (int i = 0; i < 2309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2310(int& val) {
    const int x = 2310;
    std::cout << 2310;
    for (int i = 0; i < 2310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2311(int& val) {
    const int x = 2311;
    std::cout << 2311;
    for (int i = 0; i < 2311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2312(int& val) {
    const int x = 2312;
    std::cout << 2312;
    for (int i = 0; i < 2312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2313(int& val) {
    const int x = 2313;
    std::cout << 2313;
    for (int i = 0; i < 2313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2314(int& val) {
    const int x = 2314;
    std::cout << 2314;
    for (int i = 0; i < 2314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2315(int& val) {
    const int x = 2315;
    std::cout << 2315;
    for (int i = 0; i < 2315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2316(int& val) {
    const int x = 2316;
    std::cout << 2316;
    for (int i = 0; i < 2316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2317(int& val) {
    const int x = 2317;
    std::cout << 2317;
    for (int i = 0; i < 2317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2318(int& val) {
    const int x = 2318;
    std::cout << 2318;
    for (int i = 0; i < 2318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2319(int& val) {
    const int x = 2319;
    std::cout << 2319;
    for (int i = 0; i < 2319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2320(int& val) {
    const int x = 2320;
    std::cout << 2320;
    for (int i = 0; i < 2320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2321(int& val) {
    const int x = 2321;
    std::cout << 2321;
    for (int i = 0; i < 2321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2322(int& val) {
    const int x = 2322;
    std::cout << 2322;
    for (int i = 0; i < 2322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2323(int& val) {
    const int x = 2323;
    std::cout << 2323;
    for (int i = 0; i < 2323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2324(int& val) {
    const int x = 2324;
    std::cout << 2324;
    for (int i = 0; i < 2324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2325(int& val) {
    const int x = 2325;
    std::cout << 2325;
    for (int i = 0; i < 2325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2326(int& val) {
    const int x = 2326;
    std::cout << 2326;
    for (int i = 0; i < 2326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2327(int& val) {
    const int x = 2327;
    std::cout << 2327;
    for (int i = 0; i < 2327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2328(int& val) {
    const int x = 2328;
    std::cout << 2328;
    for (int i = 0; i < 2328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2329(int& val) {
    const int x = 2329;
    std::cout << 2329;
    for (int i = 0; i < 2329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2330(int& val) {
    const int x = 2330;
    std::cout << 2330;
    for (int i = 0; i < 2330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2331(int& val) {
    const int x = 2331;
    std::cout << 2331;
    for (int i = 0; i < 2331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2332(int& val) {
    const int x = 2332;
    std::cout << 2332;
    for (int i = 0; i < 2332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2333(int& val) {
    const int x = 2333;
    std::cout << 2333;
    for (int i = 0; i < 2333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2334(int& val) {
    const int x = 2334;
    std::cout << 2334;
    for (int i = 0; i < 2334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2335(int& val) {
    const int x = 2335;
    std::cout << 2335;
    for (int i = 0; i < 2335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2336(int& val) {
    const int x = 2336;
    std::cout << 2336;
    for (int i = 0; i < 2336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2337(int& val) {
    const int x = 2337;
    std::cout << 2337;
    for (int i = 0; i < 2337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2338(int& val) {
    const int x = 2338;
    std::cout << 2338;
    for (int i = 0; i < 2338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2339(int& val) {
    const int x = 2339;
    std::cout << 2339;
    for (int i = 0; i < 2339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2340(int& val) {
    const int x = 2340;
    std::cout << 2340;
    for (int i = 0; i < 2340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2341(int& val) {
    const int x = 2341;
    std::cout << 2341;
    for (int i = 0; i < 2341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2342(int& val) {
    const int x = 2342;
    std::cout << 2342;
    for (int i = 0; i < 2342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2343(int& val) {
    const int x = 2343;
    std::cout << 2343;
    for (int i = 0; i < 2343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2344(int& val) {
    const int x = 2344;
    std::cout << 2344;
    for (int i = 0; i < 2344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2345(int& val) {
    const int x = 2345;
    std::cout << 2345;
    for (int i = 0; i < 2345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2346(int& val) {
    const int x = 2346;
    std::cout << 2346;
    for (int i = 0; i < 2346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2347(int& val) {
    const int x = 2347;
    std::cout << 2347;
    for (int i = 0; i < 2347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2348(int& val) {
    const int x = 2348;
    std::cout << 2348;
    for (int i = 0; i < 2348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2349(int& val) {
    const int x = 2349;
    std::cout << 2349;
    for (int i = 0; i < 2349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2350(int& val) {
    const int x = 2350;
    std::cout << 2350;
    for (int i = 0; i < 2350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2351(int& val) {
    const int x = 2351;
    std::cout << 2351;
    for (int i = 0; i < 2351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2352(int& val) {
    const int x = 2352;
    std::cout << 2352;
    for (int i = 0; i < 2352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2353(int& val) {
    const int x = 2353;
    std::cout << 2353;
    for (int i = 0; i < 2353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2354(int& val) {
    const int x = 2354;
    std::cout << 2354;
    for (int i = 0; i < 2354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2355(int& val) {
    const int x = 2355;
    std::cout << 2355;
    for (int i = 0; i < 2355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2356(int& val) {
    const int x = 2356;
    std::cout << 2356;
    for (int i = 0; i < 2356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2357(int& val) {
    const int x = 2357;
    std::cout << 2357;
    for (int i = 0; i < 2357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2358(int& val) {
    const int x = 2358;
    std::cout << 2358;
    for (int i = 0; i < 2358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2359(int& val) {
    const int x = 2359;
    std::cout << 2359;
    for (int i = 0; i < 2359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2360(int& val) {
    const int x = 2360;
    std::cout << 2360;
    for (int i = 0; i < 2360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2361(int& val) {
    const int x = 2361;
    std::cout << 2361;
    for (int i = 0; i < 2361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2362(int& val) {
    const int x = 2362;
    std::cout << 2362;
    for (int i = 0; i < 2362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2363(int& val) {
    const int x = 2363;
    std::cout << 2363;
    for (int i = 0; i < 2363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2364(int& val) {
    const int x = 2364;
    std::cout << 2364;
    for (int i = 0; i < 2364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2365(int& val) {
    const int x = 2365;
    std::cout << 2365;
    for (int i = 0; i < 2365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2366(int& val) {
    const int x = 2366;
    std::cout << 2366;
    for (int i = 0; i < 2366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2367(int& val) {
    const int x = 2367;
    std::cout << 2367;
    for (int i = 0; i < 2367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2368(int& val) {
    const int x = 2368;
    std::cout << 2368;
    for (int i = 0; i < 2368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2369(int& val) {
    const int x = 2369;
    std::cout << 2369;
    for (int i = 0; i < 2369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2370(int& val) {
    const int x = 2370;
    std::cout << 2370;
    for (int i = 0; i < 2370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2371(int& val) {
    const int x = 2371;
    std::cout << 2371;
    for (int i = 0; i < 2371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2372(int& val) {
    const int x = 2372;
    std::cout << 2372;
    for (int i = 0; i < 2372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2373(int& val) {
    const int x = 2373;
    std::cout << 2373;
    for (int i = 0; i < 2373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2374(int& val) {
    const int x = 2374;
    std::cout << 2374;
    for (int i = 0; i < 2374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2375(int& val) {
    const int x = 2375;
    std::cout << 2375;
    for (int i = 0; i < 2375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2376(int& val) {
    const int x = 2376;
    std::cout << 2376;
    for (int i = 0; i < 2376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2377(int& val) {
    const int x = 2377;
    std::cout << 2377;
    for (int i = 0; i < 2377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2378(int& val) {
    const int x = 2378;
    std::cout << 2378;
    for (int i = 0; i < 2378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2379(int& val) {
    const int x = 2379;
    std::cout << 2379;
    for (int i = 0; i < 2379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2380(int& val) {
    const int x = 2380;
    std::cout << 2380;
    for (int i = 0; i < 2380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2381(int& val) {
    const int x = 2381;
    std::cout << 2381;
    for (int i = 0; i < 2381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2382(int& val) {
    const int x = 2382;
    std::cout << 2382;
    for (int i = 0; i < 2382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2383(int& val) {
    const int x = 2383;
    std::cout << 2383;
    for (int i = 0; i < 2383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2384(int& val) {
    const int x = 2384;
    std::cout << 2384;
    for (int i = 0; i < 2384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2385(int& val) {
    const int x = 2385;
    std::cout << 2385;
    for (int i = 0; i < 2385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2386(int& val) {
    const int x = 2386;
    std::cout << 2386;
    for (int i = 0; i < 2386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2387(int& val) {
    const int x = 2387;
    std::cout << 2387;
    for (int i = 0; i < 2387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2388(int& val) {
    const int x = 2388;
    std::cout << 2388;
    for (int i = 0; i < 2388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2389(int& val) {
    const int x = 2389;
    std::cout << 2389;
    for (int i = 0; i < 2389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2390(int& val) {
    const int x = 2390;
    std::cout << 2390;
    for (int i = 0; i < 2390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2391(int& val) {
    const int x = 2391;
    std::cout << 2391;
    for (int i = 0; i < 2391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2392(int& val) {
    const int x = 2392;
    std::cout << 2392;
    for (int i = 0; i < 2392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2393(int& val) {
    const int x = 2393;
    std::cout << 2393;
    for (int i = 0; i < 2393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2394(int& val) {
    const int x = 2394;
    std::cout << 2394;
    for (int i = 0; i < 2394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2395(int& val) {
    const int x = 2395;
    std::cout << 2395;
    for (int i = 0; i < 2395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2396(int& val) {
    const int x = 2396;
    std::cout << 2396;
    for (int i = 0; i < 2396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2397(int& val) {
    const int x = 2397;
    std::cout << 2397;
    for (int i = 0; i < 2397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2398(int& val) {
    const int x = 2398;
    std::cout << 2398;
    for (int i = 0; i < 2398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2399(int& val) {
    const int x = 2399;
    std::cout << 2399;
    for (int i = 0; i < 2399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2400(int& val) {
    const int x = 2400;
    std::cout << 2400;
    for (int i = 0; i < 2400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2401(int& val) {
    const int x = 2401;
    std::cout << 2401;
    for (int i = 0; i < 2401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2402(int& val) {
    const int x = 2402;
    std::cout << 2402;
    for (int i = 0; i < 2402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2403(int& val) {
    const int x = 2403;
    std::cout << 2403;
    for (int i = 0; i < 2403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2404(int& val) {
    const int x = 2404;
    std::cout << 2404;
    for (int i = 0; i < 2404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2405(int& val) {
    const int x = 2405;
    std::cout << 2405;
    for (int i = 0; i < 2405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2406(int& val) {
    const int x = 2406;
    std::cout << 2406;
    for (int i = 0; i < 2406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2407(int& val) {
    const int x = 2407;
    std::cout << 2407;
    for (int i = 0; i < 2407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2408(int& val) {
    const int x = 2408;
    std::cout << 2408;
    for (int i = 0; i < 2408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2409(int& val) {
    const int x = 2409;
    std::cout << 2409;
    for (int i = 0; i < 2409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2410(int& val) {
    const int x = 2410;
    std::cout << 2410;
    for (int i = 0; i < 2410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2411(int& val) {
    const int x = 2411;
    std::cout << 2411;
    for (int i = 0; i < 2411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2412(int& val) {
    const int x = 2412;
    std::cout << 2412;
    for (int i = 0; i < 2412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2413(int& val) {
    const int x = 2413;
    std::cout << 2413;
    for (int i = 0; i < 2413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2414(int& val) {
    const int x = 2414;
    std::cout << 2414;
    for (int i = 0; i < 2414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2415(int& val) {
    const int x = 2415;
    std::cout << 2415;
    for (int i = 0; i < 2415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2416(int& val) {
    const int x = 2416;
    std::cout << 2416;
    for (int i = 0; i < 2416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2417(int& val) {
    const int x = 2417;
    std::cout << 2417;
    for (int i = 0; i < 2417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2418(int& val) {
    const int x = 2418;
    std::cout << 2418;
    for (int i = 0; i < 2418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2419(int& val) {
    const int x = 2419;
    std::cout << 2419;
    for (int i = 0; i < 2419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2420(int& val) {
    const int x = 2420;
    std::cout << 2420;
    for (int i = 0; i < 2420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2421(int& val) {
    const int x = 2421;
    std::cout << 2421;
    for (int i = 0; i < 2421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2422(int& val) {
    const int x = 2422;
    std::cout << 2422;
    for (int i = 0; i < 2422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2423(int& val) {
    const int x = 2423;
    std::cout << 2423;
    for (int i = 0; i < 2423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2424(int& val) {
    const int x = 2424;
    std::cout << 2424;
    for (int i = 0; i < 2424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2425(int& val) {
    const int x = 2425;
    std::cout << 2425;
    for (int i = 0; i < 2425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2426(int& val) {
    const int x = 2426;
    std::cout << 2426;
    for (int i = 0; i < 2426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2427(int& val) {
    const int x = 2427;
    std::cout << 2427;
    for (int i = 0; i < 2427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2428(int& val) {
    const int x = 2428;
    std::cout << 2428;
    for (int i = 0; i < 2428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2429(int& val) {
    const int x = 2429;
    std::cout << 2429;
    for (int i = 0; i < 2429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2430(int& val) {
    const int x = 2430;
    std::cout << 2430;
    for (int i = 0; i < 2430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2431(int& val) {
    const int x = 2431;
    std::cout << 2431;
    for (int i = 0; i < 2431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2432(int& val) {
    const int x = 2432;
    std::cout << 2432;
    for (int i = 0; i < 2432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2433(int& val) {
    const int x = 2433;
    std::cout << 2433;
    for (int i = 0; i < 2433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2434(int& val) {
    const int x = 2434;
    std::cout << 2434;
    for (int i = 0; i < 2434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2435(int& val) {
    const int x = 2435;
    std::cout << 2435;
    for (int i = 0; i < 2435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2436(int& val) {
    const int x = 2436;
    std::cout << 2436;
    for (int i = 0; i < 2436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2437(int& val) {
    const int x = 2437;
    std::cout << 2437;
    for (int i = 0; i < 2437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2438(int& val) {
    const int x = 2438;
    std::cout << 2438;
    for (int i = 0; i < 2438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2439(int& val) {
    const int x = 2439;
    std::cout << 2439;
    for (int i = 0; i < 2439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2440(int& val) {
    const int x = 2440;
    std::cout << 2440;
    for (int i = 0; i < 2440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2441(int& val) {
    const int x = 2441;
    std::cout << 2441;
    for (int i = 0; i < 2441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2442(int& val) {
    const int x = 2442;
    std::cout << 2442;
    for (int i = 0; i < 2442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2443(int& val) {
    const int x = 2443;
    std::cout << 2443;
    for (int i = 0; i < 2443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2444(int& val) {
    const int x = 2444;
    std::cout << 2444;
    for (int i = 0; i < 2444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2445(int& val) {
    const int x = 2445;
    std::cout << 2445;
    for (int i = 0; i < 2445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2446(int& val) {
    const int x = 2446;
    std::cout << 2446;
    for (int i = 0; i < 2446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2447(int& val) {
    const int x = 2447;
    std::cout << 2447;
    for (int i = 0; i < 2447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2448(int& val) {
    const int x = 2448;
    std::cout << 2448;
    for (int i = 0; i < 2448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2449(int& val) {
    const int x = 2449;
    std::cout << 2449;
    for (int i = 0; i < 2449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2450(int& val) {
    const int x = 2450;
    std::cout << 2450;
    for (int i = 0; i < 2450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2451(int& val) {
    const int x = 2451;
    std::cout << 2451;
    for (int i = 0; i < 2451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2452(int& val) {
    const int x = 2452;
    std::cout << 2452;
    for (int i = 0; i < 2452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2453(int& val) {
    const int x = 2453;
    std::cout << 2453;
    for (int i = 0; i < 2453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2454(int& val) {
    const int x = 2454;
    std::cout << 2454;
    for (int i = 0; i < 2454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2455(int& val) {
    const int x = 2455;
    std::cout << 2455;
    for (int i = 0; i < 2455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2456(int& val) {
    const int x = 2456;
    std::cout << 2456;
    for (int i = 0; i < 2456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2457(int& val) {
    const int x = 2457;
    std::cout << 2457;
    for (int i = 0; i < 2457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2458(int& val) {
    const int x = 2458;
    std::cout << 2458;
    for (int i = 0; i < 2458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2459(int& val) {
    const int x = 2459;
    std::cout << 2459;
    for (int i = 0; i < 2459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2460(int& val) {
    const int x = 2460;
    std::cout << 2460;
    for (int i = 0; i < 2460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2461(int& val) {
    const int x = 2461;
    std::cout << 2461;
    for (int i = 0; i < 2461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2462(int& val) {
    const int x = 2462;
    std::cout << 2462;
    for (int i = 0; i < 2462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2463(int& val) {
    const int x = 2463;
    std::cout << 2463;
    for (int i = 0; i < 2463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2464(int& val) {
    const int x = 2464;
    std::cout << 2464;
    for (int i = 0; i < 2464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2465(int& val) {
    const int x = 2465;
    std::cout << 2465;
    for (int i = 0; i < 2465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2466(int& val) {
    const int x = 2466;
    std::cout << 2466;
    for (int i = 0; i < 2466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2467(int& val) {
    const int x = 2467;
    std::cout << 2467;
    for (int i = 0; i < 2467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2468(int& val) {
    const int x = 2468;
    std::cout << 2468;
    for (int i = 0; i < 2468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2469(int& val) {
    const int x = 2469;
    std::cout << 2469;
    for (int i = 0; i < 2469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2470(int& val) {
    const int x = 2470;
    std::cout << 2470;
    for (int i = 0; i < 2470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2471(int& val) {
    const int x = 2471;
    std::cout << 2471;
    for (int i = 0; i < 2471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2472(int& val) {
    const int x = 2472;
    std::cout << 2472;
    for (int i = 0; i < 2472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2473(int& val) {
    const int x = 2473;
    std::cout << 2473;
    for (int i = 0; i < 2473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2474(int& val) {
    const int x = 2474;
    std::cout << 2474;
    for (int i = 0; i < 2474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2475(int& val) {
    const int x = 2475;
    std::cout << 2475;
    for (int i = 0; i < 2475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2476(int& val) {
    const int x = 2476;
    std::cout << 2476;
    for (int i = 0; i < 2476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2477(int& val) {
    const int x = 2477;
    std::cout << 2477;
    for (int i = 0; i < 2477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2478(int& val) {
    const int x = 2478;
    std::cout << 2478;
    for (int i = 0; i < 2478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2479(int& val) {
    const int x = 2479;
    std::cout << 2479;
    for (int i = 0; i < 2479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2480(int& val) {
    const int x = 2480;
    std::cout << 2480;
    for (int i = 0; i < 2480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2481(int& val) {
    const int x = 2481;
    std::cout << 2481;
    for (int i = 0; i < 2481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2482(int& val) {
    const int x = 2482;
    std::cout << 2482;
    for (int i = 0; i < 2482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2483(int& val) {
    const int x = 2483;
    std::cout << 2483;
    for (int i = 0; i < 2483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2484(int& val) {
    const int x = 2484;
    std::cout << 2484;
    for (int i = 0; i < 2484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2485(int& val) {
    const int x = 2485;
    std::cout << 2485;
    for (int i = 0; i < 2485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2486(int& val) {
    const int x = 2486;
    std::cout << 2486;
    for (int i = 0; i < 2486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2487(int& val) {
    const int x = 2487;
    std::cout << 2487;
    for (int i = 0; i < 2487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2488(int& val) {
    const int x = 2488;
    std::cout << 2488;
    for (int i = 0; i < 2488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2489(int& val) {
    const int x = 2489;
    std::cout << 2489;
    for (int i = 0; i < 2489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2490(int& val) {
    const int x = 2490;
    std::cout << 2490;
    for (int i = 0; i < 2490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2491(int& val) {
    const int x = 2491;
    std::cout << 2491;
    for (int i = 0; i < 2491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2492(int& val) {
    const int x = 2492;
    std::cout << 2492;
    for (int i = 0; i < 2492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2493(int& val) {
    const int x = 2493;
    std::cout << 2493;
    for (int i = 0; i < 2493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2494(int& val) {
    const int x = 2494;
    std::cout << 2494;
    for (int i = 0; i < 2494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2495(int& val) {
    const int x = 2495;
    std::cout << 2495;
    for (int i = 0; i < 2495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2496(int& val) {
    const int x = 2496;
    std::cout << 2496;
    for (int i = 0; i < 2496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2497(int& val) {
    const int x = 2497;
    std::cout << 2497;
    for (int i = 0; i < 2497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2498(int& val) {
    const int x = 2498;
    std::cout << 2498;
    for (int i = 0; i < 2498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2499(int& val) {
    const int x = 2499;
    std::cout << 2499;
    for (int i = 0; i < 2499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2500(int& val) {
    const int x = 2500;
    std::cout << 2500;
    for (int i = 0; i < 2500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2501(int& val) {
    const int x = 2501;
    std::cout << 2501;
    for (int i = 0; i < 2501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2502(int& val) {
    const int x = 2502;
    std::cout << 2502;
    for (int i = 0; i < 2502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2503(int& val) {
    const int x = 2503;
    std::cout << 2503;
    for (int i = 0; i < 2503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2504(int& val) {
    const int x = 2504;
    std::cout << 2504;
    for (int i = 0; i < 2504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2505(int& val) {
    const int x = 2505;
    std::cout << 2505;
    for (int i = 0; i < 2505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2506(int& val) {
    const int x = 2506;
    std::cout << 2506;
    for (int i = 0; i < 2506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2507(int& val) {
    const int x = 2507;
    std::cout << 2507;
    for (int i = 0; i < 2507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2508(int& val) {
    const int x = 2508;
    std::cout << 2508;
    for (int i = 0; i < 2508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2509(int& val) {
    const int x = 2509;
    std::cout << 2509;
    for (int i = 0; i < 2509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2510(int& val) {
    const int x = 2510;
    std::cout << 2510;
    for (int i = 0; i < 2510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2511(int& val) {
    const int x = 2511;
    std::cout << 2511;
    for (int i = 0; i < 2511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2512(int& val) {
    const int x = 2512;
    std::cout << 2512;
    for (int i = 0; i < 2512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2513(int& val) {
    const int x = 2513;
    std::cout << 2513;
    for (int i = 0; i < 2513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2514(int& val) {
    const int x = 2514;
    std::cout << 2514;
    for (int i = 0; i < 2514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2515(int& val) {
    const int x = 2515;
    std::cout << 2515;
    for (int i = 0; i < 2515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2516(int& val) {
    const int x = 2516;
    std::cout << 2516;
    for (int i = 0; i < 2516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2517(int& val) {
    const int x = 2517;
    std::cout << 2517;
    for (int i = 0; i < 2517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2518(int& val) {
    const int x = 2518;
    std::cout << 2518;
    for (int i = 0; i < 2518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2519(int& val) {
    const int x = 2519;
    std::cout << 2519;
    for (int i = 0; i < 2519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2520(int& val) {
    const int x = 2520;
    std::cout << 2520;
    for (int i = 0; i < 2520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2521(int& val) {
    const int x = 2521;
    std::cout << 2521;
    for (int i = 0; i < 2521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2522(int& val) {
    const int x = 2522;
    std::cout << 2522;
    for (int i = 0; i < 2522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2523(int& val) {
    const int x = 2523;
    std::cout << 2523;
    for (int i = 0; i < 2523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2524(int& val) {
    const int x = 2524;
    std::cout << 2524;
    for (int i = 0; i < 2524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2525(int& val) {
    const int x = 2525;
    std::cout << 2525;
    for (int i = 0; i < 2525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2526(int& val) {
    const int x = 2526;
    std::cout << 2526;
    for (int i = 0; i < 2526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2527(int& val) {
    const int x = 2527;
    std::cout << 2527;
    for (int i = 0; i < 2527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2528(int& val) {
    const int x = 2528;
    std::cout << 2528;
    for (int i = 0; i < 2528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2529(int& val) {
    const int x = 2529;
    std::cout << 2529;
    for (int i = 0; i < 2529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2530(int& val) {
    const int x = 2530;
    std::cout << 2530;
    for (int i = 0; i < 2530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2531(int& val) {
    const int x = 2531;
    std::cout << 2531;
    for (int i = 0; i < 2531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2532(int& val) {
    const int x = 2532;
    std::cout << 2532;
    for (int i = 0; i < 2532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2533(int& val) {
    const int x = 2533;
    std::cout << 2533;
    for (int i = 0; i < 2533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2534(int& val) {
    const int x = 2534;
    std::cout << 2534;
    for (int i = 0; i < 2534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2535(int& val) {
    const int x = 2535;
    std::cout << 2535;
    for (int i = 0; i < 2535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2536(int& val) {
    const int x = 2536;
    std::cout << 2536;
    for (int i = 0; i < 2536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2537(int& val) {
    const int x = 2537;
    std::cout << 2537;
    for (int i = 0; i < 2537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2538(int& val) {
    const int x = 2538;
    std::cout << 2538;
    for (int i = 0; i < 2538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2539(int& val) {
    const int x = 2539;
    std::cout << 2539;
    for (int i = 0; i < 2539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2540(int& val) {
    const int x = 2540;
    std::cout << 2540;
    for (int i = 0; i < 2540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2541(int& val) {
    const int x = 2541;
    std::cout << 2541;
    for (int i = 0; i < 2541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2542(int& val) {
    const int x = 2542;
    std::cout << 2542;
    for (int i = 0; i < 2542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2543(int& val) {
    const int x = 2543;
    std::cout << 2543;
    for (int i = 0; i < 2543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2544(int& val) {
    const int x = 2544;
    std::cout << 2544;
    for (int i = 0; i < 2544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2545(int& val) {
    const int x = 2545;
    std::cout << 2545;
    for (int i = 0; i < 2545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2546(int& val) {
    const int x = 2546;
    std::cout << 2546;
    for (int i = 0; i < 2546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2547(int& val) {
    const int x = 2547;
    std::cout << 2547;
    for (int i = 0; i < 2547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2548(int& val) {
    const int x = 2548;
    std::cout << 2548;
    for (int i = 0; i < 2548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2549(int& val) {
    const int x = 2549;
    std::cout << 2549;
    for (int i = 0; i < 2549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2550(int& val) {
    const int x = 2550;
    std::cout << 2550;
    for (int i = 0; i < 2550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2551(int& val) {
    const int x = 2551;
    std::cout << 2551;
    for (int i = 0; i < 2551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2552(int& val) {
    const int x = 2552;
    std::cout << 2552;
    for (int i = 0; i < 2552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2553(int& val) {
    const int x = 2553;
    std::cout << 2553;
    for (int i = 0; i < 2553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2554(int& val) {
    const int x = 2554;
    std::cout << 2554;
    for (int i = 0; i < 2554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2555(int& val) {
    const int x = 2555;
    std::cout << 2555;
    for (int i = 0; i < 2555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2556(int& val) {
    const int x = 2556;
    std::cout << 2556;
    for (int i = 0; i < 2556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2557(int& val) {
    const int x = 2557;
    std::cout << 2557;
    for (int i = 0; i < 2557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2558(int& val) {
    const int x = 2558;
    std::cout << 2558;
    for (int i = 0; i < 2558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2559(int& val) {
    const int x = 2559;
    std::cout << 2559;
    for (int i = 0; i < 2559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2560(int& val) {
    const int x = 2560;
    std::cout << 2560;
    for (int i = 0; i < 2560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2561(int& val) {
    const int x = 2561;
    std::cout << 2561;
    for (int i = 0; i < 2561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2562(int& val) {
    const int x = 2562;
    std::cout << 2562;
    for (int i = 0; i < 2562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2563(int& val) {
    const int x = 2563;
    std::cout << 2563;
    for (int i = 0; i < 2563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2564(int& val) {
    const int x = 2564;
    std::cout << 2564;
    for (int i = 0; i < 2564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2565(int& val) {
    const int x = 2565;
    std::cout << 2565;
    for (int i = 0; i < 2565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2566(int& val) {
    const int x = 2566;
    std::cout << 2566;
    for (int i = 0; i < 2566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2567(int& val) {
    const int x = 2567;
    std::cout << 2567;
    for (int i = 0; i < 2567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2568(int& val) {
    const int x = 2568;
    std::cout << 2568;
    for (int i = 0; i < 2568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2569(int& val) {
    const int x = 2569;
    std::cout << 2569;
    for (int i = 0; i < 2569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2570(int& val) {
    const int x = 2570;
    std::cout << 2570;
    for (int i = 0; i < 2570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2571(int& val) {
    const int x = 2571;
    std::cout << 2571;
    for (int i = 0; i < 2571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2572(int& val) {
    const int x = 2572;
    std::cout << 2572;
    for (int i = 0; i < 2572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2573(int& val) {
    const int x = 2573;
    std::cout << 2573;
    for (int i = 0; i < 2573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2574(int& val) {
    const int x = 2574;
    std::cout << 2574;
    for (int i = 0; i < 2574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2575(int& val) {
    const int x = 2575;
    std::cout << 2575;
    for (int i = 0; i < 2575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2576(int& val) {
    const int x = 2576;
    std::cout << 2576;
    for (int i = 0; i < 2576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2577(int& val) {
    const int x = 2577;
    std::cout << 2577;
    for (int i = 0; i < 2577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2578(int& val) {
    const int x = 2578;
    std::cout << 2578;
    for (int i = 0; i < 2578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2579(int& val) {
    const int x = 2579;
    std::cout << 2579;
    for (int i = 0; i < 2579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2580(int& val) {
    const int x = 2580;
    std::cout << 2580;
    for (int i = 0; i < 2580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2581(int& val) {
    const int x = 2581;
    std::cout << 2581;
    for (int i = 0; i < 2581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2582(int& val) {
    const int x = 2582;
    std::cout << 2582;
    for (int i = 0; i < 2582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2583(int& val) {
    const int x = 2583;
    std::cout << 2583;
    for (int i = 0; i < 2583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2584(int& val) {
    const int x = 2584;
    std::cout << 2584;
    for (int i = 0; i < 2584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2585(int& val) {
    const int x = 2585;
    std::cout << 2585;
    for (int i = 0; i < 2585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2586(int& val) {
    const int x = 2586;
    std::cout << 2586;
    for (int i = 0; i < 2586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2587(int& val) {
    const int x = 2587;
    std::cout << 2587;
    for (int i = 0; i < 2587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2588(int& val) {
    const int x = 2588;
    std::cout << 2588;
    for (int i = 0; i < 2588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2589(int& val) {
    const int x = 2589;
    std::cout << 2589;
    for (int i = 0; i < 2589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2590(int& val) {
    const int x = 2590;
    std::cout << 2590;
    for (int i = 0; i < 2590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2591(int& val) {
    const int x = 2591;
    std::cout << 2591;
    for (int i = 0; i < 2591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2592(int& val) {
    const int x = 2592;
    std::cout << 2592;
    for (int i = 0; i < 2592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2593(int& val) {
    const int x = 2593;
    std::cout << 2593;
    for (int i = 0; i < 2593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2594(int& val) {
    const int x = 2594;
    std::cout << 2594;
    for (int i = 0; i < 2594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2595(int& val) {
    const int x = 2595;
    std::cout << 2595;
    for (int i = 0; i < 2595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2596(int& val) {
    const int x = 2596;
    std::cout << 2596;
    for (int i = 0; i < 2596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2597(int& val) {
    const int x = 2597;
    std::cout << 2597;
    for (int i = 0; i < 2597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2598(int& val) {
    const int x = 2598;
    std::cout << 2598;
    for (int i = 0; i < 2598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2599(int& val) {
    const int x = 2599;
    std::cout << 2599;
    for (int i = 0; i < 2599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2600(int& val) {
    const int x = 2600;
    std::cout << 2600;
    for (int i = 0; i < 2600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2601(int& val) {
    const int x = 2601;
    std::cout << 2601;
    for (int i = 0; i < 2601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2602(int& val) {
    const int x = 2602;
    std::cout << 2602;
    for (int i = 0; i < 2602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2603(int& val) {
    const int x = 2603;
    std::cout << 2603;
    for (int i = 0; i < 2603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2604(int& val) {
    const int x = 2604;
    std::cout << 2604;
    for (int i = 0; i < 2604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2605(int& val) {
    const int x = 2605;
    std::cout << 2605;
    for (int i = 0; i < 2605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2606(int& val) {
    const int x = 2606;
    std::cout << 2606;
    for (int i = 0; i < 2606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2607(int& val) {
    const int x = 2607;
    std::cout << 2607;
    for (int i = 0; i < 2607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2608(int& val) {
    const int x = 2608;
    std::cout << 2608;
    for (int i = 0; i < 2608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2609(int& val) {
    const int x = 2609;
    std::cout << 2609;
    for (int i = 0; i < 2609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2610(int& val) {
    const int x = 2610;
    std::cout << 2610;
    for (int i = 0; i < 2610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2611(int& val) {
    const int x = 2611;
    std::cout << 2611;
    for (int i = 0; i < 2611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2612(int& val) {
    const int x = 2612;
    std::cout << 2612;
    for (int i = 0; i < 2612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2613(int& val) {
    const int x = 2613;
    std::cout << 2613;
    for (int i = 0; i < 2613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2614(int& val) {
    const int x = 2614;
    std::cout << 2614;
    for (int i = 0; i < 2614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2615(int& val) {
    const int x = 2615;
    std::cout << 2615;
    for (int i = 0; i < 2615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2616(int& val) {
    const int x = 2616;
    std::cout << 2616;
    for (int i = 0; i < 2616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2617(int& val) {
    const int x = 2617;
    std::cout << 2617;
    for (int i = 0; i < 2617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2618(int& val) {
    const int x = 2618;
    std::cout << 2618;
    for (int i = 0; i < 2618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2619(int& val) {
    const int x = 2619;
    std::cout << 2619;
    for (int i = 0; i < 2619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2620(int& val) {
    const int x = 2620;
    std::cout << 2620;
    for (int i = 0; i < 2620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2621(int& val) {
    const int x = 2621;
    std::cout << 2621;
    for (int i = 0; i < 2621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2622(int& val) {
    const int x = 2622;
    std::cout << 2622;
    for (int i = 0; i < 2622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2623(int& val) {
    const int x = 2623;
    std::cout << 2623;
    for (int i = 0; i < 2623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2624(int& val) {
    const int x = 2624;
    std::cout << 2624;
    for (int i = 0; i < 2624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2625(int& val) {
    const int x = 2625;
    std::cout << 2625;
    for (int i = 0; i < 2625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2626(int& val) {
    const int x = 2626;
    std::cout << 2626;
    for (int i = 0; i < 2626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2627(int& val) {
    const int x = 2627;
    std::cout << 2627;
    for (int i = 0; i < 2627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2628(int& val) {
    const int x = 2628;
    std::cout << 2628;
    for (int i = 0; i < 2628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2629(int& val) {
    const int x = 2629;
    std::cout << 2629;
    for (int i = 0; i < 2629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2630(int& val) {
    const int x = 2630;
    std::cout << 2630;
    for (int i = 0; i < 2630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2631(int& val) {
    const int x = 2631;
    std::cout << 2631;
    for (int i = 0; i < 2631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2632(int& val) {
    const int x = 2632;
    std::cout << 2632;
    for (int i = 0; i < 2632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2633(int& val) {
    const int x = 2633;
    std::cout << 2633;
    for (int i = 0; i < 2633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2634(int& val) {
    const int x = 2634;
    std::cout << 2634;
    for (int i = 0; i < 2634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2635(int& val) {
    const int x = 2635;
    std::cout << 2635;
    for (int i = 0; i < 2635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2636(int& val) {
    const int x = 2636;
    std::cout << 2636;
    for (int i = 0; i < 2636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2637(int& val) {
    const int x = 2637;
    std::cout << 2637;
    for (int i = 0; i < 2637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2638(int& val) {
    const int x = 2638;
    std::cout << 2638;
    for (int i = 0; i < 2638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2639(int& val) {
    const int x = 2639;
    std::cout << 2639;
    for (int i = 0; i < 2639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2640(int& val) {
    const int x = 2640;
    std::cout << 2640;
    for (int i = 0; i < 2640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2641(int& val) {
    const int x = 2641;
    std::cout << 2641;
    for (int i = 0; i < 2641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2642(int& val) {
    const int x = 2642;
    std::cout << 2642;
    for (int i = 0; i < 2642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2643(int& val) {
    const int x = 2643;
    std::cout << 2643;
    for (int i = 0; i < 2643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2644(int& val) {
    const int x = 2644;
    std::cout << 2644;
    for (int i = 0; i < 2644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2645(int& val) {
    const int x = 2645;
    std::cout << 2645;
    for (int i = 0; i < 2645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2646(int& val) {
    const int x = 2646;
    std::cout << 2646;
    for (int i = 0; i < 2646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2647(int& val) {
    const int x = 2647;
    std::cout << 2647;
    for (int i = 0; i < 2647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2648(int& val) {
    const int x = 2648;
    std::cout << 2648;
    for (int i = 0; i < 2648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2649(int& val) {
    const int x = 2649;
    std::cout << 2649;
    for (int i = 0; i < 2649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2650(int& val) {
    const int x = 2650;
    std::cout << 2650;
    for (int i = 0; i < 2650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2651(int& val) {
    const int x = 2651;
    std::cout << 2651;
    for (int i = 0; i < 2651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2652(int& val) {
    const int x = 2652;
    std::cout << 2652;
    for (int i = 0; i < 2652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2653(int& val) {
    const int x = 2653;
    std::cout << 2653;
    for (int i = 0; i < 2653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2654(int& val) {
    const int x = 2654;
    std::cout << 2654;
    for (int i = 0; i < 2654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2655(int& val) {
    const int x = 2655;
    std::cout << 2655;
    for (int i = 0; i < 2655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2656(int& val) {
    const int x = 2656;
    std::cout << 2656;
    for (int i = 0; i < 2656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2657(int& val) {
    const int x = 2657;
    std::cout << 2657;
    for (int i = 0; i < 2657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2658(int& val) {
    const int x = 2658;
    std::cout << 2658;
    for (int i = 0; i < 2658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2659(int& val) {
    const int x = 2659;
    std::cout << 2659;
    for (int i = 0; i < 2659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2660(int& val) {
    const int x = 2660;
    std::cout << 2660;
    for (int i = 0; i < 2660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2661(int& val) {
    const int x = 2661;
    std::cout << 2661;
    for (int i = 0; i < 2661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2662(int& val) {
    const int x = 2662;
    std::cout << 2662;
    for (int i = 0; i < 2662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2663(int& val) {
    const int x = 2663;
    std::cout << 2663;
    for (int i = 0; i < 2663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2664(int& val) {
    const int x = 2664;
    std::cout << 2664;
    for (int i = 0; i < 2664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2665(int& val) {
    const int x = 2665;
    std::cout << 2665;
    for (int i = 0; i < 2665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2666(int& val) {
    const int x = 2666;
    std::cout << 2666;
    for (int i = 0; i < 2666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2667(int& val) {
    const int x = 2667;
    std::cout << 2667;
    for (int i = 0; i < 2667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2668(int& val) {
    const int x = 2668;
    std::cout << 2668;
    for (int i = 0; i < 2668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2669(int& val) {
    const int x = 2669;
    std::cout << 2669;
    for (int i = 0; i < 2669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2670(int& val) {
    const int x = 2670;
    std::cout << 2670;
    for (int i = 0; i < 2670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2671(int& val) {
    const int x = 2671;
    std::cout << 2671;
    for (int i = 0; i < 2671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2672(int& val) {
    const int x = 2672;
    std::cout << 2672;
    for (int i = 0; i < 2672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2673(int& val) {
    const int x = 2673;
    std::cout << 2673;
    for (int i = 0; i < 2673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2674(int& val) {
    const int x = 2674;
    std::cout << 2674;
    for (int i = 0; i < 2674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2675(int& val) {
    const int x = 2675;
    std::cout << 2675;
    for (int i = 0; i < 2675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2676(int& val) {
    const int x = 2676;
    std::cout << 2676;
    for (int i = 0; i < 2676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2677(int& val) {
    const int x = 2677;
    std::cout << 2677;
    for (int i = 0; i < 2677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2678(int& val) {
    const int x = 2678;
    std::cout << 2678;
    for (int i = 0; i < 2678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2679(int& val) {
    const int x = 2679;
    std::cout << 2679;
    for (int i = 0; i < 2679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2680(int& val) {
    const int x = 2680;
    std::cout << 2680;
    for (int i = 0; i < 2680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2681(int& val) {
    const int x = 2681;
    std::cout << 2681;
    for (int i = 0; i < 2681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2682(int& val) {
    const int x = 2682;
    std::cout << 2682;
    for (int i = 0; i < 2682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2683(int& val) {
    const int x = 2683;
    std::cout << 2683;
    for (int i = 0; i < 2683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2684(int& val) {
    const int x = 2684;
    std::cout << 2684;
    for (int i = 0; i < 2684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2685(int& val) {
    const int x = 2685;
    std::cout << 2685;
    for (int i = 0; i < 2685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2686(int& val) {
    const int x = 2686;
    std::cout << 2686;
    for (int i = 0; i < 2686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2687(int& val) {
    const int x = 2687;
    std::cout << 2687;
    for (int i = 0; i < 2687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2688(int& val) {
    const int x = 2688;
    std::cout << 2688;
    for (int i = 0; i < 2688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2689(int& val) {
    const int x = 2689;
    std::cout << 2689;
    for (int i = 0; i < 2689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2690(int& val) {
    const int x = 2690;
    std::cout << 2690;
    for (int i = 0; i < 2690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2691(int& val) {
    const int x = 2691;
    std::cout << 2691;
    for (int i = 0; i < 2691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2692(int& val) {
    const int x = 2692;
    std::cout << 2692;
    for (int i = 0; i < 2692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2693(int& val) {
    const int x = 2693;
    std::cout << 2693;
    for (int i = 0; i < 2693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2694(int& val) {
    const int x = 2694;
    std::cout << 2694;
    for (int i = 0; i < 2694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2695(int& val) {
    const int x = 2695;
    std::cout << 2695;
    for (int i = 0; i < 2695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2696(int& val) {
    const int x = 2696;
    std::cout << 2696;
    for (int i = 0; i < 2696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2697(int& val) {
    const int x = 2697;
    std::cout << 2697;
    for (int i = 0; i < 2697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2698(int& val) {
    const int x = 2698;
    std::cout << 2698;
    for (int i = 0; i < 2698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2699(int& val) {
    const int x = 2699;
    std::cout << 2699;
    for (int i = 0; i < 2699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2700(int& val) {
    const int x = 2700;
    std::cout << 2700;
    for (int i = 0; i < 2700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2701(int& val) {
    const int x = 2701;
    std::cout << 2701;
    for (int i = 0; i < 2701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2702(int& val) {
    const int x = 2702;
    std::cout << 2702;
    for (int i = 0; i < 2702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2703(int& val) {
    const int x = 2703;
    std::cout << 2703;
    for (int i = 0; i < 2703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2704(int& val) {
    const int x = 2704;
    std::cout << 2704;
    for (int i = 0; i < 2704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2705(int& val) {
    const int x = 2705;
    std::cout << 2705;
    for (int i = 0; i < 2705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2706(int& val) {
    const int x = 2706;
    std::cout << 2706;
    for (int i = 0; i < 2706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2707(int& val) {
    const int x = 2707;
    std::cout << 2707;
    for (int i = 0; i < 2707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2708(int& val) {
    const int x = 2708;
    std::cout << 2708;
    for (int i = 0; i < 2708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2709(int& val) {
    const int x = 2709;
    std::cout << 2709;
    for (int i = 0; i < 2709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2710(int& val) {
    const int x = 2710;
    std::cout << 2710;
    for (int i = 0; i < 2710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2711(int& val) {
    const int x = 2711;
    std::cout << 2711;
    for (int i = 0; i < 2711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2712(int& val) {
    const int x = 2712;
    std::cout << 2712;
    for (int i = 0; i < 2712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2713(int& val) {
    const int x = 2713;
    std::cout << 2713;
    for (int i = 0; i < 2713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2714(int& val) {
    const int x = 2714;
    std::cout << 2714;
    for (int i = 0; i < 2714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2715(int& val) {
    const int x = 2715;
    std::cout << 2715;
    for (int i = 0; i < 2715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2716(int& val) {
    const int x = 2716;
    std::cout << 2716;
    for (int i = 0; i < 2716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2717(int& val) {
    const int x = 2717;
    std::cout << 2717;
    for (int i = 0; i < 2717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2718(int& val) {
    const int x = 2718;
    std::cout << 2718;
    for (int i = 0; i < 2718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2719(int& val) {
    const int x = 2719;
    std::cout << 2719;
    for (int i = 0; i < 2719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2720(int& val) {
    const int x = 2720;
    std::cout << 2720;
    for (int i = 0; i < 2720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2721(int& val) {
    const int x = 2721;
    std::cout << 2721;
    for (int i = 0; i < 2721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2722(int& val) {
    const int x = 2722;
    std::cout << 2722;
    for (int i = 0; i < 2722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2723(int& val) {
    const int x = 2723;
    std::cout << 2723;
    for (int i = 0; i < 2723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2724(int& val) {
    const int x = 2724;
    std::cout << 2724;
    for (int i = 0; i < 2724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2725(int& val) {
    const int x = 2725;
    std::cout << 2725;
    for (int i = 0; i < 2725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2726(int& val) {
    const int x = 2726;
    std::cout << 2726;
    for (int i = 0; i < 2726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2727(int& val) {
    const int x = 2727;
    std::cout << 2727;
    for (int i = 0; i < 2727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2728(int& val) {
    const int x = 2728;
    std::cout << 2728;
    for (int i = 0; i < 2728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2729(int& val) {
    const int x = 2729;
    std::cout << 2729;
    for (int i = 0; i < 2729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2730(int& val) {
    const int x = 2730;
    std::cout << 2730;
    for (int i = 0; i < 2730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2731(int& val) {
    const int x = 2731;
    std::cout << 2731;
    for (int i = 0; i < 2731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2732(int& val) {
    const int x = 2732;
    std::cout << 2732;
    for (int i = 0; i < 2732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2733(int& val) {
    const int x = 2733;
    std::cout << 2733;
    for (int i = 0; i < 2733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2734(int& val) {
    const int x = 2734;
    std::cout << 2734;
    for (int i = 0; i < 2734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2735(int& val) {
    const int x = 2735;
    std::cout << 2735;
    for (int i = 0; i < 2735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2736(int& val) {
    const int x = 2736;
    std::cout << 2736;
    for (int i = 0; i < 2736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2737(int& val) {
    const int x = 2737;
    std::cout << 2737;
    for (int i = 0; i < 2737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2738(int& val) {
    const int x = 2738;
    std::cout << 2738;
    for (int i = 0; i < 2738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2739(int& val) {
    const int x = 2739;
    std::cout << 2739;
    for (int i = 0; i < 2739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2740(int& val) {
    const int x = 2740;
    std::cout << 2740;
    for (int i = 0; i < 2740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2741(int& val) {
    const int x = 2741;
    std::cout << 2741;
    for (int i = 0; i < 2741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2742(int& val) {
    const int x = 2742;
    std::cout << 2742;
    for (int i = 0; i < 2742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2743(int& val) {
    const int x = 2743;
    std::cout << 2743;
    for (int i = 0; i < 2743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2744(int& val) {
    const int x = 2744;
    std::cout << 2744;
    for (int i = 0; i < 2744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2745(int& val) {
    const int x = 2745;
    std::cout << 2745;
    for (int i = 0; i < 2745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2746(int& val) {
    const int x = 2746;
    std::cout << 2746;
    for (int i = 0; i < 2746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2747(int& val) {
    const int x = 2747;
    std::cout << 2747;
    for (int i = 0; i < 2747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2748(int& val) {
    const int x = 2748;
    std::cout << 2748;
    for (int i = 0; i < 2748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2749(int& val) {
    const int x = 2749;
    std::cout << 2749;
    for (int i = 0; i < 2749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2750(int& val) {
    const int x = 2750;
    std::cout << 2750;
    for (int i = 0; i < 2750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2751(int& val) {
    const int x = 2751;
    std::cout << 2751;
    for (int i = 0; i < 2751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2752(int& val) {
    const int x = 2752;
    std::cout << 2752;
    for (int i = 0; i < 2752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2753(int& val) {
    const int x = 2753;
    std::cout << 2753;
    for (int i = 0; i < 2753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2754(int& val) {
    const int x = 2754;
    std::cout << 2754;
    for (int i = 0; i < 2754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2755(int& val) {
    const int x = 2755;
    std::cout << 2755;
    for (int i = 0; i < 2755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2756(int& val) {
    const int x = 2756;
    std::cout << 2756;
    for (int i = 0; i < 2756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2757(int& val) {
    const int x = 2757;
    std::cout << 2757;
    for (int i = 0; i < 2757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2758(int& val) {
    const int x = 2758;
    std::cout << 2758;
    for (int i = 0; i < 2758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2759(int& val) {
    const int x = 2759;
    std::cout << 2759;
    for (int i = 0; i < 2759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2760(int& val) {
    const int x = 2760;
    std::cout << 2760;
    for (int i = 0; i < 2760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2761(int& val) {
    const int x = 2761;
    std::cout << 2761;
    for (int i = 0; i < 2761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2762(int& val) {
    const int x = 2762;
    std::cout << 2762;
    for (int i = 0; i < 2762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2763(int& val) {
    const int x = 2763;
    std::cout << 2763;
    for (int i = 0; i < 2763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2764(int& val) {
    const int x = 2764;
    std::cout << 2764;
    for (int i = 0; i < 2764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2765(int& val) {
    const int x = 2765;
    std::cout << 2765;
    for (int i = 0; i < 2765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2766(int& val) {
    const int x = 2766;
    std::cout << 2766;
    for (int i = 0; i < 2766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2767(int& val) {
    const int x = 2767;
    std::cout << 2767;
    for (int i = 0; i < 2767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2768(int& val) {
    const int x = 2768;
    std::cout << 2768;
    for (int i = 0; i < 2768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2769(int& val) {
    const int x = 2769;
    std::cout << 2769;
    for (int i = 0; i < 2769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2770(int& val) {
    const int x = 2770;
    std::cout << 2770;
    for (int i = 0; i < 2770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2771(int& val) {
    const int x = 2771;
    std::cout << 2771;
    for (int i = 0; i < 2771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2772(int& val) {
    const int x = 2772;
    std::cout << 2772;
    for (int i = 0; i < 2772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2773(int& val) {
    const int x = 2773;
    std::cout << 2773;
    for (int i = 0; i < 2773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2774(int& val) {
    const int x = 2774;
    std::cout << 2774;
    for (int i = 0; i < 2774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2775(int& val) {
    const int x = 2775;
    std::cout << 2775;
    for (int i = 0; i < 2775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2776(int& val) {
    const int x = 2776;
    std::cout << 2776;
    for (int i = 0; i < 2776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2777(int& val) {
    const int x = 2777;
    std::cout << 2777;
    for (int i = 0; i < 2777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2778(int& val) {
    const int x = 2778;
    std::cout << 2778;
    for (int i = 0; i < 2778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2779(int& val) {
    const int x = 2779;
    std::cout << 2779;
    for (int i = 0; i < 2779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2780(int& val) {
    const int x = 2780;
    std::cout << 2780;
    for (int i = 0; i < 2780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2781(int& val) {
    const int x = 2781;
    std::cout << 2781;
    for (int i = 0; i < 2781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2782(int& val) {
    const int x = 2782;
    std::cout << 2782;
    for (int i = 0; i < 2782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2783(int& val) {
    const int x = 2783;
    std::cout << 2783;
    for (int i = 0; i < 2783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2784(int& val) {
    const int x = 2784;
    std::cout << 2784;
    for (int i = 0; i < 2784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2785(int& val) {
    const int x = 2785;
    std::cout << 2785;
    for (int i = 0; i < 2785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2786(int& val) {
    const int x = 2786;
    std::cout << 2786;
    for (int i = 0; i < 2786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2787(int& val) {
    const int x = 2787;
    std::cout << 2787;
    for (int i = 0; i < 2787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2788(int& val) {
    const int x = 2788;
    std::cout << 2788;
    for (int i = 0; i < 2788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2789(int& val) {
    const int x = 2789;
    std::cout << 2789;
    for (int i = 0; i < 2789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2790(int& val) {
    const int x = 2790;
    std::cout << 2790;
    for (int i = 0; i < 2790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2791(int& val) {
    const int x = 2791;
    std::cout << 2791;
    for (int i = 0; i < 2791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2792(int& val) {
    const int x = 2792;
    std::cout << 2792;
    for (int i = 0; i < 2792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2793(int& val) {
    const int x = 2793;
    std::cout << 2793;
    for (int i = 0; i < 2793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2794(int& val) {
    const int x = 2794;
    std::cout << 2794;
    for (int i = 0; i < 2794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2795(int& val) {
    const int x = 2795;
    std::cout << 2795;
    for (int i = 0; i < 2795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2796(int& val) {
    const int x = 2796;
    std::cout << 2796;
    for (int i = 0; i < 2796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2797(int& val) {
    const int x = 2797;
    std::cout << 2797;
    for (int i = 0; i < 2797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2798(int& val) {
    const int x = 2798;
    std::cout << 2798;
    for (int i = 0; i < 2798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2799(int& val) {
    const int x = 2799;
    std::cout << 2799;
    for (int i = 0; i < 2799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2800(int& val) {
    const int x = 2800;
    std::cout << 2800;
    for (int i = 0; i < 2800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2801(int& val) {
    const int x = 2801;
    std::cout << 2801;
    for (int i = 0; i < 2801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2802(int& val) {
    const int x = 2802;
    std::cout << 2802;
    for (int i = 0; i < 2802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2803(int& val) {
    const int x = 2803;
    std::cout << 2803;
    for (int i = 0; i < 2803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2804(int& val) {
    const int x = 2804;
    std::cout << 2804;
    for (int i = 0; i < 2804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2805(int& val) {
    const int x = 2805;
    std::cout << 2805;
    for (int i = 0; i < 2805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2806(int& val) {
    const int x = 2806;
    std::cout << 2806;
    for (int i = 0; i < 2806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2807(int& val) {
    const int x = 2807;
    std::cout << 2807;
    for (int i = 0; i < 2807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2808(int& val) {
    const int x = 2808;
    std::cout << 2808;
    for (int i = 0; i < 2808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2809(int& val) {
    const int x = 2809;
    std::cout << 2809;
    for (int i = 0; i < 2809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2810(int& val) {
    const int x = 2810;
    std::cout << 2810;
    for (int i = 0; i < 2810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2811(int& val) {
    const int x = 2811;
    std::cout << 2811;
    for (int i = 0; i < 2811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2812(int& val) {
    const int x = 2812;
    std::cout << 2812;
    for (int i = 0; i < 2812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2813(int& val) {
    const int x = 2813;
    std::cout << 2813;
    for (int i = 0; i < 2813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2814(int& val) {
    const int x = 2814;
    std::cout << 2814;
    for (int i = 0; i < 2814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2815(int& val) {
    const int x = 2815;
    std::cout << 2815;
    for (int i = 0; i < 2815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2816(int& val) {
    const int x = 2816;
    std::cout << 2816;
    for (int i = 0; i < 2816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2817(int& val) {
    const int x = 2817;
    std::cout << 2817;
    for (int i = 0; i < 2817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2818(int& val) {
    const int x = 2818;
    std::cout << 2818;
    for (int i = 0; i < 2818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2819(int& val) {
    const int x = 2819;
    std::cout << 2819;
    for (int i = 0; i < 2819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2820(int& val) {
    const int x = 2820;
    std::cout << 2820;
    for (int i = 0; i < 2820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2821(int& val) {
    const int x = 2821;
    std::cout << 2821;
    for (int i = 0; i < 2821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2822(int& val) {
    const int x = 2822;
    std::cout << 2822;
    for (int i = 0; i < 2822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2823(int& val) {
    const int x = 2823;
    std::cout << 2823;
    for (int i = 0; i < 2823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2824(int& val) {
    const int x = 2824;
    std::cout << 2824;
    for (int i = 0; i < 2824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2825(int& val) {
    const int x = 2825;
    std::cout << 2825;
    for (int i = 0; i < 2825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2826(int& val) {
    const int x = 2826;
    std::cout << 2826;
    for (int i = 0; i < 2826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2827(int& val) {
    const int x = 2827;
    std::cout << 2827;
    for (int i = 0; i < 2827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2828(int& val) {
    const int x = 2828;
    std::cout << 2828;
    for (int i = 0; i < 2828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2829(int& val) {
    const int x = 2829;
    std::cout << 2829;
    for (int i = 0; i < 2829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2830(int& val) {
    const int x = 2830;
    std::cout << 2830;
    for (int i = 0; i < 2830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2831(int& val) {
    const int x = 2831;
    std::cout << 2831;
    for (int i = 0; i < 2831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2832(int& val) {
    const int x = 2832;
    std::cout << 2832;
    for (int i = 0; i < 2832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2833(int& val) {
    const int x = 2833;
    std::cout << 2833;
    for (int i = 0; i < 2833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2834(int& val) {
    const int x = 2834;
    std::cout << 2834;
    for (int i = 0; i < 2834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2835(int& val) {
    const int x = 2835;
    std::cout << 2835;
    for (int i = 0; i < 2835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2836(int& val) {
    const int x = 2836;
    std::cout << 2836;
    for (int i = 0; i < 2836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2837(int& val) {
    const int x = 2837;
    std::cout << 2837;
    for (int i = 0; i < 2837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2838(int& val) {
    const int x = 2838;
    std::cout << 2838;
    for (int i = 0; i < 2838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2839(int& val) {
    const int x = 2839;
    std::cout << 2839;
    for (int i = 0; i < 2839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2840(int& val) {
    const int x = 2840;
    std::cout << 2840;
    for (int i = 0; i < 2840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2841(int& val) {
    const int x = 2841;
    std::cout << 2841;
    for (int i = 0; i < 2841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2842(int& val) {
    const int x = 2842;
    std::cout << 2842;
    for (int i = 0; i < 2842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2843(int& val) {
    const int x = 2843;
    std::cout << 2843;
    for (int i = 0; i < 2843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2844(int& val) {
    const int x = 2844;
    std::cout << 2844;
    for (int i = 0; i < 2844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2845(int& val) {
    const int x = 2845;
    std::cout << 2845;
    for (int i = 0; i < 2845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2846(int& val) {
    const int x = 2846;
    std::cout << 2846;
    for (int i = 0; i < 2846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2847(int& val) {
    const int x = 2847;
    std::cout << 2847;
    for (int i = 0; i < 2847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2848(int& val) {
    const int x = 2848;
    std::cout << 2848;
    for (int i = 0; i < 2848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2849(int& val) {
    const int x = 2849;
    std::cout << 2849;
    for (int i = 0; i < 2849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2850(int& val) {
    const int x = 2850;
    std::cout << 2850;
    for (int i = 0; i < 2850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2851(int& val) {
    const int x = 2851;
    std::cout << 2851;
    for (int i = 0; i < 2851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2852(int& val) {
    const int x = 2852;
    std::cout << 2852;
    for (int i = 0; i < 2852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2853(int& val) {
    const int x = 2853;
    std::cout << 2853;
    for (int i = 0; i < 2853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2854(int& val) {
    const int x = 2854;
    std::cout << 2854;
    for (int i = 0; i < 2854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2855(int& val) {
    const int x = 2855;
    std::cout << 2855;
    for (int i = 0; i < 2855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2856(int& val) {
    const int x = 2856;
    std::cout << 2856;
    for (int i = 0; i < 2856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2857(int& val) {
    const int x = 2857;
    std::cout << 2857;
    for (int i = 0; i < 2857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2858(int& val) {
    const int x = 2858;
    std::cout << 2858;
    for (int i = 0; i < 2858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2859(int& val) {
    const int x = 2859;
    std::cout << 2859;
    for (int i = 0; i < 2859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2860(int& val) {
    const int x = 2860;
    std::cout << 2860;
    for (int i = 0; i < 2860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2861(int& val) {
    const int x = 2861;
    std::cout << 2861;
    for (int i = 0; i < 2861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2862(int& val) {
    const int x = 2862;
    std::cout << 2862;
    for (int i = 0; i < 2862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2863(int& val) {
    const int x = 2863;
    std::cout << 2863;
    for (int i = 0; i < 2863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2864(int& val) {
    const int x = 2864;
    std::cout << 2864;
    for (int i = 0; i < 2864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2865(int& val) {
    const int x = 2865;
    std::cout << 2865;
    for (int i = 0; i < 2865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2866(int& val) {
    const int x = 2866;
    std::cout << 2866;
    for (int i = 0; i < 2866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2867(int& val) {
    const int x = 2867;
    std::cout << 2867;
    for (int i = 0; i < 2867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2868(int& val) {
    const int x = 2868;
    std::cout << 2868;
    for (int i = 0; i < 2868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2869(int& val) {
    const int x = 2869;
    std::cout << 2869;
    for (int i = 0; i < 2869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2870(int& val) {
    const int x = 2870;
    std::cout << 2870;
    for (int i = 0; i < 2870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2871(int& val) {
    const int x = 2871;
    std::cout << 2871;
    for (int i = 0; i < 2871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2872(int& val) {
    const int x = 2872;
    std::cout << 2872;
    for (int i = 0; i < 2872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2873(int& val) {
    const int x = 2873;
    std::cout << 2873;
    for (int i = 0; i < 2873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2874(int& val) {
    const int x = 2874;
    std::cout << 2874;
    for (int i = 0; i < 2874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2875(int& val) {
    const int x = 2875;
    std::cout << 2875;
    for (int i = 0; i < 2875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2876(int& val) {
    const int x = 2876;
    std::cout << 2876;
    for (int i = 0; i < 2876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2877(int& val) {
    const int x = 2877;
    std::cout << 2877;
    for (int i = 0; i < 2877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2878(int& val) {
    const int x = 2878;
    std::cout << 2878;
    for (int i = 0; i < 2878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2879(int& val) {
    const int x = 2879;
    std::cout << 2879;
    for (int i = 0; i < 2879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2880(int& val) {
    const int x = 2880;
    std::cout << 2880;
    for (int i = 0; i < 2880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2881(int& val) {
    const int x = 2881;
    std::cout << 2881;
    for (int i = 0; i < 2881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2882(int& val) {
    const int x = 2882;
    std::cout << 2882;
    for (int i = 0; i < 2882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2883(int& val) {
    const int x = 2883;
    std::cout << 2883;
    for (int i = 0; i < 2883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2884(int& val) {
    const int x = 2884;
    std::cout << 2884;
    for (int i = 0; i < 2884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2885(int& val) {
    const int x = 2885;
    std::cout << 2885;
    for (int i = 0; i < 2885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2886(int& val) {
    const int x = 2886;
    std::cout << 2886;
    for (int i = 0; i < 2886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2887(int& val) {
    const int x = 2887;
    std::cout << 2887;
    for (int i = 0; i < 2887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2888(int& val) {
    const int x = 2888;
    std::cout << 2888;
    for (int i = 0; i < 2888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2889(int& val) {
    const int x = 2889;
    std::cout << 2889;
    for (int i = 0; i < 2889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2890(int& val) {
    const int x = 2890;
    std::cout << 2890;
    for (int i = 0; i < 2890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2891(int& val) {
    const int x = 2891;
    std::cout << 2891;
    for (int i = 0; i < 2891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2892(int& val) {
    const int x = 2892;
    std::cout << 2892;
    for (int i = 0; i < 2892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2893(int& val) {
    const int x = 2893;
    std::cout << 2893;
    for (int i = 0; i < 2893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2894(int& val) {
    const int x = 2894;
    std::cout << 2894;
    for (int i = 0; i < 2894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2895(int& val) {
    const int x = 2895;
    std::cout << 2895;
    for (int i = 0; i < 2895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2896(int& val) {
    const int x = 2896;
    std::cout << 2896;
    for (int i = 0; i < 2896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2897(int& val) {
    const int x = 2897;
    std::cout << 2897;
    for (int i = 0; i < 2897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2898(int& val) {
    const int x = 2898;
    std::cout << 2898;
    for (int i = 0; i < 2898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2899(int& val) {
    const int x = 2899;
    std::cout << 2899;
    for (int i = 0; i < 2899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2900(int& val) {
    const int x = 2900;
    std::cout << 2900;
    for (int i = 0; i < 2900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2901(int& val) {
    const int x = 2901;
    std::cout << 2901;
    for (int i = 0; i < 2901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2902(int& val) {
    const int x = 2902;
    std::cout << 2902;
    for (int i = 0; i < 2902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2903(int& val) {
    const int x = 2903;
    std::cout << 2903;
    for (int i = 0; i < 2903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2904(int& val) {
    const int x = 2904;
    std::cout << 2904;
    for (int i = 0; i < 2904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2905(int& val) {
    const int x = 2905;
    std::cout << 2905;
    for (int i = 0; i < 2905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2906(int& val) {
    const int x = 2906;
    std::cout << 2906;
    for (int i = 0; i < 2906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2907(int& val) {
    const int x = 2907;
    std::cout << 2907;
    for (int i = 0; i < 2907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2908(int& val) {
    const int x = 2908;
    std::cout << 2908;
    for (int i = 0; i < 2908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2909(int& val) {
    const int x = 2909;
    std::cout << 2909;
    for (int i = 0; i < 2909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2910(int& val) {
    const int x = 2910;
    std::cout << 2910;
    for (int i = 0; i < 2910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2911(int& val) {
    const int x = 2911;
    std::cout << 2911;
    for (int i = 0; i < 2911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2912(int& val) {
    const int x = 2912;
    std::cout << 2912;
    for (int i = 0; i < 2912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2913(int& val) {
    const int x = 2913;
    std::cout << 2913;
    for (int i = 0; i < 2913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2914(int& val) {
    const int x = 2914;
    std::cout << 2914;
    for (int i = 0; i < 2914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2915(int& val) {
    const int x = 2915;
    std::cout << 2915;
    for (int i = 0; i < 2915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2916(int& val) {
    const int x = 2916;
    std::cout << 2916;
    for (int i = 0; i < 2916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2917(int& val) {
    const int x = 2917;
    std::cout << 2917;
    for (int i = 0; i < 2917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2918(int& val) {
    const int x = 2918;
    std::cout << 2918;
    for (int i = 0; i < 2918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2919(int& val) {
    const int x = 2919;
    std::cout << 2919;
    for (int i = 0; i < 2919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2920(int& val) {
    const int x = 2920;
    std::cout << 2920;
    for (int i = 0; i < 2920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2921(int& val) {
    const int x = 2921;
    std::cout << 2921;
    for (int i = 0; i < 2921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2922(int& val) {
    const int x = 2922;
    std::cout << 2922;
    for (int i = 0; i < 2922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2923(int& val) {
    const int x = 2923;
    std::cout << 2923;
    for (int i = 0; i < 2923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2924(int& val) {
    const int x = 2924;
    std::cout << 2924;
    for (int i = 0; i < 2924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2925(int& val) {
    const int x = 2925;
    std::cout << 2925;
    for (int i = 0; i < 2925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2926(int& val) {
    const int x = 2926;
    std::cout << 2926;
    for (int i = 0; i < 2926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2927(int& val) {
    const int x = 2927;
    std::cout << 2927;
    for (int i = 0; i < 2927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2928(int& val) {
    const int x = 2928;
    std::cout << 2928;
    for (int i = 0; i < 2928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2929(int& val) {
    const int x = 2929;
    std::cout << 2929;
    for (int i = 0; i < 2929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2930(int& val) {
    const int x = 2930;
    std::cout << 2930;
    for (int i = 0; i < 2930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2931(int& val) {
    const int x = 2931;
    std::cout << 2931;
    for (int i = 0; i < 2931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2932(int& val) {
    const int x = 2932;
    std::cout << 2932;
    for (int i = 0; i < 2932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2933(int& val) {
    const int x = 2933;
    std::cout << 2933;
    for (int i = 0; i < 2933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2934(int& val) {
    const int x = 2934;
    std::cout << 2934;
    for (int i = 0; i < 2934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2935(int& val) {
    const int x = 2935;
    std::cout << 2935;
    for (int i = 0; i < 2935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2936(int& val) {
    const int x = 2936;
    std::cout << 2936;
    for (int i = 0; i < 2936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2937(int& val) {
    const int x = 2937;
    std::cout << 2937;
    for (int i = 0; i < 2937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2938(int& val) {
    const int x = 2938;
    std::cout << 2938;
    for (int i = 0; i < 2938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2939(int& val) {
    const int x = 2939;
    std::cout << 2939;
    for (int i = 0; i < 2939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2940(int& val) {
    const int x = 2940;
    std::cout << 2940;
    for (int i = 0; i < 2940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2941(int& val) {
    const int x = 2941;
    std::cout << 2941;
    for (int i = 0; i < 2941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2942(int& val) {
    const int x = 2942;
    std::cout << 2942;
    for (int i = 0; i < 2942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2943(int& val) {
    const int x = 2943;
    std::cout << 2943;
    for (int i = 0; i < 2943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2944(int& val) {
    const int x = 2944;
    std::cout << 2944;
    for (int i = 0; i < 2944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2945(int& val) {
    const int x = 2945;
    std::cout << 2945;
    for (int i = 0; i < 2945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2946(int& val) {
    const int x = 2946;
    std::cout << 2946;
    for (int i = 0; i < 2946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2947(int& val) {
    const int x = 2947;
    std::cout << 2947;
    for (int i = 0; i < 2947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2948(int& val) {
    const int x = 2948;
    std::cout << 2948;
    for (int i = 0; i < 2948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2949(int& val) {
    const int x = 2949;
    std::cout << 2949;
    for (int i = 0; i < 2949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2950(int& val) {
    const int x = 2950;
    std::cout << 2950;
    for (int i = 0; i < 2950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2951(int& val) {
    const int x = 2951;
    std::cout << 2951;
    for (int i = 0; i < 2951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2952(int& val) {
    const int x = 2952;
    std::cout << 2952;
    for (int i = 0; i < 2952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2953(int& val) {
    const int x = 2953;
    std::cout << 2953;
    for (int i = 0; i < 2953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2954(int& val) {
    const int x = 2954;
    std::cout << 2954;
    for (int i = 0; i < 2954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2955(int& val) {
    const int x = 2955;
    std::cout << 2955;
    for (int i = 0; i < 2955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2956(int& val) {
    const int x = 2956;
    std::cout << 2956;
    for (int i = 0; i < 2956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2957(int& val) {
    const int x = 2957;
    std::cout << 2957;
    for (int i = 0; i < 2957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2958(int& val) {
    const int x = 2958;
    std::cout << 2958;
    for (int i = 0; i < 2958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2959(int& val) {
    const int x = 2959;
    std::cout << 2959;
    for (int i = 0; i < 2959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2960(int& val) {
    const int x = 2960;
    std::cout << 2960;
    for (int i = 0; i < 2960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2961(int& val) {
    const int x = 2961;
    std::cout << 2961;
    for (int i = 0; i < 2961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2962(int& val) {
    const int x = 2962;
    std::cout << 2962;
    for (int i = 0; i < 2962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2963(int& val) {
    const int x = 2963;
    std::cout << 2963;
    for (int i = 0; i < 2963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2964(int& val) {
    const int x = 2964;
    std::cout << 2964;
    for (int i = 0; i < 2964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2965(int& val) {
    const int x = 2965;
    std::cout << 2965;
    for (int i = 0; i < 2965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2966(int& val) {
    const int x = 2966;
    std::cout << 2966;
    for (int i = 0; i < 2966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2967(int& val) {
    const int x = 2967;
    std::cout << 2967;
    for (int i = 0; i < 2967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2968(int& val) {
    const int x = 2968;
    std::cout << 2968;
    for (int i = 0; i < 2968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2969(int& val) {
    const int x = 2969;
    std::cout << 2969;
    for (int i = 0; i < 2969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2970(int& val) {
    const int x = 2970;
    std::cout << 2970;
    for (int i = 0; i < 2970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2971(int& val) {
    const int x = 2971;
    std::cout << 2971;
    for (int i = 0; i < 2971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2972(int& val) {
    const int x = 2972;
    std::cout << 2972;
    for (int i = 0; i < 2972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2973(int& val) {
    const int x = 2973;
    std::cout << 2973;
    for (int i = 0; i < 2973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2974(int& val) {
    const int x = 2974;
    std::cout << 2974;
    for (int i = 0; i < 2974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2975(int& val) {
    const int x = 2975;
    std::cout << 2975;
    for (int i = 0; i < 2975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2976(int& val) {
    const int x = 2976;
    std::cout << 2976;
    for (int i = 0; i < 2976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2977(int& val) {
    const int x = 2977;
    std::cout << 2977;
    for (int i = 0; i < 2977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2978(int& val) {
    const int x = 2978;
    std::cout << 2978;
    for (int i = 0; i < 2978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2979(int& val) {
    const int x = 2979;
    std::cout << 2979;
    for (int i = 0; i < 2979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2980(int& val) {
    const int x = 2980;
    std::cout << 2980;
    for (int i = 0; i < 2980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2981(int& val) {
    const int x = 2981;
    std::cout << 2981;
    for (int i = 0; i < 2981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2982(int& val) {
    const int x = 2982;
    std::cout << 2982;
    for (int i = 0; i < 2982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2983(int& val) {
    const int x = 2983;
    std::cout << 2983;
    for (int i = 0; i < 2983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2984(int& val) {
    const int x = 2984;
    std::cout << 2984;
    for (int i = 0; i < 2984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2985(int& val) {
    const int x = 2985;
    std::cout << 2985;
    for (int i = 0; i < 2985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2986(int& val) {
    const int x = 2986;
    std::cout << 2986;
    for (int i = 0; i < 2986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2987(int& val) {
    const int x = 2987;
    std::cout << 2987;
    for (int i = 0; i < 2987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2988(int& val) {
    const int x = 2988;
    std::cout << 2988;
    for (int i = 0; i < 2988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2989(int& val) {
    const int x = 2989;
    std::cout << 2989;
    for (int i = 0; i < 2989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2990(int& val) {
    const int x = 2990;
    std::cout << 2990;
    for (int i = 0; i < 2990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2991(int& val) {
    const int x = 2991;
    std::cout << 2991;
    for (int i = 0; i < 2991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2992(int& val) {
    const int x = 2992;
    std::cout << 2992;
    for (int i = 0; i < 2992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2993(int& val) {
    const int x = 2993;
    std::cout << 2993;
    for (int i = 0; i < 2993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2994(int& val) {
    const int x = 2994;
    std::cout << 2994;
    for (int i = 0; i < 2994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2995(int& val) {
    const int x = 2995;
    std::cout << 2995;
    for (int i = 0; i < 2995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2996(int& val) {
    const int x = 2996;
    std::cout << 2996;
    for (int i = 0; i < 2996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2997(int& val) {
    const int x = 2997;
    std::cout << 2997;
    for (int i = 0; i < 2997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2998(int& val) {
    const int x = 2998;
    std::cout << 2998;
    for (int i = 0; i < 2998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2999(int& val) {
    const int x = 2999;
    std::cout << 2999;
    for (int i = 0; i < 2999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3000(int& val) {
    const int x = 3000;
    std::cout << 3000;
    for (int i = 0; i < 3000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3001(int& val) {
    const int x = 3001;
    std::cout << 3001;
    for (int i = 0; i < 3001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3002(int& val) {
    const int x = 3002;
    std::cout << 3002;
    for (int i = 0; i < 3002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3003(int& val) {
    const int x = 3003;
    std::cout << 3003;
    for (int i = 0; i < 3003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3004(int& val) {
    const int x = 3004;
    std::cout << 3004;
    for (int i = 0; i < 3004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3005(int& val) {
    const int x = 3005;
    std::cout << 3005;
    for (int i = 0; i < 3005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3006(int& val) {
    const int x = 3006;
    std::cout << 3006;
    for (int i = 0; i < 3006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3007(int& val) {
    const int x = 3007;
    std::cout << 3007;
    for (int i = 0; i < 3007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3008(int& val) {
    const int x = 3008;
    std::cout << 3008;
    for (int i = 0; i < 3008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3009(int& val) {
    const int x = 3009;
    std::cout << 3009;
    for (int i = 0; i < 3009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3010(int& val) {
    const int x = 3010;
    std::cout << 3010;
    for (int i = 0; i < 3010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3011(int& val) {
    const int x = 3011;
    std::cout << 3011;
    for (int i = 0; i < 3011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3012(int& val) {
    const int x = 3012;
    std::cout << 3012;
    for (int i = 0; i < 3012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3013(int& val) {
    const int x = 3013;
    std::cout << 3013;
    for (int i = 0; i < 3013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3014(int& val) {
    const int x = 3014;
    std::cout << 3014;
    for (int i = 0; i < 3014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3015(int& val) {
    const int x = 3015;
    std::cout << 3015;
    for (int i = 0; i < 3015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3016(int& val) {
    const int x = 3016;
    std::cout << 3016;
    for (int i = 0; i < 3016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3017(int& val) {
    const int x = 3017;
    std::cout << 3017;
    for (int i = 0; i < 3017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3018(int& val) {
    const int x = 3018;
    std::cout << 3018;
    for (int i = 0; i < 3018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3019(int& val) {
    const int x = 3019;
    std::cout << 3019;
    for (int i = 0; i < 3019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3020(int& val) {
    const int x = 3020;
    std::cout << 3020;
    for (int i = 0; i < 3020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3021(int& val) {
    const int x = 3021;
    std::cout << 3021;
    for (int i = 0; i < 3021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3022(int& val) {
    const int x = 3022;
    std::cout << 3022;
    for (int i = 0; i < 3022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3023(int& val) {
    const int x = 3023;
    std::cout << 3023;
    for (int i = 0; i < 3023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3024(int& val) {
    const int x = 3024;
    std::cout << 3024;
    for (int i = 0; i < 3024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3025(int& val) {
    const int x = 3025;
    std::cout << 3025;
    for (int i = 0; i < 3025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3026(int& val) {
    const int x = 3026;
    std::cout << 3026;
    for (int i = 0; i < 3026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3027(int& val) {
    const int x = 3027;
    std::cout << 3027;
    for (int i = 0; i < 3027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3028(int& val) {
    const int x = 3028;
    std::cout << 3028;
    for (int i = 0; i < 3028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3029(int& val) {
    const int x = 3029;
    std::cout << 3029;
    for (int i = 0; i < 3029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3030(int& val) {
    const int x = 3030;
    std::cout << 3030;
    for (int i = 0; i < 3030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3031(int& val) {
    const int x = 3031;
    std::cout << 3031;
    for (int i = 0; i < 3031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3032(int& val) {
    const int x = 3032;
    std::cout << 3032;
    for (int i = 0; i < 3032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3033(int& val) {
    const int x = 3033;
    std::cout << 3033;
    for (int i = 0; i < 3033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3034(int& val) {
    const int x = 3034;
    std::cout << 3034;
    for (int i = 0; i < 3034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3035(int& val) {
    const int x = 3035;
    std::cout << 3035;
    for (int i = 0; i < 3035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3036(int& val) {
    const int x = 3036;
    std::cout << 3036;
    for (int i = 0; i < 3036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3037(int& val) {
    const int x = 3037;
    std::cout << 3037;
    for (int i = 0; i < 3037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3038(int& val) {
    const int x = 3038;
    std::cout << 3038;
    for (int i = 0; i < 3038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3039(int& val) {
    const int x = 3039;
    std::cout << 3039;
    for (int i = 0; i < 3039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3040(int& val) {
    const int x = 3040;
    std::cout << 3040;
    for (int i = 0; i < 3040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3041(int& val) {
    const int x = 3041;
    std::cout << 3041;
    for (int i = 0; i < 3041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3042(int& val) {
    const int x = 3042;
    std::cout << 3042;
    for (int i = 0; i < 3042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3043(int& val) {
    const int x = 3043;
    std::cout << 3043;
    for (int i = 0; i < 3043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3044(int& val) {
    const int x = 3044;
    std::cout << 3044;
    for (int i = 0; i < 3044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3045(int& val) {
    const int x = 3045;
    std::cout << 3045;
    for (int i = 0; i < 3045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3046(int& val) {
    const int x = 3046;
    std::cout << 3046;
    for (int i = 0; i < 3046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3047(int& val) {
    const int x = 3047;
    std::cout << 3047;
    for (int i = 0; i < 3047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3048(int& val) {
    const int x = 3048;
    std::cout << 3048;
    for (int i = 0; i < 3048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3049(int& val) {
    const int x = 3049;
    std::cout << 3049;
    for (int i = 0; i < 3049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3050(int& val) {
    const int x = 3050;
    std::cout << 3050;
    for (int i = 0; i < 3050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3051(int& val) {
    const int x = 3051;
    std::cout << 3051;
    for (int i = 0; i < 3051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3052(int& val) {
    const int x = 3052;
    std::cout << 3052;
    for (int i = 0; i < 3052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3053(int& val) {
    const int x = 3053;
    std::cout << 3053;
    for (int i = 0; i < 3053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3054(int& val) {
    const int x = 3054;
    std::cout << 3054;
    for (int i = 0; i < 3054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3055(int& val) {
    const int x = 3055;
    std::cout << 3055;
    for (int i = 0; i < 3055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3056(int& val) {
    const int x = 3056;
    std::cout << 3056;
    for (int i = 0; i < 3056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3057(int& val) {
    const int x = 3057;
    std::cout << 3057;
    for (int i = 0; i < 3057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3058(int& val) {
    const int x = 3058;
    std::cout << 3058;
    for (int i = 0; i < 3058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3059(int& val) {
    const int x = 3059;
    std::cout << 3059;
    for (int i = 0; i < 3059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3060(int& val) {
    const int x = 3060;
    std::cout << 3060;
    for (int i = 0; i < 3060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3061(int& val) {
    const int x = 3061;
    std::cout << 3061;
    for (int i = 0; i < 3061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3062(int& val) {
    const int x = 3062;
    std::cout << 3062;
    for (int i = 0; i < 3062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3063(int& val) {
    const int x = 3063;
    std::cout << 3063;
    for (int i = 0; i < 3063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3064(int& val) {
    const int x = 3064;
    std::cout << 3064;
    for (int i = 0; i < 3064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3065(int& val) {
    const int x = 3065;
    std::cout << 3065;
    for (int i = 0; i < 3065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3066(int& val) {
    const int x = 3066;
    std::cout << 3066;
    for (int i = 0; i < 3066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3067(int& val) {
    const int x = 3067;
    std::cout << 3067;
    for (int i = 0; i < 3067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3068(int& val) {
    const int x = 3068;
    std::cout << 3068;
    for (int i = 0; i < 3068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3069(int& val) {
    const int x = 3069;
    std::cout << 3069;
    for (int i = 0; i < 3069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3070(int& val) {
    const int x = 3070;
    std::cout << 3070;
    for (int i = 0; i < 3070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3071(int& val) {
    const int x = 3071;
    std::cout << 3071;
    for (int i = 0; i < 3071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3072(int& val) {
    const int x = 3072;
    std::cout << 3072;
    for (int i = 0; i < 3072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3073(int& val) {
    const int x = 3073;
    std::cout << 3073;
    for (int i = 0; i < 3073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3074(int& val) {
    const int x = 3074;
    std::cout << 3074;
    for (int i = 0; i < 3074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3075(int& val) {
    const int x = 3075;
    std::cout << 3075;
    for (int i = 0; i < 3075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3076(int& val) {
    const int x = 3076;
    std::cout << 3076;
    for (int i = 0; i < 3076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3077(int& val) {
    const int x = 3077;
    std::cout << 3077;
    for (int i = 0; i < 3077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3078(int& val) {
    const int x = 3078;
    std::cout << 3078;
    for (int i = 0; i < 3078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3079(int& val) {
    const int x = 3079;
    std::cout << 3079;
    for (int i = 0; i < 3079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3080(int& val) {
    const int x = 3080;
    std::cout << 3080;
    for (int i = 0; i < 3080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3081(int& val) {
    const int x = 3081;
    std::cout << 3081;
    for (int i = 0; i < 3081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3082(int& val) {
    const int x = 3082;
    std::cout << 3082;
    for (int i = 0; i < 3082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3083(int& val) {
    const int x = 3083;
    std::cout << 3083;
    for (int i = 0; i < 3083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3084(int& val) {
    const int x = 3084;
    std::cout << 3084;
    for (int i = 0; i < 3084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3085(int& val) {
    const int x = 3085;
    std::cout << 3085;
    for (int i = 0; i < 3085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3086(int& val) {
    const int x = 3086;
    std::cout << 3086;
    for (int i = 0; i < 3086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3087(int& val) {
    const int x = 3087;
    std::cout << 3087;
    for (int i = 0; i < 3087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3088(int& val) {
    const int x = 3088;
    std::cout << 3088;
    for (int i = 0; i < 3088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3089(int& val) {
    const int x = 3089;
    std::cout << 3089;
    for (int i = 0; i < 3089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3090(int& val) {
    const int x = 3090;
    std::cout << 3090;
    for (int i = 0; i < 3090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3091(int& val) {
    const int x = 3091;
    std::cout << 3091;
    for (int i = 0; i < 3091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3092(int& val) {
    const int x = 3092;
    std::cout << 3092;
    for (int i = 0; i < 3092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3093(int& val) {
    const int x = 3093;
    std::cout << 3093;
    for (int i = 0; i < 3093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3094(int& val) {
    const int x = 3094;
    std::cout << 3094;
    for (int i = 0; i < 3094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3095(int& val) {
    const int x = 3095;
    std::cout << 3095;
    for (int i = 0; i < 3095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3096(int& val) {
    const int x = 3096;
    std::cout << 3096;
    for (int i = 0; i < 3096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3097(int& val) {
    const int x = 3097;
    std::cout << 3097;
    for (int i = 0; i < 3097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3098(int& val) {
    const int x = 3098;
    std::cout << 3098;
    for (int i = 0; i < 3098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3099(int& val) {
    const int x = 3099;
    std::cout << 3099;
    for (int i = 0; i < 3099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3100(int& val) {
    const int x = 3100;
    std::cout << 3100;
    for (int i = 0; i < 3100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3101(int& val) {
    const int x = 3101;
    std::cout << 3101;
    for (int i = 0; i < 3101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3102(int& val) {
    const int x = 3102;
    std::cout << 3102;
    for (int i = 0; i < 3102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3103(int& val) {
    const int x = 3103;
    std::cout << 3103;
    for (int i = 0; i < 3103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3104(int& val) {
    const int x = 3104;
    std::cout << 3104;
    for (int i = 0; i < 3104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3105(int& val) {
    const int x = 3105;
    std::cout << 3105;
    for (int i = 0; i < 3105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3106(int& val) {
    const int x = 3106;
    std::cout << 3106;
    for (int i = 0; i < 3106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3107(int& val) {
    const int x = 3107;
    std::cout << 3107;
    for (int i = 0; i < 3107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3108(int& val) {
    const int x = 3108;
    std::cout << 3108;
    for (int i = 0; i < 3108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3109(int& val) {
    const int x = 3109;
    std::cout << 3109;
    for (int i = 0; i < 3109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3110(int& val) {
    const int x = 3110;
    std::cout << 3110;
    for (int i = 0; i < 3110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3111(int& val) {
    const int x = 3111;
    std::cout << 3111;
    for (int i = 0; i < 3111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3112(int& val) {
    const int x = 3112;
    std::cout << 3112;
    for (int i = 0; i < 3112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3113(int& val) {
    const int x = 3113;
    std::cout << 3113;
    for (int i = 0; i < 3113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3114(int& val) {
    const int x = 3114;
    std::cout << 3114;
    for (int i = 0; i < 3114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3115(int& val) {
    const int x = 3115;
    std::cout << 3115;
    for (int i = 0; i < 3115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3116(int& val) {
    const int x = 3116;
    std::cout << 3116;
    for (int i = 0; i < 3116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3117(int& val) {
    const int x = 3117;
    std::cout << 3117;
    for (int i = 0; i < 3117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3118(int& val) {
    const int x = 3118;
    std::cout << 3118;
    for (int i = 0; i < 3118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3119(int& val) {
    const int x = 3119;
    std::cout << 3119;
    for (int i = 0; i < 3119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3120(int& val) {
    const int x = 3120;
    std::cout << 3120;
    for (int i = 0; i < 3120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3121(int& val) {
    const int x = 3121;
    std::cout << 3121;
    for (int i = 0; i < 3121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3122(int& val) {
    const int x = 3122;
    std::cout << 3122;
    for (int i = 0; i < 3122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3123(int& val) {
    const int x = 3123;
    std::cout << 3123;
    for (int i = 0; i < 3123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3124(int& val) {
    const int x = 3124;
    std::cout << 3124;
    for (int i = 0; i < 3124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3125(int& val) {
    const int x = 3125;
    std::cout << 3125;
    for (int i = 0; i < 3125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3126(int& val) {
    const int x = 3126;
    std::cout << 3126;
    for (int i = 0; i < 3126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3127(int& val) {
    const int x = 3127;
    std::cout << 3127;
    for (int i = 0; i < 3127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3128(int& val) {
    const int x = 3128;
    std::cout << 3128;
    for (int i = 0; i < 3128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3129(int& val) {
    const int x = 3129;
    std::cout << 3129;
    for (int i = 0; i < 3129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3130(int& val) {
    const int x = 3130;
    std::cout << 3130;
    for (int i = 0; i < 3130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3131(int& val) {
    const int x = 3131;
    std::cout << 3131;
    for (int i = 0; i < 3131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3132(int& val) {
    const int x = 3132;
    std::cout << 3132;
    for (int i = 0; i < 3132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3133(int& val) {
    const int x = 3133;
    std::cout << 3133;
    for (int i = 0; i < 3133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3134(int& val) {
    const int x = 3134;
    std::cout << 3134;
    for (int i = 0; i < 3134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3135(int& val) {
    const int x = 3135;
    std::cout << 3135;
    for (int i = 0; i < 3135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3136(int& val) {
    const int x = 3136;
    std::cout << 3136;
    for (int i = 0; i < 3136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3137(int& val) {
    const int x = 3137;
    std::cout << 3137;
    for (int i = 0; i < 3137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3138(int& val) {
    const int x = 3138;
    std::cout << 3138;
    for (int i = 0; i < 3138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3139(int& val) {
    const int x = 3139;
    std::cout << 3139;
    for (int i = 0; i < 3139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3140(int& val) {
    const int x = 3140;
    std::cout << 3140;
    for (int i = 0; i < 3140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3141(int& val) {
    const int x = 3141;
    std::cout << 3141;
    for (int i = 0; i < 3141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3142(int& val) {
    const int x = 3142;
    std::cout << 3142;
    for (int i = 0; i < 3142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3143(int& val) {
    const int x = 3143;
    std::cout << 3143;
    for (int i = 0; i < 3143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3144(int& val) {
    const int x = 3144;
    std::cout << 3144;
    for (int i = 0; i < 3144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3145(int& val) {
    const int x = 3145;
    std::cout << 3145;
    for (int i = 0; i < 3145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3146(int& val) {
    const int x = 3146;
    std::cout << 3146;
    for (int i = 0; i < 3146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3147(int& val) {
    const int x = 3147;
    std::cout << 3147;
    for (int i = 0; i < 3147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3148(int& val) {
    const int x = 3148;
    std::cout << 3148;
    for (int i = 0; i < 3148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3149(int& val) {
    const int x = 3149;
    std::cout << 3149;
    for (int i = 0; i < 3149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3150(int& val) {
    const int x = 3150;
    std::cout << 3150;
    for (int i = 0; i < 3150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3151(int& val) {
    const int x = 3151;
    std::cout << 3151;
    for (int i = 0; i < 3151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3152(int& val) {
    const int x = 3152;
    std::cout << 3152;
    for (int i = 0; i < 3152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3153(int& val) {
    const int x = 3153;
    std::cout << 3153;
    for (int i = 0; i < 3153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3154(int& val) {
    const int x = 3154;
    std::cout << 3154;
    for (int i = 0; i < 3154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3155(int& val) {
    const int x = 3155;
    std::cout << 3155;
    for (int i = 0; i < 3155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3156(int& val) {
    const int x = 3156;
    std::cout << 3156;
    for (int i = 0; i < 3156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3157(int& val) {
    const int x = 3157;
    std::cout << 3157;
    for (int i = 0; i < 3157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3158(int& val) {
    const int x = 3158;
    std::cout << 3158;
    for (int i = 0; i < 3158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3159(int& val) {
    const int x = 3159;
    std::cout << 3159;
    for (int i = 0; i < 3159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3160(int& val) {
    const int x = 3160;
    std::cout << 3160;
    for (int i = 0; i < 3160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3161(int& val) {
    const int x = 3161;
    std::cout << 3161;
    for (int i = 0; i < 3161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3162(int& val) {
    const int x = 3162;
    std::cout << 3162;
    for (int i = 0; i < 3162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3163(int& val) {
    const int x = 3163;
    std::cout << 3163;
    for (int i = 0; i < 3163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3164(int& val) {
    const int x = 3164;
    std::cout << 3164;
    for (int i = 0; i < 3164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3165(int& val) {
    const int x = 3165;
    std::cout << 3165;
    for (int i = 0; i < 3165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3166(int& val) {
    const int x = 3166;
    std::cout << 3166;
    for (int i = 0; i < 3166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3167(int& val) {
    const int x = 3167;
    std::cout << 3167;
    for (int i = 0; i < 3167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3168(int& val) {
    const int x = 3168;
    std::cout << 3168;
    for (int i = 0; i < 3168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3169(int& val) {
    const int x = 3169;
    std::cout << 3169;
    for (int i = 0; i < 3169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3170(int& val) {
    const int x = 3170;
    std::cout << 3170;
    for (int i = 0; i < 3170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3171(int& val) {
    const int x = 3171;
    std::cout << 3171;
    for (int i = 0; i < 3171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3172(int& val) {
    const int x = 3172;
    std::cout << 3172;
    for (int i = 0; i < 3172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3173(int& val) {
    const int x = 3173;
    std::cout << 3173;
    for (int i = 0; i < 3173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3174(int& val) {
    const int x = 3174;
    std::cout << 3174;
    for (int i = 0; i < 3174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3175(int& val) {
    const int x = 3175;
    std::cout << 3175;
    for (int i = 0; i < 3175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3176(int& val) {
    const int x = 3176;
    std::cout << 3176;
    for (int i = 0; i < 3176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3177(int& val) {
    const int x = 3177;
    std::cout << 3177;
    for (int i = 0; i < 3177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3178(int& val) {
    const int x = 3178;
    std::cout << 3178;
    for (int i = 0; i < 3178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3179(int& val) {
    const int x = 3179;
    std::cout << 3179;
    for (int i = 0; i < 3179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3180(int& val) {
    const int x = 3180;
    std::cout << 3180;
    for (int i = 0; i < 3180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3181(int& val) {
    const int x = 3181;
    std::cout << 3181;
    for (int i = 0; i < 3181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3182(int& val) {
    const int x = 3182;
    std::cout << 3182;
    for (int i = 0; i < 3182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3183(int& val) {
    const int x = 3183;
    std::cout << 3183;
    for (int i = 0; i < 3183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3184(int& val) {
    const int x = 3184;
    std::cout << 3184;
    for (int i = 0; i < 3184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3185(int& val) {
    const int x = 3185;
    std::cout << 3185;
    for (int i = 0; i < 3185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3186(int& val) {
    const int x = 3186;
    std::cout << 3186;
    for (int i = 0; i < 3186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3187(int& val) {
    const int x = 3187;
    std::cout << 3187;
    for (int i = 0; i < 3187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3188(int& val) {
    const int x = 3188;
    std::cout << 3188;
    for (int i = 0; i < 3188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3189(int& val) {
    const int x = 3189;
    std::cout << 3189;
    for (int i = 0; i < 3189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3190(int& val) {
    const int x = 3190;
    std::cout << 3190;
    for (int i = 0; i < 3190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3191(int& val) {
    const int x = 3191;
    std::cout << 3191;
    for (int i = 0; i < 3191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3192(int& val) {
    const int x = 3192;
    std::cout << 3192;
    for (int i = 0; i < 3192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3193(int& val) {
    const int x = 3193;
    std::cout << 3193;
    for (int i = 0; i < 3193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3194(int& val) {
    const int x = 3194;
    std::cout << 3194;
    for (int i = 0; i < 3194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3195(int& val) {
    const int x = 3195;
    std::cout << 3195;
    for (int i = 0; i < 3195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3196(int& val) {
    const int x = 3196;
    std::cout << 3196;
    for (int i = 0; i < 3196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3197(int& val) {
    const int x = 3197;
    std::cout << 3197;
    for (int i = 0; i < 3197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3198(int& val) {
    const int x = 3198;
    std::cout << 3198;
    for (int i = 0; i < 3198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3199(int& val) {
    const int x = 3199;
    std::cout << 3199;
    for (int i = 0; i < 3199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3200(int& val) {
    const int x = 3200;
    std::cout << 3200;
    for (int i = 0; i < 3200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3201(int& val) {
    const int x = 3201;
    std::cout << 3201;
    for (int i = 0; i < 3201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3202(int& val) {
    const int x = 3202;
    std::cout << 3202;
    for (int i = 0; i < 3202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3203(int& val) {
    const int x = 3203;
    std::cout << 3203;
    for (int i = 0; i < 3203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3204(int& val) {
    const int x = 3204;
    std::cout << 3204;
    for (int i = 0; i < 3204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3205(int& val) {
    const int x = 3205;
    std::cout << 3205;
    for (int i = 0; i < 3205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3206(int& val) {
    const int x = 3206;
    std::cout << 3206;
    for (int i = 0; i < 3206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3207(int& val) {
    const int x = 3207;
    std::cout << 3207;
    for (int i = 0; i < 3207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3208(int& val) {
    const int x = 3208;
    std::cout << 3208;
    for (int i = 0; i < 3208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3209(int& val) {
    const int x = 3209;
    std::cout << 3209;
    for (int i = 0; i < 3209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3210(int& val) {
    const int x = 3210;
    std::cout << 3210;
    for (int i = 0; i < 3210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3211(int& val) {
    const int x = 3211;
    std::cout << 3211;
    for (int i = 0; i < 3211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3212(int& val) {
    const int x = 3212;
    std::cout << 3212;
    for (int i = 0; i < 3212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3213(int& val) {
    const int x = 3213;
    std::cout << 3213;
    for (int i = 0; i < 3213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3214(int& val) {
    const int x = 3214;
    std::cout << 3214;
    for (int i = 0; i < 3214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3215(int& val) {
    const int x = 3215;
    std::cout << 3215;
    for (int i = 0; i < 3215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3216(int& val) {
    const int x = 3216;
    std::cout << 3216;
    for (int i = 0; i < 3216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3217(int& val) {
    const int x = 3217;
    std::cout << 3217;
    for (int i = 0; i < 3217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3218(int& val) {
    const int x = 3218;
    std::cout << 3218;
    for (int i = 0; i < 3218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3219(int& val) {
    const int x = 3219;
    std::cout << 3219;
    for (int i = 0; i < 3219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3220(int& val) {
    const int x = 3220;
    std::cout << 3220;
    for (int i = 0; i < 3220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3221(int& val) {
    const int x = 3221;
    std::cout << 3221;
    for (int i = 0; i < 3221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3222(int& val) {
    const int x = 3222;
    std::cout << 3222;
    for (int i = 0; i < 3222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3223(int& val) {
    const int x = 3223;
    std::cout << 3223;
    for (int i = 0; i < 3223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3224(int& val) {
    const int x = 3224;
    std::cout << 3224;
    for (int i = 0; i < 3224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3225(int& val) {
    const int x = 3225;
    std::cout << 3225;
    for (int i = 0; i < 3225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3226(int& val) {
    const int x = 3226;
    std::cout << 3226;
    for (int i = 0; i < 3226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3227(int& val) {
    const int x = 3227;
    std::cout << 3227;
    for (int i = 0; i < 3227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3228(int& val) {
    const int x = 3228;
    std::cout << 3228;
    for (int i = 0; i < 3228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3229(int& val) {
    const int x = 3229;
    std::cout << 3229;
    for (int i = 0; i < 3229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3230(int& val) {
    const int x = 3230;
    std::cout << 3230;
    for (int i = 0; i < 3230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3231(int& val) {
    const int x = 3231;
    std::cout << 3231;
    for (int i = 0; i < 3231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3232(int& val) {
    const int x = 3232;
    std::cout << 3232;
    for (int i = 0; i < 3232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3233(int& val) {
    const int x = 3233;
    std::cout << 3233;
    for (int i = 0; i < 3233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3234(int& val) {
    const int x = 3234;
    std::cout << 3234;
    for (int i = 0; i < 3234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3235(int& val) {
    const int x = 3235;
    std::cout << 3235;
    for (int i = 0; i < 3235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3236(int& val) {
    const int x = 3236;
    std::cout << 3236;
    for (int i = 0; i < 3236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3237(int& val) {
    const int x = 3237;
    std::cout << 3237;
    for (int i = 0; i < 3237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3238(int& val) {
    const int x = 3238;
    std::cout << 3238;
    for (int i = 0; i < 3238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3239(int& val) {
    const int x = 3239;
    std::cout << 3239;
    for (int i = 0; i < 3239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3240(int& val) {
    const int x = 3240;
    std::cout << 3240;
    for (int i = 0; i < 3240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3241(int& val) {
    const int x = 3241;
    std::cout << 3241;
    for (int i = 0; i < 3241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3242(int& val) {
    const int x = 3242;
    std::cout << 3242;
    for (int i = 0; i < 3242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3243(int& val) {
    const int x = 3243;
    std::cout << 3243;
    for (int i = 0; i < 3243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3244(int& val) {
    const int x = 3244;
    std::cout << 3244;
    for (int i = 0; i < 3244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3245(int& val) {
    const int x = 3245;
    std::cout << 3245;
    for (int i = 0; i < 3245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3246(int& val) {
    const int x = 3246;
    std::cout << 3246;
    for (int i = 0; i < 3246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3247(int& val) {
    const int x = 3247;
    std::cout << 3247;
    for (int i = 0; i < 3247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3248(int& val) {
    const int x = 3248;
    std::cout << 3248;
    for (int i = 0; i < 3248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3249(int& val) {
    const int x = 3249;
    std::cout << 3249;
    for (int i = 0; i < 3249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3250(int& val) {
    const int x = 3250;
    std::cout << 3250;
    for (int i = 0; i < 3250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3251(int& val) {
    const int x = 3251;
    std::cout << 3251;
    for (int i = 0; i < 3251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3252(int& val) {
    const int x = 3252;
    std::cout << 3252;
    for (int i = 0; i < 3252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3253(int& val) {
    const int x = 3253;
    std::cout << 3253;
    for (int i = 0; i < 3253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3254(int& val) {
    const int x = 3254;
    std::cout << 3254;
    for (int i = 0; i < 3254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3255(int& val) {
    const int x = 3255;
    std::cout << 3255;
    for (int i = 0; i < 3255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3256(int& val) {
    const int x = 3256;
    std::cout << 3256;
    for (int i = 0; i < 3256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3257(int& val) {
    const int x = 3257;
    std::cout << 3257;
    for (int i = 0; i < 3257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3258(int& val) {
    const int x = 3258;
    std::cout << 3258;
    for (int i = 0; i < 3258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3259(int& val) {
    const int x = 3259;
    std::cout << 3259;
    for (int i = 0; i < 3259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3260(int& val) {
    const int x = 3260;
    std::cout << 3260;
    for (int i = 0; i < 3260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3261(int& val) {
    const int x = 3261;
    std::cout << 3261;
    for (int i = 0; i < 3261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3262(int& val) {
    const int x = 3262;
    std::cout << 3262;
    for (int i = 0; i < 3262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3263(int& val) {
    const int x = 3263;
    std::cout << 3263;
    for (int i = 0; i < 3263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3264(int& val) {
    const int x = 3264;
    std::cout << 3264;
    for (int i = 0; i < 3264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3265(int& val) {
    const int x = 3265;
    std::cout << 3265;
    for (int i = 0; i < 3265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3266(int& val) {
    const int x = 3266;
    std::cout << 3266;
    for (int i = 0; i < 3266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3267(int& val) {
    const int x = 3267;
    std::cout << 3267;
    for (int i = 0; i < 3267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3268(int& val) {
    const int x = 3268;
    std::cout << 3268;
    for (int i = 0; i < 3268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3269(int& val) {
    const int x = 3269;
    std::cout << 3269;
    for (int i = 0; i < 3269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3270(int& val) {
    const int x = 3270;
    std::cout << 3270;
    for (int i = 0; i < 3270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3271(int& val) {
    const int x = 3271;
    std::cout << 3271;
    for (int i = 0; i < 3271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3272(int& val) {
    const int x = 3272;
    std::cout << 3272;
    for (int i = 0; i < 3272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3273(int& val) {
    const int x = 3273;
    std::cout << 3273;
    for (int i = 0; i < 3273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3274(int& val) {
    const int x = 3274;
    std::cout << 3274;
    for (int i = 0; i < 3274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3275(int& val) {
    const int x = 3275;
    std::cout << 3275;
    for (int i = 0; i < 3275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3276(int& val) {
    const int x = 3276;
    std::cout << 3276;
    for (int i = 0; i < 3276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3277(int& val) {
    const int x = 3277;
    std::cout << 3277;
    for (int i = 0; i < 3277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3278(int& val) {
    const int x = 3278;
    std::cout << 3278;
    for (int i = 0; i < 3278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3279(int& val) {
    const int x = 3279;
    std::cout << 3279;
    for (int i = 0; i < 3279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3280(int& val) {
    const int x = 3280;
    std::cout << 3280;
    for (int i = 0; i < 3280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3281(int& val) {
    const int x = 3281;
    std::cout << 3281;
    for (int i = 0; i < 3281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3282(int& val) {
    const int x = 3282;
    std::cout << 3282;
    for (int i = 0; i < 3282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3283(int& val) {
    const int x = 3283;
    std::cout << 3283;
    for (int i = 0; i < 3283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3284(int& val) {
    const int x = 3284;
    std::cout << 3284;
    for (int i = 0; i < 3284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3285(int& val) {
    const int x = 3285;
    std::cout << 3285;
    for (int i = 0; i < 3285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3286(int& val) {
    const int x = 3286;
    std::cout << 3286;
    for (int i = 0; i < 3286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3287(int& val) {
    const int x = 3287;
    std::cout << 3287;
    for (int i = 0; i < 3287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3288(int& val) {
    const int x = 3288;
    std::cout << 3288;
    for (int i = 0; i < 3288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3289(int& val) {
    const int x = 3289;
    std::cout << 3289;
    for (int i = 0; i < 3289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3290(int& val) {
    const int x = 3290;
    std::cout << 3290;
    for (int i = 0; i < 3290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3291(int& val) {
    const int x = 3291;
    std::cout << 3291;
    for (int i = 0; i < 3291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3292(int& val) {
    const int x = 3292;
    std::cout << 3292;
    for (int i = 0; i < 3292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3293(int& val) {
    const int x = 3293;
    std::cout << 3293;
    for (int i = 0; i < 3293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3294(int& val) {
    const int x = 3294;
    std::cout << 3294;
    for (int i = 0; i < 3294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3295(int& val) {
    const int x = 3295;
    std::cout << 3295;
    for (int i = 0; i < 3295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3296(int& val) {
    const int x = 3296;
    std::cout << 3296;
    for (int i = 0; i < 3296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3297(int& val) {
    const int x = 3297;
    std::cout << 3297;
    for (int i = 0; i < 3297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3298(int& val) {
    const int x = 3298;
    std::cout << 3298;
    for (int i = 0; i < 3298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3299(int& val) {
    const int x = 3299;
    std::cout << 3299;
    for (int i = 0; i < 3299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3300(int& val) {
    const int x = 3300;
    std::cout << 3300;
    for (int i = 0; i < 3300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3301(int& val) {
    const int x = 3301;
    std::cout << 3301;
    for (int i = 0; i < 3301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3302(int& val) {
    const int x = 3302;
    std::cout << 3302;
    for (int i = 0; i < 3302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3303(int& val) {
    const int x = 3303;
    std::cout << 3303;
    for (int i = 0; i < 3303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3304(int& val) {
    const int x = 3304;
    std::cout << 3304;
    for (int i = 0; i < 3304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3305(int& val) {
    const int x = 3305;
    std::cout << 3305;
    for (int i = 0; i < 3305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3306(int& val) {
    const int x = 3306;
    std::cout << 3306;
    for (int i = 0; i < 3306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3307(int& val) {
    const int x = 3307;
    std::cout << 3307;
    for (int i = 0; i < 3307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3308(int& val) {
    const int x = 3308;
    std::cout << 3308;
    for (int i = 0; i < 3308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3309(int& val) {
    const int x = 3309;
    std::cout << 3309;
    for (int i = 0; i < 3309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3310(int& val) {
    const int x = 3310;
    std::cout << 3310;
    for (int i = 0; i < 3310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3311(int& val) {
    const int x = 3311;
    std::cout << 3311;
    for (int i = 0; i < 3311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3312(int& val) {
    const int x = 3312;
    std::cout << 3312;
    for (int i = 0; i < 3312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3313(int& val) {
    const int x = 3313;
    std::cout << 3313;
    for (int i = 0; i < 3313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3314(int& val) {
    const int x = 3314;
    std::cout << 3314;
    for (int i = 0; i < 3314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3315(int& val) {
    const int x = 3315;
    std::cout << 3315;
    for (int i = 0; i < 3315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3316(int& val) {
    const int x = 3316;
    std::cout << 3316;
    for (int i = 0; i < 3316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3317(int& val) {
    const int x = 3317;
    std::cout << 3317;
    for (int i = 0; i < 3317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3318(int& val) {
    const int x = 3318;
    std::cout << 3318;
    for (int i = 0; i < 3318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3319(int& val) {
    const int x = 3319;
    std::cout << 3319;
    for (int i = 0; i < 3319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3320(int& val) {
    const int x = 3320;
    std::cout << 3320;
    for (int i = 0; i < 3320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3321(int& val) {
    const int x = 3321;
    std::cout << 3321;
    for (int i = 0; i < 3321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3322(int& val) {
    const int x = 3322;
    std::cout << 3322;
    for (int i = 0; i < 3322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3323(int& val) {
    const int x = 3323;
    std::cout << 3323;
    for (int i = 0; i < 3323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3324(int& val) {
    const int x = 3324;
    std::cout << 3324;
    for (int i = 0; i < 3324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3325(int& val) {
    const int x = 3325;
    std::cout << 3325;
    for (int i = 0; i < 3325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3326(int& val) {
    const int x = 3326;
    std::cout << 3326;
    for (int i = 0; i < 3326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3327(int& val) {
    const int x = 3327;
    std::cout << 3327;
    for (int i = 0; i < 3327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3328(int& val) {
    const int x = 3328;
    std::cout << 3328;
    for (int i = 0; i < 3328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3329(int& val) {
    const int x = 3329;
    std::cout << 3329;
    for (int i = 0; i < 3329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3330(int& val) {
    const int x = 3330;
    std::cout << 3330;
    for (int i = 0; i < 3330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3331(int& val) {
    const int x = 3331;
    std::cout << 3331;
    for (int i = 0; i < 3331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3332(int& val) {
    const int x = 3332;
    std::cout << 3332;
    for (int i = 0; i < 3332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3333(int& val) {
    const int x = 3333;
    std::cout << 3333;
    for (int i = 0; i < 3333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3334(int& val) {
    const int x = 3334;
    std::cout << 3334;
    for (int i = 0; i < 3334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3335(int& val) {
    const int x = 3335;
    std::cout << 3335;
    for (int i = 0; i < 3335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3336(int& val) {
    const int x = 3336;
    std::cout << 3336;
    for (int i = 0; i < 3336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3337(int& val) {
    const int x = 3337;
    std::cout << 3337;
    for (int i = 0; i < 3337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3338(int& val) {
    const int x = 3338;
    std::cout << 3338;
    for (int i = 0; i < 3338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3339(int& val) {
    const int x = 3339;
    std::cout << 3339;
    for (int i = 0; i < 3339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3340(int& val) {
    const int x = 3340;
    std::cout << 3340;
    for (int i = 0; i < 3340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3341(int& val) {
    const int x = 3341;
    std::cout << 3341;
    for (int i = 0; i < 3341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3342(int& val) {
    const int x = 3342;
    std::cout << 3342;
    for (int i = 0; i < 3342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3343(int& val) {
    const int x = 3343;
    std::cout << 3343;
    for (int i = 0; i < 3343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3344(int& val) {
    const int x = 3344;
    std::cout << 3344;
    for (int i = 0; i < 3344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3345(int& val) {
    const int x = 3345;
    std::cout << 3345;
    for (int i = 0; i < 3345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3346(int& val) {
    const int x = 3346;
    std::cout << 3346;
    for (int i = 0; i < 3346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3347(int& val) {
    const int x = 3347;
    std::cout << 3347;
    for (int i = 0; i < 3347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3348(int& val) {
    const int x = 3348;
    std::cout << 3348;
    for (int i = 0; i < 3348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3349(int& val) {
    const int x = 3349;
    std::cout << 3349;
    for (int i = 0; i < 3349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3350(int& val) {
    const int x = 3350;
    std::cout << 3350;
    for (int i = 0; i < 3350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3351(int& val) {
    const int x = 3351;
    std::cout << 3351;
    for (int i = 0; i < 3351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3352(int& val) {
    const int x = 3352;
    std::cout << 3352;
    for (int i = 0; i < 3352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3353(int& val) {
    const int x = 3353;
    std::cout << 3353;
    for (int i = 0; i < 3353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3354(int& val) {
    const int x = 3354;
    std::cout << 3354;
    for (int i = 0; i < 3354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3355(int& val) {
    const int x = 3355;
    std::cout << 3355;
    for (int i = 0; i < 3355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3356(int& val) {
    const int x = 3356;
    std::cout << 3356;
    for (int i = 0; i < 3356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3357(int& val) {
    const int x = 3357;
    std::cout << 3357;
    for (int i = 0; i < 3357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3358(int& val) {
    const int x = 3358;
    std::cout << 3358;
    for (int i = 0; i < 3358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3359(int& val) {
    const int x = 3359;
    std::cout << 3359;
    for (int i = 0; i < 3359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3360(int& val) {
    const int x = 3360;
    std::cout << 3360;
    for (int i = 0; i < 3360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3361(int& val) {
    const int x = 3361;
    std::cout << 3361;
    for (int i = 0; i < 3361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3362(int& val) {
    const int x = 3362;
    std::cout << 3362;
    for (int i = 0; i < 3362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3363(int& val) {
    const int x = 3363;
    std::cout << 3363;
    for (int i = 0; i < 3363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3364(int& val) {
    const int x = 3364;
    std::cout << 3364;
    for (int i = 0; i < 3364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3365(int& val) {
    const int x = 3365;
    std::cout << 3365;
    for (int i = 0; i < 3365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3366(int& val) {
    const int x = 3366;
    std::cout << 3366;
    for (int i = 0; i < 3366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3367(int& val) {
    const int x = 3367;
    std::cout << 3367;
    for (int i = 0; i < 3367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3368(int& val) {
    const int x = 3368;
    std::cout << 3368;
    for (int i = 0; i < 3368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3369(int& val) {
    const int x = 3369;
    std::cout << 3369;
    for (int i = 0; i < 3369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3370(int& val) {
    const int x = 3370;
    std::cout << 3370;
    for (int i = 0; i < 3370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3371(int& val) {
    const int x = 3371;
    std::cout << 3371;
    for (int i = 0; i < 3371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3372(int& val) {
    const int x = 3372;
    std::cout << 3372;
    for (int i = 0; i < 3372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3373(int& val) {
    const int x = 3373;
    std::cout << 3373;
    for (int i = 0; i < 3373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3374(int& val) {
    const int x = 3374;
    std::cout << 3374;
    for (int i = 0; i < 3374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3375(int& val) {
    const int x = 3375;
    std::cout << 3375;
    for (int i = 0; i < 3375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3376(int& val) {
    const int x = 3376;
    std::cout << 3376;
    for (int i = 0; i < 3376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3377(int& val) {
    const int x = 3377;
    std::cout << 3377;
    for (int i = 0; i < 3377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3378(int& val) {
    const int x = 3378;
    std::cout << 3378;
    for (int i = 0; i < 3378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3379(int& val) {
    const int x = 3379;
    std::cout << 3379;
    for (int i = 0; i < 3379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3380(int& val) {
    const int x = 3380;
    std::cout << 3380;
    for (int i = 0; i < 3380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3381(int& val) {
    const int x = 3381;
    std::cout << 3381;
    for (int i = 0; i < 3381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3382(int& val) {
    const int x = 3382;
    std::cout << 3382;
    for (int i = 0; i < 3382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3383(int& val) {
    const int x = 3383;
    std::cout << 3383;
    for (int i = 0; i < 3383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3384(int& val) {
    const int x = 3384;
    std::cout << 3384;
    for (int i = 0; i < 3384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3385(int& val) {
    const int x = 3385;
    std::cout << 3385;
    for (int i = 0; i < 3385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3386(int& val) {
    const int x = 3386;
    std::cout << 3386;
    for (int i = 0; i < 3386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3387(int& val) {
    const int x = 3387;
    std::cout << 3387;
    for (int i = 0; i < 3387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3388(int& val) {
    const int x = 3388;
    std::cout << 3388;
    for (int i = 0; i < 3388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3389(int& val) {
    const int x = 3389;
    std::cout << 3389;
    for (int i = 0; i < 3389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3390(int& val) {
    const int x = 3390;
    std::cout << 3390;
    for (int i = 0; i < 3390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3391(int& val) {
    const int x = 3391;
    std::cout << 3391;
    for (int i = 0; i < 3391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3392(int& val) {
    const int x = 3392;
    std::cout << 3392;
    for (int i = 0; i < 3392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3393(int& val) {
    const int x = 3393;
    std::cout << 3393;
    for (int i = 0; i < 3393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3394(int& val) {
    const int x = 3394;
    std::cout << 3394;
    for (int i = 0; i < 3394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3395(int& val) {
    const int x = 3395;
    std::cout << 3395;
    for (int i = 0; i < 3395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3396(int& val) {
    const int x = 3396;
    std::cout << 3396;
    for (int i = 0; i < 3396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3397(int& val) {
    const int x = 3397;
    std::cout << 3397;
    for (int i = 0; i < 3397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3398(int& val) {
    const int x = 3398;
    std::cout << 3398;
    for (int i = 0; i < 3398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3399(int& val) {
    const int x = 3399;
    std::cout << 3399;
    for (int i = 0; i < 3399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3400(int& val) {
    const int x = 3400;
    std::cout << 3400;
    for (int i = 0; i < 3400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3401(int& val) {
    const int x = 3401;
    std::cout << 3401;
    for (int i = 0; i < 3401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3402(int& val) {
    const int x = 3402;
    std::cout << 3402;
    for (int i = 0; i < 3402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3403(int& val) {
    const int x = 3403;
    std::cout << 3403;
    for (int i = 0; i < 3403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3404(int& val) {
    const int x = 3404;
    std::cout << 3404;
    for (int i = 0; i < 3404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3405(int& val) {
    const int x = 3405;
    std::cout << 3405;
    for (int i = 0; i < 3405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3406(int& val) {
    const int x = 3406;
    std::cout << 3406;
    for (int i = 0; i < 3406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3407(int& val) {
    const int x = 3407;
    std::cout << 3407;
    for (int i = 0; i < 3407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3408(int& val) {
    const int x = 3408;
    std::cout << 3408;
    for (int i = 0; i < 3408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3409(int& val) {
    const int x = 3409;
    std::cout << 3409;
    for (int i = 0; i < 3409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3410(int& val) {
    const int x = 3410;
    std::cout << 3410;
    for (int i = 0; i < 3410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3411(int& val) {
    const int x = 3411;
    std::cout << 3411;
    for (int i = 0; i < 3411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3412(int& val) {
    const int x = 3412;
    std::cout << 3412;
    for (int i = 0; i < 3412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3413(int& val) {
    const int x = 3413;
    std::cout << 3413;
    for (int i = 0; i < 3413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3414(int& val) {
    const int x = 3414;
    std::cout << 3414;
    for (int i = 0; i < 3414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3415(int& val) {
    const int x = 3415;
    std::cout << 3415;
    for (int i = 0; i < 3415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3416(int& val) {
    const int x = 3416;
    std::cout << 3416;
    for (int i = 0; i < 3416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3417(int& val) {
    const int x = 3417;
    std::cout << 3417;
    for (int i = 0; i < 3417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3418(int& val) {
    const int x = 3418;
    std::cout << 3418;
    for (int i = 0; i < 3418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3419(int& val) {
    const int x = 3419;
    std::cout << 3419;
    for (int i = 0; i < 3419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3420(int& val) {
    const int x = 3420;
    std::cout << 3420;
    for (int i = 0; i < 3420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3421(int& val) {
    const int x = 3421;
    std::cout << 3421;
    for (int i = 0; i < 3421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3422(int& val) {
    const int x = 3422;
    std::cout << 3422;
    for (int i = 0; i < 3422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3423(int& val) {
    const int x = 3423;
    std::cout << 3423;
    for (int i = 0; i < 3423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3424(int& val) {
    const int x = 3424;
    std::cout << 3424;
    for (int i = 0; i < 3424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3425(int& val) {
    const int x = 3425;
    std::cout << 3425;
    for (int i = 0; i < 3425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3426(int& val) {
    const int x = 3426;
    std::cout << 3426;
    for (int i = 0; i < 3426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3427(int& val) {
    const int x = 3427;
    std::cout << 3427;
    for (int i = 0; i < 3427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3428(int& val) {
    const int x = 3428;
    std::cout << 3428;
    for (int i = 0; i < 3428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3429(int& val) {
    const int x = 3429;
    std::cout << 3429;
    for (int i = 0; i < 3429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3430(int& val) {
    const int x = 3430;
    std::cout << 3430;
    for (int i = 0; i < 3430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3431(int& val) {
    const int x = 3431;
    std::cout << 3431;
    for (int i = 0; i < 3431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3432(int& val) {
    const int x = 3432;
    std::cout << 3432;
    for (int i = 0; i < 3432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3433(int& val) {
    const int x = 3433;
    std::cout << 3433;
    for (int i = 0; i < 3433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3434(int& val) {
    const int x = 3434;
    std::cout << 3434;
    for (int i = 0; i < 3434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3435(int& val) {
    const int x = 3435;
    std::cout << 3435;
    for (int i = 0; i < 3435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3436(int& val) {
    const int x = 3436;
    std::cout << 3436;
    for (int i = 0; i < 3436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3437(int& val) {
    const int x = 3437;
    std::cout << 3437;
    for (int i = 0; i < 3437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3438(int& val) {
    const int x = 3438;
    std::cout << 3438;
    for (int i = 0; i < 3438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3439(int& val) {
    const int x = 3439;
    std::cout << 3439;
    for (int i = 0; i < 3439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3440(int& val) {
    const int x = 3440;
    std::cout << 3440;
    for (int i = 0; i < 3440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3441(int& val) {
    const int x = 3441;
    std::cout << 3441;
    for (int i = 0; i < 3441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3442(int& val) {
    const int x = 3442;
    std::cout << 3442;
    for (int i = 0; i < 3442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3443(int& val) {
    const int x = 3443;
    std::cout << 3443;
    for (int i = 0; i < 3443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3444(int& val) {
    const int x = 3444;
    std::cout << 3444;
    for (int i = 0; i < 3444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3445(int& val) {
    const int x = 3445;
    std::cout << 3445;
    for (int i = 0; i < 3445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3446(int& val) {
    const int x = 3446;
    std::cout << 3446;
    for (int i = 0; i < 3446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3447(int& val) {
    const int x = 3447;
    std::cout << 3447;
    for (int i = 0; i < 3447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3448(int& val) {
    const int x = 3448;
    std::cout << 3448;
    for (int i = 0; i < 3448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3449(int& val) {
    const int x = 3449;
    std::cout << 3449;
    for (int i = 0; i < 3449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3450(int& val) {
    const int x = 3450;
    std::cout << 3450;
    for (int i = 0; i < 3450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3451(int& val) {
    const int x = 3451;
    std::cout << 3451;
    for (int i = 0; i < 3451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3452(int& val) {
    const int x = 3452;
    std::cout << 3452;
    for (int i = 0; i < 3452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3453(int& val) {
    const int x = 3453;
    std::cout << 3453;
    for (int i = 0; i < 3453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3454(int& val) {
    const int x = 3454;
    std::cout << 3454;
    for (int i = 0; i < 3454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3455(int& val) {
    const int x = 3455;
    std::cout << 3455;
    for (int i = 0; i < 3455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3456(int& val) {
    const int x = 3456;
    std::cout << 3456;
    for (int i = 0; i < 3456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3457(int& val) {
    const int x = 3457;
    std::cout << 3457;
    for (int i = 0; i < 3457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3458(int& val) {
    const int x = 3458;
    std::cout << 3458;
    for (int i = 0; i < 3458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3459(int& val) {
    const int x = 3459;
    std::cout << 3459;
    for (int i = 0; i < 3459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3460(int& val) {
    const int x = 3460;
    std::cout << 3460;
    for (int i = 0; i < 3460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3461(int& val) {
    const int x = 3461;
    std::cout << 3461;
    for (int i = 0; i < 3461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3462(int& val) {
    const int x = 3462;
    std::cout << 3462;
    for (int i = 0; i < 3462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3463(int& val) {
    const int x = 3463;
    std::cout << 3463;
    for (int i = 0; i < 3463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3464(int& val) {
    const int x = 3464;
    std::cout << 3464;
    for (int i = 0; i < 3464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3465(int& val) {
    const int x = 3465;
    std::cout << 3465;
    for (int i = 0; i < 3465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3466(int& val) {
    const int x = 3466;
    std::cout << 3466;
    for (int i = 0; i < 3466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3467(int& val) {
    const int x = 3467;
    std::cout << 3467;
    for (int i = 0; i < 3467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3468(int& val) {
    const int x = 3468;
    std::cout << 3468;
    for (int i = 0; i < 3468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3469(int& val) {
    const int x = 3469;
    std::cout << 3469;
    for (int i = 0; i < 3469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3470(int& val) {
    const int x = 3470;
    std::cout << 3470;
    for (int i = 0; i < 3470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3471(int& val) {
    const int x = 3471;
    std::cout << 3471;
    for (int i = 0; i < 3471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3472(int& val) {
    const int x = 3472;
    std::cout << 3472;
    for (int i = 0; i < 3472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3473(int& val) {
    const int x = 3473;
    std::cout << 3473;
    for (int i = 0; i < 3473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3474(int& val) {
    const int x = 3474;
    std::cout << 3474;
    for (int i = 0; i < 3474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3475(int& val) {
    const int x = 3475;
    std::cout << 3475;
    for (int i = 0; i < 3475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3476(int& val) {
    const int x = 3476;
    std::cout << 3476;
    for (int i = 0; i < 3476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3477(int& val) {
    const int x = 3477;
    std::cout << 3477;
    for (int i = 0; i < 3477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3478(int& val) {
    const int x = 3478;
    std::cout << 3478;
    for (int i = 0; i < 3478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3479(int& val) {
    const int x = 3479;
    std::cout << 3479;
    for (int i = 0; i < 3479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3480(int& val) {
    const int x = 3480;
    std::cout << 3480;
    for (int i = 0; i < 3480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3481(int& val) {
    const int x = 3481;
    std::cout << 3481;
    for (int i = 0; i < 3481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3482(int& val) {
    const int x = 3482;
    std::cout << 3482;
    for (int i = 0; i < 3482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3483(int& val) {
    const int x = 3483;
    std::cout << 3483;
    for (int i = 0; i < 3483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3484(int& val) {
    const int x = 3484;
    std::cout << 3484;
    for (int i = 0; i < 3484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3485(int& val) {
    const int x = 3485;
    std::cout << 3485;
    for (int i = 0; i < 3485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3486(int& val) {
    const int x = 3486;
    std::cout << 3486;
    for (int i = 0; i < 3486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3487(int& val) {
    const int x = 3487;
    std::cout << 3487;
    for (int i = 0; i < 3487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3488(int& val) {
    const int x = 3488;
    std::cout << 3488;
    for (int i = 0; i < 3488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3489(int& val) {
    const int x = 3489;
    std::cout << 3489;
    for (int i = 0; i < 3489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3490(int& val) {
    const int x = 3490;
    std::cout << 3490;
    for (int i = 0; i < 3490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3491(int& val) {
    const int x = 3491;
    std::cout << 3491;
    for (int i = 0; i < 3491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3492(int& val) {
    const int x = 3492;
    std::cout << 3492;
    for (int i = 0; i < 3492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3493(int& val) {
    const int x = 3493;
    std::cout << 3493;
    for (int i = 0; i < 3493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3494(int& val) {
    const int x = 3494;
    std::cout << 3494;
    for (int i = 0; i < 3494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3495(int& val) {
    const int x = 3495;
    std::cout << 3495;
    for (int i = 0; i < 3495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3496(int& val) {
    const int x = 3496;
    std::cout << 3496;
    for (int i = 0; i < 3496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3497(int& val) {
    const int x = 3497;
    std::cout << 3497;
    for (int i = 0; i < 3497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3498(int& val) {
    const int x = 3498;
    std::cout << 3498;
    for (int i = 0; i < 3498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3499(int& val) {
    const int x = 3499;
    std::cout << 3499;
    for (int i = 0; i < 3499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3500(int& val) {
    const int x = 3500;
    std::cout << 3500;
    for (int i = 0; i < 3500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3501(int& val) {
    const int x = 3501;
    std::cout << 3501;
    for (int i = 0; i < 3501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3502(int& val) {
    const int x = 3502;
    std::cout << 3502;
    for (int i = 0; i < 3502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3503(int& val) {
    const int x = 3503;
    std::cout << 3503;
    for (int i = 0; i < 3503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3504(int& val) {
    const int x = 3504;
    std::cout << 3504;
    for (int i = 0; i < 3504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3505(int& val) {
    const int x = 3505;
    std::cout << 3505;
    for (int i = 0; i < 3505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3506(int& val) {
    const int x = 3506;
    std::cout << 3506;
    for (int i = 0; i < 3506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3507(int& val) {
    const int x = 3507;
    std::cout << 3507;
    for (int i = 0; i < 3507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3508(int& val) {
    const int x = 3508;
    std::cout << 3508;
    for (int i = 0; i < 3508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3509(int& val) {
    const int x = 3509;
    std::cout << 3509;
    for (int i = 0; i < 3509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3510(int& val) {
    const int x = 3510;
    std::cout << 3510;
    for (int i = 0; i < 3510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3511(int& val) {
    const int x = 3511;
    std::cout << 3511;
    for (int i = 0; i < 3511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3512(int& val) {
    const int x = 3512;
    std::cout << 3512;
    for (int i = 0; i < 3512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3513(int& val) {
    const int x = 3513;
    std::cout << 3513;
    for (int i = 0; i < 3513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3514(int& val) {
    const int x = 3514;
    std::cout << 3514;
    for (int i = 0; i < 3514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3515(int& val) {
    const int x = 3515;
    std::cout << 3515;
    for (int i = 0; i < 3515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3516(int& val) {
    const int x = 3516;
    std::cout << 3516;
    for (int i = 0; i < 3516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3517(int& val) {
    const int x = 3517;
    std::cout << 3517;
    for (int i = 0; i < 3517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3518(int& val) {
    const int x = 3518;
    std::cout << 3518;
    for (int i = 0; i < 3518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3519(int& val) {
    const int x = 3519;
    std::cout << 3519;
    for (int i = 0; i < 3519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3520(int& val) {
    const int x = 3520;
    std::cout << 3520;
    for (int i = 0; i < 3520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3521(int& val) {
    const int x = 3521;
    std::cout << 3521;
    for (int i = 0; i < 3521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3522(int& val) {
    const int x = 3522;
    std::cout << 3522;
    for (int i = 0; i < 3522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3523(int& val) {
    const int x = 3523;
    std::cout << 3523;
    for (int i = 0; i < 3523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3524(int& val) {
    const int x = 3524;
    std::cout << 3524;
    for (int i = 0; i < 3524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3525(int& val) {
    const int x = 3525;
    std::cout << 3525;
    for (int i = 0; i < 3525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3526(int& val) {
    const int x = 3526;
    std::cout << 3526;
    for (int i = 0; i < 3526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3527(int& val) {
    const int x = 3527;
    std::cout << 3527;
    for (int i = 0; i < 3527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3528(int& val) {
    const int x = 3528;
    std::cout << 3528;
    for (int i = 0; i < 3528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3529(int& val) {
    const int x = 3529;
    std::cout << 3529;
    for (int i = 0; i < 3529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3530(int& val) {
    const int x = 3530;
    std::cout << 3530;
    for (int i = 0; i < 3530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3531(int& val) {
    const int x = 3531;
    std::cout << 3531;
    for (int i = 0; i < 3531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3532(int& val) {
    const int x = 3532;
    std::cout << 3532;
    for (int i = 0; i < 3532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3533(int& val) {
    const int x = 3533;
    std::cout << 3533;
    for (int i = 0; i < 3533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3534(int& val) {
    const int x = 3534;
    std::cout << 3534;
    for (int i = 0; i < 3534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3535(int& val) {
    const int x = 3535;
    std::cout << 3535;
    for (int i = 0; i < 3535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3536(int& val) {
    const int x = 3536;
    std::cout << 3536;
    for (int i = 0; i < 3536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3537(int& val) {
    const int x = 3537;
    std::cout << 3537;
    for (int i = 0; i < 3537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3538(int& val) {
    const int x = 3538;
    std::cout << 3538;
    for (int i = 0; i < 3538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3539(int& val) {
    const int x = 3539;
    std::cout << 3539;
    for (int i = 0; i < 3539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3540(int& val) {
    const int x = 3540;
    std::cout << 3540;
    for (int i = 0; i < 3540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3541(int& val) {
    const int x = 3541;
    std::cout << 3541;
    for (int i = 0; i < 3541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3542(int& val) {
    const int x = 3542;
    std::cout << 3542;
    for (int i = 0; i < 3542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3543(int& val) {
    const int x = 3543;
    std::cout << 3543;
    for (int i = 0; i < 3543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3544(int& val) {
    const int x = 3544;
    std::cout << 3544;
    for (int i = 0; i < 3544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3545(int& val) {
    const int x = 3545;
    std::cout << 3545;
    for (int i = 0; i < 3545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3546(int& val) {
    const int x = 3546;
    std::cout << 3546;
    for (int i = 0; i < 3546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3547(int& val) {
    const int x = 3547;
    std::cout << 3547;
    for (int i = 0; i < 3547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3548(int& val) {
    const int x = 3548;
    std::cout << 3548;
    for (int i = 0; i < 3548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3549(int& val) {
    const int x = 3549;
    std::cout << 3549;
    for (int i = 0; i < 3549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3550(int& val) {
    const int x = 3550;
    std::cout << 3550;
    for (int i = 0; i < 3550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3551(int& val) {
    const int x = 3551;
    std::cout << 3551;
    for (int i = 0; i < 3551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3552(int& val) {
    const int x = 3552;
    std::cout << 3552;
    for (int i = 0; i < 3552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3553(int& val) {
    const int x = 3553;
    std::cout << 3553;
    for (int i = 0; i < 3553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3554(int& val) {
    const int x = 3554;
    std::cout << 3554;
    for (int i = 0; i < 3554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3555(int& val) {
    const int x = 3555;
    std::cout << 3555;
    for (int i = 0; i < 3555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3556(int& val) {
    const int x = 3556;
    std::cout << 3556;
    for (int i = 0; i < 3556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3557(int& val) {
    const int x = 3557;
    std::cout << 3557;
    for (int i = 0; i < 3557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3558(int& val) {
    const int x = 3558;
    std::cout << 3558;
    for (int i = 0; i < 3558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3559(int& val) {
    const int x = 3559;
    std::cout << 3559;
    for (int i = 0; i < 3559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3560(int& val) {
    const int x = 3560;
    std::cout << 3560;
    for (int i = 0; i < 3560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3561(int& val) {
    const int x = 3561;
    std::cout << 3561;
    for (int i = 0; i < 3561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3562(int& val) {
    const int x = 3562;
    std::cout << 3562;
    for (int i = 0; i < 3562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3563(int& val) {
    const int x = 3563;
    std::cout << 3563;
    for (int i = 0; i < 3563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3564(int& val) {
    const int x = 3564;
    std::cout << 3564;
    for (int i = 0; i < 3564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3565(int& val) {
    const int x = 3565;
    std::cout << 3565;
    for (int i = 0; i < 3565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3566(int& val) {
    const int x = 3566;
    std::cout << 3566;
    for (int i = 0; i < 3566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3567(int& val) {
    const int x = 3567;
    std::cout << 3567;
    for (int i = 0; i < 3567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3568(int& val) {
    const int x = 3568;
    std::cout << 3568;
    for (int i = 0; i < 3568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3569(int& val) {
    const int x = 3569;
    std::cout << 3569;
    for (int i = 0; i < 3569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3570(int& val) {
    const int x = 3570;
    std::cout << 3570;
    for (int i = 0; i < 3570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3571(int& val) {
    const int x = 3571;
    std::cout << 3571;
    for (int i = 0; i < 3571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3572(int& val) {
    const int x = 3572;
    std::cout << 3572;
    for (int i = 0; i < 3572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3573(int& val) {
    const int x = 3573;
    std::cout << 3573;
    for (int i = 0; i < 3573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3574(int& val) {
    const int x = 3574;
    std::cout << 3574;
    for (int i = 0; i < 3574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3575(int& val) {
    const int x = 3575;
    std::cout << 3575;
    for (int i = 0; i < 3575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3576(int& val) {
    const int x = 3576;
    std::cout << 3576;
    for (int i = 0; i < 3576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3577(int& val) {
    const int x = 3577;
    std::cout << 3577;
    for (int i = 0; i < 3577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3578(int& val) {
    const int x = 3578;
    std::cout << 3578;
    for (int i = 0; i < 3578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3579(int& val) {
    const int x = 3579;
    std::cout << 3579;
    for (int i = 0; i < 3579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3580(int& val) {
    const int x = 3580;
    std::cout << 3580;
    for (int i = 0; i < 3580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3581(int& val) {
    const int x = 3581;
    std::cout << 3581;
    for (int i = 0; i < 3581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3582(int& val) {
    const int x = 3582;
    std::cout << 3582;
    for (int i = 0; i < 3582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3583(int& val) {
    const int x = 3583;
    std::cout << 3583;
    for (int i = 0; i < 3583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3584(int& val) {
    const int x = 3584;
    std::cout << 3584;
    for (int i = 0; i < 3584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3585(int& val) {
    const int x = 3585;
    std::cout << 3585;
    for (int i = 0; i < 3585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3586(int& val) {
    const int x = 3586;
    std::cout << 3586;
    for (int i = 0; i < 3586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3587(int& val) {
    const int x = 3587;
    std::cout << 3587;
    for (int i = 0; i < 3587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3588(int& val) {
    const int x = 3588;
    std::cout << 3588;
    for (int i = 0; i < 3588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3589(int& val) {
    const int x = 3589;
    std::cout << 3589;
    for (int i = 0; i < 3589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3590(int& val) {
    const int x = 3590;
    std::cout << 3590;
    for (int i = 0; i < 3590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3591(int& val) {
    const int x = 3591;
    std::cout << 3591;
    for (int i = 0; i < 3591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3592(int& val) {
    const int x = 3592;
    std::cout << 3592;
    for (int i = 0; i < 3592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3593(int& val) {
    const int x = 3593;
    std::cout << 3593;
    for (int i = 0; i < 3593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3594(int& val) {
    const int x = 3594;
    std::cout << 3594;
    for (int i = 0; i < 3594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3595(int& val) {
    const int x = 3595;
    std::cout << 3595;
    for (int i = 0; i < 3595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3596(int& val) {
    const int x = 3596;
    std::cout << 3596;
    for (int i = 0; i < 3596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3597(int& val) {
    const int x = 3597;
    std::cout << 3597;
    for (int i = 0; i < 3597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3598(int& val) {
    const int x = 3598;
    std::cout << 3598;
    for (int i = 0; i < 3598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3599(int& val) {
    const int x = 3599;
    std::cout << 3599;
    for (int i = 0; i < 3599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3600(int& val) {
    const int x = 3600;
    std::cout << 3600;
    for (int i = 0; i < 3600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3601(int& val) {
    const int x = 3601;
    std::cout << 3601;
    for (int i = 0; i < 3601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3602(int& val) {
    const int x = 3602;
    std::cout << 3602;
    for (int i = 0; i < 3602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3603(int& val) {
    const int x = 3603;
    std::cout << 3603;
    for (int i = 0; i < 3603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3604(int& val) {
    const int x = 3604;
    std::cout << 3604;
    for (int i = 0; i < 3604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3605(int& val) {
    const int x = 3605;
    std::cout << 3605;
    for (int i = 0; i < 3605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3606(int& val) {
    const int x = 3606;
    std::cout << 3606;
    for (int i = 0; i < 3606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3607(int& val) {
    const int x = 3607;
    std::cout << 3607;
    for (int i = 0; i < 3607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3608(int& val) {
    const int x = 3608;
    std::cout << 3608;
    for (int i = 0; i < 3608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3609(int& val) {
    const int x = 3609;
    std::cout << 3609;
    for (int i = 0; i < 3609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3610(int& val) {
    const int x = 3610;
    std::cout << 3610;
    for (int i = 0; i < 3610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3611(int& val) {
    const int x = 3611;
    std::cout << 3611;
    for (int i = 0; i < 3611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3612(int& val) {
    const int x = 3612;
    std::cout << 3612;
    for (int i = 0; i < 3612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3613(int& val) {
    const int x = 3613;
    std::cout << 3613;
    for (int i = 0; i < 3613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3614(int& val) {
    const int x = 3614;
    std::cout << 3614;
    for (int i = 0; i < 3614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3615(int& val) {
    const int x = 3615;
    std::cout << 3615;
    for (int i = 0; i < 3615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3616(int& val) {
    const int x = 3616;
    std::cout << 3616;
    for (int i = 0; i < 3616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3617(int& val) {
    const int x = 3617;
    std::cout << 3617;
    for (int i = 0; i < 3617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3618(int& val) {
    const int x = 3618;
    std::cout << 3618;
    for (int i = 0; i < 3618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3619(int& val) {
    const int x = 3619;
    std::cout << 3619;
    for (int i = 0; i < 3619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3620(int& val) {
    const int x = 3620;
    std::cout << 3620;
    for (int i = 0; i < 3620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3621(int& val) {
    const int x = 3621;
    std::cout << 3621;
    for (int i = 0; i < 3621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3622(int& val) {
    const int x = 3622;
    std::cout << 3622;
    for (int i = 0; i < 3622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3623(int& val) {
    const int x = 3623;
    std::cout << 3623;
    for (int i = 0; i < 3623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3624(int& val) {
    const int x = 3624;
    std::cout << 3624;
    for (int i = 0; i < 3624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3625(int& val) {
    const int x = 3625;
    std::cout << 3625;
    for (int i = 0; i < 3625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3626(int& val) {
    const int x = 3626;
    std::cout << 3626;
    for (int i = 0; i < 3626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3627(int& val) {
    const int x = 3627;
    std::cout << 3627;
    for (int i = 0; i < 3627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3628(int& val) {
    const int x = 3628;
    std::cout << 3628;
    for (int i = 0; i < 3628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3629(int& val) {
    const int x = 3629;
    std::cout << 3629;
    for (int i = 0; i < 3629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3630(int& val) {
    const int x = 3630;
    std::cout << 3630;
    for (int i = 0; i < 3630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3631(int& val) {
    const int x = 3631;
    std::cout << 3631;
    for (int i = 0; i < 3631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3632(int& val) {
    const int x = 3632;
    std::cout << 3632;
    for (int i = 0; i < 3632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3633(int& val) {
    const int x = 3633;
    std::cout << 3633;
    for (int i = 0; i < 3633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3634(int& val) {
    const int x = 3634;
    std::cout << 3634;
    for (int i = 0; i < 3634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3635(int& val) {
    const int x = 3635;
    std::cout << 3635;
    for (int i = 0; i < 3635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3636(int& val) {
    const int x = 3636;
    std::cout << 3636;
    for (int i = 0; i < 3636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3637(int& val) {
    const int x = 3637;
    std::cout << 3637;
    for (int i = 0; i < 3637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3638(int& val) {
    const int x = 3638;
    std::cout << 3638;
    for (int i = 0; i < 3638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3639(int& val) {
    const int x = 3639;
    std::cout << 3639;
    for (int i = 0; i < 3639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3640(int& val) {
    const int x = 3640;
    std::cout << 3640;
    for (int i = 0; i < 3640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3641(int& val) {
    const int x = 3641;
    std::cout << 3641;
    for (int i = 0; i < 3641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3642(int& val) {
    const int x = 3642;
    std::cout << 3642;
    for (int i = 0; i < 3642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3643(int& val) {
    const int x = 3643;
    std::cout << 3643;
    for (int i = 0; i < 3643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3644(int& val) {
    const int x = 3644;
    std::cout << 3644;
    for (int i = 0; i < 3644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3645(int& val) {
    const int x = 3645;
    std::cout << 3645;
    for (int i = 0; i < 3645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3646(int& val) {
    const int x = 3646;
    std::cout << 3646;
    for (int i = 0; i < 3646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3647(int& val) {
    const int x = 3647;
    std::cout << 3647;
    for (int i = 0; i < 3647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3648(int& val) {
    const int x = 3648;
    std::cout << 3648;
    for (int i = 0; i < 3648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3649(int& val) {
    const int x = 3649;
    std::cout << 3649;
    for (int i = 0; i < 3649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3650(int& val) {
    const int x = 3650;
    std::cout << 3650;
    for (int i = 0; i < 3650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3651(int& val) {
    const int x = 3651;
    std::cout << 3651;
    for (int i = 0; i < 3651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3652(int& val) {
    const int x = 3652;
    std::cout << 3652;
    for (int i = 0; i < 3652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3653(int& val) {
    const int x = 3653;
    std::cout << 3653;
    for (int i = 0; i < 3653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3654(int& val) {
    const int x = 3654;
    std::cout << 3654;
    for (int i = 0; i < 3654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3655(int& val) {
    const int x = 3655;
    std::cout << 3655;
    for (int i = 0; i < 3655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3656(int& val) {
    const int x = 3656;
    std::cout << 3656;
    for (int i = 0; i < 3656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3657(int& val) {
    const int x = 3657;
    std::cout << 3657;
    for (int i = 0; i < 3657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3658(int& val) {
    const int x = 3658;
    std::cout << 3658;
    for (int i = 0; i < 3658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3659(int& val) {
    const int x = 3659;
    std::cout << 3659;
    for (int i = 0; i < 3659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3660(int& val) {
    const int x = 3660;
    std::cout << 3660;
    for (int i = 0; i < 3660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3661(int& val) {
    const int x = 3661;
    std::cout << 3661;
    for (int i = 0; i < 3661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3662(int& val) {
    const int x = 3662;
    std::cout << 3662;
    for (int i = 0; i < 3662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3663(int& val) {
    const int x = 3663;
    std::cout << 3663;
    for (int i = 0; i < 3663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3664(int& val) {
    const int x = 3664;
    std::cout << 3664;
    for (int i = 0; i < 3664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3665(int& val) {
    const int x = 3665;
    std::cout << 3665;
    for (int i = 0; i < 3665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3666(int& val) {
    const int x = 3666;
    std::cout << 3666;
    for (int i = 0; i < 3666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3667(int& val) {
    const int x = 3667;
    std::cout << 3667;
    for (int i = 0; i < 3667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3668(int& val) {
    const int x = 3668;
    std::cout << 3668;
    for (int i = 0; i < 3668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3669(int& val) {
    const int x = 3669;
    std::cout << 3669;
    for (int i = 0; i < 3669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3670(int& val) {
    const int x = 3670;
    std::cout << 3670;
    for (int i = 0; i < 3670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3671(int& val) {
    const int x = 3671;
    std::cout << 3671;
    for (int i = 0; i < 3671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3672(int& val) {
    const int x = 3672;
    std::cout << 3672;
    for (int i = 0; i < 3672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3673(int& val) {
    const int x = 3673;
    std::cout << 3673;
    for (int i = 0; i < 3673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3674(int& val) {
    const int x = 3674;
    std::cout << 3674;
    for (int i = 0; i < 3674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3675(int& val) {
    const int x = 3675;
    std::cout << 3675;
    for (int i = 0; i < 3675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3676(int& val) {
    const int x = 3676;
    std::cout << 3676;
    for (int i = 0; i < 3676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3677(int& val) {
    const int x = 3677;
    std::cout << 3677;
    for (int i = 0; i < 3677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3678(int& val) {
    const int x = 3678;
    std::cout << 3678;
    for (int i = 0; i < 3678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3679(int& val) {
    const int x = 3679;
    std::cout << 3679;
    for (int i = 0; i < 3679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3680(int& val) {
    const int x = 3680;
    std::cout << 3680;
    for (int i = 0; i < 3680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3681(int& val) {
    const int x = 3681;
    std::cout << 3681;
    for (int i = 0; i < 3681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3682(int& val) {
    const int x = 3682;
    std::cout << 3682;
    for (int i = 0; i < 3682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3683(int& val) {
    const int x = 3683;
    std::cout << 3683;
    for (int i = 0; i < 3683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3684(int& val) {
    const int x = 3684;
    std::cout << 3684;
    for (int i = 0; i < 3684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3685(int& val) {
    const int x = 3685;
    std::cout << 3685;
    for (int i = 0; i < 3685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3686(int& val) {
    const int x = 3686;
    std::cout << 3686;
    for (int i = 0; i < 3686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3687(int& val) {
    const int x = 3687;
    std::cout << 3687;
    for (int i = 0; i < 3687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3688(int& val) {
    const int x = 3688;
    std::cout << 3688;
    for (int i = 0; i < 3688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3689(int& val) {
    const int x = 3689;
    std::cout << 3689;
    for (int i = 0; i < 3689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3690(int& val) {
    const int x = 3690;
    std::cout << 3690;
    for (int i = 0; i < 3690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3691(int& val) {
    const int x = 3691;
    std::cout << 3691;
    for (int i = 0; i < 3691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3692(int& val) {
    const int x = 3692;
    std::cout << 3692;
    for (int i = 0; i < 3692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3693(int& val) {
    const int x = 3693;
    std::cout << 3693;
    for (int i = 0; i < 3693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3694(int& val) {
    const int x = 3694;
    std::cout << 3694;
    for (int i = 0; i < 3694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3695(int& val) {
    const int x = 3695;
    std::cout << 3695;
    for (int i = 0; i < 3695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3696(int& val) {
    const int x = 3696;
    std::cout << 3696;
    for (int i = 0; i < 3696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3697(int& val) {
    const int x = 3697;
    std::cout << 3697;
    for (int i = 0; i < 3697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3698(int& val) {
    const int x = 3698;
    std::cout << 3698;
    for (int i = 0; i < 3698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3699(int& val) {
    const int x = 3699;
    std::cout << 3699;
    for (int i = 0; i < 3699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3700(int& val) {
    const int x = 3700;
    std::cout << 3700;
    for (int i = 0; i < 3700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3701(int& val) {
    const int x = 3701;
    std::cout << 3701;
    for (int i = 0; i < 3701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3702(int& val) {
    const int x = 3702;
    std::cout << 3702;
    for (int i = 0; i < 3702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3703(int& val) {
    const int x = 3703;
    std::cout << 3703;
    for (int i = 0; i < 3703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3704(int& val) {
    const int x = 3704;
    std::cout << 3704;
    for (int i = 0; i < 3704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3705(int& val) {
    const int x = 3705;
    std::cout << 3705;
    for (int i = 0; i < 3705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3706(int& val) {
    const int x = 3706;
    std::cout << 3706;
    for (int i = 0; i < 3706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3707(int& val) {
    const int x = 3707;
    std::cout << 3707;
    for (int i = 0; i < 3707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3708(int& val) {
    const int x = 3708;
    std::cout << 3708;
    for (int i = 0; i < 3708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3709(int& val) {
    const int x = 3709;
    std::cout << 3709;
    for (int i = 0; i < 3709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3710(int& val) {
    const int x = 3710;
    std::cout << 3710;
    for (int i = 0; i < 3710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3711(int& val) {
    const int x = 3711;
    std::cout << 3711;
    for (int i = 0; i < 3711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3712(int& val) {
    const int x = 3712;
    std::cout << 3712;
    for (int i = 0; i < 3712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3713(int& val) {
    const int x = 3713;
    std::cout << 3713;
    for (int i = 0; i < 3713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3714(int& val) {
    const int x = 3714;
    std::cout << 3714;
    for (int i = 0; i < 3714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3715(int& val) {
    const int x = 3715;
    std::cout << 3715;
    for (int i = 0; i < 3715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3716(int& val) {
    const int x = 3716;
    std::cout << 3716;
    for (int i = 0; i < 3716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3717(int& val) {
    const int x = 3717;
    std::cout << 3717;
    for (int i = 0; i < 3717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3718(int& val) {
    const int x = 3718;
    std::cout << 3718;
    for (int i = 0; i < 3718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3719(int& val) {
    const int x = 3719;
    std::cout << 3719;
    for (int i = 0; i < 3719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3720(int& val) {
    const int x = 3720;
    std::cout << 3720;
    for (int i = 0; i < 3720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3721(int& val) {
    const int x = 3721;
    std::cout << 3721;
    for (int i = 0; i < 3721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3722(int& val) {
    const int x = 3722;
    std::cout << 3722;
    for (int i = 0; i < 3722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3723(int& val) {
    const int x = 3723;
    std::cout << 3723;
    for (int i = 0; i < 3723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3724(int& val) {
    const int x = 3724;
    std::cout << 3724;
    for (int i = 0; i < 3724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3725(int& val) {
    const int x = 3725;
    std::cout << 3725;
    for (int i = 0; i < 3725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3726(int& val) {
    const int x = 3726;
    std::cout << 3726;
    for (int i = 0; i < 3726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3727(int& val) {
    const int x = 3727;
    std::cout << 3727;
    for (int i = 0; i < 3727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3728(int& val) {
    const int x = 3728;
    std::cout << 3728;
    for (int i = 0; i < 3728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3729(int& val) {
    const int x = 3729;
    std::cout << 3729;
    for (int i = 0; i < 3729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3730(int& val) {
    const int x = 3730;
    std::cout << 3730;
    for (int i = 0; i < 3730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3731(int& val) {
    const int x = 3731;
    std::cout << 3731;
    for (int i = 0; i < 3731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3732(int& val) {
    const int x = 3732;
    std::cout << 3732;
    for (int i = 0; i < 3732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3733(int& val) {
    const int x = 3733;
    std::cout << 3733;
    for (int i = 0; i < 3733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3734(int& val) {
    const int x = 3734;
    std::cout << 3734;
    for (int i = 0; i < 3734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3735(int& val) {
    const int x = 3735;
    std::cout << 3735;
    for (int i = 0; i < 3735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3736(int& val) {
    const int x = 3736;
    std::cout << 3736;
    for (int i = 0; i < 3736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3737(int& val) {
    const int x = 3737;
    std::cout << 3737;
    for (int i = 0; i < 3737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3738(int& val) {
    const int x = 3738;
    std::cout << 3738;
    for (int i = 0; i < 3738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3739(int& val) {
    const int x = 3739;
    std::cout << 3739;
    for (int i = 0; i < 3739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3740(int& val) {
    const int x = 3740;
    std::cout << 3740;
    for (int i = 0; i < 3740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3741(int& val) {
    const int x = 3741;
    std::cout << 3741;
    for (int i = 0; i < 3741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3742(int& val) {
    const int x = 3742;
    std::cout << 3742;
    for (int i = 0; i < 3742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3743(int& val) {
    const int x = 3743;
    std::cout << 3743;
    for (int i = 0; i < 3743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3744(int& val) {
    const int x = 3744;
    std::cout << 3744;
    for (int i = 0; i < 3744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3745(int& val) {
    const int x = 3745;
    std::cout << 3745;
    for (int i = 0; i < 3745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3746(int& val) {
    const int x = 3746;
    std::cout << 3746;
    for (int i = 0; i < 3746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3747(int& val) {
    const int x = 3747;
    std::cout << 3747;
    for (int i = 0; i < 3747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3748(int& val) {
    const int x = 3748;
    std::cout << 3748;
    for (int i = 0; i < 3748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3749(int& val) {
    const int x = 3749;
    std::cout << 3749;
    for (int i = 0; i < 3749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3750(int& val) {
    const int x = 3750;
    std::cout << 3750;
    for (int i = 0; i < 3750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3751(int& val) {
    const int x = 3751;
    std::cout << 3751;
    for (int i = 0; i < 3751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3752(int& val) {
    const int x = 3752;
    std::cout << 3752;
    for (int i = 0; i < 3752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3753(int& val) {
    const int x = 3753;
    std::cout << 3753;
    for (int i = 0; i < 3753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3754(int& val) {
    const int x = 3754;
    std::cout << 3754;
    for (int i = 0; i < 3754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3755(int& val) {
    const int x = 3755;
    std::cout << 3755;
    for (int i = 0; i < 3755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3756(int& val) {
    const int x = 3756;
    std::cout << 3756;
    for (int i = 0; i < 3756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3757(int& val) {
    const int x = 3757;
    std::cout << 3757;
    for (int i = 0; i < 3757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3758(int& val) {
    const int x = 3758;
    std::cout << 3758;
    for (int i = 0; i < 3758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3759(int& val) {
    const int x = 3759;
    std::cout << 3759;
    for (int i = 0; i < 3759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3760(int& val) {
    const int x = 3760;
    std::cout << 3760;
    for (int i = 0; i < 3760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3761(int& val) {
    const int x = 3761;
    std::cout << 3761;
    for (int i = 0; i < 3761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3762(int& val) {
    const int x = 3762;
    std::cout << 3762;
    for (int i = 0; i < 3762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3763(int& val) {
    const int x = 3763;
    std::cout << 3763;
    for (int i = 0; i < 3763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3764(int& val) {
    const int x = 3764;
    std::cout << 3764;
    for (int i = 0; i < 3764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3765(int& val) {
    const int x = 3765;
    std::cout << 3765;
    for (int i = 0; i < 3765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3766(int& val) {
    const int x = 3766;
    std::cout << 3766;
    for (int i = 0; i < 3766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3767(int& val) {
    const int x = 3767;
    std::cout << 3767;
    for (int i = 0; i < 3767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3768(int& val) {
    const int x = 3768;
    std::cout << 3768;
    for (int i = 0; i < 3768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3769(int& val) {
    const int x = 3769;
    std::cout << 3769;
    for (int i = 0; i < 3769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3770(int& val) {
    const int x = 3770;
    std::cout << 3770;
    for (int i = 0; i < 3770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3771(int& val) {
    const int x = 3771;
    std::cout << 3771;
    for (int i = 0; i < 3771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3772(int& val) {
    const int x = 3772;
    std::cout << 3772;
    for (int i = 0; i < 3772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3773(int& val) {
    const int x = 3773;
    std::cout << 3773;
    for (int i = 0; i < 3773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3774(int& val) {
    const int x = 3774;
    std::cout << 3774;
    for (int i = 0; i < 3774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3775(int& val) {
    const int x = 3775;
    std::cout << 3775;
    for (int i = 0; i < 3775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3776(int& val) {
    const int x = 3776;
    std::cout << 3776;
    for (int i = 0; i < 3776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3777(int& val) {
    const int x = 3777;
    std::cout << 3777;
    for (int i = 0; i < 3777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3778(int& val) {
    const int x = 3778;
    std::cout << 3778;
    for (int i = 0; i < 3778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3779(int& val) {
    const int x = 3779;
    std::cout << 3779;
    for (int i = 0; i < 3779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3780(int& val) {
    const int x = 3780;
    std::cout << 3780;
    for (int i = 0; i < 3780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3781(int& val) {
    const int x = 3781;
    std::cout << 3781;
    for (int i = 0; i < 3781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3782(int& val) {
    const int x = 3782;
    std::cout << 3782;
    for (int i = 0; i < 3782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3783(int& val) {
    const int x = 3783;
    std::cout << 3783;
    for (int i = 0; i < 3783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3784(int& val) {
    const int x = 3784;
    std::cout << 3784;
    for (int i = 0; i < 3784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3785(int& val) {
    const int x = 3785;
    std::cout << 3785;
    for (int i = 0; i < 3785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3786(int& val) {
    const int x = 3786;
    std::cout << 3786;
    for (int i = 0; i < 3786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3787(int& val) {
    const int x = 3787;
    std::cout << 3787;
    for (int i = 0; i < 3787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3788(int& val) {
    const int x = 3788;
    std::cout << 3788;
    for (int i = 0; i < 3788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3789(int& val) {
    const int x = 3789;
    std::cout << 3789;
    for (int i = 0; i < 3789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3790(int& val) {
    const int x = 3790;
    std::cout << 3790;
    for (int i = 0; i < 3790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3791(int& val) {
    const int x = 3791;
    std::cout << 3791;
    for (int i = 0; i < 3791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3792(int& val) {
    const int x = 3792;
    std::cout << 3792;
    for (int i = 0; i < 3792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3793(int& val) {
    const int x = 3793;
    std::cout << 3793;
    for (int i = 0; i < 3793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3794(int& val) {
    const int x = 3794;
    std::cout << 3794;
    for (int i = 0; i < 3794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3795(int& val) {
    const int x = 3795;
    std::cout << 3795;
    for (int i = 0; i < 3795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3796(int& val) {
    const int x = 3796;
    std::cout << 3796;
    for (int i = 0; i < 3796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3797(int& val) {
    const int x = 3797;
    std::cout << 3797;
    for (int i = 0; i < 3797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3798(int& val) {
    const int x = 3798;
    std::cout << 3798;
    for (int i = 0; i < 3798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3799(int& val) {
    const int x = 3799;
    std::cout << 3799;
    for (int i = 0; i < 3799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3800(int& val) {
    const int x = 3800;
    std::cout << 3800;
    for (int i = 0; i < 3800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3801(int& val) {
    const int x = 3801;
    std::cout << 3801;
    for (int i = 0; i < 3801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3802(int& val) {
    const int x = 3802;
    std::cout << 3802;
    for (int i = 0; i < 3802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3803(int& val) {
    const int x = 3803;
    std::cout << 3803;
    for (int i = 0; i < 3803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3804(int& val) {
    const int x = 3804;
    std::cout << 3804;
    for (int i = 0; i < 3804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3805(int& val) {
    const int x = 3805;
    std::cout << 3805;
    for (int i = 0; i < 3805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3806(int& val) {
    const int x = 3806;
    std::cout << 3806;
    for (int i = 0; i < 3806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3807(int& val) {
    const int x = 3807;
    std::cout << 3807;
    for (int i = 0; i < 3807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3808(int& val) {
    const int x = 3808;
    std::cout << 3808;
    for (int i = 0; i < 3808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3809(int& val) {
    const int x = 3809;
    std::cout << 3809;
    for (int i = 0; i < 3809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3810(int& val) {
    const int x = 3810;
    std::cout << 3810;
    for (int i = 0; i < 3810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3811(int& val) {
    const int x = 3811;
    std::cout << 3811;
    for (int i = 0; i < 3811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3812(int& val) {
    const int x = 3812;
    std::cout << 3812;
    for (int i = 0; i < 3812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3813(int& val) {
    const int x = 3813;
    std::cout << 3813;
    for (int i = 0; i < 3813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3814(int& val) {
    const int x = 3814;
    std::cout << 3814;
    for (int i = 0; i < 3814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3815(int& val) {
    const int x = 3815;
    std::cout << 3815;
    for (int i = 0; i < 3815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3816(int& val) {
    const int x = 3816;
    std::cout << 3816;
    for (int i = 0; i < 3816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3817(int& val) {
    const int x = 3817;
    std::cout << 3817;
    for (int i = 0; i < 3817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3818(int& val) {
    const int x = 3818;
    std::cout << 3818;
    for (int i = 0; i < 3818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3819(int& val) {
    const int x = 3819;
    std::cout << 3819;
    for (int i = 0; i < 3819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3820(int& val) {
    const int x = 3820;
    std::cout << 3820;
    for (int i = 0; i < 3820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3821(int& val) {
    const int x = 3821;
    std::cout << 3821;
    for (int i = 0; i < 3821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3822(int& val) {
    const int x = 3822;
    std::cout << 3822;
    for (int i = 0; i < 3822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3823(int& val) {
    const int x = 3823;
    std::cout << 3823;
    for (int i = 0; i < 3823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3824(int& val) {
    const int x = 3824;
    std::cout << 3824;
    for (int i = 0; i < 3824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3825(int& val) {
    const int x = 3825;
    std::cout << 3825;
    for (int i = 0; i < 3825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3826(int& val) {
    const int x = 3826;
    std::cout << 3826;
    for (int i = 0; i < 3826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3827(int& val) {
    const int x = 3827;
    std::cout << 3827;
    for (int i = 0; i < 3827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3828(int& val) {
    const int x = 3828;
    std::cout << 3828;
    for (int i = 0; i < 3828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3829(int& val) {
    const int x = 3829;
    std::cout << 3829;
    for (int i = 0; i < 3829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3830(int& val) {
    const int x = 3830;
    std::cout << 3830;
    for (int i = 0; i < 3830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3831(int& val) {
    const int x = 3831;
    std::cout << 3831;
    for (int i = 0; i < 3831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3832(int& val) {
    const int x = 3832;
    std::cout << 3832;
    for (int i = 0; i < 3832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3833(int& val) {
    const int x = 3833;
    std::cout << 3833;
    for (int i = 0; i < 3833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3834(int& val) {
    const int x = 3834;
    std::cout << 3834;
    for (int i = 0; i < 3834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3835(int& val) {
    const int x = 3835;
    std::cout << 3835;
    for (int i = 0; i < 3835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3836(int& val) {
    const int x = 3836;
    std::cout << 3836;
    for (int i = 0; i < 3836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3837(int& val) {
    const int x = 3837;
    std::cout << 3837;
    for (int i = 0; i < 3837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3838(int& val) {
    const int x = 3838;
    std::cout << 3838;
    for (int i = 0; i < 3838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3839(int& val) {
    const int x = 3839;
    std::cout << 3839;
    for (int i = 0; i < 3839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3840(int& val) {
    const int x = 3840;
    std::cout << 3840;
    for (int i = 0; i < 3840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3841(int& val) {
    const int x = 3841;
    std::cout << 3841;
    for (int i = 0; i < 3841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3842(int& val) {
    const int x = 3842;
    std::cout << 3842;
    for (int i = 0; i < 3842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3843(int& val) {
    const int x = 3843;
    std::cout << 3843;
    for (int i = 0; i < 3843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3844(int& val) {
    const int x = 3844;
    std::cout << 3844;
    for (int i = 0; i < 3844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3845(int& val) {
    const int x = 3845;
    std::cout << 3845;
    for (int i = 0; i < 3845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3846(int& val) {
    const int x = 3846;
    std::cout << 3846;
    for (int i = 0; i < 3846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3847(int& val) {
    const int x = 3847;
    std::cout << 3847;
    for (int i = 0; i < 3847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3848(int& val) {
    const int x = 3848;
    std::cout << 3848;
    for (int i = 0; i < 3848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3849(int& val) {
    const int x = 3849;
    std::cout << 3849;
    for (int i = 0; i < 3849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3850(int& val) {
    const int x = 3850;
    std::cout << 3850;
    for (int i = 0; i < 3850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3851(int& val) {
    const int x = 3851;
    std::cout << 3851;
    for (int i = 0; i < 3851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3852(int& val) {
    const int x = 3852;
    std::cout << 3852;
    for (int i = 0; i < 3852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3853(int& val) {
    const int x = 3853;
    std::cout << 3853;
    for (int i = 0; i < 3853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3854(int& val) {
    const int x = 3854;
    std::cout << 3854;
    for (int i = 0; i < 3854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3855(int& val) {
    const int x = 3855;
    std::cout << 3855;
    for (int i = 0; i < 3855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3856(int& val) {
    const int x = 3856;
    std::cout << 3856;
    for (int i = 0; i < 3856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3857(int& val) {
    const int x = 3857;
    std::cout << 3857;
    for (int i = 0; i < 3857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3858(int& val) {
    const int x = 3858;
    std::cout << 3858;
    for (int i = 0; i < 3858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3859(int& val) {
    const int x = 3859;
    std::cout << 3859;
    for (int i = 0; i < 3859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3860(int& val) {
    const int x = 3860;
    std::cout << 3860;
    for (int i = 0; i < 3860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3861(int& val) {
    const int x = 3861;
    std::cout << 3861;
    for (int i = 0; i < 3861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3862(int& val) {
    const int x = 3862;
    std::cout << 3862;
    for (int i = 0; i < 3862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3863(int& val) {
    const int x = 3863;
    std::cout << 3863;
    for (int i = 0; i < 3863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3864(int& val) {
    const int x = 3864;
    std::cout << 3864;
    for (int i = 0; i < 3864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3865(int& val) {
    const int x = 3865;
    std::cout << 3865;
    for (int i = 0; i < 3865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3866(int& val) {
    const int x = 3866;
    std::cout << 3866;
    for (int i = 0; i < 3866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3867(int& val) {
    const int x = 3867;
    std::cout << 3867;
    for (int i = 0; i < 3867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3868(int& val) {
    const int x = 3868;
    std::cout << 3868;
    for (int i = 0; i < 3868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3869(int& val) {
    const int x = 3869;
    std::cout << 3869;
    for (int i = 0; i < 3869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3870(int& val) {
    const int x = 3870;
    std::cout << 3870;
    for (int i = 0; i < 3870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3871(int& val) {
    const int x = 3871;
    std::cout << 3871;
    for (int i = 0; i < 3871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3872(int& val) {
    const int x = 3872;
    std::cout << 3872;
    for (int i = 0; i < 3872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3873(int& val) {
    const int x = 3873;
    std::cout << 3873;
    for (int i = 0; i < 3873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3874(int& val) {
    const int x = 3874;
    std::cout << 3874;
    for (int i = 0; i < 3874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3875(int& val) {
    const int x = 3875;
    std::cout << 3875;
    for (int i = 0; i < 3875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3876(int& val) {
    const int x = 3876;
    std::cout << 3876;
    for (int i = 0; i < 3876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3877(int& val) {
    const int x = 3877;
    std::cout << 3877;
    for (int i = 0; i < 3877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3878(int& val) {
    const int x = 3878;
    std::cout << 3878;
    for (int i = 0; i < 3878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3879(int& val) {
    const int x = 3879;
    std::cout << 3879;
    for (int i = 0; i < 3879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3880(int& val) {
    const int x = 3880;
    std::cout << 3880;
    for (int i = 0; i < 3880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3881(int& val) {
    const int x = 3881;
    std::cout << 3881;
    for (int i = 0; i < 3881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3882(int& val) {
    const int x = 3882;
    std::cout << 3882;
    for (int i = 0; i < 3882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3883(int& val) {
    const int x = 3883;
    std::cout << 3883;
    for (int i = 0; i < 3883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3884(int& val) {
    const int x = 3884;
    std::cout << 3884;
    for (int i = 0; i < 3884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3885(int& val) {
    const int x = 3885;
    std::cout << 3885;
    for (int i = 0; i < 3885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3886(int& val) {
    const int x = 3886;
    std::cout << 3886;
    for (int i = 0; i < 3886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3887(int& val) {
    const int x = 3887;
    std::cout << 3887;
    for (int i = 0; i < 3887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3888(int& val) {
    const int x = 3888;
    std::cout << 3888;
    for (int i = 0; i < 3888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3889(int& val) {
    const int x = 3889;
    std::cout << 3889;
    for (int i = 0; i < 3889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3890(int& val) {
    const int x = 3890;
    std::cout << 3890;
    for (int i = 0; i < 3890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3891(int& val) {
    const int x = 3891;
    std::cout << 3891;
    for (int i = 0; i < 3891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3892(int& val) {
    const int x = 3892;
    std::cout << 3892;
    for (int i = 0; i < 3892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3893(int& val) {
    const int x = 3893;
    std::cout << 3893;
    for (int i = 0; i < 3893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3894(int& val) {
    const int x = 3894;
    std::cout << 3894;
    for (int i = 0; i < 3894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3895(int& val) {
    const int x = 3895;
    std::cout << 3895;
    for (int i = 0; i < 3895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3896(int& val) {
    const int x = 3896;
    std::cout << 3896;
    for (int i = 0; i < 3896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3897(int& val) {
    const int x = 3897;
    std::cout << 3897;
    for (int i = 0; i < 3897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3898(int& val) {
    const int x = 3898;
    std::cout << 3898;
    for (int i = 0; i < 3898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3899(int& val) {
    const int x = 3899;
    std::cout << 3899;
    for (int i = 0; i < 3899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3900(int& val) {
    const int x = 3900;
    std::cout << 3900;
    for (int i = 0; i < 3900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3901(int& val) {
    const int x = 3901;
    std::cout << 3901;
    for (int i = 0; i < 3901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3902(int& val) {
    const int x = 3902;
    std::cout << 3902;
    for (int i = 0; i < 3902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3903(int& val) {
    const int x = 3903;
    std::cout << 3903;
    for (int i = 0; i < 3903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3904(int& val) {
    const int x = 3904;
    std::cout << 3904;
    for (int i = 0; i < 3904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3905(int& val) {
    const int x = 3905;
    std::cout << 3905;
    for (int i = 0; i < 3905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3906(int& val) {
    const int x = 3906;
    std::cout << 3906;
    for (int i = 0; i < 3906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3907(int& val) {
    const int x = 3907;
    std::cout << 3907;
    for (int i = 0; i < 3907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3908(int& val) {
    const int x = 3908;
    std::cout << 3908;
    for (int i = 0; i < 3908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3909(int& val) {
    const int x = 3909;
    std::cout << 3909;
    for (int i = 0; i < 3909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3910(int& val) {
    const int x = 3910;
    std::cout << 3910;
    for (int i = 0; i < 3910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3911(int& val) {
    const int x = 3911;
    std::cout << 3911;
    for (int i = 0; i < 3911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3912(int& val) {
    const int x = 3912;
    std::cout << 3912;
    for (int i = 0; i < 3912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3913(int& val) {
    const int x = 3913;
    std::cout << 3913;
    for (int i = 0; i < 3913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3914(int& val) {
    const int x = 3914;
    std::cout << 3914;
    for (int i = 0; i < 3914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3915(int& val) {
    const int x = 3915;
    std::cout << 3915;
    for (int i = 0; i < 3915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3916(int& val) {
    const int x = 3916;
    std::cout << 3916;
    for (int i = 0; i < 3916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3917(int& val) {
    const int x = 3917;
    std::cout << 3917;
    for (int i = 0; i < 3917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3918(int& val) {
    const int x = 3918;
    std::cout << 3918;
    for (int i = 0; i < 3918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3919(int& val) {
    const int x = 3919;
    std::cout << 3919;
    for (int i = 0; i < 3919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3920(int& val) {
    const int x = 3920;
    std::cout << 3920;
    for (int i = 0; i < 3920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3921(int& val) {
    const int x = 3921;
    std::cout << 3921;
    for (int i = 0; i < 3921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3922(int& val) {
    const int x = 3922;
    std::cout << 3922;
    for (int i = 0; i < 3922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3923(int& val) {
    const int x = 3923;
    std::cout << 3923;
    for (int i = 0; i < 3923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3924(int& val) {
    const int x = 3924;
    std::cout << 3924;
    for (int i = 0; i < 3924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3925(int& val) {
    const int x = 3925;
    std::cout << 3925;
    for (int i = 0; i < 3925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3926(int& val) {
    const int x = 3926;
    std::cout << 3926;
    for (int i = 0; i < 3926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3927(int& val) {
    const int x = 3927;
    std::cout << 3927;
    for (int i = 0; i < 3927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3928(int& val) {
    const int x = 3928;
    std::cout << 3928;
    for (int i = 0; i < 3928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3929(int& val) {
    const int x = 3929;
    std::cout << 3929;
    for (int i = 0; i < 3929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3930(int& val) {
    const int x = 3930;
    std::cout << 3930;
    for (int i = 0; i < 3930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3931(int& val) {
    const int x = 3931;
    std::cout << 3931;
    for (int i = 0; i < 3931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3932(int& val) {
    const int x = 3932;
    std::cout << 3932;
    for (int i = 0; i < 3932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3933(int& val) {
    const int x = 3933;
    std::cout << 3933;
    for (int i = 0; i < 3933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3934(int& val) {
    const int x = 3934;
    std::cout << 3934;
    for (int i = 0; i < 3934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3935(int& val) {
    const int x = 3935;
    std::cout << 3935;
    for (int i = 0; i < 3935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3936(int& val) {
    const int x = 3936;
    std::cout << 3936;
    for (int i = 0; i < 3936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3937(int& val) {
    const int x = 3937;
    std::cout << 3937;
    for (int i = 0; i < 3937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3938(int& val) {
    const int x = 3938;
    std::cout << 3938;
    for (int i = 0; i < 3938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3939(int& val) {
    const int x = 3939;
    std::cout << 3939;
    for (int i = 0; i < 3939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3940(int& val) {
    const int x = 3940;
    std::cout << 3940;
    for (int i = 0; i < 3940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3941(int& val) {
    const int x = 3941;
    std::cout << 3941;
    for (int i = 0; i < 3941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3942(int& val) {
    const int x = 3942;
    std::cout << 3942;
    for (int i = 0; i < 3942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3943(int& val) {
    const int x = 3943;
    std::cout << 3943;
    for (int i = 0; i < 3943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3944(int& val) {
    const int x = 3944;
    std::cout << 3944;
    for (int i = 0; i < 3944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3945(int& val) {
    const int x = 3945;
    std::cout << 3945;
    for (int i = 0; i < 3945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3946(int& val) {
    const int x = 3946;
    std::cout << 3946;
    for (int i = 0; i < 3946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3947(int& val) {
    const int x = 3947;
    std::cout << 3947;
    for (int i = 0; i < 3947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3948(int& val) {
    const int x = 3948;
    std::cout << 3948;
    for (int i = 0; i < 3948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3949(int& val) {
    const int x = 3949;
    std::cout << 3949;
    for (int i = 0; i < 3949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3950(int& val) {
    const int x = 3950;
    std::cout << 3950;
    for (int i = 0; i < 3950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3951(int& val) {
    const int x = 3951;
    std::cout << 3951;
    for (int i = 0; i < 3951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3952(int& val) {
    const int x = 3952;
    std::cout << 3952;
    for (int i = 0; i < 3952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3953(int& val) {
    const int x = 3953;
    std::cout << 3953;
    for (int i = 0; i < 3953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3954(int& val) {
    const int x = 3954;
    std::cout << 3954;
    for (int i = 0; i < 3954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3955(int& val) {
    const int x = 3955;
    std::cout << 3955;
    for (int i = 0; i < 3955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3956(int& val) {
    const int x = 3956;
    std::cout << 3956;
    for (int i = 0; i < 3956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3957(int& val) {
    const int x = 3957;
    std::cout << 3957;
    for (int i = 0; i < 3957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3958(int& val) {
    const int x = 3958;
    std::cout << 3958;
    for (int i = 0; i < 3958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3959(int& val) {
    const int x = 3959;
    std::cout << 3959;
    for (int i = 0; i < 3959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3960(int& val) {
    const int x = 3960;
    std::cout << 3960;
    for (int i = 0; i < 3960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3961(int& val) {
    const int x = 3961;
    std::cout << 3961;
    for (int i = 0; i < 3961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3962(int& val) {
    const int x = 3962;
    std::cout << 3962;
    for (int i = 0; i < 3962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3963(int& val) {
    const int x = 3963;
    std::cout << 3963;
    for (int i = 0; i < 3963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3964(int& val) {
    const int x = 3964;
    std::cout << 3964;
    for (int i = 0; i < 3964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3965(int& val) {
    const int x = 3965;
    std::cout << 3965;
    for (int i = 0; i < 3965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3966(int& val) {
    const int x = 3966;
    std::cout << 3966;
    for (int i = 0; i < 3966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3967(int& val) {
    const int x = 3967;
    std::cout << 3967;
    for (int i = 0; i < 3967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3968(int& val) {
    const int x = 3968;
    std::cout << 3968;
    for (int i = 0; i < 3968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3969(int& val) {
    const int x = 3969;
    std::cout << 3969;
    for (int i = 0; i < 3969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3970(int& val) {
    const int x = 3970;
    std::cout << 3970;
    for (int i = 0; i < 3970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3971(int& val) {
    const int x = 3971;
    std::cout << 3971;
    for (int i = 0; i < 3971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3972(int& val) {
    const int x = 3972;
    std::cout << 3972;
    for (int i = 0; i < 3972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3973(int& val) {
    const int x = 3973;
    std::cout << 3973;
    for (int i = 0; i < 3973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3974(int& val) {
    const int x = 3974;
    std::cout << 3974;
    for (int i = 0; i < 3974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3975(int& val) {
    const int x = 3975;
    std::cout << 3975;
    for (int i = 0; i < 3975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3976(int& val) {
    const int x = 3976;
    std::cout << 3976;
    for (int i = 0; i < 3976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3977(int& val) {
    const int x = 3977;
    std::cout << 3977;
    for (int i = 0; i < 3977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3978(int& val) {
    const int x = 3978;
    std::cout << 3978;
    for (int i = 0; i < 3978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3979(int& val) {
    const int x = 3979;
    std::cout << 3979;
    for (int i = 0; i < 3979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3980(int& val) {
    const int x = 3980;
    std::cout << 3980;
    for (int i = 0; i < 3980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3981(int& val) {
    const int x = 3981;
    std::cout << 3981;
    for (int i = 0; i < 3981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3982(int& val) {
    const int x = 3982;
    std::cout << 3982;
    for (int i = 0; i < 3982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3983(int& val) {
    const int x = 3983;
    std::cout << 3983;
    for (int i = 0; i < 3983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3984(int& val) {
    const int x = 3984;
    std::cout << 3984;
    for (int i = 0; i < 3984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3985(int& val) {
    const int x = 3985;
    std::cout << 3985;
    for (int i = 0; i < 3985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3986(int& val) {
    const int x = 3986;
    std::cout << 3986;
    for (int i = 0; i < 3986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3987(int& val) {
    const int x = 3987;
    std::cout << 3987;
    for (int i = 0; i < 3987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3988(int& val) {
    const int x = 3988;
    std::cout << 3988;
    for (int i = 0; i < 3988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3989(int& val) {
    const int x = 3989;
    std::cout << 3989;
    for (int i = 0; i < 3989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3990(int& val) {
    const int x = 3990;
    std::cout << 3990;
    for (int i = 0; i < 3990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3991(int& val) {
    const int x = 3991;
    std::cout << 3991;
    for (int i = 0; i < 3991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3992(int& val) {
    const int x = 3992;
    std::cout << 3992;
    for (int i = 0; i < 3992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3993(int& val) {
    const int x = 3993;
    std::cout << 3993;
    for (int i = 0; i < 3993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3994(int& val) {
    const int x = 3994;
    std::cout << 3994;
    for (int i = 0; i < 3994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3995(int& val) {
    const int x = 3995;
    std::cout << 3995;
    for (int i = 0; i < 3995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3996(int& val) {
    const int x = 3996;
    std::cout << 3996;
    for (int i = 0; i < 3996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3997(int& val) {
    const int x = 3997;
    std::cout << 3997;
    for (int i = 0; i < 3997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3998(int& val) {
    const int x = 3998;
    std::cout << 3998;
    for (int i = 0; i < 3998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3999(int& val) {
    const int x = 3999;
    std::cout << 3999;
    for (int i = 0; i < 3999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4000(int& val) {
    const int x = 4000;
    std::cout << 4000;
    for (int i = 0; i < 4000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4001(int& val) {
    const int x = 4001;
    std::cout << 4001;
    for (int i = 0; i < 4001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4002(int& val) {
    const int x = 4002;
    std::cout << 4002;
    for (int i = 0; i < 4002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4003(int& val) {
    const int x = 4003;
    std::cout << 4003;
    for (int i = 0; i < 4003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4004(int& val) {
    const int x = 4004;
    std::cout << 4004;
    for (int i = 0; i < 4004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4005(int& val) {
    const int x = 4005;
    std::cout << 4005;
    for (int i = 0; i < 4005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4006(int& val) {
    const int x = 4006;
    std::cout << 4006;
    for (int i = 0; i < 4006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4007(int& val) {
    const int x = 4007;
    std::cout << 4007;
    for (int i = 0; i < 4007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4008(int& val) {
    const int x = 4008;
    std::cout << 4008;
    for (int i = 0; i < 4008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4009(int& val) {
    const int x = 4009;
    std::cout << 4009;
    for (int i = 0; i < 4009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4010(int& val) {
    const int x = 4010;
    std::cout << 4010;
    for (int i = 0; i < 4010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4011(int& val) {
    const int x = 4011;
    std::cout << 4011;
    for (int i = 0; i < 4011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4012(int& val) {
    const int x = 4012;
    std::cout << 4012;
    for (int i = 0; i < 4012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4013(int& val) {
    const int x = 4013;
    std::cout << 4013;
    for (int i = 0; i < 4013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4014(int& val) {
    const int x = 4014;
    std::cout << 4014;
    for (int i = 0; i < 4014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4015(int& val) {
    const int x = 4015;
    std::cout << 4015;
    for (int i = 0; i < 4015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4016(int& val) {
    const int x = 4016;
    std::cout << 4016;
    for (int i = 0; i < 4016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4017(int& val) {
    const int x = 4017;
    std::cout << 4017;
    for (int i = 0; i < 4017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4018(int& val) {
    const int x = 4018;
    std::cout << 4018;
    for (int i = 0; i < 4018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4019(int& val) {
    const int x = 4019;
    std::cout << 4019;
    for (int i = 0; i < 4019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4020(int& val) {
    const int x = 4020;
    std::cout << 4020;
    for (int i = 0; i < 4020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4021(int& val) {
    const int x = 4021;
    std::cout << 4021;
    for (int i = 0; i < 4021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4022(int& val) {
    const int x = 4022;
    std::cout << 4022;
    for (int i = 0; i < 4022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4023(int& val) {
    const int x = 4023;
    std::cout << 4023;
    for (int i = 0; i < 4023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4024(int& val) {
    const int x = 4024;
    std::cout << 4024;
    for (int i = 0; i < 4024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4025(int& val) {
    const int x = 4025;
    std::cout << 4025;
    for (int i = 0; i < 4025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4026(int& val) {
    const int x = 4026;
    std::cout << 4026;
    for (int i = 0; i < 4026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4027(int& val) {
    const int x = 4027;
    std::cout << 4027;
    for (int i = 0; i < 4027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4028(int& val) {
    const int x = 4028;
    std::cout << 4028;
    for (int i = 0; i < 4028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4029(int& val) {
    const int x = 4029;
    std::cout << 4029;
    for (int i = 0; i < 4029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4030(int& val) {
    const int x = 4030;
    std::cout << 4030;
    for (int i = 0; i < 4030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4031(int& val) {
    const int x = 4031;
    std::cout << 4031;
    for (int i = 0; i < 4031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4032(int& val) {
    const int x = 4032;
    std::cout << 4032;
    for (int i = 0; i < 4032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4033(int& val) {
    const int x = 4033;
    std::cout << 4033;
    for (int i = 0; i < 4033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4034(int& val) {
    const int x = 4034;
    std::cout << 4034;
    for (int i = 0; i < 4034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4035(int& val) {
    const int x = 4035;
    std::cout << 4035;
    for (int i = 0; i < 4035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4036(int& val) {
    const int x = 4036;
    std::cout << 4036;
    for (int i = 0; i < 4036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4037(int& val) {
    const int x = 4037;
    std::cout << 4037;
    for (int i = 0; i < 4037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4038(int& val) {
    const int x = 4038;
    std::cout << 4038;
    for (int i = 0; i < 4038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4039(int& val) {
    const int x = 4039;
    std::cout << 4039;
    for (int i = 0; i < 4039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4040(int& val) {
    const int x = 4040;
    std::cout << 4040;
    for (int i = 0; i < 4040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4041(int& val) {
    const int x = 4041;
    std::cout << 4041;
    for (int i = 0; i < 4041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4042(int& val) {
    const int x = 4042;
    std::cout << 4042;
    for (int i = 0; i < 4042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4043(int& val) {
    const int x = 4043;
    std::cout << 4043;
    for (int i = 0; i < 4043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4044(int& val) {
    const int x = 4044;
    std::cout << 4044;
    for (int i = 0; i < 4044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4045(int& val) {
    const int x = 4045;
    std::cout << 4045;
    for (int i = 0; i < 4045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4046(int& val) {
    const int x = 4046;
    std::cout << 4046;
    for (int i = 0; i < 4046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4047(int& val) {
    const int x = 4047;
    std::cout << 4047;
    for (int i = 0; i < 4047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4048(int& val) {
    const int x = 4048;
    std::cout << 4048;
    for (int i = 0; i < 4048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4049(int& val) {
    const int x = 4049;
    std::cout << 4049;
    for (int i = 0; i < 4049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4050(int& val) {
    const int x = 4050;
    std::cout << 4050;
    for (int i = 0; i < 4050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4051(int& val) {
    const int x = 4051;
    std::cout << 4051;
    for (int i = 0; i < 4051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4052(int& val) {
    const int x = 4052;
    std::cout << 4052;
    for (int i = 0; i < 4052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4053(int& val) {
    const int x = 4053;
    std::cout << 4053;
    for (int i = 0; i < 4053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4054(int& val) {
    const int x = 4054;
    std::cout << 4054;
    for (int i = 0; i < 4054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4055(int& val) {
    const int x = 4055;
    std::cout << 4055;
    for (int i = 0; i < 4055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4056(int& val) {
    const int x = 4056;
    std::cout << 4056;
    for (int i = 0; i < 4056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4057(int& val) {
    const int x = 4057;
    std::cout << 4057;
    for (int i = 0; i < 4057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4058(int& val) {
    const int x = 4058;
    std::cout << 4058;
    for (int i = 0; i < 4058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4059(int& val) {
    const int x = 4059;
    std::cout << 4059;
    for (int i = 0; i < 4059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4060(int& val) {
    const int x = 4060;
    std::cout << 4060;
    for (int i = 0; i < 4060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4061(int& val) {
    const int x = 4061;
    std::cout << 4061;
    for (int i = 0; i < 4061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4062(int& val) {
    const int x = 4062;
    std::cout << 4062;
    for (int i = 0; i < 4062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4063(int& val) {
    const int x = 4063;
    std::cout << 4063;
    for (int i = 0; i < 4063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4064(int& val) {
    const int x = 4064;
    std::cout << 4064;
    for (int i = 0; i < 4064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4065(int& val) {
    const int x = 4065;
    std::cout << 4065;
    for (int i = 0; i < 4065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4066(int& val) {
    const int x = 4066;
    std::cout << 4066;
    for (int i = 0; i < 4066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4067(int& val) {
    const int x = 4067;
    std::cout << 4067;
    for (int i = 0; i < 4067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4068(int& val) {
    const int x = 4068;
    std::cout << 4068;
    for (int i = 0; i < 4068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4069(int& val) {
    const int x = 4069;
    std::cout << 4069;
    for (int i = 0; i < 4069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4070(int& val) {
    const int x = 4070;
    std::cout << 4070;
    for (int i = 0; i < 4070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4071(int& val) {
    const int x = 4071;
    std::cout << 4071;
    for (int i = 0; i < 4071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4072(int& val) {
    const int x = 4072;
    std::cout << 4072;
    for (int i = 0; i < 4072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4073(int& val) {
    const int x = 4073;
    std::cout << 4073;
    for (int i = 0; i < 4073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4074(int& val) {
    const int x = 4074;
    std::cout << 4074;
    for (int i = 0; i < 4074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4075(int& val) {
    const int x = 4075;
    std::cout << 4075;
    for (int i = 0; i < 4075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4076(int& val) {
    const int x = 4076;
    std::cout << 4076;
    for (int i = 0; i < 4076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4077(int& val) {
    const int x = 4077;
    std::cout << 4077;
    for (int i = 0; i < 4077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4078(int& val) {
    const int x = 4078;
    std::cout << 4078;
    for (int i = 0; i < 4078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4079(int& val) {
    const int x = 4079;
    std::cout << 4079;
    for (int i = 0; i < 4079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4080(int& val) {
    const int x = 4080;
    std::cout << 4080;
    for (int i = 0; i < 4080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4081(int& val) {
    const int x = 4081;
    std::cout << 4081;
    for (int i = 0; i < 4081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4082(int& val) {
    const int x = 4082;
    std::cout << 4082;
    for (int i = 0; i < 4082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4083(int& val) {
    const int x = 4083;
    std::cout << 4083;
    for (int i = 0; i < 4083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4084(int& val) {
    const int x = 4084;
    std::cout << 4084;
    for (int i = 0; i < 4084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4085(int& val) {
    const int x = 4085;
    std::cout << 4085;
    for (int i = 0; i < 4085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4086(int& val) {
    const int x = 4086;
    std::cout << 4086;
    for (int i = 0; i < 4086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4087(int& val) {
    const int x = 4087;
    std::cout << 4087;
    for (int i = 0; i < 4087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4088(int& val) {
    const int x = 4088;
    std::cout << 4088;
    for (int i = 0; i < 4088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4089(int& val) {
    const int x = 4089;
    std::cout << 4089;
    for (int i = 0; i < 4089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4090(int& val) {
    const int x = 4090;
    std::cout << 4090;
    for (int i = 0; i < 4090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4091(int& val) {
    const int x = 4091;
    std::cout << 4091;
    for (int i = 0; i < 4091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4092(int& val) {
    const int x = 4092;
    std::cout << 4092;
    for (int i = 0; i < 4092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4093(int& val) {
    const int x = 4093;
    std::cout << 4093;
    for (int i = 0; i < 4093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4094(int& val) {
    const int x = 4094;
    std::cout << 4094;
    for (int i = 0; i < 4094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4095(int& val) {
    const int x = 4095;
    std::cout << 4095;
    for (int i = 0; i < 4095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4096(int& val) {
    const int x = 4096;
    std::cout << 4096;
    for (int i = 0; i < 4096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4097(int& val) {
    const int x = 4097;
    std::cout << 4097;
    for (int i = 0; i < 4097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4098(int& val) {
    const int x = 4098;
    std::cout << 4098;
    for (int i = 0; i < 4098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4099(int& val) {
    const int x = 4099;
    std::cout << 4099;
    for (int i = 0; i < 4099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4100(int& val) {
    const int x = 4100;
    std::cout << 4100;
    for (int i = 0; i < 4100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4101(int& val) {
    const int x = 4101;
    std::cout << 4101;
    for (int i = 0; i < 4101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4102(int& val) {
    const int x = 4102;
    std::cout << 4102;
    for (int i = 0; i < 4102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4103(int& val) {
    const int x = 4103;
    std::cout << 4103;
    for (int i = 0; i < 4103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4104(int& val) {
    const int x = 4104;
    std::cout << 4104;
    for (int i = 0; i < 4104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4105(int& val) {
    const int x = 4105;
    std::cout << 4105;
    for (int i = 0; i < 4105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4106(int& val) {
    const int x = 4106;
    std::cout << 4106;
    for (int i = 0; i < 4106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4107(int& val) {
    const int x = 4107;
    std::cout << 4107;
    for (int i = 0; i < 4107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4108(int& val) {
    const int x = 4108;
    std::cout << 4108;
    for (int i = 0; i < 4108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4109(int& val) {
    const int x = 4109;
    std::cout << 4109;
    for (int i = 0; i < 4109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4110(int& val) {
    const int x = 4110;
    std::cout << 4110;
    for (int i = 0; i < 4110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4111(int& val) {
    const int x = 4111;
    std::cout << 4111;
    for (int i = 0; i < 4111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4112(int& val) {
    const int x = 4112;
    std::cout << 4112;
    for (int i = 0; i < 4112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4113(int& val) {
    const int x = 4113;
    std::cout << 4113;
    for (int i = 0; i < 4113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4114(int& val) {
    const int x = 4114;
    std::cout << 4114;
    for (int i = 0; i < 4114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4115(int& val) {
    const int x = 4115;
    std::cout << 4115;
    for (int i = 0; i < 4115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4116(int& val) {
    const int x = 4116;
    std::cout << 4116;
    for (int i = 0; i < 4116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4117(int& val) {
    const int x = 4117;
    std::cout << 4117;
    for (int i = 0; i < 4117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4118(int& val) {
    const int x = 4118;
    std::cout << 4118;
    for (int i = 0; i < 4118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4119(int& val) {
    const int x = 4119;
    std::cout << 4119;
    for (int i = 0; i < 4119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4120(int& val) {
    const int x = 4120;
    std::cout << 4120;
    for (int i = 0; i < 4120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4121(int& val) {
    const int x = 4121;
    std::cout << 4121;
    for (int i = 0; i < 4121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4122(int& val) {
    const int x = 4122;
    std::cout << 4122;
    for (int i = 0; i < 4122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4123(int& val) {
    const int x = 4123;
    std::cout << 4123;
    for (int i = 0; i < 4123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4124(int& val) {
    const int x = 4124;
    std::cout << 4124;
    for (int i = 0; i < 4124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4125(int& val) {
    const int x = 4125;
    std::cout << 4125;
    for (int i = 0; i < 4125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4126(int& val) {
    const int x = 4126;
    std::cout << 4126;
    for (int i = 0; i < 4126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4127(int& val) {
    const int x = 4127;
    std::cout << 4127;
    for (int i = 0; i < 4127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4128(int& val) {
    const int x = 4128;
    std::cout << 4128;
    for (int i = 0; i < 4128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4129(int& val) {
    const int x = 4129;
    std::cout << 4129;
    for (int i = 0; i < 4129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4130(int& val) {
    const int x = 4130;
    std::cout << 4130;
    for (int i = 0; i < 4130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4131(int& val) {
    const int x = 4131;
    std::cout << 4131;
    for (int i = 0; i < 4131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4132(int& val) {
    const int x = 4132;
    std::cout << 4132;
    for (int i = 0; i < 4132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4133(int& val) {
    const int x = 4133;
    std::cout << 4133;
    for (int i = 0; i < 4133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4134(int& val) {
    const int x = 4134;
    std::cout << 4134;
    for (int i = 0; i < 4134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4135(int& val) {
    const int x = 4135;
    std::cout << 4135;
    for (int i = 0; i < 4135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4136(int& val) {
    const int x = 4136;
    std::cout << 4136;
    for (int i = 0; i < 4136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4137(int& val) {
    const int x = 4137;
    std::cout << 4137;
    for (int i = 0; i < 4137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4138(int& val) {
    const int x = 4138;
    std::cout << 4138;
    for (int i = 0; i < 4138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4139(int& val) {
    const int x = 4139;
    std::cout << 4139;
    for (int i = 0; i < 4139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4140(int& val) {
    const int x = 4140;
    std::cout << 4140;
    for (int i = 0; i < 4140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4141(int& val) {
    const int x = 4141;
    std::cout << 4141;
    for (int i = 0; i < 4141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4142(int& val) {
    const int x = 4142;
    std::cout << 4142;
    for (int i = 0; i < 4142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4143(int& val) {
    const int x = 4143;
    std::cout << 4143;
    for (int i = 0; i < 4143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4144(int& val) {
    const int x = 4144;
    std::cout << 4144;
    for (int i = 0; i < 4144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4145(int& val) {
    const int x = 4145;
    std::cout << 4145;
    for (int i = 0; i < 4145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4146(int& val) {
    const int x = 4146;
    std::cout << 4146;
    for (int i = 0; i < 4146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4147(int& val) {
    const int x = 4147;
    std::cout << 4147;
    for (int i = 0; i < 4147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4148(int& val) {
    const int x = 4148;
    std::cout << 4148;
    for (int i = 0; i < 4148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4149(int& val) {
    const int x = 4149;
    std::cout << 4149;
    for (int i = 0; i < 4149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4150(int& val) {
    const int x = 4150;
    std::cout << 4150;
    for (int i = 0; i < 4150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4151(int& val) {
    const int x = 4151;
    std::cout << 4151;
    for (int i = 0; i < 4151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4152(int& val) {
    const int x = 4152;
    std::cout << 4152;
    for (int i = 0; i < 4152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4153(int& val) {
    const int x = 4153;
    std::cout << 4153;
    for (int i = 0; i < 4153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4154(int& val) {
    const int x = 4154;
    std::cout << 4154;
    for (int i = 0; i < 4154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4155(int& val) {
    const int x = 4155;
    std::cout << 4155;
    for (int i = 0; i < 4155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4156(int& val) {
    const int x = 4156;
    std::cout << 4156;
    for (int i = 0; i < 4156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4157(int& val) {
    const int x = 4157;
    std::cout << 4157;
    for (int i = 0; i < 4157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4158(int& val) {
    const int x = 4158;
    std::cout << 4158;
    for (int i = 0; i < 4158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4159(int& val) {
    const int x = 4159;
    std::cout << 4159;
    for (int i = 0; i < 4159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4160(int& val) {
    const int x = 4160;
    std::cout << 4160;
    for (int i = 0; i < 4160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4161(int& val) {
    const int x = 4161;
    std::cout << 4161;
    for (int i = 0; i < 4161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4162(int& val) {
    const int x = 4162;
    std::cout << 4162;
    for (int i = 0; i < 4162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4163(int& val) {
    const int x = 4163;
    std::cout << 4163;
    for (int i = 0; i < 4163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4164(int& val) {
    const int x = 4164;
    std::cout << 4164;
    for (int i = 0; i < 4164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4165(int& val) {
    const int x = 4165;
    std::cout << 4165;
    for (int i = 0; i < 4165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4166(int& val) {
    const int x = 4166;
    std::cout << 4166;
    for (int i = 0; i < 4166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4167(int& val) {
    const int x = 4167;
    std::cout << 4167;
    for (int i = 0; i < 4167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4168(int& val) {
    const int x = 4168;
    std::cout << 4168;
    for (int i = 0; i < 4168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4169(int& val) {
    const int x = 4169;
    std::cout << 4169;
    for (int i = 0; i < 4169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4170(int& val) {
    const int x = 4170;
    std::cout << 4170;
    for (int i = 0; i < 4170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4171(int& val) {
    const int x = 4171;
    std::cout << 4171;
    for (int i = 0; i < 4171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4172(int& val) {
    const int x = 4172;
    std::cout << 4172;
    for (int i = 0; i < 4172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4173(int& val) {
    const int x = 4173;
    std::cout << 4173;
    for (int i = 0; i < 4173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4174(int& val) {
    const int x = 4174;
    std::cout << 4174;
    for (int i = 0; i < 4174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4175(int& val) {
    const int x = 4175;
    std::cout << 4175;
    for (int i = 0; i < 4175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4176(int& val) {
    const int x = 4176;
    std::cout << 4176;
    for (int i = 0; i < 4176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4177(int& val) {
    const int x = 4177;
    std::cout << 4177;
    for (int i = 0; i < 4177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4178(int& val) {
    const int x = 4178;
    std::cout << 4178;
    for (int i = 0; i < 4178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4179(int& val) {
    const int x = 4179;
    std::cout << 4179;
    for (int i = 0; i < 4179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4180(int& val) {
    const int x = 4180;
    std::cout << 4180;
    for (int i = 0; i < 4180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4181(int& val) {
    const int x = 4181;
    std::cout << 4181;
    for (int i = 0; i < 4181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4182(int& val) {
    const int x = 4182;
    std::cout << 4182;
    for (int i = 0; i < 4182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4183(int& val) {
    const int x = 4183;
    std::cout << 4183;
    for (int i = 0; i < 4183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4184(int& val) {
    const int x = 4184;
    std::cout << 4184;
    for (int i = 0; i < 4184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4185(int& val) {
    const int x = 4185;
    std::cout << 4185;
    for (int i = 0; i < 4185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4186(int& val) {
    const int x = 4186;
    std::cout << 4186;
    for (int i = 0; i < 4186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4187(int& val) {
    const int x = 4187;
    std::cout << 4187;
    for (int i = 0; i < 4187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4188(int& val) {
    const int x = 4188;
    std::cout << 4188;
    for (int i = 0; i < 4188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4189(int& val) {
    const int x = 4189;
    std::cout << 4189;
    for (int i = 0; i < 4189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4190(int& val) {
    const int x = 4190;
    std::cout << 4190;
    for (int i = 0; i < 4190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4191(int& val) {
    const int x = 4191;
    std::cout << 4191;
    for (int i = 0; i < 4191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4192(int& val) {
    const int x = 4192;
    std::cout << 4192;
    for (int i = 0; i < 4192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4193(int& val) {
    const int x = 4193;
    std::cout << 4193;
    for (int i = 0; i < 4193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4194(int& val) {
    const int x = 4194;
    std::cout << 4194;
    for (int i = 0; i < 4194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4195(int& val) {
    const int x = 4195;
    std::cout << 4195;
    for (int i = 0; i < 4195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4196(int& val) {
    const int x = 4196;
    std::cout << 4196;
    for (int i = 0; i < 4196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4197(int& val) {
    const int x = 4197;
    std::cout << 4197;
    for (int i = 0; i < 4197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4198(int& val) {
    const int x = 4198;
    std::cout << 4198;
    for (int i = 0; i < 4198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4199(int& val) {
    const int x = 4199;
    std::cout << 4199;
    for (int i = 0; i < 4199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4200(int& val) {
    const int x = 4200;
    std::cout << 4200;
    for (int i = 0; i < 4200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4201(int& val) {
    const int x = 4201;
    std::cout << 4201;
    for (int i = 0; i < 4201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4202(int& val) {
    const int x = 4202;
    std::cout << 4202;
    for (int i = 0; i < 4202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4203(int& val) {
    const int x = 4203;
    std::cout << 4203;
    for (int i = 0; i < 4203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4204(int& val) {
    const int x = 4204;
    std::cout << 4204;
    for (int i = 0; i < 4204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4205(int& val) {
    const int x = 4205;
    std::cout << 4205;
    for (int i = 0; i < 4205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4206(int& val) {
    const int x = 4206;
    std::cout << 4206;
    for (int i = 0; i < 4206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4207(int& val) {
    const int x = 4207;
    std::cout << 4207;
    for (int i = 0; i < 4207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4208(int& val) {
    const int x = 4208;
    std::cout << 4208;
    for (int i = 0; i < 4208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4209(int& val) {
    const int x = 4209;
    std::cout << 4209;
    for (int i = 0; i < 4209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4210(int& val) {
    const int x = 4210;
    std::cout << 4210;
    for (int i = 0; i < 4210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4211(int& val) {
    const int x = 4211;
    std::cout << 4211;
    for (int i = 0; i < 4211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4212(int& val) {
    const int x = 4212;
    std::cout << 4212;
    for (int i = 0; i < 4212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4213(int& val) {
    const int x = 4213;
    std::cout << 4213;
    for (int i = 0; i < 4213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4214(int& val) {
    const int x = 4214;
    std::cout << 4214;
    for (int i = 0; i < 4214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4215(int& val) {
    const int x = 4215;
    std::cout << 4215;
    for (int i = 0; i < 4215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4216(int& val) {
    const int x = 4216;
    std::cout << 4216;
    for (int i = 0; i < 4216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4217(int& val) {
    const int x = 4217;
    std::cout << 4217;
    for (int i = 0; i < 4217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4218(int& val) {
    const int x = 4218;
    std::cout << 4218;
    for (int i = 0; i < 4218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4219(int& val) {
    const int x = 4219;
    std::cout << 4219;
    for (int i = 0; i < 4219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4220(int& val) {
    const int x = 4220;
    std::cout << 4220;
    for (int i = 0; i < 4220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4221(int& val) {
    const int x = 4221;
    std::cout << 4221;
    for (int i = 0; i < 4221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4222(int& val) {
    const int x = 4222;
    std::cout << 4222;
    for (int i = 0; i < 4222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4223(int& val) {
    const int x = 4223;
    std::cout << 4223;
    for (int i = 0; i < 4223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4224(int& val) {
    const int x = 4224;
    std::cout << 4224;
    for (int i = 0; i < 4224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4225(int& val) {
    const int x = 4225;
    std::cout << 4225;
    for (int i = 0; i < 4225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4226(int& val) {
    const int x = 4226;
    std::cout << 4226;
    for (int i = 0; i < 4226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4227(int& val) {
    const int x = 4227;
    std::cout << 4227;
    for (int i = 0; i < 4227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4228(int& val) {
    const int x = 4228;
    std::cout << 4228;
    for (int i = 0; i < 4228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4229(int& val) {
    const int x = 4229;
    std::cout << 4229;
    for (int i = 0; i < 4229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4230(int& val) {
    const int x = 4230;
    std::cout << 4230;
    for (int i = 0; i < 4230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4231(int& val) {
    const int x = 4231;
    std::cout << 4231;
    for (int i = 0; i < 4231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4232(int& val) {
    const int x = 4232;
    std::cout << 4232;
    for (int i = 0; i < 4232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4233(int& val) {
    const int x = 4233;
    std::cout << 4233;
    for (int i = 0; i < 4233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4234(int& val) {
    const int x = 4234;
    std::cout << 4234;
    for (int i = 0; i < 4234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4235(int& val) {
    const int x = 4235;
    std::cout << 4235;
    for (int i = 0; i < 4235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4236(int& val) {
    const int x = 4236;
    std::cout << 4236;
    for (int i = 0; i < 4236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4237(int& val) {
    const int x = 4237;
    std::cout << 4237;
    for (int i = 0; i < 4237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4238(int& val) {
    const int x = 4238;
    std::cout << 4238;
    for (int i = 0; i < 4238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4239(int& val) {
    const int x = 4239;
    std::cout << 4239;
    for (int i = 0; i < 4239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4240(int& val) {
    const int x = 4240;
    std::cout << 4240;
    for (int i = 0; i < 4240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4241(int& val) {
    const int x = 4241;
    std::cout << 4241;
    for (int i = 0; i < 4241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4242(int& val) {
    const int x = 4242;
    std::cout << 4242;
    for (int i = 0; i < 4242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4243(int& val) {
    const int x = 4243;
    std::cout << 4243;
    for (int i = 0; i < 4243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4244(int& val) {
    const int x = 4244;
    std::cout << 4244;
    for (int i = 0; i < 4244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4245(int& val) {
    const int x = 4245;
    std::cout << 4245;
    for (int i = 0; i < 4245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4246(int& val) {
    const int x = 4246;
    std::cout << 4246;
    for (int i = 0; i < 4246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4247(int& val) {
    const int x = 4247;
    std::cout << 4247;
    for (int i = 0; i < 4247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4248(int& val) {
    const int x = 4248;
    std::cout << 4248;
    for (int i = 0; i < 4248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4249(int& val) {
    const int x = 4249;
    std::cout << 4249;
    for (int i = 0; i < 4249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4250(int& val) {
    const int x = 4250;
    std::cout << 4250;
    for (int i = 0; i < 4250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4251(int& val) {
    const int x = 4251;
    std::cout << 4251;
    for (int i = 0; i < 4251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4252(int& val) {
    const int x = 4252;
    std::cout << 4252;
    for (int i = 0; i < 4252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4253(int& val) {
    const int x = 4253;
    std::cout << 4253;
    for (int i = 0; i < 4253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4254(int& val) {
    const int x = 4254;
    std::cout << 4254;
    for (int i = 0; i < 4254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4255(int& val) {
    const int x = 4255;
    std::cout << 4255;
    for (int i = 0; i < 4255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4256(int& val) {
    const int x = 4256;
    std::cout << 4256;
    for (int i = 0; i < 4256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4257(int& val) {
    const int x = 4257;
    std::cout << 4257;
    for (int i = 0; i < 4257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4258(int& val) {
    const int x = 4258;
    std::cout << 4258;
    for (int i = 0; i < 4258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4259(int& val) {
    const int x = 4259;
    std::cout << 4259;
    for (int i = 0; i < 4259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4260(int& val) {
    const int x = 4260;
    std::cout << 4260;
    for (int i = 0; i < 4260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4261(int& val) {
    const int x = 4261;
    std::cout << 4261;
    for (int i = 0; i < 4261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4262(int& val) {
    const int x = 4262;
    std::cout << 4262;
    for (int i = 0; i < 4262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4263(int& val) {
    const int x = 4263;
    std::cout << 4263;
    for (int i = 0; i < 4263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4264(int& val) {
    const int x = 4264;
    std::cout << 4264;
    for (int i = 0; i < 4264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4265(int& val) {
    const int x = 4265;
    std::cout << 4265;
    for (int i = 0; i < 4265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4266(int& val) {
    const int x = 4266;
    std::cout << 4266;
    for (int i = 0; i < 4266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4267(int& val) {
    const int x = 4267;
    std::cout << 4267;
    for (int i = 0; i < 4267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4268(int& val) {
    const int x = 4268;
    std::cout << 4268;
    for (int i = 0; i < 4268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4269(int& val) {
    const int x = 4269;
    std::cout << 4269;
    for (int i = 0; i < 4269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4270(int& val) {
    const int x = 4270;
    std::cout << 4270;
    for (int i = 0; i < 4270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4271(int& val) {
    const int x = 4271;
    std::cout << 4271;
    for (int i = 0; i < 4271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4272(int& val) {
    const int x = 4272;
    std::cout << 4272;
    for (int i = 0; i < 4272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4273(int& val) {
    const int x = 4273;
    std::cout << 4273;
    for (int i = 0; i < 4273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4274(int& val) {
    const int x = 4274;
    std::cout << 4274;
    for (int i = 0; i < 4274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4275(int& val) {
    const int x = 4275;
    std::cout << 4275;
    for (int i = 0; i < 4275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4276(int& val) {
    const int x = 4276;
    std::cout << 4276;
    for (int i = 0; i < 4276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4277(int& val) {
    const int x = 4277;
    std::cout << 4277;
    for (int i = 0; i < 4277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4278(int& val) {
    const int x = 4278;
    std::cout << 4278;
    for (int i = 0; i < 4278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4279(int& val) {
    const int x = 4279;
    std::cout << 4279;
    for (int i = 0; i < 4279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4280(int& val) {
    const int x = 4280;
    std::cout << 4280;
    for (int i = 0; i < 4280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4281(int& val) {
    const int x = 4281;
    std::cout << 4281;
    for (int i = 0; i < 4281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4282(int& val) {
    const int x = 4282;
    std::cout << 4282;
    for (int i = 0; i < 4282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4283(int& val) {
    const int x = 4283;
    std::cout << 4283;
    for (int i = 0; i < 4283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4284(int& val) {
    const int x = 4284;
    std::cout << 4284;
    for (int i = 0; i < 4284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4285(int& val) {
    const int x = 4285;
    std::cout << 4285;
    for (int i = 0; i < 4285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4286(int& val) {
    const int x = 4286;
    std::cout << 4286;
    for (int i = 0; i < 4286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4287(int& val) {
    const int x = 4287;
    std::cout << 4287;
    for (int i = 0; i < 4287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4288(int& val) {
    const int x = 4288;
    std::cout << 4288;
    for (int i = 0; i < 4288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4289(int& val) {
    const int x = 4289;
    std::cout << 4289;
    for (int i = 0; i < 4289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4290(int& val) {
    const int x = 4290;
    std::cout << 4290;
    for (int i = 0; i < 4290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4291(int& val) {
    const int x = 4291;
    std::cout << 4291;
    for (int i = 0; i < 4291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4292(int& val) {
    const int x = 4292;
    std::cout << 4292;
    for (int i = 0; i < 4292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4293(int& val) {
    const int x = 4293;
    std::cout << 4293;
    for (int i = 0; i < 4293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4294(int& val) {
    const int x = 4294;
    std::cout << 4294;
    for (int i = 0; i < 4294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4295(int& val) {
    const int x = 4295;
    std::cout << 4295;
    for (int i = 0; i < 4295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4296(int& val) {
    const int x = 4296;
    std::cout << 4296;
    for (int i = 0; i < 4296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4297(int& val) {
    const int x = 4297;
    std::cout << 4297;
    for (int i = 0; i < 4297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4298(int& val) {
    const int x = 4298;
    std::cout << 4298;
    for (int i = 0; i < 4298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4299(int& val) {
    const int x = 4299;
    std::cout << 4299;
    for (int i = 0; i < 4299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4300(int& val) {
    const int x = 4300;
    std::cout << 4300;
    for (int i = 0; i < 4300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4301(int& val) {
    const int x = 4301;
    std::cout << 4301;
    for (int i = 0; i < 4301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4302(int& val) {
    const int x = 4302;
    std::cout << 4302;
    for (int i = 0; i < 4302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4303(int& val) {
    const int x = 4303;
    std::cout << 4303;
    for (int i = 0; i < 4303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4304(int& val) {
    const int x = 4304;
    std::cout << 4304;
    for (int i = 0; i < 4304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4305(int& val) {
    const int x = 4305;
    std::cout << 4305;
    for (int i = 0; i < 4305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4306(int& val) {
    const int x = 4306;
    std::cout << 4306;
    for (int i = 0; i < 4306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4307(int& val) {
    const int x = 4307;
    std::cout << 4307;
    for (int i = 0; i < 4307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4308(int& val) {
    const int x = 4308;
    std::cout << 4308;
    for (int i = 0; i < 4308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4309(int& val) {
    const int x = 4309;
    std::cout << 4309;
    for (int i = 0; i < 4309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4310(int& val) {
    const int x = 4310;
    std::cout << 4310;
    for (int i = 0; i < 4310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4311(int& val) {
    const int x = 4311;
    std::cout << 4311;
    for (int i = 0; i < 4311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4312(int& val) {
    const int x = 4312;
    std::cout << 4312;
    for (int i = 0; i < 4312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4313(int& val) {
    const int x = 4313;
    std::cout << 4313;
    for (int i = 0; i < 4313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4314(int& val) {
    const int x = 4314;
    std::cout << 4314;
    for (int i = 0; i < 4314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4315(int& val) {
    const int x = 4315;
    std::cout << 4315;
    for (int i = 0; i < 4315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4316(int& val) {
    const int x = 4316;
    std::cout << 4316;
    for (int i = 0; i < 4316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4317(int& val) {
    const int x = 4317;
    std::cout << 4317;
    for (int i = 0; i < 4317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4318(int& val) {
    const int x = 4318;
    std::cout << 4318;
    for (int i = 0; i < 4318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4319(int& val) {
    const int x = 4319;
    std::cout << 4319;
    for (int i = 0; i < 4319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4320(int& val) {
    const int x = 4320;
    std::cout << 4320;
    for (int i = 0; i < 4320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4321(int& val) {
    const int x = 4321;
    std::cout << 4321;
    for (int i = 0; i < 4321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4322(int& val) {
    const int x = 4322;
    std::cout << 4322;
    for (int i = 0; i < 4322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4323(int& val) {
    const int x = 4323;
    std::cout << 4323;
    for (int i = 0; i < 4323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4324(int& val) {
    const int x = 4324;
    std::cout << 4324;
    for (int i = 0; i < 4324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4325(int& val) {
    const int x = 4325;
    std::cout << 4325;
    for (int i = 0; i < 4325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4326(int& val) {
    const int x = 4326;
    std::cout << 4326;
    for (int i = 0; i < 4326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4327(int& val) {
    const int x = 4327;
    std::cout << 4327;
    for (int i = 0; i < 4327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4328(int& val) {
    const int x = 4328;
    std::cout << 4328;
    for (int i = 0; i < 4328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4329(int& val) {
    const int x = 4329;
    std::cout << 4329;
    for (int i = 0; i < 4329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4330(int& val) {
    const int x = 4330;
    std::cout << 4330;
    for (int i = 0; i < 4330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4331(int& val) {
    const int x = 4331;
    std::cout << 4331;
    for (int i = 0; i < 4331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4332(int& val) {
    const int x = 4332;
    std::cout << 4332;
    for (int i = 0; i < 4332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4333(int& val) {
    const int x = 4333;
    std::cout << 4333;
    for (int i = 0; i < 4333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4334(int& val) {
    const int x = 4334;
    std::cout << 4334;
    for (int i = 0; i < 4334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4335(int& val) {
    const int x = 4335;
    std::cout << 4335;
    for (int i = 0; i < 4335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4336(int& val) {
    const int x = 4336;
    std::cout << 4336;
    for (int i = 0; i < 4336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4337(int& val) {
    const int x = 4337;
    std::cout << 4337;
    for (int i = 0; i < 4337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4338(int& val) {
    const int x = 4338;
    std::cout << 4338;
    for (int i = 0; i < 4338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4339(int& val) {
    const int x = 4339;
    std::cout << 4339;
    for (int i = 0; i < 4339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4340(int& val) {
    const int x = 4340;
    std::cout << 4340;
    for (int i = 0; i < 4340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4341(int& val) {
    const int x = 4341;
    std::cout << 4341;
    for (int i = 0; i < 4341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4342(int& val) {
    const int x = 4342;
    std::cout << 4342;
    for (int i = 0; i < 4342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4343(int& val) {
    const int x = 4343;
    std::cout << 4343;
    for (int i = 0; i < 4343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4344(int& val) {
    const int x = 4344;
    std::cout << 4344;
    for (int i = 0; i < 4344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4345(int& val) {
    const int x = 4345;
    std::cout << 4345;
    for (int i = 0; i < 4345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4346(int& val) {
    const int x = 4346;
    std::cout << 4346;
    for (int i = 0; i < 4346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4347(int& val) {
    const int x = 4347;
    std::cout << 4347;
    for (int i = 0; i < 4347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4348(int& val) {
    const int x = 4348;
    std::cout << 4348;
    for (int i = 0; i < 4348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4349(int& val) {
    const int x = 4349;
    std::cout << 4349;
    for (int i = 0; i < 4349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4350(int& val) {
    const int x = 4350;
    std::cout << 4350;
    for (int i = 0; i < 4350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4351(int& val) {
    const int x = 4351;
    std::cout << 4351;
    for (int i = 0; i < 4351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4352(int& val) {
    const int x = 4352;
    std::cout << 4352;
    for (int i = 0; i < 4352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4353(int& val) {
    const int x = 4353;
    std::cout << 4353;
    for (int i = 0; i < 4353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4354(int& val) {
    const int x = 4354;
    std::cout << 4354;
    for (int i = 0; i < 4354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4355(int& val) {
    const int x = 4355;
    std::cout << 4355;
    for (int i = 0; i < 4355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4356(int& val) {
    const int x = 4356;
    std::cout << 4356;
    for (int i = 0; i < 4356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4357(int& val) {
    const int x = 4357;
    std::cout << 4357;
    for (int i = 0; i < 4357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4358(int& val) {
    const int x = 4358;
    std::cout << 4358;
    for (int i = 0; i < 4358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4359(int& val) {
    const int x = 4359;
    std::cout << 4359;
    for (int i = 0; i < 4359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4360(int& val) {
    const int x = 4360;
    std::cout << 4360;
    for (int i = 0; i < 4360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4361(int& val) {
    const int x = 4361;
    std::cout << 4361;
    for (int i = 0; i < 4361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4362(int& val) {
    const int x = 4362;
    std::cout << 4362;
    for (int i = 0; i < 4362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4363(int& val) {
    const int x = 4363;
    std::cout << 4363;
    for (int i = 0; i < 4363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4364(int& val) {
    const int x = 4364;
    std::cout << 4364;
    for (int i = 0; i < 4364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4365(int& val) {
    const int x = 4365;
    std::cout << 4365;
    for (int i = 0; i < 4365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4366(int& val) {
    const int x = 4366;
    std::cout << 4366;
    for (int i = 0; i < 4366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4367(int& val) {
    const int x = 4367;
    std::cout << 4367;
    for (int i = 0; i < 4367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4368(int& val) {
    const int x = 4368;
    std::cout << 4368;
    for (int i = 0; i < 4368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4369(int& val) {
    const int x = 4369;
    std::cout << 4369;
    for (int i = 0; i < 4369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4370(int& val) {
    const int x = 4370;
    std::cout << 4370;
    for (int i = 0; i < 4370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4371(int& val) {
    const int x = 4371;
    std::cout << 4371;
    for (int i = 0; i < 4371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4372(int& val) {
    const int x = 4372;
    std::cout << 4372;
    for (int i = 0; i < 4372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4373(int& val) {
    const int x = 4373;
    std::cout << 4373;
    for (int i = 0; i < 4373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4374(int& val) {
    const int x = 4374;
    std::cout << 4374;
    for (int i = 0; i < 4374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4375(int& val) {
    const int x = 4375;
    std::cout << 4375;
    for (int i = 0; i < 4375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4376(int& val) {
    const int x = 4376;
    std::cout << 4376;
    for (int i = 0; i < 4376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4377(int& val) {
    const int x = 4377;
    std::cout << 4377;
    for (int i = 0; i < 4377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4378(int& val) {
    const int x = 4378;
    std::cout << 4378;
    for (int i = 0; i < 4378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4379(int& val) {
    const int x = 4379;
    std::cout << 4379;
    for (int i = 0; i < 4379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4380(int& val) {
    const int x = 4380;
    std::cout << 4380;
    for (int i = 0; i < 4380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4381(int& val) {
    const int x = 4381;
    std::cout << 4381;
    for (int i = 0; i < 4381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4382(int& val) {
    const int x = 4382;
    std::cout << 4382;
    for (int i = 0; i < 4382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4383(int& val) {
    const int x = 4383;
    std::cout << 4383;
    for (int i = 0; i < 4383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4384(int& val) {
    const int x = 4384;
    std::cout << 4384;
    for (int i = 0; i < 4384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4385(int& val) {
    const int x = 4385;
    std::cout << 4385;
    for (int i = 0; i < 4385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4386(int& val) {
    const int x = 4386;
    std::cout << 4386;
    for (int i = 0; i < 4386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4387(int& val) {
    const int x = 4387;
    std::cout << 4387;
    for (int i = 0; i < 4387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4388(int& val) {
    const int x = 4388;
    std::cout << 4388;
    for (int i = 0; i < 4388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4389(int& val) {
    const int x = 4389;
    std::cout << 4389;
    for (int i = 0; i < 4389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4390(int& val) {
    const int x = 4390;
    std::cout << 4390;
    for (int i = 0; i < 4390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4391(int& val) {
    const int x = 4391;
    std::cout << 4391;
    for (int i = 0; i < 4391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4392(int& val) {
    const int x = 4392;
    std::cout << 4392;
    for (int i = 0; i < 4392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4393(int& val) {
    const int x = 4393;
    std::cout << 4393;
    for (int i = 0; i < 4393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4394(int& val) {
    const int x = 4394;
    std::cout << 4394;
    for (int i = 0; i < 4394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4395(int& val) {
    const int x = 4395;
    std::cout << 4395;
    for (int i = 0; i < 4395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4396(int& val) {
    const int x = 4396;
    std::cout << 4396;
    for (int i = 0; i < 4396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4397(int& val) {
    const int x = 4397;
    std::cout << 4397;
    for (int i = 0; i < 4397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4398(int& val) {
    const int x = 4398;
    std::cout << 4398;
    for (int i = 0; i < 4398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4399(int& val) {
    const int x = 4399;
    std::cout << 4399;
    for (int i = 0; i < 4399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4400(int& val) {
    const int x = 4400;
    std::cout << 4400;
    for (int i = 0; i < 4400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4401(int& val) {
    const int x = 4401;
    std::cout << 4401;
    for (int i = 0; i < 4401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4402(int& val) {
    const int x = 4402;
    std::cout << 4402;
    for (int i = 0; i < 4402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4403(int& val) {
    const int x = 4403;
    std::cout << 4403;
    for (int i = 0; i < 4403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4404(int& val) {
    const int x = 4404;
    std::cout << 4404;
    for (int i = 0; i < 4404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4405(int& val) {
    const int x = 4405;
    std::cout << 4405;
    for (int i = 0; i < 4405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4406(int& val) {
    const int x = 4406;
    std::cout << 4406;
    for (int i = 0; i < 4406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4407(int& val) {
    const int x = 4407;
    std::cout << 4407;
    for (int i = 0; i < 4407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4408(int& val) {
    const int x = 4408;
    std::cout << 4408;
    for (int i = 0; i < 4408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4409(int& val) {
    const int x = 4409;
    std::cout << 4409;
    for (int i = 0; i < 4409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4410(int& val) {
    const int x = 4410;
    std::cout << 4410;
    for (int i = 0; i < 4410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4411(int& val) {
    const int x = 4411;
    std::cout << 4411;
    for (int i = 0; i < 4411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4412(int& val) {
    const int x = 4412;
    std::cout << 4412;
    for (int i = 0; i < 4412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4413(int& val) {
    const int x = 4413;
    std::cout << 4413;
    for (int i = 0; i < 4413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4414(int& val) {
    const int x = 4414;
    std::cout << 4414;
    for (int i = 0; i < 4414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4415(int& val) {
    const int x = 4415;
    std::cout << 4415;
    for (int i = 0; i < 4415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4416(int& val) {
    const int x = 4416;
    std::cout << 4416;
    for (int i = 0; i < 4416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4417(int& val) {
    const int x = 4417;
    std::cout << 4417;
    for (int i = 0; i < 4417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4418(int& val) {
    const int x = 4418;
    std::cout << 4418;
    for (int i = 0; i < 4418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4419(int& val) {
    const int x = 4419;
    std::cout << 4419;
    for (int i = 0; i < 4419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4420(int& val) {
    const int x = 4420;
    std::cout << 4420;
    for (int i = 0; i < 4420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4421(int& val) {
    const int x = 4421;
    std::cout << 4421;
    for (int i = 0; i < 4421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4422(int& val) {
    const int x = 4422;
    std::cout << 4422;
    for (int i = 0; i < 4422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4423(int& val) {
    const int x = 4423;
    std::cout << 4423;
    for (int i = 0; i < 4423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4424(int& val) {
    const int x = 4424;
    std::cout << 4424;
    for (int i = 0; i < 4424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4425(int& val) {
    const int x = 4425;
    std::cout << 4425;
    for (int i = 0; i < 4425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4426(int& val) {
    const int x = 4426;
    std::cout << 4426;
    for (int i = 0; i < 4426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4427(int& val) {
    const int x = 4427;
    std::cout << 4427;
    for (int i = 0; i < 4427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4428(int& val) {
    const int x = 4428;
    std::cout << 4428;
    for (int i = 0; i < 4428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4429(int& val) {
    const int x = 4429;
    std::cout << 4429;
    for (int i = 0; i < 4429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4430(int& val) {
    const int x = 4430;
    std::cout << 4430;
    for (int i = 0; i < 4430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4431(int& val) {
    const int x = 4431;
    std::cout << 4431;
    for (int i = 0; i < 4431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4432(int& val) {
    const int x = 4432;
    std::cout << 4432;
    for (int i = 0; i < 4432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4433(int& val) {
    const int x = 4433;
    std::cout << 4433;
    for (int i = 0; i < 4433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4434(int& val) {
    const int x = 4434;
    std::cout << 4434;
    for (int i = 0; i < 4434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4435(int& val) {
    const int x = 4435;
    std::cout << 4435;
    for (int i = 0; i < 4435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4436(int& val) {
    const int x = 4436;
    std::cout << 4436;
    for (int i = 0; i < 4436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4437(int& val) {
    const int x = 4437;
    std::cout << 4437;
    for (int i = 0; i < 4437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4438(int& val) {
    const int x = 4438;
    std::cout << 4438;
    for (int i = 0; i < 4438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4439(int& val) {
    const int x = 4439;
    std::cout << 4439;
    for (int i = 0; i < 4439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4440(int& val) {
    const int x = 4440;
    std::cout << 4440;
    for (int i = 0; i < 4440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4441(int& val) {
    const int x = 4441;
    std::cout << 4441;
    for (int i = 0; i < 4441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4442(int& val) {
    const int x = 4442;
    std::cout << 4442;
    for (int i = 0; i < 4442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4443(int& val) {
    const int x = 4443;
    std::cout << 4443;
    for (int i = 0; i < 4443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4444(int& val) {
    const int x = 4444;
    std::cout << 4444;
    for (int i = 0; i < 4444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4445(int& val) {
    const int x = 4445;
    std::cout << 4445;
    for (int i = 0; i < 4445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4446(int& val) {
    const int x = 4446;
    std::cout << 4446;
    for (int i = 0; i < 4446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4447(int& val) {
    const int x = 4447;
    std::cout << 4447;
    for (int i = 0; i < 4447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4448(int& val) {
    const int x = 4448;
    std::cout << 4448;
    for (int i = 0; i < 4448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4449(int& val) {
    const int x = 4449;
    std::cout << 4449;
    for (int i = 0; i < 4449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4450(int& val) {
    const int x = 4450;
    std::cout << 4450;
    for (int i = 0; i < 4450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4451(int& val) {
    const int x = 4451;
    std::cout << 4451;
    for (int i = 0; i < 4451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4452(int& val) {
    const int x = 4452;
    std::cout << 4452;
    for (int i = 0; i < 4452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4453(int& val) {
    const int x = 4453;
    std::cout << 4453;
    for (int i = 0; i < 4453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4454(int& val) {
    const int x = 4454;
    std::cout << 4454;
    for (int i = 0; i < 4454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4455(int& val) {
    const int x = 4455;
    std::cout << 4455;
    for (int i = 0; i < 4455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4456(int& val) {
    const int x = 4456;
    std::cout << 4456;
    for (int i = 0; i < 4456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4457(int& val) {
    const int x = 4457;
    std::cout << 4457;
    for (int i = 0; i < 4457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4458(int& val) {
    const int x = 4458;
    std::cout << 4458;
    for (int i = 0; i < 4458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4459(int& val) {
    const int x = 4459;
    std::cout << 4459;
    for (int i = 0; i < 4459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4460(int& val) {
    const int x = 4460;
    std::cout << 4460;
    for (int i = 0; i < 4460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4461(int& val) {
    const int x = 4461;
    std::cout << 4461;
    for (int i = 0; i < 4461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4462(int& val) {
    const int x = 4462;
    std::cout << 4462;
    for (int i = 0; i < 4462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4463(int& val) {
    const int x = 4463;
    std::cout << 4463;
    for (int i = 0; i < 4463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4464(int& val) {
    const int x = 4464;
    std::cout << 4464;
    for (int i = 0; i < 4464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4465(int& val) {
    const int x = 4465;
    std::cout << 4465;
    for (int i = 0; i < 4465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4466(int& val) {
    const int x = 4466;
    std::cout << 4466;
    for (int i = 0; i < 4466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4467(int& val) {
    const int x = 4467;
    std::cout << 4467;
    for (int i = 0; i < 4467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4468(int& val) {
    const int x = 4468;
    std::cout << 4468;
    for (int i = 0; i < 4468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4469(int& val) {
    const int x = 4469;
    std::cout << 4469;
    for (int i = 0; i < 4469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4470(int& val) {
    const int x = 4470;
    std::cout << 4470;
    for (int i = 0; i < 4470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4471(int& val) {
    const int x = 4471;
    std::cout << 4471;
    for (int i = 0; i < 4471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4472(int& val) {
    const int x = 4472;
    std::cout << 4472;
    for (int i = 0; i < 4472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4473(int& val) {
    const int x = 4473;
    std::cout << 4473;
    for (int i = 0; i < 4473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4474(int& val) {
    const int x = 4474;
    std::cout << 4474;
    for (int i = 0; i < 4474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4475(int& val) {
    const int x = 4475;
    std::cout << 4475;
    for (int i = 0; i < 4475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4476(int& val) {
    const int x = 4476;
    std::cout << 4476;
    for (int i = 0; i < 4476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4477(int& val) {
    const int x = 4477;
    std::cout << 4477;
    for (int i = 0; i < 4477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4478(int& val) {
    const int x = 4478;
    std::cout << 4478;
    for (int i = 0; i < 4478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4479(int& val) {
    const int x = 4479;
    std::cout << 4479;
    for (int i = 0; i < 4479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4480(int& val) {
    const int x = 4480;
    std::cout << 4480;
    for (int i = 0; i < 4480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4481(int& val) {
    const int x = 4481;
    std::cout << 4481;
    for (int i = 0; i < 4481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4482(int& val) {
    const int x = 4482;
    std::cout << 4482;
    for (int i = 0; i < 4482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4483(int& val) {
    const int x = 4483;
    std::cout << 4483;
    for (int i = 0; i < 4483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4484(int& val) {
    const int x = 4484;
    std::cout << 4484;
    for (int i = 0; i < 4484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4485(int& val) {
    const int x = 4485;
    std::cout << 4485;
    for (int i = 0; i < 4485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4486(int& val) {
    const int x = 4486;
    std::cout << 4486;
    for (int i = 0; i < 4486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4487(int& val) {
    const int x = 4487;
    std::cout << 4487;
    for (int i = 0; i < 4487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4488(int& val) {
    const int x = 4488;
    std::cout << 4488;
    for (int i = 0; i < 4488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4489(int& val) {
    const int x = 4489;
    std::cout << 4489;
    for (int i = 0; i < 4489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4490(int& val) {
    const int x = 4490;
    std::cout << 4490;
    for (int i = 0; i < 4490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4491(int& val) {
    const int x = 4491;
    std::cout << 4491;
    for (int i = 0; i < 4491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4492(int& val) {
    const int x = 4492;
    std::cout << 4492;
    for (int i = 0; i < 4492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4493(int& val) {
    const int x = 4493;
    std::cout << 4493;
    for (int i = 0; i < 4493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4494(int& val) {
    const int x = 4494;
    std::cout << 4494;
    for (int i = 0; i < 4494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4495(int& val) {
    const int x = 4495;
    std::cout << 4495;
    for (int i = 0; i < 4495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4496(int& val) {
    const int x = 4496;
    std::cout << 4496;
    for (int i = 0; i < 4496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4497(int& val) {
    const int x = 4497;
    std::cout << 4497;
    for (int i = 0; i < 4497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4498(int& val) {
    const int x = 4498;
    std::cout << 4498;
    for (int i = 0; i < 4498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4499(int& val) {
    const int x = 4499;
    std::cout << 4499;
    for (int i = 0; i < 4499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4500(int& val) {
    const int x = 4500;
    std::cout << 4500;
    for (int i = 0; i < 4500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4501(int& val) {
    const int x = 4501;
    std::cout << 4501;
    for (int i = 0; i < 4501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4502(int& val) {
    const int x = 4502;
    std::cout << 4502;
    for (int i = 0; i < 4502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4503(int& val) {
    const int x = 4503;
    std::cout << 4503;
    for (int i = 0; i < 4503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4504(int& val) {
    const int x = 4504;
    std::cout << 4504;
    for (int i = 0; i < 4504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4505(int& val) {
    const int x = 4505;
    std::cout << 4505;
    for (int i = 0; i < 4505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4506(int& val) {
    const int x = 4506;
    std::cout << 4506;
    for (int i = 0; i < 4506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4507(int& val) {
    const int x = 4507;
    std::cout << 4507;
    for (int i = 0; i < 4507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4508(int& val) {
    const int x = 4508;
    std::cout << 4508;
    for (int i = 0; i < 4508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4509(int& val) {
    const int x = 4509;
    std::cout << 4509;
    for (int i = 0; i < 4509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4510(int& val) {
    const int x = 4510;
    std::cout << 4510;
    for (int i = 0; i < 4510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4511(int& val) {
    const int x = 4511;
    std::cout << 4511;
    for (int i = 0; i < 4511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4512(int& val) {
    const int x = 4512;
    std::cout << 4512;
    for (int i = 0; i < 4512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4513(int& val) {
    const int x = 4513;
    std::cout << 4513;
    for (int i = 0; i < 4513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4514(int& val) {
    const int x = 4514;
    std::cout << 4514;
    for (int i = 0; i < 4514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4515(int& val) {
    const int x = 4515;
    std::cout << 4515;
    for (int i = 0; i < 4515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4516(int& val) {
    const int x = 4516;
    std::cout << 4516;
    for (int i = 0; i < 4516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4517(int& val) {
    const int x = 4517;
    std::cout << 4517;
    for (int i = 0; i < 4517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4518(int& val) {
    const int x = 4518;
    std::cout << 4518;
    for (int i = 0; i < 4518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4519(int& val) {
    const int x = 4519;
    std::cout << 4519;
    for (int i = 0; i < 4519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4520(int& val) {
    const int x = 4520;
    std::cout << 4520;
    for (int i = 0; i < 4520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4521(int& val) {
    const int x = 4521;
    std::cout << 4521;
    for (int i = 0; i < 4521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4522(int& val) {
    const int x = 4522;
    std::cout << 4522;
    for (int i = 0; i < 4522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4523(int& val) {
    const int x = 4523;
    std::cout << 4523;
    for (int i = 0; i < 4523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4524(int& val) {
    const int x = 4524;
    std::cout << 4524;
    for (int i = 0; i < 4524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4525(int& val) {
    const int x = 4525;
    std::cout << 4525;
    for (int i = 0; i < 4525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4526(int& val) {
    const int x = 4526;
    std::cout << 4526;
    for (int i = 0; i < 4526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4527(int& val) {
    const int x = 4527;
    std::cout << 4527;
    for (int i = 0; i < 4527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4528(int& val) {
    const int x = 4528;
    std::cout << 4528;
    for (int i = 0; i < 4528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4529(int& val) {
    const int x = 4529;
    std::cout << 4529;
    for (int i = 0; i < 4529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4530(int& val) {
    const int x = 4530;
    std::cout << 4530;
    for (int i = 0; i < 4530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4531(int& val) {
    const int x = 4531;
    std::cout << 4531;
    for (int i = 0; i < 4531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4532(int& val) {
    const int x = 4532;
    std::cout << 4532;
    for (int i = 0; i < 4532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4533(int& val) {
    const int x = 4533;
    std::cout << 4533;
    for (int i = 0; i < 4533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4534(int& val) {
    const int x = 4534;
    std::cout << 4534;
    for (int i = 0; i < 4534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4535(int& val) {
    const int x = 4535;
    std::cout << 4535;
    for (int i = 0; i < 4535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4536(int& val) {
    const int x = 4536;
    std::cout << 4536;
    for (int i = 0; i < 4536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4537(int& val) {
    const int x = 4537;
    std::cout << 4537;
    for (int i = 0; i < 4537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4538(int& val) {
    const int x = 4538;
    std::cout << 4538;
    for (int i = 0; i < 4538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4539(int& val) {
    const int x = 4539;
    std::cout << 4539;
    for (int i = 0; i < 4539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4540(int& val) {
    const int x = 4540;
    std::cout << 4540;
    for (int i = 0; i < 4540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4541(int& val) {
    const int x = 4541;
    std::cout << 4541;
    for (int i = 0; i < 4541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4542(int& val) {
    const int x = 4542;
    std::cout << 4542;
    for (int i = 0; i < 4542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4543(int& val) {
    const int x = 4543;
    std::cout << 4543;
    for (int i = 0; i < 4543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4544(int& val) {
    const int x = 4544;
    std::cout << 4544;
    for (int i = 0; i < 4544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4545(int& val) {
    const int x = 4545;
    std::cout << 4545;
    for (int i = 0; i < 4545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4546(int& val) {
    const int x = 4546;
    std::cout << 4546;
    for (int i = 0; i < 4546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4547(int& val) {
    const int x = 4547;
    std::cout << 4547;
    for (int i = 0; i < 4547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4548(int& val) {
    const int x = 4548;
    std::cout << 4548;
    for (int i = 0; i < 4548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4549(int& val) {
    const int x = 4549;
    std::cout << 4549;
    for (int i = 0; i < 4549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4550(int& val) {
    const int x = 4550;
    std::cout << 4550;
    for (int i = 0; i < 4550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4551(int& val) {
    const int x = 4551;
    std::cout << 4551;
    for (int i = 0; i < 4551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4552(int& val) {
    const int x = 4552;
    std::cout << 4552;
    for (int i = 0; i < 4552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4553(int& val) {
    const int x = 4553;
    std::cout << 4553;
    for (int i = 0; i < 4553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4554(int& val) {
    const int x = 4554;
    std::cout << 4554;
    for (int i = 0; i < 4554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4555(int& val) {
    const int x = 4555;
    std::cout << 4555;
    for (int i = 0; i < 4555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4556(int& val) {
    const int x = 4556;
    std::cout << 4556;
    for (int i = 0; i < 4556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4557(int& val) {
    const int x = 4557;
    std::cout << 4557;
    for (int i = 0; i < 4557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4558(int& val) {
    const int x = 4558;
    std::cout << 4558;
    for (int i = 0; i < 4558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4559(int& val) {
    const int x = 4559;
    std::cout << 4559;
    for (int i = 0; i < 4559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4560(int& val) {
    const int x = 4560;
    std::cout << 4560;
    for (int i = 0; i < 4560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4561(int& val) {
    const int x = 4561;
    std::cout << 4561;
    for (int i = 0; i < 4561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4562(int& val) {
    const int x = 4562;
    std::cout << 4562;
    for (int i = 0; i < 4562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4563(int& val) {
    const int x = 4563;
    std::cout << 4563;
    for (int i = 0; i < 4563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4564(int& val) {
    const int x = 4564;
    std::cout << 4564;
    for (int i = 0; i < 4564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4565(int& val) {
    const int x = 4565;
    std::cout << 4565;
    for (int i = 0; i < 4565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4566(int& val) {
    const int x = 4566;
    std::cout << 4566;
    for (int i = 0; i < 4566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4567(int& val) {
    const int x = 4567;
    std::cout << 4567;
    for (int i = 0; i < 4567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4568(int& val) {
    const int x = 4568;
    std::cout << 4568;
    for (int i = 0; i < 4568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4569(int& val) {
    const int x = 4569;
    std::cout << 4569;
    for (int i = 0; i < 4569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4570(int& val) {
    const int x = 4570;
    std::cout << 4570;
    for (int i = 0; i < 4570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4571(int& val) {
    const int x = 4571;
    std::cout << 4571;
    for (int i = 0; i < 4571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4572(int& val) {
    const int x = 4572;
    std::cout << 4572;
    for (int i = 0; i < 4572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4573(int& val) {
    const int x = 4573;
    std::cout << 4573;
    for (int i = 0; i < 4573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4574(int& val) {
    const int x = 4574;
    std::cout << 4574;
    for (int i = 0; i < 4574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4575(int& val) {
    const int x = 4575;
    std::cout << 4575;
    for (int i = 0; i < 4575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4576(int& val) {
    const int x = 4576;
    std::cout << 4576;
    for (int i = 0; i < 4576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4577(int& val) {
    const int x = 4577;
    std::cout << 4577;
    for (int i = 0; i < 4577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4578(int& val) {
    const int x = 4578;
    std::cout << 4578;
    for (int i = 0; i < 4578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4579(int& val) {
    const int x = 4579;
    std::cout << 4579;
    for (int i = 0; i < 4579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4580(int& val) {
    const int x = 4580;
    std::cout << 4580;
    for (int i = 0; i < 4580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4581(int& val) {
    const int x = 4581;
    std::cout << 4581;
    for (int i = 0; i < 4581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4582(int& val) {
    const int x = 4582;
    std::cout << 4582;
    for (int i = 0; i < 4582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4583(int& val) {
    const int x = 4583;
    std::cout << 4583;
    for (int i = 0; i < 4583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4584(int& val) {
    const int x = 4584;
    std::cout << 4584;
    for (int i = 0; i < 4584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4585(int& val) {
    const int x = 4585;
    std::cout << 4585;
    for (int i = 0; i < 4585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4586(int& val) {
    const int x = 4586;
    std::cout << 4586;
    for (int i = 0; i < 4586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4587(int& val) {
    const int x = 4587;
    std::cout << 4587;
    for (int i = 0; i < 4587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4588(int& val) {
    const int x = 4588;
    std::cout << 4588;
    for (int i = 0; i < 4588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4589(int& val) {
    const int x = 4589;
    std::cout << 4589;
    for (int i = 0; i < 4589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4590(int& val) {
    const int x = 4590;
    std::cout << 4590;
    for (int i = 0; i < 4590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4591(int& val) {
    const int x = 4591;
    std::cout << 4591;
    for (int i = 0; i < 4591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4592(int& val) {
    const int x = 4592;
    std::cout << 4592;
    for (int i = 0; i < 4592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4593(int& val) {
    const int x = 4593;
    std::cout << 4593;
    for (int i = 0; i < 4593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4594(int& val) {
    const int x = 4594;
    std::cout << 4594;
    for (int i = 0; i < 4594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4595(int& val) {
    const int x = 4595;
    std::cout << 4595;
    for (int i = 0; i < 4595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4596(int& val) {
    const int x = 4596;
    std::cout << 4596;
    for (int i = 0; i < 4596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4597(int& val) {
    const int x = 4597;
    std::cout << 4597;
    for (int i = 0; i < 4597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4598(int& val) {
    const int x = 4598;
    std::cout << 4598;
    for (int i = 0; i < 4598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4599(int& val) {
    const int x = 4599;
    std::cout << 4599;
    for (int i = 0; i < 4599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4600(int& val) {
    const int x = 4600;
    std::cout << 4600;
    for (int i = 0; i < 4600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4601(int& val) {
    const int x = 4601;
    std::cout << 4601;
    for (int i = 0; i < 4601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4602(int& val) {
    const int x = 4602;
    std::cout << 4602;
    for (int i = 0; i < 4602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4603(int& val) {
    const int x = 4603;
    std::cout << 4603;
    for (int i = 0; i < 4603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4604(int& val) {
    const int x = 4604;
    std::cout << 4604;
    for (int i = 0; i < 4604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4605(int& val) {
    const int x = 4605;
    std::cout << 4605;
    for (int i = 0; i < 4605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4606(int& val) {
    const int x = 4606;
    std::cout << 4606;
    for (int i = 0; i < 4606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4607(int& val) {
    const int x = 4607;
    std::cout << 4607;
    for (int i = 0; i < 4607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4608(int& val) {
    const int x = 4608;
    std::cout << 4608;
    for (int i = 0; i < 4608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4609(int& val) {
    const int x = 4609;
    std::cout << 4609;
    for (int i = 0; i < 4609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4610(int& val) {
    const int x = 4610;
    std::cout << 4610;
    for (int i = 0; i < 4610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4611(int& val) {
    const int x = 4611;
    std::cout << 4611;
    for (int i = 0; i < 4611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4612(int& val) {
    const int x = 4612;
    std::cout << 4612;
    for (int i = 0; i < 4612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4613(int& val) {
    const int x = 4613;
    std::cout << 4613;
    for (int i = 0; i < 4613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4614(int& val) {
    const int x = 4614;
    std::cout << 4614;
    for (int i = 0; i < 4614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4615(int& val) {
    const int x = 4615;
    std::cout << 4615;
    for (int i = 0; i < 4615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4616(int& val) {
    const int x = 4616;
    std::cout << 4616;
    for (int i = 0; i < 4616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4617(int& val) {
    const int x = 4617;
    std::cout << 4617;
    for (int i = 0; i < 4617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4618(int& val) {
    const int x = 4618;
    std::cout << 4618;
    for (int i = 0; i < 4618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4619(int& val) {
    const int x = 4619;
    std::cout << 4619;
    for (int i = 0; i < 4619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4620(int& val) {
    const int x = 4620;
    std::cout << 4620;
    for (int i = 0; i < 4620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4621(int& val) {
    const int x = 4621;
    std::cout << 4621;
    for (int i = 0; i < 4621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4622(int& val) {
    const int x = 4622;
    std::cout << 4622;
    for (int i = 0; i < 4622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4623(int& val) {
    const int x = 4623;
    std::cout << 4623;
    for (int i = 0; i < 4623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4624(int& val) {
    const int x = 4624;
    std::cout << 4624;
    for (int i = 0; i < 4624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4625(int& val) {
    const int x = 4625;
    std::cout << 4625;
    for (int i = 0; i < 4625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4626(int& val) {
    const int x = 4626;
    std::cout << 4626;
    for (int i = 0; i < 4626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4627(int& val) {
    const int x = 4627;
    std::cout << 4627;
    for (int i = 0; i < 4627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4628(int& val) {
    const int x = 4628;
    std::cout << 4628;
    for (int i = 0; i < 4628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4629(int& val) {
    const int x = 4629;
    std::cout << 4629;
    for (int i = 0; i < 4629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4630(int& val) {
    const int x = 4630;
    std::cout << 4630;
    for (int i = 0; i < 4630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4631(int& val) {
    const int x = 4631;
    std::cout << 4631;
    for (int i = 0; i < 4631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4632(int& val) {
    const int x = 4632;
    std::cout << 4632;
    for (int i = 0; i < 4632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4633(int& val) {
    const int x = 4633;
    std::cout << 4633;
    for (int i = 0; i < 4633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4634(int& val) {
    const int x = 4634;
    std::cout << 4634;
    for (int i = 0; i < 4634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4635(int& val) {
    const int x = 4635;
    std::cout << 4635;
    for (int i = 0; i < 4635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4636(int& val) {
    const int x = 4636;
    std::cout << 4636;
    for (int i = 0; i < 4636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4637(int& val) {
    const int x = 4637;
    std::cout << 4637;
    for (int i = 0; i < 4637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4638(int& val) {
    const int x = 4638;
    std::cout << 4638;
    for (int i = 0; i < 4638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4639(int& val) {
    const int x = 4639;
    std::cout << 4639;
    for (int i = 0; i < 4639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4640(int& val) {
    const int x = 4640;
    std::cout << 4640;
    for (int i = 0; i < 4640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4641(int& val) {
    const int x = 4641;
    std::cout << 4641;
    for (int i = 0; i < 4641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4642(int& val) {
    const int x = 4642;
    std::cout << 4642;
    for (int i = 0; i < 4642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4643(int& val) {
    const int x = 4643;
    std::cout << 4643;
    for (int i = 0; i < 4643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4644(int& val) {
    const int x = 4644;
    std::cout << 4644;
    for (int i = 0; i < 4644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4645(int& val) {
    const int x = 4645;
    std::cout << 4645;
    for (int i = 0; i < 4645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4646(int& val) {
    const int x = 4646;
    std::cout << 4646;
    for (int i = 0; i < 4646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4647(int& val) {
    const int x = 4647;
    std::cout << 4647;
    for (int i = 0; i < 4647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4648(int& val) {
    const int x = 4648;
    std::cout << 4648;
    for (int i = 0; i < 4648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4649(int& val) {
    const int x = 4649;
    std::cout << 4649;
    for (int i = 0; i < 4649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4650(int& val) {
    const int x = 4650;
    std::cout << 4650;
    for (int i = 0; i < 4650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4651(int& val) {
    const int x = 4651;
    std::cout << 4651;
    for (int i = 0; i < 4651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4652(int& val) {
    const int x = 4652;
    std::cout << 4652;
    for (int i = 0; i < 4652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4653(int& val) {
    const int x = 4653;
    std::cout << 4653;
    for (int i = 0; i < 4653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4654(int& val) {
    const int x = 4654;
    std::cout << 4654;
    for (int i = 0; i < 4654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4655(int& val) {
    const int x = 4655;
    std::cout << 4655;
    for (int i = 0; i < 4655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4656(int& val) {
    const int x = 4656;
    std::cout << 4656;
    for (int i = 0; i < 4656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4657(int& val) {
    const int x = 4657;
    std::cout << 4657;
    for (int i = 0; i < 4657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4658(int& val) {
    const int x = 4658;
    std::cout << 4658;
    for (int i = 0; i < 4658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4659(int& val) {
    const int x = 4659;
    std::cout << 4659;
    for (int i = 0; i < 4659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4660(int& val) {
    const int x = 4660;
    std::cout << 4660;
    for (int i = 0; i < 4660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4661(int& val) {
    const int x = 4661;
    std::cout << 4661;
    for (int i = 0; i < 4661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4662(int& val) {
    const int x = 4662;
    std::cout << 4662;
    for (int i = 0; i < 4662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4663(int& val) {
    const int x = 4663;
    std::cout << 4663;
    for (int i = 0; i < 4663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4664(int& val) {
    const int x = 4664;
    std::cout << 4664;
    for (int i = 0; i < 4664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4665(int& val) {
    const int x = 4665;
    std::cout << 4665;
    for (int i = 0; i < 4665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4666(int& val) {
    const int x = 4666;
    std::cout << 4666;
    for (int i = 0; i < 4666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4667(int& val) {
    const int x = 4667;
    std::cout << 4667;
    for (int i = 0; i < 4667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4668(int& val) {
    const int x = 4668;
    std::cout << 4668;
    for (int i = 0; i < 4668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4669(int& val) {
    const int x = 4669;
    std::cout << 4669;
    for (int i = 0; i < 4669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4670(int& val) {
    const int x = 4670;
    std::cout << 4670;
    for (int i = 0; i < 4670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4671(int& val) {
    const int x = 4671;
    std::cout << 4671;
    for (int i = 0; i < 4671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4672(int& val) {
    const int x = 4672;
    std::cout << 4672;
    for (int i = 0; i < 4672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4673(int& val) {
    const int x = 4673;
    std::cout << 4673;
    for (int i = 0; i < 4673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4674(int& val) {
    const int x = 4674;
    std::cout << 4674;
    for (int i = 0; i < 4674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4675(int& val) {
    const int x = 4675;
    std::cout << 4675;
    for (int i = 0; i < 4675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4676(int& val) {
    const int x = 4676;
    std::cout << 4676;
    for (int i = 0; i < 4676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4677(int& val) {
    const int x = 4677;
    std::cout << 4677;
    for (int i = 0; i < 4677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4678(int& val) {
    const int x = 4678;
    std::cout << 4678;
    for (int i = 0; i < 4678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4679(int& val) {
    const int x = 4679;
    std::cout << 4679;
    for (int i = 0; i < 4679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4680(int& val) {
    const int x = 4680;
    std::cout << 4680;
    for (int i = 0; i < 4680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4681(int& val) {
    const int x = 4681;
    std::cout << 4681;
    for (int i = 0; i < 4681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4682(int& val) {
    const int x = 4682;
    std::cout << 4682;
    for (int i = 0; i < 4682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4683(int& val) {
    const int x = 4683;
    std::cout << 4683;
    for (int i = 0; i < 4683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4684(int& val) {
    const int x = 4684;
    std::cout << 4684;
    for (int i = 0; i < 4684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4685(int& val) {
    const int x = 4685;
    std::cout << 4685;
    for (int i = 0; i < 4685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4686(int& val) {
    const int x = 4686;
    std::cout << 4686;
    for (int i = 0; i < 4686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4687(int& val) {
    const int x = 4687;
    std::cout << 4687;
    for (int i = 0; i < 4687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4688(int& val) {
    const int x = 4688;
    std::cout << 4688;
    for (int i = 0; i < 4688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4689(int& val) {
    const int x = 4689;
    std::cout << 4689;
    for (int i = 0; i < 4689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4690(int& val) {
    const int x = 4690;
    std::cout << 4690;
    for (int i = 0; i < 4690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4691(int& val) {
    const int x = 4691;
    std::cout << 4691;
    for (int i = 0; i < 4691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4692(int& val) {
    const int x = 4692;
    std::cout << 4692;
    for (int i = 0; i < 4692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4693(int& val) {
    const int x = 4693;
    std::cout << 4693;
    for (int i = 0; i < 4693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4694(int& val) {
    const int x = 4694;
    std::cout << 4694;
    for (int i = 0; i < 4694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4695(int& val) {
    const int x = 4695;
    std::cout << 4695;
    for (int i = 0; i < 4695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4696(int& val) {
    const int x = 4696;
    std::cout << 4696;
    for (int i = 0; i < 4696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4697(int& val) {
    const int x = 4697;
    std::cout << 4697;
    for (int i = 0; i < 4697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4698(int& val) {
    const int x = 4698;
    std::cout << 4698;
    for (int i = 0; i < 4698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4699(int& val) {
    const int x = 4699;
    std::cout << 4699;
    for (int i = 0; i < 4699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4700(int& val) {
    const int x = 4700;
    std::cout << 4700;
    for (int i = 0; i < 4700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4701(int& val) {
    const int x = 4701;
    std::cout << 4701;
    for (int i = 0; i < 4701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4702(int& val) {
    const int x = 4702;
    std::cout << 4702;
    for (int i = 0; i < 4702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4703(int& val) {
    const int x = 4703;
    std::cout << 4703;
    for (int i = 0; i < 4703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4704(int& val) {
    const int x = 4704;
    std::cout << 4704;
    for (int i = 0; i < 4704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4705(int& val) {
    const int x = 4705;
    std::cout << 4705;
    for (int i = 0; i < 4705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4706(int& val) {
    const int x = 4706;
    std::cout << 4706;
    for (int i = 0; i < 4706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4707(int& val) {
    const int x = 4707;
    std::cout << 4707;
    for (int i = 0; i < 4707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4708(int& val) {
    const int x = 4708;
    std::cout << 4708;
    for (int i = 0; i < 4708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4709(int& val) {
    const int x = 4709;
    std::cout << 4709;
    for (int i = 0; i < 4709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4710(int& val) {
    const int x = 4710;
    std::cout << 4710;
    for (int i = 0; i < 4710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4711(int& val) {
    const int x = 4711;
    std::cout << 4711;
    for (int i = 0; i < 4711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4712(int& val) {
    const int x = 4712;
    std::cout << 4712;
    for (int i = 0; i < 4712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4713(int& val) {
    const int x = 4713;
    std::cout << 4713;
    for (int i = 0; i < 4713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4714(int& val) {
    const int x = 4714;
    std::cout << 4714;
    for (int i = 0; i < 4714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4715(int& val) {
    const int x = 4715;
    std::cout << 4715;
    for (int i = 0; i < 4715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4716(int& val) {
    const int x = 4716;
    std::cout << 4716;
    for (int i = 0; i < 4716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4717(int& val) {
    const int x = 4717;
    std::cout << 4717;
    for (int i = 0; i < 4717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4718(int& val) {
    const int x = 4718;
    std::cout << 4718;
    for (int i = 0; i < 4718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4719(int& val) {
    const int x = 4719;
    std::cout << 4719;
    for (int i = 0; i < 4719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4720(int& val) {
    const int x = 4720;
    std::cout << 4720;
    for (int i = 0; i < 4720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4721(int& val) {
    const int x = 4721;
    std::cout << 4721;
    for (int i = 0; i < 4721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4722(int& val) {
    const int x = 4722;
    std::cout << 4722;
    for (int i = 0; i < 4722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4723(int& val) {
    const int x = 4723;
    std::cout << 4723;
    for (int i = 0; i < 4723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4724(int& val) {
    const int x = 4724;
    std::cout << 4724;
    for (int i = 0; i < 4724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4725(int& val) {
    const int x = 4725;
    std::cout << 4725;
    for (int i = 0; i < 4725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4726(int& val) {
    const int x = 4726;
    std::cout << 4726;
    for (int i = 0; i < 4726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4727(int& val) {
    const int x = 4727;
    std::cout << 4727;
    for (int i = 0; i < 4727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4728(int& val) {
    const int x = 4728;
    std::cout << 4728;
    for (int i = 0; i < 4728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4729(int& val) {
    const int x = 4729;
    std::cout << 4729;
    for (int i = 0; i < 4729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4730(int& val) {
    const int x = 4730;
    std::cout << 4730;
    for (int i = 0; i < 4730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4731(int& val) {
    const int x = 4731;
    std::cout << 4731;
    for (int i = 0; i < 4731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4732(int& val) {
    const int x = 4732;
    std::cout << 4732;
    for (int i = 0; i < 4732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4733(int& val) {
    const int x = 4733;
    std::cout << 4733;
    for (int i = 0; i < 4733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4734(int& val) {
    const int x = 4734;
    std::cout << 4734;
    for (int i = 0; i < 4734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4735(int& val) {
    const int x = 4735;
    std::cout << 4735;
    for (int i = 0; i < 4735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4736(int& val) {
    const int x = 4736;
    std::cout << 4736;
    for (int i = 0; i < 4736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4737(int& val) {
    const int x = 4737;
    std::cout << 4737;
    for (int i = 0; i < 4737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4738(int& val) {
    const int x = 4738;
    std::cout << 4738;
    for (int i = 0; i < 4738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4739(int& val) {
    const int x = 4739;
    std::cout << 4739;
    for (int i = 0; i < 4739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4740(int& val) {
    const int x = 4740;
    std::cout << 4740;
    for (int i = 0; i < 4740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4741(int& val) {
    const int x = 4741;
    std::cout << 4741;
    for (int i = 0; i < 4741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4742(int& val) {
    const int x = 4742;
    std::cout << 4742;
    for (int i = 0; i < 4742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4743(int& val) {
    const int x = 4743;
    std::cout << 4743;
    for (int i = 0; i < 4743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4744(int& val) {
    const int x = 4744;
    std::cout << 4744;
    for (int i = 0; i < 4744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4745(int& val) {
    const int x = 4745;
    std::cout << 4745;
    for (int i = 0; i < 4745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4746(int& val) {
    const int x = 4746;
    std::cout << 4746;
    for (int i = 0; i < 4746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4747(int& val) {
    const int x = 4747;
    std::cout << 4747;
    for (int i = 0; i < 4747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4748(int& val) {
    const int x = 4748;
    std::cout << 4748;
    for (int i = 0; i < 4748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4749(int& val) {
    const int x = 4749;
    std::cout << 4749;
    for (int i = 0; i < 4749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4750(int& val) {
    const int x = 4750;
    std::cout << 4750;
    for (int i = 0; i < 4750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4751(int& val) {
    const int x = 4751;
    std::cout << 4751;
    for (int i = 0; i < 4751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4752(int& val) {
    const int x = 4752;
    std::cout << 4752;
    for (int i = 0; i < 4752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4753(int& val) {
    const int x = 4753;
    std::cout << 4753;
    for (int i = 0; i < 4753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4754(int& val) {
    const int x = 4754;
    std::cout << 4754;
    for (int i = 0; i < 4754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4755(int& val) {
    const int x = 4755;
    std::cout << 4755;
    for (int i = 0; i < 4755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4756(int& val) {
    const int x = 4756;
    std::cout << 4756;
    for (int i = 0; i < 4756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4757(int& val) {
    const int x = 4757;
    std::cout << 4757;
    for (int i = 0; i < 4757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4758(int& val) {
    const int x = 4758;
    std::cout << 4758;
    for (int i = 0; i < 4758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4759(int& val) {
    const int x = 4759;
    std::cout << 4759;
    for (int i = 0; i < 4759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4760(int& val) {
    const int x = 4760;
    std::cout << 4760;
    for (int i = 0; i < 4760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4761(int& val) {
    const int x = 4761;
    std::cout << 4761;
    for (int i = 0; i < 4761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4762(int& val) {
    const int x = 4762;
    std::cout << 4762;
    for (int i = 0; i < 4762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4763(int& val) {
    const int x = 4763;
    std::cout << 4763;
    for (int i = 0; i < 4763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4764(int& val) {
    const int x = 4764;
    std::cout << 4764;
    for (int i = 0; i < 4764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4765(int& val) {
    const int x = 4765;
    std::cout << 4765;
    for (int i = 0; i < 4765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4766(int& val) {
    const int x = 4766;
    std::cout << 4766;
    for (int i = 0; i < 4766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4767(int& val) {
    const int x = 4767;
    std::cout << 4767;
    for (int i = 0; i < 4767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4768(int& val) {
    const int x = 4768;
    std::cout << 4768;
    for (int i = 0; i < 4768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4769(int& val) {
    const int x = 4769;
    std::cout << 4769;
    for (int i = 0; i < 4769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4770(int& val) {
    const int x = 4770;
    std::cout << 4770;
    for (int i = 0; i < 4770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4771(int& val) {
    const int x = 4771;
    std::cout << 4771;
    for (int i = 0; i < 4771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4772(int& val) {
    const int x = 4772;
    std::cout << 4772;
    for (int i = 0; i < 4772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4773(int& val) {
    const int x = 4773;
    std::cout << 4773;
    for (int i = 0; i < 4773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4774(int& val) {
    const int x = 4774;
    std::cout << 4774;
    for (int i = 0; i < 4774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4775(int& val) {
    const int x = 4775;
    std::cout << 4775;
    for (int i = 0; i < 4775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4776(int& val) {
    const int x = 4776;
    std::cout << 4776;
    for (int i = 0; i < 4776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4777(int& val) {
    const int x = 4777;
    std::cout << 4777;
    for (int i = 0; i < 4777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4778(int& val) {
    const int x = 4778;
    std::cout << 4778;
    for (int i = 0; i < 4778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4779(int& val) {
    const int x = 4779;
    std::cout << 4779;
    for (int i = 0; i < 4779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4780(int& val) {
    const int x = 4780;
    std::cout << 4780;
    for (int i = 0; i < 4780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4781(int& val) {
    const int x = 4781;
    std::cout << 4781;
    for (int i = 0; i < 4781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4782(int& val) {
    const int x = 4782;
    std::cout << 4782;
    for (int i = 0; i < 4782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4783(int& val) {
    const int x = 4783;
    std::cout << 4783;
    for (int i = 0; i < 4783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4784(int& val) {
    const int x = 4784;
    std::cout << 4784;
    for (int i = 0; i < 4784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4785(int& val) {
    const int x = 4785;
    std::cout << 4785;
    for (int i = 0; i < 4785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4786(int& val) {
    const int x = 4786;
    std::cout << 4786;
    for (int i = 0; i < 4786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4787(int& val) {
    const int x = 4787;
    std::cout << 4787;
    for (int i = 0; i < 4787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4788(int& val) {
    const int x = 4788;
    std::cout << 4788;
    for (int i = 0; i < 4788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4789(int& val) {
    const int x = 4789;
    std::cout << 4789;
    for (int i = 0; i < 4789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4790(int& val) {
    const int x = 4790;
    std::cout << 4790;
    for (int i = 0; i < 4790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4791(int& val) {
    const int x = 4791;
    std::cout << 4791;
    for (int i = 0; i < 4791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4792(int& val) {
    const int x = 4792;
    std::cout << 4792;
    for (int i = 0; i < 4792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4793(int& val) {
    const int x = 4793;
    std::cout << 4793;
    for (int i = 0; i < 4793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4794(int& val) {
    const int x = 4794;
    std::cout << 4794;
    for (int i = 0; i < 4794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4795(int& val) {
    const int x = 4795;
    std::cout << 4795;
    for (int i = 0; i < 4795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4796(int& val) {
    const int x = 4796;
    std::cout << 4796;
    for (int i = 0; i < 4796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4797(int& val) {
    const int x = 4797;
    std::cout << 4797;
    for (int i = 0; i < 4797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4798(int& val) {
    const int x = 4798;
    std::cout << 4798;
    for (int i = 0; i < 4798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4799(int& val) {
    const int x = 4799;
    std::cout << 4799;
    for (int i = 0; i < 4799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4800(int& val) {
    const int x = 4800;
    std::cout << 4800;
    for (int i = 0; i < 4800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4801(int& val) {
    const int x = 4801;
    std::cout << 4801;
    for (int i = 0; i < 4801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4802(int& val) {
    const int x = 4802;
    std::cout << 4802;
    for (int i = 0; i < 4802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4803(int& val) {
    const int x = 4803;
    std::cout << 4803;
    for (int i = 0; i < 4803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4804(int& val) {
    const int x = 4804;
    std::cout << 4804;
    for (int i = 0; i < 4804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4805(int& val) {
    const int x = 4805;
    std::cout << 4805;
    for (int i = 0; i < 4805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4806(int& val) {
    const int x = 4806;
    std::cout << 4806;
    for (int i = 0; i < 4806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4807(int& val) {
    const int x = 4807;
    std::cout << 4807;
    for (int i = 0; i < 4807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4808(int& val) {
    const int x = 4808;
    std::cout << 4808;
    for (int i = 0; i < 4808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4809(int& val) {
    const int x = 4809;
    std::cout << 4809;
    for (int i = 0; i < 4809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4810(int& val) {
    const int x = 4810;
    std::cout << 4810;
    for (int i = 0; i < 4810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4811(int& val) {
    const int x = 4811;
    std::cout << 4811;
    for (int i = 0; i < 4811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4812(int& val) {
    const int x = 4812;
    std::cout << 4812;
    for (int i = 0; i < 4812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4813(int& val) {
    const int x = 4813;
    std::cout << 4813;
    for (int i = 0; i < 4813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4814(int& val) {
    const int x = 4814;
    std::cout << 4814;
    for (int i = 0; i < 4814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4815(int& val) {
    const int x = 4815;
    std::cout << 4815;
    for (int i = 0; i < 4815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4816(int& val) {
    const int x = 4816;
    std::cout << 4816;
    for (int i = 0; i < 4816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4817(int& val) {
    const int x = 4817;
    std::cout << 4817;
    for (int i = 0; i < 4817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4818(int& val) {
    const int x = 4818;
    std::cout << 4818;
    for (int i = 0; i < 4818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4819(int& val) {
    const int x = 4819;
    std::cout << 4819;
    for (int i = 0; i < 4819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4820(int& val) {
    const int x = 4820;
    std::cout << 4820;
    for (int i = 0; i < 4820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4821(int& val) {
    const int x = 4821;
    std::cout << 4821;
    for (int i = 0; i < 4821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4822(int& val) {
    const int x = 4822;
    std::cout << 4822;
    for (int i = 0; i < 4822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4823(int& val) {
    const int x = 4823;
    std::cout << 4823;
    for (int i = 0; i < 4823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4824(int& val) {
    const int x = 4824;
    std::cout << 4824;
    for (int i = 0; i < 4824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4825(int& val) {
    const int x = 4825;
    std::cout << 4825;
    for (int i = 0; i < 4825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4826(int& val) {
    const int x = 4826;
    std::cout << 4826;
    for (int i = 0; i < 4826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4827(int& val) {
    const int x = 4827;
    std::cout << 4827;
    for (int i = 0; i < 4827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4828(int& val) {
    const int x = 4828;
    std::cout << 4828;
    for (int i = 0; i < 4828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4829(int& val) {
    const int x = 4829;
    std::cout << 4829;
    for (int i = 0; i < 4829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4830(int& val) {
    const int x = 4830;
    std::cout << 4830;
    for (int i = 0; i < 4830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4831(int& val) {
    const int x = 4831;
    std::cout << 4831;
    for (int i = 0; i < 4831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4832(int& val) {
    const int x = 4832;
    std::cout << 4832;
    for (int i = 0; i < 4832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4833(int& val) {
    const int x = 4833;
    std::cout << 4833;
    for (int i = 0; i < 4833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4834(int& val) {
    const int x = 4834;
    std::cout << 4834;
    for (int i = 0; i < 4834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4835(int& val) {
    const int x = 4835;
    std::cout << 4835;
    for (int i = 0; i < 4835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4836(int& val) {
    const int x = 4836;
    std::cout << 4836;
    for (int i = 0; i < 4836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4837(int& val) {
    const int x = 4837;
    std::cout << 4837;
    for (int i = 0; i < 4837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4838(int& val) {
    const int x = 4838;
    std::cout << 4838;
    for (int i = 0; i < 4838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4839(int& val) {
    const int x = 4839;
    std::cout << 4839;
    for (int i = 0; i < 4839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4840(int& val) {
    const int x = 4840;
    std::cout << 4840;
    for (int i = 0; i < 4840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4841(int& val) {
    const int x = 4841;
    std::cout << 4841;
    for (int i = 0; i < 4841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4842(int& val) {
    const int x = 4842;
    std::cout << 4842;
    for (int i = 0; i < 4842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4843(int& val) {
    const int x = 4843;
    std::cout << 4843;
    for (int i = 0; i < 4843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4844(int& val) {
    const int x = 4844;
    std::cout << 4844;
    for (int i = 0; i < 4844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4845(int& val) {
    const int x = 4845;
    std::cout << 4845;
    for (int i = 0; i < 4845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4846(int& val) {
    const int x = 4846;
    std::cout << 4846;
    for (int i = 0; i < 4846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4847(int& val) {
    const int x = 4847;
    std::cout << 4847;
    for (int i = 0; i < 4847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4848(int& val) {
    const int x = 4848;
    std::cout << 4848;
    for (int i = 0; i < 4848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4849(int& val) {
    const int x = 4849;
    std::cout << 4849;
    for (int i = 0; i < 4849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4850(int& val) {
    const int x = 4850;
    std::cout << 4850;
    for (int i = 0; i < 4850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4851(int& val) {
    const int x = 4851;
    std::cout << 4851;
    for (int i = 0; i < 4851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4852(int& val) {
    const int x = 4852;
    std::cout << 4852;
    for (int i = 0; i < 4852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4853(int& val) {
    const int x = 4853;
    std::cout << 4853;
    for (int i = 0; i < 4853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4854(int& val) {
    const int x = 4854;
    std::cout << 4854;
    for (int i = 0; i < 4854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4855(int& val) {
    const int x = 4855;
    std::cout << 4855;
    for (int i = 0; i < 4855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4856(int& val) {
    const int x = 4856;
    std::cout << 4856;
    for (int i = 0; i < 4856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4857(int& val) {
    const int x = 4857;
    std::cout << 4857;
    for (int i = 0; i < 4857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4858(int& val) {
    const int x = 4858;
    std::cout << 4858;
    for (int i = 0; i < 4858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4859(int& val) {
    const int x = 4859;
    std::cout << 4859;
    for (int i = 0; i < 4859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4860(int& val) {
    const int x = 4860;
    std::cout << 4860;
    for (int i = 0; i < 4860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4861(int& val) {
    const int x = 4861;
    std::cout << 4861;
    for (int i = 0; i < 4861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4862(int& val) {
    const int x = 4862;
    std::cout << 4862;
    for (int i = 0; i < 4862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4863(int& val) {
    const int x = 4863;
    std::cout << 4863;
    for (int i = 0; i < 4863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4864(int& val) {
    const int x = 4864;
    std::cout << 4864;
    for (int i = 0; i < 4864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4865(int& val) {
    const int x = 4865;
    std::cout << 4865;
    for (int i = 0; i < 4865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4866(int& val) {
    const int x = 4866;
    std::cout << 4866;
    for (int i = 0; i < 4866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4867(int& val) {
    const int x = 4867;
    std::cout << 4867;
    for (int i = 0; i < 4867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4868(int& val) {
    const int x = 4868;
    std::cout << 4868;
    for (int i = 0; i < 4868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4869(int& val) {
    const int x = 4869;
    std::cout << 4869;
    for (int i = 0; i < 4869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4870(int& val) {
    const int x = 4870;
    std::cout << 4870;
    for (int i = 0; i < 4870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4871(int& val) {
    const int x = 4871;
    std::cout << 4871;
    for (int i = 0; i < 4871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4872(int& val) {
    const int x = 4872;
    std::cout << 4872;
    for (int i = 0; i < 4872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4873(int& val) {
    const int x = 4873;
    std::cout << 4873;
    for (int i = 0; i < 4873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4874(int& val) {
    const int x = 4874;
    std::cout << 4874;
    for (int i = 0; i < 4874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4875(int& val) {
    const int x = 4875;
    std::cout << 4875;
    for (int i = 0; i < 4875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4876(int& val) {
    const int x = 4876;
    std::cout << 4876;
    for (int i = 0; i < 4876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4877(int& val) {
    const int x = 4877;
    std::cout << 4877;
    for (int i = 0; i < 4877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4878(int& val) {
    const int x = 4878;
    std::cout << 4878;
    for (int i = 0; i < 4878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4879(int& val) {
    const int x = 4879;
    std::cout << 4879;
    for (int i = 0; i < 4879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4880(int& val) {
    const int x = 4880;
    std::cout << 4880;
    for (int i = 0; i < 4880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4881(int& val) {
    const int x = 4881;
    std::cout << 4881;
    for (int i = 0; i < 4881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4882(int& val) {
    const int x = 4882;
    std::cout << 4882;
    for (int i = 0; i < 4882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4883(int& val) {
    const int x = 4883;
    std::cout << 4883;
    for (int i = 0; i < 4883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4884(int& val) {
    const int x = 4884;
    std::cout << 4884;
    for (int i = 0; i < 4884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4885(int& val) {
    const int x = 4885;
    std::cout << 4885;
    for (int i = 0; i < 4885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4886(int& val) {
    const int x = 4886;
    std::cout << 4886;
    for (int i = 0; i < 4886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4887(int& val) {
    const int x = 4887;
    std::cout << 4887;
    for (int i = 0; i < 4887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4888(int& val) {
    const int x = 4888;
    std::cout << 4888;
    for (int i = 0; i < 4888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4889(int& val) {
    const int x = 4889;
    std::cout << 4889;
    for (int i = 0; i < 4889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4890(int& val) {
    const int x = 4890;
    std::cout << 4890;
    for (int i = 0; i < 4890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4891(int& val) {
    const int x = 4891;
    std::cout << 4891;
    for (int i = 0; i < 4891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4892(int& val) {
    const int x = 4892;
    std::cout << 4892;
    for (int i = 0; i < 4892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4893(int& val) {
    const int x = 4893;
    std::cout << 4893;
    for (int i = 0; i < 4893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4894(int& val) {
    const int x = 4894;
    std::cout << 4894;
    for (int i = 0; i < 4894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4895(int& val) {
    const int x = 4895;
    std::cout << 4895;
    for (int i = 0; i < 4895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4896(int& val) {
    const int x = 4896;
    std::cout << 4896;
    for (int i = 0; i < 4896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4897(int& val) {
    const int x = 4897;
    std::cout << 4897;
    for (int i = 0; i < 4897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4898(int& val) {
    const int x = 4898;
    std::cout << 4898;
    for (int i = 0; i < 4898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4899(int& val) {
    const int x = 4899;
    std::cout << 4899;
    for (int i = 0; i < 4899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4900(int& val) {
    const int x = 4900;
    std::cout << 4900;
    for (int i = 0; i < 4900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4901(int& val) {
    const int x = 4901;
    std::cout << 4901;
    for (int i = 0; i < 4901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4902(int& val) {
    const int x = 4902;
    std::cout << 4902;
    for (int i = 0; i < 4902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4903(int& val) {
    const int x = 4903;
    std::cout << 4903;
    for (int i = 0; i < 4903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4904(int& val) {
    const int x = 4904;
    std::cout << 4904;
    for (int i = 0; i < 4904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4905(int& val) {
    const int x = 4905;
    std::cout << 4905;
    for (int i = 0; i < 4905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4906(int& val) {
    const int x = 4906;
    std::cout << 4906;
    for (int i = 0; i < 4906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4907(int& val) {
    const int x = 4907;
    std::cout << 4907;
    for (int i = 0; i < 4907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4908(int& val) {
    const int x = 4908;
    std::cout << 4908;
    for (int i = 0; i < 4908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4909(int& val) {
    const int x = 4909;
    std::cout << 4909;
    for (int i = 0; i < 4909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4910(int& val) {
    const int x = 4910;
    std::cout << 4910;
    for (int i = 0; i < 4910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4911(int& val) {
    const int x = 4911;
    std::cout << 4911;
    for (int i = 0; i < 4911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4912(int& val) {
    const int x = 4912;
    std::cout << 4912;
    for (int i = 0; i < 4912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4913(int& val) {
    const int x = 4913;
    std::cout << 4913;
    for (int i = 0; i < 4913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4914(int& val) {
    const int x = 4914;
    std::cout << 4914;
    for (int i = 0; i < 4914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4915(int& val) {
    const int x = 4915;
    std::cout << 4915;
    for (int i = 0; i < 4915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4916(int& val) {
    const int x = 4916;
    std::cout << 4916;
    for (int i = 0; i < 4916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4917(int& val) {
    const int x = 4917;
    std::cout << 4917;
    for (int i = 0; i < 4917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4918(int& val) {
    const int x = 4918;
    std::cout << 4918;
    for (int i = 0; i < 4918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4919(int& val) {
    const int x = 4919;
    std::cout << 4919;
    for (int i = 0; i < 4919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4920(int& val) {
    const int x = 4920;
    std::cout << 4920;
    for (int i = 0; i < 4920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4921(int& val) {
    const int x = 4921;
    std::cout << 4921;
    for (int i = 0; i < 4921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4922(int& val) {
    const int x = 4922;
    std::cout << 4922;
    for (int i = 0; i < 4922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4923(int& val) {
    const int x = 4923;
    std::cout << 4923;
    for (int i = 0; i < 4923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4924(int& val) {
    const int x = 4924;
    std::cout << 4924;
    for (int i = 0; i < 4924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4925(int& val) {
    const int x = 4925;
    std::cout << 4925;
    for (int i = 0; i < 4925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4926(int& val) {
    const int x = 4926;
    std::cout << 4926;
    for (int i = 0; i < 4926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4927(int& val) {
    const int x = 4927;
    std::cout << 4927;
    for (int i = 0; i < 4927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4928(int& val) {
    const int x = 4928;
    std::cout << 4928;
    for (int i = 0; i < 4928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4929(int& val) {
    const int x = 4929;
    std::cout << 4929;
    for (int i = 0; i < 4929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4930(int& val) {
    const int x = 4930;
    std::cout << 4930;
    for (int i = 0; i < 4930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4931(int& val) {
    const int x = 4931;
    std::cout << 4931;
    for (int i = 0; i < 4931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4932(int& val) {
    const int x = 4932;
    std::cout << 4932;
    for (int i = 0; i < 4932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4933(int& val) {
    const int x = 4933;
    std::cout << 4933;
    for (int i = 0; i < 4933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4934(int& val) {
    const int x = 4934;
    std::cout << 4934;
    for (int i = 0; i < 4934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4935(int& val) {
    const int x = 4935;
    std::cout << 4935;
    for (int i = 0; i < 4935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4936(int& val) {
    const int x = 4936;
    std::cout << 4936;
    for (int i = 0; i < 4936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4937(int& val) {
    const int x = 4937;
    std::cout << 4937;
    for (int i = 0; i < 4937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4938(int& val) {
    const int x = 4938;
    std::cout << 4938;
    for (int i = 0; i < 4938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4939(int& val) {
    const int x = 4939;
    std::cout << 4939;
    for (int i = 0; i < 4939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4940(int& val) {
    const int x = 4940;
    std::cout << 4940;
    for (int i = 0; i < 4940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4941(int& val) {
    const int x = 4941;
    std::cout << 4941;
    for (int i = 0; i < 4941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4942(int& val) {
    const int x = 4942;
    std::cout << 4942;
    for (int i = 0; i < 4942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4943(int& val) {
    const int x = 4943;
    std::cout << 4943;
    for (int i = 0; i < 4943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4944(int& val) {
    const int x = 4944;
    std::cout << 4944;
    for (int i = 0; i < 4944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4945(int& val) {
    const int x = 4945;
    std::cout << 4945;
    for (int i = 0; i < 4945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4946(int& val) {
    const int x = 4946;
    std::cout << 4946;
    for (int i = 0; i < 4946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4947(int& val) {
    const int x = 4947;
    std::cout << 4947;
    for (int i = 0; i < 4947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4948(int& val) {
    const int x = 4948;
    std::cout << 4948;
    for (int i = 0; i < 4948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4949(int& val) {
    const int x = 4949;
    std::cout << 4949;
    for (int i = 0; i < 4949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4950(int& val) {
    const int x = 4950;
    std::cout << 4950;
    for (int i = 0; i < 4950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4951(int& val) {
    const int x = 4951;
    std::cout << 4951;
    for (int i = 0; i < 4951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4952(int& val) {
    const int x = 4952;
    std::cout << 4952;
    for (int i = 0; i < 4952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4953(int& val) {
    const int x = 4953;
    std::cout << 4953;
    for (int i = 0; i < 4953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4954(int& val) {
    const int x = 4954;
    std::cout << 4954;
    for (int i = 0; i < 4954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4955(int& val) {
    const int x = 4955;
    std::cout << 4955;
    for (int i = 0; i < 4955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4956(int& val) {
    const int x = 4956;
    std::cout << 4956;
    for (int i = 0; i < 4956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4957(int& val) {
    const int x = 4957;
    std::cout << 4957;
    for (int i = 0; i < 4957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4958(int& val) {
    const int x = 4958;
    std::cout << 4958;
    for (int i = 0; i < 4958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4959(int& val) {
    const int x = 4959;
    std::cout << 4959;
    for (int i = 0; i < 4959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4960(int& val) {
    const int x = 4960;
    std::cout << 4960;
    for (int i = 0; i < 4960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4961(int& val) {
    const int x = 4961;
    std::cout << 4961;
    for (int i = 0; i < 4961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4962(int& val) {
    const int x = 4962;
    std::cout << 4962;
    for (int i = 0; i < 4962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4963(int& val) {
    const int x = 4963;
    std::cout << 4963;
    for (int i = 0; i < 4963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4964(int& val) {
    const int x = 4964;
    std::cout << 4964;
    for (int i = 0; i < 4964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4965(int& val) {
    const int x = 4965;
    std::cout << 4965;
    for (int i = 0; i < 4965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4966(int& val) {
    const int x = 4966;
    std::cout << 4966;
    for (int i = 0; i < 4966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4967(int& val) {
    const int x = 4967;
    std::cout << 4967;
    for (int i = 0; i < 4967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4968(int& val) {
    const int x = 4968;
    std::cout << 4968;
    for (int i = 0; i < 4968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4969(int& val) {
    const int x = 4969;
    std::cout << 4969;
    for (int i = 0; i < 4969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4970(int& val) {
    const int x = 4970;
    std::cout << 4970;
    for (int i = 0; i < 4970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4971(int& val) {
    const int x = 4971;
    std::cout << 4971;
    for (int i = 0; i < 4971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4972(int& val) {
    const int x = 4972;
    std::cout << 4972;
    for (int i = 0; i < 4972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4973(int& val) {
    const int x = 4973;
    std::cout << 4973;
    for (int i = 0; i < 4973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4974(int& val) {
    const int x = 4974;
    std::cout << 4974;
    for (int i = 0; i < 4974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4975(int& val) {
    const int x = 4975;
    std::cout << 4975;
    for (int i = 0; i < 4975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4976(int& val) {
    const int x = 4976;
    std::cout << 4976;
    for (int i = 0; i < 4976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4977(int& val) {
    const int x = 4977;
    std::cout << 4977;
    for (int i = 0; i < 4977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4978(int& val) {
    const int x = 4978;
    std::cout << 4978;
    for (int i = 0; i < 4978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4979(int& val) {
    const int x = 4979;
    std::cout << 4979;
    for (int i = 0; i < 4979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4980(int& val) {
    const int x = 4980;
    std::cout << 4980;
    for (int i = 0; i < 4980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4981(int& val) {
    const int x = 4981;
    std::cout << 4981;
    for (int i = 0; i < 4981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4982(int& val) {
    const int x = 4982;
    std::cout << 4982;
    for (int i = 0; i < 4982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4983(int& val) {
    const int x = 4983;
    std::cout << 4983;
    for (int i = 0; i < 4983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4984(int& val) {
    const int x = 4984;
    std::cout << 4984;
    for (int i = 0; i < 4984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4985(int& val) {
    const int x = 4985;
    std::cout << 4985;
    for (int i = 0; i < 4985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4986(int& val) {
    const int x = 4986;
    std::cout << 4986;
    for (int i = 0; i < 4986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4987(int& val) {
    const int x = 4987;
    std::cout << 4987;
    for (int i = 0; i < 4987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4988(int& val) {
    const int x = 4988;
    std::cout << 4988;
    for (int i = 0; i < 4988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4989(int& val) {
    const int x = 4989;
    std::cout << 4989;
    for (int i = 0; i < 4989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4990(int& val) {
    const int x = 4990;
    std::cout << 4990;
    for (int i = 0; i < 4990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4991(int& val) {
    const int x = 4991;
    std::cout << 4991;
    for (int i = 0; i < 4991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4992(int& val) {
    const int x = 4992;
    std::cout << 4992;
    for (int i = 0; i < 4992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4993(int& val) {
    const int x = 4993;
    std::cout << 4993;
    for (int i = 0; i < 4993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4994(int& val) {
    const int x = 4994;
    std::cout << 4994;
    for (int i = 0; i < 4994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4995(int& val) {
    const int x = 4995;
    std::cout << 4995;
    for (int i = 0; i < 4995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4996(int& val) {
    const int x = 4996;
    std::cout << 4996;
    for (int i = 0; i < 4996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4997(int& val) {
    const int x = 4997;
    std::cout << 4997;
    for (int i = 0; i < 4997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4998(int& val) {
    const int x = 4998;
    std::cout << 4998;
    for (int i = 0; i < 4998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4999(int& val) {
    const int x = 4999;
    std::cout << 4999;
    for (int i = 0; i < 4999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5000(int& val) {
    const int x = 5000;
    std::cout << 5000;
    for (int i = 0; i < 5000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5001(int& val) {
    const int x = 5001;
    std::cout << 5001;
    for (int i = 0; i < 5001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5002(int& val) {
    const int x = 5002;
    std::cout << 5002;
    for (int i = 0; i < 5002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5003(int& val) {
    const int x = 5003;
    std::cout << 5003;
    for (int i = 0; i < 5003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5004(int& val) {
    const int x = 5004;
    std::cout << 5004;
    for (int i = 0; i < 5004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5005(int& val) {
    const int x = 5005;
    std::cout << 5005;
    for (int i = 0; i < 5005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5006(int& val) {
    const int x = 5006;
    std::cout << 5006;
    for (int i = 0; i < 5006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5007(int& val) {
    const int x = 5007;
    std::cout << 5007;
    for (int i = 0; i < 5007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5008(int& val) {
    const int x = 5008;
    std::cout << 5008;
    for (int i = 0; i < 5008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5009(int& val) {
    const int x = 5009;
    std::cout << 5009;
    for (int i = 0; i < 5009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5010(int& val) {
    const int x = 5010;
    std::cout << 5010;
    for (int i = 0; i < 5010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5011(int& val) {
    const int x = 5011;
    std::cout << 5011;
    for (int i = 0; i < 5011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5012(int& val) {
    const int x = 5012;
    std::cout << 5012;
    for (int i = 0; i < 5012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5013(int& val) {
    const int x = 5013;
    std::cout << 5013;
    for (int i = 0; i < 5013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5014(int& val) {
    const int x = 5014;
    std::cout << 5014;
    for (int i = 0; i < 5014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5015(int& val) {
    const int x = 5015;
    std::cout << 5015;
    for (int i = 0; i < 5015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5016(int& val) {
    const int x = 5016;
    std::cout << 5016;
    for (int i = 0; i < 5016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5017(int& val) {
    const int x = 5017;
    std::cout << 5017;
    for (int i = 0; i < 5017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5018(int& val) {
    const int x = 5018;
    std::cout << 5018;
    for (int i = 0; i < 5018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5019(int& val) {
    const int x = 5019;
    std::cout << 5019;
    for (int i = 0; i < 5019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5020(int& val) {
    const int x = 5020;
    std::cout << 5020;
    for (int i = 0; i < 5020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5021(int& val) {
    const int x = 5021;
    std::cout << 5021;
    for (int i = 0; i < 5021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5022(int& val) {
    const int x = 5022;
    std::cout << 5022;
    for (int i = 0; i < 5022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5023(int& val) {
    const int x = 5023;
    std::cout << 5023;
    for (int i = 0; i < 5023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5024(int& val) {
    const int x = 5024;
    std::cout << 5024;
    for (int i = 0; i < 5024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5025(int& val) {
    const int x = 5025;
    std::cout << 5025;
    for (int i = 0; i < 5025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5026(int& val) {
    const int x = 5026;
    std::cout << 5026;
    for (int i = 0; i < 5026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5027(int& val) {
    const int x = 5027;
    std::cout << 5027;
    for (int i = 0; i < 5027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5028(int& val) {
    const int x = 5028;
    std::cout << 5028;
    for (int i = 0; i < 5028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5029(int& val) {
    const int x = 5029;
    std::cout << 5029;
    for (int i = 0; i < 5029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5030(int& val) {
    const int x = 5030;
    std::cout << 5030;
    for (int i = 0; i < 5030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5031(int& val) {
    const int x = 5031;
    std::cout << 5031;
    for (int i = 0; i < 5031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5032(int& val) {
    const int x = 5032;
    std::cout << 5032;
    for (int i = 0; i < 5032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5033(int& val) {
    const int x = 5033;
    std::cout << 5033;
    for (int i = 0; i < 5033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5034(int& val) {
    const int x = 5034;
    std::cout << 5034;
    for (int i = 0; i < 5034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5035(int& val) {
    const int x = 5035;
    std::cout << 5035;
    for (int i = 0; i < 5035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5036(int& val) {
    const int x = 5036;
    std::cout << 5036;
    for (int i = 0; i < 5036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5037(int& val) {
    const int x = 5037;
    std::cout << 5037;
    for (int i = 0; i < 5037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5038(int& val) {
    const int x = 5038;
    std::cout << 5038;
    for (int i = 0; i < 5038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5039(int& val) {
    const int x = 5039;
    std::cout << 5039;
    for (int i = 0; i < 5039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5040(int& val) {
    const int x = 5040;
    std::cout << 5040;
    for (int i = 0; i < 5040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5041(int& val) {
    const int x = 5041;
    std::cout << 5041;
    for (int i = 0; i < 5041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5042(int& val) {
    const int x = 5042;
    std::cout << 5042;
    for (int i = 0; i < 5042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5043(int& val) {
    const int x = 5043;
    std::cout << 5043;
    for (int i = 0; i < 5043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5044(int& val) {
    const int x = 5044;
    std::cout << 5044;
    for (int i = 0; i < 5044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5045(int& val) {
    const int x = 5045;
    std::cout << 5045;
    for (int i = 0; i < 5045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5046(int& val) {
    const int x = 5046;
    std::cout << 5046;
    for (int i = 0; i < 5046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5047(int& val) {
    const int x = 5047;
    std::cout << 5047;
    for (int i = 0; i < 5047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5048(int& val) {
    const int x = 5048;
    std::cout << 5048;
    for (int i = 0; i < 5048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5049(int& val) {
    const int x = 5049;
    std::cout << 5049;
    for (int i = 0; i < 5049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5050(int& val) {
    const int x = 5050;
    std::cout << 5050;
    for (int i = 0; i < 5050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5051(int& val) {
    const int x = 5051;
    std::cout << 5051;
    for (int i = 0; i < 5051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5052(int& val) {
    const int x = 5052;
    std::cout << 5052;
    for (int i = 0; i < 5052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5053(int& val) {
    const int x = 5053;
    std::cout << 5053;
    for (int i = 0; i < 5053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5054(int& val) {
    const int x = 5054;
    std::cout << 5054;
    for (int i = 0; i < 5054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5055(int& val) {
    const int x = 5055;
    std::cout << 5055;
    for (int i = 0; i < 5055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5056(int& val) {
    const int x = 5056;
    std::cout << 5056;
    for (int i = 0; i < 5056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5057(int& val) {
    const int x = 5057;
    std::cout << 5057;
    for (int i = 0; i < 5057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5058(int& val) {
    const int x = 5058;
    std::cout << 5058;
    for (int i = 0; i < 5058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5059(int& val) {
    const int x = 5059;
    std::cout << 5059;
    for (int i = 0; i < 5059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5060(int& val) {
    const int x = 5060;
    std::cout << 5060;
    for (int i = 0; i < 5060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5061(int& val) {
    const int x = 5061;
    std::cout << 5061;
    for (int i = 0; i < 5061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5062(int& val) {
    const int x = 5062;
    std::cout << 5062;
    for (int i = 0; i < 5062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5063(int& val) {
    const int x = 5063;
    std::cout << 5063;
    for (int i = 0; i < 5063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5064(int& val) {
    const int x = 5064;
    std::cout << 5064;
    for (int i = 0; i < 5064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5065(int& val) {
    const int x = 5065;
    std::cout << 5065;
    for (int i = 0; i < 5065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5066(int& val) {
    const int x = 5066;
    std::cout << 5066;
    for (int i = 0; i < 5066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5067(int& val) {
    const int x = 5067;
    std::cout << 5067;
    for (int i = 0; i < 5067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5068(int& val) {
    const int x = 5068;
    std::cout << 5068;
    for (int i = 0; i < 5068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5069(int& val) {
    const int x = 5069;
    std::cout << 5069;
    for (int i = 0; i < 5069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5070(int& val) {
    const int x = 5070;
    std::cout << 5070;
    for (int i = 0; i < 5070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5071(int& val) {
    const int x = 5071;
    std::cout << 5071;
    for (int i = 0; i < 5071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5072(int& val) {
    const int x = 5072;
    std::cout << 5072;
    for (int i = 0; i < 5072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5073(int& val) {
    const int x = 5073;
    std::cout << 5073;
    for (int i = 0; i < 5073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5074(int& val) {
    const int x = 5074;
    std::cout << 5074;
    for (int i = 0; i < 5074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5075(int& val) {
    const int x = 5075;
    std::cout << 5075;
    for (int i = 0; i < 5075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5076(int& val) {
    const int x = 5076;
    std::cout << 5076;
    for (int i = 0; i < 5076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5077(int& val) {
    const int x = 5077;
    std::cout << 5077;
    for (int i = 0; i < 5077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5078(int& val) {
    const int x = 5078;
    std::cout << 5078;
    for (int i = 0; i < 5078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5079(int& val) {
    const int x = 5079;
    std::cout << 5079;
    for (int i = 0; i < 5079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5080(int& val) {
    const int x = 5080;
    std::cout << 5080;
    for (int i = 0; i < 5080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5081(int& val) {
    const int x = 5081;
    std::cout << 5081;
    for (int i = 0; i < 5081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5082(int& val) {
    const int x = 5082;
    std::cout << 5082;
    for (int i = 0; i < 5082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5083(int& val) {
    const int x = 5083;
    std::cout << 5083;
    for (int i = 0; i < 5083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5084(int& val) {
    const int x = 5084;
    std::cout << 5084;
    for (int i = 0; i < 5084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5085(int& val) {
    const int x = 5085;
    std::cout << 5085;
    for (int i = 0; i < 5085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5086(int& val) {
    const int x = 5086;
    std::cout << 5086;
    for (int i = 0; i < 5086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5087(int& val) {
    const int x = 5087;
    std::cout << 5087;
    for (int i = 0; i < 5087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5088(int& val) {
    const int x = 5088;
    std::cout << 5088;
    for (int i = 0; i < 5088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5089(int& val) {
    const int x = 5089;
    std::cout << 5089;
    for (int i = 0; i < 5089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5090(int& val) {
    const int x = 5090;
    std::cout << 5090;
    for (int i = 0; i < 5090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5091(int& val) {
    const int x = 5091;
    std::cout << 5091;
    for (int i = 0; i < 5091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5092(int& val) {
    const int x = 5092;
    std::cout << 5092;
    for (int i = 0; i < 5092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5093(int& val) {
    const int x = 5093;
    std::cout << 5093;
    for (int i = 0; i < 5093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5094(int& val) {
    const int x = 5094;
    std::cout << 5094;
    for (int i = 0; i < 5094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5095(int& val) {
    const int x = 5095;
    std::cout << 5095;
    for (int i = 0; i < 5095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5096(int& val) {
    const int x = 5096;
    std::cout << 5096;
    for (int i = 0; i < 5096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5097(int& val) {
    const int x = 5097;
    std::cout << 5097;
    for (int i = 0; i < 5097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5098(int& val) {
    const int x = 5098;
    std::cout << 5098;
    for (int i = 0; i < 5098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5099(int& val) {
    const int x = 5099;
    std::cout << 5099;
    for (int i = 0; i < 5099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5100(int& val) {
    const int x = 5100;
    std::cout << 5100;
    for (int i = 0; i < 5100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5101(int& val) {
    const int x = 5101;
    std::cout << 5101;
    for (int i = 0; i < 5101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5102(int& val) {
    const int x = 5102;
    std::cout << 5102;
    for (int i = 0; i < 5102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5103(int& val) {
    const int x = 5103;
    std::cout << 5103;
    for (int i = 0; i < 5103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5104(int& val) {
    const int x = 5104;
    std::cout << 5104;
    for (int i = 0; i < 5104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5105(int& val) {
    const int x = 5105;
    std::cout << 5105;
    for (int i = 0; i < 5105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5106(int& val) {
    const int x = 5106;
    std::cout << 5106;
    for (int i = 0; i < 5106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5107(int& val) {
    const int x = 5107;
    std::cout << 5107;
    for (int i = 0; i < 5107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5108(int& val) {
    const int x = 5108;
    std::cout << 5108;
    for (int i = 0; i < 5108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5109(int& val) {
    const int x = 5109;
    std::cout << 5109;
    for (int i = 0; i < 5109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5110(int& val) {
    const int x = 5110;
    std::cout << 5110;
    for (int i = 0; i < 5110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5111(int& val) {
    const int x = 5111;
    std::cout << 5111;
    for (int i = 0; i < 5111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5112(int& val) {
    const int x = 5112;
    std::cout << 5112;
    for (int i = 0; i < 5112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5113(int& val) {
    const int x = 5113;
    std::cout << 5113;
    for (int i = 0; i < 5113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5114(int& val) {
    const int x = 5114;
    std::cout << 5114;
    for (int i = 0; i < 5114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5115(int& val) {
    const int x = 5115;
    std::cout << 5115;
    for (int i = 0; i < 5115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5116(int& val) {
    const int x = 5116;
    std::cout << 5116;
    for (int i = 0; i < 5116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5117(int& val) {
    const int x = 5117;
    std::cout << 5117;
    for (int i = 0; i < 5117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5118(int& val) {
    const int x = 5118;
    std::cout << 5118;
    for (int i = 0; i < 5118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5119(int& val) {
    const int x = 5119;
    std::cout << 5119;
    for (int i = 0; i < 5119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5120(int& val) {
    const int x = 5120;
    std::cout << 5120;
    for (int i = 0; i < 5120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5121(int& val) {
    const int x = 5121;
    std::cout << 5121;
    for (int i = 0; i < 5121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5122(int& val) {
    const int x = 5122;
    std::cout << 5122;
    for (int i = 0; i < 5122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5123(int& val) {
    const int x = 5123;
    std::cout << 5123;
    for (int i = 0; i < 5123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5124(int& val) {
    const int x = 5124;
    std::cout << 5124;
    for (int i = 0; i < 5124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5125(int& val) {
    const int x = 5125;
    std::cout << 5125;
    for (int i = 0; i < 5125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5126(int& val) {
    const int x = 5126;
    std::cout << 5126;
    for (int i = 0; i < 5126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5127(int& val) {
    const int x = 5127;
    std::cout << 5127;
    for (int i = 0; i < 5127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5128(int& val) {
    const int x = 5128;
    std::cout << 5128;
    for (int i = 0; i < 5128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5129(int& val) {
    const int x = 5129;
    std::cout << 5129;
    for (int i = 0; i < 5129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5130(int& val) {
    const int x = 5130;
    std::cout << 5130;
    for (int i = 0; i < 5130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5131(int& val) {
    const int x = 5131;
    std::cout << 5131;
    for (int i = 0; i < 5131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5132(int& val) {
    const int x = 5132;
    std::cout << 5132;
    for (int i = 0; i < 5132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5133(int& val) {
    const int x = 5133;
    std::cout << 5133;
    for (int i = 0; i < 5133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5134(int& val) {
    const int x = 5134;
    std::cout << 5134;
    for (int i = 0; i < 5134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5135(int& val) {
    const int x = 5135;
    std::cout << 5135;
    for (int i = 0; i < 5135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5136(int& val) {
    const int x = 5136;
    std::cout << 5136;
    for (int i = 0; i < 5136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5137(int& val) {
    const int x = 5137;
    std::cout << 5137;
    for (int i = 0; i < 5137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5138(int& val) {
    const int x = 5138;
    std::cout << 5138;
    for (int i = 0; i < 5138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5139(int& val) {
    const int x = 5139;
    std::cout << 5139;
    for (int i = 0; i < 5139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5140(int& val) {
    const int x = 5140;
    std::cout << 5140;
    for (int i = 0; i < 5140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5141(int& val) {
    const int x = 5141;
    std::cout << 5141;
    for (int i = 0; i < 5141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5142(int& val) {
    const int x = 5142;
    std::cout << 5142;
    for (int i = 0; i < 5142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5143(int& val) {
    const int x = 5143;
    std::cout << 5143;
    for (int i = 0; i < 5143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5144(int& val) {
    const int x = 5144;
    std::cout << 5144;
    for (int i = 0; i < 5144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5145(int& val) {
    const int x = 5145;
    std::cout << 5145;
    for (int i = 0; i < 5145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5146(int& val) {
    const int x = 5146;
    std::cout << 5146;
    for (int i = 0; i < 5146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5147(int& val) {
    const int x = 5147;
    std::cout << 5147;
    for (int i = 0; i < 5147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5148(int& val) {
    const int x = 5148;
    std::cout << 5148;
    for (int i = 0; i < 5148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5149(int& val) {
    const int x = 5149;
    std::cout << 5149;
    for (int i = 0; i < 5149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5150(int& val) {
    const int x = 5150;
    std::cout << 5150;
    for (int i = 0; i < 5150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5151(int& val) {
    const int x = 5151;
    std::cout << 5151;
    for (int i = 0; i < 5151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5152(int& val) {
    const int x = 5152;
    std::cout << 5152;
    for (int i = 0; i < 5152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5153(int& val) {
    const int x = 5153;
    std::cout << 5153;
    for (int i = 0; i < 5153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5154(int& val) {
    const int x = 5154;
    std::cout << 5154;
    for (int i = 0; i < 5154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5155(int& val) {
    const int x = 5155;
    std::cout << 5155;
    for (int i = 0; i < 5155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5156(int& val) {
    const int x = 5156;
    std::cout << 5156;
    for (int i = 0; i < 5156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5157(int& val) {
    const int x = 5157;
    std::cout << 5157;
    for (int i = 0; i < 5157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5158(int& val) {
    const int x = 5158;
    std::cout << 5158;
    for (int i = 0; i < 5158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5159(int& val) {
    const int x = 5159;
    std::cout << 5159;
    for (int i = 0; i < 5159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5160(int& val) {
    const int x = 5160;
    std::cout << 5160;
    for (int i = 0; i < 5160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5161(int& val) {
    const int x = 5161;
    std::cout << 5161;
    for (int i = 0; i < 5161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5162(int& val) {
    const int x = 5162;
    std::cout << 5162;
    for (int i = 0; i < 5162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5163(int& val) {
    const int x = 5163;
    std::cout << 5163;
    for (int i = 0; i < 5163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5164(int& val) {
    const int x = 5164;
    std::cout << 5164;
    for (int i = 0; i < 5164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5165(int& val) {
    const int x = 5165;
    std::cout << 5165;
    for (int i = 0; i < 5165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5166(int& val) {
    const int x = 5166;
    std::cout << 5166;
    for (int i = 0; i < 5166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5167(int& val) {
    const int x = 5167;
    std::cout << 5167;
    for (int i = 0; i < 5167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5168(int& val) {
    const int x = 5168;
    std::cout << 5168;
    for (int i = 0; i < 5168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5169(int& val) {
    const int x = 5169;
    std::cout << 5169;
    for (int i = 0; i < 5169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5170(int& val) {
    const int x = 5170;
    std::cout << 5170;
    for (int i = 0; i < 5170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5171(int& val) {
    const int x = 5171;
    std::cout << 5171;
    for (int i = 0; i < 5171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5172(int& val) {
    const int x = 5172;
    std::cout << 5172;
    for (int i = 0; i < 5172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5173(int& val) {
    const int x = 5173;
    std::cout << 5173;
    for (int i = 0; i < 5173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5174(int& val) {
    const int x = 5174;
    std::cout << 5174;
    for (int i = 0; i < 5174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5175(int& val) {
    const int x = 5175;
    std::cout << 5175;
    for (int i = 0; i < 5175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5176(int& val) {
    const int x = 5176;
    std::cout << 5176;
    for (int i = 0; i < 5176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5177(int& val) {
    const int x = 5177;
    std::cout << 5177;
    for (int i = 0; i < 5177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5178(int& val) {
    const int x = 5178;
    std::cout << 5178;
    for (int i = 0; i < 5178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5179(int& val) {
    const int x = 5179;
    std::cout << 5179;
    for (int i = 0; i < 5179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5180(int& val) {
    const int x = 5180;
    std::cout << 5180;
    for (int i = 0; i < 5180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5181(int& val) {
    const int x = 5181;
    std::cout << 5181;
    for (int i = 0; i < 5181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5182(int& val) {
    const int x = 5182;
    std::cout << 5182;
    for (int i = 0; i < 5182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5183(int& val) {
    const int x = 5183;
    std::cout << 5183;
    for (int i = 0; i < 5183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5184(int& val) {
    const int x = 5184;
    std::cout << 5184;
    for (int i = 0; i < 5184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5185(int& val) {
    const int x = 5185;
    std::cout << 5185;
    for (int i = 0; i < 5185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5186(int& val) {
    const int x = 5186;
    std::cout << 5186;
    for (int i = 0; i < 5186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5187(int& val) {
    const int x = 5187;
    std::cout << 5187;
    for (int i = 0; i < 5187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5188(int& val) {
    const int x = 5188;
    std::cout << 5188;
    for (int i = 0; i < 5188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5189(int& val) {
    const int x = 5189;
    std::cout << 5189;
    for (int i = 0; i < 5189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5190(int& val) {
    const int x = 5190;
    std::cout << 5190;
    for (int i = 0; i < 5190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5191(int& val) {
    const int x = 5191;
    std::cout << 5191;
    for (int i = 0; i < 5191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5192(int& val) {
    const int x = 5192;
    std::cout << 5192;
    for (int i = 0; i < 5192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5193(int& val) {
    const int x = 5193;
    std::cout << 5193;
    for (int i = 0; i < 5193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5194(int& val) {
    const int x = 5194;
    std::cout << 5194;
    for (int i = 0; i < 5194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5195(int& val) {
    const int x = 5195;
    std::cout << 5195;
    for (int i = 0; i < 5195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5196(int& val) {
    const int x = 5196;
    std::cout << 5196;
    for (int i = 0; i < 5196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5197(int& val) {
    const int x = 5197;
    std::cout << 5197;
    for (int i = 0; i < 5197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5198(int& val) {
    const int x = 5198;
    std::cout << 5198;
    for (int i = 0; i < 5198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5199(int& val) {
    const int x = 5199;
    std::cout << 5199;
    for (int i = 0; i < 5199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5200(int& val) {
    const int x = 5200;
    std::cout << 5200;
    for (int i = 0; i < 5200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5201(int& val) {
    const int x = 5201;
    std::cout << 5201;
    for (int i = 0; i < 5201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5202(int& val) {
    const int x = 5202;
    std::cout << 5202;
    for (int i = 0; i < 5202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5203(int& val) {
    const int x = 5203;
    std::cout << 5203;
    for (int i = 0; i < 5203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5204(int& val) {
    const int x = 5204;
    std::cout << 5204;
    for (int i = 0; i < 5204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5205(int& val) {
    const int x = 5205;
    std::cout << 5205;
    for (int i = 0; i < 5205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5206(int& val) {
    const int x = 5206;
    std::cout << 5206;
    for (int i = 0; i < 5206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5207(int& val) {
    const int x = 5207;
    std::cout << 5207;
    for (int i = 0; i < 5207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5208(int& val) {
    const int x = 5208;
    std::cout << 5208;
    for (int i = 0; i < 5208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5209(int& val) {
    const int x = 5209;
    std::cout << 5209;
    for (int i = 0; i < 5209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5210(int& val) {
    const int x = 5210;
    std::cout << 5210;
    for (int i = 0; i < 5210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5211(int& val) {
    const int x = 5211;
    std::cout << 5211;
    for (int i = 0; i < 5211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5212(int& val) {
    const int x = 5212;
    std::cout << 5212;
    for (int i = 0; i < 5212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5213(int& val) {
    const int x = 5213;
    std::cout << 5213;
    for (int i = 0; i < 5213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5214(int& val) {
    const int x = 5214;
    std::cout << 5214;
    for (int i = 0; i < 5214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5215(int& val) {
    const int x = 5215;
    std::cout << 5215;
    for (int i = 0; i < 5215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5216(int& val) {
    const int x = 5216;
    std::cout << 5216;
    for (int i = 0; i < 5216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5217(int& val) {
    const int x = 5217;
    std::cout << 5217;
    for (int i = 0; i < 5217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5218(int& val) {
    const int x = 5218;
    std::cout << 5218;
    for (int i = 0; i < 5218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5219(int& val) {
    const int x = 5219;
    std::cout << 5219;
    for (int i = 0; i < 5219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5220(int& val) {
    const int x = 5220;
    std::cout << 5220;
    for (int i = 0; i < 5220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5221(int& val) {
    const int x = 5221;
    std::cout << 5221;
    for (int i = 0; i < 5221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5222(int& val) {
    const int x = 5222;
    std::cout << 5222;
    for (int i = 0; i < 5222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5223(int& val) {
    const int x = 5223;
    std::cout << 5223;
    for (int i = 0; i < 5223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5224(int& val) {
    const int x = 5224;
    std::cout << 5224;
    for (int i = 0; i < 5224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5225(int& val) {
    const int x = 5225;
    std::cout << 5225;
    for (int i = 0; i < 5225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5226(int& val) {
    const int x = 5226;
    std::cout << 5226;
    for (int i = 0; i < 5226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5227(int& val) {
    const int x = 5227;
    std::cout << 5227;
    for (int i = 0; i < 5227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5228(int& val) {
    const int x = 5228;
    std::cout << 5228;
    for (int i = 0; i < 5228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5229(int& val) {
    const int x = 5229;
    std::cout << 5229;
    for (int i = 0; i < 5229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5230(int& val) {
    const int x = 5230;
    std::cout << 5230;
    for (int i = 0; i < 5230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5231(int& val) {
    const int x = 5231;
    std::cout << 5231;
    for (int i = 0; i < 5231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5232(int& val) {
    const int x = 5232;
    std::cout << 5232;
    for (int i = 0; i < 5232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5233(int& val) {
    const int x = 5233;
    std::cout << 5233;
    for (int i = 0; i < 5233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5234(int& val) {
    const int x = 5234;
    std::cout << 5234;
    for (int i = 0; i < 5234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5235(int& val) {
    const int x = 5235;
    std::cout << 5235;
    for (int i = 0; i < 5235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5236(int& val) {
    const int x = 5236;
    std::cout << 5236;
    for (int i = 0; i < 5236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5237(int& val) {
    const int x = 5237;
    std::cout << 5237;
    for (int i = 0; i < 5237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5238(int& val) {
    const int x = 5238;
    std::cout << 5238;
    for (int i = 0; i < 5238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5239(int& val) {
    const int x = 5239;
    std::cout << 5239;
    for (int i = 0; i < 5239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5240(int& val) {
    const int x = 5240;
    std::cout << 5240;
    for (int i = 0; i < 5240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5241(int& val) {
    const int x = 5241;
    std::cout << 5241;
    for (int i = 0; i < 5241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5242(int& val) {
    const int x = 5242;
    std::cout << 5242;
    for (int i = 0; i < 5242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5243(int& val) {
    const int x = 5243;
    std::cout << 5243;
    for (int i = 0; i < 5243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5244(int& val) {
    const int x = 5244;
    std::cout << 5244;
    for (int i = 0; i < 5244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5245(int& val) {
    const int x = 5245;
    std::cout << 5245;
    for (int i = 0; i < 5245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5246(int& val) {
    const int x = 5246;
    std::cout << 5246;
    for (int i = 0; i < 5246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5247(int& val) {
    const int x = 5247;
    std::cout << 5247;
    for (int i = 0; i < 5247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5248(int& val) {
    const int x = 5248;
    std::cout << 5248;
    for (int i = 0; i < 5248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5249(int& val) {
    const int x = 5249;
    std::cout << 5249;
    for (int i = 0; i < 5249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5250(int& val) {
    const int x = 5250;
    std::cout << 5250;
    for (int i = 0; i < 5250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5251(int& val) {
    const int x = 5251;
    std::cout << 5251;
    for (int i = 0; i < 5251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5252(int& val) {
    const int x = 5252;
    std::cout << 5252;
    for (int i = 0; i < 5252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5253(int& val) {
    const int x = 5253;
    std::cout << 5253;
    for (int i = 0; i < 5253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5254(int& val) {
    const int x = 5254;
    std::cout << 5254;
    for (int i = 0; i < 5254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5255(int& val) {
    const int x = 5255;
    std::cout << 5255;
    for (int i = 0; i < 5255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5256(int& val) {
    const int x = 5256;
    std::cout << 5256;
    for (int i = 0; i < 5256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5257(int& val) {
    const int x = 5257;
    std::cout << 5257;
    for (int i = 0; i < 5257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5258(int& val) {
    const int x = 5258;
    std::cout << 5258;
    for (int i = 0; i < 5258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5259(int& val) {
    const int x = 5259;
    std::cout << 5259;
    for (int i = 0; i < 5259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5260(int& val) {
    const int x = 5260;
    std::cout << 5260;
    for (int i = 0; i < 5260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5261(int& val) {
    const int x = 5261;
    std::cout << 5261;
    for (int i = 0; i < 5261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5262(int& val) {
    const int x = 5262;
    std::cout << 5262;
    for (int i = 0; i < 5262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5263(int& val) {
    const int x = 5263;
    std::cout << 5263;
    for (int i = 0; i < 5263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5264(int& val) {
    const int x = 5264;
    std::cout << 5264;
    for (int i = 0; i < 5264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5265(int& val) {
    const int x = 5265;
    std::cout << 5265;
    for (int i = 0; i < 5265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5266(int& val) {
    const int x = 5266;
    std::cout << 5266;
    for (int i = 0; i < 5266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5267(int& val) {
    const int x = 5267;
    std::cout << 5267;
    for (int i = 0; i < 5267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5268(int& val) {
    const int x = 5268;
    std::cout << 5268;
    for (int i = 0; i < 5268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5269(int& val) {
    const int x = 5269;
    std::cout << 5269;
    for (int i = 0; i < 5269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5270(int& val) {
    const int x = 5270;
    std::cout << 5270;
    for (int i = 0; i < 5270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5271(int& val) {
    const int x = 5271;
    std::cout << 5271;
    for (int i = 0; i < 5271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5272(int& val) {
    const int x = 5272;
    std::cout << 5272;
    for (int i = 0; i < 5272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5273(int& val) {
    const int x = 5273;
    std::cout << 5273;
    for (int i = 0; i < 5273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5274(int& val) {
    const int x = 5274;
    std::cout << 5274;
    for (int i = 0; i < 5274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5275(int& val) {
    const int x = 5275;
    std::cout << 5275;
    for (int i = 0; i < 5275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5276(int& val) {
    const int x = 5276;
    std::cout << 5276;
    for (int i = 0; i < 5276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5277(int& val) {
    const int x = 5277;
    std::cout << 5277;
    for (int i = 0; i < 5277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5278(int& val) {
    const int x = 5278;
    std::cout << 5278;
    for (int i = 0; i < 5278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5279(int& val) {
    const int x = 5279;
    std::cout << 5279;
    for (int i = 0; i < 5279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5280(int& val) {
    const int x = 5280;
    std::cout << 5280;
    for (int i = 0; i < 5280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5281(int& val) {
    const int x = 5281;
    std::cout << 5281;
    for (int i = 0; i < 5281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5282(int& val) {
    const int x = 5282;
    std::cout << 5282;
    for (int i = 0; i < 5282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5283(int& val) {
    const int x = 5283;
    std::cout << 5283;
    for (int i = 0; i < 5283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5284(int& val) {
    const int x = 5284;
    std::cout << 5284;
    for (int i = 0; i < 5284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5285(int& val) {
    const int x = 5285;
    std::cout << 5285;
    for (int i = 0; i < 5285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5286(int& val) {
    const int x = 5286;
    std::cout << 5286;
    for (int i = 0; i < 5286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5287(int& val) {
    const int x = 5287;
    std::cout << 5287;
    for (int i = 0; i < 5287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5288(int& val) {
    const int x = 5288;
    std::cout << 5288;
    for (int i = 0; i < 5288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5289(int& val) {
    const int x = 5289;
    std::cout << 5289;
    for (int i = 0; i < 5289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5290(int& val) {
    const int x = 5290;
    std::cout << 5290;
    for (int i = 0; i < 5290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5291(int& val) {
    const int x = 5291;
    std::cout << 5291;
    for (int i = 0; i < 5291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5292(int& val) {
    const int x = 5292;
    std::cout << 5292;
    for (int i = 0; i < 5292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5293(int& val) {
    const int x = 5293;
    std::cout << 5293;
    for (int i = 0; i < 5293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5294(int& val) {
    const int x = 5294;
    std::cout << 5294;
    for (int i = 0; i < 5294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5295(int& val) {
    const int x = 5295;
    std::cout << 5295;
    for (int i = 0; i < 5295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5296(int& val) {
    const int x = 5296;
    std::cout << 5296;
    for (int i = 0; i < 5296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5297(int& val) {
    const int x = 5297;
    std::cout << 5297;
    for (int i = 0; i < 5297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5298(int& val) {
    const int x = 5298;
    std::cout << 5298;
    for (int i = 0; i < 5298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5299(int& val) {
    const int x = 5299;
    std::cout << 5299;
    for (int i = 0; i < 5299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5300(int& val) {
    const int x = 5300;
    std::cout << 5300;
    for (int i = 0; i < 5300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5301(int& val) {
    const int x = 5301;
    std::cout << 5301;
    for (int i = 0; i < 5301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5302(int& val) {
    const int x = 5302;
    std::cout << 5302;
    for (int i = 0; i < 5302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5303(int& val) {
    const int x = 5303;
    std::cout << 5303;
    for (int i = 0; i < 5303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5304(int& val) {
    const int x = 5304;
    std::cout << 5304;
    for (int i = 0; i < 5304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5305(int& val) {
    const int x = 5305;
    std::cout << 5305;
    for (int i = 0; i < 5305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5306(int& val) {
    const int x = 5306;
    std::cout << 5306;
    for (int i = 0; i < 5306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5307(int& val) {
    const int x = 5307;
    std::cout << 5307;
    for (int i = 0; i < 5307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5308(int& val) {
    const int x = 5308;
    std::cout << 5308;
    for (int i = 0; i < 5308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5309(int& val) {
    const int x = 5309;
    std::cout << 5309;
    for (int i = 0; i < 5309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5310(int& val) {
    const int x = 5310;
    std::cout << 5310;
    for (int i = 0; i < 5310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5311(int& val) {
    const int x = 5311;
    std::cout << 5311;
    for (int i = 0; i < 5311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5312(int& val) {
    const int x = 5312;
    std::cout << 5312;
    for (int i = 0; i < 5312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5313(int& val) {
    const int x = 5313;
    std::cout << 5313;
    for (int i = 0; i < 5313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5314(int& val) {
    const int x = 5314;
    std::cout << 5314;
    for (int i = 0; i < 5314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5315(int& val) {
    const int x = 5315;
    std::cout << 5315;
    for (int i = 0; i < 5315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5316(int& val) {
    const int x = 5316;
    std::cout << 5316;
    for (int i = 0; i < 5316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5317(int& val) {
    const int x = 5317;
    std::cout << 5317;
    for (int i = 0; i < 5317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5318(int& val) {
    const int x = 5318;
    std::cout << 5318;
    for (int i = 0; i < 5318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5319(int& val) {
    const int x = 5319;
    std::cout << 5319;
    for (int i = 0; i < 5319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5320(int& val) {
    const int x = 5320;
    std::cout << 5320;
    for (int i = 0; i < 5320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5321(int& val) {
    const int x = 5321;
    std::cout << 5321;
    for (int i = 0; i < 5321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5322(int& val) {
    const int x = 5322;
    std::cout << 5322;
    for (int i = 0; i < 5322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5323(int& val) {
    const int x = 5323;
    std::cout << 5323;
    for (int i = 0; i < 5323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5324(int& val) {
    const int x = 5324;
    std::cout << 5324;
    for (int i = 0; i < 5324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5325(int& val) {
    const int x = 5325;
    std::cout << 5325;
    for (int i = 0; i < 5325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5326(int& val) {
    const int x = 5326;
    std::cout << 5326;
    for (int i = 0; i < 5326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5327(int& val) {
    const int x = 5327;
    std::cout << 5327;
    for (int i = 0; i < 5327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5328(int& val) {
    const int x = 5328;
    std::cout << 5328;
    for (int i = 0; i < 5328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5329(int& val) {
    const int x = 5329;
    std::cout << 5329;
    for (int i = 0; i < 5329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5330(int& val) {
    const int x = 5330;
    std::cout << 5330;
    for (int i = 0; i < 5330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5331(int& val) {
    const int x = 5331;
    std::cout << 5331;
    for (int i = 0; i < 5331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5332(int& val) {
    const int x = 5332;
    std::cout << 5332;
    for (int i = 0; i < 5332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5333(int& val) {
    const int x = 5333;
    std::cout << 5333;
    for (int i = 0; i < 5333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5334(int& val) {
    const int x = 5334;
    std::cout << 5334;
    for (int i = 0; i < 5334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5335(int& val) {
    const int x = 5335;
    std::cout << 5335;
    for (int i = 0; i < 5335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5336(int& val) {
    const int x = 5336;
    std::cout << 5336;
    for (int i = 0; i < 5336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5337(int& val) {
    const int x = 5337;
    std::cout << 5337;
    for (int i = 0; i < 5337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5338(int& val) {
    const int x = 5338;
    std::cout << 5338;
    for (int i = 0; i < 5338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5339(int& val) {
    const int x = 5339;
    std::cout << 5339;
    for (int i = 0; i < 5339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5340(int& val) {
    const int x = 5340;
    std::cout << 5340;
    for (int i = 0; i < 5340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5341(int& val) {
    const int x = 5341;
    std::cout << 5341;
    for (int i = 0; i < 5341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5342(int& val) {
    const int x = 5342;
    std::cout << 5342;
    for (int i = 0; i < 5342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5343(int& val) {
    const int x = 5343;
    std::cout << 5343;
    for (int i = 0; i < 5343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5344(int& val) {
    const int x = 5344;
    std::cout << 5344;
    for (int i = 0; i < 5344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5345(int& val) {
    const int x = 5345;
    std::cout << 5345;
    for (int i = 0; i < 5345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5346(int& val) {
    const int x = 5346;
    std::cout << 5346;
    for (int i = 0; i < 5346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5347(int& val) {
    const int x = 5347;
    std::cout << 5347;
    for (int i = 0; i < 5347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5348(int& val) {
    const int x = 5348;
    std::cout << 5348;
    for (int i = 0; i < 5348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5349(int& val) {
    const int x = 5349;
    std::cout << 5349;
    for (int i = 0; i < 5349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5350(int& val) {
    const int x = 5350;
    std::cout << 5350;
    for (int i = 0; i < 5350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5351(int& val) {
    const int x = 5351;
    std::cout << 5351;
    for (int i = 0; i < 5351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5352(int& val) {
    const int x = 5352;
    std::cout << 5352;
    for (int i = 0; i < 5352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5353(int& val) {
    const int x = 5353;
    std::cout << 5353;
    for (int i = 0; i < 5353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5354(int& val) {
    const int x = 5354;
    std::cout << 5354;
    for (int i = 0; i < 5354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5355(int& val) {
    const int x = 5355;
    std::cout << 5355;
    for (int i = 0; i < 5355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5356(int& val) {
    const int x = 5356;
    std::cout << 5356;
    for (int i = 0; i < 5356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5357(int& val) {
    const int x = 5357;
    std::cout << 5357;
    for (int i = 0; i < 5357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5358(int& val) {
    const int x = 5358;
    std::cout << 5358;
    for (int i = 0; i < 5358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5359(int& val) {
    const int x = 5359;
    std::cout << 5359;
    for (int i = 0; i < 5359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5360(int& val) {
    const int x = 5360;
    std::cout << 5360;
    for (int i = 0; i < 5360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5361(int& val) {
    const int x = 5361;
    std::cout << 5361;
    for (int i = 0; i < 5361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5362(int& val) {
    const int x = 5362;
    std::cout << 5362;
    for (int i = 0; i < 5362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5363(int& val) {
    const int x = 5363;
    std::cout << 5363;
    for (int i = 0; i < 5363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5364(int& val) {
    const int x = 5364;
    std::cout << 5364;
    for (int i = 0; i < 5364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5365(int& val) {
    const int x = 5365;
    std::cout << 5365;
    for (int i = 0; i < 5365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5366(int& val) {
    const int x = 5366;
    std::cout << 5366;
    for (int i = 0; i < 5366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5367(int& val) {
    const int x = 5367;
    std::cout << 5367;
    for (int i = 0; i < 5367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5368(int& val) {
    const int x = 5368;
    std::cout << 5368;
    for (int i = 0; i < 5368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5369(int& val) {
    const int x = 5369;
    std::cout << 5369;
    for (int i = 0; i < 5369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5370(int& val) {
    const int x = 5370;
    std::cout << 5370;
    for (int i = 0; i < 5370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5371(int& val) {
    const int x = 5371;
    std::cout << 5371;
    for (int i = 0; i < 5371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5372(int& val) {
    const int x = 5372;
    std::cout << 5372;
    for (int i = 0; i < 5372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5373(int& val) {
    const int x = 5373;
    std::cout << 5373;
    for (int i = 0; i < 5373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5374(int& val) {
    const int x = 5374;
    std::cout << 5374;
    for (int i = 0; i < 5374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5375(int& val) {
    const int x = 5375;
    std::cout << 5375;
    for (int i = 0; i < 5375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5376(int& val) {
    const int x = 5376;
    std::cout << 5376;
    for (int i = 0; i < 5376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5377(int& val) {
    const int x = 5377;
    std::cout << 5377;
    for (int i = 0; i < 5377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5378(int& val) {
    const int x = 5378;
    std::cout << 5378;
    for (int i = 0; i < 5378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5379(int& val) {
    const int x = 5379;
    std::cout << 5379;
    for (int i = 0; i < 5379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5380(int& val) {
    const int x = 5380;
    std::cout << 5380;
    for (int i = 0; i < 5380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5381(int& val) {
    const int x = 5381;
    std::cout << 5381;
    for (int i = 0; i < 5381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5382(int& val) {
    const int x = 5382;
    std::cout << 5382;
    for (int i = 0; i < 5382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5383(int& val) {
    const int x = 5383;
    std::cout << 5383;
    for (int i = 0; i < 5383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5384(int& val) {
    const int x = 5384;
    std::cout << 5384;
    for (int i = 0; i < 5384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5385(int& val) {
    const int x = 5385;
    std::cout << 5385;
    for (int i = 0; i < 5385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5386(int& val) {
    const int x = 5386;
    std::cout << 5386;
    for (int i = 0; i < 5386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5387(int& val) {
    const int x = 5387;
    std::cout << 5387;
    for (int i = 0; i < 5387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5388(int& val) {
    const int x = 5388;
    std::cout << 5388;
    for (int i = 0; i < 5388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5389(int& val) {
    const int x = 5389;
    std::cout << 5389;
    for (int i = 0; i < 5389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5390(int& val) {
    const int x = 5390;
    std::cout << 5390;
    for (int i = 0; i < 5390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5391(int& val) {
    const int x = 5391;
    std::cout << 5391;
    for (int i = 0; i < 5391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5392(int& val) {
    const int x = 5392;
    std::cout << 5392;
    for (int i = 0; i < 5392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5393(int& val) {
    const int x = 5393;
    std::cout << 5393;
    for (int i = 0; i < 5393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5394(int& val) {
    const int x = 5394;
    std::cout << 5394;
    for (int i = 0; i < 5394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5395(int& val) {
    const int x = 5395;
    std::cout << 5395;
    for (int i = 0; i < 5395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5396(int& val) {
    const int x = 5396;
    std::cout << 5396;
    for (int i = 0; i < 5396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5397(int& val) {
    const int x = 5397;
    std::cout << 5397;
    for (int i = 0; i < 5397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5398(int& val) {
    const int x = 5398;
    std::cout << 5398;
    for (int i = 0; i < 5398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5399(int& val) {
    const int x = 5399;
    std::cout << 5399;
    for (int i = 0; i < 5399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5400(int& val) {
    const int x = 5400;
    std::cout << 5400;
    for (int i = 0; i < 5400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5401(int& val) {
    const int x = 5401;
    std::cout << 5401;
    for (int i = 0; i < 5401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5402(int& val) {
    const int x = 5402;
    std::cout << 5402;
    for (int i = 0; i < 5402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5403(int& val) {
    const int x = 5403;
    std::cout << 5403;
    for (int i = 0; i < 5403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5404(int& val) {
    const int x = 5404;
    std::cout << 5404;
    for (int i = 0; i < 5404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5405(int& val) {
    const int x = 5405;
    std::cout << 5405;
    for (int i = 0; i < 5405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5406(int& val) {
    const int x = 5406;
    std::cout << 5406;
    for (int i = 0; i < 5406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5407(int& val) {
    const int x = 5407;
    std::cout << 5407;
    for (int i = 0; i < 5407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5408(int& val) {
    const int x = 5408;
    std::cout << 5408;
    for (int i = 0; i < 5408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5409(int& val) {
    const int x = 5409;
    std::cout << 5409;
    for (int i = 0; i < 5409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5410(int& val) {
    const int x = 5410;
    std::cout << 5410;
    for (int i = 0; i < 5410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5411(int& val) {
    const int x = 5411;
    std::cout << 5411;
    for (int i = 0; i < 5411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5412(int& val) {
    const int x = 5412;
    std::cout << 5412;
    for (int i = 0; i < 5412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5413(int& val) {
    const int x = 5413;
    std::cout << 5413;
    for (int i = 0; i < 5413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5414(int& val) {
    const int x = 5414;
    std::cout << 5414;
    for (int i = 0; i < 5414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5415(int& val) {
    const int x = 5415;
    std::cout << 5415;
    for (int i = 0; i < 5415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5416(int& val) {
    const int x = 5416;
    std::cout << 5416;
    for (int i = 0; i < 5416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5417(int& val) {
    const int x = 5417;
    std::cout << 5417;
    for (int i = 0; i < 5417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5418(int& val) {
    const int x = 5418;
    std::cout << 5418;
    for (int i = 0; i < 5418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5419(int& val) {
    const int x = 5419;
    std::cout << 5419;
    for (int i = 0; i < 5419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5420(int& val) {
    const int x = 5420;
    std::cout << 5420;
    for (int i = 0; i < 5420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5421(int& val) {
    const int x = 5421;
    std::cout << 5421;
    for (int i = 0; i < 5421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5422(int& val) {
    const int x = 5422;
    std::cout << 5422;
    for (int i = 0; i < 5422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5423(int& val) {
    const int x = 5423;
    std::cout << 5423;
    for (int i = 0; i < 5423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5424(int& val) {
    const int x = 5424;
    std::cout << 5424;
    for (int i = 0; i < 5424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5425(int& val) {
    const int x = 5425;
    std::cout << 5425;
    for (int i = 0; i < 5425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5426(int& val) {
    const int x = 5426;
    std::cout << 5426;
    for (int i = 0; i < 5426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5427(int& val) {
    const int x = 5427;
    std::cout << 5427;
    for (int i = 0; i < 5427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5428(int& val) {
    const int x = 5428;
    std::cout << 5428;
    for (int i = 0; i < 5428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5429(int& val) {
    const int x = 5429;
    std::cout << 5429;
    for (int i = 0; i < 5429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5430(int& val) {
    const int x = 5430;
    std::cout << 5430;
    for (int i = 0; i < 5430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5431(int& val) {
    const int x = 5431;
    std::cout << 5431;
    for (int i = 0; i < 5431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5432(int& val) {
    const int x = 5432;
    std::cout << 5432;
    for (int i = 0; i < 5432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5433(int& val) {
    const int x = 5433;
    std::cout << 5433;
    for (int i = 0; i < 5433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5434(int& val) {
    const int x = 5434;
    std::cout << 5434;
    for (int i = 0; i < 5434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5435(int& val) {
    const int x = 5435;
    std::cout << 5435;
    for (int i = 0; i < 5435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5436(int& val) {
    const int x = 5436;
    std::cout << 5436;
    for (int i = 0; i < 5436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5437(int& val) {
    const int x = 5437;
    std::cout << 5437;
    for (int i = 0; i < 5437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5438(int& val) {
    const int x = 5438;
    std::cout << 5438;
    for (int i = 0; i < 5438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5439(int& val) {
    const int x = 5439;
    std::cout << 5439;
    for (int i = 0; i < 5439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5440(int& val) {
    const int x = 5440;
    std::cout << 5440;
    for (int i = 0; i < 5440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5441(int& val) {
    const int x = 5441;
    std::cout << 5441;
    for (int i = 0; i < 5441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5442(int& val) {
    const int x = 5442;
    std::cout << 5442;
    for (int i = 0; i < 5442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5443(int& val) {
    const int x = 5443;
    std::cout << 5443;
    for (int i = 0; i < 5443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5444(int& val) {
    const int x = 5444;
    std::cout << 5444;
    for (int i = 0; i < 5444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5445(int& val) {
    const int x = 5445;
    std::cout << 5445;
    for (int i = 0; i < 5445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5446(int& val) {
    const int x = 5446;
    std::cout << 5446;
    for (int i = 0; i < 5446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5447(int& val) {
    const int x = 5447;
    std::cout << 5447;
    for (int i = 0; i < 5447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5448(int& val) {
    const int x = 5448;
    std::cout << 5448;
    for (int i = 0; i < 5448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5449(int& val) {
    const int x = 5449;
    std::cout << 5449;
    for (int i = 0; i < 5449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5450(int& val) {
    const int x = 5450;
    std::cout << 5450;
    for (int i = 0; i < 5450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5451(int& val) {
    const int x = 5451;
    std::cout << 5451;
    for (int i = 0; i < 5451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5452(int& val) {
    const int x = 5452;
    std::cout << 5452;
    for (int i = 0; i < 5452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5453(int& val) {
    const int x = 5453;
    std::cout << 5453;
    for (int i = 0; i < 5453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5454(int& val) {
    const int x = 5454;
    std::cout << 5454;
    for (int i = 0; i < 5454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5455(int& val) {
    const int x = 5455;
    std::cout << 5455;
    for (int i = 0; i < 5455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5456(int& val) {
    const int x = 5456;
    std::cout << 5456;
    for (int i = 0; i < 5456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5457(int& val) {
    const int x = 5457;
    std::cout << 5457;
    for (int i = 0; i < 5457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5458(int& val) {
    const int x = 5458;
    std::cout << 5458;
    for (int i = 0; i < 5458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5459(int& val) {
    const int x = 5459;
    std::cout << 5459;
    for (int i = 0; i < 5459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5460(int& val) {
    const int x = 5460;
    std::cout << 5460;
    for (int i = 0; i < 5460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5461(int& val) {
    const int x = 5461;
    std::cout << 5461;
    for (int i = 0; i < 5461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5462(int& val) {
    const int x = 5462;
    std::cout << 5462;
    for (int i = 0; i < 5462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5463(int& val) {
    const int x = 5463;
    std::cout << 5463;
    for (int i = 0; i < 5463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5464(int& val) {
    const int x = 5464;
    std::cout << 5464;
    for (int i = 0; i < 5464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5465(int& val) {
    const int x = 5465;
    std::cout << 5465;
    for (int i = 0; i < 5465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5466(int& val) {
    const int x = 5466;
    std::cout << 5466;
    for (int i = 0; i < 5466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5467(int& val) {
    const int x = 5467;
    std::cout << 5467;
    for (int i = 0; i < 5467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5468(int& val) {
    const int x = 5468;
    std::cout << 5468;
    for (int i = 0; i < 5468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5469(int& val) {
    const int x = 5469;
    std::cout << 5469;
    for (int i = 0; i < 5469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5470(int& val) {
    const int x = 5470;
    std::cout << 5470;
    for (int i = 0; i < 5470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5471(int& val) {
    const int x = 5471;
    std::cout << 5471;
    for (int i = 0; i < 5471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5472(int& val) {
    const int x = 5472;
    std::cout << 5472;
    for (int i = 0; i < 5472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5473(int& val) {
    const int x = 5473;
    std::cout << 5473;
    for (int i = 0; i < 5473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5474(int& val) {
    const int x = 5474;
    std::cout << 5474;
    for (int i = 0; i < 5474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5475(int& val) {
    const int x = 5475;
    std::cout << 5475;
    for (int i = 0; i < 5475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5476(int& val) {
    const int x = 5476;
    std::cout << 5476;
    for (int i = 0; i < 5476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5477(int& val) {
    const int x = 5477;
    std::cout << 5477;
    for (int i = 0; i < 5477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5478(int& val) {
    const int x = 5478;
    std::cout << 5478;
    for (int i = 0; i < 5478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5479(int& val) {
    const int x = 5479;
    std::cout << 5479;
    for (int i = 0; i < 5479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5480(int& val) {
    const int x = 5480;
    std::cout << 5480;
    for (int i = 0; i < 5480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5481(int& val) {
    const int x = 5481;
    std::cout << 5481;
    for (int i = 0; i < 5481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5482(int& val) {
    const int x = 5482;
    std::cout << 5482;
    for (int i = 0; i < 5482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5483(int& val) {
    const int x = 5483;
    std::cout << 5483;
    for (int i = 0; i < 5483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5484(int& val) {
    const int x = 5484;
    std::cout << 5484;
    for (int i = 0; i < 5484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5485(int& val) {
    const int x = 5485;
    std::cout << 5485;
    for (int i = 0; i < 5485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5486(int& val) {
    const int x = 5486;
    std::cout << 5486;
    for (int i = 0; i < 5486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5487(int& val) {
    const int x = 5487;
    std::cout << 5487;
    for (int i = 0; i < 5487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5488(int& val) {
    const int x = 5488;
    std::cout << 5488;
    for (int i = 0; i < 5488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5489(int& val) {
    const int x = 5489;
    std::cout << 5489;
    for (int i = 0; i < 5489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5490(int& val) {
    const int x = 5490;
    std::cout << 5490;
    for (int i = 0; i < 5490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5491(int& val) {
    const int x = 5491;
    std::cout << 5491;
    for (int i = 0; i < 5491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5492(int& val) {
    const int x = 5492;
    std::cout << 5492;
    for (int i = 0; i < 5492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5493(int& val) {
    const int x = 5493;
    std::cout << 5493;
    for (int i = 0; i < 5493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5494(int& val) {
    const int x = 5494;
    std::cout << 5494;
    for (int i = 0; i < 5494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5495(int& val) {
    const int x = 5495;
    std::cout << 5495;
    for (int i = 0; i < 5495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5496(int& val) {
    const int x = 5496;
    std::cout << 5496;
    for (int i = 0; i < 5496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5497(int& val) {
    const int x = 5497;
    std::cout << 5497;
    for (int i = 0; i < 5497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5498(int& val) {
    const int x = 5498;
    std::cout << 5498;
    for (int i = 0; i < 5498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5499(int& val) {
    const int x = 5499;
    std::cout << 5499;
    for (int i = 0; i < 5499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5500(int& val) {
    const int x = 5500;
    std::cout << 5500;
    for (int i = 0; i < 5500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5501(int& val) {
    const int x = 5501;
    std::cout << 5501;
    for (int i = 0; i < 5501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5502(int& val) {
    const int x = 5502;
    std::cout << 5502;
    for (int i = 0; i < 5502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5503(int& val) {
    const int x = 5503;
    std::cout << 5503;
    for (int i = 0; i < 5503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5504(int& val) {
    const int x = 5504;
    std::cout << 5504;
    for (int i = 0; i < 5504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5505(int& val) {
    const int x = 5505;
    std::cout << 5505;
    for (int i = 0; i < 5505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5506(int& val) {
    const int x = 5506;
    std::cout << 5506;
    for (int i = 0; i < 5506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5507(int& val) {
    const int x = 5507;
    std::cout << 5507;
    for (int i = 0; i < 5507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5508(int& val) {
    const int x = 5508;
    std::cout << 5508;
    for (int i = 0; i < 5508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5509(int& val) {
    const int x = 5509;
    std::cout << 5509;
    for (int i = 0; i < 5509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5510(int& val) {
    const int x = 5510;
    std::cout << 5510;
    for (int i = 0; i < 5510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5511(int& val) {
    const int x = 5511;
    std::cout << 5511;
    for (int i = 0; i < 5511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5512(int& val) {
    const int x = 5512;
    std::cout << 5512;
    for (int i = 0; i < 5512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5513(int& val) {
    const int x = 5513;
    std::cout << 5513;
    for (int i = 0; i < 5513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5514(int& val) {
    const int x = 5514;
    std::cout << 5514;
    for (int i = 0; i < 5514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5515(int& val) {
    const int x = 5515;
    std::cout << 5515;
    for (int i = 0; i < 5515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5516(int& val) {
    const int x = 5516;
    std::cout << 5516;
    for (int i = 0; i < 5516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5517(int& val) {
    const int x = 5517;
    std::cout << 5517;
    for (int i = 0; i < 5517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5518(int& val) {
    const int x = 5518;
    std::cout << 5518;
    for (int i = 0; i < 5518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5519(int& val) {
    const int x = 5519;
    std::cout << 5519;
    for (int i = 0; i < 5519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5520(int& val) {
    const int x = 5520;
    std::cout << 5520;
    for (int i = 0; i < 5520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5521(int& val) {
    const int x = 5521;
    std::cout << 5521;
    for (int i = 0; i < 5521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5522(int& val) {
    const int x = 5522;
    std::cout << 5522;
    for (int i = 0; i < 5522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5523(int& val) {
    const int x = 5523;
    std::cout << 5523;
    for (int i = 0; i < 5523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5524(int& val) {
    const int x = 5524;
    std::cout << 5524;
    for (int i = 0; i < 5524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5525(int& val) {
    const int x = 5525;
    std::cout << 5525;
    for (int i = 0; i < 5525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5526(int& val) {
    const int x = 5526;
    std::cout << 5526;
    for (int i = 0; i < 5526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5527(int& val) {
    const int x = 5527;
    std::cout << 5527;
    for (int i = 0; i < 5527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5528(int& val) {
    const int x = 5528;
    std::cout << 5528;
    for (int i = 0; i < 5528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5529(int& val) {
    const int x = 5529;
    std::cout << 5529;
    for (int i = 0; i < 5529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5530(int& val) {
    const int x = 5530;
    std::cout << 5530;
    for (int i = 0; i < 5530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5531(int& val) {
    const int x = 5531;
    std::cout << 5531;
    for (int i = 0; i < 5531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5532(int& val) {
    const int x = 5532;
    std::cout << 5532;
    for (int i = 0; i < 5532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5533(int& val) {
    const int x = 5533;
    std::cout << 5533;
    for (int i = 0; i < 5533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5534(int& val) {
    const int x = 5534;
    std::cout << 5534;
    for (int i = 0; i < 5534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5535(int& val) {
    const int x = 5535;
    std::cout << 5535;
    for (int i = 0; i < 5535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5536(int& val) {
    const int x = 5536;
    std::cout << 5536;
    for (int i = 0; i < 5536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5537(int& val) {
    const int x = 5537;
    std::cout << 5537;
    for (int i = 0; i < 5537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5538(int& val) {
    const int x = 5538;
    std::cout << 5538;
    for (int i = 0; i < 5538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5539(int& val) {
    const int x = 5539;
    std::cout << 5539;
    for (int i = 0; i < 5539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5540(int& val) {
    const int x = 5540;
    std::cout << 5540;
    for (int i = 0; i < 5540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5541(int& val) {
    const int x = 5541;
    std::cout << 5541;
    for (int i = 0; i < 5541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5542(int& val) {
    const int x = 5542;
    std::cout << 5542;
    for (int i = 0; i < 5542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5543(int& val) {
    const int x = 5543;
    std::cout << 5543;
    for (int i = 0; i < 5543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5544(int& val) {
    const int x = 5544;
    std::cout << 5544;
    for (int i = 0; i < 5544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5545(int& val) {
    const int x = 5545;
    std::cout << 5545;
    for (int i = 0; i < 5545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5546(int& val) {
    const int x = 5546;
    std::cout << 5546;
    for (int i = 0; i < 5546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5547(int& val) {
    const int x = 5547;
    std::cout << 5547;
    for (int i = 0; i < 5547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5548(int& val) {
    const int x = 5548;
    std::cout << 5548;
    for (int i = 0; i < 5548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5549(int& val) {
    const int x = 5549;
    std::cout << 5549;
    for (int i = 0; i < 5549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5550(int& val) {
    const int x = 5550;
    std::cout << 5550;
    for (int i = 0; i < 5550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5551(int& val) {
    const int x = 5551;
    std::cout << 5551;
    for (int i = 0; i < 5551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5552(int& val) {
    const int x = 5552;
    std::cout << 5552;
    for (int i = 0; i < 5552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5553(int& val) {
    const int x = 5553;
    std::cout << 5553;
    for (int i = 0; i < 5553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5554(int& val) {
    const int x = 5554;
    std::cout << 5554;
    for (int i = 0; i < 5554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5555(int& val) {
    const int x = 5555;
    std::cout << 5555;
    for (int i = 0; i < 5555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5556(int& val) {
    const int x = 5556;
    std::cout << 5556;
    for (int i = 0; i < 5556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5557(int& val) {
    const int x = 5557;
    std::cout << 5557;
    for (int i = 0; i < 5557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5558(int& val) {
    const int x = 5558;
    std::cout << 5558;
    for (int i = 0; i < 5558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5559(int& val) {
    const int x = 5559;
    std::cout << 5559;
    for (int i = 0; i < 5559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5560(int& val) {
    const int x = 5560;
    std::cout << 5560;
    for (int i = 0; i < 5560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5561(int& val) {
    const int x = 5561;
    std::cout << 5561;
    for (int i = 0; i < 5561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5562(int& val) {
    const int x = 5562;
    std::cout << 5562;
    for (int i = 0; i < 5562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5563(int& val) {
    const int x = 5563;
    std::cout << 5563;
    for (int i = 0; i < 5563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5564(int& val) {
    const int x = 5564;
    std::cout << 5564;
    for (int i = 0; i < 5564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5565(int& val) {
    const int x = 5565;
    std::cout << 5565;
    for (int i = 0; i < 5565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5566(int& val) {
    const int x = 5566;
    std::cout << 5566;
    for (int i = 0; i < 5566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5567(int& val) {
    const int x = 5567;
    std::cout << 5567;
    for (int i = 0; i < 5567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5568(int& val) {
    const int x = 5568;
    std::cout << 5568;
    for (int i = 0; i < 5568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5569(int& val) {
    const int x = 5569;
    std::cout << 5569;
    for (int i = 0; i < 5569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5570(int& val) {
    const int x = 5570;
    std::cout << 5570;
    for (int i = 0; i < 5570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5571(int& val) {
    const int x = 5571;
    std::cout << 5571;
    for (int i = 0; i < 5571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5572(int& val) {
    const int x = 5572;
    std::cout << 5572;
    for (int i = 0; i < 5572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5573(int& val) {
    const int x = 5573;
    std::cout << 5573;
    for (int i = 0; i < 5573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5574(int& val) {
    const int x = 5574;
    std::cout << 5574;
    for (int i = 0; i < 5574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5575(int& val) {
    const int x = 5575;
    std::cout << 5575;
    for (int i = 0; i < 5575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5576(int& val) {
    const int x = 5576;
    std::cout << 5576;
    for (int i = 0; i < 5576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5577(int& val) {
    const int x = 5577;
    std::cout << 5577;
    for (int i = 0; i < 5577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5578(int& val) {
    const int x = 5578;
    std::cout << 5578;
    for (int i = 0; i < 5578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5579(int& val) {
    const int x = 5579;
    std::cout << 5579;
    for (int i = 0; i < 5579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5580(int& val) {
    const int x = 5580;
    std::cout << 5580;
    for (int i = 0; i < 5580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5581(int& val) {
    const int x = 5581;
    std::cout << 5581;
    for (int i = 0; i < 5581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5582(int& val) {
    const int x = 5582;
    std::cout << 5582;
    for (int i = 0; i < 5582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5583(int& val) {
    const int x = 5583;
    std::cout << 5583;
    for (int i = 0; i < 5583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5584(int& val) {
    const int x = 5584;
    std::cout << 5584;
    for (int i = 0; i < 5584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5585(int& val) {
    const int x = 5585;
    std::cout << 5585;
    for (int i = 0; i < 5585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5586(int& val) {
    const int x = 5586;
    std::cout << 5586;
    for (int i = 0; i < 5586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5587(int& val) {
    const int x = 5587;
    std::cout << 5587;
    for (int i = 0; i < 5587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5588(int& val) {
    const int x = 5588;
    std::cout << 5588;
    for (int i = 0; i < 5588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5589(int& val) {
    const int x = 5589;
    std::cout << 5589;
    for (int i = 0; i < 5589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5590(int& val) {
    const int x = 5590;
    std::cout << 5590;
    for (int i = 0; i < 5590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5591(int& val) {
    const int x = 5591;
    std::cout << 5591;
    for (int i = 0; i < 5591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5592(int& val) {
    const int x = 5592;
    std::cout << 5592;
    for (int i = 0; i < 5592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5593(int& val) {
    const int x = 5593;
    std::cout << 5593;
    for (int i = 0; i < 5593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5594(int& val) {
    const int x = 5594;
    std::cout << 5594;
    for (int i = 0; i < 5594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5595(int& val) {
    const int x = 5595;
    std::cout << 5595;
    for (int i = 0; i < 5595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5596(int& val) {
    const int x = 5596;
    std::cout << 5596;
    for (int i = 0; i < 5596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5597(int& val) {
    const int x = 5597;
    std::cout << 5597;
    for (int i = 0; i < 5597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5598(int& val) {
    const int x = 5598;
    std::cout << 5598;
    for (int i = 0; i < 5598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5599(int& val) {
    const int x = 5599;
    std::cout << 5599;
    for (int i = 0; i < 5599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5600(int& val) {
    const int x = 5600;
    std::cout << 5600;
    for (int i = 0; i < 5600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5601(int& val) {
    const int x = 5601;
    std::cout << 5601;
    for (int i = 0; i < 5601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5602(int& val) {
    const int x = 5602;
    std::cout << 5602;
    for (int i = 0; i < 5602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5603(int& val) {
    const int x = 5603;
    std::cout << 5603;
    for (int i = 0; i < 5603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5604(int& val) {
    const int x = 5604;
    std::cout << 5604;
    for (int i = 0; i < 5604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5605(int& val) {
    const int x = 5605;
    std::cout << 5605;
    for (int i = 0; i < 5605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5606(int& val) {
    const int x = 5606;
    std::cout << 5606;
    for (int i = 0; i < 5606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5607(int& val) {
    const int x = 5607;
    std::cout << 5607;
    for (int i = 0; i < 5607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5608(int& val) {
    const int x = 5608;
    std::cout << 5608;
    for (int i = 0; i < 5608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5609(int& val) {
    const int x = 5609;
    std::cout << 5609;
    for (int i = 0; i < 5609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5610(int& val) {
    const int x = 5610;
    std::cout << 5610;
    for (int i = 0; i < 5610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5611(int& val) {
    const int x = 5611;
    std::cout << 5611;
    for (int i = 0; i < 5611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5612(int& val) {
    const int x = 5612;
    std::cout << 5612;
    for (int i = 0; i < 5612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5613(int& val) {
    const int x = 5613;
    std::cout << 5613;
    for (int i = 0; i < 5613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5614(int& val) {
    const int x = 5614;
    std::cout << 5614;
    for (int i = 0; i < 5614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5615(int& val) {
    const int x = 5615;
    std::cout << 5615;
    for (int i = 0; i < 5615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5616(int& val) {
    const int x = 5616;
    std::cout << 5616;
    for (int i = 0; i < 5616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5617(int& val) {
    const int x = 5617;
    std::cout << 5617;
    for (int i = 0; i < 5617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5618(int& val) {
    const int x = 5618;
    std::cout << 5618;
    for (int i = 0; i < 5618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5619(int& val) {
    const int x = 5619;
    std::cout << 5619;
    for (int i = 0; i < 5619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5620(int& val) {
    const int x = 5620;
    std::cout << 5620;
    for (int i = 0; i < 5620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5621(int& val) {
    const int x = 5621;
    std::cout << 5621;
    for (int i = 0; i < 5621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5622(int& val) {
    const int x = 5622;
    std::cout << 5622;
    for (int i = 0; i < 5622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5623(int& val) {
    const int x = 5623;
    std::cout << 5623;
    for (int i = 0; i < 5623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5624(int& val) {
    const int x = 5624;
    std::cout << 5624;
    for (int i = 0; i < 5624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5625(int& val) {
    const int x = 5625;
    std::cout << 5625;
    for (int i = 0; i < 5625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5626(int& val) {
    const int x = 5626;
    std::cout << 5626;
    for (int i = 0; i < 5626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5627(int& val) {
    const int x = 5627;
    std::cout << 5627;
    for (int i = 0; i < 5627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5628(int& val) {
    const int x = 5628;
    std::cout << 5628;
    for (int i = 0; i < 5628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5629(int& val) {
    const int x = 5629;
    std::cout << 5629;
    for (int i = 0; i < 5629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5630(int& val) {
    const int x = 5630;
    std::cout << 5630;
    for (int i = 0; i < 5630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5631(int& val) {
    const int x = 5631;
    std::cout << 5631;
    for (int i = 0; i < 5631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5632(int& val) {
    const int x = 5632;
    std::cout << 5632;
    for (int i = 0; i < 5632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5633(int& val) {
    const int x = 5633;
    std::cout << 5633;
    for (int i = 0; i < 5633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5634(int& val) {
    const int x = 5634;
    std::cout << 5634;
    for (int i = 0; i < 5634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5635(int& val) {
    const int x = 5635;
    std::cout << 5635;
    for (int i = 0; i < 5635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5636(int& val) {
    const int x = 5636;
    std::cout << 5636;
    for (int i = 0; i < 5636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5637(int& val) {
    const int x = 5637;
    std::cout << 5637;
    for (int i = 0; i < 5637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5638(int& val) {
    const int x = 5638;
    std::cout << 5638;
    for (int i = 0; i < 5638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5639(int& val) {
    const int x = 5639;
    std::cout << 5639;
    for (int i = 0; i < 5639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5640(int& val) {
    const int x = 5640;
    std::cout << 5640;
    for (int i = 0; i < 5640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5641(int& val) {
    const int x = 5641;
    std::cout << 5641;
    for (int i = 0; i < 5641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5642(int& val) {
    const int x = 5642;
    std::cout << 5642;
    for (int i = 0; i < 5642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5643(int& val) {
    const int x = 5643;
    std::cout << 5643;
    for (int i = 0; i < 5643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5644(int& val) {
    const int x = 5644;
    std::cout << 5644;
    for (int i = 0; i < 5644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5645(int& val) {
    const int x = 5645;
    std::cout << 5645;
    for (int i = 0; i < 5645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5646(int& val) {
    const int x = 5646;
    std::cout << 5646;
    for (int i = 0; i < 5646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5647(int& val) {
    const int x = 5647;
    std::cout << 5647;
    for (int i = 0; i < 5647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5648(int& val) {
    const int x = 5648;
    std::cout << 5648;
    for (int i = 0; i < 5648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5649(int& val) {
    const int x = 5649;
    std::cout << 5649;
    for (int i = 0; i < 5649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5650(int& val) {
    const int x = 5650;
    std::cout << 5650;
    for (int i = 0; i < 5650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5651(int& val) {
    const int x = 5651;
    std::cout << 5651;
    for (int i = 0; i < 5651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5652(int& val) {
    const int x = 5652;
    std::cout << 5652;
    for (int i = 0; i < 5652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5653(int& val) {
    const int x = 5653;
    std::cout << 5653;
    for (int i = 0; i < 5653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5654(int& val) {
    const int x = 5654;
    std::cout << 5654;
    for (int i = 0; i < 5654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5655(int& val) {
    const int x = 5655;
    std::cout << 5655;
    for (int i = 0; i < 5655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5656(int& val) {
    const int x = 5656;
    std::cout << 5656;
    for (int i = 0; i < 5656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5657(int& val) {
    const int x = 5657;
    std::cout << 5657;
    for (int i = 0; i < 5657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5658(int& val) {
    const int x = 5658;
    std::cout << 5658;
    for (int i = 0; i < 5658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5659(int& val) {
    const int x = 5659;
    std::cout << 5659;
    for (int i = 0; i < 5659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5660(int& val) {
    const int x = 5660;
    std::cout << 5660;
    for (int i = 0; i < 5660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5661(int& val) {
    const int x = 5661;
    std::cout << 5661;
    for (int i = 0; i < 5661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5662(int& val) {
    const int x = 5662;
    std::cout << 5662;
    for (int i = 0; i < 5662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5663(int& val) {
    const int x = 5663;
    std::cout << 5663;
    for (int i = 0; i < 5663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5664(int& val) {
    const int x = 5664;
    std::cout << 5664;
    for (int i = 0; i < 5664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5665(int& val) {
    const int x = 5665;
    std::cout << 5665;
    for (int i = 0; i < 5665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5666(int& val) {
    const int x = 5666;
    std::cout << 5666;
    for (int i = 0; i < 5666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5667(int& val) {
    const int x = 5667;
    std::cout << 5667;
    for (int i = 0; i < 5667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5668(int& val) {
    const int x = 5668;
    std::cout << 5668;
    for (int i = 0; i < 5668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5669(int& val) {
    const int x = 5669;
    std::cout << 5669;
    for (int i = 0; i < 5669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5670(int& val) {
    const int x = 5670;
    std::cout << 5670;
    for (int i = 0; i < 5670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5671(int& val) {
    const int x = 5671;
    std::cout << 5671;
    for (int i = 0; i < 5671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5672(int& val) {
    const int x = 5672;
    std::cout << 5672;
    for (int i = 0; i < 5672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5673(int& val) {
    const int x = 5673;
    std::cout << 5673;
    for (int i = 0; i < 5673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5674(int& val) {
    const int x = 5674;
    std::cout << 5674;
    for (int i = 0; i < 5674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5675(int& val) {
    const int x = 5675;
    std::cout << 5675;
    for (int i = 0; i < 5675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5676(int& val) {
    const int x = 5676;
    std::cout << 5676;
    for (int i = 0; i < 5676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5677(int& val) {
    const int x = 5677;
    std::cout << 5677;
    for (int i = 0; i < 5677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5678(int& val) {
    const int x = 5678;
    std::cout << 5678;
    for (int i = 0; i < 5678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5679(int& val) {
    const int x = 5679;
    std::cout << 5679;
    for (int i = 0; i < 5679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5680(int& val) {
    const int x = 5680;
    std::cout << 5680;
    for (int i = 0; i < 5680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5681(int& val) {
    const int x = 5681;
    std::cout << 5681;
    for (int i = 0; i < 5681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5682(int& val) {
    const int x = 5682;
    std::cout << 5682;
    for (int i = 0; i < 5682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5683(int& val) {
    const int x = 5683;
    std::cout << 5683;
    for (int i = 0; i < 5683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5684(int& val) {
    const int x = 5684;
    std::cout << 5684;
    for (int i = 0; i < 5684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5685(int& val) {
    const int x = 5685;
    std::cout << 5685;
    for (int i = 0; i < 5685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5686(int& val) {
    const int x = 5686;
    std::cout << 5686;
    for (int i = 0; i < 5686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5687(int& val) {
    const int x = 5687;
    std::cout << 5687;
    for (int i = 0; i < 5687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5688(int& val) {
    const int x = 5688;
    std::cout << 5688;
    for (int i = 0; i < 5688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5689(int& val) {
    const int x = 5689;
    std::cout << 5689;
    for (int i = 0; i < 5689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5690(int& val) {
    const int x = 5690;
    std::cout << 5690;
    for (int i = 0; i < 5690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5691(int& val) {
    const int x = 5691;
    std::cout << 5691;
    for (int i = 0; i < 5691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5692(int& val) {
    const int x = 5692;
    std::cout << 5692;
    for (int i = 0; i < 5692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5693(int& val) {
    const int x = 5693;
    std::cout << 5693;
    for (int i = 0; i < 5693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5694(int& val) {
    const int x = 5694;
    std::cout << 5694;
    for (int i = 0; i < 5694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5695(int& val) {
    const int x = 5695;
    std::cout << 5695;
    for (int i = 0; i < 5695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5696(int& val) {
    const int x = 5696;
    std::cout << 5696;
    for (int i = 0; i < 5696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5697(int& val) {
    const int x = 5697;
    std::cout << 5697;
    for (int i = 0; i < 5697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5698(int& val) {
    const int x = 5698;
    std::cout << 5698;
    for (int i = 0; i < 5698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5699(int& val) {
    const int x = 5699;
    std::cout << 5699;
    for (int i = 0; i < 5699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5700(int& val) {
    const int x = 5700;
    std::cout << 5700;
    for (int i = 0; i < 5700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5701(int& val) {
    const int x = 5701;
    std::cout << 5701;
    for (int i = 0; i < 5701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5702(int& val) {
    const int x = 5702;
    std::cout << 5702;
    for (int i = 0; i < 5702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5703(int& val) {
    const int x = 5703;
    std::cout << 5703;
    for (int i = 0; i < 5703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5704(int& val) {
    const int x = 5704;
    std::cout << 5704;
    for (int i = 0; i < 5704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5705(int& val) {
    const int x = 5705;
    std::cout << 5705;
    for (int i = 0; i < 5705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5706(int& val) {
    const int x = 5706;
    std::cout << 5706;
    for (int i = 0; i < 5706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5707(int& val) {
    const int x = 5707;
    std::cout << 5707;
    for (int i = 0; i < 5707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5708(int& val) {
    const int x = 5708;
    std::cout << 5708;
    for (int i = 0; i < 5708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5709(int& val) {
    const int x = 5709;
    std::cout << 5709;
    for (int i = 0; i < 5709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5710(int& val) {
    const int x = 5710;
    std::cout << 5710;
    for (int i = 0; i < 5710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5711(int& val) {
    const int x = 5711;
    std::cout << 5711;
    for (int i = 0; i < 5711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5712(int& val) {
    const int x = 5712;
    std::cout << 5712;
    for (int i = 0; i < 5712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5713(int& val) {
    const int x = 5713;
    std::cout << 5713;
    for (int i = 0; i < 5713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5714(int& val) {
    const int x = 5714;
    std::cout << 5714;
    for (int i = 0; i < 5714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5715(int& val) {
    const int x = 5715;
    std::cout << 5715;
    for (int i = 0; i < 5715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5716(int& val) {
    const int x = 5716;
    std::cout << 5716;
    for (int i = 0; i < 5716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5717(int& val) {
    const int x = 5717;
    std::cout << 5717;
    for (int i = 0; i < 5717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5718(int& val) {
    const int x = 5718;
    std::cout << 5718;
    for (int i = 0; i < 5718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5719(int& val) {
    const int x = 5719;
    std::cout << 5719;
    for (int i = 0; i < 5719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5720(int& val) {
    const int x = 5720;
    std::cout << 5720;
    for (int i = 0; i < 5720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5721(int& val) {
    const int x = 5721;
    std::cout << 5721;
    for (int i = 0; i < 5721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5722(int& val) {
    const int x = 5722;
    std::cout << 5722;
    for (int i = 0; i < 5722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5723(int& val) {
    const int x = 5723;
    std::cout << 5723;
    for (int i = 0; i < 5723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5724(int& val) {
    const int x = 5724;
    std::cout << 5724;
    for (int i = 0; i < 5724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5725(int& val) {
    const int x = 5725;
    std::cout << 5725;
    for (int i = 0; i < 5725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5726(int& val) {
    const int x = 5726;
    std::cout << 5726;
    for (int i = 0; i < 5726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5727(int& val) {
    const int x = 5727;
    std::cout << 5727;
    for (int i = 0; i < 5727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5728(int& val) {
    const int x = 5728;
    std::cout << 5728;
    for (int i = 0; i < 5728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5729(int& val) {
    const int x = 5729;
    std::cout << 5729;
    for (int i = 0; i < 5729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5730(int& val) {
    const int x = 5730;
    std::cout << 5730;
    for (int i = 0; i < 5730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5731(int& val) {
    const int x = 5731;
    std::cout << 5731;
    for (int i = 0; i < 5731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5732(int& val) {
    const int x = 5732;
    std::cout << 5732;
    for (int i = 0; i < 5732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5733(int& val) {
    const int x = 5733;
    std::cout << 5733;
    for (int i = 0; i < 5733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5734(int& val) {
    const int x = 5734;
    std::cout << 5734;
    for (int i = 0; i < 5734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5735(int& val) {
    const int x = 5735;
    std::cout << 5735;
    for (int i = 0; i < 5735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5736(int& val) {
    const int x = 5736;
    std::cout << 5736;
    for (int i = 0; i < 5736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5737(int& val) {
    const int x = 5737;
    std::cout << 5737;
    for (int i = 0; i < 5737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5738(int& val) {
    const int x = 5738;
    std::cout << 5738;
    for (int i = 0; i < 5738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5739(int& val) {
    const int x = 5739;
    std::cout << 5739;
    for (int i = 0; i < 5739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5740(int& val) {
    const int x = 5740;
    std::cout << 5740;
    for (int i = 0; i < 5740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5741(int& val) {
    const int x = 5741;
    std::cout << 5741;
    for (int i = 0; i < 5741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5742(int& val) {
    const int x = 5742;
    std::cout << 5742;
    for (int i = 0; i < 5742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5743(int& val) {
    const int x = 5743;
    std::cout << 5743;
    for (int i = 0; i < 5743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5744(int& val) {
    const int x = 5744;
    std::cout << 5744;
    for (int i = 0; i < 5744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5745(int& val) {
    const int x = 5745;
    std::cout << 5745;
    for (int i = 0; i < 5745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5746(int& val) {
    const int x = 5746;
    std::cout << 5746;
    for (int i = 0; i < 5746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5747(int& val) {
    const int x = 5747;
    std::cout << 5747;
    for (int i = 0; i < 5747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5748(int& val) {
    const int x = 5748;
    std::cout << 5748;
    for (int i = 0; i < 5748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5749(int& val) {
    const int x = 5749;
    std::cout << 5749;
    for (int i = 0; i < 5749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5750(int& val) {
    const int x = 5750;
    std::cout << 5750;
    for (int i = 0; i < 5750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5751(int& val) {
    const int x = 5751;
    std::cout << 5751;
    for (int i = 0; i < 5751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5752(int& val) {
    const int x = 5752;
    std::cout << 5752;
    for (int i = 0; i < 5752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5753(int& val) {
    const int x = 5753;
    std::cout << 5753;
    for (int i = 0; i < 5753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5754(int& val) {
    const int x = 5754;
    std::cout << 5754;
    for (int i = 0; i < 5754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5755(int& val) {
    const int x = 5755;
    std::cout << 5755;
    for (int i = 0; i < 5755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5756(int& val) {
    const int x = 5756;
    std::cout << 5756;
    for (int i = 0; i < 5756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5757(int& val) {
    const int x = 5757;
    std::cout << 5757;
    for (int i = 0; i < 5757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5758(int& val) {
    const int x = 5758;
    std::cout << 5758;
    for (int i = 0; i < 5758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5759(int& val) {
    const int x = 5759;
    std::cout << 5759;
    for (int i = 0; i < 5759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5760(int& val) {
    const int x = 5760;
    std::cout << 5760;
    for (int i = 0; i < 5760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5761(int& val) {
    const int x = 5761;
    std::cout << 5761;
    for (int i = 0; i < 5761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5762(int& val) {
    const int x = 5762;
    std::cout << 5762;
    for (int i = 0; i < 5762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5763(int& val) {
    const int x = 5763;
    std::cout << 5763;
    for (int i = 0; i < 5763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5764(int& val) {
    const int x = 5764;
    std::cout << 5764;
    for (int i = 0; i < 5764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5765(int& val) {
    const int x = 5765;
    std::cout << 5765;
    for (int i = 0; i < 5765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5766(int& val) {
    const int x = 5766;
    std::cout << 5766;
    for (int i = 0; i < 5766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5767(int& val) {
    const int x = 5767;
    std::cout << 5767;
    for (int i = 0; i < 5767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5768(int& val) {
    const int x = 5768;
    std::cout << 5768;
    for (int i = 0; i < 5768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5769(int& val) {
    const int x = 5769;
    std::cout << 5769;
    for (int i = 0; i < 5769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5770(int& val) {
    const int x = 5770;
    std::cout << 5770;
    for (int i = 0; i < 5770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5771(int& val) {
    const int x = 5771;
    std::cout << 5771;
    for (int i = 0; i < 5771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5772(int& val) {
    const int x = 5772;
    std::cout << 5772;
    for (int i = 0; i < 5772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5773(int& val) {
    const int x = 5773;
    std::cout << 5773;
    for (int i = 0; i < 5773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5774(int& val) {
    const int x = 5774;
    std::cout << 5774;
    for (int i = 0; i < 5774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5775(int& val) {
    const int x = 5775;
    std::cout << 5775;
    for (int i = 0; i < 5775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5776(int& val) {
    const int x = 5776;
    std::cout << 5776;
    for (int i = 0; i < 5776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5777(int& val) {
    const int x = 5777;
    std::cout << 5777;
    for (int i = 0; i < 5777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5778(int& val) {
    const int x = 5778;
    std::cout << 5778;
    for (int i = 0; i < 5778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5779(int& val) {
    const int x = 5779;
    std::cout << 5779;
    for (int i = 0; i < 5779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5780(int& val) {
    const int x = 5780;
    std::cout << 5780;
    for (int i = 0; i < 5780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5781(int& val) {
    const int x = 5781;
    std::cout << 5781;
    for (int i = 0; i < 5781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5782(int& val) {
    const int x = 5782;
    std::cout << 5782;
    for (int i = 0; i < 5782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5783(int& val) {
    const int x = 5783;
    std::cout << 5783;
    for (int i = 0; i < 5783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5784(int& val) {
    const int x = 5784;
    std::cout << 5784;
    for (int i = 0; i < 5784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5785(int& val) {
    const int x = 5785;
    std::cout << 5785;
    for (int i = 0; i < 5785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5786(int& val) {
    const int x = 5786;
    std::cout << 5786;
    for (int i = 0; i < 5786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5787(int& val) {
    const int x = 5787;
    std::cout << 5787;
    for (int i = 0; i < 5787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5788(int& val) {
    const int x = 5788;
    std::cout << 5788;
    for (int i = 0; i < 5788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5789(int& val) {
    const int x = 5789;
    std::cout << 5789;
    for (int i = 0; i < 5789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5790(int& val) {
    const int x = 5790;
    std::cout << 5790;
    for (int i = 0; i < 5790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5791(int& val) {
    const int x = 5791;
    std::cout << 5791;
    for (int i = 0; i < 5791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5792(int& val) {
    const int x = 5792;
    std::cout << 5792;
    for (int i = 0; i < 5792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5793(int& val) {
    const int x = 5793;
    std::cout << 5793;
    for (int i = 0; i < 5793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5794(int& val) {
    const int x = 5794;
    std::cout << 5794;
    for (int i = 0; i < 5794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5795(int& val) {
    const int x = 5795;
    std::cout << 5795;
    for (int i = 0; i < 5795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5796(int& val) {
    const int x = 5796;
    std::cout << 5796;
    for (int i = 0; i < 5796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5797(int& val) {
    const int x = 5797;
    std::cout << 5797;
    for (int i = 0; i < 5797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5798(int& val) {
    const int x = 5798;
    std::cout << 5798;
    for (int i = 0; i < 5798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5799(int& val) {
    const int x = 5799;
    std::cout << 5799;
    for (int i = 0; i < 5799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5800(int& val) {
    const int x = 5800;
    std::cout << 5800;
    for (int i = 0; i < 5800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5801(int& val) {
    const int x = 5801;
    std::cout << 5801;
    for (int i = 0; i < 5801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5802(int& val) {
    const int x = 5802;
    std::cout << 5802;
    for (int i = 0; i < 5802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5803(int& val) {
    const int x = 5803;
    std::cout << 5803;
    for (int i = 0; i < 5803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5804(int& val) {
    const int x = 5804;
    std::cout << 5804;
    for (int i = 0; i < 5804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5805(int& val) {
    const int x = 5805;
    std::cout << 5805;
    for (int i = 0; i < 5805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5806(int& val) {
    const int x = 5806;
    std::cout << 5806;
    for (int i = 0; i < 5806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5807(int& val) {
    const int x = 5807;
    std::cout << 5807;
    for (int i = 0; i < 5807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5808(int& val) {
    const int x = 5808;
    std::cout << 5808;
    for (int i = 0; i < 5808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5809(int& val) {
    const int x = 5809;
    std::cout << 5809;
    for (int i = 0; i < 5809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5810(int& val) {
    const int x = 5810;
    std::cout << 5810;
    for (int i = 0; i < 5810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5811(int& val) {
    const int x = 5811;
    std::cout << 5811;
    for (int i = 0; i < 5811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5812(int& val) {
    const int x = 5812;
    std::cout << 5812;
    for (int i = 0; i < 5812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5813(int& val) {
    const int x = 5813;
    std::cout << 5813;
    for (int i = 0; i < 5813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5814(int& val) {
    const int x = 5814;
    std::cout << 5814;
    for (int i = 0; i < 5814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5815(int& val) {
    const int x = 5815;
    std::cout << 5815;
    for (int i = 0; i < 5815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5816(int& val) {
    const int x = 5816;
    std::cout << 5816;
    for (int i = 0; i < 5816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5817(int& val) {
    const int x = 5817;
    std::cout << 5817;
    for (int i = 0; i < 5817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5818(int& val) {
    const int x = 5818;
    std::cout << 5818;
    for (int i = 0; i < 5818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5819(int& val) {
    const int x = 5819;
    std::cout << 5819;
    for (int i = 0; i < 5819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5820(int& val) {
    const int x = 5820;
    std::cout << 5820;
    for (int i = 0; i < 5820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5821(int& val) {
    const int x = 5821;
    std::cout << 5821;
    for (int i = 0; i < 5821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5822(int& val) {
    const int x = 5822;
    std::cout << 5822;
    for (int i = 0; i < 5822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5823(int& val) {
    const int x = 5823;
    std::cout << 5823;
    for (int i = 0; i < 5823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5824(int& val) {
    const int x = 5824;
    std::cout << 5824;
    for (int i = 0; i < 5824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5825(int& val) {
    const int x = 5825;
    std::cout << 5825;
    for (int i = 0; i < 5825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5826(int& val) {
    const int x = 5826;
    std::cout << 5826;
    for (int i = 0; i < 5826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5827(int& val) {
    const int x = 5827;
    std::cout << 5827;
    for (int i = 0; i < 5827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5828(int& val) {
    const int x = 5828;
    std::cout << 5828;
    for (int i = 0; i < 5828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5829(int& val) {
    const int x = 5829;
    std::cout << 5829;
    for (int i = 0; i < 5829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5830(int& val) {
    const int x = 5830;
    std::cout << 5830;
    for (int i = 0; i < 5830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5831(int& val) {
    const int x = 5831;
    std::cout << 5831;
    for (int i = 0; i < 5831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5832(int& val) {
    const int x = 5832;
    std::cout << 5832;
    for (int i = 0; i < 5832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5833(int& val) {
    const int x = 5833;
    std::cout << 5833;
    for (int i = 0; i < 5833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5834(int& val) {
    const int x = 5834;
    std::cout << 5834;
    for (int i = 0; i < 5834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5835(int& val) {
    const int x = 5835;
    std::cout << 5835;
    for (int i = 0; i < 5835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5836(int& val) {
    const int x = 5836;
    std::cout << 5836;
    for (int i = 0; i < 5836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5837(int& val) {
    const int x = 5837;
    std::cout << 5837;
    for (int i = 0; i < 5837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5838(int& val) {
    const int x = 5838;
    std::cout << 5838;
    for (int i = 0; i < 5838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5839(int& val) {
    const int x = 5839;
    std::cout << 5839;
    for (int i = 0; i < 5839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5840(int& val) {
    const int x = 5840;
    std::cout << 5840;
    for (int i = 0; i < 5840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5841(int& val) {
    const int x = 5841;
    std::cout << 5841;
    for (int i = 0; i < 5841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5842(int& val) {
    const int x = 5842;
    std::cout << 5842;
    for (int i = 0; i < 5842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5843(int& val) {
    const int x = 5843;
    std::cout << 5843;
    for (int i = 0; i < 5843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5844(int& val) {
    const int x = 5844;
    std::cout << 5844;
    for (int i = 0; i < 5844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5845(int& val) {
    const int x = 5845;
    std::cout << 5845;
    for (int i = 0; i < 5845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5846(int& val) {
    const int x = 5846;
    std::cout << 5846;
    for (int i = 0; i < 5846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5847(int& val) {
    const int x = 5847;
    std::cout << 5847;
    for (int i = 0; i < 5847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5848(int& val) {
    const int x = 5848;
    std::cout << 5848;
    for (int i = 0; i < 5848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5849(int& val) {
    const int x = 5849;
    std::cout << 5849;
    for (int i = 0; i < 5849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5850(int& val) {
    const int x = 5850;
    std::cout << 5850;
    for (int i = 0; i < 5850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5851(int& val) {
    const int x = 5851;
    std::cout << 5851;
    for (int i = 0; i < 5851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5852(int& val) {
    const int x = 5852;
    std::cout << 5852;
    for (int i = 0; i < 5852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5853(int& val) {
    const int x = 5853;
    std::cout << 5853;
    for (int i = 0; i < 5853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5854(int& val) {
    const int x = 5854;
    std::cout << 5854;
    for (int i = 0; i < 5854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5855(int& val) {
    const int x = 5855;
    std::cout << 5855;
    for (int i = 0; i < 5855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5856(int& val) {
    const int x = 5856;
    std::cout << 5856;
    for (int i = 0; i < 5856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5857(int& val) {
    const int x = 5857;
    std::cout << 5857;
    for (int i = 0; i < 5857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5858(int& val) {
    const int x = 5858;
    std::cout << 5858;
    for (int i = 0; i < 5858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5859(int& val) {
    const int x = 5859;
    std::cout << 5859;
    for (int i = 0; i < 5859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5860(int& val) {
    const int x = 5860;
    std::cout << 5860;
    for (int i = 0; i < 5860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5861(int& val) {
    const int x = 5861;
    std::cout << 5861;
    for (int i = 0; i < 5861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5862(int& val) {
    const int x = 5862;
    std::cout << 5862;
    for (int i = 0; i < 5862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5863(int& val) {
    const int x = 5863;
    std::cout << 5863;
    for (int i = 0; i < 5863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5864(int& val) {
    const int x = 5864;
    std::cout << 5864;
    for (int i = 0; i < 5864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5865(int& val) {
    const int x = 5865;
    std::cout << 5865;
    for (int i = 0; i < 5865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5866(int& val) {
    const int x = 5866;
    std::cout << 5866;
    for (int i = 0; i < 5866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5867(int& val) {
    const int x = 5867;
    std::cout << 5867;
    for (int i = 0; i < 5867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5868(int& val) {
    const int x = 5868;
    std::cout << 5868;
    for (int i = 0; i < 5868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5869(int& val) {
    const int x = 5869;
    std::cout << 5869;
    for (int i = 0; i < 5869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5870(int& val) {
    const int x = 5870;
    std::cout << 5870;
    for (int i = 0; i < 5870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5871(int& val) {
    const int x = 5871;
    std::cout << 5871;
    for (int i = 0; i < 5871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5872(int& val) {
    const int x = 5872;
    std::cout << 5872;
    for (int i = 0; i < 5872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5873(int& val) {
    const int x = 5873;
    std::cout << 5873;
    for (int i = 0; i < 5873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5874(int& val) {
    const int x = 5874;
    std::cout << 5874;
    for (int i = 0; i < 5874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5875(int& val) {
    const int x = 5875;
    std::cout << 5875;
    for (int i = 0; i < 5875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5876(int& val) {
    const int x = 5876;
    std::cout << 5876;
    for (int i = 0; i < 5876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5877(int& val) {
    const int x = 5877;
    std::cout << 5877;
    for (int i = 0; i < 5877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5878(int& val) {
    const int x = 5878;
    std::cout << 5878;
    for (int i = 0; i < 5878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5879(int& val) {
    const int x = 5879;
    std::cout << 5879;
    for (int i = 0; i < 5879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5880(int& val) {
    const int x = 5880;
    std::cout << 5880;
    for (int i = 0; i < 5880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5881(int& val) {
    const int x = 5881;
    std::cout << 5881;
    for (int i = 0; i < 5881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5882(int& val) {
    const int x = 5882;
    std::cout << 5882;
    for (int i = 0; i < 5882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5883(int& val) {
    const int x = 5883;
    std::cout << 5883;
    for (int i = 0; i < 5883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5884(int& val) {
    const int x = 5884;
    std::cout << 5884;
    for (int i = 0; i < 5884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5885(int& val) {
    const int x = 5885;
    std::cout << 5885;
    for (int i = 0; i < 5885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5886(int& val) {
    const int x = 5886;
    std::cout << 5886;
    for (int i = 0; i < 5886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5887(int& val) {
    const int x = 5887;
    std::cout << 5887;
    for (int i = 0; i < 5887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5888(int& val) {
    const int x = 5888;
    std::cout << 5888;
    for (int i = 0; i < 5888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5889(int& val) {
    const int x = 5889;
    std::cout << 5889;
    for (int i = 0; i < 5889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5890(int& val) {
    const int x = 5890;
    std::cout << 5890;
    for (int i = 0; i < 5890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5891(int& val) {
    const int x = 5891;
    std::cout << 5891;
    for (int i = 0; i < 5891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5892(int& val) {
    const int x = 5892;
    std::cout << 5892;
    for (int i = 0; i < 5892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5893(int& val) {
    const int x = 5893;
    std::cout << 5893;
    for (int i = 0; i < 5893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5894(int& val) {
    const int x = 5894;
    std::cout << 5894;
    for (int i = 0; i < 5894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5895(int& val) {
    const int x = 5895;
    std::cout << 5895;
    for (int i = 0; i < 5895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5896(int& val) {
    const int x = 5896;
    std::cout << 5896;
    for (int i = 0; i < 5896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5897(int& val) {
    const int x = 5897;
    std::cout << 5897;
    for (int i = 0; i < 5897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5898(int& val) {
    const int x = 5898;
    std::cout << 5898;
    for (int i = 0; i < 5898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5899(int& val) {
    const int x = 5899;
    std::cout << 5899;
    for (int i = 0; i < 5899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5900(int& val) {
    const int x = 5900;
    std::cout << 5900;
    for (int i = 0; i < 5900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5901(int& val) {
    const int x = 5901;
    std::cout << 5901;
    for (int i = 0; i < 5901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5902(int& val) {
    const int x = 5902;
    std::cout << 5902;
    for (int i = 0; i < 5902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5903(int& val) {
    const int x = 5903;
    std::cout << 5903;
    for (int i = 0; i < 5903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5904(int& val) {
    const int x = 5904;
    std::cout << 5904;
    for (int i = 0; i < 5904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5905(int& val) {
    const int x = 5905;
    std::cout << 5905;
    for (int i = 0; i < 5905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5906(int& val) {
    const int x = 5906;
    std::cout << 5906;
    for (int i = 0; i < 5906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5907(int& val) {
    const int x = 5907;
    std::cout << 5907;
    for (int i = 0; i < 5907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5908(int& val) {
    const int x = 5908;
    std::cout << 5908;
    for (int i = 0; i < 5908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5909(int& val) {
    const int x = 5909;
    std::cout << 5909;
    for (int i = 0; i < 5909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5910(int& val) {
    const int x = 5910;
    std::cout << 5910;
    for (int i = 0; i < 5910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5911(int& val) {
    const int x = 5911;
    std::cout << 5911;
    for (int i = 0; i < 5911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5912(int& val) {
    const int x = 5912;
    std::cout << 5912;
    for (int i = 0; i < 5912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5913(int& val) {
    const int x = 5913;
    std::cout << 5913;
    for (int i = 0; i < 5913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5914(int& val) {
    const int x = 5914;
    std::cout << 5914;
    for (int i = 0; i < 5914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5915(int& val) {
    const int x = 5915;
    std::cout << 5915;
    for (int i = 0; i < 5915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5916(int& val) {
    const int x = 5916;
    std::cout << 5916;
    for (int i = 0; i < 5916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5917(int& val) {
    const int x = 5917;
    std::cout << 5917;
    for (int i = 0; i < 5917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5918(int& val) {
    const int x = 5918;
    std::cout << 5918;
    for (int i = 0; i < 5918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5919(int& val) {
    const int x = 5919;
    std::cout << 5919;
    for (int i = 0; i < 5919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5920(int& val) {
    const int x = 5920;
    std::cout << 5920;
    for (int i = 0; i < 5920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5921(int& val) {
    const int x = 5921;
    std::cout << 5921;
    for (int i = 0; i < 5921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5922(int& val) {
    const int x = 5922;
    std::cout << 5922;
    for (int i = 0; i < 5922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5923(int& val) {
    const int x = 5923;
    std::cout << 5923;
    for (int i = 0; i < 5923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5924(int& val) {
    const int x = 5924;
    std::cout << 5924;
    for (int i = 0; i < 5924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5925(int& val) {
    const int x = 5925;
    std::cout << 5925;
    for (int i = 0; i < 5925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5926(int& val) {
    const int x = 5926;
    std::cout << 5926;
    for (int i = 0; i < 5926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5927(int& val) {
    const int x = 5927;
    std::cout << 5927;
    for (int i = 0; i < 5927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5928(int& val) {
    const int x = 5928;
    std::cout << 5928;
    for (int i = 0; i < 5928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5929(int& val) {
    const int x = 5929;
    std::cout << 5929;
    for (int i = 0; i < 5929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5930(int& val) {
    const int x = 5930;
    std::cout << 5930;
    for (int i = 0; i < 5930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5931(int& val) {
    const int x = 5931;
    std::cout << 5931;
    for (int i = 0; i < 5931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5932(int& val) {
    const int x = 5932;
    std::cout << 5932;
    for (int i = 0; i < 5932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5933(int& val) {
    const int x = 5933;
    std::cout << 5933;
    for (int i = 0; i < 5933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5934(int& val) {
    const int x = 5934;
    std::cout << 5934;
    for (int i = 0; i < 5934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5935(int& val) {
    const int x = 5935;
    std::cout << 5935;
    for (int i = 0; i < 5935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5936(int& val) {
    const int x = 5936;
    std::cout << 5936;
    for (int i = 0; i < 5936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5937(int& val) {
    const int x = 5937;
    std::cout << 5937;
    for (int i = 0; i < 5937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5938(int& val) {
    const int x = 5938;
    std::cout << 5938;
    for (int i = 0; i < 5938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5939(int& val) {
    const int x = 5939;
    std::cout << 5939;
    for (int i = 0; i < 5939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5940(int& val) {
    const int x = 5940;
    std::cout << 5940;
    for (int i = 0; i < 5940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5941(int& val) {
    const int x = 5941;
    std::cout << 5941;
    for (int i = 0; i < 5941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5942(int& val) {
    const int x = 5942;
    std::cout << 5942;
    for (int i = 0; i < 5942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5943(int& val) {
    const int x = 5943;
    std::cout << 5943;
    for (int i = 0; i < 5943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5944(int& val) {
    const int x = 5944;
    std::cout << 5944;
    for (int i = 0; i < 5944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5945(int& val) {
    const int x = 5945;
    std::cout << 5945;
    for (int i = 0; i < 5945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5946(int& val) {
    const int x = 5946;
    std::cout << 5946;
    for (int i = 0; i < 5946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5947(int& val) {
    const int x = 5947;
    std::cout << 5947;
    for (int i = 0; i < 5947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5948(int& val) {
    const int x = 5948;
    std::cout << 5948;
    for (int i = 0; i < 5948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5949(int& val) {
    const int x = 5949;
    std::cout << 5949;
    for (int i = 0; i < 5949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5950(int& val) {
    const int x = 5950;
    std::cout << 5950;
    for (int i = 0; i < 5950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5951(int& val) {
    const int x = 5951;
    std::cout << 5951;
    for (int i = 0; i < 5951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5952(int& val) {
    const int x = 5952;
    std::cout << 5952;
    for (int i = 0; i < 5952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5953(int& val) {
    const int x = 5953;
    std::cout << 5953;
    for (int i = 0; i < 5953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5954(int& val) {
    const int x = 5954;
    std::cout << 5954;
    for (int i = 0; i < 5954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5955(int& val) {
    const int x = 5955;
    std::cout << 5955;
    for (int i = 0; i < 5955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5956(int& val) {
    const int x = 5956;
    std::cout << 5956;
    for (int i = 0; i < 5956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5957(int& val) {
    const int x = 5957;
    std::cout << 5957;
    for (int i = 0; i < 5957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5958(int& val) {
    const int x = 5958;
    std::cout << 5958;
    for (int i = 0; i < 5958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5959(int& val) {
    const int x = 5959;
    std::cout << 5959;
    for (int i = 0; i < 5959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5960(int& val) {
    const int x = 5960;
    std::cout << 5960;
    for (int i = 0; i < 5960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5961(int& val) {
    const int x = 5961;
    std::cout << 5961;
    for (int i = 0; i < 5961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5962(int& val) {
    const int x = 5962;
    std::cout << 5962;
    for (int i = 0; i < 5962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5963(int& val) {
    const int x = 5963;
    std::cout << 5963;
    for (int i = 0; i < 5963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5964(int& val) {
    const int x = 5964;
    std::cout << 5964;
    for (int i = 0; i < 5964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5965(int& val) {
    const int x = 5965;
    std::cout << 5965;
    for (int i = 0; i < 5965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5966(int& val) {
    const int x = 5966;
    std::cout << 5966;
    for (int i = 0; i < 5966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5967(int& val) {
    const int x = 5967;
    std::cout << 5967;
    for (int i = 0; i < 5967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5968(int& val) {
    const int x = 5968;
    std::cout << 5968;
    for (int i = 0; i < 5968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5969(int& val) {
    const int x = 5969;
    std::cout << 5969;
    for (int i = 0; i < 5969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5970(int& val) {
    const int x = 5970;
    std::cout << 5970;
    for (int i = 0; i < 5970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5971(int& val) {
    const int x = 5971;
    std::cout << 5971;
    for (int i = 0; i < 5971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5972(int& val) {
    const int x = 5972;
    std::cout << 5972;
    for (int i = 0; i < 5972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5973(int& val) {
    const int x = 5973;
    std::cout << 5973;
    for (int i = 0; i < 5973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5974(int& val) {
    const int x = 5974;
    std::cout << 5974;
    for (int i = 0; i < 5974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5975(int& val) {
    const int x = 5975;
    std::cout << 5975;
    for (int i = 0; i < 5975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5976(int& val) {
    const int x = 5976;
    std::cout << 5976;
    for (int i = 0; i < 5976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5977(int& val) {
    const int x = 5977;
    std::cout << 5977;
    for (int i = 0; i < 5977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5978(int& val) {
    const int x = 5978;
    std::cout << 5978;
    for (int i = 0; i < 5978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5979(int& val) {
    const int x = 5979;
    std::cout << 5979;
    for (int i = 0; i < 5979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5980(int& val) {
    const int x = 5980;
    std::cout << 5980;
    for (int i = 0; i < 5980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5981(int& val) {
    const int x = 5981;
    std::cout << 5981;
    for (int i = 0; i < 5981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5982(int& val) {
    const int x = 5982;
    std::cout << 5982;
    for (int i = 0; i < 5982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5983(int& val) {
    const int x = 5983;
    std::cout << 5983;
    for (int i = 0; i < 5983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5984(int& val) {
    const int x = 5984;
    std::cout << 5984;
    for (int i = 0; i < 5984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5985(int& val) {
    const int x = 5985;
    std::cout << 5985;
    for (int i = 0; i < 5985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5986(int& val) {
    const int x = 5986;
    std::cout << 5986;
    for (int i = 0; i < 5986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5987(int& val) {
    const int x = 5987;
    std::cout << 5987;
    for (int i = 0; i < 5987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5988(int& val) {
    const int x = 5988;
    std::cout << 5988;
    for (int i = 0; i < 5988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5989(int& val) {
    const int x = 5989;
    std::cout << 5989;
    for (int i = 0; i < 5989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5990(int& val) {
    const int x = 5990;
    std::cout << 5990;
    for (int i = 0; i < 5990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5991(int& val) {
    const int x = 5991;
    std::cout << 5991;
    for (int i = 0; i < 5991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5992(int& val) {
    const int x = 5992;
    std::cout << 5992;
    for (int i = 0; i < 5992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5993(int& val) {
    const int x = 5993;
    std::cout << 5993;
    for (int i = 0; i < 5993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5994(int& val) {
    const int x = 5994;
    std::cout << 5994;
    for (int i = 0; i < 5994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5995(int& val) {
    const int x = 5995;
    std::cout << 5995;
    for (int i = 0; i < 5995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5996(int& val) {
    const int x = 5996;
    std::cout << 5996;
    for (int i = 0; i < 5996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5997(int& val) {
    const int x = 5997;
    std::cout << 5997;
    for (int i = 0; i < 5997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5998(int& val) {
    const int x = 5998;
    std::cout << 5998;
    for (int i = 0; i < 5998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5999(int& val) {
    const int x = 5999;
    std::cout << 5999;
    for (int i = 0; i < 5999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6000(int& val) {
    const int x = 6000;
    std::cout << 6000;
    for (int i = 0; i < 6000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6001(int& val) {
    const int x = 6001;
    std::cout << 6001;
    for (int i = 0; i < 6001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6002(int& val) {
    const int x = 6002;
    std::cout << 6002;
    for (int i = 0; i < 6002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6003(int& val) {
    const int x = 6003;
    std::cout << 6003;
    for (int i = 0; i < 6003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6004(int& val) {
    const int x = 6004;
    std::cout << 6004;
    for (int i = 0; i < 6004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6005(int& val) {
    const int x = 6005;
    std::cout << 6005;
    for (int i = 0; i < 6005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6006(int& val) {
    const int x = 6006;
    std::cout << 6006;
    for (int i = 0; i < 6006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6007(int& val) {
    const int x = 6007;
    std::cout << 6007;
    for (int i = 0; i < 6007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6008(int& val) {
    const int x = 6008;
    std::cout << 6008;
    for (int i = 0; i < 6008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6009(int& val) {
    const int x = 6009;
    std::cout << 6009;
    for (int i = 0; i < 6009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6010(int& val) {
    const int x = 6010;
    std::cout << 6010;
    for (int i = 0; i < 6010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6011(int& val) {
    const int x = 6011;
    std::cout << 6011;
    for (int i = 0; i < 6011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6012(int& val) {
    const int x = 6012;
    std::cout << 6012;
    for (int i = 0; i < 6012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6013(int& val) {
    const int x = 6013;
    std::cout << 6013;
    for (int i = 0; i < 6013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6014(int& val) {
    const int x = 6014;
    std::cout << 6014;
    for (int i = 0; i < 6014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6015(int& val) {
    const int x = 6015;
    std::cout << 6015;
    for (int i = 0; i < 6015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6016(int& val) {
    const int x = 6016;
    std::cout << 6016;
    for (int i = 0; i < 6016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6017(int& val) {
    const int x = 6017;
    std::cout << 6017;
    for (int i = 0; i < 6017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6018(int& val) {
    const int x = 6018;
    std::cout << 6018;
    for (int i = 0; i < 6018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6019(int& val) {
    const int x = 6019;
    std::cout << 6019;
    for (int i = 0; i < 6019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6020(int& val) {
    const int x = 6020;
    std::cout << 6020;
    for (int i = 0; i < 6020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6021(int& val) {
    const int x = 6021;
    std::cout << 6021;
    for (int i = 0; i < 6021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6022(int& val) {
    const int x = 6022;
    std::cout << 6022;
    for (int i = 0; i < 6022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6023(int& val) {
    const int x = 6023;
    std::cout << 6023;
    for (int i = 0; i < 6023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6024(int& val) {
    const int x = 6024;
    std::cout << 6024;
    for (int i = 0; i < 6024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6025(int& val) {
    const int x = 6025;
    std::cout << 6025;
    for (int i = 0; i < 6025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6026(int& val) {
    const int x = 6026;
    std::cout << 6026;
    for (int i = 0; i < 6026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6027(int& val) {
    const int x = 6027;
    std::cout << 6027;
    for (int i = 0; i < 6027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6028(int& val) {
    const int x = 6028;
    std::cout << 6028;
    for (int i = 0; i < 6028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6029(int& val) {
    const int x = 6029;
    std::cout << 6029;
    for (int i = 0; i < 6029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6030(int& val) {
    const int x = 6030;
    std::cout << 6030;
    for (int i = 0; i < 6030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6031(int& val) {
    const int x = 6031;
    std::cout << 6031;
    for (int i = 0; i < 6031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6032(int& val) {
    const int x = 6032;
    std::cout << 6032;
    for (int i = 0; i < 6032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6033(int& val) {
    const int x = 6033;
    std::cout << 6033;
    for (int i = 0; i < 6033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6034(int& val) {
    const int x = 6034;
    std::cout << 6034;
    for (int i = 0; i < 6034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6035(int& val) {
    const int x = 6035;
    std::cout << 6035;
    for (int i = 0; i < 6035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6036(int& val) {
    const int x = 6036;
    std::cout << 6036;
    for (int i = 0; i < 6036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6037(int& val) {
    const int x = 6037;
    std::cout << 6037;
    for (int i = 0; i < 6037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6038(int& val) {
    const int x = 6038;
    std::cout << 6038;
    for (int i = 0; i < 6038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6039(int& val) {
    const int x = 6039;
    std::cout << 6039;
    for (int i = 0; i < 6039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6040(int& val) {
    const int x = 6040;
    std::cout << 6040;
    for (int i = 0; i < 6040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6041(int& val) {
    const int x = 6041;
    std::cout << 6041;
    for (int i = 0; i < 6041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6042(int& val) {
    const int x = 6042;
    std::cout << 6042;
    for (int i = 0; i < 6042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6043(int& val) {
    const int x = 6043;
    std::cout << 6043;
    for (int i = 0; i < 6043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6044(int& val) {
    const int x = 6044;
    std::cout << 6044;
    for (int i = 0; i < 6044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6045(int& val) {
    const int x = 6045;
    std::cout << 6045;
    for (int i = 0; i < 6045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6046(int& val) {
    const int x = 6046;
    std::cout << 6046;
    for (int i = 0; i < 6046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6047(int& val) {
    const int x = 6047;
    std::cout << 6047;
    for (int i = 0; i < 6047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6048(int& val) {
    const int x = 6048;
    std::cout << 6048;
    for (int i = 0; i < 6048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6049(int& val) {
    const int x = 6049;
    std::cout << 6049;
    for (int i = 0; i < 6049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6050(int& val) {
    const int x = 6050;
    std::cout << 6050;
    for (int i = 0; i < 6050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6051(int& val) {
    const int x = 6051;
    std::cout << 6051;
    for (int i = 0; i < 6051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6052(int& val) {
    const int x = 6052;
    std::cout << 6052;
    for (int i = 0; i < 6052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6053(int& val) {
    const int x = 6053;
    std::cout << 6053;
    for (int i = 0; i < 6053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6054(int& val) {
    const int x = 6054;
    std::cout << 6054;
    for (int i = 0; i < 6054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6055(int& val) {
    const int x = 6055;
    std::cout << 6055;
    for (int i = 0; i < 6055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6056(int& val) {
    const int x = 6056;
    std::cout << 6056;
    for (int i = 0; i < 6056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6057(int& val) {
    const int x = 6057;
    std::cout << 6057;
    for (int i = 0; i < 6057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6058(int& val) {
    const int x = 6058;
    std::cout << 6058;
    for (int i = 0; i < 6058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6059(int& val) {
    const int x = 6059;
    std::cout << 6059;
    for (int i = 0; i < 6059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6060(int& val) {
    const int x = 6060;
    std::cout << 6060;
    for (int i = 0; i < 6060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6061(int& val) {
    const int x = 6061;
    std::cout << 6061;
    for (int i = 0; i < 6061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6062(int& val) {
    const int x = 6062;
    std::cout << 6062;
    for (int i = 0; i < 6062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6063(int& val) {
    const int x = 6063;
    std::cout << 6063;
    for (int i = 0; i < 6063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6064(int& val) {
    const int x = 6064;
    std::cout << 6064;
    for (int i = 0; i < 6064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6065(int& val) {
    const int x = 6065;
    std::cout << 6065;
    for (int i = 0; i < 6065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6066(int& val) {
    const int x = 6066;
    std::cout << 6066;
    for (int i = 0; i < 6066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6067(int& val) {
    const int x = 6067;
    std::cout << 6067;
    for (int i = 0; i < 6067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6068(int& val) {
    const int x = 6068;
    std::cout << 6068;
    for (int i = 0; i < 6068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6069(int& val) {
    const int x = 6069;
    std::cout << 6069;
    for (int i = 0; i < 6069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6070(int& val) {
    const int x = 6070;
    std::cout << 6070;
    for (int i = 0; i < 6070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6071(int& val) {
    const int x = 6071;
    std::cout << 6071;
    for (int i = 0; i < 6071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6072(int& val) {
    const int x = 6072;
    std::cout << 6072;
    for (int i = 0; i < 6072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6073(int& val) {
    const int x = 6073;
    std::cout << 6073;
    for (int i = 0; i < 6073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6074(int& val) {
    const int x = 6074;
    std::cout << 6074;
    for (int i = 0; i < 6074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6075(int& val) {
    const int x = 6075;
    std::cout << 6075;
    for (int i = 0; i < 6075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6076(int& val) {
    const int x = 6076;
    std::cout << 6076;
    for (int i = 0; i < 6076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6077(int& val) {
    const int x = 6077;
    std::cout << 6077;
    for (int i = 0; i < 6077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6078(int& val) {
    const int x = 6078;
    std::cout << 6078;
    for (int i = 0; i < 6078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6079(int& val) {
    const int x = 6079;
    std::cout << 6079;
    for (int i = 0; i < 6079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6080(int& val) {
    const int x = 6080;
    std::cout << 6080;
    for (int i = 0; i < 6080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6081(int& val) {
    const int x = 6081;
    std::cout << 6081;
    for (int i = 0; i < 6081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6082(int& val) {
    const int x = 6082;
    std::cout << 6082;
    for (int i = 0; i < 6082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6083(int& val) {
    const int x = 6083;
    std::cout << 6083;
    for (int i = 0; i < 6083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6084(int& val) {
    const int x = 6084;
    std::cout << 6084;
    for (int i = 0; i < 6084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6085(int& val) {
    const int x = 6085;
    std::cout << 6085;
    for (int i = 0; i < 6085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6086(int& val) {
    const int x = 6086;
    std::cout << 6086;
    for (int i = 0; i < 6086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6087(int& val) {
    const int x = 6087;
    std::cout << 6087;
    for (int i = 0; i < 6087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6088(int& val) {
    const int x = 6088;
    std::cout << 6088;
    for (int i = 0; i < 6088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6089(int& val) {
    const int x = 6089;
    std::cout << 6089;
    for (int i = 0; i < 6089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6090(int& val) {
    const int x = 6090;
    std::cout << 6090;
    for (int i = 0; i < 6090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6091(int& val) {
    const int x = 6091;
    std::cout << 6091;
    for (int i = 0; i < 6091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6092(int& val) {
    const int x = 6092;
    std::cout << 6092;
    for (int i = 0; i < 6092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6093(int& val) {
    const int x = 6093;
    std::cout << 6093;
    for (int i = 0; i < 6093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6094(int& val) {
    const int x = 6094;
    std::cout << 6094;
    for (int i = 0; i < 6094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6095(int& val) {
    const int x = 6095;
    std::cout << 6095;
    for (int i = 0; i < 6095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6096(int& val) {
    const int x = 6096;
    std::cout << 6096;
    for (int i = 0; i < 6096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6097(int& val) {
    const int x = 6097;
    std::cout << 6097;
    for (int i = 0; i < 6097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6098(int& val) {
    const int x = 6098;
    std::cout << 6098;
    for (int i = 0; i < 6098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6099(int& val) {
    const int x = 6099;
    std::cout << 6099;
    for (int i = 0; i < 6099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6100(int& val) {
    const int x = 6100;
    std::cout << 6100;
    for (int i = 0; i < 6100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6101(int& val) {
    const int x = 6101;
    std::cout << 6101;
    for (int i = 0; i < 6101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6102(int& val) {
    const int x = 6102;
    std::cout << 6102;
    for (int i = 0; i < 6102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6103(int& val) {
    const int x = 6103;
    std::cout << 6103;
    for (int i = 0; i < 6103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6104(int& val) {
    const int x = 6104;
    std::cout << 6104;
    for (int i = 0; i < 6104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6105(int& val) {
    const int x = 6105;
    std::cout << 6105;
    for (int i = 0; i < 6105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6106(int& val) {
    const int x = 6106;
    std::cout << 6106;
    for (int i = 0; i < 6106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6107(int& val) {
    const int x = 6107;
    std::cout << 6107;
    for (int i = 0; i < 6107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6108(int& val) {
    const int x = 6108;
    std::cout << 6108;
    for (int i = 0; i < 6108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6109(int& val) {
    const int x = 6109;
    std::cout << 6109;
    for (int i = 0; i < 6109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6110(int& val) {
    const int x = 6110;
    std::cout << 6110;
    for (int i = 0; i < 6110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6111(int& val) {
    const int x = 6111;
    std::cout << 6111;
    for (int i = 0; i < 6111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6112(int& val) {
    const int x = 6112;
    std::cout << 6112;
    for (int i = 0; i < 6112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6113(int& val) {
    const int x = 6113;
    std::cout << 6113;
    for (int i = 0; i < 6113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6114(int& val) {
    const int x = 6114;
    std::cout << 6114;
    for (int i = 0; i < 6114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6115(int& val) {
    const int x = 6115;
    std::cout << 6115;
    for (int i = 0; i < 6115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6116(int& val) {
    const int x = 6116;
    std::cout << 6116;
    for (int i = 0; i < 6116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6117(int& val) {
    const int x = 6117;
    std::cout << 6117;
    for (int i = 0; i < 6117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6118(int& val) {
    const int x = 6118;
    std::cout << 6118;
    for (int i = 0; i < 6118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6119(int& val) {
    const int x = 6119;
    std::cout << 6119;
    for (int i = 0; i < 6119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6120(int& val) {
    const int x = 6120;
    std::cout << 6120;
    for (int i = 0; i < 6120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6121(int& val) {
    const int x = 6121;
    std::cout << 6121;
    for (int i = 0; i < 6121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6122(int& val) {
    const int x = 6122;
    std::cout << 6122;
    for (int i = 0; i < 6122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6123(int& val) {
    const int x = 6123;
    std::cout << 6123;
    for (int i = 0; i < 6123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6124(int& val) {
    const int x = 6124;
    std::cout << 6124;
    for (int i = 0; i < 6124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6125(int& val) {
    const int x = 6125;
    std::cout << 6125;
    for (int i = 0; i < 6125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6126(int& val) {
    const int x = 6126;
    std::cout << 6126;
    for (int i = 0; i < 6126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6127(int& val) {
    const int x = 6127;
    std::cout << 6127;
    for (int i = 0; i < 6127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6128(int& val) {
    const int x = 6128;
    std::cout << 6128;
    for (int i = 0; i < 6128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6129(int& val) {
    const int x = 6129;
    std::cout << 6129;
    for (int i = 0; i < 6129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6130(int& val) {
    const int x = 6130;
    std::cout << 6130;
    for (int i = 0; i < 6130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6131(int& val) {
    const int x = 6131;
    std::cout << 6131;
    for (int i = 0; i < 6131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6132(int& val) {
    const int x = 6132;
    std::cout << 6132;
    for (int i = 0; i < 6132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6133(int& val) {
    const int x = 6133;
    std::cout << 6133;
    for (int i = 0; i < 6133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6134(int& val) {
    const int x = 6134;
    std::cout << 6134;
    for (int i = 0; i < 6134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6135(int& val) {
    const int x = 6135;
    std::cout << 6135;
    for (int i = 0; i < 6135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6136(int& val) {
    const int x = 6136;
    std::cout << 6136;
    for (int i = 0; i < 6136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6137(int& val) {
    const int x = 6137;
    std::cout << 6137;
    for (int i = 0; i < 6137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6138(int& val) {
    const int x = 6138;
    std::cout << 6138;
    for (int i = 0; i < 6138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6139(int& val) {
    const int x = 6139;
    std::cout << 6139;
    for (int i = 0; i < 6139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6140(int& val) {
    const int x = 6140;
    std::cout << 6140;
    for (int i = 0; i < 6140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6141(int& val) {
    const int x = 6141;
    std::cout << 6141;
    for (int i = 0; i < 6141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6142(int& val) {
    const int x = 6142;
    std::cout << 6142;
    for (int i = 0; i < 6142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6143(int& val) {
    const int x = 6143;
    std::cout << 6143;
    for (int i = 0; i < 6143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6144(int& val) {
    const int x = 6144;
    std::cout << 6144;
    for (int i = 0; i < 6144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6145(int& val) {
    const int x = 6145;
    std::cout << 6145;
    for (int i = 0; i < 6145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6146(int& val) {
    const int x = 6146;
    std::cout << 6146;
    for (int i = 0; i < 6146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6147(int& val) {
    const int x = 6147;
    std::cout << 6147;
    for (int i = 0; i < 6147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6148(int& val) {
    const int x = 6148;
    std::cout << 6148;
    for (int i = 0; i < 6148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6149(int& val) {
    const int x = 6149;
    std::cout << 6149;
    for (int i = 0; i < 6149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6150(int& val) {
    const int x = 6150;
    std::cout << 6150;
    for (int i = 0; i < 6150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6151(int& val) {
    const int x = 6151;
    std::cout << 6151;
    for (int i = 0; i < 6151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6152(int& val) {
    const int x = 6152;
    std::cout << 6152;
    for (int i = 0; i < 6152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6153(int& val) {
    const int x = 6153;
    std::cout << 6153;
    for (int i = 0; i < 6153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6154(int& val) {
    const int x = 6154;
    std::cout << 6154;
    for (int i = 0; i < 6154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6155(int& val) {
    const int x = 6155;
    std::cout << 6155;
    for (int i = 0; i < 6155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6156(int& val) {
    const int x = 6156;
    std::cout << 6156;
    for (int i = 0; i < 6156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6157(int& val) {
    const int x = 6157;
    std::cout << 6157;
    for (int i = 0; i < 6157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6158(int& val) {
    const int x = 6158;
    std::cout << 6158;
    for (int i = 0; i < 6158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6159(int& val) {
    const int x = 6159;
    std::cout << 6159;
    for (int i = 0; i < 6159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6160(int& val) {
    const int x = 6160;
    std::cout << 6160;
    for (int i = 0; i < 6160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6161(int& val) {
    const int x = 6161;
    std::cout << 6161;
    for (int i = 0; i < 6161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6162(int& val) {
    const int x = 6162;
    std::cout << 6162;
    for (int i = 0; i < 6162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6163(int& val) {
    const int x = 6163;
    std::cout << 6163;
    for (int i = 0; i < 6163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6164(int& val) {
    const int x = 6164;
    std::cout << 6164;
    for (int i = 0; i < 6164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6165(int& val) {
    const int x = 6165;
    std::cout << 6165;
    for (int i = 0; i < 6165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6166(int& val) {
    const int x = 6166;
    std::cout << 6166;
    for (int i = 0; i < 6166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6167(int& val) {
    const int x = 6167;
    std::cout << 6167;
    for (int i = 0; i < 6167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6168(int& val) {
    const int x = 6168;
    std::cout << 6168;
    for (int i = 0; i < 6168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6169(int& val) {
    const int x = 6169;
    std::cout << 6169;
    for (int i = 0; i < 6169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6170(int& val) {
    const int x = 6170;
    std::cout << 6170;
    for (int i = 0; i < 6170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6171(int& val) {
    const int x = 6171;
    std::cout << 6171;
    for (int i = 0; i < 6171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6172(int& val) {
    const int x = 6172;
    std::cout << 6172;
    for (int i = 0; i < 6172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6173(int& val) {
    const int x = 6173;
    std::cout << 6173;
    for (int i = 0; i < 6173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6174(int& val) {
    const int x = 6174;
    std::cout << 6174;
    for (int i = 0; i < 6174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6175(int& val) {
    const int x = 6175;
    std::cout << 6175;
    for (int i = 0; i < 6175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6176(int& val) {
    const int x = 6176;
    std::cout << 6176;
    for (int i = 0; i < 6176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6177(int& val) {
    const int x = 6177;
    std::cout << 6177;
    for (int i = 0; i < 6177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6178(int& val) {
    const int x = 6178;
    std::cout << 6178;
    for (int i = 0; i < 6178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6179(int& val) {
    const int x = 6179;
    std::cout << 6179;
    for (int i = 0; i < 6179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6180(int& val) {
    const int x = 6180;
    std::cout << 6180;
    for (int i = 0; i < 6180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6181(int& val) {
    const int x = 6181;
    std::cout << 6181;
    for (int i = 0; i < 6181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6182(int& val) {
    const int x = 6182;
    std::cout << 6182;
    for (int i = 0; i < 6182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6183(int& val) {
    const int x = 6183;
    std::cout << 6183;
    for (int i = 0; i < 6183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6184(int& val) {
    const int x = 6184;
    std::cout << 6184;
    for (int i = 0; i < 6184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6185(int& val) {
    const int x = 6185;
    std::cout << 6185;
    for (int i = 0; i < 6185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6186(int& val) {
    const int x = 6186;
    std::cout << 6186;
    for (int i = 0; i < 6186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6187(int& val) {
    const int x = 6187;
    std::cout << 6187;
    for (int i = 0; i < 6187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6188(int& val) {
    const int x = 6188;
    std::cout << 6188;
    for (int i = 0; i < 6188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6189(int& val) {
    const int x = 6189;
    std::cout << 6189;
    for (int i = 0; i < 6189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6190(int& val) {
    const int x = 6190;
    std::cout << 6190;
    for (int i = 0; i < 6190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6191(int& val) {
    const int x = 6191;
    std::cout << 6191;
    for (int i = 0; i < 6191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6192(int& val) {
    const int x = 6192;
    std::cout << 6192;
    for (int i = 0; i < 6192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6193(int& val) {
    const int x = 6193;
    std::cout << 6193;
    for (int i = 0; i < 6193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6194(int& val) {
    const int x = 6194;
    std::cout << 6194;
    for (int i = 0; i < 6194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6195(int& val) {
    const int x = 6195;
    std::cout << 6195;
    for (int i = 0; i < 6195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6196(int& val) {
    const int x = 6196;
    std::cout << 6196;
    for (int i = 0; i < 6196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6197(int& val) {
    const int x = 6197;
    std::cout << 6197;
    for (int i = 0; i < 6197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6198(int& val) {
    const int x = 6198;
    std::cout << 6198;
    for (int i = 0; i < 6198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6199(int& val) {
    const int x = 6199;
    std::cout << 6199;
    for (int i = 0; i < 6199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6200(int& val) {
    const int x = 6200;
    std::cout << 6200;
    for (int i = 0; i < 6200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6201(int& val) {
    const int x = 6201;
    std::cout << 6201;
    for (int i = 0; i < 6201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6202(int& val) {
    const int x = 6202;
    std::cout << 6202;
    for (int i = 0; i < 6202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6203(int& val) {
    const int x = 6203;
    std::cout << 6203;
    for (int i = 0; i < 6203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6204(int& val) {
    const int x = 6204;
    std::cout << 6204;
    for (int i = 0; i < 6204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6205(int& val) {
    const int x = 6205;
    std::cout << 6205;
    for (int i = 0; i < 6205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6206(int& val) {
    const int x = 6206;
    std::cout << 6206;
    for (int i = 0; i < 6206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6207(int& val) {
    const int x = 6207;
    std::cout << 6207;
    for (int i = 0; i < 6207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6208(int& val) {
    const int x = 6208;
    std::cout << 6208;
    for (int i = 0; i < 6208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6209(int& val) {
    const int x = 6209;
    std::cout << 6209;
    for (int i = 0; i < 6209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6210(int& val) {
    const int x = 6210;
    std::cout << 6210;
    for (int i = 0; i < 6210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6211(int& val) {
    const int x = 6211;
    std::cout << 6211;
    for (int i = 0; i < 6211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6212(int& val) {
    const int x = 6212;
    std::cout << 6212;
    for (int i = 0; i < 6212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6213(int& val) {
    const int x = 6213;
    std::cout << 6213;
    for (int i = 0; i < 6213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6214(int& val) {
    const int x = 6214;
    std::cout << 6214;
    for (int i = 0; i < 6214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6215(int& val) {
    const int x = 6215;
    std::cout << 6215;
    for (int i = 0; i < 6215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6216(int& val) {
    const int x = 6216;
    std::cout << 6216;
    for (int i = 0; i < 6216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6217(int& val) {
    const int x = 6217;
    std::cout << 6217;
    for (int i = 0; i < 6217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6218(int& val) {
    const int x = 6218;
    std::cout << 6218;
    for (int i = 0; i < 6218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6219(int& val) {
    const int x = 6219;
    std::cout << 6219;
    for (int i = 0; i < 6219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6220(int& val) {
    const int x = 6220;
    std::cout << 6220;
    for (int i = 0; i < 6220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6221(int& val) {
    const int x = 6221;
    std::cout << 6221;
    for (int i = 0; i < 6221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6222(int& val) {
    const int x = 6222;
    std::cout << 6222;
    for (int i = 0; i < 6222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6223(int& val) {
    const int x = 6223;
    std::cout << 6223;
    for (int i = 0; i < 6223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6224(int& val) {
    const int x = 6224;
    std::cout << 6224;
    for (int i = 0; i < 6224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6225(int& val) {
    const int x = 6225;
    std::cout << 6225;
    for (int i = 0; i < 6225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6226(int& val) {
    const int x = 6226;
    std::cout << 6226;
    for (int i = 0; i < 6226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6227(int& val) {
    const int x = 6227;
    std::cout << 6227;
    for (int i = 0; i < 6227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6228(int& val) {
    const int x = 6228;
    std::cout << 6228;
    for (int i = 0; i < 6228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6229(int& val) {
    const int x = 6229;
    std::cout << 6229;
    for (int i = 0; i < 6229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6230(int& val) {
    const int x = 6230;
    std::cout << 6230;
    for (int i = 0; i < 6230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6231(int& val) {
    const int x = 6231;
    std::cout << 6231;
    for (int i = 0; i < 6231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6232(int& val) {
    const int x = 6232;
    std::cout << 6232;
    for (int i = 0; i < 6232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6233(int& val) {
    const int x = 6233;
    std::cout << 6233;
    for (int i = 0; i < 6233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6234(int& val) {
    const int x = 6234;
    std::cout << 6234;
    for (int i = 0; i < 6234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6235(int& val) {
    const int x = 6235;
    std::cout << 6235;
    for (int i = 0; i < 6235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6236(int& val) {
    const int x = 6236;
    std::cout << 6236;
    for (int i = 0; i < 6236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6237(int& val) {
    const int x = 6237;
    std::cout << 6237;
    for (int i = 0; i < 6237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6238(int& val) {
    const int x = 6238;
    std::cout << 6238;
    for (int i = 0; i < 6238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6239(int& val) {
    const int x = 6239;
    std::cout << 6239;
    for (int i = 0; i < 6239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6240(int& val) {
    const int x = 6240;
    std::cout << 6240;
    for (int i = 0; i < 6240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6241(int& val) {
    const int x = 6241;
    std::cout << 6241;
    for (int i = 0; i < 6241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6242(int& val) {
    const int x = 6242;
    std::cout << 6242;
    for (int i = 0; i < 6242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6243(int& val) {
    const int x = 6243;
    std::cout << 6243;
    for (int i = 0; i < 6243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6244(int& val) {
    const int x = 6244;
    std::cout << 6244;
    for (int i = 0; i < 6244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6245(int& val) {
    const int x = 6245;
    std::cout << 6245;
    for (int i = 0; i < 6245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6246(int& val) {
    const int x = 6246;
    std::cout << 6246;
    for (int i = 0; i < 6246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6247(int& val) {
    const int x = 6247;
    std::cout << 6247;
    for (int i = 0; i < 6247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6248(int& val) {
    const int x = 6248;
    std::cout << 6248;
    for (int i = 0; i < 6248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6249(int& val) {
    const int x = 6249;
    std::cout << 6249;
    for (int i = 0; i < 6249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6250(int& val) {
    const int x = 6250;
    std::cout << 6250;
    for (int i = 0; i < 6250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6251(int& val) {
    const int x = 6251;
    std::cout << 6251;
    for (int i = 0; i < 6251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6252(int& val) {
    const int x = 6252;
    std::cout << 6252;
    for (int i = 0; i < 6252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6253(int& val) {
    const int x = 6253;
    std::cout << 6253;
    for (int i = 0; i < 6253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6254(int& val) {
    const int x = 6254;
    std::cout << 6254;
    for (int i = 0; i < 6254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6255(int& val) {
    const int x = 6255;
    std::cout << 6255;
    for (int i = 0; i < 6255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6256(int& val) {
    const int x = 6256;
    std::cout << 6256;
    for (int i = 0; i < 6256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6257(int& val) {
    const int x = 6257;
    std::cout << 6257;
    for (int i = 0; i < 6257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6258(int& val) {
    const int x = 6258;
    std::cout << 6258;
    for (int i = 0; i < 6258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6259(int& val) {
    const int x = 6259;
    std::cout << 6259;
    for (int i = 0; i < 6259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6260(int& val) {
    const int x = 6260;
    std::cout << 6260;
    for (int i = 0; i < 6260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6261(int& val) {
    const int x = 6261;
    std::cout << 6261;
    for (int i = 0; i < 6261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6262(int& val) {
    const int x = 6262;
    std::cout << 6262;
    for (int i = 0; i < 6262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6263(int& val) {
    const int x = 6263;
    std::cout << 6263;
    for (int i = 0; i < 6263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6264(int& val) {
    const int x = 6264;
    std::cout << 6264;
    for (int i = 0; i < 6264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6265(int& val) {
    const int x = 6265;
    std::cout << 6265;
    for (int i = 0; i < 6265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6266(int& val) {
    const int x = 6266;
    std::cout << 6266;
    for (int i = 0; i < 6266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6267(int& val) {
    const int x = 6267;
    std::cout << 6267;
    for (int i = 0; i < 6267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6268(int& val) {
    const int x = 6268;
    std::cout << 6268;
    for (int i = 0; i < 6268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6269(int& val) {
    const int x = 6269;
    std::cout << 6269;
    for (int i = 0; i < 6269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6270(int& val) {
    const int x = 6270;
    std::cout << 6270;
    for (int i = 0; i < 6270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6271(int& val) {
    const int x = 6271;
    std::cout << 6271;
    for (int i = 0; i < 6271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6272(int& val) {
    const int x = 6272;
    std::cout << 6272;
    for (int i = 0; i < 6272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6273(int& val) {
    const int x = 6273;
    std::cout << 6273;
    for (int i = 0; i < 6273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6274(int& val) {
    const int x = 6274;
    std::cout << 6274;
    for (int i = 0; i < 6274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6275(int& val) {
    const int x = 6275;
    std::cout << 6275;
    for (int i = 0; i < 6275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6276(int& val) {
    const int x = 6276;
    std::cout << 6276;
    for (int i = 0; i < 6276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6277(int& val) {
    const int x = 6277;
    std::cout << 6277;
    for (int i = 0; i < 6277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6278(int& val) {
    const int x = 6278;
    std::cout << 6278;
    for (int i = 0; i < 6278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6279(int& val) {
    const int x = 6279;
    std::cout << 6279;
    for (int i = 0; i < 6279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6280(int& val) {
    const int x = 6280;
    std::cout << 6280;
    for (int i = 0; i < 6280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6281(int& val) {
    const int x = 6281;
    std::cout << 6281;
    for (int i = 0; i < 6281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6282(int& val) {
    const int x = 6282;
    std::cout << 6282;
    for (int i = 0; i < 6282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6283(int& val) {
    const int x = 6283;
    std::cout << 6283;
    for (int i = 0; i < 6283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6284(int& val) {
    const int x = 6284;
    std::cout << 6284;
    for (int i = 0; i < 6284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6285(int& val) {
    const int x = 6285;
    std::cout << 6285;
    for (int i = 0; i < 6285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6286(int& val) {
    const int x = 6286;
    std::cout << 6286;
    for (int i = 0; i < 6286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6287(int& val) {
    const int x = 6287;
    std::cout << 6287;
    for (int i = 0; i < 6287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6288(int& val) {
    const int x = 6288;
    std::cout << 6288;
    for (int i = 0; i < 6288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6289(int& val) {
    const int x = 6289;
    std::cout << 6289;
    for (int i = 0; i < 6289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6290(int& val) {
    const int x = 6290;
    std::cout << 6290;
    for (int i = 0; i < 6290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6291(int& val) {
    const int x = 6291;
    std::cout << 6291;
    for (int i = 0; i < 6291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6292(int& val) {
    const int x = 6292;
    std::cout << 6292;
    for (int i = 0; i < 6292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6293(int& val) {
    const int x = 6293;
    std::cout << 6293;
    for (int i = 0; i < 6293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6294(int& val) {
    const int x = 6294;
    std::cout << 6294;
    for (int i = 0; i < 6294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6295(int& val) {
    const int x = 6295;
    std::cout << 6295;
    for (int i = 0; i < 6295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6296(int& val) {
    const int x = 6296;
    std::cout << 6296;
    for (int i = 0; i < 6296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6297(int& val) {
    const int x = 6297;
    std::cout << 6297;
    for (int i = 0; i < 6297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6298(int& val) {
    const int x = 6298;
    std::cout << 6298;
    for (int i = 0; i < 6298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6299(int& val) {
    const int x = 6299;
    std::cout << 6299;
    for (int i = 0; i < 6299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6300(int& val) {
    const int x = 6300;
    std::cout << 6300;
    for (int i = 0; i < 6300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6301(int& val) {
    const int x = 6301;
    std::cout << 6301;
    for (int i = 0; i < 6301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6302(int& val) {
    const int x = 6302;
    std::cout << 6302;
    for (int i = 0; i < 6302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6303(int& val) {
    const int x = 6303;
    std::cout << 6303;
    for (int i = 0; i < 6303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6304(int& val) {
    const int x = 6304;
    std::cout << 6304;
    for (int i = 0; i < 6304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6305(int& val) {
    const int x = 6305;
    std::cout << 6305;
    for (int i = 0; i < 6305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6306(int& val) {
    const int x = 6306;
    std::cout << 6306;
    for (int i = 0; i < 6306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6307(int& val) {
    const int x = 6307;
    std::cout << 6307;
    for (int i = 0; i < 6307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6308(int& val) {
    const int x = 6308;
    std::cout << 6308;
    for (int i = 0; i < 6308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6309(int& val) {
    const int x = 6309;
    std::cout << 6309;
    for (int i = 0; i < 6309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6310(int& val) {
    const int x = 6310;
    std::cout << 6310;
    for (int i = 0; i < 6310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6311(int& val) {
    const int x = 6311;
    std::cout << 6311;
    for (int i = 0; i < 6311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6312(int& val) {
    const int x = 6312;
    std::cout << 6312;
    for (int i = 0; i < 6312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6313(int& val) {
    const int x = 6313;
    std::cout << 6313;
    for (int i = 0; i < 6313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6314(int& val) {
    const int x = 6314;
    std::cout << 6314;
    for (int i = 0; i < 6314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6315(int& val) {
    const int x = 6315;
    std::cout << 6315;
    for (int i = 0; i < 6315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6316(int& val) {
    const int x = 6316;
    std::cout << 6316;
    for (int i = 0; i < 6316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6317(int& val) {
    const int x = 6317;
    std::cout << 6317;
    for (int i = 0; i < 6317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6318(int& val) {
    const int x = 6318;
    std::cout << 6318;
    for (int i = 0; i < 6318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6319(int& val) {
    const int x = 6319;
    std::cout << 6319;
    for (int i = 0; i < 6319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6320(int& val) {
    const int x = 6320;
    std::cout << 6320;
    for (int i = 0; i < 6320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6321(int& val) {
    const int x = 6321;
    std::cout << 6321;
    for (int i = 0; i < 6321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6322(int& val) {
    const int x = 6322;
    std::cout << 6322;
    for (int i = 0; i < 6322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6323(int& val) {
    const int x = 6323;
    std::cout << 6323;
    for (int i = 0; i < 6323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6324(int& val) {
    const int x = 6324;
    std::cout << 6324;
    for (int i = 0; i < 6324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6325(int& val) {
    const int x = 6325;
    std::cout << 6325;
    for (int i = 0; i < 6325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6326(int& val) {
    const int x = 6326;
    std::cout << 6326;
    for (int i = 0; i < 6326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6327(int& val) {
    const int x = 6327;
    std::cout << 6327;
    for (int i = 0; i < 6327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6328(int& val) {
    const int x = 6328;
    std::cout << 6328;
    for (int i = 0; i < 6328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6329(int& val) {
    const int x = 6329;
    std::cout << 6329;
    for (int i = 0; i < 6329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6330(int& val) {
    const int x = 6330;
    std::cout << 6330;
    for (int i = 0; i < 6330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6331(int& val) {
    const int x = 6331;
    std::cout << 6331;
    for (int i = 0; i < 6331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6332(int& val) {
    const int x = 6332;
    std::cout << 6332;
    for (int i = 0; i < 6332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6333(int& val) {
    const int x = 6333;
    std::cout << 6333;
    for (int i = 0; i < 6333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6334(int& val) {
    const int x = 6334;
    std::cout << 6334;
    for (int i = 0; i < 6334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6335(int& val) {
    const int x = 6335;
    std::cout << 6335;
    for (int i = 0; i < 6335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6336(int& val) {
    const int x = 6336;
    std::cout << 6336;
    for (int i = 0; i < 6336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6337(int& val) {
    const int x = 6337;
    std::cout << 6337;
    for (int i = 0; i < 6337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6338(int& val) {
    const int x = 6338;
    std::cout << 6338;
    for (int i = 0; i < 6338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6339(int& val) {
    const int x = 6339;
    std::cout << 6339;
    for (int i = 0; i < 6339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6340(int& val) {
    const int x = 6340;
    std::cout << 6340;
    for (int i = 0; i < 6340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6341(int& val) {
    const int x = 6341;
    std::cout << 6341;
    for (int i = 0; i < 6341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6342(int& val) {
    const int x = 6342;
    std::cout << 6342;
    for (int i = 0; i < 6342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6343(int& val) {
    const int x = 6343;
    std::cout << 6343;
    for (int i = 0; i < 6343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6344(int& val) {
    const int x = 6344;
    std::cout << 6344;
    for (int i = 0; i < 6344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6345(int& val) {
    const int x = 6345;
    std::cout << 6345;
    for (int i = 0; i < 6345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6346(int& val) {
    const int x = 6346;
    std::cout << 6346;
    for (int i = 0; i < 6346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6347(int& val) {
    const int x = 6347;
    std::cout << 6347;
    for (int i = 0; i < 6347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6348(int& val) {
    const int x = 6348;
    std::cout << 6348;
    for (int i = 0; i < 6348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6349(int& val) {
    const int x = 6349;
    std::cout << 6349;
    for (int i = 0; i < 6349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6350(int& val) {
    const int x = 6350;
    std::cout << 6350;
    for (int i = 0; i < 6350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6351(int& val) {
    const int x = 6351;
    std::cout << 6351;
    for (int i = 0; i < 6351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6352(int& val) {
    const int x = 6352;
    std::cout << 6352;
    for (int i = 0; i < 6352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6353(int& val) {
    const int x = 6353;
    std::cout << 6353;
    for (int i = 0; i < 6353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6354(int& val) {
    const int x = 6354;
    std::cout << 6354;
    for (int i = 0; i < 6354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6355(int& val) {
    const int x = 6355;
    std::cout << 6355;
    for (int i = 0; i < 6355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6356(int& val) {
    const int x = 6356;
    std::cout << 6356;
    for (int i = 0; i < 6356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6357(int& val) {
    const int x = 6357;
    std::cout << 6357;
    for (int i = 0; i < 6357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6358(int& val) {
    const int x = 6358;
    std::cout << 6358;
    for (int i = 0; i < 6358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6359(int& val) {
    const int x = 6359;
    std::cout << 6359;
    for (int i = 0; i < 6359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6360(int& val) {
    const int x = 6360;
    std::cout << 6360;
    for (int i = 0; i < 6360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6361(int& val) {
    const int x = 6361;
    std::cout << 6361;
    for (int i = 0; i < 6361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6362(int& val) {
    const int x = 6362;
    std::cout << 6362;
    for (int i = 0; i < 6362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6363(int& val) {
    const int x = 6363;
    std::cout << 6363;
    for (int i = 0; i < 6363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6364(int& val) {
    const int x = 6364;
    std::cout << 6364;
    for (int i = 0; i < 6364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6365(int& val) {
    const int x = 6365;
    std::cout << 6365;
    for (int i = 0; i < 6365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6366(int& val) {
    const int x = 6366;
    std::cout << 6366;
    for (int i = 0; i < 6366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6367(int& val) {
    const int x = 6367;
    std::cout << 6367;
    for (int i = 0; i < 6367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6368(int& val) {
    const int x = 6368;
    std::cout << 6368;
    for (int i = 0; i < 6368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6369(int& val) {
    const int x = 6369;
    std::cout << 6369;
    for (int i = 0; i < 6369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6370(int& val) {
    const int x = 6370;
    std::cout << 6370;
    for (int i = 0; i < 6370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6371(int& val) {
    const int x = 6371;
    std::cout << 6371;
    for (int i = 0; i < 6371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6372(int& val) {
    const int x = 6372;
    std::cout << 6372;
    for (int i = 0; i < 6372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6373(int& val) {
    const int x = 6373;
    std::cout << 6373;
    for (int i = 0; i < 6373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6374(int& val) {
    const int x = 6374;
    std::cout << 6374;
    for (int i = 0; i < 6374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6375(int& val) {
    const int x = 6375;
    std::cout << 6375;
    for (int i = 0; i < 6375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6376(int& val) {
    const int x = 6376;
    std::cout << 6376;
    for (int i = 0; i < 6376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6377(int& val) {
    const int x = 6377;
    std::cout << 6377;
    for (int i = 0; i < 6377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6378(int& val) {
    const int x = 6378;
    std::cout << 6378;
    for (int i = 0; i < 6378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6379(int& val) {
    const int x = 6379;
    std::cout << 6379;
    for (int i = 0; i < 6379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6380(int& val) {
    const int x = 6380;
    std::cout << 6380;
    for (int i = 0; i < 6380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6381(int& val) {
    const int x = 6381;
    std::cout << 6381;
    for (int i = 0; i < 6381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6382(int& val) {
    const int x = 6382;
    std::cout << 6382;
    for (int i = 0; i < 6382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6383(int& val) {
    const int x = 6383;
    std::cout << 6383;
    for (int i = 0; i < 6383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6384(int& val) {
    const int x = 6384;
    std::cout << 6384;
    for (int i = 0; i < 6384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6385(int& val) {
    const int x = 6385;
    std::cout << 6385;
    for (int i = 0; i < 6385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6386(int& val) {
    const int x = 6386;
    std::cout << 6386;
    for (int i = 0; i < 6386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6387(int& val) {
    const int x = 6387;
    std::cout << 6387;
    for (int i = 0; i < 6387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6388(int& val) {
    const int x = 6388;
    std::cout << 6388;
    for (int i = 0; i < 6388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6389(int& val) {
    const int x = 6389;
    std::cout << 6389;
    for (int i = 0; i < 6389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6390(int& val) {
    const int x = 6390;
    std::cout << 6390;
    for (int i = 0; i < 6390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6391(int& val) {
    const int x = 6391;
    std::cout << 6391;
    for (int i = 0; i < 6391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6392(int& val) {
    const int x = 6392;
    std::cout << 6392;
    for (int i = 0; i < 6392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6393(int& val) {
    const int x = 6393;
    std::cout << 6393;
    for (int i = 0; i < 6393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6394(int& val) {
    const int x = 6394;
    std::cout << 6394;
    for (int i = 0; i < 6394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6395(int& val) {
    const int x = 6395;
    std::cout << 6395;
    for (int i = 0; i < 6395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6396(int& val) {
    const int x = 6396;
    std::cout << 6396;
    for (int i = 0; i < 6396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6397(int& val) {
    const int x = 6397;
    std::cout << 6397;
    for (int i = 0; i < 6397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6398(int& val) {
    const int x = 6398;
    std::cout << 6398;
    for (int i = 0; i < 6398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6399(int& val) {
    const int x = 6399;
    std::cout << 6399;
    for (int i = 0; i < 6399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6400(int& val) {
    const int x = 6400;
    std::cout << 6400;
    for (int i = 0; i < 6400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6401(int& val) {
    const int x = 6401;
    std::cout << 6401;
    for (int i = 0; i < 6401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6402(int& val) {
    const int x = 6402;
    std::cout << 6402;
    for (int i = 0; i < 6402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6403(int& val) {
    const int x = 6403;
    std::cout << 6403;
    for (int i = 0; i < 6403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6404(int& val) {
    const int x = 6404;
    std::cout << 6404;
    for (int i = 0; i < 6404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6405(int& val) {
    const int x = 6405;
    std::cout << 6405;
    for (int i = 0; i < 6405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6406(int& val) {
    const int x = 6406;
    std::cout << 6406;
    for (int i = 0; i < 6406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6407(int& val) {
    const int x = 6407;
    std::cout << 6407;
    for (int i = 0; i < 6407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6408(int& val) {
    const int x = 6408;
    std::cout << 6408;
    for (int i = 0; i < 6408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6409(int& val) {
    const int x = 6409;
    std::cout << 6409;
    for (int i = 0; i < 6409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6410(int& val) {
    const int x = 6410;
    std::cout << 6410;
    for (int i = 0; i < 6410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6411(int& val) {
    const int x = 6411;
    std::cout << 6411;
    for (int i = 0; i < 6411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6412(int& val) {
    const int x = 6412;
    std::cout << 6412;
    for (int i = 0; i < 6412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6413(int& val) {
    const int x = 6413;
    std::cout << 6413;
    for (int i = 0; i < 6413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6414(int& val) {
    const int x = 6414;
    std::cout << 6414;
    for (int i = 0; i < 6414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6415(int& val) {
    const int x = 6415;
    std::cout << 6415;
    for (int i = 0; i < 6415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6416(int& val) {
    const int x = 6416;
    std::cout << 6416;
    for (int i = 0; i < 6416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6417(int& val) {
    const int x = 6417;
    std::cout << 6417;
    for (int i = 0; i < 6417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6418(int& val) {
    const int x = 6418;
    std::cout << 6418;
    for (int i = 0; i < 6418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6419(int& val) {
    const int x = 6419;
    std::cout << 6419;
    for (int i = 0; i < 6419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6420(int& val) {
    const int x = 6420;
    std::cout << 6420;
    for (int i = 0; i < 6420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6421(int& val) {
    const int x = 6421;
    std::cout << 6421;
    for (int i = 0; i < 6421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6422(int& val) {
    const int x = 6422;
    std::cout << 6422;
    for (int i = 0; i < 6422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6423(int& val) {
    const int x = 6423;
    std::cout << 6423;
    for (int i = 0; i < 6423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6424(int& val) {
    const int x = 6424;
    std::cout << 6424;
    for (int i = 0; i < 6424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6425(int& val) {
    const int x = 6425;
    std::cout << 6425;
    for (int i = 0; i < 6425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6426(int& val) {
    const int x = 6426;
    std::cout << 6426;
    for (int i = 0; i < 6426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6427(int& val) {
    const int x = 6427;
    std::cout << 6427;
    for (int i = 0; i < 6427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6428(int& val) {
    const int x = 6428;
    std::cout << 6428;
    for (int i = 0; i < 6428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6429(int& val) {
    const int x = 6429;
    std::cout << 6429;
    for (int i = 0; i < 6429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6430(int& val) {
    const int x = 6430;
    std::cout << 6430;
    for (int i = 0; i < 6430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6431(int& val) {
    const int x = 6431;
    std::cout << 6431;
    for (int i = 0; i < 6431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6432(int& val) {
    const int x = 6432;
    std::cout << 6432;
    for (int i = 0; i < 6432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6433(int& val) {
    const int x = 6433;
    std::cout << 6433;
    for (int i = 0; i < 6433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6434(int& val) {
    const int x = 6434;
    std::cout << 6434;
    for (int i = 0; i < 6434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6435(int& val) {
    const int x = 6435;
    std::cout << 6435;
    for (int i = 0; i < 6435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6436(int& val) {
    const int x = 6436;
    std::cout << 6436;
    for (int i = 0; i < 6436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6437(int& val) {
    const int x = 6437;
    std::cout << 6437;
    for (int i = 0; i < 6437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6438(int& val) {
    const int x = 6438;
    std::cout << 6438;
    for (int i = 0; i < 6438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6439(int& val) {
    const int x = 6439;
    std::cout << 6439;
    for (int i = 0; i < 6439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6440(int& val) {
    const int x = 6440;
    std::cout << 6440;
    for (int i = 0; i < 6440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6441(int& val) {
    const int x = 6441;
    std::cout << 6441;
    for (int i = 0; i < 6441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6442(int& val) {
    const int x = 6442;
    std::cout << 6442;
    for (int i = 0; i < 6442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6443(int& val) {
    const int x = 6443;
    std::cout << 6443;
    for (int i = 0; i < 6443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6444(int& val) {
    const int x = 6444;
    std::cout << 6444;
    for (int i = 0; i < 6444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6445(int& val) {
    const int x = 6445;
    std::cout << 6445;
    for (int i = 0; i < 6445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6446(int& val) {
    const int x = 6446;
    std::cout << 6446;
    for (int i = 0; i < 6446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6447(int& val) {
    const int x = 6447;
    std::cout << 6447;
    for (int i = 0; i < 6447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6448(int& val) {
    const int x = 6448;
    std::cout << 6448;
    for (int i = 0; i < 6448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6449(int& val) {
    const int x = 6449;
    std::cout << 6449;
    for (int i = 0; i < 6449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6450(int& val) {
    const int x = 6450;
    std::cout << 6450;
    for (int i = 0; i < 6450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6451(int& val) {
    const int x = 6451;
    std::cout << 6451;
    for (int i = 0; i < 6451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6452(int& val) {
    const int x = 6452;
    std::cout << 6452;
    for (int i = 0; i < 6452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6453(int& val) {
    const int x = 6453;
    std::cout << 6453;
    for (int i = 0; i < 6453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6454(int& val) {
    const int x = 6454;
    std::cout << 6454;
    for (int i = 0; i < 6454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6455(int& val) {
    const int x = 6455;
    std::cout << 6455;
    for (int i = 0; i < 6455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6456(int& val) {
    const int x = 6456;
    std::cout << 6456;
    for (int i = 0; i < 6456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6457(int& val) {
    const int x = 6457;
    std::cout << 6457;
    for (int i = 0; i < 6457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6458(int& val) {
    const int x = 6458;
    std::cout << 6458;
    for (int i = 0; i < 6458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6459(int& val) {
    const int x = 6459;
    std::cout << 6459;
    for (int i = 0; i < 6459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6460(int& val) {
    const int x = 6460;
    std::cout << 6460;
    for (int i = 0; i < 6460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6461(int& val) {
    const int x = 6461;
    std::cout << 6461;
    for (int i = 0; i < 6461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6462(int& val) {
    const int x = 6462;
    std::cout << 6462;
    for (int i = 0; i < 6462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6463(int& val) {
    const int x = 6463;
    std::cout << 6463;
    for (int i = 0; i < 6463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6464(int& val) {
    const int x = 6464;
    std::cout << 6464;
    for (int i = 0; i < 6464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6465(int& val) {
    const int x = 6465;
    std::cout << 6465;
    for (int i = 0; i < 6465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6466(int& val) {
    const int x = 6466;
    std::cout << 6466;
    for (int i = 0; i < 6466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6467(int& val) {
    const int x = 6467;
    std::cout << 6467;
    for (int i = 0; i < 6467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6468(int& val) {
    const int x = 6468;
    std::cout << 6468;
    for (int i = 0; i < 6468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6469(int& val) {
    const int x = 6469;
    std::cout << 6469;
    for (int i = 0; i < 6469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6470(int& val) {
    const int x = 6470;
    std::cout << 6470;
    for (int i = 0; i < 6470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6471(int& val) {
    const int x = 6471;
    std::cout << 6471;
    for (int i = 0; i < 6471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6472(int& val) {
    const int x = 6472;
    std::cout << 6472;
    for (int i = 0; i < 6472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6473(int& val) {
    const int x = 6473;
    std::cout << 6473;
    for (int i = 0; i < 6473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6474(int& val) {
    const int x = 6474;
    std::cout << 6474;
    for (int i = 0; i < 6474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6475(int& val) {
    const int x = 6475;
    std::cout << 6475;
    for (int i = 0; i < 6475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6476(int& val) {
    const int x = 6476;
    std::cout << 6476;
    for (int i = 0; i < 6476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6477(int& val) {
    const int x = 6477;
    std::cout << 6477;
    for (int i = 0; i < 6477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6478(int& val) {
    const int x = 6478;
    std::cout << 6478;
    for (int i = 0; i < 6478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6479(int& val) {
    const int x = 6479;
    std::cout << 6479;
    for (int i = 0; i < 6479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6480(int& val) {
    const int x = 6480;
    std::cout << 6480;
    for (int i = 0; i < 6480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6481(int& val) {
    const int x = 6481;
    std::cout << 6481;
    for (int i = 0; i < 6481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6482(int& val) {
    const int x = 6482;
    std::cout << 6482;
    for (int i = 0; i < 6482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6483(int& val) {
    const int x = 6483;
    std::cout << 6483;
    for (int i = 0; i < 6483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6484(int& val) {
    const int x = 6484;
    std::cout << 6484;
    for (int i = 0; i < 6484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6485(int& val) {
    const int x = 6485;
    std::cout << 6485;
    for (int i = 0; i < 6485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6486(int& val) {
    const int x = 6486;
    std::cout << 6486;
    for (int i = 0; i < 6486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6487(int& val) {
    const int x = 6487;
    std::cout << 6487;
    for (int i = 0; i < 6487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6488(int& val) {
    const int x = 6488;
    std::cout << 6488;
    for (int i = 0; i < 6488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6489(int& val) {
    const int x = 6489;
    std::cout << 6489;
    for (int i = 0; i < 6489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6490(int& val) {
    const int x = 6490;
    std::cout << 6490;
    for (int i = 0; i < 6490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6491(int& val) {
    const int x = 6491;
    std::cout << 6491;
    for (int i = 0; i < 6491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6492(int& val) {
    const int x = 6492;
    std::cout << 6492;
    for (int i = 0; i < 6492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6493(int& val) {
    const int x = 6493;
    std::cout << 6493;
    for (int i = 0; i < 6493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6494(int& val) {
    const int x = 6494;
    std::cout << 6494;
    for (int i = 0; i < 6494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6495(int& val) {
    const int x = 6495;
    std::cout << 6495;
    for (int i = 0; i < 6495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6496(int& val) {
    const int x = 6496;
    std::cout << 6496;
    for (int i = 0; i < 6496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6497(int& val) {
    const int x = 6497;
    std::cout << 6497;
    for (int i = 0; i < 6497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6498(int& val) {
    const int x = 6498;
    std::cout << 6498;
    for (int i = 0; i < 6498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6499(int& val) {
    const int x = 6499;
    std::cout << 6499;
    for (int i = 0; i < 6499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6500(int& val) {
    const int x = 6500;
    std::cout << 6500;
    for (int i = 0; i < 6500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6501(int& val) {
    const int x = 6501;
    std::cout << 6501;
    for (int i = 0; i < 6501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6502(int& val) {
    const int x = 6502;
    std::cout << 6502;
    for (int i = 0; i < 6502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6503(int& val) {
    const int x = 6503;
    std::cout << 6503;
    for (int i = 0; i < 6503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6504(int& val) {
    const int x = 6504;
    std::cout << 6504;
    for (int i = 0; i < 6504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6505(int& val) {
    const int x = 6505;
    std::cout << 6505;
    for (int i = 0; i < 6505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6506(int& val) {
    const int x = 6506;
    std::cout << 6506;
    for (int i = 0; i < 6506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6507(int& val) {
    const int x = 6507;
    std::cout << 6507;
    for (int i = 0; i < 6507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6508(int& val) {
    const int x = 6508;
    std::cout << 6508;
    for (int i = 0; i < 6508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6509(int& val) {
    const int x = 6509;
    std::cout << 6509;
    for (int i = 0; i < 6509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6510(int& val) {
    const int x = 6510;
    std::cout << 6510;
    for (int i = 0; i < 6510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6511(int& val) {
    const int x = 6511;
    std::cout << 6511;
    for (int i = 0; i < 6511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6512(int& val) {
    const int x = 6512;
    std::cout << 6512;
    for (int i = 0; i < 6512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6513(int& val) {
    const int x = 6513;
    std::cout << 6513;
    for (int i = 0; i < 6513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6514(int& val) {
    const int x = 6514;
    std::cout << 6514;
    for (int i = 0; i < 6514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6515(int& val) {
    const int x = 6515;
    std::cout << 6515;
    for (int i = 0; i < 6515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6516(int& val) {
    const int x = 6516;
    std::cout << 6516;
    for (int i = 0; i < 6516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6517(int& val) {
    const int x = 6517;
    std::cout << 6517;
    for (int i = 0; i < 6517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6518(int& val) {
    const int x = 6518;
    std::cout << 6518;
    for (int i = 0; i < 6518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6519(int& val) {
    const int x = 6519;
    std::cout << 6519;
    for (int i = 0; i < 6519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6520(int& val) {
    const int x = 6520;
    std::cout << 6520;
    for (int i = 0; i < 6520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6521(int& val) {
    const int x = 6521;
    std::cout << 6521;
    for (int i = 0; i < 6521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6522(int& val) {
    const int x = 6522;
    std::cout << 6522;
    for (int i = 0; i < 6522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6523(int& val) {
    const int x = 6523;
    std::cout << 6523;
    for (int i = 0; i < 6523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6524(int& val) {
    const int x = 6524;
    std::cout << 6524;
    for (int i = 0; i < 6524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6525(int& val) {
    const int x = 6525;
    std::cout << 6525;
    for (int i = 0; i < 6525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6526(int& val) {
    const int x = 6526;
    std::cout << 6526;
    for (int i = 0; i < 6526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6527(int& val) {
    const int x = 6527;
    std::cout << 6527;
    for (int i = 0; i < 6527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6528(int& val) {
    const int x = 6528;
    std::cout << 6528;
    for (int i = 0; i < 6528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6529(int& val) {
    const int x = 6529;
    std::cout << 6529;
    for (int i = 0; i < 6529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6530(int& val) {
    const int x = 6530;
    std::cout << 6530;
    for (int i = 0; i < 6530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6531(int& val) {
    const int x = 6531;
    std::cout << 6531;
    for (int i = 0; i < 6531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6532(int& val) {
    const int x = 6532;
    std::cout << 6532;
    for (int i = 0; i < 6532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6533(int& val) {
    const int x = 6533;
    std::cout << 6533;
    for (int i = 0; i < 6533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6534(int& val) {
    const int x = 6534;
    std::cout << 6534;
    for (int i = 0; i < 6534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6535(int& val) {
    const int x = 6535;
    std::cout << 6535;
    for (int i = 0; i < 6535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6536(int& val) {
    const int x = 6536;
    std::cout << 6536;
    for (int i = 0; i < 6536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6537(int& val) {
    const int x = 6537;
    std::cout << 6537;
    for (int i = 0; i < 6537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6538(int& val) {
    const int x = 6538;
    std::cout << 6538;
    for (int i = 0; i < 6538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6539(int& val) {
    const int x = 6539;
    std::cout << 6539;
    for (int i = 0; i < 6539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6540(int& val) {
    const int x = 6540;
    std::cout << 6540;
    for (int i = 0; i < 6540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6541(int& val) {
    const int x = 6541;
    std::cout << 6541;
    for (int i = 0; i < 6541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6542(int& val) {
    const int x = 6542;
    std::cout << 6542;
    for (int i = 0; i < 6542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6543(int& val) {
    const int x = 6543;
    std::cout << 6543;
    for (int i = 0; i < 6543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6544(int& val) {
    const int x = 6544;
    std::cout << 6544;
    for (int i = 0; i < 6544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6545(int& val) {
    const int x = 6545;
    std::cout << 6545;
    for (int i = 0; i < 6545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6546(int& val) {
    const int x = 6546;
    std::cout << 6546;
    for (int i = 0; i < 6546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6547(int& val) {
    const int x = 6547;
    std::cout << 6547;
    for (int i = 0; i < 6547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6548(int& val) {
    const int x = 6548;
    std::cout << 6548;
    for (int i = 0; i < 6548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6549(int& val) {
    const int x = 6549;
    std::cout << 6549;
    for (int i = 0; i < 6549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6550(int& val) {
    const int x = 6550;
    std::cout << 6550;
    for (int i = 0; i < 6550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6551(int& val) {
    const int x = 6551;
    std::cout << 6551;
    for (int i = 0; i < 6551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6552(int& val) {
    const int x = 6552;
    std::cout << 6552;
    for (int i = 0; i < 6552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6553(int& val) {
    const int x = 6553;
    std::cout << 6553;
    for (int i = 0; i < 6553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6554(int& val) {
    const int x = 6554;
    std::cout << 6554;
    for (int i = 0; i < 6554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6555(int& val) {
    const int x = 6555;
    std::cout << 6555;
    for (int i = 0; i < 6555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6556(int& val) {
    const int x = 6556;
    std::cout << 6556;
    for (int i = 0; i < 6556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6557(int& val) {
    const int x = 6557;
    std::cout << 6557;
    for (int i = 0; i < 6557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6558(int& val) {
    const int x = 6558;
    std::cout << 6558;
    for (int i = 0; i < 6558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6559(int& val) {
    const int x = 6559;
    std::cout << 6559;
    for (int i = 0; i < 6559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6560(int& val) {
    const int x = 6560;
    std::cout << 6560;
    for (int i = 0; i < 6560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6561(int& val) {
    const int x = 6561;
    std::cout << 6561;
    for (int i = 0; i < 6561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6562(int& val) {
    const int x = 6562;
    std::cout << 6562;
    for (int i = 0; i < 6562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6563(int& val) {
    const int x = 6563;
    std::cout << 6563;
    for (int i = 0; i < 6563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6564(int& val) {
    const int x = 6564;
    std::cout << 6564;
    for (int i = 0; i < 6564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6565(int& val) {
    const int x = 6565;
    std::cout << 6565;
    for (int i = 0; i < 6565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6566(int& val) {
    const int x = 6566;
    std::cout << 6566;
    for (int i = 0; i < 6566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6567(int& val) {
    const int x = 6567;
    std::cout << 6567;
    for (int i = 0; i < 6567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6568(int& val) {
    const int x = 6568;
    std::cout << 6568;
    for (int i = 0; i < 6568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6569(int& val) {
    const int x = 6569;
    std::cout << 6569;
    for (int i = 0; i < 6569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6570(int& val) {
    const int x = 6570;
    std::cout << 6570;
    for (int i = 0; i < 6570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6571(int& val) {
    const int x = 6571;
    std::cout << 6571;
    for (int i = 0; i < 6571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6572(int& val) {
    const int x = 6572;
    std::cout << 6572;
    for (int i = 0; i < 6572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6573(int& val) {
    const int x = 6573;
    std::cout << 6573;
    for (int i = 0; i < 6573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6574(int& val) {
    const int x = 6574;
    std::cout << 6574;
    for (int i = 0; i < 6574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6575(int& val) {
    const int x = 6575;
    std::cout << 6575;
    for (int i = 0; i < 6575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6576(int& val) {
    const int x = 6576;
    std::cout << 6576;
    for (int i = 0; i < 6576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6577(int& val) {
    const int x = 6577;
    std::cout << 6577;
    for (int i = 0; i < 6577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6578(int& val) {
    const int x = 6578;
    std::cout << 6578;
    for (int i = 0; i < 6578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6579(int& val) {
    const int x = 6579;
    std::cout << 6579;
    for (int i = 0; i < 6579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6580(int& val) {
    const int x = 6580;
    std::cout << 6580;
    for (int i = 0; i < 6580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6581(int& val) {
    const int x = 6581;
    std::cout << 6581;
    for (int i = 0; i < 6581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6582(int& val) {
    const int x = 6582;
    std::cout << 6582;
    for (int i = 0; i < 6582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6583(int& val) {
    const int x = 6583;
    std::cout << 6583;
    for (int i = 0; i < 6583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6584(int& val) {
    const int x = 6584;
    std::cout << 6584;
    for (int i = 0; i < 6584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6585(int& val) {
    const int x = 6585;
    std::cout << 6585;
    for (int i = 0; i < 6585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6586(int& val) {
    const int x = 6586;
    std::cout << 6586;
    for (int i = 0; i < 6586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6587(int& val) {
    const int x = 6587;
    std::cout << 6587;
    for (int i = 0; i < 6587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6588(int& val) {
    const int x = 6588;
    std::cout << 6588;
    for (int i = 0; i < 6588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6589(int& val) {
    const int x = 6589;
    std::cout << 6589;
    for (int i = 0; i < 6589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6590(int& val) {
    const int x = 6590;
    std::cout << 6590;
    for (int i = 0; i < 6590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6591(int& val) {
    const int x = 6591;
    std::cout << 6591;
    for (int i = 0; i < 6591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6592(int& val) {
    const int x = 6592;
    std::cout << 6592;
    for (int i = 0; i < 6592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6593(int& val) {
    const int x = 6593;
    std::cout << 6593;
    for (int i = 0; i < 6593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6594(int& val) {
    const int x = 6594;
    std::cout << 6594;
    for (int i = 0; i < 6594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6595(int& val) {
    const int x = 6595;
    std::cout << 6595;
    for (int i = 0; i < 6595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6596(int& val) {
    const int x = 6596;
    std::cout << 6596;
    for (int i = 0; i < 6596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6597(int& val) {
    const int x = 6597;
    std::cout << 6597;
    for (int i = 0; i < 6597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6598(int& val) {
    const int x = 6598;
    std::cout << 6598;
    for (int i = 0; i < 6598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6599(int& val) {
    const int x = 6599;
    std::cout << 6599;
    for (int i = 0; i < 6599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6600(int& val) {
    const int x = 6600;
    std::cout << 6600;
    for (int i = 0; i < 6600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6601(int& val) {
    const int x = 6601;
    std::cout << 6601;
    for (int i = 0; i < 6601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6602(int& val) {
    const int x = 6602;
    std::cout << 6602;
    for (int i = 0; i < 6602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6603(int& val) {
    const int x = 6603;
    std::cout << 6603;
    for (int i = 0; i < 6603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6604(int& val) {
    const int x = 6604;
    std::cout << 6604;
    for (int i = 0; i < 6604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6605(int& val) {
    const int x = 6605;
    std::cout << 6605;
    for (int i = 0; i < 6605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6606(int& val) {
    const int x = 6606;
    std::cout << 6606;
    for (int i = 0; i < 6606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6607(int& val) {
    const int x = 6607;
    std::cout << 6607;
    for (int i = 0; i < 6607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6608(int& val) {
    const int x = 6608;
    std::cout << 6608;
    for (int i = 0; i < 6608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6609(int& val) {
    const int x = 6609;
    std::cout << 6609;
    for (int i = 0; i < 6609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6610(int& val) {
    const int x = 6610;
    std::cout << 6610;
    for (int i = 0; i < 6610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6611(int& val) {
    const int x = 6611;
    std::cout << 6611;
    for (int i = 0; i < 6611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6612(int& val) {
    const int x = 6612;
    std::cout << 6612;
    for (int i = 0; i < 6612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6613(int& val) {
    const int x = 6613;
    std::cout << 6613;
    for (int i = 0; i < 6613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6614(int& val) {
    const int x = 6614;
    std::cout << 6614;
    for (int i = 0; i < 6614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6615(int& val) {
    const int x = 6615;
    std::cout << 6615;
    for (int i = 0; i < 6615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6616(int& val) {
    const int x = 6616;
    std::cout << 6616;
    for (int i = 0; i < 6616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6617(int& val) {
    const int x = 6617;
    std::cout << 6617;
    for (int i = 0; i < 6617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6618(int& val) {
    const int x = 6618;
    std::cout << 6618;
    for (int i = 0; i < 6618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6619(int& val) {
    const int x = 6619;
    std::cout << 6619;
    for (int i = 0; i < 6619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6620(int& val) {
    const int x = 6620;
    std::cout << 6620;
    for (int i = 0; i < 6620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6621(int& val) {
    const int x = 6621;
    std::cout << 6621;
    for (int i = 0; i < 6621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6622(int& val) {
    const int x = 6622;
    std::cout << 6622;
    for (int i = 0; i < 6622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6623(int& val) {
    const int x = 6623;
    std::cout << 6623;
    for (int i = 0; i < 6623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6624(int& val) {
    const int x = 6624;
    std::cout << 6624;
    for (int i = 0; i < 6624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6625(int& val) {
    const int x = 6625;
    std::cout << 6625;
    for (int i = 0; i < 6625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6626(int& val) {
    const int x = 6626;
    std::cout << 6626;
    for (int i = 0; i < 6626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6627(int& val) {
    const int x = 6627;
    std::cout << 6627;
    for (int i = 0; i < 6627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6628(int& val) {
    const int x = 6628;
    std::cout << 6628;
    for (int i = 0; i < 6628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6629(int& val) {
    const int x = 6629;
    std::cout << 6629;
    for (int i = 0; i < 6629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6630(int& val) {
    const int x = 6630;
    std::cout << 6630;
    for (int i = 0; i < 6630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6631(int& val) {
    const int x = 6631;
    std::cout << 6631;
    for (int i = 0; i < 6631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6632(int& val) {
    const int x = 6632;
    std::cout << 6632;
    for (int i = 0; i < 6632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6633(int& val) {
    const int x = 6633;
    std::cout << 6633;
    for (int i = 0; i < 6633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6634(int& val) {
    const int x = 6634;
    std::cout << 6634;
    for (int i = 0; i < 6634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6635(int& val) {
    const int x = 6635;
    std::cout << 6635;
    for (int i = 0; i < 6635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6636(int& val) {
    const int x = 6636;
    std::cout << 6636;
    for (int i = 0; i < 6636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6637(int& val) {
    const int x = 6637;
    std::cout << 6637;
    for (int i = 0; i < 6637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6638(int& val) {
    const int x = 6638;
    std::cout << 6638;
    for (int i = 0; i < 6638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6639(int& val) {
    const int x = 6639;
    std::cout << 6639;
    for (int i = 0; i < 6639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6640(int& val) {
    const int x = 6640;
    std::cout << 6640;
    for (int i = 0; i < 6640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6641(int& val) {
    const int x = 6641;
    std::cout << 6641;
    for (int i = 0; i < 6641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6642(int& val) {
    const int x = 6642;
    std::cout << 6642;
    for (int i = 0; i < 6642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6643(int& val) {
    const int x = 6643;
    std::cout << 6643;
    for (int i = 0; i < 6643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6644(int& val) {
    const int x = 6644;
    std::cout << 6644;
    for (int i = 0; i < 6644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6645(int& val) {
    const int x = 6645;
    std::cout << 6645;
    for (int i = 0; i < 6645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6646(int& val) {
    const int x = 6646;
    std::cout << 6646;
    for (int i = 0; i < 6646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6647(int& val) {
    const int x = 6647;
    std::cout << 6647;
    for (int i = 0; i < 6647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6648(int& val) {
    const int x = 6648;
    std::cout << 6648;
    for (int i = 0; i < 6648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6649(int& val) {
    const int x = 6649;
    std::cout << 6649;
    for (int i = 0; i < 6649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6650(int& val) {
    const int x = 6650;
    std::cout << 6650;
    for (int i = 0; i < 6650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6651(int& val) {
    const int x = 6651;
    std::cout << 6651;
    for (int i = 0; i < 6651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6652(int& val) {
    const int x = 6652;
    std::cout << 6652;
    for (int i = 0; i < 6652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6653(int& val) {
    const int x = 6653;
    std::cout << 6653;
    for (int i = 0; i < 6653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6654(int& val) {
    const int x = 6654;
    std::cout << 6654;
    for (int i = 0; i < 6654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6655(int& val) {
    const int x = 6655;
    std::cout << 6655;
    for (int i = 0; i < 6655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6656(int& val) {
    const int x = 6656;
    std::cout << 6656;
    for (int i = 0; i < 6656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6657(int& val) {
    const int x = 6657;
    std::cout << 6657;
    for (int i = 0; i < 6657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6658(int& val) {
    const int x = 6658;
    std::cout << 6658;
    for (int i = 0; i < 6658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6659(int& val) {
    const int x = 6659;
    std::cout << 6659;
    for (int i = 0; i < 6659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6660(int& val) {
    const int x = 6660;
    std::cout << 6660;
    for (int i = 0; i < 6660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6661(int& val) {
    const int x = 6661;
    std::cout << 6661;
    for (int i = 0; i < 6661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6662(int& val) {
    const int x = 6662;
    std::cout << 6662;
    for (int i = 0; i < 6662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6663(int& val) {
    const int x = 6663;
    std::cout << 6663;
    for (int i = 0; i < 6663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6664(int& val) {
    const int x = 6664;
    std::cout << 6664;
    for (int i = 0; i < 6664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6665(int& val) {
    const int x = 6665;
    std::cout << 6665;
    for (int i = 0; i < 6665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6666(int& val) {
    const int x = 6666;
    std::cout << 6666;
    for (int i = 0; i < 6666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6667(int& val) {
    const int x = 6667;
    std::cout << 6667;
    for (int i = 0; i < 6667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6668(int& val) {
    const int x = 6668;
    std::cout << 6668;
    for (int i = 0; i < 6668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6669(int& val) {
    const int x = 6669;
    std::cout << 6669;
    for (int i = 0; i < 6669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6670(int& val) {
    const int x = 6670;
    std::cout << 6670;
    for (int i = 0; i < 6670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6671(int& val) {
    const int x = 6671;
    std::cout << 6671;
    for (int i = 0; i < 6671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6672(int& val) {
    const int x = 6672;
    std::cout << 6672;
    for (int i = 0; i < 6672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6673(int& val) {
    const int x = 6673;
    std::cout << 6673;
    for (int i = 0; i < 6673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6674(int& val) {
    const int x = 6674;
    std::cout << 6674;
    for (int i = 0; i < 6674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6675(int& val) {
    const int x = 6675;
    std::cout << 6675;
    for (int i = 0; i < 6675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6676(int& val) {
    const int x = 6676;
    std::cout << 6676;
    for (int i = 0; i < 6676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6677(int& val) {
    const int x = 6677;
    std::cout << 6677;
    for (int i = 0; i < 6677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6678(int& val) {
    const int x = 6678;
    std::cout << 6678;
    for (int i = 0; i < 6678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6679(int& val) {
    const int x = 6679;
    std::cout << 6679;
    for (int i = 0; i < 6679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6680(int& val) {
    const int x = 6680;
    std::cout << 6680;
    for (int i = 0; i < 6680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6681(int& val) {
    const int x = 6681;
    std::cout << 6681;
    for (int i = 0; i < 6681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6682(int& val) {
    const int x = 6682;
    std::cout << 6682;
    for (int i = 0; i < 6682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6683(int& val) {
    const int x = 6683;
    std::cout << 6683;
    for (int i = 0; i < 6683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6684(int& val) {
    const int x = 6684;
    std::cout << 6684;
    for (int i = 0; i < 6684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6685(int& val) {
    const int x = 6685;
    std::cout << 6685;
    for (int i = 0; i < 6685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6686(int& val) {
    const int x = 6686;
    std::cout << 6686;
    for (int i = 0; i < 6686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6687(int& val) {
    const int x = 6687;
    std::cout << 6687;
    for (int i = 0; i < 6687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6688(int& val) {
    const int x = 6688;
    std::cout << 6688;
    for (int i = 0; i < 6688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6689(int& val) {
    const int x = 6689;
    std::cout << 6689;
    for (int i = 0; i < 6689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6690(int& val) {
    const int x = 6690;
    std::cout << 6690;
    for (int i = 0; i < 6690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6691(int& val) {
    const int x = 6691;
    std::cout << 6691;
    for (int i = 0; i < 6691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6692(int& val) {
    const int x = 6692;
    std::cout << 6692;
    for (int i = 0; i < 6692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6693(int& val) {
    const int x = 6693;
    std::cout << 6693;
    for (int i = 0; i < 6693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6694(int& val) {
    const int x = 6694;
    std::cout << 6694;
    for (int i = 0; i < 6694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6695(int& val) {
    const int x = 6695;
    std::cout << 6695;
    for (int i = 0; i < 6695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6696(int& val) {
    const int x = 6696;
    std::cout << 6696;
    for (int i = 0; i < 6696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6697(int& val) {
    const int x = 6697;
    std::cout << 6697;
    for (int i = 0; i < 6697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6698(int& val) {
    const int x = 6698;
    std::cout << 6698;
    for (int i = 0; i < 6698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6699(int& val) {
    const int x = 6699;
    std::cout << 6699;
    for (int i = 0; i < 6699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6700(int& val) {
    const int x = 6700;
    std::cout << 6700;
    for (int i = 0; i < 6700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6701(int& val) {
    const int x = 6701;
    std::cout << 6701;
    for (int i = 0; i < 6701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6702(int& val) {
    const int x = 6702;
    std::cout << 6702;
    for (int i = 0; i < 6702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6703(int& val) {
    const int x = 6703;
    std::cout << 6703;
    for (int i = 0; i < 6703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6704(int& val) {
    const int x = 6704;
    std::cout << 6704;
    for (int i = 0; i < 6704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6705(int& val) {
    const int x = 6705;
    std::cout << 6705;
    for (int i = 0; i < 6705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6706(int& val) {
    const int x = 6706;
    std::cout << 6706;
    for (int i = 0; i < 6706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6707(int& val) {
    const int x = 6707;
    std::cout << 6707;
    for (int i = 0; i < 6707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6708(int& val) {
    const int x = 6708;
    std::cout << 6708;
    for (int i = 0; i < 6708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6709(int& val) {
    const int x = 6709;
    std::cout << 6709;
    for (int i = 0; i < 6709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6710(int& val) {
    const int x = 6710;
    std::cout << 6710;
    for (int i = 0; i < 6710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6711(int& val) {
    const int x = 6711;
    std::cout << 6711;
    for (int i = 0; i < 6711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6712(int& val) {
    const int x = 6712;
    std::cout << 6712;
    for (int i = 0; i < 6712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6713(int& val) {
    const int x = 6713;
    std::cout << 6713;
    for (int i = 0; i < 6713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6714(int& val) {
    const int x = 6714;
    std::cout << 6714;
    for (int i = 0; i < 6714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6715(int& val) {
    const int x = 6715;
    std::cout << 6715;
    for (int i = 0; i < 6715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6716(int& val) {
    const int x = 6716;
    std::cout << 6716;
    for (int i = 0; i < 6716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6717(int& val) {
    const int x = 6717;
    std::cout << 6717;
    for (int i = 0; i < 6717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6718(int& val) {
    const int x = 6718;
    std::cout << 6718;
    for (int i = 0; i < 6718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6719(int& val) {
    const int x = 6719;
    std::cout << 6719;
    for (int i = 0; i < 6719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6720(int& val) {
    const int x = 6720;
    std::cout << 6720;
    for (int i = 0; i < 6720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6721(int& val) {
    const int x = 6721;
    std::cout << 6721;
    for (int i = 0; i < 6721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6722(int& val) {
    const int x = 6722;
    std::cout << 6722;
    for (int i = 0; i < 6722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6723(int& val) {
    const int x = 6723;
    std::cout << 6723;
    for (int i = 0; i < 6723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6724(int& val) {
    const int x = 6724;
    std::cout << 6724;
    for (int i = 0; i < 6724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6725(int& val) {
    const int x = 6725;
    std::cout << 6725;
    for (int i = 0; i < 6725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6726(int& val) {
    const int x = 6726;
    std::cout << 6726;
    for (int i = 0; i < 6726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6727(int& val) {
    const int x = 6727;
    std::cout << 6727;
    for (int i = 0; i < 6727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6728(int& val) {
    const int x = 6728;
    std::cout << 6728;
    for (int i = 0; i < 6728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6729(int& val) {
    const int x = 6729;
    std::cout << 6729;
    for (int i = 0; i < 6729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6730(int& val) {
    const int x = 6730;
    std::cout << 6730;
    for (int i = 0; i < 6730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6731(int& val) {
    const int x = 6731;
    std::cout << 6731;
    for (int i = 0; i < 6731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6732(int& val) {
    const int x = 6732;
    std::cout << 6732;
    for (int i = 0; i < 6732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6733(int& val) {
    const int x = 6733;
    std::cout << 6733;
    for (int i = 0; i < 6733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6734(int& val) {
    const int x = 6734;
    std::cout << 6734;
    for (int i = 0; i < 6734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6735(int& val) {
    const int x = 6735;
    std::cout << 6735;
    for (int i = 0; i < 6735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6736(int& val) {
    const int x = 6736;
    std::cout << 6736;
    for (int i = 0; i < 6736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6737(int& val) {
    const int x = 6737;
    std::cout << 6737;
    for (int i = 0; i < 6737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6738(int& val) {
    const int x = 6738;
    std::cout << 6738;
    for (int i = 0; i < 6738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6739(int& val) {
    const int x = 6739;
    std::cout << 6739;
    for (int i = 0; i < 6739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6740(int& val) {
    const int x = 6740;
    std::cout << 6740;
    for (int i = 0; i < 6740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6741(int& val) {
    const int x = 6741;
    std::cout << 6741;
    for (int i = 0; i < 6741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6742(int& val) {
    const int x = 6742;
    std::cout << 6742;
    for (int i = 0; i < 6742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6743(int& val) {
    const int x = 6743;
    std::cout << 6743;
    for (int i = 0; i < 6743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6744(int& val) {
    const int x = 6744;
    std::cout << 6744;
    for (int i = 0; i < 6744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6745(int& val) {
    const int x = 6745;
    std::cout << 6745;
    for (int i = 0; i < 6745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6746(int& val) {
    const int x = 6746;
    std::cout << 6746;
    for (int i = 0; i < 6746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6747(int& val) {
    const int x = 6747;
    std::cout << 6747;
    for (int i = 0; i < 6747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6748(int& val) {
    const int x = 6748;
    std::cout << 6748;
    for (int i = 0; i < 6748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6749(int& val) {
    const int x = 6749;
    std::cout << 6749;
    for (int i = 0; i < 6749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6750(int& val) {
    const int x = 6750;
    std::cout << 6750;
    for (int i = 0; i < 6750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6751(int& val) {
    const int x = 6751;
    std::cout << 6751;
    for (int i = 0; i < 6751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6752(int& val) {
    const int x = 6752;
    std::cout << 6752;
    for (int i = 0; i < 6752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6753(int& val) {
    const int x = 6753;
    std::cout << 6753;
    for (int i = 0; i < 6753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6754(int& val) {
    const int x = 6754;
    std::cout << 6754;
    for (int i = 0; i < 6754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6755(int& val) {
    const int x = 6755;
    std::cout << 6755;
    for (int i = 0; i < 6755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6756(int& val) {
    const int x = 6756;
    std::cout << 6756;
    for (int i = 0; i < 6756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6757(int& val) {
    const int x = 6757;
    std::cout << 6757;
    for (int i = 0; i < 6757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6758(int& val) {
    const int x = 6758;
    std::cout << 6758;
    for (int i = 0; i < 6758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6759(int& val) {
    const int x = 6759;
    std::cout << 6759;
    for (int i = 0; i < 6759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6760(int& val) {
    const int x = 6760;
    std::cout << 6760;
    for (int i = 0; i < 6760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6761(int& val) {
    const int x = 6761;
    std::cout << 6761;
    for (int i = 0; i < 6761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6762(int& val) {
    const int x = 6762;
    std::cout << 6762;
    for (int i = 0; i < 6762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6763(int& val) {
    const int x = 6763;
    std::cout << 6763;
    for (int i = 0; i < 6763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6764(int& val) {
    const int x = 6764;
    std::cout << 6764;
    for (int i = 0; i < 6764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6765(int& val) {
    const int x = 6765;
    std::cout << 6765;
    for (int i = 0; i < 6765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6766(int& val) {
    const int x = 6766;
    std::cout << 6766;
    for (int i = 0; i < 6766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6767(int& val) {
    const int x = 6767;
    std::cout << 6767;
    for (int i = 0; i < 6767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6768(int& val) {
    const int x = 6768;
    std::cout << 6768;
    for (int i = 0; i < 6768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6769(int& val) {
    const int x = 6769;
    std::cout << 6769;
    for (int i = 0; i < 6769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6770(int& val) {
    const int x = 6770;
    std::cout << 6770;
    for (int i = 0; i < 6770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6771(int& val) {
    const int x = 6771;
    std::cout << 6771;
    for (int i = 0; i < 6771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6772(int& val) {
    const int x = 6772;
    std::cout << 6772;
    for (int i = 0; i < 6772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6773(int& val) {
    const int x = 6773;
    std::cout << 6773;
    for (int i = 0; i < 6773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6774(int& val) {
    const int x = 6774;
    std::cout << 6774;
    for (int i = 0; i < 6774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6775(int& val) {
    const int x = 6775;
    std::cout << 6775;
    for (int i = 0; i < 6775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6776(int& val) {
    const int x = 6776;
    std::cout << 6776;
    for (int i = 0; i < 6776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6777(int& val) {
    const int x = 6777;
    std::cout << 6777;
    for (int i = 0; i < 6777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6778(int& val) {
    const int x = 6778;
    std::cout << 6778;
    for (int i = 0; i < 6778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6779(int& val) {
    const int x = 6779;
    std::cout << 6779;
    for (int i = 0; i < 6779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6780(int& val) {
    const int x = 6780;
    std::cout << 6780;
    for (int i = 0; i < 6780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6781(int& val) {
    const int x = 6781;
    std::cout << 6781;
    for (int i = 0; i < 6781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6782(int& val) {
    const int x = 6782;
    std::cout << 6782;
    for (int i = 0; i < 6782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6783(int& val) {
    const int x = 6783;
    std::cout << 6783;
    for (int i = 0; i < 6783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6784(int& val) {
    const int x = 6784;
    std::cout << 6784;
    for (int i = 0; i < 6784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6785(int& val) {
    const int x = 6785;
    std::cout << 6785;
    for (int i = 0; i < 6785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6786(int& val) {
    const int x = 6786;
    std::cout << 6786;
    for (int i = 0; i < 6786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6787(int& val) {
    const int x = 6787;
    std::cout << 6787;
    for (int i = 0; i < 6787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6788(int& val) {
    const int x = 6788;
    std::cout << 6788;
    for (int i = 0; i < 6788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6789(int& val) {
    const int x = 6789;
    std::cout << 6789;
    for (int i = 0; i < 6789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6790(int& val) {
    const int x = 6790;
    std::cout << 6790;
    for (int i = 0; i < 6790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6791(int& val) {
    const int x = 6791;
    std::cout << 6791;
    for (int i = 0; i < 6791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6792(int& val) {
    const int x = 6792;
    std::cout << 6792;
    for (int i = 0; i < 6792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6793(int& val) {
    const int x = 6793;
    std::cout << 6793;
    for (int i = 0; i < 6793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6794(int& val) {
    const int x = 6794;
    std::cout << 6794;
    for (int i = 0; i < 6794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6795(int& val) {
    const int x = 6795;
    std::cout << 6795;
    for (int i = 0; i < 6795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6796(int& val) {
    const int x = 6796;
    std::cout << 6796;
    for (int i = 0; i < 6796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6797(int& val) {
    const int x = 6797;
    std::cout << 6797;
    for (int i = 0; i < 6797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6798(int& val) {
    const int x = 6798;
    std::cout << 6798;
    for (int i = 0; i < 6798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6799(int& val) {
    const int x = 6799;
    std::cout << 6799;
    for (int i = 0; i < 6799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6800(int& val) {
    const int x = 6800;
    std::cout << 6800;
    for (int i = 0; i < 6800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6801(int& val) {
    const int x = 6801;
    std::cout << 6801;
    for (int i = 0; i < 6801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6802(int& val) {
    const int x = 6802;
    std::cout << 6802;
    for (int i = 0; i < 6802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6803(int& val) {
    const int x = 6803;
    std::cout << 6803;
    for (int i = 0; i < 6803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6804(int& val) {
    const int x = 6804;
    std::cout << 6804;
    for (int i = 0; i < 6804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6805(int& val) {
    const int x = 6805;
    std::cout << 6805;
    for (int i = 0; i < 6805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6806(int& val) {
    const int x = 6806;
    std::cout << 6806;
    for (int i = 0; i < 6806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6807(int& val) {
    const int x = 6807;
    std::cout << 6807;
    for (int i = 0; i < 6807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6808(int& val) {
    const int x = 6808;
    std::cout << 6808;
    for (int i = 0; i < 6808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6809(int& val) {
    const int x = 6809;
    std::cout << 6809;
    for (int i = 0; i < 6809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6810(int& val) {
    const int x = 6810;
    std::cout << 6810;
    for (int i = 0; i < 6810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6811(int& val) {
    const int x = 6811;
    std::cout << 6811;
    for (int i = 0; i < 6811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6812(int& val) {
    const int x = 6812;
    std::cout << 6812;
    for (int i = 0; i < 6812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6813(int& val) {
    const int x = 6813;
    std::cout << 6813;
    for (int i = 0; i < 6813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6814(int& val) {
    const int x = 6814;
    std::cout << 6814;
    for (int i = 0; i < 6814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6815(int& val) {
    const int x = 6815;
    std::cout << 6815;
    for (int i = 0; i < 6815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6816(int& val) {
    const int x = 6816;
    std::cout << 6816;
    for (int i = 0; i < 6816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6817(int& val) {
    const int x = 6817;
    std::cout << 6817;
    for (int i = 0; i < 6817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6818(int& val) {
    const int x = 6818;
    std::cout << 6818;
    for (int i = 0; i < 6818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6819(int& val) {
    const int x = 6819;
    std::cout << 6819;
    for (int i = 0; i < 6819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6820(int& val) {
    const int x = 6820;
    std::cout << 6820;
    for (int i = 0; i < 6820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6821(int& val) {
    const int x = 6821;
    std::cout << 6821;
    for (int i = 0; i < 6821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6822(int& val) {
    const int x = 6822;
    std::cout << 6822;
    for (int i = 0; i < 6822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6823(int& val) {
    const int x = 6823;
    std::cout << 6823;
    for (int i = 0; i < 6823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6824(int& val) {
    const int x = 6824;
    std::cout << 6824;
    for (int i = 0; i < 6824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6825(int& val) {
    const int x = 6825;
    std::cout << 6825;
    for (int i = 0; i < 6825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6826(int& val) {
    const int x = 6826;
    std::cout << 6826;
    for (int i = 0; i < 6826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6827(int& val) {
    const int x = 6827;
    std::cout << 6827;
    for (int i = 0; i < 6827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6828(int& val) {
    const int x = 6828;
    std::cout << 6828;
    for (int i = 0; i < 6828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6829(int& val) {
    const int x = 6829;
    std::cout << 6829;
    for (int i = 0; i < 6829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6830(int& val) {
    const int x = 6830;
    std::cout << 6830;
    for (int i = 0; i < 6830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6831(int& val) {
    const int x = 6831;
    std::cout << 6831;
    for (int i = 0; i < 6831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6832(int& val) {
    const int x = 6832;
    std::cout << 6832;
    for (int i = 0; i < 6832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6833(int& val) {
    const int x = 6833;
    std::cout << 6833;
    for (int i = 0; i < 6833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6834(int& val) {
    const int x = 6834;
    std::cout << 6834;
    for (int i = 0; i < 6834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6835(int& val) {
    const int x = 6835;
    std::cout << 6835;
    for (int i = 0; i < 6835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6836(int& val) {
    const int x = 6836;
    std::cout << 6836;
    for (int i = 0; i < 6836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6837(int& val) {
    const int x = 6837;
    std::cout << 6837;
    for (int i = 0; i < 6837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6838(int& val) {
    const int x = 6838;
    std::cout << 6838;
    for (int i = 0; i < 6838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6839(int& val) {
    const int x = 6839;
    std::cout << 6839;
    for (int i = 0; i < 6839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6840(int& val) {
    const int x = 6840;
    std::cout << 6840;
    for (int i = 0; i < 6840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6841(int& val) {
    const int x = 6841;
    std::cout << 6841;
    for (int i = 0; i < 6841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6842(int& val) {
    const int x = 6842;
    std::cout << 6842;
    for (int i = 0; i < 6842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6843(int& val) {
    const int x = 6843;
    std::cout << 6843;
    for (int i = 0; i < 6843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6844(int& val) {
    const int x = 6844;
    std::cout << 6844;
    for (int i = 0; i < 6844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6845(int& val) {
    const int x = 6845;
    std::cout << 6845;
    for (int i = 0; i < 6845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6846(int& val) {
    const int x = 6846;
    std::cout << 6846;
    for (int i = 0; i < 6846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6847(int& val) {
    const int x = 6847;
    std::cout << 6847;
    for (int i = 0; i < 6847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6848(int& val) {
    const int x = 6848;
    std::cout << 6848;
    for (int i = 0; i < 6848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6849(int& val) {
    const int x = 6849;
    std::cout << 6849;
    for (int i = 0; i < 6849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6850(int& val) {
    const int x = 6850;
    std::cout << 6850;
    for (int i = 0; i < 6850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6851(int& val) {
    const int x = 6851;
    std::cout << 6851;
    for (int i = 0; i < 6851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6852(int& val) {
    const int x = 6852;
    std::cout << 6852;
    for (int i = 0; i < 6852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6853(int& val) {
    const int x = 6853;
    std::cout << 6853;
    for (int i = 0; i < 6853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6854(int& val) {
    const int x = 6854;
    std::cout << 6854;
    for (int i = 0; i < 6854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6855(int& val) {
    const int x = 6855;
    std::cout << 6855;
    for (int i = 0; i < 6855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6856(int& val) {
    const int x = 6856;
    std::cout << 6856;
    for (int i = 0; i < 6856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6857(int& val) {
    const int x = 6857;
    std::cout << 6857;
    for (int i = 0; i < 6857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6858(int& val) {
    const int x = 6858;
    std::cout << 6858;
    for (int i = 0; i < 6858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6859(int& val) {
    const int x = 6859;
    std::cout << 6859;
    for (int i = 0; i < 6859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6860(int& val) {
    const int x = 6860;
    std::cout << 6860;
    for (int i = 0; i < 6860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6861(int& val) {
    const int x = 6861;
    std::cout << 6861;
    for (int i = 0; i < 6861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6862(int& val) {
    const int x = 6862;
    std::cout << 6862;
    for (int i = 0; i < 6862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6863(int& val) {
    const int x = 6863;
    std::cout << 6863;
    for (int i = 0; i < 6863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6864(int& val) {
    const int x = 6864;
    std::cout << 6864;
    for (int i = 0; i < 6864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6865(int& val) {
    const int x = 6865;
    std::cout << 6865;
    for (int i = 0; i < 6865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6866(int& val) {
    const int x = 6866;
    std::cout << 6866;
    for (int i = 0; i < 6866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6867(int& val) {
    const int x = 6867;
    std::cout << 6867;
    for (int i = 0; i < 6867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6868(int& val) {
    const int x = 6868;
    std::cout << 6868;
    for (int i = 0; i < 6868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6869(int& val) {
    const int x = 6869;
    std::cout << 6869;
    for (int i = 0; i < 6869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6870(int& val) {
    const int x = 6870;
    std::cout << 6870;
    for (int i = 0; i < 6870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6871(int& val) {
    const int x = 6871;
    std::cout << 6871;
    for (int i = 0; i < 6871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6872(int& val) {
    const int x = 6872;
    std::cout << 6872;
    for (int i = 0; i < 6872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6873(int& val) {
    const int x = 6873;
    std::cout << 6873;
    for (int i = 0; i < 6873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6874(int& val) {
    const int x = 6874;
    std::cout << 6874;
    for (int i = 0; i < 6874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6875(int& val) {
    const int x = 6875;
    std::cout << 6875;
    for (int i = 0; i < 6875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6876(int& val) {
    const int x = 6876;
    std::cout << 6876;
    for (int i = 0; i < 6876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6877(int& val) {
    const int x = 6877;
    std::cout << 6877;
    for (int i = 0; i < 6877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6878(int& val) {
    const int x = 6878;
    std::cout << 6878;
    for (int i = 0; i < 6878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6879(int& val) {
    const int x = 6879;
    std::cout << 6879;
    for (int i = 0; i < 6879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6880(int& val) {
    const int x = 6880;
    std::cout << 6880;
    for (int i = 0; i < 6880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6881(int& val) {
    const int x = 6881;
    std::cout << 6881;
    for (int i = 0; i < 6881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6882(int& val) {
    const int x = 6882;
    std::cout << 6882;
    for (int i = 0; i < 6882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6883(int& val) {
    const int x = 6883;
    std::cout << 6883;
    for (int i = 0; i < 6883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6884(int& val) {
    const int x = 6884;
    std::cout << 6884;
    for (int i = 0; i < 6884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6885(int& val) {
    const int x = 6885;
    std::cout << 6885;
    for (int i = 0; i < 6885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6886(int& val) {
    const int x = 6886;
    std::cout << 6886;
    for (int i = 0; i < 6886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6887(int& val) {
    const int x = 6887;
    std::cout << 6887;
    for (int i = 0; i < 6887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6888(int& val) {
    const int x = 6888;
    std::cout << 6888;
    for (int i = 0; i < 6888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6889(int& val) {
    const int x = 6889;
    std::cout << 6889;
    for (int i = 0; i < 6889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6890(int& val) {
    const int x = 6890;
    std::cout << 6890;
    for (int i = 0; i < 6890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6891(int& val) {
    const int x = 6891;
    std::cout << 6891;
    for (int i = 0; i < 6891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6892(int& val) {
    const int x = 6892;
    std::cout << 6892;
    for (int i = 0; i < 6892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6893(int& val) {
    const int x = 6893;
    std::cout << 6893;
    for (int i = 0; i < 6893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6894(int& val) {
    const int x = 6894;
    std::cout << 6894;
    for (int i = 0; i < 6894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6895(int& val) {
    const int x = 6895;
    std::cout << 6895;
    for (int i = 0; i < 6895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6896(int& val) {
    const int x = 6896;
    std::cout << 6896;
    for (int i = 0; i < 6896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6897(int& val) {
    const int x = 6897;
    std::cout << 6897;
    for (int i = 0; i < 6897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6898(int& val) {
    const int x = 6898;
    std::cout << 6898;
    for (int i = 0; i < 6898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6899(int& val) {
    const int x = 6899;
    std::cout << 6899;
    for (int i = 0; i < 6899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6900(int& val) {
    const int x = 6900;
    std::cout << 6900;
    for (int i = 0; i < 6900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6901(int& val) {
    const int x = 6901;
    std::cout << 6901;
    for (int i = 0; i < 6901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6902(int& val) {
    const int x = 6902;
    std::cout << 6902;
    for (int i = 0; i < 6902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6903(int& val) {
    const int x = 6903;
    std::cout << 6903;
    for (int i = 0; i < 6903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6904(int& val) {
    const int x = 6904;
    std::cout << 6904;
    for (int i = 0; i < 6904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6905(int& val) {
    const int x = 6905;
    std::cout << 6905;
    for (int i = 0; i < 6905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6906(int& val) {
    const int x = 6906;
    std::cout << 6906;
    for (int i = 0; i < 6906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6907(int& val) {
    const int x = 6907;
    std::cout << 6907;
    for (int i = 0; i < 6907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6908(int& val) {
    const int x = 6908;
    std::cout << 6908;
    for (int i = 0; i < 6908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6909(int& val) {
    const int x = 6909;
    std::cout << 6909;
    for (int i = 0; i < 6909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6910(int& val) {
    const int x = 6910;
    std::cout << 6910;
    for (int i = 0; i < 6910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6911(int& val) {
    const int x = 6911;
    std::cout << 6911;
    for (int i = 0; i < 6911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6912(int& val) {
    const int x = 6912;
    std::cout << 6912;
    for (int i = 0; i < 6912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6913(int& val) {
    const int x = 6913;
    std::cout << 6913;
    for (int i = 0; i < 6913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6914(int& val) {
    const int x = 6914;
    std::cout << 6914;
    for (int i = 0; i < 6914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6915(int& val) {
    const int x = 6915;
    std::cout << 6915;
    for (int i = 0; i < 6915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6916(int& val) {
    const int x = 6916;
    std::cout << 6916;
    for (int i = 0; i < 6916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6917(int& val) {
    const int x = 6917;
    std::cout << 6917;
    for (int i = 0; i < 6917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6918(int& val) {
    const int x = 6918;
    std::cout << 6918;
    for (int i = 0; i < 6918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6919(int& val) {
    const int x = 6919;
    std::cout << 6919;
    for (int i = 0; i < 6919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6920(int& val) {
    const int x = 6920;
    std::cout << 6920;
    for (int i = 0; i < 6920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6921(int& val) {
    const int x = 6921;
    std::cout << 6921;
    for (int i = 0; i < 6921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6922(int& val) {
    const int x = 6922;
    std::cout << 6922;
    for (int i = 0; i < 6922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6923(int& val) {
    const int x = 6923;
    std::cout << 6923;
    for (int i = 0; i < 6923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6924(int& val) {
    const int x = 6924;
    std::cout << 6924;
    for (int i = 0; i < 6924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6925(int& val) {
    const int x = 6925;
    std::cout << 6925;
    for (int i = 0; i < 6925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6926(int& val) {
    const int x = 6926;
    std::cout << 6926;
    for (int i = 0; i < 6926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6927(int& val) {
    const int x = 6927;
    std::cout << 6927;
    for (int i = 0; i < 6927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6928(int& val) {
    const int x = 6928;
    std::cout << 6928;
    for (int i = 0; i < 6928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6929(int& val) {
    const int x = 6929;
    std::cout << 6929;
    for (int i = 0; i < 6929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6930(int& val) {
    const int x = 6930;
    std::cout << 6930;
    for (int i = 0; i < 6930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6931(int& val) {
    const int x = 6931;
    std::cout << 6931;
    for (int i = 0; i < 6931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6932(int& val) {
    const int x = 6932;
    std::cout << 6932;
    for (int i = 0; i < 6932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6933(int& val) {
    const int x = 6933;
    std::cout << 6933;
    for (int i = 0; i < 6933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6934(int& val) {
    const int x = 6934;
    std::cout << 6934;
    for (int i = 0; i < 6934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6935(int& val) {
    const int x = 6935;
    std::cout << 6935;
    for (int i = 0; i < 6935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6936(int& val) {
    const int x = 6936;
    std::cout << 6936;
    for (int i = 0; i < 6936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6937(int& val) {
    const int x = 6937;
    std::cout << 6937;
    for (int i = 0; i < 6937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6938(int& val) {
    const int x = 6938;
    std::cout << 6938;
    for (int i = 0; i < 6938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6939(int& val) {
    const int x = 6939;
    std::cout << 6939;
    for (int i = 0; i < 6939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6940(int& val) {
    const int x = 6940;
    std::cout << 6940;
    for (int i = 0; i < 6940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6941(int& val) {
    const int x = 6941;
    std::cout << 6941;
    for (int i = 0; i < 6941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6942(int& val) {
    const int x = 6942;
    std::cout << 6942;
    for (int i = 0; i < 6942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6943(int& val) {
    const int x = 6943;
    std::cout << 6943;
    for (int i = 0; i < 6943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6944(int& val) {
    const int x = 6944;
    std::cout << 6944;
    for (int i = 0; i < 6944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6945(int& val) {
    const int x = 6945;
    std::cout << 6945;
    for (int i = 0; i < 6945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6946(int& val) {
    const int x = 6946;
    std::cout << 6946;
    for (int i = 0; i < 6946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6947(int& val) {
    const int x = 6947;
    std::cout << 6947;
    for (int i = 0; i < 6947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6948(int& val) {
    const int x = 6948;
    std::cout << 6948;
    for (int i = 0; i < 6948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6949(int& val) {
    const int x = 6949;
    std::cout << 6949;
    for (int i = 0; i < 6949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6950(int& val) {
    const int x = 6950;
    std::cout << 6950;
    for (int i = 0; i < 6950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6951(int& val) {
    const int x = 6951;
    std::cout << 6951;
    for (int i = 0; i < 6951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6952(int& val) {
    const int x = 6952;
    std::cout << 6952;
    for (int i = 0; i < 6952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6953(int& val) {
    const int x = 6953;
    std::cout << 6953;
    for (int i = 0; i < 6953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6954(int& val) {
    const int x = 6954;
    std::cout << 6954;
    for (int i = 0; i < 6954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6955(int& val) {
    const int x = 6955;
    std::cout << 6955;
    for (int i = 0; i < 6955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6956(int& val) {
    const int x = 6956;
    std::cout << 6956;
    for (int i = 0; i < 6956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6957(int& val) {
    const int x = 6957;
    std::cout << 6957;
    for (int i = 0; i < 6957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6958(int& val) {
    const int x = 6958;
    std::cout << 6958;
    for (int i = 0; i < 6958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6959(int& val) {
    const int x = 6959;
    std::cout << 6959;
    for (int i = 0; i < 6959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6960(int& val) {
    const int x = 6960;
    std::cout << 6960;
    for (int i = 0; i < 6960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6961(int& val) {
    const int x = 6961;
    std::cout << 6961;
    for (int i = 0; i < 6961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6962(int& val) {
    const int x = 6962;
    std::cout << 6962;
    for (int i = 0; i < 6962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6963(int& val) {
    const int x = 6963;
    std::cout << 6963;
    for (int i = 0; i < 6963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6964(int& val) {
    const int x = 6964;
    std::cout << 6964;
    for (int i = 0; i < 6964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6965(int& val) {
    const int x = 6965;
    std::cout << 6965;
    for (int i = 0; i < 6965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6966(int& val) {
    const int x = 6966;
    std::cout << 6966;
    for (int i = 0; i < 6966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6967(int& val) {
    const int x = 6967;
    std::cout << 6967;
    for (int i = 0; i < 6967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6968(int& val) {
    const int x = 6968;
    std::cout << 6968;
    for (int i = 0; i < 6968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6969(int& val) {
    const int x = 6969;
    std::cout << 6969;
    for (int i = 0; i < 6969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6970(int& val) {
    const int x = 6970;
    std::cout << 6970;
    for (int i = 0; i < 6970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6971(int& val) {
    const int x = 6971;
    std::cout << 6971;
    for (int i = 0; i < 6971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6972(int& val) {
    const int x = 6972;
    std::cout << 6972;
    for (int i = 0; i < 6972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6973(int& val) {
    const int x = 6973;
    std::cout << 6973;
    for (int i = 0; i < 6973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6974(int& val) {
    const int x = 6974;
    std::cout << 6974;
    for (int i = 0; i < 6974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6975(int& val) {
    const int x = 6975;
    std::cout << 6975;
    for (int i = 0; i < 6975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6976(int& val) {
    const int x = 6976;
    std::cout << 6976;
    for (int i = 0; i < 6976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6977(int& val) {
    const int x = 6977;
    std::cout << 6977;
    for (int i = 0; i < 6977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6978(int& val) {
    const int x = 6978;
    std::cout << 6978;
    for (int i = 0; i < 6978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6979(int& val) {
    const int x = 6979;
    std::cout << 6979;
    for (int i = 0; i < 6979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6980(int& val) {
    const int x = 6980;
    std::cout << 6980;
    for (int i = 0; i < 6980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6981(int& val) {
    const int x = 6981;
    std::cout << 6981;
    for (int i = 0; i < 6981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6982(int& val) {
    const int x = 6982;
    std::cout << 6982;
    for (int i = 0; i < 6982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6983(int& val) {
    const int x = 6983;
    std::cout << 6983;
    for (int i = 0; i < 6983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6984(int& val) {
    const int x = 6984;
    std::cout << 6984;
    for (int i = 0; i < 6984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6985(int& val) {
    const int x = 6985;
    std::cout << 6985;
    for (int i = 0; i < 6985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6986(int& val) {
    const int x = 6986;
    std::cout << 6986;
    for (int i = 0; i < 6986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6987(int& val) {
    const int x = 6987;
    std::cout << 6987;
    for (int i = 0; i < 6987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6988(int& val) {
    const int x = 6988;
    std::cout << 6988;
    for (int i = 0; i < 6988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6989(int& val) {
    const int x = 6989;
    std::cout << 6989;
    for (int i = 0; i < 6989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6990(int& val) {
    const int x = 6990;
    std::cout << 6990;
    for (int i = 0; i < 6990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6991(int& val) {
    const int x = 6991;
    std::cout << 6991;
    for (int i = 0; i < 6991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6992(int& val) {
    const int x = 6992;
    std::cout << 6992;
    for (int i = 0; i < 6992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6993(int& val) {
    const int x = 6993;
    std::cout << 6993;
    for (int i = 0; i < 6993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6994(int& val) {
    const int x = 6994;
    std::cout << 6994;
    for (int i = 0; i < 6994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6995(int& val) {
    const int x = 6995;
    std::cout << 6995;
    for (int i = 0; i < 6995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6996(int& val) {
    const int x = 6996;
    std::cout << 6996;
    for (int i = 0; i < 6996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6997(int& val) {
    const int x = 6997;
    std::cout << 6997;
    for (int i = 0; i < 6997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6998(int& val) {
    const int x = 6998;
    std::cout << 6998;
    for (int i = 0; i < 6998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6999(int& val) {
    const int x = 6999;
    std::cout << 6999;
    for (int i = 0; i < 6999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7000(int& val) {
    const int x = 7000;
    std::cout << 7000;
    for (int i = 0; i < 7000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7001(int& val) {
    const int x = 7001;
    std::cout << 7001;
    for (int i = 0; i < 7001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7002(int& val) {
    const int x = 7002;
    std::cout << 7002;
    for (int i = 0; i < 7002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7003(int& val) {
    const int x = 7003;
    std::cout << 7003;
    for (int i = 0; i < 7003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7004(int& val) {
    const int x = 7004;
    std::cout << 7004;
    for (int i = 0; i < 7004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7005(int& val) {
    const int x = 7005;
    std::cout << 7005;
    for (int i = 0; i < 7005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7006(int& val) {
    const int x = 7006;
    std::cout << 7006;
    for (int i = 0; i < 7006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7007(int& val) {
    const int x = 7007;
    std::cout << 7007;
    for (int i = 0; i < 7007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7008(int& val) {
    const int x = 7008;
    std::cout << 7008;
    for (int i = 0; i < 7008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7009(int& val) {
    const int x = 7009;
    std::cout << 7009;
    for (int i = 0; i < 7009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7010(int& val) {
    const int x = 7010;
    std::cout << 7010;
    for (int i = 0; i < 7010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7011(int& val) {
    const int x = 7011;
    std::cout << 7011;
    for (int i = 0; i < 7011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7012(int& val) {
    const int x = 7012;
    std::cout << 7012;
    for (int i = 0; i < 7012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7013(int& val) {
    const int x = 7013;
    std::cout << 7013;
    for (int i = 0; i < 7013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7014(int& val) {
    const int x = 7014;
    std::cout << 7014;
    for (int i = 0; i < 7014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7015(int& val) {
    const int x = 7015;
    std::cout << 7015;
    for (int i = 0; i < 7015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7016(int& val) {
    const int x = 7016;
    std::cout << 7016;
    for (int i = 0; i < 7016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7017(int& val) {
    const int x = 7017;
    std::cout << 7017;
    for (int i = 0; i < 7017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7018(int& val) {
    const int x = 7018;
    std::cout << 7018;
    for (int i = 0; i < 7018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7019(int& val) {
    const int x = 7019;
    std::cout << 7019;
    for (int i = 0; i < 7019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7020(int& val) {
    const int x = 7020;
    std::cout << 7020;
    for (int i = 0; i < 7020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7021(int& val) {
    const int x = 7021;
    std::cout << 7021;
    for (int i = 0; i < 7021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7022(int& val) {
    const int x = 7022;
    std::cout << 7022;
    for (int i = 0; i < 7022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7023(int& val) {
    const int x = 7023;
    std::cout << 7023;
    for (int i = 0; i < 7023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7024(int& val) {
    const int x = 7024;
    std::cout << 7024;
    for (int i = 0; i < 7024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7025(int& val) {
    const int x = 7025;
    std::cout << 7025;
    for (int i = 0; i < 7025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7026(int& val) {
    const int x = 7026;
    std::cout << 7026;
    for (int i = 0; i < 7026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7027(int& val) {
    const int x = 7027;
    std::cout << 7027;
    for (int i = 0; i < 7027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7028(int& val) {
    const int x = 7028;
    std::cout << 7028;
    for (int i = 0; i < 7028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7029(int& val) {
    const int x = 7029;
    std::cout << 7029;
    for (int i = 0; i < 7029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7030(int& val) {
    const int x = 7030;
    std::cout << 7030;
    for (int i = 0; i < 7030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7031(int& val) {
    const int x = 7031;
    std::cout << 7031;
    for (int i = 0; i < 7031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7032(int& val) {
    const int x = 7032;
    std::cout << 7032;
    for (int i = 0; i < 7032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7033(int& val) {
    const int x = 7033;
    std::cout << 7033;
    for (int i = 0; i < 7033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7034(int& val) {
    const int x = 7034;
    std::cout << 7034;
    for (int i = 0; i < 7034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7035(int& val) {
    const int x = 7035;
    std::cout << 7035;
    for (int i = 0; i < 7035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7036(int& val) {
    const int x = 7036;
    std::cout << 7036;
    for (int i = 0; i < 7036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7037(int& val) {
    const int x = 7037;
    std::cout << 7037;
    for (int i = 0; i < 7037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7038(int& val) {
    const int x = 7038;
    std::cout << 7038;
    for (int i = 0; i < 7038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7039(int& val) {
    const int x = 7039;
    std::cout << 7039;
    for (int i = 0; i < 7039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7040(int& val) {
    const int x = 7040;
    std::cout << 7040;
    for (int i = 0; i < 7040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7041(int& val) {
    const int x = 7041;
    std::cout << 7041;
    for (int i = 0; i < 7041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7042(int& val) {
    const int x = 7042;
    std::cout << 7042;
    for (int i = 0; i < 7042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7043(int& val) {
    const int x = 7043;
    std::cout << 7043;
    for (int i = 0; i < 7043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7044(int& val) {
    const int x = 7044;
    std::cout << 7044;
    for (int i = 0; i < 7044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7045(int& val) {
    const int x = 7045;
    std::cout << 7045;
    for (int i = 0; i < 7045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7046(int& val) {
    const int x = 7046;
    std::cout << 7046;
    for (int i = 0; i < 7046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7047(int& val) {
    const int x = 7047;
    std::cout << 7047;
    for (int i = 0; i < 7047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7048(int& val) {
    const int x = 7048;
    std::cout << 7048;
    for (int i = 0; i < 7048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7049(int& val) {
    const int x = 7049;
    std::cout << 7049;
    for (int i = 0; i < 7049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7050(int& val) {
    const int x = 7050;
    std::cout << 7050;
    for (int i = 0; i < 7050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7051(int& val) {
    const int x = 7051;
    std::cout << 7051;
    for (int i = 0; i < 7051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7052(int& val) {
    const int x = 7052;
    std::cout << 7052;
    for (int i = 0; i < 7052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7053(int& val) {
    const int x = 7053;
    std::cout << 7053;
    for (int i = 0; i < 7053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7054(int& val) {
    const int x = 7054;
    std::cout << 7054;
    for (int i = 0; i < 7054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7055(int& val) {
    const int x = 7055;
    std::cout << 7055;
    for (int i = 0; i < 7055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7056(int& val) {
    const int x = 7056;
    std::cout << 7056;
    for (int i = 0; i < 7056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7057(int& val) {
    const int x = 7057;
    std::cout << 7057;
    for (int i = 0; i < 7057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7058(int& val) {
    const int x = 7058;
    std::cout << 7058;
    for (int i = 0; i < 7058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7059(int& val) {
    const int x = 7059;
    std::cout << 7059;
    for (int i = 0; i < 7059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7060(int& val) {
    const int x = 7060;
    std::cout << 7060;
    for (int i = 0; i < 7060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7061(int& val) {
    const int x = 7061;
    std::cout << 7061;
    for (int i = 0; i < 7061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7062(int& val) {
    const int x = 7062;
    std::cout << 7062;
    for (int i = 0; i < 7062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7063(int& val) {
    const int x = 7063;
    std::cout << 7063;
    for (int i = 0; i < 7063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7064(int& val) {
    const int x = 7064;
    std::cout << 7064;
    for (int i = 0; i < 7064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7065(int& val) {
    const int x = 7065;
    std::cout << 7065;
    for (int i = 0; i < 7065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7066(int& val) {
    const int x = 7066;
    std::cout << 7066;
    for (int i = 0; i < 7066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7067(int& val) {
    const int x = 7067;
    std::cout << 7067;
    for (int i = 0; i < 7067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7068(int& val) {
    const int x = 7068;
    std::cout << 7068;
    for (int i = 0; i < 7068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7069(int& val) {
    const int x = 7069;
    std::cout << 7069;
    for (int i = 0; i < 7069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7070(int& val) {
    const int x = 7070;
    std::cout << 7070;
    for (int i = 0; i < 7070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7071(int& val) {
    const int x = 7071;
    std::cout << 7071;
    for (int i = 0; i < 7071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7072(int& val) {
    const int x = 7072;
    std::cout << 7072;
    for (int i = 0; i < 7072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7073(int& val) {
    const int x = 7073;
    std::cout << 7073;
    for (int i = 0; i < 7073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7074(int& val) {
    const int x = 7074;
    std::cout << 7074;
    for (int i = 0; i < 7074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7075(int& val) {
    const int x = 7075;
    std::cout << 7075;
    for (int i = 0; i < 7075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7076(int& val) {
    const int x = 7076;
    std::cout << 7076;
    for (int i = 0; i < 7076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7077(int& val) {
    const int x = 7077;
    std::cout << 7077;
    for (int i = 0; i < 7077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7078(int& val) {
    const int x = 7078;
    std::cout << 7078;
    for (int i = 0; i < 7078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7079(int& val) {
    const int x = 7079;
    std::cout << 7079;
    for (int i = 0; i < 7079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7080(int& val) {
    const int x = 7080;
    std::cout << 7080;
    for (int i = 0; i < 7080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7081(int& val) {
    const int x = 7081;
    std::cout << 7081;
    for (int i = 0; i < 7081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7082(int& val) {
    const int x = 7082;
    std::cout << 7082;
    for (int i = 0; i < 7082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7083(int& val) {
    const int x = 7083;
    std::cout << 7083;
    for (int i = 0; i < 7083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7084(int& val) {
    const int x = 7084;
    std::cout << 7084;
    for (int i = 0; i < 7084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7085(int& val) {
    const int x = 7085;
    std::cout << 7085;
    for (int i = 0; i < 7085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7086(int& val) {
    const int x = 7086;
    std::cout << 7086;
    for (int i = 0; i < 7086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7087(int& val) {
    const int x = 7087;
    std::cout << 7087;
    for (int i = 0; i < 7087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7088(int& val) {
    const int x = 7088;
    std::cout << 7088;
    for (int i = 0; i < 7088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7089(int& val) {
    const int x = 7089;
    std::cout << 7089;
    for (int i = 0; i < 7089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7090(int& val) {
    const int x = 7090;
    std::cout << 7090;
    for (int i = 0; i < 7090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7091(int& val) {
    const int x = 7091;
    std::cout << 7091;
    for (int i = 0; i < 7091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7092(int& val) {
    const int x = 7092;
    std::cout << 7092;
    for (int i = 0; i < 7092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7093(int& val) {
    const int x = 7093;
    std::cout << 7093;
    for (int i = 0; i < 7093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7094(int& val) {
    const int x = 7094;
    std::cout << 7094;
    for (int i = 0; i < 7094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7095(int& val) {
    const int x = 7095;
    std::cout << 7095;
    for (int i = 0; i < 7095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7096(int& val) {
    const int x = 7096;
    std::cout << 7096;
    for (int i = 0; i < 7096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7097(int& val) {
    const int x = 7097;
    std::cout << 7097;
    for (int i = 0; i < 7097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7098(int& val) {
    const int x = 7098;
    std::cout << 7098;
    for (int i = 0; i < 7098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7099(int& val) {
    const int x = 7099;
    std::cout << 7099;
    for (int i = 0; i < 7099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7100(int& val) {
    const int x = 7100;
    std::cout << 7100;
    for (int i = 0; i < 7100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7101(int& val) {
    const int x = 7101;
    std::cout << 7101;
    for (int i = 0; i < 7101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7102(int& val) {
    const int x = 7102;
    std::cout << 7102;
    for (int i = 0; i < 7102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7103(int& val) {
    const int x = 7103;
    std::cout << 7103;
    for (int i = 0; i < 7103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7104(int& val) {
    const int x = 7104;
    std::cout << 7104;
    for (int i = 0; i < 7104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7105(int& val) {
    const int x = 7105;
    std::cout << 7105;
    for (int i = 0; i < 7105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7106(int& val) {
    const int x = 7106;
    std::cout << 7106;
    for (int i = 0; i < 7106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7107(int& val) {
    const int x = 7107;
    std::cout << 7107;
    for (int i = 0; i < 7107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7108(int& val) {
    const int x = 7108;
    std::cout << 7108;
    for (int i = 0; i < 7108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7109(int& val) {
    const int x = 7109;
    std::cout << 7109;
    for (int i = 0; i < 7109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7110(int& val) {
    const int x = 7110;
    std::cout << 7110;
    for (int i = 0; i < 7110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7111(int& val) {
    const int x = 7111;
    std::cout << 7111;
    for (int i = 0; i < 7111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7112(int& val) {
    const int x = 7112;
    std::cout << 7112;
    for (int i = 0; i < 7112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7113(int& val) {
    const int x = 7113;
    std::cout << 7113;
    for (int i = 0; i < 7113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7114(int& val) {
    const int x = 7114;
    std::cout << 7114;
    for (int i = 0; i < 7114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7115(int& val) {
    const int x = 7115;
    std::cout << 7115;
    for (int i = 0; i < 7115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7116(int& val) {
    const int x = 7116;
    std::cout << 7116;
    for (int i = 0; i < 7116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7117(int& val) {
    const int x = 7117;
    std::cout << 7117;
    for (int i = 0; i < 7117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7118(int& val) {
    const int x = 7118;
    std::cout << 7118;
    for (int i = 0; i < 7118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7119(int& val) {
    const int x = 7119;
    std::cout << 7119;
    for (int i = 0; i < 7119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7120(int& val) {
    const int x = 7120;
    std::cout << 7120;
    for (int i = 0; i < 7120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7121(int& val) {
    const int x = 7121;
    std::cout << 7121;
    for (int i = 0; i < 7121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7122(int& val) {
    const int x = 7122;
    std::cout << 7122;
    for (int i = 0; i < 7122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7123(int& val) {
    const int x = 7123;
    std::cout << 7123;
    for (int i = 0; i < 7123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7124(int& val) {
    const int x = 7124;
    std::cout << 7124;
    for (int i = 0; i < 7124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7125(int& val) {
    const int x = 7125;
    std::cout << 7125;
    for (int i = 0; i < 7125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7126(int& val) {
    const int x = 7126;
    std::cout << 7126;
    for (int i = 0; i < 7126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7127(int& val) {
    const int x = 7127;
    std::cout << 7127;
    for (int i = 0; i < 7127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7128(int& val) {
    const int x = 7128;
    std::cout << 7128;
    for (int i = 0; i < 7128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7129(int& val) {
    const int x = 7129;
    std::cout << 7129;
    for (int i = 0; i < 7129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7130(int& val) {
    const int x = 7130;
    std::cout << 7130;
    for (int i = 0; i < 7130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7131(int& val) {
    const int x = 7131;
    std::cout << 7131;
    for (int i = 0; i < 7131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7132(int& val) {
    const int x = 7132;
    std::cout << 7132;
    for (int i = 0; i < 7132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7133(int& val) {
    const int x = 7133;
    std::cout << 7133;
    for (int i = 0; i < 7133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7134(int& val) {
    const int x = 7134;
    std::cout << 7134;
    for (int i = 0; i < 7134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7135(int& val) {
    const int x = 7135;
    std::cout << 7135;
    for (int i = 0; i < 7135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7136(int& val) {
    const int x = 7136;
    std::cout << 7136;
    for (int i = 0; i < 7136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7137(int& val) {
    const int x = 7137;
    std::cout << 7137;
    for (int i = 0; i < 7137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7138(int& val) {
    const int x = 7138;
    std::cout << 7138;
    for (int i = 0; i < 7138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7139(int& val) {
    const int x = 7139;
    std::cout << 7139;
    for (int i = 0; i < 7139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7140(int& val) {
    const int x = 7140;
    std::cout << 7140;
    for (int i = 0; i < 7140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7141(int& val) {
    const int x = 7141;
    std::cout << 7141;
    for (int i = 0; i < 7141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7142(int& val) {
    const int x = 7142;
    std::cout << 7142;
    for (int i = 0; i < 7142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7143(int& val) {
    const int x = 7143;
    std::cout << 7143;
    for (int i = 0; i < 7143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7144(int& val) {
    const int x = 7144;
    std::cout << 7144;
    for (int i = 0; i < 7144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7145(int& val) {
    const int x = 7145;
    std::cout << 7145;
    for (int i = 0; i < 7145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7146(int& val) {
    const int x = 7146;
    std::cout << 7146;
    for (int i = 0; i < 7146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7147(int& val) {
    const int x = 7147;
    std::cout << 7147;
    for (int i = 0; i < 7147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7148(int& val) {
    const int x = 7148;
    std::cout << 7148;
    for (int i = 0; i < 7148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7149(int& val) {
    const int x = 7149;
    std::cout << 7149;
    for (int i = 0; i < 7149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7150(int& val) {
    const int x = 7150;
    std::cout << 7150;
    for (int i = 0; i < 7150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7151(int& val) {
    const int x = 7151;
    std::cout << 7151;
    for (int i = 0; i < 7151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7152(int& val) {
    const int x = 7152;
    std::cout << 7152;
    for (int i = 0; i < 7152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7153(int& val) {
    const int x = 7153;
    std::cout << 7153;
    for (int i = 0; i < 7153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7154(int& val) {
    const int x = 7154;
    std::cout << 7154;
    for (int i = 0; i < 7154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7155(int& val) {
    const int x = 7155;
    std::cout << 7155;
    for (int i = 0; i < 7155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7156(int& val) {
    const int x = 7156;
    std::cout << 7156;
    for (int i = 0; i < 7156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7157(int& val) {
    const int x = 7157;
    std::cout << 7157;
    for (int i = 0; i < 7157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7158(int& val) {
    const int x = 7158;
    std::cout << 7158;
    for (int i = 0; i < 7158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7159(int& val) {
    const int x = 7159;
    std::cout << 7159;
    for (int i = 0; i < 7159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7160(int& val) {
    const int x = 7160;
    std::cout << 7160;
    for (int i = 0; i < 7160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7161(int& val) {
    const int x = 7161;
    std::cout << 7161;
    for (int i = 0; i < 7161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7162(int& val) {
    const int x = 7162;
    std::cout << 7162;
    for (int i = 0; i < 7162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7163(int& val) {
    const int x = 7163;
    std::cout << 7163;
    for (int i = 0; i < 7163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7164(int& val) {
    const int x = 7164;
    std::cout << 7164;
    for (int i = 0; i < 7164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7165(int& val) {
    const int x = 7165;
    std::cout << 7165;
    for (int i = 0; i < 7165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7166(int& val) {
    const int x = 7166;
    std::cout << 7166;
    for (int i = 0; i < 7166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7167(int& val) {
    const int x = 7167;
    std::cout << 7167;
    for (int i = 0; i < 7167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7168(int& val) {
    const int x = 7168;
    std::cout << 7168;
    for (int i = 0; i < 7168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7169(int& val) {
    const int x = 7169;
    std::cout << 7169;
    for (int i = 0; i < 7169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7170(int& val) {
    const int x = 7170;
    std::cout << 7170;
    for (int i = 0; i < 7170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7171(int& val) {
    const int x = 7171;
    std::cout << 7171;
    for (int i = 0; i < 7171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7172(int& val) {
    const int x = 7172;
    std::cout << 7172;
    for (int i = 0; i < 7172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7173(int& val) {
    const int x = 7173;
    std::cout << 7173;
    for (int i = 0; i < 7173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7174(int& val) {
    const int x = 7174;
    std::cout << 7174;
    for (int i = 0; i < 7174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7175(int& val) {
    const int x = 7175;
    std::cout << 7175;
    for (int i = 0; i < 7175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7176(int& val) {
    const int x = 7176;
    std::cout << 7176;
    for (int i = 0; i < 7176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7177(int& val) {
    const int x = 7177;
    std::cout << 7177;
    for (int i = 0; i < 7177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7178(int& val) {
    const int x = 7178;
    std::cout << 7178;
    for (int i = 0; i < 7178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7179(int& val) {
    const int x = 7179;
    std::cout << 7179;
    for (int i = 0; i < 7179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7180(int& val) {
    const int x = 7180;
    std::cout << 7180;
    for (int i = 0; i < 7180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7181(int& val) {
    const int x = 7181;
    std::cout << 7181;
    for (int i = 0; i < 7181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7182(int& val) {
    const int x = 7182;
    std::cout << 7182;
    for (int i = 0; i < 7182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7183(int& val) {
    const int x = 7183;
    std::cout << 7183;
    for (int i = 0; i < 7183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7184(int& val) {
    const int x = 7184;
    std::cout << 7184;
    for (int i = 0; i < 7184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7185(int& val) {
    const int x = 7185;
    std::cout << 7185;
    for (int i = 0; i < 7185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7186(int& val) {
    const int x = 7186;
    std::cout << 7186;
    for (int i = 0; i < 7186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7187(int& val) {
    const int x = 7187;
    std::cout << 7187;
    for (int i = 0; i < 7187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7188(int& val) {
    const int x = 7188;
    std::cout << 7188;
    for (int i = 0; i < 7188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7189(int& val) {
    const int x = 7189;
    std::cout << 7189;
    for (int i = 0; i < 7189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7190(int& val) {
    const int x = 7190;
    std::cout << 7190;
    for (int i = 0; i < 7190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7191(int& val) {
    const int x = 7191;
    std::cout << 7191;
    for (int i = 0; i < 7191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7192(int& val) {
    const int x = 7192;
    std::cout << 7192;
    for (int i = 0; i < 7192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7193(int& val) {
    const int x = 7193;
    std::cout << 7193;
    for (int i = 0; i < 7193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7194(int& val) {
    const int x = 7194;
    std::cout << 7194;
    for (int i = 0; i < 7194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7195(int& val) {
    const int x = 7195;
    std::cout << 7195;
    for (int i = 0; i < 7195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7196(int& val) {
    const int x = 7196;
    std::cout << 7196;
    for (int i = 0; i < 7196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7197(int& val) {
    const int x = 7197;
    std::cout << 7197;
    for (int i = 0; i < 7197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7198(int& val) {
    const int x = 7198;
    std::cout << 7198;
    for (int i = 0; i < 7198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7199(int& val) {
    const int x = 7199;
    std::cout << 7199;
    for (int i = 0; i < 7199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7200(int& val) {
    const int x = 7200;
    std::cout << 7200;
    for (int i = 0; i < 7200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7201(int& val) {
    const int x = 7201;
    std::cout << 7201;
    for (int i = 0; i < 7201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7202(int& val) {
    const int x = 7202;
    std::cout << 7202;
    for (int i = 0; i < 7202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7203(int& val) {
    const int x = 7203;
    std::cout << 7203;
    for (int i = 0; i < 7203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7204(int& val) {
    const int x = 7204;
    std::cout << 7204;
    for (int i = 0; i < 7204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7205(int& val) {
    const int x = 7205;
    std::cout << 7205;
    for (int i = 0; i < 7205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7206(int& val) {
    const int x = 7206;
    std::cout << 7206;
    for (int i = 0; i < 7206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7207(int& val) {
    const int x = 7207;
    std::cout << 7207;
    for (int i = 0; i < 7207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7208(int& val) {
    const int x = 7208;
    std::cout << 7208;
    for (int i = 0; i < 7208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7209(int& val) {
    const int x = 7209;
    std::cout << 7209;
    for (int i = 0; i < 7209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7210(int& val) {
    const int x = 7210;
    std::cout << 7210;
    for (int i = 0; i < 7210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7211(int& val) {
    const int x = 7211;
    std::cout << 7211;
    for (int i = 0; i < 7211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7212(int& val) {
    const int x = 7212;
    std::cout << 7212;
    for (int i = 0; i < 7212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7213(int& val) {
    const int x = 7213;
    std::cout << 7213;
    for (int i = 0; i < 7213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7214(int& val) {
    const int x = 7214;
    std::cout << 7214;
    for (int i = 0; i < 7214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7215(int& val) {
    const int x = 7215;
    std::cout << 7215;
    for (int i = 0; i < 7215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7216(int& val) {
    const int x = 7216;
    std::cout << 7216;
    for (int i = 0; i < 7216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7217(int& val) {
    const int x = 7217;
    std::cout << 7217;
    for (int i = 0; i < 7217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7218(int& val) {
    const int x = 7218;
    std::cout << 7218;
    for (int i = 0; i < 7218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7219(int& val) {
    const int x = 7219;
    std::cout << 7219;
    for (int i = 0; i < 7219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7220(int& val) {
    const int x = 7220;
    std::cout << 7220;
    for (int i = 0; i < 7220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7221(int& val) {
    const int x = 7221;
    std::cout << 7221;
    for (int i = 0; i < 7221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7222(int& val) {
    const int x = 7222;
    std::cout << 7222;
    for (int i = 0; i < 7222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7223(int& val) {
    const int x = 7223;
    std::cout << 7223;
    for (int i = 0; i < 7223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7224(int& val) {
    const int x = 7224;
    std::cout << 7224;
    for (int i = 0; i < 7224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7225(int& val) {
    const int x = 7225;
    std::cout << 7225;
    for (int i = 0; i < 7225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7226(int& val) {
    const int x = 7226;
    std::cout << 7226;
    for (int i = 0; i < 7226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7227(int& val) {
    const int x = 7227;
    std::cout << 7227;
    for (int i = 0; i < 7227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7228(int& val) {
    const int x = 7228;
    std::cout << 7228;
    for (int i = 0; i < 7228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7229(int& val) {
    const int x = 7229;
    std::cout << 7229;
    for (int i = 0; i < 7229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7230(int& val) {
    const int x = 7230;
    std::cout << 7230;
    for (int i = 0; i < 7230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7231(int& val) {
    const int x = 7231;
    std::cout << 7231;
    for (int i = 0; i < 7231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7232(int& val) {
    const int x = 7232;
    std::cout << 7232;
    for (int i = 0; i < 7232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7233(int& val) {
    const int x = 7233;
    std::cout << 7233;
    for (int i = 0; i < 7233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7234(int& val) {
    const int x = 7234;
    std::cout << 7234;
    for (int i = 0; i < 7234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7235(int& val) {
    const int x = 7235;
    std::cout << 7235;
    for (int i = 0; i < 7235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7236(int& val) {
    const int x = 7236;
    std::cout << 7236;
    for (int i = 0; i < 7236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7237(int& val) {
    const int x = 7237;
    std::cout << 7237;
    for (int i = 0; i < 7237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7238(int& val) {
    const int x = 7238;
    std::cout << 7238;
    for (int i = 0; i < 7238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7239(int& val) {
    const int x = 7239;
    std::cout << 7239;
    for (int i = 0; i < 7239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7240(int& val) {
    const int x = 7240;
    std::cout << 7240;
    for (int i = 0; i < 7240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7241(int& val) {
    const int x = 7241;
    std::cout << 7241;
    for (int i = 0; i < 7241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7242(int& val) {
    const int x = 7242;
    std::cout << 7242;
    for (int i = 0; i < 7242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7243(int& val) {
    const int x = 7243;
    std::cout << 7243;
    for (int i = 0; i < 7243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7244(int& val) {
    const int x = 7244;
    std::cout << 7244;
    for (int i = 0; i < 7244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7245(int& val) {
    const int x = 7245;
    std::cout << 7245;
    for (int i = 0; i < 7245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7246(int& val) {
    const int x = 7246;
    std::cout << 7246;
    for (int i = 0; i < 7246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7247(int& val) {
    const int x = 7247;
    std::cout << 7247;
    for (int i = 0; i < 7247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7248(int& val) {
    const int x = 7248;
    std::cout << 7248;
    for (int i = 0; i < 7248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7249(int& val) {
    const int x = 7249;
    std::cout << 7249;
    for (int i = 0; i < 7249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7250(int& val) {
    const int x = 7250;
    std::cout << 7250;
    for (int i = 0; i < 7250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7251(int& val) {
    const int x = 7251;
    std::cout << 7251;
    for (int i = 0; i < 7251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7252(int& val) {
    const int x = 7252;
    std::cout << 7252;
    for (int i = 0; i < 7252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7253(int& val) {
    const int x = 7253;
    std::cout << 7253;
    for (int i = 0; i < 7253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7254(int& val) {
    const int x = 7254;
    std::cout << 7254;
    for (int i = 0; i < 7254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7255(int& val) {
    const int x = 7255;
    std::cout << 7255;
    for (int i = 0; i < 7255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7256(int& val) {
    const int x = 7256;
    std::cout << 7256;
    for (int i = 0; i < 7256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7257(int& val) {
    const int x = 7257;
    std::cout << 7257;
    for (int i = 0; i < 7257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7258(int& val) {
    const int x = 7258;
    std::cout << 7258;
    for (int i = 0; i < 7258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7259(int& val) {
    const int x = 7259;
    std::cout << 7259;
    for (int i = 0; i < 7259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7260(int& val) {
    const int x = 7260;
    std::cout << 7260;
    for (int i = 0; i < 7260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7261(int& val) {
    const int x = 7261;
    std::cout << 7261;
    for (int i = 0; i < 7261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7262(int& val) {
    const int x = 7262;
    std::cout << 7262;
    for (int i = 0; i < 7262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7263(int& val) {
    const int x = 7263;
    std::cout << 7263;
    for (int i = 0; i < 7263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7264(int& val) {
    const int x = 7264;
    std::cout << 7264;
    for (int i = 0; i < 7264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7265(int& val) {
    const int x = 7265;
    std::cout << 7265;
    for (int i = 0; i < 7265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7266(int& val) {
    const int x = 7266;
    std::cout << 7266;
    for (int i = 0; i < 7266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7267(int& val) {
    const int x = 7267;
    std::cout << 7267;
    for (int i = 0; i < 7267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7268(int& val) {
    const int x = 7268;
    std::cout << 7268;
    for (int i = 0; i < 7268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7269(int& val) {
    const int x = 7269;
    std::cout << 7269;
    for (int i = 0; i < 7269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7270(int& val) {
    const int x = 7270;
    std::cout << 7270;
    for (int i = 0; i < 7270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7271(int& val) {
    const int x = 7271;
    std::cout << 7271;
    for (int i = 0; i < 7271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7272(int& val) {
    const int x = 7272;
    std::cout << 7272;
    for (int i = 0; i < 7272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7273(int& val) {
    const int x = 7273;
    std::cout << 7273;
    for (int i = 0; i < 7273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7274(int& val) {
    const int x = 7274;
    std::cout << 7274;
    for (int i = 0; i < 7274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7275(int& val) {
    const int x = 7275;
    std::cout << 7275;
    for (int i = 0; i < 7275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7276(int& val) {
    const int x = 7276;
    std::cout << 7276;
    for (int i = 0; i < 7276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7277(int& val) {
    const int x = 7277;
    std::cout << 7277;
    for (int i = 0; i < 7277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7278(int& val) {
    const int x = 7278;
    std::cout << 7278;
    for (int i = 0; i < 7278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7279(int& val) {
    const int x = 7279;
    std::cout << 7279;
    for (int i = 0; i < 7279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7280(int& val) {
    const int x = 7280;
    std::cout << 7280;
    for (int i = 0; i < 7280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7281(int& val) {
    const int x = 7281;
    std::cout << 7281;
    for (int i = 0; i < 7281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7282(int& val) {
    const int x = 7282;
    std::cout << 7282;
    for (int i = 0; i < 7282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7283(int& val) {
    const int x = 7283;
    std::cout << 7283;
    for (int i = 0; i < 7283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7284(int& val) {
    const int x = 7284;
    std::cout << 7284;
    for (int i = 0; i < 7284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7285(int& val) {
    const int x = 7285;
    std::cout << 7285;
    for (int i = 0; i < 7285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7286(int& val) {
    const int x = 7286;
    std::cout << 7286;
    for (int i = 0; i < 7286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7287(int& val) {
    const int x = 7287;
    std::cout << 7287;
    for (int i = 0; i < 7287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7288(int& val) {
    const int x = 7288;
    std::cout << 7288;
    for (int i = 0; i < 7288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7289(int& val) {
    const int x = 7289;
    std::cout << 7289;
    for (int i = 0; i < 7289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7290(int& val) {
    const int x = 7290;
    std::cout << 7290;
    for (int i = 0; i < 7290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7291(int& val) {
    const int x = 7291;
    std::cout << 7291;
    for (int i = 0; i < 7291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7292(int& val) {
    const int x = 7292;
    std::cout << 7292;
    for (int i = 0; i < 7292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7293(int& val) {
    const int x = 7293;
    std::cout << 7293;
    for (int i = 0; i < 7293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7294(int& val) {
    const int x = 7294;
    std::cout << 7294;
    for (int i = 0; i < 7294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7295(int& val) {
    const int x = 7295;
    std::cout << 7295;
    for (int i = 0; i < 7295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7296(int& val) {
    const int x = 7296;
    std::cout << 7296;
    for (int i = 0; i < 7296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7297(int& val) {
    const int x = 7297;
    std::cout << 7297;
    for (int i = 0; i < 7297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7298(int& val) {
    const int x = 7298;
    std::cout << 7298;
    for (int i = 0; i < 7298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7299(int& val) {
    const int x = 7299;
    std::cout << 7299;
    for (int i = 0; i < 7299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7300(int& val) {
    const int x = 7300;
    std::cout << 7300;
    for (int i = 0; i < 7300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7301(int& val) {
    const int x = 7301;
    std::cout << 7301;
    for (int i = 0; i < 7301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7302(int& val) {
    const int x = 7302;
    std::cout << 7302;
    for (int i = 0; i < 7302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7303(int& val) {
    const int x = 7303;
    std::cout << 7303;
    for (int i = 0; i < 7303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7304(int& val) {
    const int x = 7304;
    std::cout << 7304;
    for (int i = 0; i < 7304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7305(int& val) {
    const int x = 7305;
    std::cout << 7305;
    for (int i = 0; i < 7305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7306(int& val) {
    const int x = 7306;
    std::cout << 7306;
    for (int i = 0; i < 7306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7307(int& val) {
    const int x = 7307;
    std::cout << 7307;
    for (int i = 0; i < 7307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7308(int& val) {
    const int x = 7308;
    std::cout << 7308;
    for (int i = 0; i < 7308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7309(int& val) {
    const int x = 7309;
    std::cout << 7309;
    for (int i = 0; i < 7309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7310(int& val) {
    const int x = 7310;
    std::cout << 7310;
    for (int i = 0; i < 7310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7311(int& val) {
    const int x = 7311;
    std::cout << 7311;
    for (int i = 0; i < 7311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7312(int& val) {
    const int x = 7312;
    std::cout << 7312;
    for (int i = 0; i < 7312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7313(int& val) {
    const int x = 7313;
    std::cout << 7313;
    for (int i = 0; i < 7313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7314(int& val) {
    const int x = 7314;
    std::cout << 7314;
    for (int i = 0; i < 7314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7315(int& val) {
    const int x = 7315;
    std::cout << 7315;
    for (int i = 0; i < 7315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7316(int& val) {
    const int x = 7316;
    std::cout << 7316;
    for (int i = 0; i < 7316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7317(int& val) {
    const int x = 7317;
    std::cout << 7317;
    for (int i = 0; i < 7317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7318(int& val) {
    const int x = 7318;
    std::cout << 7318;
    for (int i = 0; i < 7318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7319(int& val) {
    const int x = 7319;
    std::cout << 7319;
    for (int i = 0; i < 7319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7320(int& val) {
    const int x = 7320;
    std::cout << 7320;
    for (int i = 0; i < 7320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7321(int& val) {
    const int x = 7321;
    std::cout << 7321;
    for (int i = 0; i < 7321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7322(int& val) {
    const int x = 7322;
    std::cout << 7322;
    for (int i = 0; i < 7322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7323(int& val) {
    const int x = 7323;
    std::cout << 7323;
    for (int i = 0; i < 7323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7324(int& val) {
    const int x = 7324;
    std::cout << 7324;
    for (int i = 0; i < 7324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7325(int& val) {
    const int x = 7325;
    std::cout << 7325;
    for (int i = 0; i < 7325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7326(int& val) {
    const int x = 7326;
    std::cout << 7326;
    for (int i = 0; i < 7326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7327(int& val) {
    const int x = 7327;
    std::cout << 7327;
    for (int i = 0; i < 7327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7328(int& val) {
    const int x = 7328;
    std::cout << 7328;
    for (int i = 0; i < 7328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7329(int& val) {
    const int x = 7329;
    std::cout << 7329;
    for (int i = 0; i < 7329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7330(int& val) {
    const int x = 7330;
    std::cout << 7330;
    for (int i = 0; i < 7330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7331(int& val) {
    const int x = 7331;
    std::cout << 7331;
    for (int i = 0; i < 7331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7332(int& val) {
    const int x = 7332;
    std::cout << 7332;
    for (int i = 0; i < 7332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7333(int& val) {
    const int x = 7333;
    std::cout << 7333;
    for (int i = 0; i < 7333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7334(int& val) {
    const int x = 7334;
    std::cout << 7334;
    for (int i = 0; i < 7334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7335(int& val) {
    const int x = 7335;
    std::cout << 7335;
    for (int i = 0; i < 7335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7336(int& val) {
    const int x = 7336;
    std::cout << 7336;
    for (int i = 0; i < 7336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7337(int& val) {
    const int x = 7337;
    std::cout << 7337;
    for (int i = 0; i < 7337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7338(int& val) {
    const int x = 7338;
    std::cout << 7338;
    for (int i = 0; i < 7338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7339(int& val) {
    const int x = 7339;
    std::cout << 7339;
    for (int i = 0; i < 7339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7340(int& val) {
    const int x = 7340;
    std::cout << 7340;
    for (int i = 0; i < 7340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7341(int& val) {
    const int x = 7341;
    std::cout << 7341;
    for (int i = 0; i < 7341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7342(int& val) {
    const int x = 7342;
    std::cout << 7342;
    for (int i = 0; i < 7342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7343(int& val) {
    const int x = 7343;
    std::cout << 7343;
    for (int i = 0; i < 7343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7344(int& val) {
    const int x = 7344;
    std::cout << 7344;
    for (int i = 0; i < 7344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7345(int& val) {
    const int x = 7345;
    std::cout << 7345;
    for (int i = 0; i < 7345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7346(int& val) {
    const int x = 7346;
    std::cout << 7346;
    for (int i = 0; i < 7346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7347(int& val) {
    const int x = 7347;
    std::cout << 7347;
    for (int i = 0; i < 7347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7348(int& val) {
    const int x = 7348;
    std::cout << 7348;
    for (int i = 0; i < 7348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7349(int& val) {
    const int x = 7349;
    std::cout << 7349;
    for (int i = 0; i < 7349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7350(int& val) {
    const int x = 7350;
    std::cout << 7350;
    for (int i = 0; i < 7350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7351(int& val) {
    const int x = 7351;
    std::cout << 7351;
    for (int i = 0; i < 7351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7352(int& val) {
    const int x = 7352;
    std::cout << 7352;
    for (int i = 0; i < 7352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7353(int& val) {
    const int x = 7353;
    std::cout << 7353;
    for (int i = 0; i < 7353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7354(int& val) {
    const int x = 7354;
    std::cout << 7354;
    for (int i = 0; i < 7354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7355(int& val) {
    const int x = 7355;
    std::cout << 7355;
    for (int i = 0; i < 7355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7356(int& val) {
    const int x = 7356;
    std::cout << 7356;
    for (int i = 0; i < 7356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7357(int& val) {
    const int x = 7357;
    std::cout << 7357;
    for (int i = 0; i < 7357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7358(int& val) {
    const int x = 7358;
    std::cout << 7358;
    for (int i = 0; i < 7358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7359(int& val) {
    const int x = 7359;
    std::cout << 7359;
    for (int i = 0; i < 7359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7360(int& val) {
    const int x = 7360;
    std::cout << 7360;
    for (int i = 0; i < 7360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7361(int& val) {
    const int x = 7361;
    std::cout << 7361;
    for (int i = 0; i < 7361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7362(int& val) {
    const int x = 7362;
    std::cout << 7362;
    for (int i = 0; i < 7362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7363(int& val) {
    const int x = 7363;
    std::cout << 7363;
    for (int i = 0; i < 7363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7364(int& val) {
    const int x = 7364;
    std::cout << 7364;
    for (int i = 0; i < 7364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7365(int& val) {
    const int x = 7365;
    std::cout << 7365;
    for (int i = 0; i < 7365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7366(int& val) {
    const int x = 7366;
    std::cout << 7366;
    for (int i = 0; i < 7366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7367(int& val) {
    const int x = 7367;
    std::cout << 7367;
    for (int i = 0; i < 7367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7368(int& val) {
    const int x = 7368;
    std::cout << 7368;
    for (int i = 0; i < 7368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7369(int& val) {
    const int x = 7369;
    std::cout << 7369;
    for (int i = 0; i < 7369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7370(int& val) {
    const int x = 7370;
    std::cout << 7370;
    for (int i = 0; i < 7370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7371(int& val) {
    const int x = 7371;
    std::cout << 7371;
    for (int i = 0; i < 7371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7372(int& val) {
    const int x = 7372;
    std::cout << 7372;
    for (int i = 0; i < 7372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7373(int& val) {
    const int x = 7373;
    std::cout << 7373;
    for (int i = 0; i < 7373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7374(int& val) {
    const int x = 7374;
    std::cout << 7374;
    for (int i = 0; i < 7374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7375(int& val) {
    const int x = 7375;
    std::cout << 7375;
    for (int i = 0; i < 7375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7376(int& val) {
    const int x = 7376;
    std::cout << 7376;
    for (int i = 0; i < 7376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7377(int& val) {
    const int x = 7377;
    std::cout << 7377;
    for (int i = 0; i < 7377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7378(int& val) {
    const int x = 7378;
    std::cout << 7378;
    for (int i = 0; i < 7378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7379(int& val) {
    const int x = 7379;
    std::cout << 7379;
    for (int i = 0; i < 7379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7380(int& val) {
    const int x = 7380;
    std::cout << 7380;
    for (int i = 0; i < 7380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7381(int& val) {
    const int x = 7381;
    std::cout << 7381;
    for (int i = 0; i < 7381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7382(int& val) {
    const int x = 7382;
    std::cout << 7382;
    for (int i = 0; i < 7382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7383(int& val) {
    const int x = 7383;
    std::cout << 7383;
    for (int i = 0; i < 7383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7384(int& val) {
    const int x = 7384;
    std::cout << 7384;
    for (int i = 0; i < 7384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7385(int& val) {
    const int x = 7385;
    std::cout << 7385;
    for (int i = 0; i < 7385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7386(int& val) {
    const int x = 7386;
    std::cout << 7386;
    for (int i = 0; i < 7386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7387(int& val) {
    const int x = 7387;
    std::cout << 7387;
    for (int i = 0; i < 7387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7388(int& val) {
    const int x = 7388;
    std::cout << 7388;
    for (int i = 0; i < 7388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7389(int& val) {
    const int x = 7389;
    std::cout << 7389;
    for (int i = 0; i < 7389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7390(int& val) {
    const int x = 7390;
    std::cout << 7390;
    for (int i = 0; i < 7390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7391(int& val) {
    const int x = 7391;
    std::cout << 7391;
    for (int i = 0; i < 7391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7392(int& val) {
    const int x = 7392;
    std::cout << 7392;
    for (int i = 0; i < 7392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7393(int& val) {
    const int x = 7393;
    std::cout << 7393;
    for (int i = 0; i < 7393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7394(int& val) {
    const int x = 7394;
    std::cout << 7394;
    for (int i = 0; i < 7394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7395(int& val) {
    const int x = 7395;
    std::cout << 7395;
    for (int i = 0; i < 7395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7396(int& val) {
    const int x = 7396;
    std::cout << 7396;
    for (int i = 0; i < 7396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7397(int& val) {
    const int x = 7397;
    std::cout << 7397;
    for (int i = 0; i < 7397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7398(int& val) {
    const int x = 7398;
    std::cout << 7398;
    for (int i = 0; i < 7398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7399(int& val) {
    const int x = 7399;
    std::cout << 7399;
    for (int i = 0; i < 7399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7400(int& val) {
    const int x = 7400;
    std::cout << 7400;
    for (int i = 0; i < 7400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7401(int& val) {
    const int x = 7401;
    std::cout << 7401;
    for (int i = 0; i < 7401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7402(int& val) {
    const int x = 7402;
    std::cout << 7402;
    for (int i = 0; i < 7402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7403(int& val) {
    const int x = 7403;
    std::cout << 7403;
    for (int i = 0; i < 7403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7404(int& val) {
    const int x = 7404;
    std::cout << 7404;
    for (int i = 0; i < 7404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7405(int& val) {
    const int x = 7405;
    std::cout << 7405;
    for (int i = 0; i < 7405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7406(int& val) {
    const int x = 7406;
    std::cout << 7406;
    for (int i = 0; i < 7406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7407(int& val) {
    const int x = 7407;
    std::cout << 7407;
    for (int i = 0; i < 7407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7408(int& val) {
    const int x = 7408;
    std::cout << 7408;
    for (int i = 0; i < 7408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7409(int& val) {
    const int x = 7409;
    std::cout << 7409;
    for (int i = 0; i < 7409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7410(int& val) {
    const int x = 7410;
    std::cout << 7410;
    for (int i = 0; i < 7410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7411(int& val) {
    const int x = 7411;
    std::cout << 7411;
    for (int i = 0; i < 7411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7412(int& val) {
    const int x = 7412;
    std::cout << 7412;
    for (int i = 0; i < 7412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7413(int& val) {
    const int x = 7413;
    std::cout << 7413;
    for (int i = 0; i < 7413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7414(int& val) {
    const int x = 7414;
    std::cout << 7414;
    for (int i = 0; i < 7414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7415(int& val) {
    const int x = 7415;
    std::cout << 7415;
    for (int i = 0; i < 7415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7416(int& val) {
    const int x = 7416;
    std::cout << 7416;
    for (int i = 0; i < 7416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7417(int& val) {
    const int x = 7417;
    std::cout << 7417;
    for (int i = 0; i < 7417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7418(int& val) {
    const int x = 7418;
    std::cout << 7418;
    for (int i = 0; i < 7418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7419(int& val) {
    const int x = 7419;
    std::cout << 7419;
    for (int i = 0; i < 7419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7420(int& val) {
    const int x = 7420;
    std::cout << 7420;
    for (int i = 0; i < 7420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7421(int& val) {
    const int x = 7421;
    std::cout << 7421;
    for (int i = 0; i < 7421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7422(int& val) {
    const int x = 7422;
    std::cout << 7422;
    for (int i = 0; i < 7422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7423(int& val) {
    const int x = 7423;
    std::cout << 7423;
    for (int i = 0; i < 7423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7424(int& val) {
    const int x = 7424;
    std::cout << 7424;
    for (int i = 0; i < 7424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7425(int& val) {
    const int x = 7425;
    std::cout << 7425;
    for (int i = 0; i < 7425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7426(int& val) {
    const int x = 7426;
    std::cout << 7426;
    for (int i = 0; i < 7426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7427(int& val) {
    const int x = 7427;
    std::cout << 7427;
    for (int i = 0; i < 7427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7428(int& val) {
    const int x = 7428;
    std::cout << 7428;
    for (int i = 0; i < 7428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7429(int& val) {
    const int x = 7429;
    std::cout << 7429;
    for (int i = 0; i < 7429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7430(int& val) {
    const int x = 7430;
    std::cout << 7430;
    for (int i = 0; i < 7430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7431(int& val) {
    const int x = 7431;
    std::cout << 7431;
    for (int i = 0; i < 7431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7432(int& val) {
    const int x = 7432;
    std::cout << 7432;
    for (int i = 0; i < 7432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7433(int& val) {
    const int x = 7433;
    std::cout << 7433;
    for (int i = 0; i < 7433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7434(int& val) {
    const int x = 7434;
    std::cout << 7434;
    for (int i = 0; i < 7434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7435(int& val) {
    const int x = 7435;
    std::cout << 7435;
    for (int i = 0; i < 7435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7436(int& val) {
    const int x = 7436;
    std::cout << 7436;
    for (int i = 0; i < 7436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7437(int& val) {
    const int x = 7437;
    std::cout << 7437;
    for (int i = 0; i < 7437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7438(int& val) {
    const int x = 7438;
    std::cout << 7438;
    for (int i = 0; i < 7438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7439(int& val) {
    const int x = 7439;
    std::cout << 7439;
    for (int i = 0; i < 7439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7440(int& val) {
    const int x = 7440;
    std::cout << 7440;
    for (int i = 0; i < 7440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7441(int& val) {
    const int x = 7441;
    std::cout << 7441;
    for (int i = 0; i < 7441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7442(int& val) {
    const int x = 7442;
    std::cout << 7442;
    for (int i = 0; i < 7442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7443(int& val) {
    const int x = 7443;
    std::cout << 7443;
    for (int i = 0; i < 7443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7444(int& val) {
    const int x = 7444;
    std::cout << 7444;
    for (int i = 0; i < 7444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7445(int& val) {
    const int x = 7445;
    std::cout << 7445;
    for (int i = 0; i < 7445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7446(int& val) {
    const int x = 7446;
    std::cout << 7446;
    for (int i = 0; i < 7446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7447(int& val) {
    const int x = 7447;
    std::cout << 7447;
    for (int i = 0; i < 7447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7448(int& val) {
    const int x = 7448;
    std::cout << 7448;
    for (int i = 0; i < 7448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7449(int& val) {
    const int x = 7449;
    std::cout << 7449;
    for (int i = 0; i < 7449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7450(int& val) {
    const int x = 7450;
    std::cout << 7450;
    for (int i = 0; i < 7450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7451(int& val) {
    const int x = 7451;
    std::cout << 7451;
    for (int i = 0; i < 7451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7452(int& val) {
    const int x = 7452;
    std::cout << 7452;
    for (int i = 0; i < 7452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7453(int& val) {
    const int x = 7453;
    std::cout << 7453;
    for (int i = 0; i < 7453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7454(int& val) {
    const int x = 7454;
    std::cout << 7454;
    for (int i = 0; i < 7454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7455(int& val) {
    const int x = 7455;
    std::cout << 7455;
    for (int i = 0; i < 7455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7456(int& val) {
    const int x = 7456;
    std::cout << 7456;
    for (int i = 0; i < 7456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7457(int& val) {
    const int x = 7457;
    std::cout << 7457;
    for (int i = 0; i < 7457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7458(int& val) {
    const int x = 7458;
    std::cout << 7458;
    for (int i = 0; i < 7458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7459(int& val) {
    const int x = 7459;
    std::cout << 7459;
    for (int i = 0; i < 7459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7460(int& val) {
    const int x = 7460;
    std::cout << 7460;
    for (int i = 0; i < 7460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7461(int& val) {
    const int x = 7461;
    std::cout << 7461;
    for (int i = 0; i < 7461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7462(int& val) {
    const int x = 7462;
    std::cout << 7462;
    for (int i = 0; i < 7462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7463(int& val) {
    const int x = 7463;
    std::cout << 7463;
    for (int i = 0; i < 7463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7464(int& val) {
    const int x = 7464;
    std::cout << 7464;
    for (int i = 0; i < 7464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7465(int& val) {
    const int x = 7465;
    std::cout << 7465;
    for (int i = 0; i < 7465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7466(int& val) {
    const int x = 7466;
    std::cout << 7466;
    for (int i = 0; i < 7466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7467(int& val) {
    const int x = 7467;
    std::cout << 7467;
    for (int i = 0; i < 7467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7468(int& val) {
    const int x = 7468;
    std::cout << 7468;
    for (int i = 0; i < 7468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7469(int& val) {
    const int x = 7469;
    std::cout << 7469;
    for (int i = 0; i < 7469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7470(int& val) {
    const int x = 7470;
    std::cout << 7470;
    for (int i = 0; i < 7470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7471(int& val) {
    const int x = 7471;
    std::cout << 7471;
    for (int i = 0; i < 7471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7472(int& val) {
    const int x = 7472;
    std::cout << 7472;
    for (int i = 0; i < 7472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7473(int& val) {
    const int x = 7473;
    std::cout << 7473;
    for (int i = 0; i < 7473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7474(int& val) {
    const int x = 7474;
    std::cout << 7474;
    for (int i = 0; i < 7474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7475(int& val) {
    const int x = 7475;
    std::cout << 7475;
    for (int i = 0; i < 7475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7476(int& val) {
    const int x = 7476;
    std::cout << 7476;
    for (int i = 0; i < 7476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7477(int& val) {
    const int x = 7477;
    std::cout << 7477;
    for (int i = 0; i < 7477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7478(int& val) {
    const int x = 7478;
    std::cout << 7478;
    for (int i = 0; i < 7478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7479(int& val) {
    const int x = 7479;
    std::cout << 7479;
    for (int i = 0; i < 7479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7480(int& val) {
    const int x = 7480;
    std::cout << 7480;
    for (int i = 0; i < 7480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7481(int& val) {
    const int x = 7481;
    std::cout << 7481;
    for (int i = 0; i < 7481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7482(int& val) {
    const int x = 7482;
    std::cout << 7482;
    for (int i = 0; i < 7482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7483(int& val) {
    const int x = 7483;
    std::cout << 7483;
    for (int i = 0; i < 7483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7484(int& val) {
    const int x = 7484;
    std::cout << 7484;
    for (int i = 0; i < 7484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7485(int& val) {
    const int x = 7485;
    std::cout << 7485;
    for (int i = 0; i < 7485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7486(int& val) {
    const int x = 7486;
    std::cout << 7486;
    for (int i = 0; i < 7486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7487(int& val) {
    const int x = 7487;
    std::cout << 7487;
    for (int i = 0; i < 7487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7488(int& val) {
    const int x = 7488;
    std::cout << 7488;
    for (int i = 0; i < 7488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7489(int& val) {
    const int x = 7489;
    std::cout << 7489;
    for (int i = 0; i < 7489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7490(int& val) {
    const int x = 7490;
    std::cout << 7490;
    for (int i = 0; i < 7490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7491(int& val) {
    const int x = 7491;
    std::cout << 7491;
    for (int i = 0; i < 7491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7492(int& val) {
    const int x = 7492;
    std::cout << 7492;
    for (int i = 0; i < 7492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7493(int& val) {
    const int x = 7493;
    std::cout << 7493;
    for (int i = 0; i < 7493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7494(int& val) {
    const int x = 7494;
    std::cout << 7494;
    for (int i = 0; i < 7494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7495(int& val) {
    const int x = 7495;
    std::cout << 7495;
    for (int i = 0; i < 7495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7496(int& val) {
    const int x = 7496;
    std::cout << 7496;
    for (int i = 0; i < 7496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7497(int& val) {
    const int x = 7497;
    std::cout << 7497;
    for (int i = 0; i < 7497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7498(int& val) {
    const int x = 7498;
    std::cout << 7498;
    for (int i = 0; i < 7498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7499(int& val) {
    const int x = 7499;
    std::cout << 7499;
    for (int i = 0; i < 7499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7500(int& val) {
    const int x = 7500;
    std::cout << 7500;
    for (int i = 0; i < 7500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7501(int& val) {
    const int x = 7501;
    std::cout << 7501;
    for (int i = 0; i < 7501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7502(int& val) {
    const int x = 7502;
    std::cout << 7502;
    for (int i = 0; i < 7502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7503(int& val) {
    const int x = 7503;
    std::cout << 7503;
    for (int i = 0; i < 7503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7504(int& val) {
    const int x = 7504;
    std::cout << 7504;
    for (int i = 0; i < 7504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7505(int& val) {
    const int x = 7505;
    std::cout << 7505;
    for (int i = 0; i < 7505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7506(int& val) {
    const int x = 7506;
    std::cout << 7506;
    for (int i = 0; i < 7506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7507(int& val) {
    const int x = 7507;
    std::cout << 7507;
    for (int i = 0; i < 7507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7508(int& val) {
    const int x = 7508;
    std::cout << 7508;
    for (int i = 0; i < 7508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7509(int& val) {
    const int x = 7509;
    std::cout << 7509;
    for (int i = 0; i < 7509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7510(int& val) {
    const int x = 7510;
    std::cout << 7510;
    for (int i = 0; i < 7510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7511(int& val) {
    const int x = 7511;
    std::cout << 7511;
    for (int i = 0; i < 7511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7512(int& val) {
    const int x = 7512;
    std::cout << 7512;
    for (int i = 0; i < 7512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7513(int& val) {
    const int x = 7513;
    std::cout << 7513;
    for (int i = 0; i < 7513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7514(int& val) {
    const int x = 7514;
    std::cout << 7514;
    for (int i = 0; i < 7514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7515(int& val) {
    const int x = 7515;
    std::cout << 7515;
    for (int i = 0; i < 7515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7516(int& val) {
    const int x = 7516;
    std::cout << 7516;
    for (int i = 0; i < 7516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7517(int& val) {
    const int x = 7517;
    std::cout << 7517;
    for (int i = 0; i < 7517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7518(int& val) {
    const int x = 7518;
    std::cout << 7518;
    for (int i = 0; i < 7518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7519(int& val) {
    const int x = 7519;
    std::cout << 7519;
    for (int i = 0; i < 7519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7520(int& val) {
    const int x = 7520;
    std::cout << 7520;
    for (int i = 0; i < 7520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7521(int& val) {
    const int x = 7521;
    std::cout << 7521;
    for (int i = 0; i < 7521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7522(int& val) {
    const int x = 7522;
    std::cout << 7522;
    for (int i = 0; i < 7522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7523(int& val) {
    const int x = 7523;
    std::cout << 7523;
    for (int i = 0; i < 7523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7524(int& val) {
    const int x = 7524;
    std::cout << 7524;
    for (int i = 0; i < 7524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7525(int& val) {
    const int x = 7525;
    std::cout << 7525;
    for (int i = 0; i < 7525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7526(int& val) {
    const int x = 7526;
    std::cout << 7526;
    for (int i = 0; i < 7526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7527(int& val) {
    const int x = 7527;
    std::cout << 7527;
    for (int i = 0; i < 7527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7528(int& val) {
    const int x = 7528;
    std::cout << 7528;
    for (int i = 0; i < 7528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7529(int& val) {
    const int x = 7529;
    std::cout << 7529;
    for (int i = 0; i < 7529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7530(int& val) {
    const int x = 7530;
    std::cout << 7530;
    for (int i = 0; i < 7530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7531(int& val) {
    const int x = 7531;
    std::cout << 7531;
    for (int i = 0; i < 7531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7532(int& val) {
    const int x = 7532;
    std::cout << 7532;
    for (int i = 0; i < 7532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7533(int& val) {
    const int x = 7533;
    std::cout << 7533;
    for (int i = 0; i < 7533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7534(int& val) {
    const int x = 7534;
    std::cout << 7534;
    for (int i = 0; i < 7534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7535(int& val) {
    const int x = 7535;
    std::cout << 7535;
    for (int i = 0; i < 7535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7536(int& val) {
    const int x = 7536;
    std::cout << 7536;
    for (int i = 0; i < 7536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7537(int& val) {
    const int x = 7537;
    std::cout << 7537;
    for (int i = 0; i < 7537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7538(int& val) {
    const int x = 7538;
    std::cout << 7538;
    for (int i = 0; i < 7538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7539(int& val) {
    const int x = 7539;
    std::cout << 7539;
    for (int i = 0; i < 7539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7540(int& val) {
    const int x = 7540;
    std::cout << 7540;
    for (int i = 0; i < 7540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7541(int& val) {
    const int x = 7541;
    std::cout << 7541;
    for (int i = 0; i < 7541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7542(int& val) {
    const int x = 7542;
    std::cout << 7542;
    for (int i = 0; i < 7542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7543(int& val) {
    const int x = 7543;
    std::cout << 7543;
    for (int i = 0; i < 7543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7544(int& val) {
    const int x = 7544;
    std::cout << 7544;
    for (int i = 0; i < 7544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7545(int& val) {
    const int x = 7545;
    std::cout << 7545;
    for (int i = 0; i < 7545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7546(int& val) {
    const int x = 7546;
    std::cout << 7546;
    for (int i = 0; i < 7546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7547(int& val) {
    const int x = 7547;
    std::cout << 7547;
    for (int i = 0; i < 7547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7548(int& val) {
    const int x = 7548;
    std::cout << 7548;
    for (int i = 0; i < 7548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7549(int& val) {
    const int x = 7549;
    std::cout << 7549;
    for (int i = 0; i < 7549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7550(int& val) {
    const int x = 7550;
    std::cout << 7550;
    for (int i = 0; i < 7550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7551(int& val) {
    const int x = 7551;
    std::cout << 7551;
    for (int i = 0; i < 7551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7552(int& val) {
    const int x = 7552;
    std::cout << 7552;
    for (int i = 0; i < 7552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7553(int& val) {
    const int x = 7553;
    std::cout << 7553;
    for (int i = 0; i < 7553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7554(int& val) {
    const int x = 7554;
    std::cout << 7554;
    for (int i = 0; i < 7554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7555(int& val) {
    const int x = 7555;
    std::cout << 7555;
    for (int i = 0; i < 7555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7556(int& val) {
    const int x = 7556;
    std::cout << 7556;
    for (int i = 0; i < 7556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7557(int& val) {
    const int x = 7557;
    std::cout << 7557;
    for (int i = 0; i < 7557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7558(int& val) {
    const int x = 7558;
    std::cout << 7558;
    for (int i = 0; i < 7558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7559(int& val) {
    const int x = 7559;
    std::cout << 7559;
    for (int i = 0; i < 7559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7560(int& val) {
    const int x = 7560;
    std::cout << 7560;
    for (int i = 0; i < 7560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7561(int& val) {
    const int x = 7561;
    std::cout << 7561;
    for (int i = 0; i < 7561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7562(int& val) {
    const int x = 7562;
    std::cout << 7562;
    for (int i = 0; i < 7562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7563(int& val) {
    const int x = 7563;
    std::cout << 7563;
    for (int i = 0; i < 7563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7564(int& val) {
    const int x = 7564;
    std::cout << 7564;
    for (int i = 0; i < 7564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7565(int& val) {
    const int x = 7565;
    std::cout << 7565;
    for (int i = 0; i < 7565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7566(int& val) {
    const int x = 7566;
    std::cout << 7566;
    for (int i = 0; i < 7566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7567(int& val) {
    const int x = 7567;
    std::cout << 7567;
    for (int i = 0; i < 7567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7568(int& val) {
    const int x = 7568;
    std::cout << 7568;
    for (int i = 0; i < 7568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7569(int& val) {
    const int x = 7569;
    std::cout << 7569;
    for (int i = 0; i < 7569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7570(int& val) {
    const int x = 7570;
    std::cout << 7570;
    for (int i = 0; i < 7570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7571(int& val) {
    const int x = 7571;
    std::cout << 7571;
    for (int i = 0; i < 7571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7572(int& val) {
    const int x = 7572;
    std::cout << 7572;
    for (int i = 0; i < 7572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7573(int& val) {
    const int x = 7573;
    std::cout << 7573;
    for (int i = 0; i < 7573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7574(int& val) {
    const int x = 7574;
    std::cout << 7574;
    for (int i = 0; i < 7574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7575(int& val) {
    const int x = 7575;
    std::cout << 7575;
    for (int i = 0; i < 7575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7576(int& val) {
    const int x = 7576;
    std::cout << 7576;
    for (int i = 0; i < 7576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7577(int& val) {
    const int x = 7577;
    std::cout << 7577;
    for (int i = 0; i < 7577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7578(int& val) {
    const int x = 7578;
    std::cout << 7578;
    for (int i = 0; i < 7578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7579(int& val) {
    const int x = 7579;
    std::cout << 7579;
    for (int i = 0; i < 7579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7580(int& val) {
    const int x = 7580;
    std::cout << 7580;
    for (int i = 0; i < 7580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7581(int& val) {
    const int x = 7581;
    std::cout << 7581;
    for (int i = 0; i < 7581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7582(int& val) {
    const int x = 7582;
    std::cout << 7582;
    for (int i = 0; i < 7582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7583(int& val) {
    const int x = 7583;
    std::cout << 7583;
    for (int i = 0; i < 7583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7584(int& val) {
    const int x = 7584;
    std::cout << 7584;
    for (int i = 0; i < 7584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7585(int& val) {
    const int x = 7585;
    std::cout << 7585;
    for (int i = 0; i < 7585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7586(int& val) {
    const int x = 7586;
    std::cout << 7586;
    for (int i = 0; i < 7586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7587(int& val) {
    const int x = 7587;
    std::cout << 7587;
    for (int i = 0; i < 7587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7588(int& val) {
    const int x = 7588;
    std::cout << 7588;
    for (int i = 0; i < 7588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7589(int& val) {
    const int x = 7589;
    std::cout << 7589;
    for (int i = 0; i < 7589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7590(int& val) {
    const int x = 7590;
    std::cout << 7590;
    for (int i = 0; i < 7590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7591(int& val) {
    const int x = 7591;
    std::cout << 7591;
    for (int i = 0; i < 7591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7592(int& val) {
    const int x = 7592;
    std::cout << 7592;
    for (int i = 0; i < 7592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7593(int& val) {
    const int x = 7593;
    std::cout << 7593;
    for (int i = 0; i < 7593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7594(int& val) {
    const int x = 7594;
    std::cout << 7594;
    for (int i = 0; i < 7594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7595(int& val) {
    const int x = 7595;
    std::cout << 7595;
    for (int i = 0; i < 7595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7596(int& val) {
    const int x = 7596;
    std::cout << 7596;
    for (int i = 0; i < 7596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7597(int& val) {
    const int x = 7597;
    std::cout << 7597;
    for (int i = 0; i < 7597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7598(int& val) {
    const int x = 7598;
    std::cout << 7598;
    for (int i = 0; i < 7598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7599(int& val) {
    const int x = 7599;
    std::cout << 7599;
    for (int i = 0; i < 7599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7600(int& val) {
    const int x = 7600;
    std::cout << 7600;
    for (int i = 0; i < 7600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7601(int& val) {
    const int x = 7601;
    std::cout << 7601;
    for (int i = 0; i < 7601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7602(int& val) {
    const int x = 7602;
    std::cout << 7602;
    for (int i = 0; i < 7602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7603(int& val) {
    const int x = 7603;
    std::cout << 7603;
    for (int i = 0; i < 7603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7604(int& val) {
    const int x = 7604;
    std::cout << 7604;
    for (int i = 0; i < 7604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7605(int& val) {
    const int x = 7605;
    std::cout << 7605;
    for (int i = 0; i < 7605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7606(int& val) {
    const int x = 7606;
    std::cout << 7606;
    for (int i = 0; i < 7606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7607(int& val) {
    const int x = 7607;
    std::cout << 7607;
    for (int i = 0; i < 7607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7608(int& val) {
    const int x = 7608;
    std::cout << 7608;
    for (int i = 0; i < 7608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7609(int& val) {
    const int x = 7609;
    std::cout << 7609;
    for (int i = 0; i < 7609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7610(int& val) {
    const int x = 7610;
    std::cout << 7610;
    for (int i = 0; i < 7610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7611(int& val) {
    const int x = 7611;
    std::cout << 7611;
    for (int i = 0; i < 7611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7612(int& val) {
    const int x = 7612;
    std::cout << 7612;
    for (int i = 0; i < 7612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7613(int& val) {
    const int x = 7613;
    std::cout << 7613;
    for (int i = 0; i < 7613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7614(int& val) {
    const int x = 7614;
    std::cout << 7614;
    for (int i = 0; i < 7614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7615(int& val) {
    const int x = 7615;
    std::cout << 7615;
    for (int i = 0; i < 7615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7616(int& val) {
    const int x = 7616;
    std::cout << 7616;
    for (int i = 0; i < 7616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7617(int& val) {
    const int x = 7617;
    std::cout << 7617;
    for (int i = 0; i < 7617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7618(int& val) {
    const int x = 7618;
    std::cout << 7618;
    for (int i = 0; i < 7618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7619(int& val) {
    const int x = 7619;
    std::cout << 7619;
    for (int i = 0; i < 7619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7620(int& val) {
    const int x = 7620;
    std::cout << 7620;
    for (int i = 0; i < 7620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7621(int& val) {
    const int x = 7621;
    std::cout << 7621;
    for (int i = 0; i < 7621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7622(int& val) {
    const int x = 7622;
    std::cout << 7622;
    for (int i = 0; i < 7622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7623(int& val) {
    const int x = 7623;
    std::cout << 7623;
    for (int i = 0; i < 7623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7624(int& val) {
    const int x = 7624;
    std::cout << 7624;
    for (int i = 0; i < 7624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7625(int& val) {
    const int x = 7625;
    std::cout << 7625;
    for (int i = 0; i < 7625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7626(int& val) {
    const int x = 7626;
    std::cout << 7626;
    for (int i = 0; i < 7626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7627(int& val) {
    const int x = 7627;
    std::cout << 7627;
    for (int i = 0; i < 7627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7628(int& val) {
    const int x = 7628;
    std::cout << 7628;
    for (int i = 0; i < 7628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7629(int& val) {
    const int x = 7629;
    std::cout << 7629;
    for (int i = 0; i < 7629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7630(int& val) {
    const int x = 7630;
    std::cout << 7630;
    for (int i = 0; i < 7630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7631(int& val) {
    const int x = 7631;
    std::cout << 7631;
    for (int i = 0; i < 7631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7632(int& val) {
    const int x = 7632;
    std::cout << 7632;
    for (int i = 0; i < 7632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7633(int& val) {
    const int x = 7633;
    std::cout << 7633;
    for (int i = 0; i < 7633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7634(int& val) {
    const int x = 7634;
    std::cout << 7634;
    for (int i = 0; i < 7634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7635(int& val) {
    const int x = 7635;
    std::cout << 7635;
    for (int i = 0; i < 7635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7636(int& val) {
    const int x = 7636;
    std::cout << 7636;
    for (int i = 0; i < 7636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7637(int& val) {
    const int x = 7637;
    std::cout << 7637;
    for (int i = 0; i < 7637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7638(int& val) {
    const int x = 7638;
    std::cout << 7638;
    for (int i = 0; i < 7638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7639(int& val) {
    const int x = 7639;
    std::cout << 7639;
    for (int i = 0; i < 7639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7640(int& val) {
    const int x = 7640;
    std::cout << 7640;
    for (int i = 0; i < 7640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7641(int& val) {
    const int x = 7641;
    std::cout << 7641;
    for (int i = 0; i < 7641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7642(int& val) {
    const int x = 7642;
    std::cout << 7642;
    for (int i = 0; i < 7642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7643(int& val) {
    const int x = 7643;
    std::cout << 7643;
    for (int i = 0; i < 7643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7644(int& val) {
    const int x = 7644;
    std::cout << 7644;
    for (int i = 0; i < 7644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7645(int& val) {
    const int x = 7645;
    std::cout << 7645;
    for (int i = 0; i < 7645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7646(int& val) {
    const int x = 7646;
    std::cout << 7646;
    for (int i = 0; i < 7646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7647(int& val) {
    const int x = 7647;
    std::cout << 7647;
    for (int i = 0; i < 7647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7648(int& val) {
    const int x = 7648;
    std::cout << 7648;
    for (int i = 0; i < 7648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7649(int& val) {
    const int x = 7649;
    std::cout << 7649;
    for (int i = 0; i < 7649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7650(int& val) {
    const int x = 7650;
    std::cout << 7650;
    for (int i = 0; i < 7650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7651(int& val) {
    const int x = 7651;
    std::cout << 7651;
    for (int i = 0; i < 7651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7652(int& val) {
    const int x = 7652;
    std::cout << 7652;
    for (int i = 0; i < 7652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7653(int& val) {
    const int x = 7653;
    std::cout << 7653;
    for (int i = 0; i < 7653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7654(int& val) {
    const int x = 7654;
    std::cout << 7654;
    for (int i = 0; i < 7654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7655(int& val) {
    const int x = 7655;
    std::cout << 7655;
    for (int i = 0; i < 7655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7656(int& val) {
    const int x = 7656;
    std::cout << 7656;
    for (int i = 0; i < 7656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7657(int& val) {
    const int x = 7657;
    std::cout << 7657;
    for (int i = 0; i < 7657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7658(int& val) {
    const int x = 7658;
    std::cout << 7658;
    for (int i = 0; i < 7658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7659(int& val) {
    const int x = 7659;
    std::cout << 7659;
    for (int i = 0; i < 7659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7660(int& val) {
    const int x = 7660;
    std::cout << 7660;
    for (int i = 0; i < 7660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7661(int& val) {
    const int x = 7661;
    std::cout << 7661;
    for (int i = 0; i < 7661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7662(int& val) {
    const int x = 7662;
    std::cout << 7662;
    for (int i = 0; i < 7662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7663(int& val) {
    const int x = 7663;
    std::cout << 7663;
    for (int i = 0; i < 7663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7664(int& val) {
    const int x = 7664;
    std::cout << 7664;
    for (int i = 0; i < 7664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7665(int& val) {
    const int x = 7665;
    std::cout << 7665;
    for (int i = 0; i < 7665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7666(int& val) {
    const int x = 7666;
    std::cout << 7666;
    for (int i = 0; i < 7666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7667(int& val) {
    const int x = 7667;
    std::cout << 7667;
    for (int i = 0; i < 7667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7668(int& val) {
    const int x = 7668;
    std::cout << 7668;
    for (int i = 0; i < 7668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7669(int& val) {
    const int x = 7669;
    std::cout << 7669;
    for (int i = 0; i < 7669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7670(int& val) {
    const int x = 7670;
    std::cout << 7670;
    for (int i = 0; i < 7670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7671(int& val) {
    const int x = 7671;
    std::cout << 7671;
    for (int i = 0; i < 7671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7672(int& val) {
    const int x = 7672;
    std::cout << 7672;
    for (int i = 0; i < 7672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7673(int& val) {
    const int x = 7673;
    std::cout << 7673;
    for (int i = 0; i < 7673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7674(int& val) {
    const int x = 7674;
    std::cout << 7674;
    for (int i = 0; i < 7674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7675(int& val) {
    const int x = 7675;
    std::cout << 7675;
    for (int i = 0; i < 7675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7676(int& val) {
    const int x = 7676;
    std::cout << 7676;
    for (int i = 0; i < 7676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7677(int& val) {
    const int x = 7677;
    std::cout << 7677;
    for (int i = 0; i < 7677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7678(int& val) {
    const int x = 7678;
    std::cout << 7678;
    for (int i = 0; i < 7678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7679(int& val) {
    const int x = 7679;
    std::cout << 7679;
    for (int i = 0; i < 7679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7680(int& val) {
    const int x = 7680;
    std::cout << 7680;
    for (int i = 0; i < 7680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7681(int& val) {
    const int x = 7681;
    std::cout << 7681;
    for (int i = 0; i < 7681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7682(int& val) {
    const int x = 7682;
    std::cout << 7682;
    for (int i = 0; i < 7682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7683(int& val) {
    const int x = 7683;
    std::cout << 7683;
    for (int i = 0; i < 7683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7684(int& val) {
    const int x = 7684;
    std::cout << 7684;
    for (int i = 0; i < 7684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7685(int& val) {
    const int x = 7685;
    std::cout << 7685;
    for (int i = 0; i < 7685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7686(int& val) {
    const int x = 7686;
    std::cout << 7686;
    for (int i = 0; i < 7686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7687(int& val) {
    const int x = 7687;
    std::cout << 7687;
    for (int i = 0; i < 7687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7688(int& val) {
    const int x = 7688;
    std::cout << 7688;
    for (int i = 0; i < 7688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7689(int& val) {
    const int x = 7689;
    std::cout << 7689;
    for (int i = 0; i < 7689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7690(int& val) {
    const int x = 7690;
    std::cout << 7690;
    for (int i = 0; i < 7690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7691(int& val) {
    const int x = 7691;
    std::cout << 7691;
    for (int i = 0; i < 7691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7692(int& val) {
    const int x = 7692;
    std::cout << 7692;
    for (int i = 0; i < 7692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7693(int& val) {
    const int x = 7693;
    std::cout << 7693;
    for (int i = 0; i < 7693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7694(int& val) {
    const int x = 7694;
    std::cout << 7694;
    for (int i = 0; i < 7694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7695(int& val) {
    const int x = 7695;
    std::cout << 7695;
    for (int i = 0; i < 7695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7696(int& val) {
    const int x = 7696;
    std::cout << 7696;
    for (int i = 0; i < 7696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7697(int& val) {
    const int x = 7697;
    std::cout << 7697;
    for (int i = 0; i < 7697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7698(int& val) {
    const int x = 7698;
    std::cout << 7698;
    for (int i = 0; i < 7698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7699(int& val) {
    const int x = 7699;
    std::cout << 7699;
    for (int i = 0; i < 7699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7700(int& val) {
    const int x = 7700;
    std::cout << 7700;
    for (int i = 0; i < 7700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7701(int& val) {
    const int x = 7701;
    std::cout << 7701;
    for (int i = 0; i < 7701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7702(int& val) {
    const int x = 7702;
    std::cout << 7702;
    for (int i = 0; i < 7702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7703(int& val) {
    const int x = 7703;
    std::cout << 7703;
    for (int i = 0; i < 7703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7704(int& val) {
    const int x = 7704;
    std::cout << 7704;
    for (int i = 0; i < 7704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7705(int& val) {
    const int x = 7705;
    std::cout << 7705;
    for (int i = 0; i < 7705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7706(int& val) {
    const int x = 7706;
    std::cout << 7706;
    for (int i = 0; i < 7706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7707(int& val) {
    const int x = 7707;
    std::cout << 7707;
    for (int i = 0; i < 7707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7708(int& val) {
    const int x = 7708;
    std::cout << 7708;
    for (int i = 0; i < 7708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7709(int& val) {
    const int x = 7709;
    std::cout << 7709;
    for (int i = 0; i < 7709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7710(int& val) {
    const int x = 7710;
    std::cout << 7710;
    for (int i = 0; i < 7710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7711(int& val) {
    const int x = 7711;
    std::cout << 7711;
    for (int i = 0; i < 7711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7712(int& val) {
    const int x = 7712;
    std::cout << 7712;
    for (int i = 0; i < 7712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7713(int& val) {
    const int x = 7713;
    std::cout << 7713;
    for (int i = 0; i < 7713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7714(int& val) {
    const int x = 7714;
    std::cout << 7714;
    for (int i = 0; i < 7714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7715(int& val) {
    const int x = 7715;
    std::cout << 7715;
    for (int i = 0; i < 7715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7716(int& val) {
    const int x = 7716;
    std::cout << 7716;
    for (int i = 0; i < 7716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7717(int& val) {
    const int x = 7717;
    std::cout << 7717;
    for (int i = 0; i < 7717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7718(int& val) {
    const int x = 7718;
    std::cout << 7718;
    for (int i = 0; i < 7718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7719(int& val) {
    const int x = 7719;
    std::cout << 7719;
    for (int i = 0; i < 7719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7720(int& val) {
    const int x = 7720;
    std::cout << 7720;
    for (int i = 0; i < 7720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7721(int& val) {
    const int x = 7721;
    std::cout << 7721;
    for (int i = 0; i < 7721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7722(int& val) {
    const int x = 7722;
    std::cout << 7722;
    for (int i = 0; i < 7722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7723(int& val) {
    const int x = 7723;
    std::cout << 7723;
    for (int i = 0; i < 7723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7724(int& val) {
    const int x = 7724;
    std::cout << 7724;
    for (int i = 0; i < 7724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7725(int& val) {
    const int x = 7725;
    std::cout << 7725;
    for (int i = 0; i < 7725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7726(int& val) {
    const int x = 7726;
    std::cout << 7726;
    for (int i = 0; i < 7726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7727(int& val) {
    const int x = 7727;
    std::cout << 7727;
    for (int i = 0; i < 7727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7728(int& val) {
    const int x = 7728;
    std::cout << 7728;
    for (int i = 0; i < 7728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7729(int& val) {
    const int x = 7729;
    std::cout << 7729;
    for (int i = 0; i < 7729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7730(int& val) {
    const int x = 7730;
    std::cout << 7730;
    for (int i = 0; i < 7730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7731(int& val) {
    const int x = 7731;
    std::cout << 7731;
    for (int i = 0; i < 7731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7732(int& val) {
    const int x = 7732;
    std::cout << 7732;
    for (int i = 0; i < 7732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7733(int& val) {
    const int x = 7733;
    std::cout << 7733;
    for (int i = 0; i < 7733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7734(int& val) {
    const int x = 7734;
    std::cout << 7734;
    for (int i = 0; i < 7734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7735(int& val) {
    const int x = 7735;
    std::cout << 7735;
    for (int i = 0; i < 7735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7736(int& val) {
    const int x = 7736;
    std::cout << 7736;
    for (int i = 0; i < 7736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7737(int& val) {
    const int x = 7737;
    std::cout << 7737;
    for (int i = 0; i < 7737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7738(int& val) {
    const int x = 7738;
    std::cout << 7738;
    for (int i = 0; i < 7738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7739(int& val) {
    const int x = 7739;
    std::cout << 7739;
    for (int i = 0; i < 7739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7740(int& val) {
    const int x = 7740;
    std::cout << 7740;
    for (int i = 0; i < 7740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7741(int& val) {
    const int x = 7741;
    std::cout << 7741;
    for (int i = 0; i < 7741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7742(int& val) {
    const int x = 7742;
    std::cout << 7742;
    for (int i = 0; i < 7742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7743(int& val) {
    const int x = 7743;
    std::cout << 7743;
    for (int i = 0; i < 7743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7744(int& val) {
    const int x = 7744;
    std::cout << 7744;
    for (int i = 0; i < 7744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7745(int& val) {
    const int x = 7745;
    std::cout << 7745;
    for (int i = 0; i < 7745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7746(int& val) {
    const int x = 7746;
    std::cout << 7746;
    for (int i = 0; i < 7746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7747(int& val) {
    const int x = 7747;
    std::cout << 7747;
    for (int i = 0; i < 7747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7748(int& val) {
    const int x = 7748;
    std::cout << 7748;
    for (int i = 0; i < 7748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7749(int& val) {
    const int x = 7749;
    std::cout << 7749;
    for (int i = 0; i < 7749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7750(int& val) {
    const int x = 7750;
    std::cout << 7750;
    for (int i = 0; i < 7750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7751(int& val) {
    const int x = 7751;
    std::cout << 7751;
    for (int i = 0; i < 7751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7752(int& val) {
    const int x = 7752;
    std::cout << 7752;
    for (int i = 0; i < 7752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7753(int& val) {
    const int x = 7753;
    std::cout << 7753;
    for (int i = 0; i < 7753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7754(int& val) {
    const int x = 7754;
    std::cout << 7754;
    for (int i = 0; i < 7754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7755(int& val) {
    const int x = 7755;
    std::cout << 7755;
    for (int i = 0; i < 7755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7756(int& val) {
    const int x = 7756;
    std::cout << 7756;
    for (int i = 0; i < 7756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7757(int& val) {
    const int x = 7757;
    std::cout << 7757;
    for (int i = 0; i < 7757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7758(int& val) {
    const int x = 7758;
    std::cout << 7758;
    for (int i = 0; i < 7758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7759(int& val) {
    const int x = 7759;
    std::cout << 7759;
    for (int i = 0; i < 7759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7760(int& val) {
    const int x = 7760;
    std::cout << 7760;
    for (int i = 0; i < 7760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7761(int& val) {
    const int x = 7761;
    std::cout << 7761;
    for (int i = 0; i < 7761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7762(int& val) {
    const int x = 7762;
    std::cout << 7762;
    for (int i = 0; i < 7762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7763(int& val) {
    const int x = 7763;
    std::cout << 7763;
    for (int i = 0; i < 7763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7764(int& val) {
    const int x = 7764;
    std::cout << 7764;
    for (int i = 0; i < 7764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7765(int& val) {
    const int x = 7765;
    std::cout << 7765;
    for (int i = 0; i < 7765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7766(int& val) {
    const int x = 7766;
    std::cout << 7766;
    for (int i = 0; i < 7766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7767(int& val) {
    const int x = 7767;
    std::cout << 7767;
    for (int i = 0; i < 7767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7768(int& val) {
    const int x = 7768;
    std::cout << 7768;
    for (int i = 0; i < 7768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7769(int& val) {
    const int x = 7769;
    std::cout << 7769;
    for (int i = 0; i < 7769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7770(int& val) {
    const int x = 7770;
    std::cout << 7770;
    for (int i = 0; i < 7770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7771(int& val) {
    const int x = 7771;
    std::cout << 7771;
    for (int i = 0; i < 7771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7772(int& val) {
    const int x = 7772;
    std::cout << 7772;
    for (int i = 0; i < 7772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7773(int& val) {
    const int x = 7773;
    std::cout << 7773;
    for (int i = 0; i < 7773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7774(int& val) {
    const int x = 7774;
    std::cout << 7774;
    for (int i = 0; i < 7774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7775(int& val) {
    const int x = 7775;
    std::cout << 7775;
    for (int i = 0; i < 7775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7776(int& val) {
    const int x = 7776;
    std::cout << 7776;
    for (int i = 0; i < 7776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7777(int& val) {
    const int x = 7777;
    std::cout << 7777;
    for (int i = 0; i < 7777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7778(int& val) {
    const int x = 7778;
    std::cout << 7778;
    for (int i = 0; i < 7778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7779(int& val) {
    const int x = 7779;
    std::cout << 7779;
    for (int i = 0; i < 7779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7780(int& val) {
    const int x = 7780;
    std::cout << 7780;
    for (int i = 0; i < 7780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7781(int& val) {
    const int x = 7781;
    std::cout << 7781;
    for (int i = 0; i < 7781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7782(int& val) {
    const int x = 7782;
    std::cout << 7782;
    for (int i = 0; i < 7782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7783(int& val) {
    const int x = 7783;
    std::cout << 7783;
    for (int i = 0; i < 7783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7784(int& val) {
    const int x = 7784;
    std::cout << 7784;
    for (int i = 0; i < 7784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7785(int& val) {
    const int x = 7785;
    std::cout << 7785;
    for (int i = 0; i < 7785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7786(int& val) {
    const int x = 7786;
    std::cout << 7786;
    for (int i = 0; i < 7786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7787(int& val) {
    const int x = 7787;
    std::cout << 7787;
    for (int i = 0; i < 7787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7788(int& val) {
    const int x = 7788;
    std::cout << 7788;
    for (int i = 0; i < 7788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7789(int& val) {
    const int x = 7789;
    std::cout << 7789;
    for (int i = 0; i < 7789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7790(int& val) {
    const int x = 7790;
    std::cout << 7790;
    for (int i = 0; i < 7790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7791(int& val) {
    const int x = 7791;
    std::cout << 7791;
    for (int i = 0; i < 7791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7792(int& val) {
    const int x = 7792;
    std::cout << 7792;
    for (int i = 0; i < 7792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7793(int& val) {
    const int x = 7793;
    std::cout << 7793;
    for (int i = 0; i < 7793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7794(int& val) {
    const int x = 7794;
    std::cout << 7794;
    for (int i = 0; i < 7794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7795(int& val) {
    const int x = 7795;
    std::cout << 7795;
    for (int i = 0; i < 7795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7796(int& val) {
    const int x = 7796;
    std::cout << 7796;
    for (int i = 0; i < 7796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7797(int& val) {
    const int x = 7797;
    std::cout << 7797;
    for (int i = 0; i < 7797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7798(int& val) {
    const int x = 7798;
    std::cout << 7798;
    for (int i = 0; i < 7798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7799(int& val) {
    const int x = 7799;
    std::cout << 7799;
    for (int i = 0; i < 7799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7800(int& val) {
    const int x = 7800;
    std::cout << 7800;
    for (int i = 0; i < 7800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7801(int& val) {
    const int x = 7801;
    std::cout << 7801;
    for (int i = 0; i < 7801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7802(int& val) {
    const int x = 7802;
    std::cout << 7802;
    for (int i = 0; i < 7802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7803(int& val) {
    const int x = 7803;
    std::cout << 7803;
    for (int i = 0; i < 7803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7804(int& val) {
    const int x = 7804;
    std::cout << 7804;
    for (int i = 0; i < 7804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7805(int& val) {
    const int x = 7805;
    std::cout << 7805;
    for (int i = 0; i < 7805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7806(int& val) {
    const int x = 7806;
    std::cout << 7806;
    for (int i = 0; i < 7806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7807(int& val) {
    const int x = 7807;
    std::cout << 7807;
    for (int i = 0; i < 7807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7808(int& val) {
    const int x = 7808;
    std::cout << 7808;
    for (int i = 0; i < 7808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7809(int& val) {
    const int x = 7809;
    std::cout << 7809;
    for (int i = 0; i < 7809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7810(int& val) {
    const int x = 7810;
    std::cout << 7810;
    for (int i = 0; i < 7810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7811(int& val) {
    const int x = 7811;
    std::cout << 7811;
    for (int i = 0; i < 7811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7812(int& val) {
    const int x = 7812;
    std::cout << 7812;
    for (int i = 0; i < 7812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7813(int& val) {
    const int x = 7813;
    std::cout << 7813;
    for (int i = 0; i < 7813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7814(int& val) {
    const int x = 7814;
    std::cout << 7814;
    for (int i = 0; i < 7814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7815(int& val) {
    const int x = 7815;
    std::cout << 7815;
    for (int i = 0; i < 7815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7816(int& val) {
    const int x = 7816;
    std::cout << 7816;
    for (int i = 0; i < 7816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7817(int& val) {
    const int x = 7817;
    std::cout << 7817;
    for (int i = 0; i < 7817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7818(int& val) {
    const int x = 7818;
    std::cout << 7818;
    for (int i = 0; i < 7818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7819(int& val) {
    const int x = 7819;
    std::cout << 7819;
    for (int i = 0; i < 7819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7820(int& val) {
    const int x = 7820;
    std::cout << 7820;
    for (int i = 0; i < 7820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7821(int& val) {
    const int x = 7821;
    std::cout << 7821;
    for (int i = 0; i < 7821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7822(int& val) {
    const int x = 7822;
    std::cout << 7822;
    for (int i = 0; i < 7822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7823(int& val) {
    const int x = 7823;
    std::cout << 7823;
    for (int i = 0; i < 7823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7824(int& val) {
    const int x = 7824;
    std::cout << 7824;
    for (int i = 0; i < 7824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7825(int& val) {
    const int x = 7825;
    std::cout << 7825;
    for (int i = 0; i < 7825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7826(int& val) {
    const int x = 7826;
    std::cout << 7826;
    for (int i = 0; i < 7826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7827(int& val) {
    const int x = 7827;
    std::cout << 7827;
    for (int i = 0; i < 7827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7828(int& val) {
    const int x = 7828;
    std::cout << 7828;
    for (int i = 0; i < 7828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7829(int& val) {
    const int x = 7829;
    std::cout << 7829;
    for (int i = 0; i < 7829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7830(int& val) {
    const int x = 7830;
    std::cout << 7830;
    for (int i = 0; i < 7830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7831(int& val) {
    const int x = 7831;
    std::cout << 7831;
    for (int i = 0; i < 7831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7832(int& val) {
    const int x = 7832;
    std::cout << 7832;
    for (int i = 0; i < 7832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7833(int& val) {
    const int x = 7833;
    std::cout << 7833;
    for (int i = 0; i < 7833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7834(int& val) {
    const int x = 7834;
    std::cout << 7834;
    for (int i = 0; i < 7834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7835(int& val) {
    const int x = 7835;
    std::cout << 7835;
    for (int i = 0; i < 7835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7836(int& val) {
    const int x = 7836;
    std::cout << 7836;
    for (int i = 0; i < 7836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7837(int& val) {
    const int x = 7837;
    std::cout << 7837;
    for (int i = 0; i < 7837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7838(int& val) {
    const int x = 7838;
    std::cout << 7838;
    for (int i = 0; i < 7838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7839(int& val) {
    const int x = 7839;
    std::cout << 7839;
    for (int i = 0; i < 7839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7840(int& val) {
    const int x = 7840;
    std::cout << 7840;
    for (int i = 0; i < 7840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7841(int& val) {
    const int x = 7841;
    std::cout << 7841;
    for (int i = 0; i < 7841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7842(int& val) {
    const int x = 7842;
    std::cout << 7842;
    for (int i = 0; i < 7842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7843(int& val) {
    const int x = 7843;
    std::cout << 7843;
    for (int i = 0; i < 7843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7844(int& val) {
    const int x = 7844;
    std::cout << 7844;
    for (int i = 0; i < 7844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7845(int& val) {
    const int x = 7845;
    std::cout << 7845;
    for (int i = 0; i < 7845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7846(int& val) {
    const int x = 7846;
    std::cout << 7846;
    for (int i = 0; i < 7846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7847(int& val) {
    const int x = 7847;
    std::cout << 7847;
    for (int i = 0; i < 7847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7848(int& val) {
    const int x = 7848;
    std::cout << 7848;
    for (int i = 0; i < 7848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7849(int& val) {
    const int x = 7849;
    std::cout << 7849;
    for (int i = 0; i < 7849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7850(int& val) {
    const int x = 7850;
    std::cout << 7850;
    for (int i = 0; i < 7850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7851(int& val) {
    const int x = 7851;
    std::cout << 7851;
    for (int i = 0; i < 7851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7852(int& val) {
    const int x = 7852;
    std::cout << 7852;
    for (int i = 0; i < 7852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7853(int& val) {
    const int x = 7853;
    std::cout << 7853;
    for (int i = 0; i < 7853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7854(int& val) {
    const int x = 7854;
    std::cout << 7854;
    for (int i = 0; i < 7854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7855(int& val) {
    const int x = 7855;
    std::cout << 7855;
    for (int i = 0; i < 7855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7856(int& val) {
    const int x = 7856;
    std::cout << 7856;
    for (int i = 0; i < 7856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7857(int& val) {
    const int x = 7857;
    std::cout << 7857;
    for (int i = 0; i < 7857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7858(int& val) {
    const int x = 7858;
    std::cout << 7858;
    for (int i = 0; i < 7858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7859(int& val) {
    const int x = 7859;
    std::cout << 7859;
    for (int i = 0; i < 7859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7860(int& val) {
    const int x = 7860;
    std::cout << 7860;
    for (int i = 0; i < 7860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7861(int& val) {
    const int x = 7861;
    std::cout << 7861;
    for (int i = 0; i < 7861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7862(int& val) {
    const int x = 7862;
    std::cout << 7862;
    for (int i = 0; i < 7862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7863(int& val) {
    const int x = 7863;
    std::cout << 7863;
    for (int i = 0; i < 7863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7864(int& val) {
    const int x = 7864;
    std::cout << 7864;
    for (int i = 0; i < 7864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7865(int& val) {
    const int x = 7865;
    std::cout << 7865;
    for (int i = 0; i < 7865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7866(int& val) {
    const int x = 7866;
    std::cout << 7866;
    for (int i = 0; i < 7866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7867(int& val) {
    const int x = 7867;
    std::cout << 7867;
    for (int i = 0; i < 7867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7868(int& val) {
    const int x = 7868;
    std::cout << 7868;
    for (int i = 0; i < 7868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7869(int& val) {
    const int x = 7869;
    std::cout << 7869;
    for (int i = 0; i < 7869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7870(int& val) {
    const int x = 7870;
    std::cout << 7870;
    for (int i = 0; i < 7870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7871(int& val) {
    const int x = 7871;
    std::cout << 7871;
    for (int i = 0; i < 7871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7872(int& val) {
    const int x = 7872;
    std::cout << 7872;
    for (int i = 0; i < 7872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7873(int& val) {
    const int x = 7873;
    std::cout << 7873;
    for (int i = 0; i < 7873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7874(int& val) {
    const int x = 7874;
    std::cout << 7874;
    for (int i = 0; i < 7874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7875(int& val) {
    const int x = 7875;
    std::cout << 7875;
    for (int i = 0; i < 7875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7876(int& val) {
    const int x = 7876;
    std::cout << 7876;
    for (int i = 0; i < 7876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7877(int& val) {
    const int x = 7877;
    std::cout << 7877;
    for (int i = 0; i < 7877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7878(int& val) {
    const int x = 7878;
    std::cout << 7878;
    for (int i = 0; i < 7878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7879(int& val) {
    const int x = 7879;
    std::cout << 7879;
    for (int i = 0; i < 7879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7880(int& val) {
    const int x = 7880;
    std::cout << 7880;
    for (int i = 0; i < 7880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7881(int& val) {
    const int x = 7881;
    std::cout << 7881;
    for (int i = 0; i < 7881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7882(int& val) {
    const int x = 7882;
    std::cout << 7882;
    for (int i = 0; i < 7882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7883(int& val) {
    const int x = 7883;
    std::cout << 7883;
    for (int i = 0; i < 7883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7884(int& val) {
    const int x = 7884;
    std::cout << 7884;
    for (int i = 0; i < 7884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7885(int& val) {
    const int x = 7885;
    std::cout << 7885;
    for (int i = 0; i < 7885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7886(int& val) {
    const int x = 7886;
    std::cout << 7886;
    for (int i = 0; i < 7886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7887(int& val) {
    const int x = 7887;
    std::cout << 7887;
    for (int i = 0; i < 7887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7888(int& val) {
    const int x = 7888;
    std::cout << 7888;
    for (int i = 0; i < 7888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7889(int& val) {
    const int x = 7889;
    std::cout << 7889;
    for (int i = 0; i < 7889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7890(int& val) {
    const int x = 7890;
    std::cout << 7890;
    for (int i = 0; i < 7890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7891(int& val) {
    const int x = 7891;
    std::cout << 7891;
    for (int i = 0; i < 7891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7892(int& val) {
    const int x = 7892;
    std::cout << 7892;
    for (int i = 0; i < 7892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7893(int& val) {
    const int x = 7893;
    std::cout << 7893;
    for (int i = 0; i < 7893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7894(int& val) {
    const int x = 7894;
    std::cout << 7894;
    for (int i = 0; i < 7894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7895(int& val) {
    const int x = 7895;
    std::cout << 7895;
    for (int i = 0; i < 7895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7896(int& val) {
    const int x = 7896;
    std::cout << 7896;
    for (int i = 0; i < 7896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7897(int& val) {
    const int x = 7897;
    std::cout << 7897;
    for (int i = 0; i < 7897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7898(int& val) {
    const int x = 7898;
    std::cout << 7898;
    for (int i = 0; i < 7898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7899(int& val) {
    const int x = 7899;
    std::cout << 7899;
    for (int i = 0; i < 7899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7900(int& val) {
    const int x = 7900;
    std::cout << 7900;
    for (int i = 0; i < 7900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7901(int& val) {
    const int x = 7901;
    std::cout << 7901;
    for (int i = 0; i < 7901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7902(int& val) {
    const int x = 7902;
    std::cout << 7902;
    for (int i = 0; i < 7902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7903(int& val) {
    const int x = 7903;
    std::cout << 7903;
    for (int i = 0; i < 7903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7904(int& val) {
    const int x = 7904;
    std::cout << 7904;
    for (int i = 0; i < 7904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7905(int& val) {
    const int x = 7905;
    std::cout << 7905;
    for (int i = 0; i < 7905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7906(int& val) {
    const int x = 7906;
    std::cout << 7906;
    for (int i = 0; i < 7906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7907(int& val) {
    const int x = 7907;
    std::cout << 7907;
    for (int i = 0; i < 7907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7908(int& val) {
    const int x = 7908;
    std::cout << 7908;
    for (int i = 0; i < 7908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7909(int& val) {
    const int x = 7909;
    std::cout << 7909;
    for (int i = 0; i < 7909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7910(int& val) {
    const int x = 7910;
    std::cout << 7910;
    for (int i = 0; i < 7910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7911(int& val) {
    const int x = 7911;
    std::cout << 7911;
    for (int i = 0; i < 7911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7912(int& val) {
    const int x = 7912;
    std::cout << 7912;
    for (int i = 0; i < 7912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7913(int& val) {
    const int x = 7913;
    std::cout << 7913;
    for (int i = 0; i < 7913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7914(int& val) {
    const int x = 7914;
    std::cout << 7914;
    for (int i = 0; i < 7914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7915(int& val) {
    const int x = 7915;
    std::cout << 7915;
    for (int i = 0; i < 7915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7916(int& val) {
    const int x = 7916;
    std::cout << 7916;
    for (int i = 0; i < 7916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7917(int& val) {
    const int x = 7917;
    std::cout << 7917;
    for (int i = 0; i < 7917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7918(int& val) {
    const int x = 7918;
    std::cout << 7918;
    for (int i = 0; i < 7918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7919(int& val) {
    const int x = 7919;
    std::cout << 7919;
    for (int i = 0; i < 7919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7920(int& val) {
    const int x = 7920;
    std::cout << 7920;
    for (int i = 0; i < 7920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7921(int& val) {
    const int x = 7921;
    std::cout << 7921;
    for (int i = 0; i < 7921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7922(int& val) {
    const int x = 7922;
    std::cout << 7922;
    for (int i = 0; i < 7922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7923(int& val) {
    const int x = 7923;
    std::cout << 7923;
    for (int i = 0; i < 7923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7924(int& val) {
    const int x = 7924;
    std::cout << 7924;
    for (int i = 0; i < 7924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7925(int& val) {
    const int x = 7925;
    std::cout << 7925;
    for (int i = 0; i < 7925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7926(int& val) {
    const int x = 7926;
    std::cout << 7926;
    for (int i = 0; i < 7926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7927(int& val) {
    const int x = 7927;
    std::cout << 7927;
    for (int i = 0; i < 7927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7928(int& val) {
    const int x = 7928;
    std::cout << 7928;
    for (int i = 0; i < 7928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7929(int& val) {
    const int x = 7929;
    std::cout << 7929;
    for (int i = 0; i < 7929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7930(int& val) {
    const int x = 7930;
    std::cout << 7930;
    for (int i = 0; i < 7930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7931(int& val) {
    const int x = 7931;
    std::cout << 7931;
    for (int i = 0; i < 7931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7932(int& val) {
    const int x = 7932;
    std::cout << 7932;
    for (int i = 0; i < 7932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7933(int& val) {
    const int x = 7933;
    std::cout << 7933;
    for (int i = 0; i < 7933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7934(int& val) {
    const int x = 7934;
    std::cout << 7934;
    for (int i = 0; i < 7934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7935(int& val) {
    const int x = 7935;
    std::cout << 7935;
    for (int i = 0; i < 7935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7936(int& val) {
    const int x = 7936;
    std::cout << 7936;
    for (int i = 0; i < 7936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7937(int& val) {
    const int x = 7937;
    std::cout << 7937;
    for (int i = 0; i < 7937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7938(int& val) {
    const int x = 7938;
    std::cout << 7938;
    for (int i = 0; i < 7938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7939(int& val) {
    const int x = 7939;
    std::cout << 7939;
    for (int i = 0; i < 7939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7940(int& val) {
    const int x = 7940;
    std::cout << 7940;
    for (int i = 0; i < 7940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7941(int& val) {
    const int x = 7941;
    std::cout << 7941;
    for (int i = 0; i < 7941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7942(int& val) {
    const int x = 7942;
    std::cout << 7942;
    for (int i = 0; i < 7942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7943(int& val) {
    const int x = 7943;
    std::cout << 7943;
    for (int i = 0; i < 7943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7944(int& val) {
    const int x = 7944;
    std::cout << 7944;
    for (int i = 0; i < 7944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7945(int& val) {
    const int x = 7945;
    std::cout << 7945;
    for (int i = 0; i < 7945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7946(int& val) {
    const int x = 7946;
    std::cout << 7946;
    for (int i = 0; i < 7946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7947(int& val) {
    const int x = 7947;
    std::cout << 7947;
    for (int i = 0; i < 7947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7948(int& val) {
    const int x = 7948;
    std::cout << 7948;
    for (int i = 0; i < 7948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7949(int& val) {
    const int x = 7949;
    std::cout << 7949;
    for (int i = 0; i < 7949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7950(int& val) {
    const int x = 7950;
    std::cout << 7950;
    for (int i = 0; i < 7950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7951(int& val) {
    const int x = 7951;
    std::cout << 7951;
    for (int i = 0; i < 7951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7952(int& val) {
    const int x = 7952;
    std::cout << 7952;
    for (int i = 0; i < 7952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7953(int& val) {
    const int x = 7953;
    std::cout << 7953;
    for (int i = 0; i < 7953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7954(int& val) {
    const int x = 7954;
    std::cout << 7954;
    for (int i = 0; i < 7954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7955(int& val) {
    const int x = 7955;
    std::cout << 7955;
    for (int i = 0; i < 7955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7956(int& val) {
    const int x = 7956;
    std::cout << 7956;
    for (int i = 0; i < 7956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7957(int& val) {
    const int x = 7957;
    std::cout << 7957;
    for (int i = 0; i < 7957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7958(int& val) {
    const int x = 7958;
    std::cout << 7958;
    for (int i = 0; i < 7958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7959(int& val) {
    const int x = 7959;
    std::cout << 7959;
    for (int i = 0; i < 7959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7960(int& val) {
    const int x = 7960;
    std::cout << 7960;
    for (int i = 0; i < 7960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7961(int& val) {
    const int x = 7961;
    std::cout << 7961;
    for (int i = 0; i < 7961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7962(int& val) {
    const int x = 7962;
    std::cout << 7962;
    for (int i = 0; i < 7962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7963(int& val) {
    const int x = 7963;
    std::cout << 7963;
    for (int i = 0; i < 7963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7964(int& val) {
    const int x = 7964;
    std::cout << 7964;
    for (int i = 0; i < 7964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7965(int& val) {
    const int x = 7965;
    std::cout << 7965;
    for (int i = 0; i < 7965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7966(int& val) {
    const int x = 7966;
    std::cout << 7966;
    for (int i = 0; i < 7966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7967(int& val) {
    const int x = 7967;
    std::cout << 7967;
    for (int i = 0; i < 7967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7968(int& val) {
    const int x = 7968;
    std::cout << 7968;
    for (int i = 0; i < 7968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7969(int& val) {
    const int x = 7969;
    std::cout << 7969;
    for (int i = 0; i < 7969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7970(int& val) {
    const int x = 7970;
    std::cout << 7970;
    for (int i = 0; i < 7970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7971(int& val) {
    const int x = 7971;
    std::cout << 7971;
    for (int i = 0; i < 7971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7972(int& val) {
    const int x = 7972;
    std::cout << 7972;
    for (int i = 0; i < 7972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7973(int& val) {
    const int x = 7973;
    std::cout << 7973;
    for (int i = 0; i < 7973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7974(int& val) {
    const int x = 7974;
    std::cout << 7974;
    for (int i = 0; i < 7974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7975(int& val) {
    const int x = 7975;
    std::cout << 7975;
    for (int i = 0; i < 7975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7976(int& val) {
    const int x = 7976;
    std::cout << 7976;
    for (int i = 0; i < 7976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7977(int& val) {
    const int x = 7977;
    std::cout << 7977;
    for (int i = 0; i < 7977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7978(int& val) {
    const int x = 7978;
    std::cout << 7978;
    for (int i = 0; i < 7978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7979(int& val) {
    const int x = 7979;
    std::cout << 7979;
    for (int i = 0; i < 7979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7980(int& val) {
    const int x = 7980;
    std::cout << 7980;
    for (int i = 0; i < 7980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7981(int& val) {
    const int x = 7981;
    std::cout << 7981;
    for (int i = 0; i < 7981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7982(int& val) {
    const int x = 7982;
    std::cout << 7982;
    for (int i = 0; i < 7982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7983(int& val) {
    const int x = 7983;
    std::cout << 7983;
    for (int i = 0; i < 7983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7984(int& val) {
    const int x = 7984;
    std::cout << 7984;
    for (int i = 0; i < 7984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7985(int& val) {
    const int x = 7985;
    std::cout << 7985;
    for (int i = 0; i < 7985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7986(int& val) {
    const int x = 7986;
    std::cout << 7986;
    for (int i = 0; i < 7986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7987(int& val) {
    const int x = 7987;
    std::cout << 7987;
    for (int i = 0; i < 7987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7988(int& val) {
    const int x = 7988;
    std::cout << 7988;
    for (int i = 0; i < 7988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7989(int& val) {
    const int x = 7989;
    std::cout << 7989;
    for (int i = 0; i < 7989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7990(int& val) {
    const int x = 7990;
    std::cout << 7990;
    for (int i = 0; i < 7990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7991(int& val) {
    const int x = 7991;
    std::cout << 7991;
    for (int i = 0; i < 7991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7992(int& val) {
    const int x = 7992;
    std::cout << 7992;
    for (int i = 0; i < 7992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7993(int& val) {
    const int x = 7993;
    std::cout << 7993;
    for (int i = 0; i < 7993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7994(int& val) {
    const int x = 7994;
    std::cout << 7994;
    for (int i = 0; i < 7994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7995(int& val) {
    const int x = 7995;
    std::cout << 7995;
    for (int i = 0; i < 7995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7996(int& val) {
    const int x = 7996;
    std::cout << 7996;
    for (int i = 0; i < 7996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7997(int& val) {
    const int x = 7997;
    std::cout << 7997;
    for (int i = 0; i < 7997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7998(int& val) {
    const int x = 7998;
    std::cout << 7998;
    for (int i = 0; i < 7998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7999(int& val) {
    const int x = 7999;
    std::cout << 7999;
    for (int i = 0; i < 7999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8000(int& val) {
    const int x = 8000;
    std::cout << 8000;
    for (int i = 0; i < 8000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8001(int& val) {
    const int x = 8001;
    std::cout << 8001;
    for (int i = 0; i < 8001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8002(int& val) {
    const int x = 8002;
    std::cout << 8002;
    for (int i = 0; i < 8002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8003(int& val) {
    const int x = 8003;
    std::cout << 8003;
    for (int i = 0; i < 8003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8004(int& val) {
    const int x = 8004;
    std::cout << 8004;
    for (int i = 0; i < 8004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8005(int& val) {
    const int x = 8005;
    std::cout << 8005;
    for (int i = 0; i < 8005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8006(int& val) {
    const int x = 8006;
    std::cout << 8006;
    for (int i = 0; i < 8006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8007(int& val) {
    const int x = 8007;
    std::cout << 8007;
    for (int i = 0; i < 8007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8008(int& val) {
    const int x = 8008;
    std::cout << 8008;
    for (int i = 0; i < 8008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8009(int& val) {
    const int x = 8009;
    std::cout << 8009;
    for (int i = 0; i < 8009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8010(int& val) {
    const int x = 8010;
    std::cout << 8010;
    for (int i = 0; i < 8010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8011(int& val) {
    const int x = 8011;
    std::cout << 8011;
    for (int i = 0; i < 8011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8012(int& val) {
    const int x = 8012;
    std::cout << 8012;
    for (int i = 0; i < 8012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8013(int& val) {
    const int x = 8013;
    std::cout << 8013;
    for (int i = 0; i < 8013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8014(int& val) {
    const int x = 8014;
    std::cout << 8014;
    for (int i = 0; i < 8014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8015(int& val) {
    const int x = 8015;
    std::cout << 8015;
    for (int i = 0; i < 8015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8016(int& val) {
    const int x = 8016;
    std::cout << 8016;
    for (int i = 0; i < 8016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8017(int& val) {
    const int x = 8017;
    std::cout << 8017;
    for (int i = 0; i < 8017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8018(int& val) {
    const int x = 8018;
    std::cout << 8018;
    for (int i = 0; i < 8018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8019(int& val) {
    const int x = 8019;
    std::cout << 8019;
    for (int i = 0; i < 8019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8020(int& val) {
    const int x = 8020;
    std::cout << 8020;
    for (int i = 0; i < 8020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8021(int& val) {
    const int x = 8021;
    std::cout << 8021;
    for (int i = 0; i < 8021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8022(int& val) {
    const int x = 8022;
    std::cout << 8022;
    for (int i = 0; i < 8022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8023(int& val) {
    const int x = 8023;
    std::cout << 8023;
    for (int i = 0; i < 8023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8024(int& val) {
    const int x = 8024;
    std::cout << 8024;
    for (int i = 0; i < 8024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8025(int& val) {
    const int x = 8025;
    std::cout << 8025;
    for (int i = 0; i < 8025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8026(int& val) {
    const int x = 8026;
    std::cout << 8026;
    for (int i = 0; i < 8026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8027(int& val) {
    const int x = 8027;
    std::cout << 8027;
    for (int i = 0; i < 8027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8028(int& val) {
    const int x = 8028;
    std::cout << 8028;
    for (int i = 0; i < 8028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8029(int& val) {
    const int x = 8029;
    std::cout << 8029;
    for (int i = 0; i < 8029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8030(int& val) {
    const int x = 8030;
    std::cout << 8030;
    for (int i = 0; i < 8030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8031(int& val) {
    const int x = 8031;
    std::cout << 8031;
    for (int i = 0; i < 8031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8032(int& val) {
    const int x = 8032;
    std::cout << 8032;
    for (int i = 0; i < 8032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8033(int& val) {
    const int x = 8033;
    std::cout << 8033;
    for (int i = 0; i < 8033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8034(int& val) {
    const int x = 8034;
    std::cout << 8034;
    for (int i = 0; i < 8034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8035(int& val) {
    const int x = 8035;
    std::cout << 8035;
    for (int i = 0; i < 8035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8036(int& val) {
    const int x = 8036;
    std::cout << 8036;
    for (int i = 0; i < 8036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8037(int& val) {
    const int x = 8037;
    std::cout << 8037;
    for (int i = 0; i < 8037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8038(int& val) {
    const int x = 8038;
    std::cout << 8038;
    for (int i = 0; i < 8038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8039(int& val) {
    const int x = 8039;
    std::cout << 8039;
    for (int i = 0; i < 8039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8040(int& val) {
    const int x = 8040;
    std::cout << 8040;
    for (int i = 0; i < 8040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8041(int& val) {
    const int x = 8041;
    std::cout << 8041;
    for (int i = 0; i < 8041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8042(int& val) {
    const int x = 8042;
    std::cout << 8042;
    for (int i = 0; i < 8042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8043(int& val) {
    const int x = 8043;
    std::cout << 8043;
    for (int i = 0; i < 8043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8044(int& val) {
    const int x = 8044;
    std::cout << 8044;
    for (int i = 0; i < 8044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8045(int& val) {
    const int x = 8045;
    std::cout << 8045;
    for (int i = 0; i < 8045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8046(int& val) {
    const int x = 8046;
    std::cout << 8046;
    for (int i = 0; i < 8046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8047(int& val) {
    const int x = 8047;
    std::cout << 8047;
    for (int i = 0; i < 8047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8048(int& val) {
    const int x = 8048;
    std::cout << 8048;
    for (int i = 0; i < 8048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8049(int& val) {
    const int x = 8049;
    std::cout << 8049;
    for (int i = 0; i < 8049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8050(int& val) {
    const int x = 8050;
    std::cout << 8050;
    for (int i = 0; i < 8050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8051(int& val) {
    const int x = 8051;
    std::cout << 8051;
    for (int i = 0; i < 8051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8052(int& val) {
    const int x = 8052;
    std::cout << 8052;
    for (int i = 0; i < 8052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8053(int& val) {
    const int x = 8053;
    std::cout << 8053;
    for (int i = 0; i < 8053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8054(int& val) {
    const int x = 8054;
    std::cout << 8054;
    for (int i = 0; i < 8054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8055(int& val) {
    const int x = 8055;
    std::cout << 8055;
    for (int i = 0; i < 8055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8056(int& val) {
    const int x = 8056;
    std::cout << 8056;
    for (int i = 0; i < 8056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8057(int& val) {
    const int x = 8057;
    std::cout << 8057;
    for (int i = 0; i < 8057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8058(int& val) {
    const int x = 8058;
    std::cout << 8058;
    for (int i = 0; i < 8058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8059(int& val) {
    const int x = 8059;
    std::cout << 8059;
    for (int i = 0; i < 8059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8060(int& val) {
    const int x = 8060;
    std::cout << 8060;
    for (int i = 0; i < 8060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8061(int& val) {
    const int x = 8061;
    std::cout << 8061;
    for (int i = 0; i < 8061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8062(int& val) {
    const int x = 8062;
    std::cout << 8062;
    for (int i = 0; i < 8062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8063(int& val) {
    const int x = 8063;
    std::cout << 8063;
    for (int i = 0; i < 8063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8064(int& val) {
    const int x = 8064;
    std::cout << 8064;
    for (int i = 0; i < 8064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8065(int& val) {
    const int x = 8065;
    std::cout << 8065;
    for (int i = 0; i < 8065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8066(int& val) {
    const int x = 8066;
    std::cout << 8066;
    for (int i = 0; i < 8066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8067(int& val) {
    const int x = 8067;
    std::cout << 8067;
    for (int i = 0; i < 8067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8068(int& val) {
    const int x = 8068;
    std::cout << 8068;
    for (int i = 0; i < 8068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8069(int& val) {
    const int x = 8069;
    std::cout << 8069;
    for (int i = 0; i < 8069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8070(int& val) {
    const int x = 8070;
    std::cout << 8070;
    for (int i = 0; i < 8070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8071(int& val) {
    const int x = 8071;
    std::cout << 8071;
    for (int i = 0; i < 8071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8072(int& val) {
    const int x = 8072;
    std::cout << 8072;
    for (int i = 0; i < 8072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8073(int& val) {
    const int x = 8073;
    std::cout << 8073;
    for (int i = 0; i < 8073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8074(int& val) {
    const int x = 8074;
    std::cout << 8074;
    for (int i = 0; i < 8074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8075(int& val) {
    const int x = 8075;
    std::cout << 8075;
    for (int i = 0; i < 8075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8076(int& val) {
    const int x = 8076;
    std::cout << 8076;
    for (int i = 0; i < 8076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8077(int& val) {
    const int x = 8077;
    std::cout << 8077;
    for (int i = 0; i < 8077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8078(int& val) {
    const int x = 8078;
    std::cout << 8078;
    for (int i = 0; i < 8078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8079(int& val) {
    const int x = 8079;
    std::cout << 8079;
    for (int i = 0; i < 8079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8080(int& val) {
    const int x = 8080;
    std::cout << 8080;
    for (int i = 0; i < 8080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8081(int& val) {
    const int x = 8081;
    std::cout << 8081;
    for (int i = 0; i < 8081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8082(int& val) {
    const int x = 8082;
    std::cout << 8082;
    for (int i = 0; i < 8082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8083(int& val) {
    const int x = 8083;
    std::cout << 8083;
    for (int i = 0; i < 8083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8084(int& val) {
    const int x = 8084;
    std::cout << 8084;
    for (int i = 0; i < 8084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8085(int& val) {
    const int x = 8085;
    std::cout << 8085;
    for (int i = 0; i < 8085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8086(int& val) {
    const int x = 8086;
    std::cout << 8086;
    for (int i = 0; i < 8086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8087(int& val) {
    const int x = 8087;
    std::cout << 8087;
    for (int i = 0; i < 8087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8088(int& val) {
    const int x = 8088;
    std::cout << 8088;
    for (int i = 0; i < 8088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8089(int& val) {
    const int x = 8089;
    std::cout << 8089;
    for (int i = 0; i < 8089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8090(int& val) {
    const int x = 8090;
    std::cout << 8090;
    for (int i = 0; i < 8090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8091(int& val) {
    const int x = 8091;
    std::cout << 8091;
    for (int i = 0; i < 8091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8092(int& val) {
    const int x = 8092;
    std::cout << 8092;
    for (int i = 0; i < 8092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8093(int& val) {
    const int x = 8093;
    std::cout << 8093;
    for (int i = 0; i < 8093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8094(int& val) {
    const int x = 8094;
    std::cout << 8094;
    for (int i = 0; i < 8094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8095(int& val) {
    const int x = 8095;
    std::cout << 8095;
    for (int i = 0; i < 8095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8096(int& val) {
    const int x = 8096;
    std::cout << 8096;
    for (int i = 0; i < 8096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8097(int& val) {
    const int x = 8097;
    std::cout << 8097;
    for (int i = 0; i < 8097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8098(int& val) {
    const int x = 8098;
    std::cout << 8098;
    for (int i = 0; i < 8098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8099(int& val) {
    const int x = 8099;
    std::cout << 8099;
    for (int i = 0; i < 8099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8100(int& val) {
    const int x = 8100;
    std::cout << 8100;
    for (int i = 0; i < 8100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8101(int& val) {
    const int x = 8101;
    std::cout << 8101;
    for (int i = 0; i < 8101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8102(int& val) {
    const int x = 8102;
    std::cout << 8102;
    for (int i = 0; i < 8102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8103(int& val) {
    const int x = 8103;
    std::cout << 8103;
    for (int i = 0; i < 8103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8104(int& val) {
    const int x = 8104;
    std::cout << 8104;
    for (int i = 0; i < 8104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8105(int& val) {
    const int x = 8105;
    std::cout << 8105;
    for (int i = 0; i < 8105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8106(int& val) {
    const int x = 8106;
    std::cout << 8106;
    for (int i = 0; i < 8106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8107(int& val) {
    const int x = 8107;
    std::cout << 8107;
    for (int i = 0; i < 8107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8108(int& val) {
    const int x = 8108;
    std::cout << 8108;
    for (int i = 0; i < 8108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8109(int& val) {
    const int x = 8109;
    std::cout << 8109;
    for (int i = 0; i < 8109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8110(int& val) {
    const int x = 8110;
    std::cout << 8110;
    for (int i = 0; i < 8110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8111(int& val) {
    const int x = 8111;
    std::cout << 8111;
    for (int i = 0; i < 8111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8112(int& val) {
    const int x = 8112;
    std::cout << 8112;
    for (int i = 0; i < 8112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8113(int& val) {
    const int x = 8113;
    std::cout << 8113;
    for (int i = 0; i < 8113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8114(int& val) {
    const int x = 8114;
    std::cout << 8114;
    for (int i = 0; i < 8114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8115(int& val) {
    const int x = 8115;
    std::cout << 8115;
    for (int i = 0; i < 8115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8116(int& val) {
    const int x = 8116;
    std::cout << 8116;
    for (int i = 0; i < 8116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8117(int& val) {
    const int x = 8117;
    std::cout << 8117;
    for (int i = 0; i < 8117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8118(int& val) {
    const int x = 8118;
    std::cout << 8118;
    for (int i = 0; i < 8118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8119(int& val) {
    const int x = 8119;
    std::cout << 8119;
    for (int i = 0; i < 8119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8120(int& val) {
    const int x = 8120;
    std::cout << 8120;
    for (int i = 0; i < 8120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8121(int& val) {
    const int x = 8121;
    std::cout << 8121;
    for (int i = 0; i < 8121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8122(int& val) {
    const int x = 8122;
    std::cout << 8122;
    for (int i = 0; i < 8122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8123(int& val) {
    const int x = 8123;
    std::cout << 8123;
    for (int i = 0; i < 8123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8124(int& val) {
    const int x = 8124;
    std::cout << 8124;
    for (int i = 0; i < 8124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8125(int& val) {
    const int x = 8125;
    std::cout << 8125;
    for (int i = 0; i < 8125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8126(int& val) {
    const int x = 8126;
    std::cout << 8126;
    for (int i = 0; i < 8126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8127(int& val) {
    const int x = 8127;
    std::cout << 8127;
    for (int i = 0; i < 8127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8128(int& val) {
    const int x = 8128;
    std::cout << 8128;
    for (int i = 0; i < 8128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8129(int& val) {
    const int x = 8129;
    std::cout << 8129;
    for (int i = 0; i < 8129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8130(int& val) {
    const int x = 8130;
    std::cout << 8130;
    for (int i = 0; i < 8130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8131(int& val) {
    const int x = 8131;
    std::cout << 8131;
    for (int i = 0; i < 8131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8132(int& val) {
    const int x = 8132;
    std::cout << 8132;
    for (int i = 0; i < 8132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8133(int& val) {
    const int x = 8133;
    std::cout << 8133;
    for (int i = 0; i < 8133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8134(int& val) {
    const int x = 8134;
    std::cout << 8134;
    for (int i = 0; i < 8134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8135(int& val) {
    const int x = 8135;
    std::cout << 8135;
    for (int i = 0; i < 8135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8136(int& val) {
    const int x = 8136;
    std::cout << 8136;
    for (int i = 0; i < 8136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8137(int& val) {
    const int x = 8137;
    std::cout << 8137;
    for (int i = 0; i < 8137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8138(int& val) {
    const int x = 8138;
    std::cout << 8138;
    for (int i = 0; i < 8138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8139(int& val) {
    const int x = 8139;
    std::cout << 8139;
    for (int i = 0; i < 8139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8140(int& val) {
    const int x = 8140;
    std::cout << 8140;
    for (int i = 0; i < 8140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8141(int& val) {
    const int x = 8141;
    std::cout << 8141;
    for (int i = 0; i < 8141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8142(int& val) {
    const int x = 8142;
    std::cout << 8142;
    for (int i = 0; i < 8142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8143(int& val) {
    const int x = 8143;
    std::cout << 8143;
    for (int i = 0; i < 8143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8144(int& val) {
    const int x = 8144;
    std::cout << 8144;
    for (int i = 0; i < 8144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8145(int& val) {
    const int x = 8145;
    std::cout << 8145;
    for (int i = 0; i < 8145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8146(int& val) {
    const int x = 8146;
    std::cout << 8146;
    for (int i = 0; i < 8146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8147(int& val) {
    const int x = 8147;
    std::cout << 8147;
    for (int i = 0; i < 8147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8148(int& val) {
    const int x = 8148;
    std::cout << 8148;
    for (int i = 0; i < 8148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8149(int& val) {
    const int x = 8149;
    std::cout << 8149;
    for (int i = 0; i < 8149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8150(int& val) {
    const int x = 8150;
    std::cout << 8150;
    for (int i = 0; i < 8150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8151(int& val) {
    const int x = 8151;
    std::cout << 8151;
    for (int i = 0; i < 8151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8152(int& val) {
    const int x = 8152;
    std::cout << 8152;
    for (int i = 0; i < 8152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8153(int& val) {
    const int x = 8153;
    std::cout << 8153;
    for (int i = 0; i < 8153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8154(int& val) {
    const int x = 8154;
    std::cout << 8154;
    for (int i = 0; i < 8154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8155(int& val) {
    const int x = 8155;
    std::cout << 8155;
    for (int i = 0; i < 8155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8156(int& val) {
    const int x = 8156;
    std::cout << 8156;
    for (int i = 0; i < 8156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8157(int& val) {
    const int x = 8157;
    std::cout << 8157;
    for (int i = 0; i < 8157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8158(int& val) {
    const int x = 8158;
    std::cout << 8158;
    for (int i = 0; i < 8158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8159(int& val) {
    const int x = 8159;
    std::cout << 8159;
    for (int i = 0; i < 8159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8160(int& val) {
    const int x = 8160;
    std::cout << 8160;
    for (int i = 0; i < 8160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8161(int& val) {
    const int x = 8161;
    std::cout << 8161;
    for (int i = 0; i < 8161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8162(int& val) {
    const int x = 8162;
    std::cout << 8162;
    for (int i = 0; i < 8162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8163(int& val) {
    const int x = 8163;
    std::cout << 8163;
    for (int i = 0; i < 8163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8164(int& val) {
    const int x = 8164;
    std::cout << 8164;
    for (int i = 0; i < 8164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8165(int& val) {
    const int x = 8165;
    std::cout << 8165;
    for (int i = 0; i < 8165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8166(int& val) {
    const int x = 8166;
    std::cout << 8166;
    for (int i = 0; i < 8166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8167(int& val) {
    const int x = 8167;
    std::cout << 8167;
    for (int i = 0; i < 8167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8168(int& val) {
    const int x = 8168;
    std::cout << 8168;
    for (int i = 0; i < 8168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8169(int& val) {
    const int x = 8169;
    std::cout << 8169;
    for (int i = 0; i < 8169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8170(int& val) {
    const int x = 8170;
    std::cout << 8170;
    for (int i = 0; i < 8170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8171(int& val) {
    const int x = 8171;
    std::cout << 8171;
    for (int i = 0; i < 8171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8172(int& val) {
    const int x = 8172;
    std::cout << 8172;
    for (int i = 0; i < 8172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8173(int& val) {
    const int x = 8173;
    std::cout << 8173;
    for (int i = 0; i < 8173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8174(int& val) {
    const int x = 8174;
    std::cout << 8174;
    for (int i = 0; i < 8174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8175(int& val) {
    const int x = 8175;
    std::cout << 8175;
    for (int i = 0; i < 8175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8176(int& val) {
    const int x = 8176;
    std::cout << 8176;
    for (int i = 0; i < 8176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8177(int& val) {
    const int x = 8177;
    std::cout << 8177;
    for (int i = 0; i < 8177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8178(int& val) {
    const int x = 8178;
    std::cout << 8178;
    for (int i = 0; i < 8178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8179(int& val) {
    const int x = 8179;
    std::cout << 8179;
    for (int i = 0; i < 8179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8180(int& val) {
    const int x = 8180;
    std::cout << 8180;
    for (int i = 0; i < 8180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8181(int& val) {
    const int x = 8181;
    std::cout << 8181;
    for (int i = 0; i < 8181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8182(int& val) {
    const int x = 8182;
    std::cout << 8182;
    for (int i = 0; i < 8182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8183(int& val) {
    const int x = 8183;
    std::cout << 8183;
    for (int i = 0; i < 8183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8184(int& val) {
    const int x = 8184;
    std::cout << 8184;
    for (int i = 0; i < 8184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8185(int& val) {
    const int x = 8185;
    std::cout << 8185;
    for (int i = 0; i < 8185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8186(int& val) {
    const int x = 8186;
    std::cout << 8186;
    for (int i = 0; i < 8186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8187(int& val) {
    const int x = 8187;
    std::cout << 8187;
    for (int i = 0; i < 8187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8188(int& val) {
    const int x = 8188;
    std::cout << 8188;
    for (int i = 0; i < 8188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8189(int& val) {
    const int x = 8189;
    std::cout << 8189;
    for (int i = 0; i < 8189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8190(int& val) {
    const int x = 8190;
    std::cout << 8190;
    for (int i = 0; i < 8190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8191(int& val) {
    const int x = 8191;
    std::cout << 8191;
    for (int i = 0; i < 8191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8192(int& val) {
    const int x = 8192;
    std::cout << 8192;
    for (int i = 0; i < 8192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8193(int& val) {
    const int x = 8193;
    std::cout << 8193;
    for (int i = 0; i < 8193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8194(int& val) {
    const int x = 8194;
    std::cout << 8194;
    for (int i = 0; i < 8194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8195(int& val) {
    const int x = 8195;
    std::cout << 8195;
    for (int i = 0; i < 8195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8196(int& val) {
    const int x = 8196;
    std::cout << 8196;
    for (int i = 0; i < 8196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8197(int& val) {
    const int x = 8197;
    std::cout << 8197;
    for (int i = 0; i < 8197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8198(int& val) {
    const int x = 8198;
    std::cout << 8198;
    for (int i = 0; i < 8198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8199(int& val) {
    const int x = 8199;
    std::cout << 8199;
    for (int i = 0; i < 8199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8200(int& val) {
    const int x = 8200;
    std::cout << 8200;
    for (int i = 0; i < 8200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8201(int& val) {
    const int x = 8201;
    std::cout << 8201;
    for (int i = 0; i < 8201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8202(int& val) {
    const int x = 8202;
    std::cout << 8202;
    for (int i = 0; i < 8202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8203(int& val) {
    const int x = 8203;
    std::cout << 8203;
    for (int i = 0; i < 8203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8204(int& val) {
    const int x = 8204;
    std::cout << 8204;
    for (int i = 0; i < 8204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8205(int& val) {
    const int x = 8205;
    std::cout << 8205;
    for (int i = 0; i < 8205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8206(int& val) {
    const int x = 8206;
    std::cout << 8206;
    for (int i = 0; i < 8206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8207(int& val) {
    const int x = 8207;
    std::cout << 8207;
    for (int i = 0; i < 8207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8208(int& val) {
    const int x = 8208;
    std::cout << 8208;
    for (int i = 0; i < 8208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8209(int& val) {
    const int x = 8209;
    std::cout << 8209;
    for (int i = 0; i < 8209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8210(int& val) {
    const int x = 8210;
    std::cout << 8210;
    for (int i = 0; i < 8210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8211(int& val) {
    const int x = 8211;
    std::cout << 8211;
    for (int i = 0; i < 8211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8212(int& val) {
    const int x = 8212;
    std::cout << 8212;
    for (int i = 0; i < 8212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8213(int& val) {
    const int x = 8213;
    std::cout << 8213;
    for (int i = 0; i < 8213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8214(int& val) {
    const int x = 8214;
    std::cout << 8214;
    for (int i = 0; i < 8214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8215(int& val) {
    const int x = 8215;
    std::cout << 8215;
    for (int i = 0; i < 8215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8216(int& val) {
    const int x = 8216;
    std::cout << 8216;
    for (int i = 0; i < 8216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8217(int& val) {
    const int x = 8217;
    std::cout << 8217;
    for (int i = 0; i < 8217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8218(int& val) {
    const int x = 8218;
    std::cout << 8218;
    for (int i = 0; i < 8218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8219(int& val) {
    const int x = 8219;
    std::cout << 8219;
    for (int i = 0; i < 8219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8220(int& val) {
    const int x = 8220;
    std::cout << 8220;
    for (int i = 0; i < 8220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8221(int& val) {
    const int x = 8221;
    std::cout << 8221;
    for (int i = 0; i < 8221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8222(int& val) {
    const int x = 8222;
    std::cout << 8222;
    for (int i = 0; i < 8222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8223(int& val) {
    const int x = 8223;
    std::cout << 8223;
    for (int i = 0; i < 8223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8224(int& val) {
    const int x = 8224;
    std::cout << 8224;
    for (int i = 0; i < 8224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8225(int& val) {
    const int x = 8225;
    std::cout << 8225;
    for (int i = 0; i < 8225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8226(int& val) {
    const int x = 8226;
    std::cout << 8226;
    for (int i = 0; i < 8226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8227(int& val) {
    const int x = 8227;
    std::cout << 8227;
    for (int i = 0; i < 8227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8228(int& val) {
    const int x = 8228;
    std::cout << 8228;
    for (int i = 0; i < 8228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8229(int& val) {
    const int x = 8229;
    std::cout << 8229;
    for (int i = 0; i < 8229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8230(int& val) {
    const int x = 8230;
    std::cout << 8230;
    for (int i = 0; i < 8230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8231(int& val) {
    const int x = 8231;
    std::cout << 8231;
    for (int i = 0; i < 8231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8232(int& val) {
    const int x = 8232;
    std::cout << 8232;
    for (int i = 0; i < 8232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8233(int& val) {
    const int x = 8233;
    std::cout << 8233;
    for (int i = 0; i < 8233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8234(int& val) {
    const int x = 8234;
    std::cout << 8234;
    for (int i = 0; i < 8234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8235(int& val) {
    const int x = 8235;
    std::cout << 8235;
    for (int i = 0; i < 8235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8236(int& val) {
    const int x = 8236;
    std::cout << 8236;
    for (int i = 0; i < 8236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8237(int& val) {
    const int x = 8237;
    std::cout << 8237;
    for (int i = 0; i < 8237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8238(int& val) {
    const int x = 8238;
    std::cout << 8238;
    for (int i = 0; i < 8238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8239(int& val) {
    const int x = 8239;
    std::cout << 8239;
    for (int i = 0; i < 8239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8240(int& val) {
    const int x = 8240;
    std::cout << 8240;
    for (int i = 0; i < 8240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8241(int& val) {
    const int x = 8241;
    std::cout << 8241;
    for (int i = 0; i < 8241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8242(int& val) {
    const int x = 8242;
    std::cout << 8242;
    for (int i = 0; i < 8242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8243(int& val) {
    const int x = 8243;
    std::cout << 8243;
    for (int i = 0; i < 8243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8244(int& val) {
    const int x = 8244;
    std::cout << 8244;
    for (int i = 0; i < 8244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8245(int& val) {
    const int x = 8245;
    std::cout << 8245;
    for (int i = 0; i < 8245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8246(int& val) {
    const int x = 8246;
    std::cout << 8246;
    for (int i = 0; i < 8246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8247(int& val) {
    const int x = 8247;
    std::cout << 8247;
    for (int i = 0; i < 8247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8248(int& val) {
    const int x = 8248;
    std::cout << 8248;
    for (int i = 0; i < 8248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8249(int& val) {
    const int x = 8249;
    std::cout << 8249;
    for (int i = 0; i < 8249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8250(int& val) {
    const int x = 8250;
    std::cout << 8250;
    for (int i = 0; i < 8250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8251(int& val) {
    const int x = 8251;
    std::cout << 8251;
    for (int i = 0; i < 8251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8252(int& val) {
    const int x = 8252;
    std::cout << 8252;
    for (int i = 0; i < 8252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8253(int& val) {
    const int x = 8253;
    std::cout << 8253;
    for (int i = 0; i < 8253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8254(int& val) {
    const int x = 8254;
    std::cout << 8254;
    for (int i = 0; i < 8254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8255(int& val) {
    const int x = 8255;
    std::cout << 8255;
    for (int i = 0; i < 8255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8256(int& val) {
    const int x = 8256;
    std::cout << 8256;
    for (int i = 0; i < 8256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8257(int& val) {
    const int x = 8257;
    std::cout << 8257;
    for (int i = 0; i < 8257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8258(int& val) {
    const int x = 8258;
    std::cout << 8258;
    for (int i = 0; i < 8258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8259(int& val) {
    const int x = 8259;
    std::cout << 8259;
    for (int i = 0; i < 8259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8260(int& val) {
    const int x = 8260;
    std::cout << 8260;
    for (int i = 0; i < 8260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8261(int& val) {
    const int x = 8261;
    std::cout << 8261;
    for (int i = 0; i < 8261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8262(int& val) {
    const int x = 8262;
    std::cout << 8262;
    for (int i = 0; i < 8262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8263(int& val) {
    const int x = 8263;
    std::cout << 8263;
    for (int i = 0; i < 8263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8264(int& val) {
    const int x = 8264;
    std::cout << 8264;
    for (int i = 0; i < 8264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8265(int& val) {
    const int x = 8265;
    std::cout << 8265;
    for (int i = 0; i < 8265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8266(int& val) {
    const int x = 8266;
    std::cout << 8266;
    for (int i = 0; i < 8266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8267(int& val) {
    const int x = 8267;
    std::cout << 8267;
    for (int i = 0; i < 8267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8268(int& val) {
    const int x = 8268;
    std::cout << 8268;
    for (int i = 0; i < 8268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8269(int& val) {
    const int x = 8269;
    std::cout << 8269;
    for (int i = 0; i < 8269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8270(int& val) {
    const int x = 8270;
    std::cout << 8270;
    for (int i = 0; i < 8270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8271(int& val) {
    const int x = 8271;
    std::cout << 8271;
    for (int i = 0; i < 8271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8272(int& val) {
    const int x = 8272;
    std::cout << 8272;
    for (int i = 0; i < 8272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8273(int& val) {
    const int x = 8273;
    std::cout << 8273;
    for (int i = 0; i < 8273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8274(int& val) {
    const int x = 8274;
    std::cout << 8274;
    for (int i = 0; i < 8274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8275(int& val) {
    const int x = 8275;
    std::cout << 8275;
    for (int i = 0; i < 8275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8276(int& val) {
    const int x = 8276;
    std::cout << 8276;
    for (int i = 0; i < 8276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8277(int& val) {
    const int x = 8277;
    std::cout << 8277;
    for (int i = 0; i < 8277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8278(int& val) {
    const int x = 8278;
    std::cout << 8278;
    for (int i = 0; i < 8278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8279(int& val) {
    const int x = 8279;
    std::cout << 8279;
    for (int i = 0; i < 8279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8280(int& val) {
    const int x = 8280;
    std::cout << 8280;
    for (int i = 0; i < 8280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8281(int& val) {
    const int x = 8281;
    std::cout << 8281;
    for (int i = 0; i < 8281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8282(int& val) {
    const int x = 8282;
    std::cout << 8282;
    for (int i = 0; i < 8282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8283(int& val) {
    const int x = 8283;
    std::cout << 8283;
    for (int i = 0; i < 8283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8284(int& val) {
    const int x = 8284;
    std::cout << 8284;
    for (int i = 0; i < 8284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8285(int& val) {
    const int x = 8285;
    std::cout << 8285;
    for (int i = 0; i < 8285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8286(int& val) {
    const int x = 8286;
    std::cout << 8286;
    for (int i = 0; i < 8286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8287(int& val) {
    const int x = 8287;
    std::cout << 8287;
    for (int i = 0; i < 8287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8288(int& val) {
    const int x = 8288;
    std::cout << 8288;
    for (int i = 0; i < 8288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8289(int& val) {
    const int x = 8289;
    std::cout << 8289;
    for (int i = 0; i < 8289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8290(int& val) {
    const int x = 8290;
    std::cout << 8290;
    for (int i = 0; i < 8290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8291(int& val) {
    const int x = 8291;
    std::cout << 8291;
    for (int i = 0; i < 8291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8292(int& val) {
    const int x = 8292;
    std::cout << 8292;
    for (int i = 0; i < 8292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8293(int& val) {
    const int x = 8293;
    std::cout << 8293;
    for (int i = 0; i < 8293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8294(int& val) {
    const int x = 8294;
    std::cout << 8294;
    for (int i = 0; i < 8294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8295(int& val) {
    const int x = 8295;
    std::cout << 8295;
    for (int i = 0; i < 8295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8296(int& val) {
    const int x = 8296;
    std::cout << 8296;
    for (int i = 0; i < 8296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8297(int& val) {
    const int x = 8297;
    std::cout << 8297;
    for (int i = 0; i < 8297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8298(int& val) {
    const int x = 8298;
    std::cout << 8298;
    for (int i = 0; i < 8298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8299(int& val) {
    const int x = 8299;
    std::cout << 8299;
    for (int i = 0; i < 8299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8300(int& val) {
    const int x = 8300;
    std::cout << 8300;
    for (int i = 0; i < 8300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8301(int& val) {
    const int x = 8301;
    std::cout << 8301;
    for (int i = 0; i < 8301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8302(int& val) {
    const int x = 8302;
    std::cout << 8302;
    for (int i = 0; i < 8302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8303(int& val) {
    const int x = 8303;
    std::cout << 8303;
    for (int i = 0; i < 8303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8304(int& val) {
    const int x = 8304;
    std::cout << 8304;
    for (int i = 0; i < 8304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8305(int& val) {
    const int x = 8305;
    std::cout << 8305;
    for (int i = 0; i < 8305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8306(int& val) {
    const int x = 8306;
    std::cout << 8306;
    for (int i = 0; i < 8306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8307(int& val) {
    const int x = 8307;
    std::cout << 8307;
    for (int i = 0; i < 8307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8308(int& val) {
    const int x = 8308;
    std::cout << 8308;
    for (int i = 0; i < 8308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8309(int& val) {
    const int x = 8309;
    std::cout << 8309;
    for (int i = 0; i < 8309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8310(int& val) {
    const int x = 8310;
    std::cout << 8310;
    for (int i = 0; i < 8310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8311(int& val) {
    const int x = 8311;
    std::cout << 8311;
    for (int i = 0; i < 8311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8312(int& val) {
    const int x = 8312;
    std::cout << 8312;
    for (int i = 0; i < 8312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8313(int& val) {
    const int x = 8313;
    std::cout << 8313;
    for (int i = 0; i < 8313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8314(int& val) {
    const int x = 8314;
    std::cout << 8314;
    for (int i = 0; i < 8314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8315(int& val) {
    const int x = 8315;
    std::cout << 8315;
    for (int i = 0; i < 8315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8316(int& val) {
    const int x = 8316;
    std::cout << 8316;
    for (int i = 0; i < 8316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8317(int& val) {
    const int x = 8317;
    std::cout << 8317;
    for (int i = 0; i < 8317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8318(int& val) {
    const int x = 8318;
    std::cout << 8318;
    for (int i = 0; i < 8318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8319(int& val) {
    const int x = 8319;
    std::cout << 8319;
    for (int i = 0; i < 8319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8320(int& val) {
    const int x = 8320;
    std::cout << 8320;
    for (int i = 0; i < 8320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8321(int& val) {
    const int x = 8321;
    std::cout << 8321;
    for (int i = 0; i < 8321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8322(int& val) {
    const int x = 8322;
    std::cout << 8322;
    for (int i = 0; i < 8322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8323(int& val) {
    const int x = 8323;
    std::cout << 8323;
    for (int i = 0; i < 8323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8324(int& val) {
    const int x = 8324;
    std::cout << 8324;
    for (int i = 0; i < 8324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8325(int& val) {
    const int x = 8325;
    std::cout << 8325;
    for (int i = 0; i < 8325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8326(int& val) {
    const int x = 8326;
    std::cout << 8326;
    for (int i = 0; i < 8326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8327(int& val) {
    const int x = 8327;
    std::cout << 8327;
    for (int i = 0; i < 8327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8328(int& val) {
    const int x = 8328;
    std::cout << 8328;
    for (int i = 0; i < 8328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8329(int& val) {
    const int x = 8329;
    std::cout << 8329;
    for (int i = 0; i < 8329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8330(int& val) {
    const int x = 8330;
    std::cout << 8330;
    for (int i = 0; i < 8330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8331(int& val) {
    const int x = 8331;
    std::cout << 8331;
    for (int i = 0; i < 8331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8332(int& val) {
    const int x = 8332;
    std::cout << 8332;
    for (int i = 0; i < 8332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8333(int& val) {
    const int x = 8333;
    std::cout << 8333;
    for (int i = 0; i < 8333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8334(int& val) {
    const int x = 8334;
    std::cout << 8334;
    for (int i = 0; i < 8334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8335(int& val) {
    const int x = 8335;
    std::cout << 8335;
    for (int i = 0; i < 8335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8336(int& val) {
    const int x = 8336;
    std::cout << 8336;
    for (int i = 0; i < 8336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8337(int& val) {
    const int x = 8337;
    std::cout << 8337;
    for (int i = 0; i < 8337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8338(int& val) {
    const int x = 8338;
    std::cout << 8338;
    for (int i = 0; i < 8338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8339(int& val) {
    const int x = 8339;
    std::cout << 8339;
    for (int i = 0; i < 8339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8340(int& val) {
    const int x = 8340;
    std::cout << 8340;
    for (int i = 0; i < 8340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8341(int& val) {
    const int x = 8341;
    std::cout << 8341;
    for (int i = 0; i < 8341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8342(int& val) {
    const int x = 8342;
    std::cout << 8342;
    for (int i = 0; i < 8342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8343(int& val) {
    const int x = 8343;
    std::cout << 8343;
    for (int i = 0; i < 8343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8344(int& val) {
    const int x = 8344;
    std::cout << 8344;
    for (int i = 0; i < 8344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8345(int& val) {
    const int x = 8345;
    std::cout << 8345;
    for (int i = 0; i < 8345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8346(int& val) {
    const int x = 8346;
    std::cout << 8346;
    for (int i = 0; i < 8346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8347(int& val) {
    const int x = 8347;
    std::cout << 8347;
    for (int i = 0; i < 8347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8348(int& val) {
    const int x = 8348;
    std::cout << 8348;
    for (int i = 0; i < 8348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8349(int& val) {
    const int x = 8349;
    std::cout << 8349;
    for (int i = 0; i < 8349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8350(int& val) {
    const int x = 8350;
    std::cout << 8350;
    for (int i = 0; i < 8350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8351(int& val) {
    const int x = 8351;
    std::cout << 8351;
    for (int i = 0; i < 8351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8352(int& val) {
    const int x = 8352;
    std::cout << 8352;
    for (int i = 0; i < 8352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8353(int& val) {
    const int x = 8353;
    std::cout << 8353;
    for (int i = 0; i < 8353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8354(int& val) {
    const int x = 8354;
    std::cout << 8354;
    for (int i = 0; i < 8354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8355(int& val) {
    const int x = 8355;
    std::cout << 8355;
    for (int i = 0; i < 8355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8356(int& val) {
    const int x = 8356;
    std::cout << 8356;
    for (int i = 0; i < 8356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8357(int& val) {
    const int x = 8357;
    std::cout << 8357;
    for (int i = 0; i < 8357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8358(int& val) {
    const int x = 8358;
    std::cout << 8358;
    for (int i = 0; i < 8358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8359(int& val) {
    const int x = 8359;
    std::cout << 8359;
    for (int i = 0; i < 8359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8360(int& val) {
    const int x = 8360;
    std::cout << 8360;
    for (int i = 0; i < 8360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8361(int& val) {
    const int x = 8361;
    std::cout << 8361;
    for (int i = 0; i < 8361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8362(int& val) {
    const int x = 8362;
    std::cout << 8362;
    for (int i = 0; i < 8362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8363(int& val) {
    const int x = 8363;
    std::cout << 8363;
    for (int i = 0; i < 8363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8364(int& val) {
    const int x = 8364;
    std::cout << 8364;
    for (int i = 0; i < 8364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8365(int& val) {
    const int x = 8365;
    std::cout << 8365;
    for (int i = 0; i < 8365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8366(int& val) {
    const int x = 8366;
    std::cout << 8366;
    for (int i = 0; i < 8366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8367(int& val) {
    const int x = 8367;
    std::cout << 8367;
    for (int i = 0; i < 8367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8368(int& val) {
    const int x = 8368;
    std::cout << 8368;
    for (int i = 0; i < 8368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8369(int& val) {
    const int x = 8369;
    std::cout << 8369;
    for (int i = 0; i < 8369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8370(int& val) {
    const int x = 8370;
    std::cout << 8370;
    for (int i = 0; i < 8370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8371(int& val) {
    const int x = 8371;
    std::cout << 8371;
    for (int i = 0; i < 8371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8372(int& val) {
    const int x = 8372;
    std::cout << 8372;
    for (int i = 0; i < 8372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8373(int& val) {
    const int x = 8373;
    std::cout << 8373;
    for (int i = 0; i < 8373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8374(int& val) {
    const int x = 8374;
    std::cout << 8374;
    for (int i = 0; i < 8374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8375(int& val) {
    const int x = 8375;
    std::cout << 8375;
    for (int i = 0; i < 8375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8376(int& val) {
    const int x = 8376;
    std::cout << 8376;
    for (int i = 0; i < 8376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8377(int& val) {
    const int x = 8377;
    std::cout << 8377;
    for (int i = 0; i < 8377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8378(int& val) {
    const int x = 8378;
    std::cout << 8378;
    for (int i = 0; i < 8378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8379(int& val) {
    const int x = 8379;
    std::cout << 8379;
    for (int i = 0; i < 8379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8380(int& val) {
    const int x = 8380;
    std::cout << 8380;
    for (int i = 0; i < 8380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8381(int& val) {
    const int x = 8381;
    std::cout << 8381;
    for (int i = 0; i < 8381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8382(int& val) {
    const int x = 8382;
    std::cout << 8382;
    for (int i = 0; i < 8382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8383(int& val) {
    const int x = 8383;
    std::cout << 8383;
    for (int i = 0; i < 8383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8384(int& val) {
    const int x = 8384;
    std::cout << 8384;
    for (int i = 0; i < 8384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8385(int& val) {
    const int x = 8385;
    std::cout << 8385;
    for (int i = 0; i < 8385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8386(int& val) {
    const int x = 8386;
    std::cout << 8386;
    for (int i = 0; i < 8386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8387(int& val) {
    const int x = 8387;
    std::cout << 8387;
    for (int i = 0; i < 8387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8388(int& val) {
    const int x = 8388;
    std::cout << 8388;
    for (int i = 0; i < 8388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8389(int& val) {
    const int x = 8389;
    std::cout << 8389;
    for (int i = 0; i < 8389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8390(int& val) {
    const int x = 8390;
    std::cout << 8390;
    for (int i = 0; i < 8390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8391(int& val) {
    const int x = 8391;
    std::cout << 8391;
    for (int i = 0; i < 8391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8392(int& val) {
    const int x = 8392;
    std::cout << 8392;
    for (int i = 0; i < 8392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8393(int& val) {
    const int x = 8393;
    std::cout << 8393;
    for (int i = 0; i < 8393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8394(int& val) {
    const int x = 8394;
    std::cout << 8394;
    for (int i = 0; i < 8394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8395(int& val) {
    const int x = 8395;
    std::cout << 8395;
    for (int i = 0; i < 8395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8396(int& val) {
    const int x = 8396;
    std::cout << 8396;
    for (int i = 0; i < 8396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8397(int& val) {
    const int x = 8397;
    std::cout << 8397;
    for (int i = 0; i < 8397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8398(int& val) {
    const int x = 8398;
    std::cout << 8398;
    for (int i = 0; i < 8398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8399(int& val) {
    const int x = 8399;
    std::cout << 8399;
    for (int i = 0; i < 8399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8400(int& val) {
    const int x = 8400;
    std::cout << 8400;
    for (int i = 0; i < 8400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8401(int& val) {
    const int x = 8401;
    std::cout << 8401;
    for (int i = 0; i < 8401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8402(int& val) {
    const int x = 8402;
    std::cout << 8402;
    for (int i = 0; i < 8402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8403(int& val) {
    const int x = 8403;
    std::cout << 8403;
    for (int i = 0; i < 8403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8404(int& val) {
    const int x = 8404;
    std::cout << 8404;
    for (int i = 0; i < 8404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8405(int& val) {
    const int x = 8405;
    std::cout << 8405;
    for (int i = 0; i < 8405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8406(int& val) {
    const int x = 8406;
    std::cout << 8406;
    for (int i = 0; i < 8406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8407(int& val) {
    const int x = 8407;
    std::cout << 8407;
    for (int i = 0; i < 8407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8408(int& val) {
    const int x = 8408;
    std::cout << 8408;
    for (int i = 0; i < 8408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8409(int& val) {
    const int x = 8409;
    std::cout << 8409;
    for (int i = 0; i < 8409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8410(int& val) {
    const int x = 8410;
    std::cout << 8410;
    for (int i = 0; i < 8410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8411(int& val) {
    const int x = 8411;
    std::cout << 8411;
    for (int i = 0; i < 8411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8412(int& val) {
    const int x = 8412;
    std::cout << 8412;
    for (int i = 0; i < 8412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8413(int& val) {
    const int x = 8413;
    std::cout << 8413;
    for (int i = 0; i < 8413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8414(int& val) {
    const int x = 8414;
    std::cout << 8414;
    for (int i = 0; i < 8414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8415(int& val) {
    const int x = 8415;
    std::cout << 8415;
    for (int i = 0; i < 8415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8416(int& val) {
    const int x = 8416;
    std::cout << 8416;
    for (int i = 0; i < 8416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8417(int& val) {
    const int x = 8417;
    std::cout << 8417;
    for (int i = 0; i < 8417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8418(int& val) {
    const int x = 8418;
    std::cout << 8418;
    for (int i = 0; i < 8418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8419(int& val) {
    const int x = 8419;
    std::cout << 8419;
    for (int i = 0; i < 8419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8420(int& val) {
    const int x = 8420;
    std::cout << 8420;
    for (int i = 0; i < 8420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8421(int& val) {
    const int x = 8421;
    std::cout << 8421;
    for (int i = 0; i < 8421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8422(int& val) {
    const int x = 8422;
    std::cout << 8422;
    for (int i = 0; i < 8422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8423(int& val) {
    const int x = 8423;
    std::cout << 8423;
    for (int i = 0; i < 8423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8424(int& val) {
    const int x = 8424;
    std::cout << 8424;
    for (int i = 0; i < 8424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8425(int& val) {
    const int x = 8425;
    std::cout << 8425;
    for (int i = 0; i < 8425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8426(int& val) {
    const int x = 8426;
    std::cout << 8426;
    for (int i = 0; i < 8426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8427(int& val) {
    const int x = 8427;
    std::cout << 8427;
    for (int i = 0; i < 8427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8428(int& val) {
    const int x = 8428;
    std::cout << 8428;
    for (int i = 0; i < 8428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8429(int& val) {
    const int x = 8429;
    std::cout << 8429;
    for (int i = 0; i < 8429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8430(int& val) {
    const int x = 8430;
    std::cout << 8430;
    for (int i = 0; i < 8430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8431(int& val) {
    const int x = 8431;
    std::cout << 8431;
    for (int i = 0; i < 8431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8432(int& val) {
    const int x = 8432;
    std::cout << 8432;
    for (int i = 0; i < 8432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8433(int& val) {
    const int x = 8433;
    std::cout << 8433;
    for (int i = 0; i < 8433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8434(int& val) {
    const int x = 8434;
    std::cout << 8434;
    for (int i = 0; i < 8434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8435(int& val) {
    const int x = 8435;
    std::cout << 8435;
    for (int i = 0; i < 8435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8436(int& val) {
    const int x = 8436;
    std::cout << 8436;
    for (int i = 0; i < 8436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8437(int& val) {
    const int x = 8437;
    std::cout << 8437;
    for (int i = 0; i < 8437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8438(int& val) {
    const int x = 8438;
    std::cout << 8438;
    for (int i = 0; i < 8438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8439(int& val) {
    const int x = 8439;
    std::cout << 8439;
    for (int i = 0; i < 8439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8440(int& val) {
    const int x = 8440;
    std::cout << 8440;
    for (int i = 0; i < 8440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8441(int& val) {
    const int x = 8441;
    std::cout << 8441;
    for (int i = 0; i < 8441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8442(int& val) {
    const int x = 8442;
    std::cout << 8442;
    for (int i = 0; i < 8442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8443(int& val) {
    const int x = 8443;
    std::cout << 8443;
    for (int i = 0; i < 8443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8444(int& val) {
    const int x = 8444;
    std::cout << 8444;
    for (int i = 0; i < 8444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8445(int& val) {
    const int x = 8445;
    std::cout << 8445;
    for (int i = 0; i < 8445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8446(int& val) {
    const int x = 8446;
    std::cout << 8446;
    for (int i = 0; i < 8446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8447(int& val) {
    const int x = 8447;
    std::cout << 8447;
    for (int i = 0; i < 8447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8448(int& val) {
    const int x = 8448;
    std::cout << 8448;
    for (int i = 0; i < 8448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8449(int& val) {
    const int x = 8449;
    std::cout << 8449;
    for (int i = 0; i < 8449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8450(int& val) {
    const int x = 8450;
    std::cout << 8450;
    for (int i = 0; i < 8450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8451(int& val) {
    const int x = 8451;
    std::cout << 8451;
    for (int i = 0; i < 8451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8452(int& val) {
    const int x = 8452;
    std::cout << 8452;
    for (int i = 0; i < 8452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8453(int& val) {
    const int x = 8453;
    std::cout << 8453;
    for (int i = 0; i < 8453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8454(int& val) {
    const int x = 8454;
    std::cout << 8454;
    for (int i = 0; i < 8454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8455(int& val) {
    const int x = 8455;
    std::cout << 8455;
    for (int i = 0; i < 8455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8456(int& val) {
    const int x = 8456;
    std::cout << 8456;
    for (int i = 0; i < 8456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8457(int& val) {
    const int x = 8457;
    std::cout << 8457;
    for (int i = 0; i < 8457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8458(int& val) {
    const int x = 8458;
    std::cout << 8458;
    for (int i = 0; i < 8458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8459(int& val) {
    const int x = 8459;
    std::cout << 8459;
    for (int i = 0; i < 8459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8460(int& val) {
    const int x = 8460;
    std::cout << 8460;
    for (int i = 0; i < 8460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8461(int& val) {
    const int x = 8461;
    std::cout << 8461;
    for (int i = 0; i < 8461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8462(int& val) {
    const int x = 8462;
    std::cout << 8462;
    for (int i = 0; i < 8462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8463(int& val) {
    const int x = 8463;
    std::cout << 8463;
    for (int i = 0; i < 8463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8464(int& val) {
    const int x = 8464;
    std::cout << 8464;
    for (int i = 0; i < 8464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8465(int& val) {
    const int x = 8465;
    std::cout << 8465;
    for (int i = 0; i < 8465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8466(int& val) {
    const int x = 8466;
    std::cout << 8466;
    for (int i = 0; i < 8466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8467(int& val) {
    const int x = 8467;
    std::cout << 8467;
    for (int i = 0; i < 8467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8468(int& val) {
    const int x = 8468;
    std::cout << 8468;
    for (int i = 0; i < 8468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8469(int& val) {
    const int x = 8469;
    std::cout << 8469;
    for (int i = 0; i < 8469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8470(int& val) {
    const int x = 8470;
    std::cout << 8470;
    for (int i = 0; i < 8470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8471(int& val) {
    const int x = 8471;
    std::cout << 8471;
    for (int i = 0; i < 8471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8472(int& val) {
    const int x = 8472;
    std::cout << 8472;
    for (int i = 0; i < 8472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8473(int& val) {
    const int x = 8473;
    std::cout << 8473;
    for (int i = 0; i < 8473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8474(int& val) {
    const int x = 8474;
    std::cout << 8474;
    for (int i = 0; i < 8474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8475(int& val) {
    const int x = 8475;
    std::cout << 8475;
    for (int i = 0; i < 8475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8476(int& val) {
    const int x = 8476;
    std::cout << 8476;
    for (int i = 0; i < 8476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8477(int& val) {
    const int x = 8477;
    std::cout << 8477;
    for (int i = 0; i < 8477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8478(int& val) {
    const int x = 8478;
    std::cout << 8478;
    for (int i = 0; i < 8478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8479(int& val) {
    const int x = 8479;
    std::cout << 8479;
    for (int i = 0; i < 8479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8480(int& val) {
    const int x = 8480;
    std::cout << 8480;
    for (int i = 0; i < 8480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8481(int& val) {
    const int x = 8481;
    std::cout << 8481;
    for (int i = 0; i < 8481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8482(int& val) {
    const int x = 8482;
    std::cout << 8482;
    for (int i = 0; i < 8482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8483(int& val) {
    const int x = 8483;
    std::cout << 8483;
    for (int i = 0; i < 8483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8484(int& val) {
    const int x = 8484;
    std::cout << 8484;
    for (int i = 0; i < 8484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8485(int& val) {
    const int x = 8485;
    std::cout << 8485;
    for (int i = 0; i < 8485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8486(int& val) {
    const int x = 8486;
    std::cout << 8486;
    for (int i = 0; i < 8486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8487(int& val) {
    const int x = 8487;
    std::cout << 8487;
    for (int i = 0; i < 8487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8488(int& val) {
    const int x = 8488;
    std::cout << 8488;
    for (int i = 0; i < 8488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8489(int& val) {
    const int x = 8489;
    std::cout << 8489;
    for (int i = 0; i < 8489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8490(int& val) {
    const int x = 8490;
    std::cout << 8490;
    for (int i = 0; i < 8490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8491(int& val) {
    const int x = 8491;
    std::cout << 8491;
    for (int i = 0; i < 8491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8492(int& val) {
    const int x = 8492;
    std::cout << 8492;
    for (int i = 0; i < 8492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8493(int& val) {
    const int x = 8493;
    std::cout << 8493;
    for (int i = 0; i < 8493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8494(int& val) {
    const int x = 8494;
    std::cout << 8494;
    for (int i = 0; i < 8494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8495(int& val) {
    const int x = 8495;
    std::cout << 8495;
    for (int i = 0; i < 8495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8496(int& val) {
    const int x = 8496;
    std::cout << 8496;
    for (int i = 0; i < 8496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8497(int& val) {
    const int x = 8497;
    std::cout << 8497;
    for (int i = 0; i < 8497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8498(int& val) {
    const int x = 8498;
    std::cout << 8498;
    for (int i = 0; i < 8498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8499(int& val) {
    const int x = 8499;
    std::cout << 8499;
    for (int i = 0; i < 8499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8500(int& val) {
    const int x = 8500;
    std::cout << 8500;
    for (int i = 0; i < 8500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8501(int& val) {
    const int x = 8501;
    std::cout << 8501;
    for (int i = 0; i < 8501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8502(int& val) {
    const int x = 8502;
    std::cout << 8502;
    for (int i = 0; i < 8502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8503(int& val) {
    const int x = 8503;
    std::cout << 8503;
    for (int i = 0; i < 8503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8504(int& val) {
    const int x = 8504;
    std::cout << 8504;
    for (int i = 0; i < 8504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8505(int& val) {
    const int x = 8505;
    std::cout << 8505;
    for (int i = 0; i < 8505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8506(int& val) {
    const int x = 8506;
    std::cout << 8506;
    for (int i = 0; i < 8506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8507(int& val) {
    const int x = 8507;
    std::cout << 8507;
    for (int i = 0; i < 8507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8508(int& val) {
    const int x = 8508;
    std::cout << 8508;
    for (int i = 0; i < 8508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8509(int& val) {
    const int x = 8509;
    std::cout << 8509;
    for (int i = 0; i < 8509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8510(int& val) {
    const int x = 8510;
    std::cout << 8510;
    for (int i = 0; i < 8510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8511(int& val) {
    const int x = 8511;
    std::cout << 8511;
    for (int i = 0; i < 8511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8512(int& val) {
    const int x = 8512;
    std::cout << 8512;
    for (int i = 0; i < 8512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8513(int& val) {
    const int x = 8513;
    std::cout << 8513;
    for (int i = 0; i < 8513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8514(int& val) {
    const int x = 8514;
    std::cout << 8514;
    for (int i = 0; i < 8514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8515(int& val) {
    const int x = 8515;
    std::cout << 8515;
    for (int i = 0; i < 8515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8516(int& val) {
    const int x = 8516;
    std::cout << 8516;
    for (int i = 0; i < 8516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8517(int& val) {
    const int x = 8517;
    std::cout << 8517;
    for (int i = 0; i < 8517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8518(int& val) {
    const int x = 8518;
    std::cout << 8518;
    for (int i = 0; i < 8518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8519(int& val) {
    const int x = 8519;
    std::cout << 8519;
    for (int i = 0; i < 8519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8520(int& val) {
    const int x = 8520;
    std::cout << 8520;
    for (int i = 0; i < 8520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8521(int& val) {
    const int x = 8521;
    std::cout << 8521;
    for (int i = 0; i < 8521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8522(int& val) {
    const int x = 8522;
    std::cout << 8522;
    for (int i = 0; i < 8522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8523(int& val) {
    const int x = 8523;
    std::cout << 8523;
    for (int i = 0; i < 8523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8524(int& val) {
    const int x = 8524;
    std::cout << 8524;
    for (int i = 0; i < 8524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8525(int& val) {
    const int x = 8525;
    std::cout << 8525;
    for (int i = 0; i < 8525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8526(int& val) {
    const int x = 8526;
    std::cout << 8526;
    for (int i = 0; i < 8526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8527(int& val) {
    const int x = 8527;
    std::cout << 8527;
    for (int i = 0; i < 8527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8528(int& val) {
    const int x = 8528;
    std::cout << 8528;
    for (int i = 0; i < 8528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8529(int& val) {
    const int x = 8529;
    std::cout << 8529;
    for (int i = 0; i < 8529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8530(int& val) {
    const int x = 8530;
    std::cout << 8530;
    for (int i = 0; i < 8530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8531(int& val) {
    const int x = 8531;
    std::cout << 8531;
    for (int i = 0; i < 8531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8532(int& val) {
    const int x = 8532;
    std::cout << 8532;
    for (int i = 0; i < 8532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8533(int& val) {
    const int x = 8533;
    std::cout << 8533;
    for (int i = 0; i < 8533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8534(int& val) {
    const int x = 8534;
    std::cout << 8534;
    for (int i = 0; i < 8534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8535(int& val) {
    const int x = 8535;
    std::cout << 8535;
    for (int i = 0; i < 8535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8536(int& val) {
    const int x = 8536;
    std::cout << 8536;
    for (int i = 0; i < 8536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8537(int& val) {
    const int x = 8537;
    std::cout << 8537;
    for (int i = 0; i < 8537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8538(int& val) {
    const int x = 8538;
    std::cout << 8538;
    for (int i = 0; i < 8538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8539(int& val) {
    const int x = 8539;
    std::cout << 8539;
    for (int i = 0; i < 8539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8540(int& val) {
    const int x = 8540;
    std::cout << 8540;
    for (int i = 0; i < 8540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8541(int& val) {
    const int x = 8541;
    std::cout << 8541;
    for (int i = 0; i < 8541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8542(int& val) {
    const int x = 8542;
    std::cout << 8542;
    for (int i = 0; i < 8542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8543(int& val) {
    const int x = 8543;
    std::cout << 8543;
    for (int i = 0; i < 8543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8544(int& val) {
    const int x = 8544;
    std::cout << 8544;
    for (int i = 0; i < 8544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8545(int& val) {
    const int x = 8545;
    std::cout << 8545;
    for (int i = 0; i < 8545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8546(int& val) {
    const int x = 8546;
    std::cout << 8546;
    for (int i = 0; i < 8546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8547(int& val) {
    const int x = 8547;
    std::cout << 8547;
    for (int i = 0; i < 8547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8548(int& val) {
    const int x = 8548;
    std::cout << 8548;
    for (int i = 0; i < 8548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8549(int& val) {
    const int x = 8549;
    std::cout << 8549;
    for (int i = 0; i < 8549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8550(int& val) {
    const int x = 8550;
    std::cout << 8550;
    for (int i = 0; i < 8550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8551(int& val) {
    const int x = 8551;
    std::cout << 8551;
    for (int i = 0; i < 8551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8552(int& val) {
    const int x = 8552;
    std::cout << 8552;
    for (int i = 0; i < 8552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8553(int& val) {
    const int x = 8553;
    std::cout << 8553;
    for (int i = 0; i < 8553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8554(int& val) {
    const int x = 8554;
    std::cout << 8554;
    for (int i = 0; i < 8554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8555(int& val) {
    const int x = 8555;
    std::cout << 8555;
    for (int i = 0; i < 8555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8556(int& val) {
    const int x = 8556;
    std::cout << 8556;
    for (int i = 0; i < 8556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8557(int& val) {
    const int x = 8557;
    std::cout << 8557;
    for (int i = 0; i < 8557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8558(int& val) {
    const int x = 8558;
    std::cout << 8558;
    for (int i = 0; i < 8558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8559(int& val) {
    const int x = 8559;
    std::cout << 8559;
    for (int i = 0; i < 8559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8560(int& val) {
    const int x = 8560;
    std::cout << 8560;
    for (int i = 0; i < 8560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8561(int& val) {
    const int x = 8561;
    std::cout << 8561;
    for (int i = 0; i < 8561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8562(int& val) {
    const int x = 8562;
    std::cout << 8562;
    for (int i = 0; i < 8562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8563(int& val) {
    const int x = 8563;
    std::cout << 8563;
    for (int i = 0; i < 8563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8564(int& val) {
    const int x = 8564;
    std::cout << 8564;
    for (int i = 0; i < 8564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8565(int& val) {
    const int x = 8565;
    std::cout << 8565;
    for (int i = 0; i < 8565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8566(int& val) {
    const int x = 8566;
    std::cout << 8566;
    for (int i = 0; i < 8566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8567(int& val) {
    const int x = 8567;
    std::cout << 8567;
    for (int i = 0; i < 8567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8568(int& val) {
    const int x = 8568;
    std::cout << 8568;
    for (int i = 0; i < 8568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8569(int& val) {
    const int x = 8569;
    std::cout << 8569;
    for (int i = 0; i < 8569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8570(int& val) {
    const int x = 8570;
    std::cout << 8570;
    for (int i = 0; i < 8570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8571(int& val) {
    const int x = 8571;
    std::cout << 8571;
    for (int i = 0; i < 8571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8572(int& val) {
    const int x = 8572;
    std::cout << 8572;
    for (int i = 0; i < 8572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8573(int& val) {
    const int x = 8573;
    std::cout << 8573;
    for (int i = 0; i < 8573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8574(int& val) {
    const int x = 8574;
    std::cout << 8574;
    for (int i = 0; i < 8574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8575(int& val) {
    const int x = 8575;
    std::cout << 8575;
    for (int i = 0; i < 8575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8576(int& val) {
    const int x = 8576;
    std::cout << 8576;
    for (int i = 0; i < 8576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8577(int& val) {
    const int x = 8577;
    std::cout << 8577;
    for (int i = 0; i < 8577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8578(int& val) {
    const int x = 8578;
    std::cout << 8578;
    for (int i = 0; i < 8578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8579(int& val) {
    const int x = 8579;
    std::cout << 8579;
    for (int i = 0; i < 8579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8580(int& val) {
    const int x = 8580;
    std::cout << 8580;
    for (int i = 0; i < 8580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8581(int& val) {
    const int x = 8581;
    std::cout << 8581;
    for (int i = 0; i < 8581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8582(int& val) {
    const int x = 8582;
    std::cout << 8582;
    for (int i = 0; i < 8582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8583(int& val) {
    const int x = 8583;
    std::cout << 8583;
    for (int i = 0; i < 8583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8584(int& val) {
    const int x = 8584;
    std::cout << 8584;
    for (int i = 0; i < 8584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8585(int& val) {
    const int x = 8585;
    std::cout << 8585;
    for (int i = 0; i < 8585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8586(int& val) {
    const int x = 8586;
    std::cout << 8586;
    for (int i = 0; i < 8586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8587(int& val) {
    const int x = 8587;
    std::cout << 8587;
    for (int i = 0; i < 8587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8588(int& val) {
    const int x = 8588;
    std::cout << 8588;
    for (int i = 0; i < 8588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8589(int& val) {
    const int x = 8589;
    std::cout << 8589;
    for (int i = 0; i < 8589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8590(int& val) {
    const int x = 8590;
    std::cout << 8590;
    for (int i = 0; i < 8590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8591(int& val) {
    const int x = 8591;
    std::cout << 8591;
    for (int i = 0; i < 8591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8592(int& val) {
    const int x = 8592;
    std::cout << 8592;
    for (int i = 0; i < 8592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8593(int& val) {
    const int x = 8593;
    std::cout << 8593;
    for (int i = 0; i < 8593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8594(int& val) {
    const int x = 8594;
    std::cout << 8594;
    for (int i = 0; i < 8594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8595(int& val) {
    const int x = 8595;
    std::cout << 8595;
    for (int i = 0; i < 8595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8596(int& val) {
    const int x = 8596;
    std::cout << 8596;
    for (int i = 0; i < 8596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8597(int& val) {
    const int x = 8597;
    std::cout << 8597;
    for (int i = 0; i < 8597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8598(int& val) {
    const int x = 8598;
    std::cout << 8598;
    for (int i = 0; i < 8598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8599(int& val) {
    const int x = 8599;
    std::cout << 8599;
    for (int i = 0; i < 8599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8600(int& val) {
    const int x = 8600;
    std::cout << 8600;
    for (int i = 0; i < 8600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8601(int& val) {
    const int x = 8601;
    std::cout << 8601;
    for (int i = 0; i < 8601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8602(int& val) {
    const int x = 8602;
    std::cout << 8602;
    for (int i = 0; i < 8602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8603(int& val) {
    const int x = 8603;
    std::cout << 8603;
    for (int i = 0; i < 8603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8604(int& val) {
    const int x = 8604;
    std::cout << 8604;
    for (int i = 0; i < 8604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8605(int& val) {
    const int x = 8605;
    std::cout << 8605;
    for (int i = 0; i < 8605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8606(int& val) {
    const int x = 8606;
    std::cout << 8606;
    for (int i = 0; i < 8606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8607(int& val) {
    const int x = 8607;
    std::cout << 8607;
    for (int i = 0; i < 8607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8608(int& val) {
    const int x = 8608;
    std::cout << 8608;
    for (int i = 0; i < 8608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8609(int& val) {
    const int x = 8609;
    std::cout << 8609;
    for (int i = 0; i < 8609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8610(int& val) {
    const int x = 8610;
    std::cout << 8610;
    for (int i = 0; i < 8610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8611(int& val) {
    const int x = 8611;
    std::cout << 8611;
    for (int i = 0; i < 8611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8612(int& val) {
    const int x = 8612;
    std::cout << 8612;
    for (int i = 0; i < 8612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8613(int& val) {
    const int x = 8613;
    std::cout << 8613;
    for (int i = 0; i < 8613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8614(int& val) {
    const int x = 8614;
    std::cout << 8614;
    for (int i = 0; i < 8614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8615(int& val) {
    const int x = 8615;
    std::cout << 8615;
    for (int i = 0; i < 8615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8616(int& val) {
    const int x = 8616;
    std::cout << 8616;
    for (int i = 0; i < 8616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8617(int& val) {
    const int x = 8617;
    std::cout << 8617;
    for (int i = 0; i < 8617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8618(int& val) {
    const int x = 8618;
    std::cout << 8618;
    for (int i = 0; i < 8618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8619(int& val) {
    const int x = 8619;
    std::cout << 8619;
    for (int i = 0; i < 8619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8620(int& val) {
    const int x = 8620;
    std::cout << 8620;
    for (int i = 0; i < 8620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8621(int& val) {
    const int x = 8621;
    std::cout << 8621;
    for (int i = 0; i < 8621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8622(int& val) {
    const int x = 8622;
    std::cout << 8622;
    for (int i = 0; i < 8622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8623(int& val) {
    const int x = 8623;
    std::cout << 8623;
    for (int i = 0; i < 8623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8624(int& val) {
    const int x = 8624;
    std::cout << 8624;
    for (int i = 0; i < 8624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8625(int& val) {
    const int x = 8625;
    std::cout << 8625;
    for (int i = 0; i < 8625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8626(int& val) {
    const int x = 8626;
    std::cout << 8626;
    for (int i = 0; i < 8626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8627(int& val) {
    const int x = 8627;
    std::cout << 8627;
    for (int i = 0; i < 8627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8628(int& val) {
    const int x = 8628;
    std::cout << 8628;
    for (int i = 0; i < 8628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8629(int& val) {
    const int x = 8629;
    std::cout << 8629;
    for (int i = 0; i < 8629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8630(int& val) {
    const int x = 8630;
    std::cout << 8630;
    for (int i = 0; i < 8630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8631(int& val) {
    const int x = 8631;
    std::cout << 8631;
    for (int i = 0; i < 8631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8632(int& val) {
    const int x = 8632;
    std::cout << 8632;
    for (int i = 0; i < 8632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8633(int& val) {
    const int x = 8633;
    std::cout << 8633;
    for (int i = 0; i < 8633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8634(int& val) {
    const int x = 8634;
    std::cout << 8634;
    for (int i = 0; i < 8634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8635(int& val) {
    const int x = 8635;
    std::cout << 8635;
    for (int i = 0; i < 8635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8636(int& val) {
    const int x = 8636;
    std::cout << 8636;
    for (int i = 0; i < 8636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8637(int& val) {
    const int x = 8637;
    std::cout << 8637;
    for (int i = 0; i < 8637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8638(int& val) {
    const int x = 8638;
    std::cout << 8638;
    for (int i = 0; i < 8638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8639(int& val) {
    const int x = 8639;
    std::cout << 8639;
    for (int i = 0; i < 8639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8640(int& val) {
    const int x = 8640;
    std::cout << 8640;
    for (int i = 0; i < 8640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8641(int& val) {
    const int x = 8641;
    std::cout << 8641;
    for (int i = 0; i < 8641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8642(int& val) {
    const int x = 8642;
    std::cout << 8642;
    for (int i = 0; i < 8642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8643(int& val) {
    const int x = 8643;
    std::cout << 8643;
    for (int i = 0; i < 8643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8644(int& val) {
    const int x = 8644;
    std::cout << 8644;
    for (int i = 0; i < 8644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8645(int& val) {
    const int x = 8645;
    std::cout << 8645;
    for (int i = 0; i < 8645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8646(int& val) {
    const int x = 8646;
    std::cout << 8646;
    for (int i = 0; i < 8646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8647(int& val) {
    const int x = 8647;
    std::cout << 8647;
    for (int i = 0; i < 8647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8648(int& val) {
    const int x = 8648;
    std::cout << 8648;
    for (int i = 0; i < 8648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8649(int& val) {
    const int x = 8649;
    std::cout << 8649;
    for (int i = 0; i < 8649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8650(int& val) {
    const int x = 8650;
    std::cout << 8650;
    for (int i = 0; i < 8650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8651(int& val) {
    const int x = 8651;
    std::cout << 8651;
    for (int i = 0; i < 8651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8652(int& val) {
    const int x = 8652;
    std::cout << 8652;
    for (int i = 0; i < 8652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8653(int& val) {
    const int x = 8653;
    std::cout << 8653;
    for (int i = 0; i < 8653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8654(int& val) {
    const int x = 8654;
    std::cout << 8654;
    for (int i = 0; i < 8654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8655(int& val) {
    const int x = 8655;
    std::cout << 8655;
    for (int i = 0; i < 8655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8656(int& val) {
    const int x = 8656;
    std::cout << 8656;
    for (int i = 0; i < 8656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8657(int& val) {
    const int x = 8657;
    std::cout << 8657;
    for (int i = 0; i < 8657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8658(int& val) {
    const int x = 8658;
    std::cout << 8658;
    for (int i = 0; i < 8658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8659(int& val) {
    const int x = 8659;
    std::cout << 8659;
    for (int i = 0; i < 8659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8660(int& val) {
    const int x = 8660;
    std::cout << 8660;
    for (int i = 0; i < 8660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8661(int& val) {
    const int x = 8661;
    std::cout << 8661;
    for (int i = 0; i < 8661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8662(int& val) {
    const int x = 8662;
    std::cout << 8662;
    for (int i = 0; i < 8662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8663(int& val) {
    const int x = 8663;
    std::cout << 8663;
    for (int i = 0; i < 8663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8664(int& val) {
    const int x = 8664;
    std::cout << 8664;
    for (int i = 0; i < 8664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8665(int& val) {
    const int x = 8665;
    std::cout << 8665;
    for (int i = 0; i < 8665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8666(int& val) {
    const int x = 8666;
    std::cout << 8666;
    for (int i = 0; i < 8666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8667(int& val) {
    const int x = 8667;
    std::cout << 8667;
    for (int i = 0; i < 8667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8668(int& val) {
    const int x = 8668;
    std::cout << 8668;
    for (int i = 0; i < 8668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8669(int& val) {
    const int x = 8669;
    std::cout << 8669;
    for (int i = 0; i < 8669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8670(int& val) {
    const int x = 8670;
    std::cout << 8670;
    for (int i = 0; i < 8670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8671(int& val) {
    const int x = 8671;
    std::cout << 8671;
    for (int i = 0; i < 8671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8672(int& val) {
    const int x = 8672;
    std::cout << 8672;
    for (int i = 0; i < 8672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8673(int& val) {
    const int x = 8673;
    std::cout << 8673;
    for (int i = 0; i < 8673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8674(int& val) {
    const int x = 8674;
    std::cout << 8674;
    for (int i = 0; i < 8674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8675(int& val) {
    const int x = 8675;
    std::cout << 8675;
    for (int i = 0; i < 8675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8676(int& val) {
    const int x = 8676;
    std::cout << 8676;
    for (int i = 0; i < 8676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8677(int& val) {
    const int x = 8677;
    std::cout << 8677;
    for (int i = 0; i < 8677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8678(int& val) {
    const int x = 8678;
    std::cout << 8678;
    for (int i = 0; i < 8678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8679(int& val) {
    const int x = 8679;
    std::cout << 8679;
    for (int i = 0; i < 8679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8680(int& val) {
    const int x = 8680;
    std::cout << 8680;
    for (int i = 0; i < 8680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8681(int& val) {
    const int x = 8681;
    std::cout << 8681;
    for (int i = 0; i < 8681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8682(int& val) {
    const int x = 8682;
    std::cout << 8682;
    for (int i = 0; i < 8682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8683(int& val) {
    const int x = 8683;
    std::cout << 8683;
    for (int i = 0; i < 8683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8684(int& val) {
    const int x = 8684;
    std::cout << 8684;
    for (int i = 0; i < 8684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8685(int& val) {
    const int x = 8685;
    std::cout << 8685;
    for (int i = 0; i < 8685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8686(int& val) {
    const int x = 8686;
    std::cout << 8686;
    for (int i = 0; i < 8686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8687(int& val) {
    const int x = 8687;
    std::cout << 8687;
    for (int i = 0; i < 8687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8688(int& val) {
    const int x = 8688;
    std::cout << 8688;
    for (int i = 0; i < 8688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8689(int& val) {
    const int x = 8689;
    std::cout << 8689;
    for (int i = 0; i < 8689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8690(int& val) {
    const int x = 8690;
    std::cout << 8690;
    for (int i = 0; i < 8690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8691(int& val) {
    const int x = 8691;
    std::cout << 8691;
    for (int i = 0; i < 8691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8692(int& val) {
    const int x = 8692;
    std::cout << 8692;
    for (int i = 0; i < 8692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8693(int& val) {
    const int x = 8693;
    std::cout << 8693;
    for (int i = 0; i < 8693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8694(int& val) {
    const int x = 8694;
    std::cout << 8694;
    for (int i = 0; i < 8694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8695(int& val) {
    const int x = 8695;
    std::cout << 8695;
    for (int i = 0; i < 8695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8696(int& val) {
    const int x = 8696;
    std::cout << 8696;
    for (int i = 0; i < 8696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8697(int& val) {
    const int x = 8697;
    std::cout << 8697;
    for (int i = 0; i < 8697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8698(int& val) {
    const int x = 8698;
    std::cout << 8698;
    for (int i = 0; i < 8698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8699(int& val) {
    const int x = 8699;
    std::cout << 8699;
    for (int i = 0; i < 8699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8700(int& val) {
    const int x = 8700;
    std::cout << 8700;
    for (int i = 0; i < 8700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8701(int& val) {
    const int x = 8701;
    std::cout << 8701;
    for (int i = 0; i < 8701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8702(int& val) {
    const int x = 8702;
    std::cout << 8702;
    for (int i = 0; i < 8702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8703(int& val) {
    const int x = 8703;
    std::cout << 8703;
    for (int i = 0; i < 8703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8704(int& val) {
    const int x = 8704;
    std::cout << 8704;
    for (int i = 0; i < 8704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8705(int& val) {
    const int x = 8705;
    std::cout << 8705;
    for (int i = 0; i < 8705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8706(int& val) {
    const int x = 8706;
    std::cout << 8706;
    for (int i = 0; i < 8706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8707(int& val) {
    const int x = 8707;
    std::cout << 8707;
    for (int i = 0; i < 8707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8708(int& val) {
    const int x = 8708;
    std::cout << 8708;
    for (int i = 0; i < 8708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8709(int& val) {
    const int x = 8709;
    std::cout << 8709;
    for (int i = 0; i < 8709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8710(int& val) {
    const int x = 8710;
    std::cout << 8710;
    for (int i = 0; i < 8710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8711(int& val) {
    const int x = 8711;
    std::cout << 8711;
    for (int i = 0; i < 8711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8712(int& val) {
    const int x = 8712;
    std::cout << 8712;
    for (int i = 0; i < 8712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8713(int& val) {
    const int x = 8713;
    std::cout << 8713;
    for (int i = 0; i < 8713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8714(int& val) {
    const int x = 8714;
    std::cout << 8714;
    for (int i = 0; i < 8714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8715(int& val) {
    const int x = 8715;
    std::cout << 8715;
    for (int i = 0; i < 8715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8716(int& val) {
    const int x = 8716;
    std::cout << 8716;
    for (int i = 0; i < 8716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8717(int& val) {
    const int x = 8717;
    std::cout << 8717;
    for (int i = 0; i < 8717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8718(int& val) {
    const int x = 8718;
    std::cout << 8718;
    for (int i = 0; i < 8718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8719(int& val) {
    const int x = 8719;
    std::cout << 8719;
    for (int i = 0; i < 8719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8720(int& val) {
    const int x = 8720;
    std::cout << 8720;
    for (int i = 0; i < 8720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8721(int& val) {
    const int x = 8721;
    std::cout << 8721;
    for (int i = 0; i < 8721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8722(int& val) {
    const int x = 8722;
    std::cout << 8722;
    for (int i = 0; i < 8722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8723(int& val) {
    const int x = 8723;
    std::cout << 8723;
    for (int i = 0; i < 8723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8724(int& val) {
    const int x = 8724;
    std::cout << 8724;
    for (int i = 0; i < 8724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8725(int& val) {
    const int x = 8725;
    std::cout << 8725;
    for (int i = 0; i < 8725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8726(int& val) {
    const int x = 8726;
    std::cout << 8726;
    for (int i = 0; i < 8726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8727(int& val) {
    const int x = 8727;
    std::cout << 8727;
    for (int i = 0; i < 8727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8728(int& val) {
    const int x = 8728;
    std::cout << 8728;
    for (int i = 0; i < 8728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8729(int& val) {
    const int x = 8729;
    std::cout << 8729;
    for (int i = 0; i < 8729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8730(int& val) {
    const int x = 8730;
    std::cout << 8730;
    for (int i = 0; i < 8730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8731(int& val) {
    const int x = 8731;
    std::cout << 8731;
    for (int i = 0; i < 8731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8732(int& val) {
    const int x = 8732;
    std::cout << 8732;
    for (int i = 0; i < 8732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8733(int& val) {
    const int x = 8733;
    std::cout << 8733;
    for (int i = 0; i < 8733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8734(int& val) {
    const int x = 8734;
    std::cout << 8734;
    for (int i = 0; i < 8734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8735(int& val) {
    const int x = 8735;
    std::cout << 8735;
    for (int i = 0; i < 8735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8736(int& val) {
    const int x = 8736;
    std::cout << 8736;
    for (int i = 0; i < 8736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8737(int& val) {
    const int x = 8737;
    std::cout << 8737;
    for (int i = 0; i < 8737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8738(int& val) {
    const int x = 8738;
    std::cout << 8738;
    for (int i = 0; i < 8738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8739(int& val) {
    const int x = 8739;
    std::cout << 8739;
    for (int i = 0; i < 8739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8740(int& val) {
    const int x = 8740;
    std::cout << 8740;
    for (int i = 0; i < 8740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8741(int& val) {
    const int x = 8741;
    std::cout << 8741;
    for (int i = 0; i < 8741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8742(int& val) {
    const int x = 8742;
    std::cout << 8742;
    for (int i = 0; i < 8742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8743(int& val) {
    const int x = 8743;
    std::cout << 8743;
    for (int i = 0; i < 8743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8744(int& val) {
    const int x = 8744;
    std::cout << 8744;
    for (int i = 0; i < 8744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8745(int& val) {
    const int x = 8745;
    std::cout << 8745;
    for (int i = 0; i < 8745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8746(int& val) {
    const int x = 8746;
    std::cout << 8746;
    for (int i = 0; i < 8746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8747(int& val) {
    const int x = 8747;
    std::cout << 8747;
    for (int i = 0; i < 8747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8748(int& val) {
    const int x = 8748;
    std::cout << 8748;
    for (int i = 0; i < 8748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8749(int& val) {
    const int x = 8749;
    std::cout << 8749;
    for (int i = 0; i < 8749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8750(int& val) {
    const int x = 8750;
    std::cout << 8750;
    for (int i = 0; i < 8750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8751(int& val) {
    const int x = 8751;
    std::cout << 8751;
    for (int i = 0; i < 8751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8752(int& val) {
    const int x = 8752;
    std::cout << 8752;
    for (int i = 0; i < 8752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8753(int& val) {
    const int x = 8753;
    std::cout << 8753;
    for (int i = 0; i < 8753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8754(int& val) {
    const int x = 8754;
    std::cout << 8754;
    for (int i = 0; i < 8754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8755(int& val) {
    const int x = 8755;
    std::cout << 8755;
    for (int i = 0; i < 8755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8756(int& val) {
    const int x = 8756;
    std::cout << 8756;
    for (int i = 0; i < 8756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8757(int& val) {
    const int x = 8757;
    std::cout << 8757;
    for (int i = 0; i < 8757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8758(int& val) {
    const int x = 8758;
    std::cout << 8758;
    for (int i = 0; i < 8758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8759(int& val) {
    const int x = 8759;
    std::cout << 8759;
    for (int i = 0; i < 8759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8760(int& val) {
    const int x = 8760;
    std::cout << 8760;
    for (int i = 0; i < 8760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8761(int& val) {
    const int x = 8761;
    std::cout << 8761;
    for (int i = 0; i < 8761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8762(int& val) {
    const int x = 8762;
    std::cout << 8762;
    for (int i = 0; i < 8762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8763(int& val) {
    const int x = 8763;
    std::cout << 8763;
    for (int i = 0; i < 8763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8764(int& val) {
    const int x = 8764;
    std::cout << 8764;
    for (int i = 0; i < 8764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8765(int& val) {
    const int x = 8765;
    std::cout << 8765;
    for (int i = 0; i < 8765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8766(int& val) {
    const int x = 8766;
    std::cout << 8766;
    for (int i = 0; i < 8766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8767(int& val) {
    const int x = 8767;
    std::cout << 8767;
    for (int i = 0; i < 8767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8768(int& val) {
    const int x = 8768;
    std::cout << 8768;
    for (int i = 0; i < 8768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8769(int& val) {
    const int x = 8769;
    std::cout << 8769;
    for (int i = 0; i < 8769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8770(int& val) {
    const int x = 8770;
    std::cout << 8770;
    for (int i = 0; i < 8770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8771(int& val) {
    const int x = 8771;
    std::cout << 8771;
    for (int i = 0; i < 8771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8772(int& val) {
    const int x = 8772;
    std::cout << 8772;
    for (int i = 0; i < 8772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8773(int& val) {
    const int x = 8773;
    std::cout << 8773;
    for (int i = 0; i < 8773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8774(int& val) {
    const int x = 8774;
    std::cout << 8774;
    for (int i = 0; i < 8774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8775(int& val) {
    const int x = 8775;
    std::cout << 8775;
    for (int i = 0; i < 8775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8776(int& val) {
    const int x = 8776;
    std::cout << 8776;
    for (int i = 0; i < 8776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8777(int& val) {
    const int x = 8777;
    std::cout << 8777;
    for (int i = 0; i < 8777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8778(int& val) {
    const int x = 8778;
    std::cout << 8778;
    for (int i = 0; i < 8778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8779(int& val) {
    const int x = 8779;
    std::cout << 8779;
    for (int i = 0; i < 8779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8780(int& val) {
    const int x = 8780;
    std::cout << 8780;
    for (int i = 0; i < 8780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8781(int& val) {
    const int x = 8781;
    std::cout << 8781;
    for (int i = 0; i < 8781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8782(int& val) {
    const int x = 8782;
    std::cout << 8782;
    for (int i = 0; i < 8782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8783(int& val) {
    const int x = 8783;
    std::cout << 8783;
    for (int i = 0; i < 8783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8784(int& val) {
    const int x = 8784;
    std::cout << 8784;
    for (int i = 0; i < 8784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8785(int& val) {
    const int x = 8785;
    std::cout << 8785;
    for (int i = 0; i < 8785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8786(int& val) {
    const int x = 8786;
    std::cout << 8786;
    for (int i = 0; i < 8786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8787(int& val) {
    const int x = 8787;
    std::cout << 8787;
    for (int i = 0; i < 8787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8788(int& val) {
    const int x = 8788;
    std::cout << 8788;
    for (int i = 0; i < 8788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8789(int& val) {
    const int x = 8789;
    std::cout << 8789;
    for (int i = 0; i < 8789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8790(int& val) {
    const int x = 8790;
    std::cout << 8790;
    for (int i = 0; i < 8790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8791(int& val) {
    const int x = 8791;
    std::cout << 8791;
    for (int i = 0; i < 8791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8792(int& val) {
    const int x = 8792;
    std::cout << 8792;
    for (int i = 0; i < 8792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8793(int& val) {
    const int x = 8793;
    std::cout << 8793;
    for (int i = 0; i < 8793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8794(int& val) {
    const int x = 8794;
    std::cout << 8794;
    for (int i = 0; i < 8794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8795(int& val) {
    const int x = 8795;
    std::cout << 8795;
    for (int i = 0; i < 8795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8796(int& val) {
    const int x = 8796;
    std::cout << 8796;
    for (int i = 0; i < 8796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8797(int& val) {
    const int x = 8797;
    std::cout << 8797;
    for (int i = 0; i < 8797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8798(int& val) {
    const int x = 8798;
    std::cout << 8798;
    for (int i = 0; i < 8798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8799(int& val) {
    const int x = 8799;
    std::cout << 8799;
    for (int i = 0; i < 8799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8800(int& val) {
    const int x = 8800;
    std::cout << 8800;
    for (int i = 0; i < 8800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8801(int& val) {
    const int x = 8801;
    std::cout << 8801;
    for (int i = 0; i < 8801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8802(int& val) {
    const int x = 8802;
    std::cout << 8802;
    for (int i = 0; i < 8802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8803(int& val) {
    const int x = 8803;
    std::cout << 8803;
    for (int i = 0; i < 8803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8804(int& val) {
    const int x = 8804;
    std::cout << 8804;
    for (int i = 0; i < 8804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8805(int& val) {
    const int x = 8805;
    std::cout << 8805;
    for (int i = 0; i < 8805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8806(int& val) {
    const int x = 8806;
    std::cout << 8806;
    for (int i = 0; i < 8806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8807(int& val) {
    const int x = 8807;
    std::cout << 8807;
    for (int i = 0; i < 8807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8808(int& val) {
    const int x = 8808;
    std::cout << 8808;
    for (int i = 0; i < 8808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8809(int& val) {
    const int x = 8809;
    std::cout << 8809;
    for (int i = 0; i < 8809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8810(int& val) {
    const int x = 8810;
    std::cout << 8810;
    for (int i = 0; i < 8810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8811(int& val) {
    const int x = 8811;
    std::cout << 8811;
    for (int i = 0; i < 8811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8812(int& val) {
    const int x = 8812;
    std::cout << 8812;
    for (int i = 0; i < 8812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8813(int& val) {
    const int x = 8813;
    std::cout << 8813;
    for (int i = 0; i < 8813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8814(int& val) {
    const int x = 8814;
    std::cout << 8814;
    for (int i = 0; i < 8814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8815(int& val) {
    const int x = 8815;
    std::cout << 8815;
    for (int i = 0; i < 8815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8816(int& val) {
    const int x = 8816;
    std::cout << 8816;
    for (int i = 0; i < 8816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8817(int& val) {
    const int x = 8817;
    std::cout << 8817;
    for (int i = 0; i < 8817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8818(int& val) {
    const int x = 8818;
    std::cout << 8818;
    for (int i = 0; i < 8818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8819(int& val) {
    const int x = 8819;
    std::cout << 8819;
    for (int i = 0; i < 8819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8820(int& val) {
    const int x = 8820;
    std::cout << 8820;
    for (int i = 0; i < 8820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8821(int& val) {
    const int x = 8821;
    std::cout << 8821;
    for (int i = 0; i < 8821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8822(int& val) {
    const int x = 8822;
    std::cout << 8822;
    for (int i = 0; i < 8822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8823(int& val) {
    const int x = 8823;
    std::cout << 8823;
    for (int i = 0; i < 8823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8824(int& val) {
    const int x = 8824;
    std::cout << 8824;
    for (int i = 0; i < 8824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8825(int& val) {
    const int x = 8825;
    std::cout << 8825;
    for (int i = 0; i < 8825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8826(int& val) {
    const int x = 8826;
    std::cout << 8826;
    for (int i = 0; i < 8826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8827(int& val) {
    const int x = 8827;
    std::cout << 8827;
    for (int i = 0; i < 8827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8828(int& val) {
    const int x = 8828;
    std::cout << 8828;
    for (int i = 0; i < 8828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8829(int& val) {
    const int x = 8829;
    std::cout << 8829;
    for (int i = 0; i < 8829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8830(int& val) {
    const int x = 8830;
    std::cout << 8830;
    for (int i = 0; i < 8830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8831(int& val) {
    const int x = 8831;
    std::cout << 8831;
    for (int i = 0; i < 8831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8832(int& val) {
    const int x = 8832;
    std::cout << 8832;
    for (int i = 0; i < 8832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8833(int& val) {
    const int x = 8833;
    std::cout << 8833;
    for (int i = 0; i < 8833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8834(int& val) {
    const int x = 8834;
    std::cout << 8834;
    for (int i = 0; i < 8834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8835(int& val) {
    const int x = 8835;
    std::cout << 8835;
    for (int i = 0; i < 8835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8836(int& val) {
    const int x = 8836;
    std::cout << 8836;
    for (int i = 0; i < 8836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8837(int& val) {
    const int x = 8837;
    std::cout << 8837;
    for (int i = 0; i < 8837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8838(int& val) {
    const int x = 8838;
    std::cout << 8838;
    for (int i = 0; i < 8838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8839(int& val) {
    const int x = 8839;
    std::cout << 8839;
    for (int i = 0; i < 8839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8840(int& val) {
    const int x = 8840;
    std::cout << 8840;
    for (int i = 0; i < 8840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8841(int& val) {
    const int x = 8841;
    std::cout << 8841;
    for (int i = 0; i < 8841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8842(int& val) {
    const int x = 8842;
    std::cout << 8842;
    for (int i = 0; i < 8842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8843(int& val) {
    const int x = 8843;
    std::cout << 8843;
    for (int i = 0; i < 8843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8844(int& val) {
    const int x = 8844;
    std::cout << 8844;
    for (int i = 0; i < 8844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8845(int& val) {
    const int x = 8845;
    std::cout << 8845;
    for (int i = 0; i < 8845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8846(int& val) {
    const int x = 8846;
    std::cout << 8846;
    for (int i = 0; i < 8846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8847(int& val) {
    const int x = 8847;
    std::cout << 8847;
    for (int i = 0; i < 8847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8848(int& val) {
    const int x = 8848;
    std::cout << 8848;
    for (int i = 0; i < 8848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8849(int& val) {
    const int x = 8849;
    std::cout << 8849;
    for (int i = 0; i < 8849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8850(int& val) {
    const int x = 8850;
    std::cout << 8850;
    for (int i = 0; i < 8850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8851(int& val) {
    const int x = 8851;
    std::cout << 8851;
    for (int i = 0; i < 8851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8852(int& val) {
    const int x = 8852;
    std::cout << 8852;
    for (int i = 0; i < 8852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8853(int& val) {
    const int x = 8853;
    std::cout << 8853;
    for (int i = 0; i < 8853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8854(int& val) {
    const int x = 8854;
    std::cout << 8854;
    for (int i = 0; i < 8854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8855(int& val) {
    const int x = 8855;
    std::cout << 8855;
    for (int i = 0; i < 8855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8856(int& val) {
    const int x = 8856;
    std::cout << 8856;
    for (int i = 0; i < 8856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8857(int& val) {
    const int x = 8857;
    std::cout << 8857;
    for (int i = 0; i < 8857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8858(int& val) {
    const int x = 8858;
    std::cout << 8858;
    for (int i = 0; i < 8858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8859(int& val) {
    const int x = 8859;
    std::cout << 8859;
    for (int i = 0; i < 8859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8860(int& val) {
    const int x = 8860;
    std::cout << 8860;
    for (int i = 0; i < 8860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8861(int& val) {
    const int x = 8861;
    std::cout << 8861;
    for (int i = 0; i < 8861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8862(int& val) {
    const int x = 8862;
    std::cout << 8862;
    for (int i = 0; i < 8862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8863(int& val) {
    const int x = 8863;
    std::cout << 8863;
    for (int i = 0; i < 8863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8864(int& val) {
    const int x = 8864;
    std::cout << 8864;
    for (int i = 0; i < 8864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8865(int& val) {
    const int x = 8865;
    std::cout << 8865;
    for (int i = 0; i < 8865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8866(int& val) {
    const int x = 8866;
    std::cout << 8866;
    for (int i = 0; i < 8866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8867(int& val) {
    const int x = 8867;
    std::cout << 8867;
    for (int i = 0; i < 8867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8868(int& val) {
    const int x = 8868;
    std::cout << 8868;
    for (int i = 0; i < 8868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8869(int& val) {
    const int x = 8869;
    std::cout << 8869;
    for (int i = 0; i < 8869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8870(int& val) {
    const int x = 8870;
    std::cout << 8870;
    for (int i = 0; i < 8870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8871(int& val) {
    const int x = 8871;
    std::cout << 8871;
    for (int i = 0; i < 8871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8872(int& val) {
    const int x = 8872;
    std::cout << 8872;
    for (int i = 0; i < 8872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8873(int& val) {
    const int x = 8873;
    std::cout << 8873;
    for (int i = 0; i < 8873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8874(int& val) {
    const int x = 8874;
    std::cout << 8874;
    for (int i = 0; i < 8874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8875(int& val) {
    const int x = 8875;
    std::cout << 8875;
    for (int i = 0; i < 8875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8876(int& val) {
    const int x = 8876;
    std::cout << 8876;
    for (int i = 0; i < 8876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8877(int& val) {
    const int x = 8877;
    std::cout << 8877;
    for (int i = 0; i < 8877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8878(int& val) {
    const int x = 8878;
    std::cout << 8878;
    for (int i = 0; i < 8878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8879(int& val) {
    const int x = 8879;
    std::cout << 8879;
    for (int i = 0; i < 8879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8880(int& val) {
    const int x = 8880;
    std::cout << 8880;
    for (int i = 0; i < 8880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8881(int& val) {
    const int x = 8881;
    std::cout << 8881;
    for (int i = 0; i < 8881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8882(int& val) {
    const int x = 8882;
    std::cout << 8882;
    for (int i = 0; i < 8882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8883(int& val) {
    const int x = 8883;
    std::cout << 8883;
    for (int i = 0; i < 8883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8884(int& val) {
    const int x = 8884;
    std::cout << 8884;
    for (int i = 0; i < 8884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8885(int& val) {
    const int x = 8885;
    std::cout << 8885;
    for (int i = 0; i < 8885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8886(int& val) {
    const int x = 8886;
    std::cout << 8886;
    for (int i = 0; i < 8886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8887(int& val) {
    const int x = 8887;
    std::cout << 8887;
    for (int i = 0; i < 8887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8888(int& val) {
    const int x = 8888;
    std::cout << 8888;
    for (int i = 0; i < 8888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8889(int& val) {
    const int x = 8889;
    std::cout << 8889;
    for (int i = 0; i < 8889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8890(int& val) {
    const int x = 8890;
    std::cout << 8890;
    for (int i = 0; i < 8890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8891(int& val) {
    const int x = 8891;
    std::cout << 8891;
    for (int i = 0; i < 8891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8892(int& val) {
    const int x = 8892;
    std::cout << 8892;
    for (int i = 0; i < 8892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8893(int& val) {
    const int x = 8893;
    std::cout << 8893;
    for (int i = 0; i < 8893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8894(int& val) {
    const int x = 8894;
    std::cout << 8894;
    for (int i = 0; i < 8894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8895(int& val) {
    const int x = 8895;
    std::cout << 8895;
    for (int i = 0; i < 8895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8896(int& val) {
    const int x = 8896;
    std::cout << 8896;
    for (int i = 0; i < 8896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8897(int& val) {
    const int x = 8897;
    std::cout << 8897;
    for (int i = 0; i < 8897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8898(int& val) {
    const int x = 8898;
    std::cout << 8898;
    for (int i = 0; i < 8898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8899(int& val) {
    const int x = 8899;
    std::cout << 8899;
    for (int i = 0; i < 8899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8900(int& val) {
    const int x = 8900;
    std::cout << 8900;
    for (int i = 0; i < 8900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8901(int& val) {
    const int x = 8901;
    std::cout << 8901;
    for (int i = 0; i < 8901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8902(int& val) {
    const int x = 8902;
    std::cout << 8902;
    for (int i = 0; i < 8902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8903(int& val) {
    const int x = 8903;
    std::cout << 8903;
    for (int i = 0; i < 8903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8904(int& val) {
    const int x = 8904;
    std::cout << 8904;
    for (int i = 0; i < 8904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8905(int& val) {
    const int x = 8905;
    std::cout << 8905;
    for (int i = 0; i < 8905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8906(int& val) {
    const int x = 8906;
    std::cout << 8906;
    for (int i = 0; i < 8906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8907(int& val) {
    const int x = 8907;
    std::cout << 8907;
    for (int i = 0; i < 8907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8908(int& val) {
    const int x = 8908;
    std::cout << 8908;
    for (int i = 0; i < 8908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8909(int& val) {
    const int x = 8909;
    std::cout << 8909;
    for (int i = 0; i < 8909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8910(int& val) {
    const int x = 8910;
    std::cout << 8910;
    for (int i = 0; i < 8910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8911(int& val) {
    const int x = 8911;
    std::cout << 8911;
    for (int i = 0; i < 8911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8912(int& val) {
    const int x = 8912;
    std::cout << 8912;
    for (int i = 0; i < 8912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8913(int& val) {
    const int x = 8913;
    std::cout << 8913;
    for (int i = 0; i < 8913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8914(int& val) {
    const int x = 8914;
    std::cout << 8914;
    for (int i = 0; i < 8914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8915(int& val) {
    const int x = 8915;
    std::cout << 8915;
    for (int i = 0; i < 8915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8916(int& val) {
    const int x = 8916;
    std::cout << 8916;
    for (int i = 0; i < 8916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8917(int& val) {
    const int x = 8917;
    std::cout << 8917;
    for (int i = 0; i < 8917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8918(int& val) {
    const int x = 8918;
    std::cout << 8918;
    for (int i = 0; i < 8918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8919(int& val) {
    const int x = 8919;
    std::cout << 8919;
    for (int i = 0; i < 8919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8920(int& val) {
    const int x = 8920;
    std::cout << 8920;
    for (int i = 0; i < 8920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8921(int& val) {
    const int x = 8921;
    std::cout << 8921;
    for (int i = 0; i < 8921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8922(int& val) {
    const int x = 8922;
    std::cout << 8922;
    for (int i = 0; i < 8922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8923(int& val) {
    const int x = 8923;
    std::cout << 8923;
    for (int i = 0; i < 8923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8924(int& val) {
    const int x = 8924;
    std::cout << 8924;
    for (int i = 0; i < 8924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8925(int& val) {
    const int x = 8925;
    std::cout << 8925;
    for (int i = 0; i < 8925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8926(int& val) {
    const int x = 8926;
    std::cout << 8926;
    for (int i = 0; i < 8926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8927(int& val) {
    const int x = 8927;
    std::cout << 8927;
    for (int i = 0; i < 8927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8928(int& val) {
    const int x = 8928;
    std::cout << 8928;
    for (int i = 0; i < 8928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8929(int& val) {
    const int x = 8929;
    std::cout << 8929;
    for (int i = 0; i < 8929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8930(int& val) {
    const int x = 8930;
    std::cout << 8930;
    for (int i = 0; i < 8930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8931(int& val) {
    const int x = 8931;
    std::cout << 8931;
    for (int i = 0; i < 8931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8932(int& val) {
    const int x = 8932;
    std::cout << 8932;
    for (int i = 0; i < 8932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8933(int& val) {
    const int x = 8933;
    std::cout << 8933;
    for (int i = 0; i < 8933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8934(int& val) {
    const int x = 8934;
    std::cout << 8934;
    for (int i = 0; i < 8934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8935(int& val) {
    const int x = 8935;
    std::cout << 8935;
    for (int i = 0; i < 8935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8936(int& val) {
    const int x = 8936;
    std::cout << 8936;
    for (int i = 0; i < 8936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8937(int& val) {
    const int x = 8937;
    std::cout << 8937;
    for (int i = 0; i < 8937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8938(int& val) {
    const int x = 8938;
    std::cout << 8938;
    for (int i = 0; i < 8938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8939(int& val) {
    const int x = 8939;
    std::cout << 8939;
    for (int i = 0; i < 8939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8940(int& val) {
    const int x = 8940;
    std::cout << 8940;
    for (int i = 0; i < 8940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8941(int& val) {
    const int x = 8941;
    std::cout << 8941;
    for (int i = 0; i < 8941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8942(int& val) {
    const int x = 8942;
    std::cout << 8942;
    for (int i = 0; i < 8942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8943(int& val) {
    const int x = 8943;
    std::cout << 8943;
    for (int i = 0; i < 8943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8944(int& val) {
    const int x = 8944;
    std::cout << 8944;
    for (int i = 0; i < 8944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8945(int& val) {
    const int x = 8945;
    std::cout << 8945;
    for (int i = 0; i < 8945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8946(int& val) {
    const int x = 8946;
    std::cout << 8946;
    for (int i = 0; i < 8946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8947(int& val) {
    const int x = 8947;
    std::cout << 8947;
    for (int i = 0; i < 8947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8948(int& val) {
    const int x = 8948;
    std::cout << 8948;
    for (int i = 0; i < 8948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8949(int& val) {
    const int x = 8949;
    std::cout << 8949;
    for (int i = 0; i < 8949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8950(int& val) {
    const int x = 8950;
    std::cout << 8950;
    for (int i = 0; i < 8950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8951(int& val) {
    const int x = 8951;
    std::cout << 8951;
    for (int i = 0; i < 8951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8952(int& val) {
    const int x = 8952;
    std::cout << 8952;
    for (int i = 0; i < 8952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8953(int& val) {
    const int x = 8953;
    std::cout << 8953;
    for (int i = 0; i < 8953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8954(int& val) {
    const int x = 8954;
    std::cout << 8954;
    for (int i = 0; i < 8954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8955(int& val) {
    const int x = 8955;
    std::cout << 8955;
    for (int i = 0; i < 8955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8956(int& val) {
    const int x = 8956;
    std::cout << 8956;
    for (int i = 0; i < 8956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8957(int& val) {
    const int x = 8957;
    std::cout << 8957;
    for (int i = 0; i < 8957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8958(int& val) {
    const int x = 8958;
    std::cout << 8958;
    for (int i = 0; i < 8958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8959(int& val) {
    const int x = 8959;
    std::cout << 8959;
    for (int i = 0; i < 8959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8960(int& val) {
    const int x = 8960;
    std::cout << 8960;
    for (int i = 0; i < 8960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8961(int& val) {
    const int x = 8961;
    std::cout << 8961;
    for (int i = 0; i < 8961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8962(int& val) {
    const int x = 8962;
    std::cout << 8962;
    for (int i = 0; i < 8962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8963(int& val) {
    const int x = 8963;
    std::cout << 8963;
    for (int i = 0; i < 8963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8964(int& val) {
    const int x = 8964;
    std::cout << 8964;
    for (int i = 0; i < 8964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8965(int& val) {
    const int x = 8965;
    std::cout << 8965;
    for (int i = 0; i < 8965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8966(int& val) {
    const int x = 8966;
    std::cout << 8966;
    for (int i = 0; i < 8966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8967(int& val) {
    const int x = 8967;
    std::cout << 8967;
    for (int i = 0; i < 8967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8968(int& val) {
    const int x = 8968;
    std::cout << 8968;
    for (int i = 0; i < 8968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8969(int& val) {
    const int x = 8969;
    std::cout << 8969;
    for (int i = 0; i < 8969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8970(int& val) {
    const int x = 8970;
    std::cout << 8970;
    for (int i = 0; i < 8970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8971(int& val) {
    const int x = 8971;
    std::cout << 8971;
    for (int i = 0; i < 8971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8972(int& val) {
    const int x = 8972;
    std::cout << 8972;
    for (int i = 0; i < 8972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8973(int& val) {
    const int x = 8973;
    std::cout << 8973;
    for (int i = 0; i < 8973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8974(int& val) {
    const int x = 8974;
    std::cout << 8974;
    for (int i = 0; i < 8974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8975(int& val) {
    const int x = 8975;
    std::cout << 8975;
    for (int i = 0; i < 8975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8976(int& val) {
    const int x = 8976;
    std::cout << 8976;
    for (int i = 0; i < 8976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8977(int& val) {
    const int x = 8977;
    std::cout << 8977;
    for (int i = 0; i < 8977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8978(int& val) {
    const int x = 8978;
    std::cout << 8978;
    for (int i = 0; i < 8978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8979(int& val) {
    const int x = 8979;
    std::cout << 8979;
    for (int i = 0; i < 8979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8980(int& val) {
    const int x = 8980;
    std::cout << 8980;
    for (int i = 0; i < 8980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8981(int& val) {
    const int x = 8981;
    std::cout << 8981;
    for (int i = 0; i < 8981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8982(int& val) {
    const int x = 8982;
    std::cout << 8982;
    for (int i = 0; i < 8982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8983(int& val) {
    const int x = 8983;
    std::cout << 8983;
    for (int i = 0; i < 8983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8984(int& val) {
    const int x = 8984;
    std::cout << 8984;
    for (int i = 0; i < 8984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8985(int& val) {
    const int x = 8985;
    std::cout << 8985;
    for (int i = 0; i < 8985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8986(int& val) {
    const int x = 8986;
    std::cout << 8986;
    for (int i = 0; i < 8986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8987(int& val) {
    const int x = 8987;
    std::cout << 8987;
    for (int i = 0; i < 8987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8988(int& val) {
    const int x = 8988;
    std::cout << 8988;
    for (int i = 0; i < 8988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8989(int& val) {
    const int x = 8989;
    std::cout << 8989;
    for (int i = 0; i < 8989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8990(int& val) {
    const int x = 8990;
    std::cout << 8990;
    for (int i = 0; i < 8990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8991(int& val) {
    const int x = 8991;
    std::cout << 8991;
    for (int i = 0; i < 8991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8992(int& val) {
    const int x = 8992;
    std::cout << 8992;
    for (int i = 0; i < 8992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8993(int& val) {
    const int x = 8993;
    std::cout << 8993;
    for (int i = 0; i < 8993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8994(int& val) {
    const int x = 8994;
    std::cout << 8994;
    for (int i = 0; i < 8994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8995(int& val) {
    const int x = 8995;
    std::cout << 8995;
    for (int i = 0; i < 8995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8996(int& val) {
    const int x = 8996;
    std::cout << 8996;
    for (int i = 0; i < 8996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8997(int& val) {
    const int x = 8997;
    std::cout << 8997;
    for (int i = 0; i < 8997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8998(int& val) {
    const int x = 8998;
    std::cout << 8998;
    for (int i = 0; i < 8998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8999(int& val) {
    const int x = 8999;
    std::cout << 8999;
    for (int i = 0; i < 8999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9000(int& val) {
    const int x = 9000;
    std::cout << 9000;
    for (int i = 0; i < 9000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9001(int& val) {
    const int x = 9001;
    std::cout << 9001;
    for (int i = 0; i < 9001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9002(int& val) {
    const int x = 9002;
    std::cout << 9002;
    for (int i = 0; i < 9002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9003(int& val) {
    const int x = 9003;
    std::cout << 9003;
    for (int i = 0; i < 9003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9004(int& val) {
    const int x = 9004;
    std::cout << 9004;
    for (int i = 0; i < 9004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9005(int& val) {
    const int x = 9005;
    std::cout << 9005;
    for (int i = 0; i < 9005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9006(int& val) {
    const int x = 9006;
    std::cout << 9006;
    for (int i = 0; i < 9006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9007(int& val) {
    const int x = 9007;
    std::cout << 9007;
    for (int i = 0; i < 9007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9008(int& val) {
    const int x = 9008;
    std::cout << 9008;
    for (int i = 0; i < 9008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9009(int& val) {
    const int x = 9009;
    std::cout << 9009;
    for (int i = 0; i < 9009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9010(int& val) {
    const int x = 9010;
    std::cout << 9010;
    for (int i = 0; i < 9010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9011(int& val) {
    const int x = 9011;
    std::cout << 9011;
    for (int i = 0; i < 9011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9012(int& val) {
    const int x = 9012;
    std::cout << 9012;
    for (int i = 0; i < 9012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9013(int& val) {
    const int x = 9013;
    std::cout << 9013;
    for (int i = 0; i < 9013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9014(int& val) {
    const int x = 9014;
    std::cout << 9014;
    for (int i = 0; i < 9014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9015(int& val) {
    const int x = 9015;
    std::cout << 9015;
    for (int i = 0; i < 9015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9016(int& val) {
    const int x = 9016;
    std::cout << 9016;
    for (int i = 0; i < 9016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9017(int& val) {
    const int x = 9017;
    std::cout << 9017;
    for (int i = 0; i < 9017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9018(int& val) {
    const int x = 9018;
    std::cout << 9018;
    for (int i = 0; i < 9018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9019(int& val) {
    const int x = 9019;
    std::cout << 9019;
    for (int i = 0; i < 9019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9020(int& val) {
    const int x = 9020;
    std::cout << 9020;
    for (int i = 0; i < 9020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9021(int& val) {
    const int x = 9021;
    std::cout << 9021;
    for (int i = 0; i < 9021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9022(int& val) {
    const int x = 9022;
    std::cout << 9022;
    for (int i = 0; i < 9022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9023(int& val) {
    const int x = 9023;
    std::cout << 9023;
    for (int i = 0; i < 9023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9024(int& val) {
    const int x = 9024;
    std::cout << 9024;
    for (int i = 0; i < 9024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9025(int& val) {
    const int x = 9025;
    std::cout << 9025;
    for (int i = 0; i < 9025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9026(int& val) {
    const int x = 9026;
    std::cout << 9026;
    for (int i = 0; i < 9026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9027(int& val) {
    const int x = 9027;
    std::cout << 9027;
    for (int i = 0; i < 9027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9028(int& val) {
    const int x = 9028;
    std::cout << 9028;
    for (int i = 0; i < 9028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9029(int& val) {
    const int x = 9029;
    std::cout << 9029;
    for (int i = 0; i < 9029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9030(int& val) {
    const int x = 9030;
    std::cout << 9030;
    for (int i = 0; i < 9030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9031(int& val) {
    const int x = 9031;
    std::cout << 9031;
    for (int i = 0; i < 9031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9032(int& val) {
    const int x = 9032;
    std::cout << 9032;
    for (int i = 0; i < 9032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9033(int& val) {
    const int x = 9033;
    std::cout << 9033;
    for (int i = 0; i < 9033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9034(int& val) {
    const int x = 9034;
    std::cout << 9034;
    for (int i = 0; i < 9034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9035(int& val) {
    const int x = 9035;
    std::cout << 9035;
    for (int i = 0; i < 9035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9036(int& val) {
    const int x = 9036;
    std::cout << 9036;
    for (int i = 0; i < 9036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9037(int& val) {
    const int x = 9037;
    std::cout << 9037;
    for (int i = 0; i < 9037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9038(int& val) {
    const int x = 9038;
    std::cout << 9038;
    for (int i = 0; i < 9038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9039(int& val) {
    const int x = 9039;
    std::cout << 9039;
    for (int i = 0; i < 9039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9040(int& val) {
    const int x = 9040;
    std::cout << 9040;
    for (int i = 0; i < 9040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9041(int& val) {
    const int x = 9041;
    std::cout << 9041;
    for (int i = 0; i < 9041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9042(int& val) {
    const int x = 9042;
    std::cout << 9042;
    for (int i = 0; i < 9042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9043(int& val) {
    const int x = 9043;
    std::cout << 9043;
    for (int i = 0; i < 9043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9044(int& val) {
    const int x = 9044;
    std::cout << 9044;
    for (int i = 0; i < 9044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9045(int& val) {
    const int x = 9045;
    std::cout << 9045;
    for (int i = 0; i < 9045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9046(int& val) {
    const int x = 9046;
    std::cout << 9046;
    for (int i = 0; i < 9046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9047(int& val) {
    const int x = 9047;
    std::cout << 9047;
    for (int i = 0; i < 9047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9048(int& val) {
    const int x = 9048;
    std::cout << 9048;
    for (int i = 0; i < 9048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9049(int& val) {
    const int x = 9049;
    std::cout << 9049;
    for (int i = 0; i < 9049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9050(int& val) {
    const int x = 9050;
    std::cout << 9050;
    for (int i = 0; i < 9050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9051(int& val) {
    const int x = 9051;
    std::cout << 9051;
    for (int i = 0; i < 9051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9052(int& val) {
    const int x = 9052;
    std::cout << 9052;
    for (int i = 0; i < 9052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9053(int& val) {
    const int x = 9053;
    std::cout << 9053;
    for (int i = 0; i < 9053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9054(int& val) {
    const int x = 9054;
    std::cout << 9054;
    for (int i = 0; i < 9054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9055(int& val) {
    const int x = 9055;
    std::cout << 9055;
    for (int i = 0; i < 9055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9056(int& val) {
    const int x = 9056;
    std::cout << 9056;
    for (int i = 0; i < 9056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9057(int& val) {
    const int x = 9057;
    std::cout << 9057;
    for (int i = 0; i < 9057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9058(int& val) {
    const int x = 9058;
    std::cout << 9058;
    for (int i = 0; i < 9058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9059(int& val) {
    const int x = 9059;
    std::cout << 9059;
    for (int i = 0; i < 9059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9060(int& val) {
    const int x = 9060;
    std::cout << 9060;
    for (int i = 0; i < 9060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9061(int& val) {
    const int x = 9061;
    std::cout << 9061;
    for (int i = 0; i < 9061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9062(int& val) {
    const int x = 9062;
    std::cout << 9062;
    for (int i = 0; i < 9062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9063(int& val) {
    const int x = 9063;
    std::cout << 9063;
    for (int i = 0; i < 9063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9064(int& val) {
    const int x = 9064;
    std::cout << 9064;
    for (int i = 0; i < 9064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9065(int& val) {
    const int x = 9065;
    std::cout << 9065;
    for (int i = 0; i < 9065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9066(int& val) {
    const int x = 9066;
    std::cout << 9066;
    for (int i = 0; i < 9066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9067(int& val) {
    const int x = 9067;
    std::cout << 9067;
    for (int i = 0; i < 9067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9068(int& val) {
    const int x = 9068;
    std::cout << 9068;
    for (int i = 0; i < 9068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9069(int& val) {
    const int x = 9069;
    std::cout << 9069;
    for (int i = 0; i < 9069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9070(int& val) {
    const int x = 9070;
    std::cout << 9070;
    for (int i = 0; i < 9070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9071(int& val) {
    const int x = 9071;
    std::cout << 9071;
    for (int i = 0; i < 9071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9072(int& val) {
    const int x = 9072;
    std::cout << 9072;
    for (int i = 0; i < 9072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9073(int& val) {
    const int x = 9073;
    std::cout << 9073;
    for (int i = 0; i < 9073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9074(int& val) {
    const int x = 9074;
    std::cout << 9074;
    for (int i = 0; i < 9074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9075(int& val) {
    const int x = 9075;
    std::cout << 9075;
    for (int i = 0; i < 9075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9076(int& val) {
    const int x = 9076;
    std::cout << 9076;
    for (int i = 0; i < 9076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9077(int& val) {
    const int x = 9077;
    std::cout << 9077;
    for (int i = 0; i < 9077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9078(int& val) {
    const int x = 9078;
    std::cout << 9078;
    for (int i = 0; i < 9078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9079(int& val) {
    const int x = 9079;
    std::cout << 9079;
    for (int i = 0; i < 9079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9080(int& val) {
    const int x = 9080;
    std::cout << 9080;
    for (int i = 0; i < 9080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9081(int& val) {
    const int x = 9081;
    std::cout << 9081;
    for (int i = 0; i < 9081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9082(int& val) {
    const int x = 9082;
    std::cout << 9082;
    for (int i = 0; i < 9082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9083(int& val) {
    const int x = 9083;
    std::cout << 9083;
    for (int i = 0; i < 9083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9084(int& val) {
    const int x = 9084;
    std::cout << 9084;
    for (int i = 0; i < 9084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9085(int& val) {
    const int x = 9085;
    std::cout << 9085;
    for (int i = 0; i < 9085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9086(int& val) {
    const int x = 9086;
    std::cout << 9086;
    for (int i = 0; i < 9086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9087(int& val) {
    const int x = 9087;
    std::cout << 9087;
    for (int i = 0; i < 9087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9088(int& val) {
    const int x = 9088;
    std::cout << 9088;
    for (int i = 0; i < 9088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9089(int& val) {
    const int x = 9089;
    std::cout << 9089;
    for (int i = 0; i < 9089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9090(int& val) {
    const int x = 9090;
    std::cout << 9090;
    for (int i = 0; i < 9090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9091(int& val) {
    const int x = 9091;
    std::cout << 9091;
    for (int i = 0; i < 9091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9092(int& val) {
    const int x = 9092;
    std::cout << 9092;
    for (int i = 0; i < 9092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9093(int& val) {
    const int x = 9093;
    std::cout << 9093;
    for (int i = 0; i < 9093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9094(int& val) {
    const int x = 9094;
    std::cout << 9094;
    for (int i = 0; i < 9094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9095(int& val) {
    const int x = 9095;
    std::cout << 9095;
    for (int i = 0; i < 9095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9096(int& val) {
    const int x = 9096;
    std::cout << 9096;
    for (int i = 0; i < 9096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9097(int& val) {
    const int x = 9097;
    std::cout << 9097;
    for (int i = 0; i < 9097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9098(int& val) {
    const int x = 9098;
    std::cout << 9098;
    for (int i = 0; i < 9098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9099(int& val) {
    const int x = 9099;
    std::cout << 9099;
    for (int i = 0; i < 9099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9100(int& val) {
    const int x = 9100;
    std::cout << 9100;
    for (int i = 0; i < 9100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9101(int& val) {
    const int x = 9101;
    std::cout << 9101;
    for (int i = 0; i < 9101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9102(int& val) {
    const int x = 9102;
    std::cout << 9102;
    for (int i = 0; i < 9102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9103(int& val) {
    const int x = 9103;
    std::cout << 9103;
    for (int i = 0; i < 9103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9104(int& val) {
    const int x = 9104;
    std::cout << 9104;
    for (int i = 0; i < 9104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9105(int& val) {
    const int x = 9105;
    std::cout << 9105;
    for (int i = 0; i < 9105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9106(int& val) {
    const int x = 9106;
    std::cout << 9106;
    for (int i = 0; i < 9106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9107(int& val) {
    const int x = 9107;
    std::cout << 9107;
    for (int i = 0; i < 9107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9108(int& val) {
    const int x = 9108;
    std::cout << 9108;
    for (int i = 0; i < 9108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9109(int& val) {
    const int x = 9109;
    std::cout << 9109;
    for (int i = 0; i < 9109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9110(int& val) {
    const int x = 9110;
    std::cout << 9110;
    for (int i = 0; i < 9110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9111(int& val) {
    const int x = 9111;
    std::cout << 9111;
    for (int i = 0; i < 9111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9112(int& val) {
    const int x = 9112;
    std::cout << 9112;
    for (int i = 0; i < 9112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9113(int& val) {
    const int x = 9113;
    std::cout << 9113;
    for (int i = 0; i < 9113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9114(int& val) {
    const int x = 9114;
    std::cout << 9114;
    for (int i = 0; i < 9114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9115(int& val) {
    const int x = 9115;
    std::cout << 9115;
    for (int i = 0; i < 9115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9116(int& val) {
    const int x = 9116;
    std::cout << 9116;
    for (int i = 0; i < 9116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9117(int& val) {
    const int x = 9117;
    std::cout << 9117;
    for (int i = 0; i < 9117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9118(int& val) {
    const int x = 9118;
    std::cout << 9118;
    for (int i = 0; i < 9118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9119(int& val) {
    const int x = 9119;
    std::cout << 9119;
    for (int i = 0; i < 9119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9120(int& val) {
    const int x = 9120;
    std::cout << 9120;
    for (int i = 0; i < 9120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9121(int& val) {
    const int x = 9121;
    std::cout << 9121;
    for (int i = 0; i < 9121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9122(int& val) {
    const int x = 9122;
    std::cout << 9122;
    for (int i = 0; i < 9122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9123(int& val) {
    const int x = 9123;
    std::cout << 9123;
    for (int i = 0; i < 9123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9124(int& val) {
    const int x = 9124;
    std::cout << 9124;
    for (int i = 0; i < 9124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9125(int& val) {
    const int x = 9125;
    std::cout << 9125;
    for (int i = 0; i < 9125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9126(int& val) {
    const int x = 9126;
    std::cout << 9126;
    for (int i = 0; i < 9126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9127(int& val) {
    const int x = 9127;
    std::cout << 9127;
    for (int i = 0; i < 9127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9128(int& val) {
    const int x = 9128;
    std::cout << 9128;
    for (int i = 0; i < 9128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9129(int& val) {
    const int x = 9129;
    std::cout << 9129;
    for (int i = 0; i < 9129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9130(int& val) {
    const int x = 9130;
    std::cout << 9130;
    for (int i = 0; i < 9130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9131(int& val) {
    const int x = 9131;
    std::cout << 9131;
    for (int i = 0; i < 9131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9132(int& val) {
    const int x = 9132;
    std::cout << 9132;
    for (int i = 0; i < 9132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9133(int& val) {
    const int x = 9133;
    std::cout << 9133;
    for (int i = 0; i < 9133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9134(int& val) {
    const int x = 9134;
    std::cout << 9134;
    for (int i = 0; i < 9134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9135(int& val) {
    const int x = 9135;
    std::cout << 9135;
    for (int i = 0; i < 9135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9136(int& val) {
    const int x = 9136;
    std::cout << 9136;
    for (int i = 0; i < 9136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9137(int& val) {
    const int x = 9137;
    std::cout << 9137;
    for (int i = 0; i < 9137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9138(int& val) {
    const int x = 9138;
    std::cout << 9138;
    for (int i = 0; i < 9138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9139(int& val) {
    const int x = 9139;
    std::cout << 9139;
    for (int i = 0; i < 9139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9140(int& val) {
    const int x = 9140;
    std::cout << 9140;
    for (int i = 0; i < 9140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9141(int& val) {
    const int x = 9141;
    std::cout << 9141;
    for (int i = 0; i < 9141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9142(int& val) {
    const int x = 9142;
    std::cout << 9142;
    for (int i = 0; i < 9142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9143(int& val) {
    const int x = 9143;
    std::cout << 9143;
    for (int i = 0; i < 9143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9144(int& val) {
    const int x = 9144;
    std::cout << 9144;
    for (int i = 0; i < 9144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9145(int& val) {
    const int x = 9145;
    std::cout << 9145;
    for (int i = 0; i < 9145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9146(int& val) {
    const int x = 9146;
    std::cout << 9146;
    for (int i = 0; i < 9146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9147(int& val) {
    const int x = 9147;
    std::cout << 9147;
    for (int i = 0; i < 9147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9148(int& val) {
    const int x = 9148;
    std::cout << 9148;
    for (int i = 0; i < 9148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9149(int& val) {
    const int x = 9149;
    std::cout << 9149;
    for (int i = 0; i < 9149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9150(int& val) {
    const int x = 9150;
    std::cout << 9150;
    for (int i = 0; i < 9150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9151(int& val) {
    const int x = 9151;
    std::cout << 9151;
    for (int i = 0; i < 9151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9152(int& val) {
    const int x = 9152;
    std::cout << 9152;
    for (int i = 0; i < 9152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9153(int& val) {
    const int x = 9153;
    std::cout << 9153;
    for (int i = 0; i < 9153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9154(int& val) {
    const int x = 9154;
    std::cout << 9154;
    for (int i = 0; i < 9154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9155(int& val) {
    const int x = 9155;
    std::cout << 9155;
    for (int i = 0; i < 9155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9156(int& val) {
    const int x = 9156;
    std::cout << 9156;
    for (int i = 0; i < 9156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9157(int& val) {
    const int x = 9157;
    std::cout << 9157;
    for (int i = 0; i < 9157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9158(int& val) {
    const int x = 9158;
    std::cout << 9158;
    for (int i = 0; i < 9158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9159(int& val) {
    const int x = 9159;
    std::cout << 9159;
    for (int i = 0; i < 9159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9160(int& val) {
    const int x = 9160;
    std::cout << 9160;
    for (int i = 0; i < 9160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9161(int& val) {
    const int x = 9161;
    std::cout << 9161;
    for (int i = 0; i < 9161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9162(int& val) {
    const int x = 9162;
    std::cout << 9162;
    for (int i = 0; i < 9162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9163(int& val) {
    const int x = 9163;
    std::cout << 9163;
    for (int i = 0; i < 9163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9164(int& val) {
    const int x = 9164;
    std::cout << 9164;
    for (int i = 0; i < 9164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9165(int& val) {
    const int x = 9165;
    std::cout << 9165;
    for (int i = 0; i < 9165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9166(int& val) {
    const int x = 9166;
    std::cout << 9166;
    for (int i = 0; i < 9166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9167(int& val) {
    const int x = 9167;
    std::cout << 9167;
    for (int i = 0; i < 9167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9168(int& val) {
    const int x = 9168;
    std::cout << 9168;
    for (int i = 0; i < 9168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9169(int& val) {
    const int x = 9169;
    std::cout << 9169;
    for (int i = 0; i < 9169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9170(int& val) {
    const int x = 9170;
    std::cout << 9170;
    for (int i = 0; i < 9170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9171(int& val) {
    const int x = 9171;
    std::cout << 9171;
    for (int i = 0; i < 9171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9172(int& val) {
    const int x = 9172;
    std::cout << 9172;
    for (int i = 0; i < 9172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9173(int& val) {
    const int x = 9173;
    std::cout << 9173;
    for (int i = 0; i < 9173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9174(int& val) {
    const int x = 9174;
    std::cout << 9174;
    for (int i = 0; i < 9174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9175(int& val) {
    const int x = 9175;
    std::cout << 9175;
    for (int i = 0; i < 9175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9176(int& val) {
    const int x = 9176;
    std::cout << 9176;
    for (int i = 0; i < 9176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9177(int& val) {
    const int x = 9177;
    std::cout << 9177;
    for (int i = 0; i < 9177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9178(int& val) {
    const int x = 9178;
    std::cout << 9178;
    for (int i = 0; i < 9178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9179(int& val) {
    const int x = 9179;
    std::cout << 9179;
    for (int i = 0; i < 9179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9180(int& val) {
    const int x = 9180;
    std::cout << 9180;
    for (int i = 0; i < 9180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9181(int& val) {
    const int x = 9181;
    std::cout << 9181;
    for (int i = 0; i < 9181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9182(int& val) {
    const int x = 9182;
    std::cout << 9182;
    for (int i = 0; i < 9182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9183(int& val) {
    const int x = 9183;
    std::cout << 9183;
    for (int i = 0; i < 9183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9184(int& val) {
    const int x = 9184;
    std::cout << 9184;
    for (int i = 0; i < 9184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9185(int& val) {
    const int x = 9185;
    std::cout << 9185;
    for (int i = 0; i < 9185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9186(int& val) {
    const int x = 9186;
    std::cout << 9186;
    for (int i = 0; i < 9186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9187(int& val) {
    const int x = 9187;
    std::cout << 9187;
    for (int i = 0; i < 9187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9188(int& val) {
    const int x = 9188;
    std::cout << 9188;
    for (int i = 0; i < 9188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9189(int& val) {
    const int x = 9189;
    std::cout << 9189;
    for (int i = 0; i < 9189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9190(int& val) {
    const int x = 9190;
    std::cout << 9190;
    for (int i = 0; i < 9190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9191(int& val) {
    const int x = 9191;
    std::cout << 9191;
    for (int i = 0; i < 9191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9192(int& val) {
    const int x = 9192;
    std::cout << 9192;
    for (int i = 0; i < 9192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9193(int& val) {
    const int x = 9193;
    std::cout << 9193;
    for (int i = 0; i < 9193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9194(int& val) {
    const int x = 9194;
    std::cout << 9194;
    for (int i = 0; i < 9194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9195(int& val) {
    const int x = 9195;
    std::cout << 9195;
    for (int i = 0; i < 9195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9196(int& val) {
    const int x = 9196;
    std::cout << 9196;
    for (int i = 0; i < 9196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9197(int& val) {
    const int x = 9197;
    std::cout << 9197;
    for (int i = 0; i < 9197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9198(int& val) {
    const int x = 9198;
    std::cout << 9198;
    for (int i = 0; i < 9198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9199(int& val) {
    const int x = 9199;
    std::cout << 9199;
    for (int i = 0; i < 9199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9200(int& val) {
    const int x = 9200;
    std::cout << 9200;
    for (int i = 0; i < 9200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9201(int& val) {
    const int x = 9201;
    std::cout << 9201;
    for (int i = 0; i < 9201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9202(int& val) {
    const int x = 9202;
    std::cout << 9202;
    for (int i = 0; i < 9202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9203(int& val) {
    const int x = 9203;
    std::cout << 9203;
    for (int i = 0; i < 9203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9204(int& val) {
    const int x = 9204;
    std::cout << 9204;
    for (int i = 0; i < 9204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9205(int& val) {
    const int x = 9205;
    std::cout << 9205;
    for (int i = 0; i < 9205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9206(int& val) {
    const int x = 9206;
    std::cout << 9206;
    for (int i = 0; i < 9206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9207(int& val) {
    const int x = 9207;
    std::cout << 9207;
    for (int i = 0; i < 9207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9208(int& val) {
    const int x = 9208;
    std::cout << 9208;
    for (int i = 0; i < 9208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9209(int& val) {
    const int x = 9209;
    std::cout << 9209;
    for (int i = 0; i < 9209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9210(int& val) {
    const int x = 9210;
    std::cout << 9210;
    for (int i = 0; i < 9210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9211(int& val) {
    const int x = 9211;
    std::cout << 9211;
    for (int i = 0; i < 9211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9212(int& val) {
    const int x = 9212;
    std::cout << 9212;
    for (int i = 0; i < 9212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9213(int& val) {
    const int x = 9213;
    std::cout << 9213;
    for (int i = 0; i < 9213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9214(int& val) {
    const int x = 9214;
    std::cout << 9214;
    for (int i = 0; i < 9214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9215(int& val) {
    const int x = 9215;
    std::cout << 9215;
    for (int i = 0; i < 9215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9216(int& val) {
    const int x = 9216;
    std::cout << 9216;
    for (int i = 0; i < 9216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9217(int& val) {
    const int x = 9217;
    std::cout << 9217;
    for (int i = 0; i < 9217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9218(int& val) {
    const int x = 9218;
    std::cout << 9218;
    for (int i = 0; i < 9218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9219(int& val) {
    const int x = 9219;
    std::cout << 9219;
    for (int i = 0; i < 9219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9220(int& val) {
    const int x = 9220;
    std::cout << 9220;
    for (int i = 0; i < 9220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9221(int& val) {
    const int x = 9221;
    std::cout << 9221;
    for (int i = 0; i < 9221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9222(int& val) {
    const int x = 9222;
    std::cout << 9222;
    for (int i = 0; i < 9222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9223(int& val) {
    const int x = 9223;
    std::cout << 9223;
    for (int i = 0; i < 9223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9224(int& val) {
    const int x = 9224;
    std::cout << 9224;
    for (int i = 0; i < 9224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9225(int& val) {
    const int x = 9225;
    std::cout << 9225;
    for (int i = 0; i < 9225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9226(int& val) {
    const int x = 9226;
    std::cout << 9226;
    for (int i = 0; i < 9226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9227(int& val) {
    const int x = 9227;
    std::cout << 9227;
    for (int i = 0; i < 9227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9228(int& val) {
    const int x = 9228;
    std::cout << 9228;
    for (int i = 0; i < 9228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9229(int& val) {
    const int x = 9229;
    std::cout << 9229;
    for (int i = 0; i < 9229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9230(int& val) {
    const int x = 9230;
    std::cout << 9230;
    for (int i = 0; i < 9230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9231(int& val) {
    const int x = 9231;
    std::cout << 9231;
    for (int i = 0; i < 9231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9232(int& val) {
    const int x = 9232;
    std::cout << 9232;
    for (int i = 0; i < 9232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9233(int& val) {
    const int x = 9233;
    std::cout << 9233;
    for (int i = 0; i < 9233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9234(int& val) {
    const int x = 9234;
    std::cout << 9234;
    for (int i = 0; i < 9234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9235(int& val) {
    const int x = 9235;
    std::cout << 9235;
    for (int i = 0; i < 9235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9236(int& val) {
    const int x = 9236;
    std::cout << 9236;
    for (int i = 0; i < 9236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9237(int& val) {
    const int x = 9237;
    std::cout << 9237;
    for (int i = 0; i < 9237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9238(int& val) {
    const int x = 9238;
    std::cout << 9238;
    for (int i = 0; i < 9238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9239(int& val) {
    const int x = 9239;
    std::cout << 9239;
    for (int i = 0; i < 9239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9240(int& val) {
    const int x = 9240;
    std::cout << 9240;
    for (int i = 0; i < 9240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9241(int& val) {
    const int x = 9241;
    std::cout << 9241;
    for (int i = 0; i < 9241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9242(int& val) {
    const int x = 9242;
    std::cout << 9242;
    for (int i = 0; i < 9242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9243(int& val) {
    const int x = 9243;
    std::cout << 9243;
    for (int i = 0; i < 9243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9244(int& val) {
    const int x = 9244;
    std::cout << 9244;
    for (int i = 0; i < 9244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9245(int& val) {
    const int x = 9245;
    std::cout << 9245;
    for (int i = 0; i < 9245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9246(int& val) {
    const int x = 9246;
    std::cout << 9246;
    for (int i = 0; i < 9246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9247(int& val) {
    const int x = 9247;
    std::cout << 9247;
    for (int i = 0; i < 9247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9248(int& val) {
    const int x = 9248;
    std::cout << 9248;
    for (int i = 0; i < 9248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9249(int& val) {
    const int x = 9249;
    std::cout << 9249;
    for (int i = 0; i < 9249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9250(int& val) {
    const int x = 9250;
    std::cout << 9250;
    for (int i = 0; i < 9250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9251(int& val) {
    const int x = 9251;
    std::cout << 9251;
    for (int i = 0; i < 9251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9252(int& val) {
    const int x = 9252;
    std::cout << 9252;
    for (int i = 0; i < 9252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9253(int& val) {
    const int x = 9253;
    std::cout << 9253;
    for (int i = 0; i < 9253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9254(int& val) {
    const int x = 9254;
    std::cout << 9254;
    for (int i = 0; i < 9254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9255(int& val) {
    const int x = 9255;
    std::cout << 9255;
    for (int i = 0; i < 9255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9256(int& val) {
    const int x = 9256;
    std::cout << 9256;
    for (int i = 0; i < 9256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9257(int& val) {
    const int x = 9257;
    std::cout << 9257;
    for (int i = 0; i < 9257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9258(int& val) {
    const int x = 9258;
    std::cout << 9258;
    for (int i = 0; i < 9258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9259(int& val) {
    const int x = 9259;
    std::cout << 9259;
    for (int i = 0; i < 9259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9260(int& val) {
    const int x = 9260;
    std::cout << 9260;
    for (int i = 0; i < 9260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9261(int& val) {
    const int x = 9261;
    std::cout << 9261;
    for (int i = 0; i < 9261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9262(int& val) {
    const int x = 9262;
    std::cout << 9262;
    for (int i = 0; i < 9262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9263(int& val) {
    const int x = 9263;
    std::cout << 9263;
    for (int i = 0; i < 9263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9264(int& val) {
    const int x = 9264;
    std::cout << 9264;
    for (int i = 0; i < 9264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9265(int& val) {
    const int x = 9265;
    std::cout << 9265;
    for (int i = 0; i < 9265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9266(int& val) {
    const int x = 9266;
    std::cout << 9266;
    for (int i = 0; i < 9266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9267(int& val) {
    const int x = 9267;
    std::cout << 9267;
    for (int i = 0; i < 9267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9268(int& val) {
    const int x = 9268;
    std::cout << 9268;
    for (int i = 0; i < 9268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9269(int& val) {
    const int x = 9269;
    std::cout << 9269;
    for (int i = 0; i < 9269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9270(int& val) {
    const int x = 9270;
    std::cout << 9270;
    for (int i = 0; i < 9270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9271(int& val) {
    const int x = 9271;
    std::cout << 9271;
    for (int i = 0; i < 9271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9272(int& val) {
    const int x = 9272;
    std::cout << 9272;
    for (int i = 0; i < 9272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9273(int& val) {
    const int x = 9273;
    std::cout << 9273;
    for (int i = 0; i < 9273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9274(int& val) {
    const int x = 9274;
    std::cout << 9274;
    for (int i = 0; i < 9274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9275(int& val) {
    const int x = 9275;
    std::cout << 9275;
    for (int i = 0; i < 9275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9276(int& val) {
    const int x = 9276;
    std::cout << 9276;
    for (int i = 0; i < 9276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9277(int& val) {
    const int x = 9277;
    std::cout << 9277;
    for (int i = 0; i < 9277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9278(int& val) {
    const int x = 9278;
    std::cout << 9278;
    for (int i = 0; i < 9278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9279(int& val) {
    const int x = 9279;
    std::cout << 9279;
    for (int i = 0; i < 9279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9280(int& val) {
    const int x = 9280;
    std::cout << 9280;
    for (int i = 0; i < 9280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9281(int& val) {
    const int x = 9281;
    std::cout << 9281;
    for (int i = 0; i < 9281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9282(int& val) {
    const int x = 9282;
    std::cout << 9282;
    for (int i = 0; i < 9282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9283(int& val) {
    const int x = 9283;
    std::cout << 9283;
    for (int i = 0; i < 9283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9284(int& val) {
    const int x = 9284;
    std::cout << 9284;
    for (int i = 0; i < 9284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9285(int& val) {
    const int x = 9285;
    std::cout << 9285;
    for (int i = 0; i < 9285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9286(int& val) {
    const int x = 9286;
    std::cout << 9286;
    for (int i = 0; i < 9286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9287(int& val) {
    const int x = 9287;
    std::cout << 9287;
    for (int i = 0; i < 9287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9288(int& val) {
    const int x = 9288;
    std::cout << 9288;
    for (int i = 0; i < 9288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9289(int& val) {
    const int x = 9289;
    std::cout << 9289;
    for (int i = 0; i < 9289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9290(int& val) {
    const int x = 9290;
    std::cout << 9290;
    for (int i = 0; i < 9290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9291(int& val) {
    const int x = 9291;
    std::cout << 9291;
    for (int i = 0; i < 9291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9292(int& val) {
    const int x = 9292;
    std::cout << 9292;
    for (int i = 0; i < 9292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9293(int& val) {
    const int x = 9293;
    std::cout << 9293;
    for (int i = 0; i < 9293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9294(int& val) {
    const int x = 9294;
    std::cout << 9294;
    for (int i = 0; i < 9294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9295(int& val) {
    const int x = 9295;
    std::cout << 9295;
    for (int i = 0; i < 9295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9296(int& val) {
    const int x = 9296;
    std::cout << 9296;
    for (int i = 0; i < 9296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9297(int& val) {
    const int x = 9297;
    std::cout << 9297;
    for (int i = 0; i < 9297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9298(int& val) {
    const int x = 9298;
    std::cout << 9298;
    for (int i = 0; i < 9298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9299(int& val) {
    const int x = 9299;
    std::cout << 9299;
    for (int i = 0; i < 9299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9300(int& val) {
    const int x = 9300;
    std::cout << 9300;
    for (int i = 0; i < 9300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9301(int& val) {
    const int x = 9301;
    std::cout << 9301;
    for (int i = 0; i < 9301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9302(int& val) {
    const int x = 9302;
    std::cout << 9302;
    for (int i = 0; i < 9302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9303(int& val) {
    const int x = 9303;
    std::cout << 9303;
    for (int i = 0; i < 9303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9304(int& val) {
    const int x = 9304;
    std::cout << 9304;
    for (int i = 0; i < 9304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9305(int& val) {
    const int x = 9305;
    std::cout << 9305;
    for (int i = 0; i < 9305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9306(int& val) {
    const int x = 9306;
    std::cout << 9306;
    for (int i = 0; i < 9306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9307(int& val) {
    const int x = 9307;
    std::cout << 9307;
    for (int i = 0; i < 9307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9308(int& val) {
    const int x = 9308;
    std::cout << 9308;
    for (int i = 0; i < 9308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9309(int& val) {
    const int x = 9309;
    std::cout << 9309;
    for (int i = 0; i < 9309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9310(int& val) {
    const int x = 9310;
    std::cout << 9310;
    for (int i = 0; i < 9310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9311(int& val) {
    const int x = 9311;
    std::cout << 9311;
    for (int i = 0; i < 9311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9312(int& val) {
    const int x = 9312;
    std::cout << 9312;
    for (int i = 0; i < 9312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9313(int& val) {
    const int x = 9313;
    std::cout << 9313;
    for (int i = 0; i < 9313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9314(int& val) {
    const int x = 9314;
    std::cout << 9314;
    for (int i = 0; i < 9314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9315(int& val) {
    const int x = 9315;
    std::cout << 9315;
    for (int i = 0; i < 9315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9316(int& val) {
    const int x = 9316;
    std::cout << 9316;
    for (int i = 0; i < 9316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9317(int& val) {
    const int x = 9317;
    std::cout << 9317;
    for (int i = 0; i < 9317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9318(int& val) {
    const int x = 9318;
    std::cout << 9318;
    for (int i = 0; i < 9318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9319(int& val) {
    const int x = 9319;
    std::cout << 9319;
    for (int i = 0; i < 9319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9320(int& val) {
    const int x = 9320;
    std::cout << 9320;
    for (int i = 0; i < 9320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9321(int& val) {
    const int x = 9321;
    std::cout << 9321;
    for (int i = 0; i < 9321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9322(int& val) {
    const int x = 9322;
    std::cout << 9322;
    for (int i = 0; i < 9322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9323(int& val) {
    const int x = 9323;
    std::cout << 9323;
    for (int i = 0; i < 9323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9324(int& val) {
    const int x = 9324;
    std::cout << 9324;
    for (int i = 0; i < 9324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9325(int& val) {
    const int x = 9325;
    std::cout << 9325;
    for (int i = 0; i < 9325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9326(int& val) {
    const int x = 9326;
    std::cout << 9326;
    for (int i = 0; i < 9326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9327(int& val) {
    const int x = 9327;
    std::cout << 9327;
    for (int i = 0; i < 9327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9328(int& val) {
    const int x = 9328;
    std::cout << 9328;
    for (int i = 0; i < 9328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9329(int& val) {
    const int x = 9329;
    std::cout << 9329;
    for (int i = 0; i < 9329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9330(int& val) {
    const int x = 9330;
    std::cout << 9330;
    for (int i = 0; i < 9330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9331(int& val) {
    const int x = 9331;
    std::cout << 9331;
    for (int i = 0; i < 9331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9332(int& val) {
    const int x = 9332;
    std::cout << 9332;
    for (int i = 0; i < 9332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9333(int& val) {
    const int x = 9333;
    std::cout << 9333;
    for (int i = 0; i < 9333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9334(int& val) {
    const int x = 9334;
    std::cout << 9334;
    for (int i = 0; i < 9334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9335(int& val) {
    const int x = 9335;
    std::cout << 9335;
    for (int i = 0; i < 9335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9336(int& val) {
    const int x = 9336;
    std::cout << 9336;
    for (int i = 0; i < 9336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9337(int& val) {
    const int x = 9337;
    std::cout << 9337;
    for (int i = 0; i < 9337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9338(int& val) {
    const int x = 9338;
    std::cout << 9338;
    for (int i = 0; i < 9338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9339(int& val) {
    const int x = 9339;
    std::cout << 9339;
    for (int i = 0; i < 9339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9340(int& val) {
    const int x = 9340;
    std::cout << 9340;
    for (int i = 0; i < 9340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9341(int& val) {
    const int x = 9341;
    std::cout << 9341;
    for (int i = 0; i < 9341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9342(int& val) {
    const int x = 9342;
    std::cout << 9342;
    for (int i = 0; i < 9342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9343(int& val) {
    const int x = 9343;
    std::cout << 9343;
    for (int i = 0; i < 9343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9344(int& val) {
    const int x = 9344;
    std::cout << 9344;
    for (int i = 0; i < 9344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9345(int& val) {
    const int x = 9345;
    std::cout << 9345;
    for (int i = 0; i < 9345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9346(int& val) {
    const int x = 9346;
    std::cout << 9346;
    for (int i = 0; i < 9346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9347(int& val) {
    const int x = 9347;
    std::cout << 9347;
    for (int i = 0; i < 9347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9348(int& val) {
    const int x = 9348;
    std::cout << 9348;
    for (int i = 0; i < 9348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9349(int& val) {
    const int x = 9349;
    std::cout << 9349;
    for (int i = 0; i < 9349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9350(int& val) {
    const int x = 9350;
    std::cout << 9350;
    for (int i = 0; i < 9350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9351(int& val) {
    const int x = 9351;
    std::cout << 9351;
    for (int i = 0; i < 9351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9352(int& val) {
    const int x = 9352;
    std::cout << 9352;
    for (int i = 0; i < 9352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9353(int& val) {
    const int x = 9353;
    std::cout << 9353;
    for (int i = 0; i < 9353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9354(int& val) {
    const int x = 9354;
    std::cout << 9354;
    for (int i = 0; i < 9354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9355(int& val) {
    const int x = 9355;
    std::cout << 9355;
    for (int i = 0; i < 9355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9356(int& val) {
    const int x = 9356;
    std::cout << 9356;
    for (int i = 0; i < 9356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9357(int& val) {
    const int x = 9357;
    std::cout << 9357;
    for (int i = 0; i < 9357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9358(int& val) {
    const int x = 9358;
    std::cout << 9358;
    for (int i = 0; i < 9358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9359(int& val) {
    const int x = 9359;
    std::cout << 9359;
    for (int i = 0; i < 9359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9360(int& val) {
    const int x = 9360;
    std::cout << 9360;
    for (int i = 0; i < 9360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9361(int& val) {
    const int x = 9361;
    std::cout << 9361;
    for (int i = 0; i < 9361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9362(int& val) {
    const int x = 9362;
    std::cout << 9362;
    for (int i = 0; i < 9362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9363(int& val) {
    const int x = 9363;
    std::cout << 9363;
    for (int i = 0; i < 9363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9364(int& val) {
    const int x = 9364;
    std::cout << 9364;
    for (int i = 0; i < 9364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9365(int& val) {
    const int x = 9365;
    std::cout << 9365;
    for (int i = 0; i < 9365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9366(int& val) {
    const int x = 9366;
    std::cout << 9366;
    for (int i = 0; i < 9366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9367(int& val) {
    const int x = 9367;
    std::cout << 9367;
    for (int i = 0; i < 9367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9368(int& val) {
    const int x = 9368;
    std::cout << 9368;
    for (int i = 0; i < 9368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9369(int& val) {
    const int x = 9369;
    std::cout << 9369;
    for (int i = 0; i < 9369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9370(int& val) {
    const int x = 9370;
    std::cout << 9370;
    for (int i = 0; i < 9370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9371(int& val) {
    const int x = 9371;
    std::cout << 9371;
    for (int i = 0; i < 9371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9372(int& val) {
    const int x = 9372;
    std::cout << 9372;
    for (int i = 0; i < 9372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9373(int& val) {
    const int x = 9373;
    std::cout << 9373;
    for (int i = 0; i < 9373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9374(int& val) {
    const int x = 9374;
    std::cout << 9374;
    for (int i = 0; i < 9374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9375(int& val) {
    const int x = 9375;
    std::cout << 9375;
    for (int i = 0; i < 9375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9376(int& val) {
    const int x = 9376;
    std::cout << 9376;
    for (int i = 0; i < 9376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9377(int& val) {
    const int x = 9377;
    std::cout << 9377;
    for (int i = 0; i < 9377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9378(int& val) {
    const int x = 9378;
    std::cout << 9378;
    for (int i = 0; i < 9378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9379(int& val) {
    const int x = 9379;
    std::cout << 9379;
    for (int i = 0; i < 9379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9380(int& val) {
    const int x = 9380;
    std::cout << 9380;
    for (int i = 0; i < 9380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9381(int& val) {
    const int x = 9381;
    std::cout << 9381;
    for (int i = 0; i < 9381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9382(int& val) {
    const int x = 9382;
    std::cout << 9382;
    for (int i = 0; i < 9382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9383(int& val) {
    const int x = 9383;
    std::cout << 9383;
    for (int i = 0; i < 9383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9384(int& val) {
    const int x = 9384;
    std::cout << 9384;
    for (int i = 0; i < 9384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9385(int& val) {
    const int x = 9385;
    std::cout << 9385;
    for (int i = 0; i < 9385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9386(int& val) {
    const int x = 9386;
    std::cout << 9386;
    for (int i = 0; i < 9386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9387(int& val) {
    const int x = 9387;
    std::cout << 9387;
    for (int i = 0; i < 9387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9388(int& val) {
    const int x = 9388;
    std::cout << 9388;
    for (int i = 0; i < 9388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9389(int& val) {
    const int x = 9389;
    std::cout << 9389;
    for (int i = 0; i < 9389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9390(int& val) {
    const int x = 9390;
    std::cout << 9390;
    for (int i = 0; i < 9390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9391(int& val) {
    const int x = 9391;
    std::cout << 9391;
    for (int i = 0; i < 9391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9392(int& val) {
    const int x = 9392;
    std::cout << 9392;
    for (int i = 0; i < 9392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9393(int& val) {
    const int x = 9393;
    std::cout << 9393;
    for (int i = 0; i < 9393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9394(int& val) {
    const int x = 9394;
    std::cout << 9394;
    for (int i = 0; i < 9394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9395(int& val) {
    const int x = 9395;
    std::cout << 9395;
    for (int i = 0; i < 9395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9396(int& val) {
    const int x = 9396;
    std::cout << 9396;
    for (int i = 0; i < 9396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9397(int& val) {
    const int x = 9397;
    std::cout << 9397;
    for (int i = 0; i < 9397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9398(int& val) {
    const int x = 9398;
    std::cout << 9398;
    for (int i = 0; i < 9398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9399(int& val) {
    const int x = 9399;
    std::cout << 9399;
    for (int i = 0; i < 9399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9400(int& val) {
    const int x = 9400;
    std::cout << 9400;
    for (int i = 0; i < 9400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9401(int& val) {
    const int x = 9401;
    std::cout << 9401;
    for (int i = 0; i < 9401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9402(int& val) {
    const int x = 9402;
    std::cout << 9402;
    for (int i = 0; i < 9402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9403(int& val) {
    const int x = 9403;
    std::cout << 9403;
    for (int i = 0; i < 9403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9404(int& val) {
    const int x = 9404;
    std::cout << 9404;
    for (int i = 0; i < 9404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9405(int& val) {
    const int x = 9405;
    std::cout << 9405;
    for (int i = 0; i < 9405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9406(int& val) {
    const int x = 9406;
    std::cout << 9406;
    for (int i = 0; i < 9406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9407(int& val) {
    const int x = 9407;
    std::cout << 9407;
    for (int i = 0; i < 9407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9408(int& val) {
    const int x = 9408;
    std::cout << 9408;
    for (int i = 0; i < 9408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9409(int& val) {
    const int x = 9409;
    std::cout << 9409;
    for (int i = 0; i < 9409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9410(int& val) {
    const int x = 9410;
    std::cout << 9410;
    for (int i = 0; i < 9410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9411(int& val) {
    const int x = 9411;
    std::cout << 9411;
    for (int i = 0; i < 9411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9412(int& val) {
    const int x = 9412;
    std::cout << 9412;
    for (int i = 0; i < 9412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9413(int& val) {
    const int x = 9413;
    std::cout << 9413;
    for (int i = 0; i < 9413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9414(int& val) {
    const int x = 9414;
    std::cout << 9414;
    for (int i = 0; i < 9414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9415(int& val) {
    const int x = 9415;
    std::cout << 9415;
    for (int i = 0; i < 9415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9416(int& val) {
    const int x = 9416;
    std::cout << 9416;
    for (int i = 0; i < 9416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9417(int& val) {
    const int x = 9417;
    std::cout << 9417;
    for (int i = 0; i < 9417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9418(int& val) {
    const int x = 9418;
    std::cout << 9418;
    for (int i = 0; i < 9418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9419(int& val) {
    const int x = 9419;
    std::cout << 9419;
    for (int i = 0; i < 9419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9420(int& val) {
    const int x = 9420;
    std::cout << 9420;
    for (int i = 0; i < 9420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9421(int& val) {
    const int x = 9421;
    std::cout << 9421;
    for (int i = 0; i < 9421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9422(int& val) {
    const int x = 9422;
    std::cout << 9422;
    for (int i = 0; i < 9422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9423(int& val) {
    const int x = 9423;
    std::cout << 9423;
    for (int i = 0; i < 9423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9424(int& val) {
    const int x = 9424;
    std::cout << 9424;
    for (int i = 0; i < 9424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9425(int& val) {
    const int x = 9425;
    std::cout << 9425;
    for (int i = 0; i < 9425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9426(int& val) {
    const int x = 9426;
    std::cout << 9426;
    for (int i = 0; i < 9426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9427(int& val) {
    const int x = 9427;
    std::cout << 9427;
    for (int i = 0; i < 9427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9428(int& val) {
    const int x = 9428;
    std::cout << 9428;
    for (int i = 0; i < 9428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9429(int& val) {
    const int x = 9429;
    std::cout << 9429;
    for (int i = 0; i < 9429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9430(int& val) {
    const int x = 9430;
    std::cout << 9430;
    for (int i = 0; i < 9430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9431(int& val) {
    const int x = 9431;
    std::cout << 9431;
    for (int i = 0; i < 9431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9432(int& val) {
    const int x = 9432;
    std::cout << 9432;
    for (int i = 0; i < 9432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9433(int& val) {
    const int x = 9433;
    std::cout << 9433;
    for (int i = 0; i < 9433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9434(int& val) {
    const int x = 9434;
    std::cout << 9434;
    for (int i = 0; i < 9434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9435(int& val) {
    const int x = 9435;
    std::cout << 9435;
    for (int i = 0; i < 9435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9436(int& val) {
    const int x = 9436;
    std::cout << 9436;
    for (int i = 0; i < 9436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9437(int& val) {
    const int x = 9437;
    std::cout << 9437;
    for (int i = 0; i < 9437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9438(int& val) {
    const int x = 9438;
    std::cout << 9438;
    for (int i = 0; i < 9438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9439(int& val) {
    const int x = 9439;
    std::cout << 9439;
    for (int i = 0; i < 9439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9440(int& val) {
    const int x = 9440;
    std::cout << 9440;
    for (int i = 0; i < 9440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9441(int& val) {
    const int x = 9441;
    std::cout << 9441;
    for (int i = 0; i < 9441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9442(int& val) {
    const int x = 9442;
    std::cout << 9442;
    for (int i = 0; i < 9442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9443(int& val) {
    const int x = 9443;
    std::cout << 9443;
    for (int i = 0; i < 9443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9444(int& val) {
    const int x = 9444;
    std::cout << 9444;
    for (int i = 0; i < 9444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9445(int& val) {
    const int x = 9445;
    std::cout << 9445;
    for (int i = 0; i < 9445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9446(int& val) {
    const int x = 9446;
    std::cout << 9446;
    for (int i = 0; i < 9446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9447(int& val) {
    const int x = 9447;
    std::cout << 9447;
    for (int i = 0; i < 9447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9448(int& val) {
    const int x = 9448;
    std::cout << 9448;
    for (int i = 0; i < 9448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9449(int& val) {
    const int x = 9449;
    std::cout << 9449;
    for (int i = 0; i < 9449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9450(int& val) {
    const int x = 9450;
    std::cout << 9450;
    for (int i = 0; i < 9450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9451(int& val) {
    const int x = 9451;
    std::cout << 9451;
    for (int i = 0; i < 9451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9452(int& val) {
    const int x = 9452;
    std::cout << 9452;
    for (int i = 0; i < 9452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9453(int& val) {
    const int x = 9453;
    std::cout << 9453;
    for (int i = 0; i < 9453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9454(int& val) {
    const int x = 9454;
    std::cout << 9454;
    for (int i = 0; i < 9454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9455(int& val) {
    const int x = 9455;
    std::cout << 9455;
    for (int i = 0; i < 9455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9456(int& val) {
    const int x = 9456;
    std::cout << 9456;
    for (int i = 0; i < 9456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9457(int& val) {
    const int x = 9457;
    std::cout << 9457;
    for (int i = 0; i < 9457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9458(int& val) {
    const int x = 9458;
    std::cout << 9458;
    for (int i = 0; i < 9458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9459(int& val) {
    const int x = 9459;
    std::cout << 9459;
    for (int i = 0; i < 9459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9460(int& val) {
    const int x = 9460;
    std::cout << 9460;
    for (int i = 0; i < 9460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9461(int& val) {
    const int x = 9461;
    std::cout << 9461;
    for (int i = 0; i < 9461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9462(int& val) {
    const int x = 9462;
    std::cout << 9462;
    for (int i = 0; i < 9462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9463(int& val) {
    const int x = 9463;
    std::cout << 9463;
    for (int i = 0; i < 9463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9464(int& val) {
    const int x = 9464;
    std::cout << 9464;
    for (int i = 0; i < 9464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9465(int& val) {
    const int x = 9465;
    std::cout << 9465;
    for (int i = 0; i < 9465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9466(int& val) {
    const int x = 9466;
    std::cout << 9466;
    for (int i = 0; i < 9466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9467(int& val) {
    const int x = 9467;
    std::cout << 9467;
    for (int i = 0; i < 9467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9468(int& val) {
    const int x = 9468;
    std::cout << 9468;
    for (int i = 0; i < 9468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9469(int& val) {
    const int x = 9469;
    std::cout << 9469;
    for (int i = 0; i < 9469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9470(int& val) {
    const int x = 9470;
    std::cout << 9470;
    for (int i = 0; i < 9470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9471(int& val) {
    const int x = 9471;
    std::cout << 9471;
    for (int i = 0; i < 9471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9472(int& val) {
    const int x = 9472;
    std::cout << 9472;
    for (int i = 0; i < 9472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9473(int& val) {
    const int x = 9473;
    std::cout << 9473;
    for (int i = 0; i < 9473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9474(int& val) {
    const int x = 9474;
    std::cout << 9474;
    for (int i = 0; i < 9474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9475(int& val) {
    const int x = 9475;
    std::cout << 9475;
    for (int i = 0; i < 9475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9476(int& val) {
    const int x = 9476;
    std::cout << 9476;
    for (int i = 0; i < 9476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9477(int& val) {
    const int x = 9477;
    std::cout << 9477;
    for (int i = 0; i < 9477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9478(int& val) {
    const int x = 9478;
    std::cout << 9478;
    for (int i = 0; i < 9478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9479(int& val) {
    const int x = 9479;
    std::cout << 9479;
    for (int i = 0; i < 9479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9480(int& val) {
    const int x = 9480;
    std::cout << 9480;
    for (int i = 0; i < 9480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9481(int& val) {
    const int x = 9481;
    std::cout << 9481;
    for (int i = 0; i < 9481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9482(int& val) {
    const int x = 9482;
    std::cout << 9482;
    for (int i = 0; i < 9482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9483(int& val) {
    const int x = 9483;
    std::cout << 9483;
    for (int i = 0; i < 9483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9484(int& val) {
    const int x = 9484;
    std::cout << 9484;
    for (int i = 0; i < 9484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9485(int& val) {
    const int x = 9485;
    std::cout << 9485;
    for (int i = 0; i < 9485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9486(int& val) {
    const int x = 9486;
    std::cout << 9486;
    for (int i = 0; i < 9486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9487(int& val) {
    const int x = 9487;
    std::cout << 9487;
    for (int i = 0; i < 9487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9488(int& val) {
    const int x = 9488;
    std::cout << 9488;
    for (int i = 0; i < 9488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9489(int& val) {
    const int x = 9489;
    std::cout << 9489;
    for (int i = 0; i < 9489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9490(int& val) {
    const int x = 9490;
    std::cout << 9490;
    for (int i = 0; i < 9490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9491(int& val) {
    const int x = 9491;
    std::cout << 9491;
    for (int i = 0; i < 9491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9492(int& val) {
    const int x = 9492;
    std::cout << 9492;
    for (int i = 0; i < 9492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9493(int& val) {
    const int x = 9493;
    std::cout << 9493;
    for (int i = 0; i < 9493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9494(int& val) {
    const int x = 9494;
    std::cout << 9494;
    for (int i = 0; i < 9494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9495(int& val) {
    const int x = 9495;
    std::cout << 9495;
    for (int i = 0; i < 9495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9496(int& val) {
    const int x = 9496;
    std::cout << 9496;
    for (int i = 0; i < 9496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9497(int& val) {
    const int x = 9497;
    std::cout << 9497;
    for (int i = 0; i < 9497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9498(int& val) {
    const int x = 9498;
    std::cout << 9498;
    for (int i = 0; i < 9498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9499(int& val) {
    const int x = 9499;
    std::cout << 9499;
    for (int i = 0; i < 9499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9500(int& val) {
    const int x = 9500;
    std::cout << 9500;
    for (int i = 0; i < 9500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9501(int& val) {
    const int x = 9501;
    std::cout << 9501;
    for (int i = 0; i < 9501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9502(int& val) {
    const int x = 9502;
    std::cout << 9502;
    for (int i = 0; i < 9502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9503(int& val) {
    const int x = 9503;
    std::cout << 9503;
    for (int i = 0; i < 9503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9504(int& val) {
    const int x = 9504;
    std::cout << 9504;
    for (int i = 0; i < 9504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9505(int& val) {
    const int x = 9505;
    std::cout << 9505;
    for (int i = 0; i < 9505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9506(int& val) {
    const int x = 9506;
    std::cout << 9506;
    for (int i = 0; i < 9506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9507(int& val) {
    const int x = 9507;
    std::cout << 9507;
    for (int i = 0; i < 9507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9508(int& val) {
    const int x = 9508;
    std::cout << 9508;
    for (int i = 0; i < 9508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9509(int& val) {
    const int x = 9509;
    std::cout << 9509;
    for (int i = 0; i < 9509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9510(int& val) {
    const int x = 9510;
    std::cout << 9510;
    for (int i = 0; i < 9510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9511(int& val) {
    const int x = 9511;
    std::cout << 9511;
    for (int i = 0; i < 9511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9512(int& val) {
    const int x = 9512;
    std::cout << 9512;
    for (int i = 0; i < 9512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9513(int& val) {
    const int x = 9513;
    std::cout << 9513;
    for (int i = 0; i < 9513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9514(int& val) {
    const int x = 9514;
    std::cout << 9514;
    for (int i = 0; i < 9514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9515(int& val) {
    const int x = 9515;
    std::cout << 9515;
    for (int i = 0; i < 9515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9516(int& val) {
    const int x = 9516;
    std::cout << 9516;
    for (int i = 0; i < 9516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9517(int& val) {
    const int x = 9517;
    std::cout << 9517;
    for (int i = 0; i < 9517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9518(int& val) {
    const int x = 9518;
    std::cout << 9518;
    for (int i = 0; i < 9518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9519(int& val) {
    const int x = 9519;
    std::cout << 9519;
    for (int i = 0; i < 9519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9520(int& val) {
    const int x = 9520;
    std::cout << 9520;
    for (int i = 0; i < 9520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9521(int& val) {
    const int x = 9521;
    std::cout << 9521;
    for (int i = 0; i < 9521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9522(int& val) {
    const int x = 9522;
    std::cout << 9522;
    for (int i = 0; i < 9522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9523(int& val) {
    const int x = 9523;
    std::cout << 9523;
    for (int i = 0; i < 9523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9524(int& val) {
    const int x = 9524;
    std::cout << 9524;
    for (int i = 0; i < 9524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9525(int& val) {
    const int x = 9525;
    std::cout << 9525;
    for (int i = 0; i < 9525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9526(int& val) {
    const int x = 9526;
    std::cout << 9526;
    for (int i = 0; i < 9526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9527(int& val) {
    const int x = 9527;
    std::cout << 9527;
    for (int i = 0; i < 9527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9528(int& val) {
    const int x = 9528;
    std::cout << 9528;
    for (int i = 0; i < 9528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9529(int& val) {
    const int x = 9529;
    std::cout << 9529;
    for (int i = 0; i < 9529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9530(int& val) {
    const int x = 9530;
    std::cout << 9530;
    for (int i = 0; i < 9530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9531(int& val) {
    const int x = 9531;
    std::cout << 9531;
    for (int i = 0; i < 9531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9532(int& val) {
    const int x = 9532;
    std::cout << 9532;
    for (int i = 0; i < 9532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9533(int& val) {
    const int x = 9533;
    std::cout << 9533;
    for (int i = 0; i < 9533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9534(int& val) {
    const int x = 9534;
    std::cout << 9534;
    for (int i = 0; i < 9534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9535(int& val) {
    const int x = 9535;
    std::cout << 9535;
    for (int i = 0; i < 9535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9536(int& val) {
    const int x = 9536;
    std::cout << 9536;
    for (int i = 0; i < 9536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9537(int& val) {
    const int x = 9537;
    std::cout << 9537;
    for (int i = 0; i < 9537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9538(int& val) {
    const int x = 9538;
    std::cout << 9538;
    for (int i = 0; i < 9538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9539(int& val) {
    const int x = 9539;
    std::cout << 9539;
    for (int i = 0; i < 9539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9540(int& val) {
    const int x = 9540;
    std::cout << 9540;
    for (int i = 0; i < 9540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9541(int& val) {
    const int x = 9541;
    std::cout << 9541;
    for (int i = 0; i < 9541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9542(int& val) {
    const int x = 9542;
    std::cout << 9542;
    for (int i = 0; i < 9542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9543(int& val) {
    const int x = 9543;
    std::cout << 9543;
    for (int i = 0; i < 9543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9544(int& val) {
    const int x = 9544;
    std::cout << 9544;
    for (int i = 0; i < 9544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9545(int& val) {
    const int x = 9545;
    std::cout << 9545;
    for (int i = 0; i < 9545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9546(int& val) {
    const int x = 9546;
    std::cout << 9546;
    for (int i = 0; i < 9546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9547(int& val) {
    const int x = 9547;
    std::cout << 9547;
    for (int i = 0; i < 9547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9548(int& val) {
    const int x = 9548;
    std::cout << 9548;
    for (int i = 0; i < 9548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9549(int& val) {
    const int x = 9549;
    std::cout << 9549;
    for (int i = 0; i < 9549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9550(int& val) {
    const int x = 9550;
    std::cout << 9550;
    for (int i = 0; i < 9550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9551(int& val) {
    const int x = 9551;
    std::cout << 9551;
    for (int i = 0; i < 9551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9552(int& val) {
    const int x = 9552;
    std::cout << 9552;
    for (int i = 0; i < 9552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9553(int& val) {
    const int x = 9553;
    std::cout << 9553;
    for (int i = 0; i < 9553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9554(int& val) {
    const int x = 9554;
    std::cout << 9554;
    for (int i = 0; i < 9554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9555(int& val) {
    const int x = 9555;
    std::cout << 9555;
    for (int i = 0; i < 9555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9556(int& val) {
    const int x = 9556;
    std::cout << 9556;
    for (int i = 0; i < 9556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9557(int& val) {
    const int x = 9557;
    std::cout << 9557;
    for (int i = 0; i < 9557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9558(int& val) {
    const int x = 9558;
    std::cout << 9558;
    for (int i = 0; i < 9558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9559(int& val) {
    const int x = 9559;
    std::cout << 9559;
    for (int i = 0; i < 9559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9560(int& val) {
    const int x = 9560;
    std::cout << 9560;
    for (int i = 0; i < 9560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9561(int& val) {
    const int x = 9561;
    std::cout << 9561;
    for (int i = 0; i < 9561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9562(int& val) {
    const int x = 9562;
    std::cout << 9562;
    for (int i = 0; i < 9562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9563(int& val) {
    const int x = 9563;
    std::cout << 9563;
    for (int i = 0; i < 9563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9564(int& val) {
    const int x = 9564;
    std::cout << 9564;
    for (int i = 0; i < 9564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9565(int& val) {
    const int x = 9565;
    std::cout << 9565;
    for (int i = 0; i < 9565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9566(int& val) {
    const int x = 9566;
    std::cout << 9566;
    for (int i = 0; i < 9566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9567(int& val) {
    const int x = 9567;
    std::cout << 9567;
    for (int i = 0; i < 9567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9568(int& val) {
    const int x = 9568;
    std::cout << 9568;
    for (int i = 0; i < 9568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9569(int& val) {
    const int x = 9569;
    std::cout << 9569;
    for (int i = 0; i < 9569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9570(int& val) {
    const int x = 9570;
    std::cout << 9570;
    for (int i = 0; i < 9570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9571(int& val) {
    const int x = 9571;
    std::cout << 9571;
    for (int i = 0; i < 9571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9572(int& val) {
    const int x = 9572;
    std::cout << 9572;
    for (int i = 0; i < 9572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9573(int& val) {
    const int x = 9573;
    std::cout << 9573;
    for (int i = 0; i < 9573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9574(int& val) {
    const int x = 9574;
    std::cout << 9574;
    for (int i = 0; i < 9574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9575(int& val) {
    const int x = 9575;
    std::cout << 9575;
    for (int i = 0; i < 9575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9576(int& val) {
    const int x = 9576;
    std::cout << 9576;
    for (int i = 0; i < 9576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9577(int& val) {
    const int x = 9577;
    std::cout << 9577;
    for (int i = 0; i < 9577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9578(int& val) {
    const int x = 9578;
    std::cout << 9578;
    for (int i = 0; i < 9578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9579(int& val) {
    const int x = 9579;
    std::cout << 9579;
    for (int i = 0; i < 9579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9580(int& val) {
    const int x = 9580;
    std::cout << 9580;
    for (int i = 0; i < 9580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9581(int& val) {
    const int x = 9581;
    std::cout << 9581;
    for (int i = 0; i < 9581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9582(int& val) {
    const int x = 9582;
    std::cout << 9582;
    for (int i = 0; i < 9582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9583(int& val) {
    const int x = 9583;
    std::cout << 9583;
    for (int i = 0; i < 9583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9584(int& val) {
    const int x = 9584;
    std::cout << 9584;
    for (int i = 0; i < 9584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9585(int& val) {
    const int x = 9585;
    std::cout << 9585;
    for (int i = 0; i < 9585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9586(int& val) {
    const int x = 9586;
    std::cout << 9586;
    for (int i = 0; i < 9586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9587(int& val) {
    const int x = 9587;
    std::cout << 9587;
    for (int i = 0; i < 9587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9588(int& val) {
    const int x = 9588;
    std::cout << 9588;
    for (int i = 0; i < 9588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9589(int& val) {
    const int x = 9589;
    std::cout << 9589;
    for (int i = 0; i < 9589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9590(int& val) {
    const int x = 9590;
    std::cout << 9590;
    for (int i = 0; i < 9590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9591(int& val) {
    const int x = 9591;
    std::cout << 9591;
    for (int i = 0; i < 9591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9592(int& val) {
    const int x = 9592;
    std::cout << 9592;
    for (int i = 0; i < 9592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9593(int& val) {
    const int x = 9593;
    std::cout << 9593;
    for (int i = 0; i < 9593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9594(int& val) {
    const int x = 9594;
    std::cout << 9594;
    for (int i = 0; i < 9594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9595(int& val) {
    const int x = 9595;
    std::cout << 9595;
    for (int i = 0; i < 9595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9596(int& val) {
    const int x = 9596;
    std::cout << 9596;
    for (int i = 0; i < 9596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9597(int& val) {
    const int x = 9597;
    std::cout << 9597;
    for (int i = 0; i < 9597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9598(int& val) {
    const int x = 9598;
    std::cout << 9598;
    for (int i = 0; i < 9598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9599(int& val) {
    const int x = 9599;
    std::cout << 9599;
    for (int i = 0; i < 9599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9600(int& val) {
    const int x = 9600;
    std::cout << 9600;
    for (int i = 0; i < 9600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9601(int& val) {
    const int x = 9601;
    std::cout << 9601;
    for (int i = 0; i < 9601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9602(int& val) {
    const int x = 9602;
    std::cout << 9602;
    for (int i = 0; i < 9602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9603(int& val) {
    const int x = 9603;
    std::cout << 9603;
    for (int i = 0; i < 9603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9604(int& val) {
    const int x = 9604;
    std::cout << 9604;
    for (int i = 0; i < 9604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9605(int& val) {
    const int x = 9605;
    std::cout << 9605;
    for (int i = 0; i < 9605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9606(int& val) {
    const int x = 9606;
    std::cout << 9606;
    for (int i = 0; i < 9606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9607(int& val) {
    const int x = 9607;
    std::cout << 9607;
    for (int i = 0; i < 9607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9608(int& val) {
    const int x = 9608;
    std::cout << 9608;
    for (int i = 0; i < 9608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9609(int& val) {
    const int x = 9609;
    std::cout << 9609;
    for (int i = 0; i < 9609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9610(int& val) {
    const int x = 9610;
    std::cout << 9610;
    for (int i = 0; i < 9610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9611(int& val) {
    const int x = 9611;
    std::cout << 9611;
    for (int i = 0; i < 9611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9612(int& val) {
    const int x = 9612;
    std::cout << 9612;
    for (int i = 0; i < 9612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9613(int& val) {
    const int x = 9613;
    std::cout << 9613;
    for (int i = 0; i < 9613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9614(int& val) {
    const int x = 9614;
    std::cout << 9614;
    for (int i = 0; i < 9614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9615(int& val) {
    const int x = 9615;
    std::cout << 9615;
    for (int i = 0; i < 9615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9616(int& val) {
    const int x = 9616;
    std::cout << 9616;
    for (int i = 0; i < 9616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9617(int& val) {
    const int x = 9617;
    std::cout << 9617;
    for (int i = 0; i < 9617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9618(int& val) {
    const int x = 9618;
    std::cout << 9618;
    for (int i = 0; i < 9618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9619(int& val) {
    const int x = 9619;
    std::cout << 9619;
    for (int i = 0; i < 9619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9620(int& val) {
    const int x = 9620;
    std::cout << 9620;
    for (int i = 0; i < 9620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9621(int& val) {
    const int x = 9621;
    std::cout << 9621;
    for (int i = 0; i < 9621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9622(int& val) {
    const int x = 9622;
    std::cout << 9622;
    for (int i = 0; i < 9622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9623(int& val) {
    const int x = 9623;
    std::cout << 9623;
    for (int i = 0; i < 9623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9624(int& val) {
    const int x = 9624;
    std::cout << 9624;
    for (int i = 0; i < 9624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9625(int& val) {
    const int x = 9625;
    std::cout << 9625;
    for (int i = 0; i < 9625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9626(int& val) {
    const int x = 9626;
    std::cout << 9626;
    for (int i = 0; i < 9626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9627(int& val) {
    const int x = 9627;
    std::cout << 9627;
    for (int i = 0; i < 9627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9628(int& val) {
    const int x = 9628;
    std::cout << 9628;
    for (int i = 0; i < 9628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9629(int& val) {
    const int x = 9629;
    std::cout << 9629;
    for (int i = 0; i < 9629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9630(int& val) {
    const int x = 9630;
    std::cout << 9630;
    for (int i = 0; i < 9630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9631(int& val) {
    const int x = 9631;
    std::cout << 9631;
    for (int i = 0; i < 9631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9632(int& val) {
    const int x = 9632;
    std::cout << 9632;
    for (int i = 0; i < 9632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9633(int& val) {
    const int x = 9633;
    std::cout << 9633;
    for (int i = 0; i < 9633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9634(int& val) {
    const int x = 9634;
    std::cout << 9634;
    for (int i = 0; i < 9634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9635(int& val) {
    const int x = 9635;
    std::cout << 9635;
    for (int i = 0; i < 9635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9636(int& val) {
    const int x = 9636;
    std::cout << 9636;
    for (int i = 0; i < 9636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9637(int& val) {
    const int x = 9637;
    std::cout << 9637;
    for (int i = 0; i < 9637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9638(int& val) {
    const int x = 9638;
    std::cout << 9638;
    for (int i = 0; i < 9638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9639(int& val) {
    const int x = 9639;
    std::cout << 9639;
    for (int i = 0; i < 9639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9640(int& val) {
    const int x = 9640;
    std::cout << 9640;
    for (int i = 0; i < 9640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9641(int& val) {
    const int x = 9641;
    std::cout << 9641;
    for (int i = 0; i < 9641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9642(int& val) {
    const int x = 9642;
    std::cout << 9642;
    for (int i = 0; i < 9642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9643(int& val) {
    const int x = 9643;
    std::cout << 9643;
    for (int i = 0; i < 9643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9644(int& val) {
    const int x = 9644;
    std::cout << 9644;
    for (int i = 0; i < 9644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9645(int& val) {
    const int x = 9645;
    std::cout << 9645;
    for (int i = 0; i < 9645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9646(int& val) {
    const int x = 9646;
    std::cout << 9646;
    for (int i = 0; i < 9646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9647(int& val) {
    const int x = 9647;
    std::cout << 9647;
    for (int i = 0; i < 9647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9648(int& val) {
    const int x = 9648;
    std::cout << 9648;
    for (int i = 0; i < 9648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9649(int& val) {
    const int x = 9649;
    std::cout << 9649;
    for (int i = 0; i < 9649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9650(int& val) {
    const int x = 9650;
    std::cout << 9650;
    for (int i = 0; i < 9650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9651(int& val) {
    const int x = 9651;
    std::cout << 9651;
    for (int i = 0; i < 9651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9652(int& val) {
    const int x = 9652;
    std::cout << 9652;
    for (int i = 0; i < 9652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9653(int& val) {
    const int x = 9653;
    std::cout << 9653;
    for (int i = 0; i < 9653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9654(int& val) {
    const int x = 9654;
    std::cout << 9654;
    for (int i = 0; i < 9654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9655(int& val) {
    const int x = 9655;
    std::cout << 9655;
    for (int i = 0; i < 9655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9656(int& val) {
    const int x = 9656;
    std::cout << 9656;
    for (int i = 0; i < 9656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9657(int& val) {
    const int x = 9657;
    std::cout << 9657;
    for (int i = 0; i < 9657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9658(int& val) {
    const int x = 9658;
    std::cout << 9658;
    for (int i = 0; i < 9658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9659(int& val) {
    const int x = 9659;
    std::cout << 9659;
    for (int i = 0; i < 9659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9660(int& val) {
    const int x = 9660;
    std::cout << 9660;
    for (int i = 0; i < 9660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9661(int& val) {
    const int x = 9661;
    std::cout << 9661;
    for (int i = 0; i < 9661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9662(int& val) {
    const int x = 9662;
    std::cout << 9662;
    for (int i = 0; i < 9662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9663(int& val) {
    const int x = 9663;
    std::cout << 9663;
    for (int i = 0; i < 9663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9664(int& val) {
    const int x = 9664;
    std::cout << 9664;
    for (int i = 0; i < 9664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9665(int& val) {
    const int x = 9665;
    std::cout << 9665;
    for (int i = 0; i < 9665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9666(int& val) {
    const int x = 9666;
    std::cout << 9666;
    for (int i = 0; i < 9666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9667(int& val) {
    const int x = 9667;
    std::cout << 9667;
    for (int i = 0; i < 9667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9668(int& val) {
    const int x = 9668;
    std::cout << 9668;
    for (int i = 0; i < 9668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9669(int& val) {
    const int x = 9669;
    std::cout << 9669;
    for (int i = 0; i < 9669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9670(int& val) {
    const int x = 9670;
    std::cout << 9670;
    for (int i = 0; i < 9670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9671(int& val) {
    const int x = 9671;
    std::cout << 9671;
    for (int i = 0; i < 9671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9672(int& val) {
    const int x = 9672;
    std::cout << 9672;
    for (int i = 0; i < 9672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9673(int& val) {
    const int x = 9673;
    std::cout << 9673;
    for (int i = 0; i < 9673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9674(int& val) {
    const int x = 9674;
    std::cout << 9674;
    for (int i = 0; i < 9674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9675(int& val) {
    const int x = 9675;
    std::cout << 9675;
    for (int i = 0; i < 9675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9676(int& val) {
    const int x = 9676;
    std::cout << 9676;
    for (int i = 0; i < 9676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9677(int& val) {
    const int x = 9677;
    std::cout << 9677;
    for (int i = 0; i < 9677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9678(int& val) {
    const int x = 9678;
    std::cout << 9678;
    for (int i = 0; i < 9678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9679(int& val) {
    const int x = 9679;
    std::cout << 9679;
    for (int i = 0; i < 9679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9680(int& val) {
    const int x = 9680;
    std::cout << 9680;
    for (int i = 0; i < 9680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9681(int& val) {
    const int x = 9681;
    std::cout << 9681;
    for (int i = 0; i < 9681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9682(int& val) {
    const int x = 9682;
    std::cout << 9682;
    for (int i = 0; i < 9682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9683(int& val) {
    const int x = 9683;
    std::cout << 9683;
    for (int i = 0; i < 9683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9684(int& val) {
    const int x = 9684;
    std::cout << 9684;
    for (int i = 0; i < 9684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9685(int& val) {
    const int x = 9685;
    std::cout << 9685;
    for (int i = 0; i < 9685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9686(int& val) {
    const int x = 9686;
    std::cout << 9686;
    for (int i = 0; i < 9686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9687(int& val) {
    const int x = 9687;
    std::cout << 9687;
    for (int i = 0; i < 9687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9688(int& val) {
    const int x = 9688;
    std::cout << 9688;
    for (int i = 0; i < 9688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9689(int& val) {
    const int x = 9689;
    std::cout << 9689;
    for (int i = 0; i < 9689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9690(int& val) {
    const int x = 9690;
    std::cout << 9690;
    for (int i = 0; i < 9690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9691(int& val) {
    const int x = 9691;
    std::cout << 9691;
    for (int i = 0; i < 9691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9692(int& val) {
    const int x = 9692;
    std::cout << 9692;
    for (int i = 0; i < 9692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9693(int& val) {
    const int x = 9693;
    std::cout << 9693;
    for (int i = 0; i < 9693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9694(int& val) {
    const int x = 9694;
    std::cout << 9694;
    for (int i = 0; i < 9694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9695(int& val) {
    const int x = 9695;
    std::cout << 9695;
    for (int i = 0; i < 9695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9696(int& val) {
    const int x = 9696;
    std::cout << 9696;
    for (int i = 0; i < 9696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9697(int& val) {
    const int x = 9697;
    std::cout << 9697;
    for (int i = 0; i < 9697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9698(int& val) {
    const int x = 9698;
    std::cout << 9698;
    for (int i = 0; i < 9698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9699(int& val) {
    const int x = 9699;
    std::cout << 9699;
    for (int i = 0; i < 9699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9700(int& val) {
    const int x = 9700;
    std::cout << 9700;
    for (int i = 0; i < 9700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9701(int& val) {
    const int x = 9701;
    std::cout << 9701;
    for (int i = 0; i < 9701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9702(int& val) {
    const int x = 9702;
    std::cout << 9702;
    for (int i = 0; i < 9702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9703(int& val) {
    const int x = 9703;
    std::cout << 9703;
    for (int i = 0; i < 9703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9704(int& val) {
    const int x = 9704;
    std::cout << 9704;
    for (int i = 0; i < 9704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9705(int& val) {
    const int x = 9705;
    std::cout << 9705;
    for (int i = 0; i < 9705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9706(int& val) {
    const int x = 9706;
    std::cout << 9706;
    for (int i = 0; i < 9706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9707(int& val) {
    const int x = 9707;
    std::cout << 9707;
    for (int i = 0; i < 9707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9708(int& val) {
    const int x = 9708;
    std::cout << 9708;
    for (int i = 0; i < 9708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9709(int& val) {
    const int x = 9709;
    std::cout << 9709;
    for (int i = 0; i < 9709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9710(int& val) {
    const int x = 9710;
    std::cout << 9710;
    for (int i = 0; i < 9710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9711(int& val) {
    const int x = 9711;
    std::cout << 9711;
    for (int i = 0; i < 9711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9712(int& val) {
    const int x = 9712;
    std::cout << 9712;
    for (int i = 0; i < 9712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9713(int& val) {
    const int x = 9713;
    std::cout << 9713;
    for (int i = 0; i < 9713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9714(int& val) {
    const int x = 9714;
    std::cout << 9714;
    for (int i = 0; i < 9714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9715(int& val) {
    const int x = 9715;
    std::cout << 9715;
    for (int i = 0; i < 9715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9716(int& val) {
    const int x = 9716;
    std::cout << 9716;
    for (int i = 0; i < 9716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9717(int& val) {
    const int x = 9717;
    std::cout << 9717;
    for (int i = 0; i < 9717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9718(int& val) {
    const int x = 9718;
    std::cout << 9718;
    for (int i = 0; i < 9718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9719(int& val) {
    const int x = 9719;
    std::cout << 9719;
    for (int i = 0; i < 9719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9720(int& val) {
    const int x = 9720;
    std::cout << 9720;
    for (int i = 0; i < 9720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9721(int& val) {
    const int x = 9721;
    std::cout << 9721;
    for (int i = 0; i < 9721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9722(int& val) {
    const int x = 9722;
    std::cout << 9722;
    for (int i = 0; i < 9722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9723(int& val) {
    const int x = 9723;
    std::cout << 9723;
    for (int i = 0; i < 9723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9724(int& val) {
    const int x = 9724;
    std::cout << 9724;
    for (int i = 0; i < 9724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9725(int& val) {
    const int x = 9725;
    std::cout << 9725;
    for (int i = 0; i < 9725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9726(int& val) {
    const int x = 9726;
    std::cout << 9726;
    for (int i = 0; i < 9726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9727(int& val) {
    const int x = 9727;
    std::cout << 9727;
    for (int i = 0; i < 9727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9728(int& val) {
    const int x = 9728;
    std::cout << 9728;
    for (int i = 0; i < 9728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9729(int& val) {
    const int x = 9729;
    std::cout << 9729;
    for (int i = 0; i < 9729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9730(int& val) {
    const int x = 9730;
    std::cout << 9730;
    for (int i = 0; i < 9730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9731(int& val) {
    const int x = 9731;
    std::cout << 9731;
    for (int i = 0; i < 9731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9732(int& val) {
    const int x = 9732;
    std::cout << 9732;
    for (int i = 0; i < 9732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9733(int& val) {
    const int x = 9733;
    std::cout << 9733;
    for (int i = 0; i < 9733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9734(int& val) {
    const int x = 9734;
    std::cout << 9734;
    for (int i = 0; i < 9734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9735(int& val) {
    const int x = 9735;
    std::cout << 9735;
    for (int i = 0; i < 9735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9736(int& val) {
    const int x = 9736;
    std::cout << 9736;
    for (int i = 0; i < 9736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9737(int& val) {
    const int x = 9737;
    std::cout << 9737;
    for (int i = 0; i < 9737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9738(int& val) {
    const int x = 9738;
    std::cout << 9738;
    for (int i = 0; i < 9738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9739(int& val) {
    const int x = 9739;
    std::cout << 9739;
    for (int i = 0; i < 9739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9740(int& val) {
    const int x = 9740;
    std::cout << 9740;
    for (int i = 0; i < 9740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9741(int& val) {
    const int x = 9741;
    std::cout << 9741;
    for (int i = 0; i < 9741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9742(int& val) {
    const int x = 9742;
    std::cout << 9742;
    for (int i = 0; i < 9742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9743(int& val) {
    const int x = 9743;
    std::cout << 9743;
    for (int i = 0; i < 9743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9744(int& val) {
    const int x = 9744;
    std::cout << 9744;
    for (int i = 0; i < 9744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9745(int& val) {
    const int x = 9745;
    std::cout << 9745;
    for (int i = 0; i < 9745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9746(int& val) {
    const int x = 9746;
    std::cout << 9746;
    for (int i = 0; i < 9746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9747(int& val) {
    const int x = 9747;
    std::cout << 9747;
    for (int i = 0; i < 9747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9748(int& val) {
    const int x = 9748;
    std::cout << 9748;
    for (int i = 0; i < 9748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9749(int& val) {
    const int x = 9749;
    std::cout << 9749;
    for (int i = 0; i < 9749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9750(int& val) {
    const int x = 9750;
    std::cout << 9750;
    for (int i = 0; i < 9750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9751(int& val) {
    const int x = 9751;
    std::cout << 9751;
    for (int i = 0; i < 9751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9752(int& val) {
    const int x = 9752;
    std::cout << 9752;
    for (int i = 0; i < 9752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9753(int& val) {
    const int x = 9753;
    std::cout << 9753;
    for (int i = 0; i < 9753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9754(int& val) {
    const int x = 9754;
    std::cout << 9754;
    for (int i = 0; i < 9754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9755(int& val) {
    const int x = 9755;
    std::cout << 9755;
    for (int i = 0; i < 9755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9756(int& val) {
    const int x = 9756;
    std::cout << 9756;
    for (int i = 0; i < 9756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9757(int& val) {
    const int x = 9757;
    std::cout << 9757;
    for (int i = 0; i < 9757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9758(int& val) {
    const int x = 9758;
    std::cout << 9758;
    for (int i = 0; i < 9758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9759(int& val) {
    const int x = 9759;
    std::cout << 9759;
    for (int i = 0; i < 9759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9760(int& val) {
    const int x = 9760;
    std::cout << 9760;
    for (int i = 0; i < 9760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9761(int& val) {
    const int x = 9761;
    std::cout << 9761;
    for (int i = 0; i < 9761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9762(int& val) {
    const int x = 9762;
    std::cout << 9762;
    for (int i = 0; i < 9762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9763(int& val) {
    const int x = 9763;
    std::cout << 9763;
    for (int i = 0; i < 9763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9764(int& val) {
    const int x = 9764;
    std::cout << 9764;
    for (int i = 0; i < 9764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9765(int& val) {
    const int x = 9765;
    std::cout << 9765;
    for (int i = 0; i < 9765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9766(int& val) {
    const int x = 9766;
    std::cout << 9766;
    for (int i = 0; i < 9766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9767(int& val) {
    const int x = 9767;
    std::cout << 9767;
    for (int i = 0; i < 9767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9768(int& val) {
    const int x = 9768;
    std::cout << 9768;
    for (int i = 0; i < 9768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9769(int& val) {
    const int x = 9769;
    std::cout << 9769;
    for (int i = 0; i < 9769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9770(int& val) {
    const int x = 9770;
    std::cout << 9770;
    for (int i = 0; i < 9770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9771(int& val) {
    const int x = 9771;
    std::cout << 9771;
    for (int i = 0; i < 9771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9772(int& val) {
    const int x = 9772;
    std::cout << 9772;
    for (int i = 0; i < 9772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9773(int& val) {
    const int x = 9773;
    std::cout << 9773;
    for (int i = 0; i < 9773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9774(int& val) {
    const int x = 9774;
    std::cout << 9774;
    for (int i = 0; i < 9774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9775(int& val) {
    const int x = 9775;
    std::cout << 9775;
    for (int i = 0; i < 9775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9776(int& val) {
    const int x = 9776;
    std::cout << 9776;
    for (int i = 0; i < 9776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9777(int& val) {
    const int x = 9777;
    std::cout << 9777;
    for (int i = 0; i < 9777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9778(int& val) {
    const int x = 9778;
    std::cout << 9778;
    for (int i = 0; i < 9778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9779(int& val) {
    const int x = 9779;
    std::cout << 9779;
    for (int i = 0; i < 9779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9780(int& val) {
    const int x = 9780;
    std::cout << 9780;
    for (int i = 0; i < 9780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9781(int& val) {
    const int x = 9781;
    std::cout << 9781;
    for (int i = 0; i < 9781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9782(int& val) {
    const int x = 9782;
    std::cout << 9782;
    for (int i = 0; i < 9782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9783(int& val) {
    const int x = 9783;
    std::cout << 9783;
    for (int i = 0; i < 9783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9784(int& val) {
    const int x = 9784;
    std::cout << 9784;
    for (int i = 0; i < 9784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9785(int& val) {
    const int x = 9785;
    std::cout << 9785;
    for (int i = 0; i < 9785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9786(int& val) {
    const int x = 9786;
    std::cout << 9786;
    for (int i = 0; i < 9786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9787(int& val) {
    const int x = 9787;
    std::cout << 9787;
    for (int i = 0; i < 9787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9788(int& val) {
    const int x = 9788;
    std::cout << 9788;
    for (int i = 0; i < 9788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9789(int& val) {
    const int x = 9789;
    std::cout << 9789;
    for (int i = 0; i < 9789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9790(int& val) {
    const int x = 9790;
    std::cout << 9790;
    for (int i = 0; i < 9790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9791(int& val) {
    const int x = 9791;
    std::cout << 9791;
    for (int i = 0; i < 9791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9792(int& val) {
    const int x = 9792;
    std::cout << 9792;
    for (int i = 0; i < 9792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9793(int& val) {
    const int x = 9793;
    std::cout << 9793;
    for (int i = 0; i < 9793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9794(int& val) {
    const int x = 9794;
    std::cout << 9794;
    for (int i = 0; i < 9794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9795(int& val) {
    const int x = 9795;
    std::cout << 9795;
    for (int i = 0; i < 9795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9796(int& val) {
    const int x = 9796;
    std::cout << 9796;
    for (int i = 0; i < 9796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9797(int& val) {
    const int x = 9797;
    std::cout << 9797;
    for (int i = 0; i < 9797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9798(int& val) {
    const int x = 9798;
    std::cout << 9798;
    for (int i = 0; i < 9798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9799(int& val) {
    const int x = 9799;
    std::cout << 9799;
    for (int i = 0; i < 9799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9800(int& val) {
    const int x = 9800;
    std::cout << 9800;
    for (int i = 0; i < 9800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9801(int& val) {
    const int x = 9801;
    std::cout << 9801;
    for (int i = 0; i < 9801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9802(int& val) {
    const int x = 9802;
    std::cout << 9802;
    for (int i = 0; i < 9802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9803(int& val) {
    const int x = 9803;
    std::cout << 9803;
    for (int i = 0; i < 9803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9804(int& val) {
    const int x = 9804;
    std::cout << 9804;
    for (int i = 0; i < 9804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9805(int& val) {
    const int x = 9805;
    std::cout << 9805;
    for (int i = 0; i < 9805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9806(int& val) {
    const int x = 9806;
    std::cout << 9806;
    for (int i = 0; i < 9806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9807(int& val) {
    const int x = 9807;
    std::cout << 9807;
    for (int i = 0; i < 9807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9808(int& val) {
    const int x = 9808;
    std::cout << 9808;
    for (int i = 0; i < 9808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9809(int& val) {
    const int x = 9809;
    std::cout << 9809;
    for (int i = 0; i < 9809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9810(int& val) {
    const int x = 9810;
    std::cout << 9810;
    for (int i = 0; i < 9810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9811(int& val) {
    const int x = 9811;
    std::cout << 9811;
    for (int i = 0; i < 9811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9812(int& val) {
    const int x = 9812;
    std::cout << 9812;
    for (int i = 0; i < 9812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9813(int& val) {
    const int x = 9813;
    std::cout << 9813;
    for (int i = 0; i < 9813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9814(int& val) {
    const int x = 9814;
    std::cout << 9814;
    for (int i = 0; i < 9814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9815(int& val) {
    const int x = 9815;
    std::cout << 9815;
    for (int i = 0; i < 9815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9816(int& val) {
    const int x = 9816;
    std::cout << 9816;
    for (int i = 0; i < 9816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9817(int& val) {
    const int x = 9817;
    std::cout << 9817;
    for (int i = 0; i < 9817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9818(int& val) {
    const int x = 9818;
    std::cout << 9818;
    for (int i = 0; i < 9818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9819(int& val) {
    const int x = 9819;
    std::cout << 9819;
    for (int i = 0; i < 9819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9820(int& val) {
    const int x = 9820;
    std::cout << 9820;
    for (int i = 0; i < 9820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9821(int& val) {
    const int x = 9821;
    std::cout << 9821;
    for (int i = 0; i < 9821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9822(int& val) {
    const int x = 9822;
    std::cout << 9822;
    for (int i = 0; i < 9822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9823(int& val) {
    const int x = 9823;
    std::cout << 9823;
    for (int i = 0; i < 9823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9824(int& val) {
    const int x = 9824;
    std::cout << 9824;
    for (int i = 0; i < 9824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9825(int& val) {
    const int x = 9825;
    std::cout << 9825;
    for (int i = 0; i < 9825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9826(int& val) {
    const int x = 9826;
    std::cout << 9826;
    for (int i = 0; i < 9826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9827(int& val) {
    const int x = 9827;
    std::cout << 9827;
    for (int i = 0; i < 9827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9828(int& val) {
    const int x = 9828;
    std::cout << 9828;
    for (int i = 0; i < 9828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9829(int& val) {
    const int x = 9829;
    std::cout << 9829;
    for (int i = 0; i < 9829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9830(int& val) {
    const int x = 9830;
    std::cout << 9830;
    for (int i = 0; i < 9830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9831(int& val) {
    const int x = 9831;
    std::cout << 9831;
    for (int i = 0; i < 9831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9832(int& val) {
    const int x = 9832;
    std::cout << 9832;
    for (int i = 0; i < 9832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9833(int& val) {
    const int x = 9833;
    std::cout << 9833;
    for (int i = 0; i < 9833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9834(int& val) {
    const int x = 9834;
    std::cout << 9834;
    for (int i = 0; i < 9834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9835(int& val) {
    const int x = 9835;
    std::cout << 9835;
    for (int i = 0; i < 9835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9836(int& val) {
    const int x = 9836;
    std::cout << 9836;
    for (int i = 0; i < 9836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9837(int& val) {
    const int x = 9837;
    std::cout << 9837;
    for (int i = 0; i < 9837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9838(int& val) {
    const int x = 9838;
    std::cout << 9838;
    for (int i = 0; i < 9838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9839(int& val) {
    const int x = 9839;
    std::cout << 9839;
    for (int i = 0; i < 9839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9840(int& val) {
    const int x = 9840;
    std::cout << 9840;
    for (int i = 0; i < 9840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9841(int& val) {
    const int x = 9841;
    std::cout << 9841;
    for (int i = 0; i < 9841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9842(int& val) {
    const int x = 9842;
    std::cout << 9842;
    for (int i = 0; i < 9842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9843(int& val) {
    const int x = 9843;
    std::cout << 9843;
    for (int i = 0; i < 9843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9844(int& val) {
    const int x = 9844;
    std::cout << 9844;
    for (int i = 0; i < 9844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9845(int& val) {
    const int x = 9845;
    std::cout << 9845;
    for (int i = 0; i < 9845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9846(int& val) {
    const int x = 9846;
    std::cout << 9846;
    for (int i = 0; i < 9846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9847(int& val) {
    const int x = 9847;
    std::cout << 9847;
    for (int i = 0; i < 9847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9848(int& val) {
    const int x = 9848;
    std::cout << 9848;
    for (int i = 0; i < 9848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9849(int& val) {
    const int x = 9849;
    std::cout << 9849;
    for (int i = 0; i < 9849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9850(int& val) {
    const int x = 9850;
    std::cout << 9850;
    for (int i = 0; i < 9850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9851(int& val) {
    const int x = 9851;
    std::cout << 9851;
    for (int i = 0; i < 9851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9852(int& val) {
    const int x = 9852;
    std::cout << 9852;
    for (int i = 0; i < 9852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9853(int& val) {
    const int x = 9853;
    std::cout << 9853;
    for (int i = 0; i < 9853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9854(int& val) {
    const int x = 9854;
    std::cout << 9854;
    for (int i = 0; i < 9854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9855(int& val) {
    const int x = 9855;
    std::cout << 9855;
    for (int i = 0; i < 9855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9856(int& val) {
    const int x = 9856;
    std::cout << 9856;
    for (int i = 0; i < 9856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9857(int& val) {
    const int x = 9857;
    std::cout << 9857;
    for (int i = 0; i < 9857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9858(int& val) {
    const int x = 9858;
    std::cout << 9858;
    for (int i = 0; i < 9858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9859(int& val) {
    const int x = 9859;
    std::cout << 9859;
    for (int i = 0; i < 9859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9860(int& val) {
    const int x = 9860;
    std::cout << 9860;
    for (int i = 0; i < 9860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9861(int& val) {
    const int x = 9861;
    std::cout << 9861;
    for (int i = 0; i < 9861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9862(int& val) {
    const int x = 9862;
    std::cout << 9862;
    for (int i = 0; i < 9862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9863(int& val) {
    const int x = 9863;
    std::cout << 9863;
    for (int i = 0; i < 9863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9864(int& val) {
    const int x = 9864;
    std::cout << 9864;
    for (int i = 0; i < 9864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9865(int& val) {
    const int x = 9865;
    std::cout << 9865;
    for (int i = 0; i < 9865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9866(int& val) {
    const int x = 9866;
    std::cout << 9866;
    for (int i = 0; i < 9866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9867(int& val) {
    const int x = 9867;
    std::cout << 9867;
    for (int i = 0; i < 9867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9868(int& val) {
    const int x = 9868;
    std::cout << 9868;
    for (int i = 0; i < 9868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9869(int& val) {
    const int x = 9869;
    std::cout << 9869;
    for (int i = 0; i < 9869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9870(int& val) {
    const int x = 9870;
    std::cout << 9870;
    for (int i = 0; i < 9870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9871(int& val) {
    const int x = 9871;
    std::cout << 9871;
    for (int i = 0; i < 9871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9872(int& val) {
    const int x = 9872;
    std::cout << 9872;
    for (int i = 0; i < 9872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9873(int& val) {
    const int x = 9873;
    std::cout << 9873;
    for (int i = 0; i < 9873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9874(int& val) {
    const int x = 9874;
    std::cout << 9874;
    for (int i = 0; i < 9874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9875(int& val) {
    const int x = 9875;
    std::cout << 9875;
    for (int i = 0; i < 9875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9876(int& val) {
    const int x = 9876;
    std::cout << 9876;
    for (int i = 0; i < 9876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9877(int& val) {
    const int x = 9877;
    std::cout << 9877;
    for (int i = 0; i < 9877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9878(int& val) {
    const int x = 9878;
    std::cout << 9878;
    for (int i = 0; i < 9878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9879(int& val) {
    const int x = 9879;
    std::cout << 9879;
    for (int i = 0; i < 9879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9880(int& val) {
    const int x = 9880;
    std::cout << 9880;
    for (int i = 0; i < 9880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9881(int& val) {
    const int x = 9881;
    std::cout << 9881;
    for (int i = 0; i < 9881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9882(int& val) {
    const int x = 9882;
    std::cout << 9882;
    for (int i = 0; i < 9882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9883(int& val) {
    const int x = 9883;
    std::cout << 9883;
    for (int i = 0; i < 9883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9884(int& val) {
    const int x = 9884;
    std::cout << 9884;
    for (int i = 0; i < 9884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9885(int& val) {
    const int x = 9885;
    std::cout << 9885;
    for (int i = 0; i < 9885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9886(int& val) {
    const int x = 9886;
    std::cout << 9886;
    for (int i = 0; i < 9886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9887(int& val) {
    const int x = 9887;
    std::cout << 9887;
    for (int i = 0; i < 9887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9888(int& val) {
    const int x = 9888;
    std::cout << 9888;
    for (int i = 0; i < 9888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9889(int& val) {
    const int x = 9889;
    std::cout << 9889;
    for (int i = 0; i < 9889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9890(int& val) {
    const int x = 9890;
    std::cout << 9890;
    for (int i = 0; i < 9890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9891(int& val) {
    const int x = 9891;
    std::cout << 9891;
    for (int i = 0; i < 9891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9892(int& val) {
    const int x = 9892;
    std::cout << 9892;
    for (int i = 0; i < 9892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9893(int& val) {
    const int x = 9893;
    std::cout << 9893;
    for (int i = 0; i < 9893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9894(int& val) {
    const int x = 9894;
    std::cout << 9894;
    for (int i = 0; i < 9894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9895(int& val) {
    const int x = 9895;
    std::cout << 9895;
    for (int i = 0; i < 9895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9896(int& val) {
    const int x = 9896;
    std::cout << 9896;
    for (int i = 0; i < 9896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9897(int& val) {
    const int x = 9897;
    std::cout << 9897;
    for (int i = 0; i < 9897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9898(int& val) {
    const int x = 9898;
    std::cout << 9898;
    for (int i = 0; i < 9898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9899(int& val) {
    const int x = 9899;
    std::cout << 9899;
    for (int i = 0; i < 9899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9900(int& val) {
    const int x = 9900;
    std::cout << 9900;
    for (int i = 0; i < 9900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9901(int& val) {
    const int x = 9901;
    std::cout << 9901;
    for (int i = 0; i < 9901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9902(int& val) {
    const int x = 9902;
    std::cout << 9902;
    for (int i = 0; i < 9902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9903(int& val) {
    const int x = 9903;
    std::cout << 9903;
    for (int i = 0; i < 9903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9904(int& val) {
    const int x = 9904;
    std::cout << 9904;
    for (int i = 0; i < 9904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9905(int& val) {
    const int x = 9905;
    std::cout << 9905;
    for (int i = 0; i < 9905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9906(int& val) {
    const int x = 9906;
    std::cout << 9906;
    for (int i = 0; i < 9906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9907(int& val) {
    const int x = 9907;
    std::cout << 9907;
    for (int i = 0; i < 9907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9908(int& val) {
    const int x = 9908;
    std::cout << 9908;
    for (int i = 0; i < 9908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9909(int& val) {
    const int x = 9909;
    std::cout << 9909;
    for (int i = 0; i < 9909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9910(int& val) {
    const int x = 9910;
    std::cout << 9910;
    for (int i = 0; i < 9910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9911(int& val) {
    const int x = 9911;
    std::cout << 9911;
    for (int i = 0; i < 9911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9912(int& val) {
    const int x = 9912;
    std::cout << 9912;
    for (int i = 0; i < 9912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9913(int& val) {
    const int x = 9913;
    std::cout << 9913;
    for (int i = 0; i < 9913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9914(int& val) {
    const int x = 9914;
    std::cout << 9914;
    for (int i = 0; i < 9914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9915(int& val) {
    const int x = 9915;
    std::cout << 9915;
    for (int i = 0; i < 9915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9916(int& val) {
    const int x = 9916;
    std::cout << 9916;
    for (int i = 0; i < 9916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9917(int& val) {
    const int x = 9917;
    std::cout << 9917;
    for (int i = 0; i < 9917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9918(int& val) {
    const int x = 9918;
    std::cout << 9918;
    for (int i = 0; i < 9918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9919(int& val) {
    const int x = 9919;
    std::cout << 9919;
    for (int i = 0; i < 9919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9920(int& val) {
    const int x = 9920;
    std::cout << 9920;
    for (int i = 0; i < 9920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9921(int& val) {
    const int x = 9921;
    std::cout << 9921;
    for (int i = 0; i < 9921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9922(int& val) {
    const int x = 9922;
    std::cout << 9922;
    for (int i = 0; i < 9922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9923(int& val) {
    const int x = 9923;
    std::cout << 9923;
    for (int i = 0; i < 9923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9924(int& val) {
    const int x = 9924;
    std::cout << 9924;
    for (int i = 0; i < 9924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9925(int& val) {
    const int x = 9925;
    std::cout << 9925;
    for (int i = 0; i < 9925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9926(int& val) {
    const int x = 9926;
    std::cout << 9926;
    for (int i = 0; i < 9926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9927(int& val) {
    const int x = 9927;
    std::cout << 9927;
    for (int i = 0; i < 9927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9928(int& val) {
    const int x = 9928;
    std::cout << 9928;
    for (int i = 0; i < 9928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9929(int& val) {
    const int x = 9929;
    std::cout << 9929;
    for (int i = 0; i < 9929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9930(int& val) {
    const int x = 9930;
    std::cout << 9930;
    for (int i = 0; i < 9930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9931(int& val) {
    const int x = 9931;
    std::cout << 9931;
    for (int i = 0; i < 9931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9932(int& val) {
    const int x = 9932;
    std::cout << 9932;
    for (int i = 0; i < 9932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9933(int& val) {
    const int x = 9933;
    std::cout << 9933;
    for (int i = 0; i < 9933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9934(int& val) {
    const int x = 9934;
    std::cout << 9934;
    for (int i = 0; i < 9934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9935(int& val) {
    const int x = 9935;
    std::cout << 9935;
    for (int i = 0; i < 9935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9936(int& val) {
    const int x = 9936;
    std::cout << 9936;
    for (int i = 0; i < 9936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9937(int& val) {
    const int x = 9937;
    std::cout << 9937;
    for (int i = 0; i < 9937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9938(int& val) {
    const int x = 9938;
    std::cout << 9938;
    for (int i = 0; i < 9938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9939(int& val) {
    const int x = 9939;
    std::cout << 9939;
    for (int i = 0; i < 9939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9940(int& val) {
    const int x = 9940;
    std::cout << 9940;
    for (int i = 0; i < 9940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9941(int& val) {
    const int x = 9941;
    std::cout << 9941;
    for (int i = 0; i < 9941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9942(int& val) {
    const int x = 9942;
    std::cout << 9942;
    for (int i = 0; i < 9942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9943(int& val) {
    const int x = 9943;
    std::cout << 9943;
    for (int i = 0; i < 9943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9944(int& val) {
    const int x = 9944;
    std::cout << 9944;
    for (int i = 0; i < 9944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9945(int& val) {
    const int x = 9945;
    std::cout << 9945;
    for (int i = 0; i < 9945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9946(int& val) {
    const int x = 9946;
    std::cout << 9946;
    for (int i = 0; i < 9946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9947(int& val) {
    const int x = 9947;
    std::cout << 9947;
    for (int i = 0; i < 9947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9948(int& val) {
    const int x = 9948;
    std::cout << 9948;
    for (int i = 0; i < 9948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9949(int& val) {
    const int x = 9949;
    std::cout << 9949;
    for (int i = 0; i < 9949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9950(int& val) {
    const int x = 9950;
    std::cout << 9950;
    for (int i = 0; i < 9950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9951(int& val) {
    const int x = 9951;
    std::cout << 9951;
    for (int i = 0; i < 9951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9952(int& val) {
    const int x = 9952;
    std::cout << 9952;
    for (int i = 0; i < 9952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9953(int& val) {
    const int x = 9953;
    std::cout << 9953;
    for (int i = 0; i < 9953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9954(int& val) {
    const int x = 9954;
    std::cout << 9954;
    for (int i = 0; i < 9954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9955(int& val) {
    const int x = 9955;
    std::cout << 9955;
    for (int i = 0; i < 9955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9956(int& val) {
    const int x = 9956;
    std::cout << 9956;
    for (int i = 0; i < 9956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9957(int& val) {
    const int x = 9957;
    std::cout << 9957;
    for (int i = 0; i < 9957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9958(int& val) {
    const int x = 9958;
    std::cout << 9958;
    for (int i = 0; i < 9958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9959(int& val) {
    const int x = 9959;
    std::cout << 9959;
    for (int i = 0; i < 9959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9960(int& val) {
    const int x = 9960;
    std::cout << 9960;
    for (int i = 0; i < 9960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9961(int& val) {
    const int x = 9961;
    std::cout << 9961;
    for (int i = 0; i < 9961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9962(int& val) {
    const int x = 9962;
    std::cout << 9962;
    for (int i = 0; i < 9962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9963(int& val) {
    const int x = 9963;
    std::cout << 9963;
    for (int i = 0; i < 9963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9964(int& val) {
    const int x = 9964;
    std::cout << 9964;
    for (int i = 0; i < 9964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9965(int& val) {
    const int x = 9965;
    std::cout << 9965;
    for (int i = 0; i < 9965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9966(int& val) {
    const int x = 9966;
    std::cout << 9966;
    for (int i = 0; i < 9966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9967(int& val) {
    const int x = 9967;
    std::cout << 9967;
    for (int i = 0; i < 9967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9968(int& val) {
    const int x = 9968;
    std::cout << 9968;
    for (int i = 0; i < 9968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9969(int& val) {
    const int x = 9969;
    std::cout << 9969;
    for (int i = 0; i < 9969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9970(int& val) {
    const int x = 9970;
    std::cout << 9970;
    for (int i = 0; i < 9970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9971(int& val) {
    const int x = 9971;
    std::cout << 9971;
    for (int i = 0; i < 9971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9972(int& val) {
    const int x = 9972;
    std::cout << 9972;
    for (int i = 0; i < 9972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9973(int& val) {
    const int x = 9973;
    std::cout << 9973;
    for (int i = 0; i < 9973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9974(int& val) {
    const int x = 9974;
    std::cout << 9974;
    for (int i = 0; i < 9974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9975(int& val) {
    const int x = 9975;
    std::cout << 9975;
    for (int i = 0; i < 9975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9976(int& val) {
    const int x = 9976;
    std::cout << 9976;
    for (int i = 0; i < 9976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9977(int& val) {
    const int x = 9977;
    std::cout << 9977;
    for (int i = 0; i < 9977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9978(int& val) {
    const int x = 9978;
    std::cout << 9978;
    for (int i = 0; i < 9978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9979(int& val) {
    const int x = 9979;
    std::cout << 9979;
    for (int i = 0; i < 9979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9980(int& val) {
    const int x = 9980;
    std::cout << 9980;
    for (int i = 0; i < 9980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9981(int& val) {
    const int x = 9981;
    std::cout << 9981;
    for (int i = 0; i < 9981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9982(int& val) {
    const int x = 9982;
    std::cout << 9982;
    for (int i = 0; i < 9982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9983(int& val) {
    const int x = 9983;
    std::cout << 9983;
    for (int i = 0; i < 9983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9984(int& val) {
    const int x = 9984;
    std::cout << 9984;
    for (int i = 0; i < 9984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9985(int& val) {
    const int x = 9985;
    std::cout << 9985;
    for (int i = 0; i < 9985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9986(int& val) {
    const int x = 9986;
    std::cout << 9986;
    for (int i = 0; i < 9986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9987(int& val) {
    const int x = 9987;
    std::cout << 9987;
    for (int i = 0; i < 9987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9988(int& val) {
    const int x = 9988;
    std::cout << 9988;
    for (int i = 0; i < 9988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9989(int& val) {
    const int x = 9989;
    std::cout << 9989;
    for (int i = 0; i < 9989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9990(int& val) {
    const int x = 9990;
    std::cout << 9990;
    for (int i = 0; i < 9990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9991(int& val) {
    const int x = 9991;
    std::cout << 9991;
    for (int i = 0; i < 9991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9992(int& val) {
    const int x = 9992;
    std::cout << 9992;
    for (int i = 0; i < 9992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9993(int& val) {
    const int x = 9993;
    std::cout << 9993;
    for (int i = 0; i < 9993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9994(int& val) {
    const int x = 9994;
    std::cout << 9994;
    for (int i = 0; i < 9994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9995(int& val) {
    const int x = 9995;
    std::cout << 9995;
    for (int i = 0; i < 9995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9996(int& val) {
    const int x = 9996;
    std::cout << 9996;
    for (int i = 0; i < 9996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9997(int& val) {
    const int x = 9997;
    std::cout << 9997;
    for (int i = 0; i < 9997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9998(int& val) {
    const int x = 9998;
    std::cout << 9998;
    for (int i = 0; i < 9998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9999(int& val) {
    const int x = 9999;
    std::cout << 9999;
    for (int i = 0; i < 9999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

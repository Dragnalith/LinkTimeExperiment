#include "lib.h"
#include <iostream>
int func_4000(int& val) {
    func_in_4000(val);
    const int x = 4000;
    std::cout << 4000;
    for (int i = 0; i < 4000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4000(int& val) {
    const int x = 4000;
    std::cout << 4000;
    for (int i = 0; i < 4000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4001(int& val) {
    func_in_4001(val);
    const int x = 4001;
    std::cout << 4001;
    for (int i = 0; i < 4001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4001(int& val) {
    const int x = 4001;
    std::cout << 4001;
    for (int i = 0; i < 4001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4002(int& val) {
    func_in_4002(val);
    const int x = 4002;
    std::cout << 4002;
    for (int i = 0; i < 4002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4002(int& val) {
    const int x = 4002;
    std::cout << 4002;
    for (int i = 0; i < 4002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4003(int& val) {
    func_in_4003(val);
    const int x = 4003;
    std::cout << 4003;
    for (int i = 0; i < 4003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4003(int& val) {
    const int x = 4003;
    std::cout << 4003;
    for (int i = 0; i < 4003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4004(int& val) {
    func_in_4004(val);
    const int x = 4004;
    std::cout << 4004;
    for (int i = 0; i < 4004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4004(int& val) {
    const int x = 4004;
    std::cout << 4004;
    for (int i = 0; i < 4004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4005(int& val) {
    func_in_4005(val);
    const int x = 4005;
    std::cout << 4005;
    for (int i = 0; i < 4005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4005(int& val) {
    const int x = 4005;
    std::cout << 4005;
    for (int i = 0; i < 4005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4006(int& val) {
    func_in_4006(val);
    const int x = 4006;
    std::cout << 4006;
    for (int i = 0; i < 4006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4006(int& val) {
    const int x = 4006;
    std::cout << 4006;
    for (int i = 0; i < 4006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4007(int& val) {
    func_in_4007(val);
    const int x = 4007;
    std::cout << 4007;
    for (int i = 0; i < 4007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4007(int& val) {
    const int x = 4007;
    std::cout << 4007;
    for (int i = 0; i < 4007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4008(int& val) {
    func_in_4008(val);
    const int x = 4008;
    std::cout << 4008;
    for (int i = 0; i < 4008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4008(int& val) {
    const int x = 4008;
    std::cout << 4008;
    for (int i = 0; i < 4008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4009(int& val) {
    func_in_4009(val);
    const int x = 4009;
    std::cout << 4009;
    for (int i = 0; i < 4009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4009(int& val) {
    const int x = 4009;
    std::cout << 4009;
    for (int i = 0; i < 4009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4010(int& val) {
    func_in_4010(val);
    const int x = 4010;
    std::cout << 4010;
    for (int i = 0; i < 4010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4010(int& val) {
    const int x = 4010;
    std::cout << 4010;
    for (int i = 0; i < 4010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4011(int& val) {
    func_in_4011(val);
    const int x = 4011;
    std::cout << 4011;
    for (int i = 0; i < 4011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4011(int& val) {
    const int x = 4011;
    std::cout << 4011;
    for (int i = 0; i < 4011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4012(int& val) {
    func_in_4012(val);
    const int x = 4012;
    std::cout << 4012;
    for (int i = 0; i < 4012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4012(int& val) {
    const int x = 4012;
    std::cout << 4012;
    for (int i = 0; i < 4012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4013(int& val) {
    func_in_4013(val);
    const int x = 4013;
    std::cout << 4013;
    for (int i = 0; i < 4013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4013(int& val) {
    const int x = 4013;
    std::cout << 4013;
    for (int i = 0; i < 4013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4014(int& val) {
    func_in_4014(val);
    const int x = 4014;
    std::cout << 4014;
    for (int i = 0; i < 4014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4014(int& val) {
    const int x = 4014;
    std::cout << 4014;
    for (int i = 0; i < 4014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4015(int& val) {
    func_in_4015(val);
    const int x = 4015;
    std::cout << 4015;
    for (int i = 0; i < 4015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4015(int& val) {
    const int x = 4015;
    std::cout << 4015;
    for (int i = 0; i < 4015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4016(int& val) {
    func_in_4016(val);
    const int x = 4016;
    std::cout << 4016;
    for (int i = 0; i < 4016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4016(int& val) {
    const int x = 4016;
    std::cout << 4016;
    for (int i = 0; i < 4016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4017(int& val) {
    func_in_4017(val);
    const int x = 4017;
    std::cout << 4017;
    for (int i = 0; i < 4017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4017(int& val) {
    const int x = 4017;
    std::cout << 4017;
    for (int i = 0; i < 4017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4018(int& val) {
    func_in_4018(val);
    const int x = 4018;
    std::cout << 4018;
    for (int i = 0; i < 4018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4018(int& val) {
    const int x = 4018;
    std::cout << 4018;
    for (int i = 0; i < 4018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4019(int& val) {
    func_in_4019(val);
    const int x = 4019;
    std::cout << 4019;
    for (int i = 0; i < 4019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4019(int& val) {
    const int x = 4019;
    std::cout << 4019;
    for (int i = 0; i < 4019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4020(int& val) {
    func_in_4020(val);
    const int x = 4020;
    std::cout << 4020;
    for (int i = 0; i < 4020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4020(int& val) {
    const int x = 4020;
    std::cout << 4020;
    for (int i = 0; i < 4020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4021(int& val) {
    func_in_4021(val);
    const int x = 4021;
    std::cout << 4021;
    for (int i = 0; i < 4021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4021(int& val) {
    const int x = 4021;
    std::cout << 4021;
    for (int i = 0; i < 4021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4022(int& val) {
    func_in_4022(val);
    const int x = 4022;
    std::cout << 4022;
    for (int i = 0; i < 4022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4022(int& val) {
    const int x = 4022;
    std::cout << 4022;
    for (int i = 0; i < 4022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4023(int& val) {
    func_in_4023(val);
    const int x = 4023;
    std::cout << 4023;
    for (int i = 0; i < 4023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4023(int& val) {
    const int x = 4023;
    std::cout << 4023;
    for (int i = 0; i < 4023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4024(int& val) {
    func_in_4024(val);
    const int x = 4024;
    std::cout << 4024;
    for (int i = 0; i < 4024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4024(int& val) {
    const int x = 4024;
    std::cout << 4024;
    for (int i = 0; i < 4024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4025(int& val) {
    func_in_4025(val);
    const int x = 4025;
    std::cout << 4025;
    for (int i = 0; i < 4025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4025(int& val) {
    const int x = 4025;
    std::cout << 4025;
    for (int i = 0; i < 4025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4026(int& val) {
    func_in_4026(val);
    const int x = 4026;
    std::cout << 4026;
    for (int i = 0; i < 4026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4026(int& val) {
    const int x = 4026;
    std::cout << 4026;
    for (int i = 0; i < 4026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4027(int& val) {
    func_in_4027(val);
    const int x = 4027;
    std::cout << 4027;
    for (int i = 0; i < 4027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4027(int& val) {
    const int x = 4027;
    std::cout << 4027;
    for (int i = 0; i < 4027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4028(int& val) {
    func_in_4028(val);
    const int x = 4028;
    std::cout << 4028;
    for (int i = 0; i < 4028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4028(int& val) {
    const int x = 4028;
    std::cout << 4028;
    for (int i = 0; i < 4028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4029(int& val) {
    func_in_4029(val);
    const int x = 4029;
    std::cout << 4029;
    for (int i = 0; i < 4029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4029(int& val) {
    const int x = 4029;
    std::cout << 4029;
    for (int i = 0; i < 4029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4030(int& val) {
    func_in_4030(val);
    const int x = 4030;
    std::cout << 4030;
    for (int i = 0; i < 4030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4030(int& val) {
    const int x = 4030;
    std::cout << 4030;
    for (int i = 0; i < 4030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4031(int& val) {
    func_in_4031(val);
    const int x = 4031;
    std::cout << 4031;
    for (int i = 0; i < 4031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4031(int& val) {
    const int x = 4031;
    std::cout << 4031;
    for (int i = 0; i < 4031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4032(int& val) {
    func_in_4032(val);
    const int x = 4032;
    std::cout << 4032;
    for (int i = 0; i < 4032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4032(int& val) {
    const int x = 4032;
    std::cout << 4032;
    for (int i = 0; i < 4032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4033(int& val) {
    func_in_4033(val);
    const int x = 4033;
    std::cout << 4033;
    for (int i = 0; i < 4033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4033(int& val) {
    const int x = 4033;
    std::cout << 4033;
    for (int i = 0; i < 4033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4034(int& val) {
    func_in_4034(val);
    const int x = 4034;
    std::cout << 4034;
    for (int i = 0; i < 4034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4034(int& val) {
    const int x = 4034;
    std::cout << 4034;
    for (int i = 0; i < 4034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4035(int& val) {
    func_in_4035(val);
    const int x = 4035;
    std::cout << 4035;
    for (int i = 0; i < 4035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4035(int& val) {
    const int x = 4035;
    std::cout << 4035;
    for (int i = 0; i < 4035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4036(int& val) {
    func_in_4036(val);
    const int x = 4036;
    std::cout << 4036;
    for (int i = 0; i < 4036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4036(int& val) {
    const int x = 4036;
    std::cout << 4036;
    for (int i = 0; i < 4036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4037(int& val) {
    func_in_4037(val);
    const int x = 4037;
    std::cout << 4037;
    for (int i = 0; i < 4037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4037(int& val) {
    const int x = 4037;
    std::cout << 4037;
    for (int i = 0; i < 4037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4038(int& val) {
    func_in_4038(val);
    const int x = 4038;
    std::cout << 4038;
    for (int i = 0; i < 4038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4038(int& val) {
    const int x = 4038;
    std::cout << 4038;
    for (int i = 0; i < 4038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4039(int& val) {
    func_in_4039(val);
    const int x = 4039;
    std::cout << 4039;
    for (int i = 0; i < 4039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4039(int& val) {
    const int x = 4039;
    std::cout << 4039;
    for (int i = 0; i < 4039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4040(int& val) {
    func_in_4040(val);
    const int x = 4040;
    std::cout << 4040;
    for (int i = 0; i < 4040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4040(int& val) {
    const int x = 4040;
    std::cout << 4040;
    for (int i = 0; i < 4040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4041(int& val) {
    func_in_4041(val);
    const int x = 4041;
    std::cout << 4041;
    for (int i = 0; i < 4041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4041(int& val) {
    const int x = 4041;
    std::cout << 4041;
    for (int i = 0; i < 4041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4042(int& val) {
    func_in_4042(val);
    const int x = 4042;
    std::cout << 4042;
    for (int i = 0; i < 4042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4042(int& val) {
    const int x = 4042;
    std::cout << 4042;
    for (int i = 0; i < 4042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4043(int& val) {
    func_in_4043(val);
    const int x = 4043;
    std::cout << 4043;
    for (int i = 0; i < 4043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4043(int& val) {
    const int x = 4043;
    std::cout << 4043;
    for (int i = 0; i < 4043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4044(int& val) {
    func_in_4044(val);
    const int x = 4044;
    std::cout << 4044;
    for (int i = 0; i < 4044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4044(int& val) {
    const int x = 4044;
    std::cout << 4044;
    for (int i = 0; i < 4044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4045(int& val) {
    func_in_4045(val);
    const int x = 4045;
    std::cout << 4045;
    for (int i = 0; i < 4045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4045(int& val) {
    const int x = 4045;
    std::cout << 4045;
    for (int i = 0; i < 4045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4046(int& val) {
    func_in_4046(val);
    const int x = 4046;
    std::cout << 4046;
    for (int i = 0; i < 4046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4046(int& val) {
    const int x = 4046;
    std::cout << 4046;
    for (int i = 0; i < 4046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4047(int& val) {
    func_in_4047(val);
    const int x = 4047;
    std::cout << 4047;
    for (int i = 0; i < 4047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4047(int& val) {
    const int x = 4047;
    std::cout << 4047;
    for (int i = 0; i < 4047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4048(int& val) {
    func_in_4048(val);
    const int x = 4048;
    std::cout << 4048;
    for (int i = 0; i < 4048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4048(int& val) {
    const int x = 4048;
    std::cout << 4048;
    for (int i = 0; i < 4048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4049(int& val) {
    func_in_4049(val);
    const int x = 4049;
    std::cout << 4049;
    for (int i = 0; i < 4049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4049(int& val) {
    const int x = 4049;
    std::cout << 4049;
    for (int i = 0; i < 4049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4050(int& val) {
    func_in_4050(val);
    const int x = 4050;
    std::cout << 4050;
    for (int i = 0; i < 4050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4050(int& val) {
    const int x = 4050;
    std::cout << 4050;
    for (int i = 0; i < 4050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4051(int& val) {
    func_in_4051(val);
    const int x = 4051;
    std::cout << 4051;
    for (int i = 0; i < 4051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4051(int& val) {
    const int x = 4051;
    std::cout << 4051;
    for (int i = 0; i < 4051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4052(int& val) {
    func_in_4052(val);
    const int x = 4052;
    std::cout << 4052;
    for (int i = 0; i < 4052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4052(int& val) {
    const int x = 4052;
    std::cout << 4052;
    for (int i = 0; i < 4052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4053(int& val) {
    func_in_4053(val);
    const int x = 4053;
    std::cout << 4053;
    for (int i = 0; i < 4053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4053(int& val) {
    const int x = 4053;
    std::cout << 4053;
    for (int i = 0; i < 4053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4054(int& val) {
    func_in_4054(val);
    const int x = 4054;
    std::cout << 4054;
    for (int i = 0; i < 4054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4054(int& val) {
    const int x = 4054;
    std::cout << 4054;
    for (int i = 0; i < 4054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4055(int& val) {
    func_in_4055(val);
    const int x = 4055;
    std::cout << 4055;
    for (int i = 0; i < 4055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4055(int& val) {
    const int x = 4055;
    std::cout << 4055;
    for (int i = 0; i < 4055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4056(int& val) {
    func_in_4056(val);
    const int x = 4056;
    std::cout << 4056;
    for (int i = 0; i < 4056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4056(int& val) {
    const int x = 4056;
    std::cout << 4056;
    for (int i = 0; i < 4056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4057(int& val) {
    func_in_4057(val);
    const int x = 4057;
    std::cout << 4057;
    for (int i = 0; i < 4057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4057(int& val) {
    const int x = 4057;
    std::cout << 4057;
    for (int i = 0; i < 4057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4058(int& val) {
    func_in_4058(val);
    const int x = 4058;
    std::cout << 4058;
    for (int i = 0; i < 4058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4058(int& val) {
    const int x = 4058;
    std::cout << 4058;
    for (int i = 0; i < 4058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4059(int& val) {
    func_in_4059(val);
    const int x = 4059;
    std::cout << 4059;
    for (int i = 0; i < 4059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4059(int& val) {
    const int x = 4059;
    std::cout << 4059;
    for (int i = 0; i < 4059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4060(int& val) {
    func_in_4060(val);
    const int x = 4060;
    std::cout << 4060;
    for (int i = 0; i < 4060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4060(int& val) {
    const int x = 4060;
    std::cout << 4060;
    for (int i = 0; i < 4060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4061(int& val) {
    func_in_4061(val);
    const int x = 4061;
    std::cout << 4061;
    for (int i = 0; i < 4061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4061(int& val) {
    const int x = 4061;
    std::cout << 4061;
    for (int i = 0; i < 4061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4062(int& val) {
    func_in_4062(val);
    const int x = 4062;
    std::cout << 4062;
    for (int i = 0; i < 4062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4062(int& val) {
    const int x = 4062;
    std::cout << 4062;
    for (int i = 0; i < 4062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4063(int& val) {
    func_in_4063(val);
    const int x = 4063;
    std::cout << 4063;
    for (int i = 0; i < 4063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4063(int& val) {
    const int x = 4063;
    std::cout << 4063;
    for (int i = 0; i < 4063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4064(int& val) {
    func_in_4064(val);
    const int x = 4064;
    std::cout << 4064;
    for (int i = 0; i < 4064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4064(int& val) {
    const int x = 4064;
    std::cout << 4064;
    for (int i = 0; i < 4064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4065(int& val) {
    func_in_4065(val);
    const int x = 4065;
    std::cout << 4065;
    for (int i = 0; i < 4065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4065(int& val) {
    const int x = 4065;
    std::cout << 4065;
    for (int i = 0; i < 4065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4066(int& val) {
    func_in_4066(val);
    const int x = 4066;
    std::cout << 4066;
    for (int i = 0; i < 4066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4066(int& val) {
    const int x = 4066;
    std::cout << 4066;
    for (int i = 0; i < 4066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4067(int& val) {
    func_in_4067(val);
    const int x = 4067;
    std::cout << 4067;
    for (int i = 0; i < 4067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4067(int& val) {
    const int x = 4067;
    std::cout << 4067;
    for (int i = 0; i < 4067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4068(int& val) {
    func_in_4068(val);
    const int x = 4068;
    std::cout << 4068;
    for (int i = 0; i < 4068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4068(int& val) {
    const int x = 4068;
    std::cout << 4068;
    for (int i = 0; i < 4068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4069(int& val) {
    func_in_4069(val);
    const int x = 4069;
    std::cout << 4069;
    for (int i = 0; i < 4069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4069(int& val) {
    const int x = 4069;
    std::cout << 4069;
    for (int i = 0; i < 4069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4070(int& val) {
    func_in_4070(val);
    const int x = 4070;
    std::cout << 4070;
    for (int i = 0; i < 4070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4070(int& val) {
    const int x = 4070;
    std::cout << 4070;
    for (int i = 0; i < 4070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4071(int& val) {
    func_in_4071(val);
    const int x = 4071;
    std::cout << 4071;
    for (int i = 0; i < 4071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4071(int& val) {
    const int x = 4071;
    std::cout << 4071;
    for (int i = 0; i < 4071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4072(int& val) {
    func_in_4072(val);
    const int x = 4072;
    std::cout << 4072;
    for (int i = 0; i < 4072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4072(int& val) {
    const int x = 4072;
    std::cout << 4072;
    for (int i = 0; i < 4072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4073(int& val) {
    func_in_4073(val);
    const int x = 4073;
    std::cout << 4073;
    for (int i = 0; i < 4073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4073(int& val) {
    const int x = 4073;
    std::cout << 4073;
    for (int i = 0; i < 4073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4074(int& val) {
    func_in_4074(val);
    const int x = 4074;
    std::cout << 4074;
    for (int i = 0; i < 4074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4074(int& val) {
    const int x = 4074;
    std::cout << 4074;
    for (int i = 0; i < 4074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4075(int& val) {
    func_in_4075(val);
    const int x = 4075;
    std::cout << 4075;
    for (int i = 0; i < 4075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4075(int& val) {
    const int x = 4075;
    std::cout << 4075;
    for (int i = 0; i < 4075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4076(int& val) {
    func_in_4076(val);
    const int x = 4076;
    std::cout << 4076;
    for (int i = 0; i < 4076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4076(int& val) {
    const int x = 4076;
    std::cout << 4076;
    for (int i = 0; i < 4076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4077(int& val) {
    func_in_4077(val);
    const int x = 4077;
    std::cout << 4077;
    for (int i = 0; i < 4077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4077(int& val) {
    const int x = 4077;
    std::cout << 4077;
    for (int i = 0; i < 4077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4078(int& val) {
    func_in_4078(val);
    const int x = 4078;
    std::cout << 4078;
    for (int i = 0; i < 4078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4078(int& val) {
    const int x = 4078;
    std::cout << 4078;
    for (int i = 0; i < 4078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4079(int& val) {
    func_in_4079(val);
    const int x = 4079;
    std::cout << 4079;
    for (int i = 0; i < 4079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4079(int& val) {
    const int x = 4079;
    std::cout << 4079;
    for (int i = 0; i < 4079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4080(int& val) {
    func_in_4080(val);
    const int x = 4080;
    std::cout << 4080;
    for (int i = 0; i < 4080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4080(int& val) {
    const int x = 4080;
    std::cout << 4080;
    for (int i = 0; i < 4080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4081(int& val) {
    func_in_4081(val);
    const int x = 4081;
    std::cout << 4081;
    for (int i = 0; i < 4081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4081(int& val) {
    const int x = 4081;
    std::cout << 4081;
    for (int i = 0; i < 4081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4082(int& val) {
    func_in_4082(val);
    const int x = 4082;
    std::cout << 4082;
    for (int i = 0; i < 4082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4082(int& val) {
    const int x = 4082;
    std::cout << 4082;
    for (int i = 0; i < 4082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4083(int& val) {
    func_in_4083(val);
    const int x = 4083;
    std::cout << 4083;
    for (int i = 0; i < 4083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4083(int& val) {
    const int x = 4083;
    std::cout << 4083;
    for (int i = 0; i < 4083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4084(int& val) {
    func_in_4084(val);
    const int x = 4084;
    std::cout << 4084;
    for (int i = 0; i < 4084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4084(int& val) {
    const int x = 4084;
    std::cout << 4084;
    for (int i = 0; i < 4084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4085(int& val) {
    func_in_4085(val);
    const int x = 4085;
    std::cout << 4085;
    for (int i = 0; i < 4085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4085(int& val) {
    const int x = 4085;
    std::cout << 4085;
    for (int i = 0; i < 4085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4086(int& val) {
    func_in_4086(val);
    const int x = 4086;
    std::cout << 4086;
    for (int i = 0; i < 4086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4086(int& val) {
    const int x = 4086;
    std::cout << 4086;
    for (int i = 0; i < 4086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4087(int& val) {
    func_in_4087(val);
    const int x = 4087;
    std::cout << 4087;
    for (int i = 0; i < 4087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4087(int& val) {
    const int x = 4087;
    std::cout << 4087;
    for (int i = 0; i < 4087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4088(int& val) {
    func_in_4088(val);
    const int x = 4088;
    std::cout << 4088;
    for (int i = 0; i < 4088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4088(int& val) {
    const int x = 4088;
    std::cout << 4088;
    for (int i = 0; i < 4088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4089(int& val) {
    func_in_4089(val);
    const int x = 4089;
    std::cout << 4089;
    for (int i = 0; i < 4089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4089(int& val) {
    const int x = 4089;
    std::cout << 4089;
    for (int i = 0; i < 4089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4090(int& val) {
    func_in_4090(val);
    const int x = 4090;
    std::cout << 4090;
    for (int i = 0; i < 4090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4090(int& val) {
    const int x = 4090;
    std::cout << 4090;
    for (int i = 0; i < 4090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4091(int& val) {
    func_in_4091(val);
    const int x = 4091;
    std::cout << 4091;
    for (int i = 0; i < 4091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4091(int& val) {
    const int x = 4091;
    std::cout << 4091;
    for (int i = 0; i < 4091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4092(int& val) {
    func_in_4092(val);
    const int x = 4092;
    std::cout << 4092;
    for (int i = 0; i < 4092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4092(int& val) {
    const int x = 4092;
    std::cout << 4092;
    for (int i = 0; i < 4092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4093(int& val) {
    func_in_4093(val);
    const int x = 4093;
    std::cout << 4093;
    for (int i = 0; i < 4093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4093(int& val) {
    const int x = 4093;
    std::cout << 4093;
    for (int i = 0; i < 4093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4094(int& val) {
    func_in_4094(val);
    const int x = 4094;
    std::cout << 4094;
    for (int i = 0; i < 4094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4094(int& val) {
    const int x = 4094;
    std::cout << 4094;
    for (int i = 0; i < 4094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4095(int& val) {
    func_in_4095(val);
    const int x = 4095;
    std::cout << 4095;
    for (int i = 0; i < 4095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4095(int& val) {
    const int x = 4095;
    std::cout << 4095;
    for (int i = 0; i < 4095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4096(int& val) {
    func_in_4096(val);
    const int x = 4096;
    std::cout << 4096;
    for (int i = 0; i < 4096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4096(int& val) {
    const int x = 4096;
    std::cout << 4096;
    for (int i = 0; i < 4096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4097(int& val) {
    func_in_4097(val);
    const int x = 4097;
    std::cout << 4097;
    for (int i = 0; i < 4097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4097(int& val) {
    const int x = 4097;
    std::cout << 4097;
    for (int i = 0; i < 4097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4098(int& val) {
    func_in_4098(val);
    const int x = 4098;
    std::cout << 4098;
    for (int i = 0; i < 4098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4098(int& val) {
    const int x = 4098;
    std::cout << 4098;
    for (int i = 0; i < 4098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4099(int& val) {
    func_in_4099(val);
    const int x = 4099;
    std::cout << 4099;
    for (int i = 0; i < 4099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4099(int& val) {
    const int x = 4099;
    std::cout << 4099;
    for (int i = 0; i < 4099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4100(int& val) {
    func_in_4100(val);
    const int x = 4100;
    std::cout << 4100;
    for (int i = 0; i < 4100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4100(int& val) {
    const int x = 4100;
    std::cout << 4100;
    for (int i = 0; i < 4100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4101(int& val) {
    func_in_4101(val);
    const int x = 4101;
    std::cout << 4101;
    for (int i = 0; i < 4101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4101(int& val) {
    const int x = 4101;
    std::cout << 4101;
    for (int i = 0; i < 4101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4102(int& val) {
    func_in_4102(val);
    const int x = 4102;
    std::cout << 4102;
    for (int i = 0; i < 4102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4102(int& val) {
    const int x = 4102;
    std::cout << 4102;
    for (int i = 0; i < 4102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4103(int& val) {
    func_in_4103(val);
    const int x = 4103;
    std::cout << 4103;
    for (int i = 0; i < 4103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4103(int& val) {
    const int x = 4103;
    std::cout << 4103;
    for (int i = 0; i < 4103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4104(int& val) {
    func_in_4104(val);
    const int x = 4104;
    std::cout << 4104;
    for (int i = 0; i < 4104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4104(int& val) {
    const int x = 4104;
    std::cout << 4104;
    for (int i = 0; i < 4104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4105(int& val) {
    func_in_4105(val);
    const int x = 4105;
    std::cout << 4105;
    for (int i = 0; i < 4105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4105(int& val) {
    const int x = 4105;
    std::cout << 4105;
    for (int i = 0; i < 4105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4106(int& val) {
    func_in_4106(val);
    const int x = 4106;
    std::cout << 4106;
    for (int i = 0; i < 4106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4106(int& val) {
    const int x = 4106;
    std::cout << 4106;
    for (int i = 0; i < 4106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4107(int& val) {
    func_in_4107(val);
    const int x = 4107;
    std::cout << 4107;
    for (int i = 0; i < 4107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4107(int& val) {
    const int x = 4107;
    std::cout << 4107;
    for (int i = 0; i < 4107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4108(int& val) {
    func_in_4108(val);
    const int x = 4108;
    std::cout << 4108;
    for (int i = 0; i < 4108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4108(int& val) {
    const int x = 4108;
    std::cout << 4108;
    for (int i = 0; i < 4108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4109(int& val) {
    func_in_4109(val);
    const int x = 4109;
    std::cout << 4109;
    for (int i = 0; i < 4109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4109(int& val) {
    const int x = 4109;
    std::cout << 4109;
    for (int i = 0; i < 4109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4110(int& val) {
    func_in_4110(val);
    const int x = 4110;
    std::cout << 4110;
    for (int i = 0; i < 4110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4110(int& val) {
    const int x = 4110;
    std::cout << 4110;
    for (int i = 0; i < 4110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4111(int& val) {
    func_in_4111(val);
    const int x = 4111;
    std::cout << 4111;
    for (int i = 0; i < 4111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4111(int& val) {
    const int x = 4111;
    std::cout << 4111;
    for (int i = 0; i < 4111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4112(int& val) {
    func_in_4112(val);
    const int x = 4112;
    std::cout << 4112;
    for (int i = 0; i < 4112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4112(int& val) {
    const int x = 4112;
    std::cout << 4112;
    for (int i = 0; i < 4112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4113(int& val) {
    func_in_4113(val);
    const int x = 4113;
    std::cout << 4113;
    for (int i = 0; i < 4113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4113(int& val) {
    const int x = 4113;
    std::cout << 4113;
    for (int i = 0; i < 4113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4114(int& val) {
    func_in_4114(val);
    const int x = 4114;
    std::cout << 4114;
    for (int i = 0; i < 4114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4114(int& val) {
    const int x = 4114;
    std::cout << 4114;
    for (int i = 0; i < 4114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4115(int& val) {
    func_in_4115(val);
    const int x = 4115;
    std::cout << 4115;
    for (int i = 0; i < 4115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4115(int& val) {
    const int x = 4115;
    std::cout << 4115;
    for (int i = 0; i < 4115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4116(int& val) {
    func_in_4116(val);
    const int x = 4116;
    std::cout << 4116;
    for (int i = 0; i < 4116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4116(int& val) {
    const int x = 4116;
    std::cout << 4116;
    for (int i = 0; i < 4116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4117(int& val) {
    func_in_4117(val);
    const int x = 4117;
    std::cout << 4117;
    for (int i = 0; i < 4117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4117(int& val) {
    const int x = 4117;
    std::cout << 4117;
    for (int i = 0; i < 4117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4118(int& val) {
    func_in_4118(val);
    const int x = 4118;
    std::cout << 4118;
    for (int i = 0; i < 4118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4118(int& val) {
    const int x = 4118;
    std::cout << 4118;
    for (int i = 0; i < 4118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4119(int& val) {
    func_in_4119(val);
    const int x = 4119;
    std::cout << 4119;
    for (int i = 0; i < 4119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4119(int& val) {
    const int x = 4119;
    std::cout << 4119;
    for (int i = 0; i < 4119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4120(int& val) {
    func_in_4120(val);
    const int x = 4120;
    std::cout << 4120;
    for (int i = 0; i < 4120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4120(int& val) {
    const int x = 4120;
    std::cout << 4120;
    for (int i = 0; i < 4120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4121(int& val) {
    func_in_4121(val);
    const int x = 4121;
    std::cout << 4121;
    for (int i = 0; i < 4121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4121(int& val) {
    const int x = 4121;
    std::cout << 4121;
    for (int i = 0; i < 4121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4122(int& val) {
    func_in_4122(val);
    const int x = 4122;
    std::cout << 4122;
    for (int i = 0; i < 4122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4122(int& val) {
    const int x = 4122;
    std::cout << 4122;
    for (int i = 0; i < 4122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4123(int& val) {
    func_in_4123(val);
    const int x = 4123;
    std::cout << 4123;
    for (int i = 0; i < 4123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4123(int& val) {
    const int x = 4123;
    std::cout << 4123;
    for (int i = 0; i < 4123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4124(int& val) {
    func_in_4124(val);
    const int x = 4124;
    std::cout << 4124;
    for (int i = 0; i < 4124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4124(int& val) {
    const int x = 4124;
    std::cout << 4124;
    for (int i = 0; i < 4124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4125(int& val) {
    func_in_4125(val);
    const int x = 4125;
    std::cout << 4125;
    for (int i = 0; i < 4125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4125(int& val) {
    const int x = 4125;
    std::cout << 4125;
    for (int i = 0; i < 4125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4126(int& val) {
    func_in_4126(val);
    const int x = 4126;
    std::cout << 4126;
    for (int i = 0; i < 4126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4126(int& val) {
    const int x = 4126;
    std::cout << 4126;
    for (int i = 0; i < 4126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4127(int& val) {
    func_in_4127(val);
    const int x = 4127;
    std::cout << 4127;
    for (int i = 0; i < 4127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4127(int& val) {
    const int x = 4127;
    std::cout << 4127;
    for (int i = 0; i < 4127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4128(int& val) {
    func_in_4128(val);
    const int x = 4128;
    std::cout << 4128;
    for (int i = 0; i < 4128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4128(int& val) {
    const int x = 4128;
    std::cout << 4128;
    for (int i = 0; i < 4128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4129(int& val) {
    func_in_4129(val);
    const int x = 4129;
    std::cout << 4129;
    for (int i = 0; i < 4129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4129(int& val) {
    const int x = 4129;
    std::cout << 4129;
    for (int i = 0; i < 4129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4130(int& val) {
    func_in_4130(val);
    const int x = 4130;
    std::cout << 4130;
    for (int i = 0; i < 4130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4130(int& val) {
    const int x = 4130;
    std::cout << 4130;
    for (int i = 0; i < 4130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4131(int& val) {
    func_in_4131(val);
    const int x = 4131;
    std::cout << 4131;
    for (int i = 0; i < 4131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4131(int& val) {
    const int x = 4131;
    std::cout << 4131;
    for (int i = 0; i < 4131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4132(int& val) {
    func_in_4132(val);
    const int x = 4132;
    std::cout << 4132;
    for (int i = 0; i < 4132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4132(int& val) {
    const int x = 4132;
    std::cout << 4132;
    for (int i = 0; i < 4132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4133(int& val) {
    func_in_4133(val);
    const int x = 4133;
    std::cout << 4133;
    for (int i = 0; i < 4133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4133(int& val) {
    const int x = 4133;
    std::cout << 4133;
    for (int i = 0; i < 4133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4134(int& val) {
    func_in_4134(val);
    const int x = 4134;
    std::cout << 4134;
    for (int i = 0; i < 4134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4134(int& val) {
    const int x = 4134;
    std::cout << 4134;
    for (int i = 0; i < 4134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4135(int& val) {
    func_in_4135(val);
    const int x = 4135;
    std::cout << 4135;
    for (int i = 0; i < 4135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4135(int& val) {
    const int x = 4135;
    std::cout << 4135;
    for (int i = 0; i < 4135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4136(int& val) {
    func_in_4136(val);
    const int x = 4136;
    std::cout << 4136;
    for (int i = 0; i < 4136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4136(int& val) {
    const int x = 4136;
    std::cout << 4136;
    for (int i = 0; i < 4136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4137(int& val) {
    func_in_4137(val);
    const int x = 4137;
    std::cout << 4137;
    for (int i = 0; i < 4137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4137(int& val) {
    const int x = 4137;
    std::cout << 4137;
    for (int i = 0; i < 4137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4138(int& val) {
    func_in_4138(val);
    const int x = 4138;
    std::cout << 4138;
    for (int i = 0; i < 4138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4138(int& val) {
    const int x = 4138;
    std::cout << 4138;
    for (int i = 0; i < 4138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4139(int& val) {
    func_in_4139(val);
    const int x = 4139;
    std::cout << 4139;
    for (int i = 0; i < 4139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4139(int& val) {
    const int x = 4139;
    std::cout << 4139;
    for (int i = 0; i < 4139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4140(int& val) {
    func_in_4140(val);
    const int x = 4140;
    std::cout << 4140;
    for (int i = 0; i < 4140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4140(int& val) {
    const int x = 4140;
    std::cout << 4140;
    for (int i = 0; i < 4140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4141(int& val) {
    func_in_4141(val);
    const int x = 4141;
    std::cout << 4141;
    for (int i = 0; i < 4141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4141(int& val) {
    const int x = 4141;
    std::cout << 4141;
    for (int i = 0; i < 4141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4142(int& val) {
    func_in_4142(val);
    const int x = 4142;
    std::cout << 4142;
    for (int i = 0; i < 4142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4142(int& val) {
    const int x = 4142;
    std::cout << 4142;
    for (int i = 0; i < 4142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4143(int& val) {
    func_in_4143(val);
    const int x = 4143;
    std::cout << 4143;
    for (int i = 0; i < 4143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4143(int& val) {
    const int x = 4143;
    std::cout << 4143;
    for (int i = 0; i < 4143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4144(int& val) {
    func_in_4144(val);
    const int x = 4144;
    std::cout << 4144;
    for (int i = 0; i < 4144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4144(int& val) {
    const int x = 4144;
    std::cout << 4144;
    for (int i = 0; i < 4144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4145(int& val) {
    func_in_4145(val);
    const int x = 4145;
    std::cout << 4145;
    for (int i = 0; i < 4145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4145(int& val) {
    const int x = 4145;
    std::cout << 4145;
    for (int i = 0; i < 4145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4146(int& val) {
    func_in_4146(val);
    const int x = 4146;
    std::cout << 4146;
    for (int i = 0; i < 4146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4146(int& val) {
    const int x = 4146;
    std::cout << 4146;
    for (int i = 0; i < 4146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4147(int& val) {
    func_in_4147(val);
    const int x = 4147;
    std::cout << 4147;
    for (int i = 0; i < 4147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4147(int& val) {
    const int x = 4147;
    std::cout << 4147;
    for (int i = 0; i < 4147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4148(int& val) {
    func_in_4148(val);
    const int x = 4148;
    std::cout << 4148;
    for (int i = 0; i < 4148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4148(int& val) {
    const int x = 4148;
    std::cout << 4148;
    for (int i = 0; i < 4148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4149(int& val) {
    func_in_4149(val);
    const int x = 4149;
    std::cout << 4149;
    for (int i = 0; i < 4149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4149(int& val) {
    const int x = 4149;
    std::cout << 4149;
    for (int i = 0; i < 4149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4150(int& val) {
    func_in_4150(val);
    const int x = 4150;
    std::cout << 4150;
    for (int i = 0; i < 4150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4150(int& val) {
    const int x = 4150;
    std::cout << 4150;
    for (int i = 0; i < 4150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4151(int& val) {
    func_in_4151(val);
    const int x = 4151;
    std::cout << 4151;
    for (int i = 0; i < 4151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4151(int& val) {
    const int x = 4151;
    std::cout << 4151;
    for (int i = 0; i < 4151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4152(int& val) {
    func_in_4152(val);
    const int x = 4152;
    std::cout << 4152;
    for (int i = 0; i < 4152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4152(int& val) {
    const int x = 4152;
    std::cout << 4152;
    for (int i = 0; i < 4152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4153(int& val) {
    func_in_4153(val);
    const int x = 4153;
    std::cout << 4153;
    for (int i = 0; i < 4153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4153(int& val) {
    const int x = 4153;
    std::cout << 4153;
    for (int i = 0; i < 4153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4154(int& val) {
    func_in_4154(val);
    const int x = 4154;
    std::cout << 4154;
    for (int i = 0; i < 4154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4154(int& val) {
    const int x = 4154;
    std::cout << 4154;
    for (int i = 0; i < 4154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4155(int& val) {
    func_in_4155(val);
    const int x = 4155;
    std::cout << 4155;
    for (int i = 0; i < 4155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4155(int& val) {
    const int x = 4155;
    std::cout << 4155;
    for (int i = 0; i < 4155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4156(int& val) {
    func_in_4156(val);
    const int x = 4156;
    std::cout << 4156;
    for (int i = 0; i < 4156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4156(int& val) {
    const int x = 4156;
    std::cout << 4156;
    for (int i = 0; i < 4156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4157(int& val) {
    func_in_4157(val);
    const int x = 4157;
    std::cout << 4157;
    for (int i = 0; i < 4157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4157(int& val) {
    const int x = 4157;
    std::cout << 4157;
    for (int i = 0; i < 4157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4158(int& val) {
    func_in_4158(val);
    const int x = 4158;
    std::cout << 4158;
    for (int i = 0; i < 4158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4158(int& val) {
    const int x = 4158;
    std::cout << 4158;
    for (int i = 0; i < 4158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4159(int& val) {
    func_in_4159(val);
    const int x = 4159;
    std::cout << 4159;
    for (int i = 0; i < 4159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4159(int& val) {
    const int x = 4159;
    std::cout << 4159;
    for (int i = 0; i < 4159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4160(int& val) {
    func_in_4160(val);
    const int x = 4160;
    std::cout << 4160;
    for (int i = 0; i < 4160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4160(int& val) {
    const int x = 4160;
    std::cout << 4160;
    for (int i = 0; i < 4160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4161(int& val) {
    func_in_4161(val);
    const int x = 4161;
    std::cout << 4161;
    for (int i = 0; i < 4161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4161(int& val) {
    const int x = 4161;
    std::cout << 4161;
    for (int i = 0; i < 4161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4162(int& val) {
    func_in_4162(val);
    const int x = 4162;
    std::cout << 4162;
    for (int i = 0; i < 4162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4162(int& val) {
    const int x = 4162;
    std::cout << 4162;
    for (int i = 0; i < 4162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4163(int& val) {
    func_in_4163(val);
    const int x = 4163;
    std::cout << 4163;
    for (int i = 0; i < 4163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4163(int& val) {
    const int x = 4163;
    std::cout << 4163;
    for (int i = 0; i < 4163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4164(int& val) {
    func_in_4164(val);
    const int x = 4164;
    std::cout << 4164;
    for (int i = 0; i < 4164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4164(int& val) {
    const int x = 4164;
    std::cout << 4164;
    for (int i = 0; i < 4164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4165(int& val) {
    func_in_4165(val);
    const int x = 4165;
    std::cout << 4165;
    for (int i = 0; i < 4165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4165(int& val) {
    const int x = 4165;
    std::cout << 4165;
    for (int i = 0; i < 4165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4166(int& val) {
    func_in_4166(val);
    const int x = 4166;
    std::cout << 4166;
    for (int i = 0; i < 4166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4166(int& val) {
    const int x = 4166;
    std::cout << 4166;
    for (int i = 0; i < 4166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4167(int& val) {
    func_in_4167(val);
    const int x = 4167;
    std::cout << 4167;
    for (int i = 0; i < 4167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4167(int& val) {
    const int x = 4167;
    std::cout << 4167;
    for (int i = 0; i < 4167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4168(int& val) {
    func_in_4168(val);
    const int x = 4168;
    std::cout << 4168;
    for (int i = 0; i < 4168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4168(int& val) {
    const int x = 4168;
    std::cout << 4168;
    for (int i = 0; i < 4168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4169(int& val) {
    func_in_4169(val);
    const int x = 4169;
    std::cout << 4169;
    for (int i = 0; i < 4169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4169(int& val) {
    const int x = 4169;
    std::cout << 4169;
    for (int i = 0; i < 4169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4170(int& val) {
    func_in_4170(val);
    const int x = 4170;
    std::cout << 4170;
    for (int i = 0; i < 4170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4170(int& val) {
    const int x = 4170;
    std::cout << 4170;
    for (int i = 0; i < 4170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4171(int& val) {
    func_in_4171(val);
    const int x = 4171;
    std::cout << 4171;
    for (int i = 0; i < 4171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4171(int& val) {
    const int x = 4171;
    std::cout << 4171;
    for (int i = 0; i < 4171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4172(int& val) {
    func_in_4172(val);
    const int x = 4172;
    std::cout << 4172;
    for (int i = 0; i < 4172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4172(int& val) {
    const int x = 4172;
    std::cout << 4172;
    for (int i = 0; i < 4172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4173(int& val) {
    func_in_4173(val);
    const int x = 4173;
    std::cout << 4173;
    for (int i = 0; i < 4173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4173(int& val) {
    const int x = 4173;
    std::cout << 4173;
    for (int i = 0; i < 4173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4174(int& val) {
    func_in_4174(val);
    const int x = 4174;
    std::cout << 4174;
    for (int i = 0; i < 4174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4174(int& val) {
    const int x = 4174;
    std::cout << 4174;
    for (int i = 0; i < 4174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4175(int& val) {
    func_in_4175(val);
    const int x = 4175;
    std::cout << 4175;
    for (int i = 0; i < 4175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4175(int& val) {
    const int x = 4175;
    std::cout << 4175;
    for (int i = 0; i < 4175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4176(int& val) {
    func_in_4176(val);
    const int x = 4176;
    std::cout << 4176;
    for (int i = 0; i < 4176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4176(int& val) {
    const int x = 4176;
    std::cout << 4176;
    for (int i = 0; i < 4176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4177(int& val) {
    func_in_4177(val);
    const int x = 4177;
    std::cout << 4177;
    for (int i = 0; i < 4177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4177(int& val) {
    const int x = 4177;
    std::cout << 4177;
    for (int i = 0; i < 4177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4178(int& val) {
    func_in_4178(val);
    const int x = 4178;
    std::cout << 4178;
    for (int i = 0; i < 4178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4178(int& val) {
    const int x = 4178;
    std::cout << 4178;
    for (int i = 0; i < 4178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4179(int& val) {
    func_in_4179(val);
    const int x = 4179;
    std::cout << 4179;
    for (int i = 0; i < 4179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4179(int& val) {
    const int x = 4179;
    std::cout << 4179;
    for (int i = 0; i < 4179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4180(int& val) {
    func_in_4180(val);
    const int x = 4180;
    std::cout << 4180;
    for (int i = 0; i < 4180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4180(int& val) {
    const int x = 4180;
    std::cout << 4180;
    for (int i = 0; i < 4180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4181(int& val) {
    func_in_4181(val);
    const int x = 4181;
    std::cout << 4181;
    for (int i = 0; i < 4181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4181(int& val) {
    const int x = 4181;
    std::cout << 4181;
    for (int i = 0; i < 4181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4182(int& val) {
    func_in_4182(val);
    const int x = 4182;
    std::cout << 4182;
    for (int i = 0; i < 4182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4182(int& val) {
    const int x = 4182;
    std::cout << 4182;
    for (int i = 0; i < 4182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4183(int& val) {
    func_in_4183(val);
    const int x = 4183;
    std::cout << 4183;
    for (int i = 0; i < 4183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4183(int& val) {
    const int x = 4183;
    std::cout << 4183;
    for (int i = 0; i < 4183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4184(int& val) {
    func_in_4184(val);
    const int x = 4184;
    std::cout << 4184;
    for (int i = 0; i < 4184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4184(int& val) {
    const int x = 4184;
    std::cout << 4184;
    for (int i = 0; i < 4184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4185(int& val) {
    func_in_4185(val);
    const int x = 4185;
    std::cout << 4185;
    for (int i = 0; i < 4185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4185(int& val) {
    const int x = 4185;
    std::cout << 4185;
    for (int i = 0; i < 4185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4186(int& val) {
    func_in_4186(val);
    const int x = 4186;
    std::cout << 4186;
    for (int i = 0; i < 4186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4186(int& val) {
    const int x = 4186;
    std::cout << 4186;
    for (int i = 0; i < 4186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4187(int& val) {
    func_in_4187(val);
    const int x = 4187;
    std::cout << 4187;
    for (int i = 0; i < 4187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4187(int& val) {
    const int x = 4187;
    std::cout << 4187;
    for (int i = 0; i < 4187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4188(int& val) {
    func_in_4188(val);
    const int x = 4188;
    std::cout << 4188;
    for (int i = 0; i < 4188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4188(int& val) {
    const int x = 4188;
    std::cout << 4188;
    for (int i = 0; i < 4188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4189(int& val) {
    func_in_4189(val);
    const int x = 4189;
    std::cout << 4189;
    for (int i = 0; i < 4189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4189(int& val) {
    const int x = 4189;
    std::cout << 4189;
    for (int i = 0; i < 4189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4190(int& val) {
    func_in_4190(val);
    const int x = 4190;
    std::cout << 4190;
    for (int i = 0; i < 4190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4190(int& val) {
    const int x = 4190;
    std::cout << 4190;
    for (int i = 0; i < 4190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4191(int& val) {
    func_in_4191(val);
    const int x = 4191;
    std::cout << 4191;
    for (int i = 0; i < 4191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4191(int& val) {
    const int x = 4191;
    std::cout << 4191;
    for (int i = 0; i < 4191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4192(int& val) {
    func_in_4192(val);
    const int x = 4192;
    std::cout << 4192;
    for (int i = 0; i < 4192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4192(int& val) {
    const int x = 4192;
    std::cout << 4192;
    for (int i = 0; i < 4192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4193(int& val) {
    func_in_4193(val);
    const int x = 4193;
    std::cout << 4193;
    for (int i = 0; i < 4193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4193(int& val) {
    const int x = 4193;
    std::cout << 4193;
    for (int i = 0; i < 4193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4194(int& val) {
    func_in_4194(val);
    const int x = 4194;
    std::cout << 4194;
    for (int i = 0; i < 4194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4194(int& val) {
    const int x = 4194;
    std::cout << 4194;
    for (int i = 0; i < 4194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4195(int& val) {
    func_in_4195(val);
    const int x = 4195;
    std::cout << 4195;
    for (int i = 0; i < 4195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4195(int& val) {
    const int x = 4195;
    std::cout << 4195;
    for (int i = 0; i < 4195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4196(int& val) {
    func_in_4196(val);
    const int x = 4196;
    std::cout << 4196;
    for (int i = 0; i < 4196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4196(int& val) {
    const int x = 4196;
    std::cout << 4196;
    for (int i = 0; i < 4196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4197(int& val) {
    func_in_4197(val);
    const int x = 4197;
    std::cout << 4197;
    for (int i = 0; i < 4197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4197(int& val) {
    const int x = 4197;
    std::cout << 4197;
    for (int i = 0; i < 4197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4198(int& val) {
    func_in_4198(val);
    const int x = 4198;
    std::cout << 4198;
    for (int i = 0; i < 4198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4198(int& val) {
    const int x = 4198;
    std::cout << 4198;
    for (int i = 0; i < 4198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4199(int& val) {
    func_in_4199(val);
    const int x = 4199;
    std::cout << 4199;
    for (int i = 0; i < 4199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4199(int& val) {
    const int x = 4199;
    std::cout << 4199;
    for (int i = 0; i < 4199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4200(int& val) {
    func_in_4200(val);
    const int x = 4200;
    std::cout << 4200;
    for (int i = 0; i < 4200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4200(int& val) {
    const int x = 4200;
    std::cout << 4200;
    for (int i = 0; i < 4200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4201(int& val) {
    func_in_4201(val);
    const int x = 4201;
    std::cout << 4201;
    for (int i = 0; i < 4201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4201(int& val) {
    const int x = 4201;
    std::cout << 4201;
    for (int i = 0; i < 4201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4202(int& val) {
    func_in_4202(val);
    const int x = 4202;
    std::cout << 4202;
    for (int i = 0; i < 4202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4202(int& val) {
    const int x = 4202;
    std::cout << 4202;
    for (int i = 0; i < 4202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4203(int& val) {
    func_in_4203(val);
    const int x = 4203;
    std::cout << 4203;
    for (int i = 0; i < 4203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4203(int& val) {
    const int x = 4203;
    std::cout << 4203;
    for (int i = 0; i < 4203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4204(int& val) {
    func_in_4204(val);
    const int x = 4204;
    std::cout << 4204;
    for (int i = 0; i < 4204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4204(int& val) {
    const int x = 4204;
    std::cout << 4204;
    for (int i = 0; i < 4204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4205(int& val) {
    func_in_4205(val);
    const int x = 4205;
    std::cout << 4205;
    for (int i = 0; i < 4205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4205(int& val) {
    const int x = 4205;
    std::cout << 4205;
    for (int i = 0; i < 4205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4206(int& val) {
    func_in_4206(val);
    const int x = 4206;
    std::cout << 4206;
    for (int i = 0; i < 4206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4206(int& val) {
    const int x = 4206;
    std::cout << 4206;
    for (int i = 0; i < 4206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4207(int& val) {
    func_in_4207(val);
    const int x = 4207;
    std::cout << 4207;
    for (int i = 0; i < 4207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4207(int& val) {
    const int x = 4207;
    std::cout << 4207;
    for (int i = 0; i < 4207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4208(int& val) {
    func_in_4208(val);
    const int x = 4208;
    std::cout << 4208;
    for (int i = 0; i < 4208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4208(int& val) {
    const int x = 4208;
    std::cout << 4208;
    for (int i = 0; i < 4208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4209(int& val) {
    func_in_4209(val);
    const int x = 4209;
    std::cout << 4209;
    for (int i = 0; i < 4209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4209(int& val) {
    const int x = 4209;
    std::cout << 4209;
    for (int i = 0; i < 4209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4210(int& val) {
    func_in_4210(val);
    const int x = 4210;
    std::cout << 4210;
    for (int i = 0; i < 4210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4210(int& val) {
    const int x = 4210;
    std::cout << 4210;
    for (int i = 0; i < 4210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4211(int& val) {
    func_in_4211(val);
    const int x = 4211;
    std::cout << 4211;
    for (int i = 0; i < 4211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4211(int& val) {
    const int x = 4211;
    std::cout << 4211;
    for (int i = 0; i < 4211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4212(int& val) {
    func_in_4212(val);
    const int x = 4212;
    std::cout << 4212;
    for (int i = 0; i < 4212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4212(int& val) {
    const int x = 4212;
    std::cout << 4212;
    for (int i = 0; i < 4212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4213(int& val) {
    func_in_4213(val);
    const int x = 4213;
    std::cout << 4213;
    for (int i = 0; i < 4213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4213(int& val) {
    const int x = 4213;
    std::cout << 4213;
    for (int i = 0; i < 4213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4214(int& val) {
    func_in_4214(val);
    const int x = 4214;
    std::cout << 4214;
    for (int i = 0; i < 4214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4214(int& val) {
    const int x = 4214;
    std::cout << 4214;
    for (int i = 0; i < 4214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4215(int& val) {
    func_in_4215(val);
    const int x = 4215;
    std::cout << 4215;
    for (int i = 0; i < 4215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4215(int& val) {
    const int x = 4215;
    std::cout << 4215;
    for (int i = 0; i < 4215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4216(int& val) {
    func_in_4216(val);
    const int x = 4216;
    std::cout << 4216;
    for (int i = 0; i < 4216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4216(int& val) {
    const int x = 4216;
    std::cout << 4216;
    for (int i = 0; i < 4216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4217(int& val) {
    func_in_4217(val);
    const int x = 4217;
    std::cout << 4217;
    for (int i = 0; i < 4217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4217(int& val) {
    const int x = 4217;
    std::cout << 4217;
    for (int i = 0; i < 4217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4218(int& val) {
    func_in_4218(val);
    const int x = 4218;
    std::cout << 4218;
    for (int i = 0; i < 4218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4218(int& val) {
    const int x = 4218;
    std::cout << 4218;
    for (int i = 0; i < 4218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4219(int& val) {
    func_in_4219(val);
    const int x = 4219;
    std::cout << 4219;
    for (int i = 0; i < 4219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4219(int& val) {
    const int x = 4219;
    std::cout << 4219;
    for (int i = 0; i < 4219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4220(int& val) {
    func_in_4220(val);
    const int x = 4220;
    std::cout << 4220;
    for (int i = 0; i < 4220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4220(int& val) {
    const int x = 4220;
    std::cout << 4220;
    for (int i = 0; i < 4220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4221(int& val) {
    func_in_4221(val);
    const int x = 4221;
    std::cout << 4221;
    for (int i = 0; i < 4221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4221(int& val) {
    const int x = 4221;
    std::cout << 4221;
    for (int i = 0; i < 4221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4222(int& val) {
    func_in_4222(val);
    const int x = 4222;
    std::cout << 4222;
    for (int i = 0; i < 4222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4222(int& val) {
    const int x = 4222;
    std::cout << 4222;
    for (int i = 0; i < 4222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4223(int& val) {
    func_in_4223(val);
    const int x = 4223;
    std::cout << 4223;
    for (int i = 0; i < 4223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4223(int& val) {
    const int x = 4223;
    std::cout << 4223;
    for (int i = 0; i < 4223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4224(int& val) {
    func_in_4224(val);
    const int x = 4224;
    std::cout << 4224;
    for (int i = 0; i < 4224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4224(int& val) {
    const int x = 4224;
    std::cout << 4224;
    for (int i = 0; i < 4224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4225(int& val) {
    func_in_4225(val);
    const int x = 4225;
    std::cout << 4225;
    for (int i = 0; i < 4225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4225(int& val) {
    const int x = 4225;
    std::cout << 4225;
    for (int i = 0; i < 4225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4226(int& val) {
    func_in_4226(val);
    const int x = 4226;
    std::cout << 4226;
    for (int i = 0; i < 4226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4226(int& val) {
    const int x = 4226;
    std::cout << 4226;
    for (int i = 0; i < 4226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4227(int& val) {
    func_in_4227(val);
    const int x = 4227;
    std::cout << 4227;
    for (int i = 0; i < 4227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4227(int& val) {
    const int x = 4227;
    std::cout << 4227;
    for (int i = 0; i < 4227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4228(int& val) {
    func_in_4228(val);
    const int x = 4228;
    std::cout << 4228;
    for (int i = 0; i < 4228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4228(int& val) {
    const int x = 4228;
    std::cout << 4228;
    for (int i = 0; i < 4228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4229(int& val) {
    func_in_4229(val);
    const int x = 4229;
    std::cout << 4229;
    for (int i = 0; i < 4229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4229(int& val) {
    const int x = 4229;
    std::cout << 4229;
    for (int i = 0; i < 4229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4230(int& val) {
    func_in_4230(val);
    const int x = 4230;
    std::cout << 4230;
    for (int i = 0; i < 4230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4230(int& val) {
    const int x = 4230;
    std::cout << 4230;
    for (int i = 0; i < 4230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4231(int& val) {
    func_in_4231(val);
    const int x = 4231;
    std::cout << 4231;
    for (int i = 0; i < 4231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4231(int& val) {
    const int x = 4231;
    std::cout << 4231;
    for (int i = 0; i < 4231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4232(int& val) {
    func_in_4232(val);
    const int x = 4232;
    std::cout << 4232;
    for (int i = 0; i < 4232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4232(int& val) {
    const int x = 4232;
    std::cout << 4232;
    for (int i = 0; i < 4232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4233(int& val) {
    func_in_4233(val);
    const int x = 4233;
    std::cout << 4233;
    for (int i = 0; i < 4233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4233(int& val) {
    const int x = 4233;
    std::cout << 4233;
    for (int i = 0; i < 4233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4234(int& val) {
    func_in_4234(val);
    const int x = 4234;
    std::cout << 4234;
    for (int i = 0; i < 4234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4234(int& val) {
    const int x = 4234;
    std::cout << 4234;
    for (int i = 0; i < 4234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4235(int& val) {
    func_in_4235(val);
    const int x = 4235;
    std::cout << 4235;
    for (int i = 0; i < 4235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4235(int& val) {
    const int x = 4235;
    std::cout << 4235;
    for (int i = 0; i < 4235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4236(int& val) {
    func_in_4236(val);
    const int x = 4236;
    std::cout << 4236;
    for (int i = 0; i < 4236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4236(int& val) {
    const int x = 4236;
    std::cout << 4236;
    for (int i = 0; i < 4236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4237(int& val) {
    func_in_4237(val);
    const int x = 4237;
    std::cout << 4237;
    for (int i = 0; i < 4237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4237(int& val) {
    const int x = 4237;
    std::cout << 4237;
    for (int i = 0; i < 4237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4238(int& val) {
    func_in_4238(val);
    const int x = 4238;
    std::cout << 4238;
    for (int i = 0; i < 4238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4238(int& val) {
    const int x = 4238;
    std::cout << 4238;
    for (int i = 0; i < 4238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4239(int& val) {
    func_in_4239(val);
    const int x = 4239;
    std::cout << 4239;
    for (int i = 0; i < 4239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4239(int& val) {
    const int x = 4239;
    std::cout << 4239;
    for (int i = 0; i < 4239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4240(int& val) {
    func_in_4240(val);
    const int x = 4240;
    std::cout << 4240;
    for (int i = 0; i < 4240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4240(int& val) {
    const int x = 4240;
    std::cout << 4240;
    for (int i = 0; i < 4240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4241(int& val) {
    func_in_4241(val);
    const int x = 4241;
    std::cout << 4241;
    for (int i = 0; i < 4241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4241(int& val) {
    const int x = 4241;
    std::cout << 4241;
    for (int i = 0; i < 4241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4242(int& val) {
    func_in_4242(val);
    const int x = 4242;
    std::cout << 4242;
    for (int i = 0; i < 4242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4242(int& val) {
    const int x = 4242;
    std::cout << 4242;
    for (int i = 0; i < 4242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4243(int& val) {
    func_in_4243(val);
    const int x = 4243;
    std::cout << 4243;
    for (int i = 0; i < 4243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4243(int& val) {
    const int x = 4243;
    std::cout << 4243;
    for (int i = 0; i < 4243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4244(int& val) {
    func_in_4244(val);
    const int x = 4244;
    std::cout << 4244;
    for (int i = 0; i < 4244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4244(int& val) {
    const int x = 4244;
    std::cout << 4244;
    for (int i = 0; i < 4244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4245(int& val) {
    func_in_4245(val);
    const int x = 4245;
    std::cout << 4245;
    for (int i = 0; i < 4245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4245(int& val) {
    const int x = 4245;
    std::cout << 4245;
    for (int i = 0; i < 4245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4246(int& val) {
    func_in_4246(val);
    const int x = 4246;
    std::cout << 4246;
    for (int i = 0; i < 4246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4246(int& val) {
    const int x = 4246;
    std::cout << 4246;
    for (int i = 0; i < 4246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4247(int& val) {
    func_in_4247(val);
    const int x = 4247;
    std::cout << 4247;
    for (int i = 0; i < 4247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4247(int& val) {
    const int x = 4247;
    std::cout << 4247;
    for (int i = 0; i < 4247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4248(int& val) {
    func_in_4248(val);
    const int x = 4248;
    std::cout << 4248;
    for (int i = 0; i < 4248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4248(int& val) {
    const int x = 4248;
    std::cout << 4248;
    for (int i = 0; i < 4248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4249(int& val) {
    func_in_4249(val);
    const int x = 4249;
    std::cout << 4249;
    for (int i = 0; i < 4249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4249(int& val) {
    const int x = 4249;
    std::cout << 4249;
    for (int i = 0; i < 4249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4250(int& val) {
    func_in_4250(val);
    const int x = 4250;
    std::cout << 4250;
    for (int i = 0; i < 4250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4250(int& val) {
    const int x = 4250;
    std::cout << 4250;
    for (int i = 0; i < 4250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4251(int& val) {
    func_in_4251(val);
    const int x = 4251;
    std::cout << 4251;
    for (int i = 0; i < 4251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4251(int& val) {
    const int x = 4251;
    std::cout << 4251;
    for (int i = 0; i < 4251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4252(int& val) {
    func_in_4252(val);
    const int x = 4252;
    std::cout << 4252;
    for (int i = 0; i < 4252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4252(int& val) {
    const int x = 4252;
    std::cout << 4252;
    for (int i = 0; i < 4252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4253(int& val) {
    func_in_4253(val);
    const int x = 4253;
    std::cout << 4253;
    for (int i = 0; i < 4253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4253(int& val) {
    const int x = 4253;
    std::cout << 4253;
    for (int i = 0; i < 4253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4254(int& val) {
    func_in_4254(val);
    const int x = 4254;
    std::cout << 4254;
    for (int i = 0; i < 4254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4254(int& val) {
    const int x = 4254;
    std::cout << 4254;
    for (int i = 0; i < 4254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4255(int& val) {
    func_in_4255(val);
    const int x = 4255;
    std::cout << 4255;
    for (int i = 0; i < 4255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4255(int& val) {
    const int x = 4255;
    std::cout << 4255;
    for (int i = 0; i < 4255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4256(int& val) {
    func_in_4256(val);
    const int x = 4256;
    std::cout << 4256;
    for (int i = 0; i < 4256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4256(int& val) {
    const int x = 4256;
    std::cout << 4256;
    for (int i = 0; i < 4256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4257(int& val) {
    func_in_4257(val);
    const int x = 4257;
    std::cout << 4257;
    for (int i = 0; i < 4257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4257(int& val) {
    const int x = 4257;
    std::cout << 4257;
    for (int i = 0; i < 4257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4258(int& val) {
    func_in_4258(val);
    const int x = 4258;
    std::cout << 4258;
    for (int i = 0; i < 4258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4258(int& val) {
    const int x = 4258;
    std::cout << 4258;
    for (int i = 0; i < 4258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4259(int& val) {
    func_in_4259(val);
    const int x = 4259;
    std::cout << 4259;
    for (int i = 0; i < 4259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4259(int& val) {
    const int x = 4259;
    std::cout << 4259;
    for (int i = 0; i < 4259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4260(int& val) {
    func_in_4260(val);
    const int x = 4260;
    std::cout << 4260;
    for (int i = 0; i < 4260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4260(int& val) {
    const int x = 4260;
    std::cout << 4260;
    for (int i = 0; i < 4260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4261(int& val) {
    func_in_4261(val);
    const int x = 4261;
    std::cout << 4261;
    for (int i = 0; i < 4261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4261(int& val) {
    const int x = 4261;
    std::cout << 4261;
    for (int i = 0; i < 4261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4262(int& val) {
    func_in_4262(val);
    const int x = 4262;
    std::cout << 4262;
    for (int i = 0; i < 4262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4262(int& val) {
    const int x = 4262;
    std::cout << 4262;
    for (int i = 0; i < 4262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4263(int& val) {
    func_in_4263(val);
    const int x = 4263;
    std::cout << 4263;
    for (int i = 0; i < 4263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4263(int& val) {
    const int x = 4263;
    std::cout << 4263;
    for (int i = 0; i < 4263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4264(int& val) {
    func_in_4264(val);
    const int x = 4264;
    std::cout << 4264;
    for (int i = 0; i < 4264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4264(int& val) {
    const int x = 4264;
    std::cout << 4264;
    for (int i = 0; i < 4264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4265(int& val) {
    func_in_4265(val);
    const int x = 4265;
    std::cout << 4265;
    for (int i = 0; i < 4265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4265(int& val) {
    const int x = 4265;
    std::cout << 4265;
    for (int i = 0; i < 4265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4266(int& val) {
    func_in_4266(val);
    const int x = 4266;
    std::cout << 4266;
    for (int i = 0; i < 4266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4266(int& val) {
    const int x = 4266;
    std::cout << 4266;
    for (int i = 0; i < 4266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4267(int& val) {
    func_in_4267(val);
    const int x = 4267;
    std::cout << 4267;
    for (int i = 0; i < 4267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4267(int& val) {
    const int x = 4267;
    std::cout << 4267;
    for (int i = 0; i < 4267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4268(int& val) {
    func_in_4268(val);
    const int x = 4268;
    std::cout << 4268;
    for (int i = 0; i < 4268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4268(int& val) {
    const int x = 4268;
    std::cout << 4268;
    for (int i = 0; i < 4268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4269(int& val) {
    func_in_4269(val);
    const int x = 4269;
    std::cout << 4269;
    for (int i = 0; i < 4269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4269(int& val) {
    const int x = 4269;
    std::cout << 4269;
    for (int i = 0; i < 4269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4270(int& val) {
    func_in_4270(val);
    const int x = 4270;
    std::cout << 4270;
    for (int i = 0; i < 4270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4270(int& val) {
    const int x = 4270;
    std::cout << 4270;
    for (int i = 0; i < 4270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4271(int& val) {
    func_in_4271(val);
    const int x = 4271;
    std::cout << 4271;
    for (int i = 0; i < 4271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4271(int& val) {
    const int x = 4271;
    std::cout << 4271;
    for (int i = 0; i < 4271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4272(int& val) {
    func_in_4272(val);
    const int x = 4272;
    std::cout << 4272;
    for (int i = 0; i < 4272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4272(int& val) {
    const int x = 4272;
    std::cout << 4272;
    for (int i = 0; i < 4272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4273(int& val) {
    func_in_4273(val);
    const int x = 4273;
    std::cout << 4273;
    for (int i = 0; i < 4273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4273(int& val) {
    const int x = 4273;
    std::cout << 4273;
    for (int i = 0; i < 4273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4274(int& val) {
    func_in_4274(val);
    const int x = 4274;
    std::cout << 4274;
    for (int i = 0; i < 4274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4274(int& val) {
    const int x = 4274;
    std::cout << 4274;
    for (int i = 0; i < 4274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4275(int& val) {
    func_in_4275(val);
    const int x = 4275;
    std::cout << 4275;
    for (int i = 0; i < 4275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4275(int& val) {
    const int x = 4275;
    std::cout << 4275;
    for (int i = 0; i < 4275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4276(int& val) {
    func_in_4276(val);
    const int x = 4276;
    std::cout << 4276;
    for (int i = 0; i < 4276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4276(int& val) {
    const int x = 4276;
    std::cout << 4276;
    for (int i = 0; i < 4276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4277(int& val) {
    func_in_4277(val);
    const int x = 4277;
    std::cout << 4277;
    for (int i = 0; i < 4277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4277(int& val) {
    const int x = 4277;
    std::cout << 4277;
    for (int i = 0; i < 4277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4278(int& val) {
    func_in_4278(val);
    const int x = 4278;
    std::cout << 4278;
    for (int i = 0; i < 4278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4278(int& val) {
    const int x = 4278;
    std::cout << 4278;
    for (int i = 0; i < 4278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4279(int& val) {
    func_in_4279(val);
    const int x = 4279;
    std::cout << 4279;
    for (int i = 0; i < 4279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4279(int& val) {
    const int x = 4279;
    std::cout << 4279;
    for (int i = 0; i < 4279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4280(int& val) {
    func_in_4280(val);
    const int x = 4280;
    std::cout << 4280;
    for (int i = 0; i < 4280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4280(int& val) {
    const int x = 4280;
    std::cout << 4280;
    for (int i = 0; i < 4280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4281(int& val) {
    func_in_4281(val);
    const int x = 4281;
    std::cout << 4281;
    for (int i = 0; i < 4281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4281(int& val) {
    const int x = 4281;
    std::cout << 4281;
    for (int i = 0; i < 4281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4282(int& val) {
    func_in_4282(val);
    const int x = 4282;
    std::cout << 4282;
    for (int i = 0; i < 4282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4282(int& val) {
    const int x = 4282;
    std::cout << 4282;
    for (int i = 0; i < 4282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4283(int& val) {
    func_in_4283(val);
    const int x = 4283;
    std::cout << 4283;
    for (int i = 0; i < 4283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4283(int& val) {
    const int x = 4283;
    std::cout << 4283;
    for (int i = 0; i < 4283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4284(int& val) {
    func_in_4284(val);
    const int x = 4284;
    std::cout << 4284;
    for (int i = 0; i < 4284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4284(int& val) {
    const int x = 4284;
    std::cout << 4284;
    for (int i = 0; i < 4284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4285(int& val) {
    func_in_4285(val);
    const int x = 4285;
    std::cout << 4285;
    for (int i = 0; i < 4285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4285(int& val) {
    const int x = 4285;
    std::cout << 4285;
    for (int i = 0; i < 4285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4286(int& val) {
    func_in_4286(val);
    const int x = 4286;
    std::cout << 4286;
    for (int i = 0; i < 4286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4286(int& val) {
    const int x = 4286;
    std::cout << 4286;
    for (int i = 0; i < 4286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4287(int& val) {
    func_in_4287(val);
    const int x = 4287;
    std::cout << 4287;
    for (int i = 0; i < 4287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4287(int& val) {
    const int x = 4287;
    std::cout << 4287;
    for (int i = 0; i < 4287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4288(int& val) {
    func_in_4288(val);
    const int x = 4288;
    std::cout << 4288;
    for (int i = 0; i < 4288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4288(int& val) {
    const int x = 4288;
    std::cout << 4288;
    for (int i = 0; i < 4288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4289(int& val) {
    func_in_4289(val);
    const int x = 4289;
    std::cout << 4289;
    for (int i = 0; i < 4289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4289(int& val) {
    const int x = 4289;
    std::cout << 4289;
    for (int i = 0; i < 4289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4290(int& val) {
    func_in_4290(val);
    const int x = 4290;
    std::cout << 4290;
    for (int i = 0; i < 4290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4290(int& val) {
    const int x = 4290;
    std::cout << 4290;
    for (int i = 0; i < 4290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4291(int& val) {
    func_in_4291(val);
    const int x = 4291;
    std::cout << 4291;
    for (int i = 0; i < 4291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4291(int& val) {
    const int x = 4291;
    std::cout << 4291;
    for (int i = 0; i < 4291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4292(int& val) {
    func_in_4292(val);
    const int x = 4292;
    std::cout << 4292;
    for (int i = 0; i < 4292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4292(int& val) {
    const int x = 4292;
    std::cout << 4292;
    for (int i = 0; i < 4292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4293(int& val) {
    func_in_4293(val);
    const int x = 4293;
    std::cout << 4293;
    for (int i = 0; i < 4293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4293(int& val) {
    const int x = 4293;
    std::cout << 4293;
    for (int i = 0; i < 4293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4294(int& val) {
    func_in_4294(val);
    const int x = 4294;
    std::cout << 4294;
    for (int i = 0; i < 4294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4294(int& val) {
    const int x = 4294;
    std::cout << 4294;
    for (int i = 0; i < 4294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4295(int& val) {
    func_in_4295(val);
    const int x = 4295;
    std::cout << 4295;
    for (int i = 0; i < 4295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4295(int& val) {
    const int x = 4295;
    std::cout << 4295;
    for (int i = 0; i < 4295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4296(int& val) {
    func_in_4296(val);
    const int x = 4296;
    std::cout << 4296;
    for (int i = 0; i < 4296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4296(int& val) {
    const int x = 4296;
    std::cout << 4296;
    for (int i = 0; i < 4296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4297(int& val) {
    func_in_4297(val);
    const int x = 4297;
    std::cout << 4297;
    for (int i = 0; i < 4297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4297(int& val) {
    const int x = 4297;
    std::cout << 4297;
    for (int i = 0; i < 4297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4298(int& val) {
    func_in_4298(val);
    const int x = 4298;
    std::cout << 4298;
    for (int i = 0; i < 4298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4298(int& val) {
    const int x = 4298;
    std::cout << 4298;
    for (int i = 0; i < 4298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4299(int& val) {
    func_in_4299(val);
    const int x = 4299;
    std::cout << 4299;
    for (int i = 0; i < 4299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4299(int& val) {
    const int x = 4299;
    std::cout << 4299;
    for (int i = 0; i < 4299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4300(int& val) {
    func_in_4300(val);
    const int x = 4300;
    std::cout << 4300;
    for (int i = 0; i < 4300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4300(int& val) {
    const int x = 4300;
    std::cout << 4300;
    for (int i = 0; i < 4300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4301(int& val) {
    func_in_4301(val);
    const int x = 4301;
    std::cout << 4301;
    for (int i = 0; i < 4301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4301(int& val) {
    const int x = 4301;
    std::cout << 4301;
    for (int i = 0; i < 4301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4302(int& val) {
    func_in_4302(val);
    const int x = 4302;
    std::cout << 4302;
    for (int i = 0; i < 4302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4302(int& val) {
    const int x = 4302;
    std::cout << 4302;
    for (int i = 0; i < 4302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4303(int& val) {
    func_in_4303(val);
    const int x = 4303;
    std::cout << 4303;
    for (int i = 0; i < 4303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4303(int& val) {
    const int x = 4303;
    std::cout << 4303;
    for (int i = 0; i < 4303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4304(int& val) {
    func_in_4304(val);
    const int x = 4304;
    std::cout << 4304;
    for (int i = 0; i < 4304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4304(int& val) {
    const int x = 4304;
    std::cout << 4304;
    for (int i = 0; i < 4304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4305(int& val) {
    func_in_4305(val);
    const int x = 4305;
    std::cout << 4305;
    for (int i = 0; i < 4305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4305(int& val) {
    const int x = 4305;
    std::cout << 4305;
    for (int i = 0; i < 4305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4306(int& val) {
    func_in_4306(val);
    const int x = 4306;
    std::cout << 4306;
    for (int i = 0; i < 4306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4306(int& val) {
    const int x = 4306;
    std::cout << 4306;
    for (int i = 0; i < 4306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4307(int& val) {
    func_in_4307(val);
    const int x = 4307;
    std::cout << 4307;
    for (int i = 0; i < 4307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4307(int& val) {
    const int x = 4307;
    std::cout << 4307;
    for (int i = 0; i < 4307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4308(int& val) {
    func_in_4308(val);
    const int x = 4308;
    std::cout << 4308;
    for (int i = 0; i < 4308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4308(int& val) {
    const int x = 4308;
    std::cout << 4308;
    for (int i = 0; i < 4308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4309(int& val) {
    func_in_4309(val);
    const int x = 4309;
    std::cout << 4309;
    for (int i = 0; i < 4309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4309(int& val) {
    const int x = 4309;
    std::cout << 4309;
    for (int i = 0; i < 4309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4310(int& val) {
    func_in_4310(val);
    const int x = 4310;
    std::cout << 4310;
    for (int i = 0; i < 4310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4310(int& val) {
    const int x = 4310;
    std::cout << 4310;
    for (int i = 0; i < 4310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4311(int& val) {
    func_in_4311(val);
    const int x = 4311;
    std::cout << 4311;
    for (int i = 0; i < 4311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4311(int& val) {
    const int x = 4311;
    std::cout << 4311;
    for (int i = 0; i < 4311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4312(int& val) {
    func_in_4312(val);
    const int x = 4312;
    std::cout << 4312;
    for (int i = 0; i < 4312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4312(int& val) {
    const int x = 4312;
    std::cout << 4312;
    for (int i = 0; i < 4312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4313(int& val) {
    func_in_4313(val);
    const int x = 4313;
    std::cout << 4313;
    for (int i = 0; i < 4313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4313(int& val) {
    const int x = 4313;
    std::cout << 4313;
    for (int i = 0; i < 4313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4314(int& val) {
    func_in_4314(val);
    const int x = 4314;
    std::cout << 4314;
    for (int i = 0; i < 4314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4314(int& val) {
    const int x = 4314;
    std::cout << 4314;
    for (int i = 0; i < 4314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4315(int& val) {
    func_in_4315(val);
    const int x = 4315;
    std::cout << 4315;
    for (int i = 0; i < 4315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4315(int& val) {
    const int x = 4315;
    std::cout << 4315;
    for (int i = 0; i < 4315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4316(int& val) {
    func_in_4316(val);
    const int x = 4316;
    std::cout << 4316;
    for (int i = 0; i < 4316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4316(int& val) {
    const int x = 4316;
    std::cout << 4316;
    for (int i = 0; i < 4316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4317(int& val) {
    func_in_4317(val);
    const int x = 4317;
    std::cout << 4317;
    for (int i = 0; i < 4317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4317(int& val) {
    const int x = 4317;
    std::cout << 4317;
    for (int i = 0; i < 4317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4318(int& val) {
    func_in_4318(val);
    const int x = 4318;
    std::cout << 4318;
    for (int i = 0; i < 4318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4318(int& val) {
    const int x = 4318;
    std::cout << 4318;
    for (int i = 0; i < 4318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4319(int& val) {
    func_in_4319(val);
    const int x = 4319;
    std::cout << 4319;
    for (int i = 0; i < 4319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4319(int& val) {
    const int x = 4319;
    std::cout << 4319;
    for (int i = 0; i < 4319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4320(int& val) {
    func_in_4320(val);
    const int x = 4320;
    std::cout << 4320;
    for (int i = 0; i < 4320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4320(int& val) {
    const int x = 4320;
    std::cout << 4320;
    for (int i = 0; i < 4320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4321(int& val) {
    func_in_4321(val);
    const int x = 4321;
    std::cout << 4321;
    for (int i = 0; i < 4321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4321(int& val) {
    const int x = 4321;
    std::cout << 4321;
    for (int i = 0; i < 4321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4322(int& val) {
    func_in_4322(val);
    const int x = 4322;
    std::cout << 4322;
    for (int i = 0; i < 4322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4322(int& val) {
    const int x = 4322;
    std::cout << 4322;
    for (int i = 0; i < 4322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4323(int& val) {
    func_in_4323(val);
    const int x = 4323;
    std::cout << 4323;
    for (int i = 0; i < 4323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4323(int& val) {
    const int x = 4323;
    std::cout << 4323;
    for (int i = 0; i < 4323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4324(int& val) {
    func_in_4324(val);
    const int x = 4324;
    std::cout << 4324;
    for (int i = 0; i < 4324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4324(int& val) {
    const int x = 4324;
    std::cout << 4324;
    for (int i = 0; i < 4324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4325(int& val) {
    func_in_4325(val);
    const int x = 4325;
    std::cout << 4325;
    for (int i = 0; i < 4325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4325(int& val) {
    const int x = 4325;
    std::cout << 4325;
    for (int i = 0; i < 4325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4326(int& val) {
    func_in_4326(val);
    const int x = 4326;
    std::cout << 4326;
    for (int i = 0; i < 4326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4326(int& val) {
    const int x = 4326;
    std::cout << 4326;
    for (int i = 0; i < 4326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4327(int& val) {
    func_in_4327(val);
    const int x = 4327;
    std::cout << 4327;
    for (int i = 0; i < 4327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4327(int& val) {
    const int x = 4327;
    std::cout << 4327;
    for (int i = 0; i < 4327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4328(int& val) {
    func_in_4328(val);
    const int x = 4328;
    std::cout << 4328;
    for (int i = 0; i < 4328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4328(int& val) {
    const int x = 4328;
    std::cout << 4328;
    for (int i = 0; i < 4328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4329(int& val) {
    func_in_4329(val);
    const int x = 4329;
    std::cout << 4329;
    for (int i = 0; i < 4329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4329(int& val) {
    const int x = 4329;
    std::cout << 4329;
    for (int i = 0; i < 4329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4330(int& val) {
    func_in_4330(val);
    const int x = 4330;
    std::cout << 4330;
    for (int i = 0; i < 4330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4330(int& val) {
    const int x = 4330;
    std::cout << 4330;
    for (int i = 0; i < 4330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4331(int& val) {
    func_in_4331(val);
    const int x = 4331;
    std::cout << 4331;
    for (int i = 0; i < 4331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4331(int& val) {
    const int x = 4331;
    std::cout << 4331;
    for (int i = 0; i < 4331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4332(int& val) {
    func_in_4332(val);
    const int x = 4332;
    std::cout << 4332;
    for (int i = 0; i < 4332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4332(int& val) {
    const int x = 4332;
    std::cout << 4332;
    for (int i = 0; i < 4332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4333(int& val) {
    func_in_4333(val);
    const int x = 4333;
    std::cout << 4333;
    for (int i = 0; i < 4333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4333(int& val) {
    const int x = 4333;
    std::cout << 4333;
    for (int i = 0; i < 4333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4334(int& val) {
    func_in_4334(val);
    const int x = 4334;
    std::cout << 4334;
    for (int i = 0; i < 4334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4334(int& val) {
    const int x = 4334;
    std::cout << 4334;
    for (int i = 0; i < 4334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4335(int& val) {
    func_in_4335(val);
    const int x = 4335;
    std::cout << 4335;
    for (int i = 0; i < 4335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4335(int& val) {
    const int x = 4335;
    std::cout << 4335;
    for (int i = 0; i < 4335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4336(int& val) {
    func_in_4336(val);
    const int x = 4336;
    std::cout << 4336;
    for (int i = 0; i < 4336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4336(int& val) {
    const int x = 4336;
    std::cout << 4336;
    for (int i = 0; i < 4336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4337(int& val) {
    func_in_4337(val);
    const int x = 4337;
    std::cout << 4337;
    for (int i = 0; i < 4337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4337(int& val) {
    const int x = 4337;
    std::cout << 4337;
    for (int i = 0; i < 4337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4338(int& val) {
    func_in_4338(val);
    const int x = 4338;
    std::cout << 4338;
    for (int i = 0; i < 4338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4338(int& val) {
    const int x = 4338;
    std::cout << 4338;
    for (int i = 0; i < 4338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4339(int& val) {
    func_in_4339(val);
    const int x = 4339;
    std::cout << 4339;
    for (int i = 0; i < 4339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4339(int& val) {
    const int x = 4339;
    std::cout << 4339;
    for (int i = 0; i < 4339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4340(int& val) {
    func_in_4340(val);
    const int x = 4340;
    std::cout << 4340;
    for (int i = 0; i < 4340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4340(int& val) {
    const int x = 4340;
    std::cout << 4340;
    for (int i = 0; i < 4340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4341(int& val) {
    func_in_4341(val);
    const int x = 4341;
    std::cout << 4341;
    for (int i = 0; i < 4341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4341(int& val) {
    const int x = 4341;
    std::cout << 4341;
    for (int i = 0; i < 4341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4342(int& val) {
    func_in_4342(val);
    const int x = 4342;
    std::cout << 4342;
    for (int i = 0; i < 4342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4342(int& val) {
    const int x = 4342;
    std::cout << 4342;
    for (int i = 0; i < 4342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4343(int& val) {
    func_in_4343(val);
    const int x = 4343;
    std::cout << 4343;
    for (int i = 0; i < 4343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4343(int& val) {
    const int x = 4343;
    std::cout << 4343;
    for (int i = 0; i < 4343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4344(int& val) {
    func_in_4344(val);
    const int x = 4344;
    std::cout << 4344;
    for (int i = 0; i < 4344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4344(int& val) {
    const int x = 4344;
    std::cout << 4344;
    for (int i = 0; i < 4344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4345(int& val) {
    func_in_4345(val);
    const int x = 4345;
    std::cout << 4345;
    for (int i = 0; i < 4345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4345(int& val) {
    const int x = 4345;
    std::cout << 4345;
    for (int i = 0; i < 4345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4346(int& val) {
    func_in_4346(val);
    const int x = 4346;
    std::cout << 4346;
    for (int i = 0; i < 4346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4346(int& val) {
    const int x = 4346;
    std::cout << 4346;
    for (int i = 0; i < 4346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4347(int& val) {
    func_in_4347(val);
    const int x = 4347;
    std::cout << 4347;
    for (int i = 0; i < 4347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4347(int& val) {
    const int x = 4347;
    std::cout << 4347;
    for (int i = 0; i < 4347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4348(int& val) {
    func_in_4348(val);
    const int x = 4348;
    std::cout << 4348;
    for (int i = 0; i < 4348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4348(int& val) {
    const int x = 4348;
    std::cout << 4348;
    for (int i = 0; i < 4348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4349(int& val) {
    func_in_4349(val);
    const int x = 4349;
    std::cout << 4349;
    for (int i = 0; i < 4349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4349(int& val) {
    const int x = 4349;
    std::cout << 4349;
    for (int i = 0; i < 4349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4350(int& val) {
    func_in_4350(val);
    const int x = 4350;
    std::cout << 4350;
    for (int i = 0; i < 4350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4350(int& val) {
    const int x = 4350;
    std::cout << 4350;
    for (int i = 0; i < 4350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4351(int& val) {
    func_in_4351(val);
    const int x = 4351;
    std::cout << 4351;
    for (int i = 0; i < 4351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4351(int& val) {
    const int x = 4351;
    std::cout << 4351;
    for (int i = 0; i < 4351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4352(int& val) {
    func_in_4352(val);
    const int x = 4352;
    std::cout << 4352;
    for (int i = 0; i < 4352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4352(int& val) {
    const int x = 4352;
    std::cout << 4352;
    for (int i = 0; i < 4352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4353(int& val) {
    func_in_4353(val);
    const int x = 4353;
    std::cout << 4353;
    for (int i = 0; i < 4353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4353(int& val) {
    const int x = 4353;
    std::cout << 4353;
    for (int i = 0; i < 4353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4354(int& val) {
    func_in_4354(val);
    const int x = 4354;
    std::cout << 4354;
    for (int i = 0; i < 4354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4354(int& val) {
    const int x = 4354;
    std::cout << 4354;
    for (int i = 0; i < 4354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4355(int& val) {
    func_in_4355(val);
    const int x = 4355;
    std::cout << 4355;
    for (int i = 0; i < 4355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4355(int& val) {
    const int x = 4355;
    std::cout << 4355;
    for (int i = 0; i < 4355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4356(int& val) {
    func_in_4356(val);
    const int x = 4356;
    std::cout << 4356;
    for (int i = 0; i < 4356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4356(int& val) {
    const int x = 4356;
    std::cout << 4356;
    for (int i = 0; i < 4356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4357(int& val) {
    func_in_4357(val);
    const int x = 4357;
    std::cout << 4357;
    for (int i = 0; i < 4357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4357(int& val) {
    const int x = 4357;
    std::cout << 4357;
    for (int i = 0; i < 4357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4358(int& val) {
    func_in_4358(val);
    const int x = 4358;
    std::cout << 4358;
    for (int i = 0; i < 4358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4358(int& val) {
    const int x = 4358;
    std::cout << 4358;
    for (int i = 0; i < 4358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4359(int& val) {
    func_in_4359(val);
    const int x = 4359;
    std::cout << 4359;
    for (int i = 0; i < 4359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4359(int& val) {
    const int x = 4359;
    std::cout << 4359;
    for (int i = 0; i < 4359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4360(int& val) {
    func_in_4360(val);
    const int x = 4360;
    std::cout << 4360;
    for (int i = 0; i < 4360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4360(int& val) {
    const int x = 4360;
    std::cout << 4360;
    for (int i = 0; i < 4360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4361(int& val) {
    func_in_4361(val);
    const int x = 4361;
    std::cout << 4361;
    for (int i = 0; i < 4361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4361(int& val) {
    const int x = 4361;
    std::cout << 4361;
    for (int i = 0; i < 4361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4362(int& val) {
    func_in_4362(val);
    const int x = 4362;
    std::cout << 4362;
    for (int i = 0; i < 4362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4362(int& val) {
    const int x = 4362;
    std::cout << 4362;
    for (int i = 0; i < 4362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4363(int& val) {
    func_in_4363(val);
    const int x = 4363;
    std::cout << 4363;
    for (int i = 0; i < 4363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4363(int& val) {
    const int x = 4363;
    std::cout << 4363;
    for (int i = 0; i < 4363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4364(int& val) {
    func_in_4364(val);
    const int x = 4364;
    std::cout << 4364;
    for (int i = 0; i < 4364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4364(int& val) {
    const int x = 4364;
    std::cout << 4364;
    for (int i = 0; i < 4364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4365(int& val) {
    func_in_4365(val);
    const int x = 4365;
    std::cout << 4365;
    for (int i = 0; i < 4365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4365(int& val) {
    const int x = 4365;
    std::cout << 4365;
    for (int i = 0; i < 4365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4366(int& val) {
    func_in_4366(val);
    const int x = 4366;
    std::cout << 4366;
    for (int i = 0; i < 4366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4366(int& val) {
    const int x = 4366;
    std::cout << 4366;
    for (int i = 0; i < 4366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4367(int& val) {
    func_in_4367(val);
    const int x = 4367;
    std::cout << 4367;
    for (int i = 0; i < 4367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4367(int& val) {
    const int x = 4367;
    std::cout << 4367;
    for (int i = 0; i < 4367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4368(int& val) {
    func_in_4368(val);
    const int x = 4368;
    std::cout << 4368;
    for (int i = 0; i < 4368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4368(int& val) {
    const int x = 4368;
    std::cout << 4368;
    for (int i = 0; i < 4368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4369(int& val) {
    func_in_4369(val);
    const int x = 4369;
    std::cout << 4369;
    for (int i = 0; i < 4369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4369(int& val) {
    const int x = 4369;
    std::cout << 4369;
    for (int i = 0; i < 4369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4370(int& val) {
    func_in_4370(val);
    const int x = 4370;
    std::cout << 4370;
    for (int i = 0; i < 4370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4370(int& val) {
    const int x = 4370;
    std::cout << 4370;
    for (int i = 0; i < 4370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4371(int& val) {
    func_in_4371(val);
    const int x = 4371;
    std::cout << 4371;
    for (int i = 0; i < 4371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4371(int& val) {
    const int x = 4371;
    std::cout << 4371;
    for (int i = 0; i < 4371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4372(int& val) {
    func_in_4372(val);
    const int x = 4372;
    std::cout << 4372;
    for (int i = 0; i < 4372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4372(int& val) {
    const int x = 4372;
    std::cout << 4372;
    for (int i = 0; i < 4372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4373(int& val) {
    func_in_4373(val);
    const int x = 4373;
    std::cout << 4373;
    for (int i = 0; i < 4373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4373(int& val) {
    const int x = 4373;
    std::cout << 4373;
    for (int i = 0; i < 4373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4374(int& val) {
    func_in_4374(val);
    const int x = 4374;
    std::cout << 4374;
    for (int i = 0; i < 4374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4374(int& val) {
    const int x = 4374;
    std::cout << 4374;
    for (int i = 0; i < 4374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4375(int& val) {
    func_in_4375(val);
    const int x = 4375;
    std::cout << 4375;
    for (int i = 0; i < 4375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4375(int& val) {
    const int x = 4375;
    std::cout << 4375;
    for (int i = 0; i < 4375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4376(int& val) {
    func_in_4376(val);
    const int x = 4376;
    std::cout << 4376;
    for (int i = 0; i < 4376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4376(int& val) {
    const int x = 4376;
    std::cout << 4376;
    for (int i = 0; i < 4376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4377(int& val) {
    func_in_4377(val);
    const int x = 4377;
    std::cout << 4377;
    for (int i = 0; i < 4377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4377(int& val) {
    const int x = 4377;
    std::cout << 4377;
    for (int i = 0; i < 4377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4378(int& val) {
    func_in_4378(val);
    const int x = 4378;
    std::cout << 4378;
    for (int i = 0; i < 4378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4378(int& val) {
    const int x = 4378;
    std::cout << 4378;
    for (int i = 0; i < 4378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4379(int& val) {
    func_in_4379(val);
    const int x = 4379;
    std::cout << 4379;
    for (int i = 0; i < 4379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4379(int& val) {
    const int x = 4379;
    std::cout << 4379;
    for (int i = 0; i < 4379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4380(int& val) {
    func_in_4380(val);
    const int x = 4380;
    std::cout << 4380;
    for (int i = 0; i < 4380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4380(int& val) {
    const int x = 4380;
    std::cout << 4380;
    for (int i = 0; i < 4380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4381(int& val) {
    func_in_4381(val);
    const int x = 4381;
    std::cout << 4381;
    for (int i = 0; i < 4381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4381(int& val) {
    const int x = 4381;
    std::cout << 4381;
    for (int i = 0; i < 4381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4382(int& val) {
    func_in_4382(val);
    const int x = 4382;
    std::cout << 4382;
    for (int i = 0; i < 4382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4382(int& val) {
    const int x = 4382;
    std::cout << 4382;
    for (int i = 0; i < 4382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4383(int& val) {
    func_in_4383(val);
    const int x = 4383;
    std::cout << 4383;
    for (int i = 0; i < 4383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4383(int& val) {
    const int x = 4383;
    std::cout << 4383;
    for (int i = 0; i < 4383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4384(int& val) {
    func_in_4384(val);
    const int x = 4384;
    std::cout << 4384;
    for (int i = 0; i < 4384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4384(int& val) {
    const int x = 4384;
    std::cout << 4384;
    for (int i = 0; i < 4384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4385(int& val) {
    func_in_4385(val);
    const int x = 4385;
    std::cout << 4385;
    for (int i = 0; i < 4385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4385(int& val) {
    const int x = 4385;
    std::cout << 4385;
    for (int i = 0; i < 4385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4386(int& val) {
    func_in_4386(val);
    const int x = 4386;
    std::cout << 4386;
    for (int i = 0; i < 4386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4386(int& val) {
    const int x = 4386;
    std::cout << 4386;
    for (int i = 0; i < 4386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4387(int& val) {
    func_in_4387(val);
    const int x = 4387;
    std::cout << 4387;
    for (int i = 0; i < 4387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4387(int& val) {
    const int x = 4387;
    std::cout << 4387;
    for (int i = 0; i < 4387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4388(int& val) {
    func_in_4388(val);
    const int x = 4388;
    std::cout << 4388;
    for (int i = 0; i < 4388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4388(int& val) {
    const int x = 4388;
    std::cout << 4388;
    for (int i = 0; i < 4388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4389(int& val) {
    func_in_4389(val);
    const int x = 4389;
    std::cout << 4389;
    for (int i = 0; i < 4389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4389(int& val) {
    const int x = 4389;
    std::cout << 4389;
    for (int i = 0; i < 4389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4390(int& val) {
    func_in_4390(val);
    const int x = 4390;
    std::cout << 4390;
    for (int i = 0; i < 4390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4390(int& val) {
    const int x = 4390;
    std::cout << 4390;
    for (int i = 0; i < 4390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4391(int& val) {
    func_in_4391(val);
    const int x = 4391;
    std::cout << 4391;
    for (int i = 0; i < 4391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4391(int& val) {
    const int x = 4391;
    std::cout << 4391;
    for (int i = 0; i < 4391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4392(int& val) {
    func_in_4392(val);
    const int x = 4392;
    std::cout << 4392;
    for (int i = 0; i < 4392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4392(int& val) {
    const int x = 4392;
    std::cout << 4392;
    for (int i = 0; i < 4392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4393(int& val) {
    func_in_4393(val);
    const int x = 4393;
    std::cout << 4393;
    for (int i = 0; i < 4393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4393(int& val) {
    const int x = 4393;
    std::cout << 4393;
    for (int i = 0; i < 4393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4394(int& val) {
    func_in_4394(val);
    const int x = 4394;
    std::cout << 4394;
    for (int i = 0; i < 4394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4394(int& val) {
    const int x = 4394;
    std::cout << 4394;
    for (int i = 0; i < 4394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4395(int& val) {
    func_in_4395(val);
    const int x = 4395;
    std::cout << 4395;
    for (int i = 0; i < 4395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4395(int& val) {
    const int x = 4395;
    std::cout << 4395;
    for (int i = 0; i < 4395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4396(int& val) {
    func_in_4396(val);
    const int x = 4396;
    std::cout << 4396;
    for (int i = 0; i < 4396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4396(int& val) {
    const int x = 4396;
    std::cout << 4396;
    for (int i = 0; i < 4396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4397(int& val) {
    func_in_4397(val);
    const int x = 4397;
    std::cout << 4397;
    for (int i = 0; i < 4397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4397(int& val) {
    const int x = 4397;
    std::cout << 4397;
    for (int i = 0; i < 4397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4398(int& val) {
    func_in_4398(val);
    const int x = 4398;
    std::cout << 4398;
    for (int i = 0; i < 4398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4398(int& val) {
    const int x = 4398;
    std::cout << 4398;
    for (int i = 0; i < 4398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4399(int& val) {
    func_in_4399(val);
    const int x = 4399;
    std::cout << 4399;
    for (int i = 0; i < 4399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4399(int& val) {
    const int x = 4399;
    std::cout << 4399;
    for (int i = 0; i < 4399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4400(int& val) {
    func_in_4400(val);
    const int x = 4400;
    std::cout << 4400;
    for (int i = 0; i < 4400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4400(int& val) {
    const int x = 4400;
    std::cout << 4400;
    for (int i = 0; i < 4400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4401(int& val) {
    func_in_4401(val);
    const int x = 4401;
    std::cout << 4401;
    for (int i = 0; i < 4401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4401(int& val) {
    const int x = 4401;
    std::cout << 4401;
    for (int i = 0; i < 4401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4402(int& val) {
    func_in_4402(val);
    const int x = 4402;
    std::cout << 4402;
    for (int i = 0; i < 4402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4402(int& val) {
    const int x = 4402;
    std::cout << 4402;
    for (int i = 0; i < 4402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4403(int& val) {
    func_in_4403(val);
    const int x = 4403;
    std::cout << 4403;
    for (int i = 0; i < 4403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4403(int& val) {
    const int x = 4403;
    std::cout << 4403;
    for (int i = 0; i < 4403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4404(int& val) {
    func_in_4404(val);
    const int x = 4404;
    std::cout << 4404;
    for (int i = 0; i < 4404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4404(int& val) {
    const int x = 4404;
    std::cout << 4404;
    for (int i = 0; i < 4404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4405(int& val) {
    func_in_4405(val);
    const int x = 4405;
    std::cout << 4405;
    for (int i = 0; i < 4405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4405(int& val) {
    const int x = 4405;
    std::cout << 4405;
    for (int i = 0; i < 4405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4406(int& val) {
    func_in_4406(val);
    const int x = 4406;
    std::cout << 4406;
    for (int i = 0; i < 4406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4406(int& val) {
    const int x = 4406;
    std::cout << 4406;
    for (int i = 0; i < 4406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4407(int& val) {
    func_in_4407(val);
    const int x = 4407;
    std::cout << 4407;
    for (int i = 0; i < 4407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4407(int& val) {
    const int x = 4407;
    std::cout << 4407;
    for (int i = 0; i < 4407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4408(int& val) {
    func_in_4408(val);
    const int x = 4408;
    std::cout << 4408;
    for (int i = 0; i < 4408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4408(int& val) {
    const int x = 4408;
    std::cout << 4408;
    for (int i = 0; i < 4408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4409(int& val) {
    func_in_4409(val);
    const int x = 4409;
    std::cout << 4409;
    for (int i = 0; i < 4409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4409(int& val) {
    const int x = 4409;
    std::cout << 4409;
    for (int i = 0; i < 4409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4410(int& val) {
    func_in_4410(val);
    const int x = 4410;
    std::cout << 4410;
    for (int i = 0; i < 4410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4410(int& val) {
    const int x = 4410;
    std::cout << 4410;
    for (int i = 0; i < 4410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4411(int& val) {
    func_in_4411(val);
    const int x = 4411;
    std::cout << 4411;
    for (int i = 0; i < 4411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4411(int& val) {
    const int x = 4411;
    std::cout << 4411;
    for (int i = 0; i < 4411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4412(int& val) {
    func_in_4412(val);
    const int x = 4412;
    std::cout << 4412;
    for (int i = 0; i < 4412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4412(int& val) {
    const int x = 4412;
    std::cout << 4412;
    for (int i = 0; i < 4412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4413(int& val) {
    func_in_4413(val);
    const int x = 4413;
    std::cout << 4413;
    for (int i = 0; i < 4413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4413(int& val) {
    const int x = 4413;
    std::cout << 4413;
    for (int i = 0; i < 4413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4414(int& val) {
    func_in_4414(val);
    const int x = 4414;
    std::cout << 4414;
    for (int i = 0; i < 4414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4414(int& val) {
    const int x = 4414;
    std::cout << 4414;
    for (int i = 0; i < 4414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4415(int& val) {
    func_in_4415(val);
    const int x = 4415;
    std::cout << 4415;
    for (int i = 0; i < 4415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4415(int& val) {
    const int x = 4415;
    std::cout << 4415;
    for (int i = 0; i < 4415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4416(int& val) {
    func_in_4416(val);
    const int x = 4416;
    std::cout << 4416;
    for (int i = 0; i < 4416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4416(int& val) {
    const int x = 4416;
    std::cout << 4416;
    for (int i = 0; i < 4416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4417(int& val) {
    func_in_4417(val);
    const int x = 4417;
    std::cout << 4417;
    for (int i = 0; i < 4417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4417(int& val) {
    const int x = 4417;
    std::cout << 4417;
    for (int i = 0; i < 4417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4418(int& val) {
    func_in_4418(val);
    const int x = 4418;
    std::cout << 4418;
    for (int i = 0; i < 4418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4418(int& val) {
    const int x = 4418;
    std::cout << 4418;
    for (int i = 0; i < 4418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4419(int& val) {
    func_in_4419(val);
    const int x = 4419;
    std::cout << 4419;
    for (int i = 0; i < 4419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4419(int& val) {
    const int x = 4419;
    std::cout << 4419;
    for (int i = 0; i < 4419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4420(int& val) {
    func_in_4420(val);
    const int x = 4420;
    std::cout << 4420;
    for (int i = 0; i < 4420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4420(int& val) {
    const int x = 4420;
    std::cout << 4420;
    for (int i = 0; i < 4420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4421(int& val) {
    func_in_4421(val);
    const int x = 4421;
    std::cout << 4421;
    for (int i = 0; i < 4421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4421(int& val) {
    const int x = 4421;
    std::cout << 4421;
    for (int i = 0; i < 4421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4422(int& val) {
    func_in_4422(val);
    const int x = 4422;
    std::cout << 4422;
    for (int i = 0; i < 4422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4422(int& val) {
    const int x = 4422;
    std::cout << 4422;
    for (int i = 0; i < 4422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4423(int& val) {
    func_in_4423(val);
    const int x = 4423;
    std::cout << 4423;
    for (int i = 0; i < 4423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4423(int& val) {
    const int x = 4423;
    std::cout << 4423;
    for (int i = 0; i < 4423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4424(int& val) {
    func_in_4424(val);
    const int x = 4424;
    std::cout << 4424;
    for (int i = 0; i < 4424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4424(int& val) {
    const int x = 4424;
    std::cout << 4424;
    for (int i = 0; i < 4424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4425(int& val) {
    func_in_4425(val);
    const int x = 4425;
    std::cout << 4425;
    for (int i = 0; i < 4425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4425(int& val) {
    const int x = 4425;
    std::cout << 4425;
    for (int i = 0; i < 4425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4426(int& val) {
    func_in_4426(val);
    const int x = 4426;
    std::cout << 4426;
    for (int i = 0; i < 4426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4426(int& val) {
    const int x = 4426;
    std::cout << 4426;
    for (int i = 0; i < 4426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4427(int& val) {
    func_in_4427(val);
    const int x = 4427;
    std::cout << 4427;
    for (int i = 0; i < 4427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4427(int& val) {
    const int x = 4427;
    std::cout << 4427;
    for (int i = 0; i < 4427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4428(int& val) {
    func_in_4428(val);
    const int x = 4428;
    std::cout << 4428;
    for (int i = 0; i < 4428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4428(int& val) {
    const int x = 4428;
    std::cout << 4428;
    for (int i = 0; i < 4428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4429(int& val) {
    func_in_4429(val);
    const int x = 4429;
    std::cout << 4429;
    for (int i = 0; i < 4429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4429(int& val) {
    const int x = 4429;
    std::cout << 4429;
    for (int i = 0; i < 4429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4430(int& val) {
    func_in_4430(val);
    const int x = 4430;
    std::cout << 4430;
    for (int i = 0; i < 4430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4430(int& val) {
    const int x = 4430;
    std::cout << 4430;
    for (int i = 0; i < 4430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4431(int& val) {
    func_in_4431(val);
    const int x = 4431;
    std::cout << 4431;
    for (int i = 0; i < 4431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4431(int& val) {
    const int x = 4431;
    std::cout << 4431;
    for (int i = 0; i < 4431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4432(int& val) {
    func_in_4432(val);
    const int x = 4432;
    std::cout << 4432;
    for (int i = 0; i < 4432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4432(int& val) {
    const int x = 4432;
    std::cout << 4432;
    for (int i = 0; i < 4432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4433(int& val) {
    func_in_4433(val);
    const int x = 4433;
    std::cout << 4433;
    for (int i = 0; i < 4433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4433(int& val) {
    const int x = 4433;
    std::cout << 4433;
    for (int i = 0; i < 4433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4434(int& val) {
    func_in_4434(val);
    const int x = 4434;
    std::cout << 4434;
    for (int i = 0; i < 4434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4434(int& val) {
    const int x = 4434;
    std::cout << 4434;
    for (int i = 0; i < 4434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4435(int& val) {
    func_in_4435(val);
    const int x = 4435;
    std::cout << 4435;
    for (int i = 0; i < 4435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4435(int& val) {
    const int x = 4435;
    std::cout << 4435;
    for (int i = 0; i < 4435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4436(int& val) {
    func_in_4436(val);
    const int x = 4436;
    std::cout << 4436;
    for (int i = 0; i < 4436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4436(int& val) {
    const int x = 4436;
    std::cout << 4436;
    for (int i = 0; i < 4436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4437(int& val) {
    func_in_4437(val);
    const int x = 4437;
    std::cout << 4437;
    for (int i = 0; i < 4437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4437(int& val) {
    const int x = 4437;
    std::cout << 4437;
    for (int i = 0; i < 4437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4438(int& val) {
    func_in_4438(val);
    const int x = 4438;
    std::cout << 4438;
    for (int i = 0; i < 4438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4438(int& val) {
    const int x = 4438;
    std::cout << 4438;
    for (int i = 0; i < 4438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4439(int& val) {
    func_in_4439(val);
    const int x = 4439;
    std::cout << 4439;
    for (int i = 0; i < 4439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4439(int& val) {
    const int x = 4439;
    std::cout << 4439;
    for (int i = 0; i < 4439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4440(int& val) {
    func_in_4440(val);
    const int x = 4440;
    std::cout << 4440;
    for (int i = 0; i < 4440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4440(int& val) {
    const int x = 4440;
    std::cout << 4440;
    for (int i = 0; i < 4440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4441(int& val) {
    func_in_4441(val);
    const int x = 4441;
    std::cout << 4441;
    for (int i = 0; i < 4441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4441(int& val) {
    const int x = 4441;
    std::cout << 4441;
    for (int i = 0; i < 4441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4442(int& val) {
    func_in_4442(val);
    const int x = 4442;
    std::cout << 4442;
    for (int i = 0; i < 4442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4442(int& val) {
    const int x = 4442;
    std::cout << 4442;
    for (int i = 0; i < 4442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4443(int& val) {
    func_in_4443(val);
    const int x = 4443;
    std::cout << 4443;
    for (int i = 0; i < 4443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4443(int& val) {
    const int x = 4443;
    std::cout << 4443;
    for (int i = 0; i < 4443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4444(int& val) {
    func_in_4444(val);
    const int x = 4444;
    std::cout << 4444;
    for (int i = 0; i < 4444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4444(int& val) {
    const int x = 4444;
    std::cout << 4444;
    for (int i = 0; i < 4444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4445(int& val) {
    func_in_4445(val);
    const int x = 4445;
    std::cout << 4445;
    for (int i = 0; i < 4445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4445(int& val) {
    const int x = 4445;
    std::cout << 4445;
    for (int i = 0; i < 4445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4446(int& val) {
    func_in_4446(val);
    const int x = 4446;
    std::cout << 4446;
    for (int i = 0; i < 4446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4446(int& val) {
    const int x = 4446;
    std::cout << 4446;
    for (int i = 0; i < 4446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4447(int& val) {
    func_in_4447(val);
    const int x = 4447;
    std::cout << 4447;
    for (int i = 0; i < 4447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4447(int& val) {
    const int x = 4447;
    std::cout << 4447;
    for (int i = 0; i < 4447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4448(int& val) {
    func_in_4448(val);
    const int x = 4448;
    std::cout << 4448;
    for (int i = 0; i < 4448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4448(int& val) {
    const int x = 4448;
    std::cout << 4448;
    for (int i = 0; i < 4448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4449(int& val) {
    func_in_4449(val);
    const int x = 4449;
    std::cout << 4449;
    for (int i = 0; i < 4449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4449(int& val) {
    const int x = 4449;
    std::cout << 4449;
    for (int i = 0; i < 4449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4450(int& val) {
    func_in_4450(val);
    const int x = 4450;
    std::cout << 4450;
    for (int i = 0; i < 4450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4450(int& val) {
    const int x = 4450;
    std::cout << 4450;
    for (int i = 0; i < 4450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4451(int& val) {
    func_in_4451(val);
    const int x = 4451;
    std::cout << 4451;
    for (int i = 0; i < 4451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4451(int& val) {
    const int x = 4451;
    std::cout << 4451;
    for (int i = 0; i < 4451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4452(int& val) {
    func_in_4452(val);
    const int x = 4452;
    std::cout << 4452;
    for (int i = 0; i < 4452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4452(int& val) {
    const int x = 4452;
    std::cout << 4452;
    for (int i = 0; i < 4452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4453(int& val) {
    func_in_4453(val);
    const int x = 4453;
    std::cout << 4453;
    for (int i = 0; i < 4453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4453(int& val) {
    const int x = 4453;
    std::cout << 4453;
    for (int i = 0; i < 4453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4454(int& val) {
    func_in_4454(val);
    const int x = 4454;
    std::cout << 4454;
    for (int i = 0; i < 4454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4454(int& val) {
    const int x = 4454;
    std::cout << 4454;
    for (int i = 0; i < 4454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4455(int& val) {
    func_in_4455(val);
    const int x = 4455;
    std::cout << 4455;
    for (int i = 0; i < 4455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4455(int& val) {
    const int x = 4455;
    std::cout << 4455;
    for (int i = 0; i < 4455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4456(int& val) {
    func_in_4456(val);
    const int x = 4456;
    std::cout << 4456;
    for (int i = 0; i < 4456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4456(int& val) {
    const int x = 4456;
    std::cout << 4456;
    for (int i = 0; i < 4456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4457(int& val) {
    func_in_4457(val);
    const int x = 4457;
    std::cout << 4457;
    for (int i = 0; i < 4457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4457(int& val) {
    const int x = 4457;
    std::cout << 4457;
    for (int i = 0; i < 4457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4458(int& val) {
    func_in_4458(val);
    const int x = 4458;
    std::cout << 4458;
    for (int i = 0; i < 4458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4458(int& val) {
    const int x = 4458;
    std::cout << 4458;
    for (int i = 0; i < 4458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4459(int& val) {
    func_in_4459(val);
    const int x = 4459;
    std::cout << 4459;
    for (int i = 0; i < 4459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4459(int& val) {
    const int x = 4459;
    std::cout << 4459;
    for (int i = 0; i < 4459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4460(int& val) {
    func_in_4460(val);
    const int x = 4460;
    std::cout << 4460;
    for (int i = 0; i < 4460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4460(int& val) {
    const int x = 4460;
    std::cout << 4460;
    for (int i = 0; i < 4460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4461(int& val) {
    func_in_4461(val);
    const int x = 4461;
    std::cout << 4461;
    for (int i = 0; i < 4461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4461(int& val) {
    const int x = 4461;
    std::cout << 4461;
    for (int i = 0; i < 4461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4462(int& val) {
    func_in_4462(val);
    const int x = 4462;
    std::cout << 4462;
    for (int i = 0; i < 4462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4462(int& val) {
    const int x = 4462;
    std::cout << 4462;
    for (int i = 0; i < 4462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4463(int& val) {
    func_in_4463(val);
    const int x = 4463;
    std::cout << 4463;
    for (int i = 0; i < 4463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4463(int& val) {
    const int x = 4463;
    std::cout << 4463;
    for (int i = 0; i < 4463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4464(int& val) {
    func_in_4464(val);
    const int x = 4464;
    std::cout << 4464;
    for (int i = 0; i < 4464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4464(int& val) {
    const int x = 4464;
    std::cout << 4464;
    for (int i = 0; i < 4464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4465(int& val) {
    func_in_4465(val);
    const int x = 4465;
    std::cout << 4465;
    for (int i = 0; i < 4465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4465(int& val) {
    const int x = 4465;
    std::cout << 4465;
    for (int i = 0; i < 4465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4466(int& val) {
    func_in_4466(val);
    const int x = 4466;
    std::cout << 4466;
    for (int i = 0; i < 4466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4466(int& val) {
    const int x = 4466;
    std::cout << 4466;
    for (int i = 0; i < 4466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4467(int& val) {
    func_in_4467(val);
    const int x = 4467;
    std::cout << 4467;
    for (int i = 0; i < 4467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4467(int& val) {
    const int x = 4467;
    std::cout << 4467;
    for (int i = 0; i < 4467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4468(int& val) {
    func_in_4468(val);
    const int x = 4468;
    std::cout << 4468;
    for (int i = 0; i < 4468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4468(int& val) {
    const int x = 4468;
    std::cout << 4468;
    for (int i = 0; i < 4468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4469(int& val) {
    func_in_4469(val);
    const int x = 4469;
    std::cout << 4469;
    for (int i = 0; i < 4469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4469(int& val) {
    const int x = 4469;
    std::cout << 4469;
    for (int i = 0; i < 4469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4470(int& val) {
    func_in_4470(val);
    const int x = 4470;
    std::cout << 4470;
    for (int i = 0; i < 4470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4470(int& val) {
    const int x = 4470;
    std::cout << 4470;
    for (int i = 0; i < 4470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4471(int& val) {
    func_in_4471(val);
    const int x = 4471;
    std::cout << 4471;
    for (int i = 0; i < 4471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4471(int& val) {
    const int x = 4471;
    std::cout << 4471;
    for (int i = 0; i < 4471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4472(int& val) {
    func_in_4472(val);
    const int x = 4472;
    std::cout << 4472;
    for (int i = 0; i < 4472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4472(int& val) {
    const int x = 4472;
    std::cout << 4472;
    for (int i = 0; i < 4472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4473(int& val) {
    func_in_4473(val);
    const int x = 4473;
    std::cout << 4473;
    for (int i = 0; i < 4473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4473(int& val) {
    const int x = 4473;
    std::cout << 4473;
    for (int i = 0; i < 4473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4474(int& val) {
    func_in_4474(val);
    const int x = 4474;
    std::cout << 4474;
    for (int i = 0; i < 4474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4474(int& val) {
    const int x = 4474;
    std::cout << 4474;
    for (int i = 0; i < 4474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4475(int& val) {
    func_in_4475(val);
    const int x = 4475;
    std::cout << 4475;
    for (int i = 0; i < 4475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4475(int& val) {
    const int x = 4475;
    std::cout << 4475;
    for (int i = 0; i < 4475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4476(int& val) {
    func_in_4476(val);
    const int x = 4476;
    std::cout << 4476;
    for (int i = 0; i < 4476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4476(int& val) {
    const int x = 4476;
    std::cout << 4476;
    for (int i = 0; i < 4476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4477(int& val) {
    func_in_4477(val);
    const int x = 4477;
    std::cout << 4477;
    for (int i = 0; i < 4477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4477(int& val) {
    const int x = 4477;
    std::cout << 4477;
    for (int i = 0; i < 4477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4478(int& val) {
    func_in_4478(val);
    const int x = 4478;
    std::cout << 4478;
    for (int i = 0; i < 4478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4478(int& val) {
    const int x = 4478;
    std::cout << 4478;
    for (int i = 0; i < 4478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4479(int& val) {
    func_in_4479(val);
    const int x = 4479;
    std::cout << 4479;
    for (int i = 0; i < 4479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4479(int& val) {
    const int x = 4479;
    std::cout << 4479;
    for (int i = 0; i < 4479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4480(int& val) {
    func_in_4480(val);
    const int x = 4480;
    std::cout << 4480;
    for (int i = 0; i < 4480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4480(int& val) {
    const int x = 4480;
    std::cout << 4480;
    for (int i = 0; i < 4480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4481(int& val) {
    func_in_4481(val);
    const int x = 4481;
    std::cout << 4481;
    for (int i = 0; i < 4481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4481(int& val) {
    const int x = 4481;
    std::cout << 4481;
    for (int i = 0; i < 4481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4482(int& val) {
    func_in_4482(val);
    const int x = 4482;
    std::cout << 4482;
    for (int i = 0; i < 4482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4482(int& val) {
    const int x = 4482;
    std::cout << 4482;
    for (int i = 0; i < 4482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4483(int& val) {
    func_in_4483(val);
    const int x = 4483;
    std::cout << 4483;
    for (int i = 0; i < 4483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4483(int& val) {
    const int x = 4483;
    std::cout << 4483;
    for (int i = 0; i < 4483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4484(int& val) {
    func_in_4484(val);
    const int x = 4484;
    std::cout << 4484;
    for (int i = 0; i < 4484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4484(int& val) {
    const int x = 4484;
    std::cout << 4484;
    for (int i = 0; i < 4484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4485(int& val) {
    func_in_4485(val);
    const int x = 4485;
    std::cout << 4485;
    for (int i = 0; i < 4485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4485(int& val) {
    const int x = 4485;
    std::cout << 4485;
    for (int i = 0; i < 4485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4486(int& val) {
    func_in_4486(val);
    const int x = 4486;
    std::cout << 4486;
    for (int i = 0; i < 4486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4486(int& val) {
    const int x = 4486;
    std::cout << 4486;
    for (int i = 0; i < 4486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4487(int& val) {
    func_in_4487(val);
    const int x = 4487;
    std::cout << 4487;
    for (int i = 0; i < 4487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4487(int& val) {
    const int x = 4487;
    std::cout << 4487;
    for (int i = 0; i < 4487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4488(int& val) {
    func_in_4488(val);
    const int x = 4488;
    std::cout << 4488;
    for (int i = 0; i < 4488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4488(int& val) {
    const int x = 4488;
    std::cout << 4488;
    for (int i = 0; i < 4488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4489(int& val) {
    func_in_4489(val);
    const int x = 4489;
    std::cout << 4489;
    for (int i = 0; i < 4489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4489(int& val) {
    const int x = 4489;
    std::cout << 4489;
    for (int i = 0; i < 4489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4490(int& val) {
    func_in_4490(val);
    const int x = 4490;
    std::cout << 4490;
    for (int i = 0; i < 4490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4490(int& val) {
    const int x = 4490;
    std::cout << 4490;
    for (int i = 0; i < 4490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4491(int& val) {
    func_in_4491(val);
    const int x = 4491;
    std::cout << 4491;
    for (int i = 0; i < 4491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4491(int& val) {
    const int x = 4491;
    std::cout << 4491;
    for (int i = 0; i < 4491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4492(int& val) {
    func_in_4492(val);
    const int x = 4492;
    std::cout << 4492;
    for (int i = 0; i < 4492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4492(int& val) {
    const int x = 4492;
    std::cout << 4492;
    for (int i = 0; i < 4492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4493(int& val) {
    func_in_4493(val);
    const int x = 4493;
    std::cout << 4493;
    for (int i = 0; i < 4493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4493(int& val) {
    const int x = 4493;
    std::cout << 4493;
    for (int i = 0; i < 4493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4494(int& val) {
    func_in_4494(val);
    const int x = 4494;
    std::cout << 4494;
    for (int i = 0; i < 4494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4494(int& val) {
    const int x = 4494;
    std::cout << 4494;
    for (int i = 0; i < 4494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4495(int& val) {
    func_in_4495(val);
    const int x = 4495;
    std::cout << 4495;
    for (int i = 0; i < 4495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4495(int& val) {
    const int x = 4495;
    std::cout << 4495;
    for (int i = 0; i < 4495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4496(int& val) {
    func_in_4496(val);
    const int x = 4496;
    std::cout << 4496;
    for (int i = 0; i < 4496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4496(int& val) {
    const int x = 4496;
    std::cout << 4496;
    for (int i = 0; i < 4496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4497(int& val) {
    func_in_4497(val);
    const int x = 4497;
    std::cout << 4497;
    for (int i = 0; i < 4497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4497(int& val) {
    const int x = 4497;
    std::cout << 4497;
    for (int i = 0; i < 4497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4498(int& val) {
    func_in_4498(val);
    const int x = 4498;
    std::cout << 4498;
    for (int i = 0; i < 4498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4498(int& val) {
    const int x = 4498;
    std::cout << 4498;
    for (int i = 0; i < 4498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4499(int& val) {
    func_in_4499(val);
    const int x = 4499;
    std::cout << 4499;
    for (int i = 0; i < 4499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4499(int& val) {
    const int x = 4499;
    std::cout << 4499;
    for (int i = 0; i < 4499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4500(int& val) {
    func_in_4500(val);
    const int x = 4500;
    std::cout << 4500;
    for (int i = 0; i < 4500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4500(int& val) {
    const int x = 4500;
    std::cout << 4500;
    for (int i = 0; i < 4500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4501(int& val) {
    func_in_4501(val);
    const int x = 4501;
    std::cout << 4501;
    for (int i = 0; i < 4501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4501(int& val) {
    const int x = 4501;
    std::cout << 4501;
    for (int i = 0; i < 4501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4502(int& val) {
    func_in_4502(val);
    const int x = 4502;
    std::cout << 4502;
    for (int i = 0; i < 4502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4502(int& val) {
    const int x = 4502;
    std::cout << 4502;
    for (int i = 0; i < 4502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4503(int& val) {
    func_in_4503(val);
    const int x = 4503;
    std::cout << 4503;
    for (int i = 0; i < 4503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4503(int& val) {
    const int x = 4503;
    std::cout << 4503;
    for (int i = 0; i < 4503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4504(int& val) {
    func_in_4504(val);
    const int x = 4504;
    std::cout << 4504;
    for (int i = 0; i < 4504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4504(int& val) {
    const int x = 4504;
    std::cout << 4504;
    for (int i = 0; i < 4504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4505(int& val) {
    func_in_4505(val);
    const int x = 4505;
    std::cout << 4505;
    for (int i = 0; i < 4505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4505(int& val) {
    const int x = 4505;
    std::cout << 4505;
    for (int i = 0; i < 4505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4506(int& val) {
    func_in_4506(val);
    const int x = 4506;
    std::cout << 4506;
    for (int i = 0; i < 4506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4506(int& val) {
    const int x = 4506;
    std::cout << 4506;
    for (int i = 0; i < 4506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4507(int& val) {
    func_in_4507(val);
    const int x = 4507;
    std::cout << 4507;
    for (int i = 0; i < 4507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4507(int& val) {
    const int x = 4507;
    std::cout << 4507;
    for (int i = 0; i < 4507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4508(int& val) {
    func_in_4508(val);
    const int x = 4508;
    std::cout << 4508;
    for (int i = 0; i < 4508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4508(int& val) {
    const int x = 4508;
    std::cout << 4508;
    for (int i = 0; i < 4508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4509(int& val) {
    func_in_4509(val);
    const int x = 4509;
    std::cout << 4509;
    for (int i = 0; i < 4509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4509(int& val) {
    const int x = 4509;
    std::cout << 4509;
    for (int i = 0; i < 4509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4510(int& val) {
    func_in_4510(val);
    const int x = 4510;
    std::cout << 4510;
    for (int i = 0; i < 4510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4510(int& val) {
    const int x = 4510;
    std::cout << 4510;
    for (int i = 0; i < 4510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4511(int& val) {
    func_in_4511(val);
    const int x = 4511;
    std::cout << 4511;
    for (int i = 0; i < 4511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4511(int& val) {
    const int x = 4511;
    std::cout << 4511;
    for (int i = 0; i < 4511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4512(int& val) {
    func_in_4512(val);
    const int x = 4512;
    std::cout << 4512;
    for (int i = 0; i < 4512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4512(int& val) {
    const int x = 4512;
    std::cout << 4512;
    for (int i = 0; i < 4512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4513(int& val) {
    func_in_4513(val);
    const int x = 4513;
    std::cout << 4513;
    for (int i = 0; i < 4513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4513(int& val) {
    const int x = 4513;
    std::cout << 4513;
    for (int i = 0; i < 4513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4514(int& val) {
    func_in_4514(val);
    const int x = 4514;
    std::cout << 4514;
    for (int i = 0; i < 4514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4514(int& val) {
    const int x = 4514;
    std::cout << 4514;
    for (int i = 0; i < 4514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4515(int& val) {
    func_in_4515(val);
    const int x = 4515;
    std::cout << 4515;
    for (int i = 0; i < 4515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4515(int& val) {
    const int x = 4515;
    std::cout << 4515;
    for (int i = 0; i < 4515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4516(int& val) {
    func_in_4516(val);
    const int x = 4516;
    std::cout << 4516;
    for (int i = 0; i < 4516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4516(int& val) {
    const int x = 4516;
    std::cout << 4516;
    for (int i = 0; i < 4516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4517(int& val) {
    func_in_4517(val);
    const int x = 4517;
    std::cout << 4517;
    for (int i = 0; i < 4517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4517(int& val) {
    const int x = 4517;
    std::cout << 4517;
    for (int i = 0; i < 4517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4518(int& val) {
    func_in_4518(val);
    const int x = 4518;
    std::cout << 4518;
    for (int i = 0; i < 4518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4518(int& val) {
    const int x = 4518;
    std::cout << 4518;
    for (int i = 0; i < 4518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4519(int& val) {
    func_in_4519(val);
    const int x = 4519;
    std::cout << 4519;
    for (int i = 0; i < 4519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4519(int& val) {
    const int x = 4519;
    std::cout << 4519;
    for (int i = 0; i < 4519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4520(int& val) {
    func_in_4520(val);
    const int x = 4520;
    std::cout << 4520;
    for (int i = 0; i < 4520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4520(int& val) {
    const int x = 4520;
    std::cout << 4520;
    for (int i = 0; i < 4520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4521(int& val) {
    func_in_4521(val);
    const int x = 4521;
    std::cout << 4521;
    for (int i = 0; i < 4521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4521(int& val) {
    const int x = 4521;
    std::cout << 4521;
    for (int i = 0; i < 4521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4522(int& val) {
    func_in_4522(val);
    const int x = 4522;
    std::cout << 4522;
    for (int i = 0; i < 4522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4522(int& val) {
    const int x = 4522;
    std::cout << 4522;
    for (int i = 0; i < 4522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4523(int& val) {
    func_in_4523(val);
    const int x = 4523;
    std::cout << 4523;
    for (int i = 0; i < 4523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4523(int& val) {
    const int x = 4523;
    std::cout << 4523;
    for (int i = 0; i < 4523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4524(int& val) {
    func_in_4524(val);
    const int x = 4524;
    std::cout << 4524;
    for (int i = 0; i < 4524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4524(int& val) {
    const int x = 4524;
    std::cout << 4524;
    for (int i = 0; i < 4524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4525(int& val) {
    func_in_4525(val);
    const int x = 4525;
    std::cout << 4525;
    for (int i = 0; i < 4525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4525(int& val) {
    const int x = 4525;
    std::cout << 4525;
    for (int i = 0; i < 4525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4526(int& val) {
    func_in_4526(val);
    const int x = 4526;
    std::cout << 4526;
    for (int i = 0; i < 4526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4526(int& val) {
    const int x = 4526;
    std::cout << 4526;
    for (int i = 0; i < 4526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4527(int& val) {
    func_in_4527(val);
    const int x = 4527;
    std::cout << 4527;
    for (int i = 0; i < 4527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4527(int& val) {
    const int x = 4527;
    std::cout << 4527;
    for (int i = 0; i < 4527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4528(int& val) {
    func_in_4528(val);
    const int x = 4528;
    std::cout << 4528;
    for (int i = 0; i < 4528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4528(int& val) {
    const int x = 4528;
    std::cout << 4528;
    for (int i = 0; i < 4528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4529(int& val) {
    func_in_4529(val);
    const int x = 4529;
    std::cout << 4529;
    for (int i = 0; i < 4529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4529(int& val) {
    const int x = 4529;
    std::cout << 4529;
    for (int i = 0; i < 4529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4530(int& val) {
    func_in_4530(val);
    const int x = 4530;
    std::cout << 4530;
    for (int i = 0; i < 4530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4530(int& val) {
    const int x = 4530;
    std::cout << 4530;
    for (int i = 0; i < 4530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4531(int& val) {
    func_in_4531(val);
    const int x = 4531;
    std::cout << 4531;
    for (int i = 0; i < 4531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4531(int& val) {
    const int x = 4531;
    std::cout << 4531;
    for (int i = 0; i < 4531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4532(int& val) {
    func_in_4532(val);
    const int x = 4532;
    std::cout << 4532;
    for (int i = 0; i < 4532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4532(int& val) {
    const int x = 4532;
    std::cout << 4532;
    for (int i = 0; i < 4532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4533(int& val) {
    func_in_4533(val);
    const int x = 4533;
    std::cout << 4533;
    for (int i = 0; i < 4533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4533(int& val) {
    const int x = 4533;
    std::cout << 4533;
    for (int i = 0; i < 4533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4534(int& val) {
    func_in_4534(val);
    const int x = 4534;
    std::cout << 4534;
    for (int i = 0; i < 4534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4534(int& val) {
    const int x = 4534;
    std::cout << 4534;
    for (int i = 0; i < 4534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4535(int& val) {
    func_in_4535(val);
    const int x = 4535;
    std::cout << 4535;
    for (int i = 0; i < 4535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4535(int& val) {
    const int x = 4535;
    std::cout << 4535;
    for (int i = 0; i < 4535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4536(int& val) {
    func_in_4536(val);
    const int x = 4536;
    std::cout << 4536;
    for (int i = 0; i < 4536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4536(int& val) {
    const int x = 4536;
    std::cout << 4536;
    for (int i = 0; i < 4536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4537(int& val) {
    func_in_4537(val);
    const int x = 4537;
    std::cout << 4537;
    for (int i = 0; i < 4537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4537(int& val) {
    const int x = 4537;
    std::cout << 4537;
    for (int i = 0; i < 4537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4538(int& val) {
    func_in_4538(val);
    const int x = 4538;
    std::cout << 4538;
    for (int i = 0; i < 4538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4538(int& val) {
    const int x = 4538;
    std::cout << 4538;
    for (int i = 0; i < 4538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4539(int& val) {
    func_in_4539(val);
    const int x = 4539;
    std::cout << 4539;
    for (int i = 0; i < 4539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4539(int& val) {
    const int x = 4539;
    std::cout << 4539;
    for (int i = 0; i < 4539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4540(int& val) {
    func_in_4540(val);
    const int x = 4540;
    std::cout << 4540;
    for (int i = 0; i < 4540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4540(int& val) {
    const int x = 4540;
    std::cout << 4540;
    for (int i = 0; i < 4540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4541(int& val) {
    func_in_4541(val);
    const int x = 4541;
    std::cout << 4541;
    for (int i = 0; i < 4541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4541(int& val) {
    const int x = 4541;
    std::cout << 4541;
    for (int i = 0; i < 4541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4542(int& val) {
    func_in_4542(val);
    const int x = 4542;
    std::cout << 4542;
    for (int i = 0; i < 4542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4542(int& val) {
    const int x = 4542;
    std::cout << 4542;
    for (int i = 0; i < 4542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4543(int& val) {
    func_in_4543(val);
    const int x = 4543;
    std::cout << 4543;
    for (int i = 0; i < 4543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4543(int& val) {
    const int x = 4543;
    std::cout << 4543;
    for (int i = 0; i < 4543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4544(int& val) {
    func_in_4544(val);
    const int x = 4544;
    std::cout << 4544;
    for (int i = 0; i < 4544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4544(int& val) {
    const int x = 4544;
    std::cout << 4544;
    for (int i = 0; i < 4544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4545(int& val) {
    func_in_4545(val);
    const int x = 4545;
    std::cout << 4545;
    for (int i = 0; i < 4545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4545(int& val) {
    const int x = 4545;
    std::cout << 4545;
    for (int i = 0; i < 4545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4546(int& val) {
    func_in_4546(val);
    const int x = 4546;
    std::cout << 4546;
    for (int i = 0; i < 4546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4546(int& val) {
    const int x = 4546;
    std::cout << 4546;
    for (int i = 0; i < 4546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4547(int& val) {
    func_in_4547(val);
    const int x = 4547;
    std::cout << 4547;
    for (int i = 0; i < 4547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4547(int& val) {
    const int x = 4547;
    std::cout << 4547;
    for (int i = 0; i < 4547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4548(int& val) {
    func_in_4548(val);
    const int x = 4548;
    std::cout << 4548;
    for (int i = 0; i < 4548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4548(int& val) {
    const int x = 4548;
    std::cout << 4548;
    for (int i = 0; i < 4548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4549(int& val) {
    func_in_4549(val);
    const int x = 4549;
    std::cout << 4549;
    for (int i = 0; i < 4549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4549(int& val) {
    const int x = 4549;
    std::cout << 4549;
    for (int i = 0; i < 4549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4550(int& val) {
    func_in_4550(val);
    const int x = 4550;
    std::cout << 4550;
    for (int i = 0; i < 4550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4550(int& val) {
    const int x = 4550;
    std::cout << 4550;
    for (int i = 0; i < 4550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4551(int& val) {
    func_in_4551(val);
    const int x = 4551;
    std::cout << 4551;
    for (int i = 0; i < 4551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4551(int& val) {
    const int x = 4551;
    std::cout << 4551;
    for (int i = 0; i < 4551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4552(int& val) {
    func_in_4552(val);
    const int x = 4552;
    std::cout << 4552;
    for (int i = 0; i < 4552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4552(int& val) {
    const int x = 4552;
    std::cout << 4552;
    for (int i = 0; i < 4552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4553(int& val) {
    func_in_4553(val);
    const int x = 4553;
    std::cout << 4553;
    for (int i = 0; i < 4553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4553(int& val) {
    const int x = 4553;
    std::cout << 4553;
    for (int i = 0; i < 4553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4554(int& val) {
    func_in_4554(val);
    const int x = 4554;
    std::cout << 4554;
    for (int i = 0; i < 4554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4554(int& val) {
    const int x = 4554;
    std::cout << 4554;
    for (int i = 0; i < 4554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4555(int& val) {
    func_in_4555(val);
    const int x = 4555;
    std::cout << 4555;
    for (int i = 0; i < 4555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4555(int& val) {
    const int x = 4555;
    std::cout << 4555;
    for (int i = 0; i < 4555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4556(int& val) {
    func_in_4556(val);
    const int x = 4556;
    std::cout << 4556;
    for (int i = 0; i < 4556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4556(int& val) {
    const int x = 4556;
    std::cout << 4556;
    for (int i = 0; i < 4556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4557(int& val) {
    func_in_4557(val);
    const int x = 4557;
    std::cout << 4557;
    for (int i = 0; i < 4557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4557(int& val) {
    const int x = 4557;
    std::cout << 4557;
    for (int i = 0; i < 4557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4558(int& val) {
    func_in_4558(val);
    const int x = 4558;
    std::cout << 4558;
    for (int i = 0; i < 4558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4558(int& val) {
    const int x = 4558;
    std::cout << 4558;
    for (int i = 0; i < 4558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4559(int& val) {
    func_in_4559(val);
    const int x = 4559;
    std::cout << 4559;
    for (int i = 0; i < 4559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4559(int& val) {
    const int x = 4559;
    std::cout << 4559;
    for (int i = 0; i < 4559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4560(int& val) {
    func_in_4560(val);
    const int x = 4560;
    std::cout << 4560;
    for (int i = 0; i < 4560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4560(int& val) {
    const int x = 4560;
    std::cout << 4560;
    for (int i = 0; i < 4560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4561(int& val) {
    func_in_4561(val);
    const int x = 4561;
    std::cout << 4561;
    for (int i = 0; i < 4561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4561(int& val) {
    const int x = 4561;
    std::cout << 4561;
    for (int i = 0; i < 4561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4562(int& val) {
    func_in_4562(val);
    const int x = 4562;
    std::cout << 4562;
    for (int i = 0; i < 4562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4562(int& val) {
    const int x = 4562;
    std::cout << 4562;
    for (int i = 0; i < 4562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4563(int& val) {
    func_in_4563(val);
    const int x = 4563;
    std::cout << 4563;
    for (int i = 0; i < 4563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4563(int& val) {
    const int x = 4563;
    std::cout << 4563;
    for (int i = 0; i < 4563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4564(int& val) {
    func_in_4564(val);
    const int x = 4564;
    std::cout << 4564;
    for (int i = 0; i < 4564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4564(int& val) {
    const int x = 4564;
    std::cout << 4564;
    for (int i = 0; i < 4564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4565(int& val) {
    func_in_4565(val);
    const int x = 4565;
    std::cout << 4565;
    for (int i = 0; i < 4565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4565(int& val) {
    const int x = 4565;
    std::cout << 4565;
    for (int i = 0; i < 4565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4566(int& val) {
    func_in_4566(val);
    const int x = 4566;
    std::cout << 4566;
    for (int i = 0; i < 4566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4566(int& val) {
    const int x = 4566;
    std::cout << 4566;
    for (int i = 0; i < 4566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4567(int& val) {
    func_in_4567(val);
    const int x = 4567;
    std::cout << 4567;
    for (int i = 0; i < 4567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4567(int& val) {
    const int x = 4567;
    std::cout << 4567;
    for (int i = 0; i < 4567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4568(int& val) {
    func_in_4568(val);
    const int x = 4568;
    std::cout << 4568;
    for (int i = 0; i < 4568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4568(int& val) {
    const int x = 4568;
    std::cout << 4568;
    for (int i = 0; i < 4568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4569(int& val) {
    func_in_4569(val);
    const int x = 4569;
    std::cout << 4569;
    for (int i = 0; i < 4569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4569(int& val) {
    const int x = 4569;
    std::cout << 4569;
    for (int i = 0; i < 4569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4570(int& val) {
    func_in_4570(val);
    const int x = 4570;
    std::cout << 4570;
    for (int i = 0; i < 4570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4570(int& val) {
    const int x = 4570;
    std::cout << 4570;
    for (int i = 0; i < 4570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4571(int& val) {
    func_in_4571(val);
    const int x = 4571;
    std::cout << 4571;
    for (int i = 0; i < 4571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4571(int& val) {
    const int x = 4571;
    std::cout << 4571;
    for (int i = 0; i < 4571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4572(int& val) {
    func_in_4572(val);
    const int x = 4572;
    std::cout << 4572;
    for (int i = 0; i < 4572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4572(int& val) {
    const int x = 4572;
    std::cout << 4572;
    for (int i = 0; i < 4572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4573(int& val) {
    func_in_4573(val);
    const int x = 4573;
    std::cout << 4573;
    for (int i = 0; i < 4573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4573(int& val) {
    const int x = 4573;
    std::cout << 4573;
    for (int i = 0; i < 4573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4574(int& val) {
    func_in_4574(val);
    const int x = 4574;
    std::cout << 4574;
    for (int i = 0; i < 4574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4574(int& val) {
    const int x = 4574;
    std::cout << 4574;
    for (int i = 0; i < 4574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4575(int& val) {
    func_in_4575(val);
    const int x = 4575;
    std::cout << 4575;
    for (int i = 0; i < 4575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4575(int& val) {
    const int x = 4575;
    std::cout << 4575;
    for (int i = 0; i < 4575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4576(int& val) {
    func_in_4576(val);
    const int x = 4576;
    std::cout << 4576;
    for (int i = 0; i < 4576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4576(int& val) {
    const int x = 4576;
    std::cout << 4576;
    for (int i = 0; i < 4576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4577(int& val) {
    func_in_4577(val);
    const int x = 4577;
    std::cout << 4577;
    for (int i = 0; i < 4577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4577(int& val) {
    const int x = 4577;
    std::cout << 4577;
    for (int i = 0; i < 4577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4578(int& val) {
    func_in_4578(val);
    const int x = 4578;
    std::cout << 4578;
    for (int i = 0; i < 4578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4578(int& val) {
    const int x = 4578;
    std::cout << 4578;
    for (int i = 0; i < 4578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4579(int& val) {
    func_in_4579(val);
    const int x = 4579;
    std::cout << 4579;
    for (int i = 0; i < 4579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4579(int& val) {
    const int x = 4579;
    std::cout << 4579;
    for (int i = 0; i < 4579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4580(int& val) {
    func_in_4580(val);
    const int x = 4580;
    std::cout << 4580;
    for (int i = 0; i < 4580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4580(int& val) {
    const int x = 4580;
    std::cout << 4580;
    for (int i = 0; i < 4580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4581(int& val) {
    func_in_4581(val);
    const int x = 4581;
    std::cout << 4581;
    for (int i = 0; i < 4581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4581(int& val) {
    const int x = 4581;
    std::cout << 4581;
    for (int i = 0; i < 4581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4582(int& val) {
    func_in_4582(val);
    const int x = 4582;
    std::cout << 4582;
    for (int i = 0; i < 4582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4582(int& val) {
    const int x = 4582;
    std::cout << 4582;
    for (int i = 0; i < 4582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4583(int& val) {
    func_in_4583(val);
    const int x = 4583;
    std::cout << 4583;
    for (int i = 0; i < 4583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4583(int& val) {
    const int x = 4583;
    std::cout << 4583;
    for (int i = 0; i < 4583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4584(int& val) {
    func_in_4584(val);
    const int x = 4584;
    std::cout << 4584;
    for (int i = 0; i < 4584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4584(int& val) {
    const int x = 4584;
    std::cout << 4584;
    for (int i = 0; i < 4584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4585(int& val) {
    func_in_4585(val);
    const int x = 4585;
    std::cout << 4585;
    for (int i = 0; i < 4585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4585(int& val) {
    const int x = 4585;
    std::cout << 4585;
    for (int i = 0; i < 4585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4586(int& val) {
    func_in_4586(val);
    const int x = 4586;
    std::cout << 4586;
    for (int i = 0; i < 4586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4586(int& val) {
    const int x = 4586;
    std::cout << 4586;
    for (int i = 0; i < 4586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4587(int& val) {
    func_in_4587(val);
    const int x = 4587;
    std::cout << 4587;
    for (int i = 0; i < 4587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4587(int& val) {
    const int x = 4587;
    std::cout << 4587;
    for (int i = 0; i < 4587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4588(int& val) {
    func_in_4588(val);
    const int x = 4588;
    std::cout << 4588;
    for (int i = 0; i < 4588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4588(int& val) {
    const int x = 4588;
    std::cout << 4588;
    for (int i = 0; i < 4588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4589(int& val) {
    func_in_4589(val);
    const int x = 4589;
    std::cout << 4589;
    for (int i = 0; i < 4589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4589(int& val) {
    const int x = 4589;
    std::cout << 4589;
    for (int i = 0; i < 4589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4590(int& val) {
    func_in_4590(val);
    const int x = 4590;
    std::cout << 4590;
    for (int i = 0; i < 4590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4590(int& val) {
    const int x = 4590;
    std::cout << 4590;
    for (int i = 0; i < 4590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4591(int& val) {
    func_in_4591(val);
    const int x = 4591;
    std::cout << 4591;
    for (int i = 0; i < 4591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4591(int& val) {
    const int x = 4591;
    std::cout << 4591;
    for (int i = 0; i < 4591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4592(int& val) {
    func_in_4592(val);
    const int x = 4592;
    std::cout << 4592;
    for (int i = 0; i < 4592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4592(int& val) {
    const int x = 4592;
    std::cout << 4592;
    for (int i = 0; i < 4592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4593(int& val) {
    func_in_4593(val);
    const int x = 4593;
    std::cout << 4593;
    for (int i = 0; i < 4593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4593(int& val) {
    const int x = 4593;
    std::cout << 4593;
    for (int i = 0; i < 4593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4594(int& val) {
    func_in_4594(val);
    const int x = 4594;
    std::cout << 4594;
    for (int i = 0; i < 4594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4594(int& val) {
    const int x = 4594;
    std::cout << 4594;
    for (int i = 0; i < 4594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4595(int& val) {
    func_in_4595(val);
    const int x = 4595;
    std::cout << 4595;
    for (int i = 0; i < 4595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4595(int& val) {
    const int x = 4595;
    std::cout << 4595;
    for (int i = 0; i < 4595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4596(int& val) {
    func_in_4596(val);
    const int x = 4596;
    std::cout << 4596;
    for (int i = 0; i < 4596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4596(int& val) {
    const int x = 4596;
    std::cout << 4596;
    for (int i = 0; i < 4596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4597(int& val) {
    func_in_4597(val);
    const int x = 4597;
    std::cout << 4597;
    for (int i = 0; i < 4597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4597(int& val) {
    const int x = 4597;
    std::cout << 4597;
    for (int i = 0; i < 4597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4598(int& val) {
    func_in_4598(val);
    const int x = 4598;
    std::cout << 4598;
    for (int i = 0; i < 4598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4598(int& val) {
    const int x = 4598;
    std::cout << 4598;
    for (int i = 0; i < 4598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4599(int& val) {
    func_in_4599(val);
    const int x = 4599;
    std::cout << 4599;
    for (int i = 0; i < 4599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4599(int& val) {
    const int x = 4599;
    std::cout << 4599;
    for (int i = 0; i < 4599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4600(int& val) {
    func_in_4600(val);
    const int x = 4600;
    std::cout << 4600;
    for (int i = 0; i < 4600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4600(int& val) {
    const int x = 4600;
    std::cout << 4600;
    for (int i = 0; i < 4600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4601(int& val) {
    func_in_4601(val);
    const int x = 4601;
    std::cout << 4601;
    for (int i = 0; i < 4601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4601(int& val) {
    const int x = 4601;
    std::cout << 4601;
    for (int i = 0; i < 4601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4602(int& val) {
    func_in_4602(val);
    const int x = 4602;
    std::cout << 4602;
    for (int i = 0; i < 4602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4602(int& val) {
    const int x = 4602;
    std::cout << 4602;
    for (int i = 0; i < 4602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4603(int& val) {
    func_in_4603(val);
    const int x = 4603;
    std::cout << 4603;
    for (int i = 0; i < 4603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4603(int& val) {
    const int x = 4603;
    std::cout << 4603;
    for (int i = 0; i < 4603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4604(int& val) {
    func_in_4604(val);
    const int x = 4604;
    std::cout << 4604;
    for (int i = 0; i < 4604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4604(int& val) {
    const int x = 4604;
    std::cout << 4604;
    for (int i = 0; i < 4604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4605(int& val) {
    func_in_4605(val);
    const int x = 4605;
    std::cout << 4605;
    for (int i = 0; i < 4605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4605(int& val) {
    const int x = 4605;
    std::cout << 4605;
    for (int i = 0; i < 4605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4606(int& val) {
    func_in_4606(val);
    const int x = 4606;
    std::cout << 4606;
    for (int i = 0; i < 4606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4606(int& val) {
    const int x = 4606;
    std::cout << 4606;
    for (int i = 0; i < 4606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4607(int& val) {
    func_in_4607(val);
    const int x = 4607;
    std::cout << 4607;
    for (int i = 0; i < 4607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4607(int& val) {
    const int x = 4607;
    std::cout << 4607;
    for (int i = 0; i < 4607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4608(int& val) {
    func_in_4608(val);
    const int x = 4608;
    std::cout << 4608;
    for (int i = 0; i < 4608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4608(int& val) {
    const int x = 4608;
    std::cout << 4608;
    for (int i = 0; i < 4608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4609(int& val) {
    func_in_4609(val);
    const int x = 4609;
    std::cout << 4609;
    for (int i = 0; i < 4609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4609(int& val) {
    const int x = 4609;
    std::cout << 4609;
    for (int i = 0; i < 4609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4610(int& val) {
    func_in_4610(val);
    const int x = 4610;
    std::cout << 4610;
    for (int i = 0; i < 4610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4610(int& val) {
    const int x = 4610;
    std::cout << 4610;
    for (int i = 0; i < 4610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4611(int& val) {
    func_in_4611(val);
    const int x = 4611;
    std::cout << 4611;
    for (int i = 0; i < 4611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4611(int& val) {
    const int x = 4611;
    std::cout << 4611;
    for (int i = 0; i < 4611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4612(int& val) {
    func_in_4612(val);
    const int x = 4612;
    std::cout << 4612;
    for (int i = 0; i < 4612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4612(int& val) {
    const int x = 4612;
    std::cout << 4612;
    for (int i = 0; i < 4612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4613(int& val) {
    func_in_4613(val);
    const int x = 4613;
    std::cout << 4613;
    for (int i = 0; i < 4613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4613(int& val) {
    const int x = 4613;
    std::cout << 4613;
    for (int i = 0; i < 4613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4614(int& val) {
    func_in_4614(val);
    const int x = 4614;
    std::cout << 4614;
    for (int i = 0; i < 4614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4614(int& val) {
    const int x = 4614;
    std::cout << 4614;
    for (int i = 0; i < 4614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4615(int& val) {
    func_in_4615(val);
    const int x = 4615;
    std::cout << 4615;
    for (int i = 0; i < 4615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4615(int& val) {
    const int x = 4615;
    std::cout << 4615;
    for (int i = 0; i < 4615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4616(int& val) {
    func_in_4616(val);
    const int x = 4616;
    std::cout << 4616;
    for (int i = 0; i < 4616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4616(int& val) {
    const int x = 4616;
    std::cout << 4616;
    for (int i = 0; i < 4616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4617(int& val) {
    func_in_4617(val);
    const int x = 4617;
    std::cout << 4617;
    for (int i = 0; i < 4617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4617(int& val) {
    const int x = 4617;
    std::cout << 4617;
    for (int i = 0; i < 4617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4618(int& val) {
    func_in_4618(val);
    const int x = 4618;
    std::cout << 4618;
    for (int i = 0; i < 4618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4618(int& val) {
    const int x = 4618;
    std::cout << 4618;
    for (int i = 0; i < 4618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4619(int& val) {
    func_in_4619(val);
    const int x = 4619;
    std::cout << 4619;
    for (int i = 0; i < 4619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4619(int& val) {
    const int x = 4619;
    std::cout << 4619;
    for (int i = 0; i < 4619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4620(int& val) {
    func_in_4620(val);
    const int x = 4620;
    std::cout << 4620;
    for (int i = 0; i < 4620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4620(int& val) {
    const int x = 4620;
    std::cout << 4620;
    for (int i = 0; i < 4620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4621(int& val) {
    func_in_4621(val);
    const int x = 4621;
    std::cout << 4621;
    for (int i = 0; i < 4621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4621(int& val) {
    const int x = 4621;
    std::cout << 4621;
    for (int i = 0; i < 4621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4622(int& val) {
    func_in_4622(val);
    const int x = 4622;
    std::cout << 4622;
    for (int i = 0; i < 4622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4622(int& val) {
    const int x = 4622;
    std::cout << 4622;
    for (int i = 0; i < 4622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4623(int& val) {
    func_in_4623(val);
    const int x = 4623;
    std::cout << 4623;
    for (int i = 0; i < 4623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4623(int& val) {
    const int x = 4623;
    std::cout << 4623;
    for (int i = 0; i < 4623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4624(int& val) {
    func_in_4624(val);
    const int x = 4624;
    std::cout << 4624;
    for (int i = 0; i < 4624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4624(int& val) {
    const int x = 4624;
    std::cout << 4624;
    for (int i = 0; i < 4624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4625(int& val) {
    func_in_4625(val);
    const int x = 4625;
    std::cout << 4625;
    for (int i = 0; i < 4625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4625(int& val) {
    const int x = 4625;
    std::cout << 4625;
    for (int i = 0; i < 4625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4626(int& val) {
    func_in_4626(val);
    const int x = 4626;
    std::cout << 4626;
    for (int i = 0; i < 4626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4626(int& val) {
    const int x = 4626;
    std::cout << 4626;
    for (int i = 0; i < 4626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4627(int& val) {
    func_in_4627(val);
    const int x = 4627;
    std::cout << 4627;
    for (int i = 0; i < 4627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4627(int& val) {
    const int x = 4627;
    std::cout << 4627;
    for (int i = 0; i < 4627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4628(int& val) {
    func_in_4628(val);
    const int x = 4628;
    std::cout << 4628;
    for (int i = 0; i < 4628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4628(int& val) {
    const int x = 4628;
    std::cout << 4628;
    for (int i = 0; i < 4628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4629(int& val) {
    func_in_4629(val);
    const int x = 4629;
    std::cout << 4629;
    for (int i = 0; i < 4629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4629(int& val) {
    const int x = 4629;
    std::cout << 4629;
    for (int i = 0; i < 4629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4630(int& val) {
    func_in_4630(val);
    const int x = 4630;
    std::cout << 4630;
    for (int i = 0; i < 4630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4630(int& val) {
    const int x = 4630;
    std::cout << 4630;
    for (int i = 0; i < 4630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4631(int& val) {
    func_in_4631(val);
    const int x = 4631;
    std::cout << 4631;
    for (int i = 0; i < 4631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4631(int& val) {
    const int x = 4631;
    std::cout << 4631;
    for (int i = 0; i < 4631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4632(int& val) {
    func_in_4632(val);
    const int x = 4632;
    std::cout << 4632;
    for (int i = 0; i < 4632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4632(int& val) {
    const int x = 4632;
    std::cout << 4632;
    for (int i = 0; i < 4632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4633(int& val) {
    func_in_4633(val);
    const int x = 4633;
    std::cout << 4633;
    for (int i = 0; i < 4633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4633(int& val) {
    const int x = 4633;
    std::cout << 4633;
    for (int i = 0; i < 4633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4634(int& val) {
    func_in_4634(val);
    const int x = 4634;
    std::cout << 4634;
    for (int i = 0; i < 4634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4634(int& val) {
    const int x = 4634;
    std::cout << 4634;
    for (int i = 0; i < 4634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4635(int& val) {
    func_in_4635(val);
    const int x = 4635;
    std::cout << 4635;
    for (int i = 0; i < 4635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4635(int& val) {
    const int x = 4635;
    std::cout << 4635;
    for (int i = 0; i < 4635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4636(int& val) {
    func_in_4636(val);
    const int x = 4636;
    std::cout << 4636;
    for (int i = 0; i < 4636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4636(int& val) {
    const int x = 4636;
    std::cout << 4636;
    for (int i = 0; i < 4636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4637(int& val) {
    func_in_4637(val);
    const int x = 4637;
    std::cout << 4637;
    for (int i = 0; i < 4637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4637(int& val) {
    const int x = 4637;
    std::cout << 4637;
    for (int i = 0; i < 4637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4638(int& val) {
    func_in_4638(val);
    const int x = 4638;
    std::cout << 4638;
    for (int i = 0; i < 4638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4638(int& val) {
    const int x = 4638;
    std::cout << 4638;
    for (int i = 0; i < 4638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4639(int& val) {
    func_in_4639(val);
    const int x = 4639;
    std::cout << 4639;
    for (int i = 0; i < 4639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4639(int& val) {
    const int x = 4639;
    std::cout << 4639;
    for (int i = 0; i < 4639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4640(int& val) {
    func_in_4640(val);
    const int x = 4640;
    std::cout << 4640;
    for (int i = 0; i < 4640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4640(int& val) {
    const int x = 4640;
    std::cout << 4640;
    for (int i = 0; i < 4640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4641(int& val) {
    func_in_4641(val);
    const int x = 4641;
    std::cout << 4641;
    for (int i = 0; i < 4641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4641(int& val) {
    const int x = 4641;
    std::cout << 4641;
    for (int i = 0; i < 4641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4642(int& val) {
    func_in_4642(val);
    const int x = 4642;
    std::cout << 4642;
    for (int i = 0; i < 4642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4642(int& val) {
    const int x = 4642;
    std::cout << 4642;
    for (int i = 0; i < 4642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4643(int& val) {
    func_in_4643(val);
    const int x = 4643;
    std::cout << 4643;
    for (int i = 0; i < 4643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4643(int& val) {
    const int x = 4643;
    std::cout << 4643;
    for (int i = 0; i < 4643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4644(int& val) {
    func_in_4644(val);
    const int x = 4644;
    std::cout << 4644;
    for (int i = 0; i < 4644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4644(int& val) {
    const int x = 4644;
    std::cout << 4644;
    for (int i = 0; i < 4644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4645(int& val) {
    func_in_4645(val);
    const int x = 4645;
    std::cout << 4645;
    for (int i = 0; i < 4645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4645(int& val) {
    const int x = 4645;
    std::cout << 4645;
    for (int i = 0; i < 4645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4646(int& val) {
    func_in_4646(val);
    const int x = 4646;
    std::cout << 4646;
    for (int i = 0; i < 4646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4646(int& val) {
    const int x = 4646;
    std::cout << 4646;
    for (int i = 0; i < 4646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4647(int& val) {
    func_in_4647(val);
    const int x = 4647;
    std::cout << 4647;
    for (int i = 0; i < 4647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4647(int& val) {
    const int x = 4647;
    std::cout << 4647;
    for (int i = 0; i < 4647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4648(int& val) {
    func_in_4648(val);
    const int x = 4648;
    std::cout << 4648;
    for (int i = 0; i < 4648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4648(int& val) {
    const int x = 4648;
    std::cout << 4648;
    for (int i = 0; i < 4648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4649(int& val) {
    func_in_4649(val);
    const int x = 4649;
    std::cout << 4649;
    for (int i = 0; i < 4649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4649(int& val) {
    const int x = 4649;
    std::cout << 4649;
    for (int i = 0; i < 4649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4650(int& val) {
    func_in_4650(val);
    const int x = 4650;
    std::cout << 4650;
    for (int i = 0; i < 4650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4650(int& val) {
    const int x = 4650;
    std::cout << 4650;
    for (int i = 0; i < 4650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4651(int& val) {
    func_in_4651(val);
    const int x = 4651;
    std::cout << 4651;
    for (int i = 0; i < 4651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4651(int& val) {
    const int x = 4651;
    std::cout << 4651;
    for (int i = 0; i < 4651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4652(int& val) {
    func_in_4652(val);
    const int x = 4652;
    std::cout << 4652;
    for (int i = 0; i < 4652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4652(int& val) {
    const int x = 4652;
    std::cout << 4652;
    for (int i = 0; i < 4652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4653(int& val) {
    func_in_4653(val);
    const int x = 4653;
    std::cout << 4653;
    for (int i = 0; i < 4653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4653(int& val) {
    const int x = 4653;
    std::cout << 4653;
    for (int i = 0; i < 4653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4654(int& val) {
    func_in_4654(val);
    const int x = 4654;
    std::cout << 4654;
    for (int i = 0; i < 4654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4654(int& val) {
    const int x = 4654;
    std::cout << 4654;
    for (int i = 0; i < 4654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4655(int& val) {
    func_in_4655(val);
    const int x = 4655;
    std::cout << 4655;
    for (int i = 0; i < 4655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4655(int& val) {
    const int x = 4655;
    std::cout << 4655;
    for (int i = 0; i < 4655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4656(int& val) {
    func_in_4656(val);
    const int x = 4656;
    std::cout << 4656;
    for (int i = 0; i < 4656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4656(int& val) {
    const int x = 4656;
    std::cout << 4656;
    for (int i = 0; i < 4656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4657(int& val) {
    func_in_4657(val);
    const int x = 4657;
    std::cout << 4657;
    for (int i = 0; i < 4657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4657(int& val) {
    const int x = 4657;
    std::cout << 4657;
    for (int i = 0; i < 4657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4658(int& val) {
    func_in_4658(val);
    const int x = 4658;
    std::cout << 4658;
    for (int i = 0; i < 4658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4658(int& val) {
    const int x = 4658;
    std::cout << 4658;
    for (int i = 0; i < 4658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4659(int& val) {
    func_in_4659(val);
    const int x = 4659;
    std::cout << 4659;
    for (int i = 0; i < 4659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4659(int& val) {
    const int x = 4659;
    std::cout << 4659;
    for (int i = 0; i < 4659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4660(int& val) {
    func_in_4660(val);
    const int x = 4660;
    std::cout << 4660;
    for (int i = 0; i < 4660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4660(int& val) {
    const int x = 4660;
    std::cout << 4660;
    for (int i = 0; i < 4660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4661(int& val) {
    func_in_4661(val);
    const int x = 4661;
    std::cout << 4661;
    for (int i = 0; i < 4661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4661(int& val) {
    const int x = 4661;
    std::cout << 4661;
    for (int i = 0; i < 4661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4662(int& val) {
    func_in_4662(val);
    const int x = 4662;
    std::cout << 4662;
    for (int i = 0; i < 4662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4662(int& val) {
    const int x = 4662;
    std::cout << 4662;
    for (int i = 0; i < 4662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4663(int& val) {
    func_in_4663(val);
    const int x = 4663;
    std::cout << 4663;
    for (int i = 0; i < 4663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4663(int& val) {
    const int x = 4663;
    std::cout << 4663;
    for (int i = 0; i < 4663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4664(int& val) {
    func_in_4664(val);
    const int x = 4664;
    std::cout << 4664;
    for (int i = 0; i < 4664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4664(int& val) {
    const int x = 4664;
    std::cout << 4664;
    for (int i = 0; i < 4664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4665(int& val) {
    func_in_4665(val);
    const int x = 4665;
    std::cout << 4665;
    for (int i = 0; i < 4665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4665(int& val) {
    const int x = 4665;
    std::cout << 4665;
    for (int i = 0; i < 4665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4666(int& val) {
    func_in_4666(val);
    const int x = 4666;
    std::cout << 4666;
    for (int i = 0; i < 4666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4666(int& val) {
    const int x = 4666;
    std::cout << 4666;
    for (int i = 0; i < 4666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4667(int& val) {
    func_in_4667(val);
    const int x = 4667;
    std::cout << 4667;
    for (int i = 0; i < 4667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4667(int& val) {
    const int x = 4667;
    std::cout << 4667;
    for (int i = 0; i < 4667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4668(int& val) {
    func_in_4668(val);
    const int x = 4668;
    std::cout << 4668;
    for (int i = 0; i < 4668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4668(int& val) {
    const int x = 4668;
    std::cout << 4668;
    for (int i = 0; i < 4668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4669(int& val) {
    func_in_4669(val);
    const int x = 4669;
    std::cout << 4669;
    for (int i = 0; i < 4669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4669(int& val) {
    const int x = 4669;
    std::cout << 4669;
    for (int i = 0; i < 4669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4670(int& val) {
    func_in_4670(val);
    const int x = 4670;
    std::cout << 4670;
    for (int i = 0; i < 4670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4670(int& val) {
    const int x = 4670;
    std::cout << 4670;
    for (int i = 0; i < 4670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4671(int& val) {
    func_in_4671(val);
    const int x = 4671;
    std::cout << 4671;
    for (int i = 0; i < 4671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4671(int& val) {
    const int x = 4671;
    std::cout << 4671;
    for (int i = 0; i < 4671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4672(int& val) {
    func_in_4672(val);
    const int x = 4672;
    std::cout << 4672;
    for (int i = 0; i < 4672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4672(int& val) {
    const int x = 4672;
    std::cout << 4672;
    for (int i = 0; i < 4672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4673(int& val) {
    func_in_4673(val);
    const int x = 4673;
    std::cout << 4673;
    for (int i = 0; i < 4673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4673(int& val) {
    const int x = 4673;
    std::cout << 4673;
    for (int i = 0; i < 4673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4674(int& val) {
    func_in_4674(val);
    const int x = 4674;
    std::cout << 4674;
    for (int i = 0; i < 4674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4674(int& val) {
    const int x = 4674;
    std::cout << 4674;
    for (int i = 0; i < 4674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4675(int& val) {
    func_in_4675(val);
    const int x = 4675;
    std::cout << 4675;
    for (int i = 0; i < 4675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4675(int& val) {
    const int x = 4675;
    std::cout << 4675;
    for (int i = 0; i < 4675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4676(int& val) {
    func_in_4676(val);
    const int x = 4676;
    std::cout << 4676;
    for (int i = 0; i < 4676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4676(int& val) {
    const int x = 4676;
    std::cout << 4676;
    for (int i = 0; i < 4676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4677(int& val) {
    func_in_4677(val);
    const int x = 4677;
    std::cout << 4677;
    for (int i = 0; i < 4677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4677(int& val) {
    const int x = 4677;
    std::cout << 4677;
    for (int i = 0; i < 4677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4678(int& val) {
    func_in_4678(val);
    const int x = 4678;
    std::cout << 4678;
    for (int i = 0; i < 4678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4678(int& val) {
    const int x = 4678;
    std::cout << 4678;
    for (int i = 0; i < 4678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4679(int& val) {
    func_in_4679(val);
    const int x = 4679;
    std::cout << 4679;
    for (int i = 0; i < 4679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4679(int& val) {
    const int x = 4679;
    std::cout << 4679;
    for (int i = 0; i < 4679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4680(int& val) {
    func_in_4680(val);
    const int x = 4680;
    std::cout << 4680;
    for (int i = 0; i < 4680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4680(int& val) {
    const int x = 4680;
    std::cout << 4680;
    for (int i = 0; i < 4680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4681(int& val) {
    func_in_4681(val);
    const int x = 4681;
    std::cout << 4681;
    for (int i = 0; i < 4681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4681(int& val) {
    const int x = 4681;
    std::cout << 4681;
    for (int i = 0; i < 4681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4682(int& val) {
    func_in_4682(val);
    const int x = 4682;
    std::cout << 4682;
    for (int i = 0; i < 4682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4682(int& val) {
    const int x = 4682;
    std::cout << 4682;
    for (int i = 0; i < 4682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4683(int& val) {
    func_in_4683(val);
    const int x = 4683;
    std::cout << 4683;
    for (int i = 0; i < 4683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4683(int& val) {
    const int x = 4683;
    std::cout << 4683;
    for (int i = 0; i < 4683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4684(int& val) {
    func_in_4684(val);
    const int x = 4684;
    std::cout << 4684;
    for (int i = 0; i < 4684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4684(int& val) {
    const int x = 4684;
    std::cout << 4684;
    for (int i = 0; i < 4684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4685(int& val) {
    func_in_4685(val);
    const int x = 4685;
    std::cout << 4685;
    for (int i = 0; i < 4685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4685(int& val) {
    const int x = 4685;
    std::cout << 4685;
    for (int i = 0; i < 4685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4686(int& val) {
    func_in_4686(val);
    const int x = 4686;
    std::cout << 4686;
    for (int i = 0; i < 4686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4686(int& val) {
    const int x = 4686;
    std::cout << 4686;
    for (int i = 0; i < 4686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4687(int& val) {
    func_in_4687(val);
    const int x = 4687;
    std::cout << 4687;
    for (int i = 0; i < 4687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4687(int& val) {
    const int x = 4687;
    std::cout << 4687;
    for (int i = 0; i < 4687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4688(int& val) {
    func_in_4688(val);
    const int x = 4688;
    std::cout << 4688;
    for (int i = 0; i < 4688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4688(int& val) {
    const int x = 4688;
    std::cout << 4688;
    for (int i = 0; i < 4688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4689(int& val) {
    func_in_4689(val);
    const int x = 4689;
    std::cout << 4689;
    for (int i = 0; i < 4689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4689(int& val) {
    const int x = 4689;
    std::cout << 4689;
    for (int i = 0; i < 4689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4690(int& val) {
    func_in_4690(val);
    const int x = 4690;
    std::cout << 4690;
    for (int i = 0; i < 4690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4690(int& val) {
    const int x = 4690;
    std::cout << 4690;
    for (int i = 0; i < 4690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4691(int& val) {
    func_in_4691(val);
    const int x = 4691;
    std::cout << 4691;
    for (int i = 0; i < 4691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4691(int& val) {
    const int x = 4691;
    std::cout << 4691;
    for (int i = 0; i < 4691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4692(int& val) {
    func_in_4692(val);
    const int x = 4692;
    std::cout << 4692;
    for (int i = 0; i < 4692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4692(int& val) {
    const int x = 4692;
    std::cout << 4692;
    for (int i = 0; i < 4692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4693(int& val) {
    func_in_4693(val);
    const int x = 4693;
    std::cout << 4693;
    for (int i = 0; i < 4693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4693(int& val) {
    const int x = 4693;
    std::cout << 4693;
    for (int i = 0; i < 4693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4694(int& val) {
    func_in_4694(val);
    const int x = 4694;
    std::cout << 4694;
    for (int i = 0; i < 4694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4694(int& val) {
    const int x = 4694;
    std::cout << 4694;
    for (int i = 0; i < 4694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4695(int& val) {
    func_in_4695(val);
    const int x = 4695;
    std::cout << 4695;
    for (int i = 0; i < 4695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4695(int& val) {
    const int x = 4695;
    std::cout << 4695;
    for (int i = 0; i < 4695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4696(int& val) {
    func_in_4696(val);
    const int x = 4696;
    std::cout << 4696;
    for (int i = 0; i < 4696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4696(int& val) {
    const int x = 4696;
    std::cout << 4696;
    for (int i = 0; i < 4696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4697(int& val) {
    func_in_4697(val);
    const int x = 4697;
    std::cout << 4697;
    for (int i = 0; i < 4697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4697(int& val) {
    const int x = 4697;
    std::cout << 4697;
    for (int i = 0; i < 4697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4698(int& val) {
    func_in_4698(val);
    const int x = 4698;
    std::cout << 4698;
    for (int i = 0; i < 4698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4698(int& val) {
    const int x = 4698;
    std::cout << 4698;
    for (int i = 0; i < 4698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4699(int& val) {
    func_in_4699(val);
    const int x = 4699;
    std::cout << 4699;
    for (int i = 0; i < 4699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4699(int& val) {
    const int x = 4699;
    std::cout << 4699;
    for (int i = 0; i < 4699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4700(int& val) {
    func_in_4700(val);
    const int x = 4700;
    std::cout << 4700;
    for (int i = 0; i < 4700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4700(int& val) {
    const int x = 4700;
    std::cout << 4700;
    for (int i = 0; i < 4700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4701(int& val) {
    func_in_4701(val);
    const int x = 4701;
    std::cout << 4701;
    for (int i = 0; i < 4701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4701(int& val) {
    const int x = 4701;
    std::cout << 4701;
    for (int i = 0; i < 4701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4702(int& val) {
    func_in_4702(val);
    const int x = 4702;
    std::cout << 4702;
    for (int i = 0; i < 4702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4702(int& val) {
    const int x = 4702;
    std::cout << 4702;
    for (int i = 0; i < 4702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4703(int& val) {
    func_in_4703(val);
    const int x = 4703;
    std::cout << 4703;
    for (int i = 0; i < 4703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4703(int& val) {
    const int x = 4703;
    std::cout << 4703;
    for (int i = 0; i < 4703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4704(int& val) {
    func_in_4704(val);
    const int x = 4704;
    std::cout << 4704;
    for (int i = 0; i < 4704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4704(int& val) {
    const int x = 4704;
    std::cout << 4704;
    for (int i = 0; i < 4704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4705(int& val) {
    func_in_4705(val);
    const int x = 4705;
    std::cout << 4705;
    for (int i = 0; i < 4705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4705(int& val) {
    const int x = 4705;
    std::cout << 4705;
    for (int i = 0; i < 4705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4706(int& val) {
    func_in_4706(val);
    const int x = 4706;
    std::cout << 4706;
    for (int i = 0; i < 4706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4706(int& val) {
    const int x = 4706;
    std::cout << 4706;
    for (int i = 0; i < 4706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4707(int& val) {
    func_in_4707(val);
    const int x = 4707;
    std::cout << 4707;
    for (int i = 0; i < 4707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4707(int& val) {
    const int x = 4707;
    std::cout << 4707;
    for (int i = 0; i < 4707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4708(int& val) {
    func_in_4708(val);
    const int x = 4708;
    std::cout << 4708;
    for (int i = 0; i < 4708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4708(int& val) {
    const int x = 4708;
    std::cout << 4708;
    for (int i = 0; i < 4708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4709(int& val) {
    func_in_4709(val);
    const int x = 4709;
    std::cout << 4709;
    for (int i = 0; i < 4709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4709(int& val) {
    const int x = 4709;
    std::cout << 4709;
    for (int i = 0; i < 4709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4710(int& val) {
    func_in_4710(val);
    const int x = 4710;
    std::cout << 4710;
    for (int i = 0; i < 4710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4710(int& val) {
    const int x = 4710;
    std::cout << 4710;
    for (int i = 0; i < 4710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4711(int& val) {
    func_in_4711(val);
    const int x = 4711;
    std::cout << 4711;
    for (int i = 0; i < 4711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4711(int& val) {
    const int x = 4711;
    std::cout << 4711;
    for (int i = 0; i < 4711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4712(int& val) {
    func_in_4712(val);
    const int x = 4712;
    std::cout << 4712;
    for (int i = 0; i < 4712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4712(int& val) {
    const int x = 4712;
    std::cout << 4712;
    for (int i = 0; i < 4712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4713(int& val) {
    func_in_4713(val);
    const int x = 4713;
    std::cout << 4713;
    for (int i = 0; i < 4713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4713(int& val) {
    const int x = 4713;
    std::cout << 4713;
    for (int i = 0; i < 4713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4714(int& val) {
    func_in_4714(val);
    const int x = 4714;
    std::cout << 4714;
    for (int i = 0; i < 4714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4714(int& val) {
    const int x = 4714;
    std::cout << 4714;
    for (int i = 0; i < 4714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4715(int& val) {
    func_in_4715(val);
    const int x = 4715;
    std::cout << 4715;
    for (int i = 0; i < 4715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4715(int& val) {
    const int x = 4715;
    std::cout << 4715;
    for (int i = 0; i < 4715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4716(int& val) {
    func_in_4716(val);
    const int x = 4716;
    std::cout << 4716;
    for (int i = 0; i < 4716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4716(int& val) {
    const int x = 4716;
    std::cout << 4716;
    for (int i = 0; i < 4716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4717(int& val) {
    func_in_4717(val);
    const int x = 4717;
    std::cout << 4717;
    for (int i = 0; i < 4717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4717(int& val) {
    const int x = 4717;
    std::cout << 4717;
    for (int i = 0; i < 4717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4718(int& val) {
    func_in_4718(val);
    const int x = 4718;
    std::cout << 4718;
    for (int i = 0; i < 4718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4718(int& val) {
    const int x = 4718;
    std::cout << 4718;
    for (int i = 0; i < 4718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4719(int& val) {
    func_in_4719(val);
    const int x = 4719;
    std::cout << 4719;
    for (int i = 0; i < 4719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4719(int& val) {
    const int x = 4719;
    std::cout << 4719;
    for (int i = 0; i < 4719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4720(int& val) {
    func_in_4720(val);
    const int x = 4720;
    std::cout << 4720;
    for (int i = 0; i < 4720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4720(int& val) {
    const int x = 4720;
    std::cout << 4720;
    for (int i = 0; i < 4720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4721(int& val) {
    func_in_4721(val);
    const int x = 4721;
    std::cout << 4721;
    for (int i = 0; i < 4721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4721(int& val) {
    const int x = 4721;
    std::cout << 4721;
    for (int i = 0; i < 4721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4722(int& val) {
    func_in_4722(val);
    const int x = 4722;
    std::cout << 4722;
    for (int i = 0; i < 4722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4722(int& val) {
    const int x = 4722;
    std::cout << 4722;
    for (int i = 0; i < 4722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4723(int& val) {
    func_in_4723(val);
    const int x = 4723;
    std::cout << 4723;
    for (int i = 0; i < 4723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4723(int& val) {
    const int x = 4723;
    std::cout << 4723;
    for (int i = 0; i < 4723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4724(int& val) {
    func_in_4724(val);
    const int x = 4724;
    std::cout << 4724;
    for (int i = 0; i < 4724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4724(int& val) {
    const int x = 4724;
    std::cout << 4724;
    for (int i = 0; i < 4724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4725(int& val) {
    func_in_4725(val);
    const int x = 4725;
    std::cout << 4725;
    for (int i = 0; i < 4725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4725(int& val) {
    const int x = 4725;
    std::cout << 4725;
    for (int i = 0; i < 4725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4726(int& val) {
    func_in_4726(val);
    const int x = 4726;
    std::cout << 4726;
    for (int i = 0; i < 4726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4726(int& val) {
    const int x = 4726;
    std::cout << 4726;
    for (int i = 0; i < 4726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4727(int& val) {
    func_in_4727(val);
    const int x = 4727;
    std::cout << 4727;
    for (int i = 0; i < 4727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4727(int& val) {
    const int x = 4727;
    std::cout << 4727;
    for (int i = 0; i < 4727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4728(int& val) {
    func_in_4728(val);
    const int x = 4728;
    std::cout << 4728;
    for (int i = 0; i < 4728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4728(int& val) {
    const int x = 4728;
    std::cout << 4728;
    for (int i = 0; i < 4728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4729(int& val) {
    func_in_4729(val);
    const int x = 4729;
    std::cout << 4729;
    for (int i = 0; i < 4729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4729(int& val) {
    const int x = 4729;
    std::cout << 4729;
    for (int i = 0; i < 4729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4730(int& val) {
    func_in_4730(val);
    const int x = 4730;
    std::cout << 4730;
    for (int i = 0; i < 4730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4730(int& val) {
    const int x = 4730;
    std::cout << 4730;
    for (int i = 0; i < 4730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4731(int& val) {
    func_in_4731(val);
    const int x = 4731;
    std::cout << 4731;
    for (int i = 0; i < 4731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4731(int& val) {
    const int x = 4731;
    std::cout << 4731;
    for (int i = 0; i < 4731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4732(int& val) {
    func_in_4732(val);
    const int x = 4732;
    std::cout << 4732;
    for (int i = 0; i < 4732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4732(int& val) {
    const int x = 4732;
    std::cout << 4732;
    for (int i = 0; i < 4732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4733(int& val) {
    func_in_4733(val);
    const int x = 4733;
    std::cout << 4733;
    for (int i = 0; i < 4733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4733(int& val) {
    const int x = 4733;
    std::cout << 4733;
    for (int i = 0; i < 4733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4734(int& val) {
    func_in_4734(val);
    const int x = 4734;
    std::cout << 4734;
    for (int i = 0; i < 4734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4734(int& val) {
    const int x = 4734;
    std::cout << 4734;
    for (int i = 0; i < 4734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4735(int& val) {
    func_in_4735(val);
    const int x = 4735;
    std::cout << 4735;
    for (int i = 0; i < 4735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4735(int& val) {
    const int x = 4735;
    std::cout << 4735;
    for (int i = 0; i < 4735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4736(int& val) {
    func_in_4736(val);
    const int x = 4736;
    std::cout << 4736;
    for (int i = 0; i < 4736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4736(int& val) {
    const int x = 4736;
    std::cout << 4736;
    for (int i = 0; i < 4736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4737(int& val) {
    func_in_4737(val);
    const int x = 4737;
    std::cout << 4737;
    for (int i = 0; i < 4737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4737(int& val) {
    const int x = 4737;
    std::cout << 4737;
    for (int i = 0; i < 4737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4738(int& val) {
    func_in_4738(val);
    const int x = 4738;
    std::cout << 4738;
    for (int i = 0; i < 4738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4738(int& val) {
    const int x = 4738;
    std::cout << 4738;
    for (int i = 0; i < 4738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4739(int& val) {
    func_in_4739(val);
    const int x = 4739;
    std::cout << 4739;
    for (int i = 0; i < 4739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4739(int& val) {
    const int x = 4739;
    std::cout << 4739;
    for (int i = 0; i < 4739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4740(int& val) {
    func_in_4740(val);
    const int x = 4740;
    std::cout << 4740;
    for (int i = 0; i < 4740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4740(int& val) {
    const int x = 4740;
    std::cout << 4740;
    for (int i = 0; i < 4740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4741(int& val) {
    func_in_4741(val);
    const int x = 4741;
    std::cout << 4741;
    for (int i = 0; i < 4741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4741(int& val) {
    const int x = 4741;
    std::cout << 4741;
    for (int i = 0; i < 4741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4742(int& val) {
    func_in_4742(val);
    const int x = 4742;
    std::cout << 4742;
    for (int i = 0; i < 4742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4742(int& val) {
    const int x = 4742;
    std::cout << 4742;
    for (int i = 0; i < 4742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4743(int& val) {
    func_in_4743(val);
    const int x = 4743;
    std::cout << 4743;
    for (int i = 0; i < 4743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4743(int& val) {
    const int x = 4743;
    std::cout << 4743;
    for (int i = 0; i < 4743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4744(int& val) {
    func_in_4744(val);
    const int x = 4744;
    std::cout << 4744;
    for (int i = 0; i < 4744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4744(int& val) {
    const int x = 4744;
    std::cout << 4744;
    for (int i = 0; i < 4744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4745(int& val) {
    func_in_4745(val);
    const int x = 4745;
    std::cout << 4745;
    for (int i = 0; i < 4745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4745(int& val) {
    const int x = 4745;
    std::cout << 4745;
    for (int i = 0; i < 4745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4746(int& val) {
    func_in_4746(val);
    const int x = 4746;
    std::cout << 4746;
    for (int i = 0; i < 4746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4746(int& val) {
    const int x = 4746;
    std::cout << 4746;
    for (int i = 0; i < 4746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4747(int& val) {
    func_in_4747(val);
    const int x = 4747;
    std::cout << 4747;
    for (int i = 0; i < 4747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4747(int& val) {
    const int x = 4747;
    std::cout << 4747;
    for (int i = 0; i < 4747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4748(int& val) {
    func_in_4748(val);
    const int x = 4748;
    std::cout << 4748;
    for (int i = 0; i < 4748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4748(int& val) {
    const int x = 4748;
    std::cout << 4748;
    for (int i = 0; i < 4748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4749(int& val) {
    func_in_4749(val);
    const int x = 4749;
    std::cout << 4749;
    for (int i = 0; i < 4749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4749(int& val) {
    const int x = 4749;
    std::cout << 4749;
    for (int i = 0; i < 4749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4750(int& val) {
    func_in_4750(val);
    const int x = 4750;
    std::cout << 4750;
    for (int i = 0; i < 4750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4750(int& val) {
    const int x = 4750;
    std::cout << 4750;
    for (int i = 0; i < 4750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4751(int& val) {
    func_in_4751(val);
    const int x = 4751;
    std::cout << 4751;
    for (int i = 0; i < 4751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4751(int& val) {
    const int x = 4751;
    std::cout << 4751;
    for (int i = 0; i < 4751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4752(int& val) {
    func_in_4752(val);
    const int x = 4752;
    std::cout << 4752;
    for (int i = 0; i < 4752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4752(int& val) {
    const int x = 4752;
    std::cout << 4752;
    for (int i = 0; i < 4752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4753(int& val) {
    func_in_4753(val);
    const int x = 4753;
    std::cout << 4753;
    for (int i = 0; i < 4753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4753(int& val) {
    const int x = 4753;
    std::cout << 4753;
    for (int i = 0; i < 4753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4754(int& val) {
    func_in_4754(val);
    const int x = 4754;
    std::cout << 4754;
    for (int i = 0; i < 4754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4754(int& val) {
    const int x = 4754;
    std::cout << 4754;
    for (int i = 0; i < 4754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4755(int& val) {
    func_in_4755(val);
    const int x = 4755;
    std::cout << 4755;
    for (int i = 0; i < 4755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4755(int& val) {
    const int x = 4755;
    std::cout << 4755;
    for (int i = 0; i < 4755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4756(int& val) {
    func_in_4756(val);
    const int x = 4756;
    std::cout << 4756;
    for (int i = 0; i < 4756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4756(int& val) {
    const int x = 4756;
    std::cout << 4756;
    for (int i = 0; i < 4756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4757(int& val) {
    func_in_4757(val);
    const int x = 4757;
    std::cout << 4757;
    for (int i = 0; i < 4757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4757(int& val) {
    const int x = 4757;
    std::cout << 4757;
    for (int i = 0; i < 4757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4758(int& val) {
    func_in_4758(val);
    const int x = 4758;
    std::cout << 4758;
    for (int i = 0; i < 4758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4758(int& val) {
    const int x = 4758;
    std::cout << 4758;
    for (int i = 0; i < 4758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4759(int& val) {
    func_in_4759(val);
    const int x = 4759;
    std::cout << 4759;
    for (int i = 0; i < 4759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4759(int& val) {
    const int x = 4759;
    std::cout << 4759;
    for (int i = 0; i < 4759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4760(int& val) {
    func_in_4760(val);
    const int x = 4760;
    std::cout << 4760;
    for (int i = 0; i < 4760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4760(int& val) {
    const int x = 4760;
    std::cout << 4760;
    for (int i = 0; i < 4760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4761(int& val) {
    func_in_4761(val);
    const int x = 4761;
    std::cout << 4761;
    for (int i = 0; i < 4761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4761(int& val) {
    const int x = 4761;
    std::cout << 4761;
    for (int i = 0; i < 4761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4762(int& val) {
    func_in_4762(val);
    const int x = 4762;
    std::cout << 4762;
    for (int i = 0; i < 4762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4762(int& val) {
    const int x = 4762;
    std::cout << 4762;
    for (int i = 0; i < 4762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4763(int& val) {
    func_in_4763(val);
    const int x = 4763;
    std::cout << 4763;
    for (int i = 0; i < 4763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4763(int& val) {
    const int x = 4763;
    std::cout << 4763;
    for (int i = 0; i < 4763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4764(int& val) {
    func_in_4764(val);
    const int x = 4764;
    std::cout << 4764;
    for (int i = 0; i < 4764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4764(int& val) {
    const int x = 4764;
    std::cout << 4764;
    for (int i = 0; i < 4764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4765(int& val) {
    func_in_4765(val);
    const int x = 4765;
    std::cout << 4765;
    for (int i = 0; i < 4765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4765(int& val) {
    const int x = 4765;
    std::cout << 4765;
    for (int i = 0; i < 4765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4766(int& val) {
    func_in_4766(val);
    const int x = 4766;
    std::cout << 4766;
    for (int i = 0; i < 4766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4766(int& val) {
    const int x = 4766;
    std::cout << 4766;
    for (int i = 0; i < 4766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4767(int& val) {
    func_in_4767(val);
    const int x = 4767;
    std::cout << 4767;
    for (int i = 0; i < 4767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4767(int& val) {
    const int x = 4767;
    std::cout << 4767;
    for (int i = 0; i < 4767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4768(int& val) {
    func_in_4768(val);
    const int x = 4768;
    std::cout << 4768;
    for (int i = 0; i < 4768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4768(int& val) {
    const int x = 4768;
    std::cout << 4768;
    for (int i = 0; i < 4768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4769(int& val) {
    func_in_4769(val);
    const int x = 4769;
    std::cout << 4769;
    for (int i = 0; i < 4769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4769(int& val) {
    const int x = 4769;
    std::cout << 4769;
    for (int i = 0; i < 4769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4770(int& val) {
    func_in_4770(val);
    const int x = 4770;
    std::cout << 4770;
    for (int i = 0; i < 4770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4770(int& val) {
    const int x = 4770;
    std::cout << 4770;
    for (int i = 0; i < 4770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4771(int& val) {
    func_in_4771(val);
    const int x = 4771;
    std::cout << 4771;
    for (int i = 0; i < 4771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4771(int& val) {
    const int x = 4771;
    std::cout << 4771;
    for (int i = 0; i < 4771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4772(int& val) {
    func_in_4772(val);
    const int x = 4772;
    std::cout << 4772;
    for (int i = 0; i < 4772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4772(int& val) {
    const int x = 4772;
    std::cout << 4772;
    for (int i = 0; i < 4772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4773(int& val) {
    func_in_4773(val);
    const int x = 4773;
    std::cout << 4773;
    for (int i = 0; i < 4773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4773(int& val) {
    const int x = 4773;
    std::cout << 4773;
    for (int i = 0; i < 4773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4774(int& val) {
    func_in_4774(val);
    const int x = 4774;
    std::cout << 4774;
    for (int i = 0; i < 4774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4774(int& val) {
    const int x = 4774;
    std::cout << 4774;
    for (int i = 0; i < 4774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4775(int& val) {
    func_in_4775(val);
    const int x = 4775;
    std::cout << 4775;
    for (int i = 0; i < 4775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4775(int& val) {
    const int x = 4775;
    std::cout << 4775;
    for (int i = 0; i < 4775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4776(int& val) {
    func_in_4776(val);
    const int x = 4776;
    std::cout << 4776;
    for (int i = 0; i < 4776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4776(int& val) {
    const int x = 4776;
    std::cout << 4776;
    for (int i = 0; i < 4776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4777(int& val) {
    func_in_4777(val);
    const int x = 4777;
    std::cout << 4777;
    for (int i = 0; i < 4777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4777(int& val) {
    const int x = 4777;
    std::cout << 4777;
    for (int i = 0; i < 4777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4778(int& val) {
    func_in_4778(val);
    const int x = 4778;
    std::cout << 4778;
    for (int i = 0; i < 4778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4778(int& val) {
    const int x = 4778;
    std::cout << 4778;
    for (int i = 0; i < 4778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4779(int& val) {
    func_in_4779(val);
    const int x = 4779;
    std::cout << 4779;
    for (int i = 0; i < 4779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4779(int& val) {
    const int x = 4779;
    std::cout << 4779;
    for (int i = 0; i < 4779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4780(int& val) {
    func_in_4780(val);
    const int x = 4780;
    std::cout << 4780;
    for (int i = 0; i < 4780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4780(int& val) {
    const int x = 4780;
    std::cout << 4780;
    for (int i = 0; i < 4780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4781(int& val) {
    func_in_4781(val);
    const int x = 4781;
    std::cout << 4781;
    for (int i = 0; i < 4781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4781(int& val) {
    const int x = 4781;
    std::cout << 4781;
    for (int i = 0; i < 4781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4782(int& val) {
    func_in_4782(val);
    const int x = 4782;
    std::cout << 4782;
    for (int i = 0; i < 4782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4782(int& val) {
    const int x = 4782;
    std::cout << 4782;
    for (int i = 0; i < 4782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4783(int& val) {
    func_in_4783(val);
    const int x = 4783;
    std::cout << 4783;
    for (int i = 0; i < 4783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4783(int& val) {
    const int x = 4783;
    std::cout << 4783;
    for (int i = 0; i < 4783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4784(int& val) {
    func_in_4784(val);
    const int x = 4784;
    std::cout << 4784;
    for (int i = 0; i < 4784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4784(int& val) {
    const int x = 4784;
    std::cout << 4784;
    for (int i = 0; i < 4784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4785(int& val) {
    func_in_4785(val);
    const int x = 4785;
    std::cout << 4785;
    for (int i = 0; i < 4785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4785(int& val) {
    const int x = 4785;
    std::cout << 4785;
    for (int i = 0; i < 4785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4786(int& val) {
    func_in_4786(val);
    const int x = 4786;
    std::cout << 4786;
    for (int i = 0; i < 4786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4786(int& val) {
    const int x = 4786;
    std::cout << 4786;
    for (int i = 0; i < 4786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4787(int& val) {
    func_in_4787(val);
    const int x = 4787;
    std::cout << 4787;
    for (int i = 0; i < 4787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4787(int& val) {
    const int x = 4787;
    std::cout << 4787;
    for (int i = 0; i < 4787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4788(int& val) {
    func_in_4788(val);
    const int x = 4788;
    std::cout << 4788;
    for (int i = 0; i < 4788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4788(int& val) {
    const int x = 4788;
    std::cout << 4788;
    for (int i = 0; i < 4788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4789(int& val) {
    func_in_4789(val);
    const int x = 4789;
    std::cout << 4789;
    for (int i = 0; i < 4789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4789(int& val) {
    const int x = 4789;
    std::cout << 4789;
    for (int i = 0; i < 4789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4790(int& val) {
    func_in_4790(val);
    const int x = 4790;
    std::cout << 4790;
    for (int i = 0; i < 4790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4790(int& val) {
    const int x = 4790;
    std::cout << 4790;
    for (int i = 0; i < 4790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4791(int& val) {
    func_in_4791(val);
    const int x = 4791;
    std::cout << 4791;
    for (int i = 0; i < 4791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4791(int& val) {
    const int x = 4791;
    std::cout << 4791;
    for (int i = 0; i < 4791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4792(int& val) {
    func_in_4792(val);
    const int x = 4792;
    std::cout << 4792;
    for (int i = 0; i < 4792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4792(int& val) {
    const int x = 4792;
    std::cout << 4792;
    for (int i = 0; i < 4792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4793(int& val) {
    func_in_4793(val);
    const int x = 4793;
    std::cout << 4793;
    for (int i = 0; i < 4793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4793(int& val) {
    const int x = 4793;
    std::cout << 4793;
    for (int i = 0; i < 4793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4794(int& val) {
    func_in_4794(val);
    const int x = 4794;
    std::cout << 4794;
    for (int i = 0; i < 4794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4794(int& val) {
    const int x = 4794;
    std::cout << 4794;
    for (int i = 0; i < 4794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4795(int& val) {
    func_in_4795(val);
    const int x = 4795;
    std::cout << 4795;
    for (int i = 0; i < 4795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4795(int& val) {
    const int x = 4795;
    std::cout << 4795;
    for (int i = 0; i < 4795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4796(int& val) {
    func_in_4796(val);
    const int x = 4796;
    std::cout << 4796;
    for (int i = 0; i < 4796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4796(int& val) {
    const int x = 4796;
    std::cout << 4796;
    for (int i = 0; i < 4796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4797(int& val) {
    func_in_4797(val);
    const int x = 4797;
    std::cout << 4797;
    for (int i = 0; i < 4797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4797(int& val) {
    const int x = 4797;
    std::cout << 4797;
    for (int i = 0; i < 4797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4798(int& val) {
    func_in_4798(val);
    const int x = 4798;
    std::cout << 4798;
    for (int i = 0; i < 4798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4798(int& val) {
    const int x = 4798;
    std::cout << 4798;
    for (int i = 0; i < 4798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4799(int& val) {
    func_in_4799(val);
    const int x = 4799;
    std::cout << 4799;
    for (int i = 0; i < 4799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4799(int& val) {
    const int x = 4799;
    std::cout << 4799;
    for (int i = 0; i < 4799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4800(int& val) {
    func_in_4800(val);
    const int x = 4800;
    std::cout << 4800;
    for (int i = 0; i < 4800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4800(int& val) {
    const int x = 4800;
    std::cout << 4800;
    for (int i = 0; i < 4800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4801(int& val) {
    func_in_4801(val);
    const int x = 4801;
    std::cout << 4801;
    for (int i = 0; i < 4801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4801(int& val) {
    const int x = 4801;
    std::cout << 4801;
    for (int i = 0; i < 4801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4802(int& val) {
    func_in_4802(val);
    const int x = 4802;
    std::cout << 4802;
    for (int i = 0; i < 4802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4802(int& val) {
    const int x = 4802;
    std::cout << 4802;
    for (int i = 0; i < 4802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4803(int& val) {
    func_in_4803(val);
    const int x = 4803;
    std::cout << 4803;
    for (int i = 0; i < 4803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4803(int& val) {
    const int x = 4803;
    std::cout << 4803;
    for (int i = 0; i < 4803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4804(int& val) {
    func_in_4804(val);
    const int x = 4804;
    std::cout << 4804;
    for (int i = 0; i < 4804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4804(int& val) {
    const int x = 4804;
    std::cout << 4804;
    for (int i = 0; i < 4804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4805(int& val) {
    func_in_4805(val);
    const int x = 4805;
    std::cout << 4805;
    for (int i = 0; i < 4805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4805(int& val) {
    const int x = 4805;
    std::cout << 4805;
    for (int i = 0; i < 4805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4806(int& val) {
    func_in_4806(val);
    const int x = 4806;
    std::cout << 4806;
    for (int i = 0; i < 4806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4806(int& val) {
    const int x = 4806;
    std::cout << 4806;
    for (int i = 0; i < 4806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4807(int& val) {
    func_in_4807(val);
    const int x = 4807;
    std::cout << 4807;
    for (int i = 0; i < 4807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4807(int& val) {
    const int x = 4807;
    std::cout << 4807;
    for (int i = 0; i < 4807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4808(int& val) {
    func_in_4808(val);
    const int x = 4808;
    std::cout << 4808;
    for (int i = 0; i < 4808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4808(int& val) {
    const int x = 4808;
    std::cout << 4808;
    for (int i = 0; i < 4808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4809(int& val) {
    func_in_4809(val);
    const int x = 4809;
    std::cout << 4809;
    for (int i = 0; i < 4809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4809(int& val) {
    const int x = 4809;
    std::cout << 4809;
    for (int i = 0; i < 4809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4810(int& val) {
    func_in_4810(val);
    const int x = 4810;
    std::cout << 4810;
    for (int i = 0; i < 4810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4810(int& val) {
    const int x = 4810;
    std::cout << 4810;
    for (int i = 0; i < 4810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4811(int& val) {
    func_in_4811(val);
    const int x = 4811;
    std::cout << 4811;
    for (int i = 0; i < 4811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4811(int& val) {
    const int x = 4811;
    std::cout << 4811;
    for (int i = 0; i < 4811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4812(int& val) {
    func_in_4812(val);
    const int x = 4812;
    std::cout << 4812;
    for (int i = 0; i < 4812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4812(int& val) {
    const int x = 4812;
    std::cout << 4812;
    for (int i = 0; i < 4812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4813(int& val) {
    func_in_4813(val);
    const int x = 4813;
    std::cout << 4813;
    for (int i = 0; i < 4813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4813(int& val) {
    const int x = 4813;
    std::cout << 4813;
    for (int i = 0; i < 4813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4814(int& val) {
    func_in_4814(val);
    const int x = 4814;
    std::cout << 4814;
    for (int i = 0; i < 4814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4814(int& val) {
    const int x = 4814;
    std::cout << 4814;
    for (int i = 0; i < 4814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4815(int& val) {
    func_in_4815(val);
    const int x = 4815;
    std::cout << 4815;
    for (int i = 0; i < 4815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4815(int& val) {
    const int x = 4815;
    std::cout << 4815;
    for (int i = 0; i < 4815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4816(int& val) {
    func_in_4816(val);
    const int x = 4816;
    std::cout << 4816;
    for (int i = 0; i < 4816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4816(int& val) {
    const int x = 4816;
    std::cout << 4816;
    for (int i = 0; i < 4816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4817(int& val) {
    func_in_4817(val);
    const int x = 4817;
    std::cout << 4817;
    for (int i = 0; i < 4817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4817(int& val) {
    const int x = 4817;
    std::cout << 4817;
    for (int i = 0; i < 4817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4818(int& val) {
    func_in_4818(val);
    const int x = 4818;
    std::cout << 4818;
    for (int i = 0; i < 4818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4818(int& val) {
    const int x = 4818;
    std::cout << 4818;
    for (int i = 0; i < 4818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4819(int& val) {
    func_in_4819(val);
    const int x = 4819;
    std::cout << 4819;
    for (int i = 0; i < 4819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4819(int& val) {
    const int x = 4819;
    std::cout << 4819;
    for (int i = 0; i < 4819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4820(int& val) {
    func_in_4820(val);
    const int x = 4820;
    std::cout << 4820;
    for (int i = 0; i < 4820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4820(int& val) {
    const int x = 4820;
    std::cout << 4820;
    for (int i = 0; i < 4820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4821(int& val) {
    func_in_4821(val);
    const int x = 4821;
    std::cout << 4821;
    for (int i = 0; i < 4821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4821(int& val) {
    const int x = 4821;
    std::cout << 4821;
    for (int i = 0; i < 4821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4822(int& val) {
    func_in_4822(val);
    const int x = 4822;
    std::cout << 4822;
    for (int i = 0; i < 4822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4822(int& val) {
    const int x = 4822;
    std::cout << 4822;
    for (int i = 0; i < 4822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4823(int& val) {
    func_in_4823(val);
    const int x = 4823;
    std::cout << 4823;
    for (int i = 0; i < 4823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4823(int& val) {
    const int x = 4823;
    std::cout << 4823;
    for (int i = 0; i < 4823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4824(int& val) {
    func_in_4824(val);
    const int x = 4824;
    std::cout << 4824;
    for (int i = 0; i < 4824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4824(int& val) {
    const int x = 4824;
    std::cout << 4824;
    for (int i = 0; i < 4824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4825(int& val) {
    func_in_4825(val);
    const int x = 4825;
    std::cout << 4825;
    for (int i = 0; i < 4825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4825(int& val) {
    const int x = 4825;
    std::cout << 4825;
    for (int i = 0; i < 4825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4826(int& val) {
    func_in_4826(val);
    const int x = 4826;
    std::cout << 4826;
    for (int i = 0; i < 4826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4826(int& val) {
    const int x = 4826;
    std::cout << 4826;
    for (int i = 0; i < 4826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4827(int& val) {
    func_in_4827(val);
    const int x = 4827;
    std::cout << 4827;
    for (int i = 0; i < 4827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4827(int& val) {
    const int x = 4827;
    std::cout << 4827;
    for (int i = 0; i < 4827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4828(int& val) {
    func_in_4828(val);
    const int x = 4828;
    std::cout << 4828;
    for (int i = 0; i < 4828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4828(int& val) {
    const int x = 4828;
    std::cout << 4828;
    for (int i = 0; i < 4828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4829(int& val) {
    func_in_4829(val);
    const int x = 4829;
    std::cout << 4829;
    for (int i = 0; i < 4829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4829(int& val) {
    const int x = 4829;
    std::cout << 4829;
    for (int i = 0; i < 4829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4830(int& val) {
    func_in_4830(val);
    const int x = 4830;
    std::cout << 4830;
    for (int i = 0; i < 4830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4830(int& val) {
    const int x = 4830;
    std::cout << 4830;
    for (int i = 0; i < 4830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4831(int& val) {
    func_in_4831(val);
    const int x = 4831;
    std::cout << 4831;
    for (int i = 0; i < 4831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4831(int& val) {
    const int x = 4831;
    std::cout << 4831;
    for (int i = 0; i < 4831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4832(int& val) {
    func_in_4832(val);
    const int x = 4832;
    std::cout << 4832;
    for (int i = 0; i < 4832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4832(int& val) {
    const int x = 4832;
    std::cout << 4832;
    for (int i = 0; i < 4832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4833(int& val) {
    func_in_4833(val);
    const int x = 4833;
    std::cout << 4833;
    for (int i = 0; i < 4833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4833(int& val) {
    const int x = 4833;
    std::cout << 4833;
    for (int i = 0; i < 4833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4834(int& val) {
    func_in_4834(val);
    const int x = 4834;
    std::cout << 4834;
    for (int i = 0; i < 4834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4834(int& val) {
    const int x = 4834;
    std::cout << 4834;
    for (int i = 0; i < 4834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4835(int& val) {
    func_in_4835(val);
    const int x = 4835;
    std::cout << 4835;
    for (int i = 0; i < 4835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4835(int& val) {
    const int x = 4835;
    std::cout << 4835;
    for (int i = 0; i < 4835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4836(int& val) {
    func_in_4836(val);
    const int x = 4836;
    std::cout << 4836;
    for (int i = 0; i < 4836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4836(int& val) {
    const int x = 4836;
    std::cout << 4836;
    for (int i = 0; i < 4836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4837(int& val) {
    func_in_4837(val);
    const int x = 4837;
    std::cout << 4837;
    for (int i = 0; i < 4837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4837(int& val) {
    const int x = 4837;
    std::cout << 4837;
    for (int i = 0; i < 4837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4838(int& val) {
    func_in_4838(val);
    const int x = 4838;
    std::cout << 4838;
    for (int i = 0; i < 4838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4838(int& val) {
    const int x = 4838;
    std::cout << 4838;
    for (int i = 0; i < 4838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4839(int& val) {
    func_in_4839(val);
    const int x = 4839;
    std::cout << 4839;
    for (int i = 0; i < 4839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4839(int& val) {
    const int x = 4839;
    std::cout << 4839;
    for (int i = 0; i < 4839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4840(int& val) {
    func_in_4840(val);
    const int x = 4840;
    std::cout << 4840;
    for (int i = 0; i < 4840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4840(int& val) {
    const int x = 4840;
    std::cout << 4840;
    for (int i = 0; i < 4840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4841(int& val) {
    func_in_4841(val);
    const int x = 4841;
    std::cout << 4841;
    for (int i = 0; i < 4841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4841(int& val) {
    const int x = 4841;
    std::cout << 4841;
    for (int i = 0; i < 4841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4842(int& val) {
    func_in_4842(val);
    const int x = 4842;
    std::cout << 4842;
    for (int i = 0; i < 4842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4842(int& val) {
    const int x = 4842;
    std::cout << 4842;
    for (int i = 0; i < 4842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4843(int& val) {
    func_in_4843(val);
    const int x = 4843;
    std::cout << 4843;
    for (int i = 0; i < 4843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4843(int& val) {
    const int x = 4843;
    std::cout << 4843;
    for (int i = 0; i < 4843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4844(int& val) {
    func_in_4844(val);
    const int x = 4844;
    std::cout << 4844;
    for (int i = 0; i < 4844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4844(int& val) {
    const int x = 4844;
    std::cout << 4844;
    for (int i = 0; i < 4844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4845(int& val) {
    func_in_4845(val);
    const int x = 4845;
    std::cout << 4845;
    for (int i = 0; i < 4845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4845(int& val) {
    const int x = 4845;
    std::cout << 4845;
    for (int i = 0; i < 4845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4846(int& val) {
    func_in_4846(val);
    const int x = 4846;
    std::cout << 4846;
    for (int i = 0; i < 4846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4846(int& val) {
    const int x = 4846;
    std::cout << 4846;
    for (int i = 0; i < 4846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4847(int& val) {
    func_in_4847(val);
    const int x = 4847;
    std::cout << 4847;
    for (int i = 0; i < 4847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4847(int& val) {
    const int x = 4847;
    std::cout << 4847;
    for (int i = 0; i < 4847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4848(int& val) {
    func_in_4848(val);
    const int x = 4848;
    std::cout << 4848;
    for (int i = 0; i < 4848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4848(int& val) {
    const int x = 4848;
    std::cout << 4848;
    for (int i = 0; i < 4848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4849(int& val) {
    func_in_4849(val);
    const int x = 4849;
    std::cout << 4849;
    for (int i = 0; i < 4849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4849(int& val) {
    const int x = 4849;
    std::cout << 4849;
    for (int i = 0; i < 4849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4850(int& val) {
    func_in_4850(val);
    const int x = 4850;
    std::cout << 4850;
    for (int i = 0; i < 4850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4850(int& val) {
    const int x = 4850;
    std::cout << 4850;
    for (int i = 0; i < 4850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4851(int& val) {
    func_in_4851(val);
    const int x = 4851;
    std::cout << 4851;
    for (int i = 0; i < 4851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4851(int& val) {
    const int x = 4851;
    std::cout << 4851;
    for (int i = 0; i < 4851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4852(int& val) {
    func_in_4852(val);
    const int x = 4852;
    std::cout << 4852;
    for (int i = 0; i < 4852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4852(int& val) {
    const int x = 4852;
    std::cout << 4852;
    for (int i = 0; i < 4852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4853(int& val) {
    func_in_4853(val);
    const int x = 4853;
    std::cout << 4853;
    for (int i = 0; i < 4853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4853(int& val) {
    const int x = 4853;
    std::cout << 4853;
    for (int i = 0; i < 4853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4854(int& val) {
    func_in_4854(val);
    const int x = 4854;
    std::cout << 4854;
    for (int i = 0; i < 4854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4854(int& val) {
    const int x = 4854;
    std::cout << 4854;
    for (int i = 0; i < 4854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4855(int& val) {
    func_in_4855(val);
    const int x = 4855;
    std::cout << 4855;
    for (int i = 0; i < 4855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4855(int& val) {
    const int x = 4855;
    std::cout << 4855;
    for (int i = 0; i < 4855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4856(int& val) {
    func_in_4856(val);
    const int x = 4856;
    std::cout << 4856;
    for (int i = 0; i < 4856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4856(int& val) {
    const int x = 4856;
    std::cout << 4856;
    for (int i = 0; i < 4856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4857(int& val) {
    func_in_4857(val);
    const int x = 4857;
    std::cout << 4857;
    for (int i = 0; i < 4857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4857(int& val) {
    const int x = 4857;
    std::cout << 4857;
    for (int i = 0; i < 4857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4858(int& val) {
    func_in_4858(val);
    const int x = 4858;
    std::cout << 4858;
    for (int i = 0; i < 4858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4858(int& val) {
    const int x = 4858;
    std::cout << 4858;
    for (int i = 0; i < 4858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4859(int& val) {
    func_in_4859(val);
    const int x = 4859;
    std::cout << 4859;
    for (int i = 0; i < 4859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4859(int& val) {
    const int x = 4859;
    std::cout << 4859;
    for (int i = 0; i < 4859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4860(int& val) {
    func_in_4860(val);
    const int x = 4860;
    std::cout << 4860;
    for (int i = 0; i < 4860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4860(int& val) {
    const int x = 4860;
    std::cout << 4860;
    for (int i = 0; i < 4860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4861(int& val) {
    func_in_4861(val);
    const int x = 4861;
    std::cout << 4861;
    for (int i = 0; i < 4861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4861(int& val) {
    const int x = 4861;
    std::cout << 4861;
    for (int i = 0; i < 4861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4862(int& val) {
    func_in_4862(val);
    const int x = 4862;
    std::cout << 4862;
    for (int i = 0; i < 4862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4862(int& val) {
    const int x = 4862;
    std::cout << 4862;
    for (int i = 0; i < 4862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4863(int& val) {
    func_in_4863(val);
    const int x = 4863;
    std::cout << 4863;
    for (int i = 0; i < 4863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4863(int& val) {
    const int x = 4863;
    std::cout << 4863;
    for (int i = 0; i < 4863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4864(int& val) {
    func_in_4864(val);
    const int x = 4864;
    std::cout << 4864;
    for (int i = 0; i < 4864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4864(int& val) {
    const int x = 4864;
    std::cout << 4864;
    for (int i = 0; i < 4864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4865(int& val) {
    func_in_4865(val);
    const int x = 4865;
    std::cout << 4865;
    for (int i = 0; i < 4865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4865(int& val) {
    const int x = 4865;
    std::cout << 4865;
    for (int i = 0; i < 4865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4866(int& val) {
    func_in_4866(val);
    const int x = 4866;
    std::cout << 4866;
    for (int i = 0; i < 4866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4866(int& val) {
    const int x = 4866;
    std::cout << 4866;
    for (int i = 0; i < 4866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4867(int& val) {
    func_in_4867(val);
    const int x = 4867;
    std::cout << 4867;
    for (int i = 0; i < 4867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4867(int& val) {
    const int x = 4867;
    std::cout << 4867;
    for (int i = 0; i < 4867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4868(int& val) {
    func_in_4868(val);
    const int x = 4868;
    std::cout << 4868;
    for (int i = 0; i < 4868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4868(int& val) {
    const int x = 4868;
    std::cout << 4868;
    for (int i = 0; i < 4868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4869(int& val) {
    func_in_4869(val);
    const int x = 4869;
    std::cout << 4869;
    for (int i = 0; i < 4869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4869(int& val) {
    const int x = 4869;
    std::cout << 4869;
    for (int i = 0; i < 4869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4870(int& val) {
    func_in_4870(val);
    const int x = 4870;
    std::cout << 4870;
    for (int i = 0; i < 4870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4870(int& val) {
    const int x = 4870;
    std::cout << 4870;
    for (int i = 0; i < 4870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4871(int& val) {
    func_in_4871(val);
    const int x = 4871;
    std::cout << 4871;
    for (int i = 0; i < 4871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4871(int& val) {
    const int x = 4871;
    std::cout << 4871;
    for (int i = 0; i < 4871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4872(int& val) {
    func_in_4872(val);
    const int x = 4872;
    std::cout << 4872;
    for (int i = 0; i < 4872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4872(int& val) {
    const int x = 4872;
    std::cout << 4872;
    for (int i = 0; i < 4872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4873(int& val) {
    func_in_4873(val);
    const int x = 4873;
    std::cout << 4873;
    for (int i = 0; i < 4873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4873(int& val) {
    const int x = 4873;
    std::cout << 4873;
    for (int i = 0; i < 4873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4874(int& val) {
    func_in_4874(val);
    const int x = 4874;
    std::cout << 4874;
    for (int i = 0; i < 4874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4874(int& val) {
    const int x = 4874;
    std::cout << 4874;
    for (int i = 0; i < 4874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4875(int& val) {
    func_in_4875(val);
    const int x = 4875;
    std::cout << 4875;
    for (int i = 0; i < 4875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4875(int& val) {
    const int x = 4875;
    std::cout << 4875;
    for (int i = 0; i < 4875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4876(int& val) {
    func_in_4876(val);
    const int x = 4876;
    std::cout << 4876;
    for (int i = 0; i < 4876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4876(int& val) {
    const int x = 4876;
    std::cout << 4876;
    for (int i = 0; i < 4876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4877(int& val) {
    func_in_4877(val);
    const int x = 4877;
    std::cout << 4877;
    for (int i = 0; i < 4877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4877(int& val) {
    const int x = 4877;
    std::cout << 4877;
    for (int i = 0; i < 4877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4878(int& val) {
    func_in_4878(val);
    const int x = 4878;
    std::cout << 4878;
    for (int i = 0; i < 4878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4878(int& val) {
    const int x = 4878;
    std::cout << 4878;
    for (int i = 0; i < 4878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4879(int& val) {
    func_in_4879(val);
    const int x = 4879;
    std::cout << 4879;
    for (int i = 0; i < 4879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4879(int& val) {
    const int x = 4879;
    std::cout << 4879;
    for (int i = 0; i < 4879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4880(int& val) {
    func_in_4880(val);
    const int x = 4880;
    std::cout << 4880;
    for (int i = 0; i < 4880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4880(int& val) {
    const int x = 4880;
    std::cout << 4880;
    for (int i = 0; i < 4880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4881(int& val) {
    func_in_4881(val);
    const int x = 4881;
    std::cout << 4881;
    for (int i = 0; i < 4881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4881(int& val) {
    const int x = 4881;
    std::cout << 4881;
    for (int i = 0; i < 4881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4882(int& val) {
    func_in_4882(val);
    const int x = 4882;
    std::cout << 4882;
    for (int i = 0; i < 4882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4882(int& val) {
    const int x = 4882;
    std::cout << 4882;
    for (int i = 0; i < 4882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4883(int& val) {
    func_in_4883(val);
    const int x = 4883;
    std::cout << 4883;
    for (int i = 0; i < 4883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4883(int& val) {
    const int x = 4883;
    std::cout << 4883;
    for (int i = 0; i < 4883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4884(int& val) {
    func_in_4884(val);
    const int x = 4884;
    std::cout << 4884;
    for (int i = 0; i < 4884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4884(int& val) {
    const int x = 4884;
    std::cout << 4884;
    for (int i = 0; i < 4884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4885(int& val) {
    func_in_4885(val);
    const int x = 4885;
    std::cout << 4885;
    for (int i = 0; i < 4885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4885(int& val) {
    const int x = 4885;
    std::cout << 4885;
    for (int i = 0; i < 4885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4886(int& val) {
    func_in_4886(val);
    const int x = 4886;
    std::cout << 4886;
    for (int i = 0; i < 4886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4886(int& val) {
    const int x = 4886;
    std::cout << 4886;
    for (int i = 0; i < 4886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4887(int& val) {
    func_in_4887(val);
    const int x = 4887;
    std::cout << 4887;
    for (int i = 0; i < 4887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4887(int& val) {
    const int x = 4887;
    std::cout << 4887;
    for (int i = 0; i < 4887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4888(int& val) {
    func_in_4888(val);
    const int x = 4888;
    std::cout << 4888;
    for (int i = 0; i < 4888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4888(int& val) {
    const int x = 4888;
    std::cout << 4888;
    for (int i = 0; i < 4888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4889(int& val) {
    func_in_4889(val);
    const int x = 4889;
    std::cout << 4889;
    for (int i = 0; i < 4889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4889(int& val) {
    const int x = 4889;
    std::cout << 4889;
    for (int i = 0; i < 4889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4890(int& val) {
    func_in_4890(val);
    const int x = 4890;
    std::cout << 4890;
    for (int i = 0; i < 4890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4890(int& val) {
    const int x = 4890;
    std::cout << 4890;
    for (int i = 0; i < 4890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4891(int& val) {
    func_in_4891(val);
    const int x = 4891;
    std::cout << 4891;
    for (int i = 0; i < 4891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4891(int& val) {
    const int x = 4891;
    std::cout << 4891;
    for (int i = 0; i < 4891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4892(int& val) {
    func_in_4892(val);
    const int x = 4892;
    std::cout << 4892;
    for (int i = 0; i < 4892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4892(int& val) {
    const int x = 4892;
    std::cout << 4892;
    for (int i = 0; i < 4892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4893(int& val) {
    func_in_4893(val);
    const int x = 4893;
    std::cout << 4893;
    for (int i = 0; i < 4893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4893(int& val) {
    const int x = 4893;
    std::cout << 4893;
    for (int i = 0; i < 4893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4894(int& val) {
    func_in_4894(val);
    const int x = 4894;
    std::cout << 4894;
    for (int i = 0; i < 4894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4894(int& val) {
    const int x = 4894;
    std::cout << 4894;
    for (int i = 0; i < 4894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4895(int& val) {
    func_in_4895(val);
    const int x = 4895;
    std::cout << 4895;
    for (int i = 0; i < 4895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4895(int& val) {
    const int x = 4895;
    std::cout << 4895;
    for (int i = 0; i < 4895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4896(int& val) {
    func_in_4896(val);
    const int x = 4896;
    std::cout << 4896;
    for (int i = 0; i < 4896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4896(int& val) {
    const int x = 4896;
    std::cout << 4896;
    for (int i = 0; i < 4896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4897(int& val) {
    func_in_4897(val);
    const int x = 4897;
    std::cout << 4897;
    for (int i = 0; i < 4897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4897(int& val) {
    const int x = 4897;
    std::cout << 4897;
    for (int i = 0; i < 4897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4898(int& val) {
    func_in_4898(val);
    const int x = 4898;
    std::cout << 4898;
    for (int i = 0; i < 4898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4898(int& val) {
    const int x = 4898;
    std::cout << 4898;
    for (int i = 0; i < 4898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4899(int& val) {
    func_in_4899(val);
    const int x = 4899;
    std::cout << 4899;
    for (int i = 0; i < 4899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4899(int& val) {
    const int x = 4899;
    std::cout << 4899;
    for (int i = 0; i < 4899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4900(int& val) {
    func_in_4900(val);
    const int x = 4900;
    std::cout << 4900;
    for (int i = 0; i < 4900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4900(int& val) {
    const int x = 4900;
    std::cout << 4900;
    for (int i = 0; i < 4900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4901(int& val) {
    func_in_4901(val);
    const int x = 4901;
    std::cout << 4901;
    for (int i = 0; i < 4901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4901(int& val) {
    const int x = 4901;
    std::cout << 4901;
    for (int i = 0; i < 4901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4902(int& val) {
    func_in_4902(val);
    const int x = 4902;
    std::cout << 4902;
    for (int i = 0; i < 4902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4902(int& val) {
    const int x = 4902;
    std::cout << 4902;
    for (int i = 0; i < 4902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4903(int& val) {
    func_in_4903(val);
    const int x = 4903;
    std::cout << 4903;
    for (int i = 0; i < 4903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4903(int& val) {
    const int x = 4903;
    std::cout << 4903;
    for (int i = 0; i < 4903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4904(int& val) {
    func_in_4904(val);
    const int x = 4904;
    std::cout << 4904;
    for (int i = 0; i < 4904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4904(int& val) {
    const int x = 4904;
    std::cout << 4904;
    for (int i = 0; i < 4904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4905(int& val) {
    func_in_4905(val);
    const int x = 4905;
    std::cout << 4905;
    for (int i = 0; i < 4905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4905(int& val) {
    const int x = 4905;
    std::cout << 4905;
    for (int i = 0; i < 4905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4906(int& val) {
    func_in_4906(val);
    const int x = 4906;
    std::cout << 4906;
    for (int i = 0; i < 4906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4906(int& val) {
    const int x = 4906;
    std::cout << 4906;
    for (int i = 0; i < 4906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4907(int& val) {
    func_in_4907(val);
    const int x = 4907;
    std::cout << 4907;
    for (int i = 0; i < 4907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4907(int& val) {
    const int x = 4907;
    std::cout << 4907;
    for (int i = 0; i < 4907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4908(int& val) {
    func_in_4908(val);
    const int x = 4908;
    std::cout << 4908;
    for (int i = 0; i < 4908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4908(int& val) {
    const int x = 4908;
    std::cout << 4908;
    for (int i = 0; i < 4908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4909(int& val) {
    func_in_4909(val);
    const int x = 4909;
    std::cout << 4909;
    for (int i = 0; i < 4909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4909(int& val) {
    const int x = 4909;
    std::cout << 4909;
    for (int i = 0; i < 4909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4910(int& val) {
    func_in_4910(val);
    const int x = 4910;
    std::cout << 4910;
    for (int i = 0; i < 4910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4910(int& val) {
    const int x = 4910;
    std::cout << 4910;
    for (int i = 0; i < 4910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4911(int& val) {
    func_in_4911(val);
    const int x = 4911;
    std::cout << 4911;
    for (int i = 0; i < 4911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4911(int& val) {
    const int x = 4911;
    std::cout << 4911;
    for (int i = 0; i < 4911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4912(int& val) {
    func_in_4912(val);
    const int x = 4912;
    std::cout << 4912;
    for (int i = 0; i < 4912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4912(int& val) {
    const int x = 4912;
    std::cout << 4912;
    for (int i = 0; i < 4912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4913(int& val) {
    func_in_4913(val);
    const int x = 4913;
    std::cout << 4913;
    for (int i = 0; i < 4913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4913(int& val) {
    const int x = 4913;
    std::cout << 4913;
    for (int i = 0; i < 4913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4914(int& val) {
    func_in_4914(val);
    const int x = 4914;
    std::cout << 4914;
    for (int i = 0; i < 4914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4914(int& val) {
    const int x = 4914;
    std::cout << 4914;
    for (int i = 0; i < 4914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4915(int& val) {
    func_in_4915(val);
    const int x = 4915;
    std::cout << 4915;
    for (int i = 0; i < 4915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4915(int& val) {
    const int x = 4915;
    std::cout << 4915;
    for (int i = 0; i < 4915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4916(int& val) {
    func_in_4916(val);
    const int x = 4916;
    std::cout << 4916;
    for (int i = 0; i < 4916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4916(int& val) {
    const int x = 4916;
    std::cout << 4916;
    for (int i = 0; i < 4916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4917(int& val) {
    func_in_4917(val);
    const int x = 4917;
    std::cout << 4917;
    for (int i = 0; i < 4917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4917(int& val) {
    const int x = 4917;
    std::cout << 4917;
    for (int i = 0; i < 4917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4918(int& val) {
    func_in_4918(val);
    const int x = 4918;
    std::cout << 4918;
    for (int i = 0; i < 4918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4918(int& val) {
    const int x = 4918;
    std::cout << 4918;
    for (int i = 0; i < 4918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4919(int& val) {
    func_in_4919(val);
    const int x = 4919;
    std::cout << 4919;
    for (int i = 0; i < 4919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4919(int& val) {
    const int x = 4919;
    std::cout << 4919;
    for (int i = 0; i < 4919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4920(int& val) {
    func_in_4920(val);
    const int x = 4920;
    std::cout << 4920;
    for (int i = 0; i < 4920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4920(int& val) {
    const int x = 4920;
    std::cout << 4920;
    for (int i = 0; i < 4920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4921(int& val) {
    func_in_4921(val);
    const int x = 4921;
    std::cout << 4921;
    for (int i = 0; i < 4921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4921(int& val) {
    const int x = 4921;
    std::cout << 4921;
    for (int i = 0; i < 4921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4922(int& val) {
    func_in_4922(val);
    const int x = 4922;
    std::cout << 4922;
    for (int i = 0; i < 4922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4922(int& val) {
    const int x = 4922;
    std::cout << 4922;
    for (int i = 0; i < 4922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4923(int& val) {
    func_in_4923(val);
    const int x = 4923;
    std::cout << 4923;
    for (int i = 0; i < 4923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4923(int& val) {
    const int x = 4923;
    std::cout << 4923;
    for (int i = 0; i < 4923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4924(int& val) {
    func_in_4924(val);
    const int x = 4924;
    std::cout << 4924;
    for (int i = 0; i < 4924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4924(int& val) {
    const int x = 4924;
    std::cout << 4924;
    for (int i = 0; i < 4924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4925(int& val) {
    func_in_4925(val);
    const int x = 4925;
    std::cout << 4925;
    for (int i = 0; i < 4925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4925(int& val) {
    const int x = 4925;
    std::cout << 4925;
    for (int i = 0; i < 4925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4926(int& val) {
    func_in_4926(val);
    const int x = 4926;
    std::cout << 4926;
    for (int i = 0; i < 4926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4926(int& val) {
    const int x = 4926;
    std::cout << 4926;
    for (int i = 0; i < 4926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4927(int& val) {
    func_in_4927(val);
    const int x = 4927;
    std::cout << 4927;
    for (int i = 0; i < 4927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4927(int& val) {
    const int x = 4927;
    std::cout << 4927;
    for (int i = 0; i < 4927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4928(int& val) {
    func_in_4928(val);
    const int x = 4928;
    std::cout << 4928;
    for (int i = 0; i < 4928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4928(int& val) {
    const int x = 4928;
    std::cout << 4928;
    for (int i = 0; i < 4928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4929(int& val) {
    func_in_4929(val);
    const int x = 4929;
    std::cout << 4929;
    for (int i = 0; i < 4929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4929(int& val) {
    const int x = 4929;
    std::cout << 4929;
    for (int i = 0; i < 4929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4930(int& val) {
    func_in_4930(val);
    const int x = 4930;
    std::cout << 4930;
    for (int i = 0; i < 4930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4930(int& val) {
    const int x = 4930;
    std::cout << 4930;
    for (int i = 0; i < 4930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4931(int& val) {
    func_in_4931(val);
    const int x = 4931;
    std::cout << 4931;
    for (int i = 0; i < 4931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4931(int& val) {
    const int x = 4931;
    std::cout << 4931;
    for (int i = 0; i < 4931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4932(int& val) {
    func_in_4932(val);
    const int x = 4932;
    std::cout << 4932;
    for (int i = 0; i < 4932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4932(int& val) {
    const int x = 4932;
    std::cout << 4932;
    for (int i = 0; i < 4932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4933(int& val) {
    func_in_4933(val);
    const int x = 4933;
    std::cout << 4933;
    for (int i = 0; i < 4933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4933(int& val) {
    const int x = 4933;
    std::cout << 4933;
    for (int i = 0; i < 4933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4934(int& val) {
    func_in_4934(val);
    const int x = 4934;
    std::cout << 4934;
    for (int i = 0; i < 4934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4934(int& val) {
    const int x = 4934;
    std::cout << 4934;
    for (int i = 0; i < 4934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4935(int& val) {
    func_in_4935(val);
    const int x = 4935;
    std::cout << 4935;
    for (int i = 0; i < 4935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4935(int& val) {
    const int x = 4935;
    std::cout << 4935;
    for (int i = 0; i < 4935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4936(int& val) {
    func_in_4936(val);
    const int x = 4936;
    std::cout << 4936;
    for (int i = 0; i < 4936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4936(int& val) {
    const int x = 4936;
    std::cout << 4936;
    for (int i = 0; i < 4936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4937(int& val) {
    func_in_4937(val);
    const int x = 4937;
    std::cout << 4937;
    for (int i = 0; i < 4937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4937(int& val) {
    const int x = 4937;
    std::cout << 4937;
    for (int i = 0; i < 4937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4938(int& val) {
    func_in_4938(val);
    const int x = 4938;
    std::cout << 4938;
    for (int i = 0; i < 4938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4938(int& val) {
    const int x = 4938;
    std::cout << 4938;
    for (int i = 0; i < 4938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4939(int& val) {
    func_in_4939(val);
    const int x = 4939;
    std::cout << 4939;
    for (int i = 0; i < 4939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4939(int& val) {
    const int x = 4939;
    std::cout << 4939;
    for (int i = 0; i < 4939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4940(int& val) {
    func_in_4940(val);
    const int x = 4940;
    std::cout << 4940;
    for (int i = 0; i < 4940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4940(int& val) {
    const int x = 4940;
    std::cout << 4940;
    for (int i = 0; i < 4940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4941(int& val) {
    func_in_4941(val);
    const int x = 4941;
    std::cout << 4941;
    for (int i = 0; i < 4941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4941(int& val) {
    const int x = 4941;
    std::cout << 4941;
    for (int i = 0; i < 4941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4942(int& val) {
    func_in_4942(val);
    const int x = 4942;
    std::cout << 4942;
    for (int i = 0; i < 4942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4942(int& val) {
    const int x = 4942;
    std::cout << 4942;
    for (int i = 0; i < 4942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4943(int& val) {
    func_in_4943(val);
    const int x = 4943;
    std::cout << 4943;
    for (int i = 0; i < 4943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4943(int& val) {
    const int x = 4943;
    std::cout << 4943;
    for (int i = 0; i < 4943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4944(int& val) {
    func_in_4944(val);
    const int x = 4944;
    std::cout << 4944;
    for (int i = 0; i < 4944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4944(int& val) {
    const int x = 4944;
    std::cout << 4944;
    for (int i = 0; i < 4944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4945(int& val) {
    func_in_4945(val);
    const int x = 4945;
    std::cout << 4945;
    for (int i = 0; i < 4945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4945(int& val) {
    const int x = 4945;
    std::cout << 4945;
    for (int i = 0; i < 4945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4946(int& val) {
    func_in_4946(val);
    const int x = 4946;
    std::cout << 4946;
    for (int i = 0; i < 4946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4946(int& val) {
    const int x = 4946;
    std::cout << 4946;
    for (int i = 0; i < 4946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4947(int& val) {
    func_in_4947(val);
    const int x = 4947;
    std::cout << 4947;
    for (int i = 0; i < 4947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4947(int& val) {
    const int x = 4947;
    std::cout << 4947;
    for (int i = 0; i < 4947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4948(int& val) {
    func_in_4948(val);
    const int x = 4948;
    std::cout << 4948;
    for (int i = 0; i < 4948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4948(int& val) {
    const int x = 4948;
    std::cout << 4948;
    for (int i = 0; i < 4948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4949(int& val) {
    func_in_4949(val);
    const int x = 4949;
    std::cout << 4949;
    for (int i = 0; i < 4949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4949(int& val) {
    const int x = 4949;
    std::cout << 4949;
    for (int i = 0; i < 4949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4950(int& val) {
    func_in_4950(val);
    const int x = 4950;
    std::cout << 4950;
    for (int i = 0; i < 4950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4950(int& val) {
    const int x = 4950;
    std::cout << 4950;
    for (int i = 0; i < 4950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4951(int& val) {
    func_in_4951(val);
    const int x = 4951;
    std::cout << 4951;
    for (int i = 0; i < 4951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4951(int& val) {
    const int x = 4951;
    std::cout << 4951;
    for (int i = 0; i < 4951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4952(int& val) {
    func_in_4952(val);
    const int x = 4952;
    std::cout << 4952;
    for (int i = 0; i < 4952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4952(int& val) {
    const int x = 4952;
    std::cout << 4952;
    for (int i = 0; i < 4952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4953(int& val) {
    func_in_4953(val);
    const int x = 4953;
    std::cout << 4953;
    for (int i = 0; i < 4953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4953(int& val) {
    const int x = 4953;
    std::cout << 4953;
    for (int i = 0; i < 4953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4954(int& val) {
    func_in_4954(val);
    const int x = 4954;
    std::cout << 4954;
    for (int i = 0; i < 4954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4954(int& val) {
    const int x = 4954;
    std::cout << 4954;
    for (int i = 0; i < 4954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4955(int& val) {
    func_in_4955(val);
    const int x = 4955;
    std::cout << 4955;
    for (int i = 0; i < 4955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4955(int& val) {
    const int x = 4955;
    std::cout << 4955;
    for (int i = 0; i < 4955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4956(int& val) {
    func_in_4956(val);
    const int x = 4956;
    std::cout << 4956;
    for (int i = 0; i < 4956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4956(int& val) {
    const int x = 4956;
    std::cout << 4956;
    for (int i = 0; i < 4956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4957(int& val) {
    func_in_4957(val);
    const int x = 4957;
    std::cout << 4957;
    for (int i = 0; i < 4957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4957(int& val) {
    const int x = 4957;
    std::cout << 4957;
    for (int i = 0; i < 4957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4958(int& val) {
    func_in_4958(val);
    const int x = 4958;
    std::cout << 4958;
    for (int i = 0; i < 4958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4958(int& val) {
    const int x = 4958;
    std::cout << 4958;
    for (int i = 0; i < 4958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4959(int& val) {
    func_in_4959(val);
    const int x = 4959;
    std::cout << 4959;
    for (int i = 0; i < 4959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4959(int& val) {
    const int x = 4959;
    std::cout << 4959;
    for (int i = 0; i < 4959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4960(int& val) {
    func_in_4960(val);
    const int x = 4960;
    std::cout << 4960;
    for (int i = 0; i < 4960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4960(int& val) {
    const int x = 4960;
    std::cout << 4960;
    for (int i = 0; i < 4960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4961(int& val) {
    func_in_4961(val);
    const int x = 4961;
    std::cout << 4961;
    for (int i = 0; i < 4961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4961(int& val) {
    const int x = 4961;
    std::cout << 4961;
    for (int i = 0; i < 4961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4962(int& val) {
    func_in_4962(val);
    const int x = 4962;
    std::cout << 4962;
    for (int i = 0; i < 4962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4962(int& val) {
    const int x = 4962;
    std::cout << 4962;
    for (int i = 0; i < 4962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4963(int& val) {
    func_in_4963(val);
    const int x = 4963;
    std::cout << 4963;
    for (int i = 0; i < 4963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4963(int& val) {
    const int x = 4963;
    std::cout << 4963;
    for (int i = 0; i < 4963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4964(int& val) {
    func_in_4964(val);
    const int x = 4964;
    std::cout << 4964;
    for (int i = 0; i < 4964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4964(int& val) {
    const int x = 4964;
    std::cout << 4964;
    for (int i = 0; i < 4964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4965(int& val) {
    func_in_4965(val);
    const int x = 4965;
    std::cout << 4965;
    for (int i = 0; i < 4965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4965(int& val) {
    const int x = 4965;
    std::cout << 4965;
    for (int i = 0; i < 4965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4966(int& val) {
    func_in_4966(val);
    const int x = 4966;
    std::cout << 4966;
    for (int i = 0; i < 4966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4966(int& val) {
    const int x = 4966;
    std::cout << 4966;
    for (int i = 0; i < 4966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4967(int& val) {
    func_in_4967(val);
    const int x = 4967;
    std::cout << 4967;
    for (int i = 0; i < 4967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4967(int& val) {
    const int x = 4967;
    std::cout << 4967;
    for (int i = 0; i < 4967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4968(int& val) {
    func_in_4968(val);
    const int x = 4968;
    std::cout << 4968;
    for (int i = 0; i < 4968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4968(int& val) {
    const int x = 4968;
    std::cout << 4968;
    for (int i = 0; i < 4968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4969(int& val) {
    func_in_4969(val);
    const int x = 4969;
    std::cout << 4969;
    for (int i = 0; i < 4969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4969(int& val) {
    const int x = 4969;
    std::cout << 4969;
    for (int i = 0; i < 4969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4970(int& val) {
    func_in_4970(val);
    const int x = 4970;
    std::cout << 4970;
    for (int i = 0; i < 4970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4970(int& val) {
    const int x = 4970;
    std::cout << 4970;
    for (int i = 0; i < 4970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4971(int& val) {
    func_in_4971(val);
    const int x = 4971;
    std::cout << 4971;
    for (int i = 0; i < 4971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4971(int& val) {
    const int x = 4971;
    std::cout << 4971;
    for (int i = 0; i < 4971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4972(int& val) {
    func_in_4972(val);
    const int x = 4972;
    std::cout << 4972;
    for (int i = 0; i < 4972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4972(int& val) {
    const int x = 4972;
    std::cout << 4972;
    for (int i = 0; i < 4972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4973(int& val) {
    func_in_4973(val);
    const int x = 4973;
    std::cout << 4973;
    for (int i = 0; i < 4973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4973(int& val) {
    const int x = 4973;
    std::cout << 4973;
    for (int i = 0; i < 4973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4974(int& val) {
    func_in_4974(val);
    const int x = 4974;
    std::cout << 4974;
    for (int i = 0; i < 4974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4974(int& val) {
    const int x = 4974;
    std::cout << 4974;
    for (int i = 0; i < 4974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4975(int& val) {
    func_in_4975(val);
    const int x = 4975;
    std::cout << 4975;
    for (int i = 0; i < 4975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4975(int& val) {
    const int x = 4975;
    std::cout << 4975;
    for (int i = 0; i < 4975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4976(int& val) {
    func_in_4976(val);
    const int x = 4976;
    std::cout << 4976;
    for (int i = 0; i < 4976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4976(int& val) {
    const int x = 4976;
    std::cout << 4976;
    for (int i = 0; i < 4976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4977(int& val) {
    func_in_4977(val);
    const int x = 4977;
    std::cout << 4977;
    for (int i = 0; i < 4977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4977(int& val) {
    const int x = 4977;
    std::cout << 4977;
    for (int i = 0; i < 4977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4978(int& val) {
    func_in_4978(val);
    const int x = 4978;
    std::cout << 4978;
    for (int i = 0; i < 4978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4978(int& val) {
    const int x = 4978;
    std::cout << 4978;
    for (int i = 0; i < 4978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4979(int& val) {
    func_in_4979(val);
    const int x = 4979;
    std::cout << 4979;
    for (int i = 0; i < 4979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4979(int& val) {
    const int x = 4979;
    std::cout << 4979;
    for (int i = 0; i < 4979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4980(int& val) {
    func_in_4980(val);
    const int x = 4980;
    std::cout << 4980;
    for (int i = 0; i < 4980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4980(int& val) {
    const int x = 4980;
    std::cout << 4980;
    for (int i = 0; i < 4980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4981(int& val) {
    func_in_4981(val);
    const int x = 4981;
    std::cout << 4981;
    for (int i = 0; i < 4981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4981(int& val) {
    const int x = 4981;
    std::cout << 4981;
    for (int i = 0; i < 4981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4982(int& val) {
    func_in_4982(val);
    const int x = 4982;
    std::cout << 4982;
    for (int i = 0; i < 4982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4982(int& val) {
    const int x = 4982;
    std::cout << 4982;
    for (int i = 0; i < 4982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4983(int& val) {
    func_in_4983(val);
    const int x = 4983;
    std::cout << 4983;
    for (int i = 0; i < 4983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4983(int& val) {
    const int x = 4983;
    std::cout << 4983;
    for (int i = 0; i < 4983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4984(int& val) {
    func_in_4984(val);
    const int x = 4984;
    std::cout << 4984;
    for (int i = 0; i < 4984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4984(int& val) {
    const int x = 4984;
    std::cout << 4984;
    for (int i = 0; i < 4984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4985(int& val) {
    func_in_4985(val);
    const int x = 4985;
    std::cout << 4985;
    for (int i = 0; i < 4985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4985(int& val) {
    const int x = 4985;
    std::cout << 4985;
    for (int i = 0; i < 4985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4986(int& val) {
    func_in_4986(val);
    const int x = 4986;
    std::cout << 4986;
    for (int i = 0; i < 4986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4986(int& val) {
    const int x = 4986;
    std::cout << 4986;
    for (int i = 0; i < 4986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4987(int& val) {
    func_in_4987(val);
    const int x = 4987;
    std::cout << 4987;
    for (int i = 0; i < 4987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4987(int& val) {
    const int x = 4987;
    std::cout << 4987;
    for (int i = 0; i < 4987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4988(int& val) {
    func_in_4988(val);
    const int x = 4988;
    std::cout << 4988;
    for (int i = 0; i < 4988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4988(int& val) {
    const int x = 4988;
    std::cout << 4988;
    for (int i = 0; i < 4988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4989(int& val) {
    func_in_4989(val);
    const int x = 4989;
    std::cout << 4989;
    for (int i = 0; i < 4989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4989(int& val) {
    const int x = 4989;
    std::cout << 4989;
    for (int i = 0; i < 4989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4990(int& val) {
    func_in_4990(val);
    const int x = 4990;
    std::cout << 4990;
    for (int i = 0; i < 4990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4990(int& val) {
    const int x = 4990;
    std::cout << 4990;
    for (int i = 0; i < 4990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4991(int& val) {
    func_in_4991(val);
    const int x = 4991;
    std::cout << 4991;
    for (int i = 0; i < 4991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4991(int& val) {
    const int x = 4991;
    std::cout << 4991;
    for (int i = 0; i < 4991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4992(int& val) {
    func_in_4992(val);
    const int x = 4992;
    std::cout << 4992;
    for (int i = 0; i < 4992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4992(int& val) {
    const int x = 4992;
    std::cout << 4992;
    for (int i = 0; i < 4992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4993(int& val) {
    func_in_4993(val);
    const int x = 4993;
    std::cout << 4993;
    for (int i = 0; i < 4993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4993(int& val) {
    const int x = 4993;
    std::cout << 4993;
    for (int i = 0; i < 4993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4994(int& val) {
    func_in_4994(val);
    const int x = 4994;
    std::cout << 4994;
    for (int i = 0; i < 4994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4994(int& val) {
    const int x = 4994;
    std::cout << 4994;
    for (int i = 0; i < 4994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4995(int& val) {
    func_in_4995(val);
    const int x = 4995;
    std::cout << 4995;
    for (int i = 0; i < 4995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4995(int& val) {
    const int x = 4995;
    std::cout << 4995;
    for (int i = 0; i < 4995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4996(int& val) {
    func_in_4996(val);
    const int x = 4996;
    std::cout << 4996;
    for (int i = 0; i < 4996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4996(int& val) {
    const int x = 4996;
    std::cout << 4996;
    for (int i = 0; i < 4996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4997(int& val) {
    func_in_4997(val);
    const int x = 4997;
    std::cout << 4997;
    for (int i = 0; i < 4997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4997(int& val) {
    const int x = 4997;
    std::cout << 4997;
    for (int i = 0; i < 4997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4998(int& val) {
    func_in_4998(val);
    const int x = 4998;
    std::cout << 4998;
    for (int i = 0; i < 4998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4998(int& val) {
    const int x = 4998;
    std::cout << 4998;
    for (int i = 0; i < 4998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4999(int& val) {
    func_in_4999(val);
    const int x = 4999;
    std::cout << 4999;
    for (int i = 0; i < 4999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4999(int& val) {
    const int x = 4999;
    std::cout << 4999;
    for (int i = 0; i < 4999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5000(int& val) {
    func_in_5000(val);
    const int x = 5000;
    std::cout << 5000;
    for (int i = 0; i < 5000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5000(int& val) {
    const int x = 5000;
    std::cout << 5000;
    for (int i = 0; i < 5000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5001(int& val) {
    func_in_5001(val);
    const int x = 5001;
    std::cout << 5001;
    for (int i = 0; i < 5001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5001(int& val) {
    const int x = 5001;
    std::cout << 5001;
    for (int i = 0; i < 5001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5002(int& val) {
    func_in_5002(val);
    const int x = 5002;
    std::cout << 5002;
    for (int i = 0; i < 5002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5002(int& val) {
    const int x = 5002;
    std::cout << 5002;
    for (int i = 0; i < 5002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5003(int& val) {
    func_in_5003(val);
    const int x = 5003;
    std::cout << 5003;
    for (int i = 0; i < 5003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5003(int& val) {
    const int x = 5003;
    std::cout << 5003;
    for (int i = 0; i < 5003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5004(int& val) {
    func_in_5004(val);
    const int x = 5004;
    std::cout << 5004;
    for (int i = 0; i < 5004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5004(int& val) {
    const int x = 5004;
    std::cout << 5004;
    for (int i = 0; i < 5004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5005(int& val) {
    func_in_5005(val);
    const int x = 5005;
    std::cout << 5005;
    for (int i = 0; i < 5005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5005(int& val) {
    const int x = 5005;
    std::cout << 5005;
    for (int i = 0; i < 5005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5006(int& val) {
    func_in_5006(val);
    const int x = 5006;
    std::cout << 5006;
    for (int i = 0; i < 5006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5006(int& val) {
    const int x = 5006;
    std::cout << 5006;
    for (int i = 0; i < 5006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5007(int& val) {
    func_in_5007(val);
    const int x = 5007;
    std::cout << 5007;
    for (int i = 0; i < 5007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5007(int& val) {
    const int x = 5007;
    std::cout << 5007;
    for (int i = 0; i < 5007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5008(int& val) {
    func_in_5008(val);
    const int x = 5008;
    std::cout << 5008;
    for (int i = 0; i < 5008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5008(int& val) {
    const int x = 5008;
    std::cout << 5008;
    for (int i = 0; i < 5008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5009(int& val) {
    func_in_5009(val);
    const int x = 5009;
    std::cout << 5009;
    for (int i = 0; i < 5009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5009(int& val) {
    const int x = 5009;
    std::cout << 5009;
    for (int i = 0; i < 5009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5010(int& val) {
    func_in_5010(val);
    const int x = 5010;
    std::cout << 5010;
    for (int i = 0; i < 5010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5010(int& val) {
    const int x = 5010;
    std::cout << 5010;
    for (int i = 0; i < 5010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5011(int& val) {
    func_in_5011(val);
    const int x = 5011;
    std::cout << 5011;
    for (int i = 0; i < 5011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5011(int& val) {
    const int x = 5011;
    std::cout << 5011;
    for (int i = 0; i < 5011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5012(int& val) {
    func_in_5012(val);
    const int x = 5012;
    std::cout << 5012;
    for (int i = 0; i < 5012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5012(int& val) {
    const int x = 5012;
    std::cout << 5012;
    for (int i = 0; i < 5012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5013(int& val) {
    func_in_5013(val);
    const int x = 5013;
    std::cout << 5013;
    for (int i = 0; i < 5013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5013(int& val) {
    const int x = 5013;
    std::cout << 5013;
    for (int i = 0; i < 5013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5014(int& val) {
    func_in_5014(val);
    const int x = 5014;
    std::cout << 5014;
    for (int i = 0; i < 5014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5014(int& val) {
    const int x = 5014;
    std::cout << 5014;
    for (int i = 0; i < 5014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5015(int& val) {
    func_in_5015(val);
    const int x = 5015;
    std::cout << 5015;
    for (int i = 0; i < 5015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5015(int& val) {
    const int x = 5015;
    std::cout << 5015;
    for (int i = 0; i < 5015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5016(int& val) {
    func_in_5016(val);
    const int x = 5016;
    std::cout << 5016;
    for (int i = 0; i < 5016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5016(int& val) {
    const int x = 5016;
    std::cout << 5016;
    for (int i = 0; i < 5016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5017(int& val) {
    func_in_5017(val);
    const int x = 5017;
    std::cout << 5017;
    for (int i = 0; i < 5017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5017(int& val) {
    const int x = 5017;
    std::cout << 5017;
    for (int i = 0; i < 5017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5018(int& val) {
    func_in_5018(val);
    const int x = 5018;
    std::cout << 5018;
    for (int i = 0; i < 5018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5018(int& val) {
    const int x = 5018;
    std::cout << 5018;
    for (int i = 0; i < 5018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5019(int& val) {
    func_in_5019(val);
    const int x = 5019;
    std::cout << 5019;
    for (int i = 0; i < 5019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5019(int& val) {
    const int x = 5019;
    std::cout << 5019;
    for (int i = 0; i < 5019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5020(int& val) {
    func_in_5020(val);
    const int x = 5020;
    std::cout << 5020;
    for (int i = 0; i < 5020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5020(int& val) {
    const int x = 5020;
    std::cout << 5020;
    for (int i = 0; i < 5020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5021(int& val) {
    func_in_5021(val);
    const int x = 5021;
    std::cout << 5021;
    for (int i = 0; i < 5021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5021(int& val) {
    const int x = 5021;
    std::cout << 5021;
    for (int i = 0; i < 5021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5022(int& val) {
    func_in_5022(val);
    const int x = 5022;
    std::cout << 5022;
    for (int i = 0; i < 5022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5022(int& val) {
    const int x = 5022;
    std::cout << 5022;
    for (int i = 0; i < 5022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5023(int& val) {
    func_in_5023(val);
    const int x = 5023;
    std::cout << 5023;
    for (int i = 0; i < 5023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5023(int& val) {
    const int x = 5023;
    std::cout << 5023;
    for (int i = 0; i < 5023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5024(int& val) {
    func_in_5024(val);
    const int x = 5024;
    std::cout << 5024;
    for (int i = 0; i < 5024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5024(int& val) {
    const int x = 5024;
    std::cout << 5024;
    for (int i = 0; i < 5024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5025(int& val) {
    func_in_5025(val);
    const int x = 5025;
    std::cout << 5025;
    for (int i = 0; i < 5025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5025(int& val) {
    const int x = 5025;
    std::cout << 5025;
    for (int i = 0; i < 5025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5026(int& val) {
    func_in_5026(val);
    const int x = 5026;
    std::cout << 5026;
    for (int i = 0; i < 5026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5026(int& val) {
    const int x = 5026;
    std::cout << 5026;
    for (int i = 0; i < 5026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5027(int& val) {
    func_in_5027(val);
    const int x = 5027;
    std::cout << 5027;
    for (int i = 0; i < 5027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5027(int& val) {
    const int x = 5027;
    std::cout << 5027;
    for (int i = 0; i < 5027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5028(int& val) {
    func_in_5028(val);
    const int x = 5028;
    std::cout << 5028;
    for (int i = 0; i < 5028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5028(int& val) {
    const int x = 5028;
    std::cout << 5028;
    for (int i = 0; i < 5028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5029(int& val) {
    func_in_5029(val);
    const int x = 5029;
    std::cout << 5029;
    for (int i = 0; i < 5029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5029(int& val) {
    const int x = 5029;
    std::cout << 5029;
    for (int i = 0; i < 5029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5030(int& val) {
    func_in_5030(val);
    const int x = 5030;
    std::cout << 5030;
    for (int i = 0; i < 5030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5030(int& val) {
    const int x = 5030;
    std::cout << 5030;
    for (int i = 0; i < 5030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5031(int& val) {
    func_in_5031(val);
    const int x = 5031;
    std::cout << 5031;
    for (int i = 0; i < 5031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5031(int& val) {
    const int x = 5031;
    std::cout << 5031;
    for (int i = 0; i < 5031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5032(int& val) {
    func_in_5032(val);
    const int x = 5032;
    std::cout << 5032;
    for (int i = 0; i < 5032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5032(int& val) {
    const int x = 5032;
    std::cout << 5032;
    for (int i = 0; i < 5032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5033(int& val) {
    func_in_5033(val);
    const int x = 5033;
    std::cout << 5033;
    for (int i = 0; i < 5033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5033(int& val) {
    const int x = 5033;
    std::cout << 5033;
    for (int i = 0; i < 5033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5034(int& val) {
    func_in_5034(val);
    const int x = 5034;
    std::cout << 5034;
    for (int i = 0; i < 5034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5034(int& val) {
    const int x = 5034;
    std::cout << 5034;
    for (int i = 0; i < 5034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5035(int& val) {
    func_in_5035(val);
    const int x = 5035;
    std::cout << 5035;
    for (int i = 0; i < 5035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5035(int& val) {
    const int x = 5035;
    std::cout << 5035;
    for (int i = 0; i < 5035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5036(int& val) {
    func_in_5036(val);
    const int x = 5036;
    std::cout << 5036;
    for (int i = 0; i < 5036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5036(int& val) {
    const int x = 5036;
    std::cout << 5036;
    for (int i = 0; i < 5036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5037(int& val) {
    func_in_5037(val);
    const int x = 5037;
    std::cout << 5037;
    for (int i = 0; i < 5037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5037(int& val) {
    const int x = 5037;
    std::cout << 5037;
    for (int i = 0; i < 5037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5038(int& val) {
    func_in_5038(val);
    const int x = 5038;
    std::cout << 5038;
    for (int i = 0; i < 5038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5038(int& val) {
    const int x = 5038;
    std::cout << 5038;
    for (int i = 0; i < 5038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5039(int& val) {
    func_in_5039(val);
    const int x = 5039;
    std::cout << 5039;
    for (int i = 0; i < 5039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5039(int& val) {
    const int x = 5039;
    std::cout << 5039;
    for (int i = 0; i < 5039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5040(int& val) {
    func_in_5040(val);
    const int x = 5040;
    std::cout << 5040;
    for (int i = 0; i < 5040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5040(int& val) {
    const int x = 5040;
    std::cout << 5040;
    for (int i = 0; i < 5040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5041(int& val) {
    func_in_5041(val);
    const int x = 5041;
    std::cout << 5041;
    for (int i = 0; i < 5041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5041(int& val) {
    const int x = 5041;
    std::cout << 5041;
    for (int i = 0; i < 5041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5042(int& val) {
    func_in_5042(val);
    const int x = 5042;
    std::cout << 5042;
    for (int i = 0; i < 5042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5042(int& val) {
    const int x = 5042;
    std::cout << 5042;
    for (int i = 0; i < 5042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5043(int& val) {
    func_in_5043(val);
    const int x = 5043;
    std::cout << 5043;
    for (int i = 0; i < 5043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5043(int& val) {
    const int x = 5043;
    std::cout << 5043;
    for (int i = 0; i < 5043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5044(int& val) {
    func_in_5044(val);
    const int x = 5044;
    std::cout << 5044;
    for (int i = 0; i < 5044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5044(int& val) {
    const int x = 5044;
    std::cout << 5044;
    for (int i = 0; i < 5044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5045(int& val) {
    func_in_5045(val);
    const int x = 5045;
    std::cout << 5045;
    for (int i = 0; i < 5045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5045(int& val) {
    const int x = 5045;
    std::cout << 5045;
    for (int i = 0; i < 5045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5046(int& val) {
    func_in_5046(val);
    const int x = 5046;
    std::cout << 5046;
    for (int i = 0; i < 5046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5046(int& val) {
    const int x = 5046;
    std::cout << 5046;
    for (int i = 0; i < 5046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5047(int& val) {
    func_in_5047(val);
    const int x = 5047;
    std::cout << 5047;
    for (int i = 0; i < 5047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5047(int& val) {
    const int x = 5047;
    std::cout << 5047;
    for (int i = 0; i < 5047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5048(int& val) {
    func_in_5048(val);
    const int x = 5048;
    std::cout << 5048;
    for (int i = 0; i < 5048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5048(int& val) {
    const int x = 5048;
    std::cout << 5048;
    for (int i = 0; i < 5048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5049(int& val) {
    func_in_5049(val);
    const int x = 5049;
    std::cout << 5049;
    for (int i = 0; i < 5049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5049(int& val) {
    const int x = 5049;
    std::cout << 5049;
    for (int i = 0; i < 5049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5050(int& val) {
    func_in_5050(val);
    const int x = 5050;
    std::cout << 5050;
    for (int i = 0; i < 5050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5050(int& val) {
    const int x = 5050;
    std::cout << 5050;
    for (int i = 0; i < 5050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5051(int& val) {
    func_in_5051(val);
    const int x = 5051;
    std::cout << 5051;
    for (int i = 0; i < 5051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5051(int& val) {
    const int x = 5051;
    std::cout << 5051;
    for (int i = 0; i < 5051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5052(int& val) {
    func_in_5052(val);
    const int x = 5052;
    std::cout << 5052;
    for (int i = 0; i < 5052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5052(int& val) {
    const int x = 5052;
    std::cout << 5052;
    for (int i = 0; i < 5052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5053(int& val) {
    func_in_5053(val);
    const int x = 5053;
    std::cout << 5053;
    for (int i = 0; i < 5053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5053(int& val) {
    const int x = 5053;
    std::cout << 5053;
    for (int i = 0; i < 5053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5054(int& val) {
    func_in_5054(val);
    const int x = 5054;
    std::cout << 5054;
    for (int i = 0; i < 5054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5054(int& val) {
    const int x = 5054;
    std::cout << 5054;
    for (int i = 0; i < 5054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5055(int& val) {
    func_in_5055(val);
    const int x = 5055;
    std::cout << 5055;
    for (int i = 0; i < 5055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5055(int& val) {
    const int x = 5055;
    std::cout << 5055;
    for (int i = 0; i < 5055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5056(int& val) {
    func_in_5056(val);
    const int x = 5056;
    std::cout << 5056;
    for (int i = 0; i < 5056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5056(int& val) {
    const int x = 5056;
    std::cout << 5056;
    for (int i = 0; i < 5056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5057(int& val) {
    func_in_5057(val);
    const int x = 5057;
    std::cout << 5057;
    for (int i = 0; i < 5057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5057(int& val) {
    const int x = 5057;
    std::cout << 5057;
    for (int i = 0; i < 5057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5058(int& val) {
    func_in_5058(val);
    const int x = 5058;
    std::cout << 5058;
    for (int i = 0; i < 5058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5058(int& val) {
    const int x = 5058;
    std::cout << 5058;
    for (int i = 0; i < 5058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5059(int& val) {
    func_in_5059(val);
    const int x = 5059;
    std::cout << 5059;
    for (int i = 0; i < 5059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5059(int& val) {
    const int x = 5059;
    std::cout << 5059;
    for (int i = 0; i < 5059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5060(int& val) {
    func_in_5060(val);
    const int x = 5060;
    std::cout << 5060;
    for (int i = 0; i < 5060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5060(int& val) {
    const int x = 5060;
    std::cout << 5060;
    for (int i = 0; i < 5060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5061(int& val) {
    func_in_5061(val);
    const int x = 5061;
    std::cout << 5061;
    for (int i = 0; i < 5061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5061(int& val) {
    const int x = 5061;
    std::cout << 5061;
    for (int i = 0; i < 5061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5062(int& val) {
    func_in_5062(val);
    const int x = 5062;
    std::cout << 5062;
    for (int i = 0; i < 5062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5062(int& val) {
    const int x = 5062;
    std::cout << 5062;
    for (int i = 0; i < 5062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5063(int& val) {
    func_in_5063(val);
    const int x = 5063;
    std::cout << 5063;
    for (int i = 0; i < 5063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5063(int& val) {
    const int x = 5063;
    std::cout << 5063;
    for (int i = 0; i < 5063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5064(int& val) {
    func_in_5064(val);
    const int x = 5064;
    std::cout << 5064;
    for (int i = 0; i < 5064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5064(int& val) {
    const int x = 5064;
    std::cout << 5064;
    for (int i = 0; i < 5064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5065(int& val) {
    func_in_5065(val);
    const int x = 5065;
    std::cout << 5065;
    for (int i = 0; i < 5065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5065(int& val) {
    const int x = 5065;
    std::cout << 5065;
    for (int i = 0; i < 5065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5066(int& val) {
    func_in_5066(val);
    const int x = 5066;
    std::cout << 5066;
    for (int i = 0; i < 5066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5066(int& val) {
    const int x = 5066;
    std::cout << 5066;
    for (int i = 0; i < 5066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5067(int& val) {
    func_in_5067(val);
    const int x = 5067;
    std::cout << 5067;
    for (int i = 0; i < 5067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5067(int& val) {
    const int x = 5067;
    std::cout << 5067;
    for (int i = 0; i < 5067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5068(int& val) {
    func_in_5068(val);
    const int x = 5068;
    std::cout << 5068;
    for (int i = 0; i < 5068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5068(int& val) {
    const int x = 5068;
    std::cout << 5068;
    for (int i = 0; i < 5068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5069(int& val) {
    func_in_5069(val);
    const int x = 5069;
    std::cout << 5069;
    for (int i = 0; i < 5069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5069(int& val) {
    const int x = 5069;
    std::cout << 5069;
    for (int i = 0; i < 5069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5070(int& val) {
    func_in_5070(val);
    const int x = 5070;
    std::cout << 5070;
    for (int i = 0; i < 5070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5070(int& val) {
    const int x = 5070;
    std::cout << 5070;
    for (int i = 0; i < 5070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5071(int& val) {
    func_in_5071(val);
    const int x = 5071;
    std::cout << 5071;
    for (int i = 0; i < 5071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5071(int& val) {
    const int x = 5071;
    std::cout << 5071;
    for (int i = 0; i < 5071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5072(int& val) {
    func_in_5072(val);
    const int x = 5072;
    std::cout << 5072;
    for (int i = 0; i < 5072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5072(int& val) {
    const int x = 5072;
    std::cout << 5072;
    for (int i = 0; i < 5072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5073(int& val) {
    func_in_5073(val);
    const int x = 5073;
    std::cout << 5073;
    for (int i = 0; i < 5073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5073(int& val) {
    const int x = 5073;
    std::cout << 5073;
    for (int i = 0; i < 5073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5074(int& val) {
    func_in_5074(val);
    const int x = 5074;
    std::cout << 5074;
    for (int i = 0; i < 5074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5074(int& val) {
    const int x = 5074;
    std::cout << 5074;
    for (int i = 0; i < 5074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5075(int& val) {
    func_in_5075(val);
    const int x = 5075;
    std::cout << 5075;
    for (int i = 0; i < 5075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5075(int& val) {
    const int x = 5075;
    std::cout << 5075;
    for (int i = 0; i < 5075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5076(int& val) {
    func_in_5076(val);
    const int x = 5076;
    std::cout << 5076;
    for (int i = 0; i < 5076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5076(int& val) {
    const int x = 5076;
    std::cout << 5076;
    for (int i = 0; i < 5076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5077(int& val) {
    func_in_5077(val);
    const int x = 5077;
    std::cout << 5077;
    for (int i = 0; i < 5077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5077(int& val) {
    const int x = 5077;
    std::cout << 5077;
    for (int i = 0; i < 5077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5078(int& val) {
    func_in_5078(val);
    const int x = 5078;
    std::cout << 5078;
    for (int i = 0; i < 5078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5078(int& val) {
    const int x = 5078;
    std::cout << 5078;
    for (int i = 0; i < 5078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5079(int& val) {
    func_in_5079(val);
    const int x = 5079;
    std::cout << 5079;
    for (int i = 0; i < 5079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5079(int& val) {
    const int x = 5079;
    std::cout << 5079;
    for (int i = 0; i < 5079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5080(int& val) {
    func_in_5080(val);
    const int x = 5080;
    std::cout << 5080;
    for (int i = 0; i < 5080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5080(int& val) {
    const int x = 5080;
    std::cout << 5080;
    for (int i = 0; i < 5080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5081(int& val) {
    func_in_5081(val);
    const int x = 5081;
    std::cout << 5081;
    for (int i = 0; i < 5081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5081(int& val) {
    const int x = 5081;
    std::cout << 5081;
    for (int i = 0; i < 5081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5082(int& val) {
    func_in_5082(val);
    const int x = 5082;
    std::cout << 5082;
    for (int i = 0; i < 5082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5082(int& val) {
    const int x = 5082;
    std::cout << 5082;
    for (int i = 0; i < 5082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5083(int& val) {
    func_in_5083(val);
    const int x = 5083;
    std::cout << 5083;
    for (int i = 0; i < 5083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5083(int& val) {
    const int x = 5083;
    std::cout << 5083;
    for (int i = 0; i < 5083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5084(int& val) {
    func_in_5084(val);
    const int x = 5084;
    std::cout << 5084;
    for (int i = 0; i < 5084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5084(int& val) {
    const int x = 5084;
    std::cout << 5084;
    for (int i = 0; i < 5084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5085(int& val) {
    func_in_5085(val);
    const int x = 5085;
    std::cout << 5085;
    for (int i = 0; i < 5085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5085(int& val) {
    const int x = 5085;
    std::cout << 5085;
    for (int i = 0; i < 5085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5086(int& val) {
    func_in_5086(val);
    const int x = 5086;
    std::cout << 5086;
    for (int i = 0; i < 5086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5086(int& val) {
    const int x = 5086;
    std::cout << 5086;
    for (int i = 0; i < 5086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5087(int& val) {
    func_in_5087(val);
    const int x = 5087;
    std::cout << 5087;
    for (int i = 0; i < 5087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5087(int& val) {
    const int x = 5087;
    std::cout << 5087;
    for (int i = 0; i < 5087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5088(int& val) {
    func_in_5088(val);
    const int x = 5088;
    std::cout << 5088;
    for (int i = 0; i < 5088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5088(int& val) {
    const int x = 5088;
    std::cout << 5088;
    for (int i = 0; i < 5088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5089(int& val) {
    func_in_5089(val);
    const int x = 5089;
    std::cout << 5089;
    for (int i = 0; i < 5089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5089(int& val) {
    const int x = 5089;
    std::cout << 5089;
    for (int i = 0; i < 5089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5090(int& val) {
    func_in_5090(val);
    const int x = 5090;
    std::cout << 5090;
    for (int i = 0; i < 5090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5090(int& val) {
    const int x = 5090;
    std::cout << 5090;
    for (int i = 0; i < 5090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5091(int& val) {
    func_in_5091(val);
    const int x = 5091;
    std::cout << 5091;
    for (int i = 0; i < 5091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5091(int& val) {
    const int x = 5091;
    std::cout << 5091;
    for (int i = 0; i < 5091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5092(int& val) {
    func_in_5092(val);
    const int x = 5092;
    std::cout << 5092;
    for (int i = 0; i < 5092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5092(int& val) {
    const int x = 5092;
    std::cout << 5092;
    for (int i = 0; i < 5092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5093(int& val) {
    func_in_5093(val);
    const int x = 5093;
    std::cout << 5093;
    for (int i = 0; i < 5093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5093(int& val) {
    const int x = 5093;
    std::cout << 5093;
    for (int i = 0; i < 5093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5094(int& val) {
    func_in_5094(val);
    const int x = 5094;
    std::cout << 5094;
    for (int i = 0; i < 5094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5094(int& val) {
    const int x = 5094;
    std::cout << 5094;
    for (int i = 0; i < 5094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5095(int& val) {
    func_in_5095(val);
    const int x = 5095;
    std::cout << 5095;
    for (int i = 0; i < 5095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5095(int& val) {
    const int x = 5095;
    std::cout << 5095;
    for (int i = 0; i < 5095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5096(int& val) {
    func_in_5096(val);
    const int x = 5096;
    std::cout << 5096;
    for (int i = 0; i < 5096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5096(int& val) {
    const int x = 5096;
    std::cout << 5096;
    for (int i = 0; i < 5096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5097(int& val) {
    func_in_5097(val);
    const int x = 5097;
    std::cout << 5097;
    for (int i = 0; i < 5097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5097(int& val) {
    const int x = 5097;
    std::cout << 5097;
    for (int i = 0; i < 5097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5098(int& val) {
    func_in_5098(val);
    const int x = 5098;
    std::cout << 5098;
    for (int i = 0; i < 5098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5098(int& val) {
    const int x = 5098;
    std::cout << 5098;
    for (int i = 0; i < 5098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5099(int& val) {
    func_in_5099(val);
    const int x = 5099;
    std::cout << 5099;
    for (int i = 0; i < 5099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5099(int& val) {
    const int x = 5099;
    std::cout << 5099;
    for (int i = 0; i < 5099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5100(int& val) {
    func_in_5100(val);
    const int x = 5100;
    std::cout << 5100;
    for (int i = 0; i < 5100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5100(int& val) {
    const int x = 5100;
    std::cout << 5100;
    for (int i = 0; i < 5100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5101(int& val) {
    func_in_5101(val);
    const int x = 5101;
    std::cout << 5101;
    for (int i = 0; i < 5101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5101(int& val) {
    const int x = 5101;
    std::cout << 5101;
    for (int i = 0; i < 5101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5102(int& val) {
    func_in_5102(val);
    const int x = 5102;
    std::cout << 5102;
    for (int i = 0; i < 5102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5102(int& val) {
    const int x = 5102;
    std::cout << 5102;
    for (int i = 0; i < 5102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5103(int& val) {
    func_in_5103(val);
    const int x = 5103;
    std::cout << 5103;
    for (int i = 0; i < 5103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5103(int& val) {
    const int x = 5103;
    std::cout << 5103;
    for (int i = 0; i < 5103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5104(int& val) {
    func_in_5104(val);
    const int x = 5104;
    std::cout << 5104;
    for (int i = 0; i < 5104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5104(int& val) {
    const int x = 5104;
    std::cout << 5104;
    for (int i = 0; i < 5104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5105(int& val) {
    func_in_5105(val);
    const int x = 5105;
    std::cout << 5105;
    for (int i = 0; i < 5105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5105(int& val) {
    const int x = 5105;
    std::cout << 5105;
    for (int i = 0; i < 5105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5106(int& val) {
    func_in_5106(val);
    const int x = 5106;
    std::cout << 5106;
    for (int i = 0; i < 5106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5106(int& val) {
    const int x = 5106;
    std::cout << 5106;
    for (int i = 0; i < 5106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5107(int& val) {
    func_in_5107(val);
    const int x = 5107;
    std::cout << 5107;
    for (int i = 0; i < 5107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5107(int& val) {
    const int x = 5107;
    std::cout << 5107;
    for (int i = 0; i < 5107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5108(int& val) {
    func_in_5108(val);
    const int x = 5108;
    std::cout << 5108;
    for (int i = 0; i < 5108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5108(int& val) {
    const int x = 5108;
    std::cout << 5108;
    for (int i = 0; i < 5108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5109(int& val) {
    func_in_5109(val);
    const int x = 5109;
    std::cout << 5109;
    for (int i = 0; i < 5109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5109(int& val) {
    const int x = 5109;
    std::cout << 5109;
    for (int i = 0; i < 5109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5110(int& val) {
    func_in_5110(val);
    const int x = 5110;
    std::cout << 5110;
    for (int i = 0; i < 5110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5110(int& val) {
    const int x = 5110;
    std::cout << 5110;
    for (int i = 0; i < 5110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5111(int& val) {
    func_in_5111(val);
    const int x = 5111;
    std::cout << 5111;
    for (int i = 0; i < 5111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5111(int& val) {
    const int x = 5111;
    std::cout << 5111;
    for (int i = 0; i < 5111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5112(int& val) {
    func_in_5112(val);
    const int x = 5112;
    std::cout << 5112;
    for (int i = 0; i < 5112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5112(int& val) {
    const int x = 5112;
    std::cout << 5112;
    for (int i = 0; i < 5112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5113(int& val) {
    func_in_5113(val);
    const int x = 5113;
    std::cout << 5113;
    for (int i = 0; i < 5113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5113(int& val) {
    const int x = 5113;
    std::cout << 5113;
    for (int i = 0; i < 5113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5114(int& val) {
    func_in_5114(val);
    const int x = 5114;
    std::cout << 5114;
    for (int i = 0; i < 5114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5114(int& val) {
    const int x = 5114;
    std::cout << 5114;
    for (int i = 0; i < 5114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5115(int& val) {
    func_in_5115(val);
    const int x = 5115;
    std::cout << 5115;
    for (int i = 0; i < 5115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5115(int& val) {
    const int x = 5115;
    std::cout << 5115;
    for (int i = 0; i < 5115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5116(int& val) {
    func_in_5116(val);
    const int x = 5116;
    std::cout << 5116;
    for (int i = 0; i < 5116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5116(int& val) {
    const int x = 5116;
    std::cout << 5116;
    for (int i = 0; i < 5116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5117(int& val) {
    func_in_5117(val);
    const int x = 5117;
    std::cout << 5117;
    for (int i = 0; i < 5117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5117(int& val) {
    const int x = 5117;
    std::cout << 5117;
    for (int i = 0; i < 5117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5118(int& val) {
    func_in_5118(val);
    const int x = 5118;
    std::cout << 5118;
    for (int i = 0; i < 5118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5118(int& val) {
    const int x = 5118;
    std::cout << 5118;
    for (int i = 0; i < 5118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5119(int& val) {
    func_in_5119(val);
    const int x = 5119;
    std::cout << 5119;
    for (int i = 0; i < 5119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5119(int& val) {
    const int x = 5119;
    std::cout << 5119;
    for (int i = 0; i < 5119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5120(int& val) {
    func_in_5120(val);
    const int x = 5120;
    std::cout << 5120;
    for (int i = 0; i < 5120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5120(int& val) {
    const int x = 5120;
    std::cout << 5120;
    for (int i = 0; i < 5120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5121(int& val) {
    func_in_5121(val);
    const int x = 5121;
    std::cout << 5121;
    for (int i = 0; i < 5121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5121(int& val) {
    const int x = 5121;
    std::cout << 5121;
    for (int i = 0; i < 5121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5122(int& val) {
    func_in_5122(val);
    const int x = 5122;
    std::cout << 5122;
    for (int i = 0; i < 5122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5122(int& val) {
    const int x = 5122;
    std::cout << 5122;
    for (int i = 0; i < 5122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5123(int& val) {
    func_in_5123(val);
    const int x = 5123;
    std::cout << 5123;
    for (int i = 0; i < 5123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5123(int& val) {
    const int x = 5123;
    std::cout << 5123;
    for (int i = 0; i < 5123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5124(int& val) {
    func_in_5124(val);
    const int x = 5124;
    std::cout << 5124;
    for (int i = 0; i < 5124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5124(int& val) {
    const int x = 5124;
    std::cout << 5124;
    for (int i = 0; i < 5124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5125(int& val) {
    func_in_5125(val);
    const int x = 5125;
    std::cout << 5125;
    for (int i = 0; i < 5125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5125(int& val) {
    const int x = 5125;
    std::cout << 5125;
    for (int i = 0; i < 5125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5126(int& val) {
    func_in_5126(val);
    const int x = 5126;
    std::cout << 5126;
    for (int i = 0; i < 5126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5126(int& val) {
    const int x = 5126;
    std::cout << 5126;
    for (int i = 0; i < 5126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5127(int& val) {
    func_in_5127(val);
    const int x = 5127;
    std::cout << 5127;
    for (int i = 0; i < 5127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5127(int& val) {
    const int x = 5127;
    std::cout << 5127;
    for (int i = 0; i < 5127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5128(int& val) {
    func_in_5128(val);
    const int x = 5128;
    std::cout << 5128;
    for (int i = 0; i < 5128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5128(int& val) {
    const int x = 5128;
    std::cout << 5128;
    for (int i = 0; i < 5128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5129(int& val) {
    func_in_5129(val);
    const int x = 5129;
    std::cout << 5129;
    for (int i = 0; i < 5129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5129(int& val) {
    const int x = 5129;
    std::cout << 5129;
    for (int i = 0; i < 5129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5130(int& val) {
    func_in_5130(val);
    const int x = 5130;
    std::cout << 5130;
    for (int i = 0; i < 5130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5130(int& val) {
    const int x = 5130;
    std::cout << 5130;
    for (int i = 0; i < 5130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5131(int& val) {
    func_in_5131(val);
    const int x = 5131;
    std::cout << 5131;
    for (int i = 0; i < 5131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5131(int& val) {
    const int x = 5131;
    std::cout << 5131;
    for (int i = 0; i < 5131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5132(int& val) {
    func_in_5132(val);
    const int x = 5132;
    std::cout << 5132;
    for (int i = 0; i < 5132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5132(int& val) {
    const int x = 5132;
    std::cout << 5132;
    for (int i = 0; i < 5132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5133(int& val) {
    func_in_5133(val);
    const int x = 5133;
    std::cout << 5133;
    for (int i = 0; i < 5133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5133(int& val) {
    const int x = 5133;
    std::cout << 5133;
    for (int i = 0; i < 5133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5134(int& val) {
    func_in_5134(val);
    const int x = 5134;
    std::cout << 5134;
    for (int i = 0; i < 5134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5134(int& val) {
    const int x = 5134;
    std::cout << 5134;
    for (int i = 0; i < 5134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5135(int& val) {
    func_in_5135(val);
    const int x = 5135;
    std::cout << 5135;
    for (int i = 0; i < 5135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5135(int& val) {
    const int x = 5135;
    std::cout << 5135;
    for (int i = 0; i < 5135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5136(int& val) {
    func_in_5136(val);
    const int x = 5136;
    std::cout << 5136;
    for (int i = 0; i < 5136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5136(int& val) {
    const int x = 5136;
    std::cout << 5136;
    for (int i = 0; i < 5136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5137(int& val) {
    func_in_5137(val);
    const int x = 5137;
    std::cout << 5137;
    for (int i = 0; i < 5137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5137(int& val) {
    const int x = 5137;
    std::cout << 5137;
    for (int i = 0; i < 5137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5138(int& val) {
    func_in_5138(val);
    const int x = 5138;
    std::cout << 5138;
    for (int i = 0; i < 5138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5138(int& val) {
    const int x = 5138;
    std::cout << 5138;
    for (int i = 0; i < 5138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5139(int& val) {
    func_in_5139(val);
    const int x = 5139;
    std::cout << 5139;
    for (int i = 0; i < 5139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5139(int& val) {
    const int x = 5139;
    std::cout << 5139;
    for (int i = 0; i < 5139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5140(int& val) {
    func_in_5140(val);
    const int x = 5140;
    std::cout << 5140;
    for (int i = 0; i < 5140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5140(int& val) {
    const int x = 5140;
    std::cout << 5140;
    for (int i = 0; i < 5140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5141(int& val) {
    func_in_5141(val);
    const int x = 5141;
    std::cout << 5141;
    for (int i = 0; i < 5141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5141(int& val) {
    const int x = 5141;
    std::cout << 5141;
    for (int i = 0; i < 5141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5142(int& val) {
    func_in_5142(val);
    const int x = 5142;
    std::cout << 5142;
    for (int i = 0; i < 5142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5142(int& val) {
    const int x = 5142;
    std::cout << 5142;
    for (int i = 0; i < 5142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5143(int& val) {
    func_in_5143(val);
    const int x = 5143;
    std::cout << 5143;
    for (int i = 0; i < 5143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5143(int& val) {
    const int x = 5143;
    std::cout << 5143;
    for (int i = 0; i < 5143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5144(int& val) {
    func_in_5144(val);
    const int x = 5144;
    std::cout << 5144;
    for (int i = 0; i < 5144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5144(int& val) {
    const int x = 5144;
    std::cout << 5144;
    for (int i = 0; i < 5144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5145(int& val) {
    func_in_5145(val);
    const int x = 5145;
    std::cout << 5145;
    for (int i = 0; i < 5145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5145(int& val) {
    const int x = 5145;
    std::cout << 5145;
    for (int i = 0; i < 5145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5146(int& val) {
    func_in_5146(val);
    const int x = 5146;
    std::cout << 5146;
    for (int i = 0; i < 5146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5146(int& val) {
    const int x = 5146;
    std::cout << 5146;
    for (int i = 0; i < 5146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5147(int& val) {
    func_in_5147(val);
    const int x = 5147;
    std::cout << 5147;
    for (int i = 0; i < 5147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5147(int& val) {
    const int x = 5147;
    std::cout << 5147;
    for (int i = 0; i < 5147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5148(int& val) {
    func_in_5148(val);
    const int x = 5148;
    std::cout << 5148;
    for (int i = 0; i < 5148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5148(int& val) {
    const int x = 5148;
    std::cout << 5148;
    for (int i = 0; i < 5148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5149(int& val) {
    func_in_5149(val);
    const int x = 5149;
    std::cout << 5149;
    for (int i = 0; i < 5149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5149(int& val) {
    const int x = 5149;
    std::cout << 5149;
    for (int i = 0; i < 5149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5150(int& val) {
    func_in_5150(val);
    const int x = 5150;
    std::cout << 5150;
    for (int i = 0; i < 5150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5150(int& val) {
    const int x = 5150;
    std::cout << 5150;
    for (int i = 0; i < 5150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5151(int& val) {
    func_in_5151(val);
    const int x = 5151;
    std::cout << 5151;
    for (int i = 0; i < 5151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5151(int& val) {
    const int x = 5151;
    std::cout << 5151;
    for (int i = 0; i < 5151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5152(int& val) {
    func_in_5152(val);
    const int x = 5152;
    std::cout << 5152;
    for (int i = 0; i < 5152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5152(int& val) {
    const int x = 5152;
    std::cout << 5152;
    for (int i = 0; i < 5152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5153(int& val) {
    func_in_5153(val);
    const int x = 5153;
    std::cout << 5153;
    for (int i = 0; i < 5153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5153(int& val) {
    const int x = 5153;
    std::cout << 5153;
    for (int i = 0; i < 5153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5154(int& val) {
    func_in_5154(val);
    const int x = 5154;
    std::cout << 5154;
    for (int i = 0; i < 5154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5154(int& val) {
    const int x = 5154;
    std::cout << 5154;
    for (int i = 0; i < 5154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5155(int& val) {
    func_in_5155(val);
    const int x = 5155;
    std::cout << 5155;
    for (int i = 0; i < 5155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5155(int& val) {
    const int x = 5155;
    std::cout << 5155;
    for (int i = 0; i < 5155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5156(int& val) {
    func_in_5156(val);
    const int x = 5156;
    std::cout << 5156;
    for (int i = 0; i < 5156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5156(int& val) {
    const int x = 5156;
    std::cout << 5156;
    for (int i = 0; i < 5156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5157(int& val) {
    func_in_5157(val);
    const int x = 5157;
    std::cout << 5157;
    for (int i = 0; i < 5157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5157(int& val) {
    const int x = 5157;
    std::cout << 5157;
    for (int i = 0; i < 5157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5158(int& val) {
    func_in_5158(val);
    const int x = 5158;
    std::cout << 5158;
    for (int i = 0; i < 5158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5158(int& val) {
    const int x = 5158;
    std::cout << 5158;
    for (int i = 0; i < 5158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5159(int& val) {
    func_in_5159(val);
    const int x = 5159;
    std::cout << 5159;
    for (int i = 0; i < 5159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5159(int& val) {
    const int x = 5159;
    std::cout << 5159;
    for (int i = 0; i < 5159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5160(int& val) {
    func_in_5160(val);
    const int x = 5160;
    std::cout << 5160;
    for (int i = 0; i < 5160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5160(int& val) {
    const int x = 5160;
    std::cout << 5160;
    for (int i = 0; i < 5160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5161(int& val) {
    func_in_5161(val);
    const int x = 5161;
    std::cout << 5161;
    for (int i = 0; i < 5161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5161(int& val) {
    const int x = 5161;
    std::cout << 5161;
    for (int i = 0; i < 5161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5162(int& val) {
    func_in_5162(val);
    const int x = 5162;
    std::cout << 5162;
    for (int i = 0; i < 5162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5162(int& val) {
    const int x = 5162;
    std::cout << 5162;
    for (int i = 0; i < 5162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5163(int& val) {
    func_in_5163(val);
    const int x = 5163;
    std::cout << 5163;
    for (int i = 0; i < 5163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5163(int& val) {
    const int x = 5163;
    std::cout << 5163;
    for (int i = 0; i < 5163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5164(int& val) {
    func_in_5164(val);
    const int x = 5164;
    std::cout << 5164;
    for (int i = 0; i < 5164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5164(int& val) {
    const int x = 5164;
    std::cout << 5164;
    for (int i = 0; i < 5164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5165(int& val) {
    func_in_5165(val);
    const int x = 5165;
    std::cout << 5165;
    for (int i = 0; i < 5165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5165(int& val) {
    const int x = 5165;
    std::cout << 5165;
    for (int i = 0; i < 5165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5166(int& val) {
    func_in_5166(val);
    const int x = 5166;
    std::cout << 5166;
    for (int i = 0; i < 5166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5166(int& val) {
    const int x = 5166;
    std::cout << 5166;
    for (int i = 0; i < 5166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5167(int& val) {
    func_in_5167(val);
    const int x = 5167;
    std::cout << 5167;
    for (int i = 0; i < 5167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5167(int& val) {
    const int x = 5167;
    std::cout << 5167;
    for (int i = 0; i < 5167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5168(int& val) {
    func_in_5168(val);
    const int x = 5168;
    std::cout << 5168;
    for (int i = 0; i < 5168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5168(int& val) {
    const int x = 5168;
    std::cout << 5168;
    for (int i = 0; i < 5168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5169(int& val) {
    func_in_5169(val);
    const int x = 5169;
    std::cout << 5169;
    for (int i = 0; i < 5169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5169(int& val) {
    const int x = 5169;
    std::cout << 5169;
    for (int i = 0; i < 5169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5170(int& val) {
    func_in_5170(val);
    const int x = 5170;
    std::cout << 5170;
    for (int i = 0; i < 5170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5170(int& val) {
    const int x = 5170;
    std::cout << 5170;
    for (int i = 0; i < 5170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5171(int& val) {
    func_in_5171(val);
    const int x = 5171;
    std::cout << 5171;
    for (int i = 0; i < 5171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5171(int& val) {
    const int x = 5171;
    std::cout << 5171;
    for (int i = 0; i < 5171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5172(int& val) {
    func_in_5172(val);
    const int x = 5172;
    std::cout << 5172;
    for (int i = 0; i < 5172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5172(int& val) {
    const int x = 5172;
    std::cout << 5172;
    for (int i = 0; i < 5172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5173(int& val) {
    func_in_5173(val);
    const int x = 5173;
    std::cout << 5173;
    for (int i = 0; i < 5173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5173(int& val) {
    const int x = 5173;
    std::cout << 5173;
    for (int i = 0; i < 5173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5174(int& val) {
    func_in_5174(val);
    const int x = 5174;
    std::cout << 5174;
    for (int i = 0; i < 5174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5174(int& val) {
    const int x = 5174;
    std::cout << 5174;
    for (int i = 0; i < 5174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5175(int& val) {
    func_in_5175(val);
    const int x = 5175;
    std::cout << 5175;
    for (int i = 0; i < 5175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5175(int& val) {
    const int x = 5175;
    std::cout << 5175;
    for (int i = 0; i < 5175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5176(int& val) {
    func_in_5176(val);
    const int x = 5176;
    std::cout << 5176;
    for (int i = 0; i < 5176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5176(int& val) {
    const int x = 5176;
    std::cout << 5176;
    for (int i = 0; i < 5176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5177(int& val) {
    func_in_5177(val);
    const int x = 5177;
    std::cout << 5177;
    for (int i = 0; i < 5177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5177(int& val) {
    const int x = 5177;
    std::cout << 5177;
    for (int i = 0; i < 5177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5178(int& val) {
    func_in_5178(val);
    const int x = 5178;
    std::cout << 5178;
    for (int i = 0; i < 5178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5178(int& val) {
    const int x = 5178;
    std::cout << 5178;
    for (int i = 0; i < 5178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5179(int& val) {
    func_in_5179(val);
    const int x = 5179;
    std::cout << 5179;
    for (int i = 0; i < 5179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5179(int& val) {
    const int x = 5179;
    std::cout << 5179;
    for (int i = 0; i < 5179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5180(int& val) {
    func_in_5180(val);
    const int x = 5180;
    std::cout << 5180;
    for (int i = 0; i < 5180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5180(int& val) {
    const int x = 5180;
    std::cout << 5180;
    for (int i = 0; i < 5180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5181(int& val) {
    func_in_5181(val);
    const int x = 5181;
    std::cout << 5181;
    for (int i = 0; i < 5181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5181(int& val) {
    const int x = 5181;
    std::cout << 5181;
    for (int i = 0; i < 5181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5182(int& val) {
    func_in_5182(val);
    const int x = 5182;
    std::cout << 5182;
    for (int i = 0; i < 5182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5182(int& val) {
    const int x = 5182;
    std::cout << 5182;
    for (int i = 0; i < 5182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5183(int& val) {
    func_in_5183(val);
    const int x = 5183;
    std::cout << 5183;
    for (int i = 0; i < 5183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5183(int& val) {
    const int x = 5183;
    std::cout << 5183;
    for (int i = 0; i < 5183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5184(int& val) {
    func_in_5184(val);
    const int x = 5184;
    std::cout << 5184;
    for (int i = 0; i < 5184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5184(int& val) {
    const int x = 5184;
    std::cout << 5184;
    for (int i = 0; i < 5184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5185(int& val) {
    func_in_5185(val);
    const int x = 5185;
    std::cout << 5185;
    for (int i = 0; i < 5185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5185(int& val) {
    const int x = 5185;
    std::cout << 5185;
    for (int i = 0; i < 5185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5186(int& val) {
    func_in_5186(val);
    const int x = 5186;
    std::cout << 5186;
    for (int i = 0; i < 5186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5186(int& val) {
    const int x = 5186;
    std::cout << 5186;
    for (int i = 0; i < 5186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5187(int& val) {
    func_in_5187(val);
    const int x = 5187;
    std::cout << 5187;
    for (int i = 0; i < 5187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5187(int& val) {
    const int x = 5187;
    std::cout << 5187;
    for (int i = 0; i < 5187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5188(int& val) {
    func_in_5188(val);
    const int x = 5188;
    std::cout << 5188;
    for (int i = 0; i < 5188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5188(int& val) {
    const int x = 5188;
    std::cout << 5188;
    for (int i = 0; i < 5188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5189(int& val) {
    func_in_5189(val);
    const int x = 5189;
    std::cout << 5189;
    for (int i = 0; i < 5189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5189(int& val) {
    const int x = 5189;
    std::cout << 5189;
    for (int i = 0; i < 5189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5190(int& val) {
    func_in_5190(val);
    const int x = 5190;
    std::cout << 5190;
    for (int i = 0; i < 5190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5190(int& val) {
    const int x = 5190;
    std::cout << 5190;
    for (int i = 0; i < 5190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5191(int& val) {
    func_in_5191(val);
    const int x = 5191;
    std::cout << 5191;
    for (int i = 0; i < 5191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5191(int& val) {
    const int x = 5191;
    std::cout << 5191;
    for (int i = 0; i < 5191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5192(int& val) {
    func_in_5192(val);
    const int x = 5192;
    std::cout << 5192;
    for (int i = 0; i < 5192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5192(int& val) {
    const int x = 5192;
    std::cout << 5192;
    for (int i = 0; i < 5192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5193(int& val) {
    func_in_5193(val);
    const int x = 5193;
    std::cout << 5193;
    for (int i = 0; i < 5193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5193(int& val) {
    const int x = 5193;
    std::cout << 5193;
    for (int i = 0; i < 5193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5194(int& val) {
    func_in_5194(val);
    const int x = 5194;
    std::cout << 5194;
    for (int i = 0; i < 5194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5194(int& val) {
    const int x = 5194;
    std::cout << 5194;
    for (int i = 0; i < 5194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5195(int& val) {
    func_in_5195(val);
    const int x = 5195;
    std::cout << 5195;
    for (int i = 0; i < 5195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5195(int& val) {
    const int x = 5195;
    std::cout << 5195;
    for (int i = 0; i < 5195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5196(int& val) {
    func_in_5196(val);
    const int x = 5196;
    std::cout << 5196;
    for (int i = 0; i < 5196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5196(int& val) {
    const int x = 5196;
    std::cout << 5196;
    for (int i = 0; i < 5196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5197(int& val) {
    func_in_5197(val);
    const int x = 5197;
    std::cout << 5197;
    for (int i = 0; i < 5197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5197(int& val) {
    const int x = 5197;
    std::cout << 5197;
    for (int i = 0; i < 5197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5198(int& val) {
    func_in_5198(val);
    const int x = 5198;
    std::cout << 5198;
    for (int i = 0; i < 5198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5198(int& val) {
    const int x = 5198;
    std::cout << 5198;
    for (int i = 0; i < 5198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5199(int& val) {
    func_in_5199(val);
    const int x = 5199;
    std::cout << 5199;
    for (int i = 0; i < 5199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5199(int& val) {
    const int x = 5199;
    std::cout << 5199;
    for (int i = 0; i < 5199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5200(int& val) {
    func_in_5200(val);
    const int x = 5200;
    std::cout << 5200;
    for (int i = 0; i < 5200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5200(int& val) {
    const int x = 5200;
    std::cout << 5200;
    for (int i = 0; i < 5200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5201(int& val) {
    func_in_5201(val);
    const int x = 5201;
    std::cout << 5201;
    for (int i = 0; i < 5201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5201(int& val) {
    const int x = 5201;
    std::cout << 5201;
    for (int i = 0; i < 5201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5202(int& val) {
    func_in_5202(val);
    const int x = 5202;
    std::cout << 5202;
    for (int i = 0; i < 5202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5202(int& val) {
    const int x = 5202;
    std::cout << 5202;
    for (int i = 0; i < 5202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5203(int& val) {
    func_in_5203(val);
    const int x = 5203;
    std::cout << 5203;
    for (int i = 0; i < 5203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5203(int& val) {
    const int x = 5203;
    std::cout << 5203;
    for (int i = 0; i < 5203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5204(int& val) {
    func_in_5204(val);
    const int x = 5204;
    std::cout << 5204;
    for (int i = 0; i < 5204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5204(int& val) {
    const int x = 5204;
    std::cout << 5204;
    for (int i = 0; i < 5204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5205(int& val) {
    func_in_5205(val);
    const int x = 5205;
    std::cout << 5205;
    for (int i = 0; i < 5205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5205(int& val) {
    const int x = 5205;
    std::cout << 5205;
    for (int i = 0; i < 5205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5206(int& val) {
    func_in_5206(val);
    const int x = 5206;
    std::cout << 5206;
    for (int i = 0; i < 5206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5206(int& val) {
    const int x = 5206;
    std::cout << 5206;
    for (int i = 0; i < 5206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5207(int& val) {
    func_in_5207(val);
    const int x = 5207;
    std::cout << 5207;
    for (int i = 0; i < 5207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5207(int& val) {
    const int x = 5207;
    std::cout << 5207;
    for (int i = 0; i < 5207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5208(int& val) {
    func_in_5208(val);
    const int x = 5208;
    std::cout << 5208;
    for (int i = 0; i < 5208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5208(int& val) {
    const int x = 5208;
    std::cout << 5208;
    for (int i = 0; i < 5208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5209(int& val) {
    func_in_5209(val);
    const int x = 5209;
    std::cout << 5209;
    for (int i = 0; i < 5209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5209(int& val) {
    const int x = 5209;
    std::cout << 5209;
    for (int i = 0; i < 5209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5210(int& val) {
    func_in_5210(val);
    const int x = 5210;
    std::cout << 5210;
    for (int i = 0; i < 5210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5210(int& val) {
    const int x = 5210;
    std::cout << 5210;
    for (int i = 0; i < 5210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5211(int& val) {
    func_in_5211(val);
    const int x = 5211;
    std::cout << 5211;
    for (int i = 0; i < 5211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5211(int& val) {
    const int x = 5211;
    std::cout << 5211;
    for (int i = 0; i < 5211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5212(int& val) {
    func_in_5212(val);
    const int x = 5212;
    std::cout << 5212;
    for (int i = 0; i < 5212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5212(int& val) {
    const int x = 5212;
    std::cout << 5212;
    for (int i = 0; i < 5212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5213(int& val) {
    func_in_5213(val);
    const int x = 5213;
    std::cout << 5213;
    for (int i = 0; i < 5213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5213(int& val) {
    const int x = 5213;
    std::cout << 5213;
    for (int i = 0; i < 5213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5214(int& val) {
    func_in_5214(val);
    const int x = 5214;
    std::cout << 5214;
    for (int i = 0; i < 5214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5214(int& val) {
    const int x = 5214;
    std::cout << 5214;
    for (int i = 0; i < 5214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5215(int& val) {
    func_in_5215(val);
    const int x = 5215;
    std::cout << 5215;
    for (int i = 0; i < 5215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5215(int& val) {
    const int x = 5215;
    std::cout << 5215;
    for (int i = 0; i < 5215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5216(int& val) {
    func_in_5216(val);
    const int x = 5216;
    std::cout << 5216;
    for (int i = 0; i < 5216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5216(int& val) {
    const int x = 5216;
    std::cout << 5216;
    for (int i = 0; i < 5216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5217(int& val) {
    func_in_5217(val);
    const int x = 5217;
    std::cout << 5217;
    for (int i = 0; i < 5217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5217(int& val) {
    const int x = 5217;
    std::cout << 5217;
    for (int i = 0; i < 5217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5218(int& val) {
    func_in_5218(val);
    const int x = 5218;
    std::cout << 5218;
    for (int i = 0; i < 5218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5218(int& val) {
    const int x = 5218;
    std::cout << 5218;
    for (int i = 0; i < 5218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5219(int& val) {
    func_in_5219(val);
    const int x = 5219;
    std::cout << 5219;
    for (int i = 0; i < 5219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5219(int& val) {
    const int x = 5219;
    std::cout << 5219;
    for (int i = 0; i < 5219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5220(int& val) {
    func_in_5220(val);
    const int x = 5220;
    std::cout << 5220;
    for (int i = 0; i < 5220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5220(int& val) {
    const int x = 5220;
    std::cout << 5220;
    for (int i = 0; i < 5220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5221(int& val) {
    func_in_5221(val);
    const int x = 5221;
    std::cout << 5221;
    for (int i = 0; i < 5221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5221(int& val) {
    const int x = 5221;
    std::cout << 5221;
    for (int i = 0; i < 5221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5222(int& val) {
    func_in_5222(val);
    const int x = 5222;
    std::cout << 5222;
    for (int i = 0; i < 5222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5222(int& val) {
    const int x = 5222;
    std::cout << 5222;
    for (int i = 0; i < 5222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5223(int& val) {
    func_in_5223(val);
    const int x = 5223;
    std::cout << 5223;
    for (int i = 0; i < 5223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5223(int& val) {
    const int x = 5223;
    std::cout << 5223;
    for (int i = 0; i < 5223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5224(int& val) {
    func_in_5224(val);
    const int x = 5224;
    std::cout << 5224;
    for (int i = 0; i < 5224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5224(int& val) {
    const int x = 5224;
    std::cout << 5224;
    for (int i = 0; i < 5224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5225(int& val) {
    func_in_5225(val);
    const int x = 5225;
    std::cout << 5225;
    for (int i = 0; i < 5225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5225(int& val) {
    const int x = 5225;
    std::cout << 5225;
    for (int i = 0; i < 5225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5226(int& val) {
    func_in_5226(val);
    const int x = 5226;
    std::cout << 5226;
    for (int i = 0; i < 5226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5226(int& val) {
    const int x = 5226;
    std::cout << 5226;
    for (int i = 0; i < 5226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5227(int& val) {
    func_in_5227(val);
    const int x = 5227;
    std::cout << 5227;
    for (int i = 0; i < 5227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5227(int& val) {
    const int x = 5227;
    std::cout << 5227;
    for (int i = 0; i < 5227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5228(int& val) {
    func_in_5228(val);
    const int x = 5228;
    std::cout << 5228;
    for (int i = 0; i < 5228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5228(int& val) {
    const int x = 5228;
    std::cout << 5228;
    for (int i = 0; i < 5228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5229(int& val) {
    func_in_5229(val);
    const int x = 5229;
    std::cout << 5229;
    for (int i = 0; i < 5229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5229(int& val) {
    const int x = 5229;
    std::cout << 5229;
    for (int i = 0; i < 5229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5230(int& val) {
    func_in_5230(val);
    const int x = 5230;
    std::cout << 5230;
    for (int i = 0; i < 5230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5230(int& val) {
    const int x = 5230;
    std::cout << 5230;
    for (int i = 0; i < 5230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5231(int& val) {
    func_in_5231(val);
    const int x = 5231;
    std::cout << 5231;
    for (int i = 0; i < 5231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5231(int& val) {
    const int x = 5231;
    std::cout << 5231;
    for (int i = 0; i < 5231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5232(int& val) {
    func_in_5232(val);
    const int x = 5232;
    std::cout << 5232;
    for (int i = 0; i < 5232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5232(int& val) {
    const int x = 5232;
    std::cout << 5232;
    for (int i = 0; i < 5232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5233(int& val) {
    func_in_5233(val);
    const int x = 5233;
    std::cout << 5233;
    for (int i = 0; i < 5233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5233(int& val) {
    const int x = 5233;
    std::cout << 5233;
    for (int i = 0; i < 5233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5234(int& val) {
    func_in_5234(val);
    const int x = 5234;
    std::cout << 5234;
    for (int i = 0; i < 5234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5234(int& val) {
    const int x = 5234;
    std::cout << 5234;
    for (int i = 0; i < 5234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5235(int& val) {
    func_in_5235(val);
    const int x = 5235;
    std::cout << 5235;
    for (int i = 0; i < 5235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5235(int& val) {
    const int x = 5235;
    std::cout << 5235;
    for (int i = 0; i < 5235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5236(int& val) {
    func_in_5236(val);
    const int x = 5236;
    std::cout << 5236;
    for (int i = 0; i < 5236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5236(int& val) {
    const int x = 5236;
    std::cout << 5236;
    for (int i = 0; i < 5236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5237(int& val) {
    func_in_5237(val);
    const int x = 5237;
    std::cout << 5237;
    for (int i = 0; i < 5237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5237(int& val) {
    const int x = 5237;
    std::cout << 5237;
    for (int i = 0; i < 5237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5238(int& val) {
    func_in_5238(val);
    const int x = 5238;
    std::cout << 5238;
    for (int i = 0; i < 5238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5238(int& val) {
    const int x = 5238;
    std::cout << 5238;
    for (int i = 0; i < 5238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5239(int& val) {
    func_in_5239(val);
    const int x = 5239;
    std::cout << 5239;
    for (int i = 0; i < 5239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5239(int& val) {
    const int x = 5239;
    std::cout << 5239;
    for (int i = 0; i < 5239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5240(int& val) {
    func_in_5240(val);
    const int x = 5240;
    std::cout << 5240;
    for (int i = 0; i < 5240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5240(int& val) {
    const int x = 5240;
    std::cout << 5240;
    for (int i = 0; i < 5240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5241(int& val) {
    func_in_5241(val);
    const int x = 5241;
    std::cout << 5241;
    for (int i = 0; i < 5241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5241(int& val) {
    const int x = 5241;
    std::cout << 5241;
    for (int i = 0; i < 5241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5242(int& val) {
    func_in_5242(val);
    const int x = 5242;
    std::cout << 5242;
    for (int i = 0; i < 5242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5242(int& val) {
    const int x = 5242;
    std::cout << 5242;
    for (int i = 0; i < 5242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5243(int& val) {
    func_in_5243(val);
    const int x = 5243;
    std::cout << 5243;
    for (int i = 0; i < 5243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5243(int& val) {
    const int x = 5243;
    std::cout << 5243;
    for (int i = 0; i < 5243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5244(int& val) {
    func_in_5244(val);
    const int x = 5244;
    std::cout << 5244;
    for (int i = 0; i < 5244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5244(int& val) {
    const int x = 5244;
    std::cout << 5244;
    for (int i = 0; i < 5244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5245(int& val) {
    func_in_5245(val);
    const int x = 5245;
    std::cout << 5245;
    for (int i = 0; i < 5245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5245(int& val) {
    const int x = 5245;
    std::cout << 5245;
    for (int i = 0; i < 5245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5246(int& val) {
    func_in_5246(val);
    const int x = 5246;
    std::cout << 5246;
    for (int i = 0; i < 5246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5246(int& val) {
    const int x = 5246;
    std::cout << 5246;
    for (int i = 0; i < 5246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5247(int& val) {
    func_in_5247(val);
    const int x = 5247;
    std::cout << 5247;
    for (int i = 0; i < 5247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5247(int& val) {
    const int x = 5247;
    std::cout << 5247;
    for (int i = 0; i < 5247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5248(int& val) {
    func_in_5248(val);
    const int x = 5248;
    std::cout << 5248;
    for (int i = 0; i < 5248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5248(int& val) {
    const int x = 5248;
    std::cout << 5248;
    for (int i = 0; i < 5248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5249(int& val) {
    func_in_5249(val);
    const int x = 5249;
    std::cout << 5249;
    for (int i = 0; i < 5249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5249(int& val) {
    const int x = 5249;
    std::cout << 5249;
    for (int i = 0; i < 5249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5250(int& val) {
    func_in_5250(val);
    const int x = 5250;
    std::cout << 5250;
    for (int i = 0; i < 5250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5250(int& val) {
    const int x = 5250;
    std::cout << 5250;
    for (int i = 0; i < 5250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5251(int& val) {
    func_in_5251(val);
    const int x = 5251;
    std::cout << 5251;
    for (int i = 0; i < 5251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5251(int& val) {
    const int x = 5251;
    std::cout << 5251;
    for (int i = 0; i < 5251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5252(int& val) {
    func_in_5252(val);
    const int x = 5252;
    std::cout << 5252;
    for (int i = 0; i < 5252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5252(int& val) {
    const int x = 5252;
    std::cout << 5252;
    for (int i = 0; i < 5252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5253(int& val) {
    func_in_5253(val);
    const int x = 5253;
    std::cout << 5253;
    for (int i = 0; i < 5253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5253(int& val) {
    const int x = 5253;
    std::cout << 5253;
    for (int i = 0; i < 5253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5254(int& val) {
    func_in_5254(val);
    const int x = 5254;
    std::cout << 5254;
    for (int i = 0; i < 5254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5254(int& val) {
    const int x = 5254;
    std::cout << 5254;
    for (int i = 0; i < 5254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5255(int& val) {
    func_in_5255(val);
    const int x = 5255;
    std::cout << 5255;
    for (int i = 0; i < 5255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5255(int& val) {
    const int x = 5255;
    std::cout << 5255;
    for (int i = 0; i < 5255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5256(int& val) {
    func_in_5256(val);
    const int x = 5256;
    std::cout << 5256;
    for (int i = 0; i < 5256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5256(int& val) {
    const int x = 5256;
    std::cout << 5256;
    for (int i = 0; i < 5256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5257(int& val) {
    func_in_5257(val);
    const int x = 5257;
    std::cout << 5257;
    for (int i = 0; i < 5257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5257(int& val) {
    const int x = 5257;
    std::cout << 5257;
    for (int i = 0; i < 5257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5258(int& val) {
    func_in_5258(val);
    const int x = 5258;
    std::cout << 5258;
    for (int i = 0; i < 5258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5258(int& val) {
    const int x = 5258;
    std::cout << 5258;
    for (int i = 0; i < 5258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5259(int& val) {
    func_in_5259(val);
    const int x = 5259;
    std::cout << 5259;
    for (int i = 0; i < 5259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5259(int& val) {
    const int x = 5259;
    std::cout << 5259;
    for (int i = 0; i < 5259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5260(int& val) {
    func_in_5260(val);
    const int x = 5260;
    std::cout << 5260;
    for (int i = 0; i < 5260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5260(int& val) {
    const int x = 5260;
    std::cout << 5260;
    for (int i = 0; i < 5260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5261(int& val) {
    func_in_5261(val);
    const int x = 5261;
    std::cout << 5261;
    for (int i = 0; i < 5261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5261(int& val) {
    const int x = 5261;
    std::cout << 5261;
    for (int i = 0; i < 5261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5262(int& val) {
    func_in_5262(val);
    const int x = 5262;
    std::cout << 5262;
    for (int i = 0; i < 5262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5262(int& val) {
    const int x = 5262;
    std::cout << 5262;
    for (int i = 0; i < 5262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5263(int& val) {
    func_in_5263(val);
    const int x = 5263;
    std::cout << 5263;
    for (int i = 0; i < 5263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5263(int& val) {
    const int x = 5263;
    std::cout << 5263;
    for (int i = 0; i < 5263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5264(int& val) {
    func_in_5264(val);
    const int x = 5264;
    std::cout << 5264;
    for (int i = 0; i < 5264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5264(int& val) {
    const int x = 5264;
    std::cout << 5264;
    for (int i = 0; i < 5264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5265(int& val) {
    func_in_5265(val);
    const int x = 5265;
    std::cout << 5265;
    for (int i = 0; i < 5265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5265(int& val) {
    const int x = 5265;
    std::cout << 5265;
    for (int i = 0; i < 5265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5266(int& val) {
    func_in_5266(val);
    const int x = 5266;
    std::cout << 5266;
    for (int i = 0; i < 5266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5266(int& val) {
    const int x = 5266;
    std::cout << 5266;
    for (int i = 0; i < 5266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5267(int& val) {
    func_in_5267(val);
    const int x = 5267;
    std::cout << 5267;
    for (int i = 0; i < 5267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5267(int& val) {
    const int x = 5267;
    std::cout << 5267;
    for (int i = 0; i < 5267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5268(int& val) {
    func_in_5268(val);
    const int x = 5268;
    std::cout << 5268;
    for (int i = 0; i < 5268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5268(int& val) {
    const int x = 5268;
    std::cout << 5268;
    for (int i = 0; i < 5268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5269(int& val) {
    func_in_5269(val);
    const int x = 5269;
    std::cout << 5269;
    for (int i = 0; i < 5269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5269(int& val) {
    const int x = 5269;
    std::cout << 5269;
    for (int i = 0; i < 5269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5270(int& val) {
    func_in_5270(val);
    const int x = 5270;
    std::cout << 5270;
    for (int i = 0; i < 5270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5270(int& val) {
    const int x = 5270;
    std::cout << 5270;
    for (int i = 0; i < 5270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5271(int& val) {
    func_in_5271(val);
    const int x = 5271;
    std::cout << 5271;
    for (int i = 0; i < 5271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5271(int& val) {
    const int x = 5271;
    std::cout << 5271;
    for (int i = 0; i < 5271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5272(int& val) {
    func_in_5272(val);
    const int x = 5272;
    std::cout << 5272;
    for (int i = 0; i < 5272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5272(int& val) {
    const int x = 5272;
    std::cout << 5272;
    for (int i = 0; i < 5272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5273(int& val) {
    func_in_5273(val);
    const int x = 5273;
    std::cout << 5273;
    for (int i = 0; i < 5273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5273(int& val) {
    const int x = 5273;
    std::cout << 5273;
    for (int i = 0; i < 5273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5274(int& val) {
    func_in_5274(val);
    const int x = 5274;
    std::cout << 5274;
    for (int i = 0; i < 5274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5274(int& val) {
    const int x = 5274;
    std::cout << 5274;
    for (int i = 0; i < 5274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5275(int& val) {
    func_in_5275(val);
    const int x = 5275;
    std::cout << 5275;
    for (int i = 0; i < 5275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5275(int& val) {
    const int x = 5275;
    std::cout << 5275;
    for (int i = 0; i < 5275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5276(int& val) {
    func_in_5276(val);
    const int x = 5276;
    std::cout << 5276;
    for (int i = 0; i < 5276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5276(int& val) {
    const int x = 5276;
    std::cout << 5276;
    for (int i = 0; i < 5276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5277(int& val) {
    func_in_5277(val);
    const int x = 5277;
    std::cout << 5277;
    for (int i = 0; i < 5277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5277(int& val) {
    const int x = 5277;
    std::cout << 5277;
    for (int i = 0; i < 5277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5278(int& val) {
    func_in_5278(val);
    const int x = 5278;
    std::cout << 5278;
    for (int i = 0; i < 5278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5278(int& val) {
    const int x = 5278;
    std::cout << 5278;
    for (int i = 0; i < 5278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5279(int& val) {
    func_in_5279(val);
    const int x = 5279;
    std::cout << 5279;
    for (int i = 0; i < 5279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5279(int& val) {
    const int x = 5279;
    std::cout << 5279;
    for (int i = 0; i < 5279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5280(int& val) {
    func_in_5280(val);
    const int x = 5280;
    std::cout << 5280;
    for (int i = 0; i < 5280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5280(int& val) {
    const int x = 5280;
    std::cout << 5280;
    for (int i = 0; i < 5280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5281(int& val) {
    func_in_5281(val);
    const int x = 5281;
    std::cout << 5281;
    for (int i = 0; i < 5281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5281(int& val) {
    const int x = 5281;
    std::cout << 5281;
    for (int i = 0; i < 5281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5282(int& val) {
    func_in_5282(val);
    const int x = 5282;
    std::cout << 5282;
    for (int i = 0; i < 5282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5282(int& val) {
    const int x = 5282;
    std::cout << 5282;
    for (int i = 0; i < 5282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5283(int& val) {
    func_in_5283(val);
    const int x = 5283;
    std::cout << 5283;
    for (int i = 0; i < 5283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5283(int& val) {
    const int x = 5283;
    std::cout << 5283;
    for (int i = 0; i < 5283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5284(int& val) {
    func_in_5284(val);
    const int x = 5284;
    std::cout << 5284;
    for (int i = 0; i < 5284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5284(int& val) {
    const int x = 5284;
    std::cout << 5284;
    for (int i = 0; i < 5284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5285(int& val) {
    func_in_5285(val);
    const int x = 5285;
    std::cout << 5285;
    for (int i = 0; i < 5285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5285(int& val) {
    const int x = 5285;
    std::cout << 5285;
    for (int i = 0; i < 5285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5286(int& val) {
    func_in_5286(val);
    const int x = 5286;
    std::cout << 5286;
    for (int i = 0; i < 5286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5286(int& val) {
    const int x = 5286;
    std::cout << 5286;
    for (int i = 0; i < 5286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5287(int& val) {
    func_in_5287(val);
    const int x = 5287;
    std::cout << 5287;
    for (int i = 0; i < 5287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5287(int& val) {
    const int x = 5287;
    std::cout << 5287;
    for (int i = 0; i < 5287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5288(int& val) {
    func_in_5288(val);
    const int x = 5288;
    std::cout << 5288;
    for (int i = 0; i < 5288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5288(int& val) {
    const int x = 5288;
    std::cout << 5288;
    for (int i = 0; i < 5288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5289(int& val) {
    func_in_5289(val);
    const int x = 5289;
    std::cout << 5289;
    for (int i = 0; i < 5289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5289(int& val) {
    const int x = 5289;
    std::cout << 5289;
    for (int i = 0; i < 5289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5290(int& val) {
    func_in_5290(val);
    const int x = 5290;
    std::cout << 5290;
    for (int i = 0; i < 5290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5290(int& val) {
    const int x = 5290;
    std::cout << 5290;
    for (int i = 0; i < 5290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5291(int& val) {
    func_in_5291(val);
    const int x = 5291;
    std::cout << 5291;
    for (int i = 0; i < 5291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5291(int& val) {
    const int x = 5291;
    std::cout << 5291;
    for (int i = 0; i < 5291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5292(int& val) {
    func_in_5292(val);
    const int x = 5292;
    std::cout << 5292;
    for (int i = 0; i < 5292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5292(int& val) {
    const int x = 5292;
    std::cout << 5292;
    for (int i = 0; i < 5292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5293(int& val) {
    func_in_5293(val);
    const int x = 5293;
    std::cout << 5293;
    for (int i = 0; i < 5293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5293(int& val) {
    const int x = 5293;
    std::cout << 5293;
    for (int i = 0; i < 5293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5294(int& val) {
    func_in_5294(val);
    const int x = 5294;
    std::cout << 5294;
    for (int i = 0; i < 5294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5294(int& val) {
    const int x = 5294;
    std::cout << 5294;
    for (int i = 0; i < 5294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5295(int& val) {
    func_in_5295(val);
    const int x = 5295;
    std::cout << 5295;
    for (int i = 0; i < 5295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5295(int& val) {
    const int x = 5295;
    std::cout << 5295;
    for (int i = 0; i < 5295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5296(int& val) {
    func_in_5296(val);
    const int x = 5296;
    std::cout << 5296;
    for (int i = 0; i < 5296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5296(int& val) {
    const int x = 5296;
    std::cout << 5296;
    for (int i = 0; i < 5296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5297(int& val) {
    func_in_5297(val);
    const int x = 5297;
    std::cout << 5297;
    for (int i = 0; i < 5297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5297(int& val) {
    const int x = 5297;
    std::cout << 5297;
    for (int i = 0; i < 5297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5298(int& val) {
    func_in_5298(val);
    const int x = 5298;
    std::cout << 5298;
    for (int i = 0; i < 5298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5298(int& val) {
    const int x = 5298;
    std::cout << 5298;
    for (int i = 0; i < 5298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5299(int& val) {
    func_in_5299(val);
    const int x = 5299;
    std::cout << 5299;
    for (int i = 0; i < 5299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5299(int& val) {
    const int x = 5299;
    std::cout << 5299;
    for (int i = 0; i < 5299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5300(int& val) {
    func_in_5300(val);
    const int x = 5300;
    std::cout << 5300;
    for (int i = 0; i < 5300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5300(int& val) {
    const int x = 5300;
    std::cout << 5300;
    for (int i = 0; i < 5300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5301(int& val) {
    func_in_5301(val);
    const int x = 5301;
    std::cout << 5301;
    for (int i = 0; i < 5301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5301(int& val) {
    const int x = 5301;
    std::cout << 5301;
    for (int i = 0; i < 5301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5302(int& val) {
    func_in_5302(val);
    const int x = 5302;
    std::cout << 5302;
    for (int i = 0; i < 5302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5302(int& val) {
    const int x = 5302;
    std::cout << 5302;
    for (int i = 0; i < 5302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5303(int& val) {
    func_in_5303(val);
    const int x = 5303;
    std::cout << 5303;
    for (int i = 0; i < 5303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5303(int& val) {
    const int x = 5303;
    std::cout << 5303;
    for (int i = 0; i < 5303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5304(int& val) {
    func_in_5304(val);
    const int x = 5304;
    std::cout << 5304;
    for (int i = 0; i < 5304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5304(int& val) {
    const int x = 5304;
    std::cout << 5304;
    for (int i = 0; i < 5304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5305(int& val) {
    func_in_5305(val);
    const int x = 5305;
    std::cout << 5305;
    for (int i = 0; i < 5305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5305(int& val) {
    const int x = 5305;
    std::cout << 5305;
    for (int i = 0; i < 5305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5306(int& val) {
    func_in_5306(val);
    const int x = 5306;
    std::cout << 5306;
    for (int i = 0; i < 5306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5306(int& val) {
    const int x = 5306;
    std::cout << 5306;
    for (int i = 0; i < 5306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5307(int& val) {
    func_in_5307(val);
    const int x = 5307;
    std::cout << 5307;
    for (int i = 0; i < 5307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5307(int& val) {
    const int x = 5307;
    std::cout << 5307;
    for (int i = 0; i < 5307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5308(int& val) {
    func_in_5308(val);
    const int x = 5308;
    std::cout << 5308;
    for (int i = 0; i < 5308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5308(int& val) {
    const int x = 5308;
    std::cout << 5308;
    for (int i = 0; i < 5308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5309(int& val) {
    func_in_5309(val);
    const int x = 5309;
    std::cout << 5309;
    for (int i = 0; i < 5309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5309(int& val) {
    const int x = 5309;
    std::cout << 5309;
    for (int i = 0; i < 5309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5310(int& val) {
    func_in_5310(val);
    const int x = 5310;
    std::cout << 5310;
    for (int i = 0; i < 5310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5310(int& val) {
    const int x = 5310;
    std::cout << 5310;
    for (int i = 0; i < 5310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5311(int& val) {
    func_in_5311(val);
    const int x = 5311;
    std::cout << 5311;
    for (int i = 0; i < 5311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5311(int& val) {
    const int x = 5311;
    std::cout << 5311;
    for (int i = 0; i < 5311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5312(int& val) {
    func_in_5312(val);
    const int x = 5312;
    std::cout << 5312;
    for (int i = 0; i < 5312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5312(int& val) {
    const int x = 5312;
    std::cout << 5312;
    for (int i = 0; i < 5312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5313(int& val) {
    func_in_5313(val);
    const int x = 5313;
    std::cout << 5313;
    for (int i = 0; i < 5313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5313(int& val) {
    const int x = 5313;
    std::cout << 5313;
    for (int i = 0; i < 5313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5314(int& val) {
    func_in_5314(val);
    const int x = 5314;
    std::cout << 5314;
    for (int i = 0; i < 5314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5314(int& val) {
    const int x = 5314;
    std::cout << 5314;
    for (int i = 0; i < 5314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5315(int& val) {
    func_in_5315(val);
    const int x = 5315;
    std::cout << 5315;
    for (int i = 0; i < 5315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5315(int& val) {
    const int x = 5315;
    std::cout << 5315;
    for (int i = 0; i < 5315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5316(int& val) {
    func_in_5316(val);
    const int x = 5316;
    std::cout << 5316;
    for (int i = 0; i < 5316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5316(int& val) {
    const int x = 5316;
    std::cout << 5316;
    for (int i = 0; i < 5316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5317(int& val) {
    func_in_5317(val);
    const int x = 5317;
    std::cout << 5317;
    for (int i = 0; i < 5317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5317(int& val) {
    const int x = 5317;
    std::cout << 5317;
    for (int i = 0; i < 5317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5318(int& val) {
    func_in_5318(val);
    const int x = 5318;
    std::cout << 5318;
    for (int i = 0; i < 5318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5318(int& val) {
    const int x = 5318;
    std::cout << 5318;
    for (int i = 0; i < 5318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5319(int& val) {
    func_in_5319(val);
    const int x = 5319;
    std::cout << 5319;
    for (int i = 0; i < 5319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5319(int& val) {
    const int x = 5319;
    std::cout << 5319;
    for (int i = 0; i < 5319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5320(int& val) {
    func_in_5320(val);
    const int x = 5320;
    std::cout << 5320;
    for (int i = 0; i < 5320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5320(int& val) {
    const int x = 5320;
    std::cout << 5320;
    for (int i = 0; i < 5320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5321(int& val) {
    func_in_5321(val);
    const int x = 5321;
    std::cout << 5321;
    for (int i = 0; i < 5321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5321(int& val) {
    const int x = 5321;
    std::cout << 5321;
    for (int i = 0; i < 5321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5322(int& val) {
    func_in_5322(val);
    const int x = 5322;
    std::cout << 5322;
    for (int i = 0; i < 5322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5322(int& val) {
    const int x = 5322;
    std::cout << 5322;
    for (int i = 0; i < 5322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5323(int& val) {
    func_in_5323(val);
    const int x = 5323;
    std::cout << 5323;
    for (int i = 0; i < 5323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5323(int& val) {
    const int x = 5323;
    std::cout << 5323;
    for (int i = 0; i < 5323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5324(int& val) {
    func_in_5324(val);
    const int x = 5324;
    std::cout << 5324;
    for (int i = 0; i < 5324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5324(int& val) {
    const int x = 5324;
    std::cout << 5324;
    for (int i = 0; i < 5324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5325(int& val) {
    func_in_5325(val);
    const int x = 5325;
    std::cout << 5325;
    for (int i = 0; i < 5325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5325(int& val) {
    const int x = 5325;
    std::cout << 5325;
    for (int i = 0; i < 5325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5326(int& val) {
    func_in_5326(val);
    const int x = 5326;
    std::cout << 5326;
    for (int i = 0; i < 5326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5326(int& val) {
    const int x = 5326;
    std::cout << 5326;
    for (int i = 0; i < 5326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5327(int& val) {
    func_in_5327(val);
    const int x = 5327;
    std::cout << 5327;
    for (int i = 0; i < 5327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5327(int& val) {
    const int x = 5327;
    std::cout << 5327;
    for (int i = 0; i < 5327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5328(int& val) {
    func_in_5328(val);
    const int x = 5328;
    std::cout << 5328;
    for (int i = 0; i < 5328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5328(int& val) {
    const int x = 5328;
    std::cout << 5328;
    for (int i = 0; i < 5328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5329(int& val) {
    func_in_5329(val);
    const int x = 5329;
    std::cout << 5329;
    for (int i = 0; i < 5329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5329(int& val) {
    const int x = 5329;
    std::cout << 5329;
    for (int i = 0; i < 5329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5330(int& val) {
    func_in_5330(val);
    const int x = 5330;
    std::cout << 5330;
    for (int i = 0; i < 5330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5330(int& val) {
    const int x = 5330;
    std::cout << 5330;
    for (int i = 0; i < 5330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5331(int& val) {
    func_in_5331(val);
    const int x = 5331;
    std::cout << 5331;
    for (int i = 0; i < 5331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5331(int& val) {
    const int x = 5331;
    std::cout << 5331;
    for (int i = 0; i < 5331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5332(int& val) {
    func_in_5332(val);
    const int x = 5332;
    std::cout << 5332;
    for (int i = 0; i < 5332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5332(int& val) {
    const int x = 5332;
    std::cout << 5332;
    for (int i = 0; i < 5332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5333(int& val) {
    func_in_5333(val);
    const int x = 5333;
    std::cout << 5333;
    for (int i = 0; i < 5333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5333(int& val) {
    const int x = 5333;
    std::cout << 5333;
    for (int i = 0; i < 5333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5334(int& val) {
    func_in_5334(val);
    const int x = 5334;
    std::cout << 5334;
    for (int i = 0; i < 5334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5334(int& val) {
    const int x = 5334;
    std::cout << 5334;
    for (int i = 0; i < 5334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5335(int& val) {
    func_in_5335(val);
    const int x = 5335;
    std::cout << 5335;
    for (int i = 0; i < 5335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5335(int& val) {
    const int x = 5335;
    std::cout << 5335;
    for (int i = 0; i < 5335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5336(int& val) {
    func_in_5336(val);
    const int x = 5336;
    std::cout << 5336;
    for (int i = 0; i < 5336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5336(int& val) {
    const int x = 5336;
    std::cout << 5336;
    for (int i = 0; i < 5336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5337(int& val) {
    func_in_5337(val);
    const int x = 5337;
    std::cout << 5337;
    for (int i = 0; i < 5337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5337(int& val) {
    const int x = 5337;
    std::cout << 5337;
    for (int i = 0; i < 5337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5338(int& val) {
    func_in_5338(val);
    const int x = 5338;
    std::cout << 5338;
    for (int i = 0; i < 5338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5338(int& val) {
    const int x = 5338;
    std::cout << 5338;
    for (int i = 0; i < 5338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5339(int& val) {
    func_in_5339(val);
    const int x = 5339;
    std::cout << 5339;
    for (int i = 0; i < 5339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5339(int& val) {
    const int x = 5339;
    std::cout << 5339;
    for (int i = 0; i < 5339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5340(int& val) {
    func_in_5340(val);
    const int x = 5340;
    std::cout << 5340;
    for (int i = 0; i < 5340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5340(int& val) {
    const int x = 5340;
    std::cout << 5340;
    for (int i = 0; i < 5340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5341(int& val) {
    func_in_5341(val);
    const int x = 5341;
    std::cout << 5341;
    for (int i = 0; i < 5341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5341(int& val) {
    const int x = 5341;
    std::cout << 5341;
    for (int i = 0; i < 5341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5342(int& val) {
    func_in_5342(val);
    const int x = 5342;
    std::cout << 5342;
    for (int i = 0; i < 5342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5342(int& val) {
    const int x = 5342;
    std::cout << 5342;
    for (int i = 0; i < 5342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5343(int& val) {
    func_in_5343(val);
    const int x = 5343;
    std::cout << 5343;
    for (int i = 0; i < 5343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5343(int& val) {
    const int x = 5343;
    std::cout << 5343;
    for (int i = 0; i < 5343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5344(int& val) {
    func_in_5344(val);
    const int x = 5344;
    std::cout << 5344;
    for (int i = 0; i < 5344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5344(int& val) {
    const int x = 5344;
    std::cout << 5344;
    for (int i = 0; i < 5344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5345(int& val) {
    func_in_5345(val);
    const int x = 5345;
    std::cout << 5345;
    for (int i = 0; i < 5345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5345(int& val) {
    const int x = 5345;
    std::cout << 5345;
    for (int i = 0; i < 5345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5346(int& val) {
    func_in_5346(val);
    const int x = 5346;
    std::cout << 5346;
    for (int i = 0; i < 5346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5346(int& val) {
    const int x = 5346;
    std::cout << 5346;
    for (int i = 0; i < 5346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5347(int& val) {
    func_in_5347(val);
    const int x = 5347;
    std::cout << 5347;
    for (int i = 0; i < 5347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5347(int& val) {
    const int x = 5347;
    std::cout << 5347;
    for (int i = 0; i < 5347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5348(int& val) {
    func_in_5348(val);
    const int x = 5348;
    std::cout << 5348;
    for (int i = 0; i < 5348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5348(int& val) {
    const int x = 5348;
    std::cout << 5348;
    for (int i = 0; i < 5348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5349(int& val) {
    func_in_5349(val);
    const int x = 5349;
    std::cout << 5349;
    for (int i = 0; i < 5349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5349(int& val) {
    const int x = 5349;
    std::cout << 5349;
    for (int i = 0; i < 5349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5350(int& val) {
    func_in_5350(val);
    const int x = 5350;
    std::cout << 5350;
    for (int i = 0; i < 5350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5350(int& val) {
    const int x = 5350;
    std::cout << 5350;
    for (int i = 0; i < 5350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5351(int& val) {
    func_in_5351(val);
    const int x = 5351;
    std::cout << 5351;
    for (int i = 0; i < 5351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5351(int& val) {
    const int x = 5351;
    std::cout << 5351;
    for (int i = 0; i < 5351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5352(int& val) {
    func_in_5352(val);
    const int x = 5352;
    std::cout << 5352;
    for (int i = 0; i < 5352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5352(int& val) {
    const int x = 5352;
    std::cout << 5352;
    for (int i = 0; i < 5352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5353(int& val) {
    func_in_5353(val);
    const int x = 5353;
    std::cout << 5353;
    for (int i = 0; i < 5353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5353(int& val) {
    const int x = 5353;
    std::cout << 5353;
    for (int i = 0; i < 5353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5354(int& val) {
    func_in_5354(val);
    const int x = 5354;
    std::cout << 5354;
    for (int i = 0; i < 5354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5354(int& val) {
    const int x = 5354;
    std::cout << 5354;
    for (int i = 0; i < 5354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5355(int& val) {
    func_in_5355(val);
    const int x = 5355;
    std::cout << 5355;
    for (int i = 0; i < 5355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5355(int& val) {
    const int x = 5355;
    std::cout << 5355;
    for (int i = 0; i < 5355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5356(int& val) {
    func_in_5356(val);
    const int x = 5356;
    std::cout << 5356;
    for (int i = 0; i < 5356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5356(int& val) {
    const int x = 5356;
    std::cout << 5356;
    for (int i = 0; i < 5356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5357(int& val) {
    func_in_5357(val);
    const int x = 5357;
    std::cout << 5357;
    for (int i = 0; i < 5357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5357(int& val) {
    const int x = 5357;
    std::cout << 5357;
    for (int i = 0; i < 5357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5358(int& val) {
    func_in_5358(val);
    const int x = 5358;
    std::cout << 5358;
    for (int i = 0; i < 5358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5358(int& val) {
    const int x = 5358;
    std::cout << 5358;
    for (int i = 0; i < 5358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5359(int& val) {
    func_in_5359(val);
    const int x = 5359;
    std::cout << 5359;
    for (int i = 0; i < 5359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5359(int& val) {
    const int x = 5359;
    std::cout << 5359;
    for (int i = 0; i < 5359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5360(int& val) {
    func_in_5360(val);
    const int x = 5360;
    std::cout << 5360;
    for (int i = 0; i < 5360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5360(int& val) {
    const int x = 5360;
    std::cout << 5360;
    for (int i = 0; i < 5360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5361(int& val) {
    func_in_5361(val);
    const int x = 5361;
    std::cout << 5361;
    for (int i = 0; i < 5361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5361(int& val) {
    const int x = 5361;
    std::cout << 5361;
    for (int i = 0; i < 5361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5362(int& val) {
    func_in_5362(val);
    const int x = 5362;
    std::cout << 5362;
    for (int i = 0; i < 5362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5362(int& val) {
    const int x = 5362;
    std::cout << 5362;
    for (int i = 0; i < 5362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5363(int& val) {
    func_in_5363(val);
    const int x = 5363;
    std::cout << 5363;
    for (int i = 0; i < 5363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5363(int& val) {
    const int x = 5363;
    std::cout << 5363;
    for (int i = 0; i < 5363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5364(int& val) {
    func_in_5364(val);
    const int x = 5364;
    std::cout << 5364;
    for (int i = 0; i < 5364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5364(int& val) {
    const int x = 5364;
    std::cout << 5364;
    for (int i = 0; i < 5364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5365(int& val) {
    func_in_5365(val);
    const int x = 5365;
    std::cout << 5365;
    for (int i = 0; i < 5365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5365(int& val) {
    const int x = 5365;
    std::cout << 5365;
    for (int i = 0; i < 5365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5366(int& val) {
    func_in_5366(val);
    const int x = 5366;
    std::cout << 5366;
    for (int i = 0; i < 5366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5366(int& val) {
    const int x = 5366;
    std::cout << 5366;
    for (int i = 0; i < 5366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5367(int& val) {
    func_in_5367(val);
    const int x = 5367;
    std::cout << 5367;
    for (int i = 0; i < 5367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5367(int& val) {
    const int x = 5367;
    std::cout << 5367;
    for (int i = 0; i < 5367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5368(int& val) {
    func_in_5368(val);
    const int x = 5368;
    std::cout << 5368;
    for (int i = 0; i < 5368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5368(int& val) {
    const int x = 5368;
    std::cout << 5368;
    for (int i = 0; i < 5368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5369(int& val) {
    func_in_5369(val);
    const int x = 5369;
    std::cout << 5369;
    for (int i = 0; i < 5369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5369(int& val) {
    const int x = 5369;
    std::cout << 5369;
    for (int i = 0; i < 5369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5370(int& val) {
    func_in_5370(val);
    const int x = 5370;
    std::cout << 5370;
    for (int i = 0; i < 5370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5370(int& val) {
    const int x = 5370;
    std::cout << 5370;
    for (int i = 0; i < 5370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5371(int& val) {
    func_in_5371(val);
    const int x = 5371;
    std::cout << 5371;
    for (int i = 0; i < 5371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5371(int& val) {
    const int x = 5371;
    std::cout << 5371;
    for (int i = 0; i < 5371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5372(int& val) {
    func_in_5372(val);
    const int x = 5372;
    std::cout << 5372;
    for (int i = 0; i < 5372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5372(int& val) {
    const int x = 5372;
    std::cout << 5372;
    for (int i = 0; i < 5372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5373(int& val) {
    func_in_5373(val);
    const int x = 5373;
    std::cout << 5373;
    for (int i = 0; i < 5373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5373(int& val) {
    const int x = 5373;
    std::cout << 5373;
    for (int i = 0; i < 5373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5374(int& val) {
    func_in_5374(val);
    const int x = 5374;
    std::cout << 5374;
    for (int i = 0; i < 5374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5374(int& val) {
    const int x = 5374;
    std::cout << 5374;
    for (int i = 0; i < 5374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5375(int& val) {
    func_in_5375(val);
    const int x = 5375;
    std::cout << 5375;
    for (int i = 0; i < 5375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5375(int& val) {
    const int x = 5375;
    std::cout << 5375;
    for (int i = 0; i < 5375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5376(int& val) {
    func_in_5376(val);
    const int x = 5376;
    std::cout << 5376;
    for (int i = 0; i < 5376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5376(int& val) {
    const int x = 5376;
    std::cout << 5376;
    for (int i = 0; i < 5376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5377(int& val) {
    func_in_5377(val);
    const int x = 5377;
    std::cout << 5377;
    for (int i = 0; i < 5377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5377(int& val) {
    const int x = 5377;
    std::cout << 5377;
    for (int i = 0; i < 5377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5378(int& val) {
    func_in_5378(val);
    const int x = 5378;
    std::cout << 5378;
    for (int i = 0; i < 5378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5378(int& val) {
    const int x = 5378;
    std::cout << 5378;
    for (int i = 0; i < 5378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5379(int& val) {
    func_in_5379(val);
    const int x = 5379;
    std::cout << 5379;
    for (int i = 0; i < 5379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5379(int& val) {
    const int x = 5379;
    std::cout << 5379;
    for (int i = 0; i < 5379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5380(int& val) {
    func_in_5380(val);
    const int x = 5380;
    std::cout << 5380;
    for (int i = 0; i < 5380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5380(int& val) {
    const int x = 5380;
    std::cout << 5380;
    for (int i = 0; i < 5380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5381(int& val) {
    func_in_5381(val);
    const int x = 5381;
    std::cout << 5381;
    for (int i = 0; i < 5381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5381(int& val) {
    const int x = 5381;
    std::cout << 5381;
    for (int i = 0; i < 5381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5382(int& val) {
    func_in_5382(val);
    const int x = 5382;
    std::cout << 5382;
    for (int i = 0; i < 5382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5382(int& val) {
    const int x = 5382;
    std::cout << 5382;
    for (int i = 0; i < 5382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5383(int& val) {
    func_in_5383(val);
    const int x = 5383;
    std::cout << 5383;
    for (int i = 0; i < 5383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5383(int& val) {
    const int x = 5383;
    std::cout << 5383;
    for (int i = 0; i < 5383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5384(int& val) {
    func_in_5384(val);
    const int x = 5384;
    std::cout << 5384;
    for (int i = 0; i < 5384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5384(int& val) {
    const int x = 5384;
    std::cout << 5384;
    for (int i = 0; i < 5384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5385(int& val) {
    func_in_5385(val);
    const int x = 5385;
    std::cout << 5385;
    for (int i = 0; i < 5385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5385(int& val) {
    const int x = 5385;
    std::cout << 5385;
    for (int i = 0; i < 5385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5386(int& val) {
    func_in_5386(val);
    const int x = 5386;
    std::cout << 5386;
    for (int i = 0; i < 5386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5386(int& val) {
    const int x = 5386;
    std::cout << 5386;
    for (int i = 0; i < 5386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5387(int& val) {
    func_in_5387(val);
    const int x = 5387;
    std::cout << 5387;
    for (int i = 0; i < 5387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5387(int& val) {
    const int x = 5387;
    std::cout << 5387;
    for (int i = 0; i < 5387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5388(int& val) {
    func_in_5388(val);
    const int x = 5388;
    std::cout << 5388;
    for (int i = 0; i < 5388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5388(int& val) {
    const int x = 5388;
    std::cout << 5388;
    for (int i = 0; i < 5388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5389(int& val) {
    func_in_5389(val);
    const int x = 5389;
    std::cout << 5389;
    for (int i = 0; i < 5389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5389(int& val) {
    const int x = 5389;
    std::cout << 5389;
    for (int i = 0; i < 5389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5390(int& val) {
    func_in_5390(val);
    const int x = 5390;
    std::cout << 5390;
    for (int i = 0; i < 5390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5390(int& val) {
    const int x = 5390;
    std::cout << 5390;
    for (int i = 0; i < 5390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5391(int& val) {
    func_in_5391(val);
    const int x = 5391;
    std::cout << 5391;
    for (int i = 0; i < 5391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5391(int& val) {
    const int x = 5391;
    std::cout << 5391;
    for (int i = 0; i < 5391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5392(int& val) {
    func_in_5392(val);
    const int x = 5392;
    std::cout << 5392;
    for (int i = 0; i < 5392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5392(int& val) {
    const int x = 5392;
    std::cout << 5392;
    for (int i = 0; i < 5392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5393(int& val) {
    func_in_5393(val);
    const int x = 5393;
    std::cout << 5393;
    for (int i = 0; i < 5393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5393(int& val) {
    const int x = 5393;
    std::cout << 5393;
    for (int i = 0; i < 5393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5394(int& val) {
    func_in_5394(val);
    const int x = 5394;
    std::cout << 5394;
    for (int i = 0; i < 5394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5394(int& val) {
    const int x = 5394;
    std::cout << 5394;
    for (int i = 0; i < 5394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5395(int& val) {
    func_in_5395(val);
    const int x = 5395;
    std::cout << 5395;
    for (int i = 0; i < 5395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5395(int& val) {
    const int x = 5395;
    std::cout << 5395;
    for (int i = 0; i < 5395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5396(int& val) {
    func_in_5396(val);
    const int x = 5396;
    std::cout << 5396;
    for (int i = 0; i < 5396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5396(int& val) {
    const int x = 5396;
    std::cout << 5396;
    for (int i = 0; i < 5396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5397(int& val) {
    func_in_5397(val);
    const int x = 5397;
    std::cout << 5397;
    for (int i = 0; i < 5397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5397(int& val) {
    const int x = 5397;
    std::cout << 5397;
    for (int i = 0; i < 5397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5398(int& val) {
    func_in_5398(val);
    const int x = 5398;
    std::cout << 5398;
    for (int i = 0; i < 5398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5398(int& val) {
    const int x = 5398;
    std::cout << 5398;
    for (int i = 0; i < 5398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5399(int& val) {
    func_in_5399(val);
    const int x = 5399;
    std::cout << 5399;
    for (int i = 0; i < 5399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5399(int& val) {
    const int x = 5399;
    std::cout << 5399;
    for (int i = 0; i < 5399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5400(int& val) {
    func_in_5400(val);
    const int x = 5400;
    std::cout << 5400;
    for (int i = 0; i < 5400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5400(int& val) {
    const int x = 5400;
    std::cout << 5400;
    for (int i = 0; i < 5400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5401(int& val) {
    func_in_5401(val);
    const int x = 5401;
    std::cout << 5401;
    for (int i = 0; i < 5401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5401(int& val) {
    const int x = 5401;
    std::cout << 5401;
    for (int i = 0; i < 5401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5402(int& val) {
    func_in_5402(val);
    const int x = 5402;
    std::cout << 5402;
    for (int i = 0; i < 5402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5402(int& val) {
    const int x = 5402;
    std::cout << 5402;
    for (int i = 0; i < 5402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5403(int& val) {
    func_in_5403(val);
    const int x = 5403;
    std::cout << 5403;
    for (int i = 0; i < 5403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5403(int& val) {
    const int x = 5403;
    std::cout << 5403;
    for (int i = 0; i < 5403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5404(int& val) {
    func_in_5404(val);
    const int x = 5404;
    std::cout << 5404;
    for (int i = 0; i < 5404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5404(int& val) {
    const int x = 5404;
    std::cout << 5404;
    for (int i = 0; i < 5404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5405(int& val) {
    func_in_5405(val);
    const int x = 5405;
    std::cout << 5405;
    for (int i = 0; i < 5405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5405(int& val) {
    const int x = 5405;
    std::cout << 5405;
    for (int i = 0; i < 5405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5406(int& val) {
    func_in_5406(val);
    const int x = 5406;
    std::cout << 5406;
    for (int i = 0; i < 5406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5406(int& val) {
    const int x = 5406;
    std::cout << 5406;
    for (int i = 0; i < 5406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5407(int& val) {
    func_in_5407(val);
    const int x = 5407;
    std::cout << 5407;
    for (int i = 0; i < 5407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5407(int& val) {
    const int x = 5407;
    std::cout << 5407;
    for (int i = 0; i < 5407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5408(int& val) {
    func_in_5408(val);
    const int x = 5408;
    std::cout << 5408;
    for (int i = 0; i < 5408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5408(int& val) {
    const int x = 5408;
    std::cout << 5408;
    for (int i = 0; i < 5408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5409(int& val) {
    func_in_5409(val);
    const int x = 5409;
    std::cout << 5409;
    for (int i = 0; i < 5409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5409(int& val) {
    const int x = 5409;
    std::cout << 5409;
    for (int i = 0; i < 5409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5410(int& val) {
    func_in_5410(val);
    const int x = 5410;
    std::cout << 5410;
    for (int i = 0; i < 5410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5410(int& val) {
    const int x = 5410;
    std::cout << 5410;
    for (int i = 0; i < 5410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5411(int& val) {
    func_in_5411(val);
    const int x = 5411;
    std::cout << 5411;
    for (int i = 0; i < 5411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5411(int& val) {
    const int x = 5411;
    std::cout << 5411;
    for (int i = 0; i < 5411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5412(int& val) {
    func_in_5412(val);
    const int x = 5412;
    std::cout << 5412;
    for (int i = 0; i < 5412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5412(int& val) {
    const int x = 5412;
    std::cout << 5412;
    for (int i = 0; i < 5412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5413(int& val) {
    func_in_5413(val);
    const int x = 5413;
    std::cout << 5413;
    for (int i = 0; i < 5413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5413(int& val) {
    const int x = 5413;
    std::cout << 5413;
    for (int i = 0; i < 5413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5414(int& val) {
    func_in_5414(val);
    const int x = 5414;
    std::cout << 5414;
    for (int i = 0; i < 5414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5414(int& val) {
    const int x = 5414;
    std::cout << 5414;
    for (int i = 0; i < 5414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5415(int& val) {
    func_in_5415(val);
    const int x = 5415;
    std::cout << 5415;
    for (int i = 0; i < 5415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5415(int& val) {
    const int x = 5415;
    std::cout << 5415;
    for (int i = 0; i < 5415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5416(int& val) {
    func_in_5416(val);
    const int x = 5416;
    std::cout << 5416;
    for (int i = 0; i < 5416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5416(int& val) {
    const int x = 5416;
    std::cout << 5416;
    for (int i = 0; i < 5416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5417(int& val) {
    func_in_5417(val);
    const int x = 5417;
    std::cout << 5417;
    for (int i = 0; i < 5417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5417(int& val) {
    const int x = 5417;
    std::cout << 5417;
    for (int i = 0; i < 5417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5418(int& val) {
    func_in_5418(val);
    const int x = 5418;
    std::cout << 5418;
    for (int i = 0; i < 5418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5418(int& val) {
    const int x = 5418;
    std::cout << 5418;
    for (int i = 0; i < 5418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5419(int& val) {
    func_in_5419(val);
    const int x = 5419;
    std::cout << 5419;
    for (int i = 0; i < 5419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5419(int& val) {
    const int x = 5419;
    std::cout << 5419;
    for (int i = 0; i < 5419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5420(int& val) {
    func_in_5420(val);
    const int x = 5420;
    std::cout << 5420;
    for (int i = 0; i < 5420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5420(int& val) {
    const int x = 5420;
    std::cout << 5420;
    for (int i = 0; i < 5420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5421(int& val) {
    func_in_5421(val);
    const int x = 5421;
    std::cout << 5421;
    for (int i = 0; i < 5421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5421(int& val) {
    const int x = 5421;
    std::cout << 5421;
    for (int i = 0; i < 5421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5422(int& val) {
    func_in_5422(val);
    const int x = 5422;
    std::cout << 5422;
    for (int i = 0; i < 5422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5422(int& val) {
    const int x = 5422;
    std::cout << 5422;
    for (int i = 0; i < 5422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5423(int& val) {
    func_in_5423(val);
    const int x = 5423;
    std::cout << 5423;
    for (int i = 0; i < 5423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5423(int& val) {
    const int x = 5423;
    std::cout << 5423;
    for (int i = 0; i < 5423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5424(int& val) {
    func_in_5424(val);
    const int x = 5424;
    std::cout << 5424;
    for (int i = 0; i < 5424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5424(int& val) {
    const int x = 5424;
    std::cout << 5424;
    for (int i = 0; i < 5424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5425(int& val) {
    func_in_5425(val);
    const int x = 5425;
    std::cout << 5425;
    for (int i = 0; i < 5425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5425(int& val) {
    const int x = 5425;
    std::cout << 5425;
    for (int i = 0; i < 5425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5426(int& val) {
    func_in_5426(val);
    const int x = 5426;
    std::cout << 5426;
    for (int i = 0; i < 5426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5426(int& val) {
    const int x = 5426;
    std::cout << 5426;
    for (int i = 0; i < 5426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5427(int& val) {
    func_in_5427(val);
    const int x = 5427;
    std::cout << 5427;
    for (int i = 0; i < 5427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5427(int& val) {
    const int x = 5427;
    std::cout << 5427;
    for (int i = 0; i < 5427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5428(int& val) {
    func_in_5428(val);
    const int x = 5428;
    std::cout << 5428;
    for (int i = 0; i < 5428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5428(int& val) {
    const int x = 5428;
    std::cout << 5428;
    for (int i = 0; i < 5428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5429(int& val) {
    func_in_5429(val);
    const int x = 5429;
    std::cout << 5429;
    for (int i = 0; i < 5429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5429(int& val) {
    const int x = 5429;
    std::cout << 5429;
    for (int i = 0; i < 5429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5430(int& val) {
    func_in_5430(val);
    const int x = 5430;
    std::cout << 5430;
    for (int i = 0; i < 5430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5430(int& val) {
    const int x = 5430;
    std::cout << 5430;
    for (int i = 0; i < 5430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5431(int& val) {
    func_in_5431(val);
    const int x = 5431;
    std::cout << 5431;
    for (int i = 0; i < 5431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5431(int& val) {
    const int x = 5431;
    std::cout << 5431;
    for (int i = 0; i < 5431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5432(int& val) {
    func_in_5432(val);
    const int x = 5432;
    std::cout << 5432;
    for (int i = 0; i < 5432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5432(int& val) {
    const int x = 5432;
    std::cout << 5432;
    for (int i = 0; i < 5432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5433(int& val) {
    func_in_5433(val);
    const int x = 5433;
    std::cout << 5433;
    for (int i = 0; i < 5433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5433(int& val) {
    const int x = 5433;
    std::cout << 5433;
    for (int i = 0; i < 5433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5434(int& val) {
    func_in_5434(val);
    const int x = 5434;
    std::cout << 5434;
    for (int i = 0; i < 5434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5434(int& val) {
    const int x = 5434;
    std::cout << 5434;
    for (int i = 0; i < 5434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5435(int& val) {
    func_in_5435(val);
    const int x = 5435;
    std::cout << 5435;
    for (int i = 0; i < 5435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5435(int& val) {
    const int x = 5435;
    std::cout << 5435;
    for (int i = 0; i < 5435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5436(int& val) {
    func_in_5436(val);
    const int x = 5436;
    std::cout << 5436;
    for (int i = 0; i < 5436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5436(int& val) {
    const int x = 5436;
    std::cout << 5436;
    for (int i = 0; i < 5436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5437(int& val) {
    func_in_5437(val);
    const int x = 5437;
    std::cout << 5437;
    for (int i = 0; i < 5437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5437(int& val) {
    const int x = 5437;
    std::cout << 5437;
    for (int i = 0; i < 5437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5438(int& val) {
    func_in_5438(val);
    const int x = 5438;
    std::cout << 5438;
    for (int i = 0; i < 5438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5438(int& val) {
    const int x = 5438;
    std::cout << 5438;
    for (int i = 0; i < 5438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5439(int& val) {
    func_in_5439(val);
    const int x = 5439;
    std::cout << 5439;
    for (int i = 0; i < 5439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5439(int& val) {
    const int x = 5439;
    std::cout << 5439;
    for (int i = 0; i < 5439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5440(int& val) {
    func_in_5440(val);
    const int x = 5440;
    std::cout << 5440;
    for (int i = 0; i < 5440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5440(int& val) {
    const int x = 5440;
    std::cout << 5440;
    for (int i = 0; i < 5440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5441(int& val) {
    func_in_5441(val);
    const int x = 5441;
    std::cout << 5441;
    for (int i = 0; i < 5441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5441(int& val) {
    const int x = 5441;
    std::cout << 5441;
    for (int i = 0; i < 5441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5442(int& val) {
    func_in_5442(val);
    const int x = 5442;
    std::cout << 5442;
    for (int i = 0; i < 5442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5442(int& val) {
    const int x = 5442;
    std::cout << 5442;
    for (int i = 0; i < 5442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5443(int& val) {
    func_in_5443(val);
    const int x = 5443;
    std::cout << 5443;
    for (int i = 0; i < 5443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5443(int& val) {
    const int x = 5443;
    std::cout << 5443;
    for (int i = 0; i < 5443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5444(int& val) {
    func_in_5444(val);
    const int x = 5444;
    std::cout << 5444;
    for (int i = 0; i < 5444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5444(int& val) {
    const int x = 5444;
    std::cout << 5444;
    for (int i = 0; i < 5444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5445(int& val) {
    func_in_5445(val);
    const int x = 5445;
    std::cout << 5445;
    for (int i = 0; i < 5445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5445(int& val) {
    const int x = 5445;
    std::cout << 5445;
    for (int i = 0; i < 5445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5446(int& val) {
    func_in_5446(val);
    const int x = 5446;
    std::cout << 5446;
    for (int i = 0; i < 5446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5446(int& val) {
    const int x = 5446;
    std::cout << 5446;
    for (int i = 0; i < 5446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5447(int& val) {
    func_in_5447(val);
    const int x = 5447;
    std::cout << 5447;
    for (int i = 0; i < 5447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5447(int& val) {
    const int x = 5447;
    std::cout << 5447;
    for (int i = 0; i < 5447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5448(int& val) {
    func_in_5448(val);
    const int x = 5448;
    std::cout << 5448;
    for (int i = 0; i < 5448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5448(int& val) {
    const int x = 5448;
    std::cout << 5448;
    for (int i = 0; i < 5448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5449(int& val) {
    func_in_5449(val);
    const int x = 5449;
    std::cout << 5449;
    for (int i = 0; i < 5449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5449(int& val) {
    const int x = 5449;
    std::cout << 5449;
    for (int i = 0; i < 5449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5450(int& val) {
    func_in_5450(val);
    const int x = 5450;
    std::cout << 5450;
    for (int i = 0; i < 5450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5450(int& val) {
    const int x = 5450;
    std::cout << 5450;
    for (int i = 0; i < 5450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5451(int& val) {
    func_in_5451(val);
    const int x = 5451;
    std::cout << 5451;
    for (int i = 0; i < 5451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5451(int& val) {
    const int x = 5451;
    std::cout << 5451;
    for (int i = 0; i < 5451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5452(int& val) {
    func_in_5452(val);
    const int x = 5452;
    std::cout << 5452;
    for (int i = 0; i < 5452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5452(int& val) {
    const int x = 5452;
    std::cout << 5452;
    for (int i = 0; i < 5452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5453(int& val) {
    func_in_5453(val);
    const int x = 5453;
    std::cout << 5453;
    for (int i = 0; i < 5453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5453(int& val) {
    const int x = 5453;
    std::cout << 5453;
    for (int i = 0; i < 5453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5454(int& val) {
    func_in_5454(val);
    const int x = 5454;
    std::cout << 5454;
    for (int i = 0; i < 5454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5454(int& val) {
    const int x = 5454;
    std::cout << 5454;
    for (int i = 0; i < 5454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5455(int& val) {
    func_in_5455(val);
    const int x = 5455;
    std::cout << 5455;
    for (int i = 0; i < 5455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5455(int& val) {
    const int x = 5455;
    std::cout << 5455;
    for (int i = 0; i < 5455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5456(int& val) {
    func_in_5456(val);
    const int x = 5456;
    std::cout << 5456;
    for (int i = 0; i < 5456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5456(int& val) {
    const int x = 5456;
    std::cout << 5456;
    for (int i = 0; i < 5456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5457(int& val) {
    func_in_5457(val);
    const int x = 5457;
    std::cout << 5457;
    for (int i = 0; i < 5457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5457(int& val) {
    const int x = 5457;
    std::cout << 5457;
    for (int i = 0; i < 5457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5458(int& val) {
    func_in_5458(val);
    const int x = 5458;
    std::cout << 5458;
    for (int i = 0; i < 5458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5458(int& val) {
    const int x = 5458;
    std::cout << 5458;
    for (int i = 0; i < 5458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5459(int& val) {
    func_in_5459(val);
    const int x = 5459;
    std::cout << 5459;
    for (int i = 0; i < 5459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5459(int& val) {
    const int x = 5459;
    std::cout << 5459;
    for (int i = 0; i < 5459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5460(int& val) {
    func_in_5460(val);
    const int x = 5460;
    std::cout << 5460;
    for (int i = 0; i < 5460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5460(int& val) {
    const int x = 5460;
    std::cout << 5460;
    for (int i = 0; i < 5460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5461(int& val) {
    func_in_5461(val);
    const int x = 5461;
    std::cout << 5461;
    for (int i = 0; i < 5461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5461(int& val) {
    const int x = 5461;
    std::cout << 5461;
    for (int i = 0; i < 5461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5462(int& val) {
    func_in_5462(val);
    const int x = 5462;
    std::cout << 5462;
    for (int i = 0; i < 5462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5462(int& val) {
    const int x = 5462;
    std::cout << 5462;
    for (int i = 0; i < 5462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5463(int& val) {
    func_in_5463(val);
    const int x = 5463;
    std::cout << 5463;
    for (int i = 0; i < 5463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5463(int& val) {
    const int x = 5463;
    std::cout << 5463;
    for (int i = 0; i < 5463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5464(int& val) {
    func_in_5464(val);
    const int x = 5464;
    std::cout << 5464;
    for (int i = 0; i < 5464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5464(int& val) {
    const int x = 5464;
    std::cout << 5464;
    for (int i = 0; i < 5464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5465(int& val) {
    func_in_5465(val);
    const int x = 5465;
    std::cout << 5465;
    for (int i = 0; i < 5465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5465(int& val) {
    const int x = 5465;
    std::cout << 5465;
    for (int i = 0; i < 5465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5466(int& val) {
    func_in_5466(val);
    const int x = 5466;
    std::cout << 5466;
    for (int i = 0; i < 5466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5466(int& val) {
    const int x = 5466;
    std::cout << 5466;
    for (int i = 0; i < 5466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5467(int& val) {
    func_in_5467(val);
    const int x = 5467;
    std::cout << 5467;
    for (int i = 0; i < 5467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5467(int& val) {
    const int x = 5467;
    std::cout << 5467;
    for (int i = 0; i < 5467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5468(int& val) {
    func_in_5468(val);
    const int x = 5468;
    std::cout << 5468;
    for (int i = 0; i < 5468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5468(int& val) {
    const int x = 5468;
    std::cout << 5468;
    for (int i = 0; i < 5468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5469(int& val) {
    func_in_5469(val);
    const int x = 5469;
    std::cout << 5469;
    for (int i = 0; i < 5469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5469(int& val) {
    const int x = 5469;
    std::cout << 5469;
    for (int i = 0; i < 5469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5470(int& val) {
    func_in_5470(val);
    const int x = 5470;
    std::cout << 5470;
    for (int i = 0; i < 5470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5470(int& val) {
    const int x = 5470;
    std::cout << 5470;
    for (int i = 0; i < 5470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5471(int& val) {
    func_in_5471(val);
    const int x = 5471;
    std::cout << 5471;
    for (int i = 0; i < 5471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5471(int& val) {
    const int x = 5471;
    std::cout << 5471;
    for (int i = 0; i < 5471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5472(int& val) {
    func_in_5472(val);
    const int x = 5472;
    std::cout << 5472;
    for (int i = 0; i < 5472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5472(int& val) {
    const int x = 5472;
    std::cout << 5472;
    for (int i = 0; i < 5472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5473(int& val) {
    func_in_5473(val);
    const int x = 5473;
    std::cout << 5473;
    for (int i = 0; i < 5473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5473(int& val) {
    const int x = 5473;
    std::cout << 5473;
    for (int i = 0; i < 5473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5474(int& val) {
    func_in_5474(val);
    const int x = 5474;
    std::cout << 5474;
    for (int i = 0; i < 5474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5474(int& val) {
    const int x = 5474;
    std::cout << 5474;
    for (int i = 0; i < 5474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5475(int& val) {
    func_in_5475(val);
    const int x = 5475;
    std::cout << 5475;
    for (int i = 0; i < 5475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5475(int& val) {
    const int x = 5475;
    std::cout << 5475;
    for (int i = 0; i < 5475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5476(int& val) {
    func_in_5476(val);
    const int x = 5476;
    std::cout << 5476;
    for (int i = 0; i < 5476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5476(int& val) {
    const int x = 5476;
    std::cout << 5476;
    for (int i = 0; i < 5476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5477(int& val) {
    func_in_5477(val);
    const int x = 5477;
    std::cout << 5477;
    for (int i = 0; i < 5477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5477(int& val) {
    const int x = 5477;
    std::cout << 5477;
    for (int i = 0; i < 5477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5478(int& val) {
    func_in_5478(val);
    const int x = 5478;
    std::cout << 5478;
    for (int i = 0; i < 5478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5478(int& val) {
    const int x = 5478;
    std::cout << 5478;
    for (int i = 0; i < 5478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5479(int& val) {
    func_in_5479(val);
    const int x = 5479;
    std::cout << 5479;
    for (int i = 0; i < 5479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5479(int& val) {
    const int x = 5479;
    std::cout << 5479;
    for (int i = 0; i < 5479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5480(int& val) {
    func_in_5480(val);
    const int x = 5480;
    std::cout << 5480;
    for (int i = 0; i < 5480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5480(int& val) {
    const int x = 5480;
    std::cout << 5480;
    for (int i = 0; i < 5480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5481(int& val) {
    func_in_5481(val);
    const int x = 5481;
    std::cout << 5481;
    for (int i = 0; i < 5481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5481(int& val) {
    const int x = 5481;
    std::cout << 5481;
    for (int i = 0; i < 5481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5482(int& val) {
    func_in_5482(val);
    const int x = 5482;
    std::cout << 5482;
    for (int i = 0; i < 5482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5482(int& val) {
    const int x = 5482;
    std::cout << 5482;
    for (int i = 0; i < 5482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5483(int& val) {
    func_in_5483(val);
    const int x = 5483;
    std::cout << 5483;
    for (int i = 0; i < 5483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5483(int& val) {
    const int x = 5483;
    std::cout << 5483;
    for (int i = 0; i < 5483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5484(int& val) {
    func_in_5484(val);
    const int x = 5484;
    std::cout << 5484;
    for (int i = 0; i < 5484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5484(int& val) {
    const int x = 5484;
    std::cout << 5484;
    for (int i = 0; i < 5484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5485(int& val) {
    func_in_5485(val);
    const int x = 5485;
    std::cout << 5485;
    for (int i = 0; i < 5485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5485(int& val) {
    const int x = 5485;
    std::cout << 5485;
    for (int i = 0; i < 5485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5486(int& val) {
    func_in_5486(val);
    const int x = 5486;
    std::cout << 5486;
    for (int i = 0; i < 5486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5486(int& val) {
    const int x = 5486;
    std::cout << 5486;
    for (int i = 0; i < 5486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5487(int& val) {
    func_in_5487(val);
    const int x = 5487;
    std::cout << 5487;
    for (int i = 0; i < 5487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5487(int& val) {
    const int x = 5487;
    std::cout << 5487;
    for (int i = 0; i < 5487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5488(int& val) {
    func_in_5488(val);
    const int x = 5488;
    std::cout << 5488;
    for (int i = 0; i < 5488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5488(int& val) {
    const int x = 5488;
    std::cout << 5488;
    for (int i = 0; i < 5488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5489(int& val) {
    func_in_5489(val);
    const int x = 5489;
    std::cout << 5489;
    for (int i = 0; i < 5489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5489(int& val) {
    const int x = 5489;
    std::cout << 5489;
    for (int i = 0; i < 5489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5490(int& val) {
    func_in_5490(val);
    const int x = 5490;
    std::cout << 5490;
    for (int i = 0; i < 5490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5490(int& val) {
    const int x = 5490;
    std::cout << 5490;
    for (int i = 0; i < 5490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5491(int& val) {
    func_in_5491(val);
    const int x = 5491;
    std::cout << 5491;
    for (int i = 0; i < 5491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5491(int& val) {
    const int x = 5491;
    std::cout << 5491;
    for (int i = 0; i < 5491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5492(int& val) {
    func_in_5492(val);
    const int x = 5492;
    std::cout << 5492;
    for (int i = 0; i < 5492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5492(int& val) {
    const int x = 5492;
    std::cout << 5492;
    for (int i = 0; i < 5492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5493(int& val) {
    func_in_5493(val);
    const int x = 5493;
    std::cout << 5493;
    for (int i = 0; i < 5493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5493(int& val) {
    const int x = 5493;
    std::cout << 5493;
    for (int i = 0; i < 5493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5494(int& val) {
    func_in_5494(val);
    const int x = 5494;
    std::cout << 5494;
    for (int i = 0; i < 5494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5494(int& val) {
    const int x = 5494;
    std::cout << 5494;
    for (int i = 0; i < 5494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5495(int& val) {
    func_in_5495(val);
    const int x = 5495;
    std::cout << 5495;
    for (int i = 0; i < 5495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5495(int& val) {
    const int x = 5495;
    std::cout << 5495;
    for (int i = 0; i < 5495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5496(int& val) {
    func_in_5496(val);
    const int x = 5496;
    std::cout << 5496;
    for (int i = 0; i < 5496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5496(int& val) {
    const int x = 5496;
    std::cout << 5496;
    for (int i = 0; i < 5496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5497(int& val) {
    func_in_5497(val);
    const int x = 5497;
    std::cout << 5497;
    for (int i = 0; i < 5497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5497(int& val) {
    const int x = 5497;
    std::cout << 5497;
    for (int i = 0; i < 5497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5498(int& val) {
    func_in_5498(val);
    const int x = 5498;
    std::cout << 5498;
    for (int i = 0; i < 5498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5498(int& val) {
    const int x = 5498;
    std::cout << 5498;
    for (int i = 0; i < 5498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5499(int& val) {
    func_in_5499(val);
    const int x = 5499;
    std::cout << 5499;
    for (int i = 0; i < 5499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5499(int& val) {
    const int x = 5499;
    std::cout << 5499;
    for (int i = 0; i < 5499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5500(int& val) {
    func_in_5500(val);
    const int x = 5500;
    std::cout << 5500;
    for (int i = 0; i < 5500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5500(int& val) {
    const int x = 5500;
    std::cout << 5500;
    for (int i = 0; i < 5500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5501(int& val) {
    func_in_5501(val);
    const int x = 5501;
    std::cout << 5501;
    for (int i = 0; i < 5501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5501(int& val) {
    const int x = 5501;
    std::cout << 5501;
    for (int i = 0; i < 5501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5502(int& val) {
    func_in_5502(val);
    const int x = 5502;
    std::cout << 5502;
    for (int i = 0; i < 5502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5502(int& val) {
    const int x = 5502;
    std::cout << 5502;
    for (int i = 0; i < 5502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5503(int& val) {
    func_in_5503(val);
    const int x = 5503;
    std::cout << 5503;
    for (int i = 0; i < 5503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5503(int& val) {
    const int x = 5503;
    std::cout << 5503;
    for (int i = 0; i < 5503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5504(int& val) {
    func_in_5504(val);
    const int x = 5504;
    std::cout << 5504;
    for (int i = 0; i < 5504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5504(int& val) {
    const int x = 5504;
    std::cout << 5504;
    for (int i = 0; i < 5504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5505(int& val) {
    func_in_5505(val);
    const int x = 5505;
    std::cout << 5505;
    for (int i = 0; i < 5505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5505(int& val) {
    const int x = 5505;
    std::cout << 5505;
    for (int i = 0; i < 5505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5506(int& val) {
    func_in_5506(val);
    const int x = 5506;
    std::cout << 5506;
    for (int i = 0; i < 5506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5506(int& val) {
    const int x = 5506;
    std::cout << 5506;
    for (int i = 0; i < 5506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5507(int& val) {
    func_in_5507(val);
    const int x = 5507;
    std::cout << 5507;
    for (int i = 0; i < 5507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5507(int& val) {
    const int x = 5507;
    std::cout << 5507;
    for (int i = 0; i < 5507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5508(int& val) {
    func_in_5508(val);
    const int x = 5508;
    std::cout << 5508;
    for (int i = 0; i < 5508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5508(int& val) {
    const int x = 5508;
    std::cout << 5508;
    for (int i = 0; i < 5508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5509(int& val) {
    func_in_5509(val);
    const int x = 5509;
    std::cout << 5509;
    for (int i = 0; i < 5509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5509(int& val) {
    const int x = 5509;
    std::cout << 5509;
    for (int i = 0; i < 5509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5510(int& val) {
    func_in_5510(val);
    const int x = 5510;
    std::cout << 5510;
    for (int i = 0; i < 5510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5510(int& val) {
    const int x = 5510;
    std::cout << 5510;
    for (int i = 0; i < 5510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5511(int& val) {
    func_in_5511(val);
    const int x = 5511;
    std::cout << 5511;
    for (int i = 0; i < 5511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5511(int& val) {
    const int x = 5511;
    std::cout << 5511;
    for (int i = 0; i < 5511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5512(int& val) {
    func_in_5512(val);
    const int x = 5512;
    std::cout << 5512;
    for (int i = 0; i < 5512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5512(int& val) {
    const int x = 5512;
    std::cout << 5512;
    for (int i = 0; i < 5512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5513(int& val) {
    func_in_5513(val);
    const int x = 5513;
    std::cout << 5513;
    for (int i = 0; i < 5513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5513(int& val) {
    const int x = 5513;
    std::cout << 5513;
    for (int i = 0; i < 5513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5514(int& val) {
    func_in_5514(val);
    const int x = 5514;
    std::cout << 5514;
    for (int i = 0; i < 5514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5514(int& val) {
    const int x = 5514;
    std::cout << 5514;
    for (int i = 0; i < 5514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5515(int& val) {
    func_in_5515(val);
    const int x = 5515;
    std::cout << 5515;
    for (int i = 0; i < 5515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5515(int& val) {
    const int x = 5515;
    std::cout << 5515;
    for (int i = 0; i < 5515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5516(int& val) {
    func_in_5516(val);
    const int x = 5516;
    std::cout << 5516;
    for (int i = 0; i < 5516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5516(int& val) {
    const int x = 5516;
    std::cout << 5516;
    for (int i = 0; i < 5516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5517(int& val) {
    func_in_5517(val);
    const int x = 5517;
    std::cout << 5517;
    for (int i = 0; i < 5517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5517(int& val) {
    const int x = 5517;
    std::cout << 5517;
    for (int i = 0; i < 5517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5518(int& val) {
    func_in_5518(val);
    const int x = 5518;
    std::cout << 5518;
    for (int i = 0; i < 5518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5518(int& val) {
    const int x = 5518;
    std::cout << 5518;
    for (int i = 0; i < 5518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5519(int& val) {
    func_in_5519(val);
    const int x = 5519;
    std::cout << 5519;
    for (int i = 0; i < 5519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5519(int& val) {
    const int x = 5519;
    std::cout << 5519;
    for (int i = 0; i < 5519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5520(int& val) {
    func_in_5520(val);
    const int x = 5520;
    std::cout << 5520;
    for (int i = 0; i < 5520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5520(int& val) {
    const int x = 5520;
    std::cout << 5520;
    for (int i = 0; i < 5520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5521(int& val) {
    func_in_5521(val);
    const int x = 5521;
    std::cout << 5521;
    for (int i = 0; i < 5521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5521(int& val) {
    const int x = 5521;
    std::cout << 5521;
    for (int i = 0; i < 5521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5522(int& val) {
    func_in_5522(val);
    const int x = 5522;
    std::cout << 5522;
    for (int i = 0; i < 5522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5522(int& val) {
    const int x = 5522;
    std::cout << 5522;
    for (int i = 0; i < 5522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5523(int& val) {
    func_in_5523(val);
    const int x = 5523;
    std::cout << 5523;
    for (int i = 0; i < 5523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5523(int& val) {
    const int x = 5523;
    std::cout << 5523;
    for (int i = 0; i < 5523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5524(int& val) {
    func_in_5524(val);
    const int x = 5524;
    std::cout << 5524;
    for (int i = 0; i < 5524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5524(int& val) {
    const int x = 5524;
    std::cout << 5524;
    for (int i = 0; i < 5524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5525(int& val) {
    func_in_5525(val);
    const int x = 5525;
    std::cout << 5525;
    for (int i = 0; i < 5525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5525(int& val) {
    const int x = 5525;
    std::cout << 5525;
    for (int i = 0; i < 5525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5526(int& val) {
    func_in_5526(val);
    const int x = 5526;
    std::cout << 5526;
    for (int i = 0; i < 5526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5526(int& val) {
    const int x = 5526;
    std::cout << 5526;
    for (int i = 0; i < 5526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5527(int& val) {
    func_in_5527(val);
    const int x = 5527;
    std::cout << 5527;
    for (int i = 0; i < 5527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5527(int& val) {
    const int x = 5527;
    std::cout << 5527;
    for (int i = 0; i < 5527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5528(int& val) {
    func_in_5528(val);
    const int x = 5528;
    std::cout << 5528;
    for (int i = 0; i < 5528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5528(int& val) {
    const int x = 5528;
    std::cout << 5528;
    for (int i = 0; i < 5528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5529(int& val) {
    func_in_5529(val);
    const int x = 5529;
    std::cout << 5529;
    for (int i = 0; i < 5529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5529(int& val) {
    const int x = 5529;
    std::cout << 5529;
    for (int i = 0; i < 5529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5530(int& val) {
    func_in_5530(val);
    const int x = 5530;
    std::cout << 5530;
    for (int i = 0; i < 5530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5530(int& val) {
    const int x = 5530;
    std::cout << 5530;
    for (int i = 0; i < 5530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5531(int& val) {
    func_in_5531(val);
    const int x = 5531;
    std::cout << 5531;
    for (int i = 0; i < 5531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5531(int& val) {
    const int x = 5531;
    std::cout << 5531;
    for (int i = 0; i < 5531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5532(int& val) {
    func_in_5532(val);
    const int x = 5532;
    std::cout << 5532;
    for (int i = 0; i < 5532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5532(int& val) {
    const int x = 5532;
    std::cout << 5532;
    for (int i = 0; i < 5532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5533(int& val) {
    func_in_5533(val);
    const int x = 5533;
    std::cout << 5533;
    for (int i = 0; i < 5533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5533(int& val) {
    const int x = 5533;
    std::cout << 5533;
    for (int i = 0; i < 5533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5534(int& val) {
    func_in_5534(val);
    const int x = 5534;
    std::cout << 5534;
    for (int i = 0; i < 5534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5534(int& val) {
    const int x = 5534;
    std::cout << 5534;
    for (int i = 0; i < 5534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5535(int& val) {
    func_in_5535(val);
    const int x = 5535;
    std::cout << 5535;
    for (int i = 0; i < 5535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5535(int& val) {
    const int x = 5535;
    std::cout << 5535;
    for (int i = 0; i < 5535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5536(int& val) {
    func_in_5536(val);
    const int x = 5536;
    std::cout << 5536;
    for (int i = 0; i < 5536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5536(int& val) {
    const int x = 5536;
    std::cout << 5536;
    for (int i = 0; i < 5536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5537(int& val) {
    func_in_5537(val);
    const int x = 5537;
    std::cout << 5537;
    for (int i = 0; i < 5537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5537(int& val) {
    const int x = 5537;
    std::cout << 5537;
    for (int i = 0; i < 5537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5538(int& val) {
    func_in_5538(val);
    const int x = 5538;
    std::cout << 5538;
    for (int i = 0; i < 5538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5538(int& val) {
    const int x = 5538;
    std::cout << 5538;
    for (int i = 0; i < 5538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5539(int& val) {
    func_in_5539(val);
    const int x = 5539;
    std::cout << 5539;
    for (int i = 0; i < 5539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5539(int& val) {
    const int x = 5539;
    std::cout << 5539;
    for (int i = 0; i < 5539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5540(int& val) {
    func_in_5540(val);
    const int x = 5540;
    std::cout << 5540;
    for (int i = 0; i < 5540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5540(int& val) {
    const int x = 5540;
    std::cout << 5540;
    for (int i = 0; i < 5540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5541(int& val) {
    func_in_5541(val);
    const int x = 5541;
    std::cout << 5541;
    for (int i = 0; i < 5541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5541(int& val) {
    const int x = 5541;
    std::cout << 5541;
    for (int i = 0; i < 5541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5542(int& val) {
    func_in_5542(val);
    const int x = 5542;
    std::cout << 5542;
    for (int i = 0; i < 5542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5542(int& val) {
    const int x = 5542;
    std::cout << 5542;
    for (int i = 0; i < 5542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5543(int& val) {
    func_in_5543(val);
    const int x = 5543;
    std::cout << 5543;
    for (int i = 0; i < 5543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5543(int& val) {
    const int x = 5543;
    std::cout << 5543;
    for (int i = 0; i < 5543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5544(int& val) {
    func_in_5544(val);
    const int x = 5544;
    std::cout << 5544;
    for (int i = 0; i < 5544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5544(int& val) {
    const int x = 5544;
    std::cout << 5544;
    for (int i = 0; i < 5544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5545(int& val) {
    func_in_5545(val);
    const int x = 5545;
    std::cout << 5545;
    for (int i = 0; i < 5545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5545(int& val) {
    const int x = 5545;
    std::cout << 5545;
    for (int i = 0; i < 5545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5546(int& val) {
    func_in_5546(val);
    const int x = 5546;
    std::cout << 5546;
    for (int i = 0; i < 5546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5546(int& val) {
    const int x = 5546;
    std::cout << 5546;
    for (int i = 0; i < 5546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5547(int& val) {
    func_in_5547(val);
    const int x = 5547;
    std::cout << 5547;
    for (int i = 0; i < 5547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5547(int& val) {
    const int x = 5547;
    std::cout << 5547;
    for (int i = 0; i < 5547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5548(int& val) {
    func_in_5548(val);
    const int x = 5548;
    std::cout << 5548;
    for (int i = 0; i < 5548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5548(int& val) {
    const int x = 5548;
    std::cout << 5548;
    for (int i = 0; i < 5548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5549(int& val) {
    func_in_5549(val);
    const int x = 5549;
    std::cout << 5549;
    for (int i = 0; i < 5549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5549(int& val) {
    const int x = 5549;
    std::cout << 5549;
    for (int i = 0; i < 5549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5550(int& val) {
    func_in_5550(val);
    const int x = 5550;
    std::cout << 5550;
    for (int i = 0; i < 5550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5550(int& val) {
    const int x = 5550;
    std::cout << 5550;
    for (int i = 0; i < 5550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5551(int& val) {
    func_in_5551(val);
    const int x = 5551;
    std::cout << 5551;
    for (int i = 0; i < 5551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5551(int& val) {
    const int x = 5551;
    std::cout << 5551;
    for (int i = 0; i < 5551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5552(int& val) {
    func_in_5552(val);
    const int x = 5552;
    std::cout << 5552;
    for (int i = 0; i < 5552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5552(int& val) {
    const int x = 5552;
    std::cout << 5552;
    for (int i = 0; i < 5552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5553(int& val) {
    func_in_5553(val);
    const int x = 5553;
    std::cout << 5553;
    for (int i = 0; i < 5553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5553(int& val) {
    const int x = 5553;
    std::cout << 5553;
    for (int i = 0; i < 5553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5554(int& val) {
    func_in_5554(val);
    const int x = 5554;
    std::cout << 5554;
    for (int i = 0; i < 5554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5554(int& val) {
    const int x = 5554;
    std::cout << 5554;
    for (int i = 0; i < 5554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5555(int& val) {
    func_in_5555(val);
    const int x = 5555;
    std::cout << 5555;
    for (int i = 0; i < 5555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5555(int& val) {
    const int x = 5555;
    std::cout << 5555;
    for (int i = 0; i < 5555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5556(int& val) {
    func_in_5556(val);
    const int x = 5556;
    std::cout << 5556;
    for (int i = 0; i < 5556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5556(int& val) {
    const int x = 5556;
    std::cout << 5556;
    for (int i = 0; i < 5556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5557(int& val) {
    func_in_5557(val);
    const int x = 5557;
    std::cout << 5557;
    for (int i = 0; i < 5557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5557(int& val) {
    const int x = 5557;
    std::cout << 5557;
    for (int i = 0; i < 5557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5558(int& val) {
    func_in_5558(val);
    const int x = 5558;
    std::cout << 5558;
    for (int i = 0; i < 5558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5558(int& val) {
    const int x = 5558;
    std::cout << 5558;
    for (int i = 0; i < 5558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5559(int& val) {
    func_in_5559(val);
    const int x = 5559;
    std::cout << 5559;
    for (int i = 0; i < 5559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5559(int& val) {
    const int x = 5559;
    std::cout << 5559;
    for (int i = 0; i < 5559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5560(int& val) {
    func_in_5560(val);
    const int x = 5560;
    std::cout << 5560;
    for (int i = 0; i < 5560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5560(int& val) {
    const int x = 5560;
    std::cout << 5560;
    for (int i = 0; i < 5560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5561(int& val) {
    func_in_5561(val);
    const int x = 5561;
    std::cout << 5561;
    for (int i = 0; i < 5561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5561(int& val) {
    const int x = 5561;
    std::cout << 5561;
    for (int i = 0; i < 5561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5562(int& val) {
    func_in_5562(val);
    const int x = 5562;
    std::cout << 5562;
    for (int i = 0; i < 5562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5562(int& val) {
    const int x = 5562;
    std::cout << 5562;
    for (int i = 0; i < 5562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5563(int& val) {
    func_in_5563(val);
    const int x = 5563;
    std::cout << 5563;
    for (int i = 0; i < 5563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5563(int& val) {
    const int x = 5563;
    std::cout << 5563;
    for (int i = 0; i < 5563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5564(int& val) {
    func_in_5564(val);
    const int x = 5564;
    std::cout << 5564;
    for (int i = 0; i < 5564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5564(int& val) {
    const int x = 5564;
    std::cout << 5564;
    for (int i = 0; i < 5564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5565(int& val) {
    func_in_5565(val);
    const int x = 5565;
    std::cout << 5565;
    for (int i = 0; i < 5565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5565(int& val) {
    const int x = 5565;
    std::cout << 5565;
    for (int i = 0; i < 5565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5566(int& val) {
    func_in_5566(val);
    const int x = 5566;
    std::cout << 5566;
    for (int i = 0; i < 5566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5566(int& val) {
    const int x = 5566;
    std::cout << 5566;
    for (int i = 0; i < 5566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5567(int& val) {
    func_in_5567(val);
    const int x = 5567;
    std::cout << 5567;
    for (int i = 0; i < 5567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5567(int& val) {
    const int x = 5567;
    std::cout << 5567;
    for (int i = 0; i < 5567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5568(int& val) {
    func_in_5568(val);
    const int x = 5568;
    std::cout << 5568;
    for (int i = 0; i < 5568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5568(int& val) {
    const int x = 5568;
    std::cout << 5568;
    for (int i = 0; i < 5568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5569(int& val) {
    func_in_5569(val);
    const int x = 5569;
    std::cout << 5569;
    for (int i = 0; i < 5569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5569(int& val) {
    const int x = 5569;
    std::cout << 5569;
    for (int i = 0; i < 5569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5570(int& val) {
    func_in_5570(val);
    const int x = 5570;
    std::cout << 5570;
    for (int i = 0; i < 5570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5570(int& val) {
    const int x = 5570;
    std::cout << 5570;
    for (int i = 0; i < 5570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5571(int& val) {
    func_in_5571(val);
    const int x = 5571;
    std::cout << 5571;
    for (int i = 0; i < 5571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5571(int& val) {
    const int x = 5571;
    std::cout << 5571;
    for (int i = 0; i < 5571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5572(int& val) {
    func_in_5572(val);
    const int x = 5572;
    std::cout << 5572;
    for (int i = 0; i < 5572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5572(int& val) {
    const int x = 5572;
    std::cout << 5572;
    for (int i = 0; i < 5572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5573(int& val) {
    func_in_5573(val);
    const int x = 5573;
    std::cout << 5573;
    for (int i = 0; i < 5573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5573(int& val) {
    const int x = 5573;
    std::cout << 5573;
    for (int i = 0; i < 5573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5574(int& val) {
    func_in_5574(val);
    const int x = 5574;
    std::cout << 5574;
    for (int i = 0; i < 5574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5574(int& val) {
    const int x = 5574;
    std::cout << 5574;
    for (int i = 0; i < 5574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5575(int& val) {
    func_in_5575(val);
    const int x = 5575;
    std::cout << 5575;
    for (int i = 0; i < 5575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5575(int& val) {
    const int x = 5575;
    std::cout << 5575;
    for (int i = 0; i < 5575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5576(int& val) {
    func_in_5576(val);
    const int x = 5576;
    std::cout << 5576;
    for (int i = 0; i < 5576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5576(int& val) {
    const int x = 5576;
    std::cout << 5576;
    for (int i = 0; i < 5576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5577(int& val) {
    func_in_5577(val);
    const int x = 5577;
    std::cout << 5577;
    for (int i = 0; i < 5577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5577(int& val) {
    const int x = 5577;
    std::cout << 5577;
    for (int i = 0; i < 5577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5578(int& val) {
    func_in_5578(val);
    const int x = 5578;
    std::cout << 5578;
    for (int i = 0; i < 5578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5578(int& val) {
    const int x = 5578;
    std::cout << 5578;
    for (int i = 0; i < 5578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5579(int& val) {
    func_in_5579(val);
    const int x = 5579;
    std::cout << 5579;
    for (int i = 0; i < 5579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5579(int& val) {
    const int x = 5579;
    std::cout << 5579;
    for (int i = 0; i < 5579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5580(int& val) {
    func_in_5580(val);
    const int x = 5580;
    std::cout << 5580;
    for (int i = 0; i < 5580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5580(int& val) {
    const int x = 5580;
    std::cout << 5580;
    for (int i = 0; i < 5580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5581(int& val) {
    func_in_5581(val);
    const int x = 5581;
    std::cout << 5581;
    for (int i = 0; i < 5581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5581(int& val) {
    const int x = 5581;
    std::cout << 5581;
    for (int i = 0; i < 5581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5582(int& val) {
    func_in_5582(val);
    const int x = 5582;
    std::cout << 5582;
    for (int i = 0; i < 5582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5582(int& val) {
    const int x = 5582;
    std::cout << 5582;
    for (int i = 0; i < 5582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5583(int& val) {
    func_in_5583(val);
    const int x = 5583;
    std::cout << 5583;
    for (int i = 0; i < 5583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5583(int& val) {
    const int x = 5583;
    std::cout << 5583;
    for (int i = 0; i < 5583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5584(int& val) {
    func_in_5584(val);
    const int x = 5584;
    std::cout << 5584;
    for (int i = 0; i < 5584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5584(int& val) {
    const int x = 5584;
    std::cout << 5584;
    for (int i = 0; i < 5584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5585(int& val) {
    func_in_5585(val);
    const int x = 5585;
    std::cout << 5585;
    for (int i = 0; i < 5585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5585(int& val) {
    const int x = 5585;
    std::cout << 5585;
    for (int i = 0; i < 5585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5586(int& val) {
    func_in_5586(val);
    const int x = 5586;
    std::cout << 5586;
    for (int i = 0; i < 5586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5586(int& val) {
    const int x = 5586;
    std::cout << 5586;
    for (int i = 0; i < 5586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5587(int& val) {
    func_in_5587(val);
    const int x = 5587;
    std::cout << 5587;
    for (int i = 0; i < 5587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5587(int& val) {
    const int x = 5587;
    std::cout << 5587;
    for (int i = 0; i < 5587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5588(int& val) {
    func_in_5588(val);
    const int x = 5588;
    std::cout << 5588;
    for (int i = 0; i < 5588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5588(int& val) {
    const int x = 5588;
    std::cout << 5588;
    for (int i = 0; i < 5588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5589(int& val) {
    func_in_5589(val);
    const int x = 5589;
    std::cout << 5589;
    for (int i = 0; i < 5589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5589(int& val) {
    const int x = 5589;
    std::cout << 5589;
    for (int i = 0; i < 5589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5590(int& val) {
    func_in_5590(val);
    const int x = 5590;
    std::cout << 5590;
    for (int i = 0; i < 5590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5590(int& val) {
    const int x = 5590;
    std::cout << 5590;
    for (int i = 0; i < 5590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5591(int& val) {
    func_in_5591(val);
    const int x = 5591;
    std::cout << 5591;
    for (int i = 0; i < 5591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5591(int& val) {
    const int x = 5591;
    std::cout << 5591;
    for (int i = 0; i < 5591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5592(int& val) {
    func_in_5592(val);
    const int x = 5592;
    std::cout << 5592;
    for (int i = 0; i < 5592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5592(int& val) {
    const int x = 5592;
    std::cout << 5592;
    for (int i = 0; i < 5592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5593(int& val) {
    func_in_5593(val);
    const int x = 5593;
    std::cout << 5593;
    for (int i = 0; i < 5593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5593(int& val) {
    const int x = 5593;
    std::cout << 5593;
    for (int i = 0; i < 5593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5594(int& val) {
    func_in_5594(val);
    const int x = 5594;
    std::cout << 5594;
    for (int i = 0; i < 5594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5594(int& val) {
    const int x = 5594;
    std::cout << 5594;
    for (int i = 0; i < 5594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5595(int& val) {
    func_in_5595(val);
    const int x = 5595;
    std::cout << 5595;
    for (int i = 0; i < 5595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5595(int& val) {
    const int x = 5595;
    std::cout << 5595;
    for (int i = 0; i < 5595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5596(int& val) {
    func_in_5596(val);
    const int x = 5596;
    std::cout << 5596;
    for (int i = 0; i < 5596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5596(int& val) {
    const int x = 5596;
    std::cout << 5596;
    for (int i = 0; i < 5596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5597(int& val) {
    func_in_5597(val);
    const int x = 5597;
    std::cout << 5597;
    for (int i = 0; i < 5597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5597(int& val) {
    const int x = 5597;
    std::cout << 5597;
    for (int i = 0; i < 5597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5598(int& val) {
    func_in_5598(val);
    const int x = 5598;
    std::cout << 5598;
    for (int i = 0; i < 5598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5598(int& val) {
    const int x = 5598;
    std::cout << 5598;
    for (int i = 0; i < 5598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5599(int& val) {
    func_in_5599(val);
    const int x = 5599;
    std::cout << 5599;
    for (int i = 0; i < 5599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5599(int& val) {
    const int x = 5599;
    std::cout << 5599;
    for (int i = 0; i < 5599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5600(int& val) {
    func_in_5600(val);
    const int x = 5600;
    std::cout << 5600;
    for (int i = 0; i < 5600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5600(int& val) {
    const int x = 5600;
    std::cout << 5600;
    for (int i = 0; i < 5600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5601(int& val) {
    func_in_5601(val);
    const int x = 5601;
    std::cout << 5601;
    for (int i = 0; i < 5601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5601(int& val) {
    const int x = 5601;
    std::cout << 5601;
    for (int i = 0; i < 5601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5602(int& val) {
    func_in_5602(val);
    const int x = 5602;
    std::cout << 5602;
    for (int i = 0; i < 5602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5602(int& val) {
    const int x = 5602;
    std::cout << 5602;
    for (int i = 0; i < 5602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5603(int& val) {
    func_in_5603(val);
    const int x = 5603;
    std::cout << 5603;
    for (int i = 0; i < 5603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5603(int& val) {
    const int x = 5603;
    std::cout << 5603;
    for (int i = 0; i < 5603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5604(int& val) {
    func_in_5604(val);
    const int x = 5604;
    std::cout << 5604;
    for (int i = 0; i < 5604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5604(int& val) {
    const int x = 5604;
    std::cout << 5604;
    for (int i = 0; i < 5604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5605(int& val) {
    func_in_5605(val);
    const int x = 5605;
    std::cout << 5605;
    for (int i = 0; i < 5605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5605(int& val) {
    const int x = 5605;
    std::cout << 5605;
    for (int i = 0; i < 5605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5606(int& val) {
    func_in_5606(val);
    const int x = 5606;
    std::cout << 5606;
    for (int i = 0; i < 5606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5606(int& val) {
    const int x = 5606;
    std::cout << 5606;
    for (int i = 0; i < 5606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5607(int& val) {
    func_in_5607(val);
    const int x = 5607;
    std::cout << 5607;
    for (int i = 0; i < 5607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5607(int& val) {
    const int x = 5607;
    std::cout << 5607;
    for (int i = 0; i < 5607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5608(int& val) {
    func_in_5608(val);
    const int x = 5608;
    std::cout << 5608;
    for (int i = 0; i < 5608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5608(int& val) {
    const int x = 5608;
    std::cout << 5608;
    for (int i = 0; i < 5608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5609(int& val) {
    func_in_5609(val);
    const int x = 5609;
    std::cout << 5609;
    for (int i = 0; i < 5609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5609(int& val) {
    const int x = 5609;
    std::cout << 5609;
    for (int i = 0; i < 5609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5610(int& val) {
    func_in_5610(val);
    const int x = 5610;
    std::cout << 5610;
    for (int i = 0; i < 5610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5610(int& val) {
    const int x = 5610;
    std::cout << 5610;
    for (int i = 0; i < 5610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5611(int& val) {
    func_in_5611(val);
    const int x = 5611;
    std::cout << 5611;
    for (int i = 0; i < 5611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5611(int& val) {
    const int x = 5611;
    std::cout << 5611;
    for (int i = 0; i < 5611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5612(int& val) {
    func_in_5612(val);
    const int x = 5612;
    std::cout << 5612;
    for (int i = 0; i < 5612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5612(int& val) {
    const int x = 5612;
    std::cout << 5612;
    for (int i = 0; i < 5612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5613(int& val) {
    func_in_5613(val);
    const int x = 5613;
    std::cout << 5613;
    for (int i = 0; i < 5613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5613(int& val) {
    const int x = 5613;
    std::cout << 5613;
    for (int i = 0; i < 5613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5614(int& val) {
    func_in_5614(val);
    const int x = 5614;
    std::cout << 5614;
    for (int i = 0; i < 5614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5614(int& val) {
    const int x = 5614;
    std::cout << 5614;
    for (int i = 0; i < 5614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5615(int& val) {
    func_in_5615(val);
    const int x = 5615;
    std::cout << 5615;
    for (int i = 0; i < 5615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5615(int& val) {
    const int x = 5615;
    std::cout << 5615;
    for (int i = 0; i < 5615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5616(int& val) {
    func_in_5616(val);
    const int x = 5616;
    std::cout << 5616;
    for (int i = 0; i < 5616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5616(int& val) {
    const int x = 5616;
    std::cout << 5616;
    for (int i = 0; i < 5616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5617(int& val) {
    func_in_5617(val);
    const int x = 5617;
    std::cout << 5617;
    for (int i = 0; i < 5617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5617(int& val) {
    const int x = 5617;
    std::cout << 5617;
    for (int i = 0; i < 5617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5618(int& val) {
    func_in_5618(val);
    const int x = 5618;
    std::cout << 5618;
    for (int i = 0; i < 5618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5618(int& val) {
    const int x = 5618;
    std::cout << 5618;
    for (int i = 0; i < 5618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5619(int& val) {
    func_in_5619(val);
    const int x = 5619;
    std::cout << 5619;
    for (int i = 0; i < 5619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5619(int& val) {
    const int x = 5619;
    std::cout << 5619;
    for (int i = 0; i < 5619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5620(int& val) {
    func_in_5620(val);
    const int x = 5620;
    std::cout << 5620;
    for (int i = 0; i < 5620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5620(int& val) {
    const int x = 5620;
    std::cout << 5620;
    for (int i = 0; i < 5620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5621(int& val) {
    func_in_5621(val);
    const int x = 5621;
    std::cout << 5621;
    for (int i = 0; i < 5621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5621(int& val) {
    const int x = 5621;
    std::cout << 5621;
    for (int i = 0; i < 5621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5622(int& val) {
    func_in_5622(val);
    const int x = 5622;
    std::cout << 5622;
    for (int i = 0; i < 5622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5622(int& val) {
    const int x = 5622;
    std::cout << 5622;
    for (int i = 0; i < 5622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5623(int& val) {
    func_in_5623(val);
    const int x = 5623;
    std::cout << 5623;
    for (int i = 0; i < 5623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5623(int& val) {
    const int x = 5623;
    std::cout << 5623;
    for (int i = 0; i < 5623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5624(int& val) {
    func_in_5624(val);
    const int x = 5624;
    std::cout << 5624;
    for (int i = 0; i < 5624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5624(int& val) {
    const int x = 5624;
    std::cout << 5624;
    for (int i = 0; i < 5624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5625(int& val) {
    func_in_5625(val);
    const int x = 5625;
    std::cout << 5625;
    for (int i = 0; i < 5625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5625(int& val) {
    const int x = 5625;
    std::cout << 5625;
    for (int i = 0; i < 5625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5626(int& val) {
    func_in_5626(val);
    const int x = 5626;
    std::cout << 5626;
    for (int i = 0; i < 5626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5626(int& val) {
    const int x = 5626;
    std::cout << 5626;
    for (int i = 0; i < 5626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5627(int& val) {
    func_in_5627(val);
    const int x = 5627;
    std::cout << 5627;
    for (int i = 0; i < 5627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5627(int& val) {
    const int x = 5627;
    std::cout << 5627;
    for (int i = 0; i < 5627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5628(int& val) {
    func_in_5628(val);
    const int x = 5628;
    std::cout << 5628;
    for (int i = 0; i < 5628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5628(int& val) {
    const int x = 5628;
    std::cout << 5628;
    for (int i = 0; i < 5628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5629(int& val) {
    func_in_5629(val);
    const int x = 5629;
    std::cout << 5629;
    for (int i = 0; i < 5629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5629(int& val) {
    const int x = 5629;
    std::cout << 5629;
    for (int i = 0; i < 5629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5630(int& val) {
    func_in_5630(val);
    const int x = 5630;
    std::cout << 5630;
    for (int i = 0; i < 5630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5630(int& val) {
    const int x = 5630;
    std::cout << 5630;
    for (int i = 0; i < 5630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5631(int& val) {
    func_in_5631(val);
    const int x = 5631;
    std::cout << 5631;
    for (int i = 0; i < 5631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5631(int& val) {
    const int x = 5631;
    std::cout << 5631;
    for (int i = 0; i < 5631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5632(int& val) {
    func_in_5632(val);
    const int x = 5632;
    std::cout << 5632;
    for (int i = 0; i < 5632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5632(int& val) {
    const int x = 5632;
    std::cout << 5632;
    for (int i = 0; i < 5632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5633(int& val) {
    func_in_5633(val);
    const int x = 5633;
    std::cout << 5633;
    for (int i = 0; i < 5633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5633(int& val) {
    const int x = 5633;
    std::cout << 5633;
    for (int i = 0; i < 5633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5634(int& val) {
    func_in_5634(val);
    const int x = 5634;
    std::cout << 5634;
    for (int i = 0; i < 5634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5634(int& val) {
    const int x = 5634;
    std::cout << 5634;
    for (int i = 0; i < 5634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5635(int& val) {
    func_in_5635(val);
    const int x = 5635;
    std::cout << 5635;
    for (int i = 0; i < 5635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5635(int& val) {
    const int x = 5635;
    std::cout << 5635;
    for (int i = 0; i < 5635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5636(int& val) {
    func_in_5636(val);
    const int x = 5636;
    std::cout << 5636;
    for (int i = 0; i < 5636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5636(int& val) {
    const int x = 5636;
    std::cout << 5636;
    for (int i = 0; i < 5636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5637(int& val) {
    func_in_5637(val);
    const int x = 5637;
    std::cout << 5637;
    for (int i = 0; i < 5637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5637(int& val) {
    const int x = 5637;
    std::cout << 5637;
    for (int i = 0; i < 5637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5638(int& val) {
    func_in_5638(val);
    const int x = 5638;
    std::cout << 5638;
    for (int i = 0; i < 5638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5638(int& val) {
    const int x = 5638;
    std::cout << 5638;
    for (int i = 0; i < 5638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5639(int& val) {
    func_in_5639(val);
    const int x = 5639;
    std::cout << 5639;
    for (int i = 0; i < 5639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5639(int& val) {
    const int x = 5639;
    std::cout << 5639;
    for (int i = 0; i < 5639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5640(int& val) {
    func_in_5640(val);
    const int x = 5640;
    std::cout << 5640;
    for (int i = 0; i < 5640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5640(int& val) {
    const int x = 5640;
    std::cout << 5640;
    for (int i = 0; i < 5640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5641(int& val) {
    func_in_5641(val);
    const int x = 5641;
    std::cout << 5641;
    for (int i = 0; i < 5641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5641(int& val) {
    const int x = 5641;
    std::cout << 5641;
    for (int i = 0; i < 5641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5642(int& val) {
    func_in_5642(val);
    const int x = 5642;
    std::cout << 5642;
    for (int i = 0; i < 5642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5642(int& val) {
    const int x = 5642;
    std::cout << 5642;
    for (int i = 0; i < 5642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5643(int& val) {
    func_in_5643(val);
    const int x = 5643;
    std::cout << 5643;
    for (int i = 0; i < 5643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5643(int& val) {
    const int x = 5643;
    std::cout << 5643;
    for (int i = 0; i < 5643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5644(int& val) {
    func_in_5644(val);
    const int x = 5644;
    std::cout << 5644;
    for (int i = 0; i < 5644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5644(int& val) {
    const int x = 5644;
    std::cout << 5644;
    for (int i = 0; i < 5644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5645(int& val) {
    func_in_5645(val);
    const int x = 5645;
    std::cout << 5645;
    for (int i = 0; i < 5645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5645(int& val) {
    const int x = 5645;
    std::cout << 5645;
    for (int i = 0; i < 5645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5646(int& val) {
    func_in_5646(val);
    const int x = 5646;
    std::cout << 5646;
    for (int i = 0; i < 5646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5646(int& val) {
    const int x = 5646;
    std::cout << 5646;
    for (int i = 0; i < 5646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5647(int& val) {
    func_in_5647(val);
    const int x = 5647;
    std::cout << 5647;
    for (int i = 0; i < 5647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5647(int& val) {
    const int x = 5647;
    std::cout << 5647;
    for (int i = 0; i < 5647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5648(int& val) {
    func_in_5648(val);
    const int x = 5648;
    std::cout << 5648;
    for (int i = 0; i < 5648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5648(int& val) {
    const int x = 5648;
    std::cout << 5648;
    for (int i = 0; i < 5648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5649(int& val) {
    func_in_5649(val);
    const int x = 5649;
    std::cout << 5649;
    for (int i = 0; i < 5649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5649(int& val) {
    const int x = 5649;
    std::cout << 5649;
    for (int i = 0; i < 5649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5650(int& val) {
    func_in_5650(val);
    const int x = 5650;
    std::cout << 5650;
    for (int i = 0; i < 5650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5650(int& val) {
    const int x = 5650;
    std::cout << 5650;
    for (int i = 0; i < 5650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5651(int& val) {
    func_in_5651(val);
    const int x = 5651;
    std::cout << 5651;
    for (int i = 0; i < 5651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5651(int& val) {
    const int x = 5651;
    std::cout << 5651;
    for (int i = 0; i < 5651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5652(int& val) {
    func_in_5652(val);
    const int x = 5652;
    std::cout << 5652;
    for (int i = 0; i < 5652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5652(int& val) {
    const int x = 5652;
    std::cout << 5652;
    for (int i = 0; i < 5652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5653(int& val) {
    func_in_5653(val);
    const int x = 5653;
    std::cout << 5653;
    for (int i = 0; i < 5653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5653(int& val) {
    const int x = 5653;
    std::cout << 5653;
    for (int i = 0; i < 5653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5654(int& val) {
    func_in_5654(val);
    const int x = 5654;
    std::cout << 5654;
    for (int i = 0; i < 5654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5654(int& val) {
    const int x = 5654;
    std::cout << 5654;
    for (int i = 0; i < 5654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5655(int& val) {
    func_in_5655(val);
    const int x = 5655;
    std::cout << 5655;
    for (int i = 0; i < 5655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5655(int& val) {
    const int x = 5655;
    std::cout << 5655;
    for (int i = 0; i < 5655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5656(int& val) {
    func_in_5656(val);
    const int x = 5656;
    std::cout << 5656;
    for (int i = 0; i < 5656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5656(int& val) {
    const int x = 5656;
    std::cout << 5656;
    for (int i = 0; i < 5656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5657(int& val) {
    func_in_5657(val);
    const int x = 5657;
    std::cout << 5657;
    for (int i = 0; i < 5657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5657(int& val) {
    const int x = 5657;
    std::cout << 5657;
    for (int i = 0; i < 5657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5658(int& val) {
    func_in_5658(val);
    const int x = 5658;
    std::cout << 5658;
    for (int i = 0; i < 5658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5658(int& val) {
    const int x = 5658;
    std::cout << 5658;
    for (int i = 0; i < 5658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5659(int& val) {
    func_in_5659(val);
    const int x = 5659;
    std::cout << 5659;
    for (int i = 0; i < 5659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5659(int& val) {
    const int x = 5659;
    std::cout << 5659;
    for (int i = 0; i < 5659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5660(int& val) {
    func_in_5660(val);
    const int x = 5660;
    std::cout << 5660;
    for (int i = 0; i < 5660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5660(int& val) {
    const int x = 5660;
    std::cout << 5660;
    for (int i = 0; i < 5660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5661(int& val) {
    func_in_5661(val);
    const int x = 5661;
    std::cout << 5661;
    for (int i = 0; i < 5661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5661(int& val) {
    const int x = 5661;
    std::cout << 5661;
    for (int i = 0; i < 5661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5662(int& val) {
    func_in_5662(val);
    const int x = 5662;
    std::cout << 5662;
    for (int i = 0; i < 5662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5662(int& val) {
    const int x = 5662;
    std::cout << 5662;
    for (int i = 0; i < 5662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5663(int& val) {
    func_in_5663(val);
    const int x = 5663;
    std::cout << 5663;
    for (int i = 0; i < 5663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5663(int& val) {
    const int x = 5663;
    std::cout << 5663;
    for (int i = 0; i < 5663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5664(int& val) {
    func_in_5664(val);
    const int x = 5664;
    std::cout << 5664;
    for (int i = 0; i < 5664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5664(int& val) {
    const int x = 5664;
    std::cout << 5664;
    for (int i = 0; i < 5664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5665(int& val) {
    func_in_5665(val);
    const int x = 5665;
    std::cout << 5665;
    for (int i = 0; i < 5665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5665(int& val) {
    const int x = 5665;
    std::cout << 5665;
    for (int i = 0; i < 5665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5666(int& val) {
    func_in_5666(val);
    const int x = 5666;
    std::cout << 5666;
    for (int i = 0; i < 5666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5666(int& val) {
    const int x = 5666;
    std::cout << 5666;
    for (int i = 0; i < 5666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5667(int& val) {
    func_in_5667(val);
    const int x = 5667;
    std::cout << 5667;
    for (int i = 0; i < 5667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5667(int& val) {
    const int x = 5667;
    std::cout << 5667;
    for (int i = 0; i < 5667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5668(int& val) {
    func_in_5668(val);
    const int x = 5668;
    std::cout << 5668;
    for (int i = 0; i < 5668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5668(int& val) {
    const int x = 5668;
    std::cout << 5668;
    for (int i = 0; i < 5668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5669(int& val) {
    func_in_5669(val);
    const int x = 5669;
    std::cout << 5669;
    for (int i = 0; i < 5669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5669(int& val) {
    const int x = 5669;
    std::cout << 5669;
    for (int i = 0; i < 5669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5670(int& val) {
    func_in_5670(val);
    const int x = 5670;
    std::cout << 5670;
    for (int i = 0; i < 5670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5670(int& val) {
    const int x = 5670;
    std::cout << 5670;
    for (int i = 0; i < 5670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5671(int& val) {
    func_in_5671(val);
    const int x = 5671;
    std::cout << 5671;
    for (int i = 0; i < 5671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5671(int& val) {
    const int x = 5671;
    std::cout << 5671;
    for (int i = 0; i < 5671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5672(int& val) {
    func_in_5672(val);
    const int x = 5672;
    std::cout << 5672;
    for (int i = 0; i < 5672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5672(int& val) {
    const int x = 5672;
    std::cout << 5672;
    for (int i = 0; i < 5672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5673(int& val) {
    func_in_5673(val);
    const int x = 5673;
    std::cout << 5673;
    for (int i = 0; i < 5673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5673(int& val) {
    const int x = 5673;
    std::cout << 5673;
    for (int i = 0; i < 5673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5674(int& val) {
    func_in_5674(val);
    const int x = 5674;
    std::cout << 5674;
    for (int i = 0; i < 5674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5674(int& val) {
    const int x = 5674;
    std::cout << 5674;
    for (int i = 0; i < 5674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5675(int& val) {
    func_in_5675(val);
    const int x = 5675;
    std::cout << 5675;
    for (int i = 0; i < 5675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5675(int& val) {
    const int x = 5675;
    std::cout << 5675;
    for (int i = 0; i < 5675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5676(int& val) {
    func_in_5676(val);
    const int x = 5676;
    std::cout << 5676;
    for (int i = 0; i < 5676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5676(int& val) {
    const int x = 5676;
    std::cout << 5676;
    for (int i = 0; i < 5676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5677(int& val) {
    func_in_5677(val);
    const int x = 5677;
    std::cout << 5677;
    for (int i = 0; i < 5677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5677(int& val) {
    const int x = 5677;
    std::cout << 5677;
    for (int i = 0; i < 5677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5678(int& val) {
    func_in_5678(val);
    const int x = 5678;
    std::cout << 5678;
    for (int i = 0; i < 5678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5678(int& val) {
    const int x = 5678;
    std::cout << 5678;
    for (int i = 0; i < 5678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5679(int& val) {
    func_in_5679(val);
    const int x = 5679;
    std::cout << 5679;
    for (int i = 0; i < 5679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5679(int& val) {
    const int x = 5679;
    std::cout << 5679;
    for (int i = 0; i < 5679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5680(int& val) {
    func_in_5680(val);
    const int x = 5680;
    std::cout << 5680;
    for (int i = 0; i < 5680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5680(int& val) {
    const int x = 5680;
    std::cout << 5680;
    for (int i = 0; i < 5680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5681(int& val) {
    func_in_5681(val);
    const int x = 5681;
    std::cout << 5681;
    for (int i = 0; i < 5681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5681(int& val) {
    const int x = 5681;
    std::cout << 5681;
    for (int i = 0; i < 5681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5682(int& val) {
    func_in_5682(val);
    const int x = 5682;
    std::cout << 5682;
    for (int i = 0; i < 5682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5682(int& val) {
    const int x = 5682;
    std::cout << 5682;
    for (int i = 0; i < 5682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5683(int& val) {
    func_in_5683(val);
    const int x = 5683;
    std::cout << 5683;
    for (int i = 0; i < 5683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5683(int& val) {
    const int x = 5683;
    std::cout << 5683;
    for (int i = 0; i < 5683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5684(int& val) {
    func_in_5684(val);
    const int x = 5684;
    std::cout << 5684;
    for (int i = 0; i < 5684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5684(int& val) {
    const int x = 5684;
    std::cout << 5684;
    for (int i = 0; i < 5684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5685(int& val) {
    func_in_5685(val);
    const int x = 5685;
    std::cout << 5685;
    for (int i = 0; i < 5685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5685(int& val) {
    const int x = 5685;
    std::cout << 5685;
    for (int i = 0; i < 5685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5686(int& val) {
    func_in_5686(val);
    const int x = 5686;
    std::cout << 5686;
    for (int i = 0; i < 5686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5686(int& val) {
    const int x = 5686;
    std::cout << 5686;
    for (int i = 0; i < 5686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5687(int& val) {
    func_in_5687(val);
    const int x = 5687;
    std::cout << 5687;
    for (int i = 0; i < 5687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5687(int& val) {
    const int x = 5687;
    std::cout << 5687;
    for (int i = 0; i < 5687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5688(int& val) {
    func_in_5688(val);
    const int x = 5688;
    std::cout << 5688;
    for (int i = 0; i < 5688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5688(int& val) {
    const int x = 5688;
    std::cout << 5688;
    for (int i = 0; i < 5688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5689(int& val) {
    func_in_5689(val);
    const int x = 5689;
    std::cout << 5689;
    for (int i = 0; i < 5689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5689(int& val) {
    const int x = 5689;
    std::cout << 5689;
    for (int i = 0; i < 5689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5690(int& val) {
    func_in_5690(val);
    const int x = 5690;
    std::cout << 5690;
    for (int i = 0; i < 5690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5690(int& val) {
    const int x = 5690;
    std::cout << 5690;
    for (int i = 0; i < 5690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5691(int& val) {
    func_in_5691(val);
    const int x = 5691;
    std::cout << 5691;
    for (int i = 0; i < 5691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5691(int& val) {
    const int x = 5691;
    std::cout << 5691;
    for (int i = 0; i < 5691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5692(int& val) {
    func_in_5692(val);
    const int x = 5692;
    std::cout << 5692;
    for (int i = 0; i < 5692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5692(int& val) {
    const int x = 5692;
    std::cout << 5692;
    for (int i = 0; i < 5692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5693(int& val) {
    func_in_5693(val);
    const int x = 5693;
    std::cout << 5693;
    for (int i = 0; i < 5693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5693(int& val) {
    const int x = 5693;
    std::cout << 5693;
    for (int i = 0; i < 5693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5694(int& val) {
    func_in_5694(val);
    const int x = 5694;
    std::cout << 5694;
    for (int i = 0; i < 5694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5694(int& val) {
    const int x = 5694;
    std::cout << 5694;
    for (int i = 0; i < 5694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5695(int& val) {
    func_in_5695(val);
    const int x = 5695;
    std::cout << 5695;
    for (int i = 0; i < 5695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5695(int& val) {
    const int x = 5695;
    std::cout << 5695;
    for (int i = 0; i < 5695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5696(int& val) {
    func_in_5696(val);
    const int x = 5696;
    std::cout << 5696;
    for (int i = 0; i < 5696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5696(int& val) {
    const int x = 5696;
    std::cout << 5696;
    for (int i = 0; i < 5696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5697(int& val) {
    func_in_5697(val);
    const int x = 5697;
    std::cout << 5697;
    for (int i = 0; i < 5697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5697(int& val) {
    const int x = 5697;
    std::cout << 5697;
    for (int i = 0; i < 5697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5698(int& val) {
    func_in_5698(val);
    const int x = 5698;
    std::cout << 5698;
    for (int i = 0; i < 5698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5698(int& val) {
    const int x = 5698;
    std::cout << 5698;
    for (int i = 0; i < 5698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5699(int& val) {
    func_in_5699(val);
    const int x = 5699;
    std::cout << 5699;
    for (int i = 0; i < 5699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5699(int& val) {
    const int x = 5699;
    std::cout << 5699;
    for (int i = 0; i < 5699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5700(int& val) {
    func_in_5700(val);
    const int x = 5700;
    std::cout << 5700;
    for (int i = 0; i < 5700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5700(int& val) {
    const int x = 5700;
    std::cout << 5700;
    for (int i = 0; i < 5700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5701(int& val) {
    func_in_5701(val);
    const int x = 5701;
    std::cout << 5701;
    for (int i = 0; i < 5701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5701(int& val) {
    const int x = 5701;
    std::cout << 5701;
    for (int i = 0; i < 5701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5702(int& val) {
    func_in_5702(val);
    const int x = 5702;
    std::cout << 5702;
    for (int i = 0; i < 5702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5702(int& val) {
    const int x = 5702;
    std::cout << 5702;
    for (int i = 0; i < 5702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5703(int& val) {
    func_in_5703(val);
    const int x = 5703;
    std::cout << 5703;
    for (int i = 0; i < 5703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5703(int& val) {
    const int x = 5703;
    std::cout << 5703;
    for (int i = 0; i < 5703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5704(int& val) {
    func_in_5704(val);
    const int x = 5704;
    std::cout << 5704;
    for (int i = 0; i < 5704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5704(int& val) {
    const int x = 5704;
    std::cout << 5704;
    for (int i = 0; i < 5704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5705(int& val) {
    func_in_5705(val);
    const int x = 5705;
    std::cout << 5705;
    for (int i = 0; i < 5705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5705(int& val) {
    const int x = 5705;
    std::cout << 5705;
    for (int i = 0; i < 5705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5706(int& val) {
    func_in_5706(val);
    const int x = 5706;
    std::cout << 5706;
    for (int i = 0; i < 5706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5706(int& val) {
    const int x = 5706;
    std::cout << 5706;
    for (int i = 0; i < 5706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5707(int& val) {
    func_in_5707(val);
    const int x = 5707;
    std::cout << 5707;
    for (int i = 0; i < 5707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5707(int& val) {
    const int x = 5707;
    std::cout << 5707;
    for (int i = 0; i < 5707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5708(int& val) {
    func_in_5708(val);
    const int x = 5708;
    std::cout << 5708;
    for (int i = 0; i < 5708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5708(int& val) {
    const int x = 5708;
    std::cout << 5708;
    for (int i = 0; i < 5708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5709(int& val) {
    func_in_5709(val);
    const int x = 5709;
    std::cout << 5709;
    for (int i = 0; i < 5709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5709(int& val) {
    const int x = 5709;
    std::cout << 5709;
    for (int i = 0; i < 5709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5710(int& val) {
    func_in_5710(val);
    const int x = 5710;
    std::cout << 5710;
    for (int i = 0; i < 5710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5710(int& val) {
    const int x = 5710;
    std::cout << 5710;
    for (int i = 0; i < 5710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5711(int& val) {
    func_in_5711(val);
    const int x = 5711;
    std::cout << 5711;
    for (int i = 0; i < 5711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5711(int& val) {
    const int x = 5711;
    std::cout << 5711;
    for (int i = 0; i < 5711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5712(int& val) {
    func_in_5712(val);
    const int x = 5712;
    std::cout << 5712;
    for (int i = 0; i < 5712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5712(int& val) {
    const int x = 5712;
    std::cout << 5712;
    for (int i = 0; i < 5712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5713(int& val) {
    func_in_5713(val);
    const int x = 5713;
    std::cout << 5713;
    for (int i = 0; i < 5713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5713(int& val) {
    const int x = 5713;
    std::cout << 5713;
    for (int i = 0; i < 5713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5714(int& val) {
    func_in_5714(val);
    const int x = 5714;
    std::cout << 5714;
    for (int i = 0; i < 5714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5714(int& val) {
    const int x = 5714;
    std::cout << 5714;
    for (int i = 0; i < 5714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5715(int& val) {
    func_in_5715(val);
    const int x = 5715;
    std::cout << 5715;
    for (int i = 0; i < 5715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5715(int& val) {
    const int x = 5715;
    std::cout << 5715;
    for (int i = 0; i < 5715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5716(int& val) {
    func_in_5716(val);
    const int x = 5716;
    std::cout << 5716;
    for (int i = 0; i < 5716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5716(int& val) {
    const int x = 5716;
    std::cout << 5716;
    for (int i = 0; i < 5716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5717(int& val) {
    func_in_5717(val);
    const int x = 5717;
    std::cout << 5717;
    for (int i = 0; i < 5717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5717(int& val) {
    const int x = 5717;
    std::cout << 5717;
    for (int i = 0; i < 5717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5718(int& val) {
    func_in_5718(val);
    const int x = 5718;
    std::cout << 5718;
    for (int i = 0; i < 5718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5718(int& val) {
    const int x = 5718;
    std::cout << 5718;
    for (int i = 0; i < 5718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5719(int& val) {
    func_in_5719(val);
    const int x = 5719;
    std::cout << 5719;
    for (int i = 0; i < 5719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5719(int& val) {
    const int x = 5719;
    std::cout << 5719;
    for (int i = 0; i < 5719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5720(int& val) {
    func_in_5720(val);
    const int x = 5720;
    std::cout << 5720;
    for (int i = 0; i < 5720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5720(int& val) {
    const int x = 5720;
    std::cout << 5720;
    for (int i = 0; i < 5720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5721(int& val) {
    func_in_5721(val);
    const int x = 5721;
    std::cout << 5721;
    for (int i = 0; i < 5721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5721(int& val) {
    const int x = 5721;
    std::cout << 5721;
    for (int i = 0; i < 5721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5722(int& val) {
    func_in_5722(val);
    const int x = 5722;
    std::cout << 5722;
    for (int i = 0; i < 5722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5722(int& val) {
    const int x = 5722;
    std::cout << 5722;
    for (int i = 0; i < 5722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5723(int& val) {
    func_in_5723(val);
    const int x = 5723;
    std::cout << 5723;
    for (int i = 0; i < 5723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5723(int& val) {
    const int x = 5723;
    std::cout << 5723;
    for (int i = 0; i < 5723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5724(int& val) {
    func_in_5724(val);
    const int x = 5724;
    std::cout << 5724;
    for (int i = 0; i < 5724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5724(int& val) {
    const int x = 5724;
    std::cout << 5724;
    for (int i = 0; i < 5724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5725(int& val) {
    func_in_5725(val);
    const int x = 5725;
    std::cout << 5725;
    for (int i = 0; i < 5725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5725(int& val) {
    const int x = 5725;
    std::cout << 5725;
    for (int i = 0; i < 5725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5726(int& val) {
    func_in_5726(val);
    const int x = 5726;
    std::cout << 5726;
    for (int i = 0; i < 5726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5726(int& val) {
    const int x = 5726;
    std::cout << 5726;
    for (int i = 0; i < 5726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5727(int& val) {
    func_in_5727(val);
    const int x = 5727;
    std::cout << 5727;
    for (int i = 0; i < 5727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5727(int& val) {
    const int x = 5727;
    std::cout << 5727;
    for (int i = 0; i < 5727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5728(int& val) {
    func_in_5728(val);
    const int x = 5728;
    std::cout << 5728;
    for (int i = 0; i < 5728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5728(int& val) {
    const int x = 5728;
    std::cout << 5728;
    for (int i = 0; i < 5728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5729(int& val) {
    func_in_5729(val);
    const int x = 5729;
    std::cout << 5729;
    for (int i = 0; i < 5729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5729(int& val) {
    const int x = 5729;
    std::cout << 5729;
    for (int i = 0; i < 5729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5730(int& val) {
    func_in_5730(val);
    const int x = 5730;
    std::cout << 5730;
    for (int i = 0; i < 5730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5730(int& val) {
    const int x = 5730;
    std::cout << 5730;
    for (int i = 0; i < 5730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5731(int& val) {
    func_in_5731(val);
    const int x = 5731;
    std::cout << 5731;
    for (int i = 0; i < 5731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5731(int& val) {
    const int x = 5731;
    std::cout << 5731;
    for (int i = 0; i < 5731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5732(int& val) {
    func_in_5732(val);
    const int x = 5732;
    std::cout << 5732;
    for (int i = 0; i < 5732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5732(int& val) {
    const int x = 5732;
    std::cout << 5732;
    for (int i = 0; i < 5732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5733(int& val) {
    func_in_5733(val);
    const int x = 5733;
    std::cout << 5733;
    for (int i = 0; i < 5733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5733(int& val) {
    const int x = 5733;
    std::cout << 5733;
    for (int i = 0; i < 5733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5734(int& val) {
    func_in_5734(val);
    const int x = 5734;
    std::cout << 5734;
    for (int i = 0; i < 5734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5734(int& val) {
    const int x = 5734;
    std::cout << 5734;
    for (int i = 0; i < 5734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5735(int& val) {
    func_in_5735(val);
    const int x = 5735;
    std::cout << 5735;
    for (int i = 0; i < 5735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5735(int& val) {
    const int x = 5735;
    std::cout << 5735;
    for (int i = 0; i < 5735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5736(int& val) {
    func_in_5736(val);
    const int x = 5736;
    std::cout << 5736;
    for (int i = 0; i < 5736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5736(int& val) {
    const int x = 5736;
    std::cout << 5736;
    for (int i = 0; i < 5736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5737(int& val) {
    func_in_5737(val);
    const int x = 5737;
    std::cout << 5737;
    for (int i = 0; i < 5737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5737(int& val) {
    const int x = 5737;
    std::cout << 5737;
    for (int i = 0; i < 5737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5738(int& val) {
    func_in_5738(val);
    const int x = 5738;
    std::cout << 5738;
    for (int i = 0; i < 5738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5738(int& val) {
    const int x = 5738;
    std::cout << 5738;
    for (int i = 0; i < 5738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5739(int& val) {
    func_in_5739(val);
    const int x = 5739;
    std::cout << 5739;
    for (int i = 0; i < 5739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5739(int& val) {
    const int x = 5739;
    std::cout << 5739;
    for (int i = 0; i < 5739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5740(int& val) {
    func_in_5740(val);
    const int x = 5740;
    std::cout << 5740;
    for (int i = 0; i < 5740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5740(int& val) {
    const int x = 5740;
    std::cout << 5740;
    for (int i = 0; i < 5740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5741(int& val) {
    func_in_5741(val);
    const int x = 5741;
    std::cout << 5741;
    for (int i = 0; i < 5741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5741(int& val) {
    const int x = 5741;
    std::cout << 5741;
    for (int i = 0; i < 5741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5742(int& val) {
    func_in_5742(val);
    const int x = 5742;
    std::cout << 5742;
    for (int i = 0; i < 5742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5742(int& val) {
    const int x = 5742;
    std::cout << 5742;
    for (int i = 0; i < 5742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5743(int& val) {
    func_in_5743(val);
    const int x = 5743;
    std::cout << 5743;
    for (int i = 0; i < 5743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5743(int& val) {
    const int x = 5743;
    std::cout << 5743;
    for (int i = 0; i < 5743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5744(int& val) {
    func_in_5744(val);
    const int x = 5744;
    std::cout << 5744;
    for (int i = 0; i < 5744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5744(int& val) {
    const int x = 5744;
    std::cout << 5744;
    for (int i = 0; i < 5744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5745(int& val) {
    func_in_5745(val);
    const int x = 5745;
    std::cout << 5745;
    for (int i = 0; i < 5745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5745(int& val) {
    const int x = 5745;
    std::cout << 5745;
    for (int i = 0; i < 5745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5746(int& val) {
    func_in_5746(val);
    const int x = 5746;
    std::cout << 5746;
    for (int i = 0; i < 5746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5746(int& val) {
    const int x = 5746;
    std::cout << 5746;
    for (int i = 0; i < 5746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5747(int& val) {
    func_in_5747(val);
    const int x = 5747;
    std::cout << 5747;
    for (int i = 0; i < 5747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5747(int& val) {
    const int x = 5747;
    std::cout << 5747;
    for (int i = 0; i < 5747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5748(int& val) {
    func_in_5748(val);
    const int x = 5748;
    std::cout << 5748;
    for (int i = 0; i < 5748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5748(int& val) {
    const int x = 5748;
    std::cout << 5748;
    for (int i = 0; i < 5748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5749(int& val) {
    func_in_5749(val);
    const int x = 5749;
    std::cout << 5749;
    for (int i = 0; i < 5749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5749(int& val) {
    const int x = 5749;
    std::cout << 5749;
    for (int i = 0; i < 5749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5750(int& val) {
    func_in_5750(val);
    const int x = 5750;
    std::cout << 5750;
    for (int i = 0; i < 5750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5750(int& val) {
    const int x = 5750;
    std::cout << 5750;
    for (int i = 0; i < 5750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5751(int& val) {
    func_in_5751(val);
    const int x = 5751;
    std::cout << 5751;
    for (int i = 0; i < 5751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5751(int& val) {
    const int x = 5751;
    std::cout << 5751;
    for (int i = 0; i < 5751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5752(int& val) {
    func_in_5752(val);
    const int x = 5752;
    std::cout << 5752;
    for (int i = 0; i < 5752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5752(int& val) {
    const int x = 5752;
    std::cout << 5752;
    for (int i = 0; i < 5752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5753(int& val) {
    func_in_5753(val);
    const int x = 5753;
    std::cout << 5753;
    for (int i = 0; i < 5753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5753(int& val) {
    const int x = 5753;
    std::cout << 5753;
    for (int i = 0; i < 5753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5754(int& val) {
    func_in_5754(val);
    const int x = 5754;
    std::cout << 5754;
    for (int i = 0; i < 5754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5754(int& val) {
    const int x = 5754;
    std::cout << 5754;
    for (int i = 0; i < 5754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5755(int& val) {
    func_in_5755(val);
    const int x = 5755;
    std::cout << 5755;
    for (int i = 0; i < 5755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5755(int& val) {
    const int x = 5755;
    std::cout << 5755;
    for (int i = 0; i < 5755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5756(int& val) {
    func_in_5756(val);
    const int x = 5756;
    std::cout << 5756;
    for (int i = 0; i < 5756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5756(int& val) {
    const int x = 5756;
    std::cout << 5756;
    for (int i = 0; i < 5756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5757(int& val) {
    func_in_5757(val);
    const int x = 5757;
    std::cout << 5757;
    for (int i = 0; i < 5757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5757(int& val) {
    const int x = 5757;
    std::cout << 5757;
    for (int i = 0; i < 5757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5758(int& val) {
    func_in_5758(val);
    const int x = 5758;
    std::cout << 5758;
    for (int i = 0; i < 5758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5758(int& val) {
    const int x = 5758;
    std::cout << 5758;
    for (int i = 0; i < 5758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5759(int& val) {
    func_in_5759(val);
    const int x = 5759;
    std::cout << 5759;
    for (int i = 0; i < 5759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5759(int& val) {
    const int x = 5759;
    std::cout << 5759;
    for (int i = 0; i < 5759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5760(int& val) {
    func_in_5760(val);
    const int x = 5760;
    std::cout << 5760;
    for (int i = 0; i < 5760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5760(int& val) {
    const int x = 5760;
    std::cout << 5760;
    for (int i = 0; i < 5760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5761(int& val) {
    func_in_5761(val);
    const int x = 5761;
    std::cout << 5761;
    for (int i = 0; i < 5761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5761(int& val) {
    const int x = 5761;
    std::cout << 5761;
    for (int i = 0; i < 5761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5762(int& val) {
    func_in_5762(val);
    const int x = 5762;
    std::cout << 5762;
    for (int i = 0; i < 5762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5762(int& val) {
    const int x = 5762;
    std::cout << 5762;
    for (int i = 0; i < 5762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5763(int& val) {
    func_in_5763(val);
    const int x = 5763;
    std::cout << 5763;
    for (int i = 0; i < 5763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5763(int& val) {
    const int x = 5763;
    std::cout << 5763;
    for (int i = 0; i < 5763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5764(int& val) {
    func_in_5764(val);
    const int x = 5764;
    std::cout << 5764;
    for (int i = 0; i < 5764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5764(int& val) {
    const int x = 5764;
    std::cout << 5764;
    for (int i = 0; i < 5764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5765(int& val) {
    func_in_5765(val);
    const int x = 5765;
    std::cout << 5765;
    for (int i = 0; i < 5765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5765(int& val) {
    const int x = 5765;
    std::cout << 5765;
    for (int i = 0; i < 5765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5766(int& val) {
    func_in_5766(val);
    const int x = 5766;
    std::cout << 5766;
    for (int i = 0; i < 5766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5766(int& val) {
    const int x = 5766;
    std::cout << 5766;
    for (int i = 0; i < 5766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5767(int& val) {
    func_in_5767(val);
    const int x = 5767;
    std::cout << 5767;
    for (int i = 0; i < 5767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5767(int& val) {
    const int x = 5767;
    std::cout << 5767;
    for (int i = 0; i < 5767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5768(int& val) {
    func_in_5768(val);
    const int x = 5768;
    std::cout << 5768;
    for (int i = 0; i < 5768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5768(int& val) {
    const int x = 5768;
    std::cout << 5768;
    for (int i = 0; i < 5768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5769(int& val) {
    func_in_5769(val);
    const int x = 5769;
    std::cout << 5769;
    for (int i = 0; i < 5769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5769(int& val) {
    const int x = 5769;
    std::cout << 5769;
    for (int i = 0; i < 5769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5770(int& val) {
    func_in_5770(val);
    const int x = 5770;
    std::cout << 5770;
    for (int i = 0; i < 5770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5770(int& val) {
    const int x = 5770;
    std::cout << 5770;
    for (int i = 0; i < 5770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5771(int& val) {
    func_in_5771(val);
    const int x = 5771;
    std::cout << 5771;
    for (int i = 0; i < 5771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5771(int& val) {
    const int x = 5771;
    std::cout << 5771;
    for (int i = 0; i < 5771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5772(int& val) {
    func_in_5772(val);
    const int x = 5772;
    std::cout << 5772;
    for (int i = 0; i < 5772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5772(int& val) {
    const int x = 5772;
    std::cout << 5772;
    for (int i = 0; i < 5772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5773(int& val) {
    func_in_5773(val);
    const int x = 5773;
    std::cout << 5773;
    for (int i = 0; i < 5773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5773(int& val) {
    const int x = 5773;
    std::cout << 5773;
    for (int i = 0; i < 5773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5774(int& val) {
    func_in_5774(val);
    const int x = 5774;
    std::cout << 5774;
    for (int i = 0; i < 5774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5774(int& val) {
    const int x = 5774;
    std::cout << 5774;
    for (int i = 0; i < 5774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5775(int& val) {
    func_in_5775(val);
    const int x = 5775;
    std::cout << 5775;
    for (int i = 0; i < 5775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5775(int& val) {
    const int x = 5775;
    std::cout << 5775;
    for (int i = 0; i < 5775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5776(int& val) {
    func_in_5776(val);
    const int x = 5776;
    std::cout << 5776;
    for (int i = 0; i < 5776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5776(int& val) {
    const int x = 5776;
    std::cout << 5776;
    for (int i = 0; i < 5776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5777(int& val) {
    func_in_5777(val);
    const int x = 5777;
    std::cout << 5777;
    for (int i = 0; i < 5777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5777(int& val) {
    const int x = 5777;
    std::cout << 5777;
    for (int i = 0; i < 5777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5778(int& val) {
    func_in_5778(val);
    const int x = 5778;
    std::cout << 5778;
    for (int i = 0; i < 5778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5778(int& val) {
    const int x = 5778;
    std::cout << 5778;
    for (int i = 0; i < 5778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5779(int& val) {
    func_in_5779(val);
    const int x = 5779;
    std::cout << 5779;
    for (int i = 0; i < 5779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5779(int& val) {
    const int x = 5779;
    std::cout << 5779;
    for (int i = 0; i < 5779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5780(int& val) {
    func_in_5780(val);
    const int x = 5780;
    std::cout << 5780;
    for (int i = 0; i < 5780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5780(int& val) {
    const int x = 5780;
    std::cout << 5780;
    for (int i = 0; i < 5780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5781(int& val) {
    func_in_5781(val);
    const int x = 5781;
    std::cout << 5781;
    for (int i = 0; i < 5781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5781(int& val) {
    const int x = 5781;
    std::cout << 5781;
    for (int i = 0; i < 5781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5782(int& val) {
    func_in_5782(val);
    const int x = 5782;
    std::cout << 5782;
    for (int i = 0; i < 5782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5782(int& val) {
    const int x = 5782;
    std::cout << 5782;
    for (int i = 0; i < 5782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5783(int& val) {
    func_in_5783(val);
    const int x = 5783;
    std::cout << 5783;
    for (int i = 0; i < 5783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5783(int& val) {
    const int x = 5783;
    std::cout << 5783;
    for (int i = 0; i < 5783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5784(int& val) {
    func_in_5784(val);
    const int x = 5784;
    std::cout << 5784;
    for (int i = 0; i < 5784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5784(int& val) {
    const int x = 5784;
    std::cout << 5784;
    for (int i = 0; i < 5784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5785(int& val) {
    func_in_5785(val);
    const int x = 5785;
    std::cout << 5785;
    for (int i = 0; i < 5785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5785(int& val) {
    const int x = 5785;
    std::cout << 5785;
    for (int i = 0; i < 5785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5786(int& val) {
    func_in_5786(val);
    const int x = 5786;
    std::cout << 5786;
    for (int i = 0; i < 5786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5786(int& val) {
    const int x = 5786;
    std::cout << 5786;
    for (int i = 0; i < 5786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5787(int& val) {
    func_in_5787(val);
    const int x = 5787;
    std::cout << 5787;
    for (int i = 0; i < 5787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5787(int& val) {
    const int x = 5787;
    std::cout << 5787;
    for (int i = 0; i < 5787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5788(int& val) {
    func_in_5788(val);
    const int x = 5788;
    std::cout << 5788;
    for (int i = 0; i < 5788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5788(int& val) {
    const int x = 5788;
    std::cout << 5788;
    for (int i = 0; i < 5788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5789(int& val) {
    func_in_5789(val);
    const int x = 5789;
    std::cout << 5789;
    for (int i = 0; i < 5789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5789(int& val) {
    const int x = 5789;
    std::cout << 5789;
    for (int i = 0; i < 5789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5790(int& val) {
    func_in_5790(val);
    const int x = 5790;
    std::cout << 5790;
    for (int i = 0; i < 5790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5790(int& val) {
    const int x = 5790;
    std::cout << 5790;
    for (int i = 0; i < 5790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5791(int& val) {
    func_in_5791(val);
    const int x = 5791;
    std::cout << 5791;
    for (int i = 0; i < 5791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5791(int& val) {
    const int x = 5791;
    std::cout << 5791;
    for (int i = 0; i < 5791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5792(int& val) {
    func_in_5792(val);
    const int x = 5792;
    std::cout << 5792;
    for (int i = 0; i < 5792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5792(int& val) {
    const int x = 5792;
    std::cout << 5792;
    for (int i = 0; i < 5792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5793(int& val) {
    func_in_5793(val);
    const int x = 5793;
    std::cout << 5793;
    for (int i = 0; i < 5793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5793(int& val) {
    const int x = 5793;
    std::cout << 5793;
    for (int i = 0; i < 5793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5794(int& val) {
    func_in_5794(val);
    const int x = 5794;
    std::cout << 5794;
    for (int i = 0; i < 5794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5794(int& val) {
    const int x = 5794;
    std::cout << 5794;
    for (int i = 0; i < 5794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5795(int& val) {
    func_in_5795(val);
    const int x = 5795;
    std::cout << 5795;
    for (int i = 0; i < 5795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5795(int& val) {
    const int x = 5795;
    std::cout << 5795;
    for (int i = 0; i < 5795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5796(int& val) {
    func_in_5796(val);
    const int x = 5796;
    std::cout << 5796;
    for (int i = 0; i < 5796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5796(int& val) {
    const int x = 5796;
    std::cout << 5796;
    for (int i = 0; i < 5796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5797(int& val) {
    func_in_5797(val);
    const int x = 5797;
    std::cout << 5797;
    for (int i = 0; i < 5797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5797(int& val) {
    const int x = 5797;
    std::cout << 5797;
    for (int i = 0; i < 5797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5798(int& val) {
    func_in_5798(val);
    const int x = 5798;
    std::cout << 5798;
    for (int i = 0; i < 5798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5798(int& val) {
    const int x = 5798;
    std::cout << 5798;
    for (int i = 0; i < 5798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5799(int& val) {
    func_in_5799(val);
    const int x = 5799;
    std::cout << 5799;
    for (int i = 0; i < 5799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5799(int& val) {
    const int x = 5799;
    std::cout << 5799;
    for (int i = 0; i < 5799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5800(int& val) {
    func_in_5800(val);
    const int x = 5800;
    std::cout << 5800;
    for (int i = 0; i < 5800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5800(int& val) {
    const int x = 5800;
    std::cout << 5800;
    for (int i = 0; i < 5800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5801(int& val) {
    func_in_5801(val);
    const int x = 5801;
    std::cout << 5801;
    for (int i = 0; i < 5801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5801(int& val) {
    const int x = 5801;
    std::cout << 5801;
    for (int i = 0; i < 5801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5802(int& val) {
    func_in_5802(val);
    const int x = 5802;
    std::cout << 5802;
    for (int i = 0; i < 5802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5802(int& val) {
    const int x = 5802;
    std::cout << 5802;
    for (int i = 0; i < 5802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5803(int& val) {
    func_in_5803(val);
    const int x = 5803;
    std::cout << 5803;
    for (int i = 0; i < 5803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5803(int& val) {
    const int x = 5803;
    std::cout << 5803;
    for (int i = 0; i < 5803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5804(int& val) {
    func_in_5804(val);
    const int x = 5804;
    std::cout << 5804;
    for (int i = 0; i < 5804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5804(int& val) {
    const int x = 5804;
    std::cout << 5804;
    for (int i = 0; i < 5804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5805(int& val) {
    func_in_5805(val);
    const int x = 5805;
    std::cout << 5805;
    for (int i = 0; i < 5805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5805(int& val) {
    const int x = 5805;
    std::cout << 5805;
    for (int i = 0; i < 5805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5806(int& val) {
    func_in_5806(val);
    const int x = 5806;
    std::cout << 5806;
    for (int i = 0; i < 5806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5806(int& val) {
    const int x = 5806;
    std::cout << 5806;
    for (int i = 0; i < 5806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5807(int& val) {
    func_in_5807(val);
    const int x = 5807;
    std::cout << 5807;
    for (int i = 0; i < 5807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5807(int& val) {
    const int x = 5807;
    std::cout << 5807;
    for (int i = 0; i < 5807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5808(int& val) {
    func_in_5808(val);
    const int x = 5808;
    std::cout << 5808;
    for (int i = 0; i < 5808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5808(int& val) {
    const int x = 5808;
    std::cout << 5808;
    for (int i = 0; i < 5808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5809(int& val) {
    func_in_5809(val);
    const int x = 5809;
    std::cout << 5809;
    for (int i = 0; i < 5809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5809(int& val) {
    const int x = 5809;
    std::cout << 5809;
    for (int i = 0; i < 5809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5810(int& val) {
    func_in_5810(val);
    const int x = 5810;
    std::cout << 5810;
    for (int i = 0; i < 5810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5810(int& val) {
    const int x = 5810;
    std::cout << 5810;
    for (int i = 0; i < 5810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5811(int& val) {
    func_in_5811(val);
    const int x = 5811;
    std::cout << 5811;
    for (int i = 0; i < 5811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5811(int& val) {
    const int x = 5811;
    std::cout << 5811;
    for (int i = 0; i < 5811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5812(int& val) {
    func_in_5812(val);
    const int x = 5812;
    std::cout << 5812;
    for (int i = 0; i < 5812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5812(int& val) {
    const int x = 5812;
    std::cout << 5812;
    for (int i = 0; i < 5812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5813(int& val) {
    func_in_5813(val);
    const int x = 5813;
    std::cout << 5813;
    for (int i = 0; i < 5813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5813(int& val) {
    const int x = 5813;
    std::cout << 5813;
    for (int i = 0; i < 5813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5814(int& val) {
    func_in_5814(val);
    const int x = 5814;
    std::cout << 5814;
    for (int i = 0; i < 5814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5814(int& val) {
    const int x = 5814;
    std::cout << 5814;
    for (int i = 0; i < 5814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5815(int& val) {
    func_in_5815(val);
    const int x = 5815;
    std::cout << 5815;
    for (int i = 0; i < 5815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5815(int& val) {
    const int x = 5815;
    std::cout << 5815;
    for (int i = 0; i < 5815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5816(int& val) {
    func_in_5816(val);
    const int x = 5816;
    std::cout << 5816;
    for (int i = 0; i < 5816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5816(int& val) {
    const int x = 5816;
    std::cout << 5816;
    for (int i = 0; i < 5816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5817(int& val) {
    func_in_5817(val);
    const int x = 5817;
    std::cout << 5817;
    for (int i = 0; i < 5817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5817(int& val) {
    const int x = 5817;
    std::cout << 5817;
    for (int i = 0; i < 5817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5818(int& val) {
    func_in_5818(val);
    const int x = 5818;
    std::cout << 5818;
    for (int i = 0; i < 5818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5818(int& val) {
    const int x = 5818;
    std::cout << 5818;
    for (int i = 0; i < 5818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5819(int& val) {
    func_in_5819(val);
    const int x = 5819;
    std::cout << 5819;
    for (int i = 0; i < 5819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5819(int& val) {
    const int x = 5819;
    std::cout << 5819;
    for (int i = 0; i < 5819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5820(int& val) {
    func_in_5820(val);
    const int x = 5820;
    std::cout << 5820;
    for (int i = 0; i < 5820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5820(int& val) {
    const int x = 5820;
    std::cout << 5820;
    for (int i = 0; i < 5820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5821(int& val) {
    func_in_5821(val);
    const int x = 5821;
    std::cout << 5821;
    for (int i = 0; i < 5821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5821(int& val) {
    const int x = 5821;
    std::cout << 5821;
    for (int i = 0; i < 5821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5822(int& val) {
    func_in_5822(val);
    const int x = 5822;
    std::cout << 5822;
    for (int i = 0; i < 5822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5822(int& val) {
    const int x = 5822;
    std::cout << 5822;
    for (int i = 0; i < 5822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5823(int& val) {
    func_in_5823(val);
    const int x = 5823;
    std::cout << 5823;
    for (int i = 0; i < 5823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5823(int& val) {
    const int x = 5823;
    std::cout << 5823;
    for (int i = 0; i < 5823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5824(int& val) {
    func_in_5824(val);
    const int x = 5824;
    std::cout << 5824;
    for (int i = 0; i < 5824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5824(int& val) {
    const int x = 5824;
    std::cout << 5824;
    for (int i = 0; i < 5824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5825(int& val) {
    func_in_5825(val);
    const int x = 5825;
    std::cout << 5825;
    for (int i = 0; i < 5825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5825(int& val) {
    const int x = 5825;
    std::cout << 5825;
    for (int i = 0; i < 5825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5826(int& val) {
    func_in_5826(val);
    const int x = 5826;
    std::cout << 5826;
    for (int i = 0; i < 5826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5826(int& val) {
    const int x = 5826;
    std::cout << 5826;
    for (int i = 0; i < 5826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5827(int& val) {
    func_in_5827(val);
    const int x = 5827;
    std::cout << 5827;
    for (int i = 0; i < 5827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5827(int& val) {
    const int x = 5827;
    std::cout << 5827;
    for (int i = 0; i < 5827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5828(int& val) {
    func_in_5828(val);
    const int x = 5828;
    std::cout << 5828;
    for (int i = 0; i < 5828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5828(int& val) {
    const int x = 5828;
    std::cout << 5828;
    for (int i = 0; i < 5828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5829(int& val) {
    func_in_5829(val);
    const int x = 5829;
    std::cout << 5829;
    for (int i = 0; i < 5829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5829(int& val) {
    const int x = 5829;
    std::cout << 5829;
    for (int i = 0; i < 5829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5830(int& val) {
    func_in_5830(val);
    const int x = 5830;
    std::cout << 5830;
    for (int i = 0; i < 5830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5830(int& val) {
    const int x = 5830;
    std::cout << 5830;
    for (int i = 0; i < 5830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5831(int& val) {
    func_in_5831(val);
    const int x = 5831;
    std::cout << 5831;
    for (int i = 0; i < 5831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5831(int& val) {
    const int x = 5831;
    std::cout << 5831;
    for (int i = 0; i < 5831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5832(int& val) {
    func_in_5832(val);
    const int x = 5832;
    std::cout << 5832;
    for (int i = 0; i < 5832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5832(int& val) {
    const int x = 5832;
    std::cout << 5832;
    for (int i = 0; i < 5832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5833(int& val) {
    func_in_5833(val);
    const int x = 5833;
    std::cout << 5833;
    for (int i = 0; i < 5833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5833(int& val) {
    const int x = 5833;
    std::cout << 5833;
    for (int i = 0; i < 5833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5834(int& val) {
    func_in_5834(val);
    const int x = 5834;
    std::cout << 5834;
    for (int i = 0; i < 5834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5834(int& val) {
    const int x = 5834;
    std::cout << 5834;
    for (int i = 0; i < 5834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5835(int& val) {
    func_in_5835(val);
    const int x = 5835;
    std::cout << 5835;
    for (int i = 0; i < 5835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5835(int& val) {
    const int x = 5835;
    std::cout << 5835;
    for (int i = 0; i < 5835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5836(int& val) {
    func_in_5836(val);
    const int x = 5836;
    std::cout << 5836;
    for (int i = 0; i < 5836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5836(int& val) {
    const int x = 5836;
    std::cout << 5836;
    for (int i = 0; i < 5836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5837(int& val) {
    func_in_5837(val);
    const int x = 5837;
    std::cout << 5837;
    for (int i = 0; i < 5837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5837(int& val) {
    const int x = 5837;
    std::cout << 5837;
    for (int i = 0; i < 5837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5838(int& val) {
    func_in_5838(val);
    const int x = 5838;
    std::cout << 5838;
    for (int i = 0; i < 5838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5838(int& val) {
    const int x = 5838;
    std::cout << 5838;
    for (int i = 0; i < 5838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5839(int& val) {
    func_in_5839(val);
    const int x = 5839;
    std::cout << 5839;
    for (int i = 0; i < 5839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5839(int& val) {
    const int x = 5839;
    std::cout << 5839;
    for (int i = 0; i < 5839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5840(int& val) {
    func_in_5840(val);
    const int x = 5840;
    std::cout << 5840;
    for (int i = 0; i < 5840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5840(int& val) {
    const int x = 5840;
    std::cout << 5840;
    for (int i = 0; i < 5840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5841(int& val) {
    func_in_5841(val);
    const int x = 5841;
    std::cout << 5841;
    for (int i = 0; i < 5841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5841(int& val) {
    const int x = 5841;
    std::cout << 5841;
    for (int i = 0; i < 5841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5842(int& val) {
    func_in_5842(val);
    const int x = 5842;
    std::cout << 5842;
    for (int i = 0; i < 5842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5842(int& val) {
    const int x = 5842;
    std::cout << 5842;
    for (int i = 0; i < 5842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5843(int& val) {
    func_in_5843(val);
    const int x = 5843;
    std::cout << 5843;
    for (int i = 0; i < 5843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5843(int& val) {
    const int x = 5843;
    std::cout << 5843;
    for (int i = 0; i < 5843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5844(int& val) {
    func_in_5844(val);
    const int x = 5844;
    std::cout << 5844;
    for (int i = 0; i < 5844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5844(int& val) {
    const int x = 5844;
    std::cout << 5844;
    for (int i = 0; i < 5844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5845(int& val) {
    func_in_5845(val);
    const int x = 5845;
    std::cout << 5845;
    for (int i = 0; i < 5845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5845(int& val) {
    const int x = 5845;
    std::cout << 5845;
    for (int i = 0; i < 5845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5846(int& val) {
    func_in_5846(val);
    const int x = 5846;
    std::cout << 5846;
    for (int i = 0; i < 5846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5846(int& val) {
    const int x = 5846;
    std::cout << 5846;
    for (int i = 0; i < 5846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5847(int& val) {
    func_in_5847(val);
    const int x = 5847;
    std::cout << 5847;
    for (int i = 0; i < 5847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5847(int& val) {
    const int x = 5847;
    std::cout << 5847;
    for (int i = 0; i < 5847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5848(int& val) {
    func_in_5848(val);
    const int x = 5848;
    std::cout << 5848;
    for (int i = 0; i < 5848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5848(int& val) {
    const int x = 5848;
    std::cout << 5848;
    for (int i = 0; i < 5848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5849(int& val) {
    func_in_5849(val);
    const int x = 5849;
    std::cout << 5849;
    for (int i = 0; i < 5849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5849(int& val) {
    const int x = 5849;
    std::cout << 5849;
    for (int i = 0; i < 5849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5850(int& val) {
    func_in_5850(val);
    const int x = 5850;
    std::cout << 5850;
    for (int i = 0; i < 5850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5850(int& val) {
    const int x = 5850;
    std::cout << 5850;
    for (int i = 0; i < 5850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5851(int& val) {
    func_in_5851(val);
    const int x = 5851;
    std::cout << 5851;
    for (int i = 0; i < 5851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5851(int& val) {
    const int x = 5851;
    std::cout << 5851;
    for (int i = 0; i < 5851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5852(int& val) {
    func_in_5852(val);
    const int x = 5852;
    std::cout << 5852;
    for (int i = 0; i < 5852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5852(int& val) {
    const int x = 5852;
    std::cout << 5852;
    for (int i = 0; i < 5852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5853(int& val) {
    func_in_5853(val);
    const int x = 5853;
    std::cout << 5853;
    for (int i = 0; i < 5853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5853(int& val) {
    const int x = 5853;
    std::cout << 5853;
    for (int i = 0; i < 5853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5854(int& val) {
    func_in_5854(val);
    const int x = 5854;
    std::cout << 5854;
    for (int i = 0; i < 5854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5854(int& val) {
    const int x = 5854;
    std::cout << 5854;
    for (int i = 0; i < 5854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5855(int& val) {
    func_in_5855(val);
    const int x = 5855;
    std::cout << 5855;
    for (int i = 0; i < 5855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5855(int& val) {
    const int x = 5855;
    std::cout << 5855;
    for (int i = 0; i < 5855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5856(int& val) {
    func_in_5856(val);
    const int x = 5856;
    std::cout << 5856;
    for (int i = 0; i < 5856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5856(int& val) {
    const int x = 5856;
    std::cout << 5856;
    for (int i = 0; i < 5856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5857(int& val) {
    func_in_5857(val);
    const int x = 5857;
    std::cout << 5857;
    for (int i = 0; i < 5857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5857(int& val) {
    const int x = 5857;
    std::cout << 5857;
    for (int i = 0; i < 5857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5858(int& val) {
    func_in_5858(val);
    const int x = 5858;
    std::cout << 5858;
    for (int i = 0; i < 5858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5858(int& val) {
    const int x = 5858;
    std::cout << 5858;
    for (int i = 0; i < 5858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5859(int& val) {
    func_in_5859(val);
    const int x = 5859;
    std::cout << 5859;
    for (int i = 0; i < 5859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5859(int& val) {
    const int x = 5859;
    std::cout << 5859;
    for (int i = 0; i < 5859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5860(int& val) {
    func_in_5860(val);
    const int x = 5860;
    std::cout << 5860;
    for (int i = 0; i < 5860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5860(int& val) {
    const int x = 5860;
    std::cout << 5860;
    for (int i = 0; i < 5860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5861(int& val) {
    func_in_5861(val);
    const int x = 5861;
    std::cout << 5861;
    for (int i = 0; i < 5861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5861(int& val) {
    const int x = 5861;
    std::cout << 5861;
    for (int i = 0; i < 5861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5862(int& val) {
    func_in_5862(val);
    const int x = 5862;
    std::cout << 5862;
    for (int i = 0; i < 5862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5862(int& val) {
    const int x = 5862;
    std::cout << 5862;
    for (int i = 0; i < 5862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5863(int& val) {
    func_in_5863(val);
    const int x = 5863;
    std::cout << 5863;
    for (int i = 0; i < 5863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5863(int& val) {
    const int x = 5863;
    std::cout << 5863;
    for (int i = 0; i < 5863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5864(int& val) {
    func_in_5864(val);
    const int x = 5864;
    std::cout << 5864;
    for (int i = 0; i < 5864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5864(int& val) {
    const int x = 5864;
    std::cout << 5864;
    for (int i = 0; i < 5864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5865(int& val) {
    func_in_5865(val);
    const int x = 5865;
    std::cout << 5865;
    for (int i = 0; i < 5865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5865(int& val) {
    const int x = 5865;
    std::cout << 5865;
    for (int i = 0; i < 5865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5866(int& val) {
    func_in_5866(val);
    const int x = 5866;
    std::cout << 5866;
    for (int i = 0; i < 5866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5866(int& val) {
    const int x = 5866;
    std::cout << 5866;
    for (int i = 0; i < 5866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5867(int& val) {
    func_in_5867(val);
    const int x = 5867;
    std::cout << 5867;
    for (int i = 0; i < 5867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5867(int& val) {
    const int x = 5867;
    std::cout << 5867;
    for (int i = 0; i < 5867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5868(int& val) {
    func_in_5868(val);
    const int x = 5868;
    std::cout << 5868;
    for (int i = 0; i < 5868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5868(int& val) {
    const int x = 5868;
    std::cout << 5868;
    for (int i = 0; i < 5868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5869(int& val) {
    func_in_5869(val);
    const int x = 5869;
    std::cout << 5869;
    for (int i = 0; i < 5869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5869(int& val) {
    const int x = 5869;
    std::cout << 5869;
    for (int i = 0; i < 5869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5870(int& val) {
    func_in_5870(val);
    const int x = 5870;
    std::cout << 5870;
    for (int i = 0; i < 5870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5870(int& val) {
    const int x = 5870;
    std::cout << 5870;
    for (int i = 0; i < 5870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5871(int& val) {
    func_in_5871(val);
    const int x = 5871;
    std::cout << 5871;
    for (int i = 0; i < 5871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5871(int& val) {
    const int x = 5871;
    std::cout << 5871;
    for (int i = 0; i < 5871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5872(int& val) {
    func_in_5872(val);
    const int x = 5872;
    std::cout << 5872;
    for (int i = 0; i < 5872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5872(int& val) {
    const int x = 5872;
    std::cout << 5872;
    for (int i = 0; i < 5872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5873(int& val) {
    func_in_5873(val);
    const int x = 5873;
    std::cout << 5873;
    for (int i = 0; i < 5873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5873(int& val) {
    const int x = 5873;
    std::cout << 5873;
    for (int i = 0; i < 5873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5874(int& val) {
    func_in_5874(val);
    const int x = 5874;
    std::cout << 5874;
    for (int i = 0; i < 5874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5874(int& val) {
    const int x = 5874;
    std::cout << 5874;
    for (int i = 0; i < 5874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5875(int& val) {
    func_in_5875(val);
    const int x = 5875;
    std::cout << 5875;
    for (int i = 0; i < 5875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5875(int& val) {
    const int x = 5875;
    std::cout << 5875;
    for (int i = 0; i < 5875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5876(int& val) {
    func_in_5876(val);
    const int x = 5876;
    std::cout << 5876;
    for (int i = 0; i < 5876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5876(int& val) {
    const int x = 5876;
    std::cout << 5876;
    for (int i = 0; i < 5876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5877(int& val) {
    func_in_5877(val);
    const int x = 5877;
    std::cout << 5877;
    for (int i = 0; i < 5877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5877(int& val) {
    const int x = 5877;
    std::cout << 5877;
    for (int i = 0; i < 5877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5878(int& val) {
    func_in_5878(val);
    const int x = 5878;
    std::cout << 5878;
    for (int i = 0; i < 5878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5878(int& val) {
    const int x = 5878;
    std::cout << 5878;
    for (int i = 0; i < 5878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5879(int& val) {
    func_in_5879(val);
    const int x = 5879;
    std::cout << 5879;
    for (int i = 0; i < 5879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5879(int& val) {
    const int x = 5879;
    std::cout << 5879;
    for (int i = 0; i < 5879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5880(int& val) {
    func_in_5880(val);
    const int x = 5880;
    std::cout << 5880;
    for (int i = 0; i < 5880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5880(int& val) {
    const int x = 5880;
    std::cout << 5880;
    for (int i = 0; i < 5880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5881(int& val) {
    func_in_5881(val);
    const int x = 5881;
    std::cout << 5881;
    for (int i = 0; i < 5881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5881(int& val) {
    const int x = 5881;
    std::cout << 5881;
    for (int i = 0; i < 5881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5882(int& val) {
    func_in_5882(val);
    const int x = 5882;
    std::cout << 5882;
    for (int i = 0; i < 5882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5882(int& val) {
    const int x = 5882;
    std::cout << 5882;
    for (int i = 0; i < 5882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5883(int& val) {
    func_in_5883(val);
    const int x = 5883;
    std::cout << 5883;
    for (int i = 0; i < 5883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5883(int& val) {
    const int x = 5883;
    std::cout << 5883;
    for (int i = 0; i < 5883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5884(int& val) {
    func_in_5884(val);
    const int x = 5884;
    std::cout << 5884;
    for (int i = 0; i < 5884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5884(int& val) {
    const int x = 5884;
    std::cout << 5884;
    for (int i = 0; i < 5884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5885(int& val) {
    func_in_5885(val);
    const int x = 5885;
    std::cout << 5885;
    for (int i = 0; i < 5885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5885(int& val) {
    const int x = 5885;
    std::cout << 5885;
    for (int i = 0; i < 5885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5886(int& val) {
    func_in_5886(val);
    const int x = 5886;
    std::cout << 5886;
    for (int i = 0; i < 5886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5886(int& val) {
    const int x = 5886;
    std::cout << 5886;
    for (int i = 0; i < 5886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5887(int& val) {
    func_in_5887(val);
    const int x = 5887;
    std::cout << 5887;
    for (int i = 0; i < 5887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5887(int& val) {
    const int x = 5887;
    std::cout << 5887;
    for (int i = 0; i < 5887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5888(int& val) {
    func_in_5888(val);
    const int x = 5888;
    std::cout << 5888;
    for (int i = 0; i < 5888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5888(int& val) {
    const int x = 5888;
    std::cout << 5888;
    for (int i = 0; i < 5888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5889(int& val) {
    func_in_5889(val);
    const int x = 5889;
    std::cout << 5889;
    for (int i = 0; i < 5889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5889(int& val) {
    const int x = 5889;
    std::cout << 5889;
    for (int i = 0; i < 5889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5890(int& val) {
    func_in_5890(val);
    const int x = 5890;
    std::cout << 5890;
    for (int i = 0; i < 5890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5890(int& val) {
    const int x = 5890;
    std::cout << 5890;
    for (int i = 0; i < 5890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5891(int& val) {
    func_in_5891(val);
    const int x = 5891;
    std::cout << 5891;
    for (int i = 0; i < 5891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5891(int& val) {
    const int x = 5891;
    std::cout << 5891;
    for (int i = 0; i < 5891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5892(int& val) {
    func_in_5892(val);
    const int x = 5892;
    std::cout << 5892;
    for (int i = 0; i < 5892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5892(int& val) {
    const int x = 5892;
    std::cout << 5892;
    for (int i = 0; i < 5892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5893(int& val) {
    func_in_5893(val);
    const int x = 5893;
    std::cout << 5893;
    for (int i = 0; i < 5893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5893(int& val) {
    const int x = 5893;
    std::cout << 5893;
    for (int i = 0; i < 5893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5894(int& val) {
    func_in_5894(val);
    const int x = 5894;
    std::cout << 5894;
    for (int i = 0; i < 5894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5894(int& val) {
    const int x = 5894;
    std::cout << 5894;
    for (int i = 0; i < 5894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5895(int& val) {
    func_in_5895(val);
    const int x = 5895;
    std::cout << 5895;
    for (int i = 0; i < 5895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5895(int& val) {
    const int x = 5895;
    std::cout << 5895;
    for (int i = 0; i < 5895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5896(int& val) {
    func_in_5896(val);
    const int x = 5896;
    std::cout << 5896;
    for (int i = 0; i < 5896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5896(int& val) {
    const int x = 5896;
    std::cout << 5896;
    for (int i = 0; i < 5896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5897(int& val) {
    func_in_5897(val);
    const int x = 5897;
    std::cout << 5897;
    for (int i = 0; i < 5897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5897(int& val) {
    const int x = 5897;
    std::cout << 5897;
    for (int i = 0; i < 5897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5898(int& val) {
    func_in_5898(val);
    const int x = 5898;
    std::cout << 5898;
    for (int i = 0; i < 5898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5898(int& val) {
    const int x = 5898;
    std::cout << 5898;
    for (int i = 0; i < 5898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5899(int& val) {
    func_in_5899(val);
    const int x = 5899;
    std::cout << 5899;
    for (int i = 0; i < 5899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5899(int& val) {
    const int x = 5899;
    std::cout << 5899;
    for (int i = 0; i < 5899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5900(int& val) {
    func_in_5900(val);
    const int x = 5900;
    std::cout << 5900;
    for (int i = 0; i < 5900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5900(int& val) {
    const int x = 5900;
    std::cout << 5900;
    for (int i = 0; i < 5900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5901(int& val) {
    func_in_5901(val);
    const int x = 5901;
    std::cout << 5901;
    for (int i = 0; i < 5901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5901(int& val) {
    const int x = 5901;
    std::cout << 5901;
    for (int i = 0; i < 5901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5902(int& val) {
    func_in_5902(val);
    const int x = 5902;
    std::cout << 5902;
    for (int i = 0; i < 5902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5902(int& val) {
    const int x = 5902;
    std::cout << 5902;
    for (int i = 0; i < 5902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5903(int& val) {
    func_in_5903(val);
    const int x = 5903;
    std::cout << 5903;
    for (int i = 0; i < 5903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5903(int& val) {
    const int x = 5903;
    std::cout << 5903;
    for (int i = 0; i < 5903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5904(int& val) {
    func_in_5904(val);
    const int x = 5904;
    std::cout << 5904;
    for (int i = 0; i < 5904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5904(int& val) {
    const int x = 5904;
    std::cout << 5904;
    for (int i = 0; i < 5904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5905(int& val) {
    func_in_5905(val);
    const int x = 5905;
    std::cout << 5905;
    for (int i = 0; i < 5905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5905(int& val) {
    const int x = 5905;
    std::cout << 5905;
    for (int i = 0; i < 5905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5906(int& val) {
    func_in_5906(val);
    const int x = 5906;
    std::cout << 5906;
    for (int i = 0; i < 5906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5906(int& val) {
    const int x = 5906;
    std::cout << 5906;
    for (int i = 0; i < 5906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5907(int& val) {
    func_in_5907(val);
    const int x = 5907;
    std::cout << 5907;
    for (int i = 0; i < 5907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5907(int& val) {
    const int x = 5907;
    std::cout << 5907;
    for (int i = 0; i < 5907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5908(int& val) {
    func_in_5908(val);
    const int x = 5908;
    std::cout << 5908;
    for (int i = 0; i < 5908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5908(int& val) {
    const int x = 5908;
    std::cout << 5908;
    for (int i = 0; i < 5908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5909(int& val) {
    func_in_5909(val);
    const int x = 5909;
    std::cout << 5909;
    for (int i = 0; i < 5909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5909(int& val) {
    const int x = 5909;
    std::cout << 5909;
    for (int i = 0; i < 5909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5910(int& val) {
    func_in_5910(val);
    const int x = 5910;
    std::cout << 5910;
    for (int i = 0; i < 5910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5910(int& val) {
    const int x = 5910;
    std::cout << 5910;
    for (int i = 0; i < 5910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5911(int& val) {
    func_in_5911(val);
    const int x = 5911;
    std::cout << 5911;
    for (int i = 0; i < 5911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5911(int& val) {
    const int x = 5911;
    std::cout << 5911;
    for (int i = 0; i < 5911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5912(int& val) {
    func_in_5912(val);
    const int x = 5912;
    std::cout << 5912;
    for (int i = 0; i < 5912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5912(int& val) {
    const int x = 5912;
    std::cout << 5912;
    for (int i = 0; i < 5912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5913(int& val) {
    func_in_5913(val);
    const int x = 5913;
    std::cout << 5913;
    for (int i = 0; i < 5913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5913(int& val) {
    const int x = 5913;
    std::cout << 5913;
    for (int i = 0; i < 5913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5914(int& val) {
    func_in_5914(val);
    const int x = 5914;
    std::cout << 5914;
    for (int i = 0; i < 5914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5914(int& val) {
    const int x = 5914;
    std::cout << 5914;
    for (int i = 0; i < 5914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5915(int& val) {
    func_in_5915(val);
    const int x = 5915;
    std::cout << 5915;
    for (int i = 0; i < 5915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5915(int& val) {
    const int x = 5915;
    std::cout << 5915;
    for (int i = 0; i < 5915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5916(int& val) {
    func_in_5916(val);
    const int x = 5916;
    std::cout << 5916;
    for (int i = 0; i < 5916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5916(int& val) {
    const int x = 5916;
    std::cout << 5916;
    for (int i = 0; i < 5916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5917(int& val) {
    func_in_5917(val);
    const int x = 5917;
    std::cout << 5917;
    for (int i = 0; i < 5917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5917(int& val) {
    const int x = 5917;
    std::cout << 5917;
    for (int i = 0; i < 5917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5918(int& val) {
    func_in_5918(val);
    const int x = 5918;
    std::cout << 5918;
    for (int i = 0; i < 5918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5918(int& val) {
    const int x = 5918;
    std::cout << 5918;
    for (int i = 0; i < 5918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5919(int& val) {
    func_in_5919(val);
    const int x = 5919;
    std::cout << 5919;
    for (int i = 0; i < 5919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5919(int& val) {
    const int x = 5919;
    std::cout << 5919;
    for (int i = 0; i < 5919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5920(int& val) {
    func_in_5920(val);
    const int x = 5920;
    std::cout << 5920;
    for (int i = 0; i < 5920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5920(int& val) {
    const int x = 5920;
    std::cout << 5920;
    for (int i = 0; i < 5920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5921(int& val) {
    func_in_5921(val);
    const int x = 5921;
    std::cout << 5921;
    for (int i = 0; i < 5921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5921(int& val) {
    const int x = 5921;
    std::cout << 5921;
    for (int i = 0; i < 5921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5922(int& val) {
    func_in_5922(val);
    const int x = 5922;
    std::cout << 5922;
    for (int i = 0; i < 5922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5922(int& val) {
    const int x = 5922;
    std::cout << 5922;
    for (int i = 0; i < 5922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5923(int& val) {
    func_in_5923(val);
    const int x = 5923;
    std::cout << 5923;
    for (int i = 0; i < 5923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5923(int& val) {
    const int x = 5923;
    std::cout << 5923;
    for (int i = 0; i < 5923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5924(int& val) {
    func_in_5924(val);
    const int x = 5924;
    std::cout << 5924;
    for (int i = 0; i < 5924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5924(int& val) {
    const int x = 5924;
    std::cout << 5924;
    for (int i = 0; i < 5924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5925(int& val) {
    func_in_5925(val);
    const int x = 5925;
    std::cout << 5925;
    for (int i = 0; i < 5925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5925(int& val) {
    const int x = 5925;
    std::cout << 5925;
    for (int i = 0; i < 5925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5926(int& val) {
    func_in_5926(val);
    const int x = 5926;
    std::cout << 5926;
    for (int i = 0; i < 5926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5926(int& val) {
    const int x = 5926;
    std::cout << 5926;
    for (int i = 0; i < 5926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5927(int& val) {
    func_in_5927(val);
    const int x = 5927;
    std::cout << 5927;
    for (int i = 0; i < 5927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5927(int& val) {
    const int x = 5927;
    std::cout << 5927;
    for (int i = 0; i < 5927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5928(int& val) {
    func_in_5928(val);
    const int x = 5928;
    std::cout << 5928;
    for (int i = 0; i < 5928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5928(int& val) {
    const int x = 5928;
    std::cout << 5928;
    for (int i = 0; i < 5928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5929(int& val) {
    func_in_5929(val);
    const int x = 5929;
    std::cout << 5929;
    for (int i = 0; i < 5929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5929(int& val) {
    const int x = 5929;
    std::cout << 5929;
    for (int i = 0; i < 5929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5930(int& val) {
    func_in_5930(val);
    const int x = 5930;
    std::cout << 5930;
    for (int i = 0; i < 5930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5930(int& val) {
    const int x = 5930;
    std::cout << 5930;
    for (int i = 0; i < 5930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5931(int& val) {
    func_in_5931(val);
    const int x = 5931;
    std::cout << 5931;
    for (int i = 0; i < 5931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5931(int& val) {
    const int x = 5931;
    std::cout << 5931;
    for (int i = 0; i < 5931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5932(int& val) {
    func_in_5932(val);
    const int x = 5932;
    std::cout << 5932;
    for (int i = 0; i < 5932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5932(int& val) {
    const int x = 5932;
    std::cout << 5932;
    for (int i = 0; i < 5932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5933(int& val) {
    func_in_5933(val);
    const int x = 5933;
    std::cout << 5933;
    for (int i = 0; i < 5933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5933(int& val) {
    const int x = 5933;
    std::cout << 5933;
    for (int i = 0; i < 5933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5934(int& val) {
    func_in_5934(val);
    const int x = 5934;
    std::cout << 5934;
    for (int i = 0; i < 5934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5934(int& val) {
    const int x = 5934;
    std::cout << 5934;
    for (int i = 0; i < 5934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5935(int& val) {
    func_in_5935(val);
    const int x = 5935;
    std::cout << 5935;
    for (int i = 0; i < 5935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5935(int& val) {
    const int x = 5935;
    std::cout << 5935;
    for (int i = 0; i < 5935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5936(int& val) {
    func_in_5936(val);
    const int x = 5936;
    std::cout << 5936;
    for (int i = 0; i < 5936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5936(int& val) {
    const int x = 5936;
    std::cout << 5936;
    for (int i = 0; i < 5936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5937(int& val) {
    func_in_5937(val);
    const int x = 5937;
    std::cout << 5937;
    for (int i = 0; i < 5937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5937(int& val) {
    const int x = 5937;
    std::cout << 5937;
    for (int i = 0; i < 5937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5938(int& val) {
    func_in_5938(val);
    const int x = 5938;
    std::cout << 5938;
    for (int i = 0; i < 5938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5938(int& val) {
    const int x = 5938;
    std::cout << 5938;
    for (int i = 0; i < 5938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5939(int& val) {
    func_in_5939(val);
    const int x = 5939;
    std::cout << 5939;
    for (int i = 0; i < 5939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5939(int& val) {
    const int x = 5939;
    std::cout << 5939;
    for (int i = 0; i < 5939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5940(int& val) {
    func_in_5940(val);
    const int x = 5940;
    std::cout << 5940;
    for (int i = 0; i < 5940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5940(int& val) {
    const int x = 5940;
    std::cout << 5940;
    for (int i = 0; i < 5940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5941(int& val) {
    func_in_5941(val);
    const int x = 5941;
    std::cout << 5941;
    for (int i = 0; i < 5941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5941(int& val) {
    const int x = 5941;
    std::cout << 5941;
    for (int i = 0; i < 5941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5942(int& val) {
    func_in_5942(val);
    const int x = 5942;
    std::cout << 5942;
    for (int i = 0; i < 5942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5942(int& val) {
    const int x = 5942;
    std::cout << 5942;
    for (int i = 0; i < 5942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5943(int& val) {
    func_in_5943(val);
    const int x = 5943;
    std::cout << 5943;
    for (int i = 0; i < 5943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5943(int& val) {
    const int x = 5943;
    std::cout << 5943;
    for (int i = 0; i < 5943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5944(int& val) {
    func_in_5944(val);
    const int x = 5944;
    std::cout << 5944;
    for (int i = 0; i < 5944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5944(int& val) {
    const int x = 5944;
    std::cout << 5944;
    for (int i = 0; i < 5944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5945(int& val) {
    func_in_5945(val);
    const int x = 5945;
    std::cout << 5945;
    for (int i = 0; i < 5945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5945(int& val) {
    const int x = 5945;
    std::cout << 5945;
    for (int i = 0; i < 5945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5946(int& val) {
    func_in_5946(val);
    const int x = 5946;
    std::cout << 5946;
    for (int i = 0; i < 5946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5946(int& val) {
    const int x = 5946;
    std::cout << 5946;
    for (int i = 0; i < 5946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5947(int& val) {
    func_in_5947(val);
    const int x = 5947;
    std::cout << 5947;
    for (int i = 0; i < 5947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5947(int& val) {
    const int x = 5947;
    std::cout << 5947;
    for (int i = 0; i < 5947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5948(int& val) {
    func_in_5948(val);
    const int x = 5948;
    std::cout << 5948;
    for (int i = 0; i < 5948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5948(int& val) {
    const int x = 5948;
    std::cout << 5948;
    for (int i = 0; i < 5948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5949(int& val) {
    func_in_5949(val);
    const int x = 5949;
    std::cout << 5949;
    for (int i = 0; i < 5949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5949(int& val) {
    const int x = 5949;
    std::cout << 5949;
    for (int i = 0; i < 5949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5950(int& val) {
    func_in_5950(val);
    const int x = 5950;
    std::cout << 5950;
    for (int i = 0; i < 5950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5950(int& val) {
    const int x = 5950;
    std::cout << 5950;
    for (int i = 0; i < 5950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5951(int& val) {
    func_in_5951(val);
    const int x = 5951;
    std::cout << 5951;
    for (int i = 0; i < 5951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5951(int& val) {
    const int x = 5951;
    std::cout << 5951;
    for (int i = 0; i < 5951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5952(int& val) {
    func_in_5952(val);
    const int x = 5952;
    std::cout << 5952;
    for (int i = 0; i < 5952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5952(int& val) {
    const int x = 5952;
    std::cout << 5952;
    for (int i = 0; i < 5952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5953(int& val) {
    func_in_5953(val);
    const int x = 5953;
    std::cout << 5953;
    for (int i = 0; i < 5953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5953(int& val) {
    const int x = 5953;
    std::cout << 5953;
    for (int i = 0; i < 5953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5954(int& val) {
    func_in_5954(val);
    const int x = 5954;
    std::cout << 5954;
    for (int i = 0; i < 5954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5954(int& val) {
    const int x = 5954;
    std::cout << 5954;
    for (int i = 0; i < 5954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5955(int& val) {
    func_in_5955(val);
    const int x = 5955;
    std::cout << 5955;
    for (int i = 0; i < 5955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5955(int& val) {
    const int x = 5955;
    std::cout << 5955;
    for (int i = 0; i < 5955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5956(int& val) {
    func_in_5956(val);
    const int x = 5956;
    std::cout << 5956;
    for (int i = 0; i < 5956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5956(int& val) {
    const int x = 5956;
    std::cout << 5956;
    for (int i = 0; i < 5956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5957(int& val) {
    func_in_5957(val);
    const int x = 5957;
    std::cout << 5957;
    for (int i = 0; i < 5957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5957(int& val) {
    const int x = 5957;
    std::cout << 5957;
    for (int i = 0; i < 5957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5958(int& val) {
    func_in_5958(val);
    const int x = 5958;
    std::cout << 5958;
    for (int i = 0; i < 5958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5958(int& val) {
    const int x = 5958;
    std::cout << 5958;
    for (int i = 0; i < 5958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5959(int& val) {
    func_in_5959(val);
    const int x = 5959;
    std::cout << 5959;
    for (int i = 0; i < 5959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5959(int& val) {
    const int x = 5959;
    std::cout << 5959;
    for (int i = 0; i < 5959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5960(int& val) {
    func_in_5960(val);
    const int x = 5960;
    std::cout << 5960;
    for (int i = 0; i < 5960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5960(int& val) {
    const int x = 5960;
    std::cout << 5960;
    for (int i = 0; i < 5960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5961(int& val) {
    func_in_5961(val);
    const int x = 5961;
    std::cout << 5961;
    for (int i = 0; i < 5961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5961(int& val) {
    const int x = 5961;
    std::cout << 5961;
    for (int i = 0; i < 5961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5962(int& val) {
    func_in_5962(val);
    const int x = 5962;
    std::cout << 5962;
    for (int i = 0; i < 5962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5962(int& val) {
    const int x = 5962;
    std::cout << 5962;
    for (int i = 0; i < 5962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5963(int& val) {
    func_in_5963(val);
    const int x = 5963;
    std::cout << 5963;
    for (int i = 0; i < 5963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5963(int& val) {
    const int x = 5963;
    std::cout << 5963;
    for (int i = 0; i < 5963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5964(int& val) {
    func_in_5964(val);
    const int x = 5964;
    std::cout << 5964;
    for (int i = 0; i < 5964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5964(int& val) {
    const int x = 5964;
    std::cout << 5964;
    for (int i = 0; i < 5964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5965(int& val) {
    func_in_5965(val);
    const int x = 5965;
    std::cout << 5965;
    for (int i = 0; i < 5965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5965(int& val) {
    const int x = 5965;
    std::cout << 5965;
    for (int i = 0; i < 5965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5966(int& val) {
    func_in_5966(val);
    const int x = 5966;
    std::cout << 5966;
    for (int i = 0; i < 5966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5966(int& val) {
    const int x = 5966;
    std::cout << 5966;
    for (int i = 0; i < 5966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5967(int& val) {
    func_in_5967(val);
    const int x = 5967;
    std::cout << 5967;
    for (int i = 0; i < 5967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5967(int& val) {
    const int x = 5967;
    std::cout << 5967;
    for (int i = 0; i < 5967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5968(int& val) {
    func_in_5968(val);
    const int x = 5968;
    std::cout << 5968;
    for (int i = 0; i < 5968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5968(int& val) {
    const int x = 5968;
    std::cout << 5968;
    for (int i = 0; i < 5968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5969(int& val) {
    func_in_5969(val);
    const int x = 5969;
    std::cout << 5969;
    for (int i = 0; i < 5969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5969(int& val) {
    const int x = 5969;
    std::cout << 5969;
    for (int i = 0; i < 5969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5970(int& val) {
    func_in_5970(val);
    const int x = 5970;
    std::cout << 5970;
    for (int i = 0; i < 5970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5970(int& val) {
    const int x = 5970;
    std::cout << 5970;
    for (int i = 0; i < 5970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5971(int& val) {
    func_in_5971(val);
    const int x = 5971;
    std::cout << 5971;
    for (int i = 0; i < 5971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5971(int& val) {
    const int x = 5971;
    std::cout << 5971;
    for (int i = 0; i < 5971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5972(int& val) {
    func_in_5972(val);
    const int x = 5972;
    std::cout << 5972;
    for (int i = 0; i < 5972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5972(int& val) {
    const int x = 5972;
    std::cout << 5972;
    for (int i = 0; i < 5972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5973(int& val) {
    func_in_5973(val);
    const int x = 5973;
    std::cout << 5973;
    for (int i = 0; i < 5973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5973(int& val) {
    const int x = 5973;
    std::cout << 5973;
    for (int i = 0; i < 5973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5974(int& val) {
    func_in_5974(val);
    const int x = 5974;
    std::cout << 5974;
    for (int i = 0; i < 5974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5974(int& val) {
    const int x = 5974;
    std::cout << 5974;
    for (int i = 0; i < 5974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5975(int& val) {
    func_in_5975(val);
    const int x = 5975;
    std::cout << 5975;
    for (int i = 0; i < 5975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5975(int& val) {
    const int x = 5975;
    std::cout << 5975;
    for (int i = 0; i < 5975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5976(int& val) {
    func_in_5976(val);
    const int x = 5976;
    std::cout << 5976;
    for (int i = 0; i < 5976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5976(int& val) {
    const int x = 5976;
    std::cout << 5976;
    for (int i = 0; i < 5976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5977(int& val) {
    func_in_5977(val);
    const int x = 5977;
    std::cout << 5977;
    for (int i = 0; i < 5977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5977(int& val) {
    const int x = 5977;
    std::cout << 5977;
    for (int i = 0; i < 5977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5978(int& val) {
    func_in_5978(val);
    const int x = 5978;
    std::cout << 5978;
    for (int i = 0; i < 5978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5978(int& val) {
    const int x = 5978;
    std::cout << 5978;
    for (int i = 0; i < 5978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5979(int& val) {
    func_in_5979(val);
    const int x = 5979;
    std::cout << 5979;
    for (int i = 0; i < 5979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5979(int& val) {
    const int x = 5979;
    std::cout << 5979;
    for (int i = 0; i < 5979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5980(int& val) {
    func_in_5980(val);
    const int x = 5980;
    std::cout << 5980;
    for (int i = 0; i < 5980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5980(int& val) {
    const int x = 5980;
    std::cout << 5980;
    for (int i = 0; i < 5980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5981(int& val) {
    func_in_5981(val);
    const int x = 5981;
    std::cout << 5981;
    for (int i = 0; i < 5981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5981(int& val) {
    const int x = 5981;
    std::cout << 5981;
    for (int i = 0; i < 5981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5982(int& val) {
    func_in_5982(val);
    const int x = 5982;
    std::cout << 5982;
    for (int i = 0; i < 5982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5982(int& val) {
    const int x = 5982;
    std::cout << 5982;
    for (int i = 0; i < 5982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5983(int& val) {
    func_in_5983(val);
    const int x = 5983;
    std::cout << 5983;
    for (int i = 0; i < 5983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5983(int& val) {
    const int x = 5983;
    std::cout << 5983;
    for (int i = 0; i < 5983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5984(int& val) {
    func_in_5984(val);
    const int x = 5984;
    std::cout << 5984;
    for (int i = 0; i < 5984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5984(int& val) {
    const int x = 5984;
    std::cout << 5984;
    for (int i = 0; i < 5984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5985(int& val) {
    func_in_5985(val);
    const int x = 5985;
    std::cout << 5985;
    for (int i = 0; i < 5985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5985(int& val) {
    const int x = 5985;
    std::cout << 5985;
    for (int i = 0; i < 5985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5986(int& val) {
    func_in_5986(val);
    const int x = 5986;
    std::cout << 5986;
    for (int i = 0; i < 5986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5986(int& val) {
    const int x = 5986;
    std::cout << 5986;
    for (int i = 0; i < 5986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5987(int& val) {
    func_in_5987(val);
    const int x = 5987;
    std::cout << 5987;
    for (int i = 0; i < 5987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5987(int& val) {
    const int x = 5987;
    std::cout << 5987;
    for (int i = 0; i < 5987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5988(int& val) {
    func_in_5988(val);
    const int x = 5988;
    std::cout << 5988;
    for (int i = 0; i < 5988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5988(int& val) {
    const int x = 5988;
    std::cout << 5988;
    for (int i = 0; i < 5988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5989(int& val) {
    func_in_5989(val);
    const int x = 5989;
    std::cout << 5989;
    for (int i = 0; i < 5989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5989(int& val) {
    const int x = 5989;
    std::cout << 5989;
    for (int i = 0; i < 5989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5990(int& val) {
    func_in_5990(val);
    const int x = 5990;
    std::cout << 5990;
    for (int i = 0; i < 5990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5990(int& val) {
    const int x = 5990;
    std::cout << 5990;
    for (int i = 0; i < 5990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5991(int& val) {
    func_in_5991(val);
    const int x = 5991;
    std::cout << 5991;
    for (int i = 0; i < 5991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5991(int& val) {
    const int x = 5991;
    std::cout << 5991;
    for (int i = 0; i < 5991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5992(int& val) {
    func_in_5992(val);
    const int x = 5992;
    std::cout << 5992;
    for (int i = 0; i < 5992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5992(int& val) {
    const int x = 5992;
    std::cout << 5992;
    for (int i = 0; i < 5992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5993(int& val) {
    func_in_5993(val);
    const int x = 5993;
    std::cout << 5993;
    for (int i = 0; i < 5993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5993(int& val) {
    const int x = 5993;
    std::cout << 5993;
    for (int i = 0; i < 5993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5994(int& val) {
    func_in_5994(val);
    const int x = 5994;
    std::cout << 5994;
    for (int i = 0; i < 5994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5994(int& val) {
    const int x = 5994;
    std::cout << 5994;
    for (int i = 0; i < 5994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5995(int& val) {
    func_in_5995(val);
    const int x = 5995;
    std::cout << 5995;
    for (int i = 0; i < 5995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5995(int& val) {
    const int x = 5995;
    std::cout << 5995;
    for (int i = 0; i < 5995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5996(int& val) {
    func_in_5996(val);
    const int x = 5996;
    std::cout << 5996;
    for (int i = 0; i < 5996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5996(int& val) {
    const int x = 5996;
    std::cout << 5996;
    for (int i = 0; i < 5996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5997(int& val) {
    func_in_5997(val);
    const int x = 5997;
    std::cout << 5997;
    for (int i = 0; i < 5997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5997(int& val) {
    const int x = 5997;
    std::cout << 5997;
    for (int i = 0; i < 5997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5998(int& val) {
    func_in_5998(val);
    const int x = 5998;
    std::cout << 5998;
    for (int i = 0; i < 5998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5998(int& val) {
    const int x = 5998;
    std::cout << 5998;
    for (int i = 0; i < 5998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5999(int& val) {
    func_in_5999(val);
    const int x = 5999;
    std::cout << 5999;
    for (int i = 0; i < 5999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5999(int& val) {
    const int x = 5999;
    std::cout << 5999;
    for (int i = 0; i < 5999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6000(int& val) {
    func_in_6000(val);
    const int x = 6000;
    std::cout << 6000;
    for (int i = 0; i < 6000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6000(int& val) {
    const int x = 6000;
    std::cout << 6000;
    for (int i = 0; i < 6000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6001(int& val) {
    func_in_6001(val);
    const int x = 6001;
    std::cout << 6001;
    for (int i = 0; i < 6001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6001(int& val) {
    const int x = 6001;
    std::cout << 6001;
    for (int i = 0; i < 6001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6002(int& val) {
    func_in_6002(val);
    const int x = 6002;
    std::cout << 6002;
    for (int i = 0; i < 6002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6002(int& val) {
    const int x = 6002;
    std::cout << 6002;
    for (int i = 0; i < 6002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6003(int& val) {
    func_in_6003(val);
    const int x = 6003;
    std::cout << 6003;
    for (int i = 0; i < 6003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6003(int& val) {
    const int x = 6003;
    std::cout << 6003;
    for (int i = 0; i < 6003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6004(int& val) {
    func_in_6004(val);
    const int x = 6004;
    std::cout << 6004;
    for (int i = 0; i < 6004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6004(int& val) {
    const int x = 6004;
    std::cout << 6004;
    for (int i = 0; i < 6004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6005(int& val) {
    func_in_6005(val);
    const int x = 6005;
    std::cout << 6005;
    for (int i = 0; i < 6005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6005(int& val) {
    const int x = 6005;
    std::cout << 6005;
    for (int i = 0; i < 6005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6006(int& val) {
    func_in_6006(val);
    const int x = 6006;
    std::cout << 6006;
    for (int i = 0; i < 6006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6006(int& val) {
    const int x = 6006;
    std::cout << 6006;
    for (int i = 0; i < 6006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6007(int& val) {
    func_in_6007(val);
    const int x = 6007;
    std::cout << 6007;
    for (int i = 0; i < 6007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6007(int& val) {
    const int x = 6007;
    std::cout << 6007;
    for (int i = 0; i < 6007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6008(int& val) {
    func_in_6008(val);
    const int x = 6008;
    std::cout << 6008;
    for (int i = 0; i < 6008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6008(int& val) {
    const int x = 6008;
    std::cout << 6008;
    for (int i = 0; i < 6008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6009(int& val) {
    func_in_6009(val);
    const int x = 6009;
    std::cout << 6009;
    for (int i = 0; i < 6009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6009(int& val) {
    const int x = 6009;
    std::cout << 6009;
    for (int i = 0; i < 6009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6010(int& val) {
    func_in_6010(val);
    const int x = 6010;
    std::cout << 6010;
    for (int i = 0; i < 6010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6010(int& val) {
    const int x = 6010;
    std::cout << 6010;
    for (int i = 0; i < 6010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6011(int& val) {
    func_in_6011(val);
    const int x = 6011;
    std::cout << 6011;
    for (int i = 0; i < 6011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6011(int& val) {
    const int x = 6011;
    std::cout << 6011;
    for (int i = 0; i < 6011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6012(int& val) {
    func_in_6012(val);
    const int x = 6012;
    std::cout << 6012;
    for (int i = 0; i < 6012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6012(int& val) {
    const int x = 6012;
    std::cout << 6012;
    for (int i = 0; i < 6012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6013(int& val) {
    func_in_6013(val);
    const int x = 6013;
    std::cout << 6013;
    for (int i = 0; i < 6013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6013(int& val) {
    const int x = 6013;
    std::cout << 6013;
    for (int i = 0; i < 6013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6014(int& val) {
    func_in_6014(val);
    const int x = 6014;
    std::cout << 6014;
    for (int i = 0; i < 6014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6014(int& val) {
    const int x = 6014;
    std::cout << 6014;
    for (int i = 0; i < 6014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6015(int& val) {
    func_in_6015(val);
    const int x = 6015;
    std::cout << 6015;
    for (int i = 0; i < 6015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6015(int& val) {
    const int x = 6015;
    std::cout << 6015;
    for (int i = 0; i < 6015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6016(int& val) {
    func_in_6016(val);
    const int x = 6016;
    std::cout << 6016;
    for (int i = 0; i < 6016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6016(int& val) {
    const int x = 6016;
    std::cout << 6016;
    for (int i = 0; i < 6016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6017(int& val) {
    func_in_6017(val);
    const int x = 6017;
    std::cout << 6017;
    for (int i = 0; i < 6017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6017(int& val) {
    const int x = 6017;
    std::cout << 6017;
    for (int i = 0; i < 6017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6018(int& val) {
    func_in_6018(val);
    const int x = 6018;
    std::cout << 6018;
    for (int i = 0; i < 6018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6018(int& val) {
    const int x = 6018;
    std::cout << 6018;
    for (int i = 0; i < 6018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6019(int& val) {
    func_in_6019(val);
    const int x = 6019;
    std::cout << 6019;
    for (int i = 0; i < 6019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6019(int& val) {
    const int x = 6019;
    std::cout << 6019;
    for (int i = 0; i < 6019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6020(int& val) {
    func_in_6020(val);
    const int x = 6020;
    std::cout << 6020;
    for (int i = 0; i < 6020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6020(int& val) {
    const int x = 6020;
    std::cout << 6020;
    for (int i = 0; i < 6020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6021(int& val) {
    func_in_6021(val);
    const int x = 6021;
    std::cout << 6021;
    for (int i = 0; i < 6021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6021(int& val) {
    const int x = 6021;
    std::cout << 6021;
    for (int i = 0; i < 6021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6022(int& val) {
    func_in_6022(val);
    const int x = 6022;
    std::cout << 6022;
    for (int i = 0; i < 6022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6022(int& val) {
    const int x = 6022;
    std::cout << 6022;
    for (int i = 0; i < 6022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6023(int& val) {
    func_in_6023(val);
    const int x = 6023;
    std::cout << 6023;
    for (int i = 0; i < 6023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6023(int& val) {
    const int x = 6023;
    std::cout << 6023;
    for (int i = 0; i < 6023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6024(int& val) {
    func_in_6024(val);
    const int x = 6024;
    std::cout << 6024;
    for (int i = 0; i < 6024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6024(int& val) {
    const int x = 6024;
    std::cout << 6024;
    for (int i = 0; i < 6024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6025(int& val) {
    func_in_6025(val);
    const int x = 6025;
    std::cout << 6025;
    for (int i = 0; i < 6025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6025(int& val) {
    const int x = 6025;
    std::cout << 6025;
    for (int i = 0; i < 6025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6026(int& val) {
    func_in_6026(val);
    const int x = 6026;
    std::cout << 6026;
    for (int i = 0; i < 6026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6026(int& val) {
    const int x = 6026;
    std::cout << 6026;
    for (int i = 0; i < 6026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6027(int& val) {
    func_in_6027(val);
    const int x = 6027;
    std::cout << 6027;
    for (int i = 0; i < 6027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6027(int& val) {
    const int x = 6027;
    std::cout << 6027;
    for (int i = 0; i < 6027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6028(int& val) {
    func_in_6028(val);
    const int x = 6028;
    std::cout << 6028;
    for (int i = 0; i < 6028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6028(int& val) {
    const int x = 6028;
    std::cout << 6028;
    for (int i = 0; i < 6028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6029(int& val) {
    func_in_6029(val);
    const int x = 6029;
    std::cout << 6029;
    for (int i = 0; i < 6029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6029(int& val) {
    const int x = 6029;
    std::cout << 6029;
    for (int i = 0; i < 6029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6030(int& val) {
    func_in_6030(val);
    const int x = 6030;
    std::cout << 6030;
    for (int i = 0; i < 6030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6030(int& val) {
    const int x = 6030;
    std::cout << 6030;
    for (int i = 0; i < 6030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6031(int& val) {
    func_in_6031(val);
    const int x = 6031;
    std::cout << 6031;
    for (int i = 0; i < 6031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6031(int& val) {
    const int x = 6031;
    std::cout << 6031;
    for (int i = 0; i < 6031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6032(int& val) {
    func_in_6032(val);
    const int x = 6032;
    std::cout << 6032;
    for (int i = 0; i < 6032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6032(int& val) {
    const int x = 6032;
    std::cout << 6032;
    for (int i = 0; i < 6032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6033(int& val) {
    func_in_6033(val);
    const int x = 6033;
    std::cout << 6033;
    for (int i = 0; i < 6033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6033(int& val) {
    const int x = 6033;
    std::cout << 6033;
    for (int i = 0; i < 6033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6034(int& val) {
    func_in_6034(val);
    const int x = 6034;
    std::cout << 6034;
    for (int i = 0; i < 6034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6034(int& val) {
    const int x = 6034;
    std::cout << 6034;
    for (int i = 0; i < 6034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6035(int& val) {
    func_in_6035(val);
    const int x = 6035;
    std::cout << 6035;
    for (int i = 0; i < 6035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6035(int& val) {
    const int x = 6035;
    std::cout << 6035;
    for (int i = 0; i < 6035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6036(int& val) {
    func_in_6036(val);
    const int x = 6036;
    std::cout << 6036;
    for (int i = 0; i < 6036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6036(int& val) {
    const int x = 6036;
    std::cout << 6036;
    for (int i = 0; i < 6036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6037(int& val) {
    func_in_6037(val);
    const int x = 6037;
    std::cout << 6037;
    for (int i = 0; i < 6037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6037(int& val) {
    const int x = 6037;
    std::cout << 6037;
    for (int i = 0; i < 6037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6038(int& val) {
    func_in_6038(val);
    const int x = 6038;
    std::cout << 6038;
    for (int i = 0; i < 6038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6038(int& val) {
    const int x = 6038;
    std::cout << 6038;
    for (int i = 0; i < 6038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6039(int& val) {
    func_in_6039(val);
    const int x = 6039;
    std::cout << 6039;
    for (int i = 0; i < 6039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6039(int& val) {
    const int x = 6039;
    std::cout << 6039;
    for (int i = 0; i < 6039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6040(int& val) {
    func_in_6040(val);
    const int x = 6040;
    std::cout << 6040;
    for (int i = 0; i < 6040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6040(int& val) {
    const int x = 6040;
    std::cout << 6040;
    for (int i = 0; i < 6040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6041(int& val) {
    func_in_6041(val);
    const int x = 6041;
    std::cout << 6041;
    for (int i = 0; i < 6041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6041(int& val) {
    const int x = 6041;
    std::cout << 6041;
    for (int i = 0; i < 6041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6042(int& val) {
    func_in_6042(val);
    const int x = 6042;
    std::cout << 6042;
    for (int i = 0; i < 6042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6042(int& val) {
    const int x = 6042;
    std::cout << 6042;
    for (int i = 0; i < 6042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6043(int& val) {
    func_in_6043(val);
    const int x = 6043;
    std::cout << 6043;
    for (int i = 0; i < 6043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6043(int& val) {
    const int x = 6043;
    std::cout << 6043;
    for (int i = 0; i < 6043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6044(int& val) {
    func_in_6044(val);
    const int x = 6044;
    std::cout << 6044;
    for (int i = 0; i < 6044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6044(int& val) {
    const int x = 6044;
    std::cout << 6044;
    for (int i = 0; i < 6044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6045(int& val) {
    func_in_6045(val);
    const int x = 6045;
    std::cout << 6045;
    for (int i = 0; i < 6045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6045(int& val) {
    const int x = 6045;
    std::cout << 6045;
    for (int i = 0; i < 6045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6046(int& val) {
    func_in_6046(val);
    const int x = 6046;
    std::cout << 6046;
    for (int i = 0; i < 6046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6046(int& val) {
    const int x = 6046;
    std::cout << 6046;
    for (int i = 0; i < 6046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6047(int& val) {
    func_in_6047(val);
    const int x = 6047;
    std::cout << 6047;
    for (int i = 0; i < 6047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6047(int& val) {
    const int x = 6047;
    std::cout << 6047;
    for (int i = 0; i < 6047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6048(int& val) {
    func_in_6048(val);
    const int x = 6048;
    std::cout << 6048;
    for (int i = 0; i < 6048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6048(int& val) {
    const int x = 6048;
    std::cout << 6048;
    for (int i = 0; i < 6048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6049(int& val) {
    func_in_6049(val);
    const int x = 6049;
    std::cout << 6049;
    for (int i = 0; i < 6049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6049(int& val) {
    const int x = 6049;
    std::cout << 6049;
    for (int i = 0; i < 6049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6050(int& val) {
    func_in_6050(val);
    const int x = 6050;
    std::cout << 6050;
    for (int i = 0; i < 6050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6050(int& val) {
    const int x = 6050;
    std::cout << 6050;
    for (int i = 0; i < 6050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6051(int& val) {
    func_in_6051(val);
    const int x = 6051;
    std::cout << 6051;
    for (int i = 0; i < 6051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6051(int& val) {
    const int x = 6051;
    std::cout << 6051;
    for (int i = 0; i < 6051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6052(int& val) {
    func_in_6052(val);
    const int x = 6052;
    std::cout << 6052;
    for (int i = 0; i < 6052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6052(int& val) {
    const int x = 6052;
    std::cout << 6052;
    for (int i = 0; i < 6052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6053(int& val) {
    func_in_6053(val);
    const int x = 6053;
    std::cout << 6053;
    for (int i = 0; i < 6053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6053(int& val) {
    const int x = 6053;
    std::cout << 6053;
    for (int i = 0; i < 6053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6054(int& val) {
    func_in_6054(val);
    const int x = 6054;
    std::cout << 6054;
    for (int i = 0; i < 6054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6054(int& val) {
    const int x = 6054;
    std::cout << 6054;
    for (int i = 0; i < 6054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6055(int& val) {
    func_in_6055(val);
    const int x = 6055;
    std::cout << 6055;
    for (int i = 0; i < 6055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6055(int& val) {
    const int x = 6055;
    std::cout << 6055;
    for (int i = 0; i < 6055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6056(int& val) {
    func_in_6056(val);
    const int x = 6056;
    std::cout << 6056;
    for (int i = 0; i < 6056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6056(int& val) {
    const int x = 6056;
    std::cout << 6056;
    for (int i = 0; i < 6056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6057(int& val) {
    func_in_6057(val);
    const int x = 6057;
    std::cout << 6057;
    for (int i = 0; i < 6057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6057(int& val) {
    const int x = 6057;
    std::cout << 6057;
    for (int i = 0; i < 6057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6058(int& val) {
    func_in_6058(val);
    const int x = 6058;
    std::cout << 6058;
    for (int i = 0; i < 6058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6058(int& val) {
    const int x = 6058;
    std::cout << 6058;
    for (int i = 0; i < 6058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6059(int& val) {
    func_in_6059(val);
    const int x = 6059;
    std::cout << 6059;
    for (int i = 0; i < 6059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6059(int& val) {
    const int x = 6059;
    std::cout << 6059;
    for (int i = 0; i < 6059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6060(int& val) {
    func_in_6060(val);
    const int x = 6060;
    std::cout << 6060;
    for (int i = 0; i < 6060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6060(int& val) {
    const int x = 6060;
    std::cout << 6060;
    for (int i = 0; i < 6060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6061(int& val) {
    func_in_6061(val);
    const int x = 6061;
    std::cout << 6061;
    for (int i = 0; i < 6061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6061(int& val) {
    const int x = 6061;
    std::cout << 6061;
    for (int i = 0; i < 6061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6062(int& val) {
    func_in_6062(val);
    const int x = 6062;
    std::cout << 6062;
    for (int i = 0; i < 6062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6062(int& val) {
    const int x = 6062;
    std::cout << 6062;
    for (int i = 0; i < 6062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6063(int& val) {
    func_in_6063(val);
    const int x = 6063;
    std::cout << 6063;
    for (int i = 0; i < 6063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6063(int& val) {
    const int x = 6063;
    std::cout << 6063;
    for (int i = 0; i < 6063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6064(int& val) {
    func_in_6064(val);
    const int x = 6064;
    std::cout << 6064;
    for (int i = 0; i < 6064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6064(int& val) {
    const int x = 6064;
    std::cout << 6064;
    for (int i = 0; i < 6064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6065(int& val) {
    func_in_6065(val);
    const int x = 6065;
    std::cout << 6065;
    for (int i = 0; i < 6065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6065(int& val) {
    const int x = 6065;
    std::cout << 6065;
    for (int i = 0; i < 6065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6066(int& val) {
    func_in_6066(val);
    const int x = 6066;
    std::cout << 6066;
    for (int i = 0; i < 6066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6066(int& val) {
    const int x = 6066;
    std::cout << 6066;
    for (int i = 0; i < 6066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6067(int& val) {
    func_in_6067(val);
    const int x = 6067;
    std::cout << 6067;
    for (int i = 0; i < 6067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6067(int& val) {
    const int x = 6067;
    std::cout << 6067;
    for (int i = 0; i < 6067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6068(int& val) {
    func_in_6068(val);
    const int x = 6068;
    std::cout << 6068;
    for (int i = 0; i < 6068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6068(int& val) {
    const int x = 6068;
    std::cout << 6068;
    for (int i = 0; i < 6068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6069(int& val) {
    func_in_6069(val);
    const int x = 6069;
    std::cout << 6069;
    for (int i = 0; i < 6069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6069(int& val) {
    const int x = 6069;
    std::cout << 6069;
    for (int i = 0; i < 6069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6070(int& val) {
    func_in_6070(val);
    const int x = 6070;
    std::cout << 6070;
    for (int i = 0; i < 6070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6070(int& val) {
    const int x = 6070;
    std::cout << 6070;
    for (int i = 0; i < 6070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6071(int& val) {
    func_in_6071(val);
    const int x = 6071;
    std::cout << 6071;
    for (int i = 0; i < 6071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6071(int& val) {
    const int x = 6071;
    std::cout << 6071;
    for (int i = 0; i < 6071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6072(int& val) {
    func_in_6072(val);
    const int x = 6072;
    std::cout << 6072;
    for (int i = 0; i < 6072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6072(int& val) {
    const int x = 6072;
    std::cout << 6072;
    for (int i = 0; i < 6072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6073(int& val) {
    func_in_6073(val);
    const int x = 6073;
    std::cout << 6073;
    for (int i = 0; i < 6073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6073(int& val) {
    const int x = 6073;
    std::cout << 6073;
    for (int i = 0; i < 6073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6074(int& val) {
    func_in_6074(val);
    const int x = 6074;
    std::cout << 6074;
    for (int i = 0; i < 6074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6074(int& val) {
    const int x = 6074;
    std::cout << 6074;
    for (int i = 0; i < 6074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6075(int& val) {
    func_in_6075(val);
    const int x = 6075;
    std::cout << 6075;
    for (int i = 0; i < 6075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6075(int& val) {
    const int x = 6075;
    std::cout << 6075;
    for (int i = 0; i < 6075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6076(int& val) {
    func_in_6076(val);
    const int x = 6076;
    std::cout << 6076;
    for (int i = 0; i < 6076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6076(int& val) {
    const int x = 6076;
    std::cout << 6076;
    for (int i = 0; i < 6076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6077(int& val) {
    func_in_6077(val);
    const int x = 6077;
    std::cout << 6077;
    for (int i = 0; i < 6077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6077(int& val) {
    const int x = 6077;
    std::cout << 6077;
    for (int i = 0; i < 6077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6078(int& val) {
    func_in_6078(val);
    const int x = 6078;
    std::cout << 6078;
    for (int i = 0; i < 6078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6078(int& val) {
    const int x = 6078;
    std::cout << 6078;
    for (int i = 0; i < 6078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6079(int& val) {
    func_in_6079(val);
    const int x = 6079;
    std::cout << 6079;
    for (int i = 0; i < 6079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6079(int& val) {
    const int x = 6079;
    std::cout << 6079;
    for (int i = 0; i < 6079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6080(int& val) {
    func_in_6080(val);
    const int x = 6080;
    std::cout << 6080;
    for (int i = 0; i < 6080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6080(int& val) {
    const int x = 6080;
    std::cout << 6080;
    for (int i = 0; i < 6080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6081(int& val) {
    func_in_6081(val);
    const int x = 6081;
    std::cout << 6081;
    for (int i = 0; i < 6081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6081(int& val) {
    const int x = 6081;
    std::cout << 6081;
    for (int i = 0; i < 6081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6082(int& val) {
    func_in_6082(val);
    const int x = 6082;
    std::cout << 6082;
    for (int i = 0; i < 6082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6082(int& val) {
    const int x = 6082;
    std::cout << 6082;
    for (int i = 0; i < 6082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6083(int& val) {
    func_in_6083(val);
    const int x = 6083;
    std::cout << 6083;
    for (int i = 0; i < 6083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6083(int& val) {
    const int x = 6083;
    std::cout << 6083;
    for (int i = 0; i < 6083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6084(int& val) {
    func_in_6084(val);
    const int x = 6084;
    std::cout << 6084;
    for (int i = 0; i < 6084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6084(int& val) {
    const int x = 6084;
    std::cout << 6084;
    for (int i = 0; i < 6084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6085(int& val) {
    func_in_6085(val);
    const int x = 6085;
    std::cout << 6085;
    for (int i = 0; i < 6085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6085(int& val) {
    const int x = 6085;
    std::cout << 6085;
    for (int i = 0; i < 6085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6086(int& val) {
    func_in_6086(val);
    const int x = 6086;
    std::cout << 6086;
    for (int i = 0; i < 6086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6086(int& val) {
    const int x = 6086;
    std::cout << 6086;
    for (int i = 0; i < 6086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6087(int& val) {
    func_in_6087(val);
    const int x = 6087;
    std::cout << 6087;
    for (int i = 0; i < 6087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6087(int& val) {
    const int x = 6087;
    std::cout << 6087;
    for (int i = 0; i < 6087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6088(int& val) {
    func_in_6088(val);
    const int x = 6088;
    std::cout << 6088;
    for (int i = 0; i < 6088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6088(int& val) {
    const int x = 6088;
    std::cout << 6088;
    for (int i = 0; i < 6088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6089(int& val) {
    func_in_6089(val);
    const int x = 6089;
    std::cout << 6089;
    for (int i = 0; i < 6089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6089(int& val) {
    const int x = 6089;
    std::cout << 6089;
    for (int i = 0; i < 6089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6090(int& val) {
    func_in_6090(val);
    const int x = 6090;
    std::cout << 6090;
    for (int i = 0; i < 6090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6090(int& val) {
    const int x = 6090;
    std::cout << 6090;
    for (int i = 0; i < 6090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6091(int& val) {
    func_in_6091(val);
    const int x = 6091;
    std::cout << 6091;
    for (int i = 0; i < 6091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6091(int& val) {
    const int x = 6091;
    std::cout << 6091;
    for (int i = 0; i < 6091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6092(int& val) {
    func_in_6092(val);
    const int x = 6092;
    std::cout << 6092;
    for (int i = 0; i < 6092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6092(int& val) {
    const int x = 6092;
    std::cout << 6092;
    for (int i = 0; i < 6092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6093(int& val) {
    func_in_6093(val);
    const int x = 6093;
    std::cout << 6093;
    for (int i = 0; i < 6093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6093(int& val) {
    const int x = 6093;
    std::cout << 6093;
    for (int i = 0; i < 6093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6094(int& val) {
    func_in_6094(val);
    const int x = 6094;
    std::cout << 6094;
    for (int i = 0; i < 6094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6094(int& val) {
    const int x = 6094;
    std::cout << 6094;
    for (int i = 0; i < 6094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6095(int& val) {
    func_in_6095(val);
    const int x = 6095;
    std::cout << 6095;
    for (int i = 0; i < 6095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6095(int& val) {
    const int x = 6095;
    std::cout << 6095;
    for (int i = 0; i < 6095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6096(int& val) {
    func_in_6096(val);
    const int x = 6096;
    std::cout << 6096;
    for (int i = 0; i < 6096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6096(int& val) {
    const int x = 6096;
    std::cout << 6096;
    for (int i = 0; i < 6096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6097(int& val) {
    func_in_6097(val);
    const int x = 6097;
    std::cout << 6097;
    for (int i = 0; i < 6097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6097(int& val) {
    const int x = 6097;
    std::cout << 6097;
    for (int i = 0; i < 6097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6098(int& val) {
    func_in_6098(val);
    const int x = 6098;
    std::cout << 6098;
    for (int i = 0; i < 6098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6098(int& val) {
    const int x = 6098;
    std::cout << 6098;
    for (int i = 0; i < 6098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6099(int& val) {
    func_in_6099(val);
    const int x = 6099;
    std::cout << 6099;
    for (int i = 0; i < 6099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6099(int& val) {
    const int x = 6099;
    std::cout << 6099;
    for (int i = 0; i < 6099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6100(int& val) {
    func_in_6100(val);
    const int x = 6100;
    std::cout << 6100;
    for (int i = 0; i < 6100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6100(int& val) {
    const int x = 6100;
    std::cout << 6100;
    for (int i = 0; i < 6100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6101(int& val) {
    func_in_6101(val);
    const int x = 6101;
    std::cout << 6101;
    for (int i = 0; i < 6101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6101(int& val) {
    const int x = 6101;
    std::cout << 6101;
    for (int i = 0; i < 6101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6102(int& val) {
    func_in_6102(val);
    const int x = 6102;
    std::cout << 6102;
    for (int i = 0; i < 6102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6102(int& val) {
    const int x = 6102;
    std::cout << 6102;
    for (int i = 0; i < 6102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6103(int& val) {
    func_in_6103(val);
    const int x = 6103;
    std::cout << 6103;
    for (int i = 0; i < 6103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6103(int& val) {
    const int x = 6103;
    std::cout << 6103;
    for (int i = 0; i < 6103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6104(int& val) {
    func_in_6104(val);
    const int x = 6104;
    std::cout << 6104;
    for (int i = 0; i < 6104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6104(int& val) {
    const int x = 6104;
    std::cout << 6104;
    for (int i = 0; i < 6104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6105(int& val) {
    func_in_6105(val);
    const int x = 6105;
    std::cout << 6105;
    for (int i = 0; i < 6105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6105(int& val) {
    const int x = 6105;
    std::cout << 6105;
    for (int i = 0; i < 6105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6106(int& val) {
    func_in_6106(val);
    const int x = 6106;
    std::cout << 6106;
    for (int i = 0; i < 6106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6106(int& val) {
    const int x = 6106;
    std::cout << 6106;
    for (int i = 0; i < 6106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6107(int& val) {
    func_in_6107(val);
    const int x = 6107;
    std::cout << 6107;
    for (int i = 0; i < 6107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6107(int& val) {
    const int x = 6107;
    std::cout << 6107;
    for (int i = 0; i < 6107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6108(int& val) {
    func_in_6108(val);
    const int x = 6108;
    std::cout << 6108;
    for (int i = 0; i < 6108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6108(int& val) {
    const int x = 6108;
    std::cout << 6108;
    for (int i = 0; i < 6108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6109(int& val) {
    func_in_6109(val);
    const int x = 6109;
    std::cout << 6109;
    for (int i = 0; i < 6109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6109(int& val) {
    const int x = 6109;
    std::cout << 6109;
    for (int i = 0; i < 6109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6110(int& val) {
    func_in_6110(val);
    const int x = 6110;
    std::cout << 6110;
    for (int i = 0; i < 6110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6110(int& val) {
    const int x = 6110;
    std::cout << 6110;
    for (int i = 0; i < 6110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6111(int& val) {
    func_in_6111(val);
    const int x = 6111;
    std::cout << 6111;
    for (int i = 0; i < 6111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6111(int& val) {
    const int x = 6111;
    std::cout << 6111;
    for (int i = 0; i < 6111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6112(int& val) {
    func_in_6112(val);
    const int x = 6112;
    std::cout << 6112;
    for (int i = 0; i < 6112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6112(int& val) {
    const int x = 6112;
    std::cout << 6112;
    for (int i = 0; i < 6112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6113(int& val) {
    func_in_6113(val);
    const int x = 6113;
    std::cout << 6113;
    for (int i = 0; i < 6113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6113(int& val) {
    const int x = 6113;
    std::cout << 6113;
    for (int i = 0; i < 6113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6114(int& val) {
    func_in_6114(val);
    const int x = 6114;
    std::cout << 6114;
    for (int i = 0; i < 6114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6114(int& val) {
    const int x = 6114;
    std::cout << 6114;
    for (int i = 0; i < 6114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6115(int& val) {
    func_in_6115(val);
    const int x = 6115;
    std::cout << 6115;
    for (int i = 0; i < 6115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6115(int& val) {
    const int x = 6115;
    std::cout << 6115;
    for (int i = 0; i < 6115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6116(int& val) {
    func_in_6116(val);
    const int x = 6116;
    std::cout << 6116;
    for (int i = 0; i < 6116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6116(int& val) {
    const int x = 6116;
    std::cout << 6116;
    for (int i = 0; i < 6116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6117(int& val) {
    func_in_6117(val);
    const int x = 6117;
    std::cout << 6117;
    for (int i = 0; i < 6117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6117(int& val) {
    const int x = 6117;
    std::cout << 6117;
    for (int i = 0; i < 6117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6118(int& val) {
    func_in_6118(val);
    const int x = 6118;
    std::cout << 6118;
    for (int i = 0; i < 6118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6118(int& val) {
    const int x = 6118;
    std::cout << 6118;
    for (int i = 0; i < 6118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6119(int& val) {
    func_in_6119(val);
    const int x = 6119;
    std::cout << 6119;
    for (int i = 0; i < 6119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6119(int& val) {
    const int x = 6119;
    std::cout << 6119;
    for (int i = 0; i < 6119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6120(int& val) {
    func_in_6120(val);
    const int x = 6120;
    std::cout << 6120;
    for (int i = 0; i < 6120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6120(int& val) {
    const int x = 6120;
    std::cout << 6120;
    for (int i = 0; i < 6120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6121(int& val) {
    func_in_6121(val);
    const int x = 6121;
    std::cout << 6121;
    for (int i = 0; i < 6121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6121(int& val) {
    const int x = 6121;
    std::cout << 6121;
    for (int i = 0; i < 6121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6122(int& val) {
    func_in_6122(val);
    const int x = 6122;
    std::cout << 6122;
    for (int i = 0; i < 6122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6122(int& val) {
    const int x = 6122;
    std::cout << 6122;
    for (int i = 0; i < 6122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6123(int& val) {
    func_in_6123(val);
    const int x = 6123;
    std::cout << 6123;
    for (int i = 0; i < 6123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6123(int& val) {
    const int x = 6123;
    std::cout << 6123;
    for (int i = 0; i < 6123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6124(int& val) {
    func_in_6124(val);
    const int x = 6124;
    std::cout << 6124;
    for (int i = 0; i < 6124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6124(int& val) {
    const int x = 6124;
    std::cout << 6124;
    for (int i = 0; i < 6124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6125(int& val) {
    func_in_6125(val);
    const int x = 6125;
    std::cout << 6125;
    for (int i = 0; i < 6125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6125(int& val) {
    const int x = 6125;
    std::cout << 6125;
    for (int i = 0; i < 6125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6126(int& val) {
    func_in_6126(val);
    const int x = 6126;
    std::cout << 6126;
    for (int i = 0; i < 6126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6126(int& val) {
    const int x = 6126;
    std::cout << 6126;
    for (int i = 0; i < 6126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6127(int& val) {
    func_in_6127(val);
    const int x = 6127;
    std::cout << 6127;
    for (int i = 0; i < 6127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6127(int& val) {
    const int x = 6127;
    std::cout << 6127;
    for (int i = 0; i < 6127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6128(int& val) {
    func_in_6128(val);
    const int x = 6128;
    std::cout << 6128;
    for (int i = 0; i < 6128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6128(int& val) {
    const int x = 6128;
    std::cout << 6128;
    for (int i = 0; i < 6128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6129(int& val) {
    func_in_6129(val);
    const int x = 6129;
    std::cout << 6129;
    for (int i = 0; i < 6129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6129(int& val) {
    const int x = 6129;
    std::cout << 6129;
    for (int i = 0; i < 6129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6130(int& val) {
    func_in_6130(val);
    const int x = 6130;
    std::cout << 6130;
    for (int i = 0; i < 6130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6130(int& val) {
    const int x = 6130;
    std::cout << 6130;
    for (int i = 0; i < 6130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6131(int& val) {
    func_in_6131(val);
    const int x = 6131;
    std::cout << 6131;
    for (int i = 0; i < 6131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6131(int& val) {
    const int x = 6131;
    std::cout << 6131;
    for (int i = 0; i < 6131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6132(int& val) {
    func_in_6132(val);
    const int x = 6132;
    std::cout << 6132;
    for (int i = 0; i < 6132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6132(int& val) {
    const int x = 6132;
    std::cout << 6132;
    for (int i = 0; i < 6132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6133(int& val) {
    func_in_6133(val);
    const int x = 6133;
    std::cout << 6133;
    for (int i = 0; i < 6133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6133(int& val) {
    const int x = 6133;
    std::cout << 6133;
    for (int i = 0; i < 6133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6134(int& val) {
    func_in_6134(val);
    const int x = 6134;
    std::cout << 6134;
    for (int i = 0; i < 6134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6134(int& val) {
    const int x = 6134;
    std::cout << 6134;
    for (int i = 0; i < 6134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6135(int& val) {
    func_in_6135(val);
    const int x = 6135;
    std::cout << 6135;
    for (int i = 0; i < 6135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6135(int& val) {
    const int x = 6135;
    std::cout << 6135;
    for (int i = 0; i < 6135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6136(int& val) {
    func_in_6136(val);
    const int x = 6136;
    std::cout << 6136;
    for (int i = 0; i < 6136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6136(int& val) {
    const int x = 6136;
    std::cout << 6136;
    for (int i = 0; i < 6136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6137(int& val) {
    func_in_6137(val);
    const int x = 6137;
    std::cout << 6137;
    for (int i = 0; i < 6137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6137(int& val) {
    const int x = 6137;
    std::cout << 6137;
    for (int i = 0; i < 6137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6138(int& val) {
    func_in_6138(val);
    const int x = 6138;
    std::cout << 6138;
    for (int i = 0; i < 6138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6138(int& val) {
    const int x = 6138;
    std::cout << 6138;
    for (int i = 0; i < 6138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6139(int& val) {
    func_in_6139(val);
    const int x = 6139;
    std::cout << 6139;
    for (int i = 0; i < 6139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6139(int& val) {
    const int x = 6139;
    std::cout << 6139;
    for (int i = 0; i < 6139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6140(int& val) {
    func_in_6140(val);
    const int x = 6140;
    std::cout << 6140;
    for (int i = 0; i < 6140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6140(int& val) {
    const int x = 6140;
    std::cout << 6140;
    for (int i = 0; i < 6140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6141(int& val) {
    func_in_6141(val);
    const int x = 6141;
    std::cout << 6141;
    for (int i = 0; i < 6141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6141(int& val) {
    const int x = 6141;
    std::cout << 6141;
    for (int i = 0; i < 6141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6142(int& val) {
    func_in_6142(val);
    const int x = 6142;
    std::cout << 6142;
    for (int i = 0; i < 6142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6142(int& val) {
    const int x = 6142;
    std::cout << 6142;
    for (int i = 0; i < 6142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6143(int& val) {
    func_in_6143(val);
    const int x = 6143;
    std::cout << 6143;
    for (int i = 0; i < 6143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6143(int& val) {
    const int x = 6143;
    std::cout << 6143;
    for (int i = 0; i < 6143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6144(int& val) {
    func_in_6144(val);
    const int x = 6144;
    std::cout << 6144;
    for (int i = 0; i < 6144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6144(int& val) {
    const int x = 6144;
    std::cout << 6144;
    for (int i = 0; i < 6144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6145(int& val) {
    func_in_6145(val);
    const int x = 6145;
    std::cout << 6145;
    for (int i = 0; i < 6145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6145(int& val) {
    const int x = 6145;
    std::cout << 6145;
    for (int i = 0; i < 6145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6146(int& val) {
    func_in_6146(val);
    const int x = 6146;
    std::cout << 6146;
    for (int i = 0; i < 6146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6146(int& val) {
    const int x = 6146;
    std::cout << 6146;
    for (int i = 0; i < 6146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6147(int& val) {
    func_in_6147(val);
    const int x = 6147;
    std::cout << 6147;
    for (int i = 0; i < 6147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6147(int& val) {
    const int x = 6147;
    std::cout << 6147;
    for (int i = 0; i < 6147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6148(int& val) {
    func_in_6148(val);
    const int x = 6148;
    std::cout << 6148;
    for (int i = 0; i < 6148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6148(int& val) {
    const int x = 6148;
    std::cout << 6148;
    for (int i = 0; i < 6148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6149(int& val) {
    func_in_6149(val);
    const int x = 6149;
    std::cout << 6149;
    for (int i = 0; i < 6149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6149(int& val) {
    const int x = 6149;
    std::cout << 6149;
    for (int i = 0; i < 6149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6150(int& val) {
    func_in_6150(val);
    const int x = 6150;
    std::cout << 6150;
    for (int i = 0; i < 6150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6150(int& val) {
    const int x = 6150;
    std::cout << 6150;
    for (int i = 0; i < 6150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6151(int& val) {
    func_in_6151(val);
    const int x = 6151;
    std::cout << 6151;
    for (int i = 0; i < 6151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6151(int& val) {
    const int x = 6151;
    std::cout << 6151;
    for (int i = 0; i < 6151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6152(int& val) {
    func_in_6152(val);
    const int x = 6152;
    std::cout << 6152;
    for (int i = 0; i < 6152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6152(int& val) {
    const int x = 6152;
    std::cout << 6152;
    for (int i = 0; i < 6152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6153(int& val) {
    func_in_6153(val);
    const int x = 6153;
    std::cout << 6153;
    for (int i = 0; i < 6153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6153(int& val) {
    const int x = 6153;
    std::cout << 6153;
    for (int i = 0; i < 6153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6154(int& val) {
    func_in_6154(val);
    const int x = 6154;
    std::cout << 6154;
    for (int i = 0; i < 6154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6154(int& val) {
    const int x = 6154;
    std::cout << 6154;
    for (int i = 0; i < 6154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6155(int& val) {
    func_in_6155(val);
    const int x = 6155;
    std::cout << 6155;
    for (int i = 0; i < 6155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6155(int& val) {
    const int x = 6155;
    std::cout << 6155;
    for (int i = 0; i < 6155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6156(int& val) {
    func_in_6156(val);
    const int x = 6156;
    std::cout << 6156;
    for (int i = 0; i < 6156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6156(int& val) {
    const int x = 6156;
    std::cout << 6156;
    for (int i = 0; i < 6156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6157(int& val) {
    func_in_6157(val);
    const int x = 6157;
    std::cout << 6157;
    for (int i = 0; i < 6157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6157(int& val) {
    const int x = 6157;
    std::cout << 6157;
    for (int i = 0; i < 6157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6158(int& val) {
    func_in_6158(val);
    const int x = 6158;
    std::cout << 6158;
    for (int i = 0; i < 6158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6158(int& val) {
    const int x = 6158;
    std::cout << 6158;
    for (int i = 0; i < 6158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6159(int& val) {
    func_in_6159(val);
    const int x = 6159;
    std::cout << 6159;
    for (int i = 0; i < 6159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6159(int& val) {
    const int x = 6159;
    std::cout << 6159;
    for (int i = 0; i < 6159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6160(int& val) {
    func_in_6160(val);
    const int x = 6160;
    std::cout << 6160;
    for (int i = 0; i < 6160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6160(int& val) {
    const int x = 6160;
    std::cout << 6160;
    for (int i = 0; i < 6160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6161(int& val) {
    func_in_6161(val);
    const int x = 6161;
    std::cout << 6161;
    for (int i = 0; i < 6161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6161(int& val) {
    const int x = 6161;
    std::cout << 6161;
    for (int i = 0; i < 6161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6162(int& val) {
    func_in_6162(val);
    const int x = 6162;
    std::cout << 6162;
    for (int i = 0; i < 6162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6162(int& val) {
    const int x = 6162;
    std::cout << 6162;
    for (int i = 0; i < 6162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6163(int& val) {
    func_in_6163(val);
    const int x = 6163;
    std::cout << 6163;
    for (int i = 0; i < 6163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6163(int& val) {
    const int x = 6163;
    std::cout << 6163;
    for (int i = 0; i < 6163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6164(int& val) {
    func_in_6164(val);
    const int x = 6164;
    std::cout << 6164;
    for (int i = 0; i < 6164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6164(int& val) {
    const int x = 6164;
    std::cout << 6164;
    for (int i = 0; i < 6164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6165(int& val) {
    func_in_6165(val);
    const int x = 6165;
    std::cout << 6165;
    for (int i = 0; i < 6165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6165(int& val) {
    const int x = 6165;
    std::cout << 6165;
    for (int i = 0; i < 6165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6166(int& val) {
    func_in_6166(val);
    const int x = 6166;
    std::cout << 6166;
    for (int i = 0; i < 6166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6166(int& val) {
    const int x = 6166;
    std::cout << 6166;
    for (int i = 0; i < 6166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6167(int& val) {
    func_in_6167(val);
    const int x = 6167;
    std::cout << 6167;
    for (int i = 0; i < 6167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6167(int& val) {
    const int x = 6167;
    std::cout << 6167;
    for (int i = 0; i < 6167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6168(int& val) {
    func_in_6168(val);
    const int x = 6168;
    std::cout << 6168;
    for (int i = 0; i < 6168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6168(int& val) {
    const int x = 6168;
    std::cout << 6168;
    for (int i = 0; i < 6168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6169(int& val) {
    func_in_6169(val);
    const int x = 6169;
    std::cout << 6169;
    for (int i = 0; i < 6169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6169(int& val) {
    const int x = 6169;
    std::cout << 6169;
    for (int i = 0; i < 6169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6170(int& val) {
    func_in_6170(val);
    const int x = 6170;
    std::cout << 6170;
    for (int i = 0; i < 6170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6170(int& val) {
    const int x = 6170;
    std::cout << 6170;
    for (int i = 0; i < 6170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6171(int& val) {
    func_in_6171(val);
    const int x = 6171;
    std::cout << 6171;
    for (int i = 0; i < 6171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6171(int& val) {
    const int x = 6171;
    std::cout << 6171;
    for (int i = 0; i < 6171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6172(int& val) {
    func_in_6172(val);
    const int x = 6172;
    std::cout << 6172;
    for (int i = 0; i < 6172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6172(int& val) {
    const int x = 6172;
    std::cout << 6172;
    for (int i = 0; i < 6172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6173(int& val) {
    func_in_6173(val);
    const int x = 6173;
    std::cout << 6173;
    for (int i = 0; i < 6173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6173(int& val) {
    const int x = 6173;
    std::cout << 6173;
    for (int i = 0; i < 6173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6174(int& val) {
    func_in_6174(val);
    const int x = 6174;
    std::cout << 6174;
    for (int i = 0; i < 6174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6174(int& val) {
    const int x = 6174;
    std::cout << 6174;
    for (int i = 0; i < 6174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6175(int& val) {
    func_in_6175(val);
    const int x = 6175;
    std::cout << 6175;
    for (int i = 0; i < 6175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6175(int& val) {
    const int x = 6175;
    std::cout << 6175;
    for (int i = 0; i < 6175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6176(int& val) {
    func_in_6176(val);
    const int x = 6176;
    std::cout << 6176;
    for (int i = 0; i < 6176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6176(int& val) {
    const int x = 6176;
    std::cout << 6176;
    for (int i = 0; i < 6176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6177(int& val) {
    func_in_6177(val);
    const int x = 6177;
    std::cout << 6177;
    for (int i = 0; i < 6177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6177(int& val) {
    const int x = 6177;
    std::cout << 6177;
    for (int i = 0; i < 6177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6178(int& val) {
    func_in_6178(val);
    const int x = 6178;
    std::cout << 6178;
    for (int i = 0; i < 6178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6178(int& val) {
    const int x = 6178;
    std::cout << 6178;
    for (int i = 0; i < 6178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6179(int& val) {
    func_in_6179(val);
    const int x = 6179;
    std::cout << 6179;
    for (int i = 0; i < 6179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6179(int& val) {
    const int x = 6179;
    std::cout << 6179;
    for (int i = 0; i < 6179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6180(int& val) {
    func_in_6180(val);
    const int x = 6180;
    std::cout << 6180;
    for (int i = 0; i < 6180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6180(int& val) {
    const int x = 6180;
    std::cout << 6180;
    for (int i = 0; i < 6180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6181(int& val) {
    func_in_6181(val);
    const int x = 6181;
    std::cout << 6181;
    for (int i = 0; i < 6181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6181(int& val) {
    const int x = 6181;
    std::cout << 6181;
    for (int i = 0; i < 6181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6182(int& val) {
    func_in_6182(val);
    const int x = 6182;
    std::cout << 6182;
    for (int i = 0; i < 6182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6182(int& val) {
    const int x = 6182;
    std::cout << 6182;
    for (int i = 0; i < 6182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6183(int& val) {
    func_in_6183(val);
    const int x = 6183;
    std::cout << 6183;
    for (int i = 0; i < 6183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6183(int& val) {
    const int x = 6183;
    std::cout << 6183;
    for (int i = 0; i < 6183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6184(int& val) {
    func_in_6184(val);
    const int x = 6184;
    std::cout << 6184;
    for (int i = 0; i < 6184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6184(int& val) {
    const int x = 6184;
    std::cout << 6184;
    for (int i = 0; i < 6184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6185(int& val) {
    func_in_6185(val);
    const int x = 6185;
    std::cout << 6185;
    for (int i = 0; i < 6185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6185(int& val) {
    const int x = 6185;
    std::cout << 6185;
    for (int i = 0; i < 6185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6186(int& val) {
    func_in_6186(val);
    const int x = 6186;
    std::cout << 6186;
    for (int i = 0; i < 6186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6186(int& val) {
    const int x = 6186;
    std::cout << 6186;
    for (int i = 0; i < 6186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6187(int& val) {
    func_in_6187(val);
    const int x = 6187;
    std::cout << 6187;
    for (int i = 0; i < 6187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6187(int& val) {
    const int x = 6187;
    std::cout << 6187;
    for (int i = 0; i < 6187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6188(int& val) {
    func_in_6188(val);
    const int x = 6188;
    std::cout << 6188;
    for (int i = 0; i < 6188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6188(int& val) {
    const int x = 6188;
    std::cout << 6188;
    for (int i = 0; i < 6188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6189(int& val) {
    func_in_6189(val);
    const int x = 6189;
    std::cout << 6189;
    for (int i = 0; i < 6189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6189(int& val) {
    const int x = 6189;
    std::cout << 6189;
    for (int i = 0; i < 6189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6190(int& val) {
    func_in_6190(val);
    const int x = 6190;
    std::cout << 6190;
    for (int i = 0; i < 6190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6190(int& val) {
    const int x = 6190;
    std::cout << 6190;
    for (int i = 0; i < 6190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6191(int& val) {
    func_in_6191(val);
    const int x = 6191;
    std::cout << 6191;
    for (int i = 0; i < 6191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6191(int& val) {
    const int x = 6191;
    std::cout << 6191;
    for (int i = 0; i < 6191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6192(int& val) {
    func_in_6192(val);
    const int x = 6192;
    std::cout << 6192;
    for (int i = 0; i < 6192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6192(int& val) {
    const int x = 6192;
    std::cout << 6192;
    for (int i = 0; i < 6192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6193(int& val) {
    func_in_6193(val);
    const int x = 6193;
    std::cout << 6193;
    for (int i = 0; i < 6193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6193(int& val) {
    const int x = 6193;
    std::cout << 6193;
    for (int i = 0; i < 6193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6194(int& val) {
    func_in_6194(val);
    const int x = 6194;
    std::cout << 6194;
    for (int i = 0; i < 6194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6194(int& val) {
    const int x = 6194;
    std::cout << 6194;
    for (int i = 0; i < 6194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6195(int& val) {
    func_in_6195(val);
    const int x = 6195;
    std::cout << 6195;
    for (int i = 0; i < 6195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6195(int& val) {
    const int x = 6195;
    std::cout << 6195;
    for (int i = 0; i < 6195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6196(int& val) {
    func_in_6196(val);
    const int x = 6196;
    std::cout << 6196;
    for (int i = 0; i < 6196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6196(int& val) {
    const int x = 6196;
    std::cout << 6196;
    for (int i = 0; i < 6196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6197(int& val) {
    func_in_6197(val);
    const int x = 6197;
    std::cout << 6197;
    for (int i = 0; i < 6197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6197(int& val) {
    const int x = 6197;
    std::cout << 6197;
    for (int i = 0; i < 6197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6198(int& val) {
    func_in_6198(val);
    const int x = 6198;
    std::cout << 6198;
    for (int i = 0; i < 6198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6198(int& val) {
    const int x = 6198;
    std::cout << 6198;
    for (int i = 0; i < 6198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6199(int& val) {
    func_in_6199(val);
    const int x = 6199;
    std::cout << 6199;
    for (int i = 0; i < 6199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6199(int& val) {
    const int x = 6199;
    std::cout << 6199;
    for (int i = 0; i < 6199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6200(int& val) {
    func_in_6200(val);
    const int x = 6200;
    std::cout << 6200;
    for (int i = 0; i < 6200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6200(int& val) {
    const int x = 6200;
    std::cout << 6200;
    for (int i = 0; i < 6200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6201(int& val) {
    func_in_6201(val);
    const int x = 6201;
    std::cout << 6201;
    for (int i = 0; i < 6201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6201(int& val) {
    const int x = 6201;
    std::cout << 6201;
    for (int i = 0; i < 6201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6202(int& val) {
    func_in_6202(val);
    const int x = 6202;
    std::cout << 6202;
    for (int i = 0; i < 6202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6202(int& val) {
    const int x = 6202;
    std::cout << 6202;
    for (int i = 0; i < 6202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6203(int& val) {
    func_in_6203(val);
    const int x = 6203;
    std::cout << 6203;
    for (int i = 0; i < 6203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6203(int& val) {
    const int x = 6203;
    std::cout << 6203;
    for (int i = 0; i < 6203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6204(int& val) {
    func_in_6204(val);
    const int x = 6204;
    std::cout << 6204;
    for (int i = 0; i < 6204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6204(int& val) {
    const int x = 6204;
    std::cout << 6204;
    for (int i = 0; i < 6204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6205(int& val) {
    func_in_6205(val);
    const int x = 6205;
    std::cout << 6205;
    for (int i = 0; i < 6205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6205(int& val) {
    const int x = 6205;
    std::cout << 6205;
    for (int i = 0; i < 6205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6206(int& val) {
    func_in_6206(val);
    const int x = 6206;
    std::cout << 6206;
    for (int i = 0; i < 6206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6206(int& val) {
    const int x = 6206;
    std::cout << 6206;
    for (int i = 0; i < 6206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6207(int& val) {
    func_in_6207(val);
    const int x = 6207;
    std::cout << 6207;
    for (int i = 0; i < 6207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6207(int& val) {
    const int x = 6207;
    std::cout << 6207;
    for (int i = 0; i < 6207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6208(int& val) {
    func_in_6208(val);
    const int x = 6208;
    std::cout << 6208;
    for (int i = 0; i < 6208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6208(int& val) {
    const int x = 6208;
    std::cout << 6208;
    for (int i = 0; i < 6208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6209(int& val) {
    func_in_6209(val);
    const int x = 6209;
    std::cout << 6209;
    for (int i = 0; i < 6209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6209(int& val) {
    const int x = 6209;
    std::cout << 6209;
    for (int i = 0; i < 6209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6210(int& val) {
    func_in_6210(val);
    const int x = 6210;
    std::cout << 6210;
    for (int i = 0; i < 6210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6210(int& val) {
    const int x = 6210;
    std::cout << 6210;
    for (int i = 0; i < 6210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6211(int& val) {
    func_in_6211(val);
    const int x = 6211;
    std::cout << 6211;
    for (int i = 0; i < 6211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6211(int& val) {
    const int x = 6211;
    std::cout << 6211;
    for (int i = 0; i < 6211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6212(int& val) {
    func_in_6212(val);
    const int x = 6212;
    std::cout << 6212;
    for (int i = 0; i < 6212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6212(int& val) {
    const int x = 6212;
    std::cout << 6212;
    for (int i = 0; i < 6212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6213(int& val) {
    func_in_6213(val);
    const int x = 6213;
    std::cout << 6213;
    for (int i = 0; i < 6213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6213(int& val) {
    const int x = 6213;
    std::cout << 6213;
    for (int i = 0; i < 6213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6214(int& val) {
    func_in_6214(val);
    const int x = 6214;
    std::cout << 6214;
    for (int i = 0; i < 6214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6214(int& val) {
    const int x = 6214;
    std::cout << 6214;
    for (int i = 0; i < 6214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6215(int& val) {
    func_in_6215(val);
    const int x = 6215;
    std::cout << 6215;
    for (int i = 0; i < 6215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6215(int& val) {
    const int x = 6215;
    std::cout << 6215;
    for (int i = 0; i < 6215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6216(int& val) {
    func_in_6216(val);
    const int x = 6216;
    std::cout << 6216;
    for (int i = 0; i < 6216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6216(int& val) {
    const int x = 6216;
    std::cout << 6216;
    for (int i = 0; i < 6216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6217(int& val) {
    func_in_6217(val);
    const int x = 6217;
    std::cout << 6217;
    for (int i = 0; i < 6217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6217(int& val) {
    const int x = 6217;
    std::cout << 6217;
    for (int i = 0; i < 6217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6218(int& val) {
    func_in_6218(val);
    const int x = 6218;
    std::cout << 6218;
    for (int i = 0; i < 6218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6218(int& val) {
    const int x = 6218;
    std::cout << 6218;
    for (int i = 0; i < 6218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6219(int& val) {
    func_in_6219(val);
    const int x = 6219;
    std::cout << 6219;
    for (int i = 0; i < 6219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6219(int& val) {
    const int x = 6219;
    std::cout << 6219;
    for (int i = 0; i < 6219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6220(int& val) {
    func_in_6220(val);
    const int x = 6220;
    std::cout << 6220;
    for (int i = 0; i < 6220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6220(int& val) {
    const int x = 6220;
    std::cout << 6220;
    for (int i = 0; i < 6220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6221(int& val) {
    func_in_6221(val);
    const int x = 6221;
    std::cout << 6221;
    for (int i = 0; i < 6221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6221(int& val) {
    const int x = 6221;
    std::cout << 6221;
    for (int i = 0; i < 6221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6222(int& val) {
    func_in_6222(val);
    const int x = 6222;
    std::cout << 6222;
    for (int i = 0; i < 6222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6222(int& val) {
    const int x = 6222;
    std::cout << 6222;
    for (int i = 0; i < 6222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6223(int& val) {
    func_in_6223(val);
    const int x = 6223;
    std::cout << 6223;
    for (int i = 0; i < 6223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6223(int& val) {
    const int x = 6223;
    std::cout << 6223;
    for (int i = 0; i < 6223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6224(int& val) {
    func_in_6224(val);
    const int x = 6224;
    std::cout << 6224;
    for (int i = 0; i < 6224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6224(int& val) {
    const int x = 6224;
    std::cout << 6224;
    for (int i = 0; i < 6224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6225(int& val) {
    func_in_6225(val);
    const int x = 6225;
    std::cout << 6225;
    for (int i = 0; i < 6225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6225(int& val) {
    const int x = 6225;
    std::cout << 6225;
    for (int i = 0; i < 6225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6226(int& val) {
    func_in_6226(val);
    const int x = 6226;
    std::cout << 6226;
    for (int i = 0; i < 6226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6226(int& val) {
    const int x = 6226;
    std::cout << 6226;
    for (int i = 0; i < 6226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6227(int& val) {
    func_in_6227(val);
    const int x = 6227;
    std::cout << 6227;
    for (int i = 0; i < 6227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6227(int& val) {
    const int x = 6227;
    std::cout << 6227;
    for (int i = 0; i < 6227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6228(int& val) {
    func_in_6228(val);
    const int x = 6228;
    std::cout << 6228;
    for (int i = 0; i < 6228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6228(int& val) {
    const int x = 6228;
    std::cout << 6228;
    for (int i = 0; i < 6228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6229(int& val) {
    func_in_6229(val);
    const int x = 6229;
    std::cout << 6229;
    for (int i = 0; i < 6229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6229(int& val) {
    const int x = 6229;
    std::cout << 6229;
    for (int i = 0; i < 6229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6230(int& val) {
    func_in_6230(val);
    const int x = 6230;
    std::cout << 6230;
    for (int i = 0; i < 6230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6230(int& val) {
    const int x = 6230;
    std::cout << 6230;
    for (int i = 0; i < 6230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6231(int& val) {
    func_in_6231(val);
    const int x = 6231;
    std::cout << 6231;
    for (int i = 0; i < 6231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6231(int& val) {
    const int x = 6231;
    std::cout << 6231;
    for (int i = 0; i < 6231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6232(int& val) {
    func_in_6232(val);
    const int x = 6232;
    std::cout << 6232;
    for (int i = 0; i < 6232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6232(int& val) {
    const int x = 6232;
    std::cout << 6232;
    for (int i = 0; i < 6232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6233(int& val) {
    func_in_6233(val);
    const int x = 6233;
    std::cout << 6233;
    for (int i = 0; i < 6233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6233(int& val) {
    const int x = 6233;
    std::cout << 6233;
    for (int i = 0; i < 6233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6234(int& val) {
    func_in_6234(val);
    const int x = 6234;
    std::cout << 6234;
    for (int i = 0; i < 6234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6234(int& val) {
    const int x = 6234;
    std::cout << 6234;
    for (int i = 0; i < 6234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6235(int& val) {
    func_in_6235(val);
    const int x = 6235;
    std::cout << 6235;
    for (int i = 0; i < 6235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6235(int& val) {
    const int x = 6235;
    std::cout << 6235;
    for (int i = 0; i < 6235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6236(int& val) {
    func_in_6236(val);
    const int x = 6236;
    std::cout << 6236;
    for (int i = 0; i < 6236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6236(int& val) {
    const int x = 6236;
    std::cout << 6236;
    for (int i = 0; i < 6236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6237(int& val) {
    func_in_6237(val);
    const int x = 6237;
    std::cout << 6237;
    for (int i = 0; i < 6237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6237(int& val) {
    const int x = 6237;
    std::cout << 6237;
    for (int i = 0; i < 6237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6238(int& val) {
    func_in_6238(val);
    const int x = 6238;
    std::cout << 6238;
    for (int i = 0; i < 6238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6238(int& val) {
    const int x = 6238;
    std::cout << 6238;
    for (int i = 0; i < 6238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6239(int& val) {
    func_in_6239(val);
    const int x = 6239;
    std::cout << 6239;
    for (int i = 0; i < 6239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6239(int& val) {
    const int x = 6239;
    std::cout << 6239;
    for (int i = 0; i < 6239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6240(int& val) {
    func_in_6240(val);
    const int x = 6240;
    std::cout << 6240;
    for (int i = 0; i < 6240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6240(int& val) {
    const int x = 6240;
    std::cout << 6240;
    for (int i = 0; i < 6240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6241(int& val) {
    func_in_6241(val);
    const int x = 6241;
    std::cout << 6241;
    for (int i = 0; i < 6241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6241(int& val) {
    const int x = 6241;
    std::cout << 6241;
    for (int i = 0; i < 6241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6242(int& val) {
    func_in_6242(val);
    const int x = 6242;
    std::cout << 6242;
    for (int i = 0; i < 6242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6242(int& val) {
    const int x = 6242;
    std::cout << 6242;
    for (int i = 0; i < 6242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6243(int& val) {
    func_in_6243(val);
    const int x = 6243;
    std::cout << 6243;
    for (int i = 0; i < 6243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6243(int& val) {
    const int x = 6243;
    std::cout << 6243;
    for (int i = 0; i < 6243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6244(int& val) {
    func_in_6244(val);
    const int x = 6244;
    std::cout << 6244;
    for (int i = 0; i < 6244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6244(int& val) {
    const int x = 6244;
    std::cout << 6244;
    for (int i = 0; i < 6244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6245(int& val) {
    func_in_6245(val);
    const int x = 6245;
    std::cout << 6245;
    for (int i = 0; i < 6245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6245(int& val) {
    const int x = 6245;
    std::cout << 6245;
    for (int i = 0; i < 6245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6246(int& val) {
    func_in_6246(val);
    const int x = 6246;
    std::cout << 6246;
    for (int i = 0; i < 6246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6246(int& val) {
    const int x = 6246;
    std::cout << 6246;
    for (int i = 0; i < 6246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6247(int& val) {
    func_in_6247(val);
    const int x = 6247;
    std::cout << 6247;
    for (int i = 0; i < 6247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6247(int& val) {
    const int x = 6247;
    std::cout << 6247;
    for (int i = 0; i < 6247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6248(int& val) {
    func_in_6248(val);
    const int x = 6248;
    std::cout << 6248;
    for (int i = 0; i < 6248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6248(int& val) {
    const int x = 6248;
    std::cout << 6248;
    for (int i = 0; i < 6248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6249(int& val) {
    func_in_6249(val);
    const int x = 6249;
    std::cout << 6249;
    for (int i = 0; i < 6249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6249(int& val) {
    const int x = 6249;
    std::cout << 6249;
    for (int i = 0; i < 6249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6250(int& val) {
    func_in_6250(val);
    const int x = 6250;
    std::cout << 6250;
    for (int i = 0; i < 6250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6250(int& val) {
    const int x = 6250;
    std::cout << 6250;
    for (int i = 0; i < 6250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6251(int& val) {
    func_in_6251(val);
    const int x = 6251;
    std::cout << 6251;
    for (int i = 0; i < 6251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6251(int& val) {
    const int x = 6251;
    std::cout << 6251;
    for (int i = 0; i < 6251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6252(int& val) {
    func_in_6252(val);
    const int x = 6252;
    std::cout << 6252;
    for (int i = 0; i < 6252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6252(int& val) {
    const int x = 6252;
    std::cout << 6252;
    for (int i = 0; i < 6252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6253(int& val) {
    func_in_6253(val);
    const int x = 6253;
    std::cout << 6253;
    for (int i = 0; i < 6253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6253(int& val) {
    const int x = 6253;
    std::cout << 6253;
    for (int i = 0; i < 6253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6254(int& val) {
    func_in_6254(val);
    const int x = 6254;
    std::cout << 6254;
    for (int i = 0; i < 6254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6254(int& val) {
    const int x = 6254;
    std::cout << 6254;
    for (int i = 0; i < 6254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6255(int& val) {
    func_in_6255(val);
    const int x = 6255;
    std::cout << 6255;
    for (int i = 0; i < 6255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6255(int& val) {
    const int x = 6255;
    std::cout << 6255;
    for (int i = 0; i < 6255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6256(int& val) {
    func_in_6256(val);
    const int x = 6256;
    std::cout << 6256;
    for (int i = 0; i < 6256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6256(int& val) {
    const int x = 6256;
    std::cout << 6256;
    for (int i = 0; i < 6256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6257(int& val) {
    func_in_6257(val);
    const int x = 6257;
    std::cout << 6257;
    for (int i = 0; i < 6257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6257(int& val) {
    const int x = 6257;
    std::cout << 6257;
    for (int i = 0; i < 6257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6258(int& val) {
    func_in_6258(val);
    const int x = 6258;
    std::cout << 6258;
    for (int i = 0; i < 6258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6258(int& val) {
    const int x = 6258;
    std::cout << 6258;
    for (int i = 0; i < 6258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6259(int& val) {
    func_in_6259(val);
    const int x = 6259;
    std::cout << 6259;
    for (int i = 0; i < 6259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6259(int& val) {
    const int x = 6259;
    std::cout << 6259;
    for (int i = 0; i < 6259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6260(int& val) {
    func_in_6260(val);
    const int x = 6260;
    std::cout << 6260;
    for (int i = 0; i < 6260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6260(int& val) {
    const int x = 6260;
    std::cout << 6260;
    for (int i = 0; i < 6260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6261(int& val) {
    func_in_6261(val);
    const int x = 6261;
    std::cout << 6261;
    for (int i = 0; i < 6261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6261(int& val) {
    const int x = 6261;
    std::cout << 6261;
    for (int i = 0; i < 6261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6262(int& val) {
    func_in_6262(val);
    const int x = 6262;
    std::cout << 6262;
    for (int i = 0; i < 6262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6262(int& val) {
    const int x = 6262;
    std::cout << 6262;
    for (int i = 0; i < 6262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6263(int& val) {
    func_in_6263(val);
    const int x = 6263;
    std::cout << 6263;
    for (int i = 0; i < 6263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6263(int& val) {
    const int x = 6263;
    std::cout << 6263;
    for (int i = 0; i < 6263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6264(int& val) {
    func_in_6264(val);
    const int x = 6264;
    std::cout << 6264;
    for (int i = 0; i < 6264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6264(int& val) {
    const int x = 6264;
    std::cout << 6264;
    for (int i = 0; i < 6264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6265(int& val) {
    func_in_6265(val);
    const int x = 6265;
    std::cout << 6265;
    for (int i = 0; i < 6265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6265(int& val) {
    const int x = 6265;
    std::cout << 6265;
    for (int i = 0; i < 6265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6266(int& val) {
    func_in_6266(val);
    const int x = 6266;
    std::cout << 6266;
    for (int i = 0; i < 6266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6266(int& val) {
    const int x = 6266;
    std::cout << 6266;
    for (int i = 0; i < 6266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6267(int& val) {
    func_in_6267(val);
    const int x = 6267;
    std::cout << 6267;
    for (int i = 0; i < 6267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6267(int& val) {
    const int x = 6267;
    std::cout << 6267;
    for (int i = 0; i < 6267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6268(int& val) {
    func_in_6268(val);
    const int x = 6268;
    std::cout << 6268;
    for (int i = 0; i < 6268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6268(int& val) {
    const int x = 6268;
    std::cout << 6268;
    for (int i = 0; i < 6268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6269(int& val) {
    func_in_6269(val);
    const int x = 6269;
    std::cout << 6269;
    for (int i = 0; i < 6269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6269(int& val) {
    const int x = 6269;
    std::cout << 6269;
    for (int i = 0; i < 6269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6270(int& val) {
    func_in_6270(val);
    const int x = 6270;
    std::cout << 6270;
    for (int i = 0; i < 6270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6270(int& val) {
    const int x = 6270;
    std::cout << 6270;
    for (int i = 0; i < 6270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6271(int& val) {
    func_in_6271(val);
    const int x = 6271;
    std::cout << 6271;
    for (int i = 0; i < 6271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6271(int& val) {
    const int x = 6271;
    std::cout << 6271;
    for (int i = 0; i < 6271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6272(int& val) {
    func_in_6272(val);
    const int x = 6272;
    std::cout << 6272;
    for (int i = 0; i < 6272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6272(int& val) {
    const int x = 6272;
    std::cout << 6272;
    for (int i = 0; i < 6272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6273(int& val) {
    func_in_6273(val);
    const int x = 6273;
    std::cout << 6273;
    for (int i = 0; i < 6273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6273(int& val) {
    const int x = 6273;
    std::cout << 6273;
    for (int i = 0; i < 6273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6274(int& val) {
    func_in_6274(val);
    const int x = 6274;
    std::cout << 6274;
    for (int i = 0; i < 6274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6274(int& val) {
    const int x = 6274;
    std::cout << 6274;
    for (int i = 0; i < 6274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6275(int& val) {
    func_in_6275(val);
    const int x = 6275;
    std::cout << 6275;
    for (int i = 0; i < 6275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6275(int& val) {
    const int x = 6275;
    std::cout << 6275;
    for (int i = 0; i < 6275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6276(int& val) {
    func_in_6276(val);
    const int x = 6276;
    std::cout << 6276;
    for (int i = 0; i < 6276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6276(int& val) {
    const int x = 6276;
    std::cout << 6276;
    for (int i = 0; i < 6276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6277(int& val) {
    func_in_6277(val);
    const int x = 6277;
    std::cout << 6277;
    for (int i = 0; i < 6277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6277(int& val) {
    const int x = 6277;
    std::cout << 6277;
    for (int i = 0; i < 6277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6278(int& val) {
    func_in_6278(val);
    const int x = 6278;
    std::cout << 6278;
    for (int i = 0; i < 6278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6278(int& val) {
    const int x = 6278;
    std::cout << 6278;
    for (int i = 0; i < 6278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6279(int& val) {
    func_in_6279(val);
    const int x = 6279;
    std::cout << 6279;
    for (int i = 0; i < 6279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6279(int& val) {
    const int x = 6279;
    std::cout << 6279;
    for (int i = 0; i < 6279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6280(int& val) {
    func_in_6280(val);
    const int x = 6280;
    std::cout << 6280;
    for (int i = 0; i < 6280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6280(int& val) {
    const int x = 6280;
    std::cout << 6280;
    for (int i = 0; i < 6280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6281(int& val) {
    func_in_6281(val);
    const int x = 6281;
    std::cout << 6281;
    for (int i = 0; i < 6281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6281(int& val) {
    const int x = 6281;
    std::cout << 6281;
    for (int i = 0; i < 6281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6282(int& val) {
    func_in_6282(val);
    const int x = 6282;
    std::cout << 6282;
    for (int i = 0; i < 6282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6282(int& val) {
    const int x = 6282;
    std::cout << 6282;
    for (int i = 0; i < 6282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6283(int& val) {
    func_in_6283(val);
    const int x = 6283;
    std::cout << 6283;
    for (int i = 0; i < 6283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6283(int& val) {
    const int x = 6283;
    std::cout << 6283;
    for (int i = 0; i < 6283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6284(int& val) {
    func_in_6284(val);
    const int x = 6284;
    std::cout << 6284;
    for (int i = 0; i < 6284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6284(int& val) {
    const int x = 6284;
    std::cout << 6284;
    for (int i = 0; i < 6284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6285(int& val) {
    func_in_6285(val);
    const int x = 6285;
    std::cout << 6285;
    for (int i = 0; i < 6285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6285(int& val) {
    const int x = 6285;
    std::cout << 6285;
    for (int i = 0; i < 6285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6286(int& val) {
    func_in_6286(val);
    const int x = 6286;
    std::cout << 6286;
    for (int i = 0; i < 6286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6286(int& val) {
    const int x = 6286;
    std::cout << 6286;
    for (int i = 0; i < 6286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6287(int& val) {
    func_in_6287(val);
    const int x = 6287;
    std::cout << 6287;
    for (int i = 0; i < 6287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6287(int& val) {
    const int x = 6287;
    std::cout << 6287;
    for (int i = 0; i < 6287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6288(int& val) {
    func_in_6288(val);
    const int x = 6288;
    std::cout << 6288;
    for (int i = 0; i < 6288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6288(int& val) {
    const int x = 6288;
    std::cout << 6288;
    for (int i = 0; i < 6288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6289(int& val) {
    func_in_6289(val);
    const int x = 6289;
    std::cout << 6289;
    for (int i = 0; i < 6289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6289(int& val) {
    const int x = 6289;
    std::cout << 6289;
    for (int i = 0; i < 6289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6290(int& val) {
    func_in_6290(val);
    const int x = 6290;
    std::cout << 6290;
    for (int i = 0; i < 6290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6290(int& val) {
    const int x = 6290;
    std::cout << 6290;
    for (int i = 0; i < 6290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6291(int& val) {
    func_in_6291(val);
    const int x = 6291;
    std::cout << 6291;
    for (int i = 0; i < 6291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6291(int& val) {
    const int x = 6291;
    std::cout << 6291;
    for (int i = 0; i < 6291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6292(int& val) {
    func_in_6292(val);
    const int x = 6292;
    std::cout << 6292;
    for (int i = 0; i < 6292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6292(int& val) {
    const int x = 6292;
    std::cout << 6292;
    for (int i = 0; i < 6292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6293(int& val) {
    func_in_6293(val);
    const int x = 6293;
    std::cout << 6293;
    for (int i = 0; i < 6293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6293(int& val) {
    const int x = 6293;
    std::cout << 6293;
    for (int i = 0; i < 6293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6294(int& val) {
    func_in_6294(val);
    const int x = 6294;
    std::cout << 6294;
    for (int i = 0; i < 6294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6294(int& val) {
    const int x = 6294;
    std::cout << 6294;
    for (int i = 0; i < 6294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6295(int& val) {
    func_in_6295(val);
    const int x = 6295;
    std::cout << 6295;
    for (int i = 0; i < 6295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6295(int& val) {
    const int x = 6295;
    std::cout << 6295;
    for (int i = 0; i < 6295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6296(int& val) {
    func_in_6296(val);
    const int x = 6296;
    std::cout << 6296;
    for (int i = 0; i < 6296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6296(int& val) {
    const int x = 6296;
    std::cout << 6296;
    for (int i = 0; i < 6296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6297(int& val) {
    func_in_6297(val);
    const int x = 6297;
    std::cout << 6297;
    for (int i = 0; i < 6297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6297(int& val) {
    const int x = 6297;
    std::cout << 6297;
    for (int i = 0; i < 6297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6298(int& val) {
    func_in_6298(val);
    const int x = 6298;
    std::cout << 6298;
    for (int i = 0; i < 6298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6298(int& val) {
    const int x = 6298;
    std::cout << 6298;
    for (int i = 0; i < 6298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6299(int& val) {
    func_in_6299(val);
    const int x = 6299;
    std::cout << 6299;
    for (int i = 0; i < 6299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6299(int& val) {
    const int x = 6299;
    std::cout << 6299;
    for (int i = 0; i < 6299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6300(int& val) {
    func_in_6300(val);
    const int x = 6300;
    std::cout << 6300;
    for (int i = 0; i < 6300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6300(int& val) {
    const int x = 6300;
    std::cout << 6300;
    for (int i = 0; i < 6300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6301(int& val) {
    func_in_6301(val);
    const int x = 6301;
    std::cout << 6301;
    for (int i = 0; i < 6301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6301(int& val) {
    const int x = 6301;
    std::cout << 6301;
    for (int i = 0; i < 6301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6302(int& val) {
    func_in_6302(val);
    const int x = 6302;
    std::cout << 6302;
    for (int i = 0; i < 6302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6302(int& val) {
    const int x = 6302;
    std::cout << 6302;
    for (int i = 0; i < 6302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6303(int& val) {
    func_in_6303(val);
    const int x = 6303;
    std::cout << 6303;
    for (int i = 0; i < 6303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6303(int& val) {
    const int x = 6303;
    std::cout << 6303;
    for (int i = 0; i < 6303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6304(int& val) {
    func_in_6304(val);
    const int x = 6304;
    std::cout << 6304;
    for (int i = 0; i < 6304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6304(int& val) {
    const int x = 6304;
    std::cout << 6304;
    for (int i = 0; i < 6304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6305(int& val) {
    func_in_6305(val);
    const int x = 6305;
    std::cout << 6305;
    for (int i = 0; i < 6305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6305(int& val) {
    const int x = 6305;
    std::cout << 6305;
    for (int i = 0; i < 6305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6306(int& val) {
    func_in_6306(val);
    const int x = 6306;
    std::cout << 6306;
    for (int i = 0; i < 6306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6306(int& val) {
    const int x = 6306;
    std::cout << 6306;
    for (int i = 0; i < 6306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6307(int& val) {
    func_in_6307(val);
    const int x = 6307;
    std::cout << 6307;
    for (int i = 0; i < 6307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6307(int& val) {
    const int x = 6307;
    std::cout << 6307;
    for (int i = 0; i < 6307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6308(int& val) {
    func_in_6308(val);
    const int x = 6308;
    std::cout << 6308;
    for (int i = 0; i < 6308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6308(int& val) {
    const int x = 6308;
    std::cout << 6308;
    for (int i = 0; i < 6308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6309(int& val) {
    func_in_6309(val);
    const int x = 6309;
    std::cout << 6309;
    for (int i = 0; i < 6309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6309(int& val) {
    const int x = 6309;
    std::cout << 6309;
    for (int i = 0; i < 6309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6310(int& val) {
    func_in_6310(val);
    const int x = 6310;
    std::cout << 6310;
    for (int i = 0; i < 6310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6310(int& val) {
    const int x = 6310;
    std::cout << 6310;
    for (int i = 0; i < 6310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6311(int& val) {
    func_in_6311(val);
    const int x = 6311;
    std::cout << 6311;
    for (int i = 0; i < 6311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6311(int& val) {
    const int x = 6311;
    std::cout << 6311;
    for (int i = 0; i < 6311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6312(int& val) {
    func_in_6312(val);
    const int x = 6312;
    std::cout << 6312;
    for (int i = 0; i < 6312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6312(int& val) {
    const int x = 6312;
    std::cout << 6312;
    for (int i = 0; i < 6312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6313(int& val) {
    func_in_6313(val);
    const int x = 6313;
    std::cout << 6313;
    for (int i = 0; i < 6313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6313(int& val) {
    const int x = 6313;
    std::cout << 6313;
    for (int i = 0; i < 6313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6314(int& val) {
    func_in_6314(val);
    const int x = 6314;
    std::cout << 6314;
    for (int i = 0; i < 6314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6314(int& val) {
    const int x = 6314;
    std::cout << 6314;
    for (int i = 0; i < 6314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6315(int& val) {
    func_in_6315(val);
    const int x = 6315;
    std::cout << 6315;
    for (int i = 0; i < 6315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6315(int& val) {
    const int x = 6315;
    std::cout << 6315;
    for (int i = 0; i < 6315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6316(int& val) {
    func_in_6316(val);
    const int x = 6316;
    std::cout << 6316;
    for (int i = 0; i < 6316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6316(int& val) {
    const int x = 6316;
    std::cout << 6316;
    for (int i = 0; i < 6316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6317(int& val) {
    func_in_6317(val);
    const int x = 6317;
    std::cout << 6317;
    for (int i = 0; i < 6317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6317(int& val) {
    const int x = 6317;
    std::cout << 6317;
    for (int i = 0; i < 6317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6318(int& val) {
    func_in_6318(val);
    const int x = 6318;
    std::cout << 6318;
    for (int i = 0; i < 6318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6318(int& val) {
    const int x = 6318;
    std::cout << 6318;
    for (int i = 0; i < 6318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6319(int& val) {
    func_in_6319(val);
    const int x = 6319;
    std::cout << 6319;
    for (int i = 0; i < 6319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6319(int& val) {
    const int x = 6319;
    std::cout << 6319;
    for (int i = 0; i < 6319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6320(int& val) {
    func_in_6320(val);
    const int x = 6320;
    std::cout << 6320;
    for (int i = 0; i < 6320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6320(int& val) {
    const int x = 6320;
    std::cout << 6320;
    for (int i = 0; i < 6320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6321(int& val) {
    func_in_6321(val);
    const int x = 6321;
    std::cout << 6321;
    for (int i = 0; i < 6321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6321(int& val) {
    const int x = 6321;
    std::cout << 6321;
    for (int i = 0; i < 6321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6322(int& val) {
    func_in_6322(val);
    const int x = 6322;
    std::cout << 6322;
    for (int i = 0; i < 6322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6322(int& val) {
    const int x = 6322;
    std::cout << 6322;
    for (int i = 0; i < 6322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6323(int& val) {
    func_in_6323(val);
    const int x = 6323;
    std::cout << 6323;
    for (int i = 0; i < 6323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6323(int& val) {
    const int x = 6323;
    std::cout << 6323;
    for (int i = 0; i < 6323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6324(int& val) {
    func_in_6324(val);
    const int x = 6324;
    std::cout << 6324;
    for (int i = 0; i < 6324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6324(int& val) {
    const int x = 6324;
    std::cout << 6324;
    for (int i = 0; i < 6324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6325(int& val) {
    func_in_6325(val);
    const int x = 6325;
    std::cout << 6325;
    for (int i = 0; i < 6325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6325(int& val) {
    const int x = 6325;
    std::cout << 6325;
    for (int i = 0; i < 6325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6326(int& val) {
    func_in_6326(val);
    const int x = 6326;
    std::cout << 6326;
    for (int i = 0; i < 6326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6326(int& val) {
    const int x = 6326;
    std::cout << 6326;
    for (int i = 0; i < 6326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6327(int& val) {
    func_in_6327(val);
    const int x = 6327;
    std::cout << 6327;
    for (int i = 0; i < 6327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6327(int& val) {
    const int x = 6327;
    std::cout << 6327;
    for (int i = 0; i < 6327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6328(int& val) {
    func_in_6328(val);
    const int x = 6328;
    std::cout << 6328;
    for (int i = 0; i < 6328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6328(int& val) {
    const int x = 6328;
    std::cout << 6328;
    for (int i = 0; i < 6328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6329(int& val) {
    func_in_6329(val);
    const int x = 6329;
    std::cout << 6329;
    for (int i = 0; i < 6329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6329(int& val) {
    const int x = 6329;
    std::cout << 6329;
    for (int i = 0; i < 6329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6330(int& val) {
    func_in_6330(val);
    const int x = 6330;
    std::cout << 6330;
    for (int i = 0; i < 6330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6330(int& val) {
    const int x = 6330;
    std::cout << 6330;
    for (int i = 0; i < 6330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6331(int& val) {
    func_in_6331(val);
    const int x = 6331;
    std::cout << 6331;
    for (int i = 0; i < 6331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6331(int& val) {
    const int x = 6331;
    std::cout << 6331;
    for (int i = 0; i < 6331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6332(int& val) {
    func_in_6332(val);
    const int x = 6332;
    std::cout << 6332;
    for (int i = 0; i < 6332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6332(int& val) {
    const int x = 6332;
    std::cout << 6332;
    for (int i = 0; i < 6332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6333(int& val) {
    func_in_6333(val);
    const int x = 6333;
    std::cout << 6333;
    for (int i = 0; i < 6333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6333(int& val) {
    const int x = 6333;
    std::cout << 6333;
    for (int i = 0; i < 6333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6334(int& val) {
    func_in_6334(val);
    const int x = 6334;
    std::cout << 6334;
    for (int i = 0; i < 6334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6334(int& val) {
    const int x = 6334;
    std::cout << 6334;
    for (int i = 0; i < 6334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6335(int& val) {
    func_in_6335(val);
    const int x = 6335;
    std::cout << 6335;
    for (int i = 0; i < 6335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6335(int& val) {
    const int x = 6335;
    std::cout << 6335;
    for (int i = 0; i < 6335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6336(int& val) {
    func_in_6336(val);
    const int x = 6336;
    std::cout << 6336;
    for (int i = 0; i < 6336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6336(int& val) {
    const int x = 6336;
    std::cout << 6336;
    for (int i = 0; i < 6336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6337(int& val) {
    func_in_6337(val);
    const int x = 6337;
    std::cout << 6337;
    for (int i = 0; i < 6337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6337(int& val) {
    const int x = 6337;
    std::cout << 6337;
    for (int i = 0; i < 6337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6338(int& val) {
    func_in_6338(val);
    const int x = 6338;
    std::cout << 6338;
    for (int i = 0; i < 6338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6338(int& val) {
    const int x = 6338;
    std::cout << 6338;
    for (int i = 0; i < 6338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6339(int& val) {
    func_in_6339(val);
    const int x = 6339;
    std::cout << 6339;
    for (int i = 0; i < 6339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6339(int& val) {
    const int x = 6339;
    std::cout << 6339;
    for (int i = 0; i < 6339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6340(int& val) {
    func_in_6340(val);
    const int x = 6340;
    std::cout << 6340;
    for (int i = 0; i < 6340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6340(int& val) {
    const int x = 6340;
    std::cout << 6340;
    for (int i = 0; i < 6340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6341(int& val) {
    func_in_6341(val);
    const int x = 6341;
    std::cout << 6341;
    for (int i = 0; i < 6341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6341(int& val) {
    const int x = 6341;
    std::cout << 6341;
    for (int i = 0; i < 6341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6342(int& val) {
    func_in_6342(val);
    const int x = 6342;
    std::cout << 6342;
    for (int i = 0; i < 6342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6342(int& val) {
    const int x = 6342;
    std::cout << 6342;
    for (int i = 0; i < 6342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6343(int& val) {
    func_in_6343(val);
    const int x = 6343;
    std::cout << 6343;
    for (int i = 0; i < 6343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6343(int& val) {
    const int x = 6343;
    std::cout << 6343;
    for (int i = 0; i < 6343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6344(int& val) {
    func_in_6344(val);
    const int x = 6344;
    std::cout << 6344;
    for (int i = 0; i < 6344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6344(int& val) {
    const int x = 6344;
    std::cout << 6344;
    for (int i = 0; i < 6344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6345(int& val) {
    func_in_6345(val);
    const int x = 6345;
    std::cout << 6345;
    for (int i = 0; i < 6345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6345(int& val) {
    const int x = 6345;
    std::cout << 6345;
    for (int i = 0; i < 6345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6346(int& val) {
    func_in_6346(val);
    const int x = 6346;
    std::cout << 6346;
    for (int i = 0; i < 6346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6346(int& val) {
    const int x = 6346;
    std::cout << 6346;
    for (int i = 0; i < 6346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6347(int& val) {
    func_in_6347(val);
    const int x = 6347;
    std::cout << 6347;
    for (int i = 0; i < 6347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6347(int& val) {
    const int x = 6347;
    std::cout << 6347;
    for (int i = 0; i < 6347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6348(int& val) {
    func_in_6348(val);
    const int x = 6348;
    std::cout << 6348;
    for (int i = 0; i < 6348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6348(int& val) {
    const int x = 6348;
    std::cout << 6348;
    for (int i = 0; i < 6348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6349(int& val) {
    func_in_6349(val);
    const int x = 6349;
    std::cout << 6349;
    for (int i = 0; i < 6349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6349(int& val) {
    const int x = 6349;
    std::cout << 6349;
    for (int i = 0; i < 6349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6350(int& val) {
    func_in_6350(val);
    const int x = 6350;
    std::cout << 6350;
    for (int i = 0; i < 6350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6350(int& val) {
    const int x = 6350;
    std::cout << 6350;
    for (int i = 0; i < 6350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6351(int& val) {
    func_in_6351(val);
    const int x = 6351;
    std::cout << 6351;
    for (int i = 0; i < 6351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6351(int& val) {
    const int x = 6351;
    std::cout << 6351;
    for (int i = 0; i < 6351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6352(int& val) {
    func_in_6352(val);
    const int x = 6352;
    std::cout << 6352;
    for (int i = 0; i < 6352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6352(int& val) {
    const int x = 6352;
    std::cout << 6352;
    for (int i = 0; i < 6352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6353(int& val) {
    func_in_6353(val);
    const int x = 6353;
    std::cout << 6353;
    for (int i = 0; i < 6353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6353(int& val) {
    const int x = 6353;
    std::cout << 6353;
    for (int i = 0; i < 6353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6354(int& val) {
    func_in_6354(val);
    const int x = 6354;
    std::cout << 6354;
    for (int i = 0; i < 6354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6354(int& val) {
    const int x = 6354;
    std::cout << 6354;
    for (int i = 0; i < 6354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6355(int& val) {
    func_in_6355(val);
    const int x = 6355;
    std::cout << 6355;
    for (int i = 0; i < 6355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6355(int& val) {
    const int x = 6355;
    std::cout << 6355;
    for (int i = 0; i < 6355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6356(int& val) {
    func_in_6356(val);
    const int x = 6356;
    std::cout << 6356;
    for (int i = 0; i < 6356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6356(int& val) {
    const int x = 6356;
    std::cout << 6356;
    for (int i = 0; i < 6356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6357(int& val) {
    func_in_6357(val);
    const int x = 6357;
    std::cout << 6357;
    for (int i = 0; i < 6357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6357(int& val) {
    const int x = 6357;
    std::cout << 6357;
    for (int i = 0; i < 6357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6358(int& val) {
    func_in_6358(val);
    const int x = 6358;
    std::cout << 6358;
    for (int i = 0; i < 6358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6358(int& val) {
    const int x = 6358;
    std::cout << 6358;
    for (int i = 0; i < 6358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6359(int& val) {
    func_in_6359(val);
    const int x = 6359;
    std::cout << 6359;
    for (int i = 0; i < 6359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6359(int& val) {
    const int x = 6359;
    std::cout << 6359;
    for (int i = 0; i < 6359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6360(int& val) {
    func_in_6360(val);
    const int x = 6360;
    std::cout << 6360;
    for (int i = 0; i < 6360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6360(int& val) {
    const int x = 6360;
    std::cout << 6360;
    for (int i = 0; i < 6360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6361(int& val) {
    func_in_6361(val);
    const int x = 6361;
    std::cout << 6361;
    for (int i = 0; i < 6361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6361(int& val) {
    const int x = 6361;
    std::cout << 6361;
    for (int i = 0; i < 6361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6362(int& val) {
    func_in_6362(val);
    const int x = 6362;
    std::cout << 6362;
    for (int i = 0; i < 6362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6362(int& val) {
    const int x = 6362;
    std::cout << 6362;
    for (int i = 0; i < 6362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6363(int& val) {
    func_in_6363(val);
    const int x = 6363;
    std::cout << 6363;
    for (int i = 0; i < 6363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6363(int& val) {
    const int x = 6363;
    std::cout << 6363;
    for (int i = 0; i < 6363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6364(int& val) {
    func_in_6364(val);
    const int x = 6364;
    std::cout << 6364;
    for (int i = 0; i < 6364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6364(int& val) {
    const int x = 6364;
    std::cout << 6364;
    for (int i = 0; i < 6364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6365(int& val) {
    func_in_6365(val);
    const int x = 6365;
    std::cout << 6365;
    for (int i = 0; i < 6365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6365(int& val) {
    const int x = 6365;
    std::cout << 6365;
    for (int i = 0; i < 6365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6366(int& val) {
    func_in_6366(val);
    const int x = 6366;
    std::cout << 6366;
    for (int i = 0; i < 6366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6366(int& val) {
    const int x = 6366;
    std::cout << 6366;
    for (int i = 0; i < 6366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6367(int& val) {
    func_in_6367(val);
    const int x = 6367;
    std::cout << 6367;
    for (int i = 0; i < 6367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6367(int& val) {
    const int x = 6367;
    std::cout << 6367;
    for (int i = 0; i < 6367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6368(int& val) {
    func_in_6368(val);
    const int x = 6368;
    std::cout << 6368;
    for (int i = 0; i < 6368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6368(int& val) {
    const int x = 6368;
    std::cout << 6368;
    for (int i = 0; i < 6368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6369(int& val) {
    func_in_6369(val);
    const int x = 6369;
    std::cout << 6369;
    for (int i = 0; i < 6369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6369(int& val) {
    const int x = 6369;
    std::cout << 6369;
    for (int i = 0; i < 6369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6370(int& val) {
    func_in_6370(val);
    const int x = 6370;
    std::cout << 6370;
    for (int i = 0; i < 6370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6370(int& val) {
    const int x = 6370;
    std::cout << 6370;
    for (int i = 0; i < 6370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6371(int& val) {
    func_in_6371(val);
    const int x = 6371;
    std::cout << 6371;
    for (int i = 0; i < 6371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6371(int& val) {
    const int x = 6371;
    std::cout << 6371;
    for (int i = 0; i < 6371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6372(int& val) {
    func_in_6372(val);
    const int x = 6372;
    std::cout << 6372;
    for (int i = 0; i < 6372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6372(int& val) {
    const int x = 6372;
    std::cout << 6372;
    for (int i = 0; i < 6372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6373(int& val) {
    func_in_6373(val);
    const int x = 6373;
    std::cout << 6373;
    for (int i = 0; i < 6373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6373(int& val) {
    const int x = 6373;
    std::cout << 6373;
    for (int i = 0; i < 6373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6374(int& val) {
    func_in_6374(val);
    const int x = 6374;
    std::cout << 6374;
    for (int i = 0; i < 6374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6374(int& val) {
    const int x = 6374;
    std::cout << 6374;
    for (int i = 0; i < 6374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6375(int& val) {
    func_in_6375(val);
    const int x = 6375;
    std::cout << 6375;
    for (int i = 0; i < 6375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6375(int& val) {
    const int x = 6375;
    std::cout << 6375;
    for (int i = 0; i < 6375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6376(int& val) {
    func_in_6376(val);
    const int x = 6376;
    std::cout << 6376;
    for (int i = 0; i < 6376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6376(int& val) {
    const int x = 6376;
    std::cout << 6376;
    for (int i = 0; i < 6376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6377(int& val) {
    func_in_6377(val);
    const int x = 6377;
    std::cout << 6377;
    for (int i = 0; i < 6377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6377(int& val) {
    const int x = 6377;
    std::cout << 6377;
    for (int i = 0; i < 6377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6378(int& val) {
    func_in_6378(val);
    const int x = 6378;
    std::cout << 6378;
    for (int i = 0; i < 6378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6378(int& val) {
    const int x = 6378;
    std::cout << 6378;
    for (int i = 0; i < 6378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6379(int& val) {
    func_in_6379(val);
    const int x = 6379;
    std::cout << 6379;
    for (int i = 0; i < 6379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6379(int& val) {
    const int x = 6379;
    std::cout << 6379;
    for (int i = 0; i < 6379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6380(int& val) {
    func_in_6380(val);
    const int x = 6380;
    std::cout << 6380;
    for (int i = 0; i < 6380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6380(int& val) {
    const int x = 6380;
    std::cout << 6380;
    for (int i = 0; i < 6380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6381(int& val) {
    func_in_6381(val);
    const int x = 6381;
    std::cout << 6381;
    for (int i = 0; i < 6381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6381(int& val) {
    const int x = 6381;
    std::cout << 6381;
    for (int i = 0; i < 6381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6382(int& val) {
    func_in_6382(val);
    const int x = 6382;
    std::cout << 6382;
    for (int i = 0; i < 6382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6382(int& val) {
    const int x = 6382;
    std::cout << 6382;
    for (int i = 0; i < 6382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6383(int& val) {
    func_in_6383(val);
    const int x = 6383;
    std::cout << 6383;
    for (int i = 0; i < 6383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6383(int& val) {
    const int x = 6383;
    std::cout << 6383;
    for (int i = 0; i < 6383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6384(int& val) {
    func_in_6384(val);
    const int x = 6384;
    std::cout << 6384;
    for (int i = 0; i < 6384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6384(int& val) {
    const int x = 6384;
    std::cout << 6384;
    for (int i = 0; i < 6384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6385(int& val) {
    func_in_6385(val);
    const int x = 6385;
    std::cout << 6385;
    for (int i = 0; i < 6385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6385(int& val) {
    const int x = 6385;
    std::cout << 6385;
    for (int i = 0; i < 6385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6386(int& val) {
    func_in_6386(val);
    const int x = 6386;
    std::cout << 6386;
    for (int i = 0; i < 6386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6386(int& val) {
    const int x = 6386;
    std::cout << 6386;
    for (int i = 0; i < 6386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6387(int& val) {
    func_in_6387(val);
    const int x = 6387;
    std::cout << 6387;
    for (int i = 0; i < 6387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6387(int& val) {
    const int x = 6387;
    std::cout << 6387;
    for (int i = 0; i < 6387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6388(int& val) {
    func_in_6388(val);
    const int x = 6388;
    std::cout << 6388;
    for (int i = 0; i < 6388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6388(int& val) {
    const int x = 6388;
    std::cout << 6388;
    for (int i = 0; i < 6388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6389(int& val) {
    func_in_6389(val);
    const int x = 6389;
    std::cout << 6389;
    for (int i = 0; i < 6389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6389(int& val) {
    const int x = 6389;
    std::cout << 6389;
    for (int i = 0; i < 6389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6390(int& val) {
    func_in_6390(val);
    const int x = 6390;
    std::cout << 6390;
    for (int i = 0; i < 6390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6390(int& val) {
    const int x = 6390;
    std::cout << 6390;
    for (int i = 0; i < 6390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6391(int& val) {
    func_in_6391(val);
    const int x = 6391;
    std::cout << 6391;
    for (int i = 0; i < 6391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6391(int& val) {
    const int x = 6391;
    std::cout << 6391;
    for (int i = 0; i < 6391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6392(int& val) {
    func_in_6392(val);
    const int x = 6392;
    std::cout << 6392;
    for (int i = 0; i < 6392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6392(int& val) {
    const int x = 6392;
    std::cout << 6392;
    for (int i = 0; i < 6392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6393(int& val) {
    func_in_6393(val);
    const int x = 6393;
    std::cout << 6393;
    for (int i = 0; i < 6393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6393(int& val) {
    const int x = 6393;
    std::cout << 6393;
    for (int i = 0; i < 6393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6394(int& val) {
    func_in_6394(val);
    const int x = 6394;
    std::cout << 6394;
    for (int i = 0; i < 6394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6394(int& val) {
    const int x = 6394;
    std::cout << 6394;
    for (int i = 0; i < 6394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6395(int& val) {
    func_in_6395(val);
    const int x = 6395;
    std::cout << 6395;
    for (int i = 0; i < 6395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6395(int& val) {
    const int x = 6395;
    std::cout << 6395;
    for (int i = 0; i < 6395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6396(int& val) {
    func_in_6396(val);
    const int x = 6396;
    std::cout << 6396;
    for (int i = 0; i < 6396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6396(int& val) {
    const int x = 6396;
    std::cout << 6396;
    for (int i = 0; i < 6396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6397(int& val) {
    func_in_6397(val);
    const int x = 6397;
    std::cout << 6397;
    for (int i = 0; i < 6397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6397(int& val) {
    const int x = 6397;
    std::cout << 6397;
    for (int i = 0; i < 6397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6398(int& val) {
    func_in_6398(val);
    const int x = 6398;
    std::cout << 6398;
    for (int i = 0; i < 6398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6398(int& val) {
    const int x = 6398;
    std::cout << 6398;
    for (int i = 0; i < 6398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6399(int& val) {
    func_in_6399(val);
    const int x = 6399;
    std::cout << 6399;
    for (int i = 0; i < 6399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6399(int& val) {
    const int x = 6399;
    std::cout << 6399;
    for (int i = 0; i < 6399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6400(int& val) {
    func_in_6400(val);
    const int x = 6400;
    std::cout << 6400;
    for (int i = 0; i < 6400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6400(int& val) {
    const int x = 6400;
    std::cout << 6400;
    for (int i = 0; i < 6400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6401(int& val) {
    func_in_6401(val);
    const int x = 6401;
    std::cout << 6401;
    for (int i = 0; i < 6401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6401(int& val) {
    const int x = 6401;
    std::cout << 6401;
    for (int i = 0; i < 6401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6402(int& val) {
    func_in_6402(val);
    const int x = 6402;
    std::cout << 6402;
    for (int i = 0; i < 6402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6402(int& val) {
    const int x = 6402;
    std::cout << 6402;
    for (int i = 0; i < 6402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6403(int& val) {
    func_in_6403(val);
    const int x = 6403;
    std::cout << 6403;
    for (int i = 0; i < 6403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6403(int& val) {
    const int x = 6403;
    std::cout << 6403;
    for (int i = 0; i < 6403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6404(int& val) {
    func_in_6404(val);
    const int x = 6404;
    std::cout << 6404;
    for (int i = 0; i < 6404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6404(int& val) {
    const int x = 6404;
    std::cout << 6404;
    for (int i = 0; i < 6404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6405(int& val) {
    func_in_6405(val);
    const int x = 6405;
    std::cout << 6405;
    for (int i = 0; i < 6405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6405(int& val) {
    const int x = 6405;
    std::cout << 6405;
    for (int i = 0; i < 6405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6406(int& val) {
    func_in_6406(val);
    const int x = 6406;
    std::cout << 6406;
    for (int i = 0; i < 6406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6406(int& val) {
    const int x = 6406;
    std::cout << 6406;
    for (int i = 0; i < 6406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6407(int& val) {
    func_in_6407(val);
    const int x = 6407;
    std::cout << 6407;
    for (int i = 0; i < 6407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6407(int& val) {
    const int x = 6407;
    std::cout << 6407;
    for (int i = 0; i < 6407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6408(int& val) {
    func_in_6408(val);
    const int x = 6408;
    std::cout << 6408;
    for (int i = 0; i < 6408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6408(int& val) {
    const int x = 6408;
    std::cout << 6408;
    for (int i = 0; i < 6408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6409(int& val) {
    func_in_6409(val);
    const int x = 6409;
    std::cout << 6409;
    for (int i = 0; i < 6409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6409(int& val) {
    const int x = 6409;
    std::cout << 6409;
    for (int i = 0; i < 6409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6410(int& val) {
    func_in_6410(val);
    const int x = 6410;
    std::cout << 6410;
    for (int i = 0; i < 6410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6410(int& val) {
    const int x = 6410;
    std::cout << 6410;
    for (int i = 0; i < 6410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6411(int& val) {
    func_in_6411(val);
    const int x = 6411;
    std::cout << 6411;
    for (int i = 0; i < 6411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6411(int& val) {
    const int x = 6411;
    std::cout << 6411;
    for (int i = 0; i < 6411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6412(int& val) {
    func_in_6412(val);
    const int x = 6412;
    std::cout << 6412;
    for (int i = 0; i < 6412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6412(int& val) {
    const int x = 6412;
    std::cout << 6412;
    for (int i = 0; i < 6412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6413(int& val) {
    func_in_6413(val);
    const int x = 6413;
    std::cout << 6413;
    for (int i = 0; i < 6413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6413(int& val) {
    const int x = 6413;
    std::cout << 6413;
    for (int i = 0; i < 6413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6414(int& val) {
    func_in_6414(val);
    const int x = 6414;
    std::cout << 6414;
    for (int i = 0; i < 6414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6414(int& val) {
    const int x = 6414;
    std::cout << 6414;
    for (int i = 0; i < 6414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6415(int& val) {
    func_in_6415(val);
    const int x = 6415;
    std::cout << 6415;
    for (int i = 0; i < 6415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6415(int& val) {
    const int x = 6415;
    std::cout << 6415;
    for (int i = 0; i < 6415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6416(int& val) {
    func_in_6416(val);
    const int x = 6416;
    std::cout << 6416;
    for (int i = 0; i < 6416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6416(int& val) {
    const int x = 6416;
    std::cout << 6416;
    for (int i = 0; i < 6416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6417(int& val) {
    func_in_6417(val);
    const int x = 6417;
    std::cout << 6417;
    for (int i = 0; i < 6417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6417(int& val) {
    const int x = 6417;
    std::cout << 6417;
    for (int i = 0; i < 6417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6418(int& val) {
    func_in_6418(val);
    const int x = 6418;
    std::cout << 6418;
    for (int i = 0; i < 6418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6418(int& val) {
    const int x = 6418;
    std::cout << 6418;
    for (int i = 0; i < 6418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6419(int& val) {
    func_in_6419(val);
    const int x = 6419;
    std::cout << 6419;
    for (int i = 0; i < 6419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6419(int& val) {
    const int x = 6419;
    std::cout << 6419;
    for (int i = 0; i < 6419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6420(int& val) {
    func_in_6420(val);
    const int x = 6420;
    std::cout << 6420;
    for (int i = 0; i < 6420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6420(int& val) {
    const int x = 6420;
    std::cout << 6420;
    for (int i = 0; i < 6420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6421(int& val) {
    func_in_6421(val);
    const int x = 6421;
    std::cout << 6421;
    for (int i = 0; i < 6421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6421(int& val) {
    const int x = 6421;
    std::cout << 6421;
    for (int i = 0; i < 6421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6422(int& val) {
    func_in_6422(val);
    const int x = 6422;
    std::cout << 6422;
    for (int i = 0; i < 6422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6422(int& val) {
    const int x = 6422;
    std::cout << 6422;
    for (int i = 0; i < 6422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6423(int& val) {
    func_in_6423(val);
    const int x = 6423;
    std::cout << 6423;
    for (int i = 0; i < 6423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6423(int& val) {
    const int x = 6423;
    std::cout << 6423;
    for (int i = 0; i < 6423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6424(int& val) {
    func_in_6424(val);
    const int x = 6424;
    std::cout << 6424;
    for (int i = 0; i < 6424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6424(int& val) {
    const int x = 6424;
    std::cout << 6424;
    for (int i = 0; i < 6424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6425(int& val) {
    func_in_6425(val);
    const int x = 6425;
    std::cout << 6425;
    for (int i = 0; i < 6425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6425(int& val) {
    const int x = 6425;
    std::cout << 6425;
    for (int i = 0; i < 6425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6426(int& val) {
    func_in_6426(val);
    const int x = 6426;
    std::cout << 6426;
    for (int i = 0; i < 6426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6426(int& val) {
    const int x = 6426;
    std::cout << 6426;
    for (int i = 0; i < 6426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6427(int& val) {
    func_in_6427(val);
    const int x = 6427;
    std::cout << 6427;
    for (int i = 0; i < 6427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6427(int& val) {
    const int x = 6427;
    std::cout << 6427;
    for (int i = 0; i < 6427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6428(int& val) {
    func_in_6428(val);
    const int x = 6428;
    std::cout << 6428;
    for (int i = 0; i < 6428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6428(int& val) {
    const int x = 6428;
    std::cout << 6428;
    for (int i = 0; i < 6428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6429(int& val) {
    func_in_6429(val);
    const int x = 6429;
    std::cout << 6429;
    for (int i = 0; i < 6429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6429(int& val) {
    const int x = 6429;
    std::cout << 6429;
    for (int i = 0; i < 6429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6430(int& val) {
    func_in_6430(val);
    const int x = 6430;
    std::cout << 6430;
    for (int i = 0; i < 6430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6430(int& val) {
    const int x = 6430;
    std::cout << 6430;
    for (int i = 0; i < 6430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6431(int& val) {
    func_in_6431(val);
    const int x = 6431;
    std::cout << 6431;
    for (int i = 0; i < 6431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6431(int& val) {
    const int x = 6431;
    std::cout << 6431;
    for (int i = 0; i < 6431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6432(int& val) {
    func_in_6432(val);
    const int x = 6432;
    std::cout << 6432;
    for (int i = 0; i < 6432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6432(int& val) {
    const int x = 6432;
    std::cout << 6432;
    for (int i = 0; i < 6432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6433(int& val) {
    func_in_6433(val);
    const int x = 6433;
    std::cout << 6433;
    for (int i = 0; i < 6433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6433(int& val) {
    const int x = 6433;
    std::cout << 6433;
    for (int i = 0; i < 6433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6434(int& val) {
    func_in_6434(val);
    const int x = 6434;
    std::cout << 6434;
    for (int i = 0; i < 6434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6434(int& val) {
    const int x = 6434;
    std::cout << 6434;
    for (int i = 0; i < 6434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6435(int& val) {
    func_in_6435(val);
    const int x = 6435;
    std::cout << 6435;
    for (int i = 0; i < 6435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6435(int& val) {
    const int x = 6435;
    std::cout << 6435;
    for (int i = 0; i < 6435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6436(int& val) {
    func_in_6436(val);
    const int x = 6436;
    std::cout << 6436;
    for (int i = 0; i < 6436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6436(int& val) {
    const int x = 6436;
    std::cout << 6436;
    for (int i = 0; i < 6436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6437(int& val) {
    func_in_6437(val);
    const int x = 6437;
    std::cout << 6437;
    for (int i = 0; i < 6437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6437(int& val) {
    const int x = 6437;
    std::cout << 6437;
    for (int i = 0; i < 6437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6438(int& val) {
    func_in_6438(val);
    const int x = 6438;
    std::cout << 6438;
    for (int i = 0; i < 6438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6438(int& val) {
    const int x = 6438;
    std::cout << 6438;
    for (int i = 0; i < 6438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6439(int& val) {
    func_in_6439(val);
    const int x = 6439;
    std::cout << 6439;
    for (int i = 0; i < 6439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6439(int& val) {
    const int x = 6439;
    std::cout << 6439;
    for (int i = 0; i < 6439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6440(int& val) {
    func_in_6440(val);
    const int x = 6440;
    std::cout << 6440;
    for (int i = 0; i < 6440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6440(int& val) {
    const int x = 6440;
    std::cout << 6440;
    for (int i = 0; i < 6440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6441(int& val) {
    func_in_6441(val);
    const int x = 6441;
    std::cout << 6441;
    for (int i = 0; i < 6441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6441(int& val) {
    const int x = 6441;
    std::cout << 6441;
    for (int i = 0; i < 6441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6442(int& val) {
    func_in_6442(val);
    const int x = 6442;
    std::cout << 6442;
    for (int i = 0; i < 6442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6442(int& val) {
    const int x = 6442;
    std::cout << 6442;
    for (int i = 0; i < 6442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6443(int& val) {
    func_in_6443(val);
    const int x = 6443;
    std::cout << 6443;
    for (int i = 0; i < 6443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6443(int& val) {
    const int x = 6443;
    std::cout << 6443;
    for (int i = 0; i < 6443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6444(int& val) {
    func_in_6444(val);
    const int x = 6444;
    std::cout << 6444;
    for (int i = 0; i < 6444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6444(int& val) {
    const int x = 6444;
    std::cout << 6444;
    for (int i = 0; i < 6444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6445(int& val) {
    func_in_6445(val);
    const int x = 6445;
    std::cout << 6445;
    for (int i = 0; i < 6445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6445(int& val) {
    const int x = 6445;
    std::cout << 6445;
    for (int i = 0; i < 6445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6446(int& val) {
    func_in_6446(val);
    const int x = 6446;
    std::cout << 6446;
    for (int i = 0; i < 6446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6446(int& val) {
    const int x = 6446;
    std::cout << 6446;
    for (int i = 0; i < 6446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6447(int& val) {
    func_in_6447(val);
    const int x = 6447;
    std::cout << 6447;
    for (int i = 0; i < 6447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6447(int& val) {
    const int x = 6447;
    std::cout << 6447;
    for (int i = 0; i < 6447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6448(int& val) {
    func_in_6448(val);
    const int x = 6448;
    std::cout << 6448;
    for (int i = 0; i < 6448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6448(int& val) {
    const int x = 6448;
    std::cout << 6448;
    for (int i = 0; i < 6448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6449(int& val) {
    func_in_6449(val);
    const int x = 6449;
    std::cout << 6449;
    for (int i = 0; i < 6449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6449(int& val) {
    const int x = 6449;
    std::cout << 6449;
    for (int i = 0; i < 6449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6450(int& val) {
    func_in_6450(val);
    const int x = 6450;
    std::cout << 6450;
    for (int i = 0; i < 6450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6450(int& val) {
    const int x = 6450;
    std::cout << 6450;
    for (int i = 0; i < 6450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6451(int& val) {
    func_in_6451(val);
    const int x = 6451;
    std::cout << 6451;
    for (int i = 0; i < 6451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6451(int& val) {
    const int x = 6451;
    std::cout << 6451;
    for (int i = 0; i < 6451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6452(int& val) {
    func_in_6452(val);
    const int x = 6452;
    std::cout << 6452;
    for (int i = 0; i < 6452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6452(int& val) {
    const int x = 6452;
    std::cout << 6452;
    for (int i = 0; i < 6452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6453(int& val) {
    func_in_6453(val);
    const int x = 6453;
    std::cout << 6453;
    for (int i = 0; i < 6453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6453(int& val) {
    const int x = 6453;
    std::cout << 6453;
    for (int i = 0; i < 6453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6454(int& val) {
    func_in_6454(val);
    const int x = 6454;
    std::cout << 6454;
    for (int i = 0; i < 6454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6454(int& val) {
    const int x = 6454;
    std::cout << 6454;
    for (int i = 0; i < 6454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6455(int& val) {
    func_in_6455(val);
    const int x = 6455;
    std::cout << 6455;
    for (int i = 0; i < 6455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6455(int& val) {
    const int x = 6455;
    std::cout << 6455;
    for (int i = 0; i < 6455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6456(int& val) {
    func_in_6456(val);
    const int x = 6456;
    std::cout << 6456;
    for (int i = 0; i < 6456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6456(int& val) {
    const int x = 6456;
    std::cout << 6456;
    for (int i = 0; i < 6456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6457(int& val) {
    func_in_6457(val);
    const int x = 6457;
    std::cout << 6457;
    for (int i = 0; i < 6457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6457(int& val) {
    const int x = 6457;
    std::cout << 6457;
    for (int i = 0; i < 6457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6458(int& val) {
    func_in_6458(val);
    const int x = 6458;
    std::cout << 6458;
    for (int i = 0; i < 6458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6458(int& val) {
    const int x = 6458;
    std::cout << 6458;
    for (int i = 0; i < 6458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6459(int& val) {
    func_in_6459(val);
    const int x = 6459;
    std::cout << 6459;
    for (int i = 0; i < 6459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6459(int& val) {
    const int x = 6459;
    std::cout << 6459;
    for (int i = 0; i < 6459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6460(int& val) {
    func_in_6460(val);
    const int x = 6460;
    std::cout << 6460;
    for (int i = 0; i < 6460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6460(int& val) {
    const int x = 6460;
    std::cout << 6460;
    for (int i = 0; i < 6460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6461(int& val) {
    func_in_6461(val);
    const int x = 6461;
    std::cout << 6461;
    for (int i = 0; i < 6461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6461(int& val) {
    const int x = 6461;
    std::cout << 6461;
    for (int i = 0; i < 6461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6462(int& val) {
    func_in_6462(val);
    const int x = 6462;
    std::cout << 6462;
    for (int i = 0; i < 6462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6462(int& val) {
    const int x = 6462;
    std::cout << 6462;
    for (int i = 0; i < 6462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6463(int& val) {
    func_in_6463(val);
    const int x = 6463;
    std::cout << 6463;
    for (int i = 0; i < 6463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6463(int& val) {
    const int x = 6463;
    std::cout << 6463;
    for (int i = 0; i < 6463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6464(int& val) {
    func_in_6464(val);
    const int x = 6464;
    std::cout << 6464;
    for (int i = 0; i < 6464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6464(int& val) {
    const int x = 6464;
    std::cout << 6464;
    for (int i = 0; i < 6464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6465(int& val) {
    func_in_6465(val);
    const int x = 6465;
    std::cout << 6465;
    for (int i = 0; i < 6465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6465(int& val) {
    const int x = 6465;
    std::cout << 6465;
    for (int i = 0; i < 6465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6466(int& val) {
    func_in_6466(val);
    const int x = 6466;
    std::cout << 6466;
    for (int i = 0; i < 6466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6466(int& val) {
    const int x = 6466;
    std::cout << 6466;
    for (int i = 0; i < 6466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6467(int& val) {
    func_in_6467(val);
    const int x = 6467;
    std::cout << 6467;
    for (int i = 0; i < 6467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6467(int& val) {
    const int x = 6467;
    std::cout << 6467;
    for (int i = 0; i < 6467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6468(int& val) {
    func_in_6468(val);
    const int x = 6468;
    std::cout << 6468;
    for (int i = 0; i < 6468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6468(int& val) {
    const int x = 6468;
    std::cout << 6468;
    for (int i = 0; i < 6468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6469(int& val) {
    func_in_6469(val);
    const int x = 6469;
    std::cout << 6469;
    for (int i = 0; i < 6469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6469(int& val) {
    const int x = 6469;
    std::cout << 6469;
    for (int i = 0; i < 6469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6470(int& val) {
    func_in_6470(val);
    const int x = 6470;
    std::cout << 6470;
    for (int i = 0; i < 6470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6470(int& val) {
    const int x = 6470;
    std::cout << 6470;
    for (int i = 0; i < 6470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6471(int& val) {
    func_in_6471(val);
    const int x = 6471;
    std::cout << 6471;
    for (int i = 0; i < 6471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6471(int& val) {
    const int x = 6471;
    std::cout << 6471;
    for (int i = 0; i < 6471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6472(int& val) {
    func_in_6472(val);
    const int x = 6472;
    std::cout << 6472;
    for (int i = 0; i < 6472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6472(int& val) {
    const int x = 6472;
    std::cout << 6472;
    for (int i = 0; i < 6472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6473(int& val) {
    func_in_6473(val);
    const int x = 6473;
    std::cout << 6473;
    for (int i = 0; i < 6473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6473(int& val) {
    const int x = 6473;
    std::cout << 6473;
    for (int i = 0; i < 6473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6474(int& val) {
    func_in_6474(val);
    const int x = 6474;
    std::cout << 6474;
    for (int i = 0; i < 6474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6474(int& val) {
    const int x = 6474;
    std::cout << 6474;
    for (int i = 0; i < 6474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6475(int& val) {
    func_in_6475(val);
    const int x = 6475;
    std::cout << 6475;
    for (int i = 0; i < 6475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6475(int& val) {
    const int x = 6475;
    std::cout << 6475;
    for (int i = 0; i < 6475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6476(int& val) {
    func_in_6476(val);
    const int x = 6476;
    std::cout << 6476;
    for (int i = 0; i < 6476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6476(int& val) {
    const int x = 6476;
    std::cout << 6476;
    for (int i = 0; i < 6476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6477(int& val) {
    func_in_6477(val);
    const int x = 6477;
    std::cout << 6477;
    for (int i = 0; i < 6477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6477(int& val) {
    const int x = 6477;
    std::cout << 6477;
    for (int i = 0; i < 6477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6478(int& val) {
    func_in_6478(val);
    const int x = 6478;
    std::cout << 6478;
    for (int i = 0; i < 6478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6478(int& val) {
    const int x = 6478;
    std::cout << 6478;
    for (int i = 0; i < 6478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6479(int& val) {
    func_in_6479(val);
    const int x = 6479;
    std::cout << 6479;
    for (int i = 0; i < 6479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6479(int& val) {
    const int x = 6479;
    std::cout << 6479;
    for (int i = 0; i < 6479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6480(int& val) {
    func_in_6480(val);
    const int x = 6480;
    std::cout << 6480;
    for (int i = 0; i < 6480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6480(int& val) {
    const int x = 6480;
    std::cout << 6480;
    for (int i = 0; i < 6480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6481(int& val) {
    func_in_6481(val);
    const int x = 6481;
    std::cout << 6481;
    for (int i = 0; i < 6481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6481(int& val) {
    const int x = 6481;
    std::cout << 6481;
    for (int i = 0; i < 6481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6482(int& val) {
    func_in_6482(val);
    const int x = 6482;
    std::cout << 6482;
    for (int i = 0; i < 6482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6482(int& val) {
    const int x = 6482;
    std::cout << 6482;
    for (int i = 0; i < 6482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6483(int& val) {
    func_in_6483(val);
    const int x = 6483;
    std::cout << 6483;
    for (int i = 0; i < 6483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6483(int& val) {
    const int x = 6483;
    std::cout << 6483;
    for (int i = 0; i < 6483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6484(int& val) {
    func_in_6484(val);
    const int x = 6484;
    std::cout << 6484;
    for (int i = 0; i < 6484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6484(int& val) {
    const int x = 6484;
    std::cout << 6484;
    for (int i = 0; i < 6484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6485(int& val) {
    func_in_6485(val);
    const int x = 6485;
    std::cout << 6485;
    for (int i = 0; i < 6485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6485(int& val) {
    const int x = 6485;
    std::cout << 6485;
    for (int i = 0; i < 6485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6486(int& val) {
    func_in_6486(val);
    const int x = 6486;
    std::cout << 6486;
    for (int i = 0; i < 6486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6486(int& val) {
    const int x = 6486;
    std::cout << 6486;
    for (int i = 0; i < 6486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6487(int& val) {
    func_in_6487(val);
    const int x = 6487;
    std::cout << 6487;
    for (int i = 0; i < 6487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6487(int& val) {
    const int x = 6487;
    std::cout << 6487;
    for (int i = 0; i < 6487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6488(int& val) {
    func_in_6488(val);
    const int x = 6488;
    std::cout << 6488;
    for (int i = 0; i < 6488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6488(int& val) {
    const int x = 6488;
    std::cout << 6488;
    for (int i = 0; i < 6488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6489(int& val) {
    func_in_6489(val);
    const int x = 6489;
    std::cout << 6489;
    for (int i = 0; i < 6489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6489(int& val) {
    const int x = 6489;
    std::cout << 6489;
    for (int i = 0; i < 6489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6490(int& val) {
    func_in_6490(val);
    const int x = 6490;
    std::cout << 6490;
    for (int i = 0; i < 6490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6490(int& val) {
    const int x = 6490;
    std::cout << 6490;
    for (int i = 0; i < 6490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6491(int& val) {
    func_in_6491(val);
    const int x = 6491;
    std::cout << 6491;
    for (int i = 0; i < 6491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6491(int& val) {
    const int x = 6491;
    std::cout << 6491;
    for (int i = 0; i < 6491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6492(int& val) {
    func_in_6492(val);
    const int x = 6492;
    std::cout << 6492;
    for (int i = 0; i < 6492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6492(int& val) {
    const int x = 6492;
    std::cout << 6492;
    for (int i = 0; i < 6492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6493(int& val) {
    func_in_6493(val);
    const int x = 6493;
    std::cout << 6493;
    for (int i = 0; i < 6493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6493(int& val) {
    const int x = 6493;
    std::cout << 6493;
    for (int i = 0; i < 6493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6494(int& val) {
    func_in_6494(val);
    const int x = 6494;
    std::cout << 6494;
    for (int i = 0; i < 6494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6494(int& val) {
    const int x = 6494;
    std::cout << 6494;
    for (int i = 0; i < 6494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6495(int& val) {
    func_in_6495(val);
    const int x = 6495;
    std::cout << 6495;
    for (int i = 0; i < 6495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6495(int& val) {
    const int x = 6495;
    std::cout << 6495;
    for (int i = 0; i < 6495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6496(int& val) {
    func_in_6496(val);
    const int x = 6496;
    std::cout << 6496;
    for (int i = 0; i < 6496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6496(int& val) {
    const int x = 6496;
    std::cout << 6496;
    for (int i = 0; i < 6496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6497(int& val) {
    func_in_6497(val);
    const int x = 6497;
    std::cout << 6497;
    for (int i = 0; i < 6497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6497(int& val) {
    const int x = 6497;
    std::cout << 6497;
    for (int i = 0; i < 6497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6498(int& val) {
    func_in_6498(val);
    const int x = 6498;
    std::cout << 6498;
    for (int i = 0; i < 6498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6498(int& val) {
    const int x = 6498;
    std::cout << 6498;
    for (int i = 0; i < 6498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6499(int& val) {
    func_in_6499(val);
    const int x = 6499;
    std::cout << 6499;
    for (int i = 0; i < 6499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6499(int& val) {
    const int x = 6499;
    std::cout << 6499;
    for (int i = 0; i < 6499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6500(int& val) {
    func_in_6500(val);
    const int x = 6500;
    std::cout << 6500;
    for (int i = 0; i < 6500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6500(int& val) {
    const int x = 6500;
    std::cout << 6500;
    for (int i = 0; i < 6500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6501(int& val) {
    func_in_6501(val);
    const int x = 6501;
    std::cout << 6501;
    for (int i = 0; i < 6501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6501(int& val) {
    const int x = 6501;
    std::cout << 6501;
    for (int i = 0; i < 6501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6502(int& val) {
    func_in_6502(val);
    const int x = 6502;
    std::cout << 6502;
    for (int i = 0; i < 6502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6502(int& val) {
    const int x = 6502;
    std::cout << 6502;
    for (int i = 0; i < 6502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6503(int& val) {
    func_in_6503(val);
    const int x = 6503;
    std::cout << 6503;
    for (int i = 0; i < 6503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6503(int& val) {
    const int x = 6503;
    std::cout << 6503;
    for (int i = 0; i < 6503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6504(int& val) {
    func_in_6504(val);
    const int x = 6504;
    std::cout << 6504;
    for (int i = 0; i < 6504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6504(int& val) {
    const int x = 6504;
    std::cout << 6504;
    for (int i = 0; i < 6504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6505(int& val) {
    func_in_6505(val);
    const int x = 6505;
    std::cout << 6505;
    for (int i = 0; i < 6505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6505(int& val) {
    const int x = 6505;
    std::cout << 6505;
    for (int i = 0; i < 6505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6506(int& val) {
    func_in_6506(val);
    const int x = 6506;
    std::cout << 6506;
    for (int i = 0; i < 6506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6506(int& val) {
    const int x = 6506;
    std::cout << 6506;
    for (int i = 0; i < 6506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6507(int& val) {
    func_in_6507(val);
    const int x = 6507;
    std::cout << 6507;
    for (int i = 0; i < 6507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6507(int& val) {
    const int x = 6507;
    std::cout << 6507;
    for (int i = 0; i < 6507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6508(int& val) {
    func_in_6508(val);
    const int x = 6508;
    std::cout << 6508;
    for (int i = 0; i < 6508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6508(int& val) {
    const int x = 6508;
    std::cout << 6508;
    for (int i = 0; i < 6508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6509(int& val) {
    func_in_6509(val);
    const int x = 6509;
    std::cout << 6509;
    for (int i = 0; i < 6509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6509(int& val) {
    const int x = 6509;
    std::cout << 6509;
    for (int i = 0; i < 6509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6510(int& val) {
    func_in_6510(val);
    const int x = 6510;
    std::cout << 6510;
    for (int i = 0; i < 6510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6510(int& val) {
    const int x = 6510;
    std::cout << 6510;
    for (int i = 0; i < 6510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6511(int& val) {
    func_in_6511(val);
    const int x = 6511;
    std::cout << 6511;
    for (int i = 0; i < 6511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6511(int& val) {
    const int x = 6511;
    std::cout << 6511;
    for (int i = 0; i < 6511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6512(int& val) {
    func_in_6512(val);
    const int x = 6512;
    std::cout << 6512;
    for (int i = 0; i < 6512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6512(int& val) {
    const int x = 6512;
    std::cout << 6512;
    for (int i = 0; i < 6512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6513(int& val) {
    func_in_6513(val);
    const int x = 6513;
    std::cout << 6513;
    for (int i = 0; i < 6513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6513(int& val) {
    const int x = 6513;
    std::cout << 6513;
    for (int i = 0; i < 6513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6514(int& val) {
    func_in_6514(val);
    const int x = 6514;
    std::cout << 6514;
    for (int i = 0; i < 6514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6514(int& val) {
    const int x = 6514;
    std::cout << 6514;
    for (int i = 0; i < 6514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6515(int& val) {
    func_in_6515(val);
    const int x = 6515;
    std::cout << 6515;
    for (int i = 0; i < 6515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6515(int& val) {
    const int x = 6515;
    std::cout << 6515;
    for (int i = 0; i < 6515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6516(int& val) {
    func_in_6516(val);
    const int x = 6516;
    std::cout << 6516;
    for (int i = 0; i < 6516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6516(int& val) {
    const int x = 6516;
    std::cout << 6516;
    for (int i = 0; i < 6516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6517(int& val) {
    func_in_6517(val);
    const int x = 6517;
    std::cout << 6517;
    for (int i = 0; i < 6517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6517(int& val) {
    const int x = 6517;
    std::cout << 6517;
    for (int i = 0; i < 6517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6518(int& val) {
    func_in_6518(val);
    const int x = 6518;
    std::cout << 6518;
    for (int i = 0; i < 6518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6518(int& val) {
    const int x = 6518;
    std::cout << 6518;
    for (int i = 0; i < 6518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6519(int& val) {
    func_in_6519(val);
    const int x = 6519;
    std::cout << 6519;
    for (int i = 0; i < 6519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6519(int& val) {
    const int x = 6519;
    std::cout << 6519;
    for (int i = 0; i < 6519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6520(int& val) {
    func_in_6520(val);
    const int x = 6520;
    std::cout << 6520;
    for (int i = 0; i < 6520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6520(int& val) {
    const int x = 6520;
    std::cout << 6520;
    for (int i = 0; i < 6520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6521(int& val) {
    func_in_6521(val);
    const int x = 6521;
    std::cout << 6521;
    for (int i = 0; i < 6521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6521(int& val) {
    const int x = 6521;
    std::cout << 6521;
    for (int i = 0; i < 6521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6522(int& val) {
    func_in_6522(val);
    const int x = 6522;
    std::cout << 6522;
    for (int i = 0; i < 6522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6522(int& val) {
    const int x = 6522;
    std::cout << 6522;
    for (int i = 0; i < 6522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6523(int& val) {
    func_in_6523(val);
    const int x = 6523;
    std::cout << 6523;
    for (int i = 0; i < 6523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6523(int& val) {
    const int x = 6523;
    std::cout << 6523;
    for (int i = 0; i < 6523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6524(int& val) {
    func_in_6524(val);
    const int x = 6524;
    std::cout << 6524;
    for (int i = 0; i < 6524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6524(int& val) {
    const int x = 6524;
    std::cout << 6524;
    for (int i = 0; i < 6524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6525(int& val) {
    func_in_6525(val);
    const int x = 6525;
    std::cout << 6525;
    for (int i = 0; i < 6525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6525(int& val) {
    const int x = 6525;
    std::cout << 6525;
    for (int i = 0; i < 6525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6526(int& val) {
    func_in_6526(val);
    const int x = 6526;
    std::cout << 6526;
    for (int i = 0; i < 6526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6526(int& val) {
    const int x = 6526;
    std::cout << 6526;
    for (int i = 0; i < 6526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6527(int& val) {
    func_in_6527(val);
    const int x = 6527;
    std::cout << 6527;
    for (int i = 0; i < 6527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6527(int& val) {
    const int x = 6527;
    std::cout << 6527;
    for (int i = 0; i < 6527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6528(int& val) {
    func_in_6528(val);
    const int x = 6528;
    std::cout << 6528;
    for (int i = 0; i < 6528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6528(int& val) {
    const int x = 6528;
    std::cout << 6528;
    for (int i = 0; i < 6528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6529(int& val) {
    func_in_6529(val);
    const int x = 6529;
    std::cout << 6529;
    for (int i = 0; i < 6529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6529(int& val) {
    const int x = 6529;
    std::cout << 6529;
    for (int i = 0; i < 6529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6530(int& val) {
    func_in_6530(val);
    const int x = 6530;
    std::cout << 6530;
    for (int i = 0; i < 6530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6530(int& val) {
    const int x = 6530;
    std::cout << 6530;
    for (int i = 0; i < 6530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6531(int& val) {
    func_in_6531(val);
    const int x = 6531;
    std::cout << 6531;
    for (int i = 0; i < 6531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6531(int& val) {
    const int x = 6531;
    std::cout << 6531;
    for (int i = 0; i < 6531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6532(int& val) {
    func_in_6532(val);
    const int x = 6532;
    std::cout << 6532;
    for (int i = 0; i < 6532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6532(int& val) {
    const int x = 6532;
    std::cout << 6532;
    for (int i = 0; i < 6532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6533(int& val) {
    func_in_6533(val);
    const int x = 6533;
    std::cout << 6533;
    for (int i = 0; i < 6533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6533(int& val) {
    const int x = 6533;
    std::cout << 6533;
    for (int i = 0; i < 6533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6534(int& val) {
    func_in_6534(val);
    const int x = 6534;
    std::cout << 6534;
    for (int i = 0; i < 6534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6534(int& val) {
    const int x = 6534;
    std::cout << 6534;
    for (int i = 0; i < 6534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6535(int& val) {
    func_in_6535(val);
    const int x = 6535;
    std::cout << 6535;
    for (int i = 0; i < 6535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6535(int& val) {
    const int x = 6535;
    std::cout << 6535;
    for (int i = 0; i < 6535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6536(int& val) {
    func_in_6536(val);
    const int x = 6536;
    std::cout << 6536;
    for (int i = 0; i < 6536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6536(int& val) {
    const int x = 6536;
    std::cout << 6536;
    for (int i = 0; i < 6536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6537(int& val) {
    func_in_6537(val);
    const int x = 6537;
    std::cout << 6537;
    for (int i = 0; i < 6537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6537(int& val) {
    const int x = 6537;
    std::cout << 6537;
    for (int i = 0; i < 6537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6538(int& val) {
    func_in_6538(val);
    const int x = 6538;
    std::cout << 6538;
    for (int i = 0; i < 6538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6538(int& val) {
    const int x = 6538;
    std::cout << 6538;
    for (int i = 0; i < 6538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6539(int& val) {
    func_in_6539(val);
    const int x = 6539;
    std::cout << 6539;
    for (int i = 0; i < 6539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6539(int& val) {
    const int x = 6539;
    std::cout << 6539;
    for (int i = 0; i < 6539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6540(int& val) {
    func_in_6540(val);
    const int x = 6540;
    std::cout << 6540;
    for (int i = 0; i < 6540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6540(int& val) {
    const int x = 6540;
    std::cout << 6540;
    for (int i = 0; i < 6540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6541(int& val) {
    func_in_6541(val);
    const int x = 6541;
    std::cout << 6541;
    for (int i = 0; i < 6541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6541(int& val) {
    const int x = 6541;
    std::cout << 6541;
    for (int i = 0; i < 6541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6542(int& val) {
    func_in_6542(val);
    const int x = 6542;
    std::cout << 6542;
    for (int i = 0; i < 6542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6542(int& val) {
    const int x = 6542;
    std::cout << 6542;
    for (int i = 0; i < 6542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6543(int& val) {
    func_in_6543(val);
    const int x = 6543;
    std::cout << 6543;
    for (int i = 0; i < 6543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6543(int& val) {
    const int x = 6543;
    std::cout << 6543;
    for (int i = 0; i < 6543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6544(int& val) {
    func_in_6544(val);
    const int x = 6544;
    std::cout << 6544;
    for (int i = 0; i < 6544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6544(int& val) {
    const int x = 6544;
    std::cout << 6544;
    for (int i = 0; i < 6544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6545(int& val) {
    func_in_6545(val);
    const int x = 6545;
    std::cout << 6545;
    for (int i = 0; i < 6545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6545(int& val) {
    const int x = 6545;
    std::cout << 6545;
    for (int i = 0; i < 6545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6546(int& val) {
    func_in_6546(val);
    const int x = 6546;
    std::cout << 6546;
    for (int i = 0; i < 6546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6546(int& val) {
    const int x = 6546;
    std::cout << 6546;
    for (int i = 0; i < 6546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6547(int& val) {
    func_in_6547(val);
    const int x = 6547;
    std::cout << 6547;
    for (int i = 0; i < 6547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6547(int& val) {
    const int x = 6547;
    std::cout << 6547;
    for (int i = 0; i < 6547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6548(int& val) {
    func_in_6548(val);
    const int x = 6548;
    std::cout << 6548;
    for (int i = 0; i < 6548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6548(int& val) {
    const int x = 6548;
    std::cout << 6548;
    for (int i = 0; i < 6548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6549(int& val) {
    func_in_6549(val);
    const int x = 6549;
    std::cout << 6549;
    for (int i = 0; i < 6549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6549(int& val) {
    const int x = 6549;
    std::cout << 6549;
    for (int i = 0; i < 6549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6550(int& val) {
    func_in_6550(val);
    const int x = 6550;
    std::cout << 6550;
    for (int i = 0; i < 6550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6550(int& val) {
    const int x = 6550;
    std::cout << 6550;
    for (int i = 0; i < 6550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6551(int& val) {
    func_in_6551(val);
    const int x = 6551;
    std::cout << 6551;
    for (int i = 0; i < 6551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6551(int& val) {
    const int x = 6551;
    std::cout << 6551;
    for (int i = 0; i < 6551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6552(int& val) {
    func_in_6552(val);
    const int x = 6552;
    std::cout << 6552;
    for (int i = 0; i < 6552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6552(int& val) {
    const int x = 6552;
    std::cout << 6552;
    for (int i = 0; i < 6552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6553(int& val) {
    func_in_6553(val);
    const int x = 6553;
    std::cout << 6553;
    for (int i = 0; i < 6553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6553(int& val) {
    const int x = 6553;
    std::cout << 6553;
    for (int i = 0; i < 6553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6554(int& val) {
    func_in_6554(val);
    const int x = 6554;
    std::cout << 6554;
    for (int i = 0; i < 6554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6554(int& val) {
    const int x = 6554;
    std::cout << 6554;
    for (int i = 0; i < 6554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6555(int& val) {
    func_in_6555(val);
    const int x = 6555;
    std::cout << 6555;
    for (int i = 0; i < 6555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6555(int& val) {
    const int x = 6555;
    std::cout << 6555;
    for (int i = 0; i < 6555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6556(int& val) {
    func_in_6556(val);
    const int x = 6556;
    std::cout << 6556;
    for (int i = 0; i < 6556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6556(int& val) {
    const int x = 6556;
    std::cout << 6556;
    for (int i = 0; i < 6556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6557(int& val) {
    func_in_6557(val);
    const int x = 6557;
    std::cout << 6557;
    for (int i = 0; i < 6557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6557(int& val) {
    const int x = 6557;
    std::cout << 6557;
    for (int i = 0; i < 6557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6558(int& val) {
    func_in_6558(val);
    const int x = 6558;
    std::cout << 6558;
    for (int i = 0; i < 6558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6558(int& val) {
    const int x = 6558;
    std::cout << 6558;
    for (int i = 0; i < 6558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6559(int& val) {
    func_in_6559(val);
    const int x = 6559;
    std::cout << 6559;
    for (int i = 0; i < 6559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6559(int& val) {
    const int x = 6559;
    std::cout << 6559;
    for (int i = 0; i < 6559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6560(int& val) {
    func_in_6560(val);
    const int x = 6560;
    std::cout << 6560;
    for (int i = 0; i < 6560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6560(int& val) {
    const int x = 6560;
    std::cout << 6560;
    for (int i = 0; i < 6560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6561(int& val) {
    func_in_6561(val);
    const int x = 6561;
    std::cout << 6561;
    for (int i = 0; i < 6561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6561(int& val) {
    const int x = 6561;
    std::cout << 6561;
    for (int i = 0; i < 6561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6562(int& val) {
    func_in_6562(val);
    const int x = 6562;
    std::cout << 6562;
    for (int i = 0; i < 6562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6562(int& val) {
    const int x = 6562;
    std::cout << 6562;
    for (int i = 0; i < 6562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6563(int& val) {
    func_in_6563(val);
    const int x = 6563;
    std::cout << 6563;
    for (int i = 0; i < 6563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6563(int& val) {
    const int x = 6563;
    std::cout << 6563;
    for (int i = 0; i < 6563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6564(int& val) {
    func_in_6564(val);
    const int x = 6564;
    std::cout << 6564;
    for (int i = 0; i < 6564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6564(int& val) {
    const int x = 6564;
    std::cout << 6564;
    for (int i = 0; i < 6564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6565(int& val) {
    func_in_6565(val);
    const int x = 6565;
    std::cout << 6565;
    for (int i = 0; i < 6565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6565(int& val) {
    const int x = 6565;
    std::cout << 6565;
    for (int i = 0; i < 6565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6566(int& val) {
    func_in_6566(val);
    const int x = 6566;
    std::cout << 6566;
    for (int i = 0; i < 6566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6566(int& val) {
    const int x = 6566;
    std::cout << 6566;
    for (int i = 0; i < 6566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6567(int& val) {
    func_in_6567(val);
    const int x = 6567;
    std::cout << 6567;
    for (int i = 0; i < 6567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6567(int& val) {
    const int x = 6567;
    std::cout << 6567;
    for (int i = 0; i < 6567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6568(int& val) {
    func_in_6568(val);
    const int x = 6568;
    std::cout << 6568;
    for (int i = 0; i < 6568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6568(int& val) {
    const int x = 6568;
    std::cout << 6568;
    for (int i = 0; i < 6568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6569(int& val) {
    func_in_6569(val);
    const int x = 6569;
    std::cout << 6569;
    for (int i = 0; i < 6569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6569(int& val) {
    const int x = 6569;
    std::cout << 6569;
    for (int i = 0; i < 6569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6570(int& val) {
    func_in_6570(val);
    const int x = 6570;
    std::cout << 6570;
    for (int i = 0; i < 6570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6570(int& val) {
    const int x = 6570;
    std::cout << 6570;
    for (int i = 0; i < 6570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6571(int& val) {
    func_in_6571(val);
    const int x = 6571;
    std::cout << 6571;
    for (int i = 0; i < 6571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6571(int& val) {
    const int x = 6571;
    std::cout << 6571;
    for (int i = 0; i < 6571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6572(int& val) {
    func_in_6572(val);
    const int x = 6572;
    std::cout << 6572;
    for (int i = 0; i < 6572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6572(int& val) {
    const int x = 6572;
    std::cout << 6572;
    for (int i = 0; i < 6572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6573(int& val) {
    func_in_6573(val);
    const int x = 6573;
    std::cout << 6573;
    for (int i = 0; i < 6573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6573(int& val) {
    const int x = 6573;
    std::cout << 6573;
    for (int i = 0; i < 6573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6574(int& val) {
    func_in_6574(val);
    const int x = 6574;
    std::cout << 6574;
    for (int i = 0; i < 6574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6574(int& val) {
    const int x = 6574;
    std::cout << 6574;
    for (int i = 0; i < 6574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6575(int& val) {
    func_in_6575(val);
    const int x = 6575;
    std::cout << 6575;
    for (int i = 0; i < 6575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6575(int& val) {
    const int x = 6575;
    std::cout << 6575;
    for (int i = 0; i < 6575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6576(int& val) {
    func_in_6576(val);
    const int x = 6576;
    std::cout << 6576;
    for (int i = 0; i < 6576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6576(int& val) {
    const int x = 6576;
    std::cout << 6576;
    for (int i = 0; i < 6576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6577(int& val) {
    func_in_6577(val);
    const int x = 6577;
    std::cout << 6577;
    for (int i = 0; i < 6577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6577(int& val) {
    const int x = 6577;
    std::cout << 6577;
    for (int i = 0; i < 6577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6578(int& val) {
    func_in_6578(val);
    const int x = 6578;
    std::cout << 6578;
    for (int i = 0; i < 6578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6578(int& val) {
    const int x = 6578;
    std::cout << 6578;
    for (int i = 0; i < 6578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6579(int& val) {
    func_in_6579(val);
    const int x = 6579;
    std::cout << 6579;
    for (int i = 0; i < 6579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6579(int& val) {
    const int x = 6579;
    std::cout << 6579;
    for (int i = 0; i < 6579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6580(int& val) {
    func_in_6580(val);
    const int x = 6580;
    std::cout << 6580;
    for (int i = 0; i < 6580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6580(int& val) {
    const int x = 6580;
    std::cout << 6580;
    for (int i = 0; i < 6580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6581(int& val) {
    func_in_6581(val);
    const int x = 6581;
    std::cout << 6581;
    for (int i = 0; i < 6581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6581(int& val) {
    const int x = 6581;
    std::cout << 6581;
    for (int i = 0; i < 6581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6582(int& val) {
    func_in_6582(val);
    const int x = 6582;
    std::cout << 6582;
    for (int i = 0; i < 6582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6582(int& val) {
    const int x = 6582;
    std::cout << 6582;
    for (int i = 0; i < 6582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6583(int& val) {
    func_in_6583(val);
    const int x = 6583;
    std::cout << 6583;
    for (int i = 0; i < 6583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6583(int& val) {
    const int x = 6583;
    std::cout << 6583;
    for (int i = 0; i < 6583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6584(int& val) {
    func_in_6584(val);
    const int x = 6584;
    std::cout << 6584;
    for (int i = 0; i < 6584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6584(int& val) {
    const int x = 6584;
    std::cout << 6584;
    for (int i = 0; i < 6584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6585(int& val) {
    func_in_6585(val);
    const int x = 6585;
    std::cout << 6585;
    for (int i = 0; i < 6585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6585(int& val) {
    const int x = 6585;
    std::cout << 6585;
    for (int i = 0; i < 6585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6586(int& val) {
    func_in_6586(val);
    const int x = 6586;
    std::cout << 6586;
    for (int i = 0; i < 6586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6586(int& val) {
    const int x = 6586;
    std::cout << 6586;
    for (int i = 0; i < 6586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6587(int& val) {
    func_in_6587(val);
    const int x = 6587;
    std::cout << 6587;
    for (int i = 0; i < 6587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6587(int& val) {
    const int x = 6587;
    std::cout << 6587;
    for (int i = 0; i < 6587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6588(int& val) {
    func_in_6588(val);
    const int x = 6588;
    std::cout << 6588;
    for (int i = 0; i < 6588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6588(int& val) {
    const int x = 6588;
    std::cout << 6588;
    for (int i = 0; i < 6588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6589(int& val) {
    func_in_6589(val);
    const int x = 6589;
    std::cout << 6589;
    for (int i = 0; i < 6589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6589(int& val) {
    const int x = 6589;
    std::cout << 6589;
    for (int i = 0; i < 6589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6590(int& val) {
    func_in_6590(val);
    const int x = 6590;
    std::cout << 6590;
    for (int i = 0; i < 6590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6590(int& val) {
    const int x = 6590;
    std::cout << 6590;
    for (int i = 0; i < 6590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6591(int& val) {
    func_in_6591(val);
    const int x = 6591;
    std::cout << 6591;
    for (int i = 0; i < 6591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6591(int& val) {
    const int x = 6591;
    std::cout << 6591;
    for (int i = 0; i < 6591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6592(int& val) {
    func_in_6592(val);
    const int x = 6592;
    std::cout << 6592;
    for (int i = 0; i < 6592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6592(int& val) {
    const int x = 6592;
    std::cout << 6592;
    for (int i = 0; i < 6592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6593(int& val) {
    func_in_6593(val);
    const int x = 6593;
    std::cout << 6593;
    for (int i = 0; i < 6593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6593(int& val) {
    const int x = 6593;
    std::cout << 6593;
    for (int i = 0; i < 6593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6594(int& val) {
    func_in_6594(val);
    const int x = 6594;
    std::cout << 6594;
    for (int i = 0; i < 6594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6594(int& val) {
    const int x = 6594;
    std::cout << 6594;
    for (int i = 0; i < 6594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6595(int& val) {
    func_in_6595(val);
    const int x = 6595;
    std::cout << 6595;
    for (int i = 0; i < 6595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6595(int& val) {
    const int x = 6595;
    std::cout << 6595;
    for (int i = 0; i < 6595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6596(int& val) {
    func_in_6596(val);
    const int x = 6596;
    std::cout << 6596;
    for (int i = 0; i < 6596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6596(int& val) {
    const int x = 6596;
    std::cout << 6596;
    for (int i = 0; i < 6596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6597(int& val) {
    func_in_6597(val);
    const int x = 6597;
    std::cout << 6597;
    for (int i = 0; i < 6597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6597(int& val) {
    const int x = 6597;
    std::cout << 6597;
    for (int i = 0; i < 6597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6598(int& val) {
    func_in_6598(val);
    const int x = 6598;
    std::cout << 6598;
    for (int i = 0; i < 6598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6598(int& val) {
    const int x = 6598;
    std::cout << 6598;
    for (int i = 0; i < 6598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6599(int& val) {
    func_in_6599(val);
    const int x = 6599;
    std::cout << 6599;
    for (int i = 0; i < 6599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6599(int& val) {
    const int x = 6599;
    std::cout << 6599;
    for (int i = 0; i < 6599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6600(int& val) {
    func_in_6600(val);
    const int x = 6600;
    std::cout << 6600;
    for (int i = 0; i < 6600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6600(int& val) {
    const int x = 6600;
    std::cout << 6600;
    for (int i = 0; i < 6600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6601(int& val) {
    func_in_6601(val);
    const int x = 6601;
    std::cout << 6601;
    for (int i = 0; i < 6601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6601(int& val) {
    const int x = 6601;
    std::cout << 6601;
    for (int i = 0; i < 6601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6602(int& val) {
    func_in_6602(val);
    const int x = 6602;
    std::cout << 6602;
    for (int i = 0; i < 6602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6602(int& val) {
    const int x = 6602;
    std::cout << 6602;
    for (int i = 0; i < 6602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6603(int& val) {
    func_in_6603(val);
    const int x = 6603;
    std::cout << 6603;
    for (int i = 0; i < 6603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6603(int& val) {
    const int x = 6603;
    std::cout << 6603;
    for (int i = 0; i < 6603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6604(int& val) {
    func_in_6604(val);
    const int x = 6604;
    std::cout << 6604;
    for (int i = 0; i < 6604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6604(int& val) {
    const int x = 6604;
    std::cout << 6604;
    for (int i = 0; i < 6604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6605(int& val) {
    func_in_6605(val);
    const int x = 6605;
    std::cout << 6605;
    for (int i = 0; i < 6605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6605(int& val) {
    const int x = 6605;
    std::cout << 6605;
    for (int i = 0; i < 6605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6606(int& val) {
    func_in_6606(val);
    const int x = 6606;
    std::cout << 6606;
    for (int i = 0; i < 6606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6606(int& val) {
    const int x = 6606;
    std::cout << 6606;
    for (int i = 0; i < 6606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6607(int& val) {
    func_in_6607(val);
    const int x = 6607;
    std::cout << 6607;
    for (int i = 0; i < 6607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6607(int& val) {
    const int x = 6607;
    std::cout << 6607;
    for (int i = 0; i < 6607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6608(int& val) {
    func_in_6608(val);
    const int x = 6608;
    std::cout << 6608;
    for (int i = 0; i < 6608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6608(int& val) {
    const int x = 6608;
    std::cout << 6608;
    for (int i = 0; i < 6608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6609(int& val) {
    func_in_6609(val);
    const int x = 6609;
    std::cout << 6609;
    for (int i = 0; i < 6609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6609(int& val) {
    const int x = 6609;
    std::cout << 6609;
    for (int i = 0; i < 6609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6610(int& val) {
    func_in_6610(val);
    const int x = 6610;
    std::cout << 6610;
    for (int i = 0; i < 6610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6610(int& val) {
    const int x = 6610;
    std::cout << 6610;
    for (int i = 0; i < 6610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6611(int& val) {
    func_in_6611(val);
    const int x = 6611;
    std::cout << 6611;
    for (int i = 0; i < 6611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6611(int& val) {
    const int x = 6611;
    std::cout << 6611;
    for (int i = 0; i < 6611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6612(int& val) {
    func_in_6612(val);
    const int x = 6612;
    std::cout << 6612;
    for (int i = 0; i < 6612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6612(int& val) {
    const int x = 6612;
    std::cout << 6612;
    for (int i = 0; i < 6612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6613(int& val) {
    func_in_6613(val);
    const int x = 6613;
    std::cout << 6613;
    for (int i = 0; i < 6613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6613(int& val) {
    const int x = 6613;
    std::cout << 6613;
    for (int i = 0; i < 6613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6614(int& val) {
    func_in_6614(val);
    const int x = 6614;
    std::cout << 6614;
    for (int i = 0; i < 6614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6614(int& val) {
    const int x = 6614;
    std::cout << 6614;
    for (int i = 0; i < 6614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6615(int& val) {
    func_in_6615(val);
    const int x = 6615;
    std::cout << 6615;
    for (int i = 0; i < 6615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6615(int& val) {
    const int x = 6615;
    std::cout << 6615;
    for (int i = 0; i < 6615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6616(int& val) {
    func_in_6616(val);
    const int x = 6616;
    std::cout << 6616;
    for (int i = 0; i < 6616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6616(int& val) {
    const int x = 6616;
    std::cout << 6616;
    for (int i = 0; i < 6616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6617(int& val) {
    func_in_6617(val);
    const int x = 6617;
    std::cout << 6617;
    for (int i = 0; i < 6617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6617(int& val) {
    const int x = 6617;
    std::cout << 6617;
    for (int i = 0; i < 6617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6618(int& val) {
    func_in_6618(val);
    const int x = 6618;
    std::cout << 6618;
    for (int i = 0; i < 6618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6618(int& val) {
    const int x = 6618;
    std::cout << 6618;
    for (int i = 0; i < 6618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6619(int& val) {
    func_in_6619(val);
    const int x = 6619;
    std::cout << 6619;
    for (int i = 0; i < 6619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6619(int& val) {
    const int x = 6619;
    std::cout << 6619;
    for (int i = 0; i < 6619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6620(int& val) {
    func_in_6620(val);
    const int x = 6620;
    std::cout << 6620;
    for (int i = 0; i < 6620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6620(int& val) {
    const int x = 6620;
    std::cout << 6620;
    for (int i = 0; i < 6620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6621(int& val) {
    func_in_6621(val);
    const int x = 6621;
    std::cout << 6621;
    for (int i = 0; i < 6621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6621(int& val) {
    const int x = 6621;
    std::cout << 6621;
    for (int i = 0; i < 6621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6622(int& val) {
    func_in_6622(val);
    const int x = 6622;
    std::cout << 6622;
    for (int i = 0; i < 6622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6622(int& val) {
    const int x = 6622;
    std::cout << 6622;
    for (int i = 0; i < 6622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6623(int& val) {
    func_in_6623(val);
    const int x = 6623;
    std::cout << 6623;
    for (int i = 0; i < 6623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6623(int& val) {
    const int x = 6623;
    std::cout << 6623;
    for (int i = 0; i < 6623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6624(int& val) {
    func_in_6624(val);
    const int x = 6624;
    std::cout << 6624;
    for (int i = 0; i < 6624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6624(int& val) {
    const int x = 6624;
    std::cout << 6624;
    for (int i = 0; i < 6624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6625(int& val) {
    func_in_6625(val);
    const int x = 6625;
    std::cout << 6625;
    for (int i = 0; i < 6625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6625(int& val) {
    const int x = 6625;
    std::cout << 6625;
    for (int i = 0; i < 6625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6626(int& val) {
    func_in_6626(val);
    const int x = 6626;
    std::cout << 6626;
    for (int i = 0; i < 6626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6626(int& val) {
    const int x = 6626;
    std::cout << 6626;
    for (int i = 0; i < 6626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6627(int& val) {
    func_in_6627(val);
    const int x = 6627;
    std::cout << 6627;
    for (int i = 0; i < 6627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6627(int& val) {
    const int x = 6627;
    std::cout << 6627;
    for (int i = 0; i < 6627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6628(int& val) {
    func_in_6628(val);
    const int x = 6628;
    std::cout << 6628;
    for (int i = 0; i < 6628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6628(int& val) {
    const int x = 6628;
    std::cout << 6628;
    for (int i = 0; i < 6628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6629(int& val) {
    func_in_6629(val);
    const int x = 6629;
    std::cout << 6629;
    for (int i = 0; i < 6629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6629(int& val) {
    const int x = 6629;
    std::cout << 6629;
    for (int i = 0; i < 6629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6630(int& val) {
    func_in_6630(val);
    const int x = 6630;
    std::cout << 6630;
    for (int i = 0; i < 6630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6630(int& val) {
    const int x = 6630;
    std::cout << 6630;
    for (int i = 0; i < 6630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6631(int& val) {
    func_in_6631(val);
    const int x = 6631;
    std::cout << 6631;
    for (int i = 0; i < 6631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6631(int& val) {
    const int x = 6631;
    std::cout << 6631;
    for (int i = 0; i < 6631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6632(int& val) {
    func_in_6632(val);
    const int x = 6632;
    std::cout << 6632;
    for (int i = 0; i < 6632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6632(int& val) {
    const int x = 6632;
    std::cout << 6632;
    for (int i = 0; i < 6632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6633(int& val) {
    func_in_6633(val);
    const int x = 6633;
    std::cout << 6633;
    for (int i = 0; i < 6633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6633(int& val) {
    const int x = 6633;
    std::cout << 6633;
    for (int i = 0; i < 6633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6634(int& val) {
    func_in_6634(val);
    const int x = 6634;
    std::cout << 6634;
    for (int i = 0; i < 6634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6634(int& val) {
    const int x = 6634;
    std::cout << 6634;
    for (int i = 0; i < 6634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6635(int& val) {
    func_in_6635(val);
    const int x = 6635;
    std::cout << 6635;
    for (int i = 0; i < 6635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6635(int& val) {
    const int x = 6635;
    std::cout << 6635;
    for (int i = 0; i < 6635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6636(int& val) {
    func_in_6636(val);
    const int x = 6636;
    std::cout << 6636;
    for (int i = 0; i < 6636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6636(int& val) {
    const int x = 6636;
    std::cout << 6636;
    for (int i = 0; i < 6636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6637(int& val) {
    func_in_6637(val);
    const int x = 6637;
    std::cout << 6637;
    for (int i = 0; i < 6637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6637(int& val) {
    const int x = 6637;
    std::cout << 6637;
    for (int i = 0; i < 6637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6638(int& val) {
    func_in_6638(val);
    const int x = 6638;
    std::cout << 6638;
    for (int i = 0; i < 6638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6638(int& val) {
    const int x = 6638;
    std::cout << 6638;
    for (int i = 0; i < 6638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6639(int& val) {
    func_in_6639(val);
    const int x = 6639;
    std::cout << 6639;
    for (int i = 0; i < 6639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6639(int& val) {
    const int x = 6639;
    std::cout << 6639;
    for (int i = 0; i < 6639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6640(int& val) {
    func_in_6640(val);
    const int x = 6640;
    std::cout << 6640;
    for (int i = 0; i < 6640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6640(int& val) {
    const int x = 6640;
    std::cout << 6640;
    for (int i = 0; i < 6640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6641(int& val) {
    func_in_6641(val);
    const int x = 6641;
    std::cout << 6641;
    for (int i = 0; i < 6641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6641(int& val) {
    const int x = 6641;
    std::cout << 6641;
    for (int i = 0; i < 6641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6642(int& val) {
    func_in_6642(val);
    const int x = 6642;
    std::cout << 6642;
    for (int i = 0; i < 6642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6642(int& val) {
    const int x = 6642;
    std::cout << 6642;
    for (int i = 0; i < 6642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6643(int& val) {
    func_in_6643(val);
    const int x = 6643;
    std::cout << 6643;
    for (int i = 0; i < 6643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6643(int& val) {
    const int x = 6643;
    std::cout << 6643;
    for (int i = 0; i < 6643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6644(int& val) {
    func_in_6644(val);
    const int x = 6644;
    std::cout << 6644;
    for (int i = 0; i < 6644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6644(int& val) {
    const int x = 6644;
    std::cout << 6644;
    for (int i = 0; i < 6644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6645(int& val) {
    func_in_6645(val);
    const int x = 6645;
    std::cout << 6645;
    for (int i = 0; i < 6645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6645(int& val) {
    const int x = 6645;
    std::cout << 6645;
    for (int i = 0; i < 6645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6646(int& val) {
    func_in_6646(val);
    const int x = 6646;
    std::cout << 6646;
    for (int i = 0; i < 6646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6646(int& val) {
    const int x = 6646;
    std::cout << 6646;
    for (int i = 0; i < 6646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6647(int& val) {
    func_in_6647(val);
    const int x = 6647;
    std::cout << 6647;
    for (int i = 0; i < 6647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6647(int& val) {
    const int x = 6647;
    std::cout << 6647;
    for (int i = 0; i < 6647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6648(int& val) {
    func_in_6648(val);
    const int x = 6648;
    std::cout << 6648;
    for (int i = 0; i < 6648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6648(int& val) {
    const int x = 6648;
    std::cout << 6648;
    for (int i = 0; i < 6648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6649(int& val) {
    func_in_6649(val);
    const int x = 6649;
    std::cout << 6649;
    for (int i = 0; i < 6649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6649(int& val) {
    const int x = 6649;
    std::cout << 6649;
    for (int i = 0; i < 6649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6650(int& val) {
    func_in_6650(val);
    const int x = 6650;
    std::cout << 6650;
    for (int i = 0; i < 6650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6650(int& val) {
    const int x = 6650;
    std::cout << 6650;
    for (int i = 0; i < 6650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6651(int& val) {
    func_in_6651(val);
    const int x = 6651;
    std::cout << 6651;
    for (int i = 0; i < 6651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6651(int& val) {
    const int x = 6651;
    std::cout << 6651;
    for (int i = 0; i < 6651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6652(int& val) {
    func_in_6652(val);
    const int x = 6652;
    std::cout << 6652;
    for (int i = 0; i < 6652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6652(int& val) {
    const int x = 6652;
    std::cout << 6652;
    for (int i = 0; i < 6652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6653(int& val) {
    func_in_6653(val);
    const int x = 6653;
    std::cout << 6653;
    for (int i = 0; i < 6653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6653(int& val) {
    const int x = 6653;
    std::cout << 6653;
    for (int i = 0; i < 6653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6654(int& val) {
    func_in_6654(val);
    const int x = 6654;
    std::cout << 6654;
    for (int i = 0; i < 6654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6654(int& val) {
    const int x = 6654;
    std::cout << 6654;
    for (int i = 0; i < 6654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6655(int& val) {
    func_in_6655(val);
    const int x = 6655;
    std::cout << 6655;
    for (int i = 0; i < 6655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6655(int& val) {
    const int x = 6655;
    std::cout << 6655;
    for (int i = 0; i < 6655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6656(int& val) {
    func_in_6656(val);
    const int x = 6656;
    std::cout << 6656;
    for (int i = 0; i < 6656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6656(int& val) {
    const int x = 6656;
    std::cout << 6656;
    for (int i = 0; i < 6656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6657(int& val) {
    func_in_6657(val);
    const int x = 6657;
    std::cout << 6657;
    for (int i = 0; i < 6657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6657(int& val) {
    const int x = 6657;
    std::cout << 6657;
    for (int i = 0; i < 6657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6658(int& val) {
    func_in_6658(val);
    const int x = 6658;
    std::cout << 6658;
    for (int i = 0; i < 6658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6658(int& val) {
    const int x = 6658;
    std::cout << 6658;
    for (int i = 0; i < 6658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6659(int& val) {
    func_in_6659(val);
    const int x = 6659;
    std::cout << 6659;
    for (int i = 0; i < 6659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6659(int& val) {
    const int x = 6659;
    std::cout << 6659;
    for (int i = 0; i < 6659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6660(int& val) {
    func_in_6660(val);
    const int x = 6660;
    std::cout << 6660;
    for (int i = 0; i < 6660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6660(int& val) {
    const int x = 6660;
    std::cout << 6660;
    for (int i = 0; i < 6660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6661(int& val) {
    func_in_6661(val);
    const int x = 6661;
    std::cout << 6661;
    for (int i = 0; i < 6661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6661(int& val) {
    const int x = 6661;
    std::cout << 6661;
    for (int i = 0; i < 6661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6662(int& val) {
    func_in_6662(val);
    const int x = 6662;
    std::cout << 6662;
    for (int i = 0; i < 6662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6662(int& val) {
    const int x = 6662;
    std::cout << 6662;
    for (int i = 0; i < 6662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6663(int& val) {
    func_in_6663(val);
    const int x = 6663;
    std::cout << 6663;
    for (int i = 0; i < 6663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6663(int& val) {
    const int x = 6663;
    std::cout << 6663;
    for (int i = 0; i < 6663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6664(int& val) {
    func_in_6664(val);
    const int x = 6664;
    std::cout << 6664;
    for (int i = 0; i < 6664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6664(int& val) {
    const int x = 6664;
    std::cout << 6664;
    for (int i = 0; i < 6664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6665(int& val) {
    func_in_6665(val);
    const int x = 6665;
    std::cout << 6665;
    for (int i = 0; i < 6665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6665(int& val) {
    const int x = 6665;
    std::cout << 6665;
    for (int i = 0; i < 6665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6666(int& val) {
    func_in_6666(val);
    const int x = 6666;
    std::cout << 6666;
    for (int i = 0; i < 6666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6666(int& val) {
    const int x = 6666;
    std::cout << 6666;
    for (int i = 0; i < 6666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6667(int& val) {
    func_in_6667(val);
    const int x = 6667;
    std::cout << 6667;
    for (int i = 0; i < 6667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6667(int& val) {
    const int x = 6667;
    std::cout << 6667;
    for (int i = 0; i < 6667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6668(int& val) {
    func_in_6668(val);
    const int x = 6668;
    std::cout << 6668;
    for (int i = 0; i < 6668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6668(int& val) {
    const int x = 6668;
    std::cout << 6668;
    for (int i = 0; i < 6668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6669(int& val) {
    func_in_6669(val);
    const int x = 6669;
    std::cout << 6669;
    for (int i = 0; i < 6669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6669(int& val) {
    const int x = 6669;
    std::cout << 6669;
    for (int i = 0; i < 6669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6670(int& val) {
    func_in_6670(val);
    const int x = 6670;
    std::cout << 6670;
    for (int i = 0; i < 6670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6670(int& val) {
    const int x = 6670;
    std::cout << 6670;
    for (int i = 0; i < 6670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6671(int& val) {
    func_in_6671(val);
    const int x = 6671;
    std::cout << 6671;
    for (int i = 0; i < 6671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6671(int& val) {
    const int x = 6671;
    std::cout << 6671;
    for (int i = 0; i < 6671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6672(int& val) {
    func_in_6672(val);
    const int x = 6672;
    std::cout << 6672;
    for (int i = 0; i < 6672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6672(int& val) {
    const int x = 6672;
    std::cout << 6672;
    for (int i = 0; i < 6672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6673(int& val) {
    func_in_6673(val);
    const int x = 6673;
    std::cout << 6673;
    for (int i = 0; i < 6673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6673(int& val) {
    const int x = 6673;
    std::cout << 6673;
    for (int i = 0; i < 6673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6674(int& val) {
    func_in_6674(val);
    const int x = 6674;
    std::cout << 6674;
    for (int i = 0; i < 6674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6674(int& val) {
    const int x = 6674;
    std::cout << 6674;
    for (int i = 0; i < 6674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6675(int& val) {
    func_in_6675(val);
    const int x = 6675;
    std::cout << 6675;
    for (int i = 0; i < 6675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6675(int& val) {
    const int x = 6675;
    std::cout << 6675;
    for (int i = 0; i < 6675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6676(int& val) {
    func_in_6676(val);
    const int x = 6676;
    std::cout << 6676;
    for (int i = 0; i < 6676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6676(int& val) {
    const int x = 6676;
    std::cout << 6676;
    for (int i = 0; i < 6676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6677(int& val) {
    func_in_6677(val);
    const int x = 6677;
    std::cout << 6677;
    for (int i = 0; i < 6677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6677(int& val) {
    const int x = 6677;
    std::cout << 6677;
    for (int i = 0; i < 6677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6678(int& val) {
    func_in_6678(val);
    const int x = 6678;
    std::cout << 6678;
    for (int i = 0; i < 6678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6678(int& val) {
    const int x = 6678;
    std::cout << 6678;
    for (int i = 0; i < 6678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6679(int& val) {
    func_in_6679(val);
    const int x = 6679;
    std::cout << 6679;
    for (int i = 0; i < 6679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6679(int& val) {
    const int x = 6679;
    std::cout << 6679;
    for (int i = 0; i < 6679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6680(int& val) {
    func_in_6680(val);
    const int x = 6680;
    std::cout << 6680;
    for (int i = 0; i < 6680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6680(int& val) {
    const int x = 6680;
    std::cout << 6680;
    for (int i = 0; i < 6680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6681(int& val) {
    func_in_6681(val);
    const int x = 6681;
    std::cout << 6681;
    for (int i = 0; i < 6681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6681(int& val) {
    const int x = 6681;
    std::cout << 6681;
    for (int i = 0; i < 6681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6682(int& val) {
    func_in_6682(val);
    const int x = 6682;
    std::cout << 6682;
    for (int i = 0; i < 6682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6682(int& val) {
    const int x = 6682;
    std::cout << 6682;
    for (int i = 0; i < 6682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6683(int& val) {
    func_in_6683(val);
    const int x = 6683;
    std::cout << 6683;
    for (int i = 0; i < 6683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6683(int& val) {
    const int x = 6683;
    std::cout << 6683;
    for (int i = 0; i < 6683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6684(int& val) {
    func_in_6684(val);
    const int x = 6684;
    std::cout << 6684;
    for (int i = 0; i < 6684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6684(int& val) {
    const int x = 6684;
    std::cout << 6684;
    for (int i = 0; i < 6684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6685(int& val) {
    func_in_6685(val);
    const int x = 6685;
    std::cout << 6685;
    for (int i = 0; i < 6685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6685(int& val) {
    const int x = 6685;
    std::cout << 6685;
    for (int i = 0; i < 6685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6686(int& val) {
    func_in_6686(val);
    const int x = 6686;
    std::cout << 6686;
    for (int i = 0; i < 6686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6686(int& val) {
    const int x = 6686;
    std::cout << 6686;
    for (int i = 0; i < 6686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6687(int& val) {
    func_in_6687(val);
    const int x = 6687;
    std::cout << 6687;
    for (int i = 0; i < 6687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6687(int& val) {
    const int x = 6687;
    std::cout << 6687;
    for (int i = 0; i < 6687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6688(int& val) {
    func_in_6688(val);
    const int x = 6688;
    std::cout << 6688;
    for (int i = 0; i < 6688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6688(int& val) {
    const int x = 6688;
    std::cout << 6688;
    for (int i = 0; i < 6688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6689(int& val) {
    func_in_6689(val);
    const int x = 6689;
    std::cout << 6689;
    for (int i = 0; i < 6689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6689(int& val) {
    const int x = 6689;
    std::cout << 6689;
    for (int i = 0; i < 6689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6690(int& val) {
    func_in_6690(val);
    const int x = 6690;
    std::cout << 6690;
    for (int i = 0; i < 6690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6690(int& val) {
    const int x = 6690;
    std::cout << 6690;
    for (int i = 0; i < 6690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6691(int& val) {
    func_in_6691(val);
    const int x = 6691;
    std::cout << 6691;
    for (int i = 0; i < 6691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6691(int& val) {
    const int x = 6691;
    std::cout << 6691;
    for (int i = 0; i < 6691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6692(int& val) {
    func_in_6692(val);
    const int x = 6692;
    std::cout << 6692;
    for (int i = 0; i < 6692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6692(int& val) {
    const int x = 6692;
    std::cout << 6692;
    for (int i = 0; i < 6692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6693(int& val) {
    func_in_6693(val);
    const int x = 6693;
    std::cout << 6693;
    for (int i = 0; i < 6693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6693(int& val) {
    const int x = 6693;
    std::cout << 6693;
    for (int i = 0; i < 6693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6694(int& val) {
    func_in_6694(val);
    const int x = 6694;
    std::cout << 6694;
    for (int i = 0; i < 6694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6694(int& val) {
    const int x = 6694;
    std::cout << 6694;
    for (int i = 0; i < 6694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6695(int& val) {
    func_in_6695(val);
    const int x = 6695;
    std::cout << 6695;
    for (int i = 0; i < 6695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6695(int& val) {
    const int x = 6695;
    std::cout << 6695;
    for (int i = 0; i < 6695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6696(int& val) {
    func_in_6696(val);
    const int x = 6696;
    std::cout << 6696;
    for (int i = 0; i < 6696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6696(int& val) {
    const int x = 6696;
    std::cout << 6696;
    for (int i = 0; i < 6696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6697(int& val) {
    func_in_6697(val);
    const int x = 6697;
    std::cout << 6697;
    for (int i = 0; i < 6697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6697(int& val) {
    const int x = 6697;
    std::cout << 6697;
    for (int i = 0; i < 6697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6698(int& val) {
    func_in_6698(val);
    const int x = 6698;
    std::cout << 6698;
    for (int i = 0; i < 6698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6698(int& val) {
    const int x = 6698;
    std::cout << 6698;
    for (int i = 0; i < 6698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6699(int& val) {
    func_in_6699(val);
    const int x = 6699;
    std::cout << 6699;
    for (int i = 0; i < 6699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6699(int& val) {
    const int x = 6699;
    std::cout << 6699;
    for (int i = 0; i < 6699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6700(int& val) {
    func_in_6700(val);
    const int x = 6700;
    std::cout << 6700;
    for (int i = 0; i < 6700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6700(int& val) {
    const int x = 6700;
    std::cout << 6700;
    for (int i = 0; i < 6700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6701(int& val) {
    func_in_6701(val);
    const int x = 6701;
    std::cout << 6701;
    for (int i = 0; i < 6701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6701(int& val) {
    const int x = 6701;
    std::cout << 6701;
    for (int i = 0; i < 6701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6702(int& val) {
    func_in_6702(val);
    const int x = 6702;
    std::cout << 6702;
    for (int i = 0; i < 6702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6702(int& val) {
    const int x = 6702;
    std::cout << 6702;
    for (int i = 0; i < 6702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6703(int& val) {
    func_in_6703(val);
    const int x = 6703;
    std::cout << 6703;
    for (int i = 0; i < 6703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6703(int& val) {
    const int x = 6703;
    std::cout << 6703;
    for (int i = 0; i < 6703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6704(int& val) {
    func_in_6704(val);
    const int x = 6704;
    std::cout << 6704;
    for (int i = 0; i < 6704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6704(int& val) {
    const int x = 6704;
    std::cout << 6704;
    for (int i = 0; i < 6704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6705(int& val) {
    func_in_6705(val);
    const int x = 6705;
    std::cout << 6705;
    for (int i = 0; i < 6705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6705(int& val) {
    const int x = 6705;
    std::cout << 6705;
    for (int i = 0; i < 6705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6706(int& val) {
    func_in_6706(val);
    const int x = 6706;
    std::cout << 6706;
    for (int i = 0; i < 6706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6706(int& val) {
    const int x = 6706;
    std::cout << 6706;
    for (int i = 0; i < 6706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6707(int& val) {
    func_in_6707(val);
    const int x = 6707;
    std::cout << 6707;
    for (int i = 0; i < 6707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6707(int& val) {
    const int x = 6707;
    std::cout << 6707;
    for (int i = 0; i < 6707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6708(int& val) {
    func_in_6708(val);
    const int x = 6708;
    std::cout << 6708;
    for (int i = 0; i < 6708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6708(int& val) {
    const int x = 6708;
    std::cout << 6708;
    for (int i = 0; i < 6708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6709(int& val) {
    func_in_6709(val);
    const int x = 6709;
    std::cout << 6709;
    for (int i = 0; i < 6709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6709(int& val) {
    const int x = 6709;
    std::cout << 6709;
    for (int i = 0; i < 6709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6710(int& val) {
    func_in_6710(val);
    const int x = 6710;
    std::cout << 6710;
    for (int i = 0; i < 6710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6710(int& val) {
    const int x = 6710;
    std::cout << 6710;
    for (int i = 0; i < 6710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6711(int& val) {
    func_in_6711(val);
    const int x = 6711;
    std::cout << 6711;
    for (int i = 0; i < 6711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6711(int& val) {
    const int x = 6711;
    std::cout << 6711;
    for (int i = 0; i < 6711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6712(int& val) {
    func_in_6712(val);
    const int x = 6712;
    std::cout << 6712;
    for (int i = 0; i < 6712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6712(int& val) {
    const int x = 6712;
    std::cout << 6712;
    for (int i = 0; i < 6712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6713(int& val) {
    func_in_6713(val);
    const int x = 6713;
    std::cout << 6713;
    for (int i = 0; i < 6713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6713(int& val) {
    const int x = 6713;
    std::cout << 6713;
    for (int i = 0; i < 6713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6714(int& val) {
    func_in_6714(val);
    const int x = 6714;
    std::cout << 6714;
    for (int i = 0; i < 6714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6714(int& val) {
    const int x = 6714;
    std::cout << 6714;
    for (int i = 0; i < 6714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6715(int& val) {
    func_in_6715(val);
    const int x = 6715;
    std::cout << 6715;
    for (int i = 0; i < 6715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6715(int& val) {
    const int x = 6715;
    std::cout << 6715;
    for (int i = 0; i < 6715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6716(int& val) {
    func_in_6716(val);
    const int x = 6716;
    std::cout << 6716;
    for (int i = 0; i < 6716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6716(int& val) {
    const int x = 6716;
    std::cout << 6716;
    for (int i = 0; i < 6716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6717(int& val) {
    func_in_6717(val);
    const int x = 6717;
    std::cout << 6717;
    for (int i = 0; i < 6717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6717(int& val) {
    const int x = 6717;
    std::cout << 6717;
    for (int i = 0; i < 6717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6718(int& val) {
    func_in_6718(val);
    const int x = 6718;
    std::cout << 6718;
    for (int i = 0; i < 6718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6718(int& val) {
    const int x = 6718;
    std::cout << 6718;
    for (int i = 0; i < 6718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6719(int& val) {
    func_in_6719(val);
    const int x = 6719;
    std::cout << 6719;
    for (int i = 0; i < 6719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6719(int& val) {
    const int x = 6719;
    std::cout << 6719;
    for (int i = 0; i < 6719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6720(int& val) {
    func_in_6720(val);
    const int x = 6720;
    std::cout << 6720;
    for (int i = 0; i < 6720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6720(int& val) {
    const int x = 6720;
    std::cout << 6720;
    for (int i = 0; i < 6720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6721(int& val) {
    func_in_6721(val);
    const int x = 6721;
    std::cout << 6721;
    for (int i = 0; i < 6721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6721(int& val) {
    const int x = 6721;
    std::cout << 6721;
    for (int i = 0; i < 6721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6722(int& val) {
    func_in_6722(val);
    const int x = 6722;
    std::cout << 6722;
    for (int i = 0; i < 6722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6722(int& val) {
    const int x = 6722;
    std::cout << 6722;
    for (int i = 0; i < 6722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6723(int& val) {
    func_in_6723(val);
    const int x = 6723;
    std::cout << 6723;
    for (int i = 0; i < 6723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6723(int& val) {
    const int x = 6723;
    std::cout << 6723;
    for (int i = 0; i < 6723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6724(int& val) {
    func_in_6724(val);
    const int x = 6724;
    std::cout << 6724;
    for (int i = 0; i < 6724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6724(int& val) {
    const int x = 6724;
    std::cout << 6724;
    for (int i = 0; i < 6724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6725(int& val) {
    func_in_6725(val);
    const int x = 6725;
    std::cout << 6725;
    for (int i = 0; i < 6725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6725(int& val) {
    const int x = 6725;
    std::cout << 6725;
    for (int i = 0; i < 6725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6726(int& val) {
    func_in_6726(val);
    const int x = 6726;
    std::cout << 6726;
    for (int i = 0; i < 6726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6726(int& val) {
    const int x = 6726;
    std::cout << 6726;
    for (int i = 0; i < 6726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6727(int& val) {
    func_in_6727(val);
    const int x = 6727;
    std::cout << 6727;
    for (int i = 0; i < 6727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6727(int& val) {
    const int x = 6727;
    std::cout << 6727;
    for (int i = 0; i < 6727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6728(int& val) {
    func_in_6728(val);
    const int x = 6728;
    std::cout << 6728;
    for (int i = 0; i < 6728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6728(int& val) {
    const int x = 6728;
    std::cout << 6728;
    for (int i = 0; i < 6728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6729(int& val) {
    func_in_6729(val);
    const int x = 6729;
    std::cout << 6729;
    for (int i = 0; i < 6729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6729(int& val) {
    const int x = 6729;
    std::cout << 6729;
    for (int i = 0; i < 6729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6730(int& val) {
    func_in_6730(val);
    const int x = 6730;
    std::cout << 6730;
    for (int i = 0; i < 6730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6730(int& val) {
    const int x = 6730;
    std::cout << 6730;
    for (int i = 0; i < 6730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6731(int& val) {
    func_in_6731(val);
    const int x = 6731;
    std::cout << 6731;
    for (int i = 0; i < 6731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6731(int& val) {
    const int x = 6731;
    std::cout << 6731;
    for (int i = 0; i < 6731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6732(int& val) {
    func_in_6732(val);
    const int x = 6732;
    std::cout << 6732;
    for (int i = 0; i < 6732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6732(int& val) {
    const int x = 6732;
    std::cout << 6732;
    for (int i = 0; i < 6732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6733(int& val) {
    func_in_6733(val);
    const int x = 6733;
    std::cout << 6733;
    for (int i = 0; i < 6733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6733(int& val) {
    const int x = 6733;
    std::cout << 6733;
    for (int i = 0; i < 6733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6734(int& val) {
    func_in_6734(val);
    const int x = 6734;
    std::cout << 6734;
    for (int i = 0; i < 6734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6734(int& val) {
    const int x = 6734;
    std::cout << 6734;
    for (int i = 0; i < 6734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6735(int& val) {
    func_in_6735(val);
    const int x = 6735;
    std::cout << 6735;
    for (int i = 0; i < 6735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6735(int& val) {
    const int x = 6735;
    std::cout << 6735;
    for (int i = 0; i < 6735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6736(int& val) {
    func_in_6736(val);
    const int x = 6736;
    std::cout << 6736;
    for (int i = 0; i < 6736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6736(int& val) {
    const int x = 6736;
    std::cout << 6736;
    for (int i = 0; i < 6736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6737(int& val) {
    func_in_6737(val);
    const int x = 6737;
    std::cout << 6737;
    for (int i = 0; i < 6737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6737(int& val) {
    const int x = 6737;
    std::cout << 6737;
    for (int i = 0; i < 6737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6738(int& val) {
    func_in_6738(val);
    const int x = 6738;
    std::cout << 6738;
    for (int i = 0; i < 6738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6738(int& val) {
    const int x = 6738;
    std::cout << 6738;
    for (int i = 0; i < 6738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6739(int& val) {
    func_in_6739(val);
    const int x = 6739;
    std::cout << 6739;
    for (int i = 0; i < 6739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6739(int& val) {
    const int x = 6739;
    std::cout << 6739;
    for (int i = 0; i < 6739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6740(int& val) {
    func_in_6740(val);
    const int x = 6740;
    std::cout << 6740;
    for (int i = 0; i < 6740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6740(int& val) {
    const int x = 6740;
    std::cout << 6740;
    for (int i = 0; i < 6740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6741(int& val) {
    func_in_6741(val);
    const int x = 6741;
    std::cout << 6741;
    for (int i = 0; i < 6741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6741(int& val) {
    const int x = 6741;
    std::cout << 6741;
    for (int i = 0; i < 6741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6742(int& val) {
    func_in_6742(val);
    const int x = 6742;
    std::cout << 6742;
    for (int i = 0; i < 6742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6742(int& val) {
    const int x = 6742;
    std::cout << 6742;
    for (int i = 0; i < 6742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6743(int& val) {
    func_in_6743(val);
    const int x = 6743;
    std::cout << 6743;
    for (int i = 0; i < 6743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6743(int& val) {
    const int x = 6743;
    std::cout << 6743;
    for (int i = 0; i < 6743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6744(int& val) {
    func_in_6744(val);
    const int x = 6744;
    std::cout << 6744;
    for (int i = 0; i < 6744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6744(int& val) {
    const int x = 6744;
    std::cout << 6744;
    for (int i = 0; i < 6744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6745(int& val) {
    func_in_6745(val);
    const int x = 6745;
    std::cout << 6745;
    for (int i = 0; i < 6745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6745(int& val) {
    const int x = 6745;
    std::cout << 6745;
    for (int i = 0; i < 6745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6746(int& val) {
    func_in_6746(val);
    const int x = 6746;
    std::cout << 6746;
    for (int i = 0; i < 6746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6746(int& val) {
    const int x = 6746;
    std::cout << 6746;
    for (int i = 0; i < 6746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6747(int& val) {
    func_in_6747(val);
    const int x = 6747;
    std::cout << 6747;
    for (int i = 0; i < 6747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6747(int& val) {
    const int x = 6747;
    std::cout << 6747;
    for (int i = 0; i < 6747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6748(int& val) {
    func_in_6748(val);
    const int x = 6748;
    std::cout << 6748;
    for (int i = 0; i < 6748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6748(int& val) {
    const int x = 6748;
    std::cout << 6748;
    for (int i = 0; i < 6748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6749(int& val) {
    func_in_6749(val);
    const int x = 6749;
    std::cout << 6749;
    for (int i = 0; i < 6749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6749(int& val) {
    const int x = 6749;
    std::cout << 6749;
    for (int i = 0; i < 6749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6750(int& val) {
    func_in_6750(val);
    const int x = 6750;
    std::cout << 6750;
    for (int i = 0; i < 6750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6750(int& val) {
    const int x = 6750;
    std::cout << 6750;
    for (int i = 0; i < 6750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6751(int& val) {
    func_in_6751(val);
    const int x = 6751;
    std::cout << 6751;
    for (int i = 0; i < 6751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6751(int& val) {
    const int x = 6751;
    std::cout << 6751;
    for (int i = 0; i < 6751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6752(int& val) {
    func_in_6752(val);
    const int x = 6752;
    std::cout << 6752;
    for (int i = 0; i < 6752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6752(int& val) {
    const int x = 6752;
    std::cout << 6752;
    for (int i = 0; i < 6752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6753(int& val) {
    func_in_6753(val);
    const int x = 6753;
    std::cout << 6753;
    for (int i = 0; i < 6753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6753(int& val) {
    const int x = 6753;
    std::cout << 6753;
    for (int i = 0; i < 6753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6754(int& val) {
    func_in_6754(val);
    const int x = 6754;
    std::cout << 6754;
    for (int i = 0; i < 6754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6754(int& val) {
    const int x = 6754;
    std::cout << 6754;
    for (int i = 0; i < 6754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6755(int& val) {
    func_in_6755(val);
    const int x = 6755;
    std::cout << 6755;
    for (int i = 0; i < 6755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6755(int& val) {
    const int x = 6755;
    std::cout << 6755;
    for (int i = 0; i < 6755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6756(int& val) {
    func_in_6756(val);
    const int x = 6756;
    std::cout << 6756;
    for (int i = 0; i < 6756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6756(int& val) {
    const int x = 6756;
    std::cout << 6756;
    for (int i = 0; i < 6756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6757(int& val) {
    func_in_6757(val);
    const int x = 6757;
    std::cout << 6757;
    for (int i = 0; i < 6757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6757(int& val) {
    const int x = 6757;
    std::cout << 6757;
    for (int i = 0; i < 6757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6758(int& val) {
    func_in_6758(val);
    const int x = 6758;
    std::cout << 6758;
    for (int i = 0; i < 6758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6758(int& val) {
    const int x = 6758;
    std::cout << 6758;
    for (int i = 0; i < 6758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6759(int& val) {
    func_in_6759(val);
    const int x = 6759;
    std::cout << 6759;
    for (int i = 0; i < 6759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6759(int& val) {
    const int x = 6759;
    std::cout << 6759;
    for (int i = 0; i < 6759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6760(int& val) {
    func_in_6760(val);
    const int x = 6760;
    std::cout << 6760;
    for (int i = 0; i < 6760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6760(int& val) {
    const int x = 6760;
    std::cout << 6760;
    for (int i = 0; i < 6760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6761(int& val) {
    func_in_6761(val);
    const int x = 6761;
    std::cout << 6761;
    for (int i = 0; i < 6761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6761(int& val) {
    const int x = 6761;
    std::cout << 6761;
    for (int i = 0; i < 6761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6762(int& val) {
    func_in_6762(val);
    const int x = 6762;
    std::cout << 6762;
    for (int i = 0; i < 6762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6762(int& val) {
    const int x = 6762;
    std::cout << 6762;
    for (int i = 0; i < 6762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6763(int& val) {
    func_in_6763(val);
    const int x = 6763;
    std::cout << 6763;
    for (int i = 0; i < 6763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6763(int& val) {
    const int x = 6763;
    std::cout << 6763;
    for (int i = 0; i < 6763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6764(int& val) {
    func_in_6764(val);
    const int x = 6764;
    std::cout << 6764;
    for (int i = 0; i < 6764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6764(int& val) {
    const int x = 6764;
    std::cout << 6764;
    for (int i = 0; i < 6764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6765(int& val) {
    func_in_6765(val);
    const int x = 6765;
    std::cout << 6765;
    for (int i = 0; i < 6765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6765(int& val) {
    const int x = 6765;
    std::cout << 6765;
    for (int i = 0; i < 6765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6766(int& val) {
    func_in_6766(val);
    const int x = 6766;
    std::cout << 6766;
    for (int i = 0; i < 6766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6766(int& val) {
    const int x = 6766;
    std::cout << 6766;
    for (int i = 0; i < 6766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6767(int& val) {
    func_in_6767(val);
    const int x = 6767;
    std::cout << 6767;
    for (int i = 0; i < 6767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6767(int& val) {
    const int x = 6767;
    std::cout << 6767;
    for (int i = 0; i < 6767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6768(int& val) {
    func_in_6768(val);
    const int x = 6768;
    std::cout << 6768;
    for (int i = 0; i < 6768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6768(int& val) {
    const int x = 6768;
    std::cout << 6768;
    for (int i = 0; i < 6768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6769(int& val) {
    func_in_6769(val);
    const int x = 6769;
    std::cout << 6769;
    for (int i = 0; i < 6769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6769(int& val) {
    const int x = 6769;
    std::cout << 6769;
    for (int i = 0; i < 6769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6770(int& val) {
    func_in_6770(val);
    const int x = 6770;
    std::cout << 6770;
    for (int i = 0; i < 6770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6770(int& val) {
    const int x = 6770;
    std::cout << 6770;
    for (int i = 0; i < 6770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6771(int& val) {
    func_in_6771(val);
    const int x = 6771;
    std::cout << 6771;
    for (int i = 0; i < 6771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6771(int& val) {
    const int x = 6771;
    std::cout << 6771;
    for (int i = 0; i < 6771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6772(int& val) {
    func_in_6772(val);
    const int x = 6772;
    std::cout << 6772;
    for (int i = 0; i < 6772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6772(int& val) {
    const int x = 6772;
    std::cout << 6772;
    for (int i = 0; i < 6772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6773(int& val) {
    func_in_6773(val);
    const int x = 6773;
    std::cout << 6773;
    for (int i = 0; i < 6773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6773(int& val) {
    const int x = 6773;
    std::cout << 6773;
    for (int i = 0; i < 6773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6774(int& val) {
    func_in_6774(val);
    const int x = 6774;
    std::cout << 6774;
    for (int i = 0; i < 6774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6774(int& val) {
    const int x = 6774;
    std::cout << 6774;
    for (int i = 0; i < 6774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6775(int& val) {
    func_in_6775(val);
    const int x = 6775;
    std::cout << 6775;
    for (int i = 0; i < 6775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6775(int& val) {
    const int x = 6775;
    std::cout << 6775;
    for (int i = 0; i < 6775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6776(int& val) {
    func_in_6776(val);
    const int x = 6776;
    std::cout << 6776;
    for (int i = 0; i < 6776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6776(int& val) {
    const int x = 6776;
    std::cout << 6776;
    for (int i = 0; i < 6776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6777(int& val) {
    func_in_6777(val);
    const int x = 6777;
    std::cout << 6777;
    for (int i = 0; i < 6777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6777(int& val) {
    const int x = 6777;
    std::cout << 6777;
    for (int i = 0; i < 6777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6778(int& val) {
    func_in_6778(val);
    const int x = 6778;
    std::cout << 6778;
    for (int i = 0; i < 6778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6778(int& val) {
    const int x = 6778;
    std::cout << 6778;
    for (int i = 0; i < 6778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6779(int& val) {
    func_in_6779(val);
    const int x = 6779;
    std::cout << 6779;
    for (int i = 0; i < 6779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6779(int& val) {
    const int x = 6779;
    std::cout << 6779;
    for (int i = 0; i < 6779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6780(int& val) {
    func_in_6780(val);
    const int x = 6780;
    std::cout << 6780;
    for (int i = 0; i < 6780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6780(int& val) {
    const int x = 6780;
    std::cout << 6780;
    for (int i = 0; i < 6780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6781(int& val) {
    func_in_6781(val);
    const int x = 6781;
    std::cout << 6781;
    for (int i = 0; i < 6781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6781(int& val) {
    const int x = 6781;
    std::cout << 6781;
    for (int i = 0; i < 6781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6782(int& val) {
    func_in_6782(val);
    const int x = 6782;
    std::cout << 6782;
    for (int i = 0; i < 6782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6782(int& val) {
    const int x = 6782;
    std::cout << 6782;
    for (int i = 0; i < 6782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6783(int& val) {
    func_in_6783(val);
    const int x = 6783;
    std::cout << 6783;
    for (int i = 0; i < 6783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6783(int& val) {
    const int x = 6783;
    std::cout << 6783;
    for (int i = 0; i < 6783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6784(int& val) {
    func_in_6784(val);
    const int x = 6784;
    std::cout << 6784;
    for (int i = 0; i < 6784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6784(int& val) {
    const int x = 6784;
    std::cout << 6784;
    for (int i = 0; i < 6784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6785(int& val) {
    func_in_6785(val);
    const int x = 6785;
    std::cout << 6785;
    for (int i = 0; i < 6785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6785(int& val) {
    const int x = 6785;
    std::cout << 6785;
    for (int i = 0; i < 6785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6786(int& val) {
    func_in_6786(val);
    const int x = 6786;
    std::cout << 6786;
    for (int i = 0; i < 6786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6786(int& val) {
    const int x = 6786;
    std::cout << 6786;
    for (int i = 0; i < 6786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6787(int& val) {
    func_in_6787(val);
    const int x = 6787;
    std::cout << 6787;
    for (int i = 0; i < 6787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6787(int& val) {
    const int x = 6787;
    std::cout << 6787;
    for (int i = 0; i < 6787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6788(int& val) {
    func_in_6788(val);
    const int x = 6788;
    std::cout << 6788;
    for (int i = 0; i < 6788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6788(int& val) {
    const int x = 6788;
    std::cout << 6788;
    for (int i = 0; i < 6788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6789(int& val) {
    func_in_6789(val);
    const int x = 6789;
    std::cout << 6789;
    for (int i = 0; i < 6789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6789(int& val) {
    const int x = 6789;
    std::cout << 6789;
    for (int i = 0; i < 6789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6790(int& val) {
    func_in_6790(val);
    const int x = 6790;
    std::cout << 6790;
    for (int i = 0; i < 6790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6790(int& val) {
    const int x = 6790;
    std::cout << 6790;
    for (int i = 0; i < 6790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6791(int& val) {
    func_in_6791(val);
    const int x = 6791;
    std::cout << 6791;
    for (int i = 0; i < 6791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6791(int& val) {
    const int x = 6791;
    std::cout << 6791;
    for (int i = 0; i < 6791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6792(int& val) {
    func_in_6792(val);
    const int x = 6792;
    std::cout << 6792;
    for (int i = 0; i < 6792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6792(int& val) {
    const int x = 6792;
    std::cout << 6792;
    for (int i = 0; i < 6792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6793(int& val) {
    func_in_6793(val);
    const int x = 6793;
    std::cout << 6793;
    for (int i = 0; i < 6793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6793(int& val) {
    const int x = 6793;
    std::cout << 6793;
    for (int i = 0; i < 6793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6794(int& val) {
    func_in_6794(val);
    const int x = 6794;
    std::cout << 6794;
    for (int i = 0; i < 6794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6794(int& val) {
    const int x = 6794;
    std::cout << 6794;
    for (int i = 0; i < 6794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6795(int& val) {
    func_in_6795(val);
    const int x = 6795;
    std::cout << 6795;
    for (int i = 0; i < 6795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6795(int& val) {
    const int x = 6795;
    std::cout << 6795;
    for (int i = 0; i < 6795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6796(int& val) {
    func_in_6796(val);
    const int x = 6796;
    std::cout << 6796;
    for (int i = 0; i < 6796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6796(int& val) {
    const int x = 6796;
    std::cout << 6796;
    for (int i = 0; i < 6796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6797(int& val) {
    func_in_6797(val);
    const int x = 6797;
    std::cout << 6797;
    for (int i = 0; i < 6797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6797(int& val) {
    const int x = 6797;
    std::cout << 6797;
    for (int i = 0; i < 6797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6798(int& val) {
    func_in_6798(val);
    const int x = 6798;
    std::cout << 6798;
    for (int i = 0; i < 6798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6798(int& val) {
    const int x = 6798;
    std::cout << 6798;
    for (int i = 0; i < 6798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6799(int& val) {
    func_in_6799(val);
    const int x = 6799;
    std::cout << 6799;
    for (int i = 0; i < 6799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6799(int& val) {
    const int x = 6799;
    std::cout << 6799;
    for (int i = 0; i < 6799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6800(int& val) {
    func_in_6800(val);
    const int x = 6800;
    std::cout << 6800;
    for (int i = 0; i < 6800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6800(int& val) {
    const int x = 6800;
    std::cout << 6800;
    for (int i = 0; i < 6800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6801(int& val) {
    func_in_6801(val);
    const int x = 6801;
    std::cout << 6801;
    for (int i = 0; i < 6801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6801(int& val) {
    const int x = 6801;
    std::cout << 6801;
    for (int i = 0; i < 6801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6802(int& val) {
    func_in_6802(val);
    const int x = 6802;
    std::cout << 6802;
    for (int i = 0; i < 6802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6802(int& val) {
    const int x = 6802;
    std::cout << 6802;
    for (int i = 0; i < 6802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6803(int& val) {
    func_in_6803(val);
    const int x = 6803;
    std::cout << 6803;
    for (int i = 0; i < 6803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6803(int& val) {
    const int x = 6803;
    std::cout << 6803;
    for (int i = 0; i < 6803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6804(int& val) {
    func_in_6804(val);
    const int x = 6804;
    std::cout << 6804;
    for (int i = 0; i < 6804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6804(int& val) {
    const int x = 6804;
    std::cout << 6804;
    for (int i = 0; i < 6804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6805(int& val) {
    func_in_6805(val);
    const int x = 6805;
    std::cout << 6805;
    for (int i = 0; i < 6805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6805(int& val) {
    const int x = 6805;
    std::cout << 6805;
    for (int i = 0; i < 6805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6806(int& val) {
    func_in_6806(val);
    const int x = 6806;
    std::cout << 6806;
    for (int i = 0; i < 6806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6806(int& val) {
    const int x = 6806;
    std::cout << 6806;
    for (int i = 0; i < 6806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6807(int& val) {
    func_in_6807(val);
    const int x = 6807;
    std::cout << 6807;
    for (int i = 0; i < 6807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6807(int& val) {
    const int x = 6807;
    std::cout << 6807;
    for (int i = 0; i < 6807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6808(int& val) {
    func_in_6808(val);
    const int x = 6808;
    std::cout << 6808;
    for (int i = 0; i < 6808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6808(int& val) {
    const int x = 6808;
    std::cout << 6808;
    for (int i = 0; i < 6808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6809(int& val) {
    func_in_6809(val);
    const int x = 6809;
    std::cout << 6809;
    for (int i = 0; i < 6809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6809(int& val) {
    const int x = 6809;
    std::cout << 6809;
    for (int i = 0; i < 6809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6810(int& val) {
    func_in_6810(val);
    const int x = 6810;
    std::cout << 6810;
    for (int i = 0; i < 6810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6810(int& val) {
    const int x = 6810;
    std::cout << 6810;
    for (int i = 0; i < 6810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6811(int& val) {
    func_in_6811(val);
    const int x = 6811;
    std::cout << 6811;
    for (int i = 0; i < 6811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6811(int& val) {
    const int x = 6811;
    std::cout << 6811;
    for (int i = 0; i < 6811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6812(int& val) {
    func_in_6812(val);
    const int x = 6812;
    std::cout << 6812;
    for (int i = 0; i < 6812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6812(int& val) {
    const int x = 6812;
    std::cout << 6812;
    for (int i = 0; i < 6812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6813(int& val) {
    func_in_6813(val);
    const int x = 6813;
    std::cout << 6813;
    for (int i = 0; i < 6813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6813(int& val) {
    const int x = 6813;
    std::cout << 6813;
    for (int i = 0; i < 6813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6814(int& val) {
    func_in_6814(val);
    const int x = 6814;
    std::cout << 6814;
    for (int i = 0; i < 6814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6814(int& val) {
    const int x = 6814;
    std::cout << 6814;
    for (int i = 0; i < 6814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6815(int& val) {
    func_in_6815(val);
    const int x = 6815;
    std::cout << 6815;
    for (int i = 0; i < 6815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6815(int& val) {
    const int x = 6815;
    std::cout << 6815;
    for (int i = 0; i < 6815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6816(int& val) {
    func_in_6816(val);
    const int x = 6816;
    std::cout << 6816;
    for (int i = 0; i < 6816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6816(int& val) {
    const int x = 6816;
    std::cout << 6816;
    for (int i = 0; i < 6816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6817(int& val) {
    func_in_6817(val);
    const int x = 6817;
    std::cout << 6817;
    for (int i = 0; i < 6817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6817(int& val) {
    const int x = 6817;
    std::cout << 6817;
    for (int i = 0; i < 6817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6818(int& val) {
    func_in_6818(val);
    const int x = 6818;
    std::cout << 6818;
    for (int i = 0; i < 6818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6818(int& val) {
    const int x = 6818;
    std::cout << 6818;
    for (int i = 0; i < 6818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6819(int& val) {
    func_in_6819(val);
    const int x = 6819;
    std::cout << 6819;
    for (int i = 0; i < 6819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6819(int& val) {
    const int x = 6819;
    std::cout << 6819;
    for (int i = 0; i < 6819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6820(int& val) {
    func_in_6820(val);
    const int x = 6820;
    std::cout << 6820;
    for (int i = 0; i < 6820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6820(int& val) {
    const int x = 6820;
    std::cout << 6820;
    for (int i = 0; i < 6820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6821(int& val) {
    func_in_6821(val);
    const int x = 6821;
    std::cout << 6821;
    for (int i = 0; i < 6821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6821(int& val) {
    const int x = 6821;
    std::cout << 6821;
    for (int i = 0; i < 6821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6822(int& val) {
    func_in_6822(val);
    const int x = 6822;
    std::cout << 6822;
    for (int i = 0; i < 6822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6822(int& val) {
    const int x = 6822;
    std::cout << 6822;
    for (int i = 0; i < 6822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6823(int& val) {
    func_in_6823(val);
    const int x = 6823;
    std::cout << 6823;
    for (int i = 0; i < 6823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6823(int& val) {
    const int x = 6823;
    std::cout << 6823;
    for (int i = 0; i < 6823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6824(int& val) {
    func_in_6824(val);
    const int x = 6824;
    std::cout << 6824;
    for (int i = 0; i < 6824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6824(int& val) {
    const int x = 6824;
    std::cout << 6824;
    for (int i = 0; i < 6824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6825(int& val) {
    func_in_6825(val);
    const int x = 6825;
    std::cout << 6825;
    for (int i = 0; i < 6825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6825(int& val) {
    const int x = 6825;
    std::cout << 6825;
    for (int i = 0; i < 6825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6826(int& val) {
    func_in_6826(val);
    const int x = 6826;
    std::cout << 6826;
    for (int i = 0; i < 6826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6826(int& val) {
    const int x = 6826;
    std::cout << 6826;
    for (int i = 0; i < 6826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6827(int& val) {
    func_in_6827(val);
    const int x = 6827;
    std::cout << 6827;
    for (int i = 0; i < 6827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6827(int& val) {
    const int x = 6827;
    std::cout << 6827;
    for (int i = 0; i < 6827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6828(int& val) {
    func_in_6828(val);
    const int x = 6828;
    std::cout << 6828;
    for (int i = 0; i < 6828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6828(int& val) {
    const int x = 6828;
    std::cout << 6828;
    for (int i = 0; i < 6828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6829(int& val) {
    func_in_6829(val);
    const int x = 6829;
    std::cout << 6829;
    for (int i = 0; i < 6829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6829(int& val) {
    const int x = 6829;
    std::cout << 6829;
    for (int i = 0; i < 6829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6830(int& val) {
    func_in_6830(val);
    const int x = 6830;
    std::cout << 6830;
    for (int i = 0; i < 6830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6830(int& val) {
    const int x = 6830;
    std::cout << 6830;
    for (int i = 0; i < 6830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6831(int& val) {
    func_in_6831(val);
    const int x = 6831;
    std::cout << 6831;
    for (int i = 0; i < 6831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6831(int& val) {
    const int x = 6831;
    std::cout << 6831;
    for (int i = 0; i < 6831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6832(int& val) {
    func_in_6832(val);
    const int x = 6832;
    std::cout << 6832;
    for (int i = 0; i < 6832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6832(int& val) {
    const int x = 6832;
    std::cout << 6832;
    for (int i = 0; i < 6832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6833(int& val) {
    func_in_6833(val);
    const int x = 6833;
    std::cout << 6833;
    for (int i = 0; i < 6833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6833(int& val) {
    const int x = 6833;
    std::cout << 6833;
    for (int i = 0; i < 6833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6834(int& val) {
    func_in_6834(val);
    const int x = 6834;
    std::cout << 6834;
    for (int i = 0; i < 6834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6834(int& val) {
    const int x = 6834;
    std::cout << 6834;
    for (int i = 0; i < 6834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6835(int& val) {
    func_in_6835(val);
    const int x = 6835;
    std::cout << 6835;
    for (int i = 0; i < 6835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6835(int& val) {
    const int x = 6835;
    std::cout << 6835;
    for (int i = 0; i < 6835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6836(int& val) {
    func_in_6836(val);
    const int x = 6836;
    std::cout << 6836;
    for (int i = 0; i < 6836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6836(int& val) {
    const int x = 6836;
    std::cout << 6836;
    for (int i = 0; i < 6836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6837(int& val) {
    func_in_6837(val);
    const int x = 6837;
    std::cout << 6837;
    for (int i = 0; i < 6837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6837(int& val) {
    const int x = 6837;
    std::cout << 6837;
    for (int i = 0; i < 6837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6838(int& val) {
    func_in_6838(val);
    const int x = 6838;
    std::cout << 6838;
    for (int i = 0; i < 6838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6838(int& val) {
    const int x = 6838;
    std::cout << 6838;
    for (int i = 0; i < 6838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6839(int& val) {
    func_in_6839(val);
    const int x = 6839;
    std::cout << 6839;
    for (int i = 0; i < 6839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6839(int& val) {
    const int x = 6839;
    std::cout << 6839;
    for (int i = 0; i < 6839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6840(int& val) {
    func_in_6840(val);
    const int x = 6840;
    std::cout << 6840;
    for (int i = 0; i < 6840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6840(int& val) {
    const int x = 6840;
    std::cout << 6840;
    for (int i = 0; i < 6840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6841(int& val) {
    func_in_6841(val);
    const int x = 6841;
    std::cout << 6841;
    for (int i = 0; i < 6841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6841(int& val) {
    const int x = 6841;
    std::cout << 6841;
    for (int i = 0; i < 6841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6842(int& val) {
    func_in_6842(val);
    const int x = 6842;
    std::cout << 6842;
    for (int i = 0; i < 6842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6842(int& val) {
    const int x = 6842;
    std::cout << 6842;
    for (int i = 0; i < 6842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6843(int& val) {
    func_in_6843(val);
    const int x = 6843;
    std::cout << 6843;
    for (int i = 0; i < 6843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6843(int& val) {
    const int x = 6843;
    std::cout << 6843;
    for (int i = 0; i < 6843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6844(int& val) {
    func_in_6844(val);
    const int x = 6844;
    std::cout << 6844;
    for (int i = 0; i < 6844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6844(int& val) {
    const int x = 6844;
    std::cout << 6844;
    for (int i = 0; i < 6844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6845(int& val) {
    func_in_6845(val);
    const int x = 6845;
    std::cout << 6845;
    for (int i = 0; i < 6845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6845(int& val) {
    const int x = 6845;
    std::cout << 6845;
    for (int i = 0; i < 6845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6846(int& val) {
    func_in_6846(val);
    const int x = 6846;
    std::cout << 6846;
    for (int i = 0; i < 6846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6846(int& val) {
    const int x = 6846;
    std::cout << 6846;
    for (int i = 0; i < 6846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6847(int& val) {
    func_in_6847(val);
    const int x = 6847;
    std::cout << 6847;
    for (int i = 0; i < 6847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6847(int& val) {
    const int x = 6847;
    std::cout << 6847;
    for (int i = 0; i < 6847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6848(int& val) {
    func_in_6848(val);
    const int x = 6848;
    std::cout << 6848;
    for (int i = 0; i < 6848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6848(int& val) {
    const int x = 6848;
    std::cout << 6848;
    for (int i = 0; i < 6848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6849(int& val) {
    func_in_6849(val);
    const int x = 6849;
    std::cout << 6849;
    for (int i = 0; i < 6849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6849(int& val) {
    const int x = 6849;
    std::cout << 6849;
    for (int i = 0; i < 6849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6850(int& val) {
    func_in_6850(val);
    const int x = 6850;
    std::cout << 6850;
    for (int i = 0; i < 6850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6850(int& val) {
    const int x = 6850;
    std::cout << 6850;
    for (int i = 0; i < 6850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6851(int& val) {
    func_in_6851(val);
    const int x = 6851;
    std::cout << 6851;
    for (int i = 0; i < 6851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6851(int& val) {
    const int x = 6851;
    std::cout << 6851;
    for (int i = 0; i < 6851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6852(int& val) {
    func_in_6852(val);
    const int x = 6852;
    std::cout << 6852;
    for (int i = 0; i < 6852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6852(int& val) {
    const int x = 6852;
    std::cout << 6852;
    for (int i = 0; i < 6852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6853(int& val) {
    func_in_6853(val);
    const int x = 6853;
    std::cout << 6853;
    for (int i = 0; i < 6853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6853(int& val) {
    const int x = 6853;
    std::cout << 6853;
    for (int i = 0; i < 6853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6854(int& val) {
    func_in_6854(val);
    const int x = 6854;
    std::cout << 6854;
    for (int i = 0; i < 6854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6854(int& val) {
    const int x = 6854;
    std::cout << 6854;
    for (int i = 0; i < 6854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6855(int& val) {
    func_in_6855(val);
    const int x = 6855;
    std::cout << 6855;
    for (int i = 0; i < 6855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6855(int& val) {
    const int x = 6855;
    std::cout << 6855;
    for (int i = 0; i < 6855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6856(int& val) {
    func_in_6856(val);
    const int x = 6856;
    std::cout << 6856;
    for (int i = 0; i < 6856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6856(int& val) {
    const int x = 6856;
    std::cout << 6856;
    for (int i = 0; i < 6856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6857(int& val) {
    func_in_6857(val);
    const int x = 6857;
    std::cout << 6857;
    for (int i = 0; i < 6857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6857(int& val) {
    const int x = 6857;
    std::cout << 6857;
    for (int i = 0; i < 6857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6858(int& val) {
    func_in_6858(val);
    const int x = 6858;
    std::cout << 6858;
    for (int i = 0; i < 6858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6858(int& val) {
    const int x = 6858;
    std::cout << 6858;
    for (int i = 0; i < 6858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6859(int& val) {
    func_in_6859(val);
    const int x = 6859;
    std::cout << 6859;
    for (int i = 0; i < 6859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6859(int& val) {
    const int x = 6859;
    std::cout << 6859;
    for (int i = 0; i < 6859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6860(int& val) {
    func_in_6860(val);
    const int x = 6860;
    std::cout << 6860;
    for (int i = 0; i < 6860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6860(int& val) {
    const int x = 6860;
    std::cout << 6860;
    for (int i = 0; i < 6860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6861(int& val) {
    func_in_6861(val);
    const int x = 6861;
    std::cout << 6861;
    for (int i = 0; i < 6861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6861(int& val) {
    const int x = 6861;
    std::cout << 6861;
    for (int i = 0; i < 6861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6862(int& val) {
    func_in_6862(val);
    const int x = 6862;
    std::cout << 6862;
    for (int i = 0; i < 6862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6862(int& val) {
    const int x = 6862;
    std::cout << 6862;
    for (int i = 0; i < 6862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6863(int& val) {
    func_in_6863(val);
    const int x = 6863;
    std::cout << 6863;
    for (int i = 0; i < 6863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6863(int& val) {
    const int x = 6863;
    std::cout << 6863;
    for (int i = 0; i < 6863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6864(int& val) {
    func_in_6864(val);
    const int x = 6864;
    std::cout << 6864;
    for (int i = 0; i < 6864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6864(int& val) {
    const int x = 6864;
    std::cout << 6864;
    for (int i = 0; i < 6864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6865(int& val) {
    func_in_6865(val);
    const int x = 6865;
    std::cout << 6865;
    for (int i = 0; i < 6865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6865(int& val) {
    const int x = 6865;
    std::cout << 6865;
    for (int i = 0; i < 6865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6866(int& val) {
    func_in_6866(val);
    const int x = 6866;
    std::cout << 6866;
    for (int i = 0; i < 6866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6866(int& val) {
    const int x = 6866;
    std::cout << 6866;
    for (int i = 0; i < 6866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6867(int& val) {
    func_in_6867(val);
    const int x = 6867;
    std::cout << 6867;
    for (int i = 0; i < 6867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6867(int& val) {
    const int x = 6867;
    std::cout << 6867;
    for (int i = 0; i < 6867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6868(int& val) {
    func_in_6868(val);
    const int x = 6868;
    std::cout << 6868;
    for (int i = 0; i < 6868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6868(int& val) {
    const int x = 6868;
    std::cout << 6868;
    for (int i = 0; i < 6868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6869(int& val) {
    func_in_6869(val);
    const int x = 6869;
    std::cout << 6869;
    for (int i = 0; i < 6869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6869(int& val) {
    const int x = 6869;
    std::cout << 6869;
    for (int i = 0; i < 6869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6870(int& val) {
    func_in_6870(val);
    const int x = 6870;
    std::cout << 6870;
    for (int i = 0; i < 6870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6870(int& val) {
    const int x = 6870;
    std::cout << 6870;
    for (int i = 0; i < 6870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6871(int& val) {
    func_in_6871(val);
    const int x = 6871;
    std::cout << 6871;
    for (int i = 0; i < 6871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6871(int& val) {
    const int x = 6871;
    std::cout << 6871;
    for (int i = 0; i < 6871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6872(int& val) {
    func_in_6872(val);
    const int x = 6872;
    std::cout << 6872;
    for (int i = 0; i < 6872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6872(int& val) {
    const int x = 6872;
    std::cout << 6872;
    for (int i = 0; i < 6872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6873(int& val) {
    func_in_6873(val);
    const int x = 6873;
    std::cout << 6873;
    for (int i = 0; i < 6873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6873(int& val) {
    const int x = 6873;
    std::cout << 6873;
    for (int i = 0; i < 6873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6874(int& val) {
    func_in_6874(val);
    const int x = 6874;
    std::cout << 6874;
    for (int i = 0; i < 6874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6874(int& val) {
    const int x = 6874;
    std::cout << 6874;
    for (int i = 0; i < 6874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6875(int& val) {
    func_in_6875(val);
    const int x = 6875;
    std::cout << 6875;
    for (int i = 0; i < 6875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6875(int& val) {
    const int x = 6875;
    std::cout << 6875;
    for (int i = 0; i < 6875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6876(int& val) {
    func_in_6876(val);
    const int x = 6876;
    std::cout << 6876;
    for (int i = 0; i < 6876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6876(int& val) {
    const int x = 6876;
    std::cout << 6876;
    for (int i = 0; i < 6876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6877(int& val) {
    func_in_6877(val);
    const int x = 6877;
    std::cout << 6877;
    for (int i = 0; i < 6877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6877(int& val) {
    const int x = 6877;
    std::cout << 6877;
    for (int i = 0; i < 6877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6878(int& val) {
    func_in_6878(val);
    const int x = 6878;
    std::cout << 6878;
    for (int i = 0; i < 6878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6878(int& val) {
    const int x = 6878;
    std::cout << 6878;
    for (int i = 0; i < 6878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6879(int& val) {
    func_in_6879(val);
    const int x = 6879;
    std::cout << 6879;
    for (int i = 0; i < 6879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6879(int& val) {
    const int x = 6879;
    std::cout << 6879;
    for (int i = 0; i < 6879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6880(int& val) {
    func_in_6880(val);
    const int x = 6880;
    std::cout << 6880;
    for (int i = 0; i < 6880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6880(int& val) {
    const int x = 6880;
    std::cout << 6880;
    for (int i = 0; i < 6880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6881(int& val) {
    func_in_6881(val);
    const int x = 6881;
    std::cout << 6881;
    for (int i = 0; i < 6881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6881(int& val) {
    const int x = 6881;
    std::cout << 6881;
    for (int i = 0; i < 6881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6882(int& val) {
    func_in_6882(val);
    const int x = 6882;
    std::cout << 6882;
    for (int i = 0; i < 6882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6882(int& val) {
    const int x = 6882;
    std::cout << 6882;
    for (int i = 0; i < 6882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6883(int& val) {
    func_in_6883(val);
    const int x = 6883;
    std::cout << 6883;
    for (int i = 0; i < 6883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6883(int& val) {
    const int x = 6883;
    std::cout << 6883;
    for (int i = 0; i < 6883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6884(int& val) {
    func_in_6884(val);
    const int x = 6884;
    std::cout << 6884;
    for (int i = 0; i < 6884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6884(int& val) {
    const int x = 6884;
    std::cout << 6884;
    for (int i = 0; i < 6884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6885(int& val) {
    func_in_6885(val);
    const int x = 6885;
    std::cout << 6885;
    for (int i = 0; i < 6885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6885(int& val) {
    const int x = 6885;
    std::cout << 6885;
    for (int i = 0; i < 6885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6886(int& val) {
    func_in_6886(val);
    const int x = 6886;
    std::cout << 6886;
    for (int i = 0; i < 6886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6886(int& val) {
    const int x = 6886;
    std::cout << 6886;
    for (int i = 0; i < 6886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6887(int& val) {
    func_in_6887(val);
    const int x = 6887;
    std::cout << 6887;
    for (int i = 0; i < 6887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6887(int& val) {
    const int x = 6887;
    std::cout << 6887;
    for (int i = 0; i < 6887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6888(int& val) {
    func_in_6888(val);
    const int x = 6888;
    std::cout << 6888;
    for (int i = 0; i < 6888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6888(int& val) {
    const int x = 6888;
    std::cout << 6888;
    for (int i = 0; i < 6888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6889(int& val) {
    func_in_6889(val);
    const int x = 6889;
    std::cout << 6889;
    for (int i = 0; i < 6889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6889(int& val) {
    const int x = 6889;
    std::cout << 6889;
    for (int i = 0; i < 6889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6890(int& val) {
    func_in_6890(val);
    const int x = 6890;
    std::cout << 6890;
    for (int i = 0; i < 6890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6890(int& val) {
    const int x = 6890;
    std::cout << 6890;
    for (int i = 0; i < 6890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6891(int& val) {
    func_in_6891(val);
    const int x = 6891;
    std::cout << 6891;
    for (int i = 0; i < 6891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6891(int& val) {
    const int x = 6891;
    std::cout << 6891;
    for (int i = 0; i < 6891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6892(int& val) {
    func_in_6892(val);
    const int x = 6892;
    std::cout << 6892;
    for (int i = 0; i < 6892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6892(int& val) {
    const int x = 6892;
    std::cout << 6892;
    for (int i = 0; i < 6892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6893(int& val) {
    func_in_6893(val);
    const int x = 6893;
    std::cout << 6893;
    for (int i = 0; i < 6893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6893(int& val) {
    const int x = 6893;
    std::cout << 6893;
    for (int i = 0; i < 6893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6894(int& val) {
    func_in_6894(val);
    const int x = 6894;
    std::cout << 6894;
    for (int i = 0; i < 6894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6894(int& val) {
    const int x = 6894;
    std::cout << 6894;
    for (int i = 0; i < 6894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6895(int& val) {
    func_in_6895(val);
    const int x = 6895;
    std::cout << 6895;
    for (int i = 0; i < 6895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6895(int& val) {
    const int x = 6895;
    std::cout << 6895;
    for (int i = 0; i < 6895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6896(int& val) {
    func_in_6896(val);
    const int x = 6896;
    std::cout << 6896;
    for (int i = 0; i < 6896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6896(int& val) {
    const int x = 6896;
    std::cout << 6896;
    for (int i = 0; i < 6896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6897(int& val) {
    func_in_6897(val);
    const int x = 6897;
    std::cout << 6897;
    for (int i = 0; i < 6897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6897(int& val) {
    const int x = 6897;
    std::cout << 6897;
    for (int i = 0; i < 6897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6898(int& val) {
    func_in_6898(val);
    const int x = 6898;
    std::cout << 6898;
    for (int i = 0; i < 6898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6898(int& val) {
    const int x = 6898;
    std::cout << 6898;
    for (int i = 0; i < 6898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6899(int& val) {
    func_in_6899(val);
    const int x = 6899;
    std::cout << 6899;
    for (int i = 0; i < 6899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6899(int& val) {
    const int x = 6899;
    std::cout << 6899;
    for (int i = 0; i < 6899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6900(int& val) {
    func_in_6900(val);
    const int x = 6900;
    std::cout << 6900;
    for (int i = 0; i < 6900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6900(int& val) {
    const int x = 6900;
    std::cout << 6900;
    for (int i = 0; i < 6900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6901(int& val) {
    func_in_6901(val);
    const int x = 6901;
    std::cout << 6901;
    for (int i = 0; i < 6901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6901(int& val) {
    const int x = 6901;
    std::cout << 6901;
    for (int i = 0; i < 6901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6902(int& val) {
    func_in_6902(val);
    const int x = 6902;
    std::cout << 6902;
    for (int i = 0; i < 6902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6902(int& val) {
    const int x = 6902;
    std::cout << 6902;
    for (int i = 0; i < 6902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6903(int& val) {
    func_in_6903(val);
    const int x = 6903;
    std::cout << 6903;
    for (int i = 0; i < 6903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6903(int& val) {
    const int x = 6903;
    std::cout << 6903;
    for (int i = 0; i < 6903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6904(int& val) {
    func_in_6904(val);
    const int x = 6904;
    std::cout << 6904;
    for (int i = 0; i < 6904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6904(int& val) {
    const int x = 6904;
    std::cout << 6904;
    for (int i = 0; i < 6904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6905(int& val) {
    func_in_6905(val);
    const int x = 6905;
    std::cout << 6905;
    for (int i = 0; i < 6905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6905(int& val) {
    const int x = 6905;
    std::cout << 6905;
    for (int i = 0; i < 6905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6906(int& val) {
    func_in_6906(val);
    const int x = 6906;
    std::cout << 6906;
    for (int i = 0; i < 6906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6906(int& val) {
    const int x = 6906;
    std::cout << 6906;
    for (int i = 0; i < 6906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6907(int& val) {
    func_in_6907(val);
    const int x = 6907;
    std::cout << 6907;
    for (int i = 0; i < 6907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6907(int& val) {
    const int x = 6907;
    std::cout << 6907;
    for (int i = 0; i < 6907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6908(int& val) {
    func_in_6908(val);
    const int x = 6908;
    std::cout << 6908;
    for (int i = 0; i < 6908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6908(int& val) {
    const int x = 6908;
    std::cout << 6908;
    for (int i = 0; i < 6908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6909(int& val) {
    func_in_6909(val);
    const int x = 6909;
    std::cout << 6909;
    for (int i = 0; i < 6909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6909(int& val) {
    const int x = 6909;
    std::cout << 6909;
    for (int i = 0; i < 6909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6910(int& val) {
    func_in_6910(val);
    const int x = 6910;
    std::cout << 6910;
    for (int i = 0; i < 6910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6910(int& val) {
    const int x = 6910;
    std::cout << 6910;
    for (int i = 0; i < 6910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6911(int& val) {
    func_in_6911(val);
    const int x = 6911;
    std::cout << 6911;
    for (int i = 0; i < 6911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6911(int& val) {
    const int x = 6911;
    std::cout << 6911;
    for (int i = 0; i < 6911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6912(int& val) {
    func_in_6912(val);
    const int x = 6912;
    std::cout << 6912;
    for (int i = 0; i < 6912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6912(int& val) {
    const int x = 6912;
    std::cout << 6912;
    for (int i = 0; i < 6912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6913(int& val) {
    func_in_6913(val);
    const int x = 6913;
    std::cout << 6913;
    for (int i = 0; i < 6913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6913(int& val) {
    const int x = 6913;
    std::cout << 6913;
    for (int i = 0; i < 6913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6914(int& val) {
    func_in_6914(val);
    const int x = 6914;
    std::cout << 6914;
    for (int i = 0; i < 6914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6914(int& val) {
    const int x = 6914;
    std::cout << 6914;
    for (int i = 0; i < 6914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6915(int& val) {
    func_in_6915(val);
    const int x = 6915;
    std::cout << 6915;
    for (int i = 0; i < 6915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6915(int& val) {
    const int x = 6915;
    std::cout << 6915;
    for (int i = 0; i < 6915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6916(int& val) {
    func_in_6916(val);
    const int x = 6916;
    std::cout << 6916;
    for (int i = 0; i < 6916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6916(int& val) {
    const int x = 6916;
    std::cout << 6916;
    for (int i = 0; i < 6916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6917(int& val) {
    func_in_6917(val);
    const int x = 6917;
    std::cout << 6917;
    for (int i = 0; i < 6917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6917(int& val) {
    const int x = 6917;
    std::cout << 6917;
    for (int i = 0; i < 6917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6918(int& val) {
    func_in_6918(val);
    const int x = 6918;
    std::cout << 6918;
    for (int i = 0; i < 6918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6918(int& val) {
    const int x = 6918;
    std::cout << 6918;
    for (int i = 0; i < 6918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6919(int& val) {
    func_in_6919(val);
    const int x = 6919;
    std::cout << 6919;
    for (int i = 0; i < 6919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6919(int& val) {
    const int x = 6919;
    std::cout << 6919;
    for (int i = 0; i < 6919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6920(int& val) {
    func_in_6920(val);
    const int x = 6920;
    std::cout << 6920;
    for (int i = 0; i < 6920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6920(int& val) {
    const int x = 6920;
    std::cout << 6920;
    for (int i = 0; i < 6920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6921(int& val) {
    func_in_6921(val);
    const int x = 6921;
    std::cout << 6921;
    for (int i = 0; i < 6921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6921(int& val) {
    const int x = 6921;
    std::cout << 6921;
    for (int i = 0; i < 6921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6922(int& val) {
    func_in_6922(val);
    const int x = 6922;
    std::cout << 6922;
    for (int i = 0; i < 6922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6922(int& val) {
    const int x = 6922;
    std::cout << 6922;
    for (int i = 0; i < 6922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6923(int& val) {
    func_in_6923(val);
    const int x = 6923;
    std::cout << 6923;
    for (int i = 0; i < 6923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6923(int& val) {
    const int x = 6923;
    std::cout << 6923;
    for (int i = 0; i < 6923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6924(int& val) {
    func_in_6924(val);
    const int x = 6924;
    std::cout << 6924;
    for (int i = 0; i < 6924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6924(int& val) {
    const int x = 6924;
    std::cout << 6924;
    for (int i = 0; i < 6924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6925(int& val) {
    func_in_6925(val);
    const int x = 6925;
    std::cout << 6925;
    for (int i = 0; i < 6925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6925(int& val) {
    const int x = 6925;
    std::cout << 6925;
    for (int i = 0; i < 6925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6926(int& val) {
    func_in_6926(val);
    const int x = 6926;
    std::cout << 6926;
    for (int i = 0; i < 6926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6926(int& val) {
    const int x = 6926;
    std::cout << 6926;
    for (int i = 0; i < 6926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6927(int& val) {
    func_in_6927(val);
    const int x = 6927;
    std::cout << 6927;
    for (int i = 0; i < 6927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6927(int& val) {
    const int x = 6927;
    std::cout << 6927;
    for (int i = 0; i < 6927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6928(int& val) {
    func_in_6928(val);
    const int x = 6928;
    std::cout << 6928;
    for (int i = 0; i < 6928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6928(int& val) {
    const int x = 6928;
    std::cout << 6928;
    for (int i = 0; i < 6928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6929(int& val) {
    func_in_6929(val);
    const int x = 6929;
    std::cout << 6929;
    for (int i = 0; i < 6929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6929(int& val) {
    const int x = 6929;
    std::cout << 6929;
    for (int i = 0; i < 6929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6930(int& val) {
    func_in_6930(val);
    const int x = 6930;
    std::cout << 6930;
    for (int i = 0; i < 6930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6930(int& val) {
    const int x = 6930;
    std::cout << 6930;
    for (int i = 0; i < 6930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6931(int& val) {
    func_in_6931(val);
    const int x = 6931;
    std::cout << 6931;
    for (int i = 0; i < 6931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6931(int& val) {
    const int x = 6931;
    std::cout << 6931;
    for (int i = 0; i < 6931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6932(int& val) {
    func_in_6932(val);
    const int x = 6932;
    std::cout << 6932;
    for (int i = 0; i < 6932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6932(int& val) {
    const int x = 6932;
    std::cout << 6932;
    for (int i = 0; i < 6932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6933(int& val) {
    func_in_6933(val);
    const int x = 6933;
    std::cout << 6933;
    for (int i = 0; i < 6933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6933(int& val) {
    const int x = 6933;
    std::cout << 6933;
    for (int i = 0; i < 6933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6934(int& val) {
    func_in_6934(val);
    const int x = 6934;
    std::cout << 6934;
    for (int i = 0; i < 6934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6934(int& val) {
    const int x = 6934;
    std::cout << 6934;
    for (int i = 0; i < 6934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6935(int& val) {
    func_in_6935(val);
    const int x = 6935;
    std::cout << 6935;
    for (int i = 0; i < 6935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6935(int& val) {
    const int x = 6935;
    std::cout << 6935;
    for (int i = 0; i < 6935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6936(int& val) {
    func_in_6936(val);
    const int x = 6936;
    std::cout << 6936;
    for (int i = 0; i < 6936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6936(int& val) {
    const int x = 6936;
    std::cout << 6936;
    for (int i = 0; i < 6936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6937(int& val) {
    func_in_6937(val);
    const int x = 6937;
    std::cout << 6937;
    for (int i = 0; i < 6937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6937(int& val) {
    const int x = 6937;
    std::cout << 6937;
    for (int i = 0; i < 6937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6938(int& val) {
    func_in_6938(val);
    const int x = 6938;
    std::cout << 6938;
    for (int i = 0; i < 6938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6938(int& val) {
    const int x = 6938;
    std::cout << 6938;
    for (int i = 0; i < 6938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6939(int& val) {
    func_in_6939(val);
    const int x = 6939;
    std::cout << 6939;
    for (int i = 0; i < 6939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6939(int& val) {
    const int x = 6939;
    std::cout << 6939;
    for (int i = 0; i < 6939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6940(int& val) {
    func_in_6940(val);
    const int x = 6940;
    std::cout << 6940;
    for (int i = 0; i < 6940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6940(int& val) {
    const int x = 6940;
    std::cout << 6940;
    for (int i = 0; i < 6940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6941(int& val) {
    func_in_6941(val);
    const int x = 6941;
    std::cout << 6941;
    for (int i = 0; i < 6941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6941(int& val) {
    const int x = 6941;
    std::cout << 6941;
    for (int i = 0; i < 6941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6942(int& val) {
    func_in_6942(val);
    const int x = 6942;
    std::cout << 6942;
    for (int i = 0; i < 6942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6942(int& val) {
    const int x = 6942;
    std::cout << 6942;
    for (int i = 0; i < 6942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6943(int& val) {
    func_in_6943(val);
    const int x = 6943;
    std::cout << 6943;
    for (int i = 0; i < 6943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6943(int& val) {
    const int x = 6943;
    std::cout << 6943;
    for (int i = 0; i < 6943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6944(int& val) {
    func_in_6944(val);
    const int x = 6944;
    std::cout << 6944;
    for (int i = 0; i < 6944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6944(int& val) {
    const int x = 6944;
    std::cout << 6944;
    for (int i = 0; i < 6944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6945(int& val) {
    func_in_6945(val);
    const int x = 6945;
    std::cout << 6945;
    for (int i = 0; i < 6945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6945(int& val) {
    const int x = 6945;
    std::cout << 6945;
    for (int i = 0; i < 6945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6946(int& val) {
    func_in_6946(val);
    const int x = 6946;
    std::cout << 6946;
    for (int i = 0; i < 6946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6946(int& val) {
    const int x = 6946;
    std::cout << 6946;
    for (int i = 0; i < 6946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6947(int& val) {
    func_in_6947(val);
    const int x = 6947;
    std::cout << 6947;
    for (int i = 0; i < 6947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6947(int& val) {
    const int x = 6947;
    std::cout << 6947;
    for (int i = 0; i < 6947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6948(int& val) {
    func_in_6948(val);
    const int x = 6948;
    std::cout << 6948;
    for (int i = 0; i < 6948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6948(int& val) {
    const int x = 6948;
    std::cout << 6948;
    for (int i = 0; i < 6948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6949(int& val) {
    func_in_6949(val);
    const int x = 6949;
    std::cout << 6949;
    for (int i = 0; i < 6949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6949(int& val) {
    const int x = 6949;
    std::cout << 6949;
    for (int i = 0; i < 6949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6950(int& val) {
    func_in_6950(val);
    const int x = 6950;
    std::cout << 6950;
    for (int i = 0; i < 6950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6950(int& val) {
    const int x = 6950;
    std::cout << 6950;
    for (int i = 0; i < 6950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6951(int& val) {
    func_in_6951(val);
    const int x = 6951;
    std::cout << 6951;
    for (int i = 0; i < 6951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6951(int& val) {
    const int x = 6951;
    std::cout << 6951;
    for (int i = 0; i < 6951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6952(int& val) {
    func_in_6952(val);
    const int x = 6952;
    std::cout << 6952;
    for (int i = 0; i < 6952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6952(int& val) {
    const int x = 6952;
    std::cout << 6952;
    for (int i = 0; i < 6952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6953(int& val) {
    func_in_6953(val);
    const int x = 6953;
    std::cout << 6953;
    for (int i = 0; i < 6953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6953(int& val) {
    const int x = 6953;
    std::cout << 6953;
    for (int i = 0; i < 6953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6954(int& val) {
    func_in_6954(val);
    const int x = 6954;
    std::cout << 6954;
    for (int i = 0; i < 6954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6954(int& val) {
    const int x = 6954;
    std::cout << 6954;
    for (int i = 0; i < 6954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6955(int& val) {
    func_in_6955(val);
    const int x = 6955;
    std::cout << 6955;
    for (int i = 0; i < 6955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6955(int& val) {
    const int x = 6955;
    std::cout << 6955;
    for (int i = 0; i < 6955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6956(int& val) {
    func_in_6956(val);
    const int x = 6956;
    std::cout << 6956;
    for (int i = 0; i < 6956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6956(int& val) {
    const int x = 6956;
    std::cout << 6956;
    for (int i = 0; i < 6956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6957(int& val) {
    func_in_6957(val);
    const int x = 6957;
    std::cout << 6957;
    for (int i = 0; i < 6957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6957(int& val) {
    const int x = 6957;
    std::cout << 6957;
    for (int i = 0; i < 6957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6958(int& val) {
    func_in_6958(val);
    const int x = 6958;
    std::cout << 6958;
    for (int i = 0; i < 6958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6958(int& val) {
    const int x = 6958;
    std::cout << 6958;
    for (int i = 0; i < 6958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6959(int& val) {
    func_in_6959(val);
    const int x = 6959;
    std::cout << 6959;
    for (int i = 0; i < 6959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6959(int& val) {
    const int x = 6959;
    std::cout << 6959;
    for (int i = 0; i < 6959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6960(int& val) {
    func_in_6960(val);
    const int x = 6960;
    std::cout << 6960;
    for (int i = 0; i < 6960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6960(int& val) {
    const int x = 6960;
    std::cout << 6960;
    for (int i = 0; i < 6960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6961(int& val) {
    func_in_6961(val);
    const int x = 6961;
    std::cout << 6961;
    for (int i = 0; i < 6961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6961(int& val) {
    const int x = 6961;
    std::cout << 6961;
    for (int i = 0; i < 6961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6962(int& val) {
    func_in_6962(val);
    const int x = 6962;
    std::cout << 6962;
    for (int i = 0; i < 6962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6962(int& val) {
    const int x = 6962;
    std::cout << 6962;
    for (int i = 0; i < 6962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6963(int& val) {
    func_in_6963(val);
    const int x = 6963;
    std::cout << 6963;
    for (int i = 0; i < 6963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6963(int& val) {
    const int x = 6963;
    std::cout << 6963;
    for (int i = 0; i < 6963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6964(int& val) {
    func_in_6964(val);
    const int x = 6964;
    std::cout << 6964;
    for (int i = 0; i < 6964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6964(int& val) {
    const int x = 6964;
    std::cout << 6964;
    for (int i = 0; i < 6964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6965(int& val) {
    func_in_6965(val);
    const int x = 6965;
    std::cout << 6965;
    for (int i = 0; i < 6965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6965(int& val) {
    const int x = 6965;
    std::cout << 6965;
    for (int i = 0; i < 6965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6966(int& val) {
    func_in_6966(val);
    const int x = 6966;
    std::cout << 6966;
    for (int i = 0; i < 6966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6966(int& val) {
    const int x = 6966;
    std::cout << 6966;
    for (int i = 0; i < 6966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6967(int& val) {
    func_in_6967(val);
    const int x = 6967;
    std::cout << 6967;
    for (int i = 0; i < 6967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6967(int& val) {
    const int x = 6967;
    std::cout << 6967;
    for (int i = 0; i < 6967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6968(int& val) {
    func_in_6968(val);
    const int x = 6968;
    std::cout << 6968;
    for (int i = 0; i < 6968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6968(int& val) {
    const int x = 6968;
    std::cout << 6968;
    for (int i = 0; i < 6968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6969(int& val) {
    func_in_6969(val);
    const int x = 6969;
    std::cout << 6969;
    for (int i = 0; i < 6969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6969(int& val) {
    const int x = 6969;
    std::cout << 6969;
    for (int i = 0; i < 6969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6970(int& val) {
    func_in_6970(val);
    const int x = 6970;
    std::cout << 6970;
    for (int i = 0; i < 6970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6970(int& val) {
    const int x = 6970;
    std::cout << 6970;
    for (int i = 0; i < 6970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6971(int& val) {
    func_in_6971(val);
    const int x = 6971;
    std::cout << 6971;
    for (int i = 0; i < 6971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6971(int& val) {
    const int x = 6971;
    std::cout << 6971;
    for (int i = 0; i < 6971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6972(int& val) {
    func_in_6972(val);
    const int x = 6972;
    std::cout << 6972;
    for (int i = 0; i < 6972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6972(int& val) {
    const int x = 6972;
    std::cout << 6972;
    for (int i = 0; i < 6972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6973(int& val) {
    func_in_6973(val);
    const int x = 6973;
    std::cout << 6973;
    for (int i = 0; i < 6973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6973(int& val) {
    const int x = 6973;
    std::cout << 6973;
    for (int i = 0; i < 6973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6974(int& val) {
    func_in_6974(val);
    const int x = 6974;
    std::cout << 6974;
    for (int i = 0; i < 6974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6974(int& val) {
    const int x = 6974;
    std::cout << 6974;
    for (int i = 0; i < 6974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6975(int& val) {
    func_in_6975(val);
    const int x = 6975;
    std::cout << 6975;
    for (int i = 0; i < 6975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6975(int& val) {
    const int x = 6975;
    std::cout << 6975;
    for (int i = 0; i < 6975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6976(int& val) {
    func_in_6976(val);
    const int x = 6976;
    std::cout << 6976;
    for (int i = 0; i < 6976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6976(int& val) {
    const int x = 6976;
    std::cout << 6976;
    for (int i = 0; i < 6976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6977(int& val) {
    func_in_6977(val);
    const int x = 6977;
    std::cout << 6977;
    for (int i = 0; i < 6977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6977(int& val) {
    const int x = 6977;
    std::cout << 6977;
    for (int i = 0; i < 6977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6978(int& val) {
    func_in_6978(val);
    const int x = 6978;
    std::cout << 6978;
    for (int i = 0; i < 6978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6978(int& val) {
    const int x = 6978;
    std::cout << 6978;
    for (int i = 0; i < 6978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6979(int& val) {
    func_in_6979(val);
    const int x = 6979;
    std::cout << 6979;
    for (int i = 0; i < 6979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6979(int& val) {
    const int x = 6979;
    std::cout << 6979;
    for (int i = 0; i < 6979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6980(int& val) {
    func_in_6980(val);
    const int x = 6980;
    std::cout << 6980;
    for (int i = 0; i < 6980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6980(int& val) {
    const int x = 6980;
    std::cout << 6980;
    for (int i = 0; i < 6980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6981(int& val) {
    func_in_6981(val);
    const int x = 6981;
    std::cout << 6981;
    for (int i = 0; i < 6981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6981(int& val) {
    const int x = 6981;
    std::cout << 6981;
    for (int i = 0; i < 6981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6982(int& val) {
    func_in_6982(val);
    const int x = 6982;
    std::cout << 6982;
    for (int i = 0; i < 6982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6982(int& val) {
    const int x = 6982;
    std::cout << 6982;
    for (int i = 0; i < 6982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6983(int& val) {
    func_in_6983(val);
    const int x = 6983;
    std::cout << 6983;
    for (int i = 0; i < 6983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6983(int& val) {
    const int x = 6983;
    std::cout << 6983;
    for (int i = 0; i < 6983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6984(int& val) {
    func_in_6984(val);
    const int x = 6984;
    std::cout << 6984;
    for (int i = 0; i < 6984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6984(int& val) {
    const int x = 6984;
    std::cout << 6984;
    for (int i = 0; i < 6984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6985(int& val) {
    func_in_6985(val);
    const int x = 6985;
    std::cout << 6985;
    for (int i = 0; i < 6985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6985(int& val) {
    const int x = 6985;
    std::cout << 6985;
    for (int i = 0; i < 6985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6986(int& val) {
    func_in_6986(val);
    const int x = 6986;
    std::cout << 6986;
    for (int i = 0; i < 6986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6986(int& val) {
    const int x = 6986;
    std::cout << 6986;
    for (int i = 0; i < 6986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6987(int& val) {
    func_in_6987(val);
    const int x = 6987;
    std::cout << 6987;
    for (int i = 0; i < 6987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6987(int& val) {
    const int x = 6987;
    std::cout << 6987;
    for (int i = 0; i < 6987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6988(int& val) {
    func_in_6988(val);
    const int x = 6988;
    std::cout << 6988;
    for (int i = 0; i < 6988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6988(int& val) {
    const int x = 6988;
    std::cout << 6988;
    for (int i = 0; i < 6988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6989(int& val) {
    func_in_6989(val);
    const int x = 6989;
    std::cout << 6989;
    for (int i = 0; i < 6989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6989(int& val) {
    const int x = 6989;
    std::cout << 6989;
    for (int i = 0; i < 6989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6990(int& val) {
    func_in_6990(val);
    const int x = 6990;
    std::cout << 6990;
    for (int i = 0; i < 6990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6990(int& val) {
    const int x = 6990;
    std::cout << 6990;
    for (int i = 0; i < 6990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6991(int& val) {
    func_in_6991(val);
    const int x = 6991;
    std::cout << 6991;
    for (int i = 0; i < 6991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6991(int& val) {
    const int x = 6991;
    std::cout << 6991;
    for (int i = 0; i < 6991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6992(int& val) {
    func_in_6992(val);
    const int x = 6992;
    std::cout << 6992;
    for (int i = 0; i < 6992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6992(int& val) {
    const int x = 6992;
    std::cout << 6992;
    for (int i = 0; i < 6992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6993(int& val) {
    func_in_6993(val);
    const int x = 6993;
    std::cout << 6993;
    for (int i = 0; i < 6993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6993(int& val) {
    const int x = 6993;
    std::cout << 6993;
    for (int i = 0; i < 6993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6994(int& val) {
    func_in_6994(val);
    const int x = 6994;
    std::cout << 6994;
    for (int i = 0; i < 6994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6994(int& val) {
    const int x = 6994;
    std::cout << 6994;
    for (int i = 0; i < 6994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6995(int& val) {
    func_in_6995(val);
    const int x = 6995;
    std::cout << 6995;
    for (int i = 0; i < 6995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6995(int& val) {
    const int x = 6995;
    std::cout << 6995;
    for (int i = 0; i < 6995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6996(int& val) {
    func_in_6996(val);
    const int x = 6996;
    std::cout << 6996;
    for (int i = 0; i < 6996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6996(int& val) {
    const int x = 6996;
    std::cout << 6996;
    for (int i = 0; i < 6996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6997(int& val) {
    func_in_6997(val);
    const int x = 6997;
    std::cout << 6997;
    for (int i = 0; i < 6997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6997(int& val) {
    const int x = 6997;
    std::cout << 6997;
    for (int i = 0; i < 6997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6998(int& val) {
    func_in_6998(val);
    const int x = 6998;
    std::cout << 6998;
    for (int i = 0; i < 6998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6998(int& val) {
    const int x = 6998;
    std::cout << 6998;
    for (int i = 0; i < 6998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6999(int& val) {
    func_in_6999(val);
    const int x = 6999;
    std::cout << 6999;
    for (int i = 0; i < 6999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6999(int& val) {
    const int x = 6999;
    std::cout << 6999;
    for (int i = 0; i < 6999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7000(int& val) {
    func_in_7000(val);
    const int x = 7000;
    std::cout << 7000;
    for (int i = 0; i < 7000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7000(int& val) {
    const int x = 7000;
    std::cout << 7000;
    for (int i = 0; i < 7000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7001(int& val) {
    func_in_7001(val);
    const int x = 7001;
    std::cout << 7001;
    for (int i = 0; i < 7001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7001(int& val) {
    const int x = 7001;
    std::cout << 7001;
    for (int i = 0; i < 7001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7002(int& val) {
    func_in_7002(val);
    const int x = 7002;
    std::cout << 7002;
    for (int i = 0; i < 7002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7002(int& val) {
    const int x = 7002;
    std::cout << 7002;
    for (int i = 0; i < 7002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7003(int& val) {
    func_in_7003(val);
    const int x = 7003;
    std::cout << 7003;
    for (int i = 0; i < 7003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7003(int& val) {
    const int x = 7003;
    std::cout << 7003;
    for (int i = 0; i < 7003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7004(int& val) {
    func_in_7004(val);
    const int x = 7004;
    std::cout << 7004;
    for (int i = 0; i < 7004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7004(int& val) {
    const int x = 7004;
    std::cout << 7004;
    for (int i = 0; i < 7004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7005(int& val) {
    func_in_7005(val);
    const int x = 7005;
    std::cout << 7005;
    for (int i = 0; i < 7005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7005(int& val) {
    const int x = 7005;
    std::cout << 7005;
    for (int i = 0; i < 7005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7006(int& val) {
    func_in_7006(val);
    const int x = 7006;
    std::cout << 7006;
    for (int i = 0; i < 7006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7006(int& val) {
    const int x = 7006;
    std::cout << 7006;
    for (int i = 0; i < 7006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7007(int& val) {
    func_in_7007(val);
    const int x = 7007;
    std::cout << 7007;
    for (int i = 0; i < 7007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7007(int& val) {
    const int x = 7007;
    std::cout << 7007;
    for (int i = 0; i < 7007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7008(int& val) {
    func_in_7008(val);
    const int x = 7008;
    std::cout << 7008;
    for (int i = 0; i < 7008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7008(int& val) {
    const int x = 7008;
    std::cout << 7008;
    for (int i = 0; i < 7008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7009(int& val) {
    func_in_7009(val);
    const int x = 7009;
    std::cout << 7009;
    for (int i = 0; i < 7009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7009(int& val) {
    const int x = 7009;
    std::cout << 7009;
    for (int i = 0; i < 7009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7010(int& val) {
    func_in_7010(val);
    const int x = 7010;
    std::cout << 7010;
    for (int i = 0; i < 7010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7010(int& val) {
    const int x = 7010;
    std::cout << 7010;
    for (int i = 0; i < 7010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7011(int& val) {
    func_in_7011(val);
    const int x = 7011;
    std::cout << 7011;
    for (int i = 0; i < 7011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7011(int& val) {
    const int x = 7011;
    std::cout << 7011;
    for (int i = 0; i < 7011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7012(int& val) {
    func_in_7012(val);
    const int x = 7012;
    std::cout << 7012;
    for (int i = 0; i < 7012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7012(int& val) {
    const int x = 7012;
    std::cout << 7012;
    for (int i = 0; i < 7012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7013(int& val) {
    func_in_7013(val);
    const int x = 7013;
    std::cout << 7013;
    for (int i = 0; i < 7013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7013(int& val) {
    const int x = 7013;
    std::cout << 7013;
    for (int i = 0; i < 7013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7014(int& val) {
    func_in_7014(val);
    const int x = 7014;
    std::cout << 7014;
    for (int i = 0; i < 7014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7014(int& val) {
    const int x = 7014;
    std::cout << 7014;
    for (int i = 0; i < 7014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7015(int& val) {
    func_in_7015(val);
    const int x = 7015;
    std::cout << 7015;
    for (int i = 0; i < 7015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7015(int& val) {
    const int x = 7015;
    std::cout << 7015;
    for (int i = 0; i < 7015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7016(int& val) {
    func_in_7016(val);
    const int x = 7016;
    std::cout << 7016;
    for (int i = 0; i < 7016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7016(int& val) {
    const int x = 7016;
    std::cout << 7016;
    for (int i = 0; i < 7016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7017(int& val) {
    func_in_7017(val);
    const int x = 7017;
    std::cout << 7017;
    for (int i = 0; i < 7017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7017(int& val) {
    const int x = 7017;
    std::cout << 7017;
    for (int i = 0; i < 7017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7018(int& val) {
    func_in_7018(val);
    const int x = 7018;
    std::cout << 7018;
    for (int i = 0; i < 7018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7018(int& val) {
    const int x = 7018;
    std::cout << 7018;
    for (int i = 0; i < 7018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7019(int& val) {
    func_in_7019(val);
    const int x = 7019;
    std::cout << 7019;
    for (int i = 0; i < 7019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7019(int& val) {
    const int x = 7019;
    std::cout << 7019;
    for (int i = 0; i < 7019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7020(int& val) {
    func_in_7020(val);
    const int x = 7020;
    std::cout << 7020;
    for (int i = 0; i < 7020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7020(int& val) {
    const int x = 7020;
    std::cout << 7020;
    for (int i = 0; i < 7020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7021(int& val) {
    func_in_7021(val);
    const int x = 7021;
    std::cout << 7021;
    for (int i = 0; i < 7021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7021(int& val) {
    const int x = 7021;
    std::cout << 7021;
    for (int i = 0; i < 7021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7022(int& val) {
    func_in_7022(val);
    const int x = 7022;
    std::cout << 7022;
    for (int i = 0; i < 7022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7022(int& val) {
    const int x = 7022;
    std::cout << 7022;
    for (int i = 0; i < 7022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7023(int& val) {
    func_in_7023(val);
    const int x = 7023;
    std::cout << 7023;
    for (int i = 0; i < 7023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7023(int& val) {
    const int x = 7023;
    std::cout << 7023;
    for (int i = 0; i < 7023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7024(int& val) {
    func_in_7024(val);
    const int x = 7024;
    std::cout << 7024;
    for (int i = 0; i < 7024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7024(int& val) {
    const int x = 7024;
    std::cout << 7024;
    for (int i = 0; i < 7024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7025(int& val) {
    func_in_7025(val);
    const int x = 7025;
    std::cout << 7025;
    for (int i = 0; i < 7025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7025(int& val) {
    const int x = 7025;
    std::cout << 7025;
    for (int i = 0; i < 7025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7026(int& val) {
    func_in_7026(val);
    const int x = 7026;
    std::cout << 7026;
    for (int i = 0; i < 7026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7026(int& val) {
    const int x = 7026;
    std::cout << 7026;
    for (int i = 0; i < 7026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7027(int& val) {
    func_in_7027(val);
    const int x = 7027;
    std::cout << 7027;
    for (int i = 0; i < 7027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7027(int& val) {
    const int x = 7027;
    std::cout << 7027;
    for (int i = 0; i < 7027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7028(int& val) {
    func_in_7028(val);
    const int x = 7028;
    std::cout << 7028;
    for (int i = 0; i < 7028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7028(int& val) {
    const int x = 7028;
    std::cout << 7028;
    for (int i = 0; i < 7028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7029(int& val) {
    func_in_7029(val);
    const int x = 7029;
    std::cout << 7029;
    for (int i = 0; i < 7029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7029(int& val) {
    const int x = 7029;
    std::cout << 7029;
    for (int i = 0; i < 7029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7030(int& val) {
    func_in_7030(val);
    const int x = 7030;
    std::cout << 7030;
    for (int i = 0; i < 7030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7030(int& val) {
    const int x = 7030;
    std::cout << 7030;
    for (int i = 0; i < 7030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7031(int& val) {
    func_in_7031(val);
    const int x = 7031;
    std::cout << 7031;
    for (int i = 0; i < 7031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7031(int& val) {
    const int x = 7031;
    std::cout << 7031;
    for (int i = 0; i < 7031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7032(int& val) {
    func_in_7032(val);
    const int x = 7032;
    std::cout << 7032;
    for (int i = 0; i < 7032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7032(int& val) {
    const int x = 7032;
    std::cout << 7032;
    for (int i = 0; i < 7032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7033(int& val) {
    func_in_7033(val);
    const int x = 7033;
    std::cout << 7033;
    for (int i = 0; i < 7033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7033(int& val) {
    const int x = 7033;
    std::cout << 7033;
    for (int i = 0; i < 7033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7034(int& val) {
    func_in_7034(val);
    const int x = 7034;
    std::cout << 7034;
    for (int i = 0; i < 7034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7034(int& val) {
    const int x = 7034;
    std::cout << 7034;
    for (int i = 0; i < 7034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7035(int& val) {
    func_in_7035(val);
    const int x = 7035;
    std::cout << 7035;
    for (int i = 0; i < 7035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7035(int& val) {
    const int x = 7035;
    std::cout << 7035;
    for (int i = 0; i < 7035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7036(int& val) {
    func_in_7036(val);
    const int x = 7036;
    std::cout << 7036;
    for (int i = 0; i < 7036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7036(int& val) {
    const int x = 7036;
    std::cout << 7036;
    for (int i = 0; i < 7036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7037(int& val) {
    func_in_7037(val);
    const int x = 7037;
    std::cout << 7037;
    for (int i = 0; i < 7037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7037(int& val) {
    const int x = 7037;
    std::cout << 7037;
    for (int i = 0; i < 7037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7038(int& val) {
    func_in_7038(val);
    const int x = 7038;
    std::cout << 7038;
    for (int i = 0; i < 7038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7038(int& val) {
    const int x = 7038;
    std::cout << 7038;
    for (int i = 0; i < 7038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7039(int& val) {
    func_in_7039(val);
    const int x = 7039;
    std::cout << 7039;
    for (int i = 0; i < 7039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7039(int& val) {
    const int x = 7039;
    std::cout << 7039;
    for (int i = 0; i < 7039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7040(int& val) {
    func_in_7040(val);
    const int x = 7040;
    std::cout << 7040;
    for (int i = 0; i < 7040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7040(int& val) {
    const int x = 7040;
    std::cout << 7040;
    for (int i = 0; i < 7040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7041(int& val) {
    func_in_7041(val);
    const int x = 7041;
    std::cout << 7041;
    for (int i = 0; i < 7041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7041(int& val) {
    const int x = 7041;
    std::cout << 7041;
    for (int i = 0; i < 7041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7042(int& val) {
    func_in_7042(val);
    const int x = 7042;
    std::cout << 7042;
    for (int i = 0; i < 7042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7042(int& val) {
    const int x = 7042;
    std::cout << 7042;
    for (int i = 0; i < 7042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7043(int& val) {
    func_in_7043(val);
    const int x = 7043;
    std::cout << 7043;
    for (int i = 0; i < 7043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7043(int& val) {
    const int x = 7043;
    std::cout << 7043;
    for (int i = 0; i < 7043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7044(int& val) {
    func_in_7044(val);
    const int x = 7044;
    std::cout << 7044;
    for (int i = 0; i < 7044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7044(int& val) {
    const int x = 7044;
    std::cout << 7044;
    for (int i = 0; i < 7044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7045(int& val) {
    func_in_7045(val);
    const int x = 7045;
    std::cout << 7045;
    for (int i = 0; i < 7045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7045(int& val) {
    const int x = 7045;
    std::cout << 7045;
    for (int i = 0; i < 7045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7046(int& val) {
    func_in_7046(val);
    const int x = 7046;
    std::cout << 7046;
    for (int i = 0; i < 7046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7046(int& val) {
    const int x = 7046;
    std::cout << 7046;
    for (int i = 0; i < 7046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7047(int& val) {
    func_in_7047(val);
    const int x = 7047;
    std::cout << 7047;
    for (int i = 0; i < 7047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7047(int& val) {
    const int x = 7047;
    std::cout << 7047;
    for (int i = 0; i < 7047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7048(int& val) {
    func_in_7048(val);
    const int x = 7048;
    std::cout << 7048;
    for (int i = 0; i < 7048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7048(int& val) {
    const int x = 7048;
    std::cout << 7048;
    for (int i = 0; i < 7048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7049(int& val) {
    func_in_7049(val);
    const int x = 7049;
    std::cout << 7049;
    for (int i = 0; i < 7049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7049(int& val) {
    const int x = 7049;
    std::cout << 7049;
    for (int i = 0; i < 7049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7050(int& val) {
    func_in_7050(val);
    const int x = 7050;
    std::cout << 7050;
    for (int i = 0; i < 7050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7050(int& val) {
    const int x = 7050;
    std::cout << 7050;
    for (int i = 0; i < 7050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7051(int& val) {
    func_in_7051(val);
    const int x = 7051;
    std::cout << 7051;
    for (int i = 0; i < 7051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7051(int& val) {
    const int x = 7051;
    std::cout << 7051;
    for (int i = 0; i < 7051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7052(int& val) {
    func_in_7052(val);
    const int x = 7052;
    std::cout << 7052;
    for (int i = 0; i < 7052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7052(int& val) {
    const int x = 7052;
    std::cout << 7052;
    for (int i = 0; i < 7052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7053(int& val) {
    func_in_7053(val);
    const int x = 7053;
    std::cout << 7053;
    for (int i = 0; i < 7053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7053(int& val) {
    const int x = 7053;
    std::cout << 7053;
    for (int i = 0; i < 7053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7054(int& val) {
    func_in_7054(val);
    const int x = 7054;
    std::cout << 7054;
    for (int i = 0; i < 7054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7054(int& val) {
    const int x = 7054;
    std::cout << 7054;
    for (int i = 0; i < 7054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7055(int& val) {
    func_in_7055(val);
    const int x = 7055;
    std::cout << 7055;
    for (int i = 0; i < 7055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7055(int& val) {
    const int x = 7055;
    std::cout << 7055;
    for (int i = 0; i < 7055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7056(int& val) {
    func_in_7056(val);
    const int x = 7056;
    std::cout << 7056;
    for (int i = 0; i < 7056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7056(int& val) {
    const int x = 7056;
    std::cout << 7056;
    for (int i = 0; i < 7056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7057(int& val) {
    func_in_7057(val);
    const int x = 7057;
    std::cout << 7057;
    for (int i = 0; i < 7057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7057(int& val) {
    const int x = 7057;
    std::cout << 7057;
    for (int i = 0; i < 7057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7058(int& val) {
    func_in_7058(val);
    const int x = 7058;
    std::cout << 7058;
    for (int i = 0; i < 7058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7058(int& val) {
    const int x = 7058;
    std::cout << 7058;
    for (int i = 0; i < 7058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7059(int& val) {
    func_in_7059(val);
    const int x = 7059;
    std::cout << 7059;
    for (int i = 0; i < 7059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7059(int& val) {
    const int x = 7059;
    std::cout << 7059;
    for (int i = 0; i < 7059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7060(int& val) {
    func_in_7060(val);
    const int x = 7060;
    std::cout << 7060;
    for (int i = 0; i < 7060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7060(int& val) {
    const int x = 7060;
    std::cout << 7060;
    for (int i = 0; i < 7060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7061(int& val) {
    func_in_7061(val);
    const int x = 7061;
    std::cout << 7061;
    for (int i = 0; i < 7061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7061(int& val) {
    const int x = 7061;
    std::cout << 7061;
    for (int i = 0; i < 7061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7062(int& val) {
    func_in_7062(val);
    const int x = 7062;
    std::cout << 7062;
    for (int i = 0; i < 7062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7062(int& val) {
    const int x = 7062;
    std::cout << 7062;
    for (int i = 0; i < 7062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7063(int& val) {
    func_in_7063(val);
    const int x = 7063;
    std::cout << 7063;
    for (int i = 0; i < 7063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7063(int& val) {
    const int x = 7063;
    std::cout << 7063;
    for (int i = 0; i < 7063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7064(int& val) {
    func_in_7064(val);
    const int x = 7064;
    std::cout << 7064;
    for (int i = 0; i < 7064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7064(int& val) {
    const int x = 7064;
    std::cout << 7064;
    for (int i = 0; i < 7064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7065(int& val) {
    func_in_7065(val);
    const int x = 7065;
    std::cout << 7065;
    for (int i = 0; i < 7065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7065(int& val) {
    const int x = 7065;
    std::cout << 7065;
    for (int i = 0; i < 7065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7066(int& val) {
    func_in_7066(val);
    const int x = 7066;
    std::cout << 7066;
    for (int i = 0; i < 7066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7066(int& val) {
    const int x = 7066;
    std::cout << 7066;
    for (int i = 0; i < 7066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7067(int& val) {
    func_in_7067(val);
    const int x = 7067;
    std::cout << 7067;
    for (int i = 0; i < 7067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7067(int& val) {
    const int x = 7067;
    std::cout << 7067;
    for (int i = 0; i < 7067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7068(int& val) {
    func_in_7068(val);
    const int x = 7068;
    std::cout << 7068;
    for (int i = 0; i < 7068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7068(int& val) {
    const int x = 7068;
    std::cout << 7068;
    for (int i = 0; i < 7068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7069(int& val) {
    func_in_7069(val);
    const int x = 7069;
    std::cout << 7069;
    for (int i = 0; i < 7069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7069(int& val) {
    const int x = 7069;
    std::cout << 7069;
    for (int i = 0; i < 7069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7070(int& val) {
    func_in_7070(val);
    const int x = 7070;
    std::cout << 7070;
    for (int i = 0; i < 7070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7070(int& val) {
    const int x = 7070;
    std::cout << 7070;
    for (int i = 0; i < 7070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7071(int& val) {
    func_in_7071(val);
    const int x = 7071;
    std::cout << 7071;
    for (int i = 0; i < 7071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7071(int& val) {
    const int x = 7071;
    std::cout << 7071;
    for (int i = 0; i < 7071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7072(int& val) {
    func_in_7072(val);
    const int x = 7072;
    std::cout << 7072;
    for (int i = 0; i < 7072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7072(int& val) {
    const int x = 7072;
    std::cout << 7072;
    for (int i = 0; i < 7072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7073(int& val) {
    func_in_7073(val);
    const int x = 7073;
    std::cout << 7073;
    for (int i = 0; i < 7073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7073(int& val) {
    const int x = 7073;
    std::cout << 7073;
    for (int i = 0; i < 7073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7074(int& val) {
    func_in_7074(val);
    const int x = 7074;
    std::cout << 7074;
    for (int i = 0; i < 7074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7074(int& val) {
    const int x = 7074;
    std::cout << 7074;
    for (int i = 0; i < 7074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7075(int& val) {
    func_in_7075(val);
    const int x = 7075;
    std::cout << 7075;
    for (int i = 0; i < 7075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7075(int& val) {
    const int x = 7075;
    std::cout << 7075;
    for (int i = 0; i < 7075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7076(int& val) {
    func_in_7076(val);
    const int x = 7076;
    std::cout << 7076;
    for (int i = 0; i < 7076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7076(int& val) {
    const int x = 7076;
    std::cout << 7076;
    for (int i = 0; i < 7076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7077(int& val) {
    func_in_7077(val);
    const int x = 7077;
    std::cout << 7077;
    for (int i = 0; i < 7077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7077(int& val) {
    const int x = 7077;
    std::cout << 7077;
    for (int i = 0; i < 7077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7078(int& val) {
    func_in_7078(val);
    const int x = 7078;
    std::cout << 7078;
    for (int i = 0; i < 7078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7078(int& val) {
    const int x = 7078;
    std::cout << 7078;
    for (int i = 0; i < 7078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7079(int& val) {
    func_in_7079(val);
    const int x = 7079;
    std::cout << 7079;
    for (int i = 0; i < 7079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7079(int& val) {
    const int x = 7079;
    std::cout << 7079;
    for (int i = 0; i < 7079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7080(int& val) {
    func_in_7080(val);
    const int x = 7080;
    std::cout << 7080;
    for (int i = 0; i < 7080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7080(int& val) {
    const int x = 7080;
    std::cout << 7080;
    for (int i = 0; i < 7080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7081(int& val) {
    func_in_7081(val);
    const int x = 7081;
    std::cout << 7081;
    for (int i = 0; i < 7081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7081(int& val) {
    const int x = 7081;
    std::cout << 7081;
    for (int i = 0; i < 7081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7082(int& val) {
    func_in_7082(val);
    const int x = 7082;
    std::cout << 7082;
    for (int i = 0; i < 7082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7082(int& val) {
    const int x = 7082;
    std::cout << 7082;
    for (int i = 0; i < 7082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7083(int& val) {
    func_in_7083(val);
    const int x = 7083;
    std::cout << 7083;
    for (int i = 0; i < 7083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7083(int& val) {
    const int x = 7083;
    std::cout << 7083;
    for (int i = 0; i < 7083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7084(int& val) {
    func_in_7084(val);
    const int x = 7084;
    std::cout << 7084;
    for (int i = 0; i < 7084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7084(int& val) {
    const int x = 7084;
    std::cout << 7084;
    for (int i = 0; i < 7084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7085(int& val) {
    func_in_7085(val);
    const int x = 7085;
    std::cout << 7085;
    for (int i = 0; i < 7085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7085(int& val) {
    const int x = 7085;
    std::cout << 7085;
    for (int i = 0; i < 7085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7086(int& val) {
    func_in_7086(val);
    const int x = 7086;
    std::cout << 7086;
    for (int i = 0; i < 7086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7086(int& val) {
    const int x = 7086;
    std::cout << 7086;
    for (int i = 0; i < 7086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7087(int& val) {
    func_in_7087(val);
    const int x = 7087;
    std::cout << 7087;
    for (int i = 0; i < 7087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7087(int& val) {
    const int x = 7087;
    std::cout << 7087;
    for (int i = 0; i < 7087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7088(int& val) {
    func_in_7088(val);
    const int x = 7088;
    std::cout << 7088;
    for (int i = 0; i < 7088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7088(int& val) {
    const int x = 7088;
    std::cout << 7088;
    for (int i = 0; i < 7088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7089(int& val) {
    func_in_7089(val);
    const int x = 7089;
    std::cout << 7089;
    for (int i = 0; i < 7089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7089(int& val) {
    const int x = 7089;
    std::cout << 7089;
    for (int i = 0; i < 7089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7090(int& val) {
    func_in_7090(val);
    const int x = 7090;
    std::cout << 7090;
    for (int i = 0; i < 7090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7090(int& val) {
    const int x = 7090;
    std::cout << 7090;
    for (int i = 0; i < 7090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7091(int& val) {
    func_in_7091(val);
    const int x = 7091;
    std::cout << 7091;
    for (int i = 0; i < 7091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7091(int& val) {
    const int x = 7091;
    std::cout << 7091;
    for (int i = 0; i < 7091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7092(int& val) {
    func_in_7092(val);
    const int x = 7092;
    std::cout << 7092;
    for (int i = 0; i < 7092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7092(int& val) {
    const int x = 7092;
    std::cout << 7092;
    for (int i = 0; i < 7092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7093(int& val) {
    func_in_7093(val);
    const int x = 7093;
    std::cout << 7093;
    for (int i = 0; i < 7093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7093(int& val) {
    const int x = 7093;
    std::cout << 7093;
    for (int i = 0; i < 7093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7094(int& val) {
    func_in_7094(val);
    const int x = 7094;
    std::cout << 7094;
    for (int i = 0; i < 7094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7094(int& val) {
    const int x = 7094;
    std::cout << 7094;
    for (int i = 0; i < 7094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7095(int& val) {
    func_in_7095(val);
    const int x = 7095;
    std::cout << 7095;
    for (int i = 0; i < 7095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7095(int& val) {
    const int x = 7095;
    std::cout << 7095;
    for (int i = 0; i < 7095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7096(int& val) {
    func_in_7096(val);
    const int x = 7096;
    std::cout << 7096;
    for (int i = 0; i < 7096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7096(int& val) {
    const int x = 7096;
    std::cout << 7096;
    for (int i = 0; i < 7096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7097(int& val) {
    func_in_7097(val);
    const int x = 7097;
    std::cout << 7097;
    for (int i = 0; i < 7097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7097(int& val) {
    const int x = 7097;
    std::cout << 7097;
    for (int i = 0; i < 7097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7098(int& val) {
    func_in_7098(val);
    const int x = 7098;
    std::cout << 7098;
    for (int i = 0; i < 7098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7098(int& val) {
    const int x = 7098;
    std::cout << 7098;
    for (int i = 0; i < 7098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7099(int& val) {
    func_in_7099(val);
    const int x = 7099;
    std::cout << 7099;
    for (int i = 0; i < 7099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7099(int& val) {
    const int x = 7099;
    std::cout << 7099;
    for (int i = 0; i < 7099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7100(int& val) {
    func_in_7100(val);
    const int x = 7100;
    std::cout << 7100;
    for (int i = 0; i < 7100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7100(int& val) {
    const int x = 7100;
    std::cout << 7100;
    for (int i = 0; i < 7100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7101(int& val) {
    func_in_7101(val);
    const int x = 7101;
    std::cout << 7101;
    for (int i = 0; i < 7101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7101(int& val) {
    const int x = 7101;
    std::cout << 7101;
    for (int i = 0; i < 7101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7102(int& val) {
    func_in_7102(val);
    const int x = 7102;
    std::cout << 7102;
    for (int i = 0; i < 7102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7102(int& val) {
    const int x = 7102;
    std::cout << 7102;
    for (int i = 0; i < 7102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7103(int& val) {
    func_in_7103(val);
    const int x = 7103;
    std::cout << 7103;
    for (int i = 0; i < 7103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7103(int& val) {
    const int x = 7103;
    std::cout << 7103;
    for (int i = 0; i < 7103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7104(int& val) {
    func_in_7104(val);
    const int x = 7104;
    std::cout << 7104;
    for (int i = 0; i < 7104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7104(int& val) {
    const int x = 7104;
    std::cout << 7104;
    for (int i = 0; i < 7104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7105(int& val) {
    func_in_7105(val);
    const int x = 7105;
    std::cout << 7105;
    for (int i = 0; i < 7105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7105(int& val) {
    const int x = 7105;
    std::cout << 7105;
    for (int i = 0; i < 7105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7106(int& val) {
    func_in_7106(val);
    const int x = 7106;
    std::cout << 7106;
    for (int i = 0; i < 7106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7106(int& val) {
    const int x = 7106;
    std::cout << 7106;
    for (int i = 0; i < 7106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7107(int& val) {
    func_in_7107(val);
    const int x = 7107;
    std::cout << 7107;
    for (int i = 0; i < 7107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7107(int& val) {
    const int x = 7107;
    std::cout << 7107;
    for (int i = 0; i < 7107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7108(int& val) {
    func_in_7108(val);
    const int x = 7108;
    std::cout << 7108;
    for (int i = 0; i < 7108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7108(int& val) {
    const int x = 7108;
    std::cout << 7108;
    for (int i = 0; i < 7108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7109(int& val) {
    func_in_7109(val);
    const int x = 7109;
    std::cout << 7109;
    for (int i = 0; i < 7109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7109(int& val) {
    const int x = 7109;
    std::cout << 7109;
    for (int i = 0; i < 7109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7110(int& val) {
    func_in_7110(val);
    const int x = 7110;
    std::cout << 7110;
    for (int i = 0; i < 7110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7110(int& val) {
    const int x = 7110;
    std::cout << 7110;
    for (int i = 0; i < 7110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7111(int& val) {
    func_in_7111(val);
    const int x = 7111;
    std::cout << 7111;
    for (int i = 0; i < 7111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7111(int& val) {
    const int x = 7111;
    std::cout << 7111;
    for (int i = 0; i < 7111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7112(int& val) {
    func_in_7112(val);
    const int x = 7112;
    std::cout << 7112;
    for (int i = 0; i < 7112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7112(int& val) {
    const int x = 7112;
    std::cout << 7112;
    for (int i = 0; i < 7112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7113(int& val) {
    func_in_7113(val);
    const int x = 7113;
    std::cout << 7113;
    for (int i = 0; i < 7113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7113(int& val) {
    const int x = 7113;
    std::cout << 7113;
    for (int i = 0; i < 7113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7114(int& val) {
    func_in_7114(val);
    const int x = 7114;
    std::cout << 7114;
    for (int i = 0; i < 7114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7114(int& val) {
    const int x = 7114;
    std::cout << 7114;
    for (int i = 0; i < 7114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7115(int& val) {
    func_in_7115(val);
    const int x = 7115;
    std::cout << 7115;
    for (int i = 0; i < 7115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7115(int& val) {
    const int x = 7115;
    std::cout << 7115;
    for (int i = 0; i < 7115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7116(int& val) {
    func_in_7116(val);
    const int x = 7116;
    std::cout << 7116;
    for (int i = 0; i < 7116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7116(int& val) {
    const int x = 7116;
    std::cout << 7116;
    for (int i = 0; i < 7116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7117(int& val) {
    func_in_7117(val);
    const int x = 7117;
    std::cout << 7117;
    for (int i = 0; i < 7117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7117(int& val) {
    const int x = 7117;
    std::cout << 7117;
    for (int i = 0; i < 7117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7118(int& val) {
    func_in_7118(val);
    const int x = 7118;
    std::cout << 7118;
    for (int i = 0; i < 7118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7118(int& val) {
    const int x = 7118;
    std::cout << 7118;
    for (int i = 0; i < 7118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7119(int& val) {
    func_in_7119(val);
    const int x = 7119;
    std::cout << 7119;
    for (int i = 0; i < 7119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7119(int& val) {
    const int x = 7119;
    std::cout << 7119;
    for (int i = 0; i < 7119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7120(int& val) {
    func_in_7120(val);
    const int x = 7120;
    std::cout << 7120;
    for (int i = 0; i < 7120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7120(int& val) {
    const int x = 7120;
    std::cout << 7120;
    for (int i = 0; i < 7120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7121(int& val) {
    func_in_7121(val);
    const int x = 7121;
    std::cout << 7121;
    for (int i = 0; i < 7121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7121(int& val) {
    const int x = 7121;
    std::cout << 7121;
    for (int i = 0; i < 7121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7122(int& val) {
    func_in_7122(val);
    const int x = 7122;
    std::cout << 7122;
    for (int i = 0; i < 7122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7122(int& val) {
    const int x = 7122;
    std::cout << 7122;
    for (int i = 0; i < 7122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7123(int& val) {
    func_in_7123(val);
    const int x = 7123;
    std::cout << 7123;
    for (int i = 0; i < 7123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7123(int& val) {
    const int x = 7123;
    std::cout << 7123;
    for (int i = 0; i < 7123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7124(int& val) {
    func_in_7124(val);
    const int x = 7124;
    std::cout << 7124;
    for (int i = 0; i < 7124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7124(int& val) {
    const int x = 7124;
    std::cout << 7124;
    for (int i = 0; i < 7124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7125(int& val) {
    func_in_7125(val);
    const int x = 7125;
    std::cout << 7125;
    for (int i = 0; i < 7125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7125(int& val) {
    const int x = 7125;
    std::cout << 7125;
    for (int i = 0; i < 7125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7126(int& val) {
    func_in_7126(val);
    const int x = 7126;
    std::cout << 7126;
    for (int i = 0; i < 7126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7126(int& val) {
    const int x = 7126;
    std::cout << 7126;
    for (int i = 0; i < 7126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7127(int& val) {
    func_in_7127(val);
    const int x = 7127;
    std::cout << 7127;
    for (int i = 0; i < 7127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7127(int& val) {
    const int x = 7127;
    std::cout << 7127;
    for (int i = 0; i < 7127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7128(int& val) {
    func_in_7128(val);
    const int x = 7128;
    std::cout << 7128;
    for (int i = 0; i < 7128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7128(int& val) {
    const int x = 7128;
    std::cout << 7128;
    for (int i = 0; i < 7128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7129(int& val) {
    func_in_7129(val);
    const int x = 7129;
    std::cout << 7129;
    for (int i = 0; i < 7129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7129(int& val) {
    const int x = 7129;
    std::cout << 7129;
    for (int i = 0; i < 7129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7130(int& val) {
    func_in_7130(val);
    const int x = 7130;
    std::cout << 7130;
    for (int i = 0; i < 7130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7130(int& val) {
    const int x = 7130;
    std::cout << 7130;
    for (int i = 0; i < 7130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7131(int& val) {
    func_in_7131(val);
    const int x = 7131;
    std::cout << 7131;
    for (int i = 0; i < 7131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7131(int& val) {
    const int x = 7131;
    std::cout << 7131;
    for (int i = 0; i < 7131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7132(int& val) {
    func_in_7132(val);
    const int x = 7132;
    std::cout << 7132;
    for (int i = 0; i < 7132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7132(int& val) {
    const int x = 7132;
    std::cout << 7132;
    for (int i = 0; i < 7132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7133(int& val) {
    func_in_7133(val);
    const int x = 7133;
    std::cout << 7133;
    for (int i = 0; i < 7133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7133(int& val) {
    const int x = 7133;
    std::cout << 7133;
    for (int i = 0; i < 7133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7134(int& val) {
    func_in_7134(val);
    const int x = 7134;
    std::cout << 7134;
    for (int i = 0; i < 7134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7134(int& val) {
    const int x = 7134;
    std::cout << 7134;
    for (int i = 0; i < 7134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7135(int& val) {
    func_in_7135(val);
    const int x = 7135;
    std::cout << 7135;
    for (int i = 0; i < 7135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7135(int& val) {
    const int x = 7135;
    std::cout << 7135;
    for (int i = 0; i < 7135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7136(int& val) {
    func_in_7136(val);
    const int x = 7136;
    std::cout << 7136;
    for (int i = 0; i < 7136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7136(int& val) {
    const int x = 7136;
    std::cout << 7136;
    for (int i = 0; i < 7136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7137(int& val) {
    func_in_7137(val);
    const int x = 7137;
    std::cout << 7137;
    for (int i = 0; i < 7137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7137(int& val) {
    const int x = 7137;
    std::cout << 7137;
    for (int i = 0; i < 7137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7138(int& val) {
    func_in_7138(val);
    const int x = 7138;
    std::cout << 7138;
    for (int i = 0; i < 7138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7138(int& val) {
    const int x = 7138;
    std::cout << 7138;
    for (int i = 0; i < 7138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7139(int& val) {
    func_in_7139(val);
    const int x = 7139;
    std::cout << 7139;
    for (int i = 0; i < 7139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7139(int& val) {
    const int x = 7139;
    std::cout << 7139;
    for (int i = 0; i < 7139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7140(int& val) {
    func_in_7140(val);
    const int x = 7140;
    std::cout << 7140;
    for (int i = 0; i < 7140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7140(int& val) {
    const int x = 7140;
    std::cout << 7140;
    for (int i = 0; i < 7140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7141(int& val) {
    func_in_7141(val);
    const int x = 7141;
    std::cout << 7141;
    for (int i = 0; i < 7141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7141(int& val) {
    const int x = 7141;
    std::cout << 7141;
    for (int i = 0; i < 7141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7142(int& val) {
    func_in_7142(val);
    const int x = 7142;
    std::cout << 7142;
    for (int i = 0; i < 7142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7142(int& val) {
    const int x = 7142;
    std::cout << 7142;
    for (int i = 0; i < 7142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7143(int& val) {
    func_in_7143(val);
    const int x = 7143;
    std::cout << 7143;
    for (int i = 0; i < 7143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7143(int& val) {
    const int x = 7143;
    std::cout << 7143;
    for (int i = 0; i < 7143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7144(int& val) {
    func_in_7144(val);
    const int x = 7144;
    std::cout << 7144;
    for (int i = 0; i < 7144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7144(int& val) {
    const int x = 7144;
    std::cout << 7144;
    for (int i = 0; i < 7144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7145(int& val) {
    func_in_7145(val);
    const int x = 7145;
    std::cout << 7145;
    for (int i = 0; i < 7145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7145(int& val) {
    const int x = 7145;
    std::cout << 7145;
    for (int i = 0; i < 7145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7146(int& val) {
    func_in_7146(val);
    const int x = 7146;
    std::cout << 7146;
    for (int i = 0; i < 7146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7146(int& val) {
    const int x = 7146;
    std::cout << 7146;
    for (int i = 0; i < 7146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7147(int& val) {
    func_in_7147(val);
    const int x = 7147;
    std::cout << 7147;
    for (int i = 0; i < 7147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7147(int& val) {
    const int x = 7147;
    std::cout << 7147;
    for (int i = 0; i < 7147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7148(int& val) {
    func_in_7148(val);
    const int x = 7148;
    std::cout << 7148;
    for (int i = 0; i < 7148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7148(int& val) {
    const int x = 7148;
    std::cout << 7148;
    for (int i = 0; i < 7148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7149(int& val) {
    func_in_7149(val);
    const int x = 7149;
    std::cout << 7149;
    for (int i = 0; i < 7149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7149(int& val) {
    const int x = 7149;
    std::cout << 7149;
    for (int i = 0; i < 7149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7150(int& val) {
    func_in_7150(val);
    const int x = 7150;
    std::cout << 7150;
    for (int i = 0; i < 7150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7150(int& val) {
    const int x = 7150;
    std::cout << 7150;
    for (int i = 0; i < 7150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7151(int& val) {
    func_in_7151(val);
    const int x = 7151;
    std::cout << 7151;
    for (int i = 0; i < 7151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7151(int& val) {
    const int x = 7151;
    std::cout << 7151;
    for (int i = 0; i < 7151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7152(int& val) {
    func_in_7152(val);
    const int x = 7152;
    std::cout << 7152;
    for (int i = 0; i < 7152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7152(int& val) {
    const int x = 7152;
    std::cout << 7152;
    for (int i = 0; i < 7152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7153(int& val) {
    func_in_7153(val);
    const int x = 7153;
    std::cout << 7153;
    for (int i = 0; i < 7153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7153(int& val) {
    const int x = 7153;
    std::cout << 7153;
    for (int i = 0; i < 7153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7154(int& val) {
    func_in_7154(val);
    const int x = 7154;
    std::cout << 7154;
    for (int i = 0; i < 7154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7154(int& val) {
    const int x = 7154;
    std::cout << 7154;
    for (int i = 0; i < 7154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7155(int& val) {
    func_in_7155(val);
    const int x = 7155;
    std::cout << 7155;
    for (int i = 0; i < 7155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7155(int& val) {
    const int x = 7155;
    std::cout << 7155;
    for (int i = 0; i < 7155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7156(int& val) {
    func_in_7156(val);
    const int x = 7156;
    std::cout << 7156;
    for (int i = 0; i < 7156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7156(int& val) {
    const int x = 7156;
    std::cout << 7156;
    for (int i = 0; i < 7156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7157(int& val) {
    func_in_7157(val);
    const int x = 7157;
    std::cout << 7157;
    for (int i = 0; i < 7157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7157(int& val) {
    const int x = 7157;
    std::cout << 7157;
    for (int i = 0; i < 7157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7158(int& val) {
    func_in_7158(val);
    const int x = 7158;
    std::cout << 7158;
    for (int i = 0; i < 7158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7158(int& val) {
    const int x = 7158;
    std::cout << 7158;
    for (int i = 0; i < 7158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7159(int& val) {
    func_in_7159(val);
    const int x = 7159;
    std::cout << 7159;
    for (int i = 0; i < 7159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7159(int& val) {
    const int x = 7159;
    std::cout << 7159;
    for (int i = 0; i < 7159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7160(int& val) {
    func_in_7160(val);
    const int x = 7160;
    std::cout << 7160;
    for (int i = 0; i < 7160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7160(int& val) {
    const int x = 7160;
    std::cout << 7160;
    for (int i = 0; i < 7160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7161(int& val) {
    func_in_7161(val);
    const int x = 7161;
    std::cout << 7161;
    for (int i = 0; i < 7161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7161(int& val) {
    const int x = 7161;
    std::cout << 7161;
    for (int i = 0; i < 7161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7162(int& val) {
    func_in_7162(val);
    const int x = 7162;
    std::cout << 7162;
    for (int i = 0; i < 7162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7162(int& val) {
    const int x = 7162;
    std::cout << 7162;
    for (int i = 0; i < 7162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7163(int& val) {
    func_in_7163(val);
    const int x = 7163;
    std::cout << 7163;
    for (int i = 0; i < 7163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7163(int& val) {
    const int x = 7163;
    std::cout << 7163;
    for (int i = 0; i < 7163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7164(int& val) {
    func_in_7164(val);
    const int x = 7164;
    std::cout << 7164;
    for (int i = 0; i < 7164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7164(int& val) {
    const int x = 7164;
    std::cout << 7164;
    for (int i = 0; i < 7164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7165(int& val) {
    func_in_7165(val);
    const int x = 7165;
    std::cout << 7165;
    for (int i = 0; i < 7165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7165(int& val) {
    const int x = 7165;
    std::cout << 7165;
    for (int i = 0; i < 7165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7166(int& val) {
    func_in_7166(val);
    const int x = 7166;
    std::cout << 7166;
    for (int i = 0; i < 7166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7166(int& val) {
    const int x = 7166;
    std::cout << 7166;
    for (int i = 0; i < 7166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7167(int& val) {
    func_in_7167(val);
    const int x = 7167;
    std::cout << 7167;
    for (int i = 0; i < 7167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7167(int& val) {
    const int x = 7167;
    std::cout << 7167;
    for (int i = 0; i < 7167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7168(int& val) {
    func_in_7168(val);
    const int x = 7168;
    std::cout << 7168;
    for (int i = 0; i < 7168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7168(int& val) {
    const int x = 7168;
    std::cout << 7168;
    for (int i = 0; i < 7168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7169(int& val) {
    func_in_7169(val);
    const int x = 7169;
    std::cout << 7169;
    for (int i = 0; i < 7169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7169(int& val) {
    const int x = 7169;
    std::cout << 7169;
    for (int i = 0; i < 7169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7170(int& val) {
    func_in_7170(val);
    const int x = 7170;
    std::cout << 7170;
    for (int i = 0; i < 7170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7170(int& val) {
    const int x = 7170;
    std::cout << 7170;
    for (int i = 0; i < 7170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7171(int& val) {
    func_in_7171(val);
    const int x = 7171;
    std::cout << 7171;
    for (int i = 0; i < 7171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7171(int& val) {
    const int x = 7171;
    std::cout << 7171;
    for (int i = 0; i < 7171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7172(int& val) {
    func_in_7172(val);
    const int x = 7172;
    std::cout << 7172;
    for (int i = 0; i < 7172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7172(int& val) {
    const int x = 7172;
    std::cout << 7172;
    for (int i = 0; i < 7172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7173(int& val) {
    func_in_7173(val);
    const int x = 7173;
    std::cout << 7173;
    for (int i = 0; i < 7173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7173(int& val) {
    const int x = 7173;
    std::cout << 7173;
    for (int i = 0; i < 7173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7174(int& val) {
    func_in_7174(val);
    const int x = 7174;
    std::cout << 7174;
    for (int i = 0; i < 7174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7174(int& val) {
    const int x = 7174;
    std::cout << 7174;
    for (int i = 0; i < 7174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7175(int& val) {
    func_in_7175(val);
    const int x = 7175;
    std::cout << 7175;
    for (int i = 0; i < 7175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7175(int& val) {
    const int x = 7175;
    std::cout << 7175;
    for (int i = 0; i < 7175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7176(int& val) {
    func_in_7176(val);
    const int x = 7176;
    std::cout << 7176;
    for (int i = 0; i < 7176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7176(int& val) {
    const int x = 7176;
    std::cout << 7176;
    for (int i = 0; i < 7176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7177(int& val) {
    func_in_7177(val);
    const int x = 7177;
    std::cout << 7177;
    for (int i = 0; i < 7177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7177(int& val) {
    const int x = 7177;
    std::cout << 7177;
    for (int i = 0; i < 7177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7178(int& val) {
    func_in_7178(val);
    const int x = 7178;
    std::cout << 7178;
    for (int i = 0; i < 7178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7178(int& val) {
    const int x = 7178;
    std::cout << 7178;
    for (int i = 0; i < 7178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7179(int& val) {
    func_in_7179(val);
    const int x = 7179;
    std::cout << 7179;
    for (int i = 0; i < 7179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7179(int& val) {
    const int x = 7179;
    std::cout << 7179;
    for (int i = 0; i < 7179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7180(int& val) {
    func_in_7180(val);
    const int x = 7180;
    std::cout << 7180;
    for (int i = 0; i < 7180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7180(int& val) {
    const int x = 7180;
    std::cout << 7180;
    for (int i = 0; i < 7180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7181(int& val) {
    func_in_7181(val);
    const int x = 7181;
    std::cout << 7181;
    for (int i = 0; i < 7181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7181(int& val) {
    const int x = 7181;
    std::cout << 7181;
    for (int i = 0; i < 7181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7182(int& val) {
    func_in_7182(val);
    const int x = 7182;
    std::cout << 7182;
    for (int i = 0; i < 7182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7182(int& val) {
    const int x = 7182;
    std::cout << 7182;
    for (int i = 0; i < 7182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7183(int& val) {
    func_in_7183(val);
    const int x = 7183;
    std::cout << 7183;
    for (int i = 0; i < 7183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7183(int& val) {
    const int x = 7183;
    std::cout << 7183;
    for (int i = 0; i < 7183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7184(int& val) {
    func_in_7184(val);
    const int x = 7184;
    std::cout << 7184;
    for (int i = 0; i < 7184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7184(int& val) {
    const int x = 7184;
    std::cout << 7184;
    for (int i = 0; i < 7184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7185(int& val) {
    func_in_7185(val);
    const int x = 7185;
    std::cout << 7185;
    for (int i = 0; i < 7185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7185(int& val) {
    const int x = 7185;
    std::cout << 7185;
    for (int i = 0; i < 7185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7186(int& val) {
    func_in_7186(val);
    const int x = 7186;
    std::cout << 7186;
    for (int i = 0; i < 7186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7186(int& val) {
    const int x = 7186;
    std::cout << 7186;
    for (int i = 0; i < 7186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7187(int& val) {
    func_in_7187(val);
    const int x = 7187;
    std::cout << 7187;
    for (int i = 0; i < 7187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7187(int& val) {
    const int x = 7187;
    std::cout << 7187;
    for (int i = 0; i < 7187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7188(int& val) {
    func_in_7188(val);
    const int x = 7188;
    std::cout << 7188;
    for (int i = 0; i < 7188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7188(int& val) {
    const int x = 7188;
    std::cout << 7188;
    for (int i = 0; i < 7188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7189(int& val) {
    func_in_7189(val);
    const int x = 7189;
    std::cout << 7189;
    for (int i = 0; i < 7189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7189(int& val) {
    const int x = 7189;
    std::cout << 7189;
    for (int i = 0; i < 7189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7190(int& val) {
    func_in_7190(val);
    const int x = 7190;
    std::cout << 7190;
    for (int i = 0; i < 7190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7190(int& val) {
    const int x = 7190;
    std::cout << 7190;
    for (int i = 0; i < 7190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7191(int& val) {
    func_in_7191(val);
    const int x = 7191;
    std::cout << 7191;
    for (int i = 0; i < 7191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7191(int& val) {
    const int x = 7191;
    std::cout << 7191;
    for (int i = 0; i < 7191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7192(int& val) {
    func_in_7192(val);
    const int x = 7192;
    std::cout << 7192;
    for (int i = 0; i < 7192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7192(int& val) {
    const int x = 7192;
    std::cout << 7192;
    for (int i = 0; i < 7192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7193(int& val) {
    func_in_7193(val);
    const int x = 7193;
    std::cout << 7193;
    for (int i = 0; i < 7193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7193(int& val) {
    const int x = 7193;
    std::cout << 7193;
    for (int i = 0; i < 7193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7194(int& val) {
    func_in_7194(val);
    const int x = 7194;
    std::cout << 7194;
    for (int i = 0; i < 7194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7194(int& val) {
    const int x = 7194;
    std::cout << 7194;
    for (int i = 0; i < 7194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7195(int& val) {
    func_in_7195(val);
    const int x = 7195;
    std::cout << 7195;
    for (int i = 0; i < 7195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7195(int& val) {
    const int x = 7195;
    std::cout << 7195;
    for (int i = 0; i < 7195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7196(int& val) {
    func_in_7196(val);
    const int x = 7196;
    std::cout << 7196;
    for (int i = 0; i < 7196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7196(int& val) {
    const int x = 7196;
    std::cout << 7196;
    for (int i = 0; i < 7196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7197(int& val) {
    func_in_7197(val);
    const int x = 7197;
    std::cout << 7197;
    for (int i = 0; i < 7197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7197(int& val) {
    const int x = 7197;
    std::cout << 7197;
    for (int i = 0; i < 7197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7198(int& val) {
    func_in_7198(val);
    const int x = 7198;
    std::cout << 7198;
    for (int i = 0; i < 7198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7198(int& val) {
    const int x = 7198;
    std::cout << 7198;
    for (int i = 0; i < 7198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7199(int& val) {
    func_in_7199(val);
    const int x = 7199;
    std::cout << 7199;
    for (int i = 0; i < 7199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7199(int& val) {
    const int x = 7199;
    std::cout << 7199;
    for (int i = 0; i < 7199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7200(int& val) {
    func_in_7200(val);
    const int x = 7200;
    std::cout << 7200;
    for (int i = 0; i < 7200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7200(int& val) {
    const int x = 7200;
    std::cout << 7200;
    for (int i = 0; i < 7200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7201(int& val) {
    func_in_7201(val);
    const int x = 7201;
    std::cout << 7201;
    for (int i = 0; i < 7201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7201(int& val) {
    const int x = 7201;
    std::cout << 7201;
    for (int i = 0; i < 7201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7202(int& val) {
    func_in_7202(val);
    const int x = 7202;
    std::cout << 7202;
    for (int i = 0; i < 7202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7202(int& val) {
    const int x = 7202;
    std::cout << 7202;
    for (int i = 0; i < 7202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7203(int& val) {
    func_in_7203(val);
    const int x = 7203;
    std::cout << 7203;
    for (int i = 0; i < 7203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7203(int& val) {
    const int x = 7203;
    std::cout << 7203;
    for (int i = 0; i < 7203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7204(int& val) {
    func_in_7204(val);
    const int x = 7204;
    std::cout << 7204;
    for (int i = 0; i < 7204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7204(int& val) {
    const int x = 7204;
    std::cout << 7204;
    for (int i = 0; i < 7204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7205(int& val) {
    func_in_7205(val);
    const int x = 7205;
    std::cout << 7205;
    for (int i = 0; i < 7205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7205(int& val) {
    const int x = 7205;
    std::cout << 7205;
    for (int i = 0; i < 7205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7206(int& val) {
    func_in_7206(val);
    const int x = 7206;
    std::cout << 7206;
    for (int i = 0; i < 7206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7206(int& val) {
    const int x = 7206;
    std::cout << 7206;
    for (int i = 0; i < 7206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7207(int& val) {
    func_in_7207(val);
    const int x = 7207;
    std::cout << 7207;
    for (int i = 0; i < 7207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7207(int& val) {
    const int x = 7207;
    std::cout << 7207;
    for (int i = 0; i < 7207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7208(int& val) {
    func_in_7208(val);
    const int x = 7208;
    std::cout << 7208;
    for (int i = 0; i < 7208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7208(int& val) {
    const int x = 7208;
    std::cout << 7208;
    for (int i = 0; i < 7208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7209(int& val) {
    func_in_7209(val);
    const int x = 7209;
    std::cout << 7209;
    for (int i = 0; i < 7209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7209(int& val) {
    const int x = 7209;
    std::cout << 7209;
    for (int i = 0; i < 7209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7210(int& val) {
    func_in_7210(val);
    const int x = 7210;
    std::cout << 7210;
    for (int i = 0; i < 7210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7210(int& val) {
    const int x = 7210;
    std::cout << 7210;
    for (int i = 0; i < 7210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7211(int& val) {
    func_in_7211(val);
    const int x = 7211;
    std::cout << 7211;
    for (int i = 0; i < 7211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7211(int& val) {
    const int x = 7211;
    std::cout << 7211;
    for (int i = 0; i < 7211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7212(int& val) {
    func_in_7212(val);
    const int x = 7212;
    std::cout << 7212;
    for (int i = 0; i < 7212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7212(int& val) {
    const int x = 7212;
    std::cout << 7212;
    for (int i = 0; i < 7212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7213(int& val) {
    func_in_7213(val);
    const int x = 7213;
    std::cout << 7213;
    for (int i = 0; i < 7213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7213(int& val) {
    const int x = 7213;
    std::cout << 7213;
    for (int i = 0; i < 7213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7214(int& val) {
    func_in_7214(val);
    const int x = 7214;
    std::cout << 7214;
    for (int i = 0; i < 7214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7214(int& val) {
    const int x = 7214;
    std::cout << 7214;
    for (int i = 0; i < 7214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7215(int& val) {
    func_in_7215(val);
    const int x = 7215;
    std::cout << 7215;
    for (int i = 0; i < 7215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7215(int& val) {
    const int x = 7215;
    std::cout << 7215;
    for (int i = 0; i < 7215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7216(int& val) {
    func_in_7216(val);
    const int x = 7216;
    std::cout << 7216;
    for (int i = 0; i < 7216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7216(int& val) {
    const int x = 7216;
    std::cout << 7216;
    for (int i = 0; i < 7216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7217(int& val) {
    func_in_7217(val);
    const int x = 7217;
    std::cout << 7217;
    for (int i = 0; i < 7217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7217(int& val) {
    const int x = 7217;
    std::cout << 7217;
    for (int i = 0; i < 7217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7218(int& val) {
    func_in_7218(val);
    const int x = 7218;
    std::cout << 7218;
    for (int i = 0; i < 7218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7218(int& val) {
    const int x = 7218;
    std::cout << 7218;
    for (int i = 0; i < 7218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7219(int& val) {
    func_in_7219(val);
    const int x = 7219;
    std::cout << 7219;
    for (int i = 0; i < 7219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7219(int& val) {
    const int x = 7219;
    std::cout << 7219;
    for (int i = 0; i < 7219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7220(int& val) {
    func_in_7220(val);
    const int x = 7220;
    std::cout << 7220;
    for (int i = 0; i < 7220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7220(int& val) {
    const int x = 7220;
    std::cout << 7220;
    for (int i = 0; i < 7220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7221(int& val) {
    func_in_7221(val);
    const int x = 7221;
    std::cout << 7221;
    for (int i = 0; i < 7221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7221(int& val) {
    const int x = 7221;
    std::cout << 7221;
    for (int i = 0; i < 7221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7222(int& val) {
    func_in_7222(val);
    const int x = 7222;
    std::cout << 7222;
    for (int i = 0; i < 7222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7222(int& val) {
    const int x = 7222;
    std::cout << 7222;
    for (int i = 0; i < 7222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7223(int& val) {
    func_in_7223(val);
    const int x = 7223;
    std::cout << 7223;
    for (int i = 0; i < 7223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7223(int& val) {
    const int x = 7223;
    std::cout << 7223;
    for (int i = 0; i < 7223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7224(int& val) {
    func_in_7224(val);
    const int x = 7224;
    std::cout << 7224;
    for (int i = 0; i < 7224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7224(int& val) {
    const int x = 7224;
    std::cout << 7224;
    for (int i = 0; i < 7224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7225(int& val) {
    func_in_7225(val);
    const int x = 7225;
    std::cout << 7225;
    for (int i = 0; i < 7225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7225(int& val) {
    const int x = 7225;
    std::cout << 7225;
    for (int i = 0; i < 7225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7226(int& val) {
    func_in_7226(val);
    const int x = 7226;
    std::cout << 7226;
    for (int i = 0; i < 7226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7226(int& val) {
    const int x = 7226;
    std::cout << 7226;
    for (int i = 0; i < 7226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7227(int& val) {
    func_in_7227(val);
    const int x = 7227;
    std::cout << 7227;
    for (int i = 0; i < 7227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7227(int& val) {
    const int x = 7227;
    std::cout << 7227;
    for (int i = 0; i < 7227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7228(int& val) {
    func_in_7228(val);
    const int x = 7228;
    std::cout << 7228;
    for (int i = 0; i < 7228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7228(int& val) {
    const int x = 7228;
    std::cout << 7228;
    for (int i = 0; i < 7228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7229(int& val) {
    func_in_7229(val);
    const int x = 7229;
    std::cout << 7229;
    for (int i = 0; i < 7229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7229(int& val) {
    const int x = 7229;
    std::cout << 7229;
    for (int i = 0; i < 7229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7230(int& val) {
    func_in_7230(val);
    const int x = 7230;
    std::cout << 7230;
    for (int i = 0; i < 7230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7230(int& val) {
    const int x = 7230;
    std::cout << 7230;
    for (int i = 0; i < 7230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7231(int& val) {
    func_in_7231(val);
    const int x = 7231;
    std::cout << 7231;
    for (int i = 0; i < 7231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7231(int& val) {
    const int x = 7231;
    std::cout << 7231;
    for (int i = 0; i < 7231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7232(int& val) {
    func_in_7232(val);
    const int x = 7232;
    std::cout << 7232;
    for (int i = 0; i < 7232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7232(int& val) {
    const int x = 7232;
    std::cout << 7232;
    for (int i = 0; i < 7232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7233(int& val) {
    func_in_7233(val);
    const int x = 7233;
    std::cout << 7233;
    for (int i = 0; i < 7233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7233(int& val) {
    const int x = 7233;
    std::cout << 7233;
    for (int i = 0; i < 7233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7234(int& val) {
    func_in_7234(val);
    const int x = 7234;
    std::cout << 7234;
    for (int i = 0; i < 7234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7234(int& val) {
    const int x = 7234;
    std::cout << 7234;
    for (int i = 0; i < 7234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7235(int& val) {
    func_in_7235(val);
    const int x = 7235;
    std::cout << 7235;
    for (int i = 0; i < 7235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7235(int& val) {
    const int x = 7235;
    std::cout << 7235;
    for (int i = 0; i < 7235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7236(int& val) {
    func_in_7236(val);
    const int x = 7236;
    std::cout << 7236;
    for (int i = 0; i < 7236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7236(int& val) {
    const int x = 7236;
    std::cout << 7236;
    for (int i = 0; i < 7236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7237(int& val) {
    func_in_7237(val);
    const int x = 7237;
    std::cout << 7237;
    for (int i = 0; i < 7237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7237(int& val) {
    const int x = 7237;
    std::cout << 7237;
    for (int i = 0; i < 7237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7238(int& val) {
    func_in_7238(val);
    const int x = 7238;
    std::cout << 7238;
    for (int i = 0; i < 7238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7238(int& val) {
    const int x = 7238;
    std::cout << 7238;
    for (int i = 0; i < 7238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7239(int& val) {
    func_in_7239(val);
    const int x = 7239;
    std::cout << 7239;
    for (int i = 0; i < 7239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7239(int& val) {
    const int x = 7239;
    std::cout << 7239;
    for (int i = 0; i < 7239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7240(int& val) {
    func_in_7240(val);
    const int x = 7240;
    std::cout << 7240;
    for (int i = 0; i < 7240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7240(int& val) {
    const int x = 7240;
    std::cout << 7240;
    for (int i = 0; i < 7240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7241(int& val) {
    func_in_7241(val);
    const int x = 7241;
    std::cout << 7241;
    for (int i = 0; i < 7241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7241(int& val) {
    const int x = 7241;
    std::cout << 7241;
    for (int i = 0; i < 7241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7242(int& val) {
    func_in_7242(val);
    const int x = 7242;
    std::cout << 7242;
    for (int i = 0; i < 7242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7242(int& val) {
    const int x = 7242;
    std::cout << 7242;
    for (int i = 0; i < 7242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7243(int& val) {
    func_in_7243(val);
    const int x = 7243;
    std::cout << 7243;
    for (int i = 0; i < 7243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7243(int& val) {
    const int x = 7243;
    std::cout << 7243;
    for (int i = 0; i < 7243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7244(int& val) {
    func_in_7244(val);
    const int x = 7244;
    std::cout << 7244;
    for (int i = 0; i < 7244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7244(int& val) {
    const int x = 7244;
    std::cout << 7244;
    for (int i = 0; i < 7244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7245(int& val) {
    func_in_7245(val);
    const int x = 7245;
    std::cout << 7245;
    for (int i = 0; i < 7245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7245(int& val) {
    const int x = 7245;
    std::cout << 7245;
    for (int i = 0; i < 7245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7246(int& val) {
    func_in_7246(val);
    const int x = 7246;
    std::cout << 7246;
    for (int i = 0; i < 7246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7246(int& val) {
    const int x = 7246;
    std::cout << 7246;
    for (int i = 0; i < 7246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7247(int& val) {
    func_in_7247(val);
    const int x = 7247;
    std::cout << 7247;
    for (int i = 0; i < 7247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7247(int& val) {
    const int x = 7247;
    std::cout << 7247;
    for (int i = 0; i < 7247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7248(int& val) {
    func_in_7248(val);
    const int x = 7248;
    std::cout << 7248;
    for (int i = 0; i < 7248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7248(int& val) {
    const int x = 7248;
    std::cout << 7248;
    for (int i = 0; i < 7248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7249(int& val) {
    func_in_7249(val);
    const int x = 7249;
    std::cout << 7249;
    for (int i = 0; i < 7249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7249(int& val) {
    const int x = 7249;
    std::cout << 7249;
    for (int i = 0; i < 7249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7250(int& val) {
    func_in_7250(val);
    const int x = 7250;
    std::cout << 7250;
    for (int i = 0; i < 7250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7250(int& val) {
    const int x = 7250;
    std::cout << 7250;
    for (int i = 0; i < 7250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7251(int& val) {
    func_in_7251(val);
    const int x = 7251;
    std::cout << 7251;
    for (int i = 0; i < 7251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7251(int& val) {
    const int x = 7251;
    std::cout << 7251;
    for (int i = 0; i < 7251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7252(int& val) {
    func_in_7252(val);
    const int x = 7252;
    std::cout << 7252;
    for (int i = 0; i < 7252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7252(int& val) {
    const int x = 7252;
    std::cout << 7252;
    for (int i = 0; i < 7252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7253(int& val) {
    func_in_7253(val);
    const int x = 7253;
    std::cout << 7253;
    for (int i = 0; i < 7253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7253(int& val) {
    const int x = 7253;
    std::cout << 7253;
    for (int i = 0; i < 7253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7254(int& val) {
    func_in_7254(val);
    const int x = 7254;
    std::cout << 7254;
    for (int i = 0; i < 7254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7254(int& val) {
    const int x = 7254;
    std::cout << 7254;
    for (int i = 0; i < 7254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7255(int& val) {
    func_in_7255(val);
    const int x = 7255;
    std::cout << 7255;
    for (int i = 0; i < 7255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7255(int& val) {
    const int x = 7255;
    std::cout << 7255;
    for (int i = 0; i < 7255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7256(int& val) {
    func_in_7256(val);
    const int x = 7256;
    std::cout << 7256;
    for (int i = 0; i < 7256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7256(int& val) {
    const int x = 7256;
    std::cout << 7256;
    for (int i = 0; i < 7256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7257(int& val) {
    func_in_7257(val);
    const int x = 7257;
    std::cout << 7257;
    for (int i = 0; i < 7257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7257(int& val) {
    const int x = 7257;
    std::cout << 7257;
    for (int i = 0; i < 7257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7258(int& val) {
    func_in_7258(val);
    const int x = 7258;
    std::cout << 7258;
    for (int i = 0; i < 7258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7258(int& val) {
    const int x = 7258;
    std::cout << 7258;
    for (int i = 0; i < 7258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7259(int& val) {
    func_in_7259(val);
    const int x = 7259;
    std::cout << 7259;
    for (int i = 0; i < 7259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7259(int& val) {
    const int x = 7259;
    std::cout << 7259;
    for (int i = 0; i < 7259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7260(int& val) {
    func_in_7260(val);
    const int x = 7260;
    std::cout << 7260;
    for (int i = 0; i < 7260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7260(int& val) {
    const int x = 7260;
    std::cout << 7260;
    for (int i = 0; i < 7260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7261(int& val) {
    func_in_7261(val);
    const int x = 7261;
    std::cout << 7261;
    for (int i = 0; i < 7261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7261(int& val) {
    const int x = 7261;
    std::cout << 7261;
    for (int i = 0; i < 7261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7262(int& val) {
    func_in_7262(val);
    const int x = 7262;
    std::cout << 7262;
    for (int i = 0; i < 7262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7262(int& val) {
    const int x = 7262;
    std::cout << 7262;
    for (int i = 0; i < 7262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7263(int& val) {
    func_in_7263(val);
    const int x = 7263;
    std::cout << 7263;
    for (int i = 0; i < 7263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7263(int& val) {
    const int x = 7263;
    std::cout << 7263;
    for (int i = 0; i < 7263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7264(int& val) {
    func_in_7264(val);
    const int x = 7264;
    std::cout << 7264;
    for (int i = 0; i < 7264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7264(int& val) {
    const int x = 7264;
    std::cout << 7264;
    for (int i = 0; i < 7264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7265(int& val) {
    func_in_7265(val);
    const int x = 7265;
    std::cout << 7265;
    for (int i = 0; i < 7265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7265(int& val) {
    const int x = 7265;
    std::cout << 7265;
    for (int i = 0; i < 7265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7266(int& val) {
    func_in_7266(val);
    const int x = 7266;
    std::cout << 7266;
    for (int i = 0; i < 7266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7266(int& val) {
    const int x = 7266;
    std::cout << 7266;
    for (int i = 0; i < 7266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7267(int& val) {
    func_in_7267(val);
    const int x = 7267;
    std::cout << 7267;
    for (int i = 0; i < 7267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7267(int& val) {
    const int x = 7267;
    std::cout << 7267;
    for (int i = 0; i < 7267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7268(int& val) {
    func_in_7268(val);
    const int x = 7268;
    std::cout << 7268;
    for (int i = 0; i < 7268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7268(int& val) {
    const int x = 7268;
    std::cout << 7268;
    for (int i = 0; i < 7268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7269(int& val) {
    func_in_7269(val);
    const int x = 7269;
    std::cout << 7269;
    for (int i = 0; i < 7269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7269(int& val) {
    const int x = 7269;
    std::cout << 7269;
    for (int i = 0; i < 7269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7270(int& val) {
    func_in_7270(val);
    const int x = 7270;
    std::cout << 7270;
    for (int i = 0; i < 7270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7270(int& val) {
    const int x = 7270;
    std::cout << 7270;
    for (int i = 0; i < 7270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7271(int& val) {
    func_in_7271(val);
    const int x = 7271;
    std::cout << 7271;
    for (int i = 0; i < 7271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7271(int& val) {
    const int x = 7271;
    std::cout << 7271;
    for (int i = 0; i < 7271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7272(int& val) {
    func_in_7272(val);
    const int x = 7272;
    std::cout << 7272;
    for (int i = 0; i < 7272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7272(int& val) {
    const int x = 7272;
    std::cout << 7272;
    for (int i = 0; i < 7272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7273(int& val) {
    func_in_7273(val);
    const int x = 7273;
    std::cout << 7273;
    for (int i = 0; i < 7273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7273(int& val) {
    const int x = 7273;
    std::cout << 7273;
    for (int i = 0; i < 7273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7274(int& val) {
    func_in_7274(val);
    const int x = 7274;
    std::cout << 7274;
    for (int i = 0; i < 7274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7274(int& val) {
    const int x = 7274;
    std::cout << 7274;
    for (int i = 0; i < 7274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7275(int& val) {
    func_in_7275(val);
    const int x = 7275;
    std::cout << 7275;
    for (int i = 0; i < 7275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7275(int& val) {
    const int x = 7275;
    std::cout << 7275;
    for (int i = 0; i < 7275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7276(int& val) {
    func_in_7276(val);
    const int x = 7276;
    std::cout << 7276;
    for (int i = 0; i < 7276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7276(int& val) {
    const int x = 7276;
    std::cout << 7276;
    for (int i = 0; i < 7276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7277(int& val) {
    func_in_7277(val);
    const int x = 7277;
    std::cout << 7277;
    for (int i = 0; i < 7277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7277(int& val) {
    const int x = 7277;
    std::cout << 7277;
    for (int i = 0; i < 7277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7278(int& val) {
    func_in_7278(val);
    const int x = 7278;
    std::cout << 7278;
    for (int i = 0; i < 7278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7278(int& val) {
    const int x = 7278;
    std::cout << 7278;
    for (int i = 0; i < 7278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7279(int& val) {
    func_in_7279(val);
    const int x = 7279;
    std::cout << 7279;
    for (int i = 0; i < 7279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7279(int& val) {
    const int x = 7279;
    std::cout << 7279;
    for (int i = 0; i < 7279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7280(int& val) {
    func_in_7280(val);
    const int x = 7280;
    std::cout << 7280;
    for (int i = 0; i < 7280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7280(int& val) {
    const int x = 7280;
    std::cout << 7280;
    for (int i = 0; i < 7280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7281(int& val) {
    func_in_7281(val);
    const int x = 7281;
    std::cout << 7281;
    for (int i = 0; i < 7281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7281(int& val) {
    const int x = 7281;
    std::cout << 7281;
    for (int i = 0; i < 7281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7282(int& val) {
    func_in_7282(val);
    const int x = 7282;
    std::cout << 7282;
    for (int i = 0; i < 7282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7282(int& val) {
    const int x = 7282;
    std::cout << 7282;
    for (int i = 0; i < 7282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7283(int& val) {
    func_in_7283(val);
    const int x = 7283;
    std::cout << 7283;
    for (int i = 0; i < 7283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7283(int& val) {
    const int x = 7283;
    std::cout << 7283;
    for (int i = 0; i < 7283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7284(int& val) {
    func_in_7284(val);
    const int x = 7284;
    std::cout << 7284;
    for (int i = 0; i < 7284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7284(int& val) {
    const int x = 7284;
    std::cout << 7284;
    for (int i = 0; i < 7284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7285(int& val) {
    func_in_7285(val);
    const int x = 7285;
    std::cout << 7285;
    for (int i = 0; i < 7285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7285(int& val) {
    const int x = 7285;
    std::cout << 7285;
    for (int i = 0; i < 7285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7286(int& val) {
    func_in_7286(val);
    const int x = 7286;
    std::cout << 7286;
    for (int i = 0; i < 7286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7286(int& val) {
    const int x = 7286;
    std::cout << 7286;
    for (int i = 0; i < 7286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7287(int& val) {
    func_in_7287(val);
    const int x = 7287;
    std::cout << 7287;
    for (int i = 0; i < 7287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7287(int& val) {
    const int x = 7287;
    std::cout << 7287;
    for (int i = 0; i < 7287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7288(int& val) {
    func_in_7288(val);
    const int x = 7288;
    std::cout << 7288;
    for (int i = 0; i < 7288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7288(int& val) {
    const int x = 7288;
    std::cout << 7288;
    for (int i = 0; i < 7288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7289(int& val) {
    func_in_7289(val);
    const int x = 7289;
    std::cout << 7289;
    for (int i = 0; i < 7289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7289(int& val) {
    const int x = 7289;
    std::cout << 7289;
    for (int i = 0; i < 7289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7290(int& val) {
    func_in_7290(val);
    const int x = 7290;
    std::cout << 7290;
    for (int i = 0; i < 7290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7290(int& val) {
    const int x = 7290;
    std::cout << 7290;
    for (int i = 0; i < 7290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7291(int& val) {
    func_in_7291(val);
    const int x = 7291;
    std::cout << 7291;
    for (int i = 0; i < 7291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7291(int& val) {
    const int x = 7291;
    std::cout << 7291;
    for (int i = 0; i < 7291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7292(int& val) {
    func_in_7292(val);
    const int x = 7292;
    std::cout << 7292;
    for (int i = 0; i < 7292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7292(int& val) {
    const int x = 7292;
    std::cout << 7292;
    for (int i = 0; i < 7292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7293(int& val) {
    func_in_7293(val);
    const int x = 7293;
    std::cout << 7293;
    for (int i = 0; i < 7293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7293(int& val) {
    const int x = 7293;
    std::cout << 7293;
    for (int i = 0; i < 7293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7294(int& val) {
    func_in_7294(val);
    const int x = 7294;
    std::cout << 7294;
    for (int i = 0; i < 7294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7294(int& val) {
    const int x = 7294;
    std::cout << 7294;
    for (int i = 0; i < 7294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7295(int& val) {
    func_in_7295(val);
    const int x = 7295;
    std::cout << 7295;
    for (int i = 0; i < 7295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7295(int& val) {
    const int x = 7295;
    std::cout << 7295;
    for (int i = 0; i < 7295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7296(int& val) {
    func_in_7296(val);
    const int x = 7296;
    std::cout << 7296;
    for (int i = 0; i < 7296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7296(int& val) {
    const int x = 7296;
    std::cout << 7296;
    for (int i = 0; i < 7296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7297(int& val) {
    func_in_7297(val);
    const int x = 7297;
    std::cout << 7297;
    for (int i = 0; i < 7297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7297(int& val) {
    const int x = 7297;
    std::cout << 7297;
    for (int i = 0; i < 7297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7298(int& val) {
    func_in_7298(val);
    const int x = 7298;
    std::cout << 7298;
    for (int i = 0; i < 7298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7298(int& val) {
    const int x = 7298;
    std::cout << 7298;
    for (int i = 0; i < 7298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7299(int& val) {
    func_in_7299(val);
    const int x = 7299;
    std::cout << 7299;
    for (int i = 0; i < 7299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7299(int& val) {
    const int x = 7299;
    std::cout << 7299;
    for (int i = 0; i < 7299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7300(int& val) {
    func_in_7300(val);
    const int x = 7300;
    std::cout << 7300;
    for (int i = 0; i < 7300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7300(int& val) {
    const int x = 7300;
    std::cout << 7300;
    for (int i = 0; i < 7300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7301(int& val) {
    func_in_7301(val);
    const int x = 7301;
    std::cout << 7301;
    for (int i = 0; i < 7301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7301(int& val) {
    const int x = 7301;
    std::cout << 7301;
    for (int i = 0; i < 7301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7302(int& val) {
    func_in_7302(val);
    const int x = 7302;
    std::cout << 7302;
    for (int i = 0; i < 7302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7302(int& val) {
    const int x = 7302;
    std::cout << 7302;
    for (int i = 0; i < 7302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7303(int& val) {
    func_in_7303(val);
    const int x = 7303;
    std::cout << 7303;
    for (int i = 0; i < 7303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7303(int& val) {
    const int x = 7303;
    std::cout << 7303;
    for (int i = 0; i < 7303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7304(int& val) {
    func_in_7304(val);
    const int x = 7304;
    std::cout << 7304;
    for (int i = 0; i < 7304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7304(int& val) {
    const int x = 7304;
    std::cout << 7304;
    for (int i = 0; i < 7304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7305(int& val) {
    func_in_7305(val);
    const int x = 7305;
    std::cout << 7305;
    for (int i = 0; i < 7305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7305(int& val) {
    const int x = 7305;
    std::cout << 7305;
    for (int i = 0; i < 7305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7306(int& val) {
    func_in_7306(val);
    const int x = 7306;
    std::cout << 7306;
    for (int i = 0; i < 7306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7306(int& val) {
    const int x = 7306;
    std::cout << 7306;
    for (int i = 0; i < 7306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7307(int& val) {
    func_in_7307(val);
    const int x = 7307;
    std::cout << 7307;
    for (int i = 0; i < 7307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7307(int& val) {
    const int x = 7307;
    std::cout << 7307;
    for (int i = 0; i < 7307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7308(int& val) {
    func_in_7308(val);
    const int x = 7308;
    std::cout << 7308;
    for (int i = 0; i < 7308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7308(int& val) {
    const int x = 7308;
    std::cout << 7308;
    for (int i = 0; i < 7308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7309(int& val) {
    func_in_7309(val);
    const int x = 7309;
    std::cout << 7309;
    for (int i = 0; i < 7309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7309(int& val) {
    const int x = 7309;
    std::cout << 7309;
    for (int i = 0; i < 7309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7310(int& val) {
    func_in_7310(val);
    const int x = 7310;
    std::cout << 7310;
    for (int i = 0; i < 7310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7310(int& val) {
    const int x = 7310;
    std::cout << 7310;
    for (int i = 0; i < 7310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7311(int& val) {
    func_in_7311(val);
    const int x = 7311;
    std::cout << 7311;
    for (int i = 0; i < 7311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7311(int& val) {
    const int x = 7311;
    std::cout << 7311;
    for (int i = 0; i < 7311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7312(int& val) {
    func_in_7312(val);
    const int x = 7312;
    std::cout << 7312;
    for (int i = 0; i < 7312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7312(int& val) {
    const int x = 7312;
    std::cout << 7312;
    for (int i = 0; i < 7312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7313(int& val) {
    func_in_7313(val);
    const int x = 7313;
    std::cout << 7313;
    for (int i = 0; i < 7313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7313(int& val) {
    const int x = 7313;
    std::cout << 7313;
    for (int i = 0; i < 7313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7314(int& val) {
    func_in_7314(val);
    const int x = 7314;
    std::cout << 7314;
    for (int i = 0; i < 7314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7314(int& val) {
    const int x = 7314;
    std::cout << 7314;
    for (int i = 0; i < 7314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7315(int& val) {
    func_in_7315(val);
    const int x = 7315;
    std::cout << 7315;
    for (int i = 0; i < 7315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7315(int& val) {
    const int x = 7315;
    std::cout << 7315;
    for (int i = 0; i < 7315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7316(int& val) {
    func_in_7316(val);
    const int x = 7316;
    std::cout << 7316;
    for (int i = 0; i < 7316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7316(int& val) {
    const int x = 7316;
    std::cout << 7316;
    for (int i = 0; i < 7316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7317(int& val) {
    func_in_7317(val);
    const int x = 7317;
    std::cout << 7317;
    for (int i = 0; i < 7317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7317(int& val) {
    const int x = 7317;
    std::cout << 7317;
    for (int i = 0; i < 7317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7318(int& val) {
    func_in_7318(val);
    const int x = 7318;
    std::cout << 7318;
    for (int i = 0; i < 7318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7318(int& val) {
    const int x = 7318;
    std::cout << 7318;
    for (int i = 0; i < 7318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7319(int& val) {
    func_in_7319(val);
    const int x = 7319;
    std::cout << 7319;
    for (int i = 0; i < 7319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7319(int& val) {
    const int x = 7319;
    std::cout << 7319;
    for (int i = 0; i < 7319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7320(int& val) {
    func_in_7320(val);
    const int x = 7320;
    std::cout << 7320;
    for (int i = 0; i < 7320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7320(int& val) {
    const int x = 7320;
    std::cout << 7320;
    for (int i = 0; i < 7320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7321(int& val) {
    func_in_7321(val);
    const int x = 7321;
    std::cout << 7321;
    for (int i = 0; i < 7321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7321(int& val) {
    const int x = 7321;
    std::cout << 7321;
    for (int i = 0; i < 7321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7322(int& val) {
    func_in_7322(val);
    const int x = 7322;
    std::cout << 7322;
    for (int i = 0; i < 7322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7322(int& val) {
    const int x = 7322;
    std::cout << 7322;
    for (int i = 0; i < 7322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7323(int& val) {
    func_in_7323(val);
    const int x = 7323;
    std::cout << 7323;
    for (int i = 0; i < 7323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7323(int& val) {
    const int x = 7323;
    std::cout << 7323;
    for (int i = 0; i < 7323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7324(int& val) {
    func_in_7324(val);
    const int x = 7324;
    std::cout << 7324;
    for (int i = 0; i < 7324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7324(int& val) {
    const int x = 7324;
    std::cout << 7324;
    for (int i = 0; i < 7324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7325(int& val) {
    func_in_7325(val);
    const int x = 7325;
    std::cout << 7325;
    for (int i = 0; i < 7325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7325(int& val) {
    const int x = 7325;
    std::cout << 7325;
    for (int i = 0; i < 7325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7326(int& val) {
    func_in_7326(val);
    const int x = 7326;
    std::cout << 7326;
    for (int i = 0; i < 7326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7326(int& val) {
    const int x = 7326;
    std::cout << 7326;
    for (int i = 0; i < 7326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7327(int& val) {
    func_in_7327(val);
    const int x = 7327;
    std::cout << 7327;
    for (int i = 0; i < 7327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7327(int& val) {
    const int x = 7327;
    std::cout << 7327;
    for (int i = 0; i < 7327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7328(int& val) {
    func_in_7328(val);
    const int x = 7328;
    std::cout << 7328;
    for (int i = 0; i < 7328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7328(int& val) {
    const int x = 7328;
    std::cout << 7328;
    for (int i = 0; i < 7328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7329(int& val) {
    func_in_7329(val);
    const int x = 7329;
    std::cout << 7329;
    for (int i = 0; i < 7329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7329(int& val) {
    const int x = 7329;
    std::cout << 7329;
    for (int i = 0; i < 7329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7330(int& val) {
    func_in_7330(val);
    const int x = 7330;
    std::cout << 7330;
    for (int i = 0; i < 7330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7330(int& val) {
    const int x = 7330;
    std::cout << 7330;
    for (int i = 0; i < 7330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7331(int& val) {
    func_in_7331(val);
    const int x = 7331;
    std::cout << 7331;
    for (int i = 0; i < 7331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7331(int& val) {
    const int x = 7331;
    std::cout << 7331;
    for (int i = 0; i < 7331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7332(int& val) {
    func_in_7332(val);
    const int x = 7332;
    std::cout << 7332;
    for (int i = 0; i < 7332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7332(int& val) {
    const int x = 7332;
    std::cout << 7332;
    for (int i = 0; i < 7332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7333(int& val) {
    func_in_7333(val);
    const int x = 7333;
    std::cout << 7333;
    for (int i = 0; i < 7333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7333(int& val) {
    const int x = 7333;
    std::cout << 7333;
    for (int i = 0; i < 7333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7334(int& val) {
    func_in_7334(val);
    const int x = 7334;
    std::cout << 7334;
    for (int i = 0; i < 7334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7334(int& val) {
    const int x = 7334;
    std::cout << 7334;
    for (int i = 0; i < 7334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7335(int& val) {
    func_in_7335(val);
    const int x = 7335;
    std::cout << 7335;
    for (int i = 0; i < 7335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7335(int& val) {
    const int x = 7335;
    std::cout << 7335;
    for (int i = 0; i < 7335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7336(int& val) {
    func_in_7336(val);
    const int x = 7336;
    std::cout << 7336;
    for (int i = 0; i < 7336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7336(int& val) {
    const int x = 7336;
    std::cout << 7336;
    for (int i = 0; i < 7336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7337(int& val) {
    func_in_7337(val);
    const int x = 7337;
    std::cout << 7337;
    for (int i = 0; i < 7337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7337(int& val) {
    const int x = 7337;
    std::cout << 7337;
    for (int i = 0; i < 7337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7338(int& val) {
    func_in_7338(val);
    const int x = 7338;
    std::cout << 7338;
    for (int i = 0; i < 7338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7338(int& val) {
    const int x = 7338;
    std::cout << 7338;
    for (int i = 0; i < 7338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7339(int& val) {
    func_in_7339(val);
    const int x = 7339;
    std::cout << 7339;
    for (int i = 0; i < 7339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7339(int& val) {
    const int x = 7339;
    std::cout << 7339;
    for (int i = 0; i < 7339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7340(int& val) {
    func_in_7340(val);
    const int x = 7340;
    std::cout << 7340;
    for (int i = 0; i < 7340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7340(int& val) {
    const int x = 7340;
    std::cout << 7340;
    for (int i = 0; i < 7340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7341(int& val) {
    func_in_7341(val);
    const int x = 7341;
    std::cout << 7341;
    for (int i = 0; i < 7341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7341(int& val) {
    const int x = 7341;
    std::cout << 7341;
    for (int i = 0; i < 7341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7342(int& val) {
    func_in_7342(val);
    const int x = 7342;
    std::cout << 7342;
    for (int i = 0; i < 7342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7342(int& val) {
    const int x = 7342;
    std::cout << 7342;
    for (int i = 0; i < 7342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7343(int& val) {
    func_in_7343(val);
    const int x = 7343;
    std::cout << 7343;
    for (int i = 0; i < 7343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7343(int& val) {
    const int x = 7343;
    std::cout << 7343;
    for (int i = 0; i < 7343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7344(int& val) {
    func_in_7344(val);
    const int x = 7344;
    std::cout << 7344;
    for (int i = 0; i < 7344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7344(int& val) {
    const int x = 7344;
    std::cout << 7344;
    for (int i = 0; i < 7344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7345(int& val) {
    func_in_7345(val);
    const int x = 7345;
    std::cout << 7345;
    for (int i = 0; i < 7345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7345(int& val) {
    const int x = 7345;
    std::cout << 7345;
    for (int i = 0; i < 7345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7346(int& val) {
    func_in_7346(val);
    const int x = 7346;
    std::cout << 7346;
    for (int i = 0; i < 7346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7346(int& val) {
    const int x = 7346;
    std::cout << 7346;
    for (int i = 0; i < 7346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7347(int& val) {
    func_in_7347(val);
    const int x = 7347;
    std::cout << 7347;
    for (int i = 0; i < 7347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7347(int& val) {
    const int x = 7347;
    std::cout << 7347;
    for (int i = 0; i < 7347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7348(int& val) {
    func_in_7348(val);
    const int x = 7348;
    std::cout << 7348;
    for (int i = 0; i < 7348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7348(int& val) {
    const int x = 7348;
    std::cout << 7348;
    for (int i = 0; i < 7348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7349(int& val) {
    func_in_7349(val);
    const int x = 7349;
    std::cout << 7349;
    for (int i = 0; i < 7349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7349(int& val) {
    const int x = 7349;
    std::cout << 7349;
    for (int i = 0; i < 7349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7350(int& val) {
    func_in_7350(val);
    const int x = 7350;
    std::cout << 7350;
    for (int i = 0; i < 7350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7350(int& val) {
    const int x = 7350;
    std::cout << 7350;
    for (int i = 0; i < 7350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7351(int& val) {
    func_in_7351(val);
    const int x = 7351;
    std::cout << 7351;
    for (int i = 0; i < 7351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7351(int& val) {
    const int x = 7351;
    std::cout << 7351;
    for (int i = 0; i < 7351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7352(int& val) {
    func_in_7352(val);
    const int x = 7352;
    std::cout << 7352;
    for (int i = 0; i < 7352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7352(int& val) {
    const int x = 7352;
    std::cout << 7352;
    for (int i = 0; i < 7352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7353(int& val) {
    func_in_7353(val);
    const int x = 7353;
    std::cout << 7353;
    for (int i = 0; i < 7353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7353(int& val) {
    const int x = 7353;
    std::cout << 7353;
    for (int i = 0; i < 7353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7354(int& val) {
    func_in_7354(val);
    const int x = 7354;
    std::cout << 7354;
    for (int i = 0; i < 7354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7354(int& val) {
    const int x = 7354;
    std::cout << 7354;
    for (int i = 0; i < 7354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7355(int& val) {
    func_in_7355(val);
    const int x = 7355;
    std::cout << 7355;
    for (int i = 0; i < 7355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7355(int& val) {
    const int x = 7355;
    std::cout << 7355;
    for (int i = 0; i < 7355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7356(int& val) {
    func_in_7356(val);
    const int x = 7356;
    std::cout << 7356;
    for (int i = 0; i < 7356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7356(int& val) {
    const int x = 7356;
    std::cout << 7356;
    for (int i = 0; i < 7356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7357(int& val) {
    func_in_7357(val);
    const int x = 7357;
    std::cout << 7357;
    for (int i = 0; i < 7357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7357(int& val) {
    const int x = 7357;
    std::cout << 7357;
    for (int i = 0; i < 7357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7358(int& val) {
    func_in_7358(val);
    const int x = 7358;
    std::cout << 7358;
    for (int i = 0; i < 7358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7358(int& val) {
    const int x = 7358;
    std::cout << 7358;
    for (int i = 0; i < 7358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7359(int& val) {
    func_in_7359(val);
    const int x = 7359;
    std::cout << 7359;
    for (int i = 0; i < 7359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7359(int& val) {
    const int x = 7359;
    std::cout << 7359;
    for (int i = 0; i < 7359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7360(int& val) {
    func_in_7360(val);
    const int x = 7360;
    std::cout << 7360;
    for (int i = 0; i < 7360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7360(int& val) {
    const int x = 7360;
    std::cout << 7360;
    for (int i = 0; i < 7360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7361(int& val) {
    func_in_7361(val);
    const int x = 7361;
    std::cout << 7361;
    for (int i = 0; i < 7361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7361(int& val) {
    const int x = 7361;
    std::cout << 7361;
    for (int i = 0; i < 7361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7362(int& val) {
    func_in_7362(val);
    const int x = 7362;
    std::cout << 7362;
    for (int i = 0; i < 7362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7362(int& val) {
    const int x = 7362;
    std::cout << 7362;
    for (int i = 0; i < 7362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7363(int& val) {
    func_in_7363(val);
    const int x = 7363;
    std::cout << 7363;
    for (int i = 0; i < 7363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7363(int& val) {
    const int x = 7363;
    std::cout << 7363;
    for (int i = 0; i < 7363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7364(int& val) {
    func_in_7364(val);
    const int x = 7364;
    std::cout << 7364;
    for (int i = 0; i < 7364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7364(int& val) {
    const int x = 7364;
    std::cout << 7364;
    for (int i = 0; i < 7364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7365(int& val) {
    func_in_7365(val);
    const int x = 7365;
    std::cout << 7365;
    for (int i = 0; i < 7365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7365(int& val) {
    const int x = 7365;
    std::cout << 7365;
    for (int i = 0; i < 7365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7366(int& val) {
    func_in_7366(val);
    const int x = 7366;
    std::cout << 7366;
    for (int i = 0; i < 7366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7366(int& val) {
    const int x = 7366;
    std::cout << 7366;
    for (int i = 0; i < 7366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7367(int& val) {
    func_in_7367(val);
    const int x = 7367;
    std::cout << 7367;
    for (int i = 0; i < 7367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7367(int& val) {
    const int x = 7367;
    std::cout << 7367;
    for (int i = 0; i < 7367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7368(int& val) {
    func_in_7368(val);
    const int x = 7368;
    std::cout << 7368;
    for (int i = 0; i < 7368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7368(int& val) {
    const int x = 7368;
    std::cout << 7368;
    for (int i = 0; i < 7368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7369(int& val) {
    func_in_7369(val);
    const int x = 7369;
    std::cout << 7369;
    for (int i = 0; i < 7369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7369(int& val) {
    const int x = 7369;
    std::cout << 7369;
    for (int i = 0; i < 7369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7370(int& val) {
    func_in_7370(val);
    const int x = 7370;
    std::cout << 7370;
    for (int i = 0; i < 7370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7370(int& val) {
    const int x = 7370;
    std::cout << 7370;
    for (int i = 0; i < 7370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7371(int& val) {
    func_in_7371(val);
    const int x = 7371;
    std::cout << 7371;
    for (int i = 0; i < 7371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7371(int& val) {
    const int x = 7371;
    std::cout << 7371;
    for (int i = 0; i < 7371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7372(int& val) {
    func_in_7372(val);
    const int x = 7372;
    std::cout << 7372;
    for (int i = 0; i < 7372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7372(int& val) {
    const int x = 7372;
    std::cout << 7372;
    for (int i = 0; i < 7372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7373(int& val) {
    func_in_7373(val);
    const int x = 7373;
    std::cout << 7373;
    for (int i = 0; i < 7373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7373(int& val) {
    const int x = 7373;
    std::cout << 7373;
    for (int i = 0; i < 7373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7374(int& val) {
    func_in_7374(val);
    const int x = 7374;
    std::cout << 7374;
    for (int i = 0; i < 7374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7374(int& val) {
    const int x = 7374;
    std::cout << 7374;
    for (int i = 0; i < 7374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7375(int& val) {
    func_in_7375(val);
    const int x = 7375;
    std::cout << 7375;
    for (int i = 0; i < 7375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7375(int& val) {
    const int x = 7375;
    std::cout << 7375;
    for (int i = 0; i < 7375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7376(int& val) {
    func_in_7376(val);
    const int x = 7376;
    std::cout << 7376;
    for (int i = 0; i < 7376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7376(int& val) {
    const int x = 7376;
    std::cout << 7376;
    for (int i = 0; i < 7376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7377(int& val) {
    func_in_7377(val);
    const int x = 7377;
    std::cout << 7377;
    for (int i = 0; i < 7377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7377(int& val) {
    const int x = 7377;
    std::cout << 7377;
    for (int i = 0; i < 7377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7378(int& val) {
    func_in_7378(val);
    const int x = 7378;
    std::cout << 7378;
    for (int i = 0; i < 7378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7378(int& val) {
    const int x = 7378;
    std::cout << 7378;
    for (int i = 0; i < 7378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7379(int& val) {
    func_in_7379(val);
    const int x = 7379;
    std::cout << 7379;
    for (int i = 0; i < 7379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7379(int& val) {
    const int x = 7379;
    std::cout << 7379;
    for (int i = 0; i < 7379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7380(int& val) {
    func_in_7380(val);
    const int x = 7380;
    std::cout << 7380;
    for (int i = 0; i < 7380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7380(int& val) {
    const int x = 7380;
    std::cout << 7380;
    for (int i = 0; i < 7380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7381(int& val) {
    func_in_7381(val);
    const int x = 7381;
    std::cout << 7381;
    for (int i = 0; i < 7381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7381(int& val) {
    const int x = 7381;
    std::cout << 7381;
    for (int i = 0; i < 7381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7382(int& val) {
    func_in_7382(val);
    const int x = 7382;
    std::cout << 7382;
    for (int i = 0; i < 7382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7382(int& val) {
    const int x = 7382;
    std::cout << 7382;
    for (int i = 0; i < 7382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7383(int& val) {
    func_in_7383(val);
    const int x = 7383;
    std::cout << 7383;
    for (int i = 0; i < 7383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7383(int& val) {
    const int x = 7383;
    std::cout << 7383;
    for (int i = 0; i < 7383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7384(int& val) {
    func_in_7384(val);
    const int x = 7384;
    std::cout << 7384;
    for (int i = 0; i < 7384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7384(int& val) {
    const int x = 7384;
    std::cout << 7384;
    for (int i = 0; i < 7384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7385(int& val) {
    func_in_7385(val);
    const int x = 7385;
    std::cout << 7385;
    for (int i = 0; i < 7385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7385(int& val) {
    const int x = 7385;
    std::cout << 7385;
    for (int i = 0; i < 7385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7386(int& val) {
    func_in_7386(val);
    const int x = 7386;
    std::cout << 7386;
    for (int i = 0; i < 7386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7386(int& val) {
    const int x = 7386;
    std::cout << 7386;
    for (int i = 0; i < 7386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7387(int& val) {
    func_in_7387(val);
    const int x = 7387;
    std::cout << 7387;
    for (int i = 0; i < 7387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7387(int& val) {
    const int x = 7387;
    std::cout << 7387;
    for (int i = 0; i < 7387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7388(int& val) {
    func_in_7388(val);
    const int x = 7388;
    std::cout << 7388;
    for (int i = 0; i < 7388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7388(int& val) {
    const int x = 7388;
    std::cout << 7388;
    for (int i = 0; i < 7388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7389(int& val) {
    func_in_7389(val);
    const int x = 7389;
    std::cout << 7389;
    for (int i = 0; i < 7389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7389(int& val) {
    const int x = 7389;
    std::cout << 7389;
    for (int i = 0; i < 7389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7390(int& val) {
    func_in_7390(val);
    const int x = 7390;
    std::cout << 7390;
    for (int i = 0; i < 7390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7390(int& val) {
    const int x = 7390;
    std::cout << 7390;
    for (int i = 0; i < 7390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7391(int& val) {
    func_in_7391(val);
    const int x = 7391;
    std::cout << 7391;
    for (int i = 0; i < 7391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7391(int& val) {
    const int x = 7391;
    std::cout << 7391;
    for (int i = 0; i < 7391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7392(int& val) {
    func_in_7392(val);
    const int x = 7392;
    std::cout << 7392;
    for (int i = 0; i < 7392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7392(int& val) {
    const int x = 7392;
    std::cout << 7392;
    for (int i = 0; i < 7392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7393(int& val) {
    func_in_7393(val);
    const int x = 7393;
    std::cout << 7393;
    for (int i = 0; i < 7393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7393(int& val) {
    const int x = 7393;
    std::cout << 7393;
    for (int i = 0; i < 7393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7394(int& val) {
    func_in_7394(val);
    const int x = 7394;
    std::cout << 7394;
    for (int i = 0; i < 7394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7394(int& val) {
    const int x = 7394;
    std::cout << 7394;
    for (int i = 0; i < 7394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7395(int& val) {
    func_in_7395(val);
    const int x = 7395;
    std::cout << 7395;
    for (int i = 0; i < 7395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7395(int& val) {
    const int x = 7395;
    std::cout << 7395;
    for (int i = 0; i < 7395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7396(int& val) {
    func_in_7396(val);
    const int x = 7396;
    std::cout << 7396;
    for (int i = 0; i < 7396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7396(int& val) {
    const int x = 7396;
    std::cout << 7396;
    for (int i = 0; i < 7396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7397(int& val) {
    func_in_7397(val);
    const int x = 7397;
    std::cout << 7397;
    for (int i = 0; i < 7397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7397(int& val) {
    const int x = 7397;
    std::cout << 7397;
    for (int i = 0; i < 7397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7398(int& val) {
    func_in_7398(val);
    const int x = 7398;
    std::cout << 7398;
    for (int i = 0; i < 7398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7398(int& val) {
    const int x = 7398;
    std::cout << 7398;
    for (int i = 0; i < 7398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7399(int& val) {
    func_in_7399(val);
    const int x = 7399;
    std::cout << 7399;
    for (int i = 0; i < 7399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7399(int& val) {
    const int x = 7399;
    std::cout << 7399;
    for (int i = 0; i < 7399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7400(int& val) {
    func_in_7400(val);
    const int x = 7400;
    std::cout << 7400;
    for (int i = 0; i < 7400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7400(int& val) {
    const int x = 7400;
    std::cout << 7400;
    for (int i = 0; i < 7400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7401(int& val) {
    func_in_7401(val);
    const int x = 7401;
    std::cout << 7401;
    for (int i = 0; i < 7401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7401(int& val) {
    const int x = 7401;
    std::cout << 7401;
    for (int i = 0; i < 7401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7402(int& val) {
    func_in_7402(val);
    const int x = 7402;
    std::cout << 7402;
    for (int i = 0; i < 7402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7402(int& val) {
    const int x = 7402;
    std::cout << 7402;
    for (int i = 0; i < 7402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7403(int& val) {
    func_in_7403(val);
    const int x = 7403;
    std::cout << 7403;
    for (int i = 0; i < 7403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7403(int& val) {
    const int x = 7403;
    std::cout << 7403;
    for (int i = 0; i < 7403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7404(int& val) {
    func_in_7404(val);
    const int x = 7404;
    std::cout << 7404;
    for (int i = 0; i < 7404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7404(int& val) {
    const int x = 7404;
    std::cout << 7404;
    for (int i = 0; i < 7404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7405(int& val) {
    func_in_7405(val);
    const int x = 7405;
    std::cout << 7405;
    for (int i = 0; i < 7405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7405(int& val) {
    const int x = 7405;
    std::cout << 7405;
    for (int i = 0; i < 7405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7406(int& val) {
    func_in_7406(val);
    const int x = 7406;
    std::cout << 7406;
    for (int i = 0; i < 7406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7406(int& val) {
    const int x = 7406;
    std::cout << 7406;
    for (int i = 0; i < 7406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7407(int& val) {
    func_in_7407(val);
    const int x = 7407;
    std::cout << 7407;
    for (int i = 0; i < 7407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7407(int& val) {
    const int x = 7407;
    std::cout << 7407;
    for (int i = 0; i < 7407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7408(int& val) {
    func_in_7408(val);
    const int x = 7408;
    std::cout << 7408;
    for (int i = 0; i < 7408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7408(int& val) {
    const int x = 7408;
    std::cout << 7408;
    for (int i = 0; i < 7408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7409(int& val) {
    func_in_7409(val);
    const int x = 7409;
    std::cout << 7409;
    for (int i = 0; i < 7409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7409(int& val) {
    const int x = 7409;
    std::cout << 7409;
    for (int i = 0; i < 7409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7410(int& val) {
    func_in_7410(val);
    const int x = 7410;
    std::cout << 7410;
    for (int i = 0; i < 7410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7410(int& val) {
    const int x = 7410;
    std::cout << 7410;
    for (int i = 0; i < 7410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7411(int& val) {
    func_in_7411(val);
    const int x = 7411;
    std::cout << 7411;
    for (int i = 0; i < 7411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7411(int& val) {
    const int x = 7411;
    std::cout << 7411;
    for (int i = 0; i < 7411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7412(int& val) {
    func_in_7412(val);
    const int x = 7412;
    std::cout << 7412;
    for (int i = 0; i < 7412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7412(int& val) {
    const int x = 7412;
    std::cout << 7412;
    for (int i = 0; i < 7412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7413(int& val) {
    func_in_7413(val);
    const int x = 7413;
    std::cout << 7413;
    for (int i = 0; i < 7413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7413(int& val) {
    const int x = 7413;
    std::cout << 7413;
    for (int i = 0; i < 7413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7414(int& val) {
    func_in_7414(val);
    const int x = 7414;
    std::cout << 7414;
    for (int i = 0; i < 7414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7414(int& val) {
    const int x = 7414;
    std::cout << 7414;
    for (int i = 0; i < 7414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7415(int& val) {
    func_in_7415(val);
    const int x = 7415;
    std::cout << 7415;
    for (int i = 0; i < 7415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7415(int& val) {
    const int x = 7415;
    std::cout << 7415;
    for (int i = 0; i < 7415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7416(int& val) {
    func_in_7416(val);
    const int x = 7416;
    std::cout << 7416;
    for (int i = 0; i < 7416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7416(int& val) {
    const int x = 7416;
    std::cout << 7416;
    for (int i = 0; i < 7416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7417(int& val) {
    func_in_7417(val);
    const int x = 7417;
    std::cout << 7417;
    for (int i = 0; i < 7417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7417(int& val) {
    const int x = 7417;
    std::cout << 7417;
    for (int i = 0; i < 7417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7418(int& val) {
    func_in_7418(val);
    const int x = 7418;
    std::cout << 7418;
    for (int i = 0; i < 7418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7418(int& val) {
    const int x = 7418;
    std::cout << 7418;
    for (int i = 0; i < 7418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7419(int& val) {
    func_in_7419(val);
    const int x = 7419;
    std::cout << 7419;
    for (int i = 0; i < 7419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7419(int& val) {
    const int x = 7419;
    std::cout << 7419;
    for (int i = 0; i < 7419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7420(int& val) {
    func_in_7420(val);
    const int x = 7420;
    std::cout << 7420;
    for (int i = 0; i < 7420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7420(int& val) {
    const int x = 7420;
    std::cout << 7420;
    for (int i = 0; i < 7420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7421(int& val) {
    func_in_7421(val);
    const int x = 7421;
    std::cout << 7421;
    for (int i = 0; i < 7421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7421(int& val) {
    const int x = 7421;
    std::cout << 7421;
    for (int i = 0; i < 7421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7422(int& val) {
    func_in_7422(val);
    const int x = 7422;
    std::cout << 7422;
    for (int i = 0; i < 7422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7422(int& val) {
    const int x = 7422;
    std::cout << 7422;
    for (int i = 0; i < 7422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7423(int& val) {
    func_in_7423(val);
    const int x = 7423;
    std::cout << 7423;
    for (int i = 0; i < 7423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7423(int& val) {
    const int x = 7423;
    std::cout << 7423;
    for (int i = 0; i < 7423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7424(int& val) {
    func_in_7424(val);
    const int x = 7424;
    std::cout << 7424;
    for (int i = 0; i < 7424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7424(int& val) {
    const int x = 7424;
    std::cout << 7424;
    for (int i = 0; i < 7424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7425(int& val) {
    func_in_7425(val);
    const int x = 7425;
    std::cout << 7425;
    for (int i = 0; i < 7425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7425(int& val) {
    const int x = 7425;
    std::cout << 7425;
    for (int i = 0; i < 7425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7426(int& val) {
    func_in_7426(val);
    const int x = 7426;
    std::cout << 7426;
    for (int i = 0; i < 7426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7426(int& val) {
    const int x = 7426;
    std::cout << 7426;
    for (int i = 0; i < 7426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7427(int& val) {
    func_in_7427(val);
    const int x = 7427;
    std::cout << 7427;
    for (int i = 0; i < 7427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7427(int& val) {
    const int x = 7427;
    std::cout << 7427;
    for (int i = 0; i < 7427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7428(int& val) {
    func_in_7428(val);
    const int x = 7428;
    std::cout << 7428;
    for (int i = 0; i < 7428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7428(int& val) {
    const int x = 7428;
    std::cout << 7428;
    for (int i = 0; i < 7428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7429(int& val) {
    func_in_7429(val);
    const int x = 7429;
    std::cout << 7429;
    for (int i = 0; i < 7429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7429(int& val) {
    const int x = 7429;
    std::cout << 7429;
    for (int i = 0; i < 7429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7430(int& val) {
    func_in_7430(val);
    const int x = 7430;
    std::cout << 7430;
    for (int i = 0; i < 7430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7430(int& val) {
    const int x = 7430;
    std::cout << 7430;
    for (int i = 0; i < 7430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7431(int& val) {
    func_in_7431(val);
    const int x = 7431;
    std::cout << 7431;
    for (int i = 0; i < 7431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7431(int& val) {
    const int x = 7431;
    std::cout << 7431;
    for (int i = 0; i < 7431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7432(int& val) {
    func_in_7432(val);
    const int x = 7432;
    std::cout << 7432;
    for (int i = 0; i < 7432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7432(int& val) {
    const int x = 7432;
    std::cout << 7432;
    for (int i = 0; i < 7432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7433(int& val) {
    func_in_7433(val);
    const int x = 7433;
    std::cout << 7433;
    for (int i = 0; i < 7433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7433(int& val) {
    const int x = 7433;
    std::cout << 7433;
    for (int i = 0; i < 7433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7434(int& val) {
    func_in_7434(val);
    const int x = 7434;
    std::cout << 7434;
    for (int i = 0; i < 7434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7434(int& val) {
    const int x = 7434;
    std::cout << 7434;
    for (int i = 0; i < 7434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7435(int& val) {
    func_in_7435(val);
    const int x = 7435;
    std::cout << 7435;
    for (int i = 0; i < 7435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7435(int& val) {
    const int x = 7435;
    std::cout << 7435;
    for (int i = 0; i < 7435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7436(int& val) {
    func_in_7436(val);
    const int x = 7436;
    std::cout << 7436;
    for (int i = 0; i < 7436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7436(int& val) {
    const int x = 7436;
    std::cout << 7436;
    for (int i = 0; i < 7436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7437(int& val) {
    func_in_7437(val);
    const int x = 7437;
    std::cout << 7437;
    for (int i = 0; i < 7437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7437(int& val) {
    const int x = 7437;
    std::cout << 7437;
    for (int i = 0; i < 7437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7438(int& val) {
    func_in_7438(val);
    const int x = 7438;
    std::cout << 7438;
    for (int i = 0; i < 7438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7438(int& val) {
    const int x = 7438;
    std::cout << 7438;
    for (int i = 0; i < 7438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7439(int& val) {
    func_in_7439(val);
    const int x = 7439;
    std::cout << 7439;
    for (int i = 0; i < 7439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7439(int& val) {
    const int x = 7439;
    std::cout << 7439;
    for (int i = 0; i < 7439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7440(int& val) {
    func_in_7440(val);
    const int x = 7440;
    std::cout << 7440;
    for (int i = 0; i < 7440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7440(int& val) {
    const int x = 7440;
    std::cout << 7440;
    for (int i = 0; i < 7440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7441(int& val) {
    func_in_7441(val);
    const int x = 7441;
    std::cout << 7441;
    for (int i = 0; i < 7441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7441(int& val) {
    const int x = 7441;
    std::cout << 7441;
    for (int i = 0; i < 7441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7442(int& val) {
    func_in_7442(val);
    const int x = 7442;
    std::cout << 7442;
    for (int i = 0; i < 7442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7442(int& val) {
    const int x = 7442;
    std::cout << 7442;
    for (int i = 0; i < 7442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7443(int& val) {
    func_in_7443(val);
    const int x = 7443;
    std::cout << 7443;
    for (int i = 0; i < 7443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7443(int& val) {
    const int x = 7443;
    std::cout << 7443;
    for (int i = 0; i < 7443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7444(int& val) {
    func_in_7444(val);
    const int x = 7444;
    std::cout << 7444;
    for (int i = 0; i < 7444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7444(int& val) {
    const int x = 7444;
    std::cout << 7444;
    for (int i = 0; i < 7444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7445(int& val) {
    func_in_7445(val);
    const int x = 7445;
    std::cout << 7445;
    for (int i = 0; i < 7445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7445(int& val) {
    const int x = 7445;
    std::cout << 7445;
    for (int i = 0; i < 7445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7446(int& val) {
    func_in_7446(val);
    const int x = 7446;
    std::cout << 7446;
    for (int i = 0; i < 7446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7446(int& val) {
    const int x = 7446;
    std::cout << 7446;
    for (int i = 0; i < 7446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7447(int& val) {
    func_in_7447(val);
    const int x = 7447;
    std::cout << 7447;
    for (int i = 0; i < 7447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7447(int& val) {
    const int x = 7447;
    std::cout << 7447;
    for (int i = 0; i < 7447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7448(int& val) {
    func_in_7448(val);
    const int x = 7448;
    std::cout << 7448;
    for (int i = 0; i < 7448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7448(int& val) {
    const int x = 7448;
    std::cout << 7448;
    for (int i = 0; i < 7448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7449(int& val) {
    func_in_7449(val);
    const int x = 7449;
    std::cout << 7449;
    for (int i = 0; i < 7449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7449(int& val) {
    const int x = 7449;
    std::cout << 7449;
    for (int i = 0; i < 7449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7450(int& val) {
    func_in_7450(val);
    const int x = 7450;
    std::cout << 7450;
    for (int i = 0; i < 7450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7450(int& val) {
    const int x = 7450;
    std::cout << 7450;
    for (int i = 0; i < 7450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7451(int& val) {
    func_in_7451(val);
    const int x = 7451;
    std::cout << 7451;
    for (int i = 0; i < 7451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7451(int& val) {
    const int x = 7451;
    std::cout << 7451;
    for (int i = 0; i < 7451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7452(int& val) {
    func_in_7452(val);
    const int x = 7452;
    std::cout << 7452;
    for (int i = 0; i < 7452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7452(int& val) {
    const int x = 7452;
    std::cout << 7452;
    for (int i = 0; i < 7452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7453(int& val) {
    func_in_7453(val);
    const int x = 7453;
    std::cout << 7453;
    for (int i = 0; i < 7453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7453(int& val) {
    const int x = 7453;
    std::cout << 7453;
    for (int i = 0; i < 7453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7454(int& val) {
    func_in_7454(val);
    const int x = 7454;
    std::cout << 7454;
    for (int i = 0; i < 7454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7454(int& val) {
    const int x = 7454;
    std::cout << 7454;
    for (int i = 0; i < 7454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7455(int& val) {
    func_in_7455(val);
    const int x = 7455;
    std::cout << 7455;
    for (int i = 0; i < 7455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7455(int& val) {
    const int x = 7455;
    std::cout << 7455;
    for (int i = 0; i < 7455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7456(int& val) {
    func_in_7456(val);
    const int x = 7456;
    std::cout << 7456;
    for (int i = 0; i < 7456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7456(int& val) {
    const int x = 7456;
    std::cout << 7456;
    for (int i = 0; i < 7456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7457(int& val) {
    func_in_7457(val);
    const int x = 7457;
    std::cout << 7457;
    for (int i = 0; i < 7457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7457(int& val) {
    const int x = 7457;
    std::cout << 7457;
    for (int i = 0; i < 7457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7458(int& val) {
    func_in_7458(val);
    const int x = 7458;
    std::cout << 7458;
    for (int i = 0; i < 7458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7458(int& val) {
    const int x = 7458;
    std::cout << 7458;
    for (int i = 0; i < 7458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7459(int& val) {
    func_in_7459(val);
    const int x = 7459;
    std::cout << 7459;
    for (int i = 0; i < 7459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7459(int& val) {
    const int x = 7459;
    std::cout << 7459;
    for (int i = 0; i < 7459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7460(int& val) {
    func_in_7460(val);
    const int x = 7460;
    std::cout << 7460;
    for (int i = 0; i < 7460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7460(int& val) {
    const int x = 7460;
    std::cout << 7460;
    for (int i = 0; i < 7460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7461(int& val) {
    func_in_7461(val);
    const int x = 7461;
    std::cout << 7461;
    for (int i = 0; i < 7461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7461(int& val) {
    const int x = 7461;
    std::cout << 7461;
    for (int i = 0; i < 7461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7462(int& val) {
    func_in_7462(val);
    const int x = 7462;
    std::cout << 7462;
    for (int i = 0; i < 7462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7462(int& val) {
    const int x = 7462;
    std::cout << 7462;
    for (int i = 0; i < 7462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7463(int& val) {
    func_in_7463(val);
    const int x = 7463;
    std::cout << 7463;
    for (int i = 0; i < 7463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7463(int& val) {
    const int x = 7463;
    std::cout << 7463;
    for (int i = 0; i < 7463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7464(int& val) {
    func_in_7464(val);
    const int x = 7464;
    std::cout << 7464;
    for (int i = 0; i < 7464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7464(int& val) {
    const int x = 7464;
    std::cout << 7464;
    for (int i = 0; i < 7464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7465(int& val) {
    func_in_7465(val);
    const int x = 7465;
    std::cout << 7465;
    for (int i = 0; i < 7465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7465(int& val) {
    const int x = 7465;
    std::cout << 7465;
    for (int i = 0; i < 7465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7466(int& val) {
    func_in_7466(val);
    const int x = 7466;
    std::cout << 7466;
    for (int i = 0; i < 7466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7466(int& val) {
    const int x = 7466;
    std::cout << 7466;
    for (int i = 0; i < 7466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7467(int& val) {
    func_in_7467(val);
    const int x = 7467;
    std::cout << 7467;
    for (int i = 0; i < 7467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7467(int& val) {
    const int x = 7467;
    std::cout << 7467;
    for (int i = 0; i < 7467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7468(int& val) {
    func_in_7468(val);
    const int x = 7468;
    std::cout << 7468;
    for (int i = 0; i < 7468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7468(int& val) {
    const int x = 7468;
    std::cout << 7468;
    for (int i = 0; i < 7468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7469(int& val) {
    func_in_7469(val);
    const int x = 7469;
    std::cout << 7469;
    for (int i = 0; i < 7469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7469(int& val) {
    const int x = 7469;
    std::cout << 7469;
    for (int i = 0; i < 7469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7470(int& val) {
    func_in_7470(val);
    const int x = 7470;
    std::cout << 7470;
    for (int i = 0; i < 7470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7470(int& val) {
    const int x = 7470;
    std::cout << 7470;
    for (int i = 0; i < 7470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7471(int& val) {
    func_in_7471(val);
    const int x = 7471;
    std::cout << 7471;
    for (int i = 0; i < 7471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7471(int& val) {
    const int x = 7471;
    std::cout << 7471;
    for (int i = 0; i < 7471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7472(int& val) {
    func_in_7472(val);
    const int x = 7472;
    std::cout << 7472;
    for (int i = 0; i < 7472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7472(int& val) {
    const int x = 7472;
    std::cout << 7472;
    for (int i = 0; i < 7472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7473(int& val) {
    func_in_7473(val);
    const int x = 7473;
    std::cout << 7473;
    for (int i = 0; i < 7473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7473(int& val) {
    const int x = 7473;
    std::cout << 7473;
    for (int i = 0; i < 7473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7474(int& val) {
    func_in_7474(val);
    const int x = 7474;
    std::cout << 7474;
    for (int i = 0; i < 7474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7474(int& val) {
    const int x = 7474;
    std::cout << 7474;
    for (int i = 0; i < 7474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7475(int& val) {
    func_in_7475(val);
    const int x = 7475;
    std::cout << 7475;
    for (int i = 0; i < 7475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7475(int& val) {
    const int x = 7475;
    std::cout << 7475;
    for (int i = 0; i < 7475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7476(int& val) {
    func_in_7476(val);
    const int x = 7476;
    std::cout << 7476;
    for (int i = 0; i < 7476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7476(int& val) {
    const int x = 7476;
    std::cout << 7476;
    for (int i = 0; i < 7476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7477(int& val) {
    func_in_7477(val);
    const int x = 7477;
    std::cout << 7477;
    for (int i = 0; i < 7477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7477(int& val) {
    const int x = 7477;
    std::cout << 7477;
    for (int i = 0; i < 7477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7478(int& val) {
    func_in_7478(val);
    const int x = 7478;
    std::cout << 7478;
    for (int i = 0; i < 7478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7478(int& val) {
    const int x = 7478;
    std::cout << 7478;
    for (int i = 0; i < 7478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7479(int& val) {
    func_in_7479(val);
    const int x = 7479;
    std::cout << 7479;
    for (int i = 0; i < 7479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7479(int& val) {
    const int x = 7479;
    std::cout << 7479;
    for (int i = 0; i < 7479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7480(int& val) {
    func_in_7480(val);
    const int x = 7480;
    std::cout << 7480;
    for (int i = 0; i < 7480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7480(int& val) {
    const int x = 7480;
    std::cout << 7480;
    for (int i = 0; i < 7480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7481(int& val) {
    func_in_7481(val);
    const int x = 7481;
    std::cout << 7481;
    for (int i = 0; i < 7481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7481(int& val) {
    const int x = 7481;
    std::cout << 7481;
    for (int i = 0; i < 7481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7482(int& val) {
    func_in_7482(val);
    const int x = 7482;
    std::cout << 7482;
    for (int i = 0; i < 7482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7482(int& val) {
    const int x = 7482;
    std::cout << 7482;
    for (int i = 0; i < 7482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7483(int& val) {
    func_in_7483(val);
    const int x = 7483;
    std::cout << 7483;
    for (int i = 0; i < 7483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7483(int& val) {
    const int x = 7483;
    std::cout << 7483;
    for (int i = 0; i < 7483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7484(int& val) {
    func_in_7484(val);
    const int x = 7484;
    std::cout << 7484;
    for (int i = 0; i < 7484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7484(int& val) {
    const int x = 7484;
    std::cout << 7484;
    for (int i = 0; i < 7484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7485(int& val) {
    func_in_7485(val);
    const int x = 7485;
    std::cout << 7485;
    for (int i = 0; i < 7485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7485(int& val) {
    const int x = 7485;
    std::cout << 7485;
    for (int i = 0; i < 7485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7486(int& val) {
    func_in_7486(val);
    const int x = 7486;
    std::cout << 7486;
    for (int i = 0; i < 7486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7486(int& val) {
    const int x = 7486;
    std::cout << 7486;
    for (int i = 0; i < 7486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7487(int& val) {
    func_in_7487(val);
    const int x = 7487;
    std::cout << 7487;
    for (int i = 0; i < 7487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7487(int& val) {
    const int x = 7487;
    std::cout << 7487;
    for (int i = 0; i < 7487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7488(int& val) {
    func_in_7488(val);
    const int x = 7488;
    std::cout << 7488;
    for (int i = 0; i < 7488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7488(int& val) {
    const int x = 7488;
    std::cout << 7488;
    for (int i = 0; i < 7488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7489(int& val) {
    func_in_7489(val);
    const int x = 7489;
    std::cout << 7489;
    for (int i = 0; i < 7489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7489(int& val) {
    const int x = 7489;
    std::cout << 7489;
    for (int i = 0; i < 7489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7490(int& val) {
    func_in_7490(val);
    const int x = 7490;
    std::cout << 7490;
    for (int i = 0; i < 7490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7490(int& val) {
    const int x = 7490;
    std::cout << 7490;
    for (int i = 0; i < 7490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7491(int& val) {
    func_in_7491(val);
    const int x = 7491;
    std::cout << 7491;
    for (int i = 0; i < 7491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7491(int& val) {
    const int x = 7491;
    std::cout << 7491;
    for (int i = 0; i < 7491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7492(int& val) {
    func_in_7492(val);
    const int x = 7492;
    std::cout << 7492;
    for (int i = 0; i < 7492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7492(int& val) {
    const int x = 7492;
    std::cout << 7492;
    for (int i = 0; i < 7492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7493(int& val) {
    func_in_7493(val);
    const int x = 7493;
    std::cout << 7493;
    for (int i = 0; i < 7493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7493(int& val) {
    const int x = 7493;
    std::cout << 7493;
    for (int i = 0; i < 7493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7494(int& val) {
    func_in_7494(val);
    const int x = 7494;
    std::cout << 7494;
    for (int i = 0; i < 7494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7494(int& val) {
    const int x = 7494;
    std::cout << 7494;
    for (int i = 0; i < 7494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7495(int& val) {
    func_in_7495(val);
    const int x = 7495;
    std::cout << 7495;
    for (int i = 0; i < 7495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7495(int& val) {
    const int x = 7495;
    std::cout << 7495;
    for (int i = 0; i < 7495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7496(int& val) {
    func_in_7496(val);
    const int x = 7496;
    std::cout << 7496;
    for (int i = 0; i < 7496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7496(int& val) {
    const int x = 7496;
    std::cout << 7496;
    for (int i = 0; i < 7496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7497(int& val) {
    func_in_7497(val);
    const int x = 7497;
    std::cout << 7497;
    for (int i = 0; i < 7497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7497(int& val) {
    const int x = 7497;
    std::cout << 7497;
    for (int i = 0; i < 7497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7498(int& val) {
    func_in_7498(val);
    const int x = 7498;
    std::cout << 7498;
    for (int i = 0; i < 7498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7498(int& val) {
    const int x = 7498;
    std::cout << 7498;
    for (int i = 0; i < 7498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7499(int& val) {
    func_in_7499(val);
    const int x = 7499;
    std::cout << 7499;
    for (int i = 0; i < 7499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7499(int& val) {
    const int x = 7499;
    std::cout << 7499;
    for (int i = 0; i < 7499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7500(int& val) {
    func_in_7500(val);
    const int x = 7500;
    std::cout << 7500;
    for (int i = 0; i < 7500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7500(int& val) {
    const int x = 7500;
    std::cout << 7500;
    for (int i = 0; i < 7500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7501(int& val) {
    func_in_7501(val);
    const int x = 7501;
    std::cout << 7501;
    for (int i = 0; i < 7501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7501(int& val) {
    const int x = 7501;
    std::cout << 7501;
    for (int i = 0; i < 7501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7502(int& val) {
    func_in_7502(val);
    const int x = 7502;
    std::cout << 7502;
    for (int i = 0; i < 7502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7502(int& val) {
    const int x = 7502;
    std::cout << 7502;
    for (int i = 0; i < 7502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7503(int& val) {
    func_in_7503(val);
    const int x = 7503;
    std::cout << 7503;
    for (int i = 0; i < 7503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7503(int& val) {
    const int x = 7503;
    std::cout << 7503;
    for (int i = 0; i < 7503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7504(int& val) {
    func_in_7504(val);
    const int x = 7504;
    std::cout << 7504;
    for (int i = 0; i < 7504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7504(int& val) {
    const int x = 7504;
    std::cout << 7504;
    for (int i = 0; i < 7504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7505(int& val) {
    func_in_7505(val);
    const int x = 7505;
    std::cout << 7505;
    for (int i = 0; i < 7505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7505(int& val) {
    const int x = 7505;
    std::cout << 7505;
    for (int i = 0; i < 7505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7506(int& val) {
    func_in_7506(val);
    const int x = 7506;
    std::cout << 7506;
    for (int i = 0; i < 7506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7506(int& val) {
    const int x = 7506;
    std::cout << 7506;
    for (int i = 0; i < 7506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7507(int& val) {
    func_in_7507(val);
    const int x = 7507;
    std::cout << 7507;
    for (int i = 0; i < 7507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7507(int& val) {
    const int x = 7507;
    std::cout << 7507;
    for (int i = 0; i < 7507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7508(int& val) {
    func_in_7508(val);
    const int x = 7508;
    std::cout << 7508;
    for (int i = 0; i < 7508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7508(int& val) {
    const int x = 7508;
    std::cout << 7508;
    for (int i = 0; i < 7508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7509(int& val) {
    func_in_7509(val);
    const int x = 7509;
    std::cout << 7509;
    for (int i = 0; i < 7509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7509(int& val) {
    const int x = 7509;
    std::cout << 7509;
    for (int i = 0; i < 7509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7510(int& val) {
    func_in_7510(val);
    const int x = 7510;
    std::cout << 7510;
    for (int i = 0; i < 7510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7510(int& val) {
    const int x = 7510;
    std::cout << 7510;
    for (int i = 0; i < 7510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7511(int& val) {
    func_in_7511(val);
    const int x = 7511;
    std::cout << 7511;
    for (int i = 0; i < 7511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7511(int& val) {
    const int x = 7511;
    std::cout << 7511;
    for (int i = 0; i < 7511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7512(int& val) {
    func_in_7512(val);
    const int x = 7512;
    std::cout << 7512;
    for (int i = 0; i < 7512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7512(int& val) {
    const int x = 7512;
    std::cout << 7512;
    for (int i = 0; i < 7512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7513(int& val) {
    func_in_7513(val);
    const int x = 7513;
    std::cout << 7513;
    for (int i = 0; i < 7513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7513(int& val) {
    const int x = 7513;
    std::cout << 7513;
    for (int i = 0; i < 7513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7514(int& val) {
    func_in_7514(val);
    const int x = 7514;
    std::cout << 7514;
    for (int i = 0; i < 7514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7514(int& val) {
    const int x = 7514;
    std::cout << 7514;
    for (int i = 0; i < 7514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7515(int& val) {
    func_in_7515(val);
    const int x = 7515;
    std::cout << 7515;
    for (int i = 0; i < 7515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7515(int& val) {
    const int x = 7515;
    std::cout << 7515;
    for (int i = 0; i < 7515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7516(int& val) {
    func_in_7516(val);
    const int x = 7516;
    std::cout << 7516;
    for (int i = 0; i < 7516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7516(int& val) {
    const int x = 7516;
    std::cout << 7516;
    for (int i = 0; i < 7516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7517(int& val) {
    func_in_7517(val);
    const int x = 7517;
    std::cout << 7517;
    for (int i = 0; i < 7517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7517(int& val) {
    const int x = 7517;
    std::cout << 7517;
    for (int i = 0; i < 7517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7518(int& val) {
    func_in_7518(val);
    const int x = 7518;
    std::cout << 7518;
    for (int i = 0; i < 7518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7518(int& val) {
    const int x = 7518;
    std::cout << 7518;
    for (int i = 0; i < 7518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7519(int& val) {
    func_in_7519(val);
    const int x = 7519;
    std::cout << 7519;
    for (int i = 0; i < 7519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7519(int& val) {
    const int x = 7519;
    std::cout << 7519;
    for (int i = 0; i < 7519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7520(int& val) {
    func_in_7520(val);
    const int x = 7520;
    std::cout << 7520;
    for (int i = 0; i < 7520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7520(int& val) {
    const int x = 7520;
    std::cout << 7520;
    for (int i = 0; i < 7520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7521(int& val) {
    func_in_7521(val);
    const int x = 7521;
    std::cout << 7521;
    for (int i = 0; i < 7521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7521(int& val) {
    const int x = 7521;
    std::cout << 7521;
    for (int i = 0; i < 7521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7522(int& val) {
    func_in_7522(val);
    const int x = 7522;
    std::cout << 7522;
    for (int i = 0; i < 7522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7522(int& val) {
    const int x = 7522;
    std::cout << 7522;
    for (int i = 0; i < 7522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7523(int& val) {
    func_in_7523(val);
    const int x = 7523;
    std::cout << 7523;
    for (int i = 0; i < 7523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7523(int& val) {
    const int x = 7523;
    std::cout << 7523;
    for (int i = 0; i < 7523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7524(int& val) {
    func_in_7524(val);
    const int x = 7524;
    std::cout << 7524;
    for (int i = 0; i < 7524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7524(int& val) {
    const int x = 7524;
    std::cout << 7524;
    for (int i = 0; i < 7524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7525(int& val) {
    func_in_7525(val);
    const int x = 7525;
    std::cout << 7525;
    for (int i = 0; i < 7525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7525(int& val) {
    const int x = 7525;
    std::cout << 7525;
    for (int i = 0; i < 7525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7526(int& val) {
    func_in_7526(val);
    const int x = 7526;
    std::cout << 7526;
    for (int i = 0; i < 7526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7526(int& val) {
    const int x = 7526;
    std::cout << 7526;
    for (int i = 0; i < 7526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7527(int& val) {
    func_in_7527(val);
    const int x = 7527;
    std::cout << 7527;
    for (int i = 0; i < 7527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7527(int& val) {
    const int x = 7527;
    std::cout << 7527;
    for (int i = 0; i < 7527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7528(int& val) {
    func_in_7528(val);
    const int x = 7528;
    std::cout << 7528;
    for (int i = 0; i < 7528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7528(int& val) {
    const int x = 7528;
    std::cout << 7528;
    for (int i = 0; i < 7528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7529(int& val) {
    func_in_7529(val);
    const int x = 7529;
    std::cout << 7529;
    for (int i = 0; i < 7529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7529(int& val) {
    const int x = 7529;
    std::cout << 7529;
    for (int i = 0; i < 7529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7530(int& val) {
    func_in_7530(val);
    const int x = 7530;
    std::cout << 7530;
    for (int i = 0; i < 7530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7530(int& val) {
    const int x = 7530;
    std::cout << 7530;
    for (int i = 0; i < 7530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7531(int& val) {
    func_in_7531(val);
    const int x = 7531;
    std::cout << 7531;
    for (int i = 0; i < 7531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7531(int& val) {
    const int x = 7531;
    std::cout << 7531;
    for (int i = 0; i < 7531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7532(int& val) {
    func_in_7532(val);
    const int x = 7532;
    std::cout << 7532;
    for (int i = 0; i < 7532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7532(int& val) {
    const int x = 7532;
    std::cout << 7532;
    for (int i = 0; i < 7532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7533(int& val) {
    func_in_7533(val);
    const int x = 7533;
    std::cout << 7533;
    for (int i = 0; i < 7533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7533(int& val) {
    const int x = 7533;
    std::cout << 7533;
    for (int i = 0; i < 7533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7534(int& val) {
    func_in_7534(val);
    const int x = 7534;
    std::cout << 7534;
    for (int i = 0; i < 7534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7534(int& val) {
    const int x = 7534;
    std::cout << 7534;
    for (int i = 0; i < 7534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7535(int& val) {
    func_in_7535(val);
    const int x = 7535;
    std::cout << 7535;
    for (int i = 0; i < 7535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7535(int& val) {
    const int x = 7535;
    std::cout << 7535;
    for (int i = 0; i < 7535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7536(int& val) {
    func_in_7536(val);
    const int x = 7536;
    std::cout << 7536;
    for (int i = 0; i < 7536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7536(int& val) {
    const int x = 7536;
    std::cout << 7536;
    for (int i = 0; i < 7536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7537(int& val) {
    func_in_7537(val);
    const int x = 7537;
    std::cout << 7537;
    for (int i = 0; i < 7537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7537(int& val) {
    const int x = 7537;
    std::cout << 7537;
    for (int i = 0; i < 7537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7538(int& val) {
    func_in_7538(val);
    const int x = 7538;
    std::cout << 7538;
    for (int i = 0; i < 7538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7538(int& val) {
    const int x = 7538;
    std::cout << 7538;
    for (int i = 0; i < 7538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7539(int& val) {
    func_in_7539(val);
    const int x = 7539;
    std::cout << 7539;
    for (int i = 0; i < 7539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7539(int& val) {
    const int x = 7539;
    std::cout << 7539;
    for (int i = 0; i < 7539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7540(int& val) {
    func_in_7540(val);
    const int x = 7540;
    std::cout << 7540;
    for (int i = 0; i < 7540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7540(int& val) {
    const int x = 7540;
    std::cout << 7540;
    for (int i = 0; i < 7540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7541(int& val) {
    func_in_7541(val);
    const int x = 7541;
    std::cout << 7541;
    for (int i = 0; i < 7541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7541(int& val) {
    const int x = 7541;
    std::cout << 7541;
    for (int i = 0; i < 7541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7542(int& val) {
    func_in_7542(val);
    const int x = 7542;
    std::cout << 7542;
    for (int i = 0; i < 7542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7542(int& val) {
    const int x = 7542;
    std::cout << 7542;
    for (int i = 0; i < 7542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7543(int& val) {
    func_in_7543(val);
    const int x = 7543;
    std::cout << 7543;
    for (int i = 0; i < 7543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7543(int& val) {
    const int x = 7543;
    std::cout << 7543;
    for (int i = 0; i < 7543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7544(int& val) {
    func_in_7544(val);
    const int x = 7544;
    std::cout << 7544;
    for (int i = 0; i < 7544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7544(int& val) {
    const int x = 7544;
    std::cout << 7544;
    for (int i = 0; i < 7544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7545(int& val) {
    func_in_7545(val);
    const int x = 7545;
    std::cout << 7545;
    for (int i = 0; i < 7545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7545(int& val) {
    const int x = 7545;
    std::cout << 7545;
    for (int i = 0; i < 7545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7546(int& val) {
    func_in_7546(val);
    const int x = 7546;
    std::cout << 7546;
    for (int i = 0; i < 7546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7546(int& val) {
    const int x = 7546;
    std::cout << 7546;
    for (int i = 0; i < 7546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7547(int& val) {
    func_in_7547(val);
    const int x = 7547;
    std::cout << 7547;
    for (int i = 0; i < 7547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7547(int& val) {
    const int x = 7547;
    std::cout << 7547;
    for (int i = 0; i < 7547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7548(int& val) {
    func_in_7548(val);
    const int x = 7548;
    std::cout << 7548;
    for (int i = 0; i < 7548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7548(int& val) {
    const int x = 7548;
    std::cout << 7548;
    for (int i = 0; i < 7548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7549(int& val) {
    func_in_7549(val);
    const int x = 7549;
    std::cout << 7549;
    for (int i = 0; i < 7549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7549(int& val) {
    const int x = 7549;
    std::cout << 7549;
    for (int i = 0; i < 7549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7550(int& val) {
    func_in_7550(val);
    const int x = 7550;
    std::cout << 7550;
    for (int i = 0; i < 7550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7550(int& val) {
    const int x = 7550;
    std::cout << 7550;
    for (int i = 0; i < 7550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7551(int& val) {
    func_in_7551(val);
    const int x = 7551;
    std::cout << 7551;
    for (int i = 0; i < 7551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7551(int& val) {
    const int x = 7551;
    std::cout << 7551;
    for (int i = 0; i < 7551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7552(int& val) {
    func_in_7552(val);
    const int x = 7552;
    std::cout << 7552;
    for (int i = 0; i < 7552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7552(int& val) {
    const int x = 7552;
    std::cout << 7552;
    for (int i = 0; i < 7552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7553(int& val) {
    func_in_7553(val);
    const int x = 7553;
    std::cout << 7553;
    for (int i = 0; i < 7553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7553(int& val) {
    const int x = 7553;
    std::cout << 7553;
    for (int i = 0; i < 7553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7554(int& val) {
    func_in_7554(val);
    const int x = 7554;
    std::cout << 7554;
    for (int i = 0; i < 7554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7554(int& val) {
    const int x = 7554;
    std::cout << 7554;
    for (int i = 0; i < 7554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7555(int& val) {
    func_in_7555(val);
    const int x = 7555;
    std::cout << 7555;
    for (int i = 0; i < 7555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7555(int& val) {
    const int x = 7555;
    std::cout << 7555;
    for (int i = 0; i < 7555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7556(int& val) {
    func_in_7556(val);
    const int x = 7556;
    std::cout << 7556;
    for (int i = 0; i < 7556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7556(int& val) {
    const int x = 7556;
    std::cout << 7556;
    for (int i = 0; i < 7556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7557(int& val) {
    func_in_7557(val);
    const int x = 7557;
    std::cout << 7557;
    for (int i = 0; i < 7557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7557(int& val) {
    const int x = 7557;
    std::cout << 7557;
    for (int i = 0; i < 7557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7558(int& val) {
    func_in_7558(val);
    const int x = 7558;
    std::cout << 7558;
    for (int i = 0; i < 7558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7558(int& val) {
    const int x = 7558;
    std::cout << 7558;
    for (int i = 0; i < 7558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7559(int& val) {
    func_in_7559(val);
    const int x = 7559;
    std::cout << 7559;
    for (int i = 0; i < 7559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7559(int& val) {
    const int x = 7559;
    std::cout << 7559;
    for (int i = 0; i < 7559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7560(int& val) {
    func_in_7560(val);
    const int x = 7560;
    std::cout << 7560;
    for (int i = 0; i < 7560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7560(int& val) {
    const int x = 7560;
    std::cout << 7560;
    for (int i = 0; i < 7560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7561(int& val) {
    func_in_7561(val);
    const int x = 7561;
    std::cout << 7561;
    for (int i = 0; i < 7561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7561(int& val) {
    const int x = 7561;
    std::cout << 7561;
    for (int i = 0; i < 7561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7562(int& val) {
    func_in_7562(val);
    const int x = 7562;
    std::cout << 7562;
    for (int i = 0; i < 7562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7562(int& val) {
    const int x = 7562;
    std::cout << 7562;
    for (int i = 0; i < 7562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7563(int& val) {
    func_in_7563(val);
    const int x = 7563;
    std::cout << 7563;
    for (int i = 0; i < 7563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7563(int& val) {
    const int x = 7563;
    std::cout << 7563;
    for (int i = 0; i < 7563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7564(int& val) {
    func_in_7564(val);
    const int x = 7564;
    std::cout << 7564;
    for (int i = 0; i < 7564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7564(int& val) {
    const int x = 7564;
    std::cout << 7564;
    for (int i = 0; i < 7564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7565(int& val) {
    func_in_7565(val);
    const int x = 7565;
    std::cout << 7565;
    for (int i = 0; i < 7565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7565(int& val) {
    const int x = 7565;
    std::cout << 7565;
    for (int i = 0; i < 7565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7566(int& val) {
    func_in_7566(val);
    const int x = 7566;
    std::cout << 7566;
    for (int i = 0; i < 7566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7566(int& val) {
    const int x = 7566;
    std::cout << 7566;
    for (int i = 0; i < 7566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7567(int& val) {
    func_in_7567(val);
    const int x = 7567;
    std::cout << 7567;
    for (int i = 0; i < 7567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7567(int& val) {
    const int x = 7567;
    std::cout << 7567;
    for (int i = 0; i < 7567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7568(int& val) {
    func_in_7568(val);
    const int x = 7568;
    std::cout << 7568;
    for (int i = 0; i < 7568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7568(int& val) {
    const int x = 7568;
    std::cout << 7568;
    for (int i = 0; i < 7568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7569(int& val) {
    func_in_7569(val);
    const int x = 7569;
    std::cout << 7569;
    for (int i = 0; i < 7569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7569(int& val) {
    const int x = 7569;
    std::cout << 7569;
    for (int i = 0; i < 7569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7570(int& val) {
    func_in_7570(val);
    const int x = 7570;
    std::cout << 7570;
    for (int i = 0; i < 7570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7570(int& val) {
    const int x = 7570;
    std::cout << 7570;
    for (int i = 0; i < 7570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7571(int& val) {
    func_in_7571(val);
    const int x = 7571;
    std::cout << 7571;
    for (int i = 0; i < 7571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7571(int& val) {
    const int x = 7571;
    std::cout << 7571;
    for (int i = 0; i < 7571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7572(int& val) {
    func_in_7572(val);
    const int x = 7572;
    std::cout << 7572;
    for (int i = 0; i < 7572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7572(int& val) {
    const int x = 7572;
    std::cout << 7572;
    for (int i = 0; i < 7572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7573(int& val) {
    func_in_7573(val);
    const int x = 7573;
    std::cout << 7573;
    for (int i = 0; i < 7573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7573(int& val) {
    const int x = 7573;
    std::cout << 7573;
    for (int i = 0; i < 7573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7574(int& val) {
    func_in_7574(val);
    const int x = 7574;
    std::cout << 7574;
    for (int i = 0; i < 7574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7574(int& val) {
    const int x = 7574;
    std::cout << 7574;
    for (int i = 0; i < 7574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7575(int& val) {
    func_in_7575(val);
    const int x = 7575;
    std::cout << 7575;
    for (int i = 0; i < 7575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7575(int& val) {
    const int x = 7575;
    std::cout << 7575;
    for (int i = 0; i < 7575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7576(int& val) {
    func_in_7576(val);
    const int x = 7576;
    std::cout << 7576;
    for (int i = 0; i < 7576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7576(int& val) {
    const int x = 7576;
    std::cout << 7576;
    for (int i = 0; i < 7576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7577(int& val) {
    func_in_7577(val);
    const int x = 7577;
    std::cout << 7577;
    for (int i = 0; i < 7577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7577(int& val) {
    const int x = 7577;
    std::cout << 7577;
    for (int i = 0; i < 7577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7578(int& val) {
    func_in_7578(val);
    const int x = 7578;
    std::cout << 7578;
    for (int i = 0; i < 7578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7578(int& val) {
    const int x = 7578;
    std::cout << 7578;
    for (int i = 0; i < 7578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7579(int& val) {
    func_in_7579(val);
    const int x = 7579;
    std::cout << 7579;
    for (int i = 0; i < 7579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7579(int& val) {
    const int x = 7579;
    std::cout << 7579;
    for (int i = 0; i < 7579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7580(int& val) {
    func_in_7580(val);
    const int x = 7580;
    std::cout << 7580;
    for (int i = 0; i < 7580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7580(int& val) {
    const int x = 7580;
    std::cout << 7580;
    for (int i = 0; i < 7580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7581(int& val) {
    func_in_7581(val);
    const int x = 7581;
    std::cout << 7581;
    for (int i = 0; i < 7581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7581(int& val) {
    const int x = 7581;
    std::cout << 7581;
    for (int i = 0; i < 7581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7582(int& val) {
    func_in_7582(val);
    const int x = 7582;
    std::cout << 7582;
    for (int i = 0; i < 7582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7582(int& val) {
    const int x = 7582;
    std::cout << 7582;
    for (int i = 0; i < 7582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7583(int& val) {
    func_in_7583(val);
    const int x = 7583;
    std::cout << 7583;
    for (int i = 0; i < 7583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7583(int& val) {
    const int x = 7583;
    std::cout << 7583;
    for (int i = 0; i < 7583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7584(int& val) {
    func_in_7584(val);
    const int x = 7584;
    std::cout << 7584;
    for (int i = 0; i < 7584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7584(int& val) {
    const int x = 7584;
    std::cout << 7584;
    for (int i = 0; i < 7584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7585(int& val) {
    func_in_7585(val);
    const int x = 7585;
    std::cout << 7585;
    for (int i = 0; i < 7585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7585(int& val) {
    const int x = 7585;
    std::cout << 7585;
    for (int i = 0; i < 7585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7586(int& val) {
    func_in_7586(val);
    const int x = 7586;
    std::cout << 7586;
    for (int i = 0; i < 7586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7586(int& val) {
    const int x = 7586;
    std::cout << 7586;
    for (int i = 0; i < 7586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7587(int& val) {
    func_in_7587(val);
    const int x = 7587;
    std::cout << 7587;
    for (int i = 0; i < 7587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7587(int& val) {
    const int x = 7587;
    std::cout << 7587;
    for (int i = 0; i < 7587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7588(int& val) {
    func_in_7588(val);
    const int x = 7588;
    std::cout << 7588;
    for (int i = 0; i < 7588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7588(int& val) {
    const int x = 7588;
    std::cout << 7588;
    for (int i = 0; i < 7588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7589(int& val) {
    func_in_7589(val);
    const int x = 7589;
    std::cout << 7589;
    for (int i = 0; i < 7589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7589(int& val) {
    const int x = 7589;
    std::cout << 7589;
    for (int i = 0; i < 7589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7590(int& val) {
    func_in_7590(val);
    const int x = 7590;
    std::cout << 7590;
    for (int i = 0; i < 7590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7590(int& val) {
    const int x = 7590;
    std::cout << 7590;
    for (int i = 0; i < 7590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7591(int& val) {
    func_in_7591(val);
    const int x = 7591;
    std::cout << 7591;
    for (int i = 0; i < 7591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7591(int& val) {
    const int x = 7591;
    std::cout << 7591;
    for (int i = 0; i < 7591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7592(int& val) {
    func_in_7592(val);
    const int x = 7592;
    std::cout << 7592;
    for (int i = 0; i < 7592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7592(int& val) {
    const int x = 7592;
    std::cout << 7592;
    for (int i = 0; i < 7592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7593(int& val) {
    func_in_7593(val);
    const int x = 7593;
    std::cout << 7593;
    for (int i = 0; i < 7593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7593(int& val) {
    const int x = 7593;
    std::cout << 7593;
    for (int i = 0; i < 7593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7594(int& val) {
    func_in_7594(val);
    const int x = 7594;
    std::cout << 7594;
    for (int i = 0; i < 7594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7594(int& val) {
    const int x = 7594;
    std::cout << 7594;
    for (int i = 0; i < 7594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7595(int& val) {
    func_in_7595(val);
    const int x = 7595;
    std::cout << 7595;
    for (int i = 0; i < 7595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7595(int& val) {
    const int x = 7595;
    std::cout << 7595;
    for (int i = 0; i < 7595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7596(int& val) {
    func_in_7596(val);
    const int x = 7596;
    std::cout << 7596;
    for (int i = 0; i < 7596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7596(int& val) {
    const int x = 7596;
    std::cout << 7596;
    for (int i = 0; i < 7596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7597(int& val) {
    func_in_7597(val);
    const int x = 7597;
    std::cout << 7597;
    for (int i = 0; i < 7597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7597(int& val) {
    const int x = 7597;
    std::cout << 7597;
    for (int i = 0; i < 7597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7598(int& val) {
    func_in_7598(val);
    const int x = 7598;
    std::cout << 7598;
    for (int i = 0; i < 7598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7598(int& val) {
    const int x = 7598;
    std::cout << 7598;
    for (int i = 0; i < 7598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7599(int& val) {
    func_in_7599(val);
    const int x = 7599;
    std::cout << 7599;
    for (int i = 0; i < 7599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7599(int& val) {
    const int x = 7599;
    std::cout << 7599;
    for (int i = 0; i < 7599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7600(int& val) {
    func_in_7600(val);
    const int x = 7600;
    std::cout << 7600;
    for (int i = 0; i < 7600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7600(int& val) {
    const int x = 7600;
    std::cout << 7600;
    for (int i = 0; i < 7600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7601(int& val) {
    func_in_7601(val);
    const int x = 7601;
    std::cout << 7601;
    for (int i = 0; i < 7601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7601(int& val) {
    const int x = 7601;
    std::cout << 7601;
    for (int i = 0; i < 7601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7602(int& val) {
    func_in_7602(val);
    const int x = 7602;
    std::cout << 7602;
    for (int i = 0; i < 7602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7602(int& val) {
    const int x = 7602;
    std::cout << 7602;
    for (int i = 0; i < 7602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7603(int& val) {
    func_in_7603(val);
    const int x = 7603;
    std::cout << 7603;
    for (int i = 0; i < 7603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7603(int& val) {
    const int x = 7603;
    std::cout << 7603;
    for (int i = 0; i < 7603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7604(int& val) {
    func_in_7604(val);
    const int x = 7604;
    std::cout << 7604;
    for (int i = 0; i < 7604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7604(int& val) {
    const int x = 7604;
    std::cout << 7604;
    for (int i = 0; i < 7604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7605(int& val) {
    func_in_7605(val);
    const int x = 7605;
    std::cout << 7605;
    for (int i = 0; i < 7605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7605(int& val) {
    const int x = 7605;
    std::cout << 7605;
    for (int i = 0; i < 7605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7606(int& val) {
    func_in_7606(val);
    const int x = 7606;
    std::cout << 7606;
    for (int i = 0; i < 7606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7606(int& val) {
    const int x = 7606;
    std::cout << 7606;
    for (int i = 0; i < 7606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7607(int& val) {
    func_in_7607(val);
    const int x = 7607;
    std::cout << 7607;
    for (int i = 0; i < 7607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7607(int& val) {
    const int x = 7607;
    std::cout << 7607;
    for (int i = 0; i < 7607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7608(int& val) {
    func_in_7608(val);
    const int x = 7608;
    std::cout << 7608;
    for (int i = 0; i < 7608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7608(int& val) {
    const int x = 7608;
    std::cout << 7608;
    for (int i = 0; i < 7608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7609(int& val) {
    func_in_7609(val);
    const int x = 7609;
    std::cout << 7609;
    for (int i = 0; i < 7609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7609(int& val) {
    const int x = 7609;
    std::cout << 7609;
    for (int i = 0; i < 7609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7610(int& val) {
    func_in_7610(val);
    const int x = 7610;
    std::cout << 7610;
    for (int i = 0; i < 7610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7610(int& val) {
    const int x = 7610;
    std::cout << 7610;
    for (int i = 0; i < 7610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7611(int& val) {
    func_in_7611(val);
    const int x = 7611;
    std::cout << 7611;
    for (int i = 0; i < 7611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7611(int& val) {
    const int x = 7611;
    std::cout << 7611;
    for (int i = 0; i < 7611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7612(int& val) {
    func_in_7612(val);
    const int x = 7612;
    std::cout << 7612;
    for (int i = 0; i < 7612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7612(int& val) {
    const int x = 7612;
    std::cout << 7612;
    for (int i = 0; i < 7612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7613(int& val) {
    func_in_7613(val);
    const int x = 7613;
    std::cout << 7613;
    for (int i = 0; i < 7613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7613(int& val) {
    const int x = 7613;
    std::cout << 7613;
    for (int i = 0; i < 7613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7614(int& val) {
    func_in_7614(val);
    const int x = 7614;
    std::cout << 7614;
    for (int i = 0; i < 7614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7614(int& val) {
    const int x = 7614;
    std::cout << 7614;
    for (int i = 0; i < 7614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7615(int& val) {
    func_in_7615(val);
    const int x = 7615;
    std::cout << 7615;
    for (int i = 0; i < 7615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7615(int& val) {
    const int x = 7615;
    std::cout << 7615;
    for (int i = 0; i < 7615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7616(int& val) {
    func_in_7616(val);
    const int x = 7616;
    std::cout << 7616;
    for (int i = 0; i < 7616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7616(int& val) {
    const int x = 7616;
    std::cout << 7616;
    for (int i = 0; i < 7616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7617(int& val) {
    func_in_7617(val);
    const int x = 7617;
    std::cout << 7617;
    for (int i = 0; i < 7617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7617(int& val) {
    const int x = 7617;
    std::cout << 7617;
    for (int i = 0; i < 7617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7618(int& val) {
    func_in_7618(val);
    const int x = 7618;
    std::cout << 7618;
    for (int i = 0; i < 7618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7618(int& val) {
    const int x = 7618;
    std::cout << 7618;
    for (int i = 0; i < 7618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7619(int& val) {
    func_in_7619(val);
    const int x = 7619;
    std::cout << 7619;
    for (int i = 0; i < 7619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7619(int& val) {
    const int x = 7619;
    std::cout << 7619;
    for (int i = 0; i < 7619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7620(int& val) {
    func_in_7620(val);
    const int x = 7620;
    std::cout << 7620;
    for (int i = 0; i < 7620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7620(int& val) {
    const int x = 7620;
    std::cout << 7620;
    for (int i = 0; i < 7620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7621(int& val) {
    func_in_7621(val);
    const int x = 7621;
    std::cout << 7621;
    for (int i = 0; i < 7621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7621(int& val) {
    const int x = 7621;
    std::cout << 7621;
    for (int i = 0; i < 7621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7622(int& val) {
    func_in_7622(val);
    const int x = 7622;
    std::cout << 7622;
    for (int i = 0; i < 7622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7622(int& val) {
    const int x = 7622;
    std::cout << 7622;
    for (int i = 0; i < 7622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7623(int& val) {
    func_in_7623(val);
    const int x = 7623;
    std::cout << 7623;
    for (int i = 0; i < 7623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7623(int& val) {
    const int x = 7623;
    std::cout << 7623;
    for (int i = 0; i < 7623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7624(int& val) {
    func_in_7624(val);
    const int x = 7624;
    std::cout << 7624;
    for (int i = 0; i < 7624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7624(int& val) {
    const int x = 7624;
    std::cout << 7624;
    for (int i = 0; i < 7624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7625(int& val) {
    func_in_7625(val);
    const int x = 7625;
    std::cout << 7625;
    for (int i = 0; i < 7625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7625(int& val) {
    const int x = 7625;
    std::cout << 7625;
    for (int i = 0; i < 7625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7626(int& val) {
    func_in_7626(val);
    const int x = 7626;
    std::cout << 7626;
    for (int i = 0; i < 7626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7626(int& val) {
    const int x = 7626;
    std::cout << 7626;
    for (int i = 0; i < 7626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7627(int& val) {
    func_in_7627(val);
    const int x = 7627;
    std::cout << 7627;
    for (int i = 0; i < 7627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7627(int& val) {
    const int x = 7627;
    std::cout << 7627;
    for (int i = 0; i < 7627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7628(int& val) {
    func_in_7628(val);
    const int x = 7628;
    std::cout << 7628;
    for (int i = 0; i < 7628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7628(int& val) {
    const int x = 7628;
    std::cout << 7628;
    for (int i = 0; i < 7628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7629(int& val) {
    func_in_7629(val);
    const int x = 7629;
    std::cout << 7629;
    for (int i = 0; i < 7629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7629(int& val) {
    const int x = 7629;
    std::cout << 7629;
    for (int i = 0; i < 7629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7630(int& val) {
    func_in_7630(val);
    const int x = 7630;
    std::cout << 7630;
    for (int i = 0; i < 7630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7630(int& val) {
    const int x = 7630;
    std::cout << 7630;
    for (int i = 0; i < 7630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7631(int& val) {
    func_in_7631(val);
    const int x = 7631;
    std::cout << 7631;
    for (int i = 0; i < 7631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7631(int& val) {
    const int x = 7631;
    std::cout << 7631;
    for (int i = 0; i < 7631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7632(int& val) {
    func_in_7632(val);
    const int x = 7632;
    std::cout << 7632;
    for (int i = 0; i < 7632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7632(int& val) {
    const int x = 7632;
    std::cout << 7632;
    for (int i = 0; i < 7632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7633(int& val) {
    func_in_7633(val);
    const int x = 7633;
    std::cout << 7633;
    for (int i = 0; i < 7633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7633(int& val) {
    const int x = 7633;
    std::cout << 7633;
    for (int i = 0; i < 7633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7634(int& val) {
    func_in_7634(val);
    const int x = 7634;
    std::cout << 7634;
    for (int i = 0; i < 7634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7634(int& val) {
    const int x = 7634;
    std::cout << 7634;
    for (int i = 0; i < 7634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7635(int& val) {
    func_in_7635(val);
    const int x = 7635;
    std::cout << 7635;
    for (int i = 0; i < 7635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7635(int& val) {
    const int x = 7635;
    std::cout << 7635;
    for (int i = 0; i < 7635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7636(int& val) {
    func_in_7636(val);
    const int x = 7636;
    std::cout << 7636;
    for (int i = 0; i < 7636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7636(int& val) {
    const int x = 7636;
    std::cout << 7636;
    for (int i = 0; i < 7636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7637(int& val) {
    func_in_7637(val);
    const int x = 7637;
    std::cout << 7637;
    for (int i = 0; i < 7637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7637(int& val) {
    const int x = 7637;
    std::cout << 7637;
    for (int i = 0; i < 7637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7638(int& val) {
    func_in_7638(val);
    const int x = 7638;
    std::cout << 7638;
    for (int i = 0; i < 7638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7638(int& val) {
    const int x = 7638;
    std::cout << 7638;
    for (int i = 0; i < 7638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7639(int& val) {
    func_in_7639(val);
    const int x = 7639;
    std::cout << 7639;
    for (int i = 0; i < 7639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7639(int& val) {
    const int x = 7639;
    std::cout << 7639;
    for (int i = 0; i < 7639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7640(int& val) {
    func_in_7640(val);
    const int x = 7640;
    std::cout << 7640;
    for (int i = 0; i < 7640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7640(int& val) {
    const int x = 7640;
    std::cout << 7640;
    for (int i = 0; i < 7640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7641(int& val) {
    func_in_7641(val);
    const int x = 7641;
    std::cout << 7641;
    for (int i = 0; i < 7641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7641(int& val) {
    const int x = 7641;
    std::cout << 7641;
    for (int i = 0; i < 7641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7642(int& val) {
    func_in_7642(val);
    const int x = 7642;
    std::cout << 7642;
    for (int i = 0; i < 7642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7642(int& val) {
    const int x = 7642;
    std::cout << 7642;
    for (int i = 0; i < 7642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7643(int& val) {
    func_in_7643(val);
    const int x = 7643;
    std::cout << 7643;
    for (int i = 0; i < 7643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7643(int& val) {
    const int x = 7643;
    std::cout << 7643;
    for (int i = 0; i < 7643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7644(int& val) {
    func_in_7644(val);
    const int x = 7644;
    std::cout << 7644;
    for (int i = 0; i < 7644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7644(int& val) {
    const int x = 7644;
    std::cout << 7644;
    for (int i = 0; i < 7644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7645(int& val) {
    func_in_7645(val);
    const int x = 7645;
    std::cout << 7645;
    for (int i = 0; i < 7645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7645(int& val) {
    const int x = 7645;
    std::cout << 7645;
    for (int i = 0; i < 7645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7646(int& val) {
    func_in_7646(val);
    const int x = 7646;
    std::cout << 7646;
    for (int i = 0; i < 7646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7646(int& val) {
    const int x = 7646;
    std::cout << 7646;
    for (int i = 0; i < 7646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7647(int& val) {
    func_in_7647(val);
    const int x = 7647;
    std::cout << 7647;
    for (int i = 0; i < 7647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7647(int& val) {
    const int x = 7647;
    std::cout << 7647;
    for (int i = 0; i < 7647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7648(int& val) {
    func_in_7648(val);
    const int x = 7648;
    std::cout << 7648;
    for (int i = 0; i < 7648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7648(int& val) {
    const int x = 7648;
    std::cout << 7648;
    for (int i = 0; i < 7648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7649(int& val) {
    func_in_7649(val);
    const int x = 7649;
    std::cout << 7649;
    for (int i = 0; i < 7649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7649(int& val) {
    const int x = 7649;
    std::cout << 7649;
    for (int i = 0; i < 7649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7650(int& val) {
    func_in_7650(val);
    const int x = 7650;
    std::cout << 7650;
    for (int i = 0; i < 7650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7650(int& val) {
    const int x = 7650;
    std::cout << 7650;
    for (int i = 0; i < 7650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7651(int& val) {
    func_in_7651(val);
    const int x = 7651;
    std::cout << 7651;
    for (int i = 0; i < 7651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7651(int& val) {
    const int x = 7651;
    std::cout << 7651;
    for (int i = 0; i < 7651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7652(int& val) {
    func_in_7652(val);
    const int x = 7652;
    std::cout << 7652;
    for (int i = 0; i < 7652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7652(int& val) {
    const int x = 7652;
    std::cout << 7652;
    for (int i = 0; i < 7652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7653(int& val) {
    func_in_7653(val);
    const int x = 7653;
    std::cout << 7653;
    for (int i = 0; i < 7653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7653(int& val) {
    const int x = 7653;
    std::cout << 7653;
    for (int i = 0; i < 7653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7654(int& val) {
    func_in_7654(val);
    const int x = 7654;
    std::cout << 7654;
    for (int i = 0; i < 7654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7654(int& val) {
    const int x = 7654;
    std::cout << 7654;
    for (int i = 0; i < 7654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7655(int& val) {
    func_in_7655(val);
    const int x = 7655;
    std::cout << 7655;
    for (int i = 0; i < 7655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7655(int& val) {
    const int x = 7655;
    std::cout << 7655;
    for (int i = 0; i < 7655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7656(int& val) {
    func_in_7656(val);
    const int x = 7656;
    std::cout << 7656;
    for (int i = 0; i < 7656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7656(int& val) {
    const int x = 7656;
    std::cout << 7656;
    for (int i = 0; i < 7656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7657(int& val) {
    func_in_7657(val);
    const int x = 7657;
    std::cout << 7657;
    for (int i = 0; i < 7657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7657(int& val) {
    const int x = 7657;
    std::cout << 7657;
    for (int i = 0; i < 7657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7658(int& val) {
    func_in_7658(val);
    const int x = 7658;
    std::cout << 7658;
    for (int i = 0; i < 7658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7658(int& val) {
    const int x = 7658;
    std::cout << 7658;
    for (int i = 0; i < 7658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7659(int& val) {
    func_in_7659(val);
    const int x = 7659;
    std::cout << 7659;
    for (int i = 0; i < 7659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7659(int& val) {
    const int x = 7659;
    std::cout << 7659;
    for (int i = 0; i < 7659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7660(int& val) {
    func_in_7660(val);
    const int x = 7660;
    std::cout << 7660;
    for (int i = 0; i < 7660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7660(int& val) {
    const int x = 7660;
    std::cout << 7660;
    for (int i = 0; i < 7660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7661(int& val) {
    func_in_7661(val);
    const int x = 7661;
    std::cout << 7661;
    for (int i = 0; i < 7661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7661(int& val) {
    const int x = 7661;
    std::cout << 7661;
    for (int i = 0; i < 7661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7662(int& val) {
    func_in_7662(val);
    const int x = 7662;
    std::cout << 7662;
    for (int i = 0; i < 7662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7662(int& val) {
    const int x = 7662;
    std::cout << 7662;
    for (int i = 0; i < 7662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7663(int& val) {
    func_in_7663(val);
    const int x = 7663;
    std::cout << 7663;
    for (int i = 0; i < 7663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7663(int& val) {
    const int x = 7663;
    std::cout << 7663;
    for (int i = 0; i < 7663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7664(int& val) {
    func_in_7664(val);
    const int x = 7664;
    std::cout << 7664;
    for (int i = 0; i < 7664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7664(int& val) {
    const int x = 7664;
    std::cout << 7664;
    for (int i = 0; i < 7664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7665(int& val) {
    func_in_7665(val);
    const int x = 7665;
    std::cout << 7665;
    for (int i = 0; i < 7665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7665(int& val) {
    const int x = 7665;
    std::cout << 7665;
    for (int i = 0; i < 7665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7666(int& val) {
    func_in_7666(val);
    const int x = 7666;
    std::cout << 7666;
    for (int i = 0; i < 7666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7666(int& val) {
    const int x = 7666;
    std::cout << 7666;
    for (int i = 0; i < 7666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7667(int& val) {
    func_in_7667(val);
    const int x = 7667;
    std::cout << 7667;
    for (int i = 0; i < 7667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7667(int& val) {
    const int x = 7667;
    std::cout << 7667;
    for (int i = 0; i < 7667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7668(int& val) {
    func_in_7668(val);
    const int x = 7668;
    std::cout << 7668;
    for (int i = 0; i < 7668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7668(int& val) {
    const int x = 7668;
    std::cout << 7668;
    for (int i = 0; i < 7668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7669(int& val) {
    func_in_7669(val);
    const int x = 7669;
    std::cout << 7669;
    for (int i = 0; i < 7669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7669(int& val) {
    const int x = 7669;
    std::cout << 7669;
    for (int i = 0; i < 7669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7670(int& val) {
    func_in_7670(val);
    const int x = 7670;
    std::cout << 7670;
    for (int i = 0; i < 7670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7670(int& val) {
    const int x = 7670;
    std::cout << 7670;
    for (int i = 0; i < 7670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7671(int& val) {
    func_in_7671(val);
    const int x = 7671;
    std::cout << 7671;
    for (int i = 0; i < 7671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7671(int& val) {
    const int x = 7671;
    std::cout << 7671;
    for (int i = 0; i < 7671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7672(int& val) {
    func_in_7672(val);
    const int x = 7672;
    std::cout << 7672;
    for (int i = 0; i < 7672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7672(int& val) {
    const int x = 7672;
    std::cout << 7672;
    for (int i = 0; i < 7672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7673(int& val) {
    func_in_7673(val);
    const int x = 7673;
    std::cout << 7673;
    for (int i = 0; i < 7673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7673(int& val) {
    const int x = 7673;
    std::cout << 7673;
    for (int i = 0; i < 7673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7674(int& val) {
    func_in_7674(val);
    const int x = 7674;
    std::cout << 7674;
    for (int i = 0; i < 7674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7674(int& val) {
    const int x = 7674;
    std::cout << 7674;
    for (int i = 0; i < 7674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7675(int& val) {
    func_in_7675(val);
    const int x = 7675;
    std::cout << 7675;
    for (int i = 0; i < 7675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7675(int& val) {
    const int x = 7675;
    std::cout << 7675;
    for (int i = 0; i < 7675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7676(int& val) {
    func_in_7676(val);
    const int x = 7676;
    std::cout << 7676;
    for (int i = 0; i < 7676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7676(int& val) {
    const int x = 7676;
    std::cout << 7676;
    for (int i = 0; i < 7676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7677(int& val) {
    func_in_7677(val);
    const int x = 7677;
    std::cout << 7677;
    for (int i = 0; i < 7677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7677(int& val) {
    const int x = 7677;
    std::cout << 7677;
    for (int i = 0; i < 7677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7678(int& val) {
    func_in_7678(val);
    const int x = 7678;
    std::cout << 7678;
    for (int i = 0; i < 7678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7678(int& val) {
    const int x = 7678;
    std::cout << 7678;
    for (int i = 0; i < 7678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7679(int& val) {
    func_in_7679(val);
    const int x = 7679;
    std::cout << 7679;
    for (int i = 0; i < 7679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7679(int& val) {
    const int x = 7679;
    std::cout << 7679;
    for (int i = 0; i < 7679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7680(int& val) {
    func_in_7680(val);
    const int x = 7680;
    std::cout << 7680;
    for (int i = 0; i < 7680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7680(int& val) {
    const int x = 7680;
    std::cout << 7680;
    for (int i = 0; i < 7680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7681(int& val) {
    func_in_7681(val);
    const int x = 7681;
    std::cout << 7681;
    for (int i = 0; i < 7681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7681(int& val) {
    const int x = 7681;
    std::cout << 7681;
    for (int i = 0; i < 7681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7682(int& val) {
    func_in_7682(val);
    const int x = 7682;
    std::cout << 7682;
    for (int i = 0; i < 7682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7682(int& val) {
    const int x = 7682;
    std::cout << 7682;
    for (int i = 0; i < 7682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7683(int& val) {
    func_in_7683(val);
    const int x = 7683;
    std::cout << 7683;
    for (int i = 0; i < 7683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7683(int& val) {
    const int x = 7683;
    std::cout << 7683;
    for (int i = 0; i < 7683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7684(int& val) {
    func_in_7684(val);
    const int x = 7684;
    std::cout << 7684;
    for (int i = 0; i < 7684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7684(int& val) {
    const int x = 7684;
    std::cout << 7684;
    for (int i = 0; i < 7684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7685(int& val) {
    func_in_7685(val);
    const int x = 7685;
    std::cout << 7685;
    for (int i = 0; i < 7685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7685(int& val) {
    const int x = 7685;
    std::cout << 7685;
    for (int i = 0; i < 7685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7686(int& val) {
    func_in_7686(val);
    const int x = 7686;
    std::cout << 7686;
    for (int i = 0; i < 7686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7686(int& val) {
    const int x = 7686;
    std::cout << 7686;
    for (int i = 0; i < 7686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7687(int& val) {
    func_in_7687(val);
    const int x = 7687;
    std::cout << 7687;
    for (int i = 0; i < 7687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7687(int& val) {
    const int x = 7687;
    std::cout << 7687;
    for (int i = 0; i < 7687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7688(int& val) {
    func_in_7688(val);
    const int x = 7688;
    std::cout << 7688;
    for (int i = 0; i < 7688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7688(int& val) {
    const int x = 7688;
    std::cout << 7688;
    for (int i = 0; i < 7688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7689(int& val) {
    func_in_7689(val);
    const int x = 7689;
    std::cout << 7689;
    for (int i = 0; i < 7689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7689(int& val) {
    const int x = 7689;
    std::cout << 7689;
    for (int i = 0; i < 7689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7690(int& val) {
    func_in_7690(val);
    const int x = 7690;
    std::cout << 7690;
    for (int i = 0; i < 7690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7690(int& val) {
    const int x = 7690;
    std::cout << 7690;
    for (int i = 0; i < 7690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7691(int& val) {
    func_in_7691(val);
    const int x = 7691;
    std::cout << 7691;
    for (int i = 0; i < 7691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7691(int& val) {
    const int x = 7691;
    std::cout << 7691;
    for (int i = 0; i < 7691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7692(int& val) {
    func_in_7692(val);
    const int x = 7692;
    std::cout << 7692;
    for (int i = 0; i < 7692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7692(int& val) {
    const int x = 7692;
    std::cout << 7692;
    for (int i = 0; i < 7692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7693(int& val) {
    func_in_7693(val);
    const int x = 7693;
    std::cout << 7693;
    for (int i = 0; i < 7693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7693(int& val) {
    const int x = 7693;
    std::cout << 7693;
    for (int i = 0; i < 7693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7694(int& val) {
    func_in_7694(val);
    const int x = 7694;
    std::cout << 7694;
    for (int i = 0; i < 7694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7694(int& val) {
    const int x = 7694;
    std::cout << 7694;
    for (int i = 0; i < 7694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7695(int& val) {
    func_in_7695(val);
    const int x = 7695;
    std::cout << 7695;
    for (int i = 0; i < 7695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7695(int& val) {
    const int x = 7695;
    std::cout << 7695;
    for (int i = 0; i < 7695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7696(int& val) {
    func_in_7696(val);
    const int x = 7696;
    std::cout << 7696;
    for (int i = 0; i < 7696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7696(int& val) {
    const int x = 7696;
    std::cout << 7696;
    for (int i = 0; i < 7696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7697(int& val) {
    func_in_7697(val);
    const int x = 7697;
    std::cout << 7697;
    for (int i = 0; i < 7697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7697(int& val) {
    const int x = 7697;
    std::cout << 7697;
    for (int i = 0; i < 7697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7698(int& val) {
    func_in_7698(val);
    const int x = 7698;
    std::cout << 7698;
    for (int i = 0; i < 7698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7698(int& val) {
    const int x = 7698;
    std::cout << 7698;
    for (int i = 0; i < 7698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7699(int& val) {
    func_in_7699(val);
    const int x = 7699;
    std::cout << 7699;
    for (int i = 0; i < 7699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7699(int& val) {
    const int x = 7699;
    std::cout << 7699;
    for (int i = 0; i < 7699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7700(int& val) {
    func_in_7700(val);
    const int x = 7700;
    std::cout << 7700;
    for (int i = 0; i < 7700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7700(int& val) {
    const int x = 7700;
    std::cout << 7700;
    for (int i = 0; i < 7700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7701(int& val) {
    func_in_7701(val);
    const int x = 7701;
    std::cout << 7701;
    for (int i = 0; i < 7701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7701(int& val) {
    const int x = 7701;
    std::cout << 7701;
    for (int i = 0; i < 7701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7702(int& val) {
    func_in_7702(val);
    const int x = 7702;
    std::cout << 7702;
    for (int i = 0; i < 7702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7702(int& val) {
    const int x = 7702;
    std::cout << 7702;
    for (int i = 0; i < 7702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7703(int& val) {
    func_in_7703(val);
    const int x = 7703;
    std::cout << 7703;
    for (int i = 0; i < 7703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7703(int& val) {
    const int x = 7703;
    std::cout << 7703;
    for (int i = 0; i < 7703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7704(int& val) {
    func_in_7704(val);
    const int x = 7704;
    std::cout << 7704;
    for (int i = 0; i < 7704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7704(int& val) {
    const int x = 7704;
    std::cout << 7704;
    for (int i = 0; i < 7704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7705(int& val) {
    func_in_7705(val);
    const int x = 7705;
    std::cout << 7705;
    for (int i = 0; i < 7705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7705(int& val) {
    const int x = 7705;
    std::cout << 7705;
    for (int i = 0; i < 7705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7706(int& val) {
    func_in_7706(val);
    const int x = 7706;
    std::cout << 7706;
    for (int i = 0; i < 7706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7706(int& val) {
    const int x = 7706;
    std::cout << 7706;
    for (int i = 0; i < 7706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7707(int& val) {
    func_in_7707(val);
    const int x = 7707;
    std::cout << 7707;
    for (int i = 0; i < 7707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7707(int& val) {
    const int x = 7707;
    std::cout << 7707;
    for (int i = 0; i < 7707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7708(int& val) {
    func_in_7708(val);
    const int x = 7708;
    std::cout << 7708;
    for (int i = 0; i < 7708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7708(int& val) {
    const int x = 7708;
    std::cout << 7708;
    for (int i = 0; i < 7708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7709(int& val) {
    func_in_7709(val);
    const int x = 7709;
    std::cout << 7709;
    for (int i = 0; i < 7709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7709(int& val) {
    const int x = 7709;
    std::cout << 7709;
    for (int i = 0; i < 7709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7710(int& val) {
    func_in_7710(val);
    const int x = 7710;
    std::cout << 7710;
    for (int i = 0; i < 7710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7710(int& val) {
    const int x = 7710;
    std::cout << 7710;
    for (int i = 0; i < 7710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7711(int& val) {
    func_in_7711(val);
    const int x = 7711;
    std::cout << 7711;
    for (int i = 0; i < 7711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7711(int& val) {
    const int x = 7711;
    std::cout << 7711;
    for (int i = 0; i < 7711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7712(int& val) {
    func_in_7712(val);
    const int x = 7712;
    std::cout << 7712;
    for (int i = 0; i < 7712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7712(int& val) {
    const int x = 7712;
    std::cout << 7712;
    for (int i = 0; i < 7712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7713(int& val) {
    func_in_7713(val);
    const int x = 7713;
    std::cout << 7713;
    for (int i = 0; i < 7713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7713(int& val) {
    const int x = 7713;
    std::cout << 7713;
    for (int i = 0; i < 7713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7714(int& val) {
    func_in_7714(val);
    const int x = 7714;
    std::cout << 7714;
    for (int i = 0; i < 7714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7714(int& val) {
    const int x = 7714;
    std::cout << 7714;
    for (int i = 0; i < 7714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7715(int& val) {
    func_in_7715(val);
    const int x = 7715;
    std::cout << 7715;
    for (int i = 0; i < 7715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7715(int& val) {
    const int x = 7715;
    std::cout << 7715;
    for (int i = 0; i < 7715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7716(int& val) {
    func_in_7716(val);
    const int x = 7716;
    std::cout << 7716;
    for (int i = 0; i < 7716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7716(int& val) {
    const int x = 7716;
    std::cout << 7716;
    for (int i = 0; i < 7716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7717(int& val) {
    func_in_7717(val);
    const int x = 7717;
    std::cout << 7717;
    for (int i = 0; i < 7717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7717(int& val) {
    const int x = 7717;
    std::cout << 7717;
    for (int i = 0; i < 7717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7718(int& val) {
    func_in_7718(val);
    const int x = 7718;
    std::cout << 7718;
    for (int i = 0; i < 7718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7718(int& val) {
    const int x = 7718;
    std::cout << 7718;
    for (int i = 0; i < 7718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7719(int& val) {
    func_in_7719(val);
    const int x = 7719;
    std::cout << 7719;
    for (int i = 0; i < 7719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7719(int& val) {
    const int x = 7719;
    std::cout << 7719;
    for (int i = 0; i < 7719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7720(int& val) {
    func_in_7720(val);
    const int x = 7720;
    std::cout << 7720;
    for (int i = 0; i < 7720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7720(int& val) {
    const int x = 7720;
    std::cout << 7720;
    for (int i = 0; i < 7720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7721(int& val) {
    func_in_7721(val);
    const int x = 7721;
    std::cout << 7721;
    for (int i = 0; i < 7721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7721(int& val) {
    const int x = 7721;
    std::cout << 7721;
    for (int i = 0; i < 7721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7722(int& val) {
    func_in_7722(val);
    const int x = 7722;
    std::cout << 7722;
    for (int i = 0; i < 7722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7722(int& val) {
    const int x = 7722;
    std::cout << 7722;
    for (int i = 0; i < 7722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7723(int& val) {
    func_in_7723(val);
    const int x = 7723;
    std::cout << 7723;
    for (int i = 0; i < 7723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7723(int& val) {
    const int x = 7723;
    std::cout << 7723;
    for (int i = 0; i < 7723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7724(int& val) {
    func_in_7724(val);
    const int x = 7724;
    std::cout << 7724;
    for (int i = 0; i < 7724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7724(int& val) {
    const int x = 7724;
    std::cout << 7724;
    for (int i = 0; i < 7724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7725(int& val) {
    func_in_7725(val);
    const int x = 7725;
    std::cout << 7725;
    for (int i = 0; i < 7725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7725(int& val) {
    const int x = 7725;
    std::cout << 7725;
    for (int i = 0; i < 7725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7726(int& val) {
    func_in_7726(val);
    const int x = 7726;
    std::cout << 7726;
    for (int i = 0; i < 7726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7726(int& val) {
    const int x = 7726;
    std::cout << 7726;
    for (int i = 0; i < 7726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7727(int& val) {
    func_in_7727(val);
    const int x = 7727;
    std::cout << 7727;
    for (int i = 0; i < 7727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7727(int& val) {
    const int x = 7727;
    std::cout << 7727;
    for (int i = 0; i < 7727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7728(int& val) {
    func_in_7728(val);
    const int x = 7728;
    std::cout << 7728;
    for (int i = 0; i < 7728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7728(int& val) {
    const int x = 7728;
    std::cout << 7728;
    for (int i = 0; i < 7728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7729(int& val) {
    func_in_7729(val);
    const int x = 7729;
    std::cout << 7729;
    for (int i = 0; i < 7729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7729(int& val) {
    const int x = 7729;
    std::cout << 7729;
    for (int i = 0; i < 7729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7730(int& val) {
    func_in_7730(val);
    const int x = 7730;
    std::cout << 7730;
    for (int i = 0; i < 7730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7730(int& val) {
    const int x = 7730;
    std::cout << 7730;
    for (int i = 0; i < 7730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7731(int& val) {
    func_in_7731(val);
    const int x = 7731;
    std::cout << 7731;
    for (int i = 0; i < 7731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7731(int& val) {
    const int x = 7731;
    std::cout << 7731;
    for (int i = 0; i < 7731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7732(int& val) {
    func_in_7732(val);
    const int x = 7732;
    std::cout << 7732;
    for (int i = 0; i < 7732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7732(int& val) {
    const int x = 7732;
    std::cout << 7732;
    for (int i = 0; i < 7732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7733(int& val) {
    func_in_7733(val);
    const int x = 7733;
    std::cout << 7733;
    for (int i = 0; i < 7733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7733(int& val) {
    const int x = 7733;
    std::cout << 7733;
    for (int i = 0; i < 7733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7734(int& val) {
    func_in_7734(val);
    const int x = 7734;
    std::cout << 7734;
    for (int i = 0; i < 7734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7734(int& val) {
    const int x = 7734;
    std::cout << 7734;
    for (int i = 0; i < 7734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7735(int& val) {
    func_in_7735(val);
    const int x = 7735;
    std::cout << 7735;
    for (int i = 0; i < 7735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7735(int& val) {
    const int x = 7735;
    std::cout << 7735;
    for (int i = 0; i < 7735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7736(int& val) {
    func_in_7736(val);
    const int x = 7736;
    std::cout << 7736;
    for (int i = 0; i < 7736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7736(int& val) {
    const int x = 7736;
    std::cout << 7736;
    for (int i = 0; i < 7736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7737(int& val) {
    func_in_7737(val);
    const int x = 7737;
    std::cout << 7737;
    for (int i = 0; i < 7737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7737(int& val) {
    const int x = 7737;
    std::cout << 7737;
    for (int i = 0; i < 7737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7738(int& val) {
    func_in_7738(val);
    const int x = 7738;
    std::cout << 7738;
    for (int i = 0; i < 7738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7738(int& val) {
    const int x = 7738;
    std::cout << 7738;
    for (int i = 0; i < 7738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7739(int& val) {
    func_in_7739(val);
    const int x = 7739;
    std::cout << 7739;
    for (int i = 0; i < 7739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7739(int& val) {
    const int x = 7739;
    std::cout << 7739;
    for (int i = 0; i < 7739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7740(int& val) {
    func_in_7740(val);
    const int x = 7740;
    std::cout << 7740;
    for (int i = 0; i < 7740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7740(int& val) {
    const int x = 7740;
    std::cout << 7740;
    for (int i = 0; i < 7740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7741(int& val) {
    func_in_7741(val);
    const int x = 7741;
    std::cout << 7741;
    for (int i = 0; i < 7741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7741(int& val) {
    const int x = 7741;
    std::cout << 7741;
    for (int i = 0; i < 7741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7742(int& val) {
    func_in_7742(val);
    const int x = 7742;
    std::cout << 7742;
    for (int i = 0; i < 7742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7742(int& val) {
    const int x = 7742;
    std::cout << 7742;
    for (int i = 0; i < 7742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7743(int& val) {
    func_in_7743(val);
    const int x = 7743;
    std::cout << 7743;
    for (int i = 0; i < 7743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7743(int& val) {
    const int x = 7743;
    std::cout << 7743;
    for (int i = 0; i < 7743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7744(int& val) {
    func_in_7744(val);
    const int x = 7744;
    std::cout << 7744;
    for (int i = 0; i < 7744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7744(int& val) {
    const int x = 7744;
    std::cout << 7744;
    for (int i = 0; i < 7744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7745(int& val) {
    func_in_7745(val);
    const int x = 7745;
    std::cout << 7745;
    for (int i = 0; i < 7745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7745(int& val) {
    const int x = 7745;
    std::cout << 7745;
    for (int i = 0; i < 7745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7746(int& val) {
    func_in_7746(val);
    const int x = 7746;
    std::cout << 7746;
    for (int i = 0; i < 7746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7746(int& val) {
    const int x = 7746;
    std::cout << 7746;
    for (int i = 0; i < 7746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7747(int& val) {
    func_in_7747(val);
    const int x = 7747;
    std::cout << 7747;
    for (int i = 0; i < 7747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7747(int& val) {
    const int x = 7747;
    std::cout << 7747;
    for (int i = 0; i < 7747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7748(int& val) {
    func_in_7748(val);
    const int x = 7748;
    std::cout << 7748;
    for (int i = 0; i < 7748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7748(int& val) {
    const int x = 7748;
    std::cout << 7748;
    for (int i = 0; i < 7748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7749(int& val) {
    func_in_7749(val);
    const int x = 7749;
    std::cout << 7749;
    for (int i = 0; i < 7749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7749(int& val) {
    const int x = 7749;
    std::cout << 7749;
    for (int i = 0; i < 7749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7750(int& val) {
    func_in_7750(val);
    const int x = 7750;
    std::cout << 7750;
    for (int i = 0; i < 7750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7750(int& val) {
    const int x = 7750;
    std::cout << 7750;
    for (int i = 0; i < 7750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7751(int& val) {
    func_in_7751(val);
    const int x = 7751;
    std::cout << 7751;
    for (int i = 0; i < 7751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7751(int& val) {
    const int x = 7751;
    std::cout << 7751;
    for (int i = 0; i < 7751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7752(int& val) {
    func_in_7752(val);
    const int x = 7752;
    std::cout << 7752;
    for (int i = 0; i < 7752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7752(int& val) {
    const int x = 7752;
    std::cout << 7752;
    for (int i = 0; i < 7752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7753(int& val) {
    func_in_7753(val);
    const int x = 7753;
    std::cout << 7753;
    for (int i = 0; i < 7753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7753(int& val) {
    const int x = 7753;
    std::cout << 7753;
    for (int i = 0; i < 7753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7754(int& val) {
    func_in_7754(val);
    const int x = 7754;
    std::cout << 7754;
    for (int i = 0; i < 7754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7754(int& val) {
    const int x = 7754;
    std::cout << 7754;
    for (int i = 0; i < 7754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7755(int& val) {
    func_in_7755(val);
    const int x = 7755;
    std::cout << 7755;
    for (int i = 0; i < 7755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7755(int& val) {
    const int x = 7755;
    std::cout << 7755;
    for (int i = 0; i < 7755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7756(int& val) {
    func_in_7756(val);
    const int x = 7756;
    std::cout << 7756;
    for (int i = 0; i < 7756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7756(int& val) {
    const int x = 7756;
    std::cout << 7756;
    for (int i = 0; i < 7756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7757(int& val) {
    func_in_7757(val);
    const int x = 7757;
    std::cout << 7757;
    for (int i = 0; i < 7757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7757(int& val) {
    const int x = 7757;
    std::cout << 7757;
    for (int i = 0; i < 7757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7758(int& val) {
    func_in_7758(val);
    const int x = 7758;
    std::cout << 7758;
    for (int i = 0; i < 7758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7758(int& val) {
    const int x = 7758;
    std::cout << 7758;
    for (int i = 0; i < 7758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7759(int& val) {
    func_in_7759(val);
    const int x = 7759;
    std::cout << 7759;
    for (int i = 0; i < 7759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7759(int& val) {
    const int x = 7759;
    std::cout << 7759;
    for (int i = 0; i < 7759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7760(int& val) {
    func_in_7760(val);
    const int x = 7760;
    std::cout << 7760;
    for (int i = 0; i < 7760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7760(int& val) {
    const int x = 7760;
    std::cout << 7760;
    for (int i = 0; i < 7760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7761(int& val) {
    func_in_7761(val);
    const int x = 7761;
    std::cout << 7761;
    for (int i = 0; i < 7761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7761(int& val) {
    const int x = 7761;
    std::cout << 7761;
    for (int i = 0; i < 7761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7762(int& val) {
    func_in_7762(val);
    const int x = 7762;
    std::cout << 7762;
    for (int i = 0; i < 7762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7762(int& val) {
    const int x = 7762;
    std::cout << 7762;
    for (int i = 0; i < 7762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7763(int& val) {
    func_in_7763(val);
    const int x = 7763;
    std::cout << 7763;
    for (int i = 0; i < 7763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7763(int& val) {
    const int x = 7763;
    std::cout << 7763;
    for (int i = 0; i < 7763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7764(int& val) {
    func_in_7764(val);
    const int x = 7764;
    std::cout << 7764;
    for (int i = 0; i < 7764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7764(int& val) {
    const int x = 7764;
    std::cout << 7764;
    for (int i = 0; i < 7764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7765(int& val) {
    func_in_7765(val);
    const int x = 7765;
    std::cout << 7765;
    for (int i = 0; i < 7765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7765(int& val) {
    const int x = 7765;
    std::cout << 7765;
    for (int i = 0; i < 7765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7766(int& val) {
    func_in_7766(val);
    const int x = 7766;
    std::cout << 7766;
    for (int i = 0; i < 7766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7766(int& val) {
    const int x = 7766;
    std::cout << 7766;
    for (int i = 0; i < 7766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7767(int& val) {
    func_in_7767(val);
    const int x = 7767;
    std::cout << 7767;
    for (int i = 0; i < 7767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7767(int& val) {
    const int x = 7767;
    std::cout << 7767;
    for (int i = 0; i < 7767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7768(int& val) {
    func_in_7768(val);
    const int x = 7768;
    std::cout << 7768;
    for (int i = 0; i < 7768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7768(int& val) {
    const int x = 7768;
    std::cout << 7768;
    for (int i = 0; i < 7768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7769(int& val) {
    func_in_7769(val);
    const int x = 7769;
    std::cout << 7769;
    for (int i = 0; i < 7769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7769(int& val) {
    const int x = 7769;
    std::cout << 7769;
    for (int i = 0; i < 7769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7770(int& val) {
    func_in_7770(val);
    const int x = 7770;
    std::cout << 7770;
    for (int i = 0; i < 7770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7770(int& val) {
    const int x = 7770;
    std::cout << 7770;
    for (int i = 0; i < 7770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7771(int& val) {
    func_in_7771(val);
    const int x = 7771;
    std::cout << 7771;
    for (int i = 0; i < 7771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7771(int& val) {
    const int x = 7771;
    std::cout << 7771;
    for (int i = 0; i < 7771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7772(int& val) {
    func_in_7772(val);
    const int x = 7772;
    std::cout << 7772;
    for (int i = 0; i < 7772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7772(int& val) {
    const int x = 7772;
    std::cout << 7772;
    for (int i = 0; i < 7772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7773(int& val) {
    func_in_7773(val);
    const int x = 7773;
    std::cout << 7773;
    for (int i = 0; i < 7773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7773(int& val) {
    const int x = 7773;
    std::cout << 7773;
    for (int i = 0; i < 7773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7774(int& val) {
    func_in_7774(val);
    const int x = 7774;
    std::cout << 7774;
    for (int i = 0; i < 7774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7774(int& val) {
    const int x = 7774;
    std::cout << 7774;
    for (int i = 0; i < 7774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7775(int& val) {
    func_in_7775(val);
    const int x = 7775;
    std::cout << 7775;
    for (int i = 0; i < 7775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7775(int& val) {
    const int x = 7775;
    std::cout << 7775;
    for (int i = 0; i < 7775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7776(int& val) {
    func_in_7776(val);
    const int x = 7776;
    std::cout << 7776;
    for (int i = 0; i < 7776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7776(int& val) {
    const int x = 7776;
    std::cout << 7776;
    for (int i = 0; i < 7776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7777(int& val) {
    func_in_7777(val);
    const int x = 7777;
    std::cout << 7777;
    for (int i = 0; i < 7777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7777(int& val) {
    const int x = 7777;
    std::cout << 7777;
    for (int i = 0; i < 7777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7778(int& val) {
    func_in_7778(val);
    const int x = 7778;
    std::cout << 7778;
    for (int i = 0; i < 7778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7778(int& val) {
    const int x = 7778;
    std::cout << 7778;
    for (int i = 0; i < 7778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7779(int& val) {
    func_in_7779(val);
    const int x = 7779;
    std::cout << 7779;
    for (int i = 0; i < 7779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7779(int& val) {
    const int x = 7779;
    std::cout << 7779;
    for (int i = 0; i < 7779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7780(int& val) {
    func_in_7780(val);
    const int x = 7780;
    std::cout << 7780;
    for (int i = 0; i < 7780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7780(int& val) {
    const int x = 7780;
    std::cout << 7780;
    for (int i = 0; i < 7780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7781(int& val) {
    func_in_7781(val);
    const int x = 7781;
    std::cout << 7781;
    for (int i = 0; i < 7781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7781(int& val) {
    const int x = 7781;
    std::cout << 7781;
    for (int i = 0; i < 7781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7782(int& val) {
    func_in_7782(val);
    const int x = 7782;
    std::cout << 7782;
    for (int i = 0; i < 7782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7782(int& val) {
    const int x = 7782;
    std::cout << 7782;
    for (int i = 0; i < 7782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7783(int& val) {
    func_in_7783(val);
    const int x = 7783;
    std::cout << 7783;
    for (int i = 0; i < 7783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7783(int& val) {
    const int x = 7783;
    std::cout << 7783;
    for (int i = 0; i < 7783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7784(int& val) {
    func_in_7784(val);
    const int x = 7784;
    std::cout << 7784;
    for (int i = 0; i < 7784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7784(int& val) {
    const int x = 7784;
    std::cout << 7784;
    for (int i = 0; i < 7784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7785(int& val) {
    func_in_7785(val);
    const int x = 7785;
    std::cout << 7785;
    for (int i = 0; i < 7785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7785(int& val) {
    const int x = 7785;
    std::cout << 7785;
    for (int i = 0; i < 7785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7786(int& val) {
    func_in_7786(val);
    const int x = 7786;
    std::cout << 7786;
    for (int i = 0; i < 7786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7786(int& val) {
    const int x = 7786;
    std::cout << 7786;
    for (int i = 0; i < 7786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7787(int& val) {
    func_in_7787(val);
    const int x = 7787;
    std::cout << 7787;
    for (int i = 0; i < 7787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7787(int& val) {
    const int x = 7787;
    std::cout << 7787;
    for (int i = 0; i < 7787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7788(int& val) {
    func_in_7788(val);
    const int x = 7788;
    std::cout << 7788;
    for (int i = 0; i < 7788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7788(int& val) {
    const int x = 7788;
    std::cout << 7788;
    for (int i = 0; i < 7788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7789(int& val) {
    func_in_7789(val);
    const int x = 7789;
    std::cout << 7789;
    for (int i = 0; i < 7789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7789(int& val) {
    const int x = 7789;
    std::cout << 7789;
    for (int i = 0; i < 7789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7790(int& val) {
    func_in_7790(val);
    const int x = 7790;
    std::cout << 7790;
    for (int i = 0; i < 7790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7790(int& val) {
    const int x = 7790;
    std::cout << 7790;
    for (int i = 0; i < 7790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7791(int& val) {
    func_in_7791(val);
    const int x = 7791;
    std::cout << 7791;
    for (int i = 0; i < 7791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7791(int& val) {
    const int x = 7791;
    std::cout << 7791;
    for (int i = 0; i < 7791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7792(int& val) {
    func_in_7792(val);
    const int x = 7792;
    std::cout << 7792;
    for (int i = 0; i < 7792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7792(int& val) {
    const int x = 7792;
    std::cout << 7792;
    for (int i = 0; i < 7792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7793(int& val) {
    func_in_7793(val);
    const int x = 7793;
    std::cout << 7793;
    for (int i = 0; i < 7793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7793(int& val) {
    const int x = 7793;
    std::cout << 7793;
    for (int i = 0; i < 7793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7794(int& val) {
    func_in_7794(val);
    const int x = 7794;
    std::cout << 7794;
    for (int i = 0; i < 7794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7794(int& val) {
    const int x = 7794;
    std::cout << 7794;
    for (int i = 0; i < 7794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7795(int& val) {
    func_in_7795(val);
    const int x = 7795;
    std::cout << 7795;
    for (int i = 0; i < 7795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7795(int& val) {
    const int x = 7795;
    std::cout << 7795;
    for (int i = 0; i < 7795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7796(int& val) {
    func_in_7796(val);
    const int x = 7796;
    std::cout << 7796;
    for (int i = 0; i < 7796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7796(int& val) {
    const int x = 7796;
    std::cout << 7796;
    for (int i = 0; i < 7796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7797(int& val) {
    func_in_7797(val);
    const int x = 7797;
    std::cout << 7797;
    for (int i = 0; i < 7797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7797(int& val) {
    const int x = 7797;
    std::cout << 7797;
    for (int i = 0; i < 7797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7798(int& val) {
    func_in_7798(val);
    const int x = 7798;
    std::cout << 7798;
    for (int i = 0; i < 7798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7798(int& val) {
    const int x = 7798;
    std::cout << 7798;
    for (int i = 0; i < 7798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7799(int& val) {
    func_in_7799(val);
    const int x = 7799;
    std::cout << 7799;
    for (int i = 0; i < 7799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7799(int& val) {
    const int x = 7799;
    std::cout << 7799;
    for (int i = 0; i < 7799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7800(int& val) {
    func_in_7800(val);
    const int x = 7800;
    std::cout << 7800;
    for (int i = 0; i < 7800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7800(int& val) {
    const int x = 7800;
    std::cout << 7800;
    for (int i = 0; i < 7800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7801(int& val) {
    func_in_7801(val);
    const int x = 7801;
    std::cout << 7801;
    for (int i = 0; i < 7801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7801(int& val) {
    const int x = 7801;
    std::cout << 7801;
    for (int i = 0; i < 7801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7802(int& val) {
    func_in_7802(val);
    const int x = 7802;
    std::cout << 7802;
    for (int i = 0; i < 7802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7802(int& val) {
    const int x = 7802;
    std::cout << 7802;
    for (int i = 0; i < 7802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7803(int& val) {
    func_in_7803(val);
    const int x = 7803;
    std::cout << 7803;
    for (int i = 0; i < 7803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7803(int& val) {
    const int x = 7803;
    std::cout << 7803;
    for (int i = 0; i < 7803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7804(int& val) {
    func_in_7804(val);
    const int x = 7804;
    std::cout << 7804;
    for (int i = 0; i < 7804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7804(int& val) {
    const int x = 7804;
    std::cout << 7804;
    for (int i = 0; i < 7804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7805(int& val) {
    func_in_7805(val);
    const int x = 7805;
    std::cout << 7805;
    for (int i = 0; i < 7805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7805(int& val) {
    const int x = 7805;
    std::cout << 7805;
    for (int i = 0; i < 7805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7806(int& val) {
    func_in_7806(val);
    const int x = 7806;
    std::cout << 7806;
    for (int i = 0; i < 7806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7806(int& val) {
    const int x = 7806;
    std::cout << 7806;
    for (int i = 0; i < 7806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7807(int& val) {
    func_in_7807(val);
    const int x = 7807;
    std::cout << 7807;
    for (int i = 0; i < 7807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7807(int& val) {
    const int x = 7807;
    std::cout << 7807;
    for (int i = 0; i < 7807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7808(int& val) {
    func_in_7808(val);
    const int x = 7808;
    std::cout << 7808;
    for (int i = 0; i < 7808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7808(int& val) {
    const int x = 7808;
    std::cout << 7808;
    for (int i = 0; i < 7808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7809(int& val) {
    func_in_7809(val);
    const int x = 7809;
    std::cout << 7809;
    for (int i = 0; i < 7809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7809(int& val) {
    const int x = 7809;
    std::cout << 7809;
    for (int i = 0; i < 7809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7810(int& val) {
    func_in_7810(val);
    const int x = 7810;
    std::cout << 7810;
    for (int i = 0; i < 7810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7810(int& val) {
    const int x = 7810;
    std::cout << 7810;
    for (int i = 0; i < 7810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7811(int& val) {
    func_in_7811(val);
    const int x = 7811;
    std::cout << 7811;
    for (int i = 0; i < 7811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7811(int& val) {
    const int x = 7811;
    std::cout << 7811;
    for (int i = 0; i < 7811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7812(int& val) {
    func_in_7812(val);
    const int x = 7812;
    std::cout << 7812;
    for (int i = 0; i < 7812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7812(int& val) {
    const int x = 7812;
    std::cout << 7812;
    for (int i = 0; i < 7812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7813(int& val) {
    func_in_7813(val);
    const int x = 7813;
    std::cout << 7813;
    for (int i = 0; i < 7813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7813(int& val) {
    const int x = 7813;
    std::cout << 7813;
    for (int i = 0; i < 7813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7814(int& val) {
    func_in_7814(val);
    const int x = 7814;
    std::cout << 7814;
    for (int i = 0; i < 7814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7814(int& val) {
    const int x = 7814;
    std::cout << 7814;
    for (int i = 0; i < 7814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7815(int& val) {
    func_in_7815(val);
    const int x = 7815;
    std::cout << 7815;
    for (int i = 0; i < 7815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7815(int& val) {
    const int x = 7815;
    std::cout << 7815;
    for (int i = 0; i < 7815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7816(int& val) {
    func_in_7816(val);
    const int x = 7816;
    std::cout << 7816;
    for (int i = 0; i < 7816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7816(int& val) {
    const int x = 7816;
    std::cout << 7816;
    for (int i = 0; i < 7816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7817(int& val) {
    func_in_7817(val);
    const int x = 7817;
    std::cout << 7817;
    for (int i = 0; i < 7817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7817(int& val) {
    const int x = 7817;
    std::cout << 7817;
    for (int i = 0; i < 7817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7818(int& val) {
    func_in_7818(val);
    const int x = 7818;
    std::cout << 7818;
    for (int i = 0; i < 7818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7818(int& val) {
    const int x = 7818;
    std::cout << 7818;
    for (int i = 0; i < 7818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7819(int& val) {
    func_in_7819(val);
    const int x = 7819;
    std::cout << 7819;
    for (int i = 0; i < 7819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7819(int& val) {
    const int x = 7819;
    std::cout << 7819;
    for (int i = 0; i < 7819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7820(int& val) {
    func_in_7820(val);
    const int x = 7820;
    std::cout << 7820;
    for (int i = 0; i < 7820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7820(int& val) {
    const int x = 7820;
    std::cout << 7820;
    for (int i = 0; i < 7820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7821(int& val) {
    func_in_7821(val);
    const int x = 7821;
    std::cout << 7821;
    for (int i = 0; i < 7821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7821(int& val) {
    const int x = 7821;
    std::cout << 7821;
    for (int i = 0; i < 7821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7822(int& val) {
    func_in_7822(val);
    const int x = 7822;
    std::cout << 7822;
    for (int i = 0; i < 7822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7822(int& val) {
    const int x = 7822;
    std::cout << 7822;
    for (int i = 0; i < 7822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7823(int& val) {
    func_in_7823(val);
    const int x = 7823;
    std::cout << 7823;
    for (int i = 0; i < 7823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7823(int& val) {
    const int x = 7823;
    std::cout << 7823;
    for (int i = 0; i < 7823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7824(int& val) {
    func_in_7824(val);
    const int x = 7824;
    std::cout << 7824;
    for (int i = 0; i < 7824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7824(int& val) {
    const int x = 7824;
    std::cout << 7824;
    for (int i = 0; i < 7824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7825(int& val) {
    func_in_7825(val);
    const int x = 7825;
    std::cout << 7825;
    for (int i = 0; i < 7825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7825(int& val) {
    const int x = 7825;
    std::cout << 7825;
    for (int i = 0; i < 7825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7826(int& val) {
    func_in_7826(val);
    const int x = 7826;
    std::cout << 7826;
    for (int i = 0; i < 7826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7826(int& val) {
    const int x = 7826;
    std::cout << 7826;
    for (int i = 0; i < 7826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7827(int& val) {
    func_in_7827(val);
    const int x = 7827;
    std::cout << 7827;
    for (int i = 0; i < 7827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7827(int& val) {
    const int x = 7827;
    std::cout << 7827;
    for (int i = 0; i < 7827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7828(int& val) {
    func_in_7828(val);
    const int x = 7828;
    std::cout << 7828;
    for (int i = 0; i < 7828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7828(int& val) {
    const int x = 7828;
    std::cout << 7828;
    for (int i = 0; i < 7828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7829(int& val) {
    func_in_7829(val);
    const int x = 7829;
    std::cout << 7829;
    for (int i = 0; i < 7829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7829(int& val) {
    const int x = 7829;
    std::cout << 7829;
    for (int i = 0; i < 7829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7830(int& val) {
    func_in_7830(val);
    const int x = 7830;
    std::cout << 7830;
    for (int i = 0; i < 7830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7830(int& val) {
    const int x = 7830;
    std::cout << 7830;
    for (int i = 0; i < 7830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7831(int& val) {
    func_in_7831(val);
    const int x = 7831;
    std::cout << 7831;
    for (int i = 0; i < 7831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7831(int& val) {
    const int x = 7831;
    std::cout << 7831;
    for (int i = 0; i < 7831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7832(int& val) {
    func_in_7832(val);
    const int x = 7832;
    std::cout << 7832;
    for (int i = 0; i < 7832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7832(int& val) {
    const int x = 7832;
    std::cout << 7832;
    for (int i = 0; i < 7832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7833(int& val) {
    func_in_7833(val);
    const int x = 7833;
    std::cout << 7833;
    for (int i = 0; i < 7833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7833(int& val) {
    const int x = 7833;
    std::cout << 7833;
    for (int i = 0; i < 7833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7834(int& val) {
    func_in_7834(val);
    const int x = 7834;
    std::cout << 7834;
    for (int i = 0; i < 7834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7834(int& val) {
    const int x = 7834;
    std::cout << 7834;
    for (int i = 0; i < 7834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7835(int& val) {
    func_in_7835(val);
    const int x = 7835;
    std::cout << 7835;
    for (int i = 0; i < 7835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7835(int& val) {
    const int x = 7835;
    std::cout << 7835;
    for (int i = 0; i < 7835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7836(int& val) {
    func_in_7836(val);
    const int x = 7836;
    std::cout << 7836;
    for (int i = 0; i < 7836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7836(int& val) {
    const int x = 7836;
    std::cout << 7836;
    for (int i = 0; i < 7836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7837(int& val) {
    func_in_7837(val);
    const int x = 7837;
    std::cout << 7837;
    for (int i = 0; i < 7837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7837(int& val) {
    const int x = 7837;
    std::cout << 7837;
    for (int i = 0; i < 7837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7838(int& val) {
    func_in_7838(val);
    const int x = 7838;
    std::cout << 7838;
    for (int i = 0; i < 7838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7838(int& val) {
    const int x = 7838;
    std::cout << 7838;
    for (int i = 0; i < 7838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7839(int& val) {
    func_in_7839(val);
    const int x = 7839;
    std::cout << 7839;
    for (int i = 0; i < 7839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7839(int& val) {
    const int x = 7839;
    std::cout << 7839;
    for (int i = 0; i < 7839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7840(int& val) {
    func_in_7840(val);
    const int x = 7840;
    std::cout << 7840;
    for (int i = 0; i < 7840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7840(int& val) {
    const int x = 7840;
    std::cout << 7840;
    for (int i = 0; i < 7840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7841(int& val) {
    func_in_7841(val);
    const int x = 7841;
    std::cout << 7841;
    for (int i = 0; i < 7841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7841(int& val) {
    const int x = 7841;
    std::cout << 7841;
    for (int i = 0; i < 7841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7842(int& val) {
    func_in_7842(val);
    const int x = 7842;
    std::cout << 7842;
    for (int i = 0; i < 7842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7842(int& val) {
    const int x = 7842;
    std::cout << 7842;
    for (int i = 0; i < 7842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7843(int& val) {
    func_in_7843(val);
    const int x = 7843;
    std::cout << 7843;
    for (int i = 0; i < 7843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7843(int& val) {
    const int x = 7843;
    std::cout << 7843;
    for (int i = 0; i < 7843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7844(int& val) {
    func_in_7844(val);
    const int x = 7844;
    std::cout << 7844;
    for (int i = 0; i < 7844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7844(int& val) {
    const int x = 7844;
    std::cout << 7844;
    for (int i = 0; i < 7844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7845(int& val) {
    func_in_7845(val);
    const int x = 7845;
    std::cout << 7845;
    for (int i = 0; i < 7845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7845(int& val) {
    const int x = 7845;
    std::cout << 7845;
    for (int i = 0; i < 7845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7846(int& val) {
    func_in_7846(val);
    const int x = 7846;
    std::cout << 7846;
    for (int i = 0; i < 7846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7846(int& val) {
    const int x = 7846;
    std::cout << 7846;
    for (int i = 0; i < 7846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7847(int& val) {
    func_in_7847(val);
    const int x = 7847;
    std::cout << 7847;
    for (int i = 0; i < 7847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7847(int& val) {
    const int x = 7847;
    std::cout << 7847;
    for (int i = 0; i < 7847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7848(int& val) {
    func_in_7848(val);
    const int x = 7848;
    std::cout << 7848;
    for (int i = 0; i < 7848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7848(int& val) {
    const int x = 7848;
    std::cout << 7848;
    for (int i = 0; i < 7848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7849(int& val) {
    func_in_7849(val);
    const int x = 7849;
    std::cout << 7849;
    for (int i = 0; i < 7849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7849(int& val) {
    const int x = 7849;
    std::cout << 7849;
    for (int i = 0; i < 7849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7850(int& val) {
    func_in_7850(val);
    const int x = 7850;
    std::cout << 7850;
    for (int i = 0; i < 7850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7850(int& val) {
    const int x = 7850;
    std::cout << 7850;
    for (int i = 0; i < 7850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7851(int& val) {
    func_in_7851(val);
    const int x = 7851;
    std::cout << 7851;
    for (int i = 0; i < 7851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7851(int& val) {
    const int x = 7851;
    std::cout << 7851;
    for (int i = 0; i < 7851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7852(int& val) {
    func_in_7852(val);
    const int x = 7852;
    std::cout << 7852;
    for (int i = 0; i < 7852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7852(int& val) {
    const int x = 7852;
    std::cout << 7852;
    for (int i = 0; i < 7852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7853(int& val) {
    func_in_7853(val);
    const int x = 7853;
    std::cout << 7853;
    for (int i = 0; i < 7853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7853(int& val) {
    const int x = 7853;
    std::cout << 7853;
    for (int i = 0; i < 7853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7854(int& val) {
    func_in_7854(val);
    const int x = 7854;
    std::cout << 7854;
    for (int i = 0; i < 7854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7854(int& val) {
    const int x = 7854;
    std::cout << 7854;
    for (int i = 0; i < 7854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7855(int& val) {
    func_in_7855(val);
    const int x = 7855;
    std::cout << 7855;
    for (int i = 0; i < 7855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7855(int& val) {
    const int x = 7855;
    std::cout << 7855;
    for (int i = 0; i < 7855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7856(int& val) {
    func_in_7856(val);
    const int x = 7856;
    std::cout << 7856;
    for (int i = 0; i < 7856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7856(int& val) {
    const int x = 7856;
    std::cout << 7856;
    for (int i = 0; i < 7856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7857(int& val) {
    func_in_7857(val);
    const int x = 7857;
    std::cout << 7857;
    for (int i = 0; i < 7857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7857(int& val) {
    const int x = 7857;
    std::cout << 7857;
    for (int i = 0; i < 7857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7858(int& val) {
    func_in_7858(val);
    const int x = 7858;
    std::cout << 7858;
    for (int i = 0; i < 7858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7858(int& val) {
    const int x = 7858;
    std::cout << 7858;
    for (int i = 0; i < 7858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7859(int& val) {
    func_in_7859(val);
    const int x = 7859;
    std::cout << 7859;
    for (int i = 0; i < 7859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7859(int& val) {
    const int x = 7859;
    std::cout << 7859;
    for (int i = 0; i < 7859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7860(int& val) {
    func_in_7860(val);
    const int x = 7860;
    std::cout << 7860;
    for (int i = 0; i < 7860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7860(int& val) {
    const int x = 7860;
    std::cout << 7860;
    for (int i = 0; i < 7860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7861(int& val) {
    func_in_7861(val);
    const int x = 7861;
    std::cout << 7861;
    for (int i = 0; i < 7861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7861(int& val) {
    const int x = 7861;
    std::cout << 7861;
    for (int i = 0; i < 7861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7862(int& val) {
    func_in_7862(val);
    const int x = 7862;
    std::cout << 7862;
    for (int i = 0; i < 7862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7862(int& val) {
    const int x = 7862;
    std::cout << 7862;
    for (int i = 0; i < 7862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7863(int& val) {
    func_in_7863(val);
    const int x = 7863;
    std::cout << 7863;
    for (int i = 0; i < 7863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7863(int& val) {
    const int x = 7863;
    std::cout << 7863;
    for (int i = 0; i < 7863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7864(int& val) {
    func_in_7864(val);
    const int x = 7864;
    std::cout << 7864;
    for (int i = 0; i < 7864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7864(int& val) {
    const int x = 7864;
    std::cout << 7864;
    for (int i = 0; i < 7864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7865(int& val) {
    func_in_7865(val);
    const int x = 7865;
    std::cout << 7865;
    for (int i = 0; i < 7865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7865(int& val) {
    const int x = 7865;
    std::cout << 7865;
    for (int i = 0; i < 7865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7866(int& val) {
    func_in_7866(val);
    const int x = 7866;
    std::cout << 7866;
    for (int i = 0; i < 7866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7866(int& val) {
    const int x = 7866;
    std::cout << 7866;
    for (int i = 0; i < 7866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7867(int& val) {
    func_in_7867(val);
    const int x = 7867;
    std::cout << 7867;
    for (int i = 0; i < 7867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7867(int& val) {
    const int x = 7867;
    std::cout << 7867;
    for (int i = 0; i < 7867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7868(int& val) {
    func_in_7868(val);
    const int x = 7868;
    std::cout << 7868;
    for (int i = 0; i < 7868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7868(int& val) {
    const int x = 7868;
    std::cout << 7868;
    for (int i = 0; i < 7868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7869(int& val) {
    func_in_7869(val);
    const int x = 7869;
    std::cout << 7869;
    for (int i = 0; i < 7869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7869(int& val) {
    const int x = 7869;
    std::cout << 7869;
    for (int i = 0; i < 7869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7870(int& val) {
    func_in_7870(val);
    const int x = 7870;
    std::cout << 7870;
    for (int i = 0; i < 7870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7870(int& val) {
    const int x = 7870;
    std::cout << 7870;
    for (int i = 0; i < 7870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7871(int& val) {
    func_in_7871(val);
    const int x = 7871;
    std::cout << 7871;
    for (int i = 0; i < 7871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7871(int& val) {
    const int x = 7871;
    std::cout << 7871;
    for (int i = 0; i < 7871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7872(int& val) {
    func_in_7872(val);
    const int x = 7872;
    std::cout << 7872;
    for (int i = 0; i < 7872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7872(int& val) {
    const int x = 7872;
    std::cout << 7872;
    for (int i = 0; i < 7872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7873(int& val) {
    func_in_7873(val);
    const int x = 7873;
    std::cout << 7873;
    for (int i = 0; i < 7873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7873(int& val) {
    const int x = 7873;
    std::cout << 7873;
    for (int i = 0; i < 7873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7874(int& val) {
    func_in_7874(val);
    const int x = 7874;
    std::cout << 7874;
    for (int i = 0; i < 7874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7874(int& val) {
    const int x = 7874;
    std::cout << 7874;
    for (int i = 0; i < 7874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7875(int& val) {
    func_in_7875(val);
    const int x = 7875;
    std::cout << 7875;
    for (int i = 0; i < 7875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7875(int& val) {
    const int x = 7875;
    std::cout << 7875;
    for (int i = 0; i < 7875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7876(int& val) {
    func_in_7876(val);
    const int x = 7876;
    std::cout << 7876;
    for (int i = 0; i < 7876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7876(int& val) {
    const int x = 7876;
    std::cout << 7876;
    for (int i = 0; i < 7876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7877(int& val) {
    func_in_7877(val);
    const int x = 7877;
    std::cout << 7877;
    for (int i = 0; i < 7877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7877(int& val) {
    const int x = 7877;
    std::cout << 7877;
    for (int i = 0; i < 7877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7878(int& val) {
    func_in_7878(val);
    const int x = 7878;
    std::cout << 7878;
    for (int i = 0; i < 7878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7878(int& val) {
    const int x = 7878;
    std::cout << 7878;
    for (int i = 0; i < 7878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7879(int& val) {
    func_in_7879(val);
    const int x = 7879;
    std::cout << 7879;
    for (int i = 0; i < 7879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7879(int& val) {
    const int x = 7879;
    std::cout << 7879;
    for (int i = 0; i < 7879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7880(int& val) {
    func_in_7880(val);
    const int x = 7880;
    std::cout << 7880;
    for (int i = 0; i < 7880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7880(int& val) {
    const int x = 7880;
    std::cout << 7880;
    for (int i = 0; i < 7880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7881(int& val) {
    func_in_7881(val);
    const int x = 7881;
    std::cout << 7881;
    for (int i = 0; i < 7881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7881(int& val) {
    const int x = 7881;
    std::cout << 7881;
    for (int i = 0; i < 7881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7882(int& val) {
    func_in_7882(val);
    const int x = 7882;
    std::cout << 7882;
    for (int i = 0; i < 7882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7882(int& val) {
    const int x = 7882;
    std::cout << 7882;
    for (int i = 0; i < 7882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7883(int& val) {
    func_in_7883(val);
    const int x = 7883;
    std::cout << 7883;
    for (int i = 0; i < 7883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7883(int& val) {
    const int x = 7883;
    std::cout << 7883;
    for (int i = 0; i < 7883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7884(int& val) {
    func_in_7884(val);
    const int x = 7884;
    std::cout << 7884;
    for (int i = 0; i < 7884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7884(int& val) {
    const int x = 7884;
    std::cout << 7884;
    for (int i = 0; i < 7884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7885(int& val) {
    func_in_7885(val);
    const int x = 7885;
    std::cout << 7885;
    for (int i = 0; i < 7885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7885(int& val) {
    const int x = 7885;
    std::cout << 7885;
    for (int i = 0; i < 7885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7886(int& val) {
    func_in_7886(val);
    const int x = 7886;
    std::cout << 7886;
    for (int i = 0; i < 7886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7886(int& val) {
    const int x = 7886;
    std::cout << 7886;
    for (int i = 0; i < 7886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7887(int& val) {
    func_in_7887(val);
    const int x = 7887;
    std::cout << 7887;
    for (int i = 0; i < 7887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7887(int& val) {
    const int x = 7887;
    std::cout << 7887;
    for (int i = 0; i < 7887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7888(int& val) {
    func_in_7888(val);
    const int x = 7888;
    std::cout << 7888;
    for (int i = 0; i < 7888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7888(int& val) {
    const int x = 7888;
    std::cout << 7888;
    for (int i = 0; i < 7888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7889(int& val) {
    func_in_7889(val);
    const int x = 7889;
    std::cout << 7889;
    for (int i = 0; i < 7889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7889(int& val) {
    const int x = 7889;
    std::cout << 7889;
    for (int i = 0; i < 7889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7890(int& val) {
    func_in_7890(val);
    const int x = 7890;
    std::cout << 7890;
    for (int i = 0; i < 7890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7890(int& val) {
    const int x = 7890;
    std::cout << 7890;
    for (int i = 0; i < 7890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7891(int& val) {
    func_in_7891(val);
    const int x = 7891;
    std::cout << 7891;
    for (int i = 0; i < 7891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7891(int& val) {
    const int x = 7891;
    std::cout << 7891;
    for (int i = 0; i < 7891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7892(int& val) {
    func_in_7892(val);
    const int x = 7892;
    std::cout << 7892;
    for (int i = 0; i < 7892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7892(int& val) {
    const int x = 7892;
    std::cout << 7892;
    for (int i = 0; i < 7892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7893(int& val) {
    func_in_7893(val);
    const int x = 7893;
    std::cout << 7893;
    for (int i = 0; i < 7893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7893(int& val) {
    const int x = 7893;
    std::cout << 7893;
    for (int i = 0; i < 7893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7894(int& val) {
    func_in_7894(val);
    const int x = 7894;
    std::cout << 7894;
    for (int i = 0; i < 7894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7894(int& val) {
    const int x = 7894;
    std::cout << 7894;
    for (int i = 0; i < 7894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7895(int& val) {
    func_in_7895(val);
    const int x = 7895;
    std::cout << 7895;
    for (int i = 0; i < 7895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7895(int& val) {
    const int x = 7895;
    std::cout << 7895;
    for (int i = 0; i < 7895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7896(int& val) {
    func_in_7896(val);
    const int x = 7896;
    std::cout << 7896;
    for (int i = 0; i < 7896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7896(int& val) {
    const int x = 7896;
    std::cout << 7896;
    for (int i = 0; i < 7896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7897(int& val) {
    func_in_7897(val);
    const int x = 7897;
    std::cout << 7897;
    for (int i = 0; i < 7897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7897(int& val) {
    const int x = 7897;
    std::cout << 7897;
    for (int i = 0; i < 7897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7898(int& val) {
    func_in_7898(val);
    const int x = 7898;
    std::cout << 7898;
    for (int i = 0; i < 7898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7898(int& val) {
    const int x = 7898;
    std::cout << 7898;
    for (int i = 0; i < 7898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7899(int& val) {
    func_in_7899(val);
    const int x = 7899;
    std::cout << 7899;
    for (int i = 0; i < 7899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7899(int& val) {
    const int x = 7899;
    std::cout << 7899;
    for (int i = 0; i < 7899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7900(int& val) {
    func_in_7900(val);
    const int x = 7900;
    std::cout << 7900;
    for (int i = 0; i < 7900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7900(int& val) {
    const int x = 7900;
    std::cout << 7900;
    for (int i = 0; i < 7900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7901(int& val) {
    func_in_7901(val);
    const int x = 7901;
    std::cout << 7901;
    for (int i = 0; i < 7901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7901(int& val) {
    const int x = 7901;
    std::cout << 7901;
    for (int i = 0; i < 7901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7902(int& val) {
    func_in_7902(val);
    const int x = 7902;
    std::cout << 7902;
    for (int i = 0; i < 7902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7902(int& val) {
    const int x = 7902;
    std::cout << 7902;
    for (int i = 0; i < 7902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7903(int& val) {
    func_in_7903(val);
    const int x = 7903;
    std::cout << 7903;
    for (int i = 0; i < 7903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7903(int& val) {
    const int x = 7903;
    std::cout << 7903;
    for (int i = 0; i < 7903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7904(int& val) {
    func_in_7904(val);
    const int x = 7904;
    std::cout << 7904;
    for (int i = 0; i < 7904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7904(int& val) {
    const int x = 7904;
    std::cout << 7904;
    for (int i = 0; i < 7904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7905(int& val) {
    func_in_7905(val);
    const int x = 7905;
    std::cout << 7905;
    for (int i = 0; i < 7905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7905(int& val) {
    const int x = 7905;
    std::cout << 7905;
    for (int i = 0; i < 7905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7906(int& val) {
    func_in_7906(val);
    const int x = 7906;
    std::cout << 7906;
    for (int i = 0; i < 7906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7906(int& val) {
    const int x = 7906;
    std::cout << 7906;
    for (int i = 0; i < 7906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7907(int& val) {
    func_in_7907(val);
    const int x = 7907;
    std::cout << 7907;
    for (int i = 0; i < 7907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7907(int& val) {
    const int x = 7907;
    std::cout << 7907;
    for (int i = 0; i < 7907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7908(int& val) {
    func_in_7908(val);
    const int x = 7908;
    std::cout << 7908;
    for (int i = 0; i < 7908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7908(int& val) {
    const int x = 7908;
    std::cout << 7908;
    for (int i = 0; i < 7908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7909(int& val) {
    func_in_7909(val);
    const int x = 7909;
    std::cout << 7909;
    for (int i = 0; i < 7909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7909(int& val) {
    const int x = 7909;
    std::cout << 7909;
    for (int i = 0; i < 7909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7910(int& val) {
    func_in_7910(val);
    const int x = 7910;
    std::cout << 7910;
    for (int i = 0; i < 7910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7910(int& val) {
    const int x = 7910;
    std::cout << 7910;
    for (int i = 0; i < 7910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7911(int& val) {
    func_in_7911(val);
    const int x = 7911;
    std::cout << 7911;
    for (int i = 0; i < 7911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7911(int& val) {
    const int x = 7911;
    std::cout << 7911;
    for (int i = 0; i < 7911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7912(int& val) {
    func_in_7912(val);
    const int x = 7912;
    std::cout << 7912;
    for (int i = 0; i < 7912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7912(int& val) {
    const int x = 7912;
    std::cout << 7912;
    for (int i = 0; i < 7912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7913(int& val) {
    func_in_7913(val);
    const int x = 7913;
    std::cout << 7913;
    for (int i = 0; i < 7913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7913(int& val) {
    const int x = 7913;
    std::cout << 7913;
    for (int i = 0; i < 7913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7914(int& val) {
    func_in_7914(val);
    const int x = 7914;
    std::cout << 7914;
    for (int i = 0; i < 7914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7914(int& val) {
    const int x = 7914;
    std::cout << 7914;
    for (int i = 0; i < 7914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7915(int& val) {
    func_in_7915(val);
    const int x = 7915;
    std::cout << 7915;
    for (int i = 0; i < 7915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7915(int& val) {
    const int x = 7915;
    std::cout << 7915;
    for (int i = 0; i < 7915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7916(int& val) {
    func_in_7916(val);
    const int x = 7916;
    std::cout << 7916;
    for (int i = 0; i < 7916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7916(int& val) {
    const int x = 7916;
    std::cout << 7916;
    for (int i = 0; i < 7916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7917(int& val) {
    func_in_7917(val);
    const int x = 7917;
    std::cout << 7917;
    for (int i = 0; i < 7917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7917(int& val) {
    const int x = 7917;
    std::cout << 7917;
    for (int i = 0; i < 7917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7918(int& val) {
    func_in_7918(val);
    const int x = 7918;
    std::cout << 7918;
    for (int i = 0; i < 7918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7918(int& val) {
    const int x = 7918;
    std::cout << 7918;
    for (int i = 0; i < 7918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7919(int& val) {
    func_in_7919(val);
    const int x = 7919;
    std::cout << 7919;
    for (int i = 0; i < 7919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7919(int& val) {
    const int x = 7919;
    std::cout << 7919;
    for (int i = 0; i < 7919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7920(int& val) {
    func_in_7920(val);
    const int x = 7920;
    std::cout << 7920;
    for (int i = 0; i < 7920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7920(int& val) {
    const int x = 7920;
    std::cout << 7920;
    for (int i = 0; i < 7920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7921(int& val) {
    func_in_7921(val);
    const int x = 7921;
    std::cout << 7921;
    for (int i = 0; i < 7921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7921(int& val) {
    const int x = 7921;
    std::cout << 7921;
    for (int i = 0; i < 7921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7922(int& val) {
    func_in_7922(val);
    const int x = 7922;
    std::cout << 7922;
    for (int i = 0; i < 7922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7922(int& val) {
    const int x = 7922;
    std::cout << 7922;
    for (int i = 0; i < 7922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7923(int& val) {
    func_in_7923(val);
    const int x = 7923;
    std::cout << 7923;
    for (int i = 0; i < 7923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7923(int& val) {
    const int x = 7923;
    std::cout << 7923;
    for (int i = 0; i < 7923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7924(int& val) {
    func_in_7924(val);
    const int x = 7924;
    std::cout << 7924;
    for (int i = 0; i < 7924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7924(int& val) {
    const int x = 7924;
    std::cout << 7924;
    for (int i = 0; i < 7924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7925(int& val) {
    func_in_7925(val);
    const int x = 7925;
    std::cout << 7925;
    for (int i = 0; i < 7925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7925(int& val) {
    const int x = 7925;
    std::cout << 7925;
    for (int i = 0; i < 7925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7926(int& val) {
    func_in_7926(val);
    const int x = 7926;
    std::cout << 7926;
    for (int i = 0; i < 7926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7926(int& val) {
    const int x = 7926;
    std::cout << 7926;
    for (int i = 0; i < 7926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7927(int& val) {
    func_in_7927(val);
    const int x = 7927;
    std::cout << 7927;
    for (int i = 0; i < 7927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7927(int& val) {
    const int x = 7927;
    std::cout << 7927;
    for (int i = 0; i < 7927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7928(int& val) {
    func_in_7928(val);
    const int x = 7928;
    std::cout << 7928;
    for (int i = 0; i < 7928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7928(int& val) {
    const int x = 7928;
    std::cout << 7928;
    for (int i = 0; i < 7928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7929(int& val) {
    func_in_7929(val);
    const int x = 7929;
    std::cout << 7929;
    for (int i = 0; i < 7929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7929(int& val) {
    const int x = 7929;
    std::cout << 7929;
    for (int i = 0; i < 7929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7930(int& val) {
    func_in_7930(val);
    const int x = 7930;
    std::cout << 7930;
    for (int i = 0; i < 7930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7930(int& val) {
    const int x = 7930;
    std::cout << 7930;
    for (int i = 0; i < 7930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7931(int& val) {
    func_in_7931(val);
    const int x = 7931;
    std::cout << 7931;
    for (int i = 0; i < 7931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7931(int& val) {
    const int x = 7931;
    std::cout << 7931;
    for (int i = 0; i < 7931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7932(int& val) {
    func_in_7932(val);
    const int x = 7932;
    std::cout << 7932;
    for (int i = 0; i < 7932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7932(int& val) {
    const int x = 7932;
    std::cout << 7932;
    for (int i = 0; i < 7932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7933(int& val) {
    func_in_7933(val);
    const int x = 7933;
    std::cout << 7933;
    for (int i = 0; i < 7933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7933(int& val) {
    const int x = 7933;
    std::cout << 7933;
    for (int i = 0; i < 7933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7934(int& val) {
    func_in_7934(val);
    const int x = 7934;
    std::cout << 7934;
    for (int i = 0; i < 7934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7934(int& val) {
    const int x = 7934;
    std::cout << 7934;
    for (int i = 0; i < 7934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7935(int& val) {
    func_in_7935(val);
    const int x = 7935;
    std::cout << 7935;
    for (int i = 0; i < 7935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7935(int& val) {
    const int x = 7935;
    std::cout << 7935;
    for (int i = 0; i < 7935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7936(int& val) {
    func_in_7936(val);
    const int x = 7936;
    std::cout << 7936;
    for (int i = 0; i < 7936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7936(int& val) {
    const int x = 7936;
    std::cout << 7936;
    for (int i = 0; i < 7936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7937(int& val) {
    func_in_7937(val);
    const int x = 7937;
    std::cout << 7937;
    for (int i = 0; i < 7937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7937(int& val) {
    const int x = 7937;
    std::cout << 7937;
    for (int i = 0; i < 7937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7938(int& val) {
    func_in_7938(val);
    const int x = 7938;
    std::cout << 7938;
    for (int i = 0; i < 7938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7938(int& val) {
    const int x = 7938;
    std::cout << 7938;
    for (int i = 0; i < 7938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7939(int& val) {
    func_in_7939(val);
    const int x = 7939;
    std::cout << 7939;
    for (int i = 0; i < 7939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7939(int& val) {
    const int x = 7939;
    std::cout << 7939;
    for (int i = 0; i < 7939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7940(int& val) {
    func_in_7940(val);
    const int x = 7940;
    std::cout << 7940;
    for (int i = 0; i < 7940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7940(int& val) {
    const int x = 7940;
    std::cout << 7940;
    for (int i = 0; i < 7940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7941(int& val) {
    func_in_7941(val);
    const int x = 7941;
    std::cout << 7941;
    for (int i = 0; i < 7941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7941(int& val) {
    const int x = 7941;
    std::cout << 7941;
    for (int i = 0; i < 7941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7942(int& val) {
    func_in_7942(val);
    const int x = 7942;
    std::cout << 7942;
    for (int i = 0; i < 7942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7942(int& val) {
    const int x = 7942;
    std::cout << 7942;
    for (int i = 0; i < 7942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7943(int& val) {
    func_in_7943(val);
    const int x = 7943;
    std::cout << 7943;
    for (int i = 0; i < 7943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7943(int& val) {
    const int x = 7943;
    std::cout << 7943;
    for (int i = 0; i < 7943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7944(int& val) {
    func_in_7944(val);
    const int x = 7944;
    std::cout << 7944;
    for (int i = 0; i < 7944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7944(int& val) {
    const int x = 7944;
    std::cout << 7944;
    for (int i = 0; i < 7944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7945(int& val) {
    func_in_7945(val);
    const int x = 7945;
    std::cout << 7945;
    for (int i = 0; i < 7945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7945(int& val) {
    const int x = 7945;
    std::cout << 7945;
    for (int i = 0; i < 7945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7946(int& val) {
    func_in_7946(val);
    const int x = 7946;
    std::cout << 7946;
    for (int i = 0; i < 7946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7946(int& val) {
    const int x = 7946;
    std::cout << 7946;
    for (int i = 0; i < 7946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7947(int& val) {
    func_in_7947(val);
    const int x = 7947;
    std::cout << 7947;
    for (int i = 0; i < 7947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7947(int& val) {
    const int x = 7947;
    std::cout << 7947;
    for (int i = 0; i < 7947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7948(int& val) {
    func_in_7948(val);
    const int x = 7948;
    std::cout << 7948;
    for (int i = 0; i < 7948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7948(int& val) {
    const int x = 7948;
    std::cout << 7948;
    for (int i = 0; i < 7948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7949(int& val) {
    func_in_7949(val);
    const int x = 7949;
    std::cout << 7949;
    for (int i = 0; i < 7949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7949(int& val) {
    const int x = 7949;
    std::cout << 7949;
    for (int i = 0; i < 7949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7950(int& val) {
    func_in_7950(val);
    const int x = 7950;
    std::cout << 7950;
    for (int i = 0; i < 7950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7950(int& val) {
    const int x = 7950;
    std::cout << 7950;
    for (int i = 0; i < 7950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7951(int& val) {
    func_in_7951(val);
    const int x = 7951;
    std::cout << 7951;
    for (int i = 0; i < 7951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7951(int& val) {
    const int x = 7951;
    std::cout << 7951;
    for (int i = 0; i < 7951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7952(int& val) {
    func_in_7952(val);
    const int x = 7952;
    std::cout << 7952;
    for (int i = 0; i < 7952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7952(int& val) {
    const int x = 7952;
    std::cout << 7952;
    for (int i = 0; i < 7952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7953(int& val) {
    func_in_7953(val);
    const int x = 7953;
    std::cout << 7953;
    for (int i = 0; i < 7953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7953(int& val) {
    const int x = 7953;
    std::cout << 7953;
    for (int i = 0; i < 7953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7954(int& val) {
    func_in_7954(val);
    const int x = 7954;
    std::cout << 7954;
    for (int i = 0; i < 7954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7954(int& val) {
    const int x = 7954;
    std::cout << 7954;
    for (int i = 0; i < 7954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7955(int& val) {
    func_in_7955(val);
    const int x = 7955;
    std::cout << 7955;
    for (int i = 0; i < 7955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7955(int& val) {
    const int x = 7955;
    std::cout << 7955;
    for (int i = 0; i < 7955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7956(int& val) {
    func_in_7956(val);
    const int x = 7956;
    std::cout << 7956;
    for (int i = 0; i < 7956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7956(int& val) {
    const int x = 7956;
    std::cout << 7956;
    for (int i = 0; i < 7956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7957(int& val) {
    func_in_7957(val);
    const int x = 7957;
    std::cout << 7957;
    for (int i = 0; i < 7957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7957(int& val) {
    const int x = 7957;
    std::cout << 7957;
    for (int i = 0; i < 7957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7958(int& val) {
    func_in_7958(val);
    const int x = 7958;
    std::cout << 7958;
    for (int i = 0; i < 7958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7958(int& val) {
    const int x = 7958;
    std::cout << 7958;
    for (int i = 0; i < 7958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7959(int& val) {
    func_in_7959(val);
    const int x = 7959;
    std::cout << 7959;
    for (int i = 0; i < 7959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7959(int& val) {
    const int x = 7959;
    std::cout << 7959;
    for (int i = 0; i < 7959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7960(int& val) {
    func_in_7960(val);
    const int x = 7960;
    std::cout << 7960;
    for (int i = 0; i < 7960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7960(int& val) {
    const int x = 7960;
    std::cout << 7960;
    for (int i = 0; i < 7960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7961(int& val) {
    func_in_7961(val);
    const int x = 7961;
    std::cout << 7961;
    for (int i = 0; i < 7961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7961(int& val) {
    const int x = 7961;
    std::cout << 7961;
    for (int i = 0; i < 7961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7962(int& val) {
    func_in_7962(val);
    const int x = 7962;
    std::cout << 7962;
    for (int i = 0; i < 7962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7962(int& val) {
    const int x = 7962;
    std::cout << 7962;
    for (int i = 0; i < 7962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7963(int& val) {
    func_in_7963(val);
    const int x = 7963;
    std::cout << 7963;
    for (int i = 0; i < 7963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7963(int& val) {
    const int x = 7963;
    std::cout << 7963;
    for (int i = 0; i < 7963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7964(int& val) {
    func_in_7964(val);
    const int x = 7964;
    std::cout << 7964;
    for (int i = 0; i < 7964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7964(int& val) {
    const int x = 7964;
    std::cout << 7964;
    for (int i = 0; i < 7964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7965(int& val) {
    func_in_7965(val);
    const int x = 7965;
    std::cout << 7965;
    for (int i = 0; i < 7965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7965(int& val) {
    const int x = 7965;
    std::cout << 7965;
    for (int i = 0; i < 7965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7966(int& val) {
    func_in_7966(val);
    const int x = 7966;
    std::cout << 7966;
    for (int i = 0; i < 7966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7966(int& val) {
    const int x = 7966;
    std::cout << 7966;
    for (int i = 0; i < 7966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7967(int& val) {
    func_in_7967(val);
    const int x = 7967;
    std::cout << 7967;
    for (int i = 0; i < 7967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7967(int& val) {
    const int x = 7967;
    std::cout << 7967;
    for (int i = 0; i < 7967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7968(int& val) {
    func_in_7968(val);
    const int x = 7968;
    std::cout << 7968;
    for (int i = 0; i < 7968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7968(int& val) {
    const int x = 7968;
    std::cout << 7968;
    for (int i = 0; i < 7968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7969(int& val) {
    func_in_7969(val);
    const int x = 7969;
    std::cout << 7969;
    for (int i = 0; i < 7969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7969(int& val) {
    const int x = 7969;
    std::cout << 7969;
    for (int i = 0; i < 7969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7970(int& val) {
    func_in_7970(val);
    const int x = 7970;
    std::cout << 7970;
    for (int i = 0; i < 7970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7970(int& val) {
    const int x = 7970;
    std::cout << 7970;
    for (int i = 0; i < 7970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7971(int& val) {
    func_in_7971(val);
    const int x = 7971;
    std::cout << 7971;
    for (int i = 0; i < 7971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7971(int& val) {
    const int x = 7971;
    std::cout << 7971;
    for (int i = 0; i < 7971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7972(int& val) {
    func_in_7972(val);
    const int x = 7972;
    std::cout << 7972;
    for (int i = 0; i < 7972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7972(int& val) {
    const int x = 7972;
    std::cout << 7972;
    for (int i = 0; i < 7972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7973(int& val) {
    func_in_7973(val);
    const int x = 7973;
    std::cout << 7973;
    for (int i = 0; i < 7973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7973(int& val) {
    const int x = 7973;
    std::cout << 7973;
    for (int i = 0; i < 7973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7974(int& val) {
    func_in_7974(val);
    const int x = 7974;
    std::cout << 7974;
    for (int i = 0; i < 7974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7974(int& val) {
    const int x = 7974;
    std::cout << 7974;
    for (int i = 0; i < 7974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7975(int& val) {
    func_in_7975(val);
    const int x = 7975;
    std::cout << 7975;
    for (int i = 0; i < 7975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7975(int& val) {
    const int x = 7975;
    std::cout << 7975;
    for (int i = 0; i < 7975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7976(int& val) {
    func_in_7976(val);
    const int x = 7976;
    std::cout << 7976;
    for (int i = 0; i < 7976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7976(int& val) {
    const int x = 7976;
    std::cout << 7976;
    for (int i = 0; i < 7976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7977(int& val) {
    func_in_7977(val);
    const int x = 7977;
    std::cout << 7977;
    for (int i = 0; i < 7977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7977(int& val) {
    const int x = 7977;
    std::cout << 7977;
    for (int i = 0; i < 7977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7978(int& val) {
    func_in_7978(val);
    const int x = 7978;
    std::cout << 7978;
    for (int i = 0; i < 7978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7978(int& val) {
    const int x = 7978;
    std::cout << 7978;
    for (int i = 0; i < 7978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7979(int& val) {
    func_in_7979(val);
    const int x = 7979;
    std::cout << 7979;
    for (int i = 0; i < 7979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7979(int& val) {
    const int x = 7979;
    std::cout << 7979;
    for (int i = 0; i < 7979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7980(int& val) {
    func_in_7980(val);
    const int x = 7980;
    std::cout << 7980;
    for (int i = 0; i < 7980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7980(int& val) {
    const int x = 7980;
    std::cout << 7980;
    for (int i = 0; i < 7980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7981(int& val) {
    func_in_7981(val);
    const int x = 7981;
    std::cout << 7981;
    for (int i = 0; i < 7981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7981(int& val) {
    const int x = 7981;
    std::cout << 7981;
    for (int i = 0; i < 7981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7982(int& val) {
    func_in_7982(val);
    const int x = 7982;
    std::cout << 7982;
    for (int i = 0; i < 7982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7982(int& val) {
    const int x = 7982;
    std::cout << 7982;
    for (int i = 0; i < 7982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7983(int& val) {
    func_in_7983(val);
    const int x = 7983;
    std::cout << 7983;
    for (int i = 0; i < 7983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7983(int& val) {
    const int x = 7983;
    std::cout << 7983;
    for (int i = 0; i < 7983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7984(int& val) {
    func_in_7984(val);
    const int x = 7984;
    std::cout << 7984;
    for (int i = 0; i < 7984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7984(int& val) {
    const int x = 7984;
    std::cout << 7984;
    for (int i = 0; i < 7984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7985(int& val) {
    func_in_7985(val);
    const int x = 7985;
    std::cout << 7985;
    for (int i = 0; i < 7985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7985(int& val) {
    const int x = 7985;
    std::cout << 7985;
    for (int i = 0; i < 7985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7986(int& val) {
    func_in_7986(val);
    const int x = 7986;
    std::cout << 7986;
    for (int i = 0; i < 7986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7986(int& val) {
    const int x = 7986;
    std::cout << 7986;
    for (int i = 0; i < 7986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7987(int& val) {
    func_in_7987(val);
    const int x = 7987;
    std::cout << 7987;
    for (int i = 0; i < 7987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7987(int& val) {
    const int x = 7987;
    std::cout << 7987;
    for (int i = 0; i < 7987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7988(int& val) {
    func_in_7988(val);
    const int x = 7988;
    std::cout << 7988;
    for (int i = 0; i < 7988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7988(int& val) {
    const int x = 7988;
    std::cout << 7988;
    for (int i = 0; i < 7988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7989(int& val) {
    func_in_7989(val);
    const int x = 7989;
    std::cout << 7989;
    for (int i = 0; i < 7989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7989(int& val) {
    const int x = 7989;
    std::cout << 7989;
    for (int i = 0; i < 7989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7990(int& val) {
    func_in_7990(val);
    const int x = 7990;
    std::cout << 7990;
    for (int i = 0; i < 7990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7990(int& val) {
    const int x = 7990;
    std::cout << 7990;
    for (int i = 0; i < 7990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7991(int& val) {
    func_in_7991(val);
    const int x = 7991;
    std::cout << 7991;
    for (int i = 0; i < 7991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7991(int& val) {
    const int x = 7991;
    std::cout << 7991;
    for (int i = 0; i < 7991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7992(int& val) {
    func_in_7992(val);
    const int x = 7992;
    std::cout << 7992;
    for (int i = 0; i < 7992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7992(int& val) {
    const int x = 7992;
    std::cout << 7992;
    for (int i = 0; i < 7992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7993(int& val) {
    func_in_7993(val);
    const int x = 7993;
    std::cout << 7993;
    for (int i = 0; i < 7993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7993(int& val) {
    const int x = 7993;
    std::cout << 7993;
    for (int i = 0; i < 7993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7994(int& val) {
    func_in_7994(val);
    const int x = 7994;
    std::cout << 7994;
    for (int i = 0; i < 7994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7994(int& val) {
    const int x = 7994;
    std::cout << 7994;
    for (int i = 0; i < 7994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7995(int& val) {
    func_in_7995(val);
    const int x = 7995;
    std::cout << 7995;
    for (int i = 0; i < 7995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7995(int& val) {
    const int x = 7995;
    std::cout << 7995;
    for (int i = 0; i < 7995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7996(int& val) {
    func_in_7996(val);
    const int x = 7996;
    std::cout << 7996;
    for (int i = 0; i < 7996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7996(int& val) {
    const int x = 7996;
    std::cout << 7996;
    for (int i = 0; i < 7996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7997(int& val) {
    func_in_7997(val);
    const int x = 7997;
    std::cout << 7997;
    for (int i = 0; i < 7997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7997(int& val) {
    const int x = 7997;
    std::cout << 7997;
    for (int i = 0; i < 7997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7998(int& val) {
    func_in_7998(val);
    const int x = 7998;
    std::cout << 7998;
    for (int i = 0; i < 7998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7998(int& val) {
    const int x = 7998;
    std::cout << 7998;
    for (int i = 0; i < 7998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7999(int& val) {
    func_in_7999(val);
    const int x = 7999;
    std::cout << 7999;
    for (int i = 0; i < 7999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7999(int& val) {
    const int x = 7999;
    std::cout << 7999;
    for (int i = 0; i < 7999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

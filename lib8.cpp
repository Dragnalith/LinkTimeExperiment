#include "lib.h"
#include <iostream>
int func_32000(int& val) {
    func_in_32000(val);
    const int x = 32000;
    std::cout << 32000;
    for (int i = 0; i < 32000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32000(int& val) {
    const int x = 32000;
    std::cout << 32000;
    for (int i = 0; i < 32000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32001(int& val) {
    func_in_32001(val);
    const int x = 32001;
    std::cout << 32001;
    for (int i = 0; i < 32001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32001(int& val) {
    const int x = 32001;
    std::cout << 32001;
    for (int i = 0; i < 32001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32002(int& val) {
    func_in_32002(val);
    const int x = 32002;
    std::cout << 32002;
    for (int i = 0; i < 32002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32002(int& val) {
    const int x = 32002;
    std::cout << 32002;
    for (int i = 0; i < 32002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32003(int& val) {
    func_in_32003(val);
    const int x = 32003;
    std::cout << 32003;
    for (int i = 0; i < 32003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32003(int& val) {
    const int x = 32003;
    std::cout << 32003;
    for (int i = 0; i < 32003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32004(int& val) {
    func_in_32004(val);
    const int x = 32004;
    std::cout << 32004;
    for (int i = 0; i < 32004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32004(int& val) {
    const int x = 32004;
    std::cout << 32004;
    for (int i = 0; i < 32004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32005(int& val) {
    func_in_32005(val);
    const int x = 32005;
    std::cout << 32005;
    for (int i = 0; i < 32005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32005(int& val) {
    const int x = 32005;
    std::cout << 32005;
    for (int i = 0; i < 32005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32006(int& val) {
    func_in_32006(val);
    const int x = 32006;
    std::cout << 32006;
    for (int i = 0; i < 32006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32006(int& val) {
    const int x = 32006;
    std::cout << 32006;
    for (int i = 0; i < 32006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32007(int& val) {
    func_in_32007(val);
    const int x = 32007;
    std::cout << 32007;
    for (int i = 0; i < 32007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32007(int& val) {
    const int x = 32007;
    std::cout << 32007;
    for (int i = 0; i < 32007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32008(int& val) {
    func_in_32008(val);
    const int x = 32008;
    std::cout << 32008;
    for (int i = 0; i < 32008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32008(int& val) {
    const int x = 32008;
    std::cout << 32008;
    for (int i = 0; i < 32008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32009(int& val) {
    func_in_32009(val);
    const int x = 32009;
    std::cout << 32009;
    for (int i = 0; i < 32009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32009(int& val) {
    const int x = 32009;
    std::cout << 32009;
    for (int i = 0; i < 32009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32010(int& val) {
    func_in_32010(val);
    const int x = 32010;
    std::cout << 32010;
    for (int i = 0; i < 32010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32010(int& val) {
    const int x = 32010;
    std::cout << 32010;
    for (int i = 0; i < 32010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32011(int& val) {
    func_in_32011(val);
    const int x = 32011;
    std::cout << 32011;
    for (int i = 0; i < 32011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32011(int& val) {
    const int x = 32011;
    std::cout << 32011;
    for (int i = 0; i < 32011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32012(int& val) {
    func_in_32012(val);
    const int x = 32012;
    std::cout << 32012;
    for (int i = 0; i < 32012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32012(int& val) {
    const int x = 32012;
    std::cout << 32012;
    for (int i = 0; i < 32012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32013(int& val) {
    func_in_32013(val);
    const int x = 32013;
    std::cout << 32013;
    for (int i = 0; i < 32013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32013(int& val) {
    const int x = 32013;
    std::cout << 32013;
    for (int i = 0; i < 32013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32014(int& val) {
    func_in_32014(val);
    const int x = 32014;
    std::cout << 32014;
    for (int i = 0; i < 32014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32014(int& val) {
    const int x = 32014;
    std::cout << 32014;
    for (int i = 0; i < 32014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32015(int& val) {
    func_in_32015(val);
    const int x = 32015;
    std::cout << 32015;
    for (int i = 0; i < 32015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32015(int& val) {
    const int x = 32015;
    std::cout << 32015;
    for (int i = 0; i < 32015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32016(int& val) {
    func_in_32016(val);
    const int x = 32016;
    std::cout << 32016;
    for (int i = 0; i < 32016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32016(int& val) {
    const int x = 32016;
    std::cout << 32016;
    for (int i = 0; i < 32016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32017(int& val) {
    func_in_32017(val);
    const int x = 32017;
    std::cout << 32017;
    for (int i = 0; i < 32017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32017(int& val) {
    const int x = 32017;
    std::cout << 32017;
    for (int i = 0; i < 32017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32018(int& val) {
    func_in_32018(val);
    const int x = 32018;
    std::cout << 32018;
    for (int i = 0; i < 32018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32018(int& val) {
    const int x = 32018;
    std::cout << 32018;
    for (int i = 0; i < 32018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32019(int& val) {
    func_in_32019(val);
    const int x = 32019;
    std::cout << 32019;
    for (int i = 0; i < 32019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32019(int& val) {
    const int x = 32019;
    std::cout << 32019;
    for (int i = 0; i < 32019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32020(int& val) {
    func_in_32020(val);
    const int x = 32020;
    std::cout << 32020;
    for (int i = 0; i < 32020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32020(int& val) {
    const int x = 32020;
    std::cout << 32020;
    for (int i = 0; i < 32020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32021(int& val) {
    func_in_32021(val);
    const int x = 32021;
    std::cout << 32021;
    for (int i = 0; i < 32021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32021(int& val) {
    const int x = 32021;
    std::cout << 32021;
    for (int i = 0; i < 32021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32022(int& val) {
    func_in_32022(val);
    const int x = 32022;
    std::cout << 32022;
    for (int i = 0; i < 32022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32022(int& val) {
    const int x = 32022;
    std::cout << 32022;
    for (int i = 0; i < 32022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32023(int& val) {
    func_in_32023(val);
    const int x = 32023;
    std::cout << 32023;
    for (int i = 0; i < 32023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32023(int& val) {
    const int x = 32023;
    std::cout << 32023;
    for (int i = 0; i < 32023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32024(int& val) {
    func_in_32024(val);
    const int x = 32024;
    std::cout << 32024;
    for (int i = 0; i < 32024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32024(int& val) {
    const int x = 32024;
    std::cout << 32024;
    for (int i = 0; i < 32024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32025(int& val) {
    func_in_32025(val);
    const int x = 32025;
    std::cout << 32025;
    for (int i = 0; i < 32025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32025(int& val) {
    const int x = 32025;
    std::cout << 32025;
    for (int i = 0; i < 32025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32026(int& val) {
    func_in_32026(val);
    const int x = 32026;
    std::cout << 32026;
    for (int i = 0; i < 32026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32026(int& val) {
    const int x = 32026;
    std::cout << 32026;
    for (int i = 0; i < 32026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32027(int& val) {
    func_in_32027(val);
    const int x = 32027;
    std::cout << 32027;
    for (int i = 0; i < 32027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32027(int& val) {
    const int x = 32027;
    std::cout << 32027;
    for (int i = 0; i < 32027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32028(int& val) {
    func_in_32028(val);
    const int x = 32028;
    std::cout << 32028;
    for (int i = 0; i < 32028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32028(int& val) {
    const int x = 32028;
    std::cout << 32028;
    for (int i = 0; i < 32028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32029(int& val) {
    func_in_32029(val);
    const int x = 32029;
    std::cout << 32029;
    for (int i = 0; i < 32029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32029(int& val) {
    const int x = 32029;
    std::cout << 32029;
    for (int i = 0; i < 32029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32030(int& val) {
    func_in_32030(val);
    const int x = 32030;
    std::cout << 32030;
    for (int i = 0; i < 32030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32030(int& val) {
    const int x = 32030;
    std::cout << 32030;
    for (int i = 0; i < 32030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32031(int& val) {
    func_in_32031(val);
    const int x = 32031;
    std::cout << 32031;
    for (int i = 0; i < 32031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32031(int& val) {
    const int x = 32031;
    std::cout << 32031;
    for (int i = 0; i < 32031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32032(int& val) {
    func_in_32032(val);
    const int x = 32032;
    std::cout << 32032;
    for (int i = 0; i < 32032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32032(int& val) {
    const int x = 32032;
    std::cout << 32032;
    for (int i = 0; i < 32032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32033(int& val) {
    func_in_32033(val);
    const int x = 32033;
    std::cout << 32033;
    for (int i = 0; i < 32033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32033(int& val) {
    const int x = 32033;
    std::cout << 32033;
    for (int i = 0; i < 32033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32034(int& val) {
    func_in_32034(val);
    const int x = 32034;
    std::cout << 32034;
    for (int i = 0; i < 32034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32034(int& val) {
    const int x = 32034;
    std::cout << 32034;
    for (int i = 0; i < 32034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32035(int& val) {
    func_in_32035(val);
    const int x = 32035;
    std::cout << 32035;
    for (int i = 0; i < 32035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32035(int& val) {
    const int x = 32035;
    std::cout << 32035;
    for (int i = 0; i < 32035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32036(int& val) {
    func_in_32036(val);
    const int x = 32036;
    std::cout << 32036;
    for (int i = 0; i < 32036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32036(int& val) {
    const int x = 32036;
    std::cout << 32036;
    for (int i = 0; i < 32036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32037(int& val) {
    func_in_32037(val);
    const int x = 32037;
    std::cout << 32037;
    for (int i = 0; i < 32037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32037(int& val) {
    const int x = 32037;
    std::cout << 32037;
    for (int i = 0; i < 32037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32038(int& val) {
    func_in_32038(val);
    const int x = 32038;
    std::cout << 32038;
    for (int i = 0; i < 32038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32038(int& val) {
    const int x = 32038;
    std::cout << 32038;
    for (int i = 0; i < 32038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32039(int& val) {
    func_in_32039(val);
    const int x = 32039;
    std::cout << 32039;
    for (int i = 0; i < 32039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32039(int& val) {
    const int x = 32039;
    std::cout << 32039;
    for (int i = 0; i < 32039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32040(int& val) {
    func_in_32040(val);
    const int x = 32040;
    std::cout << 32040;
    for (int i = 0; i < 32040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32040(int& val) {
    const int x = 32040;
    std::cout << 32040;
    for (int i = 0; i < 32040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32041(int& val) {
    func_in_32041(val);
    const int x = 32041;
    std::cout << 32041;
    for (int i = 0; i < 32041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32041(int& val) {
    const int x = 32041;
    std::cout << 32041;
    for (int i = 0; i < 32041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32042(int& val) {
    func_in_32042(val);
    const int x = 32042;
    std::cout << 32042;
    for (int i = 0; i < 32042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32042(int& val) {
    const int x = 32042;
    std::cout << 32042;
    for (int i = 0; i < 32042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32043(int& val) {
    func_in_32043(val);
    const int x = 32043;
    std::cout << 32043;
    for (int i = 0; i < 32043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32043(int& val) {
    const int x = 32043;
    std::cout << 32043;
    for (int i = 0; i < 32043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32044(int& val) {
    func_in_32044(val);
    const int x = 32044;
    std::cout << 32044;
    for (int i = 0; i < 32044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32044(int& val) {
    const int x = 32044;
    std::cout << 32044;
    for (int i = 0; i < 32044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32045(int& val) {
    func_in_32045(val);
    const int x = 32045;
    std::cout << 32045;
    for (int i = 0; i < 32045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32045(int& val) {
    const int x = 32045;
    std::cout << 32045;
    for (int i = 0; i < 32045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32046(int& val) {
    func_in_32046(val);
    const int x = 32046;
    std::cout << 32046;
    for (int i = 0; i < 32046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32046(int& val) {
    const int x = 32046;
    std::cout << 32046;
    for (int i = 0; i < 32046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32047(int& val) {
    func_in_32047(val);
    const int x = 32047;
    std::cout << 32047;
    for (int i = 0; i < 32047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32047(int& val) {
    const int x = 32047;
    std::cout << 32047;
    for (int i = 0; i < 32047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32048(int& val) {
    func_in_32048(val);
    const int x = 32048;
    std::cout << 32048;
    for (int i = 0; i < 32048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32048(int& val) {
    const int x = 32048;
    std::cout << 32048;
    for (int i = 0; i < 32048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32049(int& val) {
    func_in_32049(val);
    const int x = 32049;
    std::cout << 32049;
    for (int i = 0; i < 32049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32049(int& val) {
    const int x = 32049;
    std::cout << 32049;
    for (int i = 0; i < 32049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32050(int& val) {
    func_in_32050(val);
    const int x = 32050;
    std::cout << 32050;
    for (int i = 0; i < 32050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32050(int& val) {
    const int x = 32050;
    std::cout << 32050;
    for (int i = 0; i < 32050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32051(int& val) {
    func_in_32051(val);
    const int x = 32051;
    std::cout << 32051;
    for (int i = 0; i < 32051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32051(int& val) {
    const int x = 32051;
    std::cout << 32051;
    for (int i = 0; i < 32051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32052(int& val) {
    func_in_32052(val);
    const int x = 32052;
    std::cout << 32052;
    for (int i = 0; i < 32052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32052(int& val) {
    const int x = 32052;
    std::cout << 32052;
    for (int i = 0; i < 32052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32053(int& val) {
    func_in_32053(val);
    const int x = 32053;
    std::cout << 32053;
    for (int i = 0; i < 32053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32053(int& val) {
    const int x = 32053;
    std::cout << 32053;
    for (int i = 0; i < 32053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32054(int& val) {
    func_in_32054(val);
    const int x = 32054;
    std::cout << 32054;
    for (int i = 0; i < 32054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32054(int& val) {
    const int x = 32054;
    std::cout << 32054;
    for (int i = 0; i < 32054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32055(int& val) {
    func_in_32055(val);
    const int x = 32055;
    std::cout << 32055;
    for (int i = 0; i < 32055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32055(int& val) {
    const int x = 32055;
    std::cout << 32055;
    for (int i = 0; i < 32055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32056(int& val) {
    func_in_32056(val);
    const int x = 32056;
    std::cout << 32056;
    for (int i = 0; i < 32056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32056(int& val) {
    const int x = 32056;
    std::cout << 32056;
    for (int i = 0; i < 32056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32057(int& val) {
    func_in_32057(val);
    const int x = 32057;
    std::cout << 32057;
    for (int i = 0; i < 32057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32057(int& val) {
    const int x = 32057;
    std::cout << 32057;
    for (int i = 0; i < 32057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32058(int& val) {
    func_in_32058(val);
    const int x = 32058;
    std::cout << 32058;
    for (int i = 0; i < 32058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32058(int& val) {
    const int x = 32058;
    std::cout << 32058;
    for (int i = 0; i < 32058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32059(int& val) {
    func_in_32059(val);
    const int x = 32059;
    std::cout << 32059;
    for (int i = 0; i < 32059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32059(int& val) {
    const int x = 32059;
    std::cout << 32059;
    for (int i = 0; i < 32059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32060(int& val) {
    func_in_32060(val);
    const int x = 32060;
    std::cout << 32060;
    for (int i = 0; i < 32060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32060(int& val) {
    const int x = 32060;
    std::cout << 32060;
    for (int i = 0; i < 32060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32061(int& val) {
    func_in_32061(val);
    const int x = 32061;
    std::cout << 32061;
    for (int i = 0; i < 32061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32061(int& val) {
    const int x = 32061;
    std::cout << 32061;
    for (int i = 0; i < 32061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32062(int& val) {
    func_in_32062(val);
    const int x = 32062;
    std::cout << 32062;
    for (int i = 0; i < 32062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32062(int& val) {
    const int x = 32062;
    std::cout << 32062;
    for (int i = 0; i < 32062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32063(int& val) {
    func_in_32063(val);
    const int x = 32063;
    std::cout << 32063;
    for (int i = 0; i < 32063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32063(int& val) {
    const int x = 32063;
    std::cout << 32063;
    for (int i = 0; i < 32063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32064(int& val) {
    func_in_32064(val);
    const int x = 32064;
    std::cout << 32064;
    for (int i = 0; i < 32064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32064(int& val) {
    const int x = 32064;
    std::cout << 32064;
    for (int i = 0; i < 32064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32065(int& val) {
    func_in_32065(val);
    const int x = 32065;
    std::cout << 32065;
    for (int i = 0; i < 32065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32065(int& val) {
    const int x = 32065;
    std::cout << 32065;
    for (int i = 0; i < 32065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32066(int& val) {
    func_in_32066(val);
    const int x = 32066;
    std::cout << 32066;
    for (int i = 0; i < 32066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32066(int& val) {
    const int x = 32066;
    std::cout << 32066;
    for (int i = 0; i < 32066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32067(int& val) {
    func_in_32067(val);
    const int x = 32067;
    std::cout << 32067;
    for (int i = 0; i < 32067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32067(int& val) {
    const int x = 32067;
    std::cout << 32067;
    for (int i = 0; i < 32067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32068(int& val) {
    func_in_32068(val);
    const int x = 32068;
    std::cout << 32068;
    for (int i = 0; i < 32068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32068(int& val) {
    const int x = 32068;
    std::cout << 32068;
    for (int i = 0; i < 32068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32069(int& val) {
    func_in_32069(val);
    const int x = 32069;
    std::cout << 32069;
    for (int i = 0; i < 32069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32069(int& val) {
    const int x = 32069;
    std::cout << 32069;
    for (int i = 0; i < 32069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32070(int& val) {
    func_in_32070(val);
    const int x = 32070;
    std::cout << 32070;
    for (int i = 0; i < 32070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32070(int& val) {
    const int x = 32070;
    std::cout << 32070;
    for (int i = 0; i < 32070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32071(int& val) {
    func_in_32071(val);
    const int x = 32071;
    std::cout << 32071;
    for (int i = 0; i < 32071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32071(int& val) {
    const int x = 32071;
    std::cout << 32071;
    for (int i = 0; i < 32071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32072(int& val) {
    func_in_32072(val);
    const int x = 32072;
    std::cout << 32072;
    for (int i = 0; i < 32072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32072(int& val) {
    const int x = 32072;
    std::cout << 32072;
    for (int i = 0; i < 32072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32073(int& val) {
    func_in_32073(val);
    const int x = 32073;
    std::cout << 32073;
    for (int i = 0; i < 32073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32073(int& val) {
    const int x = 32073;
    std::cout << 32073;
    for (int i = 0; i < 32073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32074(int& val) {
    func_in_32074(val);
    const int x = 32074;
    std::cout << 32074;
    for (int i = 0; i < 32074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32074(int& val) {
    const int x = 32074;
    std::cout << 32074;
    for (int i = 0; i < 32074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32075(int& val) {
    func_in_32075(val);
    const int x = 32075;
    std::cout << 32075;
    for (int i = 0; i < 32075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32075(int& val) {
    const int x = 32075;
    std::cout << 32075;
    for (int i = 0; i < 32075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32076(int& val) {
    func_in_32076(val);
    const int x = 32076;
    std::cout << 32076;
    for (int i = 0; i < 32076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32076(int& val) {
    const int x = 32076;
    std::cout << 32076;
    for (int i = 0; i < 32076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32077(int& val) {
    func_in_32077(val);
    const int x = 32077;
    std::cout << 32077;
    for (int i = 0; i < 32077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32077(int& val) {
    const int x = 32077;
    std::cout << 32077;
    for (int i = 0; i < 32077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32078(int& val) {
    func_in_32078(val);
    const int x = 32078;
    std::cout << 32078;
    for (int i = 0; i < 32078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32078(int& val) {
    const int x = 32078;
    std::cout << 32078;
    for (int i = 0; i < 32078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32079(int& val) {
    func_in_32079(val);
    const int x = 32079;
    std::cout << 32079;
    for (int i = 0; i < 32079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32079(int& val) {
    const int x = 32079;
    std::cout << 32079;
    for (int i = 0; i < 32079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32080(int& val) {
    func_in_32080(val);
    const int x = 32080;
    std::cout << 32080;
    for (int i = 0; i < 32080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32080(int& val) {
    const int x = 32080;
    std::cout << 32080;
    for (int i = 0; i < 32080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32081(int& val) {
    func_in_32081(val);
    const int x = 32081;
    std::cout << 32081;
    for (int i = 0; i < 32081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32081(int& val) {
    const int x = 32081;
    std::cout << 32081;
    for (int i = 0; i < 32081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32082(int& val) {
    func_in_32082(val);
    const int x = 32082;
    std::cout << 32082;
    for (int i = 0; i < 32082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32082(int& val) {
    const int x = 32082;
    std::cout << 32082;
    for (int i = 0; i < 32082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32083(int& val) {
    func_in_32083(val);
    const int x = 32083;
    std::cout << 32083;
    for (int i = 0; i < 32083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32083(int& val) {
    const int x = 32083;
    std::cout << 32083;
    for (int i = 0; i < 32083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32084(int& val) {
    func_in_32084(val);
    const int x = 32084;
    std::cout << 32084;
    for (int i = 0; i < 32084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32084(int& val) {
    const int x = 32084;
    std::cout << 32084;
    for (int i = 0; i < 32084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32085(int& val) {
    func_in_32085(val);
    const int x = 32085;
    std::cout << 32085;
    for (int i = 0; i < 32085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32085(int& val) {
    const int x = 32085;
    std::cout << 32085;
    for (int i = 0; i < 32085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32086(int& val) {
    func_in_32086(val);
    const int x = 32086;
    std::cout << 32086;
    for (int i = 0; i < 32086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32086(int& val) {
    const int x = 32086;
    std::cout << 32086;
    for (int i = 0; i < 32086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32087(int& val) {
    func_in_32087(val);
    const int x = 32087;
    std::cout << 32087;
    for (int i = 0; i < 32087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32087(int& val) {
    const int x = 32087;
    std::cout << 32087;
    for (int i = 0; i < 32087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32088(int& val) {
    func_in_32088(val);
    const int x = 32088;
    std::cout << 32088;
    for (int i = 0; i < 32088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32088(int& val) {
    const int x = 32088;
    std::cout << 32088;
    for (int i = 0; i < 32088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32089(int& val) {
    func_in_32089(val);
    const int x = 32089;
    std::cout << 32089;
    for (int i = 0; i < 32089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32089(int& val) {
    const int x = 32089;
    std::cout << 32089;
    for (int i = 0; i < 32089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32090(int& val) {
    func_in_32090(val);
    const int x = 32090;
    std::cout << 32090;
    for (int i = 0; i < 32090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32090(int& val) {
    const int x = 32090;
    std::cout << 32090;
    for (int i = 0; i < 32090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32091(int& val) {
    func_in_32091(val);
    const int x = 32091;
    std::cout << 32091;
    for (int i = 0; i < 32091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32091(int& val) {
    const int x = 32091;
    std::cout << 32091;
    for (int i = 0; i < 32091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32092(int& val) {
    func_in_32092(val);
    const int x = 32092;
    std::cout << 32092;
    for (int i = 0; i < 32092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32092(int& val) {
    const int x = 32092;
    std::cout << 32092;
    for (int i = 0; i < 32092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32093(int& val) {
    func_in_32093(val);
    const int x = 32093;
    std::cout << 32093;
    for (int i = 0; i < 32093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32093(int& val) {
    const int x = 32093;
    std::cout << 32093;
    for (int i = 0; i < 32093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32094(int& val) {
    func_in_32094(val);
    const int x = 32094;
    std::cout << 32094;
    for (int i = 0; i < 32094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32094(int& val) {
    const int x = 32094;
    std::cout << 32094;
    for (int i = 0; i < 32094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32095(int& val) {
    func_in_32095(val);
    const int x = 32095;
    std::cout << 32095;
    for (int i = 0; i < 32095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32095(int& val) {
    const int x = 32095;
    std::cout << 32095;
    for (int i = 0; i < 32095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32096(int& val) {
    func_in_32096(val);
    const int x = 32096;
    std::cout << 32096;
    for (int i = 0; i < 32096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32096(int& val) {
    const int x = 32096;
    std::cout << 32096;
    for (int i = 0; i < 32096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32097(int& val) {
    func_in_32097(val);
    const int x = 32097;
    std::cout << 32097;
    for (int i = 0; i < 32097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32097(int& val) {
    const int x = 32097;
    std::cout << 32097;
    for (int i = 0; i < 32097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32098(int& val) {
    func_in_32098(val);
    const int x = 32098;
    std::cout << 32098;
    for (int i = 0; i < 32098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32098(int& val) {
    const int x = 32098;
    std::cout << 32098;
    for (int i = 0; i < 32098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32099(int& val) {
    func_in_32099(val);
    const int x = 32099;
    std::cout << 32099;
    for (int i = 0; i < 32099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32099(int& val) {
    const int x = 32099;
    std::cout << 32099;
    for (int i = 0; i < 32099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32100(int& val) {
    func_in_32100(val);
    const int x = 32100;
    std::cout << 32100;
    for (int i = 0; i < 32100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32100(int& val) {
    const int x = 32100;
    std::cout << 32100;
    for (int i = 0; i < 32100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32101(int& val) {
    func_in_32101(val);
    const int x = 32101;
    std::cout << 32101;
    for (int i = 0; i < 32101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32101(int& val) {
    const int x = 32101;
    std::cout << 32101;
    for (int i = 0; i < 32101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32102(int& val) {
    func_in_32102(val);
    const int x = 32102;
    std::cout << 32102;
    for (int i = 0; i < 32102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32102(int& val) {
    const int x = 32102;
    std::cout << 32102;
    for (int i = 0; i < 32102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32103(int& val) {
    func_in_32103(val);
    const int x = 32103;
    std::cout << 32103;
    for (int i = 0; i < 32103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32103(int& val) {
    const int x = 32103;
    std::cout << 32103;
    for (int i = 0; i < 32103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32104(int& val) {
    func_in_32104(val);
    const int x = 32104;
    std::cout << 32104;
    for (int i = 0; i < 32104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32104(int& val) {
    const int x = 32104;
    std::cout << 32104;
    for (int i = 0; i < 32104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32105(int& val) {
    func_in_32105(val);
    const int x = 32105;
    std::cout << 32105;
    for (int i = 0; i < 32105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32105(int& val) {
    const int x = 32105;
    std::cout << 32105;
    for (int i = 0; i < 32105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32106(int& val) {
    func_in_32106(val);
    const int x = 32106;
    std::cout << 32106;
    for (int i = 0; i < 32106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32106(int& val) {
    const int x = 32106;
    std::cout << 32106;
    for (int i = 0; i < 32106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32107(int& val) {
    func_in_32107(val);
    const int x = 32107;
    std::cout << 32107;
    for (int i = 0; i < 32107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32107(int& val) {
    const int x = 32107;
    std::cout << 32107;
    for (int i = 0; i < 32107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32108(int& val) {
    func_in_32108(val);
    const int x = 32108;
    std::cout << 32108;
    for (int i = 0; i < 32108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32108(int& val) {
    const int x = 32108;
    std::cout << 32108;
    for (int i = 0; i < 32108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32109(int& val) {
    func_in_32109(val);
    const int x = 32109;
    std::cout << 32109;
    for (int i = 0; i < 32109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32109(int& val) {
    const int x = 32109;
    std::cout << 32109;
    for (int i = 0; i < 32109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32110(int& val) {
    func_in_32110(val);
    const int x = 32110;
    std::cout << 32110;
    for (int i = 0; i < 32110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32110(int& val) {
    const int x = 32110;
    std::cout << 32110;
    for (int i = 0; i < 32110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32111(int& val) {
    func_in_32111(val);
    const int x = 32111;
    std::cout << 32111;
    for (int i = 0; i < 32111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32111(int& val) {
    const int x = 32111;
    std::cout << 32111;
    for (int i = 0; i < 32111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32112(int& val) {
    func_in_32112(val);
    const int x = 32112;
    std::cout << 32112;
    for (int i = 0; i < 32112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32112(int& val) {
    const int x = 32112;
    std::cout << 32112;
    for (int i = 0; i < 32112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32113(int& val) {
    func_in_32113(val);
    const int x = 32113;
    std::cout << 32113;
    for (int i = 0; i < 32113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32113(int& val) {
    const int x = 32113;
    std::cout << 32113;
    for (int i = 0; i < 32113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32114(int& val) {
    func_in_32114(val);
    const int x = 32114;
    std::cout << 32114;
    for (int i = 0; i < 32114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32114(int& val) {
    const int x = 32114;
    std::cout << 32114;
    for (int i = 0; i < 32114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32115(int& val) {
    func_in_32115(val);
    const int x = 32115;
    std::cout << 32115;
    for (int i = 0; i < 32115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32115(int& val) {
    const int x = 32115;
    std::cout << 32115;
    for (int i = 0; i < 32115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32116(int& val) {
    func_in_32116(val);
    const int x = 32116;
    std::cout << 32116;
    for (int i = 0; i < 32116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32116(int& val) {
    const int x = 32116;
    std::cout << 32116;
    for (int i = 0; i < 32116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32117(int& val) {
    func_in_32117(val);
    const int x = 32117;
    std::cout << 32117;
    for (int i = 0; i < 32117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32117(int& val) {
    const int x = 32117;
    std::cout << 32117;
    for (int i = 0; i < 32117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32118(int& val) {
    func_in_32118(val);
    const int x = 32118;
    std::cout << 32118;
    for (int i = 0; i < 32118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32118(int& val) {
    const int x = 32118;
    std::cout << 32118;
    for (int i = 0; i < 32118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32119(int& val) {
    func_in_32119(val);
    const int x = 32119;
    std::cout << 32119;
    for (int i = 0; i < 32119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32119(int& val) {
    const int x = 32119;
    std::cout << 32119;
    for (int i = 0; i < 32119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32120(int& val) {
    func_in_32120(val);
    const int x = 32120;
    std::cout << 32120;
    for (int i = 0; i < 32120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32120(int& val) {
    const int x = 32120;
    std::cout << 32120;
    for (int i = 0; i < 32120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32121(int& val) {
    func_in_32121(val);
    const int x = 32121;
    std::cout << 32121;
    for (int i = 0; i < 32121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32121(int& val) {
    const int x = 32121;
    std::cout << 32121;
    for (int i = 0; i < 32121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32122(int& val) {
    func_in_32122(val);
    const int x = 32122;
    std::cout << 32122;
    for (int i = 0; i < 32122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32122(int& val) {
    const int x = 32122;
    std::cout << 32122;
    for (int i = 0; i < 32122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32123(int& val) {
    func_in_32123(val);
    const int x = 32123;
    std::cout << 32123;
    for (int i = 0; i < 32123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32123(int& val) {
    const int x = 32123;
    std::cout << 32123;
    for (int i = 0; i < 32123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32124(int& val) {
    func_in_32124(val);
    const int x = 32124;
    std::cout << 32124;
    for (int i = 0; i < 32124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32124(int& val) {
    const int x = 32124;
    std::cout << 32124;
    for (int i = 0; i < 32124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32125(int& val) {
    func_in_32125(val);
    const int x = 32125;
    std::cout << 32125;
    for (int i = 0; i < 32125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32125(int& val) {
    const int x = 32125;
    std::cout << 32125;
    for (int i = 0; i < 32125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32126(int& val) {
    func_in_32126(val);
    const int x = 32126;
    std::cout << 32126;
    for (int i = 0; i < 32126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32126(int& val) {
    const int x = 32126;
    std::cout << 32126;
    for (int i = 0; i < 32126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32127(int& val) {
    func_in_32127(val);
    const int x = 32127;
    std::cout << 32127;
    for (int i = 0; i < 32127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32127(int& val) {
    const int x = 32127;
    std::cout << 32127;
    for (int i = 0; i < 32127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32128(int& val) {
    func_in_32128(val);
    const int x = 32128;
    std::cout << 32128;
    for (int i = 0; i < 32128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32128(int& val) {
    const int x = 32128;
    std::cout << 32128;
    for (int i = 0; i < 32128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32129(int& val) {
    func_in_32129(val);
    const int x = 32129;
    std::cout << 32129;
    for (int i = 0; i < 32129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32129(int& val) {
    const int x = 32129;
    std::cout << 32129;
    for (int i = 0; i < 32129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32130(int& val) {
    func_in_32130(val);
    const int x = 32130;
    std::cout << 32130;
    for (int i = 0; i < 32130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32130(int& val) {
    const int x = 32130;
    std::cout << 32130;
    for (int i = 0; i < 32130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32131(int& val) {
    func_in_32131(val);
    const int x = 32131;
    std::cout << 32131;
    for (int i = 0; i < 32131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32131(int& val) {
    const int x = 32131;
    std::cout << 32131;
    for (int i = 0; i < 32131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32132(int& val) {
    func_in_32132(val);
    const int x = 32132;
    std::cout << 32132;
    for (int i = 0; i < 32132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32132(int& val) {
    const int x = 32132;
    std::cout << 32132;
    for (int i = 0; i < 32132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32133(int& val) {
    func_in_32133(val);
    const int x = 32133;
    std::cout << 32133;
    for (int i = 0; i < 32133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32133(int& val) {
    const int x = 32133;
    std::cout << 32133;
    for (int i = 0; i < 32133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32134(int& val) {
    func_in_32134(val);
    const int x = 32134;
    std::cout << 32134;
    for (int i = 0; i < 32134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32134(int& val) {
    const int x = 32134;
    std::cout << 32134;
    for (int i = 0; i < 32134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32135(int& val) {
    func_in_32135(val);
    const int x = 32135;
    std::cout << 32135;
    for (int i = 0; i < 32135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32135(int& val) {
    const int x = 32135;
    std::cout << 32135;
    for (int i = 0; i < 32135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32136(int& val) {
    func_in_32136(val);
    const int x = 32136;
    std::cout << 32136;
    for (int i = 0; i < 32136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32136(int& val) {
    const int x = 32136;
    std::cout << 32136;
    for (int i = 0; i < 32136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32137(int& val) {
    func_in_32137(val);
    const int x = 32137;
    std::cout << 32137;
    for (int i = 0; i < 32137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32137(int& val) {
    const int x = 32137;
    std::cout << 32137;
    for (int i = 0; i < 32137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32138(int& val) {
    func_in_32138(val);
    const int x = 32138;
    std::cout << 32138;
    for (int i = 0; i < 32138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32138(int& val) {
    const int x = 32138;
    std::cout << 32138;
    for (int i = 0; i < 32138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32139(int& val) {
    func_in_32139(val);
    const int x = 32139;
    std::cout << 32139;
    for (int i = 0; i < 32139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32139(int& val) {
    const int x = 32139;
    std::cout << 32139;
    for (int i = 0; i < 32139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32140(int& val) {
    func_in_32140(val);
    const int x = 32140;
    std::cout << 32140;
    for (int i = 0; i < 32140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32140(int& val) {
    const int x = 32140;
    std::cout << 32140;
    for (int i = 0; i < 32140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32141(int& val) {
    func_in_32141(val);
    const int x = 32141;
    std::cout << 32141;
    for (int i = 0; i < 32141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32141(int& val) {
    const int x = 32141;
    std::cout << 32141;
    for (int i = 0; i < 32141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32142(int& val) {
    func_in_32142(val);
    const int x = 32142;
    std::cout << 32142;
    for (int i = 0; i < 32142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32142(int& val) {
    const int x = 32142;
    std::cout << 32142;
    for (int i = 0; i < 32142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32143(int& val) {
    func_in_32143(val);
    const int x = 32143;
    std::cout << 32143;
    for (int i = 0; i < 32143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32143(int& val) {
    const int x = 32143;
    std::cout << 32143;
    for (int i = 0; i < 32143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32144(int& val) {
    func_in_32144(val);
    const int x = 32144;
    std::cout << 32144;
    for (int i = 0; i < 32144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32144(int& val) {
    const int x = 32144;
    std::cout << 32144;
    for (int i = 0; i < 32144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32145(int& val) {
    func_in_32145(val);
    const int x = 32145;
    std::cout << 32145;
    for (int i = 0; i < 32145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32145(int& val) {
    const int x = 32145;
    std::cout << 32145;
    for (int i = 0; i < 32145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32146(int& val) {
    func_in_32146(val);
    const int x = 32146;
    std::cout << 32146;
    for (int i = 0; i < 32146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32146(int& val) {
    const int x = 32146;
    std::cout << 32146;
    for (int i = 0; i < 32146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32147(int& val) {
    func_in_32147(val);
    const int x = 32147;
    std::cout << 32147;
    for (int i = 0; i < 32147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32147(int& val) {
    const int x = 32147;
    std::cout << 32147;
    for (int i = 0; i < 32147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32148(int& val) {
    func_in_32148(val);
    const int x = 32148;
    std::cout << 32148;
    for (int i = 0; i < 32148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32148(int& val) {
    const int x = 32148;
    std::cout << 32148;
    for (int i = 0; i < 32148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32149(int& val) {
    func_in_32149(val);
    const int x = 32149;
    std::cout << 32149;
    for (int i = 0; i < 32149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32149(int& val) {
    const int x = 32149;
    std::cout << 32149;
    for (int i = 0; i < 32149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32150(int& val) {
    func_in_32150(val);
    const int x = 32150;
    std::cout << 32150;
    for (int i = 0; i < 32150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32150(int& val) {
    const int x = 32150;
    std::cout << 32150;
    for (int i = 0; i < 32150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32151(int& val) {
    func_in_32151(val);
    const int x = 32151;
    std::cout << 32151;
    for (int i = 0; i < 32151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32151(int& val) {
    const int x = 32151;
    std::cout << 32151;
    for (int i = 0; i < 32151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32152(int& val) {
    func_in_32152(val);
    const int x = 32152;
    std::cout << 32152;
    for (int i = 0; i < 32152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32152(int& val) {
    const int x = 32152;
    std::cout << 32152;
    for (int i = 0; i < 32152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32153(int& val) {
    func_in_32153(val);
    const int x = 32153;
    std::cout << 32153;
    for (int i = 0; i < 32153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32153(int& val) {
    const int x = 32153;
    std::cout << 32153;
    for (int i = 0; i < 32153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32154(int& val) {
    func_in_32154(val);
    const int x = 32154;
    std::cout << 32154;
    for (int i = 0; i < 32154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32154(int& val) {
    const int x = 32154;
    std::cout << 32154;
    for (int i = 0; i < 32154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32155(int& val) {
    func_in_32155(val);
    const int x = 32155;
    std::cout << 32155;
    for (int i = 0; i < 32155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32155(int& val) {
    const int x = 32155;
    std::cout << 32155;
    for (int i = 0; i < 32155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32156(int& val) {
    func_in_32156(val);
    const int x = 32156;
    std::cout << 32156;
    for (int i = 0; i < 32156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32156(int& val) {
    const int x = 32156;
    std::cout << 32156;
    for (int i = 0; i < 32156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32157(int& val) {
    func_in_32157(val);
    const int x = 32157;
    std::cout << 32157;
    for (int i = 0; i < 32157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32157(int& val) {
    const int x = 32157;
    std::cout << 32157;
    for (int i = 0; i < 32157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32158(int& val) {
    func_in_32158(val);
    const int x = 32158;
    std::cout << 32158;
    for (int i = 0; i < 32158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32158(int& val) {
    const int x = 32158;
    std::cout << 32158;
    for (int i = 0; i < 32158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32159(int& val) {
    func_in_32159(val);
    const int x = 32159;
    std::cout << 32159;
    for (int i = 0; i < 32159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32159(int& val) {
    const int x = 32159;
    std::cout << 32159;
    for (int i = 0; i < 32159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32160(int& val) {
    func_in_32160(val);
    const int x = 32160;
    std::cout << 32160;
    for (int i = 0; i < 32160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32160(int& val) {
    const int x = 32160;
    std::cout << 32160;
    for (int i = 0; i < 32160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32161(int& val) {
    func_in_32161(val);
    const int x = 32161;
    std::cout << 32161;
    for (int i = 0; i < 32161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32161(int& val) {
    const int x = 32161;
    std::cout << 32161;
    for (int i = 0; i < 32161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32162(int& val) {
    func_in_32162(val);
    const int x = 32162;
    std::cout << 32162;
    for (int i = 0; i < 32162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32162(int& val) {
    const int x = 32162;
    std::cout << 32162;
    for (int i = 0; i < 32162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32163(int& val) {
    func_in_32163(val);
    const int x = 32163;
    std::cout << 32163;
    for (int i = 0; i < 32163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32163(int& val) {
    const int x = 32163;
    std::cout << 32163;
    for (int i = 0; i < 32163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32164(int& val) {
    func_in_32164(val);
    const int x = 32164;
    std::cout << 32164;
    for (int i = 0; i < 32164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32164(int& val) {
    const int x = 32164;
    std::cout << 32164;
    for (int i = 0; i < 32164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32165(int& val) {
    func_in_32165(val);
    const int x = 32165;
    std::cout << 32165;
    for (int i = 0; i < 32165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32165(int& val) {
    const int x = 32165;
    std::cout << 32165;
    for (int i = 0; i < 32165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32166(int& val) {
    func_in_32166(val);
    const int x = 32166;
    std::cout << 32166;
    for (int i = 0; i < 32166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32166(int& val) {
    const int x = 32166;
    std::cout << 32166;
    for (int i = 0; i < 32166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32167(int& val) {
    func_in_32167(val);
    const int x = 32167;
    std::cout << 32167;
    for (int i = 0; i < 32167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32167(int& val) {
    const int x = 32167;
    std::cout << 32167;
    for (int i = 0; i < 32167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32168(int& val) {
    func_in_32168(val);
    const int x = 32168;
    std::cout << 32168;
    for (int i = 0; i < 32168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32168(int& val) {
    const int x = 32168;
    std::cout << 32168;
    for (int i = 0; i < 32168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32169(int& val) {
    func_in_32169(val);
    const int x = 32169;
    std::cout << 32169;
    for (int i = 0; i < 32169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32169(int& val) {
    const int x = 32169;
    std::cout << 32169;
    for (int i = 0; i < 32169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32170(int& val) {
    func_in_32170(val);
    const int x = 32170;
    std::cout << 32170;
    for (int i = 0; i < 32170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32170(int& val) {
    const int x = 32170;
    std::cout << 32170;
    for (int i = 0; i < 32170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32171(int& val) {
    func_in_32171(val);
    const int x = 32171;
    std::cout << 32171;
    for (int i = 0; i < 32171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32171(int& val) {
    const int x = 32171;
    std::cout << 32171;
    for (int i = 0; i < 32171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32172(int& val) {
    func_in_32172(val);
    const int x = 32172;
    std::cout << 32172;
    for (int i = 0; i < 32172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32172(int& val) {
    const int x = 32172;
    std::cout << 32172;
    for (int i = 0; i < 32172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32173(int& val) {
    func_in_32173(val);
    const int x = 32173;
    std::cout << 32173;
    for (int i = 0; i < 32173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32173(int& val) {
    const int x = 32173;
    std::cout << 32173;
    for (int i = 0; i < 32173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32174(int& val) {
    func_in_32174(val);
    const int x = 32174;
    std::cout << 32174;
    for (int i = 0; i < 32174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32174(int& val) {
    const int x = 32174;
    std::cout << 32174;
    for (int i = 0; i < 32174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32175(int& val) {
    func_in_32175(val);
    const int x = 32175;
    std::cout << 32175;
    for (int i = 0; i < 32175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32175(int& val) {
    const int x = 32175;
    std::cout << 32175;
    for (int i = 0; i < 32175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32176(int& val) {
    func_in_32176(val);
    const int x = 32176;
    std::cout << 32176;
    for (int i = 0; i < 32176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32176(int& val) {
    const int x = 32176;
    std::cout << 32176;
    for (int i = 0; i < 32176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32177(int& val) {
    func_in_32177(val);
    const int x = 32177;
    std::cout << 32177;
    for (int i = 0; i < 32177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32177(int& val) {
    const int x = 32177;
    std::cout << 32177;
    for (int i = 0; i < 32177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32178(int& val) {
    func_in_32178(val);
    const int x = 32178;
    std::cout << 32178;
    for (int i = 0; i < 32178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32178(int& val) {
    const int x = 32178;
    std::cout << 32178;
    for (int i = 0; i < 32178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32179(int& val) {
    func_in_32179(val);
    const int x = 32179;
    std::cout << 32179;
    for (int i = 0; i < 32179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32179(int& val) {
    const int x = 32179;
    std::cout << 32179;
    for (int i = 0; i < 32179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32180(int& val) {
    func_in_32180(val);
    const int x = 32180;
    std::cout << 32180;
    for (int i = 0; i < 32180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32180(int& val) {
    const int x = 32180;
    std::cout << 32180;
    for (int i = 0; i < 32180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32181(int& val) {
    func_in_32181(val);
    const int x = 32181;
    std::cout << 32181;
    for (int i = 0; i < 32181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32181(int& val) {
    const int x = 32181;
    std::cout << 32181;
    for (int i = 0; i < 32181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32182(int& val) {
    func_in_32182(val);
    const int x = 32182;
    std::cout << 32182;
    for (int i = 0; i < 32182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32182(int& val) {
    const int x = 32182;
    std::cout << 32182;
    for (int i = 0; i < 32182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32183(int& val) {
    func_in_32183(val);
    const int x = 32183;
    std::cout << 32183;
    for (int i = 0; i < 32183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32183(int& val) {
    const int x = 32183;
    std::cout << 32183;
    for (int i = 0; i < 32183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32184(int& val) {
    func_in_32184(val);
    const int x = 32184;
    std::cout << 32184;
    for (int i = 0; i < 32184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32184(int& val) {
    const int x = 32184;
    std::cout << 32184;
    for (int i = 0; i < 32184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32185(int& val) {
    func_in_32185(val);
    const int x = 32185;
    std::cout << 32185;
    for (int i = 0; i < 32185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32185(int& val) {
    const int x = 32185;
    std::cout << 32185;
    for (int i = 0; i < 32185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32186(int& val) {
    func_in_32186(val);
    const int x = 32186;
    std::cout << 32186;
    for (int i = 0; i < 32186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32186(int& val) {
    const int x = 32186;
    std::cout << 32186;
    for (int i = 0; i < 32186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32187(int& val) {
    func_in_32187(val);
    const int x = 32187;
    std::cout << 32187;
    for (int i = 0; i < 32187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32187(int& val) {
    const int x = 32187;
    std::cout << 32187;
    for (int i = 0; i < 32187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32188(int& val) {
    func_in_32188(val);
    const int x = 32188;
    std::cout << 32188;
    for (int i = 0; i < 32188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32188(int& val) {
    const int x = 32188;
    std::cout << 32188;
    for (int i = 0; i < 32188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32189(int& val) {
    func_in_32189(val);
    const int x = 32189;
    std::cout << 32189;
    for (int i = 0; i < 32189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32189(int& val) {
    const int x = 32189;
    std::cout << 32189;
    for (int i = 0; i < 32189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32190(int& val) {
    func_in_32190(val);
    const int x = 32190;
    std::cout << 32190;
    for (int i = 0; i < 32190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32190(int& val) {
    const int x = 32190;
    std::cout << 32190;
    for (int i = 0; i < 32190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32191(int& val) {
    func_in_32191(val);
    const int x = 32191;
    std::cout << 32191;
    for (int i = 0; i < 32191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32191(int& val) {
    const int x = 32191;
    std::cout << 32191;
    for (int i = 0; i < 32191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32192(int& val) {
    func_in_32192(val);
    const int x = 32192;
    std::cout << 32192;
    for (int i = 0; i < 32192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32192(int& val) {
    const int x = 32192;
    std::cout << 32192;
    for (int i = 0; i < 32192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32193(int& val) {
    func_in_32193(val);
    const int x = 32193;
    std::cout << 32193;
    for (int i = 0; i < 32193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32193(int& val) {
    const int x = 32193;
    std::cout << 32193;
    for (int i = 0; i < 32193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32194(int& val) {
    func_in_32194(val);
    const int x = 32194;
    std::cout << 32194;
    for (int i = 0; i < 32194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32194(int& val) {
    const int x = 32194;
    std::cout << 32194;
    for (int i = 0; i < 32194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32195(int& val) {
    func_in_32195(val);
    const int x = 32195;
    std::cout << 32195;
    for (int i = 0; i < 32195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32195(int& val) {
    const int x = 32195;
    std::cout << 32195;
    for (int i = 0; i < 32195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32196(int& val) {
    func_in_32196(val);
    const int x = 32196;
    std::cout << 32196;
    for (int i = 0; i < 32196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32196(int& val) {
    const int x = 32196;
    std::cout << 32196;
    for (int i = 0; i < 32196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32197(int& val) {
    func_in_32197(val);
    const int x = 32197;
    std::cout << 32197;
    for (int i = 0; i < 32197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32197(int& val) {
    const int x = 32197;
    std::cout << 32197;
    for (int i = 0; i < 32197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32198(int& val) {
    func_in_32198(val);
    const int x = 32198;
    std::cout << 32198;
    for (int i = 0; i < 32198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32198(int& val) {
    const int x = 32198;
    std::cout << 32198;
    for (int i = 0; i < 32198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32199(int& val) {
    func_in_32199(val);
    const int x = 32199;
    std::cout << 32199;
    for (int i = 0; i < 32199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32199(int& val) {
    const int x = 32199;
    std::cout << 32199;
    for (int i = 0; i < 32199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32200(int& val) {
    func_in_32200(val);
    const int x = 32200;
    std::cout << 32200;
    for (int i = 0; i < 32200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32200(int& val) {
    const int x = 32200;
    std::cout << 32200;
    for (int i = 0; i < 32200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32201(int& val) {
    func_in_32201(val);
    const int x = 32201;
    std::cout << 32201;
    for (int i = 0; i < 32201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32201(int& val) {
    const int x = 32201;
    std::cout << 32201;
    for (int i = 0; i < 32201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32202(int& val) {
    func_in_32202(val);
    const int x = 32202;
    std::cout << 32202;
    for (int i = 0; i < 32202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32202(int& val) {
    const int x = 32202;
    std::cout << 32202;
    for (int i = 0; i < 32202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32203(int& val) {
    func_in_32203(val);
    const int x = 32203;
    std::cout << 32203;
    for (int i = 0; i < 32203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32203(int& val) {
    const int x = 32203;
    std::cout << 32203;
    for (int i = 0; i < 32203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32204(int& val) {
    func_in_32204(val);
    const int x = 32204;
    std::cout << 32204;
    for (int i = 0; i < 32204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32204(int& val) {
    const int x = 32204;
    std::cout << 32204;
    for (int i = 0; i < 32204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32205(int& val) {
    func_in_32205(val);
    const int x = 32205;
    std::cout << 32205;
    for (int i = 0; i < 32205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32205(int& val) {
    const int x = 32205;
    std::cout << 32205;
    for (int i = 0; i < 32205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32206(int& val) {
    func_in_32206(val);
    const int x = 32206;
    std::cout << 32206;
    for (int i = 0; i < 32206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32206(int& val) {
    const int x = 32206;
    std::cout << 32206;
    for (int i = 0; i < 32206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32207(int& val) {
    func_in_32207(val);
    const int x = 32207;
    std::cout << 32207;
    for (int i = 0; i < 32207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32207(int& val) {
    const int x = 32207;
    std::cout << 32207;
    for (int i = 0; i < 32207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32208(int& val) {
    func_in_32208(val);
    const int x = 32208;
    std::cout << 32208;
    for (int i = 0; i < 32208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32208(int& val) {
    const int x = 32208;
    std::cout << 32208;
    for (int i = 0; i < 32208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32209(int& val) {
    func_in_32209(val);
    const int x = 32209;
    std::cout << 32209;
    for (int i = 0; i < 32209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32209(int& val) {
    const int x = 32209;
    std::cout << 32209;
    for (int i = 0; i < 32209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32210(int& val) {
    func_in_32210(val);
    const int x = 32210;
    std::cout << 32210;
    for (int i = 0; i < 32210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32210(int& val) {
    const int x = 32210;
    std::cout << 32210;
    for (int i = 0; i < 32210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32211(int& val) {
    func_in_32211(val);
    const int x = 32211;
    std::cout << 32211;
    for (int i = 0; i < 32211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32211(int& val) {
    const int x = 32211;
    std::cout << 32211;
    for (int i = 0; i < 32211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32212(int& val) {
    func_in_32212(val);
    const int x = 32212;
    std::cout << 32212;
    for (int i = 0; i < 32212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32212(int& val) {
    const int x = 32212;
    std::cout << 32212;
    for (int i = 0; i < 32212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32213(int& val) {
    func_in_32213(val);
    const int x = 32213;
    std::cout << 32213;
    for (int i = 0; i < 32213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32213(int& val) {
    const int x = 32213;
    std::cout << 32213;
    for (int i = 0; i < 32213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32214(int& val) {
    func_in_32214(val);
    const int x = 32214;
    std::cout << 32214;
    for (int i = 0; i < 32214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32214(int& val) {
    const int x = 32214;
    std::cout << 32214;
    for (int i = 0; i < 32214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32215(int& val) {
    func_in_32215(val);
    const int x = 32215;
    std::cout << 32215;
    for (int i = 0; i < 32215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32215(int& val) {
    const int x = 32215;
    std::cout << 32215;
    for (int i = 0; i < 32215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32216(int& val) {
    func_in_32216(val);
    const int x = 32216;
    std::cout << 32216;
    for (int i = 0; i < 32216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32216(int& val) {
    const int x = 32216;
    std::cout << 32216;
    for (int i = 0; i < 32216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32217(int& val) {
    func_in_32217(val);
    const int x = 32217;
    std::cout << 32217;
    for (int i = 0; i < 32217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32217(int& val) {
    const int x = 32217;
    std::cout << 32217;
    for (int i = 0; i < 32217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32218(int& val) {
    func_in_32218(val);
    const int x = 32218;
    std::cout << 32218;
    for (int i = 0; i < 32218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32218(int& val) {
    const int x = 32218;
    std::cout << 32218;
    for (int i = 0; i < 32218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32219(int& val) {
    func_in_32219(val);
    const int x = 32219;
    std::cout << 32219;
    for (int i = 0; i < 32219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32219(int& val) {
    const int x = 32219;
    std::cout << 32219;
    for (int i = 0; i < 32219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32220(int& val) {
    func_in_32220(val);
    const int x = 32220;
    std::cout << 32220;
    for (int i = 0; i < 32220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32220(int& val) {
    const int x = 32220;
    std::cout << 32220;
    for (int i = 0; i < 32220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32221(int& val) {
    func_in_32221(val);
    const int x = 32221;
    std::cout << 32221;
    for (int i = 0; i < 32221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32221(int& val) {
    const int x = 32221;
    std::cout << 32221;
    for (int i = 0; i < 32221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32222(int& val) {
    func_in_32222(val);
    const int x = 32222;
    std::cout << 32222;
    for (int i = 0; i < 32222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32222(int& val) {
    const int x = 32222;
    std::cout << 32222;
    for (int i = 0; i < 32222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32223(int& val) {
    func_in_32223(val);
    const int x = 32223;
    std::cout << 32223;
    for (int i = 0; i < 32223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32223(int& val) {
    const int x = 32223;
    std::cout << 32223;
    for (int i = 0; i < 32223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32224(int& val) {
    func_in_32224(val);
    const int x = 32224;
    std::cout << 32224;
    for (int i = 0; i < 32224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32224(int& val) {
    const int x = 32224;
    std::cout << 32224;
    for (int i = 0; i < 32224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32225(int& val) {
    func_in_32225(val);
    const int x = 32225;
    std::cout << 32225;
    for (int i = 0; i < 32225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32225(int& val) {
    const int x = 32225;
    std::cout << 32225;
    for (int i = 0; i < 32225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32226(int& val) {
    func_in_32226(val);
    const int x = 32226;
    std::cout << 32226;
    for (int i = 0; i < 32226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32226(int& val) {
    const int x = 32226;
    std::cout << 32226;
    for (int i = 0; i < 32226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32227(int& val) {
    func_in_32227(val);
    const int x = 32227;
    std::cout << 32227;
    for (int i = 0; i < 32227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32227(int& val) {
    const int x = 32227;
    std::cout << 32227;
    for (int i = 0; i < 32227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32228(int& val) {
    func_in_32228(val);
    const int x = 32228;
    std::cout << 32228;
    for (int i = 0; i < 32228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32228(int& val) {
    const int x = 32228;
    std::cout << 32228;
    for (int i = 0; i < 32228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32229(int& val) {
    func_in_32229(val);
    const int x = 32229;
    std::cout << 32229;
    for (int i = 0; i < 32229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32229(int& val) {
    const int x = 32229;
    std::cout << 32229;
    for (int i = 0; i < 32229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32230(int& val) {
    func_in_32230(val);
    const int x = 32230;
    std::cout << 32230;
    for (int i = 0; i < 32230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32230(int& val) {
    const int x = 32230;
    std::cout << 32230;
    for (int i = 0; i < 32230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32231(int& val) {
    func_in_32231(val);
    const int x = 32231;
    std::cout << 32231;
    for (int i = 0; i < 32231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32231(int& val) {
    const int x = 32231;
    std::cout << 32231;
    for (int i = 0; i < 32231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32232(int& val) {
    func_in_32232(val);
    const int x = 32232;
    std::cout << 32232;
    for (int i = 0; i < 32232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32232(int& val) {
    const int x = 32232;
    std::cout << 32232;
    for (int i = 0; i < 32232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32233(int& val) {
    func_in_32233(val);
    const int x = 32233;
    std::cout << 32233;
    for (int i = 0; i < 32233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32233(int& val) {
    const int x = 32233;
    std::cout << 32233;
    for (int i = 0; i < 32233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32234(int& val) {
    func_in_32234(val);
    const int x = 32234;
    std::cout << 32234;
    for (int i = 0; i < 32234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32234(int& val) {
    const int x = 32234;
    std::cout << 32234;
    for (int i = 0; i < 32234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32235(int& val) {
    func_in_32235(val);
    const int x = 32235;
    std::cout << 32235;
    for (int i = 0; i < 32235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32235(int& val) {
    const int x = 32235;
    std::cout << 32235;
    for (int i = 0; i < 32235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32236(int& val) {
    func_in_32236(val);
    const int x = 32236;
    std::cout << 32236;
    for (int i = 0; i < 32236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32236(int& val) {
    const int x = 32236;
    std::cout << 32236;
    for (int i = 0; i < 32236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32237(int& val) {
    func_in_32237(val);
    const int x = 32237;
    std::cout << 32237;
    for (int i = 0; i < 32237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32237(int& val) {
    const int x = 32237;
    std::cout << 32237;
    for (int i = 0; i < 32237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32238(int& val) {
    func_in_32238(val);
    const int x = 32238;
    std::cout << 32238;
    for (int i = 0; i < 32238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32238(int& val) {
    const int x = 32238;
    std::cout << 32238;
    for (int i = 0; i < 32238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32239(int& val) {
    func_in_32239(val);
    const int x = 32239;
    std::cout << 32239;
    for (int i = 0; i < 32239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32239(int& val) {
    const int x = 32239;
    std::cout << 32239;
    for (int i = 0; i < 32239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32240(int& val) {
    func_in_32240(val);
    const int x = 32240;
    std::cout << 32240;
    for (int i = 0; i < 32240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32240(int& val) {
    const int x = 32240;
    std::cout << 32240;
    for (int i = 0; i < 32240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32241(int& val) {
    func_in_32241(val);
    const int x = 32241;
    std::cout << 32241;
    for (int i = 0; i < 32241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32241(int& val) {
    const int x = 32241;
    std::cout << 32241;
    for (int i = 0; i < 32241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32242(int& val) {
    func_in_32242(val);
    const int x = 32242;
    std::cout << 32242;
    for (int i = 0; i < 32242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32242(int& val) {
    const int x = 32242;
    std::cout << 32242;
    for (int i = 0; i < 32242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32243(int& val) {
    func_in_32243(val);
    const int x = 32243;
    std::cout << 32243;
    for (int i = 0; i < 32243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32243(int& val) {
    const int x = 32243;
    std::cout << 32243;
    for (int i = 0; i < 32243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32244(int& val) {
    func_in_32244(val);
    const int x = 32244;
    std::cout << 32244;
    for (int i = 0; i < 32244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32244(int& val) {
    const int x = 32244;
    std::cout << 32244;
    for (int i = 0; i < 32244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32245(int& val) {
    func_in_32245(val);
    const int x = 32245;
    std::cout << 32245;
    for (int i = 0; i < 32245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32245(int& val) {
    const int x = 32245;
    std::cout << 32245;
    for (int i = 0; i < 32245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32246(int& val) {
    func_in_32246(val);
    const int x = 32246;
    std::cout << 32246;
    for (int i = 0; i < 32246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32246(int& val) {
    const int x = 32246;
    std::cout << 32246;
    for (int i = 0; i < 32246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32247(int& val) {
    func_in_32247(val);
    const int x = 32247;
    std::cout << 32247;
    for (int i = 0; i < 32247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32247(int& val) {
    const int x = 32247;
    std::cout << 32247;
    for (int i = 0; i < 32247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32248(int& val) {
    func_in_32248(val);
    const int x = 32248;
    std::cout << 32248;
    for (int i = 0; i < 32248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32248(int& val) {
    const int x = 32248;
    std::cout << 32248;
    for (int i = 0; i < 32248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32249(int& val) {
    func_in_32249(val);
    const int x = 32249;
    std::cout << 32249;
    for (int i = 0; i < 32249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32249(int& val) {
    const int x = 32249;
    std::cout << 32249;
    for (int i = 0; i < 32249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32250(int& val) {
    func_in_32250(val);
    const int x = 32250;
    std::cout << 32250;
    for (int i = 0; i < 32250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32250(int& val) {
    const int x = 32250;
    std::cout << 32250;
    for (int i = 0; i < 32250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32251(int& val) {
    func_in_32251(val);
    const int x = 32251;
    std::cout << 32251;
    for (int i = 0; i < 32251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32251(int& val) {
    const int x = 32251;
    std::cout << 32251;
    for (int i = 0; i < 32251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32252(int& val) {
    func_in_32252(val);
    const int x = 32252;
    std::cout << 32252;
    for (int i = 0; i < 32252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32252(int& val) {
    const int x = 32252;
    std::cout << 32252;
    for (int i = 0; i < 32252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32253(int& val) {
    func_in_32253(val);
    const int x = 32253;
    std::cout << 32253;
    for (int i = 0; i < 32253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32253(int& val) {
    const int x = 32253;
    std::cout << 32253;
    for (int i = 0; i < 32253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32254(int& val) {
    func_in_32254(val);
    const int x = 32254;
    std::cout << 32254;
    for (int i = 0; i < 32254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32254(int& val) {
    const int x = 32254;
    std::cout << 32254;
    for (int i = 0; i < 32254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32255(int& val) {
    func_in_32255(val);
    const int x = 32255;
    std::cout << 32255;
    for (int i = 0; i < 32255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32255(int& val) {
    const int x = 32255;
    std::cout << 32255;
    for (int i = 0; i < 32255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32256(int& val) {
    func_in_32256(val);
    const int x = 32256;
    std::cout << 32256;
    for (int i = 0; i < 32256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32256(int& val) {
    const int x = 32256;
    std::cout << 32256;
    for (int i = 0; i < 32256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32257(int& val) {
    func_in_32257(val);
    const int x = 32257;
    std::cout << 32257;
    for (int i = 0; i < 32257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32257(int& val) {
    const int x = 32257;
    std::cout << 32257;
    for (int i = 0; i < 32257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32258(int& val) {
    func_in_32258(val);
    const int x = 32258;
    std::cout << 32258;
    for (int i = 0; i < 32258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32258(int& val) {
    const int x = 32258;
    std::cout << 32258;
    for (int i = 0; i < 32258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32259(int& val) {
    func_in_32259(val);
    const int x = 32259;
    std::cout << 32259;
    for (int i = 0; i < 32259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32259(int& val) {
    const int x = 32259;
    std::cout << 32259;
    for (int i = 0; i < 32259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32260(int& val) {
    func_in_32260(val);
    const int x = 32260;
    std::cout << 32260;
    for (int i = 0; i < 32260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32260(int& val) {
    const int x = 32260;
    std::cout << 32260;
    for (int i = 0; i < 32260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32261(int& val) {
    func_in_32261(val);
    const int x = 32261;
    std::cout << 32261;
    for (int i = 0; i < 32261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32261(int& val) {
    const int x = 32261;
    std::cout << 32261;
    for (int i = 0; i < 32261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32262(int& val) {
    func_in_32262(val);
    const int x = 32262;
    std::cout << 32262;
    for (int i = 0; i < 32262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32262(int& val) {
    const int x = 32262;
    std::cout << 32262;
    for (int i = 0; i < 32262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32263(int& val) {
    func_in_32263(val);
    const int x = 32263;
    std::cout << 32263;
    for (int i = 0; i < 32263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32263(int& val) {
    const int x = 32263;
    std::cout << 32263;
    for (int i = 0; i < 32263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32264(int& val) {
    func_in_32264(val);
    const int x = 32264;
    std::cout << 32264;
    for (int i = 0; i < 32264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32264(int& val) {
    const int x = 32264;
    std::cout << 32264;
    for (int i = 0; i < 32264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32265(int& val) {
    func_in_32265(val);
    const int x = 32265;
    std::cout << 32265;
    for (int i = 0; i < 32265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32265(int& val) {
    const int x = 32265;
    std::cout << 32265;
    for (int i = 0; i < 32265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32266(int& val) {
    func_in_32266(val);
    const int x = 32266;
    std::cout << 32266;
    for (int i = 0; i < 32266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32266(int& val) {
    const int x = 32266;
    std::cout << 32266;
    for (int i = 0; i < 32266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32267(int& val) {
    func_in_32267(val);
    const int x = 32267;
    std::cout << 32267;
    for (int i = 0; i < 32267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32267(int& val) {
    const int x = 32267;
    std::cout << 32267;
    for (int i = 0; i < 32267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32268(int& val) {
    func_in_32268(val);
    const int x = 32268;
    std::cout << 32268;
    for (int i = 0; i < 32268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32268(int& val) {
    const int x = 32268;
    std::cout << 32268;
    for (int i = 0; i < 32268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32269(int& val) {
    func_in_32269(val);
    const int x = 32269;
    std::cout << 32269;
    for (int i = 0; i < 32269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32269(int& val) {
    const int x = 32269;
    std::cout << 32269;
    for (int i = 0; i < 32269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32270(int& val) {
    func_in_32270(val);
    const int x = 32270;
    std::cout << 32270;
    for (int i = 0; i < 32270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32270(int& val) {
    const int x = 32270;
    std::cout << 32270;
    for (int i = 0; i < 32270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32271(int& val) {
    func_in_32271(val);
    const int x = 32271;
    std::cout << 32271;
    for (int i = 0; i < 32271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32271(int& val) {
    const int x = 32271;
    std::cout << 32271;
    for (int i = 0; i < 32271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32272(int& val) {
    func_in_32272(val);
    const int x = 32272;
    std::cout << 32272;
    for (int i = 0; i < 32272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32272(int& val) {
    const int x = 32272;
    std::cout << 32272;
    for (int i = 0; i < 32272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32273(int& val) {
    func_in_32273(val);
    const int x = 32273;
    std::cout << 32273;
    for (int i = 0; i < 32273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32273(int& val) {
    const int x = 32273;
    std::cout << 32273;
    for (int i = 0; i < 32273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32274(int& val) {
    func_in_32274(val);
    const int x = 32274;
    std::cout << 32274;
    for (int i = 0; i < 32274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32274(int& val) {
    const int x = 32274;
    std::cout << 32274;
    for (int i = 0; i < 32274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32275(int& val) {
    func_in_32275(val);
    const int x = 32275;
    std::cout << 32275;
    for (int i = 0; i < 32275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32275(int& val) {
    const int x = 32275;
    std::cout << 32275;
    for (int i = 0; i < 32275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32276(int& val) {
    func_in_32276(val);
    const int x = 32276;
    std::cout << 32276;
    for (int i = 0; i < 32276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32276(int& val) {
    const int x = 32276;
    std::cout << 32276;
    for (int i = 0; i < 32276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32277(int& val) {
    func_in_32277(val);
    const int x = 32277;
    std::cout << 32277;
    for (int i = 0; i < 32277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32277(int& val) {
    const int x = 32277;
    std::cout << 32277;
    for (int i = 0; i < 32277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32278(int& val) {
    func_in_32278(val);
    const int x = 32278;
    std::cout << 32278;
    for (int i = 0; i < 32278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32278(int& val) {
    const int x = 32278;
    std::cout << 32278;
    for (int i = 0; i < 32278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32279(int& val) {
    func_in_32279(val);
    const int x = 32279;
    std::cout << 32279;
    for (int i = 0; i < 32279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32279(int& val) {
    const int x = 32279;
    std::cout << 32279;
    for (int i = 0; i < 32279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32280(int& val) {
    func_in_32280(val);
    const int x = 32280;
    std::cout << 32280;
    for (int i = 0; i < 32280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32280(int& val) {
    const int x = 32280;
    std::cout << 32280;
    for (int i = 0; i < 32280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32281(int& val) {
    func_in_32281(val);
    const int x = 32281;
    std::cout << 32281;
    for (int i = 0; i < 32281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32281(int& val) {
    const int x = 32281;
    std::cout << 32281;
    for (int i = 0; i < 32281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32282(int& val) {
    func_in_32282(val);
    const int x = 32282;
    std::cout << 32282;
    for (int i = 0; i < 32282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32282(int& val) {
    const int x = 32282;
    std::cout << 32282;
    for (int i = 0; i < 32282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32283(int& val) {
    func_in_32283(val);
    const int x = 32283;
    std::cout << 32283;
    for (int i = 0; i < 32283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32283(int& val) {
    const int x = 32283;
    std::cout << 32283;
    for (int i = 0; i < 32283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32284(int& val) {
    func_in_32284(val);
    const int x = 32284;
    std::cout << 32284;
    for (int i = 0; i < 32284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32284(int& val) {
    const int x = 32284;
    std::cout << 32284;
    for (int i = 0; i < 32284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32285(int& val) {
    func_in_32285(val);
    const int x = 32285;
    std::cout << 32285;
    for (int i = 0; i < 32285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32285(int& val) {
    const int x = 32285;
    std::cout << 32285;
    for (int i = 0; i < 32285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32286(int& val) {
    func_in_32286(val);
    const int x = 32286;
    std::cout << 32286;
    for (int i = 0; i < 32286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32286(int& val) {
    const int x = 32286;
    std::cout << 32286;
    for (int i = 0; i < 32286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32287(int& val) {
    func_in_32287(val);
    const int x = 32287;
    std::cout << 32287;
    for (int i = 0; i < 32287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32287(int& val) {
    const int x = 32287;
    std::cout << 32287;
    for (int i = 0; i < 32287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32288(int& val) {
    func_in_32288(val);
    const int x = 32288;
    std::cout << 32288;
    for (int i = 0; i < 32288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32288(int& val) {
    const int x = 32288;
    std::cout << 32288;
    for (int i = 0; i < 32288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32289(int& val) {
    func_in_32289(val);
    const int x = 32289;
    std::cout << 32289;
    for (int i = 0; i < 32289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32289(int& val) {
    const int x = 32289;
    std::cout << 32289;
    for (int i = 0; i < 32289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32290(int& val) {
    func_in_32290(val);
    const int x = 32290;
    std::cout << 32290;
    for (int i = 0; i < 32290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32290(int& val) {
    const int x = 32290;
    std::cout << 32290;
    for (int i = 0; i < 32290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32291(int& val) {
    func_in_32291(val);
    const int x = 32291;
    std::cout << 32291;
    for (int i = 0; i < 32291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32291(int& val) {
    const int x = 32291;
    std::cout << 32291;
    for (int i = 0; i < 32291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32292(int& val) {
    func_in_32292(val);
    const int x = 32292;
    std::cout << 32292;
    for (int i = 0; i < 32292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32292(int& val) {
    const int x = 32292;
    std::cout << 32292;
    for (int i = 0; i < 32292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32293(int& val) {
    func_in_32293(val);
    const int x = 32293;
    std::cout << 32293;
    for (int i = 0; i < 32293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32293(int& val) {
    const int x = 32293;
    std::cout << 32293;
    for (int i = 0; i < 32293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32294(int& val) {
    func_in_32294(val);
    const int x = 32294;
    std::cout << 32294;
    for (int i = 0; i < 32294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32294(int& val) {
    const int x = 32294;
    std::cout << 32294;
    for (int i = 0; i < 32294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32295(int& val) {
    func_in_32295(val);
    const int x = 32295;
    std::cout << 32295;
    for (int i = 0; i < 32295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32295(int& val) {
    const int x = 32295;
    std::cout << 32295;
    for (int i = 0; i < 32295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32296(int& val) {
    func_in_32296(val);
    const int x = 32296;
    std::cout << 32296;
    for (int i = 0; i < 32296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32296(int& val) {
    const int x = 32296;
    std::cout << 32296;
    for (int i = 0; i < 32296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32297(int& val) {
    func_in_32297(val);
    const int x = 32297;
    std::cout << 32297;
    for (int i = 0; i < 32297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32297(int& val) {
    const int x = 32297;
    std::cout << 32297;
    for (int i = 0; i < 32297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32298(int& val) {
    func_in_32298(val);
    const int x = 32298;
    std::cout << 32298;
    for (int i = 0; i < 32298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32298(int& val) {
    const int x = 32298;
    std::cout << 32298;
    for (int i = 0; i < 32298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32299(int& val) {
    func_in_32299(val);
    const int x = 32299;
    std::cout << 32299;
    for (int i = 0; i < 32299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32299(int& val) {
    const int x = 32299;
    std::cout << 32299;
    for (int i = 0; i < 32299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32300(int& val) {
    func_in_32300(val);
    const int x = 32300;
    std::cout << 32300;
    for (int i = 0; i < 32300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32300(int& val) {
    const int x = 32300;
    std::cout << 32300;
    for (int i = 0; i < 32300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32301(int& val) {
    func_in_32301(val);
    const int x = 32301;
    std::cout << 32301;
    for (int i = 0; i < 32301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32301(int& val) {
    const int x = 32301;
    std::cout << 32301;
    for (int i = 0; i < 32301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32302(int& val) {
    func_in_32302(val);
    const int x = 32302;
    std::cout << 32302;
    for (int i = 0; i < 32302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32302(int& val) {
    const int x = 32302;
    std::cout << 32302;
    for (int i = 0; i < 32302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32303(int& val) {
    func_in_32303(val);
    const int x = 32303;
    std::cout << 32303;
    for (int i = 0; i < 32303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32303(int& val) {
    const int x = 32303;
    std::cout << 32303;
    for (int i = 0; i < 32303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32304(int& val) {
    func_in_32304(val);
    const int x = 32304;
    std::cout << 32304;
    for (int i = 0; i < 32304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32304(int& val) {
    const int x = 32304;
    std::cout << 32304;
    for (int i = 0; i < 32304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32305(int& val) {
    func_in_32305(val);
    const int x = 32305;
    std::cout << 32305;
    for (int i = 0; i < 32305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32305(int& val) {
    const int x = 32305;
    std::cout << 32305;
    for (int i = 0; i < 32305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32306(int& val) {
    func_in_32306(val);
    const int x = 32306;
    std::cout << 32306;
    for (int i = 0; i < 32306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32306(int& val) {
    const int x = 32306;
    std::cout << 32306;
    for (int i = 0; i < 32306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32307(int& val) {
    func_in_32307(val);
    const int x = 32307;
    std::cout << 32307;
    for (int i = 0; i < 32307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32307(int& val) {
    const int x = 32307;
    std::cout << 32307;
    for (int i = 0; i < 32307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32308(int& val) {
    func_in_32308(val);
    const int x = 32308;
    std::cout << 32308;
    for (int i = 0; i < 32308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32308(int& val) {
    const int x = 32308;
    std::cout << 32308;
    for (int i = 0; i < 32308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32309(int& val) {
    func_in_32309(val);
    const int x = 32309;
    std::cout << 32309;
    for (int i = 0; i < 32309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32309(int& val) {
    const int x = 32309;
    std::cout << 32309;
    for (int i = 0; i < 32309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32310(int& val) {
    func_in_32310(val);
    const int x = 32310;
    std::cout << 32310;
    for (int i = 0; i < 32310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32310(int& val) {
    const int x = 32310;
    std::cout << 32310;
    for (int i = 0; i < 32310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32311(int& val) {
    func_in_32311(val);
    const int x = 32311;
    std::cout << 32311;
    for (int i = 0; i < 32311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32311(int& val) {
    const int x = 32311;
    std::cout << 32311;
    for (int i = 0; i < 32311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32312(int& val) {
    func_in_32312(val);
    const int x = 32312;
    std::cout << 32312;
    for (int i = 0; i < 32312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32312(int& val) {
    const int x = 32312;
    std::cout << 32312;
    for (int i = 0; i < 32312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32313(int& val) {
    func_in_32313(val);
    const int x = 32313;
    std::cout << 32313;
    for (int i = 0; i < 32313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32313(int& val) {
    const int x = 32313;
    std::cout << 32313;
    for (int i = 0; i < 32313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32314(int& val) {
    func_in_32314(val);
    const int x = 32314;
    std::cout << 32314;
    for (int i = 0; i < 32314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32314(int& val) {
    const int x = 32314;
    std::cout << 32314;
    for (int i = 0; i < 32314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32315(int& val) {
    func_in_32315(val);
    const int x = 32315;
    std::cout << 32315;
    for (int i = 0; i < 32315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32315(int& val) {
    const int x = 32315;
    std::cout << 32315;
    for (int i = 0; i < 32315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32316(int& val) {
    func_in_32316(val);
    const int x = 32316;
    std::cout << 32316;
    for (int i = 0; i < 32316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32316(int& val) {
    const int x = 32316;
    std::cout << 32316;
    for (int i = 0; i < 32316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32317(int& val) {
    func_in_32317(val);
    const int x = 32317;
    std::cout << 32317;
    for (int i = 0; i < 32317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32317(int& val) {
    const int x = 32317;
    std::cout << 32317;
    for (int i = 0; i < 32317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32318(int& val) {
    func_in_32318(val);
    const int x = 32318;
    std::cout << 32318;
    for (int i = 0; i < 32318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32318(int& val) {
    const int x = 32318;
    std::cout << 32318;
    for (int i = 0; i < 32318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32319(int& val) {
    func_in_32319(val);
    const int x = 32319;
    std::cout << 32319;
    for (int i = 0; i < 32319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32319(int& val) {
    const int x = 32319;
    std::cout << 32319;
    for (int i = 0; i < 32319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32320(int& val) {
    func_in_32320(val);
    const int x = 32320;
    std::cout << 32320;
    for (int i = 0; i < 32320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32320(int& val) {
    const int x = 32320;
    std::cout << 32320;
    for (int i = 0; i < 32320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32321(int& val) {
    func_in_32321(val);
    const int x = 32321;
    std::cout << 32321;
    for (int i = 0; i < 32321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32321(int& val) {
    const int x = 32321;
    std::cout << 32321;
    for (int i = 0; i < 32321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32322(int& val) {
    func_in_32322(val);
    const int x = 32322;
    std::cout << 32322;
    for (int i = 0; i < 32322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32322(int& val) {
    const int x = 32322;
    std::cout << 32322;
    for (int i = 0; i < 32322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32323(int& val) {
    func_in_32323(val);
    const int x = 32323;
    std::cout << 32323;
    for (int i = 0; i < 32323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32323(int& val) {
    const int x = 32323;
    std::cout << 32323;
    for (int i = 0; i < 32323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32324(int& val) {
    func_in_32324(val);
    const int x = 32324;
    std::cout << 32324;
    for (int i = 0; i < 32324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32324(int& val) {
    const int x = 32324;
    std::cout << 32324;
    for (int i = 0; i < 32324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32325(int& val) {
    func_in_32325(val);
    const int x = 32325;
    std::cout << 32325;
    for (int i = 0; i < 32325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32325(int& val) {
    const int x = 32325;
    std::cout << 32325;
    for (int i = 0; i < 32325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32326(int& val) {
    func_in_32326(val);
    const int x = 32326;
    std::cout << 32326;
    for (int i = 0; i < 32326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32326(int& val) {
    const int x = 32326;
    std::cout << 32326;
    for (int i = 0; i < 32326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32327(int& val) {
    func_in_32327(val);
    const int x = 32327;
    std::cout << 32327;
    for (int i = 0; i < 32327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32327(int& val) {
    const int x = 32327;
    std::cout << 32327;
    for (int i = 0; i < 32327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32328(int& val) {
    func_in_32328(val);
    const int x = 32328;
    std::cout << 32328;
    for (int i = 0; i < 32328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32328(int& val) {
    const int x = 32328;
    std::cout << 32328;
    for (int i = 0; i < 32328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32329(int& val) {
    func_in_32329(val);
    const int x = 32329;
    std::cout << 32329;
    for (int i = 0; i < 32329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32329(int& val) {
    const int x = 32329;
    std::cout << 32329;
    for (int i = 0; i < 32329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32330(int& val) {
    func_in_32330(val);
    const int x = 32330;
    std::cout << 32330;
    for (int i = 0; i < 32330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32330(int& val) {
    const int x = 32330;
    std::cout << 32330;
    for (int i = 0; i < 32330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32331(int& val) {
    func_in_32331(val);
    const int x = 32331;
    std::cout << 32331;
    for (int i = 0; i < 32331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32331(int& val) {
    const int x = 32331;
    std::cout << 32331;
    for (int i = 0; i < 32331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32332(int& val) {
    func_in_32332(val);
    const int x = 32332;
    std::cout << 32332;
    for (int i = 0; i < 32332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32332(int& val) {
    const int x = 32332;
    std::cout << 32332;
    for (int i = 0; i < 32332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32333(int& val) {
    func_in_32333(val);
    const int x = 32333;
    std::cout << 32333;
    for (int i = 0; i < 32333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32333(int& val) {
    const int x = 32333;
    std::cout << 32333;
    for (int i = 0; i < 32333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32334(int& val) {
    func_in_32334(val);
    const int x = 32334;
    std::cout << 32334;
    for (int i = 0; i < 32334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32334(int& val) {
    const int x = 32334;
    std::cout << 32334;
    for (int i = 0; i < 32334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32335(int& val) {
    func_in_32335(val);
    const int x = 32335;
    std::cout << 32335;
    for (int i = 0; i < 32335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32335(int& val) {
    const int x = 32335;
    std::cout << 32335;
    for (int i = 0; i < 32335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32336(int& val) {
    func_in_32336(val);
    const int x = 32336;
    std::cout << 32336;
    for (int i = 0; i < 32336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32336(int& val) {
    const int x = 32336;
    std::cout << 32336;
    for (int i = 0; i < 32336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32337(int& val) {
    func_in_32337(val);
    const int x = 32337;
    std::cout << 32337;
    for (int i = 0; i < 32337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32337(int& val) {
    const int x = 32337;
    std::cout << 32337;
    for (int i = 0; i < 32337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32338(int& val) {
    func_in_32338(val);
    const int x = 32338;
    std::cout << 32338;
    for (int i = 0; i < 32338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32338(int& val) {
    const int x = 32338;
    std::cout << 32338;
    for (int i = 0; i < 32338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32339(int& val) {
    func_in_32339(val);
    const int x = 32339;
    std::cout << 32339;
    for (int i = 0; i < 32339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32339(int& val) {
    const int x = 32339;
    std::cout << 32339;
    for (int i = 0; i < 32339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32340(int& val) {
    func_in_32340(val);
    const int x = 32340;
    std::cout << 32340;
    for (int i = 0; i < 32340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32340(int& val) {
    const int x = 32340;
    std::cout << 32340;
    for (int i = 0; i < 32340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32341(int& val) {
    func_in_32341(val);
    const int x = 32341;
    std::cout << 32341;
    for (int i = 0; i < 32341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32341(int& val) {
    const int x = 32341;
    std::cout << 32341;
    for (int i = 0; i < 32341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32342(int& val) {
    func_in_32342(val);
    const int x = 32342;
    std::cout << 32342;
    for (int i = 0; i < 32342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32342(int& val) {
    const int x = 32342;
    std::cout << 32342;
    for (int i = 0; i < 32342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32343(int& val) {
    func_in_32343(val);
    const int x = 32343;
    std::cout << 32343;
    for (int i = 0; i < 32343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32343(int& val) {
    const int x = 32343;
    std::cout << 32343;
    for (int i = 0; i < 32343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32344(int& val) {
    func_in_32344(val);
    const int x = 32344;
    std::cout << 32344;
    for (int i = 0; i < 32344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32344(int& val) {
    const int x = 32344;
    std::cout << 32344;
    for (int i = 0; i < 32344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32345(int& val) {
    func_in_32345(val);
    const int x = 32345;
    std::cout << 32345;
    for (int i = 0; i < 32345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32345(int& val) {
    const int x = 32345;
    std::cout << 32345;
    for (int i = 0; i < 32345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32346(int& val) {
    func_in_32346(val);
    const int x = 32346;
    std::cout << 32346;
    for (int i = 0; i < 32346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32346(int& val) {
    const int x = 32346;
    std::cout << 32346;
    for (int i = 0; i < 32346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32347(int& val) {
    func_in_32347(val);
    const int x = 32347;
    std::cout << 32347;
    for (int i = 0; i < 32347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32347(int& val) {
    const int x = 32347;
    std::cout << 32347;
    for (int i = 0; i < 32347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32348(int& val) {
    func_in_32348(val);
    const int x = 32348;
    std::cout << 32348;
    for (int i = 0; i < 32348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32348(int& val) {
    const int x = 32348;
    std::cout << 32348;
    for (int i = 0; i < 32348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32349(int& val) {
    func_in_32349(val);
    const int x = 32349;
    std::cout << 32349;
    for (int i = 0; i < 32349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32349(int& val) {
    const int x = 32349;
    std::cout << 32349;
    for (int i = 0; i < 32349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32350(int& val) {
    func_in_32350(val);
    const int x = 32350;
    std::cout << 32350;
    for (int i = 0; i < 32350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32350(int& val) {
    const int x = 32350;
    std::cout << 32350;
    for (int i = 0; i < 32350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32351(int& val) {
    func_in_32351(val);
    const int x = 32351;
    std::cout << 32351;
    for (int i = 0; i < 32351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32351(int& val) {
    const int x = 32351;
    std::cout << 32351;
    for (int i = 0; i < 32351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32352(int& val) {
    func_in_32352(val);
    const int x = 32352;
    std::cout << 32352;
    for (int i = 0; i < 32352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32352(int& val) {
    const int x = 32352;
    std::cout << 32352;
    for (int i = 0; i < 32352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32353(int& val) {
    func_in_32353(val);
    const int x = 32353;
    std::cout << 32353;
    for (int i = 0; i < 32353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32353(int& val) {
    const int x = 32353;
    std::cout << 32353;
    for (int i = 0; i < 32353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32354(int& val) {
    func_in_32354(val);
    const int x = 32354;
    std::cout << 32354;
    for (int i = 0; i < 32354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32354(int& val) {
    const int x = 32354;
    std::cout << 32354;
    for (int i = 0; i < 32354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32355(int& val) {
    func_in_32355(val);
    const int x = 32355;
    std::cout << 32355;
    for (int i = 0; i < 32355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32355(int& val) {
    const int x = 32355;
    std::cout << 32355;
    for (int i = 0; i < 32355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32356(int& val) {
    func_in_32356(val);
    const int x = 32356;
    std::cout << 32356;
    for (int i = 0; i < 32356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32356(int& val) {
    const int x = 32356;
    std::cout << 32356;
    for (int i = 0; i < 32356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32357(int& val) {
    func_in_32357(val);
    const int x = 32357;
    std::cout << 32357;
    for (int i = 0; i < 32357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32357(int& val) {
    const int x = 32357;
    std::cout << 32357;
    for (int i = 0; i < 32357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32358(int& val) {
    func_in_32358(val);
    const int x = 32358;
    std::cout << 32358;
    for (int i = 0; i < 32358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32358(int& val) {
    const int x = 32358;
    std::cout << 32358;
    for (int i = 0; i < 32358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32359(int& val) {
    func_in_32359(val);
    const int x = 32359;
    std::cout << 32359;
    for (int i = 0; i < 32359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32359(int& val) {
    const int x = 32359;
    std::cout << 32359;
    for (int i = 0; i < 32359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32360(int& val) {
    func_in_32360(val);
    const int x = 32360;
    std::cout << 32360;
    for (int i = 0; i < 32360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32360(int& val) {
    const int x = 32360;
    std::cout << 32360;
    for (int i = 0; i < 32360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32361(int& val) {
    func_in_32361(val);
    const int x = 32361;
    std::cout << 32361;
    for (int i = 0; i < 32361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32361(int& val) {
    const int x = 32361;
    std::cout << 32361;
    for (int i = 0; i < 32361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32362(int& val) {
    func_in_32362(val);
    const int x = 32362;
    std::cout << 32362;
    for (int i = 0; i < 32362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32362(int& val) {
    const int x = 32362;
    std::cout << 32362;
    for (int i = 0; i < 32362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32363(int& val) {
    func_in_32363(val);
    const int x = 32363;
    std::cout << 32363;
    for (int i = 0; i < 32363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32363(int& val) {
    const int x = 32363;
    std::cout << 32363;
    for (int i = 0; i < 32363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32364(int& val) {
    func_in_32364(val);
    const int x = 32364;
    std::cout << 32364;
    for (int i = 0; i < 32364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32364(int& val) {
    const int x = 32364;
    std::cout << 32364;
    for (int i = 0; i < 32364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32365(int& val) {
    func_in_32365(val);
    const int x = 32365;
    std::cout << 32365;
    for (int i = 0; i < 32365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32365(int& val) {
    const int x = 32365;
    std::cout << 32365;
    for (int i = 0; i < 32365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32366(int& val) {
    func_in_32366(val);
    const int x = 32366;
    std::cout << 32366;
    for (int i = 0; i < 32366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32366(int& val) {
    const int x = 32366;
    std::cout << 32366;
    for (int i = 0; i < 32366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32367(int& val) {
    func_in_32367(val);
    const int x = 32367;
    std::cout << 32367;
    for (int i = 0; i < 32367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32367(int& val) {
    const int x = 32367;
    std::cout << 32367;
    for (int i = 0; i < 32367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32368(int& val) {
    func_in_32368(val);
    const int x = 32368;
    std::cout << 32368;
    for (int i = 0; i < 32368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32368(int& val) {
    const int x = 32368;
    std::cout << 32368;
    for (int i = 0; i < 32368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32369(int& val) {
    func_in_32369(val);
    const int x = 32369;
    std::cout << 32369;
    for (int i = 0; i < 32369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32369(int& val) {
    const int x = 32369;
    std::cout << 32369;
    for (int i = 0; i < 32369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32370(int& val) {
    func_in_32370(val);
    const int x = 32370;
    std::cout << 32370;
    for (int i = 0; i < 32370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32370(int& val) {
    const int x = 32370;
    std::cout << 32370;
    for (int i = 0; i < 32370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32371(int& val) {
    func_in_32371(val);
    const int x = 32371;
    std::cout << 32371;
    for (int i = 0; i < 32371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32371(int& val) {
    const int x = 32371;
    std::cout << 32371;
    for (int i = 0; i < 32371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32372(int& val) {
    func_in_32372(val);
    const int x = 32372;
    std::cout << 32372;
    for (int i = 0; i < 32372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32372(int& val) {
    const int x = 32372;
    std::cout << 32372;
    for (int i = 0; i < 32372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32373(int& val) {
    func_in_32373(val);
    const int x = 32373;
    std::cout << 32373;
    for (int i = 0; i < 32373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32373(int& val) {
    const int x = 32373;
    std::cout << 32373;
    for (int i = 0; i < 32373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32374(int& val) {
    func_in_32374(val);
    const int x = 32374;
    std::cout << 32374;
    for (int i = 0; i < 32374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32374(int& val) {
    const int x = 32374;
    std::cout << 32374;
    for (int i = 0; i < 32374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32375(int& val) {
    func_in_32375(val);
    const int x = 32375;
    std::cout << 32375;
    for (int i = 0; i < 32375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32375(int& val) {
    const int x = 32375;
    std::cout << 32375;
    for (int i = 0; i < 32375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32376(int& val) {
    func_in_32376(val);
    const int x = 32376;
    std::cout << 32376;
    for (int i = 0; i < 32376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32376(int& val) {
    const int x = 32376;
    std::cout << 32376;
    for (int i = 0; i < 32376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32377(int& val) {
    func_in_32377(val);
    const int x = 32377;
    std::cout << 32377;
    for (int i = 0; i < 32377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32377(int& val) {
    const int x = 32377;
    std::cout << 32377;
    for (int i = 0; i < 32377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32378(int& val) {
    func_in_32378(val);
    const int x = 32378;
    std::cout << 32378;
    for (int i = 0; i < 32378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32378(int& val) {
    const int x = 32378;
    std::cout << 32378;
    for (int i = 0; i < 32378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32379(int& val) {
    func_in_32379(val);
    const int x = 32379;
    std::cout << 32379;
    for (int i = 0; i < 32379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32379(int& val) {
    const int x = 32379;
    std::cout << 32379;
    for (int i = 0; i < 32379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32380(int& val) {
    func_in_32380(val);
    const int x = 32380;
    std::cout << 32380;
    for (int i = 0; i < 32380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32380(int& val) {
    const int x = 32380;
    std::cout << 32380;
    for (int i = 0; i < 32380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32381(int& val) {
    func_in_32381(val);
    const int x = 32381;
    std::cout << 32381;
    for (int i = 0; i < 32381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32381(int& val) {
    const int x = 32381;
    std::cout << 32381;
    for (int i = 0; i < 32381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32382(int& val) {
    func_in_32382(val);
    const int x = 32382;
    std::cout << 32382;
    for (int i = 0; i < 32382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32382(int& val) {
    const int x = 32382;
    std::cout << 32382;
    for (int i = 0; i < 32382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32383(int& val) {
    func_in_32383(val);
    const int x = 32383;
    std::cout << 32383;
    for (int i = 0; i < 32383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32383(int& val) {
    const int x = 32383;
    std::cout << 32383;
    for (int i = 0; i < 32383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32384(int& val) {
    func_in_32384(val);
    const int x = 32384;
    std::cout << 32384;
    for (int i = 0; i < 32384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32384(int& val) {
    const int x = 32384;
    std::cout << 32384;
    for (int i = 0; i < 32384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32385(int& val) {
    func_in_32385(val);
    const int x = 32385;
    std::cout << 32385;
    for (int i = 0; i < 32385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32385(int& val) {
    const int x = 32385;
    std::cout << 32385;
    for (int i = 0; i < 32385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32386(int& val) {
    func_in_32386(val);
    const int x = 32386;
    std::cout << 32386;
    for (int i = 0; i < 32386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32386(int& val) {
    const int x = 32386;
    std::cout << 32386;
    for (int i = 0; i < 32386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32387(int& val) {
    func_in_32387(val);
    const int x = 32387;
    std::cout << 32387;
    for (int i = 0; i < 32387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32387(int& val) {
    const int x = 32387;
    std::cout << 32387;
    for (int i = 0; i < 32387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32388(int& val) {
    func_in_32388(val);
    const int x = 32388;
    std::cout << 32388;
    for (int i = 0; i < 32388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32388(int& val) {
    const int x = 32388;
    std::cout << 32388;
    for (int i = 0; i < 32388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32389(int& val) {
    func_in_32389(val);
    const int x = 32389;
    std::cout << 32389;
    for (int i = 0; i < 32389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32389(int& val) {
    const int x = 32389;
    std::cout << 32389;
    for (int i = 0; i < 32389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32390(int& val) {
    func_in_32390(val);
    const int x = 32390;
    std::cout << 32390;
    for (int i = 0; i < 32390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32390(int& val) {
    const int x = 32390;
    std::cout << 32390;
    for (int i = 0; i < 32390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32391(int& val) {
    func_in_32391(val);
    const int x = 32391;
    std::cout << 32391;
    for (int i = 0; i < 32391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32391(int& val) {
    const int x = 32391;
    std::cout << 32391;
    for (int i = 0; i < 32391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32392(int& val) {
    func_in_32392(val);
    const int x = 32392;
    std::cout << 32392;
    for (int i = 0; i < 32392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32392(int& val) {
    const int x = 32392;
    std::cout << 32392;
    for (int i = 0; i < 32392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32393(int& val) {
    func_in_32393(val);
    const int x = 32393;
    std::cout << 32393;
    for (int i = 0; i < 32393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32393(int& val) {
    const int x = 32393;
    std::cout << 32393;
    for (int i = 0; i < 32393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32394(int& val) {
    func_in_32394(val);
    const int x = 32394;
    std::cout << 32394;
    for (int i = 0; i < 32394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32394(int& val) {
    const int x = 32394;
    std::cout << 32394;
    for (int i = 0; i < 32394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32395(int& val) {
    func_in_32395(val);
    const int x = 32395;
    std::cout << 32395;
    for (int i = 0; i < 32395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32395(int& val) {
    const int x = 32395;
    std::cout << 32395;
    for (int i = 0; i < 32395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32396(int& val) {
    func_in_32396(val);
    const int x = 32396;
    std::cout << 32396;
    for (int i = 0; i < 32396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32396(int& val) {
    const int x = 32396;
    std::cout << 32396;
    for (int i = 0; i < 32396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32397(int& val) {
    func_in_32397(val);
    const int x = 32397;
    std::cout << 32397;
    for (int i = 0; i < 32397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32397(int& val) {
    const int x = 32397;
    std::cout << 32397;
    for (int i = 0; i < 32397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32398(int& val) {
    func_in_32398(val);
    const int x = 32398;
    std::cout << 32398;
    for (int i = 0; i < 32398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32398(int& val) {
    const int x = 32398;
    std::cout << 32398;
    for (int i = 0; i < 32398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32399(int& val) {
    func_in_32399(val);
    const int x = 32399;
    std::cout << 32399;
    for (int i = 0; i < 32399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32399(int& val) {
    const int x = 32399;
    std::cout << 32399;
    for (int i = 0; i < 32399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32400(int& val) {
    func_in_32400(val);
    const int x = 32400;
    std::cout << 32400;
    for (int i = 0; i < 32400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32400(int& val) {
    const int x = 32400;
    std::cout << 32400;
    for (int i = 0; i < 32400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32401(int& val) {
    func_in_32401(val);
    const int x = 32401;
    std::cout << 32401;
    for (int i = 0; i < 32401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32401(int& val) {
    const int x = 32401;
    std::cout << 32401;
    for (int i = 0; i < 32401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32402(int& val) {
    func_in_32402(val);
    const int x = 32402;
    std::cout << 32402;
    for (int i = 0; i < 32402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32402(int& val) {
    const int x = 32402;
    std::cout << 32402;
    for (int i = 0; i < 32402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32403(int& val) {
    func_in_32403(val);
    const int x = 32403;
    std::cout << 32403;
    for (int i = 0; i < 32403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32403(int& val) {
    const int x = 32403;
    std::cout << 32403;
    for (int i = 0; i < 32403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32404(int& val) {
    func_in_32404(val);
    const int x = 32404;
    std::cout << 32404;
    for (int i = 0; i < 32404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32404(int& val) {
    const int x = 32404;
    std::cout << 32404;
    for (int i = 0; i < 32404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32405(int& val) {
    func_in_32405(val);
    const int x = 32405;
    std::cout << 32405;
    for (int i = 0; i < 32405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32405(int& val) {
    const int x = 32405;
    std::cout << 32405;
    for (int i = 0; i < 32405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32406(int& val) {
    func_in_32406(val);
    const int x = 32406;
    std::cout << 32406;
    for (int i = 0; i < 32406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32406(int& val) {
    const int x = 32406;
    std::cout << 32406;
    for (int i = 0; i < 32406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32407(int& val) {
    func_in_32407(val);
    const int x = 32407;
    std::cout << 32407;
    for (int i = 0; i < 32407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32407(int& val) {
    const int x = 32407;
    std::cout << 32407;
    for (int i = 0; i < 32407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32408(int& val) {
    func_in_32408(val);
    const int x = 32408;
    std::cout << 32408;
    for (int i = 0; i < 32408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32408(int& val) {
    const int x = 32408;
    std::cout << 32408;
    for (int i = 0; i < 32408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32409(int& val) {
    func_in_32409(val);
    const int x = 32409;
    std::cout << 32409;
    for (int i = 0; i < 32409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32409(int& val) {
    const int x = 32409;
    std::cout << 32409;
    for (int i = 0; i < 32409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32410(int& val) {
    func_in_32410(val);
    const int x = 32410;
    std::cout << 32410;
    for (int i = 0; i < 32410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32410(int& val) {
    const int x = 32410;
    std::cout << 32410;
    for (int i = 0; i < 32410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32411(int& val) {
    func_in_32411(val);
    const int x = 32411;
    std::cout << 32411;
    for (int i = 0; i < 32411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32411(int& val) {
    const int x = 32411;
    std::cout << 32411;
    for (int i = 0; i < 32411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32412(int& val) {
    func_in_32412(val);
    const int x = 32412;
    std::cout << 32412;
    for (int i = 0; i < 32412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32412(int& val) {
    const int x = 32412;
    std::cout << 32412;
    for (int i = 0; i < 32412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32413(int& val) {
    func_in_32413(val);
    const int x = 32413;
    std::cout << 32413;
    for (int i = 0; i < 32413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32413(int& val) {
    const int x = 32413;
    std::cout << 32413;
    for (int i = 0; i < 32413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32414(int& val) {
    func_in_32414(val);
    const int x = 32414;
    std::cout << 32414;
    for (int i = 0; i < 32414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32414(int& val) {
    const int x = 32414;
    std::cout << 32414;
    for (int i = 0; i < 32414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32415(int& val) {
    func_in_32415(val);
    const int x = 32415;
    std::cout << 32415;
    for (int i = 0; i < 32415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32415(int& val) {
    const int x = 32415;
    std::cout << 32415;
    for (int i = 0; i < 32415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32416(int& val) {
    func_in_32416(val);
    const int x = 32416;
    std::cout << 32416;
    for (int i = 0; i < 32416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32416(int& val) {
    const int x = 32416;
    std::cout << 32416;
    for (int i = 0; i < 32416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32417(int& val) {
    func_in_32417(val);
    const int x = 32417;
    std::cout << 32417;
    for (int i = 0; i < 32417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32417(int& val) {
    const int x = 32417;
    std::cout << 32417;
    for (int i = 0; i < 32417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32418(int& val) {
    func_in_32418(val);
    const int x = 32418;
    std::cout << 32418;
    for (int i = 0; i < 32418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32418(int& val) {
    const int x = 32418;
    std::cout << 32418;
    for (int i = 0; i < 32418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32419(int& val) {
    func_in_32419(val);
    const int x = 32419;
    std::cout << 32419;
    for (int i = 0; i < 32419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32419(int& val) {
    const int x = 32419;
    std::cout << 32419;
    for (int i = 0; i < 32419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32420(int& val) {
    func_in_32420(val);
    const int x = 32420;
    std::cout << 32420;
    for (int i = 0; i < 32420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32420(int& val) {
    const int x = 32420;
    std::cout << 32420;
    for (int i = 0; i < 32420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32421(int& val) {
    func_in_32421(val);
    const int x = 32421;
    std::cout << 32421;
    for (int i = 0; i < 32421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32421(int& val) {
    const int x = 32421;
    std::cout << 32421;
    for (int i = 0; i < 32421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32422(int& val) {
    func_in_32422(val);
    const int x = 32422;
    std::cout << 32422;
    for (int i = 0; i < 32422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32422(int& val) {
    const int x = 32422;
    std::cout << 32422;
    for (int i = 0; i < 32422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32423(int& val) {
    func_in_32423(val);
    const int x = 32423;
    std::cout << 32423;
    for (int i = 0; i < 32423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32423(int& val) {
    const int x = 32423;
    std::cout << 32423;
    for (int i = 0; i < 32423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32424(int& val) {
    func_in_32424(val);
    const int x = 32424;
    std::cout << 32424;
    for (int i = 0; i < 32424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32424(int& val) {
    const int x = 32424;
    std::cout << 32424;
    for (int i = 0; i < 32424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32425(int& val) {
    func_in_32425(val);
    const int x = 32425;
    std::cout << 32425;
    for (int i = 0; i < 32425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32425(int& val) {
    const int x = 32425;
    std::cout << 32425;
    for (int i = 0; i < 32425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32426(int& val) {
    func_in_32426(val);
    const int x = 32426;
    std::cout << 32426;
    for (int i = 0; i < 32426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32426(int& val) {
    const int x = 32426;
    std::cout << 32426;
    for (int i = 0; i < 32426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32427(int& val) {
    func_in_32427(val);
    const int x = 32427;
    std::cout << 32427;
    for (int i = 0; i < 32427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32427(int& val) {
    const int x = 32427;
    std::cout << 32427;
    for (int i = 0; i < 32427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32428(int& val) {
    func_in_32428(val);
    const int x = 32428;
    std::cout << 32428;
    for (int i = 0; i < 32428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32428(int& val) {
    const int x = 32428;
    std::cout << 32428;
    for (int i = 0; i < 32428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32429(int& val) {
    func_in_32429(val);
    const int x = 32429;
    std::cout << 32429;
    for (int i = 0; i < 32429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32429(int& val) {
    const int x = 32429;
    std::cout << 32429;
    for (int i = 0; i < 32429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32430(int& val) {
    func_in_32430(val);
    const int x = 32430;
    std::cout << 32430;
    for (int i = 0; i < 32430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32430(int& val) {
    const int x = 32430;
    std::cout << 32430;
    for (int i = 0; i < 32430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32431(int& val) {
    func_in_32431(val);
    const int x = 32431;
    std::cout << 32431;
    for (int i = 0; i < 32431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32431(int& val) {
    const int x = 32431;
    std::cout << 32431;
    for (int i = 0; i < 32431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32432(int& val) {
    func_in_32432(val);
    const int x = 32432;
    std::cout << 32432;
    for (int i = 0; i < 32432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32432(int& val) {
    const int x = 32432;
    std::cout << 32432;
    for (int i = 0; i < 32432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32433(int& val) {
    func_in_32433(val);
    const int x = 32433;
    std::cout << 32433;
    for (int i = 0; i < 32433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32433(int& val) {
    const int x = 32433;
    std::cout << 32433;
    for (int i = 0; i < 32433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32434(int& val) {
    func_in_32434(val);
    const int x = 32434;
    std::cout << 32434;
    for (int i = 0; i < 32434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32434(int& val) {
    const int x = 32434;
    std::cout << 32434;
    for (int i = 0; i < 32434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32435(int& val) {
    func_in_32435(val);
    const int x = 32435;
    std::cout << 32435;
    for (int i = 0; i < 32435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32435(int& val) {
    const int x = 32435;
    std::cout << 32435;
    for (int i = 0; i < 32435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32436(int& val) {
    func_in_32436(val);
    const int x = 32436;
    std::cout << 32436;
    for (int i = 0; i < 32436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32436(int& val) {
    const int x = 32436;
    std::cout << 32436;
    for (int i = 0; i < 32436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32437(int& val) {
    func_in_32437(val);
    const int x = 32437;
    std::cout << 32437;
    for (int i = 0; i < 32437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32437(int& val) {
    const int x = 32437;
    std::cout << 32437;
    for (int i = 0; i < 32437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32438(int& val) {
    func_in_32438(val);
    const int x = 32438;
    std::cout << 32438;
    for (int i = 0; i < 32438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32438(int& val) {
    const int x = 32438;
    std::cout << 32438;
    for (int i = 0; i < 32438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32439(int& val) {
    func_in_32439(val);
    const int x = 32439;
    std::cout << 32439;
    for (int i = 0; i < 32439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32439(int& val) {
    const int x = 32439;
    std::cout << 32439;
    for (int i = 0; i < 32439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32440(int& val) {
    func_in_32440(val);
    const int x = 32440;
    std::cout << 32440;
    for (int i = 0; i < 32440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32440(int& val) {
    const int x = 32440;
    std::cout << 32440;
    for (int i = 0; i < 32440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32441(int& val) {
    func_in_32441(val);
    const int x = 32441;
    std::cout << 32441;
    for (int i = 0; i < 32441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32441(int& val) {
    const int x = 32441;
    std::cout << 32441;
    for (int i = 0; i < 32441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32442(int& val) {
    func_in_32442(val);
    const int x = 32442;
    std::cout << 32442;
    for (int i = 0; i < 32442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32442(int& val) {
    const int x = 32442;
    std::cout << 32442;
    for (int i = 0; i < 32442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32443(int& val) {
    func_in_32443(val);
    const int x = 32443;
    std::cout << 32443;
    for (int i = 0; i < 32443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32443(int& val) {
    const int x = 32443;
    std::cout << 32443;
    for (int i = 0; i < 32443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32444(int& val) {
    func_in_32444(val);
    const int x = 32444;
    std::cout << 32444;
    for (int i = 0; i < 32444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32444(int& val) {
    const int x = 32444;
    std::cout << 32444;
    for (int i = 0; i < 32444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32445(int& val) {
    func_in_32445(val);
    const int x = 32445;
    std::cout << 32445;
    for (int i = 0; i < 32445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32445(int& val) {
    const int x = 32445;
    std::cout << 32445;
    for (int i = 0; i < 32445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32446(int& val) {
    func_in_32446(val);
    const int x = 32446;
    std::cout << 32446;
    for (int i = 0; i < 32446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32446(int& val) {
    const int x = 32446;
    std::cout << 32446;
    for (int i = 0; i < 32446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32447(int& val) {
    func_in_32447(val);
    const int x = 32447;
    std::cout << 32447;
    for (int i = 0; i < 32447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32447(int& val) {
    const int x = 32447;
    std::cout << 32447;
    for (int i = 0; i < 32447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32448(int& val) {
    func_in_32448(val);
    const int x = 32448;
    std::cout << 32448;
    for (int i = 0; i < 32448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32448(int& val) {
    const int x = 32448;
    std::cout << 32448;
    for (int i = 0; i < 32448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32449(int& val) {
    func_in_32449(val);
    const int x = 32449;
    std::cout << 32449;
    for (int i = 0; i < 32449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32449(int& val) {
    const int x = 32449;
    std::cout << 32449;
    for (int i = 0; i < 32449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32450(int& val) {
    func_in_32450(val);
    const int x = 32450;
    std::cout << 32450;
    for (int i = 0; i < 32450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32450(int& val) {
    const int x = 32450;
    std::cout << 32450;
    for (int i = 0; i < 32450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32451(int& val) {
    func_in_32451(val);
    const int x = 32451;
    std::cout << 32451;
    for (int i = 0; i < 32451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32451(int& val) {
    const int x = 32451;
    std::cout << 32451;
    for (int i = 0; i < 32451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32452(int& val) {
    func_in_32452(val);
    const int x = 32452;
    std::cout << 32452;
    for (int i = 0; i < 32452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32452(int& val) {
    const int x = 32452;
    std::cout << 32452;
    for (int i = 0; i < 32452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32453(int& val) {
    func_in_32453(val);
    const int x = 32453;
    std::cout << 32453;
    for (int i = 0; i < 32453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32453(int& val) {
    const int x = 32453;
    std::cout << 32453;
    for (int i = 0; i < 32453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32454(int& val) {
    func_in_32454(val);
    const int x = 32454;
    std::cout << 32454;
    for (int i = 0; i < 32454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32454(int& val) {
    const int x = 32454;
    std::cout << 32454;
    for (int i = 0; i < 32454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32455(int& val) {
    func_in_32455(val);
    const int x = 32455;
    std::cout << 32455;
    for (int i = 0; i < 32455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32455(int& val) {
    const int x = 32455;
    std::cout << 32455;
    for (int i = 0; i < 32455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32456(int& val) {
    func_in_32456(val);
    const int x = 32456;
    std::cout << 32456;
    for (int i = 0; i < 32456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32456(int& val) {
    const int x = 32456;
    std::cout << 32456;
    for (int i = 0; i < 32456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32457(int& val) {
    func_in_32457(val);
    const int x = 32457;
    std::cout << 32457;
    for (int i = 0; i < 32457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32457(int& val) {
    const int x = 32457;
    std::cout << 32457;
    for (int i = 0; i < 32457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32458(int& val) {
    func_in_32458(val);
    const int x = 32458;
    std::cout << 32458;
    for (int i = 0; i < 32458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32458(int& val) {
    const int x = 32458;
    std::cout << 32458;
    for (int i = 0; i < 32458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32459(int& val) {
    func_in_32459(val);
    const int x = 32459;
    std::cout << 32459;
    for (int i = 0; i < 32459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32459(int& val) {
    const int x = 32459;
    std::cout << 32459;
    for (int i = 0; i < 32459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32460(int& val) {
    func_in_32460(val);
    const int x = 32460;
    std::cout << 32460;
    for (int i = 0; i < 32460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32460(int& val) {
    const int x = 32460;
    std::cout << 32460;
    for (int i = 0; i < 32460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32461(int& val) {
    func_in_32461(val);
    const int x = 32461;
    std::cout << 32461;
    for (int i = 0; i < 32461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32461(int& val) {
    const int x = 32461;
    std::cout << 32461;
    for (int i = 0; i < 32461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32462(int& val) {
    func_in_32462(val);
    const int x = 32462;
    std::cout << 32462;
    for (int i = 0; i < 32462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32462(int& val) {
    const int x = 32462;
    std::cout << 32462;
    for (int i = 0; i < 32462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32463(int& val) {
    func_in_32463(val);
    const int x = 32463;
    std::cout << 32463;
    for (int i = 0; i < 32463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32463(int& val) {
    const int x = 32463;
    std::cout << 32463;
    for (int i = 0; i < 32463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32464(int& val) {
    func_in_32464(val);
    const int x = 32464;
    std::cout << 32464;
    for (int i = 0; i < 32464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32464(int& val) {
    const int x = 32464;
    std::cout << 32464;
    for (int i = 0; i < 32464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32465(int& val) {
    func_in_32465(val);
    const int x = 32465;
    std::cout << 32465;
    for (int i = 0; i < 32465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32465(int& val) {
    const int x = 32465;
    std::cout << 32465;
    for (int i = 0; i < 32465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32466(int& val) {
    func_in_32466(val);
    const int x = 32466;
    std::cout << 32466;
    for (int i = 0; i < 32466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32466(int& val) {
    const int x = 32466;
    std::cout << 32466;
    for (int i = 0; i < 32466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32467(int& val) {
    func_in_32467(val);
    const int x = 32467;
    std::cout << 32467;
    for (int i = 0; i < 32467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32467(int& val) {
    const int x = 32467;
    std::cout << 32467;
    for (int i = 0; i < 32467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32468(int& val) {
    func_in_32468(val);
    const int x = 32468;
    std::cout << 32468;
    for (int i = 0; i < 32468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32468(int& val) {
    const int x = 32468;
    std::cout << 32468;
    for (int i = 0; i < 32468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32469(int& val) {
    func_in_32469(val);
    const int x = 32469;
    std::cout << 32469;
    for (int i = 0; i < 32469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32469(int& val) {
    const int x = 32469;
    std::cout << 32469;
    for (int i = 0; i < 32469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32470(int& val) {
    func_in_32470(val);
    const int x = 32470;
    std::cout << 32470;
    for (int i = 0; i < 32470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32470(int& val) {
    const int x = 32470;
    std::cout << 32470;
    for (int i = 0; i < 32470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32471(int& val) {
    func_in_32471(val);
    const int x = 32471;
    std::cout << 32471;
    for (int i = 0; i < 32471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32471(int& val) {
    const int x = 32471;
    std::cout << 32471;
    for (int i = 0; i < 32471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32472(int& val) {
    func_in_32472(val);
    const int x = 32472;
    std::cout << 32472;
    for (int i = 0; i < 32472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32472(int& val) {
    const int x = 32472;
    std::cout << 32472;
    for (int i = 0; i < 32472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32473(int& val) {
    func_in_32473(val);
    const int x = 32473;
    std::cout << 32473;
    for (int i = 0; i < 32473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32473(int& val) {
    const int x = 32473;
    std::cout << 32473;
    for (int i = 0; i < 32473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32474(int& val) {
    func_in_32474(val);
    const int x = 32474;
    std::cout << 32474;
    for (int i = 0; i < 32474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32474(int& val) {
    const int x = 32474;
    std::cout << 32474;
    for (int i = 0; i < 32474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32475(int& val) {
    func_in_32475(val);
    const int x = 32475;
    std::cout << 32475;
    for (int i = 0; i < 32475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32475(int& val) {
    const int x = 32475;
    std::cout << 32475;
    for (int i = 0; i < 32475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32476(int& val) {
    func_in_32476(val);
    const int x = 32476;
    std::cout << 32476;
    for (int i = 0; i < 32476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32476(int& val) {
    const int x = 32476;
    std::cout << 32476;
    for (int i = 0; i < 32476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32477(int& val) {
    func_in_32477(val);
    const int x = 32477;
    std::cout << 32477;
    for (int i = 0; i < 32477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32477(int& val) {
    const int x = 32477;
    std::cout << 32477;
    for (int i = 0; i < 32477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32478(int& val) {
    func_in_32478(val);
    const int x = 32478;
    std::cout << 32478;
    for (int i = 0; i < 32478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32478(int& val) {
    const int x = 32478;
    std::cout << 32478;
    for (int i = 0; i < 32478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32479(int& val) {
    func_in_32479(val);
    const int x = 32479;
    std::cout << 32479;
    for (int i = 0; i < 32479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32479(int& val) {
    const int x = 32479;
    std::cout << 32479;
    for (int i = 0; i < 32479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32480(int& val) {
    func_in_32480(val);
    const int x = 32480;
    std::cout << 32480;
    for (int i = 0; i < 32480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32480(int& val) {
    const int x = 32480;
    std::cout << 32480;
    for (int i = 0; i < 32480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32481(int& val) {
    func_in_32481(val);
    const int x = 32481;
    std::cout << 32481;
    for (int i = 0; i < 32481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32481(int& val) {
    const int x = 32481;
    std::cout << 32481;
    for (int i = 0; i < 32481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32482(int& val) {
    func_in_32482(val);
    const int x = 32482;
    std::cout << 32482;
    for (int i = 0; i < 32482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32482(int& val) {
    const int x = 32482;
    std::cout << 32482;
    for (int i = 0; i < 32482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32483(int& val) {
    func_in_32483(val);
    const int x = 32483;
    std::cout << 32483;
    for (int i = 0; i < 32483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32483(int& val) {
    const int x = 32483;
    std::cout << 32483;
    for (int i = 0; i < 32483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32484(int& val) {
    func_in_32484(val);
    const int x = 32484;
    std::cout << 32484;
    for (int i = 0; i < 32484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32484(int& val) {
    const int x = 32484;
    std::cout << 32484;
    for (int i = 0; i < 32484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32485(int& val) {
    func_in_32485(val);
    const int x = 32485;
    std::cout << 32485;
    for (int i = 0; i < 32485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32485(int& val) {
    const int x = 32485;
    std::cout << 32485;
    for (int i = 0; i < 32485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32486(int& val) {
    func_in_32486(val);
    const int x = 32486;
    std::cout << 32486;
    for (int i = 0; i < 32486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32486(int& val) {
    const int x = 32486;
    std::cout << 32486;
    for (int i = 0; i < 32486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32487(int& val) {
    func_in_32487(val);
    const int x = 32487;
    std::cout << 32487;
    for (int i = 0; i < 32487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32487(int& val) {
    const int x = 32487;
    std::cout << 32487;
    for (int i = 0; i < 32487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32488(int& val) {
    func_in_32488(val);
    const int x = 32488;
    std::cout << 32488;
    for (int i = 0; i < 32488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32488(int& val) {
    const int x = 32488;
    std::cout << 32488;
    for (int i = 0; i < 32488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32489(int& val) {
    func_in_32489(val);
    const int x = 32489;
    std::cout << 32489;
    for (int i = 0; i < 32489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32489(int& val) {
    const int x = 32489;
    std::cout << 32489;
    for (int i = 0; i < 32489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32490(int& val) {
    func_in_32490(val);
    const int x = 32490;
    std::cout << 32490;
    for (int i = 0; i < 32490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32490(int& val) {
    const int x = 32490;
    std::cout << 32490;
    for (int i = 0; i < 32490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32491(int& val) {
    func_in_32491(val);
    const int x = 32491;
    std::cout << 32491;
    for (int i = 0; i < 32491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32491(int& val) {
    const int x = 32491;
    std::cout << 32491;
    for (int i = 0; i < 32491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32492(int& val) {
    func_in_32492(val);
    const int x = 32492;
    std::cout << 32492;
    for (int i = 0; i < 32492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32492(int& val) {
    const int x = 32492;
    std::cout << 32492;
    for (int i = 0; i < 32492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32493(int& val) {
    func_in_32493(val);
    const int x = 32493;
    std::cout << 32493;
    for (int i = 0; i < 32493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32493(int& val) {
    const int x = 32493;
    std::cout << 32493;
    for (int i = 0; i < 32493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32494(int& val) {
    func_in_32494(val);
    const int x = 32494;
    std::cout << 32494;
    for (int i = 0; i < 32494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32494(int& val) {
    const int x = 32494;
    std::cout << 32494;
    for (int i = 0; i < 32494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32495(int& val) {
    func_in_32495(val);
    const int x = 32495;
    std::cout << 32495;
    for (int i = 0; i < 32495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32495(int& val) {
    const int x = 32495;
    std::cout << 32495;
    for (int i = 0; i < 32495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32496(int& val) {
    func_in_32496(val);
    const int x = 32496;
    std::cout << 32496;
    for (int i = 0; i < 32496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32496(int& val) {
    const int x = 32496;
    std::cout << 32496;
    for (int i = 0; i < 32496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32497(int& val) {
    func_in_32497(val);
    const int x = 32497;
    std::cout << 32497;
    for (int i = 0; i < 32497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32497(int& val) {
    const int x = 32497;
    std::cout << 32497;
    for (int i = 0; i < 32497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32498(int& val) {
    func_in_32498(val);
    const int x = 32498;
    std::cout << 32498;
    for (int i = 0; i < 32498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32498(int& val) {
    const int x = 32498;
    std::cout << 32498;
    for (int i = 0; i < 32498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32499(int& val) {
    func_in_32499(val);
    const int x = 32499;
    std::cout << 32499;
    for (int i = 0; i < 32499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32499(int& val) {
    const int x = 32499;
    std::cout << 32499;
    for (int i = 0; i < 32499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32500(int& val) {
    func_in_32500(val);
    const int x = 32500;
    std::cout << 32500;
    for (int i = 0; i < 32500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32500(int& val) {
    const int x = 32500;
    std::cout << 32500;
    for (int i = 0; i < 32500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32501(int& val) {
    func_in_32501(val);
    const int x = 32501;
    std::cout << 32501;
    for (int i = 0; i < 32501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32501(int& val) {
    const int x = 32501;
    std::cout << 32501;
    for (int i = 0; i < 32501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32502(int& val) {
    func_in_32502(val);
    const int x = 32502;
    std::cout << 32502;
    for (int i = 0; i < 32502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32502(int& val) {
    const int x = 32502;
    std::cout << 32502;
    for (int i = 0; i < 32502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32503(int& val) {
    func_in_32503(val);
    const int x = 32503;
    std::cout << 32503;
    for (int i = 0; i < 32503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32503(int& val) {
    const int x = 32503;
    std::cout << 32503;
    for (int i = 0; i < 32503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32504(int& val) {
    func_in_32504(val);
    const int x = 32504;
    std::cout << 32504;
    for (int i = 0; i < 32504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32504(int& val) {
    const int x = 32504;
    std::cout << 32504;
    for (int i = 0; i < 32504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32505(int& val) {
    func_in_32505(val);
    const int x = 32505;
    std::cout << 32505;
    for (int i = 0; i < 32505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32505(int& val) {
    const int x = 32505;
    std::cout << 32505;
    for (int i = 0; i < 32505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32506(int& val) {
    func_in_32506(val);
    const int x = 32506;
    std::cout << 32506;
    for (int i = 0; i < 32506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32506(int& val) {
    const int x = 32506;
    std::cout << 32506;
    for (int i = 0; i < 32506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32507(int& val) {
    func_in_32507(val);
    const int x = 32507;
    std::cout << 32507;
    for (int i = 0; i < 32507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32507(int& val) {
    const int x = 32507;
    std::cout << 32507;
    for (int i = 0; i < 32507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32508(int& val) {
    func_in_32508(val);
    const int x = 32508;
    std::cout << 32508;
    for (int i = 0; i < 32508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32508(int& val) {
    const int x = 32508;
    std::cout << 32508;
    for (int i = 0; i < 32508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32509(int& val) {
    func_in_32509(val);
    const int x = 32509;
    std::cout << 32509;
    for (int i = 0; i < 32509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32509(int& val) {
    const int x = 32509;
    std::cout << 32509;
    for (int i = 0; i < 32509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32510(int& val) {
    func_in_32510(val);
    const int x = 32510;
    std::cout << 32510;
    for (int i = 0; i < 32510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32510(int& val) {
    const int x = 32510;
    std::cout << 32510;
    for (int i = 0; i < 32510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32511(int& val) {
    func_in_32511(val);
    const int x = 32511;
    std::cout << 32511;
    for (int i = 0; i < 32511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32511(int& val) {
    const int x = 32511;
    std::cout << 32511;
    for (int i = 0; i < 32511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32512(int& val) {
    func_in_32512(val);
    const int x = 32512;
    std::cout << 32512;
    for (int i = 0; i < 32512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32512(int& val) {
    const int x = 32512;
    std::cout << 32512;
    for (int i = 0; i < 32512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32513(int& val) {
    func_in_32513(val);
    const int x = 32513;
    std::cout << 32513;
    for (int i = 0; i < 32513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32513(int& val) {
    const int x = 32513;
    std::cout << 32513;
    for (int i = 0; i < 32513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32514(int& val) {
    func_in_32514(val);
    const int x = 32514;
    std::cout << 32514;
    for (int i = 0; i < 32514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32514(int& val) {
    const int x = 32514;
    std::cout << 32514;
    for (int i = 0; i < 32514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32515(int& val) {
    func_in_32515(val);
    const int x = 32515;
    std::cout << 32515;
    for (int i = 0; i < 32515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32515(int& val) {
    const int x = 32515;
    std::cout << 32515;
    for (int i = 0; i < 32515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32516(int& val) {
    func_in_32516(val);
    const int x = 32516;
    std::cout << 32516;
    for (int i = 0; i < 32516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32516(int& val) {
    const int x = 32516;
    std::cout << 32516;
    for (int i = 0; i < 32516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32517(int& val) {
    func_in_32517(val);
    const int x = 32517;
    std::cout << 32517;
    for (int i = 0; i < 32517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32517(int& val) {
    const int x = 32517;
    std::cout << 32517;
    for (int i = 0; i < 32517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32518(int& val) {
    func_in_32518(val);
    const int x = 32518;
    std::cout << 32518;
    for (int i = 0; i < 32518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32518(int& val) {
    const int x = 32518;
    std::cout << 32518;
    for (int i = 0; i < 32518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32519(int& val) {
    func_in_32519(val);
    const int x = 32519;
    std::cout << 32519;
    for (int i = 0; i < 32519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32519(int& val) {
    const int x = 32519;
    std::cout << 32519;
    for (int i = 0; i < 32519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32520(int& val) {
    func_in_32520(val);
    const int x = 32520;
    std::cout << 32520;
    for (int i = 0; i < 32520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32520(int& val) {
    const int x = 32520;
    std::cout << 32520;
    for (int i = 0; i < 32520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32521(int& val) {
    func_in_32521(val);
    const int x = 32521;
    std::cout << 32521;
    for (int i = 0; i < 32521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32521(int& val) {
    const int x = 32521;
    std::cout << 32521;
    for (int i = 0; i < 32521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32522(int& val) {
    func_in_32522(val);
    const int x = 32522;
    std::cout << 32522;
    for (int i = 0; i < 32522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32522(int& val) {
    const int x = 32522;
    std::cout << 32522;
    for (int i = 0; i < 32522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32523(int& val) {
    func_in_32523(val);
    const int x = 32523;
    std::cout << 32523;
    for (int i = 0; i < 32523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32523(int& val) {
    const int x = 32523;
    std::cout << 32523;
    for (int i = 0; i < 32523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32524(int& val) {
    func_in_32524(val);
    const int x = 32524;
    std::cout << 32524;
    for (int i = 0; i < 32524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32524(int& val) {
    const int x = 32524;
    std::cout << 32524;
    for (int i = 0; i < 32524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32525(int& val) {
    func_in_32525(val);
    const int x = 32525;
    std::cout << 32525;
    for (int i = 0; i < 32525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32525(int& val) {
    const int x = 32525;
    std::cout << 32525;
    for (int i = 0; i < 32525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32526(int& val) {
    func_in_32526(val);
    const int x = 32526;
    std::cout << 32526;
    for (int i = 0; i < 32526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32526(int& val) {
    const int x = 32526;
    std::cout << 32526;
    for (int i = 0; i < 32526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32527(int& val) {
    func_in_32527(val);
    const int x = 32527;
    std::cout << 32527;
    for (int i = 0; i < 32527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32527(int& val) {
    const int x = 32527;
    std::cout << 32527;
    for (int i = 0; i < 32527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32528(int& val) {
    func_in_32528(val);
    const int x = 32528;
    std::cout << 32528;
    for (int i = 0; i < 32528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32528(int& val) {
    const int x = 32528;
    std::cout << 32528;
    for (int i = 0; i < 32528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32529(int& val) {
    func_in_32529(val);
    const int x = 32529;
    std::cout << 32529;
    for (int i = 0; i < 32529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32529(int& val) {
    const int x = 32529;
    std::cout << 32529;
    for (int i = 0; i < 32529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32530(int& val) {
    func_in_32530(val);
    const int x = 32530;
    std::cout << 32530;
    for (int i = 0; i < 32530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32530(int& val) {
    const int x = 32530;
    std::cout << 32530;
    for (int i = 0; i < 32530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32531(int& val) {
    func_in_32531(val);
    const int x = 32531;
    std::cout << 32531;
    for (int i = 0; i < 32531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32531(int& val) {
    const int x = 32531;
    std::cout << 32531;
    for (int i = 0; i < 32531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32532(int& val) {
    func_in_32532(val);
    const int x = 32532;
    std::cout << 32532;
    for (int i = 0; i < 32532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32532(int& val) {
    const int x = 32532;
    std::cout << 32532;
    for (int i = 0; i < 32532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32533(int& val) {
    func_in_32533(val);
    const int x = 32533;
    std::cout << 32533;
    for (int i = 0; i < 32533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32533(int& val) {
    const int x = 32533;
    std::cout << 32533;
    for (int i = 0; i < 32533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32534(int& val) {
    func_in_32534(val);
    const int x = 32534;
    std::cout << 32534;
    for (int i = 0; i < 32534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32534(int& val) {
    const int x = 32534;
    std::cout << 32534;
    for (int i = 0; i < 32534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32535(int& val) {
    func_in_32535(val);
    const int x = 32535;
    std::cout << 32535;
    for (int i = 0; i < 32535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32535(int& val) {
    const int x = 32535;
    std::cout << 32535;
    for (int i = 0; i < 32535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32536(int& val) {
    func_in_32536(val);
    const int x = 32536;
    std::cout << 32536;
    for (int i = 0; i < 32536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32536(int& val) {
    const int x = 32536;
    std::cout << 32536;
    for (int i = 0; i < 32536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32537(int& val) {
    func_in_32537(val);
    const int x = 32537;
    std::cout << 32537;
    for (int i = 0; i < 32537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32537(int& val) {
    const int x = 32537;
    std::cout << 32537;
    for (int i = 0; i < 32537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32538(int& val) {
    func_in_32538(val);
    const int x = 32538;
    std::cout << 32538;
    for (int i = 0; i < 32538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32538(int& val) {
    const int x = 32538;
    std::cout << 32538;
    for (int i = 0; i < 32538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32539(int& val) {
    func_in_32539(val);
    const int x = 32539;
    std::cout << 32539;
    for (int i = 0; i < 32539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32539(int& val) {
    const int x = 32539;
    std::cout << 32539;
    for (int i = 0; i < 32539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32540(int& val) {
    func_in_32540(val);
    const int x = 32540;
    std::cout << 32540;
    for (int i = 0; i < 32540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32540(int& val) {
    const int x = 32540;
    std::cout << 32540;
    for (int i = 0; i < 32540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32541(int& val) {
    func_in_32541(val);
    const int x = 32541;
    std::cout << 32541;
    for (int i = 0; i < 32541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32541(int& val) {
    const int x = 32541;
    std::cout << 32541;
    for (int i = 0; i < 32541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32542(int& val) {
    func_in_32542(val);
    const int x = 32542;
    std::cout << 32542;
    for (int i = 0; i < 32542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32542(int& val) {
    const int x = 32542;
    std::cout << 32542;
    for (int i = 0; i < 32542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32543(int& val) {
    func_in_32543(val);
    const int x = 32543;
    std::cout << 32543;
    for (int i = 0; i < 32543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32543(int& val) {
    const int x = 32543;
    std::cout << 32543;
    for (int i = 0; i < 32543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32544(int& val) {
    func_in_32544(val);
    const int x = 32544;
    std::cout << 32544;
    for (int i = 0; i < 32544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32544(int& val) {
    const int x = 32544;
    std::cout << 32544;
    for (int i = 0; i < 32544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32545(int& val) {
    func_in_32545(val);
    const int x = 32545;
    std::cout << 32545;
    for (int i = 0; i < 32545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32545(int& val) {
    const int x = 32545;
    std::cout << 32545;
    for (int i = 0; i < 32545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32546(int& val) {
    func_in_32546(val);
    const int x = 32546;
    std::cout << 32546;
    for (int i = 0; i < 32546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32546(int& val) {
    const int x = 32546;
    std::cout << 32546;
    for (int i = 0; i < 32546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32547(int& val) {
    func_in_32547(val);
    const int x = 32547;
    std::cout << 32547;
    for (int i = 0; i < 32547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32547(int& val) {
    const int x = 32547;
    std::cout << 32547;
    for (int i = 0; i < 32547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32548(int& val) {
    func_in_32548(val);
    const int x = 32548;
    std::cout << 32548;
    for (int i = 0; i < 32548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32548(int& val) {
    const int x = 32548;
    std::cout << 32548;
    for (int i = 0; i < 32548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32549(int& val) {
    func_in_32549(val);
    const int x = 32549;
    std::cout << 32549;
    for (int i = 0; i < 32549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32549(int& val) {
    const int x = 32549;
    std::cout << 32549;
    for (int i = 0; i < 32549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32550(int& val) {
    func_in_32550(val);
    const int x = 32550;
    std::cout << 32550;
    for (int i = 0; i < 32550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32550(int& val) {
    const int x = 32550;
    std::cout << 32550;
    for (int i = 0; i < 32550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32551(int& val) {
    func_in_32551(val);
    const int x = 32551;
    std::cout << 32551;
    for (int i = 0; i < 32551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32551(int& val) {
    const int x = 32551;
    std::cout << 32551;
    for (int i = 0; i < 32551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32552(int& val) {
    func_in_32552(val);
    const int x = 32552;
    std::cout << 32552;
    for (int i = 0; i < 32552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32552(int& val) {
    const int x = 32552;
    std::cout << 32552;
    for (int i = 0; i < 32552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32553(int& val) {
    func_in_32553(val);
    const int x = 32553;
    std::cout << 32553;
    for (int i = 0; i < 32553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32553(int& val) {
    const int x = 32553;
    std::cout << 32553;
    for (int i = 0; i < 32553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32554(int& val) {
    func_in_32554(val);
    const int x = 32554;
    std::cout << 32554;
    for (int i = 0; i < 32554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32554(int& val) {
    const int x = 32554;
    std::cout << 32554;
    for (int i = 0; i < 32554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32555(int& val) {
    func_in_32555(val);
    const int x = 32555;
    std::cout << 32555;
    for (int i = 0; i < 32555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32555(int& val) {
    const int x = 32555;
    std::cout << 32555;
    for (int i = 0; i < 32555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32556(int& val) {
    func_in_32556(val);
    const int x = 32556;
    std::cout << 32556;
    for (int i = 0; i < 32556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32556(int& val) {
    const int x = 32556;
    std::cout << 32556;
    for (int i = 0; i < 32556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32557(int& val) {
    func_in_32557(val);
    const int x = 32557;
    std::cout << 32557;
    for (int i = 0; i < 32557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32557(int& val) {
    const int x = 32557;
    std::cout << 32557;
    for (int i = 0; i < 32557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32558(int& val) {
    func_in_32558(val);
    const int x = 32558;
    std::cout << 32558;
    for (int i = 0; i < 32558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32558(int& val) {
    const int x = 32558;
    std::cout << 32558;
    for (int i = 0; i < 32558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32559(int& val) {
    func_in_32559(val);
    const int x = 32559;
    std::cout << 32559;
    for (int i = 0; i < 32559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32559(int& val) {
    const int x = 32559;
    std::cout << 32559;
    for (int i = 0; i < 32559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32560(int& val) {
    func_in_32560(val);
    const int x = 32560;
    std::cout << 32560;
    for (int i = 0; i < 32560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32560(int& val) {
    const int x = 32560;
    std::cout << 32560;
    for (int i = 0; i < 32560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32561(int& val) {
    func_in_32561(val);
    const int x = 32561;
    std::cout << 32561;
    for (int i = 0; i < 32561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32561(int& val) {
    const int x = 32561;
    std::cout << 32561;
    for (int i = 0; i < 32561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32562(int& val) {
    func_in_32562(val);
    const int x = 32562;
    std::cout << 32562;
    for (int i = 0; i < 32562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32562(int& val) {
    const int x = 32562;
    std::cout << 32562;
    for (int i = 0; i < 32562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32563(int& val) {
    func_in_32563(val);
    const int x = 32563;
    std::cout << 32563;
    for (int i = 0; i < 32563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32563(int& val) {
    const int x = 32563;
    std::cout << 32563;
    for (int i = 0; i < 32563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32564(int& val) {
    func_in_32564(val);
    const int x = 32564;
    std::cout << 32564;
    for (int i = 0; i < 32564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32564(int& val) {
    const int x = 32564;
    std::cout << 32564;
    for (int i = 0; i < 32564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32565(int& val) {
    func_in_32565(val);
    const int x = 32565;
    std::cout << 32565;
    for (int i = 0; i < 32565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32565(int& val) {
    const int x = 32565;
    std::cout << 32565;
    for (int i = 0; i < 32565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32566(int& val) {
    func_in_32566(val);
    const int x = 32566;
    std::cout << 32566;
    for (int i = 0; i < 32566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32566(int& val) {
    const int x = 32566;
    std::cout << 32566;
    for (int i = 0; i < 32566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32567(int& val) {
    func_in_32567(val);
    const int x = 32567;
    std::cout << 32567;
    for (int i = 0; i < 32567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32567(int& val) {
    const int x = 32567;
    std::cout << 32567;
    for (int i = 0; i < 32567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32568(int& val) {
    func_in_32568(val);
    const int x = 32568;
    std::cout << 32568;
    for (int i = 0; i < 32568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32568(int& val) {
    const int x = 32568;
    std::cout << 32568;
    for (int i = 0; i < 32568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32569(int& val) {
    func_in_32569(val);
    const int x = 32569;
    std::cout << 32569;
    for (int i = 0; i < 32569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32569(int& val) {
    const int x = 32569;
    std::cout << 32569;
    for (int i = 0; i < 32569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32570(int& val) {
    func_in_32570(val);
    const int x = 32570;
    std::cout << 32570;
    for (int i = 0; i < 32570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32570(int& val) {
    const int x = 32570;
    std::cout << 32570;
    for (int i = 0; i < 32570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32571(int& val) {
    func_in_32571(val);
    const int x = 32571;
    std::cout << 32571;
    for (int i = 0; i < 32571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32571(int& val) {
    const int x = 32571;
    std::cout << 32571;
    for (int i = 0; i < 32571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32572(int& val) {
    func_in_32572(val);
    const int x = 32572;
    std::cout << 32572;
    for (int i = 0; i < 32572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32572(int& val) {
    const int x = 32572;
    std::cout << 32572;
    for (int i = 0; i < 32572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32573(int& val) {
    func_in_32573(val);
    const int x = 32573;
    std::cout << 32573;
    for (int i = 0; i < 32573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32573(int& val) {
    const int x = 32573;
    std::cout << 32573;
    for (int i = 0; i < 32573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32574(int& val) {
    func_in_32574(val);
    const int x = 32574;
    std::cout << 32574;
    for (int i = 0; i < 32574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32574(int& val) {
    const int x = 32574;
    std::cout << 32574;
    for (int i = 0; i < 32574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32575(int& val) {
    func_in_32575(val);
    const int x = 32575;
    std::cout << 32575;
    for (int i = 0; i < 32575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32575(int& val) {
    const int x = 32575;
    std::cout << 32575;
    for (int i = 0; i < 32575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32576(int& val) {
    func_in_32576(val);
    const int x = 32576;
    std::cout << 32576;
    for (int i = 0; i < 32576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32576(int& val) {
    const int x = 32576;
    std::cout << 32576;
    for (int i = 0; i < 32576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32577(int& val) {
    func_in_32577(val);
    const int x = 32577;
    std::cout << 32577;
    for (int i = 0; i < 32577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32577(int& val) {
    const int x = 32577;
    std::cout << 32577;
    for (int i = 0; i < 32577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32578(int& val) {
    func_in_32578(val);
    const int x = 32578;
    std::cout << 32578;
    for (int i = 0; i < 32578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32578(int& val) {
    const int x = 32578;
    std::cout << 32578;
    for (int i = 0; i < 32578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32579(int& val) {
    func_in_32579(val);
    const int x = 32579;
    std::cout << 32579;
    for (int i = 0; i < 32579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32579(int& val) {
    const int x = 32579;
    std::cout << 32579;
    for (int i = 0; i < 32579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32580(int& val) {
    func_in_32580(val);
    const int x = 32580;
    std::cout << 32580;
    for (int i = 0; i < 32580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32580(int& val) {
    const int x = 32580;
    std::cout << 32580;
    for (int i = 0; i < 32580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32581(int& val) {
    func_in_32581(val);
    const int x = 32581;
    std::cout << 32581;
    for (int i = 0; i < 32581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32581(int& val) {
    const int x = 32581;
    std::cout << 32581;
    for (int i = 0; i < 32581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32582(int& val) {
    func_in_32582(val);
    const int x = 32582;
    std::cout << 32582;
    for (int i = 0; i < 32582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32582(int& val) {
    const int x = 32582;
    std::cout << 32582;
    for (int i = 0; i < 32582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32583(int& val) {
    func_in_32583(val);
    const int x = 32583;
    std::cout << 32583;
    for (int i = 0; i < 32583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32583(int& val) {
    const int x = 32583;
    std::cout << 32583;
    for (int i = 0; i < 32583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32584(int& val) {
    func_in_32584(val);
    const int x = 32584;
    std::cout << 32584;
    for (int i = 0; i < 32584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32584(int& val) {
    const int x = 32584;
    std::cout << 32584;
    for (int i = 0; i < 32584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32585(int& val) {
    func_in_32585(val);
    const int x = 32585;
    std::cout << 32585;
    for (int i = 0; i < 32585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32585(int& val) {
    const int x = 32585;
    std::cout << 32585;
    for (int i = 0; i < 32585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32586(int& val) {
    func_in_32586(val);
    const int x = 32586;
    std::cout << 32586;
    for (int i = 0; i < 32586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32586(int& val) {
    const int x = 32586;
    std::cout << 32586;
    for (int i = 0; i < 32586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32587(int& val) {
    func_in_32587(val);
    const int x = 32587;
    std::cout << 32587;
    for (int i = 0; i < 32587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32587(int& val) {
    const int x = 32587;
    std::cout << 32587;
    for (int i = 0; i < 32587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32588(int& val) {
    func_in_32588(val);
    const int x = 32588;
    std::cout << 32588;
    for (int i = 0; i < 32588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32588(int& val) {
    const int x = 32588;
    std::cout << 32588;
    for (int i = 0; i < 32588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32589(int& val) {
    func_in_32589(val);
    const int x = 32589;
    std::cout << 32589;
    for (int i = 0; i < 32589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32589(int& val) {
    const int x = 32589;
    std::cout << 32589;
    for (int i = 0; i < 32589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32590(int& val) {
    func_in_32590(val);
    const int x = 32590;
    std::cout << 32590;
    for (int i = 0; i < 32590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32590(int& val) {
    const int x = 32590;
    std::cout << 32590;
    for (int i = 0; i < 32590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32591(int& val) {
    func_in_32591(val);
    const int x = 32591;
    std::cout << 32591;
    for (int i = 0; i < 32591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32591(int& val) {
    const int x = 32591;
    std::cout << 32591;
    for (int i = 0; i < 32591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32592(int& val) {
    func_in_32592(val);
    const int x = 32592;
    std::cout << 32592;
    for (int i = 0; i < 32592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32592(int& val) {
    const int x = 32592;
    std::cout << 32592;
    for (int i = 0; i < 32592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32593(int& val) {
    func_in_32593(val);
    const int x = 32593;
    std::cout << 32593;
    for (int i = 0; i < 32593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32593(int& val) {
    const int x = 32593;
    std::cout << 32593;
    for (int i = 0; i < 32593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32594(int& val) {
    func_in_32594(val);
    const int x = 32594;
    std::cout << 32594;
    for (int i = 0; i < 32594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32594(int& val) {
    const int x = 32594;
    std::cout << 32594;
    for (int i = 0; i < 32594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32595(int& val) {
    func_in_32595(val);
    const int x = 32595;
    std::cout << 32595;
    for (int i = 0; i < 32595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32595(int& val) {
    const int x = 32595;
    std::cout << 32595;
    for (int i = 0; i < 32595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32596(int& val) {
    func_in_32596(val);
    const int x = 32596;
    std::cout << 32596;
    for (int i = 0; i < 32596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32596(int& val) {
    const int x = 32596;
    std::cout << 32596;
    for (int i = 0; i < 32596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32597(int& val) {
    func_in_32597(val);
    const int x = 32597;
    std::cout << 32597;
    for (int i = 0; i < 32597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32597(int& val) {
    const int x = 32597;
    std::cout << 32597;
    for (int i = 0; i < 32597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32598(int& val) {
    func_in_32598(val);
    const int x = 32598;
    std::cout << 32598;
    for (int i = 0; i < 32598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32598(int& val) {
    const int x = 32598;
    std::cout << 32598;
    for (int i = 0; i < 32598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32599(int& val) {
    func_in_32599(val);
    const int x = 32599;
    std::cout << 32599;
    for (int i = 0; i < 32599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32599(int& val) {
    const int x = 32599;
    std::cout << 32599;
    for (int i = 0; i < 32599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32600(int& val) {
    func_in_32600(val);
    const int x = 32600;
    std::cout << 32600;
    for (int i = 0; i < 32600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32600(int& val) {
    const int x = 32600;
    std::cout << 32600;
    for (int i = 0; i < 32600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32601(int& val) {
    func_in_32601(val);
    const int x = 32601;
    std::cout << 32601;
    for (int i = 0; i < 32601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32601(int& val) {
    const int x = 32601;
    std::cout << 32601;
    for (int i = 0; i < 32601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32602(int& val) {
    func_in_32602(val);
    const int x = 32602;
    std::cout << 32602;
    for (int i = 0; i < 32602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32602(int& val) {
    const int x = 32602;
    std::cout << 32602;
    for (int i = 0; i < 32602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32603(int& val) {
    func_in_32603(val);
    const int x = 32603;
    std::cout << 32603;
    for (int i = 0; i < 32603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32603(int& val) {
    const int x = 32603;
    std::cout << 32603;
    for (int i = 0; i < 32603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32604(int& val) {
    func_in_32604(val);
    const int x = 32604;
    std::cout << 32604;
    for (int i = 0; i < 32604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32604(int& val) {
    const int x = 32604;
    std::cout << 32604;
    for (int i = 0; i < 32604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32605(int& val) {
    func_in_32605(val);
    const int x = 32605;
    std::cout << 32605;
    for (int i = 0; i < 32605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32605(int& val) {
    const int x = 32605;
    std::cout << 32605;
    for (int i = 0; i < 32605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32606(int& val) {
    func_in_32606(val);
    const int x = 32606;
    std::cout << 32606;
    for (int i = 0; i < 32606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32606(int& val) {
    const int x = 32606;
    std::cout << 32606;
    for (int i = 0; i < 32606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32607(int& val) {
    func_in_32607(val);
    const int x = 32607;
    std::cout << 32607;
    for (int i = 0; i < 32607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32607(int& val) {
    const int x = 32607;
    std::cout << 32607;
    for (int i = 0; i < 32607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32608(int& val) {
    func_in_32608(val);
    const int x = 32608;
    std::cout << 32608;
    for (int i = 0; i < 32608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32608(int& val) {
    const int x = 32608;
    std::cout << 32608;
    for (int i = 0; i < 32608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32609(int& val) {
    func_in_32609(val);
    const int x = 32609;
    std::cout << 32609;
    for (int i = 0; i < 32609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32609(int& val) {
    const int x = 32609;
    std::cout << 32609;
    for (int i = 0; i < 32609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32610(int& val) {
    func_in_32610(val);
    const int x = 32610;
    std::cout << 32610;
    for (int i = 0; i < 32610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32610(int& val) {
    const int x = 32610;
    std::cout << 32610;
    for (int i = 0; i < 32610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32611(int& val) {
    func_in_32611(val);
    const int x = 32611;
    std::cout << 32611;
    for (int i = 0; i < 32611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32611(int& val) {
    const int x = 32611;
    std::cout << 32611;
    for (int i = 0; i < 32611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32612(int& val) {
    func_in_32612(val);
    const int x = 32612;
    std::cout << 32612;
    for (int i = 0; i < 32612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32612(int& val) {
    const int x = 32612;
    std::cout << 32612;
    for (int i = 0; i < 32612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32613(int& val) {
    func_in_32613(val);
    const int x = 32613;
    std::cout << 32613;
    for (int i = 0; i < 32613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32613(int& val) {
    const int x = 32613;
    std::cout << 32613;
    for (int i = 0; i < 32613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32614(int& val) {
    func_in_32614(val);
    const int x = 32614;
    std::cout << 32614;
    for (int i = 0; i < 32614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32614(int& val) {
    const int x = 32614;
    std::cout << 32614;
    for (int i = 0; i < 32614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32615(int& val) {
    func_in_32615(val);
    const int x = 32615;
    std::cout << 32615;
    for (int i = 0; i < 32615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32615(int& val) {
    const int x = 32615;
    std::cout << 32615;
    for (int i = 0; i < 32615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32616(int& val) {
    func_in_32616(val);
    const int x = 32616;
    std::cout << 32616;
    for (int i = 0; i < 32616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32616(int& val) {
    const int x = 32616;
    std::cout << 32616;
    for (int i = 0; i < 32616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32617(int& val) {
    func_in_32617(val);
    const int x = 32617;
    std::cout << 32617;
    for (int i = 0; i < 32617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32617(int& val) {
    const int x = 32617;
    std::cout << 32617;
    for (int i = 0; i < 32617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32618(int& val) {
    func_in_32618(val);
    const int x = 32618;
    std::cout << 32618;
    for (int i = 0; i < 32618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32618(int& val) {
    const int x = 32618;
    std::cout << 32618;
    for (int i = 0; i < 32618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32619(int& val) {
    func_in_32619(val);
    const int x = 32619;
    std::cout << 32619;
    for (int i = 0; i < 32619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32619(int& val) {
    const int x = 32619;
    std::cout << 32619;
    for (int i = 0; i < 32619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32620(int& val) {
    func_in_32620(val);
    const int x = 32620;
    std::cout << 32620;
    for (int i = 0; i < 32620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32620(int& val) {
    const int x = 32620;
    std::cout << 32620;
    for (int i = 0; i < 32620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32621(int& val) {
    func_in_32621(val);
    const int x = 32621;
    std::cout << 32621;
    for (int i = 0; i < 32621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32621(int& val) {
    const int x = 32621;
    std::cout << 32621;
    for (int i = 0; i < 32621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32622(int& val) {
    func_in_32622(val);
    const int x = 32622;
    std::cout << 32622;
    for (int i = 0; i < 32622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32622(int& val) {
    const int x = 32622;
    std::cout << 32622;
    for (int i = 0; i < 32622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32623(int& val) {
    func_in_32623(val);
    const int x = 32623;
    std::cout << 32623;
    for (int i = 0; i < 32623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32623(int& val) {
    const int x = 32623;
    std::cout << 32623;
    for (int i = 0; i < 32623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32624(int& val) {
    func_in_32624(val);
    const int x = 32624;
    std::cout << 32624;
    for (int i = 0; i < 32624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32624(int& val) {
    const int x = 32624;
    std::cout << 32624;
    for (int i = 0; i < 32624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32625(int& val) {
    func_in_32625(val);
    const int x = 32625;
    std::cout << 32625;
    for (int i = 0; i < 32625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32625(int& val) {
    const int x = 32625;
    std::cout << 32625;
    for (int i = 0; i < 32625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32626(int& val) {
    func_in_32626(val);
    const int x = 32626;
    std::cout << 32626;
    for (int i = 0; i < 32626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32626(int& val) {
    const int x = 32626;
    std::cout << 32626;
    for (int i = 0; i < 32626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32627(int& val) {
    func_in_32627(val);
    const int x = 32627;
    std::cout << 32627;
    for (int i = 0; i < 32627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32627(int& val) {
    const int x = 32627;
    std::cout << 32627;
    for (int i = 0; i < 32627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32628(int& val) {
    func_in_32628(val);
    const int x = 32628;
    std::cout << 32628;
    for (int i = 0; i < 32628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32628(int& val) {
    const int x = 32628;
    std::cout << 32628;
    for (int i = 0; i < 32628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32629(int& val) {
    func_in_32629(val);
    const int x = 32629;
    std::cout << 32629;
    for (int i = 0; i < 32629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32629(int& val) {
    const int x = 32629;
    std::cout << 32629;
    for (int i = 0; i < 32629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32630(int& val) {
    func_in_32630(val);
    const int x = 32630;
    std::cout << 32630;
    for (int i = 0; i < 32630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32630(int& val) {
    const int x = 32630;
    std::cout << 32630;
    for (int i = 0; i < 32630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32631(int& val) {
    func_in_32631(val);
    const int x = 32631;
    std::cout << 32631;
    for (int i = 0; i < 32631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32631(int& val) {
    const int x = 32631;
    std::cout << 32631;
    for (int i = 0; i < 32631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32632(int& val) {
    func_in_32632(val);
    const int x = 32632;
    std::cout << 32632;
    for (int i = 0; i < 32632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32632(int& val) {
    const int x = 32632;
    std::cout << 32632;
    for (int i = 0; i < 32632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32633(int& val) {
    func_in_32633(val);
    const int x = 32633;
    std::cout << 32633;
    for (int i = 0; i < 32633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32633(int& val) {
    const int x = 32633;
    std::cout << 32633;
    for (int i = 0; i < 32633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32634(int& val) {
    func_in_32634(val);
    const int x = 32634;
    std::cout << 32634;
    for (int i = 0; i < 32634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32634(int& val) {
    const int x = 32634;
    std::cout << 32634;
    for (int i = 0; i < 32634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32635(int& val) {
    func_in_32635(val);
    const int x = 32635;
    std::cout << 32635;
    for (int i = 0; i < 32635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32635(int& val) {
    const int x = 32635;
    std::cout << 32635;
    for (int i = 0; i < 32635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32636(int& val) {
    func_in_32636(val);
    const int x = 32636;
    std::cout << 32636;
    for (int i = 0; i < 32636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32636(int& val) {
    const int x = 32636;
    std::cout << 32636;
    for (int i = 0; i < 32636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32637(int& val) {
    func_in_32637(val);
    const int x = 32637;
    std::cout << 32637;
    for (int i = 0; i < 32637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32637(int& val) {
    const int x = 32637;
    std::cout << 32637;
    for (int i = 0; i < 32637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32638(int& val) {
    func_in_32638(val);
    const int x = 32638;
    std::cout << 32638;
    for (int i = 0; i < 32638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32638(int& val) {
    const int x = 32638;
    std::cout << 32638;
    for (int i = 0; i < 32638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32639(int& val) {
    func_in_32639(val);
    const int x = 32639;
    std::cout << 32639;
    for (int i = 0; i < 32639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32639(int& val) {
    const int x = 32639;
    std::cout << 32639;
    for (int i = 0; i < 32639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32640(int& val) {
    func_in_32640(val);
    const int x = 32640;
    std::cout << 32640;
    for (int i = 0; i < 32640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32640(int& val) {
    const int x = 32640;
    std::cout << 32640;
    for (int i = 0; i < 32640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32641(int& val) {
    func_in_32641(val);
    const int x = 32641;
    std::cout << 32641;
    for (int i = 0; i < 32641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32641(int& val) {
    const int x = 32641;
    std::cout << 32641;
    for (int i = 0; i < 32641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32642(int& val) {
    func_in_32642(val);
    const int x = 32642;
    std::cout << 32642;
    for (int i = 0; i < 32642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32642(int& val) {
    const int x = 32642;
    std::cout << 32642;
    for (int i = 0; i < 32642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32643(int& val) {
    func_in_32643(val);
    const int x = 32643;
    std::cout << 32643;
    for (int i = 0; i < 32643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32643(int& val) {
    const int x = 32643;
    std::cout << 32643;
    for (int i = 0; i < 32643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32644(int& val) {
    func_in_32644(val);
    const int x = 32644;
    std::cout << 32644;
    for (int i = 0; i < 32644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32644(int& val) {
    const int x = 32644;
    std::cout << 32644;
    for (int i = 0; i < 32644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32645(int& val) {
    func_in_32645(val);
    const int x = 32645;
    std::cout << 32645;
    for (int i = 0; i < 32645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32645(int& val) {
    const int x = 32645;
    std::cout << 32645;
    for (int i = 0; i < 32645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32646(int& val) {
    func_in_32646(val);
    const int x = 32646;
    std::cout << 32646;
    for (int i = 0; i < 32646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32646(int& val) {
    const int x = 32646;
    std::cout << 32646;
    for (int i = 0; i < 32646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32647(int& val) {
    func_in_32647(val);
    const int x = 32647;
    std::cout << 32647;
    for (int i = 0; i < 32647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32647(int& val) {
    const int x = 32647;
    std::cout << 32647;
    for (int i = 0; i < 32647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32648(int& val) {
    func_in_32648(val);
    const int x = 32648;
    std::cout << 32648;
    for (int i = 0; i < 32648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32648(int& val) {
    const int x = 32648;
    std::cout << 32648;
    for (int i = 0; i < 32648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32649(int& val) {
    func_in_32649(val);
    const int x = 32649;
    std::cout << 32649;
    for (int i = 0; i < 32649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32649(int& val) {
    const int x = 32649;
    std::cout << 32649;
    for (int i = 0; i < 32649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32650(int& val) {
    func_in_32650(val);
    const int x = 32650;
    std::cout << 32650;
    for (int i = 0; i < 32650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32650(int& val) {
    const int x = 32650;
    std::cout << 32650;
    for (int i = 0; i < 32650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32651(int& val) {
    func_in_32651(val);
    const int x = 32651;
    std::cout << 32651;
    for (int i = 0; i < 32651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32651(int& val) {
    const int x = 32651;
    std::cout << 32651;
    for (int i = 0; i < 32651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32652(int& val) {
    func_in_32652(val);
    const int x = 32652;
    std::cout << 32652;
    for (int i = 0; i < 32652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32652(int& val) {
    const int x = 32652;
    std::cout << 32652;
    for (int i = 0; i < 32652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32653(int& val) {
    func_in_32653(val);
    const int x = 32653;
    std::cout << 32653;
    for (int i = 0; i < 32653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32653(int& val) {
    const int x = 32653;
    std::cout << 32653;
    for (int i = 0; i < 32653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32654(int& val) {
    func_in_32654(val);
    const int x = 32654;
    std::cout << 32654;
    for (int i = 0; i < 32654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32654(int& val) {
    const int x = 32654;
    std::cout << 32654;
    for (int i = 0; i < 32654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32655(int& val) {
    func_in_32655(val);
    const int x = 32655;
    std::cout << 32655;
    for (int i = 0; i < 32655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32655(int& val) {
    const int x = 32655;
    std::cout << 32655;
    for (int i = 0; i < 32655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32656(int& val) {
    func_in_32656(val);
    const int x = 32656;
    std::cout << 32656;
    for (int i = 0; i < 32656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32656(int& val) {
    const int x = 32656;
    std::cout << 32656;
    for (int i = 0; i < 32656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32657(int& val) {
    func_in_32657(val);
    const int x = 32657;
    std::cout << 32657;
    for (int i = 0; i < 32657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32657(int& val) {
    const int x = 32657;
    std::cout << 32657;
    for (int i = 0; i < 32657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32658(int& val) {
    func_in_32658(val);
    const int x = 32658;
    std::cout << 32658;
    for (int i = 0; i < 32658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32658(int& val) {
    const int x = 32658;
    std::cout << 32658;
    for (int i = 0; i < 32658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32659(int& val) {
    func_in_32659(val);
    const int x = 32659;
    std::cout << 32659;
    for (int i = 0; i < 32659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32659(int& val) {
    const int x = 32659;
    std::cout << 32659;
    for (int i = 0; i < 32659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32660(int& val) {
    func_in_32660(val);
    const int x = 32660;
    std::cout << 32660;
    for (int i = 0; i < 32660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32660(int& val) {
    const int x = 32660;
    std::cout << 32660;
    for (int i = 0; i < 32660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32661(int& val) {
    func_in_32661(val);
    const int x = 32661;
    std::cout << 32661;
    for (int i = 0; i < 32661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32661(int& val) {
    const int x = 32661;
    std::cout << 32661;
    for (int i = 0; i < 32661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32662(int& val) {
    func_in_32662(val);
    const int x = 32662;
    std::cout << 32662;
    for (int i = 0; i < 32662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32662(int& val) {
    const int x = 32662;
    std::cout << 32662;
    for (int i = 0; i < 32662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32663(int& val) {
    func_in_32663(val);
    const int x = 32663;
    std::cout << 32663;
    for (int i = 0; i < 32663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32663(int& val) {
    const int x = 32663;
    std::cout << 32663;
    for (int i = 0; i < 32663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32664(int& val) {
    func_in_32664(val);
    const int x = 32664;
    std::cout << 32664;
    for (int i = 0; i < 32664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32664(int& val) {
    const int x = 32664;
    std::cout << 32664;
    for (int i = 0; i < 32664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32665(int& val) {
    func_in_32665(val);
    const int x = 32665;
    std::cout << 32665;
    for (int i = 0; i < 32665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32665(int& val) {
    const int x = 32665;
    std::cout << 32665;
    for (int i = 0; i < 32665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32666(int& val) {
    func_in_32666(val);
    const int x = 32666;
    std::cout << 32666;
    for (int i = 0; i < 32666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32666(int& val) {
    const int x = 32666;
    std::cout << 32666;
    for (int i = 0; i < 32666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32667(int& val) {
    func_in_32667(val);
    const int x = 32667;
    std::cout << 32667;
    for (int i = 0; i < 32667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32667(int& val) {
    const int x = 32667;
    std::cout << 32667;
    for (int i = 0; i < 32667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32668(int& val) {
    func_in_32668(val);
    const int x = 32668;
    std::cout << 32668;
    for (int i = 0; i < 32668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32668(int& val) {
    const int x = 32668;
    std::cout << 32668;
    for (int i = 0; i < 32668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32669(int& val) {
    func_in_32669(val);
    const int x = 32669;
    std::cout << 32669;
    for (int i = 0; i < 32669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32669(int& val) {
    const int x = 32669;
    std::cout << 32669;
    for (int i = 0; i < 32669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32670(int& val) {
    func_in_32670(val);
    const int x = 32670;
    std::cout << 32670;
    for (int i = 0; i < 32670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32670(int& val) {
    const int x = 32670;
    std::cout << 32670;
    for (int i = 0; i < 32670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32671(int& val) {
    func_in_32671(val);
    const int x = 32671;
    std::cout << 32671;
    for (int i = 0; i < 32671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32671(int& val) {
    const int x = 32671;
    std::cout << 32671;
    for (int i = 0; i < 32671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32672(int& val) {
    func_in_32672(val);
    const int x = 32672;
    std::cout << 32672;
    for (int i = 0; i < 32672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32672(int& val) {
    const int x = 32672;
    std::cout << 32672;
    for (int i = 0; i < 32672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32673(int& val) {
    func_in_32673(val);
    const int x = 32673;
    std::cout << 32673;
    for (int i = 0; i < 32673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32673(int& val) {
    const int x = 32673;
    std::cout << 32673;
    for (int i = 0; i < 32673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32674(int& val) {
    func_in_32674(val);
    const int x = 32674;
    std::cout << 32674;
    for (int i = 0; i < 32674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32674(int& val) {
    const int x = 32674;
    std::cout << 32674;
    for (int i = 0; i < 32674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32675(int& val) {
    func_in_32675(val);
    const int x = 32675;
    std::cout << 32675;
    for (int i = 0; i < 32675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32675(int& val) {
    const int x = 32675;
    std::cout << 32675;
    for (int i = 0; i < 32675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32676(int& val) {
    func_in_32676(val);
    const int x = 32676;
    std::cout << 32676;
    for (int i = 0; i < 32676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32676(int& val) {
    const int x = 32676;
    std::cout << 32676;
    for (int i = 0; i < 32676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32677(int& val) {
    func_in_32677(val);
    const int x = 32677;
    std::cout << 32677;
    for (int i = 0; i < 32677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32677(int& val) {
    const int x = 32677;
    std::cout << 32677;
    for (int i = 0; i < 32677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32678(int& val) {
    func_in_32678(val);
    const int x = 32678;
    std::cout << 32678;
    for (int i = 0; i < 32678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32678(int& val) {
    const int x = 32678;
    std::cout << 32678;
    for (int i = 0; i < 32678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32679(int& val) {
    func_in_32679(val);
    const int x = 32679;
    std::cout << 32679;
    for (int i = 0; i < 32679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32679(int& val) {
    const int x = 32679;
    std::cout << 32679;
    for (int i = 0; i < 32679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32680(int& val) {
    func_in_32680(val);
    const int x = 32680;
    std::cout << 32680;
    for (int i = 0; i < 32680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32680(int& val) {
    const int x = 32680;
    std::cout << 32680;
    for (int i = 0; i < 32680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32681(int& val) {
    func_in_32681(val);
    const int x = 32681;
    std::cout << 32681;
    for (int i = 0; i < 32681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32681(int& val) {
    const int x = 32681;
    std::cout << 32681;
    for (int i = 0; i < 32681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32682(int& val) {
    func_in_32682(val);
    const int x = 32682;
    std::cout << 32682;
    for (int i = 0; i < 32682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32682(int& val) {
    const int x = 32682;
    std::cout << 32682;
    for (int i = 0; i < 32682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32683(int& val) {
    func_in_32683(val);
    const int x = 32683;
    std::cout << 32683;
    for (int i = 0; i < 32683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32683(int& val) {
    const int x = 32683;
    std::cout << 32683;
    for (int i = 0; i < 32683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32684(int& val) {
    func_in_32684(val);
    const int x = 32684;
    std::cout << 32684;
    for (int i = 0; i < 32684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32684(int& val) {
    const int x = 32684;
    std::cout << 32684;
    for (int i = 0; i < 32684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32685(int& val) {
    func_in_32685(val);
    const int x = 32685;
    std::cout << 32685;
    for (int i = 0; i < 32685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32685(int& val) {
    const int x = 32685;
    std::cout << 32685;
    for (int i = 0; i < 32685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32686(int& val) {
    func_in_32686(val);
    const int x = 32686;
    std::cout << 32686;
    for (int i = 0; i < 32686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32686(int& val) {
    const int x = 32686;
    std::cout << 32686;
    for (int i = 0; i < 32686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32687(int& val) {
    func_in_32687(val);
    const int x = 32687;
    std::cout << 32687;
    for (int i = 0; i < 32687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32687(int& val) {
    const int x = 32687;
    std::cout << 32687;
    for (int i = 0; i < 32687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32688(int& val) {
    func_in_32688(val);
    const int x = 32688;
    std::cout << 32688;
    for (int i = 0; i < 32688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32688(int& val) {
    const int x = 32688;
    std::cout << 32688;
    for (int i = 0; i < 32688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32689(int& val) {
    func_in_32689(val);
    const int x = 32689;
    std::cout << 32689;
    for (int i = 0; i < 32689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32689(int& val) {
    const int x = 32689;
    std::cout << 32689;
    for (int i = 0; i < 32689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32690(int& val) {
    func_in_32690(val);
    const int x = 32690;
    std::cout << 32690;
    for (int i = 0; i < 32690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32690(int& val) {
    const int x = 32690;
    std::cout << 32690;
    for (int i = 0; i < 32690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32691(int& val) {
    func_in_32691(val);
    const int x = 32691;
    std::cout << 32691;
    for (int i = 0; i < 32691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32691(int& val) {
    const int x = 32691;
    std::cout << 32691;
    for (int i = 0; i < 32691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32692(int& val) {
    func_in_32692(val);
    const int x = 32692;
    std::cout << 32692;
    for (int i = 0; i < 32692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32692(int& val) {
    const int x = 32692;
    std::cout << 32692;
    for (int i = 0; i < 32692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32693(int& val) {
    func_in_32693(val);
    const int x = 32693;
    std::cout << 32693;
    for (int i = 0; i < 32693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32693(int& val) {
    const int x = 32693;
    std::cout << 32693;
    for (int i = 0; i < 32693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32694(int& val) {
    func_in_32694(val);
    const int x = 32694;
    std::cout << 32694;
    for (int i = 0; i < 32694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32694(int& val) {
    const int x = 32694;
    std::cout << 32694;
    for (int i = 0; i < 32694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32695(int& val) {
    func_in_32695(val);
    const int x = 32695;
    std::cout << 32695;
    for (int i = 0; i < 32695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32695(int& val) {
    const int x = 32695;
    std::cout << 32695;
    for (int i = 0; i < 32695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32696(int& val) {
    func_in_32696(val);
    const int x = 32696;
    std::cout << 32696;
    for (int i = 0; i < 32696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32696(int& val) {
    const int x = 32696;
    std::cout << 32696;
    for (int i = 0; i < 32696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32697(int& val) {
    func_in_32697(val);
    const int x = 32697;
    std::cout << 32697;
    for (int i = 0; i < 32697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32697(int& val) {
    const int x = 32697;
    std::cout << 32697;
    for (int i = 0; i < 32697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32698(int& val) {
    func_in_32698(val);
    const int x = 32698;
    std::cout << 32698;
    for (int i = 0; i < 32698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32698(int& val) {
    const int x = 32698;
    std::cout << 32698;
    for (int i = 0; i < 32698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32699(int& val) {
    func_in_32699(val);
    const int x = 32699;
    std::cout << 32699;
    for (int i = 0; i < 32699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32699(int& val) {
    const int x = 32699;
    std::cout << 32699;
    for (int i = 0; i < 32699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32700(int& val) {
    func_in_32700(val);
    const int x = 32700;
    std::cout << 32700;
    for (int i = 0; i < 32700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32700(int& val) {
    const int x = 32700;
    std::cout << 32700;
    for (int i = 0; i < 32700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32701(int& val) {
    func_in_32701(val);
    const int x = 32701;
    std::cout << 32701;
    for (int i = 0; i < 32701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32701(int& val) {
    const int x = 32701;
    std::cout << 32701;
    for (int i = 0; i < 32701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32702(int& val) {
    func_in_32702(val);
    const int x = 32702;
    std::cout << 32702;
    for (int i = 0; i < 32702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32702(int& val) {
    const int x = 32702;
    std::cout << 32702;
    for (int i = 0; i < 32702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32703(int& val) {
    func_in_32703(val);
    const int x = 32703;
    std::cout << 32703;
    for (int i = 0; i < 32703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32703(int& val) {
    const int x = 32703;
    std::cout << 32703;
    for (int i = 0; i < 32703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32704(int& val) {
    func_in_32704(val);
    const int x = 32704;
    std::cout << 32704;
    for (int i = 0; i < 32704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32704(int& val) {
    const int x = 32704;
    std::cout << 32704;
    for (int i = 0; i < 32704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32705(int& val) {
    func_in_32705(val);
    const int x = 32705;
    std::cout << 32705;
    for (int i = 0; i < 32705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32705(int& val) {
    const int x = 32705;
    std::cout << 32705;
    for (int i = 0; i < 32705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32706(int& val) {
    func_in_32706(val);
    const int x = 32706;
    std::cout << 32706;
    for (int i = 0; i < 32706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32706(int& val) {
    const int x = 32706;
    std::cout << 32706;
    for (int i = 0; i < 32706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32707(int& val) {
    func_in_32707(val);
    const int x = 32707;
    std::cout << 32707;
    for (int i = 0; i < 32707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32707(int& val) {
    const int x = 32707;
    std::cout << 32707;
    for (int i = 0; i < 32707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32708(int& val) {
    func_in_32708(val);
    const int x = 32708;
    std::cout << 32708;
    for (int i = 0; i < 32708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32708(int& val) {
    const int x = 32708;
    std::cout << 32708;
    for (int i = 0; i < 32708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32709(int& val) {
    func_in_32709(val);
    const int x = 32709;
    std::cout << 32709;
    for (int i = 0; i < 32709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32709(int& val) {
    const int x = 32709;
    std::cout << 32709;
    for (int i = 0; i < 32709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32710(int& val) {
    func_in_32710(val);
    const int x = 32710;
    std::cout << 32710;
    for (int i = 0; i < 32710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32710(int& val) {
    const int x = 32710;
    std::cout << 32710;
    for (int i = 0; i < 32710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32711(int& val) {
    func_in_32711(val);
    const int x = 32711;
    std::cout << 32711;
    for (int i = 0; i < 32711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32711(int& val) {
    const int x = 32711;
    std::cout << 32711;
    for (int i = 0; i < 32711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32712(int& val) {
    func_in_32712(val);
    const int x = 32712;
    std::cout << 32712;
    for (int i = 0; i < 32712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32712(int& val) {
    const int x = 32712;
    std::cout << 32712;
    for (int i = 0; i < 32712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32713(int& val) {
    func_in_32713(val);
    const int x = 32713;
    std::cout << 32713;
    for (int i = 0; i < 32713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32713(int& val) {
    const int x = 32713;
    std::cout << 32713;
    for (int i = 0; i < 32713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32714(int& val) {
    func_in_32714(val);
    const int x = 32714;
    std::cout << 32714;
    for (int i = 0; i < 32714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32714(int& val) {
    const int x = 32714;
    std::cout << 32714;
    for (int i = 0; i < 32714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32715(int& val) {
    func_in_32715(val);
    const int x = 32715;
    std::cout << 32715;
    for (int i = 0; i < 32715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32715(int& val) {
    const int x = 32715;
    std::cout << 32715;
    for (int i = 0; i < 32715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32716(int& val) {
    func_in_32716(val);
    const int x = 32716;
    std::cout << 32716;
    for (int i = 0; i < 32716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32716(int& val) {
    const int x = 32716;
    std::cout << 32716;
    for (int i = 0; i < 32716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32717(int& val) {
    func_in_32717(val);
    const int x = 32717;
    std::cout << 32717;
    for (int i = 0; i < 32717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32717(int& val) {
    const int x = 32717;
    std::cout << 32717;
    for (int i = 0; i < 32717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32718(int& val) {
    func_in_32718(val);
    const int x = 32718;
    std::cout << 32718;
    for (int i = 0; i < 32718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32718(int& val) {
    const int x = 32718;
    std::cout << 32718;
    for (int i = 0; i < 32718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32719(int& val) {
    func_in_32719(val);
    const int x = 32719;
    std::cout << 32719;
    for (int i = 0; i < 32719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32719(int& val) {
    const int x = 32719;
    std::cout << 32719;
    for (int i = 0; i < 32719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32720(int& val) {
    func_in_32720(val);
    const int x = 32720;
    std::cout << 32720;
    for (int i = 0; i < 32720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32720(int& val) {
    const int x = 32720;
    std::cout << 32720;
    for (int i = 0; i < 32720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32721(int& val) {
    func_in_32721(val);
    const int x = 32721;
    std::cout << 32721;
    for (int i = 0; i < 32721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32721(int& val) {
    const int x = 32721;
    std::cout << 32721;
    for (int i = 0; i < 32721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32722(int& val) {
    func_in_32722(val);
    const int x = 32722;
    std::cout << 32722;
    for (int i = 0; i < 32722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32722(int& val) {
    const int x = 32722;
    std::cout << 32722;
    for (int i = 0; i < 32722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32723(int& val) {
    func_in_32723(val);
    const int x = 32723;
    std::cout << 32723;
    for (int i = 0; i < 32723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32723(int& val) {
    const int x = 32723;
    std::cout << 32723;
    for (int i = 0; i < 32723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32724(int& val) {
    func_in_32724(val);
    const int x = 32724;
    std::cout << 32724;
    for (int i = 0; i < 32724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32724(int& val) {
    const int x = 32724;
    std::cout << 32724;
    for (int i = 0; i < 32724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32725(int& val) {
    func_in_32725(val);
    const int x = 32725;
    std::cout << 32725;
    for (int i = 0; i < 32725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32725(int& val) {
    const int x = 32725;
    std::cout << 32725;
    for (int i = 0; i < 32725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32726(int& val) {
    func_in_32726(val);
    const int x = 32726;
    std::cout << 32726;
    for (int i = 0; i < 32726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32726(int& val) {
    const int x = 32726;
    std::cout << 32726;
    for (int i = 0; i < 32726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32727(int& val) {
    func_in_32727(val);
    const int x = 32727;
    std::cout << 32727;
    for (int i = 0; i < 32727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32727(int& val) {
    const int x = 32727;
    std::cout << 32727;
    for (int i = 0; i < 32727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32728(int& val) {
    func_in_32728(val);
    const int x = 32728;
    std::cout << 32728;
    for (int i = 0; i < 32728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32728(int& val) {
    const int x = 32728;
    std::cout << 32728;
    for (int i = 0; i < 32728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32729(int& val) {
    func_in_32729(val);
    const int x = 32729;
    std::cout << 32729;
    for (int i = 0; i < 32729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32729(int& val) {
    const int x = 32729;
    std::cout << 32729;
    for (int i = 0; i < 32729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32730(int& val) {
    func_in_32730(val);
    const int x = 32730;
    std::cout << 32730;
    for (int i = 0; i < 32730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32730(int& val) {
    const int x = 32730;
    std::cout << 32730;
    for (int i = 0; i < 32730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32731(int& val) {
    func_in_32731(val);
    const int x = 32731;
    std::cout << 32731;
    for (int i = 0; i < 32731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32731(int& val) {
    const int x = 32731;
    std::cout << 32731;
    for (int i = 0; i < 32731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32732(int& val) {
    func_in_32732(val);
    const int x = 32732;
    std::cout << 32732;
    for (int i = 0; i < 32732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32732(int& val) {
    const int x = 32732;
    std::cout << 32732;
    for (int i = 0; i < 32732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32733(int& val) {
    func_in_32733(val);
    const int x = 32733;
    std::cout << 32733;
    for (int i = 0; i < 32733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32733(int& val) {
    const int x = 32733;
    std::cout << 32733;
    for (int i = 0; i < 32733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32734(int& val) {
    func_in_32734(val);
    const int x = 32734;
    std::cout << 32734;
    for (int i = 0; i < 32734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32734(int& val) {
    const int x = 32734;
    std::cout << 32734;
    for (int i = 0; i < 32734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32735(int& val) {
    func_in_32735(val);
    const int x = 32735;
    std::cout << 32735;
    for (int i = 0; i < 32735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32735(int& val) {
    const int x = 32735;
    std::cout << 32735;
    for (int i = 0; i < 32735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32736(int& val) {
    func_in_32736(val);
    const int x = 32736;
    std::cout << 32736;
    for (int i = 0; i < 32736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32736(int& val) {
    const int x = 32736;
    std::cout << 32736;
    for (int i = 0; i < 32736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32737(int& val) {
    func_in_32737(val);
    const int x = 32737;
    std::cout << 32737;
    for (int i = 0; i < 32737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32737(int& val) {
    const int x = 32737;
    std::cout << 32737;
    for (int i = 0; i < 32737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32738(int& val) {
    func_in_32738(val);
    const int x = 32738;
    std::cout << 32738;
    for (int i = 0; i < 32738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32738(int& val) {
    const int x = 32738;
    std::cout << 32738;
    for (int i = 0; i < 32738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32739(int& val) {
    func_in_32739(val);
    const int x = 32739;
    std::cout << 32739;
    for (int i = 0; i < 32739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32739(int& val) {
    const int x = 32739;
    std::cout << 32739;
    for (int i = 0; i < 32739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32740(int& val) {
    func_in_32740(val);
    const int x = 32740;
    std::cout << 32740;
    for (int i = 0; i < 32740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32740(int& val) {
    const int x = 32740;
    std::cout << 32740;
    for (int i = 0; i < 32740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32741(int& val) {
    func_in_32741(val);
    const int x = 32741;
    std::cout << 32741;
    for (int i = 0; i < 32741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32741(int& val) {
    const int x = 32741;
    std::cout << 32741;
    for (int i = 0; i < 32741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32742(int& val) {
    func_in_32742(val);
    const int x = 32742;
    std::cout << 32742;
    for (int i = 0; i < 32742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32742(int& val) {
    const int x = 32742;
    std::cout << 32742;
    for (int i = 0; i < 32742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32743(int& val) {
    func_in_32743(val);
    const int x = 32743;
    std::cout << 32743;
    for (int i = 0; i < 32743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32743(int& val) {
    const int x = 32743;
    std::cout << 32743;
    for (int i = 0; i < 32743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32744(int& val) {
    func_in_32744(val);
    const int x = 32744;
    std::cout << 32744;
    for (int i = 0; i < 32744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32744(int& val) {
    const int x = 32744;
    std::cout << 32744;
    for (int i = 0; i < 32744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32745(int& val) {
    func_in_32745(val);
    const int x = 32745;
    std::cout << 32745;
    for (int i = 0; i < 32745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32745(int& val) {
    const int x = 32745;
    std::cout << 32745;
    for (int i = 0; i < 32745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32746(int& val) {
    func_in_32746(val);
    const int x = 32746;
    std::cout << 32746;
    for (int i = 0; i < 32746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32746(int& val) {
    const int x = 32746;
    std::cout << 32746;
    for (int i = 0; i < 32746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32747(int& val) {
    func_in_32747(val);
    const int x = 32747;
    std::cout << 32747;
    for (int i = 0; i < 32747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32747(int& val) {
    const int x = 32747;
    std::cout << 32747;
    for (int i = 0; i < 32747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32748(int& val) {
    func_in_32748(val);
    const int x = 32748;
    std::cout << 32748;
    for (int i = 0; i < 32748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32748(int& val) {
    const int x = 32748;
    std::cout << 32748;
    for (int i = 0; i < 32748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32749(int& val) {
    func_in_32749(val);
    const int x = 32749;
    std::cout << 32749;
    for (int i = 0; i < 32749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32749(int& val) {
    const int x = 32749;
    std::cout << 32749;
    for (int i = 0; i < 32749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32750(int& val) {
    func_in_32750(val);
    const int x = 32750;
    std::cout << 32750;
    for (int i = 0; i < 32750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32750(int& val) {
    const int x = 32750;
    std::cout << 32750;
    for (int i = 0; i < 32750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32751(int& val) {
    func_in_32751(val);
    const int x = 32751;
    std::cout << 32751;
    for (int i = 0; i < 32751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32751(int& val) {
    const int x = 32751;
    std::cout << 32751;
    for (int i = 0; i < 32751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32752(int& val) {
    func_in_32752(val);
    const int x = 32752;
    std::cout << 32752;
    for (int i = 0; i < 32752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32752(int& val) {
    const int x = 32752;
    std::cout << 32752;
    for (int i = 0; i < 32752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32753(int& val) {
    func_in_32753(val);
    const int x = 32753;
    std::cout << 32753;
    for (int i = 0; i < 32753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32753(int& val) {
    const int x = 32753;
    std::cout << 32753;
    for (int i = 0; i < 32753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32754(int& val) {
    func_in_32754(val);
    const int x = 32754;
    std::cout << 32754;
    for (int i = 0; i < 32754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32754(int& val) {
    const int x = 32754;
    std::cout << 32754;
    for (int i = 0; i < 32754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32755(int& val) {
    func_in_32755(val);
    const int x = 32755;
    std::cout << 32755;
    for (int i = 0; i < 32755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32755(int& val) {
    const int x = 32755;
    std::cout << 32755;
    for (int i = 0; i < 32755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32756(int& val) {
    func_in_32756(val);
    const int x = 32756;
    std::cout << 32756;
    for (int i = 0; i < 32756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32756(int& val) {
    const int x = 32756;
    std::cout << 32756;
    for (int i = 0; i < 32756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32757(int& val) {
    func_in_32757(val);
    const int x = 32757;
    std::cout << 32757;
    for (int i = 0; i < 32757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32757(int& val) {
    const int x = 32757;
    std::cout << 32757;
    for (int i = 0; i < 32757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32758(int& val) {
    func_in_32758(val);
    const int x = 32758;
    std::cout << 32758;
    for (int i = 0; i < 32758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32758(int& val) {
    const int x = 32758;
    std::cout << 32758;
    for (int i = 0; i < 32758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32759(int& val) {
    func_in_32759(val);
    const int x = 32759;
    std::cout << 32759;
    for (int i = 0; i < 32759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32759(int& val) {
    const int x = 32759;
    std::cout << 32759;
    for (int i = 0; i < 32759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32760(int& val) {
    func_in_32760(val);
    const int x = 32760;
    std::cout << 32760;
    for (int i = 0; i < 32760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32760(int& val) {
    const int x = 32760;
    std::cout << 32760;
    for (int i = 0; i < 32760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32761(int& val) {
    func_in_32761(val);
    const int x = 32761;
    std::cout << 32761;
    for (int i = 0; i < 32761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32761(int& val) {
    const int x = 32761;
    std::cout << 32761;
    for (int i = 0; i < 32761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32762(int& val) {
    func_in_32762(val);
    const int x = 32762;
    std::cout << 32762;
    for (int i = 0; i < 32762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32762(int& val) {
    const int x = 32762;
    std::cout << 32762;
    for (int i = 0; i < 32762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32763(int& val) {
    func_in_32763(val);
    const int x = 32763;
    std::cout << 32763;
    for (int i = 0; i < 32763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32763(int& val) {
    const int x = 32763;
    std::cout << 32763;
    for (int i = 0; i < 32763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32764(int& val) {
    func_in_32764(val);
    const int x = 32764;
    std::cout << 32764;
    for (int i = 0; i < 32764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32764(int& val) {
    const int x = 32764;
    std::cout << 32764;
    for (int i = 0; i < 32764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32765(int& val) {
    func_in_32765(val);
    const int x = 32765;
    std::cout << 32765;
    for (int i = 0; i < 32765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32765(int& val) {
    const int x = 32765;
    std::cout << 32765;
    for (int i = 0; i < 32765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32766(int& val) {
    func_in_32766(val);
    const int x = 32766;
    std::cout << 32766;
    for (int i = 0; i < 32766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32766(int& val) {
    const int x = 32766;
    std::cout << 32766;
    for (int i = 0; i < 32766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32767(int& val) {
    func_in_32767(val);
    const int x = 32767;
    std::cout << 32767;
    for (int i = 0; i < 32767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32767(int& val) {
    const int x = 32767;
    std::cout << 32767;
    for (int i = 0; i < 32767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32768(int& val) {
    func_in_32768(val);
    const int x = 32768;
    std::cout << 32768;
    for (int i = 0; i < 32768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32768(int& val) {
    const int x = 32768;
    std::cout << 32768;
    for (int i = 0; i < 32768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32769(int& val) {
    func_in_32769(val);
    const int x = 32769;
    std::cout << 32769;
    for (int i = 0; i < 32769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32769(int& val) {
    const int x = 32769;
    std::cout << 32769;
    for (int i = 0; i < 32769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32770(int& val) {
    func_in_32770(val);
    const int x = 32770;
    std::cout << 32770;
    for (int i = 0; i < 32770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32770(int& val) {
    const int x = 32770;
    std::cout << 32770;
    for (int i = 0; i < 32770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32771(int& val) {
    func_in_32771(val);
    const int x = 32771;
    std::cout << 32771;
    for (int i = 0; i < 32771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32771(int& val) {
    const int x = 32771;
    std::cout << 32771;
    for (int i = 0; i < 32771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32772(int& val) {
    func_in_32772(val);
    const int x = 32772;
    std::cout << 32772;
    for (int i = 0; i < 32772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32772(int& val) {
    const int x = 32772;
    std::cout << 32772;
    for (int i = 0; i < 32772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32773(int& val) {
    func_in_32773(val);
    const int x = 32773;
    std::cout << 32773;
    for (int i = 0; i < 32773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32773(int& val) {
    const int x = 32773;
    std::cout << 32773;
    for (int i = 0; i < 32773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32774(int& val) {
    func_in_32774(val);
    const int x = 32774;
    std::cout << 32774;
    for (int i = 0; i < 32774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32774(int& val) {
    const int x = 32774;
    std::cout << 32774;
    for (int i = 0; i < 32774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32775(int& val) {
    func_in_32775(val);
    const int x = 32775;
    std::cout << 32775;
    for (int i = 0; i < 32775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32775(int& val) {
    const int x = 32775;
    std::cout << 32775;
    for (int i = 0; i < 32775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32776(int& val) {
    func_in_32776(val);
    const int x = 32776;
    std::cout << 32776;
    for (int i = 0; i < 32776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32776(int& val) {
    const int x = 32776;
    std::cout << 32776;
    for (int i = 0; i < 32776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32777(int& val) {
    func_in_32777(val);
    const int x = 32777;
    std::cout << 32777;
    for (int i = 0; i < 32777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32777(int& val) {
    const int x = 32777;
    std::cout << 32777;
    for (int i = 0; i < 32777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32778(int& val) {
    func_in_32778(val);
    const int x = 32778;
    std::cout << 32778;
    for (int i = 0; i < 32778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32778(int& val) {
    const int x = 32778;
    std::cout << 32778;
    for (int i = 0; i < 32778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32779(int& val) {
    func_in_32779(val);
    const int x = 32779;
    std::cout << 32779;
    for (int i = 0; i < 32779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32779(int& val) {
    const int x = 32779;
    std::cout << 32779;
    for (int i = 0; i < 32779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32780(int& val) {
    func_in_32780(val);
    const int x = 32780;
    std::cout << 32780;
    for (int i = 0; i < 32780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32780(int& val) {
    const int x = 32780;
    std::cout << 32780;
    for (int i = 0; i < 32780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32781(int& val) {
    func_in_32781(val);
    const int x = 32781;
    std::cout << 32781;
    for (int i = 0; i < 32781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32781(int& val) {
    const int x = 32781;
    std::cout << 32781;
    for (int i = 0; i < 32781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32782(int& val) {
    func_in_32782(val);
    const int x = 32782;
    std::cout << 32782;
    for (int i = 0; i < 32782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32782(int& val) {
    const int x = 32782;
    std::cout << 32782;
    for (int i = 0; i < 32782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32783(int& val) {
    func_in_32783(val);
    const int x = 32783;
    std::cout << 32783;
    for (int i = 0; i < 32783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32783(int& val) {
    const int x = 32783;
    std::cout << 32783;
    for (int i = 0; i < 32783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32784(int& val) {
    func_in_32784(val);
    const int x = 32784;
    std::cout << 32784;
    for (int i = 0; i < 32784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32784(int& val) {
    const int x = 32784;
    std::cout << 32784;
    for (int i = 0; i < 32784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32785(int& val) {
    func_in_32785(val);
    const int x = 32785;
    std::cout << 32785;
    for (int i = 0; i < 32785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32785(int& val) {
    const int x = 32785;
    std::cout << 32785;
    for (int i = 0; i < 32785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32786(int& val) {
    func_in_32786(val);
    const int x = 32786;
    std::cout << 32786;
    for (int i = 0; i < 32786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32786(int& val) {
    const int x = 32786;
    std::cout << 32786;
    for (int i = 0; i < 32786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32787(int& val) {
    func_in_32787(val);
    const int x = 32787;
    std::cout << 32787;
    for (int i = 0; i < 32787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32787(int& val) {
    const int x = 32787;
    std::cout << 32787;
    for (int i = 0; i < 32787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32788(int& val) {
    func_in_32788(val);
    const int x = 32788;
    std::cout << 32788;
    for (int i = 0; i < 32788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32788(int& val) {
    const int x = 32788;
    std::cout << 32788;
    for (int i = 0; i < 32788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32789(int& val) {
    func_in_32789(val);
    const int x = 32789;
    std::cout << 32789;
    for (int i = 0; i < 32789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32789(int& val) {
    const int x = 32789;
    std::cout << 32789;
    for (int i = 0; i < 32789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32790(int& val) {
    func_in_32790(val);
    const int x = 32790;
    std::cout << 32790;
    for (int i = 0; i < 32790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32790(int& val) {
    const int x = 32790;
    std::cout << 32790;
    for (int i = 0; i < 32790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32791(int& val) {
    func_in_32791(val);
    const int x = 32791;
    std::cout << 32791;
    for (int i = 0; i < 32791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32791(int& val) {
    const int x = 32791;
    std::cout << 32791;
    for (int i = 0; i < 32791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32792(int& val) {
    func_in_32792(val);
    const int x = 32792;
    std::cout << 32792;
    for (int i = 0; i < 32792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32792(int& val) {
    const int x = 32792;
    std::cout << 32792;
    for (int i = 0; i < 32792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32793(int& val) {
    func_in_32793(val);
    const int x = 32793;
    std::cout << 32793;
    for (int i = 0; i < 32793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32793(int& val) {
    const int x = 32793;
    std::cout << 32793;
    for (int i = 0; i < 32793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32794(int& val) {
    func_in_32794(val);
    const int x = 32794;
    std::cout << 32794;
    for (int i = 0; i < 32794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32794(int& val) {
    const int x = 32794;
    std::cout << 32794;
    for (int i = 0; i < 32794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32795(int& val) {
    func_in_32795(val);
    const int x = 32795;
    std::cout << 32795;
    for (int i = 0; i < 32795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32795(int& val) {
    const int x = 32795;
    std::cout << 32795;
    for (int i = 0; i < 32795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32796(int& val) {
    func_in_32796(val);
    const int x = 32796;
    std::cout << 32796;
    for (int i = 0; i < 32796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32796(int& val) {
    const int x = 32796;
    std::cout << 32796;
    for (int i = 0; i < 32796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32797(int& val) {
    func_in_32797(val);
    const int x = 32797;
    std::cout << 32797;
    for (int i = 0; i < 32797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32797(int& val) {
    const int x = 32797;
    std::cout << 32797;
    for (int i = 0; i < 32797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32798(int& val) {
    func_in_32798(val);
    const int x = 32798;
    std::cout << 32798;
    for (int i = 0; i < 32798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32798(int& val) {
    const int x = 32798;
    std::cout << 32798;
    for (int i = 0; i < 32798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32799(int& val) {
    func_in_32799(val);
    const int x = 32799;
    std::cout << 32799;
    for (int i = 0; i < 32799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32799(int& val) {
    const int x = 32799;
    std::cout << 32799;
    for (int i = 0; i < 32799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32800(int& val) {
    func_in_32800(val);
    const int x = 32800;
    std::cout << 32800;
    for (int i = 0; i < 32800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32800(int& val) {
    const int x = 32800;
    std::cout << 32800;
    for (int i = 0; i < 32800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32801(int& val) {
    func_in_32801(val);
    const int x = 32801;
    std::cout << 32801;
    for (int i = 0; i < 32801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32801(int& val) {
    const int x = 32801;
    std::cout << 32801;
    for (int i = 0; i < 32801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32802(int& val) {
    func_in_32802(val);
    const int x = 32802;
    std::cout << 32802;
    for (int i = 0; i < 32802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32802(int& val) {
    const int x = 32802;
    std::cout << 32802;
    for (int i = 0; i < 32802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32803(int& val) {
    func_in_32803(val);
    const int x = 32803;
    std::cout << 32803;
    for (int i = 0; i < 32803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32803(int& val) {
    const int x = 32803;
    std::cout << 32803;
    for (int i = 0; i < 32803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32804(int& val) {
    func_in_32804(val);
    const int x = 32804;
    std::cout << 32804;
    for (int i = 0; i < 32804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32804(int& val) {
    const int x = 32804;
    std::cout << 32804;
    for (int i = 0; i < 32804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32805(int& val) {
    func_in_32805(val);
    const int x = 32805;
    std::cout << 32805;
    for (int i = 0; i < 32805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32805(int& val) {
    const int x = 32805;
    std::cout << 32805;
    for (int i = 0; i < 32805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32806(int& val) {
    func_in_32806(val);
    const int x = 32806;
    std::cout << 32806;
    for (int i = 0; i < 32806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32806(int& val) {
    const int x = 32806;
    std::cout << 32806;
    for (int i = 0; i < 32806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32807(int& val) {
    func_in_32807(val);
    const int x = 32807;
    std::cout << 32807;
    for (int i = 0; i < 32807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32807(int& val) {
    const int x = 32807;
    std::cout << 32807;
    for (int i = 0; i < 32807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32808(int& val) {
    func_in_32808(val);
    const int x = 32808;
    std::cout << 32808;
    for (int i = 0; i < 32808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32808(int& val) {
    const int x = 32808;
    std::cout << 32808;
    for (int i = 0; i < 32808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32809(int& val) {
    func_in_32809(val);
    const int x = 32809;
    std::cout << 32809;
    for (int i = 0; i < 32809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32809(int& val) {
    const int x = 32809;
    std::cout << 32809;
    for (int i = 0; i < 32809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32810(int& val) {
    func_in_32810(val);
    const int x = 32810;
    std::cout << 32810;
    for (int i = 0; i < 32810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32810(int& val) {
    const int x = 32810;
    std::cout << 32810;
    for (int i = 0; i < 32810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32811(int& val) {
    func_in_32811(val);
    const int x = 32811;
    std::cout << 32811;
    for (int i = 0; i < 32811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32811(int& val) {
    const int x = 32811;
    std::cout << 32811;
    for (int i = 0; i < 32811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32812(int& val) {
    func_in_32812(val);
    const int x = 32812;
    std::cout << 32812;
    for (int i = 0; i < 32812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32812(int& val) {
    const int x = 32812;
    std::cout << 32812;
    for (int i = 0; i < 32812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32813(int& val) {
    func_in_32813(val);
    const int x = 32813;
    std::cout << 32813;
    for (int i = 0; i < 32813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32813(int& val) {
    const int x = 32813;
    std::cout << 32813;
    for (int i = 0; i < 32813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32814(int& val) {
    func_in_32814(val);
    const int x = 32814;
    std::cout << 32814;
    for (int i = 0; i < 32814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32814(int& val) {
    const int x = 32814;
    std::cout << 32814;
    for (int i = 0; i < 32814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32815(int& val) {
    func_in_32815(val);
    const int x = 32815;
    std::cout << 32815;
    for (int i = 0; i < 32815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32815(int& val) {
    const int x = 32815;
    std::cout << 32815;
    for (int i = 0; i < 32815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32816(int& val) {
    func_in_32816(val);
    const int x = 32816;
    std::cout << 32816;
    for (int i = 0; i < 32816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32816(int& val) {
    const int x = 32816;
    std::cout << 32816;
    for (int i = 0; i < 32816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32817(int& val) {
    func_in_32817(val);
    const int x = 32817;
    std::cout << 32817;
    for (int i = 0; i < 32817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32817(int& val) {
    const int x = 32817;
    std::cout << 32817;
    for (int i = 0; i < 32817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32818(int& val) {
    func_in_32818(val);
    const int x = 32818;
    std::cout << 32818;
    for (int i = 0; i < 32818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32818(int& val) {
    const int x = 32818;
    std::cout << 32818;
    for (int i = 0; i < 32818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32819(int& val) {
    func_in_32819(val);
    const int x = 32819;
    std::cout << 32819;
    for (int i = 0; i < 32819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32819(int& val) {
    const int x = 32819;
    std::cout << 32819;
    for (int i = 0; i < 32819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32820(int& val) {
    func_in_32820(val);
    const int x = 32820;
    std::cout << 32820;
    for (int i = 0; i < 32820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32820(int& val) {
    const int x = 32820;
    std::cout << 32820;
    for (int i = 0; i < 32820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32821(int& val) {
    func_in_32821(val);
    const int x = 32821;
    std::cout << 32821;
    for (int i = 0; i < 32821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32821(int& val) {
    const int x = 32821;
    std::cout << 32821;
    for (int i = 0; i < 32821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32822(int& val) {
    func_in_32822(val);
    const int x = 32822;
    std::cout << 32822;
    for (int i = 0; i < 32822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32822(int& val) {
    const int x = 32822;
    std::cout << 32822;
    for (int i = 0; i < 32822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32823(int& val) {
    func_in_32823(val);
    const int x = 32823;
    std::cout << 32823;
    for (int i = 0; i < 32823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32823(int& val) {
    const int x = 32823;
    std::cout << 32823;
    for (int i = 0; i < 32823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32824(int& val) {
    func_in_32824(val);
    const int x = 32824;
    std::cout << 32824;
    for (int i = 0; i < 32824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32824(int& val) {
    const int x = 32824;
    std::cout << 32824;
    for (int i = 0; i < 32824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32825(int& val) {
    func_in_32825(val);
    const int x = 32825;
    std::cout << 32825;
    for (int i = 0; i < 32825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32825(int& val) {
    const int x = 32825;
    std::cout << 32825;
    for (int i = 0; i < 32825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32826(int& val) {
    func_in_32826(val);
    const int x = 32826;
    std::cout << 32826;
    for (int i = 0; i < 32826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32826(int& val) {
    const int x = 32826;
    std::cout << 32826;
    for (int i = 0; i < 32826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32827(int& val) {
    func_in_32827(val);
    const int x = 32827;
    std::cout << 32827;
    for (int i = 0; i < 32827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32827(int& val) {
    const int x = 32827;
    std::cout << 32827;
    for (int i = 0; i < 32827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32828(int& val) {
    func_in_32828(val);
    const int x = 32828;
    std::cout << 32828;
    for (int i = 0; i < 32828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32828(int& val) {
    const int x = 32828;
    std::cout << 32828;
    for (int i = 0; i < 32828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32829(int& val) {
    func_in_32829(val);
    const int x = 32829;
    std::cout << 32829;
    for (int i = 0; i < 32829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32829(int& val) {
    const int x = 32829;
    std::cout << 32829;
    for (int i = 0; i < 32829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32830(int& val) {
    func_in_32830(val);
    const int x = 32830;
    std::cout << 32830;
    for (int i = 0; i < 32830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32830(int& val) {
    const int x = 32830;
    std::cout << 32830;
    for (int i = 0; i < 32830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32831(int& val) {
    func_in_32831(val);
    const int x = 32831;
    std::cout << 32831;
    for (int i = 0; i < 32831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32831(int& val) {
    const int x = 32831;
    std::cout << 32831;
    for (int i = 0; i < 32831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32832(int& val) {
    func_in_32832(val);
    const int x = 32832;
    std::cout << 32832;
    for (int i = 0; i < 32832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32832(int& val) {
    const int x = 32832;
    std::cout << 32832;
    for (int i = 0; i < 32832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32833(int& val) {
    func_in_32833(val);
    const int x = 32833;
    std::cout << 32833;
    for (int i = 0; i < 32833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32833(int& val) {
    const int x = 32833;
    std::cout << 32833;
    for (int i = 0; i < 32833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32834(int& val) {
    func_in_32834(val);
    const int x = 32834;
    std::cout << 32834;
    for (int i = 0; i < 32834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32834(int& val) {
    const int x = 32834;
    std::cout << 32834;
    for (int i = 0; i < 32834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32835(int& val) {
    func_in_32835(val);
    const int x = 32835;
    std::cout << 32835;
    for (int i = 0; i < 32835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32835(int& val) {
    const int x = 32835;
    std::cout << 32835;
    for (int i = 0; i < 32835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32836(int& val) {
    func_in_32836(val);
    const int x = 32836;
    std::cout << 32836;
    for (int i = 0; i < 32836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32836(int& val) {
    const int x = 32836;
    std::cout << 32836;
    for (int i = 0; i < 32836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32837(int& val) {
    func_in_32837(val);
    const int x = 32837;
    std::cout << 32837;
    for (int i = 0; i < 32837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32837(int& val) {
    const int x = 32837;
    std::cout << 32837;
    for (int i = 0; i < 32837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32838(int& val) {
    func_in_32838(val);
    const int x = 32838;
    std::cout << 32838;
    for (int i = 0; i < 32838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32838(int& val) {
    const int x = 32838;
    std::cout << 32838;
    for (int i = 0; i < 32838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32839(int& val) {
    func_in_32839(val);
    const int x = 32839;
    std::cout << 32839;
    for (int i = 0; i < 32839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32839(int& val) {
    const int x = 32839;
    std::cout << 32839;
    for (int i = 0; i < 32839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32840(int& val) {
    func_in_32840(val);
    const int x = 32840;
    std::cout << 32840;
    for (int i = 0; i < 32840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32840(int& val) {
    const int x = 32840;
    std::cout << 32840;
    for (int i = 0; i < 32840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32841(int& val) {
    func_in_32841(val);
    const int x = 32841;
    std::cout << 32841;
    for (int i = 0; i < 32841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32841(int& val) {
    const int x = 32841;
    std::cout << 32841;
    for (int i = 0; i < 32841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32842(int& val) {
    func_in_32842(val);
    const int x = 32842;
    std::cout << 32842;
    for (int i = 0; i < 32842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32842(int& val) {
    const int x = 32842;
    std::cout << 32842;
    for (int i = 0; i < 32842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32843(int& val) {
    func_in_32843(val);
    const int x = 32843;
    std::cout << 32843;
    for (int i = 0; i < 32843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32843(int& val) {
    const int x = 32843;
    std::cout << 32843;
    for (int i = 0; i < 32843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32844(int& val) {
    func_in_32844(val);
    const int x = 32844;
    std::cout << 32844;
    for (int i = 0; i < 32844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32844(int& val) {
    const int x = 32844;
    std::cout << 32844;
    for (int i = 0; i < 32844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32845(int& val) {
    func_in_32845(val);
    const int x = 32845;
    std::cout << 32845;
    for (int i = 0; i < 32845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32845(int& val) {
    const int x = 32845;
    std::cout << 32845;
    for (int i = 0; i < 32845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32846(int& val) {
    func_in_32846(val);
    const int x = 32846;
    std::cout << 32846;
    for (int i = 0; i < 32846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32846(int& val) {
    const int x = 32846;
    std::cout << 32846;
    for (int i = 0; i < 32846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32847(int& val) {
    func_in_32847(val);
    const int x = 32847;
    std::cout << 32847;
    for (int i = 0; i < 32847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32847(int& val) {
    const int x = 32847;
    std::cout << 32847;
    for (int i = 0; i < 32847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32848(int& val) {
    func_in_32848(val);
    const int x = 32848;
    std::cout << 32848;
    for (int i = 0; i < 32848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32848(int& val) {
    const int x = 32848;
    std::cout << 32848;
    for (int i = 0; i < 32848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32849(int& val) {
    func_in_32849(val);
    const int x = 32849;
    std::cout << 32849;
    for (int i = 0; i < 32849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32849(int& val) {
    const int x = 32849;
    std::cout << 32849;
    for (int i = 0; i < 32849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32850(int& val) {
    func_in_32850(val);
    const int x = 32850;
    std::cout << 32850;
    for (int i = 0; i < 32850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32850(int& val) {
    const int x = 32850;
    std::cout << 32850;
    for (int i = 0; i < 32850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32851(int& val) {
    func_in_32851(val);
    const int x = 32851;
    std::cout << 32851;
    for (int i = 0; i < 32851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32851(int& val) {
    const int x = 32851;
    std::cout << 32851;
    for (int i = 0; i < 32851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32852(int& val) {
    func_in_32852(val);
    const int x = 32852;
    std::cout << 32852;
    for (int i = 0; i < 32852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32852(int& val) {
    const int x = 32852;
    std::cout << 32852;
    for (int i = 0; i < 32852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32853(int& val) {
    func_in_32853(val);
    const int x = 32853;
    std::cout << 32853;
    for (int i = 0; i < 32853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32853(int& val) {
    const int x = 32853;
    std::cout << 32853;
    for (int i = 0; i < 32853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32854(int& val) {
    func_in_32854(val);
    const int x = 32854;
    std::cout << 32854;
    for (int i = 0; i < 32854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32854(int& val) {
    const int x = 32854;
    std::cout << 32854;
    for (int i = 0; i < 32854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32855(int& val) {
    func_in_32855(val);
    const int x = 32855;
    std::cout << 32855;
    for (int i = 0; i < 32855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32855(int& val) {
    const int x = 32855;
    std::cout << 32855;
    for (int i = 0; i < 32855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32856(int& val) {
    func_in_32856(val);
    const int x = 32856;
    std::cout << 32856;
    for (int i = 0; i < 32856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32856(int& val) {
    const int x = 32856;
    std::cout << 32856;
    for (int i = 0; i < 32856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32857(int& val) {
    func_in_32857(val);
    const int x = 32857;
    std::cout << 32857;
    for (int i = 0; i < 32857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32857(int& val) {
    const int x = 32857;
    std::cout << 32857;
    for (int i = 0; i < 32857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32858(int& val) {
    func_in_32858(val);
    const int x = 32858;
    std::cout << 32858;
    for (int i = 0; i < 32858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32858(int& val) {
    const int x = 32858;
    std::cout << 32858;
    for (int i = 0; i < 32858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32859(int& val) {
    func_in_32859(val);
    const int x = 32859;
    std::cout << 32859;
    for (int i = 0; i < 32859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32859(int& val) {
    const int x = 32859;
    std::cout << 32859;
    for (int i = 0; i < 32859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32860(int& val) {
    func_in_32860(val);
    const int x = 32860;
    std::cout << 32860;
    for (int i = 0; i < 32860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32860(int& val) {
    const int x = 32860;
    std::cout << 32860;
    for (int i = 0; i < 32860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32861(int& val) {
    func_in_32861(val);
    const int x = 32861;
    std::cout << 32861;
    for (int i = 0; i < 32861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32861(int& val) {
    const int x = 32861;
    std::cout << 32861;
    for (int i = 0; i < 32861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32862(int& val) {
    func_in_32862(val);
    const int x = 32862;
    std::cout << 32862;
    for (int i = 0; i < 32862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32862(int& val) {
    const int x = 32862;
    std::cout << 32862;
    for (int i = 0; i < 32862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32863(int& val) {
    func_in_32863(val);
    const int x = 32863;
    std::cout << 32863;
    for (int i = 0; i < 32863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32863(int& val) {
    const int x = 32863;
    std::cout << 32863;
    for (int i = 0; i < 32863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32864(int& val) {
    func_in_32864(val);
    const int x = 32864;
    std::cout << 32864;
    for (int i = 0; i < 32864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32864(int& val) {
    const int x = 32864;
    std::cout << 32864;
    for (int i = 0; i < 32864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32865(int& val) {
    func_in_32865(val);
    const int x = 32865;
    std::cout << 32865;
    for (int i = 0; i < 32865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32865(int& val) {
    const int x = 32865;
    std::cout << 32865;
    for (int i = 0; i < 32865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32866(int& val) {
    func_in_32866(val);
    const int x = 32866;
    std::cout << 32866;
    for (int i = 0; i < 32866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32866(int& val) {
    const int x = 32866;
    std::cout << 32866;
    for (int i = 0; i < 32866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32867(int& val) {
    func_in_32867(val);
    const int x = 32867;
    std::cout << 32867;
    for (int i = 0; i < 32867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32867(int& val) {
    const int x = 32867;
    std::cout << 32867;
    for (int i = 0; i < 32867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32868(int& val) {
    func_in_32868(val);
    const int x = 32868;
    std::cout << 32868;
    for (int i = 0; i < 32868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32868(int& val) {
    const int x = 32868;
    std::cout << 32868;
    for (int i = 0; i < 32868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32869(int& val) {
    func_in_32869(val);
    const int x = 32869;
    std::cout << 32869;
    for (int i = 0; i < 32869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32869(int& val) {
    const int x = 32869;
    std::cout << 32869;
    for (int i = 0; i < 32869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32870(int& val) {
    func_in_32870(val);
    const int x = 32870;
    std::cout << 32870;
    for (int i = 0; i < 32870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32870(int& val) {
    const int x = 32870;
    std::cout << 32870;
    for (int i = 0; i < 32870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32871(int& val) {
    func_in_32871(val);
    const int x = 32871;
    std::cout << 32871;
    for (int i = 0; i < 32871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32871(int& val) {
    const int x = 32871;
    std::cout << 32871;
    for (int i = 0; i < 32871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32872(int& val) {
    func_in_32872(val);
    const int x = 32872;
    std::cout << 32872;
    for (int i = 0; i < 32872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32872(int& val) {
    const int x = 32872;
    std::cout << 32872;
    for (int i = 0; i < 32872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32873(int& val) {
    func_in_32873(val);
    const int x = 32873;
    std::cout << 32873;
    for (int i = 0; i < 32873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32873(int& val) {
    const int x = 32873;
    std::cout << 32873;
    for (int i = 0; i < 32873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32874(int& val) {
    func_in_32874(val);
    const int x = 32874;
    std::cout << 32874;
    for (int i = 0; i < 32874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32874(int& val) {
    const int x = 32874;
    std::cout << 32874;
    for (int i = 0; i < 32874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32875(int& val) {
    func_in_32875(val);
    const int x = 32875;
    std::cout << 32875;
    for (int i = 0; i < 32875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32875(int& val) {
    const int x = 32875;
    std::cout << 32875;
    for (int i = 0; i < 32875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32876(int& val) {
    func_in_32876(val);
    const int x = 32876;
    std::cout << 32876;
    for (int i = 0; i < 32876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32876(int& val) {
    const int x = 32876;
    std::cout << 32876;
    for (int i = 0; i < 32876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32877(int& val) {
    func_in_32877(val);
    const int x = 32877;
    std::cout << 32877;
    for (int i = 0; i < 32877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32877(int& val) {
    const int x = 32877;
    std::cout << 32877;
    for (int i = 0; i < 32877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32878(int& val) {
    func_in_32878(val);
    const int x = 32878;
    std::cout << 32878;
    for (int i = 0; i < 32878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32878(int& val) {
    const int x = 32878;
    std::cout << 32878;
    for (int i = 0; i < 32878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32879(int& val) {
    func_in_32879(val);
    const int x = 32879;
    std::cout << 32879;
    for (int i = 0; i < 32879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32879(int& val) {
    const int x = 32879;
    std::cout << 32879;
    for (int i = 0; i < 32879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32880(int& val) {
    func_in_32880(val);
    const int x = 32880;
    std::cout << 32880;
    for (int i = 0; i < 32880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32880(int& val) {
    const int x = 32880;
    std::cout << 32880;
    for (int i = 0; i < 32880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32881(int& val) {
    func_in_32881(val);
    const int x = 32881;
    std::cout << 32881;
    for (int i = 0; i < 32881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32881(int& val) {
    const int x = 32881;
    std::cout << 32881;
    for (int i = 0; i < 32881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32882(int& val) {
    func_in_32882(val);
    const int x = 32882;
    std::cout << 32882;
    for (int i = 0; i < 32882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32882(int& val) {
    const int x = 32882;
    std::cout << 32882;
    for (int i = 0; i < 32882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32883(int& val) {
    func_in_32883(val);
    const int x = 32883;
    std::cout << 32883;
    for (int i = 0; i < 32883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32883(int& val) {
    const int x = 32883;
    std::cout << 32883;
    for (int i = 0; i < 32883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32884(int& val) {
    func_in_32884(val);
    const int x = 32884;
    std::cout << 32884;
    for (int i = 0; i < 32884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32884(int& val) {
    const int x = 32884;
    std::cout << 32884;
    for (int i = 0; i < 32884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32885(int& val) {
    func_in_32885(val);
    const int x = 32885;
    std::cout << 32885;
    for (int i = 0; i < 32885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32885(int& val) {
    const int x = 32885;
    std::cout << 32885;
    for (int i = 0; i < 32885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32886(int& val) {
    func_in_32886(val);
    const int x = 32886;
    std::cout << 32886;
    for (int i = 0; i < 32886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32886(int& val) {
    const int x = 32886;
    std::cout << 32886;
    for (int i = 0; i < 32886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32887(int& val) {
    func_in_32887(val);
    const int x = 32887;
    std::cout << 32887;
    for (int i = 0; i < 32887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32887(int& val) {
    const int x = 32887;
    std::cout << 32887;
    for (int i = 0; i < 32887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32888(int& val) {
    func_in_32888(val);
    const int x = 32888;
    std::cout << 32888;
    for (int i = 0; i < 32888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32888(int& val) {
    const int x = 32888;
    std::cout << 32888;
    for (int i = 0; i < 32888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32889(int& val) {
    func_in_32889(val);
    const int x = 32889;
    std::cout << 32889;
    for (int i = 0; i < 32889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32889(int& val) {
    const int x = 32889;
    std::cout << 32889;
    for (int i = 0; i < 32889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32890(int& val) {
    func_in_32890(val);
    const int x = 32890;
    std::cout << 32890;
    for (int i = 0; i < 32890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32890(int& val) {
    const int x = 32890;
    std::cout << 32890;
    for (int i = 0; i < 32890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32891(int& val) {
    func_in_32891(val);
    const int x = 32891;
    std::cout << 32891;
    for (int i = 0; i < 32891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32891(int& val) {
    const int x = 32891;
    std::cout << 32891;
    for (int i = 0; i < 32891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32892(int& val) {
    func_in_32892(val);
    const int x = 32892;
    std::cout << 32892;
    for (int i = 0; i < 32892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32892(int& val) {
    const int x = 32892;
    std::cout << 32892;
    for (int i = 0; i < 32892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32893(int& val) {
    func_in_32893(val);
    const int x = 32893;
    std::cout << 32893;
    for (int i = 0; i < 32893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32893(int& val) {
    const int x = 32893;
    std::cout << 32893;
    for (int i = 0; i < 32893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32894(int& val) {
    func_in_32894(val);
    const int x = 32894;
    std::cout << 32894;
    for (int i = 0; i < 32894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32894(int& val) {
    const int x = 32894;
    std::cout << 32894;
    for (int i = 0; i < 32894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32895(int& val) {
    func_in_32895(val);
    const int x = 32895;
    std::cout << 32895;
    for (int i = 0; i < 32895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32895(int& val) {
    const int x = 32895;
    std::cout << 32895;
    for (int i = 0; i < 32895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32896(int& val) {
    func_in_32896(val);
    const int x = 32896;
    std::cout << 32896;
    for (int i = 0; i < 32896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32896(int& val) {
    const int x = 32896;
    std::cout << 32896;
    for (int i = 0; i < 32896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32897(int& val) {
    func_in_32897(val);
    const int x = 32897;
    std::cout << 32897;
    for (int i = 0; i < 32897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32897(int& val) {
    const int x = 32897;
    std::cout << 32897;
    for (int i = 0; i < 32897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32898(int& val) {
    func_in_32898(val);
    const int x = 32898;
    std::cout << 32898;
    for (int i = 0; i < 32898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32898(int& val) {
    const int x = 32898;
    std::cout << 32898;
    for (int i = 0; i < 32898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32899(int& val) {
    func_in_32899(val);
    const int x = 32899;
    std::cout << 32899;
    for (int i = 0; i < 32899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32899(int& val) {
    const int x = 32899;
    std::cout << 32899;
    for (int i = 0; i < 32899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32900(int& val) {
    func_in_32900(val);
    const int x = 32900;
    std::cout << 32900;
    for (int i = 0; i < 32900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32900(int& val) {
    const int x = 32900;
    std::cout << 32900;
    for (int i = 0; i < 32900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32901(int& val) {
    func_in_32901(val);
    const int x = 32901;
    std::cout << 32901;
    for (int i = 0; i < 32901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32901(int& val) {
    const int x = 32901;
    std::cout << 32901;
    for (int i = 0; i < 32901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32902(int& val) {
    func_in_32902(val);
    const int x = 32902;
    std::cout << 32902;
    for (int i = 0; i < 32902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32902(int& val) {
    const int x = 32902;
    std::cout << 32902;
    for (int i = 0; i < 32902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32903(int& val) {
    func_in_32903(val);
    const int x = 32903;
    std::cout << 32903;
    for (int i = 0; i < 32903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32903(int& val) {
    const int x = 32903;
    std::cout << 32903;
    for (int i = 0; i < 32903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32904(int& val) {
    func_in_32904(val);
    const int x = 32904;
    std::cout << 32904;
    for (int i = 0; i < 32904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32904(int& val) {
    const int x = 32904;
    std::cout << 32904;
    for (int i = 0; i < 32904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32905(int& val) {
    func_in_32905(val);
    const int x = 32905;
    std::cout << 32905;
    for (int i = 0; i < 32905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32905(int& val) {
    const int x = 32905;
    std::cout << 32905;
    for (int i = 0; i < 32905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32906(int& val) {
    func_in_32906(val);
    const int x = 32906;
    std::cout << 32906;
    for (int i = 0; i < 32906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32906(int& val) {
    const int x = 32906;
    std::cout << 32906;
    for (int i = 0; i < 32906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32907(int& val) {
    func_in_32907(val);
    const int x = 32907;
    std::cout << 32907;
    for (int i = 0; i < 32907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32907(int& val) {
    const int x = 32907;
    std::cout << 32907;
    for (int i = 0; i < 32907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32908(int& val) {
    func_in_32908(val);
    const int x = 32908;
    std::cout << 32908;
    for (int i = 0; i < 32908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32908(int& val) {
    const int x = 32908;
    std::cout << 32908;
    for (int i = 0; i < 32908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32909(int& val) {
    func_in_32909(val);
    const int x = 32909;
    std::cout << 32909;
    for (int i = 0; i < 32909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32909(int& val) {
    const int x = 32909;
    std::cout << 32909;
    for (int i = 0; i < 32909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32910(int& val) {
    func_in_32910(val);
    const int x = 32910;
    std::cout << 32910;
    for (int i = 0; i < 32910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32910(int& val) {
    const int x = 32910;
    std::cout << 32910;
    for (int i = 0; i < 32910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32911(int& val) {
    func_in_32911(val);
    const int x = 32911;
    std::cout << 32911;
    for (int i = 0; i < 32911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32911(int& val) {
    const int x = 32911;
    std::cout << 32911;
    for (int i = 0; i < 32911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32912(int& val) {
    func_in_32912(val);
    const int x = 32912;
    std::cout << 32912;
    for (int i = 0; i < 32912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32912(int& val) {
    const int x = 32912;
    std::cout << 32912;
    for (int i = 0; i < 32912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32913(int& val) {
    func_in_32913(val);
    const int x = 32913;
    std::cout << 32913;
    for (int i = 0; i < 32913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32913(int& val) {
    const int x = 32913;
    std::cout << 32913;
    for (int i = 0; i < 32913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32914(int& val) {
    func_in_32914(val);
    const int x = 32914;
    std::cout << 32914;
    for (int i = 0; i < 32914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32914(int& val) {
    const int x = 32914;
    std::cout << 32914;
    for (int i = 0; i < 32914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32915(int& val) {
    func_in_32915(val);
    const int x = 32915;
    std::cout << 32915;
    for (int i = 0; i < 32915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32915(int& val) {
    const int x = 32915;
    std::cout << 32915;
    for (int i = 0; i < 32915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32916(int& val) {
    func_in_32916(val);
    const int x = 32916;
    std::cout << 32916;
    for (int i = 0; i < 32916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32916(int& val) {
    const int x = 32916;
    std::cout << 32916;
    for (int i = 0; i < 32916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32917(int& val) {
    func_in_32917(val);
    const int x = 32917;
    std::cout << 32917;
    for (int i = 0; i < 32917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32917(int& val) {
    const int x = 32917;
    std::cout << 32917;
    for (int i = 0; i < 32917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32918(int& val) {
    func_in_32918(val);
    const int x = 32918;
    std::cout << 32918;
    for (int i = 0; i < 32918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32918(int& val) {
    const int x = 32918;
    std::cout << 32918;
    for (int i = 0; i < 32918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32919(int& val) {
    func_in_32919(val);
    const int x = 32919;
    std::cout << 32919;
    for (int i = 0; i < 32919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32919(int& val) {
    const int x = 32919;
    std::cout << 32919;
    for (int i = 0; i < 32919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32920(int& val) {
    func_in_32920(val);
    const int x = 32920;
    std::cout << 32920;
    for (int i = 0; i < 32920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32920(int& val) {
    const int x = 32920;
    std::cout << 32920;
    for (int i = 0; i < 32920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32921(int& val) {
    func_in_32921(val);
    const int x = 32921;
    std::cout << 32921;
    for (int i = 0; i < 32921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32921(int& val) {
    const int x = 32921;
    std::cout << 32921;
    for (int i = 0; i < 32921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32922(int& val) {
    func_in_32922(val);
    const int x = 32922;
    std::cout << 32922;
    for (int i = 0; i < 32922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32922(int& val) {
    const int x = 32922;
    std::cout << 32922;
    for (int i = 0; i < 32922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32923(int& val) {
    func_in_32923(val);
    const int x = 32923;
    std::cout << 32923;
    for (int i = 0; i < 32923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32923(int& val) {
    const int x = 32923;
    std::cout << 32923;
    for (int i = 0; i < 32923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32924(int& val) {
    func_in_32924(val);
    const int x = 32924;
    std::cout << 32924;
    for (int i = 0; i < 32924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32924(int& val) {
    const int x = 32924;
    std::cout << 32924;
    for (int i = 0; i < 32924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32925(int& val) {
    func_in_32925(val);
    const int x = 32925;
    std::cout << 32925;
    for (int i = 0; i < 32925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32925(int& val) {
    const int x = 32925;
    std::cout << 32925;
    for (int i = 0; i < 32925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32926(int& val) {
    func_in_32926(val);
    const int x = 32926;
    std::cout << 32926;
    for (int i = 0; i < 32926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32926(int& val) {
    const int x = 32926;
    std::cout << 32926;
    for (int i = 0; i < 32926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32927(int& val) {
    func_in_32927(val);
    const int x = 32927;
    std::cout << 32927;
    for (int i = 0; i < 32927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32927(int& val) {
    const int x = 32927;
    std::cout << 32927;
    for (int i = 0; i < 32927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32928(int& val) {
    func_in_32928(val);
    const int x = 32928;
    std::cout << 32928;
    for (int i = 0; i < 32928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32928(int& val) {
    const int x = 32928;
    std::cout << 32928;
    for (int i = 0; i < 32928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32929(int& val) {
    func_in_32929(val);
    const int x = 32929;
    std::cout << 32929;
    for (int i = 0; i < 32929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32929(int& val) {
    const int x = 32929;
    std::cout << 32929;
    for (int i = 0; i < 32929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32930(int& val) {
    func_in_32930(val);
    const int x = 32930;
    std::cout << 32930;
    for (int i = 0; i < 32930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32930(int& val) {
    const int x = 32930;
    std::cout << 32930;
    for (int i = 0; i < 32930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32931(int& val) {
    func_in_32931(val);
    const int x = 32931;
    std::cout << 32931;
    for (int i = 0; i < 32931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32931(int& val) {
    const int x = 32931;
    std::cout << 32931;
    for (int i = 0; i < 32931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32932(int& val) {
    func_in_32932(val);
    const int x = 32932;
    std::cout << 32932;
    for (int i = 0; i < 32932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32932(int& val) {
    const int x = 32932;
    std::cout << 32932;
    for (int i = 0; i < 32932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32933(int& val) {
    func_in_32933(val);
    const int x = 32933;
    std::cout << 32933;
    for (int i = 0; i < 32933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32933(int& val) {
    const int x = 32933;
    std::cout << 32933;
    for (int i = 0; i < 32933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32934(int& val) {
    func_in_32934(val);
    const int x = 32934;
    std::cout << 32934;
    for (int i = 0; i < 32934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32934(int& val) {
    const int x = 32934;
    std::cout << 32934;
    for (int i = 0; i < 32934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32935(int& val) {
    func_in_32935(val);
    const int x = 32935;
    std::cout << 32935;
    for (int i = 0; i < 32935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32935(int& val) {
    const int x = 32935;
    std::cout << 32935;
    for (int i = 0; i < 32935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32936(int& val) {
    func_in_32936(val);
    const int x = 32936;
    std::cout << 32936;
    for (int i = 0; i < 32936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32936(int& val) {
    const int x = 32936;
    std::cout << 32936;
    for (int i = 0; i < 32936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32937(int& val) {
    func_in_32937(val);
    const int x = 32937;
    std::cout << 32937;
    for (int i = 0; i < 32937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32937(int& val) {
    const int x = 32937;
    std::cout << 32937;
    for (int i = 0; i < 32937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32938(int& val) {
    func_in_32938(val);
    const int x = 32938;
    std::cout << 32938;
    for (int i = 0; i < 32938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32938(int& val) {
    const int x = 32938;
    std::cout << 32938;
    for (int i = 0; i < 32938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32939(int& val) {
    func_in_32939(val);
    const int x = 32939;
    std::cout << 32939;
    for (int i = 0; i < 32939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32939(int& val) {
    const int x = 32939;
    std::cout << 32939;
    for (int i = 0; i < 32939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32940(int& val) {
    func_in_32940(val);
    const int x = 32940;
    std::cout << 32940;
    for (int i = 0; i < 32940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32940(int& val) {
    const int x = 32940;
    std::cout << 32940;
    for (int i = 0; i < 32940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32941(int& val) {
    func_in_32941(val);
    const int x = 32941;
    std::cout << 32941;
    for (int i = 0; i < 32941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32941(int& val) {
    const int x = 32941;
    std::cout << 32941;
    for (int i = 0; i < 32941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32942(int& val) {
    func_in_32942(val);
    const int x = 32942;
    std::cout << 32942;
    for (int i = 0; i < 32942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32942(int& val) {
    const int x = 32942;
    std::cout << 32942;
    for (int i = 0; i < 32942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32943(int& val) {
    func_in_32943(val);
    const int x = 32943;
    std::cout << 32943;
    for (int i = 0; i < 32943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32943(int& val) {
    const int x = 32943;
    std::cout << 32943;
    for (int i = 0; i < 32943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32944(int& val) {
    func_in_32944(val);
    const int x = 32944;
    std::cout << 32944;
    for (int i = 0; i < 32944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32944(int& val) {
    const int x = 32944;
    std::cout << 32944;
    for (int i = 0; i < 32944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32945(int& val) {
    func_in_32945(val);
    const int x = 32945;
    std::cout << 32945;
    for (int i = 0; i < 32945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32945(int& val) {
    const int x = 32945;
    std::cout << 32945;
    for (int i = 0; i < 32945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32946(int& val) {
    func_in_32946(val);
    const int x = 32946;
    std::cout << 32946;
    for (int i = 0; i < 32946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32946(int& val) {
    const int x = 32946;
    std::cout << 32946;
    for (int i = 0; i < 32946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32947(int& val) {
    func_in_32947(val);
    const int x = 32947;
    std::cout << 32947;
    for (int i = 0; i < 32947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32947(int& val) {
    const int x = 32947;
    std::cout << 32947;
    for (int i = 0; i < 32947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32948(int& val) {
    func_in_32948(val);
    const int x = 32948;
    std::cout << 32948;
    for (int i = 0; i < 32948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32948(int& val) {
    const int x = 32948;
    std::cout << 32948;
    for (int i = 0; i < 32948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32949(int& val) {
    func_in_32949(val);
    const int x = 32949;
    std::cout << 32949;
    for (int i = 0; i < 32949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32949(int& val) {
    const int x = 32949;
    std::cout << 32949;
    for (int i = 0; i < 32949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32950(int& val) {
    func_in_32950(val);
    const int x = 32950;
    std::cout << 32950;
    for (int i = 0; i < 32950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32950(int& val) {
    const int x = 32950;
    std::cout << 32950;
    for (int i = 0; i < 32950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32951(int& val) {
    func_in_32951(val);
    const int x = 32951;
    std::cout << 32951;
    for (int i = 0; i < 32951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32951(int& val) {
    const int x = 32951;
    std::cout << 32951;
    for (int i = 0; i < 32951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32952(int& val) {
    func_in_32952(val);
    const int x = 32952;
    std::cout << 32952;
    for (int i = 0; i < 32952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32952(int& val) {
    const int x = 32952;
    std::cout << 32952;
    for (int i = 0; i < 32952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32953(int& val) {
    func_in_32953(val);
    const int x = 32953;
    std::cout << 32953;
    for (int i = 0; i < 32953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32953(int& val) {
    const int x = 32953;
    std::cout << 32953;
    for (int i = 0; i < 32953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32954(int& val) {
    func_in_32954(val);
    const int x = 32954;
    std::cout << 32954;
    for (int i = 0; i < 32954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32954(int& val) {
    const int x = 32954;
    std::cout << 32954;
    for (int i = 0; i < 32954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32955(int& val) {
    func_in_32955(val);
    const int x = 32955;
    std::cout << 32955;
    for (int i = 0; i < 32955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32955(int& val) {
    const int x = 32955;
    std::cout << 32955;
    for (int i = 0; i < 32955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32956(int& val) {
    func_in_32956(val);
    const int x = 32956;
    std::cout << 32956;
    for (int i = 0; i < 32956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32956(int& val) {
    const int x = 32956;
    std::cout << 32956;
    for (int i = 0; i < 32956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32957(int& val) {
    func_in_32957(val);
    const int x = 32957;
    std::cout << 32957;
    for (int i = 0; i < 32957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32957(int& val) {
    const int x = 32957;
    std::cout << 32957;
    for (int i = 0; i < 32957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32958(int& val) {
    func_in_32958(val);
    const int x = 32958;
    std::cout << 32958;
    for (int i = 0; i < 32958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32958(int& val) {
    const int x = 32958;
    std::cout << 32958;
    for (int i = 0; i < 32958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32959(int& val) {
    func_in_32959(val);
    const int x = 32959;
    std::cout << 32959;
    for (int i = 0; i < 32959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32959(int& val) {
    const int x = 32959;
    std::cout << 32959;
    for (int i = 0; i < 32959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32960(int& val) {
    func_in_32960(val);
    const int x = 32960;
    std::cout << 32960;
    for (int i = 0; i < 32960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32960(int& val) {
    const int x = 32960;
    std::cout << 32960;
    for (int i = 0; i < 32960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32961(int& val) {
    func_in_32961(val);
    const int x = 32961;
    std::cout << 32961;
    for (int i = 0; i < 32961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32961(int& val) {
    const int x = 32961;
    std::cout << 32961;
    for (int i = 0; i < 32961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32962(int& val) {
    func_in_32962(val);
    const int x = 32962;
    std::cout << 32962;
    for (int i = 0; i < 32962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32962(int& val) {
    const int x = 32962;
    std::cout << 32962;
    for (int i = 0; i < 32962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32963(int& val) {
    func_in_32963(val);
    const int x = 32963;
    std::cout << 32963;
    for (int i = 0; i < 32963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32963(int& val) {
    const int x = 32963;
    std::cout << 32963;
    for (int i = 0; i < 32963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32964(int& val) {
    func_in_32964(val);
    const int x = 32964;
    std::cout << 32964;
    for (int i = 0; i < 32964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32964(int& val) {
    const int x = 32964;
    std::cout << 32964;
    for (int i = 0; i < 32964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32965(int& val) {
    func_in_32965(val);
    const int x = 32965;
    std::cout << 32965;
    for (int i = 0; i < 32965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32965(int& val) {
    const int x = 32965;
    std::cout << 32965;
    for (int i = 0; i < 32965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32966(int& val) {
    func_in_32966(val);
    const int x = 32966;
    std::cout << 32966;
    for (int i = 0; i < 32966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32966(int& val) {
    const int x = 32966;
    std::cout << 32966;
    for (int i = 0; i < 32966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32967(int& val) {
    func_in_32967(val);
    const int x = 32967;
    std::cout << 32967;
    for (int i = 0; i < 32967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32967(int& val) {
    const int x = 32967;
    std::cout << 32967;
    for (int i = 0; i < 32967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32968(int& val) {
    func_in_32968(val);
    const int x = 32968;
    std::cout << 32968;
    for (int i = 0; i < 32968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32968(int& val) {
    const int x = 32968;
    std::cout << 32968;
    for (int i = 0; i < 32968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32969(int& val) {
    func_in_32969(val);
    const int x = 32969;
    std::cout << 32969;
    for (int i = 0; i < 32969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32969(int& val) {
    const int x = 32969;
    std::cout << 32969;
    for (int i = 0; i < 32969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32970(int& val) {
    func_in_32970(val);
    const int x = 32970;
    std::cout << 32970;
    for (int i = 0; i < 32970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32970(int& val) {
    const int x = 32970;
    std::cout << 32970;
    for (int i = 0; i < 32970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32971(int& val) {
    func_in_32971(val);
    const int x = 32971;
    std::cout << 32971;
    for (int i = 0; i < 32971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32971(int& val) {
    const int x = 32971;
    std::cout << 32971;
    for (int i = 0; i < 32971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32972(int& val) {
    func_in_32972(val);
    const int x = 32972;
    std::cout << 32972;
    for (int i = 0; i < 32972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32972(int& val) {
    const int x = 32972;
    std::cout << 32972;
    for (int i = 0; i < 32972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32973(int& val) {
    func_in_32973(val);
    const int x = 32973;
    std::cout << 32973;
    for (int i = 0; i < 32973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32973(int& val) {
    const int x = 32973;
    std::cout << 32973;
    for (int i = 0; i < 32973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32974(int& val) {
    func_in_32974(val);
    const int x = 32974;
    std::cout << 32974;
    for (int i = 0; i < 32974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32974(int& val) {
    const int x = 32974;
    std::cout << 32974;
    for (int i = 0; i < 32974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32975(int& val) {
    func_in_32975(val);
    const int x = 32975;
    std::cout << 32975;
    for (int i = 0; i < 32975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32975(int& val) {
    const int x = 32975;
    std::cout << 32975;
    for (int i = 0; i < 32975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32976(int& val) {
    func_in_32976(val);
    const int x = 32976;
    std::cout << 32976;
    for (int i = 0; i < 32976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32976(int& val) {
    const int x = 32976;
    std::cout << 32976;
    for (int i = 0; i < 32976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32977(int& val) {
    func_in_32977(val);
    const int x = 32977;
    std::cout << 32977;
    for (int i = 0; i < 32977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32977(int& val) {
    const int x = 32977;
    std::cout << 32977;
    for (int i = 0; i < 32977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32978(int& val) {
    func_in_32978(val);
    const int x = 32978;
    std::cout << 32978;
    for (int i = 0; i < 32978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32978(int& val) {
    const int x = 32978;
    std::cout << 32978;
    for (int i = 0; i < 32978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32979(int& val) {
    func_in_32979(val);
    const int x = 32979;
    std::cout << 32979;
    for (int i = 0; i < 32979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32979(int& val) {
    const int x = 32979;
    std::cout << 32979;
    for (int i = 0; i < 32979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32980(int& val) {
    func_in_32980(val);
    const int x = 32980;
    std::cout << 32980;
    for (int i = 0; i < 32980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32980(int& val) {
    const int x = 32980;
    std::cout << 32980;
    for (int i = 0; i < 32980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32981(int& val) {
    func_in_32981(val);
    const int x = 32981;
    std::cout << 32981;
    for (int i = 0; i < 32981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32981(int& val) {
    const int x = 32981;
    std::cout << 32981;
    for (int i = 0; i < 32981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32982(int& val) {
    func_in_32982(val);
    const int x = 32982;
    std::cout << 32982;
    for (int i = 0; i < 32982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32982(int& val) {
    const int x = 32982;
    std::cout << 32982;
    for (int i = 0; i < 32982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32983(int& val) {
    func_in_32983(val);
    const int x = 32983;
    std::cout << 32983;
    for (int i = 0; i < 32983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32983(int& val) {
    const int x = 32983;
    std::cout << 32983;
    for (int i = 0; i < 32983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32984(int& val) {
    func_in_32984(val);
    const int x = 32984;
    std::cout << 32984;
    for (int i = 0; i < 32984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32984(int& val) {
    const int x = 32984;
    std::cout << 32984;
    for (int i = 0; i < 32984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32985(int& val) {
    func_in_32985(val);
    const int x = 32985;
    std::cout << 32985;
    for (int i = 0; i < 32985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32985(int& val) {
    const int x = 32985;
    std::cout << 32985;
    for (int i = 0; i < 32985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32986(int& val) {
    func_in_32986(val);
    const int x = 32986;
    std::cout << 32986;
    for (int i = 0; i < 32986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32986(int& val) {
    const int x = 32986;
    std::cout << 32986;
    for (int i = 0; i < 32986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32987(int& val) {
    func_in_32987(val);
    const int x = 32987;
    std::cout << 32987;
    for (int i = 0; i < 32987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32987(int& val) {
    const int x = 32987;
    std::cout << 32987;
    for (int i = 0; i < 32987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32988(int& val) {
    func_in_32988(val);
    const int x = 32988;
    std::cout << 32988;
    for (int i = 0; i < 32988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32988(int& val) {
    const int x = 32988;
    std::cout << 32988;
    for (int i = 0; i < 32988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32989(int& val) {
    func_in_32989(val);
    const int x = 32989;
    std::cout << 32989;
    for (int i = 0; i < 32989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32989(int& val) {
    const int x = 32989;
    std::cout << 32989;
    for (int i = 0; i < 32989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32990(int& val) {
    func_in_32990(val);
    const int x = 32990;
    std::cout << 32990;
    for (int i = 0; i < 32990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32990(int& val) {
    const int x = 32990;
    std::cout << 32990;
    for (int i = 0; i < 32990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32991(int& val) {
    func_in_32991(val);
    const int x = 32991;
    std::cout << 32991;
    for (int i = 0; i < 32991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32991(int& val) {
    const int x = 32991;
    std::cout << 32991;
    for (int i = 0; i < 32991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32992(int& val) {
    func_in_32992(val);
    const int x = 32992;
    std::cout << 32992;
    for (int i = 0; i < 32992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32992(int& val) {
    const int x = 32992;
    std::cout << 32992;
    for (int i = 0; i < 32992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32993(int& val) {
    func_in_32993(val);
    const int x = 32993;
    std::cout << 32993;
    for (int i = 0; i < 32993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32993(int& val) {
    const int x = 32993;
    std::cout << 32993;
    for (int i = 0; i < 32993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32994(int& val) {
    func_in_32994(val);
    const int x = 32994;
    std::cout << 32994;
    for (int i = 0; i < 32994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32994(int& val) {
    const int x = 32994;
    std::cout << 32994;
    for (int i = 0; i < 32994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32995(int& val) {
    func_in_32995(val);
    const int x = 32995;
    std::cout << 32995;
    for (int i = 0; i < 32995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32995(int& val) {
    const int x = 32995;
    std::cout << 32995;
    for (int i = 0; i < 32995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32996(int& val) {
    func_in_32996(val);
    const int x = 32996;
    std::cout << 32996;
    for (int i = 0; i < 32996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32996(int& val) {
    const int x = 32996;
    std::cout << 32996;
    for (int i = 0; i < 32996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32997(int& val) {
    func_in_32997(val);
    const int x = 32997;
    std::cout << 32997;
    for (int i = 0; i < 32997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32997(int& val) {
    const int x = 32997;
    std::cout << 32997;
    for (int i = 0; i < 32997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32998(int& val) {
    func_in_32998(val);
    const int x = 32998;
    std::cout << 32998;
    for (int i = 0; i < 32998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32998(int& val) {
    const int x = 32998;
    std::cout << 32998;
    for (int i = 0; i < 32998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32999(int& val) {
    func_in_32999(val);
    const int x = 32999;
    std::cout << 32999;
    for (int i = 0; i < 32999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32999(int& val) {
    const int x = 32999;
    std::cout << 32999;
    for (int i = 0; i < 32999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33000(int& val) {
    func_in_33000(val);
    const int x = 33000;
    std::cout << 33000;
    for (int i = 0; i < 33000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33000(int& val) {
    const int x = 33000;
    std::cout << 33000;
    for (int i = 0; i < 33000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33001(int& val) {
    func_in_33001(val);
    const int x = 33001;
    std::cout << 33001;
    for (int i = 0; i < 33001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33001(int& val) {
    const int x = 33001;
    std::cout << 33001;
    for (int i = 0; i < 33001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33002(int& val) {
    func_in_33002(val);
    const int x = 33002;
    std::cout << 33002;
    for (int i = 0; i < 33002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33002(int& val) {
    const int x = 33002;
    std::cout << 33002;
    for (int i = 0; i < 33002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33003(int& val) {
    func_in_33003(val);
    const int x = 33003;
    std::cout << 33003;
    for (int i = 0; i < 33003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33003(int& val) {
    const int x = 33003;
    std::cout << 33003;
    for (int i = 0; i < 33003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33004(int& val) {
    func_in_33004(val);
    const int x = 33004;
    std::cout << 33004;
    for (int i = 0; i < 33004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33004(int& val) {
    const int x = 33004;
    std::cout << 33004;
    for (int i = 0; i < 33004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33005(int& val) {
    func_in_33005(val);
    const int x = 33005;
    std::cout << 33005;
    for (int i = 0; i < 33005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33005(int& val) {
    const int x = 33005;
    std::cout << 33005;
    for (int i = 0; i < 33005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33006(int& val) {
    func_in_33006(val);
    const int x = 33006;
    std::cout << 33006;
    for (int i = 0; i < 33006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33006(int& val) {
    const int x = 33006;
    std::cout << 33006;
    for (int i = 0; i < 33006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33007(int& val) {
    func_in_33007(val);
    const int x = 33007;
    std::cout << 33007;
    for (int i = 0; i < 33007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33007(int& val) {
    const int x = 33007;
    std::cout << 33007;
    for (int i = 0; i < 33007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33008(int& val) {
    func_in_33008(val);
    const int x = 33008;
    std::cout << 33008;
    for (int i = 0; i < 33008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33008(int& val) {
    const int x = 33008;
    std::cout << 33008;
    for (int i = 0; i < 33008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33009(int& val) {
    func_in_33009(val);
    const int x = 33009;
    std::cout << 33009;
    for (int i = 0; i < 33009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33009(int& val) {
    const int x = 33009;
    std::cout << 33009;
    for (int i = 0; i < 33009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33010(int& val) {
    func_in_33010(val);
    const int x = 33010;
    std::cout << 33010;
    for (int i = 0; i < 33010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33010(int& val) {
    const int x = 33010;
    std::cout << 33010;
    for (int i = 0; i < 33010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33011(int& val) {
    func_in_33011(val);
    const int x = 33011;
    std::cout << 33011;
    for (int i = 0; i < 33011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33011(int& val) {
    const int x = 33011;
    std::cout << 33011;
    for (int i = 0; i < 33011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33012(int& val) {
    func_in_33012(val);
    const int x = 33012;
    std::cout << 33012;
    for (int i = 0; i < 33012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33012(int& val) {
    const int x = 33012;
    std::cout << 33012;
    for (int i = 0; i < 33012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33013(int& val) {
    func_in_33013(val);
    const int x = 33013;
    std::cout << 33013;
    for (int i = 0; i < 33013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33013(int& val) {
    const int x = 33013;
    std::cout << 33013;
    for (int i = 0; i < 33013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33014(int& val) {
    func_in_33014(val);
    const int x = 33014;
    std::cout << 33014;
    for (int i = 0; i < 33014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33014(int& val) {
    const int x = 33014;
    std::cout << 33014;
    for (int i = 0; i < 33014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33015(int& val) {
    func_in_33015(val);
    const int x = 33015;
    std::cout << 33015;
    for (int i = 0; i < 33015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33015(int& val) {
    const int x = 33015;
    std::cout << 33015;
    for (int i = 0; i < 33015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33016(int& val) {
    func_in_33016(val);
    const int x = 33016;
    std::cout << 33016;
    for (int i = 0; i < 33016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33016(int& val) {
    const int x = 33016;
    std::cout << 33016;
    for (int i = 0; i < 33016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33017(int& val) {
    func_in_33017(val);
    const int x = 33017;
    std::cout << 33017;
    for (int i = 0; i < 33017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33017(int& val) {
    const int x = 33017;
    std::cout << 33017;
    for (int i = 0; i < 33017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33018(int& val) {
    func_in_33018(val);
    const int x = 33018;
    std::cout << 33018;
    for (int i = 0; i < 33018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33018(int& val) {
    const int x = 33018;
    std::cout << 33018;
    for (int i = 0; i < 33018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33019(int& val) {
    func_in_33019(val);
    const int x = 33019;
    std::cout << 33019;
    for (int i = 0; i < 33019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33019(int& val) {
    const int x = 33019;
    std::cout << 33019;
    for (int i = 0; i < 33019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33020(int& val) {
    func_in_33020(val);
    const int x = 33020;
    std::cout << 33020;
    for (int i = 0; i < 33020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33020(int& val) {
    const int x = 33020;
    std::cout << 33020;
    for (int i = 0; i < 33020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33021(int& val) {
    func_in_33021(val);
    const int x = 33021;
    std::cout << 33021;
    for (int i = 0; i < 33021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33021(int& val) {
    const int x = 33021;
    std::cout << 33021;
    for (int i = 0; i < 33021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33022(int& val) {
    func_in_33022(val);
    const int x = 33022;
    std::cout << 33022;
    for (int i = 0; i < 33022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33022(int& val) {
    const int x = 33022;
    std::cout << 33022;
    for (int i = 0; i < 33022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33023(int& val) {
    func_in_33023(val);
    const int x = 33023;
    std::cout << 33023;
    for (int i = 0; i < 33023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33023(int& val) {
    const int x = 33023;
    std::cout << 33023;
    for (int i = 0; i < 33023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33024(int& val) {
    func_in_33024(val);
    const int x = 33024;
    std::cout << 33024;
    for (int i = 0; i < 33024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33024(int& val) {
    const int x = 33024;
    std::cout << 33024;
    for (int i = 0; i < 33024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33025(int& val) {
    func_in_33025(val);
    const int x = 33025;
    std::cout << 33025;
    for (int i = 0; i < 33025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33025(int& val) {
    const int x = 33025;
    std::cout << 33025;
    for (int i = 0; i < 33025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33026(int& val) {
    func_in_33026(val);
    const int x = 33026;
    std::cout << 33026;
    for (int i = 0; i < 33026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33026(int& val) {
    const int x = 33026;
    std::cout << 33026;
    for (int i = 0; i < 33026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33027(int& val) {
    func_in_33027(val);
    const int x = 33027;
    std::cout << 33027;
    for (int i = 0; i < 33027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33027(int& val) {
    const int x = 33027;
    std::cout << 33027;
    for (int i = 0; i < 33027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33028(int& val) {
    func_in_33028(val);
    const int x = 33028;
    std::cout << 33028;
    for (int i = 0; i < 33028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33028(int& val) {
    const int x = 33028;
    std::cout << 33028;
    for (int i = 0; i < 33028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33029(int& val) {
    func_in_33029(val);
    const int x = 33029;
    std::cout << 33029;
    for (int i = 0; i < 33029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33029(int& val) {
    const int x = 33029;
    std::cout << 33029;
    for (int i = 0; i < 33029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33030(int& val) {
    func_in_33030(val);
    const int x = 33030;
    std::cout << 33030;
    for (int i = 0; i < 33030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33030(int& val) {
    const int x = 33030;
    std::cout << 33030;
    for (int i = 0; i < 33030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33031(int& val) {
    func_in_33031(val);
    const int x = 33031;
    std::cout << 33031;
    for (int i = 0; i < 33031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33031(int& val) {
    const int x = 33031;
    std::cout << 33031;
    for (int i = 0; i < 33031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33032(int& val) {
    func_in_33032(val);
    const int x = 33032;
    std::cout << 33032;
    for (int i = 0; i < 33032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33032(int& val) {
    const int x = 33032;
    std::cout << 33032;
    for (int i = 0; i < 33032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33033(int& val) {
    func_in_33033(val);
    const int x = 33033;
    std::cout << 33033;
    for (int i = 0; i < 33033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33033(int& val) {
    const int x = 33033;
    std::cout << 33033;
    for (int i = 0; i < 33033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33034(int& val) {
    func_in_33034(val);
    const int x = 33034;
    std::cout << 33034;
    for (int i = 0; i < 33034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33034(int& val) {
    const int x = 33034;
    std::cout << 33034;
    for (int i = 0; i < 33034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33035(int& val) {
    func_in_33035(val);
    const int x = 33035;
    std::cout << 33035;
    for (int i = 0; i < 33035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33035(int& val) {
    const int x = 33035;
    std::cout << 33035;
    for (int i = 0; i < 33035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33036(int& val) {
    func_in_33036(val);
    const int x = 33036;
    std::cout << 33036;
    for (int i = 0; i < 33036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33036(int& val) {
    const int x = 33036;
    std::cout << 33036;
    for (int i = 0; i < 33036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33037(int& val) {
    func_in_33037(val);
    const int x = 33037;
    std::cout << 33037;
    for (int i = 0; i < 33037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33037(int& val) {
    const int x = 33037;
    std::cout << 33037;
    for (int i = 0; i < 33037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33038(int& val) {
    func_in_33038(val);
    const int x = 33038;
    std::cout << 33038;
    for (int i = 0; i < 33038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33038(int& val) {
    const int x = 33038;
    std::cout << 33038;
    for (int i = 0; i < 33038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33039(int& val) {
    func_in_33039(val);
    const int x = 33039;
    std::cout << 33039;
    for (int i = 0; i < 33039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33039(int& val) {
    const int x = 33039;
    std::cout << 33039;
    for (int i = 0; i < 33039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33040(int& val) {
    func_in_33040(val);
    const int x = 33040;
    std::cout << 33040;
    for (int i = 0; i < 33040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33040(int& val) {
    const int x = 33040;
    std::cout << 33040;
    for (int i = 0; i < 33040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33041(int& val) {
    func_in_33041(val);
    const int x = 33041;
    std::cout << 33041;
    for (int i = 0; i < 33041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33041(int& val) {
    const int x = 33041;
    std::cout << 33041;
    for (int i = 0; i < 33041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33042(int& val) {
    func_in_33042(val);
    const int x = 33042;
    std::cout << 33042;
    for (int i = 0; i < 33042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33042(int& val) {
    const int x = 33042;
    std::cout << 33042;
    for (int i = 0; i < 33042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33043(int& val) {
    func_in_33043(val);
    const int x = 33043;
    std::cout << 33043;
    for (int i = 0; i < 33043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33043(int& val) {
    const int x = 33043;
    std::cout << 33043;
    for (int i = 0; i < 33043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33044(int& val) {
    func_in_33044(val);
    const int x = 33044;
    std::cout << 33044;
    for (int i = 0; i < 33044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33044(int& val) {
    const int x = 33044;
    std::cout << 33044;
    for (int i = 0; i < 33044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33045(int& val) {
    func_in_33045(val);
    const int x = 33045;
    std::cout << 33045;
    for (int i = 0; i < 33045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33045(int& val) {
    const int x = 33045;
    std::cout << 33045;
    for (int i = 0; i < 33045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33046(int& val) {
    func_in_33046(val);
    const int x = 33046;
    std::cout << 33046;
    for (int i = 0; i < 33046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33046(int& val) {
    const int x = 33046;
    std::cout << 33046;
    for (int i = 0; i < 33046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33047(int& val) {
    func_in_33047(val);
    const int x = 33047;
    std::cout << 33047;
    for (int i = 0; i < 33047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33047(int& val) {
    const int x = 33047;
    std::cout << 33047;
    for (int i = 0; i < 33047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33048(int& val) {
    func_in_33048(val);
    const int x = 33048;
    std::cout << 33048;
    for (int i = 0; i < 33048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33048(int& val) {
    const int x = 33048;
    std::cout << 33048;
    for (int i = 0; i < 33048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33049(int& val) {
    func_in_33049(val);
    const int x = 33049;
    std::cout << 33049;
    for (int i = 0; i < 33049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33049(int& val) {
    const int x = 33049;
    std::cout << 33049;
    for (int i = 0; i < 33049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33050(int& val) {
    func_in_33050(val);
    const int x = 33050;
    std::cout << 33050;
    for (int i = 0; i < 33050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33050(int& val) {
    const int x = 33050;
    std::cout << 33050;
    for (int i = 0; i < 33050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33051(int& val) {
    func_in_33051(val);
    const int x = 33051;
    std::cout << 33051;
    for (int i = 0; i < 33051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33051(int& val) {
    const int x = 33051;
    std::cout << 33051;
    for (int i = 0; i < 33051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33052(int& val) {
    func_in_33052(val);
    const int x = 33052;
    std::cout << 33052;
    for (int i = 0; i < 33052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33052(int& val) {
    const int x = 33052;
    std::cout << 33052;
    for (int i = 0; i < 33052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33053(int& val) {
    func_in_33053(val);
    const int x = 33053;
    std::cout << 33053;
    for (int i = 0; i < 33053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33053(int& val) {
    const int x = 33053;
    std::cout << 33053;
    for (int i = 0; i < 33053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33054(int& val) {
    func_in_33054(val);
    const int x = 33054;
    std::cout << 33054;
    for (int i = 0; i < 33054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33054(int& val) {
    const int x = 33054;
    std::cout << 33054;
    for (int i = 0; i < 33054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33055(int& val) {
    func_in_33055(val);
    const int x = 33055;
    std::cout << 33055;
    for (int i = 0; i < 33055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33055(int& val) {
    const int x = 33055;
    std::cout << 33055;
    for (int i = 0; i < 33055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33056(int& val) {
    func_in_33056(val);
    const int x = 33056;
    std::cout << 33056;
    for (int i = 0; i < 33056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33056(int& val) {
    const int x = 33056;
    std::cout << 33056;
    for (int i = 0; i < 33056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33057(int& val) {
    func_in_33057(val);
    const int x = 33057;
    std::cout << 33057;
    for (int i = 0; i < 33057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33057(int& val) {
    const int x = 33057;
    std::cout << 33057;
    for (int i = 0; i < 33057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33058(int& val) {
    func_in_33058(val);
    const int x = 33058;
    std::cout << 33058;
    for (int i = 0; i < 33058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33058(int& val) {
    const int x = 33058;
    std::cout << 33058;
    for (int i = 0; i < 33058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33059(int& val) {
    func_in_33059(val);
    const int x = 33059;
    std::cout << 33059;
    for (int i = 0; i < 33059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33059(int& val) {
    const int x = 33059;
    std::cout << 33059;
    for (int i = 0; i < 33059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33060(int& val) {
    func_in_33060(val);
    const int x = 33060;
    std::cout << 33060;
    for (int i = 0; i < 33060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33060(int& val) {
    const int x = 33060;
    std::cout << 33060;
    for (int i = 0; i < 33060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33061(int& val) {
    func_in_33061(val);
    const int x = 33061;
    std::cout << 33061;
    for (int i = 0; i < 33061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33061(int& val) {
    const int x = 33061;
    std::cout << 33061;
    for (int i = 0; i < 33061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33062(int& val) {
    func_in_33062(val);
    const int x = 33062;
    std::cout << 33062;
    for (int i = 0; i < 33062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33062(int& val) {
    const int x = 33062;
    std::cout << 33062;
    for (int i = 0; i < 33062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33063(int& val) {
    func_in_33063(val);
    const int x = 33063;
    std::cout << 33063;
    for (int i = 0; i < 33063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33063(int& val) {
    const int x = 33063;
    std::cout << 33063;
    for (int i = 0; i < 33063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33064(int& val) {
    func_in_33064(val);
    const int x = 33064;
    std::cout << 33064;
    for (int i = 0; i < 33064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33064(int& val) {
    const int x = 33064;
    std::cout << 33064;
    for (int i = 0; i < 33064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33065(int& val) {
    func_in_33065(val);
    const int x = 33065;
    std::cout << 33065;
    for (int i = 0; i < 33065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33065(int& val) {
    const int x = 33065;
    std::cout << 33065;
    for (int i = 0; i < 33065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33066(int& val) {
    func_in_33066(val);
    const int x = 33066;
    std::cout << 33066;
    for (int i = 0; i < 33066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33066(int& val) {
    const int x = 33066;
    std::cout << 33066;
    for (int i = 0; i < 33066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33067(int& val) {
    func_in_33067(val);
    const int x = 33067;
    std::cout << 33067;
    for (int i = 0; i < 33067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33067(int& val) {
    const int x = 33067;
    std::cout << 33067;
    for (int i = 0; i < 33067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33068(int& val) {
    func_in_33068(val);
    const int x = 33068;
    std::cout << 33068;
    for (int i = 0; i < 33068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33068(int& val) {
    const int x = 33068;
    std::cout << 33068;
    for (int i = 0; i < 33068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33069(int& val) {
    func_in_33069(val);
    const int x = 33069;
    std::cout << 33069;
    for (int i = 0; i < 33069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33069(int& val) {
    const int x = 33069;
    std::cout << 33069;
    for (int i = 0; i < 33069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33070(int& val) {
    func_in_33070(val);
    const int x = 33070;
    std::cout << 33070;
    for (int i = 0; i < 33070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33070(int& val) {
    const int x = 33070;
    std::cout << 33070;
    for (int i = 0; i < 33070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33071(int& val) {
    func_in_33071(val);
    const int x = 33071;
    std::cout << 33071;
    for (int i = 0; i < 33071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33071(int& val) {
    const int x = 33071;
    std::cout << 33071;
    for (int i = 0; i < 33071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33072(int& val) {
    func_in_33072(val);
    const int x = 33072;
    std::cout << 33072;
    for (int i = 0; i < 33072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33072(int& val) {
    const int x = 33072;
    std::cout << 33072;
    for (int i = 0; i < 33072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33073(int& val) {
    func_in_33073(val);
    const int x = 33073;
    std::cout << 33073;
    for (int i = 0; i < 33073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33073(int& val) {
    const int x = 33073;
    std::cout << 33073;
    for (int i = 0; i < 33073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33074(int& val) {
    func_in_33074(val);
    const int x = 33074;
    std::cout << 33074;
    for (int i = 0; i < 33074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33074(int& val) {
    const int x = 33074;
    std::cout << 33074;
    for (int i = 0; i < 33074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33075(int& val) {
    func_in_33075(val);
    const int x = 33075;
    std::cout << 33075;
    for (int i = 0; i < 33075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33075(int& val) {
    const int x = 33075;
    std::cout << 33075;
    for (int i = 0; i < 33075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33076(int& val) {
    func_in_33076(val);
    const int x = 33076;
    std::cout << 33076;
    for (int i = 0; i < 33076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33076(int& val) {
    const int x = 33076;
    std::cout << 33076;
    for (int i = 0; i < 33076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33077(int& val) {
    func_in_33077(val);
    const int x = 33077;
    std::cout << 33077;
    for (int i = 0; i < 33077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33077(int& val) {
    const int x = 33077;
    std::cout << 33077;
    for (int i = 0; i < 33077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33078(int& val) {
    func_in_33078(val);
    const int x = 33078;
    std::cout << 33078;
    for (int i = 0; i < 33078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33078(int& val) {
    const int x = 33078;
    std::cout << 33078;
    for (int i = 0; i < 33078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33079(int& val) {
    func_in_33079(val);
    const int x = 33079;
    std::cout << 33079;
    for (int i = 0; i < 33079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33079(int& val) {
    const int x = 33079;
    std::cout << 33079;
    for (int i = 0; i < 33079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33080(int& val) {
    func_in_33080(val);
    const int x = 33080;
    std::cout << 33080;
    for (int i = 0; i < 33080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33080(int& val) {
    const int x = 33080;
    std::cout << 33080;
    for (int i = 0; i < 33080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33081(int& val) {
    func_in_33081(val);
    const int x = 33081;
    std::cout << 33081;
    for (int i = 0; i < 33081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33081(int& val) {
    const int x = 33081;
    std::cout << 33081;
    for (int i = 0; i < 33081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33082(int& val) {
    func_in_33082(val);
    const int x = 33082;
    std::cout << 33082;
    for (int i = 0; i < 33082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33082(int& val) {
    const int x = 33082;
    std::cout << 33082;
    for (int i = 0; i < 33082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33083(int& val) {
    func_in_33083(val);
    const int x = 33083;
    std::cout << 33083;
    for (int i = 0; i < 33083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33083(int& val) {
    const int x = 33083;
    std::cout << 33083;
    for (int i = 0; i < 33083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33084(int& val) {
    func_in_33084(val);
    const int x = 33084;
    std::cout << 33084;
    for (int i = 0; i < 33084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33084(int& val) {
    const int x = 33084;
    std::cout << 33084;
    for (int i = 0; i < 33084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33085(int& val) {
    func_in_33085(val);
    const int x = 33085;
    std::cout << 33085;
    for (int i = 0; i < 33085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33085(int& val) {
    const int x = 33085;
    std::cout << 33085;
    for (int i = 0; i < 33085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33086(int& val) {
    func_in_33086(val);
    const int x = 33086;
    std::cout << 33086;
    for (int i = 0; i < 33086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33086(int& val) {
    const int x = 33086;
    std::cout << 33086;
    for (int i = 0; i < 33086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33087(int& val) {
    func_in_33087(val);
    const int x = 33087;
    std::cout << 33087;
    for (int i = 0; i < 33087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33087(int& val) {
    const int x = 33087;
    std::cout << 33087;
    for (int i = 0; i < 33087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33088(int& val) {
    func_in_33088(val);
    const int x = 33088;
    std::cout << 33088;
    for (int i = 0; i < 33088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33088(int& val) {
    const int x = 33088;
    std::cout << 33088;
    for (int i = 0; i < 33088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33089(int& val) {
    func_in_33089(val);
    const int x = 33089;
    std::cout << 33089;
    for (int i = 0; i < 33089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33089(int& val) {
    const int x = 33089;
    std::cout << 33089;
    for (int i = 0; i < 33089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33090(int& val) {
    func_in_33090(val);
    const int x = 33090;
    std::cout << 33090;
    for (int i = 0; i < 33090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33090(int& val) {
    const int x = 33090;
    std::cout << 33090;
    for (int i = 0; i < 33090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33091(int& val) {
    func_in_33091(val);
    const int x = 33091;
    std::cout << 33091;
    for (int i = 0; i < 33091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33091(int& val) {
    const int x = 33091;
    std::cout << 33091;
    for (int i = 0; i < 33091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33092(int& val) {
    func_in_33092(val);
    const int x = 33092;
    std::cout << 33092;
    for (int i = 0; i < 33092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33092(int& val) {
    const int x = 33092;
    std::cout << 33092;
    for (int i = 0; i < 33092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33093(int& val) {
    func_in_33093(val);
    const int x = 33093;
    std::cout << 33093;
    for (int i = 0; i < 33093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33093(int& val) {
    const int x = 33093;
    std::cout << 33093;
    for (int i = 0; i < 33093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33094(int& val) {
    func_in_33094(val);
    const int x = 33094;
    std::cout << 33094;
    for (int i = 0; i < 33094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33094(int& val) {
    const int x = 33094;
    std::cout << 33094;
    for (int i = 0; i < 33094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33095(int& val) {
    func_in_33095(val);
    const int x = 33095;
    std::cout << 33095;
    for (int i = 0; i < 33095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33095(int& val) {
    const int x = 33095;
    std::cout << 33095;
    for (int i = 0; i < 33095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33096(int& val) {
    func_in_33096(val);
    const int x = 33096;
    std::cout << 33096;
    for (int i = 0; i < 33096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33096(int& val) {
    const int x = 33096;
    std::cout << 33096;
    for (int i = 0; i < 33096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33097(int& val) {
    func_in_33097(val);
    const int x = 33097;
    std::cout << 33097;
    for (int i = 0; i < 33097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33097(int& val) {
    const int x = 33097;
    std::cout << 33097;
    for (int i = 0; i < 33097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33098(int& val) {
    func_in_33098(val);
    const int x = 33098;
    std::cout << 33098;
    for (int i = 0; i < 33098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33098(int& val) {
    const int x = 33098;
    std::cout << 33098;
    for (int i = 0; i < 33098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33099(int& val) {
    func_in_33099(val);
    const int x = 33099;
    std::cout << 33099;
    for (int i = 0; i < 33099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33099(int& val) {
    const int x = 33099;
    std::cout << 33099;
    for (int i = 0; i < 33099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33100(int& val) {
    func_in_33100(val);
    const int x = 33100;
    std::cout << 33100;
    for (int i = 0; i < 33100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33100(int& val) {
    const int x = 33100;
    std::cout << 33100;
    for (int i = 0; i < 33100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33101(int& val) {
    func_in_33101(val);
    const int x = 33101;
    std::cout << 33101;
    for (int i = 0; i < 33101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33101(int& val) {
    const int x = 33101;
    std::cout << 33101;
    for (int i = 0; i < 33101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33102(int& val) {
    func_in_33102(val);
    const int x = 33102;
    std::cout << 33102;
    for (int i = 0; i < 33102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33102(int& val) {
    const int x = 33102;
    std::cout << 33102;
    for (int i = 0; i < 33102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33103(int& val) {
    func_in_33103(val);
    const int x = 33103;
    std::cout << 33103;
    for (int i = 0; i < 33103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33103(int& val) {
    const int x = 33103;
    std::cout << 33103;
    for (int i = 0; i < 33103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33104(int& val) {
    func_in_33104(val);
    const int x = 33104;
    std::cout << 33104;
    for (int i = 0; i < 33104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33104(int& val) {
    const int x = 33104;
    std::cout << 33104;
    for (int i = 0; i < 33104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33105(int& val) {
    func_in_33105(val);
    const int x = 33105;
    std::cout << 33105;
    for (int i = 0; i < 33105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33105(int& val) {
    const int x = 33105;
    std::cout << 33105;
    for (int i = 0; i < 33105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33106(int& val) {
    func_in_33106(val);
    const int x = 33106;
    std::cout << 33106;
    for (int i = 0; i < 33106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33106(int& val) {
    const int x = 33106;
    std::cout << 33106;
    for (int i = 0; i < 33106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33107(int& val) {
    func_in_33107(val);
    const int x = 33107;
    std::cout << 33107;
    for (int i = 0; i < 33107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33107(int& val) {
    const int x = 33107;
    std::cout << 33107;
    for (int i = 0; i < 33107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33108(int& val) {
    func_in_33108(val);
    const int x = 33108;
    std::cout << 33108;
    for (int i = 0; i < 33108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33108(int& val) {
    const int x = 33108;
    std::cout << 33108;
    for (int i = 0; i < 33108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33109(int& val) {
    func_in_33109(val);
    const int x = 33109;
    std::cout << 33109;
    for (int i = 0; i < 33109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33109(int& val) {
    const int x = 33109;
    std::cout << 33109;
    for (int i = 0; i < 33109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33110(int& val) {
    func_in_33110(val);
    const int x = 33110;
    std::cout << 33110;
    for (int i = 0; i < 33110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33110(int& val) {
    const int x = 33110;
    std::cout << 33110;
    for (int i = 0; i < 33110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33111(int& val) {
    func_in_33111(val);
    const int x = 33111;
    std::cout << 33111;
    for (int i = 0; i < 33111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33111(int& val) {
    const int x = 33111;
    std::cout << 33111;
    for (int i = 0; i < 33111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33112(int& val) {
    func_in_33112(val);
    const int x = 33112;
    std::cout << 33112;
    for (int i = 0; i < 33112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33112(int& val) {
    const int x = 33112;
    std::cout << 33112;
    for (int i = 0; i < 33112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33113(int& val) {
    func_in_33113(val);
    const int x = 33113;
    std::cout << 33113;
    for (int i = 0; i < 33113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33113(int& val) {
    const int x = 33113;
    std::cout << 33113;
    for (int i = 0; i < 33113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33114(int& val) {
    func_in_33114(val);
    const int x = 33114;
    std::cout << 33114;
    for (int i = 0; i < 33114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33114(int& val) {
    const int x = 33114;
    std::cout << 33114;
    for (int i = 0; i < 33114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33115(int& val) {
    func_in_33115(val);
    const int x = 33115;
    std::cout << 33115;
    for (int i = 0; i < 33115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33115(int& val) {
    const int x = 33115;
    std::cout << 33115;
    for (int i = 0; i < 33115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33116(int& val) {
    func_in_33116(val);
    const int x = 33116;
    std::cout << 33116;
    for (int i = 0; i < 33116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33116(int& val) {
    const int x = 33116;
    std::cout << 33116;
    for (int i = 0; i < 33116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33117(int& val) {
    func_in_33117(val);
    const int x = 33117;
    std::cout << 33117;
    for (int i = 0; i < 33117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33117(int& val) {
    const int x = 33117;
    std::cout << 33117;
    for (int i = 0; i < 33117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33118(int& val) {
    func_in_33118(val);
    const int x = 33118;
    std::cout << 33118;
    for (int i = 0; i < 33118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33118(int& val) {
    const int x = 33118;
    std::cout << 33118;
    for (int i = 0; i < 33118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33119(int& val) {
    func_in_33119(val);
    const int x = 33119;
    std::cout << 33119;
    for (int i = 0; i < 33119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33119(int& val) {
    const int x = 33119;
    std::cout << 33119;
    for (int i = 0; i < 33119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33120(int& val) {
    func_in_33120(val);
    const int x = 33120;
    std::cout << 33120;
    for (int i = 0; i < 33120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33120(int& val) {
    const int x = 33120;
    std::cout << 33120;
    for (int i = 0; i < 33120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33121(int& val) {
    func_in_33121(val);
    const int x = 33121;
    std::cout << 33121;
    for (int i = 0; i < 33121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33121(int& val) {
    const int x = 33121;
    std::cout << 33121;
    for (int i = 0; i < 33121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33122(int& val) {
    func_in_33122(val);
    const int x = 33122;
    std::cout << 33122;
    for (int i = 0; i < 33122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33122(int& val) {
    const int x = 33122;
    std::cout << 33122;
    for (int i = 0; i < 33122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33123(int& val) {
    func_in_33123(val);
    const int x = 33123;
    std::cout << 33123;
    for (int i = 0; i < 33123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33123(int& val) {
    const int x = 33123;
    std::cout << 33123;
    for (int i = 0; i < 33123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33124(int& val) {
    func_in_33124(val);
    const int x = 33124;
    std::cout << 33124;
    for (int i = 0; i < 33124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33124(int& val) {
    const int x = 33124;
    std::cout << 33124;
    for (int i = 0; i < 33124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33125(int& val) {
    func_in_33125(val);
    const int x = 33125;
    std::cout << 33125;
    for (int i = 0; i < 33125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33125(int& val) {
    const int x = 33125;
    std::cout << 33125;
    for (int i = 0; i < 33125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33126(int& val) {
    func_in_33126(val);
    const int x = 33126;
    std::cout << 33126;
    for (int i = 0; i < 33126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33126(int& val) {
    const int x = 33126;
    std::cout << 33126;
    for (int i = 0; i < 33126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33127(int& val) {
    func_in_33127(val);
    const int x = 33127;
    std::cout << 33127;
    for (int i = 0; i < 33127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33127(int& val) {
    const int x = 33127;
    std::cout << 33127;
    for (int i = 0; i < 33127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33128(int& val) {
    func_in_33128(val);
    const int x = 33128;
    std::cout << 33128;
    for (int i = 0; i < 33128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33128(int& val) {
    const int x = 33128;
    std::cout << 33128;
    for (int i = 0; i < 33128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33129(int& val) {
    func_in_33129(val);
    const int x = 33129;
    std::cout << 33129;
    for (int i = 0; i < 33129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33129(int& val) {
    const int x = 33129;
    std::cout << 33129;
    for (int i = 0; i < 33129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33130(int& val) {
    func_in_33130(val);
    const int x = 33130;
    std::cout << 33130;
    for (int i = 0; i < 33130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33130(int& val) {
    const int x = 33130;
    std::cout << 33130;
    for (int i = 0; i < 33130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33131(int& val) {
    func_in_33131(val);
    const int x = 33131;
    std::cout << 33131;
    for (int i = 0; i < 33131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33131(int& val) {
    const int x = 33131;
    std::cout << 33131;
    for (int i = 0; i < 33131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33132(int& val) {
    func_in_33132(val);
    const int x = 33132;
    std::cout << 33132;
    for (int i = 0; i < 33132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33132(int& val) {
    const int x = 33132;
    std::cout << 33132;
    for (int i = 0; i < 33132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33133(int& val) {
    func_in_33133(val);
    const int x = 33133;
    std::cout << 33133;
    for (int i = 0; i < 33133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33133(int& val) {
    const int x = 33133;
    std::cout << 33133;
    for (int i = 0; i < 33133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33134(int& val) {
    func_in_33134(val);
    const int x = 33134;
    std::cout << 33134;
    for (int i = 0; i < 33134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33134(int& val) {
    const int x = 33134;
    std::cout << 33134;
    for (int i = 0; i < 33134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33135(int& val) {
    func_in_33135(val);
    const int x = 33135;
    std::cout << 33135;
    for (int i = 0; i < 33135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33135(int& val) {
    const int x = 33135;
    std::cout << 33135;
    for (int i = 0; i < 33135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33136(int& val) {
    func_in_33136(val);
    const int x = 33136;
    std::cout << 33136;
    for (int i = 0; i < 33136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33136(int& val) {
    const int x = 33136;
    std::cout << 33136;
    for (int i = 0; i < 33136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33137(int& val) {
    func_in_33137(val);
    const int x = 33137;
    std::cout << 33137;
    for (int i = 0; i < 33137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33137(int& val) {
    const int x = 33137;
    std::cout << 33137;
    for (int i = 0; i < 33137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33138(int& val) {
    func_in_33138(val);
    const int x = 33138;
    std::cout << 33138;
    for (int i = 0; i < 33138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33138(int& val) {
    const int x = 33138;
    std::cout << 33138;
    for (int i = 0; i < 33138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33139(int& val) {
    func_in_33139(val);
    const int x = 33139;
    std::cout << 33139;
    for (int i = 0; i < 33139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33139(int& val) {
    const int x = 33139;
    std::cout << 33139;
    for (int i = 0; i < 33139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33140(int& val) {
    func_in_33140(val);
    const int x = 33140;
    std::cout << 33140;
    for (int i = 0; i < 33140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33140(int& val) {
    const int x = 33140;
    std::cout << 33140;
    for (int i = 0; i < 33140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33141(int& val) {
    func_in_33141(val);
    const int x = 33141;
    std::cout << 33141;
    for (int i = 0; i < 33141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33141(int& val) {
    const int x = 33141;
    std::cout << 33141;
    for (int i = 0; i < 33141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33142(int& val) {
    func_in_33142(val);
    const int x = 33142;
    std::cout << 33142;
    for (int i = 0; i < 33142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33142(int& val) {
    const int x = 33142;
    std::cout << 33142;
    for (int i = 0; i < 33142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33143(int& val) {
    func_in_33143(val);
    const int x = 33143;
    std::cout << 33143;
    for (int i = 0; i < 33143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33143(int& val) {
    const int x = 33143;
    std::cout << 33143;
    for (int i = 0; i < 33143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33144(int& val) {
    func_in_33144(val);
    const int x = 33144;
    std::cout << 33144;
    for (int i = 0; i < 33144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33144(int& val) {
    const int x = 33144;
    std::cout << 33144;
    for (int i = 0; i < 33144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33145(int& val) {
    func_in_33145(val);
    const int x = 33145;
    std::cout << 33145;
    for (int i = 0; i < 33145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33145(int& val) {
    const int x = 33145;
    std::cout << 33145;
    for (int i = 0; i < 33145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33146(int& val) {
    func_in_33146(val);
    const int x = 33146;
    std::cout << 33146;
    for (int i = 0; i < 33146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33146(int& val) {
    const int x = 33146;
    std::cout << 33146;
    for (int i = 0; i < 33146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33147(int& val) {
    func_in_33147(val);
    const int x = 33147;
    std::cout << 33147;
    for (int i = 0; i < 33147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33147(int& val) {
    const int x = 33147;
    std::cout << 33147;
    for (int i = 0; i < 33147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33148(int& val) {
    func_in_33148(val);
    const int x = 33148;
    std::cout << 33148;
    for (int i = 0; i < 33148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33148(int& val) {
    const int x = 33148;
    std::cout << 33148;
    for (int i = 0; i < 33148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33149(int& val) {
    func_in_33149(val);
    const int x = 33149;
    std::cout << 33149;
    for (int i = 0; i < 33149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33149(int& val) {
    const int x = 33149;
    std::cout << 33149;
    for (int i = 0; i < 33149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33150(int& val) {
    func_in_33150(val);
    const int x = 33150;
    std::cout << 33150;
    for (int i = 0; i < 33150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33150(int& val) {
    const int x = 33150;
    std::cout << 33150;
    for (int i = 0; i < 33150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33151(int& val) {
    func_in_33151(val);
    const int x = 33151;
    std::cout << 33151;
    for (int i = 0; i < 33151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33151(int& val) {
    const int x = 33151;
    std::cout << 33151;
    for (int i = 0; i < 33151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33152(int& val) {
    func_in_33152(val);
    const int x = 33152;
    std::cout << 33152;
    for (int i = 0; i < 33152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33152(int& val) {
    const int x = 33152;
    std::cout << 33152;
    for (int i = 0; i < 33152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33153(int& val) {
    func_in_33153(val);
    const int x = 33153;
    std::cout << 33153;
    for (int i = 0; i < 33153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33153(int& val) {
    const int x = 33153;
    std::cout << 33153;
    for (int i = 0; i < 33153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33154(int& val) {
    func_in_33154(val);
    const int x = 33154;
    std::cout << 33154;
    for (int i = 0; i < 33154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33154(int& val) {
    const int x = 33154;
    std::cout << 33154;
    for (int i = 0; i < 33154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33155(int& val) {
    func_in_33155(val);
    const int x = 33155;
    std::cout << 33155;
    for (int i = 0; i < 33155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33155(int& val) {
    const int x = 33155;
    std::cout << 33155;
    for (int i = 0; i < 33155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33156(int& val) {
    func_in_33156(val);
    const int x = 33156;
    std::cout << 33156;
    for (int i = 0; i < 33156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33156(int& val) {
    const int x = 33156;
    std::cout << 33156;
    for (int i = 0; i < 33156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33157(int& val) {
    func_in_33157(val);
    const int x = 33157;
    std::cout << 33157;
    for (int i = 0; i < 33157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33157(int& val) {
    const int x = 33157;
    std::cout << 33157;
    for (int i = 0; i < 33157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33158(int& val) {
    func_in_33158(val);
    const int x = 33158;
    std::cout << 33158;
    for (int i = 0; i < 33158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33158(int& val) {
    const int x = 33158;
    std::cout << 33158;
    for (int i = 0; i < 33158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33159(int& val) {
    func_in_33159(val);
    const int x = 33159;
    std::cout << 33159;
    for (int i = 0; i < 33159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33159(int& val) {
    const int x = 33159;
    std::cout << 33159;
    for (int i = 0; i < 33159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33160(int& val) {
    func_in_33160(val);
    const int x = 33160;
    std::cout << 33160;
    for (int i = 0; i < 33160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33160(int& val) {
    const int x = 33160;
    std::cout << 33160;
    for (int i = 0; i < 33160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33161(int& val) {
    func_in_33161(val);
    const int x = 33161;
    std::cout << 33161;
    for (int i = 0; i < 33161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33161(int& val) {
    const int x = 33161;
    std::cout << 33161;
    for (int i = 0; i < 33161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33162(int& val) {
    func_in_33162(val);
    const int x = 33162;
    std::cout << 33162;
    for (int i = 0; i < 33162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33162(int& val) {
    const int x = 33162;
    std::cout << 33162;
    for (int i = 0; i < 33162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33163(int& val) {
    func_in_33163(val);
    const int x = 33163;
    std::cout << 33163;
    for (int i = 0; i < 33163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33163(int& val) {
    const int x = 33163;
    std::cout << 33163;
    for (int i = 0; i < 33163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33164(int& val) {
    func_in_33164(val);
    const int x = 33164;
    std::cout << 33164;
    for (int i = 0; i < 33164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33164(int& val) {
    const int x = 33164;
    std::cout << 33164;
    for (int i = 0; i < 33164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33165(int& val) {
    func_in_33165(val);
    const int x = 33165;
    std::cout << 33165;
    for (int i = 0; i < 33165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33165(int& val) {
    const int x = 33165;
    std::cout << 33165;
    for (int i = 0; i < 33165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33166(int& val) {
    func_in_33166(val);
    const int x = 33166;
    std::cout << 33166;
    for (int i = 0; i < 33166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33166(int& val) {
    const int x = 33166;
    std::cout << 33166;
    for (int i = 0; i < 33166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33167(int& val) {
    func_in_33167(val);
    const int x = 33167;
    std::cout << 33167;
    for (int i = 0; i < 33167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33167(int& val) {
    const int x = 33167;
    std::cout << 33167;
    for (int i = 0; i < 33167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33168(int& val) {
    func_in_33168(val);
    const int x = 33168;
    std::cout << 33168;
    for (int i = 0; i < 33168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33168(int& val) {
    const int x = 33168;
    std::cout << 33168;
    for (int i = 0; i < 33168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33169(int& val) {
    func_in_33169(val);
    const int x = 33169;
    std::cout << 33169;
    for (int i = 0; i < 33169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33169(int& val) {
    const int x = 33169;
    std::cout << 33169;
    for (int i = 0; i < 33169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33170(int& val) {
    func_in_33170(val);
    const int x = 33170;
    std::cout << 33170;
    for (int i = 0; i < 33170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33170(int& val) {
    const int x = 33170;
    std::cout << 33170;
    for (int i = 0; i < 33170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33171(int& val) {
    func_in_33171(val);
    const int x = 33171;
    std::cout << 33171;
    for (int i = 0; i < 33171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33171(int& val) {
    const int x = 33171;
    std::cout << 33171;
    for (int i = 0; i < 33171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33172(int& val) {
    func_in_33172(val);
    const int x = 33172;
    std::cout << 33172;
    for (int i = 0; i < 33172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33172(int& val) {
    const int x = 33172;
    std::cout << 33172;
    for (int i = 0; i < 33172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33173(int& val) {
    func_in_33173(val);
    const int x = 33173;
    std::cout << 33173;
    for (int i = 0; i < 33173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33173(int& val) {
    const int x = 33173;
    std::cout << 33173;
    for (int i = 0; i < 33173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33174(int& val) {
    func_in_33174(val);
    const int x = 33174;
    std::cout << 33174;
    for (int i = 0; i < 33174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33174(int& val) {
    const int x = 33174;
    std::cout << 33174;
    for (int i = 0; i < 33174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33175(int& val) {
    func_in_33175(val);
    const int x = 33175;
    std::cout << 33175;
    for (int i = 0; i < 33175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33175(int& val) {
    const int x = 33175;
    std::cout << 33175;
    for (int i = 0; i < 33175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33176(int& val) {
    func_in_33176(val);
    const int x = 33176;
    std::cout << 33176;
    for (int i = 0; i < 33176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33176(int& val) {
    const int x = 33176;
    std::cout << 33176;
    for (int i = 0; i < 33176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33177(int& val) {
    func_in_33177(val);
    const int x = 33177;
    std::cout << 33177;
    for (int i = 0; i < 33177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33177(int& val) {
    const int x = 33177;
    std::cout << 33177;
    for (int i = 0; i < 33177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33178(int& val) {
    func_in_33178(val);
    const int x = 33178;
    std::cout << 33178;
    for (int i = 0; i < 33178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33178(int& val) {
    const int x = 33178;
    std::cout << 33178;
    for (int i = 0; i < 33178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33179(int& val) {
    func_in_33179(val);
    const int x = 33179;
    std::cout << 33179;
    for (int i = 0; i < 33179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33179(int& val) {
    const int x = 33179;
    std::cout << 33179;
    for (int i = 0; i < 33179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33180(int& val) {
    func_in_33180(val);
    const int x = 33180;
    std::cout << 33180;
    for (int i = 0; i < 33180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33180(int& val) {
    const int x = 33180;
    std::cout << 33180;
    for (int i = 0; i < 33180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33181(int& val) {
    func_in_33181(val);
    const int x = 33181;
    std::cout << 33181;
    for (int i = 0; i < 33181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33181(int& val) {
    const int x = 33181;
    std::cout << 33181;
    for (int i = 0; i < 33181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33182(int& val) {
    func_in_33182(val);
    const int x = 33182;
    std::cout << 33182;
    for (int i = 0; i < 33182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33182(int& val) {
    const int x = 33182;
    std::cout << 33182;
    for (int i = 0; i < 33182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33183(int& val) {
    func_in_33183(val);
    const int x = 33183;
    std::cout << 33183;
    for (int i = 0; i < 33183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33183(int& val) {
    const int x = 33183;
    std::cout << 33183;
    for (int i = 0; i < 33183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33184(int& val) {
    func_in_33184(val);
    const int x = 33184;
    std::cout << 33184;
    for (int i = 0; i < 33184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33184(int& val) {
    const int x = 33184;
    std::cout << 33184;
    for (int i = 0; i < 33184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33185(int& val) {
    func_in_33185(val);
    const int x = 33185;
    std::cout << 33185;
    for (int i = 0; i < 33185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33185(int& val) {
    const int x = 33185;
    std::cout << 33185;
    for (int i = 0; i < 33185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33186(int& val) {
    func_in_33186(val);
    const int x = 33186;
    std::cout << 33186;
    for (int i = 0; i < 33186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33186(int& val) {
    const int x = 33186;
    std::cout << 33186;
    for (int i = 0; i < 33186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33187(int& val) {
    func_in_33187(val);
    const int x = 33187;
    std::cout << 33187;
    for (int i = 0; i < 33187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33187(int& val) {
    const int x = 33187;
    std::cout << 33187;
    for (int i = 0; i < 33187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33188(int& val) {
    func_in_33188(val);
    const int x = 33188;
    std::cout << 33188;
    for (int i = 0; i < 33188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33188(int& val) {
    const int x = 33188;
    std::cout << 33188;
    for (int i = 0; i < 33188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33189(int& val) {
    func_in_33189(val);
    const int x = 33189;
    std::cout << 33189;
    for (int i = 0; i < 33189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33189(int& val) {
    const int x = 33189;
    std::cout << 33189;
    for (int i = 0; i < 33189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33190(int& val) {
    func_in_33190(val);
    const int x = 33190;
    std::cout << 33190;
    for (int i = 0; i < 33190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33190(int& val) {
    const int x = 33190;
    std::cout << 33190;
    for (int i = 0; i < 33190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33191(int& val) {
    func_in_33191(val);
    const int x = 33191;
    std::cout << 33191;
    for (int i = 0; i < 33191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33191(int& val) {
    const int x = 33191;
    std::cout << 33191;
    for (int i = 0; i < 33191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33192(int& val) {
    func_in_33192(val);
    const int x = 33192;
    std::cout << 33192;
    for (int i = 0; i < 33192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33192(int& val) {
    const int x = 33192;
    std::cout << 33192;
    for (int i = 0; i < 33192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33193(int& val) {
    func_in_33193(val);
    const int x = 33193;
    std::cout << 33193;
    for (int i = 0; i < 33193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33193(int& val) {
    const int x = 33193;
    std::cout << 33193;
    for (int i = 0; i < 33193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33194(int& val) {
    func_in_33194(val);
    const int x = 33194;
    std::cout << 33194;
    for (int i = 0; i < 33194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33194(int& val) {
    const int x = 33194;
    std::cout << 33194;
    for (int i = 0; i < 33194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33195(int& val) {
    func_in_33195(val);
    const int x = 33195;
    std::cout << 33195;
    for (int i = 0; i < 33195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33195(int& val) {
    const int x = 33195;
    std::cout << 33195;
    for (int i = 0; i < 33195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33196(int& val) {
    func_in_33196(val);
    const int x = 33196;
    std::cout << 33196;
    for (int i = 0; i < 33196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33196(int& val) {
    const int x = 33196;
    std::cout << 33196;
    for (int i = 0; i < 33196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33197(int& val) {
    func_in_33197(val);
    const int x = 33197;
    std::cout << 33197;
    for (int i = 0; i < 33197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33197(int& val) {
    const int x = 33197;
    std::cout << 33197;
    for (int i = 0; i < 33197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33198(int& val) {
    func_in_33198(val);
    const int x = 33198;
    std::cout << 33198;
    for (int i = 0; i < 33198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33198(int& val) {
    const int x = 33198;
    std::cout << 33198;
    for (int i = 0; i < 33198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33199(int& val) {
    func_in_33199(val);
    const int x = 33199;
    std::cout << 33199;
    for (int i = 0; i < 33199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33199(int& val) {
    const int x = 33199;
    std::cout << 33199;
    for (int i = 0; i < 33199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33200(int& val) {
    func_in_33200(val);
    const int x = 33200;
    std::cout << 33200;
    for (int i = 0; i < 33200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33200(int& val) {
    const int x = 33200;
    std::cout << 33200;
    for (int i = 0; i < 33200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33201(int& val) {
    func_in_33201(val);
    const int x = 33201;
    std::cout << 33201;
    for (int i = 0; i < 33201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33201(int& val) {
    const int x = 33201;
    std::cout << 33201;
    for (int i = 0; i < 33201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33202(int& val) {
    func_in_33202(val);
    const int x = 33202;
    std::cout << 33202;
    for (int i = 0; i < 33202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33202(int& val) {
    const int x = 33202;
    std::cout << 33202;
    for (int i = 0; i < 33202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33203(int& val) {
    func_in_33203(val);
    const int x = 33203;
    std::cout << 33203;
    for (int i = 0; i < 33203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33203(int& val) {
    const int x = 33203;
    std::cout << 33203;
    for (int i = 0; i < 33203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33204(int& val) {
    func_in_33204(val);
    const int x = 33204;
    std::cout << 33204;
    for (int i = 0; i < 33204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33204(int& val) {
    const int x = 33204;
    std::cout << 33204;
    for (int i = 0; i < 33204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33205(int& val) {
    func_in_33205(val);
    const int x = 33205;
    std::cout << 33205;
    for (int i = 0; i < 33205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33205(int& val) {
    const int x = 33205;
    std::cout << 33205;
    for (int i = 0; i < 33205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33206(int& val) {
    func_in_33206(val);
    const int x = 33206;
    std::cout << 33206;
    for (int i = 0; i < 33206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33206(int& val) {
    const int x = 33206;
    std::cout << 33206;
    for (int i = 0; i < 33206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33207(int& val) {
    func_in_33207(val);
    const int x = 33207;
    std::cout << 33207;
    for (int i = 0; i < 33207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33207(int& val) {
    const int x = 33207;
    std::cout << 33207;
    for (int i = 0; i < 33207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33208(int& val) {
    func_in_33208(val);
    const int x = 33208;
    std::cout << 33208;
    for (int i = 0; i < 33208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33208(int& val) {
    const int x = 33208;
    std::cout << 33208;
    for (int i = 0; i < 33208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33209(int& val) {
    func_in_33209(val);
    const int x = 33209;
    std::cout << 33209;
    for (int i = 0; i < 33209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33209(int& val) {
    const int x = 33209;
    std::cout << 33209;
    for (int i = 0; i < 33209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33210(int& val) {
    func_in_33210(val);
    const int x = 33210;
    std::cout << 33210;
    for (int i = 0; i < 33210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33210(int& val) {
    const int x = 33210;
    std::cout << 33210;
    for (int i = 0; i < 33210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33211(int& val) {
    func_in_33211(val);
    const int x = 33211;
    std::cout << 33211;
    for (int i = 0; i < 33211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33211(int& val) {
    const int x = 33211;
    std::cout << 33211;
    for (int i = 0; i < 33211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33212(int& val) {
    func_in_33212(val);
    const int x = 33212;
    std::cout << 33212;
    for (int i = 0; i < 33212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33212(int& val) {
    const int x = 33212;
    std::cout << 33212;
    for (int i = 0; i < 33212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33213(int& val) {
    func_in_33213(val);
    const int x = 33213;
    std::cout << 33213;
    for (int i = 0; i < 33213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33213(int& val) {
    const int x = 33213;
    std::cout << 33213;
    for (int i = 0; i < 33213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33214(int& val) {
    func_in_33214(val);
    const int x = 33214;
    std::cout << 33214;
    for (int i = 0; i < 33214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33214(int& val) {
    const int x = 33214;
    std::cout << 33214;
    for (int i = 0; i < 33214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33215(int& val) {
    func_in_33215(val);
    const int x = 33215;
    std::cout << 33215;
    for (int i = 0; i < 33215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33215(int& val) {
    const int x = 33215;
    std::cout << 33215;
    for (int i = 0; i < 33215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33216(int& val) {
    func_in_33216(val);
    const int x = 33216;
    std::cout << 33216;
    for (int i = 0; i < 33216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33216(int& val) {
    const int x = 33216;
    std::cout << 33216;
    for (int i = 0; i < 33216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33217(int& val) {
    func_in_33217(val);
    const int x = 33217;
    std::cout << 33217;
    for (int i = 0; i < 33217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33217(int& val) {
    const int x = 33217;
    std::cout << 33217;
    for (int i = 0; i < 33217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33218(int& val) {
    func_in_33218(val);
    const int x = 33218;
    std::cout << 33218;
    for (int i = 0; i < 33218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33218(int& val) {
    const int x = 33218;
    std::cout << 33218;
    for (int i = 0; i < 33218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33219(int& val) {
    func_in_33219(val);
    const int x = 33219;
    std::cout << 33219;
    for (int i = 0; i < 33219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33219(int& val) {
    const int x = 33219;
    std::cout << 33219;
    for (int i = 0; i < 33219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33220(int& val) {
    func_in_33220(val);
    const int x = 33220;
    std::cout << 33220;
    for (int i = 0; i < 33220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33220(int& val) {
    const int x = 33220;
    std::cout << 33220;
    for (int i = 0; i < 33220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33221(int& val) {
    func_in_33221(val);
    const int x = 33221;
    std::cout << 33221;
    for (int i = 0; i < 33221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33221(int& val) {
    const int x = 33221;
    std::cout << 33221;
    for (int i = 0; i < 33221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33222(int& val) {
    func_in_33222(val);
    const int x = 33222;
    std::cout << 33222;
    for (int i = 0; i < 33222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33222(int& val) {
    const int x = 33222;
    std::cout << 33222;
    for (int i = 0; i < 33222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33223(int& val) {
    func_in_33223(val);
    const int x = 33223;
    std::cout << 33223;
    for (int i = 0; i < 33223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33223(int& val) {
    const int x = 33223;
    std::cout << 33223;
    for (int i = 0; i < 33223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33224(int& val) {
    func_in_33224(val);
    const int x = 33224;
    std::cout << 33224;
    for (int i = 0; i < 33224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33224(int& val) {
    const int x = 33224;
    std::cout << 33224;
    for (int i = 0; i < 33224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33225(int& val) {
    func_in_33225(val);
    const int x = 33225;
    std::cout << 33225;
    for (int i = 0; i < 33225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33225(int& val) {
    const int x = 33225;
    std::cout << 33225;
    for (int i = 0; i < 33225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33226(int& val) {
    func_in_33226(val);
    const int x = 33226;
    std::cout << 33226;
    for (int i = 0; i < 33226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33226(int& val) {
    const int x = 33226;
    std::cout << 33226;
    for (int i = 0; i < 33226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33227(int& val) {
    func_in_33227(val);
    const int x = 33227;
    std::cout << 33227;
    for (int i = 0; i < 33227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33227(int& val) {
    const int x = 33227;
    std::cout << 33227;
    for (int i = 0; i < 33227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33228(int& val) {
    func_in_33228(val);
    const int x = 33228;
    std::cout << 33228;
    for (int i = 0; i < 33228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33228(int& val) {
    const int x = 33228;
    std::cout << 33228;
    for (int i = 0; i < 33228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33229(int& val) {
    func_in_33229(val);
    const int x = 33229;
    std::cout << 33229;
    for (int i = 0; i < 33229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33229(int& val) {
    const int x = 33229;
    std::cout << 33229;
    for (int i = 0; i < 33229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33230(int& val) {
    func_in_33230(val);
    const int x = 33230;
    std::cout << 33230;
    for (int i = 0; i < 33230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33230(int& val) {
    const int x = 33230;
    std::cout << 33230;
    for (int i = 0; i < 33230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33231(int& val) {
    func_in_33231(val);
    const int x = 33231;
    std::cout << 33231;
    for (int i = 0; i < 33231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33231(int& val) {
    const int x = 33231;
    std::cout << 33231;
    for (int i = 0; i < 33231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33232(int& val) {
    func_in_33232(val);
    const int x = 33232;
    std::cout << 33232;
    for (int i = 0; i < 33232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33232(int& val) {
    const int x = 33232;
    std::cout << 33232;
    for (int i = 0; i < 33232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33233(int& val) {
    func_in_33233(val);
    const int x = 33233;
    std::cout << 33233;
    for (int i = 0; i < 33233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33233(int& val) {
    const int x = 33233;
    std::cout << 33233;
    for (int i = 0; i < 33233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33234(int& val) {
    func_in_33234(val);
    const int x = 33234;
    std::cout << 33234;
    for (int i = 0; i < 33234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33234(int& val) {
    const int x = 33234;
    std::cout << 33234;
    for (int i = 0; i < 33234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33235(int& val) {
    func_in_33235(val);
    const int x = 33235;
    std::cout << 33235;
    for (int i = 0; i < 33235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33235(int& val) {
    const int x = 33235;
    std::cout << 33235;
    for (int i = 0; i < 33235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33236(int& val) {
    func_in_33236(val);
    const int x = 33236;
    std::cout << 33236;
    for (int i = 0; i < 33236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33236(int& val) {
    const int x = 33236;
    std::cout << 33236;
    for (int i = 0; i < 33236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33237(int& val) {
    func_in_33237(val);
    const int x = 33237;
    std::cout << 33237;
    for (int i = 0; i < 33237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33237(int& val) {
    const int x = 33237;
    std::cout << 33237;
    for (int i = 0; i < 33237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33238(int& val) {
    func_in_33238(val);
    const int x = 33238;
    std::cout << 33238;
    for (int i = 0; i < 33238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33238(int& val) {
    const int x = 33238;
    std::cout << 33238;
    for (int i = 0; i < 33238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33239(int& val) {
    func_in_33239(val);
    const int x = 33239;
    std::cout << 33239;
    for (int i = 0; i < 33239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33239(int& val) {
    const int x = 33239;
    std::cout << 33239;
    for (int i = 0; i < 33239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33240(int& val) {
    func_in_33240(val);
    const int x = 33240;
    std::cout << 33240;
    for (int i = 0; i < 33240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33240(int& val) {
    const int x = 33240;
    std::cout << 33240;
    for (int i = 0; i < 33240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33241(int& val) {
    func_in_33241(val);
    const int x = 33241;
    std::cout << 33241;
    for (int i = 0; i < 33241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33241(int& val) {
    const int x = 33241;
    std::cout << 33241;
    for (int i = 0; i < 33241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33242(int& val) {
    func_in_33242(val);
    const int x = 33242;
    std::cout << 33242;
    for (int i = 0; i < 33242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33242(int& val) {
    const int x = 33242;
    std::cout << 33242;
    for (int i = 0; i < 33242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33243(int& val) {
    func_in_33243(val);
    const int x = 33243;
    std::cout << 33243;
    for (int i = 0; i < 33243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33243(int& val) {
    const int x = 33243;
    std::cout << 33243;
    for (int i = 0; i < 33243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33244(int& val) {
    func_in_33244(val);
    const int x = 33244;
    std::cout << 33244;
    for (int i = 0; i < 33244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33244(int& val) {
    const int x = 33244;
    std::cout << 33244;
    for (int i = 0; i < 33244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33245(int& val) {
    func_in_33245(val);
    const int x = 33245;
    std::cout << 33245;
    for (int i = 0; i < 33245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33245(int& val) {
    const int x = 33245;
    std::cout << 33245;
    for (int i = 0; i < 33245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33246(int& val) {
    func_in_33246(val);
    const int x = 33246;
    std::cout << 33246;
    for (int i = 0; i < 33246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33246(int& val) {
    const int x = 33246;
    std::cout << 33246;
    for (int i = 0; i < 33246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33247(int& val) {
    func_in_33247(val);
    const int x = 33247;
    std::cout << 33247;
    for (int i = 0; i < 33247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33247(int& val) {
    const int x = 33247;
    std::cout << 33247;
    for (int i = 0; i < 33247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33248(int& val) {
    func_in_33248(val);
    const int x = 33248;
    std::cout << 33248;
    for (int i = 0; i < 33248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33248(int& val) {
    const int x = 33248;
    std::cout << 33248;
    for (int i = 0; i < 33248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33249(int& val) {
    func_in_33249(val);
    const int x = 33249;
    std::cout << 33249;
    for (int i = 0; i < 33249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33249(int& val) {
    const int x = 33249;
    std::cout << 33249;
    for (int i = 0; i < 33249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33250(int& val) {
    func_in_33250(val);
    const int x = 33250;
    std::cout << 33250;
    for (int i = 0; i < 33250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33250(int& val) {
    const int x = 33250;
    std::cout << 33250;
    for (int i = 0; i < 33250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33251(int& val) {
    func_in_33251(val);
    const int x = 33251;
    std::cout << 33251;
    for (int i = 0; i < 33251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33251(int& val) {
    const int x = 33251;
    std::cout << 33251;
    for (int i = 0; i < 33251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33252(int& val) {
    func_in_33252(val);
    const int x = 33252;
    std::cout << 33252;
    for (int i = 0; i < 33252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33252(int& val) {
    const int x = 33252;
    std::cout << 33252;
    for (int i = 0; i < 33252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33253(int& val) {
    func_in_33253(val);
    const int x = 33253;
    std::cout << 33253;
    for (int i = 0; i < 33253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33253(int& val) {
    const int x = 33253;
    std::cout << 33253;
    for (int i = 0; i < 33253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33254(int& val) {
    func_in_33254(val);
    const int x = 33254;
    std::cout << 33254;
    for (int i = 0; i < 33254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33254(int& val) {
    const int x = 33254;
    std::cout << 33254;
    for (int i = 0; i < 33254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33255(int& val) {
    func_in_33255(val);
    const int x = 33255;
    std::cout << 33255;
    for (int i = 0; i < 33255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33255(int& val) {
    const int x = 33255;
    std::cout << 33255;
    for (int i = 0; i < 33255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33256(int& val) {
    func_in_33256(val);
    const int x = 33256;
    std::cout << 33256;
    for (int i = 0; i < 33256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33256(int& val) {
    const int x = 33256;
    std::cout << 33256;
    for (int i = 0; i < 33256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33257(int& val) {
    func_in_33257(val);
    const int x = 33257;
    std::cout << 33257;
    for (int i = 0; i < 33257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33257(int& val) {
    const int x = 33257;
    std::cout << 33257;
    for (int i = 0; i < 33257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33258(int& val) {
    func_in_33258(val);
    const int x = 33258;
    std::cout << 33258;
    for (int i = 0; i < 33258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33258(int& val) {
    const int x = 33258;
    std::cout << 33258;
    for (int i = 0; i < 33258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33259(int& val) {
    func_in_33259(val);
    const int x = 33259;
    std::cout << 33259;
    for (int i = 0; i < 33259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33259(int& val) {
    const int x = 33259;
    std::cout << 33259;
    for (int i = 0; i < 33259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33260(int& val) {
    func_in_33260(val);
    const int x = 33260;
    std::cout << 33260;
    for (int i = 0; i < 33260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33260(int& val) {
    const int x = 33260;
    std::cout << 33260;
    for (int i = 0; i < 33260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33261(int& val) {
    func_in_33261(val);
    const int x = 33261;
    std::cout << 33261;
    for (int i = 0; i < 33261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33261(int& val) {
    const int x = 33261;
    std::cout << 33261;
    for (int i = 0; i < 33261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33262(int& val) {
    func_in_33262(val);
    const int x = 33262;
    std::cout << 33262;
    for (int i = 0; i < 33262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33262(int& val) {
    const int x = 33262;
    std::cout << 33262;
    for (int i = 0; i < 33262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33263(int& val) {
    func_in_33263(val);
    const int x = 33263;
    std::cout << 33263;
    for (int i = 0; i < 33263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33263(int& val) {
    const int x = 33263;
    std::cout << 33263;
    for (int i = 0; i < 33263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33264(int& val) {
    func_in_33264(val);
    const int x = 33264;
    std::cout << 33264;
    for (int i = 0; i < 33264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33264(int& val) {
    const int x = 33264;
    std::cout << 33264;
    for (int i = 0; i < 33264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33265(int& val) {
    func_in_33265(val);
    const int x = 33265;
    std::cout << 33265;
    for (int i = 0; i < 33265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33265(int& val) {
    const int x = 33265;
    std::cout << 33265;
    for (int i = 0; i < 33265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33266(int& val) {
    func_in_33266(val);
    const int x = 33266;
    std::cout << 33266;
    for (int i = 0; i < 33266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33266(int& val) {
    const int x = 33266;
    std::cout << 33266;
    for (int i = 0; i < 33266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33267(int& val) {
    func_in_33267(val);
    const int x = 33267;
    std::cout << 33267;
    for (int i = 0; i < 33267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33267(int& val) {
    const int x = 33267;
    std::cout << 33267;
    for (int i = 0; i < 33267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33268(int& val) {
    func_in_33268(val);
    const int x = 33268;
    std::cout << 33268;
    for (int i = 0; i < 33268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33268(int& val) {
    const int x = 33268;
    std::cout << 33268;
    for (int i = 0; i < 33268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33269(int& val) {
    func_in_33269(val);
    const int x = 33269;
    std::cout << 33269;
    for (int i = 0; i < 33269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33269(int& val) {
    const int x = 33269;
    std::cout << 33269;
    for (int i = 0; i < 33269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33270(int& val) {
    func_in_33270(val);
    const int x = 33270;
    std::cout << 33270;
    for (int i = 0; i < 33270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33270(int& val) {
    const int x = 33270;
    std::cout << 33270;
    for (int i = 0; i < 33270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33271(int& val) {
    func_in_33271(val);
    const int x = 33271;
    std::cout << 33271;
    for (int i = 0; i < 33271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33271(int& val) {
    const int x = 33271;
    std::cout << 33271;
    for (int i = 0; i < 33271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33272(int& val) {
    func_in_33272(val);
    const int x = 33272;
    std::cout << 33272;
    for (int i = 0; i < 33272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33272(int& val) {
    const int x = 33272;
    std::cout << 33272;
    for (int i = 0; i < 33272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33273(int& val) {
    func_in_33273(val);
    const int x = 33273;
    std::cout << 33273;
    for (int i = 0; i < 33273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33273(int& val) {
    const int x = 33273;
    std::cout << 33273;
    for (int i = 0; i < 33273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33274(int& val) {
    func_in_33274(val);
    const int x = 33274;
    std::cout << 33274;
    for (int i = 0; i < 33274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33274(int& val) {
    const int x = 33274;
    std::cout << 33274;
    for (int i = 0; i < 33274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33275(int& val) {
    func_in_33275(val);
    const int x = 33275;
    std::cout << 33275;
    for (int i = 0; i < 33275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33275(int& val) {
    const int x = 33275;
    std::cout << 33275;
    for (int i = 0; i < 33275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33276(int& val) {
    func_in_33276(val);
    const int x = 33276;
    std::cout << 33276;
    for (int i = 0; i < 33276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33276(int& val) {
    const int x = 33276;
    std::cout << 33276;
    for (int i = 0; i < 33276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33277(int& val) {
    func_in_33277(val);
    const int x = 33277;
    std::cout << 33277;
    for (int i = 0; i < 33277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33277(int& val) {
    const int x = 33277;
    std::cout << 33277;
    for (int i = 0; i < 33277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33278(int& val) {
    func_in_33278(val);
    const int x = 33278;
    std::cout << 33278;
    for (int i = 0; i < 33278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33278(int& val) {
    const int x = 33278;
    std::cout << 33278;
    for (int i = 0; i < 33278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33279(int& val) {
    func_in_33279(val);
    const int x = 33279;
    std::cout << 33279;
    for (int i = 0; i < 33279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33279(int& val) {
    const int x = 33279;
    std::cout << 33279;
    for (int i = 0; i < 33279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33280(int& val) {
    func_in_33280(val);
    const int x = 33280;
    std::cout << 33280;
    for (int i = 0; i < 33280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33280(int& val) {
    const int x = 33280;
    std::cout << 33280;
    for (int i = 0; i < 33280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33281(int& val) {
    func_in_33281(val);
    const int x = 33281;
    std::cout << 33281;
    for (int i = 0; i < 33281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33281(int& val) {
    const int x = 33281;
    std::cout << 33281;
    for (int i = 0; i < 33281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33282(int& val) {
    func_in_33282(val);
    const int x = 33282;
    std::cout << 33282;
    for (int i = 0; i < 33282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33282(int& val) {
    const int x = 33282;
    std::cout << 33282;
    for (int i = 0; i < 33282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33283(int& val) {
    func_in_33283(val);
    const int x = 33283;
    std::cout << 33283;
    for (int i = 0; i < 33283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33283(int& val) {
    const int x = 33283;
    std::cout << 33283;
    for (int i = 0; i < 33283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33284(int& val) {
    func_in_33284(val);
    const int x = 33284;
    std::cout << 33284;
    for (int i = 0; i < 33284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33284(int& val) {
    const int x = 33284;
    std::cout << 33284;
    for (int i = 0; i < 33284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33285(int& val) {
    func_in_33285(val);
    const int x = 33285;
    std::cout << 33285;
    for (int i = 0; i < 33285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33285(int& val) {
    const int x = 33285;
    std::cout << 33285;
    for (int i = 0; i < 33285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33286(int& val) {
    func_in_33286(val);
    const int x = 33286;
    std::cout << 33286;
    for (int i = 0; i < 33286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33286(int& val) {
    const int x = 33286;
    std::cout << 33286;
    for (int i = 0; i < 33286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33287(int& val) {
    func_in_33287(val);
    const int x = 33287;
    std::cout << 33287;
    for (int i = 0; i < 33287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33287(int& val) {
    const int x = 33287;
    std::cout << 33287;
    for (int i = 0; i < 33287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33288(int& val) {
    func_in_33288(val);
    const int x = 33288;
    std::cout << 33288;
    for (int i = 0; i < 33288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33288(int& val) {
    const int x = 33288;
    std::cout << 33288;
    for (int i = 0; i < 33288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33289(int& val) {
    func_in_33289(val);
    const int x = 33289;
    std::cout << 33289;
    for (int i = 0; i < 33289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33289(int& val) {
    const int x = 33289;
    std::cout << 33289;
    for (int i = 0; i < 33289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33290(int& val) {
    func_in_33290(val);
    const int x = 33290;
    std::cout << 33290;
    for (int i = 0; i < 33290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33290(int& val) {
    const int x = 33290;
    std::cout << 33290;
    for (int i = 0; i < 33290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33291(int& val) {
    func_in_33291(val);
    const int x = 33291;
    std::cout << 33291;
    for (int i = 0; i < 33291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33291(int& val) {
    const int x = 33291;
    std::cout << 33291;
    for (int i = 0; i < 33291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33292(int& val) {
    func_in_33292(val);
    const int x = 33292;
    std::cout << 33292;
    for (int i = 0; i < 33292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33292(int& val) {
    const int x = 33292;
    std::cout << 33292;
    for (int i = 0; i < 33292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33293(int& val) {
    func_in_33293(val);
    const int x = 33293;
    std::cout << 33293;
    for (int i = 0; i < 33293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33293(int& val) {
    const int x = 33293;
    std::cout << 33293;
    for (int i = 0; i < 33293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33294(int& val) {
    func_in_33294(val);
    const int x = 33294;
    std::cout << 33294;
    for (int i = 0; i < 33294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33294(int& val) {
    const int x = 33294;
    std::cout << 33294;
    for (int i = 0; i < 33294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33295(int& val) {
    func_in_33295(val);
    const int x = 33295;
    std::cout << 33295;
    for (int i = 0; i < 33295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33295(int& val) {
    const int x = 33295;
    std::cout << 33295;
    for (int i = 0; i < 33295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33296(int& val) {
    func_in_33296(val);
    const int x = 33296;
    std::cout << 33296;
    for (int i = 0; i < 33296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33296(int& val) {
    const int x = 33296;
    std::cout << 33296;
    for (int i = 0; i < 33296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33297(int& val) {
    func_in_33297(val);
    const int x = 33297;
    std::cout << 33297;
    for (int i = 0; i < 33297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33297(int& val) {
    const int x = 33297;
    std::cout << 33297;
    for (int i = 0; i < 33297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33298(int& val) {
    func_in_33298(val);
    const int x = 33298;
    std::cout << 33298;
    for (int i = 0; i < 33298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33298(int& val) {
    const int x = 33298;
    std::cout << 33298;
    for (int i = 0; i < 33298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33299(int& val) {
    func_in_33299(val);
    const int x = 33299;
    std::cout << 33299;
    for (int i = 0; i < 33299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33299(int& val) {
    const int x = 33299;
    std::cout << 33299;
    for (int i = 0; i < 33299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33300(int& val) {
    func_in_33300(val);
    const int x = 33300;
    std::cout << 33300;
    for (int i = 0; i < 33300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33300(int& val) {
    const int x = 33300;
    std::cout << 33300;
    for (int i = 0; i < 33300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33301(int& val) {
    func_in_33301(val);
    const int x = 33301;
    std::cout << 33301;
    for (int i = 0; i < 33301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33301(int& val) {
    const int x = 33301;
    std::cout << 33301;
    for (int i = 0; i < 33301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33302(int& val) {
    func_in_33302(val);
    const int x = 33302;
    std::cout << 33302;
    for (int i = 0; i < 33302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33302(int& val) {
    const int x = 33302;
    std::cout << 33302;
    for (int i = 0; i < 33302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33303(int& val) {
    func_in_33303(val);
    const int x = 33303;
    std::cout << 33303;
    for (int i = 0; i < 33303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33303(int& val) {
    const int x = 33303;
    std::cout << 33303;
    for (int i = 0; i < 33303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33304(int& val) {
    func_in_33304(val);
    const int x = 33304;
    std::cout << 33304;
    for (int i = 0; i < 33304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33304(int& val) {
    const int x = 33304;
    std::cout << 33304;
    for (int i = 0; i < 33304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33305(int& val) {
    func_in_33305(val);
    const int x = 33305;
    std::cout << 33305;
    for (int i = 0; i < 33305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33305(int& val) {
    const int x = 33305;
    std::cout << 33305;
    for (int i = 0; i < 33305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33306(int& val) {
    func_in_33306(val);
    const int x = 33306;
    std::cout << 33306;
    for (int i = 0; i < 33306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33306(int& val) {
    const int x = 33306;
    std::cout << 33306;
    for (int i = 0; i < 33306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33307(int& val) {
    func_in_33307(val);
    const int x = 33307;
    std::cout << 33307;
    for (int i = 0; i < 33307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33307(int& val) {
    const int x = 33307;
    std::cout << 33307;
    for (int i = 0; i < 33307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33308(int& val) {
    func_in_33308(val);
    const int x = 33308;
    std::cout << 33308;
    for (int i = 0; i < 33308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33308(int& val) {
    const int x = 33308;
    std::cout << 33308;
    for (int i = 0; i < 33308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33309(int& val) {
    func_in_33309(val);
    const int x = 33309;
    std::cout << 33309;
    for (int i = 0; i < 33309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33309(int& val) {
    const int x = 33309;
    std::cout << 33309;
    for (int i = 0; i < 33309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33310(int& val) {
    func_in_33310(val);
    const int x = 33310;
    std::cout << 33310;
    for (int i = 0; i < 33310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33310(int& val) {
    const int x = 33310;
    std::cout << 33310;
    for (int i = 0; i < 33310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33311(int& val) {
    func_in_33311(val);
    const int x = 33311;
    std::cout << 33311;
    for (int i = 0; i < 33311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33311(int& val) {
    const int x = 33311;
    std::cout << 33311;
    for (int i = 0; i < 33311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33312(int& val) {
    func_in_33312(val);
    const int x = 33312;
    std::cout << 33312;
    for (int i = 0; i < 33312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33312(int& val) {
    const int x = 33312;
    std::cout << 33312;
    for (int i = 0; i < 33312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33313(int& val) {
    func_in_33313(val);
    const int x = 33313;
    std::cout << 33313;
    for (int i = 0; i < 33313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33313(int& val) {
    const int x = 33313;
    std::cout << 33313;
    for (int i = 0; i < 33313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33314(int& val) {
    func_in_33314(val);
    const int x = 33314;
    std::cout << 33314;
    for (int i = 0; i < 33314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33314(int& val) {
    const int x = 33314;
    std::cout << 33314;
    for (int i = 0; i < 33314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33315(int& val) {
    func_in_33315(val);
    const int x = 33315;
    std::cout << 33315;
    for (int i = 0; i < 33315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33315(int& val) {
    const int x = 33315;
    std::cout << 33315;
    for (int i = 0; i < 33315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33316(int& val) {
    func_in_33316(val);
    const int x = 33316;
    std::cout << 33316;
    for (int i = 0; i < 33316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33316(int& val) {
    const int x = 33316;
    std::cout << 33316;
    for (int i = 0; i < 33316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33317(int& val) {
    func_in_33317(val);
    const int x = 33317;
    std::cout << 33317;
    for (int i = 0; i < 33317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33317(int& val) {
    const int x = 33317;
    std::cout << 33317;
    for (int i = 0; i < 33317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33318(int& val) {
    func_in_33318(val);
    const int x = 33318;
    std::cout << 33318;
    for (int i = 0; i < 33318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33318(int& val) {
    const int x = 33318;
    std::cout << 33318;
    for (int i = 0; i < 33318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33319(int& val) {
    func_in_33319(val);
    const int x = 33319;
    std::cout << 33319;
    for (int i = 0; i < 33319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33319(int& val) {
    const int x = 33319;
    std::cout << 33319;
    for (int i = 0; i < 33319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33320(int& val) {
    func_in_33320(val);
    const int x = 33320;
    std::cout << 33320;
    for (int i = 0; i < 33320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33320(int& val) {
    const int x = 33320;
    std::cout << 33320;
    for (int i = 0; i < 33320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33321(int& val) {
    func_in_33321(val);
    const int x = 33321;
    std::cout << 33321;
    for (int i = 0; i < 33321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33321(int& val) {
    const int x = 33321;
    std::cout << 33321;
    for (int i = 0; i < 33321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33322(int& val) {
    func_in_33322(val);
    const int x = 33322;
    std::cout << 33322;
    for (int i = 0; i < 33322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33322(int& val) {
    const int x = 33322;
    std::cout << 33322;
    for (int i = 0; i < 33322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33323(int& val) {
    func_in_33323(val);
    const int x = 33323;
    std::cout << 33323;
    for (int i = 0; i < 33323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33323(int& val) {
    const int x = 33323;
    std::cout << 33323;
    for (int i = 0; i < 33323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33324(int& val) {
    func_in_33324(val);
    const int x = 33324;
    std::cout << 33324;
    for (int i = 0; i < 33324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33324(int& val) {
    const int x = 33324;
    std::cout << 33324;
    for (int i = 0; i < 33324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33325(int& val) {
    func_in_33325(val);
    const int x = 33325;
    std::cout << 33325;
    for (int i = 0; i < 33325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33325(int& val) {
    const int x = 33325;
    std::cout << 33325;
    for (int i = 0; i < 33325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33326(int& val) {
    func_in_33326(val);
    const int x = 33326;
    std::cout << 33326;
    for (int i = 0; i < 33326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33326(int& val) {
    const int x = 33326;
    std::cout << 33326;
    for (int i = 0; i < 33326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33327(int& val) {
    func_in_33327(val);
    const int x = 33327;
    std::cout << 33327;
    for (int i = 0; i < 33327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33327(int& val) {
    const int x = 33327;
    std::cout << 33327;
    for (int i = 0; i < 33327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33328(int& val) {
    func_in_33328(val);
    const int x = 33328;
    std::cout << 33328;
    for (int i = 0; i < 33328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33328(int& val) {
    const int x = 33328;
    std::cout << 33328;
    for (int i = 0; i < 33328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33329(int& val) {
    func_in_33329(val);
    const int x = 33329;
    std::cout << 33329;
    for (int i = 0; i < 33329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33329(int& val) {
    const int x = 33329;
    std::cout << 33329;
    for (int i = 0; i < 33329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33330(int& val) {
    func_in_33330(val);
    const int x = 33330;
    std::cout << 33330;
    for (int i = 0; i < 33330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33330(int& val) {
    const int x = 33330;
    std::cout << 33330;
    for (int i = 0; i < 33330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33331(int& val) {
    func_in_33331(val);
    const int x = 33331;
    std::cout << 33331;
    for (int i = 0; i < 33331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33331(int& val) {
    const int x = 33331;
    std::cout << 33331;
    for (int i = 0; i < 33331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33332(int& val) {
    func_in_33332(val);
    const int x = 33332;
    std::cout << 33332;
    for (int i = 0; i < 33332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33332(int& val) {
    const int x = 33332;
    std::cout << 33332;
    for (int i = 0; i < 33332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33333(int& val) {
    func_in_33333(val);
    const int x = 33333;
    std::cout << 33333;
    for (int i = 0; i < 33333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33333(int& val) {
    const int x = 33333;
    std::cout << 33333;
    for (int i = 0; i < 33333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33334(int& val) {
    func_in_33334(val);
    const int x = 33334;
    std::cout << 33334;
    for (int i = 0; i < 33334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33334(int& val) {
    const int x = 33334;
    std::cout << 33334;
    for (int i = 0; i < 33334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33335(int& val) {
    func_in_33335(val);
    const int x = 33335;
    std::cout << 33335;
    for (int i = 0; i < 33335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33335(int& val) {
    const int x = 33335;
    std::cout << 33335;
    for (int i = 0; i < 33335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33336(int& val) {
    func_in_33336(val);
    const int x = 33336;
    std::cout << 33336;
    for (int i = 0; i < 33336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33336(int& val) {
    const int x = 33336;
    std::cout << 33336;
    for (int i = 0; i < 33336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33337(int& val) {
    func_in_33337(val);
    const int x = 33337;
    std::cout << 33337;
    for (int i = 0; i < 33337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33337(int& val) {
    const int x = 33337;
    std::cout << 33337;
    for (int i = 0; i < 33337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33338(int& val) {
    func_in_33338(val);
    const int x = 33338;
    std::cout << 33338;
    for (int i = 0; i < 33338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33338(int& val) {
    const int x = 33338;
    std::cout << 33338;
    for (int i = 0; i < 33338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33339(int& val) {
    func_in_33339(val);
    const int x = 33339;
    std::cout << 33339;
    for (int i = 0; i < 33339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33339(int& val) {
    const int x = 33339;
    std::cout << 33339;
    for (int i = 0; i < 33339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33340(int& val) {
    func_in_33340(val);
    const int x = 33340;
    std::cout << 33340;
    for (int i = 0; i < 33340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33340(int& val) {
    const int x = 33340;
    std::cout << 33340;
    for (int i = 0; i < 33340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33341(int& val) {
    func_in_33341(val);
    const int x = 33341;
    std::cout << 33341;
    for (int i = 0; i < 33341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33341(int& val) {
    const int x = 33341;
    std::cout << 33341;
    for (int i = 0; i < 33341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33342(int& val) {
    func_in_33342(val);
    const int x = 33342;
    std::cout << 33342;
    for (int i = 0; i < 33342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33342(int& val) {
    const int x = 33342;
    std::cout << 33342;
    for (int i = 0; i < 33342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33343(int& val) {
    func_in_33343(val);
    const int x = 33343;
    std::cout << 33343;
    for (int i = 0; i < 33343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33343(int& val) {
    const int x = 33343;
    std::cout << 33343;
    for (int i = 0; i < 33343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33344(int& val) {
    func_in_33344(val);
    const int x = 33344;
    std::cout << 33344;
    for (int i = 0; i < 33344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33344(int& val) {
    const int x = 33344;
    std::cout << 33344;
    for (int i = 0; i < 33344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33345(int& val) {
    func_in_33345(val);
    const int x = 33345;
    std::cout << 33345;
    for (int i = 0; i < 33345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33345(int& val) {
    const int x = 33345;
    std::cout << 33345;
    for (int i = 0; i < 33345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33346(int& val) {
    func_in_33346(val);
    const int x = 33346;
    std::cout << 33346;
    for (int i = 0; i < 33346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33346(int& val) {
    const int x = 33346;
    std::cout << 33346;
    for (int i = 0; i < 33346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33347(int& val) {
    func_in_33347(val);
    const int x = 33347;
    std::cout << 33347;
    for (int i = 0; i < 33347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33347(int& val) {
    const int x = 33347;
    std::cout << 33347;
    for (int i = 0; i < 33347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33348(int& val) {
    func_in_33348(val);
    const int x = 33348;
    std::cout << 33348;
    for (int i = 0; i < 33348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33348(int& val) {
    const int x = 33348;
    std::cout << 33348;
    for (int i = 0; i < 33348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33349(int& val) {
    func_in_33349(val);
    const int x = 33349;
    std::cout << 33349;
    for (int i = 0; i < 33349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33349(int& val) {
    const int x = 33349;
    std::cout << 33349;
    for (int i = 0; i < 33349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33350(int& val) {
    func_in_33350(val);
    const int x = 33350;
    std::cout << 33350;
    for (int i = 0; i < 33350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33350(int& val) {
    const int x = 33350;
    std::cout << 33350;
    for (int i = 0; i < 33350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33351(int& val) {
    func_in_33351(val);
    const int x = 33351;
    std::cout << 33351;
    for (int i = 0; i < 33351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33351(int& val) {
    const int x = 33351;
    std::cout << 33351;
    for (int i = 0; i < 33351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33352(int& val) {
    func_in_33352(val);
    const int x = 33352;
    std::cout << 33352;
    for (int i = 0; i < 33352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33352(int& val) {
    const int x = 33352;
    std::cout << 33352;
    for (int i = 0; i < 33352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33353(int& val) {
    func_in_33353(val);
    const int x = 33353;
    std::cout << 33353;
    for (int i = 0; i < 33353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33353(int& val) {
    const int x = 33353;
    std::cout << 33353;
    for (int i = 0; i < 33353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33354(int& val) {
    func_in_33354(val);
    const int x = 33354;
    std::cout << 33354;
    for (int i = 0; i < 33354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33354(int& val) {
    const int x = 33354;
    std::cout << 33354;
    for (int i = 0; i < 33354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33355(int& val) {
    func_in_33355(val);
    const int x = 33355;
    std::cout << 33355;
    for (int i = 0; i < 33355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33355(int& val) {
    const int x = 33355;
    std::cout << 33355;
    for (int i = 0; i < 33355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33356(int& val) {
    func_in_33356(val);
    const int x = 33356;
    std::cout << 33356;
    for (int i = 0; i < 33356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33356(int& val) {
    const int x = 33356;
    std::cout << 33356;
    for (int i = 0; i < 33356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33357(int& val) {
    func_in_33357(val);
    const int x = 33357;
    std::cout << 33357;
    for (int i = 0; i < 33357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33357(int& val) {
    const int x = 33357;
    std::cout << 33357;
    for (int i = 0; i < 33357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33358(int& val) {
    func_in_33358(val);
    const int x = 33358;
    std::cout << 33358;
    for (int i = 0; i < 33358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33358(int& val) {
    const int x = 33358;
    std::cout << 33358;
    for (int i = 0; i < 33358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33359(int& val) {
    func_in_33359(val);
    const int x = 33359;
    std::cout << 33359;
    for (int i = 0; i < 33359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33359(int& val) {
    const int x = 33359;
    std::cout << 33359;
    for (int i = 0; i < 33359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33360(int& val) {
    func_in_33360(val);
    const int x = 33360;
    std::cout << 33360;
    for (int i = 0; i < 33360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33360(int& val) {
    const int x = 33360;
    std::cout << 33360;
    for (int i = 0; i < 33360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33361(int& val) {
    func_in_33361(val);
    const int x = 33361;
    std::cout << 33361;
    for (int i = 0; i < 33361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33361(int& val) {
    const int x = 33361;
    std::cout << 33361;
    for (int i = 0; i < 33361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33362(int& val) {
    func_in_33362(val);
    const int x = 33362;
    std::cout << 33362;
    for (int i = 0; i < 33362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33362(int& val) {
    const int x = 33362;
    std::cout << 33362;
    for (int i = 0; i < 33362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33363(int& val) {
    func_in_33363(val);
    const int x = 33363;
    std::cout << 33363;
    for (int i = 0; i < 33363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33363(int& val) {
    const int x = 33363;
    std::cout << 33363;
    for (int i = 0; i < 33363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33364(int& val) {
    func_in_33364(val);
    const int x = 33364;
    std::cout << 33364;
    for (int i = 0; i < 33364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33364(int& val) {
    const int x = 33364;
    std::cout << 33364;
    for (int i = 0; i < 33364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33365(int& val) {
    func_in_33365(val);
    const int x = 33365;
    std::cout << 33365;
    for (int i = 0; i < 33365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33365(int& val) {
    const int x = 33365;
    std::cout << 33365;
    for (int i = 0; i < 33365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33366(int& val) {
    func_in_33366(val);
    const int x = 33366;
    std::cout << 33366;
    for (int i = 0; i < 33366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33366(int& val) {
    const int x = 33366;
    std::cout << 33366;
    for (int i = 0; i < 33366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33367(int& val) {
    func_in_33367(val);
    const int x = 33367;
    std::cout << 33367;
    for (int i = 0; i < 33367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33367(int& val) {
    const int x = 33367;
    std::cout << 33367;
    for (int i = 0; i < 33367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33368(int& val) {
    func_in_33368(val);
    const int x = 33368;
    std::cout << 33368;
    for (int i = 0; i < 33368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33368(int& val) {
    const int x = 33368;
    std::cout << 33368;
    for (int i = 0; i < 33368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33369(int& val) {
    func_in_33369(val);
    const int x = 33369;
    std::cout << 33369;
    for (int i = 0; i < 33369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33369(int& val) {
    const int x = 33369;
    std::cout << 33369;
    for (int i = 0; i < 33369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33370(int& val) {
    func_in_33370(val);
    const int x = 33370;
    std::cout << 33370;
    for (int i = 0; i < 33370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33370(int& val) {
    const int x = 33370;
    std::cout << 33370;
    for (int i = 0; i < 33370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33371(int& val) {
    func_in_33371(val);
    const int x = 33371;
    std::cout << 33371;
    for (int i = 0; i < 33371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33371(int& val) {
    const int x = 33371;
    std::cout << 33371;
    for (int i = 0; i < 33371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33372(int& val) {
    func_in_33372(val);
    const int x = 33372;
    std::cout << 33372;
    for (int i = 0; i < 33372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33372(int& val) {
    const int x = 33372;
    std::cout << 33372;
    for (int i = 0; i < 33372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33373(int& val) {
    func_in_33373(val);
    const int x = 33373;
    std::cout << 33373;
    for (int i = 0; i < 33373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33373(int& val) {
    const int x = 33373;
    std::cout << 33373;
    for (int i = 0; i < 33373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33374(int& val) {
    func_in_33374(val);
    const int x = 33374;
    std::cout << 33374;
    for (int i = 0; i < 33374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33374(int& val) {
    const int x = 33374;
    std::cout << 33374;
    for (int i = 0; i < 33374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33375(int& val) {
    func_in_33375(val);
    const int x = 33375;
    std::cout << 33375;
    for (int i = 0; i < 33375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33375(int& val) {
    const int x = 33375;
    std::cout << 33375;
    for (int i = 0; i < 33375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33376(int& val) {
    func_in_33376(val);
    const int x = 33376;
    std::cout << 33376;
    for (int i = 0; i < 33376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33376(int& val) {
    const int x = 33376;
    std::cout << 33376;
    for (int i = 0; i < 33376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33377(int& val) {
    func_in_33377(val);
    const int x = 33377;
    std::cout << 33377;
    for (int i = 0; i < 33377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33377(int& val) {
    const int x = 33377;
    std::cout << 33377;
    for (int i = 0; i < 33377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33378(int& val) {
    func_in_33378(val);
    const int x = 33378;
    std::cout << 33378;
    for (int i = 0; i < 33378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33378(int& val) {
    const int x = 33378;
    std::cout << 33378;
    for (int i = 0; i < 33378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33379(int& val) {
    func_in_33379(val);
    const int x = 33379;
    std::cout << 33379;
    for (int i = 0; i < 33379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33379(int& val) {
    const int x = 33379;
    std::cout << 33379;
    for (int i = 0; i < 33379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33380(int& val) {
    func_in_33380(val);
    const int x = 33380;
    std::cout << 33380;
    for (int i = 0; i < 33380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33380(int& val) {
    const int x = 33380;
    std::cout << 33380;
    for (int i = 0; i < 33380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33381(int& val) {
    func_in_33381(val);
    const int x = 33381;
    std::cout << 33381;
    for (int i = 0; i < 33381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33381(int& val) {
    const int x = 33381;
    std::cout << 33381;
    for (int i = 0; i < 33381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33382(int& val) {
    func_in_33382(val);
    const int x = 33382;
    std::cout << 33382;
    for (int i = 0; i < 33382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33382(int& val) {
    const int x = 33382;
    std::cout << 33382;
    for (int i = 0; i < 33382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33383(int& val) {
    func_in_33383(val);
    const int x = 33383;
    std::cout << 33383;
    for (int i = 0; i < 33383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33383(int& val) {
    const int x = 33383;
    std::cout << 33383;
    for (int i = 0; i < 33383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33384(int& val) {
    func_in_33384(val);
    const int x = 33384;
    std::cout << 33384;
    for (int i = 0; i < 33384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33384(int& val) {
    const int x = 33384;
    std::cout << 33384;
    for (int i = 0; i < 33384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33385(int& val) {
    func_in_33385(val);
    const int x = 33385;
    std::cout << 33385;
    for (int i = 0; i < 33385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33385(int& val) {
    const int x = 33385;
    std::cout << 33385;
    for (int i = 0; i < 33385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33386(int& val) {
    func_in_33386(val);
    const int x = 33386;
    std::cout << 33386;
    for (int i = 0; i < 33386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33386(int& val) {
    const int x = 33386;
    std::cout << 33386;
    for (int i = 0; i < 33386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33387(int& val) {
    func_in_33387(val);
    const int x = 33387;
    std::cout << 33387;
    for (int i = 0; i < 33387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33387(int& val) {
    const int x = 33387;
    std::cout << 33387;
    for (int i = 0; i < 33387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33388(int& val) {
    func_in_33388(val);
    const int x = 33388;
    std::cout << 33388;
    for (int i = 0; i < 33388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33388(int& val) {
    const int x = 33388;
    std::cout << 33388;
    for (int i = 0; i < 33388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33389(int& val) {
    func_in_33389(val);
    const int x = 33389;
    std::cout << 33389;
    for (int i = 0; i < 33389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33389(int& val) {
    const int x = 33389;
    std::cout << 33389;
    for (int i = 0; i < 33389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33390(int& val) {
    func_in_33390(val);
    const int x = 33390;
    std::cout << 33390;
    for (int i = 0; i < 33390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33390(int& val) {
    const int x = 33390;
    std::cout << 33390;
    for (int i = 0; i < 33390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33391(int& val) {
    func_in_33391(val);
    const int x = 33391;
    std::cout << 33391;
    for (int i = 0; i < 33391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33391(int& val) {
    const int x = 33391;
    std::cout << 33391;
    for (int i = 0; i < 33391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33392(int& val) {
    func_in_33392(val);
    const int x = 33392;
    std::cout << 33392;
    for (int i = 0; i < 33392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33392(int& val) {
    const int x = 33392;
    std::cout << 33392;
    for (int i = 0; i < 33392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33393(int& val) {
    func_in_33393(val);
    const int x = 33393;
    std::cout << 33393;
    for (int i = 0; i < 33393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33393(int& val) {
    const int x = 33393;
    std::cout << 33393;
    for (int i = 0; i < 33393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33394(int& val) {
    func_in_33394(val);
    const int x = 33394;
    std::cout << 33394;
    for (int i = 0; i < 33394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33394(int& val) {
    const int x = 33394;
    std::cout << 33394;
    for (int i = 0; i < 33394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33395(int& val) {
    func_in_33395(val);
    const int x = 33395;
    std::cout << 33395;
    for (int i = 0; i < 33395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33395(int& val) {
    const int x = 33395;
    std::cout << 33395;
    for (int i = 0; i < 33395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33396(int& val) {
    func_in_33396(val);
    const int x = 33396;
    std::cout << 33396;
    for (int i = 0; i < 33396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33396(int& val) {
    const int x = 33396;
    std::cout << 33396;
    for (int i = 0; i < 33396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33397(int& val) {
    func_in_33397(val);
    const int x = 33397;
    std::cout << 33397;
    for (int i = 0; i < 33397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33397(int& val) {
    const int x = 33397;
    std::cout << 33397;
    for (int i = 0; i < 33397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33398(int& val) {
    func_in_33398(val);
    const int x = 33398;
    std::cout << 33398;
    for (int i = 0; i < 33398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33398(int& val) {
    const int x = 33398;
    std::cout << 33398;
    for (int i = 0; i < 33398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33399(int& val) {
    func_in_33399(val);
    const int x = 33399;
    std::cout << 33399;
    for (int i = 0; i < 33399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33399(int& val) {
    const int x = 33399;
    std::cout << 33399;
    for (int i = 0; i < 33399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33400(int& val) {
    func_in_33400(val);
    const int x = 33400;
    std::cout << 33400;
    for (int i = 0; i < 33400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33400(int& val) {
    const int x = 33400;
    std::cout << 33400;
    for (int i = 0; i < 33400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33401(int& val) {
    func_in_33401(val);
    const int x = 33401;
    std::cout << 33401;
    for (int i = 0; i < 33401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33401(int& val) {
    const int x = 33401;
    std::cout << 33401;
    for (int i = 0; i < 33401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33402(int& val) {
    func_in_33402(val);
    const int x = 33402;
    std::cout << 33402;
    for (int i = 0; i < 33402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33402(int& val) {
    const int x = 33402;
    std::cout << 33402;
    for (int i = 0; i < 33402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33403(int& val) {
    func_in_33403(val);
    const int x = 33403;
    std::cout << 33403;
    for (int i = 0; i < 33403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33403(int& val) {
    const int x = 33403;
    std::cout << 33403;
    for (int i = 0; i < 33403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33404(int& val) {
    func_in_33404(val);
    const int x = 33404;
    std::cout << 33404;
    for (int i = 0; i < 33404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33404(int& val) {
    const int x = 33404;
    std::cout << 33404;
    for (int i = 0; i < 33404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33405(int& val) {
    func_in_33405(val);
    const int x = 33405;
    std::cout << 33405;
    for (int i = 0; i < 33405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33405(int& val) {
    const int x = 33405;
    std::cout << 33405;
    for (int i = 0; i < 33405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33406(int& val) {
    func_in_33406(val);
    const int x = 33406;
    std::cout << 33406;
    for (int i = 0; i < 33406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33406(int& val) {
    const int x = 33406;
    std::cout << 33406;
    for (int i = 0; i < 33406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33407(int& val) {
    func_in_33407(val);
    const int x = 33407;
    std::cout << 33407;
    for (int i = 0; i < 33407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33407(int& val) {
    const int x = 33407;
    std::cout << 33407;
    for (int i = 0; i < 33407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33408(int& val) {
    func_in_33408(val);
    const int x = 33408;
    std::cout << 33408;
    for (int i = 0; i < 33408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33408(int& val) {
    const int x = 33408;
    std::cout << 33408;
    for (int i = 0; i < 33408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33409(int& val) {
    func_in_33409(val);
    const int x = 33409;
    std::cout << 33409;
    for (int i = 0; i < 33409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33409(int& val) {
    const int x = 33409;
    std::cout << 33409;
    for (int i = 0; i < 33409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33410(int& val) {
    func_in_33410(val);
    const int x = 33410;
    std::cout << 33410;
    for (int i = 0; i < 33410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33410(int& val) {
    const int x = 33410;
    std::cout << 33410;
    for (int i = 0; i < 33410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33411(int& val) {
    func_in_33411(val);
    const int x = 33411;
    std::cout << 33411;
    for (int i = 0; i < 33411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33411(int& val) {
    const int x = 33411;
    std::cout << 33411;
    for (int i = 0; i < 33411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33412(int& val) {
    func_in_33412(val);
    const int x = 33412;
    std::cout << 33412;
    for (int i = 0; i < 33412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33412(int& val) {
    const int x = 33412;
    std::cout << 33412;
    for (int i = 0; i < 33412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33413(int& val) {
    func_in_33413(val);
    const int x = 33413;
    std::cout << 33413;
    for (int i = 0; i < 33413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33413(int& val) {
    const int x = 33413;
    std::cout << 33413;
    for (int i = 0; i < 33413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33414(int& val) {
    func_in_33414(val);
    const int x = 33414;
    std::cout << 33414;
    for (int i = 0; i < 33414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33414(int& val) {
    const int x = 33414;
    std::cout << 33414;
    for (int i = 0; i < 33414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33415(int& val) {
    func_in_33415(val);
    const int x = 33415;
    std::cout << 33415;
    for (int i = 0; i < 33415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33415(int& val) {
    const int x = 33415;
    std::cout << 33415;
    for (int i = 0; i < 33415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33416(int& val) {
    func_in_33416(val);
    const int x = 33416;
    std::cout << 33416;
    for (int i = 0; i < 33416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33416(int& val) {
    const int x = 33416;
    std::cout << 33416;
    for (int i = 0; i < 33416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33417(int& val) {
    func_in_33417(val);
    const int x = 33417;
    std::cout << 33417;
    for (int i = 0; i < 33417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33417(int& val) {
    const int x = 33417;
    std::cout << 33417;
    for (int i = 0; i < 33417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33418(int& val) {
    func_in_33418(val);
    const int x = 33418;
    std::cout << 33418;
    for (int i = 0; i < 33418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33418(int& val) {
    const int x = 33418;
    std::cout << 33418;
    for (int i = 0; i < 33418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33419(int& val) {
    func_in_33419(val);
    const int x = 33419;
    std::cout << 33419;
    for (int i = 0; i < 33419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33419(int& val) {
    const int x = 33419;
    std::cout << 33419;
    for (int i = 0; i < 33419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33420(int& val) {
    func_in_33420(val);
    const int x = 33420;
    std::cout << 33420;
    for (int i = 0; i < 33420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33420(int& val) {
    const int x = 33420;
    std::cout << 33420;
    for (int i = 0; i < 33420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33421(int& val) {
    func_in_33421(val);
    const int x = 33421;
    std::cout << 33421;
    for (int i = 0; i < 33421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33421(int& val) {
    const int x = 33421;
    std::cout << 33421;
    for (int i = 0; i < 33421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33422(int& val) {
    func_in_33422(val);
    const int x = 33422;
    std::cout << 33422;
    for (int i = 0; i < 33422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33422(int& val) {
    const int x = 33422;
    std::cout << 33422;
    for (int i = 0; i < 33422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33423(int& val) {
    func_in_33423(val);
    const int x = 33423;
    std::cout << 33423;
    for (int i = 0; i < 33423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33423(int& val) {
    const int x = 33423;
    std::cout << 33423;
    for (int i = 0; i < 33423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33424(int& val) {
    func_in_33424(val);
    const int x = 33424;
    std::cout << 33424;
    for (int i = 0; i < 33424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33424(int& val) {
    const int x = 33424;
    std::cout << 33424;
    for (int i = 0; i < 33424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33425(int& val) {
    func_in_33425(val);
    const int x = 33425;
    std::cout << 33425;
    for (int i = 0; i < 33425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33425(int& val) {
    const int x = 33425;
    std::cout << 33425;
    for (int i = 0; i < 33425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33426(int& val) {
    func_in_33426(val);
    const int x = 33426;
    std::cout << 33426;
    for (int i = 0; i < 33426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33426(int& val) {
    const int x = 33426;
    std::cout << 33426;
    for (int i = 0; i < 33426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33427(int& val) {
    func_in_33427(val);
    const int x = 33427;
    std::cout << 33427;
    for (int i = 0; i < 33427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33427(int& val) {
    const int x = 33427;
    std::cout << 33427;
    for (int i = 0; i < 33427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33428(int& val) {
    func_in_33428(val);
    const int x = 33428;
    std::cout << 33428;
    for (int i = 0; i < 33428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33428(int& val) {
    const int x = 33428;
    std::cout << 33428;
    for (int i = 0; i < 33428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33429(int& val) {
    func_in_33429(val);
    const int x = 33429;
    std::cout << 33429;
    for (int i = 0; i < 33429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33429(int& val) {
    const int x = 33429;
    std::cout << 33429;
    for (int i = 0; i < 33429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33430(int& val) {
    func_in_33430(val);
    const int x = 33430;
    std::cout << 33430;
    for (int i = 0; i < 33430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33430(int& val) {
    const int x = 33430;
    std::cout << 33430;
    for (int i = 0; i < 33430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33431(int& val) {
    func_in_33431(val);
    const int x = 33431;
    std::cout << 33431;
    for (int i = 0; i < 33431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33431(int& val) {
    const int x = 33431;
    std::cout << 33431;
    for (int i = 0; i < 33431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33432(int& val) {
    func_in_33432(val);
    const int x = 33432;
    std::cout << 33432;
    for (int i = 0; i < 33432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33432(int& val) {
    const int x = 33432;
    std::cout << 33432;
    for (int i = 0; i < 33432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33433(int& val) {
    func_in_33433(val);
    const int x = 33433;
    std::cout << 33433;
    for (int i = 0; i < 33433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33433(int& val) {
    const int x = 33433;
    std::cout << 33433;
    for (int i = 0; i < 33433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33434(int& val) {
    func_in_33434(val);
    const int x = 33434;
    std::cout << 33434;
    for (int i = 0; i < 33434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33434(int& val) {
    const int x = 33434;
    std::cout << 33434;
    for (int i = 0; i < 33434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33435(int& val) {
    func_in_33435(val);
    const int x = 33435;
    std::cout << 33435;
    for (int i = 0; i < 33435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33435(int& val) {
    const int x = 33435;
    std::cout << 33435;
    for (int i = 0; i < 33435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33436(int& val) {
    func_in_33436(val);
    const int x = 33436;
    std::cout << 33436;
    for (int i = 0; i < 33436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33436(int& val) {
    const int x = 33436;
    std::cout << 33436;
    for (int i = 0; i < 33436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33437(int& val) {
    func_in_33437(val);
    const int x = 33437;
    std::cout << 33437;
    for (int i = 0; i < 33437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33437(int& val) {
    const int x = 33437;
    std::cout << 33437;
    for (int i = 0; i < 33437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33438(int& val) {
    func_in_33438(val);
    const int x = 33438;
    std::cout << 33438;
    for (int i = 0; i < 33438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33438(int& val) {
    const int x = 33438;
    std::cout << 33438;
    for (int i = 0; i < 33438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33439(int& val) {
    func_in_33439(val);
    const int x = 33439;
    std::cout << 33439;
    for (int i = 0; i < 33439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33439(int& val) {
    const int x = 33439;
    std::cout << 33439;
    for (int i = 0; i < 33439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33440(int& val) {
    func_in_33440(val);
    const int x = 33440;
    std::cout << 33440;
    for (int i = 0; i < 33440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33440(int& val) {
    const int x = 33440;
    std::cout << 33440;
    for (int i = 0; i < 33440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33441(int& val) {
    func_in_33441(val);
    const int x = 33441;
    std::cout << 33441;
    for (int i = 0; i < 33441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33441(int& val) {
    const int x = 33441;
    std::cout << 33441;
    for (int i = 0; i < 33441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33442(int& val) {
    func_in_33442(val);
    const int x = 33442;
    std::cout << 33442;
    for (int i = 0; i < 33442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33442(int& val) {
    const int x = 33442;
    std::cout << 33442;
    for (int i = 0; i < 33442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33443(int& val) {
    func_in_33443(val);
    const int x = 33443;
    std::cout << 33443;
    for (int i = 0; i < 33443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33443(int& val) {
    const int x = 33443;
    std::cout << 33443;
    for (int i = 0; i < 33443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33444(int& val) {
    func_in_33444(val);
    const int x = 33444;
    std::cout << 33444;
    for (int i = 0; i < 33444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33444(int& val) {
    const int x = 33444;
    std::cout << 33444;
    for (int i = 0; i < 33444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33445(int& val) {
    func_in_33445(val);
    const int x = 33445;
    std::cout << 33445;
    for (int i = 0; i < 33445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33445(int& val) {
    const int x = 33445;
    std::cout << 33445;
    for (int i = 0; i < 33445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33446(int& val) {
    func_in_33446(val);
    const int x = 33446;
    std::cout << 33446;
    for (int i = 0; i < 33446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33446(int& val) {
    const int x = 33446;
    std::cout << 33446;
    for (int i = 0; i < 33446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33447(int& val) {
    func_in_33447(val);
    const int x = 33447;
    std::cout << 33447;
    for (int i = 0; i < 33447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33447(int& val) {
    const int x = 33447;
    std::cout << 33447;
    for (int i = 0; i < 33447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33448(int& val) {
    func_in_33448(val);
    const int x = 33448;
    std::cout << 33448;
    for (int i = 0; i < 33448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33448(int& val) {
    const int x = 33448;
    std::cout << 33448;
    for (int i = 0; i < 33448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33449(int& val) {
    func_in_33449(val);
    const int x = 33449;
    std::cout << 33449;
    for (int i = 0; i < 33449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33449(int& val) {
    const int x = 33449;
    std::cout << 33449;
    for (int i = 0; i < 33449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33450(int& val) {
    func_in_33450(val);
    const int x = 33450;
    std::cout << 33450;
    for (int i = 0; i < 33450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33450(int& val) {
    const int x = 33450;
    std::cout << 33450;
    for (int i = 0; i < 33450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33451(int& val) {
    func_in_33451(val);
    const int x = 33451;
    std::cout << 33451;
    for (int i = 0; i < 33451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33451(int& val) {
    const int x = 33451;
    std::cout << 33451;
    for (int i = 0; i < 33451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33452(int& val) {
    func_in_33452(val);
    const int x = 33452;
    std::cout << 33452;
    for (int i = 0; i < 33452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33452(int& val) {
    const int x = 33452;
    std::cout << 33452;
    for (int i = 0; i < 33452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33453(int& val) {
    func_in_33453(val);
    const int x = 33453;
    std::cout << 33453;
    for (int i = 0; i < 33453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33453(int& val) {
    const int x = 33453;
    std::cout << 33453;
    for (int i = 0; i < 33453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33454(int& val) {
    func_in_33454(val);
    const int x = 33454;
    std::cout << 33454;
    for (int i = 0; i < 33454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33454(int& val) {
    const int x = 33454;
    std::cout << 33454;
    for (int i = 0; i < 33454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33455(int& val) {
    func_in_33455(val);
    const int x = 33455;
    std::cout << 33455;
    for (int i = 0; i < 33455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33455(int& val) {
    const int x = 33455;
    std::cout << 33455;
    for (int i = 0; i < 33455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33456(int& val) {
    func_in_33456(val);
    const int x = 33456;
    std::cout << 33456;
    for (int i = 0; i < 33456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33456(int& val) {
    const int x = 33456;
    std::cout << 33456;
    for (int i = 0; i < 33456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33457(int& val) {
    func_in_33457(val);
    const int x = 33457;
    std::cout << 33457;
    for (int i = 0; i < 33457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33457(int& val) {
    const int x = 33457;
    std::cout << 33457;
    for (int i = 0; i < 33457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33458(int& val) {
    func_in_33458(val);
    const int x = 33458;
    std::cout << 33458;
    for (int i = 0; i < 33458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33458(int& val) {
    const int x = 33458;
    std::cout << 33458;
    for (int i = 0; i < 33458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33459(int& val) {
    func_in_33459(val);
    const int x = 33459;
    std::cout << 33459;
    for (int i = 0; i < 33459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33459(int& val) {
    const int x = 33459;
    std::cout << 33459;
    for (int i = 0; i < 33459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33460(int& val) {
    func_in_33460(val);
    const int x = 33460;
    std::cout << 33460;
    for (int i = 0; i < 33460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33460(int& val) {
    const int x = 33460;
    std::cout << 33460;
    for (int i = 0; i < 33460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33461(int& val) {
    func_in_33461(val);
    const int x = 33461;
    std::cout << 33461;
    for (int i = 0; i < 33461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33461(int& val) {
    const int x = 33461;
    std::cout << 33461;
    for (int i = 0; i < 33461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33462(int& val) {
    func_in_33462(val);
    const int x = 33462;
    std::cout << 33462;
    for (int i = 0; i < 33462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33462(int& val) {
    const int x = 33462;
    std::cout << 33462;
    for (int i = 0; i < 33462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33463(int& val) {
    func_in_33463(val);
    const int x = 33463;
    std::cout << 33463;
    for (int i = 0; i < 33463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33463(int& val) {
    const int x = 33463;
    std::cout << 33463;
    for (int i = 0; i < 33463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33464(int& val) {
    func_in_33464(val);
    const int x = 33464;
    std::cout << 33464;
    for (int i = 0; i < 33464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33464(int& val) {
    const int x = 33464;
    std::cout << 33464;
    for (int i = 0; i < 33464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33465(int& val) {
    func_in_33465(val);
    const int x = 33465;
    std::cout << 33465;
    for (int i = 0; i < 33465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33465(int& val) {
    const int x = 33465;
    std::cout << 33465;
    for (int i = 0; i < 33465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33466(int& val) {
    func_in_33466(val);
    const int x = 33466;
    std::cout << 33466;
    for (int i = 0; i < 33466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33466(int& val) {
    const int x = 33466;
    std::cout << 33466;
    for (int i = 0; i < 33466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33467(int& val) {
    func_in_33467(val);
    const int x = 33467;
    std::cout << 33467;
    for (int i = 0; i < 33467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33467(int& val) {
    const int x = 33467;
    std::cout << 33467;
    for (int i = 0; i < 33467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33468(int& val) {
    func_in_33468(val);
    const int x = 33468;
    std::cout << 33468;
    for (int i = 0; i < 33468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33468(int& val) {
    const int x = 33468;
    std::cout << 33468;
    for (int i = 0; i < 33468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33469(int& val) {
    func_in_33469(val);
    const int x = 33469;
    std::cout << 33469;
    for (int i = 0; i < 33469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33469(int& val) {
    const int x = 33469;
    std::cout << 33469;
    for (int i = 0; i < 33469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33470(int& val) {
    func_in_33470(val);
    const int x = 33470;
    std::cout << 33470;
    for (int i = 0; i < 33470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33470(int& val) {
    const int x = 33470;
    std::cout << 33470;
    for (int i = 0; i < 33470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33471(int& val) {
    func_in_33471(val);
    const int x = 33471;
    std::cout << 33471;
    for (int i = 0; i < 33471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33471(int& val) {
    const int x = 33471;
    std::cout << 33471;
    for (int i = 0; i < 33471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33472(int& val) {
    func_in_33472(val);
    const int x = 33472;
    std::cout << 33472;
    for (int i = 0; i < 33472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33472(int& val) {
    const int x = 33472;
    std::cout << 33472;
    for (int i = 0; i < 33472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33473(int& val) {
    func_in_33473(val);
    const int x = 33473;
    std::cout << 33473;
    for (int i = 0; i < 33473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33473(int& val) {
    const int x = 33473;
    std::cout << 33473;
    for (int i = 0; i < 33473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33474(int& val) {
    func_in_33474(val);
    const int x = 33474;
    std::cout << 33474;
    for (int i = 0; i < 33474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33474(int& val) {
    const int x = 33474;
    std::cout << 33474;
    for (int i = 0; i < 33474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33475(int& val) {
    func_in_33475(val);
    const int x = 33475;
    std::cout << 33475;
    for (int i = 0; i < 33475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33475(int& val) {
    const int x = 33475;
    std::cout << 33475;
    for (int i = 0; i < 33475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33476(int& val) {
    func_in_33476(val);
    const int x = 33476;
    std::cout << 33476;
    for (int i = 0; i < 33476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33476(int& val) {
    const int x = 33476;
    std::cout << 33476;
    for (int i = 0; i < 33476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33477(int& val) {
    func_in_33477(val);
    const int x = 33477;
    std::cout << 33477;
    for (int i = 0; i < 33477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33477(int& val) {
    const int x = 33477;
    std::cout << 33477;
    for (int i = 0; i < 33477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33478(int& val) {
    func_in_33478(val);
    const int x = 33478;
    std::cout << 33478;
    for (int i = 0; i < 33478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33478(int& val) {
    const int x = 33478;
    std::cout << 33478;
    for (int i = 0; i < 33478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33479(int& val) {
    func_in_33479(val);
    const int x = 33479;
    std::cout << 33479;
    for (int i = 0; i < 33479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33479(int& val) {
    const int x = 33479;
    std::cout << 33479;
    for (int i = 0; i < 33479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33480(int& val) {
    func_in_33480(val);
    const int x = 33480;
    std::cout << 33480;
    for (int i = 0; i < 33480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33480(int& val) {
    const int x = 33480;
    std::cout << 33480;
    for (int i = 0; i < 33480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33481(int& val) {
    func_in_33481(val);
    const int x = 33481;
    std::cout << 33481;
    for (int i = 0; i < 33481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33481(int& val) {
    const int x = 33481;
    std::cout << 33481;
    for (int i = 0; i < 33481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33482(int& val) {
    func_in_33482(val);
    const int x = 33482;
    std::cout << 33482;
    for (int i = 0; i < 33482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33482(int& val) {
    const int x = 33482;
    std::cout << 33482;
    for (int i = 0; i < 33482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33483(int& val) {
    func_in_33483(val);
    const int x = 33483;
    std::cout << 33483;
    for (int i = 0; i < 33483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33483(int& val) {
    const int x = 33483;
    std::cout << 33483;
    for (int i = 0; i < 33483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33484(int& val) {
    func_in_33484(val);
    const int x = 33484;
    std::cout << 33484;
    for (int i = 0; i < 33484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33484(int& val) {
    const int x = 33484;
    std::cout << 33484;
    for (int i = 0; i < 33484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33485(int& val) {
    func_in_33485(val);
    const int x = 33485;
    std::cout << 33485;
    for (int i = 0; i < 33485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33485(int& val) {
    const int x = 33485;
    std::cout << 33485;
    for (int i = 0; i < 33485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33486(int& val) {
    func_in_33486(val);
    const int x = 33486;
    std::cout << 33486;
    for (int i = 0; i < 33486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33486(int& val) {
    const int x = 33486;
    std::cout << 33486;
    for (int i = 0; i < 33486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33487(int& val) {
    func_in_33487(val);
    const int x = 33487;
    std::cout << 33487;
    for (int i = 0; i < 33487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33487(int& val) {
    const int x = 33487;
    std::cout << 33487;
    for (int i = 0; i < 33487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33488(int& val) {
    func_in_33488(val);
    const int x = 33488;
    std::cout << 33488;
    for (int i = 0; i < 33488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33488(int& val) {
    const int x = 33488;
    std::cout << 33488;
    for (int i = 0; i < 33488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33489(int& val) {
    func_in_33489(val);
    const int x = 33489;
    std::cout << 33489;
    for (int i = 0; i < 33489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33489(int& val) {
    const int x = 33489;
    std::cout << 33489;
    for (int i = 0; i < 33489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33490(int& val) {
    func_in_33490(val);
    const int x = 33490;
    std::cout << 33490;
    for (int i = 0; i < 33490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33490(int& val) {
    const int x = 33490;
    std::cout << 33490;
    for (int i = 0; i < 33490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33491(int& val) {
    func_in_33491(val);
    const int x = 33491;
    std::cout << 33491;
    for (int i = 0; i < 33491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33491(int& val) {
    const int x = 33491;
    std::cout << 33491;
    for (int i = 0; i < 33491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33492(int& val) {
    func_in_33492(val);
    const int x = 33492;
    std::cout << 33492;
    for (int i = 0; i < 33492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33492(int& val) {
    const int x = 33492;
    std::cout << 33492;
    for (int i = 0; i < 33492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33493(int& val) {
    func_in_33493(val);
    const int x = 33493;
    std::cout << 33493;
    for (int i = 0; i < 33493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33493(int& val) {
    const int x = 33493;
    std::cout << 33493;
    for (int i = 0; i < 33493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33494(int& val) {
    func_in_33494(val);
    const int x = 33494;
    std::cout << 33494;
    for (int i = 0; i < 33494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33494(int& val) {
    const int x = 33494;
    std::cout << 33494;
    for (int i = 0; i < 33494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33495(int& val) {
    func_in_33495(val);
    const int x = 33495;
    std::cout << 33495;
    for (int i = 0; i < 33495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33495(int& val) {
    const int x = 33495;
    std::cout << 33495;
    for (int i = 0; i < 33495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33496(int& val) {
    func_in_33496(val);
    const int x = 33496;
    std::cout << 33496;
    for (int i = 0; i < 33496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33496(int& val) {
    const int x = 33496;
    std::cout << 33496;
    for (int i = 0; i < 33496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33497(int& val) {
    func_in_33497(val);
    const int x = 33497;
    std::cout << 33497;
    for (int i = 0; i < 33497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33497(int& val) {
    const int x = 33497;
    std::cout << 33497;
    for (int i = 0; i < 33497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33498(int& val) {
    func_in_33498(val);
    const int x = 33498;
    std::cout << 33498;
    for (int i = 0; i < 33498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33498(int& val) {
    const int x = 33498;
    std::cout << 33498;
    for (int i = 0; i < 33498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33499(int& val) {
    func_in_33499(val);
    const int x = 33499;
    std::cout << 33499;
    for (int i = 0; i < 33499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33499(int& val) {
    const int x = 33499;
    std::cout << 33499;
    for (int i = 0; i < 33499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33500(int& val) {
    func_in_33500(val);
    const int x = 33500;
    std::cout << 33500;
    for (int i = 0; i < 33500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33500(int& val) {
    const int x = 33500;
    std::cout << 33500;
    for (int i = 0; i < 33500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33501(int& val) {
    func_in_33501(val);
    const int x = 33501;
    std::cout << 33501;
    for (int i = 0; i < 33501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33501(int& val) {
    const int x = 33501;
    std::cout << 33501;
    for (int i = 0; i < 33501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33502(int& val) {
    func_in_33502(val);
    const int x = 33502;
    std::cout << 33502;
    for (int i = 0; i < 33502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33502(int& val) {
    const int x = 33502;
    std::cout << 33502;
    for (int i = 0; i < 33502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33503(int& val) {
    func_in_33503(val);
    const int x = 33503;
    std::cout << 33503;
    for (int i = 0; i < 33503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33503(int& val) {
    const int x = 33503;
    std::cout << 33503;
    for (int i = 0; i < 33503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33504(int& val) {
    func_in_33504(val);
    const int x = 33504;
    std::cout << 33504;
    for (int i = 0; i < 33504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33504(int& val) {
    const int x = 33504;
    std::cout << 33504;
    for (int i = 0; i < 33504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33505(int& val) {
    func_in_33505(val);
    const int x = 33505;
    std::cout << 33505;
    for (int i = 0; i < 33505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33505(int& val) {
    const int x = 33505;
    std::cout << 33505;
    for (int i = 0; i < 33505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33506(int& val) {
    func_in_33506(val);
    const int x = 33506;
    std::cout << 33506;
    for (int i = 0; i < 33506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33506(int& val) {
    const int x = 33506;
    std::cout << 33506;
    for (int i = 0; i < 33506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33507(int& val) {
    func_in_33507(val);
    const int x = 33507;
    std::cout << 33507;
    for (int i = 0; i < 33507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33507(int& val) {
    const int x = 33507;
    std::cout << 33507;
    for (int i = 0; i < 33507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33508(int& val) {
    func_in_33508(val);
    const int x = 33508;
    std::cout << 33508;
    for (int i = 0; i < 33508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33508(int& val) {
    const int x = 33508;
    std::cout << 33508;
    for (int i = 0; i < 33508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33509(int& val) {
    func_in_33509(val);
    const int x = 33509;
    std::cout << 33509;
    for (int i = 0; i < 33509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33509(int& val) {
    const int x = 33509;
    std::cout << 33509;
    for (int i = 0; i < 33509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33510(int& val) {
    func_in_33510(val);
    const int x = 33510;
    std::cout << 33510;
    for (int i = 0; i < 33510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33510(int& val) {
    const int x = 33510;
    std::cout << 33510;
    for (int i = 0; i < 33510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33511(int& val) {
    func_in_33511(val);
    const int x = 33511;
    std::cout << 33511;
    for (int i = 0; i < 33511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33511(int& val) {
    const int x = 33511;
    std::cout << 33511;
    for (int i = 0; i < 33511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33512(int& val) {
    func_in_33512(val);
    const int x = 33512;
    std::cout << 33512;
    for (int i = 0; i < 33512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33512(int& val) {
    const int x = 33512;
    std::cout << 33512;
    for (int i = 0; i < 33512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33513(int& val) {
    func_in_33513(val);
    const int x = 33513;
    std::cout << 33513;
    for (int i = 0; i < 33513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33513(int& val) {
    const int x = 33513;
    std::cout << 33513;
    for (int i = 0; i < 33513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33514(int& val) {
    func_in_33514(val);
    const int x = 33514;
    std::cout << 33514;
    for (int i = 0; i < 33514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33514(int& val) {
    const int x = 33514;
    std::cout << 33514;
    for (int i = 0; i < 33514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33515(int& val) {
    func_in_33515(val);
    const int x = 33515;
    std::cout << 33515;
    for (int i = 0; i < 33515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33515(int& val) {
    const int x = 33515;
    std::cout << 33515;
    for (int i = 0; i < 33515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33516(int& val) {
    func_in_33516(val);
    const int x = 33516;
    std::cout << 33516;
    for (int i = 0; i < 33516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33516(int& val) {
    const int x = 33516;
    std::cout << 33516;
    for (int i = 0; i < 33516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33517(int& val) {
    func_in_33517(val);
    const int x = 33517;
    std::cout << 33517;
    for (int i = 0; i < 33517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33517(int& val) {
    const int x = 33517;
    std::cout << 33517;
    for (int i = 0; i < 33517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33518(int& val) {
    func_in_33518(val);
    const int x = 33518;
    std::cout << 33518;
    for (int i = 0; i < 33518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33518(int& val) {
    const int x = 33518;
    std::cout << 33518;
    for (int i = 0; i < 33518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33519(int& val) {
    func_in_33519(val);
    const int x = 33519;
    std::cout << 33519;
    for (int i = 0; i < 33519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33519(int& val) {
    const int x = 33519;
    std::cout << 33519;
    for (int i = 0; i < 33519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33520(int& val) {
    func_in_33520(val);
    const int x = 33520;
    std::cout << 33520;
    for (int i = 0; i < 33520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33520(int& val) {
    const int x = 33520;
    std::cout << 33520;
    for (int i = 0; i < 33520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33521(int& val) {
    func_in_33521(val);
    const int x = 33521;
    std::cout << 33521;
    for (int i = 0; i < 33521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33521(int& val) {
    const int x = 33521;
    std::cout << 33521;
    for (int i = 0; i < 33521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33522(int& val) {
    func_in_33522(val);
    const int x = 33522;
    std::cout << 33522;
    for (int i = 0; i < 33522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33522(int& val) {
    const int x = 33522;
    std::cout << 33522;
    for (int i = 0; i < 33522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33523(int& val) {
    func_in_33523(val);
    const int x = 33523;
    std::cout << 33523;
    for (int i = 0; i < 33523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33523(int& val) {
    const int x = 33523;
    std::cout << 33523;
    for (int i = 0; i < 33523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33524(int& val) {
    func_in_33524(val);
    const int x = 33524;
    std::cout << 33524;
    for (int i = 0; i < 33524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33524(int& val) {
    const int x = 33524;
    std::cout << 33524;
    for (int i = 0; i < 33524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33525(int& val) {
    func_in_33525(val);
    const int x = 33525;
    std::cout << 33525;
    for (int i = 0; i < 33525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33525(int& val) {
    const int x = 33525;
    std::cout << 33525;
    for (int i = 0; i < 33525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33526(int& val) {
    func_in_33526(val);
    const int x = 33526;
    std::cout << 33526;
    for (int i = 0; i < 33526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33526(int& val) {
    const int x = 33526;
    std::cout << 33526;
    for (int i = 0; i < 33526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33527(int& val) {
    func_in_33527(val);
    const int x = 33527;
    std::cout << 33527;
    for (int i = 0; i < 33527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33527(int& val) {
    const int x = 33527;
    std::cout << 33527;
    for (int i = 0; i < 33527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33528(int& val) {
    func_in_33528(val);
    const int x = 33528;
    std::cout << 33528;
    for (int i = 0; i < 33528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33528(int& val) {
    const int x = 33528;
    std::cout << 33528;
    for (int i = 0; i < 33528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33529(int& val) {
    func_in_33529(val);
    const int x = 33529;
    std::cout << 33529;
    for (int i = 0; i < 33529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33529(int& val) {
    const int x = 33529;
    std::cout << 33529;
    for (int i = 0; i < 33529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33530(int& val) {
    func_in_33530(val);
    const int x = 33530;
    std::cout << 33530;
    for (int i = 0; i < 33530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33530(int& val) {
    const int x = 33530;
    std::cout << 33530;
    for (int i = 0; i < 33530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33531(int& val) {
    func_in_33531(val);
    const int x = 33531;
    std::cout << 33531;
    for (int i = 0; i < 33531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33531(int& val) {
    const int x = 33531;
    std::cout << 33531;
    for (int i = 0; i < 33531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33532(int& val) {
    func_in_33532(val);
    const int x = 33532;
    std::cout << 33532;
    for (int i = 0; i < 33532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33532(int& val) {
    const int x = 33532;
    std::cout << 33532;
    for (int i = 0; i < 33532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33533(int& val) {
    func_in_33533(val);
    const int x = 33533;
    std::cout << 33533;
    for (int i = 0; i < 33533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33533(int& val) {
    const int x = 33533;
    std::cout << 33533;
    for (int i = 0; i < 33533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33534(int& val) {
    func_in_33534(val);
    const int x = 33534;
    std::cout << 33534;
    for (int i = 0; i < 33534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33534(int& val) {
    const int x = 33534;
    std::cout << 33534;
    for (int i = 0; i < 33534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33535(int& val) {
    func_in_33535(val);
    const int x = 33535;
    std::cout << 33535;
    for (int i = 0; i < 33535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33535(int& val) {
    const int x = 33535;
    std::cout << 33535;
    for (int i = 0; i < 33535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33536(int& val) {
    func_in_33536(val);
    const int x = 33536;
    std::cout << 33536;
    for (int i = 0; i < 33536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33536(int& val) {
    const int x = 33536;
    std::cout << 33536;
    for (int i = 0; i < 33536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33537(int& val) {
    func_in_33537(val);
    const int x = 33537;
    std::cout << 33537;
    for (int i = 0; i < 33537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33537(int& val) {
    const int x = 33537;
    std::cout << 33537;
    for (int i = 0; i < 33537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33538(int& val) {
    func_in_33538(val);
    const int x = 33538;
    std::cout << 33538;
    for (int i = 0; i < 33538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33538(int& val) {
    const int x = 33538;
    std::cout << 33538;
    for (int i = 0; i < 33538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33539(int& val) {
    func_in_33539(val);
    const int x = 33539;
    std::cout << 33539;
    for (int i = 0; i < 33539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33539(int& val) {
    const int x = 33539;
    std::cout << 33539;
    for (int i = 0; i < 33539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33540(int& val) {
    func_in_33540(val);
    const int x = 33540;
    std::cout << 33540;
    for (int i = 0; i < 33540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33540(int& val) {
    const int x = 33540;
    std::cout << 33540;
    for (int i = 0; i < 33540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33541(int& val) {
    func_in_33541(val);
    const int x = 33541;
    std::cout << 33541;
    for (int i = 0; i < 33541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33541(int& val) {
    const int x = 33541;
    std::cout << 33541;
    for (int i = 0; i < 33541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33542(int& val) {
    func_in_33542(val);
    const int x = 33542;
    std::cout << 33542;
    for (int i = 0; i < 33542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33542(int& val) {
    const int x = 33542;
    std::cout << 33542;
    for (int i = 0; i < 33542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33543(int& val) {
    func_in_33543(val);
    const int x = 33543;
    std::cout << 33543;
    for (int i = 0; i < 33543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33543(int& val) {
    const int x = 33543;
    std::cout << 33543;
    for (int i = 0; i < 33543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33544(int& val) {
    func_in_33544(val);
    const int x = 33544;
    std::cout << 33544;
    for (int i = 0; i < 33544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33544(int& val) {
    const int x = 33544;
    std::cout << 33544;
    for (int i = 0; i < 33544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33545(int& val) {
    func_in_33545(val);
    const int x = 33545;
    std::cout << 33545;
    for (int i = 0; i < 33545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33545(int& val) {
    const int x = 33545;
    std::cout << 33545;
    for (int i = 0; i < 33545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33546(int& val) {
    func_in_33546(val);
    const int x = 33546;
    std::cout << 33546;
    for (int i = 0; i < 33546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33546(int& val) {
    const int x = 33546;
    std::cout << 33546;
    for (int i = 0; i < 33546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33547(int& val) {
    func_in_33547(val);
    const int x = 33547;
    std::cout << 33547;
    for (int i = 0; i < 33547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33547(int& val) {
    const int x = 33547;
    std::cout << 33547;
    for (int i = 0; i < 33547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33548(int& val) {
    func_in_33548(val);
    const int x = 33548;
    std::cout << 33548;
    for (int i = 0; i < 33548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33548(int& val) {
    const int x = 33548;
    std::cout << 33548;
    for (int i = 0; i < 33548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33549(int& val) {
    func_in_33549(val);
    const int x = 33549;
    std::cout << 33549;
    for (int i = 0; i < 33549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33549(int& val) {
    const int x = 33549;
    std::cout << 33549;
    for (int i = 0; i < 33549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33550(int& val) {
    func_in_33550(val);
    const int x = 33550;
    std::cout << 33550;
    for (int i = 0; i < 33550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33550(int& val) {
    const int x = 33550;
    std::cout << 33550;
    for (int i = 0; i < 33550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33551(int& val) {
    func_in_33551(val);
    const int x = 33551;
    std::cout << 33551;
    for (int i = 0; i < 33551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33551(int& val) {
    const int x = 33551;
    std::cout << 33551;
    for (int i = 0; i < 33551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33552(int& val) {
    func_in_33552(val);
    const int x = 33552;
    std::cout << 33552;
    for (int i = 0; i < 33552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33552(int& val) {
    const int x = 33552;
    std::cout << 33552;
    for (int i = 0; i < 33552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33553(int& val) {
    func_in_33553(val);
    const int x = 33553;
    std::cout << 33553;
    for (int i = 0; i < 33553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33553(int& val) {
    const int x = 33553;
    std::cout << 33553;
    for (int i = 0; i < 33553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33554(int& val) {
    func_in_33554(val);
    const int x = 33554;
    std::cout << 33554;
    for (int i = 0; i < 33554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33554(int& val) {
    const int x = 33554;
    std::cout << 33554;
    for (int i = 0; i < 33554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33555(int& val) {
    func_in_33555(val);
    const int x = 33555;
    std::cout << 33555;
    for (int i = 0; i < 33555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33555(int& val) {
    const int x = 33555;
    std::cout << 33555;
    for (int i = 0; i < 33555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33556(int& val) {
    func_in_33556(val);
    const int x = 33556;
    std::cout << 33556;
    for (int i = 0; i < 33556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33556(int& val) {
    const int x = 33556;
    std::cout << 33556;
    for (int i = 0; i < 33556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33557(int& val) {
    func_in_33557(val);
    const int x = 33557;
    std::cout << 33557;
    for (int i = 0; i < 33557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33557(int& val) {
    const int x = 33557;
    std::cout << 33557;
    for (int i = 0; i < 33557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33558(int& val) {
    func_in_33558(val);
    const int x = 33558;
    std::cout << 33558;
    for (int i = 0; i < 33558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33558(int& val) {
    const int x = 33558;
    std::cout << 33558;
    for (int i = 0; i < 33558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33559(int& val) {
    func_in_33559(val);
    const int x = 33559;
    std::cout << 33559;
    for (int i = 0; i < 33559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33559(int& val) {
    const int x = 33559;
    std::cout << 33559;
    for (int i = 0; i < 33559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33560(int& val) {
    func_in_33560(val);
    const int x = 33560;
    std::cout << 33560;
    for (int i = 0; i < 33560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33560(int& val) {
    const int x = 33560;
    std::cout << 33560;
    for (int i = 0; i < 33560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33561(int& val) {
    func_in_33561(val);
    const int x = 33561;
    std::cout << 33561;
    for (int i = 0; i < 33561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33561(int& val) {
    const int x = 33561;
    std::cout << 33561;
    for (int i = 0; i < 33561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33562(int& val) {
    func_in_33562(val);
    const int x = 33562;
    std::cout << 33562;
    for (int i = 0; i < 33562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33562(int& val) {
    const int x = 33562;
    std::cout << 33562;
    for (int i = 0; i < 33562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33563(int& val) {
    func_in_33563(val);
    const int x = 33563;
    std::cout << 33563;
    for (int i = 0; i < 33563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33563(int& val) {
    const int x = 33563;
    std::cout << 33563;
    for (int i = 0; i < 33563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33564(int& val) {
    func_in_33564(val);
    const int x = 33564;
    std::cout << 33564;
    for (int i = 0; i < 33564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33564(int& val) {
    const int x = 33564;
    std::cout << 33564;
    for (int i = 0; i < 33564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33565(int& val) {
    func_in_33565(val);
    const int x = 33565;
    std::cout << 33565;
    for (int i = 0; i < 33565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33565(int& val) {
    const int x = 33565;
    std::cout << 33565;
    for (int i = 0; i < 33565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33566(int& val) {
    func_in_33566(val);
    const int x = 33566;
    std::cout << 33566;
    for (int i = 0; i < 33566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33566(int& val) {
    const int x = 33566;
    std::cout << 33566;
    for (int i = 0; i < 33566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33567(int& val) {
    func_in_33567(val);
    const int x = 33567;
    std::cout << 33567;
    for (int i = 0; i < 33567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33567(int& val) {
    const int x = 33567;
    std::cout << 33567;
    for (int i = 0; i < 33567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33568(int& val) {
    func_in_33568(val);
    const int x = 33568;
    std::cout << 33568;
    for (int i = 0; i < 33568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33568(int& val) {
    const int x = 33568;
    std::cout << 33568;
    for (int i = 0; i < 33568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33569(int& val) {
    func_in_33569(val);
    const int x = 33569;
    std::cout << 33569;
    for (int i = 0; i < 33569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33569(int& val) {
    const int x = 33569;
    std::cout << 33569;
    for (int i = 0; i < 33569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33570(int& val) {
    func_in_33570(val);
    const int x = 33570;
    std::cout << 33570;
    for (int i = 0; i < 33570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33570(int& val) {
    const int x = 33570;
    std::cout << 33570;
    for (int i = 0; i < 33570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33571(int& val) {
    func_in_33571(val);
    const int x = 33571;
    std::cout << 33571;
    for (int i = 0; i < 33571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33571(int& val) {
    const int x = 33571;
    std::cout << 33571;
    for (int i = 0; i < 33571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33572(int& val) {
    func_in_33572(val);
    const int x = 33572;
    std::cout << 33572;
    for (int i = 0; i < 33572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33572(int& val) {
    const int x = 33572;
    std::cout << 33572;
    for (int i = 0; i < 33572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33573(int& val) {
    func_in_33573(val);
    const int x = 33573;
    std::cout << 33573;
    for (int i = 0; i < 33573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33573(int& val) {
    const int x = 33573;
    std::cout << 33573;
    for (int i = 0; i < 33573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33574(int& val) {
    func_in_33574(val);
    const int x = 33574;
    std::cout << 33574;
    for (int i = 0; i < 33574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33574(int& val) {
    const int x = 33574;
    std::cout << 33574;
    for (int i = 0; i < 33574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33575(int& val) {
    func_in_33575(val);
    const int x = 33575;
    std::cout << 33575;
    for (int i = 0; i < 33575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33575(int& val) {
    const int x = 33575;
    std::cout << 33575;
    for (int i = 0; i < 33575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33576(int& val) {
    func_in_33576(val);
    const int x = 33576;
    std::cout << 33576;
    for (int i = 0; i < 33576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33576(int& val) {
    const int x = 33576;
    std::cout << 33576;
    for (int i = 0; i < 33576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33577(int& val) {
    func_in_33577(val);
    const int x = 33577;
    std::cout << 33577;
    for (int i = 0; i < 33577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33577(int& val) {
    const int x = 33577;
    std::cout << 33577;
    for (int i = 0; i < 33577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33578(int& val) {
    func_in_33578(val);
    const int x = 33578;
    std::cout << 33578;
    for (int i = 0; i < 33578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33578(int& val) {
    const int x = 33578;
    std::cout << 33578;
    for (int i = 0; i < 33578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33579(int& val) {
    func_in_33579(val);
    const int x = 33579;
    std::cout << 33579;
    for (int i = 0; i < 33579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33579(int& val) {
    const int x = 33579;
    std::cout << 33579;
    for (int i = 0; i < 33579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33580(int& val) {
    func_in_33580(val);
    const int x = 33580;
    std::cout << 33580;
    for (int i = 0; i < 33580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33580(int& val) {
    const int x = 33580;
    std::cout << 33580;
    for (int i = 0; i < 33580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33581(int& val) {
    func_in_33581(val);
    const int x = 33581;
    std::cout << 33581;
    for (int i = 0; i < 33581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33581(int& val) {
    const int x = 33581;
    std::cout << 33581;
    for (int i = 0; i < 33581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33582(int& val) {
    func_in_33582(val);
    const int x = 33582;
    std::cout << 33582;
    for (int i = 0; i < 33582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33582(int& val) {
    const int x = 33582;
    std::cout << 33582;
    for (int i = 0; i < 33582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33583(int& val) {
    func_in_33583(val);
    const int x = 33583;
    std::cout << 33583;
    for (int i = 0; i < 33583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33583(int& val) {
    const int x = 33583;
    std::cout << 33583;
    for (int i = 0; i < 33583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33584(int& val) {
    func_in_33584(val);
    const int x = 33584;
    std::cout << 33584;
    for (int i = 0; i < 33584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33584(int& val) {
    const int x = 33584;
    std::cout << 33584;
    for (int i = 0; i < 33584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33585(int& val) {
    func_in_33585(val);
    const int x = 33585;
    std::cout << 33585;
    for (int i = 0; i < 33585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33585(int& val) {
    const int x = 33585;
    std::cout << 33585;
    for (int i = 0; i < 33585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33586(int& val) {
    func_in_33586(val);
    const int x = 33586;
    std::cout << 33586;
    for (int i = 0; i < 33586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33586(int& val) {
    const int x = 33586;
    std::cout << 33586;
    for (int i = 0; i < 33586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33587(int& val) {
    func_in_33587(val);
    const int x = 33587;
    std::cout << 33587;
    for (int i = 0; i < 33587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33587(int& val) {
    const int x = 33587;
    std::cout << 33587;
    for (int i = 0; i < 33587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33588(int& val) {
    func_in_33588(val);
    const int x = 33588;
    std::cout << 33588;
    for (int i = 0; i < 33588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33588(int& val) {
    const int x = 33588;
    std::cout << 33588;
    for (int i = 0; i < 33588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33589(int& val) {
    func_in_33589(val);
    const int x = 33589;
    std::cout << 33589;
    for (int i = 0; i < 33589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33589(int& val) {
    const int x = 33589;
    std::cout << 33589;
    for (int i = 0; i < 33589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33590(int& val) {
    func_in_33590(val);
    const int x = 33590;
    std::cout << 33590;
    for (int i = 0; i < 33590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33590(int& val) {
    const int x = 33590;
    std::cout << 33590;
    for (int i = 0; i < 33590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33591(int& val) {
    func_in_33591(val);
    const int x = 33591;
    std::cout << 33591;
    for (int i = 0; i < 33591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33591(int& val) {
    const int x = 33591;
    std::cout << 33591;
    for (int i = 0; i < 33591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33592(int& val) {
    func_in_33592(val);
    const int x = 33592;
    std::cout << 33592;
    for (int i = 0; i < 33592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33592(int& val) {
    const int x = 33592;
    std::cout << 33592;
    for (int i = 0; i < 33592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33593(int& val) {
    func_in_33593(val);
    const int x = 33593;
    std::cout << 33593;
    for (int i = 0; i < 33593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33593(int& val) {
    const int x = 33593;
    std::cout << 33593;
    for (int i = 0; i < 33593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33594(int& val) {
    func_in_33594(val);
    const int x = 33594;
    std::cout << 33594;
    for (int i = 0; i < 33594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33594(int& val) {
    const int x = 33594;
    std::cout << 33594;
    for (int i = 0; i < 33594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33595(int& val) {
    func_in_33595(val);
    const int x = 33595;
    std::cout << 33595;
    for (int i = 0; i < 33595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33595(int& val) {
    const int x = 33595;
    std::cout << 33595;
    for (int i = 0; i < 33595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33596(int& val) {
    func_in_33596(val);
    const int x = 33596;
    std::cout << 33596;
    for (int i = 0; i < 33596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33596(int& val) {
    const int x = 33596;
    std::cout << 33596;
    for (int i = 0; i < 33596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33597(int& val) {
    func_in_33597(val);
    const int x = 33597;
    std::cout << 33597;
    for (int i = 0; i < 33597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33597(int& val) {
    const int x = 33597;
    std::cout << 33597;
    for (int i = 0; i < 33597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33598(int& val) {
    func_in_33598(val);
    const int x = 33598;
    std::cout << 33598;
    for (int i = 0; i < 33598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33598(int& val) {
    const int x = 33598;
    std::cout << 33598;
    for (int i = 0; i < 33598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33599(int& val) {
    func_in_33599(val);
    const int x = 33599;
    std::cout << 33599;
    for (int i = 0; i < 33599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33599(int& val) {
    const int x = 33599;
    std::cout << 33599;
    for (int i = 0; i < 33599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33600(int& val) {
    func_in_33600(val);
    const int x = 33600;
    std::cout << 33600;
    for (int i = 0; i < 33600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33600(int& val) {
    const int x = 33600;
    std::cout << 33600;
    for (int i = 0; i < 33600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33601(int& val) {
    func_in_33601(val);
    const int x = 33601;
    std::cout << 33601;
    for (int i = 0; i < 33601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33601(int& val) {
    const int x = 33601;
    std::cout << 33601;
    for (int i = 0; i < 33601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33602(int& val) {
    func_in_33602(val);
    const int x = 33602;
    std::cout << 33602;
    for (int i = 0; i < 33602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33602(int& val) {
    const int x = 33602;
    std::cout << 33602;
    for (int i = 0; i < 33602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33603(int& val) {
    func_in_33603(val);
    const int x = 33603;
    std::cout << 33603;
    for (int i = 0; i < 33603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33603(int& val) {
    const int x = 33603;
    std::cout << 33603;
    for (int i = 0; i < 33603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33604(int& val) {
    func_in_33604(val);
    const int x = 33604;
    std::cout << 33604;
    for (int i = 0; i < 33604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33604(int& val) {
    const int x = 33604;
    std::cout << 33604;
    for (int i = 0; i < 33604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33605(int& val) {
    func_in_33605(val);
    const int x = 33605;
    std::cout << 33605;
    for (int i = 0; i < 33605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33605(int& val) {
    const int x = 33605;
    std::cout << 33605;
    for (int i = 0; i < 33605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33606(int& val) {
    func_in_33606(val);
    const int x = 33606;
    std::cout << 33606;
    for (int i = 0; i < 33606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33606(int& val) {
    const int x = 33606;
    std::cout << 33606;
    for (int i = 0; i < 33606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33607(int& val) {
    func_in_33607(val);
    const int x = 33607;
    std::cout << 33607;
    for (int i = 0; i < 33607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33607(int& val) {
    const int x = 33607;
    std::cout << 33607;
    for (int i = 0; i < 33607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33608(int& val) {
    func_in_33608(val);
    const int x = 33608;
    std::cout << 33608;
    for (int i = 0; i < 33608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33608(int& val) {
    const int x = 33608;
    std::cout << 33608;
    for (int i = 0; i < 33608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33609(int& val) {
    func_in_33609(val);
    const int x = 33609;
    std::cout << 33609;
    for (int i = 0; i < 33609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33609(int& val) {
    const int x = 33609;
    std::cout << 33609;
    for (int i = 0; i < 33609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33610(int& val) {
    func_in_33610(val);
    const int x = 33610;
    std::cout << 33610;
    for (int i = 0; i < 33610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33610(int& val) {
    const int x = 33610;
    std::cout << 33610;
    for (int i = 0; i < 33610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33611(int& val) {
    func_in_33611(val);
    const int x = 33611;
    std::cout << 33611;
    for (int i = 0; i < 33611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33611(int& val) {
    const int x = 33611;
    std::cout << 33611;
    for (int i = 0; i < 33611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33612(int& val) {
    func_in_33612(val);
    const int x = 33612;
    std::cout << 33612;
    for (int i = 0; i < 33612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33612(int& val) {
    const int x = 33612;
    std::cout << 33612;
    for (int i = 0; i < 33612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33613(int& val) {
    func_in_33613(val);
    const int x = 33613;
    std::cout << 33613;
    for (int i = 0; i < 33613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33613(int& val) {
    const int x = 33613;
    std::cout << 33613;
    for (int i = 0; i < 33613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33614(int& val) {
    func_in_33614(val);
    const int x = 33614;
    std::cout << 33614;
    for (int i = 0; i < 33614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33614(int& val) {
    const int x = 33614;
    std::cout << 33614;
    for (int i = 0; i < 33614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33615(int& val) {
    func_in_33615(val);
    const int x = 33615;
    std::cout << 33615;
    for (int i = 0; i < 33615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33615(int& val) {
    const int x = 33615;
    std::cout << 33615;
    for (int i = 0; i < 33615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33616(int& val) {
    func_in_33616(val);
    const int x = 33616;
    std::cout << 33616;
    for (int i = 0; i < 33616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33616(int& val) {
    const int x = 33616;
    std::cout << 33616;
    for (int i = 0; i < 33616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33617(int& val) {
    func_in_33617(val);
    const int x = 33617;
    std::cout << 33617;
    for (int i = 0; i < 33617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33617(int& val) {
    const int x = 33617;
    std::cout << 33617;
    for (int i = 0; i < 33617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33618(int& val) {
    func_in_33618(val);
    const int x = 33618;
    std::cout << 33618;
    for (int i = 0; i < 33618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33618(int& val) {
    const int x = 33618;
    std::cout << 33618;
    for (int i = 0; i < 33618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33619(int& val) {
    func_in_33619(val);
    const int x = 33619;
    std::cout << 33619;
    for (int i = 0; i < 33619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33619(int& val) {
    const int x = 33619;
    std::cout << 33619;
    for (int i = 0; i < 33619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33620(int& val) {
    func_in_33620(val);
    const int x = 33620;
    std::cout << 33620;
    for (int i = 0; i < 33620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33620(int& val) {
    const int x = 33620;
    std::cout << 33620;
    for (int i = 0; i < 33620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33621(int& val) {
    func_in_33621(val);
    const int x = 33621;
    std::cout << 33621;
    for (int i = 0; i < 33621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33621(int& val) {
    const int x = 33621;
    std::cout << 33621;
    for (int i = 0; i < 33621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33622(int& val) {
    func_in_33622(val);
    const int x = 33622;
    std::cout << 33622;
    for (int i = 0; i < 33622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33622(int& val) {
    const int x = 33622;
    std::cout << 33622;
    for (int i = 0; i < 33622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33623(int& val) {
    func_in_33623(val);
    const int x = 33623;
    std::cout << 33623;
    for (int i = 0; i < 33623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33623(int& val) {
    const int x = 33623;
    std::cout << 33623;
    for (int i = 0; i < 33623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33624(int& val) {
    func_in_33624(val);
    const int x = 33624;
    std::cout << 33624;
    for (int i = 0; i < 33624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33624(int& val) {
    const int x = 33624;
    std::cout << 33624;
    for (int i = 0; i < 33624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33625(int& val) {
    func_in_33625(val);
    const int x = 33625;
    std::cout << 33625;
    for (int i = 0; i < 33625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33625(int& val) {
    const int x = 33625;
    std::cout << 33625;
    for (int i = 0; i < 33625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33626(int& val) {
    func_in_33626(val);
    const int x = 33626;
    std::cout << 33626;
    for (int i = 0; i < 33626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33626(int& val) {
    const int x = 33626;
    std::cout << 33626;
    for (int i = 0; i < 33626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33627(int& val) {
    func_in_33627(val);
    const int x = 33627;
    std::cout << 33627;
    for (int i = 0; i < 33627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33627(int& val) {
    const int x = 33627;
    std::cout << 33627;
    for (int i = 0; i < 33627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33628(int& val) {
    func_in_33628(val);
    const int x = 33628;
    std::cout << 33628;
    for (int i = 0; i < 33628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33628(int& val) {
    const int x = 33628;
    std::cout << 33628;
    for (int i = 0; i < 33628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33629(int& val) {
    func_in_33629(val);
    const int x = 33629;
    std::cout << 33629;
    for (int i = 0; i < 33629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33629(int& val) {
    const int x = 33629;
    std::cout << 33629;
    for (int i = 0; i < 33629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33630(int& val) {
    func_in_33630(val);
    const int x = 33630;
    std::cout << 33630;
    for (int i = 0; i < 33630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33630(int& val) {
    const int x = 33630;
    std::cout << 33630;
    for (int i = 0; i < 33630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33631(int& val) {
    func_in_33631(val);
    const int x = 33631;
    std::cout << 33631;
    for (int i = 0; i < 33631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33631(int& val) {
    const int x = 33631;
    std::cout << 33631;
    for (int i = 0; i < 33631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33632(int& val) {
    func_in_33632(val);
    const int x = 33632;
    std::cout << 33632;
    for (int i = 0; i < 33632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33632(int& val) {
    const int x = 33632;
    std::cout << 33632;
    for (int i = 0; i < 33632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33633(int& val) {
    func_in_33633(val);
    const int x = 33633;
    std::cout << 33633;
    for (int i = 0; i < 33633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33633(int& val) {
    const int x = 33633;
    std::cout << 33633;
    for (int i = 0; i < 33633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33634(int& val) {
    func_in_33634(val);
    const int x = 33634;
    std::cout << 33634;
    for (int i = 0; i < 33634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33634(int& val) {
    const int x = 33634;
    std::cout << 33634;
    for (int i = 0; i < 33634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33635(int& val) {
    func_in_33635(val);
    const int x = 33635;
    std::cout << 33635;
    for (int i = 0; i < 33635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33635(int& val) {
    const int x = 33635;
    std::cout << 33635;
    for (int i = 0; i < 33635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33636(int& val) {
    func_in_33636(val);
    const int x = 33636;
    std::cout << 33636;
    for (int i = 0; i < 33636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33636(int& val) {
    const int x = 33636;
    std::cout << 33636;
    for (int i = 0; i < 33636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33637(int& val) {
    func_in_33637(val);
    const int x = 33637;
    std::cout << 33637;
    for (int i = 0; i < 33637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33637(int& val) {
    const int x = 33637;
    std::cout << 33637;
    for (int i = 0; i < 33637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33638(int& val) {
    func_in_33638(val);
    const int x = 33638;
    std::cout << 33638;
    for (int i = 0; i < 33638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33638(int& val) {
    const int x = 33638;
    std::cout << 33638;
    for (int i = 0; i < 33638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33639(int& val) {
    func_in_33639(val);
    const int x = 33639;
    std::cout << 33639;
    for (int i = 0; i < 33639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33639(int& val) {
    const int x = 33639;
    std::cout << 33639;
    for (int i = 0; i < 33639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33640(int& val) {
    func_in_33640(val);
    const int x = 33640;
    std::cout << 33640;
    for (int i = 0; i < 33640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33640(int& val) {
    const int x = 33640;
    std::cout << 33640;
    for (int i = 0; i < 33640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33641(int& val) {
    func_in_33641(val);
    const int x = 33641;
    std::cout << 33641;
    for (int i = 0; i < 33641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33641(int& val) {
    const int x = 33641;
    std::cout << 33641;
    for (int i = 0; i < 33641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33642(int& val) {
    func_in_33642(val);
    const int x = 33642;
    std::cout << 33642;
    for (int i = 0; i < 33642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33642(int& val) {
    const int x = 33642;
    std::cout << 33642;
    for (int i = 0; i < 33642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33643(int& val) {
    func_in_33643(val);
    const int x = 33643;
    std::cout << 33643;
    for (int i = 0; i < 33643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33643(int& val) {
    const int x = 33643;
    std::cout << 33643;
    for (int i = 0; i < 33643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33644(int& val) {
    func_in_33644(val);
    const int x = 33644;
    std::cout << 33644;
    for (int i = 0; i < 33644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33644(int& val) {
    const int x = 33644;
    std::cout << 33644;
    for (int i = 0; i < 33644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33645(int& val) {
    func_in_33645(val);
    const int x = 33645;
    std::cout << 33645;
    for (int i = 0; i < 33645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33645(int& val) {
    const int x = 33645;
    std::cout << 33645;
    for (int i = 0; i < 33645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33646(int& val) {
    func_in_33646(val);
    const int x = 33646;
    std::cout << 33646;
    for (int i = 0; i < 33646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33646(int& val) {
    const int x = 33646;
    std::cout << 33646;
    for (int i = 0; i < 33646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33647(int& val) {
    func_in_33647(val);
    const int x = 33647;
    std::cout << 33647;
    for (int i = 0; i < 33647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33647(int& val) {
    const int x = 33647;
    std::cout << 33647;
    for (int i = 0; i < 33647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33648(int& val) {
    func_in_33648(val);
    const int x = 33648;
    std::cout << 33648;
    for (int i = 0; i < 33648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33648(int& val) {
    const int x = 33648;
    std::cout << 33648;
    for (int i = 0; i < 33648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33649(int& val) {
    func_in_33649(val);
    const int x = 33649;
    std::cout << 33649;
    for (int i = 0; i < 33649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33649(int& val) {
    const int x = 33649;
    std::cout << 33649;
    for (int i = 0; i < 33649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33650(int& val) {
    func_in_33650(val);
    const int x = 33650;
    std::cout << 33650;
    for (int i = 0; i < 33650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33650(int& val) {
    const int x = 33650;
    std::cout << 33650;
    for (int i = 0; i < 33650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33651(int& val) {
    func_in_33651(val);
    const int x = 33651;
    std::cout << 33651;
    for (int i = 0; i < 33651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33651(int& val) {
    const int x = 33651;
    std::cout << 33651;
    for (int i = 0; i < 33651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33652(int& val) {
    func_in_33652(val);
    const int x = 33652;
    std::cout << 33652;
    for (int i = 0; i < 33652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33652(int& val) {
    const int x = 33652;
    std::cout << 33652;
    for (int i = 0; i < 33652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33653(int& val) {
    func_in_33653(val);
    const int x = 33653;
    std::cout << 33653;
    for (int i = 0; i < 33653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33653(int& val) {
    const int x = 33653;
    std::cout << 33653;
    for (int i = 0; i < 33653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33654(int& val) {
    func_in_33654(val);
    const int x = 33654;
    std::cout << 33654;
    for (int i = 0; i < 33654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33654(int& val) {
    const int x = 33654;
    std::cout << 33654;
    for (int i = 0; i < 33654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33655(int& val) {
    func_in_33655(val);
    const int x = 33655;
    std::cout << 33655;
    for (int i = 0; i < 33655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33655(int& val) {
    const int x = 33655;
    std::cout << 33655;
    for (int i = 0; i < 33655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33656(int& val) {
    func_in_33656(val);
    const int x = 33656;
    std::cout << 33656;
    for (int i = 0; i < 33656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33656(int& val) {
    const int x = 33656;
    std::cout << 33656;
    for (int i = 0; i < 33656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33657(int& val) {
    func_in_33657(val);
    const int x = 33657;
    std::cout << 33657;
    for (int i = 0; i < 33657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33657(int& val) {
    const int x = 33657;
    std::cout << 33657;
    for (int i = 0; i < 33657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33658(int& val) {
    func_in_33658(val);
    const int x = 33658;
    std::cout << 33658;
    for (int i = 0; i < 33658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33658(int& val) {
    const int x = 33658;
    std::cout << 33658;
    for (int i = 0; i < 33658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33659(int& val) {
    func_in_33659(val);
    const int x = 33659;
    std::cout << 33659;
    for (int i = 0; i < 33659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33659(int& val) {
    const int x = 33659;
    std::cout << 33659;
    for (int i = 0; i < 33659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33660(int& val) {
    func_in_33660(val);
    const int x = 33660;
    std::cout << 33660;
    for (int i = 0; i < 33660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33660(int& val) {
    const int x = 33660;
    std::cout << 33660;
    for (int i = 0; i < 33660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33661(int& val) {
    func_in_33661(val);
    const int x = 33661;
    std::cout << 33661;
    for (int i = 0; i < 33661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33661(int& val) {
    const int x = 33661;
    std::cout << 33661;
    for (int i = 0; i < 33661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33662(int& val) {
    func_in_33662(val);
    const int x = 33662;
    std::cout << 33662;
    for (int i = 0; i < 33662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33662(int& val) {
    const int x = 33662;
    std::cout << 33662;
    for (int i = 0; i < 33662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33663(int& val) {
    func_in_33663(val);
    const int x = 33663;
    std::cout << 33663;
    for (int i = 0; i < 33663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33663(int& val) {
    const int x = 33663;
    std::cout << 33663;
    for (int i = 0; i < 33663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33664(int& val) {
    func_in_33664(val);
    const int x = 33664;
    std::cout << 33664;
    for (int i = 0; i < 33664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33664(int& val) {
    const int x = 33664;
    std::cout << 33664;
    for (int i = 0; i < 33664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33665(int& val) {
    func_in_33665(val);
    const int x = 33665;
    std::cout << 33665;
    for (int i = 0; i < 33665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33665(int& val) {
    const int x = 33665;
    std::cout << 33665;
    for (int i = 0; i < 33665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33666(int& val) {
    func_in_33666(val);
    const int x = 33666;
    std::cout << 33666;
    for (int i = 0; i < 33666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33666(int& val) {
    const int x = 33666;
    std::cout << 33666;
    for (int i = 0; i < 33666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33667(int& val) {
    func_in_33667(val);
    const int x = 33667;
    std::cout << 33667;
    for (int i = 0; i < 33667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33667(int& val) {
    const int x = 33667;
    std::cout << 33667;
    for (int i = 0; i < 33667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33668(int& val) {
    func_in_33668(val);
    const int x = 33668;
    std::cout << 33668;
    for (int i = 0; i < 33668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33668(int& val) {
    const int x = 33668;
    std::cout << 33668;
    for (int i = 0; i < 33668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33669(int& val) {
    func_in_33669(val);
    const int x = 33669;
    std::cout << 33669;
    for (int i = 0; i < 33669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33669(int& val) {
    const int x = 33669;
    std::cout << 33669;
    for (int i = 0; i < 33669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33670(int& val) {
    func_in_33670(val);
    const int x = 33670;
    std::cout << 33670;
    for (int i = 0; i < 33670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33670(int& val) {
    const int x = 33670;
    std::cout << 33670;
    for (int i = 0; i < 33670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33671(int& val) {
    func_in_33671(val);
    const int x = 33671;
    std::cout << 33671;
    for (int i = 0; i < 33671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33671(int& val) {
    const int x = 33671;
    std::cout << 33671;
    for (int i = 0; i < 33671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33672(int& val) {
    func_in_33672(val);
    const int x = 33672;
    std::cout << 33672;
    for (int i = 0; i < 33672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33672(int& val) {
    const int x = 33672;
    std::cout << 33672;
    for (int i = 0; i < 33672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33673(int& val) {
    func_in_33673(val);
    const int x = 33673;
    std::cout << 33673;
    for (int i = 0; i < 33673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33673(int& val) {
    const int x = 33673;
    std::cout << 33673;
    for (int i = 0; i < 33673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33674(int& val) {
    func_in_33674(val);
    const int x = 33674;
    std::cout << 33674;
    for (int i = 0; i < 33674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33674(int& val) {
    const int x = 33674;
    std::cout << 33674;
    for (int i = 0; i < 33674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33675(int& val) {
    func_in_33675(val);
    const int x = 33675;
    std::cout << 33675;
    for (int i = 0; i < 33675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33675(int& val) {
    const int x = 33675;
    std::cout << 33675;
    for (int i = 0; i < 33675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33676(int& val) {
    func_in_33676(val);
    const int x = 33676;
    std::cout << 33676;
    for (int i = 0; i < 33676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33676(int& val) {
    const int x = 33676;
    std::cout << 33676;
    for (int i = 0; i < 33676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33677(int& val) {
    func_in_33677(val);
    const int x = 33677;
    std::cout << 33677;
    for (int i = 0; i < 33677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33677(int& val) {
    const int x = 33677;
    std::cout << 33677;
    for (int i = 0; i < 33677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33678(int& val) {
    func_in_33678(val);
    const int x = 33678;
    std::cout << 33678;
    for (int i = 0; i < 33678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33678(int& val) {
    const int x = 33678;
    std::cout << 33678;
    for (int i = 0; i < 33678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33679(int& val) {
    func_in_33679(val);
    const int x = 33679;
    std::cout << 33679;
    for (int i = 0; i < 33679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33679(int& val) {
    const int x = 33679;
    std::cout << 33679;
    for (int i = 0; i < 33679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33680(int& val) {
    func_in_33680(val);
    const int x = 33680;
    std::cout << 33680;
    for (int i = 0; i < 33680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33680(int& val) {
    const int x = 33680;
    std::cout << 33680;
    for (int i = 0; i < 33680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33681(int& val) {
    func_in_33681(val);
    const int x = 33681;
    std::cout << 33681;
    for (int i = 0; i < 33681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33681(int& val) {
    const int x = 33681;
    std::cout << 33681;
    for (int i = 0; i < 33681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33682(int& val) {
    func_in_33682(val);
    const int x = 33682;
    std::cout << 33682;
    for (int i = 0; i < 33682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33682(int& val) {
    const int x = 33682;
    std::cout << 33682;
    for (int i = 0; i < 33682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33683(int& val) {
    func_in_33683(val);
    const int x = 33683;
    std::cout << 33683;
    for (int i = 0; i < 33683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33683(int& val) {
    const int x = 33683;
    std::cout << 33683;
    for (int i = 0; i < 33683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33684(int& val) {
    func_in_33684(val);
    const int x = 33684;
    std::cout << 33684;
    for (int i = 0; i < 33684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33684(int& val) {
    const int x = 33684;
    std::cout << 33684;
    for (int i = 0; i < 33684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33685(int& val) {
    func_in_33685(val);
    const int x = 33685;
    std::cout << 33685;
    for (int i = 0; i < 33685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33685(int& val) {
    const int x = 33685;
    std::cout << 33685;
    for (int i = 0; i < 33685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33686(int& val) {
    func_in_33686(val);
    const int x = 33686;
    std::cout << 33686;
    for (int i = 0; i < 33686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33686(int& val) {
    const int x = 33686;
    std::cout << 33686;
    for (int i = 0; i < 33686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33687(int& val) {
    func_in_33687(val);
    const int x = 33687;
    std::cout << 33687;
    for (int i = 0; i < 33687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33687(int& val) {
    const int x = 33687;
    std::cout << 33687;
    for (int i = 0; i < 33687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33688(int& val) {
    func_in_33688(val);
    const int x = 33688;
    std::cout << 33688;
    for (int i = 0; i < 33688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33688(int& val) {
    const int x = 33688;
    std::cout << 33688;
    for (int i = 0; i < 33688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33689(int& val) {
    func_in_33689(val);
    const int x = 33689;
    std::cout << 33689;
    for (int i = 0; i < 33689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33689(int& val) {
    const int x = 33689;
    std::cout << 33689;
    for (int i = 0; i < 33689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33690(int& val) {
    func_in_33690(val);
    const int x = 33690;
    std::cout << 33690;
    for (int i = 0; i < 33690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33690(int& val) {
    const int x = 33690;
    std::cout << 33690;
    for (int i = 0; i < 33690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33691(int& val) {
    func_in_33691(val);
    const int x = 33691;
    std::cout << 33691;
    for (int i = 0; i < 33691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33691(int& val) {
    const int x = 33691;
    std::cout << 33691;
    for (int i = 0; i < 33691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33692(int& val) {
    func_in_33692(val);
    const int x = 33692;
    std::cout << 33692;
    for (int i = 0; i < 33692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33692(int& val) {
    const int x = 33692;
    std::cout << 33692;
    for (int i = 0; i < 33692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33693(int& val) {
    func_in_33693(val);
    const int x = 33693;
    std::cout << 33693;
    for (int i = 0; i < 33693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33693(int& val) {
    const int x = 33693;
    std::cout << 33693;
    for (int i = 0; i < 33693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33694(int& val) {
    func_in_33694(val);
    const int x = 33694;
    std::cout << 33694;
    for (int i = 0; i < 33694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33694(int& val) {
    const int x = 33694;
    std::cout << 33694;
    for (int i = 0; i < 33694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33695(int& val) {
    func_in_33695(val);
    const int x = 33695;
    std::cout << 33695;
    for (int i = 0; i < 33695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33695(int& val) {
    const int x = 33695;
    std::cout << 33695;
    for (int i = 0; i < 33695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33696(int& val) {
    func_in_33696(val);
    const int x = 33696;
    std::cout << 33696;
    for (int i = 0; i < 33696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33696(int& val) {
    const int x = 33696;
    std::cout << 33696;
    for (int i = 0; i < 33696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33697(int& val) {
    func_in_33697(val);
    const int x = 33697;
    std::cout << 33697;
    for (int i = 0; i < 33697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33697(int& val) {
    const int x = 33697;
    std::cout << 33697;
    for (int i = 0; i < 33697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33698(int& val) {
    func_in_33698(val);
    const int x = 33698;
    std::cout << 33698;
    for (int i = 0; i < 33698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33698(int& val) {
    const int x = 33698;
    std::cout << 33698;
    for (int i = 0; i < 33698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33699(int& val) {
    func_in_33699(val);
    const int x = 33699;
    std::cout << 33699;
    for (int i = 0; i < 33699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33699(int& val) {
    const int x = 33699;
    std::cout << 33699;
    for (int i = 0; i < 33699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33700(int& val) {
    func_in_33700(val);
    const int x = 33700;
    std::cout << 33700;
    for (int i = 0; i < 33700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33700(int& val) {
    const int x = 33700;
    std::cout << 33700;
    for (int i = 0; i < 33700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33701(int& val) {
    func_in_33701(val);
    const int x = 33701;
    std::cout << 33701;
    for (int i = 0; i < 33701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33701(int& val) {
    const int x = 33701;
    std::cout << 33701;
    for (int i = 0; i < 33701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33702(int& val) {
    func_in_33702(val);
    const int x = 33702;
    std::cout << 33702;
    for (int i = 0; i < 33702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33702(int& val) {
    const int x = 33702;
    std::cout << 33702;
    for (int i = 0; i < 33702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33703(int& val) {
    func_in_33703(val);
    const int x = 33703;
    std::cout << 33703;
    for (int i = 0; i < 33703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33703(int& val) {
    const int x = 33703;
    std::cout << 33703;
    for (int i = 0; i < 33703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33704(int& val) {
    func_in_33704(val);
    const int x = 33704;
    std::cout << 33704;
    for (int i = 0; i < 33704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33704(int& val) {
    const int x = 33704;
    std::cout << 33704;
    for (int i = 0; i < 33704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33705(int& val) {
    func_in_33705(val);
    const int x = 33705;
    std::cout << 33705;
    for (int i = 0; i < 33705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33705(int& val) {
    const int x = 33705;
    std::cout << 33705;
    for (int i = 0; i < 33705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33706(int& val) {
    func_in_33706(val);
    const int x = 33706;
    std::cout << 33706;
    for (int i = 0; i < 33706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33706(int& val) {
    const int x = 33706;
    std::cout << 33706;
    for (int i = 0; i < 33706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33707(int& val) {
    func_in_33707(val);
    const int x = 33707;
    std::cout << 33707;
    for (int i = 0; i < 33707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33707(int& val) {
    const int x = 33707;
    std::cout << 33707;
    for (int i = 0; i < 33707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33708(int& val) {
    func_in_33708(val);
    const int x = 33708;
    std::cout << 33708;
    for (int i = 0; i < 33708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33708(int& val) {
    const int x = 33708;
    std::cout << 33708;
    for (int i = 0; i < 33708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33709(int& val) {
    func_in_33709(val);
    const int x = 33709;
    std::cout << 33709;
    for (int i = 0; i < 33709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33709(int& val) {
    const int x = 33709;
    std::cout << 33709;
    for (int i = 0; i < 33709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33710(int& val) {
    func_in_33710(val);
    const int x = 33710;
    std::cout << 33710;
    for (int i = 0; i < 33710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33710(int& val) {
    const int x = 33710;
    std::cout << 33710;
    for (int i = 0; i < 33710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33711(int& val) {
    func_in_33711(val);
    const int x = 33711;
    std::cout << 33711;
    for (int i = 0; i < 33711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33711(int& val) {
    const int x = 33711;
    std::cout << 33711;
    for (int i = 0; i < 33711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33712(int& val) {
    func_in_33712(val);
    const int x = 33712;
    std::cout << 33712;
    for (int i = 0; i < 33712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33712(int& val) {
    const int x = 33712;
    std::cout << 33712;
    for (int i = 0; i < 33712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33713(int& val) {
    func_in_33713(val);
    const int x = 33713;
    std::cout << 33713;
    for (int i = 0; i < 33713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33713(int& val) {
    const int x = 33713;
    std::cout << 33713;
    for (int i = 0; i < 33713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33714(int& val) {
    func_in_33714(val);
    const int x = 33714;
    std::cout << 33714;
    for (int i = 0; i < 33714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33714(int& val) {
    const int x = 33714;
    std::cout << 33714;
    for (int i = 0; i < 33714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33715(int& val) {
    func_in_33715(val);
    const int x = 33715;
    std::cout << 33715;
    for (int i = 0; i < 33715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33715(int& val) {
    const int x = 33715;
    std::cout << 33715;
    for (int i = 0; i < 33715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33716(int& val) {
    func_in_33716(val);
    const int x = 33716;
    std::cout << 33716;
    for (int i = 0; i < 33716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33716(int& val) {
    const int x = 33716;
    std::cout << 33716;
    for (int i = 0; i < 33716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33717(int& val) {
    func_in_33717(val);
    const int x = 33717;
    std::cout << 33717;
    for (int i = 0; i < 33717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33717(int& val) {
    const int x = 33717;
    std::cout << 33717;
    for (int i = 0; i < 33717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33718(int& val) {
    func_in_33718(val);
    const int x = 33718;
    std::cout << 33718;
    for (int i = 0; i < 33718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33718(int& val) {
    const int x = 33718;
    std::cout << 33718;
    for (int i = 0; i < 33718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33719(int& val) {
    func_in_33719(val);
    const int x = 33719;
    std::cout << 33719;
    for (int i = 0; i < 33719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33719(int& val) {
    const int x = 33719;
    std::cout << 33719;
    for (int i = 0; i < 33719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33720(int& val) {
    func_in_33720(val);
    const int x = 33720;
    std::cout << 33720;
    for (int i = 0; i < 33720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33720(int& val) {
    const int x = 33720;
    std::cout << 33720;
    for (int i = 0; i < 33720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33721(int& val) {
    func_in_33721(val);
    const int x = 33721;
    std::cout << 33721;
    for (int i = 0; i < 33721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33721(int& val) {
    const int x = 33721;
    std::cout << 33721;
    for (int i = 0; i < 33721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33722(int& val) {
    func_in_33722(val);
    const int x = 33722;
    std::cout << 33722;
    for (int i = 0; i < 33722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33722(int& val) {
    const int x = 33722;
    std::cout << 33722;
    for (int i = 0; i < 33722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33723(int& val) {
    func_in_33723(val);
    const int x = 33723;
    std::cout << 33723;
    for (int i = 0; i < 33723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33723(int& val) {
    const int x = 33723;
    std::cout << 33723;
    for (int i = 0; i < 33723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33724(int& val) {
    func_in_33724(val);
    const int x = 33724;
    std::cout << 33724;
    for (int i = 0; i < 33724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33724(int& val) {
    const int x = 33724;
    std::cout << 33724;
    for (int i = 0; i < 33724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33725(int& val) {
    func_in_33725(val);
    const int x = 33725;
    std::cout << 33725;
    for (int i = 0; i < 33725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33725(int& val) {
    const int x = 33725;
    std::cout << 33725;
    for (int i = 0; i < 33725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33726(int& val) {
    func_in_33726(val);
    const int x = 33726;
    std::cout << 33726;
    for (int i = 0; i < 33726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33726(int& val) {
    const int x = 33726;
    std::cout << 33726;
    for (int i = 0; i < 33726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33727(int& val) {
    func_in_33727(val);
    const int x = 33727;
    std::cout << 33727;
    for (int i = 0; i < 33727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33727(int& val) {
    const int x = 33727;
    std::cout << 33727;
    for (int i = 0; i < 33727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33728(int& val) {
    func_in_33728(val);
    const int x = 33728;
    std::cout << 33728;
    for (int i = 0; i < 33728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33728(int& val) {
    const int x = 33728;
    std::cout << 33728;
    for (int i = 0; i < 33728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33729(int& val) {
    func_in_33729(val);
    const int x = 33729;
    std::cout << 33729;
    for (int i = 0; i < 33729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33729(int& val) {
    const int x = 33729;
    std::cout << 33729;
    for (int i = 0; i < 33729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33730(int& val) {
    func_in_33730(val);
    const int x = 33730;
    std::cout << 33730;
    for (int i = 0; i < 33730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33730(int& val) {
    const int x = 33730;
    std::cout << 33730;
    for (int i = 0; i < 33730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33731(int& val) {
    func_in_33731(val);
    const int x = 33731;
    std::cout << 33731;
    for (int i = 0; i < 33731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33731(int& val) {
    const int x = 33731;
    std::cout << 33731;
    for (int i = 0; i < 33731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33732(int& val) {
    func_in_33732(val);
    const int x = 33732;
    std::cout << 33732;
    for (int i = 0; i < 33732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33732(int& val) {
    const int x = 33732;
    std::cout << 33732;
    for (int i = 0; i < 33732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33733(int& val) {
    func_in_33733(val);
    const int x = 33733;
    std::cout << 33733;
    for (int i = 0; i < 33733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33733(int& val) {
    const int x = 33733;
    std::cout << 33733;
    for (int i = 0; i < 33733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33734(int& val) {
    func_in_33734(val);
    const int x = 33734;
    std::cout << 33734;
    for (int i = 0; i < 33734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33734(int& val) {
    const int x = 33734;
    std::cout << 33734;
    for (int i = 0; i < 33734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33735(int& val) {
    func_in_33735(val);
    const int x = 33735;
    std::cout << 33735;
    for (int i = 0; i < 33735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33735(int& val) {
    const int x = 33735;
    std::cout << 33735;
    for (int i = 0; i < 33735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33736(int& val) {
    func_in_33736(val);
    const int x = 33736;
    std::cout << 33736;
    for (int i = 0; i < 33736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33736(int& val) {
    const int x = 33736;
    std::cout << 33736;
    for (int i = 0; i < 33736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33737(int& val) {
    func_in_33737(val);
    const int x = 33737;
    std::cout << 33737;
    for (int i = 0; i < 33737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33737(int& val) {
    const int x = 33737;
    std::cout << 33737;
    for (int i = 0; i < 33737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33738(int& val) {
    func_in_33738(val);
    const int x = 33738;
    std::cout << 33738;
    for (int i = 0; i < 33738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33738(int& val) {
    const int x = 33738;
    std::cout << 33738;
    for (int i = 0; i < 33738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33739(int& val) {
    func_in_33739(val);
    const int x = 33739;
    std::cout << 33739;
    for (int i = 0; i < 33739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33739(int& val) {
    const int x = 33739;
    std::cout << 33739;
    for (int i = 0; i < 33739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33740(int& val) {
    func_in_33740(val);
    const int x = 33740;
    std::cout << 33740;
    for (int i = 0; i < 33740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33740(int& val) {
    const int x = 33740;
    std::cout << 33740;
    for (int i = 0; i < 33740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33741(int& val) {
    func_in_33741(val);
    const int x = 33741;
    std::cout << 33741;
    for (int i = 0; i < 33741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33741(int& val) {
    const int x = 33741;
    std::cout << 33741;
    for (int i = 0; i < 33741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33742(int& val) {
    func_in_33742(val);
    const int x = 33742;
    std::cout << 33742;
    for (int i = 0; i < 33742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33742(int& val) {
    const int x = 33742;
    std::cout << 33742;
    for (int i = 0; i < 33742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33743(int& val) {
    func_in_33743(val);
    const int x = 33743;
    std::cout << 33743;
    for (int i = 0; i < 33743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33743(int& val) {
    const int x = 33743;
    std::cout << 33743;
    for (int i = 0; i < 33743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33744(int& val) {
    func_in_33744(val);
    const int x = 33744;
    std::cout << 33744;
    for (int i = 0; i < 33744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33744(int& val) {
    const int x = 33744;
    std::cout << 33744;
    for (int i = 0; i < 33744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33745(int& val) {
    func_in_33745(val);
    const int x = 33745;
    std::cout << 33745;
    for (int i = 0; i < 33745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33745(int& val) {
    const int x = 33745;
    std::cout << 33745;
    for (int i = 0; i < 33745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33746(int& val) {
    func_in_33746(val);
    const int x = 33746;
    std::cout << 33746;
    for (int i = 0; i < 33746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33746(int& val) {
    const int x = 33746;
    std::cout << 33746;
    for (int i = 0; i < 33746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33747(int& val) {
    func_in_33747(val);
    const int x = 33747;
    std::cout << 33747;
    for (int i = 0; i < 33747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33747(int& val) {
    const int x = 33747;
    std::cout << 33747;
    for (int i = 0; i < 33747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33748(int& val) {
    func_in_33748(val);
    const int x = 33748;
    std::cout << 33748;
    for (int i = 0; i < 33748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33748(int& val) {
    const int x = 33748;
    std::cout << 33748;
    for (int i = 0; i < 33748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33749(int& val) {
    func_in_33749(val);
    const int x = 33749;
    std::cout << 33749;
    for (int i = 0; i < 33749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33749(int& val) {
    const int x = 33749;
    std::cout << 33749;
    for (int i = 0; i < 33749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33750(int& val) {
    func_in_33750(val);
    const int x = 33750;
    std::cout << 33750;
    for (int i = 0; i < 33750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33750(int& val) {
    const int x = 33750;
    std::cout << 33750;
    for (int i = 0; i < 33750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33751(int& val) {
    func_in_33751(val);
    const int x = 33751;
    std::cout << 33751;
    for (int i = 0; i < 33751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33751(int& val) {
    const int x = 33751;
    std::cout << 33751;
    for (int i = 0; i < 33751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33752(int& val) {
    func_in_33752(val);
    const int x = 33752;
    std::cout << 33752;
    for (int i = 0; i < 33752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33752(int& val) {
    const int x = 33752;
    std::cout << 33752;
    for (int i = 0; i < 33752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33753(int& val) {
    func_in_33753(val);
    const int x = 33753;
    std::cout << 33753;
    for (int i = 0; i < 33753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33753(int& val) {
    const int x = 33753;
    std::cout << 33753;
    for (int i = 0; i < 33753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33754(int& val) {
    func_in_33754(val);
    const int x = 33754;
    std::cout << 33754;
    for (int i = 0; i < 33754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33754(int& val) {
    const int x = 33754;
    std::cout << 33754;
    for (int i = 0; i < 33754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33755(int& val) {
    func_in_33755(val);
    const int x = 33755;
    std::cout << 33755;
    for (int i = 0; i < 33755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33755(int& val) {
    const int x = 33755;
    std::cout << 33755;
    for (int i = 0; i < 33755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33756(int& val) {
    func_in_33756(val);
    const int x = 33756;
    std::cout << 33756;
    for (int i = 0; i < 33756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33756(int& val) {
    const int x = 33756;
    std::cout << 33756;
    for (int i = 0; i < 33756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33757(int& val) {
    func_in_33757(val);
    const int x = 33757;
    std::cout << 33757;
    for (int i = 0; i < 33757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33757(int& val) {
    const int x = 33757;
    std::cout << 33757;
    for (int i = 0; i < 33757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33758(int& val) {
    func_in_33758(val);
    const int x = 33758;
    std::cout << 33758;
    for (int i = 0; i < 33758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33758(int& val) {
    const int x = 33758;
    std::cout << 33758;
    for (int i = 0; i < 33758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33759(int& val) {
    func_in_33759(val);
    const int x = 33759;
    std::cout << 33759;
    for (int i = 0; i < 33759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33759(int& val) {
    const int x = 33759;
    std::cout << 33759;
    for (int i = 0; i < 33759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33760(int& val) {
    func_in_33760(val);
    const int x = 33760;
    std::cout << 33760;
    for (int i = 0; i < 33760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33760(int& val) {
    const int x = 33760;
    std::cout << 33760;
    for (int i = 0; i < 33760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33761(int& val) {
    func_in_33761(val);
    const int x = 33761;
    std::cout << 33761;
    for (int i = 0; i < 33761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33761(int& val) {
    const int x = 33761;
    std::cout << 33761;
    for (int i = 0; i < 33761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33762(int& val) {
    func_in_33762(val);
    const int x = 33762;
    std::cout << 33762;
    for (int i = 0; i < 33762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33762(int& val) {
    const int x = 33762;
    std::cout << 33762;
    for (int i = 0; i < 33762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33763(int& val) {
    func_in_33763(val);
    const int x = 33763;
    std::cout << 33763;
    for (int i = 0; i < 33763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33763(int& val) {
    const int x = 33763;
    std::cout << 33763;
    for (int i = 0; i < 33763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33764(int& val) {
    func_in_33764(val);
    const int x = 33764;
    std::cout << 33764;
    for (int i = 0; i < 33764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33764(int& val) {
    const int x = 33764;
    std::cout << 33764;
    for (int i = 0; i < 33764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33765(int& val) {
    func_in_33765(val);
    const int x = 33765;
    std::cout << 33765;
    for (int i = 0; i < 33765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33765(int& val) {
    const int x = 33765;
    std::cout << 33765;
    for (int i = 0; i < 33765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33766(int& val) {
    func_in_33766(val);
    const int x = 33766;
    std::cout << 33766;
    for (int i = 0; i < 33766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33766(int& val) {
    const int x = 33766;
    std::cout << 33766;
    for (int i = 0; i < 33766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33767(int& val) {
    func_in_33767(val);
    const int x = 33767;
    std::cout << 33767;
    for (int i = 0; i < 33767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33767(int& val) {
    const int x = 33767;
    std::cout << 33767;
    for (int i = 0; i < 33767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33768(int& val) {
    func_in_33768(val);
    const int x = 33768;
    std::cout << 33768;
    for (int i = 0; i < 33768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33768(int& val) {
    const int x = 33768;
    std::cout << 33768;
    for (int i = 0; i < 33768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33769(int& val) {
    func_in_33769(val);
    const int x = 33769;
    std::cout << 33769;
    for (int i = 0; i < 33769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33769(int& val) {
    const int x = 33769;
    std::cout << 33769;
    for (int i = 0; i < 33769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33770(int& val) {
    func_in_33770(val);
    const int x = 33770;
    std::cout << 33770;
    for (int i = 0; i < 33770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33770(int& val) {
    const int x = 33770;
    std::cout << 33770;
    for (int i = 0; i < 33770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33771(int& val) {
    func_in_33771(val);
    const int x = 33771;
    std::cout << 33771;
    for (int i = 0; i < 33771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33771(int& val) {
    const int x = 33771;
    std::cout << 33771;
    for (int i = 0; i < 33771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33772(int& val) {
    func_in_33772(val);
    const int x = 33772;
    std::cout << 33772;
    for (int i = 0; i < 33772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33772(int& val) {
    const int x = 33772;
    std::cout << 33772;
    for (int i = 0; i < 33772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33773(int& val) {
    func_in_33773(val);
    const int x = 33773;
    std::cout << 33773;
    for (int i = 0; i < 33773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33773(int& val) {
    const int x = 33773;
    std::cout << 33773;
    for (int i = 0; i < 33773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33774(int& val) {
    func_in_33774(val);
    const int x = 33774;
    std::cout << 33774;
    for (int i = 0; i < 33774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33774(int& val) {
    const int x = 33774;
    std::cout << 33774;
    for (int i = 0; i < 33774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33775(int& val) {
    func_in_33775(val);
    const int x = 33775;
    std::cout << 33775;
    for (int i = 0; i < 33775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33775(int& val) {
    const int x = 33775;
    std::cout << 33775;
    for (int i = 0; i < 33775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33776(int& val) {
    func_in_33776(val);
    const int x = 33776;
    std::cout << 33776;
    for (int i = 0; i < 33776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33776(int& val) {
    const int x = 33776;
    std::cout << 33776;
    for (int i = 0; i < 33776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33777(int& val) {
    func_in_33777(val);
    const int x = 33777;
    std::cout << 33777;
    for (int i = 0; i < 33777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33777(int& val) {
    const int x = 33777;
    std::cout << 33777;
    for (int i = 0; i < 33777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33778(int& val) {
    func_in_33778(val);
    const int x = 33778;
    std::cout << 33778;
    for (int i = 0; i < 33778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33778(int& val) {
    const int x = 33778;
    std::cout << 33778;
    for (int i = 0; i < 33778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33779(int& val) {
    func_in_33779(val);
    const int x = 33779;
    std::cout << 33779;
    for (int i = 0; i < 33779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33779(int& val) {
    const int x = 33779;
    std::cout << 33779;
    for (int i = 0; i < 33779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33780(int& val) {
    func_in_33780(val);
    const int x = 33780;
    std::cout << 33780;
    for (int i = 0; i < 33780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33780(int& val) {
    const int x = 33780;
    std::cout << 33780;
    for (int i = 0; i < 33780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33781(int& val) {
    func_in_33781(val);
    const int x = 33781;
    std::cout << 33781;
    for (int i = 0; i < 33781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33781(int& val) {
    const int x = 33781;
    std::cout << 33781;
    for (int i = 0; i < 33781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33782(int& val) {
    func_in_33782(val);
    const int x = 33782;
    std::cout << 33782;
    for (int i = 0; i < 33782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33782(int& val) {
    const int x = 33782;
    std::cout << 33782;
    for (int i = 0; i < 33782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33783(int& val) {
    func_in_33783(val);
    const int x = 33783;
    std::cout << 33783;
    for (int i = 0; i < 33783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33783(int& val) {
    const int x = 33783;
    std::cout << 33783;
    for (int i = 0; i < 33783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33784(int& val) {
    func_in_33784(val);
    const int x = 33784;
    std::cout << 33784;
    for (int i = 0; i < 33784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33784(int& val) {
    const int x = 33784;
    std::cout << 33784;
    for (int i = 0; i < 33784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33785(int& val) {
    func_in_33785(val);
    const int x = 33785;
    std::cout << 33785;
    for (int i = 0; i < 33785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33785(int& val) {
    const int x = 33785;
    std::cout << 33785;
    for (int i = 0; i < 33785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33786(int& val) {
    func_in_33786(val);
    const int x = 33786;
    std::cout << 33786;
    for (int i = 0; i < 33786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33786(int& val) {
    const int x = 33786;
    std::cout << 33786;
    for (int i = 0; i < 33786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33787(int& val) {
    func_in_33787(val);
    const int x = 33787;
    std::cout << 33787;
    for (int i = 0; i < 33787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33787(int& val) {
    const int x = 33787;
    std::cout << 33787;
    for (int i = 0; i < 33787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33788(int& val) {
    func_in_33788(val);
    const int x = 33788;
    std::cout << 33788;
    for (int i = 0; i < 33788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33788(int& val) {
    const int x = 33788;
    std::cout << 33788;
    for (int i = 0; i < 33788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33789(int& val) {
    func_in_33789(val);
    const int x = 33789;
    std::cout << 33789;
    for (int i = 0; i < 33789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33789(int& val) {
    const int x = 33789;
    std::cout << 33789;
    for (int i = 0; i < 33789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33790(int& val) {
    func_in_33790(val);
    const int x = 33790;
    std::cout << 33790;
    for (int i = 0; i < 33790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33790(int& val) {
    const int x = 33790;
    std::cout << 33790;
    for (int i = 0; i < 33790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33791(int& val) {
    func_in_33791(val);
    const int x = 33791;
    std::cout << 33791;
    for (int i = 0; i < 33791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33791(int& val) {
    const int x = 33791;
    std::cout << 33791;
    for (int i = 0; i < 33791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33792(int& val) {
    func_in_33792(val);
    const int x = 33792;
    std::cout << 33792;
    for (int i = 0; i < 33792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33792(int& val) {
    const int x = 33792;
    std::cout << 33792;
    for (int i = 0; i < 33792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33793(int& val) {
    func_in_33793(val);
    const int x = 33793;
    std::cout << 33793;
    for (int i = 0; i < 33793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33793(int& val) {
    const int x = 33793;
    std::cout << 33793;
    for (int i = 0; i < 33793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33794(int& val) {
    func_in_33794(val);
    const int x = 33794;
    std::cout << 33794;
    for (int i = 0; i < 33794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33794(int& val) {
    const int x = 33794;
    std::cout << 33794;
    for (int i = 0; i < 33794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33795(int& val) {
    func_in_33795(val);
    const int x = 33795;
    std::cout << 33795;
    for (int i = 0; i < 33795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33795(int& val) {
    const int x = 33795;
    std::cout << 33795;
    for (int i = 0; i < 33795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33796(int& val) {
    func_in_33796(val);
    const int x = 33796;
    std::cout << 33796;
    for (int i = 0; i < 33796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33796(int& val) {
    const int x = 33796;
    std::cout << 33796;
    for (int i = 0; i < 33796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33797(int& val) {
    func_in_33797(val);
    const int x = 33797;
    std::cout << 33797;
    for (int i = 0; i < 33797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33797(int& val) {
    const int x = 33797;
    std::cout << 33797;
    for (int i = 0; i < 33797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33798(int& val) {
    func_in_33798(val);
    const int x = 33798;
    std::cout << 33798;
    for (int i = 0; i < 33798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33798(int& val) {
    const int x = 33798;
    std::cout << 33798;
    for (int i = 0; i < 33798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33799(int& val) {
    func_in_33799(val);
    const int x = 33799;
    std::cout << 33799;
    for (int i = 0; i < 33799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33799(int& val) {
    const int x = 33799;
    std::cout << 33799;
    for (int i = 0; i < 33799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33800(int& val) {
    func_in_33800(val);
    const int x = 33800;
    std::cout << 33800;
    for (int i = 0; i < 33800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33800(int& val) {
    const int x = 33800;
    std::cout << 33800;
    for (int i = 0; i < 33800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33801(int& val) {
    func_in_33801(val);
    const int x = 33801;
    std::cout << 33801;
    for (int i = 0; i < 33801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33801(int& val) {
    const int x = 33801;
    std::cout << 33801;
    for (int i = 0; i < 33801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33802(int& val) {
    func_in_33802(val);
    const int x = 33802;
    std::cout << 33802;
    for (int i = 0; i < 33802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33802(int& val) {
    const int x = 33802;
    std::cout << 33802;
    for (int i = 0; i < 33802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33803(int& val) {
    func_in_33803(val);
    const int x = 33803;
    std::cout << 33803;
    for (int i = 0; i < 33803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33803(int& val) {
    const int x = 33803;
    std::cout << 33803;
    for (int i = 0; i < 33803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33804(int& val) {
    func_in_33804(val);
    const int x = 33804;
    std::cout << 33804;
    for (int i = 0; i < 33804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33804(int& val) {
    const int x = 33804;
    std::cout << 33804;
    for (int i = 0; i < 33804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33805(int& val) {
    func_in_33805(val);
    const int x = 33805;
    std::cout << 33805;
    for (int i = 0; i < 33805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33805(int& val) {
    const int x = 33805;
    std::cout << 33805;
    for (int i = 0; i < 33805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33806(int& val) {
    func_in_33806(val);
    const int x = 33806;
    std::cout << 33806;
    for (int i = 0; i < 33806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33806(int& val) {
    const int x = 33806;
    std::cout << 33806;
    for (int i = 0; i < 33806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33807(int& val) {
    func_in_33807(val);
    const int x = 33807;
    std::cout << 33807;
    for (int i = 0; i < 33807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33807(int& val) {
    const int x = 33807;
    std::cout << 33807;
    for (int i = 0; i < 33807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33808(int& val) {
    func_in_33808(val);
    const int x = 33808;
    std::cout << 33808;
    for (int i = 0; i < 33808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33808(int& val) {
    const int x = 33808;
    std::cout << 33808;
    for (int i = 0; i < 33808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33809(int& val) {
    func_in_33809(val);
    const int x = 33809;
    std::cout << 33809;
    for (int i = 0; i < 33809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33809(int& val) {
    const int x = 33809;
    std::cout << 33809;
    for (int i = 0; i < 33809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33810(int& val) {
    func_in_33810(val);
    const int x = 33810;
    std::cout << 33810;
    for (int i = 0; i < 33810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33810(int& val) {
    const int x = 33810;
    std::cout << 33810;
    for (int i = 0; i < 33810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33811(int& val) {
    func_in_33811(val);
    const int x = 33811;
    std::cout << 33811;
    for (int i = 0; i < 33811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33811(int& val) {
    const int x = 33811;
    std::cout << 33811;
    for (int i = 0; i < 33811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33812(int& val) {
    func_in_33812(val);
    const int x = 33812;
    std::cout << 33812;
    for (int i = 0; i < 33812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33812(int& val) {
    const int x = 33812;
    std::cout << 33812;
    for (int i = 0; i < 33812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33813(int& val) {
    func_in_33813(val);
    const int x = 33813;
    std::cout << 33813;
    for (int i = 0; i < 33813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33813(int& val) {
    const int x = 33813;
    std::cout << 33813;
    for (int i = 0; i < 33813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33814(int& val) {
    func_in_33814(val);
    const int x = 33814;
    std::cout << 33814;
    for (int i = 0; i < 33814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33814(int& val) {
    const int x = 33814;
    std::cout << 33814;
    for (int i = 0; i < 33814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33815(int& val) {
    func_in_33815(val);
    const int x = 33815;
    std::cout << 33815;
    for (int i = 0; i < 33815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33815(int& val) {
    const int x = 33815;
    std::cout << 33815;
    for (int i = 0; i < 33815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33816(int& val) {
    func_in_33816(val);
    const int x = 33816;
    std::cout << 33816;
    for (int i = 0; i < 33816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33816(int& val) {
    const int x = 33816;
    std::cout << 33816;
    for (int i = 0; i < 33816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33817(int& val) {
    func_in_33817(val);
    const int x = 33817;
    std::cout << 33817;
    for (int i = 0; i < 33817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33817(int& val) {
    const int x = 33817;
    std::cout << 33817;
    for (int i = 0; i < 33817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33818(int& val) {
    func_in_33818(val);
    const int x = 33818;
    std::cout << 33818;
    for (int i = 0; i < 33818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33818(int& val) {
    const int x = 33818;
    std::cout << 33818;
    for (int i = 0; i < 33818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33819(int& val) {
    func_in_33819(val);
    const int x = 33819;
    std::cout << 33819;
    for (int i = 0; i < 33819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33819(int& val) {
    const int x = 33819;
    std::cout << 33819;
    for (int i = 0; i < 33819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33820(int& val) {
    func_in_33820(val);
    const int x = 33820;
    std::cout << 33820;
    for (int i = 0; i < 33820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33820(int& val) {
    const int x = 33820;
    std::cout << 33820;
    for (int i = 0; i < 33820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33821(int& val) {
    func_in_33821(val);
    const int x = 33821;
    std::cout << 33821;
    for (int i = 0; i < 33821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33821(int& val) {
    const int x = 33821;
    std::cout << 33821;
    for (int i = 0; i < 33821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33822(int& val) {
    func_in_33822(val);
    const int x = 33822;
    std::cout << 33822;
    for (int i = 0; i < 33822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33822(int& val) {
    const int x = 33822;
    std::cout << 33822;
    for (int i = 0; i < 33822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33823(int& val) {
    func_in_33823(val);
    const int x = 33823;
    std::cout << 33823;
    for (int i = 0; i < 33823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33823(int& val) {
    const int x = 33823;
    std::cout << 33823;
    for (int i = 0; i < 33823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33824(int& val) {
    func_in_33824(val);
    const int x = 33824;
    std::cout << 33824;
    for (int i = 0; i < 33824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33824(int& val) {
    const int x = 33824;
    std::cout << 33824;
    for (int i = 0; i < 33824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33825(int& val) {
    func_in_33825(val);
    const int x = 33825;
    std::cout << 33825;
    for (int i = 0; i < 33825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33825(int& val) {
    const int x = 33825;
    std::cout << 33825;
    for (int i = 0; i < 33825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33826(int& val) {
    func_in_33826(val);
    const int x = 33826;
    std::cout << 33826;
    for (int i = 0; i < 33826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33826(int& val) {
    const int x = 33826;
    std::cout << 33826;
    for (int i = 0; i < 33826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33827(int& val) {
    func_in_33827(val);
    const int x = 33827;
    std::cout << 33827;
    for (int i = 0; i < 33827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33827(int& val) {
    const int x = 33827;
    std::cout << 33827;
    for (int i = 0; i < 33827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33828(int& val) {
    func_in_33828(val);
    const int x = 33828;
    std::cout << 33828;
    for (int i = 0; i < 33828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33828(int& val) {
    const int x = 33828;
    std::cout << 33828;
    for (int i = 0; i < 33828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33829(int& val) {
    func_in_33829(val);
    const int x = 33829;
    std::cout << 33829;
    for (int i = 0; i < 33829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33829(int& val) {
    const int x = 33829;
    std::cout << 33829;
    for (int i = 0; i < 33829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33830(int& val) {
    func_in_33830(val);
    const int x = 33830;
    std::cout << 33830;
    for (int i = 0; i < 33830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33830(int& val) {
    const int x = 33830;
    std::cout << 33830;
    for (int i = 0; i < 33830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33831(int& val) {
    func_in_33831(val);
    const int x = 33831;
    std::cout << 33831;
    for (int i = 0; i < 33831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33831(int& val) {
    const int x = 33831;
    std::cout << 33831;
    for (int i = 0; i < 33831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33832(int& val) {
    func_in_33832(val);
    const int x = 33832;
    std::cout << 33832;
    for (int i = 0; i < 33832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33832(int& val) {
    const int x = 33832;
    std::cout << 33832;
    for (int i = 0; i < 33832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33833(int& val) {
    func_in_33833(val);
    const int x = 33833;
    std::cout << 33833;
    for (int i = 0; i < 33833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33833(int& val) {
    const int x = 33833;
    std::cout << 33833;
    for (int i = 0; i < 33833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33834(int& val) {
    func_in_33834(val);
    const int x = 33834;
    std::cout << 33834;
    for (int i = 0; i < 33834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33834(int& val) {
    const int x = 33834;
    std::cout << 33834;
    for (int i = 0; i < 33834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33835(int& val) {
    func_in_33835(val);
    const int x = 33835;
    std::cout << 33835;
    for (int i = 0; i < 33835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33835(int& val) {
    const int x = 33835;
    std::cout << 33835;
    for (int i = 0; i < 33835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33836(int& val) {
    func_in_33836(val);
    const int x = 33836;
    std::cout << 33836;
    for (int i = 0; i < 33836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33836(int& val) {
    const int x = 33836;
    std::cout << 33836;
    for (int i = 0; i < 33836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33837(int& val) {
    func_in_33837(val);
    const int x = 33837;
    std::cout << 33837;
    for (int i = 0; i < 33837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33837(int& val) {
    const int x = 33837;
    std::cout << 33837;
    for (int i = 0; i < 33837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33838(int& val) {
    func_in_33838(val);
    const int x = 33838;
    std::cout << 33838;
    for (int i = 0; i < 33838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33838(int& val) {
    const int x = 33838;
    std::cout << 33838;
    for (int i = 0; i < 33838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33839(int& val) {
    func_in_33839(val);
    const int x = 33839;
    std::cout << 33839;
    for (int i = 0; i < 33839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33839(int& val) {
    const int x = 33839;
    std::cout << 33839;
    for (int i = 0; i < 33839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33840(int& val) {
    func_in_33840(val);
    const int x = 33840;
    std::cout << 33840;
    for (int i = 0; i < 33840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33840(int& val) {
    const int x = 33840;
    std::cout << 33840;
    for (int i = 0; i < 33840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33841(int& val) {
    func_in_33841(val);
    const int x = 33841;
    std::cout << 33841;
    for (int i = 0; i < 33841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33841(int& val) {
    const int x = 33841;
    std::cout << 33841;
    for (int i = 0; i < 33841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33842(int& val) {
    func_in_33842(val);
    const int x = 33842;
    std::cout << 33842;
    for (int i = 0; i < 33842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33842(int& val) {
    const int x = 33842;
    std::cout << 33842;
    for (int i = 0; i < 33842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33843(int& val) {
    func_in_33843(val);
    const int x = 33843;
    std::cout << 33843;
    for (int i = 0; i < 33843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33843(int& val) {
    const int x = 33843;
    std::cout << 33843;
    for (int i = 0; i < 33843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33844(int& val) {
    func_in_33844(val);
    const int x = 33844;
    std::cout << 33844;
    for (int i = 0; i < 33844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33844(int& val) {
    const int x = 33844;
    std::cout << 33844;
    for (int i = 0; i < 33844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33845(int& val) {
    func_in_33845(val);
    const int x = 33845;
    std::cout << 33845;
    for (int i = 0; i < 33845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33845(int& val) {
    const int x = 33845;
    std::cout << 33845;
    for (int i = 0; i < 33845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33846(int& val) {
    func_in_33846(val);
    const int x = 33846;
    std::cout << 33846;
    for (int i = 0; i < 33846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33846(int& val) {
    const int x = 33846;
    std::cout << 33846;
    for (int i = 0; i < 33846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33847(int& val) {
    func_in_33847(val);
    const int x = 33847;
    std::cout << 33847;
    for (int i = 0; i < 33847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33847(int& val) {
    const int x = 33847;
    std::cout << 33847;
    for (int i = 0; i < 33847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33848(int& val) {
    func_in_33848(val);
    const int x = 33848;
    std::cout << 33848;
    for (int i = 0; i < 33848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33848(int& val) {
    const int x = 33848;
    std::cout << 33848;
    for (int i = 0; i < 33848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33849(int& val) {
    func_in_33849(val);
    const int x = 33849;
    std::cout << 33849;
    for (int i = 0; i < 33849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33849(int& val) {
    const int x = 33849;
    std::cout << 33849;
    for (int i = 0; i < 33849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33850(int& val) {
    func_in_33850(val);
    const int x = 33850;
    std::cout << 33850;
    for (int i = 0; i < 33850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33850(int& val) {
    const int x = 33850;
    std::cout << 33850;
    for (int i = 0; i < 33850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33851(int& val) {
    func_in_33851(val);
    const int x = 33851;
    std::cout << 33851;
    for (int i = 0; i < 33851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33851(int& val) {
    const int x = 33851;
    std::cout << 33851;
    for (int i = 0; i < 33851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33852(int& val) {
    func_in_33852(val);
    const int x = 33852;
    std::cout << 33852;
    for (int i = 0; i < 33852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33852(int& val) {
    const int x = 33852;
    std::cout << 33852;
    for (int i = 0; i < 33852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33853(int& val) {
    func_in_33853(val);
    const int x = 33853;
    std::cout << 33853;
    for (int i = 0; i < 33853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33853(int& val) {
    const int x = 33853;
    std::cout << 33853;
    for (int i = 0; i < 33853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33854(int& val) {
    func_in_33854(val);
    const int x = 33854;
    std::cout << 33854;
    for (int i = 0; i < 33854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33854(int& val) {
    const int x = 33854;
    std::cout << 33854;
    for (int i = 0; i < 33854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33855(int& val) {
    func_in_33855(val);
    const int x = 33855;
    std::cout << 33855;
    for (int i = 0; i < 33855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33855(int& val) {
    const int x = 33855;
    std::cout << 33855;
    for (int i = 0; i < 33855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33856(int& val) {
    func_in_33856(val);
    const int x = 33856;
    std::cout << 33856;
    for (int i = 0; i < 33856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33856(int& val) {
    const int x = 33856;
    std::cout << 33856;
    for (int i = 0; i < 33856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33857(int& val) {
    func_in_33857(val);
    const int x = 33857;
    std::cout << 33857;
    for (int i = 0; i < 33857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33857(int& val) {
    const int x = 33857;
    std::cout << 33857;
    for (int i = 0; i < 33857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33858(int& val) {
    func_in_33858(val);
    const int x = 33858;
    std::cout << 33858;
    for (int i = 0; i < 33858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33858(int& val) {
    const int x = 33858;
    std::cout << 33858;
    for (int i = 0; i < 33858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33859(int& val) {
    func_in_33859(val);
    const int x = 33859;
    std::cout << 33859;
    for (int i = 0; i < 33859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33859(int& val) {
    const int x = 33859;
    std::cout << 33859;
    for (int i = 0; i < 33859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33860(int& val) {
    func_in_33860(val);
    const int x = 33860;
    std::cout << 33860;
    for (int i = 0; i < 33860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33860(int& val) {
    const int x = 33860;
    std::cout << 33860;
    for (int i = 0; i < 33860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33861(int& val) {
    func_in_33861(val);
    const int x = 33861;
    std::cout << 33861;
    for (int i = 0; i < 33861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33861(int& val) {
    const int x = 33861;
    std::cout << 33861;
    for (int i = 0; i < 33861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33862(int& val) {
    func_in_33862(val);
    const int x = 33862;
    std::cout << 33862;
    for (int i = 0; i < 33862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33862(int& val) {
    const int x = 33862;
    std::cout << 33862;
    for (int i = 0; i < 33862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33863(int& val) {
    func_in_33863(val);
    const int x = 33863;
    std::cout << 33863;
    for (int i = 0; i < 33863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33863(int& val) {
    const int x = 33863;
    std::cout << 33863;
    for (int i = 0; i < 33863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33864(int& val) {
    func_in_33864(val);
    const int x = 33864;
    std::cout << 33864;
    for (int i = 0; i < 33864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33864(int& val) {
    const int x = 33864;
    std::cout << 33864;
    for (int i = 0; i < 33864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33865(int& val) {
    func_in_33865(val);
    const int x = 33865;
    std::cout << 33865;
    for (int i = 0; i < 33865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33865(int& val) {
    const int x = 33865;
    std::cout << 33865;
    for (int i = 0; i < 33865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33866(int& val) {
    func_in_33866(val);
    const int x = 33866;
    std::cout << 33866;
    for (int i = 0; i < 33866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33866(int& val) {
    const int x = 33866;
    std::cout << 33866;
    for (int i = 0; i < 33866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33867(int& val) {
    func_in_33867(val);
    const int x = 33867;
    std::cout << 33867;
    for (int i = 0; i < 33867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33867(int& val) {
    const int x = 33867;
    std::cout << 33867;
    for (int i = 0; i < 33867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33868(int& val) {
    func_in_33868(val);
    const int x = 33868;
    std::cout << 33868;
    for (int i = 0; i < 33868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33868(int& val) {
    const int x = 33868;
    std::cout << 33868;
    for (int i = 0; i < 33868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33869(int& val) {
    func_in_33869(val);
    const int x = 33869;
    std::cout << 33869;
    for (int i = 0; i < 33869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33869(int& val) {
    const int x = 33869;
    std::cout << 33869;
    for (int i = 0; i < 33869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33870(int& val) {
    func_in_33870(val);
    const int x = 33870;
    std::cout << 33870;
    for (int i = 0; i < 33870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33870(int& val) {
    const int x = 33870;
    std::cout << 33870;
    for (int i = 0; i < 33870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33871(int& val) {
    func_in_33871(val);
    const int x = 33871;
    std::cout << 33871;
    for (int i = 0; i < 33871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33871(int& val) {
    const int x = 33871;
    std::cout << 33871;
    for (int i = 0; i < 33871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33872(int& val) {
    func_in_33872(val);
    const int x = 33872;
    std::cout << 33872;
    for (int i = 0; i < 33872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33872(int& val) {
    const int x = 33872;
    std::cout << 33872;
    for (int i = 0; i < 33872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33873(int& val) {
    func_in_33873(val);
    const int x = 33873;
    std::cout << 33873;
    for (int i = 0; i < 33873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33873(int& val) {
    const int x = 33873;
    std::cout << 33873;
    for (int i = 0; i < 33873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33874(int& val) {
    func_in_33874(val);
    const int x = 33874;
    std::cout << 33874;
    for (int i = 0; i < 33874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33874(int& val) {
    const int x = 33874;
    std::cout << 33874;
    for (int i = 0; i < 33874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33875(int& val) {
    func_in_33875(val);
    const int x = 33875;
    std::cout << 33875;
    for (int i = 0; i < 33875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33875(int& val) {
    const int x = 33875;
    std::cout << 33875;
    for (int i = 0; i < 33875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33876(int& val) {
    func_in_33876(val);
    const int x = 33876;
    std::cout << 33876;
    for (int i = 0; i < 33876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33876(int& val) {
    const int x = 33876;
    std::cout << 33876;
    for (int i = 0; i < 33876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33877(int& val) {
    func_in_33877(val);
    const int x = 33877;
    std::cout << 33877;
    for (int i = 0; i < 33877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33877(int& val) {
    const int x = 33877;
    std::cout << 33877;
    for (int i = 0; i < 33877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33878(int& val) {
    func_in_33878(val);
    const int x = 33878;
    std::cout << 33878;
    for (int i = 0; i < 33878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33878(int& val) {
    const int x = 33878;
    std::cout << 33878;
    for (int i = 0; i < 33878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33879(int& val) {
    func_in_33879(val);
    const int x = 33879;
    std::cout << 33879;
    for (int i = 0; i < 33879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33879(int& val) {
    const int x = 33879;
    std::cout << 33879;
    for (int i = 0; i < 33879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33880(int& val) {
    func_in_33880(val);
    const int x = 33880;
    std::cout << 33880;
    for (int i = 0; i < 33880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33880(int& val) {
    const int x = 33880;
    std::cout << 33880;
    for (int i = 0; i < 33880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33881(int& val) {
    func_in_33881(val);
    const int x = 33881;
    std::cout << 33881;
    for (int i = 0; i < 33881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33881(int& val) {
    const int x = 33881;
    std::cout << 33881;
    for (int i = 0; i < 33881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33882(int& val) {
    func_in_33882(val);
    const int x = 33882;
    std::cout << 33882;
    for (int i = 0; i < 33882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33882(int& val) {
    const int x = 33882;
    std::cout << 33882;
    for (int i = 0; i < 33882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33883(int& val) {
    func_in_33883(val);
    const int x = 33883;
    std::cout << 33883;
    for (int i = 0; i < 33883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33883(int& val) {
    const int x = 33883;
    std::cout << 33883;
    for (int i = 0; i < 33883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33884(int& val) {
    func_in_33884(val);
    const int x = 33884;
    std::cout << 33884;
    for (int i = 0; i < 33884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33884(int& val) {
    const int x = 33884;
    std::cout << 33884;
    for (int i = 0; i < 33884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33885(int& val) {
    func_in_33885(val);
    const int x = 33885;
    std::cout << 33885;
    for (int i = 0; i < 33885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33885(int& val) {
    const int x = 33885;
    std::cout << 33885;
    for (int i = 0; i < 33885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33886(int& val) {
    func_in_33886(val);
    const int x = 33886;
    std::cout << 33886;
    for (int i = 0; i < 33886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33886(int& val) {
    const int x = 33886;
    std::cout << 33886;
    for (int i = 0; i < 33886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33887(int& val) {
    func_in_33887(val);
    const int x = 33887;
    std::cout << 33887;
    for (int i = 0; i < 33887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33887(int& val) {
    const int x = 33887;
    std::cout << 33887;
    for (int i = 0; i < 33887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33888(int& val) {
    func_in_33888(val);
    const int x = 33888;
    std::cout << 33888;
    for (int i = 0; i < 33888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33888(int& val) {
    const int x = 33888;
    std::cout << 33888;
    for (int i = 0; i < 33888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33889(int& val) {
    func_in_33889(val);
    const int x = 33889;
    std::cout << 33889;
    for (int i = 0; i < 33889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33889(int& val) {
    const int x = 33889;
    std::cout << 33889;
    for (int i = 0; i < 33889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33890(int& val) {
    func_in_33890(val);
    const int x = 33890;
    std::cout << 33890;
    for (int i = 0; i < 33890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33890(int& val) {
    const int x = 33890;
    std::cout << 33890;
    for (int i = 0; i < 33890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33891(int& val) {
    func_in_33891(val);
    const int x = 33891;
    std::cout << 33891;
    for (int i = 0; i < 33891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33891(int& val) {
    const int x = 33891;
    std::cout << 33891;
    for (int i = 0; i < 33891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33892(int& val) {
    func_in_33892(val);
    const int x = 33892;
    std::cout << 33892;
    for (int i = 0; i < 33892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33892(int& val) {
    const int x = 33892;
    std::cout << 33892;
    for (int i = 0; i < 33892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33893(int& val) {
    func_in_33893(val);
    const int x = 33893;
    std::cout << 33893;
    for (int i = 0; i < 33893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33893(int& val) {
    const int x = 33893;
    std::cout << 33893;
    for (int i = 0; i < 33893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33894(int& val) {
    func_in_33894(val);
    const int x = 33894;
    std::cout << 33894;
    for (int i = 0; i < 33894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33894(int& val) {
    const int x = 33894;
    std::cout << 33894;
    for (int i = 0; i < 33894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33895(int& val) {
    func_in_33895(val);
    const int x = 33895;
    std::cout << 33895;
    for (int i = 0; i < 33895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33895(int& val) {
    const int x = 33895;
    std::cout << 33895;
    for (int i = 0; i < 33895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33896(int& val) {
    func_in_33896(val);
    const int x = 33896;
    std::cout << 33896;
    for (int i = 0; i < 33896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33896(int& val) {
    const int x = 33896;
    std::cout << 33896;
    for (int i = 0; i < 33896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33897(int& val) {
    func_in_33897(val);
    const int x = 33897;
    std::cout << 33897;
    for (int i = 0; i < 33897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33897(int& val) {
    const int x = 33897;
    std::cout << 33897;
    for (int i = 0; i < 33897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33898(int& val) {
    func_in_33898(val);
    const int x = 33898;
    std::cout << 33898;
    for (int i = 0; i < 33898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33898(int& val) {
    const int x = 33898;
    std::cout << 33898;
    for (int i = 0; i < 33898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33899(int& val) {
    func_in_33899(val);
    const int x = 33899;
    std::cout << 33899;
    for (int i = 0; i < 33899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33899(int& val) {
    const int x = 33899;
    std::cout << 33899;
    for (int i = 0; i < 33899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33900(int& val) {
    func_in_33900(val);
    const int x = 33900;
    std::cout << 33900;
    for (int i = 0; i < 33900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33900(int& val) {
    const int x = 33900;
    std::cout << 33900;
    for (int i = 0; i < 33900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33901(int& val) {
    func_in_33901(val);
    const int x = 33901;
    std::cout << 33901;
    for (int i = 0; i < 33901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33901(int& val) {
    const int x = 33901;
    std::cout << 33901;
    for (int i = 0; i < 33901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33902(int& val) {
    func_in_33902(val);
    const int x = 33902;
    std::cout << 33902;
    for (int i = 0; i < 33902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33902(int& val) {
    const int x = 33902;
    std::cout << 33902;
    for (int i = 0; i < 33902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33903(int& val) {
    func_in_33903(val);
    const int x = 33903;
    std::cout << 33903;
    for (int i = 0; i < 33903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33903(int& val) {
    const int x = 33903;
    std::cout << 33903;
    for (int i = 0; i < 33903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33904(int& val) {
    func_in_33904(val);
    const int x = 33904;
    std::cout << 33904;
    for (int i = 0; i < 33904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33904(int& val) {
    const int x = 33904;
    std::cout << 33904;
    for (int i = 0; i < 33904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33905(int& val) {
    func_in_33905(val);
    const int x = 33905;
    std::cout << 33905;
    for (int i = 0; i < 33905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33905(int& val) {
    const int x = 33905;
    std::cout << 33905;
    for (int i = 0; i < 33905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33906(int& val) {
    func_in_33906(val);
    const int x = 33906;
    std::cout << 33906;
    for (int i = 0; i < 33906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33906(int& val) {
    const int x = 33906;
    std::cout << 33906;
    for (int i = 0; i < 33906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33907(int& val) {
    func_in_33907(val);
    const int x = 33907;
    std::cout << 33907;
    for (int i = 0; i < 33907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33907(int& val) {
    const int x = 33907;
    std::cout << 33907;
    for (int i = 0; i < 33907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33908(int& val) {
    func_in_33908(val);
    const int x = 33908;
    std::cout << 33908;
    for (int i = 0; i < 33908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33908(int& val) {
    const int x = 33908;
    std::cout << 33908;
    for (int i = 0; i < 33908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33909(int& val) {
    func_in_33909(val);
    const int x = 33909;
    std::cout << 33909;
    for (int i = 0; i < 33909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33909(int& val) {
    const int x = 33909;
    std::cout << 33909;
    for (int i = 0; i < 33909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33910(int& val) {
    func_in_33910(val);
    const int x = 33910;
    std::cout << 33910;
    for (int i = 0; i < 33910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33910(int& val) {
    const int x = 33910;
    std::cout << 33910;
    for (int i = 0; i < 33910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33911(int& val) {
    func_in_33911(val);
    const int x = 33911;
    std::cout << 33911;
    for (int i = 0; i < 33911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33911(int& val) {
    const int x = 33911;
    std::cout << 33911;
    for (int i = 0; i < 33911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33912(int& val) {
    func_in_33912(val);
    const int x = 33912;
    std::cout << 33912;
    for (int i = 0; i < 33912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33912(int& val) {
    const int x = 33912;
    std::cout << 33912;
    for (int i = 0; i < 33912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33913(int& val) {
    func_in_33913(val);
    const int x = 33913;
    std::cout << 33913;
    for (int i = 0; i < 33913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33913(int& val) {
    const int x = 33913;
    std::cout << 33913;
    for (int i = 0; i < 33913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33914(int& val) {
    func_in_33914(val);
    const int x = 33914;
    std::cout << 33914;
    for (int i = 0; i < 33914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33914(int& val) {
    const int x = 33914;
    std::cout << 33914;
    for (int i = 0; i < 33914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33915(int& val) {
    func_in_33915(val);
    const int x = 33915;
    std::cout << 33915;
    for (int i = 0; i < 33915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33915(int& val) {
    const int x = 33915;
    std::cout << 33915;
    for (int i = 0; i < 33915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33916(int& val) {
    func_in_33916(val);
    const int x = 33916;
    std::cout << 33916;
    for (int i = 0; i < 33916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33916(int& val) {
    const int x = 33916;
    std::cout << 33916;
    for (int i = 0; i < 33916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33917(int& val) {
    func_in_33917(val);
    const int x = 33917;
    std::cout << 33917;
    for (int i = 0; i < 33917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33917(int& val) {
    const int x = 33917;
    std::cout << 33917;
    for (int i = 0; i < 33917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33918(int& val) {
    func_in_33918(val);
    const int x = 33918;
    std::cout << 33918;
    for (int i = 0; i < 33918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33918(int& val) {
    const int x = 33918;
    std::cout << 33918;
    for (int i = 0; i < 33918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33919(int& val) {
    func_in_33919(val);
    const int x = 33919;
    std::cout << 33919;
    for (int i = 0; i < 33919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33919(int& val) {
    const int x = 33919;
    std::cout << 33919;
    for (int i = 0; i < 33919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33920(int& val) {
    func_in_33920(val);
    const int x = 33920;
    std::cout << 33920;
    for (int i = 0; i < 33920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33920(int& val) {
    const int x = 33920;
    std::cout << 33920;
    for (int i = 0; i < 33920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33921(int& val) {
    func_in_33921(val);
    const int x = 33921;
    std::cout << 33921;
    for (int i = 0; i < 33921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33921(int& val) {
    const int x = 33921;
    std::cout << 33921;
    for (int i = 0; i < 33921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33922(int& val) {
    func_in_33922(val);
    const int x = 33922;
    std::cout << 33922;
    for (int i = 0; i < 33922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33922(int& val) {
    const int x = 33922;
    std::cout << 33922;
    for (int i = 0; i < 33922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33923(int& val) {
    func_in_33923(val);
    const int x = 33923;
    std::cout << 33923;
    for (int i = 0; i < 33923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33923(int& val) {
    const int x = 33923;
    std::cout << 33923;
    for (int i = 0; i < 33923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33924(int& val) {
    func_in_33924(val);
    const int x = 33924;
    std::cout << 33924;
    for (int i = 0; i < 33924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33924(int& val) {
    const int x = 33924;
    std::cout << 33924;
    for (int i = 0; i < 33924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33925(int& val) {
    func_in_33925(val);
    const int x = 33925;
    std::cout << 33925;
    for (int i = 0; i < 33925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33925(int& val) {
    const int x = 33925;
    std::cout << 33925;
    for (int i = 0; i < 33925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33926(int& val) {
    func_in_33926(val);
    const int x = 33926;
    std::cout << 33926;
    for (int i = 0; i < 33926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33926(int& val) {
    const int x = 33926;
    std::cout << 33926;
    for (int i = 0; i < 33926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33927(int& val) {
    func_in_33927(val);
    const int x = 33927;
    std::cout << 33927;
    for (int i = 0; i < 33927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33927(int& val) {
    const int x = 33927;
    std::cout << 33927;
    for (int i = 0; i < 33927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33928(int& val) {
    func_in_33928(val);
    const int x = 33928;
    std::cout << 33928;
    for (int i = 0; i < 33928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33928(int& val) {
    const int x = 33928;
    std::cout << 33928;
    for (int i = 0; i < 33928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33929(int& val) {
    func_in_33929(val);
    const int x = 33929;
    std::cout << 33929;
    for (int i = 0; i < 33929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33929(int& val) {
    const int x = 33929;
    std::cout << 33929;
    for (int i = 0; i < 33929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33930(int& val) {
    func_in_33930(val);
    const int x = 33930;
    std::cout << 33930;
    for (int i = 0; i < 33930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33930(int& val) {
    const int x = 33930;
    std::cout << 33930;
    for (int i = 0; i < 33930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33931(int& val) {
    func_in_33931(val);
    const int x = 33931;
    std::cout << 33931;
    for (int i = 0; i < 33931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33931(int& val) {
    const int x = 33931;
    std::cout << 33931;
    for (int i = 0; i < 33931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33932(int& val) {
    func_in_33932(val);
    const int x = 33932;
    std::cout << 33932;
    for (int i = 0; i < 33932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33932(int& val) {
    const int x = 33932;
    std::cout << 33932;
    for (int i = 0; i < 33932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33933(int& val) {
    func_in_33933(val);
    const int x = 33933;
    std::cout << 33933;
    for (int i = 0; i < 33933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33933(int& val) {
    const int x = 33933;
    std::cout << 33933;
    for (int i = 0; i < 33933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33934(int& val) {
    func_in_33934(val);
    const int x = 33934;
    std::cout << 33934;
    for (int i = 0; i < 33934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33934(int& val) {
    const int x = 33934;
    std::cout << 33934;
    for (int i = 0; i < 33934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33935(int& val) {
    func_in_33935(val);
    const int x = 33935;
    std::cout << 33935;
    for (int i = 0; i < 33935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33935(int& val) {
    const int x = 33935;
    std::cout << 33935;
    for (int i = 0; i < 33935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33936(int& val) {
    func_in_33936(val);
    const int x = 33936;
    std::cout << 33936;
    for (int i = 0; i < 33936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33936(int& val) {
    const int x = 33936;
    std::cout << 33936;
    for (int i = 0; i < 33936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33937(int& val) {
    func_in_33937(val);
    const int x = 33937;
    std::cout << 33937;
    for (int i = 0; i < 33937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33937(int& val) {
    const int x = 33937;
    std::cout << 33937;
    for (int i = 0; i < 33937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33938(int& val) {
    func_in_33938(val);
    const int x = 33938;
    std::cout << 33938;
    for (int i = 0; i < 33938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33938(int& val) {
    const int x = 33938;
    std::cout << 33938;
    for (int i = 0; i < 33938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33939(int& val) {
    func_in_33939(val);
    const int x = 33939;
    std::cout << 33939;
    for (int i = 0; i < 33939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33939(int& val) {
    const int x = 33939;
    std::cout << 33939;
    for (int i = 0; i < 33939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33940(int& val) {
    func_in_33940(val);
    const int x = 33940;
    std::cout << 33940;
    for (int i = 0; i < 33940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33940(int& val) {
    const int x = 33940;
    std::cout << 33940;
    for (int i = 0; i < 33940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33941(int& val) {
    func_in_33941(val);
    const int x = 33941;
    std::cout << 33941;
    for (int i = 0; i < 33941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33941(int& val) {
    const int x = 33941;
    std::cout << 33941;
    for (int i = 0; i < 33941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33942(int& val) {
    func_in_33942(val);
    const int x = 33942;
    std::cout << 33942;
    for (int i = 0; i < 33942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33942(int& val) {
    const int x = 33942;
    std::cout << 33942;
    for (int i = 0; i < 33942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33943(int& val) {
    func_in_33943(val);
    const int x = 33943;
    std::cout << 33943;
    for (int i = 0; i < 33943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33943(int& val) {
    const int x = 33943;
    std::cout << 33943;
    for (int i = 0; i < 33943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33944(int& val) {
    func_in_33944(val);
    const int x = 33944;
    std::cout << 33944;
    for (int i = 0; i < 33944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33944(int& val) {
    const int x = 33944;
    std::cout << 33944;
    for (int i = 0; i < 33944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33945(int& val) {
    func_in_33945(val);
    const int x = 33945;
    std::cout << 33945;
    for (int i = 0; i < 33945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33945(int& val) {
    const int x = 33945;
    std::cout << 33945;
    for (int i = 0; i < 33945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33946(int& val) {
    func_in_33946(val);
    const int x = 33946;
    std::cout << 33946;
    for (int i = 0; i < 33946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33946(int& val) {
    const int x = 33946;
    std::cout << 33946;
    for (int i = 0; i < 33946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33947(int& val) {
    func_in_33947(val);
    const int x = 33947;
    std::cout << 33947;
    for (int i = 0; i < 33947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33947(int& val) {
    const int x = 33947;
    std::cout << 33947;
    for (int i = 0; i < 33947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33948(int& val) {
    func_in_33948(val);
    const int x = 33948;
    std::cout << 33948;
    for (int i = 0; i < 33948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33948(int& val) {
    const int x = 33948;
    std::cout << 33948;
    for (int i = 0; i < 33948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33949(int& val) {
    func_in_33949(val);
    const int x = 33949;
    std::cout << 33949;
    for (int i = 0; i < 33949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33949(int& val) {
    const int x = 33949;
    std::cout << 33949;
    for (int i = 0; i < 33949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33950(int& val) {
    func_in_33950(val);
    const int x = 33950;
    std::cout << 33950;
    for (int i = 0; i < 33950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33950(int& val) {
    const int x = 33950;
    std::cout << 33950;
    for (int i = 0; i < 33950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33951(int& val) {
    func_in_33951(val);
    const int x = 33951;
    std::cout << 33951;
    for (int i = 0; i < 33951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33951(int& val) {
    const int x = 33951;
    std::cout << 33951;
    for (int i = 0; i < 33951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33952(int& val) {
    func_in_33952(val);
    const int x = 33952;
    std::cout << 33952;
    for (int i = 0; i < 33952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33952(int& val) {
    const int x = 33952;
    std::cout << 33952;
    for (int i = 0; i < 33952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33953(int& val) {
    func_in_33953(val);
    const int x = 33953;
    std::cout << 33953;
    for (int i = 0; i < 33953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33953(int& val) {
    const int x = 33953;
    std::cout << 33953;
    for (int i = 0; i < 33953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33954(int& val) {
    func_in_33954(val);
    const int x = 33954;
    std::cout << 33954;
    for (int i = 0; i < 33954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33954(int& val) {
    const int x = 33954;
    std::cout << 33954;
    for (int i = 0; i < 33954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33955(int& val) {
    func_in_33955(val);
    const int x = 33955;
    std::cout << 33955;
    for (int i = 0; i < 33955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33955(int& val) {
    const int x = 33955;
    std::cout << 33955;
    for (int i = 0; i < 33955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33956(int& val) {
    func_in_33956(val);
    const int x = 33956;
    std::cout << 33956;
    for (int i = 0; i < 33956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33956(int& val) {
    const int x = 33956;
    std::cout << 33956;
    for (int i = 0; i < 33956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33957(int& val) {
    func_in_33957(val);
    const int x = 33957;
    std::cout << 33957;
    for (int i = 0; i < 33957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33957(int& val) {
    const int x = 33957;
    std::cout << 33957;
    for (int i = 0; i < 33957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33958(int& val) {
    func_in_33958(val);
    const int x = 33958;
    std::cout << 33958;
    for (int i = 0; i < 33958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33958(int& val) {
    const int x = 33958;
    std::cout << 33958;
    for (int i = 0; i < 33958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33959(int& val) {
    func_in_33959(val);
    const int x = 33959;
    std::cout << 33959;
    for (int i = 0; i < 33959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33959(int& val) {
    const int x = 33959;
    std::cout << 33959;
    for (int i = 0; i < 33959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33960(int& val) {
    func_in_33960(val);
    const int x = 33960;
    std::cout << 33960;
    for (int i = 0; i < 33960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33960(int& val) {
    const int x = 33960;
    std::cout << 33960;
    for (int i = 0; i < 33960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33961(int& val) {
    func_in_33961(val);
    const int x = 33961;
    std::cout << 33961;
    for (int i = 0; i < 33961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33961(int& val) {
    const int x = 33961;
    std::cout << 33961;
    for (int i = 0; i < 33961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33962(int& val) {
    func_in_33962(val);
    const int x = 33962;
    std::cout << 33962;
    for (int i = 0; i < 33962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33962(int& val) {
    const int x = 33962;
    std::cout << 33962;
    for (int i = 0; i < 33962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33963(int& val) {
    func_in_33963(val);
    const int x = 33963;
    std::cout << 33963;
    for (int i = 0; i < 33963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33963(int& val) {
    const int x = 33963;
    std::cout << 33963;
    for (int i = 0; i < 33963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33964(int& val) {
    func_in_33964(val);
    const int x = 33964;
    std::cout << 33964;
    for (int i = 0; i < 33964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33964(int& val) {
    const int x = 33964;
    std::cout << 33964;
    for (int i = 0; i < 33964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33965(int& val) {
    func_in_33965(val);
    const int x = 33965;
    std::cout << 33965;
    for (int i = 0; i < 33965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33965(int& val) {
    const int x = 33965;
    std::cout << 33965;
    for (int i = 0; i < 33965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33966(int& val) {
    func_in_33966(val);
    const int x = 33966;
    std::cout << 33966;
    for (int i = 0; i < 33966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33966(int& val) {
    const int x = 33966;
    std::cout << 33966;
    for (int i = 0; i < 33966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33967(int& val) {
    func_in_33967(val);
    const int x = 33967;
    std::cout << 33967;
    for (int i = 0; i < 33967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33967(int& val) {
    const int x = 33967;
    std::cout << 33967;
    for (int i = 0; i < 33967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33968(int& val) {
    func_in_33968(val);
    const int x = 33968;
    std::cout << 33968;
    for (int i = 0; i < 33968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33968(int& val) {
    const int x = 33968;
    std::cout << 33968;
    for (int i = 0; i < 33968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33969(int& val) {
    func_in_33969(val);
    const int x = 33969;
    std::cout << 33969;
    for (int i = 0; i < 33969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33969(int& val) {
    const int x = 33969;
    std::cout << 33969;
    for (int i = 0; i < 33969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33970(int& val) {
    func_in_33970(val);
    const int x = 33970;
    std::cout << 33970;
    for (int i = 0; i < 33970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33970(int& val) {
    const int x = 33970;
    std::cout << 33970;
    for (int i = 0; i < 33970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33971(int& val) {
    func_in_33971(val);
    const int x = 33971;
    std::cout << 33971;
    for (int i = 0; i < 33971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33971(int& val) {
    const int x = 33971;
    std::cout << 33971;
    for (int i = 0; i < 33971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33972(int& val) {
    func_in_33972(val);
    const int x = 33972;
    std::cout << 33972;
    for (int i = 0; i < 33972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33972(int& val) {
    const int x = 33972;
    std::cout << 33972;
    for (int i = 0; i < 33972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33973(int& val) {
    func_in_33973(val);
    const int x = 33973;
    std::cout << 33973;
    for (int i = 0; i < 33973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33973(int& val) {
    const int x = 33973;
    std::cout << 33973;
    for (int i = 0; i < 33973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33974(int& val) {
    func_in_33974(val);
    const int x = 33974;
    std::cout << 33974;
    for (int i = 0; i < 33974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33974(int& val) {
    const int x = 33974;
    std::cout << 33974;
    for (int i = 0; i < 33974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33975(int& val) {
    func_in_33975(val);
    const int x = 33975;
    std::cout << 33975;
    for (int i = 0; i < 33975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33975(int& val) {
    const int x = 33975;
    std::cout << 33975;
    for (int i = 0; i < 33975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33976(int& val) {
    func_in_33976(val);
    const int x = 33976;
    std::cout << 33976;
    for (int i = 0; i < 33976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33976(int& val) {
    const int x = 33976;
    std::cout << 33976;
    for (int i = 0; i < 33976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33977(int& val) {
    func_in_33977(val);
    const int x = 33977;
    std::cout << 33977;
    for (int i = 0; i < 33977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33977(int& val) {
    const int x = 33977;
    std::cout << 33977;
    for (int i = 0; i < 33977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33978(int& val) {
    func_in_33978(val);
    const int x = 33978;
    std::cout << 33978;
    for (int i = 0; i < 33978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33978(int& val) {
    const int x = 33978;
    std::cout << 33978;
    for (int i = 0; i < 33978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33979(int& val) {
    func_in_33979(val);
    const int x = 33979;
    std::cout << 33979;
    for (int i = 0; i < 33979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33979(int& val) {
    const int x = 33979;
    std::cout << 33979;
    for (int i = 0; i < 33979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33980(int& val) {
    func_in_33980(val);
    const int x = 33980;
    std::cout << 33980;
    for (int i = 0; i < 33980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33980(int& val) {
    const int x = 33980;
    std::cout << 33980;
    for (int i = 0; i < 33980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33981(int& val) {
    func_in_33981(val);
    const int x = 33981;
    std::cout << 33981;
    for (int i = 0; i < 33981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33981(int& val) {
    const int x = 33981;
    std::cout << 33981;
    for (int i = 0; i < 33981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33982(int& val) {
    func_in_33982(val);
    const int x = 33982;
    std::cout << 33982;
    for (int i = 0; i < 33982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33982(int& val) {
    const int x = 33982;
    std::cout << 33982;
    for (int i = 0; i < 33982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33983(int& val) {
    func_in_33983(val);
    const int x = 33983;
    std::cout << 33983;
    for (int i = 0; i < 33983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33983(int& val) {
    const int x = 33983;
    std::cout << 33983;
    for (int i = 0; i < 33983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33984(int& val) {
    func_in_33984(val);
    const int x = 33984;
    std::cout << 33984;
    for (int i = 0; i < 33984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33984(int& val) {
    const int x = 33984;
    std::cout << 33984;
    for (int i = 0; i < 33984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33985(int& val) {
    func_in_33985(val);
    const int x = 33985;
    std::cout << 33985;
    for (int i = 0; i < 33985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33985(int& val) {
    const int x = 33985;
    std::cout << 33985;
    for (int i = 0; i < 33985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33986(int& val) {
    func_in_33986(val);
    const int x = 33986;
    std::cout << 33986;
    for (int i = 0; i < 33986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33986(int& val) {
    const int x = 33986;
    std::cout << 33986;
    for (int i = 0; i < 33986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33987(int& val) {
    func_in_33987(val);
    const int x = 33987;
    std::cout << 33987;
    for (int i = 0; i < 33987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33987(int& val) {
    const int x = 33987;
    std::cout << 33987;
    for (int i = 0; i < 33987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33988(int& val) {
    func_in_33988(val);
    const int x = 33988;
    std::cout << 33988;
    for (int i = 0; i < 33988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33988(int& val) {
    const int x = 33988;
    std::cout << 33988;
    for (int i = 0; i < 33988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33989(int& val) {
    func_in_33989(val);
    const int x = 33989;
    std::cout << 33989;
    for (int i = 0; i < 33989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33989(int& val) {
    const int x = 33989;
    std::cout << 33989;
    for (int i = 0; i < 33989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33990(int& val) {
    func_in_33990(val);
    const int x = 33990;
    std::cout << 33990;
    for (int i = 0; i < 33990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33990(int& val) {
    const int x = 33990;
    std::cout << 33990;
    for (int i = 0; i < 33990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33991(int& val) {
    func_in_33991(val);
    const int x = 33991;
    std::cout << 33991;
    for (int i = 0; i < 33991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33991(int& val) {
    const int x = 33991;
    std::cout << 33991;
    for (int i = 0; i < 33991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33992(int& val) {
    func_in_33992(val);
    const int x = 33992;
    std::cout << 33992;
    for (int i = 0; i < 33992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33992(int& val) {
    const int x = 33992;
    std::cout << 33992;
    for (int i = 0; i < 33992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33993(int& val) {
    func_in_33993(val);
    const int x = 33993;
    std::cout << 33993;
    for (int i = 0; i < 33993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33993(int& val) {
    const int x = 33993;
    std::cout << 33993;
    for (int i = 0; i < 33993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33994(int& val) {
    func_in_33994(val);
    const int x = 33994;
    std::cout << 33994;
    for (int i = 0; i < 33994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33994(int& val) {
    const int x = 33994;
    std::cout << 33994;
    for (int i = 0; i < 33994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33995(int& val) {
    func_in_33995(val);
    const int x = 33995;
    std::cout << 33995;
    for (int i = 0; i < 33995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33995(int& val) {
    const int x = 33995;
    std::cout << 33995;
    for (int i = 0; i < 33995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33996(int& val) {
    func_in_33996(val);
    const int x = 33996;
    std::cout << 33996;
    for (int i = 0; i < 33996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33996(int& val) {
    const int x = 33996;
    std::cout << 33996;
    for (int i = 0; i < 33996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33997(int& val) {
    func_in_33997(val);
    const int x = 33997;
    std::cout << 33997;
    for (int i = 0; i < 33997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33997(int& val) {
    const int x = 33997;
    std::cout << 33997;
    for (int i = 0; i < 33997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33998(int& val) {
    func_in_33998(val);
    const int x = 33998;
    std::cout << 33998;
    for (int i = 0; i < 33998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33998(int& val) {
    const int x = 33998;
    std::cout << 33998;
    for (int i = 0; i < 33998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33999(int& val) {
    func_in_33999(val);
    const int x = 33999;
    std::cout << 33999;
    for (int i = 0; i < 33999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33999(int& val) {
    const int x = 33999;
    std::cout << 33999;
    for (int i = 0; i < 33999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34000(int& val) {
    func_in_34000(val);
    const int x = 34000;
    std::cout << 34000;
    for (int i = 0; i < 34000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34000(int& val) {
    const int x = 34000;
    std::cout << 34000;
    for (int i = 0; i < 34000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34001(int& val) {
    func_in_34001(val);
    const int x = 34001;
    std::cout << 34001;
    for (int i = 0; i < 34001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34001(int& val) {
    const int x = 34001;
    std::cout << 34001;
    for (int i = 0; i < 34001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34002(int& val) {
    func_in_34002(val);
    const int x = 34002;
    std::cout << 34002;
    for (int i = 0; i < 34002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34002(int& val) {
    const int x = 34002;
    std::cout << 34002;
    for (int i = 0; i < 34002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34003(int& val) {
    func_in_34003(val);
    const int x = 34003;
    std::cout << 34003;
    for (int i = 0; i < 34003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34003(int& val) {
    const int x = 34003;
    std::cout << 34003;
    for (int i = 0; i < 34003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34004(int& val) {
    func_in_34004(val);
    const int x = 34004;
    std::cout << 34004;
    for (int i = 0; i < 34004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34004(int& val) {
    const int x = 34004;
    std::cout << 34004;
    for (int i = 0; i < 34004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34005(int& val) {
    func_in_34005(val);
    const int x = 34005;
    std::cout << 34005;
    for (int i = 0; i < 34005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34005(int& val) {
    const int x = 34005;
    std::cout << 34005;
    for (int i = 0; i < 34005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34006(int& val) {
    func_in_34006(val);
    const int x = 34006;
    std::cout << 34006;
    for (int i = 0; i < 34006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34006(int& val) {
    const int x = 34006;
    std::cout << 34006;
    for (int i = 0; i < 34006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34007(int& val) {
    func_in_34007(val);
    const int x = 34007;
    std::cout << 34007;
    for (int i = 0; i < 34007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34007(int& val) {
    const int x = 34007;
    std::cout << 34007;
    for (int i = 0; i < 34007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34008(int& val) {
    func_in_34008(val);
    const int x = 34008;
    std::cout << 34008;
    for (int i = 0; i < 34008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34008(int& val) {
    const int x = 34008;
    std::cout << 34008;
    for (int i = 0; i < 34008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34009(int& val) {
    func_in_34009(val);
    const int x = 34009;
    std::cout << 34009;
    for (int i = 0; i < 34009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34009(int& val) {
    const int x = 34009;
    std::cout << 34009;
    for (int i = 0; i < 34009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34010(int& val) {
    func_in_34010(val);
    const int x = 34010;
    std::cout << 34010;
    for (int i = 0; i < 34010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34010(int& val) {
    const int x = 34010;
    std::cout << 34010;
    for (int i = 0; i < 34010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34011(int& val) {
    func_in_34011(val);
    const int x = 34011;
    std::cout << 34011;
    for (int i = 0; i < 34011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34011(int& val) {
    const int x = 34011;
    std::cout << 34011;
    for (int i = 0; i < 34011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34012(int& val) {
    func_in_34012(val);
    const int x = 34012;
    std::cout << 34012;
    for (int i = 0; i < 34012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34012(int& val) {
    const int x = 34012;
    std::cout << 34012;
    for (int i = 0; i < 34012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34013(int& val) {
    func_in_34013(val);
    const int x = 34013;
    std::cout << 34013;
    for (int i = 0; i < 34013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34013(int& val) {
    const int x = 34013;
    std::cout << 34013;
    for (int i = 0; i < 34013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34014(int& val) {
    func_in_34014(val);
    const int x = 34014;
    std::cout << 34014;
    for (int i = 0; i < 34014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34014(int& val) {
    const int x = 34014;
    std::cout << 34014;
    for (int i = 0; i < 34014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34015(int& val) {
    func_in_34015(val);
    const int x = 34015;
    std::cout << 34015;
    for (int i = 0; i < 34015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34015(int& val) {
    const int x = 34015;
    std::cout << 34015;
    for (int i = 0; i < 34015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34016(int& val) {
    func_in_34016(val);
    const int x = 34016;
    std::cout << 34016;
    for (int i = 0; i < 34016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34016(int& val) {
    const int x = 34016;
    std::cout << 34016;
    for (int i = 0; i < 34016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34017(int& val) {
    func_in_34017(val);
    const int x = 34017;
    std::cout << 34017;
    for (int i = 0; i < 34017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34017(int& val) {
    const int x = 34017;
    std::cout << 34017;
    for (int i = 0; i < 34017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34018(int& val) {
    func_in_34018(val);
    const int x = 34018;
    std::cout << 34018;
    for (int i = 0; i < 34018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34018(int& val) {
    const int x = 34018;
    std::cout << 34018;
    for (int i = 0; i < 34018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34019(int& val) {
    func_in_34019(val);
    const int x = 34019;
    std::cout << 34019;
    for (int i = 0; i < 34019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34019(int& val) {
    const int x = 34019;
    std::cout << 34019;
    for (int i = 0; i < 34019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34020(int& val) {
    func_in_34020(val);
    const int x = 34020;
    std::cout << 34020;
    for (int i = 0; i < 34020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34020(int& val) {
    const int x = 34020;
    std::cout << 34020;
    for (int i = 0; i < 34020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34021(int& val) {
    func_in_34021(val);
    const int x = 34021;
    std::cout << 34021;
    for (int i = 0; i < 34021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34021(int& val) {
    const int x = 34021;
    std::cout << 34021;
    for (int i = 0; i < 34021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34022(int& val) {
    func_in_34022(val);
    const int x = 34022;
    std::cout << 34022;
    for (int i = 0; i < 34022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34022(int& val) {
    const int x = 34022;
    std::cout << 34022;
    for (int i = 0; i < 34022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34023(int& val) {
    func_in_34023(val);
    const int x = 34023;
    std::cout << 34023;
    for (int i = 0; i < 34023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34023(int& val) {
    const int x = 34023;
    std::cout << 34023;
    for (int i = 0; i < 34023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34024(int& val) {
    func_in_34024(val);
    const int x = 34024;
    std::cout << 34024;
    for (int i = 0; i < 34024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34024(int& val) {
    const int x = 34024;
    std::cout << 34024;
    for (int i = 0; i < 34024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34025(int& val) {
    func_in_34025(val);
    const int x = 34025;
    std::cout << 34025;
    for (int i = 0; i < 34025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34025(int& val) {
    const int x = 34025;
    std::cout << 34025;
    for (int i = 0; i < 34025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34026(int& val) {
    func_in_34026(val);
    const int x = 34026;
    std::cout << 34026;
    for (int i = 0; i < 34026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34026(int& val) {
    const int x = 34026;
    std::cout << 34026;
    for (int i = 0; i < 34026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34027(int& val) {
    func_in_34027(val);
    const int x = 34027;
    std::cout << 34027;
    for (int i = 0; i < 34027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34027(int& val) {
    const int x = 34027;
    std::cout << 34027;
    for (int i = 0; i < 34027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34028(int& val) {
    func_in_34028(val);
    const int x = 34028;
    std::cout << 34028;
    for (int i = 0; i < 34028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34028(int& val) {
    const int x = 34028;
    std::cout << 34028;
    for (int i = 0; i < 34028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34029(int& val) {
    func_in_34029(val);
    const int x = 34029;
    std::cout << 34029;
    for (int i = 0; i < 34029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34029(int& val) {
    const int x = 34029;
    std::cout << 34029;
    for (int i = 0; i < 34029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34030(int& val) {
    func_in_34030(val);
    const int x = 34030;
    std::cout << 34030;
    for (int i = 0; i < 34030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34030(int& val) {
    const int x = 34030;
    std::cout << 34030;
    for (int i = 0; i < 34030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34031(int& val) {
    func_in_34031(val);
    const int x = 34031;
    std::cout << 34031;
    for (int i = 0; i < 34031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34031(int& val) {
    const int x = 34031;
    std::cout << 34031;
    for (int i = 0; i < 34031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34032(int& val) {
    func_in_34032(val);
    const int x = 34032;
    std::cout << 34032;
    for (int i = 0; i < 34032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34032(int& val) {
    const int x = 34032;
    std::cout << 34032;
    for (int i = 0; i < 34032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34033(int& val) {
    func_in_34033(val);
    const int x = 34033;
    std::cout << 34033;
    for (int i = 0; i < 34033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34033(int& val) {
    const int x = 34033;
    std::cout << 34033;
    for (int i = 0; i < 34033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34034(int& val) {
    func_in_34034(val);
    const int x = 34034;
    std::cout << 34034;
    for (int i = 0; i < 34034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34034(int& val) {
    const int x = 34034;
    std::cout << 34034;
    for (int i = 0; i < 34034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34035(int& val) {
    func_in_34035(val);
    const int x = 34035;
    std::cout << 34035;
    for (int i = 0; i < 34035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34035(int& val) {
    const int x = 34035;
    std::cout << 34035;
    for (int i = 0; i < 34035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34036(int& val) {
    func_in_34036(val);
    const int x = 34036;
    std::cout << 34036;
    for (int i = 0; i < 34036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34036(int& val) {
    const int x = 34036;
    std::cout << 34036;
    for (int i = 0; i < 34036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34037(int& val) {
    func_in_34037(val);
    const int x = 34037;
    std::cout << 34037;
    for (int i = 0; i < 34037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34037(int& val) {
    const int x = 34037;
    std::cout << 34037;
    for (int i = 0; i < 34037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34038(int& val) {
    func_in_34038(val);
    const int x = 34038;
    std::cout << 34038;
    for (int i = 0; i < 34038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34038(int& val) {
    const int x = 34038;
    std::cout << 34038;
    for (int i = 0; i < 34038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34039(int& val) {
    func_in_34039(val);
    const int x = 34039;
    std::cout << 34039;
    for (int i = 0; i < 34039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34039(int& val) {
    const int x = 34039;
    std::cout << 34039;
    for (int i = 0; i < 34039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34040(int& val) {
    func_in_34040(val);
    const int x = 34040;
    std::cout << 34040;
    for (int i = 0; i < 34040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34040(int& val) {
    const int x = 34040;
    std::cout << 34040;
    for (int i = 0; i < 34040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34041(int& val) {
    func_in_34041(val);
    const int x = 34041;
    std::cout << 34041;
    for (int i = 0; i < 34041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34041(int& val) {
    const int x = 34041;
    std::cout << 34041;
    for (int i = 0; i < 34041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34042(int& val) {
    func_in_34042(val);
    const int x = 34042;
    std::cout << 34042;
    for (int i = 0; i < 34042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34042(int& val) {
    const int x = 34042;
    std::cout << 34042;
    for (int i = 0; i < 34042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34043(int& val) {
    func_in_34043(val);
    const int x = 34043;
    std::cout << 34043;
    for (int i = 0; i < 34043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34043(int& val) {
    const int x = 34043;
    std::cout << 34043;
    for (int i = 0; i < 34043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34044(int& val) {
    func_in_34044(val);
    const int x = 34044;
    std::cout << 34044;
    for (int i = 0; i < 34044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34044(int& val) {
    const int x = 34044;
    std::cout << 34044;
    for (int i = 0; i < 34044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34045(int& val) {
    func_in_34045(val);
    const int x = 34045;
    std::cout << 34045;
    for (int i = 0; i < 34045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34045(int& val) {
    const int x = 34045;
    std::cout << 34045;
    for (int i = 0; i < 34045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34046(int& val) {
    func_in_34046(val);
    const int x = 34046;
    std::cout << 34046;
    for (int i = 0; i < 34046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34046(int& val) {
    const int x = 34046;
    std::cout << 34046;
    for (int i = 0; i < 34046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34047(int& val) {
    func_in_34047(val);
    const int x = 34047;
    std::cout << 34047;
    for (int i = 0; i < 34047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34047(int& val) {
    const int x = 34047;
    std::cout << 34047;
    for (int i = 0; i < 34047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34048(int& val) {
    func_in_34048(val);
    const int x = 34048;
    std::cout << 34048;
    for (int i = 0; i < 34048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34048(int& val) {
    const int x = 34048;
    std::cout << 34048;
    for (int i = 0; i < 34048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34049(int& val) {
    func_in_34049(val);
    const int x = 34049;
    std::cout << 34049;
    for (int i = 0; i < 34049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34049(int& val) {
    const int x = 34049;
    std::cout << 34049;
    for (int i = 0; i < 34049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34050(int& val) {
    func_in_34050(val);
    const int x = 34050;
    std::cout << 34050;
    for (int i = 0; i < 34050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34050(int& val) {
    const int x = 34050;
    std::cout << 34050;
    for (int i = 0; i < 34050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34051(int& val) {
    func_in_34051(val);
    const int x = 34051;
    std::cout << 34051;
    for (int i = 0; i < 34051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34051(int& val) {
    const int x = 34051;
    std::cout << 34051;
    for (int i = 0; i < 34051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34052(int& val) {
    func_in_34052(val);
    const int x = 34052;
    std::cout << 34052;
    for (int i = 0; i < 34052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34052(int& val) {
    const int x = 34052;
    std::cout << 34052;
    for (int i = 0; i < 34052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34053(int& val) {
    func_in_34053(val);
    const int x = 34053;
    std::cout << 34053;
    for (int i = 0; i < 34053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34053(int& val) {
    const int x = 34053;
    std::cout << 34053;
    for (int i = 0; i < 34053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34054(int& val) {
    func_in_34054(val);
    const int x = 34054;
    std::cout << 34054;
    for (int i = 0; i < 34054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34054(int& val) {
    const int x = 34054;
    std::cout << 34054;
    for (int i = 0; i < 34054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34055(int& val) {
    func_in_34055(val);
    const int x = 34055;
    std::cout << 34055;
    for (int i = 0; i < 34055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34055(int& val) {
    const int x = 34055;
    std::cout << 34055;
    for (int i = 0; i < 34055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34056(int& val) {
    func_in_34056(val);
    const int x = 34056;
    std::cout << 34056;
    for (int i = 0; i < 34056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34056(int& val) {
    const int x = 34056;
    std::cout << 34056;
    for (int i = 0; i < 34056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34057(int& val) {
    func_in_34057(val);
    const int x = 34057;
    std::cout << 34057;
    for (int i = 0; i < 34057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34057(int& val) {
    const int x = 34057;
    std::cout << 34057;
    for (int i = 0; i < 34057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34058(int& val) {
    func_in_34058(val);
    const int x = 34058;
    std::cout << 34058;
    for (int i = 0; i < 34058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34058(int& val) {
    const int x = 34058;
    std::cout << 34058;
    for (int i = 0; i < 34058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34059(int& val) {
    func_in_34059(val);
    const int x = 34059;
    std::cout << 34059;
    for (int i = 0; i < 34059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34059(int& val) {
    const int x = 34059;
    std::cout << 34059;
    for (int i = 0; i < 34059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34060(int& val) {
    func_in_34060(val);
    const int x = 34060;
    std::cout << 34060;
    for (int i = 0; i < 34060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34060(int& val) {
    const int x = 34060;
    std::cout << 34060;
    for (int i = 0; i < 34060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34061(int& val) {
    func_in_34061(val);
    const int x = 34061;
    std::cout << 34061;
    for (int i = 0; i < 34061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34061(int& val) {
    const int x = 34061;
    std::cout << 34061;
    for (int i = 0; i < 34061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34062(int& val) {
    func_in_34062(val);
    const int x = 34062;
    std::cout << 34062;
    for (int i = 0; i < 34062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34062(int& val) {
    const int x = 34062;
    std::cout << 34062;
    for (int i = 0; i < 34062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34063(int& val) {
    func_in_34063(val);
    const int x = 34063;
    std::cout << 34063;
    for (int i = 0; i < 34063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34063(int& val) {
    const int x = 34063;
    std::cout << 34063;
    for (int i = 0; i < 34063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34064(int& val) {
    func_in_34064(val);
    const int x = 34064;
    std::cout << 34064;
    for (int i = 0; i < 34064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34064(int& val) {
    const int x = 34064;
    std::cout << 34064;
    for (int i = 0; i < 34064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34065(int& val) {
    func_in_34065(val);
    const int x = 34065;
    std::cout << 34065;
    for (int i = 0; i < 34065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34065(int& val) {
    const int x = 34065;
    std::cout << 34065;
    for (int i = 0; i < 34065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34066(int& val) {
    func_in_34066(val);
    const int x = 34066;
    std::cout << 34066;
    for (int i = 0; i < 34066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34066(int& val) {
    const int x = 34066;
    std::cout << 34066;
    for (int i = 0; i < 34066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34067(int& val) {
    func_in_34067(val);
    const int x = 34067;
    std::cout << 34067;
    for (int i = 0; i < 34067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34067(int& val) {
    const int x = 34067;
    std::cout << 34067;
    for (int i = 0; i < 34067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34068(int& val) {
    func_in_34068(val);
    const int x = 34068;
    std::cout << 34068;
    for (int i = 0; i < 34068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34068(int& val) {
    const int x = 34068;
    std::cout << 34068;
    for (int i = 0; i < 34068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34069(int& val) {
    func_in_34069(val);
    const int x = 34069;
    std::cout << 34069;
    for (int i = 0; i < 34069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34069(int& val) {
    const int x = 34069;
    std::cout << 34069;
    for (int i = 0; i < 34069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34070(int& val) {
    func_in_34070(val);
    const int x = 34070;
    std::cout << 34070;
    for (int i = 0; i < 34070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34070(int& val) {
    const int x = 34070;
    std::cout << 34070;
    for (int i = 0; i < 34070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34071(int& val) {
    func_in_34071(val);
    const int x = 34071;
    std::cout << 34071;
    for (int i = 0; i < 34071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34071(int& val) {
    const int x = 34071;
    std::cout << 34071;
    for (int i = 0; i < 34071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34072(int& val) {
    func_in_34072(val);
    const int x = 34072;
    std::cout << 34072;
    for (int i = 0; i < 34072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34072(int& val) {
    const int x = 34072;
    std::cout << 34072;
    for (int i = 0; i < 34072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34073(int& val) {
    func_in_34073(val);
    const int x = 34073;
    std::cout << 34073;
    for (int i = 0; i < 34073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34073(int& val) {
    const int x = 34073;
    std::cout << 34073;
    for (int i = 0; i < 34073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34074(int& val) {
    func_in_34074(val);
    const int x = 34074;
    std::cout << 34074;
    for (int i = 0; i < 34074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34074(int& val) {
    const int x = 34074;
    std::cout << 34074;
    for (int i = 0; i < 34074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34075(int& val) {
    func_in_34075(val);
    const int x = 34075;
    std::cout << 34075;
    for (int i = 0; i < 34075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34075(int& val) {
    const int x = 34075;
    std::cout << 34075;
    for (int i = 0; i < 34075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34076(int& val) {
    func_in_34076(val);
    const int x = 34076;
    std::cout << 34076;
    for (int i = 0; i < 34076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34076(int& val) {
    const int x = 34076;
    std::cout << 34076;
    for (int i = 0; i < 34076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34077(int& val) {
    func_in_34077(val);
    const int x = 34077;
    std::cout << 34077;
    for (int i = 0; i < 34077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34077(int& val) {
    const int x = 34077;
    std::cout << 34077;
    for (int i = 0; i < 34077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34078(int& val) {
    func_in_34078(val);
    const int x = 34078;
    std::cout << 34078;
    for (int i = 0; i < 34078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34078(int& val) {
    const int x = 34078;
    std::cout << 34078;
    for (int i = 0; i < 34078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34079(int& val) {
    func_in_34079(val);
    const int x = 34079;
    std::cout << 34079;
    for (int i = 0; i < 34079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34079(int& val) {
    const int x = 34079;
    std::cout << 34079;
    for (int i = 0; i < 34079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34080(int& val) {
    func_in_34080(val);
    const int x = 34080;
    std::cout << 34080;
    for (int i = 0; i < 34080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34080(int& val) {
    const int x = 34080;
    std::cout << 34080;
    for (int i = 0; i < 34080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34081(int& val) {
    func_in_34081(val);
    const int x = 34081;
    std::cout << 34081;
    for (int i = 0; i < 34081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34081(int& val) {
    const int x = 34081;
    std::cout << 34081;
    for (int i = 0; i < 34081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34082(int& val) {
    func_in_34082(val);
    const int x = 34082;
    std::cout << 34082;
    for (int i = 0; i < 34082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34082(int& val) {
    const int x = 34082;
    std::cout << 34082;
    for (int i = 0; i < 34082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34083(int& val) {
    func_in_34083(val);
    const int x = 34083;
    std::cout << 34083;
    for (int i = 0; i < 34083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34083(int& val) {
    const int x = 34083;
    std::cout << 34083;
    for (int i = 0; i < 34083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34084(int& val) {
    func_in_34084(val);
    const int x = 34084;
    std::cout << 34084;
    for (int i = 0; i < 34084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34084(int& val) {
    const int x = 34084;
    std::cout << 34084;
    for (int i = 0; i < 34084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34085(int& val) {
    func_in_34085(val);
    const int x = 34085;
    std::cout << 34085;
    for (int i = 0; i < 34085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34085(int& val) {
    const int x = 34085;
    std::cout << 34085;
    for (int i = 0; i < 34085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34086(int& val) {
    func_in_34086(val);
    const int x = 34086;
    std::cout << 34086;
    for (int i = 0; i < 34086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34086(int& val) {
    const int x = 34086;
    std::cout << 34086;
    for (int i = 0; i < 34086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34087(int& val) {
    func_in_34087(val);
    const int x = 34087;
    std::cout << 34087;
    for (int i = 0; i < 34087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34087(int& val) {
    const int x = 34087;
    std::cout << 34087;
    for (int i = 0; i < 34087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34088(int& val) {
    func_in_34088(val);
    const int x = 34088;
    std::cout << 34088;
    for (int i = 0; i < 34088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34088(int& val) {
    const int x = 34088;
    std::cout << 34088;
    for (int i = 0; i < 34088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34089(int& val) {
    func_in_34089(val);
    const int x = 34089;
    std::cout << 34089;
    for (int i = 0; i < 34089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34089(int& val) {
    const int x = 34089;
    std::cout << 34089;
    for (int i = 0; i < 34089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34090(int& val) {
    func_in_34090(val);
    const int x = 34090;
    std::cout << 34090;
    for (int i = 0; i < 34090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34090(int& val) {
    const int x = 34090;
    std::cout << 34090;
    for (int i = 0; i < 34090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34091(int& val) {
    func_in_34091(val);
    const int x = 34091;
    std::cout << 34091;
    for (int i = 0; i < 34091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34091(int& val) {
    const int x = 34091;
    std::cout << 34091;
    for (int i = 0; i < 34091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34092(int& val) {
    func_in_34092(val);
    const int x = 34092;
    std::cout << 34092;
    for (int i = 0; i < 34092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34092(int& val) {
    const int x = 34092;
    std::cout << 34092;
    for (int i = 0; i < 34092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34093(int& val) {
    func_in_34093(val);
    const int x = 34093;
    std::cout << 34093;
    for (int i = 0; i < 34093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34093(int& val) {
    const int x = 34093;
    std::cout << 34093;
    for (int i = 0; i < 34093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34094(int& val) {
    func_in_34094(val);
    const int x = 34094;
    std::cout << 34094;
    for (int i = 0; i < 34094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34094(int& val) {
    const int x = 34094;
    std::cout << 34094;
    for (int i = 0; i < 34094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34095(int& val) {
    func_in_34095(val);
    const int x = 34095;
    std::cout << 34095;
    for (int i = 0; i < 34095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34095(int& val) {
    const int x = 34095;
    std::cout << 34095;
    for (int i = 0; i < 34095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34096(int& val) {
    func_in_34096(val);
    const int x = 34096;
    std::cout << 34096;
    for (int i = 0; i < 34096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34096(int& val) {
    const int x = 34096;
    std::cout << 34096;
    for (int i = 0; i < 34096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34097(int& val) {
    func_in_34097(val);
    const int x = 34097;
    std::cout << 34097;
    for (int i = 0; i < 34097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34097(int& val) {
    const int x = 34097;
    std::cout << 34097;
    for (int i = 0; i < 34097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34098(int& val) {
    func_in_34098(val);
    const int x = 34098;
    std::cout << 34098;
    for (int i = 0; i < 34098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34098(int& val) {
    const int x = 34098;
    std::cout << 34098;
    for (int i = 0; i < 34098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34099(int& val) {
    func_in_34099(val);
    const int x = 34099;
    std::cout << 34099;
    for (int i = 0; i < 34099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34099(int& val) {
    const int x = 34099;
    std::cout << 34099;
    for (int i = 0; i < 34099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34100(int& val) {
    func_in_34100(val);
    const int x = 34100;
    std::cout << 34100;
    for (int i = 0; i < 34100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34100(int& val) {
    const int x = 34100;
    std::cout << 34100;
    for (int i = 0; i < 34100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34101(int& val) {
    func_in_34101(val);
    const int x = 34101;
    std::cout << 34101;
    for (int i = 0; i < 34101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34101(int& val) {
    const int x = 34101;
    std::cout << 34101;
    for (int i = 0; i < 34101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34102(int& val) {
    func_in_34102(val);
    const int x = 34102;
    std::cout << 34102;
    for (int i = 0; i < 34102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34102(int& val) {
    const int x = 34102;
    std::cout << 34102;
    for (int i = 0; i < 34102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34103(int& val) {
    func_in_34103(val);
    const int x = 34103;
    std::cout << 34103;
    for (int i = 0; i < 34103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34103(int& val) {
    const int x = 34103;
    std::cout << 34103;
    for (int i = 0; i < 34103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34104(int& val) {
    func_in_34104(val);
    const int x = 34104;
    std::cout << 34104;
    for (int i = 0; i < 34104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34104(int& val) {
    const int x = 34104;
    std::cout << 34104;
    for (int i = 0; i < 34104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34105(int& val) {
    func_in_34105(val);
    const int x = 34105;
    std::cout << 34105;
    for (int i = 0; i < 34105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34105(int& val) {
    const int x = 34105;
    std::cout << 34105;
    for (int i = 0; i < 34105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34106(int& val) {
    func_in_34106(val);
    const int x = 34106;
    std::cout << 34106;
    for (int i = 0; i < 34106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34106(int& val) {
    const int x = 34106;
    std::cout << 34106;
    for (int i = 0; i < 34106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34107(int& val) {
    func_in_34107(val);
    const int x = 34107;
    std::cout << 34107;
    for (int i = 0; i < 34107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34107(int& val) {
    const int x = 34107;
    std::cout << 34107;
    for (int i = 0; i < 34107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34108(int& val) {
    func_in_34108(val);
    const int x = 34108;
    std::cout << 34108;
    for (int i = 0; i < 34108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34108(int& val) {
    const int x = 34108;
    std::cout << 34108;
    for (int i = 0; i < 34108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34109(int& val) {
    func_in_34109(val);
    const int x = 34109;
    std::cout << 34109;
    for (int i = 0; i < 34109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34109(int& val) {
    const int x = 34109;
    std::cout << 34109;
    for (int i = 0; i < 34109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34110(int& val) {
    func_in_34110(val);
    const int x = 34110;
    std::cout << 34110;
    for (int i = 0; i < 34110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34110(int& val) {
    const int x = 34110;
    std::cout << 34110;
    for (int i = 0; i < 34110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34111(int& val) {
    func_in_34111(val);
    const int x = 34111;
    std::cout << 34111;
    for (int i = 0; i < 34111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34111(int& val) {
    const int x = 34111;
    std::cout << 34111;
    for (int i = 0; i < 34111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34112(int& val) {
    func_in_34112(val);
    const int x = 34112;
    std::cout << 34112;
    for (int i = 0; i < 34112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34112(int& val) {
    const int x = 34112;
    std::cout << 34112;
    for (int i = 0; i < 34112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34113(int& val) {
    func_in_34113(val);
    const int x = 34113;
    std::cout << 34113;
    for (int i = 0; i < 34113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34113(int& val) {
    const int x = 34113;
    std::cout << 34113;
    for (int i = 0; i < 34113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34114(int& val) {
    func_in_34114(val);
    const int x = 34114;
    std::cout << 34114;
    for (int i = 0; i < 34114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34114(int& val) {
    const int x = 34114;
    std::cout << 34114;
    for (int i = 0; i < 34114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34115(int& val) {
    func_in_34115(val);
    const int x = 34115;
    std::cout << 34115;
    for (int i = 0; i < 34115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34115(int& val) {
    const int x = 34115;
    std::cout << 34115;
    for (int i = 0; i < 34115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34116(int& val) {
    func_in_34116(val);
    const int x = 34116;
    std::cout << 34116;
    for (int i = 0; i < 34116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34116(int& val) {
    const int x = 34116;
    std::cout << 34116;
    for (int i = 0; i < 34116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34117(int& val) {
    func_in_34117(val);
    const int x = 34117;
    std::cout << 34117;
    for (int i = 0; i < 34117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34117(int& val) {
    const int x = 34117;
    std::cout << 34117;
    for (int i = 0; i < 34117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34118(int& val) {
    func_in_34118(val);
    const int x = 34118;
    std::cout << 34118;
    for (int i = 0; i < 34118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34118(int& val) {
    const int x = 34118;
    std::cout << 34118;
    for (int i = 0; i < 34118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34119(int& val) {
    func_in_34119(val);
    const int x = 34119;
    std::cout << 34119;
    for (int i = 0; i < 34119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34119(int& val) {
    const int x = 34119;
    std::cout << 34119;
    for (int i = 0; i < 34119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34120(int& val) {
    func_in_34120(val);
    const int x = 34120;
    std::cout << 34120;
    for (int i = 0; i < 34120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34120(int& val) {
    const int x = 34120;
    std::cout << 34120;
    for (int i = 0; i < 34120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34121(int& val) {
    func_in_34121(val);
    const int x = 34121;
    std::cout << 34121;
    for (int i = 0; i < 34121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34121(int& val) {
    const int x = 34121;
    std::cout << 34121;
    for (int i = 0; i < 34121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34122(int& val) {
    func_in_34122(val);
    const int x = 34122;
    std::cout << 34122;
    for (int i = 0; i < 34122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34122(int& val) {
    const int x = 34122;
    std::cout << 34122;
    for (int i = 0; i < 34122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34123(int& val) {
    func_in_34123(val);
    const int x = 34123;
    std::cout << 34123;
    for (int i = 0; i < 34123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34123(int& val) {
    const int x = 34123;
    std::cout << 34123;
    for (int i = 0; i < 34123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34124(int& val) {
    func_in_34124(val);
    const int x = 34124;
    std::cout << 34124;
    for (int i = 0; i < 34124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34124(int& val) {
    const int x = 34124;
    std::cout << 34124;
    for (int i = 0; i < 34124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34125(int& val) {
    func_in_34125(val);
    const int x = 34125;
    std::cout << 34125;
    for (int i = 0; i < 34125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34125(int& val) {
    const int x = 34125;
    std::cout << 34125;
    for (int i = 0; i < 34125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34126(int& val) {
    func_in_34126(val);
    const int x = 34126;
    std::cout << 34126;
    for (int i = 0; i < 34126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34126(int& val) {
    const int x = 34126;
    std::cout << 34126;
    for (int i = 0; i < 34126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34127(int& val) {
    func_in_34127(val);
    const int x = 34127;
    std::cout << 34127;
    for (int i = 0; i < 34127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34127(int& val) {
    const int x = 34127;
    std::cout << 34127;
    for (int i = 0; i < 34127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34128(int& val) {
    func_in_34128(val);
    const int x = 34128;
    std::cout << 34128;
    for (int i = 0; i < 34128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34128(int& val) {
    const int x = 34128;
    std::cout << 34128;
    for (int i = 0; i < 34128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34129(int& val) {
    func_in_34129(val);
    const int x = 34129;
    std::cout << 34129;
    for (int i = 0; i < 34129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34129(int& val) {
    const int x = 34129;
    std::cout << 34129;
    for (int i = 0; i < 34129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34130(int& val) {
    func_in_34130(val);
    const int x = 34130;
    std::cout << 34130;
    for (int i = 0; i < 34130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34130(int& val) {
    const int x = 34130;
    std::cout << 34130;
    for (int i = 0; i < 34130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34131(int& val) {
    func_in_34131(val);
    const int x = 34131;
    std::cout << 34131;
    for (int i = 0; i < 34131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34131(int& val) {
    const int x = 34131;
    std::cout << 34131;
    for (int i = 0; i < 34131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34132(int& val) {
    func_in_34132(val);
    const int x = 34132;
    std::cout << 34132;
    for (int i = 0; i < 34132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34132(int& val) {
    const int x = 34132;
    std::cout << 34132;
    for (int i = 0; i < 34132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34133(int& val) {
    func_in_34133(val);
    const int x = 34133;
    std::cout << 34133;
    for (int i = 0; i < 34133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34133(int& val) {
    const int x = 34133;
    std::cout << 34133;
    for (int i = 0; i < 34133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34134(int& val) {
    func_in_34134(val);
    const int x = 34134;
    std::cout << 34134;
    for (int i = 0; i < 34134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34134(int& val) {
    const int x = 34134;
    std::cout << 34134;
    for (int i = 0; i < 34134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34135(int& val) {
    func_in_34135(val);
    const int x = 34135;
    std::cout << 34135;
    for (int i = 0; i < 34135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34135(int& val) {
    const int x = 34135;
    std::cout << 34135;
    for (int i = 0; i < 34135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34136(int& val) {
    func_in_34136(val);
    const int x = 34136;
    std::cout << 34136;
    for (int i = 0; i < 34136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34136(int& val) {
    const int x = 34136;
    std::cout << 34136;
    for (int i = 0; i < 34136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34137(int& val) {
    func_in_34137(val);
    const int x = 34137;
    std::cout << 34137;
    for (int i = 0; i < 34137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34137(int& val) {
    const int x = 34137;
    std::cout << 34137;
    for (int i = 0; i < 34137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34138(int& val) {
    func_in_34138(val);
    const int x = 34138;
    std::cout << 34138;
    for (int i = 0; i < 34138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34138(int& val) {
    const int x = 34138;
    std::cout << 34138;
    for (int i = 0; i < 34138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34139(int& val) {
    func_in_34139(val);
    const int x = 34139;
    std::cout << 34139;
    for (int i = 0; i < 34139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34139(int& val) {
    const int x = 34139;
    std::cout << 34139;
    for (int i = 0; i < 34139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34140(int& val) {
    func_in_34140(val);
    const int x = 34140;
    std::cout << 34140;
    for (int i = 0; i < 34140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34140(int& val) {
    const int x = 34140;
    std::cout << 34140;
    for (int i = 0; i < 34140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34141(int& val) {
    func_in_34141(val);
    const int x = 34141;
    std::cout << 34141;
    for (int i = 0; i < 34141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34141(int& val) {
    const int x = 34141;
    std::cout << 34141;
    for (int i = 0; i < 34141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34142(int& val) {
    func_in_34142(val);
    const int x = 34142;
    std::cout << 34142;
    for (int i = 0; i < 34142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34142(int& val) {
    const int x = 34142;
    std::cout << 34142;
    for (int i = 0; i < 34142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34143(int& val) {
    func_in_34143(val);
    const int x = 34143;
    std::cout << 34143;
    for (int i = 0; i < 34143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34143(int& val) {
    const int x = 34143;
    std::cout << 34143;
    for (int i = 0; i < 34143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34144(int& val) {
    func_in_34144(val);
    const int x = 34144;
    std::cout << 34144;
    for (int i = 0; i < 34144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34144(int& val) {
    const int x = 34144;
    std::cout << 34144;
    for (int i = 0; i < 34144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34145(int& val) {
    func_in_34145(val);
    const int x = 34145;
    std::cout << 34145;
    for (int i = 0; i < 34145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34145(int& val) {
    const int x = 34145;
    std::cout << 34145;
    for (int i = 0; i < 34145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34146(int& val) {
    func_in_34146(val);
    const int x = 34146;
    std::cout << 34146;
    for (int i = 0; i < 34146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34146(int& val) {
    const int x = 34146;
    std::cout << 34146;
    for (int i = 0; i < 34146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34147(int& val) {
    func_in_34147(val);
    const int x = 34147;
    std::cout << 34147;
    for (int i = 0; i < 34147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34147(int& val) {
    const int x = 34147;
    std::cout << 34147;
    for (int i = 0; i < 34147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34148(int& val) {
    func_in_34148(val);
    const int x = 34148;
    std::cout << 34148;
    for (int i = 0; i < 34148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34148(int& val) {
    const int x = 34148;
    std::cout << 34148;
    for (int i = 0; i < 34148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34149(int& val) {
    func_in_34149(val);
    const int x = 34149;
    std::cout << 34149;
    for (int i = 0; i < 34149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34149(int& val) {
    const int x = 34149;
    std::cout << 34149;
    for (int i = 0; i < 34149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34150(int& val) {
    func_in_34150(val);
    const int x = 34150;
    std::cout << 34150;
    for (int i = 0; i < 34150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34150(int& val) {
    const int x = 34150;
    std::cout << 34150;
    for (int i = 0; i < 34150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34151(int& val) {
    func_in_34151(val);
    const int x = 34151;
    std::cout << 34151;
    for (int i = 0; i < 34151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34151(int& val) {
    const int x = 34151;
    std::cout << 34151;
    for (int i = 0; i < 34151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34152(int& val) {
    func_in_34152(val);
    const int x = 34152;
    std::cout << 34152;
    for (int i = 0; i < 34152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34152(int& val) {
    const int x = 34152;
    std::cout << 34152;
    for (int i = 0; i < 34152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34153(int& val) {
    func_in_34153(val);
    const int x = 34153;
    std::cout << 34153;
    for (int i = 0; i < 34153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34153(int& val) {
    const int x = 34153;
    std::cout << 34153;
    for (int i = 0; i < 34153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34154(int& val) {
    func_in_34154(val);
    const int x = 34154;
    std::cout << 34154;
    for (int i = 0; i < 34154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34154(int& val) {
    const int x = 34154;
    std::cout << 34154;
    for (int i = 0; i < 34154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34155(int& val) {
    func_in_34155(val);
    const int x = 34155;
    std::cout << 34155;
    for (int i = 0; i < 34155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34155(int& val) {
    const int x = 34155;
    std::cout << 34155;
    for (int i = 0; i < 34155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34156(int& val) {
    func_in_34156(val);
    const int x = 34156;
    std::cout << 34156;
    for (int i = 0; i < 34156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34156(int& val) {
    const int x = 34156;
    std::cout << 34156;
    for (int i = 0; i < 34156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34157(int& val) {
    func_in_34157(val);
    const int x = 34157;
    std::cout << 34157;
    for (int i = 0; i < 34157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34157(int& val) {
    const int x = 34157;
    std::cout << 34157;
    for (int i = 0; i < 34157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34158(int& val) {
    func_in_34158(val);
    const int x = 34158;
    std::cout << 34158;
    for (int i = 0; i < 34158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34158(int& val) {
    const int x = 34158;
    std::cout << 34158;
    for (int i = 0; i < 34158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34159(int& val) {
    func_in_34159(val);
    const int x = 34159;
    std::cout << 34159;
    for (int i = 0; i < 34159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34159(int& val) {
    const int x = 34159;
    std::cout << 34159;
    for (int i = 0; i < 34159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34160(int& val) {
    func_in_34160(val);
    const int x = 34160;
    std::cout << 34160;
    for (int i = 0; i < 34160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34160(int& val) {
    const int x = 34160;
    std::cout << 34160;
    for (int i = 0; i < 34160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34161(int& val) {
    func_in_34161(val);
    const int x = 34161;
    std::cout << 34161;
    for (int i = 0; i < 34161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34161(int& val) {
    const int x = 34161;
    std::cout << 34161;
    for (int i = 0; i < 34161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34162(int& val) {
    func_in_34162(val);
    const int x = 34162;
    std::cout << 34162;
    for (int i = 0; i < 34162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34162(int& val) {
    const int x = 34162;
    std::cout << 34162;
    for (int i = 0; i < 34162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34163(int& val) {
    func_in_34163(val);
    const int x = 34163;
    std::cout << 34163;
    for (int i = 0; i < 34163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34163(int& val) {
    const int x = 34163;
    std::cout << 34163;
    for (int i = 0; i < 34163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34164(int& val) {
    func_in_34164(val);
    const int x = 34164;
    std::cout << 34164;
    for (int i = 0; i < 34164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34164(int& val) {
    const int x = 34164;
    std::cout << 34164;
    for (int i = 0; i < 34164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34165(int& val) {
    func_in_34165(val);
    const int x = 34165;
    std::cout << 34165;
    for (int i = 0; i < 34165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34165(int& val) {
    const int x = 34165;
    std::cout << 34165;
    for (int i = 0; i < 34165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34166(int& val) {
    func_in_34166(val);
    const int x = 34166;
    std::cout << 34166;
    for (int i = 0; i < 34166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34166(int& val) {
    const int x = 34166;
    std::cout << 34166;
    for (int i = 0; i < 34166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34167(int& val) {
    func_in_34167(val);
    const int x = 34167;
    std::cout << 34167;
    for (int i = 0; i < 34167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34167(int& val) {
    const int x = 34167;
    std::cout << 34167;
    for (int i = 0; i < 34167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34168(int& val) {
    func_in_34168(val);
    const int x = 34168;
    std::cout << 34168;
    for (int i = 0; i < 34168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34168(int& val) {
    const int x = 34168;
    std::cout << 34168;
    for (int i = 0; i < 34168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34169(int& val) {
    func_in_34169(val);
    const int x = 34169;
    std::cout << 34169;
    for (int i = 0; i < 34169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34169(int& val) {
    const int x = 34169;
    std::cout << 34169;
    for (int i = 0; i < 34169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34170(int& val) {
    func_in_34170(val);
    const int x = 34170;
    std::cout << 34170;
    for (int i = 0; i < 34170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34170(int& val) {
    const int x = 34170;
    std::cout << 34170;
    for (int i = 0; i < 34170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34171(int& val) {
    func_in_34171(val);
    const int x = 34171;
    std::cout << 34171;
    for (int i = 0; i < 34171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34171(int& val) {
    const int x = 34171;
    std::cout << 34171;
    for (int i = 0; i < 34171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34172(int& val) {
    func_in_34172(val);
    const int x = 34172;
    std::cout << 34172;
    for (int i = 0; i < 34172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34172(int& val) {
    const int x = 34172;
    std::cout << 34172;
    for (int i = 0; i < 34172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34173(int& val) {
    func_in_34173(val);
    const int x = 34173;
    std::cout << 34173;
    for (int i = 0; i < 34173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34173(int& val) {
    const int x = 34173;
    std::cout << 34173;
    for (int i = 0; i < 34173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34174(int& val) {
    func_in_34174(val);
    const int x = 34174;
    std::cout << 34174;
    for (int i = 0; i < 34174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34174(int& val) {
    const int x = 34174;
    std::cout << 34174;
    for (int i = 0; i < 34174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34175(int& val) {
    func_in_34175(val);
    const int x = 34175;
    std::cout << 34175;
    for (int i = 0; i < 34175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34175(int& val) {
    const int x = 34175;
    std::cout << 34175;
    for (int i = 0; i < 34175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34176(int& val) {
    func_in_34176(val);
    const int x = 34176;
    std::cout << 34176;
    for (int i = 0; i < 34176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34176(int& val) {
    const int x = 34176;
    std::cout << 34176;
    for (int i = 0; i < 34176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34177(int& val) {
    func_in_34177(val);
    const int x = 34177;
    std::cout << 34177;
    for (int i = 0; i < 34177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34177(int& val) {
    const int x = 34177;
    std::cout << 34177;
    for (int i = 0; i < 34177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34178(int& val) {
    func_in_34178(val);
    const int x = 34178;
    std::cout << 34178;
    for (int i = 0; i < 34178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34178(int& val) {
    const int x = 34178;
    std::cout << 34178;
    for (int i = 0; i < 34178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34179(int& val) {
    func_in_34179(val);
    const int x = 34179;
    std::cout << 34179;
    for (int i = 0; i < 34179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34179(int& val) {
    const int x = 34179;
    std::cout << 34179;
    for (int i = 0; i < 34179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34180(int& val) {
    func_in_34180(val);
    const int x = 34180;
    std::cout << 34180;
    for (int i = 0; i < 34180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34180(int& val) {
    const int x = 34180;
    std::cout << 34180;
    for (int i = 0; i < 34180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34181(int& val) {
    func_in_34181(val);
    const int x = 34181;
    std::cout << 34181;
    for (int i = 0; i < 34181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34181(int& val) {
    const int x = 34181;
    std::cout << 34181;
    for (int i = 0; i < 34181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34182(int& val) {
    func_in_34182(val);
    const int x = 34182;
    std::cout << 34182;
    for (int i = 0; i < 34182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34182(int& val) {
    const int x = 34182;
    std::cout << 34182;
    for (int i = 0; i < 34182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34183(int& val) {
    func_in_34183(val);
    const int x = 34183;
    std::cout << 34183;
    for (int i = 0; i < 34183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34183(int& val) {
    const int x = 34183;
    std::cout << 34183;
    for (int i = 0; i < 34183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34184(int& val) {
    func_in_34184(val);
    const int x = 34184;
    std::cout << 34184;
    for (int i = 0; i < 34184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34184(int& val) {
    const int x = 34184;
    std::cout << 34184;
    for (int i = 0; i < 34184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34185(int& val) {
    func_in_34185(val);
    const int x = 34185;
    std::cout << 34185;
    for (int i = 0; i < 34185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34185(int& val) {
    const int x = 34185;
    std::cout << 34185;
    for (int i = 0; i < 34185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34186(int& val) {
    func_in_34186(val);
    const int x = 34186;
    std::cout << 34186;
    for (int i = 0; i < 34186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34186(int& val) {
    const int x = 34186;
    std::cout << 34186;
    for (int i = 0; i < 34186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34187(int& val) {
    func_in_34187(val);
    const int x = 34187;
    std::cout << 34187;
    for (int i = 0; i < 34187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34187(int& val) {
    const int x = 34187;
    std::cout << 34187;
    for (int i = 0; i < 34187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34188(int& val) {
    func_in_34188(val);
    const int x = 34188;
    std::cout << 34188;
    for (int i = 0; i < 34188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34188(int& val) {
    const int x = 34188;
    std::cout << 34188;
    for (int i = 0; i < 34188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34189(int& val) {
    func_in_34189(val);
    const int x = 34189;
    std::cout << 34189;
    for (int i = 0; i < 34189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34189(int& val) {
    const int x = 34189;
    std::cout << 34189;
    for (int i = 0; i < 34189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34190(int& val) {
    func_in_34190(val);
    const int x = 34190;
    std::cout << 34190;
    for (int i = 0; i < 34190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34190(int& val) {
    const int x = 34190;
    std::cout << 34190;
    for (int i = 0; i < 34190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34191(int& val) {
    func_in_34191(val);
    const int x = 34191;
    std::cout << 34191;
    for (int i = 0; i < 34191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34191(int& val) {
    const int x = 34191;
    std::cout << 34191;
    for (int i = 0; i < 34191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34192(int& val) {
    func_in_34192(val);
    const int x = 34192;
    std::cout << 34192;
    for (int i = 0; i < 34192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34192(int& val) {
    const int x = 34192;
    std::cout << 34192;
    for (int i = 0; i < 34192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34193(int& val) {
    func_in_34193(val);
    const int x = 34193;
    std::cout << 34193;
    for (int i = 0; i < 34193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34193(int& val) {
    const int x = 34193;
    std::cout << 34193;
    for (int i = 0; i < 34193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34194(int& val) {
    func_in_34194(val);
    const int x = 34194;
    std::cout << 34194;
    for (int i = 0; i < 34194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34194(int& val) {
    const int x = 34194;
    std::cout << 34194;
    for (int i = 0; i < 34194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34195(int& val) {
    func_in_34195(val);
    const int x = 34195;
    std::cout << 34195;
    for (int i = 0; i < 34195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34195(int& val) {
    const int x = 34195;
    std::cout << 34195;
    for (int i = 0; i < 34195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34196(int& val) {
    func_in_34196(val);
    const int x = 34196;
    std::cout << 34196;
    for (int i = 0; i < 34196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34196(int& val) {
    const int x = 34196;
    std::cout << 34196;
    for (int i = 0; i < 34196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34197(int& val) {
    func_in_34197(val);
    const int x = 34197;
    std::cout << 34197;
    for (int i = 0; i < 34197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34197(int& val) {
    const int x = 34197;
    std::cout << 34197;
    for (int i = 0; i < 34197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34198(int& val) {
    func_in_34198(val);
    const int x = 34198;
    std::cout << 34198;
    for (int i = 0; i < 34198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34198(int& val) {
    const int x = 34198;
    std::cout << 34198;
    for (int i = 0; i < 34198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34199(int& val) {
    func_in_34199(val);
    const int x = 34199;
    std::cout << 34199;
    for (int i = 0; i < 34199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34199(int& val) {
    const int x = 34199;
    std::cout << 34199;
    for (int i = 0; i < 34199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34200(int& val) {
    func_in_34200(val);
    const int x = 34200;
    std::cout << 34200;
    for (int i = 0; i < 34200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34200(int& val) {
    const int x = 34200;
    std::cout << 34200;
    for (int i = 0; i < 34200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34201(int& val) {
    func_in_34201(val);
    const int x = 34201;
    std::cout << 34201;
    for (int i = 0; i < 34201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34201(int& val) {
    const int x = 34201;
    std::cout << 34201;
    for (int i = 0; i < 34201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34202(int& val) {
    func_in_34202(val);
    const int x = 34202;
    std::cout << 34202;
    for (int i = 0; i < 34202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34202(int& val) {
    const int x = 34202;
    std::cout << 34202;
    for (int i = 0; i < 34202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34203(int& val) {
    func_in_34203(val);
    const int x = 34203;
    std::cout << 34203;
    for (int i = 0; i < 34203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34203(int& val) {
    const int x = 34203;
    std::cout << 34203;
    for (int i = 0; i < 34203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34204(int& val) {
    func_in_34204(val);
    const int x = 34204;
    std::cout << 34204;
    for (int i = 0; i < 34204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34204(int& val) {
    const int x = 34204;
    std::cout << 34204;
    for (int i = 0; i < 34204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34205(int& val) {
    func_in_34205(val);
    const int x = 34205;
    std::cout << 34205;
    for (int i = 0; i < 34205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34205(int& val) {
    const int x = 34205;
    std::cout << 34205;
    for (int i = 0; i < 34205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34206(int& val) {
    func_in_34206(val);
    const int x = 34206;
    std::cout << 34206;
    for (int i = 0; i < 34206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34206(int& val) {
    const int x = 34206;
    std::cout << 34206;
    for (int i = 0; i < 34206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34207(int& val) {
    func_in_34207(val);
    const int x = 34207;
    std::cout << 34207;
    for (int i = 0; i < 34207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34207(int& val) {
    const int x = 34207;
    std::cout << 34207;
    for (int i = 0; i < 34207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34208(int& val) {
    func_in_34208(val);
    const int x = 34208;
    std::cout << 34208;
    for (int i = 0; i < 34208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34208(int& val) {
    const int x = 34208;
    std::cout << 34208;
    for (int i = 0; i < 34208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34209(int& val) {
    func_in_34209(val);
    const int x = 34209;
    std::cout << 34209;
    for (int i = 0; i < 34209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34209(int& val) {
    const int x = 34209;
    std::cout << 34209;
    for (int i = 0; i < 34209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34210(int& val) {
    func_in_34210(val);
    const int x = 34210;
    std::cout << 34210;
    for (int i = 0; i < 34210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34210(int& val) {
    const int x = 34210;
    std::cout << 34210;
    for (int i = 0; i < 34210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34211(int& val) {
    func_in_34211(val);
    const int x = 34211;
    std::cout << 34211;
    for (int i = 0; i < 34211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34211(int& val) {
    const int x = 34211;
    std::cout << 34211;
    for (int i = 0; i < 34211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34212(int& val) {
    func_in_34212(val);
    const int x = 34212;
    std::cout << 34212;
    for (int i = 0; i < 34212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34212(int& val) {
    const int x = 34212;
    std::cout << 34212;
    for (int i = 0; i < 34212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34213(int& val) {
    func_in_34213(val);
    const int x = 34213;
    std::cout << 34213;
    for (int i = 0; i < 34213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34213(int& val) {
    const int x = 34213;
    std::cout << 34213;
    for (int i = 0; i < 34213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34214(int& val) {
    func_in_34214(val);
    const int x = 34214;
    std::cout << 34214;
    for (int i = 0; i < 34214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34214(int& val) {
    const int x = 34214;
    std::cout << 34214;
    for (int i = 0; i < 34214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34215(int& val) {
    func_in_34215(val);
    const int x = 34215;
    std::cout << 34215;
    for (int i = 0; i < 34215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34215(int& val) {
    const int x = 34215;
    std::cout << 34215;
    for (int i = 0; i < 34215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34216(int& val) {
    func_in_34216(val);
    const int x = 34216;
    std::cout << 34216;
    for (int i = 0; i < 34216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34216(int& val) {
    const int x = 34216;
    std::cout << 34216;
    for (int i = 0; i < 34216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34217(int& val) {
    func_in_34217(val);
    const int x = 34217;
    std::cout << 34217;
    for (int i = 0; i < 34217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34217(int& val) {
    const int x = 34217;
    std::cout << 34217;
    for (int i = 0; i < 34217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34218(int& val) {
    func_in_34218(val);
    const int x = 34218;
    std::cout << 34218;
    for (int i = 0; i < 34218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34218(int& val) {
    const int x = 34218;
    std::cout << 34218;
    for (int i = 0; i < 34218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34219(int& val) {
    func_in_34219(val);
    const int x = 34219;
    std::cout << 34219;
    for (int i = 0; i < 34219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34219(int& val) {
    const int x = 34219;
    std::cout << 34219;
    for (int i = 0; i < 34219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34220(int& val) {
    func_in_34220(val);
    const int x = 34220;
    std::cout << 34220;
    for (int i = 0; i < 34220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34220(int& val) {
    const int x = 34220;
    std::cout << 34220;
    for (int i = 0; i < 34220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34221(int& val) {
    func_in_34221(val);
    const int x = 34221;
    std::cout << 34221;
    for (int i = 0; i < 34221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34221(int& val) {
    const int x = 34221;
    std::cout << 34221;
    for (int i = 0; i < 34221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34222(int& val) {
    func_in_34222(val);
    const int x = 34222;
    std::cout << 34222;
    for (int i = 0; i < 34222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34222(int& val) {
    const int x = 34222;
    std::cout << 34222;
    for (int i = 0; i < 34222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34223(int& val) {
    func_in_34223(val);
    const int x = 34223;
    std::cout << 34223;
    for (int i = 0; i < 34223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34223(int& val) {
    const int x = 34223;
    std::cout << 34223;
    for (int i = 0; i < 34223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34224(int& val) {
    func_in_34224(val);
    const int x = 34224;
    std::cout << 34224;
    for (int i = 0; i < 34224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34224(int& val) {
    const int x = 34224;
    std::cout << 34224;
    for (int i = 0; i < 34224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34225(int& val) {
    func_in_34225(val);
    const int x = 34225;
    std::cout << 34225;
    for (int i = 0; i < 34225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34225(int& val) {
    const int x = 34225;
    std::cout << 34225;
    for (int i = 0; i < 34225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34226(int& val) {
    func_in_34226(val);
    const int x = 34226;
    std::cout << 34226;
    for (int i = 0; i < 34226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34226(int& val) {
    const int x = 34226;
    std::cout << 34226;
    for (int i = 0; i < 34226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34227(int& val) {
    func_in_34227(val);
    const int x = 34227;
    std::cout << 34227;
    for (int i = 0; i < 34227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34227(int& val) {
    const int x = 34227;
    std::cout << 34227;
    for (int i = 0; i < 34227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34228(int& val) {
    func_in_34228(val);
    const int x = 34228;
    std::cout << 34228;
    for (int i = 0; i < 34228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34228(int& val) {
    const int x = 34228;
    std::cout << 34228;
    for (int i = 0; i < 34228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34229(int& val) {
    func_in_34229(val);
    const int x = 34229;
    std::cout << 34229;
    for (int i = 0; i < 34229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34229(int& val) {
    const int x = 34229;
    std::cout << 34229;
    for (int i = 0; i < 34229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34230(int& val) {
    func_in_34230(val);
    const int x = 34230;
    std::cout << 34230;
    for (int i = 0; i < 34230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34230(int& val) {
    const int x = 34230;
    std::cout << 34230;
    for (int i = 0; i < 34230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34231(int& val) {
    func_in_34231(val);
    const int x = 34231;
    std::cout << 34231;
    for (int i = 0; i < 34231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34231(int& val) {
    const int x = 34231;
    std::cout << 34231;
    for (int i = 0; i < 34231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34232(int& val) {
    func_in_34232(val);
    const int x = 34232;
    std::cout << 34232;
    for (int i = 0; i < 34232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34232(int& val) {
    const int x = 34232;
    std::cout << 34232;
    for (int i = 0; i < 34232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34233(int& val) {
    func_in_34233(val);
    const int x = 34233;
    std::cout << 34233;
    for (int i = 0; i < 34233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34233(int& val) {
    const int x = 34233;
    std::cout << 34233;
    for (int i = 0; i < 34233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34234(int& val) {
    func_in_34234(val);
    const int x = 34234;
    std::cout << 34234;
    for (int i = 0; i < 34234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34234(int& val) {
    const int x = 34234;
    std::cout << 34234;
    for (int i = 0; i < 34234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34235(int& val) {
    func_in_34235(val);
    const int x = 34235;
    std::cout << 34235;
    for (int i = 0; i < 34235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34235(int& val) {
    const int x = 34235;
    std::cout << 34235;
    for (int i = 0; i < 34235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34236(int& val) {
    func_in_34236(val);
    const int x = 34236;
    std::cout << 34236;
    for (int i = 0; i < 34236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34236(int& val) {
    const int x = 34236;
    std::cout << 34236;
    for (int i = 0; i < 34236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34237(int& val) {
    func_in_34237(val);
    const int x = 34237;
    std::cout << 34237;
    for (int i = 0; i < 34237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34237(int& val) {
    const int x = 34237;
    std::cout << 34237;
    for (int i = 0; i < 34237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34238(int& val) {
    func_in_34238(val);
    const int x = 34238;
    std::cout << 34238;
    for (int i = 0; i < 34238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34238(int& val) {
    const int x = 34238;
    std::cout << 34238;
    for (int i = 0; i < 34238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34239(int& val) {
    func_in_34239(val);
    const int x = 34239;
    std::cout << 34239;
    for (int i = 0; i < 34239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34239(int& val) {
    const int x = 34239;
    std::cout << 34239;
    for (int i = 0; i < 34239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34240(int& val) {
    func_in_34240(val);
    const int x = 34240;
    std::cout << 34240;
    for (int i = 0; i < 34240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34240(int& val) {
    const int x = 34240;
    std::cout << 34240;
    for (int i = 0; i < 34240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34241(int& val) {
    func_in_34241(val);
    const int x = 34241;
    std::cout << 34241;
    for (int i = 0; i < 34241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34241(int& val) {
    const int x = 34241;
    std::cout << 34241;
    for (int i = 0; i < 34241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34242(int& val) {
    func_in_34242(val);
    const int x = 34242;
    std::cout << 34242;
    for (int i = 0; i < 34242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34242(int& val) {
    const int x = 34242;
    std::cout << 34242;
    for (int i = 0; i < 34242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34243(int& val) {
    func_in_34243(val);
    const int x = 34243;
    std::cout << 34243;
    for (int i = 0; i < 34243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34243(int& val) {
    const int x = 34243;
    std::cout << 34243;
    for (int i = 0; i < 34243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34244(int& val) {
    func_in_34244(val);
    const int x = 34244;
    std::cout << 34244;
    for (int i = 0; i < 34244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34244(int& val) {
    const int x = 34244;
    std::cout << 34244;
    for (int i = 0; i < 34244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34245(int& val) {
    func_in_34245(val);
    const int x = 34245;
    std::cout << 34245;
    for (int i = 0; i < 34245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34245(int& val) {
    const int x = 34245;
    std::cout << 34245;
    for (int i = 0; i < 34245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34246(int& val) {
    func_in_34246(val);
    const int x = 34246;
    std::cout << 34246;
    for (int i = 0; i < 34246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34246(int& val) {
    const int x = 34246;
    std::cout << 34246;
    for (int i = 0; i < 34246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34247(int& val) {
    func_in_34247(val);
    const int x = 34247;
    std::cout << 34247;
    for (int i = 0; i < 34247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34247(int& val) {
    const int x = 34247;
    std::cout << 34247;
    for (int i = 0; i < 34247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34248(int& val) {
    func_in_34248(val);
    const int x = 34248;
    std::cout << 34248;
    for (int i = 0; i < 34248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34248(int& val) {
    const int x = 34248;
    std::cout << 34248;
    for (int i = 0; i < 34248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34249(int& val) {
    func_in_34249(val);
    const int x = 34249;
    std::cout << 34249;
    for (int i = 0; i < 34249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34249(int& val) {
    const int x = 34249;
    std::cout << 34249;
    for (int i = 0; i < 34249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34250(int& val) {
    func_in_34250(val);
    const int x = 34250;
    std::cout << 34250;
    for (int i = 0; i < 34250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34250(int& val) {
    const int x = 34250;
    std::cout << 34250;
    for (int i = 0; i < 34250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34251(int& val) {
    func_in_34251(val);
    const int x = 34251;
    std::cout << 34251;
    for (int i = 0; i < 34251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34251(int& val) {
    const int x = 34251;
    std::cout << 34251;
    for (int i = 0; i < 34251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34252(int& val) {
    func_in_34252(val);
    const int x = 34252;
    std::cout << 34252;
    for (int i = 0; i < 34252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34252(int& val) {
    const int x = 34252;
    std::cout << 34252;
    for (int i = 0; i < 34252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34253(int& val) {
    func_in_34253(val);
    const int x = 34253;
    std::cout << 34253;
    for (int i = 0; i < 34253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34253(int& val) {
    const int x = 34253;
    std::cout << 34253;
    for (int i = 0; i < 34253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34254(int& val) {
    func_in_34254(val);
    const int x = 34254;
    std::cout << 34254;
    for (int i = 0; i < 34254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34254(int& val) {
    const int x = 34254;
    std::cout << 34254;
    for (int i = 0; i < 34254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34255(int& val) {
    func_in_34255(val);
    const int x = 34255;
    std::cout << 34255;
    for (int i = 0; i < 34255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34255(int& val) {
    const int x = 34255;
    std::cout << 34255;
    for (int i = 0; i < 34255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34256(int& val) {
    func_in_34256(val);
    const int x = 34256;
    std::cout << 34256;
    for (int i = 0; i < 34256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34256(int& val) {
    const int x = 34256;
    std::cout << 34256;
    for (int i = 0; i < 34256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34257(int& val) {
    func_in_34257(val);
    const int x = 34257;
    std::cout << 34257;
    for (int i = 0; i < 34257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34257(int& val) {
    const int x = 34257;
    std::cout << 34257;
    for (int i = 0; i < 34257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34258(int& val) {
    func_in_34258(val);
    const int x = 34258;
    std::cout << 34258;
    for (int i = 0; i < 34258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34258(int& val) {
    const int x = 34258;
    std::cout << 34258;
    for (int i = 0; i < 34258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34259(int& val) {
    func_in_34259(val);
    const int x = 34259;
    std::cout << 34259;
    for (int i = 0; i < 34259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34259(int& val) {
    const int x = 34259;
    std::cout << 34259;
    for (int i = 0; i < 34259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34260(int& val) {
    func_in_34260(val);
    const int x = 34260;
    std::cout << 34260;
    for (int i = 0; i < 34260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34260(int& val) {
    const int x = 34260;
    std::cout << 34260;
    for (int i = 0; i < 34260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34261(int& val) {
    func_in_34261(val);
    const int x = 34261;
    std::cout << 34261;
    for (int i = 0; i < 34261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34261(int& val) {
    const int x = 34261;
    std::cout << 34261;
    for (int i = 0; i < 34261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34262(int& val) {
    func_in_34262(val);
    const int x = 34262;
    std::cout << 34262;
    for (int i = 0; i < 34262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34262(int& val) {
    const int x = 34262;
    std::cout << 34262;
    for (int i = 0; i < 34262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34263(int& val) {
    func_in_34263(val);
    const int x = 34263;
    std::cout << 34263;
    for (int i = 0; i < 34263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34263(int& val) {
    const int x = 34263;
    std::cout << 34263;
    for (int i = 0; i < 34263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34264(int& val) {
    func_in_34264(val);
    const int x = 34264;
    std::cout << 34264;
    for (int i = 0; i < 34264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34264(int& val) {
    const int x = 34264;
    std::cout << 34264;
    for (int i = 0; i < 34264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34265(int& val) {
    func_in_34265(val);
    const int x = 34265;
    std::cout << 34265;
    for (int i = 0; i < 34265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34265(int& val) {
    const int x = 34265;
    std::cout << 34265;
    for (int i = 0; i < 34265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34266(int& val) {
    func_in_34266(val);
    const int x = 34266;
    std::cout << 34266;
    for (int i = 0; i < 34266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34266(int& val) {
    const int x = 34266;
    std::cout << 34266;
    for (int i = 0; i < 34266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34267(int& val) {
    func_in_34267(val);
    const int x = 34267;
    std::cout << 34267;
    for (int i = 0; i < 34267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34267(int& val) {
    const int x = 34267;
    std::cout << 34267;
    for (int i = 0; i < 34267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34268(int& val) {
    func_in_34268(val);
    const int x = 34268;
    std::cout << 34268;
    for (int i = 0; i < 34268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34268(int& val) {
    const int x = 34268;
    std::cout << 34268;
    for (int i = 0; i < 34268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34269(int& val) {
    func_in_34269(val);
    const int x = 34269;
    std::cout << 34269;
    for (int i = 0; i < 34269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34269(int& val) {
    const int x = 34269;
    std::cout << 34269;
    for (int i = 0; i < 34269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34270(int& val) {
    func_in_34270(val);
    const int x = 34270;
    std::cout << 34270;
    for (int i = 0; i < 34270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34270(int& val) {
    const int x = 34270;
    std::cout << 34270;
    for (int i = 0; i < 34270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34271(int& val) {
    func_in_34271(val);
    const int x = 34271;
    std::cout << 34271;
    for (int i = 0; i < 34271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34271(int& val) {
    const int x = 34271;
    std::cout << 34271;
    for (int i = 0; i < 34271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34272(int& val) {
    func_in_34272(val);
    const int x = 34272;
    std::cout << 34272;
    for (int i = 0; i < 34272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34272(int& val) {
    const int x = 34272;
    std::cout << 34272;
    for (int i = 0; i < 34272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34273(int& val) {
    func_in_34273(val);
    const int x = 34273;
    std::cout << 34273;
    for (int i = 0; i < 34273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34273(int& val) {
    const int x = 34273;
    std::cout << 34273;
    for (int i = 0; i < 34273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34274(int& val) {
    func_in_34274(val);
    const int x = 34274;
    std::cout << 34274;
    for (int i = 0; i < 34274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34274(int& val) {
    const int x = 34274;
    std::cout << 34274;
    for (int i = 0; i < 34274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34275(int& val) {
    func_in_34275(val);
    const int x = 34275;
    std::cout << 34275;
    for (int i = 0; i < 34275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34275(int& val) {
    const int x = 34275;
    std::cout << 34275;
    for (int i = 0; i < 34275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34276(int& val) {
    func_in_34276(val);
    const int x = 34276;
    std::cout << 34276;
    for (int i = 0; i < 34276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34276(int& val) {
    const int x = 34276;
    std::cout << 34276;
    for (int i = 0; i < 34276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34277(int& val) {
    func_in_34277(val);
    const int x = 34277;
    std::cout << 34277;
    for (int i = 0; i < 34277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34277(int& val) {
    const int x = 34277;
    std::cout << 34277;
    for (int i = 0; i < 34277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34278(int& val) {
    func_in_34278(val);
    const int x = 34278;
    std::cout << 34278;
    for (int i = 0; i < 34278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34278(int& val) {
    const int x = 34278;
    std::cout << 34278;
    for (int i = 0; i < 34278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34279(int& val) {
    func_in_34279(val);
    const int x = 34279;
    std::cout << 34279;
    for (int i = 0; i < 34279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34279(int& val) {
    const int x = 34279;
    std::cout << 34279;
    for (int i = 0; i < 34279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34280(int& val) {
    func_in_34280(val);
    const int x = 34280;
    std::cout << 34280;
    for (int i = 0; i < 34280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34280(int& val) {
    const int x = 34280;
    std::cout << 34280;
    for (int i = 0; i < 34280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34281(int& val) {
    func_in_34281(val);
    const int x = 34281;
    std::cout << 34281;
    for (int i = 0; i < 34281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34281(int& val) {
    const int x = 34281;
    std::cout << 34281;
    for (int i = 0; i < 34281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34282(int& val) {
    func_in_34282(val);
    const int x = 34282;
    std::cout << 34282;
    for (int i = 0; i < 34282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34282(int& val) {
    const int x = 34282;
    std::cout << 34282;
    for (int i = 0; i < 34282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34283(int& val) {
    func_in_34283(val);
    const int x = 34283;
    std::cout << 34283;
    for (int i = 0; i < 34283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34283(int& val) {
    const int x = 34283;
    std::cout << 34283;
    for (int i = 0; i < 34283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34284(int& val) {
    func_in_34284(val);
    const int x = 34284;
    std::cout << 34284;
    for (int i = 0; i < 34284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34284(int& val) {
    const int x = 34284;
    std::cout << 34284;
    for (int i = 0; i < 34284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34285(int& val) {
    func_in_34285(val);
    const int x = 34285;
    std::cout << 34285;
    for (int i = 0; i < 34285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34285(int& val) {
    const int x = 34285;
    std::cout << 34285;
    for (int i = 0; i < 34285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34286(int& val) {
    func_in_34286(val);
    const int x = 34286;
    std::cout << 34286;
    for (int i = 0; i < 34286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34286(int& val) {
    const int x = 34286;
    std::cout << 34286;
    for (int i = 0; i < 34286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34287(int& val) {
    func_in_34287(val);
    const int x = 34287;
    std::cout << 34287;
    for (int i = 0; i < 34287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34287(int& val) {
    const int x = 34287;
    std::cout << 34287;
    for (int i = 0; i < 34287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34288(int& val) {
    func_in_34288(val);
    const int x = 34288;
    std::cout << 34288;
    for (int i = 0; i < 34288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34288(int& val) {
    const int x = 34288;
    std::cout << 34288;
    for (int i = 0; i < 34288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34289(int& val) {
    func_in_34289(val);
    const int x = 34289;
    std::cout << 34289;
    for (int i = 0; i < 34289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34289(int& val) {
    const int x = 34289;
    std::cout << 34289;
    for (int i = 0; i < 34289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34290(int& val) {
    func_in_34290(val);
    const int x = 34290;
    std::cout << 34290;
    for (int i = 0; i < 34290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34290(int& val) {
    const int x = 34290;
    std::cout << 34290;
    for (int i = 0; i < 34290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34291(int& val) {
    func_in_34291(val);
    const int x = 34291;
    std::cout << 34291;
    for (int i = 0; i < 34291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34291(int& val) {
    const int x = 34291;
    std::cout << 34291;
    for (int i = 0; i < 34291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34292(int& val) {
    func_in_34292(val);
    const int x = 34292;
    std::cout << 34292;
    for (int i = 0; i < 34292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34292(int& val) {
    const int x = 34292;
    std::cout << 34292;
    for (int i = 0; i < 34292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34293(int& val) {
    func_in_34293(val);
    const int x = 34293;
    std::cout << 34293;
    for (int i = 0; i < 34293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34293(int& val) {
    const int x = 34293;
    std::cout << 34293;
    for (int i = 0; i < 34293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34294(int& val) {
    func_in_34294(val);
    const int x = 34294;
    std::cout << 34294;
    for (int i = 0; i < 34294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34294(int& val) {
    const int x = 34294;
    std::cout << 34294;
    for (int i = 0; i < 34294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34295(int& val) {
    func_in_34295(val);
    const int x = 34295;
    std::cout << 34295;
    for (int i = 0; i < 34295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34295(int& val) {
    const int x = 34295;
    std::cout << 34295;
    for (int i = 0; i < 34295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34296(int& val) {
    func_in_34296(val);
    const int x = 34296;
    std::cout << 34296;
    for (int i = 0; i < 34296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34296(int& val) {
    const int x = 34296;
    std::cout << 34296;
    for (int i = 0; i < 34296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34297(int& val) {
    func_in_34297(val);
    const int x = 34297;
    std::cout << 34297;
    for (int i = 0; i < 34297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34297(int& val) {
    const int x = 34297;
    std::cout << 34297;
    for (int i = 0; i < 34297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34298(int& val) {
    func_in_34298(val);
    const int x = 34298;
    std::cout << 34298;
    for (int i = 0; i < 34298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34298(int& val) {
    const int x = 34298;
    std::cout << 34298;
    for (int i = 0; i < 34298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34299(int& val) {
    func_in_34299(val);
    const int x = 34299;
    std::cout << 34299;
    for (int i = 0; i < 34299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34299(int& val) {
    const int x = 34299;
    std::cout << 34299;
    for (int i = 0; i < 34299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34300(int& val) {
    func_in_34300(val);
    const int x = 34300;
    std::cout << 34300;
    for (int i = 0; i < 34300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34300(int& val) {
    const int x = 34300;
    std::cout << 34300;
    for (int i = 0; i < 34300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34301(int& val) {
    func_in_34301(val);
    const int x = 34301;
    std::cout << 34301;
    for (int i = 0; i < 34301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34301(int& val) {
    const int x = 34301;
    std::cout << 34301;
    for (int i = 0; i < 34301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34302(int& val) {
    func_in_34302(val);
    const int x = 34302;
    std::cout << 34302;
    for (int i = 0; i < 34302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34302(int& val) {
    const int x = 34302;
    std::cout << 34302;
    for (int i = 0; i < 34302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34303(int& val) {
    func_in_34303(val);
    const int x = 34303;
    std::cout << 34303;
    for (int i = 0; i < 34303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34303(int& val) {
    const int x = 34303;
    std::cout << 34303;
    for (int i = 0; i < 34303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34304(int& val) {
    func_in_34304(val);
    const int x = 34304;
    std::cout << 34304;
    for (int i = 0; i < 34304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34304(int& val) {
    const int x = 34304;
    std::cout << 34304;
    for (int i = 0; i < 34304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34305(int& val) {
    func_in_34305(val);
    const int x = 34305;
    std::cout << 34305;
    for (int i = 0; i < 34305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34305(int& val) {
    const int x = 34305;
    std::cout << 34305;
    for (int i = 0; i < 34305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34306(int& val) {
    func_in_34306(val);
    const int x = 34306;
    std::cout << 34306;
    for (int i = 0; i < 34306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34306(int& val) {
    const int x = 34306;
    std::cout << 34306;
    for (int i = 0; i < 34306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34307(int& val) {
    func_in_34307(val);
    const int x = 34307;
    std::cout << 34307;
    for (int i = 0; i < 34307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34307(int& val) {
    const int x = 34307;
    std::cout << 34307;
    for (int i = 0; i < 34307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34308(int& val) {
    func_in_34308(val);
    const int x = 34308;
    std::cout << 34308;
    for (int i = 0; i < 34308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34308(int& val) {
    const int x = 34308;
    std::cout << 34308;
    for (int i = 0; i < 34308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34309(int& val) {
    func_in_34309(val);
    const int x = 34309;
    std::cout << 34309;
    for (int i = 0; i < 34309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34309(int& val) {
    const int x = 34309;
    std::cout << 34309;
    for (int i = 0; i < 34309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34310(int& val) {
    func_in_34310(val);
    const int x = 34310;
    std::cout << 34310;
    for (int i = 0; i < 34310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34310(int& val) {
    const int x = 34310;
    std::cout << 34310;
    for (int i = 0; i < 34310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34311(int& val) {
    func_in_34311(val);
    const int x = 34311;
    std::cout << 34311;
    for (int i = 0; i < 34311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34311(int& val) {
    const int x = 34311;
    std::cout << 34311;
    for (int i = 0; i < 34311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34312(int& val) {
    func_in_34312(val);
    const int x = 34312;
    std::cout << 34312;
    for (int i = 0; i < 34312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34312(int& val) {
    const int x = 34312;
    std::cout << 34312;
    for (int i = 0; i < 34312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34313(int& val) {
    func_in_34313(val);
    const int x = 34313;
    std::cout << 34313;
    for (int i = 0; i < 34313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34313(int& val) {
    const int x = 34313;
    std::cout << 34313;
    for (int i = 0; i < 34313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34314(int& val) {
    func_in_34314(val);
    const int x = 34314;
    std::cout << 34314;
    for (int i = 0; i < 34314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34314(int& val) {
    const int x = 34314;
    std::cout << 34314;
    for (int i = 0; i < 34314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34315(int& val) {
    func_in_34315(val);
    const int x = 34315;
    std::cout << 34315;
    for (int i = 0; i < 34315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34315(int& val) {
    const int x = 34315;
    std::cout << 34315;
    for (int i = 0; i < 34315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34316(int& val) {
    func_in_34316(val);
    const int x = 34316;
    std::cout << 34316;
    for (int i = 0; i < 34316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34316(int& val) {
    const int x = 34316;
    std::cout << 34316;
    for (int i = 0; i < 34316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34317(int& val) {
    func_in_34317(val);
    const int x = 34317;
    std::cout << 34317;
    for (int i = 0; i < 34317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34317(int& val) {
    const int x = 34317;
    std::cout << 34317;
    for (int i = 0; i < 34317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34318(int& val) {
    func_in_34318(val);
    const int x = 34318;
    std::cout << 34318;
    for (int i = 0; i < 34318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34318(int& val) {
    const int x = 34318;
    std::cout << 34318;
    for (int i = 0; i < 34318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34319(int& val) {
    func_in_34319(val);
    const int x = 34319;
    std::cout << 34319;
    for (int i = 0; i < 34319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34319(int& val) {
    const int x = 34319;
    std::cout << 34319;
    for (int i = 0; i < 34319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34320(int& val) {
    func_in_34320(val);
    const int x = 34320;
    std::cout << 34320;
    for (int i = 0; i < 34320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34320(int& val) {
    const int x = 34320;
    std::cout << 34320;
    for (int i = 0; i < 34320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34321(int& val) {
    func_in_34321(val);
    const int x = 34321;
    std::cout << 34321;
    for (int i = 0; i < 34321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34321(int& val) {
    const int x = 34321;
    std::cout << 34321;
    for (int i = 0; i < 34321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34322(int& val) {
    func_in_34322(val);
    const int x = 34322;
    std::cout << 34322;
    for (int i = 0; i < 34322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34322(int& val) {
    const int x = 34322;
    std::cout << 34322;
    for (int i = 0; i < 34322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34323(int& val) {
    func_in_34323(val);
    const int x = 34323;
    std::cout << 34323;
    for (int i = 0; i < 34323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34323(int& val) {
    const int x = 34323;
    std::cout << 34323;
    for (int i = 0; i < 34323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34324(int& val) {
    func_in_34324(val);
    const int x = 34324;
    std::cout << 34324;
    for (int i = 0; i < 34324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34324(int& val) {
    const int x = 34324;
    std::cout << 34324;
    for (int i = 0; i < 34324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34325(int& val) {
    func_in_34325(val);
    const int x = 34325;
    std::cout << 34325;
    for (int i = 0; i < 34325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34325(int& val) {
    const int x = 34325;
    std::cout << 34325;
    for (int i = 0; i < 34325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34326(int& val) {
    func_in_34326(val);
    const int x = 34326;
    std::cout << 34326;
    for (int i = 0; i < 34326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34326(int& val) {
    const int x = 34326;
    std::cout << 34326;
    for (int i = 0; i < 34326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34327(int& val) {
    func_in_34327(val);
    const int x = 34327;
    std::cout << 34327;
    for (int i = 0; i < 34327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34327(int& val) {
    const int x = 34327;
    std::cout << 34327;
    for (int i = 0; i < 34327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34328(int& val) {
    func_in_34328(val);
    const int x = 34328;
    std::cout << 34328;
    for (int i = 0; i < 34328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34328(int& val) {
    const int x = 34328;
    std::cout << 34328;
    for (int i = 0; i < 34328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34329(int& val) {
    func_in_34329(val);
    const int x = 34329;
    std::cout << 34329;
    for (int i = 0; i < 34329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34329(int& val) {
    const int x = 34329;
    std::cout << 34329;
    for (int i = 0; i < 34329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34330(int& val) {
    func_in_34330(val);
    const int x = 34330;
    std::cout << 34330;
    for (int i = 0; i < 34330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34330(int& val) {
    const int x = 34330;
    std::cout << 34330;
    for (int i = 0; i < 34330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34331(int& val) {
    func_in_34331(val);
    const int x = 34331;
    std::cout << 34331;
    for (int i = 0; i < 34331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34331(int& val) {
    const int x = 34331;
    std::cout << 34331;
    for (int i = 0; i < 34331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34332(int& val) {
    func_in_34332(val);
    const int x = 34332;
    std::cout << 34332;
    for (int i = 0; i < 34332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34332(int& val) {
    const int x = 34332;
    std::cout << 34332;
    for (int i = 0; i < 34332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34333(int& val) {
    func_in_34333(val);
    const int x = 34333;
    std::cout << 34333;
    for (int i = 0; i < 34333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34333(int& val) {
    const int x = 34333;
    std::cout << 34333;
    for (int i = 0; i < 34333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34334(int& val) {
    func_in_34334(val);
    const int x = 34334;
    std::cout << 34334;
    for (int i = 0; i < 34334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34334(int& val) {
    const int x = 34334;
    std::cout << 34334;
    for (int i = 0; i < 34334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34335(int& val) {
    func_in_34335(val);
    const int x = 34335;
    std::cout << 34335;
    for (int i = 0; i < 34335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34335(int& val) {
    const int x = 34335;
    std::cout << 34335;
    for (int i = 0; i < 34335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34336(int& val) {
    func_in_34336(val);
    const int x = 34336;
    std::cout << 34336;
    for (int i = 0; i < 34336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34336(int& val) {
    const int x = 34336;
    std::cout << 34336;
    for (int i = 0; i < 34336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34337(int& val) {
    func_in_34337(val);
    const int x = 34337;
    std::cout << 34337;
    for (int i = 0; i < 34337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34337(int& val) {
    const int x = 34337;
    std::cout << 34337;
    for (int i = 0; i < 34337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34338(int& val) {
    func_in_34338(val);
    const int x = 34338;
    std::cout << 34338;
    for (int i = 0; i < 34338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34338(int& val) {
    const int x = 34338;
    std::cout << 34338;
    for (int i = 0; i < 34338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34339(int& val) {
    func_in_34339(val);
    const int x = 34339;
    std::cout << 34339;
    for (int i = 0; i < 34339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34339(int& val) {
    const int x = 34339;
    std::cout << 34339;
    for (int i = 0; i < 34339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34340(int& val) {
    func_in_34340(val);
    const int x = 34340;
    std::cout << 34340;
    for (int i = 0; i < 34340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34340(int& val) {
    const int x = 34340;
    std::cout << 34340;
    for (int i = 0; i < 34340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34341(int& val) {
    func_in_34341(val);
    const int x = 34341;
    std::cout << 34341;
    for (int i = 0; i < 34341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34341(int& val) {
    const int x = 34341;
    std::cout << 34341;
    for (int i = 0; i < 34341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34342(int& val) {
    func_in_34342(val);
    const int x = 34342;
    std::cout << 34342;
    for (int i = 0; i < 34342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34342(int& val) {
    const int x = 34342;
    std::cout << 34342;
    for (int i = 0; i < 34342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34343(int& val) {
    func_in_34343(val);
    const int x = 34343;
    std::cout << 34343;
    for (int i = 0; i < 34343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34343(int& val) {
    const int x = 34343;
    std::cout << 34343;
    for (int i = 0; i < 34343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34344(int& val) {
    func_in_34344(val);
    const int x = 34344;
    std::cout << 34344;
    for (int i = 0; i < 34344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34344(int& val) {
    const int x = 34344;
    std::cout << 34344;
    for (int i = 0; i < 34344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34345(int& val) {
    func_in_34345(val);
    const int x = 34345;
    std::cout << 34345;
    for (int i = 0; i < 34345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34345(int& val) {
    const int x = 34345;
    std::cout << 34345;
    for (int i = 0; i < 34345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34346(int& val) {
    func_in_34346(val);
    const int x = 34346;
    std::cout << 34346;
    for (int i = 0; i < 34346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34346(int& val) {
    const int x = 34346;
    std::cout << 34346;
    for (int i = 0; i < 34346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34347(int& val) {
    func_in_34347(val);
    const int x = 34347;
    std::cout << 34347;
    for (int i = 0; i < 34347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34347(int& val) {
    const int x = 34347;
    std::cout << 34347;
    for (int i = 0; i < 34347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34348(int& val) {
    func_in_34348(val);
    const int x = 34348;
    std::cout << 34348;
    for (int i = 0; i < 34348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34348(int& val) {
    const int x = 34348;
    std::cout << 34348;
    for (int i = 0; i < 34348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34349(int& val) {
    func_in_34349(val);
    const int x = 34349;
    std::cout << 34349;
    for (int i = 0; i < 34349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34349(int& val) {
    const int x = 34349;
    std::cout << 34349;
    for (int i = 0; i < 34349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34350(int& val) {
    func_in_34350(val);
    const int x = 34350;
    std::cout << 34350;
    for (int i = 0; i < 34350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34350(int& val) {
    const int x = 34350;
    std::cout << 34350;
    for (int i = 0; i < 34350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34351(int& val) {
    func_in_34351(val);
    const int x = 34351;
    std::cout << 34351;
    for (int i = 0; i < 34351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34351(int& val) {
    const int x = 34351;
    std::cout << 34351;
    for (int i = 0; i < 34351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34352(int& val) {
    func_in_34352(val);
    const int x = 34352;
    std::cout << 34352;
    for (int i = 0; i < 34352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34352(int& val) {
    const int x = 34352;
    std::cout << 34352;
    for (int i = 0; i < 34352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34353(int& val) {
    func_in_34353(val);
    const int x = 34353;
    std::cout << 34353;
    for (int i = 0; i < 34353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34353(int& val) {
    const int x = 34353;
    std::cout << 34353;
    for (int i = 0; i < 34353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34354(int& val) {
    func_in_34354(val);
    const int x = 34354;
    std::cout << 34354;
    for (int i = 0; i < 34354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34354(int& val) {
    const int x = 34354;
    std::cout << 34354;
    for (int i = 0; i < 34354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34355(int& val) {
    func_in_34355(val);
    const int x = 34355;
    std::cout << 34355;
    for (int i = 0; i < 34355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34355(int& val) {
    const int x = 34355;
    std::cout << 34355;
    for (int i = 0; i < 34355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34356(int& val) {
    func_in_34356(val);
    const int x = 34356;
    std::cout << 34356;
    for (int i = 0; i < 34356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34356(int& val) {
    const int x = 34356;
    std::cout << 34356;
    for (int i = 0; i < 34356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34357(int& val) {
    func_in_34357(val);
    const int x = 34357;
    std::cout << 34357;
    for (int i = 0; i < 34357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34357(int& val) {
    const int x = 34357;
    std::cout << 34357;
    for (int i = 0; i < 34357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34358(int& val) {
    func_in_34358(val);
    const int x = 34358;
    std::cout << 34358;
    for (int i = 0; i < 34358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34358(int& val) {
    const int x = 34358;
    std::cout << 34358;
    for (int i = 0; i < 34358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34359(int& val) {
    func_in_34359(val);
    const int x = 34359;
    std::cout << 34359;
    for (int i = 0; i < 34359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34359(int& val) {
    const int x = 34359;
    std::cout << 34359;
    for (int i = 0; i < 34359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34360(int& val) {
    func_in_34360(val);
    const int x = 34360;
    std::cout << 34360;
    for (int i = 0; i < 34360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34360(int& val) {
    const int x = 34360;
    std::cout << 34360;
    for (int i = 0; i < 34360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34361(int& val) {
    func_in_34361(val);
    const int x = 34361;
    std::cout << 34361;
    for (int i = 0; i < 34361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34361(int& val) {
    const int x = 34361;
    std::cout << 34361;
    for (int i = 0; i < 34361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34362(int& val) {
    func_in_34362(val);
    const int x = 34362;
    std::cout << 34362;
    for (int i = 0; i < 34362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34362(int& val) {
    const int x = 34362;
    std::cout << 34362;
    for (int i = 0; i < 34362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34363(int& val) {
    func_in_34363(val);
    const int x = 34363;
    std::cout << 34363;
    for (int i = 0; i < 34363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34363(int& val) {
    const int x = 34363;
    std::cout << 34363;
    for (int i = 0; i < 34363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34364(int& val) {
    func_in_34364(val);
    const int x = 34364;
    std::cout << 34364;
    for (int i = 0; i < 34364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34364(int& val) {
    const int x = 34364;
    std::cout << 34364;
    for (int i = 0; i < 34364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34365(int& val) {
    func_in_34365(val);
    const int x = 34365;
    std::cout << 34365;
    for (int i = 0; i < 34365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34365(int& val) {
    const int x = 34365;
    std::cout << 34365;
    for (int i = 0; i < 34365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34366(int& val) {
    func_in_34366(val);
    const int x = 34366;
    std::cout << 34366;
    for (int i = 0; i < 34366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34366(int& val) {
    const int x = 34366;
    std::cout << 34366;
    for (int i = 0; i < 34366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34367(int& val) {
    func_in_34367(val);
    const int x = 34367;
    std::cout << 34367;
    for (int i = 0; i < 34367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34367(int& val) {
    const int x = 34367;
    std::cout << 34367;
    for (int i = 0; i < 34367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34368(int& val) {
    func_in_34368(val);
    const int x = 34368;
    std::cout << 34368;
    for (int i = 0; i < 34368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34368(int& val) {
    const int x = 34368;
    std::cout << 34368;
    for (int i = 0; i < 34368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34369(int& val) {
    func_in_34369(val);
    const int x = 34369;
    std::cout << 34369;
    for (int i = 0; i < 34369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34369(int& val) {
    const int x = 34369;
    std::cout << 34369;
    for (int i = 0; i < 34369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34370(int& val) {
    func_in_34370(val);
    const int x = 34370;
    std::cout << 34370;
    for (int i = 0; i < 34370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34370(int& val) {
    const int x = 34370;
    std::cout << 34370;
    for (int i = 0; i < 34370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34371(int& val) {
    func_in_34371(val);
    const int x = 34371;
    std::cout << 34371;
    for (int i = 0; i < 34371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34371(int& val) {
    const int x = 34371;
    std::cout << 34371;
    for (int i = 0; i < 34371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34372(int& val) {
    func_in_34372(val);
    const int x = 34372;
    std::cout << 34372;
    for (int i = 0; i < 34372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34372(int& val) {
    const int x = 34372;
    std::cout << 34372;
    for (int i = 0; i < 34372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34373(int& val) {
    func_in_34373(val);
    const int x = 34373;
    std::cout << 34373;
    for (int i = 0; i < 34373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34373(int& val) {
    const int x = 34373;
    std::cout << 34373;
    for (int i = 0; i < 34373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34374(int& val) {
    func_in_34374(val);
    const int x = 34374;
    std::cout << 34374;
    for (int i = 0; i < 34374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34374(int& val) {
    const int x = 34374;
    std::cout << 34374;
    for (int i = 0; i < 34374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34375(int& val) {
    func_in_34375(val);
    const int x = 34375;
    std::cout << 34375;
    for (int i = 0; i < 34375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34375(int& val) {
    const int x = 34375;
    std::cout << 34375;
    for (int i = 0; i < 34375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34376(int& val) {
    func_in_34376(val);
    const int x = 34376;
    std::cout << 34376;
    for (int i = 0; i < 34376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34376(int& val) {
    const int x = 34376;
    std::cout << 34376;
    for (int i = 0; i < 34376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34377(int& val) {
    func_in_34377(val);
    const int x = 34377;
    std::cout << 34377;
    for (int i = 0; i < 34377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34377(int& val) {
    const int x = 34377;
    std::cout << 34377;
    for (int i = 0; i < 34377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34378(int& val) {
    func_in_34378(val);
    const int x = 34378;
    std::cout << 34378;
    for (int i = 0; i < 34378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34378(int& val) {
    const int x = 34378;
    std::cout << 34378;
    for (int i = 0; i < 34378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34379(int& val) {
    func_in_34379(val);
    const int x = 34379;
    std::cout << 34379;
    for (int i = 0; i < 34379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34379(int& val) {
    const int x = 34379;
    std::cout << 34379;
    for (int i = 0; i < 34379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34380(int& val) {
    func_in_34380(val);
    const int x = 34380;
    std::cout << 34380;
    for (int i = 0; i < 34380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34380(int& val) {
    const int x = 34380;
    std::cout << 34380;
    for (int i = 0; i < 34380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34381(int& val) {
    func_in_34381(val);
    const int x = 34381;
    std::cout << 34381;
    for (int i = 0; i < 34381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34381(int& val) {
    const int x = 34381;
    std::cout << 34381;
    for (int i = 0; i < 34381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34382(int& val) {
    func_in_34382(val);
    const int x = 34382;
    std::cout << 34382;
    for (int i = 0; i < 34382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34382(int& val) {
    const int x = 34382;
    std::cout << 34382;
    for (int i = 0; i < 34382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34383(int& val) {
    func_in_34383(val);
    const int x = 34383;
    std::cout << 34383;
    for (int i = 0; i < 34383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34383(int& val) {
    const int x = 34383;
    std::cout << 34383;
    for (int i = 0; i < 34383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34384(int& val) {
    func_in_34384(val);
    const int x = 34384;
    std::cout << 34384;
    for (int i = 0; i < 34384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34384(int& val) {
    const int x = 34384;
    std::cout << 34384;
    for (int i = 0; i < 34384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34385(int& val) {
    func_in_34385(val);
    const int x = 34385;
    std::cout << 34385;
    for (int i = 0; i < 34385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34385(int& val) {
    const int x = 34385;
    std::cout << 34385;
    for (int i = 0; i < 34385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34386(int& val) {
    func_in_34386(val);
    const int x = 34386;
    std::cout << 34386;
    for (int i = 0; i < 34386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34386(int& val) {
    const int x = 34386;
    std::cout << 34386;
    for (int i = 0; i < 34386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34387(int& val) {
    func_in_34387(val);
    const int x = 34387;
    std::cout << 34387;
    for (int i = 0; i < 34387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34387(int& val) {
    const int x = 34387;
    std::cout << 34387;
    for (int i = 0; i < 34387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34388(int& val) {
    func_in_34388(val);
    const int x = 34388;
    std::cout << 34388;
    for (int i = 0; i < 34388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34388(int& val) {
    const int x = 34388;
    std::cout << 34388;
    for (int i = 0; i < 34388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34389(int& val) {
    func_in_34389(val);
    const int x = 34389;
    std::cout << 34389;
    for (int i = 0; i < 34389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34389(int& val) {
    const int x = 34389;
    std::cout << 34389;
    for (int i = 0; i < 34389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34390(int& val) {
    func_in_34390(val);
    const int x = 34390;
    std::cout << 34390;
    for (int i = 0; i < 34390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34390(int& val) {
    const int x = 34390;
    std::cout << 34390;
    for (int i = 0; i < 34390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34391(int& val) {
    func_in_34391(val);
    const int x = 34391;
    std::cout << 34391;
    for (int i = 0; i < 34391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34391(int& val) {
    const int x = 34391;
    std::cout << 34391;
    for (int i = 0; i < 34391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34392(int& val) {
    func_in_34392(val);
    const int x = 34392;
    std::cout << 34392;
    for (int i = 0; i < 34392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34392(int& val) {
    const int x = 34392;
    std::cout << 34392;
    for (int i = 0; i < 34392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34393(int& val) {
    func_in_34393(val);
    const int x = 34393;
    std::cout << 34393;
    for (int i = 0; i < 34393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34393(int& val) {
    const int x = 34393;
    std::cout << 34393;
    for (int i = 0; i < 34393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34394(int& val) {
    func_in_34394(val);
    const int x = 34394;
    std::cout << 34394;
    for (int i = 0; i < 34394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34394(int& val) {
    const int x = 34394;
    std::cout << 34394;
    for (int i = 0; i < 34394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34395(int& val) {
    func_in_34395(val);
    const int x = 34395;
    std::cout << 34395;
    for (int i = 0; i < 34395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34395(int& val) {
    const int x = 34395;
    std::cout << 34395;
    for (int i = 0; i < 34395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34396(int& val) {
    func_in_34396(val);
    const int x = 34396;
    std::cout << 34396;
    for (int i = 0; i < 34396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34396(int& val) {
    const int x = 34396;
    std::cout << 34396;
    for (int i = 0; i < 34396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34397(int& val) {
    func_in_34397(val);
    const int x = 34397;
    std::cout << 34397;
    for (int i = 0; i < 34397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34397(int& val) {
    const int x = 34397;
    std::cout << 34397;
    for (int i = 0; i < 34397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34398(int& val) {
    func_in_34398(val);
    const int x = 34398;
    std::cout << 34398;
    for (int i = 0; i < 34398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34398(int& val) {
    const int x = 34398;
    std::cout << 34398;
    for (int i = 0; i < 34398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34399(int& val) {
    func_in_34399(val);
    const int x = 34399;
    std::cout << 34399;
    for (int i = 0; i < 34399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34399(int& val) {
    const int x = 34399;
    std::cout << 34399;
    for (int i = 0; i < 34399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34400(int& val) {
    func_in_34400(val);
    const int x = 34400;
    std::cout << 34400;
    for (int i = 0; i < 34400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34400(int& val) {
    const int x = 34400;
    std::cout << 34400;
    for (int i = 0; i < 34400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34401(int& val) {
    func_in_34401(val);
    const int x = 34401;
    std::cout << 34401;
    for (int i = 0; i < 34401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34401(int& val) {
    const int x = 34401;
    std::cout << 34401;
    for (int i = 0; i < 34401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34402(int& val) {
    func_in_34402(val);
    const int x = 34402;
    std::cout << 34402;
    for (int i = 0; i < 34402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34402(int& val) {
    const int x = 34402;
    std::cout << 34402;
    for (int i = 0; i < 34402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34403(int& val) {
    func_in_34403(val);
    const int x = 34403;
    std::cout << 34403;
    for (int i = 0; i < 34403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34403(int& val) {
    const int x = 34403;
    std::cout << 34403;
    for (int i = 0; i < 34403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34404(int& val) {
    func_in_34404(val);
    const int x = 34404;
    std::cout << 34404;
    for (int i = 0; i < 34404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34404(int& val) {
    const int x = 34404;
    std::cout << 34404;
    for (int i = 0; i < 34404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34405(int& val) {
    func_in_34405(val);
    const int x = 34405;
    std::cout << 34405;
    for (int i = 0; i < 34405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34405(int& val) {
    const int x = 34405;
    std::cout << 34405;
    for (int i = 0; i < 34405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34406(int& val) {
    func_in_34406(val);
    const int x = 34406;
    std::cout << 34406;
    for (int i = 0; i < 34406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34406(int& val) {
    const int x = 34406;
    std::cout << 34406;
    for (int i = 0; i < 34406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34407(int& val) {
    func_in_34407(val);
    const int x = 34407;
    std::cout << 34407;
    for (int i = 0; i < 34407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34407(int& val) {
    const int x = 34407;
    std::cout << 34407;
    for (int i = 0; i < 34407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34408(int& val) {
    func_in_34408(val);
    const int x = 34408;
    std::cout << 34408;
    for (int i = 0; i < 34408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34408(int& val) {
    const int x = 34408;
    std::cout << 34408;
    for (int i = 0; i < 34408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34409(int& val) {
    func_in_34409(val);
    const int x = 34409;
    std::cout << 34409;
    for (int i = 0; i < 34409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34409(int& val) {
    const int x = 34409;
    std::cout << 34409;
    for (int i = 0; i < 34409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34410(int& val) {
    func_in_34410(val);
    const int x = 34410;
    std::cout << 34410;
    for (int i = 0; i < 34410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34410(int& val) {
    const int x = 34410;
    std::cout << 34410;
    for (int i = 0; i < 34410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34411(int& val) {
    func_in_34411(val);
    const int x = 34411;
    std::cout << 34411;
    for (int i = 0; i < 34411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34411(int& val) {
    const int x = 34411;
    std::cout << 34411;
    for (int i = 0; i < 34411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34412(int& val) {
    func_in_34412(val);
    const int x = 34412;
    std::cout << 34412;
    for (int i = 0; i < 34412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34412(int& val) {
    const int x = 34412;
    std::cout << 34412;
    for (int i = 0; i < 34412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34413(int& val) {
    func_in_34413(val);
    const int x = 34413;
    std::cout << 34413;
    for (int i = 0; i < 34413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34413(int& val) {
    const int x = 34413;
    std::cout << 34413;
    for (int i = 0; i < 34413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34414(int& val) {
    func_in_34414(val);
    const int x = 34414;
    std::cout << 34414;
    for (int i = 0; i < 34414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34414(int& val) {
    const int x = 34414;
    std::cout << 34414;
    for (int i = 0; i < 34414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34415(int& val) {
    func_in_34415(val);
    const int x = 34415;
    std::cout << 34415;
    for (int i = 0; i < 34415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34415(int& val) {
    const int x = 34415;
    std::cout << 34415;
    for (int i = 0; i < 34415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34416(int& val) {
    func_in_34416(val);
    const int x = 34416;
    std::cout << 34416;
    for (int i = 0; i < 34416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34416(int& val) {
    const int x = 34416;
    std::cout << 34416;
    for (int i = 0; i < 34416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34417(int& val) {
    func_in_34417(val);
    const int x = 34417;
    std::cout << 34417;
    for (int i = 0; i < 34417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34417(int& val) {
    const int x = 34417;
    std::cout << 34417;
    for (int i = 0; i < 34417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34418(int& val) {
    func_in_34418(val);
    const int x = 34418;
    std::cout << 34418;
    for (int i = 0; i < 34418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34418(int& val) {
    const int x = 34418;
    std::cout << 34418;
    for (int i = 0; i < 34418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34419(int& val) {
    func_in_34419(val);
    const int x = 34419;
    std::cout << 34419;
    for (int i = 0; i < 34419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34419(int& val) {
    const int x = 34419;
    std::cout << 34419;
    for (int i = 0; i < 34419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34420(int& val) {
    func_in_34420(val);
    const int x = 34420;
    std::cout << 34420;
    for (int i = 0; i < 34420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34420(int& val) {
    const int x = 34420;
    std::cout << 34420;
    for (int i = 0; i < 34420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34421(int& val) {
    func_in_34421(val);
    const int x = 34421;
    std::cout << 34421;
    for (int i = 0; i < 34421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34421(int& val) {
    const int x = 34421;
    std::cout << 34421;
    for (int i = 0; i < 34421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34422(int& val) {
    func_in_34422(val);
    const int x = 34422;
    std::cout << 34422;
    for (int i = 0; i < 34422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34422(int& val) {
    const int x = 34422;
    std::cout << 34422;
    for (int i = 0; i < 34422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34423(int& val) {
    func_in_34423(val);
    const int x = 34423;
    std::cout << 34423;
    for (int i = 0; i < 34423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34423(int& val) {
    const int x = 34423;
    std::cout << 34423;
    for (int i = 0; i < 34423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34424(int& val) {
    func_in_34424(val);
    const int x = 34424;
    std::cout << 34424;
    for (int i = 0; i < 34424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34424(int& val) {
    const int x = 34424;
    std::cout << 34424;
    for (int i = 0; i < 34424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34425(int& val) {
    func_in_34425(val);
    const int x = 34425;
    std::cout << 34425;
    for (int i = 0; i < 34425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34425(int& val) {
    const int x = 34425;
    std::cout << 34425;
    for (int i = 0; i < 34425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34426(int& val) {
    func_in_34426(val);
    const int x = 34426;
    std::cout << 34426;
    for (int i = 0; i < 34426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34426(int& val) {
    const int x = 34426;
    std::cout << 34426;
    for (int i = 0; i < 34426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34427(int& val) {
    func_in_34427(val);
    const int x = 34427;
    std::cout << 34427;
    for (int i = 0; i < 34427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34427(int& val) {
    const int x = 34427;
    std::cout << 34427;
    for (int i = 0; i < 34427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34428(int& val) {
    func_in_34428(val);
    const int x = 34428;
    std::cout << 34428;
    for (int i = 0; i < 34428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34428(int& val) {
    const int x = 34428;
    std::cout << 34428;
    for (int i = 0; i < 34428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34429(int& val) {
    func_in_34429(val);
    const int x = 34429;
    std::cout << 34429;
    for (int i = 0; i < 34429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34429(int& val) {
    const int x = 34429;
    std::cout << 34429;
    for (int i = 0; i < 34429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34430(int& val) {
    func_in_34430(val);
    const int x = 34430;
    std::cout << 34430;
    for (int i = 0; i < 34430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34430(int& val) {
    const int x = 34430;
    std::cout << 34430;
    for (int i = 0; i < 34430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34431(int& val) {
    func_in_34431(val);
    const int x = 34431;
    std::cout << 34431;
    for (int i = 0; i < 34431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34431(int& val) {
    const int x = 34431;
    std::cout << 34431;
    for (int i = 0; i < 34431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34432(int& val) {
    func_in_34432(val);
    const int x = 34432;
    std::cout << 34432;
    for (int i = 0; i < 34432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34432(int& val) {
    const int x = 34432;
    std::cout << 34432;
    for (int i = 0; i < 34432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34433(int& val) {
    func_in_34433(val);
    const int x = 34433;
    std::cout << 34433;
    for (int i = 0; i < 34433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34433(int& val) {
    const int x = 34433;
    std::cout << 34433;
    for (int i = 0; i < 34433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34434(int& val) {
    func_in_34434(val);
    const int x = 34434;
    std::cout << 34434;
    for (int i = 0; i < 34434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34434(int& val) {
    const int x = 34434;
    std::cout << 34434;
    for (int i = 0; i < 34434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34435(int& val) {
    func_in_34435(val);
    const int x = 34435;
    std::cout << 34435;
    for (int i = 0; i < 34435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34435(int& val) {
    const int x = 34435;
    std::cout << 34435;
    for (int i = 0; i < 34435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34436(int& val) {
    func_in_34436(val);
    const int x = 34436;
    std::cout << 34436;
    for (int i = 0; i < 34436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34436(int& val) {
    const int x = 34436;
    std::cout << 34436;
    for (int i = 0; i < 34436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34437(int& val) {
    func_in_34437(val);
    const int x = 34437;
    std::cout << 34437;
    for (int i = 0; i < 34437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34437(int& val) {
    const int x = 34437;
    std::cout << 34437;
    for (int i = 0; i < 34437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34438(int& val) {
    func_in_34438(val);
    const int x = 34438;
    std::cout << 34438;
    for (int i = 0; i < 34438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34438(int& val) {
    const int x = 34438;
    std::cout << 34438;
    for (int i = 0; i < 34438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34439(int& val) {
    func_in_34439(val);
    const int x = 34439;
    std::cout << 34439;
    for (int i = 0; i < 34439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34439(int& val) {
    const int x = 34439;
    std::cout << 34439;
    for (int i = 0; i < 34439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34440(int& val) {
    func_in_34440(val);
    const int x = 34440;
    std::cout << 34440;
    for (int i = 0; i < 34440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34440(int& val) {
    const int x = 34440;
    std::cout << 34440;
    for (int i = 0; i < 34440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34441(int& val) {
    func_in_34441(val);
    const int x = 34441;
    std::cout << 34441;
    for (int i = 0; i < 34441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34441(int& val) {
    const int x = 34441;
    std::cout << 34441;
    for (int i = 0; i < 34441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34442(int& val) {
    func_in_34442(val);
    const int x = 34442;
    std::cout << 34442;
    for (int i = 0; i < 34442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34442(int& val) {
    const int x = 34442;
    std::cout << 34442;
    for (int i = 0; i < 34442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34443(int& val) {
    func_in_34443(val);
    const int x = 34443;
    std::cout << 34443;
    for (int i = 0; i < 34443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34443(int& val) {
    const int x = 34443;
    std::cout << 34443;
    for (int i = 0; i < 34443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34444(int& val) {
    func_in_34444(val);
    const int x = 34444;
    std::cout << 34444;
    for (int i = 0; i < 34444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34444(int& val) {
    const int x = 34444;
    std::cout << 34444;
    for (int i = 0; i < 34444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34445(int& val) {
    func_in_34445(val);
    const int x = 34445;
    std::cout << 34445;
    for (int i = 0; i < 34445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34445(int& val) {
    const int x = 34445;
    std::cout << 34445;
    for (int i = 0; i < 34445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34446(int& val) {
    func_in_34446(val);
    const int x = 34446;
    std::cout << 34446;
    for (int i = 0; i < 34446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34446(int& val) {
    const int x = 34446;
    std::cout << 34446;
    for (int i = 0; i < 34446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34447(int& val) {
    func_in_34447(val);
    const int x = 34447;
    std::cout << 34447;
    for (int i = 0; i < 34447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34447(int& val) {
    const int x = 34447;
    std::cout << 34447;
    for (int i = 0; i < 34447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34448(int& val) {
    func_in_34448(val);
    const int x = 34448;
    std::cout << 34448;
    for (int i = 0; i < 34448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34448(int& val) {
    const int x = 34448;
    std::cout << 34448;
    for (int i = 0; i < 34448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34449(int& val) {
    func_in_34449(val);
    const int x = 34449;
    std::cout << 34449;
    for (int i = 0; i < 34449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34449(int& val) {
    const int x = 34449;
    std::cout << 34449;
    for (int i = 0; i < 34449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34450(int& val) {
    func_in_34450(val);
    const int x = 34450;
    std::cout << 34450;
    for (int i = 0; i < 34450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34450(int& val) {
    const int x = 34450;
    std::cout << 34450;
    for (int i = 0; i < 34450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34451(int& val) {
    func_in_34451(val);
    const int x = 34451;
    std::cout << 34451;
    for (int i = 0; i < 34451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34451(int& val) {
    const int x = 34451;
    std::cout << 34451;
    for (int i = 0; i < 34451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34452(int& val) {
    func_in_34452(val);
    const int x = 34452;
    std::cout << 34452;
    for (int i = 0; i < 34452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34452(int& val) {
    const int x = 34452;
    std::cout << 34452;
    for (int i = 0; i < 34452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34453(int& val) {
    func_in_34453(val);
    const int x = 34453;
    std::cout << 34453;
    for (int i = 0; i < 34453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34453(int& val) {
    const int x = 34453;
    std::cout << 34453;
    for (int i = 0; i < 34453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34454(int& val) {
    func_in_34454(val);
    const int x = 34454;
    std::cout << 34454;
    for (int i = 0; i < 34454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34454(int& val) {
    const int x = 34454;
    std::cout << 34454;
    for (int i = 0; i < 34454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34455(int& val) {
    func_in_34455(val);
    const int x = 34455;
    std::cout << 34455;
    for (int i = 0; i < 34455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34455(int& val) {
    const int x = 34455;
    std::cout << 34455;
    for (int i = 0; i < 34455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34456(int& val) {
    func_in_34456(val);
    const int x = 34456;
    std::cout << 34456;
    for (int i = 0; i < 34456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34456(int& val) {
    const int x = 34456;
    std::cout << 34456;
    for (int i = 0; i < 34456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34457(int& val) {
    func_in_34457(val);
    const int x = 34457;
    std::cout << 34457;
    for (int i = 0; i < 34457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34457(int& val) {
    const int x = 34457;
    std::cout << 34457;
    for (int i = 0; i < 34457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34458(int& val) {
    func_in_34458(val);
    const int x = 34458;
    std::cout << 34458;
    for (int i = 0; i < 34458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34458(int& val) {
    const int x = 34458;
    std::cout << 34458;
    for (int i = 0; i < 34458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34459(int& val) {
    func_in_34459(val);
    const int x = 34459;
    std::cout << 34459;
    for (int i = 0; i < 34459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34459(int& val) {
    const int x = 34459;
    std::cout << 34459;
    for (int i = 0; i < 34459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34460(int& val) {
    func_in_34460(val);
    const int x = 34460;
    std::cout << 34460;
    for (int i = 0; i < 34460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34460(int& val) {
    const int x = 34460;
    std::cout << 34460;
    for (int i = 0; i < 34460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34461(int& val) {
    func_in_34461(val);
    const int x = 34461;
    std::cout << 34461;
    for (int i = 0; i < 34461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34461(int& val) {
    const int x = 34461;
    std::cout << 34461;
    for (int i = 0; i < 34461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34462(int& val) {
    func_in_34462(val);
    const int x = 34462;
    std::cout << 34462;
    for (int i = 0; i < 34462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34462(int& val) {
    const int x = 34462;
    std::cout << 34462;
    for (int i = 0; i < 34462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34463(int& val) {
    func_in_34463(val);
    const int x = 34463;
    std::cout << 34463;
    for (int i = 0; i < 34463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34463(int& val) {
    const int x = 34463;
    std::cout << 34463;
    for (int i = 0; i < 34463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34464(int& val) {
    func_in_34464(val);
    const int x = 34464;
    std::cout << 34464;
    for (int i = 0; i < 34464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34464(int& val) {
    const int x = 34464;
    std::cout << 34464;
    for (int i = 0; i < 34464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34465(int& val) {
    func_in_34465(val);
    const int x = 34465;
    std::cout << 34465;
    for (int i = 0; i < 34465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34465(int& val) {
    const int x = 34465;
    std::cout << 34465;
    for (int i = 0; i < 34465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34466(int& val) {
    func_in_34466(val);
    const int x = 34466;
    std::cout << 34466;
    for (int i = 0; i < 34466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34466(int& val) {
    const int x = 34466;
    std::cout << 34466;
    for (int i = 0; i < 34466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34467(int& val) {
    func_in_34467(val);
    const int x = 34467;
    std::cout << 34467;
    for (int i = 0; i < 34467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34467(int& val) {
    const int x = 34467;
    std::cout << 34467;
    for (int i = 0; i < 34467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34468(int& val) {
    func_in_34468(val);
    const int x = 34468;
    std::cout << 34468;
    for (int i = 0; i < 34468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34468(int& val) {
    const int x = 34468;
    std::cout << 34468;
    for (int i = 0; i < 34468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34469(int& val) {
    func_in_34469(val);
    const int x = 34469;
    std::cout << 34469;
    for (int i = 0; i < 34469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34469(int& val) {
    const int x = 34469;
    std::cout << 34469;
    for (int i = 0; i < 34469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34470(int& val) {
    func_in_34470(val);
    const int x = 34470;
    std::cout << 34470;
    for (int i = 0; i < 34470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34470(int& val) {
    const int x = 34470;
    std::cout << 34470;
    for (int i = 0; i < 34470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34471(int& val) {
    func_in_34471(val);
    const int x = 34471;
    std::cout << 34471;
    for (int i = 0; i < 34471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34471(int& val) {
    const int x = 34471;
    std::cout << 34471;
    for (int i = 0; i < 34471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34472(int& val) {
    func_in_34472(val);
    const int x = 34472;
    std::cout << 34472;
    for (int i = 0; i < 34472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34472(int& val) {
    const int x = 34472;
    std::cout << 34472;
    for (int i = 0; i < 34472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34473(int& val) {
    func_in_34473(val);
    const int x = 34473;
    std::cout << 34473;
    for (int i = 0; i < 34473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34473(int& val) {
    const int x = 34473;
    std::cout << 34473;
    for (int i = 0; i < 34473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34474(int& val) {
    func_in_34474(val);
    const int x = 34474;
    std::cout << 34474;
    for (int i = 0; i < 34474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34474(int& val) {
    const int x = 34474;
    std::cout << 34474;
    for (int i = 0; i < 34474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34475(int& val) {
    func_in_34475(val);
    const int x = 34475;
    std::cout << 34475;
    for (int i = 0; i < 34475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34475(int& val) {
    const int x = 34475;
    std::cout << 34475;
    for (int i = 0; i < 34475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34476(int& val) {
    func_in_34476(val);
    const int x = 34476;
    std::cout << 34476;
    for (int i = 0; i < 34476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34476(int& val) {
    const int x = 34476;
    std::cout << 34476;
    for (int i = 0; i < 34476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34477(int& val) {
    func_in_34477(val);
    const int x = 34477;
    std::cout << 34477;
    for (int i = 0; i < 34477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34477(int& val) {
    const int x = 34477;
    std::cout << 34477;
    for (int i = 0; i < 34477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34478(int& val) {
    func_in_34478(val);
    const int x = 34478;
    std::cout << 34478;
    for (int i = 0; i < 34478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34478(int& val) {
    const int x = 34478;
    std::cout << 34478;
    for (int i = 0; i < 34478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34479(int& val) {
    func_in_34479(val);
    const int x = 34479;
    std::cout << 34479;
    for (int i = 0; i < 34479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34479(int& val) {
    const int x = 34479;
    std::cout << 34479;
    for (int i = 0; i < 34479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34480(int& val) {
    func_in_34480(val);
    const int x = 34480;
    std::cout << 34480;
    for (int i = 0; i < 34480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34480(int& val) {
    const int x = 34480;
    std::cout << 34480;
    for (int i = 0; i < 34480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34481(int& val) {
    func_in_34481(val);
    const int x = 34481;
    std::cout << 34481;
    for (int i = 0; i < 34481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34481(int& val) {
    const int x = 34481;
    std::cout << 34481;
    for (int i = 0; i < 34481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34482(int& val) {
    func_in_34482(val);
    const int x = 34482;
    std::cout << 34482;
    for (int i = 0; i < 34482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34482(int& val) {
    const int x = 34482;
    std::cout << 34482;
    for (int i = 0; i < 34482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34483(int& val) {
    func_in_34483(val);
    const int x = 34483;
    std::cout << 34483;
    for (int i = 0; i < 34483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34483(int& val) {
    const int x = 34483;
    std::cout << 34483;
    for (int i = 0; i < 34483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34484(int& val) {
    func_in_34484(val);
    const int x = 34484;
    std::cout << 34484;
    for (int i = 0; i < 34484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34484(int& val) {
    const int x = 34484;
    std::cout << 34484;
    for (int i = 0; i < 34484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34485(int& val) {
    func_in_34485(val);
    const int x = 34485;
    std::cout << 34485;
    for (int i = 0; i < 34485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34485(int& val) {
    const int x = 34485;
    std::cout << 34485;
    for (int i = 0; i < 34485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34486(int& val) {
    func_in_34486(val);
    const int x = 34486;
    std::cout << 34486;
    for (int i = 0; i < 34486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34486(int& val) {
    const int x = 34486;
    std::cout << 34486;
    for (int i = 0; i < 34486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34487(int& val) {
    func_in_34487(val);
    const int x = 34487;
    std::cout << 34487;
    for (int i = 0; i < 34487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34487(int& val) {
    const int x = 34487;
    std::cout << 34487;
    for (int i = 0; i < 34487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34488(int& val) {
    func_in_34488(val);
    const int x = 34488;
    std::cout << 34488;
    for (int i = 0; i < 34488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34488(int& val) {
    const int x = 34488;
    std::cout << 34488;
    for (int i = 0; i < 34488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34489(int& val) {
    func_in_34489(val);
    const int x = 34489;
    std::cout << 34489;
    for (int i = 0; i < 34489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34489(int& val) {
    const int x = 34489;
    std::cout << 34489;
    for (int i = 0; i < 34489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34490(int& val) {
    func_in_34490(val);
    const int x = 34490;
    std::cout << 34490;
    for (int i = 0; i < 34490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34490(int& val) {
    const int x = 34490;
    std::cout << 34490;
    for (int i = 0; i < 34490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34491(int& val) {
    func_in_34491(val);
    const int x = 34491;
    std::cout << 34491;
    for (int i = 0; i < 34491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34491(int& val) {
    const int x = 34491;
    std::cout << 34491;
    for (int i = 0; i < 34491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34492(int& val) {
    func_in_34492(val);
    const int x = 34492;
    std::cout << 34492;
    for (int i = 0; i < 34492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34492(int& val) {
    const int x = 34492;
    std::cout << 34492;
    for (int i = 0; i < 34492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34493(int& val) {
    func_in_34493(val);
    const int x = 34493;
    std::cout << 34493;
    for (int i = 0; i < 34493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34493(int& val) {
    const int x = 34493;
    std::cout << 34493;
    for (int i = 0; i < 34493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34494(int& val) {
    func_in_34494(val);
    const int x = 34494;
    std::cout << 34494;
    for (int i = 0; i < 34494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34494(int& val) {
    const int x = 34494;
    std::cout << 34494;
    for (int i = 0; i < 34494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34495(int& val) {
    func_in_34495(val);
    const int x = 34495;
    std::cout << 34495;
    for (int i = 0; i < 34495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34495(int& val) {
    const int x = 34495;
    std::cout << 34495;
    for (int i = 0; i < 34495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34496(int& val) {
    func_in_34496(val);
    const int x = 34496;
    std::cout << 34496;
    for (int i = 0; i < 34496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34496(int& val) {
    const int x = 34496;
    std::cout << 34496;
    for (int i = 0; i < 34496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34497(int& val) {
    func_in_34497(val);
    const int x = 34497;
    std::cout << 34497;
    for (int i = 0; i < 34497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34497(int& val) {
    const int x = 34497;
    std::cout << 34497;
    for (int i = 0; i < 34497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34498(int& val) {
    func_in_34498(val);
    const int x = 34498;
    std::cout << 34498;
    for (int i = 0; i < 34498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34498(int& val) {
    const int x = 34498;
    std::cout << 34498;
    for (int i = 0; i < 34498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34499(int& val) {
    func_in_34499(val);
    const int x = 34499;
    std::cout << 34499;
    for (int i = 0; i < 34499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34499(int& val) {
    const int x = 34499;
    std::cout << 34499;
    for (int i = 0; i < 34499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34500(int& val) {
    func_in_34500(val);
    const int x = 34500;
    std::cout << 34500;
    for (int i = 0; i < 34500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34500(int& val) {
    const int x = 34500;
    std::cout << 34500;
    for (int i = 0; i < 34500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34501(int& val) {
    func_in_34501(val);
    const int x = 34501;
    std::cout << 34501;
    for (int i = 0; i < 34501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34501(int& val) {
    const int x = 34501;
    std::cout << 34501;
    for (int i = 0; i < 34501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34502(int& val) {
    func_in_34502(val);
    const int x = 34502;
    std::cout << 34502;
    for (int i = 0; i < 34502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34502(int& val) {
    const int x = 34502;
    std::cout << 34502;
    for (int i = 0; i < 34502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34503(int& val) {
    func_in_34503(val);
    const int x = 34503;
    std::cout << 34503;
    for (int i = 0; i < 34503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34503(int& val) {
    const int x = 34503;
    std::cout << 34503;
    for (int i = 0; i < 34503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34504(int& val) {
    func_in_34504(val);
    const int x = 34504;
    std::cout << 34504;
    for (int i = 0; i < 34504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34504(int& val) {
    const int x = 34504;
    std::cout << 34504;
    for (int i = 0; i < 34504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34505(int& val) {
    func_in_34505(val);
    const int x = 34505;
    std::cout << 34505;
    for (int i = 0; i < 34505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34505(int& val) {
    const int x = 34505;
    std::cout << 34505;
    for (int i = 0; i < 34505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34506(int& val) {
    func_in_34506(val);
    const int x = 34506;
    std::cout << 34506;
    for (int i = 0; i < 34506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34506(int& val) {
    const int x = 34506;
    std::cout << 34506;
    for (int i = 0; i < 34506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34507(int& val) {
    func_in_34507(val);
    const int x = 34507;
    std::cout << 34507;
    for (int i = 0; i < 34507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34507(int& val) {
    const int x = 34507;
    std::cout << 34507;
    for (int i = 0; i < 34507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34508(int& val) {
    func_in_34508(val);
    const int x = 34508;
    std::cout << 34508;
    for (int i = 0; i < 34508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34508(int& val) {
    const int x = 34508;
    std::cout << 34508;
    for (int i = 0; i < 34508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34509(int& val) {
    func_in_34509(val);
    const int x = 34509;
    std::cout << 34509;
    for (int i = 0; i < 34509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34509(int& val) {
    const int x = 34509;
    std::cout << 34509;
    for (int i = 0; i < 34509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34510(int& val) {
    func_in_34510(val);
    const int x = 34510;
    std::cout << 34510;
    for (int i = 0; i < 34510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34510(int& val) {
    const int x = 34510;
    std::cout << 34510;
    for (int i = 0; i < 34510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34511(int& val) {
    func_in_34511(val);
    const int x = 34511;
    std::cout << 34511;
    for (int i = 0; i < 34511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34511(int& val) {
    const int x = 34511;
    std::cout << 34511;
    for (int i = 0; i < 34511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34512(int& val) {
    func_in_34512(val);
    const int x = 34512;
    std::cout << 34512;
    for (int i = 0; i < 34512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34512(int& val) {
    const int x = 34512;
    std::cout << 34512;
    for (int i = 0; i < 34512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34513(int& val) {
    func_in_34513(val);
    const int x = 34513;
    std::cout << 34513;
    for (int i = 0; i < 34513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34513(int& val) {
    const int x = 34513;
    std::cout << 34513;
    for (int i = 0; i < 34513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34514(int& val) {
    func_in_34514(val);
    const int x = 34514;
    std::cout << 34514;
    for (int i = 0; i < 34514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34514(int& val) {
    const int x = 34514;
    std::cout << 34514;
    for (int i = 0; i < 34514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34515(int& val) {
    func_in_34515(val);
    const int x = 34515;
    std::cout << 34515;
    for (int i = 0; i < 34515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34515(int& val) {
    const int x = 34515;
    std::cout << 34515;
    for (int i = 0; i < 34515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34516(int& val) {
    func_in_34516(val);
    const int x = 34516;
    std::cout << 34516;
    for (int i = 0; i < 34516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34516(int& val) {
    const int x = 34516;
    std::cout << 34516;
    for (int i = 0; i < 34516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34517(int& val) {
    func_in_34517(val);
    const int x = 34517;
    std::cout << 34517;
    for (int i = 0; i < 34517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34517(int& val) {
    const int x = 34517;
    std::cout << 34517;
    for (int i = 0; i < 34517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34518(int& val) {
    func_in_34518(val);
    const int x = 34518;
    std::cout << 34518;
    for (int i = 0; i < 34518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34518(int& val) {
    const int x = 34518;
    std::cout << 34518;
    for (int i = 0; i < 34518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34519(int& val) {
    func_in_34519(val);
    const int x = 34519;
    std::cout << 34519;
    for (int i = 0; i < 34519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34519(int& val) {
    const int x = 34519;
    std::cout << 34519;
    for (int i = 0; i < 34519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34520(int& val) {
    func_in_34520(val);
    const int x = 34520;
    std::cout << 34520;
    for (int i = 0; i < 34520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34520(int& val) {
    const int x = 34520;
    std::cout << 34520;
    for (int i = 0; i < 34520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34521(int& val) {
    func_in_34521(val);
    const int x = 34521;
    std::cout << 34521;
    for (int i = 0; i < 34521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34521(int& val) {
    const int x = 34521;
    std::cout << 34521;
    for (int i = 0; i < 34521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34522(int& val) {
    func_in_34522(val);
    const int x = 34522;
    std::cout << 34522;
    for (int i = 0; i < 34522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34522(int& val) {
    const int x = 34522;
    std::cout << 34522;
    for (int i = 0; i < 34522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34523(int& val) {
    func_in_34523(val);
    const int x = 34523;
    std::cout << 34523;
    for (int i = 0; i < 34523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34523(int& val) {
    const int x = 34523;
    std::cout << 34523;
    for (int i = 0; i < 34523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34524(int& val) {
    func_in_34524(val);
    const int x = 34524;
    std::cout << 34524;
    for (int i = 0; i < 34524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34524(int& val) {
    const int x = 34524;
    std::cout << 34524;
    for (int i = 0; i < 34524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34525(int& val) {
    func_in_34525(val);
    const int x = 34525;
    std::cout << 34525;
    for (int i = 0; i < 34525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34525(int& val) {
    const int x = 34525;
    std::cout << 34525;
    for (int i = 0; i < 34525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34526(int& val) {
    func_in_34526(val);
    const int x = 34526;
    std::cout << 34526;
    for (int i = 0; i < 34526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34526(int& val) {
    const int x = 34526;
    std::cout << 34526;
    for (int i = 0; i < 34526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34527(int& val) {
    func_in_34527(val);
    const int x = 34527;
    std::cout << 34527;
    for (int i = 0; i < 34527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34527(int& val) {
    const int x = 34527;
    std::cout << 34527;
    for (int i = 0; i < 34527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34528(int& val) {
    func_in_34528(val);
    const int x = 34528;
    std::cout << 34528;
    for (int i = 0; i < 34528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34528(int& val) {
    const int x = 34528;
    std::cout << 34528;
    for (int i = 0; i < 34528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34529(int& val) {
    func_in_34529(val);
    const int x = 34529;
    std::cout << 34529;
    for (int i = 0; i < 34529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34529(int& val) {
    const int x = 34529;
    std::cout << 34529;
    for (int i = 0; i < 34529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34530(int& val) {
    func_in_34530(val);
    const int x = 34530;
    std::cout << 34530;
    for (int i = 0; i < 34530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34530(int& val) {
    const int x = 34530;
    std::cout << 34530;
    for (int i = 0; i < 34530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34531(int& val) {
    func_in_34531(val);
    const int x = 34531;
    std::cout << 34531;
    for (int i = 0; i < 34531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34531(int& val) {
    const int x = 34531;
    std::cout << 34531;
    for (int i = 0; i < 34531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34532(int& val) {
    func_in_34532(val);
    const int x = 34532;
    std::cout << 34532;
    for (int i = 0; i < 34532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34532(int& val) {
    const int x = 34532;
    std::cout << 34532;
    for (int i = 0; i < 34532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34533(int& val) {
    func_in_34533(val);
    const int x = 34533;
    std::cout << 34533;
    for (int i = 0; i < 34533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34533(int& val) {
    const int x = 34533;
    std::cout << 34533;
    for (int i = 0; i < 34533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34534(int& val) {
    func_in_34534(val);
    const int x = 34534;
    std::cout << 34534;
    for (int i = 0; i < 34534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34534(int& val) {
    const int x = 34534;
    std::cout << 34534;
    for (int i = 0; i < 34534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34535(int& val) {
    func_in_34535(val);
    const int x = 34535;
    std::cout << 34535;
    for (int i = 0; i < 34535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34535(int& val) {
    const int x = 34535;
    std::cout << 34535;
    for (int i = 0; i < 34535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34536(int& val) {
    func_in_34536(val);
    const int x = 34536;
    std::cout << 34536;
    for (int i = 0; i < 34536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34536(int& val) {
    const int x = 34536;
    std::cout << 34536;
    for (int i = 0; i < 34536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34537(int& val) {
    func_in_34537(val);
    const int x = 34537;
    std::cout << 34537;
    for (int i = 0; i < 34537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34537(int& val) {
    const int x = 34537;
    std::cout << 34537;
    for (int i = 0; i < 34537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34538(int& val) {
    func_in_34538(val);
    const int x = 34538;
    std::cout << 34538;
    for (int i = 0; i < 34538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34538(int& val) {
    const int x = 34538;
    std::cout << 34538;
    for (int i = 0; i < 34538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34539(int& val) {
    func_in_34539(val);
    const int x = 34539;
    std::cout << 34539;
    for (int i = 0; i < 34539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34539(int& val) {
    const int x = 34539;
    std::cout << 34539;
    for (int i = 0; i < 34539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34540(int& val) {
    func_in_34540(val);
    const int x = 34540;
    std::cout << 34540;
    for (int i = 0; i < 34540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34540(int& val) {
    const int x = 34540;
    std::cout << 34540;
    for (int i = 0; i < 34540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34541(int& val) {
    func_in_34541(val);
    const int x = 34541;
    std::cout << 34541;
    for (int i = 0; i < 34541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34541(int& val) {
    const int x = 34541;
    std::cout << 34541;
    for (int i = 0; i < 34541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34542(int& val) {
    func_in_34542(val);
    const int x = 34542;
    std::cout << 34542;
    for (int i = 0; i < 34542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34542(int& val) {
    const int x = 34542;
    std::cout << 34542;
    for (int i = 0; i < 34542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34543(int& val) {
    func_in_34543(val);
    const int x = 34543;
    std::cout << 34543;
    for (int i = 0; i < 34543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34543(int& val) {
    const int x = 34543;
    std::cout << 34543;
    for (int i = 0; i < 34543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34544(int& val) {
    func_in_34544(val);
    const int x = 34544;
    std::cout << 34544;
    for (int i = 0; i < 34544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34544(int& val) {
    const int x = 34544;
    std::cout << 34544;
    for (int i = 0; i < 34544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34545(int& val) {
    func_in_34545(val);
    const int x = 34545;
    std::cout << 34545;
    for (int i = 0; i < 34545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34545(int& val) {
    const int x = 34545;
    std::cout << 34545;
    for (int i = 0; i < 34545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34546(int& val) {
    func_in_34546(val);
    const int x = 34546;
    std::cout << 34546;
    for (int i = 0; i < 34546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34546(int& val) {
    const int x = 34546;
    std::cout << 34546;
    for (int i = 0; i < 34546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34547(int& val) {
    func_in_34547(val);
    const int x = 34547;
    std::cout << 34547;
    for (int i = 0; i < 34547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34547(int& val) {
    const int x = 34547;
    std::cout << 34547;
    for (int i = 0; i < 34547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34548(int& val) {
    func_in_34548(val);
    const int x = 34548;
    std::cout << 34548;
    for (int i = 0; i < 34548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34548(int& val) {
    const int x = 34548;
    std::cout << 34548;
    for (int i = 0; i < 34548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34549(int& val) {
    func_in_34549(val);
    const int x = 34549;
    std::cout << 34549;
    for (int i = 0; i < 34549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34549(int& val) {
    const int x = 34549;
    std::cout << 34549;
    for (int i = 0; i < 34549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34550(int& val) {
    func_in_34550(val);
    const int x = 34550;
    std::cout << 34550;
    for (int i = 0; i < 34550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34550(int& val) {
    const int x = 34550;
    std::cout << 34550;
    for (int i = 0; i < 34550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34551(int& val) {
    func_in_34551(val);
    const int x = 34551;
    std::cout << 34551;
    for (int i = 0; i < 34551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34551(int& val) {
    const int x = 34551;
    std::cout << 34551;
    for (int i = 0; i < 34551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34552(int& val) {
    func_in_34552(val);
    const int x = 34552;
    std::cout << 34552;
    for (int i = 0; i < 34552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34552(int& val) {
    const int x = 34552;
    std::cout << 34552;
    for (int i = 0; i < 34552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34553(int& val) {
    func_in_34553(val);
    const int x = 34553;
    std::cout << 34553;
    for (int i = 0; i < 34553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34553(int& val) {
    const int x = 34553;
    std::cout << 34553;
    for (int i = 0; i < 34553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34554(int& val) {
    func_in_34554(val);
    const int x = 34554;
    std::cout << 34554;
    for (int i = 0; i < 34554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34554(int& val) {
    const int x = 34554;
    std::cout << 34554;
    for (int i = 0; i < 34554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34555(int& val) {
    func_in_34555(val);
    const int x = 34555;
    std::cout << 34555;
    for (int i = 0; i < 34555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34555(int& val) {
    const int x = 34555;
    std::cout << 34555;
    for (int i = 0; i < 34555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34556(int& val) {
    func_in_34556(val);
    const int x = 34556;
    std::cout << 34556;
    for (int i = 0; i < 34556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34556(int& val) {
    const int x = 34556;
    std::cout << 34556;
    for (int i = 0; i < 34556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34557(int& val) {
    func_in_34557(val);
    const int x = 34557;
    std::cout << 34557;
    for (int i = 0; i < 34557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34557(int& val) {
    const int x = 34557;
    std::cout << 34557;
    for (int i = 0; i < 34557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34558(int& val) {
    func_in_34558(val);
    const int x = 34558;
    std::cout << 34558;
    for (int i = 0; i < 34558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34558(int& val) {
    const int x = 34558;
    std::cout << 34558;
    for (int i = 0; i < 34558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34559(int& val) {
    func_in_34559(val);
    const int x = 34559;
    std::cout << 34559;
    for (int i = 0; i < 34559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34559(int& val) {
    const int x = 34559;
    std::cout << 34559;
    for (int i = 0; i < 34559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34560(int& val) {
    func_in_34560(val);
    const int x = 34560;
    std::cout << 34560;
    for (int i = 0; i < 34560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34560(int& val) {
    const int x = 34560;
    std::cout << 34560;
    for (int i = 0; i < 34560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34561(int& val) {
    func_in_34561(val);
    const int x = 34561;
    std::cout << 34561;
    for (int i = 0; i < 34561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34561(int& val) {
    const int x = 34561;
    std::cout << 34561;
    for (int i = 0; i < 34561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34562(int& val) {
    func_in_34562(val);
    const int x = 34562;
    std::cout << 34562;
    for (int i = 0; i < 34562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34562(int& val) {
    const int x = 34562;
    std::cout << 34562;
    for (int i = 0; i < 34562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34563(int& val) {
    func_in_34563(val);
    const int x = 34563;
    std::cout << 34563;
    for (int i = 0; i < 34563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34563(int& val) {
    const int x = 34563;
    std::cout << 34563;
    for (int i = 0; i < 34563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34564(int& val) {
    func_in_34564(val);
    const int x = 34564;
    std::cout << 34564;
    for (int i = 0; i < 34564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34564(int& val) {
    const int x = 34564;
    std::cout << 34564;
    for (int i = 0; i < 34564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34565(int& val) {
    func_in_34565(val);
    const int x = 34565;
    std::cout << 34565;
    for (int i = 0; i < 34565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34565(int& val) {
    const int x = 34565;
    std::cout << 34565;
    for (int i = 0; i < 34565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34566(int& val) {
    func_in_34566(val);
    const int x = 34566;
    std::cout << 34566;
    for (int i = 0; i < 34566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34566(int& val) {
    const int x = 34566;
    std::cout << 34566;
    for (int i = 0; i < 34566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34567(int& val) {
    func_in_34567(val);
    const int x = 34567;
    std::cout << 34567;
    for (int i = 0; i < 34567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34567(int& val) {
    const int x = 34567;
    std::cout << 34567;
    for (int i = 0; i < 34567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34568(int& val) {
    func_in_34568(val);
    const int x = 34568;
    std::cout << 34568;
    for (int i = 0; i < 34568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34568(int& val) {
    const int x = 34568;
    std::cout << 34568;
    for (int i = 0; i < 34568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34569(int& val) {
    func_in_34569(val);
    const int x = 34569;
    std::cout << 34569;
    for (int i = 0; i < 34569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34569(int& val) {
    const int x = 34569;
    std::cout << 34569;
    for (int i = 0; i < 34569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34570(int& val) {
    func_in_34570(val);
    const int x = 34570;
    std::cout << 34570;
    for (int i = 0; i < 34570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34570(int& val) {
    const int x = 34570;
    std::cout << 34570;
    for (int i = 0; i < 34570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34571(int& val) {
    func_in_34571(val);
    const int x = 34571;
    std::cout << 34571;
    for (int i = 0; i < 34571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34571(int& val) {
    const int x = 34571;
    std::cout << 34571;
    for (int i = 0; i < 34571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34572(int& val) {
    func_in_34572(val);
    const int x = 34572;
    std::cout << 34572;
    for (int i = 0; i < 34572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34572(int& val) {
    const int x = 34572;
    std::cout << 34572;
    for (int i = 0; i < 34572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34573(int& val) {
    func_in_34573(val);
    const int x = 34573;
    std::cout << 34573;
    for (int i = 0; i < 34573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34573(int& val) {
    const int x = 34573;
    std::cout << 34573;
    for (int i = 0; i < 34573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34574(int& val) {
    func_in_34574(val);
    const int x = 34574;
    std::cout << 34574;
    for (int i = 0; i < 34574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34574(int& val) {
    const int x = 34574;
    std::cout << 34574;
    for (int i = 0; i < 34574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34575(int& val) {
    func_in_34575(val);
    const int x = 34575;
    std::cout << 34575;
    for (int i = 0; i < 34575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34575(int& val) {
    const int x = 34575;
    std::cout << 34575;
    for (int i = 0; i < 34575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34576(int& val) {
    func_in_34576(val);
    const int x = 34576;
    std::cout << 34576;
    for (int i = 0; i < 34576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34576(int& val) {
    const int x = 34576;
    std::cout << 34576;
    for (int i = 0; i < 34576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34577(int& val) {
    func_in_34577(val);
    const int x = 34577;
    std::cout << 34577;
    for (int i = 0; i < 34577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34577(int& val) {
    const int x = 34577;
    std::cout << 34577;
    for (int i = 0; i < 34577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34578(int& val) {
    func_in_34578(val);
    const int x = 34578;
    std::cout << 34578;
    for (int i = 0; i < 34578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34578(int& val) {
    const int x = 34578;
    std::cout << 34578;
    for (int i = 0; i < 34578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34579(int& val) {
    func_in_34579(val);
    const int x = 34579;
    std::cout << 34579;
    for (int i = 0; i < 34579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34579(int& val) {
    const int x = 34579;
    std::cout << 34579;
    for (int i = 0; i < 34579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34580(int& val) {
    func_in_34580(val);
    const int x = 34580;
    std::cout << 34580;
    for (int i = 0; i < 34580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34580(int& val) {
    const int x = 34580;
    std::cout << 34580;
    for (int i = 0; i < 34580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34581(int& val) {
    func_in_34581(val);
    const int x = 34581;
    std::cout << 34581;
    for (int i = 0; i < 34581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34581(int& val) {
    const int x = 34581;
    std::cout << 34581;
    for (int i = 0; i < 34581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34582(int& val) {
    func_in_34582(val);
    const int x = 34582;
    std::cout << 34582;
    for (int i = 0; i < 34582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34582(int& val) {
    const int x = 34582;
    std::cout << 34582;
    for (int i = 0; i < 34582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34583(int& val) {
    func_in_34583(val);
    const int x = 34583;
    std::cout << 34583;
    for (int i = 0; i < 34583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34583(int& val) {
    const int x = 34583;
    std::cout << 34583;
    for (int i = 0; i < 34583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34584(int& val) {
    func_in_34584(val);
    const int x = 34584;
    std::cout << 34584;
    for (int i = 0; i < 34584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34584(int& val) {
    const int x = 34584;
    std::cout << 34584;
    for (int i = 0; i < 34584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34585(int& val) {
    func_in_34585(val);
    const int x = 34585;
    std::cout << 34585;
    for (int i = 0; i < 34585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34585(int& val) {
    const int x = 34585;
    std::cout << 34585;
    for (int i = 0; i < 34585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34586(int& val) {
    func_in_34586(val);
    const int x = 34586;
    std::cout << 34586;
    for (int i = 0; i < 34586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34586(int& val) {
    const int x = 34586;
    std::cout << 34586;
    for (int i = 0; i < 34586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34587(int& val) {
    func_in_34587(val);
    const int x = 34587;
    std::cout << 34587;
    for (int i = 0; i < 34587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34587(int& val) {
    const int x = 34587;
    std::cout << 34587;
    for (int i = 0; i < 34587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34588(int& val) {
    func_in_34588(val);
    const int x = 34588;
    std::cout << 34588;
    for (int i = 0; i < 34588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34588(int& val) {
    const int x = 34588;
    std::cout << 34588;
    for (int i = 0; i < 34588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34589(int& val) {
    func_in_34589(val);
    const int x = 34589;
    std::cout << 34589;
    for (int i = 0; i < 34589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34589(int& val) {
    const int x = 34589;
    std::cout << 34589;
    for (int i = 0; i < 34589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34590(int& val) {
    func_in_34590(val);
    const int x = 34590;
    std::cout << 34590;
    for (int i = 0; i < 34590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34590(int& val) {
    const int x = 34590;
    std::cout << 34590;
    for (int i = 0; i < 34590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34591(int& val) {
    func_in_34591(val);
    const int x = 34591;
    std::cout << 34591;
    for (int i = 0; i < 34591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34591(int& val) {
    const int x = 34591;
    std::cout << 34591;
    for (int i = 0; i < 34591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34592(int& val) {
    func_in_34592(val);
    const int x = 34592;
    std::cout << 34592;
    for (int i = 0; i < 34592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34592(int& val) {
    const int x = 34592;
    std::cout << 34592;
    for (int i = 0; i < 34592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34593(int& val) {
    func_in_34593(val);
    const int x = 34593;
    std::cout << 34593;
    for (int i = 0; i < 34593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34593(int& val) {
    const int x = 34593;
    std::cout << 34593;
    for (int i = 0; i < 34593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34594(int& val) {
    func_in_34594(val);
    const int x = 34594;
    std::cout << 34594;
    for (int i = 0; i < 34594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34594(int& val) {
    const int x = 34594;
    std::cout << 34594;
    for (int i = 0; i < 34594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34595(int& val) {
    func_in_34595(val);
    const int x = 34595;
    std::cout << 34595;
    for (int i = 0; i < 34595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34595(int& val) {
    const int x = 34595;
    std::cout << 34595;
    for (int i = 0; i < 34595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34596(int& val) {
    func_in_34596(val);
    const int x = 34596;
    std::cout << 34596;
    for (int i = 0; i < 34596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34596(int& val) {
    const int x = 34596;
    std::cout << 34596;
    for (int i = 0; i < 34596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34597(int& val) {
    func_in_34597(val);
    const int x = 34597;
    std::cout << 34597;
    for (int i = 0; i < 34597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34597(int& val) {
    const int x = 34597;
    std::cout << 34597;
    for (int i = 0; i < 34597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34598(int& val) {
    func_in_34598(val);
    const int x = 34598;
    std::cout << 34598;
    for (int i = 0; i < 34598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34598(int& val) {
    const int x = 34598;
    std::cout << 34598;
    for (int i = 0; i < 34598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34599(int& val) {
    func_in_34599(val);
    const int x = 34599;
    std::cout << 34599;
    for (int i = 0; i < 34599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34599(int& val) {
    const int x = 34599;
    std::cout << 34599;
    for (int i = 0; i < 34599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34600(int& val) {
    func_in_34600(val);
    const int x = 34600;
    std::cout << 34600;
    for (int i = 0; i < 34600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34600(int& val) {
    const int x = 34600;
    std::cout << 34600;
    for (int i = 0; i < 34600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34601(int& val) {
    func_in_34601(val);
    const int x = 34601;
    std::cout << 34601;
    for (int i = 0; i < 34601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34601(int& val) {
    const int x = 34601;
    std::cout << 34601;
    for (int i = 0; i < 34601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34602(int& val) {
    func_in_34602(val);
    const int x = 34602;
    std::cout << 34602;
    for (int i = 0; i < 34602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34602(int& val) {
    const int x = 34602;
    std::cout << 34602;
    for (int i = 0; i < 34602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34603(int& val) {
    func_in_34603(val);
    const int x = 34603;
    std::cout << 34603;
    for (int i = 0; i < 34603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34603(int& val) {
    const int x = 34603;
    std::cout << 34603;
    for (int i = 0; i < 34603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34604(int& val) {
    func_in_34604(val);
    const int x = 34604;
    std::cout << 34604;
    for (int i = 0; i < 34604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34604(int& val) {
    const int x = 34604;
    std::cout << 34604;
    for (int i = 0; i < 34604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34605(int& val) {
    func_in_34605(val);
    const int x = 34605;
    std::cout << 34605;
    for (int i = 0; i < 34605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34605(int& val) {
    const int x = 34605;
    std::cout << 34605;
    for (int i = 0; i < 34605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34606(int& val) {
    func_in_34606(val);
    const int x = 34606;
    std::cout << 34606;
    for (int i = 0; i < 34606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34606(int& val) {
    const int x = 34606;
    std::cout << 34606;
    for (int i = 0; i < 34606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34607(int& val) {
    func_in_34607(val);
    const int x = 34607;
    std::cout << 34607;
    for (int i = 0; i < 34607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34607(int& val) {
    const int x = 34607;
    std::cout << 34607;
    for (int i = 0; i < 34607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34608(int& val) {
    func_in_34608(val);
    const int x = 34608;
    std::cout << 34608;
    for (int i = 0; i < 34608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34608(int& val) {
    const int x = 34608;
    std::cout << 34608;
    for (int i = 0; i < 34608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34609(int& val) {
    func_in_34609(val);
    const int x = 34609;
    std::cout << 34609;
    for (int i = 0; i < 34609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34609(int& val) {
    const int x = 34609;
    std::cout << 34609;
    for (int i = 0; i < 34609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34610(int& val) {
    func_in_34610(val);
    const int x = 34610;
    std::cout << 34610;
    for (int i = 0; i < 34610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34610(int& val) {
    const int x = 34610;
    std::cout << 34610;
    for (int i = 0; i < 34610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34611(int& val) {
    func_in_34611(val);
    const int x = 34611;
    std::cout << 34611;
    for (int i = 0; i < 34611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34611(int& val) {
    const int x = 34611;
    std::cout << 34611;
    for (int i = 0; i < 34611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34612(int& val) {
    func_in_34612(val);
    const int x = 34612;
    std::cout << 34612;
    for (int i = 0; i < 34612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34612(int& val) {
    const int x = 34612;
    std::cout << 34612;
    for (int i = 0; i < 34612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34613(int& val) {
    func_in_34613(val);
    const int x = 34613;
    std::cout << 34613;
    for (int i = 0; i < 34613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34613(int& val) {
    const int x = 34613;
    std::cout << 34613;
    for (int i = 0; i < 34613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34614(int& val) {
    func_in_34614(val);
    const int x = 34614;
    std::cout << 34614;
    for (int i = 0; i < 34614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34614(int& val) {
    const int x = 34614;
    std::cout << 34614;
    for (int i = 0; i < 34614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34615(int& val) {
    func_in_34615(val);
    const int x = 34615;
    std::cout << 34615;
    for (int i = 0; i < 34615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34615(int& val) {
    const int x = 34615;
    std::cout << 34615;
    for (int i = 0; i < 34615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34616(int& val) {
    func_in_34616(val);
    const int x = 34616;
    std::cout << 34616;
    for (int i = 0; i < 34616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34616(int& val) {
    const int x = 34616;
    std::cout << 34616;
    for (int i = 0; i < 34616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34617(int& val) {
    func_in_34617(val);
    const int x = 34617;
    std::cout << 34617;
    for (int i = 0; i < 34617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34617(int& val) {
    const int x = 34617;
    std::cout << 34617;
    for (int i = 0; i < 34617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34618(int& val) {
    func_in_34618(val);
    const int x = 34618;
    std::cout << 34618;
    for (int i = 0; i < 34618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34618(int& val) {
    const int x = 34618;
    std::cout << 34618;
    for (int i = 0; i < 34618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34619(int& val) {
    func_in_34619(val);
    const int x = 34619;
    std::cout << 34619;
    for (int i = 0; i < 34619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34619(int& val) {
    const int x = 34619;
    std::cout << 34619;
    for (int i = 0; i < 34619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34620(int& val) {
    func_in_34620(val);
    const int x = 34620;
    std::cout << 34620;
    for (int i = 0; i < 34620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34620(int& val) {
    const int x = 34620;
    std::cout << 34620;
    for (int i = 0; i < 34620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34621(int& val) {
    func_in_34621(val);
    const int x = 34621;
    std::cout << 34621;
    for (int i = 0; i < 34621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34621(int& val) {
    const int x = 34621;
    std::cout << 34621;
    for (int i = 0; i < 34621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34622(int& val) {
    func_in_34622(val);
    const int x = 34622;
    std::cout << 34622;
    for (int i = 0; i < 34622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34622(int& val) {
    const int x = 34622;
    std::cout << 34622;
    for (int i = 0; i < 34622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34623(int& val) {
    func_in_34623(val);
    const int x = 34623;
    std::cout << 34623;
    for (int i = 0; i < 34623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34623(int& val) {
    const int x = 34623;
    std::cout << 34623;
    for (int i = 0; i < 34623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34624(int& val) {
    func_in_34624(val);
    const int x = 34624;
    std::cout << 34624;
    for (int i = 0; i < 34624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34624(int& val) {
    const int x = 34624;
    std::cout << 34624;
    for (int i = 0; i < 34624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34625(int& val) {
    func_in_34625(val);
    const int x = 34625;
    std::cout << 34625;
    for (int i = 0; i < 34625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34625(int& val) {
    const int x = 34625;
    std::cout << 34625;
    for (int i = 0; i < 34625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34626(int& val) {
    func_in_34626(val);
    const int x = 34626;
    std::cout << 34626;
    for (int i = 0; i < 34626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34626(int& val) {
    const int x = 34626;
    std::cout << 34626;
    for (int i = 0; i < 34626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34627(int& val) {
    func_in_34627(val);
    const int x = 34627;
    std::cout << 34627;
    for (int i = 0; i < 34627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34627(int& val) {
    const int x = 34627;
    std::cout << 34627;
    for (int i = 0; i < 34627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34628(int& val) {
    func_in_34628(val);
    const int x = 34628;
    std::cout << 34628;
    for (int i = 0; i < 34628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34628(int& val) {
    const int x = 34628;
    std::cout << 34628;
    for (int i = 0; i < 34628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34629(int& val) {
    func_in_34629(val);
    const int x = 34629;
    std::cout << 34629;
    for (int i = 0; i < 34629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34629(int& val) {
    const int x = 34629;
    std::cout << 34629;
    for (int i = 0; i < 34629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34630(int& val) {
    func_in_34630(val);
    const int x = 34630;
    std::cout << 34630;
    for (int i = 0; i < 34630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34630(int& val) {
    const int x = 34630;
    std::cout << 34630;
    for (int i = 0; i < 34630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34631(int& val) {
    func_in_34631(val);
    const int x = 34631;
    std::cout << 34631;
    for (int i = 0; i < 34631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34631(int& val) {
    const int x = 34631;
    std::cout << 34631;
    for (int i = 0; i < 34631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34632(int& val) {
    func_in_34632(val);
    const int x = 34632;
    std::cout << 34632;
    for (int i = 0; i < 34632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34632(int& val) {
    const int x = 34632;
    std::cout << 34632;
    for (int i = 0; i < 34632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34633(int& val) {
    func_in_34633(val);
    const int x = 34633;
    std::cout << 34633;
    for (int i = 0; i < 34633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34633(int& val) {
    const int x = 34633;
    std::cout << 34633;
    for (int i = 0; i < 34633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34634(int& val) {
    func_in_34634(val);
    const int x = 34634;
    std::cout << 34634;
    for (int i = 0; i < 34634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34634(int& val) {
    const int x = 34634;
    std::cout << 34634;
    for (int i = 0; i < 34634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34635(int& val) {
    func_in_34635(val);
    const int x = 34635;
    std::cout << 34635;
    for (int i = 0; i < 34635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34635(int& val) {
    const int x = 34635;
    std::cout << 34635;
    for (int i = 0; i < 34635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34636(int& val) {
    func_in_34636(val);
    const int x = 34636;
    std::cout << 34636;
    for (int i = 0; i < 34636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34636(int& val) {
    const int x = 34636;
    std::cout << 34636;
    for (int i = 0; i < 34636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34637(int& val) {
    func_in_34637(val);
    const int x = 34637;
    std::cout << 34637;
    for (int i = 0; i < 34637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34637(int& val) {
    const int x = 34637;
    std::cout << 34637;
    for (int i = 0; i < 34637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34638(int& val) {
    func_in_34638(val);
    const int x = 34638;
    std::cout << 34638;
    for (int i = 0; i < 34638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34638(int& val) {
    const int x = 34638;
    std::cout << 34638;
    for (int i = 0; i < 34638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34639(int& val) {
    func_in_34639(val);
    const int x = 34639;
    std::cout << 34639;
    for (int i = 0; i < 34639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34639(int& val) {
    const int x = 34639;
    std::cout << 34639;
    for (int i = 0; i < 34639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34640(int& val) {
    func_in_34640(val);
    const int x = 34640;
    std::cout << 34640;
    for (int i = 0; i < 34640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34640(int& val) {
    const int x = 34640;
    std::cout << 34640;
    for (int i = 0; i < 34640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34641(int& val) {
    func_in_34641(val);
    const int x = 34641;
    std::cout << 34641;
    for (int i = 0; i < 34641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34641(int& val) {
    const int x = 34641;
    std::cout << 34641;
    for (int i = 0; i < 34641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34642(int& val) {
    func_in_34642(val);
    const int x = 34642;
    std::cout << 34642;
    for (int i = 0; i < 34642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34642(int& val) {
    const int x = 34642;
    std::cout << 34642;
    for (int i = 0; i < 34642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34643(int& val) {
    func_in_34643(val);
    const int x = 34643;
    std::cout << 34643;
    for (int i = 0; i < 34643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34643(int& val) {
    const int x = 34643;
    std::cout << 34643;
    for (int i = 0; i < 34643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34644(int& val) {
    func_in_34644(val);
    const int x = 34644;
    std::cout << 34644;
    for (int i = 0; i < 34644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34644(int& val) {
    const int x = 34644;
    std::cout << 34644;
    for (int i = 0; i < 34644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34645(int& val) {
    func_in_34645(val);
    const int x = 34645;
    std::cout << 34645;
    for (int i = 0; i < 34645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34645(int& val) {
    const int x = 34645;
    std::cout << 34645;
    for (int i = 0; i < 34645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34646(int& val) {
    func_in_34646(val);
    const int x = 34646;
    std::cout << 34646;
    for (int i = 0; i < 34646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34646(int& val) {
    const int x = 34646;
    std::cout << 34646;
    for (int i = 0; i < 34646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34647(int& val) {
    func_in_34647(val);
    const int x = 34647;
    std::cout << 34647;
    for (int i = 0; i < 34647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34647(int& val) {
    const int x = 34647;
    std::cout << 34647;
    for (int i = 0; i < 34647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34648(int& val) {
    func_in_34648(val);
    const int x = 34648;
    std::cout << 34648;
    for (int i = 0; i < 34648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34648(int& val) {
    const int x = 34648;
    std::cout << 34648;
    for (int i = 0; i < 34648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34649(int& val) {
    func_in_34649(val);
    const int x = 34649;
    std::cout << 34649;
    for (int i = 0; i < 34649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34649(int& val) {
    const int x = 34649;
    std::cout << 34649;
    for (int i = 0; i < 34649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34650(int& val) {
    func_in_34650(val);
    const int x = 34650;
    std::cout << 34650;
    for (int i = 0; i < 34650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34650(int& val) {
    const int x = 34650;
    std::cout << 34650;
    for (int i = 0; i < 34650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34651(int& val) {
    func_in_34651(val);
    const int x = 34651;
    std::cout << 34651;
    for (int i = 0; i < 34651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34651(int& val) {
    const int x = 34651;
    std::cout << 34651;
    for (int i = 0; i < 34651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34652(int& val) {
    func_in_34652(val);
    const int x = 34652;
    std::cout << 34652;
    for (int i = 0; i < 34652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34652(int& val) {
    const int x = 34652;
    std::cout << 34652;
    for (int i = 0; i < 34652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34653(int& val) {
    func_in_34653(val);
    const int x = 34653;
    std::cout << 34653;
    for (int i = 0; i < 34653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34653(int& val) {
    const int x = 34653;
    std::cout << 34653;
    for (int i = 0; i < 34653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34654(int& val) {
    func_in_34654(val);
    const int x = 34654;
    std::cout << 34654;
    for (int i = 0; i < 34654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34654(int& val) {
    const int x = 34654;
    std::cout << 34654;
    for (int i = 0; i < 34654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34655(int& val) {
    func_in_34655(val);
    const int x = 34655;
    std::cout << 34655;
    for (int i = 0; i < 34655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34655(int& val) {
    const int x = 34655;
    std::cout << 34655;
    for (int i = 0; i < 34655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34656(int& val) {
    func_in_34656(val);
    const int x = 34656;
    std::cout << 34656;
    for (int i = 0; i < 34656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34656(int& val) {
    const int x = 34656;
    std::cout << 34656;
    for (int i = 0; i < 34656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34657(int& val) {
    func_in_34657(val);
    const int x = 34657;
    std::cout << 34657;
    for (int i = 0; i < 34657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34657(int& val) {
    const int x = 34657;
    std::cout << 34657;
    for (int i = 0; i < 34657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34658(int& val) {
    func_in_34658(val);
    const int x = 34658;
    std::cout << 34658;
    for (int i = 0; i < 34658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34658(int& val) {
    const int x = 34658;
    std::cout << 34658;
    for (int i = 0; i < 34658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34659(int& val) {
    func_in_34659(val);
    const int x = 34659;
    std::cout << 34659;
    for (int i = 0; i < 34659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34659(int& val) {
    const int x = 34659;
    std::cout << 34659;
    for (int i = 0; i < 34659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34660(int& val) {
    func_in_34660(val);
    const int x = 34660;
    std::cout << 34660;
    for (int i = 0; i < 34660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34660(int& val) {
    const int x = 34660;
    std::cout << 34660;
    for (int i = 0; i < 34660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34661(int& val) {
    func_in_34661(val);
    const int x = 34661;
    std::cout << 34661;
    for (int i = 0; i < 34661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34661(int& val) {
    const int x = 34661;
    std::cout << 34661;
    for (int i = 0; i < 34661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34662(int& val) {
    func_in_34662(val);
    const int x = 34662;
    std::cout << 34662;
    for (int i = 0; i < 34662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34662(int& val) {
    const int x = 34662;
    std::cout << 34662;
    for (int i = 0; i < 34662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34663(int& val) {
    func_in_34663(val);
    const int x = 34663;
    std::cout << 34663;
    for (int i = 0; i < 34663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34663(int& val) {
    const int x = 34663;
    std::cout << 34663;
    for (int i = 0; i < 34663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34664(int& val) {
    func_in_34664(val);
    const int x = 34664;
    std::cout << 34664;
    for (int i = 0; i < 34664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34664(int& val) {
    const int x = 34664;
    std::cout << 34664;
    for (int i = 0; i < 34664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34665(int& val) {
    func_in_34665(val);
    const int x = 34665;
    std::cout << 34665;
    for (int i = 0; i < 34665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34665(int& val) {
    const int x = 34665;
    std::cout << 34665;
    for (int i = 0; i < 34665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34666(int& val) {
    func_in_34666(val);
    const int x = 34666;
    std::cout << 34666;
    for (int i = 0; i < 34666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34666(int& val) {
    const int x = 34666;
    std::cout << 34666;
    for (int i = 0; i < 34666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34667(int& val) {
    func_in_34667(val);
    const int x = 34667;
    std::cout << 34667;
    for (int i = 0; i < 34667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34667(int& val) {
    const int x = 34667;
    std::cout << 34667;
    for (int i = 0; i < 34667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34668(int& val) {
    func_in_34668(val);
    const int x = 34668;
    std::cout << 34668;
    for (int i = 0; i < 34668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34668(int& val) {
    const int x = 34668;
    std::cout << 34668;
    for (int i = 0; i < 34668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34669(int& val) {
    func_in_34669(val);
    const int x = 34669;
    std::cout << 34669;
    for (int i = 0; i < 34669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34669(int& val) {
    const int x = 34669;
    std::cout << 34669;
    for (int i = 0; i < 34669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34670(int& val) {
    func_in_34670(val);
    const int x = 34670;
    std::cout << 34670;
    for (int i = 0; i < 34670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34670(int& val) {
    const int x = 34670;
    std::cout << 34670;
    for (int i = 0; i < 34670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34671(int& val) {
    func_in_34671(val);
    const int x = 34671;
    std::cout << 34671;
    for (int i = 0; i < 34671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34671(int& val) {
    const int x = 34671;
    std::cout << 34671;
    for (int i = 0; i < 34671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34672(int& val) {
    func_in_34672(val);
    const int x = 34672;
    std::cout << 34672;
    for (int i = 0; i < 34672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34672(int& val) {
    const int x = 34672;
    std::cout << 34672;
    for (int i = 0; i < 34672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34673(int& val) {
    func_in_34673(val);
    const int x = 34673;
    std::cout << 34673;
    for (int i = 0; i < 34673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34673(int& val) {
    const int x = 34673;
    std::cout << 34673;
    for (int i = 0; i < 34673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34674(int& val) {
    func_in_34674(val);
    const int x = 34674;
    std::cout << 34674;
    for (int i = 0; i < 34674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34674(int& val) {
    const int x = 34674;
    std::cout << 34674;
    for (int i = 0; i < 34674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34675(int& val) {
    func_in_34675(val);
    const int x = 34675;
    std::cout << 34675;
    for (int i = 0; i < 34675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34675(int& val) {
    const int x = 34675;
    std::cout << 34675;
    for (int i = 0; i < 34675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34676(int& val) {
    func_in_34676(val);
    const int x = 34676;
    std::cout << 34676;
    for (int i = 0; i < 34676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34676(int& val) {
    const int x = 34676;
    std::cout << 34676;
    for (int i = 0; i < 34676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34677(int& val) {
    func_in_34677(val);
    const int x = 34677;
    std::cout << 34677;
    for (int i = 0; i < 34677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34677(int& val) {
    const int x = 34677;
    std::cout << 34677;
    for (int i = 0; i < 34677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34678(int& val) {
    func_in_34678(val);
    const int x = 34678;
    std::cout << 34678;
    for (int i = 0; i < 34678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34678(int& val) {
    const int x = 34678;
    std::cout << 34678;
    for (int i = 0; i < 34678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34679(int& val) {
    func_in_34679(val);
    const int x = 34679;
    std::cout << 34679;
    for (int i = 0; i < 34679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34679(int& val) {
    const int x = 34679;
    std::cout << 34679;
    for (int i = 0; i < 34679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34680(int& val) {
    func_in_34680(val);
    const int x = 34680;
    std::cout << 34680;
    for (int i = 0; i < 34680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34680(int& val) {
    const int x = 34680;
    std::cout << 34680;
    for (int i = 0; i < 34680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34681(int& val) {
    func_in_34681(val);
    const int x = 34681;
    std::cout << 34681;
    for (int i = 0; i < 34681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34681(int& val) {
    const int x = 34681;
    std::cout << 34681;
    for (int i = 0; i < 34681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34682(int& val) {
    func_in_34682(val);
    const int x = 34682;
    std::cout << 34682;
    for (int i = 0; i < 34682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34682(int& val) {
    const int x = 34682;
    std::cout << 34682;
    for (int i = 0; i < 34682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34683(int& val) {
    func_in_34683(val);
    const int x = 34683;
    std::cout << 34683;
    for (int i = 0; i < 34683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34683(int& val) {
    const int x = 34683;
    std::cout << 34683;
    for (int i = 0; i < 34683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34684(int& val) {
    func_in_34684(val);
    const int x = 34684;
    std::cout << 34684;
    for (int i = 0; i < 34684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34684(int& val) {
    const int x = 34684;
    std::cout << 34684;
    for (int i = 0; i < 34684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34685(int& val) {
    func_in_34685(val);
    const int x = 34685;
    std::cout << 34685;
    for (int i = 0; i < 34685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34685(int& val) {
    const int x = 34685;
    std::cout << 34685;
    for (int i = 0; i < 34685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34686(int& val) {
    func_in_34686(val);
    const int x = 34686;
    std::cout << 34686;
    for (int i = 0; i < 34686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34686(int& val) {
    const int x = 34686;
    std::cout << 34686;
    for (int i = 0; i < 34686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34687(int& val) {
    func_in_34687(val);
    const int x = 34687;
    std::cout << 34687;
    for (int i = 0; i < 34687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34687(int& val) {
    const int x = 34687;
    std::cout << 34687;
    for (int i = 0; i < 34687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34688(int& val) {
    func_in_34688(val);
    const int x = 34688;
    std::cout << 34688;
    for (int i = 0; i < 34688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34688(int& val) {
    const int x = 34688;
    std::cout << 34688;
    for (int i = 0; i < 34688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34689(int& val) {
    func_in_34689(val);
    const int x = 34689;
    std::cout << 34689;
    for (int i = 0; i < 34689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34689(int& val) {
    const int x = 34689;
    std::cout << 34689;
    for (int i = 0; i < 34689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34690(int& val) {
    func_in_34690(val);
    const int x = 34690;
    std::cout << 34690;
    for (int i = 0; i < 34690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34690(int& val) {
    const int x = 34690;
    std::cout << 34690;
    for (int i = 0; i < 34690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34691(int& val) {
    func_in_34691(val);
    const int x = 34691;
    std::cout << 34691;
    for (int i = 0; i < 34691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34691(int& val) {
    const int x = 34691;
    std::cout << 34691;
    for (int i = 0; i < 34691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34692(int& val) {
    func_in_34692(val);
    const int x = 34692;
    std::cout << 34692;
    for (int i = 0; i < 34692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34692(int& val) {
    const int x = 34692;
    std::cout << 34692;
    for (int i = 0; i < 34692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34693(int& val) {
    func_in_34693(val);
    const int x = 34693;
    std::cout << 34693;
    for (int i = 0; i < 34693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34693(int& val) {
    const int x = 34693;
    std::cout << 34693;
    for (int i = 0; i < 34693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34694(int& val) {
    func_in_34694(val);
    const int x = 34694;
    std::cout << 34694;
    for (int i = 0; i < 34694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34694(int& val) {
    const int x = 34694;
    std::cout << 34694;
    for (int i = 0; i < 34694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34695(int& val) {
    func_in_34695(val);
    const int x = 34695;
    std::cout << 34695;
    for (int i = 0; i < 34695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34695(int& val) {
    const int x = 34695;
    std::cout << 34695;
    for (int i = 0; i < 34695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34696(int& val) {
    func_in_34696(val);
    const int x = 34696;
    std::cout << 34696;
    for (int i = 0; i < 34696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34696(int& val) {
    const int x = 34696;
    std::cout << 34696;
    for (int i = 0; i < 34696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34697(int& val) {
    func_in_34697(val);
    const int x = 34697;
    std::cout << 34697;
    for (int i = 0; i < 34697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34697(int& val) {
    const int x = 34697;
    std::cout << 34697;
    for (int i = 0; i < 34697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34698(int& val) {
    func_in_34698(val);
    const int x = 34698;
    std::cout << 34698;
    for (int i = 0; i < 34698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34698(int& val) {
    const int x = 34698;
    std::cout << 34698;
    for (int i = 0; i < 34698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34699(int& val) {
    func_in_34699(val);
    const int x = 34699;
    std::cout << 34699;
    for (int i = 0; i < 34699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34699(int& val) {
    const int x = 34699;
    std::cout << 34699;
    for (int i = 0; i < 34699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34700(int& val) {
    func_in_34700(val);
    const int x = 34700;
    std::cout << 34700;
    for (int i = 0; i < 34700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34700(int& val) {
    const int x = 34700;
    std::cout << 34700;
    for (int i = 0; i < 34700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34701(int& val) {
    func_in_34701(val);
    const int x = 34701;
    std::cout << 34701;
    for (int i = 0; i < 34701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34701(int& val) {
    const int x = 34701;
    std::cout << 34701;
    for (int i = 0; i < 34701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34702(int& val) {
    func_in_34702(val);
    const int x = 34702;
    std::cout << 34702;
    for (int i = 0; i < 34702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34702(int& val) {
    const int x = 34702;
    std::cout << 34702;
    for (int i = 0; i < 34702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34703(int& val) {
    func_in_34703(val);
    const int x = 34703;
    std::cout << 34703;
    for (int i = 0; i < 34703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34703(int& val) {
    const int x = 34703;
    std::cout << 34703;
    for (int i = 0; i < 34703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34704(int& val) {
    func_in_34704(val);
    const int x = 34704;
    std::cout << 34704;
    for (int i = 0; i < 34704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34704(int& val) {
    const int x = 34704;
    std::cout << 34704;
    for (int i = 0; i < 34704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34705(int& val) {
    func_in_34705(val);
    const int x = 34705;
    std::cout << 34705;
    for (int i = 0; i < 34705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34705(int& val) {
    const int x = 34705;
    std::cout << 34705;
    for (int i = 0; i < 34705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34706(int& val) {
    func_in_34706(val);
    const int x = 34706;
    std::cout << 34706;
    for (int i = 0; i < 34706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34706(int& val) {
    const int x = 34706;
    std::cout << 34706;
    for (int i = 0; i < 34706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34707(int& val) {
    func_in_34707(val);
    const int x = 34707;
    std::cout << 34707;
    for (int i = 0; i < 34707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34707(int& val) {
    const int x = 34707;
    std::cout << 34707;
    for (int i = 0; i < 34707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34708(int& val) {
    func_in_34708(val);
    const int x = 34708;
    std::cout << 34708;
    for (int i = 0; i < 34708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34708(int& val) {
    const int x = 34708;
    std::cout << 34708;
    for (int i = 0; i < 34708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34709(int& val) {
    func_in_34709(val);
    const int x = 34709;
    std::cout << 34709;
    for (int i = 0; i < 34709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34709(int& val) {
    const int x = 34709;
    std::cout << 34709;
    for (int i = 0; i < 34709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34710(int& val) {
    func_in_34710(val);
    const int x = 34710;
    std::cout << 34710;
    for (int i = 0; i < 34710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34710(int& val) {
    const int x = 34710;
    std::cout << 34710;
    for (int i = 0; i < 34710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34711(int& val) {
    func_in_34711(val);
    const int x = 34711;
    std::cout << 34711;
    for (int i = 0; i < 34711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34711(int& val) {
    const int x = 34711;
    std::cout << 34711;
    for (int i = 0; i < 34711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34712(int& val) {
    func_in_34712(val);
    const int x = 34712;
    std::cout << 34712;
    for (int i = 0; i < 34712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34712(int& val) {
    const int x = 34712;
    std::cout << 34712;
    for (int i = 0; i < 34712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34713(int& val) {
    func_in_34713(val);
    const int x = 34713;
    std::cout << 34713;
    for (int i = 0; i < 34713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34713(int& val) {
    const int x = 34713;
    std::cout << 34713;
    for (int i = 0; i < 34713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34714(int& val) {
    func_in_34714(val);
    const int x = 34714;
    std::cout << 34714;
    for (int i = 0; i < 34714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34714(int& val) {
    const int x = 34714;
    std::cout << 34714;
    for (int i = 0; i < 34714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34715(int& val) {
    func_in_34715(val);
    const int x = 34715;
    std::cout << 34715;
    for (int i = 0; i < 34715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34715(int& val) {
    const int x = 34715;
    std::cout << 34715;
    for (int i = 0; i < 34715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34716(int& val) {
    func_in_34716(val);
    const int x = 34716;
    std::cout << 34716;
    for (int i = 0; i < 34716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34716(int& val) {
    const int x = 34716;
    std::cout << 34716;
    for (int i = 0; i < 34716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34717(int& val) {
    func_in_34717(val);
    const int x = 34717;
    std::cout << 34717;
    for (int i = 0; i < 34717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34717(int& val) {
    const int x = 34717;
    std::cout << 34717;
    for (int i = 0; i < 34717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34718(int& val) {
    func_in_34718(val);
    const int x = 34718;
    std::cout << 34718;
    for (int i = 0; i < 34718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34718(int& val) {
    const int x = 34718;
    std::cout << 34718;
    for (int i = 0; i < 34718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34719(int& val) {
    func_in_34719(val);
    const int x = 34719;
    std::cout << 34719;
    for (int i = 0; i < 34719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34719(int& val) {
    const int x = 34719;
    std::cout << 34719;
    for (int i = 0; i < 34719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34720(int& val) {
    func_in_34720(val);
    const int x = 34720;
    std::cout << 34720;
    for (int i = 0; i < 34720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34720(int& val) {
    const int x = 34720;
    std::cout << 34720;
    for (int i = 0; i < 34720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34721(int& val) {
    func_in_34721(val);
    const int x = 34721;
    std::cout << 34721;
    for (int i = 0; i < 34721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34721(int& val) {
    const int x = 34721;
    std::cout << 34721;
    for (int i = 0; i < 34721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34722(int& val) {
    func_in_34722(val);
    const int x = 34722;
    std::cout << 34722;
    for (int i = 0; i < 34722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34722(int& val) {
    const int x = 34722;
    std::cout << 34722;
    for (int i = 0; i < 34722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34723(int& val) {
    func_in_34723(val);
    const int x = 34723;
    std::cout << 34723;
    for (int i = 0; i < 34723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34723(int& val) {
    const int x = 34723;
    std::cout << 34723;
    for (int i = 0; i < 34723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34724(int& val) {
    func_in_34724(val);
    const int x = 34724;
    std::cout << 34724;
    for (int i = 0; i < 34724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34724(int& val) {
    const int x = 34724;
    std::cout << 34724;
    for (int i = 0; i < 34724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34725(int& val) {
    func_in_34725(val);
    const int x = 34725;
    std::cout << 34725;
    for (int i = 0; i < 34725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34725(int& val) {
    const int x = 34725;
    std::cout << 34725;
    for (int i = 0; i < 34725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34726(int& val) {
    func_in_34726(val);
    const int x = 34726;
    std::cout << 34726;
    for (int i = 0; i < 34726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34726(int& val) {
    const int x = 34726;
    std::cout << 34726;
    for (int i = 0; i < 34726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34727(int& val) {
    func_in_34727(val);
    const int x = 34727;
    std::cout << 34727;
    for (int i = 0; i < 34727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34727(int& val) {
    const int x = 34727;
    std::cout << 34727;
    for (int i = 0; i < 34727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34728(int& val) {
    func_in_34728(val);
    const int x = 34728;
    std::cout << 34728;
    for (int i = 0; i < 34728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34728(int& val) {
    const int x = 34728;
    std::cout << 34728;
    for (int i = 0; i < 34728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34729(int& val) {
    func_in_34729(val);
    const int x = 34729;
    std::cout << 34729;
    for (int i = 0; i < 34729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34729(int& val) {
    const int x = 34729;
    std::cout << 34729;
    for (int i = 0; i < 34729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34730(int& val) {
    func_in_34730(val);
    const int x = 34730;
    std::cout << 34730;
    for (int i = 0; i < 34730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34730(int& val) {
    const int x = 34730;
    std::cout << 34730;
    for (int i = 0; i < 34730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34731(int& val) {
    func_in_34731(val);
    const int x = 34731;
    std::cout << 34731;
    for (int i = 0; i < 34731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34731(int& val) {
    const int x = 34731;
    std::cout << 34731;
    for (int i = 0; i < 34731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34732(int& val) {
    func_in_34732(val);
    const int x = 34732;
    std::cout << 34732;
    for (int i = 0; i < 34732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34732(int& val) {
    const int x = 34732;
    std::cout << 34732;
    for (int i = 0; i < 34732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34733(int& val) {
    func_in_34733(val);
    const int x = 34733;
    std::cout << 34733;
    for (int i = 0; i < 34733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34733(int& val) {
    const int x = 34733;
    std::cout << 34733;
    for (int i = 0; i < 34733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34734(int& val) {
    func_in_34734(val);
    const int x = 34734;
    std::cout << 34734;
    for (int i = 0; i < 34734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34734(int& val) {
    const int x = 34734;
    std::cout << 34734;
    for (int i = 0; i < 34734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34735(int& val) {
    func_in_34735(val);
    const int x = 34735;
    std::cout << 34735;
    for (int i = 0; i < 34735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34735(int& val) {
    const int x = 34735;
    std::cout << 34735;
    for (int i = 0; i < 34735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34736(int& val) {
    func_in_34736(val);
    const int x = 34736;
    std::cout << 34736;
    for (int i = 0; i < 34736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34736(int& val) {
    const int x = 34736;
    std::cout << 34736;
    for (int i = 0; i < 34736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34737(int& val) {
    func_in_34737(val);
    const int x = 34737;
    std::cout << 34737;
    for (int i = 0; i < 34737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34737(int& val) {
    const int x = 34737;
    std::cout << 34737;
    for (int i = 0; i < 34737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34738(int& val) {
    func_in_34738(val);
    const int x = 34738;
    std::cout << 34738;
    for (int i = 0; i < 34738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34738(int& val) {
    const int x = 34738;
    std::cout << 34738;
    for (int i = 0; i < 34738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34739(int& val) {
    func_in_34739(val);
    const int x = 34739;
    std::cout << 34739;
    for (int i = 0; i < 34739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34739(int& val) {
    const int x = 34739;
    std::cout << 34739;
    for (int i = 0; i < 34739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34740(int& val) {
    func_in_34740(val);
    const int x = 34740;
    std::cout << 34740;
    for (int i = 0; i < 34740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34740(int& val) {
    const int x = 34740;
    std::cout << 34740;
    for (int i = 0; i < 34740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34741(int& val) {
    func_in_34741(val);
    const int x = 34741;
    std::cout << 34741;
    for (int i = 0; i < 34741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34741(int& val) {
    const int x = 34741;
    std::cout << 34741;
    for (int i = 0; i < 34741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34742(int& val) {
    func_in_34742(val);
    const int x = 34742;
    std::cout << 34742;
    for (int i = 0; i < 34742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34742(int& val) {
    const int x = 34742;
    std::cout << 34742;
    for (int i = 0; i < 34742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34743(int& val) {
    func_in_34743(val);
    const int x = 34743;
    std::cout << 34743;
    for (int i = 0; i < 34743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34743(int& val) {
    const int x = 34743;
    std::cout << 34743;
    for (int i = 0; i < 34743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34744(int& val) {
    func_in_34744(val);
    const int x = 34744;
    std::cout << 34744;
    for (int i = 0; i < 34744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34744(int& val) {
    const int x = 34744;
    std::cout << 34744;
    for (int i = 0; i < 34744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34745(int& val) {
    func_in_34745(val);
    const int x = 34745;
    std::cout << 34745;
    for (int i = 0; i < 34745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34745(int& val) {
    const int x = 34745;
    std::cout << 34745;
    for (int i = 0; i < 34745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34746(int& val) {
    func_in_34746(val);
    const int x = 34746;
    std::cout << 34746;
    for (int i = 0; i < 34746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34746(int& val) {
    const int x = 34746;
    std::cout << 34746;
    for (int i = 0; i < 34746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34747(int& val) {
    func_in_34747(val);
    const int x = 34747;
    std::cout << 34747;
    for (int i = 0; i < 34747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34747(int& val) {
    const int x = 34747;
    std::cout << 34747;
    for (int i = 0; i < 34747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34748(int& val) {
    func_in_34748(val);
    const int x = 34748;
    std::cout << 34748;
    for (int i = 0; i < 34748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34748(int& val) {
    const int x = 34748;
    std::cout << 34748;
    for (int i = 0; i < 34748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34749(int& val) {
    func_in_34749(val);
    const int x = 34749;
    std::cout << 34749;
    for (int i = 0; i < 34749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34749(int& val) {
    const int x = 34749;
    std::cout << 34749;
    for (int i = 0; i < 34749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34750(int& val) {
    func_in_34750(val);
    const int x = 34750;
    std::cout << 34750;
    for (int i = 0; i < 34750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34750(int& val) {
    const int x = 34750;
    std::cout << 34750;
    for (int i = 0; i < 34750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34751(int& val) {
    func_in_34751(val);
    const int x = 34751;
    std::cout << 34751;
    for (int i = 0; i < 34751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34751(int& val) {
    const int x = 34751;
    std::cout << 34751;
    for (int i = 0; i < 34751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34752(int& val) {
    func_in_34752(val);
    const int x = 34752;
    std::cout << 34752;
    for (int i = 0; i < 34752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34752(int& val) {
    const int x = 34752;
    std::cout << 34752;
    for (int i = 0; i < 34752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34753(int& val) {
    func_in_34753(val);
    const int x = 34753;
    std::cout << 34753;
    for (int i = 0; i < 34753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34753(int& val) {
    const int x = 34753;
    std::cout << 34753;
    for (int i = 0; i < 34753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34754(int& val) {
    func_in_34754(val);
    const int x = 34754;
    std::cout << 34754;
    for (int i = 0; i < 34754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34754(int& val) {
    const int x = 34754;
    std::cout << 34754;
    for (int i = 0; i < 34754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34755(int& val) {
    func_in_34755(val);
    const int x = 34755;
    std::cout << 34755;
    for (int i = 0; i < 34755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34755(int& val) {
    const int x = 34755;
    std::cout << 34755;
    for (int i = 0; i < 34755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34756(int& val) {
    func_in_34756(val);
    const int x = 34756;
    std::cout << 34756;
    for (int i = 0; i < 34756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34756(int& val) {
    const int x = 34756;
    std::cout << 34756;
    for (int i = 0; i < 34756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34757(int& val) {
    func_in_34757(val);
    const int x = 34757;
    std::cout << 34757;
    for (int i = 0; i < 34757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34757(int& val) {
    const int x = 34757;
    std::cout << 34757;
    for (int i = 0; i < 34757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34758(int& val) {
    func_in_34758(val);
    const int x = 34758;
    std::cout << 34758;
    for (int i = 0; i < 34758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34758(int& val) {
    const int x = 34758;
    std::cout << 34758;
    for (int i = 0; i < 34758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34759(int& val) {
    func_in_34759(val);
    const int x = 34759;
    std::cout << 34759;
    for (int i = 0; i < 34759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34759(int& val) {
    const int x = 34759;
    std::cout << 34759;
    for (int i = 0; i < 34759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34760(int& val) {
    func_in_34760(val);
    const int x = 34760;
    std::cout << 34760;
    for (int i = 0; i < 34760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34760(int& val) {
    const int x = 34760;
    std::cout << 34760;
    for (int i = 0; i < 34760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34761(int& val) {
    func_in_34761(val);
    const int x = 34761;
    std::cout << 34761;
    for (int i = 0; i < 34761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34761(int& val) {
    const int x = 34761;
    std::cout << 34761;
    for (int i = 0; i < 34761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34762(int& val) {
    func_in_34762(val);
    const int x = 34762;
    std::cout << 34762;
    for (int i = 0; i < 34762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34762(int& val) {
    const int x = 34762;
    std::cout << 34762;
    for (int i = 0; i < 34762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34763(int& val) {
    func_in_34763(val);
    const int x = 34763;
    std::cout << 34763;
    for (int i = 0; i < 34763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34763(int& val) {
    const int x = 34763;
    std::cout << 34763;
    for (int i = 0; i < 34763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34764(int& val) {
    func_in_34764(val);
    const int x = 34764;
    std::cout << 34764;
    for (int i = 0; i < 34764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34764(int& val) {
    const int x = 34764;
    std::cout << 34764;
    for (int i = 0; i < 34764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34765(int& val) {
    func_in_34765(val);
    const int x = 34765;
    std::cout << 34765;
    for (int i = 0; i < 34765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34765(int& val) {
    const int x = 34765;
    std::cout << 34765;
    for (int i = 0; i < 34765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34766(int& val) {
    func_in_34766(val);
    const int x = 34766;
    std::cout << 34766;
    for (int i = 0; i < 34766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34766(int& val) {
    const int x = 34766;
    std::cout << 34766;
    for (int i = 0; i < 34766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34767(int& val) {
    func_in_34767(val);
    const int x = 34767;
    std::cout << 34767;
    for (int i = 0; i < 34767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34767(int& val) {
    const int x = 34767;
    std::cout << 34767;
    for (int i = 0; i < 34767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34768(int& val) {
    func_in_34768(val);
    const int x = 34768;
    std::cout << 34768;
    for (int i = 0; i < 34768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34768(int& val) {
    const int x = 34768;
    std::cout << 34768;
    for (int i = 0; i < 34768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34769(int& val) {
    func_in_34769(val);
    const int x = 34769;
    std::cout << 34769;
    for (int i = 0; i < 34769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34769(int& val) {
    const int x = 34769;
    std::cout << 34769;
    for (int i = 0; i < 34769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34770(int& val) {
    func_in_34770(val);
    const int x = 34770;
    std::cout << 34770;
    for (int i = 0; i < 34770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34770(int& val) {
    const int x = 34770;
    std::cout << 34770;
    for (int i = 0; i < 34770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34771(int& val) {
    func_in_34771(val);
    const int x = 34771;
    std::cout << 34771;
    for (int i = 0; i < 34771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34771(int& val) {
    const int x = 34771;
    std::cout << 34771;
    for (int i = 0; i < 34771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34772(int& val) {
    func_in_34772(val);
    const int x = 34772;
    std::cout << 34772;
    for (int i = 0; i < 34772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34772(int& val) {
    const int x = 34772;
    std::cout << 34772;
    for (int i = 0; i < 34772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34773(int& val) {
    func_in_34773(val);
    const int x = 34773;
    std::cout << 34773;
    for (int i = 0; i < 34773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34773(int& val) {
    const int x = 34773;
    std::cout << 34773;
    for (int i = 0; i < 34773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34774(int& val) {
    func_in_34774(val);
    const int x = 34774;
    std::cout << 34774;
    for (int i = 0; i < 34774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34774(int& val) {
    const int x = 34774;
    std::cout << 34774;
    for (int i = 0; i < 34774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34775(int& val) {
    func_in_34775(val);
    const int x = 34775;
    std::cout << 34775;
    for (int i = 0; i < 34775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34775(int& val) {
    const int x = 34775;
    std::cout << 34775;
    for (int i = 0; i < 34775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34776(int& val) {
    func_in_34776(val);
    const int x = 34776;
    std::cout << 34776;
    for (int i = 0; i < 34776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34776(int& val) {
    const int x = 34776;
    std::cout << 34776;
    for (int i = 0; i < 34776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34777(int& val) {
    func_in_34777(val);
    const int x = 34777;
    std::cout << 34777;
    for (int i = 0; i < 34777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34777(int& val) {
    const int x = 34777;
    std::cout << 34777;
    for (int i = 0; i < 34777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34778(int& val) {
    func_in_34778(val);
    const int x = 34778;
    std::cout << 34778;
    for (int i = 0; i < 34778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34778(int& val) {
    const int x = 34778;
    std::cout << 34778;
    for (int i = 0; i < 34778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34779(int& val) {
    func_in_34779(val);
    const int x = 34779;
    std::cout << 34779;
    for (int i = 0; i < 34779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34779(int& val) {
    const int x = 34779;
    std::cout << 34779;
    for (int i = 0; i < 34779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34780(int& val) {
    func_in_34780(val);
    const int x = 34780;
    std::cout << 34780;
    for (int i = 0; i < 34780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34780(int& val) {
    const int x = 34780;
    std::cout << 34780;
    for (int i = 0; i < 34780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34781(int& val) {
    func_in_34781(val);
    const int x = 34781;
    std::cout << 34781;
    for (int i = 0; i < 34781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34781(int& val) {
    const int x = 34781;
    std::cout << 34781;
    for (int i = 0; i < 34781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34782(int& val) {
    func_in_34782(val);
    const int x = 34782;
    std::cout << 34782;
    for (int i = 0; i < 34782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34782(int& val) {
    const int x = 34782;
    std::cout << 34782;
    for (int i = 0; i < 34782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34783(int& val) {
    func_in_34783(val);
    const int x = 34783;
    std::cout << 34783;
    for (int i = 0; i < 34783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34783(int& val) {
    const int x = 34783;
    std::cout << 34783;
    for (int i = 0; i < 34783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34784(int& val) {
    func_in_34784(val);
    const int x = 34784;
    std::cout << 34784;
    for (int i = 0; i < 34784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34784(int& val) {
    const int x = 34784;
    std::cout << 34784;
    for (int i = 0; i < 34784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34785(int& val) {
    func_in_34785(val);
    const int x = 34785;
    std::cout << 34785;
    for (int i = 0; i < 34785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34785(int& val) {
    const int x = 34785;
    std::cout << 34785;
    for (int i = 0; i < 34785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34786(int& val) {
    func_in_34786(val);
    const int x = 34786;
    std::cout << 34786;
    for (int i = 0; i < 34786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34786(int& val) {
    const int x = 34786;
    std::cout << 34786;
    for (int i = 0; i < 34786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34787(int& val) {
    func_in_34787(val);
    const int x = 34787;
    std::cout << 34787;
    for (int i = 0; i < 34787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34787(int& val) {
    const int x = 34787;
    std::cout << 34787;
    for (int i = 0; i < 34787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34788(int& val) {
    func_in_34788(val);
    const int x = 34788;
    std::cout << 34788;
    for (int i = 0; i < 34788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34788(int& val) {
    const int x = 34788;
    std::cout << 34788;
    for (int i = 0; i < 34788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34789(int& val) {
    func_in_34789(val);
    const int x = 34789;
    std::cout << 34789;
    for (int i = 0; i < 34789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34789(int& val) {
    const int x = 34789;
    std::cout << 34789;
    for (int i = 0; i < 34789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34790(int& val) {
    func_in_34790(val);
    const int x = 34790;
    std::cout << 34790;
    for (int i = 0; i < 34790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34790(int& val) {
    const int x = 34790;
    std::cout << 34790;
    for (int i = 0; i < 34790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34791(int& val) {
    func_in_34791(val);
    const int x = 34791;
    std::cout << 34791;
    for (int i = 0; i < 34791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34791(int& val) {
    const int x = 34791;
    std::cout << 34791;
    for (int i = 0; i < 34791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34792(int& val) {
    func_in_34792(val);
    const int x = 34792;
    std::cout << 34792;
    for (int i = 0; i < 34792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34792(int& val) {
    const int x = 34792;
    std::cout << 34792;
    for (int i = 0; i < 34792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34793(int& val) {
    func_in_34793(val);
    const int x = 34793;
    std::cout << 34793;
    for (int i = 0; i < 34793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34793(int& val) {
    const int x = 34793;
    std::cout << 34793;
    for (int i = 0; i < 34793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34794(int& val) {
    func_in_34794(val);
    const int x = 34794;
    std::cout << 34794;
    for (int i = 0; i < 34794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34794(int& val) {
    const int x = 34794;
    std::cout << 34794;
    for (int i = 0; i < 34794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34795(int& val) {
    func_in_34795(val);
    const int x = 34795;
    std::cout << 34795;
    for (int i = 0; i < 34795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34795(int& val) {
    const int x = 34795;
    std::cout << 34795;
    for (int i = 0; i < 34795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34796(int& val) {
    func_in_34796(val);
    const int x = 34796;
    std::cout << 34796;
    for (int i = 0; i < 34796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34796(int& val) {
    const int x = 34796;
    std::cout << 34796;
    for (int i = 0; i < 34796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34797(int& val) {
    func_in_34797(val);
    const int x = 34797;
    std::cout << 34797;
    for (int i = 0; i < 34797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34797(int& val) {
    const int x = 34797;
    std::cout << 34797;
    for (int i = 0; i < 34797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34798(int& val) {
    func_in_34798(val);
    const int x = 34798;
    std::cout << 34798;
    for (int i = 0; i < 34798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34798(int& val) {
    const int x = 34798;
    std::cout << 34798;
    for (int i = 0; i < 34798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34799(int& val) {
    func_in_34799(val);
    const int x = 34799;
    std::cout << 34799;
    for (int i = 0; i < 34799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34799(int& val) {
    const int x = 34799;
    std::cout << 34799;
    for (int i = 0; i < 34799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34800(int& val) {
    func_in_34800(val);
    const int x = 34800;
    std::cout << 34800;
    for (int i = 0; i < 34800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34800(int& val) {
    const int x = 34800;
    std::cout << 34800;
    for (int i = 0; i < 34800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34801(int& val) {
    func_in_34801(val);
    const int x = 34801;
    std::cout << 34801;
    for (int i = 0; i < 34801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34801(int& val) {
    const int x = 34801;
    std::cout << 34801;
    for (int i = 0; i < 34801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34802(int& val) {
    func_in_34802(val);
    const int x = 34802;
    std::cout << 34802;
    for (int i = 0; i < 34802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34802(int& val) {
    const int x = 34802;
    std::cout << 34802;
    for (int i = 0; i < 34802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34803(int& val) {
    func_in_34803(val);
    const int x = 34803;
    std::cout << 34803;
    for (int i = 0; i < 34803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34803(int& val) {
    const int x = 34803;
    std::cout << 34803;
    for (int i = 0; i < 34803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34804(int& val) {
    func_in_34804(val);
    const int x = 34804;
    std::cout << 34804;
    for (int i = 0; i < 34804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34804(int& val) {
    const int x = 34804;
    std::cout << 34804;
    for (int i = 0; i < 34804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34805(int& val) {
    func_in_34805(val);
    const int x = 34805;
    std::cout << 34805;
    for (int i = 0; i < 34805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34805(int& val) {
    const int x = 34805;
    std::cout << 34805;
    for (int i = 0; i < 34805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34806(int& val) {
    func_in_34806(val);
    const int x = 34806;
    std::cout << 34806;
    for (int i = 0; i < 34806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34806(int& val) {
    const int x = 34806;
    std::cout << 34806;
    for (int i = 0; i < 34806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34807(int& val) {
    func_in_34807(val);
    const int x = 34807;
    std::cout << 34807;
    for (int i = 0; i < 34807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34807(int& val) {
    const int x = 34807;
    std::cout << 34807;
    for (int i = 0; i < 34807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34808(int& val) {
    func_in_34808(val);
    const int x = 34808;
    std::cout << 34808;
    for (int i = 0; i < 34808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34808(int& val) {
    const int x = 34808;
    std::cout << 34808;
    for (int i = 0; i < 34808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34809(int& val) {
    func_in_34809(val);
    const int x = 34809;
    std::cout << 34809;
    for (int i = 0; i < 34809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34809(int& val) {
    const int x = 34809;
    std::cout << 34809;
    for (int i = 0; i < 34809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34810(int& val) {
    func_in_34810(val);
    const int x = 34810;
    std::cout << 34810;
    for (int i = 0; i < 34810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34810(int& val) {
    const int x = 34810;
    std::cout << 34810;
    for (int i = 0; i < 34810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34811(int& val) {
    func_in_34811(val);
    const int x = 34811;
    std::cout << 34811;
    for (int i = 0; i < 34811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34811(int& val) {
    const int x = 34811;
    std::cout << 34811;
    for (int i = 0; i < 34811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34812(int& val) {
    func_in_34812(val);
    const int x = 34812;
    std::cout << 34812;
    for (int i = 0; i < 34812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34812(int& val) {
    const int x = 34812;
    std::cout << 34812;
    for (int i = 0; i < 34812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34813(int& val) {
    func_in_34813(val);
    const int x = 34813;
    std::cout << 34813;
    for (int i = 0; i < 34813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34813(int& val) {
    const int x = 34813;
    std::cout << 34813;
    for (int i = 0; i < 34813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34814(int& val) {
    func_in_34814(val);
    const int x = 34814;
    std::cout << 34814;
    for (int i = 0; i < 34814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34814(int& val) {
    const int x = 34814;
    std::cout << 34814;
    for (int i = 0; i < 34814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34815(int& val) {
    func_in_34815(val);
    const int x = 34815;
    std::cout << 34815;
    for (int i = 0; i < 34815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34815(int& val) {
    const int x = 34815;
    std::cout << 34815;
    for (int i = 0; i < 34815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34816(int& val) {
    func_in_34816(val);
    const int x = 34816;
    std::cout << 34816;
    for (int i = 0; i < 34816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34816(int& val) {
    const int x = 34816;
    std::cout << 34816;
    for (int i = 0; i < 34816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34817(int& val) {
    func_in_34817(val);
    const int x = 34817;
    std::cout << 34817;
    for (int i = 0; i < 34817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34817(int& val) {
    const int x = 34817;
    std::cout << 34817;
    for (int i = 0; i < 34817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34818(int& val) {
    func_in_34818(val);
    const int x = 34818;
    std::cout << 34818;
    for (int i = 0; i < 34818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34818(int& val) {
    const int x = 34818;
    std::cout << 34818;
    for (int i = 0; i < 34818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34819(int& val) {
    func_in_34819(val);
    const int x = 34819;
    std::cout << 34819;
    for (int i = 0; i < 34819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34819(int& val) {
    const int x = 34819;
    std::cout << 34819;
    for (int i = 0; i < 34819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34820(int& val) {
    func_in_34820(val);
    const int x = 34820;
    std::cout << 34820;
    for (int i = 0; i < 34820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34820(int& val) {
    const int x = 34820;
    std::cout << 34820;
    for (int i = 0; i < 34820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34821(int& val) {
    func_in_34821(val);
    const int x = 34821;
    std::cout << 34821;
    for (int i = 0; i < 34821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34821(int& val) {
    const int x = 34821;
    std::cout << 34821;
    for (int i = 0; i < 34821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34822(int& val) {
    func_in_34822(val);
    const int x = 34822;
    std::cout << 34822;
    for (int i = 0; i < 34822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34822(int& val) {
    const int x = 34822;
    std::cout << 34822;
    for (int i = 0; i < 34822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34823(int& val) {
    func_in_34823(val);
    const int x = 34823;
    std::cout << 34823;
    for (int i = 0; i < 34823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34823(int& val) {
    const int x = 34823;
    std::cout << 34823;
    for (int i = 0; i < 34823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34824(int& val) {
    func_in_34824(val);
    const int x = 34824;
    std::cout << 34824;
    for (int i = 0; i < 34824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34824(int& val) {
    const int x = 34824;
    std::cout << 34824;
    for (int i = 0; i < 34824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34825(int& val) {
    func_in_34825(val);
    const int x = 34825;
    std::cout << 34825;
    for (int i = 0; i < 34825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34825(int& val) {
    const int x = 34825;
    std::cout << 34825;
    for (int i = 0; i < 34825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34826(int& val) {
    func_in_34826(val);
    const int x = 34826;
    std::cout << 34826;
    for (int i = 0; i < 34826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34826(int& val) {
    const int x = 34826;
    std::cout << 34826;
    for (int i = 0; i < 34826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34827(int& val) {
    func_in_34827(val);
    const int x = 34827;
    std::cout << 34827;
    for (int i = 0; i < 34827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34827(int& val) {
    const int x = 34827;
    std::cout << 34827;
    for (int i = 0; i < 34827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34828(int& val) {
    func_in_34828(val);
    const int x = 34828;
    std::cout << 34828;
    for (int i = 0; i < 34828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34828(int& val) {
    const int x = 34828;
    std::cout << 34828;
    for (int i = 0; i < 34828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34829(int& val) {
    func_in_34829(val);
    const int x = 34829;
    std::cout << 34829;
    for (int i = 0; i < 34829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34829(int& val) {
    const int x = 34829;
    std::cout << 34829;
    for (int i = 0; i < 34829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34830(int& val) {
    func_in_34830(val);
    const int x = 34830;
    std::cout << 34830;
    for (int i = 0; i < 34830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34830(int& val) {
    const int x = 34830;
    std::cout << 34830;
    for (int i = 0; i < 34830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34831(int& val) {
    func_in_34831(val);
    const int x = 34831;
    std::cout << 34831;
    for (int i = 0; i < 34831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34831(int& val) {
    const int x = 34831;
    std::cout << 34831;
    for (int i = 0; i < 34831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34832(int& val) {
    func_in_34832(val);
    const int x = 34832;
    std::cout << 34832;
    for (int i = 0; i < 34832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34832(int& val) {
    const int x = 34832;
    std::cout << 34832;
    for (int i = 0; i < 34832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34833(int& val) {
    func_in_34833(val);
    const int x = 34833;
    std::cout << 34833;
    for (int i = 0; i < 34833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34833(int& val) {
    const int x = 34833;
    std::cout << 34833;
    for (int i = 0; i < 34833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34834(int& val) {
    func_in_34834(val);
    const int x = 34834;
    std::cout << 34834;
    for (int i = 0; i < 34834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34834(int& val) {
    const int x = 34834;
    std::cout << 34834;
    for (int i = 0; i < 34834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34835(int& val) {
    func_in_34835(val);
    const int x = 34835;
    std::cout << 34835;
    for (int i = 0; i < 34835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34835(int& val) {
    const int x = 34835;
    std::cout << 34835;
    for (int i = 0; i < 34835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34836(int& val) {
    func_in_34836(val);
    const int x = 34836;
    std::cout << 34836;
    for (int i = 0; i < 34836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34836(int& val) {
    const int x = 34836;
    std::cout << 34836;
    for (int i = 0; i < 34836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34837(int& val) {
    func_in_34837(val);
    const int x = 34837;
    std::cout << 34837;
    for (int i = 0; i < 34837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34837(int& val) {
    const int x = 34837;
    std::cout << 34837;
    for (int i = 0; i < 34837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34838(int& val) {
    func_in_34838(val);
    const int x = 34838;
    std::cout << 34838;
    for (int i = 0; i < 34838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34838(int& val) {
    const int x = 34838;
    std::cout << 34838;
    for (int i = 0; i < 34838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34839(int& val) {
    func_in_34839(val);
    const int x = 34839;
    std::cout << 34839;
    for (int i = 0; i < 34839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34839(int& val) {
    const int x = 34839;
    std::cout << 34839;
    for (int i = 0; i < 34839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34840(int& val) {
    func_in_34840(val);
    const int x = 34840;
    std::cout << 34840;
    for (int i = 0; i < 34840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34840(int& val) {
    const int x = 34840;
    std::cout << 34840;
    for (int i = 0; i < 34840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34841(int& val) {
    func_in_34841(val);
    const int x = 34841;
    std::cout << 34841;
    for (int i = 0; i < 34841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34841(int& val) {
    const int x = 34841;
    std::cout << 34841;
    for (int i = 0; i < 34841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34842(int& val) {
    func_in_34842(val);
    const int x = 34842;
    std::cout << 34842;
    for (int i = 0; i < 34842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34842(int& val) {
    const int x = 34842;
    std::cout << 34842;
    for (int i = 0; i < 34842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34843(int& val) {
    func_in_34843(val);
    const int x = 34843;
    std::cout << 34843;
    for (int i = 0; i < 34843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34843(int& val) {
    const int x = 34843;
    std::cout << 34843;
    for (int i = 0; i < 34843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34844(int& val) {
    func_in_34844(val);
    const int x = 34844;
    std::cout << 34844;
    for (int i = 0; i < 34844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34844(int& val) {
    const int x = 34844;
    std::cout << 34844;
    for (int i = 0; i < 34844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34845(int& val) {
    func_in_34845(val);
    const int x = 34845;
    std::cout << 34845;
    for (int i = 0; i < 34845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34845(int& val) {
    const int x = 34845;
    std::cout << 34845;
    for (int i = 0; i < 34845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34846(int& val) {
    func_in_34846(val);
    const int x = 34846;
    std::cout << 34846;
    for (int i = 0; i < 34846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34846(int& val) {
    const int x = 34846;
    std::cout << 34846;
    for (int i = 0; i < 34846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34847(int& val) {
    func_in_34847(val);
    const int x = 34847;
    std::cout << 34847;
    for (int i = 0; i < 34847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34847(int& val) {
    const int x = 34847;
    std::cout << 34847;
    for (int i = 0; i < 34847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34848(int& val) {
    func_in_34848(val);
    const int x = 34848;
    std::cout << 34848;
    for (int i = 0; i < 34848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34848(int& val) {
    const int x = 34848;
    std::cout << 34848;
    for (int i = 0; i < 34848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34849(int& val) {
    func_in_34849(val);
    const int x = 34849;
    std::cout << 34849;
    for (int i = 0; i < 34849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34849(int& val) {
    const int x = 34849;
    std::cout << 34849;
    for (int i = 0; i < 34849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34850(int& val) {
    func_in_34850(val);
    const int x = 34850;
    std::cout << 34850;
    for (int i = 0; i < 34850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34850(int& val) {
    const int x = 34850;
    std::cout << 34850;
    for (int i = 0; i < 34850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34851(int& val) {
    func_in_34851(val);
    const int x = 34851;
    std::cout << 34851;
    for (int i = 0; i < 34851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34851(int& val) {
    const int x = 34851;
    std::cout << 34851;
    for (int i = 0; i < 34851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34852(int& val) {
    func_in_34852(val);
    const int x = 34852;
    std::cout << 34852;
    for (int i = 0; i < 34852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34852(int& val) {
    const int x = 34852;
    std::cout << 34852;
    for (int i = 0; i < 34852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34853(int& val) {
    func_in_34853(val);
    const int x = 34853;
    std::cout << 34853;
    for (int i = 0; i < 34853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34853(int& val) {
    const int x = 34853;
    std::cout << 34853;
    for (int i = 0; i < 34853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34854(int& val) {
    func_in_34854(val);
    const int x = 34854;
    std::cout << 34854;
    for (int i = 0; i < 34854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34854(int& val) {
    const int x = 34854;
    std::cout << 34854;
    for (int i = 0; i < 34854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34855(int& val) {
    func_in_34855(val);
    const int x = 34855;
    std::cout << 34855;
    for (int i = 0; i < 34855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34855(int& val) {
    const int x = 34855;
    std::cout << 34855;
    for (int i = 0; i < 34855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34856(int& val) {
    func_in_34856(val);
    const int x = 34856;
    std::cout << 34856;
    for (int i = 0; i < 34856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34856(int& val) {
    const int x = 34856;
    std::cout << 34856;
    for (int i = 0; i < 34856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34857(int& val) {
    func_in_34857(val);
    const int x = 34857;
    std::cout << 34857;
    for (int i = 0; i < 34857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34857(int& val) {
    const int x = 34857;
    std::cout << 34857;
    for (int i = 0; i < 34857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34858(int& val) {
    func_in_34858(val);
    const int x = 34858;
    std::cout << 34858;
    for (int i = 0; i < 34858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34858(int& val) {
    const int x = 34858;
    std::cout << 34858;
    for (int i = 0; i < 34858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34859(int& val) {
    func_in_34859(val);
    const int x = 34859;
    std::cout << 34859;
    for (int i = 0; i < 34859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34859(int& val) {
    const int x = 34859;
    std::cout << 34859;
    for (int i = 0; i < 34859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34860(int& val) {
    func_in_34860(val);
    const int x = 34860;
    std::cout << 34860;
    for (int i = 0; i < 34860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34860(int& val) {
    const int x = 34860;
    std::cout << 34860;
    for (int i = 0; i < 34860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34861(int& val) {
    func_in_34861(val);
    const int x = 34861;
    std::cout << 34861;
    for (int i = 0; i < 34861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34861(int& val) {
    const int x = 34861;
    std::cout << 34861;
    for (int i = 0; i < 34861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34862(int& val) {
    func_in_34862(val);
    const int x = 34862;
    std::cout << 34862;
    for (int i = 0; i < 34862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34862(int& val) {
    const int x = 34862;
    std::cout << 34862;
    for (int i = 0; i < 34862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34863(int& val) {
    func_in_34863(val);
    const int x = 34863;
    std::cout << 34863;
    for (int i = 0; i < 34863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34863(int& val) {
    const int x = 34863;
    std::cout << 34863;
    for (int i = 0; i < 34863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34864(int& val) {
    func_in_34864(val);
    const int x = 34864;
    std::cout << 34864;
    for (int i = 0; i < 34864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34864(int& val) {
    const int x = 34864;
    std::cout << 34864;
    for (int i = 0; i < 34864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34865(int& val) {
    func_in_34865(val);
    const int x = 34865;
    std::cout << 34865;
    for (int i = 0; i < 34865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34865(int& val) {
    const int x = 34865;
    std::cout << 34865;
    for (int i = 0; i < 34865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34866(int& val) {
    func_in_34866(val);
    const int x = 34866;
    std::cout << 34866;
    for (int i = 0; i < 34866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34866(int& val) {
    const int x = 34866;
    std::cout << 34866;
    for (int i = 0; i < 34866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34867(int& val) {
    func_in_34867(val);
    const int x = 34867;
    std::cout << 34867;
    for (int i = 0; i < 34867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34867(int& val) {
    const int x = 34867;
    std::cout << 34867;
    for (int i = 0; i < 34867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34868(int& val) {
    func_in_34868(val);
    const int x = 34868;
    std::cout << 34868;
    for (int i = 0; i < 34868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34868(int& val) {
    const int x = 34868;
    std::cout << 34868;
    for (int i = 0; i < 34868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34869(int& val) {
    func_in_34869(val);
    const int x = 34869;
    std::cout << 34869;
    for (int i = 0; i < 34869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34869(int& val) {
    const int x = 34869;
    std::cout << 34869;
    for (int i = 0; i < 34869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34870(int& val) {
    func_in_34870(val);
    const int x = 34870;
    std::cout << 34870;
    for (int i = 0; i < 34870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34870(int& val) {
    const int x = 34870;
    std::cout << 34870;
    for (int i = 0; i < 34870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34871(int& val) {
    func_in_34871(val);
    const int x = 34871;
    std::cout << 34871;
    for (int i = 0; i < 34871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34871(int& val) {
    const int x = 34871;
    std::cout << 34871;
    for (int i = 0; i < 34871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34872(int& val) {
    func_in_34872(val);
    const int x = 34872;
    std::cout << 34872;
    for (int i = 0; i < 34872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34872(int& val) {
    const int x = 34872;
    std::cout << 34872;
    for (int i = 0; i < 34872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34873(int& val) {
    func_in_34873(val);
    const int x = 34873;
    std::cout << 34873;
    for (int i = 0; i < 34873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34873(int& val) {
    const int x = 34873;
    std::cout << 34873;
    for (int i = 0; i < 34873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34874(int& val) {
    func_in_34874(val);
    const int x = 34874;
    std::cout << 34874;
    for (int i = 0; i < 34874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34874(int& val) {
    const int x = 34874;
    std::cout << 34874;
    for (int i = 0; i < 34874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34875(int& val) {
    func_in_34875(val);
    const int x = 34875;
    std::cout << 34875;
    for (int i = 0; i < 34875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34875(int& val) {
    const int x = 34875;
    std::cout << 34875;
    for (int i = 0; i < 34875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34876(int& val) {
    func_in_34876(val);
    const int x = 34876;
    std::cout << 34876;
    for (int i = 0; i < 34876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34876(int& val) {
    const int x = 34876;
    std::cout << 34876;
    for (int i = 0; i < 34876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34877(int& val) {
    func_in_34877(val);
    const int x = 34877;
    std::cout << 34877;
    for (int i = 0; i < 34877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34877(int& val) {
    const int x = 34877;
    std::cout << 34877;
    for (int i = 0; i < 34877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34878(int& val) {
    func_in_34878(val);
    const int x = 34878;
    std::cout << 34878;
    for (int i = 0; i < 34878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34878(int& val) {
    const int x = 34878;
    std::cout << 34878;
    for (int i = 0; i < 34878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34879(int& val) {
    func_in_34879(val);
    const int x = 34879;
    std::cout << 34879;
    for (int i = 0; i < 34879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34879(int& val) {
    const int x = 34879;
    std::cout << 34879;
    for (int i = 0; i < 34879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34880(int& val) {
    func_in_34880(val);
    const int x = 34880;
    std::cout << 34880;
    for (int i = 0; i < 34880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34880(int& val) {
    const int x = 34880;
    std::cout << 34880;
    for (int i = 0; i < 34880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34881(int& val) {
    func_in_34881(val);
    const int x = 34881;
    std::cout << 34881;
    for (int i = 0; i < 34881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34881(int& val) {
    const int x = 34881;
    std::cout << 34881;
    for (int i = 0; i < 34881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34882(int& val) {
    func_in_34882(val);
    const int x = 34882;
    std::cout << 34882;
    for (int i = 0; i < 34882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34882(int& val) {
    const int x = 34882;
    std::cout << 34882;
    for (int i = 0; i < 34882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34883(int& val) {
    func_in_34883(val);
    const int x = 34883;
    std::cout << 34883;
    for (int i = 0; i < 34883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34883(int& val) {
    const int x = 34883;
    std::cout << 34883;
    for (int i = 0; i < 34883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34884(int& val) {
    func_in_34884(val);
    const int x = 34884;
    std::cout << 34884;
    for (int i = 0; i < 34884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34884(int& val) {
    const int x = 34884;
    std::cout << 34884;
    for (int i = 0; i < 34884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34885(int& val) {
    func_in_34885(val);
    const int x = 34885;
    std::cout << 34885;
    for (int i = 0; i < 34885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34885(int& val) {
    const int x = 34885;
    std::cout << 34885;
    for (int i = 0; i < 34885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34886(int& val) {
    func_in_34886(val);
    const int x = 34886;
    std::cout << 34886;
    for (int i = 0; i < 34886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34886(int& val) {
    const int x = 34886;
    std::cout << 34886;
    for (int i = 0; i < 34886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34887(int& val) {
    func_in_34887(val);
    const int x = 34887;
    std::cout << 34887;
    for (int i = 0; i < 34887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34887(int& val) {
    const int x = 34887;
    std::cout << 34887;
    for (int i = 0; i < 34887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34888(int& val) {
    func_in_34888(val);
    const int x = 34888;
    std::cout << 34888;
    for (int i = 0; i < 34888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34888(int& val) {
    const int x = 34888;
    std::cout << 34888;
    for (int i = 0; i < 34888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34889(int& val) {
    func_in_34889(val);
    const int x = 34889;
    std::cout << 34889;
    for (int i = 0; i < 34889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34889(int& val) {
    const int x = 34889;
    std::cout << 34889;
    for (int i = 0; i < 34889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34890(int& val) {
    func_in_34890(val);
    const int x = 34890;
    std::cout << 34890;
    for (int i = 0; i < 34890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34890(int& val) {
    const int x = 34890;
    std::cout << 34890;
    for (int i = 0; i < 34890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34891(int& val) {
    func_in_34891(val);
    const int x = 34891;
    std::cout << 34891;
    for (int i = 0; i < 34891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34891(int& val) {
    const int x = 34891;
    std::cout << 34891;
    for (int i = 0; i < 34891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34892(int& val) {
    func_in_34892(val);
    const int x = 34892;
    std::cout << 34892;
    for (int i = 0; i < 34892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34892(int& val) {
    const int x = 34892;
    std::cout << 34892;
    for (int i = 0; i < 34892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34893(int& val) {
    func_in_34893(val);
    const int x = 34893;
    std::cout << 34893;
    for (int i = 0; i < 34893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34893(int& val) {
    const int x = 34893;
    std::cout << 34893;
    for (int i = 0; i < 34893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34894(int& val) {
    func_in_34894(val);
    const int x = 34894;
    std::cout << 34894;
    for (int i = 0; i < 34894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34894(int& val) {
    const int x = 34894;
    std::cout << 34894;
    for (int i = 0; i < 34894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34895(int& val) {
    func_in_34895(val);
    const int x = 34895;
    std::cout << 34895;
    for (int i = 0; i < 34895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34895(int& val) {
    const int x = 34895;
    std::cout << 34895;
    for (int i = 0; i < 34895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34896(int& val) {
    func_in_34896(val);
    const int x = 34896;
    std::cout << 34896;
    for (int i = 0; i < 34896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34896(int& val) {
    const int x = 34896;
    std::cout << 34896;
    for (int i = 0; i < 34896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34897(int& val) {
    func_in_34897(val);
    const int x = 34897;
    std::cout << 34897;
    for (int i = 0; i < 34897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34897(int& val) {
    const int x = 34897;
    std::cout << 34897;
    for (int i = 0; i < 34897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34898(int& val) {
    func_in_34898(val);
    const int x = 34898;
    std::cout << 34898;
    for (int i = 0; i < 34898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34898(int& val) {
    const int x = 34898;
    std::cout << 34898;
    for (int i = 0; i < 34898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34899(int& val) {
    func_in_34899(val);
    const int x = 34899;
    std::cout << 34899;
    for (int i = 0; i < 34899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34899(int& val) {
    const int x = 34899;
    std::cout << 34899;
    for (int i = 0; i < 34899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34900(int& val) {
    func_in_34900(val);
    const int x = 34900;
    std::cout << 34900;
    for (int i = 0; i < 34900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34900(int& val) {
    const int x = 34900;
    std::cout << 34900;
    for (int i = 0; i < 34900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34901(int& val) {
    func_in_34901(val);
    const int x = 34901;
    std::cout << 34901;
    for (int i = 0; i < 34901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34901(int& val) {
    const int x = 34901;
    std::cout << 34901;
    for (int i = 0; i < 34901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34902(int& val) {
    func_in_34902(val);
    const int x = 34902;
    std::cout << 34902;
    for (int i = 0; i < 34902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34902(int& val) {
    const int x = 34902;
    std::cout << 34902;
    for (int i = 0; i < 34902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34903(int& val) {
    func_in_34903(val);
    const int x = 34903;
    std::cout << 34903;
    for (int i = 0; i < 34903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34903(int& val) {
    const int x = 34903;
    std::cout << 34903;
    for (int i = 0; i < 34903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34904(int& val) {
    func_in_34904(val);
    const int x = 34904;
    std::cout << 34904;
    for (int i = 0; i < 34904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34904(int& val) {
    const int x = 34904;
    std::cout << 34904;
    for (int i = 0; i < 34904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34905(int& val) {
    func_in_34905(val);
    const int x = 34905;
    std::cout << 34905;
    for (int i = 0; i < 34905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34905(int& val) {
    const int x = 34905;
    std::cout << 34905;
    for (int i = 0; i < 34905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34906(int& val) {
    func_in_34906(val);
    const int x = 34906;
    std::cout << 34906;
    for (int i = 0; i < 34906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34906(int& val) {
    const int x = 34906;
    std::cout << 34906;
    for (int i = 0; i < 34906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34907(int& val) {
    func_in_34907(val);
    const int x = 34907;
    std::cout << 34907;
    for (int i = 0; i < 34907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34907(int& val) {
    const int x = 34907;
    std::cout << 34907;
    for (int i = 0; i < 34907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34908(int& val) {
    func_in_34908(val);
    const int x = 34908;
    std::cout << 34908;
    for (int i = 0; i < 34908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34908(int& val) {
    const int x = 34908;
    std::cout << 34908;
    for (int i = 0; i < 34908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34909(int& val) {
    func_in_34909(val);
    const int x = 34909;
    std::cout << 34909;
    for (int i = 0; i < 34909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34909(int& val) {
    const int x = 34909;
    std::cout << 34909;
    for (int i = 0; i < 34909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34910(int& val) {
    func_in_34910(val);
    const int x = 34910;
    std::cout << 34910;
    for (int i = 0; i < 34910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34910(int& val) {
    const int x = 34910;
    std::cout << 34910;
    for (int i = 0; i < 34910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34911(int& val) {
    func_in_34911(val);
    const int x = 34911;
    std::cout << 34911;
    for (int i = 0; i < 34911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34911(int& val) {
    const int x = 34911;
    std::cout << 34911;
    for (int i = 0; i < 34911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34912(int& val) {
    func_in_34912(val);
    const int x = 34912;
    std::cout << 34912;
    for (int i = 0; i < 34912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34912(int& val) {
    const int x = 34912;
    std::cout << 34912;
    for (int i = 0; i < 34912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34913(int& val) {
    func_in_34913(val);
    const int x = 34913;
    std::cout << 34913;
    for (int i = 0; i < 34913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34913(int& val) {
    const int x = 34913;
    std::cout << 34913;
    for (int i = 0; i < 34913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34914(int& val) {
    func_in_34914(val);
    const int x = 34914;
    std::cout << 34914;
    for (int i = 0; i < 34914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34914(int& val) {
    const int x = 34914;
    std::cout << 34914;
    for (int i = 0; i < 34914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34915(int& val) {
    func_in_34915(val);
    const int x = 34915;
    std::cout << 34915;
    for (int i = 0; i < 34915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34915(int& val) {
    const int x = 34915;
    std::cout << 34915;
    for (int i = 0; i < 34915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34916(int& val) {
    func_in_34916(val);
    const int x = 34916;
    std::cout << 34916;
    for (int i = 0; i < 34916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34916(int& val) {
    const int x = 34916;
    std::cout << 34916;
    for (int i = 0; i < 34916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34917(int& val) {
    func_in_34917(val);
    const int x = 34917;
    std::cout << 34917;
    for (int i = 0; i < 34917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34917(int& val) {
    const int x = 34917;
    std::cout << 34917;
    for (int i = 0; i < 34917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34918(int& val) {
    func_in_34918(val);
    const int x = 34918;
    std::cout << 34918;
    for (int i = 0; i < 34918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34918(int& val) {
    const int x = 34918;
    std::cout << 34918;
    for (int i = 0; i < 34918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34919(int& val) {
    func_in_34919(val);
    const int x = 34919;
    std::cout << 34919;
    for (int i = 0; i < 34919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34919(int& val) {
    const int x = 34919;
    std::cout << 34919;
    for (int i = 0; i < 34919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34920(int& val) {
    func_in_34920(val);
    const int x = 34920;
    std::cout << 34920;
    for (int i = 0; i < 34920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34920(int& val) {
    const int x = 34920;
    std::cout << 34920;
    for (int i = 0; i < 34920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34921(int& val) {
    func_in_34921(val);
    const int x = 34921;
    std::cout << 34921;
    for (int i = 0; i < 34921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34921(int& val) {
    const int x = 34921;
    std::cout << 34921;
    for (int i = 0; i < 34921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34922(int& val) {
    func_in_34922(val);
    const int x = 34922;
    std::cout << 34922;
    for (int i = 0; i < 34922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34922(int& val) {
    const int x = 34922;
    std::cout << 34922;
    for (int i = 0; i < 34922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34923(int& val) {
    func_in_34923(val);
    const int x = 34923;
    std::cout << 34923;
    for (int i = 0; i < 34923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34923(int& val) {
    const int x = 34923;
    std::cout << 34923;
    for (int i = 0; i < 34923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34924(int& val) {
    func_in_34924(val);
    const int x = 34924;
    std::cout << 34924;
    for (int i = 0; i < 34924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34924(int& val) {
    const int x = 34924;
    std::cout << 34924;
    for (int i = 0; i < 34924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34925(int& val) {
    func_in_34925(val);
    const int x = 34925;
    std::cout << 34925;
    for (int i = 0; i < 34925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34925(int& val) {
    const int x = 34925;
    std::cout << 34925;
    for (int i = 0; i < 34925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34926(int& val) {
    func_in_34926(val);
    const int x = 34926;
    std::cout << 34926;
    for (int i = 0; i < 34926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34926(int& val) {
    const int x = 34926;
    std::cout << 34926;
    for (int i = 0; i < 34926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34927(int& val) {
    func_in_34927(val);
    const int x = 34927;
    std::cout << 34927;
    for (int i = 0; i < 34927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34927(int& val) {
    const int x = 34927;
    std::cout << 34927;
    for (int i = 0; i < 34927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34928(int& val) {
    func_in_34928(val);
    const int x = 34928;
    std::cout << 34928;
    for (int i = 0; i < 34928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34928(int& val) {
    const int x = 34928;
    std::cout << 34928;
    for (int i = 0; i < 34928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34929(int& val) {
    func_in_34929(val);
    const int x = 34929;
    std::cout << 34929;
    for (int i = 0; i < 34929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34929(int& val) {
    const int x = 34929;
    std::cout << 34929;
    for (int i = 0; i < 34929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34930(int& val) {
    func_in_34930(val);
    const int x = 34930;
    std::cout << 34930;
    for (int i = 0; i < 34930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34930(int& val) {
    const int x = 34930;
    std::cout << 34930;
    for (int i = 0; i < 34930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34931(int& val) {
    func_in_34931(val);
    const int x = 34931;
    std::cout << 34931;
    for (int i = 0; i < 34931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34931(int& val) {
    const int x = 34931;
    std::cout << 34931;
    for (int i = 0; i < 34931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34932(int& val) {
    func_in_34932(val);
    const int x = 34932;
    std::cout << 34932;
    for (int i = 0; i < 34932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34932(int& val) {
    const int x = 34932;
    std::cout << 34932;
    for (int i = 0; i < 34932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34933(int& val) {
    func_in_34933(val);
    const int x = 34933;
    std::cout << 34933;
    for (int i = 0; i < 34933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34933(int& val) {
    const int x = 34933;
    std::cout << 34933;
    for (int i = 0; i < 34933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34934(int& val) {
    func_in_34934(val);
    const int x = 34934;
    std::cout << 34934;
    for (int i = 0; i < 34934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34934(int& val) {
    const int x = 34934;
    std::cout << 34934;
    for (int i = 0; i < 34934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34935(int& val) {
    func_in_34935(val);
    const int x = 34935;
    std::cout << 34935;
    for (int i = 0; i < 34935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34935(int& val) {
    const int x = 34935;
    std::cout << 34935;
    for (int i = 0; i < 34935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34936(int& val) {
    func_in_34936(val);
    const int x = 34936;
    std::cout << 34936;
    for (int i = 0; i < 34936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34936(int& val) {
    const int x = 34936;
    std::cout << 34936;
    for (int i = 0; i < 34936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34937(int& val) {
    func_in_34937(val);
    const int x = 34937;
    std::cout << 34937;
    for (int i = 0; i < 34937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34937(int& val) {
    const int x = 34937;
    std::cout << 34937;
    for (int i = 0; i < 34937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34938(int& val) {
    func_in_34938(val);
    const int x = 34938;
    std::cout << 34938;
    for (int i = 0; i < 34938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34938(int& val) {
    const int x = 34938;
    std::cout << 34938;
    for (int i = 0; i < 34938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34939(int& val) {
    func_in_34939(val);
    const int x = 34939;
    std::cout << 34939;
    for (int i = 0; i < 34939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34939(int& val) {
    const int x = 34939;
    std::cout << 34939;
    for (int i = 0; i < 34939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34940(int& val) {
    func_in_34940(val);
    const int x = 34940;
    std::cout << 34940;
    for (int i = 0; i < 34940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34940(int& val) {
    const int x = 34940;
    std::cout << 34940;
    for (int i = 0; i < 34940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34941(int& val) {
    func_in_34941(val);
    const int x = 34941;
    std::cout << 34941;
    for (int i = 0; i < 34941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34941(int& val) {
    const int x = 34941;
    std::cout << 34941;
    for (int i = 0; i < 34941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34942(int& val) {
    func_in_34942(val);
    const int x = 34942;
    std::cout << 34942;
    for (int i = 0; i < 34942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34942(int& val) {
    const int x = 34942;
    std::cout << 34942;
    for (int i = 0; i < 34942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34943(int& val) {
    func_in_34943(val);
    const int x = 34943;
    std::cout << 34943;
    for (int i = 0; i < 34943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34943(int& val) {
    const int x = 34943;
    std::cout << 34943;
    for (int i = 0; i < 34943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34944(int& val) {
    func_in_34944(val);
    const int x = 34944;
    std::cout << 34944;
    for (int i = 0; i < 34944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34944(int& val) {
    const int x = 34944;
    std::cout << 34944;
    for (int i = 0; i < 34944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34945(int& val) {
    func_in_34945(val);
    const int x = 34945;
    std::cout << 34945;
    for (int i = 0; i < 34945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34945(int& val) {
    const int x = 34945;
    std::cout << 34945;
    for (int i = 0; i < 34945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34946(int& val) {
    func_in_34946(val);
    const int x = 34946;
    std::cout << 34946;
    for (int i = 0; i < 34946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34946(int& val) {
    const int x = 34946;
    std::cout << 34946;
    for (int i = 0; i < 34946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34947(int& val) {
    func_in_34947(val);
    const int x = 34947;
    std::cout << 34947;
    for (int i = 0; i < 34947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34947(int& val) {
    const int x = 34947;
    std::cout << 34947;
    for (int i = 0; i < 34947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34948(int& val) {
    func_in_34948(val);
    const int x = 34948;
    std::cout << 34948;
    for (int i = 0; i < 34948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34948(int& val) {
    const int x = 34948;
    std::cout << 34948;
    for (int i = 0; i < 34948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34949(int& val) {
    func_in_34949(val);
    const int x = 34949;
    std::cout << 34949;
    for (int i = 0; i < 34949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34949(int& val) {
    const int x = 34949;
    std::cout << 34949;
    for (int i = 0; i < 34949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34950(int& val) {
    func_in_34950(val);
    const int x = 34950;
    std::cout << 34950;
    for (int i = 0; i < 34950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34950(int& val) {
    const int x = 34950;
    std::cout << 34950;
    for (int i = 0; i < 34950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34951(int& val) {
    func_in_34951(val);
    const int x = 34951;
    std::cout << 34951;
    for (int i = 0; i < 34951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34951(int& val) {
    const int x = 34951;
    std::cout << 34951;
    for (int i = 0; i < 34951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34952(int& val) {
    func_in_34952(val);
    const int x = 34952;
    std::cout << 34952;
    for (int i = 0; i < 34952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34952(int& val) {
    const int x = 34952;
    std::cout << 34952;
    for (int i = 0; i < 34952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34953(int& val) {
    func_in_34953(val);
    const int x = 34953;
    std::cout << 34953;
    for (int i = 0; i < 34953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34953(int& val) {
    const int x = 34953;
    std::cout << 34953;
    for (int i = 0; i < 34953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34954(int& val) {
    func_in_34954(val);
    const int x = 34954;
    std::cout << 34954;
    for (int i = 0; i < 34954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34954(int& val) {
    const int x = 34954;
    std::cout << 34954;
    for (int i = 0; i < 34954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34955(int& val) {
    func_in_34955(val);
    const int x = 34955;
    std::cout << 34955;
    for (int i = 0; i < 34955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34955(int& val) {
    const int x = 34955;
    std::cout << 34955;
    for (int i = 0; i < 34955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34956(int& val) {
    func_in_34956(val);
    const int x = 34956;
    std::cout << 34956;
    for (int i = 0; i < 34956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34956(int& val) {
    const int x = 34956;
    std::cout << 34956;
    for (int i = 0; i < 34956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34957(int& val) {
    func_in_34957(val);
    const int x = 34957;
    std::cout << 34957;
    for (int i = 0; i < 34957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34957(int& val) {
    const int x = 34957;
    std::cout << 34957;
    for (int i = 0; i < 34957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34958(int& val) {
    func_in_34958(val);
    const int x = 34958;
    std::cout << 34958;
    for (int i = 0; i < 34958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34958(int& val) {
    const int x = 34958;
    std::cout << 34958;
    for (int i = 0; i < 34958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34959(int& val) {
    func_in_34959(val);
    const int x = 34959;
    std::cout << 34959;
    for (int i = 0; i < 34959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34959(int& val) {
    const int x = 34959;
    std::cout << 34959;
    for (int i = 0; i < 34959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34960(int& val) {
    func_in_34960(val);
    const int x = 34960;
    std::cout << 34960;
    for (int i = 0; i < 34960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34960(int& val) {
    const int x = 34960;
    std::cout << 34960;
    for (int i = 0; i < 34960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34961(int& val) {
    func_in_34961(val);
    const int x = 34961;
    std::cout << 34961;
    for (int i = 0; i < 34961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34961(int& val) {
    const int x = 34961;
    std::cout << 34961;
    for (int i = 0; i < 34961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34962(int& val) {
    func_in_34962(val);
    const int x = 34962;
    std::cout << 34962;
    for (int i = 0; i < 34962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34962(int& val) {
    const int x = 34962;
    std::cout << 34962;
    for (int i = 0; i < 34962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34963(int& val) {
    func_in_34963(val);
    const int x = 34963;
    std::cout << 34963;
    for (int i = 0; i < 34963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34963(int& val) {
    const int x = 34963;
    std::cout << 34963;
    for (int i = 0; i < 34963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34964(int& val) {
    func_in_34964(val);
    const int x = 34964;
    std::cout << 34964;
    for (int i = 0; i < 34964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34964(int& val) {
    const int x = 34964;
    std::cout << 34964;
    for (int i = 0; i < 34964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34965(int& val) {
    func_in_34965(val);
    const int x = 34965;
    std::cout << 34965;
    for (int i = 0; i < 34965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34965(int& val) {
    const int x = 34965;
    std::cout << 34965;
    for (int i = 0; i < 34965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34966(int& val) {
    func_in_34966(val);
    const int x = 34966;
    std::cout << 34966;
    for (int i = 0; i < 34966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34966(int& val) {
    const int x = 34966;
    std::cout << 34966;
    for (int i = 0; i < 34966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34967(int& val) {
    func_in_34967(val);
    const int x = 34967;
    std::cout << 34967;
    for (int i = 0; i < 34967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34967(int& val) {
    const int x = 34967;
    std::cout << 34967;
    for (int i = 0; i < 34967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34968(int& val) {
    func_in_34968(val);
    const int x = 34968;
    std::cout << 34968;
    for (int i = 0; i < 34968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34968(int& val) {
    const int x = 34968;
    std::cout << 34968;
    for (int i = 0; i < 34968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34969(int& val) {
    func_in_34969(val);
    const int x = 34969;
    std::cout << 34969;
    for (int i = 0; i < 34969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34969(int& val) {
    const int x = 34969;
    std::cout << 34969;
    for (int i = 0; i < 34969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34970(int& val) {
    func_in_34970(val);
    const int x = 34970;
    std::cout << 34970;
    for (int i = 0; i < 34970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34970(int& val) {
    const int x = 34970;
    std::cout << 34970;
    for (int i = 0; i < 34970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34971(int& val) {
    func_in_34971(val);
    const int x = 34971;
    std::cout << 34971;
    for (int i = 0; i < 34971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34971(int& val) {
    const int x = 34971;
    std::cout << 34971;
    for (int i = 0; i < 34971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34972(int& val) {
    func_in_34972(val);
    const int x = 34972;
    std::cout << 34972;
    for (int i = 0; i < 34972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34972(int& val) {
    const int x = 34972;
    std::cout << 34972;
    for (int i = 0; i < 34972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34973(int& val) {
    func_in_34973(val);
    const int x = 34973;
    std::cout << 34973;
    for (int i = 0; i < 34973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34973(int& val) {
    const int x = 34973;
    std::cout << 34973;
    for (int i = 0; i < 34973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34974(int& val) {
    func_in_34974(val);
    const int x = 34974;
    std::cout << 34974;
    for (int i = 0; i < 34974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34974(int& val) {
    const int x = 34974;
    std::cout << 34974;
    for (int i = 0; i < 34974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34975(int& val) {
    func_in_34975(val);
    const int x = 34975;
    std::cout << 34975;
    for (int i = 0; i < 34975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34975(int& val) {
    const int x = 34975;
    std::cout << 34975;
    for (int i = 0; i < 34975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34976(int& val) {
    func_in_34976(val);
    const int x = 34976;
    std::cout << 34976;
    for (int i = 0; i < 34976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34976(int& val) {
    const int x = 34976;
    std::cout << 34976;
    for (int i = 0; i < 34976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34977(int& val) {
    func_in_34977(val);
    const int x = 34977;
    std::cout << 34977;
    for (int i = 0; i < 34977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34977(int& val) {
    const int x = 34977;
    std::cout << 34977;
    for (int i = 0; i < 34977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34978(int& val) {
    func_in_34978(val);
    const int x = 34978;
    std::cout << 34978;
    for (int i = 0; i < 34978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34978(int& val) {
    const int x = 34978;
    std::cout << 34978;
    for (int i = 0; i < 34978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34979(int& val) {
    func_in_34979(val);
    const int x = 34979;
    std::cout << 34979;
    for (int i = 0; i < 34979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34979(int& val) {
    const int x = 34979;
    std::cout << 34979;
    for (int i = 0; i < 34979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34980(int& val) {
    func_in_34980(val);
    const int x = 34980;
    std::cout << 34980;
    for (int i = 0; i < 34980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34980(int& val) {
    const int x = 34980;
    std::cout << 34980;
    for (int i = 0; i < 34980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34981(int& val) {
    func_in_34981(val);
    const int x = 34981;
    std::cout << 34981;
    for (int i = 0; i < 34981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34981(int& val) {
    const int x = 34981;
    std::cout << 34981;
    for (int i = 0; i < 34981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34982(int& val) {
    func_in_34982(val);
    const int x = 34982;
    std::cout << 34982;
    for (int i = 0; i < 34982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34982(int& val) {
    const int x = 34982;
    std::cout << 34982;
    for (int i = 0; i < 34982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34983(int& val) {
    func_in_34983(val);
    const int x = 34983;
    std::cout << 34983;
    for (int i = 0; i < 34983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34983(int& val) {
    const int x = 34983;
    std::cout << 34983;
    for (int i = 0; i < 34983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34984(int& val) {
    func_in_34984(val);
    const int x = 34984;
    std::cout << 34984;
    for (int i = 0; i < 34984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34984(int& val) {
    const int x = 34984;
    std::cout << 34984;
    for (int i = 0; i < 34984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34985(int& val) {
    func_in_34985(val);
    const int x = 34985;
    std::cout << 34985;
    for (int i = 0; i < 34985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34985(int& val) {
    const int x = 34985;
    std::cout << 34985;
    for (int i = 0; i < 34985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34986(int& val) {
    func_in_34986(val);
    const int x = 34986;
    std::cout << 34986;
    for (int i = 0; i < 34986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34986(int& val) {
    const int x = 34986;
    std::cout << 34986;
    for (int i = 0; i < 34986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34987(int& val) {
    func_in_34987(val);
    const int x = 34987;
    std::cout << 34987;
    for (int i = 0; i < 34987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34987(int& val) {
    const int x = 34987;
    std::cout << 34987;
    for (int i = 0; i < 34987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34988(int& val) {
    func_in_34988(val);
    const int x = 34988;
    std::cout << 34988;
    for (int i = 0; i < 34988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34988(int& val) {
    const int x = 34988;
    std::cout << 34988;
    for (int i = 0; i < 34988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34989(int& val) {
    func_in_34989(val);
    const int x = 34989;
    std::cout << 34989;
    for (int i = 0; i < 34989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34989(int& val) {
    const int x = 34989;
    std::cout << 34989;
    for (int i = 0; i < 34989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34990(int& val) {
    func_in_34990(val);
    const int x = 34990;
    std::cout << 34990;
    for (int i = 0; i < 34990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34990(int& val) {
    const int x = 34990;
    std::cout << 34990;
    for (int i = 0; i < 34990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34991(int& val) {
    func_in_34991(val);
    const int x = 34991;
    std::cout << 34991;
    for (int i = 0; i < 34991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34991(int& val) {
    const int x = 34991;
    std::cout << 34991;
    for (int i = 0; i < 34991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34992(int& val) {
    func_in_34992(val);
    const int x = 34992;
    std::cout << 34992;
    for (int i = 0; i < 34992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34992(int& val) {
    const int x = 34992;
    std::cout << 34992;
    for (int i = 0; i < 34992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34993(int& val) {
    func_in_34993(val);
    const int x = 34993;
    std::cout << 34993;
    for (int i = 0; i < 34993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34993(int& val) {
    const int x = 34993;
    std::cout << 34993;
    for (int i = 0; i < 34993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34994(int& val) {
    func_in_34994(val);
    const int x = 34994;
    std::cout << 34994;
    for (int i = 0; i < 34994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34994(int& val) {
    const int x = 34994;
    std::cout << 34994;
    for (int i = 0; i < 34994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34995(int& val) {
    func_in_34995(val);
    const int x = 34995;
    std::cout << 34995;
    for (int i = 0; i < 34995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34995(int& val) {
    const int x = 34995;
    std::cout << 34995;
    for (int i = 0; i < 34995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34996(int& val) {
    func_in_34996(val);
    const int x = 34996;
    std::cout << 34996;
    for (int i = 0; i < 34996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34996(int& val) {
    const int x = 34996;
    std::cout << 34996;
    for (int i = 0; i < 34996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34997(int& val) {
    func_in_34997(val);
    const int x = 34997;
    std::cout << 34997;
    for (int i = 0; i < 34997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34997(int& val) {
    const int x = 34997;
    std::cout << 34997;
    for (int i = 0; i < 34997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34998(int& val) {
    func_in_34998(val);
    const int x = 34998;
    std::cout << 34998;
    for (int i = 0; i < 34998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34998(int& val) {
    const int x = 34998;
    std::cout << 34998;
    for (int i = 0; i < 34998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34999(int& val) {
    func_in_34999(val);
    const int x = 34999;
    std::cout << 34999;
    for (int i = 0; i < 34999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34999(int& val) {
    const int x = 34999;
    std::cout << 34999;
    for (int i = 0; i < 34999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35000(int& val) {
    func_in_35000(val);
    const int x = 35000;
    std::cout << 35000;
    for (int i = 0; i < 35000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35000(int& val) {
    const int x = 35000;
    std::cout << 35000;
    for (int i = 0; i < 35000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35001(int& val) {
    func_in_35001(val);
    const int x = 35001;
    std::cout << 35001;
    for (int i = 0; i < 35001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35001(int& val) {
    const int x = 35001;
    std::cout << 35001;
    for (int i = 0; i < 35001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35002(int& val) {
    func_in_35002(val);
    const int x = 35002;
    std::cout << 35002;
    for (int i = 0; i < 35002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35002(int& val) {
    const int x = 35002;
    std::cout << 35002;
    for (int i = 0; i < 35002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35003(int& val) {
    func_in_35003(val);
    const int x = 35003;
    std::cout << 35003;
    for (int i = 0; i < 35003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35003(int& val) {
    const int x = 35003;
    std::cout << 35003;
    for (int i = 0; i < 35003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35004(int& val) {
    func_in_35004(val);
    const int x = 35004;
    std::cout << 35004;
    for (int i = 0; i < 35004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35004(int& val) {
    const int x = 35004;
    std::cout << 35004;
    for (int i = 0; i < 35004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35005(int& val) {
    func_in_35005(val);
    const int x = 35005;
    std::cout << 35005;
    for (int i = 0; i < 35005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35005(int& val) {
    const int x = 35005;
    std::cout << 35005;
    for (int i = 0; i < 35005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35006(int& val) {
    func_in_35006(val);
    const int x = 35006;
    std::cout << 35006;
    for (int i = 0; i < 35006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35006(int& val) {
    const int x = 35006;
    std::cout << 35006;
    for (int i = 0; i < 35006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35007(int& val) {
    func_in_35007(val);
    const int x = 35007;
    std::cout << 35007;
    for (int i = 0; i < 35007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35007(int& val) {
    const int x = 35007;
    std::cout << 35007;
    for (int i = 0; i < 35007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35008(int& val) {
    func_in_35008(val);
    const int x = 35008;
    std::cout << 35008;
    for (int i = 0; i < 35008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35008(int& val) {
    const int x = 35008;
    std::cout << 35008;
    for (int i = 0; i < 35008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35009(int& val) {
    func_in_35009(val);
    const int x = 35009;
    std::cout << 35009;
    for (int i = 0; i < 35009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35009(int& val) {
    const int x = 35009;
    std::cout << 35009;
    for (int i = 0; i < 35009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35010(int& val) {
    func_in_35010(val);
    const int x = 35010;
    std::cout << 35010;
    for (int i = 0; i < 35010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35010(int& val) {
    const int x = 35010;
    std::cout << 35010;
    for (int i = 0; i < 35010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35011(int& val) {
    func_in_35011(val);
    const int x = 35011;
    std::cout << 35011;
    for (int i = 0; i < 35011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35011(int& val) {
    const int x = 35011;
    std::cout << 35011;
    for (int i = 0; i < 35011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35012(int& val) {
    func_in_35012(val);
    const int x = 35012;
    std::cout << 35012;
    for (int i = 0; i < 35012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35012(int& val) {
    const int x = 35012;
    std::cout << 35012;
    for (int i = 0; i < 35012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35013(int& val) {
    func_in_35013(val);
    const int x = 35013;
    std::cout << 35013;
    for (int i = 0; i < 35013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35013(int& val) {
    const int x = 35013;
    std::cout << 35013;
    for (int i = 0; i < 35013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35014(int& val) {
    func_in_35014(val);
    const int x = 35014;
    std::cout << 35014;
    for (int i = 0; i < 35014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35014(int& val) {
    const int x = 35014;
    std::cout << 35014;
    for (int i = 0; i < 35014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35015(int& val) {
    func_in_35015(val);
    const int x = 35015;
    std::cout << 35015;
    for (int i = 0; i < 35015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35015(int& val) {
    const int x = 35015;
    std::cout << 35015;
    for (int i = 0; i < 35015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35016(int& val) {
    func_in_35016(val);
    const int x = 35016;
    std::cout << 35016;
    for (int i = 0; i < 35016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35016(int& val) {
    const int x = 35016;
    std::cout << 35016;
    for (int i = 0; i < 35016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35017(int& val) {
    func_in_35017(val);
    const int x = 35017;
    std::cout << 35017;
    for (int i = 0; i < 35017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35017(int& val) {
    const int x = 35017;
    std::cout << 35017;
    for (int i = 0; i < 35017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35018(int& val) {
    func_in_35018(val);
    const int x = 35018;
    std::cout << 35018;
    for (int i = 0; i < 35018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35018(int& val) {
    const int x = 35018;
    std::cout << 35018;
    for (int i = 0; i < 35018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35019(int& val) {
    func_in_35019(val);
    const int x = 35019;
    std::cout << 35019;
    for (int i = 0; i < 35019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35019(int& val) {
    const int x = 35019;
    std::cout << 35019;
    for (int i = 0; i < 35019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35020(int& val) {
    func_in_35020(val);
    const int x = 35020;
    std::cout << 35020;
    for (int i = 0; i < 35020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35020(int& val) {
    const int x = 35020;
    std::cout << 35020;
    for (int i = 0; i < 35020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35021(int& val) {
    func_in_35021(val);
    const int x = 35021;
    std::cout << 35021;
    for (int i = 0; i < 35021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35021(int& val) {
    const int x = 35021;
    std::cout << 35021;
    for (int i = 0; i < 35021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35022(int& val) {
    func_in_35022(val);
    const int x = 35022;
    std::cout << 35022;
    for (int i = 0; i < 35022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35022(int& val) {
    const int x = 35022;
    std::cout << 35022;
    for (int i = 0; i < 35022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35023(int& val) {
    func_in_35023(val);
    const int x = 35023;
    std::cout << 35023;
    for (int i = 0; i < 35023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35023(int& val) {
    const int x = 35023;
    std::cout << 35023;
    for (int i = 0; i < 35023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35024(int& val) {
    func_in_35024(val);
    const int x = 35024;
    std::cout << 35024;
    for (int i = 0; i < 35024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35024(int& val) {
    const int x = 35024;
    std::cout << 35024;
    for (int i = 0; i < 35024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35025(int& val) {
    func_in_35025(val);
    const int x = 35025;
    std::cout << 35025;
    for (int i = 0; i < 35025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35025(int& val) {
    const int x = 35025;
    std::cout << 35025;
    for (int i = 0; i < 35025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35026(int& val) {
    func_in_35026(val);
    const int x = 35026;
    std::cout << 35026;
    for (int i = 0; i < 35026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35026(int& val) {
    const int x = 35026;
    std::cout << 35026;
    for (int i = 0; i < 35026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35027(int& val) {
    func_in_35027(val);
    const int x = 35027;
    std::cout << 35027;
    for (int i = 0; i < 35027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35027(int& val) {
    const int x = 35027;
    std::cout << 35027;
    for (int i = 0; i < 35027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35028(int& val) {
    func_in_35028(val);
    const int x = 35028;
    std::cout << 35028;
    for (int i = 0; i < 35028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35028(int& val) {
    const int x = 35028;
    std::cout << 35028;
    for (int i = 0; i < 35028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35029(int& val) {
    func_in_35029(val);
    const int x = 35029;
    std::cout << 35029;
    for (int i = 0; i < 35029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35029(int& val) {
    const int x = 35029;
    std::cout << 35029;
    for (int i = 0; i < 35029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35030(int& val) {
    func_in_35030(val);
    const int x = 35030;
    std::cout << 35030;
    for (int i = 0; i < 35030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35030(int& val) {
    const int x = 35030;
    std::cout << 35030;
    for (int i = 0; i < 35030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35031(int& val) {
    func_in_35031(val);
    const int x = 35031;
    std::cout << 35031;
    for (int i = 0; i < 35031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35031(int& val) {
    const int x = 35031;
    std::cout << 35031;
    for (int i = 0; i < 35031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35032(int& val) {
    func_in_35032(val);
    const int x = 35032;
    std::cout << 35032;
    for (int i = 0; i < 35032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35032(int& val) {
    const int x = 35032;
    std::cout << 35032;
    for (int i = 0; i < 35032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35033(int& val) {
    func_in_35033(val);
    const int x = 35033;
    std::cout << 35033;
    for (int i = 0; i < 35033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35033(int& val) {
    const int x = 35033;
    std::cout << 35033;
    for (int i = 0; i < 35033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35034(int& val) {
    func_in_35034(val);
    const int x = 35034;
    std::cout << 35034;
    for (int i = 0; i < 35034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35034(int& val) {
    const int x = 35034;
    std::cout << 35034;
    for (int i = 0; i < 35034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35035(int& val) {
    func_in_35035(val);
    const int x = 35035;
    std::cout << 35035;
    for (int i = 0; i < 35035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35035(int& val) {
    const int x = 35035;
    std::cout << 35035;
    for (int i = 0; i < 35035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35036(int& val) {
    func_in_35036(val);
    const int x = 35036;
    std::cout << 35036;
    for (int i = 0; i < 35036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35036(int& val) {
    const int x = 35036;
    std::cout << 35036;
    for (int i = 0; i < 35036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35037(int& val) {
    func_in_35037(val);
    const int x = 35037;
    std::cout << 35037;
    for (int i = 0; i < 35037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35037(int& val) {
    const int x = 35037;
    std::cout << 35037;
    for (int i = 0; i < 35037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35038(int& val) {
    func_in_35038(val);
    const int x = 35038;
    std::cout << 35038;
    for (int i = 0; i < 35038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35038(int& val) {
    const int x = 35038;
    std::cout << 35038;
    for (int i = 0; i < 35038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35039(int& val) {
    func_in_35039(val);
    const int x = 35039;
    std::cout << 35039;
    for (int i = 0; i < 35039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35039(int& val) {
    const int x = 35039;
    std::cout << 35039;
    for (int i = 0; i < 35039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35040(int& val) {
    func_in_35040(val);
    const int x = 35040;
    std::cout << 35040;
    for (int i = 0; i < 35040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35040(int& val) {
    const int x = 35040;
    std::cout << 35040;
    for (int i = 0; i < 35040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35041(int& val) {
    func_in_35041(val);
    const int x = 35041;
    std::cout << 35041;
    for (int i = 0; i < 35041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35041(int& val) {
    const int x = 35041;
    std::cout << 35041;
    for (int i = 0; i < 35041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35042(int& val) {
    func_in_35042(val);
    const int x = 35042;
    std::cout << 35042;
    for (int i = 0; i < 35042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35042(int& val) {
    const int x = 35042;
    std::cout << 35042;
    for (int i = 0; i < 35042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35043(int& val) {
    func_in_35043(val);
    const int x = 35043;
    std::cout << 35043;
    for (int i = 0; i < 35043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35043(int& val) {
    const int x = 35043;
    std::cout << 35043;
    for (int i = 0; i < 35043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35044(int& val) {
    func_in_35044(val);
    const int x = 35044;
    std::cout << 35044;
    for (int i = 0; i < 35044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35044(int& val) {
    const int x = 35044;
    std::cout << 35044;
    for (int i = 0; i < 35044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35045(int& val) {
    func_in_35045(val);
    const int x = 35045;
    std::cout << 35045;
    for (int i = 0; i < 35045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35045(int& val) {
    const int x = 35045;
    std::cout << 35045;
    for (int i = 0; i < 35045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35046(int& val) {
    func_in_35046(val);
    const int x = 35046;
    std::cout << 35046;
    for (int i = 0; i < 35046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35046(int& val) {
    const int x = 35046;
    std::cout << 35046;
    for (int i = 0; i < 35046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35047(int& val) {
    func_in_35047(val);
    const int x = 35047;
    std::cout << 35047;
    for (int i = 0; i < 35047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35047(int& val) {
    const int x = 35047;
    std::cout << 35047;
    for (int i = 0; i < 35047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35048(int& val) {
    func_in_35048(val);
    const int x = 35048;
    std::cout << 35048;
    for (int i = 0; i < 35048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35048(int& val) {
    const int x = 35048;
    std::cout << 35048;
    for (int i = 0; i < 35048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35049(int& val) {
    func_in_35049(val);
    const int x = 35049;
    std::cout << 35049;
    for (int i = 0; i < 35049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35049(int& val) {
    const int x = 35049;
    std::cout << 35049;
    for (int i = 0; i < 35049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35050(int& val) {
    func_in_35050(val);
    const int x = 35050;
    std::cout << 35050;
    for (int i = 0; i < 35050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35050(int& val) {
    const int x = 35050;
    std::cout << 35050;
    for (int i = 0; i < 35050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35051(int& val) {
    func_in_35051(val);
    const int x = 35051;
    std::cout << 35051;
    for (int i = 0; i < 35051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35051(int& val) {
    const int x = 35051;
    std::cout << 35051;
    for (int i = 0; i < 35051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35052(int& val) {
    func_in_35052(val);
    const int x = 35052;
    std::cout << 35052;
    for (int i = 0; i < 35052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35052(int& val) {
    const int x = 35052;
    std::cout << 35052;
    for (int i = 0; i < 35052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35053(int& val) {
    func_in_35053(val);
    const int x = 35053;
    std::cout << 35053;
    for (int i = 0; i < 35053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35053(int& val) {
    const int x = 35053;
    std::cout << 35053;
    for (int i = 0; i < 35053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35054(int& val) {
    func_in_35054(val);
    const int x = 35054;
    std::cout << 35054;
    for (int i = 0; i < 35054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35054(int& val) {
    const int x = 35054;
    std::cout << 35054;
    for (int i = 0; i < 35054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35055(int& val) {
    func_in_35055(val);
    const int x = 35055;
    std::cout << 35055;
    for (int i = 0; i < 35055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35055(int& val) {
    const int x = 35055;
    std::cout << 35055;
    for (int i = 0; i < 35055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35056(int& val) {
    func_in_35056(val);
    const int x = 35056;
    std::cout << 35056;
    for (int i = 0; i < 35056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35056(int& val) {
    const int x = 35056;
    std::cout << 35056;
    for (int i = 0; i < 35056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35057(int& val) {
    func_in_35057(val);
    const int x = 35057;
    std::cout << 35057;
    for (int i = 0; i < 35057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35057(int& val) {
    const int x = 35057;
    std::cout << 35057;
    for (int i = 0; i < 35057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35058(int& val) {
    func_in_35058(val);
    const int x = 35058;
    std::cout << 35058;
    for (int i = 0; i < 35058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35058(int& val) {
    const int x = 35058;
    std::cout << 35058;
    for (int i = 0; i < 35058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35059(int& val) {
    func_in_35059(val);
    const int x = 35059;
    std::cout << 35059;
    for (int i = 0; i < 35059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35059(int& val) {
    const int x = 35059;
    std::cout << 35059;
    for (int i = 0; i < 35059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35060(int& val) {
    func_in_35060(val);
    const int x = 35060;
    std::cout << 35060;
    for (int i = 0; i < 35060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35060(int& val) {
    const int x = 35060;
    std::cout << 35060;
    for (int i = 0; i < 35060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35061(int& val) {
    func_in_35061(val);
    const int x = 35061;
    std::cout << 35061;
    for (int i = 0; i < 35061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35061(int& val) {
    const int x = 35061;
    std::cout << 35061;
    for (int i = 0; i < 35061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35062(int& val) {
    func_in_35062(val);
    const int x = 35062;
    std::cout << 35062;
    for (int i = 0; i < 35062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35062(int& val) {
    const int x = 35062;
    std::cout << 35062;
    for (int i = 0; i < 35062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35063(int& val) {
    func_in_35063(val);
    const int x = 35063;
    std::cout << 35063;
    for (int i = 0; i < 35063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35063(int& val) {
    const int x = 35063;
    std::cout << 35063;
    for (int i = 0; i < 35063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35064(int& val) {
    func_in_35064(val);
    const int x = 35064;
    std::cout << 35064;
    for (int i = 0; i < 35064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35064(int& val) {
    const int x = 35064;
    std::cout << 35064;
    for (int i = 0; i < 35064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35065(int& val) {
    func_in_35065(val);
    const int x = 35065;
    std::cout << 35065;
    for (int i = 0; i < 35065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35065(int& val) {
    const int x = 35065;
    std::cout << 35065;
    for (int i = 0; i < 35065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35066(int& val) {
    func_in_35066(val);
    const int x = 35066;
    std::cout << 35066;
    for (int i = 0; i < 35066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35066(int& val) {
    const int x = 35066;
    std::cout << 35066;
    for (int i = 0; i < 35066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35067(int& val) {
    func_in_35067(val);
    const int x = 35067;
    std::cout << 35067;
    for (int i = 0; i < 35067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35067(int& val) {
    const int x = 35067;
    std::cout << 35067;
    for (int i = 0; i < 35067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35068(int& val) {
    func_in_35068(val);
    const int x = 35068;
    std::cout << 35068;
    for (int i = 0; i < 35068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35068(int& val) {
    const int x = 35068;
    std::cout << 35068;
    for (int i = 0; i < 35068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35069(int& val) {
    func_in_35069(val);
    const int x = 35069;
    std::cout << 35069;
    for (int i = 0; i < 35069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35069(int& val) {
    const int x = 35069;
    std::cout << 35069;
    for (int i = 0; i < 35069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35070(int& val) {
    func_in_35070(val);
    const int x = 35070;
    std::cout << 35070;
    for (int i = 0; i < 35070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35070(int& val) {
    const int x = 35070;
    std::cout << 35070;
    for (int i = 0; i < 35070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35071(int& val) {
    func_in_35071(val);
    const int x = 35071;
    std::cout << 35071;
    for (int i = 0; i < 35071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35071(int& val) {
    const int x = 35071;
    std::cout << 35071;
    for (int i = 0; i < 35071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35072(int& val) {
    func_in_35072(val);
    const int x = 35072;
    std::cout << 35072;
    for (int i = 0; i < 35072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35072(int& val) {
    const int x = 35072;
    std::cout << 35072;
    for (int i = 0; i < 35072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35073(int& val) {
    func_in_35073(val);
    const int x = 35073;
    std::cout << 35073;
    for (int i = 0; i < 35073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35073(int& val) {
    const int x = 35073;
    std::cout << 35073;
    for (int i = 0; i < 35073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35074(int& val) {
    func_in_35074(val);
    const int x = 35074;
    std::cout << 35074;
    for (int i = 0; i < 35074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35074(int& val) {
    const int x = 35074;
    std::cout << 35074;
    for (int i = 0; i < 35074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35075(int& val) {
    func_in_35075(val);
    const int x = 35075;
    std::cout << 35075;
    for (int i = 0; i < 35075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35075(int& val) {
    const int x = 35075;
    std::cout << 35075;
    for (int i = 0; i < 35075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35076(int& val) {
    func_in_35076(val);
    const int x = 35076;
    std::cout << 35076;
    for (int i = 0; i < 35076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35076(int& val) {
    const int x = 35076;
    std::cout << 35076;
    for (int i = 0; i < 35076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35077(int& val) {
    func_in_35077(val);
    const int x = 35077;
    std::cout << 35077;
    for (int i = 0; i < 35077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35077(int& val) {
    const int x = 35077;
    std::cout << 35077;
    for (int i = 0; i < 35077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35078(int& val) {
    func_in_35078(val);
    const int x = 35078;
    std::cout << 35078;
    for (int i = 0; i < 35078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35078(int& val) {
    const int x = 35078;
    std::cout << 35078;
    for (int i = 0; i < 35078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35079(int& val) {
    func_in_35079(val);
    const int x = 35079;
    std::cout << 35079;
    for (int i = 0; i < 35079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35079(int& val) {
    const int x = 35079;
    std::cout << 35079;
    for (int i = 0; i < 35079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35080(int& val) {
    func_in_35080(val);
    const int x = 35080;
    std::cout << 35080;
    for (int i = 0; i < 35080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35080(int& val) {
    const int x = 35080;
    std::cout << 35080;
    for (int i = 0; i < 35080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35081(int& val) {
    func_in_35081(val);
    const int x = 35081;
    std::cout << 35081;
    for (int i = 0; i < 35081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35081(int& val) {
    const int x = 35081;
    std::cout << 35081;
    for (int i = 0; i < 35081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35082(int& val) {
    func_in_35082(val);
    const int x = 35082;
    std::cout << 35082;
    for (int i = 0; i < 35082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35082(int& val) {
    const int x = 35082;
    std::cout << 35082;
    for (int i = 0; i < 35082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35083(int& val) {
    func_in_35083(val);
    const int x = 35083;
    std::cout << 35083;
    for (int i = 0; i < 35083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35083(int& val) {
    const int x = 35083;
    std::cout << 35083;
    for (int i = 0; i < 35083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35084(int& val) {
    func_in_35084(val);
    const int x = 35084;
    std::cout << 35084;
    for (int i = 0; i < 35084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35084(int& val) {
    const int x = 35084;
    std::cout << 35084;
    for (int i = 0; i < 35084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35085(int& val) {
    func_in_35085(val);
    const int x = 35085;
    std::cout << 35085;
    for (int i = 0; i < 35085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35085(int& val) {
    const int x = 35085;
    std::cout << 35085;
    for (int i = 0; i < 35085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35086(int& val) {
    func_in_35086(val);
    const int x = 35086;
    std::cout << 35086;
    for (int i = 0; i < 35086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35086(int& val) {
    const int x = 35086;
    std::cout << 35086;
    for (int i = 0; i < 35086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35087(int& val) {
    func_in_35087(val);
    const int x = 35087;
    std::cout << 35087;
    for (int i = 0; i < 35087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35087(int& val) {
    const int x = 35087;
    std::cout << 35087;
    for (int i = 0; i < 35087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35088(int& val) {
    func_in_35088(val);
    const int x = 35088;
    std::cout << 35088;
    for (int i = 0; i < 35088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35088(int& val) {
    const int x = 35088;
    std::cout << 35088;
    for (int i = 0; i < 35088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35089(int& val) {
    func_in_35089(val);
    const int x = 35089;
    std::cout << 35089;
    for (int i = 0; i < 35089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35089(int& val) {
    const int x = 35089;
    std::cout << 35089;
    for (int i = 0; i < 35089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35090(int& val) {
    func_in_35090(val);
    const int x = 35090;
    std::cout << 35090;
    for (int i = 0; i < 35090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35090(int& val) {
    const int x = 35090;
    std::cout << 35090;
    for (int i = 0; i < 35090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35091(int& val) {
    func_in_35091(val);
    const int x = 35091;
    std::cout << 35091;
    for (int i = 0; i < 35091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35091(int& val) {
    const int x = 35091;
    std::cout << 35091;
    for (int i = 0; i < 35091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35092(int& val) {
    func_in_35092(val);
    const int x = 35092;
    std::cout << 35092;
    for (int i = 0; i < 35092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35092(int& val) {
    const int x = 35092;
    std::cout << 35092;
    for (int i = 0; i < 35092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35093(int& val) {
    func_in_35093(val);
    const int x = 35093;
    std::cout << 35093;
    for (int i = 0; i < 35093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35093(int& val) {
    const int x = 35093;
    std::cout << 35093;
    for (int i = 0; i < 35093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35094(int& val) {
    func_in_35094(val);
    const int x = 35094;
    std::cout << 35094;
    for (int i = 0; i < 35094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35094(int& val) {
    const int x = 35094;
    std::cout << 35094;
    for (int i = 0; i < 35094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35095(int& val) {
    func_in_35095(val);
    const int x = 35095;
    std::cout << 35095;
    for (int i = 0; i < 35095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35095(int& val) {
    const int x = 35095;
    std::cout << 35095;
    for (int i = 0; i < 35095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35096(int& val) {
    func_in_35096(val);
    const int x = 35096;
    std::cout << 35096;
    for (int i = 0; i < 35096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35096(int& val) {
    const int x = 35096;
    std::cout << 35096;
    for (int i = 0; i < 35096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35097(int& val) {
    func_in_35097(val);
    const int x = 35097;
    std::cout << 35097;
    for (int i = 0; i < 35097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35097(int& val) {
    const int x = 35097;
    std::cout << 35097;
    for (int i = 0; i < 35097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35098(int& val) {
    func_in_35098(val);
    const int x = 35098;
    std::cout << 35098;
    for (int i = 0; i < 35098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35098(int& val) {
    const int x = 35098;
    std::cout << 35098;
    for (int i = 0; i < 35098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35099(int& val) {
    func_in_35099(val);
    const int x = 35099;
    std::cout << 35099;
    for (int i = 0; i < 35099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35099(int& val) {
    const int x = 35099;
    std::cout << 35099;
    for (int i = 0; i < 35099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35100(int& val) {
    func_in_35100(val);
    const int x = 35100;
    std::cout << 35100;
    for (int i = 0; i < 35100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35100(int& val) {
    const int x = 35100;
    std::cout << 35100;
    for (int i = 0; i < 35100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35101(int& val) {
    func_in_35101(val);
    const int x = 35101;
    std::cout << 35101;
    for (int i = 0; i < 35101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35101(int& val) {
    const int x = 35101;
    std::cout << 35101;
    for (int i = 0; i < 35101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35102(int& val) {
    func_in_35102(val);
    const int x = 35102;
    std::cout << 35102;
    for (int i = 0; i < 35102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35102(int& val) {
    const int x = 35102;
    std::cout << 35102;
    for (int i = 0; i < 35102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35103(int& val) {
    func_in_35103(val);
    const int x = 35103;
    std::cout << 35103;
    for (int i = 0; i < 35103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35103(int& val) {
    const int x = 35103;
    std::cout << 35103;
    for (int i = 0; i < 35103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35104(int& val) {
    func_in_35104(val);
    const int x = 35104;
    std::cout << 35104;
    for (int i = 0; i < 35104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35104(int& val) {
    const int x = 35104;
    std::cout << 35104;
    for (int i = 0; i < 35104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35105(int& val) {
    func_in_35105(val);
    const int x = 35105;
    std::cout << 35105;
    for (int i = 0; i < 35105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35105(int& val) {
    const int x = 35105;
    std::cout << 35105;
    for (int i = 0; i < 35105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35106(int& val) {
    func_in_35106(val);
    const int x = 35106;
    std::cout << 35106;
    for (int i = 0; i < 35106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35106(int& val) {
    const int x = 35106;
    std::cout << 35106;
    for (int i = 0; i < 35106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35107(int& val) {
    func_in_35107(val);
    const int x = 35107;
    std::cout << 35107;
    for (int i = 0; i < 35107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35107(int& val) {
    const int x = 35107;
    std::cout << 35107;
    for (int i = 0; i < 35107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35108(int& val) {
    func_in_35108(val);
    const int x = 35108;
    std::cout << 35108;
    for (int i = 0; i < 35108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35108(int& val) {
    const int x = 35108;
    std::cout << 35108;
    for (int i = 0; i < 35108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35109(int& val) {
    func_in_35109(val);
    const int x = 35109;
    std::cout << 35109;
    for (int i = 0; i < 35109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35109(int& val) {
    const int x = 35109;
    std::cout << 35109;
    for (int i = 0; i < 35109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35110(int& val) {
    func_in_35110(val);
    const int x = 35110;
    std::cout << 35110;
    for (int i = 0; i < 35110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35110(int& val) {
    const int x = 35110;
    std::cout << 35110;
    for (int i = 0; i < 35110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35111(int& val) {
    func_in_35111(val);
    const int x = 35111;
    std::cout << 35111;
    for (int i = 0; i < 35111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35111(int& val) {
    const int x = 35111;
    std::cout << 35111;
    for (int i = 0; i < 35111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35112(int& val) {
    func_in_35112(val);
    const int x = 35112;
    std::cout << 35112;
    for (int i = 0; i < 35112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35112(int& val) {
    const int x = 35112;
    std::cout << 35112;
    for (int i = 0; i < 35112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35113(int& val) {
    func_in_35113(val);
    const int x = 35113;
    std::cout << 35113;
    for (int i = 0; i < 35113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35113(int& val) {
    const int x = 35113;
    std::cout << 35113;
    for (int i = 0; i < 35113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35114(int& val) {
    func_in_35114(val);
    const int x = 35114;
    std::cout << 35114;
    for (int i = 0; i < 35114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35114(int& val) {
    const int x = 35114;
    std::cout << 35114;
    for (int i = 0; i < 35114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35115(int& val) {
    func_in_35115(val);
    const int x = 35115;
    std::cout << 35115;
    for (int i = 0; i < 35115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35115(int& val) {
    const int x = 35115;
    std::cout << 35115;
    for (int i = 0; i < 35115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35116(int& val) {
    func_in_35116(val);
    const int x = 35116;
    std::cout << 35116;
    for (int i = 0; i < 35116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35116(int& val) {
    const int x = 35116;
    std::cout << 35116;
    for (int i = 0; i < 35116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35117(int& val) {
    func_in_35117(val);
    const int x = 35117;
    std::cout << 35117;
    for (int i = 0; i < 35117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35117(int& val) {
    const int x = 35117;
    std::cout << 35117;
    for (int i = 0; i < 35117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35118(int& val) {
    func_in_35118(val);
    const int x = 35118;
    std::cout << 35118;
    for (int i = 0; i < 35118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35118(int& val) {
    const int x = 35118;
    std::cout << 35118;
    for (int i = 0; i < 35118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35119(int& val) {
    func_in_35119(val);
    const int x = 35119;
    std::cout << 35119;
    for (int i = 0; i < 35119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35119(int& val) {
    const int x = 35119;
    std::cout << 35119;
    for (int i = 0; i < 35119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35120(int& val) {
    func_in_35120(val);
    const int x = 35120;
    std::cout << 35120;
    for (int i = 0; i < 35120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35120(int& val) {
    const int x = 35120;
    std::cout << 35120;
    for (int i = 0; i < 35120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35121(int& val) {
    func_in_35121(val);
    const int x = 35121;
    std::cout << 35121;
    for (int i = 0; i < 35121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35121(int& val) {
    const int x = 35121;
    std::cout << 35121;
    for (int i = 0; i < 35121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35122(int& val) {
    func_in_35122(val);
    const int x = 35122;
    std::cout << 35122;
    for (int i = 0; i < 35122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35122(int& val) {
    const int x = 35122;
    std::cout << 35122;
    for (int i = 0; i < 35122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35123(int& val) {
    func_in_35123(val);
    const int x = 35123;
    std::cout << 35123;
    for (int i = 0; i < 35123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35123(int& val) {
    const int x = 35123;
    std::cout << 35123;
    for (int i = 0; i < 35123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35124(int& val) {
    func_in_35124(val);
    const int x = 35124;
    std::cout << 35124;
    for (int i = 0; i < 35124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35124(int& val) {
    const int x = 35124;
    std::cout << 35124;
    for (int i = 0; i < 35124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35125(int& val) {
    func_in_35125(val);
    const int x = 35125;
    std::cout << 35125;
    for (int i = 0; i < 35125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35125(int& val) {
    const int x = 35125;
    std::cout << 35125;
    for (int i = 0; i < 35125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35126(int& val) {
    func_in_35126(val);
    const int x = 35126;
    std::cout << 35126;
    for (int i = 0; i < 35126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35126(int& val) {
    const int x = 35126;
    std::cout << 35126;
    for (int i = 0; i < 35126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35127(int& val) {
    func_in_35127(val);
    const int x = 35127;
    std::cout << 35127;
    for (int i = 0; i < 35127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35127(int& val) {
    const int x = 35127;
    std::cout << 35127;
    for (int i = 0; i < 35127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35128(int& val) {
    func_in_35128(val);
    const int x = 35128;
    std::cout << 35128;
    for (int i = 0; i < 35128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35128(int& val) {
    const int x = 35128;
    std::cout << 35128;
    for (int i = 0; i < 35128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35129(int& val) {
    func_in_35129(val);
    const int x = 35129;
    std::cout << 35129;
    for (int i = 0; i < 35129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35129(int& val) {
    const int x = 35129;
    std::cout << 35129;
    for (int i = 0; i < 35129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35130(int& val) {
    func_in_35130(val);
    const int x = 35130;
    std::cout << 35130;
    for (int i = 0; i < 35130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35130(int& val) {
    const int x = 35130;
    std::cout << 35130;
    for (int i = 0; i < 35130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35131(int& val) {
    func_in_35131(val);
    const int x = 35131;
    std::cout << 35131;
    for (int i = 0; i < 35131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35131(int& val) {
    const int x = 35131;
    std::cout << 35131;
    for (int i = 0; i < 35131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35132(int& val) {
    func_in_35132(val);
    const int x = 35132;
    std::cout << 35132;
    for (int i = 0; i < 35132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35132(int& val) {
    const int x = 35132;
    std::cout << 35132;
    for (int i = 0; i < 35132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35133(int& val) {
    func_in_35133(val);
    const int x = 35133;
    std::cout << 35133;
    for (int i = 0; i < 35133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35133(int& val) {
    const int x = 35133;
    std::cout << 35133;
    for (int i = 0; i < 35133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35134(int& val) {
    func_in_35134(val);
    const int x = 35134;
    std::cout << 35134;
    for (int i = 0; i < 35134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35134(int& val) {
    const int x = 35134;
    std::cout << 35134;
    for (int i = 0; i < 35134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35135(int& val) {
    func_in_35135(val);
    const int x = 35135;
    std::cout << 35135;
    for (int i = 0; i < 35135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35135(int& val) {
    const int x = 35135;
    std::cout << 35135;
    for (int i = 0; i < 35135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35136(int& val) {
    func_in_35136(val);
    const int x = 35136;
    std::cout << 35136;
    for (int i = 0; i < 35136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35136(int& val) {
    const int x = 35136;
    std::cout << 35136;
    for (int i = 0; i < 35136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35137(int& val) {
    func_in_35137(val);
    const int x = 35137;
    std::cout << 35137;
    for (int i = 0; i < 35137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35137(int& val) {
    const int x = 35137;
    std::cout << 35137;
    for (int i = 0; i < 35137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35138(int& val) {
    func_in_35138(val);
    const int x = 35138;
    std::cout << 35138;
    for (int i = 0; i < 35138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35138(int& val) {
    const int x = 35138;
    std::cout << 35138;
    for (int i = 0; i < 35138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35139(int& val) {
    func_in_35139(val);
    const int x = 35139;
    std::cout << 35139;
    for (int i = 0; i < 35139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35139(int& val) {
    const int x = 35139;
    std::cout << 35139;
    for (int i = 0; i < 35139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35140(int& val) {
    func_in_35140(val);
    const int x = 35140;
    std::cout << 35140;
    for (int i = 0; i < 35140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35140(int& val) {
    const int x = 35140;
    std::cout << 35140;
    for (int i = 0; i < 35140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35141(int& val) {
    func_in_35141(val);
    const int x = 35141;
    std::cout << 35141;
    for (int i = 0; i < 35141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35141(int& val) {
    const int x = 35141;
    std::cout << 35141;
    for (int i = 0; i < 35141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35142(int& val) {
    func_in_35142(val);
    const int x = 35142;
    std::cout << 35142;
    for (int i = 0; i < 35142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35142(int& val) {
    const int x = 35142;
    std::cout << 35142;
    for (int i = 0; i < 35142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35143(int& val) {
    func_in_35143(val);
    const int x = 35143;
    std::cout << 35143;
    for (int i = 0; i < 35143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35143(int& val) {
    const int x = 35143;
    std::cout << 35143;
    for (int i = 0; i < 35143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35144(int& val) {
    func_in_35144(val);
    const int x = 35144;
    std::cout << 35144;
    for (int i = 0; i < 35144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35144(int& val) {
    const int x = 35144;
    std::cout << 35144;
    for (int i = 0; i < 35144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35145(int& val) {
    func_in_35145(val);
    const int x = 35145;
    std::cout << 35145;
    for (int i = 0; i < 35145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35145(int& val) {
    const int x = 35145;
    std::cout << 35145;
    for (int i = 0; i < 35145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35146(int& val) {
    func_in_35146(val);
    const int x = 35146;
    std::cout << 35146;
    for (int i = 0; i < 35146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35146(int& val) {
    const int x = 35146;
    std::cout << 35146;
    for (int i = 0; i < 35146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35147(int& val) {
    func_in_35147(val);
    const int x = 35147;
    std::cout << 35147;
    for (int i = 0; i < 35147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35147(int& val) {
    const int x = 35147;
    std::cout << 35147;
    for (int i = 0; i < 35147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35148(int& val) {
    func_in_35148(val);
    const int x = 35148;
    std::cout << 35148;
    for (int i = 0; i < 35148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35148(int& val) {
    const int x = 35148;
    std::cout << 35148;
    for (int i = 0; i < 35148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35149(int& val) {
    func_in_35149(val);
    const int x = 35149;
    std::cout << 35149;
    for (int i = 0; i < 35149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35149(int& val) {
    const int x = 35149;
    std::cout << 35149;
    for (int i = 0; i < 35149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35150(int& val) {
    func_in_35150(val);
    const int x = 35150;
    std::cout << 35150;
    for (int i = 0; i < 35150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35150(int& val) {
    const int x = 35150;
    std::cout << 35150;
    for (int i = 0; i < 35150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35151(int& val) {
    func_in_35151(val);
    const int x = 35151;
    std::cout << 35151;
    for (int i = 0; i < 35151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35151(int& val) {
    const int x = 35151;
    std::cout << 35151;
    for (int i = 0; i < 35151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35152(int& val) {
    func_in_35152(val);
    const int x = 35152;
    std::cout << 35152;
    for (int i = 0; i < 35152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35152(int& val) {
    const int x = 35152;
    std::cout << 35152;
    for (int i = 0; i < 35152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35153(int& val) {
    func_in_35153(val);
    const int x = 35153;
    std::cout << 35153;
    for (int i = 0; i < 35153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35153(int& val) {
    const int x = 35153;
    std::cout << 35153;
    for (int i = 0; i < 35153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35154(int& val) {
    func_in_35154(val);
    const int x = 35154;
    std::cout << 35154;
    for (int i = 0; i < 35154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35154(int& val) {
    const int x = 35154;
    std::cout << 35154;
    for (int i = 0; i < 35154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35155(int& val) {
    func_in_35155(val);
    const int x = 35155;
    std::cout << 35155;
    for (int i = 0; i < 35155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35155(int& val) {
    const int x = 35155;
    std::cout << 35155;
    for (int i = 0; i < 35155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35156(int& val) {
    func_in_35156(val);
    const int x = 35156;
    std::cout << 35156;
    for (int i = 0; i < 35156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35156(int& val) {
    const int x = 35156;
    std::cout << 35156;
    for (int i = 0; i < 35156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35157(int& val) {
    func_in_35157(val);
    const int x = 35157;
    std::cout << 35157;
    for (int i = 0; i < 35157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35157(int& val) {
    const int x = 35157;
    std::cout << 35157;
    for (int i = 0; i < 35157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35158(int& val) {
    func_in_35158(val);
    const int x = 35158;
    std::cout << 35158;
    for (int i = 0; i < 35158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35158(int& val) {
    const int x = 35158;
    std::cout << 35158;
    for (int i = 0; i < 35158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35159(int& val) {
    func_in_35159(val);
    const int x = 35159;
    std::cout << 35159;
    for (int i = 0; i < 35159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35159(int& val) {
    const int x = 35159;
    std::cout << 35159;
    for (int i = 0; i < 35159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35160(int& val) {
    func_in_35160(val);
    const int x = 35160;
    std::cout << 35160;
    for (int i = 0; i < 35160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35160(int& val) {
    const int x = 35160;
    std::cout << 35160;
    for (int i = 0; i < 35160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35161(int& val) {
    func_in_35161(val);
    const int x = 35161;
    std::cout << 35161;
    for (int i = 0; i < 35161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35161(int& val) {
    const int x = 35161;
    std::cout << 35161;
    for (int i = 0; i < 35161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35162(int& val) {
    func_in_35162(val);
    const int x = 35162;
    std::cout << 35162;
    for (int i = 0; i < 35162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35162(int& val) {
    const int x = 35162;
    std::cout << 35162;
    for (int i = 0; i < 35162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35163(int& val) {
    func_in_35163(val);
    const int x = 35163;
    std::cout << 35163;
    for (int i = 0; i < 35163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35163(int& val) {
    const int x = 35163;
    std::cout << 35163;
    for (int i = 0; i < 35163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35164(int& val) {
    func_in_35164(val);
    const int x = 35164;
    std::cout << 35164;
    for (int i = 0; i < 35164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35164(int& val) {
    const int x = 35164;
    std::cout << 35164;
    for (int i = 0; i < 35164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35165(int& val) {
    func_in_35165(val);
    const int x = 35165;
    std::cout << 35165;
    for (int i = 0; i < 35165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35165(int& val) {
    const int x = 35165;
    std::cout << 35165;
    for (int i = 0; i < 35165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35166(int& val) {
    func_in_35166(val);
    const int x = 35166;
    std::cout << 35166;
    for (int i = 0; i < 35166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35166(int& val) {
    const int x = 35166;
    std::cout << 35166;
    for (int i = 0; i < 35166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35167(int& val) {
    func_in_35167(val);
    const int x = 35167;
    std::cout << 35167;
    for (int i = 0; i < 35167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35167(int& val) {
    const int x = 35167;
    std::cout << 35167;
    for (int i = 0; i < 35167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35168(int& val) {
    func_in_35168(val);
    const int x = 35168;
    std::cout << 35168;
    for (int i = 0; i < 35168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35168(int& val) {
    const int x = 35168;
    std::cout << 35168;
    for (int i = 0; i < 35168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35169(int& val) {
    func_in_35169(val);
    const int x = 35169;
    std::cout << 35169;
    for (int i = 0; i < 35169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35169(int& val) {
    const int x = 35169;
    std::cout << 35169;
    for (int i = 0; i < 35169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35170(int& val) {
    func_in_35170(val);
    const int x = 35170;
    std::cout << 35170;
    for (int i = 0; i < 35170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35170(int& val) {
    const int x = 35170;
    std::cout << 35170;
    for (int i = 0; i < 35170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35171(int& val) {
    func_in_35171(val);
    const int x = 35171;
    std::cout << 35171;
    for (int i = 0; i < 35171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35171(int& val) {
    const int x = 35171;
    std::cout << 35171;
    for (int i = 0; i < 35171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35172(int& val) {
    func_in_35172(val);
    const int x = 35172;
    std::cout << 35172;
    for (int i = 0; i < 35172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35172(int& val) {
    const int x = 35172;
    std::cout << 35172;
    for (int i = 0; i < 35172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35173(int& val) {
    func_in_35173(val);
    const int x = 35173;
    std::cout << 35173;
    for (int i = 0; i < 35173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35173(int& val) {
    const int x = 35173;
    std::cout << 35173;
    for (int i = 0; i < 35173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35174(int& val) {
    func_in_35174(val);
    const int x = 35174;
    std::cout << 35174;
    for (int i = 0; i < 35174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35174(int& val) {
    const int x = 35174;
    std::cout << 35174;
    for (int i = 0; i < 35174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35175(int& val) {
    func_in_35175(val);
    const int x = 35175;
    std::cout << 35175;
    for (int i = 0; i < 35175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35175(int& val) {
    const int x = 35175;
    std::cout << 35175;
    for (int i = 0; i < 35175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35176(int& val) {
    func_in_35176(val);
    const int x = 35176;
    std::cout << 35176;
    for (int i = 0; i < 35176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35176(int& val) {
    const int x = 35176;
    std::cout << 35176;
    for (int i = 0; i < 35176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35177(int& val) {
    func_in_35177(val);
    const int x = 35177;
    std::cout << 35177;
    for (int i = 0; i < 35177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35177(int& val) {
    const int x = 35177;
    std::cout << 35177;
    for (int i = 0; i < 35177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35178(int& val) {
    func_in_35178(val);
    const int x = 35178;
    std::cout << 35178;
    for (int i = 0; i < 35178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35178(int& val) {
    const int x = 35178;
    std::cout << 35178;
    for (int i = 0; i < 35178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35179(int& val) {
    func_in_35179(val);
    const int x = 35179;
    std::cout << 35179;
    for (int i = 0; i < 35179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35179(int& val) {
    const int x = 35179;
    std::cout << 35179;
    for (int i = 0; i < 35179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35180(int& val) {
    func_in_35180(val);
    const int x = 35180;
    std::cout << 35180;
    for (int i = 0; i < 35180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35180(int& val) {
    const int x = 35180;
    std::cout << 35180;
    for (int i = 0; i < 35180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35181(int& val) {
    func_in_35181(val);
    const int x = 35181;
    std::cout << 35181;
    for (int i = 0; i < 35181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35181(int& val) {
    const int x = 35181;
    std::cout << 35181;
    for (int i = 0; i < 35181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35182(int& val) {
    func_in_35182(val);
    const int x = 35182;
    std::cout << 35182;
    for (int i = 0; i < 35182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35182(int& val) {
    const int x = 35182;
    std::cout << 35182;
    for (int i = 0; i < 35182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35183(int& val) {
    func_in_35183(val);
    const int x = 35183;
    std::cout << 35183;
    for (int i = 0; i < 35183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35183(int& val) {
    const int x = 35183;
    std::cout << 35183;
    for (int i = 0; i < 35183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35184(int& val) {
    func_in_35184(val);
    const int x = 35184;
    std::cout << 35184;
    for (int i = 0; i < 35184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35184(int& val) {
    const int x = 35184;
    std::cout << 35184;
    for (int i = 0; i < 35184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35185(int& val) {
    func_in_35185(val);
    const int x = 35185;
    std::cout << 35185;
    for (int i = 0; i < 35185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35185(int& val) {
    const int x = 35185;
    std::cout << 35185;
    for (int i = 0; i < 35185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35186(int& val) {
    func_in_35186(val);
    const int x = 35186;
    std::cout << 35186;
    for (int i = 0; i < 35186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35186(int& val) {
    const int x = 35186;
    std::cout << 35186;
    for (int i = 0; i < 35186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35187(int& val) {
    func_in_35187(val);
    const int x = 35187;
    std::cout << 35187;
    for (int i = 0; i < 35187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35187(int& val) {
    const int x = 35187;
    std::cout << 35187;
    for (int i = 0; i < 35187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35188(int& val) {
    func_in_35188(val);
    const int x = 35188;
    std::cout << 35188;
    for (int i = 0; i < 35188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35188(int& val) {
    const int x = 35188;
    std::cout << 35188;
    for (int i = 0; i < 35188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35189(int& val) {
    func_in_35189(val);
    const int x = 35189;
    std::cout << 35189;
    for (int i = 0; i < 35189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35189(int& val) {
    const int x = 35189;
    std::cout << 35189;
    for (int i = 0; i < 35189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35190(int& val) {
    func_in_35190(val);
    const int x = 35190;
    std::cout << 35190;
    for (int i = 0; i < 35190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35190(int& val) {
    const int x = 35190;
    std::cout << 35190;
    for (int i = 0; i < 35190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35191(int& val) {
    func_in_35191(val);
    const int x = 35191;
    std::cout << 35191;
    for (int i = 0; i < 35191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35191(int& val) {
    const int x = 35191;
    std::cout << 35191;
    for (int i = 0; i < 35191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35192(int& val) {
    func_in_35192(val);
    const int x = 35192;
    std::cout << 35192;
    for (int i = 0; i < 35192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35192(int& val) {
    const int x = 35192;
    std::cout << 35192;
    for (int i = 0; i < 35192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35193(int& val) {
    func_in_35193(val);
    const int x = 35193;
    std::cout << 35193;
    for (int i = 0; i < 35193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35193(int& val) {
    const int x = 35193;
    std::cout << 35193;
    for (int i = 0; i < 35193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35194(int& val) {
    func_in_35194(val);
    const int x = 35194;
    std::cout << 35194;
    for (int i = 0; i < 35194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35194(int& val) {
    const int x = 35194;
    std::cout << 35194;
    for (int i = 0; i < 35194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35195(int& val) {
    func_in_35195(val);
    const int x = 35195;
    std::cout << 35195;
    for (int i = 0; i < 35195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35195(int& val) {
    const int x = 35195;
    std::cout << 35195;
    for (int i = 0; i < 35195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35196(int& val) {
    func_in_35196(val);
    const int x = 35196;
    std::cout << 35196;
    for (int i = 0; i < 35196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35196(int& val) {
    const int x = 35196;
    std::cout << 35196;
    for (int i = 0; i < 35196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35197(int& val) {
    func_in_35197(val);
    const int x = 35197;
    std::cout << 35197;
    for (int i = 0; i < 35197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35197(int& val) {
    const int x = 35197;
    std::cout << 35197;
    for (int i = 0; i < 35197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35198(int& val) {
    func_in_35198(val);
    const int x = 35198;
    std::cout << 35198;
    for (int i = 0; i < 35198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35198(int& val) {
    const int x = 35198;
    std::cout << 35198;
    for (int i = 0; i < 35198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35199(int& val) {
    func_in_35199(val);
    const int x = 35199;
    std::cout << 35199;
    for (int i = 0; i < 35199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35199(int& val) {
    const int x = 35199;
    std::cout << 35199;
    for (int i = 0; i < 35199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35200(int& val) {
    func_in_35200(val);
    const int x = 35200;
    std::cout << 35200;
    for (int i = 0; i < 35200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35200(int& val) {
    const int x = 35200;
    std::cout << 35200;
    for (int i = 0; i < 35200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35201(int& val) {
    func_in_35201(val);
    const int x = 35201;
    std::cout << 35201;
    for (int i = 0; i < 35201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35201(int& val) {
    const int x = 35201;
    std::cout << 35201;
    for (int i = 0; i < 35201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35202(int& val) {
    func_in_35202(val);
    const int x = 35202;
    std::cout << 35202;
    for (int i = 0; i < 35202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35202(int& val) {
    const int x = 35202;
    std::cout << 35202;
    for (int i = 0; i < 35202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35203(int& val) {
    func_in_35203(val);
    const int x = 35203;
    std::cout << 35203;
    for (int i = 0; i < 35203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35203(int& val) {
    const int x = 35203;
    std::cout << 35203;
    for (int i = 0; i < 35203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35204(int& val) {
    func_in_35204(val);
    const int x = 35204;
    std::cout << 35204;
    for (int i = 0; i < 35204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35204(int& val) {
    const int x = 35204;
    std::cout << 35204;
    for (int i = 0; i < 35204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35205(int& val) {
    func_in_35205(val);
    const int x = 35205;
    std::cout << 35205;
    for (int i = 0; i < 35205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35205(int& val) {
    const int x = 35205;
    std::cout << 35205;
    for (int i = 0; i < 35205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35206(int& val) {
    func_in_35206(val);
    const int x = 35206;
    std::cout << 35206;
    for (int i = 0; i < 35206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35206(int& val) {
    const int x = 35206;
    std::cout << 35206;
    for (int i = 0; i < 35206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35207(int& val) {
    func_in_35207(val);
    const int x = 35207;
    std::cout << 35207;
    for (int i = 0; i < 35207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35207(int& val) {
    const int x = 35207;
    std::cout << 35207;
    for (int i = 0; i < 35207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35208(int& val) {
    func_in_35208(val);
    const int x = 35208;
    std::cout << 35208;
    for (int i = 0; i < 35208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35208(int& val) {
    const int x = 35208;
    std::cout << 35208;
    for (int i = 0; i < 35208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35209(int& val) {
    func_in_35209(val);
    const int x = 35209;
    std::cout << 35209;
    for (int i = 0; i < 35209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35209(int& val) {
    const int x = 35209;
    std::cout << 35209;
    for (int i = 0; i < 35209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35210(int& val) {
    func_in_35210(val);
    const int x = 35210;
    std::cout << 35210;
    for (int i = 0; i < 35210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35210(int& val) {
    const int x = 35210;
    std::cout << 35210;
    for (int i = 0; i < 35210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35211(int& val) {
    func_in_35211(val);
    const int x = 35211;
    std::cout << 35211;
    for (int i = 0; i < 35211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35211(int& val) {
    const int x = 35211;
    std::cout << 35211;
    for (int i = 0; i < 35211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35212(int& val) {
    func_in_35212(val);
    const int x = 35212;
    std::cout << 35212;
    for (int i = 0; i < 35212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35212(int& val) {
    const int x = 35212;
    std::cout << 35212;
    for (int i = 0; i < 35212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35213(int& val) {
    func_in_35213(val);
    const int x = 35213;
    std::cout << 35213;
    for (int i = 0; i < 35213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35213(int& val) {
    const int x = 35213;
    std::cout << 35213;
    for (int i = 0; i < 35213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35214(int& val) {
    func_in_35214(val);
    const int x = 35214;
    std::cout << 35214;
    for (int i = 0; i < 35214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35214(int& val) {
    const int x = 35214;
    std::cout << 35214;
    for (int i = 0; i < 35214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35215(int& val) {
    func_in_35215(val);
    const int x = 35215;
    std::cout << 35215;
    for (int i = 0; i < 35215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35215(int& val) {
    const int x = 35215;
    std::cout << 35215;
    for (int i = 0; i < 35215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35216(int& val) {
    func_in_35216(val);
    const int x = 35216;
    std::cout << 35216;
    for (int i = 0; i < 35216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35216(int& val) {
    const int x = 35216;
    std::cout << 35216;
    for (int i = 0; i < 35216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35217(int& val) {
    func_in_35217(val);
    const int x = 35217;
    std::cout << 35217;
    for (int i = 0; i < 35217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35217(int& val) {
    const int x = 35217;
    std::cout << 35217;
    for (int i = 0; i < 35217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35218(int& val) {
    func_in_35218(val);
    const int x = 35218;
    std::cout << 35218;
    for (int i = 0; i < 35218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35218(int& val) {
    const int x = 35218;
    std::cout << 35218;
    for (int i = 0; i < 35218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35219(int& val) {
    func_in_35219(val);
    const int x = 35219;
    std::cout << 35219;
    for (int i = 0; i < 35219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35219(int& val) {
    const int x = 35219;
    std::cout << 35219;
    for (int i = 0; i < 35219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35220(int& val) {
    func_in_35220(val);
    const int x = 35220;
    std::cout << 35220;
    for (int i = 0; i < 35220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35220(int& val) {
    const int x = 35220;
    std::cout << 35220;
    for (int i = 0; i < 35220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35221(int& val) {
    func_in_35221(val);
    const int x = 35221;
    std::cout << 35221;
    for (int i = 0; i < 35221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35221(int& val) {
    const int x = 35221;
    std::cout << 35221;
    for (int i = 0; i < 35221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35222(int& val) {
    func_in_35222(val);
    const int x = 35222;
    std::cout << 35222;
    for (int i = 0; i < 35222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35222(int& val) {
    const int x = 35222;
    std::cout << 35222;
    for (int i = 0; i < 35222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35223(int& val) {
    func_in_35223(val);
    const int x = 35223;
    std::cout << 35223;
    for (int i = 0; i < 35223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35223(int& val) {
    const int x = 35223;
    std::cout << 35223;
    for (int i = 0; i < 35223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35224(int& val) {
    func_in_35224(val);
    const int x = 35224;
    std::cout << 35224;
    for (int i = 0; i < 35224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35224(int& val) {
    const int x = 35224;
    std::cout << 35224;
    for (int i = 0; i < 35224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35225(int& val) {
    func_in_35225(val);
    const int x = 35225;
    std::cout << 35225;
    for (int i = 0; i < 35225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35225(int& val) {
    const int x = 35225;
    std::cout << 35225;
    for (int i = 0; i < 35225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35226(int& val) {
    func_in_35226(val);
    const int x = 35226;
    std::cout << 35226;
    for (int i = 0; i < 35226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35226(int& val) {
    const int x = 35226;
    std::cout << 35226;
    for (int i = 0; i < 35226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35227(int& val) {
    func_in_35227(val);
    const int x = 35227;
    std::cout << 35227;
    for (int i = 0; i < 35227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35227(int& val) {
    const int x = 35227;
    std::cout << 35227;
    for (int i = 0; i < 35227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35228(int& val) {
    func_in_35228(val);
    const int x = 35228;
    std::cout << 35228;
    for (int i = 0; i < 35228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35228(int& val) {
    const int x = 35228;
    std::cout << 35228;
    for (int i = 0; i < 35228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35229(int& val) {
    func_in_35229(val);
    const int x = 35229;
    std::cout << 35229;
    for (int i = 0; i < 35229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35229(int& val) {
    const int x = 35229;
    std::cout << 35229;
    for (int i = 0; i < 35229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35230(int& val) {
    func_in_35230(val);
    const int x = 35230;
    std::cout << 35230;
    for (int i = 0; i < 35230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35230(int& val) {
    const int x = 35230;
    std::cout << 35230;
    for (int i = 0; i < 35230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35231(int& val) {
    func_in_35231(val);
    const int x = 35231;
    std::cout << 35231;
    for (int i = 0; i < 35231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35231(int& val) {
    const int x = 35231;
    std::cout << 35231;
    for (int i = 0; i < 35231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35232(int& val) {
    func_in_35232(val);
    const int x = 35232;
    std::cout << 35232;
    for (int i = 0; i < 35232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35232(int& val) {
    const int x = 35232;
    std::cout << 35232;
    for (int i = 0; i < 35232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35233(int& val) {
    func_in_35233(val);
    const int x = 35233;
    std::cout << 35233;
    for (int i = 0; i < 35233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35233(int& val) {
    const int x = 35233;
    std::cout << 35233;
    for (int i = 0; i < 35233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35234(int& val) {
    func_in_35234(val);
    const int x = 35234;
    std::cout << 35234;
    for (int i = 0; i < 35234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35234(int& val) {
    const int x = 35234;
    std::cout << 35234;
    for (int i = 0; i < 35234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35235(int& val) {
    func_in_35235(val);
    const int x = 35235;
    std::cout << 35235;
    for (int i = 0; i < 35235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35235(int& val) {
    const int x = 35235;
    std::cout << 35235;
    for (int i = 0; i < 35235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35236(int& val) {
    func_in_35236(val);
    const int x = 35236;
    std::cout << 35236;
    for (int i = 0; i < 35236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35236(int& val) {
    const int x = 35236;
    std::cout << 35236;
    for (int i = 0; i < 35236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35237(int& val) {
    func_in_35237(val);
    const int x = 35237;
    std::cout << 35237;
    for (int i = 0; i < 35237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35237(int& val) {
    const int x = 35237;
    std::cout << 35237;
    for (int i = 0; i < 35237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35238(int& val) {
    func_in_35238(val);
    const int x = 35238;
    std::cout << 35238;
    for (int i = 0; i < 35238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35238(int& val) {
    const int x = 35238;
    std::cout << 35238;
    for (int i = 0; i < 35238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35239(int& val) {
    func_in_35239(val);
    const int x = 35239;
    std::cout << 35239;
    for (int i = 0; i < 35239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35239(int& val) {
    const int x = 35239;
    std::cout << 35239;
    for (int i = 0; i < 35239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35240(int& val) {
    func_in_35240(val);
    const int x = 35240;
    std::cout << 35240;
    for (int i = 0; i < 35240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35240(int& val) {
    const int x = 35240;
    std::cout << 35240;
    for (int i = 0; i < 35240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35241(int& val) {
    func_in_35241(val);
    const int x = 35241;
    std::cout << 35241;
    for (int i = 0; i < 35241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35241(int& val) {
    const int x = 35241;
    std::cout << 35241;
    for (int i = 0; i < 35241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35242(int& val) {
    func_in_35242(val);
    const int x = 35242;
    std::cout << 35242;
    for (int i = 0; i < 35242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35242(int& val) {
    const int x = 35242;
    std::cout << 35242;
    for (int i = 0; i < 35242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35243(int& val) {
    func_in_35243(val);
    const int x = 35243;
    std::cout << 35243;
    for (int i = 0; i < 35243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35243(int& val) {
    const int x = 35243;
    std::cout << 35243;
    for (int i = 0; i < 35243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35244(int& val) {
    func_in_35244(val);
    const int x = 35244;
    std::cout << 35244;
    for (int i = 0; i < 35244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35244(int& val) {
    const int x = 35244;
    std::cout << 35244;
    for (int i = 0; i < 35244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35245(int& val) {
    func_in_35245(val);
    const int x = 35245;
    std::cout << 35245;
    for (int i = 0; i < 35245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35245(int& val) {
    const int x = 35245;
    std::cout << 35245;
    for (int i = 0; i < 35245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35246(int& val) {
    func_in_35246(val);
    const int x = 35246;
    std::cout << 35246;
    for (int i = 0; i < 35246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35246(int& val) {
    const int x = 35246;
    std::cout << 35246;
    for (int i = 0; i < 35246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35247(int& val) {
    func_in_35247(val);
    const int x = 35247;
    std::cout << 35247;
    for (int i = 0; i < 35247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35247(int& val) {
    const int x = 35247;
    std::cout << 35247;
    for (int i = 0; i < 35247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35248(int& val) {
    func_in_35248(val);
    const int x = 35248;
    std::cout << 35248;
    for (int i = 0; i < 35248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35248(int& val) {
    const int x = 35248;
    std::cout << 35248;
    for (int i = 0; i < 35248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35249(int& val) {
    func_in_35249(val);
    const int x = 35249;
    std::cout << 35249;
    for (int i = 0; i < 35249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35249(int& val) {
    const int x = 35249;
    std::cout << 35249;
    for (int i = 0; i < 35249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35250(int& val) {
    func_in_35250(val);
    const int x = 35250;
    std::cout << 35250;
    for (int i = 0; i < 35250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35250(int& val) {
    const int x = 35250;
    std::cout << 35250;
    for (int i = 0; i < 35250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35251(int& val) {
    func_in_35251(val);
    const int x = 35251;
    std::cout << 35251;
    for (int i = 0; i < 35251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35251(int& val) {
    const int x = 35251;
    std::cout << 35251;
    for (int i = 0; i < 35251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35252(int& val) {
    func_in_35252(val);
    const int x = 35252;
    std::cout << 35252;
    for (int i = 0; i < 35252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35252(int& val) {
    const int x = 35252;
    std::cout << 35252;
    for (int i = 0; i < 35252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35253(int& val) {
    func_in_35253(val);
    const int x = 35253;
    std::cout << 35253;
    for (int i = 0; i < 35253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35253(int& val) {
    const int x = 35253;
    std::cout << 35253;
    for (int i = 0; i < 35253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35254(int& val) {
    func_in_35254(val);
    const int x = 35254;
    std::cout << 35254;
    for (int i = 0; i < 35254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35254(int& val) {
    const int x = 35254;
    std::cout << 35254;
    for (int i = 0; i < 35254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35255(int& val) {
    func_in_35255(val);
    const int x = 35255;
    std::cout << 35255;
    for (int i = 0; i < 35255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35255(int& val) {
    const int x = 35255;
    std::cout << 35255;
    for (int i = 0; i < 35255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35256(int& val) {
    func_in_35256(val);
    const int x = 35256;
    std::cout << 35256;
    for (int i = 0; i < 35256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35256(int& val) {
    const int x = 35256;
    std::cout << 35256;
    for (int i = 0; i < 35256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35257(int& val) {
    func_in_35257(val);
    const int x = 35257;
    std::cout << 35257;
    for (int i = 0; i < 35257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35257(int& val) {
    const int x = 35257;
    std::cout << 35257;
    for (int i = 0; i < 35257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35258(int& val) {
    func_in_35258(val);
    const int x = 35258;
    std::cout << 35258;
    for (int i = 0; i < 35258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35258(int& val) {
    const int x = 35258;
    std::cout << 35258;
    for (int i = 0; i < 35258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35259(int& val) {
    func_in_35259(val);
    const int x = 35259;
    std::cout << 35259;
    for (int i = 0; i < 35259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35259(int& val) {
    const int x = 35259;
    std::cout << 35259;
    for (int i = 0; i < 35259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35260(int& val) {
    func_in_35260(val);
    const int x = 35260;
    std::cout << 35260;
    for (int i = 0; i < 35260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35260(int& val) {
    const int x = 35260;
    std::cout << 35260;
    for (int i = 0; i < 35260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35261(int& val) {
    func_in_35261(val);
    const int x = 35261;
    std::cout << 35261;
    for (int i = 0; i < 35261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35261(int& val) {
    const int x = 35261;
    std::cout << 35261;
    for (int i = 0; i < 35261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35262(int& val) {
    func_in_35262(val);
    const int x = 35262;
    std::cout << 35262;
    for (int i = 0; i < 35262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35262(int& val) {
    const int x = 35262;
    std::cout << 35262;
    for (int i = 0; i < 35262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35263(int& val) {
    func_in_35263(val);
    const int x = 35263;
    std::cout << 35263;
    for (int i = 0; i < 35263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35263(int& val) {
    const int x = 35263;
    std::cout << 35263;
    for (int i = 0; i < 35263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35264(int& val) {
    func_in_35264(val);
    const int x = 35264;
    std::cout << 35264;
    for (int i = 0; i < 35264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35264(int& val) {
    const int x = 35264;
    std::cout << 35264;
    for (int i = 0; i < 35264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35265(int& val) {
    func_in_35265(val);
    const int x = 35265;
    std::cout << 35265;
    for (int i = 0; i < 35265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35265(int& val) {
    const int x = 35265;
    std::cout << 35265;
    for (int i = 0; i < 35265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35266(int& val) {
    func_in_35266(val);
    const int x = 35266;
    std::cout << 35266;
    for (int i = 0; i < 35266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35266(int& val) {
    const int x = 35266;
    std::cout << 35266;
    for (int i = 0; i < 35266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35267(int& val) {
    func_in_35267(val);
    const int x = 35267;
    std::cout << 35267;
    for (int i = 0; i < 35267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35267(int& val) {
    const int x = 35267;
    std::cout << 35267;
    for (int i = 0; i < 35267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35268(int& val) {
    func_in_35268(val);
    const int x = 35268;
    std::cout << 35268;
    for (int i = 0; i < 35268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35268(int& val) {
    const int x = 35268;
    std::cout << 35268;
    for (int i = 0; i < 35268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35269(int& val) {
    func_in_35269(val);
    const int x = 35269;
    std::cout << 35269;
    for (int i = 0; i < 35269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35269(int& val) {
    const int x = 35269;
    std::cout << 35269;
    for (int i = 0; i < 35269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35270(int& val) {
    func_in_35270(val);
    const int x = 35270;
    std::cout << 35270;
    for (int i = 0; i < 35270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35270(int& val) {
    const int x = 35270;
    std::cout << 35270;
    for (int i = 0; i < 35270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35271(int& val) {
    func_in_35271(val);
    const int x = 35271;
    std::cout << 35271;
    for (int i = 0; i < 35271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35271(int& val) {
    const int x = 35271;
    std::cout << 35271;
    for (int i = 0; i < 35271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35272(int& val) {
    func_in_35272(val);
    const int x = 35272;
    std::cout << 35272;
    for (int i = 0; i < 35272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35272(int& val) {
    const int x = 35272;
    std::cout << 35272;
    for (int i = 0; i < 35272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35273(int& val) {
    func_in_35273(val);
    const int x = 35273;
    std::cout << 35273;
    for (int i = 0; i < 35273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35273(int& val) {
    const int x = 35273;
    std::cout << 35273;
    for (int i = 0; i < 35273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35274(int& val) {
    func_in_35274(val);
    const int x = 35274;
    std::cout << 35274;
    for (int i = 0; i < 35274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35274(int& val) {
    const int x = 35274;
    std::cout << 35274;
    for (int i = 0; i < 35274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35275(int& val) {
    func_in_35275(val);
    const int x = 35275;
    std::cout << 35275;
    for (int i = 0; i < 35275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35275(int& val) {
    const int x = 35275;
    std::cout << 35275;
    for (int i = 0; i < 35275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35276(int& val) {
    func_in_35276(val);
    const int x = 35276;
    std::cout << 35276;
    for (int i = 0; i < 35276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35276(int& val) {
    const int x = 35276;
    std::cout << 35276;
    for (int i = 0; i < 35276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35277(int& val) {
    func_in_35277(val);
    const int x = 35277;
    std::cout << 35277;
    for (int i = 0; i < 35277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35277(int& val) {
    const int x = 35277;
    std::cout << 35277;
    for (int i = 0; i < 35277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35278(int& val) {
    func_in_35278(val);
    const int x = 35278;
    std::cout << 35278;
    for (int i = 0; i < 35278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35278(int& val) {
    const int x = 35278;
    std::cout << 35278;
    for (int i = 0; i < 35278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35279(int& val) {
    func_in_35279(val);
    const int x = 35279;
    std::cout << 35279;
    for (int i = 0; i < 35279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35279(int& val) {
    const int x = 35279;
    std::cout << 35279;
    for (int i = 0; i < 35279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35280(int& val) {
    func_in_35280(val);
    const int x = 35280;
    std::cout << 35280;
    for (int i = 0; i < 35280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35280(int& val) {
    const int x = 35280;
    std::cout << 35280;
    for (int i = 0; i < 35280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35281(int& val) {
    func_in_35281(val);
    const int x = 35281;
    std::cout << 35281;
    for (int i = 0; i < 35281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35281(int& val) {
    const int x = 35281;
    std::cout << 35281;
    for (int i = 0; i < 35281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35282(int& val) {
    func_in_35282(val);
    const int x = 35282;
    std::cout << 35282;
    for (int i = 0; i < 35282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35282(int& val) {
    const int x = 35282;
    std::cout << 35282;
    for (int i = 0; i < 35282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35283(int& val) {
    func_in_35283(val);
    const int x = 35283;
    std::cout << 35283;
    for (int i = 0; i < 35283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35283(int& val) {
    const int x = 35283;
    std::cout << 35283;
    for (int i = 0; i < 35283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35284(int& val) {
    func_in_35284(val);
    const int x = 35284;
    std::cout << 35284;
    for (int i = 0; i < 35284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35284(int& val) {
    const int x = 35284;
    std::cout << 35284;
    for (int i = 0; i < 35284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35285(int& val) {
    func_in_35285(val);
    const int x = 35285;
    std::cout << 35285;
    for (int i = 0; i < 35285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35285(int& val) {
    const int x = 35285;
    std::cout << 35285;
    for (int i = 0; i < 35285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35286(int& val) {
    func_in_35286(val);
    const int x = 35286;
    std::cout << 35286;
    for (int i = 0; i < 35286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35286(int& val) {
    const int x = 35286;
    std::cout << 35286;
    for (int i = 0; i < 35286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35287(int& val) {
    func_in_35287(val);
    const int x = 35287;
    std::cout << 35287;
    for (int i = 0; i < 35287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35287(int& val) {
    const int x = 35287;
    std::cout << 35287;
    for (int i = 0; i < 35287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35288(int& val) {
    func_in_35288(val);
    const int x = 35288;
    std::cout << 35288;
    for (int i = 0; i < 35288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35288(int& val) {
    const int x = 35288;
    std::cout << 35288;
    for (int i = 0; i < 35288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35289(int& val) {
    func_in_35289(val);
    const int x = 35289;
    std::cout << 35289;
    for (int i = 0; i < 35289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35289(int& val) {
    const int x = 35289;
    std::cout << 35289;
    for (int i = 0; i < 35289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35290(int& val) {
    func_in_35290(val);
    const int x = 35290;
    std::cout << 35290;
    for (int i = 0; i < 35290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35290(int& val) {
    const int x = 35290;
    std::cout << 35290;
    for (int i = 0; i < 35290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35291(int& val) {
    func_in_35291(val);
    const int x = 35291;
    std::cout << 35291;
    for (int i = 0; i < 35291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35291(int& val) {
    const int x = 35291;
    std::cout << 35291;
    for (int i = 0; i < 35291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35292(int& val) {
    func_in_35292(val);
    const int x = 35292;
    std::cout << 35292;
    for (int i = 0; i < 35292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35292(int& val) {
    const int x = 35292;
    std::cout << 35292;
    for (int i = 0; i < 35292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35293(int& val) {
    func_in_35293(val);
    const int x = 35293;
    std::cout << 35293;
    for (int i = 0; i < 35293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35293(int& val) {
    const int x = 35293;
    std::cout << 35293;
    for (int i = 0; i < 35293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35294(int& val) {
    func_in_35294(val);
    const int x = 35294;
    std::cout << 35294;
    for (int i = 0; i < 35294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35294(int& val) {
    const int x = 35294;
    std::cout << 35294;
    for (int i = 0; i < 35294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35295(int& val) {
    func_in_35295(val);
    const int x = 35295;
    std::cout << 35295;
    for (int i = 0; i < 35295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35295(int& val) {
    const int x = 35295;
    std::cout << 35295;
    for (int i = 0; i < 35295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35296(int& val) {
    func_in_35296(val);
    const int x = 35296;
    std::cout << 35296;
    for (int i = 0; i < 35296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35296(int& val) {
    const int x = 35296;
    std::cout << 35296;
    for (int i = 0; i < 35296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35297(int& val) {
    func_in_35297(val);
    const int x = 35297;
    std::cout << 35297;
    for (int i = 0; i < 35297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35297(int& val) {
    const int x = 35297;
    std::cout << 35297;
    for (int i = 0; i < 35297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35298(int& val) {
    func_in_35298(val);
    const int x = 35298;
    std::cout << 35298;
    for (int i = 0; i < 35298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35298(int& val) {
    const int x = 35298;
    std::cout << 35298;
    for (int i = 0; i < 35298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35299(int& val) {
    func_in_35299(val);
    const int x = 35299;
    std::cout << 35299;
    for (int i = 0; i < 35299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35299(int& val) {
    const int x = 35299;
    std::cout << 35299;
    for (int i = 0; i < 35299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35300(int& val) {
    func_in_35300(val);
    const int x = 35300;
    std::cout << 35300;
    for (int i = 0; i < 35300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35300(int& val) {
    const int x = 35300;
    std::cout << 35300;
    for (int i = 0; i < 35300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35301(int& val) {
    func_in_35301(val);
    const int x = 35301;
    std::cout << 35301;
    for (int i = 0; i < 35301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35301(int& val) {
    const int x = 35301;
    std::cout << 35301;
    for (int i = 0; i < 35301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35302(int& val) {
    func_in_35302(val);
    const int x = 35302;
    std::cout << 35302;
    for (int i = 0; i < 35302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35302(int& val) {
    const int x = 35302;
    std::cout << 35302;
    for (int i = 0; i < 35302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35303(int& val) {
    func_in_35303(val);
    const int x = 35303;
    std::cout << 35303;
    for (int i = 0; i < 35303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35303(int& val) {
    const int x = 35303;
    std::cout << 35303;
    for (int i = 0; i < 35303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35304(int& val) {
    func_in_35304(val);
    const int x = 35304;
    std::cout << 35304;
    for (int i = 0; i < 35304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35304(int& val) {
    const int x = 35304;
    std::cout << 35304;
    for (int i = 0; i < 35304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35305(int& val) {
    func_in_35305(val);
    const int x = 35305;
    std::cout << 35305;
    for (int i = 0; i < 35305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35305(int& val) {
    const int x = 35305;
    std::cout << 35305;
    for (int i = 0; i < 35305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35306(int& val) {
    func_in_35306(val);
    const int x = 35306;
    std::cout << 35306;
    for (int i = 0; i < 35306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35306(int& val) {
    const int x = 35306;
    std::cout << 35306;
    for (int i = 0; i < 35306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35307(int& val) {
    func_in_35307(val);
    const int x = 35307;
    std::cout << 35307;
    for (int i = 0; i < 35307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35307(int& val) {
    const int x = 35307;
    std::cout << 35307;
    for (int i = 0; i < 35307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35308(int& val) {
    func_in_35308(val);
    const int x = 35308;
    std::cout << 35308;
    for (int i = 0; i < 35308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35308(int& val) {
    const int x = 35308;
    std::cout << 35308;
    for (int i = 0; i < 35308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35309(int& val) {
    func_in_35309(val);
    const int x = 35309;
    std::cout << 35309;
    for (int i = 0; i < 35309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35309(int& val) {
    const int x = 35309;
    std::cout << 35309;
    for (int i = 0; i < 35309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35310(int& val) {
    func_in_35310(val);
    const int x = 35310;
    std::cout << 35310;
    for (int i = 0; i < 35310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35310(int& val) {
    const int x = 35310;
    std::cout << 35310;
    for (int i = 0; i < 35310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35311(int& val) {
    func_in_35311(val);
    const int x = 35311;
    std::cout << 35311;
    for (int i = 0; i < 35311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35311(int& val) {
    const int x = 35311;
    std::cout << 35311;
    for (int i = 0; i < 35311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35312(int& val) {
    func_in_35312(val);
    const int x = 35312;
    std::cout << 35312;
    for (int i = 0; i < 35312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35312(int& val) {
    const int x = 35312;
    std::cout << 35312;
    for (int i = 0; i < 35312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35313(int& val) {
    func_in_35313(val);
    const int x = 35313;
    std::cout << 35313;
    for (int i = 0; i < 35313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35313(int& val) {
    const int x = 35313;
    std::cout << 35313;
    for (int i = 0; i < 35313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35314(int& val) {
    func_in_35314(val);
    const int x = 35314;
    std::cout << 35314;
    for (int i = 0; i < 35314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35314(int& val) {
    const int x = 35314;
    std::cout << 35314;
    for (int i = 0; i < 35314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35315(int& val) {
    func_in_35315(val);
    const int x = 35315;
    std::cout << 35315;
    for (int i = 0; i < 35315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35315(int& val) {
    const int x = 35315;
    std::cout << 35315;
    for (int i = 0; i < 35315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35316(int& val) {
    func_in_35316(val);
    const int x = 35316;
    std::cout << 35316;
    for (int i = 0; i < 35316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35316(int& val) {
    const int x = 35316;
    std::cout << 35316;
    for (int i = 0; i < 35316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35317(int& val) {
    func_in_35317(val);
    const int x = 35317;
    std::cout << 35317;
    for (int i = 0; i < 35317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35317(int& val) {
    const int x = 35317;
    std::cout << 35317;
    for (int i = 0; i < 35317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35318(int& val) {
    func_in_35318(val);
    const int x = 35318;
    std::cout << 35318;
    for (int i = 0; i < 35318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35318(int& val) {
    const int x = 35318;
    std::cout << 35318;
    for (int i = 0; i < 35318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35319(int& val) {
    func_in_35319(val);
    const int x = 35319;
    std::cout << 35319;
    for (int i = 0; i < 35319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35319(int& val) {
    const int x = 35319;
    std::cout << 35319;
    for (int i = 0; i < 35319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35320(int& val) {
    func_in_35320(val);
    const int x = 35320;
    std::cout << 35320;
    for (int i = 0; i < 35320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35320(int& val) {
    const int x = 35320;
    std::cout << 35320;
    for (int i = 0; i < 35320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35321(int& val) {
    func_in_35321(val);
    const int x = 35321;
    std::cout << 35321;
    for (int i = 0; i < 35321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35321(int& val) {
    const int x = 35321;
    std::cout << 35321;
    for (int i = 0; i < 35321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35322(int& val) {
    func_in_35322(val);
    const int x = 35322;
    std::cout << 35322;
    for (int i = 0; i < 35322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35322(int& val) {
    const int x = 35322;
    std::cout << 35322;
    for (int i = 0; i < 35322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35323(int& val) {
    func_in_35323(val);
    const int x = 35323;
    std::cout << 35323;
    for (int i = 0; i < 35323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35323(int& val) {
    const int x = 35323;
    std::cout << 35323;
    for (int i = 0; i < 35323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35324(int& val) {
    func_in_35324(val);
    const int x = 35324;
    std::cout << 35324;
    for (int i = 0; i < 35324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35324(int& val) {
    const int x = 35324;
    std::cout << 35324;
    for (int i = 0; i < 35324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35325(int& val) {
    func_in_35325(val);
    const int x = 35325;
    std::cout << 35325;
    for (int i = 0; i < 35325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35325(int& val) {
    const int x = 35325;
    std::cout << 35325;
    for (int i = 0; i < 35325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35326(int& val) {
    func_in_35326(val);
    const int x = 35326;
    std::cout << 35326;
    for (int i = 0; i < 35326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35326(int& val) {
    const int x = 35326;
    std::cout << 35326;
    for (int i = 0; i < 35326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35327(int& val) {
    func_in_35327(val);
    const int x = 35327;
    std::cout << 35327;
    for (int i = 0; i < 35327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35327(int& val) {
    const int x = 35327;
    std::cout << 35327;
    for (int i = 0; i < 35327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35328(int& val) {
    func_in_35328(val);
    const int x = 35328;
    std::cout << 35328;
    for (int i = 0; i < 35328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35328(int& val) {
    const int x = 35328;
    std::cout << 35328;
    for (int i = 0; i < 35328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35329(int& val) {
    func_in_35329(val);
    const int x = 35329;
    std::cout << 35329;
    for (int i = 0; i < 35329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35329(int& val) {
    const int x = 35329;
    std::cout << 35329;
    for (int i = 0; i < 35329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35330(int& val) {
    func_in_35330(val);
    const int x = 35330;
    std::cout << 35330;
    for (int i = 0; i < 35330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35330(int& val) {
    const int x = 35330;
    std::cout << 35330;
    for (int i = 0; i < 35330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35331(int& val) {
    func_in_35331(val);
    const int x = 35331;
    std::cout << 35331;
    for (int i = 0; i < 35331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35331(int& val) {
    const int x = 35331;
    std::cout << 35331;
    for (int i = 0; i < 35331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35332(int& val) {
    func_in_35332(val);
    const int x = 35332;
    std::cout << 35332;
    for (int i = 0; i < 35332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35332(int& val) {
    const int x = 35332;
    std::cout << 35332;
    for (int i = 0; i < 35332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35333(int& val) {
    func_in_35333(val);
    const int x = 35333;
    std::cout << 35333;
    for (int i = 0; i < 35333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35333(int& val) {
    const int x = 35333;
    std::cout << 35333;
    for (int i = 0; i < 35333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35334(int& val) {
    func_in_35334(val);
    const int x = 35334;
    std::cout << 35334;
    for (int i = 0; i < 35334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35334(int& val) {
    const int x = 35334;
    std::cout << 35334;
    for (int i = 0; i < 35334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35335(int& val) {
    func_in_35335(val);
    const int x = 35335;
    std::cout << 35335;
    for (int i = 0; i < 35335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35335(int& val) {
    const int x = 35335;
    std::cout << 35335;
    for (int i = 0; i < 35335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35336(int& val) {
    func_in_35336(val);
    const int x = 35336;
    std::cout << 35336;
    for (int i = 0; i < 35336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35336(int& val) {
    const int x = 35336;
    std::cout << 35336;
    for (int i = 0; i < 35336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35337(int& val) {
    func_in_35337(val);
    const int x = 35337;
    std::cout << 35337;
    for (int i = 0; i < 35337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35337(int& val) {
    const int x = 35337;
    std::cout << 35337;
    for (int i = 0; i < 35337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35338(int& val) {
    func_in_35338(val);
    const int x = 35338;
    std::cout << 35338;
    for (int i = 0; i < 35338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35338(int& val) {
    const int x = 35338;
    std::cout << 35338;
    for (int i = 0; i < 35338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35339(int& val) {
    func_in_35339(val);
    const int x = 35339;
    std::cout << 35339;
    for (int i = 0; i < 35339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35339(int& val) {
    const int x = 35339;
    std::cout << 35339;
    for (int i = 0; i < 35339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35340(int& val) {
    func_in_35340(val);
    const int x = 35340;
    std::cout << 35340;
    for (int i = 0; i < 35340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35340(int& val) {
    const int x = 35340;
    std::cout << 35340;
    for (int i = 0; i < 35340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35341(int& val) {
    func_in_35341(val);
    const int x = 35341;
    std::cout << 35341;
    for (int i = 0; i < 35341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35341(int& val) {
    const int x = 35341;
    std::cout << 35341;
    for (int i = 0; i < 35341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35342(int& val) {
    func_in_35342(val);
    const int x = 35342;
    std::cout << 35342;
    for (int i = 0; i < 35342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35342(int& val) {
    const int x = 35342;
    std::cout << 35342;
    for (int i = 0; i < 35342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35343(int& val) {
    func_in_35343(val);
    const int x = 35343;
    std::cout << 35343;
    for (int i = 0; i < 35343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35343(int& val) {
    const int x = 35343;
    std::cout << 35343;
    for (int i = 0; i < 35343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35344(int& val) {
    func_in_35344(val);
    const int x = 35344;
    std::cout << 35344;
    for (int i = 0; i < 35344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35344(int& val) {
    const int x = 35344;
    std::cout << 35344;
    for (int i = 0; i < 35344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35345(int& val) {
    func_in_35345(val);
    const int x = 35345;
    std::cout << 35345;
    for (int i = 0; i < 35345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35345(int& val) {
    const int x = 35345;
    std::cout << 35345;
    for (int i = 0; i < 35345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35346(int& val) {
    func_in_35346(val);
    const int x = 35346;
    std::cout << 35346;
    for (int i = 0; i < 35346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35346(int& val) {
    const int x = 35346;
    std::cout << 35346;
    for (int i = 0; i < 35346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35347(int& val) {
    func_in_35347(val);
    const int x = 35347;
    std::cout << 35347;
    for (int i = 0; i < 35347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35347(int& val) {
    const int x = 35347;
    std::cout << 35347;
    for (int i = 0; i < 35347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35348(int& val) {
    func_in_35348(val);
    const int x = 35348;
    std::cout << 35348;
    for (int i = 0; i < 35348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35348(int& val) {
    const int x = 35348;
    std::cout << 35348;
    for (int i = 0; i < 35348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35349(int& val) {
    func_in_35349(val);
    const int x = 35349;
    std::cout << 35349;
    for (int i = 0; i < 35349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35349(int& val) {
    const int x = 35349;
    std::cout << 35349;
    for (int i = 0; i < 35349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35350(int& val) {
    func_in_35350(val);
    const int x = 35350;
    std::cout << 35350;
    for (int i = 0; i < 35350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35350(int& val) {
    const int x = 35350;
    std::cout << 35350;
    for (int i = 0; i < 35350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35351(int& val) {
    func_in_35351(val);
    const int x = 35351;
    std::cout << 35351;
    for (int i = 0; i < 35351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35351(int& val) {
    const int x = 35351;
    std::cout << 35351;
    for (int i = 0; i < 35351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35352(int& val) {
    func_in_35352(val);
    const int x = 35352;
    std::cout << 35352;
    for (int i = 0; i < 35352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35352(int& val) {
    const int x = 35352;
    std::cout << 35352;
    for (int i = 0; i < 35352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35353(int& val) {
    func_in_35353(val);
    const int x = 35353;
    std::cout << 35353;
    for (int i = 0; i < 35353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35353(int& val) {
    const int x = 35353;
    std::cout << 35353;
    for (int i = 0; i < 35353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35354(int& val) {
    func_in_35354(val);
    const int x = 35354;
    std::cout << 35354;
    for (int i = 0; i < 35354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35354(int& val) {
    const int x = 35354;
    std::cout << 35354;
    for (int i = 0; i < 35354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35355(int& val) {
    func_in_35355(val);
    const int x = 35355;
    std::cout << 35355;
    for (int i = 0; i < 35355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35355(int& val) {
    const int x = 35355;
    std::cout << 35355;
    for (int i = 0; i < 35355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35356(int& val) {
    func_in_35356(val);
    const int x = 35356;
    std::cout << 35356;
    for (int i = 0; i < 35356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35356(int& val) {
    const int x = 35356;
    std::cout << 35356;
    for (int i = 0; i < 35356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35357(int& val) {
    func_in_35357(val);
    const int x = 35357;
    std::cout << 35357;
    for (int i = 0; i < 35357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35357(int& val) {
    const int x = 35357;
    std::cout << 35357;
    for (int i = 0; i < 35357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35358(int& val) {
    func_in_35358(val);
    const int x = 35358;
    std::cout << 35358;
    for (int i = 0; i < 35358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35358(int& val) {
    const int x = 35358;
    std::cout << 35358;
    for (int i = 0; i < 35358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35359(int& val) {
    func_in_35359(val);
    const int x = 35359;
    std::cout << 35359;
    for (int i = 0; i < 35359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35359(int& val) {
    const int x = 35359;
    std::cout << 35359;
    for (int i = 0; i < 35359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35360(int& val) {
    func_in_35360(val);
    const int x = 35360;
    std::cout << 35360;
    for (int i = 0; i < 35360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35360(int& val) {
    const int x = 35360;
    std::cout << 35360;
    for (int i = 0; i < 35360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35361(int& val) {
    func_in_35361(val);
    const int x = 35361;
    std::cout << 35361;
    for (int i = 0; i < 35361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35361(int& val) {
    const int x = 35361;
    std::cout << 35361;
    for (int i = 0; i < 35361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35362(int& val) {
    func_in_35362(val);
    const int x = 35362;
    std::cout << 35362;
    for (int i = 0; i < 35362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35362(int& val) {
    const int x = 35362;
    std::cout << 35362;
    for (int i = 0; i < 35362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35363(int& val) {
    func_in_35363(val);
    const int x = 35363;
    std::cout << 35363;
    for (int i = 0; i < 35363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35363(int& val) {
    const int x = 35363;
    std::cout << 35363;
    for (int i = 0; i < 35363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35364(int& val) {
    func_in_35364(val);
    const int x = 35364;
    std::cout << 35364;
    for (int i = 0; i < 35364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35364(int& val) {
    const int x = 35364;
    std::cout << 35364;
    for (int i = 0; i < 35364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35365(int& val) {
    func_in_35365(val);
    const int x = 35365;
    std::cout << 35365;
    for (int i = 0; i < 35365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35365(int& val) {
    const int x = 35365;
    std::cout << 35365;
    for (int i = 0; i < 35365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35366(int& val) {
    func_in_35366(val);
    const int x = 35366;
    std::cout << 35366;
    for (int i = 0; i < 35366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35366(int& val) {
    const int x = 35366;
    std::cout << 35366;
    for (int i = 0; i < 35366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35367(int& val) {
    func_in_35367(val);
    const int x = 35367;
    std::cout << 35367;
    for (int i = 0; i < 35367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35367(int& val) {
    const int x = 35367;
    std::cout << 35367;
    for (int i = 0; i < 35367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35368(int& val) {
    func_in_35368(val);
    const int x = 35368;
    std::cout << 35368;
    for (int i = 0; i < 35368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35368(int& val) {
    const int x = 35368;
    std::cout << 35368;
    for (int i = 0; i < 35368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35369(int& val) {
    func_in_35369(val);
    const int x = 35369;
    std::cout << 35369;
    for (int i = 0; i < 35369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35369(int& val) {
    const int x = 35369;
    std::cout << 35369;
    for (int i = 0; i < 35369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35370(int& val) {
    func_in_35370(val);
    const int x = 35370;
    std::cout << 35370;
    for (int i = 0; i < 35370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35370(int& val) {
    const int x = 35370;
    std::cout << 35370;
    for (int i = 0; i < 35370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35371(int& val) {
    func_in_35371(val);
    const int x = 35371;
    std::cout << 35371;
    for (int i = 0; i < 35371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35371(int& val) {
    const int x = 35371;
    std::cout << 35371;
    for (int i = 0; i < 35371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35372(int& val) {
    func_in_35372(val);
    const int x = 35372;
    std::cout << 35372;
    for (int i = 0; i < 35372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35372(int& val) {
    const int x = 35372;
    std::cout << 35372;
    for (int i = 0; i < 35372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35373(int& val) {
    func_in_35373(val);
    const int x = 35373;
    std::cout << 35373;
    for (int i = 0; i < 35373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35373(int& val) {
    const int x = 35373;
    std::cout << 35373;
    for (int i = 0; i < 35373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35374(int& val) {
    func_in_35374(val);
    const int x = 35374;
    std::cout << 35374;
    for (int i = 0; i < 35374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35374(int& val) {
    const int x = 35374;
    std::cout << 35374;
    for (int i = 0; i < 35374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35375(int& val) {
    func_in_35375(val);
    const int x = 35375;
    std::cout << 35375;
    for (int i = 0; i < 35375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35375(int& val) {
    const int x = 35375;
    std::cout << 35375;
    for (int i = 0; i < 35375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35376(int& val) {
    func_in_35376(val);
    const int x = 35376;
    std::cout << 35376;
    for (int i = 0; i < 35376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35376(int& val) {
    const int x = 35376;
    std::cout << 35376;
    for (int i = 0; i < 35376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35377(int& val) {
    func_in_35377(val);
    const int x = 35377;
    std::cout << 35377;
    for (int i = 0; i < 35377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35377(int& val) {
    const int x = 35377;
    std::cout << 35377;
    for (int i = 0; i < 35377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35378(int& val) {
    func_in_35378(val);
    const int x = 35378;
    std::cout << 35378;
    for (int i = 0; i < 35378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35378(int& val) {
    const int x = 35378;
    std::cout << 35378;
    for (int i = 0; i < 35378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35379(int& val) {
    func_in_35379(val);
    const int x = 35379;
    std::cout << 35379;
    for (int i = 0; i < 35379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35379(int& val) {
    const int x = 35379;
    std::cout << 35379;
    for (int i = 0; i < 35379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35380(int& val) {
    func_in_35380(val);
    const int x = 35380;
    std::cout << 35380;
    for (int i = 0; i < 35380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35380(int& val) {
    const int x = 35380;
    std::cout << 35380;
    for (int i = 0; i < 35380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35381(int& val) {
    func_in_35381(val);
    const int x = 35381;
    std::cout << 35381;
    for (int i = 0; i < 35381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35381(int& val) {
    const int x = 35381;
    std::cout << 35381;
    for (int i = 0; i < 35381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35382(int& val) {
    func_in_35382(val);
    const int x = 35382;
    std::cout << 35382;
    for (int i = 0; i < 35382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35382(int& val) {
    const int x = 35382;
    std::cout << 35382;
    for (int i = 0; i < 35382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35383(int& val) {
    func_in_35383(val);
    const int x = 35383;
    std::cout << 35383;
    for (int i = 0; i < 35383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35383(int& val) {
    const int x = 35383;
    std::cout << 35383;
    for (int i = 0; i < 35383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35384(int& val) {
    func_in_35384(val);
    const int x = 35384;
    std::cout << 35384;
    for (int i = 0; i < 35384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35384(int& val) {
    const int x = 35384;
    std::cout << 35384;
    for (int i = 0; i < 35384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35385(int& val) {
    func_in_35385(val);
    const int x = 35385;
    std::cout << 35385;
    for (int i = 0; i < 35385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35385(int& val) {
    const int x = 35385;
    std::cout << 35385;
    for (int i = 0; i < 35385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35386(int& val) {
    func_in_35386(val);
    const int x = 35386;
    std::cout << 35386;
    for (int i = 0; i < 35386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35386(int& val) {
    const int x = 35386;
    std::cout << 35386;
    for (int i = 0; i < 35386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35387(int& val) {
    func_in_35387(val);
    const int x = 35387;
    std::cout << 35387;
    for (int i = 0; i < 35387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35387(int& val) {
    const int x = 35387;
    std::cout << 35387;
    for (int i = 0; i < 35387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35388(int& val) {
    func_in_35388(val);
    const int x = 35388;
    std::cout << 35388;
    for (int i = 0; i < 35388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35388(int& val) {
    const int x = 35388;
    std::cout << 35388;
    for (int i = 0; i < 35388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35389(int& val) {
    func_in_35389(val);
    const int x = 35389;
    std::cout << 35389;
    for (int i = 0; i < 35389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35389(int& val) {
    const int x = 35389;
    std::cout << 35389;
    for (int i = 0; i < 35389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35390(int& val) {
    func_in_35390(val);
    const int x = 35390;
    std::cout << 35390;
    for (int i = 0; i < 35390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35390(int& val) {
    const int x = 35390;
    std::cout << 35390;
    for (int i = 0; i < 35390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35391(int& val) {
    func_in_35391(val);
    const int x = 35391;
    std::cout << 35391;
    for (int i = 0; i < 35391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35391(int& val) {
    const int x = 35391;
    std::cout << 35391;
    for (int i = 0; i < 35391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35392(int& val) {
    func_in_35392(val);
    const int x = 35392;
    std::cout << 35392;
    for (int i = 0; i < 35392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35392(int& val) {
    const int x = 35392;
    std::cout << 35392;
    for (int i = 0; i < 35392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35393(int& val) {
    func_in_35393(val);
    const int x = 35393;
    std::cout << 35393;
    for (int i = 0; i < 35393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35393(int& val) {
    const int x = 35393;
    std::cout << 35393;
    for (int i = 0; i < 35393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35394(int& val) {
    func_in_35394(val);
    const int x = 35394;
    std::cout << 35394;
    for (int i = 0; i < 35394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35394(int& val) {
    const int x = 35394;
    std::cout << 35394;
    for (int i = 0; i < 35394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35395(int& val) {
    func_in_35395(val);
    const int x = 35395;
    std::cout << 35395;
    for (int i = 0; i < 35395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35395(int& val) {
    const int x = 35395;
    std::cout << 35395;
    for (int i = 0; i < 35395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35396(int& val) {
    func_in_35396(val);
    const int x = 35396;
    std::cout << 35396;
    for (int i = 0; i < 35396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35396(int& val) {
    const int x = 35396;
    std::cout << 35396;
    for (int i = 0; i < 35396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35397(int& val) {
    func_in_35397(val);
    const int x = 35397;
    std::cout << 35397;
    for (int i = 0; i < 35397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35397(int& val) {
    const int x = 35397;
    std::cout << 35397;
    for (int i = 0; i < 35397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35398(int& val) {
    func_in_35398(val);
    const int x = 35398;
    std::cout << 35398;
    for (int i = 0; i < 35398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35398(int& val) {
    const int x = 35398;
    std::cout << 35398;
    for (int i = 0; i < 35398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35399(int& val) {
    func_in_35399(val);
    const int x = 35399;
    std::cout << 35399;
    for (int i = 0; i < 35399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35399(int& val) {
    const int x = 35399;
    std::cout << 35399;
    for (int i = 0; i < 35399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35400(int& val) {
    func_in_35400(val);
    const int x = 35400;
    std::cout << 35400;
    for (int i = 0; i < 35400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35400(int& val) {
    const int x = 35400;
    std::cout << 35400;
    for (int i = 0; i < 35400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35401(int& val) {
    func_in_35401(val);
    const int x = 35401;
    std::cout << 35401;
    for (int i = 0; i < 35401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35401(int& val) {
    const int x = 35401;
    std::cout << 35401;
    for (int i = 0; i < 35401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35402(int& val) {
    func_in_35402(val);
    const int x = 35402;
    std::cout << 35402;
    for (int i = 0; i < 35402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35402(int& val) {
    const int x = 35402;
    std::cout << 35402;
    for (int i = 0; i < 35402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35403(int& val) {
    func_in_35403(val);
    const int x = 35403;
    std::cout << 35403;
    for (int i = 0; i < 35403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35403(int& val) {
    const int x = 35403;
    std::cout << 35403;
    for (int i = 0; i < 35403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35404(int& val) {
    func_in_35404(val);
    const int x = 35404;
    std::cout << 35404;
    for (int i = 0; i < 35404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35404(int& val) {
    const int x = 35404;
    std::cout << 35404;
    for (int i = 0; i < 35404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35405(int& val) {
    func_in_35405(val);
    const int x = 35405;
    std::cout << 35405;
    for (int i = 0; i < 35405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35405(int& val) {
    const int x = 35405;
    std::cout << 35405;
    for (int i = 0; i < 35405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35406(int& val) {
    func_in_35406(val);
    const int x = 35406;
    std::cout << 35406;
    for (int i = 0; i < 35406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35406(int& val) {
    const int x = 35406;
    std::cout << 35406;
    for (int i = 0; i < 35406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35407(int& val) {
    func_in_35407(val);
    const int x = 35407;
    std::cout << 35407;
    for (int i = 0; i < 35407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35407(int& val) {
    const int x = 35407;
    std::cout << 35407;
    for (int i = 0; i < 35407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35408(int& val) {
    func_in_35408(val);
    const int x = 35408;
    std::cout << 35408;
    for (int i = 0; i < 35408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35408(int& val) {
    const int x = 35408;
    std::cout << 35408;
    for (int i = 0; i < 35408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35409(int& val) {
    func_in_35409(val);
    const int x = 35409;
    std::cout << 35409;
    for (int i = 0; i < 35409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35409(int& val) {
    const int x = 35409;
    std::cout << 35409;
    for (int i = 0; i < 35409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35410(int& val) {
    func_in_35410(val);
    const int x = 35410;
    std::cout << 35410;
    for (int i = 0; i < 35410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35410(int& val) {
    const int x = 35410;
    std::cout << 35410;
    for (int i = 0; i < 35410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35411(int& val) {
    func_in_35411(val);
    const int x = 35411;
    std::cout << 35411;
    for (int i = 0; i < 35411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35411(int& val) {
    const int x = 35411;
    std::cout << 35411;
    for (int i = 0; i < 35411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35412(int& val) {
    func_in_35412(val);
    const int x = 35412;
    std::cout << 35412;
    for (int i = 0; i < 35412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35412(int& val) {
    const int x = 35412;
    std::cout << 35412;
    for (int i = 0; i < 35412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35413(int& val) {
    func_in_35413(val);
    const int x = 35413;
    std::cout << 35413;
    for (int i = 0; i < 35413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35413(int& val) {
    const int x = 35413;
    std::cout << 35413;
    for (int i = 0; i < 35413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35414(int& val) {
    func_in_35414(val);
    const int x = 35414;
    std::cout << 35414;
    for (int i = 0; i < 35414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35414(int& val) {
    const int x = 35414;
    std::cout << 35414;
    for (int i = 0; i < 35414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35415(int& val) {
    func_in_35415(val);
    const int x = 35415;
    std::cout << 35415;
    for (int i = 0; i < 35415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35415(int& val) {
    const int x = 35415;
    std::cout << 35415;
    for (int i = 0; i < 35415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35416(int& val) {
    func_in_35416(val);
    const int x = 35416;
    std::cout << 35416;
    for (int i = 0; i < 35416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35416(int& val) {
    const int x = 35416;
    std::cout << 35416;
    for (int i = 0; i < 35416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35417(int& val) {
    func_in_35417(val);
    const int x = 35417;
    std::cout << 35417;
    for (int i = 0; i < 35417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35417(int& val) {
    const int x = 35417;
    std::cout << 35417;
    for (int i = 0; i < 35417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35418(int& val) {
    func_in_35418(val);
    const int x = 35418;
    std::cout << 35418;
    for (int i = 0; i < 35418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35418(int& val) {
    const int x = 35418;
    std::cout << 35418;
    for (int i = 0; i < 35418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35419(int& val) {
    func_in_35419(val);
    const int x = 35419;
    std::cout << 35419;
    for (int i = 0; i < 35419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35419(int& val) {
    const int x = 35419;
    std::cout << 35419;
    for (int i = 0; i < 35419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35420(int& val) {
    func_in_35420(val);
    const int x = 35420;
    std::cout << 35420;
    for (int i = 0; i < 35420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35420(int& val) {
    const int x = 35420;
    std::cout << 35420;
    for (int i = 0; i < 35420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35421(int& val) {
    func_in_35421(val);
    const int x = 35421;
    std::cout << 35421;
    for (int i = 0; i < 35421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35421(int& val) {
    const int x = 35421;
    std::cout << 35421;
    for (int i = 0; i < 35421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35422(int& val) {
    func_in_35422(val);
    const int x = 35422;
    std::cout << 35422;
    for (int i = 0; i < 35422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35422(int& val) {
    const int x = 35422;
    std::cout << 35422;
    for (int i = 0; i < 35422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35423(int& val) {
    func_in_35423(val);
    const int x = 35423;
    std::cout << 35423;
    for (int i = 0; i < 35423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35423(int& val) {
    const int x = 35423;
    std::cout << 35423;
    for (int i = 0; i < 35423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35424(int& val) {
    func_in_35424(val);
    const int x = 35424;
    std::cout << 35424;
    for (int i = 0; i < 35424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35424(int& val) {
    const int x = 35424;
    std::cout << 35424;
    for (int i = 0; i < 35424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35425(int& val) {
    func_in_35425(val);
    const int x = 35425;
    std::cout << 35425;
    for (int i = 0; i < 35425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35425(int& val) {
    const int x = 35425;
    std::cout << 35425;
    for (int i = 0; i < 35425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35426(int& val) {
    func_in_35426(val);
    const int x = 35426;
    std::cout << 35426;
    for (int i = 0; i < 35426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35426(int& val) {
    const int x = 35426;
    std::cout << 35426;
    for (int i = 0; i < 35426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35427(int& val) {
    func_in_35427(val);
    const int x = 35427;
    std::cout << 35427;
    for (int i = 0; i < 35427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35427(int& val) {
    const int x = 35427;
    std::cout << 35427;
    for (int i = 0; i < 35427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35428(int& val) {
    func_in_35428(val);
    const int x = 35428;
    std::cout << 35428;
    for (int i = 0; i < 35428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35428(int& val) {
    const int x = 35428;
    std::cout << 35428;
    for (int i = 0; i < 35428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35429(int& val) {
    func_in_35429(val);
    const int x = 35429;
    std::cout << 35429;
    for (int i = 0; i < 35429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35429(int& val) {
    const int x = 35429;
    std::cout << 35429;
    for (int i = 0; i < 35429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35430(int& val) {
    func_in_35430(val);
    const int x = 35430;
    std::cout << 35430;
    for (int i = 0; i < 35430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35430(int& val) {
    const int x = 35430;
    std::cout << 35430;
    for (int i = 0; i < 35430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35431(int& val) {
    func_in_35431(val);
    const int x = 35431;
    std::cout << 35431;
    for (int i = 0; i < 35431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35431(int& val) {
    const int x = 35431;
    std::cout << 35431;
    for (int i = 0; i < 35431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35432(int& val) {
    func_in_35432(val);
    const int x = 35432;
    std::cout << 35432;
    for (int i = 0; i < 35432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35432(int& val) {
    const int x = 35432;
    std::cout << 35432;
    for (int i = 0; i < 35432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35433(int& val) {
    func_in_35433(val);
    const int x = 35433;
    std::cout << 35433;
    for (int i = 0; i < 35433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35433(int& val) {
    const int x = 35433;
    std::cout << 35433;
    for (int i = 0; i < 35433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35434(int& val) {
    func_in_35434(val);
    const int x = 35434;
    std::cout << 35434;
    for (int i = 0; i < 35434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35434(int& val) {
    const int x = 35434;
    std::cout << 35434;
    for (int i = 0; i < 35434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35435(int& val) {
    func_in_35435(val);
    const int x = 35435;
    std::cout << 35435;
    for (int i = 0; i < 35435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35435(int& val) {
    const int x = 35435;
    std::cout << 35435;
    for (int i = 0; i < 35435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35436(int& val) {
    func_in_35436(val);
    const int x = 35436;
    std::cout << 35436;
    for (int i = 0; i < 35436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35436(int& val) {
    const int x = 35436;
    std::cout << 35436;
    for (int i = 0; i < 35436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35437(int& val) {
    func_in_35437(val);
    const int x = 35437;
    std::cout << 35437;
    for (int i = 0; i < 35437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35437(int& val) {
    const int x = 35437;
    std::cout << 35437;
    for (int i = 0; i < 35437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35438(int& val) {
    func_in_35438(val);
    const int x = 35438;
    std::cout << 35438;
    for (int i = 0; i < 35438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35438(int& val) {
    const int x = 35438;
    std::cout << 35438;
    for (int i = 0; i < 35438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35439(int& val) {
    func_in_35439(val);
    const int x = 35439;
    std::cout << 35439;
    for (int i = 0; i < 35439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35439(int& val) {
    const int x = 35439;
    std::cout << 35439;
    for (int i = 0; i < 35439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35440(int& val) {
    func_in_35440(val);
    const int x = 35440;
    std::cout << 35440;
    for (int i = 0; i < 35440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35440(int& val) {
    const int x = 35440;
    std::cout << 35440;
    for (int i = 0; i < 35440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35441(int& val) {
    func_in_35441(val);
    const int x = 35441;
    std::cout << 35441;
    for (int i = 0; i < 35441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35441(int& val) {
    const int x = 35441;
    std::cout << 35441;
    for (int i = 0; i < 35441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35442(int& val) {
    func_in_35442(val);
    const int x = 35442;
    std::cout << 35442;
    for (int i = 0; i < 35442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35442(int& val) {
    const int x = 35442;
    std::cout << 35442;
    for (int i = 0; i < 35442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35443(int& val) {
    func_in_35443(val);
    const int x = 35443;
    std::cout << 35443;
    for (int i = 0; i < 35443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35443(int& val) {
    const int x = 35443;
    std::cout << 35443;
    for (int i = 0; i < 35443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35444(int& val) {
    func_in_35444(val);
    const int x = 35444;
    std::cout << 35444;
    for (int i = 0; i < 35444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35444(int& val) {
    const int x = 35444;
    std::cout << 35444;
    for (int i = 0; i < 35444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35445(int& val) {
    func_in_35445(val);
    const int x = 35445;
    std::cout << 35445;
    for (int i = 0; i < 35445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35445(int& val) {
    const int x = 35445;
    std::cout << 35445;
    for (int i = 0; i < 35445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35446(int& val) {
    func_in_35446(val);
    const int x = 35446;
    std::cout << 35446;
    for (int i = 0; i < 35446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35446(int& val) {
    const int x = 35446;
    std::cout << 35446;
    for (int i = 0; i < 35446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35447(int& val) {
    func_in_35447(val);
    const int x = 35447;
    std::cout << 35447;
    for (int i = 0; i < 35447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35447(int& val) {
    const int x = 35447;
    std::cout << 35447;
    for (int i = 0; i < 35447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35448(int& val) {
    func_in_35448(val);
    const int x = 35448;
    std::cout << 35448;
    for (int i = 0; i < 35448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35448(int& val) {
    const int x = 35448;
    std::cout << 35448;
    for (int i = 0; i < 35448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35449(int& val) {
    func_in_35449(val);
    const int x = 35449;
    std::cout << 35449;
    for (int i = 0; i < 35449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35449(int& val) {
    const int x = 35449;
    std::cout << 35449;
    for (int i = 0; i < 35449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35450(int& val) {
    func_in_35450(val);
    const int x = 35450;
    std::cout << 35450;
    for (int i = 0; i < 35450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35450(int& val) {
    const int x = 35450;
    std::cout << 35450;
    for (int i = 0; i < 35450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35451(int& val) {
    func_in_35451(val);
    const int x = 35451;
    std::cout << 35451;
    for (int i = 0; i < 35451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35451(int& val) {
    const int x = 35451;
    std::cout << 35451;
    for (int i = 0; i < 35451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35452(int& val) {
    func_in_35452(val);
    const int x = 35452;
    std::cout << 35452;
    for (int i = 0; i < 35452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35452(int& val) {
    const int x = 35452;
    std::cout << 35452;
    for (int i = 0; i < 35452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35453(int& val) {
    func_in_35453(val);
    const int x = 35453;
    std::cout << 35453;
    for (int i = 0; i < 35453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35453(int& val) {
    const int x = 35453;
    std::cout << 35453;
    for (int i = 0; i < 35453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35454(int& val) {
    func_in_35454(val);
    const int x = 35454;
    std::cout << 35454;
    for (int i = 0; i < 35454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35454(int& val) {
    const int x = 35454;
    std::cout << 35454;
    for (int i = 0; i < 35454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35455(int& val) {
    func_in_35455(val);
    const int x = 35455;
    std::cout << 35455;
    for (int i = 0; i < 35455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35455(int& val) {
    const int x = 35455;
    std::cout << 35455;
    for (int i = 0; i < 35455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35456(int& val) {
    func_in_35456(val);
    const int x = 35456;
    std::cout << 35456;
    for (int i = 0; i < 35456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35456(int& val) {
    const int x = 35456;
    std::cout << 35456;
    for (int i = 0; i < 35456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35457(int& val) {
    func_in_35457(val);
    const int x = 35457;
    std::cout << 35457;
    for (int i = 0; i < 35457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35457(int& val) {
    const int x = 35457;
    std::cout << 35457;
    for (int i = 0; i < 35457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35458(int& val) {
    func_in_35458(val);
    const int x = 35458;
    std::cout << 35458;
    for (int i = 0; i < 35458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35458(int& val) {
    const int x = 35458;
    std::cout << 35458;
    for (int i = 0; i < 35458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35459(int& val) {
    func_in_35459(val);
    const int x = 35459;
    std::cout << 35459;
    for (int i = 0; i < 35459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35459(int& val) {
    const int x = 35459;
    std::cout << 35459;
    for (int i = 0; i < 35459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35460(int& val) {
    func_in_35460(val);
    const int x = 35460;
    std::cout << 35460;
    for (int i = 0; i < 35460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35460(int& val) {
    const int x = 35460;
    std::cout << 35460;
    for (int i = 0; i < 35460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35461(int& val) {
    func_in_35461(val);
    const int x = 35461;
    std::cout << 35461;
    for (int i = 0; i < 35461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35461(int& val) {
    const int x = 35461;
    std::cout << 35461;
    for (int i = 0; i < 35461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35462(int& val) {
    func_in_35462(val);
    const int x = 35462;
    std::cout << 35462;
    for (int i = 0; i < 35462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35462(int& val) {
    const int x = 35462;
    std::cout << 35462;
    for (int i = 0; i < 35462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35463(int& val) {
    func_in_35463(val);
    const int x = 35463;
    std::cout << 35463;
    for (int i = 0; i < 35463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35463(int& val) {
    const int x = 35463;
    std::cout << 35463;
    for (int i = 0; i < 35463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35464(int& val) {
    func_in_35464(val);
    const int x = 35464;
    std::cout << 35464;
    for (int i = 0; i < 35464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35464(int& val) {
    const int x = 35464;
    std::cout << 35464;
    for (int i = 0; i < 35464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35465(int& val) {
    func_in_35465(val);
    const int x = 35465;
    std::cout << 35465;
    for (int i = 0; i < 35465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35465(int& val) {
    const int x = 35465;
    std::cout << 35465;
    for (int i = 0; i < 35465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35466(int& val) {
    func_in_35466(val);
    const int x = 35466;
    std::cout << 35466;
    for (int i = 0; i < 35466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35466(int& val) {
    const int x = 35466;
    std::cout << 35466;
    for (int i = 0; i < 35466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35467(int& val) {
    func_in_35467(val);
    const int x = 35467;
    std::cout << 35467;
    for (int i = 0; i < 35467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35467(int& val) {
    const int x = 35467;
    std::cout << 35467;
    for (int i = 0; i < 35467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35468(int& val) {
    func_in_35468(val);
    const int x = 35468;
    std::cout << 35468;
    for (int i = 0; i < 35468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35468(int& val) {
    const int x = 35468;
    std::cout << 35468;
    for (int i = 0; i < 35468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35469(int& val) {
    func_in_35469(val);
    const int x = 35469;
    std::cout << 35469;
    for (int i = 0; i < 35469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35469(int& val) {
    const int x = 35469;
    std::cout << 35469;
    for (int i = 0; i < 35469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35470(int& val) {
    func_in_35470(val);
    const int x = 35470;
    std::cout << 35470;
    for (int i = 0; i < 35470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35470(int& val) {
    const int x = 35470;
    std::cout << 35470;
    for (int i = 0; i < 35470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35471(int& val) {
    func_in_35471(val);
    const int x = 35471;
    std::cout << 35471;
    for (int i = 0; i < 35471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35471(int& val) {
    const int x = 35471;
    std::cout << 35471;
    for (int i = 0; i < 35471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35472(int& val) {
    func_in_35472(val);
    const int x = 35472;
    std::cout << 35472;
    for (int i = 0; i < 35472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35472(int& val) {
    const int x = 35472;
    std::cout << 35472;
    for (int i = 0; i < 35472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35473(int& val) {
    func_in_35473(val);
    const int x = 35473;
    std::cout << 35473;
    for (int i = 0; i < 35473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35473(int& val) {
    const int x = 35473;
    std::cout << 35473;
    for (int i = 0; i < 35473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35474(int& val) {
    func_in_35474(val);
    const int x = 35474;
    std::cout << 35474;
    for (int i = 0; i < 35474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35474(int& val) {
    const int x = 35474;
    std::cout << 35474;
    for (int i = 0; i < 35474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35475(int& val) {
    func_in_35475(val);
    const int x = 35475;
    std::cout << 35475;
    for (int i = 0; i < 35475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35475(int& val) {
    const int x = 35475;
    std::cout << 35475;
    for (int i = 0; i < 35475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35476(int& val) {
    func_in_35476(val);
    const int x = 35476;
    std::cout << 35476;
    for (int i = 0; i < 35476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35476(int& val) {
    const int x = 35476;
    std::cout << 35476;
    for (int i = 0; i < 35476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35477(int& val) {
    func_in_35477(val);
    const int x = 35477;
    std::cout << 35477;
    for (int i = 0; i < 35477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35477(int& val) {
    const int x = 35477;
    std::cout << 35477;
    for (int i = 0; i < 35477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35478(int& val) {
    func_in_35478(val);
    const int x = 35478;
    std::cout << 35478;
    for (int i = 0; i < 35478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35478(int& val) {
    const int x = 35478;
    std::cout << 35478;
    for (int i = 0; i < 35478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35479(int& val) {
    func_in_35479(val);
    const int x = 35479;
    std::cout << 35479;
    for (int i = 0; i < 35479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35479(int& val) {
    const int x = 35479;
    std::cout << 35479;
    for (int i = 0; i < 35479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35480(int& val) {
    func_in_35480(val);
    const int x = 35480;
    std::cout << 35480;
    for (int i = 0; i < 35480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35480(int& val) {
    const int x = 35480;
    std::cout << 35480;
    for (int i = 0; i < 35480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35481(int& val) {
    func_in_35481(val);
    const int x = 35481;
    std::cout << 35481;
    for (int i = 0; i < 35481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35481(int& val) {
    const int x = 35481;
    std::cout << 35481;
    for (int i = 0; i < 35481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35482(int& val) {
    func_in_35482(val);
    const int x = 35482;
    std::cout << 35482;
    for (int i = 0; i < 35482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35482(int& val) {
    const int x = 35482;
    std::cout << 35482;
    for (int i = 0; i < 35482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35483(int& val) {
    func_in_35483(val);
    const int x = 35483;
    std::cout << 35483;
    for (int i = 0; i < 35483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35483(int& val) {
    const int x = 35483;
    std::cout << 35483;
    for (int i = 0; i < 35483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35484(int& val) {
    func_in_35484(val);
    const int x = 35484;
    std::cout << 35484;
    for (int i = 0; i < 35484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35484(int& val) {
    const int x = 35484;
    std::cout << 35484;
    for (int i = 0; i < 35484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35485(int& val) {
    func_in_35485(val);
    const int x = 35485;
    std::cout << 35485;
    for (int i = 0; i < 35485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35485(int& val) {
    const int x = 35485;
    std::cout << 35485;
    for (int i = 0; i < 35485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35486(int& val) {
    func_in_35486(val);
    const int x = 35486;
    std::cout << 35486;
    for (int i = 0; i < 35486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35486(int& val) {
    const int x = 35486;
    std::cout << 35486;
    for (int i = 0; i < 35486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35487(int& val) {
    func_in_35487(val);
    const int x = 35487;
    std::cout << 35487;
    for (int i = 0; i < 35487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35487(int& val) {
    const int x = 35487;
    std::cout << 35487;
    for (int i = 0; i < 35487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35488(int& val) {
    func_in_35488(val);
    const int x = 35488;
    std::cout << 35488;
    for (int i = 0; i < 35488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35488(int& val) {
    const int x = 35488;
    std::cout << 35488;
    for (int i = 0; i < 35488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35489(int& val) {
    func_in_35489(val);
    const int x = 35489;
    std::cout << 35489;
    for (int i = 0; i < 35489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35489(int& val) {
    const int x = 35489;
    std::cout << 35489;
    for (int i = 0; i < 35489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35490(int& val) {
    func_in_35490(val);
    const int x = 35490;
    std::cout << 35490;
    for (int i = 0; i < 35490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35490(int& val) {
    const int x = 35490;
    std::cout << 35490;
    for (int i = 0; i < 35490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35491(int& val) {
    func_in_35491(val);
    const int x = 35491;
    std::cout << 35491;
    for (int i = 0; i < 35491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35491(int& val) {
    const int x = 35491;
    std::cout << 35491;
    for (int i = 0; i < 35491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35492(int& val) {
    func_in_35492(val);
    const int x = 35492;
    std::cout << 35492;
    for (int i = 0; i < 35492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35492(int& val) {
    const int x = 35492;
    std::cout << 35492;
    for (int i = 0; i < 35492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35493(int& val) {
    func_in_35493(val);
    const int x = 35493;
    std::cout << 35493;
    for (int i = 0; i < 35493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35493(int& val) {
    const int x = 35493;
    std::cout << 35493;
    for (int i = 0; i < 35493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35494(int& val) {
    func_in_35494(val);
    const int x = 35494;
    std::cout << 35494;
    for (int i = 0; i < 35494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35494(int& val) {
    const int x = 35494;
    std::cout << 35494;
    for (int i = 0; i < 35494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35495(int& val) {
    func_in_35495(val);
    const int x = 35495;
    std::cout << 35495;
    for (int i = 0; i < 35495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35495(int& val) {
    const int x = 35495;
    std::cout << 35495;
    for (int i = 0; i < 35495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35496(int& val) {
    func_in_35496(val);
    const int x = 35496;
    std::cout << 35496;
    for (int i = 0; i < 35496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35496(int& val) {
    const int x = 35496;
    std::cout << 35496;
    for (int i = 0; i < 35496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35497(int& val) {
    func_in_35497(val);
    const int x = 35497;
    std::cout << 35497;
    for (int i = 0; i < 35497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35497(int& val) {
    const int x = 35497;
    std::cout << 35497;
    for (int i = 0; i < 35497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35498(int& val) {
    func_in_35498(val);
    const int x = 35498;
    std::cout << 35498;
    for (int i = 0; i < 35498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35498(int& val) {
    const int x = 35498;
    std::cout << 35498;
    for (int i = 0; i < 35498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35499(int& val) {
    func_in_35499(val);
    const int x = 35499;
    std::cout << 35499;
    for (int i = 0; i < 35499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35499(int& val) {
    const int x = 35499;
    std::cout << 35499;
    for (int i = 0; i < 35499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35500(int& val) {
    func_in_35500(val);
    const int x = 35500;
    std::cout << 35500;
    for (int i = 0; i < 35500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35500(int& val) {
    const int x = 35500;
    std::cout << 35500;
    for (int i = 0; i < 35500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35501(int& val) {
    func_in_35501(val);
    const int x = 35501;
    std::cout << 35501;
    for (int i = 0; i < 35501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35501(int& val) {
    const int x = 35501;
    std::cout << 35501;
    for (int i = 0; i < 35501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35502(int& val) {
    func_in_35502(val);
    const int x = 35502;
    std::cout << 35502;
    for (int i = 0; i < 35502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35502(int& val) {
    const int x = 35502;
    std::cout << 35502;
    for (int i = 0; i < 35502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35503(int& val) {
    func_in_35503(val);
    const int x = 35503;
    std::cout << 35503;
    for (int i = 0; i < 35503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35503(int& val) {
    const int x = 35503;
    std::cout << 35503;
    for (int i = 0; i < 35503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35504(int& val) {
    func_in_35504(val);
    const int x = 35504;
    std::cout << 35504;
    for (int i = 0; i < 35504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35504(int& val) {
    const int x = 35504;
    std::cout << 35504;
    for (int i = 0; i < 35504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35505(int& val) {
    func_in_35505(val);
    const int x = 35505;
    std::cout << 35505;
    for (int i = 0; i < 35505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35505(int& val) {
    const int x = 35505;
    std::cout << 35505;
    for (int i = 0; i < 35505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35506(int& val) {
    func_in_35506(val);
    const int x = 35506;
    std::cout << 35506;
    for (int i = 0; i < 35506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35506(int& val) {
    const int x = 35506;
    std::cout << 35506;
    for (int i = 0; i < 35506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35507(int& val) {
    func_in_35507(val);
    const int x = 35507;
    std::cout << 35507;
    for (int i = 0; i < 35507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35507(int& val) {
    const int x = 35507;
    std::cout << 35507;
    for (int i = 0; i < 35507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35508(int& val) {
    func_in_35508(val);
    const int x = 35508;
    std::cout << 35508;
    for (int i = 0; i < 35508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35508(int& val) {
    const int x = 35508;
    std::cout << 35508;
    for (int i = 0; i < 35508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35509(int& val) {
    func_in_35509(val);
    const int x = 35509;
    std::cout << 35509;
    for (int i = 0; i < 35509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35509(int& val) {
    const int x = 35509;
    std::cout << 35509;
    for (int i = 0; i < 35509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35510(int& val) {
    func_in_35510(val);
    const int x = 35510;
    std::cout << 35510;
    for (int i = 0; i < 35510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35510(int& val) {
    const int x = 35510;
    std::cout << 35510;
    for (int i = 0; i < 35510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35511(int& val) {
    func_in_35511(val);
    const int x = 35511;
    std::cout << 35511;
    for (int i = 0; i < 35511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35511(int& val) {
    const int x = 35511;
    std::cout << 35511;
    for (int i = 0; i < 35511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35512(int& val) {
    func_in_35512(val);
    const int x = 35512;
    std::cout << 35512;
    for (int i = 0; i < 35512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35512(int& val) {
    const int x = 35512;
    std::cout << 35512;
    for (int i = 0; i < 35512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35513(int& val) {
    func_in_35513(val);
    const int x = 35513;
    std::cout << 35513;
    for (int i = 0; i < 35513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35513(int& val) {
    const int x = 35513;
    std::cout << 35513;
    for (int i = 0; i < 35513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35514(int& val) {
    func_in_35514(val);
    const int x = 35514;
    std::cout << 35514;
    for (int i = 0; i < 35514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35514(int& val) {
    const int x = 35514;
    std::cout << 35514;
    for (int i = 0; i < 35514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35515(int& val) {
    func_in_35515(val);
    const int x = 35515;
    std::cout << 35515;
    for (int i = 0; i < 35515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35515(int& val) {
    const int x = 35515;
    std::cout << 35515;
    for (int i = 0; i < 35515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35516(int& val) {
    func_in_35516(val);
    const int x = 35516;
    std::cout << 35516;
    for (int i = 0; i < 35516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35516(int& val) {
    const int x = 35516;
    std::cout << 35516;
    for (int i = 0; i < 35516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35517(int& val) {
    func_in_35517(val);
    const int x = 35517;
    std::cout << 35517;
    for (int i = 0; i < 35517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35517(int& val) {
    const int x = 35517;
    std::cout << 35517;
    for (int i = 0; i < 35517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35518(int& val) {
    func_in_35518(val);
    const int x = 35518;
    std::cout << 35518;
    for (int i = 0; i < 35518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35518(int& val) {
    const int x = 35518;
    std::cout << 35518;
    for (int i = 0; i < 35518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35519(int& val) {
    func_in_35519(val);
    const int x = 35519;
    std::cout << 35519;
    for (int i = 0; i < 35519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35519(int& val) {
    const int x = 35519;
    std::cout << 35519;
    for (int i = 0; i < 35519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35520(int& val) {
    func_in_35520(val);
    const int x = 35520;
    std::cout << 35520;
    for (int i = 0; i < 35520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35520(int& val) {
    const int x = 35520;
    std::cout << 35520;
    for (int i = 0; i < 35520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35521(int& val) {
    func_in_35521(val);
    const int x = 35521;
    std::cout << 35521;
    for (int i = 0; i < 35521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35521(int& val) {
    const int x = 35521;
    std::cout << 35521;
    for (int i = 0; i < 35521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35522(int& val) {
    func_in_35522(val);
    const int x = 35522;
    std::cout << 35522;
    for (int i = 0; i < 35522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35522(int& val) {
    const int x = 35522;
    std::cout << 35522;
    for (int i = 0; i < 35522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35523(int& val) {
    func_in_35523(val);
    const int x = 35523;
    std::cout << 35523;
    for (int i = 0; i < 35523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35523(int& val) {
    const int x = 35523;
    std::cout << 35523;
    for (int i = 0; i < 35523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35524(int& val) {
    func_in_35524(val);
    const int x = 35524;
    std::cout << 35524;
    for (int i = 0; i < 35524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35524(int& val) {
    const int x = 35524;
    std::cout << 35524;
    for (int i = 0; i < 35524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35525(int& val) {
    func_in_35525(val);
    const int x = 35525;
    std::cout << 35525;
    for (int i = 0; i < 35525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35525(int& val) {
    const int x = 35525;
    std::cout << 35525;
    for (int i = 0; i < 35525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35526(int& val) {
    func_in_35526(val);
    const int x = 35526;
    std::cout << 35526;
    for (int i = 0; i < 35526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35526(int& val) {
    const int x = 35526;
    std::cout << 35526;
    for (int i = 0; i < 35526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35527(int& val) {
    func_in_35527(val);
    const int x = 35527;
    std::cout << 35527;
    for (int i = 0; i < 35527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35527(int& val) {
    const int x = 35527;
    std::cout << 35527;
    for (int i = 0; i < 35527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35528(int& val) {
    func_in_35528(val);
    const int x = 35528;
    std::cout << 35528;
    for (int i = 0; i < 35528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35528(int& val) {
    const int x = 35528;
    std::cout << 35528;
    for (int i = 0; i < 35528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35529(int& val) {
    func_in_35529(val);
    const int x = 35529;
    std::cout << 35529;
    for (int i = 0; i < 35529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35529(int& val) {
    const int x = 35529;
    std::cout << 35529;
    for (int i = 0; i < 35529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35530(int& val) {
    func_in_35530(val);
    const int x = 35530;
    std::cout << 35530;
    for (int i = 0; i < 35530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35530(int& val) {
    const int x = 35530;
    std::cout << 35530;
    for (int i = 0; i < 35530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35531(int& val) {
    func_in_35531(val);
    const int x = 35531;
    std::cout << 35531;
    for (int i = 0; i < 35531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35531(int& val) {
    const int x = 35531;
    std::cout << 35531;
    for (int i = 0; i < 35531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35532(int& val) {
    func_in_35532(val);
    const int x = 35532;
    std::cout << 35532;
    for (int i = 0; i < 35532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35532(int& val) {
    const int x = 35532;
    std::cout << 35532;
    for (int i = 0; i < 35532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35533(int& val) {
    func_in_35533(val);
    const int x = 35533;
    std::cout << 35533;
    for (int i = 0; i < 35533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35533(int& val) {
    const int x = 35533;
    std::cout << 35533;
    for (int i = 0; i < 35533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35534(int& val) {
    func_in_35534(val);
    const int x = 35534;
    std::cout << 35534;
    for (int i = 0; i < 35534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35534(int& val) {
    const int x = 35534;
    std::cout << 35534;
    for (int i = 0; i < 35534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35535(int& val) {
    func_in_35535(val);
    const int x = 35535;
    std::cout << 35535;
    for (int i = 0; i < 35535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35535(int& val) {
    const int x = 35535;
    std::cout << 35535;
    for (int i = 0; i < 35535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35536(int& val) {
    func_in_35536(val);
    const int x = 35536;
    std::cout << 35536;
    for (int i = 0; i < 35536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35536(int& val) {
    const int x = 35536;
    std::cout << 35536;
    for (int i = 0; i < 35536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35537(int& val) {
    func_in_35537(val);
    const int x = 35537;
    std::cout << 35537;
    for (int i = 0; i < 35537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35537(int& val) {
    const int x = 35537;
    std::cout << 35537;
    for (int i = 0; i < 35537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35538(int& val) {
    func_in_35538(val);
    const int x = 35538;
    std::cout << 35538;
    for (int i = 0; i < 35538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35538(int& val) {
    const int x = 35538;
    std::cout << 35538;
    for (int i = 0; i < 35538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35539(int& val) {
    func_in_35539(val);
    const int x = 35539;
    std::cout << 35539;
    for (int i = 0; i < 35539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35539(int& val) {
    const int x = 35539;
    std::cout << 35539;
    for (int i = 0; i < 35539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35540(int& val) {
    func_in_35540(val);
    const int x = 35540;
    std::cout << 35540;
    for (int i = 0; i < 35540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35540(int& val) {
    const int x = 35540;
    std::cout << 35540;
    for (int i = 0; i < 35540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35541(int& val) {
    func_in_35541(val);
    const int x = 35541;
    std::cout << 35541;
    for (int i = 0; i < 35541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35541(int& val) {
    const int x = 35541;
    std::cout << 35541;
    for (int i = 0; i < 35541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35542(int& val) {
    func_in_35542(val);
    const int x = 35542;
    std::cout << 35542;
    for (int i = 0; i < 35542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35542(int& val) {
    const int x = 35542;
    std::cout << 35542;
    for (int i = 0; i < 35542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35543(int& val) {
    func_in_35543(val);
    const int x = 35543;
    std::cout << 35543;
    for (int i = 0; i < 35543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35543(int& val) {
    const int x = 35543;
    std::cout << 35543;
    for (int i = 0; i < 35543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35544(int& val) {
    func_in_35544(val);
    const int x = 35544;
    std::cout << 35544;
    for (int i = 0; i < 35544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35544(int& val) {
    const int x = 35544;
    std::cout << 35544;
    for (int i = 0; i < 35544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35545(int& val) {
    func_in_35545(val);
    const int x = 35545;
    std::cout << 35545;
    for (int i = 0; i < 35545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35545(int& val) {
    const int x = 35545;
    std::cout << 35545;
    for (int i = 0; i < 35545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35546(int& val) {
    func_in_35546(val);
    const int x = 35546;
    std::cout << 35546;
    for (int i = 0; i < 35546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35546(int& val) {
    const int x = 35546;
    std::cout << 35546;
    for (int i = 0; i < 35546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35547(int& val) {
    func_in_35547(val);
    const int x = 35547;
    std::cout << 35547;
    for (int i = 0; i < 35547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35547(int& val) {
    const int x = 35547;
    std::cout << 35547;
    for (int i = 0; i < 35547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35548(int& val) {
    func_in_35548(val);
    const int x = 35548;
    std::cout << 35548;
    for (int i = 0; i < 35548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35548(int& val) {
    const int x = 35548;
    std::cout << 35548;
    for (int i = 0; i < 35548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35549(int& val) {
    func_in_35549(val);
    const int x = 35549;
    std::cout << 35549;
    for (int i = 0; i < 35549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35549(int& val) {
    const int x = 35549;
    std::cout << 35549;
    for (int i = 0; i < 35549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35550(int& val) {
    func_in_35550(val);
    const int x = 35550;
    std::cout << 35550;
    for (int i = 0; i < 35550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35550(int& val) {
    const int x = 35550;
    std::cout << 35550;
    for (int i = 0; i < 35550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35551(int& val) {
    func_in_35551(val);
    const int x = 35551;
    std::cout << 35551;
    for (int i = 0; i < 35551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35551(int& val) {
    const int x = 35551;
    std::cout << 35551;
    for (int i = 0; i < 35551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35552(int& val) {
    func_in_35552(val);
    const int x = 35552;
    std::cout << 35552;
    for (int i = 0; i < 35552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35552(int& val) {
    const int x = 35552;
    std::cout << 35552;
    for (int i = 0; i < 35552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35553(int& val) {
    func_in_35553(val);
    const int x = 35553;
    std::cout << 35553;
    for (int i = 0; i < 35553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35553(int& val) {
    const int x = 35553;
    std::cout << 35553;
    for (int i = 0; i < 35553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35554(int& val) {
    func_in_35554(val);
    const int x = 35554;
    std::cout << 35554;
    for (int i = 0; i < 35554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35554(int& val) {
    const int x = 35554;
    std::cout << 35554;
    for (int i = 0; i < 35554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35555(int& val) {
    func_in_35555(val);
    const int x = 35555;
    std::cout << 35555;
    for (int i = 0; i < 35555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35555(int& val) {
    const int x = 35555;
    std::cout << 35555;
    for (int i = 0; i < 35555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35556(int& val) {
    func_in_35556(val);
    const int x = 35556;
    std::cout << 35556;
    for (int i = 0; i < 35556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35556(int& val) {
    const int x = 35556;
    std::cout << 35556;
    for (int i = 0; i < 35556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35557(int& val) {
    func_in_35557(val);
    const int x = 35557;
    std::cout << 35557;
    for (int i = 0; i < 35557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35557(int& val) {
    const int x = 35557;
    std::cout << 35557;
    for (int i = 0; i < 35557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35558(int& val) {
    func_in_35558(val);
    const int x = 35558;
    std::cout << 35558;
    for (int i = 0; i < 35558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35558(int& val) {
    const int x = 35558;
    std::cout << 35558;
    for (int i = 0; i < 35558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35559(int& val) {
    func_in_35559(val);
    const int x = 35559;
    std::cout << 35559;
    for (int i = 0; i < 35559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35559(int& val) {
    const int x = 35559;
    std::cout << 35559;
    for (int i = 0; i < 35559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35560(int& val) {
    func_in_35560(val);
    const int x = 35560;
    std::cout << 35560;
    for (int i = 0; i < 35560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35560(int& val) {
    const int x = 35560;
    std::cout << 35560;
    for (int i = 0; i < 35560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35561(int& val) {
    func_in_35561(val);
    const int x = 35561;
    std::cout << 35561;
    for (int i = 0; i < 35561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35561(int& val) {
    const int x = 35561;
    std::cout << 35561;
    for (int i = 0; i < 35561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35562(int& val) {
    func_in_35562(val);
    const int x = 35562;
    std::cout << 35562;
    for (int i = 0; i < 35562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35562(int& val) {
    const int x = 35562;
    std::cout << 35562;
    for (int i = 0; i < 35562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35563(int& val) {
    func_in_35563(val);
    const int x = 35563;
    std::cout << 35563;
    for (int i = 0; i < 35563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35563(int& val) {
    const int x = 35563;
    std::cout << 35563;
    for (int i = 0; i < 35563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35564(int& val) {
    func_in_35564(val);
    const int x = 35564;
    std::cout << 35564;
    for (int i = 0; i < 35564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35564(int& val) {
    const int x = 35564;
    std::cout << 35564;
    for (int i = 0; i < 35564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35565(int& val) {
    func_in_35565(val);
    const int x = 35565;
    std::cout << 35565;
    for (int i = 0; i < 35565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35565(int& val) {
    const int x = 35565;
    std::cout << 35565;
    for (int i = 0; i < 35565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35566(int& val) {
    func_in_35566(val);
    const int x = 35566;
    std::cout << 35566;
    for (int i = 0; i < 35566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35566(int& val) {
    const int x = 35566;
    std::cout << 35566;
    for (int i = 0; i < 35566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35567(int& val) {
    func_in_35567(val);
    const int x = 35567;
    std::cout << 35567;
    for (int i = 0; i < 35567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35567(int& val) {
    const int x = 35567;
    std::cout << 35567;
    for (int i = 0; i < 35567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35568(int& val) {
    func_in_35568(val);
    const int x = 35568;
    std::cout << 35568;
    for (int i = 0; i < 35568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35568(int& val) {
    const int x = 35568;
    std::cout << 35568;
    for (int i = 0; i < 35568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35569(int& val) {
    func_in_35569(val);
    const int x = 35569;
    std::cout << 35569;
    for (int i = 0; i < 35569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35569(int& val) {
    const int x = 35569;
    std::cout << 35569;
    for (int i = 0; i < 35569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35570(int& val) {
    func_in_35570(val);
    const int x = 35570;
    std::cout << 35570;
    for (int i = 0; i < 35570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35570(int& val) {
    const int x = 35570;
    std::cout << 35570;
    for (int i = 0; i < 35570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35571(int& val) {
    func_in_35571(val);
    const int x = 35571;
    std::cout << 35571;
    for (int i = 0; i < 35571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35571(int& val) {
    const int x = 35571;
    std::cout << 35571;
    for (int i = 0; i < 35571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35572(int& val) {
    func_in_35572(val);
    const int x = 35572;
    std::cout << 35572;
    for (int i = 0; i < 35572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35572(int& val) {
    const int x = 35572;
    std::cout << 35572;
    for (int i = 0; i < 35572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35573(int& val) {
    func_in_35573(val);
    const int x = 35573;
    std::cout << 35573;
    for (int i = 0; i < 35573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35573(int& val) {
    const int x = 35573;
    std::cout << 35573;
    for (int i = 0; i < 35573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35574(int& val) {
    func_in_35574(val);
    const int x = 35574;
    std::cout << 35574;
    for (int i = 0; i < 35574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35574(int& val) {
    const int x = 35574;
    std::cout << 35574;
    for (int i = 0; i < 35574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35575(int& val) {
    func_in_35575(val);
    const int x = 35575;
    std::cout << 35575;
    for (int i = 0; i < 35575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35575(int& val) {
    const int x = 35575;
    std::cout << 35575;
    for (int i = 0; i < 35575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35576(int& val) {
    func_in_35576(val);
    const int x = 35576;
    std::cout << 35576;
    for (int i = 0; i < 35576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35576(int& val) {
    const int x = 35576;
    std::cout << 35576;
    for (int i = 0; i < 35576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35577(int& val) {
    func_in_35577(val);
    const int x = 35577;
    std::cout << 35577;
    for (int i = 0; i < 35577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35577(int& val) {
    const int x = 35577;
    std::cout << 35577;
    for (int i = 0; i < 35577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35578(int& val) {
    func_in_35578(val);
    const int x = 35578;
    std::cout << 35578;
    for (int i = 0; i < 35578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35578(int& val) {
    const int x = 35578;
    std::cout << 35578;
    for (int i = 0; i < 35578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35579(int& val) {
    func_in_35579(val);
    const int x = 35579;
    std::cout << 35579;
    for (int i = 0; i < 35579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35579(int& val) {
    const int x = 35579;
    std::cout << 35579;
    for (int i = 0; i < 35579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35580(int& val) {
    func_in_35580(val);
    const int x = 35580;
    std::cout << 35580;
    for (int i = 0; i < 35580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35580(int& val) {
    const int x = 35580;
    std::cout << 35580;
    for (int i = 0; i < 35580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35581(int& val) {
    func_in_35581(val);
    const int x = 35581;
    std::cout << 35581;
    for (int i = 0; i < 35581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35581(int& val) {
    const int x = 35581;
    std::cout << 35581;
    for (int i = 0; i < 35581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35582(int& val) {
    func_in_35582(val);
    const int x = 35582;
    std::cout << 35582;
    for (int i = 0; i < 35582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35582(int& val) {
    const int x = 35582;
    std::cout << 35582;
    for (int i = 0; i < 35582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35583(int& val) {
    func_in_35583(val);
    const int x = 35583;
    std::cout << 35583;
    for (int i = 0; i < 35583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35583(int& val) {
    const int x = 35583;
    std::cout << 35583;
    for (int i = 0; i < 35583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35584(int& val) {
    func_in_35584(val);
    const int x = 35584;
    std::cout << 35584;
    for (int i = 0; i < 35584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35584(int& val) {
    const int x = 35584;
    std::cout << 35584;
    for (int i = 0; i < 35584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35585(int& val) {
    func_in_35585(val);
    const int x = 35585;
    std::cout << 35585;
    for (int i = 0; i < 35585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35585(int& val) {
    const int x = 35585;
    std::cout << 35585;
    for (int i = 0; i < 35585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35586(int& val) {
    func_in_35586(val);
    const int x = 35586;
    std::cout << 35586;
    for (int i = 0; i < 35586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35586(int& val) {
    const int x = 35586;
    std::cout << 35586;
    for (int i = 0; i < 35586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35587(int& val) {
    func_in_35587(val);
    const int x = 35587;
    std::cout << 35587;
    for (int i = 0; i < 35587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35587(int& val) {
    const int x = 35587;
    std::cout << 35587;
    for (int i = 0; i < 35587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35588(int& val) {
    func_in_35588(val);
    const int x = 35588;
    std::cout << 35588;
    for (int i = 0; i < 35588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35588(int& val) {
    const int x = 35588;
    std::cout << 35588;
    for (int i = 0; i < 35588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35589(int& val) {
    func_in_35589(val);
    const int x = 35589;
    std::cout << 35589;
    for (int i = 0; i < 35589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35589(int& val) {
    const int x = 35589;
    std::cout << 35589;
    for (int i = 0; i < 35589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35590(int& val) {
    func_in_35590(val);
    const int x = 35590;
    std::cout << 35590;
    for (int i = 0; i < 35590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35590(int& val) {
    const int x = 35590;
    std::cout << 35590;
    for (int i = 0; i < 35590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35591(int& val) {
    func_in_35591(val);
    const int x = 35591;
    std::cout << 35591;
    for (int i = 0; i < 35591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35591(int& val) {
    const int x = 35591;
    std::cout << 35591;
    for (int i = 0; i < 35591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35592(int& val) {
    func_in_35592(val);
    const int x = 35592;
    std::cout << 35592;
    for (int i = 0; i < 35592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35592(int& val) {
    const int x = 35592;
    std::cout << 35592;
    for (int i = 0; i < 35592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35593(int& val) {
    func_in_35593(val);
    const int x = 35593;
    std::cout << 35593;
    for (int i = 0; i < 35593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35593(int& val) {
    const int x = 35593;
    std::cout << 35593;
    for (int i = 0; i < 35593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35594(int& val) {
    func_in_35594(val);
    const int x = 35594;
    std::cout << 35594;
    for (int i = 0; i < 35594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35594(int& val) {
    const int x = 35594;
    std::cout << 35594;
    for (int i = 0; i < 35594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35595(int& val) {
    func_in_35595(val);
    const int x = 35595;
    std::cout << 35595;
    for (int i = 0; i < 35595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35595(int& val) {
    const int x = 35595;
    std::cout << 35595;
    for (int i = 0; i < 35595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35596(int& val) {
    func_in_35596(val);
    const int x = 35596;
    std::cout << 35596;
    for (int i = 0; i < 35596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35596(int& val) {
    const int x = 35596;
    std::cout << 35596;
    for (int i = 0; i < 35596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35597(int& val) {
    func_in_35597(val);
    const int x = 35597;
    std::cout << 35597;
    for (int i = 0; i < 35597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35597(int& val) {
    const int x = 35597;
    std::cout << 35597;
    for (int i = 0; i < 35597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35598(int& val) {
    func_in_35598(val);
    const int x = 35598;
    std::cout << 35598;
    for (int i = 0; i < 35598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35598(int& val) {
    const int x = 35598;
    std::cout << 35598;
    for (int i = 0; i < 35598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35599(int& val) {
    func_in_35599(val);
    const int x = 35599;
    std::cout << 35599;
    for (int i = 0; i < 35599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35599(int& val) {
    const int x = 35599;
    std::cout << 35599;
    for (int i = 0; i < 35599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35600(int& val) {
    func_in_35600(val);
    const int x = 35600;
    std::cout << 35600;
    for (int i = 0; i < 35600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35600(int& val) {
    const int x = 35600;
    std::cout << 35600;
    for (int i = 0; i < 35600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35601(int& val) {
    func_in_35601(val);
    const int x = 35601;
    std::cout << 35601;
    for (int i = 0; i < 35601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35601(int& val) {
    const int x = 35601;
    std::cout << 35601;
    for (int i = 0; i < 35601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35602(int& val) {
    func_in_35602(val);
    const int x = 35602;
    std::cout << 35602;
    for (int i = 0; i < 35602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35602(int& val) {
    const int x = 35602;
    std::cout << 35602;
    for (int i = 0; i < 35602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35603(int& val) {
    func_in_35603(val);
    const int x = 35603;
    std::cout << 35603;
    for (int i = 0; i < 35603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35603(int& val) {
    const int x = 35603;
    std::cout << 35603;
    for (int i = 0; i < 35603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35604(int& val) {
    func_in_35604(val);
    const int x = 35604;
    std::cout << 35604;
    for (int i = 0; i < 35604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35604(int& val) {
    const int x = 35604;
    std::cout << 35604;
    for (int i = 0; i < 35604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35605(int& val) {
    func_in_35605(val);
    const int x = 35605;
    std::cout << 35605;
    for (int i = 0; i < 35605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35605(int& val) {
    const int x = 35605;
    std::cout << 35605;
    for (int i = 0; i < 35605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35606(int& val) {
    func_in_35606(val);
    const int x = 35606;
    std::cout << 35606;
    for (int i = 0; i < 35606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35606(int& val) {
    const int x = 35606;
    std::cout << 35606;
    for (int i = 0; i < 35606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35607(int& val) {
    func_in_35607(val);
    const int x = 35607;
    std::cout << 35607;
    for (int i = 0; i < 35607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35607(int& val) {
    const int x = 35607;
    std::cout << 35607;
    for (int i = 0; i < 35607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35608(int& val) {
    func_in_35608(val);
    const int x = 35608;
    std::cout << 35608;
    for (int i = 0; i < 35608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35608(int& val) {
    const int x = 35608;
    std::cout << 35608;
    for (int i = 0; i < 35608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35609(int& val) {
    func_in_35609(val);
    const int x = 35609;
    std::cout << 35609;
    for (int i = 0; i < 35609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35609(int& val) {
    const int x = 35609;
    std::cout << 35609;
    for (int i = 0; i < 35609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35610(int& val) {
    func_in_35610(val);
    const int x = 35610;
    std::cout << 35610;
    for (int i = 0; i < 35610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35610(int& val) {
    const int x = 35610;
    std::cout << 35610;
    for (int i = 0; i < 35610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35611(int& val) {
    func_in_35611(val);
    const int x = 35611;
    std::cout << 35611;
    for (int i = 0; i < 35611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35611(int& val) {
    const int x = 35611;
    std::cout << 35611;
    for (int i = 0; i < 35611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35612(int& val) {
    func_in_35612(val);
    const int x = 35612;
    std::cout << 35612;
    for (int i = 0; i < 35612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35612(int& val) {
    const int x = 35612;
    std::cout << 35612;
    for (int i = 0; i < 35612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35613(int& val) {
    func_in_35613(val);
    const int x = 35613;
    std::cout << 35613;
    for (int i = 0; i < 35613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35613(int& val) {
    const int x = 35613;
    std::cout << 35613;
    for (int i = 0; i < 35613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35614(int& val) {
    func_in_35614(val);
    const int x = 35614;
    std::cout << 35614;
    for (int i = 0; i < 35614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35614(int& val) {
    const int x = 35614;
    std::cout << 35614;
    for (int i = 0; i < 35614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35615(int& val) {
    func_in_35615(val);
    const int x = 35615;
    std::cout << 35615;
    for (int i = 0; i < 35615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35615(int& val) {
    const int x = 35615;
    std::cout << 35615;
    for (int i = 0; i < 35615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35616(int& val) {
    func_in_35616(val);
    const int x = 35616;
    std::cout << 35616;
    for (int i = 0; i < 35616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35616(int& val) {
    const int x = 35616;
    std::cout << 35616;
    for (int i = 0; i < 35616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35617(int& val) {
    func_in_35617(val);
    const int x = 35617;
    std::cout << 35617;
    for (int i = 0; i < 35617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35617(int& val) {
    const int x = 35617;
    std::cout << 35617;
    for (int i = 0; i < 35617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35618(int& val) {
    func_in_35618(val);
    const int x = 35618;
    std::cout << 35618;
    for (int i = 0; i < 35618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35618(int& val) {
    const int x = 35618;
    std::cout << 35618;
    for (int i = 0; i < 35618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35619(int& val) {
    func_in_35619(val);
    const int x = 35619;
    std::cout << 35619;
    for (int i = 0; i < 35619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35619(int& val) {
    const int x = 35619;
    std::cout << 35619;
    for (int i = 0; i < 35619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35620(int& val) {
    func_in_35620(val);
    const int x = 35620;
    std::cout << 35620;
    for (int i = 0; i < 35620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35620(int& val) {
    const int x = 35620;
    std::cout << 35620;
    for (int i = 0; i < 35620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35621(int& val) {
    func_in_35621(val);
    const int x = 35621;
    std::cout << 35621;
    for (int i = 0; i < 35621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35621(int& val) {
    const int x = 35621;
    std::cout << 35621;
    for (int i = 0; i < 35621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35622(int& val) {
    func_in_35622(val);
    const int x = 35622;
    std::cout << 35622;
    for (int i = 0; i < 35622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35622(int& val) {
    const int x = 35622;
    std::cout << 35622;
    for (int i = 0; i < 35622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35623(int& val) {
    func_in_35623(val);
    const int x = 35623;
    std::cout << 35623;
    for (int i = 0; i < 35623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35623(int& val) {
    const int x = 35623;
    std::cout << 35623;
    for (int i = 0; i < 35623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35624(int& val) {
    func_in_35624(val);
    const int x = 35624;
    std::cout << 35624;
    for (int i = 0; i < 35624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35624(int& val) {
    const int x = 35624;
    std::cout << 35624;
    for (int i = 0; i < 35624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35625(int& val) {
    func_in_35625(val);
    const int x = 35625;
    std::cout << 35625;
    for (int i = 0; i < 35625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35625(int& val) {
    const int x = 35625;
    std::cout << 35625;
    for (int i = 0; i < 35625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35626(int& val) {
    func_in_35626(val);
    const int x = 35626;
    std::cout << 35626;
    for (int i = 0; i < 35626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35626(int& val) {
    const int x = 35626;
    std::cout << 35626;
    for (int i = 0; i < 35626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35627(int& val) {
    func_in_35627(val);
    const int x = 35627;
    std::cout << 35627;
    for (int i = 0; i < 35627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35627(int& val) {
    const int x = 35627;
    std::cout << 35627;
    for (int i = 0; i < 35627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35628(int& val) {
    func_in_35628(val);
    const int x = 35628;
    std::cout << 35628;
    for (int i = 0; i < 35628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35628(int& val) {
    const int x = 35628;
    std::cout << 35628;
    for (int i = 0; i < 35628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35629(int& val) {
    func_in_35629(val);
    const int x = 35629;
    std::cout << 35629;
    for (int i = 0; i < 35629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35629(int& val) {
    const int x = 35629;
    std::cout << 35629;
    for (int i = 0; i < 35629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35630(int& val) {
    func_in_35630(val);
    const int x = 35630;
    std::cout << 35630;
    for (int i = 0; i < 35630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35630(int& val) {
    const int x = 35630;
    std::cout << 35630;
    for (int i = 0; i < 35630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35631(int& val) {
    func_in_35631(val);
    const int x = 35631;
    std::cout << 35631;
    for (int i = 0; i < 35631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35631(int& val) {
    const int x = 35631;
    std::cout << 35631;
    for (int i = 0; i < 35631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35632(int& val) {
    func_in_35632(val);
    const int x = 35632;
    std::cout << 35632;
    for (int i = 0; i < 35632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35632(int& val) {
    const int x = 35632;
    std::cout << 35632;
    for (int i = 0; i < 35632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35633(int& val) {
    func_in_35633(val);
    const int x = 35633;
    std::cout << 35633;
    for (int i = 0; i < 35633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35633(int& val) {
    const int x = 35633;
    std::cout << 35633;
    for (int i = 0; i < 35633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35634(int& val) {
    func_in_35634(val);
    const int x = 35634;
    std::cout << 35634;
    for (int i = 0; i < 35634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35634(int& val) {
    const int x = 35634;
    std::cout << 35634;
    for (int i = 0; i < 35634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35635(int& val) {
    func_in_35635(val);
    const int x = 35635;
    std::cout << 35635;
    for (int i = 0; i < 35635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35635(int& val) {
    const int x = 35635;
    std::cout << 35635;
    for (int i = 0; i < 35635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35636(int& val) {
    func_in_35636(val);
    const int x = 35636;
    std::cout << 35636;
    for (int i = 0; i < 35636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35636(int& val) {
    const int x = 35636;
    std::cout << 35636;
    for (int i = 0; i < 35636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35637(int& val) {
    func_in_35637(val);
    const int x = 35637;
    std::cout << 35637;
    for (int i = 0; i < 35637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35637(int& val) {
    const int x = 35637;
    std::cout << 35637;
    for (int i = 0; i < 35637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35638(int& val) {
    func_in_35638(val);
    const int x = 35638;
    std::cout << 35638;
    for (int i = 0; i < 35638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35638(int& val) {
    const int x = 35638;
    std::cout << 35638;
    for (int i = 0; i < 35638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35639(int& val) {
    func_in_35639(val);
    const int x = 35639;
    std::cout << 35639;
    for (int i = 0; i < 35639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35639(int& val) {
    const int x = 35639;
    std::cout << 35639;
    for (int i = 0; i < 35639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35640(int& val) {
    func_in_35640(val);
    const int x = 35640;
    std::cout << 35640;
    for (int i = 0; i < 35640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35640(int& val) {
    const int x = 35640;
    std::cout << 35640;
    for (int i = 0; i < 35640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35641(int& val) {
    func_in_35641(val);
    const int x = 35641;
    std::cout << 35641;
    for (int i = 0; i < 35641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35641(int& val) {
    const int x = 35641;
    std::cout << 35641;
    for (int i = 0; i < 35641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35642(int& val) {
    func_in_35642(val);
    const int x = 35642;
    std::cout << 35642;
    for (int i = 0; i < 35642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35642(int& val) {
    const int x = 35642;
    std::cout << 35642;
    for (int i = 0; i < 35642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35643(int& val) {
    func_in_35643(val);
    const int x = 35643;
    std::cout << 35643;
    for (int i = 0; i < 35643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35643(int& val) {
    const int x = 35643;
    std::cout << 35643;
    for (int i = 0; i < 35643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35644(int& val) {
    func_in_35644(val);
    const int x = 35644;
    std::cout << 35644;
    for (int i = 0; i < 35644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35644(int& val) {
    const int x = 35644;
    std::cout << 35644;
    for (int i = 0; i < 35644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35645(int& val) {
    func_in_35645(val);
    const int x = 35645;
    std::cout << 35645;
    for (int i = 0; i < 35645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35645(int& val) {
    const int x = 35645;
    std::cout << 35645;
    for (int i = 0; i < 35645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35646(int& val) {
    func_in_35646(val);
    const int x = 35646;
    std::cout << 35646;
    for (int i = 0; i < 35646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35646(int& val) {
    const int x = 35646;
    std::cout << 35646;
    for (int i = 0; i < 35646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35647(int& val) {
    func_in_35647(val);
    const int x = 35647;
    std::cout << 35647;
    for (int i = 0; i < 35647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35647(int& val) {
    const int x = 35647;
    std::cout << 35647;
    for (int i = 0; i < 35647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35648(int& val) {
    func_in_35648(val);
    const int x = 35648;
    std::cout << 35648;
    for (int i = 0; i < 35648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35648(int& val) {
    const int x = 35648;
    std::cout << 35648;
    for (int i = 0; i < 35648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35649(int& val) {
    func_in_35649(val);
    const int x = 35649;
    std::cout << 35649;
    for (int i = 0; i < 35649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35649(int& val) {
    const int x = 35649;
    std::cout << 35649;
    for (int i = 0; i < 35649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35650(int& val) {
    func_in_35650(val);
    const int x = 35650;
    std::cout << 35650;
    for (int i = 0; i < 35650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35650(int& val) {
    const int x = 35650;
    std::cout << 35650;
    for (int i = 0; i < 35650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35651(int& val) {
    func_in_35651(val);
    const int x = 35651;
    std::cout << 35651;
    for (int i = 0; i < 35651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35651(int& val) {
    const int x = 35651;
    std::cout << 35651;
    for (int i = 0; i < 35651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35652(int& val) {
    func_in_35652(val);
    const int x = 35652;
    std::cout << 35652;
    for (int i = 0; i < 35652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35652(int& val) {
    const int x = 35652;
    std::cout << 35652;
    for (int i = 0; i < 35652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35653(int& val) {
    func_in_35653(val);
    const int x = 35653;
    std::cout << 35653;
    for (int i = 0; i < 35653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35653(int& val) {
    const int x = 35653;
    std::cout << 35653;
    for (int i = 0; i < 35653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35654(int& val) {
    func_in_35654(val);
    const int x = 35654;
    std::cout << 35654;
    for (int i = 0; i < 35654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35654(int& val) {
    const int x = 35654;
    std::cout << 35654;
    for (int i = 0; i < 35654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35655(int& val) {
    func_in_35655(val);
    const int x = 35655;
    std::cout << 35655;
    for (int i = 0; i < 35655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35655(int& val) {
    const int x = 35655;
    std::cout << 35655;
    for (int i = 0; i < 35655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35656(int& val) {
    func_in_35656(val);
    const int x = 35656;
    std::cout << 35656;
    for (int i = 0; i < 35656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35656(int& val) {
    const int x = 35656;
    std::cout << 35656;
    for (int i = 0; i < 35656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35657(int& val) {
    func_in_35657(val);
    const int x = 35657;
    std::cout << 35657;
    for (int i = 0; i < 35657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35657(int& val) {
    const int x = 35657;
    std::cout << 35657;
    for (int i = 0; i < 35657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35658(int& val) {
    func_in_35658(val);
    const int x = 35658;
    std::cout << 35658;
    for (int i = 0; i < 35658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35658(int& val) {
    const int x = 35658;
    std::cout << 35658;
    for (int i = 0; i < 35658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35659(int& val) {
    func_in_35659(val);
    const int x = 35659;
    std::cout << 35659;
    for (int i = 0; i < 35659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35659(int& val) {
    const int x = 35659;
    std::cout << 35659;
    for (int i = 0; i < 35659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35660(int& val) {
    func_in_35660(val);
    const int x = 35660;
    std::cout << 35660;
    for (int i = 0; i < 35660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35660(int& val) {
    const int x = 35660;
    std::cout << 35660;
    for (int i = 0; i < 35660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35661(int& val) {
    func_in_35661(val);
    const int x = 35661;
    std::cout << 35661;
    for (int i = 0; i < 35661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35661(int& val) {
    const int x = 35661;
    std::cout << 35661;
    for (int i = 0; i < 35661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35662(int& val) {
    func_in_35662(val);
    const int x = 35662;
    std::cout << 35662;
    for (int i = 0; i < 35662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35662(int& val) {
    const int x = 35662;
    std::cout << 35662;
    for (int i = 0; i < 35662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35663(int& val) {
    func_in_35663(val);
    const int x = 35663;
    std::cout << 35663;
    for (int i = 0; i < 35663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35663(int& val) {
    const int x = 35663;
    std::cout << 35663;
    for (int i = 0; i < 35663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35664(int& val) {
    func_in_35664(val);
    const int x = 35664;
    std::cout << 35664;
    for (int i = 0; i < 35664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35664(int& val) {
    const int x = 35664;
    std::cout << 35664;
    for (int i = 0; i < 35664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35665(int& val) {
    func_in_35665(val);
    const int x = 35665;
    std::cout << 35665;
    for (int i = 0; i < 35665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35665(int& val) {
    const int x = 35665;
    std::cout << 35665;
    for (int i = 0; i < 35665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35666(int& val) {
    func_in_35666(val);
    const int x = 35666;
    std::cout << 35666;
    for (int i = 0; i < 35666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35666(int& val) {
    const int x = 35666;
    std::cout << 35666;
    for (int i = 0; i < 35666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35667(int& val) {
    func_in_35667(val);
    const int x = 35667;
    std::cout << 35667;
    for (int i = 0; i < 35667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35667(int& val) {
    const int x = 35667;
    std::cout << 35667;
    for (int i = 0; i < 35667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35668(int& val) {
    func_in_35668(val);
    const int x = 35668;
    std::cout << 35668;
    for (int i = 0; i < 35668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35668(int& val) {
    const int x = 35668;
    std::cout << 35668;
    for (int i = 0; i < 35668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35669(int& val) {
    func_in_35669(val);
    const int x = 35669;
    std::cout << 35669;
    for (int i = 0; i < 35669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35669(int& val) {
    const int x = 35669;
    std::cout << 35669;
    for (int i = 0; i < 35669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35670(int& val) {
    func_in_35670(val);
    const int x = 35670;
    std::cout << 35670;
    for (int i = 0; i < 35670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35670(int& val) {
    const int x = 35670;
    std::cout << 35670;
    for (int i = 0; i < 35670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35671(int& val) {
    func_in_35671(val);
    const int x = 35671;
    std::cout << 35671;
    for (int i = 0; i < 35671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35671(int& val) {
    const int x = 35671;
    std::cout << 35671;
    for (int i = 0; i < 35671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35672(int& val) {
    func_in_35672(val);
    const int x = 35672;
    std::cout << 35672;
    for (int i = 0; i < 35672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35672(int& val) {
    const int x = 35672;
    std::cout << 35672;
    for (int i = 0; i < 35672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35673(int& val) {
    func_in_35673(val);
    const int x = 35673;
    std::cout << 35673;
    for (int i = 0; i < 35673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35673(int& val) {
    const int x = 35673;
    std::cout << 35673;
    for (int i = 0; i < 35673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35674(int& val) {
    func_in_35674(val);
    const int x = 35674;
    std::cout << 35674;
    for (int i = 0; i < 35674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35674(int& val) {
    const int x = 35674;
    std::cout << 35674;
    for (int i = 0; i < 35674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35675(int& val) {
    func_in_35675(val);
    const int x = 35675;
    std::cout << 35675;
    for (int i = 0; i < 35675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35675(int& val) {
    const int x = 35675;
    std::cout << 35675;
    for (int i = 0; i < 35675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35676(int& val) {
    func_in_35676(val);
    const int x = 35676;
    std::cout << 35676;
    for (int i = 0; i < 35676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35676(int& val) {
    const int x = 35676;
    std::cout << 35676;
    for (int i = 0; i < 35676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35677(int& val) {
    func_in_35677(val);
    const int x = 35677;
    std::cout << 35677;
    for (int i = 0; i < 35677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35677(int& val) {
    const int x = 35677;
    std::cout << 35677;
    for (int i = 0; i < 35677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35678(int& val) {
    func_in_35678(val);
    const int x = 35678;
    std::cout << 35678;
    for (int i = 0; i < 35678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35678(int& val) {
    const int x = 35678;
    std::cout << 35678;
    for (int i = 0; i < 35678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35679(int& val) {
    func_in_35679(val);
    const int x = 35679;
    std::cout << 35679;
    for (int i = 0; i < 35679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35679(int& val) {
    const int x = 35679;
    std::cout << 35679;
    for (int i = 0; i < 35679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35680(int& val) {
    func_in_35680(val);
    const int x = 35680;
    std::cout << 35680;
    for (int i = 0; i < 35680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35680(int& val) {
    const int x = 35680;
    std::cout << 35680;
    for (int i = 0; i < 35680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35681(int& val) {
    func_in_35681(val);
    const int x = 35681;
    std::cout << 35681;
    for (int i = 0; i < 35681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35681(int& val) {
    const int x = 35681;
    std::cout << 35681;
    for (int i = 0; i < 35681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35682(int& val) {
    func_in_35682(val);
    const int x = 35682;
    std::cout << 35682;
    for (int i = 0; i < 35682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35682(int& val) {
    const int x = 35682;
    std::cout << 35682;
    for (int i = 0; i < 35682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35683(int& val) {
    func_in_35683(val);
    const int x = 35683;
    std::cout << 35683;
    for (int i = 0; i < 35683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35683(int& val) {
    const int x = 35683;
    std::cout << 35683;
    for (int i = 0; i < 35683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35684(int& val) {
    func_in_35684(val);
    const int x = 35684;
    std::cout << 35684;
    for (int i = 0; i < 35684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35684(int& val) {
    const int x = 35684;
    std::cout << 35684;
    for (int i = 0; i < 35684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35685(int& val) {
    func_in_35685(val);
    const int x = 35685;
    std::cout << 35685;
    for (int i = 0; i < 35685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35685(int& val) {
    const int x = 35685;
    std::cout << 35685;
    for (int i = 0; i < 35685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35686(int& val) {
    func_in_35686(val);
    const int x = 35686;
    std::cout << 35686;
    for (int i = 0; i < 35686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35686(int& val) {
    const int x = 35686;
    std::cout << 35686;
    for (int i = 0; i < 35686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35687(int& val) {
    func_in_35687(val);
    const int x = 35687;
    std::cout << 35687;
    for (int i = 0; i < 35687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35687(int& val) {
    const int x = 35687;
    std::cout << 35687;
    for (int i = 0; i < 35687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35688(int& val) {
    func_in_35688(val);
    const int x = 35688;
    std::cout << 35688;
    for (int i = 0; i < 35688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35688(int& val) {
    const int x = 35688;
    std::cout << 35688;
    for (int i = 0; i < 35688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35689(int& val) {
    func_in_35689(val);
    const int x = 35689;
    std::cout << 35689;
    for (int i = 0; i < 35689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35689(int& val) {
    const int x = 35689;
    std::cout << 35689;
    for (int i = 0; i < 35689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35690(int& val) {
    func_in_35690(val);
    const int x = 35690;
    std::cout << 35690;
    for (int i = 0; i < 35690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35690(int& val) {
    const int x = 35690;
    std::cout << 35690;
    for (int i = 0; i < 35690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35691(int& val) {
    func_in_35691(val);
    const int x = 35691;
    std::cout << 35691;
    for (int i = 0; i < 35691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35691(int& val) {
    const int x = 35691;
    std::cout << 35691;
    for (int i = 0; i < 35691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35692(int& val) {
    func_in_35692(val);
    const int x = 35692;
    std::cout << 35692;
    for (int i = 0; i < 35692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35692(int& val) {
    const int x = 35692;
    std::cout << 35692;
    for (int i = 0; i < 35692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35693(int& val) {
    func_in_35693(val);
    const int x = 35693;
    std::cout << 35693;
    for (int i = 0; i < 35693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35693(int& val) {
    const int x = 35693;
    std::cout << 35693;
    for (int i = 0; i < 35693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35694(int& val) {
    func_in_35694(val);
    const int x = 35694;
    std::cout << 35694;
    for (int i = 0; i < 35694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35694(int& val) {
    const int x = 35694;
    std::cout << 35694;
    for (int i = 0; i < 35694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35695(int& val) {
    func_in_35695(val);
    const int x = 35695;
    std::cout << 35695;
    for (int i = 0; i < 35695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35695(int& val) {
    const int x = 35695;
    std::cout << 35695;
    for (int i = 0; i < 35695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35696(int& val) {
    func_in_35696(val);
    const int x = 35696;
    std::cout << 35696;
    for (int i = 0; i < 35696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35696(int& val) {
    const int x = 35696;
    std::cout << 35696;
    for (int i = 0; i < 35696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35697(int& val) {
    func_in_35697(val);
    const int x = 35697;
    std::cout << 35697;
    for (int i = 0; i < 35697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35697(int& val) {
    const int x = 35697;
    std::cout << 35697;
    for (int i = 0; i < 35697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35698(int& val) {
    func_in_35698(val);
    const int x = 35698;
    std::cout << 35698;
    for (int i = 0; i < 35698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35698(int& val) {
    const int x = 35698;
    std::cout << 35698;
    for (int i = 0; i < 35698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35699(int& val) {
    func_in_35699(val);
    const int x = 35699;
    std::cout << 35699;
    for (int i = 0; i < 35699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35699(int& val) {
    const int x = 35699;
    std::cout << 35699;
    for (int i = 0; i < 35699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35700(int& val) {
    func_in_35700(val);
    const int x = 35700;
    std::cout << 35700;
    for (int i = 0; i < 35700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35700(int& val) {
    const int x = 35700;
    std::cout << 35700;
    for (int i = 0; i < 35700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35701(int& val) {
    func_in_35701(val);
    const int x = 35701;
    std::cout << 35701;
    for (int i = 0; i < 35701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35701(int& val) {
    const int x = 35701;
    std::cout << 35701;
    for (int i = 0; i < 35701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35702(int& val) {
    func_in_35702(val);
    const int x = 35702;
    std::cout << 35702;
    for (int i = 0; i < 35702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35702(int& val) {
    const int x = 35702;
    std::cout << 35702;
    for (int i = 0; i < 35702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35703(int& val) {
    func_in_35703(val);
    const int x = 35703;
    std::cout << 35703;
    for (int i = 0; i < 35703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35703(int& val) {
    const int x = 35703;
    std::cout << 35703;
    for (int i = 0; i < 35703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35704(int& val) {
    func_in_35704(val);
    const int x = 35704;
    std::cout << 35704;
    for (int i = 0; i < 35704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35704(int& val) {
    const int x = 35704;
    std::cout << 35704;
    for (int i = 0; i < 35704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35705(int& val) {
    func_in_35705(val);
    const int x = 35705;
    std::cout << 35705;
    for (int i = 0; i < 35705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35705(int& val) {
    const int x = 35705;
    std::cout << 35705;
    for (int i = 0; i < 35705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35706(int& val) {
    func_in_35706(val);
    const int x = 35706;
    std::cout << 35706;
    for (int i = 0; i < 35706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35706(int& val) {
    const int x = 35706;
    std::cout << 35706;
    for (int i = 0; i < 35706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35707(int& val) {
    func_in_35707(val);
    const int x = 35707;
    std::cout << 35707;
    for (int i = 0; i < 35707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35707(int& val) {
    const int x = 35707;
    std::cout << 35707;
    for (int i = 0; i < 35707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35708(int& val) {
    func_in_35708(val);
    const int x = 35708;
    std::cout << 35708;
    for (int i = 0; i < 35708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35708(int& val) {
    const int x = 35708;
    std::cout << 35708;
    for (int i = 0; i < 35708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35709(int& val) {
    func_in_35709(val);
    const int x = 35709;
    std::cout << 35709;
    for (int i = 0; i < 35709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35709(int& val) {
    const int x = 35709;
    std::cout << 35709;
    for (int i = 0; i < 35709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35710(int& val) {
    func_in_35710(val);
    const int x = 35710;
    std::cout << 35710;
    for (int i = 0; i < 35710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35710(int& val) {
    const int x = 35710;
    std::cout << 35710;
    for (int i = 0; i < 35710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35711(int& val) {
    func_in_35711(val);
    const int x = 35711;
    std::cout << 35711;
    for (int i = 0; i < 35711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35711(int& val) {
    const int x = 35711;
    std::cout << 35711;
    for (int i = 0; i < 35711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35712(int& val) {
    func_in_35712(val);
    const int x = 35712;
    std::cout << 35712;
    for (int i = 0; i < 35712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35712(int& val) {
    const int x = 35712;
    std::cout << 35712;
    for (int i = 0; i < 35712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35713(int& val) {
    func_in_35713(val);
    const int x = 35713;
    std::cout << 35713;
    for (int i = 0; i < 35713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35713(int& val) {
    const int x = 35713;
    std::cout << 35713;
    for (int i = 0; i < 35713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35714(int& val) {
    func_in_35714(val);
    const int x = 35714;
    std::cout << 35714;
    for (int i = 0; i < 35714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35714(int& val) {
    const int x = 35714;
    std::cout << 35714;
    for (int i = 0; i < 35714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35715(int& val) {
    func_in_35715(val);
    const int x = 35715;
    std::cout << 35715;
    for (int i = 0; i < 35715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35715(int& val) {
    const int x = 35715;
    std::cout << 35715;
    for (int i = 0; i < 35715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35716(int& val) {
    func_in_35716(val);
    const int x = 35716;
    std::cout << 35716;
    for (int i = 0; i < 35716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35716(int& val) {
    const int x = 35716;
    std::cout << 35716;
    for (int i = 0; i < 35716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35717(int& val) {
    func_in_35717(val);
    const int x = 35717;
    std::cout << 35717;
    for (int i = 0; i < 35717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35717(int& val) {
    const int x = 35717;
    std::cout << 35717;
    for (int i = 0; i < 35717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35718(int& val) {
    func_in_35718(val);
    const int x = 35718;
    std::cout << 35718;
    for (int i = 0; i < 35718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35718(int& val) {
    const int x = 35718;
    std::cout << 35718;
    for (int i = 0; i < 35718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35719(int& val) {
    func_in_35719(val);
    const int x = 35719;
    std::cout << 35719;
    for (int i = 0; i < 35719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35719(int& val) {
    const int x = 35719;
    std::cout << 35719;
    for (int i = 0; i < 35719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35720(int& val) {
    func_in_35720(val);
    const int x = 35720;
    std::cout << 35720;
    for (int i = 0; i < 35720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35720(int& val) {
    const int x = 35720;
    std::cout << 35720;
    for (int i = 0; i < 35720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35721(int& val) {
    func_in_35721(val);
    const int x = 35721;
    std::cout << 35721;
    for (int i = 0; i < 35721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35721(int& val) {
    const int x = 35721;
    std::cout << 35721;
    for (int i = 0; i < 35721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35722(int& val) {
    func_in_35722(val);
    const int x = 35722;
    std::cout << 35722;
    for (int i = 0; i < 35722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35722(int& val) {
    const int x = 35722;
    std::cout << 35722;
    for (int i = 0; i < 35722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35723(int& val) {
    func_in_35723(val);
    const int x = 35723;
    std::cout << 35723;
    for (int i = 0; i < 35723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35723(int& val) {
    const int x = 35723;
    std::cout << 35723;
    for (int i = 0; i < 35723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35724(int& val) {
    func_in_35724(val);
    const int x = 35724;
    std::cout << 35724;
    for (int i = 0; i < 35724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35724(int& val) {
    const int x = 35724;
    std::cout << 35724;
    for (int i = 0; i < 35724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35725(int& val) {
    func_in_35725(val);
    const int x = 35725;
    std::cout << 35725;
    for (int i = 0; i < 35725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35725(int& val) {
    const int x = 35725;
    std::cout << 35725;
    for (int i = 0; i < 35725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35726(int& val) {
    func_in_35726(val);
    const int x = 35726;
    std::cout << 35726;
    for (int i = 0; i < 35726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35726(int& val) {
    const int x = 35726;
    std::cout << 35726;
    for (int i = 0; i < 35726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35727(int& val) {
    func_in_35727(val);
    const int x = 35727;
    std::cout << 35727;
    for (int i = 0; i < 35727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35727(int& val) {
    const int x = 35727;
    std::cout << 35727;
    for (int i = 0; i < 35727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35728(int& val) {
    func_in_35728(val);
    const int x = 35728;
    std::cout << 35728;
    for (int i = 0; i < 35728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35728(int& val) {
    const int x = 35728;
    std::cout << 35728;
    for (int i = 0; i < 35728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35729(int& val) {
    func_in_35729(val);
    const int x = 35729;
    std::cout << 35729;
    for (int i = 0; i < 35729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35729(int& val) {
    const int x = 35729;
    std::cout << 35729;
    for (int i = 0; i < 35729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35730(int& val) {
    func_in_35730(val);
    const int x = 35730;
    std::cout << 35730;
    for (int i = 0; i < 35730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35730(int& val) {
    const int x = 35730;
    std::cout << 35730;
    for (int i = 0; i < 35730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35731(int& val) {
    func_in_35731(val);
    const int x = 35731;
    std::cout << 35731;
    for (int i = 0; i < 35731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35731(int& val) {
    const int x = 35731;
    std::cout << 35731;
    for (int i = 0; i < 35731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35732(int& val) {
    func_in_35732(val);
    const int x = 35732;
    std::cout << 35732;
    for (int i = 0; i < 35732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35732(int& val) {
    const int x = 35732;
    std::cout << 35732;
    for (int i = 0; i < 35732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35733(int& val) {
    func_in_35733(val);
    const int x = 35733;
    std::cout << 35733;
    for (int i = 0; i < 35733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35733(int& val) {
    const int x = 35733;
    std::cout << 35733;
    for (int i = 0; i < 35733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35734(int& val) {
    func_in_35734(val);
    const int x = 35734;
    std::cout << 35734;
    for (int i = 0; i < 35734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35734(int& val) {
    const int x = 35734;
    std::cout << 35734;
    for (int i = 0; i < 35734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35735(int& val) {
    func_in_35735(val);
    const int x = 35735;
    std::cout << 35735;
    for (int i = 0; i < 35735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35735(int& val) {
    const int x = 35735;
    std::cout << 35735;
    for (int i = 0; i < 35735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35736(int& val) {
    func_in_35736(val);
    const int x = 35736;
    std::cout << 35736;
    for (int i = 0; i < 35736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35736(int& val) {
    const int x = 35736;
    std::cout << 35736;
    for (int i = 0; i < 35736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35737(int& val) {
    func_in_35737(val);
    const int x = 35737;
    std::cout << 35737;
    for (int i = 0; i < 35737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35737(int& val) {
    const int x = 35737;
    std::cout << 35737;
    for (int i = 0; i < 35737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35738(int& val) {
    func_in_35738(val);
    const int x = 35738;
    std::cout << 35738;
    for (int i = 0; i < 35738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35738(int& val) {
    const int x = 35738;
    std::cout << 35738;
    for (int i = 0; i < 35738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35739(int& val) {
    func_in_35739(val);
    const int x = 35739;
    std::cout << 35739;
    for (int i = 0; i < 35739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35739(int& val) {
    const int x = 35739;
    std::cout << 35739;
    for (int i = 0; i < 35739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35740(int& val) {
    func_in_35740(val);
    const int x = 35740;
    std::cout << 35740;
    for (int i = 0; i < 35740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35740(int& val) {
    const int x = 35740;
    std::cout << 35740;
    for (int i = 0; i < 35740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35741(int& val) {
    func_in_35741(val);
    const int x = 35741;
    std::cout << 35741;
    for (int i = 0; i < 35741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35741(int& val) {
    const int x = 35741;
    std::cout << 35741;
    for (int i = 0; i < 35741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35742(int& val) {
    func_in_35742(val);
    const int x = 35742;
    std::cout << 35742;
    for (int i = 0; i < 35742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35742(int& val) {
    const int x = 35742;
    std::cout << 35742;
    for (int i = 0; i < 35742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35743(int& val) {
    func_in_35743(val);
    const int x = 35743;
    std::cout << 35743;
    for (int i = 0; i < 35743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35743(int& val) {
    const int x = 35743;
    std::cout << 35743;
    for (int i = 0; i < 35743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35744(int& val) {
    func_in_35744(val);
    const int x = 35744;
    std::cout << 35744;
    for (int i = 0; i < 35744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35744(int& val) {
    const int x = 35744;
    std::cout << 35744;
    for (int i = 0; i < 35744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35745(int& val) {
    func_in_35745(val);
    const int x = 35745;
    std::cout << 35745;
    for (int i = 0; i < 35745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35745(int& val) {
    const int x = 35745;
    std::cout << 35745;
    for (int i = 0; i < 35745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35746(int& val) {
    func_in_35746(val);
    const int x = 35746;
    std::cout << 35746;
    for (int i = 0; i < 35746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35746(int& val) {
    const int x = 35746;
    std::cout << 35746;
    for (int i = 0; i < 35746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35747(int& val) {
    func_in_35747(val);
    const int x = 35747;
    std::cout << 35747;
    for (int i = 0; i < 35747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35747(int& val) {
    const int x = 35747;
    std::cout << 35747;
    for (int i = 0; i < 35747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35748(int& val) {
    func_in_35748(val);
    const int x = 35748;
    std::cout << 35748;
    for (int i = 0; i < 35748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35748(int& val) {
    const int x = 35748;
    std::cout << 35748;
    for (int i = 0; i < 35748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35749(int& val) {
    func_in_35749(val);
    const int x = 35749;
    std::cout << 35749;
    for (int i = 0; i < 35749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35749(int& val) {
    const int x = 35749;
    std::cout << 35749;
    for (int i = 0; i < 35749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35750(int& val) {
    func_in_35750(val);
    const int x = 35750;
    std::cout << 35750;
    for (int i = 0; i < 35750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35750(int& val) {
    const int x = 35750;
    std::cout << 35750;
    for (int i = 0; i < 35750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35751(int& val) {
    func_in_35751(val);
    const int x = 35751;
    std::cout << 35751;
    for (int i = 0; i < 35751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35751(int& val) {
    const int x = 35751;
    std::cout << 35751;
    for (int i = 0; i < 35751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35752(int& val) {
    func_in_35752(val);
    const int x = 35752;
    std::cout << 35752;
    for (int i = 0; i < 35752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35752(int& val) {
    const int x = 35752;
    std::cout << 35752;
    for (int i = 0; i < 35752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35753(int& val) {
    func_in_35753(val);
    const int x = 35753;
    std::cout << 35753;
    for (int i = 0; i < 35753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35753(int& val) {
    const int x = 35753;
    std::cout << 35753;
    for (int i = 0; i < 35753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35754(int& val) {
    func_in_35754(val);
    const int x = 35754;
    std::cout << 35754;
    for (int i = 0; i < 35754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35754(int& val) {
    const int x = 35754;
    std::cout << 35754;
    for (int i = 0; i < 35754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35755(int& val) {
    func_in_35755(val);
    const int x = 35755;
    std::cout << 35755;
    for (int i = 0; i < 35755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35755(int& val) {
    const int x = 35755;
    std::cout << 35755;
    for (int i = 0; i < 35755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35756(int& val) {
    func_in_35756(val);
    const int x = 35756;
    std::cout << 35756;
    for (int i = 0; i < 35756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35756(int& val) {
    const int x = 35756;
    std::cout << 35756;
    for (int i = 0; i < 35756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35757(int& val) {
    func_in_35757(val);
    const int x = 35757;
    std::cout << 35757;
    for (int i = 0; i < 35757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35757(int& val) {
    const int x = 35757;
    std::cout << 35757;
    for (int i = 0; i < 35757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35758(int& val) {
    func_in_35758(val);
    const int x = 35758;
    std::cout << 35758;
    for (int i = 0; i < 35758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35758(int& val) {
    const int x = 35758;
    std::cout << 35758;
    for (int i = 0; i < 35758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35759(int& val) {
    func_in_35759(val);
    const int x = 35759;
    std::cout << 35759;
    for (int i = 0; i < 35759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35759(int& val) {
    const int x = 35759;
    std::cout << 35759;
    for (int i = 0; i < 35759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35760(int& val) {
    func_in_35760(val);
    const int x = 35760;
    std::cout << 35760;
    for (int i = 0; i < 35760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35760(int& val) {
    const int x = 35760;
    std::cout << 35760;
    for (int i = 0; i < 35760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35761(int& val) {
    func_in_35761(val);
    const int x = 35761;
    std::cout << 35761;
    for (int i = 0; i < 35761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35761(int& val) {
    const int x = 35761;
    std::cout << 35761;
    for (int i = 0; i < 35761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35762(int& val) {
    func_in_35762(val);
    const int x = 35762;
    std::cout << 35762;
    for (int i = 0; i < 35762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35762(int& val) {
    const int x = 35762;
    std::cout << 35762;
    for (int i = 0; i < 35762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35763(int& val) {
    func_in_35763(val);
    const int x = 35763;
    std::cout << 35763;
    for (int i = 0; i < 35763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35763(int& val) {
    const int x = 35763;
    std::cout << 35763;
    for (int i = 0; i < 35763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35764(int& val) {
    func_in_35764(val);
    const int x = 35764;
    std::cout << 35764;
    for (int i = 0; i < 35764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35764(int& val) {
    const int x = 35764;
    std::cout << 35764;
    for (int i = 0; i < 35764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35765(int& val) {
    func_in_35765(val);
    const int x = 35765;
    std::cout << 35765;
    for (int i = 0; i < 35765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35765(int& val) {
    const int x = 35765;
    std::cout << 35765;
    for (int i = 0; i < 35765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35766(int& val) {
    func_in_35766(val);
    const int x = 35766;
    std::cout << 35766;
    for (int i = 0; i < 35766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35766(int& val) {
    const int x = 35766;
    std::cout << 35766;
    for (int i = 0; i < 35766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35767(int& val) {
    func_in_35767(val);
    const int x = 35767;
    std::cout << 35767;
    for (int i = 0; i < 35767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35767(int& val) {
    const int x = 35767;
    std::cout << 35767;
    for (int i = 0; i < 35767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35768(int& val) {
    func_in_35768(val);
    const int x = 35768;
    std::cout << 35768;
    for (int i = 0; i < 35768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35768(int& val) {
    const int x = 35768;
    std::cout << 35768;
    for (int i = 0; i < 35768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35769(int& val) {
    func_in_35769(val);
    const int x = 35769;
    std::cout << 35769;
    for (int i = 0; i < 35769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35769(int& val) {
    const int x = 35769;
    std::cout << 35769;
    for (int i = 0; i < 35769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35770(int& val) {
    func_in_35770(val);
    const int x = 35770;
    std::cout << 35770;
    for (int i = 0; i < 35770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35770(int& val) {
    const int x = 35770;
    std::cout << 35770;
    for (int i = 0; i < 35770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35771(int& val) {
    func_in_35771(val);
    const int x = 35771;
    std::cout << 35771;
    for (int i = 0; i < 35771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35771(int& val) {
    const int x = 35771;
    std::cout << 35771;
    for (int i = 0; i < 35771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35772(int& val) {
    func_in_35772(val);
    const int x = 35772;
    std::cout << 35772;
    for (int i = 0; i < 35772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35772(int& val) {
    const int x = 35772;
    std::cout << 35772;
    for (int i = 0; i < 35772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35773(int& val) {
    func_in_35773(val);
    const int x = 35773;
    std::cout << 35773;
    for (int i = 0; i < 35773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35773(int& val) {
    const int x = 35773;
    std::cout << 35773;
    for (int i = 0; i < 35773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35774(int& val) {
    func_in_35774(val);
    const int x = 35774;
    std::cout << 35774;
    for (int i = 0; i < 35774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35774(int& val) {
    const int x = 35774;
    std::cout << 35774;
    for (int i = 0; i < 35774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35775(int& val) {
    func_in_35775(val);
    const int x = 35775;
    std::cout << 35775;
    for (int i = 0; i < 35775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35775(int& val) {
    const int x = 35775;
    std::cout << 35775;
    for (int i = 0; i < 35775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35776(int& val) {
    func_in_35776(val);
    const int x = 35776;
    std::cout << 35776;
    for (int i = 0; i < 35776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35776(int& val) {
    const int x = 35776;
    std::cout << 35776;
    for (int i = 0; i < 35776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35777(int& val) {
    func_in_35777(val);
    const int x = 35777;
    std::cout << 35777;
    for (int i = 0; i < 35777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35777(int& val) {
    const int x = 35777;
    std::cout << 35777;
    for (int i = 0; i < 35777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35778(int& val) {
    func_in_35778(val);
    const int x = 35778;
    std::cout << 35778;
    for (int i = 0; i < 35778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35778(int& val) {
    const int x = 35778;
    std::cout << 35778;
    for (int i = 0; i < 35778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35779(int& val) {
    func_in_35779(val);
    const int x = 35779;
    std::cout << 35779;
    for (int i = 0; i < 35779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35779(int& val) {
    const int x = 35779;
    std::cout << 35779;
    for (int i = 0; i < 35779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35780(int& val) {
    func_in_35780(val);
    const int x = 35780;
    std::cout << 35780;
    for (int i = 0; i < 35780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35780(int& val) {
    const int x = 35780;
    std::cout << 35780;
    for (int i = 0; i < 35780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35781(int& val) {
    func_in_35781(val);
    const int x = 35781;
    std::cout << 35781;
    for (int i = 0; i < 35781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35781(int& val) {
    const int x = 35781;
    std::cout << 35781;
    for (int i = 0; i < 35781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35782(int& val) {
    func_in_35782(val);
    const int x = 35782;
    std::cout << 35782;
    for (int i = 0; i < 35782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35782(int& val) {
    const int x = 35782;
    std::cout << 35782;
    for (int i = 0; i < 35782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35783(int& val) {
    func_in_35783(val);
    const int x = 35783;
    std::cout << 35783;
    for (int i = 0; i < 35783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35783(int& val) {
    const int x = 35783;
    std::cout << 35783;
    for (int i = 0; i < 35783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35784(int& val) {
    func_in_35784(val);
    const int x = 35784;
    std::cout << 35784;
    for (int i = 0; i < 35784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35784(int& val) {
    const int x = 35784;
    std::cout << 35784;
    for (int i = 0; i < 35784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35785(int& val) {
    func_in_35785(val);
    const int x = 35785;
    std::cout << 35785;
    for (int i = 0; i < 35785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35785(int& val) {
    const int x = 35785;
    std::cout << 35785;
    for (int i = 0; i < 35785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35786(int& val) {
    func_in_35786(val);
    const int x = 35786;
    std::cout << 35786;
    for (int i = 0; i < 35786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35786(int& val) {
    const int x = 35786;
    std::cout << 35786;
    for (int i = 0; i < 35786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35787(int& val) {
    func_in_35787(val);
    const int x = 35787;
    std::cout << 35787;
    for (int i = 0; i < 35787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35787(int& val) {
    const int x = 35787;
    std::cout << 35787;
    for (int i = 0; i < 35787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35788(int& val) {
    func_in_35788(val);
    const int x = 35788;
    std::cout << 35788;
    for (int i = 0; i < 35788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35788(int& val) {
    const int x = 35788;
    std::cout << 35788;
    for (int i = 0; i < 35788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35789(int& val) {
    func_in_35789(val);
    const int x = 35789;
    std::cout << 35789;
    for (int i = 0; i < 35789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35789(int& val) {
    const int x = 35789;
    std::cout << 35789;
    for (int i = 0; i < 35789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35790(int& val) {
    func_in_35790(val);
    const int x = 35790;
    std::cout << 35790;
    for (int i = 0; i < 35790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35790(int& val) {
    const int x = 35790;
    std::cout << 35790;
    for (int i = 0; i < 35790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35791(int& val) {
    func_in_35791(val);
    const int x = 35791;
    std::cout << 35791;
    for (int i = 0; i < 35791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35791(int& val) {
    const int x = 35791;
    std::cout << 35791;
    for (int i = 0; i < 35791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35792(int& val) {
    func_in_35792(val);
    const int x = 35792;
    std::cout << 35792;
    for (int i = 0; i < 35792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35792(int& val) {
    const int x = 35792;
    std::cout << 35792;
    for (int i = 0; i < 35792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35793(int& val) {
    func_in_35793(val);
    const int x = 35793;
    std::cout << 35793;
    for (int i = 0; i < 35793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35793(int& val) {
    const int x = 35793;
    std::cout << 35793;
    for (int i = 0; i < 35793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35794(int& val) {
    func_in_35794(val);
    const int x = 35794;
    std::cout << 35794;
    for (int i = 0; i < 35794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35794(int& val) {
    const int x = 35794;
    std::cout << 35794;
    for (int i = 0; i < 35794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35795(int& val) {
    func_in_35795(val);
    const int x = 35795;
    std::cout << 35795;
    for (int i = 0; i < 35795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35795(int& val) {
    const int x = 35795;
    std::cout << 35795;
    for (int i = 0; i < 35795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35796(int& val) {
    func_in_35796(val);
    const int x = 35796;
    std::cout << 35796;
    for (int i = 0; i < 35796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35796(int& val) {
    const int x = 35796;
    std::cout << 35796;
    for (int i = 0; i < 35796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35797(int& val) {
    func_in_35797(val);
    const int x = 35797;
    std::cout << 35797;
    for (int i = 0; i < 35797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35797(int& val) {
    const int x = 35797;
    std::cout << 35797;
    for (int i = 0; i < 35797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35798(int& val) {
    func_in_35798(val);
    const int x = 35798;
    std::cout << 35798;
    for (int i = 0; i < 35798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35798(int& val) {
    const int x = 35798;
    std::cout << 35798;
    for (int i = 0; i < 35798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35799(int& val) {
    func_in_35799(val);
    const int x = 35799;
    std::cout << 35799;
    for (int i = 0; i < 35799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35799(int& val) {
    const int x = 35799;
    std::cout << 35799;
    for (int i = 0; i < 35799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35800(int& val) {
    func_in_35800(val);
    const int x = 35800;
    std::cout << 35800;
    for (int i = 0; i < 35800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35800(int& val) {
    const int x = 35800;
    std::cout << 35800;
    for (int i = 0; i < 35800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35801(int& val) {
    func_in_35801(val);
    const int x = 35801;
    std::cout << 35801;
    for (int i = 0; i < 35801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35801(int& val) {
    const int x = 35801;
    std::cout << 35801;
    for (int i = 0; i < 35801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35802(int& val) {
    func_in_35802(val);
    const int x = 35802;
    std::cout << 35802;
    for (int i = 0; i < 35802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35802(int& val) {
    const int x = 35802;
    std::cout << 35802;
    for (int i = 0; i < 35802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35803(int& val) {
    func_in_35803(val);
    const int x = 35803;
    std::cout << 35803;
    for (int i = 0; i < 35803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35803(int& val) {
    const int x = 35803;
    std::cout << 35803;
    for (int i = 0; i < 35803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35804(int& val) {
    func_in_35804(val);
    const int x = 35804;
    std::cout << 35804;
    for (int i = 0; i < 35804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35804(int& val) {
    const int x = 35804;
    std::cout << 35804;
    for (int i = 0; i < 35804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35805(int& val) {
    func_in_35805(val);
    const int x = 35805;
    std::cout << 35805;
    for (int i = 0; i < 35805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35805(int& val) {
    const int x = 35805;
    std::cout << 35805;
    for (int i = 0; i < 35805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35806(int& val) {
    func_in_35806(val);
    const int x = 35806;
    std::cout << 35806;
    for (int i = 0; i < 35806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35806(int& val) {
    const int x = 35806;
    std::cout << 35806;
    for (int i = 0; i < 35806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35807(int& val) {
    func_in_35807(val);
    const int x = 35807;
    std::cout << 35807;
    for (int i = 0; i < 35807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35807(int& val) {
    const int x = 35807;
    std::cout << 35807;
    for (int i = 0; i < 35807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35808(int& val) {
    func_in_35808(val);
    const int x = 35808;
    std::cout << 35808;
    for (int i = 0; i < 35808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35808(int& val) {
    const int x = 35808;
    std::cout << 35808;
    for (int i = 0; i < 35808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35809(int& val) {
    func_in_35809(val);
    const int x = 35809;
    std::cout << 35809;
    for (int i = 0; i < 35809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35809(int& val) {
    const int x = 35809;
    std::cout << 35809;
    for (int i = 0; i < 35809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35810(int& val) {
    func_in_35810(val);
    const int x = 35810;
    std::cout << 35810;
    for (int i = 0; i < 35810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35810(int& val) {
    const int x = 35810;
    std::cout << 35810;
    for (int i = 0; i < 35810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35811(int& val) {
    func_in_35811(val);
    const int x = 35811;
    std::cout << 35811;
    for (int i = 0; i < 35811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35811(int& val) {
    const int x = 35811;
    std::cout << 35811;
    for (int i = 0; i < 35811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35812(int& val) {
    func_in_35812(val);
    const int x = 35812;
    std::cout << 35812;
    for (int i = 0; i < 35812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35812(int& val) {
    const int x = 35812;
    std::cout << 35812;
    for (int i = 0; i < 35812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35813(int& val) {
    func_in_35813(val);
    const int x = 35813;
    std::cout << 35813;
    for (int i = 0; i < 35813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35813(int& val) {
    const int x = 35813;
    std::cout << 35813;
    for (int i = 0; i < 35813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35814(int& val) {
    func_in_35814(val);
    const int x = 35814;
    std::cout << 35814;
    for (int i = 0; i < 35814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35814(int& val) {
    const int x = 35814;
    std::cout << 35814;
    for (int i = 0; i < 35814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35815(int& val) {
    func_in_35815(val);
    const int x = 35815;
    std::cout << 35815;
    for (int i = 0; i < 35815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35815(int& val) {
    const int x = 35815;
    std::cout << 35815;
    for (int i = 0; i < 35815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35816(int& val) {
    func_in_35816(val);
    const int x = 35816;
    std::cout << 35816;
    for (int i = 0; i < 35816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35816(int& val) {
    const int x = 35816;
    std::cout << 35816;
    for (int i = 0; i < 35816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35817(int& val) {
    func_in_35817(val);
    const int x = 35817;
    std::cout << 35817;
    for (int i = 0; i < 35817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35817(int& val) {
    const int x = 35817;
    std::cout << 35817;
    for (int i = 0; i < 35817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35818(int& val) {
    func_in_35818(val);
    const int x = 35818;
    std::cout << 35818;
    for (int i = 0; i < 35818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35818(int& val) {
    const int x = 35818;
    std::cout << 35818;
    for (int i = 0; i < 35818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35819(int& val) {
    func_in_35819(val);
    const int x = 35819;
    std::cout << 35819;
    for (int i = 0; i < 35819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35819(int& val) {
    const int x = 35819;
    std::cout << 35819;
    for (int i = 0; i < 35819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35820(int& val) {
    func_in_35820(val);
    const int x = 35820;
    std::cout << 35820;
    for (int i = 0; i < 35820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35820(int& val) {
    const int x = 35820;
    std::cout << 35820;
    for (int i = 0; i < 35820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35821(int& val) {
    func_in_35821(val);
    const int x = 35821;
    std::cout << 35821;
    for (int i = 0; i < 35821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35821(int& val) {
    const int x = 35821;
    std::cout << 35821;
    for (int i = 0; i < 35821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35822(int& val) {
    func_in_35822(val);
    const int x = 35822;
    std::cout << 35822;
    for (int i = 0; i < 35822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35822(int& val) {
    const int x = 35822;
    std::cout << 35822;
    for (int i = 0; i < 35822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35823(int& val) {
    func_in_35823(val);
    const int x = 35823;
    std::cout << 35823;
    for (int i = 0; i < 35823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35823(int& val) {
    const int x = 35823;
    std::cout << 35823;
    for (int i = 0; i < 35823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35824(int& val) {
    func_in_35824(val);
    const int x = 35824;
    std::cout << 35824;
    for (int i = 0; i < 35824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35824(int& val) {
    const int x = 35824;
    std::cout << 35824;
    for (int i = 0; i < 35824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35825(int& val) {
    func_in_35825(val);
    const int x = 35825;
    std::cout << 35825;
    for (int i = 0; i < 35825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35825(int& val) {
    const int x = 35825;
    std::cout << 35825;
    for (int i = 0; i < 35825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35826(int& val) {
    func_in_35826(val);
    const int x = 35826;
    std::cout << 35826;
    for (int i = 0; i < 35826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35826(int& val) {
    const int x = 35826;
    std::cout << 35826;
    for (int i = 0; i < 35826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35827(int& val) {
    func_in_35827(val);
    const int x = 35827;
    std::cout << 35827;
    for (int i = 0; i < 35827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35827(int& val) {
    const int x = 35827;
    std::cout << 35827;
    for (int i = 0; i < 35827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35828(int& val) {
    func_in_35828(val);
    const int x = 35828;
    std::cout << 35828;
    for (int i = 0; i < 35828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35828(int& val) {
    const int x = 35828;
    std::cout << 35828;
    for (int i = 0; i < 35828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35829(int& val) {
    func_in_35829(val);
    const int x = 35829;
    std::cout << 35829;
    for (int i = 0; i < 35829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35829(int& val) {
    const int x = 35829;
    std::cout << 35829;
    for (int i = 0; i < 35829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35830(int& val) {
    func_in_35830(val);
    const int x = 35830;
    std::cout << 35830;
    for (int i = 0; i < 35830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35830(int& val) {
    const int x = 35830;
    std::cout << 35830;
    for (int i = 0; i < 35830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35831(int& val) {
    func_in_35831(val);
    const int x = 35831;
    std::cout << 35831;
    for (int i = 0; i < 35831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35831(int& val) {
    const int x = 35831;
    std::cout << 35831;
    for (int i = 0; i < 35831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35832(int& val) {
    func_in_35832(val);
    const int x = 35832;
    std::cout << 35832;
    for (int i = 0; i < 35832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35832(int& val) {
    const int x = 35832;
    std::cout << 35832;
    for (int i = 0; i < 35832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35833(int& val) {
    func_in_35833(val);
    const int x = 35833;
    std::cout << 35833;
    for (int i = 0; i < 35833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35833(int& val) {
    const int x = 35833;
    std::cout << 35833;
    for (int i = 0; i < 35833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35834(int& val) {
    func_in_35834(val);
    const int x = 35834;
    std::cout << 35834;
    for (int i = 0; i < 35834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35834(int& val) {
    const int x = 35834;
    std::cout << 35834;
    for (int i = 0; i < 35834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35835(int& val) {
    func_in_35835(val);
    const int x = 35835;
    std::cout << 35835;
    for (int i = 0; i < 35835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35835(int& val) {
    const int x = 35835;
    std::cout << 35835;
    for (int i = 0; i < 35835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35836(int& val) {
    func_in_35836(val);
    const int x = 35836;
    std::cout << 35836;
    for (int i = 0; i < 35836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35836(int& val) {
    const int x = 35836;
    std::cout << 35836;
    for (int i = 0; i < 35836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35837(int& val) {
    func_in_35837(val);
    const int x = 35837;
    std::cout << 35837;
    for (int i = 0; i < 35837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35837(int& val) {
    const int x = 35837;
    std::cout << 35837;
    for (int i = 0; i < 35837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35838(int& val) {
    func_in_35838(val);
    const int x = 35838;
    std::cout << 35838;
    for (int i = 0; i < 35838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35838(int& val) {
    const int x = 35838;
    std::cout << 35838;
    for (int i = 0; i < 35838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35839(int& val) {
    func_in_35839(val);
    const int x = 35839;
    std::cout << 35839;
    for (int i = 0; i < 35839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35839(int& val) {
    const int x = 35839;
    std::cout << 35839;
    for (int i = 0; i < 35839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35840(int& val) {
    func_in_35840(val);
    const int x = 35840;
    std::cout << 35840;
    for (int i = 0; i < 35840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35840(int& val) {
    const int x = 35840;
    std::cout << 35840;
    for (int i = 0; i < 35840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35841(int& val) {
    func_in_35841(val);
    const int x = 35841;
    std::cout << 35841;
    for (int i = 0; i < 35841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35841(int& val) {
    const int x = 35841;
    std::cout << 35841;
    for (int i = 0; i < 35841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35842(int& val) {
    func_in_35842(val);
    const int x = 35842;
    std::cout << 35842;
    for (int i = 0; i < 35842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35842(int& val) {
    const int x = 35842;
    std::cout << 35842;
    for (int i = 0; i < 35842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35843(int& val) {
    func_in_35843(val);
    const int x = 35843;
    std::cout << 35843;
    for (int i = 0; i < 35843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35843(int& val) {
    const int x = 35843;
    std::cout << 35843;
    for (int i = 0; i < 35843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35844(int& val) {
    func_in_35844(val);
    const int x = 35844;
    std::cout << 35844;
    for (int i = 0; i < 35844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35844(int& val) {
    const int x = 35844;
    std::cout << 35844;
    for (int i = 0; i < 35844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35845(int& val) {
    func_in_35845(val);
    const int x = 35845;
    std::cout << 35845;
    for (int i = 0; i < 35845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35845(int& val) {
    const int x = 35845;
    std::cout << 35845;
    for (int i = 0; i < 35845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35846(int& val) {
    func_in_35846(val);
    const int x = 35846;
    std::cout << 35846;
    for (int i = 0; i < 35846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35846(int& val) {
    const int x = 35846;
    std::cout << 35846;
    for (int i = 0; i < 35846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35847(int& val) {
    func_in_35847(val);
    const int x = 35847;
    std::cout << 35847;
    for (int i = 0; i < 35847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35847(int& val) {
    const int x = 35847;
    std::cout << 35847;
    for (int i = 0; i < 35847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35848(int& val) {
    func_in_35848(val);
    const int x = 35848;
    std::cout << 35848;
    for (int i = 0; i < 35848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35848(int& val) {
    const int x = 35848;
    std::cout << 35848;
    for (int i = 0; i < 35848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35849(int& val) {
    func_in_35849(val);
    const int x = 35849;
    std::cout << 35849;
    for (int i = 0; i < 35849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35849(int& val) {
    const int x = 35849;
    std::cout << 35849;
    for (int i = 0; i < 35849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35850(int& val) {
    func_in_35850(val);
    const int x = 35850;
    std::cout << 35850;
    for (int i = 0; i < 35850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35850(int& val) {
    const int x = 35850;
    std::cout << 35850;
    for (int i = 0; i < 35850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35851(int& val) {
    func_in_35851(val);
    const int x = 35851;
    std::cout << 35851;
    for (int i = 0; i < 35851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35851(int& val) {
    const int x = 35851;
    std::cout << 35851;
    for (int i = 0; i < 35851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35852(int& val) {
    func_in_35852(val);
    const int x = 35852;
    std::cout << 35852;
    for (int i = 0; i < 35852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35852(int& val) {
    const int x = 35852;
    std::cout << 35852;
    for (int i = 0; i < 35852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35853(int& val) {
    func_in_35853(val);
    const int x = 35853;
    std::cout << 35853;
    for (int i = 0; i < 35853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35853(int& val) {
    const int x = 35853;
    std::cout << 35853;
    for (int i = 0; i < 35853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35854(int& val) {
    func_in_35854(val);
    const int x = 35854;
    std::cout << 35854;
    for (int i = 0; i < 35854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35854(int& val) {
    const int x = 35854;
    std::cout << 35854;
    for (int i = 0; i < 35854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35855(int& val) {
    func_in_35855(val);
    const int x = 35855;
    std::cout << 35855;
    for (int i = 0; i < 35855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35855(int& val) {
    const int x = 35855;
    std::cout << 35855;
    for (int i = 0; i < 35855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35856(int& val) {
    func_in_35856(val);
    const int x = 35856;
    std::cout << 35856;
    for (int i = 0; i < 35856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35856(int& val) {
    const int x = 35856;
    std::cout << 35856;
    for (int i = 0; i < 35856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35857(int& val) {
    func_in_35857(val);
    const int x = 35857;
    std::cout << 35857;
    for (int i = 0; i < 35857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35857(int& val) {
    const int x = 35857;
    std::cout << 35857;
    for (int i = 0; i < 35857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35858(int& val) {
    func_in_35858(val);
    const int x = 35858;
    std::cout << 35858;
    for (int i = 0; i < 35858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35858(int& val) {
    const int x = 35858;
    std::cout << 35858;
    for (int i = 0; i < 35858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35859(int& val) {
    func_in_35859(val);
    const int x = 35859;
    std::cout << 35859;
    for (int i = 0; i < 35859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35859(int& val) {
    const int x = 35859;
    std::cout << 35859;
    for (int i = 0; i < 35859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35860(int& val) {
    func_in_35860(val);
    const int x = 35860;
    std::cout << 35860;
    for (int i = 0; i < 35860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35860(int& val) {
    const int x = 35860;
    std::cout << 35860;
    for (int i = 0; i < 35860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35861(int& val) {
    func_in_35861(val);
    const int x = 35861;
    std::cout << 35861;
    for (int i = 0; i < 35861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35861(int& val) {
    const int x = 35861;
    std::cout << 35861;
    for (int i = 0; i < 35861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35862(int& val) {
    func_in_35862(val);
    const int x = 35862;
    std::cout << 35862;
    for (int i = 0; i < 35862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35862(int& val) {
    const int x = 35862;
    std::cout << 35862;
    for (int i = 0; i < 35862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35863(int& val) {
    func_in_35863(val);
    const int x = 35863;
    std::cout << 35863;
    for (int i = 0; i < 35863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35863(int& val) {
    const int x = 35863;
    std::cout << 35863;
    for (int i = 0; i < 35863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35864(int& val) {
    func_in_35864(val);
    const int x = 35864;
    std::cout << 35864;
    for (int i = 0; i < 35864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35864(int& val) {
    const int x = 35864;
    std::cout << 35864;
    for (int i = 0; i < 35864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35865(int& val) {
    func_in_35865(val);
    const int x = 35865;
    std::cout << 35865;
    for (int i = 0; i < 35865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35865(int& val) {
    const int x = 35865;
    std::cout << 35865;
    for (int i = 0; i < 35865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35866(int& val) {
    func_in_35866(val);
    const int x = 35866;
    std::cout << 35866;
    for (int i = 0; i < 35866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35866(int& val) {
    const int x = 35866;
    std::cout << 35866;
    for (int i = 0; i < 35866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35867(int& val) {
    func_in_35867(val);
    const int x = 35867;
    std::cout << 35867;
    for (int i = 0; i < 35867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35867(int& val) {
    const int x = 35867;
    std::cout << 35867;
    for (int i = 0; i < 35867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35868(int& val) {
    func_in_35868(val);
    const int x = 35868;
    std::cout << 35868;
    for (int i = 0; i < 35868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35868(int& val) {
    const int x = 35868;
    std::cout << 35868;
    for (int i = 0; i < 35868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35869(int& val) {
    func_in_35869(val);
    const int x = 35869;
    std::cout << 35869;
    for (int i = 0; i < 35869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35869(int& val) {
    const int x = 35869;
    std::cout << 35869;
    for (int i = 0; i < 35869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35870(int& val) {
    func_in_35870(val);
    const int x = 35870;
    std::cout << 35870;
    for (int i = 0; i < 35870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35870(int& val) {
    const int x = 35870;
    std::cout << 35870;
    for (int i = 0; i < 35870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35871(int& val) {
    func_in_35871(val);
    const int x = 35871;
    std::cout << 35871;
    for (int i = 0; i < 35871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35871(int& val) {
    const int x = 35871;
    std::cout << 35871;
    for (int i = 0; i < 35871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35872(int& val) {
    func_in_35872(val);
    const int x = 35872;
    std::cout << 35872;
    for (int i = 0; i < 35872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35872(int& val) {
    const int x = 35872;
    std::cout << 35872;
    for (int i = 0; i < 35872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35873(int& val) {
    func_in_35873(val);
    const int x = 35873;
    std::cout << 35873;
    for (int i = 0; i < 35873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35873(int& val) {
    const int x = 35873;
    std::cout << 35873;
    for (int i = 0; i < 35873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35874(int& val) {
    func_in_35874(val);
    const int x = 35874;
    std::cout << 35874;
    for (int i = 0; i < 35874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35874(int& val) {
    const int x = 35874;
    std::cout << 35874;
    for (int i = 0; i < 35874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35875(int& val) {
    func_in_35875(val);
    const int x = 35875;
    std::cout << 35875;
    for (int i = 0; i < 35875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35875(int& val) {
    const int x = 35875;
    std::cout << 35875;
    for (int i = 0; i < 35875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35876(int& val) {
    func_in_35876(val);
    const int x = 35876;
    std::cout << 35876;
    for (int i = 0; i < 35876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35876(int& val) {
    const int x = 35876;
    std::cout << 35876;
    for (int i = 0; i < 35876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35877(int& val) {
    func_in_35877(val);
    const int x = 35877;
    std::cout << 35877;
    for (int i = 0; i < 35877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35877(int& val) {
    const int x = 35877;
    std::cout << 35877;
    for (int i = 0; i < 35877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35878(int& val) {
    func_in_35878(val);
    const int x = 35878;
    std::cout << 35878;
    for (int i = 0; i < 35878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35878(int& val) {
    const int x = 35878;
    std::cout << 35878;
    for (int i = 0; i < 35878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35879(int& val) {
    func_in_35879(val);
    const int x = 35879;
    std::cout << 35879;
    for (int i = 0; i < 35879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35879(int& val) {
    const int x = 35879;
    std::cout << 35879;
    for (int i = 0; i < 35879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35880(int& val) {
    func_in_35880(val);
    const int x = 35880;
    std::cout << 35880;
    for (int i = 0; i < 35880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35880(int& val) {
    const int x = 35880;
    std::cout << 35880;
    for (int i = 0; i < 35880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35881(int& val) {
    func_in_35881(val);
    const int x = 35881;
    std::cout << 35881;
    for (int i = 0; i < 35881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35881(int& val) {
    const int x = 35881;
    std::cout << 35881;
    for (int i = 0; i < 35881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35882(int& val) {
    func_in_35882(val);
    const int x = 35882;
    std::cout << 35882;
    for (int i = 0; i < 35882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35882(int& val) {
    const int x = 35882;
    std::cout << 35882;
    for (int i = 0; i < 35882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35883(int& val) {
    func_in_35883(val);
    const int x = 35883;
    std::cout << 35883;
    for (int i = 0; i < 35883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35883(int& val) {
    const int x = 35883;
    std::cout << 35883;
    for (int i = 0; i < 35883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35884(int& val) {
    func_in_35884(val);
    const int x = 35884;
    std::cout << 35884;
    for (int i = 0; i < 35884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35884(int& val) {
    const int x = 35884;
    std::cout << 35884;
    for (int i = 0; i < 35884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35885(int& val) {
    func_in_35885(val);
    const int x = 35885;
    std::cout << 35885;
    for (int i = 0; i < 35885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35885(int& val) {
    const int x = 35885;
    std::cout << 35885;
    for (int i = 0; i < 35885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35886(int& val) {
    func_in_35886(val);
    const int x = 35886;
    std::cout << 35886;
    for (int i = 0; i < 35886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35886(int& val) {
    const int x = 35886;
    std::cout << 35886;
    for (int i = 0; i < 35886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35887(int& val) {
    func_in_35887(val);
    const int x = 35887;
    std::cout << 35887;
    for (int i = 0; i < 35887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35887(int& val) {
    const int x = 35887;
    std::cout << 35887;
    for (int i = 0; i < 35887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35888(int& val) {
    func_in_35888(val);
    const int x = 35888;
    std::cout << 35888;
    for (int i = 0; i < 35888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35888(int& val) {
    const int x = 35888;
    std::cout << 35888;
    for (int i = 0; i < 35888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35889(int& val) {
    func_in_35889(val);
    const int x = 35889;
    std::cout << 35889;
    for (int i = 0; i < 35889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35889(int& val) {
    const int x = 35889;
    std::cout << 35889;
    for (int i = 0; i < 35889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35890(int& val) {
    func_in_35890(val);
    const int x = 35890;
    std::cout << 35890;
    for (int i = 0; i < 35890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35890(int& val) {
    const int x = 35890;
    std::cout << 35890;
    for (int i = 0; i < 35890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35891(int& val) {
    func_in_35891(val);
    const int x = 35891;
    std::cout << 35891;
    for (int i = 0; i < 35891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35891(int& val) {
    const int x = 35891;
    std::cout << 35891;
    for (int i = 0; i < 35891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35892(int& val) {
    func_in_35892(val);
    const int x = 35892;
    std::cout << 35892;
    for (int i = 0; i < 35892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35892(int& val) {
    const int x = 35892;
    std::cout << 35892;
    for (int i = 0; i < 35892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35893(int& val) {
    func_in_35893(val);
    const int x = 35893;
    std::cout << 35893;
    for (int i = 0; i < 35893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35893(int& val) {
    const int x = 35893;
    std::cout << 35893;
    for (int i = 0; i < 35893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35894(int& val) {
    func_in_35894(val);
    const int x = 35894;
    std::cout << 35894;
    for (int i = 0; i < 35894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35894(int& val) {
    const int x = 35894;
    std::cout << 35894;
    for (int i = 0; i < 35894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35895(int& val) {
    func_in_35895(val);
    const int x = 35895;
    std::cout << 35895;
    for (int i = 0; i < 35895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35895(int& val) {
    const int x = 35895;
    std::cout << 35895;
    for (int i = 0; i < 35895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35896(int& val) {
    func_in_35896(val);
    const int x = 35896;
    std::cout << 35896;
    for (int i = 0; i < 35896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35896(int& val) {
    const int x = 35896;
    std::cout << 35896;
    for (int i = 0; i < 35896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35897(int& val) {
    func_in_35897(val);
    const int x = 35897;
    std::cout << 35897;
    for (int i = 0; i < 35897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35897(int& val) {
    const int x = 35897;
    std::cout << 35897;
    for (int i = 0; i < 35897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35898(int& val) {
    func_in_35898(val);
    const int x = 35898;
    std::cout << 35898;
    for (int i = 0; i < 35898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35898(int& val) {
    const int x = 35898;
    std::cout << 35898;
    for (int i = 0; i < 35898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35899(int& val) {
    func_in_35899(val);
    const int x = 35899;
    std::cout << 35899;
    for (int i = 0; i < 35899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35899(int& val) {
    const int x = 35899;
    std::cout << 35899;
    for (int i = 0; i < 35899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35900(int& val) {
    func_in_35900(val);
    const int x = 35900;
    std::cout << 35900;
    for (int i = 0; i < 35900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35900(int& val) {
    const int x = 35900;
    std::cout << 35900;
    for (int i = 0; i < 35900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35901(int& val) {
    func_in_35901(val);
    const int x = 35901;
    std::cout << 35901;
    for (int i = 0; i < 35901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35901(int& val) {
    const int x = 35901;
    std::cout << 35901;
    for (int i = 0; i < 35901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35902(int& val) {
    func_in_35902(val);
    const int x = 35902;
    std::cout << 35902;
    for (int i = 0; i < 35902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35902(int& val) {
    const int x = 35902;
    std::cout << 35902;
    for (int i = 0; i < 35902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35903(int& val) {
    func_in_35903(val);
    const int x = 35903;
    std::cout << 35903;
    for (int i = 0; i < 35903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35903(int& val) {
    const int x = 35903;
    std::cout << 35903;
    for (int i = 0; i < 35903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35904(int& val) {
    func_in_35904(val);
    const int x = 35904;
    std::cout << 35904;
    for (int i = 0; i < 35904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35904(int& val) {
    const int x = 35904;
    std::cout << 35904;
    for (int i = 0; i < 35904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35905(int& val) {
    func_in_35905(val);
    const int x = 35905;
    std::cout << 35905;
    for (int i = 0; i < 35905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35905(int& val) {
    const int x = 35905;
    std::cout << 35905;
    for (int i = 0; i < 35905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35906(int& val) {
    func_in_35906(val);
    const int x = 35906;
    std::cout << 35906;
    for (int i = 0; i < 35906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35906(int& val) {
    const int x = 35906;
    std::cout << 35906;
    for (int i = 0; i < 35906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35907(int& val) {
    func_in_35907(val);
    const int x = 35907;
    std::cout << 35907;
    for (int i = 0; i < 35907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35907(int& val) {
    const int x = 35907;
    std::cout << 35907;
    for (int i = 0; i < 35907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35908(int& val) {
    func_in_35908(val);
    const int x = 35908;
    std::cout << 35908;
    for (int i = 0; i < 35908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35908(int& val) {
    const int x = 35908;
    std::cout << 35908;
    for (int i = 0; i < 35908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35909(int& val) {
    func_in_35909(val);
    const int x = 35909;
    std::cout << 35909;
    for (int i = 0; i < 35909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35909(int& val) {
    const int x = 35909;
    std::cout << 35909;
    for (int i = 0; i < 35909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35910(int& val) {
    func_in_35910(val);
    const int x = 35910;
    std::cout << 35910;
    for (int i = 0; i < 35910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35910(int& val) {
    const int x = 35910;
    std::cout << 35910;
    for (int i = 0; i < 35910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35911(int& val) {
    func_in_35911(val);
    const int x = 35911;
    std::cout << 35911;
    for (int i = 0; i < 35911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35911(int& val) {
    const int x = 35911;
    std::cout << 35911;
    for (int i = 0; i < 35911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35912(int& val) {
    func_in_35912(val);
    const int x = 35912;
    std::cout << 35912;
    for (int i = 0; i < 35912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35912(int& val) {
    const int x = 35912;
    std::cout << 35912;
    for (int i = 0; i < 35912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35913(int& val) {
    func_in_35913(val);
    const int x = 35913;
    std::cout << 35913;
    for (int i = 0; i < 35913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35913(int& val) {
    const int x = 35913;
    std::cout << 35913;
    for (int i = 0; i < 35913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35914(int& val) {
    func_in_35914(val);
    const int x = 35914;
    std::cout << 35914;
    for (int i = 0; i < 35914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35914(int& val) {
    const int x = 35914;
    std::cout << 35914;
    for (int i = 0; i < 35914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35915(int& val) {
    func_in_35915(val);
    const int x = 35915;
    std::cout << 35915;
    for (int i = 0; i < 35915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35915(int& val) {
    const int x = 35915;
    std::cout << 35915;
    for (int i = 0; i < 35915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35916(int& val) {
    func_in_35916(val);
    const int x = 35916;
    std::cout << 35916;
    for (int i = 0; i < 35916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35916(int& val) {
    const int x = 35916;
    std::cout << 35916;
    for (int i = 0; i < 35916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35917(int& val) {
    func_in_35917(val);
    const int x = 35917;
    std::cout << 35917;
    for (int i = 0; i < 35917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35917(int& val) {
    const int x = 35917;
    std::cout << 35917;
    for (int i = 0; i < 35917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35918(int& val) {
    func_in_35918(val);
    const int x = 35918;
    std::cout << 35918;
    for (int i = 0; i < 35918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35918(int& val) {
    const int x = 35918;
    std::cout << 35918;
    for (int i = 0; i < 35918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35919(int& val) {
    func_in_35919(val);
    const int x = 35919;
    std::cout << 35919;
    for (int i = 0; i < 35919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35919(int& val) {
    const int x = 35919;
    std::cout << 35919;
    for (int i = 0; i < 35919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35920(int& val) {
    func_in_35920(val);
    const int x = 35920;
    std::cout << 35920;
    for (int i = 0; i < 35920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35920(int& val) {
    const int x = 35920;
    std::cout << 35920;
    for (int i = 0; i < 35920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35921(int& val) {
    func_in_35921(val);
    const int x = 35921;
    std::cout << 35921;
    for (int i = 0; i < 35921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35921(int& val) {
    const int x = 35921;
    std::cout << 35921;
    for (int i = 0; i < 35921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35922(int& val) {
    func_in_35922(val);
    const int x = 35922;
    std::cout << 35922;
    for (int i = 0; i < 35922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35922(int& val) {
    const int x = 35922;
    std::cout << 35922;
    for (int i = 0; i < 35922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35923(int& val) {
    func_in_35923(val);
    const int x = 35923;
    std::cout << 35923;
    for (int i = 0; i < 35923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35923(int& val) {
    const int x = 35923;
    std::cout << 35923;
    for (int i = 0; i < 35923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35924(int& val) {
    func_in_35924(val);
    const int x = 35924;
    std::cout << 35924;
    for (int i = 0; i < 35924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35924(int& val) {
    const int x = 35924;
    std::cout << 35924;
    for (int i = 0; i < 35924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35925(int& val) {
    func_in_35925(val);
    const int x = 35925;
    std::cout << 35925;
    for (int i = 0; i < 35925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35925(int& val) {
    const int x = 35925;
    std::cout << 35925;
    for (int i = 0; i < 35925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35926(int& val) {
    func_in_35926(val);
    const int x = 35926;
    std::cout << 35926;
    for (int i = 0; i < 35926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35926(int& val) {
    const int x = 35926;
    std::cout << 35926;
    for (int i = 0; i < 35926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35927(int& val) {
    func_in_35927(val);
    const int x = 35927;
    std::cout << 35927;
    for (int i = 0; i < 35927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35927(int& val) {
    const int x = 35927;
    std::cout << 35927;
    for (int i = 0; i < 35927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35928(int& val) {
    func_in_35928(val);
    const int x = 35928;
    std::cout << 35928;
    for (int i = 0; i < 35928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35928(int& val) {
    const int x = 35928;
    std::cout << 35928;
    for (int i = 0; i < 35928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35929(int& val) {
    func_in_35929(val);
    const int x = 35929;
    std::cout << 35929;
    for (int i = 0; i < 35929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35929(int& val) {
    const int x = 35929;
    std::cout << 35929;
    for (int i = 0; i < 35929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35930(int& val) {
    func_in_35930(val);
    const int x = 35930;
    std::cout << 35930;
    for (int i = 0; i < 35930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35930(int& val) {
    const int x = 35930;
    std::cout << 35930;
    for (int i = 0; i < 35930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35931(int& val) {
    func_in_35931(val);
    const int x = 35931;
    std::cout << 35931;
    for (int i = 0; i < 35931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35931(int& val) {
    const int x = 35931;
    std::cout << 35931;
    for (int i = 0; i < 35931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35932(int& val) {
    func_in_35932(val);
    const int x = 35932;
    std::cout << 35932;
    for (int i = 0; i < 35932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35932(int& val) {
    const int x = 35932;
    std::cout << 35932;
    for (int i = 0; i < 35932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35933(int& val) {
    func_in_35933(val);
    const int x = 35933;
    std::cout << 35933;
    for (int i = 0; i < 35933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35933(int& val) {
    const int x = 35933;
    std::cout << 35933;
    for (int i = 0; i < 35933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35934(int& val) {
    func_in_35934(val);
    const int x = 35934;
    std::cout << 35934;
    for (int i = 0; i < 35934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35934(int& val) {
    const int x = 35934;
    std::cout << 35934;
    for (int i = 0; i < 35934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35935(int& val) {
    func_in_35935(val);
    const int x = 35935;
    std::cout << 35935;
    for (int i = 0; i < 35935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35935(int& val) {
    const int x = 35935;
    std::cout << 35935;
    for (int i = 0; i < 35935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35936(int& val) {
    func_in_35936(val);
    const int x = 35936;
    std::cout << 35936;
    for (int i = 0; i < 35936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35936(int& val) {
    const int x = 35936;
    std::cout << 35936;
    for (int i = 0; i < 35936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35937(int& val) {
    func_in_35937(val);
    const int x = 35937;
    std::cout << 35937;
    for (int i = 0; i < 35937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35937(int& val) {
    const int x = 35937;
    std::cout << 35937;
    for (int i = 0; i < 35937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35938(int& val) {
    func_in_35938(val);
    const int x = 35938;
    std::cout << 35938;
    for (int i = 0; i < 35938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35938(int& val) {
    const int x = 35938;
    std::cout << 35938;
    for (int i = 0; i < 35938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35939(int& val) {
    func_in_35939(val);
    const int x = 35939;
    std::cout << 35939;
    for (int i = 0; i < 35939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35939(int& val) {
    const int x = 35939;
    std::cout << 35939;
    for (int i = 0; i < 35939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35940(int& val) {
    func_in_35940(val);
    const int x = 35940;
    std::cout << 35940;
    for (int i = 0; i < 35940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35940(int& val) {
    const int x = 35940;
    std::cout << 35940;
    for (int i = 0; i < 35940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35941(int& val) {
    func_in_35941(val);
    const int x = 35941;
    std::cout << 35941;
    for (int i = 0; i < 35941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35941(int& val) {
    const int x = 35941;
    std::cout << 35941;
    for (int i = 0; i < 35941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35942(int& val) {
    func_in_35942(val);
    const int x = 35942;
    std::cout << 35942;
    for (int i = 0; i < 35942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35942(int& val) {
    const int x = 35942;
    std::cout << 35942;
    for (int i = 0; i < 35942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35943(int& val) {
    func_in_35943(val);
    const int x = 35943;
    std::cout << 35943;
    for (int i = 0; i < 35943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35943(int& val) {
    const int x = 35943;
    std::cout << 35943;
    for (int i = 0; i < 35943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35944(int& val) {
    func_in_35944(val);
    const int x = 35944;
    std::cout << 35944;
    for (int i = 0; i < 35944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35944(int& val) {
    const int x = 35944;
    std::cout << 35944;
    for (int i = 0; i < 35944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35945(int& val) {
    func_in_35945(val);
    const int x = 35945;
    std::cout << 35945;
    for (int i = 0; i < 35945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35945(int& val) {
    const int x = 35945;
    std::cout << 35945;
    for (int i = 0; i < 35945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35946(int& val) {
    func_in_35946(val);
    const int x = 35946;
    std::cout << 35946;
    for (int i = 0; i < 35946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35946(int& val) {
    const int x = 35946;
    std::cout << 35946;
    for (int i = 0; i < 35946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35947(int& val) {
    func_in_35947(val);
    const int x = 35947;
    std::cout << 35947;
    for (int i = 0; i < 35947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35947(int& val) {
    const int x = 35947;
    std::cout << 35947;
    for (int i = 0; i < 35947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35948(int& val) {
    func_in_35948(val);
    const int x = 35948;
    std::cout << 35948;
    for (int i = 0; i < 35948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35948(int& val) {
    const int x = 35948;
    std::cout << 35948;
    for (int i = 0; i < 35948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35949(int& val) {
    func_in_35949(val);
    const int x = 35949;
    std::cout << 35949;
    for (int i = 0; i < 35949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35949(int& val) {
    const int x = 35949;
    std::cout << 35949;
    for (int i = 0; i < 35949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35950(int& val) {
    func_in_35950(val);
    const int x = 35950;
    std::cout << 35950;
    for (int i = 0; i < 35950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35950(int& val) {
    const int x = 35950;
    std::cout << 35950;
    for (int i = 0; i < 35950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35951(int& val) {
    func_in_35951(val);
    const int x = 35951;
    std::cout << 35951;
    for (int i = 0; i < 35951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35951(int& val) {
    const int x = 35951;
    std::cout << 35951;
    for (int i = 0; i < 35951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35952(int& val) {
    func_in_35952(val);
    const int x = 35952;
    std::cout << 35952;
    for (int i = 0; i < 35952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35952(int& val) {
    const int x = 35952;
    std::cout << 35952;
    for (int i = 0; i < 35952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35953(int& val) {
    func_in_35953(val);
    const int x = 35953;
    std::cout << 35953;
    for (int i = 0; i < 35953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35953(int& val) {
    const int x = 35953;
    std::cout << 35953;
    for (int i = 0; i < 35953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35954(int& val) {
    func_in_35954(val);
    const int x = 35954;
    std::cout << 35954;
    for (int i = 0; i < 35954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35954(int& val) {
    const int x = 35954;
    std::cout << 35954;
    for (int i = 0; i < 35954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35955(int& val) {
    func_in_35955(val);
    const int x = 35955;
    std::cout << 35955;
    for (int i = 0; i < 35955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35955(int& val) {
    const int x = 35955;
    std::cout << 35955;
    for (int i = 0; i < 35955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35956(int& val) {
    func_in_35956(val);
    const int x = 35956;
    std::cout << 35956;
    for (int i = 0; i < 35956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35956(int& val) {
    const int x = 35956;
    std::cout << 35956;
    for (int i = 0; i < 35956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35957(int& val) {
    func_in_35957(val);
    const int x = 35957;
    std::cout << 35957;
    for (int i = 0; i < 35957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35957(int& val) {
    const int x = 35957;
    std::cout << 35957;
    for (int i = 0; i < 35957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35958(int& val) {
    func_in_35958(val);
    const int x = 35958;
    std::cout << 35958;
    for (int i = 0; i < 35958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35958(int& val) {
    const int x = 35958;
    std::cout << 35958;
    for (int i = 0; i < 35958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35959(int& val) {
    func_in_35959(val);
    const int x = 35959;
    std::cout << 35959;
    for (int i = 0; i < 35959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35959(int& val) {
    const int x = 35959;
    std::cout << 35959;
    for (int i = 0; i < 35959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35960(int& val) {
    func_in_35960(val);
    const int x = 35960;
    std::cout << 35960;
    for (int i = 0; i < 35960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35960(int& val) {
    const int x = 35960;
    std::cout << 35960;
    for (int i = 0; i < 35960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35961(int& val) {
    func_in_35961(val);
    const int x = 35961;
    std::cout << 35961;
    for (int i = 0; i < 35961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35961(int& val) {
    const int x = 35961;
    std::cout << 35961;
    for (int i = 0; i < 35961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35962(int& val) {
    func_in_35962(val);
    const int x = 35962;
    std::cout << 35962;
    for (int i = 0; i < 35962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35962(int& val) {
    const int x = 35962;
    std::cout << 35962;
    for (int i = 0; i < 35962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35963(int& val) {
    func_in_35963(val);
    const int x = 35963;
    std::cout << 35963;
    for (int i = 0; i < 35963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35963(int& val) {
    const int x = 35963;
    std::cout << 35963;
    for (int i = 0; i < 35963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35964(int& val) {
    func_in_35964(val);
    const int x = 35964;
    std::cout << 35964;
    for (int i = 0; i < 35964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35964(int& val) {
    const int x = 35964;
    std::cout << 35964;
    for (int i = 0; i < 35964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35965(int& val) {
    func_in_35965(val);
    const int x = 35965;
    std::cout << 35965;
    for (int i = 0; i < 35965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35965(int& val) {
    const int x = 35965;
    std::cout << 35965;
    for (int i = 0; i < 35965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35966(int& val) {
    func_in_35966(val);
    const int x = 35966;
    std::cout << 35966;
    for (int i = 0; i < 35966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35966(int& val) {
    const int x = 35966;
    std::cout << 35966;
    for (int i = 0; i < 35966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35967(int& val) {
    func_in_35967(val);
    const int x = 35967;
    std::cout << 35967;
    for (int i = 0; i < 35967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35967(int& val) {
    const int x = 35967;
    std::cout << 35967;
    for (int i = 0; i < 35967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35968(int& val) {
    func_in_35968(val);
    const int x = 35968;
    std::cout << 35968;
    for (int i = 0; i < 35968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35968(int& val) {
    const int x = 35968;
    std::cout << 35968;
    for (int i = 0; i < 35968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35969(int& val) {
    func_in_35969(val);
    const int x = 35969;
    std::cout << 35969;
    for (int i = 0; i < 35969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35969(int& val) {
    const int x = 35969;
    std::cout << 35969;
    for (int i = 0; i < 35969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35970(int& val) {
    func_in_35970(val);
    const int x = 35970;
    std::cout << 35970;
    for (int i = 0; i < 35970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35970(int& val) {
    const int x = 35970;
    std::cout << 35970;
    for (int i = 0; i < 35970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35971(int& val) {
    func_in_35971(val);
    const int x = 35971;
    std::cout << 35971;
    for (int i = 0; i < 35971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35971(int& val) {
    const int x = 35971;
    std::cout << 35971;
    for (int i = 0; i < 35971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35972(int& val) {
    func_in_35972(val);
    const int x = 35972;
    std::cout << 35972;
    for (int i = 0; i < 35972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35972(int& val) {
    const int x = 35972;
    std::cout << 35972;
    for (int i = 0; i < 35972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35973(int& val) {
    func_in_35973(val);
    const int x = 35973;
    std::cout << 35973;
    for (int i = 0; i < 35973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35973(int& val) {
    const int x = 35973;
    std::cout << 35973;
    for (int i = 0; i < 35973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35974(int& val) {
    func_in_35974(val);
    const int x = 35974;
    std::cout << 35974;
    for (int i = 0; i < 35974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35974(int& val) {
    const int x = 35974;
    std::cout << 35974;
    for (int i = 0; i < 35974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35975(int& val) {
    func_in_35975(val);
    const int x = 35975;
    std::cout << 35975;
    for (int i = 0; i < 35975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35975(int& val) {
    const int x = 35975;
    std::cout << 35975;
    for (int i = 0; i < 35975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35976(int& val) {
    func_in_35976(val);
    const int x = 35976;
    std::cout << 35976;
    for (int i = 0; i < 35976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35976(int& val) {
    const int x = 35976;
    std::cout << 35976;
    for (int i = 0; i < 35976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35977(int& val) {
    func_in_35977(val);
    const int x = 35977;
    std::cout << 35977;
    for (int i = 0; i < 35977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35977(int& val) {
    const int x = 35977;
    std::cout << 35977;
    for (int i = 0; i < 35977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35978(int& val) {
    func_in_35978(val);
    const int x = 35978;
    std::cout << 35978;
    for (int i = 0; i < 35978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35978(int& val) {
    const int x = 35978;
    std::cout << 35978;
    for (int i = 0; i < 35978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35979(int& val) {
    func_in_35979(val);
    const int x = 35979;
    std::cout << 35979;
    for (int i = 0; i < 35979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35979(int& val) {
    const int x = 35979;
    std::cout << 35979;
    for (int i = 0; i < 35979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35980(int& val) {
    func_in_35980(val);
    const int x = 35980;
    std::cout << 35980;
    for (int i = 0; i < 35980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35980(int& val) {
    const int x = 35980;
    std::cout << 35980;
    for (int i = 0; i < 35980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35981(int& val) {
    func_in_35981(val);
    const int x = 35981;
    std::cout << 35981;
    for (int i = 0; i < 35981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35981(int& val) {
    const int x = 35981;
    std::cout << 35981;
    for (int i = 0; i < 35981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35982(int& val) {
    func_in_35982(val);
    const int x = 35982;
    std::cout << 35982;
    for (int i = 0; i < 35982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35982(int& val) {
    const int x = 35982;
    std::cout << 35982;
    for (int i = 0; i < 35982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35983(int& val) {
    func_in_35983(val);
    const int x = 35983;
    std::cout << 35983;
    for (int i = 0; i < 35983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35983(int& val) {
    const int x = 35983;
    std::cout << 35983;
    for (int i = 0; i < 35983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35984(int& val) {
    func_in_35984(val);
    const int x = 35984;
    std::cout << 35984;
    for (int i = 0; i < 35984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35984(int& val) {
    const int x = 35984;
    std::cout << 35984;
    for (int i = 0; i < 35984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35985(int& val) {
    func_in_35985(val);
    const int x = 35985;
    std::cout << 35985;
    for (int i = 0; i < 35985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35985(int& val) {
    const int x = 35985;
    std::cout << 35985;
    for (int i = 0; i < 35985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35986(int& val) {
    func_in_35986(val);
    const int x = 35986;
    std::cout << 35986;
    for (int i = 0; i < 35986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35986(int& val) {
    const int x = 35986;
    std::cout << 35986;
    for (int i = 0; i < 35986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35987(int& val) {
    func_in_35987(val);
    const int x = 35987;
    std::cout << 35987;
    for (int i = 0; i < 35987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35987(int& val) {
    const int x = 35987;
    std::cout << 35987;
    for (int i = 0; i < 35987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35988(int& val) {
    func_in_35988(val);
    const int x = 35988;
    std::cout << 35988;
    for (int i = 0; i < 35988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35988(int& val) {
    const int x = 35988;
    std::cout << 35988;
    for (int i = 0; i < 35988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35989(int& val) {
    func_in_35989(val);
    const int x = 35989;
    std::cout << 35989;
    for (int i = 0; i < 35989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35989(int& val) {
    const int x = 35989;
    std::cout << 35989;
    for (int i = 0; i < 35989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35990(int& val) {
    func_in_35990(val);
    const int x = 35990;
    std::cout << 35990;
    for (int i = 0; i < 35990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35990(int& val) {
    const int x = 35990;
    std::cout << 35990;
    for (int i = 0; i < 35990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35991(int& val) {
    func_in_35991(val);
    const int x = 35991;
    std::cout << 35991;
    for (int i = 0; i < 35991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35991(int& val) {
    const int x = 35991;
    std::cout << 35991;
    for (int i = 0; i < 35991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35992(int& val) {
    func_in_35992(val);
    const int x = 35992;
    std::cout << 35992;
    for (int i = 0; i < 35992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35992(int& val) {
    const int x = 35992;
    std::cout << 35992;
    for (int i = 0; i < 35992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35993(int& val) {
    func_in_35993(val);
    const int x = 35993;
    std::cout << 35993;
    for (int i = 0; i < 35993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35993(int& val) {
    const int x = 35993;
    std::cout << 35993;
    for (int i = 0; i < 35993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35994(int& val) {
    func_in_35994(val);
    const int x = 35994;
    std::cout << 35994;
    for (int i = 0; i < 35994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35994(int& val) {
    const int x = 35994;
    std::cout << 35994;
    for (int i = 0; i < 35994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35995(int& val) {
    func_in_35995(val);
    const int x = 35995;
    std::cout << 35995;
    for (int i = 0; i < 35995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35995(int& val) {
    const int x = 35995;
    std::cout << 35995;
    for (int i = 0; i < 35995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35996(int& val) {
    func_in_35996(val);
    const int x = 35996;
    std::cout << 35996;
    for (int i = 0; i < 35996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35996(int& val) {
    const int x = 35996;
    std::cout << 35996;
    for (int i = 0; i < 35996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35997(int& val) {
    func_in_35997(val);
    const int x = 35997;
    std::cout << 35997;
    for (int i = 0; i < 35997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35997(int& val) {
    const int x = 35997;
    std::cout << 35997;
    for (int i = 0; i < 35997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35998(int& val) {
    func_in_35998(val);
    const int x = 35998;
    std::cout << 35998;
    for (int i = 0; i < 35998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35998(int& val) {
    const int x = 35998;
    std::cout << 35998;
    for (int i = 0; i < 35998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35999(int& val) {
    func_in_35999(val);
    const int x = 35999;
    std::cout << 35999;
    for (int i = 0; i < 35999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35999(int& val) {
    const int x = 35999;
    std::cout << 35999;
    for (int i = 0; i < 35999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
